---
phase: 05-tcp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Sequence/Sequence/SequenceBase.cs
  - WPF_Example/Custom/SystemHandler.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - WPF_Example/Device/IAxisController.cs
autonomous: true
requirements: [SEQ-01, SEQ-02]
must_haves:
  truths:
    - "StartAll() 호출 시 모든 Action이 순차 실행된다"
    - "각 Action 완료 후 다음 Action으로 자동 진행된다"
    - "MoveZ 스텝에서 SIMUL_MODE일 때 SimulImagePath 이미지를 로드한다"
    - "IsDynamicFAIMode일 때 ProcessTest가 StartAll을 호출한다"
  artifacts:
    - path: "WPF_Example/Sequence/Sequence/SequenceBase.cs"
      provides: "StartAll() 메서드, ExecuteAction() 다음 Action 진행 로직"
      contains: "StartAll"
    - path: "WPF_Example/Custom/SystemHandler.cs"
      provides: "ProcessTest IsDynamicFAIMode 분기"
      contains: "IsDynamicFAIMode"
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "MoveZ 스텝, SimulImagePath 이미지 로드"
      contains: "MoveZ"
    - path: "WPF_Example/Device/IAxisController.cs"
      provides: "Z축 이동 인터페이스 스텁"
      contains: "IAxisController"
  key_links:
    - from: "Custom/SystemHandler.cs ProcessTest()"
      to: "SequenceBase.StartAll()"
      via: "seq.StartAll(packet)"
      pattern: "seq\\.StartAll"
    - from: "SequenceBase.ExecuteAction()"
      to: "Actions[CurrentActionIndex]"
      via: "CurrentActionIndex++ 후 CurAction 교체"
      pattern: "CurrentActionIndex\\+\\+"
---

<objective>
시퀀스 프레임워크를 확장하여 다중 Action 순차 실행, Z축 이동 스텝, SIMUL_MODE 이미지 로드를 구현한다.

Purpose: 검사 시작 시 모든 Shot의 Action이 순서대로 실행되고, 각 Shot 위치에서 Z축 이동 후 카메라 Grab이 수행되는 기반을 만든다.
Output: SequenceBase.StartAll(), ExecuteAction 다음 Action 진행, Action_FAIMeasurement MoveZ 스텝, IAxisController 인터페이스
</objective>

<execution_context>
@C:\Users\tech\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\tech\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tcp/05-CONTEXT.md

<interfaces>
<!-- SequenceBase 현재 구조 — executor가 수정할 핵심 메서드 -->

From WPF_Example/Sequence/Sequence/SequenceBase.cs:
```csharp
// 현재 Start 메서드 (단일 Action 실행)
protected bool Start(int actionIndex = 0) {
    if (State != EContextState.Idle) return false;
    CurrentActionIndex = actionIndex;
    EndActionIndex = actionIndex;  // ← 단일 Action만 실행
    Context.Clear();
    Command = ESequenceCommmand.Start;
    IsFinished = false;
    OnStart?.Invoke(Context);
    return true;
}

// ExecuteAction — Finish 분기에 다음 Action 진행 로직 없음
private void ExecuteAction(ActionBase action) {
    // ... OnBegin, Run 호출 ...
    else if (actionContext.State == EContextState.Finish) {
        CurAction.OnEnd();
        IsDoneBegin = false;
        Context.CopyFrom(actionContext);
        OnActionChanged?.Invoke(actionContext);
        if (CurrentActionIndex >= EndActionIndex) {
            Context.Result = actionContext.Result;
            Finish();
        }
        // ← 여기에 else { CurrentActionIndex++; CurAction = Actions[CurrentActionIndex]; } 추가 필요
    }
}

// 기존 Start(TestPacket) — GetIndexOf로 Action명 매칭
public bool Start(TestPacket packet) {
    RequestPacket = packet;
    string actName = packet.Identifier2;
    int actionIndex = GetIndexOf(actName);
    if(actionIndex == -1) return false;
    return Start(actionIndex);
}
```

From WPF_Example/Custom/SystemHandler.cs:
```csharp
// ProcessTest — 현재 단순 Start 호출
private bool ProcessTest(TestPacket packet) {
    return Sequences.Start(packet);
}
```

From WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs:
```csharp
private enum EStep { Init, Grab, Measure, End }

// ShotParam에서 SimulImagePath, ZPosition, DelayMs 접근 가능
public ShotConfig ShotParam => Param as ShotConfig;
```

From WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs:
```csharp
public double ZPosition { get; set; }
public int DelayMs { get; set; }
public string SimulImagePath { get; set; } = "";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SequenceBase StartAll() 추가 + ExecuteAction 다음 Action 진행</name>
  <files>WPF_Example/Sequence/Sequence/SequenceBase.cs</files>
  <read_first>
    - WPF_Example/Sequence/Sequence/SequenceBase.cs (현재 Start/ExecuteAction 구조 확인)
    - WPF_Example/Custom/Sequence/SequenceHandler.cs (IsDynamicFAIMode, RebuildInspectionActions 패턴 확인)
  </read_first>
  <action>
SequenceBase.cs에 두 가지 변경을 수행한다.

**1. StartAll(TestPacket) 메서드 추가 (per D-01)**

`Start(int actionIndex)` 메서드 바로 아래에 다음 메서드를 추가한다:

```csharp
//260409 hbk Phase 5: 모든 Action 순차 실행 (D-01)
public bool StartAll(TestPacket packet) {
    if (State != EContextState.Idle) return false;
    if (Actions == null || Actions.Length == 0) return false;

    RequestPacket = packet;
    CurrentActionIndex = 0;
    EndActionIndex = Actions.Length - 1;

    Context.Clear();
    Command = ESequenceCommmand.Start;
    IsFinished = false;

    OnStart?.Invoke(Context);
    return true;
}
```

**2. ExecuteAction()에서 다음 Action 진행 로직 추가 (per D-02)**

`ExecuteAction` 메서드의 `else if (actionContext.State == EContextState.Finish)` 블록 내부에서, 기존 `if (CurrentActionIndex >= EndActionIndex)` 블록 뒤에 `else` 분기를 추가한다:

현재 코드:
```csharp
if (CurrentActionIndex >= EndActionIndex) {
    Context.Result = actionContext.Result;
    Finish();
}
```

변경 후:
```csharp
if (CurrentActionIndex >= EndActionIndex) {
    Context.Result = actionContext.Result;
    Finish();
} else {
    //260409 hbk Phase 5: 다음 Action으로 진행 (D-02)
    CurrentActionIndex++;
    CurAction = Actions[CurrentActionIndex];
}
```

주의: `OnActionChanged?.Invoke(actionContext);` 호출은 이미 위쪽에서 수행되므로 else 블록에서 중복 호출하지 않는다. 이벤트 발생 후 다음 Action으로 넘어가는 순서가 보장된다.
  </action>
  <verify>
    <automated>cd C:/Info/Project/DataMeasurement && grep -n "StartAll" WPF_Example/Sequence/Sequence/SequenceBase.cs && grep -n "CurrentActionIndex++" WPF_Example/Sequence/Sequence/SequenceBase.cs</automated>
  </verify>
  <acceptance_criteria>
    - grep "public bool StartAll(TestPacket packet)" SequenceBase.cs 결과가 1줄 존재
    - grep "EndActionIndex = Actions.Length - 1" SequenceBase.cs 결과가 StartAll 내부에 존재
    - grep "CurrentActionIndex++" SequenceBase.cs 결과가 ExecuteAction 내부에 존재
    - grep "CurAction = Actions\[CurrentActionIndex\]" SequenceBase.cs 결과가 else 블록에 존재
    - 기존 Start(int), Start(TestPacket), Start(EAction) 메서드는 수정 없이 유지
  </acceptance_criteria>
  <done>StartAll()이 모든 Action을 순차 실행하도록 EndActionIndex를 Actions.Length-1로 설정하고, ExecuteAction()이 Action 완료 시 다음 Action으로 진행한다</done>
</task>

<task type="auto">
  <name>Task 2: ProcessTest IsDynamicFAIMode 분기 + Action_FAIMeasurement MoveZ + IAxisController 스텁</name>
  <files>WPF_Example/Custom/SystemHandler.cs, WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs, WPF_Example/Device/IAxisController.cs</files>
  <read_first>
    - WPF_Example/Custom/SystemHandler.cs (ProcessTest 현재 구현 확인)
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (EStep enum, Run() 확인)
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs (ZPosition, DelayMs, SimulImagePath 필드)
    - WPF_Example/Custom/TcpServer/ResourceMap.cs (ESite, EResource.Sequence 매핑 확인)
    - WPF_Example/Device/Camera/VirtualCamera.cs (GrabHalconImage 패턴 참조)
  </read_first>
  <action>
3개 파일을 수정/생성한다.

**1. IAxisController.cs 생성 (per D-09)**

`WPF_Example/Device/IAxisController.cs`에 Z축 이동 인터페이스를 생성한다:

```csharp
//260409 hbk Phase 5: Z축 이동 인터페이스 스텁 (D-09)
namespace ReringProject.Device {

    /// <summary>
    /// Z축 모터 제어 인터페이스. 실제 HW 연동은 별도 Phase에서 구현.
    /// </summary>
    public interface IAxisController {
        bool MoveToPosition(double positionMm);
        bool IsMoveDone { get; }
        double CurrentPosition { get; }
    }
}
```

DatumMeasurement.csproj에 이 파일을 `<Compile Include>` 로 추가한다. 기존 `<Compile Include="Device\DeviceHandler.cs" />` 근처에 `<Compile Include="Device\IAxisController.cs" />` 를 추가.

**2. ProcessTest() IsDynamicFAIMode 분기 추가 (per D-03)**

`Custom/SystemHandler.cs`의 `ProcessTest(TestPacket packet)` 메서드를 다음으로 교체한다:

```csharp
//260409 hbk Phase 5: IsDynamicFAIMode 분기 (D-03)
private bool ProcessTest(TestPacket packet) {
    if (Sequences.IsDynamicFAIMode) {
        string seqName = ResourceMap.Find(EResource.Sequence, (ESite)packet.Site);
        SequenceBase seq = Sequences[seqName];
        if (seq == null) return false;
        return seq.StartAll(packet);
    }
    return Sequences.Start(packet);
}
```

using 문에 `using ReringProject.Network;`가 이미 있는지 확인. `ResourceMap.Find`는 static 메서드이므로 직접 호출 가능.

**3. Action_FAIMeasurement에 MoveZ 스텝 추가 (per D-08, D-10, D-11)**

Action_FAIMeasurement.cs를 수정한다:

a) EStep enum에 `MoveZ`를 `Init`과 `Grab` 사이에 추가:
```csharp
private enum EStep {
    Init,
    MoveZ,    //260409 hbk Phase 5: Z축 이동 스텝 (D-08)
    Grab,
    Measure,
    End
}
```

b) using 추가 (파일 상단): `using System.IO;` (File.Exists 사용)

c) `Init` case의 마지막 줄을 `Step = (int)EStep.MoveZ;`로 변경 (기존 `EStep.Grab` 에서 변경)

d) `MoveZ` case를 `Init` case와 `Grab` case 사이에 추가:
```csharp
case EStep.MoveZ:
    //260409 hbk Phase 5: Z축 이동 + SIMUL 이미지 준비 (D-08, D-10, D-11)
    #if SIMUL_MODE
    // SIMUL: Z축 이동 건너뜀, DelayMs 무시
    #else
    // 실 장비: IAxisController 구현 후 연동 예정
    // if (axisController != null) {
    //     axisController.MoveToPosition(ShotParam.ZPosition);
    //     Thread.Sleep(ShotParam.DelayMs);
    // }
    if (ShotParam != null && ShotParam.DelayMs > 0) {
        System.Threading.Thread.Sleep(ShotParam.DelayMs);
    }
    #endif
    Step = (int)EStep.Grab;
    break;
```

e) `Grab` case에서 SimulImagePath 이미지 로드 로직 추가 (per D-10). 기존 `GrabHalconImage` 호출 전에 SIMUL_MODE 분기를 추가:

현재 Grab case:
```csharp
case EStep.Grab:
    if (ShotParam != null && !ShotParam.HasImage) {
        HImage image = SystemHandler.Handle.Devices.GrabHalconImage(ShotParam);
        ...
    }
    Step = (int)EStep.Measure;
    break;
```

변경 후:
```csharp
case EStep.Grab:
    if (ShotParam != null && !ShotParam.HasImage) {
        HImage image = null;
        //260409 hbk Phase 5: SimulImagePath 이미지 로드 (D-10)
        #if SIMUL_MODE
        if (!string.IsNullOrEmpty(ShotParam.SimulImagePath) && File.Exists(ShotParam.SimulImagePath)) {
            try {
                image = new HImage(ShotParam.SimulImagePath);
            } catch {
                image = null;
            }
        }
        if (image == null) {
            image = SystemHandler.Handle.Devices.GrabHalconImage(ShotParam);
        }
        #else
        image = SystemHandler.Handle.Devices.GrabHalconImage(ShotParam);
        #endif
        if (image != null) {
            ShotParam.SetImage(image);
            pMyContext.ResultHalconImage?.Dispose();
            pMyContext.ResultHalconImage = image.CopyImage();
            image.Dispose();
        }
    }
    Step = (int)EStep.Measure;
    break;
```
  </action>
  <verify>
    <automated>cd C:/Info/Project/DataMeasurement && grep -n "IsDynamicFAIMode" WPF_Example/Custom/SystemHandler.cs && grep -n "MoveZ" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs && grep -n "IAxisController" WPF_Example/Device/IAxisController.cs && grep -n "IAxisController" WPF_Example/DatumMeasurement.csproj</automated>
  </verify>
  <acceptance_criteria>
    - grep "Sequences.IsDynamicFAIMode" Custom/SystemHandler.cs 결과 1줄 존재
    - grep "seq.StartAll(packet)" Custom/SystemHandler.cs 결과 1줄 존재
    - grep "Sequences.Start(packet)" Custom/SystemHandler.cs 결과가 else 분기에 유지
    - grep "MoveZ" Action_FAIMeasurement.cs에 EStep enum과 case 블록 양쪽에 존재
    - grep "SimulImagePath" Action_FAIMeasurement.cs에 SIMUL_MODE 분기 내에 존재
    - IAxisController.cs 파일이 존재하며 MoveToPosition, IsMoveDone, CurrentPosition 포함
    - DatumMeasurement.csproj에 Device\IAxisController.cs Compile Include 존재
  </acceptance_criteria>
  <done>ProcessTest가 IsDynamicFAIMode 분기로 StartAll 호출하고, Action_FAIMeasurement가 MoveZ 스텝으로 Z축 이동 후 SimulImagePath 이미지를 로드하며, IAxisController 인터페이스 스텁이 생성된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TCP Client -> ProcessTest | 외부 핸들러가 TestPacket으로 검사 시작 요청 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | S (Spoofing) | ProcessTest() | accept | 기존 TCP 서버의 클라이언트 인증 메커니즘 유지. 내부 네트워크 전용 시스템. |
| T-05-02 | T (Tampering) | TestPacket.Site | mitigate | ResourceMap.Find가 유효하지 않은 ESite에 대해 null 반환 → seq == null 체크로 false 반환 (기존 패턴 유지) |
| T-05-03 | D (DoS) | StartAll() | mitigate | State != Idle 체크로 중복 실행 방지. Actions null/empty 체크 추가. |
| T-05-04 | E (Elevation) | IAxisController | accept | 인터페이스 스텁만 정의, 실제 HW 연동 없음. 구현 시 재평가. |
</threat_model>

<verification>
1. SequenceBase.cs에 StartAll 메서드 존재 확인
2. ExecuteAction에서 CurrentActionIndex++ 로직 존재 확인
3. ProcessTest에 IsDynamicFAIMode 분기 존재 확인
4. Action_FAIMeasurement EStep에 MoveZ 존재 확인
5. IAxisController.cs 파일 존재 확인
6. 빌드: MSBuild DatumMeasurement.csproj 성공 (SIMUL_MODE)
</verification>

<success_criteria>
- StartAll(TestPacket)이 EndActionIndex = Actions.Length - 1로 설정하여 모든 Action 실행
- ExecuteAction이 Action 완료 후 CurrentActionIndex < EndActionIndex일 때 다음 Action으로 진행
- ProcessTest가 IsDynamicFAIMode true일 때 seq.StartAll(packet) 호출
- Action_FAIMeasurement가 Init -> MoveZ -> Grab -> Measure -> End 순서로 실행
- SIMUL_MODE에서 SimulImagePath 이미지 로드 가능
</success_criteria>

<output>
After completion, create `.planning/phases/05-tcp/05-01-SUMMARY.md`
</output>
