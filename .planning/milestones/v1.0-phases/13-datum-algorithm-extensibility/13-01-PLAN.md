---
phase: 13-datum-algorithm-extensibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
autonomous: true
requirements:
  - Req-5d-CircleTwoHorizontal-direction
  - Req-5d-VerticalTwoHorizontal-direction
tags: [datum, algorithm, validation, phase-13]
must_haves:
  truths:
    - "CircleTwoHorizontal 티칭 시 수평 ROI 결합선 phi 가 ±15° 밖이면 TryTeachDatum 이 false 를 반환하고 error 문자열에 'Horizontal line orientation out of range' 가 포함된다"
    - "VerticalTwoHorizontal 티칭 시 수평 결합선 phi 가 ±15° 밖이면 같은 방식으로 실패한다"
    - "CircleTwoHorizontal 티칭 시 수평선과 수직 가상선 사이 각도가 90°±5° 를 벗어나면 'Horizontal/Vertical perpendicularity violated' 를 반환한다"
    - "VerticalTwoHorizontal 티칭 시 검출된 수직 라인과 수평 결합선 각도가 90°±5° 를 벗어나면 같은 방식으로 실패한다"
    - "TwoLineIntersect 경로는 각도 검증을 호출하지 않아 기존 Phase 4/11/12 회귀가 없다"
    - "msbuild Debug/x64 가 exit 0 로 성공하고 DatumFindingService.cs 를 참조하는 신규 warning 이 0 개이다"
  artifacts:
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "private static ValidateHorizontalVerticalAngles helper + 2 const + 2 call-site 연결"
      contains: "ValidateHorizontalVerticalAngles"
  key_links:
    - from: "TryTeachCircleTwoHorizontal success branch"
      to: "ValidateHorizontalVerticalAngles"
      via: "호출 직전 vertPhiCircle = Math.PI / 2.0 계산"
      pattern: "ValidateHorizontalVerticalAngles\\(.*Math.PI \\/ 2"
    - from: "TryTeachVerticalTwoHorizontal success branch"
      to: "ValidateHorizontalVerticalAngles"
      via: "호출 직전 vertPhiDetected = Math.Atan2(vrE-vrB, vcE-vcB) 계산"
      pattern: "ValidateHorizontalVerticalAngles\\(.*Math.Atan2\\(vrE"
---

<objective>
Phase 12 에서 Req 5d (방향 정합성 검사) 가 `// TODO: Phase 13 — 방향 정합성 검사` 리터럴 코멘트로 deferred 되었다. Phase 13 Plan 01 은 이 TODO 를 실제 검증 게이트로 교체한다.

**Purpose:** CircleTwoHorizontal / VerticalTwoHorizontal 알고리즘에서 사용자가 티칭용 수평 ROI 2개를 수평선 기준으로 크게 기울여(±15° 초과) 그리거나, 수평/수직 방향이 직각에 가깝지 않은 비현실적 배치를 그리면 티칭을 실패시켜 잘못된 Datum 저장을 방지한다.

**Output:**
- `WPF_Example/Halcon/Algorithms/DatumFindingService.cs`: `private const double HORIZONTAL_TOLERANCE_DEG = 15.0;` + `private const double PERPENDICULAR_TOLERANCE_DEG = 5.0;` 상수 2개 추가 (L17 `MIN_HORIZONTAL_EDGES` 아래)
- 같은 파일: `private static bool ValidateHorizontalVerticalAngles(double horizPhiRad, double vertPhiRad, out string error)` helper 신규
- `TryTeachCircleTwoHorizontal` 의 `// TODO: Phase 13 — 방향 정합성 검사` 주석 (L377) 제거 + helper 호출 블록으로 교체 (수직 phi = `Math.PI / 2.0`)
- `TryTeachVerticalTwoHorizontal` 의 `LastTeachSucceeded = true` 직후 (L520 근처) helper 호출 블록 추가 (수직 phi = `Math.Atan2(vrE - vrB, vcE - vcB)`)
- msbuild Debug/x64 성공 + DatumFindingService.cs 대상 신규 warning 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-datum-algorithm-extensibility/13-CONTEXT.md
@.planning/phases/13-datum-algorithm-extensibility/13-PATTERNS.md
@.planning/phases/12-datum-circle-vertical-horizontal-intersection/12-SPEC.md
@.planning/phases/12-datum-circle-vertical-horizontal-intersection/12-02-SUMMARY.md
@WPF_Example/Halcon/Algorithms/DatumFindingService.cs
@CLAUDE.md

<interfaces>
<!-- 이 Plan 은 DatumFindingService.cs 내부만 수정. 외부 시그니처 변경 없음. -->
<!-- Phase 12 Plan 02 가 확정한 3-way dispatch 구조를 그대로 유지하고 success branch 말미에만 추가. -->

From WPF_Example/Halcon/Algorithms/DatumFindingService.cs (Phase 12 Plan 02 확정):
```csharp
public class DatumFindingService
{
    private const int MIN_HORIZONTAL_EDGES = 10;                          // L17
    public bool TryFindDatum(HImage, DatumConfig, out HTuple transform, out string error);  // L28 — UNTOUCHED (런타임)
    public bool TryTeachDatum(HImage, DatumConfig, out string error);     // L127 — dispatch, UNTOUCHED
    private bool TryTeachTwoLineIntersect(HImage, DatumConfig, out string error);    // L151 — UNTOUCHED (검증 없음)
    private bool TryTeachCircleTwoHorizontal(HImage, DatumConfig, out string error); // L244 — 말미에 검증 추가
    private bool TryTeachVerticalTwoHorizontal(HImage, DatumConfig, out string error);// L396 — 말미에 검증 추가
    private bool TryFindLine(...);                                        // L542 — UNTOUCHED
    private bool TryExtractEdgePoints(...);                               // L616 — UNTOUCHED
}
```

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (Phase 12 Plan 01 확정, 참고 전용):
```csharp
public EDatumAlgorithm AlgorithmTypeEnum { get; }  // L57 — 알고리즘 식별
public double RefAngleRad { get; set; }             // 수평 결합선 방향각 (success branch 직전에 이미 대입됨)
public bool LastTeachSucceeded { get; set; }        // L164 — 검증 실패 시 false 로 되돌림
```

**현재 Phase 12 Plan 02 success branch 꼬리 코드:**
- CircleTwoHorizontal (L358-378): RefOrigin/RefAngle 대입 → Line1/Line2 Detected 대입 → LastTeachSucceeded=true → **L377 TODO 주석** → return true.
- VerticalTwoHorizontal (L505-522): RefOrigin/RefAngle 대입 → Line1/Line2 Detected 대입 → LastTeachSucceeded=true → return true (TODO 주석 없음, Phase 13 에서 추가 삽입).
</interfaces>

**Brace style (PATTERNS.md § File Classification):** `DatumFindingService.cs` = **Allman**. 신규 helper / 호출 블록 모두 Allman 스타일. `if (...) { ... }` 한 줄 금지.

**C# 7.2 제약:**
- `$"..."` interpolated string **사용 가능** (Phase 12 기존 코드에서도 사용하지 않았으나 C# 6 부터 가능). **단 이 파일의 관례는 `+` concat** — Phase 12 에서도 `error = "Horizontal line fit failed: " + edgeErrorA;` 형식. **이 Plan 에서도 `+` concat 유지** (일관성).
- `out var` 허용 (C# 7.0), `out string error` 허용 (C# 7.0).
- `switch expression`, `record`, `nullable reference types` 금지.

**주석 convention (CLAUDE.md + 13-CONTEXT.md § code_context):** 모든 신규/수정 라인에 `//260424 hbk Phase 13 D-09..D-12 — <intent>` 를 붙인다. 여러 D 가 걸치는 상수/helper 는 `D-09..D-12` 로 요약 가능.

**SPEC AC 에러 리터럴 (13-CONTEXT.md L78 D-11):**
- 수평 phi out of range: `"Horizontal line orientation out of range: XX.X° (expected ±15.0°)"` — 구체 포맷은 아래 action 블록 참조.
- 수직 교차각 out of range: `"Horizontal/Vertical perpendicularity violated: delta=XX.X° (expected 90°±5.0°)"` — 구체 포맷은 아래 action 블록 참조.

PATTERNS.md §4 의 코드 스케치를 **action 블록 그대로** 사용. 에러 메시지에 숫자 포맷은 `.ToString("F1")` 으로 소수점 1자리.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ValidateHorizontalVerticalAngles helper + 2 constants</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs L1-20 (namespace + class 선언 + MIN_HORIZONTAL_EDGES L17)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs L536-550 (`private bool TryFindLine` 선언 부근 — 이 helper 와 동일 위치/스타일로 새 helper 삽입 위치 결정)
    - .planning/phases/13-datum-algorithm-extensibility/13-PATTERNS.md §4 (완전 스니펫 411-448)
  </read_first>
  <action>
    1. **상수 2개 추가** — L17 `private const int MIN_HORIZONTAL_EDGES = 10;` **바로 다음 줄**에 두 상수를 삽입:

    ```csharp
            //260424 hbk Phase 13 D-10 — Req 5d 방향 정합성 임계각 (고정값; 사용자 튜닝 필요해지면 DatumConfig 필드화 Deferred)
            private const double HORIZONTAL_TOLERANCE_DEG    = 15.0;
            private const double PERPENDICULAR_TOLERANCE_DEG = 5.0;
    ```

    2. **private static helper 추가** — `TryFindLine` 선언 직전 (L541 `/// <summary>` 주석 위) 에 아래 helper 를 삽입. Allman brace 엄수. `out string error` 반환.

    ```csharp
            //260424 hbk Phase 13 D-09..D-12 — 수평선 phi 방향 + 수평/수직 직각성 검증 게이트
            //  CircleTwoHorizontal / VerticalTwoHorizontal 공통. TwoLineIntersect 는 무효(호출 안 함).
            //  horizPhiRad = 수평 결합선 Atan2, vertPhiRad = 수직 가상선(Circle) 또는 검출 수직선(Vertical) Atan2.
            //  실패 시 error 에 SPEC AC 리터럴 반환. NaN/Infinity 입력은 수평 체크에서 자연히 걸러짐(Abs > tol).
            private static bool ValidateHorizontalVerticalAngles(
                double horizPhiRad, double vertPhiRad, out string error)
            {
                //260424 hbk Phase 13 D-09 — 수평 phi 를 [-90°, +90°] 로 normalize 후 절댓값 비교
                double horizDeg = Math.Abs(horizPhiRad * 180.0 / Math.PI);
                if (horizDeg > 90.0) horizDeg = 180.0 - horizDeg;
                if (horizDeg > HORIZONTAL_TOLERANCE_DEG)
                {
                    error = "Horizontal line orientation out of range: "
                          + horizDeg.ToString("F1")
                          + "° (expected ±"
                          + HORIZONTAL_TOLERANCE_DEG.ToString("F1")
                          + "°)"; //260424 hbk Phase 13 D-11 SPEC AC literal (Req 5d)
                    return false;
                }

                //260424 hbk Phase 13 D-09 — 수평선과 수직선 사이 각도 = |phi_h − phi_v| 를 [0°, 180°) 로 정규화 → 90° 와의 편차
                double deltaDeg = Math.Abs((horizPhiRad - vertPhiRad) * 180.0 / Math.PI);
                while (deltaDeg >= 180.0) deltaDeg -= 180.0;
                double perpErr = Math.Abs(deltaDeg - 90.0);
                if (perpErr > PERPENDICULAR_TOLERANCE_DEG)
                {
                    error = "Horizontal/Vertical perpendicularity violated: delta="
                          + deltaDeg.ToString("F1")
                          + "° (expected 90°±"
                          + PERPENDICULAR_TOLERANCE_DEG.ToString("F1")
                          + "°)"; //260424 hbk Phase 13 D-11 SPEC AC literal (Req 5d)
                    return false;
                }

                error = null;
                return true;
            }
    ```

    3. 이 Task 는 helper 정의만 추가한다. 호출 연결은 Task 2/3 에서 수행.

    4. **중요:** `±` 는 `±` 문자의 Unicode escape (C# 7.2 UTF-8 소스 이슈 회피). `°` 는 `°`. 소스 인코딩 문제가 없다면 직접 `±` / `°` 를 써도 무방하나 이 파일은 UTF-8 without BOM 이 표준이라 escape 사용 권장 — 단, 기존 Phase 12 에러 리터럴(`"Horizontal line fit failed: ..."`) 은 ASCII 만 사용하므로 우회 필요. 만약 `°` escape 가 grep 가독성을 해치면 **대안으로 `" (expected +/-15.0 deg)"`** 같은 ASCII-only 포맷도 허용 (단 에러 literal 의 "expected" prefix 와 숫자는 유지). 이 대안을 쓸 경우 must_haves error 문자열 contains 체크도 이에 맞춰 grep 하되, **"Horizontal line orientation out of range"** 와 **"Horizontal/Vertical perpendicularity violated"** 라는 prefix 는 **반드시 그대로** 사용.

    5. msbuild Debug/x64 빌드 실행:
    ```bash
    msbuild WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal
    ```
    exit 0 확인. DatumFindingService.cs 대상 신규 warning 0 확인.

    6. 커밋: `feat(phase-13-01): add ValidateHorizontalVerticalAngles helper + tolerance constants`
  </action>
  <verify>
    <automated>
      (1) grep -cF "private const double HORIZONTAL_TOLERANCE_DEG" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 1
      (2) grep -cF "private const double PERPENDICULAR_TOLERANCE_DEG" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 1
      (3) grep -cE "private static bool ValidateHorizontalVerticalAngles" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 1
      (4) grep -cF "Horizontal line orientation out of range:" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 1
      (5) grep -cF "Horizontal/Vertical perpendicularity violated:" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 1
      (6) msbuild WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal → exit 0
    </automated>
  </verify>
  <done>
    - 2 상수 + 1 helper 추가 완료
    - msbuild Debug/x64 exit 0
    - DatumFindingService.cs 에 신규 warning 0 (VirtualCamera.cs / VisionAlgorithmService.cs 기존 경고 제외)
    - git commit 완료
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire validation into TryTeachCircleTwoHorizontal</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs L355-395 (CircleTwoHorizontal success branch — L358-376 기준값 저장 + L377 TODO 주석 + L378 return true + L380-391 catch/finally)
    - .planning/phases/13-datum-algorithm-extensibility/13-PATTERNS.md §4 L452-466 (호출 블록 스니펫)
  </read_first>
  <action>
    1. **L377 의 `// TODO: Phase 13 — 방향 정합성 검사 ...` 주석 1줄 삭제**.

    2. **`return true;` (L378) 바로 위에** 아래 블록 삽입:

    ```csharp
                    //260424 hbk Phase 13 D-09..D-12 — Req 5d 방향 정합성 검증 (CircleTwoHorizontal: 수직 가상선 phi = π/2 고정)
                    double vertPhiCircle = Math.PI / 2.0; // 수직 가상선은 col=const 이므로 Atan2(1,0)=π/2
                    string angleError;
                    if (!ValidateHorizontalVerticalAngles(config.RefAngleRad, vertPhiCircle, out angleError))
                    {
                        config.LastTeachSucceeded = false;
                        error = angleError;
                        return false;
                    }
    ```

    **주의 — 배치 순서:**
    - `config.RefAngleRad` 대입 (L361) 과 `config.LastTeachSucceeded = true;` (L375) 이후, `return true;` 직전에 삽입해야 함. 현재 순서대로라면 L376 (주석 삭제 위치) 자리에 블록이 들어감.
    - 검증 실패 시 `config.LastTeachSucceeded = false;` 로 되돌리는 것이 중요 — 직전 L375 에서 true 로 set 되었음.
    - `error = angleError;` 로 helper 의 SPEC AC 리터럴을 그대로 호출자에게 전달.

    3. **들여쓰기:** try 블록 내부 (8 spaces × 2 = 16 spaces). PATTERNS.md §4 스니펫과 동일.

    4. **Brace:** Allman. `if (!ValidateHorizontalVerticalAngles(...))` 다음 줄에 `{` — 한 줄 if 금지.

    5. msbuild Debug/x64 빌드 실행 + exit 0 확인.

    6. 커밋: `feat(phase-13-01): wire direction validation into TryTeachCircleTwoHorizontal`
  </action>
  <verify>
    <automated>
      (1) grep -cF "// TODO: Phase 13 — 방향 정합성 검사" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 0 (기존 TODO 제거 확인)
      (2) grep -cF "double vertPhiCircle = Math.PI / 2.0" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 1
      (3) grep -cF "ValidateHorizontalVerticalAngles(config.RefAngleRad, vertPhiCircle" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 1
      (4) awk '/private bool TryTeachCircleTwoHorizontal/,/^        }$/' WPF_Example/Halcon/Algorithms/DatumFindingService.cs | grep -c "ValidateHorizontalVerticalAngles" → 1 (CircleTwoHorizontal 범위 안에만 1건)
      (5) msbuild WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal → exit 0
    </automated>
  </verify>
  <done>
    - L377 TODO 주석 제거 + 검증 블록 삽입 완료
    - 검증 실패 시 LastTeachSucceeded=false + error 전달 확인 (코드 리뷰)
    - msbuild Debug/x64 exit 0, 신규 warning 0
    - git commit 완료
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire validation into TryTeachVerticalTwoHorizontal</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs L485-525 (VerticalTwoHorizontal success branch — L485-488 IntersectionLl 입력(vrB/vcB/vrE/vcE) + L505-520 기준값 저장 + L520 LastTeachSucceeded=true + L522 return true)
    - .planning/phases/13-datum-algorithm-extensibility/13-PATTERNS.md §4 L468-482 (호출 블록 스니펫)
  </read_first>
  <action>
    1. **`return true;` (L522 근처) 바로 위에** 아래 블록 삽입. Task 2 와 달리 이 경로는 TODO 주석이 없으므로 제거할 것 없음.

    ```csharp
                    //260424 hbk Phase 13 D-09..D-12 — Req 5d 방향 정합성 검증 (VerticalTwoHorizontal: 수직 phi 는 검출된 수직 라인 Atan2)
                    double vertPhiDetected = Math.Atan2(vrE - vrB, vcE - vcB);
                    string angleError;
                    if (!ValidateHorizontalVerticalAngles(config.RefAngleRad, vertPhiDetected, out angleError))
                    {
                        config.LastTeachSucceeded = false;
                        error = angleError;
                        return false;
                    }
    ```

    **변수 참조 확인:**
    - `vrB`, `vcB`, `vrE`, `vcE` 는 L410 의 `TryFindLine` out 파라미터로 이미 선언된 double. L485-488 IntersectionLl 호출 직전까지 scope 내. L505-522 기준값 저장 블록에서도 접근 가능 (같은 try 블록).
    - `config.RefAngleRad` 는 L508 에서 `Math.Atan2(hrE.D - hrB.D, hcE.D - hcB.D)` 로 이미 대입됨 (수평 결합선 방향각).

    **배치 순서:**
    - `config.LastTeachSucceeded = true;` (L520) 와 `return true;` (L522) 사이에 삽입.
    - Task 2 와 동일 — 검증 실패 시 LastTeachSucceeded 를 false 로 되돌리는 것이 핵심.

    2. **들여쓰기:** 16 spaces (try 내부). Allman brace.

    3. msbuild Debug/x64 빌드 실행 + exit 0 확인.

    4. 빌드 후 자체 점검 (코드 리뷰):
       - TwoLineIntersect (TryTeachTwoLineIntersect L151-241) 에는 ValidateHorizontalVerticalAngles 호출이 **0건** 이어야 함 (D-12 scope 제한).
       - CircleTwoHorizontal / VerticalTwoHorizontal 에는 각각 **1건** 호출 — 총 **2건**.
       - public TryFindDatum (L28-115, 런타임 경로) 에는 호출 0건 (Phase 12 SPEC Out-of-scope 유지).

    5. 커밋: `feat(phase-13-01): wire direction validation into TryTeachVerticalTwoHorizontal`
  </action>
  <verify>
    <automated>
      (1) grep -cF "Math.Atan2(vrE - vrB, vcE - vcB)" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 1
      (2) grep -cF "ValidateHorizontalVerticalAngles(config.RefAngleRad, vertPhiDetected" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 1
      (3) grep -cF "ValidateHorizontalVerticalAngles(" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 3 (1 helper 정의 + 2 호출)
      (4) awk '/private bool TryTeachTwoLineIntersect/,/^        }$/' WPF_Example/Halcon/Algorithms/DatumFindingService.cs | grep -c "ValidateHorizontalVerticalAngles" → 0 (TwoLineIntersect 경로는 호출 없음 — D-12 scope 제한)
      (5) awk '/public bool TryFindDatum/,/^        }$/' WPF_Example/Halcon/Algorithms/DatumFindingService.cs | grep -c "ValidateHorizontalVerticalAngles" → 0 (런타임 경로 untouched)
      (6) msbuild WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal → exit 0
    </automated>
  </verify>
  <done>
    - 검증 블록 삽입 완료, Circle+Vertical 2 경로 모두 helper 호출
    - TwoLineIntersect + TryFindDatum 는 untouched (D-12 + D-13 scope)
    - msbuild Debug/x64 exit 0, 신규 warning 0
    - git commit 완료
  </done>
</task>

</tasks>

<verification>
**파일 수준:**
- WPF_Example/Halcon/Algorithms/DatumFindingService.cs 만 수정 (다른 파일 건드림 금지)
- 신규 `public` / `internal` 시그니처 0건 (모두 private static / private const)
- 기존 public `TryFindDatum` / `TryTeachDatum` / 기존 private helpers 시그니처 변경 0건

**수준 검증 (Task 1+2+3 누적):**
```
grep -cF "private const double HORIZONTAL_TOLERANCE_DEG = 15.0"         → 1
grep -cF "private const double PERPENDICULAR_TOLERANCE_DEG = 5.0"        → 1
grep -cF "ValidateHorizontalVerticalAngles("                              → 3 (def 1 + call 2)
grep -cF "Horizontal line orientation out of range:"                     → 1
grep -cF "Horizontal/Vertical perpendicularity violated:"                → 1
grep -cF "// TODO: Phase 13"                                              → 0 (Task 2 에서 제거)
grep -cF "//260424 hbk Phase 13"                                          → ≥ 6 (helper + 2 call site + constants + comments)
```

**빌드:**
- msbuild Debug/x64 exit 0
- DatumFindingService.cs 를 referencing 하는 신규 warning 0 (기존 VirtualCamera.cs CS0162 / VisionAlgorithmService.cs CS0219 / MSB3884 는 Plan 01 scope 밖)

**SIMUL_MODE 검증(본 Plan 자동 verify 스코프 아님 — Phase 수준 UAT 에서 최종 확인):**
- CircleTwoHorizontal 티칭 시 수평 ROI 2개를 눈에 띄게 기울여(20°+) 그리면 "Horizontal line orientation out of range: XX.X°" 에러 발생 (label_drawHint 빨간색)
- VerticalTwoHorizontal 에서도 동일
- 수직 가상선/검출 수직선과 수평 결합선 각도가 90°±5° 범위 내면 성공 (정상 배치)

**회귀 확인:**
- TwoLineIntersect 티칭(기존 Phase 4/11 패턴) 실행 시 기존 에러 리터럴 패턴(`"Line1: "`, `"Line2: "`, `"Lines are collinear (identical)..."`, `"Lines are parallel..."`) 유지, 각도 검증 메시지 0건.
</verification>

<success_criteria>
- [ ] 상수 2개 추가 (`HORIZONTAL_TOLERANCE_DEG=15.0`, `PERPENDICULAR_TOLERANCE_DEG=5.0`)
- [ ] `ValidateHorizontalVerticalAngles` helper 1개 추가 (private static, Allman brace)
- [ ] CircleTwoHorizontal success branch 에서 helper 호출 (수직 phi = π/2) — TODO 주석 교체
- [ ] VerticalTwoHorizontal success branch 에서 helper 호출 (수직 phi = Atan2(vrE−vrB, vcE−vcB))
- [ ] TwoLineIntersect + TryFindDatum 경로에는 호출 0건
- [ ] 검증 실패 시 `LastTeachSucceeded=false` + error 에 SPEC AC 리터럴 포함
- [ ] 주석 convention `//260424 hbk Phase 13 D-09..D-12` 모든 신규 라인 준수
- [ ] msbuild Debug/x64 exit 0 + DatumFindingService.cs 신규 warning 0
- [ ] 3 commit 완료 (Task 1/2/3 각 1건)
</success_criteria>

<output>
After completion, create `.planning/phases/13-datum-algorithm-extensibility/13-01-SUMMARY.md` following the template structure:
- Files Changed 테이블
- Req 5d coverage 매핑 (CircleTwoHorizontal / VerticalTwoHorizontal 각 1건 호출)
- Error-literal 카탈로그 (2개 신규 리터럴)
- Build Result 블록
- Deviations from Plan (없으면 "Plan Adherence: All tasks executed as specified")
- Self-Check 체크리스트
</output>
</content>
</invoke>