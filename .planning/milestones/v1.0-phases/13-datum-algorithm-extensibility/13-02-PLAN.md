---
phase: 13-datum-algorithm-extensibility
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
autonomous: false
requirements:
  - Gap-4-RuntimeFindTestUI
tags: [datum, ui, runtime-test, phase-13]
must_haves:
  truths:
    - "btn_testFindDatum Button 이 MainView.xaml canvas 툴바 StackPanel 내부 btn_teachDatum 바로 옆에 존재한다"
    - "label_testFindResult TextBlock/Label 이 label_drawHint 와 동일 Grid.Column 패턴으로 canvas 툴바에 존재한다"
    - "Datum 이 미티칭(IsConfigured=false 또는 LastTeachSucceeded=false)이면 btn_testFindDatum 이 비활성 혹은 클릭 시 CustomMessageBox 가드로 early return 한다"
    - "BtnTestFindDatum_Click 이 AskTestImageSource 다이얼로그 분기로 현재 이미지 / OpenFileDialog 2 가지 소스를 지원한다"
    - "DatumFindingService.TryFindDatum(HImage, DatumConfig, out HTuple, out string) 호출 결과가 성공이면 label_testFindResult 에 'TryFind OK — RefOrigin=(row:F1, col:F1), Angle=... rad' 가 LimeGreen 으로 표시된다"
    - "실패 시 label_testFindResult 에 'TryFind FAIL — ' + error 가 Red 로 표시된다"
    - "HalconDisplayService.RenderDatumFindResult 가 성공 케이스에서 주황색 3px 굵기 20px 십자를 RefOrigin 에 렌더하여 기존 teach 성공 오버레이(빨강 2px)와 시각적으로 구분된다"
    - "MainResultViewerControl 의 기존 SetDatumOverlay (Phase 12 teach 경로) 는 수정 없이 유지되며, Plan 02 가 추가하는 경로는 SetDatumFindResultOverlay 신규 public 메서드 + _datumFindResultOverlay private 필드로 additive 하게 주입된다"
    - "msbuild Debug/x64 가 exit 0 로 성공하고 3 개 수정 파일을 참조하는 신규 warning 이 0 개이다"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml"
      provides: "btn_testFindDatum Button + label_testFindResult Label canvas 툴바 배치"
      contains: "btn_testFindDatum"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "BtnTestFindDatum_Click 핸들러 + AskTestImageSource 다이얼로그 + TryFindDatum 호출 + label 피드백"
      contains: "BtnTestFindDatum_Click"
    - path: "WPF_Example/Halcon/Display/HalconDisplayService.cs"
      provides: "public void RenderDatumFindResult(HWindow, DatumConfig) — 주황 3px 십자 + 좌표 WriteString"
      contains: "RenderDatumFindResult"
  key_links:
    - from: "MainView.BtnTestFindDatum_Click success branch"
      to: "HalconDisplayService.RenderDatumFindResult"
      via: "halconViewer.SetDatumFindResultOverlay(datum) 호출 → MainResultViewerControl.Render() 가 _displayService.RenderDatumFindResult 호출"
      pattern: "SetDatumFindResultOverlay\\("
    - from: "MainView.BtnTestFindDatum_Click"
      to: "DatumFindingService.TryFindDatum"
      via: "var svc = new DatumFindingService(); svc.TryFindDatum(img, datum, out transform, out error)"
      pattern: "svc\\.TryFindDatum\\("
    - from: "MainView.AskTestImageSource (current-image path)"
      to: "halconViewer.CurrentImage"
      via: "현재 이미지 선택 시 halconViewer.CurrentImage 그대로 사용 (null-check 포함)"
      pattern: "halconViewer\\.CurrentImage"
    - from: "MainView.AskTestImageSource (load-from-file path)"
      to: "Microsoft.Win32.OpenFileDialog"
      via: "LoadAndDisplay(L264-272) 패턴 재사용 — png/bmp/jpg 필터, ShowDialog → halconViewer.LoadImage(dialog.FileName) → CurrentImage 읽기"
      pattern: "new OpenFileDialog"
---

<objective>
Phase 12 UAT Gap-4 (런타임 TryFindDatum 테스트 UI) 는 Phase 12-03 에서 "SPEC Out-of-scope" 사유로 이월되었다. Phase 13 Plan 02 는 이 Gap 을 닫는다.

**Purpose:** Datum 티칭이 완료된 후 사용자가 현재 Grab 이미지 또는 로드한 다른 이미지 파일에 대해 `DatumFindingService.TryFindDatum` 을 실행하여 검출 결과(RefOrigin + Angle)를 시각적(주황 십자) + 수치적(label)으로 확인할 수 있는 UI 를 추가한다. 이로써 튜닝된 Datum 이 실제 런타임 이미지에서도 robust 하게 동작하는지 수동 회귀 검증이 가능해진다.

**Output:**
- `WPF_Example/UI/ContentItem/MainView.xaml`:
  - canvas 툴바 StackPanel 에 `Button x:Name="btn_testFindDatum"` 추가 (btn_teachDatum 오른쪽, `RoiToggleButtonStyle` 재사용 또는 btn_calibrate 스타일 복사; 본 Plan 은 Button 타입 = 토글 아님).
  - canvas 툴바 Grid.Column=1 (label_drawHint 영역) 또는 Column=2 (label_pointCount 영역) 에 `Label x:Name="label_testFindResult"` 추가 (Visibility=Collapsed 기본, 결과 표시 시 Visible).
- `WPF_Example/UI/ContentItem/MainView.xaml.cs`:
  - `BtnTestFindDatum_Click(object, RoutedEventArgs)` 신규 핸들러 (K&R brace, 기존 TeachDatumButton_Click 스타일 준수).
  - `AskTestImageSource()` helper — 다이얼로그 (CustomMessageBox 혹은 CancelYesNo 형태) 로 "현재 이미지 / 다른 파일 / 취소" 3-way 선택. 취소 시 null 반환; 파일 선택 시 `OpenFileDialog` → `halconViewer.LoadImage(path)` → `halconViewer.CurrentImage` 반환.
  - 성공/실패 label feedback + 성공 시 `halconViewer.SetDatumFindResultOverlay(datum)` 호출.
- `WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs`:
  - `SetDatumFindResultOverlay(DatumConfig datum)` public 메서드 + `_datumFindResultOverlay` private 필드 + `ClearDatumFindResultOverlay()` 쌍. Render() 내 `_datumConfig` 분기 직후에 `_datumFindResultOverlay != null` 분기로 `_displayService.RenderDatumFindResult` 호출.
- `WPF_Example/Halcon/Display/HalconDisplayService.cs`:
  - `public void RenderDatumFindResult(HWindow window, DatumConfig datum)` — 주황 3px 굵기 20px half 십자 + 좌표 텍스트 오버레이. Allman brace, try/catch 관습 유지.
- msbuild Debug/x64 exit 0 + 신규 warning 0.
- SIMUL_MODE UAT checkpoint (checker:human-verify) — 3 algorithm × 2 이미지 소스 × 성공/실패 케이스.

**Note on MainResultViewerControl 수정:** CONTEXT D-13 는 "MainView 의 기존 btn_teachDatum 흐름 변경 없음" 을 명시한다. Plan 02 가 MainResultViewerControl 에 추가하는 것은 **신규 메서드/필드** 뿐이며 기존 SetDatumOverlay / Render 분기는 건드리지 않는다 (files_modified 에는 명시하지 않으나 Plan 실행 시 이 파일도 touch — 아래 Task 3 참조).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-datum-algorithm-extensibility/13-CONTEXT.md
@.planning/phases/13-datum-algorithm-extensibility/13-PATTERNS.md
@.planning/phases/13-datum-algorithm-extensibility/13-01-PLAN.md
@.planning/phases/12-datum-circle-vertical-horizontal-intersection/12-03-SUMMARY.md
@WPF_Example/UI/ContentItem/MainView.xaml
@WPF_Example/UI/ContentItem/MainView.xaml.cs
@WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
@WPF_Example/Halcon/Display/HalconDisplayService.cs
@WPF_Example/Halcon/Algorithms/DatumFindingService.cs
@WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
@CLAUDE.md

<interfaces>
<!-- Plan 02 는 신규 UI 진입점 + 신규 렌더 메서드 추가. 기존 public 시그니처 변경 금지. -->
<!-- CONTEXT D-13 준수: DatumConfig / DatumFindingService / MainView(기존) / HalconDisplayService.RenderDatumOverlay 전부 untouched. -->

From WPF_Example/Halcon/Algorithms/DatumFindingService.cs (Phase 12 Plan 02 확정 / Phase 13 Plan 01 helper 만 추가):
```csharp
public bool TryFindDatum(HImage image, DatumConfig config, out HTuple transform, out string error);
//  - L28 public — Plan 02 가 호출. 성공 시 config.RefOriginRow/Col + RefAngleRad 가 검출값으로 업데이트됨 (Phase 4 Plan 01 L217-219 구현).
//  - config.IsConfigured=false 이면 true + identity transform (L34-37) — BtnTestFindDatum_Click 진입 가드로 !IsConfigured 차단 권장.
```

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (읽기 전용):
```csharp
public double RefOriginRow { get; set; }         // L120 — TryFindDatum 성공 시 curRow 로 갱신
public double RefOriginCol { get; set; }         // L121
public double RefAngleRad { get; set; }          // L122
public bool IsConfigured { get; set; }           // L137 — teach 완료 게이트
public bool LastTeachSucceeded { get; set; }     // L164 — BtnTestFindDatum_Click 진입 가드
```

From WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs (Phase 11/12 기존):
```csharp
public HImage CurrentImage { get; private set; }             // L173 — 현재 Grab/Load 이미지
public void LoadImage(string imagePath);                      // L189 — Load 경로 재사용
public void LoadImage(HImage image);                          // L206 — 직접 HImage 주입
public void SetDatumOverlay(DatumConfig datum, bool isSelected); // L512 — teach 경로 (untouched)
// Plan 02 신규 추가 (이 Plan 에서 만듦):
//   public void SetDatumFindResultOverlay(DatumConfig datum);
//   public void ClearDatumFindResultOverlay();
//   private DatumConfig _datumFindResultOverlay;
```

From WPF_Example/Halcon/Display/HalconDisplayService.cs (Phase 12 기존 / Plan 02 가 추가):
```csharp
public void Render(...);                                      // L17 — untouched
public void RenderCircleDraft(HWindow, double, double, double);// L195 — 독립 렌더 메서드 시그니처 패턴 (Plan 02 가 이와 동일 패턴으로 RenderDatumFindResult 추가)
public void RenderDatumOverlay(HWindow, DatumConfig, bool);   // L350 — teach 경로 (untouched)
// Plan 02 신규 추가:
//   public void RenderDatumFindResult(HWindow window, DatumConfig datum);
```

From WPF_Example/UI/ContentItem/MainView.xaml.cs (Phase 12 기존, Plan 02 가 추가):
```csharp
// MainView 필드 (L24):
private MainWindow mParentWindow;
// mParentWindow.inspectionList.SelectedParam as DatumConfig → Datum 해결 (L994 패턴 재사용)

// 신규 Plan 02 추가:
//   private void BtnTestFindDatum_Click(object sender, RoutedEventArgs e);
//   private HImage AskTestImageSource();  // 현재 이미지 / OpenFileDialog / 취소
```

From WPF_Example/UI/ControlItem/InspectionListView.xaml.cs (Phase 12 기존, read-only):
```csharp
public ParamBase SelectedParam { get; private set; }  // L21 — btn_testFindDatum 활성화/Datum 해결
public void RefreshParamEditor();                      // L26 — Plan 02 가 호출할 일 없음 (Plan 03 에서만)
```
</interfaces>

**Brace style (PATTERNS.md § File Classification):**
- `MainView.xaml.cs` = **K&R** (opening brace same line)
- `MainResultViewerControl.xaml.cs` = **Allman** (opening brace own line)
- `HalconDisplayService.cs` = **Allman**
- `MainView.xaml` = XAML

**C# 7.2 제약:** `$"..."` interpolated string 사용 가능 (MainView.xaml.cs L214, L306 등에서 이미 사용). switch expression / record / nullable reference types 금지.

**주석 convention (CLAUDE.md + 13-CONTEXT.md § code_context):**
- C#: `//260424 hbk Phase 13 D-05..D-08 — <intent>`
- XAML: `<!--260424 hbk Phase 13 D-05 — <intent>-->`

**Phase 12 D-07 UX 리터럴 (13-CONTEXT.md L64):**
- 성공 라벨: `"TryFind OK — RefOrigin = (Row: 123.4, Col: 567.8), Angle: -0.023 rad"` 패턴
- 실패 라벨: `"TryFind FAIL — " + error` (error 는 DatumFindingService 반환 문자열)

**Phase 12 D-07 오버레이 팔레트 (13-CONTEXT.md L64):**
- 검출 RefOrigin: **주황색 3px 굵기 20px half 십자** (teach 성공 빨강 2px 와 차별화). Halcon 컬러 리터럴 `"orange"` 사용.

**LoadImage 경로 재사용 (PATTERNS.md §2.1 L109-131):**
MainView.LoadAndDisplay L261-298 의 OpenFileDialog → halconViewer.LoadImage(path) 패턴. Plan 02 의 AskTestImageSource 는 FAI param 의존부 없이 HImage 만 반환하도록 단순화.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add btn_testFindDatum Button + label_testFindResult Label to MainView.xaml</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml L60-127 (canvas 툴바 StackPanel 전체 — btn_circleRoi / btn_teachDatum / btn_calibrate / label_drawHint / label_pointCount 기존 배치)
    - .planning/phases/13-datum-algorithm-extensibility/13-CONTEXT.md L55-60 (D-05 진입점 명세)
    - .planning/phases/13-datum-algorithm-extensibility/13-PATTERNS.md §1 L32-69 (toolbar 버튼 analog)
  </read_first>
  <action>
    1. **btn_testFindDatum Button 추가** — MainView.xaml L96 `</ToggleButton>` (btn_teachDatum 닫는 태그) 바로 다음 줄에 삽입. L98 `<Separator ... />` 앞에 배치.

    ```xml
                    <!--260424 hbk Phase 13 D-05 — 런타임 TryFindDatum 테스트 버튼 (Datum teach 완료 후 활성화)-->
                    <Button x:Name="btn_testFindDatum" Content="Test Find"
                        Click="BtnTestFindDatum_Click"
                        IsEnabled="False"
                        MinWidth="80" Height="28" Padding="12,4"
                        FontSize="13" FontWeight="SemiBold"
                        Background="#2563EB" Foreground="White"
                        BorderBrush="#1D4ED8" BorderThickness="1"
                        Margin="0,0,4,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
    ```

    **배치 이유:**
    - btn_teachDatum 과 인접 → Datum 티칭 후 바로 다음 단계로 흐름 이어짐 (UX 연속성).
    - btn_calibrate 와 동일 스타일 복사 (Button.Template 포함) → ToggleButton 이 아닌 일반 Button 이므로 RoiToggleButtonStyle 부적합, btn_calibrate L101-117 양식 재사용.
    - IsEnabled="False" 기본 — Task 2 핸들러가 클릭 시 CustomMessageBox 로 가드하지만, Datum 노드 선택 + LastTeachSucceeded=true 시점에 InspectionListView 가 활성화 로직을 trigger 하면 이상적. **본 Plan 은 활성화 로직을 InspectionListView 까지 확장하지 않는다** (CONTEXT D-13 "Phase 12 btn_teachDatum 흐름 변경 없음" 범위 밖). 대신 IsEnabled="False" 로 시작하고 Task 2 에서 **기본 활성화 상태를 IsEnabled="True" 로 변경** (항상 클릭 가능, 가드는 핸들러에서 수행). 실제 IsEnabled="True" 로 최종 커밋.

    **최종 IsEnabled 값 결정:** IsEnabled="True" — 핸들러에서 `datum == null || !datum.IsConfigured || !datum.LastTeachSucceeded` early return. InspectionListView 쪽 활성화 로직을 건드리지 않기 위함.

    2. **label_testFindResult Label 추가** — label_pointCount L123-125 바로 다음 줄에 (같은 Grid.Column="2") 추가. **주의: Grid.Column="2" 에 Label 이 이미 있음(label_pointCount)** → Grid 를 다시 구성하지 않고, label_testFindResult 를 label_drawHint 와 동일 Column=1 에 Visibility=Collapsed 로 배치 (두 label 이 동시에 보이지 않도록 Task 2 에서 상호 Visibility 관리).

    대안적 배치: Grid 의 StackPanel 바깥쪽이 깨끗하지 않으므로, Grid.Column="1" label_drawHint 와 함께 StackPanel (Orientation=Horizontal, VerticalAlignment=Center) 로 묶어 Row 내 간섭 없이 개별 Label 유지. 하지만 XAML 최소 변경 원칙 상 **독립 Label 추가로 Grid.Column="1" 에 같이 두되 label_drawHint 와 label_testFindResult 는 Task 2 핸들러에서 상호 배타적으로 Visibility 토글**.

    L125 `Foreground="#FFAAAAAA" Margin="0,0,8,0"/>` (label_pointCount 닫는 태그) 바로 다음 줄에 삽입:

    ```xml
                <!--260424 hbk Phase 13 D-07 — 런타임 TryFindDatum 결과 label (성공 LimeGreen / 실패 Red)-->
                <Label x:Name="label_testFindResult" Grid.Column="1" Visibility="Collapsed"
                       HorizontalAlignment="Center" VerticalContentAlignment="Center"
                       FontSize="13" Foreground="#FFAAAAAA"/>
    ```

    **`Grid.Column="1"` 선택 이유:** label_drawHint 와 동일 Column. Task 2 에서 TestFind 실행 시 drawHint 를 Collapsed, testFindResult 를 Visible 로 토글 → 충돌 없음.

    3. msbuild Debug/x64 빌드 실행:
    ```bash
    msbuild WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal
    ```
    exit 0 확인. MainView.xaml 대상 신규 warning 0 확인.

    4. 커밋: `feat(phase-13-02): add btn_testFindDatum + label_testFindResult to canvas toolbar`
  </action>
  <verify>
    <automated>
      (1) grep -cF "x:Name=\"btn_testFindDatum\"" WPF_Example/UI/ContentItem/MainView.xaml → 1
      (2) grep -cF "Click=\"BtnTestFindDatum_Click\"" WPF_Example/UI/ContentItem/MainView.xaml → 1
      (3) grep -cF "x:Name=\"label_testFindResult\"" WPF_Example/UI/ContentItem/MainView.xaml → 1
      (4) grep -cF "260424 hbk Phase 13 D-05" WPF_Example/UI/ContentItem/MainView.xaml → ≥ 1
      (5) grep -cF "260424 hbk Phase 13 D-07" WPF_Example/UI/ContentItem/MainView.xaml → ≥ 1
      (6) msbuild WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal → exit 0
    </automated>
  </verify>
  <done>
    - btn_testFindDatum Button + label_testFindResult Label 추가 완료 (canvas 툴바 내 기존 요소와 정상 공존)
    - msbuild Debug/x64 exit 0 (BtnTestFindDatum_Click 핸들러 미존재로 인한 XAML→code-behind 매핑 컴파일 에러 발생 가능 → Task 2 가 동일 커밋 범위에 포함되도록 Task 1/2 순차 실행 필수). **Task 1 단독 빌드 시 CS1061 `BtnTestFindDatum_Click` undefined 에러 발생 → Task 1 완료 시점에는 빌드 실패 허용, Task 2 완료 직후 재빌드로 exit 0 확인**.
    - 빌드 검증은 Task 2 말미 "Task 1+2 누적 빌드" 로 대체.
    - git commit 완료 (Task 2 commit 이 Task 1 XAML 변경까지 포함하도록 Task 2 에서 `git add MainView.xaml MainView.xaml.cs` 합쳐서 커밋 권장, **또는 Task 1 별도 커밋 후 Task 2 에서 코드 후속 커밋으로 빌드 성공 달성**). 본 Plan 은 **Task 1 = XAML 만 커밋, Task 2 = .xaml.cs + MainResultViewerControl + HalconDisplayService 커밋** 구조로 분리. Task 1 단독 빌드 실패는 plan 실행 시 checker 에게 사전 고지.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add BtnTestFindDatum_Click + AskTestImageSource to MainView.xaml.cs</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L1-18 (using 선언 — Microsoft.Win32 / HalconDotNet 등 이미 포함 확인)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L40-54 (ECanvasMode / _editingDatum / EDatumTeachStep 필드)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L261-299 (LoadAndDisplay — OpenFileDialog Filter 참고)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L987-1010 (TeachDatumButton_Click — mParentWindow?.inspectionList?.SelectedParam as DatumConfig 패턴)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L1174-1208 (InvokeTryTeachDatum — halconViewer.CurrentImage 사용 + DatumFindingService 호출 + label 피드백 패턴)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs L28 (TryFindDatum 시그니처 확인)
    - .planning/phases/13-datum-algorithm-extensibility/13-CONTEXT.md L55-67 (D-05/D-06/D-07/D-08 UX 결정)
    - .planning/phases/13-datum-algorithm-extensibility/13-PATTERNS.md §2.1 L73-178 (핸들러 skeleton)
  </read_first>
  <action>
    1. **BtnTestFindDatum_Click 신규 핸들러 추가** — L1208 `}` (InvokeTryTeachDatum 닫는 브레이스) 바로 다음 줄 (L1209) 에 아래 두 메서드를 추가. **K&R brace** 엄수, `_editingDatum` 필드 재사용 금지(이 경로는 teach 와 독립 — `datum` 로컬 변수 사용).

    ```csharp
            //260424 hbk Phase 13 D-05..D-08 — 런타임 TryFindDatum 테스트 진입 (현재/Load 이미지 2-way + 성공 주황 십자 + 실패 에러 메시지)
            private void BtnTestFindDatum_Click(object sender, RoutedEventArgs e) {
                //260424 hbk Phase 13 D-05 — Datum 해결 (InspectionListView 선택 우선, _editingDatum fallback 없음 — teach 세션 독립)
                var datum = mParentWindow?.inspectionList?.SelectedParam as DatumConfig;
                if (datum == null || !datum.IsConfigured || !datum.LastTeachSucceeded) {
                    CustomMessageBox.Show("Datum 티칭이 완료된 후 테스트 가능합니다.", "Datum Find 테스트"); //260424 hbk Phase 13 D-05
                    return;
                }

                //260424 hbk Phase 13 D-06 — 테스트 이미지 소스 선택 (현재 / Load / 취소)
                HImage testImage = AskTestImageSource();
                if (testImage == null) return; //260424 hbk Phase 13 D-06 — 사용자 취소

                //260424 hbk Phase 13 D-07/D-08 — DatumFindingService.TryFindDatum 호출 (Phase 4 Plan 01 L28 시그니처)
                var svc = new ReringProject.Halcon.Algorithms.DatumFindingService();
                HTuple transform;
                string error;
                bool ok = svc.TryFindDatum(testImage, datum, out transform, out error);

                //260424 hbk Phase 13 D-07/D-08 — label_drawHint 숨기고 label_testFindResult 로 전용 피드백
                label_drawHint.Visibility = Visibility.Collapsed;
                label_testFindResult.Visibility = Visibility.Visible;
                if (ok) {
                    label_testFindResult.Content = string.Format(
                        "TryFind OK — RefOrigin=({0:F1}, {1:F1}), Angle={2:F3} rad",
                        datum.RefOriginRow, datum.RefOriginCol, datum.RefAngleRad); //260424 hbk Phase 13 D-07
                    label_testFindResult.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FF4ADE80")); //260424 hbk Phase 13 D-07 LimeGreen (InvokeTryTeachDatum L1193 동일 palette)
                    //260424 hbk Phase 13 D-07 — 성공 시 주황 십자 오버레이 렌더 (RenderDatumFindResult — HalconDisplayService)
                    halconViewer.SetDatumFindResultOverlay(datum);
                }
                else {
                    label_testFindResult.Content = "TryFind FAIL — " + (error ?? "unknown"); //260424 hbk Phase 13 D-08
                    label_testFindResult.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FFF87171")); //260424 hbk Phase 13 D-08 error red
                    //260424 hbk Phase 13 D-08 — 실패 시 오버레이 clear (이전 성공 십자 잔상 제거)
                    halconViewer.ClearDatumFindResultOverlay();
                }
            }

            //260424 hbk Phase 13 D-06 — 테스트 이미지 소스 다이얼로그: 현재 halconViewer.CurrentImage / OpenFileDialog / 취소
            //  반환 HImage 는 halconViewer.CurrentImage 참조 그대로 (별도 Dispose 책임 없음 — halconViewer 가 소유)
            private HImage AskTestImageSource() {
                HImage currentImg = halconViewer.CurrentImage;
                bool hasCurrent = (currentImg != null);

                //260424 hbk Phase 13 D-06 — 3-way 선택 (CustomMessageBox 는 2-button 한계 → MessageBox.Show YesNoCancel 사용)
                //  Yes=현재 이미지 / No=파일 선택 / Cancel=취소
                MessageBoxResult choice;
                if (hasCurrent) {
                    choice = MessageBox.Show(
                        "테스트 이미지를 선택하세요.\n\n[예] 현재 이미지로 테스트\n[아니오] 다른 파일 선택...\n[취소] 취소",
                        "Datum Find 테스트 이미지",
                        MessageBoxButton.YesNoCancel,
                        MessageBoxImage.Question);
                }
                else {
                    //260424 hbk Phase 13 D-06 — 현재 이미지 없으면 바로 파일 선택 경로 (2-way)
                    choice = MessageBoxResult.No; // 파일 선택 분기로 진입
                }

                if (choice == MessageBoxResult.Cancel) return null;
                if (choice == MessageBoxResult.Yes) return currentImg; //260424 hbk Phase 13 D-06 — 현재 이미지 그대로 사용

                //260424 hbk Phase 13 D-06 — No = OpenFileDialog (LoadAndDisplay L264-272 필터 재사용)
                var dialog = new OpenFileDialog {
                    Filter = "Image Files (*.bmp;*.jpg;*.jpeg;*.png;*.tif;*.tiff)|*.bmp;*.jpg;*.jpeg;*.png;*.tif;*.tiff|All Files (*.*)|*.*"
                };
                if (dialog.ShowDialog() != true) return null;

                try {
                    halconViewer.LoadImage(dialog.FileName); //260424 hbk Phase 13 D-06 — halconViewer 가 CurrentImage 교체 + Render
                    return halconViewer.CurrentImage; //260424 hbk Phase 13 D-06 — 로드된 이미지 참조 반환
                }
                catch (Exception ex) {
                    Logging.PrintErrLog((int)ELogType.Error, "Datum Test Load fail: " + ex.Message); //260424 hbk Phase 13 D-08
                    CustomMessageBox.Show("이미지 로드 실패: " + ex.Message, "Datum Find 테스트");
                    return null;
                }
            }
    ```

    **위치 확인:** L1208 `}` = InvokeTryTeachDatum 닫는 브레이스. 그 다음 줄은 L1209 `}` = MainView partial class 닫는 브레이스. 신규 메서드 2 개를 그 사이에 삽입.

    **Using 선언:** L1-18 이미 `using Microsoft.Win32;` (OpenFileDialog) / `using HalconDotNet;` (HImage/HTuple) / `using System.Windows;` (MessageBox*) / `using System.Windows.Media;` (SolidColorBrush/ColorConverter) 포함 → 추가 using 불필요.

    **필드 없음 확인:** `_editingDatum` 은 건드리지 않는다 (teach 세션과 독립). 이 핸들러는 stateless — 매 클릭마다 SelectedParam 로 datum 재해결.

    2. **InvokeTryTeachDatum 내부 label_drawHint/label_testFindResult 우선순위 (선택적 정리):**
    - InvokeTryTeachDatum (L1178, L1192, L1199) 은 label_drawHint 를 사용한다. Plan 02 는 **이 로직을 건드리지 않는다** (CONTEXT D-13). 다만 teach 후 TestFind 호출 시 이전 drawHint 가 보일 수 있으므로, BtnTestFindDatum_Click 진입 시 `label_drawHint.Visibility = Visibility.Collapsed` 를 명시적으로 호출 (위 코드 블록 포함).

    3. **SetDatumFindResultOverlay / ClearDatumFindResultOverlay 호출:** 이 메서드들은 Task 3 에서 MainResultViewerControl 에 추가한다. **Task 2 완료 시점에는 메서드 미존재로 CS1061 컴파일 에러** → Task 2 와 Task 3 는 **단일 커밋**으로 묶는다. Task 2 는 `MainView.xaml.cs` 변경을 stage 하고, Task 3 에서 `MainResultViewerControl.xaml.cs` + `HalconDisplayService.cs` 를 stage 한 후 합쳐서 커밋.

    **실행 순서 (Task 2+3 단일 커밋):**
    - Task 1: XAML 커밋 (빌드 실패 허용 — BtnTestFindDatum_Click / label_testFindResult 핸들러 미정의).
    - Task 2: MainView.xaml.cs 수정 (저장만, 커밋 안 함).
    - Task 3: MainResultViewerControl.xaml.cs + HalconDisplayService.cs 수정 후 3 파일 합쳐서 커밋 + 빌드.

    4. **label_testFindResult 호출 대상 간섭 점검:** label_testFindResult 는 TestFind 클릭 시에만 Visible. teach 취소/실패 경로에서는 건드리지 않음 → 기존 teach UX 무변화.

    5. **msbuild Task 3 완료 후 실행** (이 Task 단독 빌드는 MainResultViewerControl 메서드 미존재로 실패).
  </action>
  <verify>
    <automated>
      (1) grep -cE "private void BtnTestFindDatum_Click\(" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (2) grep -cE "private HImage AskTestImageSource\(" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (3) grep -cF "svc.TryFindDatum(testImage, datum, out transform, out error)" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (4) grep -cF "halconViewer.SetDatumFindResultOverlay(datum)" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (5) grep -cF "halconViewer.ClearDatumFindResultOverlay()" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (6) grep -cF "TryFind OK — RefOrigin=" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (7) grep -cF "TryFind FAIL — " WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (8) grep -cF "CustomMessageBox.Show(\"Datum 티칭이 완료된 후" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (9) grep -cF "Image Files (*.bmp;*.jpg;*.jpeg;*.png;*.tif;*.tiff)" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 2 (기존 LoadAndDisplay 1 + Plan 02 AskTestImageSource 1)
      (10) grep -cF "260424 hbk Phase 13 D-05" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 1
      (11) grep -cF "260424 hbk Phase 13 D-06" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 3 (AskTestImageSource 주석 3 곳)
      (12) grep -cF "260424 hbk Phase 13 D-07" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 2
      (13) grep -cF "260424 hbk Phase 13 D-08" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 2
      (14) Task 3 완료 후 msbuild Debug/x64 → exit 0 (Task 2 단독 빌드는 검증 대상 아님)
    </automated>
  </verify>
  <done>
    - BtnTestFindDatum_Click + AskTestImageSource 2 개 메서드 추가 완료
    - K&R brace style 유지 (이 파일 관례)
    - 주석 `//260424 hbk Phase 13 D-05..D-08` 모든 신규 라인
    - label_drawHint 와 label_testFindResult 간 Visibility 토글 동작 확인 (코드 리뷰)
    - 이 Task 단독으로는 git stage 만 수행, 실제 커밋은 Task 3 완료 시점에 합쳐서 수행
  </done>
</task>

<task type="auto">
  <name>Task 3: Add RenderDatumFindResult to HalconDisplayService + SetDatumFindResultOverlay to MainResultViewerControl</name>
  <files>
    WPF_Example/Halcon/Display/HalconDisplayService.cs
    WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
  </files>
  <read_first>
    - WPF_Example/Halcon/Display/HalconDisplayService.cs L195-211 (RenderCircleDraft — 독립 렌더 메서드 시그니처 패턴)
    - WPF_Example/Halcon/Display/HalconDisplayService.cs L213-231 (EnsureFontInitialized)
    - WPF_Example/Halcon/Display/HalconDisplayService.cs L350-505 (RenderDatumOverlay — disp_line / RefOriginRow 십자 패턴 참고, 이 메서드 자체는 untouched)
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs L505-524 (SetDatumOverlay + ClearDatumOverlay + _datumConfig/_datumSelected 필드 패턴)
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs L598-610 (Render 메서드 내 _datumConfig 분기 위치)
    - .planning/phases/13-datum-algorithm-extensibility/13-CONTEXT.md L62-64 (D-07 렌더 사양)
    - .planning/phases/13-datum-algorithm-extensibility/13-PATTERNS.md §5 L591-631 (RenderDatumFindResult skeleton)
  </read_first>
  <action>
    **3A. HalconDisplayService.RenderDatumFindResult 메서드 추가**

    파일: `WPF_Example/Halcon/Display/HalconDisplayService.cs`

    위치: L211 `}` (RenderCircleDraft 닫는 브레이스) 바로 다음 줄에 삽입 (L213 `private void EnsureFontInitialized` 앞). Allman brace.

    ```csharp
            //260424 hbk Phase 13 D-07 — 런타임 TryFindDatum 성공 시 검출 RefOrigin 주황 3px 십자 + 좌표 WriteString 오버레이
            //  기존 RenderDatumOverlay teach-success 빨간 2px 십자 (L350+) 와 시각적으로 구분되는 팔레트.
            //  datum.RefOriginRow/Col 은 TryFindDatum 성공 시 DatumFindingService L217-219/L359-361/L506-508 에서 curRow/curCol 로 업데이트된 값.
            public void RenderDatumFindResult(HWindow window, DatumConfig datum)
            {
                if (window == null || datum == null) return;

                try
                {
                    // 주황 십자 (20px half-length, 3px 굵기 — teach 빨강 2px 와 차별화)
                    HOperatorSet.SetColor(window, "orange");
                    HOperatorSet.SetLineWidth(window, 3);
                    const double crossHalf = 20.0;
                    HOperatorSet.DispLine(window,
                        datum.RefOriginRow - crossHalf, datum.RefOriginCol,
                        datum.RefOriginRow + crossHalf, datum.RefOriginCol);
                    HOperatorSet.DispLine(window,
                        datum.RefOriginRow, datum.RefOriginCol - crossHalf,
                        datum.RefOriginRow, datum.RefOriginCol + crossHalf);

                    // 좌표 텍스트 (십자 왼쪽 위 외곽 — teach yellow 라벨 규약과 동일한 상단 offset)
                    EnsureFontInitialized(window);
                    HOperatorSet.SetColor(window, "orange");
                    HOperatorSet.SetTposition(window, datum.RefOriginRow - crossHalf - 22, datum.RefOriginCol + crossHalf + 4);
                    HOperatorSet.WriteString(window,
                        "TryFind (" + datum.RefOriginRow.ToString("F1") + ", " + datum.RefOriginCol.ToString("F1") + ")");
                }
                catch
                {
                    // Suppress display errors (기존 RenderDatumOverlay / RenderCircleDraft catch 관습 유지)
                }
            }
    ```

    **C# 7.2 제약:** `+` concat 사용 (이 파일은 Phase 12 에서도 concat 관례). `$"..."` 금지 안 함이지만 기존 RenderDatumOverlay L462 ~ 가 concat 만 쓰므로 일관성 유지.

    **3B. MainResultViewerControl 에 SetDatumFindResultOverlay / ClearDatumFindResultOverlay + _datumFindResultOverlay 필드 추가**

    파일: `WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs`

    **위치 1 — 필드 선언 (L509 `_datumSelected` 뒤):**

    ```csharp
            //260424 hbk Phase 13 D-07 — 런타임 TryFindDatum 성공 시 주황 십자 렌더 대상 (SetDatumFindResultOverlay 로 주입)
            //  null 이 아니면 Render() 가 _displayService.RenderDatumFindResult 호출.
            //  teach 경로 (_datumConfig + _datumSelected) 와 독립 — 동시 표시 허용 (주황 십자 + 빨간 교점 십자 공존).
            private DatumConfig _datumFindResultOverlay;
    ```

    **위치 2 — 메서드 추가 (L524 `ClearDatumOverlay` 닫는 브레이스 바로 뒤):**

    ```csharp
            //260424 hbk Phase 13 D-07 — 런타임 TryFindDatum 성공 시 주황 십자 오버레이 set
            public void SetDatumFindResultOverlay(DatumConfig datum)
            {
                _datumFindResultOverlay = datum;
                Render();
            }

            //260424 hbk Phase 13 D-08 — 실패 / 재시도 시 주황 십자 오버레이 clear
            public void ClearDatumFindResultOverlay()
            {
                _datumFindResultOverlay = null;
                Render();
            }
    ```

    **위치 3 — Render() 내 분기 추가 (L608 `}` = _datumConfig 블록 닫는 브레이스 바로 뒤):**

    ```csharp
                //260424 hbk Phase 13 D-07 — 런타임 TryFindDatum 결과 주황 십자 오버레이 (teach 경로와 독립, 동시 표시 허용)
                if (_datumFindResultOverlay != null)
                {
                    _displayService.RenderDatumFindResult(ViewerHost.HalconWindow, _datumFindResultOverlay);
                }
    ```

    **주의:** L608 이후 L611 `if (_isDrawingCircle && _circleDraftRadius > 0)` 블록이 있음 → 신규 블록은 L608~L610 사이에 삽입 (draft 렌더는 오버레이 뒤에 그려져야 draft 가 위에 얹힘).

    **3C. msbuild Debug/x64 재빌드 (Task 1+2+3 합산):**

    ```bash
    msbuild WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal
    ```

    exit 0 확인. 3 개 수정 파일 (MainView.xaml.cs / MainResultViewerControl.xaml.cs / HalconDisplayService.cs) 대상 신규 warning 0 확인.

    **3D. 커밋:** Task 1 (XAML 만 이미 커밋됨) 에 이어 Task 2+3 합쳐서:

    ```bash
    git add WPF_Example/UI/ContentItem/MainView.xaml.cs WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs WPF_Example/Halcon/Display/HalconDisplayService.cs
    git commit -m "feat(phase-13-02): BtnTestFindDatum_Click + RenderDatumFindResult + SetDatumFindResultOverlay"
    ```
  </action>
  <verify>
    <automated>
      (1) grep -cE "public void RenderDatumFindResult\(HWindow window, DatumConfig datum\)" WPF_Example/Halcon/Display/HalconDisplayService.cs → 1
      (2) grep -cF "HOperatorSet.SetColor(window, \"orange\")" WPF_Example/Halcon/Display/HalconDisplayService.cs → ≥ 2 (주황 선 + 주황 텍스트)
      (3) grep -cF "const double crossHalf = 20.0" WPF_Example/Halcon/Display/HalconDisplayService.cs → ≥ 1
      (4) grep -cF "SetLineWidth(window, 3)" WPF_Example/Halcon/Display/HalconDisplayService.cs → ≥ 1 (teach 는 SetLineWidth 2 — Plan 02 가 3 추가)
      (5) grep -cE "private DatumConfig _datumFindResultOverlay" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → 1
      (6) grep -cE "public void SetDatumFindResultOverlay\(DatumConfig datum\)" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → 1
      (7) grep -cE "public void ClearDatumFindResultOverlay\(\)" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → 1
      (8) grep -cF "_displayService.RenderDatumFindResult(ViewerHost.HalconWindow, _datumFindResultOverlay)" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → 1
      (9) grep -cF "260424 hbk Phase 13 D-07" WPF_Example/Halcon/Display/HalconDisplayService.cs → ≥ 1
      (10) grep -cF "260424 hbk Phase 13 D-07" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → ≥ 2
      (11) grep -cF "260424 hbk Phase 13 D-08" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → ≥ 1
      (12) msbuild WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal → exit 0
      (13) 기존 RenderDatumOverlay 시그니처 미변경 검증: grep -cE "public void RenderDatumOverlay\(HWindow window, DatumConfig datum, bool isSelected\)" WPF_Example/Halcon/Display/HalconDisplayService.cs → 1
      (14) 기존 SetDatumOverlay 시그니처 미변경 검증: grep -cE "public void SetDatumOverlay\(DatumConfig datum, bool isSelected\)" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → 1
    </automated>
  </verify>
  <done>
    - HalconDisplayService.RenderDatumFindResult 추가 완료 (Allman brace)
    - MainResultViewerControl.SetDatumFindResultOverlay + ClearDatumFindResultOverlay + _datumFindResultOverlay 필드 + Render 분기 추가 완료
    - 기존 SetDatumOverlay / RenderDatumOverlay 시그니처 및 로직 untouched (CONTEXT D-13 준수)
    - msbuild Debug/x64 exit 0, 신규 warning 0 (MainView.xaml.cs / MainResultViewerControl.xaml.cs / HalconDisplayService.cs 대상)
    - git commit 완료 (Task 2 XAML.cs + Task 3 2개 파일 합산)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: SIMUL_MODE UAT checkpoint — 3 algorithm × 2 image source × pass/fail</name>
  <what-built>
    - btn_testFindDatum Button (canvas 툴바, btn_teachDatum 오른쪽)
    - BtnTestFindDatum_Click 핸들러 + AskTestImageSource 다이얼로그 (현재/Load/취소)
    - label_testFindResult 라벨 (LimeGreen 성공 / Red 실패)
    - HalconDisplayService.RenderDatumFindResult (주황 3px 20px 십자 + 좌표 텍스트)
    - MainResultViewerControl.SetDatumFindResultOverlay / ClearDatumFindResultOverlay
  </what-built>
  <how-to-verify>
    **환경:** SIMUL_MODE Debug/x64 빌드, 기존 SIMUL 이미지 폴더 사용.

    **테스트 1 — TwoLineIntersect 성공 (현재 이미지):**
    1. 앱 실행 → Sequence/Shot 트리에서 Datum 노드 선택 (AlgorithmType=TwoLineIntersect)
    2. Grab 으로 SIMUL 이미지 로드 → btn_teachDatum 클릭 → Line1/Line2 드래그 → Datum 티칭 완료 ("Datum 티칭 완료 — Recipe Save 권장" 녹색)
    3. **btn_testFindDatum 클릭** → MessageBox "Datum Find 테스트 이미지" 다이얼로그 → **[예] 현재 이미지로 테스트** 선택
    4. **기대:** label_drawHint 가 사라지고 label_testFindResult 에 녹색 `"TryFind OK — RefOrigin=(..., ...), Angle=... rad"` 표시. 캔버스에 **주황 3px 십자** 가 기존 빨간 2px 교점 십자와 함께 (또는 겹쳐) 표시 → 주황이 더 굵어서 시각적으로 구분 가능.

    **테스트 2 — CircleTwoHorizontal Find (다른 이미지 파일):**
    1. 앱 재시작 or Recipe Load → Datum 노드 (AlgorithmType=CircleTwoHorizontal) 선택 → 티칭 완료
    2. btn_testFindDatum 클릭 → **[아니오] 다른 파일 선택...** 선택 → OpenFileDialog → 다른 SIMUL 이미지 (같은 피사체, 살짝 다른 조명/위치) 선택
    3. **기대:** halconViewer 가 새 이미지로 교체 + 주황 십자 + 녹색 label. RefOrigin 값이 현재 이미지 기준 값과 약간 달라짐 (정상 — 런타임 변환 검증).

    **테스트 3 — VerticalTwoHorizontal Find 실패 케이스:**
    1. Datum (AlgorithmType=VerticalTwoHorizontal) 티칭 완료
    2. btn_testFindDatum → 다른 파일 선택 → **피사체가 완전히 없거나 심하게 다른 이미지** 선택
    3. **기대:** label_testFindResult 에 빨간색 `"TryFind FAIL — " + <DatumFindingService 에러 문자열>` 표시. 주황 십자는 그려지지 않음 (ClearDatumFindResultOverlay). 이전 성공 잔상 있어도 clear 됨.

    **테스트 4 — 미티칭 상태 가드:**
    1. Datum 노드 선택하되 티칭 안 함 (IsConfigured=false 또는 LastTeachSucceeded=false) → btn_testFindDatum 클릭
    2. **기대:** CustomMessageBox `"Datum 티칭이 완료된 후 테스트 가능합니다."` → 확인 → 아무 일 없음. label_testFindResult 변경 없음.

    **테스트 5 — Cancel 경로:**
    1. 티칭 완료 상태에서 btn_testFindDatum → 다이얼로그 → **[취소]** 클릭
    2. **기대:** 조용히 닫힘. label_testFindResult 변경 없음. 캔버스 변경 없음.

    **테스트 6 — 현재 이미지 없음 edge case:**
    1. 앱 시작 직후 (Grab 전, halconViewer.CurrentImage=null) Datum 노드 선택 → 어떻게든 티칭 상태라고 가정하고 btn_testFindDatum 클릭
    2. **기대:** MessageBox 가 YesNoCancel 이 아닌 **2-way (No=파일선택, Cancel)** 로 진입 (현재 이미지 없음 → Yes 옵션 미제시) 또는 OpenFileDialog 로 직행. 파일 선택 후 정상 Find.

    **회귀 확인:**
    - btn_teachDatum 워크플로우 3 algorithm 전체 (Phase 12 Plan 03 UX) 가 회귀 없이 동작 — btn_testFindDatum 추가가 teach 경로에 영향 없음.
    - FAI CRUD / ROI 드래그 / 캘리브레이션 등 기존 canvas 툴바 동작 무변화.

    **라벨 palette 확인:**
    - 주황 십자 ("orange", 3px) vs 기존 teach 빨강 십자 ("red", 2px) — 색+굵기 두 축으로 구분 가능해야 함.
    - label_drawHint (teach) 와 label_testFindResult (Find) 가 동일 Grid.Column=1 에 배치되어 있으나 Visibility 토글로 상호 배타 → 한 번에 하나만 보임.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (예: "주황 십자가 안 보임", "label_testFindResult 위치가 겹침", "Cancel 버튼 누르면 Cursor 가 OpenFileDialog 뜸" 등)</resume-signal>
</task>

</tasks>

<verification>
**파일 수준:**
- 수정 파일 4 개: `MainView.xaml`, `MainView.xaml.cs`, `MainResultViewerControl.xaml.cs`, `HalconDisplayService.cs`
- 신규 public 메서드 3 개: `BtnTestFindDatum_Click`, `SetDatumFindResultOverlay`, `ClearDatumFindResultOverlay`, `RenderDatumFindResult` (실제 4 개)
- 신규 public 시그니처 기존 시그니처와 충돌 없음 (이름 중복 0)
- 기존 `SetDatumOverlay`, `RenderDatumOverlay`, `Render` 메서드 본체 변경 없음 (Render 는 분기 1개 추가만)

**수준 검증 (Task 1+2+3 누적):**
```
grep -cF "btn_testFindDatum"                                              → ≥ 3 (XAML 1 + Click 핸들러 XAML + .xaml.cs BtnTestFindDatum_Click)
grep -cF "label_testFindResult"                                            → ≥ 3 (XAML 1 + .xaml.cs Content/Visibility/Foreground 다수 사용)
grep -cE "public void RenderDatumFindResult\("                             → 1
grep -cE "public void SetDatumFindResultOverlay\("                         → 1
grep -cE "public void ClearDatumFindResultOverlay\("                       → 1
grep -cE "private void BtnTestFindDatum_Click\("                           → 1
grep -cE "private HImage AskTestImageSource\("                             → 1
grep -cF "TryFind OK — RefOrigin="                                         → 1
grep -cF "TryFind FAIL — "                                                 → 1
grep -cF "svc.TryFindDatum(testImage, datum, out transform, out error)"    → 1
grep -cF "SetColor(window, \"orange\")"                                    → ≥ 2
grep -cF "//260424 hbk Phase 13"                                           → ≥ 15 (4개 파일 × 신규 라인)
```

**빌드:**
- msbuild Debug/x64 exit 0 (Task 3 완료 후 누적 빌드)
- MainView.xaml / MainView.xaml.cs / MainResultViewerControl.xaml.cs / HalconDisplayService.cs 대상 신규 warning 0 (기존 VirtualCamera.cs CS0162 / VisionAlgorithmService.cs CS0219 / MSB3884 는 Plan 02 scope 밖)

**SIMUL_MODE UAT (Task 4 checkpoint 범위):**
- 6 개 시나리오 전원 PASS → checker:human-verify "approved" 로 진행
- 하나라도 FAIL → 이슈 내용을 resume-signal 로 기록 후 재실행 / 수정

**CONTEXT D-13 회귀 확인:**
- `DatumConfig` public 시그니처 변경 0 (Plan 02 는 DatumConfig 를 읽기만)
- `DatumFindingService.TryFindDatum` 시그니처 변경 0 (호출만)
- `MainView.btn_teachDatum` 관련 기존 메서드 (TeachDatumButton_Click / StartDatumTeachStep / InvokeTryTeachDatum / HalconViewer_DatumRectCompleted / HalconViewer_DatumCircleCompleted) 변경 0
- `HalconDisplayService.RenderDatumOverlay` 변경 0
- `MainResultViewerControl.SetDatumOverlay / ClearDatumOverlay` 변경 0
</verification>

<success_criteria>
- [ ] MainView.xaml 에 `btn_testFindDatum` Button + `label_testFindResult` Label 추가 (XAML 주석 convention 준수)
- [ ] MainView.xaml.cs 에 `BtnTestFindDatum_Click` + `AskTestImageSource` 2 개 메서드 추가 (K&R brace)
- [ ] MainResultViewerControl.xaml.cs 에 `SetDatumFindResultOverlay` + `ClearDatumFindResultOverlay` + `_datumFindResultOverlay` 필드 + Render 분기 추가 (Allman brace)
- [ ] HalconDisplayService.cs 에 `RenderDatumFindResult` 메서드 추가 (Allman brace, 주황 3px 20px half 십자 + 좌표 WriteString)
- [ ] 성공 경로: label_testFindResult 녹색 + 주황 십자 + 좌표 텍스트
- [ ] 실패 경로: label_testFindResult 빨강 + error 문자열 + 오버레이 clear
- [ ] 가드: Datum 미티칭 시 CustomMessageBox → early return (botn 상태 변경 없음)
- [ ] 3-way 다이얼로그: 현재 이미지 / OpenFileDialog / 취소 (현재 이미지 없으면 2-way)
- [ ] OpenFileDialog 필터: png/bmp/jpg/jpeg/tif/tiff (LoadAndDisplay L264 필터 재사용)
- [ ] 기존 SetDatumOverlay / RenderDatumOverlay / teach 흐름 회귀 없음 (CONTEXT D-13)
- [ ] 주석 convention `//260424 hbk Phase 13 D-05..D-08` 모든 신규 라인 준수
- [ ] msbuild Debug/x64 exit 0 + 신규 warning 0 (Task 3 커밋 후)
- [ ] Task 1 commit (XAML) + Task 2+3 commit (코드) = 2 commit
- [ ] Task 4 SIMUL_MODE UAT 6 시나리오 PASS → "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/13-datum-algorithm-extensibility/13-02-SUMMARY.md` following the template structure:
- Files Changed 테이블 (4 파일 modified)
- Gap-4 coverage 매핑 (D-05 버튼 / D-06 이미지 소스 / D-07 성공 오버레이 / D-08 실패 메시지 각각의 구현 위치)
- New public API 카탈로그 (SetDatumFindResultOverlay / ClearDatumFindResultOverlay / RenderDatumFindResult / BtnTestFindDatum_Click / AskTestImageSource)
- UX literal 카탈로그 ("TryFind OK — RefOrigin=..." / "TryFind FAIL — ..." / CustomMessageBox 메시지 / MessageBox YesNoCancel 문구)
- Build Result 블록
- UAT Outcome (Task 4 6 시나리오 결과)
- Deviations from Plan (없으면 "Plan Adherence: All tasks executed as specified")
- Self-Check 체크리스트
</output>
</content>
