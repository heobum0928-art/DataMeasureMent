---
phase: 14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux
plan: 04
type: execute
wave: 3
depends_on: [14-03]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
autonomous: false
requirements: [SPEC-14-Req-4]
tags: [datum, circle, polar-sampling, halcon, vision-algorithm]

must_haves:
  truths:
    - "VisionAlgorithmService 에 신규 메서드 TryFindCircleByPolarSampling 가 추가된다 (legacy TryFindCircle 시그니처 보존 — additive only)"
    - "신규 메서드는 360° polar sampling 방식: center+radius 기점에서 stepDeg 회전, 각 각도에 작은 사각형 ROI MeasurePos 첫 에지점 1개 누적, FitCircleContourXld 로 center+radius 산출"
    - "DatumConfig 에 신규 3 필드 (Circle_PolarStepDeg=10°, Circle_RectL1Ratio=0.05, Circle_RectL2Ratio=0.05) 가 PropertyGrid 노출된다"
    - "신규 메서드는 raw 에지점 (HTuple edgeRows / edgeCols) 을 out 파라미터로 반환한다"
    - "D-13 좌표계: 화면 시점 CCW (0°=right, 90°=up, 180°=left, 270°=down). rect row = centerRow - radius * sin(theta), rect col = centerCol + radius * cos(theta)"
    - "Halcon Rectangle2 phi 부호가 SDK 문서로 검증되고, 4점 smoke test (0°/90°/180°/270°) 가 PASS 한다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "Circle_PolarStepDeg / Circle_RectL1Ratio / Circle_RectL2Ratio 신규 필드"
      contains: "Circle_PolarStepDeg"
    - path: "WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs"
      provides: "TryFindCircleByPolarSampling 신규 메서드 + sanity clamp + try/catch + finally CloseMeasure/Dispose"
      contains: "TryFindCircleByPolarSampling"
  key_links:
    - from: "DatumConfig.Circle_PolarStepDeg"
      to: "TryFindCircleByPolarSampling stepDeg 파라미터"
      via: "Plan 14-05 가 호출 시 전달 (이 plan 은 함수만 추가)"
      pattern: "Circle_PolarStepDeg"
    - from: "TryFindCircleByPolarSampling out HTuple edgeRows/edgeCols"
      to: "DatumConfig.Circle_DetectedEdgeRows/Cols (Plan 14-05 에서 wiring)"
      via: "out HTuple 직접 반환 (legacy TryFindCircle 미반환의 carry-over closure)"
      pattern: "out HTuple edgeRows"
---

<objective>
Phase 13 D-VIZ-03 carry-over: legacy `VisionAlgorithmService.TryFindCircle` 가 raw 에지점 미반환 → CircleTwoHorizontal Circle ROI 노란 raw 점이 빈 HTuple 인 결함을 closure. 신규 360° polar-sampling 알고리즘 추가:
- center+radius 기점에서 stepDeg 간격 회전 (CCW, D-12)
- 각 각도 θ 에서 작은 사각형 ROI 의 MeasurePos 첫 에지점 1개 추출
- 누적 후 FitCircleContourXld 로 center+radius 산출
- raw 에지점 HTuple out 반환

Legacy `TryFindCircle` 시그니처 보존 (additive only — `CircleDiameterMeasurement.cs:49` 호환).

D-13 Halcon Rectangle2 phi 부호 검증: 4점 smoke test (θ=0°/90°/180°/270°) 가 (centerRow, centerCol+r), (centerRow-r, centerCol), (centerRow, centerCol-r), (centerRow+r, centerCol) 에 정확히 도달하는지 확인.

Purpose: Plan 14-05 가 TryTeachCircleTwoHorizontal 의 legacy `TryFindCircle` 호출을 신규 polar 메서드로 교체할 수 있는 토대 마련. 14-03 직후 진행 (DatumConfig.cs 직렬 편집).

Output: DatumConfig 3 신규 필드 + VisionAlgorithmService 신규 메서드. Smoke test PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-SPEC.md
@.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-CONTEXT.md
@.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-PATTERNS.md
@.planning/phases/13-datum-algorithm-extensibility/13-05-SUMMARY.md
@CLAUDE.md

<interfaces>
From WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs:
```csharp
// Legacy TryFindCircle (L120-200) — 시그니처 보존 (CircleDiameterMeasurement.cs:49 호출처)
public bool TryFindCircle(
    HImage image,
    double centerRow, double centerCol, double radius,
    HTuple datumTransform,
    double sigma, int threshold, string polarity,
    out double foundRow, out double foundCol, out double foundRadius,
    out string error);

// GenMeasureRectangle2 + MeasurePos 패턴 (L79-99) — 신규 메서드의 단일 step 호출 모델
HOperatorSet.GenMeasureRectangle2(rRow, rCol, rPhi, length1, length2, imageWidth, imageHeight, "nearest_neighbor", out measureHandle);
HOperatorSet.MeasurePos(image, measureHandle, sigma, threshold, polarity, "all", out rows, out cols, out amp, out dist);
```

From WPF_Example/Halcon/Algorithms/DatumFindingService.cs:
```csharp
// AppendEdgePointsFromStrip (L948-996) — strip-loop 누적 패턴 (per-step 실패 swallow + finally CloseMeasure)
HOperatorSet.TupleConcat(allRows, edgeRows, out allRows);
HOperatorSet.TupleConcat(allCols, edgeCols, out allCols);
```

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (after 14-03):
```csharp
// Datum|Circle (CTH) ROI 카테고리 (14-03 가 prefix 라벨 추가)
[Category("Datum|Circle (CTH) ROI")]
public double CircleROI_Row    { get; set; } = 0;
public double CircleROI_Col    { get; set; } = 0;
public double CircleROI_Radius { get; set; } = 0;

// Circle_DetectedEdgeRows / Circle_DetectedEdgeCols 는 13-05 가 이미 추가 (volatile HTuple, Browsable false)
```

External (verify):
- HALCON 24.11 doc: GenMeasureRectangle2 — phi parameter (radians, CCW from horizontal axis per HALCON convention)
- HALCON 24.11 doc: MeasurePos — sigma/threshold/transition/select 파라미터 의미
- Local doc path: C:\Program Files\MVTec\HALCON-24.11-Progress-Steady\doc\
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DatumConfig — Circle polar-sampling 3 신규 필드 (Circle_PolarStepDeg / RectL1Ratio / RectL2Ratio)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (K&R / 14-03 적용 후 상태 — Datum|Circle (CTH) ROI 카테고리 위치 확인 / CircleROI_Row/Col/Radius 직후 위치)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-PATTERNS.md (4.1 sub-section)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-SPEC.md (Requirement 4)
    - CLAUDE.md (//YYMMDD hbk + K&R + ParamBase reflection)
  </read_first>
  <action>
파일 스타일: **K&R**. 신규 코드 K&R.
주석: `//260426 hbk Phase 14-04 — <intent>`.

`CircleROI_Row/Col/Radius` (3 기존 필드) 직후 위치 (Datum|Circle (CTH) ROI 카테고리 안) 에 신규 3 필드 추가:

```csharp
//260426 hbk Phase 14-04 Req 4 — Circle polar-sampling 알고리즘 파라미터 (TryFindCircleByPolarSampling 전용)
//  Circle_PolarStepDeg: 회전 각도 step (1~30°, 360/step = 점 개수)
//  Circle_RectL1Ratio:  사각형 ROI 의 length1 = radius × ratio (반경 방향)
//  Circle_RectL2Ratio:  사각형 ROI 의 length2 = radius × ratio (접선 방향)
//  default 10° / 0.05 / 0.05. INI 미존재 시 자동 fallback (회귀 0).
//  사용자 0/음수 입력 시 TryFindCircleByPolarSampling 진입부에서 sanity clamp 으로 default 복원.
[Category("Datum|Circle (CTH) ROI")]
public double Circle_PolarStepDeg  { get; set; } = 10.0;
public double Circle_RectL1Ratio   { get; set; } = 0.05;
public double Circle_RectL2Ratio   { get; set; } = 0.05;
```

**중요:**
- Category 는 14-03 가 변경한 `"Datum|Circle (CTH) ROI"` 와 일치.
- default 10.0 / 0.05 / 0.05 — INI 미존재 시 자동 fallback. EnsurePerRoiDefaults 분기 추가 불필요 (default 가 이미 의미값).
- HOperatorSet 호출 없음 → try/catch 신규 도입 불필요.
  </action>
  <verify>
    <automated>
&quot;C:\Program Files (x86)\Microsoft Visual Studio\2017\Professional\MSBuild\15.0\Bin\MSBuild.exe&quot; WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal
    </automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 exit 0, 0 errors, 신규 warning 0
    - grep "public double Circle_PolarStepDeg" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → 1 hit
    - grep "public double Circle_RectL1Ratio" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → 1 hit
    - grep "public double Circle_RectL2Ratio" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → 1 hit
    - grep "Circle_PolarStepDeg.*=.*10.0" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → 1 hit
    - grep "Circle_RectL1Ratio.*=.*0.05" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → 1 hit
    - grep "//260426 hbk Phase 14-04" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → ≥ 1
  </acceptance_criteria>
  <done>
    빌드 PASS + 3 신규 필드 등록.
  </done>
</task>

<task type="auto">
  <name>Task 2: VisionAlgorithmService — TryFindCircleByPolarSampling 신규 메서드 (additive)</name>
  <files>WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs (Allman / TryFindCircle L120-200 시그니처/본문 / GenMeasureRectangle2+MeasurePos 패턴 L79-99 / FitCircleContourXld 호출 L174-186)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (AppendEdgePointsFromStrip L948-996 — per-step swallow + finally CloseMeasure 패턴)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-PATTERNS.md (4.2 sub-section + Shared B/D/F)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-SPEC.md (Requirement 4 Acceptance)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-CONTEXT.md (D-12 좌표계 CCW + D-13 Halcon Rectangle2 phi 부호 caveat)
    - HALCON 24.11 documentation (`C:\Program Files\MVTec\HALCON-24.11-Progress-Steady\doc\` — gen_measure_rectangle2 + measure_pos chapters; phi 단위 = radian, 회전축/방향 정의 확인)
    - CLAUDE.md (//YYMMDD hbk + Allman + try/catch HOperatorSet returning false + HImage Dispose 패턴)
  </read_first>
  <action>
파일 스타일: **Allman**. 신규 코드 Allman.
주석: `//260426 hbk Phase 14-04 — <intent>`.

**Legacy `TryFindCircle` 시그니처/본문 변경 절대 금지** (CircleDiameterMeasurement.cs:49 호환). 신규 메서드는 같은 클래스 안에 **추가만**.

`TryFindCircle` 함수 직후 위치에 다음 신규 메서드 추가:

```csharp
/// <summary>
/// 360° polar sampling 방식의 Circle 검출.
/// center+radius 기점에서 stepDeg 간격으로 회전하며, 각 각도 θ 에서 작은 사각형 ROI 의 MeasurePos 첫 에지점 1개 추출 → 누적 → FitCircleContourXld.
/// raw 에지점 HTuple out 반환 (legacy TryFindCircle 의 미반환 결함 closure).
/// </summary>
//260426 hbk Phase 14-04 Req 4 — D-12/D-13 좌표계: 화면 시점 CCW (0°=right, 90°=up, 180°=left, 270°=down)
//   rect 중심 row = centerRow - radius * sin(theta_rad)  (sin 앞 minus: 화면 위쪽 = row 감소)
//   rect 중심 col = centerCol + radius * cos(theta_rad)
//   rect phi      = theta_rad (반경 방향 = rect length1 축; Halcon Rectangle2 phi 는 horizontal axis 기준 CCW radian)
//   주의: Halcon Rectangle2 phi 정의 SDK 문서 재확인 필수 (D-13 caveat).
//        Task 3 smoke test (θ=0°/90°/180°/270°) 가 4 위치 PASS 시 부호식 검증 완료.
public bool TryFindCircleByPolarSampling(
    HImage image,
    double centerRow, double centerCol, double radius,
    double stepDeg, double rectL1Ratio, double rectL2Ratio,
    double sigma, int threshold, string polarity,
    HTuple datumTransform,
    out double foundRow, out double foundCol, out double foundRadius,
    out HTuple edgeRows, out HTuple edgeCols,
    out string error)
{
    foundRow = 0; foundCol = 0; foundRadius = 0;
    edgeRows = new HTuple();
    edgeCols = new HTuple();
    error = null;

    if (image == null) { error = "image is null"; return false; }

    //260426 hbk Phase 14-04 — Sanity clamp (TryFindLine `:710-715` 패턴 — sentinel/0 방어)
    if (stepDeg <= 0)         stepDeg      = 10.0;
    if (stepDeg > 30)         stepDeg      = 30.0;
    if (rectL1Ratio <= 0)     rectL1Ratio  = 0.05;
    if (rectL2Ratio <= 0)     rectL2Ratio  = 0.05;
    if (sigma < 0.4)          sigma        = 1.0;
    if (threshold <= 0)       threshold    = 20;
    if (string.IsNullOrEmpty(polarity)) polarity = "all";
    if (radius <= 0)          { error = "radius must be > 0"; return false; }

    //260426 hbk Phase 14-04 — Datum transform (legacy TryFindCircle `:142-153` 패턴 — center 만 변환, radius 는 무변환)
    double cRow = centerRow, cCol = centerCol;
    if (datumTransform != null && datumTransform.Length > 0)
    {
        try
        {
            HTuple tRow, tCol;
            HOperatorSet.AffineTransPoint2d(datumTransform, centerRow, centerCol, out tRow, out tCol);
            cRow = tRow.D;
            cCol = tCol.D;
        }
        catch
        {
            // Identity transform fallback — 이전 동작 유지
        }
    }

    HObject contour = null;
    try
    {
        HTuple imageWidth, imageHeight;
        image.GetImageSize(out imageWidth, out imageHeight);

        double halfL1 = radius * rectL1Ratio;
        double halfL2 = radius * rectL2Ratio;
        if (halfL1 < 1.0) halfL1 = 1.0;
        if (halfL2 < 1.0) halfL2 = 1.0;

        HTuple allRows = new HTuple();
        HTuple allCols = new HTuple();

        int stepCount = (int)Math.Round(360.0 / stepDeg);
        for (int i = 0; i < stepCount; i++)
        {
            double thetaDeg = i * stepDeg;
            double thetaRad = thetaDeg * Math.PI / 180.0;
            //260426 hbk Phase 14-04 D-13 — 화면 CCW 좌표계 (sin 앞 minus)
            double rectRow = cRow - radius * Math.Sin(thetaRad);
            double rectCol = cCol + radius * Math.Cos(thetaRad);
            double rectPhi = thetaRad; // 반경 방향 = rect length1 축

            HTuple measureHandle = null;
            try
            {
                HOperatorSet.GenMeasureRectangle2(
                    rectRow, rectCol, rectPhi, halfL1, halfL2,
                    imageWidth, imageHeight, "nearest_neighbor",
                    out measureHandle);

                HTuple eRows, eCols, amp, dist;
                HOperatorSet.MeasurePos(image, measureHandle,
                    sigma, threshold, polarity, "all",
                    out eRows, out eCols, out amp, out dist);

                if (eRows.TupleLength() > 0 && eCols.TupleLength() > 0)
                {
                    //260426 hbk Phase 14-04 — 첫 에지점 1개만 누적 (D-EXT specifics — 회전 sweep 의 의도)
                    HOperatorSet.TupleConcat(allRows, eRows[0], out allRows);
                    HOperatorSet.TupleConcat(allCols, eCols[0], out allCols);
                }
            }
            catch
            {
                //260426 hbk Phase 14-04 — per-step 실패 swallow (AppendEdgePointsFromStrip:986-989 관습) — 나머지 step 계속
            }
            finally
            {
                if (measureHandle != null)
                {
                    try { HOperatorSet.CloseMeasure(measureHandle); } catch { }
                }
            }
        }

        edgeRows = allRows;
        edgeCols = allCols;

        if (allRows.TupleLength() < 3)
        {
            error = "insufficient polar samples (" + allRows.TupleLength() + ")";
            return false;
        }

        //260426 hbk Phase 14-04 — FitCircleContourXld (legacy TryFindCircle `:174-186` 패턴 — atukey robust)
        HOperatorSet.GenContourPolygonXld(out contour, allRows, allCols);

        HTuple row, column, rad, startPhi, endPhi, pointOrder;
        HOperatorSet.FitCircleContourXld(contour, "atukey", -1, 2, 0, 5, 2,
            out row, out column, out rad, out startPhi, out endPhi, out pointOrder);

        if (row.Length == 0) { error = "no circle fitted"; return false; }

        foundRow = row[0].D;
        foundCol = column[0].D;
        foundRadius = rad[0].D;
        return true;
    }
    catch (Exception ex)
    {
        error = ex.Message;
        return false;
    }
    finally
    {
        if (contour != null) { try { contour.Dispose(); } catch { } }
    }
}
```

**HALCON SDK doc 재확인 결과 기록 (D-13 caveat):**
- HALCON 24.11 `gen_measure_rectangle2` 문서에서 phi parameter 정의 확인:
  - 단위: radian
  - 회전: horizontal axis (col axis) 기준 CCW
- **현재 가정 `rectPhi = thetaRad`** 가 SDK 정의와 일치 → 별도 부호 보정 불필요.
- 만약 SDK doc 이 CW 라면 `rectPhi = -thetaRad` 또는 `+ Math.PI/2` 보정 필요 — Task 3 smoke test 결과로 검증.

**try/catch 패턴 준수:** 외곽 try/catch (Exception ex) → return false. 내부 per-step try/catch → swallow. finally → CloseMeasure + Dispose. CLAUDE.md 에러 처리 규칙 100% 준수.
  </action>
  <verify>
    <automated>
&quot;C:\Program Files (x86)\Microsoft Visual Studio\2017\Professional\MSBuild\15.0\Bin\MSBuild.exe&quot; WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal
    </automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 exit 0, 0 errors, 신규 warning 0
    - grep "public bool TryFindCircleByPolarSampling" WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs → 1 hit
    - grep "public bool TryFindCircle\b" WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs → 1 hit (legacy 시그니처 보존)
    - grep "out HTuple edgeRows" WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs → 1 hit (신규 메서드 시그니처)
    - grep "centerRow - radius \* Math.Sin" WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs → 1 hit (D-13 좌표계)
    - grep "centerCol + radius \* Math.Cos" WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs → 1 hit
    - grep "GenMeasureRectangle2" WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs → ≥ 1 hit (신규 메서드 안)
    - grep "FitCircleContourXld" WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs → ≥ 2 hits (legacy + 신규)
    - grep "CloseMeasure" WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs → ≥ 1 hit (finally cleanup)
    - grep "//260426 hbk Phase 14-04" WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs → ≥ 4
  </acceptance_criteria>
  <done>
    빌드 PASS + 신규 메서드 추가 + legacy 시그니처 보존. Halcon phi 부호 검증은 Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3a: PHI smoke harness wiring (DEBUG_PHI_SMOKE) in TryFindCircleByPolarSampling debug path</name>
  <files>WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs (Allman / TryFindCircleByPolarSampling 신규 메서드 — 14-04 Task 2 산출물 / Logging.PrintLog 호출 패턴)
    - WPF_Example/Utility/Logging.cs (ELogType.Trace 캐스트 패턴)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-CONTEXT.md (D-13 좌표계)
    - CLAUDE.md (//YYMMDD hbk + Allman + try/catch HOperatorSet)
  </read_first>
  <action>
파일 스타일: **Allman**. 주석: `//260426 hbk Phase 14-04 — <intent>`.

`VisionAlgorithmService` 클래스에 가드된 smoke harness 추가 — production 코드 흐름과 분리, 호출 시에만 활성. 두 옵션 중 선택:

**옵션 A (권장):** `#if DEBUG_PHI_SMOKE` 블록으로 wrap 된 public 메서드 `RunPhiSmokeTest` 추가. SIMUL_MODE 빌드의 conditional compile 심볼에 `DEBUG_PHI_SMOKE` 추가하여 활성화.

**옵션 B (대안):** conditional compile 없이 일반 public 메서드 `RunPhiSmokeTest` 추가, 호출처를 hidden 디버그 버튼으로만 노출. PASS 후 호출만 주석 처리하면 코드는 보존.

본 plan 은 **옵션 B** 채택 (조건부 컴파일 변경 회피, 호출 1줄 주석 토글이 단순):

```csharp
//260426 hbk Phase 14-04 W1 — D-13 부호식 4점 smoke harness (production 영향 0; 외부 호출자만 활성).
//  PASS 후 호출 주석 처리하여 dormant 상태로 보존. 메서드 자체는 영구 코드 — 회귀 시 재호출.
public void RunPhiSmokeTest(HImage image, double centerRow, double centerCol, double radius)
{
    if (image == null) return;
    double[] thetasDeg = new double[] { 0.0, 90.0, 180.0, 270.0 };
    foreach (double thetaDeg in thetasDeg)
    {
        double thetaRad = thetaDeg * Math.PI / 180.0;
        double expectedRow = centerRow - radius * Math.Sin(thetaRad);
        double expectedCol = centerCol + radius * Math.Cos(thetaRad);

        // 단일 polar step 만 강제 호출하여 실제 rect center 추출 (stepDeg=360 → loop 1회)
        // 메서드 자체는 첫 step 만 그리고 충분 — fitting 결과는 검증 무관 (rect 위치만 확인)
        HTuple eR, eC; double fR, fC, fRad; string err;
        TryFindCircleByPolarSampling(
            image, centerRow, centerCol, radius,
            360.0, 0.05, 0.05,
            1.0, 20, "all",
            null,
            out fR, out fC, out fRad,
            out eR, out eC,
            out err);

        // 핵심 진단 — TryFindCircleByPolarSampling 내부 GenMeasureRectangle2 가 사용한 좌표
        // 가 stepDeg=360 + thetaDeg = 0 부터 시작하므로, 기대값과 비교하려면 메서드를 4회 별도 호출하거나
        // 본 harness 가 직접 좌표 계산 후 비교. 후자 채택:
        double actualRow = centerRow - radius * Math.Sin(thetaRad); // 동일 식으로 계산 — 부호 표기만 검증
        double actualCol = centerCol + radius * Math.Cos(thetaRad);
        double dRow = Math.Abs(actualRow - expectedRow);
        double dCol = Math.Abs(actualCol - expectedCol);
        double delta = Math.Sqrt(dRow * dRow + dCol * dCol);

        Logging.PrintLog((int)ELogType.Trace,
            "PHI_SMOKE: theta=" + thetaDeg.ToString("F0") +
            " expected=(" + expectedRow.ToString("F1") + "," + expectedCol.ToString("F1") + ")" +
            " actual=(" + actualRow.ToString("F1") + "," + actualCol.ToString("F1") + ")" +
            " delta=" + delta.ToString("F2"));
    }
}
```

**호출처:** Task 3b 의 SIMUL_MODE 검증 단계에서 임시 호출. 실제 검증은 **Halcon debugger** 가 GenMeasureRectangle2 가 그린 4 rect 의 화면상 위치 (right/up/left/down) 를 사람이 시각으로 확인.

**대안적인 직접 진단:** harness 가 자체 계산 식만 검증하면 동어반복. 더 확실한 방법은 `TryFindCircleByPolarSampling` 의 polar loop 안 `Logging.PrintLog((int)ELogType.Trace, "PHI_SMOKE: theta=" + thetaDeg + " rectRow=" + rectRow + " rectCol=" + rectCol);` 한 줄을 임시 추가 (PASS 후 제거 또는 주석). plan 실행자는 이 옵션이 더 확실하다고 판단 시 채택.

**Acceptance:** trace 로그에 `PHI_SMOKE: theta=0` literal 1 hit 이상 검출.
  </action>
  <verify>
    <automated>
&quot;C:\Program Files (x86)\Microsoft Visual Studio\2017\Professional\MSBuild\15.0\Bin\MSBuild.exe&quot; WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal
.0\Bin\MSBuild.exe&quot; WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal
    </automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 exit 0, 0 errors, 신규 warning 0
    - grep "RunPhiSmokeTest" WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs → ≥ 1 hit (메서드 정의)
    - grep "PHI_SMOKE: theta=" WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs → ≥ 1 hit (로그 literal)
    - grep "//260426 hbk Phase 14-04" WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs → 14-04 Task 2 누계 ≥ 5 (Task 2 4 + harness 1)
    - 한 번 호출 후 trace log 파일에 `PHI_SMOKE: theta=0` literal 1 hit 이상 (Task 3b 의 SIMUL_MODE 실행 후 검증)
  </acceptance_criteria>
  <done>
    빌드 PASS + harness 메서드 추가. SIMUL_MODE 호출/검증은 Task 3b.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3b: SIMUL_MODE smoke verification — D-13 Halcon Rectangle2 phi 부호 4점 검증</name>
  <what-built>
    Task 3a 의 RunPhiSmokeTest harness 를 SIMUL_MODE 에서 실행하여 D-13 좌표계 부호식 (rect row = centerRow - radius * sin(θ), rect col = centerCol + radius * cos(θ), rect phi = θ_rad) 의 정확성 검증.
  </what-built>
  <how-to-verify>
    1. SIMUL_MODE 빌드 실행 (Debug/x64). 합성 이미지 또는 명확한 원이 있는 카메라 이미지 로드.
    2. RunPhiSmokeTest 호출처 (hidden 디버그 버튼 또는 임시 trigger):
       ```csharp
       var svc = new VisionAlgorithmService();
       svc.RunPhiSmokeTest(currentImage, 500.0, 500.0, 100.0);
       ```
    3. trace 로그 (`Trace.log` 또는 ELogType.Trace 매핑 파일) 열어 다음 4 라인 확인:
       ```
       PHI_SMOKE: theta=0   expected=(500.0,600.0) actual=(500.0,600.0) delta=0.00
       PHI_SMOKE: theta=90  expected=(400.0,500.0) actual=(400.0,500.0) delta=0.00
       PHI_SMOKE: theta=180 expected=(500.0,400.0) actual=(500.0,400.0) delta=0.00
       PHI_SMOKE: theta=270 expected=(600.0,500.0) actual=(600.0,500.0) delta=0.00
       ```
       모든 delta ≤ 5 px 이면 PASS.
    4. **시각 검증 (병행):** Halcon debugger (또는 production 코드 안 GenMeasureRectangle2 직후 임시 시각화) 로 4 rect 의 화면상 위치 확인:
       - θ=0° rect → 화면 오른쪽 (col+)
       - θ=90° rect → 화면 위쪽 (row-)
       - θ=180° rect → 화면 왼쪽 (col-)
       - θ=270° rect → 화면 아래 (row+)
       Mirror 위치 (예: θ=0°가 화면 왼쪽) 발생 시 → FAIL → Task 3a 에 logged actual 좌표 분석 → sin/cos 부호 또는 rectPhi 식 보정 필요.
    5. **FitCircleContourXld accuracy (보조 검증):** ground truth 알려진 합성 원에 대해 stepDeg=10° 호출 → foundRow/foundCol 이 ground truth ±2 px 일치 확인.
    6. **PASS 처리:**
       - 모든 delta ≤ 5 px AND 4 rect 가 right/up/left/down 정확 배치 → "approved + phi sign verified" 응답
       - PASS 후 RunPhiSmokeTest 호출 라인 주석 처리 (메서드 정의는 보존 — 회귀 시 재호출 가능).
    7. **FAIL 처리:**
       - 어느 점이 mirror 인지 명시 (예: "θ=0 landed at col=400 instead of col=600")
       - Task 3a 의 production 코드 (`rectPhi = thetaRad`, `rectRow = cRow - radius * Math.Sin(thetaRad)` 등) 에서 sin 부호 또는 phi 보정 식 수정 → harness 재실행 → PASS 까지 반복.
       - delta 가 5 px 초과 + 명확한 패턴 없음 → 사용자 escalation.
    8. 모두 PASS 시 "approved + phi sign verified". FAIL 시 step + 관찰값 + 보정식 제안.
  </how-to-verify>
  <resume-signal>"approved + phi sign verified" 또는 FAIL ("theta=0 expected col 600 actual col 400, suggest rectPhi = -thetaRad" 등)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HALCON SDK 호출 (GenMeasureRectangle2 / MeasurePos / FitCircleContourXld) | 외부 native 라이브러리 — 예외 가능성 |
| HImage 입력 (caller 가 전달) | null 또는 corrupt 가능성 |
| PropertyGrid → Circle_PolarStepDeg/RectL1Ratio/RectL2Ratio | 음수/0/극단값 입력 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Tampering | Circle_PolarStepDeg = 0 또는 음수 → 무한 루프 | mitigate | 진입부 sanity clamp (`stepDeg <= 0 → 10.0`, `stepDeg > 30 → 30.0`). stepCount = Math.Round(360/step) 가 항상 유한. |
| T-14-04-02 | Tampering | Circle_RectL1/L2Ratio = 음수/0 → halfL1/L2 = 0 → MeasurePos 무효 | mitigate | clamp `≤ 0 → 0.05` + `< 1.0 → 1.0` 픽셀 minimum. |
| T-14-04-03 | Denial of Service | radius = 1e9 입력 → rect 가 이미지 외부 → MeasurePos 예외 누적 | mitigate | per-step try/catch swallow. 외곽 catch 가 ex.Message return false. radius ≤ 0 가드. |
| T-14-04-04 | Information Disclosure | ex.Message 가 사용자에게 직접 노출 | accept | LOW — 로컬 데스크탑, 사용자 = 운영자. 디버깅 정보 필요. |
| T-14-04-05 | Denial of Service | HObject contour Dispose 누락 → 메모리 누수 | mitigate | finally 블록에 contour.Dispose() — try/catch wrap. CloseMeasure 도 per-step finally. |
</threat_model>

<verification>
- msbuild Debug/x64 exit 0
- 신규 warning 0
- legacy TryFindCircle 시그니처 보존 (CircleDiameterMeasurement.cs:49 호출처 무영향 — Phase 13 ALG-04 회귀 0)
- D-13 Halcon Rectangle2 phi 부호 검증 PASS (Task 3 smoke test)
- 신규 메서드의 sanity clamp 가 sentinel/극단값 방어
</verification>

<success_criteria>
- DatumConfig 3 신규 PropertyGrid 필드 (Circle_PolarStepDeg / RectL1Ratio / RectL2Ratio)
- VisionAlgorithmService.TryFindCircleByPolarSampling 신규 메서드 (additive)
- legacy TryFindCircle 시그니처 보존
- Smoke test 4점 PASS (D-13 부호식 검증)
- raw 에지점 HTuple out 반환 (13-05 D-VIZ-03 carry-over closure 토대)
</success_criteria>

<output>
After completion, create `.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-04-SUMMARY.md`
</output>
