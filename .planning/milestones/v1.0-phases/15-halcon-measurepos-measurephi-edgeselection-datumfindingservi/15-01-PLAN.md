---
phase: 15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
autonomous: true
requirements:
  - PHASE15-Req-EdgeSelection-Field
  - PHASE15-Req-INI-Compat
must_haves:
  truths:
    - "DatumConfig 가 6개 *_EdgeSelection 프로퍼티를 가진다 (Line1/Line2/Vertical/Circle/Horizontal_A/Horizontal_B)"
    - "각 *_EdgeSelection 은 PropertyGrid 에서 [First, Last, All] 드롭다운으로 노출된다"
    - "기존 INI(레시피) 로드 후 EnsurePerRoiDefaults() 가 호출되면 비어있는 *_EdgeSelection 6 개가 모두 \"First\" 로 채워진다"
    - "EdgeOptionLists.Selections 정적 리스트가 [\"First\", \"Last\", \"All\"] 순서로 노출된다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs"
      provides: "Selections static list"
      contains: "Selections"
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "6 *_EdgeSelection properties + 6 *_EdgeSelectionList accessors + 6 EnsurePerRoiDefaults fallback lines"
      contains: "_EdgeSelection"
  key_links:
    - from: "DatumConfig 6 *_EdgeSelection properties"
      to: "EdgeOptionLists.Selections"
      via: "ItemsSourceProperty(*_EdgeSelectionList) -> List<string> getter -> EdgeOptionLists.Selections"
      pattern: "EdgeOptionLists\\.Selections"
    - from: "DatumConfig.EnsurePerRoiDefaults"
      to: "*_EdgeSelection 기본값 \"First\""
      via: "if (string.IsNullOrEmpty(*_EdgeSelection)) *_EdgeSelection = \"First\""
      pattern: "_EdgeSelection = \"First\""
---

<objective>
DatumConfig 의 6 ROI(Line1, Line2, Vertical, Circle, Horizontal_A, Horizontal_B) 각각에 `*_EdgeSelection` 프로퍼티를 추가하여 사용자가 PropertyGrid 에서 First/Last/All 을 선택할 수 있게 한다. 기존 INI 레시피(EdgeSelection 필드 부재)는 EnsurePerRoiDefaults() 의 6 라인 fallback 으로 자동 "First" 채움 — 무손실 하위호환.

이 플랜은 데이터 모델만 변경하며 런타임 동작은 바꾸지 않는다 (Plan 15-02/03/04 가 새 필드를 실제로 소비). EdgeOptionLists.Selections 정적 리스트를 신규 추가해 ItemsSource 단일 소스 원칙(EdgeDirection / EdgePolarity 와 동일 패턴)을 유지한다.

Purpose: HALCON MeasurePos 의 selection 인자를 사용자가 명시 제어할 수 있는 단일 진실 원천(DatumConfig) 을 마련한다.
Output: EdgeOptionLists.cs (Selections 1 라인 추가) + DatumConfig.cs (6 프로퍼티 + 6 list accessor + 6 fallback 라인 추가).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-CONTEXT.md
@CLAUDE.md
@WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs
@WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs

<interfaces>
<!-- Existing pattern in EdgeOptionLists.cs (extend, do NOT redesign) -->
```csharp
namespace ReringProject.Sequence
{
    public static class EdgeOptionLists
    {
        public static readonly List<string> Directions      = new List<string> { "LtoR", "RtoL", "TtoB", "BtoT" };
        public static readonly List<string> FAIPolarities   = new List<string> { "DarkToLight", "LightToDark" };
        public static readonly List<string> DatumPolarities = new List<string> { "all", "positive", "negative" };
        // ADD HERE: Selections (Halcon MeasurePos 'first'/'last'/'all'; Datum UI 표기 = First/Last/All)
    }
}
```

<!-- Existing per-ROI EdgeDirection pattern in DatumConfig.cs (lines 109-119, Line1 group; same for Line2/Vertical/Circle/H_A/H_B) -->
```csharp
[ItemsSourceProperty(nameof(Line1_EdgeDirectionList))]
public string Line1_EdgeDirection   { get; set; } = "";
// ...
[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> Line1_EdgeDirectionList { get { return EdgeOptionLists.Directions; } }
[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> Line1_EdgePolarityList  { get { return EdgeOptionLists.DatumPolarities; } }
```

<!-- Existing EnsurePerRoiDefaults() Line1 fallback block (lines 402-408) -->
```csharp
// Line1
if (Line1_EdgeThreshold == 0)        Line1_EdgeThreshold = fbThreshold;
if (Line1_Sigma == 0)                Line1_Sigma = fbSigma;
if (string.IsNullOrEmpty(Line1_EdgeDirection)) Line1_EdgeDirection = fbDirection;
if (Line1_EdgeSampleCount == 0)      Line1_EdgeSampleCount = fbSampleCount;
if (Line1_EdgeTrimCount == 0)        Line1_EdgeTrimCount = fbTrimCount;
if (string.IsNullOrEmpty(Line1_EdgePolarity)) Line1_EdgePolarity = fbPolarity;
// ADD: if (string.IsNullOrEmpty(Line1_EdgeSelection)) Line1_EdgeSelection = fbSelection;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: EdgeOptionLists.Selections 정적 리스트 추가</name>
  <files>WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs (전체 22 라인)
    - .planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-CONTEXT.md (LOCKED 결정 — Selections = ["First","Last","All"])
  </read_first>
  <action>
`EdgeOptionLists` 정적 클래스에 Selections 리스트를 추가한다. 기존 Directions / FAIPolarities / DatumPolarities 와 동일한 K&R 한 줄 패턴 유지.

추가할 라인 (DatumPolarities 아래에 1 라인 + XML 주석 1 라인):

```csharp
// MeasurePos 'first'/'last'/'all' 의 Datum UI 표기. //260429 hbk Phase 15 — DatumConfig.*_EdgeSelection ItemsSource 단일 소스
public static readonly List<string> Selections = new List<string> { "First", "Last", "All" }; //260429 hbk Phase 15
```

문자열 값은 PascalCase ("First"/"Last"/"All") — DatumConfig 저장 포맷과 동일하게 하여 Equality 체크 단순화. 실제 Halcon 호출시 lower-case 변환은 소비자(AppendEdgePointsFromStrip — Plan 15-02) 책임.
  </action>
  <verify>
    <automated>grep -n "Selections" "C:/Info/Project/DataMeasurement/WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs"</automated>
  </verify>
  <done>
    - Selections 정적 리스트가 EdgeOptionLists 안에 존재
    - 값 = ["First","Last","All"] (정확한 순서, 정확한 대소문자)
    - //260429 hbk 주석 1개 이상 존재 (신규 라인)
    - 기존 Directions / FAIPolarities / DatumPolarities 변경 없음
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DatumConfig 6 *_EdgeSelection 프로퍼티 + ItemsSource accessor 추가</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (lines 100-310 — 6 ROI EdgeDirection/EdgePolarity 패턴 확인)
    - WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs (Task 1 산출물 — Selections 사용)
  </read_first>
  <action>
DatumConfig 의 6 ROI 각각에 `*_EdgeSelection` 프로퍼티 + `*_EdgeSelectionList` 접근자를 추가한다. 기존 EdgeDirection/EdgePolarity 패턴(`[ItemsSourceProperty(nameof(*_List))]` + `[Browsable(false)]` list getter) 그대로 복제.

각 ROI 그룹별 정확한 삽입 위치 (해당 ROI 의 EdgePolarity 라인 바로 뒤):

**Line1 그룹 (line 113-119 부근)** — `Line1_EdgePolarity` 라인 다음에 추가:
```csharp
[ItemsSourceProperty(nameof(Line1_EdgeSelectionList))] //260429 hbk Phase 15 — EdgeSelection 명시 처리
public string Line1_EdgeSelection { get; set; } = ""; //260429 hbk Phase 15
```
그리고 `Line1_EdgePolarityList` 접근자 다음에 추가:
```csharp
[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> Line1_EdgeSelectionList { get { return EdgeOptionLists.Selections; } } //260429 hbk Phase 15
```

**Vertical 그룹 (line 144-150 부근)** — Vertical_EdgePolarity / List 다음에 동일 패턴 (`Vertical_EdgeSelection` + `Vertical_EdgeSelectionList`).

**Line2 그룹 (line 179-185 부근)** — Line2_EdgePolarity / List 다음에 (`Line2_EdgeSelection` + `Line2_EdgeSelectionList`).

**Circle 그룹 (line 216-222 부근)** — Circle_EdgePolarity / List 다음에 (`Circle_EdgeSelection` + `Circle_EdgeSelectionList`).

**Horizontal_A 그룹 (line 253-259 부근)** — Horizontal_A_EdgePolarity / List 다음에 (`Horizontal_A_EdgeSelection` + `Horizontal_A_EdgeSelectionList`).

**Horizontal_B 그룹 (line 288-294 부근)** — Horizontal_B_EdgePolarity / List 다음에 (`Horizontal_B_EdgeSelection` + `Horizontal_B_EdgeSelectionList`).

규칙:
- 모든 신규 라인에 `//260429 hbk Phase 15` 주석.
- 초기값 = `""` sentinel (EnsurePerRoiDefaults 가 채울 것 — Task 3).
- Category 신규 추가 X (해당 그룹의 기존 Category — 예: `[Category("Datum|Line1 (TLI) Edge")]` — 가 그대로 적용됨).
- accessor list getter 도 6 개 — 각 ROI 별 별도 getter 유지 (PropertyTools 가 nameof 로 참조 가능해야 하므로 공유 불가).
  </action>
  <verify>
    <automated>grep -c "_EdgeSelection " "C:/Info/Project/DataMeasurement/WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"</automated>
  </verify>
  <done>
    - `grep -c "public string .*_EdgeSelection " DatumConfig.cs` ≥ 6
    - `grep -c "_EdgeSelectionList { get" DatumConfig.cs` ≥ 6
    - `grep -c "ItemsSourceProperty(nameof(.*_EdgeSelectionList))" DatumConfig.cs` ≥ 6
    - **Sentinel 빈 문자열 검증 (Info 8)**: `grep -c "_EdgeSelection { get; set; } = """ DatumConfig.cs` == 6 — 6 ROI 모두 sentinel "" 로 초기화 ("First" 하드코딩 방지; EnsurePerRoiDefaults 가 채울 것)
    - 모든 신규 라인에 `//260429 hbk` 주석
    - 빌드: msbuild Debug/x64 PASS, 신규 warning 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: EnsurePerRoiDefaults() 6 *_EdgeSelection fallback 추가 (INI 하위호환)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (lines 393-462 — EnsurePerRoiDefaults 전체 구조)
    - 15-CONTEXT.md (LOCKED: 기본값 "First")
  </read_first>
  <action>
`EnsurePerRoiDefaults()` 메서드 안에 6 ROI 각각의 EdgeSelection sentinel 분기를 추가한다. 기존 EdgePolarity fallback 패턴과 동일.

1. 메서드 상단의 fallback 변수 블록(line 394-400 부근, `string fbPolarity = ...` 라인 다음)에 추가:
```csharp
string fbSelection = "First"; //260429 hbk Phase 15 — EdgeSelection 기본값 (Halcon MeasurePos "first" 와 매핑)
```

2. 각 ROI 의 fallback 블록 끝(EdgePolarity 라인 바로 뒤)에 1 라인씩 추가:

**Line1 (line 408 다음):**
```csharp
if (string.IsNullOrEmpty(Line1_EdgeSelection)) Line1_EdgeSelection = fbSelection; //260429 hbk Phase 15
```

**Line2 (line 416 다음):**
```csharp
if (string.IsNullOrEmpty(Line2_EdgeSelection)) Line2_EdgeSelection = fbSelection; //260429 hbk Phase 15
```

**Circle (line 424 다음):**
```csharp
if (string.IsNullOrEmpty(Circle_EdgeSelection)) Circle_EdgeSelection = fbSelection; //260429 hbk Phase 15
```

**Horizontal_A (line 432 다음):**
```csharp
if (string.IsNullOrEmpty(Horizontal_A_EdgeSelection)) Horizontal_A_EdgeSelection = fbSelection; //260429 hbk Phase 15
```

**Horizontal_B (line 440 다음):**
```csharp
if (string.IsNullOrEmpty(Horizontal_B_EdgeSelection)) Horizontal_B_EdgeSelection = fbSelection; //260429 hbk Phase 15
```

**Vertical (Vertical 그룹 마이그레이션 블록 — line 451 `Vertical_EdgePolarity` 분기 바로 뒤)** — Vertical 은 Line1 으로부터 복사하지 않고 fbSelection 만 사용 (의미상 별도 그룹):
```csharp
if (string.IsNullOrEmpty(Vertical_EdgeSelection)) Vertical_EdgeSelection = fbSelection; //260429 hbk Phase 15
```

규칙:
- 멱등성 보장 (sentinel == "" 일 때만 채움 — 사용자가 한 번이라도 값 지정하면 보존).
- 모든 신규 라인에 `//260429 hbk Phase 15` 주석.
- 기존 분기 순서/들여쓰기/한 줄 if 스타일 그대로 유지.
  </action>
  <verify>
    <automated>grep -c "_EdgeSelection = fbSelection" "C:/Info/Project/DataMeasurement/WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"</automated>
  </verify>
  <done>
    - `grep -c "_EdgeSelection = fbSelection" DatumConfig.cs` == 6
    - `grep -n "fbSelection = \"First\"" DatumConfig.cs` 1 매치 (선언 라인)
    - 빌드: msbuild Debug/x64 PASS, warning 0
    - INI 하위호환: 기존 Setting.ini (EdgeSelection 필드 부재) 로드 후 EnsurePerRoiDefaults() 호출 시 6 ROI 모두 EdgeSelection == "First" — 다음 plan 의 helper 가 이 값을 정상 소비
  </done>
</task>

</tasks>

<verification>
- 빌드: `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64` PASS, 신규 warning 0
- grep 검증:
  - `grep -c "Selections = new List<string>" EdgeOptionLists.cs` == 1
  - `grep -c "_EdgeSelection " DatumConfig.cs` ≥ 6 (프로퍼티 — trailing space)
  - `grep -c "_EdgeSelectionList " DatumConfig.cs` ≥ 6 (accessor)
  - `grep -c "_EdgeSelection = fbSelection" DatumConfig.cs` == 6
- INI 회귀 (수동, 선택): 기존 Setting.ini 로드 → 6 ROI EdgeSelection 가 "First" 로 채워짐 (Plan 15-02 helper 호출 후 자동 검증됨)
</verification>

<success_criteria>
1. EdgeOptionLists.Selections 가 ["First","Last","All"] 로 노출
2. DatumConfig 6 ROI 각각 *_EdgeSelection (string, sentinel="") + *_EdgeSelectionList (List<string>) 프로퍼티 보유
3. PropertyGrid 에서 6 EdgeSelection 모두 드롭다운으로 노출 (기존 EdgeDirection/EdgePolarity 와 동일 UX)
4. EnsurePerRoiDefaults() 호출 후 sentinel "" 가 모두 "First" 로 자동 채워짐 (INI 하위호환)
5. 빌드 PASS, 신규 warning 0
</success_criteria>

<output>
After completion, create `.planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-01-SUMMARY.md` with: files changed, line counts, grep evidence, INI 하위호환 검증 노트.
</output>
