---
phase: 15
plan: 03
type: execute
wave: 3
depends_on: [15-01, 15-02]
files_modified:
  - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
autonomous: true
requirements:
  - PHASE15-Req-Circle-Selection
must_haves:
  truths:
    - "TryFindCircleByPolarSampling 시그니처에 string selection 파라미터가 추가되어 있다"
    - "TryFindCircleByPolarSampling 안의 MeasurePos 호출이 \"all\" 하드코딩 대신 selection 변수(소문자 변환)를 사용한다"
    - "rectPhi = thetaRad 회전 로직은 그대로 유지 (Phase 14-04 의 polar sampling 의도 보존)"
    - "DatumFindingService.TryTeachCircleTwoHorizontal 의 TryFindCircleByPolarSampling 호출이 config.Circle_EdgeSelection 인자를 전달한다"
  artifacts:
    - path: "WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs"
      provides: "TryFindCircleByPolarSampling(selection) — 명시적 selection"
      contains: "selection"
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "TryTeachCircleTwoHorizontal Circle 검출 — Circle_EdgeSelection wiring"
      contains: "Circle_EdgeSelection"
  key_links:
    - from: "TryTeachCircleTwoHorizontal"
      to: "TryFindCircleByPolarSampling"
      via: "config.Circle_EdgeSelection 인자"
      pattern: "Circle_EdgeSelection"
    - from: "TryFindCircleByPolarSampling"
      to: "HOperatorSet.MeasurePos"
      via: "selectionLower 변수 (\"all\" 하드코딩 제거)"
      pattern: "MeasurePos\\([^)]*selectionLower"
---

<objective>
`VisionAlgorithmService.TryFindCircleByPolarSampling` (line 214-340) 의 MeasurePos 가 `"all"` 하드코딩 + `eRows[0]` 인덱스 0 만 누적하는 패턴을 정리하여, 호출자가 사용자 의도(`Circle_EdgeSelection`)에 맞게 명시 제어할 수 있게 한다. Phase 14-04 의 핵심 회전 의도(`rectPhi = thetaRad` — 반경 방향 측정)는 **그대로 유지** — 이 plan 은 selection 인자화만 정리.

DatumFindingService.TryTeachCircleTwoHorizontal 의 호출도 함께 wiring (`config.Circle_EdgeSelection` 전달).

> **Wave/depends_on 일탈 노트 (Blocker 1 — revision iteration 1):**
> 본 plan 은 `wave: 3, depends_on: [15-01, 15-02]` 로 설정. CONTEXT.md `<specifics>` line 130 은 원래 "15-04 (당시 번호) 는 15-01 만 의존, parallel with 15-02/03 가능" 으로 권고했으나, planner 는 같은 file (DatumFindingService.cs) 을 새 15-02 (helper signature change + 7 caller wiring 병합본) 가 광범위하게 수정하기 때문에 same-file 충돌 회피 규칙에 따라 15-02 이후 직렬 실행으로 결정. CONTEXT 의 "parallel with 15-02/03" 권고는 same-file 충돌 회피 규칙(`files_modified` 겹침 → 후행 wave 강제) 에 의해 의도적으로 오버라이드됨.
> (참고: revision iteration 1 에서 원래 15-02+15-03 두 plan 을 단일 wave-2 plan 으로 병합했기 때문에 본 plan 의 새 의존성은 [15-01, 15-02-merged] 이고 wave 는 3 — 원래 wave 4 가 아님. 병합으로 단계 수가 1 줄어들어 자연스럽게 1 wave 앞당겨짐.)

> **Circle accumulation 정책 노트 (Warning 4):**
> 본 plan 은 `selection == "All"` 일 때 eRows 전체 누적, 그 외 (First/Last) 는 eRows[0] 단일점 누적으로 정책 분기를 도입한다. 이는 CONTEXT 결정 "단순화 (eRows[0] 인덱싱 제거)" 의 자연 확장 — Phase 14-04 smoke test 의 360° polar sample 개수 기대값(`stepCount`) 을 보존하기 위해 First/Last 분기는 기존 단일점 누적을 유지.

Purpose: Circle 360° polar sampling 에서도 사용자의 EdgeSelection 의도가 반영되도록 정합화. (예: "First" 면 회전 sweep 의 첫 에지점만 / "All" 면 모든 에지점 누적 — 기존 동작과의 차이는 미세하지만 의미적 일관성 회복.)
Output: VisionAlgorithmService.cs (시그니처 + MeasurePos 인자 정리) + DatumFindingService.cs (Circle 호출 wiring 1 사이트).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-CONTEXT.md
@.planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-01-PLAN.md
@.planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-02-PLAN.md
@CLAUDE.md
@WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
@WPF_Example/Halcon/Algorithms/DatumFindingService.cs

<interfaces>
<!-- TryFindCircleByPolarSampling current signature (line 214-222) -->
```csharp
public bool TryFindCircleByPolarSampling(
    HImage image,
    double centerRow, double centerCol, double radius,
    double stepDeg, double rectL1Ratio, double rectL2Ratio,
    double sigma, int threshold, string polarity,
    HTuple datumTransform,
    out double foundRow, out double foundCol, out double foundRadius,
    out HTuple edgeRows, out HTuple edgeCols,
    out string error)
```

<!-- TARGET signature -->
```csharp
public bool TryFindCircleByPolarSampling(
    HImage image,
    double centerRow, double centerCol, double radius,
    double stepDeg, double rectL1Ratio, double rectL2Ratio,
    double sigma, int threshold, string polarity, string selection,    //260429 hbk Phase 15
    HTuple datumTransform,
    out double foundRow, out double foundCol, out double foundRadius,
    out HTuple edgeRows, out HTuple edgeCols,
    out string error)
```

<!-- CURRENT problematic loop body (line 282-300) -->
```csharp
HOperatorSet.GenMeasureRectangle2(
    rectRow, rectCol, rectPhi, halfL1, halfL2,
    imageWidth, imageHeight, "nearest_neighbor",
    out measureHandle);

HTuple eRows, eCols, amp, dist;
HOperatorSet.MeasurePos(image, measureHandle,
    sigma, threshold, polarity, "all",                  // ← 하드코딩
    out eRows, out eCols, out amp, out dist);

if (eRows.TupleLength() > 0 && eCols.TupleLength() > 0)
{
    HOperatorSet.TupleConcat(allRows, eRows[0], out allRows);  // ← [0] 인덱스 (사실상 first 효과)
    HOperatorSet.TupleConcat(allCols, eCols[0], out allCols);
}
```

<!-- Caller in DatumFindingService (line 338-346) -->
```csharp
if (!visionSvc.TryFindCircleByPolarSampling(
        image,
        config.CircleROI_Row, config.CircleROI_Col, config.CircleROI_Radius,
        config.Circle_PolarStepDeg, config.Circle_RectL1Ratio, config.Circle_RectL2Ratio,
        config.Circle_Sigma, config.Circle_EdgeThreshold, config.Circle_EdgePolarity,
        null, // teaching-phase identity transform
        out centerRow, out centerCol, out radius,
        out circleEdgeRows, out circleEdgeCols,
        out circleError))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: TryFindCircleByPolarSampling 시그니처 + MeasurePos selection 인자화 + smoke harness caller 검증</name>
  <files>WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs lines 200-347 (TryFindCircleByPolarSampling 전체)
    - 15-CONTEXT.md (LOCKED: rectPhi=thetaRad 보존, selection 만 정리, 기본 "first")
    - WPF_Example/Halcon/Algorithms/MeasurementAlgorithm.cs lines 175-180 (CANONICAL selection 변환)
  </read_first>
  <action>
**A. 시그니처 변경 (line 214-222)** — `string polarity,` 다음에 `string selection,` 삽입:
```csharp
public bool TryFindCircleByPolarSampling(
    HImage image,
    double centerRow, double centerCol, double radius,
    double stepDeg, double rectL1Ratio, double rectL2Ratio,
    double sigma, int threshold, string polarity, string selection,  //260429 hbk Phase 15 — selection 명시 처리
    HTuple datumTransform,
    out double foundRow, out double foundCol, out double foundRadius,
    out HTuple edgeRows, out HTuple edgeCols,
    out string error)
```

**B. sanity clamp 추가** (line 238 부근, polarity clamp 다음):
```csharp
if (string.IsNullOrEmpty(selection)) selection = "First"; //260429 hbk Phase 15
//260429 hbk Phase 15 — PascalCase → Halcon lower-case 변환
string selectionLower =
    string.Equals(selection, "Last", StringComparison.OrdinalIgnoreCase) ? "last" :
    string.Equals(selection, "All",  StringComparison.OrdinalIgnoreCase) ? "all"  : "first";
```

**C. MeasurePos 호출 수정 (line 291-293)**:
```csharp
HTuple eRows, eCols, amp, dist;
HOperatorSet.MeasurePos(image, measureHandle,
    sigma, threshold, polarity, selectionLower,         //260429 hbk Phase 15 — "all" → selectionLower
    out eRows, out eCols, out amp, out dist);
```

**D. eRows 인덱싱 정리 (line 295-300)** — 기존 [0] 인덱싱은 selection="first" 일 때 사실상 동일하지만 의미적으로 selection 의 의도가 lower 변환에서 처리되므로 selection 별 정책 분기를 도입한다. **단, Phase 14-04 의 의도(360° sweep × 각도당 1점) 를 보존**해야 하므로 다음 정책 적용:
- selection == "First"/"Last" → 기존 그대로 [0] 인덱스 유지 (Halcon 이 first/last 모드에서 1점만 반환).
- selection == "All" → eRows 전체 누적 (각 step 에서 모든 에지점 — 360° × N 형태).

이를 위해 본문 변경:
```csharp
if (eRows.TupleLength() > 0 && eCols.TupleLength() > 0)
{
    //260429 hbk Phase 15 — selection 정책 반영: First/Last 는 1점 (Phase 14-04 stepCount 보존), All 은 전체 누적
    if (string.Equals(selectionLower, "all", StringComparison.OrdinalIgnoreCase))
    {
        HOperatorSet.TupleConcat(allRows, eRows, out allRows);
        HOperatorSet.TupleConcat(allCols, eCols, out allCols);
    }
    else
    {
        HOperatorSet.TupleConcat(allRows, eRows[0], out allRows);
        HOperatorSet.TupleConcat(allCols, eCols[0], out allCols);
    }
}
```

**E. rectPhi = thetaRad 보존** (line 280) — **변경 금지**. Phase 14-04 D-13 의 핵심 의도. 검증: 이 라인의 위/아래 주석 그대로 유지.

**F. smoke harness 및 기타 caller 검증 (Warning 5 — 정의적 처리)**:

다음 grep 으로 TryFindCircleByPolarSampling 의 모든 caller 를 명시적으로 식별 (정의 라인 제외):
```bash
grep -rn "TryFindCircleByPolarSampling" "C:/Info/Project/DataMeasurement/WPF_Example/" | grep -v "public bool TryFindCircleByPolarSampling"
```

**처리 규칙**:
- 발견된 모든 caller 사이트에 selection 인자 추가 — smoke harness 가 호출하면 `"First"` 전달, DatumFindingService.TryTeachCircleTwoHorizontal 는 Task 2 에서 별도 처리 (config.Circle_EdgeSelection 전달).
- 발견된 caller 가 DatumFindingService.cs 의 1 사이트만이라면 본 Task 에서는 추가 작업 없음 (Task 2 가 처리). SUMMARY 에 "no other callers in solution" 명시.
- 발견된 caller 가 그 외 파일(예: VisionAlgorithmService 내부 smoke harness, 테스트 코드)에도 있다면 본 Task 에서 즉시 selection 인자 추가 후 SUMMARY 에 caller 표 기록.

규칙:
- 모든 변경 라인에 `//260429 hbk Phase 15` 주석.
- C# 7.2 한정.
  </action>
  <verify>
    <automated>grep -n "selectionLower" "C:/Info/Project/DataMeasurement/WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs"</automated>
  </verify>
  <done>
    - `grep -c "selectionLower" VisionAlgorithmService.cs` ≥ 3 (변환 + MeasurePos 호출 + selection All 분기)
    - `grep "MeasurePos.*\"all\"" VisionAlgorithmService.cs` 0 매치 (TryFindCircleByPolarSampling 안에서)
    - `grep "rectPhi = thetaRad" VisionAlgorithmService.cs` ≥ 1 (보존 확인)
    - 시그니처에 `string selection` 추가 — `grep "string polarity, string selection" VisionAlgorithmService.cs` ≥ 1
    - **모든 caller 검증 (Warning 5)**: `grep -rn "TryFindCircleByPolarSampling" WPF_Example/ | grep -v "public bool TryFindCircleByPolarSampling"` 실행 후 — DatumFindingService.cs 외 caller 가 발견되면 모두 selection 인자 추가됨; 없으면 SUMMARY 에 "no other callers in solution" 명시.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DatumFindingService.TryTeachCircleTwoHorizontal Circle 호출에 Circle_EdgeSelection 전달 + 통합 빌드 검증</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs lines 320-360 (TryFindCircleByPolarSampling 호출부)
    - 본 plan Task 1 의 시그니처 변경 (selection 위치 — polarity 다음)
  </read_first>
  <action>
DatumFindingService.cs 의 `TryTeachCircleTwoHorizontal` 안 `TryFindCircleByPolarSampling` 호출(line 338-346 부근) 에 `config.Circle_EdgeSelection` 인자를 추가한다.

기존 호출:
```csharp
if (!visionSvc.TryFindCircleByPolarSampling(
        image,
        config.CircleROI_Row, config.CircleROI_Col, config.CircleROI_Radius,
        config.Circle_PolarStepDeg, config.Circle_RectL1Ratio, config.Circle_RectL2Ratio,
        config.Circle_Sigma, config.Circle_EdgeThreshold, config.Circle_EdgePolarity,
        null, // teaching-phase identity transform
        out centerRow, out centerCol, out radius,
        out circleEdgeRows, out circleEdgeCols,
        out circleError))
```

수정 후:
```csharp
if (!visionSvc.TryFindCircleByPolarSampling(
        image,
        config.CircleROI_Row, config.CircleROI_Col, config.CircleROI_Radius,
        config.Circle_PolarStepDeg, config.Circle_RectL1Ratio, config.Circle_RectL2Ratio,
        config.Circle_Sigma, config.Circle_EdgeThreshold, config.Circle_EdgePolarity,
        config.Circle_EdgeSelection,                       //260429 hbk Phase 15 — selection 전파 (DatumConfig.Circle_EdgeSelection)
        null, // teaching-phase identity transform
        out centerRow, out centerCol, out radius,
        out circleEdgeRows, out circleEdgeCols,
        out circleError))
```

규칙:
- `config.Circle_EdgeSelection` 위치는 polarity 다음, datumTransform(`null`) 앞 — Task 1 시그니처와 정확히 일치.
- 라인 변경에 `//260429 hbk Phase 15` 주석.

**빌드 검증 (필수, 통합)**:
```bash
msbuild "C:/Info/Project/DataMeasurement/WPF_Example/DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /v:minimal
```
- Plan 15-01 + 15-02 + 15-03 누적 후 PASS 필수.
- 신규 warning 0 (DatumFindingService.cs / VisionAlgorithmService.cs).

**동시성 주의**: 본 plan 은 DatumFindingService.cs 도 수정 — Plan 15-02 와 동일 파일. Wave 분리 (15-02 wave 2 → 15-03 wave 3) 로 직렬 실행 보장.
  </action>
  <verify>
    <automated>grep -n "config.Circle_EdgeSelection" "C:/Info/Project/DataMeasurement/WPF_Example/Halcon/Algorithms/DatumFindingService.cs"</automated>
  </verify>
  <done>
    - `grep -c "config.Circle_EdgeSelection" DatumFindingService.cs` ≥ 1 (Circle 호출 사이트)
    - 빌드 PASS: `msbuild DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64` exit 0
    - 신규 warning 0 (수정 두 파일 한정)
    - 회귀: TwoLineIntersect / VerticalTwoHorizontal 영향 0 (Circle 분기만 수정)
  </done>
</task>

</tasks>

<verification>
- 빌드 (Plan 15-03 단독 + 15-01/15-02 누적): msbuild PASS, warning 0
- grep:
  - `grep -c "selectionLower" VisionAlgorithmService.cs` ≥ 3
  - `grep -c "config.Circle_EdgeSelection" DatumFindingService.cs` ≥ 1
  - `grep "rectPhi = thetaRad" VisionAlgorithmService.cs` ≥ 1 (Phase 14-04 보존)
- 동시성: Plan 15-02 → 15-03 순서 (둘 다 DatumFindingService.cs 수정 — 충돌 방지, wave 2 → wave 3)
</verification>

<success_criteria>
1. TryFindCircleByPolarSampling 시그니처에 string selection 파라미터 추가
2. MeasurePos 호출이 selectionLower 변수 사용 ("all" 하드코딩 제거)
3. selection == "All" 일 때 eRows 전체 누적, 기본 (First/Last) 은 [0] 단일점 누적 (Phase 14-04 sweep 의도 보존)
4. rectPhi = thetaRad 회전 로직 보존 (변경 금지)
5. DatumFindingService.TryTeachCircleTwoHorizontal Circle 호출에 config.Circle_EdgeSelection 전달
6. 모든 TryFindCircleByPolarSampling caller (smoke harness 포함) 가 selection 인자 전달 또는 "no other callers" 확인
7. 빌드 PASS, 신규 warning 0
</success_criteria>

<output>
After completion, create `.planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-03-SUMMARY.md` with: VisionAlgorithmService 변경 diff 요약, Phase 14-04 회전 로직 보존 확인 노트, caller 검증 표 (DatumFindingService + smoke harness/기타 — 또는 "no other callers"), 빌드 결과.
</output>
