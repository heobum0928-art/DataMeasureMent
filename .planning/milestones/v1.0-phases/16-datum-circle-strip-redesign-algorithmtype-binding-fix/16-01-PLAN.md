---
phase: 16-datum-circle-strip-redesign-algorithmtype-binding-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
autonomous: true
requirements: [R1, R2]
must_haves:
  truths:
    - "원 ROI 그린 직후 Strip 사각형이 stepCount 개 (= round(360/PolarStepDeg)) 화면에 정적으로 표시된다 (회색 thin line, fill 없음)"
    - "PropertyGrid 의 RectL1Ratio 0.05 → 0.20 변경 시 strip 사각형 크기가 즉시 변한다 (RaisePropertyChanged + RefreshParamEditor → SetDatumOverlay 재호출 경로)"
    - "btn_teachDatum 클릭 후 LastTeachSucceeded == true 분기에서 검출 원 (녹색 light green) + Center cross (노란색 size=12) + Raw edge points (회색 size=4) 가 표시된다"
    - "검출 원 좌표가 PropertyGrid 의 CircleCenter_Row / CircleCenter_Col / CircleDetected_Radius 결과와 일치한다"
    - "z-order: ROI 경계 (밑) → Strip 사각형 → Raw edge points → 검출 원 → Center cross (맨 위) — center 가 가려지지 않는다"
    - "CircleTwoHorizontal 외 알고리즘 (TwoLineIntersect / VerticalTwoHorizontal) 에서는 strip 시각화 / 검출 원 / center cross 모두 렌더 안 됨 (회귀 0)"
    - "VisionAlgorithmService.cs / DatumFindingService.cs 모두 git diff 0 라인 (Phase 14-04 D-13 / Phase 15-02 / 15-03 결정 보존)"
    - "msbuild Debug/x64 PASS, 신규 warning 0 on HalconDisplayService.cs"
  artifacts:
    - path: "WPF_Example/Halcon/Display/HalconDisplayService.cs"
      provides: "RenderDatumOverlay Circle 분기 재작성 (pre-teach Strip 시각화 helper + post-teach 검출 원/center 시각화)"
      contains: "RenderCircleStripOverlay"
  key_links:
    - from: "HalconDisplayService.RenderDatumOverlay (Circle 분기, datum.AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal)"
      to: "신규 private static helper RenderCircleStripOverlay (CircleROI 가 있을 때 stepCount 개 GenRectangle2 + DispLine 로 strip 외곽선만 그림)"
      via: "datum.CircleROI_Radius > 0 가드 + datum.Circle_PolarStepDeg / Circle_RectL1Ratio / Circle_RectL2Ratio 직접 read"
      pattern: "RenderCircleStripOverlay\\s*\\(window,\\s*datum"
    - from: "HalconDisplayService.RenderDatumOverlay (LastTeachSucceeded 분기)"
      to: "검출 원 시각화 (녹색 light green + center cross 노란색 size=12)"
      via: "datum.CircleDetected_Radius > 0 + datum.CircleCenter_Row/Col, DispCross size=12 노란색"
      pattern: "light green|\"green\""
---

<objective>
Datum CircleTwoHorizontal 의 시각화 결함 (R1 + R2) 을 HalconDisplayService.RenderDatumOverlay Circle 분기 재작성으로 해결한다. **알고리즘 코드 (VisionAlgorithmService.TryFindCircleByPolarSampling, Phase 14-04 D-13 `rectPhi=thetaRad` + Phase 15-03 selection 분기) 는 한 줄도 변경하지 않는다.**

Purpose:
- 사용자가 원 ROI 를 그린 직후, **알고리즘이 실제 사용할 Strip 사각형 셋팅** (원호를 포함하는 작은 사각형 stepCount 개) 을 시각적으로 확인하여 RectL1Ratio / RectL2Ratio / PolarStepDeg 파라미터 의도를 직관적으로 조정 가능하게 한다.
- 티칭 완료 후 검출된 원 (녹색) + center cross (노란색, 큰 사이즈) 를 두드러지게 표시하여 "정밀한 원 center 가 최종 목적" 이라는 사용자 요구를 충족한다.

Output:
- WPF_Example/Halcon/Display/HalconDisplayService.cs 단일 파일 편집 — Circle 분기 재작성 + neue private helper RenderCircleStripOverlay 추가.
- VisionAlgorithmService.cs / DatumFindingService.cs / DatumConfig.cs **읽기만** — diff 0 검증.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-datum-circle-strip-redesign-algorithmtype-binding-fix/16-CONTEXT.md
@.planning/phases/16-datum-circle-strip-redesign-algorithmtype-binding-fix/16-SPEC.md
@.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-04-PLAN.md
@.planning/phases/13-datum-algorithm-extensibility/13-05-SUMMARY.md
@WPF_Example/Halcon/Display/HalconDisplayService.cs
@WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
@WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs

<interfaces>
<!-- 핵심 시그니처 — executor 가 직접 사용. 코드베이스 탐색 불필요. -->

From WPF_Example/Halcon/Display/HalconDisplayService.cs (현재):
- `public void RenderDatumOverlay(HWindow window, DatumConfig datum, bool isSelected)` (line 432)
- `private const double EXTEND_PX = 10000.0` (line 382)
- `private static void DrawExtendedLine(HWindow window, double r1, double c1, double r2, double c2)` (line 387)
- `private static void RenderRawEdgePoints(HWindow window, HTuple rows, HTuple cols, string color)` (line 413)
- 기존 Circle 분기 (line 491-504): `DispCircle(datum.CircleROI_Row, CircleROI_Col, CircleROI_Radius)` + DrawRoiLabelAt
- 기존 LastTeachSucceeded Circle 분기 (line 580-597): `DispCircle(CircleCenter_Row, CircleCenter_Col, CircleDetected_Radius)` + 빨간 6px 십자

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs:
```csharp
public double CircleROI_Row    { get; set; } = 0;     // line 203
public double CircleROI_Col    { get; set; } = 0;     // line 204
public double CircleROI_Radius { get; set; } = 0;     // line 205
public double Circle_PolarStepDeg { get; set; } = 10.0;  // line 215
public double Circle_RectL1Ratio  { get; set; } = 0.05;  // line 216 (반경 방향)
public double Circle_RectL2Ratio  { get; set; } = 0.05;  // line 217 (접선 방향)
public double CircleCenter_Row;     // detected center
public double CircleCenter_Col;     // detected center
public double CircleDetected_Radius;
public bool LastTeachSucceeded;
public HTuple Circle_DetectedEdgeRows;  // raw edges
public HTuple Circle_DetectedEdgeCols;  // raw edges
public EDatumAlgorithm AlgorithmTypeEnum;  // CircleTwoHorizontal 분기 키
```

From WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs (READ-ONLY 보존):
- `TryFindCircleByPolarSampling` 의 strip 생성 식 (canonical, 시각화는 동일 식 미러링 필수):
  ```
  for k in 0..stepCount-1:
    thetaRad = k * stepRad   (stepRad = PolarStepDeg * π/180)
    rectRow  = CircleROI_Row + CircleROI_Radius * sin(thetaRad)   // 왼쪽 반지름 끝점 → 회전
    rectCol  = CircleROI_Col - CircleROI_Radius * cos(thetaRad)
    rectPhi  = thetaRad         // Phase 14-04 D-13 — 보존 결정
    length1  = CircleROI_Radius * Circle_RectL1Ratio
    length2  = CircleROI_Radius * Circle_RectL2Ratio
  ```
  **이 strip 식이 알고리즘 canonical. Plan 16-01 의 시각화는 이 식을 그대로 미러링 (alg 코드 미수정).**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RenderCircleStripOverlay private helper 추가 + Circle 분기 pre-teach 시각화 재작성</name>
  <files>WPF_Example/Halcon/Display/HalconDisplayService.cs</files>
  <read_first>
    1. `WPF_Example/Halcon/Display/HalconDisplayService.cs` 전체 (Allman brace style 재확인 + 기존 RenderDatumOverlay 구조 + DrawExtendedLine / RenderRawEdgePoints 패턴 학습 — line 380-430)
    2. `WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs` 의 TryFindCircleByPolarSampling 메서드 (strip 생성 루프 — canonical 식 그대로 미러링하기 위함, 변경 금지)
    3. `WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` line 200-220 (CircleROI_* + Circle_PolarStepDeg + Circle_RectL1Ratio + Circle_RectL2Ratio 필드 시그니처 확인)
    4. `.planning/phases/13-datum-algorithm-extensibility/13-05-SUMMARY.md` (DispCross batch 시각화 패턴 + Allman style 컨벤션 확인)
  </read_first>
  <action>
    **D-01 / D-02 / D-03 / D-08 (CONTEXT.md verbatim) 구현.**

    HalconDisplayService.cs 의 기존 Circle 분기 코드 (line 490-504, `if (datum.AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal && datum.CircleROI_Radius > 0)` 블록) 을 다음과 같이 **확장** 한다 (기존 DispCircle ROI 외곽 + DrawRoiLabelAt 라벨 그대로 보존, **그 뒤에** strip 시각화 추가).

    1) **신규 private static helper `RenderCircleStripOverlay`** 를 클래스 하단 (RenderRawEdgePoints 아래, line 428 근처) 에 Allman style 로 추가:

    ```csharp
    //260429 hbk Phase 16 D-01 — 원 ROI 그린 직후 알고리즘이 사용할 strip 사각형 stepCount 개를 정적으로 시각화
    //  VisionAlgorithmService.TryFindCircleByPolarSampling 의 strip 생성 식 (Phase 14-04 D-13 보존) 을 그대로 미러링.
    //  rectRow = ROI_Row + Radius * sin(thetaRad), rectCol = ROI_Col - Radius * cos(thetaRad), rectPhi = thetaRad.
    //  length1 = Radius * RectL1Ratio (반경 방향), length2 = Radius * RectL2Ratio (접선 방향). fill 없음 — DispLine 외곽선만.
    private static void RenderCircleStripOverlay(HWindow window, DatumConfig datum)
    {
        if (datum == null) return;
        if (datum.CircleROI_Radius <= 0) return;
        double stepDeg = datum.Circle_PolarStepDeg;
        if (stepDeg < 1.0) stepDeg = 1.0;       //260429 hbk Phase 16 D-01 — 0/음수 division 방지 (CONTEXT D-01: 1°~30° 범위)
        if (stepDeg > 30.0) stepDeg = 30.0;
        int stepCount = (int)Math.Round(360.0 / stepDeg);
        if (stepCount <= 0) return;

        double radius  = datum.CircleROI_Radius;
        double centerR = datum.CircleROI_Row;
        double centerC = datum.CircleROI_Col;
        double length1 = radius * datum.Circle_RectL1Ratio;     //260429 hbk Phase 16 D-01 — 반경 방향
        double length2 = radius * datum.Circle_RectL2Ratio;     //260429 hbk Phase 16 D-01 — 접선 방향
        if (length1 < 1.0) length1 = 1.0;       //260429 hbk Phase 16 — 1px 미만이면 시각화 의미 없음, 자동 floor
        if (length2 < 1.0) length2 = 1.0;

        try
        {
            //260429 hbk Phase 16 D-02 — Strip 색상: 회색 thin line, fill 없음 (cyan/magenta/yellow 와 충돌 회피)
            HOperatorSet.SetColor(window, "gray");
            HOperatorSet.SetLineWidth(window, 1);
            double stepRad = stepDeg * Math.PI / 180.0;
            for (int k = 0; k < stepCount; k++)
            {
                double thetaRad = k * stepRad;
                //260429 hbk Phase 16 D-01 — 알고리즘 canonical 식 미러 (VisionAlgorithmService.TryFindCircleByPolarSampling 와 동일)
                double rectRow = centerR + radius * Math.Sin(thetaRad);
                double rectCol = centerC - radius * Math.Cos(thetaRad);
                double rectPhi = thetaRad;
                //  GenRectangle2 → 외곽선만 DispLine 4 변. (HALCON 24.11: GenRectangle2(out HObject, row, col, phi, l1, l2) 후
                //   DispObj 로 그리면 fill — 본 phase D-02: fill 없음 → 4 corner 좌표 직접 계산하여 DispLine 4 회.)
                double cosP = Math.Cos(rectPhi);
                double sinP = Math.Sin(rectPhi);
                //  로컬 4 corner: (-l1,-l2), (-l1,+l2), (+l1,+l2), (+l1,-l2)
                double r1 = rectRow + (-length1) * cosP - (-length2) * sinP;
                double c1 = rectCol + (-length1) * sinP + (-length2) * cosP;
                double r2 = rectRow + (-length1) * cosP - ( length2) * sinP;
                double c2 = rectCol + (-length1) * sinP + ( length2) * cosP;
                double r3 = rectRow + ( length1) * cosP - ( length2) * sinP;
                double c3 = rectCol + ( length1) * sinP + ( length2) * cosP;
                double r4 = rectRow + ( length1) * cosP - (-length2) * sinP;
                double c4 = rectCol + ( length1) * sinP + (-length2) * cosP;
                HOperatorSet.DispLine(window, r1, c1, r2, c2);
                HOperatorSet.DispLine(window, r2, c2, r3, c3);
                HOperatorSet.DispLine(window, r3, c3, r4, c4);
                HOperatorSet.DispLine(window, r4, c4, r1, c1);
            }
        }
        catch
        {
            //260429 hbk Phase 16 — RenderDatumOverlay 의 catch 컨벤션 유지 (display 에러 무시)
        }
    }
    ```

    2) **기존 Circle ROI 분기 (line 490-504) 직후** 에 strip 시각화 호출 추가:

    ```csharp
    //260424 hbk Phase 12 D-10 — Circle ROI 검색 영역 (CircleTwoHorizontal 일 때만 렌더, Line1/Line2 와 동일 색)
    if (datum.AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal
        && datum.CircleROI_Radius > 0)
    {
        HOperatorSet.SetColor(window, color);
        HOperatorSet.SetLineWidth(window, lineWidth);
        HOperatorSet.DispCircle(window,
            datum.CircleROI_Row, datum.CircleROI_Col, datum.CircleROI_Radius);

        //260424 hbk Phase 12 Gap-2 — "Circle" 라벨 (원 위쪽 외곽 바로 바깥)
        DrawRoiLabelAt(window,
            datum.CircleROI_Row - datum.CircleROI_Radius - 22,
            datum.CircleROI_Col - datum.CircleROI_Radius,
            "Circle");

        //260429 hbk Phase 16 D-01/D-02/D-08 — pre-teach Strip 사각형 stepCount 개 정적 시각화 (z-order: ROI 경계 위)
        RenderCircleStripOverlay(window, datum);
    }
    ```

    **D-02 verbatim**: "Strip 색상은 회색 thin line, fill 없음, GenRectangle2 + DispLine 폴리곤 외곽선 only".
    **D-08 verbatim z-order**: "ROI 경계 (under) → Strip 사각형 (under) → Raw edge points → 검출 원 → Center cross (top)".
    **D-22 보존**: VisionAlgorithmService / DatumFindingService 한 줄도 만지지 않는다 (시각화는 알고리즘 식을 미러링만).

    모든 신규 / 수정 라인 위에 `//260429 hbk Phase 16 D-XX <reason>` 주석 (D-20, 사용자 강제 컨벤션). Allman brace style 유지.
  </action>
  <acceptance_criteria>
    1. `grep -n "RenderCircleStripOverlay" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 최소 2 hits (정의 1 + 호출 1)
    2. `grep -c "//260429 hbk" WPF_Example/Halcon/Display/HalconDisplayService.cs` ≥ 8 (Task 1 신규 라인 주석)
    3. `grep -n "Circle_PolarStepDeg" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 최소 1 hit
    4. `grep -n "Circle_RectL1Ratio" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 최소 1 hit
    5. `grep -n "Circle_RectL2Ratio" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 최소 1 hit
    6. `grep -n "SetColor.*\"gray\"" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 최소 1 hit (Strip 회색)
    7. `git diff --stat WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs` 결과 0 라인 (보존)
    8. `git diff --stat WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 결과 0 라인 (보존)
    9. `git diff --stat WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 결과 0 라인 (Plan 16-01 범위에서는 무수정)
    10. `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64` PASS, HalconDisplayService.cs 신규 warning 0
  </acceptance_criteria>
  <verify>
    <automated>cd WPF_Example && msbuild DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo</automated>
  </verify>
  <done>RenderCircleStripOverlay helper 정의 + Circle 분기 호출 wiring 완료. 빌드 PASS, 알고리즘 파일 diff 0.</done>
</task>

<task type="auto">
  <name>Task 2: post-teach 검출 원 시각화 색상/사이즈 변경 + Center cross 노란색 size=12 + Raw edges size=4</name>
  <files>WPF_Example/Halcon/Display/HalconDisplayService.cs</files>
  <read_first>
    1. `WPF_Example/Halcon/Display/HalconDisplayService.cs` line 555-607 (LastTeachSucceeded 분기 — 기존 Line1/Line2 노란/cyan 외삽 + Circle yellow + 빨간 center 6px 십자 + RenderRawEdgePoints 6 ROI 호출 — 본 Task 가 Circle 부분만 색상/사이즈 변경)
    2. `WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 의 Circle_DetectedEdgeRows / Circle_DetectedEdgeCols / CircleCenter_Row / CircleCenter_Col / CircleDetected_Radius 필드 (Phase 13-05 / 14-04 에서 write-back 완료, 본 phase 추가 필드 불필요)
    3. `.planning/phases/16-datum-circle-strip-redesign-algorithmtype-binding-fix/16-CONTEXT.md` D-04 / D-05 / D-06 / D-07 / D-08 (색상 + 사이즈 + z-order verbatim 결정)
  </read_first>
  <action>
    **D-04 / D-05 / D-06 / D-07 / D-08 (CONTEXT.md verbatim) 구현.**

    HalconDisplayService.cs 의 기존 LastTeachSucceeded 분기 (line 555-607) 에서 **CircleTwoHorizontal 검출 원 블록 (line 579-597)** 만 다음과 같이 교체. **Line1/Line2 외삽 / 빨간 십자(교점) / RenderRawEdgePoints 6 ROI 호출은 그대로 보존** (회귀 0).

    **변경 1: Circle 검출 원 색상 yellow → light green (D-05 verbatim "녹색 light green").**

    ```csharp
    //260424 hbk Phase 12 D-13 — CircleTwoHorizontal 검출 원 오버레이 (노란 원 + 빨간 중심 십자) — 색상 / 사이즈 Phase 16 재정의
    //260429 hbk Phase 16 D-05 — 검출 원 색상 yellow → "light green" (검출 성공 = 녹색 컨벤션, Phase 7 의 녹/적 에지 연장)
    //260429 hbk Phase 16 D-06 — Center cross 색상 red → "yellow", 6px → 12px (정밀 원 center 가 최종 목적, 가장 두드러지게)
    //260429 hbk Phase 16 D-08 — z-order: 검출 원 그린 후 center cross (top) — center 가 가려지지 않게.
    if (datum.AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal
        && datum.CircleDetected_Radius > 0)
    {
        //260429 hbk Phase 16 D-05 — 검출 원 = 녹색 (light green)
        HOperatorSet.SetColor(window, "light green");
        HOperatorSet.SetLineWidth(window, 2);
        HOperatorSet.DispCircle(window,
            datum.CircleCenter_Row, datum.CircleCenter_Col, datum.CircleDetected_Radius);

        //260429 hbk Phase 16 D-06 — Center cross = 노란색 + size=12 (큰 사이즈, 굵기 강조)
        HOperatorSet.SetColor(window, "yellow");
        HOperatorSet.SetLineWidth(window, 3);                       //260429 hbk Phase 16 D-06 — 굵기 강조 (line width 3)
        const double circleCenterCrossHalf = 12.0;                  //260429 hbk Phase 16 D-06 — 6 → 12px (size=12, CONTEXT D-06)
        HOperatorSet.DispLine(window,
            datum.CircleCenter_Row - circleCenterCrossHalf, datum.CircleCenter_Col,
            datum.CircleCenter_Row + circleCenterCrossHalf, datum.CircleCenter_Col);
        HOperatorSet.DispLine(window,
            datum.CircleCenter_Row, datum.CircleCenter_Col - circleCenterCrossHalf,
            datum.CircleCenter_Row, datum.CircleCenter_Col + circleCenterCrossHalf);
    }
    ```

    **변경 2: Raw edge points 시각화 size 6 → 4 (D-07 verbatim "회색 작은 십자가 size=4 — 검출 과정 trace 용").**

    기존 RenderRawEdgePoints 헬퍼 (line 413) 의 `HOperatorSet.DispCross(window, rows, cols, 6.0, 0.0)` 를 그대로 두면 6 ROI 모두 size=6 이 적용된다. 본 phase 의 D-07 은 **Circle 의 raw points 만 size=4 + 회색** 으로 (다른 ROI 색상은 Phase 13-05 그대로 보존). 따라서:

    1) `RenderRawEdgePoints` 시그니처에 **optional 인자 추가**:

    ```csharp
    //260425 hbk Phase 13 D-VIZ-05 — DispCross batch 시각화
    //260429 hbk Phase 16 D-07 — size 인자 + 기본값 6 (기존 호출 시그니처 하위호환). Circle 호출처에서만 4 + "gray" 로 override.
    private static void RenderRawEdgePoints(HWindow window, HTuple rows, HTuple cols, string color, double size = 6.0)
    {
        // ... 기존 본문 ...
        HOperatorSet.DispCross(window, rows, cols, size, 0.0);     //260429 hbk Phase 16 D-07 — 6 하드코딩 → size 인자
        // ...
    }
    ```

    2) Circle raw points 호출 (line 602) 만 다음과 같이 변경:

    ```csharp
    //260425 hbk Phase 13 D-VIZ-05 — Circle raw 검출 에지점 (yellow size=6) — Phase 16 D-07 로 회색 size=4 변경
    //260429 hbk Phase 16 D-07 — Circle raw points = 회색 작은 십자가 size=4 (검출 trace 용, Phase 13-05 의 yellow 와 시각 구분)
    RenderRawEdgePoints(window, datum.Circle_DetectedEdgeRows, datum.Circle_DetectedEdgeCols, "gray", 4.0);
    ```

    **나머지 5 ROI (Line1 cyan / Line2 magenta / HorizA green / HorizB lime / Vertical orange) 호출은 size 인자 미지정 → 기본값 6 적용 → Phase 13-05 시각화 회귀 0.**

    **변경 3: z-order 가드 (D-08 verbatim "ROI 경계 (under) → Strip 사각형 (under) → Raw edge points → 검출 원 → Center cross (top)").**

    현재 LastTeachSucceeded 블록의 호출 순서를 다음으로 정렬:
    1. Line1/Line2 외삽 (Line1 yellow / Line2 cyan — 기존)
    2. 교점 빨간 십자 (TwoLineIntersect — 기존)
    3. **RenderRawEdgePoints 6 ROI 호출 → 검출 원 → Center cross 노란 십자 12px** (z-order: raw under, 검출 원 mid, center top)

    Circle 분기 (CircleTwoHorizontal) 일 때만 raw points / 검출 원 / center cross 추가 시각화. Line1/Line2 외삽 + 빨간 교점 십자는 TwoLineIntersect 시각화이므로 CircleTwoHorizontal / VerticalTwoHorizontal 에서는 Line1Detected/Line2Detected 좌표가 0/sentinel 이므로 자연스럽게 외삽이 0길이로 그려진다 (회귀 0 — 기존 Phase 13-05 동작 그대로).

    z-order 정렬을 위해 Circle 분기 블록을 **RenderRawEdgePoints 6 ROI 호출 뒤에 위치** 시킨다. 즉 line 599-606 의 RenderRawEdgePoints 6 호출을 먼저 실행 → 그 다음 Circle 분기의 검출 원 + center cross.

    **D-22 보존**: VisionAlgorithmService.cs / DatumFindingService.cs / DatumConfig.cs 한 줄도 만지지 않는다.

    모든 변경 라인 위에 `//260429 hbk Phase 16 D-XX <reason>` 주석.
  </action>
  <acceptance_criteria>
    1. `grep -n "light green" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 최소 1 hit (D-05 검출 원 색상)
    2. `grep -n "circleCenterCrossHalf = 12" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 1 hit (D-06 size=12)
    3. `grep -n "RenderRawEdgePoints.*\"gray\".*4" WPF_Example/Halcon/Display/HalconDisplayService.cs` 또는 `grep -n "Circle_DetectedEdgeRows.*\"gray\"" ...` 가 1 hit (D-07 회색 size=4)
    4. `grep -n "double size = 6.0" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 1 hit (RenderRawEdgePoints 시그니처 확장 + 하위호환 default)
    5. `grep -c "//260429 hbk" WPF_Example/Halcon/Display/HalconDisplayService.cs` ≥ Task 1 결과 + 6 추가 (Task 2 신규 주석)
    6. 기존 Phase 13-05 의 5 ROI raw 색상 라인 (cyan / magenta / yellow Line2 / green / lime green / orange) 이 모두 grep 으로 발견됨 (회귀 0):
       - `grep -n "Line1_DetectedEdgeRows.*\"cyan\"" ...` 1 hit
       - `grep -n "Line2_DetectedEdgeRows.*\"magenta\"" ...` 1 hit
       - `grep -n "Horizontal_A_DetectedEdgeRows.*\"green\"" ...` 1 hit
       - `grep -n "Horizontal_B_DetectedEdgeRows.*\"lime green\"" ...` 1 hit
       - `grep -n "Vertical_DetectedEdgeRows.*\"orange\"" ...` 1 hit
    7. `git diff --stat WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs` 결과 0 라인 (D-22 보존 검증)
    8. `git diff --stat WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 결과 0 라인 (D-22 보존 검증)
    9. `git diff --stat WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 결과 0 라인 (Plan 16-01 범위)
    10. `msbuild ... Debug/x64` PASS, HalconDisplayService.cs 신규 warning 0
  </acceptance_criteria>
  <verify>
    <automated>cd WPF_Example && msbuild DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo</automated>
  </verify>
  <done>검출 원 light green + center cross yellow size=12 + Raw edges 회색 size=4 적용. z-order 정렬. 빌드 PASS. 알고리즘 파일 diff 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 본 phase 는 로컬 데스크탑 WPF UI 의 시각화 변경. 네트워크/인증/저장소/외부 입력 없음. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | N/A | HalconDisplayService.RenderCircleStripOverlay | accept | No network, no auth, no file write outside existing pattern, no external input introduced. 로컬 HALCON HWindow 그래픽 호출만. |
</threat_model>

<verification>
- 사용자 SIMUL_MODE 또는 실 카메라 데이터로 원 ROI 그린 직후 strip 사각형이 stepCount 개 (PolarStepDeg=10° → 36개) 회색으로 표시되는지 확인
- RectL1Ratio 0.05 → 0.20 변경 시 strip 크기가 시각적으로 변하는지 확인 (PropertyGrid 변경 → Phase 12-03 의 RaisePropertyChanged + RefreshParamEditor 경로가 SetDatumOverlay 재호출까지 닿는지)
- btn_teachDatum 클릭 후 검출 원 (light green) + center cross (노란색 12px) + raw edges (회색 size=4) 가 표시되는지
- 검출 원 좌표 = PropertyGrid 의 CircleCenter_Row / CircleCenter_Col 표시값과 일치
- (UAT 16-03 에서 사용자 검증)
</verification>

<success_criteria>
- [ ] RenderCircleStripOverlay private helper 추가 (line 410~430 영역)
- [ ] RenderDatumOverlay Circle 분기 (line 491-504) 가 RenderCircleStripOverlay 호출 추가
- [ ] LastTeachSucceeded 분기의 Circle 검출 원 색상 yellow → light green
- [ ] Center cross 색상 red → yellow, half size 6 → 12, line width 2 → 3
- [ ] Circle raw edges 호출이 "gray" size=4 로 변경 (다른 5 ROI 호출 회귀 0)
- [ ] RenderRawEdgePoints 시그니처 size 인자 추가 (default 6 — 하위호환)
- [ ] msbuild Debug/x64 PASS, 신규 warning 0 on HalconDisplayService.cs
- [ ] git diff --stat 결과: VisionAlgorithmService.cs / DatumFindingService.cs / DatumConfig.cs 모두 0 라인
- [ ] 모든 신규/수정 라인 위 //260429 hbk Phase 16 D-XX 주석
- [ ] 단일 commit (또는 task 별 2 commits) — `feat(16-01): HalconDisplayService Circle overlay 재작성 — pre-teach strip + post-teach 검출 원/center 시각화`
</success_criteria>

<output>
After completion, create `.planning/phases/16-datum-circle-strip-redesign-algorithmtype-binding-fix/16-01-SUMMARY.md` with:
- 변경 라인 수 + 신규 helper RenderCircleStripOverlay 위치 + diff 0 보존 파일 명시
- 변경하지 않은 항목 명시 (Phase 14-04 D-13 rectPhi=thetaRad / Phase 15-02/03 selection wiring / DatumConfig 데이터 모델)
- Plan 16-02 / 16-03 인계 메모 (16-02 는 다른 파일이므로 Wave 1 병렬, 16-03 UAT 는 Wave 2 의존)
</output>
