---
phase: 17-datum-ux-circle-strip-1-test-find-detectedorigin-hover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
autonomous: true
requirements_addressed:
  - P17-D-01
  - P17-D-02
  - P17-D-03
  - P17-D-04
  - P17-D-17
  - P17-D-18
  - P17-D-19
  - P17-D-20
  - 16-UAT-carry-#1
  - 16-UAT-carry-#2
  - 16-UAT-carry-#3
  - 16-UAT-carry-#16
must_haves:
  truths:
    - "Circle pre-teach 시각화는 0° (3시 방향) 위치에 단 1개의 회색 strip 사각형만 그린다 (D-01)"
    - "DatumConfig 에 Circle_RadialDirection 신규 string 필드가 추가되어 PropertyGrid 에 ComboBox(Inward/Outward) 로 노출된다 (D-02)"
    - "EdgeOptionLists.RadialDirections 가 [\"Inward\", \"Outward\"] 단일 소스로 존재한다 (D-02)"
    - "TryTeachCircleTwoHorizontal 에서 Circle_RadialDirection 이 polarity (positive/negative) 로 매핑되어 TryFindCircleByPolarSampling 에 전달된다 (D-02)"
    - "DatumFindingService.cs 의 *계산 로직* 라인은 한 줄도 변경되지 않는다 (D-17 — 신규 polarity local + 인자 1개 교체만 허용, 합 ≤ 2 라인)"
    - "VisionAlgorithmService.cs 는 한 줄도 변경되지 않는다 (D-17)"
    - "EnsurePerRoiDefaults 가 Circle_RadialDirection 에 대해 sentinel \"\" → \"Inward\" fallback 을 idempotent 하게 적용한다 (D-02 / S2 패턴)"
    - "msbuild Debug/x64 PASS, 신규 warning 0 on 수정 범위"
    - "기존 Phase 16 INI 레시피가 Circle_RadialDirection 미존재 상태로도 EnsurePerRoiDefaults 호출 후 \"Inward\" 로 자동 보충되어 정상 로드된다 (INI 하위호환)"
  artifacts:
    - path: WPF_Example/Halcon/Display/HalconDisplayService.cs
      provides: "RenderCircleStripOverlay 1개 strip + RenderDatumFindResult signature 보존 (D-13 변경은 Plan 17-03)"
      contains: "thetaRad = 0.0"
    - path: WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
      provides: "Circle_RadialDirection 필드 + EnsurePerRoiDefaults 보충"
      contains: "Circle_RadialDirection"
    - path: WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs
      provides: "RadialDirections 정적 리스트"
      contains: "RadialDirections"
    - path: WPF_Example/Halcon/Algorithms/DatumFindingService.cs
      provides: "TryTeachCircleTwoHorizontal caller 측 polarity 매핑 1라인"
      contains: "Circle_RadialDirection"
  key_links:
    - from: "DatumConfig.Circle_RadialDirection"
      to: "EdgeOptionLists.RadialDirections"
      via: "[ItemsSourceProperty] attribute"
      pattern: "ItemsSourceProperty.nameof.Circle_RadialDirectionList"
    - from: "DatumFindingService.TryTeachCircleTwoHorizontal"
      to: "VisionAlgorithmService.TryFindCircleByPolarSampling"
      via: "polarity 인자 (string)"
      pattern: "circlePolarity"
    - from: "HalconDisplayService.RenderCircleStripOverlay"
      to: "DatumConfig.Circle_RectL1Ratio + Circle_RectL2Ratio + CircleROI_*"
      via: "1 회 thetaRad=0.0 strip 계산"
      pattern: "thetaRad\\s*=\\s*0\\.0"
---

<objective>
Cluster A 구현 — Circle 시각화 1개 strip + RadialDirection enum + EdgeDirection 정책 정리.

Phase 16 UAT carry-over #1, #2, #3, #16 을 한 번에 해소한다.
- (#1) 36개 strip 동시 표시 → 0° 단일 strip 만 표시 (D-01).
- (#2) PropertyGrid Circle_RadialDirection (Inward/Outward) 신규 enum (D-02). DatumConfig 데이터 모델 + EdgeOptionLists 단일 소스 + DatumFindingService caller 1 라인 매핑.
- (#3) Circle 분기에서 Circle_EdgeDirection 동적 hide — 본 Plan 은 *데이터 모델 only* (실제 hide 로직은 ICustomTypeDescriptor 구현이 들어가는 Plan 17-02 범위, 본 Plan 은 D-03 의 분기 조건만 사전 합의 표기).
- (#16) EdgeDirection 모든 옵션 허용 + tooltip — 본 Plan 은 EdgeOptionLists.Directions 가 이미 4 옵션 보유함을 검증하고 PropertyGrid Description tooltip 을 EdgeDirection 필드에 추가.

Purpose: Circle 알고리즘의 사용자 인지 부담 최소화 (1개 strip 으로 방향/크기/내외 직관) + RadialDirection 으로 안→밖 / 밖→안 그라디언트 명시 선택 + EdgeDirection 슬롯 제한 폐지.

Output: HalconDisplayService.cs (RenderCircleStripOverlay 단일 thetaRad=0.0 호출), DatumConfig.cs (Circle_RadialDirection 필드 + EnsurePerRoiDefaults + 모든 *_EdgeDirection 에 [Description] tooltip), EdgeOptionLists.cs (RadialDirections 추가), DatumFindingService.cs (TryTeachCircleTwoHorizontal polarity 매핑 1 라인).

알고리즘 보존 — DatumFindingService 의 *계산 로직* 0 라인 변경. 허용: caller 측 polarity local + 인자 1개 교체만 (합 ≤ 2 라인). VisionAlgorithmService 0 라인.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-CONTEXT.md
@.planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-PATTERNS.md
@.planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-UI-SPEC.md
@.planning/phases/16-datum-circle-strip-redesign-algorithmtype-binding-fix/16-UAT.md
@.planning/phases/16-datum-circle-strip-redesign-algorithmtype-binding-fix/16-01-SUMMARY.md

<interfaces>
<!-- Existing call sites that this plan touches -->

HalconDisplayService.cs L441-498:
  public void RenderCircleStripOverlay(HWindow window, DatumConfig datum)
  // 현재: stepCount 루프로 N 개 strip 그림. Phase 17 D-01 — thetaRad=0.0 단일 호출만 남김.

DatumFindingService.cs L342-351:
  bool circleOk = visionSvc.TryFindCircleByPolarSampling(
      image, config.CircleROI_Row, config.CircleROI_Col, config.CircleROI_Radius,
      config.Circle_PolarStepDeg, config.Circle_RectL1Ratio, config.Circle_RectL2Ratio,
      config.Circle_EdgeThreshold, config.Circle_Sigma,
      config.Circle_EdgePolarity,                // ← Phase 17 D-02 에서 circlePolarity 로 교체
      config.Circle_EdgeSelection, datumTransform: null,
      out double cRow, out double cCol, out double cRadius, ...);

DatumConfig.cs (existing field block, L225-239):
  [ItemsSourceProperty(nameof(Circle_EdgeDirectionList))]
  public string Circle_EdgeDirection { get; set; } = "";
  [ItemsSourceProperty(nameof(Circle_EdgeSelectionList))]
  public string Circle_EdgeSelection { get; set; } = "";
  [PropertyTools.DataAnnotations.Browsable(false)]
  public List<string> Circle_EdgeDirectionList { get { return EdgeOptionLists.Directions; } }
  [PropertyTools.DataAnnotations.Browsable(false)]
  public List<string> Circle_EdgeSelectionList { get { return EdgeOptionLists.Selections; } }

EdgeOptionLists.cs (existing):
  public static readonly List<string> Directions = new List<string> { "LtoR", "RtoL", "TtoB", "BtoT" };  // L13
  public static readonly List<string> Selections = new List<string> { "First", "Last", "All" };          // L22

EnsurePerRoiDefaults pattern (DatumConfig.cs L418-494):
  if (string.IsNullOrEmpty(Circle_EdgeSelection)) Circle_EdgeSelection = "First";
  // ↑ 같은 패턴으로 Circle_RadialDirection 추가
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HalconDisplayService.RenderCircleStripOverlay — stepCount 루프 제거, thetaRad=0.0 단일 strip</name>
  <files>WPF_Example/Halcon/Display/HalconDisplayService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Display/HalconDisplayService.cs (L432-498 전체 읽기 — 기존 RenderCircleStripOverlay 본문 + 인근 RenderDatumOverlay 호출 위치 확인)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-CONTEXT.md (D-01)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-PATTERNS.md (HalconDisplayService.cs 섹션 — D-01 분석 + 코드 excerpt)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-UI-SPEC.md (§ "RenderCircleStripOverlay 변경 사항")
    - .planning/phases/16-datum-circle-strip-redesign-algorithmtype-binding-fix/16-01-SUMMARY.md (Phase 16 RenderCircleStripOverlay 원본 구현 — 무엇을 줄이는지 컨텍스트)
  </read_first>
  <action>
    HalconDisplayService.RenderCircleStripOverlay 의 본문을 다음과 같이 재작성한다 (Allman, D-19).

    1) 기존 가드 + 파라미터 계산 (L444-461) 그대로 유지:
       - `if (datum == null) return;` / `if (datum.CircleROI_Radius <= 0) return;`
       - stepDeg 클램프 (1~30) — *유지*. 단 stepDeg/stepCount/stepRad 변수가 더 이상 루프에 사용되지 않으므로 unused variable warning 회피를 위해 **stepCount/stepRad/stepDeg 의 strip 루프 관련 사용을 제거**. (stepDeg 자체는 데이터 모델 보존 — 본 Plan 은 시각화만 변경.)
       - `double radius = datum.CircleROI_Radius; double centerR = datum.CircleROI_Row; double centerC = datum.CircleROI_Col;` 그대로.
       - `length1`/`length2` cap (Math.Min(radius * Ratio, 12.0)) + floor (1.0) 로직 그대로.

    2) `for (int k = 0; k < stepCount; k++)` 루프 제거 → 루프 본문 1회만 실행:
       ```csharp
       HOperatorSet.SetColor(window, "gray");
       HOperatorSet.SetLineWidth(window, 1);
       double thetaRad = 0.0; //260430 hbk Phase 17 D-01 — 3시 방향 strip 1개만 표시
       double rectRow  = centerR - radius * Math.Sin(thetaRad);
       double rectCol  = centerC + radius * Math.Cos(thetaRad);
       double rectPhi  = thetaRad;
       double cosP = Math.Cos(rectPhi);
       double sinP = Math.Sin(rectPhi);
       double r1 = rectRow + (-length1) * cosP - (-length2) * sinP;
       double c1 = rectCol + (-length1) * sinP + (-length2) * cosP;
       double r2 = rectRow + ( length1) * cosP - (-length2) * sinP;
       double c2 = rectCol + ( length1) * sinP + (-length2) * cosP;
       double r3 = rectRow + ( length1) * cosP - ( length2) * sinP;
       double c3 = rectCol + ( length1) * sinP + ( length2) * cosP;
       double r4 = rectRow + (-length1) * cosP - ( length2) * sinP;
       double c4 = rectCol + (-length1) * sinP + ( length2) * cosP;
       HOperatorSet.DispLine(window, r1, c1, r2, c2);
       HOperatorSet.DispLine(window, r2, c2, r3, c3);
       HOperatorSet.DispLine(window, r3, c3, r4, c4);
       HOperatorSet.DispLine(window, r4, c4, r1, c1);
       ```
       (위 corner 산식 + 4 DispLine 은 기존 구현의 thetaRad 무관 회전 변환 — 그대로 옮겨오며 //260430 hbk Phase 17 D-01 주석을 적어도 1 라인 (thetaRad=0.0 라인) 에 부착.)

    3) 메소드 시그니처는 변경 없음 — `public void RenderCircleStripOverlay(HWindow window, DatumConfig datum)`. RenderDatumOverlay (L502-695) 의 호출 사이트는 손대지 않는다 (CircleTwoHorizontal 분기에서 그대로 호출됨).

    4) RectL1Ratio/RectL2Ratio 변경 시 즉시 갱신 (Phase 16 D-03 패턴): RenderCircleStripOverlay 자체는 매 호출마다 datum.* 을 다시 읽으므로 패턴 보존. 별도 코드 변경 불필요.

    5) Phase 17 D-13 (RenderDatumFindResult 본문 교체) 는 본 Plan 범위 *아님* — Plan 17-03 에서 처리. 본 Task 는 RenderDatumFindResult / RenderDatumOverlay 본문을 건드리지 않는다.

    Brace style: Allman (D-19). 모든 추가/수정 라인 위에 `//260430 hbk Phase 17 D-01 — <reason>` 주석.
  </action>
  <verify>
    <automated>
      grep -c "thetaRad = 0.0" WPF_Example/Halcon/Display/HalconDisplayService.cs && \
      grep -c "for (int k = 0; k &lt; stepCount" WPF_Example/Halcon/Display/HalconDisplayService.cs && \
      msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "thetaRad = 0.0" WPF_Example/Halcon/Display/HalconDisplayService.cs` returns at least 1 match (RenderCircleStripOverlay 단일 strip 라인).
    - `grep -c "for.*k.*stepCount" WPF_Example/Halcon/Display/HalconDisplayService.cs` returns 0 (stepCount 기반 strip 루프 제거 확인).
    - `grep -c "//260430 hbk Phase 17 D-01" WPF_Example/Halcon/Display/HalconDisplayService.cs` returns ≥ 1.
    - msbuild Debug/x64 exit code = 0, 신규 warning 0 on HalconDisplayService.cs (`/p:WarningLevel=4` 시 기존 warning 카운트 ± 0).
    - `git diff WPF_Example/Halcon/Display/HalconDisplayService.cs | grep -E "^\\+" | grep -v "^\\+\\+\\+" | wc -l` ≤ 30 (본 Task 범위는 strip 루프 → 단일 호출 축소).
    - RenderDatumFindResult 메소드 본문 변경 라인 0 (Plan 17-03 범위) — `git diff WPF_Example/Halcon/Display/HalconDisplayService.cs` 에 RenderDatumFindResult 의 try 블록 내부 변경 없음.
  </acceptance_criteria>
  <done>
    RenderCircleStripOverlay 가 thetaRad=0.0 단일 호출만으로 4 DispLine 을 그리고, stepCount 루프가 제거됨. 빌드 PASS, 17-UI-SPEC §"RenderCircleStripOverlay 변경 사항" 사양 준수.
  </done>
</task>

<task type="auto">
  <name>Task 2: EdgeOptionLists.RadialDirections + DatumConfig.Circle_RadialDirection 필드 + EnsurePerRoiDefaults 보충 + 모든 *_EdgeDirection 에 Description tooltip</name>
  <files>
    WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs
    WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  </files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs (전체 — 24 라인 단일 파일)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (L1-50 클래스 헤더, L100-260 *_EdgeDirection 필드 그룹, L225-260 Circle 그룹, L341-413 transient 블록 — Plan 17-03 영역과 충돌 회피용 위치 확인, L418-494 EnsurePerRoiDefaults)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-CONTEXT.md (D-02, D-04)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-PATTERNS.md (DatumConfig.cs / EdgeOptionLists.cs 섹션 — Pattern S2)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-UI-SPEC.md (§ "Circle_RadialDirection PropertyGrid 필드", § "EdgeDirection ComboBox", Copywriting Contract § "EdgeDirection Tooltip")
  </read_first>
  <action>
    **Step A — EdgeOptionLists.cs 추가** (file 끝의 K&R 클로징 brace 직전):

    ```csharp
    // Datum Circle ROI 안→밖 / 밖→안 그라디언트 polarity (CTH only). //260430 hbk Phase 17 D-02
    public static readonly List<string> RadialDirections = new List<string> { "Inward", "Outward" }; //260430 hbk Phase 17 D-02
    ```

    **Step B — DatumConfig.cs Circle_RadialDirection 필드 추가**.

    위치: 기존 Circle 그룹 (L225-239 근방) — Circle_EdgeSelection 다음 라인. PATTERNS.md "DatumConfig.cs §D-02" excerpt 그대로.

    ```csharp
    [ItemsSourceProperty(nameof(Circle_RadialDirectionList))] //260430 hbk Phase 17 D-02
    public string Circle_RadialDirection { get; set; } = ""; //260430 hbk Phase 17 D-02

    [PropertyTools.DataAnnotations.Browsable(false)] //260430 hbk Phase 17 D-02
    public List<string> Circle_RadialDirectionList { get { return EdgeOptionLists.RadialDirections; } } //260430 hbk Phase 17 D-02
    ```

    Category attribute: 기존 Circle 그룹과 동일 (`[Category("Circle")]` 또는 인근 필드의 Category 를 따름). 만약 인근 필드에 Category attribute 없다면 그대로 미부착.

    **Step C — EnsurePerRoiDefaults 보충** (DatumConfig.cs L418-494 내부, 기존 Circle_EdgeSelection 의 fallback 라인 바로 다음):

    ```csharp
    if (string.IsNullOrEmpty(Circle_RadialDirection)) Circle_RadialDirection = "Inward"; //260430 hbk Phase 17 D-02
    ```

    Idempotent — 빈 string 일 때만 fallback. INI 하위호환 (Phase 16 레시피 로드 후 자동 보충).

    **Step D — 모든 *_EdgeDirection 필드에 [System.ComponentModel.Description] tooltip 추가** (D-04 + UI-SPEC Copywriting):

    대상 필드 (DatumConfig.cs 에 존재하는 모든 *_EdgeDirection — Line1_EdgeDirection / Line2_EdgeDirection / Vertical_EdgeDirection / Circle_EdgeDirection / Horizontal_A_EdgeDirection / Horizontal_B_EdgeDirection 6개).

    각 필드 선언 위에 추가:

    ```csharp
    [System.ComponentModel.Description("일반적으로 수평 방향 ROI 에는 LtoR 또는 RtoL 을 권장합니다.")] //260430 hbk Phase 17 D-04
    ```

    PropertyTools.Wpf 가 System.ComponentModel.Description 을 PropertyGrid tooltip 으로 표시 — 사용자 인지 비용 0.

    **Step E — D-03 (Circle_EdgeDirection 동적 hide) 데이터 모델 only**: 본 Plan 은 ICustomTypeDescriptor 구현하지 않음 (Plan 17-02 범위). Circle_EdgeDirection 필드 자체는 그대로 유지 — Plan 17-02 의 IsHiddenForAlgorithm 메서드가 CircleTwoHorizontal 시 Circle_EdgeDirection 을 필터링하도록 별도 처리. 본 Plan 의 Step D 에서 Description 만 추가하고 hide 로직은 미구현.

    **Step F — 클래스 선언 변경 금지**: `public class DatumConfig : ParamBase` 라인을 본 Plan 에서 변경하지 않는다. ICustomTypeDescriptor 구현은 Plan 17-02 단독 책임 (영역 충돌 방지).

    Brace style: K&R (DatumConfig 기존 스타일, PATTERNS.md S6 표). 모든 라인에 `//260430 hbk Phase 17 D-XX — <reason>` 주석.
  </action>
  <verify>
    <automated>
      grep -c "RadialDirections" WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs && \
      grep -c "Circle_RadialDirection" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs && \
      grep -c "EdgeDirection 설정을 반대로\\|일반적으로 수평 방향" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs && \
      msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "RadialDirections" WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs` returns ≥ 1 line containing `new List&lt;string&gt; { "Inward", "Outward" }`.
    - `grep -c "Circle_RadialDirection" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns ≥ 4 (필드 declaration + ItemsSourceProperty + List getter + EnsurePerRoiDefaults).
    - `grep "if (string.IsNullOrEmpty(Circle_RadialDirection))" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns 1 match (EnsurePerRoiDefaults 내부).
    - `grep -c "System.ComponentModel.Description.*수평 방향" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns ≥ 6 (6개 *_EdgeDirection 필드 모두 tooltip 부착).
    - `grep -c "//260430 hbk Phase 17 D-02" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns ≥ 5.
    - `grep -c "//260430 hbk Phase 17 D-04" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns ≥ 6.
    - `grep -c "ICustomTypeDescriptor" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns 0 (Plan 17-02 영역, 본 Plan 미구현).
    - `grep "public class DatumConfig" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns the existing line *unchanged* (no ICustomTypeDescriptor on declaration).
    - `grep -c "DetectedOriginRow\\|DetectedOriginCol\\|DetectedRefAngle\\|DetectedEdgeCount\\|DetectedFitRMSE\\|DetectedAngleDeg" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns 0 (Plan 17-03 영역, 본 Plan 미구현).
    - msbuild Debug/x64 exit code = 0, 신규 warning 0 on DatumConfig.cs / EdgeOptionLists.cs.
  </acceptance_criteria>
  <done>
    EdgeOptionLists 가 RadialDirections 단일 소스 보유, DatumConfig 에 Circle_RadialDirection 신규 ComboBox 필드 + sentinel/fallback 정착, 6개 *_EdgeDirection 필드에 한국어 tooltip 부착. ICustomTypeDescriptor / transient 필드는 미손댐 (Plan 17-02/17-03 영역 보존).
  </done>
</task>

<task type="auto">
  <name>Task 3: DatumFindingService.TryTeachCircleTwoHorizontal — Circle_RadialDirection → polarity 매핑 1라인 (algorithm preservation)</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (L320-360 — TryTeachCircleTwoHorizontal Circle 호출 부근)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-CONTEXT.md (D-02, D-17)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-PATTERNS.md (DatumFindingService.cs §D-02 caller)
    - .planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-03-SUMMARY.md (Circle 호출 selection 인자화 — 본 라인이 어떻게 wired 되어 있는지 확인)
  </read_first>
  <action>
    DatumFindingService.cs 의 TryTeachCircleTwoHorizontal 메소드 내에서 `visionSvc.TryFindCircleByPolarSampling(...)` 호출 (L342 근방, Phase 15-03 이후 selection 인자가 추가된 그 호출) **앞에 1 라인** 추가:

    ```csharp
    //260430 hbk Phase 17 D-02 — Circle_RadialDirection ("Inward"/"Outward") → polarity ("positive"/"negative") override (EdgePolarity 무시)
    string circlePolarity = string.Equals(config.Circle_RadialDirection, "Outward", System.StringComparison.OrdinalIgnoreCase)
        ? "negative" : "positive";
    ```

    그리고 **기존 호출의 polarity 인자 1개를 교체**: `config.Circle_EdgePolarity` (원래 인자) → `circlePolarity`. 같은 라인에 `//260430 hbk Phase 17 D-02 — RadialDirection 우선` 주석 부착.

    이 외 *어떤 라인도* DatumFindingService.cs 에 추가/수정/삭제하지 않는다. 특히 다음 영역 0 라인 변경:
    - VisionAlgorithmService.cs 호출 인자 순서 변경 0 (selection / datumTransform 등 Phase 15-03 wiring 보존).
    - TryTeachCircleTwoHorizontal 의 fitting / IntersectionLl / 라인 측정 로직 0 라인 변경.
    - TryFindDatum / TryTeachTwoLineIntersect / TryTeachVerticalTwoHorizontal 0 라인 변경.
    - Plan 17-03 의 transient write-back (DetectedOriginRow/Col/RefAngle, DetectedEdgeCount/FitRMSE/AngleDeg) 은 본 Plan 범위 *아님* — Plan 17-03 에서 추가.

    **D-17 acceptance bound (본 Plan): DatumFindingService.cs 의 신규 + 변경 라인 합 ≤ 2 라인** (caller polarity local 1 라인 추가 + 인자 1 개 교체 = 라인 단위 +1, 변경 ±1 → 총 추가 라인은 2). 주석 라인은 카운트에서 제외하거나 별도 카운트 — 아래 acceptance_criteria 참조.

    Brace style: Allman (D-19). 신규 라인 위에 `//260430 hbk Phase 17 D-02` 주석.
  </action>
  <verify>
    <automated>
      grep -c "circlePolarity" WPF_Example/Halcon/Algorithms/DatumFindingService.cs && \
      grep -c "Circle_RadialDirection" WPF_Example/Halcon/Algorithms/DatumFindingService.cs && \
      msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "circlePolarity" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` returns ≥ 2 라인 (1 선언 + 1 사용).
    - `grep -c "Circle_RadialDirection" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` returns ≥ 1 (선언 라인 내 reference).
    - `grep -c "config.Circle_EdgePolarity" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` returns 0 (RadialDirection override 후 EdgePolarity 미사용 — 본 caller 한정).
    - **Algorithm preservation diff bound (D-17 본 Plan):**
      `git diff WPF_Example/Halcon/Algorithms/DatumFindingService.cs | grep -E "^\\+[^+]" | grep -v "^\\+\\+\\+" | grep -v "^\\+\\s*//" | grep -v "^\\+\\s*$" | wc -l` ≤ **2** (주석/공백 제외 신규 코드 라인 ≤ 2).
      `git diff WPF_Example/Halcon/Algorithms/DatumFindingService.cs | grep -E "^-[^-]" | grep -v "^---" | wc -l` ≤ **1** (인자 1 개 교체 — 1 라인 삭제).
    - `git diff WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs | wc -l` = **0** (보존 강제, D-17).
    - `grep -c "DetectedOriginRow\\|DetectedOriginCol\\|DetectedRefAngle\\|DetectedEdgeCount\\|DetectedFitRMSE\\|DetectedAngleDeg" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` returns 0 (Plan 17-03 범위, 본 Plan 미구현).
    - `grep -c "//260430 hbk Phase 17 D-02" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` returns ≥ 2.
    - msbuild Debug/x64 exit code = 0, 신규 warning 0 on DatumFindingService.cs.
  </acceptance_criteria>
  <done>
    TryTeachCircleTwoHorizontal 가 Circle_RadialDirection 을 polarity 로 매핑하여 TryFindCircleByPolarSampling 에 전달. 알고리즘 계산 로직 0 라인 변경 (D-17). VisionAlgorithmService 0 라인. 빌드 PASS.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → PropertyGrid | Local user types/selects values into PropertyGrid (untrusted strings into Circle_RadialDirection / *_EdgeDirection). |
| INI file → DatumConfig | User-controlled recipe INI file may contain arbitrary strings for Circle_RadialDirection (loaded via ParamBase reflection). |

No new trust boundary added. No network surface. No data store change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Tampering | DatumConfig.Circle_RadialDirection | mitigate | EnsurePerRoiDefaults sentinel "" → "Inward" fallback (Step C). DatumFindingService caller uses `OrdinalIgnoreCase` equality with explicit "Outward" — any other string defaults to "positive" (safe-default). No string is passed unsanitized to HALCON. |
| T-17-01-02 | Tampering | INI recipe file | accept | INI 파일은 로컬 디스크의 사용자 통제 영역. RadialDirection 외 값 입력 시 fallback "Inward" 적용. ParamBase reflection 자체는 Phase 5 부터 사용 중 — 기존 위협 모델 변경 없음. |
| T-17-01-03 | DoS | RenderCircleStripOverlay 루프 제거 | mitigate | stepCount 루프 제거 → DispLine 호출 N → 4 로 축소. UI thread 경량화 (긍정 영향). |
| T-17-01-04 | Information Disclosure | DatumConfig PropertyGrid Description tooltip | accept | tooltip 텍스트는 UI 사용자 가이드, 기밀 데이터 미포함. |
| T-17-01-05 | Tampering | DatumFindingService circlePolarity local | mitigate | 신규 string 변수는 stack-local, 외부 노출 없음. polarity 인자 도메인이 {"positive", "negative"} 로 축소되어 기존 polarity 분기 대비 *공격 표면 감소*. |

No high-severity threats. Algorithm preservation (D-17) 은 회귀 위험 통제 정책으로 이미 보장 (≤ 2 라인 bound + diff acceptance).
</threat_model>

<verification>
Phase-level checks:

```bash
# 알고리즘 보존 (D-17)
git diff WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs | wc -l  # = 0
git diff WPF_Example/Halcon/Algorithms/DatumFindingService.cs | grep -E "^\\+[^+]" | grep -v "^\\+\\s*//" | grep -v "^\\+\\s*$" | wc -l  # ≤ 2

# Plan 17-01 범위 변경 카운트
grep -c "//260430 hbk Phase 17 D-01" WPF_Example/Halcon/Display/HalconDisplayService.cs   # ≥ 1
grep -c "//260430 hbk Phase 17 D-02" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs # ≥ 5
grep -c "//260430 hbk Phase 17 D-02" WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs # ≥ 1
grep -c "//260430 hbk Phase 17 D-02" WPF_Example/Halcon/Algorithms/DatumFindingService.cs # ≥ 2
grep -c "//260430 hbk Phase 17 D-04" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs # ≥ 6

# 빌드
msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64

# Plan 영역 충돌 방지 (Plan 17-02/17-03 영역 미침범)
grep -c "ICustomTypeDescriptor" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs       # = 0 (Plan 17-02)
grep -c "DetectedOriginRow" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs           # = 0 (Plan 17-03)
grep -c "DetectedEdgeCount" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs           # = 0 (Plan 17-03)
```

회귀 검증 (수동, UAT 17-04 가 정식 검증):
- Phase 16 INI 레시피 (Circle_RadialDirection 미존재) 로드 후 EnsurePerRoiDefaults 가 "Inward" 로 보충됨을 확인.
- CircleTwoHorizontal Datum 선택 + ROI 그리기 시 캔버스에 회색 strip 사각형이 1개만 (3시 방향) 보임.
</verification>

<success_criteria>
- 모든 must_haves.truths 가 grep + msbuild 로 검증 가능.
- DatumFindingService.cs 신규 코드 라인 ≤ 2 (주석/공백 제외) — D-17 enforced.
- VisionAlgorithmService.cs diff = 0 라인 — D-17 enforced.
- HalconDisplayService.RenderCircleStripOverlay 가 stepCount 루프 0개 / thetaRad=0.0 단일 호출 1개 보유.
- DatumConfig.Circle_RadialDirection 이 PropertyGrid ComboBox (EdgeOptionLists.RadialDirections) 로 노출되며 INI 하위호환 (sentinel "" + "Inward" fallback).
- 6 개 *_EdgeDirection 필드에 한국어 Description tooltip 부착 (D-04).
- msbuild Debug/x64 PASS, 신규 warning 0.
- Plan 17-02/17-03 영역 (ICustomTypeDescriptor / transient 필드 / write-back / RenderDatumFindResult) 미침범.
</success_criteria>

<output>
After completion, create `.planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-01-SUMMARY.md` with:
- 변경 파일 4개 (HalconDisplayService.cs, DatumConfig.cs, EdgeOptionLists.cs, DatumFindingService.cs) + 라인 카운트
- D-17 acceptance bound 실측치 (DatumFindingService 추가 라인 / VisionAlgorithmService diff 0 확인)
- msbuild output 요약 (PASS + warning delta)
- 다음 단계: Plan 17-02 (Cluster B+C — Edit 모드 + ICustomTypeDescriptor + 모달)
</output>
