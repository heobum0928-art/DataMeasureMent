---
phase: 17-datum-ux-circle-strip-1-test-find-detectedorigin-hover
plan: 03
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
  - WPF_Example/UI/ContentItem/MainView.xaml
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: true
requirements_addressed:
  - P17-D-13
  - P17-D-14
  - P17-D-15
  - P17-D-16
  - P17-D-17
  - P17-D-18
  - P17-D-19
  - P17-D-20
  - 16-UAT-carry-#5
  - 16-UAT-carry-#17
  - 16-UAT-carry-#18
must_haves:
  truths:
    - "DatumConfig 에 transient DetectedOriginRow/Col + DetectedRefAngle 3 개 volatile double 필드가 [Browsable(false)] [PropertyTools.DataAnnotations.Browsable(false)] [JsonIgnore] 3종 attribute 로 추가된다 (D-13)"
    - "DatumConfig 에 [Browsable(true)] [ReadOnly(true)] DetectedEdgeCount (int) / DetectedFitRMSE (double) / DetectedAngleDeg (double) 3 개 결과 메트릭 필드가 \"Datum|Result\" 카테고리로 추가된다 (D-16)"
    - "DatumFindingService.TryFindDatum 성공 분기에서 DetectedOrigin* + DetectedRefAngle + DetectedEdgeCount + DetectedAngleDeg 결과 transient 필드에 write-back 라인이 추가된다 (D-13/D-16)"
    - "DatumFindingService.TryFindDatum 실패 / 조기 return 분기에서 LastFindSucceeded=false 보장 (D-13)"
    - "DatumFindingService.cs 의 *계산 로직* 라인은 한 줄도 변경되지 않는다 (D-17 — 신규 write-back 라인 ≤ 8, 기존 라인 수정 0)"
    - "VisionAlgorithmService.cs 는 한 줄도 변경되지 않는다 (D-17)"
    - "HalconDisplayService.RenderDatumFindResult 메소드의 본문이 purple DispCross size=14 + 좌표 텍스트 + DetectedRefAngle 화살표 패턴으로 교체된다 (D-13)"
    - "HalconDisplayService.RenderDatumOverlay 의 LastTeachSucceeded 분기 마지막에 RenderDatumFindResult(window, datum) 호출이 추가된다 (D-13 z-stack last)"
    - "MainView.xaml 의 canvasToolbar Grid Column 2 에 X/Y/Gray 3 TextBlock (txt_hoverX, txt_hoverY, txt_hoverG) 이 추가된다 (D-15)"
    - "MainView.UpdatePointerLabel 가 기존 label_pos 갱신 + 신규 3 TextBlock 갱신을 양쪽 수행한다 (D-15 — 기존 PointerInfoChanged 파이프라인 재사용, 신규 GetGrayval 호출 0)"
    - "MainView.BtnTestFindDatum_Click 성공 경로가 TryFindDatum 호출 → transient 필드 자동 write-back → SetDatumOverlay(datum, true) → RenderDatumFindResult 자동 호출 (RenderDatumOverlay 내부) 흐름이 wired 된다 (D-14)"
    - "Plan 17-02 의 InspectionListView.OnParamEditorSelectionChanged 5-step 리셋에 DetectedOriginRow/Col/RefAngle = 0 라인이 추가된다 (Plan 17-02 의존성 wiring)"
    - "msbuild Debug/x64 PASS, 신규 warning 0 on 수정 범위"
  artifacts:
    - path: WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
      provides: "transient DetectedOrigin* + 결과 메트릭 6 신규 필드 (Plan 17-03 영역만)"
      contains: "DetectedOriginRow"
    - path: WPF_Example/Halcon/Algorithms/DatumFindingService.cs
      provides: "TryFindDatum 성공/실패 분기 transient write-back 라인"
      contains: "config.DetectedOriginRow"
    - path: WPF_Example/Halcon/Display/HalconDisplayService.cs
      provides: "RenderDatumFindResult 본문 교체 (purple DispCross + 좌표 텍스트 + 화살표) + RenderDatumOverlay z-stack last 호출"
      contains: "purple"
    - path: WPF_Example/UI/ContentItem/MainView.xaml
      provides: "canvasToolbar X/Y/Gray TextBlock 3개"
      contains: "txt_hoverX"
    - path: WPF_Example/UI/ContentItem/MainView.xaml.cs
      provides: "UpdatePointerLabel 확장 + BtnTestFindDatum_Click 성공 경로"
      contains: "txt_hoverX"
  key_links:
    - from: "DatumFindingService.TryFindDatum"
      to: "DatumConfig.DetectedOriginRow/Col + DetectedRefAngle + DetectedEdgeCount + DetectedAngleDeg + LastFindSucceeded"
      via: "성공 분기 transient write-back"
      pattern: "config\\.DetectedOriginRow"
    - from: "HalconDisplayService.RenderDatumOverlay LastTeachSucceeded 분기 마지막"
      to: "RenderDatumFindResult(window, datum)"
      via: "z-stack last 호출"
      pattern: "RenderDatumFindResult"
    - from: "MainView.UpdatePointerLabel"
      to: "txt_hoverX / txt_hoverY / txt_hoverG"
      via: "기존 PointerInfoChanged 파이프라인"
      pattern: "txt_hoverX.Text"
    - from: "MainView.BtnTestFindDatum_Click"
      to: "DatumFindingService.TryFindDatum + halconViewer.SetDatumOverlay"
      via: "Test Find 트리거 → write-back → re-render"
      pattern: "TryFindDatum"
---

<objective>
Cluster D 구현 — DetectedOrigin transient 필드 + Test Find DetectedOrigin 시각화 + ROI 결과 메트릭 PropertyGrid 노출 + 마우스 hover X/Y/Gray.

Phase 16 UAT carry-over #5 (부분), #17, #18 을 한 번에 해소.

D-13 (#17): DetectedOrigin transient 필드 (Row/Col/RefAngle 3 volatile double, [Browsable(false)] [JsonIgnore] [PropertyTools Browsable(false)] 3종 attribute) + TryFindDatum 성공 시 write-back + HalconDisplayService.RenderDatumFindResult 본문 교체 (purple DispCross size=14 + 좌표 텍스트 + DetectedRefAngle 화살표).

D-14: 기존 btn_testFindDatum 재사용 (UI-SPEC LOCKED — 신규 버튼 추가 없음). Click 핸들러가 TryFindDatum 호출 → 자동 write-back (D-13) → SetDatumOverlay(datum, true) → RenderDatumOverlay 가 RenderDatumFindResult 자동 호출.

D-15 (#18): 마우스 hover X/Y/Gray 상단 툴바 1줄 — PATTERNS.md gap #4 분석에 따라 *기존 PublishPointerInfo 파이프라인 (MainResultViewerControl L1297-1319) 재사용*. 신규 GetGrayval 호출 0. canvasToolbar 에 3 TextBlock 추가 + UpdatePointerLabel 1 라인 확장.

D-16 (#5 부분): ROI 결과 메트릭 PropertyGrid 노출 — DetectedEdgeCount (int) / DetectedFitRMSE (double) / DetectedAngleDeg (double) 3 개 [ReadOnly(true)] 필드. 별도 미리보기 창은 deferred (CONTEXT.md 명시).

Plan 17-02 wiring: OnParamEditorSelectionChanged 5-step 리셋의 Step 3 DetectedOriginRow/Col/RefAngle 0 라인 추가.

Purpose: 사용자가 btn_testFindDatum 으로 런타임 Datum 검출 결과를 즉시 시각화 + 결과 품질 메트릭 PropertyGrid 확인 + 캔버스 hover 시 픽셀 좌표/밝기 라이브 표시.

Output: DatumConfig.cs (transient/metric 블록만 — Plan 17-01 RadialDirection 블록 + Plan 17-02 ICustomTypeDescriptor 블록과 영역 분리), DatumFindingService.cs (TryFindDatum write-back ≤ 8 라인, 계산 로직 0 변경), HalconDisplayService.cs (RenderDatumFindResult 본문 교체 + RenderDatumOverlay 1 라인 호출 추가), MainView.xaml (3 TextBlock), MainView.xaml.cs (UpdatePointerLabel 확장 + BtnTestFindDatum_Click 성공 경로 + InspectionListView 5-step Step 3 wiring).

알고리즘 보존 (D-17): DatumFindingService.cs 의 *계산 로직* 0 라인 변경. 허용: TryFindDatum 의 *결과만* transient 필드에 기록하는 신규 라인 ≤ 8 (D-13 + D-16 combined). VisionAlgorithmService.cs 0 라인.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-CONTEXT.md
@.planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-PATTERNS.md
@.planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-UI-SPEC.md
@.planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-01-SUMMARY.md
@.planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-02-SUMMARY.md
@.planning/phases/16-datum-circle-strip-redesign-algorithmtype-binding-fix/16-UAT.md
@.planning/phases/13-datum-algorithm-extensibility/13-05-SUMMARY.md

<interfaces>
<!-- Existing call sites and helpers this plan touches -->

DatumConfig.cs (transient field group L341-413, after Plan 17-01 추가):
  // Phase 13-05 raw edges 패턴 — volatile HTuple/double + [Browsable(false)] [PropertyTools Browsable(false)] [JsonIgnore]
  [PropertyTools.DataAnnotations.Browsable(false)]
  public double Line1Detected_RowS { get; set; }  // 예시 — 본 Plan 의 패턴 source

  // Plan 17-03 추가 위치: 기존 transient 블록 끝 (L413 직후)
  public double DetectedOriginRow { get; set; }  // (예고)
  public double DetectedOriginCol { get; set; }
  public double DetectedRefAngle { get; set; }
  // + Browsable(true) ReadOnly(true) DetectedEdgeCount/FitRMSE/AngleDeg

DatumFindingService.cs (TryFindDatum L46-150 근방):
  L46:  EnsurePerRoiDefaults(config) 호출
  L70, L92, L110, L116: 조기 return false 분기 (LastFindSucceeded=false 추가 필요)
  L73-74: config.Line1_DetectedEdgeRows = line1RawRows; (Phase 13-05 write-back 패턴)
  L132 (success 직전): curRow.D / curCol.D / curAngle 가 계산된 상태 — 본 Plan 에서 transient write-back 라인 추가
  L138 (catch 블록): error = ex.Message; 직전에 LastFindSucceeded = false

HalconDisplayService.cs:
  L216-244: RenderDatumFindResult — Phase 13 D-07 기존 본문 (orange + RefOriginRow/Col)
  L246-264: EnsureFontInitialized — REUSE 그대로
  L387-408: DrawExtendedLine — Plan 17-03 미사용
  L502-695: RenderDatumOverlay — LastTeachSucceeded 분기 (L688 근방, 분기 마지막) 에 RenderDatumFindResult(window, datum) 호출 추가

MainResultViewerControl.xaml.cs (read-only 참조):
  L1297-1319: PublishPointerInfo — 이미 GetGrayval 호출 + PointerInfoChanged 이벤트 발행
  PointerInfoChanged event 시그니처: (double x, double y, double? grayValue)

MainView.xaml.cs:
  L367-383: HalconViewer_PointerInfoChanged + UpdatePointerLabel(double x, double y, double? grayValue)
    private void UpdatePointerLabel(double x, double y, double? grayValue) {
        if (label_pos == null) return;
        label_pos.Content = string.Format("X:{0:0.0}, Y:{1:0.0}, G:{2}",
            x, y, grayValue.HasValue ? grayValue.Value.ToString("0.0") : "-");
    }

  L1497-1532: BtnTestFindDatum_Click — Plan 17-02 가 실패 모달 변환만 처리. 본 Plan 이 성공 경로 본격 변경.

MainView.xaml (canvasToolbar Grid):
  L141-146: label_drawHint (Column 1) + label_pointCount (Column 2 — Visibility="Collapsed")
  → label_pointCount 위치에 hover TextBlock StackPanel 배치 또는 label_pointCount 대체.

UI-SPEC LOCKED 색상: "purple" (DispCross + 좌표 텍스트 + 화살표 통일)
UI-SPEC LOCKED 크기: DispCross size=14, lineWidth=2, arrowLength=20, arrowHead=5
UI-SPEC LOCKED 포맷: "Find ({row:F1}, {col:F1})"

EDatumAlgorithm enum (Phase 12-01): TwoLineIntersect / CircleTwoHorizontal / VerticalTwoHorizontal
TryFindDatum 의 검출 라인/원/교점 계산 로직 — 본 Plan 0 라인 변경.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DatumConfig — transient DetectedOrigin* + 결과 메트릭 6 신규 필드 (Plan 17-03 영역만)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (L341-413 transient 블록 — Plan 17-01 의 RadialDirection 블록 위치 확인 + Plan 17-02 의 ICustomTypeDescriptor 블록 위치 확인 → 본 Plan 영역 = transient 블록 끝)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-CONTEXT.md (D-13, D-16, code_context § "transient volatile HTuple/double 필드 + [Browsable(false)] + ParamBase reflection 자동 무시")
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-PATTERNS.md (DatumConfig.cs §D-13/D-16 + caveat re: INI)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-UI-SPEC.md (§ "DatumConfig transient 필드" 표)
    - .planning/phases/13-datum-algorithm-extensibility/13-05-SUMMARY.md (Phase 13-05 raw edges 패턴 — 동일 [Browsable(false)] + ParamBase reflection 자동 무시 검증된 사례)
  </read_first>
  <action>
    **Step A — transient 필드 블록 추가**.

    위치: 기존 transient 필드 블록 (예: L385-413) 끝, 다음 메소드 시작 직전. Plan 17-01 의 Circle_RadialDirection 블록 (Circle 그룹 인근, 별도 위치) 및 Plan 17-02 의 ICustomTypeDescriptor 블록 (클래스 끝부분, 생성자 직전) 과 *영역 분리* 유지.

    PATTERNS.md §D-13/D-16 excerpt 그대로:

    ```csharp
    //260430 hbk Phase 17 D-13 — DetectedOrigin transient (TryFindDatum write-back, INI 0 영향 — ParamBase double 직렬화하나 [Browsable(false)] 로 PropertyGrid 미표시)
    [System.ComponentModel.Browsable(false)]
    [PropertyTools.DataAnnotations.Browsable(false)]
    [Newtonsoft.Json.JsonIgnore]
    public double DetectedOriginRow { get; set; } //260430 hbk Phase 17 D-13
    [System.ComponentModel.Browsable(false)]
    [PropertyTools.DataAnnotations.Browsable(false)]
    [Newtonsoft.Json.JsonIgnore]
    public double DetectedOriginCol { get; set; } //260430 hbk Phase 17 D-13
    [System.ComponentModel.Browsable(false)]
    [PropertyTools.DataAnnotations.Browsable(false)]
    [Newtonsoft.Json.JsonIgnore]
    public double DetectedRefAngle { get; set; } //260430 hbk Phase 17 D-13

    //260430 hbk Phase 17 D-16 — 결과 메트릭 PropertyGrid 노출 (ReadOnly, 사용자가 검출 품질 즉시 확인)
    [Category("Datum|Result")] //260430 hbk Phase 17 D-16
    [System.ComponentModel.ReadOnly(true)] //260430 hbk Phase 17 D-16
    public int DetectedEdgeCount { get; set; } //260430 hbk Phase 17 D-16

    [Category("Datum|Result")]
    [System.ComponentModel.ReadOnly(true)]
    public double DetectedFitRMSE { get; set; } //260430 hbk Phase 17 D-16

    [Category("Datum|Result")]
    [System.ComponentModel.ReadOnly(true)]
    public double DetectedAngleDeg { get; set; } //260430 hbk Phase 17 D-16
    ```

    **Step B — PropertyTools ReadOnly 검증** (PATTERNS.md gap #5):

    PropertyTools.Wpf 가 `System.ComponentModel.ReadOnly(true)` 를 honor 하는지 확인:
    ```bash
    grep -rn "ReadOnly\\b" packages/PropertyTools.Wpf*/lib/ 2>/dev/null | head -5
    ```

    또는 PropertyTools 소스에서 `ReadOnlyAttribute` 처리 부 grep:
    ```bash
    grep -rn "ReadOnlyAttribute\\|System.ComponentModel.ReadOnly" packages/PropertyTools* 2>/dev/null | head -10
    ```

    만약 PropertyTools 가 `[PropertyTools.DataAnnotations.ReadOnly]` 만 honor 하면 (System.ComponentModel.ReadOnly 미지원), attribute 를 다음으로 교체:
    ```csharp
    [PropertyTools.DataAnnotations.ReadOnly(true)]
    ```
    또는 양쪽 부착 (compile-time + runtime safety):
    ```csharp
    [System.ComponentModel.ReadOnly(true)]
    [PropertyTools.DataAnnotations.ReadOnly(true)]
    ```

    어느 쪽이든 SUMMARY.md 에 검증 결과 명시.

    **Step C — 영역 충돌 방지**:
    - Plan 17-01 의 `Circle_RadialDirection` + `Circle_RadialDirectionList` 라인은 *그대로 유지* (수정 0).
    - Plan 17-02 의 `ICustomTypeDescriptor` 메소드 그룹 + `IsHiddenForAlgorithm` helper + 클래스 헤더 `, System.ComponentModel.ICustomTypeDescriptor` 는 *그대로 유지* (수정 0).
    - EnsurePerRoiDefaults: DetectedOrigin/Metric 필드는 default 0.0/0 자체가 unset sentinel — EnsurePerRoiDefaults 추가 라인 *불필요* (Pattern S2 의 적용 제외 명시, PATTERNS.md L573).

    **Step D — ParamBase INI 자동 무시 검증** (read-only):
    ```bash
    grep -n "Browsable\\|ParamBase.Save\\|ParamBase.Load" WPF_Example/Sequence/Param/ParamBase.cs
    ```
    Phase 11/13-05 progress note: ParamBase reflection 이 [Browsable(false)] 를 무시하고 public double 을 직렬화 → INI 에 0 으로 기록됨. 본 Plan 은 *DetectedOrigin* 등 transient 가 INI 에 0 으로 직렬화되는 것을 *허용* (UI-SPEC LOCKED — Phase 13-05 D-VIZ-01 패턴 그대로). 신규 IO 위협 없음.

    **Step E — D-16 메트릭 INI 보존 정책**: DetectedEdgeCount/FitRMSE/AngleDeg 도 ParamBase 가 INI 에 기록할 가능성 있음 — Phase 13-05 패턴과 동일하게 *읽기 전용 표시 + INI 0 기록 허용* (런타임 측정 결과는 매 실행마다 갱신되므로 INI 의 stale 값은 의미 없음). 별도 처리 없음.

    Brace style: K&R (DatumConfig 기존 스타일).
  </action>
  <verify>
    <automated>
      grep -c "DetectedOriginRow\\|DetectedOriginCol\\|DetectedRefAngle" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs && \
      grep -c "DetectedEdgeCount\\|DetectedFitRMSE\\|DetectedAngleDeg" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs && \
      grep -c "Datum|Result" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs && \
      msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public double DetectedOriginRow\\b" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns 1.
    - `grep -c "public double DetectedOriginCol\\b" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns 1.
    - `grep -c "public double DetectedRefAngle\\b" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns 1.
    - `grep -c "public int DetectedEdgeCount\\b" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns 1.
    - `grep -c "public double DetectedFitRMSE\\b" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns 1.
    - `grep -c "public double DetectedAngleDeg\\b" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns 1.
    - `grep -c "Datum|Result" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns ≥ 3 (3 메트릭 필드의 Category attribute).
    - `grep -c "JsonIgnore" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns ≥ 3 (DetectedOrigin/RefAngle 3 개 필드 모두 [JsonIgnore]).
    - `grep -B1 "public double DetectedOriginRow" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs | grep -c "PropertyTools.DataAnnotations.Browsable(false)"` returns ≥ 1 (PropertyTools attribute 부착 확인).
    - `grep -c "ReadOnly" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns ≥ 3 (3 메트릭 필드).
    - `grep -c "//260430 hbk Phase 17 D-13" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns ≥ 3.
    - `grep -c "//260430 hbk Phase 17 D-16" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns ≥ 4.
    - 회귀 (Plan 17-01): `grep -c "Circle_RadialDirection" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns ≥ 4.
    - 회귀 (Plan 17-02): `grep -c "ICustomTypeDescriptor\\|IsHiddenForAlgorithm" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns ≥ 4.
    - msbuild Debug/x64 exit code = 0, 신규 warning 0 on DatumConfig.cs.
  </acceptance_criteria>
  <done>
    DatumConfig 가 6 신규 필드 (3 transient + 3 메트릭) 보유. transient 3 개는 [Browsable(false)] [PropertyTools Browsable(false)] [JsonIgnore] 3종 attribute 로 PropertyGrid + JSON 미노출. 메트릭 3 개는 "Datum|Result" 카테고리 ReadOnly. Plan 17-01 / 17-02 영역 회귀 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: DatumFindingService.TryFindDatum — 성공/실패 분기 transient write-back (algorithm preservation, ≤ 8 라인)</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (TryFindDatum 메소드 전체 — L46-150 근방, 모든 조기 return false 위치 + 성공 분기 위치 + catch 블록 위치 식별)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-CONTEXT.md (D-13, D-16, D-17)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-PATTERNS.md (DatumFindingService.cs §D-13/D-16 write-back)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-01-SUMMARY.md (Plan 17-01 의 polarity 매핑 라인 위치 확인 — 본 Plan 의 write-back 라인과 분리)
    - .planning/phases/13-datum-algorithm-extensibility/13-05-SUMMARY.md (write-back 라인 패턴 source)
  </read_first>
  <action>
    **Step A — TryFindDatum 성공 분기 write-back 라인 추가**.

    grep 으로 정확 위치 식별:
    ```bash
    grep -n "TryFindDatum\\|return true\\|return false\\|catch.*Exception" WPF_Example/Halcon/Algorithms/DatumFindingService.cs | head -40
    ```

    TryFindDatum 의 메소드 본문에서 *최종 성공 직전* (예: `return true;` 직전, 또는 try 블록 끝부분에서 curRow.D / curCol.D / curAngle 변수가 모두 계산된 라인 다음) 에 다음 ≤ 8 라인 추가:

    ```csharp
    //260430 hbk Phase 17 D-13 — DetectedOrigin transient write-back (RenderDatumFindResult 입력)
    config.DetectedOriginRow = curRow.D;
    config.DetectedOriginCol = curCol.D;
    config.DetectedRefAngle  = curAngle;
    //260430 hbk Phase 17 D-16 — 결과 메트릭 (검출 점 개수 합계 + 각도 deg)
    config.DetectedEdgeCount = (line1RawRows != null ? line1RawRows.TupleLength() : 0)
                             + (line2RawRows != null ? line2RawRows.TupleLength() : 0);
    config.DetectedFitRMSE   = 0.0; //260430 hbk Phase 17 D-16 — fit RMSE 미수집 (Plan 17-03 placeholder)
    config.DetectedAngleDeg  = curAngle * 180.0 / System.Math.PI;
    config.LastFindSucceeded = true; //260430 hbk Phase 17 D-13
    ```

    > **변수명 적응**: TryFindDatum 의 실제 결과 변수명이 다르면 (예: curRow → originRow / curAngle → datumAngle) grep 으로 확인 후 그 이름 사용. 알고리즘 본문은 *건드리지 않고* 결과 변수만 인용.

    > **DetectedEdgeCount 변형 — TLI 가 아닌 알고리즘 (CTH/VTH) 분기에서는 line1RawRows/line2RawRows 대신 다른 raw 점 변수**: TryFindDatum 가 alg switch dispatch 한다면 각 분기 내부에 같은 4 라인 (DetectedOriginRow/Col/RefAngle + LastFindSucceeded=true) 반복. 알고리즘별 EdgeCount 변수가 다르면 정확한 변수명으로 매핑. **합 ≤ 8 라인 (D-17 bound) — 만약 각 알고리즘 분기마다 5 라인 × 3 알고리즘 = 15 라인 필요하면 SUMMARY.md 에 명시하고 D-17 bound 를 ≤ 16 라인으로 조정 (이는 정당한 알고리즘 보존 — 결과 write-back 만, 계산 로직 0)**.

    실제 TryFindDatum 구조 분석 후 정확한 라인 수 결정. 본 Action 의 acceptance_criteria 에서 bound 를 명확히 검증.

    **Step B — 실패/조기 return 분기 LastFindSucceeded=false**:

    PATTERNS.md §D-13 분석에 따라 두 가지 옵션:
    - 옵션 (a): 조기 return false 4 곳 (L70/L92/L110/L116) + catch 블록 (L138) 각각 앞에 `config.LastFindSucceeded = false;` 1 라인씩 추가 → 5 라인.
    - 옵션 (b): 메소드 시작에 single boolean local + 모든 return 경로에서 catch-finally 패턴.

    **본 Plan 채택: 옵션 (a)** — 단순/명확, D-17 bound 영향 ≤ 5 라인. 만약 메소드 시작에 `EnsurePerRoiDefaults(config)` 직후에 `config.LastFindSucceeded = false;` 1 라인을 추가하면 모든 조기 return 자동 처리 (성공 분기에서 true 로 덮어쓰기). 이 형태가 더 안전하고 ≤ 1 라인.

    **본 Plan 최종 채택: 메소드 시작 1 라인 reset + 성공 분기 끝 LastFindSucceeded=true (위 Step A 에 포함)**:

    ```csharp
    //260430 hbk Phase 17 D-13 — 메서드 시작 시 reset (조기 return / catch 시 false 보장)
    config.LastFindSucceeded = false;
    ```

    위치: TryFindDatum 본문 시작 (예: EnsurePerRoiDefaults(config) 호출 직후).

    **Step C — 알고리즘 보존 검증 (D-17)**:

    합산 acceptance bound (Plan 17-03):
    - 메서드 시작 reset 1 라인 + 성공 분기 write-back ≤ 8 라인 = 본 Task 신규 ≤ 9 라인.
    - Plan 17-01 의 baseline (≤ 2 라인) + Plan 17-03 신규 (≤ 9 라인) = DatumFindingService.cs 누적 신규 코드 ≤ 11 라인 (주석/공백 제외).

    **본 Plan acceptance bound: 본 Task 신규 코드 라인 (주석/공백 제외) ≤ 9 라인.**

    *기존 라인 수정 0* — write-back 라인은 모두 신규 추가, 기존 코드 0 라인 modify/delete.

    Brace style: Allman (DatumFindingService 기존 스타일).
  </action>
  <verify>
    <automated>
      grep -c "config.DetectedOriginRow\\|config.DetectedOriginCol\\|config.DetectedRefAngle" WPF_Example/Halcon/Algorithms/DatumFindingService.cs && \
      grep -c "config.LastFindSucceeded" WPF_Example/Halcon/Algorithms/DatumFindingService.cs && \
      msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "config.DetectedOriginRow" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` returns ≥ 1.
    - `grep -c "config.DetectedOriginCol" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` returns ≥ 1.
    - `grep -c "config.DetectedRefAngle" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` returns ≥ 1.
    - `grep -c "config.DetectedEdgeCount" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` returns ≥ 1.
    - `grep -c "config.DetectedAngleDeg" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` returns ≥ 1.
    - `grep -c "config.LastFindSucceeded" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` returns ≥ 2 (시작 reset false + 성공 분기 true).
    - **Algorithm preservation diff bound (D-17 본 Plan 신규):**
      `git diff WPF_Example/Halcon/Algorithms/DatumFindingService.cs | grep -E "^\\+[^+]" | grep -v "^\\+\\+\\+" | grep -v "^\\+\\s*//" | grep -v "^\\+\\s*$" | wc -l` ≤ **(Plan 17-01 baseline 2) + 9 = 11** (누적 — 주석/공백 제외 신규 코드 ≤ 11).
      `git diff WPF_Example/Halcon/Algorithms/DatumFindingService.cs | grep -E "^-[^-]" | grep -v "^---" | wc -l` ≤ **1** (Plan 17-01 의 EdgePolarity → circlePolarity 인자 교체 1 라인 외 0).
    - `git diff WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs | wc -l` = **0** (보존 강제, D-17).
    - `grep -c "//260430 hbk Phase 17 D-13" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` returns ≥ 2.
    - `grep -c "//260430 hbk Phase 17 D-16" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` returns ≥ 2.
    - msbuild Debug/x64 exit code = 0, 신규 warning 0 on DatumFindingService.cs.
  </acceptance_criteria>
  <done>
    TryFindDatum 가 시작 시 LastFindSucceeded=false 리셋, 성공 분기에서 DetectedOrigin/RefAngle/EdgeCount/FitRMSE/AngleDeg + LastFindSucceeded=true write-back. 조기 return / catch 시 LastFindSucceeded=false 자동 유지. 계산 로직 0 라인 변경, VisionAlgorithmService 0 라인 (D-17). 누적 DatumFindingService 신규 코드 ≤ 11 라인.
  </done>
</task>

<task type="auto">
  <name>Task 3: HalconDisplayService — RenderDatumFindResult 본문 교체 (purple) + RenderDatumOverlay z-stack last 호출</name>
  <files>WPF_Example/Halcon/Display/HalconDisplayService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Display/HalconDisplayService.cs (L216-244 RenderDatumFindResult 기존 본문, L246-264 EnsureFontInitialized, L502-695 RenderDatumOverlay 의 LastTeachSucceeded 분기 마지막 위치 — L688 근방)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-CONTEXT.md (D-13)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-PATTERNS.md (HalconDisplayService.cs §D-13 — purple variant 코드 excerpt)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-UI-SPEC.md (§ "RenderDatumFindResult 변경 사항", § "Render Order")
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-01-SUMMARY.md (Plan 17-01 의 RenderCircleStripOverlay 영역 — 본 Plan 영역과 분리 확인)
  </read_first>
  <action>
    **Step A — RenderDatumFindResult 본문 교체** (L216-244).

    메소드 시그니처는 *유지*: `public void RenderDatumFindResult(HWindow window, DatumConfig datum)`.

    L218-243 의 기존 본문 (orange + RefOriginRow/Col 기반) 을 PATTERNS.md §D-13 코드 excerpt 로 교체:

    ```csharp
    public void RenderDatumFindResult(HWindow window, DatumConfig datum)
    {
        if (window == null || datum == null) return;
        if (!datum.LastFindSucceeded) return; //260430 hbk Phase 17 D-13 — find 성공 분기에서만 렌더
        try
        {
            //260430 hbk Phase 17 D-13 — purple DispCross size=14 lineWidth=2 (UI-SPEC LOCKED)
            HOperatorSet.SetColor(window, "purple");
            HOperatorSet.SetLineWidth(window, 2);
            const double crossHalf = 14.0; //260430 hbk Phase 17 D-13
            HOperatorSet.DispLine(window,
                datum.DetectedOriginRow - crossHalf, datum.DetectedOriginCol,
                datum.DetectedOriginRow + crossHalf, datum.DetectedOriginCol);
            HOperatorSet.DispLine(window,
                datum.DetectedOriginRow, datum.DetectedOriginCol - crossHalf,
                datum.DetectedOriginRow, datum.DetectedOriginCol + crossHalf);

            //260430 hbk Phase 17 D-13 — 좌표 텍스트 "Find (row, col)"
            EnsureFontInitialized(window);
            HOperatorSet.SetTposition(window,
                datum.DetectedOriginRow - crossHalf - 22,
                datum.DetectedOriginCol + crossHalf + 4);
            HOperatorSet.WriteString(window,
                "Find (" + datum.DetectedOriginRow.ToString("F1") + ", "
                         + datum.DetectedOriginCol.ToString("F1") + ")");

            //260430 hbk Phase 17 D-13 — DetectedRefAngle 방향 화살표 (DrawDirectionArrow 패턴 inlined, length=20, head=5)
            double angle  = datum.DetectedRefAngle;
            double aLen   = 20.0;
            double headLn = 5.0;
            double endRow = datum.DetectedOriginRow + aLen * System.Math.Sin(angle);
            double endCol = datum.DetectedOriginCol + aLen * System.Math.Cos(angle);
            HOperatorSet.DispLine(window, datum.DetectedOriginRow, datum.DetectedOriginCol, endRow, endCol);
            double a1 = angle + 2.5, a2 = angle - 2.5;
            HOperatorSet.DispLine(window, endRow, endCol,
                endRow + headLn * System.Math.Sin(a1), endCol + headLn * System.Math.Cos(a1));
            HOperatorSet.DispLine(window, endRow, endCol,
                endRow + headLn * System.Math.Sin(a2), endCol + headLn * System.Math.Cos(a2));
        }
        catch
        {
            // RenderDatumOverlay catch 관습 — 표시 오류 무시
        }
    }
    ```

    Brace style: Allman (D-19, HalconDisplayService 기존 스타일).

    **Step B — RenderDatumOverlay z-stack last 호출 추가**:

    L502-695 의 RenderDatumOverlay 메소드에서 *LastTeachSucceeded 분기 마지막* (L688 근방, 분기 종료 직전) 에 1 라인 추가:

    ```csharp
    //260430 hbk Phase 17 D-13 — z-stack last: DetectedOrigin purple cross 가 가장 위에 그려짐
    RenderDatumFindResult(window, datum);
    ```

    grep 으로 정확한 분기 끝 라인 식별:
    ```bash
    grep -n "LastTeachSucceeded\\|RenderDatumFindResult" WPF_Example/Halcon/Display/HalconDisplayService.cs | head -20
    ```

    **Step C — 영역 충돌 방지**:
    - Plan 17-01 의 RenderCircleStripOverlay 변경 (thetaRad=0.0 단일 strip) 은 *그대로 유지*. 본 Plan 은 RenderCircleStripOverlay 미수정.
    - EnsureFontInitialized / DrawExtendedLine / RenderRawEdgePoints helper 미수정.

    **Step D — 좌표 텍스트 한국어 vs 영어 결정**: UI-SPEC LOCKED `"Find (row, col)"` (영어) — 본 Plan 그대로 채택. SetTposition 은 row/col 좌표계 (HALCON 표준).
  </action>
  <verify>
    <automated>
      grep -c "LastFindSucceeded" WPF_Example/Halcon/Display/HalconDisplayService.cs && \
      grep -c "purple" WPF_Example/Halcon/Display/HalconDisplayService.cs && \
      grep -c "RenderDatumFindResult(window, datum)" WPF_Example/Halcon/Display/HalconDisplayService.cs && \
      msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "if (!datum.LastFindSucceeded) return" WPF_Example/Halcon/Display/HalconDisplayService.cs` returns 1 (RenderDatumFindResult 진입 가드).
    - `grep -c "SetColor(window, \"purple\")" WPF_Example/Halcon/Display/HalconDisplayService.cs` returns ≥ 1 (purple DispCross).
    - `grep -c "DetectedOriginRow\\|DetectedOriginCol\\|DetectedRefAngle" WPF_Example/Halcon/Display/HalconDisplayService.cs` returns ≥ 5 (DispLine 4 + 화살표 endRow/endCol 계산 + 좌표 텍스트).
    - `grep "Find (.*ToString.*F1.*ToString.*F1" WPF_Example/Halcon/Display/HalconDisplayService.cs` returns ≥ 1 (좌표 텍스트 포맷).
    - `grep -c "RenderDatumFindResult(window, datum)" WPF_Example/Halcon/Display/HalconDisplayService.cs` returns ≥ 2 (메소드 정의 + RenderDatumOverlay 마지막 호출).
    - `grep -c "//260430 hbk Phase 17 D-13" WPF_Example/Halcon/Display/HalconDisplayService.cs` returns ≥ 4.
    - 회귀 (Plan 17-01): `grep "thetaRad = 0.0" WPF_Example/Halcon/Display/HalconDisplayService.cs` returns ≥ 1 (Plan 17-01 의 단일 strip 라인 보존).
    - 회귀 (Plan 17-01): `grep -c "for.*k.*stepCount" WPF_Example/Halcon/Display/HalconDisplayService.cs` returns 0 (stepCount 루프 재도입 없음).
    - 기존 RenderDatumFindResult 본문에서 사용되던 `RefOriginRow` / `RefOriginCol` 기반 라인 제거 확인: `grep "datum.RefOriginRow\\|datum.RefOriginCol" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 본 Task 후 *기존 라인 수와 변화* — 변화량이 RenderDatumFindResult 의 기존 사용 라인 (대략 2-3 개) 만큼 감소. 정확한 검증은 git diff 로 확인.
    - msbuild Debug/x64 exit code = 0, 신규 warning 0 on HalconDisplayService.cs.
  </acceptance_criteria>
  <done>
    RenderDatumFindResult 가 LastFindSucceeded gate 하 purple DispCross size=14 + "Find (row, col)" 좌표 텍스트 + DetectedRefAngle 화살표 렌더. RenderDatumOverlay 의 LastTeachSucceeded 분기 마지막에 호출 추가 (z-stack last). Plan 17-01 의 thetaRad=0.0 단일 strip 보존.
  </done>
</task>

<task type="auto">
  <name>Task 4: MainView XAML — canvasToolbar X/Y/Gray 3 TextBlock 추가 (D-15)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml (L61-152 canvasToolbar Grid + Column 정의 + L141-146 label_drawHint / label_pointCount 분석)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-CONTEXT.md (D-15)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-PATTERNS.md (MainView.xaml 섹션)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-UI-SPEC.md (§ "Hover 좌표 + 밝기 TextBlock" 표 + Empty State)
  </read_first>
  <action>
    **Step A — label_pointCount 대체 결정 (PATTERNS.md gap 안내)**: label_pointCount 는 `Visibility="Collapsed"` 로 미사용. 본 Plan 채택: *대체* (코드 단순화). label_pointCount 라인 (L144-146) 을 다음 StackPanel 로 교체:

    ```xml
    <!--260430 hbk Phase 17 D-15 — Hover 좌표 + 밝기 (X / Y / Gray)-->
    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right"
                VerticalAlignment="Center" Margin="0,0,8,0">
        <TextBlock x:Name="txt_hoverX" Text="X: N/A" FontSize="13" Foreground="#FFAAAAAA"
                   Margin="0,0,8,0" VerticalAlignment="Center"/>
        <TextBlock Text="·" FontSize="13" Foreground="#FFAAAAAA"
                   Margin="0,0,8,0" VerticalAlignment="Center"/>
        <TextBlock x:Name="txt_hoverY" Text="Y: N/A" FontSize="13" Foreground="#FFAAAAAA"
                   Margin="0,0,8,0" VerticalAlignment="Center"/>
        <TextBlock Text="·" FontSize="13" Foreground="#FFAAAAAA"
                   Margin="0,0,8,0" VerticalAlignment="Center"/>
        <TextBlock x:Name="txt_hoverG" Text="Gray: N/A" FontSize="13" Foreground="#FFAAAAAA"
                   VerticalAlignment="Center"/>
    </StackPanel>
    ```

    **Step B — Grid 정의 무수정**: canvasToolbar Grid 의 ColumnDefinitions / Border Padding / 다른 Column (Column 0/1) 미수정.

    **Step C — label_drawHint 보존**: Column 1 의 label_drawHint 는 그대로 유지 (Plan 17-02 가 D-12 모달 변환 시 사용 패턴 제거 — 본 Plan 은 label_drawHint XAML 자체는 수정하지 않음, code-behind 에서 .Content 할당 라인이 사라졌을 뿐).

    **Step D — label_pos 보존**: 캔버스 하단 별도 오버레이 label_pos 는 *그대로 유지* (UI-SPEC § "Hover 좌표 + 밝기 TextBlock" 명시: "기존 label_pos 는 그대로 유지. 툴바 hover 표시는 추가적으로 상단에 배치"). 단, 본 Plan 은 label_pointCount 를 대체하므로 *상단 툴바 hover* 가 신규 추가, *하단 label_pos* 는 기존 유지 → 양쪽 hover 표시.

    XAML 주석은 `<!--...-->` 형식 (XAML 표준).
  </action>
  <verify>
    <automated>
      grep -c "txt_hoverX\\|txt_hoverY\\|txt_hoverG" WPF_Example/UI/ContentItem/MainView.xaml && \
      grep -c "label_pointCount" WPF_Example/UI/ContentItem/MainView.xaml
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "txt_hoverX" WPF_Example/UI/ContentItem/MainView.xaml` returns ≥ 1.
    - `grep "txt_hoverY" WPF_Example/UI/ContentItem/MainView.xaml` returns ≥ 1.
    - `grep "txt_hoverG" WPF_Example/UI/ContentItem/MainView.xaml` returns ≥ 1.
    - `grep "Text=\"X: N/A\"" WPF_Example/UI/ContentItem/MainView.xaml` returns 1.
    - `grep "Text=\"Gray: N/A\"" WPF_Example/UI/ContentItem/MainView.xaml` returns 1.
    - `grep -c "label_pointCount" WPF_Example/UI/ContentItem/MainView.xaml` returns 0 (대체 완료).
    - `grep -c "label_drawHint\\|label_pos" WPF_Example/UI/ContentItem/MainView.xaml` returns ≥ 2 (보존).
    - `grep -c "//260430 hbk Phase 17 D-15\\|260430 hbk Phase 17 D-15" WPF_Example/UI/ContentItem/MainView.xaml` returns ≥ 1 (XAML 주석 형식).
    - `grep "Grid.Column=\"2\"" WPF_Example/UI/ContentItem/MainView.xaml` returns ≥ 1 (StackPanel Column 2 배치).
  </acceptance_criteria>
  <done>
    canvasToolbar Column 2 에 X/Y/Gray 3 TextBlock StackPanel. 초기 상태 "X: N/A · Y: N/A · Gray: N/A". 기존 label_pos / label_drawHint 보존.
  </done>
</task>

<task type="auto">
  <name>Task 5: MainView code-behind — UpdatePointerLabel 확장 + BtnTestFindDatum_Click 성공 경로 + InspectionListView 5-step Step 3 wiring</name>
  <files>
    WPF_Example/UI/ContentItem/MainView.xaml.cs
    WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  </files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (L367-383 UpdatePointerLabel + HalconViewer_PointerInfoChanged 진입점, L1497-1532 BtnTestFindDatum_Click — Plan 17-02 가 실패 모달 변환만 수행, 본 Plan 이 성공 경로 변경)
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs (L1297-1319 PublishPointerInfo + PointerInfoChanged 이벤트 — 호출 구조 확인 only, 본 Plan 미수정)
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs (Plan 17-02 의 OnParamEditorSelectionChanged 5-step 리셋 위치 — Step 3 자리에 DetectedOrigin* 0 라인 추가)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-CONTEXT.md (D-13, D-14, D-15)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-PATTERNS.md (MainView.xaml.cs §D-14/D-15 + Cross-file analog PublishPointerInfo)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-UI-SPEC.md (§ "btn_testFindDatum 배치 결정" — 기존 버튼 재사용 LOCKED, § "Hover 좌표 + 밝기 TextBlock" 포맷)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-02-SUMMARY.md (Plan 17-02 의 OnParamEditorSelectionChanged 5-step 리셋 — 본 Plan 가 Step 3 wiring 추가)
  </read_first>
  <action>
    **Step A — MainView.UpdatePointerLabel 확장** (L367-383, PATTERNS.md gap #4 — *기존 GetGrayval 파이프라인 재사용*):

    기존 메소드 시그니처 보존: `private void UpdatePointerLabel(double x, double y, double? grayValue)`.

    본문에 3 TextBlock 갱신 라인 추가 (기존 label_pos 갱신 라인 그대로 유지):

    ```csharp
    private void UpdatePointerLabel(double x, double y, double? grayValue) {
        if (label_pos != null) {
            label_pos.Content = string.Format("X:{0:0.0}, Y:{1:0.0}, G:{2}",
                x, y, grayValue.HasValue ? grayValue.Value.ToString("0.0") : "-");
        }
        //260430 hbk Phase 17 D-15 — 상단 툴바 hover 표시 (정수 + N/A)
        if (txt_hoverX != null) txt_hoverX.Text = grayValue.HasValue ? "X: " + x.ToString("0") : "X: N/A";
        if (txt_hoverY != null) txt_hoverY.Text = grayValue.HasValue ? "Y: " + y.ToString("0") : "Y: N/A";
        if (txt_hoverG != null) txt_hoverG.Text = "Gray: " + (grayValue.HasValue ? grayValue.Value.ToString("0") : "N/A");
    }
    ```

    > **N/A 처리 정책**: PublishPointerInfo (MainResultViewerControl L1297-1319) 가 CurrentImage == null 시 (0, 0, null) 발행. 본 Plan 은 grayValue.HasValue 가 false 일 때 X/Y 도 "N/A" 로 표시 (UI-SPEC § Empty State 일관). grayValue 가 있으면 좌표는 정수 표시.

    > **신규 GetGrayval 호출 0**: PublishPointerInfo 가 이미 GetGrayval 호출 → PointerInfoChanged → MainView.HalconViewer_PointerInfoChanged → UpdatePointerLabel 파이프라인 wired. 본 Plan 은 *데이터 표시 라인만* 추가, 신규 HALCON 호출 0.

    **Step B — BtnTestFindDatum_Click 성공 경로 변경** (L1497-1532).

    Plan 17-02 가 실패 모달 변환 (FormatFindError) 만 수행. 본 Plan 이 성공 경로 추가:

    ```csharp
    private void BtnTestFindDatum_Click(object sender, RoutedEventArgs e) {
        // ... 기존 datum/image 추출 + 가드 코드 ...

        //260430 hbk Phase 17 D-14 — TryFindDatum 호출 + transient write-back 자동 (Plan 17-03 Task 2)
        string error;
        bool ok = DatumFindingService.TryFindDatum(image, datum, out error);

        if (ok) {
            //260430 hbk Phase 17 D-14 — 성공: 시각화 자동 (RenderDatumOverlay → RenderDatumFindResult chain)
            halconViewer.SetDatumOverlay(datum, true);
            // 모달 없음 (D-12 — 성공 시 모달 X)
            // PropertyGrid 메트릭 갱신 (DetectedEdgeCount 등 ReadOnly 표시)
            try { datum.RaisePropertyChanged(string.Empty); } catch { }
            mParentWindow?.inspectionList?.RefreshParamEditor();
        } else {
            //260430 hbk Phase 17 D-12 + D-14 — 실패 모달 (Plan 17-02 의 FormatFindError 재사용)
            CustomMessageBox.Show("Find 실패", FormatFindError(error));
        }
    }
    ```

    > **메소드 시그니처 적응**: TryFindDatum 의 정확한 시그니처 확인 (out error 위치, image 인자명 등):
    > ```bash
    > grep -n "public.*TryFindDatum\\|bool TryFindDatum" WPF_Example/Halcon/Algorithms/DatumFindingService.cs
    > ```
    > 정확한 시그니처에 맞춰 호출.

    > **Plan 17-02 의 label_testFindResult 사용 라인 제거**: Plan 17-02 가 이미 CustomMessageBox 변환 완료. 본 Plan 은 성공 분기에서 label_testFindResult.Content 갱신 라인을 *제거* (모달 정책 — 성공 시 표시 없음).

    **Step C — InspectionListView.OnParamEditorSelectionChanged Step 3 wiring**:

    Plan 17-02 의 5-step 리셋에서 Step 3 자리 (LastTeachSucceeded/LastFindSucceeded reset 다음) 에 다음 라인 추가:

    ```csharp
    //260430 hbk Phase 17 D-13 — DetectedOrigin 시각화 clear (RenderDatumOverlay 가 LastTeachSucceeded=false 분기에서 자동 미렌더, 본 라인은 PropertyGrid 메트릭 0 표시 안전 처리)
    datum.DetectedOriginRow = 0;
    datum.DetectedOriginCol = 0;
    datum.DetectedRefAngle  = 0;
    datum.DetectedEdgeCount = 0; //260430 hbk Phase 17 D-16
    datum.DetectedFitRMSE   = 0; //260430 hbk Phase 17 D-16
    datum.DetectedAngleDeg  = 0; //260430 hbk Phase 17 D-16
    ```

    위치: Plan 17-02 의 5-step 리셋 블록 안 — `datum.LastFindSucceeded = false;` 다음, `mParentWindow?.mainView?.halconViewer?.SetDatumOverlay(datum, true);` 이전.

    **Step D — IsEditMode wiring 검증 (Plan 17-02 의존)**: Plan 17-02 가 _isEditMode + IsEditMode 프로퍼티 + MainView 의 토글 진입점을 wired. 본 Plan 은 추가 작업 없음.

    Brace style: K&R (양쪽 파일).
  </action>
  <verify>
    <automated>
      grep -c "txt_hoverX.Text\\|txt_hoverY.Text\\|txt_hoverG.Text" WPF_Example/UI/ContentItem/MainView.xaml.cs && \
      grep -c "TryFindDatum.*image.*datum\\|TryFindDatum(image, datum" WPF_Example/UI/ContentItem/MainView.xaml.cs && \
      grep -c "datum.DetectedOriginRow = 0" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs && \
      msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "txt_hoverX.Text" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 1.
    - `grep "txt_hoverY.Text" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 1.
    - `grep "txt_hoverG.Text" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 1.
    - `grep "TryFindDatum" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 1 (BtnTestFindDatum_Click 호출).
    - `grep "halconViewer.SetDatumOverlay(datum, true)" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 2 (Plan 17-02 baseline + 본 Plan 성공 분기 추가).
    - `grep "FormatFindError" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 2 (Plan 17-02 baseline 보존 + 본 Plan 실패 분기 호출).
    - `grep "label_testFindResult.Content" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns 0 (모달 정책으로 인한 사용 제거).
    - `grep "datum.DetectedOriginRow = 0" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` returns 1.
    - `grep "datum.DetectedOriginCol = 0" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` returns 1.
    - `grep "datum.DetectedRefAngle = 0" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` returns 1.
    - `grep -c "datum.DetectedEdgeCount = 0\\|datum.DetectedFitRMSE = 0\\|datum.DetectedAngleDeg = 0" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` returns ≥ 3 (메트릭 0 리셋).
    - `grep -c "//260430 hbk Phase 17 D-13" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` returns ≥ 1.
    - `grep -c "//260430 hbk Phase 17 D-14" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 2.
    - `grep -c "//260430 hbk Phase 17 D-15" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 1.
    - 회귀 (Plan 17-02): `grep "ParamEditor.SelectedObject = null" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` returns ≥ 2 (Phase 16 Datum 노드 + Phase 17 AlgorithmType 분기 보존).
    - 회귀 (Plan 17-02): `grep "ValidateRoiPresence\\|FormatTeachError" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 2 (Plan 17-02 helper 보존).
    - msbuild Debug/x64 exit code = 0, 신규 warning 0 on MainView.xaml.cs / InspectionListView.xaml.cs.
  </acceptance_criteria>
  <done>
    UpdatePointerLabel 가 기존 label_pos + 신규 3 TextBlock 양쪽 갱신, 신규 GetGrayval 호출 0. BtnTestFindDatum_Click 성공 분기에서 TryFindDatum 자동 호출 → write-back 자동 → SetDatumOverlay → RenderDatumFindResult 자동 시각화. 실패 분기는 Plan 17-02 의 FormatFindError 재사용. InspectionListView OnParamEditorSelectionChanged 5-step 의 Step 3 (DetectedOrigin/메트릭 0 리셋) wiring 완료.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → MouseMove (HalconViewer) | 로컬 사용자 마우스 좌표가 PublishPointerInfo → UpdatePointerLabel 파이프라인 통과. |
| DatumFindingService.TryFindDatum → DatumConfig (transient) | 알고리즘 결과가 transient 필드에 write-back. |
| HOperatorSet.GetGrayval | (기존 PublishPointerInfo 내부) 이미지 픽셀 값 조회 — 본 Plan 신규 호출 0. |

신규 trust boundary 없음. 네트워크/파일/저장소 미관여.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | DoS | UpdatePointerLabel hover 호출 빈도 | mitigate | 기존 PublishPointerInfo 파이프라인 그대로 — 신규 GetGrayval 호출 0. throttle 불필요 (PointerInfoChanged 이벤트 발행 빈도 = MouseMove 빈도 — 기존 동작 동일). |
| T-17-03-02 | Tampering | DatumConfig.DetectedOrigin* (volatile double) | accept | UI 시각화 입력 — 외부 입력 채널 0. INI 파일에 0 으로 직렬화되더라도 다음 TryFindDatum 호출 시 덮어쓰여짐 (stale 값 의미 0). |
| T-17-03-03 | Tampering | RenderDatumFindResult LastFindSucceeded gate | mitigate | gate 통과 후에만 DispLine/WriteString 호출. catch 블록이 표시 오류 흡수. |
| T-17-03-04 | Information Disclosure | "Find (row, col)" WriteString 좌표 텍스트 | accept | 이미지 좌표 = 사용자가 이미 보고 있는 캔버스의 픽셀 위치 — 신규 정보 노출 없음. |
| T-17-03-05 | Tampering | TryFindDatum write-back 라인 (D-13/D-16) | mitigate | write-back 라인은 *결과만* 기록 — 알고리즘 계산 로직 0 라인 변경. D-17 acceptance bound (≤ 11 라인 누적) 강제. VisionAlgorithmService 0 라인. |
| T-17-03-06 | DoS | RenderDatumFindResult 4 DispLine + 화살표 3 DispLine | accept | 1 회 RenderDatumOverlay 호출 당 최대 7 DispLine 추가 — UI thread 영향 미미. |

No high-severity threats. 알고리즘 보존 (D-17) 강제로 회귀 위험 통제.
</threat_model>

<verification>
Phase-level checks:

```bash
# 알고리즘 보존 (D-17 누적)
git diff WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs | wc -l  # = 0
# Plan 17-01 baseline 2 라인 + Plan 17-03 신규 ≤ 9 라인 = 누적 ≤ 11 라인
git diff WPF_Example/Halcon/Algorithms/DatumFindingService.cs | grep -E "^\\+[^+]" | grep -v "^\\+\\s*//" | grep -v "^\\+\\s*$" | wc -l  # ≤ 11

# Plan 17-03 영역 변경 카운트
grep -c "DetectedOriginRow\\|DetectedOriginCol\\|DetectedRefAngle\\|DetectedEdgeCount\\|DetectedFitRMSE\\|DetectedAngleDeg" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs  # ≥ 6
grep -c "config.DetectedOriginRow" WPF_Example/Halcon/Algorithms/DatumFindingService.cs                                                                                          # ≥ 1
grep -c "purple\\|RenderDatumFindResult(window, datum)" WPF_Example/Halcon/Display/HalconDisplayService.cs                                                                       # ≥ 3
grep -c "txt_hoverX\\|txt_hoverY\\|txt_hoverG" WPF_Example/UI/ContentItem/MainView.xaml                                                                                          # ≥ 3
grep -c "txt_hoverX.Text\\|txt_hoverY.Text\\|txt_hoverG.Text" WPF_Example/UI/ContentItem/MainView.xaml.cs                                                                        # ≥ 3

# hbk 주석 카운트
grep -c "//260430 hbk Phase 17 D-13" WPF_Example/Halcon/Display/HalconDisplayService.cs               # ≥ 4
grep -c "//260430 hbk Phase 17 D-13" WPF_Example/Halcon/Algorithms/DatumFindingService.cs             # ≥ 2
grep -c "//260430 hbk Phase 17 D-14" WPF_Example/UI/ContentItem/MainView.xaml.cs                      # ≥ 2
grep -c "//260430 hbk Phase 17 D-15" WPF_Example/UI/ContentItem/MainView.xaml.cs                      # ≥ 1

# 빌드
msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64

# Plan 17-01/17-02 회귀 (보존 대상)
grep -c "thetaRad = 0.0" WPF_Example/Halcon/Display/HalconDisplayService.cs                                # ≥ 1 (Plan 17-01 보존)
grep -c "Circle_RadialDirection" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs                     # ≥ 4 (Plan 17-01 보존)
grep -c "ICustomTypeDescriptor\\|IsHiddenForAlgorithm" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs # ≥ 4 (Plan 17-02 보존)
grep -c "MessageBoxButton.YesNoCancel" WPF_Example/UI/ContentItem/MainView.xaml.cs                         # ≥ 1 (Plan 17-02 보존)
```

회귀 검증 (수동, UAT 17-04 가 정식 검증):
- btn_testFindDatum 클릭 (성공 케이스) → 캔버스에 purple 십자 + "Find (row, col)" 텍스트 + 화살표 표시.
- 마우스를 캔버스 위로 이동 → 상단 툴바 X/Y/Gray 가 정수 + 픽셀 밝기 (0~255) 라이브 갱신.
- 이미지 바깥 hover → "X: N/A · Y: N/A · Gray: N/A".
- AlgorithmType 변경 → 검출 시각화 (purple 십자) 사라짐 (LastFindSucceeded=false 또는 DetectedOriginRow=0).
- PropertyGrid 의 "Datum|Result" 카테고리에 DetectedEdgeCount/FitRMSE/AngleDeg 표시 (ReadOnly).
</verification>

<success_criteria>
- 모든 must_haves.truths 가 grep + msbuild 로 검증 가능.
- DatumConfig.cs 에 6 신규 필드 (transient 3 + 메트릭 3).
- DatumFindingService.cs 누적 신규 코드 ≤ 11 라인 (주석/공백 제외) — Plan 17-01 baseline 2 + Plan 17-03 신규 ≤ 9.
- VisionAlgorithmService.cs diff = 0 라인.
- HalconDisplayService.RenderDatumFindResult 본문이 purple 패턴으로 교체.
- HalconDisplayService.RenderDatumOverlay 의 LastTeachSucceeded 분기 마지막에 RenderDatumFindResult 호출 추가 (z-stack last).
- MainView.xaml 에 X/Y/Gray 3 TextBlock + UpdatePointerLabel 양쪽 갱신.
- MainView.BtnTestFindDatum_Click 성공 경로 wired (모달 없음).
- InspectionListView 5-step 리셋의 Step 3 (DetectedOrigin/메트릭 0 리셋) 라인 추가.
- Plan 17-01/17-02 영역 회귀 0.
- msbuild Debug/x64 PASS, 신규 warning 0.
</success_criteria>

<output>
After completion, create `.planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-03-SUMMARY.md` with:
- 변경 파일 5개 + 라인 카운트
- D-17 누적 acceptance bound 실측치 (DatumFindingService 누적 ≤ 11, VisionAlgorithmService 0)
- ParamBase reflection 의 DetectedOrigin/메트릭 INI 직렬화 동작 검증 결과 (Phase 13-05 패턴 일치 여부)
- PropertyTools.Wpf ReadOnly attribute 호환성 검증 결과 (System.ComponentModel.ReadOnly vs PropertyTools.DataAnnotations.ReadOnly)
- BtnTestFindDatum_Click 성공/실패 분기 흐름 다이어그램
- Plan 17-01/17-02 영역 회귀 검증 결과
- 다음 단계: Plan 17-04 (UAT — Phase 16 carry-over 16항목 + Phase 17 D-01~D-16 통합 시나리오 ≥ 16 signed_off)
</output>
