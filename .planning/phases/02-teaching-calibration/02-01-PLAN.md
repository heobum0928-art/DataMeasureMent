---
phase: 02-teaching-calibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
  - WPF_Example/Sequence/Param/CameraSlaveParam.cs
  - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: true
requirements: [TCH-01, TCH-02]

must_haves:
  truths:
    - "FAI 선택 시 캔버스에 해당 FAI의 ROI가 노란색으로 하이라이트 표시된다"
    - "미선택 FAI의 ROI는 초록색으로 표시된다"
    - "ROI 중앙에 에지 검색 방향 화살표(흰색)가 표시된다"
    - "ROI가 설정되지 않은 FAI 선택 시 'ROI not set' 텍스트가 표시된다"
    - "PixelResolution 필드가 INI에 저장/로드된다"
    - "PolygonPoints 필드가 INI에 저장/로드된다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs"
      provides: "ToRoiDefinition() 변환 메서드, PolygonPoints string 필드, PixelResolutionX/Y 필드"
      contains: "public RoiDefinition ToRoiDefinition()"
    - path: "WPF_Example/Sequence/Param/CameraSlaveParam.cs"
      provides: "카메라별 PixelResolution 필드"
      contains: "public double PixelResolution"
    - path: "WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs"
      provides: "selectedRoiId 지원하는 UpdateDisplayState 오버로드"
      contains: "public void UpdateDisplayState"
    - path: "WPF_Example/Halcon/Display/HalconDisplayService.cs"
      provides: "에지 방향 화살표 렌더링"
      contains: "DrawDirectionArrow"
  key_links:
    - from: "FAIConfig.ToRoiDefinition()"
      to: "RoiDefinition"
      via: "Rectangle2 중심점+반축 → 직교 bounding box 변환"
      pattern: "new RoiDefinition"
    - from: "MainView.FAIResults_SelectionChanged"
      to: "MainResultViewerControl.UpdateDisplayState"
      via: "FAI 선택 이벤트 → ROI 리스트 + selectedRoiId 전달"
      pattern: "halconViewer\\.UpdateDisplayState"
    - from: "HalconDisplayService.Render"
      to: "DrawDirectionArrow"
      via: "ROI 렌더링 후 선택 ROI에 화살표 추가"
      pattern: "DrawDirectionArrow"
---

<objective>
FAIConfig에 ROI 변환 메서드와 새 필드를 추가하고, 캔버스 렌더링 파이프라인에 selectedRoiId 지원 + 에지 방향 화살표를 추가하여, FAI 선택 시 ROI 오버레이가 하이라이트되는 기반을 완성한다.

Purpose: Phase 2의 모든 ROI 시각화(Rect/Polygon 드로잉, 캘리브레이션)의 데이터 모델 + 렌더링 기반.
Output: FAIConfig.ToRoiDefinition() + 렌더링 파이프라인 + FAI 선택 → ROI 하이라이트 동작.
</objective>

<execution_context>
@C:\Users\tech\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\tech\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-teaching-calibration/02-CONTEXT.md
@.planning/phases/02-teaching-calibration/02-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From WPF_Example/Halcon/Models/RoiDefinition.cs:
```csharp
[DataContract]
public class RoiDefinition
{
    [DataMember] public string Id { get; set; }
    [DataMember] public string Name { get; set; }
    [DataMember] public double Row1 { get; set; }
    [DataMember] public double Column1 { get; set; }
    [DataMember] public double Row2 { get; set; }
    [DataMember] public double Column2 { get; set; }
    [DataMember] public bool IsTaught { get; set; }
    [DataMember] public double Sigma { get; set; } = 1.0;
    [DataMember] public int EdgeThreshold { get; set; } = 10;
    [DataMember] public string EdgeDirection { get; set; } = "LtoR";
    [DataMember] public double PixelResolutionX { get; set; } = 1.0;
    [DataMember] public double PixelResolutionY { get; set; } = 1.0;
}
```

From WPF_Example/Halcon/Display/HalconDisplayService.cs:
```csharp
public void Render(HWindow window, HImage image, IEnumerable<RoiDefinition> rois,
    string selectedRoiId, RoiDefinition draftRoi = null,
    IEnumerable<EdgeInspectionOverlay> inspectionOverlays = null,
    IEnumerable<string> displayMessages = null)
// ROI colors: roi.Id == selectedRoiId ? "yellow" : "green"
// Line width: selectedRoiId ? 3 : 2
```

From WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs:
```csharp
// Current: passes null for selectedRoiId
_displayService.Render(ViewerHost.HalconWindow, CurrentImage, _rois, null, null,
    _inspectionOverlays.Concat(BuildTransientOverlays()).ToList(),
    _displayMessages.Concat(BuildTransientMessages()).ToList());

public void UpdateDisplayState(IEnumerable<RoiDefinition> rois,
    IEnumerable<EdgeInspectionOverlay> overlays, IEnumerable<string> messages)
```

From WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs:
```csharp
public class FAIConfig : ParamBase {
    [Category("ROI")]
    public double ROI_Row { get; set; }
    public double ROI_Col { get; set; }
    public double ROI_Phi { get; set; }
    public double ROI_Length1 { get; set; }
    public double ROI_Length2 { get; set; }
    [Category("Edge|Measurement")]
    public EEdgeMeasureType MeasureType { get; set; }
    public double Threshold { get; set; } = 30.0;
    public double Sigma { get; set; } = 1.0;
    [Browsable(false)] public string FAIName { get; set; }
}
```

From WPF_Example/Sequence/Param/CameraSlaveParam.cs:
```csharp
public class CameraSlaveParam : ParamBase, ICameraParam {
    [Category("General|AOI")]
    public double PixelToUM_Offset { get; set; }
    // PixelResolution field will be added here
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data model extensions — FAIConfig fields + ToRoiDefinition + CameraSlaveParam.PixelResolution</name>
  <files>
    WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs,
    WPF_Example/Sequence/Param/CameraSlaveParam.cs
  </files>
  <read_first>
    WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs,
    WPF_Example/Sequence/Param/CameraSlaveParam.cs,
    WPF_Example/Sequence/Param/ParamBase.cs,
    WPF_Example/Halcon/Models/RoiDefinition.cs
  </read_first>
  <action>
1. **FAIConfig.cs** — Add three new fields and one method:

   a. Add `using ReringProject.Halcon.Models;` and `using System.ComponentModel;` imports at the top.

   b. Add these properties after the existing Tolerance category block (before the Result section):

   ```csharp
   // Calibration (per D-12, D-16: camera-level calibration stored in CameraSlaveParam,
   // but FAIConfig also carries PixelResolution for RoiDefinition compatibility)
   [Category("Calibration")]
   public double PixelResolutionX { get; set; } = 1.0;  // mm/pixel
   public double PixelResolutionY { get; set; } = 1.0;  // mm/pixel

   // Polygon ROI (per D-15: serialized as "x1,y1;x2,y2;x3,y3" string for INI storage)
   [Category("ROI")]
   public string PolygonPoints { get; set; } = "";
   ```

   c. Add `ToRoiDefinition()` method after `ClearResult()`:

   ```csharp
   /// <summary>Converts FAIConfig Rectangle2 params (center+half-lengths+phi) to RoiDefinition bounding box.</summary>
   public RoiDefinition ToRoiDefinition()
   {
       bool isTaught = ROI_Length1 > 0 && ROI_Length2 > 0;
       if (!isTaught)
       {
           return new RoiDefinition
           {
               Id = FAIName ?? "FAI",
               Name = FAIName ?? "FAI",
               IsTaught = false
           };
       }

       double sinPhi = Math.Sin(ROI_Phi);
       double cosPhi = Math.Cos(ROI_Phi);
       double dRow = Math.Abs(ROI_Length1 * cosPhi) + Math.Abs(ROI_Length2 * sinPhi);
       double dCol = Math.Abs(ROI_Length1 * sinPhi) + Math.Abs(ROI_Length2 * cosPhi);

       return new RoiDefinition
       {
           Id = FAIName ?? "FAI",
           Name = FAIName ?? "FAI",
           Row1 = ROI_Row - dRow,
           Column1 = ROI_Col - dCol,
           Row2 = ROI_Row + dRow,
           Column2 = ROI_Col + dCol,
           IsTaught = true,
           Sigma = Sigma,
           EdgeThreshold = (int)Threshold,
           EdgeDirection = "LtoR",
           PixelResolutionX = PixelResolutionX,
           PixelResolutionY = PixelResolutionY
       };
   }
   ```

2. **CameraSlaveParam.cs** — Add PixelResolution field per D-12/D-16:

   Add after the existing `PixelToUM_Offset` property in the `[Category("General|AOI")]` section:

   ```csharp
   [System.ComponentModel.Description("mm/pixel calibration factor for this camera")]
   public double PixelResolution { get; set; } = 1.0;  // mm/pixel (per D-12)
   ```

   This field is auto-serialized to INI by ParamBase.Save/Load (public property with get/set).
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && grep -n "ToRoiDefinition" WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs && grep -n "PolygonPoints" WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs && grep -n "PixelResolution" WPF_Example/Sequence/Param/CameraSlaveParam.cs</automated>
  </verify>
  <acceptance_criteria>
    - FAIConfig.cs contains `public RoiDefinition ToRoiDefinition()`
    - FAIConfig.cs contains `public string PolygonPoints { get; set; } = ""`
    - FAIConfig.cs contains `public double PixelResolutionX { get; set; } = 1.0`
    - FAIConfig.cs contains `public double PixelResolutionY { get; set; } = 1.0`
    - FAIConfig.cs contains `using ReringProject.Halcon.Models;`
    - CameraSlaveParam.cs contains `public double PixelResolution { get; set; } = 1.0`
    - ToRoiDefinition returns IsTaught=false when ROI_Length1 or ROI_Length2 is 0
    - ToRoiDefinition computes bounding box using sin/cos of ROI_Phi
  </acceptance_criteria>
  <done>FAIConfig has ToRoiDefinition(), PolygonPoints, PixelResolutionX/Y fields. CameraSlaveParam has PixelResolution field. All public properties auto-saved to INI via ParamBase.</done>
</task>

<task type="auto">
  <name>Task 2: ROI rendering pipeline — selectedRoiId support + direction arrow + FAI selection wiring</name>
  <files>
    WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs,
    WPF_Example/Halcon/Display/HalconDisplayService.cs,
    WPF_Example/UI/ContentItem/MainView.xaml.cs
  </files>
  <read_first>
    WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs,
    WPF_Example/Halcon/Display/HalconDisplayService.cs,
    WPF_Example/UI/ContentItem/MainView.xaml.cs,
    WPF_Example/UI/ContentItem/MainView.xaml,
    WPF_Example/UI/ViewModel/InspectionViewModel.cs,
    WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
  </read_first>
  <action>
1. **MainResultViewerControl.xaml.cs** — Add selectedRoiId support:

   a. Add a private field `private string _selectedRoiId;` alongside the existing `_rois` field.

   b. Add a new overload of `UpdateDisplayState`:

   ```csharp
   /// <summary>Updates display with ROI highlight support (per D-01, D-03).</summary>
   public void UpdateDisplayState(IEnumerable<RoiDefinition> rois, string selectedRoiId,
       IEnumerable<EdgeInspectionOverlay> overlays, IEnumerable<string> messages)
   {
       _selectedRoiId = selectedRoiId;
       UpdateDisplayState(rois, overlays, messages);
   }
   ```

   c. In the existing `Render()` private method, change the `_displayService.Render(...)` call to pass `_selectedRoiId` instead of `null` as the 4th argument:

   ```csharp
   _displayService.Render(
       ViewerHost.HalconWindow,
       CurrentImage,
       _rois,
       _selectedRoiId,  // was: null
       null,
       _inspectionOverlays.Concat(BuildTransientOverlays()).ToList(),
       _displayMessages.Concat(BuildTransientMessages()).ToList());
   ```

2. **HalconDisplayService.cs** — Add edge direction arrow rendering:

   a. After the ROI rectangle rendering loop (after `DrawRectangleOutline`), add arrow rendering for the selected ROI:

   Inside the `if (rois != null)` block, after drawing each ROI rectangle, add:

   ```csharp
   if (roi.Id == selectedRoiId && roi.IsTaught)
   {
       DrawDirectionArrow(window, roi);
   }
   ```

   b. Add the `DrawDirectionArrow` private method at the bottom of the class (per D-02, UI-SPEC arrow specs):

   ```csharp
   /// <summary>Draws edge search direction arrow at ROI center (per D-02). White, 2px line + arrowhead.</summary>
   private static void DrawDirectionArrow(HWindow window, RoiDefinition roi)
   {
       double centerRow = (roi.Row1 + roi.Row2) / 2.0;
       double centerCol = (roi.Column1 + roi.Column2) / 2.0;
       double arrowLength = 15.0;

       // Determine arrow direction from EdgeDirection
       double dRow = 0, dCol = 0;
       switch (roi.EdgeDirection)
       {
           case "LtoR": dCol = arrowLength; break;
           case "RtoL": dCol = -arrowLength; break;
           case "TtoB": dRow = arrowLength; break;
           case "BtoT": dRow = -arrowLength; break;
           default: dCol = arrowLength; break;
       }

       window.SetColor("white");
       window.SetLineWidth(2);
       // Main line
       window.DispLine(centerRow - dRow / 2, centerCol - dCol / 2,
                        centerRow + dRow / 2, centerCol + dCol / 2);
       // Arrowhead (two short lines at 30-degree angle)
       double headLen = 5.0;
       double endRow = centerRow + dRow / 2;
       double endCol = centerCol + dCol / 2;
       double angle = Math.Atan2(dRow, dCol);
       double a1 = angle + 2.5;  // ~143 degrees
       double a2 = angle - 2.5;  // ~-143 degrees
       window.DispLine(endRow, endCol,
                        endRow + headLen * Math.Sin(a1), endCol + headLen * Math.Cos(a1));
       window.DispLine(endRow, endCol,
                        endRow + headLen * Math.Sin(a2), endCol + headLen * Math.Cos(a2));
   }
   ```

   IMPORTANT: Add `using System;` at the top of HalconDisplayService.cs if not already present (needed for Math.Atan2/Sin/Cos).

3. **MainView.xaml.cs** — Wire FAI selection to ROI highlight:

   a. Replace the `FAIResults_SelectionChanged` stub (currently has only a comment) with:

   ```csharp
   private void FAIResults_SelectionChanged(object sender, SelectionChangedEventArgs e)
   {
       var selectedRow = dataGrid_faiResults.SelectedItem as FAIResultRow;
       if (selectedRow == null)
       {
           // No selection: show all ROIs without highlight
           var allRois = GetCurrentFAIRois();
           if (allRois.Count > 0)
               halconViewer.UpdateDisplayState(allRois, null, null, null);
           return;
       }

       // Show all ROIs, highlight selected FAI's ROI (per D-01, D-03)
       var rois = GetCurrentFAIRois();
       string selectedRoiId = selectedRow.FAIName;
       halconViewer.UpdateDisplayState(rois, selectedRoiId, null, null);

       // If selected FAI has no ROI taught, show hint text
       var selectedFai = selectedRow.SourceFAI;
       if (selectedFai != null && (selectedFai.ROI_Length1 <= 0 || selectedFai.ROI_Length2 <= 0))
       {
           label_message.Content = "ROI not set";
           label_message.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FFAAAAAA"));
           label_message.Visibility = Visibility.Visible;
       }
   }
   ```

   b. Add the `GetCurrentFAIRois()` helper method in MainView.xaml.cs:

   ```csharp
   /// <summary>Collects RoiDefinitions from all FAIs of the currently displayed shot.</summary>
   private List<RoiDefinition> GetCurrentFAIRois()
   {
       var result = new List<RoiDefinition>();
       // Iterate through all rows in the DataGrid (each row = one FAI)
       foreach (var item in dataGrid_faiResults.Items)
       {
           var row = item as FAIResultRow;
           if (row?.SourceFAI != null)
           {
               var roi = row.SourceFAI.ToRoiDefinition();
               if (roi.IsTaught)
                   result.Add(roi);
           }
       }
       return result;
   }
   ```

   c. Add `using ReringProject.Halcon.Models;` import if not already present.

   d. **FAIResultRow.SourceFAI**: Check if `FAIResultRow` has a `SourceFAI` property that returns the underlying `FAIConfig`. If it doesn't exist, add it:
      - Read `FAIResultRow.cs` (likely in `WPF_Example/UI/ViewModel/` or nearby).
      - Add `[Browsable(false)] public FAIConfig SourceFAI { get; }` property backed by the FAIConfig passed in constructor.
      - This is needed to access `ToRoiDefinition()` and ROI field values from the DataGrid row.
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && grep -n "UpdateDisplayState.*selectedRoiId" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs && grep -n "DrawDirectionArrow" WPF_Example/Halcon/Display/HalconDisplayService.cs && grep -n "GetCurrentFAIRois" WPF_Example/UI/ContentItem/MainView.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - MainResultViewerControl.xaml.cs contains `public void UpdateDisplayState(IEnumerable<RoiDefinition> rois, string selectedRoiId,`
    - MainResultViewerControl.xaml.cs Render method passes `_selectedRoiId` (not `null`) to _displayService.Render
    - HalconDisplayService.cs contains `private static void DrawDirectionArrow(HWindow window, RoiDefinition roi)`
    - HalconDisplayService.cs Render loop calls `DrawDirectionArrow` when `roi.Id == selectedRoiId`
    - MainView.xaml.cs FAIResults_SelectionChanged calls `halconViewer.UpdateDisplayState` with 4 args (rois, selectedRoiId, overlays, messages)
    - MainView.xaml.cs contains `private List<RoiDefinition> GetCurrentFAIRois()`
    - MainView.xaml.cs shows "ROI not set" label when selected FAI has ROI_Length1 <= 0 or ROI_Length2 <= 0
  </acceptance_criteria>
  <done>FAI 선택 시 캔버스에 모든 ROI가 초록색으로, 선택 ROI가 노란색으로 표시되며, 선택 ROI 중앙에 에지 방향 화살표가 표시된다. 미교습 FAI 선택 시 "ROI not set" 텍스트가 표시된다.</done>
</task>

</tasks>

<verification>
1. Build: MSBuild 컴파일 오류 없음
2. FAIConfig.ToRoiDefinition() 호출 시 RoiDefinition 반환 (Length1=0이면 IsTaught=false)
3. DataGrid에서 FAI 행 선택 시 halconViewer.UpdateDisplayState가 selectedRoiId와 함께 호출됨
4. HalconDisplayService.Render에서 선택 ROI에 흰색 화살표 렌더링됨
</verification>

<success_criteria>
- FAIConfig에 ToRoiDefinition(), PolygonPoints, PixelResolutionX/Y 필드가 존재하고 INI 직렬화 가능
- CameraSlaveParam에 PixelResolution 필드 존재
- DataGrid FAI 행 선택 → 캔버스 ROI 하이라이트(노란색) + 방향 화살표(흰색) 동작
- 미교습 FAI 선택 시 "ROI not set" 표시
</success_criteria>

<output>
After completion, create `.planning/phases/02-teaching-calibration/02-01-SUMMARY.md`
</output>
