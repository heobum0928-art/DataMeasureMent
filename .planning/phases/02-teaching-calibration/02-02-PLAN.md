---
phase: 02-teaching-calibration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
  - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
autonomous: false
requirements: [TCH-01, ALG-03]

must_haves:
  truths:
    - "캔버스 상단에 Rect ROI / Polygon ROI / Calibrate 버튼이 있는 툴바가 표시된다"
    - "Rect ROI 버튼 클릭 후 캔버스에서 드래그하면 사각형 ROI가 그려지고 FAIConfig에 저장된다"
    - "Polygon ROI 버튼 클릭 후 점을 클릭하면 다각형이 그려지고 우클릭으로 완성되어 FAIConfig.PolygonPoints에 저장된다"
    - "Calibrate 버튼 클릭 후 2점을 클릭하면 픽셀 거리가 계산되고, mm 입력 다이얼로그 후 mm/pixel이 CameraSlaveParam.PixelResolution에 저장된다"
    - "Escape 키로 모든 드로잉 모드를 취소할 수 있다"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml"
      provides: "Canvas toolbar strip with Rect ROI / Polygon ROI / Calibrate buttons"
      contains: "Rect ROI"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "ROI drawing modes, polygon point collection, calibration 2-point flow"
      contains: "StartRectDrawMode"
  key_links:
    - from: "Rect ROI button click"
      to: "HalconViewerControl.StartRectangleDrawing"
      via: "MainView code-behind toggle mode"
      pattern: "StartRectangleDrawing"
    - from: "Polygon right-click complete"
      to: "FAIConfig.PolygonPoints"
      via: "Point list serialized to semicolon-delimited string"
      pattern: "PolygonPoints.*="
    - from: "Calibration 2-point click"
      to: "CameraSlaveParam.PixelResolution"
      via: "pixel distance / mm input = mm/pixel"
      pattern: "PixelResolution.*="
---

<objective>
캔버스 상단에 ROI 편집 + 캘리브레이션 툴바를 추가하고, Rect ROI 드래그 설정, Polygon ROI 점 클릭 생성, 2점 캘리브레이션 플로우를 구현하여 사용자가 FAI ROI를 설정하고 mm/pixel 변환 계수를 적용할 수 있게 한다.

Purpose: 사용자가 FAI의 측정 영역을 캔버스에서 직접 설정하고, 픽셀-mm 변환을 교정할 수 있는 인터랙션 완성.
Output: 동작하는 툴바 UI + Rect ROI 드로잉 + Polygon ROI 드로잉 + 캘리브레이션 플로우.
</objective>

<execution_context>
@C:\Users\tech\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\tech\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-teaching-calibration/02-CONTEXT.md
@.planning/phases/02-teaching-calibration/02-UI-SPEC.md
@.planning/phases/02-teaching-calibration/02-01-SUMMARY.md

<interfaces>
<!-- Interfaces created by Plan 01 that this plan depends on -->

From WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs (after Plan 01):
```csharp
public class FAIConfig : ParamBase {
    // Existing ROI fields
    public double ROI_Row { get; set; }
    public double ROI_Col { get; set; }
    public double ROI_Phi { get; set; }
    public double ROI_Length1 { get; set; }
    public double ROI_Length2 { get; set; }
    // New in Plan 01
    public string PolygonPoints { get; set; } = "";
    public double PixelResolutionX { get; set; } = 1.0;
    public double PixelResolutionY { get; set; } = 1.0;
    public RoiDefinition ToRoiDefinition() { ... }
    [Browsable(false)] public string FAIName { get; set; }
}
```

From WPF_Example/Sequence/Param/CameraSlaveParam.cs (after Plan 01):
```csharp
public class CameraSlaveParam : ParamBase, ICameraParam {
    public double PixelResolution { get; set; } = 1.0;  // mm/pixel
}
```

From WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs (after Plan 01):
```csharp
public void UpdateDisplayState(IEnumerable<RoiDefinition> rois, string selectedRoiId,
    IEnumerable<EdgeInspectionOverlay> overlays, IEnumerable<string> messages)
```

From WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs:
```csharp
public void StartRectangleDrawing()  // enters drag-to-draw mode
public RoiDefinition CommitActiveRectangle()  // returns drawn rect, exits draw mode
```

From WPF_Example/UI/Dialog/TextInputBoxWindow.xaml.cs:
```csharp
// NOTE: class name has typo: TextInputBoxWinidow (not Window)
public partial class TextInputBoxWinidow : Window {
    public string Text { get; private set; }
    public TextInputBoxWinidow(string message, string initialText) { ... }
    // Usage: var dlg = new TextInputBoxWinidow("title", ""); if (dlg.ShowDialog() == true) { var val = dlg.Text; }
}
```

From WPF_Example/UI/ContentItem/MainView.xaml (current layout):
```xml
<Grid>
  <Grid.RowDefinitions>
    <RowDefinition Height="6*"/>   <!-- Canvas area -->
    <RowDefinition Height="5"/>    <!-- Splitter -->
    <RowDefinition Height="4*"/>   <!-- DataGrid -->
  </Grid.RowDefinitions>
  <Grid Grid.Row="0" Background="#FF303030">
    <dmv:MainResultViewerControl x:Name="halconViewer"/>
    <Label x:Name="label_message" .../>
    <Border VerticalAlignment="Bottom" Background="#111827" Padding="12,6">
      <!-- Position bar -->
    </Border>
  </Grid>
</Grid>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canvas toolbar XAML + Rect ROI drawing mode</name>
  <files>
    WPF_Example/UI/ContentItem/MainView.xaml,
    WPF_Example/UI/ContentItem/MainView.xaml.cs
  </files>
  <read_first>
    WPF_Example/UI/ContentItem/MainView.xaml,
    WPF_Example/UI/ContentItem/MainView.xaml.cs,
    WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs,
    WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
  </read_first>
  <action>
1. **MainView.xaml** — Add toolbar strip inside the canvas Grid.Row="0" area:

   Inside `<Grid Grid.Row="0" Background="#FF303030">`, add a new Border BEFORE the existing `<Border VerticalAlignment="Bottom" ...>` (the position bar). The new toolbar goes at the TOP:

   ```xml
   <!-- Phase 2 Canvas Toolbar (per D-09, D-10, D-11, UI-SPEC toolbar) -->
   <Border x:Name="canvasToolbar" VerticalAlignment="Top" Background="#111827"
           Padding="8,4" Height="36" Panel.ZIndex="10">
       <StackPanel Orientation="Horizontal">
           <ToggleButton x:Name="btn_rectRoi" Content="Rect ROI"
               Click="RectRoiButton_Click"
               MinWidth="80" Height="28" Padding="12,4"
               FontSize="13" FontWeight="SemiBold" Margin="0,0,4,0">
               <ToggleButton.Style>
                   <Style TargetType="ToggleButton">
                       <Setter Property="Background" Value="Transparent"/>
                       <Setter Property="Foreground" Value="#FFAAAAAA"/>
                       <Setter Property="BorderBrush" Value="#FF555555"/>
                       <Setter Property="BorderThickness" Value="1"/>
                       <Setter Property="Template">
                           <Setter.Value>
                               <ControlTemplate TargetType="ToggleButton">
                                   <Border x:Name="border" Background="{TemplateBinding Background}"
                                           BorderBrush="{TemplateBinding BorderBrush}"
                                           BorderThickness="{TemplateBinding BorderThickness}"
                                           CornerRadius="4" Padding="{TemplateBinding Padding}">
                                       <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                   </Border>
                                   <ControlTemplate.Triggers>
                                       <Trigger Property="IsChecked" Value="True">
                                           <Setter TargetName="border" Property="Background" Value="#2563EB"/>
                                           <Setter Property="Foreground" Value="White"/>
                                           <Setter TargetName="border" Property="BorderBrush" Value="#1D4ED8"/>
                                       </Trigger>
                                       <Trigger Property="IsMouseOver" Value="True">
                                           <Setter TargetName="border" Property="Background" Value="#3A3A4A"/>
                                           <Setter Property="Foreground" Value="White"/>
                                       </Trigger>
                                       <MultiTrigger>
                                           <MultiTrigger.Conditions>
                                               <Condition Property="IsMouseOver" Value="True"/>
                                               <Condition Property="IsChecked" Value="True"/>
                                           </MultiTrigger.Conditions>
                                           <Setter TargetName="border" Property="Background" Value="#2563EB"/>
                                       </MultiTrigger>
                                       <Trigger Property="IsEnabled" Value="False">
                                           <Setter TargetName="border" Property="Background" Value="Transparent"/>
                                           <Setter Property="Foreground" Value="#FF666666"/>
                                           <Setter TargetName="border" Property="BorderBrush" Value="#FF444444"/>
                                       </Trigger>
                                   </ControlTemplate.Triggers>
                               </ControlTemplate>
                           </Setter.Value>
                       </Setter>
                   </Style>
               </ToggleButton.Style>
           </ToggleButton>

           <ToggleButton x:Name="btn_polygonRoi" Content="Polygon ROI"
               Click="PolygonRoiButton_Click"
               MinWidth="80" Height="28" Padding="12,4"
               FontSize="13" FontWeight="SemiBold" Margin="0,0,4,0">
               <!-- Same Style as btn_rectRoi — copy the entire Style block above -->
               <!-- OR define the style as a resource and share it -->
           </ToggleButton>

           <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}"
                      Margin="4,4"/>

           <Button x:Name="btn_calibrate" Content="Calibrate"
               Click="CalibrateButton_Click"
               MinWidth="80" Height="28" Padding="12,4"
               FontSize="13" FontWeight="SemiBold"
               Background="#2563EB" Foreground="White"
               BorderBrush="#1D4ED8" BorderThickness="1">
               <Button.Template>
                   <ControlTemplate TargetType="Button">
                       <Border Background="{TemplateBinding Background}"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="4" Padding="{TemplateBinding Padding}">
                           <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                       </Border>
                   </ControlTemplate>
               </Button.Template>
           </Button>
       </StackPanel>
   </Border>

   <!-- Phase 2: Drawing mode hint text -->
   <Label x:Name="label_drawHint" Visibility="Collapsed"
          VerticalAlignment="Top" HorizontalAlignment="Center"
          Margin="0,40,0,0" FontSize="13"
          Foreground="#FFAAAAAA" IsHitTestVisible="False" Panel.ZIndex="5"/>

   <!-- Phase 2: Polygon point count badge -->
   <Label x:Name="label_pointCount" Visibility="Collapsed"
          VerticalAlignment="Top" HorizontalAlignment="Right"
          Margin="0,40,8,0" FontSize="13"
          Foreground="#FFAAAAAA" IsHitTestVisible="False" Panel.ZIndex="5"/>
   ```

   IMPORTANT: To avoid duplicating the ToggleButton style, define it as a UserControl.Resources Style:
   - Add `<UserControl.Resources>` block before `<Grid>` with a keyed style `x:Key="RoiToggleButtonStyle"`.
   - Reference it on both ToggleButtons with `Style="{StaticResource RoiToggleButtonStyle}"`.

2. **MainView.xaml.cs** — Add Rect ROI drawing mode:

   a. Add private fields for mode tracking:

   ```csharp
   // Phase 2: Drawing mode state
   private enum ECanvasMode { None, RectRoi, PolygonRoi, Calibration }
   private ECanvasMode _canvasMode = ECanvasMode.None;
   private FAIConfig _editingFai;  // FAI currently being edited
   ```

   b. Add `RectRoiButton_Click` handler:

   ```csharp
   private void RectRoiButton_Click(object sender, RoutedEventArgs e)
   {
       if (btn_rectRoi.IsChecked == true)
       {
           // Enter rect draw mode
           ExitCanvasMode();  // cancel any prior mode
           _canvasMode = ECanvasMode.RectRoi;
           btn_rectRoi.IsChecked = true;

           // Find selected FAI from DataGrid
           var selectedRow = dataGrid_faiResults.SelectedItem as FAIResultRow;
           if (selectedRow?.SourceFAI == null)
           {
               CustomMessageBox.Show("FAI를 먼저 선택하세요.", "Rect ROI");
               ExitCanvasMode();
               return;
           }
           _editingFai = selectedRow.SourceFAI;

           label_drawHint.Content = "드래그하여 ROI를 설정하세요";
           label_drawHint.Visibility = Visibility.Visible;
           halconViewer.StartRectangleDrawing();
       }
       else
       {
           // Commit the rectangle if drawn
           CommitRectRoi();
       }
   }

   private void CommitRectRoi()
   {
       if (_canvasMode != ECanvasMode.RectRoi || _editingFai == null)
       {
           ExitCanvasMode();
           return;
       }

       var roi = halconViewer.CommitActiveRectangle();
       if (roi != null)
       {
           // Convert RoiDefinition bounding box back to Rectangle2 center+half-lengths
           _editingFai.ROI_Row = (roi.Row1 + roi.Row2) / 2.0;
           _editingFai.ROI_Col = (roi.Column1 + roi.Column2) / 2.0;
           _editingFai.ROI_Phi = 0.0;
           _editingFai.ROI_Length1 = (roi.Row2 - roi.Row1) / 2.0;
           _editingFai.ROI_Length2 = (roi.Column2 - roi.Column1) / 2.0;

           // Refresh canvas to show new ROI
           var rois = GetCurrentFAIRois();
           halconViewer.UpdateDisplayState(rois, _editingFai.FAIName, null, null);
       }
       ExitCanvasMode();
   }
   ```

   c. Add `ExitCanvasMode` helper:

   ```csharp
   private void ExitCanvasMode()
   {
       _canvasMode = ECanvasMode.None;
       _editingFai = null;
       btn_rectRoi.IsChecked = false;
       btn_polygonRoi.IsChecked = false;
       label_drawHint.Visibility = Visibility.Collapsed;
       label_pointCount.Visibility = Visibility.Collapsed;
       btn_calibrate.Content = "Calibrate";
       _calibrationPoints.Clear();
   }
   ```

   d. Add Escape key handler — in `MainView_Loaded`, subscribe to `PreviewKeyDown`:

   ```csharp
   // In MainView_Loaded, add:
   PreviewKeyDown += MainView_PreviewKeyDown;
   ```

   ```csharp
   private void MainView_PreviewKeyDown(object sender, System.Windows.Input.KeyEventArgs e)
   {
       if (e.Key == System.Windows.Input.Key.Escape && _canvasMode != ECanvasMode.None)
       {
           ExitCanvasMode();
           e.Handled = true;
       }
   }
   ```

   e. Add placeholder handlers for Polygon and Calibrate buttons (to be filled in Tasks 2 and 3):

   ```csharp
   private void PolygonRoiButton_Click(object sender, RoutedEventArgs e) { /* Task 2 */ }
   private void CalibrateButton_Click(object sender, RoutedEventArgs e) { /* Task 3 */ }
   private readonly List<System.Windows.Point> _polygonPoints = new List<System.Windows.Point>();
   private readonly List<System.Windows.Point> _calibrationPoints = new List<System.Windows.Point>();
   ```
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && grep -n "btn_rectRoi\|btn_polygonRoi\|btn_calibrate\|canvasToolbar" WPF_Example/UI/ContentItem/MainView.xaml && grep -n "RectRoiButton_Click\|CommitRectRoi\|ExitCanvasMode\|ECanvasMode" WPF_Example/UI/ContentItem/MainView.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - MainView.xaml contains `x:Name="canvasToolbar"` with `VerticalAlignment="Top"`
    - MainView.xaml contains `x:Name="btn_rectRoi"` ToggleButton with Content="Rect ROI"
    - MainView.xaml contains `x:Name="btn_polygonRoi"` ToggleButton with Content="Polygon ROI"
    - MainView.xaml contains `x:Name="btn_calibrate"` Button with Content="Calibrate"
    - MainView.xaml contains `x:Name="label_drawHint"` Label
    - MainView.xaml contains `x:Name="label_pointCount"` Label
    - MainView.xaml.cs contains `private enum ECanvasMode { None, RectRoi, PolygonRoi, Calibration }`
    - MainView.xaml.cs contains `private void RectRoiButton_Click(`
    - MainView.xaml.cs contains `private void CommitRectRoi()`
    - MainView.xaml.cs CommitRectRoi sets `_editingFai.ROI_Row`, `ROI_Col`, `ROI_Phi`, `ROI_Length1`, `ROI_Length2`
    - MainView.xaml.cs contains `private void ExitCanvasMode()` that resets all mode state
    - MainView.xaml.cs Escape key handler calls `ExitCanvasMode()`
  </acceptance_criteria>
  <done>캔버스 상단에 3개 버튼 툴바가 표시되고, Rect ROI 버튼으로 드래그 ROI 설정이 동작하며, Escape로 취소 가능.</done>
</task>

<task type="auto">
  <name>Task 2: Polygon ROI point-by-point drawing mode</name>
  <files>
    WPF_Example/UI/ContentItem/MainView.xaml.cs,
    WPF_Example/Halcon/Display/HalconDisplayService.cs,
    WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
  </files>
  <read_first>
    WPF_Example/UI/ContentItem/MainView.xaml.cs,
    WPF_Example/UI/ContentItem/MainView.xaml,
    WPF_Example/Halcon/Display/HalconDisplayService.cs,
    WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs,
    WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
  </read_first>
  <action>
1. **MainView.xaml.cs** — Implement Polygon ROI drawing:

   a. Fill in the `PolygonRoiButton_Click` handler:

   ```csharp
   private void PolygonRoiButton_Click(object sender, RoutedEventArgs e)
   {
       if (btn_polygonRoi.IsChecked == true)
       {
           ExitCanvasMode();
           _canvasMode = ECanvasMode.PolygonRoi;
           btn_polygonRoi.IsChecked = true;

           var selectedRow = dataGrid_faiResults.SelectedItem as FAIResultRow;
           if (selectedRow?.SourceFAI == null)
           {
               CustomMessageBox.Show("FAI를 먼저 선택하세요.", "Polygon ROI");
               ExitCanvasMode();
               return;
           }
           _editingFai = selectedRow.SourceFAI;
           _polygonPoints.Clear();

           label_drawHint.Content = "점을 클릭, 우클릭으로 완성 (최소 3점)";
           label_drawHint.Visibility = Visibility.Visible;
           label_pointCount.Content = "0 / 20 pts";
           label_pointCount.Visibility = Visibility.Visible;

           // Subscribe to mouse events on the halconViewer for point placement
           halconViewer.HalconMouseDown += HalconViewer_PolygonMouseDown;
       }
       else
       {
           ExitCanvasMode();
       }
   }
   ```

   b. Add mouse handler for polygon point placement. NOTE: Check how `halconViewer` (MainResultViewerControl) exposes mouse events. It likely has `HalconMouseDown` or wraps the HWindowControl mouse events. If it exposes `PointerInfoChanged`, the executor should use the `MouseDown` event on the HWindowControl child. The executor must read `MainResultViewerControl.xaml.cs` to find the correct event.

   Alternative approach if MainResultViewerControl doesn't expose HalconMouseDown: Use the `ViewerHost` (HWindowControlWPF) mouse events, or add a transparent overlay Canvas inside MainView.xaml's Grid.Row="0" with `Panel.ZIndex="3"` that captures clicks during polygon mode. This overlay Canvas approach is simpler:

   ```csharp
   // If MainResultViewerControl does not expose mouse click events for world coordinates,
   // use the halconViewer's PointerInfoChanged event which already fires on mouse move.
   // For click detection, subscribe to halconViewer.MouseLeftButtonDown / MouseRightButtonDown.
   //
   // The clicks need image-space (row, col) coordinates.
   // Use halconViewer to convert screen coords to image coords if possible.
   // Otherwise, use the position label values (X, Y from PointerInfoChanged).

   private void HalconViewer_PolygonMouseDown(object sender, System.Windows.Input.MouseButtonEventArgs e)
   {
       if (_canvasMode != ECanvasMode.PolygonRoi) return;

       if (e.RightButton == System.Windows.Input.MouseButtonState.Pressed)
       {
           // Right-click: complete polygon (per D-07)
           if (_polygonPoints.Count >= 3)
           {
               CompletePolygon();
           }
           e.Handled = true;
           return;
       }

       if (e.LeftButton == System.Windows.Input.MouseButtonState.Pressed)
       {
           if (_polygonPoints.Count >= 20)
           {
               label_pointCount.Content = "20 / 20 pts MAX";
               return;
           }

           // Get image coordinates from the viewer
           // The executor should determine how to get (row, col) from the click position.
           // Option A: Use halconViewer's last known pointer position from PointerInfoChanged
           // Option B: Convert mouse position via HWindowControl coordinate transform
           var pos = GetImageCoordinatesFromMouse(e);
           if (pos.HasValue)
           {
               _polygonPoints.Add(pos.Value);
               label_pointCount.Content = $"{_polygonPoints.Count} / 20 pts";

               // Render polygon points on canvas (red dots)
               RenderPolygonDraft();
           }
           e.Handled = true;
       }
   }
   ```

   c. Add polygon completion:

   ```csharp
   private void CompletePolygon()
   {
       if (_editingFai == null || _polygonPoints.Count < 3) return;

       // Serialize to "x1,y1;x2,y2;..." format (per D-15)
       var sb = new System.Text.StringBuilder();
       for (int i = 0; i < _polygonPoints.Count; i++)
       {
           if (i > 0) sb.Append(";");
           sb.AppendFormat("{0:F1},{1:F1}", _polygonPoints[i].X, _polygonPoints[i].Y);
       }
       _editingFai.PolygonPoints = sb.ToString();

       // Refresh canvas to show committed polygon in blue
       var rois = GetCurrentFAIRois();
       halconViewer.UpdateDisplayState(rois, _editingFai.FAIName, null, null);

       ExitCanvasMode();
   }
   ```

   d. Add polygon draft rendering helper:

   ```csharp
   private void RenderPolygonDraft()
   {
       // Re-render current ROIs + draw polygon points as transient overlay
       var rois = GetCurrentFAIRois();
       halconViewer.UpdateDisplayState(rois, _editingFai?.FAIName, null, null);
       // The polygon points will be rendered by HalconDisplayService extension (see below)
   }
   ```

   e. Update `ExitCanvasMode()` to unsubscribe polygon mouse handler:

   ```csharp
   // In ExitCanvasMode(), add:
   halconViewer.HalconMouseDown -= HalconViewer_PolygonMouseDown;
   // (or whatever event was subscribed — use the same event name)
   ```

   f. Add helper to get image coordinates from mouse click. The executor should check if `MainResultViewerControl` has a method to convert screen-to-image coordinates. If not, track the last known position from `PointerInfoChanged`:

   ```csharp
   private double _lastPointerRow, _lastPointerCol;

   // In the existing HalconViewer_PointerInfoChanged handler, store:
   // _lastPointerRow = info.Y; _lastPointerCol = info.X;

   private System.Windows.Point? GetImageCoordinatesFromMouse(System.Windows.Input.MouseButtonEventArgs e)
   {
       // Use last known pointer position (updated by PointerInfoChanged on mouse move)
       return new System.Windows.Point(_lastPointerCol, _lastPointerRow);
   }
   ```

2. **HalconDisplayService.cs** — Add polygon rendering:

   In the `Render` method, after the ROI rectangle loop and before displaying messages, add polygon rendering:

   ```csharp
   // Render polygon outlines for ROIs that have PolygonPoints
   // This requires passing polygon data through the rendering pipeline.
   ```

   Actually, polygon rendering is better handled as a separate overlay. Since the polygon data lives in FAIConfig.PolygonPoints (a string), and the Render method takes RoiDefinition objects (which don't have polygon data), the simplest approach is:

   Add a new method to HalconDisplayService:

   ```csharp
   /// <summary>Renders a polygon outline on the HWindow. Points as (col, row) pairs.</summary>
   public void RenderPolygon(HWindow window, IList<System.Windows.Point> points, string color, int lineWidth)
   {
       if (window == null || points == null || points.Count < 3) return;
       window.SetColor(color);
       window.SetLineWidth(lineWidth);
       for (int i = 0; i < points.Count; i++)
       {
           int next = (i + 1) % points.Count;
           window.DispLine(points[i].Y, points[i].X, points[next].Y, points[next].X);
       }
   }

   /// <summary>Renders polygon draft points (red dots during drawing).</summary>
   public void RenderPolygonPoints(HWindow window, IList<System.Windows.Point> points, string color)
   {
       if (window == null || points == null) return;
       window.SetColor(color);
       foreach (var pt in points)
       {
           // Small filled circle at each point (radius 4px approximation)
           window.DispLine(pt.Y - 4, pt.X, pt.Y + 4, pt.X);
           window.DispLine(pt.Y, pt.X - 4, pt.Y, pt.X + 4);
       }
   }
   ```

3. **MainResultViewerControl.xaml.cs** — Add polygon rendering support:

   Add an overload or extend `UpdateDisplayState` to accept polygon data, OR add a separate method:

   ```csharp
   private IList<System.Windows.Point> _polygonDraftPoints;
   private string _polygonColor = "blue";

   public void SetPolygonDraft(IList<System.Windows.Point> points, string color)
   {
       _polygonDraftPoints = points;
       _polygonColor = color;
       Render();
   }

   public void ClearPolygonDraft()
   {
       _polygonDraftPoints = null;
       Render();
   }
   ```

   In the `Render()` method, after the `_displayService.Render(...)` call, add:

   ```csharp
   if (_polygonDraftPoints != null && _polygonDraftPoints.Count > 0)
   {
       if (_polygonDraftPoints.Count >= 3)
           _displayService.RenderPolygon(ViewerHost.HalconWindow, _polygonDraftPoints, _polygonColor, 2);
       _displayService.RenderPolygonPoints(ViewerHost.HalconWindow, _polygonDraftPoints, "red");
   }
   ```
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && grep -n "PolygonRoiButton_Click\|CompletePolygon\|_polygonPoints" WPF_Example/UI/ContentItem/MainView.xaml.cs && grep -n "RenderPolygon" WPF_Example/Halcon/Display/HalconDisplayService.cs && grep -n "SetPolygonDraft\|_polygonDraftPoints" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - MainView.xaml.cs PolygonRoiButton_Click enters PolygonRoi mode and subscribes to mouse events
    - MainView.xaml.cs CompletePolygon serializes points to "x1,y1;x2,y2;..." format and writes to _editingFai.PolygonPoints
    - MainView.xaml.cs enforces 3-point minimum (CompletePolygon checks `_polygonPoints.Count >= 3`)
    - MainView.xaml.cs enforces 20-point maximum (check in mouse handler)
    - MainView.xaml.cs label_pointCount shows "N / 20 pts" format
    - HalconDisplayService.cs contains `public void RenderPolygon(HWindow window, IList<System.Windows.Point> points, string color, int lineWidth)`
    - HalconDisplayService.cs contains `public void RenderPolygonPoints(`
    - MainResultViewerControl.xaml.cs contains `public void SetPolygonDraft(` 
    - MainResultViewerControl.xaml.cs Render() calls RenderPolygon for draft points
  </acceptance_criteria>
  <done>Polygon ROI 버튼 클릭 → 점 클릭으로 다각형 그리기 → 우클릭 완성 → FAIConfig.PolygonPoints에 직렬화 저장. 3~20점 제한 동작. Escape 취소 동작.</done>
</task>

<task type="auto">
  <name>Task 3: 2-point calibration flow (ALG-03)</name>
  <files>
    WPF_Example/UI/ContentItem/MainView.xaml.cs
  </files>
  <read_first>
    WPF_Example/UI/ContentItem/MainView.xaml.cs,
    WPF_Example/UI/ContentItem/MainView.xaml,
    WPF_Example/UI/Dialog/TextInputBoxWindow.xaml.cs,
    WPF_Example/Sequence/Param/CameraSlaveParam.cs,
    WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  </read_first>
  <action>
1. **MainView.xaml.cs** — Implement CalibrateButton_Click and 2-point measurement flow:

   a. Fill in the `CalibrateButton_Click` handler:

   ```csharp
   private void CalibrateButton_Click(object sender, RoutedEventArgs e)
   {
       ExitCanvasMode();
       _canvasMode = ECanvasMode.Calibration;
       _calibrationPoints.Clear();

       btn_calibrate.Content = "Pick Point 1";
       label_drawHint.Content = "캔버스에서 첫 번째 점을 클릭하세요";
       label_drawHint.Visibility = Visibility.Visible;

       // Subscribe to mouse click for point picking
       halconViewer.MouseLeftButtonDown += HalconViewer_CalibrationMouseDown;
   }
   ```

   b. Add calibration mouse handler:

   ```csharp
   private void HalconViewer_CalibrationMouseDown(object sender, System.Windows.Input.MouseButtonEventArgs e)
   {
       if (_canvasMode != ECanvasMode.Calibration) return;

       var pos = GetImageCoordinatesFromMouse(e);
       if (!pos.HasValue) return;

       _calibrationPoints.Add(pos.Value);
       e.Handled = true;

       if (_calibrationPoints.Count == 1)
       {
           btn_calibrate.Content = "Pick Point 2";
           label_drawHint.Content = "캔버스에서 두 번째 점을 클릭하세요";
           // Render point 1 marker (white circle)
           // Use a transient overlay or re-render
       }
       else if (_calibrationPoints.Count == 2)
       {
           halconViewer.MouseLeftButtonDown -= HalconViewer_CalibrationMouseDown;
           FinishCalibration();
       }
   }
   ```

   c. Add `FinishCalibration` method:

   ```csharp
   private void FinishCalibration()
   {
       var p1 = _calibrationPoints[0];
       var p2 = _calibrationPoints[1];

       // Calculate pixel distance
       double dx = p2.X - p1.X;
       double dy = p2.Y - p1.Y;
       double pixelDistance = Math.Sqrt(dx * dx + dy * dy);

       if (pixelDistance < 1.0)
       {
           CustomMessageBox.Show("두 점 사이의 거리가 너무 가깝습니다.", "캘리브레이션");
           ExitCanvasMode();
           return;
       }

       // Show TextInputBoxWinidow for mm input (per D-13, UI-SPEC calibration flow)
       // NOTE: class name typo in original code: TextInputBoxWinidow (not Window)
       var dlg = new TextInputBoxWinidow(
           string.Format("두 점 사이의 실제 거리(mm)를 입력하세요:\n(픽셀 거리: {0:F1} px)", pixelDistance),
           "");
       dlg.Title = "실제 거리 입력";
       dlg.Owner = Window.GetWindow(this);

       if (dlg.ShowDialog() == true)
       {
           double realMm;
           if (double.TryParse(dlg.Text, out realMm) && realMm > 0)
           {
               double mmPerPixel = realMm / pixelDistance;

               // Apply to CameraSlaveParam.PixelResolution (per D-12, D-16)
               ApplyCalibrationResult(mmPerPixel);

               // Show confirmation (per UI-SPEC: "1 px = X.XXX mm 적용됨" for 3 seconds)
               label_message.Content = string.Format("1 px = {0:F4} mm 적용됨", mmPerPixel);
               label_message.Foreground = new SolidColorBrush(Colors.White);
               label_message.Visibility = Visibility.Visible;

               // Auto-hide after 3 seconds
               var timer = new System.Windows.Threading.DispatcherTimer();
               timer.Interval = TimeSpan.FromSeconds(3);
               timer.Tick += (s, args) =>
               {
                   timer.Stop();
                   label_message.Visibility = Visibility.Collapsed;
               };
               timer.Start();
           }
           else
           {
               CustomMessageBox.Show("유효한 숫자를 입력하세요.", "캘리브레이션");
           }
       }

       ExitCanvasMode();
   }
   ```

   d. Add `ApplyCalibrationResult` helper:

   ```csharp
   /// <summary>Applies mm/pixel calibration to the current camera's CameraSlaveParam and all FAIs (per D-12).</summary>
   private void ApplyCalibrationResult(double mmPerPixel)
   {
       // Find the current CameraSlaveParam from the selected shot/action
       var selectedRow = dataGrid_faiResults.SelectedItem as FAIResultRow;
       if (selectedRow?.SourceFAI != null)
       {
           // Apply to FAIConfig level
           var shot = selectedRow.SourceFAI.Owner as ShotConfig;
           if (shot != null)
           {
               // CameraSlaveParam is the shot itself (ShotConfig extends CameraSlaveParam)
               shot.PixelResolution = mmPerPixel;

               // Also update all FAIs under this shot for RoiDefinition compatibility
               foreach (FAIConfig fai in shot.FAIList)
               {
                   fai.PixelResolutionX = mmPerPixel;
                   fai.PixelResolutionY = mmPerPixel;
               }
           }
       }
   }
   ```

   e. Update `ExitCanvasMode` to also unsubscribe calibration handler and reset button label:

   ```csharp
   // In ExitCanvasMode(), ensure:
   halconViewer.MouseLeftButtonDown -= HalconViewer_CalibrationMouseDown;
   btn_calibrate.Content = "Calibrate";
   _calibrationPoints.Clear();
   ```
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && grep -n "CalibrateButton_Click\|FinishCalibration\|ApplyCalibrationResult\|TextInputBoxWinidow" WPF_Example/UI/ContentItem/MainView.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - MainView.xaml.cs CalibrateButton_Click sets _canvasMode to Calibration and subscribes to MouseLeftButtonDown
    - MainView.xaml.cs btn_calibrate.Content changes to "Pick Point 1" then "Pick Point 2"
    - MainView.xaml.cs FinishCalibration calculates pixel distance via Math.Sqrt
    - MainView.xaml.cs opens `new TextInputBoxWinidow(` with title "실제 거리 입력"
    - MainView.xaml.cs computes `mmPerPixel = realMm / pixelDistance`
    - MainView.xaml.cs ApplyCalibrationResult sets `shot.PixelResolution = mmPerPixel`
    - MainView.xaml.cs ApplyCalibrationResult sets all FAIs' `PixelResolutionX` and `PixelResolutionY`
    - MainView.xaml.cs shows "1 px = X.XXX mm 적용됨" label for 3 seconds
    - MainView.xaml.cs validates input is positive double, shows error "유효한 숫자를 입력하세요" on failure
    - MainView.xaml.cs ExitCanvasMode resets btn_calibrate.Content to "Calibrate"
  </acceptance_criteria>
  <done>Calibrate 버튼 → 2점 클릭 → 픽셀 거리 계산 → mm 입력 다이얼로그 → mm/pixel 자동 계산 → CameraSlaveParam.PixelResolution + FAI PixelResolution 적용. 결과 확인 텍스트 3초간 표시.</done>
</task>

</tasks>

<verification>
1. Build: MSBuild 컴파일 오류 없음
2. 캔버스 상단에 Rect ROI / Polygon ROI / Calibrate 버튼 3개가 포함된 툴바가 표시됨
3. Rect ROI: 드래그로 사각형 → FAIConfig.ROI_Row/Col/Phi/Length1/Length2 업데이트
4. Polygon ROI: 클릭으로 점 배치 → 우클릭 완성 → FAIConfig.PolygonPoints 업데이트
5. Calibrate: 2점 클릭 → mm 입력 → CameraSlaveParam.PixelResolution 업데이트
6. Escape 키로 모든 모드 취소 가능
</verification>

<success_criteria>
- 캔버스 상단 툴바에 3개 버튼(Rect ROI, Polygon ROI, Calibrate) 표시
- Rect ROI 드래그 설정 → FAIConfig ROI 필드 업데이트 → 캔버스에 노란색 ROI 표시
- Polygon ROI 점 클릭 → 3~20점 → 우클릭 완성 → PolygonPoints 직렬화 저장
- Calibrate 2점 → mm 입력 → PixelResolution 적용 → "1 px = X.XXX mm 적용됨" 확인 표시
- Escape로 모든 드로잉 모드 취소
</success_criteria>

<output>
After completion, create `.planning/phases/02-teaching-calibration/02-02-SUMMARY.md`
</output>
