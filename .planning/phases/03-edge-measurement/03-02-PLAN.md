---
phase: 03-edge-measurement
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
  - WPF_Example/Sequence/Sequence/SequenceContext.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: false
requirements: [ALG-04]

must_haves:
  truths:
    - "측정 후 캔버스에 에지 1, 에지 2 위치가 라인으로 표시된다"
    - "에지 간 연결선(거리선)이 cyan 색상으로 표시된다"
    - "OK 결과 에지 라인은 green, NG 결과 에지 라인은 red로 표시된다"
    - "측정 결과 텍스트(FAI명, 거리mm, OK/NG)가 화면 상단에 표시된다"
    - "시퀀스 검사 완료 후 FAIResultRow가 갱신되어 DataGrid에 측정값이 표시된다"
  artifacts:
    - path: "WPF_Example/Halcon/Display/HalconDisplayService.cs"
      provides: "FAI-Edge OK/NG, FAI-DistLine RoiId 색상 분기"
      contains: "FAI-Edge"
    - path: "WPF_Example/Sequence/Sequence/SequenceContext.cs"
      provides: "ActionContext.DisplayMessages + SequenceContext.DisplayMessages + CopyFrom 복사"
      contains: "DisplayMessages"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "DisplayContextToViewer에서 context.DisplayMessages 전달 + RefreshFAIResultRows"
      contains: "RefreshFAIResultRows"
  key_links:
    - from: "Action_FAIMeasurement (Plan 01)"
      to: "HalconDisplayService.Render"
      via: "pMyContext.InspectionOverlays -> SequenceContext -> UpdateDisplayState"
      pattern: "InspectionOverlays"
    - from: "ActionContext.DisplayMessages"
      to: "SequenceContext.DisplayMessages"
      via: "CopyFrom 메서드"
      pattern: "DisplayMessages"
    - from: "MainView.DisplayContextToViewer"
      to: "MainResultViewerControl.UpdateDisplayState"
      via: "context.DisplayMessages 전달"
      pattern: "context.DisplayMessages"
---

<objective>
측정 결과를 캔버스 오버레이(에지 라인 + 거리 텍스트 + OK/NG 판정)로 시각화하고, DataGrid FAIResultRow를 자동 갱신

Purpose: Plan 01에서 계산된 측정 결과(InspectionOverlays, DisplayMessages)를 기존 HalconDisplayService 렌더링 파이프라인에 연결하여 사용자가 에지 위치, 거리, 판정을 시각적으로 확인할 수 있게 한다.
Output: HalconDisplayService에 FAI 결과 색상 분기 추가, SequenceContext에 DisplayMessages 필드 추가, MainView에 displayMessages 전달 + FAIResultRow 갱신 연결
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-edge-measurement/03-RESEARCH.md
@.planning/phases/03-edge-measurement/03-01-SUMMARY.md

@WPF_Example/Halcon/Display/HalconDisplayService.cs
@WPF_Example/Sequence/Sequence/SequenceContext.cs
@WPF_Example/UI/ContentItem/MainView.xaml.cs
@WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
@WPF_Example/UI/ViewModel/FAIResultRow.cs

<interfaces>
<!-- HalconDisplayService.Render signature -->
From WPF_Example/Halcon/Display/HalconDisplayService.cs:
```csharp
public void Render(
    HWindow window, HImage image,
    IList<RoiDefinition> rois, string selectedRoiId,
    RoiDefinition draftRoi,
    IList<EdgeInspectionOverlay> inspectionOverlays,
    IList<string> displayMessages)
// RoiId color mapping (line 79-128):
//   "ManualMeasure-End" -> red (last before else)
//   else -> blue (linewidth 2)
// displayMessages: DispText at "window" coords, y=12+(line*28), x=12, "yellow"
```

<!-- ActionContext and SequenceContext -->
From WPF_Example/Sequence/Sequence/SequenceContext.cs:
```csharp
public class ActionContext {
    public HImage ResultHalconImage { get; set; }
    public string ResultImagePath { get; set; }
    public List<EdgeInspectionOverlay> InspectionOverlays { get; set; } = new List<EdgeInspectionOverlay>();
    // NO DisplayMessages field yet
    public virtual void Clear() { /* disposes image, clears overlays */ }
}
public class SequenceContextAction {
    public HImage ResultHalconImage { get; set; }
    public List<EdgeInspectionOverlay> InspectionOverlays { get; set; } = new List<EdgeInspectionOverlay>();
    // NO DisplayMessages field yet
    public void CopyFrom(SequenceContext seqContext) { /* copies image + overlays */ }
}
public class SequenceContext {
    public HImage ResultHalconImage { get; set; }
    public List<EdgeInspectionOverlay> InspectionOverlays { get; set; } = new List<EdgeInspectionOverlay>();
    // NO DisplayMessages field yet
    public void CopyFrom(ActionContext actionContext) { /* copies image + overlays */ }
}
```

<!-- MainView display pipeline -->
From WPF_Example/UI/ContentItem/MainView.xaml.cs:
```csharp
public void DisplayParam(SequenceContext context, ParamBase param)
    -> DisplayContextToViewer(context, ConvertParamRects(param))
private bool DisplayContextToViewer(SequenceContext context, IEnumerable<RoiDefinition> rois)
    -> halconViewer.UpdateDisplayState(roiList, context.InspectionOverlays, null)
    //                                                                      ^^^^ messages=null
```

<!-- MainResultViewerControl -->
From WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs:
```csharp
public void UpdateDisplayState(IEnumerable<RoiDefinition> rois, string selectedRoiId,
    IEnumerable<EdgeInspectionOverlay> overlays, IEnumerable<string> messages)
public void UpdateDisplayState(IEnumerable<RoiDefinition> rois,
    IEnumerable<EdgeInspectionOverlay> overlays, IEnumerable<string> messages)
```

<!-- FAIResultRow -->
From WPF_Example/UI/ViewModel/FAIResultRow.cs:
```csharp
public class FAIResultRow : Observable {
    public void Refresh();  // OnPropertyChanged for all display properties
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HalconDisplayService FAI RoiId 색상 + SequenceContext DisplayMessages 필드 + MainView 파이프라인 연결</name>
  <files>WPF_Example/Halcon/Display/HalconDisplayService.cs, WPF_Example/Sequence/Sequence/SequenceContext.cs, WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    WPF_Example/Halcon/Display/HalconDisplayService.cs
    WPF_Example/Sequence/Sequence/SequenceContext.cs
    WPF_Example/UI/ContentItem/MainView.xaml.cs
    WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
    WPF_Example/UI/ViewModel/FAIResultRow.cs
  </read_first>
  <action>
**1. HalconDisplayService.cs 수정** — Render 메서드의 inspectionOverlays RoiId 색상 분기에 FAI 결과용 패턴 추가.

기존 `else if (string.Equals(overlay.RoiId, "ManualMeasure-End"...))` 블록(line ~120) 뒤, `else { window.SetColor("blue"); }` 블록(line ~125) 앞에 다음 분기 삽입:

```csharp
//260409 hbk Phase 3: FAI edge measurement result overlay colors
else if (overlay.RoiId != null && overlay.RoiId.StartsWith("FAI-Edge", StringComparison.OrdinalIgnoreCase))
{
    bool isNG = overlay.RoiId.EndsWith("-NG", StringComparison.OrdinalIgnoreCase);
    window.SetColor(isNG ? "red" : "green");
    window.SetLineWidth(2);
}
else if (string.Equals(overlay.RoiId, "FAI-DistLine", StringComparison.OrdinalIgnoreCase))
{
    window.SetColor("cyan");
    window.SetLineWidth(1);
}
```

Plan 01의 FAIEdgeMeasurementService가 RoiId를 다음 규칙으로 생성:
- 에지 1: `"FAI-Edge1-OK"` 또는 `"FAI-Edge1-NG"` (fai.IsPass 기반)
- 에지 2: `"FAI-Edge2-OK"` 또는 `"FAI-Edge2-NG"`
- 연결선: `"FAI-DistLine"`

**2. SequenceContext.cs 수정** — 3개 클래스에 `DisplayMessages` 필드 추가.

`ActionContext` 클래스 (line ~32):
- `InspectionOverlays` 선언 다음 줄에 추가:
  `public List<string> DisplayMessages { get; set; } = new List<string>(); //260409 hbk`
- `Clear()` 메서드에 `InspectionOverlays.Clear();` 다음 줄에 추가:
  `DisplayMessages.Clear(); //260409 hbk`

`SequenceContextAction` 클래스 (line ~80):
- `InspectionOverlays` 선언 다음 줄에 추가:
  `public List<string> DisplayMessages { get; set; } = new List<string>(); //260409 hbk`
- `Clear()` 메서드에 `InspectionOverlays.Clear();` 다음 줄에 추가:
  `DisplayMessages.Clear(); //260409 hbk`
- `CopyFrom(SequenceContext seqContext)` 메서드에 InspectionOverlays 복사 다음 줄에 추가:
  ```csharp
  DisplayMessages = seqContext.DisplayMessages == null
      ? new List<string>()
      : new List<string>(seqContext.DisplayMessages); //260409 hbk
  ```

`SequenceContext` 클래스 (line ~83):
- `InspectionOverlays` 선언 다음 줄에 추가:
  `public List<string> DisplayMessages { get; set; } = new List<string>(); //260409 hbk`
- `Clear()` 메서드에 `InspectionOverlays.Clear();` 다음 줄에 추가:
  `DisplayMessages.Clear(); //260409 hbk`
- `CopyFrom(ActionContext actionContext)` 메서드에 InspectionOverlays 복사 다음 줄에 추가:
  ```csharp
  DisplayMessages = actionContext.DisplayMessages == null
      ? new List<string>()
      : new List<string>(actionContext.DisplayMessages); //260409 hbk
  ```

**3. MainView.xaml.cs 수정** — DisplayContextToViewer에서 DisplayMessages 전달 + FAIResultRow 갱신.

3a. `DisplayContextToViewer` 메서드 내의 `UpdateDisplayState` 호출 3곳에서 마지막 인자 `null`을 `context.DisplayMessages`로 변경:
```csharp
halconViewer.UpdateDisplayState(roiList, context.InspectionOverlays, context.DisplayMessages); //260409 hbk
```

3b. MainView 클래스에 새 private 메서드 추가:
```csharp
//260409 hbk Phase 3: refresh FAI result rows after measurement
private void RefreshFAIResultRows()
{
    if (FAIResults == null || FAIResults.ItemsSource == null) return;
    foreach (var item in FAIResults.ItemsSource)
    {
        var row = item as FAIResultRow;
        if (row != null) row.Refresh();
    }
}
```

3c. `DisplayParam` 메서드 끝(ExecuteOnUi 람다 내부, `tb_result.Content = resultStr;` 이후)에 추가:
```csharp
RefreshFAIResultRows(); //260409 hbk Phase 3
```

3d. `DisplaySequenceContext` 메서드 끝(ExecuteOnUi 람다 내부)에도 동일하게 추가:
```csharp
RefreshFAIResultRows(); //260409 hbk Phase 3
```
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && grep -c "FAI-Edge\|FAI-DistLine" WPF_Example/Halcon/Display/HalconDisplayService.cs && grep -c "DisplayMessages\|RefreshFAIResultRows" WPF_Example/UI/ContentItem/MainView.xaml.cs && grep -c "DisplayMessages" WPF_Example/Sequence/Sequence/SequenceContext.cs</automated>
  </verify>
  <acceptance_criteria>
    - HalconDisplayService.cs에 `"FAI-Edge"` StartsWith 분기 존재
    - HalconDisplayService.cs에 `"FAI-DistLine"` Equals 분기 존재
    - HalconDisplayService.cs에 `isNG ? "red" : "green"` 패턴 존재
    - SequenceContext.cs에 `public List<string> DisplayMessages` 가 3회 이상 등장 (ActionContext, SequenceContextAction, SequenceContext)
    - SequenceContext.cs의 Clear 메서드들에 `DisplayMessages.Clear()` 존재
    - SequenceContext.cs의 CopyFrom 메서드들에 `DisplayMessages =` 복사 로직 존재
    - MainView.xaml.cs의 DisplayContextToViewer에서 `context.DisplayMessages` 전달 (null이 아님)
    - MainView.xaml.cs에 `private void RefreshFAIResultRows()` 메서드 존재
    - MainView.xaml.cs의 DisplayParam에서 `RefreshFAIResultRows()` 호출 존재
    - 모든 수정에 `//260409 hbk` 주석 포함
  </acceptance_criteria>
  <done>HalconDisplayService가 FAI 에지 오버레이를 OK=green/NG=red로 렌더링하고, DisplayMessages가 ActionContext -> SequenceContext -> MainView -> MainResultViewerControl 파이프라인으로 전달되며, FAIResultRow가 측정 완료 후 자동 갱신된다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 3 에지 측정 전체 시각 검증</name>
  <files>none</files>
  <action>
사용자가 SIMUL_MODE 빌드 실행 후 에지 측정 결과를 시각적으로 검증한다. 자동화 작업 없음 — 순수 시각 확인 체크포인트.

검증 대상:
1. FAIEdgeMeasurementService - MeasurePos로 에지 페어 검출 + mm 거리 계산
2. Action_FAIMeasurement 스텁 교체 - 실제 측정 수행 + 공차 판정
3. 캔버스 오버레이 - 에지 라인(OK=green/NG=red) + 연결선(cyan) + 결과 텍스트
4. FAIResultRow 자동 갱신 - DataGrid에 측정값 표시

검증 절차:
1. 앱을 SIMUL_MODE로 빌드 실행
2. 레시피에서 FAI가 있는 Shot 선택
3. FAI의 ROI가 설정된 상태에서 해당 Shot의 SimulImagePath에 에지가 있는 이미지 확인
4. 시퀀스 검사 실행 (또는 수동 트리거)
5. 확인 사항:
   - 캔버스에 에지 위치 라인이 green(OK) 또는 red(NG)로 표시되는가
   - 에지 간 연결선이 cyan으로 표시되는가
   - 화면 상단에 FAI별 측정 결과 텍스트가 yellow로 표시되는가
   - DataGrid FAI 결과 테이블에 MeasuredValue와 OK/NG 판정이 표시되는가
6. FAIConfig의 NominalValue와 Tolerance를 조정하여 OK/NG 판정이 변경되는지 확인
  </action>
  <verify>사용자가 "approved" 입력 또는 발견된 문제 설명</verify>
  <done>사용자가 에지 측정 오버레이(에지 라인, 연결선, 텍스트)와 DataGrid 결과 갱신을 시각적으로 확인하고 승인</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| InspectionOverlays -> HalconDisplayService | Action 스레드에서 생성된 오버레이가 UI 스레드로 전달됨 |
| ActionContext -> SequenceContext CopyFrom | Action 결과가 Sequence 컨텍스트로 복사될 때 스레드 안전성 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | DisplayMessages 문자열 | accept | 내부 생성 문자열이며 외부 입력 아님. DispText에 그대로 전달 |
| T-03-06 | Denial of Service | 대량 오버레이 렌더링 | accept | FAI 최대 100개 x 3 오버레이 = 300개, HWindow 렌더링 한계 내 |
| T-03-07 | Tampering | SequenceContext.CopyFrom | mitigate | null 체크 + 방어적 복사 (new List<string>(source)) 패턴으로 원본 리스트 참조 공유 방지 |
</threat_model>

<verification>
1. HalconDisplayService에서 "FAI-Edge1-OK" RoiId가 green으로, "FAI-Edge1-NG"가 red로 렌더링됨
2. "FAI-DistLine" RoiId가 cyan으로 렌더링됨
3. DisplayMessages가 ActionContext -> SequenceContext -> MainView -> MainResultViewerControl 파이프라인으로 전달됨
4. FAIResultRow.Refresh()가 측정 완료 후 호출되어 DataGrid가 갱신됨
5. 빌드 에러 없음
</verification>

<success_criteria>
- 측정 후 캔버스에 에지 라인(green/red)과 연결선(cyan)이 표시된다
- 화면 상단에 FAI별 측정 결과 텍스트가 표시된다
- DataGrid에 MeasuredValue와 OK/NG 판정이 자동 갱신된다
- 사용자가 시각적으로 에지 측정 결과를 확인하고 승인한다
</success_criteria>

<output>
After completion, create `.planning/phases/03-edge-measurement/03-02-SUMMARY.md`
</output>
