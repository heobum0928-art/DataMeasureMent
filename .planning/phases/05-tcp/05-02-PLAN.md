---
phase: 05-tcp
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - WPF_Example/TcpServer/VisionResponsePacket.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Custom/Sequence/SequenceHandler.cs
  - WPF_Example/MainWindow.xaml.cs
autonomous: true
requirements: [SEQ-03, SEQ-04]
must_haves:
  truths:
    - "전체 FAI 결과를 종합하여 최종 OK/NG 판정이 산출된다"
    - "측정 완료 후 FAI별 결과 데이터가 TCP 패킷으로 호스트에 전송된다"
    - "Shot별 Action 완료 시 UI 결과 테이블이 실시간 갱신된다"
    - "전체 시퀀스 완료 시 최종 종합 판정이 UI에 표시된다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "InspectionSequence 클래스 (AddResponse 오버라이드로 FAI 결과 TCP 전송)"
      contains: "InspectionSequence"
    - path: "WPF_Example/TcpServer/VisionResponsePacket.cs"
      provides: "TestResultPacket에 FAI 동적 결과 리스트 필드 추가"
      contains: "FAIResults"
    - path: "WPF_Example/Custom/Sequence/SequenceHandler.cs"
      provides: "InspectionSequence로 시퀀스 교체"
      contains: "InspectionSequence"
  key_links:
    - from: "InspectionSequence.AddResponse()"
      to: "TestResultPacket.FAIResults"
      via: "FAI 결과를 리스트로 채움"
      pattern: "FAIResults"
    - from: "SequenceBase.Finish()"
      to: "InspectionSequence.AddResponse()"
      via: "RequestPacket != null이면 AddResponse 호출"
      pattern: "AddResponse"
    - from: "MainWindow.OnSequenceFinish"
      to: "MainView.DisplaySequenceContext"
      via: "기존 이벤트 + 종합 판정 표시"
      pattern: "OnSequenceFinish"
---

<objective>
TCP 응답 패킷에 FAI별 개별 결과를 동적으로 전송하고, 종합 OK/NG 판정을 산출하며, Shot별 실시간 UI 갱신과 최종 판정 표시를 구현한다.

Purpose: 호스트가 검사 결과를 FAI 단위로 수신하고, 사용자가 검사 진행 중 Shot별 결과를 실시간으로 확인하며 최종 판정을 볼 수 있다.
Output: InspectionSequence (AddResponse), TestResultPacket FAI 결과 리스트, UI 종합 판정 표시
</objective>

<execution_context>
@C:\Users\tech\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\tech\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tcp/05-CONTEXT.md
@.planning/phases/05-tcp/05-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 추가된 핵심 인터페이스 -->

From WPF_Example/Sequence/Sequence/SequenceBase.cs (Plan 01 결과):
```csharp
// 모든 Action 순차 실행
public bool StartAll(TestPacket packet);

// ExecuteAction에서 Action 완료 후 다음 Action 진행
// OnActionChanged 이벤트가 각 Action 완료 시 발생
public event EventActionChanged OnActionChanged;

// 마지막 Action 완료 시 Finish() 호출 → AddResponse() 호출
protected virtual void AddResponse() { }
```

From WPF_Example/Custom/Sequence/Top/Sequence_Top.cs (AddResponse 패턴 참조):
```csharp
protected override void AddResponse() {
    if (RequestPacket == null) return;
    var responsePacket = new TestResultPacket {
        Target = RequestPacket.Sender,
        Site = RequestPacket.Site,
        InspectionType = RequestPacket.TestType,
        Result = pMyContext.ResultInfo,
        Angle = pMyContext.dAngle,
        X = pMyContext.CenterOffsetXmm,
        Y = pMyContext.CenterOffsetYmm,
    };
    ResponseQueue.Enqueue(responsePacket);
}
```

From WPF_Example/TcpServer/VisionResponsePacket.cs (TestResultPacket 현재 구조):
```csharp
public class TestResultPacket : VisionResponsePacket {
    public int InspectionType { get; set; }
    public EVisionResultType Result { get; set; }
    public double Angle { get; set; }
    public double X { get; set; }
    public double Y { get; set; }
    public const int MaxListCount = 10;
    public VisionResponseListData[] visionResults = new VisionResponseListData[MaxListCount];
}
```

From VisionResponsePacket.Convert (직렬화 패턴):
```csharp
// Bottom Inspection 분기에서 visionResults[10] 배열 순회 패턴
if ((testPacket.InspectionType == (int)ETestType.Inspection) && testPacket.Site == (int)ESequence.Bottom) {
    for (int i = 0; i < 10; i++) { ... }
}
```

From WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs:
```csharp
public double MeasuredValue { get; set; }
public bool IsPass { get; set; }
public string FAIName { get; set; }
```

From WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs:
```csharp
public List<ShotConfig> Shots { get; private set; }
public int ShotCount => Shots.Count;
```

From WPF_Example/MainWindow.xaml.cs (기존 이벤트 등록):
```csharp
mSystemHandler.Sequences[i].OnFinish += OnSequenceFinish;
// OnSequenceFinish -> mainView.DisplaySequenceContext(context)
// DisplaySequenceContext -> RefreshFAIResultRows() 호출 (이미 구현)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TestResultPacket FAI 동적 리스트 확장 + InspectionSequence 생성</name>
  <files>WPF_Example/TcpServer/VisionResponsePacket.cs, WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs, WPF_Example/DatumMeasurement.csproj</files>
  <read_first>
    - WPF_Example/TcpServer/VisionResponsePacket.cs (TestResultPacket 구조, VisionResponsePacket.Convert 직렬화 로직)
    - WPF_Example/Custom/Sequence/Top/Sequence_Top.cs (TopSequence, AddResponse 패턴)
    - WPF_Example/Custom/Sequence/Bottom/Sequence_Bottom.cs (BottomSequence, visionResults 패턴)
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs (FAIList 접근)
    - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs (MeasuredValue, IsPass)
    - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs (Shots 리스트)
    - WPF_Example/Custom/Sequence/SequenceHandler.cs (RecipeManager, RegisterSequences)
  </read_first>
  <action>
3개 파일을 수정/생성한다.

**1. TestResultPacket에 FAI 동적 결과 리스트 추가 (per D-04, D-05, D-06)**

`VisionResponsePacket.cs`의 `TestResultPacket` 클래스에 필드를 추가한다:

```csharp
public class TestResultPacket : VisionResponsePacket {
    // ... 기존 필드 유지 ...

    //260409 hbk Phase 5: FAI별 동적 결과 리스트 (D-04)
    public List<FAIResultData> FAIResults { get; set; } = new List<FAIResultData>();
    public int FAICount => FAIResults.Count;
    public bool IsDynamicFAI { get; set; } = false;
    // ... 기존 코드 유지 ...
}
```

같은 파일에 `FAIResultData` 클래스를 `VisionResponseListData` 클래스 아래에 추가:

```csharp
//260409 hbk Phase 5: FAI별 개별 측정 결과 (D-04, D-05)
public class FAIResultData {
    public string FAIName { get; set; }
    public EVisionResultType Result { get; set; }
    public double DistanceMm { get; set; }

    public FAIResultData() {
        Result = EVisionResultType.NG;
        DistanceMm = 0;
    }

    public FAIResultData(string name, bool isPass, double distMm) {
        FAIName = name;
        Result = isPass ? EVisionResultType.OK : EVisionResultType.NG;
        DistanceMm = distMm;
    }
}
```

`VisionResponsePacket.Convert` 메서드의 `EVisionResponseType.Test` case에서, 기존 Bottom 분기 `if` 조건 앞에 동적 FAI 분기를 추가한다:

기존:
```csharp
if ((testPacket.InspectionType == (int)ETestType.Inspection) && testPacket.Site == (int)ESequence.Bottom) {
```

변경 후 (이 if 앞에 추가):
```csharp
//260409 hbk Phase 5: 동적 FAI 모드 직렬화 (D-05)
if (testPacket.IsDynamicFAI) {
    msg += testPacket.GetResultString();
    msg += VisionServer.MSG_CONTENTS_SEPERATOR;
    msg += testPacket.FAICount.ToString();
    for (int i = 0; i < testPacket.FAICount; i++) {
        msg += VisionServer.MSG_CONTENTS_SEPERATOR;
        var faiData = testPacket.FAIResults[i];
        msg += (faiData.Result == EVisionResultType.OK) ? TEST_RESULT_PASS : TEST_RESULT_FAIL;
        msg += VisionServer.MSG_CONTENTS_SEPERATOR;
        msg += faiData.DistanceMm.ToString("0.000");
    }
}
else if ((testPacket.InspectionType == (int)ETestType.Inspection) && testPacket.Site == (int)ESequence.Bottom) {
```

using 추가 필요: `using System.Collections.Generic;` (이미 존재하면 무시)

**2. InspectionSequence.cs 생성 (per D-07)**

`WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 파일을 생성한다. TopSequence 패턴을 따르되, AddResponse에서 FAI 결과를 종합한다:

```csharp
//260409 hbk Phase 5: 동적 FAI 검사 시퀀스 (D-07)
using ReringProject.Define;
using ReringProject.Device;
using ReringProject.Network;
using ReringProject.UI;
using System.Windows;

namespace ReringProject.Sequence {

    public class InspectionSequenceContext : SequenceContext {
        public EVisionResultType ResultInfo { get; set; }

        public InspectionSequenceContext(InspectionSequence source) : base(source) { }

        public override void Clear() {
            ResultInfo = EVisionResultType.NG;
            base.Clear();
        }

        public override void CopyFrom(ActionContext actionContext) {
            base.CopyFrom(actionContext);
            Result = actionContext.Result;
            if (actionContext is FAIMeasurementContext faiContext) {
                // 각 Action의 AllPass가 false면 NG
                if (!faiContext.AllPass) {
                    ResultInfo = EVisionResultType.NG;
                }
            }
        }
    }

    public class InspectionSequence : SequenceBase {
        private readonly DeviceHandler pDevs;
        private readonly InspectionSequenceContext pMyContext;
        private readonly CameraMasterParam pMyParam;
        private readonly string DefaultCamera;
        private readonly string DefaultLight;

        public InspectionSequence(ESequence seqID, string name, int algIndex, string defaultCamera, string defaultLight) : base(seqID, name) {
            pDevs = SystemHandler.Handle.Devices;
            Context = new InspectionSequenceContext(this);
            pMyContext = Context as InspectionSequenceContext;
            Param = new CameraMasterParam(this);
            pMyParam = Param as CameraMasterParam;
            DefaultCamera = defaultCamera;
            DefaultLight = defaultLight;
        }

        //260409 hbk Phase 5: 종합 판정 + FAI별 결과 TCP 전송 (D-07)
        protected override void AddResponse() {
            if (RequestPacket == null) return;

            var recipeManager = SystemHandler.Handle.Sequences.RecipeManager;

            // 종합 판정: 모든 FAI가 Pass여야 OK
            bool allPass = true;
            var responsePacket = new TestResultPacket {
                Target = RequestPacket.Sender,
                Site = RequestPacket.Site,
                InspectionType = RequestPacket.TestType,
                IsDynamicFAI = true,
            };

            foreach (var shot in recipeManager.Shots) {
                foreach (var fai in shot.FAIList) {
                    if (!fai.IsPass) allPass = false;
                    responsePacket.FAIResults.Add(new FAIResultData(
                        fai.FAIName ?? "FAI",
                        fai.IsPass,
                        fai.MeasuredValue
                    ));
                }
            }

            responsePacket.Result = allPass ? EVisionResultType.OK : EVisionResultType.NG;
            pMyContext.ResultInfo = responsePacket.Result;

            ResponseQueue.Enqueue(responsePacket);
        }

        public override void OnCreate() {
            pMyParam.LightGroupName = DefaultLight;
            pMyParam.DeviceName = DefaultCamera;

            var camera = pDevs[pMyParam.DeviceName];
            if (camera == null || camera.Properties == null) {
                CustomMessageBox.Show("Error", $"Camera {pMyParam.DeviceName} - Initialize Fail", MessageBoxImage.Error);
                IsInitialized = false;
                Context.State = EContextState.Error;
                return;
            }

            IsInitialized = true;
            base.OnCreate();
        }

        public override void OnLoad() {
            SystemHandler.Handle.Lights.ApplyLight(pMyParam);
            base.OnLoad();
        }

        public override void OnRelease() {
            IsInitialized = false;
            base.OnRelease();
        }
    }
}
```

DatumMeasurement.csproj에 `<Compile Include="Custom\Sequence\Inspection\InspectionSequence.cs" />` 추가. 기존 `Action_FAIMeasurement.cs` Include 근처에 배치.

**3. SequenceHandler RegisterSequences에 InspectionSequence 교체**

이 작업은 Task 2에서 수행. 현재는 기존 TopSequence가 Top에 등록되어 있고, Dynamic FAI 모드에서 InspectionSequence를 사용해야 한다. 그러나 RegisterSequences()는 앱 시작 시 한 번만 호출되므로, 기존 TopSequence를 InspectionSequence로 교체하면 비동적 모드에서도 영향을 준다.

접근 방식: RegisterSequences에서 Top 시퀀스를 InspectionSequence로 교체한다. InspectionSequence의 AddResponse는 IsDynamicFAI=true로 FAI 동적 리스트를 전송하므로, 기존 비동적 모드에서는 FAIResults가 비어있을 뿐 기능에 영향 없다.

`Custom/Sequence/SequenceHandler.cs`의 `RegisterSequences()` 메서드에서 Top 시퀀스를 교체:

현재:
```csharp
SequenceBuilder.RegisterSequence(
    new TopSequence(ESequence.Top, SEQ_TOP, Top_Alg_Index, DeviceHandler.CAMERA_TOP, LightHandler.LIGHT_TOP),
```

변경:
```csharp
//260409 hbk Phase 5: 동적 FAI 모드용 InspectionSequence (D-07)
SequenceBuilder.RegisterSequence(
    new InspectionSequence(ESequence.Top, SEQ_TOP, Top_Alg_Index, DeviceHandler.CAMERA_TOP, LightHandler.LIGHT_TOP),
```

Side도 필요시 교체 가능하나, 현재 Top만 Dynamic FAI 대상이므로 Top만 교체.
  </action>
  <verify>
    <automated>cd C:/Info/Project/DataMeasurement && grep -n "FAIResultData" WPF_Example/TcpServer/VisionResponsePacket.cs && grep -n "class InspectionSequence" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs && grep -n "IsDynamicFAI" WPF_Example/TcpServer/VisionResponsePacket.cs && grep -n "InspectionSequence" WPF_Example/Custom/Sequence/SequenceHandler.cs</automated>
  </verify>
  <acceptance_criteria>
    - VisionResponsePacket.cs에 FAIResultData 클래스 존재 (FAIName, Result, DistanceMm 필드)
    - TestResultPacket에 FAIResults 리스트와 IsDynamicFAI 플래그 존재
    - VisionResponsePacket.Convert에 IsDynamicFAI 분기 존재 (FAICount + 개별 결과 직렬화)
    - InspectionSequence.cs 파일에 AddResponse 오버라이드 존재
    - AddResponse에서 recipeManager.Shots 순회하며 FAIResults 채움
    - AddResponse에서 allPass = 모든 FAI.IsPass AND 연산으로 종합 판정
    - SequenceHandler.RegisterSequences에서 Top이 InspectionSequence로 교체
    - DatumMeasurement.csproj에 InspectionSequence.cs Compile Include 존재
  </acceptance_criteria>
  <done>TestResultPacket이 FAI별 동적 결과를 전송하고, InspectionSequence가 종합 판정을 산출하여 TCP 패킷을 생성한다</done>
</task>

<task type="auto">
  <name>Task 2: UI 종합 판정 표시 + OnActionChanged 이벤트 재등록</name>
  <files>WPF_Example/MainWindow.xaml.cs</files>
  <read_first>
    - WPF_Example/MainWindow.xaml.cs (OnSequenceFinish, 이벤트 등록/해제 패턴)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (DisplaySequenceContext, DisplayActionContext, RefreshFAIResultRows)
    - WPF_Example/UI/ViewModel/InspectionViewModel.cs (FAI 결과 표시 구조 확인)
  </read_first>
  <action>
MainWindow.xaml.cs에서 두 가지 변경을 수행한다.

**1. OnActionChanged 이벤트 등록 추가 (per D-12)**

기존 이벤트 등록에서 OnActionChanged는 MainView.DisplayActionContext에서 이미 처리되고 있으나, MainWindow 수준에서 이벤트를 등록하지 않고 있다. SequenceBase의 OnActionChanged는 ExecuteAction 내부에서 발생하며, MainView.DisplayActionContext가 이를 처리하는 경로를 확인한다.

실제로 MainView.DisplayActionContext는 SequenceBase에서 직접 호출되는 것이 아니라, MainWindow가 이벤트를 등록해야 호출된다. 기존 코드를 확인하면:
- `OnFinish`, `OnStart`, `OnStop`, `OnError`는 MainWindow에서 등록
- `OnActionChanged`는 MainWindow에서 등록되지 않음

그러나 DisplayActionContext는 다른 경로로 호출될 수 있다. 기존 코드 확인 후, OnActionChanged 이벤트가 MainWindow에서 등록되지 않았다면 추가한다.

기존 이벤트 등록 블록 (for 루프 내):
```csharp
mSystemHandler.Sequences[i].OnStart += OnSequenceStart;
mSystemHandler.Sequences[i].OnStop += OnSequenceStop;
mSystemHandler.Sequences[i].OnError += OnSequenceError;
mSystemHandler.Sequences[i].OnFinish += OnSequenceFinish;
```

이 블록에 추가:
```csharp
//260409 hbk Phase 5: Shot별 실시간 UI 갱신 (D-12)
mSystemHandler.Sequences[i].OnActionChanged += OnActionChanged;
```

해당 이벤트 해제 블록에도 추가:
```csharp
//260409 hbk Phase 5: Shot별 실시간 UI 갱신 해제 (D-12)
mSystemHandler.Sequences[i].OnActionChanged -= OnActionChanged;
```

**2. OnActionChanged 핸들러 추가 (per D-12)**

OnSequenceFinish 메서드 근처에 새 핸들러를 추가:

```csharp
//260409 hbk Phase 5: Shot별 Action 완료 시 실시간 UI 갱신 (D-12)
private void OnActionChanged(ActionContext context) {
    Dispatcher.BeginInvoke(System.Windows.Threading.DispatcherPriority.Normal, new Action(() => {
        mainView.DisplayActionContext(context);
    }));
}
```

주의: `DisplayActionContext`는 이미 내부에서 `RefreshFAIResultRows()`를 호출하므로 (Phase 3에서 추가), Shot별 FAI 결과가 실시간으로 DataGrid에 갱신된다.

**3. OnSequenceFinish에 종합 판정 로그 추가 (per D-13)**

기존 OnSequenceFinish 메서드 내에서 `mainView.DisplaySequenceContext(context)` 호출 후에 종합 판정 로그를 추가:

```csharp
//260409 hbk Phase 5: 최종 종합 판정 로그 (D-13)
Logging.PrintLog((int)ELogType.Result, "Sequence {0} Final Result: {1} ({2}ms)",
    context.Source.Name, context.ResultString, context.Timer.ElapsedMilliseconds);
```

이 로그는 `Logging.PrintLog` 호출이 이미 OnSequenceFinish 내에 존재하지만, 종합 판정임을 명시하는 메시지로 추가한다. `DisplaySequenceContext`는 이미 `RefreshFAIResultRows()`를 호출하므로 최종 테이블 갱신도 처리된다.
  </action>
  <verify>
    <automated>cd C:/Info/Project/DataMeasurement && grep -n "OnActionChanged" WPF_Example/MainWindow.xaml.cs && grep -n "DisplayActionContext" WPF_Example/MainWindow.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - grep "OnActionChanged += OnActionChanged" MainWindow.xaml.cs 결과가 이벤트 등록 블록에 존재
    - grep "OnActionChanged -= OnActionChanged" MainWindow.xaml.cs 결과가 이벤트 해제 블록에 존재
    - grep "private void OnActionChanged" MainWindow.xaml.cs 결과가 핸들러 메서드로 존재
    - grep "DisplayActionContext" MainWindow.xaml.cs 결과가 OnActionChanged 핸들러 내부에 존재
    - grep "Final Result" MainWindow.xaml.cs 결과가 OnSequenceFinish 내에 존재
  </acceptance_criteria>
  <done>OnActionChanged 이벤트로 Shot별 실시간 UI 갱신이 동작하고, 시퀀스 완료 시 종합 판정이 로그에 표시된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| InspectionSequence -> TCP Client | 측정 결과를 외부 호스트로 전송 |
| RecipeManager.Shots -> AddResponse | 내부 데이터를 패킷으로 변환 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | I (Information Disclosure) | FAIResults TCP 전송 | accept | 측정 결과는 호스트가 수신해야 하는 정보. 내부 네트워크 전용. |
| T-05-06 | T (Tampering) | FAIResults 직렬화 | mitigate | FAIResultData는 서버 측에서만 생성. 클라이언트 입력으로 생성되지 않음. Convert 역직렬화는 기존 패턴 유지. |
| T-05-07 | D (DoS) | AddResponse FAI 순회 | accept | FAI 개수는 레시피에 의해 제한 (100개 이하 설계). 성능 영향 무시 가능. |
</threat_model>

<verification>
1. TestResultPacket에 FAIResults 리스트와 FAIResultData 클래스 존재 확인
2. VisionResponsePacket.Convert에 IsDynamicFAI 분기 존재 확인
3. InspectionSequence.cs에 AddResponse 오버라이드 존재 확인
4. SequenceHandler에서 Top 시퀀스가 InspectionSequence로 교체 확인
5. MainWindow에 OnActionChanged 이벤트 등록/핸들러 존재 확인
6. 빌드: MSBuild DatumMeasurement.csproj 성공 (SIMUL_MODE)
</verification>

<success_criteria>
- InspectionSequence.AddResponse가 모든 Shot의 FAI 결과를 종합하여 allPass 판정 산출
- TestResultPacket이 FAI별 Result(P/F)와 DistanceMm을 동적 리스트로 포함
- TCP 패킷이 RESULT,Site,Type,TotalResult,FAICount,[FAI1_Result,FAI1_Dist,...] 형태로 직렬화
- OnActionChanged 이벤트로 Shot별 Action 완료 시 UI DataGrid 실시간 갱신
- OnSequenceFinish에서 최종 종합 판정이 표시됨
</success_criteria>

<output>
After completion, create `.planning/phases/05-tcp/05-02-SUMMARY.md`
</output>
