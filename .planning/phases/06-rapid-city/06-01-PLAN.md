---
phase: 06-rapid-city
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgePairDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/PointToLineDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/PointToPointDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/LineToLineAngleMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/LineToLineDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs
  - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
  - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
autonomous: true
requirements: [RC-03]
must_haves:
  truths:
    - "MeasurementBase 추상 클래스가 ParamBase를 상속하고 TryExecute 추상 메서드를 선언한다"
    - "6종 파생 클래스가 각각 TryExecute()를 override하여 Halcon 알고리즘으로 측정한다"
    - "MeasurementFactory.Create(typeName)가 올바른 파생 클래스 인스턴스를 반환한다"
    - "VisionAlgorithmService가 FitLine, FindCircle, IntersectLines 등 빌딩 블록을 제공한다"
    - "FAIConfig가 List<MeasurementBase> Measurements를 소유한다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs"
      provides: "추상 클래스 — DatumRef, NominalValue, TolerancePlus/Minus, TryExecute, EvaluateJudgement"
      contains: "abstract class MeasurementBase"
    - path: "WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs"
      provides: "Factory 패턴 — 타입 문자열로 파생 클래스 생성"
      exports: ["MeasurementFactory.Create"]
    - path: "WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs"
      provides: "Halcon 빌딩 블록 — FitLine, FindCircle, IntersectLines, DistancePointToLine, etc."
      exports: ["VisionAlgorithmService"]
  key_links:
    - from: "EdgePairDistanceMeasurement"
      to: "FAIEdgeMeasurementService"
      via: "TryExecute 내부에서 래핑 호출"
      pattern: "FAIEdgeMeasurementService.*TryMeasure"
    - from: "FAIConfig"
      to: "MeasurementBase"
      via: "List<MeasurementBase> Measurements"
      pattern: "List<MeasurementBase>"
---

<objective>
MeasurementBase 추상 클래스 + 6종 파생 측정 알고리즘 + VisionAlgorithmService Halcon 빌딩 블록 + MeasurementFactory를 생성하고, FAIConfig에 List<MeasurementBase> Measurements 프로퍼티를 추가한다.

Purpose: Rapid City 75개+ FAI의 다양한 측정 유형(에지 페어, 점-선 거리, 점-점 거리, 선-선 각도, 원 직경, 선-선 거리)을 지원하기 위한 Multi-Algorithm 구조 구현 (D-14~D-21).
Output: MeasurementBase.cs, 6개 파생 클래스, VisionAlgorithmService.cs, MeasurementFactory.cs, FAIConfig 수정
</objective>

<execution_context>
@C:/Users/tech/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-rapid-city/06-CONTEXT.md
@.planning/phases/06-rapid-city/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From WPF_Example/Sequence/Param/ParamBase.cs:
```csharp
public class ParamBase : INotifyPropertyChanged {
    public object Owner { get; set; }
    public string OwnerName { get; }
    public SequenceBase Parent { get; set; }
    public ParamBase(object owner) { ... }
    // Save/Load: 리플렉션 기반. Int32, Double, String, Boolean, Rect, Line, Circle 자동 직렬화.
    // List<T>는 자동 직렬화 안 됨 — InspectionRecipeManager에서 수동 처리 필요.
}
```

From WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs:
```csharp
public class FAIConfig : ParamBase {
    public double ROI_Row, ROI_Col, ROI_Phi, ROI_Length1, ROI_Length2;
    public int EdgeThreshold; public double Sigma;
    public string EdgeDirection, EdgeSelection, EdgePolarity;
    public int EdgeSampleCount, EdgeTrimCount;
    public double PixelResolutionX, PixelResolutionY;
    public string PolygonPoints;
    public double NominalValue, UpperTolerance, LowerTolerance;
    public double MeasuredValue; public bool IsPass;
    public string FAIName;
    public FAIConfig(object owner) : base(owner) { }
    public FAIConfig(object owner, string name) : base(owner) { FAIName = name; }
    public void SetResult(double measuredValue) { ... }
    public void ClearResult() { ... }
    public RoiDefinition ToRoiDefinition() { ... }
}
```

From WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs:
```csharp
public class FAIEdgeMeasurementService {
    public bool TryMeasure(HImage image, FAIConfig fai, out FAIEdgeMeasurementResult result);
    public bool TryMeasure(HImage image, FAIConfig fai, HTuple transform, out FAIEdgeMeasurementResult result);
}
```

From WPF_Example/Halcon/Models/FAIEdgeMeasurementResult.cs:
```csharp
// 에지 측정 결과 — DistanceMm, OverlayItems
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MeasurementBase 추상 클래스 + VisionAlgorithmService + MeasurementFactory 생성</name>
  <files>
    WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
    WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
    WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs
  </files>
  <read_first>
    WPF_Example/Sequence/Param/ParamBase.cs
    WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs
    WPF_Example/Halcon/Algorithms/DatumFindingService.cs
    WPF_Example/Halcon/Algorithms/MeasurementAlgorithm.cs
    WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  </read_first>
  <action>
1. **MeasurementBase.cs** 생성 (WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs):
   - namespace: `ReringProject.Sequence` (FAIConfig과 동일)
   - `public abstract class MeasurementBase : ParamBase`
   - 생성자: `public MeasurementBase(object owner) : base(owner) { }`
   - 공통 프로퍼티 (per D-14):
     - `[Category("Measurement|Reference")] public string DatumRef { get; set; } = "";` — 빈 문자열 = 무보정 (D-10 호환)
     - `[Category("Measurement|Tolerance")] public double NominalValue { get; set; }`
     - `[Category("Measurement|Tolerance")] public double TolerancePlus { get; set; }`
     - `[Category("Measurement|Tolerance")] public double ToleranceMinus { get; set; }`
     - `[Browsable(false)] public double LastMeasuredValue { get; set; }`
     - `[Browsable(false)] public bool LastJudgement { get; set; }` — true=OK
     - `[Category("Measurement|Info")] public string MeasurementName { get; set; } = "";`
     - `[Browsable(false)] public string TypeName { get; }` — abstract 또는 virtual로 각 파생 클래스의 factory 키 반환
   - 추상 메서드: `public abstract bool TryExecute(HImage image, HTuple datumTransform, double pixelResolution, out double resultMm, out string error);`
     - HTuple 사용 (HomMat2D가 아닌 HTuple — 기존 DatumFindingService 패턴 호환, Pitfall 7 참고)
   - 공차 판정 메서드: `public bool EvaluateJudgement(double valueMm)` — LastMeasuredValue = valueMm, lower = NominalValue + ToleranceMinus (음수), upper = NominalValue + TolerancePlus, LastJudgement = (valueMm >= lower && valueMm <= upper), return LastJudgement
   - 결과 초기화: `public void ClearResult()` — LastMeasuredValue = 0, LastJudgement = false
   - //260410 hbk 주석 필수

2. **VisionAlgorithmService.cs** 생성 (WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs):
   - namespace: `ReringProject.Halcon.Algorithms`
   - `public class VisionAlgorithmService` (per D-18)
   - 모든 public 메서드는 `try { ... } catch { return false; }` 패턴 (Halcon 예외 처리 컨벤션)
   - 메서드:
     - `public bool TryFitLine(HImage image, double row, double col, double phi, double length1, double length2, HTuple datumTransform, int sampleCount, int trimCount, double sigma, string polarity, out double row1, out double col1, out double row2, out double col2, out double angle, out string error)`
       — ROI에서 에지 점 검출 후 FitLineContourXld. 기존 FAIEdgeMeasurementService 패턴 참고. datumTransform으로 ROI 좌표를 AffineTransPoint2d 변환 후 사용.
     - `public bool TryFindCircle(HImage image, double row, double col, double radius, HTuple datumTransform, double sigma, string polarity, out double centerRow, out double centerCol, out double foundRadius, out string error)`
       — 원형 ROI에서 에지 검출 + FitCircleContourXld
     - `public static double DistancePointToLine(double pRow, double pCol, double lRow1, double lCol1, double lRow2, double lCol2)`
       — 점-선 거리: |cross product| / line_length. 순수 수학 계산 (Halcon에 DistancePl 없음).
     - `public static double DistancePointToPoint(double row1, double col1, double row2, double col2)`
       — 유클리드 거리: Math.Sqrt((r2-r1)^2 + (c2-c1)^2)
     - `public static double AngleLineLine(double row1a, double col1a, double row1b, double col1b, double row2a, double col2a, double row2b, double col2b)`
       — 두 직선 각도(라디안): Math.Atan2 기반 차이. 또는 HOperatorSet.AngleLl 사용.
     - `public static bool IntersectLines(double row1a, double col1a, double row1b, double col1b, double row2a, double col2a, double row2b, double col2b, out double intRow, out double intCol)`
       — HOperatorSet.IntersectionLl 사용 (DatumFindingService 기존 패턴)
     - `public static void AffineTransformPoint(HTuple homMat2D, double row, double col, out double transRow, out double transCol)`
       — HOperatorSet.AffineTransPoint2d 래핑 (기존 FAIEdgeMeasurementService 패턴)
   - //260410 hbk 주석 필수

3. **MeasurementFactory.cs** 생성 (WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs):
   - namespace: `ReringProject.Sequence`
   - `public static class MeasurementFactory` (per D-17)
   - `public static MeasurementBase Create(string typeName, object owner)` — switch 문 (C# 7.2, switch expression 불가):
     - "EdgePairDistance" → new EdgePairDistanceMeasurement(owner)
     - "PointToLineDistance" → new PointToLineDistanceMeasurement(owner)
     - "PointToPointDistance" → new PointToPointDistanceMeasurement(owner)
     - "LineToLineAngle" → new LineToLineAngleMeasurement(owner)
     - "CircleDiameter" → new CircleDiameterMeasurement(owner)
     - "LineToLineDistance" → new LineToLineDistanceMeasurement(owner)
     - default → null
   - `public static string[] GetTypeNames()` — 등록된 6종 타입 이름 배열 반환 (UI ComboBox용)
  </action>
  <verify>
    <automated>msbuild C:/Info/Project/DataMeasurement/WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - MeasurementBase.cs 파일에 `public abstract class MeasurementBase : ParamBase` 존재
    - MeasurementBase.cs에 `public abstract bool TryExecute(HImage image, HTuple datumTransform, double pixelResolution, out double resultMm, out string error)` 존재
    - MeasurementBase.cs에 `public bool EvaluateJudgement(double valueMm)` 존재
    - MeasurementBase.cs에 `public string DatumRef` 존재
    - VisionAlgorithmService.cs에 `public bool TryFitLine(` 존재
    - VisionAlgorithmService.cs에 `public bool TryFindCircle(` 존재
    - VisionAlgorithmService.cs에 `public static double DistancePointToLine(` 존재
    - VisionAlgorithmService.cs에 `public static double DistancePointToPoint(` 존재
    - VisionAlgorithmService.cs에 `public static double AngleLineLine(` 존재
    - VisionAlgorithmService.cs에 `public static bool IntersectLines(` 존재
    - MeasurementFactory.cs에 `public static MeasurementBase Create(string typeName, object owner)` 존재
    - MeasurementFactory.cs에 case "EdgePairDistance", "PointToLineDistance", "PointToPointDistance", "LineToLineAngle", "CircleDiameter", "LineToLineDistance" 6개 case 존재
    - 모든 파일에 //260410 hbk 주석 존재
  </acceptance_criteria>
  <done>MeasurementBase 추상 클래스, VisionAlgorithmService 7개 빌딩 블록, MeasurementFactory 6종 타입 매핑이 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 2: 6종 파생 Measurement 클래스 + FAIConfig.Measurements 추가</name>
  <files>
    WPF_Example/Custom/Sequence/Inspection/Measurements/EdgePairDistanceMeasurement.cs
    WPF_Example/Custom/Sequence/Inspection/Measurements/PointToLineDistanceMeasurement.cs
    WPF_Example/Custom/Sequence/Inspection/Measurements/PointToPointDistanceMeasurement.cs
    WPF_Example/Custom/Sequence/Inspection/Measurements/LineToLineAngleMeasurement.cs
    WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs
    WPF_Example/Custom/Sequence/Inspection/Measurements/LineToLineDistanceMeasurement.cs
    WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
  </files>
  <read_first>
    WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
    WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
    WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs
    WPF_Example/Halcon/Models/FAIEdgeMeasurementResult.cs
    WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
    WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs
  </read_first>
  <action>
1. **Measurements/** 디렉토리 생성 (WPF_Example/Custom/Sequence/Inspection/Measurements/)

2. **EdgePairDistanceMeasurement.cs** (per D-15, D-19):
   - `public class EdgePairDistanceMeasurement : MeasurementBase`
   - TypeName override → "EdgePairDistance"
   - ROI 프로퍼티: `ROI_Row`, `ROI_Col`, `ROI_Phi`, `ROI_Length1`, `ROI_Length2` (double) — 기존 FAIConfig ROI 필드와 동일 구조
   - 에지 파라미터: `EdgeThreshold` (int, default 10), `Sigma` (double, default 1.0), `EdgeDirection` (string, "LtoR"), `EdgeSelection` (string, "First"), `EdgeSampleCount` (int, 20), `EdgeTrimCount` (int, 10), `EdgePolarity` (string, "DarkToLight")
   - `PixelResolutionX` (double, 1.0), `PixelResolutionY` (double, 1.0)
   - TryExecute 구현: FAIEdgeMeasurementService를 래핑 (D-19). 내부에서 임시 FAIConfig 인스턴스를 만들어 ROI/에지 파라미터를 복사, `new FAIEdgeMeasurementService().TryMeasure(image, tempFai, datumTransform, out result)` 호출. resultMm = result.DistanceMm. 기존 FAIEdgeMeasurementService 삭제하지 않음.
   - [Category("EdgePair|ROI")] 등 PropertyGrid 카테고리 적용

3. **PointToLineDistanceMeasurement.cs** (per D-15):
   - `public class PointToLineDistanceMeasurement : MeasurementBase`
   - TypeName → "PointToLineDistance"
   - 프로퍼티: PointRoi (Row, Col, Phi, Length1, Length2 — double 5개) + LineRoi (Row, Col, Phi, Length1, Length2 — double 5개)
   - 에지 파라미터: SampleCount, TrimCount, Sigma, Polarity (Point/Line 각각 또는 공용)
   - TryExecute: (1) VisionAlgorithmService.AffineTransformPoint로 ROI 보정, (2) PointRoi에서 에지 점 검출(MeasurePos), (3) LineRoi에서 TryFitLine으로 직선 피팅, (4) VisionAlgorithmService.DistancePointToLine(에지 점, 직선), (5) 픽셀→mm 변환 (resultMm = distPixel * pixelResolution)

4. **PointToPointDistanceMeasurement.cs** (per D-15):
   - TypeName → "PointToPointDistance"
   - 프로퍼티: Point1Roi + Point2Roi (각 Row, Col, Phi, Length1, Length2)
   - TryExecute: 각 ROI에서 에지 점 검출 → DistancePointToPoint → mm 변환

5. **LineToLineAngleMeasurement.cs** (per D-15):
   - TypeName → "LineToLineAngle"
   - 프로퍼티: Line1Roi + Line2Roi (각 Row, Col, Phi, Length1, Length2)
   - TryExecute: 각 ROI에서 TryFitLine → AngleLineLine → 결과는 degree (라디안→도 변환)
   - 주의: 각도 측정이므로 pixelResolution 미적용 — 결과 단위는 degree

6. **CircleDiameterMeasurement.cs** (per D-15):
   - TypeName → "CircleDiameter"
   - 프로퍼티: CircleRoi_Row, CircleRoi_Col, CircleRoi_Radius (원형 탐색 영역)
   - TryExecute: TryFindCircle → 직경 = radius * 2 * pixelResolution

7. **LineToLineDistanceMeasurement.cs** (per D-15):
   - TypeName → "LineToLineDistance"
   - 프로퍼티: Line1Roi + Line2Roi (각 Row, Col, Phi, Length1, Length2)
   - TryExecute: 각 ROI에서 TryFitLine → Line1 중점에서 Line2까지 DistancePointToLine → mm 변환

8. **FAIConfig.cs 수정** (per D-20):
   - `[Browsable(false)] public List<MeasurementBase> Measurements { get; private set; } = new List<MeasurementBase>();` 추가
   - `public MeasurementBase AddMeasurement(string typeName)` 메서드: MeasurementFactory.Create(typeName, this) 호출 → Measurements.Add → 반환
   - `public bool RemoveMeasurement(int index)` 메서드
   - `public void ClearMeasurements()` 메서드
   - 기존 에지 측정 필드(EdgeDirection, EdgeSelection 등)는 유지 — 하위 호환. EdgePairDistanceMeasurement로 래핑할 때 이 필드에서 복사 가능.
   - //260410 hbk 주석 필수

9. 모든 .cs 파일을 **DatumMeasurement.csproj**에 `<Compile Include=...>` 추가 (packages.config 스타일 프로젝트이므로 수동 등록 필요)
  </action>
  <verify>
    <automated>msbuild C:/Info/Project/DataMeasurement/WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - EdgePairDistanceMeasurement.cs에 `public class EdgePairDistanceMeasurement : MeasurementBase` 존재
    - EdgePairDistanceMeasurement.cs에 `FAIEdgeMeasurementService` 참조 존재 (래핑 확인)
    - PointToLineDistanceMeasurement.cs에 `DistancePointToLine` 호출 존재
    - PointToPointDistanceMeasurement.cs에 `DistancePointToPoint` 호출 존재
    - LineToLineAngleMeasurement.cs에 `AngleLineLine` 호출 존재
    - CircleDiameterMeasurement.cs에 `TryFindCircle` 호출 존재
    - LineToLineDistanceMeasurement.cs에 `DistancePointToLine` 호출 존재
    - 6개 파생 클래스 모두 `override bool TryExecute(` 존재
    - FAIConfig.cs에 `public List<MeasurementBase> Measurements` 존재
    - FAIConfig.cs에 `public MeasurementBase AddMeasurement(string typeName)` 존재
    - DatumMeasurement.csproj에 6개 Measurements/*.cs Compile Include 존재
    - 빌드 성공 (exit code 0)
  </acceptance_criteria>
  <done>6종 Measurement 파생 클래스가 각각 TryExecute를 구현하고, FAIConfig가 List&lt;MeasurementBase&gt;를 소유하며, 빌드 성공</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INI 레시피 파일 → MeasurementFactory | 외부 INI 파일의 Type= 값이 factory에 전달됨 |
| Halcon 이미지 → VisionAlgorithmService | 카메라/파일에서 온 HImage가 알고리즘에 전달됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T (Tampering) | MeasurementFactory.Create | mitigate | switch default → return null. 호출측에서 null 체크 후 로그 + skip. 미등록 타입명은 무시. |
| T-06-02 | D (Denial of Service) | VisionAlgorithmService.TryFitLine | mitigate | 모든 Halcon 호출을 try/catch로 감싸고 false 반환. HImage null 체크를 메서드 진입점에서 수행. |
| T-06-03 | I (Information Disclosure) | MeasurementBase.LastMeasuredValue | accept | 로컬 데스크톱 앱, 네트워크 노출 없음. 측정 결과는 INI에 저장되지 않음 (Browsable(false), 휘발성). |
</threat_model>

<verification>
- 빌드 성공: `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64`
- MeasurementBase 추상 클래스에 DatumRef, NominalValue, TolerancePlus, ToleranceMinus, TryExecute, EvaluateJudgement 존재
- 6종 파생 클래스 모두 TryExecute override 구현
- VisionAlgorithmService에 7개 빌딩 블록 메서드 존재
- MeasurementFactory에 6개 타입 매핑 존재
- FAIConfig에 List&lt;MeasurementBase&gt; Measurements 존재
</verification>

<success_criteria>
- MeasurementBase + 6종 파생 클래스 + VisionAlgorithmService + MeasurementFactory가 컴파일 성공
- FAIConfig.AddMeasurement("EdgePairDistance")가 EdgePairDistanceMeasurement 인스턴스를 반환
- 모든 파생 클래스가 자기 알고리즘에 필요한 ROI 프로퍼티만 소유
- EdgePairDistanceMeasurement가 기존 FAIEdgeMeasurementService를 래핑하여 재사용
</success_criteria>

<output>
After completion, create `.planning/phases/06-rapid-city/06-01-SUMMARY.md`
</output>
