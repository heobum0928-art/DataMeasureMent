---
phase: 06-rapid-city
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: true
requirements: [RC-01, RC-02, RC-04]
must_haves:
  truths:
    - "InspectionSequence가 List<DatumConfig> DatumConfigs를 소유한다"
    - "InspectionSequence에 DisplayName 프로퍼티가 있어 사용자 편집 가능하다"
    - "DatumConfig에 DatumName과 ImageSourceMode 필드가 존재한다"
    - "ShotConfig에서 DatumConfig 단일 소유가 제거되었다"
    - "ShotConfig에 Ring/Back/Coax/Side 조명 필드 8개가 추가되었다"
    - "InspectionSequence.TryRunDatumPhase가 Multi-Datum을 순회하여 transform dictionary를 생성한다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "Fixture 역할 — DisplayName, DatumConfigs, TryRunDatumPhase, TryGetDatumTransform"
      contains: "List<DatumConfig> DatumConfigs"
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "DatumName + ImageSourceMode 필드 추가"
      contains: "DatumName"
    - path: "WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs"
      provides: "조명 필드 8개 + Datum 소유 제거"
      contains: "RingLight_Enabled"
  key_links:
    - from: "InspectionSequence"
      to: "DatumConfig"
      via: "List<DatumConfig> DatumConfigs"
      pattern: "DatumConfigs"
    - from: "InspectionSequence"
      to: "DatumFindingService"
      via: "TryRunDatumPhase"
      pattern: "TryFindDatum"
---

<objective>
Datum을 ShotConfig에서 InspectionSequence(Fixture) 레벨로 승격하고, Multi-Datum 지원(List<DatumConfig>), DatumConfig에 DatumName/ImageSourceMode 추가, ShotConfig에 조명 필드 8개 추가, ShotConfig.Datum 단일 소유 제거를 수행한다.

Purpose: Rapid City 제품은 한 Fixture에 Datum B, C 등 여러 기준좌표를 사용한다. Datum을 Shot이 아닌 Sequence(Fixture) 레벨에서 관리하여 같은 Fixture 내 모든 Shot이 공유하도록 한다 (D-01~D-13).
Output: InspectionSequence 확장, DatumConfig 확장, ShotConfig 수정
</objective>

<execution_context>
@C:/Users/tech/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-rapid-city/06-CONTEXT.md
@.planning/phases/06-rapid-city/06-RESEARCH.md

<interfaces>
<!-- Key types the executor needs. -->

From WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (현재):
```csharp
public class InspectionSequence : SequenceBase {
    // 기존: 카메라, 조명, 알고리즘 인덱스 등
    public InspectionSequence(ESequence seqID, string name, int algIndex, string defaultCamera, string defaultLight) : base(seqID, name) { ... }
    public override void OnCreate() { ... }
    public override void OnLoad() { ... }
    public override void OnRelease() { ... }
}
// SequenceBase.Name { get; private set; } — 편집 불가
```

From WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs (현재):
```csharp
public class ShotConfig : CameraSlaveParam {
    public double ZPosition { get; set; }
    public int DelayMs { get; set; }
    public string SimulImagePath { get; set; } = "";
    public List<FAIConfig> FAIList { get; private set; } = new List<FAIConfig>();
    public string ShotName { get; set; }
    public DatumConfig Datum { get; set; }  // ← Phase 6에서 제거 대상
    // ... SetImage, GetImage, ClearImage, AddFAI, RemoveFAI, ClearAllResults
}
```

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (현재):
```csharp
public class DatumConfig : ParamBase {
    // Line1/Line2 ROI 필드, IsConfigured, RefOriginRow/Col/AngleRad
    // CurrentTransform(HTuple), LastFindSucceeded(bool)
    public DatumConfig(object owner) : base(owner) { }
}
```

From WPF_Example/Halcon/Algorithms/DatumFindingService.cs:
```csharp
public class DatumFindingService {
    public bool TryFindDatum(HImage image, DatumConfig config, out HTuple transform, out string error);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DatumConfig 확장 + InspectionSequence Fixture 구조 + ShotConfig Datum 제거</name>
  <files>
    WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
    WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
    WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
  </files>
  <read_first>
    WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
    WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
    WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
    WPF_Example/Halcon/Algorithms/DatumFindingService.cs
    WPF_Example/Sequence/Sequence/SequenceBase.cs
    WPF_Example/Sequence/Param/CameraSlaveParam.cs
  </read_first>
  <action>
1. **DatumConfig.cs 수정** (per D-05, D-06, D-07, D-08):
   - DatumConfig 클래스 자체는 최소 수정 (D-05). 새 프로퍼티만 추가:
     - `[Category("Datum|Identity")] public string DatumName { get; set; } = "Datum_1";` (D-06)
     - `[Category("Datum|ImageSource")] public string ImageSourceMode { get; set; } = "Dedicated";` (D-07, D-08. string 사용 — ParamBase INI 자동 직렬화 호환. 값: "Dedicated" 또는 "ReuseFromShot")
     - `[Category("Datum|ImageSource")] public string ReuseFromShotName { get; set; } = "";` (ReuseFromShot 모드 시 Shot 이름)
   - 기존 필드(Line1_ROI_*, Line2_ROI_*, IsConfigured, RefOriginRow/Col, RefAngleRad, CurrentTransform, LastFindSucceeded) 전부 유지
   - //260410 hbk Phase 6: DatumName + ImageSourceMode (D-06, D-07) 주석 추가

2. **InspectionSequence.cs 수정** (per D-01, D-02, D-04):
   - 추가할 필드/프로퍼티:
     - `//260410 hbk Phase 6: Fixture DisplayName (D-01)`
     - `public string DisplayName { get; set; } = "";` — 사용자 편집 가능 이름. 빈 문자열이면 `Name`(시스템 이름)을 대신 표시.
     - `//260410 hbk Phase 6: Multi-Datum (D-04)`
     - `[Browsable(false)] public List<DatumConfig> DatumConfigs { get; private set; } = new List<DatumConfig>();`
     - `private Dictionary<string, HTuple> _datumTransforms = new Dictionary<string, HTuple>();`
   - 추가할 메서드:
     - `public DatumConfig AddDatum(string name = null)` — 이름 기본값 "Datum_{Count+1}", new DatumConfig(this) { DatumName = name }, DatumConfigs.Add, return
     - `public bool RemoveDatum(int index)` — 범위 체크, DatumConfigs.RemoveAt(index)
     - `public bool TryRunDatumPhase(HImage image, out string error)` (per D-09) — _datumTransforms.Clear(), DatumFindingService 인스턴스 생성, foreach DatumConfig: TryFindDatum(image, datum, out transform, out datumError) → 실패시 error 설정 + return false → 성공시 _datumTransforms[datum.DatumName] = transform. 모두 성공 return true.
     - `public bool TryGetDatumTransform(string datumRef, out HTuple transform)` (per D-10) — datumRef가 null 또는 빈 문자열이면 HomMat2dIdentity(out transform) + return true. 아니면 _datumTransforms.TryGetValue(datumRef, out transform).
     - `public string GetDisplayName()` — return string.IsNullOrEmpty(DisplayName) ? Name : DisplayName;
   - using 추가: `using HalconDotNet;` (HTuple, HImage용), `using System.Collections.Generic;` (List, Dictionary)
   - DatumFindingService는 `using ReringProject.Halcon.Algorithms;`로 참조

3. **ShotConfig.cs 수정** (per D-04, D-11, D-12, D-13, D-25):
   - **DatumConfig 소유 제거:**
     - `public DatumConfig Datum { get; set; }` 프로퍼티 제거 (또는 `[Obsolete]`로 마킹 후 null 반환)
     - 생성자에서 `Datum = new DatumConfig(this);` 라인 제거
     - `ClearAllResults()`에서 `if (Datum != null) { ... }` 블록 제거
   - **조명 필드 추가** (per D-11):
     - `//260410 hbk Phase 6: Multi-Light (D-11)`
     - `[Category("Light|Ring")] public bool RingLight_Enabled { get; set; }` (기본 false)
     - `[Category("Light|Ring")] public int RingLight_Brightness { get; set; }` (기본 0, 범위 0~100)
     - `[Category("Light|Back")] public bool BackLight_Enabled { get; set; }`
     - `[Category("Light|Back")] public int BackLight_Brightness { get; set; }`
     - `[Category("Light|Coax")] public bool CoaxLight_Enabled { get; set; }`
     - `[Category("Light|Coax")] public int CoaxLight_Brightness { get; set; }`
     - `[Category("Light|Side")] public bool SideLight_Enabled { get; set; }`
     - `[Category("Light|Side")] public int SideLight_Brightness { get; set; }`
   - ParamBase.Save/Load가 bool(Boolean)과 int(Int32)을 자동 직렬화하므로 조명 필드는 추가 직렬화 코드 불필요 (Pitfall 5 참고)
   - //260410 hbk 주석 필수
  </action>
  <verify>
    <automated>msbuild C:/Info/Project/DataMeasurement/WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - DatumConfig.cs에 `public string DatumName { get; set; } = "Datum_1"` 존재
    - DatumConfig.cs에 `public string ImageSourceMode { get; set; } = "Dedicated"` 존재
    - InspectionSequence.cs에 `public string DisplayName { get; set; }` 존재
    - InspectionSequence.cs에 `public List<DatumConfig> DatumConfigs` 존재
    - InspectionSequence.cs에 `public bool TryRunDatumPhase(HImage image, out string error)` 존재
    - InspectionSequence.cs에 `public bool TryGetDatumTransform(string datumRef, out HTuple transform)` 존재
    - InspectionSequence.cs에 `DatumFindingService` 참조 존재
    - ShotConfig.cs에 `DatumConfig Datum` 프로퍼티가 제거되었거나 Obsolete 마킹됨
    - ShotConfig.cs에 `public bool RingLight_Enabled` 존재
    - ShotConfig.cs에 `public int RingLight_Brightness` 존재
    - ShotConfig.cs에 `BackLight_Enabled`, `CoaxLight_Enabled`, `SideLight_Enabled` 존재
    - 모든 파일에 //260410 hbk 주석 존재
  </acceptance_criteria>
  <done>InspectionSequence가 Fixture 역할(DisplayName, Multi-Datum), ShotConfig에서 Datum 제거 + 조명 8필드 추가, DatumConfig에 DatumName/ImageSourceMode 추가 완료. 빌드 성공.</done>
</task>

<task type="auto">
  <name>Task 2: ShotConfig.Datum 제거에 따른 컴파일 에러 수정</name>
  <files>
    WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
    WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs
    WPF_Example/UI/ControlItem/InspectionListViewModel.cs
  </files>
  <read_first>
    WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
    WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs
    WPF_Example/UI/ControlItem/InspectionListViewModel.cs
    WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
    WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  </read_first>
  <action>
ShotConfig.Datum 제거 후 해당 필드를 참조하는 모든 코드에서 컴파일 에러가 발생한다. RESEARCH.md의 "ShotConfig.Datum 제거 영향 범위" 참고. 각 파일의 에러를 수정한다:

1. **Action_FAIMeasurement.cs** 수정:
   - 기존: `shot.Datum`으로 DatumConfig 접근 → Datum 찾기 → transform
   - 변경: Datum 관련 코드를 **주석 처리 또는 스텁으로 교체**. 실제 Datum 실행 흐름 재설계는 Plan 03에서 수행.
   - `shot.Datum` 참조 → 제거. Datum 관련 EStep이 있으면 해당 블록을 `// TODO: Phase 6 Plan 03에서 InspectionSequence.TryRunDatumPhase로 재설계` 주석으로 대체.
   - Datum transform 없이 측정이 돌아가도록 identity transform을 기본값으로 사용: `HTuple identityTransform; HOperatorSet.HomMat2dIdentity(out identityTransform);` 후 기존 transform 변수에 대입.
   - ClearAllResults에서 Datum 초기화 호출이 있으면 제거.

2. **InspectionRecipeManager.cs** 수정:
   - `AddShot()` 메서드: `shot.Datum = new DatumConfig(shot)` 라인 제거
   - `Save()` 메서드: `SHOT_{s}_DATUM` 섹션 저장 코드 → 주석 처리 또는 제거. `// TODO: Phase 6 Plan 03에서 새 포맷으로 재설계`
   - `Load()` 메서드: `SHOT_{s}_DATUM` 섹션 로드 코드 → 주석 처리 또는 제거.
   - Datum 관련 코드만 제거하고, Shot/FAI 관련 기존 Save/Load는 유지 (Plan 03에서 전면 재설계)

3. **InspectionListViewModel.cs** 수정:
   - `CreateSequenceNode()` 또는 유사 메서드에서 Action(Shot) 노드 하위에 Datum 노드를 추가하는 코드 → 주석 처리.
   - `// TODO: Phase 6 Plan 04에서 Sequence 레벨 Datum 노드로 재설계`
   - 기존 Datum 노드 생성 코드가 `shot.Datum`을 참조하므로 컴파일 에러 해소를 위해 제거 필수.

4. 다른 파일에서 `shot.Datum` 또는 `ShotConfig.Datum`을 참조하는 곳이 있다면 동일하게 수정. grep으로 `\.Datum` 참조를 확인: `grep -rn "\.Datum" WPF_Example/ --include="*.cs" | grep -v DatumConfig | grep -v DatumFinding | grep -v DatumName`

- //260410 hbk 주석 필수
  </action>
  <verify>
    <automated>msbuild C:/Info/Project/DataMeasurement/WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Action_FAIMeasurement.cs에 `shot.Datum` 또는 `ShotParam.Datum` 참조가 없음 (grep 확인)
    - InspectionRecipeManager.cs에 `SHOT_.*_DATUM` 섹션 저장/로드 코드가 주석 처리 또는 제거됨
    - InspectionListViewModel.cs에 `shot.Datum` 참조가 없음
    - 빌드 성공 (exit code 0)
    - 모든 수정 파일에 //260410 hbk 주석 존재
  </acceptance_criteria>
  <done>ShotConfig.Datum 제거에 따른 모든 컴파일 에러가 해소되고, Datum 관련 기능은 identity transform fallback으로 임시 동작하며, 빌드 성공</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INI 레시피 → DatumConfig 필드 | 외부 INI 파일에서 DatumName, ImageSourceMode 값 로드 |
| DatumFindingService → HTuple transform | Datum 찾기 결과가 Dictionary에 캐시됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | T (Tampering) | DatumConfig.ImageSourceMode | mitigate | string 값 검증: "Dedicated" 또는 "ReuseFromShot" 외 값은 기본값 "Dedicated"로 fallback. Load 시 검증 로직 추가. |
| T-06-05 | D (Denial of Service) | InspectionSequence.TryRunDatumPhase | mitigate | DatumConfigs가 비어있으면 즉시 true 반환 (D-10 호환). 각 DatumConfig TryFindDatum 실패 시 즉시 false + 명확한 에러 메시지. |
| T-06-06 | T (Tampering) | ShotConfig 조명 Brightness | accept | 0~100 범위 검증은 PropertyGrid UI에서 수행. INI에서 비정상 값 로드 시 ParamBase가 기본값 사용. 로컬 앱이므로 수용. |
</threat_model>

<verification>
- 빌드 성공: `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64`
- InspectionSequence에 DisplayName, DatumConfigs, TryRunDatumPhase, TryGetDatumTransform 존재
- DatumConfig에 DatumName, ImageSourceMode 존재
- ShotConfig에 Datum 프로퍼티 제거됨, 조명 8필드 존재
- `grep -rn "shot\.Datum\b\|ShotParam\.Datum\b" WPF_Example/ --include="*.cs"` 결과 없음
</verification>

<success_criteria>
- InspectionSequence가 Fixture 역할 수행: DisplayName + Multi-Datum (List&lt;DatumConfig&gt;)
- ShotConfig에서 단일 DatumConfig 소유 완전 제거
- ShotConfig에 Ring/Back/Coax/Side 조명 bool+int 8필드 추가 + ParamBase 자동 INI 직렬화
- 기존 Datum 참조 코드 모두 수정되어 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/06-rapid-city/06-02-SUMMARY.md`
</output>
