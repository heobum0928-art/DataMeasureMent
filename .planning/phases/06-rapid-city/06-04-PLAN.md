---
phase: 06-rapid-city
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - WPF_Example/UI/ControlItem/Node.cs
  - WPF_Example/UI/ControlItem/InspectionListViewModel.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  - WPF_Example/UI/ViewModel/MeasurementResultRow.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/UI/ViewModel/InspectionViewModel.cs
autonomous: false
requirements: [RC-06]
must_haves:
  truths:
    - "UI 트리에 Sequence > Datum(형제) + Action(Shot) > FAI > Measurement 계층이 표시된다"
    - "Datum 노드가 Sequence 직접 자식이며 Action과 같은 레벨 형제이다"
    - "Measurement 노드 선택 시 PropertyGrid에 파생 클래스의 프로퍼티가 표시된다"
    - "결과 테이블이 Measurement 단위로 한 행씩 표시된다 (FAI별 1행 아닌 Measurement별 1행)"
    - "Sequence 노드에 DisplayName이 표시된다"
  artifacts:
    - path: "WPF_Example/UI/ControlItem/Node.cs"
      provides: "ENodeType.Measurement 추가"
      contains: "Measurement"
    - path: "WPF_Example/UI/ControlItem/InspectionListViewModel.cs"
      provides: "트리 생성 — Sequence > Datum + Shot > FAI > Measurement"
      contains: "ENodeType.Measurement"
    - path: "WPF_Example/UI/ViewModel/MeasurementResultRow.cs"
      provides: "Measurement 결과 행 ViewModel"
      contains: "class MeasurementResultRow"
  key_links:
    - from: "InspectionListViewModel"
      to: "InspectionSequence.DatumConfigs"
      via: "Sequence 노드 하위에 Datum 노드 생성"
      pattern: "DatumConfigs"
    - from: "InspectionListViewModel"
      to: "FAIConfig.Measurements"
      via: "FAI 노드 하위에 Measurement 노드 생성"
      pattern: "fai.Measurements"
    - from: "MainView"
      to: "MeasurementResultRow"
      via: "결과 테이블 DataGrid"
      pattern: "MeasurementResultRow"
---

<objective>
UI 트리를 Phase 6 Fixture-Datum-Shot-FAI-Measurement 계층으로 재구성하고, 결과 테이블을 Measurement 단위 행으로 변경한다. Datum 노드를 Sequence 직접 자식으로 이동하고, Measurement 노드를 FAI 하위에 추가한다.

Purpose: 사용자가 75개+ FAI와 다양한 Measurement 타입을 트리에서 탐색/편집하고, 측정 결과를 Measurement 단위로 확인할 수 있다 (D-21, D-24, D-25, RC-06).
Output: 트리 재구성, MeasurementResultRow, 결과 테이블 변경
</objective>

<execution_context>
@C:/Users/tech/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-rapid-city/06-CONTEXT.md
@.planning/phases/06-rapid-city/06-RESEARCH.md
@.planning/phases/06-rapid-city/06-01-SUMMARY.md
@.planning/phases/06-rapid-city/06-02-SUMMARY.md

<interfaces>
<!-- Key types from Plan 01 and Plan 02. -->

From InspectionSequence.cs (Plan 02):
```csharp
public class InspectionSequence : SequenceBase {
    public string DisplayName { get; set; }
    public List<DatumConfig> DatumConfigs { get; private set; }
    public string GetDisplayName(); // DisplayName이 비어있으면 Name 반환
}
```

From FAIConfig.cs (Plan 01 수정):
```csharp
public class FAIConfig : ParamBase {
    public List<MeasurementBase> Measurements { get; private set; }
    // 기존: FAIName, ROI 필드 등
}
```

From MeasurementBase.cs (Plan 01):
```csharp
public abstract class MeasurementBase : ParamBase {
    public string DatumRef { get; set; }
    public string MeasurementName { get; set; }
    public double NominalValue, TolerancePlus, ToleranceMinus;
    public double LastMeasuredValue;
    public bool LastJudgement;
    public abstract string TypeName { get; }
}
```

From Node.cs (현재):
```csharp
public enum ENodeType { Recipe, Sequence, Action, SubSequence, FAI, Datum }
public class Node { ENodeType NodeType; object ParamData; string Name; ... }
public class CompositeNode : Node { List<Node> Children; }
```

From InspectionListViewModel.cs (현재):
```csharp
// CreateSequenceNode — Sequence > Action(Shot) > FAI 트리 생성
// Phase 4에서 Action 하위에 Datum 노드 추가됨 (Plan 02에서 주석 처리됨)
```

From FAIResultRow.cs (현재):
```csharp
// FAIConfig 래핑 — FAI별 1행: FAIName, MeasuredValue, IsPass 등
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ENodeType.Measurement + 트리 재구성 (Sequence > Datum + Shot > FAI > Measurement)</name>
  <files>
    WPF_Example/UI/ControlItem/Node.cs
    WPF_Example/UI/ControlItem/InspectionListViewModel.cs
    WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  </files>
  <read_first>
    WPF_Example/UI/ControlItem/Node.cs
    WPF_Example/UI/ControlItem/InspectionListViewModel.cs
    WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
    WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
    WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
    WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
  </read_first>
  <action>
1. **Node.cs 수정:**
   - `ENodeType` enum에 `Measurement` 값 추가: `Measurement = 6` (또는 기존 값 뒤에 추가)
   - `Node.ImageSource` switch에 `case ENodeType.Measurement:` 추가 — 적절한 아이콘 문자열 반환 (기존 패턴 참고)
   - //260410 hbk 주석

2. **InspectionListViewModel.cs — CreateSequenceNode 재설계** (per D-24, D-25):
   - Sequence 노드 생성 시:
     - `seqNode.Name` = `(seq as InspectionSequence)?.GetDisplayName() ?? seq.Name` (D-01)
   - **Datum 노드를 Sequence 직접 자식으로 추가** (D-25):
     ```
     if (seq is InspectionSequence inspSeq) {
         foreach (var datum in inspSeq.DatumConfigs) {
             var datumNode = new CompositeNode {
                 Name = datum.DatumName ?? "Datum",
                 NodeType = ENodeType.Datum,
                 ParamData = datum,
                 SequenceName = seq.Name,
                 SequenceID = seq.ID
             };
             seqNode.Children.Add(datumNode);
         }
     }
     ```
   - Action(Shot) 노드는 Datum 노드 뒤에 추가 (기존 코드 위치 유지)
   - **FAI 노드 하위에 Measurement 노드 추가:**
     ```
     foreach (var faiConfig in shot.FAIList) {
         var faiNode = new CompositeNode {
             Name = faiConfig.FAIName ?? "FAI",
             NodeType = ENodeType.FAI,
             ParamData = faiConfig,
             ...
         };
         // Measurement 하위 노드 (D-24 트리 구조)
         foreach (var meas in faiConfig.Measurements) {
             var measNode = new Node {
                 Name = meas.MeasurementName ?? meas.TypeName ?? "Measurement",
                 NodeType = ENodeType.Measurement,
                 ParamData = meas,
                 SequenceName = seq.Name,
                 SequenceID = seq.ID,
                 ActionID = action.ActionID
             };
             faiNode.Children.Add(measNode);
         }
         actionNode.Children.Add(faiNode);
     }
     ```
   - Phase 4에서 Action 하위에 Datum 노드를 추가하던 코드가 Plan 02에서 주석 처리됨 — 해당 주석 제거하고 위의 Sequence 레벨 코드로 대체

3. **InspectionListView.xaml.cs — Measurement 노드 선택 처리:**
   - TreeView SelectionChanged 이벤트 핸들러에서 `ENodeType.Measurement` case 추가:
     - `selectedNode.ParamData`가 `MeasurementBase`일 때 — PropertyGrid에 해당 MeasurementBase 인스턴스 바인딩
     - 기존 FAI 선택과 유사한 패턴. `OnMeasurementSelected` 또는 기존 `OnNodeSelected` 패턴 확장.
   - Datum CRUD 버튼 연동 (AddDatum, RemoveDatum) — InspectionSequence.AddDatum/RemoveDatum 호출 — 트리 갱신
   - Measurement CRUD 버튼 연동 (AddMeasurement, RemoveMeasurement) — FAIConfig.AddMeasurement/RemoveMeasurement 호출 — 트리 갱신
   - //260410 hbk 주석

4. **.csproj에 MeasurementResultRow.cs Compile Include 추가** (Task 2에서 생성하는 파일)
  </action>
  <verify>
    <automated>msbuild C:/Info/Project/DataMeasurement/WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Node.cs에 `Measurement` enum 값 존재 (`grep "Measurement" WPF_Example/UI/ControlItem/Node.cs`)
    - InspectionListViewModel.cs에 `ENodeType.Measurement` 참조 존재
    - InspectionListViewModel.cs에 `DatumConfigs` 참조 존재 (Sequence 레벨 Datum 노드 생성)
    - InspectionListViewModel.cs에 `fai.*Measurements` 또는 `Measurements` 루프 존재 (Measurement 노드 생성)
    - InspectionListViewModel.cs에 `GetDisplayName` 호출 존재
    - InspectionListView.xaml.cs에 `ENodeType.Measurement` case 존재
    - 빌드 성공
  </acceptance_criteria>
  <done>UI 트리가 Sequence(DisplayName) > Datum(DatumName) + Action(Shot) > FAI > Measurement 4계층 구조를 표시</done>
</task>

<task type="auto">
  <name>Task 2: MeasurementResultRow + 결과 테이블 Measurement 단위 전환</name>
  <files>
    WPF_Example/UI/ViewModel/MeasurementResultRow.cs
    WPF_Example/UI/ContentItem/MainView.xaml.cs
    WPF_Example/UI/ViewModel/InspectionViewModel.cs
  </files>
  <read_first>
    WPF_Example/UI/ViewModel/FAIResultRow.cs
    WPF_Example/UI/ContentItem/MainView.xaml.cs
    WPF_Example/UI/ViewModel/InspectionViewModel.cs
    WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
    WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
  </read_first>
  <action>
1. **MeasurementResultRow.cs 생성** (WPF_Example/UI/ViewModel/MeasurementResultRow.cs):
   - namespace: `ReringProject.UI.ViewModel` (FAIResultRow와 동일)
   - `public class MeasurementResultRow : INotifyPropertyChanged` (per D-21)
   - 생성자: `public MeasurementResultRow(MeasurementBase measurement, string faiName)`
   - 프로퍼티:
     - `public string FAIName { get; }` — 소속 FAI 이름
     - `public string MeasurementName { get; }` — measurement.MeasurementName ?? measurement.TypeName
     - `public string TypeName { get; }` — measurement.TypeName (알고리즘 종류 표시)
     - `public string DatumRef { get; }` — measurement.DatumRef
     - `public double NominalValue { get; }` — measurement.NominalValue
     - `public double TolerancePlus { get; }` — measurement.TolerancePlus
     - `public double ToleranceMinus { get; }` — measurement.ToleranceMinus
     - `public double MeasuredValue { get { return _measurement.LastMeasuredValue; } }` — 바인딩용
     - `public bool IsPass { get { return _measurement.LastJudgement; } }` — 바인딩용
     - `public bool HasResult { get { return _measurement.LastMeasuredValue > 0; } }` — 기존 FAIResultRow.HasResult 패턴
     - `public string ResultDisplay { get; }` — 타입별 포맷: 각도면 "XX.XX deg", 거리면 "XX.XXXmm", 직경이면 "XX.XXXmm"
   - `private MeasurementBase _measurement` 필드
   - `public void Refresh()` — PropertyChanged 이벤트 발생 (MeasuredValue, IsPass, HasResult, ResultDisplay)
   - //260410 hbk 주석

2. **InspectionViewModel.cs 수정:**
   - 결과 테이블용 ObservableCollection을 `ObservableCollection<MeasurementResultRow>` 타입으로 변경 (기존 `ObservableCollection<FAIResultRow>` 교체 또는 새 컬렉션 추가)
   - 프로퍼티명: `MeasurementResults` (기존 `FAIResults`와 병행하거나 교체)
   - `OnActionSelected` 또는 `UpdateResults` 메서드에서:
     - ShotConfig.FAIList 순회 — 각 FAI의 Measurements 순회 — `new MeasurementResultRow(meas, fai.FAIName)` 생성 — MeasurementResults에 추가
   - `RefreshResults` 메서드: 각 MeasurementResultRow.Refresh() 호출 (측정 후 UI 갱신용)
   - //260410 hbk 주석

3. **MainView.xaml.cs 수정:**
   - 결과 DataGrid의 ItemsSource를 `MeasurementResults`로 변경 (기존 FAIResults에서)
   - DataGrid 컬럼 조정:
     - FAIName 컬럼 유지
     - MeasurementName 컬럼 추가
     - TypeName 컬럼 추가
     - DatumRef 컬럼 추가
     - NominalValue 컬럼 유지
     - TolerancePlus, ToleranceMinus 컬럼 유지
     - MeasuredValue 컬럼 유지
     - IsPass 컬럼 유지 (OK/NG 표시)
   - XAML 수정이 필요하면 MainView.xaml도 수정 (DataGrid 컬럼 정의가 XAML에 있을 경우)
   - //260410 hbk 주석

4. **DatumMeasurement.csproj에 MeasurementResultRow.cs Compile Include 추가**
  </action>
  <verify>
    <automated>msbuild C:/Info/Project/DataMeasurement/WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - MeasurementResultRow.cs 파일 존재
    - MeasurementResultRow.cs에 `public class MeasurementResultRow` 존재
    - MeasurementResultRow.cs에 `MeasurementBase` 참조 존재
    - MeasurementResultRow.cs에 `FAIName`, `MeasurementName`, `TypeName`, `DatumRef`, `MeasuredValue`, `IsPass` 프로퍼티 존재
    - InspectionViewModel.cs에 `MeasurementResultRow` 참조 존재
    - InspectionViewModel.cs에 `fai.Measurements` 또는 `Measurements` 루프 존재
    - MainView.xaml.cs 또는 MainView.xaml에 `MeasurementResults` 또는 `MeasurementResultRow` 참조 존재
    - 빌드 성공
  </acceptance_criteria>
  <done>결과 테이블이 Measurement 단위로 한 행씩 표시되고, FAIName/MeasurementName/TypeName/DatumRef/공차/측정값/판정 컬럼이 포함됨</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 6 전체 UI 검증</name>
  <files></files>
  <action>사용자가 Phase 6 전체 기능을 시각적으로 검증한다: Fixture/Multi-Datum 구조, 6종 측정 알고리즘, 조명 필드, 새 INI 포맷, UI 트리 Sequence > Datum + Shot > FAI > Measurement 계층, 결과 테이블 Measurement 단위 표시</action>
  <what-built>
Phase 6 전체 기능: Fixture/Multi-Datum 구조, 6종 측정 알고리즘, 조명 필드, 새 INI 포맷, UI 트리 Sequence > Datum + Shot > FAI > Measurement 계층, 결과 테이블 Measurement 단위 표시
  </what-built>
  <how-to-verify>
1. 앱 실행 (Debug x64)
2. **UI 트리 확인**: InspectionListView에서 Sequence 노드 아래에 Datum 노드와 Action(Shot) 노드가 형제로 표시되는지 확인
3. **Datum CRUD**: Datum 추가/삭제 버튼 동작 확인
4. **Measurement CRUD**: FAI 노드 선택 후 Measurement 추가 -- 타입 선택 -- 트리에 Measurement 노드 표시 확인
5. **PropertyGrid**: Measurement 노드 선택 시 해당 타입의 프로퍼티(ROI, DatumRef, 공차 등) 표시 확인
6. **조명 필드**: Shot(Action) 노드 PropertyGrid에 Ring/Back/Coax/Side Enabled + Brightness 필드 표시 확인
7. **Sequence DisplayName**: Sequence 노드의 이름이 DisplayName으로 표시되고 PropertyGrid에서 편집 가능한지 확인
8. **레시피 저장/로드**: 레시피 저장 후 앱 재시작 -- 로드 -- 트리 구조가 동일한지 확인
9. **기존 포맷 거부**: Phase 5 포맷 INI 파일 로드 시도 -- "새 레시피로 작성하세요" 메시지 표시 확인
10. **결과 테이블**: 검사 실행 후 결과 테이블에 Measurement별 한 행이 표시되는지 확인 (FAIName + MeasurementName + TypeName + DatumRef + 공차 + 측정값 + 판정)
  </how-to-verify>
  <verify>
    <automated>msbuild C:/Info/Project/DataMeasurement/WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal 2>&1 | tail -5</automated>
  </verify>
  <done>사용자가 Phase 6 전체 UI 기능을 확인하고 approved 입력</done>
  <resume-signal>"approved" 입력 또는 문제점 기술</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI TreeView -> ParamData (object) | 트리 노드에서 ParamData를 MeasurementBase로 캐스팅 |
| DataGrid -> MeasurementResultRow | 사용자에게 측정 결과 표시 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | T (Tampering) | InspectionListView Measurement 노드 | accept | 로컬 UI. ParamData 캐스팅 실패 시 null 체크로 안전 처리. |
| T-06-11 | I (Information Disclosure) | MeasurementResultRow 측정값 표시 | accept | 로컬 데스크톱 앱. 측정 결과는 사용자에게 표시되어야 하는 정보. |
</threat_model>

<verification>
- 빌드 성공: `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64`
- UI 트리에 Sequence > Datum + Shot > FAI > Measurement 4계층 표시
- 결과 테이블 Measurement 단위 행 표시
- Sequence DisplayName 표시 + 편집 가능
</verification>

<success_criteria>
- ENodeType.Measurement 추가 + 트리에 Measurement 노드 표시
- Datum 노드가 Sequence 직접 자식 (Action과 형제)
- MeasurementResultRow가 Measurement 단위 결과 행 제공
- 결과 테이블이 FAI별 1행에서 Measurement별 1행으로 변경
- 사용자가 UI에서 전체 4계층 탐색/편집 가능
</success_criteria>

<output>
After completion, create `.planning/phases/06-rapid-city/06-04-SUMMARY.md`
</output>
