---
phase: 064-light-channel-expansion
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/TcpServer/VisionRequestPacket.cs
  - WPF_Example/TcpServer/VisionResponsePacket.cs
autonomous: true
requirements: [LIGHT-01]

must_haves:
  truths:
    - "$PREP:1,2@ 수신 시 PrepPacket.Site=1, PrepPacket.ZIndex=2 로 파싱됨"
    - "파싱 실패(필드 부족/비정수) → null 반환"
    - "PrepAckPacket.IsOk=true → $PREP_ACK:1,2,OK@ 직렬화"
    - "PrepAckPacket.IsOk=false → $PREP_ACK:1,2,FAIL@ 직렬화"
    - "기존 $LIGHT, $TEST, $ALIGN_TEST, $ALIGN_CALIB 파서 무변경"
  artifacts:
    - path: "WPF_Example/TcpServer/VisionRequestPacket.cs"
      provides: "PrepPacket 클래스 + CMD_RECV_PREP 상수 + AsPrep() 메서드 + case CMD_RECV_PREP 분기"
      contains: "class PrepPacket"
    - path: "WPF_Example/TcpServer/VisionResponsePacket.cs"
      provides: "PrepAckPacket 클래스 + CMD_SEND_PREP_ACK 상수 + AsPrep() 메서드 + EVisionResponseType.PrepAck case"
      contains: "class PrepAckPacket"
  key_links:
    - from: "VisionRequestPacket.Convert(string msg)"
      to: "case CMD_RECV_PREP"
      via: "switch(msgList[0])"
      pattern: "CMD_RECV_PREP"
    - from: "VisionResponsePacket.Convert(VisionResponsePacket)"
      to: "EVisionResponseType.PrepAck case"
      via: "switch(packet.ResponseType)"
      pattern: "PrepAck"
---

<objective>
$PREP 수신 패킷 파서와 $PREP_ACK 응답 빌더를 구현한다.

Purpose: D-01/D-02 결정 이행 — TCP 레이어에서 $PREP 커맨드를 인식하고 PrepAck 응답을 직렬화하는 계층 추가.
Output: PrepPacket + PrepAckPacket 클래스, Convert() 분기 추가.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/064-light-channel-expansion/064-CONTEXT.md
@.planning/LIGHT-CHANNEL-DESIGN.md
</context>

<interfaces>
<!-- Phase 63 AlignTest/AlignCalib 패턴 — PrepPacket 은 동일 패턴으로 추가 -->
<!-- From WPF_Example/TcpServer/VisionRequestPacket.cs (기존 패턴 요약) -->
```csharp
// VisionRequestType enum 에 새 값 추가 위치 (AlignCalib 다음)
public enum VisionRequestType {
    RecipeChange, RecipeGet, SiteStatus, Light, Test,
    AlignTest,    //260624 hbk Phase 63
    AlignCalib,   //260624 hbk Phase 63
    // ← 여기에 Prep 추가
    Unknown = 999
}

// Convert(string msg) switch 블록 마지막 case (Unknown 앞)
case CMD_RECV_ALIGN_CALIB:
    ...
    break;
// ← 여기에 case CMD_RECV_PREP 추가

// As*() 헬퍼 패턴 (파일 하단)
public AlignTestPacket AsAlignTest() { ... }
public AlignCalibPacket AsAlignCalib() { ... }
// ← 여기에 AsPrep() 추가
```

<!-- From WPF_Example/TcpServer/VisionResponsePacket.cs (기존 패턴 요약) -->
```csharp
// EVisionResponseType enum (AlignCalib 다음)
public enum EVisionResponseType {
    RecipeChange, RecipeGet, SiteStatus, Light, Test, GrabStatus,
    AlignResult,  //260624 hbk Phase 63
    AlignCalib,   //260624 hbk Phase 63
    // ← 여기에 PrepAck 추가
    Unknown = 999
}

// Convert(VisionResponsePacket packet) switch 마지막 case (Unknown 앞)
case EVisionResponseType.AlignCalib:
    msg += BuildAlignCalibMessage(packet.AsAlignCalib());
    break;
// ← 여기에 EVisionResponseType.PrepAck case 추가

// AsAlignCalib() 헬퍼 다음
// ← 여기에 AsPrepAck() 추가
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: VisionRequestPacket — PrepPacket 파서 추가</name>
  <files>WPF_Example/TcpServer/VisionRequestPacket.cs</files>
  <read_first>
    - WPF_Example/TcpServer/VisionRequestPacket.cs (전체 — enum, CMD 상수, Convert(string), As*() 헬퍼, 서브클래스 위치 파악)
  </read_first>
  <action>
    VisionRequestPacket.cs 에 아래 4곳을 수정한다. 기존 코드 로직은 byte-identical 보존.

    **[1] VisionRequestType enum — AlignCalib 다음에 추가:**
    ```csharp
    AlignCalib,     //260624 hbk Phase 63 AV-09: $ALIGN_CALIB 수신 타입
    Prep,           //260625 hbk Phase 64 LIGHT-01: $PREP 수신 타입
    ```

    **[2] CMD 상수 — CMD_RECV_ALIGN_CALIB 다음에 추가:**
    ```csharp
    public const string CMD_RECV_ALIGN_CALIB = "ALIGN_CALIB"; //260624 hbk Phase 63 AV-09
    public const string CMD_RECV_PREP = "PREP";               //260625 hbk Phase 64 LIGHT-01: $PREP 수신 커맨드
    ```

    **[3] Convert(string msg) switch 블록 — case CMD_RECV_ALIGN_CALIB 의 break 다음, 닫는 } 앞에 추가:**
    ```csharp
                case CMD_RECV_PREP: //260625 hbk Phase 64 LIGHT-01
                    packet = new PrepPacket();
                    PrepPacket prepPacket = packet.AsPrep();
                    dataList = msgList[1].Split(VisionServer.MSG_CONTENTS_SEPERATOR);
                    bool bPrepOk = TryParsePrepFields(dataList, prepPacket);
                    if (!bPrepOk) { return null; }
                    break;
    ```

    **[4] TryParsePrepFields 정적 메서드 — TryParseAlignCalibFields 다음에 추가:**
    ```csharp
            //260625 hbk Phase 64 LIGHT-01: $PREP 수신 파서. dataList[0]=site, dataList[1]=z_index.
            //  필드 부족/비정수 → false 반환(null 응답). 헝가리언 + if-else + 30줄 이내.
            private static bool TryParsePrepFields(string[] dataList, PrepPacket prepPacket)
            {
                bool bHasFields = dataList != null && dataList.Length >= 2;
                if (!bHasFields) { return false; }

                int nSite = 0;
                bool bSiteOk = Int32.TryParse(dataList[0], out nSite);
                if (!bSiteOk) { return false; }
                prepPacket.Site = nSite;

                int nZIndex = 0;
                bool bZIndexOk = Int32.TryParse(dataList[1], out nZIndex);
                if (!bZIndexOk) { return false; }
                prepPacket.ZIndex = nZIndex;

                return true;
            }
    ```

    **[5] AsPrep() 헬퍼 메서드 — AsAlignCalib() 다음에 추가:**
    ```csharp
            //260625 hbk Phase 64 LIGHT-01
            public PrepPacket AsPrep() {
                if (RequestType != VisionRequestType.Prep) return null;
                return this as PrepPacket;
            }
    ```

    **[6] PrepPacket 서브클래스 — AlignCalibPacket 클래스 다음에 추가:**
    ```csharp
        //260625 hbk Phase 64 LIGHT-01: $PREP 수신 패킷. ZIndex = 조명 세팅 대상 Shot z_index.
        public class PrepPacket : VisionRequestPacket {
            public int ZIndex { get; set; }

            public PrepPacket() : base(VisionRequestType.Prep) {
            }
        }
    ```

    주의사항:
    - 삼항 연산자 금지, if-else 사용.
    - 기존 AlignTest/AlignCalib case 로직 무변경.
    - 변수명 헝가리언 (nSite, bSiteOk, nZIndex, bZIndexOk, bHasFields, bPrepOk).
  </action>
  <acceptance_criteria>
    - `grep -n "CMD_RECV_PREP" WPF_Example/TcpServer/VisionRequestPacket.cs` 결과 2건 이상 (상수 + case)
    - `grep -n "class PrepPacket" WPF_Example/TcpServer/VisionRequestPacket.cs` 결과 1건
    - `grep -n "AsPrep" WPF_Example/TcpServer/VisionRequestPacket.cs` 결과 2건 (메서드 + 호출)
    - `grep -n "TryParsePrepFields" WPF_Example/TcpServer/VisionRequestPacket.cs` 결과 2건 (정의 + 호출)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "PrepPacket\|CMD_RECV_PREP\|AsPrep\|TryParsePrepFields" "WPF_Example/TcpServer/VisionRequestPacket.cs"</automated>
  </verify>
  <done>
    - VisionRequestType.Prep enum 값 추가
    - CMD_RECV_PREP 상수 추가
    - case CMD_RECV_PREP 분기 추가 (TryParsePrepFields 호출)
    - TryParsePrepFields 정적 메서드 추가
    - AsPrep() 헬퍼 추가
    - PrepPacket 서브클래스 추가
  </done>
</task>

<task type="auto">
  <name>Task 2: VisionResponsePacket — PrepAckPacket 빌더 추가</name>
  <files>WPF_Example/TcpServer/VisionResponsePacket.cs</files>
  <read_first>
    - WPF_Example/TcpServer/VisionResponsePacket.cs (전체 — EVisionResponseType enum, CMD 상수, Convert(VisionResponsePacket), As*() 헬퍼 위치 파악)
  </read_first>
  <action>
    VisionResponsePacket.cs 에 아래 4곳을 수정한다. 기존 코드 로직 byte-identical 보존.

    **[1] EVisionResponseType enum — AlignCalib 다음에 추가:**
    ```csharp
    AlignCalib,    //260624 hbk Phase 63 AV-09: Align 캘리브 ack 응답 ($ALIGN_CALIB)
    PrepAck,       //260625 hbk Phase 64 LIGHT-01: $PREP_ACK 응답
    ```

    **[2] CMD 상수 — CMD_SEND_ALIGN_CALIB 다음에 추가:**
    ```csharp
    public const string CMD_SEND_ALIGN_CALIB = "ALIGN_CALIB";     //260624 hbk Phase 63 AV-09
    public const string CMD_SEND_PREP_ACK = "PREP_ACK";           //260625 hbk Phase 64 LIGHT-01: $PREP_ACK 송신 커맨드
    ```

    **[3] Convert(VisionResponsePacket packet) switch 블록 — case EVisionResponseType.AlignCalib 의 break 다음, Unknown case 앞에 추가:**
    ```csharp
                case EVisionResponseType.PrepAck:
                    msg += BuildPrepAckMessage(packet.AsPrepAck()); //260625 hbk Phase 64 LIGHT-01
                    break;
    ```

    **[4] BuildPrepAckMessage 정적 메서드 — BuildAlignCalibMessage 다음에 추가:**
    ```csharp
            //260625 hbk Phase 64 LIGHT-01: $PREP_ACK:site,z_index,OK|FAIL@ 직렬화.
            //  IsOk=true → OK, IsOk=false → FAIL. 헝가리언 + if-else + 30줄 이내.
            private static string BuildPrepAckMessage(PrepAckPacket packet)
            {
                string szMsg = "";
                szMsg += CMD_SEND_PREP_ACK;
                szMsg += VisionServer.MSG_CMD_SEPERATOR;       // ':'
                szMsg += packet.Site.ToString();
                szMsg += VisionServer.MSG_CONTENTS_SEPERATOR;  // ','
                szMsg += packet.ZIndex.ToString();
                szMsg += VisionServer.MSG_CONTENTS_SEPERATOR;  // ','
                bool bIsOk = packet.IsOk;
                if (bIsOk)
                {
                    szMsg += "OK";
                }
                else
                {
                    szMsg += "FAIL";
                }
                return szMsg;
            }
    ```

    **[5] AsPrepAck() 헬퍼 메서드 — AsAlignCalib() 다음에 추가:**
    ```csharp
            //260625 hbk Phase 64 LIGHT-01
            public PrepAckPacket AsPrepAck() {
                if (ResponseType != EVisionResponseType.PrepAck) return null;
                return this as PrepAckPacket;
            }
    ```

    **[6] PrepAckPacket 서브클래스 — AlignCalibResultPacket 클래스 다음에 추가:**
    ```csharp
        //260625 hbk Phase 64 LIGHT-01: $PREP_ACK 응답 패킷.
        //  IsOk=true → $PREP_ACK:site,z_index,OK@ / IsOk=false → $PREP_ACK:site,z_index,FAIL@
        public class PrepAckPacket : VisionResponsePacket {
            public int ZIndex { get; set; }
            public bool IsOk { get; set; }

            public PrepAckPacket() : base(EVisionResponseType.PrepAck) {
            }
        }
    ```

    주의사항:
    - 삼항 연산자 금지, if-else 사용 (bIsOk 분기).
    - 기존 AlignResult/AlignCalib 직렬화 로직 무변경.
    - 변수명 헝가리언 (szMsg, bIsOk).
  </action>
  <acceptance_criteria>
    - `grep -n "CMD_SEND_PREP_ACK" WPF_Example/TcpServer/VisionResponsePacket.cs` 결과 2건 이상 (상수 + 메서드 내 사용)
    - `grep -n "class PrepAckPacket" WPF_Example/TcpServer/VisionResponsePacket.cs` 결과 1건
    - `grep -n "BuildPrepAckMessage" WPF_Example/TcpServer/VisionResponsePacket.cs` 결과 2건 (정의 + 호출)
    - `grep -n "PrepAck" WPF_Example/TcpServer/VisionResponsePacket.cs` 결과 4건 이상
  </acceptance_criteria>
  <verify>
    <automated>grep -n "PrepAck\|CMD_SEND_PREP_ACK\|BuildPrepAckMessage" "WPF_Example/TcpServer/VisionResponsePacket.cs"</automated>
  </verify>
  <done>
    - EVisionResponseType.PrepAck enum 값 추가
    - CMD_SEND_PREP_ACK 상수 추가
    - case EVisionResponseType.PrepAck 분기 추가
    - BuildPrepAckMessage 정적 메서드 추가
    - AsPrepAck() 헬퍼 추가
    - PrepAckPacket 서브클래스 추가 (ZIndex, IsOk 속성)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TCP 소켓 → VisionRequestPacket.Convert(string) | 외부 입력, 필드 파싱 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-064-02 | Spoofing | $PREP 파서 — site/z_index 조작 | mitigate | TryParsePrepFields: 정수 파싱 실패 → null 반환(패킷 무시), 음수 z_index 는 ProcessPrep(064-03)에서 Shot 조회 실패 → FAIL 응답 |
| T-064-03 | Tampering | 기존 $LIGHT/$TEST 파서 | accept | case 블록 추가만, 기존 블록 byte-identical 보존 |
</threat_model>

<verification>
```bash
# 1. PrepPacket 존재 확인
grep -n "class PrepPacket\|CMD_RECV_PREP\|AsPrep\b\|TryParsePrepFields" "WPF_Example/TcpServer/VisionRequestPacket.cs"

# 2. PrepAckPacket 존재 확인
grep -n "class PrepAckPacket\|CMD_SEND_PREP_ACK\|AsPrepAck\|BuildPrepAckMessage" "WPF_Example/TcpServer/VisionResponsePacket.cs"

# 3. 기존 커맨드 무변경 확인 (AlignCalib case 아직 존재)
grep -n "CMD_RECV_ALIGN_CALIB\|CMD_RECV_ALIGN_TEST" "WPF_Example/TcpServer/VisionRequestPacket.cs"

# 4. msbuild
msbuild "WPF_Example/DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal 2>&1 | tail -5
```
</verification>

<success_criteria>
- $PREP:1,2@ → PrepPacket(Site=1, ZIndex=2) 파싱 성공 (TryParsePrepFields 반환 true)
- 필드 부족/비정수 → null 반환
- PrepAckPacket(Site=1, ZIndex=2, IsOk=true) → "PREP_ACK:1,2,OK" 직렬화
- PrepAckPacket(Site=1, ZIndex=2, IsOk=false) → "PREP_ACK:1,2,FAIL" 직렬화
- msbuild Debug/x64 PASS
</success_criteria>

<output>
After completion, create `.planning/phases/064-light-channel-expansion/064-02-SUMMARY.md`
</output>
