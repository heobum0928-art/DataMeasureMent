---
phase: 064-light-channel-expansion
plan: "03"
type: execute
wave: 2
depends_on: ["064-02"]
files_modified:
  - WPF_Example/Custom/SystemHandler.cs
autonomous: true
requirements: [LIGHT-01]

must_haves:
  truths:
    - "MainRun() switch-case 에 VisionRequestType.Prep case 추가됨"
    - "ProcessPrep() 가 PrepPacket 에서 site → Identifier 매핑 후 InspectionSequence 접근"
    - "ApplyShotLights 성공 → PrepAckPacket.IsOk=true 반환"
    - "Shot 없음 / InspectionSequence null → PrepAckPacket.IsOk=false 반환"
    - "기존 ProcessTest, ProcessAlignTest, ProcessAlignCalib 로직 무변경"
  artifacts:
    - path: "WPF_Example/Custom/SystemHandler.cs"
      provides: "ProcessPrep() 메서드 + MainRun switch-case Prep 분기"
      contains: "private PrepAckPacket ProcessPrep"
  key_links:
    - from: "MainRun() switch(packet.RequestType)"
      to: "ProcessPrep(packet.AsPrep())"
      via: "case VisionRequestType.Prep"
      pattern: "VisionRequestType.Prep"
    - from: "ProcessPrep(PrepPacket packet)"
      to: "inspSeq.ApplyShotLights(packet.ZIndex)"
      via: "Sequences[seqName] as InspectionSequence"
      pattern: "ApplyShotLights"
---

<objective>
SystemHandler.MainRun() 에 $PREP 분기를 추가하고 ProcessPrep() 을 구현한다.

Purpose: D-12 결정 이행 — $PREP 수신 시 z_index로 InspectionSequence 를 찾고 ApplyShotLights() 를 호출하여 PrepAck 응답을 반환.
Output: ProcessPrep() 메서드 + MainRun switch-case 추가.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/064-light-channel-expansion/064-CONTEXT.md
@.planning/phases/064-light-channel-expansion/064-04-PLAN.md
</context>

<interfaces>
<!-- From WPF_Example/Custom/SystemHandler.cs (기존 패턴 — ProcessAlignCalib 다음에 추가) -->
```csharp
// MainRun() switch 마지막 case 위치:
case VisionRequestType.AlignCalib:                                 //260624 hbk Phase 63 AV-09
    responsePacket = ProcessAlignCalib(packet.AsAlignCalib());
    break;
case VisionRequestType.Unknown:
    //occurs error
    break;
// ← case VisionRequestType.Prep 를 AlignCalib 와 Unknown 사이에 추가

// ProcessAlignCalib 패턴 (ProcessPrep 는 이 패턴 따름):
private AlignCalibResultPacket ProcessAlignCalib(AlignCalibPacket packet)
{
    AlignCalibResultPacket resultPacket = new AlignCalibResultPacket();
    bool bHasPacket = packet != null;
    if (!bHasPacket) { return null; }
    resultPacket.Target = packet.Sender;
    ...
    return resultPacket;
}
```

<!-- From InspectionSequence 접근 패턴 (InspectionRecipeManager.cs 에서 확인) -->
```csharp
// Site → Identifier 매핑: ResourceMap 또는 Sequences 직접 조회 패턴
// ProcessTest 에서 사용하는 패턴:
string seqName = packet.Identifier;
SequenceBase seq = Sequences[seqName];
// ← $PREP 는 Identifier 가 없으므로, Site 기반 접근이 필요.
// InspectionSequence 전체 순회 패턴:
var recipeManager = SystemHandler.Handle.Sequences.RecipeManager;
// 또는 ESequence enum 기반으로 직접 참조 가능.
// Phase 64 에서는 "이 PC 의 인스펙션 시퀀스" 를 Site에 관계없이 조회 —
// Sequences 를 순회하여 InspectionSequence 를 찾는 방식 채택 (ProcessPrep 설명 참조).
```

<!-- PrepAckPacket 인터페이스 (064-02에서 생성) -->
```csharp
public class PrepAckPacket : VisionResponsePacket {
    public int ZIndex { get; set; }
    public bool IsOk { get; set; }
    public PrepAckPacket() : base(EVisionResponseType.PrepAck) { }
}
```

<!-- ApplyShotLights 인터페이스 (064-04에서 구현 예정) -->
```csharp
// InspectionSequence 에 추가될 public 메서드
public bool ApplyShotLights(int nZIndex);
// 반환: true = 조명 세팅 성공, false = Shot 없음 또는 조명 오류
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: MainRun switch-case 에 VisionRequestType.Prep 분기 추가</name>
  <files>WPF_Example/Custom/SystemHandler.cs</files>
  <read_first>
    - WPF_Example/Custom/SystemHandler.cs (1~80줄 — MainRun() switch 전체 구조 파악)
  </read_first>
  <action>
    Custom/SystemHandler.cs 의 MainRun() switch 블록에서 `case VisionRequestType.AlignCalib:` 의 break 다음, `case VisionRequestType.Unknown:` 앞에 아래를 추가한다.

    ```csharp
                    case VisionRequestType.Prep:                                   //260625 hbk Phase 64 LIGHT-01
                        responsePacket = ProcessPrep(packet.AsPrep());
                        break;
    ```

    주의사항:
    - 삼항 연산자 금지. 기존 AlignTest/AlignCalib/Test case 무변경.
    - packet.AsPrep() 은 064-02에서 추가된 메서드.
  </action>
  <acceptance_criteria>
    - `grep -n "VisionRequestType.Prep" WPF_Example/Custom/SystemHandler.cs` 결과 2건 이상 (case + 주석)
    - `grep -n "ProcessPrep" WPF_Example/Custom/SystemHandler.cs` 결과 2건 이상 (호출 + 정의)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "VisionRequestType.Prep\|ProcessPrep" "WPF_Example/Custom/SystemHandler.cs"</automated>
  </verify>
  <done>case VisionRequestType.Prep 분기가 switch 블록에 추가됨.</done>
</task>

<task type="auto">
  <name>Task 2: ProcessPrep() 메서드 구현</name>
  <files>WPF_Example/Custom/SystemHandler.cs</files>
  <read_first>
    - WPF_Example/Custom/SystemHandler.cs (267~296줄 — ProcessAlignCalib 패턴 확인)
    - WPF_Example/Custom/SystemHandler.cs (241~265줄 — ProcessTest 의 Sequences 접근 패턴 확인)
  </read_first>
  <action>
    Custom/SystemHandler.cs 에서 ProcessAlignCalib() 메서드 다음에 ProcessPrep() 를 추가한다. 30~40줄 이내.

    ```csharp
        //260625 hbk Phase 64 LIGHT-01 (D-12): $PREP 처리.
        //  z_index → 이 PC InspectionSequence 찾기 → ApplyShotLights() 호출 → PrepAck 반환.
        //  Site 필드는 ACK 에 echo만 함. 실제 시퀀스 라우팅은 이 PC 소속 InspectionSequence 전부 대상.
        private PrepAckPacket ProcessPrep(PrepPacket packet)
        {
            PrepAckPacket ackPacket = new PrepAckPacket();
            bool bHasPacket = packet != null;
            if (!bHasPacket)
            {
                return null;
            }
            ackPacket.Target = packet.Sender;
            ackPacket.Site = packet.Site;
            ackPacket.ZIndex = packet.ZIndex;
            ackPacket.IsOk = false; // 기본값 FAIL — 성공 시 true 로 덮어씀

            bool bApplied = ApplyPrepToSequences(packet.ZIndex);
            if (bApplied)
            {
                ackPacket.IsOk = true;
            }
            return ackPacket;
        }

        //260625 hbk Phase 64 LIGHT-01: Sequences 순회 → InspectionSequence 찾기 → ApplyShotLights 호출.
        //  하나라도 성공하면 true 반환. 매칭 InspectionSequence 없으면 false.
        private bool ApplyPrepToSequences(int nZIndex)
        {
            bool bAnyApplied = false;
            int nCount = Sequences.Count;
            for (int i = 0; i < nCount; i++)
            {
                SequenceBase seqBase = Sequences[i];
                InspectionSequence inspSeq = seqBase as InspectionSequence;
                bool bIsInsp = inspSeq != null;
                if (!bIsInsp)
                {
                    continue;
                }
                bool bOk = inspSeq.ApplyShotLights(nZIndex);
                if (bOk)
                {
                    bAnyApplied = true;
                }
            }
            return bAnyApplied;
        }
    ```

    주의사항:
    - 삼항 연산자 금지, if-else 사용.
    - 헝가리언 표기법: nZIndex, bHasPacket, bApplied, bAnyApplied, bIsInsp, bOk, nCount.
    - InspectionSequence 참조를 위해 파일 상단 `using ReringProject.Sequence;` 가 이미 있는지 확인 — 없으면 추가.
    - Sequences[i] 인덱스 접근: SequenceHandler 의 인덱서가 있는지 확인. 없으면 `Sequences.GetAll()` 또는 foreach 패턴으로 수정. grep으로 SequenceHandler 인덱서/foreach 패턴 먼저 확인하고 기존 패턴에 맞게 구현.
    - 함수 30~40줄 이내 유지 위해 ApplyPrepToSequences 별도 메서드로 분리.
  </action>
  <acceptance_criteria>
    - `grep -n "private PrepAckPacket ProcessPrep" WPF_Example/Custom/SystemHandler.cs` 결과 1건
    - `grep -n "ApplyPrepToSequences" WPF_Example/Custom/SystemHandler.cs` 결과 2건 (정의 + 호출)
    - `grep -n "InspectionSequence" WPF_Example/Custom/SystemHandler.cs` 결과 1건 이상 (as 캐스팅)
    - `grep -n "ApplyShotLights" WPF_Example/Custom/SystemHandler.cs` 결과 1건 (호출)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "ProcessPrep\|ApplyPrepToSequences\|ApplyShotLights\|InspectionSequence" "WPF_Example/Custom/SystemHandler.cs"</automated>
  </verify>
  <done>
    - ProcessPrep() 메서드 구현 완료 (PrepAckPacket 반환)
    - ApplyPrepToSequences() 헬퍼 메서드 구현 완료
    - msbuild Debug/x64 PASS
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TCP → ProcessPrep() | 외부 site/z_index 값이 InspectionSequence 조회에 사용됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-064-04 | Elevation | ProcessPrep z_index=-1 또는 범위 외 | mitigate | ApplyShotLights(nZIndex) 내부에서 Shot 조회 실패 → false 반환 → IsOk=false(FAIL 응답) |
| T-064-05 | Denial of Service | Sequences 순회 중 null InspectionSequence | mitigate | inspSeq != null 가드 (as 캐스팅 실패 = null) |
</threat_model>

<verification>
```bash
# 1. case Prep 분기 확인
grep -n "VisionRequestType.Prep\|ProcessPrep" "WPF_Example/Custom/SystemHandler.cs"

# 2. ApplyPrepToSequences + ApplyShotLights 호출 확인
grep -n "ApplyPrepToSequences\|ApplyShotLights\|InspectionSequence" "WPF_Example/Custom/SystemHandler.cs"

# 3. 기존 ProcessAlignCalib 무변경 확인
grep -n "ProcessAlignCalib\|ProcessAlignTest" "WPF_Example/Custom/SystemHandler.cs"

# 4. msbuild
msbuild "WPF_Example/DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal 2>&1 | tail -5
```
</verification>

<success_criteria>
- VisionRequestType.Prep case 분기 추가 완료
- ProcessPrep() 메서드: packet null 가드 → ACK 기본 FAIL → ApplyPrepToSequences() 호출 → IsOk 설정
- ApplyPrepToSequences(): Sequences 순회 → InspectionSequence as 캐스팅 → ApplyShotLights(nZIndex) → 하나라도 성공 시 true
- 기존 ProcessAlignTest/ProcessAlignCalib/ProcessTest 로직 무변경
- msbuild Debug/x64 PASS
</success_criteria>

<output>
After completion, create `.planning/phases/064-light-channel-expansion/064-03-SUMMARY.md`
</output>
