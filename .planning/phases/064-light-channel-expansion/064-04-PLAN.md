---
phase: 064-light-channel-expansion
plan: "04"
type: execute
wave: 2
depends_on: ["064-01"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
autonomous: true
requirements: [LIGHT-01]

must_haves:
  truths:
    - "ApplyShotLights(nZIndex) 가 public 메서드로 InspectionSequence 에 존재"
    - "nZIndex 에 해당 Shot 없음 → false 반환"
    - "RingLight_Enabled=true → LightHandler.SetOnOff(LIGHT_RING, true) + SetLevel(LIGHT_RING, RingLight_Brightness)"
    - "RingLight_Enabled=false → LightHandler.SetOnOff(LIGHT_RING, false)"
    - "BackLight, CoaxLight, SideLight 동일 패턴으로 BACK, ALIGN_COAX, BAR 그룹 제어"
    - "기존 AddResponse, AddResponseV1Cycle 등 검사 흐름 코드 무변경"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "ApplyShotLights(int nZIndex) + FindShotByZIndex + ApplyShotLightsInternal"
      contains: "public bool ApplyShotLights"
  key_links:
    - from: "ApplyShotLights(int nZIndex)"
      to: "LightHandler.Handle.SetOnOff / SetLevel"
      via: "ApplyShotLightsInternal(ShotConfig shot)"
      pattern: "LightHandler.Handle.Set"
    - from: "FindShotByZIndex(int nZIndex)"
      to: "recipeManager.Shots 순회"
      via: "shot.ZIndex == nZIndex && shot.OwnerSequenceName == Name"
      pattern: "ZIndex.*nZIndex"
---

<objective>
InspectionSequence 에 ApplyShotLights() 를 구현하여 ShotConfig 기반 조명 자동 세팅을 완성한다.

Purpose: D-10/D-11/D-12 결정 이행 — z_index 로 ShotConfig 를 찾고 RingLight/BackLight/CoaxLight/SideLight 를 각 LightGroup 에 연결.
Output: ApplyShotLights(int nZIndex) public 메서드 + 헬퍼 2종.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/064-light-channel-expansion/064-CONTEXT.md
</context>

<interfaces>
<!-- ShotConfig 조명 속성 (기존 정의, 변경 없음) -->
```csharp
// From WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
[Category("Light|Ring")]
public bool RingLight_Enabled { get; set; }
public int RingLight_Brightness { get; set; }

[Category("Light|Back")]
public bool BackLight_Enabled { get; set; }
public int BackLight_Brightness { get; set; }

[Category("Light|Coax")]
public bool CoaxLight_Enabled { get; set; }   // D-11: INI 키 이름 CoaxLight_* 보존
public int CoaxLight_Brightness { get; set; }

[Category("Light|Side")]
public bool SideLight_Enabled { get; set; }
public int SideLight_Brightness { get; set; }
```

<!-- LightGroup 상수 (064-01에서 정의) -->
```csharp
// From WPF_Example/Custom/Device/LightHandler.cs (064-01 완료 후)
public const string LIGHT_RING       = "RING";        // Ring 통합 그룹
public const string LIGHT_BACK       = "BACK";        // Back 그룹
public const string LIGHT_BAR        = "BAR";         // Bar 통합 그룹
public const string LIGHT_ALIGN_COAX = "ALIGN_COAX";  // Align 동축 그룹
```

<!-- LightHandler API -->
```csharp
public bool SetOnOff(string groupName, bool onOff);  // 그룹 전체 ON/OFF
public bool SetLevel(string groupName, int level);   // 그룹 전체 레벨 설정
// 기존 ApplyLight(ICameraParam) 패턴: SetOnOff(true) → SetLevel() 순서
```

<!-- InspectionSequence 내 Shot 조회 패턴 (기존 코드에서 확인) -->
```csharp
// recipeManager.Shots 순회 + OwnerSequenceName 필터 (ComputeLastZIndex에서 확인)
var recipeManager = SystemHandler.Handle.Sequences.RecipeManager;
foreach (var shot in recipeManager.Shots) {
    if (shot == null) continue;
    bool bOwnedByThisSeq = shot.OwnerSequenceName == Name;
    bool bZMatch = shot.ZIndex == nZIndex;
    if (bOwnedByThisSeq && bZMatch) { /* 매칭 */ }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: FindShotByZIndex 헬퍼 구현</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (260~290줄 — ComputeLastZIndex 의 recipeManager.Shots 순회 + OwnerSequenceName 필터 패턴 확인)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (38~90줄 — 클래스 구조 + private 필드 확인)
  </read_first>
  <action>
    InspectionSequence.cs 에서 ComputeLastZIndex() 메서드 다음에 FindShotByZIndex() 를 추가한다.

    ```csharp
        //260625 hbk Phase 64 LIGHT-01 (D-12): z_index + OwnerSequenceName 기반 ShotConfig 조회.
        //  이 시퀀스 소유 Shot 중 첫 번째 매칭을 반환. 없으면 null.
        //  ComputeLastZIndex 의 순회 패턴 동일 — foreach + OwnerSequenceName + ZIndex 이중 조건.
        private ShotConfig FindShotByZIndex(int nZIndex)
        {
            var recipeManager = SystemHandler.Handle.Sequences.RecipeManager;
            bool bHasManager = recipeManager != null;
            if (!bHasManager)
            {
                return null;
            }
            foreach (var shot in recipeManager.Shots)
            {
                bool bIsNull = shot == null;
                if (bIsNull)
                {
                    continue;
                }
                bool bOwnedByThisSeq = shot.OwnerSequenceName == Name;
                bool bZMatch = shot.ZIndex == nZIndex;
                bool bInScope = bOwnedByThisSeq && bZMatch;
                if (bInScope)
                {
                    return shot;
                }
            }
            return null;
        }
    ```

    주의사항:
    - 삼항 연산자 금지, if-else/continue 사용.
    - 헝가리언: nZIndex, bHasManager, bIsNull, bOwnedByThisSeq, bZMatch, bInScope.
    - foreach var 타입은 ShotConfig.
    - DATUM_Z_INDEX(=0) Shot 도 조회 가능 — $PREP 에 z_index=0 수신 시 Datum Shot 조명 세팅 의도일 수 있음, 막지 않음.
  </action>
  <acceptance_criteria>
    - `grep -n "private ShotConfig FindShotByZIndex" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 결과 1건
    - `grep -n "OwnerSequenceName.*Name\|Name.*OwnerSequenceName" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 결과 4건 이상 (기존 + 신규)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "FindShotByZIndex\|OwnerSequenceName" "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"</automated>
  </verify>
  <done>FindShotByZIndex(int nZIndex) private 메서드가 InspectionSequence 에 추가됨.</done>
</task>

<task type="auto">
  <name>Task 2: ApplyShotLights public 진입점 + ApplyShotLightsInternal 구현</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (580~590줄 — OnLoad 의 Lights.ApplyLight(pMyParam) 패턴 확인)
  </read_first>
  <action>
    InspectionSequence.cs 에서 FindShotByZIndex() 다음에 아래 두 메서드를 추가한다.

    ```csharp
        //260625 hbk Phase 64 LIGHT-01 (D-10/D-12): $PREP 수신 → z_index Shot 조회 → 조명 세팅.
        //  ProcessPrep() 이 호출하는 public 진입점. Shot 없으면 false.
        //  CoaxLight_* 는 ALIGN_COAX 그룹으로 매핑 (D-11: INI 키 이름 보존, 그룹명만 변경).
        public bool ApplyShotLights(int nZIndex)
        {
            ShotConfig shot = FindShotByZIndex(nZIndex);
            bool bHasShot = shot != null;
            if (!bHasShot)
            {
                Logging.PrintLog((int)ELogType.LightController,
                    "[PREP] Shot not found for ZIndex={0}, Seq={1} //260625 hbk Phase 64", nZIndex, Name);
                return false;
            }
            ApplyShotLightsInternal(shot);
            return true;
        }

        //260625 hbk Phase 64 LIGHT-01 (D-10): ShotConfig 4종 조명 → LightHandler 5종 그룹 적용.
        //  RingLight → RING / BackLight → BACK / CoaxLight → ALIGN_COAX / SideLight → BAR
        //  Enabled=true: SetOnOff(true) 먼저, 이후 SetLevel (ApplyLight 순서 동일).
        //  Enabled=false: SetOnOff(false) 만 호출.
        private void ApplyShotLightsInternal(ShotConfig shot)
        {
            if (shot.RingLight_Enabled)
            {
                LightHandler.Handle.SetOnOff(LightHandler.LIGHT_RING, true);
                LightHandler.Handle.SetLevel(LightHandler.LIGHT_RING, shot.RingLight_Brightness);
            }
            else
            {
                LightHandler.Handle.SetOnOff(LightHandler.LIGHT_RING, false);
            }

            if (shot.BackLight_Enabled)
            {
                LightHandler.Handle.SetOnOff(LightHandler.LIGHT_BACK, true);
                LightHandler.Handle.SetLevel(LightHandler.LIGHT_BACK, shot.BackLight_Brightness);
            }
            else
            {
                LightHandler.Handle.SetOnOff(LightHandler.LIGHT_BACK, false);
            }

            if (shot.CoaxLight_Enabled)
            {
                LightHandler.Handle.SetOnOff(LightHandler.LIGHT_ALIGN_COAX, true);
                LightHandler.Handle.SetLevel(LightHandler.LIGHT_ALIGN_COAX, shot.CoaxLight_Brightness);
            }
            else
            {
                LightHandler.Handle.SetOnOff(LightHandler.LIGHT_ALIGN_COAX, false);
            }

            if (shot.SideLight_Enabled)
            {
                LightHandler.Handle.SetOnOff(LightHandler.LIGHT_BAR, true);
                LightHandler.Handle.SetLevel(LightHandler.LIGHT_BAR, shot.SideLight_Brightness);
            }
            else
            {
                LightHandler.Handle.SetOnOff(LightHandler.LIGHT_BAR, false);
            }
        }
    ```

    주의사항:
    - 삼항 연산자 금지, 전부 if-else 사용.
    - 헝가리언: nZIndex, bHasShot.
    - LightHandler.LIGHT_RING 등 상수는 Custom/Device/LightHandler.cs (064-01) 에서 정의됨 — using ReringProject.Device 가 이미 있으므로 직접 참조 가능.
    - LIGHT_BACK 는 LightHandler.LIGHT_BACK(물리 채널명) 이며 그룹명도 동일 ("BACK") — 정상.
    - LIGHT_ALIGN_COAX 상수도 LightHandler 에 정의됨 — LightHandler.LIGHT_ALIGN_COAX 참조.
    - LIGHT_BAR 상수도 LightHandler.LIGHT_BAR 참조.
    - ApplyShotLightsInternal 은 30줄을 약간 초과하나, 4종 조명 × (if/SetOnOff/SetLevel/else/SetOnOff) 5줄 반복 구조 — 기능 분할 시 응집도 손상, 현 구조 유지.
  </action>
  <acceptance_criteria>
    - `grep -n "public bool ApplyShotLights" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 결과 1건
    - `grep -n "ApplyShotLightsInternal" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 결과 2건 (정의 + 호출)
    - `grep -n "LightHandler.Handle.SetOnOff\|LightHandler.Handle.SetLevel" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 결과 8건 (4종 × 각 1~2줄)
    - `grep -n "LIGHT_RING\b\|LIGHT_BACK\b\|LIGHT_ALIGN_COAX\|LIGHT_BAR\b" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 결과 8건 이상
  </acceptance_criteria>
  <verify>
    <automated>grep -n "ApplyShotLights\|LightHandler.Handle.Set" "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"</automated>
  </verify>
  <done>
    - ApplyShotLights(int nZIndex) public 메서드 추가 완료
    - ApplyShotLightsInternal(ShotConfig shot) private 메서드 추가 완료
    - RingLight → LIGHT_RING / BackLight → LIGHT_BACK / CoaxLight → LIGHT_ALIGN_COAX / SideLight → LIGHT_BAR 매핑 완료
    - msbuild Debug/x64 PASS
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProcessPrep() → ApplyShotLights() | z_index 가 외부(TCP)에서 전달됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-064-06 | Tampering | z_index 조작으로 의도하지 않은 Shot 조명 세팅 | accept | Shot 조명값은 레시피에서 로드된 ShotConfig 기반 — 조작 시 비정상 Shot 조명이지만 검사 안전에 영향 없음(측정은 $TEST 이후) |
| T-064-07 | Denial of Service | ApplyShotLightsInternal LightHandler.SetLevel 반환값 무시 | accept | SetLevel은 CmdTable 비동기 큐잉 — 내부 실패는 Execute 스레드가 로그. PREP_ACK 는 true 전달됨(현재 SIMUL 환경 우선) |
</threat_model>

<verification>
```bash
# 1. ApplyShotLights 존재 확인
grep -n "public bool ApplyShotLights\|ApplyShotLightsInternal\|FindShotByZIndex" "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"

# 2. LightHandler 그룹 상수 참조 확인 (RING/BACK/BAR/ALIGN_COAX)
grep -n "LightHandler.LIGHT_RING\|LightHandler.LIGHT_BACK\|LightHandler.LIGHT_BAR\|LightHandler.LIGHT_ALIGN_COAX" "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"

# 3. SetOnOff/SetLevel 8건 이상 확인
grep -c "SetOnOff\|SetLevel" "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"

# 4. 기존 AddResponse/AddResponseV1Cycle 무변경 확인
grep -n "AddResponse\b\|AddResponseV1Cycle" "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"

# 5. msbuild
msbuild "WPF_Example/DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal 2>&1 | tail -5
```
</verification>

<success_criteria>
- ApplyShotLights(int nZIndex): Shot 없음 → false / Shot 있음 → ApplyShotLightsInternal() 호출 → true
- ApplyShotLightsInternal: 4종 조명 Enabled 분기 → SetOnOff + SetLevel (또는 SetOnOff false)
- 매핑: RingLight→LIGHT_RING, BackLight→LIGHT_BACK, CoaxLight→LIGHT_ALIGN_COAX, SideLight→LIGHT_BAR
- 기존 InspectionSequence 검사 흐름(AddResponse, OnLoad 등) 무변경
- msbuild Debug/x64 PASS
</success_criteria>

<output>
After completion, create `.planning/phases/064-light-channel-expansion/064-04-SUMMARY.md`
</output>
