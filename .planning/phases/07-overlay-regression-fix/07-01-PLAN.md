---
phase: 07-overlay-regression-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgePairDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/PointToLineDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/PointToPointDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/LineToLineAngleMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/LineToLineDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs
autonomous: true
requirements: [ALG-04]
tags: [wpf, halcon, measurement, overlay]

must_haves:
  truths:
    - "MeasurementBase.TryExecute 시그니처에 out List<EdgeInspectionOverlay> overlays 파라미터가 존재한다 (D-01)"
    - "6개 파생 Measurement 클래스가 새 시그니처로 override되어 빌드가 통과한다 (D-15)"
    - "EdgePairDistanceMeasurement.TryExecute는 FAIEdgeMeasurementService result.Overlays를 그대로 전달한다 (D-09, D-10)"
    - "5개 Measurement(PointToLine/PointToPoint/LineToLineAngle/LineToLineDistance/CircleDiameter)는 빈 리스트를 반환한다 (D-03)"
    - "EdgeInspectionOverlay 모델은 수정되지 않는다 (D-11)"
    - "HalconDisplayService / FAIEdgeMeasurementService는 수정되지 않는다 (D-12, D-13)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs"
      provides: "out overlays 파라미터가 추가된 abstract TryExecute 시그니처"
      contains: "out List<EdgeInspectionOverlay> overlays"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/EdgePairDistanceMeasurement.cs"
      provides: "FAIEdgeMeasurementService.result.Overlays를 overlays out 파라미터로 전달"
      contains: "overlays = result.Overlays"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/PointToLineDistanceMeasurement.cs"
      provides: "빈 overlay 리스트 반환"
      contains: "overlays = new List<EdgeInspectionOverlay>()"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/PointToPointDistanceMeasurement.cs"
      provides: "빈 overlay 리스트 반환"
      contains: "overlays = new List<EdgeInspectionOverlay>()"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/LineToLineAngleMeasurement.cs"
      provides: "빈 overlay 리스트 반환"
      contains: "overlays = new List<EdgeInspectionOverlay>()"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/LineToLineDistanceMeasurement.cs"
      provides: "빈 overlay 리스트 반환"
      contains: "overlays = new List<EdgeInspectionOverlay>()"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs"
      provides: "빈 overlay 리스트 반환"
      contains: "overlays = new List<EdgeInspectionOverlay>()"
  key_links:
    - from: "MeasurementBase (abstract)"
      to: "6 derived Measurement classes"
      via: "override 시그니처 통일"
      pattern: "out List<EdgeInspectionOverlay> overlays"
    - from: "EdgePairDistanceMeasurement.TryExecute"
      to: "FAIEdgeMeasurementResult.Overlays"
      via: "service.TryMeasure 결과 재노출"
      pattern: "overlays\\s*=\\s*result\\.Overlays"
---

<objective>
Phase 6에서 도입된 `MeasurementBase.TryExecute` 시그니처에 `out List<EdgeInspectionOverlay> overlays` 파라미터를 추가하고, 이를 상속하는 6개 파생 클래스의 override를 일괄 갱신한다. EdgePair 1종은 기존 FAIEdgeMeasurementService 산출물(result.Overlays)을 그대로 전달하고, 나머지 5종은 빈 리스트를 반환하여 빌드 통과 + Phase 3 에지 시각화 복구 경로를 연결한다.

Purpose: Phase 6이 만든 overlay 출력 경로 단절(Gap I1)의 첫 번째 필수 조건 — 컴파일 가능한 overlay 전달 통로를 신설한다. Action_FAIMeasurement 루프의 누적 로직(Plan 02)은 이 시그니처 위에서만 구현 가능.
Output: 수정된 7개 파일(abstract 1 + derived 6). `msbuild /p:Configuration=Debug /p:Platform=x64` 성공.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-overlay-regression-fix/07-CONTEXT.md

<interfaces>
<!-- 현재 시그니처 (수정 전) — 모든 6개 파생 클래스가 이 형태로 override 중 -->

From WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (L45-50):
```csharp
public abstract bool TryExecute( //260413 hbk
    HImage image,
    HTuple datumTransform,
    double pixelResolution,
    out double resultValue,
    out string error);
```

목표 시그니처 (D-01):
```csharp
public abstract bool TryExecute( //260413 hbk
    HImage image,
    HTuple datumTransform,
    double pixelResolution,
    out double resultValue,
    out string error,
    out List<EdgeInspectionOverlay> overlays); //260422 hbk Phase 7: overlay 반환 경로 (D-01)
```

EdgeInspectionOverlay namespace: `ReringProject.Halcon.Models` (using 추가 필요 시).

From WPF_Example/Halcon/Models/FAIEdgeMeasurementResult.cs:
- `public List<EdgeInspectionOverlay> Overlays { get; set; }` — EdgePair 파생 클래스가 그대로 전달.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: MeasurementBase abstract 시그니처에 out overlays 추가</name>
  <files>WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (현재 시그니처 확인 — L45-50)
    - WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs (타입 네임스페이스 확인 — ReringProject.Halcon.Models)
    - .planning/phases/07-overlay-regression-fix/07-CONTEXT.md (D-01 시그니처 사본)
  </read_first>
  <action>
    파일 상단 using 블록에 두 개의 using을 추가:
    ```csharp
    using System.Collections.Generic; //260422 hbk Phase 7: List<T> (D-01)
    using ReringProject.Halcon.Models; //260422 hbk Phase 7: EdgeInspectionOverlay (D-01)
    ```
    (이미 존재하면 중복 추가하지 말고, 없는 것만 추가한다. 확인 방법: grep으로 `using System.Collections.Generic` 및 `using ReringProject.Halcon.Models` 기존재 여부 체크.)

    L45-50의 abstract TryExecute 선언을 다음으로 치환한다 (D-01 시그니처 verbatim):
    ```csharp
    public abstract bool TryExecute( //260413 hbk //260422 hbk Phase 7: out overlays 추가 (D-01)
        HImage image,
        HTuple datumTransform,
        double pixelResolution,
        out double resultValue,
        out string error,
        out List<EdgeInspectionOverlay> overlays);
    ```

    요약 주석(XML doc `///`)은 기존 내용 유지. 수정한 선언 라인 자체에는 `//260422 hbk Phase 7: out overlays 추가 (D-01)` 주석을 부착한다(CLAUDE.md memory 준수).

    이 task는 MeasurementBase만 수정한다. 파생 클래스는 다음 task에서 처리하므로 이 task 완료 직후 프로젝트는 일시적으로 컴파일 실패 상태(6개 override 미갱신)가 된다 — 정상. Plan 01 전체 완료 시점에만 빌드 통과를 판정한다.
  </action>
  <verify>
    <automated>grep -n "out List&lt;EdgeInspectionOverlay&gt; overlays" WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs</automated>
  </verify>
  <acceptance_criteria>
    - MeasurementBase.cs에 `out List<EdgeInspectionOverlay> overlays);` 라인 정확히 1건 존재 (grep)
    - MeasurementBase.cs에 `using System.Collections.Generic;` 존재 (grep)
    - MeasurementBase.cs에 `using ReringProject.Halcon.Models;` 존재 (grep)
    - MeasurementBase.cs에 `//260422 hbk` 주석 최소 1건 (수정 트레이서빌리티 — CLAUDE.md 규칙)
    - MeasurementBase.cs에 기존 abstract 5-파라미터 TryExecute는 존재하지 않는다: `grep -n "out string error);" MeasurementBase.cs` 결과 0건
  </acceptance_criteria>
  <done>MeasurementBase.TryExecute가 6-파라미터(out overlays 포함) abstract 시그니처로 선언됨.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: EdgePairDistanceMeasurement override에 result.Overlays 전달</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/EdgePairDistanceMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgePairDistanceMeasurement.cs (현재 override L37-92)
    - WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs (result.Overlays 출력 확인)
    - WPF_Example/Halcon/Models/FAIEdgeMeasurementResult.cs (Overlays 프로퍼티 존재 확인)
  </read_first>
  <action>
    파일 상단 using 블록에 추가(이미 있으면 스킵):
    ```csharp
    using System.Collections.Generic; //260422 hbk Phase 7: List<T> (D-01)
    ```
    (`using ReringProject.Halcon.Models;`는 이미 L5에 존재 — 재확인 후 미존재 시만 추가.)

    L37-42의 override TryExecute 시그니처를 다음으로 치환:
    ```csharp
    public override bool TryExecute( //260413 hbk //260422 hbk Phase 7: out overlays 추가 (D-01)
        HImage image,
        HTuple datumTransform,
        double pixelResolution,
        out double resultValue,
        out string error,
        out List<EdgeInspectionOverlay> overlays)
    ```

    메서드 본문의 변경 지점:
    1. L44-45 초기값 할당 블록에 `overlays = new List<EdgeInspectionOverlay>(); //260422 hbk Phase 7: 실패 경로에서도 non-null (D-02)` 추가.
       ```csharp
       resultValue = 0;
       error = null;
       overlays = new List<EdgeInspectionOverlay>(); //260422 hbk Phase 7: 실패 경로에서도 non-null (D-02)
       ```
    2. 성공 경로 L82-91 (service.TryMeasure 호출 이후 블록)을 수정:
       ```csharp
       var service = new FAIEdgeMeasurementService();
       FAIEdgeMeasurementResult result;
       if (!service.TryMeasure(image, temp, datumTransform, out result))
       {
           error = "FAIEdgeMeasurementService.TryMeasure failed";
           return false;
       }

       resultValue = result.DistanceMm;
       if (result.Overlays != null) overlays = result.Overlays; //260422 hbk Phase 7: service 산출 overlay 전달 (D-09, D-10)
       return true;
       ```
       (result.Overlays null-safe 처리 — null이면 초기값 빈 리스트 유지.)

    중간 실패 경로(image == null, Owner is not FAIConfig, service 실패)는 overlays 초기값(빈 리스트) 그대로 반환 — 추가 코드 불필요(D-02).

    EdgeThreshold / Sigma / EdgeDirection / 기타 기존 파라미터 및 ROI 복사 로직(L54-80)은 수정하지 않는다.
  </action>
  <verify>
    <automated>grep -n "out List&lt;EdgeInspectionOverlay&gt; overlays" WPF_Example/Custom/Sequence/Inspection/Measurements/EdgePairDistanceMeasurement.cs</automated>
  </verify>
  <acceptance_criteria>
    - EdgePairDistanceMeasurement.cs에 `out List<EdgeInspectionOverlay> overlays)` 존재 (grep 정확히 1건)
    - EdgePairDistanceMeasurement.cs에 정규식 `overlays\s*=\s*result\.Overlays` 매치 1건 (grep -E)
    - EdgePairDistanceMeasurement.cs에 `overlays = new List<EdgeInspectionOverlay>();` 최소 1건 (초기값)
    - 파일에 `//260422 hbk` 주석 최소 2건
    - FAIEdgeMeasurementService 또는 FAIEdgeMeasurementResult 파일은 수정되지 않았다: `git diff --name-only WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs WPF_Example/Halcon/Models/FAIEdgeMeasurementResult.cs` 출력 0건 (D-13)
  </acceptance_criteria>
  <done>EdgePair override가 새 시그니처로 컴파일되고 result.Overlays를 overlays out 파라미터로 노출.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 5종 Measurement override — 빈 overlay 리스트 반환</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/PointToLineDistanceMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/Measurements/PointToPointDistanceMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/Measurements/LineToLineAngleMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/Measurements/LineToLineDistanceMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/PointToLineDistanceMeasurement.cs (레퍼런스 패턴)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/PointToPointDistanceMeasurement.cs
    - WPF_Example/Custom/Sequence/Inspection/Measurements/LineToLineAngleMeasurement.cs
    - WPF_Example/Custom/Sequence/Inspection/Measurements/LineToLineDistanceMeasurement.cs
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs
  </read_first>
  <action>
    5개 파일 각각에 동일 패턴 적용. 레퍼런스: PointToLineDistanceMeasurement.cs L41-46 (현재 시그니처 라인).

    각 파일에서:

    1. 상단 using 블록에 추가(중복 방지 체크 후):
       ```csharp
       using System.Collections.Generic; //260422 hbk Phase 7: List<T> (D-01)
       using ReringProject.Halcon.Models; //260422 hbk Phase 7: EdgeInspectionOverlay (D-01)
       ```

    2. override TryExecute 시그니처를 다음으로 치환(현재는 5-파라미터 형태):
       ```csharp
       public override bool TryExecute( //260413 hbk //260422 hbk Phase 7: out overlays 추가 (D-01)
           HImage image,
           HTuple datumTransform,
           double pixelResolution,
           out double resultValue,
           out string error,
           out List<EdgeInspectionOverlay> overlays)
       ```

    3. 메서드 본문 최상단 `resultValue = 0; error = null;` 초기화 블록 직후에 한 줄 추가:
       ```csharp
       overlays = new List<EdgeInspectionOverlay>(); //260422 hbk Phase 7: 5종 overlay 미구현 — 빈 리스트 반환 (D-03)
       ```

    4. 나머지 측정 로직(svc.TryFitLine 호출 등)은 **절대 수정하지 않는다**. Phase 7 스코프는 시각화 회귀 복구 한정이며, 값 로직은 보존 대상(D-11, anti-goal).

    5개 파일 모두 동일 변경 패턴이며 다른 수정은 없다. 파일별 기존 ROI/Edge 파라미터 필드나 본문 알고리즘은 건드리지 않는다.
  </action>
  <verify>
    <automated>for f in PointToLineDistanceMeasurement PointToPointDistanceMeasurement LineToLineAngleMeasurement LineToLineDistanceMeasurement CircleDiameterMeasurement; do grep -l "out List&lt;EdgeInspectionOverlay&gt; overlays" "WPF_Example/Custom/Sequence/Inspection/Measurements/$f.cs" || echo "MISSING: $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - 5개 파일 각각에 `out List<EdgeInspectionOverlay> overlays)` 문자열 존재 (grep 각 파일 1건)
    - 5개 파일 각각에 `overlays = new List<EdgeInspectionOverlay>();` 문자열 정확히 1건
    - 5개 파일 각각에 `//260422 hbk` 주석 최소 2건
    - 5개 파일에서 기존 `out string error);` 종결 시그니처는 모두 사라졌다: `grep -l "out string error);" WPF_Example/Custom/Sequence/Inspection/Measurements/*.cs` 결과 0건
    - 5개 파일 안에 `result.Overlays` 문자열이 없다 (EdgePair가 아닌 5종은 service 호출 자체가 없음): `grep -n "result\.Overlays" WPF_Example/Custom/Sequence/Inspection/Measurements/PointToLineDistanceMeasurement.cs WPF_Example/Custom/Sequence/Inspection/Measurements/PointToPointDistanceMeasurement.cs WPF_Example/Custom/Sequence/Inspection/Measurements/LineToLineAngleMeasurement.cs WPF_Example/Custom/Sequence/Inspection/Measurements/LineToLineDistanceMeasurement.cs WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs` 결과 0건
  </acceptance_criteria>
  <done>5종 Measurement override가 새 시그니처로 컴파일되고 모두 빈 overlay 리스트를 out 파라미터로 반환.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: 빌드 검증 — Debug x64</name>
  <files>(빌드 산출물만; 소스 변경 없음)</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (Task 1 결과 재확인)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgePairDistanceMeasurement.cs
    - WPF_Example/Custom/Sequence/Inspection/Measurements/PointToLineDistanceMeasurement.cs
  </read_first>
  <action>
    전체 솔루션 빌드. 명령:
    ```
    msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal
    ```
    종료 코드 0이 아닐 경우:
    1. 출력에서 error CS* 라인을 찾는다.
    2. 누락된 override가 있다면 Task 1-3로 회귀하여 해당 파일 재점검.
    3. Action_FAIMeasurement.cs에서 호출부가 시그니처 불일치 오류(CS1501/CS7036)를 내는 경우 — 이는 Plan 02의 작업이다. Plan 01 스코프에서는 Action_FAIMeasurement.cs의 호출부 시그니처 불일치 **오류는 허용되지 않는다**(Plan 01 완료 시점에 솔루션 전체 빌드 통과가 must-have).

    **중요:** Plan 01만으로는 Action_FAIMeasurement의 호출부(L157)가 여전히 5-파라미터 호출이므로 빌드 실패가 예상된다. 따라서 이 task는 **Plan 01 종료 조건을 완화**한다:
    - MeasurementBase + 6개 파생 클래스 파일에 대해서만 **개별 파일 컴파일 확인**(시그니처 일관성).
    - 솔루션 전체 빌드는 Plan 02 Task 2에서 통과 조건으로 검증.

    따라서 실제 수행 명령:
    ```
    msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /t:Rebuild 2>&1 | tee build.log
    ```
    build.log에서 기대 동작:
    - `MeasurementBase.cs`, `EdgePairDistanceMeasurement.cs`, `PointToLineDistanceMeasurement.cs`, `PointToPointDistanceMeasurement.cs`, `LineToLineAngleMeasurement.cs`, `LineToLineDistanceMeasurement.cs`, `CircleDiameterMeasurement.cs` 관련 **CS0534 (override 미구현)** 오류가 0건이어야 한다.
    - `Action_FAIMeasurement.cs` 관련 **CS1501 / CS7036 / CS1739** (호출부 파라미터 수 불일치) 오류는 허용 — Plan 02가 처리.
    - 그 외 신규 오류는 허용되지 않는다.

    build.log를 .planning/phases/07-overlay-regression-fix/07-01-BUILD.log로 복사하여 증거 보존.
  </action>
  <verify>
    <automated>grep -E "error CS0534" .planning/phases/07-overlay-regression-fix/07-01-BUILD.log 2>&amp;1 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - 07-01-BUILD.log 파일 존재
    - 07-01-BUILD.log에 `error CS0534` (override 미구현) 0건
    - 07-01-BUILD.log에 MeasurementBase.cs / 6개 파생 클래스 파일명 + `error` 조합 0건 (grep `error.*MeasurementBase\.cs\|error.*DistanceMeasurement\.cs\|error.*AngleMeasurement\.cs\|error.*DiameterMeasurement\.cs`)
    - Action_FAIMeasurement 관련 오류는 존재할 수 있으나 유형은 CS1501/CS7036/CS1739 중 하나여야 함 (호출부 파라미터 수 문제 → Plan 02에서 해결 예정)
  </acceptance_criteria>
  <done>6-파라미터 시그니처가 abstract + 6개 파생 override 전체에서 일관되게 컴파일됨. Action_FAIMeasurement 호출부 오류는 Plan 02에 인계.</done>
</task>

</tasks>

<verification>
- [ ] MeasurementBase.cs abstract TryExecute에 `out List<EdgeInspectionOverlay> overlays` 존재
- [ ] 6개 파생 클래스(EdgePair/PointToLine/PointToPoint/LineToLineAngle/LineToLineDistance/CircleDiameter)가 전부 6-파라미터 override
- [ ] EdgePair만 `overlays = result.Overlays` 전달, 5종은 `overlays = new List<EdgeInspectionOverlay>()` 빈 리스트
- [ ] EdgeInspectionOverlay.cs, FAIEdgeMeasurementService.cs, FAIEdgeMeasurementResult.cs, HalconDisplayService.cs 파일 **수정 없음**
- [ ] 빌드 로그에서 CS0534(override 미구현) 0건
- [ ] 모든 수정 라인에 `//260422 hbk` 주석 부착
</verification>

<success_criteria>
- 6개 파생 Measurement 클래스가 새 abstract 시그니처와 일치하여 override 누락 오류 없음
- EdgePair는 FAIEdgeMeasurementService 산출 overlay를 out 파라미터로 전달 가능한 상태
- 5종은 빈 리스트로 Phase 7 스코프 최소화 원칙 준수(D-03, D-11)
- Plan 02가 Action_FAIMeasurement 루프를 재작성할 수 있는 시그니처 기반 완성
</success_criteria>

<output>
After completion, create `.planning/phases/07-overlay-regression-fix/07-01-SUMMARY.md`
</output>
