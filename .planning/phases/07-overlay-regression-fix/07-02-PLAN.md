---
phase: 07-overlay-regression-fix
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: false
requirements: [ALG-04]
tags: [wpf, halcon, measurement, overlay, action]

must_haves:
  truths:
    - "Action_FAIMeasurement Measure 루프가 각 Measurement의 overlay를 누적한다 (D-04, D-05)"
    - "루프 종료 후 `pMyContext.InspectionOverlays = new List<EdgeInspectionOverlay>()` 초기화 라인이 더 이상 존재하지 않는다 (L190 블로커 제거)"
    - "판정 결과에 따라 `FAI-Edge*` overlay의 RoiId에 `-OK` 또는 `-NG` suffix가 부여된다 (D-06, D-07, D-08)"
    - "FAI-DistLine overlay는 suffix 없이 원본 ID 그대로 유지된다 (D-07)"
    - "SIMUL_MODE + D:\\1.bmp에서 Top 시퀀스 실행 시 캔버스에 녹/적 에지 + 청록 DistLine 재표시된다 (D-14)"
    - "빌드 (msbuild Debug/x64) 통과 — 오류 0"
    - "측정 판정 / DataGrid 값 흐름은 변경되지 않는다 (EvaluateJudgement, meas.LastJudgement, fai.IsPass, fai.MeasuredValue 보존)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "overlay 누적 + suffix 부여 + 초기화 라인 제거가 반영된 Measure 스텝"
      contains: "overlayAcc"
    - path: ".planning/phases/07-overlay-regression-fix/07-02-BUILD.log"
      provides: "빌드 통과 증거 — error 0건"
      contains: "Build succeeded"
  key_links:
    - from: "Action_FAIMeasurement.Measure 루프"
      to: "meas.TryExecute(...out measOverlays)"
      via: "Plan 01에서 확장된 out overlays 파라미터 호출"
      pattern: "out\\s+measOverlays|out\\s+List<EdgeInspectionOverlay>\\s+measOverlays"
    - from: "overlayAcc 누적"
      to: "pMyContext.InspectionOverlays"
      via: "루프 종료 후 대입"
      pattern: "pMyContext\\.InspectionOverlays\\s*=\\s*overlayAcc"
    - from: "EvaluateJudgement 결과"
      to: "overlay RoiId -OK/-NG suffix"
      via: "StartsWith(\"FAI-Edge\") 조건부 suffix"
      pattern: "StartsWith\\(\"FAI-Edge\""
---

<objective>
Action_FAIMeasurement.cs의 Measure 스텝 루프를 재작성하여 (1) 각 Measurement가 반환한 overlay를 `overlayAcc`에 누적, (2) `EvaluateJudgement` 호출 후 판정 결과를 overlay RoiId에 `-OK`/`-NG` suffix로 부여, (3) L190의 `pMyContext.InspectionOverlays = new List<EdgeInspectionOverlay>()` 초기화 블로커 라인을 `pMyContext.InspectionOverlays = overlayAcc`로 대체한다. SIMUL_MODE 육안 검증으로 Phase 3 시각화(녹/적 에지 + 청록 DistLine) 복구를 확인한다.

Purpose: Phase 6이 만든 시각화 회귀의 유일한 원인 지점(L190)을 수리하여 ALG-04 요구사항 복구.
Output: Action_FAIMeasurement.cs 수정 + 전체 빌드 통과 + SIMUL_MODE 육안 확인 사인오프.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-overlay-regression-fix/07-CONTEXT.md
@.planning/phases/07-overlay-regression-fix/07-01-PLAN.md

<interfaces>
<!-- Plan 01 완료 후 사용 가능한 시그니처 -->

From WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (Plan 01 결과):
```csharp
public abstract bool TryExecute(
    HImage image,
    HTuple datumTransform,
    double pixelResolution,
    out double resultValue,
    out string error,
    out List<EdgeInspectionOverlay> overlays);
```

From WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs:
- `string RoiId { get; set; }` — `FAI-Edge1`, `FAI-Edge2`, `FAI-DistLine` 중 하나
- `FAI-Edge*`에만 `-OK`/`-NG` suffix 부여(D-06, D-07).

From WPF_Example/Halcon/Display/HalconDisplayService.cs L127-137 (수정 금지, 참고):
- `FAI-Edge*-OK` → 녹색
- `FAI-Edge*-NG` → 적색
- `FAI-DistLine` → 청록

기존 Measure 스텝 구조 (Action_FAIMeasurement.cs L134-193, 수정 전):
- `foreach (var fai in ShotParam.FAIList)` 외부 루프
- `foreach (var meas in fai.Measurements)` 내부 루프
- `meas.TryExecute(image, transform, fai.PixelResolutionX, out resultValue, out measError)` — Plan 01 후 6-parameter로 업데이트 필요
- `fai.IsPass = faiAllPass`, `fai.MeasuredValue = fai.Measurements[0].LastMeasuredValue` 집계 로직 — **보존 대상**
- L188-191: `pMyContext.AllPass` / `MeasuredCount` / `InspectionOverlays = new List<...>()` / `Step = End` — 이 중 InspectionOverlays 라인만 교체
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Measure 루프 재작성 — overlay 누적 + suffix 부여 + 초기화 라인 제거</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (L100-200 — Measure 스텝 전체 구조, 특히 L134-193)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (Plan 01 결과 — 6-파라미터 TryExecute 시그니처)
    - WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs (RoiId 프로퍼티 setter 확인)
    - .planning/phases/07-overlay-regression-fix/07-CONTEXT.md (D-04 ~ D-08, `<specifics>` 섹션 템플릿 verbatim)
  </read_first>
  <action>
    파일 상단 using 블록에 추가(존재 확인 후 미존재 시에만):
    ```csharp
    using System; //260422 hbk Phase 7: StringComparison (D-06)
    using System.Collections.Generic; //260422 hbk Phase 7: List<T>
    using ReringProject.Halcon.Models; //260422 hbk Phase 7: EdgeInspectionOverlay
    ```
    (기존 using 중 중복된 건 추가 금지. 특히 `using System;`은 거의 확실히 존재 — 확인 필수.)

    L134-193 `case EStep.Measure:` 블록을 아래 형태로 치환. **기존 FAI 집계(fai.IsPass / fai.MeasuredValue) 로직과 parentSeq2 / transform fallback 로직은 반드시 보존**:

    ```csharp
    //260413 hbk Phase 6: FAI 루프 → Measurement 루프로 재설계 (D-09, D-10, D-20)
    //260422 hbk Phase 7: overlay 누적 + 판정 suffix 부여 (D-04 ~ D-08)
    case EStep.Measure: {
        var parentSeq2 = ShotParam != null ? ShotParam.Parent as InspectionSequence : null;
        bool allPass = true;
        int measuredCount = 0;
        var overlayAcc = new List<EdgeInspectionOverlay>(); //260422 hbk Phase 7: Shot 단위 overlay 누적 (D-04, D-05)
        if (ShotParam != null) {
            using (var image = ShotParam.GetImage()) {
                if (image != null) {
                    foreach (var fai in ShotParam.FAIList) {
                        bool faiAllPass = true;
                        foreach (var meas in fai.Measurements) {
                            HTuple transform;
                            if (parentSeq2 == null || !parentSeq2.TryGetDatumTransform(meas.DatumRef, out transform)) {
                                //260413 hbk Fixture 미존재 또는 미지정 DatumRef → identity fallback
                                try {
                                    HOperatorSet.HomMat2dIdentity(out transform);
                                } catch {
                                    transform = new HTuple();
                                }
                            }
                            double resultValue;
                            string measError;
                            List<EdgeInspectionOverlay> measOverlays; //260422 hbk Phase 7: (D-01)
                            bool ok = false;
                            try {
                                ok = meas.TryExecute(image, transform, fai.PixelResolutionX, out resultValue, out measError, out measOverlays); //260422 hbk Phase 7: 6-param (D-01)
                            } catch (Exception ex) {
                                ok = false;
                                resultValue = 0;
                                measError = ex.Message;
                                measOverlays = null; //260422 hbk Phase 7: 예외 경로 null-safe (D-02)
                            }
                            if (ok) {
                                meas.EvaluateJudgement(resultValue);
                            } else {
                                Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Measurement '" + (meas.MeasurementName ?? meas.TypeName) + "' failed: " + (measError ?? ""));
                                meas.ClearResult();
                                meas.LastJudgement = false;
                            }
                            //260422 hbk Phase 7: FAI-Edge* overlay에 판정 suffix 부여 (D-06, D-07, D-08)
                            if (measOverlays != null) {
                                string suffix = meas.LastJudgement ? "-OK" : "-NG";
                                foreach (var ov in measOverlays) {
                                    if (ov == null) continue;
                                    if (string.IsNullOrEmpty(ov.RoiId)) continue;
                                    if (ov.RoiId.StartsWith("FAI-Edge", StringComparison.OrdinalIgnoreCase)) {
                                        ov.RoiId = ov.RoiId + suffix;
                                    }
                                    //260422 hbk FAI-DistLine 등은 suffix 미부여 — 청록 고정 (D-07)
                                }
                                overlayAcc.AddRange(measOverlays); //260422 hbk Phase 7: Shot 단위 누적 (D-04)
                            }
                            if (!meas.LastJudgement) {
                                faiAllPass = false;
                            }
                            measuredCount++;
                        }
                        //260413 hbk FAIConfig legacy 필드(IsPass/MeasuredValue)에 대표 결과 집계 —
                        // 첫 Measurement 결과를 사용해 UI/TCP 호환 유지
                        if (fai.Measurements.Count > 0) {
                            fai.IsPass = faiAllPass;
                            fai.MeasuredValue = fai.Measurements[0].LastMeasuredValue;
                        } else {
                            fai.ClearResult();
                        }
                        if (!faiAllPass) allPass = false;
                    }
                }
            }
        }
        pMyContext.AllPass = allPass;
        pMyContext.MeasuredCount = measuredCount;
        pMyContext.InspectionOverlays = overlayAcc; //260422 hbk Phase 7: 초기화 라인 교체 — overlay 누적 결과 반영 (D-04, Gap I1)
        Step = (int)EStep.End;
        break;
    }
    ```

    핵심 변경 포인트:
    1. `var overlayAcc = new List<EdgeInspectionOverlay>();` 루프 진입 전 선언.
    2. `TryExecute` 호출에 `out measOverlays` 6번째 인자 추가.
    3. `EvaluateJudgement` / `ClearResult` 호출 직후 suffix 부여 블록 추가.
    4. **L190 `pMyContext.InspectionOverlays = new List<EdgeInspectionOverlay>();` 라인을 `pMyContext.InspectionOverlays = overlayAcc;` 로 교체**.
    5. 기존 `fai.IsPass` / `fai.MeasuredValue` 집계 로직 **변경 없음**(anti-goal).
    6. 기존 `EStep.Grab`, `EStep.End`, `GrabOrLoadDatumImage` 등 다른 스텝은 **수정 금지**.

    주의사항:
    - C# 7.2 제약: `out var` 인라인 선언 허용되지만 가독성 위해 기존 스타일대로 `List<EdgeInspectionOverlay> measOverlays;` 사전 선언 권장.
    - 기존 파일의 brace style은 K&R(open brace on same line) — 유지.
    - 기존 파일은 들여쓰기 4-space. 유지.
  </action>
  <verify>
    <automated>grep -nE "pMyContext\.InspectionOverlays\s*=\s*overlayAcc" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</automated>
  </verify>
  <acceptance_criteria>
    - `pMyContext.InspectionOverlays = overlayAcc;` 정확히 1건 존재 (grep)
    - `pMyContext.InspectionOverlays = new List<EdgeInspectionOverlay>();` 존재하지 않음: `grep -c "pMyContext\.InspectionOverlays\s*=\s*new List" Action_FAIMeasurement.cs` 결과 0
    - `var overlayAcc = new List<EdgeInspectionOverlay>();` 정확히 1건
    - `out measOverlays` 문자열 최소 1건 (TryExecute 호출부)
    - `StartsWith("FAI-Edge"` 최소 1건 (suffix 조건부 부여)
    - `"-OK"` 과 `"-NG"` 문자열 각각 최소 1건
    - `overlayAcc.AddRange` 최소 1건
    - `fai.IsPass = faiAllPass;` 보존 (grep 1건)
    - `fai.MeasuredValue = fai.Measurements[0].LastMeasuredValue;` 보존 (grep 1건)
    - `//260422 hbk` 주석 최소 4건 (수정 트레이서빌리티)
  </acceptance_criteria>
  <done>Measure 루프가 overlay를 누적하고 판정 suffix를 부여하며, InspectionOverlays 블로커 라인이 제거됨.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 전체 솔루션 빌드 통과 검증</name>
  <files>.planning/phases/07-overlay-regression-fix/07-02-BUILD.log</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (Task 1 결과 재확인)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (Plan 01 결과)
  </read_first>
  <action>
    전체 솔루션 재빌드 실행:
    ```
    msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /t:Rebuild 2>&amp;1 | tee .planning/phases/07-overlay-regression-fix/07-02-BUILD.log
    ```

    기대 결과:
    - `Build succeeded.` 포함
    - `0 Error(s)` 포함
    - 경고는 허용 (Phase 7 스코프에서 warning 0 의무 없음)

    빌드 실패 시:
    - CS1501/CS7036/CS1739(파라미터 불일치) → Task 1 TryExecute 호출부 재확인(6-param).
    - CS0246(타입 미발견 — EdgeInspectionOverlay / List<T>) → using 누락 점검.
    - CS0534(override 미구현) → Plan 01 회귀. Plan 01의 파생 클래스 수정 재확인.

    빌드 통과 확인 후, 이 task 완료. 실제 시퀀스 실행 및 육안 검증은 다음 checkpoint task.
  </action>
  <verify>
    <automated>grep -c "0 Error" .planning/phases/07-overlay-regression-fix/07-02-BUILD.log</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07-overlay-regression-fix/07-02-BUILD.log` 파일 존재
    - 로그에 `Build succeeded.` 문자열 1건 이상 (grep)
    - 로그에 `0 Error(s)` 문자열 1건 이상 (grep — 정확 형식 msbuild 15.0 표준)
    - 로그에 `error CS` 패턴 0건 (`grep -c "error CS" 07-02-BUILD.log` → 0)
    - `DatumMeasurement.exe`가 빌드 출력 디렉터리에 갱신됨: `bin/x64/Debug/DatumMeasurement.exe` 존재 (WR test — ls 또는 파일 수정 시각 확인)
  </acceptance_criteria>
  <done>솔루션 전체가 Debug/x64로 error 0건 빌드 통과. 실행 가능한 EXE 산출.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SIMUL_MODE 육안 검증 — 에지 overlay 복구 확인</name>
  <files>.planning/phases/07-overlay-regression-fix/07-02-UAT-shots/ (스크린샷 증거 저장 경로)</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (Task 1 결과)
    - WPF_Example/Halcon/Display/HalconDisplayService.cs (L127-137 색상 분기 — 수정 금지, 진단용 참고)
  </read_first>
  <what-built>
    - MeasurementBase.TryExecute 시그니처에 out overlays 파라미터 신설 (Plan 01)
    - Action_FAIMeasurement Measure 루프가 EdgePair overlay를 누적 + 판정 suffix 부여 (Plan 02 Task 1)
    - 솔루션 Debug/x64 빌드 통과 (Plan 02 Task 2)
  </what-built>
  <how-to-verify>
    준비:
    1. `bin/x64/Debug/DatumMeasurement.exe` 실행 (Debug SIMUL_MODE 빌드).
    2. Top Shot에 EdgePair 타입 Measurement 1개 이상이 설정된 기존 레시피 로드.
       - 기존 Phase 6 UAT 레시피(rapid-city) 재사용 가능.
       - Shot의 SimulImagePath 가 `D:\1.bmp`로 설정돼 있는지 확인.
    3. 메인 뷰에서 Top 시퀀스 1회 실행(또는 TCP 테스트 명령) — Measure 스텝이 실행되어 InspectionOverlays가 pMyContext에 주입되어야 한다.

    검증:
    4. 캔버스(이미지 뷰)에 다음 3종 overlay가 **표시**되는지 확인:
       - 에지 라인 2개 (FAI-Edge1, FAI-Edge2): 판정 OK이면 **녹색**, NG이면 **적색**.
       - 거리 라인 1개 (FAI-DistLine): **청록색** (판정 무관).
    5. 공차를 의도적으로 좁게 설정(tolerance ±0.001 등)하여 NG를 유도 → 에지 2줄이 **적색**으로 바뀌는지 확인 (suffix -NG 경로).
    6. 공차를 넓게(±10 등) 복원하여 **녹색** 복귀 확인 (suffix -OK 경로).
    7. DataGrid / 결과 테이블의 **측정값(mm)과 OK/NG 문자열은 변화 없이 정상 표시**됨 확인 (값 로직 anti-goal).

    비교 기준:
    - Phase 3 UAT 시점의 시각화 재현이 기준. Phase 6 이전 기록된 스크린샷이 있다면 대조.
    - 회귀 전/후 비교 스크린샷을 `.planning/phases/07-overlay-regression-fix/07-02-UAT-shots/` 디렉터리에 저장 (OK shot, NG shot 각 1장).

    실패 기준:
    - 에지 overlay가 전혀 표시되지 않음 → InspectionOverlays 주입 실패 → Task 1 코드 재확인.
    - 색상이 녹/적 대신 회색/파랑 등 → suffix 부여 실패 또는 RoiId 규약 불일치 → HalconDisplayService L127-137 참고(수정은 금지, 원인만 진단).
    - DistLine이 표시되지 않음 → FAIEdgeMeasurementService.BuildOverlaysBoth/Single 호출 경로 확인(EdgeSelection == "Both" 인지).
    - 측정값 또는 판정 결과가 바뀜 → 알고리즘 로직에 의도치 않은 변경 발생 → Task 1 코드 diff 재검토.
  </how-to-verify>
  <action>
    사용자가 위 `how-to-verify` 단계를 실행한다. 실행자(Claude)는 스크린샷 저장 디렉터리만 사전에 생성한다:
    ```
    mkdir -p .planning/phases/07-overlay-regression-fix/07-02-UAT-shots
    ```
    그 후 사용자의 응답을 기다린다. 응답이 "approved"이면 다음 단계로, 문제 보고이면 Task 1으로 회귀하여 원인 진단.
  </action>
  <verify>
    <automated>ls .planning/phases/07-overlay-regression-fix/07-02-UAT-shots/ 2>&amp;1</automated>
  </verify>
  <acceptance_criteria>
    - 사용자로부터 "approved" 또는 "passed — screenshots saved" 응답 수신
    - `.planning/phases/07-overlay-regression-fix/07-02-UAT-shots/` 디렉터리 존재
    - OK 상태 스크린샷 최소 1장 + NG 상태 스크린샷 최소 1장 저장 (사용자 제공)
  </acceptance_criteria>
  <done>SIMUL_MODE 시퀀스 실행에서 Phase 3 overlay 규약(녹/적 에지 + 청록 DistLine) 재현 확인 + 사용자 승인.</done>
  <resume-signal>OK shot + NG shot 확인 후 "approved" 또는 "passed — screenshots saved" 응답. 문제 발견 시 상세 기술.</resume-signal>
</task>

</tasks>

<verification>
- [ ] Action_FAIMeasurement.cs Measure 루프에 `overlayAcc` + `AddRange` 누적 로직 존재
- [ ] L190 초기화 라인 (`InspectionOverlays = new List<EdgeInspectionOverlay>()`) 제거됨 (grep 0건)
- [ ] `StartsWith("FAI-Edge"` + `-OK`/`-NG` suffix 코드 존재
- [ ] 솔루션 빌드 통과 (Debug/x64, error 0)
- [ ] SIMUL_MODE에서 EdgePair FAI의 녹/적 에지 + 청록 DistLine 재표시 확인
- [ ] 공차 조정 시 OK/NG 색상 전환 동작 확인
- [ ] DataGrid 측정값 / 판정 문자열 정상 유지 (값 로직 anti-goal)
- [ ] HalconDisplayService / FAIEdgeMeasurementService / EdgeInspectionOverlay 파일 변경 없음 (git diff)
</verification>

<success_criteria>
- Gap I1 해소: Action_FAIMeasurement.cs:190의 overlay 초기화 블로커 제거
- ALG-04 요구사항(시각화 회귀 복구) 충족
- Phase 3 에지 overlay 규약(FAI-Edge*-OK/NG 녹적 + FAI-DistLine 청록) SIMUL_MODE에서 재현
- 측정값 / 공차 판정 / DataGrid 흐름 변경 없음
- Phase 9 VERIFICATION 작업의 사전조건 완성
</success_criteria>

<output>
After completion, create `.planning/phases/07-overlay-regression-fix/07-02-SUMMARY.md`
</output>
