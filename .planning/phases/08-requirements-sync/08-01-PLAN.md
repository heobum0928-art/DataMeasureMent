---
phase: 08-requirements-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [RC-01, RC-02, RC-03, RC-04, RC-05, RC-06]
tags: [doc, requirements, traceability]

must_haves:
  truths:
    - "REQUIREMENTS.md v1 Requirements 블록 안에 'Rapid City 확장' 섹션이 존재하고 RC-01..RC-06 체크박스 6개([x])가 모두 기재되어 있다"
    - "Traceability 표에서 Phase 2/3/4/5/6에 속한 모든 요구사항 행의 Status가 실제 구현 상태와 일치한다 (TCH-01/TCH-02/ALG-03/SEQ-01..SEQ-04/ALG-04/RC-01..RC-06 = Complete)"
    - "Coverage 주석 하단의 'RC-01..RC-06은 Phase 6에서 구현되었으나 본 문서 정의/본문 체크박스 등록은 Phase 8...' 문장이 제거되었다"
    - "문서 하단 Last-updated 꼬리표가 2026-04-23 Phase 8 동기화 내용으로 갱신되어 있다"
    - "문서 수정 외에는 어떤 코드/설정 파일도 변경되지 않는다"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "RC-01..RC-06 정의 6건 + Traceability 갱신 + Coverage 갱신"
      contains: "## Rapid City 확장"
  key_links:
    - from: "REQUIREMENTS.md 본문 RC 체크박스"
      to: "Traceability 표 RC 행 Status"
      via: "두 위치 모두 Complete로 일관"
      pattern: "RC-0[1-6].*Complete"
    - from: "REQUIREMENTS.md 본문 TCH/SEQ/ALG-04 체크박스"
      to: "Traceability 표 해당 행 Status"
      via: "체크박스 [x]와 Status Complete 동기"
      pattern: "\\*\\*(TCH-0[12]|ALG-0[34]|SEQ-0[1-4])\\*\\*"
---

<objective>
Phase 8 — 요구사항 & 트레이서빌리티 동기화

Purpose: Phase 6에서 구현되었으나 `.planning/REQUIREMENTS.md`에 정식 등록되지 않은 RC-01..RC-06을 문서화하고, Phase 2/3/5/7의 완료 내역이 Traceability 표에 반영되지 않은 stale 상태를 정합화한다. v1.0 감사 Gap G1(RC orphan) + G2(stale traceability)를 해소한다.

Output: `.planning/REQUIREMENTS.md` 단일 파일 수정. 세 가지 변화 — (1) v1 Requirements 블록 안 "Rapid City 확장" 섹션 신설(RC-01..RC-06 체크박스 6건), (2) Traceability 표 Status 정합화(10행 갱신), (3) Coverage 하단 과도기 주석 삭제 + Last-updated 꼬리표 갱신.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-requirements-sync/08-CONTEXT.md
@.planning/phases/06-rapid-city/06-RESEARCH.md
@.planning/phases/06-rapid-city/06-02-SUMMARY.md
@.planning/phases/06-rapid-city/06-03-SUMMARY.md
@.planning/phases/06-rapid-city/06-04-SUMMARY.md
@.planning/phases/07-overlay-regression-fix/07-02-SUMMARY.md
@.planning/v1.0-MILESTONE-AUDIT.md

<source_strings>
<!-- 이 섹션은 본문에 삽입할 정확한 문자열을 제공한다. 실행자는 이를 그대로 복사하여 사용할 것. -->

RC-01 한 줄 정의 (D-01 기반, 06-RESEARCH L90 첫 문장 발췌):
  Sequence가 Fixture(한 면)로 동작하며 List<DatumConfig>를 소유한다

RC-02 한 줄 정의:
  DatumConfig가 ShotConfig에서 Sequence 레벨로 승격되어 Multi-Datum을 지원한다

RC-03 한 줄 정의:
  MeasurementBase 파생 클래스 6종이 각각 TryExecute()로 측정을 수행한다

RC-04 한 줄 정의:
  ShotConfig에 Ring/Back/Coax/Side 조명 필드가 추가되고 INI로 저장/로드된다

RC-05 한 줄 정의:
  새 INI 포맷으로 레시피 저장/로드가 동작하고, 기존 포맷은 안내 메시지를 표시한다

RC-06 한 줄 정의:
  UI 트리에서 Sequence > Datum + Shot > FAI > Measurement 구조를 탐색할 수 있고, 결과 테이블이 Measurement 단위로 표시된다
</source_strings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 'Rapid City 확장' 섹션 신설 (RC-01..RC-06 정의 6건)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (현재 97 lines, L1-50 형식 확인 + L31-36 "검사 시퀀스" 섹션 종단 지점 확인)
    - .planning/phases/08-requirements-sync/08-CONTEXT.md (D-01, D-02, D-06 결정사항)
    - .planning/phases/06-rapid-city/06-RESEARCH.md L88-95 (RC-01..RC-06 한 줄 원전)
  </read_first>
  <action>
기존 "### 검사 시퀀스" 섹션(현재 L31-36)과 "## v2 Requirements" 섹션(현재 L38) 사이에 새 섹션을 삽입한다. D-06: "Rapid City 확장" 섹션은 "검사 시퀀스" 다음, v2 Requirements 앞에 배치.

현재 L36 다음 줄(L37 빈 줄) 바로 다음에 아래 블록을 삽입하라:

```
### Rapid City 확장

- [x] **RC-01**: Sequence가 Fixture(한 면)로 동작하며 List<DatumConfig>를 소유한다
- [x] **RC-02**: DatumConfig가 ShotConfig에서 Sequence 레벨로 승격되어 Multi-Datum을 지원한다
- [x] **RC-03**: MeasurementBase 파생 클래스 6종이 각각 TryExecute()로 측정을 수행한다
- [x] **RC-04**: ShotConfig에 Ring/Back/Coax/Side 조명 필드가 추가되고 INI로 저장/로드된다
- [x] **RC-05**: 새 INI 포맷으로 레시피 저장/로드가 동작하고, 기존 포맷은 안내 메시지를 표시한다
- [x] **RC-06**: UI 트리에서 Sequence > Datum + Shot > FAI > Measurement 구조를 탐색할 수 있고, 결과 테이블이 Measurement 단위로 표시된다

```

중요 제약:
- 섹션 제목은 정확히 "### Rapid City 확장" (앞에 "Rapid City 확장 (Phase 6)" 같은 수식 불가 — Traceability 표와 일관된 담백한 톤)
- 체크박스는 모두 [x] (D-02)
- 각 항목 형식은 기존 UI/TCH/ALG/SEQ와 동일: `- [x] **ID**: 한 줄 설명`
- 설명 문자열은 위에 제시된 `<source_strings>` 블록의 내용을 그대로 사용 (D-01: RESEARCH L90-95 기반 경량 리라이트, Acceptance criteria/Gap 메타 없음)
- 섹션 뒤에 빈 줄 1개 (기존 섹션 구분 패턴 유지)
- "## v2 Requirements" 헤더 및 본문은 건드리지 않는다
- 이 task는 본문 정의 추가만 담당. Traceability 표 갱신은 Task 2.
  </action>
  <verify>
    <automated>grep -c "^### Rapid City 확장$" .planning/REQUIREMENTS.md | grep -q "^1$" && grep -c "^- \[x\] \*\*RC-0[1-6]\*\*:" .planning/REQUIREMENTS.md | grep -q "^6$" && grep -q "^- \[x\] \*\*RC-01\*\*: Sequence가 Fixture(한 면)로 동작하며 List<DatumConfig>를 소유한다$" .planning/REQUIREMENTS.md && grep -q "^- \[x\] \*\*RC-06\*\*: UI 트리에서 Sequence > Datum + Shot > FAI > Measurement 구조를 탐색할 수 있고, 결과 테이블이 Measurement 단위로 표시된다$" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### Rapid City 확장$" .planning/REQUIREMENTS.md` == 1
    - `grep -c "^- \[x\] \*\*RC-0[1-6]\*\*:" .planning/REQUIREMENTS.md` == 6 (정확히 6행)
    - `grep -q "^- \[x\] \*\*RC-01\*\*: Sequence가 Fixture(한 면)로 동작하며 List<DatumConfig>를 소유한다$" .planning/REQUIREMENTS.md` 매칭 성공
    - `grep -q "^- \[x\] \*\*RC-02\*\*: DatumConfig가 ShotConfig에서 Sequence 레벨로 승격되어 Multi-Datum을 지원한다$" .planning/REQUIREMENTS.md` 매칭 성공
    - `grep -q "^- \[x\] \*\*RC-03\*\*: MeasurementBase 파생 클래스 6종이 각각 TryExecute()로 측정을 수행한다$" .planning/REQUIREMENTS.md` 매칭 성공
    - `grep -q "^- \[x\] \*\*RC-04\*\*: ShotConfig에 Ring/Back/Coax/Side 조명 필드가 추가되고 INI로 저장/로드된다$" .planning/REQUIREMENTS.md` 매칭 성공
    - `grep -q "^- \[x\] \*\*RC-05\*\*: 새 INI 포맷으로 레시피 저장/로드가 동작하고, 기존 포맷은 안내 메시지를 표시한다$" .planning/REQUIREMENTS.md` 매칭 성공
    - `grep -q "^- \[x\] \*\*RC-06\*\*: UI 트리에서 Sequence > Datum + Shot > FAI > Measurement 구조를 탐색할 수 있고, 결과 테이블이 Measurement 단위로 표시된다$" .planning/REQUIREMENTS.md` 매칭 성공
    - 섹션은 "### 검사 시퀀스" 블록 이후, "## v2 Requirements" 이전에 위치 (순서 확인: `grep -n "^### 검사 시퀀스\|^### Rapid City 확장\|^## v2 Requirements" .planning/REQUIREMENTS.md` 출력 line 번호가 오름차순으로 3행)
  </acceptance_criteria>
  <done>
    - "### Rapid City 확장" 섹션이 정확한 위치에 존재 ("검사 시퀀스" 다음, "v2 Requirements" 앞)
    - RC-01..RC-06 여섯 줄 모두 [x] 체크박스 + 한 줄 정의로 기재됨
    - 각 정의 문자열이 `<source_strings>` 블록과 정확히 일치
    - v2 Requirements 이하 기존 내용 무변경 (섹션 본문 외 파일 영역은 변경 없음)
  </done>
</task>

<task type="auto">
  <name>Task 2: Traceability 표 Status 갱신 (Pending → Complete 10행)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (Task 1 완료 후 상태, L60-87 영역의 Traceability 표)
    - .planning/phases/08-requirements-sync/08-CONTEXT.md (D-03, D-04, D-05 결정사항)
    - .planning/phases/07-overlay-regression-fix/07-02-SUMMARY.md (ALG-04 UAT 통과 근거)
    - .planning/ROADMAP.md (Phase 2/3/5/6 Complete 상태 근거)
  </read_first>
  <action>
Traceability 표의 Status 열만 갱신한다. 현재 표 행 포맷:

```
| Requirement | Phase | Status |
|-------------|-------|--------|
```

다음 10개 행을 정확히 아래 내용으로 교체하라 (Requirement/Phase 열은 기존 유지, Status 열만 변경):

Phase 2 (D-03) — 3건 "Pending" → "Complete":
```
| TCH-01 | Phase 2 | Complete |
| TCH-02 | Phase 2 | Complete |
| ALG-03 | Phase 2 | Complete |
```

ALG-04 (D-05) — Phase 열 문자열 "Phase 3 → Phase 7 (gap closure)" 유지, Status만 "Pending" → "Complete":
```
| ALG-04 | Phase 3 → Phase 7 (gap closure) | Complete |
```

Phase 5 (D-04) — 4건 "Pending" → "Complete":
```
| SEQ-01 | Phase 5 | Complete |
| SEQ-02 | Phase 5 | Complete |
| SEQ-03 | Phase 5 | Complete |
| SEQ-04 | Phase 5 | Complete |
```

RC-01..RC-06 (D-02) — Phase 열 간결화 "Phase 6 (등록은 Phase 8)" → "Phase 6" (D-07 discretion), Status "Pending" → "Complete":
```
| RC-01 | Phase 6 | Complete |
| RC-02 | Phase 6 | Complete |
| RC-03 | Phase 6 | Complete |
| RC-04 | Phase 6 | Complete |
| RC-05 | Phase 6 | Complete |
| RC-06 | Phase 6 | Complete |
```

건드리지 말아야 할 행 (무변경):
- UI-01..UI-05 (이미 Complete)
- ALG-01, ALG-02 (이미 Complete)
- ALG-05 (이미 Complete)

표 전체 행 수는 변화 없음 (22행 유지). 표 헤더/구분선도 변경 없음. 표 아래의 "Coverage" 섹션은 Task 3.
  </action>
  <verify>
    <automated>grep -cE "^\| (TCH-01|TCH-02|ALG-03) \| Phase 2 \| Complete \|$" .planning/REQUIREMENTS.md | grep -q "^3$" && grep -qE "^\| ALG-04 \| Phase 3 → Phase 7 \(gap closure\) \| Complete \|$" .planning/REQUIREMENTS.md && grep -cE "^\| SEQ-0[1-4] \| Phase 5 \| Complete \|$" .planning/REQUIREMENTS.md | grep -q "^4$" && grep -cE "^\| RC-0[1-6] \| Phase 6 \| Complete \|$" .planning/REQUIREMENTS.md | grep -q "^6$" && ! grep -qE "\| Pending \|" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\| (TCH-01\|TCH-02\|ALG-03) \| Phase 2 \| Complete \|$" .planning/REQUIREMENTS.md` == 3
    - `grep -qE "^\| ALG-04 \| Phase 3 → Phase 7 \(gap closure\) \| Complete \|$" .planning/REQUIREMENTS.md` 매칭 성공 (Phase 열 문자열 유지, Status만 Complete)
    - `grep -cE "^\| SEQ-0[1-4] \| Phase 5 \| Complete \|$" .planning/REQUIREMENTS.md` == 4
    - `grep -cE "^\| RC-0[1-6] \| Phase 6 \| Complete \|$" .planning/REQUIREMENTS.md` == 6 (Phase 열이 단순 "Phase 6"으로 간결화됨)
    - `grep -c "\| Pending \|" .planning/REQUIREMENTS.md` == 0 (Pending이 표 내부에 하나도 남지 않음)
    - `grep -c "^\| " .planning/REQUIREMENTS.md` == 23 이상 (표 헤더 1 + 구분선 1 + 22 데이터 행) — 표 행 수 유지
    - RC 행의 Phase 열에 "(등록은 Phase 8)" 문구가 완전히 제거됨: `grep -c "등록은 Phase 8" .planning/REQUIREMENTS.md` == 0
  </acceptance_criteria>
  <done>
    - Traceability 표 내 Pending 문자열이 0개
    - TCH-01/TCH-02/ALG-03 3행 모두 Status=Complete
    - ALG-04 Status=Complete (Phase 문자열 "Phase 3 → Phase 7 (gap closure)" 유지)
    - SEQ-01..SEQ-04 4행 모두 Status=Complete
    - RC-01..RC-06 6행 모두 Phase="Phase 6" + Status=Complete
    - UI 섹션 및 ALG-01/02/05 행 무변경
  </done>
</task>

<task type="auto">
  <name>Task 3: Coverage 과도기 주석 삭제 + Last-updated 꼬리표 갱신</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (Task 1, 2 완료 후 상태, L89-97 Coverage + 꼬리표 영역)
    - .planning/phases/08-requirements-sync/08-CONTEXT.md (D-07, D-08 결정사항)
  </read_first>
  <action>
Coverage 섹션과 문서 하단 꼬리표를 다음 두 가지만 정확히 수정한다:

(A) Coverage 섹션 하단 과도기 주석 1줄 삭제 (D-07):

현재 존재하는 다음 줄을 완전히 제거:
```
- 참고: RC-01..RC-06은 Phase 6에서 구현되었으나 본 문서 정의/본문 체크박스 등록은 Phase 8(요구사항 동기화)에서 수행된다.
```

삭제 후 Coverage 블록의 나머지 3줄(v1 requirements/Mapped/Unmapped)은 건드리지 않는다:
- "- v1 requirements: 22 total (UI-01..UI-05, TCH-01..TCH-02, ALG-01..ALG-05, SEQ-01..SEQ-04, RC-01..RC-06)" — 유지 (D-07: 총계 22 유지)
- "- Mapped to phases: 22" — 유지
- "- Unmapped: 0" — 유지

(B) 문서 최하단 Last-updated 꼬리표 치환 (D-08):

현재 마지막 줄:
```
*Last updated: 2026-04-22 — gap closure phase 7~10 배정 (full sync in Phase 8)*
```

정확히 다음 문자열로 교체:
```
*Last updated: 2026-04-23 — Phase 8: RC-01..RC-06 정의 등록 + traceability 정합화*
```

바로 위 라인 "*Requirements defined: 2026-04-02*"은 무변경. 구분선 "---"도 무변경.

이 task 이후 파일 전체 라인 수는 원본 97 → 대략 +7 (RC 섹션 8줄 추가) -1 (과도기 주석 삭제) = 103 정도가 된다 (정확 수치 아님, 참고용).
  </action>
  <verify>
    <automated>! grep -q "RC-01..RC-06은 Phase 6에서 구현되었으나" .planning/REQUIREMENTS.md && grep -q "^\*Last updated: 2026-04-23 — Phase 8: RC-01..RC-06 정의 등록 + traceability 정합화\*$" .planning/REQUIREMENTS.md && grep -q "^- v1 requirements: 22 total (UI-01..UI-05, TCH-01..TCH-02, ALG-01..ALG-05, SEQ-01..SEQ-04, RC-01..RC-06)$" .planning/REQUIREMENTS.md && grep -q "^- Mapped to phases: 22$" .planning/REQUIREMENTS.md && grep -q "^- Unmapped: 0$" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RC-01..RC-06은 Phase 6에서 구현되었으나" .planning/REQUIREMENTS.md` == 0 (과도기 주석 제거)
    - `grep -c "등록은 Phase 8에서 수행된다" .planning/REQUIREMENTS.md` == 0 (과도기 주석 제거 확인)
    - `grep -q "^\*Last updated: 2026-04-23 — Phase 8: RC-01..RC-06 정의 등록 + traceability 정합화\*$" .planning/REQUIREMENTS.md` 매칭 성공
    - `grep -c "^\*Last updated:" .planning/REQUIREMENTS.md` == 1 (Last updated 라인은 단 1개)
    - `grep -q "^- v1 requirements: 22 total (UI-01..UI-05, TCH-01..TCH-02, ALG-01..ALG-05, SEQ-01..SEQ-04, RC-01..RC-06)$" .planning/REQUIREMENTS.md` 매칭 성공 (총계 22 유지)
    - `grep -q "^- Mapped to phases: 22$" .planning/REQUIREMENTS.md` 매칭 성공
    - `grep -q "^- Unmapped: 0$" .planning/REQUIREMENTS.md` 매칭 성공
    - `grep -q "^\*Requirements defined: 2026-04-02\*$" .planning/REQUIREMENTS.md` 매칭 성공 (첫 번째 꼬리표 무변경 확인)
  </acceptance_criteria>
  <done>
    - Coverage 하단 과도기 주석 1줄 삭제 완료
    - Coverage 본문 3줄(v1/Mapped/Unmapped) 무변경
    - 문서 하단 Last-updated 꼬리표가 2026-04-23 Phase 8 문구로 갱신됨
    - "Requirements defined: 2026-04-02" 라인 무변경
  </done>
</task>

</tasks>

<verification>
## 전체 Phase 검증 (모든 task 완료 후)

**파일 범위 검증 (단일 파일 변경 보장):**
```bash
git diff --name-only HEAD .planning/REQUIREMENTS.md | wc -l   # == 1 (REQUIREMENTS.md만)
git diff --name-only HEAD -- WPF_Example/ | wc -l             # == 0 (코드 변경 없음)
```

**Status 일관성 교차 검증:**
```bash
# 본문 체크박스 [x]와 Traceability Complete의 일관성
grep -c "^- \[x\] \*\*RC-0[1-6]\*\*:" .planning/REQUIREMENTS.md        # == 6
grep -cE "^\| RC-0[1-6] \| Phase 6 \| Complete \|$" .planning/REQUIREMENTS.md   # == 6

# Pending 완전 제거 (표 행 기준)
grep -c "\| Pending \|" .planning/REQUIREMENTS.md                      # == 0
```

**섹션 배치 순서 검증:**
```bash
grep -n "^### 검사 시퀀스\|^### Rapid City 확장\|^## v2 Requirements" .planning/REQUIREMENTS.md
# 3행 모두 출력되어야 하며 line 번호가 오름차순
```

**Coverage/꼬리표 검증:**
```bash
grep -c "RC-01..RC-06은 Phase 6에서 구현되었으나" .planning/REQUIREMENTS.md   # == 0
grep -c "^\*Last updated: 2026-04-23 — Phase 8" .planning/REQUIREMENTS.md     # == 1
```

**Markdown 구조 sanity:**
```bash
# v1 / v2 섹션 헤더 무손상
grep -c "^## v1 Requirements$" .planning/REQUIREMENTS.md   # == 1
grep -c "^## v2 Requirements$" .planning/REQUIREMENTS.md   # == 1
grep -c "^## Traceability$" .planning/REQUIREMENTS.md      # == 1
grep -c "^## Out of Scope$" .planning/REQUIREMENTS.md      # == 1
```
</verification>

<success_criteria>
ROADMAP Phase 8 Success Criteria 3항목 전부 충족:

1. ✅ **REQUIREMENTS.md에 "Rapid City 확장" 섹션이 추가되고 RC-01..RC-06 정의가 기재된다**
   → Task 1 완료 시 `grep -c "^- \[x\] \*\*RC-0[1-6]\*\*:"` == 6

2. ✅ **Traceability 표 Status 및 본문 체크박스가 Phase 2/3/4/5/6의 실제 상태와 일치한다**
   → Task 1 (체크박스 [x]) + Task 2 (Status Complete) 완료 시 Pending 0건, 모든 Complete 요구사항의 본문 체크박스 [x] 일관성 확보

3. ✅ **Coverage 총계가 갱신된다 (v1: 16 → 22)**
   → 현재 이미 "22 total" 표기된 상태 유지 (D-07). 단, 과도기 주석 삭제(Task 3)로 Coverage 블록이 감사 시점의 최종 상태가 됨.

**추가 보증:**
- 문서 외 파일 무변경 (코드/설정/테스트 touch 없음)
- v2 Requirements / Out of Scope / Traceability 표 헤더 무변경
- UI/ALG-01/ALG-02/ALG-05 기존 Complete 행 무변경
</success_criteria>

<output>
After completion, create `.planning/phases/08-requirements-sync/08-01-SUMMARY.md` recording:
- 변경된 파일 단일(`.planning/REQUIREMENTS.md`)
- 섹션 추가 위치 (검사 시퀀스 ↔ v2 Requirements 사이)
- Traceability Status Complete로 갱신된 10행 목록
- 삭제된 과도기 주석 원문 1줄
- 갱신된 Last-updated 꼬리표 문자열
- D-01..D-08 결정사항 준수 확인
- 코드 변경 0건 확인
</output>
