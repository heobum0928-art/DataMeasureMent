---
phase: 13-datum-algorithm-extensibility
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified:
  - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: false
requirements:
  - Gap-1-DatumRoiEdit
tags: [datum, ui, roi-edit, phase-13]
must_haves:
  truths:
    - "MainView.HalconViewer_RoiMoveCompleted 이벤트가 Datum 컨텍스트(IsCurrentNodeDatum=true)에서 발화하면 MainView 가 Datum 분기로 early return 하여 기존 FAI 로직을 실행하지 않는다"
    - "Datum ROI 이동 후 ApplyDatumRoiDelta 가 AlgorithmTypeEnum 과 e.RoiId 에 따라 Line1_Row/Col, Line2_Row/Col, CircleROI_Row/Col, Horizontal_A_Row/Col, Horizontal_B_Row/Col 중 해당 필드에 DeltaRow/DeltaCol 을 누적 반영한다"
    - "이동 직후 InvokeTryTeachDatumForEdit 가 자동 호출되어 LastTeachSucceeded 갱신 + Datum 오버레이(RenderDatumOverlay)가 새 좌표를 반영한다"
    - "기존 FAI ROI 이동 (Quick 260423-o53, L514-538) 경로가 회귀 없이 동작한다 — Datum 컨텍스트가 아닐 때 기존 FindFAIByName + DeltaRow/DeltaCol 반영 + UpdateDisplayState 그대로 실행"
    - "write-back 직후 이중 신호 패턴 (`RaisePropertyChanged('')` + `RefreshParamEditor()` + `SetDatumOverlay(datum, true)`) 이 유지되어 PropertyGrid + 캔버스 모두 즉시 갱신된다"
    - "MainResultViewerControl 이 Datum ROI 후보를 hit-test 대상으로 인식한다 — SetDatumRoiCandidates(List<RoiDefinition>) public API 로 주입받고 HitTestSelectedRoi 가 _rois fallback 으로 _datumRoiCandidates 를 검사한다"
    - "Datum ROI 의 RoiDefinition.Id prefix 는 'Datum.' (예: 'Datum.Line1', 'Datum.Circle', 'Datum.HorizontalA', 'Datum.HorizontalB', 'Datum.Vertical') 규약으로 MainView 가 RoiId 로 ROI 종류를 식별한다"
    - "msbuild Debug/x64 가 exit 0 로 성공하고 2 개 수정 파일을 참조하는 신규 warning 이 0 개이다"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs"
      provides: "SetDatumRoiCandidates(List<RoiDefinition>) public API + _datumRoiCandidates private field + HitTestSelectedRoi fallback 확장 + Datum-mode hit-test 게이트 (Edit 모드 무관)"
      contains: "SetDatumRoiCandidates"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "HalconViewer_RoiMoveCompleted Datum 분기 (early return) + ApplyDatumRoiDelta + InvokeTryTeachDatumForEdit + IsCurrentNodeDatum helper + PublishDatumRoiCandidates helper (InspectionListView SelectionChanged 에서 호출)"
      contains: "ApplyDatumRoiDelta"
  key_links:
    - from: "MainView.HalconViewer_RoiMoveCompleted (L514)"
      to: "MainView.ApplyDatumRoiDelta"
      via: "IsCurrentNodeDatum(out DatumConfig datum) 이 true 면 Datum 분기 — FindFAIByName 호출 전에 early return"
      pattern: "IsCurrentNodeDatum\\(out"
    - from: "MainView.ApplyDatumRoiDelta"
      to: "MainView.InvokeTryTeachDatumForEdit"
      via: "delta 반영 직후 InvokeTryTeachDatumForEdit(datum) 호출 — D-03 즉시 재티칭"
      pattern: "InvokeTryTeachDatumForEdit\\("
    - from: "MainResultViewerControl.HitTestSelectedRoi"
      to: "MainResultViewerControl._datumRoiCandidates"
      via: "_rois FirstOrDefault 이 null 이면 _datumRoiCandidates 에서 동일 hit-test 수행 (Rect + Circle 분기)"
      pattern: "_datumRoiCandidates"
    - from: "MainView.InspectionListView.SelectedItemChanged (선택 변경 시)"
      to: "MainResultViewerControl.SetDatumRoiCandidates"
      via: "PublishDatumRoiCandidates(datum) helper — DatumConfig 를 RoiDefinition 리스트로 변환하여 halconViewer 에 주입"
      pattern: "SetDatumRoiCandidates\\("
---

<objective>
Phase 12 UAT Gap-1 (TeachDatum 모드에서 그린 ROI 를 사후 드래그 이동 불가) 는 Phase 12-03 SUMMARY 에서 "기존 Quick 260423-o53 가 FAIConfig 대상 RoiMoveCompleted 이벤트만 구현 — DatumConfig 경로 확장 미수행" 사유로 Phase 13 이월되었다. Phase 13 Plan 03 이 이 Gap 을 닫는다.

**Purpose:** Datum 노드 선택 상태에서 티칭된 ROI (Line1/Line2/Circle/Horizontal A/B/Vertical) 를 마우스 드래그로 이동하면, 이동 완료 시 해당 `DatumConfig` 필드에 delta 가 즉시 반영되고 `InvokeTryTeachDatum` 이 자동 재호출되어 검출 오버레이(RefOrigin 십자, 검출 원 등)가 새 좌표를 반영한다. 사용자가 각 ROI 를 처음부터 다시 그릴 필요 없이 위치 미세 조정만 가능해진다.

**Output:**
- `WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs`:
  - `List<RoiDefinition> _datumRoiCandidates` private 필드
  - `public void SetDatumRoiCandidates(IList<RoiDefinition> datumRois)` public API (MainView 가 Datum 노드 선택 시 호출 / 비선택 시 빈 리스트)
  - `public void ClearDatumRoiCandidates()` convenience (선택 해제 시 호출)
  - `HitTestSelectedRoi(Point imagePoint)` — 기존 `_rois` FirstOrDefault 이 null 이면 `_datumRoiCandidates` 에서 fallback 히트 검사 (Rect/Circle 공통 로직 재사용)
  - MouseDown Edit-mode 게이트 (L696 `if (_isEditMode && !IsAnyDrawingModeActive() && HasImage)`) 를 Datum 후보가 있으면 `_isEditMode` 무관 통과 — Datum 노드 선택 = 자동 편집 허용 (CONTEXT D-01)
  - MouseUp RoiMoveCompleted 발화 (L917-950) 는 기존 그대로 — RoiId 가 `"Datum."` prefix 면 MainView 가 Datum 분기 실행
- `WPF_Example/UI/ContentItem/MainView.xaml.cs`:
  - `HalconViewer_RoiMoveCompleted` L514-538 수정 — 최상단에 `if (IsCurrentNodeDatum(out DatumConfig datum)) { HandleDatumRoiMove(datum, e); return; }` 삽입, 기존 FAI 로직은 그 아래로 이동 (untouched)
  - `private bool IsCurrentNodeDatum(out DatumConfig datum)` helper — `mParentWindow?.inspectionList?.SelectedParam as DatumConfig` 로 확인
  - `private void HandleDatumRoiMove(DatumConfig datum, RoiMoveCompletedArgs e)` — ApplyDatumRoiDelta + 이중 신호 + InvokeTryTeachDatumForEdit 호출 순차
  - `private void ApplyDatumRoiDelta(DatumConfig datum, RoiMoveCompletedArgs e)` — RoiId prefix 파싱 (`"Datum.Line1"` / `"Datum.Line2"` / `"Datum.Circle"` / `"Datum.HorizontalA"` / `"Datum.HorizontalB"` / `"Datum.Vertical"`) → 해당 필드에 DeltaRow/DeltaCol 누적
  - `private void InvokeTryTeachDatumForEdit(DatumConfig datum)` — InvokeTryTeachDatum L1174-1208 과 유사하나 `_editingDatum` state 를 건드리지 않음 (edit 세션은 _editingDatum=null 상태에서 호출됨). 성공 시 label_drawHint 재활성 + SetDatumOverlay 갱신 + **halconViewer.SetDatumRoiCandidates 로 후보 리스트 재-publish** (이동 후 좌표 반영)
  - `private void PublishDatumRoiCandidates(DatumConfig datum)` — DatumConfig 의 ROI 필드를 `RoiDefinition` 리스트로 변환하여 `halconViewer.SetDatumRoiCandidates` 호출. `_rectBboxFromCenter` 헬퍼 사용 (Rectangle2 phi=0 → Row1/Col1/Row2/Col2 bbox).
  - InspectionListView 의 **selection-change 이벤트**를 구독하여 Datum 노드 선택 시 PublishDatumRoiCandidates, Datum 비선택 시 ClearDatumRoiCandidates 호출. (Plan 03 은 InspectionListView 자체를 수정하지 않고 MainView 에서 `mParentWindow.inspectionList.SelectedItemChanged` 또는 equivalent 구독 — 해당 이벤트가 없으면 SelectedParam 변경을 감지하는 대안으로 btn_teachDatum IsEnabled 토글 타이밍에 연계. **구현 상세는 아래 action 블록 참조.**)

- msbuild Debug/x64 exit 0 + 신규 warning 0.
- SIMUL_MODE UAT checkpoint — 3 algorithm × 각 ROI 종류별 이동 + FAI 회귀.

**Note on scope:** CONTEXT D-04 는 "MainResultViewerControl hit-test 가 Datum ROI 목록을 어떻게 알지는 MainView 에서 selection-aware 힌트 주입 또는 hit-test 로직 확장 — 방식은 planner 재량" 이라고 명시. 본 Plan 은 **옵션 A (SetDatumRoiCandidates 주입 API)** 를 선택 (PATTERNS.md §3 L343 권장). 기존 `_rois` (FAI 목록) 는 untouched — Datum 편집이 FAI 리스트를 오염시키지 않는다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-datum-algorithm-extensibility/13-CONTEXT.md
@.planning/phases/13-datum-algorithm-extensibility/13-PATTERNS.md
@.planning/phases/13-datum-algorithm-extensibility/13-01-PLAN.md
@.planning/phases/13-datum-algorithm-extensibility/13-02-PLAN.md
@.planning/phases/12-datum-circle-vertical-horizontal-intersection/12-03-SUMMARY.md
@WPF_Example/UI/ContentItem/MainView.xaml.cs
@WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
@WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
@WPF_Example/Halcon/Models/RoiDefinition.cs
@WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
@CLAUDE.md

<interfaces>
<!-- Plan 03 은 Datum ROI 편집 경로를 new helper + hit-test fallback 으로 additive 추가. 기존 FAI RoiMoveCompleted / hit-test 시그니처/로직 변경 금지. -->
<!-- CONTEXT D-13 준수: DatumConfig / DatumFindingService / HalconDisplayService / btn_teachDatum 관련 기존 메서드 전부 untouched. -->

From WPF_Example/Halcon/Models/RoiDefinition.cs (Phase 11 기존):
```csharp
public enum RoiShape { Rect, Polygon, Circle }      // L6
public class RoiDefinition                          // L9
{
    public string Id { get; set; }                  // "Datum.Line1" 등 prefix 규약
    public string Name { get; set; }
    public RoiShape Shape { get; set; } = RoiShape.Rect;
    public double Row1, Column1, Row2, Column2;     // Rect bbox (phi=0 가정)
    public double CenterRow, CenterCol, Radius;     // Circle
    public bool IsTaught { get; set; }              // true = teach 완료 (Datum ROI 는 티칭 후 이동 허용이므로 true 고정)
    public RoiDefinition Clone();                   // L87 — MemberwiseClone
}
```

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (읽기/쓰기 모두):
```csharp
public EDatumAlgorithm AlgorithmTypeEnum { get; }        // L57 — 알고리즘 식별
public double Line1_Row, Line1_Col, Line1_Phi, Line1_Length1, Line1_Length2;      // L79-83
public double Line2_Row, Line2_Col, Line2_Phi, Line2_Length1, Line2_Length2;      // L87-91
public double CircleROI_Row, CircleROI_Col, CircleROI_Radius;                      // L96-98
public double Horizontal_A_Row, Horizontal_A_Col, Horizontal_A_Phi, Horizontal_A_Length1, Horizontal_A_Length2;  // L104-108
public double Horizontal_B_Row, Horizontal_B_Col, Horizontal_B_Phi, Horizontal_B_Length1, Horizontal_B_Length2;  // L112-116
public bool IsConfigured, LastTeachSucceeded;             // L137, L164 — Plan 03 에서 재티칭 시 재갱신
public void RaisePropertyChanged(string name);            // ParamBase — INotifyPropertyChanged 발동용
```

From WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs (Phase 11/12 기존):
```csharp
public HImage CurrentImage { get; private set; }          // L173
public event EventHandler<RoiMoveCompletedArgs> RoiMoveCompleted;    // (event declaration — 검색 필요)
public void SetDatumOverlay(DatumConfig datum, bool isSelected);     // L512
private RoiDefinition HitTestSelectedRoi(Point imagePoint);          // L304 — Plan 03 이 확장
private List<RoiDefinition> _rois;                                   // FAI ROI 리스트 (untouched)
private string _selectedRoiId;                                        // 현재 선택 ROI
private bool _isEditMode;                                             // Edit 모드 플래그 (Datum 모드에서는 무관)

// Plan 03 신규 추가:
//   private List<RoiDefinition> _datumRoiCandidates;
//   public void SetDatumRoiCandidates(IList<RoiDefinition> datumRois);
//   public void ClearDatumRoiCandidates();
```

From WPF_Example/UI/ContentItem/MainView.xaml.cs (Phase 12 기존, Plan 03 이 수정):
```csharp
private MainWindow mParentWindow;                         // L24
private DatumConfig _editingDatum;                        // L54 — teach 세션 상태 (Plan 03 edit 세션과 독립)

private void HalconViewer_RoiMoveCompleted(object sender, RoiMoveCompletedArgs e);  // L515 — Plan 03 이 Datum 분기 추가
private FAIConfig FindFAIByName(string roiId);            // (검색 — 기존 FAI hit-test 경로, untouched)
private void InvokeTryTeachDatum();                       // L1174 — teach 경로 (untouched, Plan 03 은 InvokeTryTeachDatumForEdit 별도 생성)

// Plan 03 신규 추가:
//   private bool IsCurrentNodeDatum(out DatumConfig datum);
//   private void HandleDatumRoiMove(DatumConfig datum, RoiMoveCompletedArgs e);
//   private void ApplyDatumRoiDelta(DatumConfig datum, RoiMoveCompletedArgs e);
//   private void InvokeTryTeachDatumForEdit(DatumConfig datum);
//   private void PublishDatumRoiCandidates(DatumConfig datum);
//   private void ClearDatumRoiCandidates();
//   private static RoiDefinition BuildDatumRectCandidate(string id, double centerRow, double centerCol, double halfH, double halfW);
//   private static RoiDefinition BuildDatumCircleCandidate(string id, double centerRow, double centerCol, double radius);
```

From WPF_Example/UI/ControlItem/InspectionListView.xaml.cs (Phase 12 기존, read-only for Plan 03):
```csharp
public ParamBase SelectedParam { get; private set; }      // L21
public void RefreshParamEditor();                          // L26 — Plan 03 이 Datum edit 후 호출 (PATTERNS.md §C 패턴)
// SelectionChanged 이벤트: tree control 자체의 SelectionChanged 는 partial class 내부에 존재 (L283 에서 SelectedParam = param 대입). Plan 03 이 구독할 public event 없음.
// → PublishDatumRoiCandidates 는 **MainView 의 InvokeTryTeachDatum 성공 직후 + btn_teachDatum 취소 경로 + Plan 03 edit 경로 완료 시 수동 호출**. 실시간 selection-change 구독은 본 Plan 범위 밖 (추후 확장 시 InspectionListView 에 public event 추가).
```
</interfaces>

**Brace style (PATTERNS.md § File Classification):**
- `MainView.xaml.cs` = **K&R**
- `MainResultViewerControl.xaml.cs` = **Allman**

**C# 7.2 제약:** `out var` 사용 가능 (MainView.xaml.cs 에서는 다른 메서드에서 이미 사용하는 양식 — C# 7.0 feature). switch expression / record / nullable reference types 금지.

**주석 convention:**
- MainView.xaml.cs: `//260424 hbk Phase 13 D-01..D-04 — <intent>`
- MainResultViewerControl.xaml.cs: `//260424 hbk Phase 13 D-02..D-04 — <intent>`

**Datum RoiId prefix 규약 (본 Plan 이 최초 정의):**
- `"Datum.Line1"` — TwoLineIntersect Line1 OR VerticalTwoHorizontal 수직 ROI (DatumConfig.Line1_*)
- `"Datum.Line2"` — TwoLineIntersect Line2 (DatumConfig.Line2_*)
- `"Datum.Circle"` — CircleTwoHorizontal Circle ROI (DatumConfig.CircleROI_*)
- `"Datum.HorizontalA"` — CircleTwoHorizontal + VerticalTwoHorizontal 공용 (DatumConfig.Horizontal_A_*)
- `"Datum.HorizontalB"` — 동 (DatumConfig.Horizontal_B_*)
- **"Datum.Vertical"** 은 사용하지 않음 — VerticalTwoHorizontal 의 수직 ROI 는 DatumConfig.Line1_* 에 저장되므로 RoiId 도 `"Datum.Line1"` 로 통일 (Phase 12 Plan 03 HalconViewer_DatumRectCompleted L1110 D-07 재사용 시맨틱스 준수).

**Rect bbox 계산 (phi=0 전제, Phase 12 Plan 03 L1102-1105 참조):**
```csharp
// Rectangle2 (centerRow, centerCol, phi=0, halfH=Length1, halfW=Length2) ↔ bbox (Row1, Col1, Row2, Col2)
Row1 = centerRow - halfH;  Row2 = centerRow + halfH;
Col1 = centerCol - halfW;  Col2 = centerCol + halfW;
```

**Datum edit 시 teach 세션 독립성 (중요):**
Plan 03 의 edit 경로는 `_editingDatum` 상태를 건드리지 않는다. `_editingDatum` 은 btn_teachDatum click → Line drag → Done 으로 끝나는 teach 세션 전용. Edit 은 Datum 노드 선택 상태 + 마우스 드래그만 trigger → `mParentWindow?.inspectionList?.SelectedParam as DatumConfig` 로 매 drag 마다 해결. 즉 **btn_teachDatum 이 IsChecked=false 상태에서도 Datum 노드 선택 + 이미지 있음 → drag 시 edit 경로 동작**.

**취소 경로:** RoiMoveCompleted 이 발화하지 않는 짧은 드래그(DeltaRow, DeltaCol < 0.5px)는 기존 L940 필터로 이벤트 자체가 발화 안 됨 → 취소 경로 별도 처리 불필요.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend MainResultViewerControl with Datum ROI candidates + hit-test fallback</name>
  <files>WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs L1-15 (using + namespace)
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs L39-60 (RoiMoveCompletedArgs / RoiGeometryChangedArgs DTO)
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs L303-334 (HitTestSelectedRoi — Plan 03 확장 대상)
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs L505-525 (SetDatumOverlay / ClearDatumOverlay 패턴 — 신규 API 양식 참고)
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs L690-725 (MouseDown Edit-mode 게이트 — Datum 경로 게이트 추가 대상)
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs L917-950 (MouseUp RoiMoveCompleted 발화)
    - .planning/phases/13-datum-algorithm-extensibility/13-CONTEXT.md L40-51 (D-01..D-04)
    - .planning/phases/13-datum-algorithm-extensibility/13-PATTERNS.md §3 L229-351 (hit-test 확장 지침)
  </read_first>
  <action>
    **1A. `_datumRoiCandidates` 필드 추가**

    위치: L509 `_datumSelected` 필드 바로 뒤 (Plan 02 가 L510 에 `_datumFindResultOverlay` 를 추가했다면 그 아래). **Plan 02 완료 상태 전제** — `_datumFindResultOverlay` 필드가 존재.

    ```csharp
            //260424 hbk Phase 13 D-02..D-04 — Datum ROI 편집 후보 리스트 (Datum 노드 선택 시 MainView 가 주입, 비선택 시 clear)
            //  teach 경로 (_datumConfig + _datumSelected) 와 별도 — 편집 hit-test 전용. _rois(FAI) 와도 독립.
            //  RoiId 규약: "Datum.Line1" / "Datum.Line2" / "Datum.Circle" / "Datum.HorizontalA" / "Datum.HorizontalB"
            private List<RoiDefinition> _datumRoiCandidates = new List<RoiDefinition>();
    ```

    **1B. `SetDatumRoiCandidates` / `ClearDatumRoiCandidates` public 메서드 추가**

    위치: L524 `ClearDatumOverlay()` 닫는 브레이스 바로 뒤 (Plan 02 의 `SetDatumFindResultOverlay` / `ClearDatumFindResultOverlay` 메서드 다음).

    ```csharp
            //260424 hbk Phase 13 D-02..D-04 — Datum ROI 편집 후보 주입 (MainView 가 Datum 노드 선택 시 호출)
            public void SetDatumRoiCandidates(IList<RoiDefinition> datumRois)
            {
                if (datumRois == null)
                {
                    _datumRoiCandidates.Clear();
                }
                else
                {
                    _datumRoiCandidates = datumRois.Where(r => r != null).Select(r => r.Clone()).ToList();
                }
                // Render 호출 없음 — hit-test 전용, 렌더링은 기존 _datumConfig 분기가 담당 (중복 표시 방지).
            }

            //260424 hbk Phase 13 D-02..D-04 — Datum 노드 비선택 시 후보 clear
            public void ClearDatumRoiCandidates()
            {
                _datumRoiCandidates.Clear();
            }
    ```

    **1C. `HitTestSelectedRoi` 확장 — _rois fallback 으로 _datumRoiCandidates 검사**

    기존 L304-334 메서드를 **교체**:

    ```csharp
            //260423 hbk ROI hit-test: 선택된 ROI 내부 클릭인지 판정
            //260424 hbk Phase 13 D-02..D-04 — _rois (FAI) null fallback 으로 _datumRoiCandidates 에서 동일 hit-test 수행
            private RoiDefinition HitTestSelectedRoi(Point imagePoint)
            {
                // 기존 FAI 경로: _selectedRoiId 와 일치하는 _rois 항목 검사
                if (!string.IsNullOrEmpty(_selectedRoiId))
                {
                    var roi = _rois.FirstOrDefault(r => r.Id == _selectedRoiId);
                    if (roi != null)
                    {
                        RoiDefinition hit = HitTestOneRoi(roi, imagePoint);
                        if (hit != null) return hit;
                    }
                }

                //260424 hbk Phase 13 D-02..D-04 — Datum 후보 fallback (Datum 노드 선택 시 MainView 가 주입)
                //  _selectedRoiId 무관 — Datum ROI 는 어느 것이든 hit 허용 (CONTEXT D-01 "hit-test 기반 자동 선택")
                if (_datumRoiCandidates != null && _datumRoiCandidates.Count > 0)
                {
                    foreach (var candidate in _datumRoiCandidates)
                    {
                        if (candidate == null) continue;
                        RoiDefinition hit = HitTestOneRoi(candidate, imagePoint);
                        if (hit != null) return hit;
                    }
                }

                return null;
            }

            //260424 hbk Phase 13 D-02..D-04 — 기존 HitTestSelectedRoi 의 Rect/Circle 판정 로직을 재사용 가능 helper 로 추출
            private static RoiDefinition HitTestOneRoi(RoiDefinition roi, Point imagePoint)
            {
                if (roi == null) return null;

                if (roi.Shape == RoiShape.Circle)
                {
                    double dr = imagePoint.Y - roi.CenterRow;
                    double dc = imagePoint.X - roi.CenterCol;
                    if (Math.Sqrt(dr * dr + dc * dc) <= roi.Radius)
                    {
                        return roi;
                    }
                    return null;
                }

                if (imagePoint.Y >= roi.Row1 && imagePoint.Y <= roi.Row2 &&
                    imagePoint.X >= roi.Column1 && imagePoint.X <= roi.Column2)
                {
                    return roi;
                }
                return null;
            }
    ```

    **1D. MouseDown Edit-mode 게이트 확장 — Datum 후보 있으면 Edit 모드 무관 통과**

    기존 L695-724 의 `if (_isEditMode && !IsAnyDrawingModeActive() && HasImage)` 게이트를 **교체**:

    ```csharp
                //260423 hbk Edit 모드 전용: 핸들 히트 → 리사이즈 시작, 바디 히트 → 이동 시작
                //260424 hbk Phase 13 D-01 — Datum 후보 리스트가 있으면 Edit 모드 무관 통과 (Datum 노드 선택 = 자동 편집 허용)
                bool datumCandidatesPresent = (_datumRoiCandidates != null && _datumRoiCandidates.Count > 0);
                if ((_isEditMode || datumCandidatesPresent) && !IsAnyDrawingModeActive() && HasImage)
                {
                    //260423 hbk Edit 핸들 hit 은 FAI(_rois)만 지원 — Datum 은 리사이즈 제공 안 함 (이동만, D-02 scope)
                    if (_isEditMode)
                    {
                        var selectedRoi = _rois.FirstOrDefault(r => r.Id == _selectedRoiId && r.IsTaught);
                        if (selectedRoi != null)
                        {
                            var handleHit = HitTestEditHandle(selectedRoi, mouseState.ImagePoint);
                            if (handleHit.Handle != ResizeHandle.None)
                            {
                                _isResizingRoi = true;
                                _resizingHandle = handleHit.Handle;
                                _resizingPolygonIndex = handleHit.PolyIndex;
                                _resizingRoiSnapshot = selectedRoi.Clone();
                                SetPanCursor(Cursors.SizeAll);
                                PublishPointerInfo();
                                return;
                            }
                        }
                    }

                    var hitRoi = HitTestSelectedRoi(mouseState.ImagePoint);
                    if (hitRoi != null)
                    {
                        _isMovingRoi = true;
                        _moveStartImagePoint = mouseState.ImagePoint;
                        _movingRoiSnapshot = hitRoi.Clone();
                        SetPanCursor(Cursors.SizeAll);
                        PublishPointerInfo();
                        return;
                    }
                }
    ```

    **주의:**
    - `_isMovingRoi` / `_movingRoiSnapshot` 은 기존 FAI 이동에도 사용됨 → Datum 이동도 동일 메커니즘 공유. MouseUp 시 L917-950 RoiMoveCompleted 발화도 동일 코드 재사용. RoiId prefix 로만 MainView 가 구분.
    - **HitTestEditHandle 은 Datum 에 적용 안 함** — Datum 은 이동만 (D-02 "편집 대상 범위 = ROI 이동만, 리사이즈는 Deferred"). `_isEditMode=false && datumCandidatesPresent=true` 경로에서는 리사이즈 분기를 건너뛰고 바로 hit-test→이동만.
    - `_movingRoiSnapshot` 이 Datum ROI 이면 L925 의 Shape 분기로 Row/Col 이동량 계산 → 기존 로직 그대로 동작.

    **1E. `using System.Linq;` 확인** — L3 에 이미 있음. `using System.Collections.Generic;` L2 에 있음. `HalconDotNet` L9. 추가 using 불필요.

    **1F. 빌드 실행:** Task 2 완료 후 합산 빌드 (이 Task 단독 빌드는 가능 — MainResultViewerControl 변경만으로 compile ok).

    ```bash
    msbuild WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal
    ```

    exit 0 확인. 신규 warning 0 확인.

    **1G. 커밋:** Task 2 완료 후 합쳐서 커밋. 이 Task 는 stage 만 수행.
  </action>
  <verify>
    <automated>
      (1) grep -cE "private List<RoiDefinition> _datumRoiCandidates" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → 1
      (2) grep -cE "public void SetDatumRoiCandidates\(IList<RoiDefinition> datumRois\)" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → 1
      (3) grep -cE "public void ClearDatumRoiCandidates\(\)" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → 1
      (4) grep -cE "private static RoiDefinition HitTestOneRoi\(" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → 1
      (5) grep -cF "datumCandidatesPresent" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → ≥ 1
      (6) grep -cF "_datumRoiCandidates.Clear()" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → ≥ 2
      (7) grep -cF "260424 hbk Phase 13 D-01" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → ≥ 1
      (8) grep -cF "260424 hbk Phase 13 D-02..D-04" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → ≥ 4
      (9) 기존 SetDatumOverlay / ClearDatumOverlay 시그니처 미변경: grep -cE "public void SetDatumOverlay\(DatumConfig datum, bool isSelected\)" → 1
      (10) 기존 RoiMoveCompleted 이벤트 발화 로직 미변경 검증: awk '/_isMovingRoi && _movingRoiSnapshot/,/return;$/' WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs | grep -c "RoiMoveCompleted?.Invoke" → 1 (기존 1건 유지)
      (11) msbuild WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal → exit 0
    </automated>
  </verify>
  <done>
    - _datumRoiCandidates 필드 + SetDatumRoiCandidates / ClearDatumRoiCandidates public 메서드 추가
    - HitTestSelectedRoi 확장 + HitTestOneRoi helper 추출
    - MouseDown Edit-mode 게이트 Datum 후보 통과 (리사이즈 분기는 Edit 모드에서만, Datum 은 이동만)
    - 기존 SetDatumOverlay / RoiMoveCompleted / HitTestEditHandle 시그니처/로직 untouched (CONTEXT D-13)
    - msbuild 단독 빌드 exit 0
    - git stage 만 수행 (Task 2 합쳐서 커밋)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Datum branch to MainView HalconViewer_RoiMoveCompleted + ApplyDatumRoiDelta + PublishDatumRoiCandidates</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L514-538 (HalconViewer_RoiMoveCompleted — Datum 분기 추가 대상)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L1095-1164 (HalconViewer_DatumRectCompleted / HalconViewer_DatumCircleCompleted — write-back 패턴 참고)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L1102-1105 (Rect bbox → Rectangle2 center/halfH/halfW 변환 — Plan 03 역방향)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L1140-1144 (Phase 12 Gap-3 이중 신호 — RaisePropertyChanged + RefreshParamEditor + SetDatumOverlay)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L1174-1208 (InvokeTryTeachDatum — Plan 03 InvokeTryTeachDatumForEdit analog)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L1000-1010 (SelectedParam as DatumConfig 해결 패턴)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs L56-115 (AlgorithmTypeEnum + 모든 ROI 필드)
    - .planning/phases/13-datum-algorithm-extensibility/13-CONTEXT.md L40-51 (D-01..D-04)
    - .planning/phases/13-datum-algorithm-extensibility/13-PATTERNS.md §2.2 L180-226 (Datum 분기 skeleton)
    - .planning/phases/13-datum-algorithm-extensibility/13-PATTERNS.md §C L659-668 (이중 신호 패턴)
  </read_first>
  <action>
    **2A. HalconViewer_RoiMoveCompleted 에 Datum 분기 삽입**

    기존 L514-538 메서드를 **교체**. 최상단에 Datum 분기 추가, 기존 FAI 로직은 그 아래로 이동 (한 줄도 변경하지 않음).

    ```csharp
            //260423 hbk ROI 이동 완료 → FAI 모델 좌표 반영
            //260424 hbk Phase 13 D-01..D-04 — Datum 컨텍스트(SelectedParam=DatumConfig)에서는 Datum 분기로 early return
            private void HalconViewer_RoiMoveCompleted(object sender, RoiMoveCompletedArgs e) {
                if (e == null || string.IsNullOrEmpty(e.RoiId)) return;

                //260424 hbk Phase 13 D-01..D-04 — Datum 분기 우선 (FAI lookup 전에)
                DatumConfig datum;
                if (IsCurrentNodeDatum(out datum) && e.RoiId != null && e.RoiId.StartsWith("Datum.")) {
                    HandleDatumRoiMove(datum, e);
                    return;
                }

                //260423 hbk 기존 FAI 경로 (untouched from Phase 11 Quick 260423-o53)
                var fai = FindFAIByName(e.RoiId);
                if (fai == null) return;

                bool handledCircle = false;
                foreach (var m in fai.Measurements) {
                    var circle = m as CircleDiameterMeasurement;
                    if (circle != null) {
                        circle.Circle_Row += e.DeltaRow;
                        circle.Circle_Col += e.DeltaCol;
                        handledCircle = true;
                        break;
                    }
                }
                if (!handledCircle) {
                    fai.ROI_Row += e.DeltaRow;
                    fai.ROI_Col += e.DeltaCol;
                }

                var rois = GetCurrentFAIRois();
                halconViewer.UpdateDisplayState(rois, e.RoiId, null, null);
            }
    ```

    **주의 — FAI 경로 untouched:** L517 `var fai = FindFAIByName(e.RoiId);` 부터 L537 `halconViewer.UpdateDisplayState(...)` 까지 **단 한 줄도 변경하지 않는다**. Datum 분기는 최상단에 early return 으로만 삽입. Datum RoiId prefix `"Datum."` 이 아닌 경우 기존 FAI 로직 그대로 실행.

    **2B. IsCurrentNodeDatum helper + HandleDatumRoiMove 추가**

    위치: HalconViewer_RoiMoveCompleted 메서드 바로 다음 (L538 `}` 바로 뒤, `ConvertParamRects` L540 앞).

    ```csharp
            //260424 hbk Phase 13 D-01 — 현재 선택 노드가 Datum 인지 판정 (teach 중/비중 무관)
            private bool IsCurrentNodeDatum(out DatumConfig datum) {
                datum = mParentWindow?.inspectionList?.SelectedParam as DatumConfig;
                return datum != null;
            }

            //260424 hbk Phase 13 D-01..D-04 — Datum ROI 이동 완료 처리 (delta 반영 → 이중 신호 → 자동 재티칭 → 후보 re-publish)
            private void HandleDatumRoiMove(DatumConfig datum, RoiMoveCompletedArgs e) {
                //260424 hbk Phase 13 D-02 — RoiId prefix 로 AlgorithmType 별 필드 매핑
                ApplyDatumRoiDelta(datum, e);

                //260424 hbk Phase 13 D-04 — Phase 12 Gap-3 이중 신호 패턴 (PATTERNS §C)
                try { datum.RaisePropertyChanged(string.Empty); } catch { }
                mParentWindow?.inspectionList?.RefreshParamEditor();
                halconViewer.SetDatumOverlay(datum, true);

                //260424 hbk Phase 13 D-03 — 즉시 자동 재티칭
                InvokeTryTeachDatumForEdit(datum);

                //260424 hbk Phase 13 D-02..D-04 — 이동/재티칭 후 후보 리스트 re-publish (좌표 갱신 반영)
                PublishDatumRoiCandidates(datum);
            }
    ```

    **2C. ApplyDatumRoiDelta — RoiId prefix 파싱 + 필드 매핑**

    위치: HandleDatumRoiMove 바로 뒤.

    ```csharp
            //260424 hbk Phase 13 D-02 — RoiId prefix 로 DatumConfig 필드 매핑 (delta 누적)
            //  VerticalTwoHorizontal 의 수직 ROI 는 Line1_* 에 저장 (Phase 12 D-07 재사용) → RoiId="Datum.Line1"
            private void ApplyDatumRoiDelta(DatumConfig datum, RoiMoveCompletedArgs e) {
                if (datum == null || e == null || string.IsNullOrEmpty(e.RoiId)) return;

                switch (e.RoiId) {
                    case "Datum.Line1":
                        datum.Line1_Row += e.DeltaRow;
                        datum.Line1_Col += e.DeltaCol;
                        break;
                    case "Datum.Line2":
                        datum.Line2_Row += e.DeltaRow;
                        datum.Line2_Col += e.DeltaCol;
                        break;
                    case "Datum.Circle":
                        datum.CircleROI_Row += e.DeltaRow;
                        datum.CircleROI_Col += e.DeltaCol;
                        // Radius 는 이동 불변
                        break;
                    case "Datum.HorizontalA":
                        datum.Horizontal_A_Row += e.DeltaRow;
                        datum.Horizontal_A_Col += e.DeltaCol;
                        break;
                    case "Datum.HorizontalB":
                        datum.Horizontal_B_Row += e.DeltaRow;
                        datum.Horizontal_B_Col += e.DeltaCol;
                        break;
                    default:
                        //260424 hbk Phase 13 D-02 — 알 수 없는 RoiId (로깅만, 이동 무시)
                        Logging.PrintLog((int)ELogType.Trace, "Datum ROI move: unknown RoiId=" + e.RoiId);
                        break;
                }
            }
    ```

    **2D. InvokeTryTeachDatumForEdit — edit 세션 전용 재티칭**

    위치: ApplyDatumRoiDelta 바로 뒤.

    ```csharp
            //260424 hbk Phase 13 D-03 — Edit 세션 전용 TryTeachDatum 자동 재호출 (_editingDatum 상태 건드리지 않음)
            //  InvokeTryTeachDatum (L1174, teach 세션용) 과 유사하나 canvas mode / btn_teachDatum.IsChecked 조작 없음.
            //  성공 시 label_drawHint 녹색 재사용, 실패 시 빨강 — 기존 palette 준수.
            private void InvokeTryTeachDatumForEdit(DatumConfig datum) {
                if (datum == null) return;

                HImage img = halconViewer.CurrentImage;
                if (img == null) {
                    //260424 hbk Phase 13 D-03 — 이미지 없으면 조용히 스킵 (PropertyGrid/오버레이는 이미 갱신됨)
                    return;
                }

                var svc = new ReringProject.Halcon.Algorithms.DatumFindingService();
                string error;
                bool ok = svc.TryTeachDatum(img, datum, out error);
                if (ok) {
                    label_drawHint.Content = "Datum ROI 이동 — 재티칭 OK"; //260424 hbk Phase 13 D-03
                    label_drawHint.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FF4ADE80")); //260424 hbk Phase 13 D-03 success green
                    label_drawHint.Visibility = Visibility.Visible;
                    halconViewer.SetDatumOverlay(datum, true);
                }
                else {
                    label_drawHint.Content = "Datum ROI 이동 — 재티칭 실패: " + (error ?? "unknown"); //260424 hbk Phase 13 D-03
                    label_drawHint.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FFF87171")); //260424 hbk Phase 13 D-03 error red
                    label_drawHint.Visibility = Visibility.Visible;
                    // 실패해도 SetDatumOverlay 는 호출 — RenderDatumOverlay 가 LastTeachSucceeded=false 분기로 detection 오버레이 생략
                    halconViewer.SetDatumOverlay(datum, true);
                }
            }
    ```

    **2E. PublishDatumRoiCandidates + helper**

    위치: InvokeTryTeachDatumForEdit 바로 뒤.

    ```csharp
            //260424 hbk Phase 13 D-02..D-04 — DatumConfig 의 ROI 필드를 RoiDefinition 리스트로 변환하여 halconViewer 에 hit-test 후보로 주입
            //  AlgorithmType 별로 유효 ROI 만 publish:
            //    TwoLineIntersect: Line1, Line2 (Length1>0 && Length2>0 조건)
            //    CircleTwoHorizontal: Circle (Radius>0), HorizontalA, HorizontalB
            //    VerticalTwoHorizontal: Line1 (수직 ROI 재사용), HorizontalA, HorizontalB
            private void PublishDatumRoiCandidates(DatumConfig datum) {
                if (datum == null) { halconViewer.ClearDatumRoiCandidates(); return; }

                var list = new List<ReringProject.Halcon.Models.RoiDefinition>();
                switch (datum.AlgorithmTypeEnum) {
                    case EDatumAlgorithm.TwoLineIntersect:
                        if (datum.Line1_Length1 > 0 && datum.Line1_Length2 > 0) {
                            list.Add(BuildDatumRectCandidate("Datum.Line1", datum.Line1_Row, datum.Line1_Col, datum.Line1_Length1, datum.Line1_Length2));
                        }
                        if (datum.Line2_Length1 > 0 && datum.Line2_Length2 > 0) {
                            list.Add(BuildDatumRectCandidate("Datum.Line2", datum.Line2_Row, datum.Line2_Col, datum.Line2_Length1, datum.Line2_Length2));
                        }
                        break;
                    case EDatumAlgorithm.CircleTwoHorizontal:
                        if (datum.CircleROI_Radius > 0) {
                            list.Add(BuildDatumCircleCandidate("Datum.Circle", datum.CircleROI_Row, datum.CircleROI_Col, datum.CircleROI_Radius));
                        }
                        if (datum.Horizontal_A_Length1 > 0 && datum.Horizontal_A_Length2 > 0) {
                            list.Add(BuildDatumRectCandidate("Datum.HorizontalA", datum.Horizontal_A_Row, datum.Horizontal_A_Col, datum.Horizontal_A_Length1, datum.Horizontal_A_Length2));
                        }
                        if (datum.Horizontal_B_Length1 > 0 && datum.Horizontal_B_Length2 > 0) {
                            list.Add(BuildDatumRectCandidate("Datum.HorizontalB", datum.Horizontal_B_Row, datum.Horizontal_B_Col, datum.Horizontal_B_Length1, datum.Horizontal_B_Length2));
                        }
                        break;
                    case EDatumAlgorithm.VerticalTwoHorizontal:
                        // 수직 ROI 는 Line1_* 재사용 (Phase 12 D-07) — RoiId 도 "Datum.Line1" 로 통일
                        if (datum.Line1_Length1 > 0 && datum.Line1_Length2 > 0) {
                            list.Add(BuildDatumRectCandidate("Datum.Line1", datum.Line1_Row, datum.Line1_Col, datum.Line1_Length1, datum.Line1_Length2));
                        }
                        if (datum.Horizontal_A_Length1 > 0 && datum.Horizontal_A_Length2 > 0) {
                            list.Add(BuildDatumRectCandidate("Datum.HorizontalA", datum.Horizontal_A_Row, datum.Horizontal_A_Col, datum.Horizontal_A_Length1, datum.Horizontal_A_Length2));
                        }
                        if (datum.Horizontal_B_Length1 > 0 && datum.Horizontal_B_Length2 > 0) {
                            list.Add(BuildDatumRectCandidate("Datum.HorizontalB", datum.Horizontal_B_Row, datum.Horizontal_B_Col, datum.Horizontal_B_Length1, datum.Horizontal_B_Length2));
                        }
                        break;
                }

                halconViewer.SetDatumRoiCandidates(list);
            }

            //260424 hbk Phase 13 D-02 — Rectangle2 (centerRow, centerCol, phi=0, halfH=Length1, halfW=Length2) → RoiDefinition bbox
            private static ReringProject.Halcon.Models.RoiDefinition BuildDatumRectCandidate(string id, double centerRow, double centerCol, double halfH, double halfW) {
                return new ReringProject.Halcon.Models.RoiDefinition {
                    Id = id,
                    Name = id,
                    Shape = ReringProject.Halcon.Models.RoiShape.Rect,
                    Row1 = centerRow - halfH,
                    Row2 = centerRow + halfH,
                    Column1 = centerCol - halfW,
                    Column2 = centerCol + halfW,
                    IsTaught = true //260424 hbk Phase 13 D-02 — Datum ROI 는 티칭 후 이동이므로 true
                };
            }

            //260424 hbk Phase 13 D-02 — Circle → RoiDefinition
            private static ReringProject.Halcon.Models.RoiDefinition BuildDatumCircleCandidate(string id, double centerRow, double centerCol, double radius) {
                return new ReringProject.Halcon.Models.RoiDefinition {
                    Id = id,
                    Name = id,
                    Shape = ReringProject.Halcon.Models.RoiShape.Circle,
                    CenterRow = centerRow,
                    CenterCol = centerCol,
                    Radius = radius,
                    IsTaught = true
                };
            }
    ```

    **2F. PublishDatumRoiCandidates 호출 지점 선택**

    Plan 03 은 **InspectionListView 를 수정하지 않는다** (CONTEXT D-13). 따라서 selection-change 이벤트 구독 대신 다음 3 지점에서 publish/clear 를 수행:

    1. **TeachDatumButton_Click (L987) 진입 시** — Datum 노드 선택 확인 직후 PublishDatumRoiCandidates(datum). teach 중에도 edit 허용 (단, teach 와 edit 경로가 RoiId prefix 로 구분되므로 충돌 없음 — teach 는 draft ROI 이지 Datum.Line1 prefix 아님).
    2. **InvokeTryTeachDatum 성공 직후 (L1196)** — teach 완료 시점에 publish 하여 이후 드래그 이동 가능.
    3. **HandleDatumRoiMove 말미 (위 2B 에 이미 포함됨)** — 이동 후 좌표 갱신된 값으로 re-publish.

    **코드 삽입 위치:**

    - L1004 `_editingDatum = datum;` 다음 줄에:
      ```csharp
                  //260424 hbk Phase 13 D-01..D-04 — Datum 노드 선택 시 hit-test 후보 publish (teach 시작 시점)
                  PublishDatumRoiCandidates(datum);
      ```

    - L1196 `halconViewer.SetDatumOverlay(_editingDatum, true);` 다음 줄에:
      ```csharp
                  //260424 hbk Phase 13 D-01..D-04 — teach 완료 시점에 후보 리스트 갱신 (이후 edit 드래그 가능)
                  PublishDatumRoiCandidates(_editingDatum);
      ```

    **ExitCanvasMode 에서 clear 는 하지 않는다** — teach 취소되어도 Datum 노드 선택 상태는 유지되며 edit 은 가능해야 함 (CONTEXT D-02 "TeachDatum 모드 중이 아니어도 이동 가능").

    **초기 publish 는 언제?** 앱 시작 직후 Datum 노드 선택 시 → 이상적으로는 InspectionListView.SelectionChanged 구독 필요. 본 Plan 은 **teach 진입 시점**까지 publish 를 deferred 허용 — 사용자가 teach 를 한 번은 해야 edit 경로 가동. 취약점으로 UAT 에서 확인 (Task 3 checkpoint #4 "selection 후 teach 없이 edit 시도").

    **대안 (planner 재량):** InspectionListView 의 `RefreshParamEditor` 가 이미 존재 → SelectedParam 변경 시 MainView 가 polling 으로 감지하는 메커니즘. 본 Plan 은 **복잡도 최소화 차원에서 polling 구현하지 않음**. UAT 에서 "teach 없이 edit 시도" 가 필요한 것으로 드러나면 follow-up Quick 으로 InspectionListView.SelectionChanged 이벤트 추가.

    **2G. 빌드 + Task 1 합산 커밋**

    ```bash
    msbuild WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal
    ```

    exit 0 확인. 신규 warning 0 확인.

    ```bash
    git add WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs WPF_Example/UI/ContentItem/MainView.xaml.cs
    git commit -m "feat(phase-13-03): Datum ROI move + auto re-teach (MainResultViewerControl hit-test + MainView branch)"
    ```
  </action>
  <verify>
    <automated>
      (1) grep -cE "private bool IsCurrentNodeDatum\(out DatumConfig datum\)" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (2) grep -cE "private void HandleDatumRoiMove\(DatumConfig datum, RoiMoveCompletedArgs e\)" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (3) grep -cE "private void ApplyDatumRoiDelta\(DatumConfig datum, RoiMoveCompletedArgs e\)" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (4) grep -cE "private void InvokeTryTeachDatumForEdit\(DatumConfig datum\)" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (5) grep -cE "private void PublishDatumRoiCandidates\(DatumConfig datum\)" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (6) grep -cE "private static .+RoiDefinition BuildDatumRectCandidate\(" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (7) grep -cE "private static .+RoiDefinition BuildDatumCircleCandidate\(" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (8) grep -cF "e.RoiId.StartsWith(\"Datum.\")" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (9) grep -cF "case \"Datum.Line1\":" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (10) grep -cF "case \"Datum.Line2\":" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (11) grep -cF "case \"Datum.Circle\":" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (12) grep -cF "case \"Datum.HorizontalA\":" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (13) grep -cF "case \"Datum.HorizontalB\":" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (14) grep -cF "halconViewer.SetDatumRoiCandidates(list)" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (15) grep -cF "PublishDatumRoiCandidates(" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 3 (진입 시 + teach 완료 시 + HandleDatumRoiMove 말미)
      (16) 기존 FAI 경로 untouched 검증: awk '/private void HalconViewer_RoiMoveCompleted/,/^        private/' WPF_Example/UI/ContentItem/MainView.xaml.cs | grep -c "FindFAIByName(e.RoiId)" → 1
      (17) awk '/private void HalconViewer_RoiMoveCompleted/,/^        private/' WPF_Example/UI/ContentItem/MainView.xaml.cs | grep -c "handledCircle" → ≥ 2 (bool 선언 + 조건 분기)
      (18) awk '/private void HalconViewer_RoiMoveCompleted/,/^        private/' WPF_Example/UI/ContentItem/MainView.xaml.cs | grep -c "fai.ROI_Row += e.DeltaRow" → 1
      (19) awk '/private void HalconViewer_RoiMoveCompleted/,/^        private/' WPF_Example/UI/ContentItem/MainView.xaml.cs | grep -c "halconViewer.UpdateDisplayState(rois, e.RoiId, null, null)" → 1 (FAI 경로 말미 untouched)
      (20) grep -cF "260424 hbk Phase 13 D-01" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 3
      (21) grep -cF "260424 hbk Phase 13 D-02" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 5
      (22) grep -cF "260424 hbk Phase 13 D-03" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 3
      (23) grep -cF "260424 hbk Phase 13 D-04" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 2
      (24) msbuild WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal → exit 0
    </automated>
  </verify>
  <done>
    - HalconViewer_RoiMoveCompleted 에 Datum 분기 추가 (최상단 early return), 기존 FAI 로직 한 줄도 변경 없음
    - IsCurrentNodeDatum / HandleDatumRoiMove / ApplyDatumRoiDelta / InvokeTryTeachDatumForEdit / PublishDatumRoiCandidates / BuildDatum{Rect,Circle}Candidate 6 개 신규 메서드
    - PublishDatumRoiCandidates 호출: TeachDatumButton_Click 진입 시 + InvokeTryTeachDatum 성공 후 + HandleDatumRoiMove 말미
    - 이중 신호 (RaisePropertyChanged + RefreshParamEditor + SetDatumOverlay) 패턴 유지
    - 자동 재티칭 성공/실패 모두 label_drawHint 피드백 + SetDatumOverlay 갱신
    - K&R brace 유지
    - 기존 InvokeTryTeachDatum / HalconViewer_DatumRectCompleted / HalconViewer_DatumCircleCompleted / FAI hit-test 시그니처 및 로직 untouched
    - msbuild Debug/x64 exit 0, 신규 warning 0
    - Task 1+2 합산 git commit 완료
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SIMUL_MODE UAT checkpoint — 3 algorithm × ROI 종류별 이동 + FAI 회귀</name>
  <what-built>
    - MainResultViewerControl._datumRoiCandidates 필드 + SetDatumRoiCandidates / ClearDatumRoiCandidates / HitTestSelectedRoi fallback / MouseDown Datum 모드 통과
    - MainView.HalconViewer_RoiMoveCompleted Datum 분기 (early return) + ApplyDatumRoiDelta + InvokeTryTeachDatumForEdit + PublishDatumRoiCandidates
    - Datum RoiId prefix 규약: `"Datum.Line1"` / `"Datum.Line2"` / `"Datum.Circle"` / `"Datum.HorizontalA"` / `"Datum.HorizontalB"`
  </what-built>
  <how-to-verify>
    **환경:** SIMUL_MODE Debug/x64 빌드.

    **테스트 1 — TwoLineIntersect Line1 이동:**
    1. Datum (AlgorithmType=TwoLineIntersect) 노드 선택 → btn_teachDatum → Line1/Line2 드래그로 티칭 완료.
    2. 캔버스에서 Line1 rect 내부 클릭 → 드래그로 이동 → MouseUp.
    3. **기대:**
       - label_drawHint 에 "Datum ROI 이동 — 재티칭 OK" 녹색 표시 (또는 실패 시 빨강 + error).
       - 빨간 교점 십자 (RefOrigin) 가 새 위치로 이동 (Line1 가 움직인 결과 교점 좌표 변화).
       - PropertyGrid 의 Line1_Row / Line1_Col 값이 이동량만큼 증가.
       - 재이동 가능 — 같은 Line1 를 연속해서 여러 번 이동 OK.

    **테스트 2 — TwoLineIntersect Line2 이동:**
    1. 위 Datum 상태에서 Line2 rect 드래그 이동.
    2. **기대:** label_drawHint 녹색 + 교점 십자 이동 + PropertyGrid Line2_Row/Col 갱신. Line1 불변.

    **테스트 3 — CircleTwoHorizontal Circle 이동:**
    1. Datum (AlgorithmType=CircleTwoHorizontal) 티칭 완료.
    2. Circle ROI (노란/그린 draft 원) 내부 클릭 → 드래그 이동.
    3. **기대:** 
       - CircleROI_Row/Col 업데이트, Radius 불변.
       - 재티칭 후 검출 원 (노란) + RefOrigin 빨간 십자 이동.
       - Horizontal A/B ROI 불변.

    **테스트 4 — CircleTwoHorizontal Horizontal A/B 이동:**
    1. Horizontal A rect 드래그 이동 → Horizontal_A_Row/Col 변화, B 불변, 재티칭 OK.
    2. Horizontal B rect 드래그 이동 → Horizontal_B_Row/Col 변화, A 불변.
    3. **기대:** 각 이동 후 수평 결합선 방향이 재계산되어 RefOrigin 좌표 변화. Phase 13 Plan 01 의 방향 정합성 검사 임계 (±15° / 90°±5°) 를 벗어나면 재티칭 FAIL → label_drawHint 빨강 + "Horizontal line orientation out of range: ..." 같은 error 표시.

    **테스트 5 — VerticalTwoHorizontal 수직 ROI 이동:**
    1. Datum (AlgorithmType=VerticalTwoHorizontal) 티칭 완료.
    2. 수직 ROI (Line1 rect) 드래그 이동.
    3. **기대:** Line1_Row/Col 변화, 재티칭 성공 시 RefOrigin 십자 이동.

    **테스트 6 — VerticalTwoHorizontal Horizontal A/B 이동:**
    1. Horizontal A/B 각각 이동.
    2. **기대:** Test 4 와 동일 패턴.

    **테스트 7 — FAI ROI 이동 회귀 (CRITICAL):**
    1. Shot > FAI 노드 (RectangleMeasurement 등) 선택 → FAI ROI 를 Edit 모드에서 드래그 이동.
    2. **기대:** Phase 11 Quick 260423-o53 동작 그대로 — FAI.ROI_Row/Col 반영 + UpdateDisplayState + 오버레이 갱신. Datum 분기가 실행되지 않아야 함 (IsCurrentNodeDatum=false).

    **테스트 8 — CircleDiameterMeasurement Circle ROI 이동 회귀:**
    1. FAI with CircleDiameterMeasurement 선택 → Circle ROI 드래그 이동.
    2. **기대:** circle.Circle_Row/Col 반영 (L523-529 경로 untouched).

    **테스트 9 — Datum 노드 비선택 시 hit-test 비활성:**
    1. Datum 노드 선택 해제 (예: 다른 Shot/FAI 로 이동) → 이전 Datum 위치에 있는 rect/circle 영역 클릭 시도.
    2. **기대:** hit-test 발화하지 않음 — _datumRoiCandidates 가 publish 되지 않았으므로 drag 불가. (단 PublishDatumRoiCandidates 가 clear 경로 없는 구조이므로 이전 Datum 의 후보가 남아있을 수 있음 → 이 경우 limitation 으로 UAT 에서 확인 후 follow-up).

    **테스트 10 — teach 중에 edit 시도 (edge case):**
    1. btn_teachDatum 클릭 → Line1 드로잉 모드 상태.
    2. 이전에 그려진 Datum ROI 내부 클릭 시도.
    3. **기대:** 드로잉 모드가 활성이므로 IsAnyDrawingModeActive=true → MouseDown 게이트 진입 안 함 → 드로잉 로직 정상 동작. Datum edit 은 teach 완료 후에만 가능.

    **테스트 11 — 짧은 클릭 (drag 0):**
    1. Datum ROI 내부 클릭만 (드래그 0px).
    2. **기대:** L940 `Math.Abs(dr) > 0.5 || Math.Abs(dc) > 0.5` 필터로 RoiMoveCompleted 발화 안 함 → 재티칭 트리거 안 됨. 조용히 넘어감.

    **테스트 12 — Phase 13 Plan 01 방향 정합성 게이트 통합:**
    1. CircleTwoHorizontal 티칭 완료 → Horizontal A 를 크게 기울여진 방향으로 이동 (불가능 — 이동만이므로 phi 불변). 하지만 Horizontal A 를 다른 위치로 크게 이동 → 수평 결합선 기울기 변화 가능.
    2. **기대:** 재티칭 시 Plan 01 ValidateHorizontalVerticalAngles 가 발동하여 실패 가능. label_drawHint 빨강 + "Horizontal line orientation out of range" 또는 "Horizontal/Vertical perpendicularity violated" 표시.

    **회귀 확인 (반드시 PASS):**
    - 기존 Phase 12 Plan 03 teach 흐름 3 algorithm (TwoLineIntersect / CircleTwoHorizontal / VerticalTwoHorizontal) 완전 회귀 없음 — btn_teachDatum 동작, 라벨 (L1/L2/Circle/H-A/H-B), PropertyGrid write-back, Gap-2/Gap-3 fix 모두 유지.
    - Phase 13 Plan 02 btn_testFindDatum 흐름 회귀 없음 — Datum 이 미티칭이든 티칭이든 test find 동작 변화 없음.
    - FAI CRUD / ROI 드래그 / Circle ROI 드래그 모두 회귀 없음.

    **Visual palette 구분:**
    - teach 성공 RefOrigin: 빨간 2px 십자 (RenderDatumOverlay)
    - edit 후 재티칭 성공 RefOrigin: 동일 빨간 2px (같은 RenderDatumOverlay 호출 — SetDatumOverlay 경로)
    - Plan 02 TryFind 성공 RefOrigin: 주황 3px 십자 (RenderDatumFindResult)
    → Plan 03 은 teach 와 동일 오버레이 재사용 → 시각 변화는 "좌표만 이동". 이는 intended behavior.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (예: "Line1 이동 후 재티칭 안 됨", "FAI ROI 이동 시 Datum 분기로 잘못 진입", "Datum 노드 선택만 하고 teach 없이 edit 시도 시 동작 안 함 (expected limitation)", "테스트 12 방향 정합성 게이트 발동 확인")</resume-signal>
</task>

</tasks>

<verification>
**파일 수준:**
- 수정 파일 2 개: `MainResultViewerControl.xaml.cs`, `MainView.xaml.cs`
- MainResultViewerControl 신규 public 2 개 (SetDatumRoiCandidates, ClearDatumRoiCandidates)
- MainResultViewerControl 신규 private 2 개 (_datumRoiCandidates 필드, HitTestOneRoi 메서드)
- MainResultViewerControl 수정 기존 1 개 (HitTestSelectedRoi — _rois 경로 유지 + _datumRoiCandidates fallback 추가)
- MainResultViewerControl MouseDown 게이트 확장 (게이트 조건에 datumCandidatesPresent OR 추가 — 기존 FAI Edit 모드 경로 유지)
- MainView 신규 private 6 개 + 수정 기존 1 개 (HalconViewer_RoiMoveCompleted — 최상단에 Datum 분기 추가, FAI 로직 한 줄도 변경 없음)
- PublishDatumRoiCandidates 호출 3 지점 (TeachDatumButton_Click 진입, InvokeTryTeachDatum 성공, HandleDatumRoiMove 말미)

**수준 검증 (Task 1+2 누적):**
```
grep -cE "private List<RoiDefinition> _datumRoiCandidates"       → 1
grep -cE "public void SetDatumRoiCandidates\("                    → 1 (MainResultViewerControl)
grep -cE "private static RoiDefinition HitTestOneRoi\("           → 1
grep -cE "private bool IsCurrentNodeDatum\("                      → 1 (MainView)
grep -cE "private void HandleDatumRoiMove\("                      → 1
grep -cE "private void ApplyDatumRoiDelta\("                      → 1
grep -cE "private void InvokeTryTeachDatumForEdit\("              → 1
grep -cE "private void PublishDatumRoiCandidates\("               → 1
grep -cE "private static .+RoiDefinition BuildDatumRectCandidate\(" → 1
grep -cE "private static .+RoiDefinition BuildDatumCircleCandidate\(" → 1
grep -cF "e.RoiId.StartsWith(\"Datum.\")"                         → 1
grep -c "case \"Datum\\."                                          → 5 (Line1/Line2/Circle/HorizontalA/HorizontalB)
grep -cF "PublishDatumRoiCandidates("                              → ≥ 3
grep -cF "//260424 hbk Phase 13"                                   → ≥ 20
```

**빌드:**
- msbuild Debug/x64 exit 0 (Task 2 커밋 후 누적 빌드)
- MainResultViewerControl.xaml.cs / MainView.xaml.cs 대상 신규 warning 0

**SIMUL_MODE UAT (Task 3 checkpoint 범위):**
- 12 개 시나리오 전원 PASS (단 테스트 9 의 "Datum 비선택 후 후보 잔류" 는 알려진 limitation — UAT 에서 명시 확인) → checker:human-verify "approved"

**CONTEXT D-13 회귀 확인:**
- `DatumConfig` public 시그니처 변경 0 (Plan 03 은 DatumConfig 필드를 read/write 만)
- `DatumFindingService.TryTeachDatum / TryFindDatum` 시그니처 변경 0 (호출만 — TryTeachDatum 은 edit 세션 재티칭에서 호출)
- `HalconDisplayService.RenderDatumOverlay` 시그니처 변경 0 (SetDatumOverlay 호출만)
- `MainView.TeachDatumButton_Click / HalconViewer_DatumRectCompleted / HalconViewer_DatumCircleCompleted / InvokeTryTeachDatum / StartDatumTeachStep / AdvanceDatumTeachStep / GetFirstStep / GetNextStep / ExitCanvasMode` 로직 변경 0 (진입 시점에 PublishDatumRoiCandidates 호출만 삽입 — 기존 로직 유지)
- 기존 `HalconViewer_RoiMoveCompleted` 의 FAI 경로 (L517-537) 한 줄도 변경 없음 — Datum 분기는 최상단에 early return 으로만 추가
- 기존 `HitTestSelectedRoi` 의 `_rois` 검사 로직은 HitTestOneRoi helper 로 추출되어 동작 동일 (refactor, 행동 변화 없음)

**Phase 13 Plan 01 게이트와의 통합:**
- Plan 03 의 InvokeTryTeachDatumForEdit 는 DatumFindingService.TryTeachDatum 을 호출 → Plan 01 의 ValidateHorizontalVerticalAngles 가 자동 발동. CircleTwoHorizontal / VerticalTwoHorizontal edit 시 방향 정합성 위반하면 재티칭 FAIL → label_drawHint 빨강 + error. 별도 wiring 불필요.

**Phase 13 Plan 02 와의 통합:**
- Plan 02 의 btn_testFindDatum 은 SelectedParam 을 datum 으로 해결. Plan 03 의 edit 은 `_editingDatum` 이 아닌 SelectedParam 를 기준으로 동작 — 경로 충돌 없음.
- Plan 02 의 `_datumFindResultOverlay` 와 Plan 03 의 `_datumRoiCandidates` 는 독립 필드 — MainResultViewerControl 에 각자 추가.
</verification>

<success_criteria>
- [ ] MainResultViewerControl.xaml.cs 에 `_datumRoiCandidates` 필드 + SetDatumRoiCandidates / ClearDatumRoiCandidates public + HitTestSelectedRoi fallback + HitTestOneRoi helper + MouseDown 게이트 확장 (Allman brace)
- [ ] MainView.xaml.cs 에 HalconViewer_RoiMoveCompleted Datum 분기 (최상단 early return) + IsCurrentNodeDatum + HandleDatumRoiMove + ApplyDatumRoiDelta + InvokeTryTeachDatumForEdit + PublishDatumRoiCandidates + BuildDatumRectCandidate + BuildDatumCircleCandidate (K&R brace)
- [ ] PublishDatumRoiCandidates 호출 3 지점 (TeachDatumButton_Click 진입 + InvokeTryTeachDatum 성공 + HandleDatumRoiMove 말미)
- [ ] ApplyDatumRoiDelta: 5 case switch (Datum.Line1/Line2/Circle/HorizontalA/HorizontalB) + default log
- [ ] VerticalTwoHorizontal 의 수직 ROI 는 RoiId="Datum.Line1" 로 통일 (Phase 12 D-07 재사용)
- [ ] 이중 신호 (RaisePropertyChanged + RefreshParamEditor + SetDatumOverlay) 유지 (Phase 12 Gap-3 패턴)
- [ ] 자동 재티칭 성공/실패 모두 label_drawHint 피드백 + SetDatumOverlay 갱신
- [ ] Phase 13 Plan 01 ValidateHorizontalVerticalAngles 가 재티칭 경로에서 자동 발동 (별도 wiring 없이)
- [ ] 기존 FAI ROI 이동 로직 (L517-537) 한 줄도 변경 없음 — 회귀 0
- [ ] 기존 Phase 12 Plan 03 teach 흐름 회귀 없음
- [ ] CONTEXT D-13 준수: DatumConfig / DatumFindingService / HalconDisplayService public 시그니처 변경 0
- [ ] 주석 convention `//260424 hbk Phase 13 D-01..D-04` 모든 신규 라인
- [ ] msbuild Debug/x64 exit 0 + 신규 warning 0
- [ ] Task 1+2 합산 1 commit
- [ ] Task 3 SIMUL_MODE UAT 12 시나리오 PASS → "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/13-datum-algorithm-extensibility/13-03-SUMMARY.md` following the template structure:
- Files Changed 테이블 (2 파일 modified)
- Gap-1 coverage 매핑 (D-01 진입 트리거 / D-02 편집 대상 범위 / D-03 자동 재티칭 / D-04 기존 코드 재사용 각각의 구현 위치)
- New public/private API 카탈로그 (SetDatumRoiCandidates / ClearDatumRoiCandidates / IsCurrentNodeDatum / HandleDatumRoiMove / ApplyDatumRoiDelta / InvokeTryTeachDatumForEdit / PublishDatumRoiCandidates / BuildDatum*Candidate / HitTestOneRoi)
- RoiId prefix 규약 문서화 (Datum.Line1 / Datum.Line2 / Datum.Circle / Datum.HorizontalA / Datum.HorizontalB)
- Build Result 블록
- UAT Outcome (Task 3 12 시나리오 결과 + 회귀 확인)
- Known limitations (Datum 비선택 후 후보 잔류 — InspectionListView.SelectionChanged 이벤트 미구독의 결과)
- Phase 13 Plan 01 + Plan 02 통합 확인 (Plan 01 방향 정합성 게이트 자동 발동 / Plan 02 btn_testFindDatum 독립 동작)
- Deviations from Plan (없으면 "Plan Adherence: All tasks executed as specified")
- Self-Check 체크리스트
</output>
</content>
