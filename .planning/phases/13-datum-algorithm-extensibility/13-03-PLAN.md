---
phase: 13-datum-algorithm-extensibility
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified:
  - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: false
requirements:
  - Gap-1-DatumRoiEdit
  - Gap-A-DatumRoiDelete
tags: [datum, ui, roi-edit, roi-delete, phase-13]
must_haves:
  truths:
    - "Datum 노드가 선택된 상태에서 캔버스의 Datum ROI(Line1/Line2/Circle/HorizA/HorizB)를 마우스 드래그하면 해당 DatumConfig 필드(Row/Col)에 delta 가 누적되고 LastTeachSucceeded 가 갱신된다 (Gap-1)"
    - "Datum ROI 를 클릭하면 _selectedRoiId 가 'Datum.*' 로 set 되어 기존 우클릭 컨텍스트 메뉴의 Delete 항목이 동작한다 (Gap-A)"
    - "RoiDeleteRequested 이벤트가 'Datum.*' RoiId 로 발화하면 MainView 가 Datum 분기로 early return 하여 해당 DatumConfig 의 ROI 필드(Length1/Length2/Radius)를 0 으로 clear + IsConfigured/LastTeachSucceeded false → RenderDatumOverlay 의 그리기 가드(if Length1>0 && Length2>0)에 걸려 시각적으로 사라진다 (Gap-A)"
    - "기존 FAI ROI 이동(Quick 260423-o53) / 컨텍스트 메뉴 Delete(FindFAIByName 경로) 가 회귀 없이 동작 — Datum 분기는 RoiId.StartsWith('Datum.') 로 분기"
    - "Plan 02 의 CustomMessageBox.Show 두 호출의 (message, title) 인자 순서를 (title, message) 로 교체한다 — 코드베이스 다른 호출과 일치 (cleanup)"
    - "msbuild Debug/x64 가 exit 0 로 성공하고 2 개 수정 파일을 참조하는 신규 warning 이 0 개이다"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs"
      provides: "_datumRoiCandidates 필드 + SetDatumRoiCandidates/ClearDatumRoiCandidates public + HitTestSelectedRoi fallback + MouseDown Datum 모드 통과 + Datum 클릭 시 _selectedRoiId set"
      contains: "SetDatumRoiCandidates"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "HalconViewer_RoiMoveCompleted Datum 분기 + HalconViewer_RoiDeleteRequested Datum 분기 + ApplyDatumRoiDelta + ClearDatumRoiFields + InvokeTryTeachDatumForEdit + PublishDatumRoiCandidates + Build* helpers + CustomMessageBox 인자 순서 fix"
      contains: "ClearDatumRoiFields"
  key_links:
    - from: "MainView.HalconViewer_RoiMoveCompleted"
      to: "MainView.ApplyDatumRoiDelta"
      via: "IsCurrentNodeDatum && e.RoiId.StartsWith('Datum.') 시 분기"
      pattern: "IsCurrentNodeDatum\\(out"
    - from: "MainView.HalconViewer_RoiDeleteRequested"
      to: "MainView.ClearDatumRoiFields"
      via: "roiId.StartsWith('Datum.') 시 분기 — FAI lookup 전에 early return"
      pattern: "ClearDatumRoiFields\\("
    - from: "MainResultViewerControl.HitTestSelectedRoi"
      to: "MainResultViewerControl._datumRoiCandidates"
      via: "_rois FirstOrDefault null fallback"
      pattern: "_datumRoiCandidates"
---

<objective>
**Phase 13 scope 재분할 (사용자 의도 정직 매핑 결과 — 2026-04-25 split):**

이전 13-03 (Gap-1 + Gap-A + Gap-C 합본) 에서 **Gap-C (Detected Line Extension) 분리** → 시각화 묶음(13-05) 으로 이전.
이번 13-03 은 **즉시 응급 처치 가능한 UX 빈틈 2건만** 처리 — 사용자가 그린 ROI 를 옮기고 지울 수 있게 한다.

1. **Gap-1 (ROI Edit)**: Datum ROI 드래그 이동 → 즉시 자동 재티칭 → 검출 오버레이 갱신
2. **Gap-A (ROI Delete)**: 기존 우클릭 컨텍스트 메뉴 Delete 가 Datum 에서도 동작 — `RoiDeleteRequested` 이벤트 재사용

**Plan 02 follow-up:**
- CustomMessageBox.Show 두 호출의 인자 순서가 (message, title) → (title, message) 로 swap 됨 — 코드베이스 다른 호출(`CustomMessageBox.Show("Error", "Camera Init Fail", ...)` 패턴)과 일치시킴

**13-04 로 이월 (per-ROI 에지 파라미터):**
- EdgeDirection / EdgeThreshold / EdgeSampleCount / EdgeTrimCount / EdgePolarity per ROI
- DatumConfig 스키마 확장 (25 필드 추가, INI 하위호환)
- DatumFindingService 가 per-ROI 파라미터 사용
- PropertyGrid 노출

**13-05 로 이월 (시각화):**
- 검출 라인 화면 끝까지 외삽 (이전 Gap-C, EXTEND_PX 외삽 helper)
- Raw 검출 에지점들 캔버스 점 마커 표시
- 최종 reference 좌표 (교점/중심) UI 라벨 텍스트 표시

**Output (이번 13-03):**
- `WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs`:
  - `private List<RoiDefinition> _datumRoiCandidates` 필드
  - `public void SetDatumRoiCandidates(IList<RoiDefinition>)` / `public void ClearDatumRoiCandidates()` API
  - `HitTestSelectedRoi` fallback (기존 _rois null 시 _datumRoiCandidates 검사)
  - `HitTestOneRoi` private static helper (Rect/Circle 판정 로직 추출)
  - MouseDown Edit-mode 게이트 → `_isEditMode || datumCandidatesPresent` 통과
  - **Datum hit 시 `_selectedRoiId = hitRoi.Id` set** — 기존 컨텍스트 메뉴 Delete 가 그대로 동작 (Gap-A 핵심)
- `WPF_Example/UI/ContentItem/MainView.xaml.cs`:
  - `HalconViewer_RoiMoveCompleted` 최상단 Datum 분기 (early return)
  - `HalconViewer_RoiDeleteRequested` 최상단 Datum 분기 (early return)
  - `IsCurrentNodeDatum(out DatumConfig)` helper
  - `HandleDatumRoiMove(DatumConfig, RoiMoveCompletedArgs)` — ApplyDatumRoiDelta + 이중 신호 + InvokeTryTeachDatumForEdit + PublishDatumRoiCandidates
  - `ApplyDatumRoiDelta(DatumConfig, RoiMoveCompletedArgs)` — 5-case switch
  - `ClearDatumRoiFields(DatumConfig, string roiId)` — 5-case switch, 해당 ROI 필드 0 reset + IsConfigured/LastTeachSucceeded false
  - `InvokeTryTeachDatumForEdit(DatumConfig)` — TryTeachDatum 자동 재호출 + label_drawHint 피드백
  - `PublishDatumRoiCandidates(DatumConfig)` — DatumConfig → RoiDefinition 리스트 변환 + halconViewer.SetDatumRoiCandidates
  - `BuildDatumRectCandidate(string id, double row, double col, double halfH, double halfW)` private static
  - `BuildDatumCircleCandidate(string id, double row, double col, double radius)` private static
  - PublishDatumRoiCandidates 호출 3 지점: TeachDatumButton_Click 진입 + InvokeTryTeachDatum 성공 + HandleDatumRoiMove 말미
  - **CustomMessageBox.Show 두 호출 인자 순서 (message, title) → (title, message) swap** (Plan 02 cleanup)

- msbuild Debug/x64 exit 0 + 신규 warning 0
- SIMUL_MODE UAT checkpoint

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-datum-algorithm-extensibility/13-CONTEXT.md
@.planning/phases/13-datum-algorithm-extensibility/13-PATTERNS.md
@.planning/phases/13-datum-algorithm-extensibility/13-01-SUMMARY.md
@.planning/phases/13-datum-algorithm-extensibility/13-02-SUMMARY.md
@WPF_Example/UI/ContentItem/MainView.xaml.cs
@WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
@WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
@WPF_Example/Halcon/Models/RoiDefinition.cs
@WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
@CLAUDE.md

<existing_infra_reuse>
**Critical — 기존 인프라 재사용 (코드 신설 최소화):**

`MainResultViewerControl.xaml.cs` 에 이미 존재:
- `public event EventHandler<string> RoiDeleteRequested;` (L127)
- `private void DeleteRoiMenuItem_Click(...)` 컨텍스트 메뉴 핸들러 (L1283) — `_selectedRoiId` 검사 후 `RoiDeleteRequested?.Invoke(this, _selectedRoiId)` 발화

`MainView.xaml.cs` 에 이미 존재:
- `halconViewer.RoiDeleteRequested += HalconViewer_RoiDeleteRequested;` 구독 (L65)
- `private void HalconViewer_RoiDeleteRequested(object sender, string roiId)` (L486) — 현재 FAI 만 처리

**즉 Datum delete 는 기존 이벤트 흐름에 분기만 추가하면 됨:**
1. MainResultViewerControl: Datum ROI 클릭 시 `_selectedRoiId = hitRoi.Id` set → 기존 컨텍스트 메뉴 Delete 가 그대로 동작
2. MainView.HalconViewer_RoiDeleteRequested: 최상단에 `if (roiId != null && roiId.StartsWith("Datum.") && IsCurrentNodeDatum(out var datum)) { ClearDatumRoiFields(datum, roiId); return; }` 삽입
</existing_infra_reuse>

<interfaces>
From WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs (Plan 02 까지 확정):
```csharp
public HImage CurrentImage { get; private set; }
public event EventHandler<RoiMoveCompletedArgs> RoiMoveCompleted;       // L116
public event EventHandler<string> RoiDeleteRequested;                    // L127 — Datum 도 재사용
public void SetDatumOverlay(DatumConfig datum, bool isSelected);         // L512 (untouched)
public void SetDatumFindResultOverlay(DatumConfig datum);                // Plan 02
public void ClearDatumFindResultOverlay();                                // Plan 02
private RoiDefinition HitTestSelectedRoi(Point imagePoint);              // L304 — Plan 03 확장
private List<RoiDefinition> _rois;                                       // FAI ROI 리스트 (untouched)
private string _selectedRoiId;                                            // 현재 선택 ROI — Datum hit 시에도 set
private bool _isEditMode;                                                 // Edit 모드 (Datum 모드 무관)
// Plan 03 신규:
//   private List<RoiDefinition> _datumRoiCandidates;
//   public void SetDatumRoiCandidates(IList<RoiDefinition>);
//   public void ClearDatumRoiCandidates();
//   private static RoiDefinition HitTestOneRoi(RoiDefinition, Point);
```

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs:
```csharp
public EDatumAlgorithm AlgorithmTypeEnum { get; }
public double Line1_Row, Line1_Col, Line1_Phi, Line1_Length1, Line1_Length2;
public double Line2_Row, Line2_Col, Line2_Phi, Line2_Length1, Line2_Length2;
public double CircleROI_Row, CircleROI_Col, CircleROI_Radius;
public double Horizontal_A_Row, Horizontal_A_Col, Horizontal_A_Phi, Horizontal_A_Length1, Horizontal_A_Length2;
public double Horizontal_B_Row, Horizontal_B_Col, Horizontal_B_Phi, Horizontal_B_Length1, Horizontal_B_Length2;
public bool IsConfigured, LastTeachSucceeded;
public double Line1Detected_RBegin, Line1Detected_CBegin, Line1Detected_REnd, Line1Detected_CEnd;
public double Line2Detected_RBegin, Line2Detected_CBegin, Line2Detected_REnd, Line2Detected_CEnd;
public double CircleCenter_Row, CircleCenter_Col, CircleDetected_Radius;
public void RaisePropertyChanged(string name);
```

From WPF_Example/UI/ContentItem/MainView.xaml.cs:
```csharp
private MainWindow mParentWindow;
private DatumConfig _editingDatum;                        // teach 세션 (edit 과 독립)
private void HalconViewer_RoiMoveCompleted(object sender, RoiMoveCompletedArgs e);  // L515 — Datum 분기 추가
private void HalconViewer_RoiDeleteRequested(object sender, string roiId);          // L486 — Datum 분기 추가
private void InvokeTryTeachDatum();                        // L1174 (untouched)
// 신규:
//   private bool IsCurrentNodeDatum(out DatumConfig);
//   private void HandleDatumRoiMove(DatumConfig, RoiMoveCompletedArgs);
//   private void ApplyDatumRoiDelta(DatumConfig, RoiMoveCompletedArgs);
//   private void ClearDatumRoiFields(DatumConfig, string roiId);
//   private void InvokeTryTeachDatumForEdit(DatumConfig);
//   private void PublishDatumRoiCandidates(DatumConfig);
//   private static RoiDefinition BuildDatumRectCandidate(string, double, double, double, double);
//   private static RoiDefinition BuildDatumCircleCandidate(string, double, double, double);
```
</interfaces>

**Brace style (PATTERNS.md):**
- `MainView.xaml.cs` = K&R
- `MainResultViewerControl.xaml.cs` = Allman

**C# 7.2:** `out var` OK. switch expression / record / nullable reference types 금지.

**주석 convention:** 모든 신규 라인에 `//260425 hbk Phase 13 D-01..D-04` 또는 `//260425 hbk Phase 13 D-A` 형식.

**Datum RoiId prefix 규약 (Plan 03 이 최초 정의 + 사용):**
- `"Datum.Line1"` — TwoLineIntersect Line1 OR VerticalTwoHorizontal 수직 ROI
- `"Datum.Line2"` — TwoLineIntersect Line2
- `"Datum.Circle"` — CircleTwoHorizontal Circle ROI
- `"Datum.HorizontalA"` / `"Datum.HorizontalB"` — CircleTwoHorizontal + VerticalTwoHorizontal 공용

</context>

<tasks>

<task type="auto">
  <name>Task 1: MainResultViewerControl — Datum ROI hit-test fallback + _selectedRoiId set on Datum hit</name>
  <files>WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs L300-340 (HitTestSelectedRoi)
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs L505-540 (SetDatumOverlay/SetDatumFindResultOverlay 패턴)
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs L690-740 (MouseDown Edit-mode 게이트)
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs L940-980 (MouseUp RoiMoveCompleted 발화)
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs L1280-1290 (DeleteRoiMenuItem_Click — _selectedRoiId 사용 패턴)
  </read_first>
  <action>
    **1A. `_datumRoiCandidates` 필드 추가** (Plan 02 의 _datumFindResultOverlay 필드 다음 줄):

    ```csharp
            //260425 hbk Phase 13 D-02 — Datum ROI 편집 후보 (Datum 노드 선택 시 MainView 가 주입)
            private List<RoiDefinition> _datumRoiCandidates = new List<RoiDefinition>();
    ```

    **1B. `SetDatumRoiCandidates` / `ClearDatumRoiCandidates` 추가** (Plan 02 의 ClearDatumFindResultOverlay 다음):

    ```csharp
            //260425 hbk Phase 13 D-02 — Datum ROI 후보 주입 (MainView publish 진입점)
            public void SetDatumRoiCandidates(IList<RoiDefinition> datumRois)
            {
                if (datumRois == null)
                {
                    _datumRoiCandidates.Clear();
                }
                else
                {
                    _datumRoiCandidates = datumRois.Where(r => r != null).Select(r => r.Clone()).ToList();
                }
            }

            //260425 hbk Phase 13 D-02 — Datum 노드 비선택 시 후보 clear
            public void ClearDatumRoiCandidates()
            {
                _datumRoiCandidates.Clear();
            }
    ```

    **1C. `HitTestSelectedRoi` 확장 + `HitTestOneRoi` helper 추출** (기존 메서드 교체):

    ```csharp
            //260423 hbk ROI hit-test
            //260425 hbk Phase 13 D-02 — _rois (FAI) FirstOrDefault null fallback 으로 _datumRoiCandidates 검사
            private RoiDefinition HitTestSelectedRoi(Point imagePoint)
            {
                if (!string.IsNullOrEmpty(_selectedRoiId))
                {
                    var roi = _rois.FirstOrDefault(r => r.Id == _selectedRoiId);
                    if (roi != null)
                    {
                        var hit = HitTestOneRoi(roi, imagePoint);
                        if (hit != null) return hit;
                    }
                }

                //260425 hbk Phase 13 D-02 — Datum 후보 fallback (_selectedRoiId 무관 hit 허용)
                if (_datumRoiCandidates != null && _datumRoiCandidates.Count > 0)
                {
                    foreach (var candidate in _datumRoiCandidates)
                    {
                        if (candidate == null) continue;
                        var hit = HitTestOneRoi(candidate, imagePoint);
                        if (hit != null) return hit;
                    }
                }

                return null;
            }

            //260425 hbk Phase 13 D-02 — Rect/Circle 공통 hit 판정 helper
            private static RoiDefinition HitTestOneRoi(RoiDefinition roi, Point imagePoint)
            {
                if (roi == null) return null;
                if (roi.Shape == RoiShape.Circle)
                {
                    double dr = imagePoint.Y - roi.CenterRow;
                    double dc = imagePoint.X - roi.CenterCol;
                    if (Math.Sqrt(dr * dr + dc * dc) <= roi.Radius) return roi;
                    return null;
                }
                if (imagePoint.Y >= roi.Row1 && imagePoint.Y <= roi.Row2 &&
                    imagePoint.X >= roi.Column1 && imagePoint.X <= roi.Column2)
                {
                    return roi;
                }
                return null;
            }
    ```

    **1D. MouseDown Edit-mode 게이트 확장** (기존 `if (_isEditMode && !IsAnyDrawingModeActive() && HasImage)` 교체):

    ```csharp
                //260423 hbk Edit 모드 또는
                //260425 hbk Phase 13 D-01 — Datum 후보가 publish 되어 있으면 _isEditMode 무관 통과 (Datum 노드 선택 = 자동 편집 허용)
                bool datumCandidatesPresent = (_datumRoiCandidates != null && _datumRoiCandidates.Count > 0);
                if ((_isEditMode || datumCandidatesPresent) && !IsAnyDrawingModeActive() && HasImage)
                {
                    // Edit 모드 전용 (FAI 리사이즈 핸들) — Datum 은 리사이즈 미지원
                    if (_isEditMode)
                    {
                        var selectedRoi = _rois.FirstOrDefault(r => r.Id == _selectedRoiId && r.IsTaught);
                        if (selectedRoi != null)
                        {
                            var handleHit = HitTestEditHandle(selectedRoi, mouseState.ImagePoint);
                            if (handleHit.Handle != ResizeHandle.None)
                            {
                                _isResizingRoi = true;
                                _resizingHandle = handleHit.Handle;
                                _resizingPolygonIndex = handleHit.PolyIndex;
                                _resizingRoiSnapshot = selectedRoi.Clone();
                                SetPanCursor(Cursors.SizeAll);
                                PublishPointerInfo();
                                return;
                            }
                        }
                    }

                    var hitRoi = HitTestSelectedRoi(mouseState.ImagePoint);
                    if (hitRoi != null)
                    {
                        //260425 hbk Phase 13 D-02 — Datum hit 시 _selectedRoiId 도 set → 기존 컨텍스트 메뉴 Delete 가 그대로 동작
                        if (hitRoi.Id != null && hitRoi.Id.StartsWith("Datum."))
                        {
                            _selectedRoiId = hitRoi.Id;
                        }
                        _isMovingRoi = true;
                        _moveStartImagePoint = mouseState.ImagePoint;
                        _movingRoiSnapshot = hitRoi.Clone();
                        SetPanCursor(Cursors.SizeAll);
                        PublishPointerInfo();
                        return;
                    }
                }
    ```

    **1E. msbuild 단독 빌드 (Task 2 가 호출하기 전이라 SetDatumRoiCandidates 가 미사용 warning 가능 — Task 2 완료 후 합산 빌드로 검증):**

    ```bash
    msbuild WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal
    ```
    exit 0 확인.

    **1F. git add 만 수행, commit 은 Task 2 완료 후 합산.**
  </action>
  <verify>
    <automated>
      (1) grep -cE "private List&lt;RoiDefinition&gt; _datumRoiCandidates" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → 1
      (2) grep -cE "public void SetDatumRoiCandidates\(IList&lt;RoiDefinition&gt; datumRois\)" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → 1
      (3) grep -cE "public void ClearDatumRoiCandidates\(\)" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → 1
      (4) grep -cE "private static RoiDefinition HitTestOneRoi\(" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → 1
      (5) grep -cF "datumCandidatesPresent" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → ≥ 1
      (6) grep -cF "_selectedRoiId = hitRoi.Id" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → ≥ 1
      (7) grep -cF "hitRoi.Id.StartsWith(\"Datum.\")" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → 1
      (8) msbuild WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal → exit 0
    </automated>
  </verify>
  <done>
    - 필드/메서드/HitTest 확장/MouseDown 게이트/Datum hit 시 _selectedRoiId set 완료
    - 기존 SetDatumOverlay / RoiMoveCompleted / RoiDeleteRequested 시그니처 untouched
    - 단독 빌드 exit 0
    - git stage 만 (commit 은 Task 2 합산)
  </done>
</task>

<task type="auto">
  <name>Task 2: MainView — Datum 분기 (move + delete) + Plan 02 CustomMessageBox 인자 순서 fix</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L60-70 (이벤트 구독)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L486-512 (HalconViewer_RoiDeleteRequested — FAI 경로)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L514-540 (HalconViewer_RoiMoveCompleted — FAI 경로)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L987-1010 (TeachDatumButton_Click — SelectedParam as DatumConfig)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L1095-1170 (HalconViewer_DatumRectCompleted/CircleCompleted)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L1174-1208 (InvokeTryTeachDatum — Plan 02 가 CustomMessageBox 사용한 자리)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs Plan 02 가 추가한 BtnTestFindDatum_Click + AskTestImageSource (CustomMessageBox.Show 2 곳)
  </read_first>
  <action>
    **2A. HalconViewer_RoiMoveCompleted 에 Datum 분기 추가** (기존 메서드 교체, 기존 FAI 로직 한 줄도 변경 금지):

    ```csharp
            //260423 hbk ROI 이동 완료 → FAI 모델 좌표 반영
            //260425 hbk Phase 13 D-01..D-04 — Datum 컨텍스트(SelectedParam=DatumConfig + RoiId='Datum.*') 에서는 Datum 분기로 early return
            private void HalconViewer_RoiMoveCompleted(object sender, RoiMoveCompletedArgs e) {
                if (e == null || string.IsNullOrEmpty(e.RoiId)) return;

                //260425 hbk Phase 13 D-01..D-04 — Datum 분기 우선 (FAI lookup 전에)
                DatumConfig datum;
                if (e.RoiId.StartsWith("Datum.") && IsCurrentNodeDatum(out datum)) {
                    HandleDatumRoiMove(datum, e);
                    return;
                }

                //260423 hbk 기존 FAI 경로 (untouched from Phase 11 Quick 260423-o53)
                var fai = FindFAIByName(e.RoiId);
                if (fai == null) return;

                bool handledCircle = false;
                foreach (var m in fai.Measurements) {
                    var circle = m as CircleDiameterMeasurement;
                    if (circle != null) {
                        circle.Circle_Row += e.DeltaRow;
                        circle.Circle_Col += e.DeltaCol;
                        handledCircle = true;
                        break;
                    }
                }
                if (!handledCircle) {
                    fai.ROI_Row += e.DeltaRow;
                    fai.ROI_Col += e.DeltaCol;
                }

                var rois = GetCurrentFAIRois();
                halconViewer.UpdateDisplayState(rois, e.RoiId, null, null);
            }
    ```

    **2B. HalconViewer_RoiDeleteRequested 에 Datum 분기 추가** (기존 메서드 교체):

    ```csharp
            //260423 hbk ContextMenu Delete 핸들러
            //260425 hbk Phase 13 D-A — Datum RoiId prefix 면 Datum 분기로 early return (FAI lookup 전에)
            private void HalconViewer_RoiDeleteRequested(object sender, string roiId) {
                if (string.IsNullOrEmpty(roiId)) return;

                //260425 hbk Phase 13 D-A — Datum 분기 우선
                if (roiId.StartsWith("Datum.")) {
                    DatumConfig datum;
                    if (IsCurrentNodeDatum(out datum)) {
                        ClearDatumRoiFields(datum, roiId);
                        try { datum.RaisePropertyChanged(string.Empty); } catch { }
                        mParentWindow?.inspectionList?.RefreshParamEditor();
                        halconViewer.SetDatumOverlay(datum, true);
                        PublishDatumRoiCandidates(datum); // 잔존 ROI 만 후보로 남도록 갱신
                    }
                    return;
                }

                //260423 hbk 기존 FAI 경로 (untouched)
                var fai = FindFAIByName(roiId);
                if (fai == null) return;

                fai.ROI_Row = 0;
                fai.ROI_Col = 0;
                fai.ROI_Phi = 0;
                fai.ROI_Length1 = 0;
                fai.ROI_Length2 = 0;
                fai.PolygonPoints = "";
                foreach (var m in fai.Measurements) {
                    var circle = m as CircleDiameterMeasurement;
                    if (circle != null) {
                        circle.Circle_Row = 0;
                        circle.Circle_Col = 0;
                        circle.Circle_Radius = 0;
                    }
                }
                var rois = GetCurrentFAIRois();
                halconViewer.UpdateDisplayState(rois, null, null, null);
            }
    ```

    **2C. 신규 helpers 추가** (HalconViewer_RoiMoveCompleted 다음 위치):

    ```csharp
            //260425 hbk Phase 13 D-01 — 현재 선택 노드가 Datum 인지 판정
            private bool IsCurrentNodeDatum(out DatumConfig datum) {
                datum = mParentWindow?.inspectionList?.SelectedParam as DatumConfig;
                return datum != null;
            }

            //260425 hbk Phase 13 D-01..D-04 — Datum ROI 이동 후 처리 (delta + 이중 신호 + 자동 재티칭 + 후보 publish)
            private void HandleDatumRoiMove(DatumConfig datum, RoiMoveCompletedArgs e) {
                ApplyDatumRoiDelta(datum, e);
                try { datum.RaisePropertyChanged(string.Empty); } catch { }
                mParentWindow?.inspectionList?.RefreshParamEditor();
                halconViewer.SetDatumOverlay(datum, true);
                InvokeTryTeachDatumForEdit(datum);
                PublishDatumRoiCandidates(datum);
            }

            //260425 hbk Phase 13 D-02 — RoiId prefix 별 DatumConfig 필드 매핑 (delta 누적)
            private void ApplyDatumRoiDelta(DatumConfig datum, RoiMoveCompletedArgs e) {
                if (datum == null || e == null || string.IsNullOrEmpty(e.RoiId)) return;
                switch (e.RoiId) {
                    case "Datum.Line1":
                        datum.Line1_Row += e.DeltaRow; datum.Line1_Col += e.DeltaCol; break;
                    case "Datum.Line2":
                        datum.Line2_Row += e.DeltaRow; datum.Line2_Col += e.DeltaCol; break;
                    case "Datum.Circle":
                        datum.CircleROI_Row += e.DeltaRow; datum.CircleROI_Col += e.DeltaCol; break;
                    case "Datum.HorizontalA":
                        datum.Horizontal_A_Row += e.DeltaRow; datum.Horizontal_A_Col += e.DeltaCol; break;
                    case "Datum.HorizontalB":
                        datum.Horizontal_B_Row += e.DeltaRow; datum.Horizontal_B_Col += e.DeltaCol; break;
                    default:
                        Logging.PrintLog((int)ELogType.Trace, "Datum ROI move: unknown RoiId=" + e.RoiId);
                        break;
                }
            }

            //260425 hbk Phase 13 D-A — RoiId prefix 별 ROI 필드 0 reset (Length1/Length2/Radius) + IsConfigured/LastTeachSucceeded false
            //  RenderDatumOverlay 의 그리기 가드(if Length1>0 && Length2>0)에 걸려 시각적으로 사라짐.
            private void ClearDatumRoiFields(DatumConfig datum, string roiId) {
                if (datum == null || string.IsNullOrEmpty(roiId)) return;
                switch (roiId) {
                    case "Datum.Line1":
                        datum.Line1_Row = 0; datum.Line1_Col = 0; datum.Line1_Phi = 0;
                        datum.Line1_Length1 = 0; datum.Line1_Length2 = 0;
                        break;
                    case "Datum.Line2":
                        datum.Line2_Row = 0; datum.Line2_Col = 0; datum.Line2_Phi = 0;
                        datum.Line2_Length1 = 0; datum.Line2_Length2 = 0;
                        break;
                    case "Datum.Circle":
                        datum.CircleROI_Row = 0; datum.CircleROI_Col = 0; datum.CircleROI_Radius = 0;
                        break;
                    case "Datum.HorizontalA":
                        datum.Horizontal_A_Row = 0; datum.Horizontal_A_Col = 0; datum.Horizontal_A_Phi = 0;
                        datum.Horizontal_A_Length1 = 0; datum.Horizontal_A_Length2 = 0;
                        break;
                    case "Datum.HorizontalB":
                        datum.Horizontal_B_Row = 0; datum.Horizontal_B_Col = 0; datum.Horizontal_B_Phi = 0;
                        datum.Horizontal_B_Length1 = 0; datum.Horizontal_B_Length2 = 0;
                        break;
                    default:
                        Logging.PrintLog((int)ELogType.Trace, "Datum ROI delete: unknown RoiId=" + roiId);
                        return;
                }
                //260425 hbk Phase 13 D-A — 어느 ROI 든 삭제되면 Datum 자체가 불완전 → 검증 disable
                datum.IsConfigured = false;
                datum.LastTeachSucceeded = false;
            }

            //260425 hbk Phase 13 D-03 — Edit 세션 전용 자동 재티칭 (_editingDatum 건드리지 않음)
            private void InvokeTryTeachDatumForEdit(DatumConfig datum) {
                if (datum == null) return;
                HImage img = halconViewer.CurrentImage;
                if (img == null) return;
                var svc = new ReringProject.Halcon.Algorithms.DatumFindingService();
                string error;
                bool ok = svc.TryTeachDatum(img, datum, out error);
                if (ok) {
                    label_drawHint.Content = "Datum ROI 이동 — 재티칭 OK";
                    label_drawHint.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FF4ADE80"));
                    label_drawHint.Visibility = Visibility.Visible;
                }
                else {
                    label_drawHint.Content = "Datum ROI 이동 — 재티칭 실패: " + (error ?? "unknown");
                    label_drawHint.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FFF87171"));
                    label_drawHint.Visibility = Visibility.Visible;
                }
                halconViewer.SetDatumOverlay(datum, true);
            }

            //260425 hbk Phase 13 D-02 — DatumConfig → RoiDefinition 리스트 → halconViewer.SetDatumRoiCandidates publish
            private void PublishDatumRoiCandidates(DatumConfig datum) {
                if (datum == null) { halconViewer.ClearDatumRoiCandidates(); return; }
                var list = new List<ReringProject.Halcon.Models.RoiDefinition>();
                switch (datum.AlgorithmTypeEnum) {
                    case EDatumAlgorithm.TwoLineIntersect:
                        if (datum.Line1_Length1 > 0 && datum.Line1_Length2 > 0)
                            list.Add(BuildDatumRectCandidate("Datum.Line1", datum.Line1_Row, datum.Line1_Col, datum.Line1_Length1, datum.Line1_Length2));
                        if (datum.Line2_Length1 > 0 && datum.Line2_Length2 > 0)
                            list.Add(BuildDatumRectCandidate("Datum.Line2", datum.Line2_Row, datum.Line2_Col, datum.Line2_Length1, datum.Line2_Length2));
                        break;
                    case EDatumAlgorithm.CircleTwoHorizontal:
                        if (datum.CircleROI_Radius > 0)
                            list.Add(BuildDatumCircleCandidate("Datum.Circle", datum.CircleROI_Row, datum.CircleROI_Col, datum.CircleROI_Radius));
                        if (datum.Horizontal_A_Length1 > 0 && datum.Horizontal_A_Length2 > 0)
                            list.Add(BuildDatumRectCandidate("Datum.HorizontalA", datum.Horizontal_A_Row, datum.Horizontal_A_Col, datum.Horizontal_A_Length1, datum.Horizontal_A_Length2));
                        if (datum.Horizontal_B_Length1 > 0 && datum.Horizontal_B_Length2 > 0)
                            list.Add(BuildDatumRectCandidate("Datum.HorizontalB", datum.Horizontal_B_Row, datum.Horizontal_B_Col, datum.Horizontal_B_Length1, datum.Horizontal_B_Length2));
                        break;
                    case EDatumAlgorithm.VerticalTwoHorizontal:
                        if (datum.Line1_Length1 > 0 && datum.Line1_Length2 > 0)
                            list.Add(BuildDatumRectCandidate("Datum.Line1", datum.Line1_Row, datum.Line1_Col, datum.Line1_Length1, datum.Line1_Length2));
                        if (datum.Horizontal_A_Length1 > 0 && datum.Horizontal_A_Length2 > 0)
                            list.Add(BuildDatumRectCandidate("Datum.HorizontalA", datum.Horizontal_A_Row, datum.Horizontal_A_Col, datum.Horizontal_A_Length1, datum.Horizontal_A_Length2));
                        if (datum.Horizontal_B_Length1 > 0 && datum.Horizontal_B_Length2 > 0)
                            list.Add(BuildDatumRectCandidate("Datum.HorizontalB", datum.Horizontal_B_Row, datum.Horizontal_B_Col, datum.Horizontal_B_Length1, datum.Horizontal_B_Length2));
                        break;
                }
                halconViewer.SetDatumRoiCandidates(list);
            }

            //260425 hbk Phase 13 D-02 — Rectangle2 (centerRow, centerCol, phi=0, halfH, halfW) → bbox
            private static ReringProject.Halcon.Models.RoiDefinition BuildDatumRectCandidate(string id, double centerRow, double centerCol, double halfH, double halfW) {
                return new ReringProject.Halcon.Models.RoiDefinition {
                    Id = id, Name = id,
                    Shape = ReringProject.Halcon.Models.RoiShape.Rect,
                    Row1 = centerRow - halfH, Row2 = centerRow + halfH,
                    Column1 = centerCol - halfW, Column2 = centerCol + halfW,
                    IsTaught = true
                };
            }

            //260425 hbk Phase 13 D-02 — Circle → RoiDefinition
            private static ReringProject.Halcon.Models.RoiDefinition BuildDatumCircleCandidate(string id, double centerRow, double centerCol, double radius) {
                return new ReringProject.Halcon.Models.RoiDefinition {
                    Id = id, Name = id,
                    Shape = ReringProject.Halcon.Models.RoiShape.Circle,
                    CenterRow = centerRow, CenterCol = centerCol, Radius = radius,
                    IsTaught = true
                };
            }
    ```

    **2D. PublishDatumRoiCandidates 호출 3 지점 추가:**

    1. **TeachDatumButton_Click** — `_editingDatum = datum;` 다음 줄에:
    ```csharp
                //260425 hbk Phase 13 D-01..D-04 — teach 진입 시점에 후보 publish (이후 edit/delete 가능)
                PublishDatumRoiCandidates(datum);
    ```

    2. **InvokeTryTeachDatum** — 성공 분기 말미 `halconViewer.SetDatumOverlay(_editingDatum, true);` 다음 줄에:
    ```csharp
                //260425 hbk Phase 13 D-01..D-04 — teach 완료 시점에 후보 갱신
                PublishDatumRoiCandidates(_editingDatum);
    ```

    3. (HandleDatumRoiMove 말미 — 위 2C 에 이미 포함됨)

    **2E. Plan 02 CustomMessageBox 인자 순서 fix** — `BtnTestFindDatum_Click` / `AskTestImageSource` 의 `CustomMessageBox.Show` 호출 2 곳 모두 (message, title) → (title, message) 로 swap:

    BEFORE:
    ```csharp
    CustomMessageBox.Show("Datum 티칭이 완료된 후 테스트 가능합니다.", "Datum Find 테스트");
    CustomMessageBox.Show("이미지 로드 실패: " + ex.Message, "Datum Find 테스트");
    ```

    AFTER:
    ```csharp
    CustomMessageBox.Show("Datum Find 테스트", "Datum 티칭이 완료된 후 테스트 가능합니다."); //260425 hbk Phase 13 cleanup — Plan 02 인자 순서 fix
    CustomMessageBox.Show("Datum Find 테스트", "이미지 로드 실패: " + ex.Message); //260425 hbk Phase 13 cleanup
    ```

    **2F. msbuild 합산 빌드 + 커밋:**

    ```bash
    msbuild WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal
    ```
    exit 0 + 신규 warning 0 확인.

    ```bash
    git add WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs WPF_Example/UI/ContentItem/MainView.xaml.cs
    git commit --no-verify -m "feat(phase-13-03): Datum ROI move/delete + auto re-teach + CustomMessageBox arg fix"
    ```
  </action>
  <verify>
    <automated>
      (1) grep -cE "private bool IsCurrentNodeDatum\(out DatumConfig datum\)" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (2) grep -cE "private void HandleDatumRoiMove\(" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (3) grep -cE "private void ApplyDatumRoiDelta\(" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (4) grep -cE "private void ClearDatumRoiFields\(" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (5) grep -cE "private void InvokeTryTeachDatumForEdit\(" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (6) grep -cE "private void PublishDatumRoiCandidates\(" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (7) grep -cE "BuildDatumRectCandidate\(" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 2 (def 1 + 호출 다수)
      (8) grep -cF "roiId.StartsWith(\"Datum.\")" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (9) grep -cF "e.RoiId.StartsWith(\"Datum.\")" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (10) grep -c "case \"Datum\\.Line1\":" WPF_Example/UI/ContentItem/MainView.xaml.cs → 2 (ApplyDelta 1 + ClearFields 1)
      (11) grep -cF "PublishDatumRoiCandidates(" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 4 (def 1 + 호출 3+)
      (12) grep -cF "CustomMessageBox.Show(\"Datum Find 테스트\"" WPF_Example/UI/ContentItem/MainView.xaml.cs → 2 (인자 순서 fix 확인)
      (13) FAI 경로 untouched: awk '/private void HalconViewer_RoiMoveCompleted/,/^        private/' WPF_Example/UI/ContentItem/MainView.xaml.cs | grep -c "FindFAIByName(e.RoiId)" → 1
      (14) FAI 경로 untouched: awk '/private void HalconViewer_RoiDeleteRequested/,/^        private/' WPF_Example/UI/ContentItem/MainView.xaml.cs | grep -c "FindFAIByName(roiId)" → 1
      (15) msbuild WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal → exit 0
    </automated>
  </verify>
  <done>
    - MainView 에 Datum 분기 (move + delete) + 6 신규 메서드 + 3 PublishDatumRoiCandidates 호출 지점 + Plan 02 CustomMessageBox 인자 순서 fix
    - 기존 FAI move/delete 로직 한 줄도 변경 없음
    - msbuild Debug/x64 exit 0, 신규 warning 0
    - Task 1+2 합산 1 commit 완료
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SIMUL_MODE UAT — 3 algorithm × 이동/삭제 + Plan 02 CustomMessageBox 라벨 확인</name>
  <what-built>
    - Datum ROI 드래그 이동 + 자동 재티칭 (Gap-1)
    - Datum ROI 우클릭 컨텍스트 메뉴 Delete (Gap-A) — 기존 인프라 재사용
    - Plan 02 CustomMessageBox 인자 순서 fix (제목/본문 정상)
  </what-built>
  <how-to-verify>
    **환경:** SIMUL_MODE Debug/x64 빌드.

    ### Gap-1 ROI 이동

    **테스트 1 (TwoLineIntersect Line1):** Datum 티칭 후 Line1 rect 내부 클릭 → 드래그 이동 → MouseUp.
    - 기대: label_drawHint "Datum ROI 이동 — 재티칭 OK" 녹색. 빨간 교점 십자가 새 위치로 이동. PropertyGrid Line1_Row/Col 갱신.

    **테스트 2 (TwoLineIntersect Line2):** Line2 동일 패턴.

    **테스트 3 (CircleTwoHorizontal):** Circle / HorizontalA / HorizontalB 각각 이동 → 재티칭 OK.

    **테스트 4 (VerticalTwoHorizontal):** Line1(수직) / HorizontalA / HorizontalB 각각 이동 → 재티칭 OK.

    **테스트 5 (방향 정합성 게이트 통합):** CircleTwoHorizontal 에서 HorizontalA 를 멀리 이동시켜 수평 결합선 기울기 ±15° 초과 시 → label_drawHint 빨강 + "Horizontal line orientation out of range" (Phase 13 Plan 01 게이트 자동 발동).

    ### Gap-A ROI 삭제

    **테스트 6 (Delete via 우클릭 메뉴):** Datum ROI 클릭 → 우클릭 → 컨텍스트 메뉴 "Delete" → 해당 ROI 가 사라짐. PropertyGrid 의 해당 필드 0 으로 reset.

    **테스트 7 (모든 ROI 삭제 후 Datum 무효화):** 한 ROI 라도 삭제하면 IsConfigured=false / LastTeachSucceeded=false → 검출 라인/교점 십자/검출 원 모두 사라짐.

    **테스트 8 (재티칭 가능):** 삭제 후 btn_teachDatum 으로 다시 티칭 가능.

    ### FAI 회귀 (CRITICAL)

    **테스트 9:** FAI ROI 이동 (Quick 260423-o53 경로) — Datum 분기 진입 안 하는 것 확인.
    **테스트 10:** FAI 우클릭 Delete — FindFAIByName 경로 그대로 동작.
    **테스트 11:** CircleDiameterMeasurement Circle ROI 이동 — circle.Circle_Row/Col 반영.

    ### Plan 02 CustomMessageBox 라벨 확인

    **테스트 12 (미티칭 가드 다이얼로그):** Datum 미티칭 상태에서 btn_testFindDatum 클릭 → 다이얼로그 **타이틀 = "Datum Find 테스트"**, **본문 = "Datum 티칭이 완료된 후 테스트 가능합니다."** (Plan 02 인자 swap 수정 확인).

    ### Plan 02 메인 success/failure path (이전 UAT 미완료분)

    **테스트 13:** Datum 티칭 완료 → btn_testFindDatum → [예] 현재 이미지 → 녹색 라벨 + 주황 3px 십자.
    **테스트 14:** btn_testFindDatum → [아니오] OpenFileDialog → 다른 이미지 → 주황 십자.
    **테스트 15:** btn_testFindDatum → [취소] → 조용히 닫힘.

    **회귀 확인 통합:**
    - Phase 12 Plan 03 teach 흐름 3 algorithm 회귀 없음
    - Phase 13 Plan 01 방향 정합성 게이트 정상 발동 (이동 후 재티칭 시)
    - Phase 13 Plan 02 btn_testFindDatum 정상 동작 (타이틀/본문 정상)
  </how-to-verify>
  <resume-signal>"approved" 또는 구체 이슈 (예: "Line2 이동 후 재티칭 실패", "Delete 가 컨텍스트 메뉴에 안 보임", "CustomMessageBox 타이틀이 여전히 잘림")</resume-signal>
</task>

</tasks>

<verification>
**파일 수준:**
- 수정 파일 2 개: `MainResultViewerControl.xaml.cs`, `MainView.xaml.cs`
- MainResultViewerControl: 신규 public 2 (SetDatumRoiCandidates, ClearDatumRoiCandidates), 신규 private 2 (_datumRoiCandidates 필드, HitTestOneRoi), 기존 HitTestSelectedRoi 수정, MouseDown 게이트 확장
- MainView: 신규 private 6 (IsCurrentNodeDatum, HandleDatumRoiMove, ApplyDatumRoiDelta, ClearDatumRoiFields, InvokeTryTeachDatumForEdit, PublishDatumRoiCandidates) + Build* 2 + 기존 RoiMoveCompleted/RoiDeleteRequested 에 Datum 분기 추가 (FAI 경로 한 줄도 변경 없음) + Plan 02 CustomMessageBox 인자 순서 fix 2 곳

**빌드:**
- msbuild Debug/x64 exit 0
- 2 개 수정 파일 대상 신규 warning 0

**SIMUL_MODE UAT (Task 3):** 15 테스트 시나리오 통과 → "approved"

**CONTEXT D-13 회귀 확인:**
- DatumConfig public 시그니처 변경 0 (read/write 만)
- DatumFindingService.TryTeachDatum / TryFindDatum 시그니처 변경 0 (호출만)
- 기존 SetDatumOverlay / SetDatumFindResultOverlay 시그니처 변경 0
- 기존 FAI RoiMoveCompleted/RoiDeleteRequested 경로 한 줄도 변경 없음

**Phase 13 Plan 01 + Plan 02 통합:**
- Plan 01 ValidateHorizontalVerticalAngles: InvokeTryTeachDatumForEdit 의 TryTeachDatum 호출에서 자동 발동 (별도 와이어링 불필요)
- Plan 02 btn_testFindDatum + CustomMessageBox 인자 순서 fix 가 같은 commit 에 포함

**13-04 / 13-05 로 이월 (이번 plan 범위 밖):**
- Per-ROI 에지 파라미터 (EdgeDirection / EdgeThreshold / EdgeSampleCount / EdgeTrimCount / EdgePolarity per ROI) → 13-04
- DatumConfig 스키마 확장 + INI 하위호환 마이그레이션 → 13-04
- DatumFindingService 의 5 개 ROI 별 에지 측정 호출부 와이어링 → 13-04
- PropertyGrid 노출 → 13-04
- Detected line 외삽 (EXTEND_PX) → 13-05
- Raw 검출 에지점 시각화 → 13-05
- 최종 reference 좌표 텍스트 표시 → 13-05
</verification>

<success_criteria>
- [ ] MainResultViewerControl 에 _datumRoiCandidates / SetDatumRoiCandidates / ClearDatumRoiCandidates / HitTestOneRoi / HitTestSelectedRoi fallback / MouseDown Datum 통과 / Datum hit 시 _selectedRoiId set
- [ ] MainView 에 IsCurrentNodeDatum / HandleDatumRoiMove / ApplyDatumRoiDelta / ClearDatumRoiFields / InvokeTryTeachDatumForEdit / PublishDatumRoiCandidates / BuildDatumRectCandidate / BuildDatumCircleCandidate
- [ ] HalconViewer_RoiMoveCompleted 최상단 Datum 분기 + 기존 FAI 로직 한 줄도 변경 없음
- [ ] HalconViewer_RoiDeleteRequested 최상단 Datum 분기 + 기존 FAI 로직 한 줄도 변경 없음
- [ ] PublishDatumRoiCandidates 호출 3 지점 (TeachDatumButton_Click 진입 + InvokeTryTeachDatum 성공 + HandleDatumRoiMove 말미)
- [ ] ClearDatumRoiFields 가 IsConfigured/LastTeachSucceeded false 도 set
- [ ] Plan 02 CustomMessageBox.Show 인자 순서 fix 2 곳 (BtnTestFindDatum_Click + AskTestImageSource)
- [ ] msbuild Debug/x64 exit 0 + 신규 warning 0
- [ ] 1 commit (Task 1+2 합산)
- [ ] Task 3 SIMUL_MODE UAT 15 시나리오 통과 → "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/13-datum-algorithm-extensibility/13-03-SUMMARY.md`:
- Files Changed 테이블 (2 파일)
- Gap-1 / Gap-A coverage 매핑
- Plan 02 CustomMessageBox cleanup 명시
- New API 카탈로그
- RoiId prefix 규약 ("Datum.Line1" / "Datum.Line2" / "Datum.Circle" / "Datum.HorizontalA" / "Datum.HorizontalB")
- Build Result 블록
- UAT Outcome (Task 3 15 시나리오 결과)
- 13-04 / 13-05 이월 항목 명시 (per-ROI 에지 파라미터 + 시각화)
- Self-Check 체크리스트
</output>
</content>
</invoke>