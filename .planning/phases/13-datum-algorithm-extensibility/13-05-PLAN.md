---
phase: 13-datum-algorithm-extensibility
plan: "05"
type: execute
wave: 5
depends_on: [13-04]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
  - WPF_Example/UI/ContentItem/MainView.xaml
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: false
requirements:
  - Gap-C-DatumDetectedLineExtension
  - Gap-RawEdgePointDisplay
  - Gap-RefCoordsTextDisplay
tags: [datum, visualization, edge-points, line-extension, ref-coords, phase-13]
must_haves:
  truths:
    - "RenderDatumOverlay 의 LastTeachSucceeded 분기 detected line(Line1Detected_*, Line2Detected_*) 이 unit-vector × EXTEND_PX(=10000.0) 외삽으로 이미지 가장자리까지 확장 표시되고, HALCON DispLine 의 자동 클리핑으로 화면 밖은 잘림 (Gap-C)"
    - "DatumFindingService 가 라인/원 검출 후 raw 에지점 (Halcon HTuple row/col 배열) 을 DatumConfig 의 volatile 필드 (Line1_DetectedEdgeRows/Cols, Line2_*, Circle_*, Horizontal_A_*, Horizontal_B_*) 에 기록한다 — HTuple 은 [Browsable(false)] runtime-only (ParamBase 직렬화 무시) (Gap-RawEdgePointDisplay)"
    - "HalconDisplayService 가 LastTeachSucceeded 분기에서 각 ROI 의 raw 에지점을 작은 점 마커 (DispCross 또는 DispRegion) 로 렌더 — ROI 별 색상 구분 (Line1=cyan, Line2=magenta, Circle=yellow, HorizA=green, HorizB=lime) (Gap-RawEdgePointDisplay)"
    - "MainView 에 신규 WPF Label `label_datumRefCoords` 추가 — Datum 노드 선택 또는 티칭/이동 직후 'RefOrigin=(R, C), Angle=X°, CircleCenter=(R, C), Radius=X' 형식 텍스트 표시 (Gap-RefCoordsTextDisplay)"
    - "기존 RefOrigin 빨간 십자 / 노란 검출 원 / magenta Datum Origin 라벨 / yellow ROI 라벨 모두 시그니처 untouched (additive 만)"
    - "Phase 13 누적 통합: Plan 03 ROI 이동/삭제 / Plan 04 per-ROI 파라미터 회귀 0"
    - "msbuild Debug/x64 가 exit 0 로 성공하고 5 개 수정 파일을 참조하는 신규 warning 이 0 개이다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "5 ROI × 2 volatile HTuple 필드 (DetectedEdgeRows/Cols) — runtime 전용 raw 에지점 저장"
      contains: "Line1_DetectedEdgeRows"
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "TryFindLine / TryExtractEdgePoints 후 raw row/col HTuple 을 DatumConfig 해당 ROI 필드에 write-back"
      contains: "Line1_DetectedEdgeRows ="
    - path: "WPF_Example/Halcon/Display/HalconDisplayService.cs"
      provides: "EXTEND_PX 상수 + DrawExtendedLine helper + RenderRawEdgePoints (5 ROI × 색상) + RenderDatumOverlay 의 line 외삽 교체"
      contains: "EXTEND_PX"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml"
      provides: "label_datumRefCoords (canvas 툴바 또는 상태바)"
      contains: "label_datumRefCoords"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "UpdateDatumRefCoordsLabel(DatumConfig) — InspectionListView 선택 변경 / TeachDatum 성공 / RoiMove 성공 3 지점 호출"
      contains: "UpdateDatumRefCoordsLabel"
  key_links:
    - from: "HalconDisplayService.RenderDatumOverlay (LastTeachSucceeded branch)"
      to: "extended detected lines"
      via: "unit-vector × EXTEND_PX 외삽 후 DispLine"
      pattern: "EXTEND_PX"
    - from: "DatumFindingService.TryFindLine"
      to: "DatumConfig.Line1_DetectedEdgeRows / Line1_DetectedEdgeCols"
      via: "검출 후 row/col HTuple 직접 대입 (Browsable(false) runtime 전용)"
      pattern: "config\\.Line1_DetectedEdgeRows ="
    - from: "HalconDisplayService.RenderDatumOverlay"
      to: "HalconDisplayService.RenderRawEdgePoints"
      via: "LastTeachSucceeded 분기 내부 5 ROI loop"
      pattern: "RenderRawEdgePoints"
    - from: "MainView.HalconViewer_RoiMoveCompleted (Datum 분기)"
      to: "MainView.UpdateDatumRefCoordsLabel"
      via: "HandleDatumRoiMove 말미 호출"
      pattern: "UpdateDatumRefCoordsLabel"
---

<objective>
**Phase 13 scope 재분할 (2026-04-25 split):** 13-03 / 13-04 에 이어 **시각화 묶음** — 사용자가 검출 결과를 한눈에 확인하고 reference 좌표를 텍스트로 읽을 수 있게 한다.

**현재 상태 (13-04 까지):**
- Datum 티칭 성공 시 검출 라인 2개 (yellow + cyan) + 빨간 교점 십자 + 노란 검출 원 표시 — **그러나:**
  - 검출 라인이 ROI 안쪽 짧은 segment 만 그려짐 → 라인 방향 / 위치 가시성 떨어짐
  - 라인/원 피팅에 사용된 **raw 에지점**들은 시각화 안 됨 → 검출 신뢰도 / outlier 진단 불가능
  - reference 좌표 (`RefOrigin`, `RefAngleRad`, `CircleCenter`, `CircleDetected_Radius`) 가 PropertyGrid 에는 있으나 캔버스 옆 한눈에 보이는 텍스트는 없음

**이번 13-05 가 추가하는 것:**

1. **Detected line 외삽 표시 (Gap-C — 13-03 에서 분리)**:
   - `RenderDatumOverlay` 의 `Line1Detected_*` / `Line2Detected_*` DispLine 호출을 unit-vector × `EXTEND_PX = 10000.0` 외삽 후 DispLine 으로 교체
   - HALCON DispLine 자동 클리핑으로 30K~50K 이미지에서도 안전
   - private static helper `DrawExtendedLine(HWindow, r1, c1, r2, c2)` 신설

2. **Raw 에지점 시각화 (Gap-RawEdgePointDisplay)**:
   - DatumFindingService 가 라인/원 피팅 직전 검출된 raw row/col HTuple 을 DatumConfig 의 volatile 필드에 write-back
   - DatumConfig 에 5 ROI × 2 = 10 신규 HTuple 필드 (`<ROI>_DetectedEdgeRows` / `<ROI>_DetectedEdgeCols`) — `[Browsable(false)]` runtime 전용 (ParamBase reflection 이 HTuple 무시 — Phase 4 D-11 패턴)
   - HalconDisplayService 가 LastTeachSucceeded 분기에서 각 ROI 의 raw 점을 작은 cross 마커 (DispCross 또는 DispRegion gen_cross_contour_xld) 로 렌더
   - **ROI 별 색상 구분** (사용자가 어떤 ROI 의 점인지 즉시 인식):
     - Line1: `cyan`
     - Line2: `magenta`
     - Circle: `yellow`
     - Horizontal_A: `green`
     - Horizontal_B: `lime`
   - 점 크기 6 px (cross half), 굵기 1 px

3. **Reference 좌표 텍스트 표시 (Gap-RefCoordsTextDisplay)**:
   - MainView XAML 에 신규 WPF `Label x:Name="label_datumRefCoords"` 추가 — canvas 툴바 (label_drawHint 와 같은 영역) 또는 상태바
   - MainView code-behind `UpdateDatumRefCoordsLabel(DatumConfig)` 메서드:
     - `IsConfigured && LastTeachSucceeded` 일 때:
       - `"RefOrigin = (R: 1234.5, C: 6789.0), Angle = 12.34°"`
       - CircleTwoHorizontal 알고리즘이면 추가: `", CircleCenter = (R: ..., C: ...), Radius = X.XX"`
     - 아니면: `"Datum 미설정"` 회색
   - 호출 3 지점:
     - InspectionListView SelectedParam 변경 (Datum 노드 선택) → label 갱신
     - InvokeTryTeachDatum 성공 직후 (Phase 12 패턴)
     - HandleDatumRoiMove 말미 (13-03 패턴) — 이동 후 재티칭 결과 반영

**Out of scope:**
- **Per-ROI 색상 PropertyGrid 노출** (사용자 색상 변경) — 13-05 는 hardcoded 5 색상으로 시작
- **Raw edge point CSV export** (디버깅용 텍스트 덤프) — 별도 phase
- **3D / overlay z-ordering 제어** — HALCON 자동 순서

**Output:**

- `WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs`:
  - 10 신규 volatile HTuple 필드 (5 ROI × 2): `<ROI>_DetectedEdgeRows` / `<ROI>_DetectedEdgeCols`
  - 모두 `[Browsable(false)]` (PropertyGrid 미노출)
  - HTuple 이라 ParamBase reflection 자동 무시 → INI 직렬화 영향 0

- `WPF_Example/Halcon/Algorithms/DatumFindingService.cs`:
  - `TryFindLine` 의 검출 분기에서 raw row/col 을 caller 가 받을 수 있도록 시그니처 확장 (`out HTuple edgeRowsOut, out HTuple edgeColsOut`)
  - `TryExtractEdgePoints` 도 동일 (이미 row/col 을 반환하면 caller 에서 DatumConfig 에 대입)
  - 5 ROI 호출부 모두 검출 결과를 DatumConfig 해당 필드에 write-back

- `WPF_Example/Halcon/Display/HalconDisplayService.cs`:
  - `private const double EXTEND_PX = 10000.0;`
  - `private static void DrawExtendedLine(HWindow, r1, c1, r2, c2)`
  - `private static void RenderRawEdgePoints(HWindow, HTuple rows, HTuple cols, string color)`
  - `RenderDatumOverlay` 의 LastTeachSucceeded 분기:
    - DispLine → DrawExtendedLine 교체 (Line1, Line2)
    - 각 ROI 별 RenderRawEdgePoints 호출 (5 ROI)

- `WPF_Example/UI/ContentItem/MainView.xaml`:
  - `<Label x:Name="label_datumRefCoords" .../>` — label_drawHint 와 같은 그리드 영역, Visibility="Collapsed" 기본

- `WPF_Example/UI/ContentItem/MainView.xaml.cs`:
  - `private void UpdateDatumRefCoordsLabel(DatumConfig datum)` — 텍스트 포맷 + Visibility 토글
  - 호출 3 지점:
    - `InspectionListView_SelectedParamChanged` (또는 동등 이벤트) — Datum 노드 선택 시
    - `InvokeTryTeachDatum` 성공 분기 말미
    - `HandleDatumRoiMove` 말미 (13-03 추가)

- msbuild Debug/x64 exit 0 + 신규 warning 0
- SIMUL_MODE UAT checkpoint

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-datum-algorithm-extensibility/13-CONTEXT.md
@.planning/phases/13-datum-algorithm-extensibility/13-PATTERNS.md
@.planning/phases/13-datum-algorithm-extensibility/13-04-SUMMARY.md
@WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
@WPF_Example/Halcon/Algorithms/DatumFindingService.cs
@WPF_Example/Halcon/Display/HalconDisplayService.cs
@WPF_Example/UI/ContentItem/MainView.xaml
@WPF_Example/UI/ContentItem/MainView.xaml.cs
@WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
@CLAUDE.md

<existing_infra_reuse>
**Critical — 기존 인프라 재사용:**

DatumConfig 의 HTuple runtime-only 필드 패턴 (Phase 4 D-11):
```csharp
[PropertyTools.DataAnnotations.Browsable(false)]
public HTuple CurrentTransform { get; set; }   // ← ParamBase 직렬화 무시 (HTuple 미지원)
```
→ 이 패턴을 10 신규 필드에 그대로 적용. INI/PropertyGrid 영향 0.

HalconDisplayService 의 점 마커 패턴 (HOperatorSet.DispCross 또는 그래픽 라인 2개):
```csharp
// 옵션 A: DispCross (HALCON built-in)
HOperatorSet.DispCross(window, row, col, sizeFloat, angleRad);

// 옵션 B: DispLine 2개 (수동 십자) — RefOrigin 빨간 십자가 사용 중인 패턴
HOperatorSet.DispLine(window, row - half, col, row + half, col);
HOperatorSet.DispLine(window, row, col - half, row, col + half);
```
→ 옵션 A (DispCross) 가 batch HTuple 일괄 처리 가능 → raw 점 다수 (수십~수백 개) 효율적. **권장 = 옵션 A.**

MainView 의 label_drawHint 패턴 (Phase 12 D-04):
```xml
<Label x:Name="label_drawHint" Grid.Column="1" Visibility="Collapsed"
       HorizontalAlignment="Center" VerticalContentAlignment="Center"
       FontSize="13" Foreground="#FFAAAAAA"/>
```
→ label_datumRefCoords 동일 패턴, Grid.Column 만 분리.

InspectionListView SelectedParam 변경 이벤트 (Phase 12 D-13):
- `inspectionList.RefreshParamEditor()` / `SelectedParam` 변경 시 MainView 가 어떻게 알지 — 기존 패턴 검토 필요.
- 가장 안전: MainView 에 `inspectionList.SelectionChanged += ...` 또는 PropertyChanged 이벤트 구독; 또는 InspectionListView 가 호출하는 콜백.
- 실제 구현은 execute 시점에 InspectionListView code 확인 후 결정.
</existing_infra_reuse>

<interfaces>
From DatumConfig.cs (13-04 까지 확정):
```csharp
public HTuple CurrentTransform { get; set; }   // 기존 runtime-only 패턴
public bool LastTeachSucceeded { get; set; }
public double Line1Detected_RBegin, ..., Line2Detected_CEnd;  // line endpoints
public double CircleCenter_Row, CircleCenter_Col, CircleDetected_Radius;

// 13-05 신규 (5 ROI × 2 = 10):
//   Line1_DetectedEdgeRows / Line1_DetectedEdgeCols  (HTuple)
//   Line2_DetectedEdgeRows / Line2_DetectedEdgeCols
//   Circle_DetectedEdgeRows / Circle_DetectedEdgeCols
//   Horizontal_A_DetectedEdgeRows / Horizontal_A_DetectedEdgeCols
//   Horizontal_B_DetectedEdgeRows / Horizontal_B_DetectedEdgeCols
```

From DatumFindingService.cs (13-04 까지 확정):
```csharp
private static bool TryFindLine(
    HImage image, ..., string direction, int sampleCount, int trimCount,
    out double rowBegin, out double colBegin, out double rowEnd, out double colEnd,
    out string error);

// 13-05 시그니처 확장 (+2 out 파라미터):
private static bool TryFindLine(
    HImage image, ..., string direction, int sampleCount, int trimCount,
    out double rowBegin, out double colBegin, out double rowEnd, out double colEnd,
    out HTuple edgeRowsOut, out HTuple edgeColsOut,    // ← 추가
    out string error);

// TryExtractEdgePoints 는 이미 row/col HTuple 반환 — caller 가 DatumConfig 에 대입
```

From HalconDisplayService.cs:
```csharp
// 13-05 신규:
private const double EXTEND_PX = 10000.0;
private static void DrawExtendedLine(HWindow window, double r1, double c1, double r2, double c2);
private static void RenderRawEdgePoints(HWindow window, HTuple rows, HTuple cols, string color);

// RenderDatumOverlay 의 LastTeachSucceeded 분기 내부에서 호출:
//   DispLine(Line1Detected_*) → DrawExtendedLine
//   RenderRawEdgePoints(Line1_DetectedEdgeRows, Cols, "cyan")
//   RenderRawEdgePoints(Line2_DetectedEdgeRows, Cols, "magenta")
//   ... 5 ROI 반복 (있을 때만)
```

From MainView.xaml.cs:
```csharp
private void UpdateDatumRefCoordsLabel(DatumConfig datum);  // 신규
// 호출 3 지점:
//   - InspectionListView SelectedParam 변경 시 (Datum 노드)
//   - InvokeTryTeachDatum 성공 분기 말미
//   - HandleDatumRoiMove 말미 (13-03)
```

</interfaces>

**Brace style:**
- `DatumConfig.cs` = K&R
- `DatumFindingService.cs` = Allman
- `HalconDisplayService.cs` = Allman
- `MainView.xaml.cs` = K&R

**C# 7.2:** out HTuple 가능. switch expression 금지.

**주석 convention:** `//260425 hbk Phase 13 D-VIZ-XX — <intent>` (VIZ = visualization)

**색상 팔레트 (raw edge points):**
- Line1: `"cyan"` — TwoLineIntersect/VerticalTwoHorizontal
- Line2: `"magenta"` — TwoLineIntersect 만
- Circle: `"yellow"` — CircleTwoHorizontal 만
- Horizontal_A: `"green"` — CircleTwoHorizontal/VerticalTwoHorizontal
- Horizontal_B: `"lime"` — CircleTwoHorizontal/VerticalTwoHorizontal

(detected line color (`"yellow"` Line1, `"cyan"` Line2) 와 점 색상이 일부 겹치지만 점/선 형태가 달라 시각적으로 구분 가능 — 향후 충돌 발생 시 점 색상만 시프트.)

**EXTEND_PX rationale (Gap-C):** HALCON `DispLine` 은 화면 밖 좌표를 자동으로 클리핑한다. 따라서 정확한 이미지 경계 계산 없이 unit-vector × 10000.0 px 외삽하면 어떤 이미지 크기에도 안전하게 가장자리까지 확장된 시각 효과를 얻는다. 30K~50K 이미지에서도 충분한 마진.

</context>

<tasks>

<task type="auto">
  <name>Task 1: DatumConfig — 10 volatile HTuple 필드 추가 (raw edge point 저장)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs L139-173 (Phase 4 D-11 / Phase 11 D-11 / Phase 12 D-10 휘발성 필드 패턴)
  </read_first>
  <action>
    **1A. Line1Detected_* 필드 그룹 직후 (현재 L162 LastTeachSucceeded 직전 또는 직후) 5 ROI × 2 HTuple 필드 추가:**

    ```csharp
            //260425 hbk Phase 13 D-VIZ-01 — raw 검출 에지점 (TryFindLine / TryExtractEdgePoints / TryFindCircle 직후 write-back)
            //  HTuple 은 ParamBase 직렬화 미지원 (Phase 4 D-11 패턴 동일) → INI 영향 0, runtime 전용.
            //  RenderDatumOverlay 가 LastTeachSucceeded 분기에서 ROI 별 색상으로 점 마커 렌더.
            [PropertyTools.DataAnnotations.Browsable(false)]
            public HTuple Line1_DetectedEdgeRows { get; set; }
            [PropertyTools.DataAnnotations.Browsable(false)]
            public HTuple Line1_DetectedEdgeCols { get; set; }

            [PropertyTools.DataAnnotations.Browsable(false)]
            public HTuple Line2_DetectedEdgeRows { get; set; }
            [PropertyTools.DataAnnotations.Browsable(false)]
            public HTuple Line2_DetectedEdgeCols { get; set; }

            [PropertyTools.DataAnnotations.Browsable(false)]
            public HTuple Circle_DetectedEdgeRows { get; set; }
            [PropertyTools.DataAnnotations.Browsable(false)]
            public HTuple Circle_DetectedEdgeCols { get; set; }

            [PropertyTools.DataAnnotations.Browsable(false)]
            public HTuple Horizontal_A_DetectedEdgeRows { get; set; }
            [PropertyTools.DataAnnotations.Browsable(false)]
            public HTuple Horizontal_A_DetectedEdgeCols { get; set; }

            [PropertyTools.DataAnnotations.Browsable(false)]
            public HTuple Horizontal_B_DetectedEdgeRows { get; set; }
            [PropertyTools.DataAnnotations.Browsable(false)]
            public HTuple Horizontal_B_DetectedEdgeCols { get; set; }
    ```

    **1B. msbuild 단독 빌드:**

    ```bash
    msbuild WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal
    ```
    exit 0 (필드만 추가, 사용자 없음 → 미사용 warning 가능, Task 2 후 해소).

    **1C. git add 만, commit 은 모든 task 완료 후 합산.**
  </action>
  <verify>
    <automated>
      (1) grep -c "Line1_DetectedEdgeRows" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → ≥ 1
      (2) grep -c "Horizontal_B_DetectedEdgeCols" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → ≥ 1
      (3) grep -cE "public HTuple [A-Za-z_]+_DetectedEdge(Rows|Cols)" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → 10
      (4) grep -B1 "public HTuple .*_DetectedEdge" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs | grep -c "Browsable(false)" → ≥ 10
      (5) msbuild Debug/x64 → exit 0
    </automated>
  </verify>
  <done>
    - 10 신규 volatile HTuple 필드 + Browsable(false) 적용
    - INI / PropertyGrid 영향 0 (HTuple ParamBase 직렬화 무시 + Browsable false)
    - 단독 빌드 exit 0
  </done>
</task>

<task type="auto">
  <name>Task 2: DatumFindingService — TryFindLine 시그니처 +2 out (HTuple) + 5 ROI write-back</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs 전체
    - 특히 TryFindLine 본체 (MeasurePos 호출 직후 row/col HTuple 가용 지점)
    - TryExtractEdgePoints 본체 (이미 row/col HTuple 반환 — caller 에서 사용)
    - TryFindCircle 호출 (VisionAlgorithmService — out 파라미터 확인)
  </read_first>
  <action>
    **2A. TryFindLine 시그니처 확장** (13-04 까지의 6 파라미터 + 추가 out 2개):

    ```csharp
    //260425 hbk Phase 13 D-VIZ-02 — raw edge points 외부 노출 (out HTuple edgeRowsOut/edgeColsOut)
    //  caller 가 DatumConfig 의 ROI 별 DetectedEdgeRows/Cols 에 대입 → RenderDatumOverlay 가 점 마커 렌더.
    //  edge 가 0개 검출되면 빈 HTuple 반환 (length 0 → RenderRawEdgePoints 에서 no-op).
    private static bool TryFindLine(
        HImage image, HTuple imageWidth, HTuple imageHeight,
        double row, double col, double phi, double length1, double length2,
        double sigma, int threshold, string polarity,
        string direction, int sampleCount, int trimCount,
        out double rowBegin, out double colBegin, out double rowEnd, out double colEnd,
        out HTuple edgeRowsOut, out HTuple edgeColsOut,
        out string error)
    ```

    **본체 변경:** MeasurePos 후 row/col HTuple 을 edgeRowsOut/edgeColsOut 에 대입. 실패 분기 (Halcon 예외) 에서도 빈 HTuple 초기화 → null 방지.

    **2B. TryTeachTwoLineIntersect — Line1/Line2 호출부 write-back:**

    ```csharp
    //260425 hbk Phase 13 D-VIZ-03 — Line1 raw 점 DatumConfig 기록
    HTuple line1Rows, line1Cols;
    if (!TryFindLine(image, imageWidth, imageHeight,
        config.Line1_Row, ..., config.Line1_EdgeTrimCount,
        out r1b, out c1b, out r1e, out c1e,
        out line1Rows, out line1Cols,
        out err)) { ... return false; }
    config.Line1_DetectedEdgeRows = line1Rows;
    config.Line1_DetectedEdgeCols = line1Cols;
    ```

    Line2 동일 패턴 (`Line2_*` 사용).

    **2C. TryTeachCircleTwoHorizontal — Circle / HorizontalA / HorizontalB write-back:**

    - VisionAlgorithmService.TryFindCircle 가 raw 점을 반환하면 `Circle_DetectedEdgeRows/Cols` 에 대입. 반환 안 하면 빈 HTuple 두고 향후 phase 로 이월 (Out of scope cleanup).
    - TryExtractEdgePoints 두 호출의 row/col 결과를 각각 `Horizontal_A_DetectedEdgeRows/Cols` / `Horizontal_B_DetectedEdgeRows/Cols` 에 대입.

    **2D. TryTeachVerticalTwoHorizontal — Line1(수직) + HorizontalA + HorizontalB write-back:**

    - 수직 Line1: `Line1_DetectedEdgeRows/Cols` 에 대입.
    - HorizontalA/B: 위와 동일.

    **2E. TryFindDatum (런타임) — 동일 매핑:**

    런타임에서도 raw 점이 의미 있음 (검출 신뢰 진단). 동일 write-back 패턴.

    **2F. msbuild 단독 빌드 + git add (commit Task 4 후):**
  </action>
  <verify>
    <automated>
      (1) grep -cE "out HTuple edgeRowsOut, out HTuple edgeColsOut" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → ≥ 1 (TryFindLine def)
      (2) grep -c "config.Line1_DetectedEdgeRows = " WPF_Example/Halcon/Algorithms/DatumFindingService.cs → ≥ 2 (TryTeach + TryFind)
      (3) grep -c "config.Line2_DetectedEdgeRows = " WPF_Example/Halcon/Algorithms/DatumFindingService.cs → ≥ 1
      (4) grep -c "config.Horizontal_A_DetectedEdgeRows = " WPF_Example/Halcon/Algorithms/DatumFindingService.cs → ≥ 1
      (5) grep -c "config.Horizontal_B_DetectedEdgeRows = " WPF_Example/Halcon/Algorithms/DatumFindingService.cs → ≥ 1
      (6) msbuild Debug/x64 → exit 0
    </automated>
  </verify>
  <done>
    - TryFindLine 시그니처 +2 out (HTuple)
    - 5 ROI 호출부 모두 write-back
    - TryTeach + TryFindDatum 양 경로 동일
  </done>
</task>

<task type="auto">
  <name>Task 3: HalconDisplayService — EXTEND_PX + DrawExtendedLine + RenderRawEdgePoints + RenderDatumOverlay 통합</name>
  <files>WPF_Example/Halcon/Display/HalconDisplayService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Display/HalconDisplayService.cs L380-545 (RenderDatumOverlay 전체)
    - 특히 L491-505 (Line1Detected/Line2Detected DispLine — 외삽 교체 대상)
    - L195-210 (RenderCircleDraft — 독립 무상태 메서드 시그니처 패턴)
  </read_first>
  <action>
    **3A. private const + helpers 추가** (RenderDatumOverlay 메서드 직전):

    ```csharp
            //260425 hbk Phase 13 D-VIZ-04 — 검출 라인 외삽 거리 (HALCON DispLine 자동 클리핑 활용; 30K~50K 이미지에서도 충분)
            private const double EXTEND_PX = 10000.0;

            //260425 hbk Phase 13 D-VIZ-04 — 두 점 (r1,c1)-(r2,c2) 를 unit-vector × EXTEND_PX 로 양쪽 외삽 후 DispLine
            //  HALCON 은 화면 밖 좌표를 자동 클리핑하므로 이미지 width/height 조회 불필요.
            //  두 점이 동일하면 (lenSq=0) DispLine 호출 자체를 skip — divide by zero 방지.
            private static void DrawExtendedLine(HWindow window, double r1, double c1, double r2, double c2)
            {
                double dr = r2 - r1;
                double dc = c2 - c1;
                double lenSq = dr * dr + dc * dc;
                if (lenSq < 1e-9) return; // degenerate
                double len = Math.Sqrt(lenSq);
                double ur = dr / len;
                double uc = dc / len;
                double exR1 = r1 - ur * EXTEND_PX;
                double exC1 = c1 - uc * EXTEND_PX;
                double exR2 = r2 + ur * EXTEND_PX;
                double exC2 = c2 + uc * EXTEND_PX;
                HOperatorSet.DispLine(window, exR1, exC1, exR2, exC2);
            }

            //260425 hbk Phase 13 D-VIZ-05 — raw 검출 에지점들을 작은 cross 마커로 일괄 렌더
            //  rows/cols 가 null 이거나 length 0 이면 no-op (안전).
            //  size 6 px, line width 1 (기존 RenderCircleDraft 빨간 십자 6 px 패턴과 일치).
            private static void RenderRawEdgePoints(HWindow window, HTuple rows, HTuple cols, string color)
            {
                if (rows == null || cols == null) return;
                int n = rows.TupleLength();
                if (n == 0 || cols.TupleLength() != n) return;
                try
                {
                    HOperatorSet.SetColor(window, color);
                    HOperatorSet.SetLineWidth(window, 1);
                    // HALCON DispCross batch: rows/cols HTuple 일괄 처리, size 6, angle 0
                    HOperatorSet.DispCross(window, rows, cols, 6.0, 0.0);
                }
                catch
                {
                    // Suppress display errors (RenderDatumOverlay catch 관습)
                }
            }
    ```

    **3B. RenderDatumOverlay LastTeachSucceeded 분기 — Line1/Line2 DispLine → DrawExtendedLine 교체:**

    BEFORE (L497-505):
    ```csharp
                    HOperatorSet.SetColor(window, "yellow");
                    HOperatorSet.SetLineWidth(window, 2);
                    HOperatorSet.DispLine(window,
                        datum.Line1Detected_RBegin, datum.Line1Detected_CBegin,
                        datum.Line1Detected_REnd,   datum.Line1Detected_CEnd);

                    HOperatorSet.SetColor(window, "cyan");
                    HOperatorSet.DispLine(window,
                        datum.Line2Detected_RBegin, datum.Line2Detected_CBegin,
                        datum.Line2Detected_REnd,   datum.Line2Detected_CEnd);
    ```

    AFTER:
    ```csharp
                    //260425 hbk Phase 13 D-VIZ-04 — Line1 detected 외삽
                    HOperatorSet.SetColor(window, "yellow");
                    HOperatorSet.SetLineWidth(window, 2);
                    DrawExtendedLine(window,
                        datum.Line1Detected_RBegin, datum.Line1Detected_CBegin,
                        datum.Line1Detected_REnd,   datum.Line1Detected_CEnd);

                    //260425 hbk Phase 13 D-VIZ-04 — Line2 detected 외삽
                    HOperatorSet.SetColor(window, "cyan");
                    DrawExtendedLine(window,
                        datum.Line2Detected_RBegin, datum.Line2Detected_CBegin,
                        datum.Line2Detected_REnd,   datum.Line2Detected_CEnd);
    ```

    **3C. RenderDatumOverlay LastTeachSucceeded 분기 말미 — 5 ROI raw 점 렌더 추가:**

    Circle 검출 원 분기 (L516-534) 직후:
    ```csharp
                    //260425 hbk Phase 13 D-VIZ-05 — 5 ROI raw 검출 에지점 (있을 때만)
                    RenderRawEdgePoints(window, datum.Line1_DetectedEdgeRows, datum.Line1_DetectedEdgeCols, "cyan");
                    RenderRawEdgePoints(window, datum.Line2_DetectedEdgeRows, datum.Line2_DetectedEdgeCols, "magenta");
                    RenderRawEdgePoints(window, datum.Circle_DetectedEdgeRows, datum.Circle_DetectedEdgeCols, "yellow");
                    RenderRawEdgePoints(window, datum.Horizontal_A_DetectedEdgeRows, datum.Horizontal_A_DetectedEdgeCols, "green");
                    RenderRawEdgePoints(window, datum.Horizontal_B_DetectedEdgeRows, datum.Horizontal_B_DetectedEdgeCols, "lime");
    ```

    **3D. msbuild + git add:**
  </action>
  <verify>
    <automated>
      (1) grep -cE "private const double EXTEND_PX = 10000\.0" WPF_Example/Halcon/Display/HalconDisplayService.cs → 1
      (2) grep -cE "private static void DrawExtendedLine\(" WPF_Example/Halcon/Display/HalconDisplayService.cs → 1
      (3) grep -cE "private static void RenderRawEdgePoints\(" WPF_Example/Halcon/Display/HalconDisplayService.cs → 1
      (4) grep -cF "DrawExtendedLine(window," WPF_Example/Halcon/Display/HalconDisplayService.cs → ≥ 2
      (5) grep -cF "RenderRawEdgePoints(window," WPF_Example/Halcon/Display/HalconDisplayService.cs → ≥ 5 (5 ROI)
      (6) grep -c "Line1Detected_RBegin" WPF_Example/Halcon/Display/HalconDisplayService.cs | head -1 → 외삽 helper 인자로만 사용 (DispLine 직접 호출 없음): grep -cE "DispLine\(window,\s*datum\.Line1Detected" → 0
      (7) RefOrigin 빨간 십자 / 검출 원 / magenta 십자 시그니처 미변경: grep -cF "datum.RefOriginRow - crossHalf, datum.RefOriginCol" → ≥ 1
      (8) msbuild Debug/x64 → exit 0
    </automated>
  </verify>
  <done>
    - EXTEND_PX 상수 + DrawExtendedLine + RenderRawEdgePoints helper 추가
    - Line1/Line2 detected 외삽 교체
    - 5 ROI raw 점 렌더 추가
    - 기존 RefOrigin / CircleDetected / magenta 십자 / yellow ROI 라벨 시그니처 untouched
  </done>
</task>

<task type="auto">
  <name>Task 4: MainView XAML + code-behind — label_datumRefCoords + UpdateDatumRefCoordsLabel + 3 호출 지점</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml L83-130 (캔버스 툴바 + label_drawHint / label_pointCount 패턴)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L987-1010 (TeachDatumButton_Click — _editingDatum)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L1174-1208 (InvokeTryTeachDatum 성공 분기)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs HandleDatumRoiMove (13-03 추가, 말미)
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs SelectedParam / SelectionChanged 이벤트 패턴
  </read_first>
  <action>
    **4A. MainView.xaml — label_datumRefCoords 추가:**

    label_drawHint 옆 또는 별도 컬럼:
    ```xml
            <!--260425 hbk Phase 13 D-VIZ-06 — Datum reference 좌표 텍스트 라벨 (캔버스 옆 한눈에 보이도록)-->
            <Label x:Name="label_datumRefCoords" Grid.Column="2" Visibility="Collapsed"
                   HorizontalAlignment="Right" VerticalContentAlignment="Center"
                   FontSize="13" Foreground="#FFCBD5E0" Margin="0,0,8,0"/>
    ```
    Grid.Column 은 캔버스 툴바의 실제 ColumnDefinitions 검토 후 결정 (label_drawHint 의 다음 컬럼 또는 추가 컬럼 신설).

    **4B. MainView.xaml.cs — UpdateDatumRefCoordsLabel 메서드 추가:**

    ```csharp
            //260425 hbk Phase 13 D-VIZ-06 — Datum reference 좌표 텍스트 갱신
            //  IsConfigured && LastTeachSucceeded 시 RefOrigin + Angle (+ CircleCenter/Radius) 표시.
            //  미설정 시 회색 'Datum 미설정'.
            private void UpdateDatumRefCoordsLabel(DatumConfig datum) {
                if (label_datumRefCoords == null) return;
                if (datum == null) {
                    label_datumRefCoords.Visibility = Visibility.Collapsed;
                    return;
                }
                if (!datum.IsConfigured || !datum.LastTeachSucceeded) {
                    label_datumRefCoords.Content = "Datum 미설정";
                    label_datumRefCoords.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FF888888"));
                    label_datumRefCoords.Visibility = Visibility.Visible;
                    return;
                }
                double angleDeg = datum.RefAngleRad * 180.0 / Math.PI;
                string text = "RefOrigin = (R: " + datum.RefOriginRow.ToString("F1")
                            + ", C: " + datum.RefOriginCol.ToString("F1")
                            + "), Angle = " + angleDeg.ToString("F2") + "°";
                if (datum.AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal && datum.CircleDetected_Radius > 0) {
                    text += "  |  CircleCenter = (R: " + datum.CircleCenter_Row.ToString("F1")
                          + ", C: " + datum.CircleCenter_Col.ToString("F1")
                          + "), Radius = " + datum.CircleDetected_Radius.ToString("F2");
                }
                label_datumRefCoords.Content = text;
                label_datumRefCoords.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FFCBD5E0"));
                label_datumRefCoords.Visibility = Visibility.Visible;
            }
    ```

    **4C. 호출 3 지점 추가:**

    1. **InspectionListView SelectedParam 변경** (Datum 노드 선택 시):
       - InspectionListView 가 어떤 이벤트 / 콜백 패턴을 사용하는지 코드 확인 후 적절한 hook 에 추가.
       - 가장 안전한 방안: MainView 가 inspectionList 선택 변경을 감지하는 기존 hook 에 한 줄 추가:
       ```csharp
       //260425 hbk Phase 13 D-VIZ-06 — Datum 노드 선택 시 reference 좌표 라벨 갱신
       UpdateDatumRefCoordsLabel(mParentWindow?.inspectionList?.SelectedParam as DatumConfig);
       ```

    2. **InvokeTryTeachDatum** 성공 분기 말미 (`halconViewer.SetDatumOverlay(_editingDatum, true);` 다음):
       ```csharp
       //260425 hbk Phase 13 D-VIZ-06 — teach 성공 시 좌표 라벨 갱신
       UpdateDatumRefCoordsLabel(_editingDatum);
       ```

    3. **HandleDatumRoiMove** 말미 (13-03 추가, `PublishDatumRoiCandidates(datum);` 다음):
       ```csharp
       //260425 hbk Phase 13 D-VIZ-06 — ROI 이동 후 자동 재티칭 결과로 좌표 라벨 갱신
       UpdateDatumRefCoordsLabel(datum);
       ```

    **4D. msbuild 합산 빌드 + 단일 commit:**

    ```bash
    msbuild WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal
    ```
    exit 0 + 신규 warning 0.

    ```bash
    git add WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs \
            WPF_Example/Halcon/Algorithms/DatumFindingService.cs \
            WPF_Example/Halcon/Display/HalconDisplayService.cs \
            WPF_Example/UI/ContentItem/MainView.xaml \
            WPF_Example/UI/ContentItem/MainView.xaml.cs
    git commit --no-verify -m "feat(phase-13-05): Datum visualization — extended detected lines + raw edge points + ref coords label"
    ```
  </action>
  <verify>
    <automated>
      (1) grep -c "label_datumRefCoords" WPF_Example/UI/ContentItem/MainView.xaml → 1
      (2) grep -cE "private void UpdateDatumRefCoordsLabel\(" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1
      (3) grep -cF "UpdateDatumRefCoordsLabel(" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 4 (def 1 + 호출 3+)
      (4) msbuild Debug/x64 → exit 0
      (5) 신규 warning 0 (5 개 수정 파일 참조)
    </automated>
  </verify>
  <done>
    - label_datumRefCoords XAML 추가
    - UpdateDatumRefCoordsLabel 메서드 + 3 호출 지점
    - msbuild Debug/x64 exit 0, 신규 warning 0
    - 4 task 합산 1 commit 완료
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: SIMUL_MODE UAT — 라인 외삽 + raw 에지점 + reference 좌표 라벨</name>
  <what-built>
    - 검출 라인 이미지 가장자리까지 확장 (Gap-C)
    - 5 ROI 별 raw 검출 에지점 색상 점 마커 표시 (Gap-RawEdgePointDisplay)
    - reference 좌표 텍스트 라벨 표시 (Gap-RefCoordsTextDisplay)
  </what-built>
  <how-to-verify>
    **환경:** SIMUL_MODE Debug/x64 빌드.

    ### 라인 외삽 (Gap-C)

    **테스트 1 (TwoLineIntersect):** Datum 티칭 완료 → yellow Line1 + cyan Line2 가 **이미지 양 끝까지** 그어짐 (이전엔 짧은 segment).

    **테스트 2 (CircleTwoHorizontal):** Line1Detected/Line2Detected 슬롯에 들어간 라인들이 외삽됨.

    **테스트 3 (VerticalTwoHorizontal):** 동일.

    ### Raw 에지점 (Gap-RawEdgePointDisplay)

    **테스트 4 (TwoLineIntersect):** Line1 영역에 cyan 점 다수 + Line2 영역에 magenta 점 다수 표시. 점들이 검출 라인 위 분포.

    **테스트 5 (CircleTwoHorizontal):** Circle 영역 노란 점 (TryFindCircle 가 raw 반환 시) + HorizA green + HorizB lime.

    **테스트 6 (VerticalTwoHorizontal):** Line1(수직) cyan + HorizA green + HorizB lime.

    **테스트 7 (per-ROI 파라미터 영향):** 13-04 의 Line1_EdgeThreshold 를 점진 증가 → cyan 점 개수 감소 시각 확인. (per-ROI 파라미터와 raw 점 시각화의 통합 검증.)

    ### Reference 좌표 라벨 (Gap-RefCoordsTextDisplay)

    **테스트 8:** Datum 노드 선택 → 라벨 표시:
    - 미설정 → "Datum 미설정" 회색
    - TwoLineIntersect 티칭 후 → "RefOrigin = (R: 1234.5, C: 6789.0), Angle = 12.34°"
    - CircleTwoHorizontal 티칭 후 → 위 + " | CircleCenter = (R: ..., C: ...), Radius = X.XX"

    **테스트 9 (ROI 이동 시 좌표 갱신):** 티칭 완료 후 ROI 드래그 이동 (13-03 경로) → 자동 재티칭 후 라벨 즉시 새 좌표 반영.

    **테스트 10 (다른 노드 선택 시 숨김):** Datum → FAI 노드 선택 → 라벨 Visibility=Collapsed.

    ### 회귀 (CRITICAL)

    **테스트 11 (Phase 13-01 방향 게이트):** 정상 발동.
    **테스트 12 (Phase 13-02 btn_testFindDatum):** 정상 동작.
    **테스트 13 (Phase 13-03 ROI 이동/삭제):** 정상 동작 + 라벨도 갱신.
    **테스트 14 (Phase 13-04 per-ROI 파라미터):** 정상 동작.
    **테스트 15 (FAI 무관):** FAI/Measurement 흐름 회귀 0.

    ### 빌드
    - msbuild Debug/x64 exit 0 + 신규 warning 0
  </how-to-verify>
  <resume-signal>"approved" 또는 구체 이슈 (예: "Line1 cyan 점이 안 보임", "라벨 텍스트가 잘림", "원 검출 시 노란 점 안 나옴")</resume-signal>
</task>

</tasks>

<verification>
**파일 수준:**
- 수정 파일 5 개: `DatumConfig.cs`, `DatumFindingService.cs`, `HalconDisplayService.cs`, `MainView.xaml`, `MainView.xaml.cs`
- DatumConfig: 10 신규 volatile HTuple 필드 (5 ROI × 2)
- DatumFindingService: TryFindLine 시그니처 +2 out (HTuple), 5 ROI write-back
- HalconDisplayService: EXTEND_PX + DrawExtendedLine + RenderRawEdgePoints + RenderDatumOverlay 통합 (Line 외삽 교체 + 5 ROI raw 점 렌더)
- MainView.xaml: label_datumRefCoords 1 추가
- MainView.xaml.cs: UpdateDatumRefCoordsLabel + 3 호출 지점

**빌드:**
- msbuild Debug/x64 exit 0
- 5 개 수정 파일 대상 신규 warning 0

**SIMUL_MODE UAT (Task 5):** 15 테스트 시나리오 통과 → "approved"

**누적 회귀 확인 (Phase 13 전체):**
- Plan 01 방향 정합성 게이트 정상
- Plan 02 btn_testFindDatum 정상
- Plan 03 ROI 이동/삭제 정상 + 라벨 갱신 추가됨
- Plan 04 per-ROI 파라미터 정상 + raw 점에 영향 시각 확인 가능
- 기존 FAI / Measurement 회귀 0
</verification>

<success_criteria>
- [ ] DatumConfig 에 10 신규 volatile HTuple 필드 (5 ROI × 2) + Browsable(false) 모두 적용
- [ ] DatumFindingService.TryFindLine 시그니처 +2 out (HTuple)
- [ ] DatumFindingService 5 ROI write-back (Line1/Line2/Circle/HorizA/HorizB)
- [ ] HalconDisplayService 에 EXTEND_PX + DrawExtendedLine + RenderRawEdgePoints
- [ ] RenderDatumOverlay 의 Line1Detected/Line2Detected DispLine → DrawExtendedLine 교체
- [ ] RenderDatumOverlay 의 LastTeachSucceeded 분기 말미에 5 ROI RenderRawEdgePoints 호출 (cyan/magenta/yellow/green/lime)
- [ ] MainView.xaml 에 label_datumRefCoords 추가 (Visibility="Collapsed" 기본)
- [ ] MainView.xaml.cs 에 UpdateDatumRefCoordsLabel + 3 호출 지점 (Datum 노드 선택 / 티칭 성공 / ROI 이동 후 재티칭)
- [ ] CircleTwoHorizontal 일 때 라벨에 CircleCenter / Radius 추가
- [ ] msbuild Debug/x64 exit 0 + 신규 warning 0
- [ ] 1 commit (4 task 합산)
- [ ] Task 5 SIMUL_MODE UAT 15 시나리오 통과 → "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/13-datum-algorithm-extensibility/13-05-SUMMARY.md`:
- Files Changed 테이블 (5 파일)
- Gap-C / Gap-RawEdgePointDisplay / Gap-RefCoordsTextDisplay coverage 매핑
- ROI 색상 팔레트 표 (Line1=cyan, Line2=magenta, Circle=yellow, HorizA=green, HorizB=lime)
- EXTEND_PX rationale (HALCON 자동 클리핑 활용)
- HTuple volatile 필드 패턴 설명 (Phase 4 D-11 패턴 동일, ParamBase 자동 무시)
- Build Result 블록
- UAT Outcome (Task 5 15 시나리오 결과)
- Phase 13 완료 종합 (4 plan 누적: 방향 게이트 / btn_testFindDatum / ROI 이동·삭제 / per-ROI 파라미터 / 시각화)
- Self-Check 체크리스트
</output>
</content>
</invoke>
</invoke>