---
phase: 14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: false
requirements: [SPEC-14-Req-1]
tags: [datum, roi-edit, circle, resize, wpf, halcon]

must_haves:
  truths:
    - "Circle Datum ROI 를 마우스 드래그로 이동하면 자동 재티칭이 발동되어 검출 원/raw 점이 새 위치로 즉시 갱신된다 (SIMUL_MODE 사용자 육안 PASS)"
    - "Circle Datum ROI 가 Edit 모드에서 N/S/E/W 4 위치에 시각적인 작은 사각형 핸들로 표시된다"
    - "Circle Datum ROI 의 N/S/E/W 핸들 중 어느 것을 잡고 드래그해도 반경이 변경된다 (drag 종점-center 거리)"
    - "Circle Datum ROI resize 완료 시 자동 재티칭이 발동되어 검출 원이 새 반경으로 갱신된다"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs"
      provides: "Datum Circle ROI Edit 핸들 렌더 / hit-test / resize drag 상태 머신"
      contains: "_datumRoiCandidates"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "HalconViewer_RoiGeometryChanged Datum 분기 + HandleDatumRoiResize helper"
      contains: "HandleDatumRoiResize"
  key_links:
    - from: "MainResultViewerControl.xaml.cs MouseUp resize branch"
      to: "MainView.xaml.cs HalconViewer_RoiGeometryChanged"
      via: "RoiGeometryChanged event with RoiId='Datum.Circle'"
      pattern: "RoiGeometryChanged\\?\\.Invoke"
    - from: "MainView.HandleDatumRoiResize"
      to: "InvokeTryTeachDatumForEdit + PublishDatumRoiCandidates + UpdateDatumRefCoordsLabel"
      via: "5-step write-back chain (HandleDatumRoiMove pattern)"
      pattern: "InvokeTryTeachDatumForEdit"
---

<objective>
Phase 13 UAT carry-over Test 1 결함 fix:
1. (Move 회귀) Circle Datum ROI 드래그 이동 시 자동 재티칭이 사용자 시점에서 미발동되어 검출 결과가 갱신 안 되는 현상 진단/수정
2. (Resize 미동작) Circle Datum ROI Edit 모드 N/S/E/W 4 핸들이 시각적으로 표시되지 않고 동작도 안 되는 현상 — 핸들 렌더 + hit-test + drag 상태 머신을 Datum 후보까지 확장 + 단일 RoiGeometryChanged 이벤트로 MainView 에 통보 (D-04 단일 이벤트 확장)

Purpose: Datum 3 알고리즘 모두에서 ROI 인터랙션 (이동/resize) 후 자동 재티칭 → 검출 결과 즉시 갱신이라는 Phase 13 UX 약속을 Circle 케이스까지 마무리한다. 14-02/14-03/14-04 와 독립 (서로 다른 파일).

Output: MainResultViewerControl Edit 핸들/resize 상태 머신이 _rois 뿐 아니라 _datumRoiCandidates 도 lookup. MainView 에 신규 HandleDatumRoiResize helper. SIMUL_MODE 육안 검증.

I2 audit (D-04 conflict-check): Drag 상태 머신 (MainResultViewerControl.xaml.cs `:760-779` `_resizingHandle` state) 검토 결과, RoiGeometryChanged 는 FAI Circle resize 에서 이미 발행됨. Datum 분기는 MainView.HalconViewer_RoiGeometryChanged 의 FAI lookup **이전** 에 추가되므로 신규 이벤트 신설 불필요 (D-04 단일 이벤트 확장 무충돌 검증 완료).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-SPEC.md
@.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-CONTEXT.md
@.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-PATTERNS.md
@.planning/phases/13-datum-algorithm-extensibility/13-06-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Phase 13 자동 재티칭 라우팅 entry points (MainView.xaml.cs) -->
<!-- HandleDatumRoiMove 의 5-step 패턴: write-back → RaisePropertyChanged → RefreshParamEditor → SetDatumOverlay → InvokeTryTeachDatumForEdit → PublishDatumRoiCandidates → UpdateDatumRefCoordsLabel -->

From WPF_Example/UI/ContentItem/MainView.xaml.cs:
```csharp
private void HalconViewer_RoiGeometryChanged(object sender, RoiGeometryChangedArgs e); // L447-483, FAI 전용 분기 — Datum 우선 분기 추가 대상
private void HandleDatumRoiMove(DatumConfig datum, RoiMoveCompletedArgs e);            // L567-577, 신규 HandleDatumRoiResize 의 모델
private void ApplyDatumRoiDelta(DatumConfig datum, RoiMoveCompletedArgs e);            // L579-597
private void InvokeTryTeachDatumForEdit(DatumConfig datum);                            // L646-664
private void PublishDatumRoiCandidates(DatumConfig datum);                             // L697-728
private void UpdateDatumRefCoordsLabel(DatumConfig datum);                             // 13-05 신규
private bool IsCurrentNodeDatum(out DatumConfig datum);                                // L562-565 helper
public class RoiGeometryChangedArgs                                                    // RoiId / Shape / CenterRow / CenterCol / Radius / ...
```

From WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs:
```csharp
private const double HandleHalfSizeImage = 10.0;     // L134
private List<RoiDefinition> _rois;                   // 메인 FAI ROI
private List<RoiDefinition> _datumRoiCandidates;     // Datum publish 시 set
private bool _isEditMode;
private bool _isResizingRoi;
private ResizeHandle _resizingHandle;
private RoiDefinition _resizingRoiSnapshot;

private void RenderEditHandles();                                              // L493-510, _isEditMode + _rois 만 — 확장 대상
private List<EditHandle> GetEditHandles(RoiDefinition roi);                    // L365-372, Circle 4 핸들 (N/S/E/W) 정상 동작
private (ResizeHandle Handle, int PolyIndex) HitTestEditHandle(RoiDefinition roi, Point p);
private void ApplyResizeToTarget(RoiDefinition target, RoiDefinition snap, Point imagePoint);  // L415-458, Circle 분기 정상
public event EventHandler<RoiGeometryChangedArgs> RoiGeometryChanged;          // MouseUp 시 발행 (L982-1010)
```

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs:
```csharp
public bool IsConfigured { get; }            // 자동 재티칭 가드 (Phase 13-07)
public bool LastTeachSucceeded { get; set; }
public double CircleROI_Row { get; set; }
public double CircleROI_Col { get; set; }
public double CircleROI_Radius { get; set; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MainResultViewerControl — Datum Circle Edit 핸들 렌더 + hit-test + drag 상태 머신 확장</name>
  <files>WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs (전체 — Allman 스타일 / _datumRoiCandidates / HitTestSelectedRoi / RenderEditHandles / GetEditHandles / ApplyResizeToTarget / MouseUp resize 블록 위치 파악)
    - W3 fix — explicit handle coverage anchor: WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs:365-372 (GetEditHandles Circle branch — N/S/E/W 4 핸들 등록 위치)
    - W3 fix — handle-to-enum mapping anchor: WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs:420-427 (ApplyResizeToTarget Circle 분기 — 4 핸들이 단일 ResizeHandle.CircleRadius 분기로 매핑되는지 확인)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-PATTERNS.md (1.1 / 1.2 / 1.3 sub-section — analog code 인용)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-SPEC.md (Requirement 1 — Acceptance)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-CONTEXT.md (D-01 / D-02 / D-04 결정 — 단일 RoiGeometryChanged 이벤트 확장)
    - .planning/phases/13-datum-algorithm-extensibility/13-06-SUMMARY.md (자동 재티칭 라우팅)
    - CLAUDE.md (//YYMMDD hbk 주석 + Allman 스타일 + try/catch HOperatorSet)
  </read_first>
  <action>
파일 스타일: **Allman** (이 파일은 Allman). 신규 코드도 Allman 유지.
주석: 모든 신규/수정 라인 위에 `//260426 hbk Phase 14-01 — <intent>` (CLAUDE.md feedback_comment_convention 강제).

**A. RenderEditHandles 확장 (D-01 시각적 핸들 표시)** — 현재 함수 (`:493-510`):
```csharp
private void RenderEditHandles()
{
    if (!_isEditMode) return;
    var roi = _rois.FirstOrDefault(r => r.Id == _selectedRoiId && r.IsTaught);
    if (roi == null) return;
    // ... DispRectangle1 핸들 그리기
}
```
다음으로 교체:
```csharp
//260426 hbk Phase 14-01 D-01 — Datum Circle Edit 핸들도 보이도록: _isEditMode OR _datumRoiCandidates 존재 시 활성. lookup 은 _rois → _datumRoiCandidates fallback.
private void RenderEditHandles()
{
    bool datumCandidatesPresent = (_datumRoiCandidates != null && _datumRoiCandidates.Count > 0);
    if (!_isEditMode && !datumCandidatesPresent) return;

    RoiDefinition roi = null;
    if (_isEditMode && !string.IsNullOrEmpty(_selectedRoiId))
    {
        roi = _rois.FirstOrDefault(r => r.Id == _selectedRoiId && r.IsTaught);
    }
    if (roi == null && datumCandidatesPresent)
    {
        //260426 hbk Phase 14-01 — Datum Circle 후보 fallback (Edit 모드 비활성이어도 핸들 노출)
        roi = _datumRoiCandidates.FirstOrDefault(r => r.Shape == RoiShape.Circle);
    }
    if (roi == null) return;

    var window = ViewerHost.HalconWindow;
    window.SetColor("yellow"); //260426 hbk Phase 14-01 — Datum 시각 일관 (cyan→yellow)
    window.SetLineWidth(2);
    foreach (var h in GetEditHandles(roi))
    {
        window.DispRectangle1(
            h.Pos.Y - HandleHalfSizeImage,
            h.Pos.X - HandleHalfSizeImage,
            h.Pos.Y + HandleHalfSizeImage,
            h.Pos.X + HandleHalfSizeImage);
    }
}
```

**B. MouseDown resize 진입 가드 확장 (D-02 4 핸들 동일 동작)** — 현재 (`:760-779`) 의 주석 `// Datum 은 리사이즈 미지원` 라인을 제거하고 가드 일반화:
```csharp
//260426 hbk Phase 14-01 — Edit 모드 OR Datum 후보 존재 시 핸들 hit-test 활성 (Datum Circle resize 지원)
bool datumCandidatesPresent = (_datumRoiCandidates != null && _datumRoiCandidates.Count > 0);
if (_isEditMode || datumCandidatesPresent)
{
    RoiDefinition selectedRoi = null;
    if (_isEditMode && !string.IsNullOrEmpty(_selectedRoiId))
    {
        selectedRoi = _rois.FirstOrDefault(r => r.Id == _selectedRoiId && r.IsTaught);
    }
    if (selectedRoi == null && datumCandidatesPresent)
    {
        //260426 hbk Phase 14-01 — Datum Circle fallback (mouse 위치 기반 hit-test 는 GetEditHandles 가 처리)
        selectedRoi = _datumRoiCandidates.FirstOrDefault(r => r.Shape == RoiShape.Circle);
    }
    if (selectedRoi != null)
    {
        var handleHit = HitTestEditHandle(selectedRoi, mouseState.ImagePoint);
        if (handleHit.Handle != ResizeHandle.None)
        {
            _isResizingRoi = true;
            _resizingHandle = handleHit.Handle;
            _resizingPolygonIndex = handleHit.PolyIndex;
            _resizingRoiSnapshot = selectedRoi.Clone();
            SetPanCursor(Cursors.SizeAll);
            PublishPointerInfo();
            return;
        }
    }
}
```

**C. MouseMove resize 단계 — `_rois` lookup fallback 추가** (현재 `:864-875` 의 resizing branch 안):
```csharp
//260426 hbk Phase 14-01 — Datum Circle resize 시 _rois 에 없으면 _datumRoiCandidates lookup
var target = _rois.FirstOrDefault(r => r.Id == _resizingRoiSnapshot.Id);
if (target == null && _datumRoiCandidates != null)
{
    target = _datumRoiCandidates.FirstOrDefault(r => r.Id == _resizingRoiSnapshot.Id);
}
if (target == null) return;
ApplyResizeToTarget(target, _resizingRoiSnapshot, mouseState.ImagePoint);
```

**D. MouseUp resize 완료 — RoiGeometryChanged 발행 시 동일 fallback** (현재 `:982-1010`):
```csharp
//260426 hbk Phase 14-01 D-04 — Datum Circle resize 완료 시 RoiGeometryChanged 발행 (단일 이벤트 확장, 신규 이벤트 X)
if (_isResizingRoi && _resizingRoiSnapshot != null)
{
    var target = _rois.FirstOrDefault(r => r.Id == _resizingRoiSnapshot.Id);
    if (target == null && _datumRoiCandidates != null)
    {
        target = _datumRoiCandidates.FirstOrDefault(r => r.Id == _resizingRoiSnapshot.Id);
    }
    string movedId = _resizingRoiSnapshot.Id;
    RoiShape shape = _resizingRoiSnapshot.Shape;

    if (target != null)
    {
        RoiGeometryChanged?.Invoke(this, new RoiGeometryChangedArgs
        {
            RoiId = movedId,
            Shape = shape,
            CenterRow = target.CenterRow,
            CenterCol = target.CenterCol,
            Radius = target.Radius,
        });
    }

    _isResizingRoi = false;
    _resizingHandle = ResizeHandle.None;
    _resizingRoiSnapshot = null;
    SetPanCursor(Cursors.Arrow);
}
```

**E. 기존 주석 `// Datum 은 리사이즈 미지원` (`:761` 부근)을 다음으로 대체** (또는 삭제):
```csharp
//260426 hbk Phase 14-01 — (제거됨) Datum 도 리사이즈 지원
```

**HOperatorSet 호출 변경 없음** (이 파일 변경은 HALCON 호출 비포함). try/catch 패턴 새로 도입 불필요.
  </action>
  <verify>
    <automated>
&quot;C:\Program Files (x86)\Microsoft Visual Studio\2017\Professional\MSBuild\15.0\Bin\MSBuild.exe&quot; WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal
    </automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 exit 0, 0 errors, 신규 warning 0
    - grep "Datum 은 리사이즈 미지원" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → 0 hits (주석 제거 또는 대체됨)
    - grep -c "//260426 hbk Phase 14-01" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → ≥ 4 (신규/수정 블록 4개 이상)
    - grep "_datumRoiCandidates" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → ≥ 5 hits (RenderEditHandles + MouseDown + MouseMove + MouseUp + 기존 1)
    - grep "RoiGeometryChanged" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → 기존 호출 보존 (신규 이벤트 신설 X — 단일 이벤트 확장)
    - W3 fix — N/S/E/W handle coverage:
      - grep -c 'ResizeHandle\.\w*' WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs → ≥ 4 hits in GetEditHandles Circle branch (4 핸들 등록)
      - grep -c 'ResizeHandle.CircleRadius' WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs in ApplyResizeToTarget Circle 분기 → 1 hit (4 핸들이 단일 CircleRadius 분기로 매핑 — D-02 동일 동작 보장)
      - 만약 기존 코드가 4 distinct enum (CircleN/CircleS/CircleE/CircleW) 사용 중이면, Task 2 (또는 본 Task 안) 에서 통일 case 블록 추가 또는 4 enum 모두 동일 로직 (drag 종점-center 거리 = 새 반경) 으로 매핑. 이 contingency 발생 시 plan 실행자가 Task 2 action 에 명시 후 처리.
  </acceptance_criteria>
  <done>
    빌드 PASS + Datum 가드 일반화 완료 + 핸들 렌더 fallback 동작. 실제 동작 검증은 Task 2 + Task 3 통합 후.
  </done>
</task>

<task type="auto">
  <name>Task 2: MainView — HalconViewer_RoiGeometryChanged Datum 분기 + HandleDatumRoiResize helper + Move 자동 재티칭 진단/fix</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (전체 — K&R 스타일 / HalconViewer_RoiGeometryChanged L447-483 / HalconViewer_RoiMoveCompleted L528-559 / HandleDatumRoiMove L567-577 / ApplyDatumRoiDelta L579-597 / IsCurrentNodeDatum L562-565 / InvokeTryTeachDatumForEdit L646-664 / NotifyDatumParamMaybeChanged L638-643 / PublishDatumRoiCandidates L697-728 / UpdateDatumRefCoordsLabel)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-PATTERNS.md (1.3 + 1.4 sub-section)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-SPEC.md (Requirement 1 Acceptance)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-CONTEXT.md (D-03 / D-04)
    - .planning/phases/13-datum-algorithm-extensibility/13-06-SUMMARY.md (Phase 13-07 Dispatcher.BeginInvoke(Background) defer 패턴)
    - CLAUDE.md (//YYMMDD hbk + K&R + ELogType 캐스트)
  </read_first>
  <action>
파일 스타일: **K&R** (이 파일은 K&R). 신규 코드 K&R 유지.
주석: `//260426 hbk Phase 14-01 — <intent>`.

**A. HalconViewer_RoiGeometryChanged 시작부에 Datum 우선 분기 삽입** (L447-483 함수):
```csharp
//260426 hbk Phase 14-01 D-04 — Datum 분기 우선 (FAI lookup 전에) — 단일 RoiGeometryChanged 이벤트 확장
private void HalconViewer_RoiGeometryChanged(object sender, RoiGeometryChangedArgs e) {
    if (e == null || string.IsNullOrEmpty(e.RoiId)) return;

    //260426 hbk Phase 14-01 — Datum.* RoiId 면 FAI 분기 진입 전에 처리하고 return
    DatumConfig datum;
    if (e.RoiId.StartsWith("Datum.") && IsCurrentNodeDatum(out datum) && datum != null) {
        HandleDatumRoiResize(datum, e);
        return;
    }

    var fai = FindFAIByName(e.RoiId);
    if (fai == null) return;
    // ... 기존 FAI Circle/Polygon/Rect 분기 untouched
}
```

**B. HandleDatumRoiResize helper 신규 추가** (HandleDatumRoiMove 직후 위치):
```csharp
//260426 hbk Phase 14-01 D-04 — Datum ROI resize 후처리 (HandleDatumRoiMove 5-step 패턴 동일, delta vs absolute 차이만)
private void HandleDatumRoiResize(DatumConfig datum, RoiGeometryChangedArgs e) {
    if (datum == null || e == null) return;

    //260426 hbk Phase 14-01 — Circle 절대 좌표 직접 대입 (resize 는 delta 가 아닌 새 절대값)
    if (e.RoiId == "Datum.Circle" && e.Shape == RoiShape.Circle) {
        datum.CircleROI_Row    = e.CenterRow;
        datum.CircleROI_Col    = e.CenterCol;
        datum.CircleROI_Radius = e.Radius;
    }
    //260426 hbk Phase 14-01 — Rect 핸들은 Phase 14 scope 외 (deferred to ROI Edit 전반 재설계 phase)

    //260426 hbk Phase 14-01 — write-back 후 이중 신호 (HandleDatumRoiMove 패턴 — PATTERNS.md Shared C)
    try { datum.RaisePropertyChanged(string.Empty); } catch { }
    mParentWindow?.inspectionList?.RefreshParamEditor();
    halconViewer.SetDatumOverlay(datum, true);

    //260426 hbk Phase 14-01 D-03 — 자동 재티칭 (Phase 13-07 Dispatcher.BeginInvoke(Background) defer 패턴)
    Dispatcher.BeginInvoke(System.Windows.Threading.DispatcherPriority.Background, new Action(() => {
        InvokeTryTeachDatumForEdit(datum);
        PublishDatumRoiCandidates(datum);
        UpdateDatumRefCoordsLabel(datum);
    }));
}
```

**C. HalconViewer_RoiMoveCompleted 진단 로깅 추가** (L528-559) — Move 자동 재티칭 회귀 1차 진단:
함수 시작부 (Datum 분기 진입 전 또는 직후) 에 다음 추가:
```csharp
//260426 hbk Phase 14-01 — Move 자동 재티칭 회귀 진단 로그 (Phase 14-05 verify 시 PASS 면 제거 가능)
if (e != null && !string.IsNullOrEmpty(e.RoiId) && e.RoiId.StartsWith("Datum.")) {
    Logging.PrintLog((int)ELogType.Trace,
        "Datum ROI move: id=" + e.RoiId + " dr=" + e.DeltaRow + " dc=" + e.DeltaCol);
}
```

**D. HandleDatumRoiMove (L567-577) 의 InvokeTryTeachDatumForEdit 호출을 Dispatcher.BeginInvoke(Background) 로 wrap** — Move 회귀 fix (Phase 13-07 패턴):
```csharp
//260426 hbk Phase 14-01 D-03 — Move 자동 재티칭 미발동 회귀 fix: Dispatcher defer (Phase 13-07 Fix A 패턴)
Dispatcher.BeginInvoke(System.Windows.Threading.DispatcherPriority.Background, new Action(() => {
    InvokeTryTeachDatumForEdit(datum);
    PublishDatumRoiCandidates(datum);
    UpdateDatumRefCoordsLabel(datum);
}));
```
(기존 동기 호출 라인 3 줄을 위 BeginInvoke 람다로 옮김)

**E. InvokeTryTeachDatumForEdit (L646-664) 진입/종료 로깅** — 진단용:
```csharp
//260426 hbk Phase 14-01 — 진단: 자동 재티칭 진입/종료 로깅
Logging.PrintLog((int)ELogType.Trace, "InvokeTryTeachDatumForEdit ENTRY: IsConfigured=" + datum?.IsConfigured);
// ... 기존 본문
Logging.PrintLog((int)ELogType.Trace, "InvokeTryTeachDatumForEdit EXIT: LastTeachSucceeded=" + datum?.LastTeachSucceeded);
```
  </action>
  <verify>
    <automated>
&quot;C:\Program Files (x86)\Microsoft Visual Studio\2017\Professional\MSBuild\15.0\Bin\MSBuild.exe&quot; WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal
    </automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 exit 0, 0 errors, 신규 warning 0
    - grep -c "HandleDatumRoiResize" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 2 (정의 + 호출)
    - grep "e.RoiId.StartsWith(\"Datum.\")" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 2 hits (RoiGeometryChanged + RoiMoveCompleted 진단)
    - grep -c "//260426 hbk Phase 14-01" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 6
    - grep "Dispatcher.BeginInvoke" WPF_Example/UI/ContentItem/MainView.xaml.cs → HandleDatumRoiResize + HandleDatumRoiMove 둘 다 적용 (≥ 2 occurrences with InvokeTryTeachDatumForEdit nearby)
    - grep "InvokeTryTeachDatumForEdit ENTRY" WPF_Example/UI/ContentItem/MainView.xaml.cs → 1 hit (진단 로그)
  </acceptance_criteria>
  <done>
    빌드 PASS + HandleDatumRoiResize 정의 + RoiGeometryChanged Datum 분기 + Move 회귀 Dispatcher defer fix 적용. 실제 동작은 Task 3 SIMUL_MODE checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SIMUL_MODE 육안 검증 — Circle ROI 이동/resize 자동 재티칭</name>
  <what-built>
    Circle Datum ROI Edit 핸들 (N/S/E/W 노란 사각형) 표시 + 4 핸들 어디든 드래그로 반경 변경 + Resize 완료 시 자동 재티칭 → 검출 원/raw 점 갱신. Move 회귀도 Dispatcher.BeginInvoke 패턴으로 fix.
  </what-built>
  <how-to-verify>
    1. SIMUL_MODE 빌드 실행 (Debug/x64)
    2. CircleTwoHorizontal 알고리즘 Datum 노드 선택 후 btn_teachDatum 으로 (Vertical 또는 Circle ROI 등) 티칭 완료 → 검출 원/raw 점이 캔버스에 노란 색으로 보이는 상태 만들기
    3. **Move 시나리오:** Circle ROI 내부를 드래그하여 다른 위치로 이동 → 검출 원/raw 점이 새 위치에서 즉시 다시 그려지는지 확인 (자동 재티칭 발동 확인)
    4. **Resize 시나리오 (E 핸들):** Circle ROI 의 동쪽 (오른쪽) 작은 노란 사각형 핸들이 보이는지 시각 확인 → E 핸들을 잡고 드래그 → ROI 반경이 변경되고 마우스 놓으면 검출 원이 새 반경으로 다시 그려지는지 확인
    5. **Resize 시나리오 (N/S/W 핸들):** 북/남/서 3 핸들 각각 동일하게 잡고 드래그 → 모두 반경 변경 + 자동 재티칭 발동 확인
    6. **회귀 없음 확인:** TwoLineIntersect 알고리즘으로 전환 → Line1/Line2 ROI 드래그 이동 → 기존 동작 유지 (검출 라인 새 위치 반영) — Phase 13 회귀 0 보장
    7. 모두 PASS 시 "approved" 응답. 실패 항목은 어느 단계 (Move / Resize-E / Resize-N / Resize-S / Resize-W / 회귀) 인지 명시.
  </how-to-verify>
  <resume-signal>"approved" 또는 실패 단계 + 관찰된 동작 기술 (예: "E 핸들 보이지만 드래그 시 반경 변경 안 됨")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 마우스 → MainResultViewerControl | 사용자 입력만 신뢰 — 외부 untrusted 입력 없음 (오프라인 데스크탑) |
| DatumConfig PropertyGrid → INI persistence | INI 직렬화는 Phase 13-04 EnsurePerRoiDefaults sanity clamp 가 이미 처리 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | Circle resize 시 음수/0 반경 입력 가능성 | mitigate | ApplyResizeToTarget Circle 분기 (`:420-427`) 의 `Math.Max(1.0, newRadius)` 가드 이미 존재 — 보존 (변경 없음) |
| T-14-01-02 | Denial of Service | InvokeTryTeachDatumForEdit 가 UI 스레드 블록 → 마우스 이벤트 누적 | mitigate | Dispatcher.BeginInvoke(Background) defer 패턴 (Phase 13-07 Fix A) 적용 — UI thread 즉시 반환 |
| T-14-01-03 | Information Disclosure | Trace 로깅으로 Datum 좌표 노출 | accept | LOW — 데스크탑 단일 사용자, 로그는 로컬 파일, 비밀 정보 없음 |
</threat_model>

<verification>
- msbuild Debug/x64 exit 0
- 신규 warning 0
- Phase 13 ALG-05 회귀 없음 (TwoLineIntersect 회귀 없음 확인은 Task 3 Step 6)
- Phase 13 ALG-04 회귀 없음 (FAI 측정 경로 무변경 — RoiGeometryChanged 의 FAI 분기 손대지 않음)
</verification>

<success_criteria>
- Circle Datum ROI 이동 → 자동 재티칭 → 검출 원/raw 점 즉시 갱신 (SIMUL_MODE PASS)
- Circle Datum ROI Edit 모드 N/S/E/W 4 노란 사각형 핸들 시각 표시
- 4 핸들 어디든 드래그 → 반경 변경 + 자동 재티칭 (SIMUL_MODE PASS)
- TwoLineIntersect 회귀 없음
</success_criteria>

<output>
After completion, create `.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-01-SUMMARY.md`
</output>
