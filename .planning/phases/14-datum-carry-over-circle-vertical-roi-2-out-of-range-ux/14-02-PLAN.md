---
phase: 14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
autonomous: false
requirements: [SPEC-14-Req-2]
tags: [datum, two-line-intersect, validation, propertygrid]

must_haves:
  truths:
    - "DatumConfig 에 신규 PropertyGrid 필드 TwoLineAngleToleranceDeg (default 10°) 가 노출된다"
    - "TwoLineIntersect 알고리즘에서 두 라인 각도가 90°±N° 를 벗어나면 LastTeachSucceeded=false + fail 라벨 텍스트가 표시된다"
    - "TwoLineAngleToleranceDeg=0 입력 시 게이트 off (어떤 각도여도 PASS)"
    - "기존 INI (Phase 4/11/12/13) 로드 시 키가 없으면 default 10° 자동 적용 (회귀 0)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "TwoLineAngleToleranceDeg 신규 PropertyGrid 필드"
      contains: "TwoLineAngleToleranceDeg"
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "TryTeachTwoLineIntersect 끝부분 각도 게이트 + fail 라벨 literal"
      contains: "Two-line angle out of range"
  key_links:
    - from: "DatumConfig.TwoLineAngleToleranceDeg"
      to: "TryTeachTwoLineIntersect 게이트"
      via: "config.TwoLineAngleToleranceDeg 직접 참조"
      pattern: "TwoLineAngleToleranceDeg"
---

<objective>
Phase 13 UAT Test 2 carry-over (UX 갭): TwoLineIntersect 알고리즘이 두 라인 각도 무검증으로 휘어진 라인에서도 datum origin 산출하는 문제 해결. CircleTwoHorizontal/VerticalTwoHorizontal 의 ValidateHorizontalVerticalAngles 와 별개로, TwoLineIntersect 전용 직각성 게이트를 PropertyGrid 사용자 임계각으로 추가.

Purpose: 어떤 알고리즘이든 라인이 휘어졌으면 fail 라벨로 거부한다는 사용자 UX 기대 충족. 14-01/14-03/14-04 와 독립.

Output: DatumConfig 신규 필드 1 + DatumFindingService TryTeachTwoLineIntersect 끝부분 게이트 추가. SIMUL_MODE 육안 검증.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-SPEC.md
@.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-CONTEXT.md
@.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-PATTERNS.md
@CLAUDE.md

<interfaces>
From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs:
```csharp
// L32-38 — Datum|Algorithm category (existing)
[Category("Datum|Algorithm")]
[ItemsSourceProperty(nameof(AlgorithmTypeList))]
public string AlgorithmType { get; set; } = "TwoLineIntersect";

public bool LastTeachSucceeded { get; set; }   // 게이트 실패 시 false 대입
```

From WPF_Example/Halcon/Algorithms/DatumFindingService.cs:
```csharp
// L179-289 — TryTeachTwoLineIntersect (Phase 12 D-09 — switch 디스패치 진입점)
//   끝부분 (L267-281) 의 LastTeachSucceeded=true 직전 위치가 게이트 삽입 지점
//   line1 / line2 의 RowBegin / ColBegin / RowEnd / ColEnd 변수가 본문에 이미 존재
//   error string out parameter 보유

// L639-675 — ValidateHorizontalVerticalAngles (CircleTwoHorizontal/VerticalTwoHorizontal 전용 — TwoLineIntersect 미적용; D-12 의도 보존)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DatumConfig — TwoLineAngleToleranceDeg PropertyGrid 필드 추가</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (전체 — K&R 스타일 / Datum|Algorithm category 위치 L32-38 / EnsurePerRoiDefaults 패턴 L326-374 / ParamBase reflection 직렬화 패턴)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-PATTERNS.md (2.1 sub-section)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-SPEC.md (Requirement 2 — Acceptance)
    - CLAUDE.md (//YYMMDD hbk + K&R + ParamBase reflection — int/double/string 만 직렬화)
  </read_first>
  <action>
파일 스타일: **K&R**. 신규 코드 K&R.
주석: `//260426 hbk Phase 14-02 — <intent>`.

`AlgorithmType` 속성 정의 (L32-38) 직후 위치에 다음 신규 필드 추가:
```csharp
//260426 hbk Phase 14-02 Req 2 — TwoLineIntersect 두 라인 직각성 게이트 임계각 (도)
//  0  = 게이트 off (어떤 각도여도 PASS)
//  10 = default (90°±10° 허용)
//  range hint: 0~45°. INI 미존재 시 default 10° (값이 0 이면 명시 off 의도로 간주).
//  ParamBase reflection 자동 직렬화 — 별도 Save/Load 코드 불필요.
[Category("Datum|Algorithm")]
public double TwoLineAngleToleranceDeg { get; set; } = 10.0;
```

**중요:**
- `[PropertyTools.DataAnnotations.Browsable(false)]` 미적용 — PropertyGrid 노출 의도.
- `[Category("Datum|Algorithm")]` — 기존 AlgorithmType 과 같은 카테고리.
- default 10.0 — INI 미존재 시 자동 fallback (회귀 0). 사용자가 명시 0 입력 시 게이트 off 의도.
- EnsurePerRoiDefaults 분기 추가 **불필요** — default value 가 INI 로드 전에 이미 의미값 (10.0). sentinel 0 이 사용자 의도 (off) 와 충돌 가능하지만 SPEC 에서 0=off 로 명시 정의했으므로 마이그레이션 분기 X.
- HOperatorSet 호출 없음 → try/catch 신규 도입 불필요.
  </action>
  <verify>
    <automated>
&quot;C:\Program Files (x86)\Microsoft Visual Studio\2017\Professional\MSBuild\15.0\Bin\MSBuild.exe&quot; WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal
    </automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 exit 0, 0 errors, 신규 warning 0
    - grep "public double TwoLineAngleToleranceDeg" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → 정확히 1 hit
    - grep "TwoLineAngleToleranceDeg.*=.*10.0" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → 1 hit (default 10.0)
    - grep "//260426 hbk Phase 14-02" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → ≥ 1 hit
    - grep "Category(\"Datum|Algorithm\")" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → ≥ 2 hits (AlgorithmType + 신규 필드)
  </acceptance_criteria>
  <done>
    빌드 PASS + 신규 필드 등록. INI 하위호환은 ParamBase reflection 패턴이 자동 처리.
  </done>
</task>

<task type="auto">
  <name>Task 2: DatumFindingService — TryTeachTwoLineIntersect 끝부분 두 라인 각도 게이트 추가</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (Allman 스타일 / TryTeachTwoLineIntersect L179-289 본문 — line1RowBegin/ColBegin/RowEnd/ColEnd line2RowBegin/...ColEnd 변수명 정확히 확인 / LastTeachSucceeded=true 위치 L279 / ValidateHorizontalVerticalAngles helper L639-675 컨벤션)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-PATTERNS.md (2.2 sub-section + Shared B 에러 처리)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-SPEC.md (Requirement 2 fail 라벨 literal)
    - CLAUDE.md (//YYMMDD hbk + Allman + try/catch HOperatorSet)
  </read_first>
  <action>
파일 스타일: **Allman**. 신규 코드 Allman.
주석: `//260426 hbk Phase 14-02 — <intent>`.

`TryTeachTwoLineIntersect` 끝부분 (L267-281 부근, `config.LastTeachSucceeded = true;` 직전) 에 다음 게이트 블록 삽입:

```csharp
//260426 hbk Phase 14-02 Req 2 — TwoLineIntersect 두 라인 각도 게이트
//  사용자 임계각 N=TwoLineAngleToleranceDeg, 0 이면 게이트 off (PASS).
//  측정값 = |line1 phi - line2 phi| 를 [0,180) 정규화 후 90° 와의 편차.
//  CircleTwoHorizontal/VerticalTwoHorizontal 의 ValidateHorizontalVerticalAngles 와 별개 게이트 (TwoLineIntersect 한정).
if (config.TwoLineAngleToleranceDeg > 0.0)
{
    double phi1 = Math.Atan2(line1RowEnd - line1RowBegin, line1ColEnd - line1ColBegin);
    double phi2 = Math.Atan2(line2RowEnd - line2RowBegin, line2ColEnd - line2ColBegin);
    double deltaDeg = Math.Abs((phi1 - phi2) * 180.0 / Math.PI);
    while (deltaDeg >= 180.0) deltaDeg -= 180.0;
    double perpErr = Math.Abs(deltaDeg - 90.0);
    if (perpErr > config.TwoLineAngleToleranceDeg)
    {
        config.LastTeachSucceeded = false;
        //260426 hbk Phase 14-02 SPEC AC literal (Req 2) — ASCII 컨벤션 (deg, +/-) — Phase 13 ValidateHorizontalVerticalAngles L651-655 와 동일
        error = "Two-line angle out of range: "
              + deltaDeg.ToString("F1")
              + " deg (expected 90 +/- "
              + config.TwoLineAngleToleranceDeg.ToString("F1")
              + " deg)";
        return false;
    }
}
```

**위치 주의:**
- `LastTeachSucceeded = true;` 라인 **앞** 에 배치 (게이트 실패 시 LastTeachSucceeded=false + early return 보장).
- `Line1Detected_*` 좌표 저장 (L271-278) 라인 **앞** 에 배치 (게이트 실패 시 오버레이 미저장 — 이전 성공 잔류 허용 D-13 설계).

**변수명 검증:**
- 본문에 이미 존재해야 하는 변수명: `line1RowBegin`, `line1ColBegin`, `line1RowEnd`, `line1ColEnd`, `line2RowBegin`, `line2ColBegin`, `line2RowEnd`, `line2ColEnd` (TryFindLine out 결과). 만약 실제 변수명이 다르면 (`l1RB` 등) 본문 grep 으로 재확인 후 매핑.

**HOperatorSet 호출 없음** (Math.Atan2 만 사용) → 신규 try/catch 도입 불필요. 기존 함수 try/catch 가 이미 본문 wrapping.

**Phase 13 ValidateHorizontalVerticalAngles 회귀 검증:** CircleTwoHorizontal/VerticalTwoHorizontal 경로는 이 함수와 무관 (별 함수). 회귀 영향 0.
  </action>
  <verify>
    <automated>
&quot;C:\Program Files (x86)\Microsoft Visual Studio\2017\Professional\MSBuild\15.0\Bin\MSBuild.exe&quot; WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal
    </automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 exit 0, 0 errors, 신규 warning 0
    - grep "Two-line angle out of range" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 정확히 1 hit (literal)
    - grep "config.TwoLineAngleToleranceDeg" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → ≥ 2 hits (게이트 가드 + 에러 메시지)
    - grep "//260426 hbk Phase 14-02" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → ≥ 2 hits
    - grep -c "Math.Atan2(line1RowEnd" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 1 hit (게이트 phi1 계산)
    - grep "ValidateHorizontalVerticalAngles" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 기존 호출 보존 (3+ hits — TryTeachTwoLineIntersect 에는 추가 안 함)
  </acceptance_criteria>
  <done>
    빌드 PASS + 게이트 코드 삽입. SIMUL_MODE 동작 검증은 Task 3.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SIMUL_MODE 육안 검증 — TwoLineIntersect 각도 게이트</name>
  <what-built>
    DatumConfig 에 TwoLineAngleToleranceDeg PropertyGrid 필드 (default 10°) + DatumFindingService TryTeachTwoLineIntersect 끝부분 두 라인 각도 게이트 (90°±N°). N=0 시 게이트 off.
  </what-built>
  <how-to-verify>
    1. SIMUL_MODE 빌드 실행 (Debug/x64)
    2. TwoLineIntersect 알고리즘 Datum 노드 선택 → PropertyGrid 에 "TwoLineAngleToleranceDeg" 항목이 "Datum|Algorithm" 카테고리에 노출되는지 확인 (default 10.0)
    3. **PASS 시나리오:** 정상 90° 두 라인으로 티칭 → TryTeachDatum 성공 (LastTeachSucceeded=true) + RefOrigin 산출 확인
    4. **FAIL 시나리오 (out-of-range):** 두 라인을 60° 정도로 비스듬하게 ROI 설정 → TwoLineAngleToleranceDeg=10 유지 → btn_teachDatum 실행 → fail 라벨에 정확히 다음 형식 텍스트 포함 확인:
       `Two-line angle out of range: <측정값>.0 deg (expected 90 +/- 10.0 deg)` (소수점 1자리, ASCII)
       LastTeachSucceeded=false 확인 (오버레이 미갱신 또는 fail 라벨 표시).
    5. **OFF 시나리오:** TwoLineAngleToleranceDeg=0 변경 → 동일 비스듬한 두 라인 → btn_teachDatum → 게이트 통과 + 정상 PASS (LastTeachSucceeded=true).
    6. **INI 하위호환:** Phase 4/11/12/13 기존 INI 로드 → 신규 키 없어도 default 10° 자동 적용 → 정상 동작 확인 (회귀 0)
    7. 모두 PASS 시 "approved". 실패 항목 명시.
  </how-to-verify>
  <resume-signal>"approved" 또는 실패 단계 + 관찰된 동작 (예: "fail 라벨 형식 다름: 'Angle exceeded'")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 PropertyGrid 입력 → DatumConfig.TwoLineAngleToleranceDeg | double 필드 — 음수/극단값 입력 가능 |
| INI 파일 (사용자 편집 가능) → DatumConfig 필드 | ParamBase reflection 직렬화 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Tampering | TwoLineAngleToleranceDeg 음수 입력 → 게이트 항상 fail | accept | LOW — `> 0.0` 가드로 음수 시 게이트 off (PASS). 사용자 의도 가능. |
| T-14-02-02 | Tampering | TwoLineAngleToleranceDeg = 1e6 등 극단값 → 게이트 사실상 off | accept | LOW — 게이트 off 와 동일 효과, 안전. |
| T-14-02-03 | Denial of Service | Math.Atan2 무한 루프 가능성 | mitigate | `while (deltaDeg >= 180.0) deltaDeg -= 180.0;` — 최대 1-2 회 반복 (Atan2 출력 ±π → 0~360°). 무한 loop 불가능 (deltaDeg 단조 감소). |
</threat_model>

<verification>
- msbuild Debug/x64 exit 0
- 신규 warning 0
- Phase 12 CircleTwoHorizontal / VerticalTwoHorizontal 회귀 없음 (ValidateHorizontalVerticalAngles 호출 보존)
- INI 하위호환 (default 10° 자동 적용)
</verification>

<success_criteria>
- TwoLineAngleToleranceDeg PropertyGrid 노출 + 사용자 편집 가능
- 60° 두 라인 + N=10 → fail 라벨 표시 + LastTeachSucceeded=false (SIMUL_MODE PASS)
- N=0 → 게이트 off, 어떤 각도여도 PASS
- 90° 두 라인 → 정상 PASS (회귀 없음)
</success_criteria>

<output>
After completion, create `.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-02-SUMMARY.md`
</output>
