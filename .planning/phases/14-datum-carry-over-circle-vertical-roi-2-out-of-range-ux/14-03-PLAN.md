---
phase: 14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux
plan: 03
type: execute
wave: 2
depends_on: [14-02]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
autonomous: false
requirements: [SPEC-14-Req-3]
tags: [datum, vertical, propertygrid, ini-migration, halcon]

must_haves:
  truths:
    - "DatumConfig 에 Vertical 그룹 13 신규 필드 (5 geometry + 6 edge + 2 raw) 가 추가된다"
    - "VerticalTwoHorizontal 알고리즘 선택 시 PropertyGrid 에 Vertical 카테고리 6 edge 필드가 노출된다 (D-08 fallback: Category 라벨로 시각 구분)"
    - "기존 Phase 12/13 VerticalTwoHorizontal INI 로드 시 Vertical_* 값이 Line1_* 와 동일 값으로 자동 채워진다 (idempotent)"
    - "DatumFindingService.TryTeachVerticalTwoHorizontal 가 config.Line1_* 대신 config.Vertical_* 슬롯을 사용한다"
    - "MainView PublishDatumRoiCandidates / ApplyDatumRoiDelta / ClearDatumRoiFields 의 VerticalTwoHorizontal 케이스가 Datum.Vertical RoiId 를 발행/처리한다"
    - "HalconDisplayService RenderDatumOverlay 가 Vertical_DetectedEdge* raw 점을 orange 색으로 렌더한다"
    - "VerticalTwoHorizontal 티칭 후 PropertyGrid Vertical_EdgeThreshold 변경 → 자동 재티칭 발동 → 검출 라인 시각 변화 (NotifyDatumParamMaybeChanged 자동 라우팅)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "Vertical 그룹 13 필드 + EnsurePerRoiDefaults Vertical 마이그레이션 분기"
      contains: "Vertical_EdgeThreshold"
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "TryTeachVerticalTwoHorizontal — Line1_* → Vertical_* 슬롯 교체"
      contains: "config.Vertical_Row"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "PublishDatumRoiCandidates VerticalTwoHorizontal Datum.Vertical RoiId + ApplyDatumRoiDelta + ClearDatumRoiFields 분기"
      contains: "Datum.Vertical"
    - path: "WPF_Example/Halcon/Display/HalconDisplayService.cs"
      provides: "RenderDatumOverlay Vertical raw 점 orange 색"
      contains: "Vertical_DetectedEdgeRows"
  key_links:
    - from: "DatumConfig.Vertical_*"
      to: "TryTeachVerticalTwoHorizontal"
      via: "config.Vertical_Row/Col/Phi/Length1/Length2/EdgeThreshold/Sigma/EdgeDirection/EdgeSampleCount/EdgeTrimCount/EdgePolarity 직접 참조"
      pattern: "config\\.Vertical_"
    - from: "MainView.PublishDatumRoiCandidates VerticalTwoHorizontal case"
      to: "MainResultViewerControl _datumRoiCandidates"
      via: "Datum.Vertical RoiId"
      pattern: "Datum\\.Vertical"
    - from: "EnsurePerRoiDefaults"
      to: "Vertical_* sentinel-based copy from Line1_*"
      via: "if (Vertical_EdgeThreshold == 0) Vertical_EdgeThreshold = (Line1_EdgeThreshold > 0) ? Line1_EdgeThreshold : fbThreshold;"
      pattern: "Vertical_EdgeThreshold == 0"
---

<objective>
Phase 13 UAT Test 1 issue 2 carry-over: VerticalTwoHorizontal 알고리즘이 의미상 별도인 수직 ROI 를 Line1_* 슬롯에 재사용하여 사용자가 PropertyGrid 에서 "Line1_EdgeThreshold" 항목을 봐야 수직 ROI 파라미터를 튜닝하는 의미 혼동 해결.

D-07 알고리즘별 동적 가시성: **사용자 승인 (2026-04-26) 으로 D-08 fallback (3) Category-prefix labeling 즉시 채택** — Category 문자열에 알고리즘 태그 prefix 추가 ("Vertical (VTH)", "Line1 (TLI)" 등). PropertyTools 3.1.0 [Browsable] 런타임 토글은 본 phase 에서 시도하지 않음 (TypeDescriptor.AddAttributes / SelectedObject wrapper 패턴은 별도 phase 에서 재시도 가능 — 백로그). SPEC AC line 102 ("Vertical 카테고리 6 edge 필드 노출") 은 prefix-fallback 으로 충족 — 13 Vertical_* 필드가 "Vertical (VTH)" 카테고리 아래 항상 가시되며, AC 는 알고리즘별 조건부 숨김이 아닌 "필드 노출" 을 요구.

D-07 dynamic hide deferred per user approval 2026-04-26 — fallback (Category prefix labeling) adopted; dynamic [Browsable] toggling spiked to backlog.

D-05/D-06 INI 하위호환: `EnsurePerRoiDefaults` 양쪽 채움 정책 — Vertical_* sentinel 시 Line1_* 에서 1회 복사, Line1_* zero-out 안 함 (회귀 위험 0).

Purpose: VerticalTwoHorizontal 알고리즘의 의미적 분리 + 기존 INI 무손실 + 자동 재티칭 라우팅 보존. 14-02 완료 후 진행 (DatumConfig.cs 직렬 편집).

Output: DatumConfig 13 신규 필드 + DatumFindingService Line1_* → Vertical_* search/replace + MainView 4 분기 추가 + HalconDisplayService 1 줄 추가. SIMUL_MODE 육안 검증.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-SPEC.md
@.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-CONTEXT.md
@.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-PATTERNS.md
@.planning/phases/13-datum-algorithm-extensibility/13-04-SUMMARY.md
@.planning/phases/13-datum-algorithm-extensibility/13-05-SUMMARY.md
@.planning/phases/13-datum-algorithm-extensibility/13-06-SUMMARY.md
@CLAUDE.md

<interfaces>
From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs:
```csharp
// Line1 그룹 13 필드 — 신규 Vertical 그룹의 모델 (L78-109)
[Category("Datum|Line1 ROI")]
public double Line1_Row { get; set; } = 0;
public double Line1_Col { get; set; } = 0;
[PropertyTools.DataAnnotations.Browsable(false)]
public double Line1_Phi { get; set; } = 0;
public double Line1_PhiDeg { get { return Line1_Phi * 180.0 / System.Math.PI; } set { Line1_Phi = value * System.Math.PI / 180.0; } }
public double Line1_Length1 { get; set; } = 0;
public double Line1_Length2 { get; set; } = 0;

[Category("Datum|Line1 Edge")]
public int    Line1_EdgeThreshold   { get; set; } = 0;
public double Line1_Sigma           { get; set; } = 0;
[ItemsSourceProperty(nameof(Line1_EdgeDirectionList))]
public string Line1_EdgeDirection   { get; set; } = "";
public int    Line1_EdgeSampleCount { get; set; } = 0;
public int    Line1_EdgeTrimCount   { get; set; } = 0;
[ItemsSourceProperty(nameof(Line1_EdgePolarity))]
public string Line1_EdgePolarity    { get; set; } = "";

[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> Line1_EdgeDirectionList { get { return EdgeOptionLists.Directions; } }
[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> Line1_EdgePolarityList  { get { return EdgeOptionLists.DatumPolarities; } }

// Volatile HTuple raw 점 (Phase 13-05 패턴)
[PropertyTools.DataAnnotations.Browsable(false)]
public HTuple Line1_DetectedEdgeRows { get; set; }
[PropertyTools.DataAnnotations.Browsable(false)]
public HTuple Line1_DetectedEdgeCols { get; set; }

// EnsurePerRoiDefaults (L326-374) — 5 ROI sentinel-based migration 패턴
public void EnsurePerRoiDefaults();
```

From WPF_Example/Halcon/Algorithms/DatumFindingService.cs:
```csharp
// TryTeachVerticalTwoHorizontal (L468-637) — 현재 config.Line1_* 슬롯 재사용 (L486-503)
// 변경 대상: Line1_* → Vertical_* search/replace + 라벨 "Line1(vertical)" → "Vertical"
// raw 점 저장 (L604-607 부근): config.Line1_DetectedEdgeRows = vertRawRows; → Vertical_*
// Line1Detected_* (오버레이용 line begin/end 좌표) 는 Phase 14 scope 외 — 보존
```

From WPF_Example/UI/ContentItem/MainView.xaml.cs:
```csharp
// PublishDatumRoiCandidates (L697-728) — VerticalTwoHorizontal 케이스 L719-726
// ApplyDatumRoiDelta (L579-597) — switch 케이스 추가
// ClearDatumRoiFields (L601-630) — switch 케이스 추가
// Datum 티칭 step 함수 (별도 grep): GetFirstStep / GetNextStep / EDatumTeachStep
//   VerticalTwoHorizontal 의 첫 ROI 티칭 step 이 Datum.Line1 으로 set 되어 있을 수 있음 (조사 필요)
```

From WPF_Example/Halcon/Display/HalconDisplayService.cs:
```csharp
// RenderDatumOverlay (Allman) — LastTeachSucceeded 분기 안 raw 점 매핑 (L585-590)
RenderRawEdgePoints(window, datum.Line1_DetectedEdgeRows, datum.Line1_DetectedEdgeCols, "cyan");
RenderRawEdgePoints(window, datum.Line2_DetectedEdgeRows, datum.Line2_DetectedEdgeCols, "magenta");
RenderRawEdgePoints(window, datum.Circle_DetectedEdgeRows, datum.Circle_DetectedEdgeCols, "yellow");
RenderRawEdgePoints(window, datum.Horizontal_A_DetectedEdgeRows, datum.Horizontal_A_DetectedEdgeCols, "green");
RenderRawEdgePoints(window, datum.Horizontal_B_DetectedEdgeRows, datum.Horizontal_B_DetectedEdgeCols, "lime green");
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DatumConfig — Vertical 그룹 13 필드 신설 + EnsurePerRoiDefaults 마이그레이션 분기 + Category prefix 라벨</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (전체 — K&R / Line1 그룹 L78-109 / Volatile HTuple L298-321 / EnsurePerRoiDefaults L326-374)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-PATTERNS.md (3.1 + 3.2 + 3.6 sub-section — Category prefix fallback)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-SPEC.md (Requirement 3 Acceptance)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-CONTEXT.md (D-05 / D-06 / D-07 / D-08 결정 — Category prefix labeling 채택)
    - CLAUDE.md (//YYMMDD hbk + K&R + ParamBase reflection)
  </read_first>
  <action>
파일 스타일: **K&R**. 신규 코드 K&R.
주석: `//260426 hbk Phase 14-03 — <intent>`.

**A. Line1 그룹 (L78-109) 직후, Line2 그룹 시작 전 위치에 Vertical 그룹 13 필드 추가:**

```csharp
//260426 hbk Phase 14-03 Req 3 — Vertical ROI 그룹 (VerticalTwoHorizontal 전용 수직 에지 라인 — 의미상 별도)
//  D-08 Category prefix labeling — "(VTH)" 태그로 알고리즘 가시 구분 (PropertyGrid 동적 숨김 fallback)
[Category("Datum|Vertical (VTH) ROI")]
public double Vertical_Row { get; set; } = 0;
public double Vertical_Col { get; set; } = 0;
[PropertyTools.DataAnnotations.Browsable(false)]
public double Vertical_Phi { get; set; } = 0;
public double Vertical_PhiDeg
{
    get { return Vertical_Phi * 180.0 / System.Math.PI; }
    set { Vertical_Phi = value * System.Math.PI / 180.0; }
}
public double Vertical_Length1 { get; set; } = 0;
public double Vertical_Length2 { get; set; } = 0;

//260426 hbk Phase 14-03 Req 3 — Vertical ROI 전용 에지 파라미터 (per-ROI 독립 튜닝)
[Category("Datum|Vertical (VTH) Edge")]
public int    Vertical_EdgeThreshold   { get; set; } = 0;
public double Vertical_Sigma           { get; set; } = 0;
[ItemsSourceProperty(nameof(Vertical_EdgeDirectionList))]
public string Vertical_EdgeDirection   { get; set; } = "";
public int    Vertical_EdgeSampleCount { get; set; } = 0;
public int    Vertical_EdgeTrimCount   { get; set; } = 0;
[ItemsSourceProperty(nameof(Vertical_EdgePolarityList))]
public string Vertical_EdgePolarity    { get; set; } = "";

[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> Vertical_EdgeDirectionList { get { return EdgeOptionLists.Directions; } }
[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> Vertical_EdgePolarityList  { get { return EdgeOptionLists.DatumPolarities; } }
```

**B. Volatile HTuple raw 점 필드 (Line1_DetectedEdge* 인근, L298-321 부근) 에 Vertical 추가:**

```csharp
//260426 hbk Phase 14-03 Req 3 — Vertical raw 검출 에지점 (volatile, INI 영향 0 — Phase 13-05 D-VIZ-01 패턴)
[PropertyTools.DataAnnotations.Browsable(false)]
public HTuple Vertical_DetectedEdgeRows { get; set; }
[PropertyTools.DataAnnotations.Browsable(false)]
public HTuple Vertical_DetectedEdgeCols { get; set; }
```

**C. (D-08 fallback) 기존 Line1 / Line2 / Circle / Horizontal_A / Horizontal_B 카테고리에 알고리즘 태그 prefix 추가** — 사용자 정보량 감소 1차 목표 충족:
```csharp
// 기존:           [Category("Datum|Line1 ROI")]
// 변경:           [Category("Datum|Line1 (TLI) ROI")]
//
// 기존:           [Category("Datum|Line1 Edge")]
// 변경:           [Category("Datum|Line1 (TLI) Edge")]
//
// 동일 패턴:
//   Line2 → "Datum|Line2 (TLI) ROI" / "Datum|Line2 (TLI) Edge"
//   Circle → "Datum|Circle (CTH) ROI" / "Datum|Circle (CTH) Edge"
//   Horizontal_A → "Datum|Horizontal_A (CTH/VTH) ROI" / "Datum|Horizontal_A (CTH/VTH) Edge"
//   Horizontal_B → "Datum|Horizontal_B (CTH/VTH) ROI" / "Datum|Horizontal_B (CTH/VTH) Edge"
```
**중요:** Category 문자열만 변경 — INI 영향 0 (PropertyGrid 표시용만). PropertyTools.DataAnnotations.Browsable 토글 X.

**D. EnsurePerRoiDefaults 에 Vertical 마이그레이션 분기 추가** (L326-374 함수 안, 기존 5 ROI 분기 뒤):

```csharp
//260426 hbk Phase 14-03 D-05/D-06 — Vertical 그룹 INI 하위호환 마이그레이션
//  기존 INI 의 Line1_* 값을 Vertical_* sentinel(==0/"") 일 때 1회 복사. Line1_* 는 zero-out 안 함 (회귀 위험 0, 사용자가 알고리즘을 TwoLineIntersect 로 다시 바꿔도 Line1_* 즉시 사용 가능).
//  idempotent: 두 번째 호출 시 Vertical_* 가 모두 의미값 → 분기 미진입.
if (Vertical_EdgeThreshold == 0)   Vertical_EdgeThreshold   = (Line1_EdgeThreshold > 0)    ? Line1_EdgeThreshold    : fbThreshold;
if (Vertical_Sigma == 0)           Vertical_Sigma           = (Line1_Sigma > 0)            ? Line1_Sigma            : fbSigma;
if (string.IsNullOrEmpty(Vertical_EdgeDirection))   Vertical_EdgeDirection   = !string.IsNullOrEmpty(Line1_EdgeDirection)   ? Line1_EdgeDirection   : fbDirection;
if (Vertical_EdgeSampleCount == 0) Vertical_EdgeSampleCount = (Line1_EdgeSampleCount > 0)  ? Line1_EdgeSampleCount  : fbSampleCount;
if (Vertical_EdgeTrimCount == 0)   Vertical_EdgeTrimCount   = (Line1_EdgeTrimCount > 0)    ? Line1_EdgeTrimCount    : fbTrimCount;
if (string.IsNullOrEmpty(Vertical_EdgePolarity))    Vertical_EdgePolarity    = !string.IsNullOrEmpty(Line1_EdgePolarity)    ? Line1_EdgePolarity    : fbPolarity;

//260426 hbk Phase 14-03 D-05 — Geometry 1회 복사 (사용자가 Vertical 그룹만 보고 알고리즘 운용 가능하도록)
//   sentinel: Vertical_Length1 == 0
if (Vertical_Length1 == 0 && Line1_Length1 > 0) {
    Vertical_Row     = Line1_Row;
    Vertical_Col     = Line1_Col;
    Vertical_Phi     = Line1_Phi;
    Vertical_Length1 = Line1_Length1;
    Vertical_Length2 = Line1_Length2;
}
```

**중요:** 14-02 가 추가한 `TwoLineAngleToleranceDeg` 필드와 충돌 없음 (다른 카테고리, EnsurePerRoiDefaults 무관).
  </action>
  <verify>
    <automated>
&quot;C:\Program Files (x86)\Microsoft Visual Studio\2017\Professional\MSBuild\15.0\Bin\MSBuild.exe&quot; WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal
    </automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 exit 0, 0 errors, 신규 warning 0
    - W2 fix — exact-name greps for each declared Vertical_* field (loose `public.*Vertical_` would PASS with missing fields):
      - grep -c '^\s*public\s\+double\s\+Vertical_Row\s*{' WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs == 1
      - grep -c '^\s*public\s\+double\s\+Vertical_Col\s*{' WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs == 1
      - grep -c '^\s*public\s\+double\s\+Vertical_Phi\s*{' WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs == 1
      - grep -c '^\s*public\s\+double\s\+Vertical_Length1\s*{' WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs == 1
      - grep -c '^\s*public\s\+double\s\+Vertical_Length2\s*{' WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs == 1
      - grep -c '^\s*public\s\+int\s\+Vertical_EdgeThreshold\s*{' WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs == 1
      - grep -c '^\s*public\s\+double\s\+Vertical_Sigma\s*{' WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs == 1
      - grep -c '^\s*public\s\+string\s\+Vertical_EdgeDirection\s*{' WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs == 1
      - grep -c '^\s*public\s\+int\s\+Vertical_EdgeSampleCount\s*{' WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs == 1
      - grep -c '^\s*public\s\+int\s\+Vertical_EdgeTrimCount\s*{' WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs == 1
      - grep -c '^\s*public\s\+string\s\+Vertical_EdgePolarity\s*{' WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs == 1
      - grep -c 'Vertical_DetectedEdgeRows' WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs >= 1
      - grep -c 'Vertical_DetectedEdgeCols' WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs >= 1
      - (Note: types match existing Line1_* declarations — int for EdgeThreshold/EdgeSampleCount/EdgeTrimCount, double for Sigma, string for EdgeDirection/EdgePolarity. Verified against DatumConfig.cs:97-104.)
    - grep "Vertical_EdgeThreshold" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → ≥ 2 hits (선언 + EnsurePerRoiDefaults)
    - grep "Vertical_DetectedEdgeRows" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → 1 hit (선언)
    - grep "Vertical_DetectedEdgeCols" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → 1 hit (선언)
    - grep "Datum|Vertical (VTH)" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → ≥ 2 hits (ROI + Edge category)
    - grep "Datum|Line1 (TLI)" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → ≥ 2 hits (Category prefix 적용)
    - grep "Datum|Circle (CTH)" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → ≥ 1 hit
    - grep "Datum|Horizontal_A (CTH/VTH)" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → ≥ 1 hit
    - grep "//260426 hbk Phase 14-03" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → ≥ 4
    - grep "if (Vertical_EdgeThreshold == 0)" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs → 1 hit (마이그레이션 idempotency)
  </acceptance_criteria>
  <done>
    빌드 PASS + 13 신규 필드 + Category prefix 적용 + EnsurePerRoiDefaults Vertical 분기 추가.
  </done>
</task>

<task type="auto">
  <name>Task 2: DatumFindingService — TryTeachVerticalTwoHorizontal Line1_* → Vertical_* 슬롯 교체</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (Allman / TryTeachVerticalTwoHorizontal L468-637 본문 — config.Line1_Row/Col/Phi/Length1/Length2/Sigma/EdgeThreshold/EdgePolarity/EdgeDirection/EdgeSampleCount/EdgeTrimCount 참조 위치 / Line1_DetectedEdgeRows/Cols write-back 위치 / "Line1(vertical)" 진단 라벨)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-PATTERNS.md (3.3 sub-section)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-SPEC.md (Requirement 3)
    - CLAUDE.md (//YYMMDD hbk + Allman + try/catch HOperatorSet)
  </read_first>
  <action>
파일 스타일: **Allman**. 신규 코드 Allman.
주석: `//260426 hbk Phase 14-03 — Vertical_* 슬롯 (Line1_* 재사용 종료)`.

`TryTeachVerticalTwoHorizontal` 함수 (L468-637) 안의 수직 라인 피팅 블록 (L486-503 부근) 에서 `config.Line1_*` 12 참조를 `config.Vertical_*` 로 교체:

```csharp
//260426 hbk Phase 14-03 Req 3 — Vertical_* 슬롯 (의미적 분리, Line1_* 재사용 종료)
if (!TryFindLine(
        image, imageWidth, imageHeight,
        config.Vertical_Row, config.Vertical_Col, config.Vertical_Phi,
        config.Vertical_Length1, config.Vertical_Length2,
        config.Vertical_Sigma, config.Vertical_EdgeThreshold, config.Vertical_EdgePolarity,
        config.Vertical_EdgeDirection, config.Vertical_EdgeSampleCount, config.Vertical_EdgeTrimCount,
        out vrB, out vcB, out vrE, out vcE,
        out vertRawRows, out vertRawCols,
        out lineError,
        "Vertical")) //260426 hbk Phase 14-03 — 진단 로그 레이블 변경 ("Line1(vertical)" → "Vertical")
{
    config.LastTeachSucceeded = false;
    error = "Vertical line fit failed: " + lineError;
    return false;
}
```

수직 라인 raw 점 write-back (L604-607 부근):
```csharp
//260426 hbk Phase 14-03 Req 3 — 수직 라인 raw 점 Vertical_DetectedEdge* 에 기록 (Line1_DetectedEdge* 슬롯 종료)
config.Vertical_DetectedEdgeRows = vertRawRows;
config.Vertical_DetectedEdgeCols = vertRawCols;
```

**중요 보존:**
- `Line1Detected_RBegin/REnd/CBegin/CEnd` 등 line begin/end 좌표 (overlay 외삽용) 는 **그대로 유지** (Phase 14 scope 외, RenderDatumOverlay 의 검출 라인 외삽 그리기에 사용). PATTERNS.md 3.3 last paragraph 참조.
- `TryTeachTwoLineIntersect` (Line1_* 본래 사용) 는 무관 — 변경 없음.
- `TryTeachCircleTwoHorizontal` 도 변경 없음 (14-05 plan 영역).

**HOperatorSet 호출 패턴 보존** (TryFindLine 내부에서 처리, 이 함수 본문은 호출만).
  </action>
  <verify>
    <automated>
&quot;C:\Program Files (x86)\Microsoft Visual Studio\2017\Professional\MSBuild\15.0\Bin\MSBuild.exe&quot; WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal
    </automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 exit 0, 0 errors, 신규 warning 0
    - grep -c "config.Vertical_" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → ≥ 12 (TryTeachVerticalTwoHorizontal 내부 12 슬롯 교체 + write-back 2)
    - grep "config.Vertical_DetectedEdgeRows" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 1 hit
    - grep "config.Vertical_DetectedEdgeCols" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 1 hit
    - grep -A1 "TryFindLine" WPF_Example/Halcon/Algorithms/DatumFindingService.cs | grep "config.Vertical_Row" → 1 hit (수직 라인 호출에만 Vertical_*)
    - grep "\"Vertical\"" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → ≥ 1 hit (진단 라벨 변경 — "Line1(vertical)" 대체 확인)
    - grep "//260426 hbk Phase 14-03" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → ≥ 2
    - grep "Line1Detected_" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 기존 보존 (≥ 4 hits — overlay 외삽용 begin/end)
    - grep "config.Line1_Row" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → TryTeachTwoLineIntersect 의 Line1 본래 참조만 남음 (TryTeachVerticalTwoHorizontal 안 0 hit 검증)
  </acceptance_criteria>
  <done>
    빌드 PASS + Vertical_* 슬롯 교체 완료 + Line1Detected_* overlay 좌표 보존.
  </done>
</task>

<task type="auto">
  <name>Task 3: MainView — Datum.Vertical 분기 추가 (PublishDatumRoiCandidates / ApplyDatumRoiDelta / ClearDatumRoiFields / Datum 티칭 step)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (K&R / PublishDatumRoiCandidates L697-728 / ApplyDatumRoiDelta L579-597 / ClearDatumRoiFields L601-630 / EDatumTeachStep / GetFirstStep / GetNextStep / HalconViewer_DatumRectCompleted — VerticalTwoHorizontal 의 첫 ROI step 명명 위치 grep 으로 식별)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-PATTERNS.md (3.4 sub-section)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-SPEC.md (Requirement 3)
    - CLAUDE.md (//YYMMDD hbk + K&R)
  </read_first>
  <action>
파일 스타일: **K&R**. 신규 코드 K&R.
주석: `//260426 hbk Phase 14-03 — <intent>`.

**A. PublishDatumRoiCandidates VerticalTwoHorizontal 케이스 (L719-726):**
기존 `Datum.Line1` RoiId 발행을 `Datum.Vertical` 로 교체 + 필드 참조 Vertical_* 로:
```csharp
case EDatumAlgorithm.VerticalTwoHorizontal:
    //260426 hbk Phase 14-03 Req 3 — Line1_* → Vertical_* 슬롯 교체 (RoiId 도 Datum.Vertical 로)
    if (datum.Vertical_Length1 > 0 && datum.Vertical_Length2 > 0)
        list.Add(BuildDatumRectCandidate("Datum.Vertical", datum.Vertical_Row, datum.Vertical_Col, datum.Vertical_Length1, datum.Vertical_Length2));
    if (datum.Horizontal_A_Length1 > 0 && datum.Horizontal_A_Length2 > 0)
        list.Add(BuildDatumRectCandidate("Datum.HorizontalA", datum.Horizontal_A_Row, datum.Horizontal_A_Col, datum.Horizontal_A_Length1, datum.Horizontal_A_Length2));
    if (datum.Horizontal_B_Length1 > 0 && datum.Horizontal_B_Length2 > 0)
        list.Add(BuildDatumRectCandidate("Datum.HorizontalB", datum.Horizontal_B_Row, datum.Horizontal_B_Col, datum.Horizontal_B_Length1, datum.Horizontal_B_Length2));
    break;
```

**B. ApplyDatumRoiDelta switch (L579-597) 에 Datum.Vertical 케이스 추가:**
```csharp
case "Datum.Vertical":
    //260426 hbk Phase 14-03 Req 3 — Vertical ROI 이동 (Line1_* 슬롯 종료)
    datum.Vertical_Row += e.DeltaRow;
    datum.Vertical_Col += e.DeltaCol;
    break;
```

**C. ClearDatumRoiFields switch (L601-630) 에 Datum.Vertical 케이스 추가:**
```csharp
case "Datum.Vertical":
    //260426 hbk Phase 14-03 Req 3 — Vertical ROI 클리어
    datum.Vertical_Row = 0;
    datum.Vertical_Col = 0;
    datum.Vertical_Phi = 0;
    datum.Vertical_Length1 = 0;
    datum.Vertical_Length2 = 0;
    break;
```

**D. Datum 티칭 step 명명 — W4 fix: precheck + branched implementation**

**Step D.1 (precheck — MUST run first):** `EDatumTeachStep` enum 위치 식별 + Vertical member 존재 확인.
```bash
# 1) enum 정의 위치 grep (파일 위치 식별 — MainView.xaml.cs 또는 별도 파일)
grep -rn "enum EDatumTeachStep" WPF_Example/
# 2) 해당 파일에서 Vertical member 존재 확인
grep -c 'Vertical\s*[,=}]' <path-from-step-1>
```
다음 변수 set:
- `HAS_VERTICAL_STEP = true` (step 1 의 grep -c 결과 ≥ 1)
- `HAS_VERTICAL_STEP = false` (결과 == 0)

precheck 결과를 plan 진행 주석으로 한 줄 기록 (예: `//260426 hbk Phase 14-03 W4 precheck — EDatumTeachStep.Vertical: <true|false> in <enum-file>:Lxxx`).

---

**Step D.2 (branched implementation):**

**Branch A (HAS_VERTICAL_STEP = true):** 기존 case `EDatumTeachStep.Vertical` 의 `_editingDatum.Line1_*` write-back 을 `_editingDatum.Vertical_*` 로 단순 교체. enum / GetFirstStep / GetNextStep 변경 없음.
```csharp
//260426 hbk Phase 14-03 W4-A — 기존 EDatumTeachStep.Vertical case 의 Line1_* write-back 을 Vertical_* 로 교체
case EDatumTeachStep.Vertical:
    _editingDatum.Vertical_Row     = (rowBegin + rowEnd) / 2.0;
    _editingDatum.Vertical_Col     = (colBegin + colEnd) / 2.0;
    _editingDatum.Vertical_Length1 = Math.Abs(colEnd - colBegin) / 2.0;
    _editingDatum.Vertical_Length2 = Math.Abs(rowEnd - rowBegin) / 2.0;
    _editingDatum.Vertical_Phi     = 0;
    break;
```

**Branch B (HAS_VERTICAL_STEP = false):** enum 에 `Vertical` member 신규 추가 + 3 helper 함수 update + DatumRectCompleted case 신규 분기.
1. `EDatumTeachStep` enum 정의 파일에 `Vertical` member 추가 (Line1 직후 위치)
2. `GetFirstStep` 의 `case EDatumAlgorithm.VerticalTwoHorizontal: return EDatumTeachStep.Vertical;` 추가
3. `GetNextStep` 의 VerticalTwoHorizontal 체인: `Vertical → HorizontalA → HorizontalB → Done` 추가
4. `HalconViewer_DatumRectCompleted` 에 case `EDatumTeachStep.Vertical` 신규 분기 (Branch A 동일 코드)

좌표 계산 식 (rowBegin/rowEnd/colBegin/colEnd → Row/Col/Length1/Length2 매핑) 은 기존 Line1 case 와 정확히 동일 — 본문 grep 으로 식 확인 후 매핑.
  </action>
  <verify>
    <automated>
&quot;C:\Program Files (x86)\Microsoft Visual Studio\2017\Professional\MSBuild\15.0\Bin\MSBuild.exe&quot; WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal
    </automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 exit 0, 0 errors, 신규 warning 0
    - grep -c "Datum.Vertical" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 3 (PublishDatumRoiCandidates + ApplyDatumRoiDelta + ClearDatumRoiFields)
    - grep "datum.Vertical_Row" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 2 hits (PublishDatumRoiCandidates + ApplyDatumRoiDelta)
    - grep "datum.Vertical_Length1" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 1 hit
    - grep -c "//260426 hbk Phase 14-03" WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 3
    - W4 branched acceptance:
      - **Branch A (HAS_VERTICAL_STEP=true):**
        - grep -c 'case EDatumTeachStep.Vertical' WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 1 hit (기존 case 보존)
        - grep -c '_editingDatum.Vertical_' WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 4 hits (Row/Col/Length1/Length2 write-back, Line1_* 대체 완료)
        - enum 파일 변경 없음 (precheck 파일과 동일 — git diff 확인)
      - **Branch B (HAS_VERTICAL_STEP=false):**
        - grep -c 'Vertical' <enum-file-from-precheck> → ≥ 1 hit (신규 enum member 추가 확인)
        - grep -c 'case EDatumAlgorithm.VerticalTwoHorizontal' WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 2 hits (GetFirstStep + GetNextStep 분기 추가)
        - grep -c 'case EDatumTeachStep.Vertical' WPF_Example/UI/ContentItem/MainView.xaml.cs → 1 hit (HalconViewer_DatumRectCompleted 신규 case)
        - grep -c '_editingDatum.Vertical_' WPF_Example/UI/ContentItem/MainView.xaml.cs → ≥ 4 hits
    - precheck 결과 주석 (`//260426 hbk Phase 14-03 W4 precheck — EDatumTeachStep.Vertical: <true|false>`) 1 hit 존재 확인
  </acceptance_criteria>
  <done>
    빌드 PASS + Datum.Vertical RoiId 발행/처리/클리어 + 티칭 step 핸들러 Vertical 분기.
  </done>
</task>

<task type="auto">
  <name>Task 4: HalconDisplayService — RenderDatumOverlay Vertical raw 점 orange 색 매핑</name>
  <files>WPF_Example/Halcon/Display/HalconDisplayService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Display/HalconDisplayService.cs (Allman / RenderDatumOverlay LastTeachSucceeded 분기 L585-590 — 5 ROI raw 점 RenderRawEdgePoints 호출 패턴 / RenderRawEdgePoints L413-428 helper)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-PATTERNS.md (3.5 sub-section)
    - CLAUDE.md (//YYMMDD hbk + Allman + try/catch HOperatorSet)
  </read_first>
  <action>
파일 스타일: **Allman**. 신규 코드 Allman.
주석: `//260426 hbk Phase 14-03 — Vertical raw 점 (orange 신규)`.

`RenderDatumOverlay` 의 LastTeachSucceeded 분기 안 5 ROI RenderRawEdgePoints 호출 직후 (L590 부근) 한 줄 추가:

```csharp
//260426 hbk Phase 14-03 Req 3 — Vertical 그룹 raw 점 (Line1 cyan 과 시각 구분: orange 신규 — 미사용 색상)
RenderRawEdgePoints(window, datum.Vertical_DetectedEdgeRows, datum.Vertical_DetectedEdgeCols, "orange");
```

**색상 선택 근거:** `cyan`/`magenta`/`yellow`/`green`/`lime green` 모두 5 ROI 에서 사용 중 → 미사용 `orange` 채택. CONTEXT specifics "cyan 재사용 또는 신규 색" 두 옵션 중 시각 구분 우선 신규 색.

**RenderRawEdgePoints 함수 자체는 변경 없음** (null/length-0 가드가 이미 존재 — Vertical_DetectedEdgeRows 가 빈 HTuple 이어도 안전).
  </action>
  <verify>
    <automated>
&quot;C:\Program Files (x86)\Microsoft Visual Studio\2017\Professional\MSBuild\15.0\Bin\MSBuild.exe&quot; WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal
    </automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 exit 0, 0 errors, 신규 warning 0
    - grep "datum.Vertical_DetectedEdgeRows" WPF_Example/Halcon/Display/HalconDisplayService.cs → 1 hit (RenderRawEdgePoints 호출)
    - grep "\"orange\"" WPF_Example/Halcon/Display/HalconDisplayService.cs → ≥ 1 hit (Vertical 매핑)
    - grep "//260426 hbk Phase 14-03" WPF_Example/Halcon/Display/HalconDisplayService.cs → 1 hit
    - grep -c "RenderRawEdgePoints(window, datum\\." WPF_Example/Halcon/Display/HalconDisplayService.cs → ≥ 6 (기존 5 + 신규 Vertical 1)
  </acceptance_criteria>
  <done>
    빌드 PASS + Vertical raw 점 orange 매핑 추가. SIMUL_MODE 시각 검증은 Task 5.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: SIMUL_MODE 육안 검증 — Vertical 그룹 + INI 마이그레이션 + 자동 재티칭</name>
  <what-built>
    DatumConfig Vertical 그룹 13 신규 필드 + Category prefix 알고리즘 태그 ("Vertical (VTH)" 등) + EnsurePerRoiDefaults 마이그레이션 (Line1_* → Vertical_* sentinel-based 1회 복사) + DatumFindingService.TryTeachVerticalTwoHorizontal Vertical_* 슬롯 교체 + MainView Datum.Vertical RoiId 분기 + HalconDisplayService Vertical raw 점 orange 매핑.
  </what-built>
  <how-to-verify>
    1. SIMUL_MODE 빌드 실행 (Debug/x64)
    2. **PropertyGrid Category 검증:** Datum 노드 선택 → PropertyGrid 에 다음 카테고리 노출 확인:
       - Datum|Algorithm
       - Datum|Line1 (TLI) ROI / Datum|Line1 (TLI) Edge
       - Datum|Line2 (TLI) ROI / Datum|Line2 (TLI) Edge
       - Datum|Circle (CTH) ROI / Datum|Circle (CTH) Edge
       - Datum|Horizontal_A (CTH/VTH) ROI / Datum|Horizontal_A (CTH/VTH) Edge
       - Datum|Horizontal_B (CTH/VTH) ROI / Datum|Horizontal_B (CTH/VTH) Edge
       - Datum|Vertical (VTH) ROI / Datum|Vertical (VTH) Edge ← 신규 6 edge 필드 노출 확인
    3. **INI 하위호환:** 기존 Phase 12/13 VerticalTwoHorizontal INI 레시피 로드 → DatumConfig Vertical_EdgeThreshold/Sigma/EdgeDirection/EdgeSampleCount/EdgeTrimCount/EdgePolarity 가 Line1_* 와 동일 값으로 자동 채워짐 확인 (PropertyGrid 표시 확인). Vertical_Row/Col/Length1/Length2 도 Line1_* 와 동일.
    4. **VerticalTwoHorizontal 티칭:** 알고리즘 VerticalTwoHorizontal 선택 → btn_teachDatum → Vertical / HorizontalA / HorizontalB 3 ROI 차례로 티칭 → 자동 InvokeTryTeachDatum → 검출 라인 + raw 점 (Vertical=orange / HorizA=green / HorizB=lime green) 캔버스 표시 확인
    5. **자동 재티칭 wiring:** PropertyGrid 에서 Vertical_EdgeThreshold 값 변경 (예: 30 → 50) → 자동 재티칭 발동 → 검출 라인 시각 변화 확인 (NotifyDatumParamMaybeChanged 라우팅 — Phase 13-06+13-07 패턴)
    6. **Vertical ROI 이동:** Vertical ROI 드래그 이동 → Datum.Vertical 분기 통과 → 자동 재티칭 → 새 위치 검출 라인 표시 확인
    7. **회귀 없음:** TwoLineIntersect 알고리즘 전환 → Line1/Line2 ROI 정상 티칭 → Phase 13 동작 보존 확인. INI 저장 후 앱 재시작 → 기존 레시피 로드 무오류 확인.
    8. 모두 PASS 시 "approved". 실패 항목 명시.
  </how-to-verify>
  <resume-signal>"approved" 또는 실패 단계 + 관찰된 동작</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INI 사용자 편집 → DatumConfig.Vertical_* 필드 | Phase 12/13 INI 레시피 사용자가 수동 편집 가능 — 음수/극단값 입력 가능 |
| PropertyGrid 사용자 입력 → Vertical_EdgeThreshold/Sigma | sentinel 0 충돌 가능성 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Tampering | Vertical_EdgeThreshold = 0 sentinel 입력 → EnsurePerRoiDefaults 가 fallback 으로 덮어씀 | accept | LOW — 사용자 의도된 0 입력은 PropertyGrid 가 즉시 fallback 으로 교체 (다음 INI 로드 시). 사용자 0 입력 의도 = 의미 없음 (threshold 0 은 invalid). 안전. |
| T-14-03-02 | Tampering | Vertical_Phi 비-라디안 입력 (PhiDeg 미경유) | mitigate | PhiDeg wrapper 가 자동 변환 — PropertyGrid 사용자는 PhiDeg 사용. INI 직접 편집은 자기 책임. |
| T-14-03-03 | Information Disclosure | INI 양쪽 Line1_/Vertical_ 동시 저장 → 디스크 사용량 증가 | accept | LOW — 13 필드 × 16 bytes = 208 bytes 추가, 데스크탑 환경 무영향. |
</threat_model>

<verification>
- msbuild Debug/x64 exit 0
- 신규 warning 0
- Phase 12 TwoLineIntersect / CircleTwoHorizontal 회귀 없음 (Line1_* 본래 사용 보존, Circle 경로 무변경)
- INI 하위호환 (Phase 4/11/12/13 기존 레시피 로드 무오류)
- Phase 13-06+13-07 자동 재티칭 wiring 보존 (Vertical_* 필드도 NotifyDatumParamMaybeChanged 가 자동 라우팅)
</verification>

<success_criteria>
- DatumConfig 13 신규 Vertical 필드 (5 geometry + 6 edge + 2 raw)
- PropertyGrid Vertical 카테고리 노출 + 기존 6 카테고리에 알고리즘 prefix 라벨 (TLI/CTH/VTH)
- 기존 INI 자동 마이그레이션 (Line1_* → Vertical_* sentinel 시 1회 복사)
- TryTeachVerticalTwoHorizontal Vertical_* 슬롯 사용
- MainView Datum.Vertical 4 분기 정상 동작
- HalconDisplayService Vertical raw 점 orange 색
- 자동 재티칭 wiring 보존 (SIMUL_MODE PASS)
</success_criteria>

<output>
After completion, create `.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-03-SUMMARY.md`
</output>
