---
phase: 14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux
plan: 05
type: execute
wave: 4
depends_on: [14-03, 14-04]
files_modified:
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
autonomous: false
requirements: [SPEC-14-Req-5]
tags: [datum, integration, circle-two-horizontal, vertical-two-horizontal, verify, btn-test-find-datum]

must_haves:
  truths:
    - "DatumFindingService.TryTeachCircleTwoHorizontal 의 Circle 검출 호출이 legacy TryFindCircle 에서 신규 TryFindCircleByPolarSampling 으로 교체된다"
    - "교체 후 Circle ROI 의 raw 에지점이 DatumConfig.Circle_DetectedEdgeRows/Cols 에 직접 채워진다 (13-05 D-VIZ-03 carry-over closure)"
    - "btn_testFindDatum 으로 3 알고리즘 (TwoLineIntersect / CircleTwoHorizontal / VerticalTwoHorizontal) 모두에서 검출이 정상 시행된다"
    - "TwoLineIntersect: LimeGreen 'TryFind OK — RefOrigin=(...), Angle=... rad' + 주황 십자 (회귀 없음)"
    - "CircleTwoHorizontal: LimeGreen RefOrigin + CircleCenter + Radius 라벨 + 주황 십자 + Circle raw 점 36개 (step=10°)"
    - "VerticalTwoHorizontal: LimeGreen RefOrigin + 주황 십자 + Vertical/HorizA/HorizB raw 점 (Vertical=orange / HorizA=green / HorizB=lime green)"
    - "FAI 측정 경로 회귀 없음 (CircleDiameterMeasurement 포함, Phase 13 Test 8 시나리오 PASS 유지)"
  artifacts:
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "TryTeachCircleTwoHorizontal Circle 검출 호출 교체 + raw 점 직접 write-back"
      contains: "TryFindCircleByPolarSampling"
  key_links:
    - from: "TryTeachCircleTwoHorizontal"
      to: "VisionAlgorithmService.TryFindCircleByPolarSampling"
      via: "config.Circle_PolarStepDeg / Circle_RectL1Ratio / Circle_RectL2Ratio 전달"
      pattern: "TryFindCircleByPolarSampling"
    - from: "VisionAlgorithmService.TryFindCircleByPolarSampling out HTuple edgeRows/edgeCols"
      to: "config.Circle_DetectedEdgeRows / Circle_DetectedEdgeCols"
      via: "직접 대입 (13-05 빈 HTuple closure)"
      pattern: "config.Circle_DetectedEdgeRows = circleEdgeRows"
---

<objective>
Phase 13 UAT Test 3 + D-VIZ-03 carry-over closure: btn_testFindDatum 으로 3 알고리즘 (TwoLineIntersect / CircleTwoHorizontal / VerticalTwoHorizontal) 모두에서 검출이 정상 시행되도록 통합 검증.

핵심 변경:
- TryTeachCircleTwoHorizontal 의 Circle 검출 호출을 legacy `TryFindCircle` (raw 점 미반환) → 신규 `TryFindCircleByPolarSampling` (14-04 추가, raw 점 반환) 로 교체
- Circle raw 점 write-back: `config.Circle_DetectedEdgeRows/Cols = circleEdgeRows/Cols` (이전엔 빈 HTuple)
- SIMUL_MODE btn_testFindDatum 으로 3 알고리즘 PASS 검증

D-10 단순 verify 우선 / D-11 contingency: PASS path 가 default. FAIL 시 진단 로그 추가 → 결함 식별 → fix → 재 retest.

Purpose: 14-01 (Circle ROI UX) + 14-02 (TwoLine 게이트) + 14-03 (Vertical 그룹) + 14-04 (Polar Circle) 통합 후 사용자 시점에서 Datum 3 알고리즘 모두 운영 가능 상태 확정.

Output: DatumFindingService 1 파일 변경 + SIMUL_MODE UAT 3 시나리오 PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-SPEC.md
@.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-CONTEXT.md
@.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-PATTERNS.md
@.planning/phases/13-datum-algorithm-extensibility/13-05-SUMMARY.md
@.planning/phases/13-datum-algorithm-extensibility/13-UAT.md
@CLAUDE.md

<interfaces>
From WPF_Example/Halcon/Algorithms/DatumFindingService.cs:
```csharp
// TryTeachCircleTwoHorizontal (L292-464) — 현재 Circle 검출 호출 (L306-321)
//   visionSvc.TryFindCircle(image, CircleROI_Row, CircleROI_Col, CircleROI_Radius, null,
//                           Circle_Sigma, Circle_EdgeThreshold, Circle_EdgePolarity,
//                           out centerRow, out centerCol, out radius, out circleError)
//   → 교체 대상: TryFindCircleByPolarSampling (14-04 신규)
//
// 현재 Circle raw 점 write-back (L329-330 부근):
//   config.Circle_DetectedEdgeRows = new HTuple();   // 빈 — D-VIZ-03 carry-over
//   config.Circle_DetectedEdgeCols = new HTuple();
//
// 에러 literal 보존: "Circle fit failed: " + circleError (Phase 12 D-14 SPEC AC literal Req 5c)
```

From WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs (after 14-04):
```csharp
public bool TryFindCircleByPolarSampling(
    HImage image,
    double centerRow, double centerCol, double radius,
    double stepDeg, double rectL1Ratio, double rectL2Ratio,
    double sigma, int threshold, string polarity,
    HTuple datumTransform,
    out double foundRow, out double foundCol, out double foundRadius,
    out HTuple edgeRows, out HTuple edgeCols,
    out string error);
```

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (after 14-03 + 14-04):
```csharp
public double Circle_PolarStepDeg  { get; set; } = 10.0;
public double Circle_RectL1Ratio   { get; set; } = 0.05;
public double Circle_RectL2Ratio   { get; set; } = 0.05;
public double Circle_Sigma         { get; set; }   // existing per-ROI
public int    Circle_EdgeThreshold { get; set; }   // existing per-ROI
public string Circle_EdgePolarity  { get; set; }   // existing per-ROI
public HTuple Circle_DetectedEdgeRows { get; set; }  // existing volatile (Phase 13-05)
public HTuple Circle_DetectedEdgeCols { get; set; }  // existing volatile
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TryTeachCircleTwoHorizontal — Circle 검출 호출 교체 (TryFindCircle → TryFindCircleByPolarSampling) + raw 점 write-back</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (Allman / TryTeachCircleTwoHorizontal L292-464 본문 — Circle 검출 호출 위치 L306-321 / Circle raw 점 write-back L329-330 / "Circle fit failed: " 에러 literal / 후속 수평 ROI 처리 로직 보존)
    - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs (14-04 적용 후 TryFindCircleByPolarSampling 시그니처 정확 확인)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (14-03+14-04 적용 후 Circle_PolarStepDeg/RectL1Ratio/RectL2Ratio 필드 + 기존 Circle_Sigma/EdgeThreshold/EdgePolarity 필드 확인)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-PATTERNS.md (5.1 sub-section)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-SPEC.md (Requirement 5 Acceptance)
    - .planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-CONTEXT.md (D-10 PASS path 우선 / D-11 FAIL contingency)
    - CLAUDE.md (//YYMMDD hbk + Allman + try/catch HOperatorSet)
  </read_first>
  <action>
파일 스타일: **Allman**. 신규 코드 Allman.
주석: `//260426 hbk Phase 14-05 — <intent>`.

`TryTeachCircleTwoHorizontal` 함수 (L292-464) 안의 Circle 검출 블록 (L306-330 부근) 을 다음으로 교체:

```csharp
//260426 hbk Phase 14-05 D-10 — TryFindCircle → TryFindCircleByPolarSampling 교체 (raw 점 반환 + 360° 분포)
var visionSvc = new VisionAlgorithmService();
double centerRow, centerCol, radius;
HTuple circleEdgeRows, circleEdgeCols;
string circleError;
//260426 hbk Phase 14-05 — Circle per-ROI 에지 파라미터 + Polar sampling 파라미터 (14-04 신규 3 필드)
if (!visionSvc.TryFindCircleByPolarSampling(
        image,
        config.CircleROI_Row, config.CircleROI_Col, config.CircleROI_Radius,
        config.Circle_PolarStepDeg, config.Circle_RectL1Ratio, config.Circle_RectL2Ratio,
        config.Circle_Sigma, config.Circle_EdgeThreshold, config.Circle_EdgePolarity,
        null, // teaching-phase identity transform (legacy 동일)
        out centerRow, out centerCol, out radius,
        out circleEdgeRows, out circleEdgeCols,
        out circleError))
{
    config.LastTeachSucceeded = false;
    //260423 hbk Phase 12 D-14 SPEC AC literal (Req 5c) 보존 — error 포맷 변경 없음 (회귀 0)
    error = "Circle fit failed: " + circleError;
    return false;
}

config.CircleCenter_Row      = centerRow;
config.CircleCenter_Col      = centerCol;
config.CircleDetected_Radius = radius;
//260426 hbk Phase 14-05 — Phase 13-05 D-VIZ-03 carry-over closure: raw 점 직접 반환 (이전엔 빈 HTuple)
config.Circle_DetectedEdgeRows = circleEdgeRows;
config.Circle_DetectedEdgeCols = circleEdgeCols;
```

**보존 검증:**
- Circle 검출 이후의 수평 라인 검출 / intersection / ValidateHorizontalVerticalAngles 호출 / RefOrigin 산출 등 후속 로직은 **변경 없음** (D-10 단순 교체).
- `"Circle fit failed: " + circleError` literal 보존 — Phase 12 D-14 SPEC AC literal (Req 5c) 회귀 차단.
- `null` (teaching-phase identity transform) 전달 — legacy 호출과 동일.
- TryTeachVerticalTwoHorizontal 는 이 plan 에서 변경 없음 (14-03 가 이미 Vertical_* 슬롯 교체 완료).

**진단 로깅 (D-11 FAIL contingency 대비) — 진입부에 추가 (PASS 시 제거 가능):**
```csharp
//260426 hbk Phase 14-05 D-11 — FAIL contingency 진단: 호출 전후 변수 로깅 (Task 2 PASS 시 주석 처리)
Logging.PrintLog((int)ELogType.Trace,
    "TryTeachCircleTwoHorizontal: ROI=(" + config.CircleROI_Row + "," + config.CircleROI_Col + ",r=" + config.CircleROI_Radius + ") " +
    "polar(step=" + config.Circle_PolarStepDeg + " L1=" + config.Circle_RectL1Ratio + " L2=" + config.Circle_RectL2Ratio + ")");
```

**HOperatorSet 호출 변경 없음** (Wrapper 메서드 호출만). 외곽 함수의 try/catch 보존.
  </action>
  <verify>
    <automated>
&quot;C:\Program Files (x86)\Microsoft Visual Studio\2017\Professional\MSBuild\15.0\Bin\MSBuild.exe&quot; WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal
    </automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 exit 0, 0 errors, 신규 warning 0
    - grep "TryFindCircleByPolarSampling" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 1 hit (TryTeachCircleTwoHorizontal 안)
    - grep "config.Circle_PolarStepDeg" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 1 hit (호출 인자)
    - grep "config.Circle_RectL1Ratio" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 1 hit
    - grep "config.Circle_RectL2Ratio" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 1 hit
    - grep "config.Circle_DetectedEdgeRows = circleEdgeRows" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 1 hit (raw 점 직접 write-back)
    - grep "Circle fit failed: " WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 1 hit (literal 보존)
    - grep "//260426 hbk Phase 14-05" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → ≥ 3
    - grep "config.Circle_DetectedEdgeRows = new HTuple()" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 0 hits (이전 빈 HTuple 코드 제거)
    - grep "visionSvc.TryFindCircle\b" WPF_Example/Halcon/Algorithms/DatumFindingService.cs → 0 hits (TryTeachCircleTwoHorizontal 안 — legacy 호출 제거 확인). 다른 함수에 legacy 호출이 있다면 그건 보존 OK.
  </acceptance_criteria>
  <done>
    빌드 PASS + Circle 검출 교체 완료 + raw 점 직접 write-back. SIMUL_MODE UAT 는 Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SIMUL_MODE btn_testFindDatum 3 알고리즘 통합 UAT (D-10 PASS path 우선)</name>
  <what-built>
    14-01..14-04 통합 + TryTeachCircleTwoHorizontal Circle 검출 폴라샘플링 교체. 사용자 시점에서 btn_testFindDatum 으로 3 알고리즘 (TwoLineIntersect / CircleTwoHorizontal / VerticalTwoHorizontal) 모두 정상 시행 + 검출 결과 시각화 검증.
  </what-built>
  <how-to-verify>
    SIMUL_MODE 빌드 실행 (Debug/x64). 3 알고리즘 차례로 검증.

    **시나리오 1 — TwoLineIntersect (회귀 검증):**
    1. 알고리즘 TwoLineIntersect 선택
    2. btn_teachDatum → Line1 / Line2 ROI 정상 티칭
    3. btn_testFindDatum 클릭
    4. **AC 12 검증:** LimeGreen 텍스트 `"TryFind OK — RefOrigin=(...), Angle=... rad"` 표시 + 주황 십자 RefOrigin 위치 표시 → PASS
    5. (14-02 회귀 검증) Line1/Line2 각도 비스듬하게 조정 + TwoLineAngleToleranceDeg=10 → btn_teachDatum → fail 라벨 표시 → 회귀 없음 확인

    **시나리오 2 — CircleTwoHorizontal:**
    1. 알고리즘 CircleTwoHorizontal 선택
    2. btn_teachDatum → Circle / HorizontalA / HorizontalB ROI 정상 티칭
    3. btn_testFindDatum 클릭
    4. **AC 13 검증:**
       - LimeGreen 텍스트 `"TryFind OK — RefOrigin=(...), Angle=... rad"` + CircleCenter + Radius 라벨 표시
       - 주황 십자 RefOrigin 위치
       - Circle ROI 위에 노란 raw 에지점 36 개 (step=10° default) 가 360° 분포로 표시 (D-VIZ-03 carry-over closure 검증)
    5. **AC 9 검증:** Circle_PolarStepDeg=1 변경 → 자동 재티칭 → raw 점 360 개 표시 (시각적으로 거의 연속 원)
    6. **AC 10 검증:** 폴라샘플링 center 가 기존 알고리즘 center 와 ±2px 일치 — 동일 이미지에서 알고리즘 선택을 (임시 진단) 양쪽으로 비교, 또는 ground truth 가 알려진 합성 이미지 사용

    **시나리오 3 — VerticalTwoHorizontal:**
    1. 알고리즘 VerticalTwoHorizontal 선택
    2. btn_teachDatum → Vertical / HorizontalA / HorizontalB ROI 정상 티칭
    3. btn_testFindDatum 클릭
    4. **AC 14 검증:**
       - LimeGreen 텍스트 `"TryFind OK — RefOrigin=(...), Angle=... rad"` + 주황 십자
       - Vertical raw 점 (orange) + HorizontalA raw 점 (green) + HorizontalB raw 점 (lime green) 표시
    5. (14-03 회귀 검증) Vertical_EdgeThreshold 변경 → 자동 재티칭 → 검출 라인 시각 변화

    **회귀 검증 — FAI 측정 경로:**
    1. CircleDiameterMeasurement 가 포함된 FAI 측정 실행
    2. Phase 13 Test 8 시나리오 (CircleDiameterMeasurement 측정 PASS) 유지 확인 — legacy `TryFindCircle` 시그니처 보존이 유효한지 검증
    3. INI 저장 후 재시작 → 모든 레시피 로드 무오류

    **D-11 FAIL contingency:**
    어느 시나리오라도 FAIL 시 → Task 1 의 진단 로깅으로 결함 식별 → 결함 specific fix:
    - Phi 와이어링 결함 → DatumFindingService TryTeachCircleTwoHorizontal 에서 GenContourPolygonXld 결과 좌표 시각화 추가
    - per-ROI 파라미터 누락 → 14-03 의 슬롯 교체 검증 (Vertical_*) 또는 14-04 의 sanity clamp 검증
    - strip-loop 오적용 → 14-04 의 stepCount 계산 / 누적 로직 디버깅
    - 결함 fix 후 재 retest → PASS 까지 반복

    모두 PASS 시 "approved + 3 algorithms verified". FAIL 시 시나리오 + 결함 + 추가 fix 제안.
  </how-to-verify>
  <resume-signal>"approved + 3 algorithms verified" 또는 FAIL 시나리오 + 결함 명세 + 추가 plan stub 제안 (D-11 contingency)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 14-04 신규 메서드 → Plan 14-05 호출처 | 14-04 sanity clamp 가 모든 비정상 입력 방어. 신뢰 경계 통과. |
| 사용자 PropertyGrid → Circle_PolarStepDeg/RectL1Ratio/RectL2Ratio | 14-04 가 처리 (음수/0/극단값 clamp) — 14-05 는 단순 전달 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05-01 | Tampering | TryFindCircleByPolarSampling 호출 인자가 잘못된 순서 (DatumConfig 필드 ↔ 메서드 시그니처 mismatch) | mitigate | Task 1 acceptance criteria 의 grep 검증 (config.Circle_PolarStepDeg/RectL1Ratio/RectL2Ratio 인자 순서 = 메서드 시그니처 순서) + msbuild 컴파일 에러로 catch |
| T-14-05-02 | Repudiation | btn_testFindDatum FAIL 시 사용자가 어느 step 실패인지 모름 | mitigate | Task 1 진단 로깅 (TryTeachCircleTwoHorizontal 진입 시 ROI + polar 파라미터 trace) |
| T-14-05-03 | Information Disclosure | error literal "Circle fit failed: " + ex.Message 노출 | accept | LOW — Phase 12 D-14 보존 요구. 디버깅 정보 필요. 데스크탑 단일 사용자. |
</threat_model>

<verification>
- msbuild Debug/x64 exit 0
- 신규 warning 0
- legacy TryFindCircle 보존 (CircleDiameterMeasurement.cs:49 호환 — Phase 13 Test 8 회귀 0)
- "Circle fit failed: " 에러 literal 보존 (Phase 12 D-14 SPEC AC literal Req 5c)
- 3 알고리즘 SIMUL_MODE PASS (Task 2)
- INI 하위호환 (Phase 4/11/12/13 기존 레시피 로드 무오류)
</verification>

<success_criteria>
- TryTeachCircleTwoHorizontal Circle 검출 호출 교체 완료
- Circle raw 점 직접 write-back (Phase 13 D-VIZ-03 carry-over closure)
- btn_testFindDatum: TwoLineIntersect / CircleTwoHorizontal / VerticalTwoHorizontal 3 알고리즘 모두 PASS
- FAI 측정 경로 회귀 없음
- Phase 14 5 sub-phase 통합 완료
</success_criteria>

<output>
After completion, create `.planning/phases/14-datum-carry-over-circle-vertical-roi-2-out-of-range-ux/14-05-SUMMARY.md`
</output>
