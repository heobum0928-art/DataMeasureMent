---
phase: 15
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
autonomous: true
requirements:
  - PHASE15-Req-MeasurePhi-Mapping
  - PHASE15-Req-EdgeSelection-Field
must_haves:
  truths:
    - "AppendEdgePointsFromStrip 시그니처에 string direction, string selection 두 파라미터가 추가되어 있다"
    - "AppendEdgePointsFromStrip 내부에서 direction → measurePhi 4-way 매핑을 수행한다 (TtoB=-π/2, BtoT=+π/2, RtoL=π, LtoR=0)"
    - "GenMeasureRectangle2 가 SmallestRectangle2 의 자동 도출 rp 대신 명시 측정한 measurePhi 를 사용한다"
    - "MeasurePos 가 \"all\" 하드코딩 대신 인자로 받은 selection 변수를 사용한다 (대소문자 lower 변환)"
    - "기존 \"SmallestRectangle2 가 strip Phi 자동 도출 → manual effectivePhi 불필요\" 잘못된 정당화 주석이 제거되어 있다"
    - "Trace 로그에 measurePhi (degrees) 와 selection 정보가 포함되어 있다"
    - "TryFindLine 시그니처에 string selection 파라미터가 추가되어 있다"
    - "TryExtractEdgePoints 시그니처에 string selection 파라미터가 추가되어 있다"
    - "두 함수 모두 AppendEdgePointsFromStrip 호출시 direction + selection + roiLabel 4 인자 모두 전달한다"
    - "DatumFindingService 의 7개 호출 사이트(Line1/Line2/Vertical/Horizontal_A×2/Horizontal_B×2)가 config.<ROI>_EdgeSelection 을 전달한다"
    - "TwoLineIntersect / CircleTwoHorizontal / VerticalTwoHorizontal 3 알고리즘 모두 컴파일 PASS — 통합 빌드 PASS"
  artifacts:
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "AppendEdgePointsFromStrip(direction, selection, roiLabel) + TryFindLine(selection) + TryExtractEdgePoints(selection) + 7 호출 사이트 wiring"
      contains: "_EdgeSelection"
  key_links:
    - from: "AppendEdgePointsFromStrip"
      to: "HOperatorSet.GenMeasureRectangle2"
      via: "measurePhi (인자)"
      pattern: "GenMeasureRectangle2\\([^)]*measurePhi"
    - from: "AppendEdgePointsFromStrip"
      to: "HOperatorSet.MeasurePos"
      via: "selectionLower (인자)"
      pattern: "MeasurePos\\([^)]*selectionLower"
    - from: "TryFindLine / TryExtractEdgePoints"
      to: "AppendEdgePointsFromStrip"
      via: "selection + direction + roiLabel 인자 전달"
      pattern: "AppendEdgePointsFromStrip\\([^)]*direction[^)]*selection"
    - from: "DatumFindingService 7 호출 사이트"
      to: "DatumConfig.*_EdgeSelection"
      via: "config.<ROI>_EdgeSelection 인자 전달"
      pattern: "config\\.[A-Za-z_]+_EdgeSelection"
---

<objective>
DatumFindingService 의 핵심 helper `AppendEdgePointsFromStrip` 을 두 가지 측면에서 캐노니컬 패턴(MeasurementAlgorithm.cs:130-178, FAIEdgeMeasurementService.cs:82-106)과 정합화하고, **동일 wave 안에서** 그 변경 시그니처를 호출자(`TryFindLine`, `TryExtractEdgePoints`) 두 함수와 7 개 caller 사이트(`TryTeachTwoLineIntersect` × 2, `TryTeachCircleTwoHorizontal` × 2, `TryTeachVerticalTwoHorizontal` × 3)에 전파하여 **빌드를 깨지 않은 채** 단대단(end-to-end) 데이터 흐름을 완성한다.

1. **EdgeDirection → measurePhi 명시 매핑**: 현재 `SmallestRectangle2` 가 도출하는 자동 phi(`rp`)는 strip bounding box 의 기하학적 도출일 뿐 사용자의 BtoT vs TtoB 부호 의도를 구분 못 한다. 4-way switch 로 명시 매핑하여 polarity 의미 일관성 회복.
2. **selection 인자화**: `MeasurePos` 의 `"all"` 하드코딩 제거 → `*_EdgeSelection` (Plan 15-01 산출) 가 helper 까지 전파.
3. **7 caller wiring**: 6 ROI 의 사용자가 PropertyGrid 에서 선택한 EdgeSelection 이 HALCON MeasurePos 까지 도달.

> **Merge 노트 (revision iteration 1):** 본 plan 은 원래 분리되어 있던 15-02 (helper signature change, autonomous=true 였으나 빌드 깨짐 의도) + 15-03 (7 caller wiring) 를 단일 wave-2 atomic 단위로 병합. 이유: 15-02 단독 적용시 빌드 깨짐 → autonomous 의미 위배 → 두 plan 분리는 인공적이고 회복력 저하. 병합으로 빌드 회복이 본 plan 산출물의 일부가 됨. (Checker Blocker 2 option a 적용.)

> **로그 강화 노트 (Warning 3):** 본 plan 은 Trace 로그용 `roiLabel` 파라미터를 추가한다 — CONTEXT 의 "Claude's Discretion: Trace 로그 보강 여부" 조항 행사 (CONTEXT decisions 에는 direction/selection 만 명시되었으나 로그 보강은 discretion 영역).

Purpose: 실 데이터 UAT "Horizontal_A no edges found across 50 strips" 결함의 직접 원인 제거 (BtoT 케이스에서 자동 rp 가 +π 방향으로 도출되어 사용자가 의도한 -π/2 와 부호 반전) + 사용자의 EdgeSelection 의도가 helper 까지 전파.

Output: DatumFindingService.cs 의 (a) AppendEdgePointsFromStrip 시그니처/본문/주석 캐노니컬 정합 + (b) TryFindLine / TryExtractEdgePoints 시그니처 + (c) 7 caller wiring + (d) **통합 빌드 PASS, 신규 warning 0**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-CONTEXT.md
@.planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-01-PLAN.md
@CLAUDE.md
@WPF_Example/Halcon/Algorithms/DatumFindingService.cs
@WPF_Example/Halcon/Algorithms/MeasurementAlgorithm.cs
@WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs

<interfaces>
<!-- CANONICAL reference: MeasurementAlgorithm.cs lines 130-178 -->
```csharp
var measurePhi = string.Equals(roi.EdgeDirection, "TtoB", StringComparison.OrdinalIgnoreCase)
    ? -Math.PI / 2.0
    : string.Equals(roi.EdgeDirection, "RtoL", StringComparison.OrdinalIgnoreCase)
        ? Math.PI
        : string.Equals(roi.EdgeDirection, "BtoT", StringComparison.OrdinalIgnoreCase) ? Math.PI / 2.0 : 0.0;

// strip 루프 안에서:
HOperatorSet.SmallestRectangle2(stripRegion, out rr, out rc, out rp, out rh, out rw);
HOperatorSet.GenMeasureRectangle2(rr, rc, measurePhi, rh, rw, ...); // rp 미사용, measurePhi 명시
HOperatorSet.MeasurePos(image, handle, sigma, threshold, polarity,
    string.Equals(roi.EdgeSelection, "Last", StringComparison.OrdinalIgnoreCase) ? "last"
    : string.Equals(roi.EdgeSelection, "All", StringComparison.OrdinalIgnoreCase) ? "all" : "first",
    out rows, out cols, out amp, out dist);
```

<!-- CURRENT BUGGY signature (lines 979-984) — DO NOT preserve, REPLACE -->
```csharp
private void AppendEdgePointsFromStrip(
    HImage image,
    double row1, double col1, double row2, double col2,
    HTuple imageWidth, HTuple imageHeight,
    double sigma, int threshold, string polarity,
    ref HTuple allRows, ref HTuple allCols)
```

<!-- TARGET signature -->
```csharp
private void AppendEdgePointsFromStrip(
    HImage image,
    double row1, double col1, double row2, double col2,
    HTuple imageWidth, HTuple imageHeight,
    double sigma, int threshold, string polarity,
    string direction, string selection,        //260429 hbk Phase 15 — measurePhi 매핑 + selection 명시화
    ref HTuple allRows, ref HTuple allCols,
    string roiLabel)                            //260429 hbk Phase 15 — Trace 로그용 ROI 레이블 (Claude's Discretion)
```

<!-- TryFindLine current signature (line 717-727) -->
```csharp
private bool TryFindLine(
    HImage image, HTuple imageWidth, HTuple imageHeight,
    double roiRow, double roiCol, double roiPhi, double roiLength1, double roiLength2,
    double sigma, int threshold, string polarity,
    string direction, int sampleCount, int trimCount,
    out double lineRowBegin, out double lineColBegin,
    out double lineRowEnd, out double lineColEnd,
    out HTuple edgeRowsOut, out HTuple edgeColsOut,
    out string error,
    string roiLabel)
```

<!-- TARGET TryFindLine signature -->
```csharp
private bool TryFindLine(
    HImage image, HTuple imageWidth, HTuple imageHeight,
    double roiRow, double roiCol, double roiPhi, double roiLength1, double roiLength2,
    double sigma, int threshold, string polarity,
    string direction, string selection,                 //260429 hbk Phase 15 — selection 인자 추가
    int sampleCount, int trimCount,
    out double lineRowBegin, out double lineColBegin,
    out double lineRowEnd, out double lineColEnd,
    out HTuple edgeRowsOut, out HTuple edgeColsOut,
    out string error,
    string roiLabel)
```

<!-- TryExtractEdgePoints current signature (line 860-867) -->
```csharp
private bool TryExtractEdgePoints(
    HImage image, HTuple imageWidth, HTuple imageHeight,
    double roiRow, double roiCol, double roiPhi, double roiLength1, double roiLength2,
    double sigma, int threshold, string polarity,
    string direction, int sampleCount, int trimCount,
    out HTuple rowEdge, out HTuple colEdge,
    out string error,
    string roiLabel)
```

<!-- TARGET TryExtractEdgePoints signature -->
```csharp
private bool TryExtractEdgePoints(
    HImage image, HTuple imageWidth, HTuple imageHeight,
    double roiRow, double roiCol, double roiPhi, double roiLength1, double roiLength2,
    double sigma, int threshold, string polarity,
    string direction, string selection,                 //260429 hbk Phase 15
    int sampleCount, int trimCount,
    out HTuple rowEdge, out HTuple colEdge,
    out string error,
    string roiLabel)
```

<!-- 7 caller sites (DatumFindingService.cs) -->
1. TryTeachTwoLineIntersect Line1 — line 197-205 — pass `config.Line1_EdgeSelection`
2. TryTeachTwoLineIntersect Line2 — line 219-227 — pass `config.Line2_EdgeSelection`
3. TryTeachCircleTwoHorizontal Horizontal_A — line 364-371 — pass `config.Horizontal_A_EdgeSelection`
4. TryTeachCircleTwoHorizontal Horizontal_B — line 385-392 — pass `config.Horizontal_B_EdgeSelection`
5. TryTeachVerticalTwoHorizontal Vertical — line 514-523 — pass `config.Vertical_EdgeSelection`
6. TryTeachVerticalTwoHorizontal Horizontal_A — line 537-544 — pass `config.Horizontal_A_EdgeSelection`
7. TryTeachVerticalTwoHorizontal Horizontal_B — line 558-565 — pass `config.Horizontal_B_EdgeSelection`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AppendEdgePointsFromStrip 시그니처 확장 + measurePhi 매핑 + selection 인자화 + 잘못된 정당화 주석 제거</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs lines 975-1023 (AppendEdgePointsFromStrip 전체 본문)
    - WPF_Example/Halcon/Algorithms/MeasurementAlgorithm.cs lines 130-178 (CANONICAL measurePhi 매핑 패턴)
    - WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs lines 82-106, 360-365 (CANONICAL selection 변환 패턴)
    - 15-CONTEXT.md `<decisions>` 섹션 (LOCKED)
  </read_first>
  <action>
DatumFindingService.cs 의 `AppendEdgePointsFromStrip` (line 975-1023) 을 다음과 같이 전면 교체한다.

**시그니처 변경:**
- 기존: `(image, row1, col1, row2, col2, imageWidth, imageHeight, sigma, threshold, polarity, ref allRows, ref allCols)`
- 신규: `(image, row1, col1, row2, col2, imageWidth, imageHeight, sigma, threshold, polarity, direction, selection, ref allRows, ref allCols, roiLabel)`
  - `direction` (string): "LtoR" / "RtoL" / "TtoB" / "BtoT"
  - `selection` (string): "First" / "Last" / "All" (사용자 PascalCase — 내부에서 lower 변환)
  - `roiLabel` (string): Trace 로그용 — Claude's Discretion 항목, CONTEXT 명시 결정 외 추가.

**메서드 위 주석 정리:**
1. 기존 line 977 의 `SmallestRectangle2 로 strip Phi 자동 도출 → manual effectivePhi 불필요.` 라인 **삭제** (이게 잘못된 가정의 정당화 근원).
2. 새 주석 추가:
```csharp
//260429 hbk Phase 15 — direction → measurePhi 4-way 명시 매핑 (BtoT/TtoB 부호 구분), selection 인자화.
//  CANONICAL refs: MeasurementAlgorithm.cs:130-178, FAIEdgeMeasurementService.cs:82-106, VisionAlgorithmService.cs:63-72.
//  SmallestRectangle2 의 rp 자동 도출은 사용자 의도(BtoT vs TtoB) 를 구분 못 함 → polarity 의미 뒤집힘.
```

**본문 교체:**
```csharp
private void AppendEdgePointsFromStrip(
    HImage image,
    double row1, double col1, double row2, double col2,
    HTuple imageWidth, HTuple imageHeight,
    double sigma, int threshold, string polarity,
    string direction, string selection,                          //260429 hbk Phase 15
    ref HTuple allRows, ref HTuple allCols,
    string roiLabel)                                              //260429 hbk Phase 15
{
    //260429 hbk Phase 15 — direction → measurePhi (CANONICAL: MeasurementAlgorithm.cs:130-178)
    double measurePhi;
    if (string.Equals(direction, "TtoB", StringComparison.OrdinalIgnoreCase))      measurePhi = -Math.PI / 2.0;
    else if (string.Equals(direction, "BtoT", StringComparison.OrdinalIgnoreCase)) measurePhi = +Math.PI / 2.0;
    else if (string.Equals(direction, "RtoL", StringComparison.OrdinalIgnoreCase)) measurePhi = Math.PI;
    else                                                                            measurePhi = 0.0; // LtoR 기본 //260429 hbk

    //260429 hbk Phase 15 — selection (PascalCase) → Halcon MeasurePos 인자 (lower)
    string selectionLower =
        string.Equals(selection, "Last", StringComparison.OrdinalIgnoreCase) ? "last" :
        string.Equals(selection, "All",  StringComparison.OrdinalIgnoreCase) ? "all"  : "first";

    HObject stripRegion = null;
    HTuple measureHandle = null;
    try
    {
        HOperatorSet.GenRectangle1(out stripRegion, row1, col1, row2, col2);

        HTuple rr, rc, rp, rh, rw;
        HOperatorSet.SmallestRectangle2(stripRegion, out rr, out rc, out rp, out rh, out rw);
        //260429 hbk Phase 15 — rp(자동 phi) 는 더 이상 사용 안 함; measurePhi 만 사용. rh/rw 는 유효 (strip 의 half-W/H).

        HOperatorSet.GenMeasureRectangle2(
            rr, rc, measurePhi, rh, rw,                           //260429 hbk Phase 15 — rp → measurePhi
            imageWidth, imageHeight, "nearest_neighbor",
            out measureHandle);

        HTuple edgeRows, edgeCols, amp, dist;
        HOperatorSet.MeasurePos(
            image, measureHandle, sigma, threshold,
            polarity, selectionLower,                              //260429 hbk Phase 15 — "all" → selectionLower
            out edgeRows, out edgeCols, out amp, out dist);

        //260429 hbk Phase 15 — Trace 로그 강화: measurePhi (deg) + selection 노출 (디버깅 편의)
        Logging.PrintLog((int)ELogType.Trace,
            string.Format("[Datum.{0}] strip MeasurePos: dir={1} measurePhi={2:F1}deg sel={3} edges={4}",
                roiLabel ?? "?", direction ?? "?", measurePhi * 180.0 / Math.PI, selectionLower,
                edgeRows.TupleLength()));

        if (edgeRows.TupleLength() <= 0 || edgeCols.TupleLength() <= 0)
        {
            return;
        }

        HOperatorSet.TupleConcat(allRows, edgeRows, out allRows);
        HOperatorSet.TupleConcat(allCols, edgeCols, out allCols);
    }
    catch (Exception ex)
    {
        //260429 hbk Phase 15 — 빈 catch 진단 강화: 라벨 + 예외 메시지 Trace 로그 (per-strip swallow 정책 유지)
        try
        {
            Logging.PrintLog((int)ELogType.Trace,
                string.Format("[Datum.{0}] strip swallowed: {1}", roiLabel ?? "?", ex.Message));
        }
        catch { }
    }
    finally
    {
        if (measureHandle != null) { try { HOperatorSet.CloseMeasure(measureHandle); } catch { } }
        if (stripRegion != null)   { try { stripRegion.Dispose(); } catch { } }
    }
}
```

규칙:
- 모든 변경 라인에 `//260429 hbk Phase 15` 주석 (CLAUDE.md feedback 메모리).
- C# 7.2 한정 (switch expression / pattern matching 금지). if/else 체인 사용.
- 파일 brace 스타일은 K&R/Allman 혼재이지만 메서드 자체는 Allman (기존 라인 988 `try {` 도 한 줄 — 그대로 유지).
- 호출자 7 사이트는 Task 2 + 3 에서 즉시 후속 — 본 plan 안에서 빌드 회복.
  </action>
  <verify>
    <automated>grep -n "measurePhi" "C:/Info/Project/DataMeasurement/WPF_Example/Halcon/Algorithms/DatumFindingService.cs"</automated>
  </verify>
  <done>
    - `grep "measurePhi" DatumFindingService.cs` ≥ 4 매치 (선언 + GenMeasureRectangle2 + Trace + 본문)
    - `grep "selectionLower" DatumFindingService.cs` ≥ 2 매치 (변환 + MeasurePos 인자)
    - `grep "MeasurePos(.*\"all\"" DatumFindingService.cs` 0 매치 (helper 안에서 "all" 하드코딩 제거 — VisionAlgorithmService 의 polar sampling Circle 코드는 Plan 15-03 범위)
    - `grep "manual effectivePhi 불필요" DatumFindingService.cs` 0 매치 (잘못된 정당화 주석 제거)
    - 시그니처: `grep "string direction, string selection" DatumFindingService.cs` ≥ 1
    - 빌드 검증은 본 plan Task 3 끝까지 진행 후 수행 (atomic wave-2 단위)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TryFindLine + TryExtractEdgePoints 시그니처에 selection 추가 + 내부 AppendEdgePointsFromStrip 호출 인자 전달</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs lines 717-973 (두 helper 전체)
    - 본 plan Task 1 (AppendEdgePointsFromStrip 신규 시그니처 — direction/selection/roiLabel 인자 순서)
    - 15-CONTEXT.md (LOCKED: roiPhi 파라미터는 미사용이지만 그대로 유지 — 미래 ROI 회전 보정용)
  </read_first>
  <action>
**A. TryFindLine 시그니처 변경 (line 717-727)**:
- `string direction, int sampleCount, int trimCount,` → `string direction, string selection, int sampleCount, int trimCount,`
- 신규 라인 표시 주석 `//260429 hbk Phase 15 — selection 파라미터 추가 (DatumConfig.*_EdgeSelection 전파)`

**B. TryFindLine sanity clamp 추가 (line 736-742 부근)** — `if (string.IsNullOrEmpty(direction)) direction = "LtoR";` 다음에:
```csharp
if (string.IsNullOrEmpty(selection)) selection = "First"; //260429 hbk Phase 15 — Halcon "first" 매핑 (canonical)
```

**C. TryFindLine 안의 AppendEdgePointsFromStrip 호출 2건(line 776-780, 789-793) 수정**:
```csharp
AppendEdgePointsFromStrip(
    image, r1, left, r2, right,
    imageWidth, imageHeight,
    sigma, threshold, polarity,
    direction, selection,                          //260429 hbk Phase 15
    ref allRows, ref allCols,
    roiLabel);                                     //260429 hbk Phase 15 — Trace 로그용 ROI 레이블 전달
```
(scanHorizontal 분기와 vertical 분기 모두 동일하게 수정 — 2건)

**D. TryExtractEdgePoints 시그니처 변경 (line 860-867)** — TryFindLine 과 동일 위치에 `string selection` 추가:
```csharp
string direction, string selection,                //260429 hbk Phase 15
int sampleCount, int trimCount,
```

**E. TryExtractEdgePoints sanity clamp 추가** — `if (string.IsNullOrEmpty(direction)) direction = "LtoR";` (line 877 부근) 다음에:
```csharp
if (string.IsNullOrEmpty(selection)) selection = "First"; //260429 hbk Phase 15
```

**F. TryExtractEdgePoints 안의 AppendEdgePointsFromStrip 호출 2건(line 913-917, 926-930) 수정** — TryFindLine 과 동일 패턴:
```csharp
AppendEdgePointsFromStrip(
    image, r1, left, r2, right,
    imageWidth, imageHeight,
    sigma, threshold, polarity,
    direction, selection,                          //260429 hbk Phase 15
    ref allRows, ref allCols,
    roiLabel);                                     //260429 hbk Phase 15
```

규칙:
- 인자 순서: direction → selection → sampleCount → trimCount (CONTEXT 권고 패턴 유지)
- 모든 변경 라인에 `//260429 hbk Phase 15` 주석.
- 함수 안의 polarity sanity clamp ("all" 기본) 는 그대로 유지 (Datum polarity 의미 — 다른 분야).
  </action>
  <verify>
    <automated>grep -c "string direction, string selection" "C:/Info/Project/DataMeasurement/WPF_Example/Halcon/Algorithms/DatumFindingService.cs"</automated>
  </verify>
  <done>
    - `grep -c "string direction, string selection" DatumFindingService.cs` ≥ 2 (TryFindLine + TryExtractEdgePoints 시그니처)
    - `grep -c "if (string.IsNullOrEmpty(selection))" DatumFindingService.cs` ≥ 2
    - `grep -c "AppendEdgePointsFromStrip(" DatumFindingService.cs` == 4 (호출 4건 — TryFindLine 2 + TryExtractEdgePoints 2; helper 자체 정의 1건은 별개)
    - 4 호출 모두 `direction, selection,` + `roiLabel` 인자 포함 (multi-line grep 또는 시각 확인)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 7 호출 사이트 wiring — config.<ROI>_EdgeSelection 인자 전달 + 통합 빌드 검증</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs lines 188-235 (TryTeachTwoLineIntersect Line1+Line2 호출)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs lines 360-400 (TryTeachCircleTwoHorizontal Horizontal_A+B 호출)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs lines 510-575 (TryTeachVerticalTwoHorizontal Vertical+H_A+H_B 호출)
    - 15-CONTEXT.md (LOCKED: 7 사이트 정확한 좌표)
  </read_first>
  <action>
DatumFindingService 의 7 개 호출 사이트에 `config.<ROI>_EdgeSelection` 인자를 추가한다. 각 사이트의 기존 인자 패턴은 동일:
```
config.<ROI>_Sigma, config.<ROI>_EdgeThreshold, config.<ROI>_EdgePolarity,
config.<ROI>_EdgeDirection, config.<ROI>_EdgeSampleCount, config.<ROI>_EdgeTrimCount,
```
→ EdgeDirection 다음, EdgeSampleCount 앞에 EdgeSelection 삽입:
```
config.<ROI>_EdgeDirection, config.<ROI>_EdgeSelection,            //260429 hbk Phase 15
config.<ROI>_EdgeSampleCount, config.<ROI>_EdgeTrimCount,
```

**7 사이트 정확 위치 (Task 1+2 시그니처 변경 후 라인 번호 약간 시프트 가능 — ROI 식별자로 grep 후 적용):**

1. **TryTeachTwoLineIntersect Line1** (line 197-205 부근, `TryFindLine(image, imageWidth, imageHeight, config.Line1_Row, ...`):
   - `config.Line1_EdgeDirection,` 다음에 `config.Line1_EdgeSelection,` 삽입.

2. **TryTeachTwoLineIntersect Line2** (line 219-227 부근, `TryFindLine(image, imageWidth, imageHeight, config.Line2_Row, ...`):
   - `config.Line2_EdgeDirection,` 다음에 `config.Line2_EdgeSelection,` 삽입.

3. **TryTeachCircleTwoHorizontal Horizontal_A** (line 364-371 부근, `TryExtractEdgePoints(image, imageWidth, imageHeight, config.Horizontal_A_Row, ...`):
   - `config.Horizontal_A_EdgeDirection,` 다음에 `config.Horizontal_A_EdgeSelection,` 삽입.

4. **TryTeachCircleTwoHorizontal Horizontal_B** (line 385-392 부근, 같은 패턴):
   - `config.Horizontal_B_EdgeDirection,` 다음에 `config.Horizontal_B_EdgeSelection,` 삽입.

5. **TryTeachVerticalTwoHorizontal Vertical** (line 514-523 부근, `TryFindLine(image, imageWidth, imageHeight, config.Vertical_Row, ...`):
   - `config.Vertical_EdgeDirection,` 다음에 `config.Vertical_EdgeSelection,` 삽입.

6. **TryTeachVerticalTwoHorizontal Horizontal_A** (line 537-544 부근):
   - `config.Horizontal_A_EdgeDirection,` 다음에 `config.Horizontal_A_EdgeSelection,` 삽입.

7. **TryTeachVerticalTwoHorizontal Horizontal_B** (line 558-565 부근):
   - `config.Horizontal_B_EdgeDirection,` 다음에 `config.Horizontal_B_EdgeSelection,` 삽입.

규칙:
- 7 사이트 모두 `//260429 hbk Phase 15 — EdgeSelection 전파 (DatumConfig → MeasurePos)` 주석.
- 인자 위치/개수 정확 — 잘못된 위치 삽입 시 컴파일러가 string→int 변환 에러로 알려줌.
- TryTeachCircleTwoHorizontal 의 Circle 검출 호출(VisionAlgorithmService.TryFindCircleByPolarSampling, line 338) 은 **본 plan 범위 아님** — Plan 15-03 (renumbered, was 15-04) 에서 별도 처리.

**통합 빌드 검증 (필수)**:
```bash
msbuild "C:/Info/Project/DataMeasurement/WPF_Example/DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /v:minimal
```
- exit 0 PASS, 신규 warning 0 (DatumFindingService.cs 한정).
- 본 plan 의 Task 1+2+3 완료 후 빌드 회복 — atomic wave-2 단위.
  </action>
  <verify>
    <automated>grep -c "_EdgeSelection," "C:/Info/Project/DataMeasurement/WPF_Example/Halcon/Algorithms/DatumFindingService.cs"</automated>
  </verify>
  <done>
    - `grep -c "_EdgeSelection," DatumFindingService.cs` == 7 (정확히 7 사이트)
    - `grep -c "Line1_EdgeSelection," DatumFindingService.cs` == 1
    - `grep -c "Line2_EdgeSelection," DatumFindingService.cs` == 1
    - `grep -c "Vertical_EdgeSelection," DatumFindingService.cs` == 1
    - `grep -c "Horizontal_A_EdgeSelection," DatumFindingService.cs` == 2
    - `grep -c "Horizontal_B_EdgeSelection," DatumFindingService.cs` == 2
    - **EdgeSelection 위치 검증 (Info 7)**: `grep -A1 "_EdgeDirection," DatumFindingService.cs | grep -c "_EdgeSelection,"` ≥ 7 (EdgeDirection 다음 라인에 EdgeSelection 정확 삽입 확인)
    - **빌드 PASS**: `msbuild DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64` exit 0, 신규 warning 0 on DatumFindingService.cs
    - 6 ROI 의 wiring 회귀 테스트 (Plan 15-04 UAT 에서 수행)
  </done>
</task>

</tasks>

<verification>
- 통합 빌드 (Plan 15-01 + 15-02 누적):
  - `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64` PASS
  - 신규 warning 0 (DatumFindingService.cs)
- grep 검증:
  - `grep -n "GenMeasureRectangle2.*measurePhi" DatumFindingService.cs` 1 매치 (helper 안)
  - `grep -n "MeasurePos.*selectionLower" DatumFindingService.cs` 1 매치
  - `grep -c "_EdgeSelection," DatumFindingService.cs` == 7
  - `grep -c "string direction, string selection" DatumFindingService.cs` ≥ 2
  - `grep -c "//260429 hbk" DatumFindingService.cs` 신규 ≥ 16 (helper 본문 + clamp 2 + AppendEdge 호출 4 + 7 site = 15+)
- 회귀 검증: TwoLineIntersect / CircleTwoHorizontal / VerticalTwoHorizontal 3 알고리즘 모두 컴파일 깨짐 0
</verification>

<success_criteria>
1. AppendEdgePointsFromStrip 시그니처에 `string direction, string selection, ..., string roiLabel` 추가
2. 함수 본문에서 4-way direction → measurePhi 매핑 (TtoB=-π/2, BtoT=+π/2, RtoL=π, LtoR=0)
3. GenMeasureRectangle2 가 measurePhi (rp 아님) 사용
4. MeasurePos 가 selectionLower ("all" 하드코딩 아님) 사용
5. 잘못된 정당화 주석 ("SmallestRectangle2 가 strip Phi 자동 도출 → manual effectivePhi 불필요") 삭제
6. Trace 로그에 measurePhi (deg) + selection + roiLabel 포함
7. 빈 catch 블록에 진단 로그 추가 (per-strip swallow 정책 유지)
8. TryFindLine 시그니처에 string selection 파라미터 추가
9. TryExtractEdgePoints 시그니처에 string selection 파라미터 추가
10. 두 helper 모두 selection sanity clamp ("First" 기본) 보유
11. AppendEdgePointsFromStrip 호출 4건 모두 direction + selection + roiLabel 인자 전달
12. 7 호출 사이트 모두 config.<ROI>_EdgeSelection 인자 전달
13. msbuild Debug/x64 PASS, 신규 warning 0
14. (실데이터 회귀 검증은 Plan 15-04 UAT)
</success_criteria>

<output>
After completion, create `.planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-02-SUMMARY.md` with: helper before/after diff 요약, 7 사이트 wiring 표 (사이트 / ROI / 라인번호 / 추가된 EdgeSelection 인자), grep 증거, 통합 빌드 결과 (msbuild output 요약).
</output>
