---
phase: 15
plan: 04
type: execute
wave: 4
depends_on: [15-02, 15-03]
files_modified:
  - .planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-UAT.md
autonomous: false
requirements:
  - PHASE15-Req-UAT-RealData
must_haves:
  truths:
    - "TwoLineIntersect / CircleTwoHorizontal / VerticalTwoHorizontal 3 알고리즘 모두 실 데이터 4 EdgeDirection (LtoR/RtoL/TtoB/BtoT) 시나리오에서 PASS"
    - "ROI 이동 후 자동 재티칭이 3 알고리즘 모두에서 정상 동작 — 새 위치 반영"
    - "#1405 IntersectionLl quick task UAT 4건이 본 phase 에서 흡수 검증됨 (VS Rebuild + CTH/VTH 재티칭 + TwoLineIntersect 회귀)"
    - "SIMUL_MODE Phase 14-05 동일 시나리오 재실행 회귀 PASS"
    - "EdgeSelection First/Last/All 3 옵션 모두 PropertyGrid 에서 노출 + 최소 1 시나리오에서 First→Last 변경시 검출점 변화 확인 (다중 에지 ROI 사용 권장)"
    - "기존 INI 레시피 (EdgeSelection 필드 부재) 로드 후 자동 \"First\" 채움 확인 — 무손실 하위호환"
  artifacts:
    - path: ".planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-UAT.md"
      provides: "Phase 15 UAT sign-off 문서"
      contains: "result: PASS"
  key_links:
    - from: "사용자 PropertyGrid EdgeSelection 변경"
      to: "Halcon MeasurePos selection 인자"
      via: "DatumConfig.<ROI>_EdgeSelection → TryFindLine/Extract → AppendEdgePointsFromStrip → MeasurePos"
      pattern: "EdgeSelection"
    - from: "EdgeDirection BtoT/TtoB"
      to: "올바른 measurePhi 부호"
      via: "AppendEdgePointsFromStrip 4-way 매핑"
      pattern: "measurePhi"
---

<objective>
Phase 15 의 코드 변경(15-01~15-03)이 실 데이터 + SIMUL 양쪽에서 회귀를 일으키지 않고, 본래 결함(BtoT 케이스 0 edges + EdgeSelection 부재)을 해결했음을 사용자 육안으로 검증한다.

본 plan 은 사람-검증 체크포인트 — 사용자가 실제로 DataMeasurement.exe 를 실행하여 3 알고리즘 × 4 방향 × ROI 이동 시나리오를 확인하고, #1405 quick task carry-over 4건도 흡수한다.

자동화 가능한 검증(빌드 / grep)은 15-01~15-03 plan 에서 끝남. 본 plan 은 Halcon + 카메라 의존 검증 — 자동화 불가.

Purpose: 구조 결함 해결의 의미적 검증 — "코드가 컴파일된다" 가 아니라 "실제 BtoT EdgeDirection 으로 ROI 그어도 에지점이 검출된다 + ROI 이동 후 자동 재티칭이 새 위치 반영한다".
Output: 15-UAT.md 사인오프 문서 (3 알고리즘 × 4 방향 + ROI 이동 + #1405 4건 + SIMUL 회귀 = ~15+ 시나리오 PASS 기록).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-CONTEXT.md
@.planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-01-PLAN.md
@.planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-02-PLAN.md
@.planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-03-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 15-UAT.md 시나리오 골격 작성 (born-pending)</name>
  <files>.planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-UAT.md</files>
  <read_first>
    - .planning/phases/14-*/14-05-SUMMARY.md or 14-UAT (Phase 14-05 UAT 양식 참조)
    - .planning/phases/09-*/05-HUMAN-UAT.md (born-signed_off 양식 참조 — Test/Result/Summary)
  </read_first>
  <action>
다음 골격을 가진 15-UAT.md 를 생성한다 (status: pending — Task 2 에서 사용자 PASS 후 signed_off 로 변경):

```markdown
---
phase: 15
status: pending
created: 2026-04-29
updated: 2026-04-29
total_tests: 15
passed: 0
pending: 15
---

# Phase 15 UAT — HALCON MeasurePos 정합성 (measurePhi 매핑 + EdgeSelection 명시 처리)

## 사전 준비
- VS 에서 DatumMeasurement.csproj Rebuild → bin/x64/Debug/DatumMeasurement.exe
- HALCON 24.11 + 카메라 SDK 설치 확인
- 기존 Setting.ini (Phase 14-05 사용된 레시피) 백업 후 로드

## 자동 검증 (Plan 15-01~03 에서 PASS 확인)
- [ ] 빌드 PASS, 신규 warning 0
- [ ] grep 검증: `grep -c "_EdgeSelection," DatumFindingService.cs` == 7
- [ ] grep 검증: `grep "measurePhi" DatumFindingService.cs` ≥ 4
- [ ] grep 검증: `grep "MeasurePos.*\"all\"" DatumFindingService.cs` 0 매치 (helper 안)
- [ ] INI 하위호환: 기존 레시피 로드 후 PropertyGrid 의 6 ROI EdgeSelection 모두 "First" 표시

## Tests

### Test 1 — TwoLineIntersect × LtoR (Line1 + Line2)
- 시나리오: Line1 ROI 그리고 EdgeDirection=LtoR, Line2 ROI 그리고 EdgeDirection=LtoR → btn_teachDatum
- 기대: 두 라인 모두 raw 에지점 + 검출 라인(cyan/magenta) 표시, 교점/각도 좌표 라벨 표시
- result: pending

### Test 2 — TwoLineIntersect × RtoL
- 시나리오: 동일하되 EdgeDirection=RtoL
- 기대: 검출 PASS, raw 점이 ROI 우→좌 방향으로 분포
- result: pending

### Test 3 — TwoLineIntersect × TtoB
- 시나리오: EdgeDirection=TtoB (수직 strip 슬라이스)
- 기대: 검출 PASS — Phase 15 핵심 회귀 (이전엔 자동 rp 가 의도와 부호 반전 가능)
- result: pending

### Test 4 — TwoLineIntersect × BtoT
- 시나리오: EdgeDirection=BtoT (수직 strip, polarity 부호 반전 의도)
- 기대: 검출 PASS — Phase 15 핵심 회귀 (실 데이터 UAT 에서 노출됐던 결함의 직접 검증)
- result: pending

### Test 5 — CircleTwoHorizontal × LtoR (Horizontal_A/B), Circle EdgeSelection=First
- 시나리오: Circle ROI + Horizontal_A + Horizontal_B 모두 LtoR, Circle_EdgeSelection=First
- 기대: 원 검출 + 360° polar 점 + 수평 2-ROI concat 라인 + 교점 표시
- result: pending

### Test 6 — CircleTwoHorizontal × Horizontal direction = RtoL/BtoT 혼합
- 시나리오: Horizontal_A=LtoR, Horizontal_B=RtoL (또는 BtoT 시도)
- 기대: 둘 다 정상 검출 (이전엔 BtoT 부호 반전으로 0 edges)
- result: pending

### Test 7 — VerticalTwoHorizontal × Vertical TtoB + Horizontal LtoR
- 시나리오: Vertical ROI EdgeDirection=TtoB, Horizontal_A/B=LtoR
- 기대: 수직 라인 검출 + 수평 concat 라인 + 교점
- result: pending

### Test 8 — VerticalTwoHorizontal × Vertical BtoT
- 시나리오: Vertical EdgeDirection=BtoT (수직 ROI 부호 반전 — Phase 15 핵심)
- 기대: 검출 PASS
- result: pending

### Test 9 — EdgeSelection First → Last → All 변경 회귀 (다중 에지 ROI 권장)
- 시나리오: TwoLineIntersect Line1 의 EdgeSelection 을 First → Last → All 로 변경하며 재티칭
- 기대: **최소 한 시나리오에서** First→Last 변경시 검출 라인 위치/raw 점 분포가 변화 확인 (단일 에지 ROI 는 First==Last 일 수 있음 — 주의)
- 사용자 가이드: **다중 에지 ROI** 사용 권장 — ROI 안에 여러 명암 전이가 있는 영역(예: 패턴 경계, 글자 영역, 두 평행 에지가 있는 부품) 을 선택하면 First/Last 차이 명확히 관찰 가능. 단일 에지만 있는 ROI 에서는 First/Last/All 결과가 모두 동일할 수 있음 — 이 경우 ROI 위치를 다중 에지 영역으로 옮긴 후 재시도.
- result: pending

### Test 10 — ROI 이동 후 자동 재티칭 (TwoLineIntersect)
- 시나리오: 티칭 완료 후 Line1 ROI 드래그 이동
- 기대: RoiMoveCompleted → InvokeTryTeachDatum 자동 호출 → 새 위치에서 재검출
- result: pending

### Test 11 — ROI 이동 후 자동 재티칭 (CircleTwoHorizontal)
- 시나리오: Circle ROI 또는 Horizontal_A ROI 드래그 이동
- 기대: 자동 재검출 + 새 위치 반영
- result: pending

### Test 12 — ROI 이동 후 자동 재티칭 (VerticalTwoHorizontal)
- 시나리오: Vertical ROI 드래그 이동
- 기대: 자동 재검출 + 새 위치 반영
- result: pending

### Test 13 — #1405 IntersectionLl carry-over 검증 (CTH 재티칭 후 IntersectionLl PASS)
- 시나리오: CircleTwoHorizontal 티칭 후 ROI 이동 + 재티칭 → IntersectionLl 호출 정상 (length-2 fit 인자 길이 불일치 없음)
- 기대: 예외 없음, 교점 좌표 출력
- 출처: 260429-c2e quick task — UAT 미완료 carry-over 4건 중 핵심 1건
- result: pending

### Test 14 — #1405 carry-over: VTH ConcatObj→TupleConcat 패턴 회귀
- 시나리오: VerticalTwoHorizontal 재티칭 → 교점 정상 출력
- 출처: 260429-c2e
- result: pending

### Test 15 — SIMUL_MODE Phase 14-05 동일 시나리오 재실행
- 시나리오: SIMUL 데이터로 3 알고리즘 btn_testFindDatum 통합 (Phase 14-05 UAT 시나리오 그대로 재실행)
- 기대: 모두 PASS (회귀 0)
- result: pending

## Summary
- Total: 15
- Passed: 0
- Pending: 15
- Failed: 0

## Sign-off
- 사용자: pending
- 일자: pending
- 비고: pending
```

생성 후 Task 2 의 checkpoint 로 진입.
  </action>
  <verify>
    <automated>test -f "C:/Info/Project/DataMeasurement/.planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-UAT.md" && grep -c "result: pending" "C:/Info/Project/DataMeasurement/.planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-UAT.md"</automated>
  </verify>
  <done>
    - 15-UAT.md 파일 존재
    - `grep -c "result: pending" 15-UAT.md` ≥ 15
    - status: pending in frontmatter
    - Test 1~15 시나리오 모두 작성됨
    - Test 9 의 기대값이 "최소 한 시나리오에서 변화 확인" 으로 표현되었고 사용자 가이드 (다중 에지 ROI 정의) 포함
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 사용자 UAT 실행 + Test 1~15 결과 기록 + sign-off</name>
  <what-built>
Phase 15 의 3 코드 plan (15-01 EdgeOptionLists.Selections + DatumConfig 6 EdgeSelection / 15-02 AppendEdgePointsFromStrip measurePhi+selection + TryFindLine/TryExtractEdgePoints+7 wiring atomic 통합 / 15-03 TryFindCircleByPolarSampling selection + Circle wiring) 통합 결과:
- DatumConfig 의 6 ROI 모두에서 사용자가 PropertyGrid 로 EdgeSelection (First/Last/All) 을 선택 가능
- AppendEdgePointsFromStrip 가 EdgeDirection → measurePhi 4-way 명시 매핑 (BtoT/TtoB 부호 구분 회복)
- 7 사이트 wiring 으로 사용자 의도가 HALCON MeasurePos 까지 단대단 전파
- Circle 알고리즘 (polar sampling) 도 selection 명시 처리
- 빌드 PASS, 신규 warning 0
  </what-built>
  <how-to-verify>
**전제**: VS 에서 DatumMeasurement.csproj Rebuild → bin/x64/Debug/DatumMeasurement.exe 실행

**필수 시나리오 (15-UAT.md 의 Test 1~15)**:

1. Test 1~4 — TwoLineIntersect × 4 EdgeDirection: Line1+Line2 ROI 그리고 각 방향(LtoR/RtoL/TtoB/BtoT) 별 검출 PASS 확인 (특히 BtoT/TtoB — Phase 15 핵심 회귀)
2. Test 5~6 — CircleTwoHorizontal × Horizontal direction 변화: Circle 검출 + 수평 2-ROI concat 라인 + 교점 표시 + Horizontal_B 를 RtoL 또는 BtoT 로 변경 시에도 검출 PASS
3. Test 7~8 — VerticalTwoHorizontal × Vertical direction TtoB/BtoT: 수직 라인 검출 + 수평 concat 라인 + 교점
4. Test 9 — EdgeSelection 옵션 변화: 다중 에지 ROI (패턴 경계, 글자 영역 등) 에서 EdgeSelection First → Last → All 변경하며 재티칭, 옵션별 검출 차이 확인
5. Test 10~12 — ROI 이동 자동 재티칭: 3 알고리즘 모두 ROI 드래그 후 새 위치 반영
6. Test 13~14 — #1405 carry-over 흡수: CTH/VTH 재티칭 후 IntersectionLl 예외 없음 (length-2 fit 인자 길이 불일치 결함 회귀 0)
7. Test 15 — SIMUL_MODE Phase 14-05 동일 시나리오 재실행 PASS 확인

**각 Test 수행 후**: 15-UAT.md 의 해당 Test 의 `result: pending` 을 `result: PASS (2026-04-29 user-confirmed)` 또는 `result: FAIL <이유>` 로 갱신.

**모든 Test PASS 시**:
- frontmatter `status: pending` → `status: signed_off`
- frontmatter `passed: 0` → `passed: 15`, `pending: 15` → `pending: 0`
- Summary 섹션 갱신
- Sign-off 섹션에 사용자명/일자 기재

**FAIL 발생 시**:
- 해당 Test 의 result 에 FAIL 사유 기록
- gap_closure 워크플로우로 진입 (별도 plan 으로 fix)
  </how-to-verify>
  <resume-signal>모든 15 Test PASS 후 "approved" 또는 FAIL 발생 시 사유 보고</resume-signal>
</task>

</tasks>

<verification>
- 15-UAT.md 파일 존재, 15 테스트 모두 result: PASS (사용자 확인 후)
- frontmatter status: signed_off, passed: 15, pending: 0
- Sign-off 섹션 사용자명/일자 기재
</verification>

<success_criteria>
1. 15-UAT.md 가 존재하고 15 시나리오 모두 PASS 사인오프
2. TwoLineIntersect / CircleTwoHorizontal / VerticalTwoHorizontal 3 알고리즘 모두 4 EdgeDirection (LtoR/RtoL/TtoB/BtoT) 에서 검출 동작
3. ROI 이동 자동 재티칭 3 알고리즘 모두 정상
4. #1405 carry-over 4건 흡수 (CTH/VTH 재티칭 회귀 + TwoLineIntersect 회귀 + IntersectionLl 예외 없음)
5. SIMUL_MODE Phase 14-05 회귀 0
6. EdgeSelection First/Last/All 3 옵션 PropertyGrid 노출 + 다중 에지 ROI 시나리오에서 의미적 차이 확인
7. 기존 INI 레시피 무손실 하위호환 확인
</success_criteria>

<output>
After completion, create `.planning/phases/15-halcon-measurepos-measurephi-edgeselection-datumfindingservi/15-04-SUMMARY.md` with: 15 시나리오 결과 표, FAIL 발생 시 처리 plan 참조, 다음 단계 (Phase 15 close 또는 v1.0 milestone audit).
</output>
