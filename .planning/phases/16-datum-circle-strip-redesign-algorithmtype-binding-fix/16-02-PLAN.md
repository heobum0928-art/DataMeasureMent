---
phase: 16-datum-circle-strip-redesign-algorithmtype-binding-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: true
requirements: [R3, R4]
must_haves:
  truths:
    - "ROI 임의 이동 후 Datum 1 → 2 → 3 클릭 시 PropertyGrid AlgorithmType combobox 가 매번 즉시 (TwoLineIntersect → CircleTwoHorizontal → VerticalTwoHorizontal) 갱신된다"
    - "ROI 이동 후 Datum 2 클릭 → btn_teachDatum 클릭 시 CircleTwoHorizontal 코드 경로가 실제 실행된다 (이전 알고리즘 stale 호출 0)"
    - "Datum 노드 선택 핸들러에서 PropertyGrid.SelectedObject 가 null → newDatum 의 두 단계 force rebind 로 갱신된다"
    - "ROI 이동 5회 시 HALCON edge measurement 자동 호출 0회 (InvokeTryTeachDatumForEdit 호출 없음, MeasurePos / TryFindLine trace 0건 추가)"
    - "btn_teachDatum 1회 클릭 시 InvokeTryTeachDatum 1회 실행 (수동 트리거는 보존)"
    - "ROI 이동 후 LastTeachSucceeded 가 변경되지 않음 (D-14: 이전 검출 원/center 시각화는 stale 데이터 표시 — 사용자가 mismatch 인지)"
    - "VisionAlgorithmService.cs / DatumFindingService.cs git diff 0 라인 (D-22 보존)"
    - "msbuild Debug/x64 PASS, 신규 warning 0 on InspectionListView.xaml.cs / MainView.xaml.cs"
  artifacts:
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "Datum 노드 선택 시 PropertyGrid SelectedObject null → new force rebind (D-09 / D-10) — InspectionList_SelectionChanged Datum 분기 재설계"
      contains: "ParamEditor.SelectedObject = null"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "HandleDatumRoiMove / HandleDatumRoiResize 의 InvokeTryTeachDatumForEdit 자동 호출 라인 직접 삭제 (D-13)"
      contains: "//260429 hbk Phase 16 D-13 — auto-reteach off"
  key_links:
    - from: "InspectionListView.InspectionList_SelectionChanged (Datum 분기, line 338-358)"
      to: "PropertyGrid SelectedObject 강제 재바인딩 (null 할당 → 새 DatumConfig 인스턴스 할당) — AlgorithmType combobox 까지 닿게"
      via: "RefreshParamEditor() 가 이미 null→new 패턴을 보유 — Datum 분기에서 SetParam 호출 직후 추가 호출"
      pattern: "RefreshParamEditor\\(\\)"
    - from: "MainView.HandleDatumRoiMove (line 583)"
      to: "InvokeTryTeachDatumForEdit 호출 라인 삭제 (Dispatcher.BeginInvoke 블록 내부)"
      via: "직접 라인 제거 + 주석 (자동 재티칭 off, 수동 btn_teachDatum 으로만 트리거)"
      pattern: "//260429 hbk Phase 16 D-13"
    - from: "MainView.HandleDatumRoiResize (line 622)"
      to: "InvokeTryTeachDatumForEdit 호출 라인 삭제 (Dispatcher.BeginInvoke 블록 내부)"
      via: "직접 라인 제거 + 주석"
      pattern: "//260429 hbk Phase 16 D-13"
---

<objective>
Datum AlgorithmType binding 결함 (R3) + Auto-reteach 리소스 과부하 (R4) 를 두 파일 직접 수술로 해결한다. **알고리즘 코드 (VisionAlgorithmService / DatumFindingService) 는 한 줄도 변경하지 않는다.**

Purpose:
- Phase 15 UAT Test 10/11/12 의 근본 원인: ROI 편집 후 Datum 전환 시 PropertyGrid AlgorithmType combobox 가 stale + btn_teachDatum 이 이전 알고리즘 코드 경로 호출. 원인은 InspectionListView 의 Datum 선택 핸들러가 PropertyGrid SelectedObject 를 RaisePropertyChanged + RefreshParamEditor 만으로 처리 → AlgorithmType 까지 닿지 않음. **D-09 / D-10: 매 Datum 클릭마다 PropertyGrid.SelectedObject = null → 새 DatumConfig 인스턴스 할당의 두 단계 force rebind.**
- Phase 13-04 / 14-01 의 Move/Resize 자동 재티칭 (HALCON edge measurement 매번 호출) 은 사용자 리소스 과부하. **D-13: MainView.HandleDatumRoiMove / HandleDatumRoiResize 의 InvokeTryTeachDatumForEdit 자동 호출 라인 직접 삭제.** 사용자가 btn_teachDatum 으로 수동 트리거만 하도록 정책 변경.

Output:
- WPF_Example/UI/ControlItem/InspectionListView.xaml.cs — Datum 분기에서 force rebind 추가 (D-09 / D-10).
- WPF_Example/UI/ContentItem/MainView.xaml.cs — HandleDatumRoiMove / HandleDatumRoiResize 의 자동 재티칭 호출 직접 삭제 (D-13).
- VisionAlgorithmService / DatumFindingService / DatumConfig **읽기만** — diff 0 검증 (D-22).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-datum-circle-strip-redesign-algorithmtype-binding-fix/16-CONTEXT.md
@.planning/phases/16-datum-circle-strip-redesign-algorithmtype-binding-fix/16-SPEC.md
@.planning/phases/12-datum-circle-vertical-horizontal-intersection/12-03-SUMMARY.md
@WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
@WPF_Example/UI/ContentItem/MainView.xaml.cs
@WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs

<interfaces>
<!-- 핵심 시그니처 — executor 가 직접 사용. -->

From WPF_Example/UI/ControlItem/InspectionListView.xaml.cs:
```csharp
public ParamBase SelectedParam { get; private set; } = null;       // line 23
public void RefreshParamEditor()                                    // line 28
{
    if (ParamEditor == null) return;
    var current = SelectedParam;
    ParamEditor.SelectedObject = null;                              // null 할당
    ParamEditor.SelectedObject = current;                           // 재할당 — 두 단계 force rebind
}
private void InspectionList_SelectionChanged(object sender, ...)   // line 294
{
    // ... line 338: if (item.NodeType == ENodeType.Datum)
    //     line 349: if (itemParam is DatumConfig datumCfg)
    //         line 350: mParentWindow.mainView.halconViewer.SetDatumOverlay(datumCfg, true);
    //         line 352: mParentWindow.mainView.PublishDatumRoiCandidates(datumCfg);
    // ... 본 phase 추가: ParamEditor.SelectedObject 강제 null→new force rebind
}
```

From WPF_Example/UI/ContentItem/MainView.xaml.cs:
```csharp
private void HandleDatumRoiMove(DatumConfig datum, RoiMoveCompletedArgs e)        // line 583
{
    ApplyDatumRoiDelta(datum, e);
    try { datum.RaisePropertyChanged(string.Empty); } catch { }
    mParentWindow?.inspectionList?.RefreshParamEditor();
    halconViewer.SetDatumOverlay(datum, true);
    Dispatcher.BeginInvoke(System.Windows.Threading.DispatcherPriority.Background, new Action(() => {
        InvokeTryTeachDatumForEdit(datum);              // line 590 — 본 phase D-13: 삭제 대상
        PublishDatumRoiCandidates(datum);
        UpdateDatumRefCoordsLabel(datum);
    }));
}

private void HandleDatumRoiResize(DatumConfig datum, RoiGeometryChangedArgs e)   // line 598
{
    // ... write-back ...
    Dispatcher.BeginInvoke(System.Windows.Threading.DispatcherPriority.Background, new Action(() => {
        InvokeTryTeachDatumForEdit(datum);              // line 623 — 본 phase D-13: 삭제 대상
        PublishDatumRoiCandidates(datum);
        UpdateDatumRefCoordsLabel(datum);
    }));
}

private void InvokeTryTeachDatum()                                                // line 1460
// ↑ btn_teachDatum 수동 트리거 — 변경 없음 (보존, D-15)
```

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs:
```csharp
public string AlgorithmType { get; set; } = "TwoLineIntersect";   // PropertyGrid combobox binding 대상
public EDatumAlgorithm AlgorithmTypeEnum { get; }                  // string → enum 변환 helper
public bool LastTeachSucceeded { get; set; }                       // D-14: ROI 이동 후 변경 안 함
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: InspectionListView Datum 분기 force rebind (D-09 / D-10) — PropertyGrid SelectedObject null → newDatum</name>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <read_first>
    1. `WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` line 1-100 (RefreshParamEditor 메서드 + TryTriggerDatumAutoReteach 패턴 학습 — 이미 null→new 패턴 보유)
    2. `WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` line 280-410 (InspectionList_SelectionChanged 의 Datum / FAI / Measurement / Action 분기 흐름 — Datum 분기는 line 338-358)
    3. `.planning/phases/12-datum-circle-vertical-horizontal-intersection/12-03-SUMMARY.md` (RaisePropertyChanged + RefreshParamEditor 패턴 — 본 phase 가 한 단계 더 깊게 수술)
    4. `.planning/phases/16-datum-circle-strip-redesign-algorithmtype-binding-fix/16-CONTEXT.md` D-09 / D-10 / D-11 (verbatim 결정값)
  </read_first>
  <action>
    **D-09 / D-10 / D-11 (CONTEXT.md verbatim) 구현.**

    InspectionListView.InspectionList_SelectionChanged 의 Datum 분기 (line 338-358) 를 다음과 같이 수정. 핵심 변경: **Datum 노드 선택 시 PropertyGrid.SelectedObject 를 강제 null→new 두 단계 force rebind** — RefreshParamEditor() 호출만으로는 SetParam(line 328) 직전 시점에 SelectedObject 가 _이미_ 새 ParamBase 인스턴스로 할당된 후이므로 null→new 의 효과가 줄어듦. 핵심은 **SetParam 이후, ENodeType.Datum 분기 안에서 명시적으로 `ParamEditor.SelectedObject = null` → `ParamEditor.SelectedObject = datumCfg`** 로 두 단계 처리하는 것.

    1) **InspectionList_SelectionChanged 의 Datum 분기 (line 338-358) 에 force rebind 라인 추가**:

    기존 line 348-353 영역:
    ```csharp
    //260410 hbk Phase 4 gap fix: show Datum overlay on canvas when Datum node selected
    if (itemParam is DatumConfig datumCfg) {
        mParentWindow.mainView.halconViewer.SetDatumOverlay(datumCfg, true);
        //260426 hbk Phase 13 D-A1 — 이미 티칭된 Datum 도 selection 즉시 ROI hit-test 가능하도록 후보 publish
        mParentWindow.mainView.PublishDatumRoiCandidates(datumCfg);
    }
    ```

    →

    ```csharp
    //260410 hbk Phase 4 gap fix: show Datum overlay on canvas when Datum node selected
    if (itemParam is DatumConfig datumCfg) {
        mParentWindow.mainView.halconViewer.SetDatumOverlay(datumCfg, true);
        //260426 hbk Phase 13 D-A1 — 이미 티칭된 Datum 도 selection 즉시 ROI hit-test 가능하도록 후보 publish
        mParentWindow.mainView.PublishDatumRoiCandidates(datumCfg);

        //260429 hbk Phase 16 D-09/D-10 — Datum 전환 시 PropertyGrid SelectedObject 강제 null→new force rebind.
        //  이유: Phase 15 UAT Test 10~12 결함 — ROI 이동/생성 후 Datum 전환 시 AlgorithmType combobox 가 stale.
        //  RaisePropertyChanged + RefreshParamEditor 만으로는 AlgorithmType (string property) combobox 까지 안 닿음.
        //  SetParam(line 328) 이 SelectedParam 만 갱신하고 ParamEditor.SelectedObject 는 다른 경로(외부 binding)로 갱신될 때
        //  combobox 가 이전 reference 의 stale 값을 유지함 → null 할당으로 binding 강제 해제 후 새 인스턴스 할당.
        //260429 hbk Phase 16 D-09 — _editingDatum 강제 재할당 (이전 reference 보존 안 함, 매번 새 DatumConfig 인스턴스로 즉시 교체)
        if (ParamEditor != null)
        {
            //260429 hbk Phase 16 D-10 — null 할당으로 PropertyGrid 의 기존 binding 강제 해제 후, 새 인스턴스 할당
            ParamEditor.SelectedObject = null;
            ParamEditor.SelectedObject = datumCfg;
        }
    }
    ```

    **D-09 verbatim**: "InspectionListView 의 Datum 노드 선택 핸들러에서 `_editingDatum` 강제 재할당 — 이전 reference 보존 안 함, 매번 새로 선택된 DatumConfig 인스턴스로 즉시 교체." (본 코드베이스에서 `_editingDatum` 명시 필드는 InspectionListView 가 아닌 MainView 에 존재 — InspectionListView 의 등가 reference 는 `ParamEditor.SelectedObject` 이므로 위 패턴이 D-09 의 의도 그대로 충족.)

    **D-10 verbatim**: "PropertyGrid SelectedObject **null → newDatum 의 두 단계 force rebind** — null 할당으로 PropertyGrid 의 기존 binding 강제 해제 후, 새 인스턴스 할당. AlgorithmType combobox 가 stale 상태로 남지 않도록 보장."

    **D-11 verbatim**: "ROI 편집 모드 (Datum.TeachDatum / FAI ROI Edit) 진입/종료 시 `_editingDatum` reference 무영향 — 편집 모드 상태와 reference 교체 로직을 분리. 편집 모드 종료 시 마지막 Datum 노드 선택 상태를 보존하지 않고, 다음 노드 클릭 시 항상 새로 교체." → InspectionList_SelectionChanged 는 트리 노드 선택 변경 시마다 fire 되므로 위 force rebind 는 매 클릭마다 실행. 편집 모드 상태 (btn_teachDatum.IsChecked) 와 무관하게 동작 — 별도 분리 가드 불필요.

    **D-12 verbatim**: "AlgorithmType combobox 변경 자체는 자동 재티칭 안 함 — D-13 (Auto-reteach off) 와 일관. 사용자가 명시적으로 btn_teachDatum 클릭해야 새 알고리즘으로 티칭 실행." → 본 task 는 binding 만 갱신하고 InvokeTryTeachDatum / NotifyDatumParamMaybeChanged 트리거 추가 안 함 (확인: 위 코드에 자동 재티칭 호출 0).

    **D-22 보존**: VisionAlgorithmService.cs / DatumFindingService.cs / DatumConfig.cs 한 줄도 만지지 않는다.

    모든 신규 라인 위에 `//260429 hbk Phase 16 D-XX <reason>` 주석 (D-20).
  </action>
  <acceptance_criteria>
    1. `grep -n "ParamEditor.SelectedObject = null" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` 가 최소 2 hits (RefreshParamEditor 1 + Datum 분기 force rebind 1)
    2. `grep -n "ParamEditor.SelectedObject = datumCfg" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` 가 1 hit (D-10 새 인스턴스 할당)
    3. `grep -n "//260429 hbk Phase 16 D-09" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` 가 최소 1 hit
    4. `grep -n "//260429 hbk Phase 16 D-10" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` 가 최소 1 hit
    5. `grep -c "//260429 hbk" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` ≥ 4 (Task 1 신규 라인 주석)
    6. force rebind 코드가 **`if (itemParam is DatumConfig datumCfg)` 블록 내부** 에 위치 (다른 노드 타입에는 영향 없음 — Action / FAI / Measurement 분기 무영향)
    7. `git diff --stat WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs` 결과 0 라인
    8. `git diff --stat WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 결과 0 라인
    9. `git diff --stat WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 결과 0 라인
    10. `msbuild ... Debug/x64` PASS, InspectionListView.xaml.cs 신규 warning 0
  </acceptance_criteria>
  <verify>
    <automated>cd WPF_Example && msbuild DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo</automated>
  </verify>
  <done>InspectionList_SelectionChanged Datum 분기에 PropertyGrid SelectedObject null→datumCfg force rebind 추가. 빌드 PASS, 알고리즘 파일 diff 0.</done>
</task>

<task type="auto">
  <name>Task 2: MainView Auto-reteach off (D-13) — HandleDatumRoiMove / HandleDatumRoiResize 의 InvokeTryTeachDatumForEdit 자동 호출 직접 삭제</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    1. `WPF_Example/UI/ContentItem/MainView.xaml.cs` line 580-700 (HandleDatumRoiMove + HandleDatumRoiResize + ApplyDatumRoiDelta + ClearDatumRoiFields + NotifyDatumParamMaybeChanged + InvokeTryTeachDatumForEdit 핸들러 흐름 학습)
    2. `WPF_Example/UI/ContentItem/MainView.xaml.cs` line 1260-1500 (btn_teachDatum_Click + InvokeTryTeachDatum 수동 트리거 — D-15: 변경 금지 보존)
    3. `.planning/phases/16-datum-circle-strip-redesign-algorithmtype-binding-fix/16-CONTEXT.md` D-13 / D-14 / D-15 / D-16 verbatim
  </read_first>
  <action>
    **D-13 / D-14 / D-15 / D-16 (CONTEXT.md verbatim) 구현.**

    MainView.xaml.cs 의 두 메서드에서 `InvokeTryTeachDatumForEdit(datum);` 라인을 **직접 삭제** 하고 주석으로 대체. **InvokeTryTeachDatum (수동 트리거, line 1460) / NotifyDatumParamMaybeChanged (PropertyGrid 변경 시 자동 재티칭, line 696) 는 그대로 보존** — 사용자가 명시적으로 btn_teachDatum 클릭하거나 PropertyGrid 의 EdgeDirection / EdgePolarity 변경 시에만 재티칭. ROI 이동/리사이즈는 자동 재티칭 안 함.

    **단, NotifyDatumParamMaybeChanged 도 D-12 ("AlgorithmType combobox 변경 자체는 자동 재티칭 안 함") 와 D-13 ("ROI 이동 후 자동 재티칭 안 함") 의 일관성을 위해 유지/제거 결정 필요.**
    - 16-CONTEXT.md D-12: "AlgorithmType combobox 변경 자체는 자동 재티칭 안 함."
    - 16-CONTEXT.md D-13: "MainView.RoiMoveCompleted 의 InvokeTryTeachDatum 호출 단순 제거."
    - 사용자 인용 3 (CONTEXT.md): "전부 ROI이동 혹은 사이즈만 변경해도 매번 티칭다시 하는데 리소스가 너무 많이 잡아먹는거 같아"
    - **결정 (이 Plan 의 Discretion 범위): NotifyDatumParamMaybeChanged 도 함께 deactivate** — D-12 / D-13 일관성. PropertyGrid 의 EdgeDirection / EdgePolarity / RectL1Ratio 등 변경 시 자동 재티칭이 호출되면 D-12 ("AlgorithmType combobox 변경 자체는 자동 재티칭 안 함") 와 같은 정책이 다른 파라미터에는 다르게 적용되는 비일관성 발생. 사용자 명시 인용 "전부 ROI이동 혹은 사이즈만 변경해도 매번 티칭다시 하는데" 는 ROI 만 언급했으나 의도는 "수동 트리거 일원화" → NotifyDatumParamMaybeChanged 도 본문을 noop 으로 만든다 (시그니처는 호출처 회귀 방지로 보존).

    1) **HandleDatumRoiMove (line 583-595) 수정**:

    ```csharp
    //260425 hbk Phase 13 D-01..D-04 — Datum ROI 이동 후 처리 (delta + 이중 신호 + 자동 재티칭 + 후보 publish)
    //260426 hbk Phase 14-01 D-03 — Move 자동 재티칭 미발동 회귀 fix: Dispatcher.BeginInvoke(Background) defer (Phase 13-07 Fix A 패턴)
    //260429 hbk Phase 16 D-13 — Auto-reteach off: ROI 이동 후 자동 InvokeTryTeachDatumForEdit 호출 삭제 (사용자가 btn_teachDatum 수동 트리거)
    private void HandleDatumRoiMove(DatumConfig datum, RoiMoveCompletedArgs e) {
        ApplyDatumRoiDelta(datum, e);
        try { datum.RaisePropertyChanged(string.Empty); } catch { }
        mParentWindow?.inspectionList?.RefreshParamEditor();
        halconViewer.SetDatumOverlay(datum, true);
        //260429 hbk Phase 16 D-13 — Dispatcher.BeginInvoke 자동 재티칭 블록 삭제 (CONTEXT D-13 verbatim).
        //  Phase 14-01 의 Dispatcher.BeginInvoke(Background) defer 패턴은 자동 재티칭이 fire 되어야만 의미가 있음.
        //  본 phase 에서 자동 재티칭 자체를 제거하므로 PublishDatumRoiCandidates / UpdateDatumRefCoordsLabel 만 inline 호출.
        //260429 hbk Phase 16 D-14 — ROI 이동 후 LastTeachSucceeded 가 변경되지 않음 → 검출 원/center 시각화는 stale 데이터를 그대로 보여줌
        PublishDatumRoiCandidates(datum);
        UpdateDatumRefCoordsLabel(datum);
    }
    ```

    2) **HandleDatumRoiResize (line 598-627) 수정** — Dispatcher.BeginInvoke 블록 같은 방식으로 자동 재티칭 호출 삭제:

    ```csharp
    //260426 hbk Phase 14-01 D-04 — Datum ROI resize 후처리 (HandleDatumRoiMove 5-step 패턴 동일, delta vs absolute 차이만)
    //260429 hbk Phase 16 D-13 — Auto-reteach off: ROI resize 후 자동 InvokeTryTeachDatumForEdit 호출 삭제
    private void HandleDatumRoiResize(DatumConfig datum, RoiGeometryChangedArgs e) {
        if (datum == null || e == null) return;
        // ... write-back 로직 그대로 보존 (line 601-614) ...

        //260426 hbk Phase 14-01 — write-back 후 이중 신호 (HandleDatumRoiMove 패턴)
        try { datum.RaisePropertyChanged(string.Empty); } catch { }
        mParentWindow?.inspectionList?.RefreshParamEditor();
        halconViewer.SetDatumOverlay(datum, true);

        //260429 hbk Phase 16 D-13 — Dispatcher.BeginInvoke 자동 재티칭 블록 삭제 (CONTEXT D-13 verbatim).
        //260429 hbk Phase 16 D-14 — ROI resize 후 LastTeachSucceeded 가 변경되지 않음
        PublishDatumRoiCandidates(datum);
        UpdateDatumRefCoordsLabel(datum);
    }
    ```

    3) **NotifyDatumParamMaybeChanged (line 696) noop 화** (D-12 / D-13 일관성, "수동 트리거 일원화"):

    ```csharp
    //260426 hbk Phase 13-06 — UAT Test 6 (minor) gap closure: PropertyGrid 파라미터 변경 → 자동 재티칭 트리거
    //260426 hbk Phase 13-07 — UAT Test D recovery fix: LastTeachSucceeded 가드 제거 (IsConfigured만 유지)
    //260429 hbk Phase 16 D-12/D-13 — Auto-reteach off (수동 트리거 일원화):
    //  PropertyGrid 파라미터 변경 (EdgeDirection / EdgePolarity / AlgorithmType 등) 시 자동 재티칭 안 함.
    //  사용자가 btn_teachDatum 수동 클릭해야만 검출 갱신. 시그니처는 호출처 (InspectionListView.TryTriggerDatumAutoReteach) 회귀 방지로 보존.
    public void NotifyDatumParamMaybeChanged(DatumConfig datum) {
        //260429 hbk Phase 16 D-12/D-13 — noop: 자동 재티칭 정책 폐지 (CONTEXT D-13 verbatim "단순화 우선")
        return;
    }
    ```

    4) **InvokeTryTeachDatum (수동 트리거, line 1460) — 변경 금지 보존** (D-15 / D-16: btn_teachDatum 단일 트리거 유지). 별도 progress UI 미도입, 별도 "티칭 완료" 버튼 미신설.

    5) **InvokeTryTeachDatumForEdit (line 705) 자체 — 본 phase 에서 호출처가 모두 사라지지만, 시그니처/본문 보존** (Reflection / 외부 호출 가능성 + 향후 phase 에서 재활성화 가능성, 회귀 위험 0). 본 phase 는 호출 라인만 제거.

    **D-22 보존**: VisionAlgorithmService.cs / DatumFindingService.cs / DatumConfig.cs 한 줄도 만지지 않는다. InvokeTryTeachDatumForEdit 본문 (line 705-728) 보존 (Halcon 호출 코드 그대로).

    모든 변경 라인 위에 `//260429 hbk Phase 16 D-XX <reason>` 주석.
  </action>
  <acceptance_criteria>
    1. `grep -n "InvokeTryTeachDatumForEdit" WPF_Example/UI/ContentItem/MainView.xaml.cs` 결과:
       - 메서드 정의 1 hit (line 705 부근, 보존)
       - 호출 hit 0 (HandleDatumRoiMove + HandleDatumRoiResize 의 호출 라인 모두 삭제됨)
    2. `grep -n "//260429 hbk Phase 16 D-13" WPF_Example/UI/ContentItem/MainView.xaml.cs` 가 최소 3 hits (HandleDatumRoiMove 1 + HandleDatumRoiResize 1 + NotifyDatumParamMaybeChanged 1)
    3. `grep -n "Dispatcher.BeginInvoke.*Background" WPF_Example/UI/ContentItem/MainView.xaml.cs` 결과 — HandleDatumRoiMove / HandleDatumRoiResize 안에서 Dispatcher.BeginInvoke 라인이 모두 사라짐 (자동 재티칭 블록 제거)
    4. `grep -n "PublishDatumRoiCandidates(datum)" WPF_Example/UI/ContentItem/MainView.xaml.cs` 가 최소 2 hits (Move + Resize 모두 inline 호출 보존)
    5. `grep -n "UpdateDatumRefCoordsLabel(datum)" WPF_Example/UI/ContentItem/MainView.xaml.cs` 가 최소 2 hits (Move + Resize 모두 inline 호출 보존)
    6. NotifyDatumParamMaybeChanged 의 본문이 `return;` 한 줄 noop 으로 변경 (시그니처 보존)
    7. InvokeTryTeachDatum (line 1460 부근, 수동 btn_teachDatum 트리거) 본문 변경 0 — `grep -A 5 "private void InvokeTryTeachDatum()"` 출력이 Phase 13-04 / 14 의 기존 호출과 동일
    8. `grep -c "//260429 hbk" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 6 (Task 2 신규 주석)
    9. `git diff --stat WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs` 결과 0 라인
    10. `git diff --stat WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 결과 0 라인
    11. `git diff --stat WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 결과 0 라인
    12. `msbuild ... Debug/x64` PASS, MainView.xaml.cs 신규 warning 0
  </acceptance_criteria>
  <verify>
    <automated>cd WPF_Example && msbuild DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo</automated>
  </verify>
  <done>HandleDatumRoiMove / HandleDatumRoiResize 의 InvokeTryTeachDatumForEdit 자동 호출 라인 삭제. NotifyDatumParamMaybeChanged noop. btn_teachDatum 수동 트리거는 보존. 빌드 PASS. 알고리즘 파일 diff 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 본 phase 는 로컬 데스크탑 WPF UI 의 핸들러 변경. 네트워크/인증/저장소/외부 입력 없음. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | N/A | InspectionListView.InspectionList_SelectionChanged + MainView.HandleDatumRoiMove/Resize | accept | No network, no auth, no file write outside existing pattern, no external input introduced. WPF 핸들러 로컬 reference 교체 + 자동 호출 라인 삭제만. |
</threat_model>

<verification>
- 사용자 SIMUL_MODE 또는 실 카메라 데이터로 ROI 이동 5회 후 Datum 1 → 2 → 3 클릭 시 PropertyGrid AlgorithmType combobox 가 매번 즉시 갱신되는지 확인
- ROI 이동 5회 시 Logging.Trace 에 "InvokeTryTeachDatumForEdit ENTRY" 또는 "MeasurePos" 또는 "TryFindLine" 호출 0건 확인 (Auto-reteach off 검증)
- btn_teachDatum 1회 클릭 시 InvokeTryTeachDatum → DatumFindingService.TryTeachDatum 1회 호출 (Logging.Trace 검증)
- Datum 2 (CircleTwoHorizontal) 선택 → btn_teachDatum 클릭 시 DatumFindingService.TryTeachCircleTwoHorizontal 코드 경로 실행 확인 (이전 알고리즘 stale 호출 0)
- (UAT 16-03 에서 사용자 검증)
</verification>

<success_criteria>
- [ ] InspectionListView.xaml.cs Datum 분기에 PropertyGrid SelectedObject null → datumCfg force rebind 추가 (D-09 / D-10)
- [ ] MainView.xaml.cs HandleDatumRoiMove / HandleDatumRoiResize 의 Dispatcher.BeginInvoke 자동 재티칭 블록 삭제 (D-13)
- [ ] PublishDatumRoiCandidates / UpdateDatumRefCoordsLabel 호출은 inline 으로 보존 (회귀 0)
- [ ] NotifyDatumParamMaybeChanged 본문 noop (D-12 / D-13 일관성)
- [ ] InvokeTryTeachDatum (수동 트리거, btn_teachDatum) 변경 0 (D-15 / D-16 보존)
- [ ] InvokeTryTeachDatumForEdit 메서드 본문 보존 (호출처만 0)
- [ ] msbuild Debug/x64 PASS, 신규 warning 0 on InspectionListView.xaml.cs / MainView.xaml.cs
- [ ] git diff --stat: VisionAlgorithmService.cs / DatumFindingService.cs / DatumConfig.cs 모두 0 라인
- [ ] 모든 신규/수정 라인 위 //260429 hbk Phase 16 D-XX 주석
- [ ] 두 commits — `feat(16-02): InspectionListView Datum SelectedObject force rebind (D-09/D-10)` + `feat(16-02): MainView Auto-reteach off — HandleDatumRoiMove/Resize 자동 호출 삭제 (D-13)`
</success_criteria>

<output>
After completion, create `.planning/phases/16-datum-circle-strip-redesign-algorithmtype-binding-fix/16-02-SUMMARY.md` with:
- 변경 라인 수 + 두 파일 별 변경 요약
- diff 0 보존 파일 명시 (VisionAlgorithmService / DatumFindingService / DatumConfig)
- 변경하지 않은 항목 명시 (InvokeTryTeachDatum 수동 트리거 / InvokeTryTeachDatumForEdit 시그니처 / Phase 14-01 Dispatcher.BeginInvoke 패턴은 자동 재티칭 자체 제거로 무의미해진 것만 삭제)
- Plan 16-03 UAT 인계 메모 (Test 6/7/8/13/14/15 + Test 5/10/11/12 absorbed + 신규 D-13 시나리오)
</output>
