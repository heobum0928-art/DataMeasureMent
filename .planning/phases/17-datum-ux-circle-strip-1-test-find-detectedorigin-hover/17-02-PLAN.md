---
phase: 17-datum-ux-circle-strip-1-test-find-detectedorigin-hover
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: true
requirements_addressed:
  - P17-D-05
  - P17-D-06
  - P17-D-07
  - P17-D-08
  - P17-D-09
  - P17-D-10
  - P17-D-11
  - P17-D-12
  - P17-D-17
  - P17-D-18
  - P17-D-19
  - P17-D-20
  - 16-UAT-carry-#6
  - 16-UAT-carry-#8
  - 16-UAT-carry-#9
  - 16-UAT-carry-#10
  - 16-UAT-carry-#11
  - 16-UAT-carry-#12
  - 16-UAT-carry-#13
  - 16-UAT-carry-#14
  - 16-UAT-carry-#15
must_haves:
  truths:
    - "캔버스 진입만으로는 미리보기 도형이 그려지지 않고, MouseLeftButtonDown 이벤트에서 드래그가 시작된다 (D-05)"
    - "_isEditMode flag 가 false 이면 HitTestSelectedRoi (또는 동등 진입점) 가 즉시 null 반환하여 Rect/Circle/Polygon 모두 클릭/드래그가 ROI 에 닿지 않는다 (D-06)"
    - "Datum ROI 우클릭 → Delete 메뉴 클릭 시 CustomMessageBox 3-button 모달 (단일 / 전체 / 취소) 이 표시되고 사용자 선택에 따라 ClearDatumRoiFields / ClearAllDatumRoiFields / 무동작 분기된다 (D-07)"
    - "DatumConfig 가 ICustomTypeDescriptor 를 구현하여 PropertyGrid 가 AlgorithmType 별로 해당 알고리즘에 속한 필드만 노출한다 (D-08, D-09)"
    - "AlgorithmType combobox 변경 시 PropertyGrid SelectedObject null→new force rebind + LastTeachSucceeded=false + DetectedOrigin 시각화 clear + ROI 보존 + 자동 재검출 없음 (D-10)"
    - "btn_teachDatum 클릭 시 새 알고리즘이 요구하는 ROI 슬롯이 비어 있으면 친절한 한국어 에러 모달 (UI-SPEC Copywriting Contract) 표시 후 teach 미실행 (D-11)"
    - "teach + Test Find 양쪽 모두 실패 시 CustomMessageBox 사유 모달 표시, 성공 시 모달 없음. \"검출된 에지가 없습니다\" 류 에러는 EdgeDirection 힌트 메시지로 변환 (D-12)"
    - "DatumConfig.cs 의 ParamBase INI reflection (Save/Load) 경로는 ICustomTypeDescriptor 추가 후에도 깨지지 않는다 — GetProperties() 무인자가 base TypeDescriptor 위임 (D-09 CONTEXT 169-172 안전 패턴)"
    - "Phase 16 D-09/D-10 (force rebind) + D-13/D-14 (Auto-reteach off) 동작 보존 — 회귀 0"
    - "DatumFindingService.cs 0 라인 변경, VisionAlgorithmService.cs 0 라인 변경 (D-17)"
    - "msbuild Debug/x64 PASS, 신규 warning 0 on 수정 범위"
  artifacts:
    - path: WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
      provides: "ICustomTypeDescriptor 구현 + IsHiddenForAlgorithm 필터 (Plan 17-02 영역)"
      contains: "ICustomTypeDescriptor"
    - path: WPF_Example/UI/ContentItem/MainView.xaml.cs
      provides: "btn_teachDatum 호환성 가드 + Delete 3-button 모달 호출 + 실패 모달 + AlgorithmType 변경 흐름 진입점"
      contains: "ValidateRoiPresence"
    - path: WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
      provides: "_isEditMode single gate + 좌클릭+드래그 시작 패턴 정정"
      contains: "_isEditMode"
    - path: WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
      provides: "AlgorithmType combobox 변경 감지 + 5-step 리셋 (D-10)"
      contains: "AlgorithmType"
  key_links:
    - from: "MainResultViewerControl._isEditMode"
      to: "HitTestSelectedRoi (또는 HitTestOneRoi 호출 사이트)"
      via: "early-return null gate"
      pattern: "if\\s*\\(!_isEditMode\\)\\s*return\\s+null"
    - from: "MainView.HalconViewer_RoiDeleteRequested"
      to: "CustomMessageBox.ShowConfirmation (YesNoCancel)"
      via: "확인 모달"
      pattern: "ShowConfirmation"
    - from: "DatumConfig.GetProperties(Attribute[])"
      to: "IsHiddenForAlgorithm (AlgorithmTypeEnum 별 필터)"
      via: "PropertyDescriptorCollection 필터링"
      pattern: "IsHiddenForAlgorithm"
    - from: "InspectionListView.OnParamEditorSelectionChanged"
      to: "DatumConfig.LastTeachSucceeded / DetectedOrigin* / SelectedObject null→new"
      via: "AlgorithmType 변경 5-step 리셋"
      pattern: "AlgorithmType"
---

<objective>
Cluster B + C 통합 구현 — Edit 모드 단일 gate + 좌클릭+드래그 그리기 + Delete 3-button 모달 + PropertyGrid 동적 노출 + 모달 정책.

Phase 16 UAT carry-over #6, #8, #9, #10, #11, #12, #13, #14, #15 를 한 번에 해소.

Cluster B (D-05, D-06, D-07) — 그리기/Edit/Delete UX 일관화:
- (#8) 좌클릭+드래그부터 그리기 시작 (캔버스 진입 미리보기 제거).
- (#9, #13) Edit 모드에서만 사이즈/이동 가능 — Rect+Circle+Polygon 단일 _isEditMode gate.
- (#14) Delete ROI 모달 — 단일/전체/취소 3-button.

Cluster C (D-08, D-09, D-10, D-11, D-12) — PropertyGrid 동적 노출 + 모달 정책:
- (#11, #15) AlgorithmType 별 PropertyGrid 동적 노출 — DatumConfig 에 ICustomTypeDescriptor 구현 (TLI/CTH/VTH 모두 동일 패턴).
- (#12) AlgorithmType 변경 흐름 — force rebind + LastTeachSucceeded reset + 시각화 clear + ROI 보존 + 자동 재검출 없음.
- (#11 호환성 가드) btn_teachDatum 클릭 시 ROI 부재 친절한 에러 모달.
- (#6, #10) 성공 시 모달 X / 실패 시 사유 모달 — teach + Test Find 양쪽 동일.

Purpose: 사용자가 알고리즘별로 깔끔한 PropertyGrid 만 보고, 의도하지 않은 ROI 변형/실수 삭제를 방지하며, 실패 시점에 다음 액션이 명확하게 안내되는 UX.

Output: DatumConfig.cs (ICustomTypeDescriptor 블록 — Plan 17-01 의 RadialDirection 블록과 영역 분리), MainView.xaml.cs (btn_teachDatum 호환성 가드 + Delete 모달 + 실패 모달 변환 + AlgorithmType 변경 흐름), MainResultViewerControl.xaml.cs (_isEditMode + 좌클릭+드래그 시작 정정), InspectionListView.xaml.cs (AlgorithmType combobox 감지 + 5-step 리셋).

알고리즘 보존 (D-17): DatumFindingService.cs / VisionAlgorithmService.cs 0 라인 변경.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-CONTEXT.md
@.planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-PATTERNS.md
@.planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-UI-SPEC.md
@.planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-01-SUMMARY.md
@.planning/phases/16-datum-circle-strip-redesign-algorithmtype-binding-fix/16-UAT.md
@.planning/phases/16-datum-circle-strip-redesign-algorithmtype-binding-fix/16-02-SUMMARY.md
@.planning/phases/13-datum-algorithm-extensibility/13-03-SUMMARY.md
@.planning/phases/12-datum-circle-vertical-horizontal-intersection/12-03-SUMMARY.md

<interfaces>
<!-- Existing call sites and helpers this plan touches -->

MainResultViewerControl.xaml.cs (PATTERNS.md gap #1+#2):
  L305-329: HitTestSelectedRoi(Point imagePoint) — _rois 우선, _datumRoiCandidates fallback
  L312, L323: HitTestSelectedRoi 호출 사이트 (드래그/리사이즈 시작)
  L332-348: HitTestOneRoi(...) — Rect/Circle/Polygon 공용 hit-test (Phase 13-03)
  L876:     ViewerHost_HMouseMove — preview 동작 위치
  L1297-1319: PublishPointerInfo — 이미 GetGrayval 호출 + PointerInfoChanged 이벤트 발행 (Plan 17-03 활용)

MainView.xaml.cs (PATTERNS.md):
  L495-509: HalconViewer_RoiDeleteRequested — 현재 ClearDatumRoiFields 호출, 확인 모달 없음
  L1258-1296: TeachDatumButton_Click region — Datum 미선택 시 CustomMessageBox 호출 + GetFirstStep
  L1497-1532: BtnTestFindDatum_Click — Plan 17-03 에서 본격 변경, 본 Plan 은 실패 모달 변환만
  L1422-1426: RaisePropertyChanged + RefreshParamEditor + SetDatumOverlay 패턴 (S3)
  L1484-1492: InvokeTryTeachDatum 실패 분기 — label_drawHint.Content 사용 → CustomMessageBox 변환

InspectionListView.xaml.cs (PATTERNS.md):
  L26-33: RefreshParamEditor() — null→current rebind
  L65: OnParamEditorSelectionChanged 진입점 (combobox 필터 가드)
  L348-366: Datum 노드 선택 force rebind 블록 (Phase 16-02)

CustomMessageBox.cs:
  ShowConfirmation(string title, string message, MessageBoxButton buttons) -> MessageBoxResult
  Show(string title, string message) -> MessageBoxResult
  // 3-button: PATTERNS.md 분석에 따라 YesNoCancel 재사용 (Yes=단일 / No=전체 / Cancel=취소).
  // 옵션 (b): 새 overload 추가 — 본 Plan 은 옵션 (a) 채택 (lower risk).

DatumConfig.cs (existing class declaration L14):
  public class DatumConfig : ParamBase {
  // → public class DatumConfig : ParamBase, System.ComponentModel.ICustomTypeDescriptor {
  // GetProperties() 무인자: TypeDescriptor.GetProperties(this, true) 위임 (CONTEXT 169-172)
  // GetProperties(Attribute[]): AlgorithmType 별 필터링

EDatumAlgorithm enum (Phase 12-01 기준): TwoLineIntersect / CircleTwoHorizontal / VerticalTwoHorizontal
DatumConfig.AlgorithmTypeEnum (Phase 12-01 helper) — string AlgorithmType → enum 변환
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MainResultViewerControl — _isEditMode 단일 gate + 좌클릭+드래그 그리기 시작 정정 (D-05 + D-06)</name>
  <files>WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs (전체 — 특히 L300-360 HitTestSelectedRoi/HitTestOneRoi, L860-920 ViewerHost_HMouseMove + 드래그 시작 분기, _isDrawingRect / _isDrawingCircle 가 true 로 설정되는 *모든* 위치를 grep 으로 식별)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-CONTEXT.md (D-05, D-06)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-PATTERNS.md (MainResultViewerControl 섹션 + Cross-file analog + gap #1+#2)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-UI-SPEC.md (§ "그리기 진입 상태", § "_isEditMode 단일 gate")
    - .planning/phases/13-datum-algorithm-extensibility/13-03-SUMMARY.md (HitTestOneRoi 도입 컨텍스트)
  </read_first>
  <action>
    **Step A — `_isEditMode` 필드 + IsEditMode 프로퍼티 추가** (PATTERNS.md gap #2 — 정확한 필드 그룹은 파일 상단 다른 _isXxx flag 들 인근에 배치):

    ```csharp
    //260430 hbk Phase 17 D-06 — Edit 모드 단일 gate (Rect+Circle+Polygon 공통)
    private bool _isEditMode = false;
    public bool IsEditMode { get { return _isEditMode; } set { _isEditMode = value; } }
    ```

    **Step B — HitTestSelectedRoi 진입 가드** (L305 또는 동등한 진입점):

    ```csharp
    private RoiDefinition HitTestSelectedRoi(Point imagePoint) {
        if (!_isEditMode) return null; //260430 hbk Phase 17 D-06 — Edit OFF 시 hit-test 불가
        // ... existing body unchanged ...
    }
    ```

    HitTestOneRoi (L332-348) 자체는 수정하지 않는다 — HitTestSelectedRoi 가 진입 게이트 역할. Datum candidates fallback 도 같은 진입 가드로 통합 차단됨 (의도).

    **Step C — 좌클릭+드래그 시작 패턴 정정 (D-05)**:

    먼저 아래 grep 으로 정확한 위치 식별:
    ```bash
    grep -n "_isDrawingRect\\s*=\\s*true\\|_isDrawingCircle\\s*=\\s*true" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
    ```

    각 일치 라인이 **MouseEnter / MouseMove 핸들러** 안에 있다면 → MouseLeftButtonDown 핸들러로 이동시키거나 동등하게 해당 라인을 `e.LeftButton == MouseButtonState.Pressed` 가드로 감싸기. 이미 MouseLeftButtonDown 핸들러 안에 있으면 손대지 않음.

    구체 패턴 (PATTERNS.md L320 안내):
    - **현재 (MouseEnter / MouseMove 진입만으로 _isDrawingRect=true):** 캔버스 hover 시 preview rect 생김.
    - **변경 후:** MouseLeftButtonDown (좌클릭) 이벤트에서만 _isDrawingRect=true 로 진입. MouseMove 는 *이미 _isDrawingRect=true 인 상태에서* size 갱신만.

    가능한 최소 변경 형태 (조건부 가드):
    ```csharp
    //260430 hbk Phase 17 D-05 — 좌클릭+드래그부터 그리기 시작 (캔버스 hover 미리보기 제거)
    if (e.LeftButton != MouseButtonState.Pressed) return;
    _isDrawingRect = true;
    ```

    Polygon 경로는 점 클릭 모델로 이미 LeftButtonDown 트리거 — D-05 정책 부합. 변경 불필요.

    **Step D — IsEditMode 외부 set 진입점 확인 (참고 only)**: MainView.xaml.cs 의 Edit 모드 ON/OFF 토글 버튼 또는 ContextMenu "Edit" 클릭이 `mainResultViewer.IsEditMode = true/false` 를 호출하도록 한다. 본 Task 는 setter 만 노출 — 호출자는 Task 4 (MainView) 에서 wiring.

    Brace style: K&R (PATTERNS.md S6).
  </action>
  <verify>
    <automated>
      grep -c "_isEditMode" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs && \
      grep -c "if (!_isEditMode) return null" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs && \
      msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_isEditMode" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` returns ≥ 4 (필드 + setter + getter + HitTestSelectedRoi 가드).
    - `grep "if (!_isEditMode)" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` returns at least 1 line containing `return null` (HitTestSelectedRoi 진입 가드).
    - `grep "public bool IsEditMode" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` returns 1.
    - `grep -c "//260430 hbk Phase 17 D-05" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` returns ≥ 1.
    - `grep -c "//260430 hbk Phase 17 D-06" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` returns ≥ 3.
    - msbuild Debug/x64 exit code = 0, 신규 warning 0 on MainResultViewerControl.xaml.cs.
  </acceptance_criteria>
  <done>
    _isEditMode 단일 gate 가 HitTestSelectedRoi 진입을 차단, 좌클릭 없이 hover 진입만으로 그리기 모드가 활성화되지 않음. Edit 모드 OFF 상태에서 ROI 클릭/드래그 불가, ON 시 Rect+Circle+Polygon 모두 동작.
  </done>
</task>

<task type="auto">
  <name>Task 2: DatumConfig — ICustomTypeDescriptor 구현 + IsHiddenForAlgorithm 필터 (D-08, D-09 / Plan 17-02 영역만)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (L14 클래스 선언, L341-413 transient 블록 — Plan 17-03 영역 충돌 회피용 위치 확인, L418-494 EnsurePerRoiDefaults, L496+ 생성자 / 메소드 영역 — ICustomTypeDescriptor 삽입 위치)
    - WPF_Example/Sequence/Param/ParamBase.cs (Save/Load reflection 경로 — GetProperties() 무인자 위임이 깨지지 않는지 확인)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-CONTEXT.md (D-08, D-09, code_context § "알려진 제약 PropertyTools.Wpf" 169-172)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-PATTERNS.md (DatumConfig.cs §D-09)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-UI-SPEC.md (§ "ICustomTypeDescriptor 구현" 표 — TLI/CTH/VTH 노출/숨김 그룹)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-01-SUMMARY.md (Plan 17-01 영역 확인 — Circle_RadialDirection 필드는 17-01 에서 추가 완료)
  </read_first>
  <action>
    **Step A — 클래스 선언 변경**:

    L14 (또는 실제 클래스 헤더 라인):
    ```csharp
    //260430 hbk Phase 17 D-09 — PropertyGrid 동적 노출 (AlgorithmType 별)
    public class DatumConfig : ParamBase, System.ComponentModel.ICustomTypeDescriptor {
    ```

    **Step B — ICustomTypeDescriptor 구현 블록 추가** (생성자 *직전*, 클래스 본문 끝부분 적절한 영역):

    ```csharp
    //260430 hbk Phase 17 D-09 — PropertyGrid 동적 노출 (AlgorithmType 별 필터)
    //  PropertyTools.Wpf 가 ICustomTypeDescriptor.GetProperties(Attribute[]) 를 호출 (PropertyGrid 용).
    //  ParamBase INI 직렬화는 GetProperties() 무인자 → base 위임으로 보호 (CONTEXT 172).
    public System.ComponentModel.PropertyDescriptorCollection GetProperties(System.Attribute[] attributes) {
        var all = System.ComponentModel.TypeDescriptor.GetProperties(this, attributes, true);
        var alg = AlgorithmTypeEnum;
        var keep = new System.Collections.Generic.List<System.ComponentModel.PropertyDescriptor>();
        foreach (System.ComponentModel.PropertyDescriptor pd in all) {
            if (IsHiddenForAlgorithm(pd.Name, alg)) continue; //260430 hbk Phase 17 D-09
            keep.Add(pd);
        }
        return new System.ComponentModel.PropertyDescriptorCollection(keep.ToArray());
    }
    public System.ComponentModel.PropertyDescriptorCollection GetProperties() {
        //260430 hbk Phase 17 D-09 — INI reflection 보호 (ParamBase Save/Load 경로)
        return System.ComponentModel.TypeDescriptor.GetProperties(this, true);
    }
    public System.ComponentModel.AttributeCollection GetAttributes() { return System.ComponentModel.TypeDescriptor.GetAttributes(this, true); }
    public string GetClassName() { return System.ComponentModel.TypeDescriptor.GetClassName(this, true); }
    public string GetComponentName() { return System.ComponentModel.TypeDescriptor.GetComponentName(this, true); }
    public System.ComponentModel.TypeConverter GetConverter() { return System.ComponentModel.TypeDescriptor.GetConverter(this, true); }
    public System.ComponentModel.EventDescriptor GetDefaultEvent() { return System.ComponentModel.TypeDescriptor.GetDefaultEvent(this, true); }
    public System.ComponentModel.PropertyDescriptor GetDefaultProperty() { return System.ComponentModel.TypeDescriptor.GetDefaultProperty(this, true); }
    public object GetEditor(System.Type editorBaseType) { return System.ComponentModel.TypeDescriptor.GetEditor(this, editorBaseType, true); }
    public System.ComponentModel.EventDescriptorCollection GetEvents(System.Attribute[] attributes) { return System.ComponentModel.TypeDescriptor.GetEvents(this, attributes, true); }
    public System.ComponentModel.EventDescriptorCollection GetEvents() { return System.ComponentModel.TypeDescriptor.GetEvents(this, true); }
    public object GetPropertyOwner(System.ComponentModel.PropertyDescriptor pd) { return this; }

    //260430 hbk Phase 17 D-08/D-09 — AlgorithmType 별 노출 그룹 (UI-SPEC 표)
    private static bool IsHiddenForAlgorithm(string name, EDatumAlgorithm alg) {
        switch (alg) {
            case EDatumAlgorithm.TwoLineIntersect:
                if (name.StartsWith("Circle_") || name.StartsWith("CircleROI_") || name.StartsWith("CircleCenter_") || name.StartsWith("CircleDetected_")) return true;
                if (name.StartsWith("Vertical_")) return true;
                if (name.StartsWith("Horizontal_A_") || name.StartsWith("Horizontal_B_")) return true;
                return false;
            case EDatumAlgorithm.CircleTwoHorizontal:
                if (name.StartsWith("Line1_") || name.StartsWith("Line1Detected_")) return true;
                if (name.StartsWith("Line2_") || name.StartsWith("Line2Detected_")) return true;
                if (name.StartsWith("Vertical_")) return true;
                if (name == "Circle_EdgeDirection") return true; //260430 hbk Phase 17 D-03 — Circle 분기에서 EdgeDirection hide
                return false;
            case EDatumAlgorithm.VerticalTwoHorizontal:
                if (name.StartsWith("Line1_") || name.StartsWith("Line1Detected_")) return true;
                if (name.StartsWith("Line2_") || name.StartsWith("Line2Detected_")) return true;
                if (name.StartsWith("Circle_") || name.StartsWith("CircleROI_") || name.StartsWith("CircleCenter_") || name.StartsWith("CircleDetected_")) return true;
                return false;
        }
        return false;
    }
    ```

    **Step C — 영역 충돌 방지**: Plan 17-01 의 Circle_RadialDirection 필드 블록 (이미 추가됨), Plan 17-03 의 transient 필드 블록 (DetectedOriginRow/Col/RefAngle/EdgeCount/FitRMSE/AngleDeg) **에 손대지 않는다**. 본 Task 는 **클래스 헤더 + ICustomTypeDescriptor 메소드 그룹 + IsHiddenForAlgorithm helper** 만 추가.

    **Step D — INI 보호 검증 (read-only 확인)**: ParamBase.Save/Load 가 사용하는 메소드를 grep 으로 확인:
    ```bash
    grep -n "GetProperties\\|GetType().*GetProperties" WPF_Example/Sequence/Param/ParamBase.cs
    ```
    - ParamBase 가 `obj.GetType().GetProperties(...)` (System.Reflection) 을 사용한다면 ICustomTypeDescriptor 영향 없음 (System.Reflection ≠ System.ComponentModel).
    - ParamBase 가 `TypeDescriptor.GetProperties(...)` (System.ComponentModel) 을 사용하더라도, 본 Task 의 GetProperties() 무인자가 `TypeDescriptor.GetProperties(this, true)` 로 위임하여 ICustomTypeDescriptor 자체 필터를 우회 → 안전.
    - 만약 ParamBase 가 `this.GetType().GetProperties()` (Reflection) 을 사용한다면 ICustomTypeDescriptor 와 무관하므로 우회 불필요 — 본 Task 만으로 INI 무영향.

    Reflection 경로가 발견되면 SUMMARY.md 에 명시. 본 Task 의 작업은 동일.

    Brace style: K&R (DatumConfig 기존 스타일).
  </action>
  <verify>
    <automated>
      grep -c "ICustomTypeDescriptor" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs && \
      grep -c "IsHiddenForAlgorithm" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs && \
      grep -c "GetPropertyOwner" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs && \
      msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "public class DatumConfig" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs | grep "ICustomTypeDescriptor"` returns 1 (클래스 선언이 인터페이스 구현 포함).
    - `grep -c "ICustomTypeDescriptor" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns ≥ 2.
    - `grep -c "IsHiddenForAlgorithm" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns ≥ 2 (선언 + 호출).
    - `grep -c "case EDatumAlgorithm." WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns ≥ 3 (TLI/CTH/VTH 3 분기).
    - `grep "TypeDescriptor.GetProperties(this, true)" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns ≥ 1 (GetProperties() 무인자가 base 위임).
    - `grep "Circle_EdgeDirection" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs | grep "return true"` returns ≥ 1 (D-03 hide 라인 존재).
    - `grep -c "//260430 hbk Phase 17 D-09" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns ≥ 3.
    - msbuild Debug/x64 exit code = 0, 신규 warning 0 on DatumConfig.cs.
    - 회귀 검증 (read-only): Plan 17-01 의 Circle_RadialDirection 필드 블록은 그대로 존재 — `grep -c "Circle_RadialDirection" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` returns ≥ 4 (Plan 17-01 변경 회귀 0).
  </acceptance_criteria>
  <done>
    DatumConfig 가 ICustomTypeDescriptor 를 구현하여 PropertyGrid 가 AlgorithmType 별 필드만 노출. Circle 분기에서 Circle_EdgeDirection 자동 hide (D-03). ParamBase INI Save/Load 경로 회귀 0 (GetProperties() 무인자가 base 위임).
  </done>
</task>

<task type="auto">
  <name>Task 3: InspectionListView — AlgorithmType combobox 변경 감지 + 5-step 리셋 (D-10) + Edit 모드 진입점 (D-06 wiring)</name>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs (L26-33 RefreshParamEditor, L65 OnParamEditorSelectionChanged, L348-366 Datum 노드 선택 force rebind)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-CONTEXT.md (D-10)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-PATTERNS.md (InspectionListView.xaml.cs 섹션 + MainView §D-10)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-UI-SPEC.md (§ "AlgorithmType 변경 흐름" — 5-step 시퀀스)
    - .planning/phases/16-datum-circle-strip-redesign-algorithmtype-binding-fix/16-02-SUMMARY.md (Phase 16 D-09/D-10 force rebind 패턴 — 보존 대상)
  </read_first>
  <action>
    **Step A — OnParamEditorSelectionChanged 확장** (L65 핸들러).

    PATTERNS.md MainView §D-10 의 코드 excerpt 그대로 — 단, "MainView 가 InspectionListView 의 ParamEditor 를 호스팅" 인지 "InspectionListView 자체가 OnParamEditorSelectionChanged 를 등록" 인지 grep 으로 먼저 확인:

    ```bash
    grep -n "OnParamEditorSelectionChanged\\|ParamEditor.SelectionChanged" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
    grep -n "OnParamEditorSelectionChanged\\|ParamEditor.SelectionChanged" WPF_Example/UI/ContentItem/MainView.xaml.cs
    ```

    핸들러가 거주하는 파일에 다음 분기 추가 (현재 파일이 InspectionListView 라고 가정 — 다르면 MainView 로 위치 조정):

    ```csharp
    private void OnParamEditorSelectionChanged(object sender, SelectionChangedEventArgs e) {
        if (!(e.OriginalSource is ComboBox)) return;
        //260430 hbk Phase 17 D-10 — AlgorithmType combobox 변경 감지 (5-step 리셋)
        var datum = ParamEditor != null ? ParamEditor.SelectedObject as DatumConfig : null;
        if (datum == null) return;
        // ComboBox 의 SelectedValue 가 AlgorithmType 문자열인지 확인 — 다른 string ComboBox (EdgeDirection 등) 와 구분
        var combo = e.OriginalSource as ComboBox;
        if (combo == null) return;
        string newValue = combo.SelectedValue as string;
        // AlgorithmType 식별 — DatumConfig.AlgorithmType 의 현재 값과 ComboBox SelectedValue 가 일치하면 AlgorithmType combobox
        if (string.Equals(newValue, datum.AlgorithmType, System.StringComparison.Ordinal)
            && (newValue == "TwoLineIntersect" || newValue == "CircleTwoHorizontal" || newValue == "VerticalTwoHorizontal")) {
            // Step 1: force rebind (PropertyDescriptor 재계산 + ICustomTypeDescriptor 신규 필터 적용)
            ParamEditor.SelectedObject = null;
            ParamEditor.SelectedObject = datum;
            // Step 2: 검출 상태 reset
            datum.LastTeachSucceeded = false; //260430 hbk Phase 17 D-10
            datum.LastFindSucceeded  = false; //260430 hbk Phase 17 D-10
            // Step 3: DetectedOrigin 시각화 clear (Plan 17-03 신규 필드 0 리셋 — Plan 17-03 가 이 필드들을 정의)
            // 본 Plan 시점에 이 필드들이 아직 DatumConfig 에 없으므로 try/catch 없이 직접 0 대입 시 컴파일 실패.
            // 해결: Plan 17-03 의존이지만 정확한 wave 순서 (17-02 → 17-03) 보장을 위해 본 라인은 Plan 17-03 에서 wiring.
            // 본 Task 에서는 LastTeachSucceeded/LastFindSucceeded reset 만 수행 — DetectedOrigin* 0 리셋은 Plan 17-03 가 본 핸들러에 추가 라인으로 wiring.
            // Step 4: ROI 보존 — 명시적 액션 없음 (DatumConfig 의 ROI 필드 미수정).
            // Step 5: 자동 재검출 없음 — Phase 16 D-13/D-14 보존, 별도 액션 없음.
            // Step 6: 캔버스 시각화 갱신 (RenderDatumOverlay 가 LastTeachSucceeded=false 분기에서 자동 미렌더)
            mParentWindow?.mainView?.halconViewer?.SetDatumOverlay(datum, true);
        }
        // 기타 ComboBox 변경 (EdgeDirection / EdgeSelection / RadialDirection 등): 본 핸들러 추가 동작 없음
    }
    ```

    > **Plan 의존성 노트:** Plan 17-03 가 DetectedOrigin* 필드를 추가한 뒤 본 핸들러의 Step 3 자리에 다음 라인을 추가한다 (Plan 17-03 Task에서 명시):
    > ```csharp
    > datum.DetectedOriginRow = 0;
    > datum.DetectedOriginCol = 0;
    > datum.DetectedRefAngle  = 0;
    > ```

    **Step B — Datum 노드 선택 force rebind 보존**: L348-366 의 Phase 16-02 force rebind 블록 (`ParamEditor.SelectedObject = null; ParamEditor.SelectedObject = datumCfg;`) 에 손대지 않는다. ICustomTypeDescriptor 가 새 SelectedObject 할당 시 자동으로 GetProperties(Attribute[]) 재호출되어 AlgorithmType-aware 필터 적용 — 별도 wiring 불필요.

    **Step C — RefreshParamEditor 보존**: L26-33 패턴 보존. 본 Task 가 추가한 5-step 리셋이 RefreshParamEditor 와 동일 효과를 노출 — 코드 중복은 의도적 (Phase 16 패턴 보존 + 신규 AlgorithmType 분기 명시성).

    Brace style: K&R.
  </action>
  <verify>
    <automated>
      grep -c "AlgorithmType" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs && \
      grep -c "LastTeachSucceeded = false" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs && \
      msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AlgorithmType" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` returns ≥ 3 (newValue 비교 + datum.AlgorithmType 비교 + 분기).
    - `grep "LastTeachSucceeded = false" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` returns 1.
    - `grep "LastFindSucceeded = false" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` returns 1.
    - `grep "ParamEditor.SelectedObject = null" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` returns ≥ 2 (Phase 16 Datum 노드 force rebind 보존 + Phase 17 AlgorithmType 분기).
    - `grep "TwoLineIntersect.*CircleTwoHorizontal.*VerticalTwoHorizontal\\|VerticalTwoHorizontal" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` returns ≥ 1 (3-알고리즘 가드).
    - `grep -c "//260430 hbk Phase 17 D-10" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` returns ≥ 2.
    - msbuild Debug/x64 exit code = 0, 신규 warning 0 on InspectionListView.xaml.cs.
    - 회귀 (Phase 16): `grep "ParamEditor.SelectedObject = datumCfg\\|ParamEditor.SelectedObject = current" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` returns ≥ 1 (Phase 16 D-09/D-10 force rebind 라인 보존 — 회귀 0).
  </acceptance_criteria>
  <done>
    AlgorithmType combobox 변경 시 PropertyGrid force rebind + LastTeachSucceeded/LastFindSucceeded reset + 캔버스 갱신. ROI 보존, 자동 재검출 없음 (Phase 16 D-13/D-14 정책 유지). DetectedOrigin* 0 리셋은 Plan 17-03 가 본 핸들러에 추가 라인으로 wiring.
  </done>
</task>

<task type="auto">
  <name>Task 4: MainView — Delete 3-button 모달 + btn_teachDatum 호환성 가드 + teach 실패 모달 변환 (D-07, D-11, D-12)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (L495-509 HalconViewer_RoiDeleteRequested, L1258-1296 TeachDatumButton_Click region, L1484-1492 InvokeTryTeachDatum 실패 분기, L1497-1532 BtnTestFindDatum_Click — 본 Plan 은 실패 모달 변환만, Plan 17-03 가 본 핸들러 본격 변경)
    - WPF_Example/UI/Dialog/CustomMessageBox.cs (Show + ShowConfirmation 시그니처 — 3-button 옵션 확인)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-CONTEXT.md (D-07, D-11, D-12)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-PATTERNS.md (MainView.xaml.cs 섹션 §D-07/D-11/D-12, gap #3 — 3-button modal API 결정)
    - .planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-UI-SPEC.md (Copywriting Contract § "Delete ROI 모달", § "실패 모달 — teach", § "실패 모달 — Test Find")
    - .planning/phases/13-datum-algorithm-extensibility/13-03-SUMMARY.md (CustomMessageBox (title, message) swap 패턴)
  </read_first>
  <action>
    **Step A — 3-button modal API 결정 (PATTERNS.md gap #3): 옵션 (a) YesNoCancel 재사용**.

    근거: MessageBoxButton 표준 enum 에 3-way 직접 매핑 가능, CustomMessageBox 본체 변경 0, 회귀 위험 최소.

    매핑:
    - Yes → "이 ROI만 삭제"
    - No  → "현재 Datum의 모든 ROI 삭제"
    - Cancel → "취소"

    UI-SPEC Copywriting 의 모달 message 본문에 [예]/[아니오]/[취소] 가 어떤 동작인지 명시 텍스트로 안내.

    **Step B — HalconViewer_RoiDeleteRequested 확장** (L495-509):

    ```csharp
    private void HalconViewer_RoiDeleteRequested(object sender, ...EventArgs e) {
        // ... 기존 datum/roiId 추출 코드 그대로 ...
        //260430 hbk Phase 17 D-07 — Delete 모달 (단일 / 전체 / 취소)
        var choice = CustomMessageBox.ShowConfirmation(
            "ROI 삭제",
            "선택한 ROI 를 삭제하시겠습니까?\n\n[예] 이 ROI만 삭제\n[아니오] 현재 Datum 의 모든 ROI 삭제\n[취소] 취소",
            MessageBoxButton.YesNoCancel);
        if (choice == MessageBoxResult.Cancel || choice == MessageBoxResult.None) return;
        if (choice == MessageBoxResult.Yes) {
            ClearDatumRoiFields(datum, roiId); //260430 hbk Phase 17 D-07
        } else {
            ClearAllDatumRoiFields(datum); //260430 hbk Phase 17 D-07
        }
        // 기존 후처리 (RaisePropertyChanged + RefreshParamEditor + SetDatumOverlay 트리플) 보존
    }
    ```

    **Step C — ClearAllDatumRoiFields helper 추가** (D-07):

    ```csharp
    //260430 hbk Phase 17 D-07 — 현재 Datum 의 모든 ROI 필드 0 리셋
    private static void ClearAllDatumRoiFields(DatumConfig datum) {
        if (datum == null) return;
        // Line1 / Line2 / Vertical / Circle / Horizontal_A / Horizontal_B 의 ROI 좌표 필드를 0 으로 (또는 sentinel) 리셋
        // ClearDatumRoiFields 의 기존 분기를 6 ROI 모두 호출하는 형태로 reuse
        ClearDatumRoiFields(datum, "Datum.Line1");
        ClearDatumRoiFields(datum, "Datum.Line2");
        ClearDatumRoiFields(datum, "Datum.Vertical");
        ClearDatumRoiFields(datum, "Datum.Circle");
        ClearDatumRoiFields(datum, "Datum.Horizontal_A");
        ClearDatumRoiFields(datum, "Datum.Horizontal_B");
    }
    ```

    실제 RoiId prefix 형태는 Phase 13-03 SUMMARY 의 `RoiId.StartsWith("Datum.")` 패턴 grep 으로 정확값 확인:
    ```bash
    grep -n "Datum\\." WPF_Example/UI/ContentItem/MainView.xaml.cs | head -20
    ```

    **Step D — btn_teachDatum 호환성 가드 (D-11)**: TeachDatumButton_Click region (L1258-1296) 에서 `_datumTeachStep = GetFirstStep(...)` 직후 다음 라인 추가:

    ```csharp
    //260430 hbk Phase 17 D-11 — 새 알고리즘이 요구하는 ROI 슬롯 비어 있으면 친절한 에러
    string missingRoiMsg = ValidateRoiPresence(datum, datum.AlgorithmTypeEnum);
    if (missingRoiMsg != null) {
        CustomMessageBox.Show("티칭 실패", missingRoiMsg);
        btn_teachDatum.IsChecked = false;
        _canvasMode = ECanvasMode.None;
        return;
    }
    ```

    Helper:

    ```csharp
    //260430 hbk Phase 17 D-11 — UI-SPEC Copywriting Contract § "실패 모달 — teach"
    private static string ValidateRoiPresence(DatumConfig d, EDatumAlgorithm alg) {
        switch (alg) {
            case EDatumAlgorithm.TwoLineIntersect:
                if (d.Line1_Length1 <= 0 || d.Line2_Length1 <= 0)
                    return "Line1/Line2 ROI 가 없습니다. 캔버스에 ROI 를 그리고 다시 시도하세요.";
                break;
            case EDatumAlgorithm.CircleTwoHorizontal:
                if (d.CircleROI_Radius <= 0)
                    return "Circle ROI 가 없습니다. 캔버스에 원을 그리고 다시 시도하세요.";
                if (d.Horizontal_A_Length1 <= 0 || d.Horizontal_B_Length1 <= 0)
                    return "Horizontal A/B ROI 가 없습니다. 캔버스에 ROI 를 그리고 다시 시도하세요.";
                break;
            case EDatumAlgorithm.VerticalTwoHorizontal:
                if (d.Vertical_Length1 <= 0)
                    return "Vertical ROI 가 없습니다. 캔버스에 수직 ROI 를 그리고 다시 시도하세요.";
                if (d.Horizontal_A_Length1 <= 0 || d.Horizontal_B_Length1 <= 0)
                    return "Horizontal A/B ROI 가 없습니다. 캔버스에 ROI 를 그리고 다시 시도하세요.";
                break;
        }
        return null;
    }
    ```

    필드명 (Line1_Length1 / CircleROI_Radius / Horizontal_A_Length1 / Vertical_Length1) 은 grep 으로 실제 DatumConfig 필드와 정합 확인:
    ```bash
    grep -n "Line1_Length1\\|CircleROI_Radius\\|Horizontal_A_Length1\\|Vertical_Length1" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
    ```
    필드명이 다르면 (예: Line1_Length / Vertical_L1 등) 실제 필드명으로 수정.

    **Step E — InvokeTryTeachDatum 실패 모달 변환 (D-12)**: L1484-1492 의 `label_drawHint.Content = "Datum 티칭 실패: " + error;` 패턴 → CustomMessageBox 호출:

    ```csharp
    //260430 hbk Phase 17 D-12 — teach 실패 사유 모달 (성공 시 모달 X)
    CustomMessageBox.Show("티칭 실패", FormatTeachError(error));
    ```

    Helper (D-04 검출 0 힌트 통합):

    ```csharp
    //260430 hbk Phase 17 D-12 + D-04 — 검출 0개 힌트 통합
    private static string FormatTeachError(string err) {
        if (err == null) err = "unknown";
        if (err.IndexOf("no edges", System.StringComparison.OrdinalIgnoreCase) >= 0
            || err.IndexOf("insufficient edges", System.StringComparison.OrdinalIgnoreCase) >= 0
            || err.IndexOf("insufficient polar samples", System.StringComparison.OrdinalIgnoreCase) >= 0) {
            return "검출된 에지가 없습니다. EdgeDirection 설정을 반대로 변경한 후 다시 시도하세요.";
        }
        return "티칭에 실패했습니다: " + err;
    }
    ```

    **Step F — Test Find 실패 모달 변환 (D-12, BtnTestFindDatum_Click 의 실패 분기)**: L1527 `label_testFindResult.Content = "TryFind FAIL — " + error;` → 같은 패턴으로 CustomMessageBox.Show 호출 + FormatFindError helper:

    ```csharp
    private static string FormatFindError(string err) {
        if (err == null) err = "unknown";
        if (err.IndexOf("no edges", System.StringComparison.OrdinalIgnoreCase) >= 0
            || err.IndexOf("insufficient edges", System.StringComparison.OrdinalIgnoreCase) >= 0
            || err.IndexOf("insufficient polar samples", System.StringComparison.OrdinalIgnoreCase) >= 0) {
            return "검출된 에지가 없습니다. EdgeDirection 설정을 반대로 변경한 후 다시 시도하세요.";
        }
        return "Datum Find 에 실패했습니다: " + err;
    }
    ```

    > **Plan 17-03 와 boundary**: BtnTestFindDatum_Click 의 *성공 경로* (TryFindDatum 호출 + transient write-back + RenderDatumFindResult 호출) 는 Plan 17-03 의 책임. 본 Plan 은 *실패 모달 메시지 변환 라인만* 변경.

    **Step G — IsEditMode wiring**: MainView 의 Edit 모드 토글 진입점 (예: btn_edit / 컨텍스트 메뉴 Edit 클릭 핸들러) 을 grep 으로 식별:
    ```bash
    grep -n "_isEditMode\\|IsEditMode\\|btn_edit\\|EditMode" WPF_Example/UI/ContentItem/MainView.xaml.cs
    ```
    해당 핸들러에서 `mainResultViewer.IsEditMode = true/false` 호출 추가. 만약 Edit 토글 UI 가 아직 없다면 (Phase 14-01 분석) 본 Task 는 *프로퍼티 설정 라인만* 추가하고 UI 트리거는 기존 MainView 의 Edit 진입 진입점 (예: btn_teachDatum off 시 false, ContextMenu "Edit" 클릭 시 true) 에 wired.

    Brace style: K&R.
  </action>
  <verify>
    <automated>
      grep -c "ShowConfirmation.*YesNoCancel\\|MessageBoxButton.YesNoCancel" WPF_Example/UI/ContentItem/MainView.xaml.cs && \
      grep -c "ValidateRoiPresence\\|FormatTeachError\\|FormatFindError" WPF_Example/UI/ContentItem/MainView.xaml.cs && \
      grep -c "ClearAllDatumRoiFields" WPF_Example/UI/ContentItem/MainView.xaml.cs && \
      msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "MessageBoxButton.YesNoCancel" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 1 (Delete 3-button 모달).
    - `grep -c "이 ROI만 삭제\\|현재 Datum 의 모든 ROI 삭제" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 1 (UI-SPEC Copywriting 한국어 verbatim).
    - `grep "ValidateRoiPresence" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 2 (helper 선언 + 호출).
    - `grep "ClearAllDatumRoiFields" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 2 (helper 선언 + 호출).
    - `grep "FormatTeachError\\|FormatFindError" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 4 (양쪽 helper 선언 + 호출).
    - `grep -c "검출된 에지가 없습니다. EdgeDirection 설정을 반대로" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 2 (D-04 힌트 — teach + find 양쪽).
    - `grep -c "CustomMessageBox.Show.*티칭 실패\\|CustomMessageBox.Show.*Find 실패" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 2.
    - `grep "label_drawHint.Content.*Datum 티칭 실패" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns 0 (label_drawHint 사유 표시 패턴 제거 확인).
    - `grep "IsEditMode\\s*=" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 1 (Edit 모드 진입점 wiring).
    - `grep -c "//260430 hbk Phase 17 D-07" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 2.
    - `grep -c "//260430 hbk Phase 17 D-11" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 2.
    - `grep -c "//260430 hbk Phase 17 D-12" WPF_Example/UI/ContentItem/MainView.xaml.cs` returns ≥ 2.
    - msbuild Debug/x64 exit code = 0, 신규 warning 0 on MainView.xaml.cs.
  </acceptance_criteria>
  <done>
    Delete 우클릭 → 3-button 모달 → ClearDatumRoiFields / ClearAllDatumRoiFields / 취소 분기. btn_teachDatum 클릭 시 ROI 부재 친절한 에러 모달. teach + Test Find 실패 시 CustomMessageBox 사유 모달 (성공 시 모달 없음). 검출 0 케이스에 EdgeDirection 힌트. IsEditMode wired.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → CustomMessageBox | 로컬 사용자가 모달의 [예]/[아니오]/[취소] 버튼 선택. |
| User → PropertyGrid (AlgorithmType) | 로컬 사용자가 ComboBox 에서 string AlgorithmType 선택. |
| User → ContextMenu Delete | 로컬 사용자가 우클릭 → Delete 메뉴 선택. |
| MainResultViewerControl → MainView IsEditMode | UI 컨트롤 간 프로퍼티 노출 (intra-process). |

신규 trust boundary 없음. 네트워크/파일/저장소 미관여.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Tampering | DatumConfig.AlgorithmType (string) | mitigate | InspectionListView.OnParamEditorSelectionChanged 가 newValue 를 {"TwoLineIntersect","CircleTwoHorizontal","VerticalTwoHorizontal"} whitelist 로 가드. 비매칭 string 은 분기 진입 안 함 (Phase 12-01 fallback "TwoLineIntersect" 그대로 동작). |
| T-17-02-02 | Tampering | ICustomTypeDescriptor.GetProperties | mitigate | GetProperties() 무인자가 base TypeDescriptor 위임 — INI Save/Load 우회 위험 0. 필터 화이트리스트는 enum switch — 비매칭 alg 시 모든 필드 노출 (안전 default). |
| T-17-02-03 | DoS | OnParamEditorSelectionChanged 5-step | accept | 1 회 ComboBox change 당 1 회 실행. 자동 재검출 없음 (D-13/D-14 보존) → HALCON 호출 0. |
| T-17-02-04 | Repudiation | Delete 3-button 모달 | mitigate | 사용자 의도 분리 (단일 vs 전체 vs 취소) — 한 번의 클릭으로 의도하지 않은 전체 삭제 방지. Cancel 이 default-safe (None 도 동일 처리). |
| T-17-02-05 | Tampering | _isEditMode boolean | mitigate | UI-only flag, intra-process. Edit 모드 OFF 가 default → 의도하지 않은 ROI 변형 방지. |
| T-17-02-06 | Information Disclosure | 실패 모달 메시지 | accept | DatumFindingService 의 error 문자열을 그대로 노출 — HALCON 내부 메시지 (\"insufficient polar samples\" 등) 외에 기밀 정보 미포함. |

No high-severity threats. Algorithm preservation (D-17) 강제 — DatumFindingService.cs / VisionAlgorithmService.cs 0 라인 변경.
</threat_model>

<verification>
Phase-level checks:

```bash
# 알고리즘 보존 (D-17)
git diff WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs | wc -l  # = 0
# Plan 17-01 이후 누적: DatumFindingService.cs 추가 라인 ≤ 2 (Plan 17-01 범위) — Plan 17-02 는 0 라인
git diff --stat WPF_Example/Halcon/Algorithms/DatumFindingService.cs       # Plan 17-01 baseline 외 추가 변경 없음

# Plan 17-02 영역 변경 카운트
grep -c "ICustomTypeDescriptor" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs                 # ≥ 2
grep -c "_isEditMode" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs                     # ≥ 4
grep -c "MessageBoxButton.YesNoCancel" WPF_Example/UI/ContentItem/MainView.xaml.cs                   # ≥ 1
grep -c "AlgorithmType" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs                        # ≥ 3

# hbk 주석 카운트
grep -c "//260430 hbk Phase 17 D-0[5-9]\\|//260430 hbk Phase 17 D-1[0-2]" WPF_Example/UI/ContentItem/MainView.xaml.cs                     # ≥ 8
grep -c "//260430 hbk Phase 17 D-09" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs                                                  # ≥ 3
grep -c "//260430 hbk Phase 17 D-10" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs                                                  # ≥ 2

# 빌드
msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64

# Phase 16 회귀 (보존 대상)
grep "ParamEditor.SelectedObject = null" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs       # ≥ 2 라인 (16-02 Datum 노드 force rebind + 17-02 AlgorithmType 분기)
# Auto-reteach off 보존
grep "InvokeTryTeachDatumForEdit\\|HandleDatumRoiMove\\|HandleDatumRoiResize" WPF_Example/UI/ContentItem/MainView.xaml.cs
# → 자동 재티칭 호출 라인이 *추가되지 않았음* 을 확인 (Phase 16 D-13/D-14 보존)

# Plan 17-03 영역 미침범
grep -c "DetectedOriginRow\\|DetectedOriginCol\\|DetectedRefAngle\\|DetectedEdgeCount\\|DetectedFitRMSE\\|DetectedAngleDeg" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs  # = 0 (Plan 17-03 영역)
```

회귀 검증 (수동, UAT 17-04 가 정식 검증):
- TLI Datum 선택 시 PropertyGrid 에 Line1_*/Line2_* 만 노출, Circle_*/Vertical_*/Horizontal_*/Circle_EdgeDirection 비노출.
- CTH Datum 선택 시 Circle_* (RadialDirection 포함, Circle_EdgeDirection 제외) + Horizontal_A/B_* 만 노출.
- VTH Datum 선택 시 Vertical_*/Horizontal_A/B_* 만 노출.
- AlgorithmType combobox 변경 시 PropertyGrid 즉시 갱신 + LastTeachSucceeded=false (검출 시각화 사라짐).
- 우클릭 → Delete → 3-button 모달, Yes/No/Cancel 동작 확인.
- btn_teachDatum 클릭 (CTH 인데 Circle ROI 없음) → "Circle ROI 가 없습니다. 캔버스에 원을 그리고 다시 시도하세요." 모달.
- teach 실패 (검출 0 케이스) → "검출된 에지가 없습니다. EdgeDirection 설정을 반대로 변경한 후 다시 시도하세요." 모달.
</verification>

<success_criteria>
- 모든 must_haves.truths 가 grep + msbuild 로 검증 가능.
- DatumConfig.cs 클래스 선언이 ICustomTypeDescriptor 구현 포함.
- IsHiddenForAlgorithm 가 3 개 enum 분기 (TLI/CTH/VTH) 보유, Circle 분기에서 Circle_EdgeDirection 명시 hide.
- MainResultViewerControl._isEditMode 단일 gate 가 HitTestSelectedRoi 진입을 차단.
- 좌클릭+드래그 시작 패턴 정정 (MouseEnter/MouseMove preview 진입 제거).
- Delete 우클릭 → CustomMessageBox YesNoCancel 3-button 모달.
- btn_teachDatum 호환성 가드 (ValidateRoiPresence) + 한국어 친절한 에러.
- teach + Test Find 양쪽 실패 모달 (FormatTeachError / FormatFindError) + EdgeDirection 0 검출 힌트.
- AlgorithmType combobox 변경 시 PropertyGrid force rebind + LastTeachSucceeded reset + ROI 보존 + 자동 재검출 없음.
- DatumFindingService.cs / VisionAlgorithmService.cs 0 라인 변경 (Plan 17-01 baseline 보존).
- msbuild Debug/x64 PASS, 신규 warning 0.
- Plan 17-03 영역 (DetectedOrigin* 필드 / write-back / RenderDatumFindResult / hover TextBlocks) 미침범.
</success_criteria>

<output>
After completion, create `.planning/phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-02-SUMMARY.md` with:
- 변경 파일 4개 + 라인 카운트
- ICustomTypeDescriptor 통합 결과 (PropertyGrid AlgorithmType 별 필드 노출 검증)
- ParamBase INI reflection 경로 회귀 검증 결과 (Reflection vs ComponentModel 경로 분석)
- D-17 acceptance bound 실측치 (DatumFindingService 추가 0 / VisionAlgorithmService 0)
- Phase 16 D-09/D-10/D-13/D-14 보존 검증
- 다음 단계: Plan 17-03 (Cluster D — DetectedOrigin transient + RenderDatumFindResult + Hover)
</output>
