---
phase: 17
slug: datum-ux-circle-strip-1-test-find-detectedorigin-hover
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-30
platform: WPF / .NET Framework 4.8
---

# Phase 17 — UI Design Contract

> Visual and interaction contract for WPF desktop phases.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> NOTE: This is a WPF desktop application. All values are in WPF Device-Independent Pixels (DIP).
> No CSS, no Tailwind, no design tokens. Primitives are WPF XAML controls and HALCON Disp* calls.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (WPF native XAML) |
| Preset | not applicable |
| Component library | PropertyTools.Wpf (PropertyGrid), WPF.MDI, HalconDotNet HWindow |
| Icon library | /Resource/*.png (local) |
| Font | System default (Segoe UI) — no custom font declared |

---

## Spacing Scale

WPF Margin/Padding values. All existing toolbar elements use these constants — do not introduce new values.

| Token | Value (DIP) | Usage |
|-------|-------------|-------|
| xs | 4 | Icon gap, separator horizontal margin |
| sm | 8 | canvasToolbar Padding horizontal |
| md | 12 | canvasToolbar Padding horizontal (label_pos) |
| toolbar-h | 36 | canvasToolbar Height (locked — existing) |
| btn-h | 28 | All toolbar ToggleButton/Button Height (locked — existing) |
| btn-pad | 12,4 | All toolbar button Padding (locked — existing) |
| btn-gap | 4 | Margin right between toolbar buttons (locked — existing) |
| toolbar-pad | 8,4 | Border canvasToolbar Padding (locked — existing) |

Exceptions:
- hover TextBlock group: `Margin="0,0,8,0"` right of hover display, consistent with label_pos right margin.
- PropertyGrid category header: FontSize="14" Bold (locked — existing InspectionListView template).
- Delete modal 3-button layout: uses CustomMessageBox built-in button layout (no custom spacing).

---

## Typography

WPF TextBlock/Label properties. Use values that match the existing toolbar convention exactly.

| Role | FontSize | FontWeight | Usage |
|------|----------|------------|-------|
| Toolbar button label | 13 | SemiBold | btn_teachDatum, btn_testFindDatum, btn_rectRoi, btn_calibrate — locked existing |
| Toolbar info text | 13 | Normal | label_drawHint, label_testFindResult, hover TextBlock group — match existing labels |
| Canvas bottom label | 13 | Normal | label_datumRefCoords — locked existing |
| Canvas pos label | 16 | Normal | label_pos — locked existing |
| PropertyGrid category header | 14 | Bold | InspectionListView CategoryControlTemplate — locked existing |
| PropertyGrid field label | 12 | Normal | PropertyTools.Wpf default — do not override |
| Modal dialog | System default | Normal | CustomMessageBox — no override |

Line height: WPF auto (no explicit LineHeight set anywhere in existing toolbar/label code).

---

## Color

### WPF UI Surfaces

| Role | Value | Usage |
|------|-------|-------|
| Toolbar background | #111827 | canvasToolbar Border Background — locked existing |
| Canvas background | #FF303030 | Canvas Grid Background — locked existing |
| Button active/checked | #2563EB | ToggleButton IsChecked=True background — locked existing |
| Button active border | #1D4ED8 | ToggleButton IsChecked=True border — locked existing |
| Button hover | #3A3A4A | ToggleButton IsMouseOver background — locked existing |
| Button default foreground | #FFAAAAAA | ToggleButton default foreground — locked existing |
| Button disabled foreground | #FF666666 | ToggleButton IsEnabled=False — locked existing |
| Info label foreground | #FFCBD5E0 | label_datumRefCoords foreground — locked existing |
| Hover TextBlock foreground | #FFAAAAAA | Same as label_drawHint — match existing toolbar info text |

### HALCON Overlay Color Palette

Full palette audit (do not collide with existing assignments):

| Color | HALCON value | Existing assignment |
|-------|-------------|---------------------|
| cyan | "cyan" | Line1 detected 외삽선, Line1 raw edges, ROI overlay (selected) |
| magenta | "magenta" | Line2 raw edges, Datum Origin reference cross (IsConfigured) |
| yellow | "yellow" | Line1 detected line / Circle center cross (size=12) / calibration points |
| red | "red" | teach-success intersection cross / draft draw / Circle ROI center marker |
| green | "green" | Horizontal A raw edges / fitted line / calibration connecting line |
| light green | "#90EE90" | Detected circle outline (CircleTwoHorizontal) / Horizontal B raw edges |
| gray | "gray" | Circle raw edges (size=4) / Circle strip overlay (pre-teach) |
| orange | "orange" | Vertical raw edges / existing RenderDatumFindResult (Phase 13 D-07 teach-origin cross) |
| blue | "blue" | ROI overlay (unselected) / Datum overlay (unselected) |
| white | "white" | Direction arrow |

**DetectedOrigin 시각화 색상 결정 (Claude's Discretion — LOCKED HERE):**

`RenderDatumFindResult` (신규, D-13)의 DetectedOrigin DispCross 색상 = **"purple"**

근거:
- orange 는 이미 Vertical raw edges + 기존 Phase 13 RenderDatumFindResult teach-origin cross 양쪽에서 사용 중 (충돌).
- purple 은 현재 팔레트 미사용 — 유일하게 비어 있는 고대비 색상.
- cyan 은 Line1 선과 충돌.
- magenta 는 Datum Origin reference cross 와 충돌 우려.

| Element | HALCON color | Line width | Size |
|---------|-------------|------------|------|
| DetectedOrigin DispCross | "purple" | 2 | 14 px |
| DetectedOrigin 좌표 텍스트 label | "purple" | — | font-18 (EnsureFontInitialized 기준) |
| 기준각(DetectedRefAngle) 방향 화살표 | "purple" | 2 | arrowHead 5px (DrawDirectionArrow 패턴) |

**DispCross size 결정 (Claude's Discretion — LOCKED HERE):** size = **14**

근거: D-13 에서 14 권장. 기존 Circle center cross (12), calibration cross (20), intersection cross (half=20) 과 구분. 12~16 허용 범위 중간값.

---

## Component Inventory

### Cluster A — Circle 시각화 + EdgeDirection

#### RenderCircleStripOverlay (HalconDisplayService.cs)
- stepCount 루프 제거 → `thetaRad = 0.0` 단일 호출만 남김
- 색상: "gray", LineWidth=1 (기존 유지)
- 조건: `datum.CircleROI_Radius > 0` 시에만 (기존 가드 유지)
- RectL1Ratio/RectL2Ratio 변경 시 RenderDatumOverlay 를 통해 즉시 갱신 (Phase 16 D-03 패턴 유지)

#### Circle_RadialDirection PropertyGrid 필드 (DatumConfig.cs)
- Type: string (sentinel `""` = unset, fallback "Inward")
- PropertyGrid 표시명: `Circle_RadialDirection`
- Category: 기존 `"Circle"` 카테고리
- Allowed values (EdgeOptionLists.RadialDirections): `["Inward", "Outward"]`
- ComboBox 형식 (PropertyTools.Wpf 자동 — ItemsSource 를 attribute 로 지정)
- [Browsable(true)] — AlgorithmType=CircleTwoHorizontal 일 때만 동적 노출 (D-09 ICustomTypeDescriptor)
- INI 저장/로드: ParamBase reflection 자동 처리

#### Circle_EdgeDirection 동적 숨김
- AlgorithmType=CircleTwoHorizontal 일 때 ICustomTypeDescriptor.GetProperties 에서 `Circle_EdgeDirection` 제거
- INI 필드 자체는 삭제하지 않음 (하위호환)

#### EdgeDirection ComboBox (모든 ROI 슬롯)
- ItemsSource: `["LtoR", "RtoL", "TtoB", "BtoT"]` 4개 모두 활성
- Tooltip: `"일반적으로 수평 방향 ROI 에는 LtoR/RtoL 을 권장합니다"` (PropertyTools.Wpf Description attribute 경유)

### Cluster B — Edit 모드 + 그리기 UX

#### 그리기 진입 상태 (MainView.xaml.cs)
- btn_teachDatum ON + **MouseLeftButtonDown** 이벤트에서 드래그 시작
- MouseMove: 크기 확장 (preview shape)
- MouseLeftButtonUp: 확정 (HalconViewer_DatumRect/CircleCompleted 경로)
- 캔버스 진입만으로는 미리보기 도형 안 그림 (기존 MouseEnter 진입 동작 제거)

#### _isEditMode 단일 gate (MainView.xaml.cs)
- `private bool _isEditMode` flag
- HitTestOneRoi 진입 조건: `if (!_isEditMode) return null;`
- Rect + Circle + Polygon 모두 동일 gate 적용
- Edit 모드 진입/해제: 기존 패턴 (btn_teachDatum / Edit 버튼 연동) 그대로

#### Delete ROI 모달 (CustomMessageBox)
- 진입: 우클릭 ContextMenu "Delete" 메뉴아이템 클릭
- 시그니처: `CustomMessageBox(title, message, buttons)` — Phase 13-03 패턴
- 구체적 XAML/코드 결정: Copywriting Contract 섹션 참조

### Cluster C — PropertyGrid 동적 노출

#### ICustomTypeDescriptor 구현 (DatumConfig.cs)
- `GetProperties(Attribute[])` override: AlgorithmType 검사 후 PropertyDescriptor 필터링
- `GetProperties()` 무인자: `base` 위임 (ParamBase INI reflection 보호)
- 필터 규칙:

| AlgorithmType | 노출 그룹 | 숨김 그룹 |
|---------------|-----------|-----------|
| TwoLineIntersect | Line1_*, Line2_* | Circle_*, Vertical_*, Horizontal_A_*, Horizontal_B_* |
| CircleTwoHorizontal | Circle_* (RadialDirection 포함), Horizontal_A_*, Horizontal_B_* | Line1_*, Line2_*, Vertical_*, Circle_EdgeDirection |
| VerticalTwoHorizontal | Vertical_*, Horizontal_A_*, Horizontal_B_* | Line1_*, Line2_*, Circle_* |

- 기존 compile-time [Browsable(false)] (SourceShotName, raw edge HTuples 등) 보존 — ICustomTypeDescriptor 와 별개 layer

#### AlgorithmType 변경 흐름 (D-10)
1. PropertyGrid 즉시 갱신: SelectedObject null→new force rebind (Phase 16 D-09/D-10 패턴 재사용)
2. LastTeachSucceeded = false 리셋
3. DetectedOriginRow/Col / DetectedRefAngle 시각화 clear (RenderDatumOverlay 가 LastFindSucceeded=false 분기 안 그림)
4. ROI 자체 보존 (삭제 안 함)
5. 자동 재검출 안 함

#### AlgorithmType 변경 시 호환성 가드 (D-11)
- ROI 자동 삭제 안 함
- btn_teachDatum 클릭 시점에 새 알고리즘이 요구하는 ROI 슬롯 비어 있으면 실패 모달 표시
- 변경 즉시 경고 모달: **없음** (btn_teachDatum 시점에만 — 사용자가 ROI 를 먼저 그릴 기회 보장)

**AlgorithmType 변경 시 즉시 경고 표시 여부 결정 (Claude's Discretion — LOCKED HERE):** btn_teachDatum 클릭 시점 only. 변경 즉시 경고 모달 없음.

근거: ROI 가 이미 그려져 있으면 호환성 문제가 없음. 사용자가 AlgorithmType 변경 후 ROI 를 추가로 그리는 워크플로우를 방해하지 않기 위해 즉시 경고를 생략하고 teach 시점에 최종 검증.

#### 모달 정책 (D-12)
- 성공: 모달 없음. HalconDisplayService 시각화 결과만 표시.
- 실패: CustomMessageBox 로 구체적 에러 메시지 표시 (teach + find 양쪽 동일).

### Cluster D — 신규 UI 기능

#### btn_testFindDatum 배치 결정 (Claude's Discretion — LOCKED HERE)

**기존 MainView 툴바의 `btn_testFindDatum` 버튼 재사용 (신규 버튼 추가 안 함)**

근거:
- 현재 MainView.xaml line 99: `<Button x:Name="btn_testFindDatum" Content="Test Find" ... />` 이미 존재.
- InspectionListView 우클릭 메뉴 추가 시 우클릭 대상 노드 컨텍스트 추출 로직 필요 → 복잡도 증가.
- 기존 버튼이 이미 올바른 위치(툴바)에 있고 `BtnTestFindDatum_Click` 핸들러가 wired.
- 필요 작업: 핸들러 내부에서 `DatumFindingService.TryFindDatum` 호출 + 성공 시 transient write-back + `RenderDatumFindResult` 호출로 교체.

#### RenderDatumFindResult (HalconDisplayService.cs — 신규/교체)

기존 Phase 13 D-07 RenderDatumFindResult (RefOriginRow/Col 기반 orange 십자) 를 Phase 17 D-13 버전으로 교체:

| 렌더 요소 | HALCON 연산 | 색상 | 크기/굵기 |
|-----------|-------------|------|-----------|
| DetectedOrigin DispCross | HOperatorSet.DispCross | "purple" | size=14, lineWidth=2 |
| 좌표 텍스트 ("Find (row, col)") | HOperatorSet.WriteString | "purple" | font-18 (EnsureFontInitialized) |
| DetectedRefAngle 방향 화살표 | DispLine (2개, 화살표 패턴) | "purple" | arrowHead=5px, lineWidth=2 |

- 진입 조건: `datum.LastFindSucceeded == true`
- 호출 위치: `RenderDatumOverlay` 내부 마지막 (`LastFindSucceeded` 분기 하에)

#### DatumConfig transient 필드 (D-13 + D-16)

추가 필드 전체 목록:

| 필드명 | Type | Attribute | 목적 |
|--------|------|-----------|------|
| DetectedOriginRow | volatile double | [Browsable(false)] [JsonIgnore] | RenderDatumFindResult 입력 |
| DetectedOriginCol | volatile double | [Browsable(false)] [JsonIgnore] | RenderDatumFindResult 입력 |
| DetectedRefAngle | volatile double | [Browsable(false)] [JsonIgnore] | 기준각 화살표 렌더 입력 |
| DetectedEdgeCount | int | [Browsable(true)] [ReadOnly(true)] | PropertyGrid 검출 품질 표시 |
| DetectedFitRMSE | double | [Browsable(true)] [ReadOnly(true)] | PropertyGrid 검출 품질 표시 |
| DetectedAngleDeg | double | [Browsable(true)] [ReadOnly(true)] | PropertyGrid 검출 품질 표시 |

- [PropertyTools.DataAnnotations.Browsable(false)] 도 함께 적용 (compile-time + runtime 양쪽 보호)
- ParamBase.Save/Load reflection 에서 자동 제외 (volatile double 은 ParamBase reflection 이 저장하지 않음 — Phase 13-05 패턴 연장)
- PropertyGrid 에서 [ReadOnly(true)] 필드는 편집 불가 (read-only row 표시)

#### Hover 좌표 + 밝기 TextBlock (D-15)

**배치:** MainView.xaml 의 `canvasToolbar` Border 내부 `Grid.Column="1"` 영역 (기존 `label_drawHint` + `label_testFindResult` 와 같은 영역)

기존 `label_pos` (canvas 하단 오버레이 `X:{0:0.0}, Y:{1:0.0}, G:{2}`) 는 그대로 유지. 툴바 hover 표시는 추가적으로 상단에 배치.

| 속성 | 값 |
|------|----|
| 컨트롤 | TextBlock (3개, 또는 단일 TextBlock) |
| 포맷 | `X: nnn  ·  Y: nnn  ·  Gray: nnn` |
| FontSize | 13 |
| FontWeight | Normal |
| Foreground | #FFAAAAAA |
| Margin | 0,0,8,0 (우측 여백) |
| HorizontalAlignment | Right (Grid.Column="2" 우측 정렬, label_pointCount 대체 또는 병치) |
| Visibility | Visible (항상 — 이미지 없으면 "N/A" 표시) |

**포맷 상세:**
- 이미지 위 hover: `X: 123  ·  Y: 456  ·  Gray: 200`
- 이미지 바깥 hover: `X: N/A  ·  Y: N/A  ·  Gray: N/A`
- 정수 표시 (`ToString("0")` — D-15 "정수" 명시)

**이벤트 핸들러:**
- 진입점: `HalconViewer.HMouseMove` (HMouseEventArgsWPF e) — `MainResultViewerControl.ViewerHost_HMouseMove` 경유 → `MainView` 로 콜백 또는 직접 구독
- 구현: `HOperatorSet.GetGrayval(image, (int)e.Y, (int)e.X)` — 이미지 범위 밖 예외는 catch → "N/A" 표시
- mm 단위 추가: 없음 (deferred — CONTEXT.md 명시)

**Hover 좌표 mm 표시 여부 결정 (Claude's Discretion — LOCKED HERE):** 없음 (픽셀 row/col + Gray 정수만).

근거: D-15 명시 + CONTEXT.md deferred 항목. Datum 보정 전/후 좌표계 결정 필요 → Phase 18 이후.

---

## Copywriting Contract

한국어 UI. 모든 모달 텍스트, 툴팁, 힌트는 한국어.

### Delete ROI 모달 (D-07)

| 요소 | 텍스트 |
|------|--------|
| 모달 title | `"ROI 삭제"` |
| 모달 message | `"선택한 ROI 를 삭제하시겠습니까?"` |
| Button 1 | `"이 ROI만 삭제"` |
| Button 2 | `"현재 Datum의 모든 ROI 삭제"` |
| Button 3 (cancel) | `"취소"` |

CustomMessageBox 시그니처: `(title, message, buttons[])` — Phase 13-03 패턴 유지.

### 실패 모달 — teach (D-12)

| 시나리오 | Title | Message |
|---------|-------|---------|
| ROI 슬롯 없음 (일반) | `"티칭 실패"` | `"필요한 ROI 가 없습니다. 캔버스에 ROI 를 그리고 다시 시도하세요."` |
| Circle ROI 없음 (CTH) | `"티칭 실패"` | `"Circle ROI 가 없습니다. 캔버스에 원을 그리고 다시 시도하세요."` |
| Vertical ROI 없음 (VTH) | `"티칭 실패"` | `"Vertical ROI 가 없습니다. 캔버스에 수직 ROI 를 그리고 다시 시도하세요."` |
| 검출 에지 0개 (EdgeDirection 힌트) | `"티칭 실패"` | `"검출된 에지가 없습니다. EdgeDirection 설정을 반대로 변경한 후 다시 시도하세요."` |
| 기타 알고리즘 실패 | `"티칭 실패"` | `"티칭에 실패했습니다: {사유}."` (DatumFindingService 에서 전달된 에러 문자열 포함) |

### 실패 모달 — Test Find (D-12)

| 시나리오 | Title | Message |
|---------|-------|---------|
| 티칭 미완료 | `"Find 실패"` | `"Datum 티칭이 완료되지 않았습니다. 먼저 Teach Datum 을 실행하세요."` |
| 검출 에지 0개 | `"Find 실패"` | `"검출된 에지가 없습니다. EdgeDirection 설정을 반대로 변경한 후 다시 시도하세요."` |
| 기타 알고리즘 실패 | `"Find 실패"` | `"Datum Find 에 실패했습니다: {사유}."` |

### EdgeDirection Tooltip (D-04)

| PropertyGrid 필드 | Tooltip 텍스트 |
|-------------------|---------------|
| `*_EdgeDirection` (모든 슬롯) | `"일반적으로 수평 방향 ROI 에는 LtoR 또는 RtoL 을 권장합니다."` |

PropertyTools.Wpf: `[System.ComponentModel.Description("일반적으로 수평 방향 ROI 에는 LtoR 또는 RtoL 을 권장합니다.")]` attribute 로 적용.

### 성공 피드백 (D-12)

| 동작 | 피드백 |
|------|--------|
| btn_teachDatum 성공 | 모달 없음. HalconDisplayService RenderDatumOverlay 시각화만. |
| btn_testFindDatum 성공 | 모달 없음. RenderDatumFindResult purple DispCross 시각화만. |

### Empty State

| 상황 | 텍스트 |
|------|--------|
| 이미지 없음 (기존 label_message) | `"NO Image"` (기존 — 변경 없음) |
| Datum 노드 선택 전 hover | `X: N/A  ·  Y: N/A  ·  Gray: N/A` |

---

## Interaction States

### btn_teachDatum 상태머신

| 상태 | IsEnabled | IsChecked | 동작 |
|------|-----------|-----------|------|
| Datum 노드 미선택 | false | false | 비활성 |
| Datum 노드 선택 | true | false | 클릭 → Drawing 모드 ON |
| Drawing 모드 ON | true | true | 좌클릭+드래그 → ROI 그리기, 완료 시 자동 teach 실행 |

### _isEditMode 상태머신

| 상태 | HitTestOneRoi | 결과 |
|------|---------------|------|
| false (기본) | 반환 null (즉시) | ROI 클릭/드래그 불가 |
| true | 정상 hit-test 수행 | Rect + Circle + Polygon 이동/리사이즈 가능 |

### AlgorithmType 변경 흐름

```
사용자 ComboBox 변경
  → ICustomTypeDescriptor.GetProperties() 재계산
  → SelectedObject null→new rebind (Phase 16 D-09/D-10 패턴)
  → LastTeachSucceeded = false
  → DetectedOriginRow/Col/RefAngle = 0 (시각화 clear)
  → ROI 보존 (삭제 없음)
  → 자동 재검출 없음
  → 사용자가 btn_teachDatum 클릭해야 새 알고리즘 실행
```

---

## HALCON Overlay Rendering Contract

### 전체 팔레트 할당 (Phase 17 이후 기준)

| 색상 | HALCON 값 | 담당 요소 |
|------|-----------|----------|
| cyan | "cyan" | Line1 detected 외삽선, Line1 raw edges, ROI overlay (selected) |
| magenta | "magenta" | Line2 raw edges, Datum Origin reference cross |
| yellow | "yellow" | Circle center cross (size=12), calibration points, Line1 detected line |
| red | "red" | Teach-success intersection cross, draft draw, Circle ROI center marker |
| green | "green" | Horizontal A raw edges, fitted line |
| `#90EE90` | "#90EE90" | Detected circle outline, Horizontal B raw edges |
| gray | "gray" | Circle raw edges (size=4), Circle strip overlay (1개 pre-teach) |
| orange | "orange" | Vertical raw edges, Phase 13 기존 RenderDatumFindResult (교체 대상) |
| blue | "blue" | ROI overlay (unselected), Datum overlay (unselected) |
| white | "white" | Direction arrow |
| **purple** | **"purple"** | **DetectedOrigin DispCross + 좌표 텍스트 + RefAngle 화살표 (Phase 17 신규)** |

### RenderCircleStripOverlay 변경 사항

- 변경 전: stepCount 개 루프 (0° ~ 360°-stepDeg)
- 변경 후: `thetaRad = 0.0` 단일 사각형만 (0° = 3시 방향 고정)
- 색상/굵기 불변: "gray", LineWidth=1
- length1/length2 cap 로직 (12px cap, 1px floor) 불변

### RenderDatumFindResult 변경 사항

Phase 13 D-07 기존 구현 (orange, RefOriginRow/Col 기반) →
Phase 17 D-13 신규 구현 (purple, DetectedOriginRow/Col 기반):

```
// 진입 조건: datum.LastFindSucceeded && datum != null
// 1. purple DispCross size=14
HOperatorSet.SetColor(window, "purple");
HOperatorSet.SetLineWidth(window, 2);
HOperatorSet.DispCross(window,
    new HTuple(datum.DetectedOriginRow),
    new HTuple(datum.DetectedOriginCol),
    new HTuple(14.0), new HTuple(0.0));

// 2. 좌표 텍스트 (십자 위 외곽)
HOperatorSet.SetColor(window, "purple");
HOperatorSet.SetTposition(window,
    datum.DetectedOriginRow - 20, datum.DetectedOriginCol + 16);
HOperatorSet.WriteString(window,
    "Find (" + datum.DetectedOriginRow.ToString("F1") + ", "
             + datum.DetectedOriginCol.ToString("F1") + ")");

// 3. DetectedRefAngle 방향 화살표 (DrawDirectionArrow 패턴)
// arrowLength=20, arrowHead=5px, lineWidth=2, 색상 "purple"
```

---

## Registry Safety

Not applicable. This is a WPF desktop application with no shadcn, no npm registry, no third-party component registry.

---

## Code Style Contract

| 파일 | Brace style | 근거 |
|------|-------------|------|
| HalconDisplayService.cs | Allman | 기존 파일 스타일 (D-19) |
| DatumConfig.cs | Allman | 기존 파일 스타일 |
| DatumFindingService.cs | Allman | 기존 파일 스타일 |
| MainView.xaml.cs | K&R | 기존 파일 스타일 (D-19) |
| InspectionListView.xaml.cs | K&R | 기존 파일 스타일 |
| EdgeOptionLists.cs | 기존 스타일 따름 | — |

모든 변경 라인: `//260430 hbk Phase 17 <reason>` 주석 필수 (D-18).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
