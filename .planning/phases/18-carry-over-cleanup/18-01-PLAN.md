---
phase: 18
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
autonomous: true
requirements:
  - CO-01
must_haves:
  truths:
    - "CTH Datum 선택 시 PropertyGrid의 Circle_RadialDirection 콤보박스에 정확히 Inward/Outward 두 항목만 표시된다"
    - "기존 INI 직렬화(ParamBase reflection 경로)에는 영향을 주지 않는다"
    - "빌드가 경고 증가 없이 성공한다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "GetProperties(Attribute[]) — ItemsSource 소스 프로퍼티 whitelist 추가"
      contains: "allNoFilter"
  key_links:
    - from: "DatumConfig.GetProperties(Attribute[])"
      to: "PropertyTools.Wpf PropertyGrid"
      via: "ICustomTypeDescriptor"
      pattern: "allNoFilter.*sourceNames"
---

<objective>
DatumConfig.GetProperties(Attribute[]) ICustomTypeDescriptor 구현에서 [ItemsSourceProperty] 바인딩 소스 프로퍼티(Circle_RadialDirectionList 등)가 [Browsable(false)]로 인해 PropertyDescriptorCollection에서 누락되어 PropertyTools.Wpf가 이름 조회에 실패하는 버그를 수정한다. (per D-02)

Purpose: Circle_RadialDirection PropertyGrid 콤보박스가 EdgeOptionLists.Directions(4항목) 대신 RadialDirections(Inward/Outward 2항목)를 올바르게 표시하도록 한다.
Output: DatumConfig.cs GetProperties 수정 + 빌드 검증
</objective>

<execution_context>
@C:\Users\tech\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\tech\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- DatumConfig.cs 핵심 구조 (실행자가 직접 참고) -->
<!-- 파일: WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs -->

L239-249: Circle_RadialDirection + ItemsSource 바인딩
```csharp
[ItemsSourceProperty(nameof(Circle_RadialDirectionList))] //260503 hbk Phase 17 D-02
public string Circle_RadialDirection { get; set; } = "";

[PropertyTools.DataAnnotations.Browsable(false)] //260503 hbk Phase 17 D-02
public List<string> Circle_RadialDirectionList { get { return EdgeOptionLists.RadialDirections; } }
```

L545-554: GetProperties(Attribute[]) 현재 구현 (수정 대상)
```csharp
public System.ComponentModel.PropertyDescriptorCollection GetProperties(System.Attribute[] attributes) {
    var all = System.ComponentModel.TypeDescriptor.GetProperties(this, attributes, true);
    var alg = AlgorithmTypeEnum;
    var keep = new List<System.ComponentModel.PropertyDescriptor>();
    foreach (System.ComponentModel.PropertyDescriptor pd in all) {
        if (IsHiddenForAlgorithm(pd.Name, alg)) continue;
        keep.Add(pd);
    }
    return new System.ComponentModel.PropertyDescriptorCollection(keep.ToArray());
}
```

L427-439: DetectedOriginRow transient 패턴 (DatumConfig.CircleStripSuccesses의 참고 패턴)
```csharp
[System.ComponentModel.Browsable(false)]
[PropertyTools.DataAnnotations.Browsable(false)]
[Newtonsoft.Json.JsonIgnore]
public double DetectedOriginRow { get; set; }
```

EdgeOptionLists.cs:
```csharp
public static readonly List<string> RadialDirections = new List<string> { "Inward", "Outward" };
public static readonly List<string> Directions = new List<string> { "LtoR", "RtoL", "TtoB", "BtoT" };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GetProperties whitelist — ItemsSource 소스 프로퍼티 강제 포함</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (전체 파일, 특히 L239-249, L545-554, L462-538)
    - WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs (RadialDirections 확인)
  </read_first>
  <action>
DatumConfig.cs L545-554의 GetProperties(Attribute[]) 메서드를 아래 방식으로 수정한다. (per D-01, D-02)

Root cause: TypeDescriptor.GetProperties(this, attributes, true) 호출 시 attributes에 BrowsableAttribute.Yes 필터가 포함되면 [PropertyTools.DataAnnotations.Browsable(false)] 항목이 제외된다. PropertyTools.Wpf는 [ItemsSourceProperty("Circle_RadialDirectionList")]를 읽고 동일 컬렉션에서 "Circle_RadialDirectionList" 이름을 찾지 못해 fallback으로 첫 번째 List&lt;string&gt;(EdgeOptionLists.Directions)을 사용한다.

수정 방법 (Option A — 기존 구조 최소 변경):
GetProperties(Attribute[]) 메서드 끝에서 return 직전에, Browsable 필터 없이 전체 프로퍼티를 재조회하여 [ItemsSourceProperty] 소스 List&lt;&gt; 프로퍼티들을 keep에 강제 추가한다.

구체적 코드:
```csharp
//260505 hbk Phase 18 CO-01 — ItemsSource 소스 List<> 프로퍼티는 [Browsable(false)]이므로
// TypeDescriptor.GetProperties(this, attributes, true) 에서 제외됨.
// PropertyTools.Wpf 가 [ItemsSourceProperty] 이름을 GetProperties 반환 컬렉션에서 조회할 때
// Circle_RadialDirectionList 등을 찾지 못해 fallback(Directions 4항목)이 적용되는 버그.
// → Browsable 필터 없이 전체 재조회하여 소스 프로퍼티를 명시적으로 추가.
var allNoFilter = System.ComponentModel.TypeDescriptor.GetProperties(this, true); //260505 hbk Phase 18 CO-01
var sourceNames = new System.Collections.Generic.HashSet<string> { //260505 hbk Phase 18 CO-01
    nameof(AlgorithmTypeList),
    nameof(Circle_EdgeDirectionList), nameof(Circle_EdgePolarityList),
    nameof(Circle_EdgeSelectionList), nameof(Circle_RadialDirectionList),
    nameof(Horizontal_A_EdgeDirectionList), nameof(Horizontal_A_EdgePolarityList),
    nameof(Horizontal_A_EdgeSelectionList),
    nameof(Horizontal_B_EdgeDirectionList), nameof(Horizontal_B_EdgePolarityList),
    nameof(Horizontal_B_EdgeSelectionList),
    nameof(Line1_EdgeDirectionList), nameof(Line1_EdgePolarityList), nameof(Line1_EdgeSelectionList),
    nameof(Line2_EdgeDirectionList), nameof(Line2_EdgePolarityList), nameof(Line2_EdgeSelectionList),
    nameof(Vertical_EdgeDirectionList), nameof(Vertical_EdgePolarityList), nameof(Vertical_EdgeSelectionList),
}; //260505 hbk Phase 18 CO-01
foreach (System.ComponentModel.PropertyDescriptor pd in allNoFilter) { //260505 hbk Phase 18 CO-01
    if (sourceNames.Contains(pd.Name) && !keep.Any(k => k.Name == pd.Name)) //260505 hbk Phase 18 CO-01
        keep.Add(pd); //260505 hbk Phase 18 CO-01
} //260505 hbk Phase 18 CO-01
```

삽입 위치: 기존 foreach 루프(IsHiddenForAlgorithm 필터) 종료 이후, `return new PropertyDescriptorCollection(...)` 직전.

주의사항:
- nameof()로 참조하는 프로퍼티 이름이 실제로 DatumConfig에 존재하는지 확인. 없는 이름이 있으면 컴파일 에러 발생 — 실제 존재하는 프로퍼티 이름만 포함.
- AllNoFilter로 재조회한 항목 중 keep에 이미 있는 이름은 skip (중복 방지).
- DatumConfig에 AlgorithmTypeList, Horizontal_A/B_EdgeDirectionList 등이 실제로 선언되어 있는지 파일 읽기 후 확인하여 없는 항목은 nameof 목록에서 제거.
  </action>
  <verify>
    <automated>
      cd C:\Info\Project\DataMeasurement &&
      msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /verbosity:minimal 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "allNoFilter" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 출력 ≥ 1
    - `grep -c "Circle_RadialDirectionList" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 출력 ≥ 2 (정의 + sourceNames 참조)
    - `grep -c "sourceNames" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 출력 ≥ 1
    - msbuild Debug/x64 빌드 PASS (Build succeeded 메시지, 오류 0)
    - 모든 변경 라인에 `//260505 hbk Phase 18 CO-01` 주석 포함
  </acceptance_criteria>
  <done>DatumConfig.GetProperties(Attribute[])가 Circle_RadialDirectionList를 keep에 포함하여 빌드가 통과한다. PropertyGrid 런타임에서 CTH Datum 선택 시 콤보박스에 Inward/Outward 2항목만 표시된다 (런타임 검증은 UAT에서 수행).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PropertyGrid → DatumConfig | PropertyTools.Wpf가 ICustomTypeDescriptor를 통해 속성 목록을 조회 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Tampering | DatumConfig.GetProperties | accept | 내부 PropertyGrid 바인딩 전용 메서드. 외부 입력 없음. |
</threat_model>

<verification>
1. msbuild Debug/x64 PASS
2. `grep -c "allNoFilter" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` ≥ 1
3. `grep -c "Circle_RadialDirectionList" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` ≥ 2
4. (런타임) CTH Datum PropertyGrid → Circle_RadialDirection 드롭다운 → Inward/Outward 2항목만 확인
</verification>

<success_criteria>
- DatumConfig.GetProperties(Attribute[]) 수정 완료
- 빌드 오류 0, 신규 경고 0
- Circle_RadialDirectionList가 keep 컬렉션에 명시적으로 포함됨을 grep으로 확인
</success_criteria>

<output>
완료 후 .planning/phases/18-carry-over-cleanup/18-01-SUMMARY.md 생성
</output>
