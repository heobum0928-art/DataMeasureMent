---
phase: 18
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml
  - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: true
requirements:
  - CO-04
must_haves:
  truths:
    - "TeachDatum 모드(btn_teachDatum ON) + Datum ROI 위 우클릭 시 '로 다시 그리기' 메뉴 항목이 Visible"
    - "TeachDatum 모드가 아니거나 ROI 위가 아닌 위치에서 우클릭 시 메뉴 항목이 Collapsed"
    - "메뉴 항목 클릭 시 해당 Datum ROI의 Length1/Length2/Radius가 0으로 리셋된다"
    - "빌드가 경고 증가 없이 성공한다"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainResultViewerControl.xaml"
      provides: "RedrawRoiMenuItem XAML 항목"
      contains: "RedrawRoiMenuItem"
    - path: "WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs"
      provides: "IsTeachDatumMode 프로퍼티 + UpdateContextMenuState 분기 + RoiRedrawRequested 이벤트"
      contains: "IsTeachDatumMode"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "RoiRedrawRequested 구독 + ClearDatumRoiFields 호출"
      contains: "RoiRedrawRequested"
  key_links:
    - from: "MainResultViewerControl.UpdateContextMenuState"
      to: "RedrawRoiMenuItem.Visibility"
      via: "IsTeachDatumMode + HitTestRoiAtPoint"
      pattern: "IsTeachDatumMode.*HitTestRoiAtPoint|RedrawRoiMenuItem.*Visibility"
    - from: "MainView.TeachDatumButton_Click"
      to: "halconViewer.IsTeachDatumMode"
      via: "프로퍼티 설정"
      pattern: "halconViewer\\.IsTeachDatumMode"
---

<objective>
MainResultViewerControl ContextMenu에 "ROI 다시 그리기" 항목을 추가하여, TeachDatum 모드에서 Datum ROI를 우클릭하면 해당 ROI의 치수를 0으로 리셋하는 escape hatch를 노출한다. (per D-07, D-08, D-09, D-10, D-11)

Purpose: 이미 그린 Datum ROI를 처음부터 다시 그리고 싶을 때 메뉴로 진입할 수 있게 한다.
Output: XAML MenuItem + IsTeachDatumMode 프로퍼티 + RoiRedrawRequested 이벤트 + MainView 구독 코드
</objective>

<execution_context>
@C:\Users\tech\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\tech\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- MainResultViewerControl.xaml 현재 ContextMenu 구조 (L16-41) -->
```xml
<Border.ContextMenu>
    <ContextMenu x:Name="ViewerContextMenu">
        <MenuItem x:Name="EditRoiMenuItem"   Header="Edit ROI"    Click="EditRoiMenuItem_Click"/>
        <MenuItem x:Name="DeleteRoiMenuItem" Header="Delete ROI"  Click="DeleteRoiMenuItem_Click"/>
        <Separator/>
        <MenuItem x:Name="CrosshairMenuItem" Header="Crosshair"   Click="CrosshairMenuItem_Click"/>
        <Separator/>
        <MenuItem x:Name="ManualMeasureMenuItem" Header="Manual Measure" .../>
        <MenuItem x:Name="ClearMeasureMenuItem"  Header="Clear Measure"  .../>
        <Separator/>
        <MenuItem Header="Zoom In"  Click="ZoomInMenuItem_Click"/>
        <MenuItem Header="Zoom Out" Click="ZoomOutMenuItem_Click"/>
        <Separator/>
        <MenuItem Header="Fit Image" Click="FitImageMenuItem_Click"/>
    </ContextMenu>
</Border.ContextMenu>
```

<!-- MainResultViewerControl.xaml.cs 핵심 메서드 -->
// HitTestRoiAtPoint(Point imagePoint): _rois + _datumRoiCandidates에서 hit-test, _isEditMode 무관 (L340-367)
// UpdateContextMenuState(): ManualMeasure/Edit/Delete 가시성 제어 (L1418-1452)
// _lastMouseImagePoint: 마우스 위치 Point (이미 업데이트됨)
// _datumRoiCandidates: List&lt;RoiDefinition&gt; (Datum ROI 후보)

// RoiDefinition.Id 형식: "Datum.Circle", "Datum.HorizontalA", "Datum.HorizontalB",
//                         "Datum.Line1", "Datum.Line2", "Datum.Vertical"

<!-- MainView.xaml.cs 핵심 메서드 -->
// ClearDatumRoiFields(DatumConfig datum, string roiId): 특정 roiId의 치수를 0으로 리셋 (L667-701)
// _editingDatum: 현재 티칭 중인 DatumConfig (L1574 체크됨)
// TeachDatumButton_Click: btn_teachDatum 토글 → _canvasMode = TeachDatum/None 전환
// halconViewer: MainResultViewerControl 인스턴스

<!-- RoiDeleteRequested 이벤트 패턴 참고 (기존 코드에서 검색하여 동일 패턴 적용) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MainResultViewerControl — IsTeachDatumMode 프로퍼티 + RoiRedrawRequested 이벤트 + UpdateContextMenuState 확장</name>
  <files>WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs (전체, 특히 L1418-1452 UpdateContextMenuState, L337-367 HitTestRoiAtPoint, 기존 이벤트 선언부)
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml (ContextMenu 구조 확인)
  </read_first>
  <action>
MainResultViewerControl.xaml.cs에 세 가지를 추가한다.

1. IsTeachDatumMode 프로퍼티 추가 (기존 _isEditMode 같은 bool 필드 근처에):
```csharp
//260505 hbk Phase 18 CO-04 — TeachDatum 모드 여부. MainView.TeachDatumButton_Click 에서 true/false 설정.
//  UpdateContextMenuState 가 "ROI 다시 그리기" 메뉴 가시성 결정에 사용.
public bool IsTeachDatumMode { get; set; } //260505 hbk Phase 18 CO-04
```

2. RoiRedrawRequested 이벤트 추가 (기존 이벤트 선언부 근처, RoiDeleteRequested 패턴과 동일한 방식):
```csharp
//260505 hbk Phase 18 CO-04 — "ROI 다시 그리기" 메뉴 클릭 시 발생. 인자 = hit-test 통과한 roiId.
public event System.Action&lt;string&gt; RoiRedrawRequested; //260505 hbk Phase 18 CO-04
```

3. UpdateContextMenuState() 메서드 끝(기존 EditRoiMenuItem/DeleteRoiMenuItem 블록 이후)에 RedrawRoiMenuItem 가시성 결정 로직 추가:
```csharp
//260505 hbk Phase 18 CO-04 — "ROI 다시 그리기" 메뉴: TeachDatum 모드 + 우클릭 위치에 Datum ROI 있을 때만 표시
if (RedrawRoiMenuItem != null) //260505 hbk Phase 18 CO-04
{ //260505 hbk Phase 18 CO-04
    RoiDefinition hitRoi = IsTeachDatumMode ? HitTestRoiAtPoint(_lastMouseImagePoint) : null; //260505 hbk Phase 18 CO-04
    bool isDatumRoi = hitRoi != null && hitRoi.Id != null && hitRoi.Id.StartsWith("Datum."); //260505 hbk Phase 18 CO-04
    RedrawRoiMenuItem.Visibility = isDatumRoi ? Visibility.Visible : Visibility.Collapsed; //260505 hbk Phase 18 CO-04
} //260505 hbk Phase 18 CO-04
```

4. RedrawRoiMenuItem_Click 핸들러 추가:
```csharp
//260505 hbk Phase 18 CO-04 — "ROI 다시 그리기" 클릭: hit-test 후 이벤트 발행 → MainView가 ClearDatumRoiFields 호출
private void RedrawRoiMenuItem_Click(object sender, System.Windows.RoutedEventArgs e) //260505 hbk Phase 18 CO-04
{ //260505 hbk Phase 18 CO-04
    var hitRoi = HitTestRoiAtPoint(_lastMouseImagePoint); //260505 hbk Phase 18 CO-04
    if (hitRoi != null && hitRoi.Id != null && hitRoi.Id.StartsWith("Datum.")) //260505 hbk Phase 18 CO-04
    { //260505 hbk Phase 18 CO-04
        RoiRedrawRequested?.Invoke(hitRoi.Id); //260505 hbk Phase 18 CO-04
    } //260505 hbk Phase 18 CO-04
} //260505 hbk Phase 18 CO-04
```

파일 brace 스타일: 이 파일의 기존 Allman/K&R 스타일을 확인하여 동일하게 작성.
  </action>
  <verify>
    <automated>
      grep -c "IsTeachDatumMode" "C:\Info\Project\DataMeasurement\WPF_Example\UI\ContentItem\MainResultViewerControl.xaml.cs"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "IsTeachDatumMode" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` ≥ 2 (선언 + UpdateContextMenuState 사용)
    - `grep -c "RoiRedrawRequested" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` ≥ 2 (선언 + 핸들러 호출)
    - `grep -c "RedrawRoiMenuItem" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` ≥ 2 (UpdateContextMenuState + Click 핸들러)
    - 모든 추가 라인에 `//260505 hbk Phase 18 CO-04` 주석 포함
  </acceptance_criteria>
  <done>MainResultViewerControl.xaml.cs에 IsTeachDatumMode 프로퍼티, RoiRedrawRequested 이벤트, UpdateContextMenuState 분기, Click 핸들러가 추가된다.</done>
</task>

<task type="auto">
  <name>Task 2: XAML MenuItem 추가 + MainView 구독 연결</name>
  <files>WPF_Example/UI/ContentItem/MainResultViewerControl.xaml, WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml (ContextMenu 구조 L16-44)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (TeachDatumButton_Click, ClearDatumRoiFields, halconViewer 초기화 구역)
  </read_first>
  <action>
[Step A] MainResultViewerControl.xaml ContextMenu에 MenuItem 추가:

DeleteRoiMenuItem 바로 다음(기존 Separator 전)에 삽입:
```xml
&lt;MenuItem x:Name="RedrawRoiMenuItem"
          Header="ROI 다시 그리기"
          Click="RedrawRoiMenuItem_Click"
          Visibility="Collapsed"/&gt;
```

초기 Visibility는 Collapsed — UpdateContextMenuState가 조건에 따라 Visible로 전환한다.

[Step B] MainView.xaml.cs에서 RoiRedrawRequested 구독:

파일에서 halconViewer 이벤트 구독 초기화 블록을 찾아(기존 RoiDeleteRequested 구독이 있는 위치), 동일한 곳에 추가:
```csharp
//260505 hbk Phase 18 CO-04 — "ROI 다시 그리기": Length/Radius 0 리셋 후 오버레이 갱신
halconViewer.RoiRedrawRequested += (roiId) => //260505 hbk Phase 18 CO-04
{ //260505 hbk Phase 18 CO-04
    if (_editingDatum != null) //260505 hbk Phase 18 CO-04
    { //260505 hbk Phase 18 CO-04
        ClearDatumRoiFields(_editingDatum, roiId); //260505 hbk Phase 18 CO-04
        halconViewer.SetDatumOverlay(_editingDatum, false); //260505 hbk Phase 18 CO-04
        PublishDatumRoiCandidates(_editingDatum); //260505 hbk Phase 18 CO-04
    } //260505 hbk Phase 18 CO-04
}; //260505 hbk Phase 18 CO-04
```

[Step C] MainView.xaml.cs TeachDatumButton_Click 핸들러에서 IsTeachDatumMode 설정:

TeachDatumButton_Click 내부에서 _canvasMode = ECanvasMode.TeachDatum 로 설정하는 라인 바로 다음에:
```csharp
halconViewer.IsTeachDatumMode = true; //260505 hbk Phase 18 CO-04 — "ROI 다시 그리기" 메뉴 활성화
```

TeachDatum 종료(btn_teachDatum OFF) 경로에서:
```csharp
halconViewer.IsTeachDatumMode = false; //260505 hbk Phase 18 CO-04 — TeachDatum 종료 시 메뉴 숨김
```

TeachDatumButton_Click 내에서 _canvasMode = ECanvasMode.None 또는 false로 전환하는 모든 경로(early return, 정상 종료)를 찾아 IsTeachDatumMode = false를 설정. InvokeTryTeachDatum 완료 콜백에서도 _editingDatum = null 이전에 false 설정.

주의: XAML에서 Visibility="Collapsed" 초기값을 반드시 지정해야 함 — 지정 안 하면 최초 우클릭 전에 항목이 보임.
  </action>
  <verify>
    <automated>
      msbuild "C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /verbosity:minimal 2>&1 | Select-String -Pattern "error|warning|succeeded"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RedrawRoiMenuItem" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml` ≥ 1 (MenuItem 선언)
    - `grep -c "ROI 다시 그리기" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml` = 1
    - `grep -c "RoiRedrawRequested" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 1 (구독)
    - `grep -c "IsTeachDatumMode" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 2 (true + false 설정)
    - msbuild Debug/x64 빌드 PASS (오류 0)
    - 모든 추가 라인에 `//260505 hbk Phase 18 CO-04` 주석 포함
  </acceptance_criteria>
  <done>XAML MenuItem이 초기 Collapsed로 추가되고, MainView가 RoiRedrawRequested를 구독하며, IsTeachDatumMode가 TeachDatum 모드 진입/종료 시 올바르게 설정된다. 빌드가 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 우클릭 이벤트 → ContextMenu | 사용자 입력으로 메뉴 항목 가시성 결정 |
| RoiRedrawRequested → ClearDatumRoiFields | 이벤트를 통해 DatumConfig 필드 리셋 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Tampering | ClearDatumRoiFields | accept | TeachDatum 모드에서만 노출 + _editingDatum null 가드 적용 |
| T-18-02-02 | Denial of Service | UpdateContextMenuState | accept | per-click 실행, O(n) hit-test로 성능 위험 없음 |
</threat_model>

<verification>
1. msbuild Debug/x64 PASS
2. `grep -c "RedrawRoiMenuItem" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml` ≥ 1
3. `grep -c "RoiRedrawRequested" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 1
4. `grep -c "IsTeachDatumMode" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` ≥ 2
5. (런타임) TeachDatum 모드 ON + Datum ROI 위 우클릭 → "ROI 다시 그리기" 항목 표시
6. (런타임) 클릭 후 해당 ROI 오버레이 사라짐 확인
</verification>

<success_criteria>
- XAML MenuItem RedrawRoiMenuItem 추가 (Visibility=Collapsed)
- MainResultViewerControl: IsTeachDatumMode + RoiRedrawRequested + UpdateContextMenuState 분기 + Click 핸들러
- MainView: RoiRedrawRequested 구독 + IsTeachDatumMode 설정 (TeachDatum ON/OFF 경로 모두)
- 빌드 오류 0
</success_criteria>

<output>
완료 후 .planning/phases/18-carry-over-cleanup/18-02-SUMMARY.md 생성
</output>
