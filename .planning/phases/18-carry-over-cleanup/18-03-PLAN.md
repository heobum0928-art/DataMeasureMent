---
phase: 18
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: true
requirements:
  - CO-06
must_haves:
  truths:
    - "티칭 실패 모달 메시지가 '[DatumName] 검출된 에지가 없습니다...' 형식으로 표시된다"
    - "FormatTeachError 시그니처가 DatumConfig datum 인자를 포함한다"
    - "빌드가 경고 증가 없이 성공한다"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "FormatTeachError(DatumConfig datum, string err) — datum.DatumName 접두사 포함"
      contains: "datum.DatumName"
  key_links:
    - from: "MainView.InvokeTryTeachDatum 완료 콜백"
      to: "FormatTeachError(_editingDatum, error)"
      via: "FormatTeachError 호출 사이트 (L1605)"
      pattern: "FormatTeachError.*_editingDatum"
---

<objective>
FormatTeachError(string err) 시그니처를 FormatTeachError(DatumConfig datum, string err)로 확장하여, 티칭 실패 모달에 "[DatumName] " 접두사를 추가한다. (per D-17, D-18, D-19)

Purpose: 여러 Datum이 있는 환경에서 어느 Datum의 티칭이 실패했는지 모달 메시지로 즉시 식별 가능하게 한다.
Output: MainView.xaml.cs FormatTeachError 시그니처 변경 + 호출 사이트 1곳 수정
</objective>

<execution_context>
@C:\Users\tech\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\tech\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- MainView.xaml.cs 관련 코드 (연구자 확인) -->

현재 FormatTeachError 정의 (L741-749):
```csharp
private static string FormatTeachError(string err) {
    if (err == null) err = "unknown";
    if (err.IndexOf("no edges", System.StringComparison.OrdinalIgnoreCase) >= 0
        || err.IndexOf("insufficient edges", System.StringComparison.OrdinalIgnoreCase) >= 0
        || err.IndexOf("insufficient polar samples", System.StringComparison.OrdinalIgnoreCase) >= 0) {
        return "검출된 에지가 없습니다. EdgeDirection 설정을 반대로 변경한 후 다시 시도하세요.";
    }
    return "티칭에 실패했습니다: " + err;
}
```

유일한 호출 사이트 (L1605):
```csharp
CustomMessageBox.Show("티칭 실패", FormatTeachError(error)); //260503 hbk Phase 17 D-12
```

DatumConfig.DatumName 프로퍼티: L19에 선언됨.
_editingDatum: 이 문맥(L1574)에서 null 체크 완료됨.

FormatFindError (L752-760): 별도 메서드 — CO-06 수정 대상 아님, 변경 없음.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FormatTeachError 시그니처 확장 + 호출 사이트 수정</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (L740-760 FormatTeachError/FormatFindError, L1600-1612 호출 사이트)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (DatumName 프로퍼티 확인 — L19 부근)
  </read_first>
  <action>
[Step A] FormatTeachError 메서드 시그니처와 본문 수정 (L741-749):

기존 메서드를 아래와 같이 교체한다:
```csharp
//260505 hbk Phase 18 CO-06 — datum 인자 추가 → 에러 메시지에 [DatumName] 접두사 포함 (D-17)
private static string FormatTeachError(DatumConfig datum, string err) { //260505 hbk Phase 18 CO-06
    if (err == null) err = "unknown"; //260505 hbk Phase 18 CO-06
    string prefix = (datum != null && !string.IsNullOrEmpty(datum.DatumName)) //260505 hbk Phase 18 CO-06
        ? "[" + datum.DatumName + "] " : ""; //260505 hbk Phase 18 CO-06
    if (err.IndexOf("no edges", System.StringComparison.OrdinalIgnoreCase) >= 0 //260505 hbk Phase 18 CO-06
        || err.IndexOf("insufficient edges", System.StringComparison.OrdinalIgnoreCase) >= 0 //260505 hbk Phase 18 CO-06
        || err.IndexOf("insufficient polar samples", System.StringComparison.OrdinalIgnoreCase) >= 0) { //260505 hbk Phase 18 CO-06
        return prefix + "검출된 에지가 없습니다. EdgeDirection 설정을 반대로 변경한 후 다시 시도하세요."; //260505 hbk Phase 18 CO-06
    } //260505 hbk Phase 18 CO-06
    return prefix + "티칭에 실패했습니다: " + err; //260505 hbk Phase 18 CO-06
} //260505 hbk Phase 18 CO-06
```

주의사항:
- 프로퍼티 이름은 반드시 `datum.DatumName` 사용 (CONTEXT.md의 `datum.Name` 표기는 추상적 표현이며 실제 프로퍼티는 DatumName — 연구자 A2 확인)
- 기존 주석 `//260503 hbk Phase 17 D-12 + D-04 — teach 실패 사유 모달...`은 삭제하고 위 주석으로 교체

[Step B] 호출 사이트 수정 (L1605):

기존:
```csharp
CustomMessageBox.Show("티칭 실패", FormatTeachError(error)); //260503 hbk Phase 17 D-12
```

수정 후:
```csharp
CustomMessageBox.Show("티칭 실패", FormatTeachError(_editingDatum, error)); //260505 hbk Phase 18 CO-06
```

`_editingDatum`은 이 위치(InvokeTryTeachDatum 완료 콜백)에서 L1574 null 체크로 보장되어 있다. 단, L1611에서 `_editingDatum = null`로 클리어되므로 호출 순서 확인 — FormatTeachError 호출이 _editingDatum = null 이전에 위치하는지 확인.

[보충] FormatFindError(L752-760)는 수정하지 않는다.
  </action>
  <verify>
    <automated>
      msbuild "C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /verbosity:minimal 2>&1 | Select-String -Pattern "error|succeeded"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FormatTeachError" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 2 (정의 + 호출)
    - `grep -c "datum.DatumName" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 1
    - `grep -c "_editingDatum, error" WPF_Example/UI/ContentItem/MainView.xaml.cs` = 1 (호출 사이트)
    - `grep -c "DatumConfig datum, string err" WPF_Example/UI/ContentItem/MainView.xaml.cs` = 1 (새 시그니처)
    - msbuild Debug/x64 빌드 PASS (오류 0)
    - 모든 변경 라인에 `//260505 hbk Phase 18 CO-06` 주석 포함
  </acceptance_criteria>
  <done>FormatTeachError가 DatumConfig 인자를 받아 "[DatumName] " 접두사를 포함한 에러 메시지를 반환하고, 유일한 호출 사이트(L1605)가 _editingDatum을 전달하며, 빌드가 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DatumConfig.DatumName → 모달 메시지 | 사용자 입력값이 모달 텍스트에 포함됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | Information Disclosure | FormatTeachError 모달 | accept | DatumName은 사용자가 직접 설정한 값. 내부 경로 노출 없음. null/empty 가드 적용됨. |
</threat_model>

<verification>
1. msbuild Debug/x64 PASS
2. `grep -c "datum.DatumName" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 1
3. `grep -c "FormatTeachError" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 2
4. (런타임) CTH Datum 티칭 실패 시 모달에 "[DatumName] ..." 형식 메시지 표시
</verification>

<success_criteria>
- FormatTeachError(DatumConfig datum, string err) 시그니처 확정
- datum.DatumName 기반 접두사 로직 포함
- 호출 사이트 1곳(_editingDatum 전달) 수정 완료
- 빌드 오류 0
</success_criteria>

<output>
완료 후 .planning/phases/18-carry-over-cleanup/18-03-SUMMARY.md 생성
</output>
