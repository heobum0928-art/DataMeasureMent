---
phase: 18
plan: 04
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
autonomous: true
requirements:
  - CO-05
must_haves:
  truths:
    - "CTH 티칭 성공 후 Canvas에서 성공한 polar strip은 녹색, 실패한 strip은 빨강으로 표시된다"
    - "TryFindCircleByPolarSampling 시그니처에 out bool[] stripSuccesses 파라미터가 추가된다"
    - "TryFindCircleTwoHorizontal(find 경로)에서는 CircleStripSuccesses를 갱신하지 않는다 (D-14)"
    - "RenderCircleStripOverlay 루프 완료 후 SetColor('gray')로 상태를 복원한다"
    - "빌드가 경고 증가 없이 성공한다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "CircleStripSuccesses bool[] transient 필드"
      contains: "CircleStripSuccesses"
    - path: "WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs"
      provides: "TryFindCircleByPolarSampling + out bool[] stripSuccesses"
      contains: "stripSuccesses"
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "TryTeachCircleTwoHorizontal — config.CircleStripSuccesses 갱신"
      contains: "CircleStripSuccesses"
    - path: "WPF_Example/Halcon/Display/HalconDisplayService.cs"
      provides: "RenderCircleStripOverlay — per-strip 색상 분기"
      contains: "stripSuccesses"
  key_links:
    - from: "VisionAlgorithmService.TryFindCircleByPolarSampling"
      to: "DatumFindingService.TryTeachCircleTwoHorizontal"
      via: "out bool[] stripSuccesses"
      pattern: "stripSuccesses.*TryFindCircleByPolarSampling"
    - from: "DatumConfig.CircleStripSuccesses"
      to: "HalconDisplayService.RenderCircleStripOverlay"
      via: "datum.CircleStripSuccesses"
      pattern: "datum\\.CircleStripSuccesses"
---

<objective>
Circle polar sampling 결과에서 per-strip 성공/실패 정보를 TryFindCircleByPolarSampling→TryTeachCircleTwoHorizontal→DatumConfig.CircleStripSuccesses→RenderCircleStripOverlay 경로로 전달하여, 오버레이에서 성공 strip=녹색/실패 strip=빨강으로 표시한다. (per D-12~D-16)

Purpose: 사용자가 Circle polar sampling에서 어떤 각도의 strip이 에지를 검출하지 못했는지 즉시 시각적으로 파악할 수 있게 한다.
Output: 4개 파일 수정 — VisionAlgorithmService(시그니처), DatumConfig(transient 필드), DatumFindingService(write-back), HalconDisplayService(색상 분기)
</objective>

<execution_context>
@C:\Users\tech\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\tech\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- VisionAlgorithmService.cs 현재 TryFindCircleByPolarSampling 시그니처 (L214-222) -->
```csharp
public bool TryFindCircleByPolarSampling(
    HImage image,
    double centerRow, double centerCol, double radius,
    double stepDeg, double rectL1Ratio, double rectL2Ratio,
    double sigma, int threshold, string polarity, string selection,
    HTuple datumTransform,
    out double foundRow, out double foundCol, out double foundRadius,
    out HTuple edgeRows, out HTuple edgeCols,
    out string error)
```

<!-- 루프 내부 구조 (L278-331) -->
```csharp
int stepCount = (int)Math.Round(360.0 / stepDeg);
for (int i = 0; i < stepCount; i++) {
    // ... GenRectangle2, GenMeasureRectangle2, MeasurePos ...
    if (eRows.TupleLength() > 0 && eCols.TupleLength() > 0) {
        // TupleConcat allRows/allCols 누적
    }
    // catch: per-step 실패 swallow
    // finally: CloseMeasure
}
edgeRows = allRows;
edgeCols = allCols;
if (allRows.TupleLength() < 3) { error = "insufficient polar samples..."; return false; }
```

<!-- DatumFindingService TryTeachCircleTwoHorizontal 호출 (L730-744) -->
```csharp
if (!visionSvc.TryFindCircleByPolarSampling(
        image, config.CircleROI_Row, config.CircleROI_Col, config.CircleROI_Radius,
        config.Circle_PolarStepDeg, config.Circle_RectL1Ratio, config.Circle_RectL2Ratio,
        config.Circle_Sigma, config.Circle_EdgeThreshold, circlePolarity,
        config.Circle_EdgeSelection,
        null,
        out centerRow, out centerCol, out radius,
        out circleEdgeRows, out circleEdgeCols,
        out circleError)) { ... }
```

<!-- DatumFindingService TryFindCircleTwoHorizontal 호출 (L201-213) — find 경로 (D-14: 갱신 없음) -->
```csharp
if (!visionSvc.TryFindCircleByPolarSampling(
        image,
        config.CircleROI_Row, ...,
        null,
        out centerRow, out centerCol, out radius,
        out circleEdgeRows, out circleEdgeCols,
        out circleError)) { ... }
```

<!-- HalconDisplayService RenderCircleStripOverlay 루프 (L481-516) -->
```csharp
HOperatorSet.SetColor(window, "gray");  // 현재 고정 색상
HOperatorSet.SetLineWidth(window, 1);
for (int i = 0; i < stepCount; i++) {
    // DispLine x4
}
```

<!-- DatumConfig DetectedOriginRow transient 패턴 (참고용, L427-439) -->
```csharp
[System.ComponentModel.Browsable(false)]
[PropertyTools.DataAnnotations.Browsable(false)]
[Newtonsoft.Json.JsonIgnore]
public double DetectedOriginRow { get; set; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VisionAlgorithmService — out bool[] stripSuccesses 파라미터 추가 + 루프 내 성공/실패 기록</name>
  <files>WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs (L214-340 TryFindCircleByPolarSampling 전체)
  </read_first>
  <action>
TryFindCircleByPolarSampling 메서드에 `out bool[] stripSuccesses` 파라미터를 추가하고, 루프 내에서 per-strip 성공 여부를 기록한다.

[Step A] 시그니처 변경:
기존 `out string error` 앞에 `out bool[] stripSuccesses` 추가:
```csharp
public bool TryFindCircleByPolarSampling(
    HImage image,
    double centerRow, double centerCol, double radius,
    double stepDeg, double rectL1Ratio, double rectL2Ratio,
    double sigma, int threshold, string polarity, string selection,
    HTuple datumTransform,
    out double foundRow, out double foundCol, out double foundRadius,
    out HTuple edgeRows, out HTuple edgeCols,
    out bool[] stripSuccesses, //260505 hbk Phase 18 CO-05 — per-strip 검출 성공 여부
    out string error)
```

[Step B] 메서드 본문 첫 부분에 초기화 추가 (foundRow = 0... 라인들 바로 다음):
```csharp
stripSuccesses = null; //260505 hbk Phase 18 CO-05
```

[Step C] stepCount 계산 직후 stripSuccesses 배열 할당:
```csharp
int stepCount = (int)Math.Round(360.0 / stepDeg);
bool[] strips = new bool[stepCount]; //260505 hbk Phase 18 CO-05 — per-strip 성공 여부 배열
```

[Step D] 루프 내부 수정 — `if (eRows.TupleLength() > 0 && ...)` 블록에서 `strips[i] = true` 설정:
```csharp
if (eRows.TupleLength() > 0 && eCols.TupleLength() > 0)
{
    strips[i] = true; //260505 hbk Phase 18 CO-05 — 이 strip 검출 성공
    // ... 기존 TupleConcat 로직 ...
}
// strips[i] 기본값 false (배열 초기화 시 자동)
```

[Step E] 루프 종료 후, `edgeRows = allRows;` 라인 바로 앞에:
```csharp
stripSuccesses = strips; //260505 hbk Phase 18 CO-05 — 호출자에게 per-strip 결과 전달
```

주의: catch 블록에서는 strips[i]를 false로 명시적으로 설정하지 않아도 됨 (bool[] 기본값 false). MeasurePos 실패(TupleLength == 0)도 strip failure로 간주됨.
  </action>
  <verify>
    <automated>
      grep -c "stripSuccesses" "C:\Info\Project\DataMeasurement\WPF_Example\Halcon\Algorithms\VisionAlgorithmService.cs"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "stripSuccesses" WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs` ≥ 4 (시그니처 + 초기화 + 배열 할당 + 대입)
    - `grep -c "strips\[i\] = true" WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs` = 1
    - 모든 추가 라인에 `//260505 hbk Phase 18 CO-05` 주석 포함
  </acceptance_criteria>
  <done>TryFindCircleByPolarSampling이 per-strip 성공 여부를 bool[]로 반환할 준비가 되었다.</done>
</task>

<task type="auto">
  <name>Task 2: DatumConfig transient 필드 + DatumFindingService write-back + HalconDisplayService 색상 분기</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs, WPF_Example/Halcon/Algorithms/DatumFindingService.cs, WPF_Example/Halcon/Display/HalconDisplayService.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (L427-440 DetectedOriginRow 패턴 + L239-249 Circle 필드)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (L195-214 TryFindCircleTwoHorizontal 호출, L706-755 TryTeachCircleTwoHorizontal 호출)
    - WPF_Example/Halcon/Display/HalconDisplayService.cs (L457-516 RenderCircleStripOverlay)
  </read_first>
  <action>
[Step A] DatumConfig.cs — CircleStripSuccesses transient 필드 추가 (DetectedOriginRow 패턴 동일):
DetectedOriginRow 선언 블록(L427-439) 바로 다음에 추가:
```csharp
//260505 hbk Phase 18 CO-05 — Circle polar strip 별 검출 성공 여부 (TryTeachCircleTwoHorizontal write-back).
//  INI/JSON 직렬화 제외 (transient). RenderCircleStripOverlay 소비. bool[] 크기 = stepCount.
[System.ComponentModel.Browsable(false)] //260505 hbk Phase 18 CO-05
[PropertyTools.DataAnnotations.Browsable(false)] //260505 hbk Phase 18 CO-05
[Newtonsoft.Json.JsonIgnore] //260505 hbk Phase 18 CO-05
public bool[] CircleStripSuccesses { get; set; } //260505 hbk Phase 18 CO-05
```

[Step B] DatumFindingService.cs — TryTeachCircleTwoHorizontal 호출 사이트 (teach 경로, L730 근처) 갱신:
시그니처 변경으로 컴파일 에러가 발생하는 곳. out 파라미터 목록에 `out bool[] circleStrips` 추가 후 config에 write-back:
```csharp
bool[] circleStrips; //260505 hbk Phase 18 CO-05
if (!visionSvc.TryFindCircleByPolarSampling(
        image,
        config.CircleROI_Row, config.CircleROI_Col, config.CircleROI_Radius,
        config.Circle_PolarStepDeg, config.Circle_RectL1Ratio, config.Circle_RectL2Ratio,
        config.Circle_Sigma, config.Circle_EdgeThreshold, circlePolarity,
        config.Circle_EdgeSelection,
        null,
        out centerRow, out centerCol, out radius,
        out circleEdgeRows, out circleEdgeCols,
        out circleStrips, //260505 hbk Phase 18 CO-05
        out circleError))
{
    config.LastTeachSucceeded = false;
    error = "Circle fit failed: " + circleError;
    return false;
}
config.CircleStripSuccesses = circleStrips; //260505 hbk Phase 18 CO-05 — per-strip 성공 여부 보관 (D-14: teach 경로만)
```

[Step C] DatumFindingService.cs — TryFindCircleTwoHorizontal 호출 사이트 (find 경로, L201 근처) 갱신:
D-14 결정: find 경로에서는 CircleStripSuccesses를 갱신하지 않는다. 신규 out 파라미터를 임시 변수로 수신 후 무시:
```csharp
bool[] unusedStrips; //260505 hbk Phase 18 CO-05 — D-14: find 경로는 strip 색상 갱신 없음
if (!visionSvc.TryFindCircleByPolarSampling(
        image,
        config.CircleROI_Row, ...,
        null,
        out centerRow, out centerCol, out radius,
        out circleEdgeRows, out circleEdgeCols,
        out unusedStrips, //260505 hbk Phase 18 CO-05
        out circleError))
{ ... }
```

C# 7.0 discard(`out _`)도 사용 가능하나, .NET Framework 4.8 C# 7.2 환경에서 discard는 지원됨. 어느 쪽이든 가능하나 `out bool[] unusedStrips`가 더 명시적.

[Step D] HalconDisplayService.cs — RenderCircleStripOverlay 색상 분기:
기존 `HOperatorSet.SetColor(window, "gray");` 라인을 루프 바깥에서 유지하되, 루프 내부에서 per-strip 색상을 결정하도록 변경:

```csharp
//260505 hbk Phase 18 CO-05 — per-strip 성공/실패 색상 분기 (D-15)
bool[] successes = datum.CircleStripSuccesses; //260505 hbk Phase 18 CO-05
HOperatorSet.SetLineWidth(window, 1);
for (int i = 0; i < stepCount; i++)
{
    //260505 hbk Phase 18 CO-05 — green=성공, red=실패, gray=데이터 없음(fallback)
    string stripColor = "gray"; //260505 hbk Phase 18 CO-05
    if (successes != null && i < successes.Length) //260505 hbk Phase 18 CO-05
        stripColor = successes[i] ? "green" : "red"; //260505 hbk Phase 18 CO-05
    HOperatorSet.SetColor(window, stripColor); //260505 hbk Phase 18 CO-05
    // ... 기존 DispLine x4 코드 ...
}
//260505 hbk Phase 18 CO-05 — 루프 완료 후 SetColor 상태 복원 (Halcon Window 전역 상태 오염 방지)
HOperatorSet.SetColor(window, "gray"); //260505 hbk Phase 18 CO-05
```

기존 `HOperatorSet.SetColor(window, "gray");`(루프 바깥, 현재 L484) 위치에 있는 SetColor 호출을 제거하고, 루프 내부에서 per-strip SetColor를 호출하며, 루프 종료 후 다시 "gray"로 복원.

주의: try 블록 내에 있으므로 SetColor 복원은 finally 또는 루프 종료 직후에 배치.
  </action>
  <verify>
    <automated>
      msbuild "C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /verbosity:minimal 2>&1 | Select-String -Pattern "error|warning|succeeded"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CircleStripSuccesses" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` ≥ 1 (프로퍼티 선언)
    - `grep -c "CircleStripSuccesses" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` ≥ 1 (write-back)
    - `grep -c "CircleStripSuccesses" WPF_Example/Halcon/Display/HalconDisplayService.cs` ≥ 1 (소비)
    - `grep -c "unusedStrips\|out _" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` ≥ 1 (find 경로 무시)
    - `grep -c "stripColor" WPF_Example/Halcon/Display/HalconDisplayService.cs` ≥ 3 (선언 + if 분기 + SetColor 호출)
    - `grep -c "D-14" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` ≥ 1 (find 경로 의도 주석)
    - msbuild Debug/x64 빌드 PASS (오류 0)
    - 모든 추가 라인에 `//260505 hbk Phase 18 CO-05` 주석 포함
  </acceptance_criteria>
  <done>DatumConfig에 CircleStripSuccesses 필드가 추가되고, TryTeachCircleTwoHorizontal이 teach 완료 시 write-back하며, RenderCircleStripOverlay가 per-strip 색상으로 렌더링한다. TryFindCircleTwoHorizontal(find 경로)은 CircleStripSuccesses를 갱신하지 않는다. 빌드가 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VisionAlgorithmService → DatumConfig | 알고리즘 결과(bool[])를 모델 필드에 기록 |
| DatumConfig.CircleStripSuccesses → HalconDisplayService | 렌더러가 transient 필드를 소비 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-01 | Tampering | DatumConfig.CircleStripSuccesses | accept | transient 필드로 INI/JSON 직렬화 제외됨. null + bounds check 적용됨. |
| T-18-04-02 | Denial of Service | RenderCircleStripOverlay SetColor 루프 | accept | stepCount는 최대 360 (stepDeg ≥ 1). SetColor per-strip은 O(n) 상수 비용. |
</threat_model>

<verification>
1. msbuild Debug/x64 PASS
2. `grep -c "CircleStripSuccesses" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` ≥ 1
3. `grep -c "CircleStripSuccesses" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` ≥ 1
4. `grep -c "stripColor" WPF_Example/Halcon/Display/HalconDisplayService.cs` ≥ 3
5. (런타임) CTH 티칭 성공 후 오버레이에서 녹색/빨강 strip 혼합 표시 확인
</verification>

<success_criteria>
- 4개 파일 수정 완료 (VisionAlgorithmService, DatumConfig, DatumFindingService, HalconDisplayService)
- TryFindCircleByPolarSampling 시그니처 확장 + per-strip 기록
- DatumConfig.CircleStripSuccesses transient 필드 (INI/JSON 제외)
- TryTeachCircleTwoHorizontal write-back + TryFindCircleTwoHorizontal 무시
- RenderCircleStripOverlay per-strip 색상 분기 + 루프 후 SetColor 복원
- 빌드 오류 0
</success_criteria>

<output>
완료 후 .planning/phases/18-carry-over-cleanup/18-04-SUMMARY.md 생성
</output>
