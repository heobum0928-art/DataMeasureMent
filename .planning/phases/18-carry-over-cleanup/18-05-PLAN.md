---
phase: 18
plan: 05
type: execute
wave: 3
depends_on:
  - "18-01"
  - "18-02"
  - "18-03"
  - "18-04"
files_modified:
  - .planning/phases/18-carry-over-cleanup/18-UAT.md
autonomous: true
requirements:
  - CO-03
must_haves:
  truths:
    - "18-UAT.md Test 10이 IsConfigured=false(새 Datum) 시나리오와 IsConfigured=true(재티칭) 시나리오 두 가지를 모두 명문화한다"
    - "Test 10에 grep 자동 검증 acceptance criterion이 포함된다"
    - "ValidateRoiPresence가 MainView.xaml.cs에 최소 2회 이상 등장한다 (정의 + 호출)"
  artifacts:
    - path: ".planning/phases/18-carry-over-cleanup/18-UAT.md"
      provides: "Phase 18 전체 UAT 문서 (Test 1~N + Test 10 spec 명문화)"
      contains: "ValidateRoiPresence"
  key_links:
    - from: "18-UAT.md Test 10"
      to: "MainView.xaml.cs ValidateRoiPresence"
      via: "grep acceptance criterion"
      pattern: "grep.*ValidateRoiPresence"
---

<objective>
Phase 18 UAT 문서(18-UAT.md)를 신규 생성하고, 특히 CO-03 요구사항인 Test 10을 IsConfigured 게이팅 동작에 맞게 재작성하여 사양을 명문화한다. 코드 변경 없음. (per D-04, D-05, D-06)

Purpose: Phase 17 Test 10의 "UI-SPEC vs UAT 모순" SKIP 항목을 올바른 사양으로 명문화하여 CO-03을 spec으로 해소한다.
Output: .planning/phases/18-carry-over-cleanup/18-UAT.md (신규 생성)
</objective>

<execution_context>
@C:\Users\tech\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\tech\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- ValidateRoiPresence 코드 (MainView.xaml.cs L717-738) — CO-03 spec 기준 -->
```csharp
private static string ValidateRoiPresence(DatumConfig d, EDatumAlgorithm alg) {
    if (d == null) return null;
    switch (alg) {
        case EDatumAlgorithm.TwoLineIntersect:
            if (d.Line1_Length1 <= 0 || d.Line2_Length1 <= 0)
                return "Line1/Line2 ROI 가 없습니다. 캔버스에 ROI 를 그리고 다시 시도하세요.";
            break;
        case EDatumAlgorithm.CircleTwoHorizontal:
            if (d.CircleROI_Radius <= 0)
                return "Circle ROI 가 없습니다. 캔버스에 원을 그리고 다시 시도하세요.";
            if (d.Horizontal_A_Length1 <= 0 || d.Horizontal_B_Length1 <= 0)
                return "Horizontal A/B ROI 가 없습니다. 캔버스에 ROI 를 그리고 다시 시도하세요.";
            break;
        case EDatumAlgorithm.VerticalTwoHorizontal:
            if (d.Vertical_Length1 <= 0)
                return "Vertical ROI 가 없습니다. 캔버스에 수직 ROI 를 그리고 다시 시도하세요.";
            if (d.Horizontal_A_Length1 <= 0 || d.Horizontal_B_Length1 <= 0)
                return "Horizontal A/B ROI 가 없습니다. 캔버스에 ROI 를 그리고 다시 시도하세요.";
            break;
    }
    return null;
}
```

<!-- TeachDatumButton_Click IsConfigured 게이팅 (MainView.xaml.cs L1347-1362) -->
```csharp
if (datum.IsConfigured) {  // 재티칭(IsConfigured=true)일 때만 ValidateRoiPresence 실행
    string missingRoiMsg = ValidateRoiPresence(datum, datum.AlgorithmTypeEnum);
    if (missingRoiMsg != null) {
        CustomMessageBox.Show("티칭 실패", missingRoiMsg);
        return;
    }
}
// IsConfigured=false (새 Datum) → StartDatumTeachStep wizard 진행
```

<!-- Phase 17 Test 10 원문 (SKIP 상태) -->
- 기존 기대: ROI 미생성 상태에서 btn_teachDatum 클릭 → ValidateRoiPresence 모달 표시
- 실제 동작: IsConfigured=false(새 Datum) → 모달 없이 wizard 진행 (올바른 동작 — D-04 결정)
- 결론: Test 10 자체가 잘못된 시나리오를 기대했음. 올바른 spec 재작성 필요.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 18-UAT.md 신규 생성 (Test 1~6 + Test 10 CO-03 명문화)</name>
  <files>.planning/phases/18-carry-over-cleanup/18-UAT.md</files>
  <read_first>
    - .planning/milestones/v1.0-phases/17-datum-ux-circle-strip-1-test-find-detectedorigin-hover/17-UAT.md (포맷 참고 + Test 10 원문)
    - .planning/phases/18-carry-over-cleanup/18-CONTEXT.md (D-04~D-06 결정)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (L717-738 ValidateRoiPresence, L1330-1376 TeachDatumButton_Click)
  </read_first>
  <action>
.planning/phases/18-carry-over-cleanup/18-UAT.md를 신규 생성한다. Phase 18의 5개 CO 항목에 대한 런타임 검증 항목을 포함한다.

파일 구조는 17-UAT.md 포맷을 따른다:

```yaml
---
phase: 18-carry-over-cleanup
updated: 2026-05-05
status: pending
summary:
  total: 6
  passed: 0
  failed: 0
  not_tested: 6
  skipped: 0
---
```

각 Test 항목은 아래 형식:
```
### Test N — [제목]
**단계**: ...
**기대**: ...
**Acceptance**: ...
**source**: CO-XX
**result**: not_tested
**notes**:
---
```

작성할 6개 Test:

**Test 1 — CO-01: Circle_RadialDirection PropertyGrid ItemsSource**
단계:
1. 애플리케이션 시작 → InspectionListView에서 CTH Datum 선택
2. PropertyGrid에서 Circle_RadialDirection 필드의 드롭다운 클릭

기대:
- 드롭다운에 "Inward"/"Outward" 두 항목만 표시 (LtoR/RtoL/TtoB/BtoT 없음)

Acceptance:
- `grep -c "Circle_RadialDirectionList" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` ≥ 2
- `grep -c "allNoFilter" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` ≥ 1
- 런타임: 콤보박스 항목 수 = 2

source: CO-01

---

**Test 2 — CO-04-A: "ROI 다시 그리기" 메뉴 표시 조건**
단계:
1. CTH Datum 선택 → btn_teachDatum ON (TeachDatum 모드 진입)
2. 캔버스에서 Datum Circle ROI 위에 우클릭

기대:
- ContextMenu에 "ROI 다시 그리기" 항목이 Visible로 표시됨
- 빈 캔버스 위 우클릭 시 해당 항목 Collapsed (미표시)

Acceptance:
- `grep -c "RedrawRoiMenuItem" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml` ≥ 1
- `grep -c "IsTeachDatumMode" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` ≥ 2

source: CO-04

---

**Test 3 — CO-04-B: "ROI 다시 그리기" 실행 동작**
단계:
1. CTH Datum 선택 → btn_teachDatum ON
2. Datum Circle ROI 위 우클릭 → "ROI 다시 그리기" 클릭

기대:
- Circle ROI 오버레이가 캔버스에서 사라짐 (Length/Radius = 0 리셋)
- btn_teachDatum 상태 유지 (TeachDatum 모드 OFF 되지 않음)

Acceptance:
- `grep -c "RoiRedrawRequested" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 1
- `grep -c "ClearDatumRoiFields" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 1

source: CO-04

---

**Test 4 — CO-05: Circle Strip 색상 분기**
단계:
1. CTH Datum 선택 → 정상 ROI 설정 → btn_teachDatum 클릭 (성공 케이스)
2. 캔버스에서 Circle 주변 polar strip 색상 확인
3. (선택) EdgeDirection을 의도적으로 반대로 설정 후 재티칭 → 실패 strip 확인

기대:
- 에지 검출 성공 strip: 녹색 (green)
- 에지 검출 실패 strip: 빨강 (red)
- CircleStripSuccesses가 null이거나 인덱스 범위 초과 시: 회색 (gray, fallback)

Acceptance:
- `grep -c "CircleStripSuccesses" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` ≥ 1
- `grep -c "stripColor" WPF_Example/Halcon/Display/HalconDisplayService.cs` ≥ 3

source: CO-05

---

**Test 5 — CO-06: FormatTeachError [DatumName] 접두사**
단계:
1. CTH Datum 이름 = "Datum 2" 확인 (PropertyGrid DatumName 필드)
2. EdgeDirection을 의도적으로 반대로 설정 → btn_teachDatum 클릭 (검출 실패 유도)
3. 실패 모달 텍스트 확인

기대:
- 모달 타이틀: "티칭 실패"
- 모달 메시지: "[Datum 2] 검출된 에지가 없습니다. EdgeDirection 설정을 반대로 변경한 후 다시 시도하세요."

Acceptance:
- `grep -c "datum.DatumName" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 1
- `grep -c "DatumConfig datum, string err" WPF_Example/UI/ContentItem/MainView.xaml.cs` = 1

source: CO-06

---

**Test 6 — CO-03: btn_teachDatum 호환성 가드 사양 명문화 (IsConfigured 게이팅)**

이 테스트는 Phase 17 Test 10 SKIP 항목을 올바른 사양으로 재작성한 것이다.
코드 변경 없음 — IsConfigured 게이팅(hotfix#3)이 이미 올바른 동작을 구현한다.

**시나리오 A — IsConfigured=false (새 Datum, 첫 티칭):**
단계:
1. 새 Datum 생성 → InspectionListView에서 선택 (IsConfigured=false 상태)
2. AlgorithmType = CircleTwoHorizontal 설정
3. 캔버스에 ROI를 그리지 않은 채로 btn_teachDatum 클릭

기대:
- ValidateRoiPresence 모달 표시 없음 (IsConfigured=false → 가드 스킵)
- Wizard 단계(StartDatumTeachStep)로 즉시 진입 — 첫 ROI 그리기 안내 시작

**시나리오 B — IsConfigured=true (기존 Datum 재티칭, ROI 삭제 후):**
단계:
1. 이미 티칭 완료된 CTH Datum 선택 (IsConfigured=true)
2. PropertyGrid에서 CircleROI_Radius를 0으로 수동 설정 (또는 Delete ROI 메뉴 사용)
3. btn_teachDatum 클릭

기대:
- ValidateRoiPresence 모달 표시: Title="티칭 실패", Message="Circle ROI 가 없습니다. 캔버스에 원을 그리고 다시 시도하세요."
- btn_teachDatum 자동 OFF + canvasMode = None

**Acceptance:**
- `grep -c "ValidateRoiPresence" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 2
- `grep -c "IsConfigured" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 1
- 시나리오 A: 새 Datum + ROI 미생성 + btn_teachDatum 클릭 → 모달 없이 wizard 진행
- 시나리오 B: 기존 Datum + ROI 삭제 + btn_teachDatum 클릭 → ValidateRoiPresence 모달 표시

source: CO-03 (D-04, D-05, D-06)
  </action>
  <verify>
    <automated>
      grep -c "ValidateRoiPresence" "C:\Info\Project\DataMeasurement\WPF_Example\UI\ContentItem\MainView.xaml.cs"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/18-carry-over-cleanup/18-UAT.md` 파일 존재
    - `grep -c "ValidateRoiPresence" .planning/phases/18-carry-over-cleanup/18-UAT.md` ≥ 1
    - `grep -c "IsConfigured=false" .planning/phases/18-carry-over-cleanup/18-UAT.md` ≥ 1 (시나리오 A)
    - `grep -c "IsConfigured=true" .planning/phases/18-carry-over-cleanup/18-UAT.md` ≥ 1 (시나리오 B)
    - `grep -c "CO-0" .planning/phases/18-carry-over-cleanup/18-UAT.md` ≥ 5 (5개 CO 항목 모두 참조)
    - `grep -c "ValidateRoiPresence" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 2 (코드 기준 충족 확인)
    - 각 Test에 result: not_tested 상태
  </acceptance_criteria>
  <done>18-UAT.md가 6개 Test를 포함하여 생성되고, Test 6(CO-03)이 IsConfigured 게이팅의 두 시나리오를 모두 명문화하며, grep acceptance criterion이 포함된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT 문서 → 코드 검증 | grep 명령으로 코드 현황을 검증 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05-01 | Repudiation | UAT 문서 기록 | accept | 문서 파일은 git 이력으로 추적됨. 코드 변경 없음. |
</threat_model>

<verification>
1. `.planning/phases/18-carry-over-cleanup/18-UAT.md` 파일 존재
2. `grep -c "ValidateRoiPresence" .planning/phases/18-carry-over-cleanup/18-UAT.md` ≥ 1
3. `grep -c "ValidateRoiPresence" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 2 (정의 + 호출)
4. Test 6에 시나리오 A(IsConfigured=false)와 시나리오 B(IsConfigured=true) 모두 포함
5. 5개 CO 항목 (CO-01/CO-03/CO-04/CO-05/CO-06) 모두 Test에 반영
</verification>

<success_criteria>
- 18-UAT.md 신규 생성 (6개 Test)
- Test 6이 IsConfigured 게이팅 두 시나리오를 spec으로 명문화
- 모든 Test에 grep-verifiable acceptance criterion 포함
- CO-03 요구사항 해소 확인 (grep 명령 실행 결과 ≥ 2)
</success_criteria>

<output>
완료 후 .planning/phases/18-carry-over-cleanup/18-05-SUMMARY.md 생성
</output>
