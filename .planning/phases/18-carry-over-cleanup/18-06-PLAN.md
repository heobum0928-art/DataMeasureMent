---
phase: 18
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
autonomous: false
gap_closure: true
requirements:
  - CO-01
must_haves:
  truths:
    - "CTH Datum 선택 시 PropertyGrid의 Circle_RadialDirection 콤보박스에 정확히 Inward/Outward 두 항목만 표시된다 (런타임 검증)"
    - "TLI/VTH 등 다른 알고리즘의 *EdgeDirection 콤보박스 회귀 없음 (각 알고리즘 의도된 항목만)"
    - "PropertyGrid 표면에 *List 프로퍼티 노출 없음 (IsHiddenForAlgorithm 으로 차단)"
    - "기존 INI 직렬화(ParamBase reflection)에 영향 없음 — List<string> 미직렬화 보존"
    - "msbuild Debug/x64 PASS, 신규 warning 0"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "[Browsable(false)] 일부 제거 + IsHiddenForAlgorithm 에 *List 일괄 hide 블록"
      contains: "Phase 18 18-06"
  key_links:
    - from: "DatumConfig.*List 프로퍼티 (Browsable 노출)"
      to: "PropertyTools.Wpf [ItemsSourceProperty] resolution"
      via: "owner.GetType().GetProperty(name) reflection"
      pattern: "PropertyTools 가 List<> 를 직접 발견하도록 노출"
---

<objective>
Phase 18-01 의 GetProperties whitelist 패턴(allNoFilter+sourceNames) 이 wrong layer (no-op fix) 임을 인정하고, PropertyTools.Wpf 가 실제로 사용하는 reflection lookup 경로 (`owner.GetType().GetProperty(name)`) 에 맞춰 *List 프로퍼티들의 [Browsable(false)] 를 제거하되, PropertyGrid 표면 노출은 IsHiddenForAlgorithm 에서 차단한다. (per UAT Test 1 root cause H1)

Purpose: Circle_RadialDirection PropertyGrid 콤보박스가 Inward/Outward 2항목만 표시하도록 근본 수정. 정적 grep 가 아닌 런타임 검증으로 PASS 확인.
Output: DatumConfig.cs 수정 + msbuild PASS + 사용자 런타임 UAT 3시나리오 통과.
</objective>

<execution_context>
@C:\Users\tech\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\tech\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-carry-over-cleanup/18-UAT.md
@.planning/phases/18-carry-over-cleanup/18-01-SUMMARY.md

<interfaces>
<!-- DatumConfig.cs 핵심 구조 -->
<!-- 파일: WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs -->

L242-249: Circle 카테고리 *List 프로퍼티들 (수정 대상)
```csharp
[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> Circle_EdgeDirectionList { get { return EdgeOptionLists.Directions; } }
[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> Circle_EdgePolarityList  { get { return EdgeOptionLists.DatumPolarities; } }
[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> Circle_EdgeSelectionList { get { return EdgeOptionLists.Selections; } }
[PropertyTools.DataAnnotations.Browsable(false)] //260503 hbk Phase 17 D-02
public List<string> Circle_RadialDirectionList { get { return EdgeOptionLists.RadialDirections; } }
```

L599-623: IsHiddenForAlgorithm (확장 대상)
```csharp
private static bool IsHiddenForAlgorithm(string name, EDatumAlgorithm alg) {
    switch (alg) {
        case EDatumAlgorithm.TwoLineIntersect:
            ...
        case EDatumAlgorithm.CircleTwoHorizontal:
            ...
        case EDatumAlgorithm.VerticalTwoHorizontal:
            ...
    }
    return false;
}
```

L545-587: GetProperties(Attribute[]) — 18-01 의 whitelist 패턴 (보존, deprecation 주석만 추가)
- allNoFilter + sourceNames foreach 블록은 H1 확정으로 no-op 임이 판명됨
- 코드 자체는 보존 (정리는 Phase 19 PropertyGrid 동적 노출 기반화 범위)
</interfaces>

<root_cause_summary>
Diagnosed 2026-05-07 (gsd-debugger H1): PropertyTools.Wpf 가 `[ItemsSourceProperty(nameof(Circle_RadialDirectionList))]` resolve 시 `ICustomTypeDescriptor.GetProperties(Attribute[])` 반환 컬렉션이 아니라 `owner.GetType().GetProperty("Circle_RadialDirectionList").GetValue(owner)` **직접 reflection** 호출 사용. *List 프로퍼티에 `[PropertyTools.DataAnnotations.Browsable(false)]` 가 있어 PropertyTools 가 못 찾고 fallback (Circle_EdgeDirectionList = 4항목) 적용.

→ 18-01 의 GetProperties whitelist 는 PropertyTools 가 도달하지 않는 레이어 → no-op fix.
</root_cause_summary>
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: H1 확정 — Circle_RadialDirectionList getter 호출 추적</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (L249 Circle_RadialDirectionList getter)
    - WPF_Example/Utility/Logging.cs (PrintLog 시그니처 + ELogType.Trace 존재 확인)
  </read_first>
  <action>
H1 (PropertyTools 가 GetProperties 우회) 확정용 임시 trace 로깅 추가. Circle_RadialDirectionList getter 진입 시 1회 출력.

**Logging.PrintLog 시그니처 확정 (2026-05-07 grep):**
- `Logging.cs:275` → `public static bool PrintLog(int logID, string msg)` ✓
- CLAUDE.md 준수: ELogType 은 항상 `(int)` 캐스트로 호출

L249 의 getter body 를 한 줄에서 multi-line 으로 변환:
```csharp
[PropertyTools.DataAnnotations.Browsable(false)] //260503 hbk Phase 17 D-02
public List<string> Circle_RadialDirectionList { //260507 hbk Phase 18 18-06 H1 trace — getter 호출 여부 검증
    get {
        ReringProject.Utility.Logging.PrintLog((int)ELogType.Trace, "[CO-01 H1 trace] Circle_RadialDirectionList getter called"); //260507 hbk Phase 18 18-06 H1 trace
        return EdgeOptionLists.RadialDirections;
    }
}
```

만약 ELogType.Trace 가 미정의면 (read_first 시점 grep `enum ELogType` 결과로 확인): `(int)ELogType.Camera` 또는 ID=1 같은 기본 trace 채널 사용 (`ReringProject.Utility.Logging.PrintLog(0, "...")` fallback). Trace 채널이 없으면 `Logging.PrintLog(0, ...)` 로 안전 fallback.
  </action>
  <verify>
    <automated>
      msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /verbosity:minimal 2>&1 | tail -5
    </automated>
  </verify>
  <human_check>
    1. msbuild Debug/x64 PASS 후 DatumMeasurement.exe 실행
    2. CTH Datum 선택 → PropertyGrid 에서 Circle_RadialDirection 드롭다운 클릭
    3. Trace 로그 파일 확인 — "[CO-01 H1 trace] Circle_RadialDirectionList getter called" 가 찍혔는가?
       - **호출 안 됨 (예상)** → H1 확정 → resume signal: `approved`
       - **호출됨** → H1 기각 → resume signal: `h1-rejected` → planner 재호출 필요 (다른 가설)
  </human_check>
  <resume_signals>
    - approved: H1 확정, Task 2 진행
    - h1-rejected: getter 가 호출되는데 4항목 표시 → 다른 원인 (PropertyTools getter 캐싱 or 컬렉션 swap). Task 중단, planner 재진단 의뢰.
  </resume_signals>
  <commit_message>chore(18-06): CO-01 H1 trace — Circle_RadialDirectionList getter 호출 추적 로그 임시 추가</commit_message>
</task>

<task type="auto">
  <name>Task 2: *List 프로퍼티 [Browsable(false)] 제거 + IsHiddenForAlgorithm 일괄 hide 블록 추가</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (전체 — *List 프로퍼티 21개 위치 확인 + IsHiddenForAlgorithm L603-623)
  </read_first>
  <action>
**(A) *List 프로퍼티의 `[PropertyTools.DataAnnotations.Browsable(false)]` 제거**

**파일 grep 검증 (2026-05-07):** DatumConfig.cs 에는 `public List<string> *List` 패턴 **총 21건** 존재.

처리 대상 = **알고리즘별 active *List 20개** (사용자 알고리즘 선택 시 PropertyGrid 콤보박스 ItemsSource 로 사용):
- AlgorithmTypeList (L43)
- Line1_EdgeDirectionList/EdgePolarityList/EdgeSelectionList (L121/123/125)
- Vertical_EdgeDirectionList/EdgePolarityList/EdgeSelectionList (L157/159/161)
- Line2_EdgeDirectionList/EdgePolarityList/EdgeSelectionList (L197/199/201)
- Circle_EdgeDirectionList/EdgePolarityList/EdgeSelectionList/RadialDirectionList (L243/245/247/249)
- Horizontal_A_EdgeDirectionList/EdgePolarityList/EdgeSelectionList (L287/289/291)
- Horizontal_B_EdgeDirectionList/EdgePolarityList/EdgeSelectionList (L327/329/331)

**제외 대상 = legacy `EdgePolarityList` (L351)**: 짝 프로퍼티 `EdgePolarity` (L348) 가 자체적으로 `[Browsable(false)]` 처리되어 있어 PropertyGrid 에 노출되지 않음. ItemsSource resolver 미사용 → Browsable 유지로 충분. 본 plan 범위 외 (정리는 Phase 19/20 코드 정리 phase 에서 결정).

각 active `*List` 프로퍼티 위의 `[PropertyTools.DataAnnotations.Browsable(false)]` 한 줄을 다음으로 교체:
```csharp
//260507 hbk Phase 18 18-06 — Browsable 제거: PropertyTools 가 [ItemsSourceProperty] reflection lookup 시 발견 가능하도록.
//  PropertyGrid 표면 노출은 아래 IsHiddenForAlgorithm 의 *List 일괄 hide 블록으로 차단.
```

`[System.ComponentModel.Browsable(false)]` 가 같이 붙은 항목 (예: DetectedOriginRow 와 같은 transient 패턴) 은 `*List` 가 아니므로 제외.

**(B) IsHiddenForAlgorithm 메서드 시작에 일괄 hide 블록 추가**

L603 의 `private static bool IsHiddenForAlgorithm(string name, EDatumAlgorithm alg) {` 직후, switch 문 진입 전에 다음 추가:
```csharp
//260507 hbk Phase 18 18-06 — *List ItemsSource 소스 프로퍼티는 PropertyGrid 노출에서 일괄 차단.
//  CO-01 fix: Browsable(false) 제거로 PropertyTools reflection lookup 활성화 + 여기서 표면 노출만 차단.
if (name.EndsWith("List")) return true; //260507 hbk Phase 18 18-06
```

**`EndsWith("List")` 충돌 검증 (2026-05-07 grep 완료):** DatumConfig.cs 의 `List` 로 끝나는 프로퍼티 21건 모두 ItemsSource 소스 (active 20 + legacy EdgePolarityList 1). false-positive = 0. legacy `EdgePolarityList` 도 PropertyGrid 에 보이면 안 되는 ItemsSource 컬렉션이라 동일 hide 가 의도와 부합 (EdgePolarity 짝 자체가 hidden 이므로 사용자 영향 0). → accept.
  </action>
  <verify>
    <automated>
      msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /verbosity:minimal 2>&1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "260507 hbk Phase 18 18-06 — Browsable 제거" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` ≥ 20 (active *List 20개 교체 주석)
    - `grep -c "260507 hbk Phase 18 18-06" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` ≥ 21 (위 20 + IsHiddenForAlgorithm 블록 1)
    - `grep -c "PropertyTools.DataAnnotations.Browsable(false)" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` baseline 대비 -20 (active *List 20 제거. legacy EdgePolarityList L351 + 비 *List Browsable 항목은 보존)
    - `grep -c "EndsWith(\"List\")" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` ≥ 1
    - msbuild Debug/x64 PASS, 신규 warning 0
  </acceptance_criteria>
  <commit_message>fix(18-06): CO-01 root cause — *List Browsable 제거 + IsHiddenForAlgorithm 일괄 hide</commit_message>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: 런타임 UAT 3 시나리오 검증</name>
  <human_check>
    msbuild Debug/x64 빌드 후 DatumMeasurement.exe 실행:

    **시나리오 1 (CTH RadialDirection — 본 fix 의 핵심):**
    - InspectionListView 에서 AlgorithmType=CircleTwoHorizontal Datum 선택
    - PropertyGrid 에서 Circle_RadialDirection 드롭다운 클릭
    - 기대: "Inward" / "Outward" 2항목만. LtoR/RtoL/TtoB/BtoT 미표시. ✓

    **시나리오 2 (회귀 방지 — 다른 알고리즘):**
    - TwoLineIntersect Datum 선택 → Line1_EdgeDirection 드롭다운
    - 기대: LtoR/RtoL/TtoB/BtoT 4항목 (의도된 Directions). ✓
    - VerticalTwoHorizontal Datum 선택 → Vertical_EdgeDirection 드롭다운
    - 기대: 의도된 Directions 항목. ✓

    **시나리오 3 (PropertyGrid 표면 — *List 미노출 회귀):**
    - 어떤 알고리즘이든 PropertyGrid 스크롤
    - 기대: "Circle_RadialDirectionList", "Line1_EdgeDirectionList" 등 *List 라는 row 가 보이지 않음. ✓

    셋 다 PASS → resume signal: `approved`
    하나라도 FAIL → resume signal: `regression: <어느 시나리오>` 또는 `still failing` (시나리오 1 재실패)
  </human_check>
  <resume_signals>
    - approved: 3 시나리오 모두 PASS, Task 4 진행
    - regression: 회귀 발생, 원복 또는 미세조정 의뢰
    - still failing: 시나리오 1 미통과, planner 재진단 의뢰 (H2/H3 가설로 분기)
  </resume_signals>
</task>

<task type="auto">
  <name>Task 4: H1 trace 로그 제거 + 최종 commit</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (L249 부근 trace 로그 라인)
  </read_first>
  <action>
Task 1 에서 추가한 임시 trace 코드를 원복 (단, [Browsable(false)] 는 Task 2 에서 제거된 상태):
```csharp
//260507 hbk Phase 18 18-06 — Browsable 제거: ... (Task 2 주석 보존)
public List<string> Circle_RadialDirectionList { get { return EdgeOptionLists.RadialDirections; } } //260503 hbk Phase 17 D-02
```

Logging.PrintLog 호출 라인 + multi-line getter 구조 → 한 줄 expression-bodied 로 복귀.

msbuild Debug/x64 재검증.
  </action>
  <verify>
    <automated>
      msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /verbosity:minimal 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CO-01 H1 trace" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` = 0
    - `grep -c "Circle_RadialDirectionList" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` ≥ 1 (정의 1건)
    - **Task 2 주석 보존 검증**: `grep -c "260507 hbk Phase 18 18-06 — Browsable 제거" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` ≥ 20 (Task 2 의 active *List 20개 주석 그대로 유지)
    - **IsHiddenForAlgorithm 블록 보존**: `grep -c "EndsWith(\"List\")" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` ≥ 1
    - msbuild Debug/x64 PASS, 신규 warning 0
  </acceptance_criteria>
  <commit_message>chore(18-06): CO-01 H1 trace 제거 — UAT PASS 확정 후 정리</commit_message>
</task>

</tasks>

<dependency_graph>
requires:
  - phase: 18-01
    provides: GetProperties(Attribute[]) whitelist 패턴 (보존, but no-op 임이 H1 으로 확정)
provides:
  - DatumConfig.cs *List 프로퍼티 PropertyTools reflection 노출
  - IsHiddenForAlgorithm *List 일괄 hide 블록
deprecates:
  - 18-01 의 allNoFilter + sourceNames whitelist (코드 보존, Phase 19 PropertyGrid 재설계 시 정리)
affects:
  - Phase 19 (PropertyGrid 동적 노출 기반화) — ICustomTypeDescriptor 와 PropertyTools 의 lookup 경로 차이를 인지한 설계 필요
</dependency_graph>

<threat_model>
T-18-06-01 (Tampering / Information Disclosure — INI 직렬화):
  source: ParamBase.Save/Load reflection 경로
  threat: *List 프로퍼티 [PropertyTools.DataAnnotations.Browsable(false)] 제거 시 INI 직렬화 대상에 신규 키 추가 위험
  mitigation:
    - ParamBase.cs Save/Load 가 List<string> 타입 처리 여부 read_first 시점 grep 검증 (Task 2 read_first 단계).
    - **사전 분석 (2026-05-07):** ParamBase 는 18-01 SUMMARY 에서 확인된 대로 List<string> 타입을 직렬화에서 제외 (switch-case 가 string/int/double/bool/enum 만 처리). 18-01 의 GetProperties whitelist 가 INI 영향 없이 통과한 이력이 1차 보강 증거.
    - 제거하는 어트리뷰트는 `PropertyTools.DataAnnotations.Browsable` (PropertyGrid UI 어트리뷰트) 이며 `System.ComponentModel.Browsable` 이 아님 → 표준 reflection 직렬화 경로 영향 0.
  status: accept (pre-analysis), Task 2 read_first 단계에서 ParamBase.Save/Load 본문 재확인.

T-18-06-02 (Regression — EndsWith("List") false-positive):
  source: IsHiddenForAlgorithm 의 광역 매치
  threat: 의도하지 않은 데이터 프로퍼티 hide
  mitigation: DatumConfig.cs 전체 grep 결과 `List` 로 끝나는 프로퍼티 21건 모두 ItemsSource 컬렉션 (active 20 + legacy EdgePolarityList 1) — false-positive = 0. legacy 도 hidden 됨이 의도와 부합 (EdgePolarity 짝 자체가 hidden).
  status: accept

T-18-06-03 (Denial of Service — IsHiddenForAlgorithm overhead):
  source: PropertyGrid 재바인딩 시 매 프로퍼티마다 IsHiddenForAlgorithm 호출
  threat: EndsWith("List") 추가로 인한 미세한 성능 영향
  mitigation: O(L) 문자열 검사 (L=4). 영향 무시 가능 (accept).
  status: accept
</threat_model>

<self_check>
- [ ] msbuild Debug/x64 PASS, 신규 warning 0
- [ ] Task 1 H1 trace 가 commit fbfba37 이후 추가됨 (4 commit 시퀀스: trace add → fix → trace remove)
- [ ] Task 2 active *List Browsable 제거 정확히 20개 (legacy EdgePolarityList L351 제외)
- [ ] Task 2 IsHiddenForAlgorithm `EndsWith("List")` 1개 추가
- [ ] Task 3 런타임 3시나리오 사용자 PASS
- [ ] Task 4 trace 완전 제거 (grep = 0)
- [ ] 모든 변경 라인 `//260507 hbk Phase 18 18-06` 주석
- [ ] 18-01 의 whitelist 코드는 보존 (deprecation 주석만 commit message 에 명시)
</self_check>
