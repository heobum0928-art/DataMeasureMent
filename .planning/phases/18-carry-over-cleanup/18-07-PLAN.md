---
phase: 18
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: false
gap_closure: true
requirements:
  - CO-04
must_haves:
  truths:
    - "새 Datum (IsConfigured=false) + btn_teachDatum 클릭: 모달 없이 wizard(StartDatumTeachStep) 즉시 시작"
    - "기존 Datum (IsConfigured=true) + 일부 ROI 누락 + btn_teachDatum: 기존 ValidateRoiPresence 친절 에러 모달 표시 (재티칭 모달 X)"
    - "기존 Datum (IsConfigured=true) + 모든 ROI 존재 + btn_teachDatum: 신규 '재티칭 확인' 모달 표시 → No=취소(ExitCanvasMode + 버튼 OFF), Yes=즉시 teach 진행"
    - "msbuild Debug/x64 PASS, 신규 warning 0"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "TeachDatumButton_Click 에 재티칭 확인 모달 분기"
      contains: "재티칭 확인"
  key_links:
    - from: "TeachDatumButton_Click ON branch"
      to: "InvokeTryTeachDatum (Done step)"
      via: "datum.IsConfigured + ValidateRoiPresence null + 모든 ROI 존재 → 사용자 확인"
      pattern: "CustomMessageBox.ShowConfirmation YesNo"
---

<objective>
기존 티칭된 Datum (IsConfigured=true + 모든 ROI 존재) 에서 btn_teachDatum 클릭 시 사용자에게 명시적 확인 모달을 띄워 (1) 의도치 않은 silent re-teach 방지, (2) 사용자에게 "버튼 먹힘" 시각 신호 제공. (per UAT Test 2 reclassified — minor enhancement)

Purpose: silent re-teach 동작을 의식적으로 사용자가 승인하도록 게이트 추가.
Output: TeachDatumButton_Click 단일 분기 추가 + msbuild PASS + 사용자 시나리오 3개 PASS.
</objective>

<execution_context>
@C:\Users\tech\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\tech\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-carry-over-cleanup/18-UAT.md

<interfaces>
<!-- MainView.xaml.cs TeachDatumButton_Click 현재 흐름 (L1342-1392) -->

```csharp
private void TeachDatumButton_Click(object sender, RoutedEventArgs e) {
    if (btn_teachDatum.IsChecked == true) {
        ExitCanvasMode();
        _canvasMode = ECanvasMode.TeachDatum;
        btn_teachDatum.IsChecked = true;
        halconViewer.IsTeachDatumMode = true;

        var datum = mParentWindow?.inspectionList?.SelectedParam as DatumConfig;
        if (datum == null) { /* 모달 + return */ }
        _editingDatum = datum;
        PublishDatumRoiCandidates(datum);

        if (datum.IsConfigured) {
            string missingRoiMsg = ValidateRoiPresence(datum, datum.AlgorithmTypeEnum);
            if (missingRoiMsg != null) {
                CustomMessageBox.Show("티칭 실패", missingRoiMsg);
                btn_teachDatum.IsChecked = false;
                _canvasMode = ECanvasMode.None;
                _editingDatum = null;
                halconViewer.IsEditMode = false;
                halconViewer.IsTeachDatumMode = false;
                return;
            }
            // ← 여기 (L1376 와 L1378 사이) 본 plan 의 재티칭 확인 모달 삽입
        }

        halconViewer.IsEditMode = false;
        _datumTeachStep = GetFirstMissingStep(datum);
        StartDatumTeachStep(_datumTeachStep);
    }
    else { /* 수동 OFF */ }
}
```

<!-- CustomMessageBox.ShowConfirmation 시그니처 (기존 사용처 L521-525) -->
```csharp
var choice = CustomMessageBox.ShowConfirmation(
    "ROI 삭제",
    "현재 Datum 의 모든 ROI 를 삭제하시겠습니까?",
    MessageBoxButton.OKCancel);
if (choice != MessageBoxResult.OK) return;
```

→ 본 plan 에서 동일 시그니처로 `MessageBoxButton.YesNo` 사용. 반환은 `MessageBoxResult.Yes/No`.
</interfaces>

<scenario_table>
| datum.IsConfigured | ROI 누락 | 동작 |
|---|---|---|
| false (신규) | N/A | wizard 시작 (모달 X) — 기존 흐름 보존 |
| true | 일부 누락 | ValidateRoiPresence 친절 에러 모달 (본 plan 모달 도달 X) — 기존 흐름 보존 |
| true | 모두 존재 | **본 plan 의 재티칭 확인 모달** (Yes/No) → No=취소, Yes=Done step (InvokeTryTeachDatum) |
</scenario_table>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TeachDatumButton_Click 에 재티칭 확인 모달 분기 추가</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (L1342-1392 TeachDatumButton_Click 본문)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (L515-525 CustomMessageBox.ShowConfirmation 사용 사례)
  </read_first>
  <action>
L1376 (`if (missingRoiMsg != null) { ... return; }` 닫는 `}`) 와 L1378 (`halconViewer.IsEditMode = false;`) 사이에 다음 분기 추가. **L1376-1377 의 빈 줄을 활용**해 `if (datum.IsConfigured) { ... }` 블록의 닫는 `}` 직전에 삽입:

```csharp
                //260507 hbk Phase 18 18-07 — 재티칭 확인 모달: IsConfigured=true & 모든 ROI 존재 → 즉시 teach 시나리오에서 사용자 의사 확인.
                //  Silent re-teach 방지 + 버튼 먹힘 시각 신호 제공. ValidateRoiPresence 가 null 통과한 시점이므로 모든 ROI 존재 보장.
                var reteachChoice = CustomMessageBox.ShowConfirmation( //260507 hbk Phase 18 18-07
                    "재티칭 확인", //260507 hbk Phase 18 18-07
                    "이 Datum 은 이미 티칭되어 있습니다.\n다시 티칭하시겠습니까?", //260507 hbk Phase 18 18-07
                    MessageBoxButton.YesNo); //260507 hbk Phase 18 18-07
                if (reteachChoice != MessageBoxResult.Yes) { //260507 hbk Phase 18 18-07
                    btn_teachDatum.IsChecked = false; //260507 hbk Phase 18 18-07
                    _canvasMode = ECanvasMode.None; //260507 hbk Phase 18 18-07
                    _editingDatum = null; //260507 hbk Phase 18 18-07
                    halconViewer.IsEditMode = false; //260507 hbk Phase 18 18-07
                    halconViewer.IsTeachDatumMode = false; //260507 hbk Phase 18 18-07
                    return; //260507 hbk Phase 18 18-07
                } //260507 hbk Phase 18 18-07
```

삽입 정확한 위치: `if (datum.IsConfigured) { ... ValidateRoiPresence ... if (missingRoiMsg != null) { ... return; } /* ← 본 분기 추가 */ }` — `if (datum.IsConfigured)` 블록의 닫는 `}` 바로 위, ValidateRoiPresence 가드의 닫는 `}` 바로 아래.

주의:
- ExitCanvasMode() 는 호출하지 않음 — `if (datum.IsConfigured)` 진입 전에 이미 `ExitCanvasMode()` (L1344) 가 호출되어 prior canvas 정리 완료. No 분기는 ValidateRoiPresence 친절 에러 가드 (L1369-1374) 와 동일한 정리 패턴 사용 (개별 필드 reset).
- CustomMessageBox.ShowConfirmation 은 동기 모달. 모달 표시 중 다른 입력 차단됨 — race condition 없음.
- C# 7.2 호환: `var` + `MessageBoxButton.YesNo` 모두 .NET Framework 4.8 표준.
- **Yes 분기 동작 (명시):** 본 분기 안에 Yes-경로 코드는 없음. Yes 선택 시 모달 닫히고 `if (reteachChoice != ...)` 가 false 가 되어 통과 → `if (datum.IsConfigured)` 블록 종료 → L1378 `halconViewer.IsEditMode = false;` → L1384 `_datumTeachStep = GetFirstMissingStep(datum);` → 모든 ROI 존재이므로 결과는 `EDatumTeachStep.Done` → L1385 `StartDatumTeachStep(Done)` → `InvokeTryTeachDatum()` (기존 silent re-teach 경로) 그대로 실행. **즉, Yes = "기존 동작 유지" 의 명시적 사용자 승인.**
  </action>
  <verify>
    <automated>
      msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /verbosity:minimal 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "재티칭 확인" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 1
    - `grep -c "260507 hbk Phase 18 18-07" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 12 (실제: 코멘트 2 + 모달 호출 4 + if 1 + reset 5 + return 1 + 닫는 } 1 = 14, ≥12 로 완화)
    - `grep -c "MessageBoxButton.YesNo" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 1
    - `grep -c "reteachChoice" WPF_Example/UI/ContentItem/MainView.xaml.cs` ≥ 2 (선언 + 비교)
    - msbuild Debug/x64 PASS, 신규 warning 0
  </acceptance_criteria>
  <commit_message>feat(18-07): 재티칭 확인 모달 추가 — silent re-teach 방지 + 사용자 신호 제공</commit_message>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: 시나리오 3개 런타임 검증</name>
  <human_check>
    msbuild Debug/x64 빌드 후 DatumMeasurement.exe 실행:

    **시나리오 1 — 새 Datum (IsConfigured=false):**
    - 새 CTH Datum 생성 → 선택 (IsConfigured=false 상태)
    - btn_teachDatum 클릭
    - 기대: 모달 없이 즉시 wizard 시작 ("Step 1/3: Circle 검색 영역 ..." 안내 텍스트). ✓

    **시나리오 2 — 기존 Datum + ROI 일부 누락 (ValidateRoiPresence 가드):**
    - 기존 티칭된 CTH Datum 선택 → PropertyGrid 에서 CircleROI_Radius 를 0 으로 변경
    - btn_teachDatum 클릭
    - 기대: "티칭 실패" 모달 + Circle ROI 누락 메시지. **재티칭 모달 X**. ✓

    **시나리오 3 — 기존 Datum + 모든 ROI 존재 (본 plan 의 핵심):**
    - 기존 티칭된 CTH Datum 선택 (모든 ROI 존재) → 이미지 로드 상태
    - btn_teachDatum 클릭
    - 기대: "재티칭 확인" 모달 표시 ("이 Datum 은 이미 티칭되어 있습니다. 다시 티칭하시겠습니까?")
    - **3a. No 클릭** → 모달 닫힘, btn_teachDatum 토글 OFF, TeachDatum 모드 미진입. ✓
    - **3b. Yes 클릭** → 모달 닫힘, 즉시 teach 실행 (overlay 갱신 또는 실패 시 [DatumName] 모달). ✓

    셋 다 PASS → resume signal: `approved`
    하나라도 FAIL → resume signal: `regression: <어느 시나리오>` 또는 `still failing: <설명>`
  </human_check>
  <resume_signals>
    - approved: 3 시나리오 모두 PASS, 다음 단계 진행 (full UAT 재실행)
    - regression: 회귀 발생, 분기 위치 또는 reset 항목 미세조정 의뢰
    - still failing: 모달 자체 미표시 또는 No 분기 누락 — read_first 결과로 시그니처 재확인
  </resume_signals>
</task>

</tasks>

<dependency_graph>
requires: []
provides:
  - TeachDatumButton_Click 의 silent re-teach → 명시적 확인 모달 게이트
affects:
  - UAT Tests 2/3/4/5/6 — 모달 통해 TeachDatum 모드 진입 가능 → carry-over UAT 재실행 시 자연스러운 흐름
</dependency_graph>

<threat_model>
T-18-07-01 (UI Race / Repudiation):
  source: 모달 표시 중 다른 UI 이벤트
  threat: 모달 표시 중 다른 버튼 클릭으로 상태 머신 꼬임
  mitigation: CustomMessageBox.ShowConfirmation 은 동기 모달 (다른 입력 차단). accept.
  status: accept

T-18-07-02 (Information Disclosure):
  source: 모달 텍스트
  threat: DatumName 등 사용자 식별자 노출
  mitigation: 본 모달은 generic 텍스트 ("이 Datum") — DatumName 미포함. accept.
  status: accept
</threat_model>

<self_check>
- [ ] msbuild Debug/x64 PASS, 신규 warning 0
- [ ] CustomMessageBox.ShowConfirmation 시그니처 (string title, string message, MessageBoxButton) 올바름
- [ ] No 분기 reset 5개 필드: IsChecked / _canvasMode / _editingDatum / IsEditMode / IsTeachDatumMode
- [ ] No 분기 ExitCanvasMode() 호출 안 함 (이미 L1344 에서 prior 정리됨, 개별 reset 으로 충분)
- [ ] 모든 변경 라인 `//260507 hbk Phase 18 18-07` 주석
- [ ] 시나리오 1/2/3a/3b 사용자 PASS
- [ ] CO-04 의 Test 2 (UAT 메뉴 표시 조건) 가 본 모달 통해 자연스럽게 검증 가능 — full UAT 재실행 시 활용
</self_check>
