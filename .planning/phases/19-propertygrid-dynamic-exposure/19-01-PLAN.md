---
phase: 19-propertygrid-dynamic-exposure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements:
  - QUAL-03
  - CO-02

must_haves:
  truths:
    - "DynamicPropertyHelper.FilterProperties 메서드가 존재하고, hideFunc + sourceNames 두 개의 제어 인자를 받는다"
    - "DatumConfig.GetProperties(Attribute[]) 가 Phase 17-02 / Phase 18 CO-01 과 동일한 PropertyDescriptor 목록을 반환한다 (회귀 0)"
    - "msbuild Debug/x64 가 새 파일 미등록 CS0246 오류 없이 PASS 한다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs"
      provides: "FilterProperties 공통 정적 헬퍼"
      exports: ["DynamicPropertyHelper.FilterProperties"]
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "DatumConfig 리팩토링 — GetProperties 내부를 헬퍼 호출로 교체"
      contains: "DynamicPropertyHelper.FilterProperties"
    - path: "WPF_Example/DatumMeasurement.csproj"
      provides: "DynamicPropertyHelper.cs Compile ItemGroup 등록"
      contains: "DynamicPropertyHelper.cs"
  key_links:
    - from: "DatumConfig.GetProperties(Attribute[])"
      to: "DynamicPropertyHelper.FilterProperties"
      via: "직접 정적 메서드 호출"
      pattern: "DynamicPropertyHelper\\.FilterProperties"
    - from: "DatumConfig.IsHiddenForAlgorithm"
      to: "DynamicPropertyHelper.FilterProperties hideFunc 인자"
      via: "람다 콜백"
      pattern: "IsHiddenForAlgorithm\\(name, alg\\)"
---

<objective>
Phase 19 Wave 1: DynamicPropertyHelper 공통 헬퍼 신규 생성 + DatumConfig 리팩토링.

DatumConfig.GetProperties(Attribute[]) 내부의 allNoFilter+sourceNames 패턴(Phase 18 CO-01)을
독립 정적 헬퍼 DynamicPropertyHelper.FilterProperties 로 추출하고,
DatumConfig 의 GetProperties 를 헬퍼 호출로 교체한다.

Purpose: Wave 2(FAIConfig ICustomTypeDescriptor 구현)의 기반을 만들고, CO-02(DatumConfig 동작 회귀 0) 를 확립한다.
Output: DynamicPropertyHelper.cs (신규), DatumConfig.cs (GetProperties 교체), DatumMeasurement.csproj (Compile 등록)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- DatumConfig.cs 현재 GetProperties 블록 (lines 555-586) — 헬퍼로 추출할 원본 -->
```csharp
// DatumConfig.cs lines 555-586
public System.ComponentModel.PropertyDescriptorCollection GetProperties(System.Attribute[] attributes) {
    var all = System.ComponentModel.TypeDescriptor.GetProperties(this, attributes, true);
    var alg = AlgorithmTypeEnum;
    var keep = new List<System.ComponentModel.PropertyDescriptor>();
    foreach (System.ComponentModel.PropertyDescriptor pd in all) {
        if (IsHiddenForAlgorithm(pd.Name, alg)) continue;
        keep.Add(pd);
    }
    var allNoFilter = System.ComponentModel.TypeDescriptor.GetProperties(this, true);
    var sourceNames = new System.Collections.Generic.HashSet<string> {
        nameof(AlgorithmTypeList),
        nameof(Circle_EdgeDirectionList), nameof(Circle_EdgePolarityList),
        nameof(Circle_EdgeSelectionList), nameof(Circle_RadialDirectionList),
        nameof(Horizontal_A_EdgeDirectionList), nameof(Horizontal_A_EdgePolarityList),
        nameof(Horizontal_A_EdgeSelectionList),
        nameof(Horizontal_B_EdgeDirectionList), nameof(Horizontal_B_EdgePolarityList),
        nameof(Horizontal_B_EdgeSelectionList),
        nameof(Line1_EdgeDirectionList), nameof(Line1_EdgePolarityList), nameof(Line1_EdgeSelectionList),
        nameof(Line2_EdgeDirectionList), nameof(Line2_EdgePolarityList), nameof(Line2_EdgeSelectionList),
        nameof(Vertical_EdgeDirectionList), nameof(Vertical_EdgePolarityList), nameof(Vertical_EdgeSelectionList),
    };
    foreach (System.ComponentModel.PropertyDescriptor pd in allNoFilter) {
        if (sourceNames.Contains(pd.Name) && !keep.Exists(k => k.Name == pd.Name))
            keep.Add(pd);
    }
    return new System.ComponentModel.PropertyDescriptorCollection(keep.ToArray());
}
```

<!-- DatumMeasurement.csproj Inspection 블록 삽입 위치 (lines 207-218) -->
```xml
    <Compile Include="Custom\Sequence\Inspection\DatumConfig.cs" />
    <Compile Include="Custom\Sequence\Inspection\EDatumAlgorithm.cs" />
    <Compile Include="Custom\Sequence\Inspection\EdgeOptionLists.cs" />
    <Compile Include="Custom\Sequence\Inspection\FAIConfig.cs" />
    <Compile Include="Custom\Sequence\Inspection\ShotConfig.cs" />
    <Compile Include="Custom\Sequence\Inspection\InspectionRecipeManager.cs" />
    <Compile Include="Custom\Sequence\Inspection\Action_FAIMeasurement.cs" />
    <Compile Include="Custom\Sequence\Inspection\InspectionSequence.cs" />
    <Compile Include="Custom\Sequence\Inspection\InspectionMasterParam.cs" />
    <Compile Include="Custom\Sequence\Inspection\MeasurementBase.cs" />
    <Compile Include="Custom\Sequence\Inspection\MeasurementFactory.cs" />
    <!-- DynamicPropertyHelper.cs 를 MeasurementFactory.cs 다음 줄에 추가 -->
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DynamicPropertyHelper.cs 신규 생성 + csproj Compile 등록</name>
  <files>
    WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs
    WPF_Example/DatumMeasurement.csproj
  </files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (lines 1-7 헤더/네임스페이스 패턴, lines 555-586 추출 원본)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs (static class 선언 패턴)
    - WPF_Example/DatumMeasurement.csproj (lines 200-225 Compile ItemGroup — 삽입 위치 확인)
  </read_first>
  <action>
**DynamicPropertyHelper.cs 생성 — 정확한 내용:**

파일 경로: `WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs`

```csharp
//260507 hbk Phase 19 QUAL-03: DynamicPropertyHelper — FilterProperties 공통 헬퍼 (DatumConfig/FAIConfig 공유)
using System.Collections.Generic;

namespace ReringProject.Sequence {

    /// <summary>
    /// PropertyTools.Wpf PropertyGrid용 동적 필터링 헬퍼.
    /// hideFunc이 true를 반환하는 프로퍼티를 제외하고, sourceNames 화이트리스트에 해당하는
    /// [Browsable(false)] 소스 프로퍼티를 강제 포함한다 (Phase 18 CO-01 패턴).
    /// ParamBase INI 직렬화는 GetType().GetProperties() Reflection 경로 사용 → 이 헬퍼 영향 없음.
    /// </summary>
    public static class DynamicPropertyHelper { //260507 hbk Phase 19 QUAL-03

        /// <summary>
        /// PropertyGrid용 PropertyDescriptorCollection 을 동적 필터링하여 반환한다.
        /// </summary>
        /// <param name="obj">대상 객체 (ICustomTypeDescriptor.GetProperties 의 this)</param>
        /// <param name="attrs">PropertyGrid 가 전달하는 Attribute 필터 배열</param>
        /// <param name="hideFunc">true 반환 시 해당 프로퍼티를 숨긴다</param>
        /// <param name="sourceNames">[Browsable(false)] ItemsSource 프로퍼티 명 집합 — 강제 포함 화이트리스트</param>
        public static System.ComponentModel.PropertyDescriptorCollection FilterProperties( //260507 hbk Phase 19 QUAL-03
            object obj,
            System.Attribute[] attrs,
            System.Func<string, bool> hideFunc,
            System.Collections.Generic.HashSet<string> sourceNames) {
            var all = System.ComponentModel.TypeDescriptor.GetProperties(obj, attrs, true); //260507 hbk Phase 19 QUAL-03
            var keep = new List<System.ComponentModel.PropertyDescriptor>(); //260507 hbk Phase 19 QUAL-03
            foreach (System.ComponentModel.PropertyDescriptor pd in all) {
                if (hideFunc(pd.Name)) continue; //260507 hbk Phase 19 QUAL-03 — hideFunc 콜백 필터
                keep.Add(pd);
            }
            // Phase 18 CO-01 패턴: [Browsable(false)] ItemsSource 소스 프로퍼티는 attrs 필터에서 제외됨.
            // Browsable 필터 없는 전체 재조회 후 sourceNames 화이트리스트 항목만 명시 추가.
            var allNoFilter = System.ComponentModel.TypeDescriptor.GetProperties(obj, true); //260507 hbk Phase 18 CO-01 패턴
            foreach (System.ComponentModel.PropertyDescriptor pd in allNoFilter) { //260507 hbk Phase 18 CO-01 패턴
                if (sourceNames.Contains(pd.Name) && !keep.Exists(k => k.Name == pd.Name)) //260507 hbk Phase 18 CO-01 패턴
                    keep.Add(pd); //260507 hbk Phase 18 CO-01 패턴
            }
            return new System.ComponentModel.PropertyDescriptorCollection(keep.ToArray()); //260507 hbk Phase 19 QUAL-03
        }
    }
}
```

**DatumMeasurement.csproj 수정 — MeasurementFactory.cs 항목 바로 다음에 삽입:**

`<Compile Include="Custom\Sequence\Inspection\MeasurementFactory.cs" />` 라인 다음에 아래 줄 추가:
```xml
    <Compile Include="Custom\Sequence\Inspection\DynamicPropertyHelper.cs" />
```

경로 구분자: 반드시 백슬래시 `\` 사용. 슬래시 `/` 금지.
  </action>
  <verify>
    <automated>grep -n "DynamicPropertyHelper" "WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs" && grep -n "DynamicPropertyHelper.cs" "WPF_Example/DatumMeasurement.csproj"</automated>
  </verify>
  <acceptance_criteria>
    - DynamicPropertyHelper.cs 파일 존재: `WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs`
    - 네임스페이스: `namespace ReringProject.Sequence {` (K&R 스타일 — 중괄호 같은 줄)
    - 클래스: `public static class DynamicPropertyHelper {`
    - 메서드 시그니처: `public static System.ComponentModel.PropertyDescriptorCollection FilterProperties(`
    - 인자 4개: `object obj`, `System.Attribute[] attrs`, `System.Func<string, bool> hideFunc`, `System.Collections.Generic.HashSet<string> sourceNames`
    - 내부 allNoFilter 라인: `var allNoFilter = System.ComponentModel.TypeDescriptor.GetProperties(obj, true);`
    - csproj 등록: `grep "DynamicPropertyHelper.cs" WPF_Example/DatumMeasurement.csproj` 1건 이상 출력
    - csproj 경로 구분자: `Custom\Sequence\Inspection\DynamicPropertyHelper.cs` (백슬래시)
    - 파일 헤더 첫 줄: `//260507 hbk Phase 19 QUAL-03:`
  </acceptance_criteria>
  <done>DynamicPropertyHelper.cs 가 생성되고, csproj 에 Compile 항목이 등록되어 빌드 경로에 포함된다.</done>
</task>

<task type="auto">
  <name>Task 2: DatumConfig.GetProperties 리팩토링 (헬퍼 호출로 교체)</name>
  <files>
    WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  </files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (lines 550-630 — GetProperties 블록 전체 + IsHiddenForAlgorithm + 클래스 종료)
    - WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs (방금 생성한 파일 — FilterProperties 시그니처 확인)
  </read_first>
  <action>
**DatumConfig.cs lines 555-586 교체 — 정확한 변경:**

현재 GetProperties(Attribute[]) 메서드 전체 본문(lines 555-586)을 아래로 교체한다.
IsHiddenForAlgorithm, GetProperties() 무인자, 10개 stub 메서드는 변경하지 않는다.

**교체 전 (lines 555-586):**
```csharp
        public System.ComponentModel.PropertyDescriptorCollection GetProperties(System.Attribute[] attributes) {
            var all = System.ComponentModel.TypeDescriptor.GetProperties(this, attributes, true);
            var alg = AlgorithmTypeEnum;
            var keep = new List<System.ComponentModel.PropertyDescriptor>();
            foreach (System.ComponentModel.PropertyDescriptor pd in all) {
                if (IsHiddenForAlgorithm(pd.Name, alg)) continue; //260503 hbk Phase 17 D-09 — alg 별 hide 필터
                keep.Add(pd);
            }
            //260505 hbk Phase 18 CO-01 — ItemsSource 소스 List<> 프로퍼티는 [Browsable(false)]이므로
            // TypeDescriptor.GetProperties(this, attributes, true) 에서 제외됨.
            // PropertyTools.Wpf 가 [ItemsSourceProperty] 이름을 GetProperties 반환 컬렉션에서 조회할 때
            // Circle_RadialDirectionList 등을 찾지 못해 fallback(Directions 4항목)이 적용되는 버그.
            // → Browsable 필터 없이 전체 재조회하여 소스 프로퍼티를 명시적으로 추가.
            var allNoFilter = System.ComponentModel.TypeDescriptor.GetProperties(this, true); //260505 hbk Phase 18 CO-01
            var sourceNames = new System.Collections.Generic.HashSet<string> { //260505 hbk Phase 18 CO-01
                nameof(AlgorithmTypeList),
                nameof(Circle_EdgeDirectionList), nameof(Circle_EdgePolarityList),
                nameof(Circle_EdgeSelectionList), nameof(Circle_RadialDirectionList),
                nameof(Horizontal_A_EdgeDirectionList), nameof(Horizontal_A_EdgePolarityList),
                nameof(Horizontal_A_EdgeSelectionList),
                nameof(Horizontal_B_EdgeDirectionList), nameof(Horizontal_B_EdgePolarityList),
                nameof(Horizontal_B_EdgeSelectionList),
                nameof(Line1_EdgeDirectionList), nameof(Line1_EdgePolarityList), nameof(Line1_EdgeSelectionList),
                nameof(Line2_EdgeDirectionList), nameof(Line2_EdgePolarityList), nameof(Line2_EdgeSelectionList),
                nameof(Vertical_EdgeDirectionList), nameof(Vertical_EdgePolarityList), nameof(Vertical_EdgeSelectionList),
            }; //260505 hbk Phase 18 CO-01
            foreach (System.ComponentModel.PropertyDescriptor pd in allNoFilter) { //260505 hbk Phase 18 CO-01
                if (sourceNames.Contains(pd.Name) && !keep.Exists(k => k.Name == pd.Name)) //260505 hbk Phase 18 CO-01
                    keep.Add(pd); //260505 hbk Phase 18 CO-01
            } //260505 hbk Phase 18 CO-01
            return new System.ComponentModel.PropertyDescriptorCollection(keep.ToArray());
        }
```

**교체 후:**
```csharp
        //260507 hbk Phase 19 QUAL-03: DynamicPropertyHelper 헬퍼로 위임 (Phase 17 D-09 + Phase 18 CO-01 동작 동일)
        public System.ComponentModel.PropertyDescriptorCollection GetProperties(System.Attribute[] attributes) {
            var alg = AlgorithmTypeEnum; //260507 hbk Phase 19 QUAL-03
            var sourceNames = new System.Collections.Generic.HashSet<string> { //260507 hbk Phase 18 CO-01 패턴 유지
                nameof(AlgorithmTypeList),
                nameof(Circle_EdgeDirectionList), nameof(Circle_EdgePolarityList),
                nameof(Circle_EdgeSelectionList), nameof(Circle_RadialDirectionList),
                nameof(Horizontal_A_EdgeDirectionList), nameof(Horizontal_A_EdgePolarityList),
                nameof(Horizontal_A_EdgeSelectionList),
                nameof(Horizontal_B_EdgeDirectionList), nameof(Horizontal_B_EdgePolarityList),
                nameof(Horizontal_B_EdgeSelectionList),
                nameof(Line1_EdgeDirectionList), nameof(Line1_EdgePolarityList), nameof(Line1_EdgeSelectionList),
                nameof(Line2_EdgeDirectionList), nameof(Line2_EdgePolarityList), nameof(Line2_EdgeSelectionList),
                nameof(Vertical_EdgeDirectionList), nameof(Vertical_EdgePolarityList), nameof(Vertical_EdgeSelectionList),
            };
            return DynamicPropertyHelper.FilterProperties(this, attributes, name => IsHiddenForAlgorithm(name, alg), sourceNames); //260507 hbk Phase 19 QUAL-03
        }
```

**변경하지 않는 부분:**
- line 587-590: `public System.ComponentModel.PropertyDescriptorCollection GetProperties()` 무인자 오버로드
- lines 591-600: 10개 ICustomTypeDescriptor stub 메서드
- lines 602-626: `IsHiddenForAlgorithm` static 메서드 전체
  </action>
  <verify>
    <automated>grep -n "DynamicPropertyHelper.FilterProperties" "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"</automated>
  </verify>
  <acceptance_criteria>
    - grep `DynamicPropertyHelper\.FilterProperties` DatumConfig.cs → 1건 출력
    - grep `IsHiddenForAlgorithm` DatumConfig.cs → 2건 이상 출력 (메서드 정의 + 람다 콜백)
    - GetProperties(Attribute[]) 내부에 `var all = System.ComponentModel.TypeDescriptor` 라인이 없다 (직접 호출 제거됨)
    - GetProperties(Attribute[]) 내부에 `var keep = new List` 라인이 없다 (헬퍼로 이전됨)
    - `//260507 hbk Phase 19 QUAL-03:` 주석이 GetProperties(Attribute[]) 위에 존재한다
    - GetProperties() 무인자 오버로드(TypeDescriptor.GetProperties(this, true) 반환)가 그대로 보존된다
    - IsHiddenForAlgorithm 메서드 정의가 그대로 보존된다
  </acceptance_criteria>
  <done>DatumConfig.GetProperties(Attribute[]) 가 DynamicPropertyHelper.FilterProperties 를 호출하며, 기존 동작(TLI/CTH/VTH 동적 노출 + Circle_RadialDirectionList 소스 강제 포함)이 회귀 없이 유지된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PropertyTools.Wpf → ICustomTypeDescriptor | PropertyGrid 가 GetProperties(Attribute[]) 를 호출 — 반환 컬렉션 내용이 UI 노출 범위를 결정 |
| ParamBase.Save/Load → Reflection | INI 직렬화는 GetType().GetProperties() 사용 — ICustomTypeDescriptor 개입 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | DynamicPropertyHelper.FilterProperties | accept | 정적 유틸리티 — 입력 obj/attrs/hideFunc/sourceNames 는 모두 동일 어셈블리 내부에서 전달. 외부 신뢰 경계 없음 |
| T-19-02 | Denial of Service | hideFunc 람다 | mitigate | hideFunc 이 null 이면 NullReferenceException 발생 → FilterProperties 메서드 진입 전 `if (hideFunc == null) throw new ArgumentNullException("hideFunc");` 추가 |
| T-19-03 | Information Disclosure | sourceNames 화이트리스트 | accept | sourceNames 는 프로퍼티 이름 문자열 집합 — 기밀 데이터 아님. 미등록 항목은 simply 포함되지 않음 |
</threat_model>

<verification>
## Wave 1 완료 검증

```powershell
# 1. 파일 존재 확인
Test-Path "WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs"

# 2. csproj Compile 등록 확인
Select-String -Path "WPF_Example/DatumMeasurement.csproj" -Pattern "DynamicPropertyHelper.cs"

# 3. DatumConfig 헬퍼 호출 교체 확인
Select-String -Path "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs" -Pattern "DynamicPropertyHelper\.FilterProperties"

# 4. DatumConfig IsHiddenForAlgorithm 보존 확인
Select-String -Path "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs" -Pattern "IsHiddenForAlgorithm"

# 5. msbuild (SIMUL_MODE 빌드)
msbuild "WPF_Example/DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal 2>&1 | Select-String -Pattern "error|warning|Build succeeded"
```
</verification>

<success_criteria>
- DynamicPropertyHelper.cs 파일 존재, namespace ReringProject.Sequence, static class DynamicPropertyHelper
- FilterProperties 시그니처: (object, Attribute[], Func&lt;string,bool&gt;, HashSet&lt;string&gt;) → PropertyDescriptorCollection
- DatumConfig.GetProperties(Attribute[]) 내부가 DynamicPropertyHelper.FilterProperties 단일 호출로 교체됨
- DatumConfig.IsHiddenForAlgorithm, GetProperties(), 10개 stub 보존
- csproj Compile 항목 등록됨
- msbuild Debug/x64 PASS, 신규 오류/경고 0
- 모든 수정 라인에 `//260507 hbk Phase 19` 주석 존재
</success_criteria>

<output>
After completion, create `.planning/phases/19-propertygrid-dynamic-exposure/19-01-SUMMARY.md`
</output>
