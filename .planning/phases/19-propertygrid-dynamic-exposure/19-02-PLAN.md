---
phase: 19-propertygrid-dynamic-exposure
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
autonomous: true
requirements:
  - QUAL-03
  - CO-02

must_haves:
  truths:
    - "FAIConfig PropertyGrid 에서 EdgeMeasureType 드롭다운이 노출된다 (6가지 타입 목록)"
    - "CircleDiameter 선택 시 EdgeDirection/EdgePolarity/EdgeSelection/EdgeSampleCount/EdgeTrimCount/Sigma 가 PropertyGrid 에서 숨겨진다"
    - "EdgePairDistance (기본값) 선택 시 위 6개 필드가 모두 노출된다"
    - "INI 직렬화 경로(ParamBase.Save/Load)가 EdgeMeasureType 을 정상적으로 읽고 쓴다"
    - "DatumConfig PropertyGrid 동적 노출이 Phase 17-02 동작 그대로 유지된다 (CO-02 회귀 0)"
    - "msbuild Debug/x64 PASS, 신규 warning 0"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs"
      provides: "FAIConfig ICustomTypeDescriptor 구현 + EdgeMeasureType 필드"
      contains: "ICustomTypeDescriptor"
      exports: ["GetProperties(Attribute[])", "IsHiddenForEdgeMeasureType", "EdgeMeasureType", "EdgeMeasureTypeList"]
  key_links:
    - from: "FAIConfig.GetProperties(Attribute[])"
      to: "DynamicPropertyHelper.FilterProperties"
      via: "정적 메서드 호출"
      pattern: "DynamicPropertyHelper\\.FilterProperties"
    - from: "FAIConfig.EdgeMeasureType"
      to: "FAIConfig.EdgeMeasureTypeList"
      via: "[ItemsSourceProperty(nameof(EdgeMeasureTypeList))]"
      pattern: "ItemsSourceProperty.*EdgeMeasureTypeList"
    - from: "FAIConfig.IsHiddenForEdgeMeasureType"
      to: "DynamicPropertyHelper.FilterProperties hideFunc"
      via: "람다 콜백"
      pattern: "IsHiddenForEdgeMeasureType"
---

<objective>
Phase 19 Wave 2: FAIConfig 에 ICustomTypeDescriptor 구현 + EdgeMeasureType 동적 드롭다운 추가.

DynamicPropertyHelper.FilterProperties(Wave 1)를 이용해 FAIConfig 에 동적 PropertyGrid 필터링을 구현한다.
CircleDiameter 선택 시 에지 파라미터 6개(+각 List 프로퍼티)를 PropertyGrid 에서 숨긴다.
EdgeMeasureType 은 INI 에 저장되고, 미존재 시 "EdgePairDistance" 기본값으로 폴백한다.

Purpose: QUAL-03 완성 + CO-02 (Phase 17 Test 8 DatumConfig 동작 회귀 0 확인) 최종 충족.
Output: FAIConfig.cs (ICustomTypeDescriptor 추가, EdgeMeasureType 필드, IsHiddenForEdgeMeasureType, GetProperties 구현)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-propertygrid-dynamic-exposure/19-01-SUMMARY.md
</context>

<interfaces>
<!-- FAIConfig.cs 현재 상단 (lines 1-10) — 클래스 선언 변경 위치 -->
```csharp
// FAIConfig.cs lines 1-10
using System;
using System.Collections.Generic;
using PropertyTools.DataAnnotations;
using ReringProject.Halcon.Models;
using ReringProject.Utility;
//260423 hbk WR-RT-02 ComboBox 옵션 소스

namespace ReringProject.Sequence {

    public class FAIConfig : ParamBase {
        // → public class FAIConfig : ParamBase, System.ComponentModel.ICustomTypeDescriptor { 로 변경
```

<!-- FAIConfig.cs 현재 Edge|Measurement 블록 (lines 46-62) — EdgeMeasureType 삽입 위치 -->
```csharp
// FAIConfig.cs lines 46-62
        // Edge Measurement //260409 hbk
        [Category("Edge|Measurement")]
        public int EdgeThreshold { get; set; } = 10; //260409 hbk RoiDefinition 호환
        public double Sigma { get; set; } = 1.0;
        [ItemsSourceProperty(nameof(EdgeDirectionList))] //260423 hbk WR-RT-02 ComboBox 처리
        public string EdgeDirection { get; set; } = "LtoR"; //260409 hbk LtoR, RtoL, TtoB, BtoT
        public string EdgeSelection { get; set; } = "First"; //260409 hbk First, Last, Both
        public int EdgeSampleCount { get; set; } = 20; //260409 hbk 샘플 스트립 수
        public int EdgeTrimCount { get; set; } = 10; //260409 hbk 극값 제거 수
        [ItemsSourceProperty(nameof(EdgePolarityList))] //260423 hbk WR-RT-02 ComboBox 처리
        public string EdgePolarity { get; set; } = "DarkToLight"; //260409 hbk DarkToLight, LightToDark
        [PropertyTools.DataAnnotations.Browsable(false)]
        public List<string> EdgeDirectionList { get { return EdgeOptionLists.Directions; } }
        [PropertyTools.DataAnnotations.Browsable(false)]
        public List<string> EdgePolarityList { get { return EdgeOptionLists.FAIPolarities; } }
```

<!-- DynamicPropertyHelper.FilterProperties 시그니처 (Wave 1 생성) -->
```csharp
public static System.ComponentModel.PropertyDescriptorCollection FilterProperties(
    object obj,
    System.Attribute[] attrs,
    System.Func<string, bool> hideFunc,
    System.Collections.Generic.HashSet<string> sourceNames)
```

<!-- MeasurementFactory.GetTypeNames() 반환값 -->
```csharp
// MeasurementFactory.cs GetTypeNames()
return new string[] {
    "EdgePairDistance", "PointToLineDistance", "PointToPointDistance",
    "LineToLineAngle", "CircleDiameter", "LineToLineDistance"
};
```

<!-- DatumConfig ICustomTypeDescriptor stub 10개 패턴 (그대로 복사) -->
```csharp
public System.ComponentModel.AttributeCollection GetAttributes() { return System.ComponentModel.TypeDescriptor.GetAttributes(this, true); }
public string GetClassName() { return System.ComponentModel.TypeDescriptor.GetClassName(this, true); }
public string GetComponentName() { return System.ComponentModel.TypeDescriptor.GetComponentName(this, true); }
public System.ComponentModel.TypeConverter GetConverter() { return System.ComponentModel.TypeDescriptor.GetConverter(this, true); }
public System.ComponentModel.EventDescriptor GetDefaultEvent() { return System.ComponentModel.TypeDescriptor.GetDefaultEvent(this, true); }
public System.ComponentModel.PropertyDescriptor GetDefaultProperty() { return System.ComponentModel.TypeDescriptor.GetDefaultProperty(this, true); }
public object GetEditor(System.Type editorBaseType) { return System.ComponentModel.TypeDescriptor.GetEditor(this, editorBaseType, true); }
public System.ComponentModel.EventDescriptorCollection GetEvents(System.Attribute[] attributes) { return System.ComponentModel.TypeDescriptor.GetEvents(this, attributes, true); }
public System.ComponentModel.EventDescriptorCollection GetEvents() { return System.ComponentModel.TypeDescriptor.GetEvents(this, true); }
public object GetPropertyOwner(System.ComponentModel.PropertyDescriptor pd) { return this; }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: FAIConfig 클래스 선언 변경 + EdgeMeasureType 필드 추가</name>
  <files>
    WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
  </files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs (전체 파일 — 현재 구조 파악, 특히 lines 1-70)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (lines 37-54 AlgorithmType + AlgorithmTypeList 패턴)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs (GetTypeNames 반환값 확인)
  </read_first>
  <action>
**변경 1 — 클래스 선언에 ICustomTypeDescriptor 추가 (line 10):**

```csharp
// 변경 전
    public class FAIConfig : ParamBase {

// 변경 후
    //260507 hbk Phase 19 QUAL-03: ICustomTypeDescriptor 추가 — PropertyGrid 동적 노출용
    public class FAIConfig : ParamBase, System.ComponentModel.ICustomTypeDescriptor {
```

**변경 2 — EdgeMeasureType + EdgeMeasureTypeList 필드 추가:**

삽입 위치: `// Edge Measurement //260409 hbk` 주석 바로 뒤, `[Category("Edge|Measurement")]` 와 `public int EdgeThreshold` 사이.

삽입 내용:
```csharp
        //260507 hbk Phase 19 QUAL-03: EdgeMeasureType — 측정 알고리즘 선택 (INI 직렬화, 미존재 시 기본값 "EdgePairDistance")
        //  저장 타입: string (ParamBase.Save/Load switch가 string 지원 — ParamBase.cs:330-363 확인)
        //  유효값: MeasurementFactory.GetTypeNames() 목록 (6종)
        //  미존재 INI 로드 시: property 기본값 "EdgePairDistance" 가 유지됨 (ParamBase.Load 가 INI 키 미존재 시 기본값 보존)
        [Category("Edge|Measurement")]
        [ItemsSourceProperty(nameof(EdgeMeasureTypeList))] //260507 hbk Phase 19 QUAL-03 — PropertyGrid 드롭다운 목록
        public string EdgeMeasureType { get; set; } = "EdgePairDistance"; //260507 hbk Phase 19 QUAL-03

        //260507 hbk Phase 19 QUAL-03 — EdgeMeasureType 드롭다운 옵션 (MeasurementFactory 단일 소스, 하드코딩 금지)
        [PropertyTools.DataAnnotations.Browsable(false)]
        public List<string> EdgeMeasureTypeList {
            get { return new List<string>(MeasurementFactory.GetTypeNames()); } //260507 hbk Phase 19 QUAL-03
        }
```

주의:
- 기존 `[Category("Edge|Measurement")]` 는 EdgeThreshold 위에 이미 있으므로, 삽입 후 EdgeThreshold 의 Category 어트리뷰트는 제거(EdgeMeasureType 블록의 [Category] 가 그룹을 시작). 단, 기존 `// Edge Measurement //260409 hbk` 주석과 기존 `[Category("Edge|Measurement")]`(EdgeThreshold 위) 순서를 정확히 파악하여 중복 없이 삽입할 것.
- EdgeMeasureType 필드는 EdgeThreshold 보다 앞에 위치해야 한다 (Category 그룹의 첫 번째 표시 항목).
- 기존 EdgeThreshold 위의 `[Category("Edge|Measurement")]` 어트리뷰트는 EdgeMeasureType 으로 옮겨졌으므로 EdgeThreshold 에서 제거한다. (EdgeMeasureType 이 같은 Category 에 있으므로 그룹이 EdgeMeasureType 에서 시작됨)
  </action>
  <verify>
    <automated>grep -n "EdgeMeasureType\|ICustomTypeDescriptor\|EdgeMeasureTypeList" "WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs"</automated>
  </verify>
  <acceptance_criteria>
    - `public class FAIConfig : ParamBase, System.ComponentModel.ICustomTypeDescriptor {` 라인 존재
    - `public string EdgeMeasureType { get; set; } = "EdgePairDistance";` 라인 존재
    - `[ItemsSourceProperty(nameof(EdgeMeasureTypeList))]` 어트리뷰트가 EdgeMeasureType 바로 위에 존재
    - `public List<string> EdgeMeasureTypeList` 프로퍼티 존재
    - EdgeMeasureTypeList getter 내부: `MeasurementFactory.GetTypeNames()` 호출 (하드코딩 금지)
    - `[PropertyTools.DataAnnotations.Browsable(false)]` 가 EdgeMeasureTypeList 위에 존재
    - `//260507 hbk Phase 19 QUAL-03` 주석이 신규 라인들에 존재
  </acceptance_criteria>
  <done>FAIConfig 가 ICustomTypeDescriptor 를 선언하고 EdgeMeasureType 드롭다운 필드를 갖는다.</done>
</task>

<task type="auto">
  <name>Task 2: FAIConfig ICustomTypeDescriptor 메서드 전체 구현 (GetProperties + IsHiddenForEdgeMeasureType + stub 10개)</name>
  <files>
    WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
  </files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs (Task 1 완료 후 현재 상태 — 파일 끝부분 확인)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (lines 551-600 ICustomTypeDescriptor 구현 패턴 전체)
    - WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs (FilterProperties 시그니처)
  </read_first>
  <action>
**FAIConfig.cs 파일 끝 닫는 중괄호 `}` 직전(클래스 내부 마지막)에 아래 블록을 추가한다.**

추가할 내용 전체 (K&R 스타일 — 중괄호 선언 줄 끝):

```csharp
        //260507 hbk Phase 19 QUAL-03 — PropertyGrid 동적 노출 (EdgeMeasureType 별 필터)
        //  PropertyTools.Wpf 가 ICustomTypeDescriptor.GetProperties(Attribute[]) 를 호출 (PropertyGrid 전용).
        //  ParamBase INI 직렬화는 GetType().GetProperties() Reflection 경로 사용 → ICustomTypeDescriptor 영향 0.
        //  안전 장치: GetProperties() 무인자는 base TypeDescriptor 위임 (System.ComponentModel 우회 사용처 보호).
        public System.ComponentModel.PropertyDescriptorCollection GetProperties(System.Attribute[] attributes) { //260507 hbk Phase 19 QUAL-03
            var sourceNames = new System.Collections.Generic.HashSet<string> { //260507 hbk Phase 19 QUAL-03
                nameof(EdgeMeasureTypeList), //260507 hbk Phase 19 QUAL-03
                nameof(EdgeDirectionList), //260507 hbk Phase 18 CO-01 패턴 — EdgeDirectionList 강제 포함
                nameof(EdgePolarityList), //260507 hbk Phase 18 CO-01 패턴 — EdgePolarityList 강제 포함
            };
            return DynamicPropertyHelper.FilterProperties(this, attributes, name => IsHiddenForEdgeMeasureType(name, EdgeMeasureType), sourceNames); //260507 hbk Phase 19 QUAL-03
        }
        public System.ComponentModel.PropertyDescriptorCollection GetProperties() { //260507 hbk Phase 19 QUAL-03
            //260507 hbk Phase 19 QUAL-03 — INI reflection 경로 보호 (System.ComponentModel 사용처가 있을 경우 base 위임)
            return System.ComponentModel.TypeDescriptor.GetProperties(this, true);
        }
        public System.ComponentModel.AttributeCollection GetAttributes() { return System.ComponentModel.TypeDescriptor.GetAttributes(this, true); } //260507 hbk Phase 19 QUAL-03
        public string GetClassName() { return System.ComponentModel.TypeDescriptor.GetClassName(this, true); } //260507 hbk Phase 19 QUAL-03
        public string GetComponentName() { return System.ComponentModel.TypeDescriptor.GetComponentName(this, true); } //260507 hbk Phase 19 QUAL-03
        public System.ComponentModel.TypeConverter GetConverter() { return System.ComponentModel.TypeDescriptor.GetConverter(this, true); } //260507 hbk Phase 19 QUAL-03
        public System.ComponentModel.EventDescriptor GetDefaultEvent() { return System.ComponentModel.TypeDescriptor.GetDefaultEvent(this, true); } //260507 hbk Phase 19 QUAL-03
        public System.ComponentModel.PropertyDescriptor GetDefaultProperty() { return System.ComponentModel.TypeDescriptor.GetDefaultProperty(this, true); } //260507 hbk Phase 19 QUAL-03
        public object GetEditor(System.Type editorBaseType) { return System.ComponentModel.TypeDescriptor.GetEditor(this, editorBaseType, true); } //260507 hbk Phase 19 QUAL-03
        public System.ComponentModel.EventDescriptorCollection GetEvents(System.Attribute[] attributes) { return System.ComponentModel.TypeDescriptor.GetEvents(this, attributes, true); } //260507 hbk Phase 19 QUAL-03
        public System.ComponentModel.EventDescriptorCollection GetEvents() { return System.ComponentModel.TypeDescriptor.GetEvents(this, true); } //260507 hbk Phase 19 QUAL-03
        public object GetPropertyOwner(System.ComponentModel.PropertyDescriptor pd) { return this; } //260507 hbk Phase 19 QUAL-03

        //260507 hbk Phase 19 QUAL-03 — EdgeMeasureType 별 숨김 규칙:
        //   CircleDiameter: EdgeDirection/EdgePolarity/EdgeSelection/EdgeSampleCount/EdgeTrimCount/Sigma + 각 List 숨김
        //   그 외 모든 타입: 숨김 없음 (EdgePairDistance, PointToLineDistance 등)
        private static bool IsHiddenForEdgeMeasureType(string name, string edgeMeasureType) { //260507 hbk Phase 19 QUAL-03
            if (edgeMeasureType == "CircleDiameter") {
                if (name == "EdgeDirection"  || name == "EdgeDirectionList")  return true; //260507 hbk Phase 19 QUAL-03
                if (name == "EdgePolarity"   || name == "EdgePolarityList")   return true; //260507 hbk Phase 19 QUAL-03
                if (name == "EdgeSelection")                                   return true; //260507 hbk Phase 19 QUAL-03
                if (name == "EdgeSampleCount")                                 return true; //260507 hbk Phase 19 QUAL-03
                if (name == "EdgeTrimCount")                                   return true; //260507 hbk Phase 19 QUAL-03
                if (name == "Sigma")                                           return true; //260507 hbk Phase 19 QUAL-03
            }
            return false;
        }
```

이 블록을 `public void ClearResult()` 메서드 이후, 파일 끝 `}` (클래스 닫는 중괄호) 직전에 삽입한다.
파일의 마지막 두 줄은 `    }` (클래스) + `}` (네임스페이스) 이어야 한다.
  </action>
  <verify>
    <automated>grep -n "GetProperties\|IsHiddenForEdgeMeasureType\|GetAttributes\|GetPropertyOwner" "WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs"</automated>
  </verify>
  <acceptance_criteria>
    - `GetProperties(System.Attribute[] attributes)` 메서드 존재 (1건)
    - `GetProperties()` 무인자 오버로드 존재 (1건)
    - `IsHiddenForEdgeMeasureType` private static 메서드 존재 (1건)
    - `GetPropertyOwner` stub 메서드 존재 (ICustomTypeDescriptor 완전 구현 마커)
    - `GetAttributes`, `GetClassName`, `GetComponentName`, `GetConverter`, `GetDefaultEvent`, `GetDefaultProperty`, `GetEditor`, `GetEvents` (2개 오버로드) 총 9개 stub 존재
    - IsHiddenForEdgeMeasureType 내부: `"CircleDiameter"` 분기 + `"EdgeDirection"` / `"EdgeDirectionList"` / `"EdgePolarity"` / `"EdgePolarityList"` / `"EdgeSelection"` / `"EdgeSampleCount"` / `"EdgeTrimCount"` / `"Sigma"` 8개 hide 조건
    - sourceNames HashSet 내부: `nameof(EdgeMeasureTypeList)`, `nameof(EdgeDirectionList)`, `nameof(EdgePolarityList)` 3개
    - `DynamicPropertyHelper.FilterProperties(this, attributes, name => IsHiddenForEdgeMeasureType(name, EdgeMeasureType), sourceNames)` 호출 라인 존재
    - msbuild Debug/x64: `Build succeeded` 출력, `error` 0건
  </acceptance_criteria>
  <done>FAIConfig 가 ICustomTypeDescriptor 를 완전히 구현하여, CircleDiameter 선택 시 에지 파라미터 6종이 PropertyGrid 에서 숨겨지고, 다른 타입에서는 모두 노출된다. INI 직렬화는 영향받지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PropertyTools.Wpf → FAIConfig.GetProperties(Attribute[]) | PropertyGrid 가 EdgeMeasureType 값에 따라 동적으로 다른 필드 집합을 표시 |
| ParamBase.Save/Load → FAIConfig Reflection | INI 직렬화는 GetType().GetProperties() 사용 — ICustomTypeDescriptor 개입 없음 (EdgeMeasureType 정상 저장) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Information Disclosure | FAIConfig.IsHiddenForEdgeMeasureType | accept | 숨김 로직은 UI 표시 제어만 수행 — 숨겨진 필드도 INI 에 저장되고 로드됨. 데이터 소실 없음 |
| T-19-05 | Tampering | EdgeMeasureType INI 미존재 | mitigate | property getter 기본값 `"EdgePairDistance"` + ParamBase.Load 키 미존재 시 기본값 보존 패턴으로 처리 — 별도 방어 코드 불필요 |
| T-19-06 | Denial of Service | EdgeMeasureTypeList 새 인스턴스 생성 | accept | PropertyGrid 열람 시마다 new List<string> 생성 — DatumConfig.AlgorithmTypeList 동일 패턴, 성능 영향 무시 가능 (6 items) |
</threat_model>

<verification>
## Wave 2 완료 검증

```powershell
# 1. FAIConfig ICustomTypeDescriptor 선언 확인
Select-String -Path "WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs" -Pattern "ICustomTypeDescriptor"

# 2. EdgeMeasureType 필드 + ItemsSourceProperty 확인
Select-String -Path "WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs" -Pattern "EdgeMeasureType"

# 3. IsHiddenForEdgeMeasureType 확인
Select-String -Path "WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs" -Pattern "IsHiddenForEdgeMeasureType"

# 4. DynamicPropertyHelper 호출 확인
Select-String -Path "WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs" -Pattern "DynamicPropertyHelper\.FilterProperties"

# 5. DatumConfig 회귀 없음 확인 (CO-02)
Select-String -Path "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs" -Pattern "DynamicPropertyHelper\.FilterProperties"
Select-String -Path "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs" -Pattern "IsHiddenForAlgorithm"

# 6. msbuild
msbuild "WPF_Example/DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal 2>&1 | Select-String -Pattern "error|warning|Build succeeded"
```

## CO-02 Phase 17 Test 8 재검증 체크리스트 (UAT 용)

DatumConfig 동적 노출 회귀 0 확인 (Phase 17-02 동작 그대로):
- TLI: PropertyGrid 에서 Line1_*/Line2_* 노출, Circle_*/Vertical_*/Horizontal_A_*/Horizontal_B_* 숨김
- CTH: Circle_* (RadialDirection 포함) + Horizontal_A_*/B_* 노출, Circle_EdgeDirection 숨김, Line1_*/Line2_*/Vertical_* 숨김
- VTH: Vertical_* + Horizontal_A_*/B_* 노출, Line1_*/Line2_*/Circle_* 숨김
- Circle_RadialDirection 드롭다운: Inward/Outward 두 값만 표시 (CO-01 회귀 없음)
</verification>

<success_criteria>
- FAIConfig 에 `System.ComponentModel.ICustomTypeDescriptor` 인터페이스 선언 추가됨
- EdgeMeasureType: string 필드, 기본값 "EdgePairDistance", [Category("Edge|Measurement")], [ItemsSourceProperty(nameof(EdgeMeasureTypeList))]
- EdgeMeasureTypeList: [Browsable(false)], MeasurementFactory.GetTypeNames() 호출 (하드코딩 금지)
- IsHiddenForEdgeMeasureType: CircleDiameter 분기에서 6개 필드+2개 List 프로퍼티 hide
- GetProperties(Attribute[]): DynamicPropertyHelper.FilterProperties 위임, sourceNames 3개(EdgeMeasureTypeList/EdgeDirectionList/EdgePolarityList)
- GetProperties() 무인자: TypeDescriptor.GetProperties(this, true) 위임
- ICustomTypeDescriptor stub 10개 모두 구현
- msbuild Debug/x64 PASS, 신규 warning 0
- 모든 수정 라인에 `//260507 hbk Phase 19` 주석 존재
- DatumConfig 동작 회귀 0 (CO-02)
</success_criteria>

<output>
After completion, create `.planning/phases/19-propertygrid-dynamic-exposure/19-02-SUMMARY.md`
</output>
