---
phase: 20
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
autonomous: true
requirements:
  - QUAL-02
  - QUAL-04
must_haves:
  truths:
    - "DatumFindingService.cs 에서 `?:` (~21) / `??` (~10) / `?.` 연산자가 명시적 if/else 로 교체되었다 (D-04 LINQ tail, D-05 expression-bodied 예외 적용)"
    - "'what' 주석 제거, 알고리즘 의도/HALCON 호출 의도 / Datum CTH polarity / 수치 근거 주석은 보존되었다 (D-08, D-09)"
    - "Datum 티칭/검출 (TLI/CTH/VTH 3 알고리즘 + Phase 28 helper 호출) 결과가 변환 전과 byte-identical 동등 (Wave 3 회귀에서 검증)"
  artifacts:
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "Datum 티칭 + 검출 (TwoLineIntersect / CircleTwoHorizontal / VerticalTwoHorizontal) + Phase 28 polarity helper 호출"
      forbids_pattern: "\\?\\?"
      forbids_pattern_2: "\\?[^?:.A-Za-z]"
  key_links:
    - from: "DatumFindingService.cs"
      to: "EdgeOptionLists.MapRadialDirectionToHalconPolarity"
      via: "Phase 28 helper (line 200, 730 hotspot — 28-03 이미 helper 호출로 교체)"
      pattern: "MapRadialDirectionToHalconPolarity"
    - from: "DatumFindingService.cs"
      to: "VisionAlgorithmService.TryFindCircle"
      via: "CircleTwoHorizontal + 28-02 polar dispatch"
      pattern: "TryFindCircle\\("
---

<objective>
DatumFindingService.cs (Halcon HTuple null 가드 다수 + Phase 28 polarity helper 통합 후 잔여 `?:` 21곳 / `??` 10곳) 을 명시적 if/else 와 임시변수 + null 체크로 변환한다 (D-01).

Purpose: QUAL-02/QUAL-04 — Phase 20 핵심 부담 5 파일 중 가장 알고리즘 위험도 높은 파일. Datum 티칭 (TLI/CTH/VTH 3 알고리즘) + Phase 28 helper 통합 경로의 회귀 0 보장이 결정적.
Output: 변환된 DatumFindingService.cs + Wave 3 회귀 검증으로 Datum CTH/VTH/TLI byte-identical (1e-9 mm) 보존.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-code-style-cleanup/20-CONTEXT.md
@.planning/phases/20-code-style-cleanup/20-01-PLAN.md
@.planning/phases/28-fai-circlediameter-datum-circle/28-CONTEXT.md
@CLAUDE.md
@WPF_Example/Halcon/Algorithms/DatumFindingService.cs
</context>

<conversion_patterns>
<!-- 20-01-PLAN.md §conversion_patterns 와 동일 (P-1 ~ P-8). 핵심 추가: -->

**P-9 (Halcon HTuple null 가드 — DatumFindingService.cs 특이 패턴):**
```csharp
// Before:
HTuple result = htuple?.TupleLength() ?? 0;

// After:
HTuple result = 0;
if (htuple != null) result = htuple.TupleLength();
```
HTuple 은 Halcon SDK 객체 — null 체크 후 메서드 호출은 의미적으로 동등 (Halcon 자체가 동일한 null 검사 수행).

**P-10 (try/catch 보존):** CONVENTIONS.md §Error Handling — `HOperatorSet.*` 호출은 `try { } catch { return false; }` 패턴 필수. 변환이 try/catch 구조를 깨트리면 안 됨.
</conversion_patterns>

<read_first>
1. `WPF_Example/Halcon/Algorithms/DatumFindingService.cs` — 전체 (~900 줄, 가장 큰 파일 중 하나 — 청크 단위 읽기 가능)
2. `.planning/phases/20-code-style-cleanup/20-CONTEXT.md` — D-01 ~ D-17
3. `.planning/phases/28-fai-circlediameter-datum-circle/28-CONTEXT.md` — 28-03 helper 통합 흔적 (line 200, 730 hotspot 이미 helper call 로 교체됨)
</read_first>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DatumFindingService.cs 연산자 변환 (전반부 — TLI 경로)</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <behavior>
    - TryTeachDatum(TLI) 의 검출 결과 byte-identical (Phase 4/12-02 검증된 경로 회귀 0)
    - TryFindDatum (runtime) 동작 보존 (Phase 12-02 SPEC out-of-scope 였던 부분 — 이번에도 시그니처/동작 둘 다 무변경)
    - HOperatorSet.* try/catch wrapping 보존 (CONVENTIONS.md §Error Handling)
  </behavior>
  <action>
1. DatumFindingService.cs 읽기 (~900 줄). Grep 으로 `??` 와 `?:` 위치 확인.
2. 전반부 (TLI 경로 — 대략 line 1 ~ 500) 의 `??` / `?:` 변환.
3. 변환 패턴:
   - `??` (10 곳) → P-1, P-2, P-9 (HTuple 가드).
   - `?:` (21 곳 중 전반부 ~10) → P-4 (early return 우선).
   - `?.` 검출되면 P-2 또는 P-9 패턴, LINQ tail 인 경우 D-04 유지.
4. try/catch 블록 안의 변환 — try { } catch { return false; } 구조 절대 깨지 말 것 (P-10).
5. Halcon HOperatorSet 호출 시퀀스의 인자 순서 / 의미는 절대 변경 X — Phase 12/14/15/28 의 측정 결과 회귀 0 직결.
6. 'what' 주석 제거 (P-7) — 단, "trimCount=2 이유", "polarity 부호 결함 fix (Phase 15-02)", "HALCON #1405 IntersectionLl 버그 회피" 같은 알고리즘 의도 주석은 모두 보존.
7. 변환 라인에 `//260508 hbk Phase 20` 마커, 변환 안 한 라인의 기존 hbk 마커 (`//260415 hbk`, `//260429 hbk Phase 28-03` 등) 는 그대로.
  </action>
  <verify>
    <automated>
      cd C:/Info/Project/DataMeasurement &amp;&amp; powershell -Command "$f='WPF_Example/Halcon/Algorithms/DatumFindingService.cs'; $c=Get-Content $f -Raw; $qq=([regex]::Matches($c,'\?\?(?!=)')).Count; $qd=([regex]::Matches($c,'\?\.')).Count; Write-Host \"qq=$qq qd=$qd\""
    </automated>
    <build>
      cd C:/Info/Project/DataMeasurement &amp;&amp; "C:/Program Files (x86)/Microsoft Visual Studio/2019/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:m
    </build>
    <expected>Task 1 종료 시 qq 잔여는 후반부 만큼 (Task 2 에서 0 화). qd 는 LINQ tail 제외 0. msbuild PASS.</expected>
  </verify>
  <done>
    - 전반부 (TLI 경로) `??` 10곳 + `?:` ~10곳 변환 완료
    - HOperatorSet try/catch 구조 보존
    - msbuild Debug/x64 PASS (Task 1 단독에서 빌드 가능 상태)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DatumFindingService.cs 연산자 변환 (후반부 — CTH/VTH 경로) + 주석 정리</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <behavior>
    - TryTeachDatum(CTH) / TryTeachDatum(VTH) byte-identical (Phase 12-02/15-02/16/28 검증 경로)
    - line ~200 / ~730 의 Phase 28 helper 호출 부 (`MapRadialDirectionToHalconPolarity`) 시그니처 무변경
    - 'what' 주석 정리 후 알고리즘 의도/HALCON #1405 fix/Phase 28 polarity 의도 주석 보존
  </behavior>
  <action>
1. DatumFindingService.cs 후반부 (line ~500 ~ 끝) 의 잔여 `?:` ~11곳 변환 (P-4 + early return).
2. CTH/VTH 경로의 polarity 분기에서 `?:` 가 polarity → HOperatorSet 인자 매핑하는 케이스 — 분해 시 분기 라벨 명확히 (예: `string halconPolarity = "negative"; if (...) halconPolarity = "positive";`).
3. Phase 28 helper 호출 라인 (`MapRadialDirectionToHalconPolarity(...)`) 은 절대 분해 X — 단일 호출이므로 D-04 정신과 동일.
4. line 200, 730 의 helper 호출 부근의 기존 `//260508 hbk Phase 28` 마커는 보존 (D-13 — 이 라인은 Phase 20 변환 대상 아님).
5. 후반부 'what' 주석 (P-7) 정리 — 알고리즘 의도 (예: "MIN_HORIZONTAL_EDGES=10: 10개 미만이면 fit 신뢰도 부족"), HALCON SDK quirk 주석 (예: "#1405 IntersectionLl bug — TupleConcat 단일 GenContourPolygonXld 패턴") 보존.
6. 변환 라인에 `//260508 hbk Phase 20` 마커.
7. 최종 grep 으로 `??` (`??=` 제외) = 0, `?.` = 0 (LINQ tail 제외) 확인.
  </action>
  <verify>
    <automated>
      cd C:/Info/Project/DataMeasurement &amp;&amp; powershell -Command "$f='WPF_Example/Halcon/Algorithms/DatumFindingService.cs'; $c=Get-Content $f -Raw; $qq=([regex]::Matches($c,'\?\?(?!=)')).Count; $qd=([regex]::Matches($c,'\?\.')).Count; $hbk20=([regex]::Matches($c,'260508 hbk Phase 20')).Count; $hbk28=([regex]::Matches($c,'260508 hbk Phase 28')).Count; Write-Host \"qq=$qq qd=$qd hbk20=$hbk20 hbk28=$hbk28\""
    </automated>
    <build>
      cd C:/Info/Project/DataMeasurement &amp;&amp; "C:/Program Files (x86)/Microsoft Visual Studio/2019/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:m
    </build>
    <expected>qq=0, qd=0 (LINQ tail 제외 — 사람 검토 시 false positive 가능), hbk20 ≥ 25 (10 ?? + ~21 ?: 변환), hbk28 보존 (D-13 — Phase 28 마커 라인 무변경).</expected>
  </verify>
  <done>
    - 후반부 (CTH/VTH) `?:` 변환 완료
    - 'what' 주석 제거, 알고리즘 의도 주석 보존
    - 최종 grep: 파일 전체에서 `??` = 0, `?.` = 0 (LINQ tail/string literal 제외)
    - Phase 28 helper 호출 부 라인 무변경 (line 200, 730)
    - msbuild Debug/x64 PASS, 신규 warning 0
  </done>
</task>

</tasks>

<acceptance_criteria>
1. powershell grep `??` (`??=` 제외) = 0
2. powershell grep `?.` (LINQ tail 제외) = 0
3. msbuild Debug/x64 exit 0, 신규 warning = 0
4. `260508 hbk Phase 28` 마커 카운트 변환 전·후 동일 (D-13 보존 검증)
5. line 200 + line 730 의 `MapRadialDirectionToHalconPolarity(...)` 호출 시그니처 무변경 (Phase 28 회귀 0)
6. HOperatorSet try/catch wrap 패턴 손상 0 — `try.*catch` 블록 카운트 변환 전·후 동일
</acceptance_criteria>

<verification>
사람 검토 포인트:
- HALCON HOperatorSet 시퀀스의 인자 순서/의미 보존
- HTuple null 가드 분해 (P-9) 가 Halcon SDK 의 lazy 평가와 호환
- Phase 12-02 / 15-02 / 28-03 의 hbk 마커 라인 손상 0

자동화:
- msbuild PASS + 신규 warning 0
- Wave 3 plan 에서 SIMUL_MODE 1회 실행 후 Datum 검출 결과 byte-identical (1e-9 mm)
</verification>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 변환 전후 (HALCON algorithm semantics) | `?:`/`??` 분해가 알고리즘 분기 잘못 만들면 Datum CTH/VTH/TLI 검출 결과 변경 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02-01 | Tampering | DatumFindingService.cs CTH polarity 분기 분해 | mitigate | P-4 패턴 강제 + Wave 3 byte-identical 회귀 (Phase 28 SIMUL UAT 레시피, 1e-9 mm 마진). 사람 검토 — polarity 분기 라벨 명확. |
| T-20-02-02 | Tampering | HOperatorSet try/catch 구조 손상 | mitigate | P-10 강제 + acceptance criteria #6 (try/catch 카운트 동일). |
| T-20-02-03 | Tampering | hbk 마커 (D-12) 잘못된 교체 — Phase 28 line 200/730 손상 | mitigate | D-13 + acceptance criteria #4 (Phase 28 마커 카운트 보존). |

Severity: MEDIUM — Datum 알고리즘 회귀는 시스템 전체 측정 정확도 영향. Wave 3 회귀로 검출 가능하지만, 본 plan 단계에서 의미론적 동등 변환 강제 필요.
</threat_model>

<success_criteria>
- DatumFindingService.cs 의 `?:`/`??`/`?.` (예외 제외) 변환 완료
- 'what' 주석 제거 / 'why' 보존
- msbuild PASS, 신규 warning 0
- Wave 3 plan 에서 byte-identical (1e-9 mm) 검증 후 AC #3 충족
</success_criteria>

<output>
After completion, create `.planning/phases/20-code-style-cleanup/20-02-SUMMARY.md`:
- 변환 라인 수 (?? / ?: 별)
- HOperatorSet try/catch 블록 카운트 (전후 동일 검증)
- Phase 28 helper 호출 부 손상 0 명시
- 보존된 알고리즘 의도 주석 목록 (요약)
</output>
