---
phase: 20
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: true
requirements:
  - QUAL-02
  - QUAL-04
must_haves:
  truths:
    - "InspectionListView.xaml.cs 의 `?.` (~9 — 모두 ViewModel/InspectionVM null safety) / `??` (~1) / `?:` (~11) 가 명시적 if/else 로 교체되었다 (D-04 LINQ tail / D-05 expression-bodied 예외)"
    - "TreeView selection / FAI CRUD / Datum 노드 선택 → btn_teachDatum 활성화 의도 주석 보존"
    - "ViewModel.RootModel?.ExpandAll() / _inspectionVm?.ClearResults() 등 9곳의 null safety 가 의미론적으로 동등 변환"
  artifacts:
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "Inspection TreeView CRUD UI + Datum 노드 선택 → MainView 라우팅"
      forbids_pattern: "\\?\\?"
      forbids_pattern_2: "_inspectionVm\\?\\."
  key_links:
    - from: "InspectionListView.xaml.cs"
      to: "InspectionViewModel (ClearResults / RootModel.ExpandAll)"
      via: "ViewModel null safety + selection event"
      pattern: "_inspectionVm|ViewModel"
---

<objective>
InspectionListView.xaml.cs (TreeView selection null 가드 + InspectionViewModel.ClearResults 호출 9곳 + `?:` 11 + `??` 1) 의 모든 연산자를 명시적 if/else 로 변환한다.

Purpose: QUAL-02/QUAL-04 — Phase 1 (UI 재설계) + Phase 17-02 (DatumConfig PropertyGrid) + Phase 18 (CO-04 ROI redraw) 의 InspectionListView 통합 잔여. ViewModel null safety 패턴이 9곳 동일 — 일괄 변환 가능.
Output: 변환된 InspectionListView.xaml.cs + Wave 3 SIMUL UAT 에서 TreeView CRUD / Datum 노드 선택 / FAI 측정 결과 표시 byte-identical 보존.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-code-style-cleanup/20-CONTEXT.md
@.planning/phases/20-code-style-cleanup/20-01-PLAN.md
@CLAUDE.md
@WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
</context>

<conversion_patterns>
<!-- 20-01-PLAN.md §conversion_patterns (P-1 ~ P-8) 와 동일. -->
<!-- 본 파일 핵심 반복 패턴: -->

**C# 7.2 한정 (W1 — .NET Framework 4.8):** `??=` (null-coalescing assignment), `is { }` property pattern, switch expression (`x switch { ... }`), tuple deconstruction with discards 사용 금지. 명시적 if/else / 임시변수 + null 체크 패턴만 허용 (D-01 강제).

**P-13 (ViewModel null safety — 9 위치 동일):**
```csharp
// Before:
_inspectionVm?.ClearResults();

// After:
if (_inspectionVm != null) _inspectionVm.ClearResults();

// 동일 라인이 여러 번 반복되므로, 패턴 일괄 적용.
```

**P-14 (RootModel chain):**
```csharp
// Before (line 234):
ViewModel.RootModel?.ExpandAll();

// After:
var rootModel = ViewModel.RootModel;
if (rootModel != null) rootModel.ExpandAll();

// 또는 single-line if:
if (ViewModel.RootModel != null) ViewModel.RootModel.ExpandAll();
```
실행자 결정: single-line if 가 충분히 명확하면 임시변수 생략 가능 (D-01 의 명시성 충족).
</conversion_patterns>

<read_first>
1. `WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` — 전체
2. `.planning/phases/20-code-style-cleanup/20-CONTEXT.md` — D-01 ~ D-17
3. `.planning/STATE.md` Decisions §Phase 1 (FAI CRUD wired in InspectionListView), §Phase 18 (CO-04)

**파일 인코딩 보존 (W3 — D-09 한국어 nuance 주석 mojibake 방지):**
- 변환 전 BOM 헤더 확인 (UTF-8 BOM 권장 — VS2019 기본).
- powershell `Get-Content -Encoding UTF8` / `Set-Content -Encoding UTF8` 사용. 메모장 또는 다른 에디터의 자동 인코딩 변환(CP949 등) 금지.
- TreeView selection / FAI CRUD 의 한국어 도메인 주석 (D-09) mojibake 방지 + Wave 2 (20-08) 종합 grep 으로 재검증.
</read_first>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: InspectionListView.xaml.cs `?.` (9곳) + `??` (1곳) 변환</name>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <behavior>
    - 9개 `_inspectionVm?.ClearResults()` (line 401, 448, 482, 488, 773, 794, 812, 833) 모두 P-13 패턴
    - line 234 `ViewModel.RootModel?.ExpandAll()` P-14 패턴
    - `??` (1곳) P-1 또는 P-2
    - 변환 후 동일 라인이 9번 반복되더라도 일관된 패턴 (P-13)
  </behavior>
  <action>
0. **인코딩 baseline (W3):** `Get-Content WPF_Example/UI/ControlItem/InspectionListView.xaml.cs -Encoding Byte -TotalCount 3` → BOM `EF BB BF` 여부 기록. 이후 모든 read/write 는 UTF-8 (BOM 보존) 으로 통일.
1. 파일 읽기 (전체).
2. `?.` 9 곳 (`_inspectionVm?.ClearResults()`) → P-13:
   ```csharp
   // Before:
   _inspectionVm?.ClearResults();
   // After:
   if (_inspectionVm != null) _inspectionVm.ClearResults();
   ```
3. line 234 `ViewModel.RootModel?.ExpandAll()` → P-14:
   ```csharp
   // After:
   if (ViewModel.RootModel != null) ViewModel.RootModel.ExpandAll();
   ```
4. `??` (1곳) — P-1 또는 P-2 패턴.
5. 변환 라인에 `//260508 hbk Phase 20` 마커 (D-12). 단, 9개 ClearResults 라인이 짧기 때문에 인라인 마커 (`if (_inspectionVm != null) _inspectionVm.ClearResults(); //260508 hbk Phase 20`) 가 적절.
  </action>
  <verify>
    <automated>
      cd C:/Info/Project/DataMeasurement &amp;&amp; powershell -Command "$f='WPF_Example/UI/ControlItem/InspectionListView.xaml.cs'; $c=Get-Content $f -Raw -Encoding UTF8; $vm=([regex]::Matches($c,'_inspectionVm\?\.')).Count; $qq=([regex]::Matches($c,'\?\?(?!=)')).Count; $rm=([regex]::Matches($c,'RootModel\?\.')).Count; Write-Host \"vm=$vm qq=$qq rm=$rm\""
    </automated>
    <expected>vm=0, qq=0, rm=0</expected>
  </verify>
  <done>
    - 9곳 `_inspectionVm?.ClearResults()` 모두 P-13
    - line 234 `RootModel?.ExpandAll()` P-14
    - `??` 1곳 변환
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: InspectionListView.xaml.cs `?:` (11곳) 변환 + 'what' 주석 정리</name>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <behavior>
    - `?:` 11곳 모두 P-4 (early return 우선 가능 시 우선)
    - TreeView selection 분기 / FAI CRUD 분기 / Datum 노드 vs FAI 노드 분기 의도 주석 보존
    - 한국어 nuance 주석 보존 (D-09)
  </behavior>
  <action>
1. `?:` 11곳 변환 — P-4. TreeView selection 분기 (예: `var label = isDatum ? "Datum" : "FAI";`) 는 분해.
2. 'what' 주석 정리:
   - 보존: TreeView selection → btn_teachDatum 활성화 의도 (Phase 12-03), FAI CRUD 컨텍스트 메뉴 의도 (Phase 1), 한국어 도메인 nuance.
   - 제거: 변수명/메서드명 자명한 한 줄 주석.
3. 한국어 nuance 주석 (D-09) — read_first §인코딩 규칙 준수, mojibake 방지.
4. 변환 라인 `//260508 hbk Phase 20` 마커. 비변환 라인의 기존 hbk 마커 (`//260415 hbk Phase 12-03`, `//260505 hbk Phase 18` 등) 보존.
5. 최종 grep — `?:` 단순 패턴 ≈ 0 (string literal / generic constraint false positive 사람 검토).
  </action>
  <verify>
    <automated>
      cd C:/Info/Project/DataMeasurement &amp;&amp; powershell -Command "$f='WPF_Example/UI/ControlItem/InspectionListView.xaml.cs'; $c=Get-Content $f -Raw -Encoding UTF8; $qq=([regex]::Matches($c,'\?\?(?!=)')).Count; $qd=([regex]::Matches($c,'\?\.')).Count; $hbk20=([regex]::Matches($c,'260508 hbk Phase 20')).Count; Write-Host \"qq=$qq qd=$qd hbk20=$hbk20\""
    </automated>
    <build>
      cd C:/Info/Project/DataMeasurement &amp;&amp; "C:/Program Files (x86)/Microsoft Visual Studio/2019/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:m
    </build>
    <expected>qq=0, qd=0 (LINQ tail 제외), hbk20 ≥ 21 (10 ?. + 11 ?: + 1 ??), msbuild PASS, 신규 warning 0.</expected>
  </verify>
  <done>
    - `?:` 11곳 변환
    - 'what' 주석 제거, 'why' (TreeView selection / FAI CRUD / Datum 노드 분기 의도) 보존
    - msbuild Debug/x64 PASS, 신규 warning 0
  </done>
</task>

</tasks>

<acceptance_criteria>
1. powershell grep `??` (`??=` 제외) = 0
2. powershell grep `?.` (LINQ tail 제외) = 0
3. msbuild Debug/x64 exit 0, 신규 warning 0
4. 한국어 nuance 주석 보존 + mojibake 없음 (W3 — D-09 사람 검토)
5. Phase 1 / Phase 12-03 / Phase 18 의도 주석 보존
6. C# 7.2 호환 (W1 — `??=` / switch expression / `is { }` 도입 0)
</acceptance_criteria>

<verification>
사람 검토 포인트:
- 9개 ClearResults 호출이 모두 동일 시점 (TreeView selection 변경 / FAI CRUD 액션) 에 의미적으로 동등 동작
- Datum 노드 선택 시 btn_teachDatum 활성화 분기 (Phase 12-03 / 17-02) 보존
- FAI Add/Remove 컨텍스트 메뉴 (Phase 1 D-XX) 보존
- 한국어 주석 mojibake 없음 (W3)

자동화:
- msbuild PASS + 신규 warning 0
- Wave 3 plan 에서 SIMUL_MODE 에서 TreeView 클릭 → ClearResults 호출 → FAI 측정 결과 cleared 확인
</verification>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 변환 전후 (UI null safety) | _inspectionVm null 체크 분해 잘못 시 NRE |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05-01 | Tampering | _inspectionVm 9곳 P-13 분해 | mitigate | 패턴 일관성 (9곳 동일 패턴) + msbuild PASS + Wave 3 SIMUL UAT. |
| T-20-05-02 | Tampering | hbk 마커 (D-12) Phase 1/12-03/18 의도 손상 | mitigate | D-13 + acceptance criteria #5. |

Severity: LOW — UI null safety 분해는 의미론적 동등 변환. 9곳 동일 패턴이라 일관성 검증 쉬움.
</threat_model>

<success_criteria>
- InspectionListView.xaml.cs `?:`/`??`/`?.` (예외 제외) 변환 완료
- 'what' 주석 제거 / 'why' (selection / CRUD 의도) 보존
- msbuild PASS, 신규 warning 0
</success_criteria>

<output>
After completion, create `.planning/phases/20-code-style-cleanup/20-05-SUMMARY.md`:
- 변환 라인 수 (?? / ?: / ?. 별)
- 9곳 ClearResults P-13 패턴 적용 확인
- 보존된 'why' 주석 카테고리
- 한국어 주석 mojibake 없음 확인 (W3)
</output>
