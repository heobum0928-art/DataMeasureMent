---
phase: 20
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs
  - WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs
autonomous: true
requirements:
  - QUAL-02
  - QUAL-04
must_haves:
  truths:
    - "DatumConfig / DynamicPropertyHelper / EdgeOptionLists / CircleDiameterMeasurement (4 light Custom/Sequence 파일) 의 `?:` / `??` / `?.` 가 명시적 if/else 로 교체되었다 (D-04/D-05 예외 적용)"
    - "Phase 17-02 DatumConfig ICustomTypeDescriptor 동작, Phase 19-01 DynamicPropertyHelper.FilterProperties 동작, Phase 28-01 EdgeOptionLists 헬퍼/defaults 동작, Phase 28-02 CircleDiameterMeasurement TryExecute 분기 동작 보존"
    - "'what' 주석 제거, ICustomTypeDescriptor 구현 의도 / Phase 28 polarity helper 의도 / FAI polar default 사유 주석은 보존"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "Datum 모델 + ICustomTypeDescriptor + per-ROI 파라미터"
    - path: "WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs"
      provides: "FilterProperties 정적 헬퍼 (Phase 19-01)"
    - path: "WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs"
      provides: "MapRadialDirectionToHalconPolarity + 4 FAI polar defaults (Phase 28-01)"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs"
      provides: "FAI CircleDiameter measurement + Circle_RadialDirection 분기 (Phase 28-02)"
  key_links:
    - from: "DatumConfig.cs"
      to: "DynamicPropertyHelper.FilterProperties"
      via: "ICustomTypeDescriptor.GetProperties 위임 (Phase 19-01)"
      pattern: "FilterProperties"
    - from: "CircleDiameterMeasurement.cs"
      to: "EdgeOptionLists.MapRadialDirectionToHalconPolarity + FaiCircle* defaults"
      via: "Circle_RadialDirection 폴라 분기 (Phase 28-02)"
      pattern: "MapRadialDirectionToHalconPolarity|FaiCircle"
---

<objective>
4 개의 경량 Custom/Sequence 파일 (DatumConfig + DynamicPropertyHelper + EdgeOptionLists + CircleDiameterMeasurement) 의 잔여 `?:` / `??` / `?.` 연산자를 명시적 if/else 로 변환한다 (D-01).

Purpose: QUAL-02/QUAL-04 — 경량 파일 묶음. 변환 포인트 ~5 (rough). Phase 17-02 / 19-01 / 28-01 / 28-02 의 결정 (ICustomTypeDescriptor + helper + defaults) 동작 보존이 결정적.
Output: 4 파일 변환 완료 + Wave 2 회귀 검증 (20-08) 으로 byte-identical 보존.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-code-style-cleanup/20-CONTEXT.md
@.planning/phases/20-code-style-cleanup/20-01-PLAN.md
@CLAUDE.md
@WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
@WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs
@WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs
@WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs
</context>

<conversion_patterns>
<!-- 20-01-PLAN.md §conversion_patterns (P-1 ~ P-8) 와 동일. -->

**C# 7.2 한정 (W1 — .NET Framework 4.8):** `??=` (null-coalescing assignment), `is { }` property pattern, switch expression (`x switch { ... }`), tuple deconstruction with discards 사용 금지. 명시적 if/else / 임시변수 + null 체크 패턴만 허용 (D-01 강제).
</conversion_patterns>

<read_first>
1. `WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` — 전체
2. `WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs` — 전체 (Phase 19-01 신규)
3. `WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs` — 전체 (Phase 28-01 정적 helper + defaults)
4. `WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs` — 전체 (Phase 28-02 분기)
5. `.planning/phases/20-code-style-cleanup/20-CONTEXT.md` — D-01 ~ D-17
</read_first>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DatumConfig.cs + DynamicPropertyHelper.cs 변환</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs, WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs</files>
  <behavior>
    - DatumConfig ICustomTypeDescriptor (Phase 17-02 / 19-01) 동작 보존 — TLI/CTH/VTH 동적 PropertyGrid 노출
    - DynamicPropertyHelper.FilterProperties (Phase 19-01) 위임 호출 시그니처 무변경
    - DatumConfig per-ROI 필드 sentinel + EnsurePerRoiDefaults idempotent migration (Phase 13-04) 동작 보존
  </behavior>
  <action>
0. **hbk 마커 baseline (W2 — D-13 보존 검증):**
   ```powershell
   foreach ($f in @('WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs','WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs')) {
     $c = Get-Content $f -Raw -Encoding UTF8
     $hbkBaseline = ([regex]::Matches($c,'260[0-9]{4} hbk Phase [0-9]+')).Count
     # Phase 20 (260508) 마커는 baseline 시점에 0 → 측정값 = 비-Phase-20 hbk (Phase 17-02 / 19-01 / 13-04 등)
     Write-Host "${f}: baseline_hbk_pre20 = $hbkBaseline"
   }
   ```
   각 파일별 N 값 기록 → acceptance criteria #4 의 expected 값으로 사용.
1. DatumConfig.cs 의 `?:` / `??` / `?.` grep — 카운트 적음 (rough 0~5).
2. 발견된 모든 인스턴스 변환 (P-1 ~ P-8). ICustomTypeDescriptor.GetProperties / GetAttributes 등 인터페이스 메서드의 분기는 P-4 명시 if/else.
3. DynamicPropertyHelper.cs 의 `?:` / `??` / `?.` 변환. Phase 19-01 의 hideFunc null-guard / sourceNames null-safe 패턴 (이미 명시 if 인 경우 변경 X).
4. 'what' 주석 정리 — ICustomTypeDescriptor 구현 의도 (D-08), Phase 19-01 위임 사유, Phase 17-02 동적 hide 사유는 보존.
5. 변환 라인 `//260508 hbk Phase 20`. Phase 17-02 / 19-01 의 hbk 마커 라인이 변환 대상이면 D-12 (교체), 비변환이면 D-13 (보존).
  </action>
  <verify>
    <automated>cd C:/Info/Project/DataMeasurement &amp;&amp; powershell -Command "$files = @('WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs','WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs'); foreach ($f in $files) { $c=Get-Content $f -Raw -Encoding UTF8; $qq=([regex]::Matches($c,'\?\?(?!=)')).Count; $qd=([regex]::Matches($c,'\?\.')).Count; $hbk20=([regex]::Matches($c,'260508 hbk Phase 20')).Count; $hbkOther=([regex]::Matches($c,'260[0-9]{4} hbk Phase [0-9]+')).Count - $hbk20; Write-Host \"$f qq=$qq qd=$qd hbk20=$hbk20 hbk_pre20=$hbkOther\" }"</automated>
    <expected>각 파일 qq=0, qd=0 (LINQ tail 제외). hbk_pre20 = baseline N (D-13 보존).</expected>
  </verify>
  <done>
    - 두 파일의 `?:` / `??` / `?.` 변환 완료
    - ICustomTypeDescriptor 동작 보존 (사람 검토)
    - hbk 마커 D-12/D-13 정책 적용 (hbk_pre20 = baseline)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: EdgeOptionLists.cs + CircleDiameterMeasurement.cs 변환</name>
  <files>WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs, WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs</files>
  <behavior>
    - Phase 28-01 의 MapRadialDirectionToHalconPolarity static helper 동작 보존 (단일 source of truth)
    - Phase 28-01 의 4 FAI polar defaults (FaiCirclePolarStepDeg / RectL1Ratio / RectL2Ratio / EdgeSelection) 값 보존
    - Phase 28-02 의 CircleDiameterMeasurement.TryExecute 분기 (string.IsNullOrEmpty(Circle_RadialDirection) → fit, Inward/Outward → polar) 동작 보존
    - REQ-28-04 INI 하위호환 — Circle_RadialDirection 미존재 시 default "" → fit 경로 자동 진입
  </behavior>
  <action>
0. **hbk 마커 baseline (W2 — D-13 보존 검증):**
   ```powershell
   foreach ($f in @('WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs','WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs')) {
     $c = Get-Content $f -Raw -Encoding UTF8
     $hbkBaseline = ([regex]::Matches($c,'260[0-9]{4} hbk Phase [0-9]+')).Count
     # Phase 28 (260508 Phase 28) 마커 다수 — 본 phase 의 D-13 핵심 보존 대상
     $hbk28 = ([regex]::Matches($c,'260508 hbk Phase 28')).Count
     Write-Host "${f}: baseline_hbk_total = $hbkBaseline, hbk_phase28 = $hbk28"
   }
   ```
   각 파일별 hbkBaseline + hbk28 값 기록. 변환 후 `total - hbk20` = baseline 동등성 검증.
1. EdgeOptionLists.cs 의 `?:` / `??` / `?.` grep. fully-qualified `System.StringComparison` 사용 (Phase 28-01 패턴) 보존.
2. 발견된 인스턴스 변환 (P-1 ~ P-8). Phase 28-01 의 helper + defaults 정의 라인은 무변경 (해당 라인은 변환할 연산자 없음).
3. CircleDiameterMeasurement.cs 의 `?:` / `??` / `?.` grep. Phase 28-02 의 `string.IsNullOrEmpty(Circle_RadialDirection)` 분기는 이미 명시 if 패턴이므로 변환 X.
4. 발견된 인스턴스 변환. Helper 호출 (`MapRadialDirectionToHalconPolarity`) 라인 무변경.
5. 'what' 주석 정리 — Phase 28-01 helper sole-source 의도, Phase 28-02 분기 의도, REQ-28-03 default-equality 의도는 보존.
6. 변환 라인 `//260508 hbk Phase 20`. Phase 28-01 / 28-02 의 hbk 마커 라인은 D-13 (변환 안 한 라인 — 보존).
  </action>
  <verify>
    <automated>cd C:/Info/Project/DataMeasurement &amp;&amp; powershell -Command "$files = @('WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs','WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs'); foreach ($f in $files) { $c=Get-Content $f -Raw -Encoding UTF8; $qq=([regex]::Matches($c,'\?\?(?!=)')).Count; $qd=([regex]::Matches($c,'\?\.')).Count; $hbk28=([regex]::Matches($c,'260508 hbk Phase 28')).Count; $hbk20=([regex]::Matches($c,'260508 hbk Phase 20')).Count; $hbkOther=([regex]::Matches($c,'260[0-9]{4} hbk Phase [0-9]+')).Count - $hbk20; Write-Host \"$f qq=$qq qd=$qd hbk28=$hbk28 hbk_pre20=$hbkOther\" }"</automated>
    <build>cd C:/Info/Project/DataMeasurement &amp;&amp; "C:/Program Files (x86)/Microsoft Visual Studio/2019/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:m</build>
    <expected>각 파일 qq=0, qd=0, hbk28 = baseline (Phase 28 마커 카운트 변환 전·후 동일), hbk_pre20 = baseline (D-13 전체 보존). msbuild PASS, 신규 warning 0.</expected>
  </verify>
  <done>
    - 두 파일의 `?:` / `??` / `?.` 변환 완료
    - Phase 28-01 helper + defaults 동작 보존
    - Phase 28-02 TryExecute 분기 동작 보존
    - hbk 마커 D-12/D-13 정책 적용 (Phase 28 마커 + 기타 비-Phase-20 마커 모두 baseline 보존)
    - msbuild Debug/x64 PASS, 신규 warning 0
  </done>
</task>

</tasks>

<acceptance_criteria>
1. 4 파일 모두 powershell grep `??` (`??=` 제외) = 0
2. 4 파일 모두 powershell grep `?.` (LINQ tail 제외) = 0
3. msbuild Debug/x64 exit 0, 신규 warning 0
4. **hbk 마커 보존 (W2 — D-13):** 4 파일 각각 변환 전 baseline `(grep -c '260[0-9]{4} hbk Phase [0-9]+' <file> except 260508 prefix)` 카운트 = N. 변환 후 동일 N 유지 (Phase 17-02 / 19-01 / 13-04 / 28-01 / 28-02 마커 라인은 비변환 → 보존).
5. EdgeOptionLists.cs 의 `260508 hbk Phase 28` 마커 카운트 변환 전·후 동일 (D-13 — Phase 28 마커 핵심 보존)
6. CircleDiameterMeasurement.cs 의 Circle_RadialDirection 분기 시그니처 무변경 (Phase 28-02 회귀 0)
7. C# 7.2 호환 (W1 — `??=` / switch expression / `is { }` 도입 0)
</acceptance_criteria>

<verification>
사람 검토 포인트:
- DatumConfig ICustomTypeDescriptor 인터페이스 메서드 (GetProperties 등) 의 분기 분해가 Phase 17-02 동적 hide 동작 보존
- DynamicPropertyHelper.FilterProperties 시그니처 무변경 (Phase 19-01)
- EdgeOptionLists.MapRadialDirectionToHalconPolarity 시그니처 무변경 (Phase 28-01)
- CircleDiameterMeasurement.TryExecute 의 fit/polar 분기 동작 무변경

자동화:
- msbuild PASS + 신규 warning 0
- Wave 2 (20-08) SIMUL UAT 에서 Datum CTH + FAI CircleDiameter (Phase 28 검증 경로) 동작 검증
</verification>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 변환 전후 (Phase 17-02 / 19-01 / 28-01 / 28-02 결정) | 위 결정의 인터페이스/시그니처 손상 시 v1.1 회귀 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06-01 | Tampering | DatumConfig ICustomTypeDescriptor 인터페이스 메서드 분해 손상 | mitigate | 인터페이스 메서드 시그니처 무변경 강제 + 20-08 SIMUL UAT (Phase 17-02 동적 hide 검증). |
| T-20-06-02 | Tampering | EdgeOptionLists / CircleDiameterMeasurement Phase 28 결정 손상 | mitigate | 헬퍼 호출 / 분기 시그니처 무변경 + acceptance criteria #4, #5, #6 + 20-08 byte-identical 회귀. |

Severity: LOW — 경량 파일이며 변환 포인트 ~5. 핵심 결정 (인터페이스, 헬퍼) 시그니처 무변경 강제로 mitigation 충분.
</threat_model>

<success_criteria>
- 4 파일 모두 `?:`/`??`/`?.` (예외 제외) 변환 완료
- Phase 17-02 / 19-01 / 28-01 / 28-02 결정 시그니처 보존
- msbuild PASS, 신규 warning 0
- hbk_pre20 카운트 baseline 보존 (W2)
</success_criteria>

<output>
After completion, create `.planning/phases/20-code-style-cleanup/20-06-SUMMARY.md`:
- 4 파일별 변환 라인 수 (?? / ?: / ?. 별)
- Phase 17-02 / 19-01 / 28-01 / 28-02 결정 보존 명시
- D-04/D-05 예외 적용 라인 (있다면)
- 4 파일별 hbk 마커 baseline N + 변환 후 N (W2 — D-13 동등성 증명)
</output>
