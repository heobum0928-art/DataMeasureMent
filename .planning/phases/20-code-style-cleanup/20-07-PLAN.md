---
phase: 20
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
  - WPF_Example/UI/Dialog/ComboInputBox.cs
  - WPF_Example/UI/Dialog/ComboInputBoxWindow.xaml
  - WPF_Example/UI/Dialog/ComboInputBoxWindow.xaml.cs
autonomous: true
requirements:
  - QUAL-02
  - QUAL-04
must_haves:
  truths:
    - "VisionAlgorithmService / HalconDisplayService / ComboInputBox / ComboInputBoxWindow.xaml.cs (4 light Halcon + Dialog 파일) 의 `?:` / `??` / `?.` 가 명시적 if/else 로 교체되었다 (D-04/D-05 예외)"
    - "ComboInputBoxWindow.xaml (XAML 파일) 의 Binding `?.` 패턴은 (사용자 결정) 변환 대상이 아님 — D-01 의 '코드' 범위 외, plan_design_guidance 의 옵션 (b) 채택"
    - "Phase 18 CO-04 RoiRedrawRequested 의도 (HalconDisplayService) / Phase 28 helper 호출 (VisionAlgorithmService.TryFindCircle) 시그니처 무변경"
    - "검출 strip 색상 분기 (Phase 18 CO-05 — 성공=녹색/실패=빨강) 동작 보존"
  artifacts:
    - path: "WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs"
      provides: "TryFindCircle / TryFitLine / Halcon 알고리즘 wrapper"
    - path: "WPF_Example/Halcon/Display/HalconDisplayService.cs"
      provides: "Halcon overlay 렌더 + Datum/FAI strip 색상 분기 + Phase 11/13/15/17/18 결정"
    - path: "WPF_Example/UI/Dialog/ComboInputBox.cs"
      provides: "Combo 입력 다이얼로그 정적 helper"
    - path: "WPF_Example/UI/Dialog/ComboInputBoxWindow.xaml"
      provides: "Combo 입력 윈도우 XAML — Binding 변환 대상 외 (사용자 가정 명시)"
    - path: "WPF_Example/UI/Dialog/ComboInputBoxWindow.xaml.cs"
      provides: "Combo 입력 윈도우 code-behind"
  key_links:
    - from: "VisionAlgorithmService.cs"
      to: "DatumFindingService (CircleTwoHorizontal teaching) + CircleDiameterMeasurement (Phase 28-02 polar)"
      via: "TryFindCircle 시그니처 (Phase 28 호출 경로)"
      pattern: "TryFindCircle"
    - from: "HalconDisplayService.cs"
      to: "MainResultViewerControl / MainView (overlay 렌더 호출)"
      via: "Render*Overlay 메서드 (Phase 11/13/15/17/18)"
      pattern: "Render.*Overlay"
---

<objective>
4 개 경량 Halcon + Dialog 파일 (VisionAlgorithmService + HalconDisplayService + ComboInputBox + ComboInputBoxWindow.xaml.cs) + 1 XAML (변환 대상 외 가정 명시) 의 잔여 `?:` / `??` / `?.` 연산자를 명시적 if/else 로 변환한다.

Purpose: QUAL-02/QUAL-04 — 경량 파일 묶음. HalconDisplayService 는 Phase 11/13/15/17/18 의 모든 overlay 결정이 누적된 파일이라 의도 주석 보존이 결정적. ComboInputBoxWindow.xaml 의 Binding `?.` 변환 정책 결정 (옵션 b — 변환 대상 외) 을 plan 에 명시.
Output: 4 파일 변환 + 1 XAML 가정 명시 + Wave 2 회귀 검증 (20-08) byte-identical 보존.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-code-style-cleanup/20-CONTEXT.md
@.planning/phases/20-code-style-cleanup/20-01-PLAN.md
@CLAUDE.md
@WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
@WPF_Example/Halcon/Display/HalconDisplayService.cs
@WPF_Example/UI/Dialog/ComboInputBox.cs
@WPF_Example/UI/Dialog/ComboInputBoxWindow.xaml
@WPF_Example/UI/Dialog/ComboInputBoxWindow.xaml.cs
</context>

<conversion_patterns>
<!-- 20-01-PLAN.md §conversion_patterns (P-1 ~ P-8) 와 동일. -->

**C# 7.2 한정 (W1 — .NET Framework 4.8):** `??=` (null-coalescing assignment), `is { }` property pattern, switch expression (`x switch { ... }`), tuple deconstruction with discards 사용 금지. 명시적 if/else / 임시변수 + null 체크 패턴만 허용 (D-01 강제).
</conversion_patterns>

<assumptions>
**A-01 (XAML Binding 변환 정책):** ComboInputBoxWindow.xaml 의 Binding 식 (`{Binding Foo?.Bar}`) 은 D-01 의 "코드" 범위 외로 간주. CONTEXT.md plan_design_guidance 의 옵션 (b) 채택 — 단순 유지. IValueConverter 도입 비용 대비 가독성 손실 큼.
- 사람 검토 권고: 본 phase 시작 후 ComboInputBoxWindow.xaml grep 결과 `?.` = 0 확인됨 (planner pre-check). 따라서 본 plan 은 XAML 무변경.
- 만약 다른 XAML 에서 `?.` 발견 시 별도 phase 또는 quick task 로 처리.
</assumptions>

<read_first>
1. `WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs` — 전체
2. `WPF_Example/Halcon/Display/HalconDisplayService.cs` — 전체 (Phase 11/13/15/17/18 결정 누적 — 가장 큰 light 파일)
3. `WPF_Example/UI/Dialog/ComboInputBox.cs` — 전체 (단순)
4. `WPF_Example/UI/Dialog/ComboInputBoxWindow.xaml` — 전체 (변환 대상 외 — 사람 검토용)
5. `WPF_Example/UI/Dialog/ComboInputBoxWindow.xaml.cs` — 전체 (단순)
6. `.planning/phases/20-code-style-cleanup/20-CONTEXT.md` — D-01 ~ D-17
</read_first>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: VisionAlgorithmService.cs + HalconDisplayService.cs 변환</name>
  <files>WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs, WPF_Example/Halcon/Display/HalconDisplayService.cs</files>
  <behavior>
    - VisionAlgorithmService.TryFindCircle 시그니처 무변경 (DatumFindingService + CircleDiameterMeasurement 호출 경로 보존)
    - HalconDisplayService.Render*Overlay 시그니처 무변경 (MainResultViewerControl / MainView 호출 경로 보존)
    - HalconDisplayService 의 Phase 18 CO-05 strip 색상 분기 (성공=녹색/실패=빨강) 동작 보존
    - HalconDisplayService 의 Phase 12-03 RenderDatumOverlay 알고리즘별 분기 동작 보존
    - HOperatorSet try/catch wrap 보존 (P-10)
  </behavior>
  <action>
0. **hbk 마커 baseline (W2 — D-13 보존 검증):**
   ```powershell
   foreach ($f in @('WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs','WPF_Example/Halcon/Display/HalconDisplayService.cs')) {
     $c = Get-Content $f -Raw -Encoding UTF8
     $hbkBaseline = ([regex]::Matches($c,'260[0-9]{4} hbk Phase [0-9]+')).Count
     # Phase 11 / 13-04 / 15-02 / 17-02 / 18 마커 다수 — D-13 보존 핵심 대상
     Write-Host "${f}: baseline_hbk_pre20 = $hbkBaseline"
   }
   ```
   각 파일별 N 값 기록 → acceptance criteria #5 의 expected 값으로 사용.
1. VisionAlgorithmService.cs 의 `?:` / `??` / `?.` grep. 발견된 인스턴스 변환 (P-1 ~ P-9).
2. HalconDisplayService.cs 의 `?:` / `??` / `?.` grep. 발견된 인스턴스 변환. Phase 18 CO-04 (`CircleStripSuccesses` 분기), Phase 18 CO-05 (성공/실패 색상), Phase 12-03 RenderDatumOverlay 알고리즘별 분기는 모두 의미 보존.
3. 'what' 주석 정리 — Phase 11 Circle 도형 분기, Phase 13-04 strip orientation 의도, Phase 15-02 measurePhi 매핑 의도, Phase 17-02 Datum 분기, Phase 18 CO-04/CO-05 의도는 모두 보존.
4. 변환 라인 `//260508 hbk Phase 20`. Phase 11/12-03/13-04/15-02/17-02/18 의 hbk 마커 라인이 변환 대상이면 D-12, 비변환이면 D-13.
5. HOperatorSet try/catch 보존 (P-10).
  </action>
  <verify>
    <automated>cd C:/Info/Project/DataMeasurement &amp;&amp; powershell -Command "$files = @('WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs','WPF_Example/Halcon/Display/HalconDisplayService.cs'); foreach ($f in $files) { $c=Get-Content $f -Raw -Encoding UTF8; $qq=([regex]::Matches($c,'\?\?(?!=)')).Count; $qd=([regex]::Matches($c,'\?\.')).Count; $hbk20=([regex]::Matches($c,'260508 hbk Phase 20')).Count; $hbkOther=([regex]::Matches($c,'260[0-9]{4} hbk Phase [0-9]+')).Count - $hbk20; Write-Host \"$f qq=$qq qd=$qd hbk20=$hbk20 hbk_pre20=$hbkOther\" }"</automated>
    <expected>각 파일 qq=0, qd=0 (LINQ tail 제외). hbk_pre20 = baseline N (D-13 보존).</expected>
  </verify>
  <done>
    - 두 파일의 `?:` / `??` / `?.` 변환 완료
    - TryFindCircle / Render*Overlay 시그니처 무변경
    - Phase 11 ~ Phase 18 의도 주석 보존
    - HOperatorSet try/catch 보존
    - hbk_pre20 = baseline (D-13 — Phase 11/13/15/17/18 마커 보존)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ComboInputBox.cs + ComboInputBoxWindow.xaml.cs 변환 + XAML 가정 검증</name>
  <files>WPF_Example/UI/Dialog/ComboInputBox.cs, WPF_Example/UI/Dialog/ComboInputBoxWindow.xaml.cs, WPF_Example/UI/Dialog/ComboInputBoxWindow.xaml</files>
  <behavior>
    - ComboInputBox.cs / ComboInputBoxWindow.xaml.cs 의 `?:` / `??` / `?.` 분해
    - ComboInputBoxWindow.xaml 의 Binding `?.` 검출 시 사용자 합의 (옵션 b — 변환 대상 외) 따라 무변경. `?.` = 0 확인 (planner pre-check 결과).
    - ComboInputBox.Show 정적 메서드 시그니처 무변경 (외부 호출 경로 보존)
  </behavior>
  <action>
0. **hbk 마커 baseline (W2):**
   ```powershell
   foreach ($f in @('WPF_Example/UI/Dialog/ComboInputBox.cs','WPF_Example/UI/Dialog/ComboInputBoxWindow.xaml.cs')) {
     $c = Get-Content $f -Raw -Encoding UTF8
     $hbkBaseline = ([regex]::Matches($c,'260[0-9]{4} hbk Phase [0-9]+')).Count
     Write-Host "${f}: baseline_hbk_pre20 = $hbkBaseline"
   }
   ```
   값 기록.
1. ComboInputBox.cs 의 `?:` / `??` / `?.` grep. 발견된 인스턴스 변환 (P-1 ~ P-8).
2. ComboInputBoxWindow.xaml.cs 의 `?:` / `??` / `?.` grep. 발견된 인스턴스 변환. WPF Window 라이프사이클 (Loaded / Closing) 핸들러 동작 보존.
3. ComboInputBoxWindow.xaml grep — `?.` 검출 시 무변경 + plan SUMMARY 에 카운트 명시. `?.` 미검출 시 plan SUMMARY 에 "XAML 변환 불필요 — `?.` = 0" 명시 (assumption A-01 검증).
4. 'what' 주석 정리. ComboInputBox 는 단순 utility — 한 줄 주석 대부분 자명.
5. 변환 라인 `//260508 hbk Phase 20`.
  </action>
  <verify>
    <automated>cd C:/Info/Project/DataMeasurement &amp;&amp; powershell -Command "$files = @('WPF_Example/UI/Dialog/ComboInputBox.cs','WPF_Example/UI/Dialog/ComboInputBoxWindow.xaml.cs'); foreach ($f in $files) { $c=Get-Content $f -Raw -Encoding UTF8; $qq=([regex]::Matches($c,'\?\?(?!=)')).Count; $qd=([regex]::Matches($c,'\?\.')).Count; $hbk20=([regex]::Matches($c,'260508 hbk Phase 20')).Count; $hbkOther=([regex]::Matches($c,'260[0-9]{4} hbk Phase [0-9]+')).Count - $hbk20; Write-Host \"$f qq=$qq qd=$qd hbk_pre20=$hbkOther\" }; $xc=Get-Content 'WPF_Example/UI/Dialog/ComboInputBoxWindow.xaml' -Raw -Encoding UTF8; $xqd=([regex]::Matches($xc,'\?\.')).Count; Write-Host \"XAML qd=$xqd\""</automated>
    <build>cd C:/Info/Project/DataMeasurement &amp;&amp; "C:/Program Files (x86)/Microsoft Visual Studio/2019/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:m</build>
    <expected>두 .cs 파일 qq=0, qd=0, hbk_pre20 = baseline. XAML qd=0 (검증 — 변환 대상 외). msbuild PASS, 신규 warning 0.</expected>
  </verify>
  <done>
    - ComboInputBox.cs + ComboInputBoxWindow.xaml.cs 변환 완료
    - XAML 의 `?.` 카운트 SUMMARY 에 명시
    - msbuild PASS, 신규 warning 0
    - hbk_pre20 = baseline (D-13)
  </done>
</task>

</tasks>

<acceptance_criteria>
1. 4 .cs 파일 모두 powershell grep `??` (`??=` 제외) = 0
2. 4 .cs 파일 모두 powershell grep `?.` (LINQ tail 제외) = 0
3. ComboInputBoxWindow.xaml — `?.` 카운트 SUMMARY 에 명시 (현재 0 확인 — assumption A-01 검증)
4. msbuild Debug/x64 exit 0, 신규 warning 0
5. **hbk 마커 보존 (W2 — D-13):** 4 .cs 파일 각각 변환 전 baseline `(grep -c '260[0-9]{4} hbk Phase [0-9]+' <file> except 260508 prefix)` 카운트 = N. 변환 후 동일 N 유지 (Phase 11 / 13-04 / 15-02 / 17-02 / 18 마커 라인은 비변환 → 보존).
6. VisionAlgorithmService.TryFindCircle / HalconDisplayService.Render*Overlay 시그니처 무변경
7. Phase 11 ~ Phase 18 의도 주석 보존
8. C# 7.2 호환 (W1 — `??=` / switch expression / `is { }` 도입 0)
</acceptance_criteria>

<verification>
사람 검토 포인트:
- HalconDisplayService 의 Phase 18 CO-05 strip 색상 (`CircleStripSuccesses` → 성공/실패 분기) 변환 후 색상 매핑 동등성
- VisionAlgorithmService.TryFindCircle 의 datumTransform=null 호출 경로 (Phase 12-02 teaching identity) 보존
- ComboInputBox 의 사용자 입력 처리 분기 보존

자동화:
- msbuild PASS + 신규 warning 0
- Wave 2 (20-08) SIMUL UAT 에서 Datum 검출 strip 색상 + Datum/FAI overlay 시각 검증
</verification>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 변환 전후 (Halcon overlay 의미) | RenderDatumOverlay / RenderCircleStripOverlay 분기 손상 시 시각 피드백 잘못 |
| XAML Binding 변환 가정 | A-01 가정 잘못 시 (XAML 에 `?.` 있는 경우) 미변환 잔재 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07-01 | Tampering | HalconDisplayService Phase 18 CO-05 분기 손상 | mitigate | 강제 시그니처 무변경 + 20-08 SIMUL UAT 시각 검증. |
| T-20-07-02 | Tampering | VisionAlgorithmService.TryFindCircle 시그니처 손상 | mitigate | 시그니처 무변경 강제 + 20-08 byte-identical 회귀 (Datum CTH + FAI CircleDiameter). |
| T-20-07-03 | Tampering | XAML A-01 가정 오판 | accept | planner pre-check 결과 `?.` = 0 확인. acceptance criteria #3 으로 Task 2 에서 재검증. |
| T-20-07-04 | Tampering | hbk 마커 (D-12) Phase 11/13/15/17/18 의도 손상 | mitigate | D-13 + acceptance criteria #5 (W2 hbk_pre20 baseline 동등성). |

Severity: LOW — 4 .cs 파일 모두 변환 포인트 적음, 핵심 시그니처 보존 강제. XAML A-01 은 pre-check 검증됨.
</threat_model>

<success_criteria>
- 4 .cs 파일 + 1 XAML (변환 대상 외) 처리 완료
- Phase 11/12-03/13-04/15-02/17-02/18 의도 주석 보존
- msbuild PASS, 신규 warning 0
- hbk_pre20 baseline 보존 (W2)
</success_criteria>

<output>
After completion, create `.planning/phases/20-code-style-cleanup/20-07-SUMMARY.md`:
- 4 .cs 파일별 변환 라인 수 (?? / ?: / ?. 별)
- XAML `?.` 카운트 (assumption A-01 검증)
- Phase 11 ~ Phase 18 의도 주석 보존 명시
- 4 .cs 파일별 hbk 마커 baseline N + 변환 후 N (W2 — D-13 동등성 증명)
</output>
