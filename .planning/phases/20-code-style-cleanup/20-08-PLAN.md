---
phase: 20
plan: 08
type: execute
wave: 2
depends_on: ["20-01", "20-02", "20-03", "20-04", "20-05", "20-06", "20-07"]
files_modified: []
autonomous: false
requirements:
  - QUAL-02
  - QUAL-04
must_haves:
  truths:
    - "AC #3: SIMUL_MODE 검사 시나리오 (Datum 티칭 TLI/CTH/VTH + FAI Edge 6종 + CircleDiameter) 가 Phase 20 변환 전·후 byte-identical 동등 (1e-9 mm 마진) — 14 파일 변환 종합 회귀"
    - "AC #4: msbuild Debug/x64 PASS + 신규 warning 0 — 14 파일 변환 종합"
    - "AC #1: 14 파일 전체에서 `?:` / `??` / `?.` 가 명시적 if/else 로 교체됨 (D-04 LINQ tail / D-05 expression-bodied 예외 적용) — Wave 1 완료 후 종합 grep"
    - "AC #2: 14 파일 전체에서 'what' 주석 제거 / 'why' 주석 보존 (사람 검토)"
  artifacts:
    - path: ".planning/phases/20-code-style-cleanup/20-VERIFICATION.md"
      provides: "Phase 20 종합 회귀 검증 결과 (4 AC × 14 파일 매트릭스 + SIMUL UAT 결과)"
  key_links:
    - from: "Phase 28 SIMUL UAT 레시피"
      to: "Phase 20 회귀 검증"
      via: "D-15 — 동일 레시피 재사용 (D:\\1.bmp 기반)"
      pattern: "Phase 28 SIMUL"
---

<objective>
Wave 1 (7 변환 plan: 20-01 ~ 20-07) 이 14 파일을 모두 변환한 후, 변환 전·후의 의미론적 동등성을 종합 검증한다 — msbuild Debug/x64 PASS + 신규 warning 0 + SIMUL_MODE byte-identical (1e-9 mm 마진) + 'what'/'why' 주석 사람 검토.

Purpose: ROADMAP §Phase 20 Success Criteria 4개 충족 + REQUIREMENTS QUAL-02/QUAL-04 sign-off. Wave 1 의 각 plan 은 단위 PASS 만 검증, 본 plan 이 종합 회귀 + sign-off.
Output: .planning/phases/20-code-style-cleanup/20-VERIFICATION.md (4 AC × 14 파일 매트릭스 + SIMUL UAT 결과 + sign-off frontmatter).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-code-style-cleanup/20-CONTEXT.md
@.planning/phases/20-code-style-cleanup/20-01-PLAN.md
@.planning/phases/20-code-style-cleanup/20-02-PLAN.md
@.planning/phases/20-code-style-cleanup/20-03-PLAN.md
@.planning/phases/20-code-style-cleanup/20-04-PLAN.md
@.planning/phases/20-code-style-cleanup/20-05-PLAN.md
@.planning/phases/20-code-style-cleanup/20-06-PLAN.md
@.planning/phases/20-code-style-cleanup/20-07-PLAN.md
@.planning/phases/28-fai-circlediameter-datum-circle/28-UAT.md
@CLAUDE.md
</context>

<read_first>
1. 14 파일 (Wave 1 변환 결과)
2. Wave 1 의 7 SUMMARY 파일 (.planning/phases/20-code-style-cleanup/20-{01..07}-SUMMARY.md)
3. .planning/phases/28-fai-circlediameter-datum-circle/28-UAT.md — 재사용 SIMUL UAT 레시피 (D-15)
</read_first>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 종합 grep 검증 (AC #1) + msbuild 회귀 검증 (AC #4) + 인코딩 mojibake 검증 (W3)</name>
  <files>.planning/phases/20-code-style-cleanup/20-VERIFICATION.md</files>
  <behavior>
    - 14 파일 전체에서 powershell grep `??` (`??=` 제외) = 0
    - 14 파일 전체에서 powershell grep `?.Invoke` = 0
    - 14 파일 전체에서 powershell grep `?.` (LINQ tail false positive 사람 검토 후) ≈ 0
    - 14 파일 전체에서 한국어 주석 mojibake (CP949 깨짐 / 잘못된 BOM) 없음 — W3 D-09 보존
    - msbuild Debug/x64 exit 0
    - 신규 warning 0 (Phase 20 시작 전 기준점 대비)
  </behavior>
  <action>
1. powershell 일괄 grep 스크립트 실행:
   ```powershell
   $files = @(
     'WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs',
     'WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs',
     'WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs',
     'WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs',
     'WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs',
     'WPF_Example/Halcon/Algorithms/DatumFindingService.cs',
     'WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs',
     'WPF_Example/Halcon/Display/HalconDisplayService.cs',
     'WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs',
     'WPF_Example/UI/ContentItem/MainView.xaml.cs',
     'WPF_Example/UI/ControlItem/InspectionListView.xaml.cs',
     'WPF_Example/UI/Dialog/ComboInputBox.cs',
     'WPF_Example/UI/Dialog/ComboInputBoxWindow.xaml',
     'WPF_Example/UI/Dialog/ComboInputBoxWindow.xaml.cs'
   )
   $report = @()
   foreach ($f in $files) {
     $c = Get-Content $f -Raw -Encoding UTF8
     $qq = ([regex]::Matches($c, '\?\?(?!=)')).Count
     $qd = ([regex]::Matches($c, '\?\.')).Count
     $invoke = ([regex]::Matches($c, '\?\.Invoke')).Count
     $hbk20 = ([regex]::Matches($c, '260508 hbk Phase 20')).Count
     # W3 mojibake detection — typical CP949→UTF8 misread artifacts
     $mojibake = ([regex]::Matches($c, '[ㅋ-ㅎ]{2,}|占쏙옙|�')).Count
     $report += [PSCustomObject]@{ File=$f; QQ=$qq; QD=$qd; Invoke=$invoke; HBK20=$hbk20; Mojibake=$mojibake }
   }
   $report | Format-Table -AutoSize
   ```
2. msbuild Debug/x64 빌드:
   ```powershell
   & "C:/Program Files (x86)/Microsoft Visual Studio/2019/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:m
   ```
   exit code = 0, 신규 warning 0 확인.
3. 결과를 .planning/phases/20-code-style-cleanup/20-VERIFICATION.md 에 기록 (AC #1, AC #4 매트릭스 + W3 mojibake 컬럼).
4. LINQ tail / 문자열 리터럴 false positive 사람 검토 — 잔여 `?.` / `?:` 라인 별도 표로 명시.
5. **W3 한국어 주석 보존 사람 검토**: mojibake 카운트 = 0 자동 검출 외에, Wave 1 SUMMARY 의 'what'/'why' 주석 카테고리에서 한국어 nuance 라인 샘플 5건 추출 → 변환 전·후 텍스트 동일 확인 (사람 spot-check).
  </action>
  <verify>
    <automated>cd C:/Info/Project/DataMeasurement &amp;&amp; powershell -Command "Test-Path .planning/phases/20-code-style-cleanup/20-VERIFICATION.md"</automated>
    <expected>14 파일 모두 QQ=0, Invoke=0, Mojibake=0; QD 잔여는 LINQ tail 사람 검토 결과로 분류; msbuild exit 0; 신규 warning = 0; 20-VERIFICATION.md 생성 확인.</expected>
  </verify>
  <done>
    - 20-VERIFICATION.md AC #1 매트릭스 작성 (14 파일 × QQ/QD/Invoke/Mojibake 카운트)
    - 20-VERIFICATION.md AC #4 결과 (msbuild exit + warning delta) 기록
    - LINQ tail 잔여 `?.` 사람 검토 결과 분류 (D-04 보존 vs 누락)
    - W3 한국어 주석 mojibake = 0 + spot-check PASS 기록
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SIMUL_MODE byte-identical 회귀 검증 (AC #3) — 사람 합의</name>
  <files>(checkpoint — 변경 파일 없음, 사람 검증 단계)</files>
  <action>
**[Checkpoint task — Claude 가 사용자 합의를 받기 위한 단계. 자동 실행 불가.]**

What was built (Wave 1 + Task 1):
- Wave 1 (20-01 ~ 20-07) 가 14 파일의 `?:`/`??`/`?.` 변환을 완료.
- Task 1 이 grep + msbuild + mojibake 자동 검증 완료.
- 이제 SIMUL_MODE 1회 실행 + Phase 28 SIMUL UAT 레시피 재사용 (D-15) 으로 Datum CTH/VTH/TLI 티칭 + FAI Edge 6종 + CircleDiameter 측정 결과가 변환 전·후 byte-identical (1e-9 mm 마진) 임을 검증.

How to verify (사용자 가이드):

1. **사전 준비** — 변환 전 baseline 기준점 확보:
   - Phase 20 시작 전 commit (e.g., `git rev-parse HEAD~N` 또는 STATE.md last_activity 시점) 의 SIMUL UAT 결과를 참조 (Phase 28 sign-off 시점 — `02adf80`).
   - Phase 28 28-UAT.md 의 baseline 측정값 (Test 1 결과) 을 비교 기준으로 사용.

2. **SIMUL_MODE 실행:**
   - DatumMeasurement.exe 실행 (Debug/x64 빌드 산출물).
   - 사용 레시피: Phase 28 SIMUL UAT 레시피 (D:\\1.bmp 이미지).
   - 실행 시퀀스:
     a. Datum 티칭 — TLI (TwoLineIntersect) 1회
     b. Datum 티칭 — CTH (CircleTwoHorizontal) 1회
     c. Datum 티칭 — VTH (VerticalTwoHorizontal) 1회
     d. FAI 측정 — Edge 6종 (각각) 1회씩
     e. FAI 측정 — CircleDiameter (Phase 28-02 fit + polar 양쪽 분기) 각 1회

3. **결과 비교 (byte-identical 1e-9 mm 마진 — D-16):**
   - Datum 티칭: 검출 origin (Row, Col) — 변환 전·후 1e-9 mm 마진 이내
   - FAI 측정: measured mm 값 — 동일 마진
   - 검출 strip 색상 (Phase 18 CO-05) — 시각적 동일

4. **차이 발견 시:**
   - 변환 버그 — D-16 따라 즉시 보고. 차이 발생 파일 식별 후 해당 plan 의 변환 라인 재검토.
   - 0 차이 시 — AC #3 PASS, sign-off 진행.

5. **'what'/'why' 주석 사람 검토 (AC #2):**
   - Wave 1 의 7 SUMMARY 파일 의 'what 주석 제거 / why 주석 보존' 카테고리 표 검토.
   - 사용자 합의 시 AC #2 PASS.

**Resume signal — Type "approved" if (둘 중 택일):**

**경로 A (정도 — D-16 byte-identical 직접 검증):**
- SIMUL_MODE 실행 결과 byte-identical (1e-9 mm 마진 — D-16) 확인
- 'what'/'why' 주석 정리 사용자 합의

**경로 B (code-inspection fallback — W5 강화 4 항목 정당화 의무):**

D-16 의 1e-9 mm byte-identical 임계는 Phase 20 의 엄격 정량 기준이다. SIMUL UAT 직접 실행을 생략하려면 다음 4 항목 모두를 SUMMARY.md (또는 20-VERIFICATION.md) 에 명시 의무:

1. **의미론적 동등 변환 증명** — Phase 20 의 모든 변환 패턴 (P-1 `??` 분해 / P-2 `?.` chain / P-3 event handler / P-4 `?:` 삼항 / P-9 HTuple null guard / P-10 try/catch wrap) 은 컴파일러 IL 레벨에서 동치임을 명시. 근거: D-01 정책 + Phase 28 28-UAT.md Test 2/3 의 동일 패턴 (`?:` → if/else 가 의미적 동등 변환이라는 사용자 + checker 합의된 사례).

2. **msbuild Debug/x64 PASS + 신규 warning 0** — Task 1 의 msbuild 결과 첨부 (exit 0 + warning delta = 0). 컴파일 단위에서 IL 검증 통과 = 시그니처 / 분기 / 예외 흐름 무손상 증명.

3. **Wave 1 모든 plan 의 acceptance_criteria 자동 grep 통과** — 14 파일 × QQ=0 + Invoke=0 + QD (LINQ tail 외 0) + mojibake=0 매트릭스 (Task 1 산출). 변환 누락 0 증명.

4. **hbk 마커 보존 카운트 (D-13 + W2)** — Wave 1 plan 별 SUMMARY 의 hbk_pre20 baseline N + 변환 후 N 동등성 표 제출. 변환 안 한 라인의 무수정 증명 (의도하지 않은 라인 손상 0).

**위 4 항목 모두 충족 시에만** SIMUL UAT 직접 실행 생략 가능. 4 항목 중 **단 하나라도 미충족** 시 D-16 의 byte-identical (1e-9 mm) 직접 비교 의무로 자동 회귀 — 경로 B 사용 불가.

Describe issues if:
- byte-identical 차이 발견 (어느 파일 / 어느 알고리즘 / 차이 값)
- 주석 정리 과도/부족
- 경로 B 의 4 항목 중 하나라도 충족 불가 — 사유 명시 후 경로 A 회귀 진행
  </action>
  <verify>
    <automated>(checkpoint — 자동 검증 없음. 사용자 응답 대기.)</automated>
    <expected>사용자 "approved" 또는 issue description.</expected>
  </verify>
  <done>
    - 사용자 "approved" 수신
    - AC #3 (SIMUL_MODE byte-identical) PASS via 경로 A, 또는 경로 B (4 항목 정당화 모두 SUMMARY/VERIFICATION 에 명시)
    - AC #2 ('what'/'why' 주석) PASS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 20-VERIFICATION.md 작성 + STATE.md 갱신 + ROADMAP 체크박스</name>
  <files>.planning/phases/20-code-style-cleanup/20-VERIFICATION.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <behavior>
    - 20-VERIFICATION.md frontmatter status=signed_off (사용자 합의 후)
    - 4 AC 모두 PASS 기록
    - STATE.md current position → Phase 21 (다음 phase)
    - ROADMAP.md Phase 20 체크박스 [x], `**Plans:**` 8 plans 표기
    - REQUIREMENTS.md QUAL-02 / QUAL-04 [x] 표시
  </behavior>
  <action>
1. Task 2 의 사용자 합의 (approved) 확인.
2. 20-VERIFICATION.md 에 4 AC × 14 파일 매트릭스 + SIMUL UAT 결과 (경로 A) 또는 W5 4 항목 정당화 (경로 B) 기록. frontmatter status=signed_off, signed_off_date=YYYY-MM-DD.
3. STATE.md:
   - current_position → Phase 21 (메모리 이미지 버퍼)
   - last_activity 갱신
   - Decisions section 에 [Phase 20]: 14 파일 `?:`/`??`/`?.` 명시 if/else 변환 + 'what' 주석 제거 + Phase 28 SIMUL UAT byte-identical (1e-9 mm) 회귀 0 항목 추가
4. ROADMAP.md Phase 20 [x] 체크 + `**Plans:** 8 plans` 명시 + 8 plan 리스트 [x] 표시.
5. REQUIREMENTS.md QUAL-02 / QUAL-04 [x] 마크.
6. git commit:
   ```
   docs(20): sign off Phase 20 — 14 files style cleanup, byte-identical regression PASS
   ```
  </action>
  <verify>
    <automated>cd C:/Info/Project/DataMeasurement &amp;&amp; powershell -Command "Get-Content .planning/phases/20-code-style-cleanup/20-VERIFICATION.md -Raw -Encoding UTF8 | Select-String 'status:\s*signed_off'"</automated>
    <expected>frontmatter signed_off 매치, STATE.md 갱신, ROADMAP.md 체크박스 갱신, REQUIREMENTS.md QUAL-02/04 [x].</expected>
  </verify>
  <done>
    - 20-VERIFICATION.md status=signed_off
    - STATE.md current position → Phase 21
    - ROADMAP.md Phase 20 [x], 8 plans 명시
    - REQUIREMENTS.md QUAL-02 / QUAL-04 [x]
    - git commit 완료
  </done>
</task>

</tasks>

<acceptance_criteria>
1. **AC #1** (operator 변환): 14 파일 전체에서 `??` (`??=` 제외) = 0, `?.Invoke` = 0, `?.` LINQ tail 외 = 0 — Task 1 grep 매트릭스 + 사람 검토.
2. **AC #2** ('what' 주석 제거): 14 파일 전체에서 'why' 주석만 보존 — Task 2 사람 검토 (Wave 1 SUMMARY 카테고리 표 검토). **추가 (W3):** 한국어 nuance 주석 mojibake 없음 — Task 1 자동 mojibake grep + 사람 spot-check 5건 PASS.
3. **AC #3** (SIMUL_MODE 동등성): Phase 28 SIMUL UAT 레시피 재실행 시 검출 origin / measured mm 변환 전·후 byte-identical (1e-9 mm 마진 — D-16) — 경로 A. 또는 경로 B (W5 4 항목 정당화 — 의미론적 동등 + msbuild PASS + Wave 1 grep 매트릭스 + hbk baseline 보존 모두 SUMMARY/VERIFICATION 에 명시) 사용자 합의.
4. **AC #4** (msbuild + warning 0): msbuild Debug/x64 exit 0, 신규 warning = 0 — Task 1 자동 검증.
</acceptance_criteria>

<verification>
종합 검증 — 4 AC 모두 PASS 시 sign-off:
- AC #1, AC #4 — 자동 (Task 1)
- AC #2, AC #3 — 사람 검토 (Task 2 checkpoint)
- 모두 PASS 시 Task 3 sign-off + 다음 phase 진행
</verification>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Wave 1 변환 종합 → SIMUL UAT | 14 파일 변환 누적 결과가 의미론적으로 동등하지 않으면 시스템 회귀 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-08-01 | Tampering | 14 파일 변환 누적 회귀 (cumulative semantic drift) | mitigate | SIMUL UAT 1회 실행 (D-14) + byte-identical 1e-9 mm 마진 (D-16). 차이 발견 시 즉시 plan 재검토. |
| T-20-08-02 | Repudiation | sign-off 후 회귀 발견 | mitigate | 20-VERIFICATION.md 에 4 AC 매트릭스 + SIMUL UAT 결과 (경로 A) 또는 W5 4 항목 정당화 (경로 B) 명시 — 추적 가능성 확보. |
| T-20-08-03 | Tampering | 한국어 주석 mojibake (CP949 / BOM 손실) | mitigate | W3 — Task 1 자동 mojibake grep + 사람 spot-check (AC #2 보강). |

Severity: MEDIUM — 종합 회귀가 한 번에 검증되므로 단일 fail point. SIMUL UAT 1회 실행 (또는 W5 4 항목 정당화) 으로 mitigation.
</threat_model>

<success_criteria>
- 4 AC 모두 PASS
- 20-VERIFICATION.md status=signed_off
- STATE.md / ROADMAP.md / REQUIREMENTS.md 갱신
- git commit 완료
- Phase 20 sign-off 완료, Phase 21 진행 가능
</success_criteria>

<output>
After completion:
- `.planning/phases/20-code-style-cleanup/20-VERIFICATION.md` (4 AC 매트릭스 + SIMUL UAT 결과 (경로 A) 또는 W5 4 항목 정당화 (경로 B) + sign-off)
- `.planning/phases/20-code-style-cleanup/20-08-SUMMARY.md` (Wave 2 종합 회귀 결과 + sign-off date)
</output>
