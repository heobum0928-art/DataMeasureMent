---
phase: 21-memory-image-buffer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: true
requirements:
  - BUF-01
  - BUF-02
tags:
  - documentation
  - lifecycle
  - phase-21
  - buffer

must_haves:
  truths:
    - "ShotConfig.SetImage / GetImage / ClearImage / HasImage / ClearAllResults 메서드 5개에 수명 계약 XML doc(///<summary>) 이 존재한다"
    - "InspectionRecipeManager.ClearShots 메서드에 lifetime 계약 XML doc 이 존재하며 호출 시점 3채널을 명시한다"
    - "Action_FAIMeasurement.cs EStep.Init 의 ShotParam.ClearAllResults() 호출 위에 D-02 channel #2 sequence-reset 의도 마킹 주석이 존재한다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs"
      provides: "5 XML doc blocks (HasImage / SetImage / GetImage / ClearImage / ClearAllResults)"
      contains: "/// <summary>"
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs"
      provides: "ClearShots XML doc with 3-channel lifetime contract"
      contains: "/// <summary>"
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "EStep.Init sequence-reset marker comment (Phase 21)"
      contains: "//260510 hbk Phase 21"
  key_links:
    - from: "ShotConfig.GetImage XML doc"
      to: "Action_FAIMeasurement.cs:141 using-block consumer + MainView.xaml.cs:113-127 try/finally consumer"
      via: "doc-string mentions caller-disposes-clone contract"
      pattern: "caller MUST dispose"
    - from: "ShotConfig.ClearImage XML doc"
      to: "3 dispose channels (recipe change subscriber / sequence reset EStep.Init / app shutdown Release)"
      via: "doc-string enumerates channels"
      pattern: "channel"
---

<objective>
Phase 21 의 D-06 (수명 계약 XML doc 강화) 와 D-02 channel #2 (sequence reset 마킹) 의 코드 인접 문서화를 수행한다. 신규 클래스/인터페이스 0, 신규 동작 0 — 순수 문서/주석 작업으로 AC#3 ("버퍼가 보유하는 HImage 수명 보장 시점이 코드 주석 또는 명시적 함수로 문서화") 를 충족한다.

Purpose: BUF-02 의 "lifetime 명시적 관리" 요구를 코드 인접 위치(메서드 위 XML doc + sequence-reset 마킹 주석)로 가시화하여, 향후 유지보수자가 grep `Phase 21 BUF-02` 또는 `///<summary>` 만으로 수명 계약을 추적 가능하게 만든다.
Output: 3개 파일 XML doc + 1 마킹 주석 추가 — 동작 변경 0, msbuild PASS, 회귀 0 (byte-identical 의미 보존).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-memory-image-buffer/21-CONTEXT.md
@.planning/phases/21-memory-image-buffer/21-PATTERNS.md
@./CLAUDE.md

<interfaces>
<!-- ShotConfig 의 buffer-related public 멤버 (XML doc 강화 대상). 파일은 K&R brace style. -->
<!-- 현재 상태 (Phase 20 미접촉 — `?.` 보존): -->

From WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs:
```csharp
[Browsable(false)]
public bool HasImage {
    get { lock (_imageLock) { return _image != null; } }
}                                                                   // L48-50

public void SetImage(HImage image) {                                // L59
    lock (_imageLock) {
        _image?.Dispose();
        _image = image?.CopyImage();
    }
}

public HImage GetImage() {                                          // L66
    lock (_imageLock) {
        return _image?.CopyImage();
    }
}

public void ClearImage() {                                          // L72
    lock (_imageLock) {
        _image?.Dispose();
        _image = null;
    }
}

public void ClearAllResults() {                                     // L96
    ClearImage();
    //260413 hbk Phase 6: Datum 초기화는 InspectionSequence(Fixture) 레벨에서 수행 (D-04)
    foreach (var fai in FAIList) {
        fai.ClearResult();
    }
}
```

From WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs:
```csharp
public void ClearShots() {                                          // L51
    foreach (var shot in Shots) {
        shot.ClearImage();
    }
    Shots.Clear();
}
```

From WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs:
```csharp
public override ActionContext Run() {                               // L57
    switch ((EStep)Step) {
        case EStep.Init:
            ShotParam?.ClearAllResults();                           // L60 — sequence reset trigger
            Step = (int)EStep.MoveZ;
            break;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ShotConfig 5 멤버 XML doc 강화 (D-06)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs (전체)
    - .planning/phases/21-memory-image-buffer/21-PATTERNS.md §"ShotConfig.cs (model, thread-safe in-memory buffer)"
    - .planning/phases/21-memory-image-buffer/21-CONTEXT.md §"D-06"
  </read_first>
  <acceptance_criteria>
    - grep -c '/// <summary>' WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs 가 **5 이상** 증가 (XML doc block 5개 신규)
    - HasImage 속성 (현재 L47-50) 위에 `/// <summary>` 블록 + `_imageLock` 동기화 보장 문구 포함
    - SetImage 메서드 (현재 L59) 위에 `/// <summary>` 블록 + "clone-on-input" + "caller retains ownership of input" 문구 포함
    - GetImage 메서드 (현재 L66) 위에 `/// <summary>` 블록 + "caller MUST dispose" 또는 "호출자가 dispose 책임" 문구 포함
    - ClearImage 메서드 (현재 L72) 위에 `/// <summary>` 블록 + 3개 dispose 채널 (recipe change / sequence reset / app shutdown) 명시 문구 포함
    - ClearAllResults 메서드 (현재 L96) 위에 `/// <summary>` 블록 + "sequence reset trigger" + "Action_FAIMeasurement.EStep.Init" 인용 문구 포함
    - 각 XML doc 블록 아래(또는 위)에 `//260510 hbk Phase 21` 마커 주석 1줄 존재 (XML `///` 라인은 마커 stacking 대상 아님 — 별도 일반 주석)
    - **동작 라인 (L43~L102) 텍스트 byte-identical 보존** — `_image?.Dispose()` `?.` 등 기존 operator NO 변경 (Phase 20 D-12: 미터치 라인 보존)
    - msbuild Debug/x64 PASS (`MSBuild.exe WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64`) — warning 0 신규
    - K&R brace style 보존 (file-wide convention)
  </acceptance_criteria>
  <action>
ShotConfig.cs L47-L102 의 5개 멤버 위에 다음 XML doc 블록을 **추가**한다 (메서드 시그니처 라인 자체는 변경 금지). 각 doc 블록은 그 메서드/속성 시그니처 바로 위에 위치한다. 마커 주석 `//260510 hbk Phase 21: ...` 은 XML doc 블록 위 첫 줄에 배치한다 (XML `///` 라인은 컴파일러 인식 doc 이므로 마커 라인과 분리 — Phase 20 D-12 stacking 회피).

**(1) HasImage 위 (현재 L47 `[Browsable(false)]` 직전):**
```csharp
        //260510 hbk Phase 21: BUF-02 lifetime contract — _imageLock 동기화 가시화
        /// <summary>
        /// 현재 buffer 에 HImage 가 보관되어 있는지 여부.
        /// _imageLock 으로 동기화되므로 임의 thread 에서 안전하게 호출 가능하다.
        /// </summary>
        [Browsable(false)]
        public bool HasImage {
```

**(2) SetImage 위 (현재 L59 직전):**
```csharp
        //260510 hbk Phase 21: BUF-02 lifetime contract — clone-on-input + 자동 dispose
        /// <summary>
        /// 입력된 HImage 를 내부 buffer 에 clone 하여 보관한다.
        /// 기존 _image 가 있으면 자동으로 Dispose 후 교체된다.
        /// 호출자는 입력 image 의 소유권을 그대로 보유하며, 호출 후에도 직접 Dispose 책임을 진다
        /// (이 메서드는 image.CopyImage() 만 보관하고 입력 본체는 건드리지 않는다).
        /// </summary>
        public void SetImage(HImage image) {
```

**(3) GetImage 위 (현재 L66 직전):**
```csharp
        //260510 hbk Phase 21: BUF-02 lifetime contract — clone-on-output, caller-disposes
        /// <summary>
        /// 현재 buffer 의 HImage 를 clone 하여 반환한다.
        /// **호출자가 반환된 HImage 의 Dispose 책임을 진다** — using 블록 또는 try/finally 로 해제할 것.
        /// 정규 소비 패턴: <c>using (var img = shot.GetImage()) { ... }</c>
        ///   (Action_FAIMeasurement.cs Measure 단계 참고) 또는
        /// <c>HImage img = null; try { img = shot.GetImage(); ... } finally { if (img != null) img.Dispose(); }</c>
        ///   (MainView.DisplayShotImage 참고).
        /// buffer 가 비어 있으면 null 을 반환하므로 사용 전 null 검사 필요.
        /// </summary>
        public HImage GetImage() {
```

**(4) ClearImage 위 (현재 L72 직전):**
```csharp
        //260510 hbk Phase 21: BUF-02 lifetime contract — 명시 해제 hook (3 channels)
        /// <summary>
        /// 내부 buffer 의 HImage 수명을 종료한다 (Dispose 후 null 로 재설정).
        /// Phase 21 BUF-02 lifetime 계약상 다음 3개 채널에서 호출되어야 한다:
        ///   (1) 레시피 변경 — Custom/SystemHandler.cs OnRecipeChanged subscriber 가
        ///       InspectionRecipeManager.ClearShots() 를 호출하여 모든 Shot 에 전파.
        ///   (2) 시퀀스 리셋 — Action_FAIMeasurement.cs EStep.Init 에서
        ///       ShotParam.ClearAllResults() 를 호출하여 도달 (Run 사이클 진입 시).
        ///   (3) 앱 종료 — SystemHandler.Release() 에서
        ///       Sequences.RecipeManager.ClearShots() 를 호출하여 도달.
        /// 멱등 (idempotent): 이미 비어 있는 buffer 에 호출해도 안전 (null-safe).
        /// </summary>
        public void ClearImage() {
```

**(5) ClearAllResults 위 (현재 L96 직전, 기존 `//260413 hbk Phase 6` 주석은 메서드 본문 안에 있으므로 영향 없음):**
```csharp
        //260510 hbk Phase 21: BUF-02 channel #2 — sequence reset 진입점
        /// <summary>
        /// Shot 의 모든 결과를 초기화한다 — image buffer Dispose + 각 FAI 결과 clear.
        /// Phase 21 BUF-02 lifetime 계약상 sequence reset 트리거 — Action_FAIMeasurement.cs
        /// EStep.Init 단계 (Run 사이클 진입 시 매번) 에서 호출된다.
        /// 별도 OnReset 이벤트/메서드를 SequenceBase 에 도입하지 않고 EStep.Init → ClearAllResults
        /// 경로로 충족 (Phase 21 D-04).
        /// </summary>
        public void ClearAllResults() {
```

**중요:**
- `[Browsable(false)]` 속성은 HasImage 의 doc 블록 **아래** (시그니처 라인 바로 위) 에 유지한다 — 위 (1) 예시 그대로.
- 동작 코드 (L43-46 _imageLock/_image 필드, L49 lock get body, L57-104 메서드 본문) 는 **byte-identical 보존**. `?.Dispose()` 등 Phase 20 미접촉 `?.` 연산자는 그대로 둔다 (Phase 20 D-12: 미변경 라인 stacking 회피).
- 파일 K&R brace style 보존 — XML doc 추가는 brace 영향 없음.
- using directive 추가 불요 (XML 문법은 컴파일러 내장).
  </action>
  <verify>
    <automated>cd /d C:\Info\Project\DataMeasurement &amp;&amp; "%ProgramFiles%\Microsoft Visual Studio\2019\Community\MSBuild\Current\Bin\MSBuild.exe" WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal /nologo</automated>
    grep 검증:
      - `findstr /c:"/// <summary>" WPF_Example\Custom\Sequence\Inspection\ShotConfig.cs` 결과가 **최소 5건** (HasImage / SetImage / GetImage / ClearImage / ClearAllResults)
      - `findstr /c:"260510 hbk Phase 21" WPF_Example\Custom\Sequence\Inspection\ShotConfig.cs` 결과가 **최소 5건**
      - `findstr /c:"caller MUST dispose" /c:"호출자" WPF_Example\Custom\Sequence\Inspection\ShotConfig.cs` 결과가 GetImage doc 안에 1건 이상
      - `findstr /c:"channel" WPF_Example\Custom\Sequence\Inspection\ShotConfig.cs` 결과가 ClearImage doc 안에 (3) 라벨 포함하여 표시
    msbuild output: "0 Warning(s)" "0 Error(s)" 또는 신규 0
  </verify>
  <done>5개 XML doc 블록 추가 + 5개 hbk 마커 + msbuild PASS + grep 검증 통과 + 동작 라인 byte-identical 보존.</done>
</task>

<task type="auto">
  <name>Task 2: InspectionRecipeManager.ClearShots XML doc 강화 + Action_FAIMeasurement EStep.Init 마킹 (D-06 + D-02 channel #2)</name>
  <files>
    WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs,
    WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  </files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs L40-L60 (ClearShots 메서드 + 인접 컨텍스트)
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs L55-L65 (Run + EStep.Init)
    - .planning/phases/21-memory-image-buffer/21-PATTERNS.md §"InspectionRecipeManager.cs" 와 §"Action_FAIMeasurement.cs"
    - .planning/phases/21-memory-image-buffer/21-CONTEXT.md §"D-02" 와 §"D-06"
  </read_first>
  <acceptance_criteria>
    - InspectionRecipeManager.ClearShots (현재 L51-L56) 위에 `/// <summary>` 블록 추가 — "Lifetime contract" + "(1) recipe change" + "(2) app shutdown" + "Idempotent" 키워드 포함
    - InspectionRecipeManager.cs 에 `//260510 hbk Phase 21` 마커 1건 이상 추가
    - Action_FAIMeasurement.cs EStep.Init 의 `ShotParam?.ClearAllResults();` 라인 (현재 L60) **바로 위** 에 `//260510 hbk Phase 21: BUF-02 channel #2 — sequence reset trigger ...` 마킹 주석 1줄 추가
    - Action_FAIMeasurement.cs L60 라인 자체는 byte-identical 보존 (`?.` 연산자 NO 변경 — Phase 20 D-12)
    - InspectionRecipeManager.cs ClearShots 메서드 본문 (L52-L55) byte-identical 보존
    - msbuild Debug/x64 PASS, warning 0 신규
    - K&R brace style 보존
  </acceptance_criteria>
  <action>
**File A: WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs**

L51 의 `public void ClearShots() {` **직전**에 다음 블록을 삽입한다:

```csharp
        //260510 hbk Phase 21: BUF-02 lifetime owner — recipe change + app shutdown 채널
        /// <summary>
        /// 모든 Shot 의 image buffer 를 Dispose 하고 Shot 리스트를 비운다.
        /// Phase 21 BUF-02 lifetime 계약상 다음 채널에서 호출되어야 한다:
        ///   (1) 레시피 변경 — Custom/SystemHandler.cs 의 OnRecipeChanged subscriber 가 호출
        ///       (이 호출 채널이 Phase 21 의 신규 wire — 기존에는 Load() 내부에서만 호출됨).
        ///   (2) 앱 종료 — SystemHandler.Release() 에서 Sequences.Dispose() 직전 호출
        ///       (이 호출 채널이 Phase 21 의 신규 wire — 기존 Release() 에는 누락됨).
        /// 또한 LoadPhase6Format() 도 INI 재로드 직전에 호출하므로 (1) subscriber 와 중복될 수
        /// 있으나, ClearImage 가 null-safe 이므로 멱등 (idempotent) 호출 안전.
        /// </summary>
        public void ClearShots() {
            foreach (var shot in Shots) {
                shot.ClearImage();
            }
            Shots.Clear();
        }
```

**중요:** L51-L56 본문 (foreach + Clear) 은 byte-identical 유지. 위 코드의 시그니처 + 본문은 현재 파일 그대로이며 위에 doc 블록만 추가한다.

***

**File B: WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs**

현재 L57-L62 의 EStep.Init 케이스:

```csharp
        public override ActionContext Run() {
            switch ((EStep)Step) {
                case EStep.Init:
                    ShotParam?.ClearAllResults();
                    Step = (int)EStep.MoveZ;
                    break;
```

L60 (`ShotParam?.ClearAllResults();`) **바로 위**, 즉 L59 와 L60 사이에 다음 마커 주석을 1줄 삽입한다:

```csharp
        public override ActionContext Run() {
            switch ((EStep)Step) {
                case EStep.Init:
                    //260510 hbk Phase 21: BUF-02 channel #2 — sequence reset 트리거 (Run 사이클 진입 시 image buffer + FAI results dispose)
                    ShotParam?.ClearAllResults();
                    Step = (int)EStep.MoveZ;
                    break;
```

**중요:**
- `ShotParam?.ClearAllResults();` 라인 자체는 **byte-identical 보존** — `?.` 연산자 NO 변경 (Phase 20 미접촉 라인, Phase 20 D-12 stacking 회피).
- 들여쓰기는 주변 코드 (case 본문 들여쓰기 = 20 spaces / 5 tabs) 와 일치.
- Phase 20 D-04 LINQ-tail-equivalent 예외 영역 (이벤트 invoke / Linq tail) 에 해당하지 않으므로 신규 코드는 D-01 if/else 정책 대상이지만 **이번 task 는 신규 동작 코드 0** — 주석만 추가하므로 D-01 적용 불요.

***

msbuild Debug/x64 PASS 확인 후 commit 메시지 (단일 commit OR 분리 commit, executor 결정).
  </action>
  <verify>
    <automated>cd /d C:\Info\Project\DataMeasurement &amp;&amp; "%ProgramFiles%\Microsoft Visual Studio\2019\Community\MSBuild\Current\Bin\MSBuild.exe" WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal /nologo</automated>
    grep 검증:
      - `findstr /c:"/// <summary>" WPF_Example\Custom\Sequence\Inspection\InspectionRecipeManager.cs` 결과 — ClearShots 위 신규 doc 블록 인식 (기존 doc 0 → 1+ 증가)
      - `findstr /c:"260510 hbk Phase 21" WPF_Example\Custom\Sequence\Inspection\InspectionRecipeManager.cs` 결과 ≥ 1
      - `findstr /c:"260510 hbk Phase 21" WPF_Example\Custom\Sequence\Inspection\Action_FAIMeasurement.cs` 결과 ≥ 1
      - `findstr /c:"channel #2" WPF_Example\Custom\Sequence\Inspection\Action_FAIMeasurement.cs` 결과 ≥ 1
      - `findstr /c:"ShotParam?.ClearAllResults" WPF_Example\Custom\Sequence\Inspection\Action_FAIMeasurement.cs` 결과 = 1 (라인 byte-identical 보존)
    msbuild output: warning 0 신규
  </verify>
  <done>InspectionRecipeManager.ClearShots XML doc + Action_FAIMeasurement EStep.Init 마커 추가 + msbuild PASS + 동작 라인 byte-identical 보존.</done>
</task>

</tasks>

<threat_model>
**No new attack surface — internal buffer lifecycle documentation only.**
이 plan 은 XML doc + 마커 주석 추가만 수행하며 동작 코드 변경 0. 기존 `_imageLock` thread-safety 보장은 그대로 상속된다. 신규 입력/네트워크/사용자 인터페이스 0. ASVS L1 N/A — 외부 공격 표면 변화 없음.

**고려 사항 (정보 목적):**

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-01 | Information disclosure (doc) | XML doc 본문 | accept | 한국어/영문 doc 텍스트가 source 에 포함되지만 secret/credential 0 — 일반 lifetime 계약 설명만 |
| T-21-01-02 | Race (read-after-dispose) | GetImage / ClearImage 동시 호출 | accept | 기존 `_imageLock` 가 이미 mitigate — 이 plan 은 doc 으로 가시화만 수행 (행위 미변경) |
</threat_model>

<verification>
- msbuild Debug/x64 PASS, 신규 warning 0
- ShotConfig.cs 5개 XML doc + 5개 hbk 마커 추가 (grep ≥ 5)
- InspectionRecipeManager.cs ClearShots 위 XML doc + 1개 hbk 마커
- Action_FAIMeasurement.cs L60 위 hbk 마커 1개 (D-02 channel #2)
- 모든 동작 라인 byte-identical 보존 (`?.` 등 Phase 20 미접촉 operator 그대로)
- K&R brace style 위반 없음
</verification>

<success_criteria>
- AC#3 ("수명 보장 시점 문서화") 의 1차 충족 (Plan 02 가 wire-up 코드 측면 추가 마무리)
- AC#4 의 msbuild PASS 분량 충족
- BUF-02 의 "lifetime 명시적 관리" 의 문서적 측면 100% 충족
- 행위 변경 0 → byte-identical 회귀 (Plan 03 UAT 에서 SIMUL_MODE 1회 검사로 검증)
</success_criteria>

<output>
After completion, create `.planning/phases/21-memory-image-buffer/21-01-SUMMARY.md` 에:
- 추가한 XML doc 블록 6개 (ShotConfig 5 + InspectionRecipeManager 1) 의 위치 (파일:라인) 와 핵심 문구
- 추가한 마커 주석 7개 (ShotConfig 5 + InspectionRecipeManager 1 + Action_FAIMeasurement 1)
- msbuild output 요약 (Errors / Warnings 카운트)
- 변경 라인 수 / 추가 라인 수 / byte-identical 보존된 동작 라인 라인 번호 목록
</output>
</content>
</invoke>