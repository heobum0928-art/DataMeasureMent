---
phase: 21-memory-image-buffer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/SystemHandler.cs
  - WPF_Example/SystemHandler.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs
autonomous: true
requirements:
  - BUF-01
  - BUF-02
tags:
  - lifecycle
  - event-subscriber
  - phase-21
  - buffer

must_haves:
  truths:
    - "Custom/SystemHandler.cs 에 buffer-flush 핸들러 메서드 (예: OnRecipeChanged_FlushBuffers) 가 존재하고 Sequences.RecipeManager.ClearShots() 를 호출한다"
    - "Sequences.OnRecipeChanged 이벤트에 위 핸들러가 +=, -= 페어로 wire-up 된다 (D-02 channel #1)"
    - "WPF_Example/SystemHandler.cs Release() 의 Sequences.Dispose() 직전에 Sequences.RecipeManager.ClearShots() 호출 라인이 존재한다 (D-02 channel #3)"
    - "InspectionRecipeManager.ClearShots() 호출 시 Logging.PrintLog 1줄이 출력된다 (AC#2 dispose 입증 도구 — D-11 권장 ① HImage 인스턴스 카운터 의 logging-기반 등가물)"
  artifacts:
    - path: "WPF_Example/Custom/SystemHandler.cs"
      provides: "WireBufferLifecycle() partial method + OnRecipeChanged_FlushBuffers handler"
      contains: "OnRecipeChanged += "
    - path: "WPF_Example/SystemHandler.cs"
      provides: "Initialize() 에 WireBufferLifecycle() 호출 + Release() 에 ClearShots() 호출 추가"
      contains: "Sequences.RecipeManager.ClearShots()"
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs"
      provides: "ClearShots Logging.PrintLog instrumentation (AC#2 dispose 입증)"
      contains: "Logging.PrintLog"
  key_links:
    - from: "Custom/SystemHandler.cs OnRecipeChanged_FlushBuffers"
      to: "Sequences.RecipeManager.ClearShots()"
      via: "메서드 호출 1줄"
      pattern: "ClearShots\\(\\)"
    - from: "WPF_Example/SystemHandler.cs Initialize()"
      to: "Custom/SystemHandler.cs WireBufferLifecycle()"
      via: "partial method 호출"
      pattern: "WireBufferLifecycle\\(\\)"
    - from: "WPF_Example/SystemHandler.cs Release()"
      to: "Sequences.RecipeManager.ClearShots()"
      via: "Sequences.Dispose() 직전 1줄 추가"
      pattern: "Sequences\\.RecipeManager\\.ClearShots"
---

<objective>
Phase 21 의 D-02 lifetime 3 채널 중 #1 (recipe change) 와 #3 (app shutdown) 의 코드 wire-up 을 수행한다 + AC#2 dispose 입증 도구로 ClearShots 에 Logging.PrintLog 1줄을 추가한다 (D-11 ① "HImage 인스턴스 카운터" 의 logging-기반 등가물).

Purpose:
- D-02 channel #1: 현재 `OnRecipeChanged` 이벤트에 buffer-flush subscriber 가 누락되어 있어 메모리 buffer 가 명시 훅으로 해제되지 않는다 (Load() 내부 ClearShots 의존). Phase 21 의 핵심 신규 동작.
- D-02 channel #3: 현재 `SystemHandler.Release()` 에 ClearShots 호출이 누락되어 있어 앱 종료 시 buffer 가 GC 까지 잔존할 수 있다. 명시 dispose 추가.
- AC#2 입증: ClearShots 가 호출될 때마다 Logging.PrintLog 가 1줄 남기도록 instrument → Plan 03 UAT 에서 recipe load × N회 후 log line 카운트로 dispose 입증.

Output: 3개 파일 wire-up + 1줄 logging — msbuild PASS, SIMUL_MODE 회귀 0 (행위 보존, 새 동작은 추가 dispose call + 1 log line).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-memory-image-buffer/21-CONTEXT.md
@.planning/phases/21-memory-image-buffer/21-PATTERNS.md
@./CLAUDE.md

<interfaces>
<!-- Custom/SystemHandler.cs 는 SystemHandler 의 partial class. 현재 ProcessLightSet/ProcessRecipeChange/MainRun 만 정의. -->
<!-- Initialize/Release 는 framework WPF_Example/SystemHandler.cs 측에 있음. -->

From WPF_Example/SystemHandler.cs Initialize() (L103-146) — 핵심 라인:
```csharp
        public void Initialize() {
            ...                                                     // L103-110
            // 2) Sequence handler
            Sequences = SequenceHandler.Handle;                     // L112
            ...                                                     // L113-127 TCP server, RawImageSaver, system thread, Login
            // 6) Hook sequence creation callbacks
            Sequences.ExecOnCreate();                               // L134
            // 7) Collect recipe list
            Recipes.CollectRecipe();                                // L138
            ...                                                     // L141-144 Localize
            Logging.PrintLog((int)ELogType.Trace, "[SYSTEM] Initialized");  // L145
        }
```

From WPF_Example/SystemHandler.cs Release() (L163-193):
```csharp
        public void Release() {
            Setting.Save();                                         // L165
            Devices.Dispose();                                      // L168
            Sequences.Dispose();                                    // L171  ← 이 직전에 ClearShots 추가
            RawImageSaver?.Dispose();                               // L174
            RawImageSaver = null;                                   // L175
            Server.Dispose();                                       // L178
            Lights.Release();                                       // L181
            IsTerminated = true;                                    // L185
            mSystemThread.Join(1000);                               // L186
            Logging.PrintLog((int)ELogType.Trace, "[SYSTEM] Released");  // L188
            Logging.Stop();                                         // L191
            IsReleased = true;                                      // L192
        }
```

From WPF_Example/Custom/SystemHandler.cs (현재):
```csharp
namespace ReringProject {
    public sealed partial class SystemHandler {                     // L14
        // 현재 ProcessLightSet (L75) / ProcessRecipeChange (L156) / MainRun (L16) 만 정의
        // Initialize / Release 는 framework 쪽 (이 파일에는 override 없음)
    }
}
```

From WPF_Example/MainWindow.xaml.cs (subscriber pattern reference, L82):
```csharp
            mSystemHandler.Sequences.OnRecipeChanged += this.OnLoadRecipe;
```

From WPF_Example/MainWindow.xaml.cs (handler signature, L236):
```csharp
        public void OnLoadRecipe(object sender, RecipeChangedEventArgs args) {
            inspectionList.OnLoadRecipe(args.RecipeName);
        }
```

From WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs (Plan 01 가 doc 추가, 본문 byte-identical):
```csharp
        public void ClearShots() {                                   // L51
            foreach (var shot in Shots) {
                shot.ClearImage();
            }
            Shots.Clear();
        }
```

**Logging API (utility 사용 패턴):**
```csharp
Logging.PrintLog((int)ELogType.Trace, "[InspectionRecipeManager] ClearShots disposed {0} shot buffers", Shots.Count);
```
(SystemHandler.cs L145, L188 와 동일 패턴 — `(int)ELogType.Trace`, format string + args)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WPF_Example/SystemHandler.cs Release() 에 ClearShots 호출 추가 (D-02 channel #3)</name>
  <files>WPF_Example/SystemHandler.cs</files>
  <read_first>
    - WPF_Example/SystemHandler.cs L160-L195 (Release 메서드 전체)
    - .planning/phases/21-memory-image-buffer/21-PATTERNS.md §"WPF_Example/SystemHandler.cs (controller, Release lifecycle)"
    - .planning/phases/21-memory-image-buffer/21-CONTEXT.md §"D-02" channel #3
  </read_first>
  <acceptance_criteria>
    - WPF_Example/SystemHandler.cs Release() 메서드 안에 `Sequences.RecipeManager.ClearShots();` 호출 라인이 **Sequences.Dispose() 직전**에 위치한다
    - 새 라인 끝에 `//260510 hbk Phase 21: BUF-02 channel #3 (app shutdown buffer flush)` 마커 주석이 있다
    - Sequences.Dispose() 라인 자체는 byte-identical 보존
    - msbuild Debug/x64 PASS, warning 0 신규
    - K&R brace style 보존
  </acceptance_criteria>
  <action>
WPF_Example/SystemHandler.cs L170-L171 의 현재 상태:

```csharp
            // Release sequences.
            Sequences.Dispose();
```

**아래 형태로 변경**한다 (`Sequences.Dispose();` 직전에 ClearShots 호출 추가):

```csharp
            //260510 hbk Phase 21: BUF-02 channel #3 (app shutdown buffer flush — Sequences.Dispose 가 ClearShots 를 호출하지 않으므로 명시 dispose)
            Sequences.RecipeManager.ClearShots();
            // Release sequences.
            Sequences.Dispose();
```

**중요:**
- `// Release sequences.` 주석은 보존 (Phase 20 D-08 "why" 주석 유지 정책).
- `Sequences.Dispose();` 라인 byte-identical.
- `Sequences.RecipeManager` 는 `SystemHandler.Handle.Sequences.RecipeManager` 와 동일 (singleton 자기참조 — RecipeManager 는 SequenceHandler.Custom.cs L24 에 public 인스턴스 멤버).
- 새 라인 끝 마커는 EOL 위치 (라인 길이가 길면 line-above 마커로 분리해도 됨 — executor 판단; 단, hbk 마커 1개는 반드시 존재).
- 신규 라인 1개에 if/else 정책 (D-01) 영향 없음 (operator 0).
  </action>
  <verify>
    <automated>cd /d C:\Info\Project\DataMeasurement &amp;&amp; "%ProgramFiles%\Microsoft Visual Studio\2019\Community\MSBuild\Current\Bin\MSBuild.exe" WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal /nologo</automated>
    grep 검증:
      - `findstr /c:"Sequences.RecipeManager.ClearShots" WPF_Example\SystemHandler.cs` 결과 = 1
      - `findstr /c:"260510 hbk Phase 21" WPF_Example\SystemHandler.cs` 결과 ≥ 1
      - `findstr /c:"Sequences.Dispose()" WPF_Example\SystemHandler.cs` 결과 = 1 (보존)
    msbuild: 0 Errors, 0 신규 Warnings
  </verify>
  <done>Release() 에 ClearShots 호출 + 마커 추가, msbuild PASS, 기존 라인 byte-identical 보존.</done>
</task>

<task type="auto">
  <name>Task 2: Custom/SystemHandler.cs 에 buffer-flush subscriber wire-up + WireBufferLifecycle() partial method 신설 (D-02 channel #1, D-03)</name>
  <files>
    WPF_Example/Custom/SystemHandler.cs,
    WPF_Example/SystemHandler.cs
  </files>
  <read_first>
    - WPF_Example/Custom/SystemHandler.cs L1-L50 (using directives + class header + 기존 멤버)
    - WPF_Example/SystemHandler.cs L100-L150 (Initialize 메서드 전체)
    - WPF_Example/MainWindow.xaml.cs L78-L92 (subscriber pattern analog)
    - WPF_Example/MainWindow.xaml.cs L233-L245 (handler signature analog)
    - WPF_Example/Sequence/SequenceHandler.cs L40-L55 (OnRecipeChanged event declaration + RecipeChangedEventArgs)
    - .planning/phases/21-memory-image-buffer/21-PATTERNS.md §"Custom/SystemHandler.cs (controller, event subscriber wire-up)"
    - .planning/phases/21-memory-image-buffer/21-CONTEXT.md §"D-02" channel #1, §"D-03"
  </read_first>
  <acceptance_criteria>
    - WPF_Example/Custom/SystemHandler.cs 에 `private void OnRecipeChanged_FlushBuffers(object sender, RecipeChangedEventArgs args)` 핸들러 메서드 존재 — 본문은 `Sequences.RecipeManager.ClearShots();` 한 줄
    - WPF_Example/Custom/SystemHandler.cs 에 `private void WireBufferLifecycle()` 메서드 존재 — 본문은 `Sequences.OnRecipeChanged += OnRecipeChanged_FlushBuffers;` 한 줄
    - WPF_Example/Custom/SystemHandler.cs 에 `internal void UnwireBufferLifecycle()` 메서드 존재 — 본문은 `Sequences.OnRecipeChanged -= OnRecipeChanged_FlushBuffers;` 한 줄 (subscriber 해제 — D-04 Claude's Discretion: lifecycle 보호)
    - WPF_Example/SystemHandler.cs Initialize() 에서 `Sequences = SequenceHandler.Handle;` (L112) **이후**, `Logging.PrintLog((int)ELogType.Trace, "[SYSTEM] Initialized");` (L145) **이전** 위치에 `WireBufferLifecycle();` 호출 라인 추가
    - WPF_Example/SystemHandler.cs Release() 에서 새로 추가하는 `Sequences.RecipeManager.ClearShots();` 라인 (Task 1 에서 추가) **직전 또는 직후** 에 `UnwireBufferLifecycle();` 호출 라인 추가 (Sequences 가 살아 있는 동안 unsubscribe)
    - 모든 신규 라인에 `//260510 hbk Phase 21` 마커 (라인 끝 또는 라인 위)
    - using directive 추가 불요 (`RecipeChangedEventArgs` 는 `ReringProject.Sequence` 네임스페이스, Custom/SystemHandler.cs 가 이미 L10 에서 `using ReringProject.Sequence;` 보유)
    - msbuild Debug/x64 PASS, warning 0 신규
    - K&R brace style 보존 (Custom/SystemHandler.cs file-wide K&R)
  </acceptance_criteria>
  <action>
**File A: WPF_Example/Custom/SystemHandler.cs**

기존 partial class body 의 적당한 위치 (예: 마지막 메서드 SendTestError L243 뒤, 클래스 닫는 `}` 직전) 에 다음 3개 메서드를 추가한다:

```csharp
        //260510 hbk Phase 21: BUF-02 channel #1 — recipe change buffer flush wire-up (D-02 / D-03)
        private void WireBufferLifecycle() {
            //260510 hbk Phase 21: OnRecipeChanged subscriber 등록 — Sequences 가 SequenceHandler.Handle 로 초기화된 후 호출되어야 함
            Sequences.OnRecipeChanged += OnRecipeChanged_FlushBuffers;
        }

        //260510 hbk Phase 21: BUF-02 channel #1 — Release 시점 unsubscribe (subscriber lifecycle 보호 — D-04 Claude's Discretion)
        internal void UnwireBufferLifecycle() {
            //260510 hbk Phase 21: 멱등 — 미등록 상태에서도 안전 (delegate -= null 무동작)
            Sequences.OnRecipeChanged -= OnRecipeChanged_FlushBuffers;
        }

        //260510 hbk Phase 21: BUF-02 channel #1 — recipe change → InspectionRecipeManager.ClearShots() 전파
        private void OnRecipeChanged_FlushBuffers(object sender, RecipeChangedEventArgs args) {
            //260510 hbk Phase 21: 모든 Shot 의 image buffer dispose — Load() 경로 의존에서 명시 훅으로 승격
            Sequences.RecipeManager.ClearShots();
        }
```

**중요:**
- `RecipeChangedEventArgs` 는 `ReringProject.Sequence` 네임스페이스. Custom/SystemHandler.cs 는 이미 L10 에 `using ReringProject.Sequence;` 가 있으므로 추가 using 불요.
- `Sequences` 는 partial class 의 멤버 (framework 쪽 SystemHandler.cs L43 에 `public SequenceHandler Sequences { get; private set; }` 정의) — 직접 접근 가능.
- `Sequences.OnRecipeChanged` 는 framework `WPF_Example/Sequence/SequenceHandler.cs` 에 `public event` 으로 정의 — `+=` 외부 가능.
- `Sequences.RecipeManager` 는 Custom partial `WPF_Example/Custom/Sequence/SequenceHandler.cs:24` 에 `public InspectionRecipeManager RecipeManager` 인스턴스로 노출.
- 핸들러 본문에 if/else 도 ternary 도 없는 plain 메서드 호출 — Phase 20 D-01 정책 영향 0.
- K&R brace style ({ on same line) 매칭.

***

**File B: WPF_Example/SystemHandler.cs (Initialize + Release 두 위치 수정)**

**B-1) Initialize() (L103-146) 변경:**

L112 의 `Sequences = SequenceHandler.Handle;` 와 L134 의 `Sequences.ExecOnCreate();` 사이가 자연스럽지만, ExecOnCreate 직후 → Recipes.CollectRecipe() 직전이 더 안전 (sequence callbacks 가 모두 hook 된 뒤 buffer wire-up). **추천 위치:** L134 `Sequences.ExecOnCreate();` 직후 (아래 예시 참고). executor 가 단순화 우선 시 L112 직후 (Sequences = ... 다음 줄) 도 허용 — 두 위치 모두 functional 동등.

현재 L132-L139:

```csharp
            // 6) Hook sequence creation callbacks
            //    Typically sets up per-sequence resources.
            Sequences.ExecOnCreate();

            // 7) Collect recipe list
            //    Scans configured recipe directories.
            Recipes.CollectRecipe();
```

**아래 형태로 변경:**

```csharp
            // 6) Hook sequence creation callbacks
            //    Typically sets up per-sequence resources.
            Sequences.ExecOnCreate();

            //260510 hbk Phase 21: BUF-02 channel #1 — OnRecipeChanged subscriber 등록 (Sequences 가 살아있고 ExecOnCreate 가 끝난 뒤 wire)
            WireBufferLifecycle();

            // 7) Collect recipe list
            //    Scans configured recipe directories.
            Recipes.CollectRecipe();
```

**중요:** `WireBufferLifecycle()` 는 partial class 의 private 메서드이므로 framework partial 측에서 호출 가능 (동일 클래스). 신규 partial method 선언은 불요 — 일반 메서드.

***

**B-2) Release() — Task 1 의 ClearShots 호출 라인 옆에 UnwireBufferLifecycle() 호출 추가:**

Task 1 결과 상태 (현재 plan 의 Task 1 적용 후):

```csharp
            //260510 hbk Phase 21: BUF-02 channel #3 (app shutdown buffer flush — Sequences.Dispose 가 ClearShots 를 호출하지 않으므로 명시 dispose)
            Sequences.RecipeManager.ClearShots();
            // Release sequences.
            Sequences.Dispose();
```

**아래 형태로 변경 (`UnwireBufferLifecycle()` 추가):**

```csharp
            //260510 hbk Phase 21: BUF-02 channel #1 — subscriber 해제 (Sequences 가 살아있는 동안 unwire)
            UnwireBufferLifecycle();
            //260510 hbk Phase 21: BUF-02 channel #3 (app shutdown buffer flush — Sequences.Dispose 가 ClearShots 를 호출하지 않으므로 명시 dispose)
            Sequences.RecipeManager.ClearShots();
            // Release sequences.
            Sequences.Dispose();
```

**중요:**
- `UnwireBufferLifecycle()` 는 ClearShots 직전이어야 한다 (event handler 가 더 이상 호출되지 않도록 먼저 unsubscribe → 그 후 명시 dispose → Sequences.Dispose).
- 두 신규 라인 모두 hbk 마커 보유.
- 기존 `// Release sequences.` 주석 + `Sequences.Dispose();` 라인 byte-identical.
  </action>
  <verify>
    <automated>cd /d C:\Info\Project\DataMeasurement &amp;&amp; "%ProgramFiles%\Microsoft Visual Studio\2019\Community\MSBuild\Current\Bin\MSBuild.exe" WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal /nologo</automated>
    grep 검증:
      - `findstr /c:"WireBufferLifecycle" WPF_Example\Custom\SystemHandler.cs` ≥ 2 (선언 + 본문 또는 unwire)
      - `findstr /c:"OnRecipeChanged += " WPF_Example\Custom\SystemHandler.cs` 결과 = 1
      - `findstr /c:"OnRecipeChanged -= " WPF_Example\Custom\SystemHandler.cs` 결과 = 1
      - `findstr /c:"OnRecipeChanged_FlushBuffers" WPF_Example\Custom\SystemHandler.cs` ≥ 3 (선언 1 + +=1 + -=1)
      - `findstr /c:"WireBufferLifecycle()" WPF_Example\SystemHandler.cs` 결과 = 1 (Initialize 호출)
      - `findstr /c:"UnwireBufferLifecycle()" WPF_Example\SystemHandler.cs` 결과 = 1 (Release 호출)
      - `findstr /c:"Sequences.RecipeManager.ClearShots" WPF_Example\SystemHandler.cs` 결과 = 1
      - `findstr /c:"260510 hbk Phase 21" WPF_Example\Custom\SystemHandler.cs` ≥ 5 (3 메서드 헤더 + 핸들러 본문 마커)
      - `findstr /c:"260510 hbk Phase 21" WPF_Example\SystemHandler.cs` ≥ 3 (Initialize wire + Release unwire + Release ClearShots)
    msbuild: 0 Errors, 0 신규 Warnings
  </verify>
  <done>Custom/SystemHandler.cs 3 메서드 추가 + framework SystemHandler.cs Initialize/Release 2 호출 추가 + 마커 ≥ 8 + msbuild PASS.</done>
</task>

<task type="auto">
  <name>Task 3: InspectionRecipeManager.ClearShots 에 Logging instrumentation 추가 (AC#2 dispose 입증 — D-11 ① 등가물)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs L1-L60 (using directives + ClearShots 메서드 + Plan 01 추가 doc 확인)
    - WPF_Example/SystemHandler.cs L80-L90 (Logging.SetLog ELogType.Trace 사용 패턴)
    - .planning/phases/21-memory-image-buffer/21-CONTEXT.md §"D-11" 권장 ① HImage 인스턴스 카운터
  </read_first>
  <acceptance_criteria>
    - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs ClearShots 메서드 본문에 `Logging.PrintLog((int)ELogType.Trace, ...)` 호출 1줄 존재
    - 로그 메시지에 `[InspectionRecipeManager] ClearShots` 식별자 + Shots.Count (dispose 한 buffer 수) 가 format 으로 포함됨
    - Logging 호출은 foreach (var shot in Shots) 루프 **이전** (Shots.Count 가 dispose 전 값을 출력하도록) 또는 **이후 + Shots.Clear() 직전** (executor 결정 — 두 위치 모두 OK; pre-loop 추천) 에 위치
    - using directive 추가 불요 (`Logging`, `ELogType` 모두 `ReringProject.Utility`, `ReringProject.Setting` — 이 파일이 이미 L7 `using ReringProject.Setting;` + L8 `using ReringProject.Utility;` 보유)
    - 신규 라인에 `//260510 hbk Phase 21: BUF-02 dispose 입증 instrumentation` 마커
    - 메서드 시그니처 `public void ClearShots()` byte-identical 보존
    - foreach + Shots.Clear() 본문 byte-identical 보존
    - msbuild Debug/x64 PASS
  </acceptance_criteria>
  <action>
Plan 01 적용 후 InspectionRecipeManager.cs 의 ClearShots 메서드는 다음과 같다:

```csharp
        //260510 hbk Phase 21: BUF-02 lifetime owner — recipe change + app shutdown 채널
        /// <summary>
        /// 모든 Shot 의 image buffer 를 Dispose 하고 Shot 리스트를 비운다.
        /// ... (Plan 01 doc 본문)
        /// </summary>
        public void ClearShots() {
            foreach (var shot in Shots) {
                shot.ClearImage();
            }
            Shots.Clear();
        }
```

**아래 형태로 변경 (foreach 루프 위에 Logging 1줄 추가):**

```csharp
        //260510 hbk Phase 21: BUF-02 lifetime owner — recipe change + app shutdown 채널
        /// <summary>
        /// 모든 Shot 의 image buffer 를 Dispose 하고 Shot 리스트를 비운다.
        /// ... (Plan 01 doc 본문)
        /// </summary>
        public void ClearShots() {
            //260510 hbk Phase 21: BUF-02 dispose 입증 instrumentation — UAT 가 recipe load × N 회 후 이 로그 라인 카운트로 dispose 검증
            Logging.PrintLog((int)ELogType.Trace, "[InspectionRecipeManager] ClearShots disposed {0} shot buffers", Shots.Count);
            foreach (var shot in Shots) {
                shot.ClearImage();
            }
            Shots.Clear();
        }
```

**중요:**
- Logging 호출은 foreach **이전**에 위치 — 그래야 Shots.Count 가 dispose 전 값을 출력 (UAT 검증 시점에 "N개 buffer 가 있었음" 입증).
- format string `"[InspectionRecipeManager] ClearShots disposed {0} shot buffers"` 는 grep 으로 식별 가능한 고유 문자열.
- Plan 03 UAT 가 Trace 로그 파일에서 이 라인을 grep 하여 dispose 호출 횟수 카운트.
- foreach + Shots.Clear() 본문은 byte-identical.
- 추가 using directive 불요 — `Logging` (ReringProject.Utility, L8), `ELogType` (ReringProject.Setting, L7) 모두 이미 in scope.
- 새 라인 1개 — Phase 20 D-01 if/else 정책: operator 0 → 영향 없음.
  </action>
  <verify>
    <automated>cd /d C:\Info\Project\DataMeasurement &amp;&amp; "%ProgramFiles%\Microsoft Visual Studio\2019\Community\MSBuild\Current\Bin\MSBuild.exe" WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal /nologo</automated>
    grep 검증:
      - `findstr /c:"[InspectionRecipeManager] ClearShots disposed" WPF_Example\Custom\Sequence\Inspection\InspectionRecipeManager.cs` 결과 = 1
      - `findstr /c:"Logging.PrintLog" WPF_Example\Custom\Sequence\Inspection\InspectionRecipeManager.cs` ≥ 1 (이전에는 0 — 신규 instrumentation)
      - `findstr /c:"260510 hbk Phase 21: BUF-02 dispose 입증" WPF_Example\Custom\Sequence\Inspection\InspectionRecipeManager.cs` 결과 = 1
      - `findstr /c:"foreach (var shot in Shots)" WPF_Example\Custom\Sequence\Inspection\InspectionRecipeManager.cs` 결과 = 1 (보존)
    msbuild: 0 Errors, 0 신규 Warnings
  </verify>
  <done>ClearShots 에 Logging.PrintLog 1줄 추가 + 마커 + msbuild PASS + 본문 byte-identical (Shots.Count 1줄만 신규).</done>
</task>

</tasks>

<threat_model>
**제한된 신규 attack surface — internal lifecycle wire-up. 외부 입력 0.**

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | Memory leak / DoS | OnRecipeChanged subscriber 가 누락 → 반복 recipe 변경 시 buffer 누적 | mitigate | **이 plan 이 직접 수정** — channel #1 wire-up 추가가 mitigation 자체 |
| T-21-02-02 | Memory leak (subscriber lifecycle) | App shutdown 시 subscriber 미해제 → SequenceHandler 가 살아있으면 핸들러 참조 잔존 | mitigate | UnwireBufferLifecycle() 를 Release() 에서 호출 (singleton lifetime 이므로 실제 leak 위험 낮음 — 방어적 unsubscribe) |
| T-21-02-03 | Race (concurrent ClearShots ↔ Run grab) | Recipe change 중 Action_FAIMeasurement.EStep.Grab 의 ShotParam.SetImage 동시 진행 | accept | 기존 `_imageLock` (ShotConfig.cs L44) 가 mitigate — SetImage / ClearImage 가 동일 lock 보호. ClearShots 는 thread-safe per-Shot. 추가 보호 불요. |
| T-21-02-04 | Information disclosure (logging) | Trace 로그에 `Shots.Count` 수치 노출 | accept | 일반 운영 metric — secret/credential 0. 로그 파일은 local disk only (no network). |
| T-21-02-05 | DoS via log spam | recipe load 빈번 호출 시 로그 1줄 × N | accept | Trace 레벨 + format string 짧음 (1줄/event) — 운영상 문제 없음. UAT 후 instrumentation 제거 옵션은 별도 phase 검토 가능 (현재는 진단 가치 우선). |

**ASVS L1 평가:** 인증/인가/네트워크 입력 변경 0. 내부 lifecycle 만 수정.
</threat_model>

<verification>
- msbuild Debug/x64 PASS, 신규 warning 0
- Custom/SystemHandler.cs: WireBufferLifecycle / UnwireBufferLifecycle / OnRecipeChanged_FlushBuffers 3 메서드 신설
- WPF_Example/SystemHandler.cs Initialize() 에 WireBufferLifecycle() 호출 1라인
- WPF_Example/SystemHandler.cs Release() 에 UnwireBufferLifecycle() + Sequences.RecipeManager.ClearShots() 2라인
- InspectionRecipeManager.cs ClearShots 에 Logging.PrintLog 1라인
- 모든 신규 라인 hbk 마커 보유
- 기존 코드 라인 byte-identical 보존 (Plan 01 doc 추가 라인 포함)
</verification>

<success_criteria>
- AC#2 ("레시피 변경 또는 시퀀스 Reset 이벤트 발생 시 버퍼 내 모든 HImage 가 Dispose 된다") 의 코드 측면 100% 충족 (channel #1 + #3 wire-up 완료, channel #2 는 기존 EStep.Init 경로 + Plan 01 마킹 으로 충족)
- AC#4 ("msbuild Debug/x64 PASS, 신규 warning 0") 충족
- BUF-02 ("이미지 버퍼의 lifetime 을 명시적으로 관리한다") 의 코드 측면 100% 충족
- Plan 03 UAT 가 SIMUL_MODE 1회 검사 + recipe load × 5회 후 trace 로그에서 `[InspectionRecipeManager] ClearShots disposed N shot buffers` 라인을 카운트하여 AC#2 dispose 입증 (D-11 ① 등가물).
</success_criteria>

<output>
After completion, create `.planning/phases/21-memory-image-buffer/21-02-SUMMARY.md` 에:
- Custom/SystemHandler.cs 신설 3 메서드 (시그니처 + 본문 1줄)
- WPF_Example/SystemHandler.cs Initialize/Release 변경 라인 (before/after diff 형식)
- InspectionRecipeManager.ClearShots Logging instrumentation 위치
- 추가된 hbk 마커 카운트 (≥ 8)
- msbuild output 요약
- D-02 3 채널 wire-up 매트릭스 (channel / 위치 / 코드 라인 / 검증)
</output>
</content>
</invoke>