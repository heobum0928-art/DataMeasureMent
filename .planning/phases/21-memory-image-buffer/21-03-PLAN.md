---
phase: 21-memory-image-buffer
plan: 03
type: execute
wave: 2
depends_on:
  - 21-01-PLAN.md
  - 21-02-PLAN.md
files_modified:
  - .planning/phases/21-memory-image-buffer/21-UAT.md
  - .planning/phases/21-memory-image-buffer/21-VERIFICATION.md
autonomous: false
requirements:
  - BUF-01
  - BUF-02
tags:
  - uat
  - verification
  - phase-21
  - signoff

must_haves:
  truths:
    - "21-VERIFICATION.md 가 AC#1 grep audit 결과 (Option A — File. / HImage.ReadImage / BitmapImage 호출 부재) 와 AC#3 (Plan 01 XML doc 6 블록 인용) 와 AC#4 (msbuild PASS) 를 코드 grep 만으로 입증 완료 (status=verified 또는 partial)"
    - "21-UAT.md 가 4개 사용자 실행 테스트 (T1 결과 이미지 표시 / T2 recipe load × 5 dispose log 카운트 / T3 SIMUL 회귀 / T4 msbuild) 를 가지며 사용자 결과 기록"
    - "사용자 sign-off 또는 partial sign-off (gap 명시) 가 21-UAT.md frontmatter 에 기록"
  artifacts:
    - path: ".planning/phases/21-memory-image-buffer/21-VERIFICATION.md"
      provides: "AC#1/AC#3/AC#4 코드 audit + msbuild 결과"
      contains: "AC#1"
    - path: ".planning/phases/21-memory-image-buffer/21-UAT.md"
      provides: "4 테스트 시나리오 + 사용자 결과 + sign-off frontmatter"
      contains: "Test 1"
  key_links:
    - from: "21-VERIFICATION.md AC#1 grep audit"
      to: "MainView.xaml.cs DisplayShotImage / DisplayFAIImage"
      via: "grep 결과 0 disk-API hits"
      pattern: "0 hits"
    - from: "21-UAT.md Test 2 dispose 입증"
      to: "Trace 로그 `[InspectionRecipeManager] ClearShots disposed`"
      via: "recipe load × 5 후 카운트 비교"
      pattern: "ClearShots disposed"
---

<objective>
Phase 21 의 회귀 검증 (AC#4 byte-identical regression) + AC#1 disk-free 표시 입증 (D-08 Option C — grep + SIMUL UAT) + AC#2 dispose 입증 (D-11 ① logging 카운트) 를 수행하고 sign-off 한다.

Purpose: BUF-01 / BUF-02 의 모든 AC 충족 입증, Phase 20 와 동일한 자동 grep 기반 verification + 사용자 SIMUL UAT 패턴 (Phase 28 / Phase 20 sign-off 와 동일 흐름) 적용.
Output: 21-VERIFICATION.md (자동 grep + msbuild) + 21-UAT.md (사용자 SIMUL 테스트 4건) + sign-off frontmatter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-memory-image-buffer/21-CONTEXT.md
@.planning/phases/21-memory-image-buffer/21-PATTERNS.md
@.planning/phases/21-memory-image-buffer/21-01-PLAN.md
@.planning/phases/21-memory-image-buffer/21-02-PLAN.md
@./CLAUDE.md

<verification_targets>
**AC#1 (디스크 비접근 표시):** D-08 Option C — Option A (grep audit) + Option B (SIMUL 1회 검사 시 결과 이미지 클릭 후 디스크 I/O 부재 시각 확인).
- grep 대상: WPF_Example/UI/ContentItem/MainView.xaml.cs 의 DisplayShotImage/DisplayFAIImage 메서드 본문에서 forbidden API 호출 유무. 기대 결과: 0건.
- forbidden API 패턴:
  * `HImage.ReadImage`, `new HImage(<string>)` (path 인자)
  * `File.Open`, `File.OpenRead`, `File.ReadAllBytes`, `FileStream`
  * `BitmapImage.UriSource`, `new BitmapImage(new Uri(`

**AC#2 (dispose 입증):** D-11 ① logging 카운트.
- 로그 파일 위치: `WPF_Example/SystemHandler.cs:79` `Setting.GetLogSavePath(ELogType.Trace)` — 일반적으로 `D:\Trace\YYYY-MM-DD.log` (실제 경로는 SystemSetting.GetLogSavePath 결과).
- 사용자 시나리오: SIMUL_MODE 앱 실행 → recipe N 로드 → recipe M 로드 → recipe N 로드 → recipe M 로드 → recipe N 로드 (총 5회 LoadRecipe).
- 기대 결과: Trace 로그에 `[InspectionRecipeManager] ClearShots disposed {n} shot buffers` 라인이 ≥ 5건.
  - 정확 카운트는 `LoadPhase6Format` 내부 ClearShots (recipe 마다 1) + OnRecipeChanged subscriber ClearShots (recipe 마다 1) 가 둘 다 호출되므로 recipe 1회당 2 라인 → 총 ≥ 10 라인 가능 (멱등 호출, 정상). 사용자 확인 시 "≥ 5" 임계 적용.
- 앱 종료 시 Release() 가 ClearShots 1회 추가 호출 → 종료 후 로그 ≥ +1.

**AC#3 (수명 보장 시점 문서화):** Plan 01 의 XML doc 블록 6개 + 마커 7개 grep.
- 기대 결과:
  * `findstr /c:"/// <summary>" WPF_Example\Custom\Sequence\Inspection\ShotConfig.cs` ≥ 5
  * `findstr /c:"260510 hbk Phase 21" WPF_Example\Custom\Sequence\Inspection\ShotConfig.cs` ≥ 5
  * `findstr /c:"260510 hbk Phase 21" WPF_Example\Custom\Sequence\Inspection\InspectionRecipeManager.cs` ≥ 1
  * `findstr /c:"260510 hbk Phase 21" WPF_Example\Custom\Sequence\Inspection\Action_FAIMeasurement.cs` ≥ 1

**AC#4 (msbuild + SIMUL_MODE 회귀):** D-10 byte-identical 의미 보존 + 신규 warning 0.
- msbuild Debug/x64 PASS — Plan 01/02 누적 결과로 한 번 더 fresh 빌드.
- SIMUL UAT: 사용자 시나리오 1회 (Datum 티칭 + FAI 측정 + 결과 이미지 리뷰 클릭) → Phase 20 sign-off 시점 동작과 동일.
</verification_targets>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 21-VERIFICATION.md 작성 — AC#1 grep audit + AC#3 doc 입증 + AC#4 msbuild + 자동 가능 항목 verified 마킹</name>
  <files>.planning/phases/21-memory-image-buffer/21-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/21-memory-image-buffer/21-CONTEXT.md §"D-08" §"D-09" §"D-10"
    - .planning/phases/21-memory-image-buffer/21-PATTERNS.md §"AC#1 grep audit scope (D-08)"
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L99-L132 (DisplayFAIImage / DisplayShotImage 본문 — disk-free 경로 인용)
    - .planning/phases/20-code-style-cleanup/20-VERIFICATION.md (template/format 참고)
  </read_first>
  <acceptance_criteria>
    - 파일 `.planning/phases/21-memory-image-buffer/21-VERIFICATION.md` 가 존재하고 frontmatter `phase: 21-memory-image-buffer`, `status: verified` (또는 `partial` if SIMUL UAT 미실행 — Plan 03 Task 2 가 처리)
    - AC#1 섹션: grep 명령 + 결과 (forbidden API 0 hits in DisplayShotImage/DisplayFAIImage), MainView.xaml.cs L113-L132 코드 인용
    - AC#3 섹션: Plan 01 의 XML doc 6 블록 + 7 마커 grep 결과 (실제 카운트), 6 doc 블록의 위치 (파일:라인)
    - AC#4 섹션: msbuild Debug/x64 fresh 실행 결과 (Errors/Warnings 카운트, 신규 0 입증)
    - AC#2 섹션: "사용자 SIMUL UAT 가 dispose 입증 (Plan 03 Task 2 → 21-UAT.md Test 2)" 으로 위임 명시 (코드 audit 가능한 부분 — Logging.PrintLog 1줄 grep 입증으로 보강)
    - Date stamp: 2026-05-10 (UTC) 또는 그 이후
  </acceptance_criteria>
  <action>
다음 형식으로 `.planning/phases/21-memory-image-buffer/21-VERIFICATION.md` 를 작성한다:

```markdown
***
phase: 21-memory-image-buffer
verification_date: 2026-05-10
status: verified  # AC#1, AC#3, AC#4 자동 audit 완료. AC#2 는 21-UAT.md Test 2 에 위임.
verified_by: gsd-checker (automated grep + msbuild)
ac_status:
  AC1: verified  # disk-free 표시 경로 — grep 0 hits
  AC2: deferred_to_uat  # dispose 입증 — 21-UAT.md Test 2 사용자 시나리오
  AC3: verified  # XML doc + 마커 grep 통과
  AC4: verified  # msbuild Debug/x64 PASS, warning 0 신규
***

# Phase 21 Verification Report

## AC#1 — 디스크 비접근 표시 경로 (grep audit, D-08 Option A)

**Method:** D-08 Option A (grep audit) — Option B (SIMUL UAT 시각 확인) 는 21-UAT.md Test 1 에 위임 (D-08 Option C 합산).

**Search scope:** `WPF_Example/UI/ContentItem/MainView.xaml.cs` 메서드 `DisplayFAIImage` (L101-L110) / `DisplayShotImage` (L113-L132).

**Forbidden API patterns (CONTEXT.md D-08 + PATTERNS.md §"AC#1 grep audit scope"):**

| Pattern | findstr 명령 | 결과 |
|---------|-------------|------|
| `HImage.ReadImage` | `findstr /n "HImage.ReadImage" WPF_Example\UI\ContentItem\MainView.xaml.cs` | 0 hits ✅ |
| `new HImage(` (path 생성자) | `findstr /n "new HImage(" WPF_Example\UI\ContentItem\MainView.xaml.cs` | 0 hits ✅ |
| `File.` (System.IO.File) | `findstr /n "File\." WPF_Example\UI\ContentItem\MainView.xaml.cs` | (검증 결과 — DisplayShotImage 범위 0; 기타 메서드 hits 가 있어도 AC#1 scope 외) |
| `BitmapImage` | `findstr /n "BitmapImage" WPF_Example\UI\ContentItem\MainView.xaml.cs` | 0 hits ✅ |
| `FileStream` | `findstr /n "FileStream" WPF_Example\UI\ContentItem\MainView.xaml.cs` | 0 hits ✅ |

**확인된 disk-free 경로 (MainView.xaml.cs L113-L132 인용):**

```csharp
private void DisplayShotImage(ShotConfig shot) {
    if (shot != null && shot.HasImage) {
        HImage img = null;
        try {
            img = shot.GetImage();              // ← 메모리 clone (Plan 01 XML doc 가 caller-disposes 명시)
            if (img != null) {
                halconViewer.LoadImage(img);    // ← HImage 인스턴스 직접 로드 (디스크 I/O 0)
                ...
```

**결론:** AC#1 충족 — 결과 이미지 표시 경로에 디스크 API 호출 0건. ✅ verified.

***

## AC#2 — dispose 입증 (위임)

D-11 ① logging 카운트 방식 — 21-UAT.md Test 2 (recipe load × 5 사용자 시나리오) 에 위임.

**보조 코드 입증 (Plan 02 Task 3 instrumentation):**

```bash
findstr /n "[InspectionRecipeManager] ClearShots disposed" WPF_Example\Custom\Sequence\Inspection\InspectionRecipeManager.cs
# 결과: 1 hit (ClearShots 메서드 본문 L? — Plan 02 추가)
```

InspectionRecipeManager.ClearShots() 가 호출될 때마다 Trace 로그에 1줄을 출력하므로, UAT Test 2 의 사용자 recipe load × 5 시나리오에서 로그 라인 카운트로 dispose 검증.

***

## AC#3 — 수명 보장 시점 문서화 (Plan 01 XML doc grep)

**Plan 01 추가 doc 블록 6개 (예상 위치):**

| 파일 | 멤버 | findstr 결과 |
|------|------|--------------|
| ShotConfig.cs | HasImage | `/// <summary>` 직전 + `260510 hbk Phase 21` 마커 |
| ShotConfig.cs | SetImage | `/// <summary>` 직전 + `260510 hbk Phase 21` 마커 |
| ShotConfig.cs | GetImage | `/// <summary>` 직전 + `260510 hbk Phase 21` 마커 + "caller MUST dispose" 또는 "호출자" 문구 |
| ShotConfig.cs | ClearImage | `/// <summary>` 직전 + `260510 hbk Phase 21` 마커 + "channel" + "(1)" + "(2)" + "(3)" 채널 enum |
| ShotConfig.cs | ClearAllResults | `/// <summary>` 직전 + `260510 hbk Phase 21` 마커 + "EStep.Init" 인용 |
| InspectionRecipeManager.cs | ClearShots | `/// <summary>` 직전 + `260510 hbk Phase 21` 마커 |
| Action_FAIMeasurement.cs | EStep.Init L60 | line-above `260510 hbk Phase 21: BUF-02 channel #2` 마커 (XML doc 아닌 inline 마커) |

**자동 카운트 결과:**

| 명령 | 결과 |
|------|------|
| `findstr /c:"/// <summary>" WPF_Example\Custom\Sequence\Inspection\ShotConfig.cs` | ≥ 5 |
| `findstr /c:"260510 hbk Phase 21" WPF_Example\Custom\Sequence\Inspection\ShotConfig.cs` | ≥ 5 |
| `findstr /c:"260510 hbk Phase 21" WPF_Example\Custom\Sequence\Inspection\InspectionRecipeManager.cs` | ≥ 2 (Plan 01 doc + Plan 02 logging instrumentation) |
| `findstr /c:"260510 hbk Phase 21" WPF_Example\Custom\Sequence\Inspection\Action_FAIMeasurement.cs` | ≥ 1 |
| `findstr /c:"260510 hbk Phase 21" WPF_Example\Custom\SystemHandler.cs` | ≥ 5 (Plan 02 — WireBufferLifecycle / UnwireBufferLifecycle / OnRecipeChanged_FlushBuffers + 마커) |
| `findstr /c:"260510 hbk Phase 21" WPF_Example\SystemHandler.cs` | ≥ 3 (Plan 02 — Initialize wire / Release unwire / Release ClearShots) |

**결론:** AC#3 충족 — 6개 멤버 + 1개 sequence-step 위치 모두 코드 인접 문서/마커 보유. ✅ verified.

***

## AC#4 — msbuild Debug/x64 PASS (D-10)

**Command:** `MSBuild.exe WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal /nologo`

**Result:**
- Errors: 0
- Warnings: 0 신규 (Plan 01 + Plan 02 누적 후 최종 빌드)
- Output: `bin\x64\Debug\DatumMeasurement.exe` 생성

**SIMUL_MODE 회귀:** 21-UAT.md Test 3 (사용자 SIMUL 1회 검사) 에 위임.

**결론:** AC#4 자동 부분 충족. SIMUL 회귀는 UAT 에 위임. ✅ verified (build 부분).

***

## 종합

| AC | 자동 입증 | UAT 입증 | 종합 |
|----|----------|---------|------|
| AC#1 | ✅ grep 0 hits | (Test 1 보강) | verified |
| AC#2 | (instrumentation grep) | Test 2 카운트 | deferred_to_uat |
| AC#3 | ✅ doc/마커 grep | — | verified |
| AC#4 | ✅ msbuild PASS | Test 3 SIMUL | partial (build verified, regression to UAT) |

Phase 21 sign-off 는 21-UAT.md 의 사용자 결과 (Test 1-4) 를 받은 후 최종 결정.
```

**중요:**
- 실제 grep 결과 카운트는 executor 가 Plan 01/02 결과 적용 후 실측값으로 채운다.
- forbidden API table 의 결과 셀에 실 카운트를 기입.
- File.Open / FileStream 의 경우 — MainView.xaml.cs 에 다른 메서드 (예: 결과 이미지 export) 에서 사용될 수 있으나 **DisplayShotImage / DisplayFAIImage 메서드 본문 내** 0 hits 임을 확인하면 충분 (AC#1 scope 한정).
- 한국어/영문 혼용 OK — 도메인 용어 일관 유지.
  </action>
  <verify>
    <automated>powershell -Command "if (Test-Path '.planning\phases\21-memory-image-buffer\21-VERIFICATION.md') { Write-Host 'EXISTS' } else { Write-Host 'MISSING'; exit 1 }"</automated>
    수동 grep 검증 (executor 가 작성 후):
      - 파일 frontmatter 에 `phase: 21-memory-image-buffer`, `status: verified` 또는 `partial` 포함
      - "AC#1", "AC#2", "AC#3", "AC#4" 4개 섹션 헤더 존재
      - msbuild output 카운트 (Errors=0, Warnings=0 신규) 기록
  </verify>
  <done>21-VERIFICATION.md 작성 완료, 4 AC 섹션 + 자동 grep 결과 + msbuild 결과 기록.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 21-UAT.md SIMUL_MODE 사용자 테스트 4건 (T1 결과 이미지 / T2 dispose 카운트 / T3 SIMUL 회귀 / T4 msbuild)</name>
  <files>.planning/phases/21-memory-image-buffer/21-UAT.md</files>
  <read_first>
    - .planning/phases/21-memory-image-buffer/21-VERIFICATION.md (Task 1 결과)
    - .planning/phases/21-memory-image-buffer/21-CONTEXT.md §"D-08" Option B + §"D-10" + §"D-11"
    - WPF_Example/Setting/SystemSetting.cs (GetLogSavePath 메서드 — Trace 로그 파일 위치 확인)
  </read_first>
  <acceptance_criteria>
    - 파일 `.planning/phases/21-memory-image-buffer/21-UAT.md` 가 존재
    - frontmatter: `phase: 21-memory-image-buffer`, `status: signed_off` (사용자 4/4 PASS) 또는 `partial` (gap 명시)
    - Test 1 (AC#1 보강 — 결과 이미지 표시 SIMUL 시각 확인) — 사용자 result 기록
    - Test 2 (AC#2 — recipe load × 5 후 ClearShots 로그 카운트) — 사용자 result + 로그 카운트 수치 기록
    - Test 3 (AC#4 SIMUL 회귀 — Datum 티칭 + FAI 측정 1회) — 사용자 result 기록
    - Test 4 (AC#4 msbuild) — 자동 PASS 인용 (사용자가 result: PASS 마킹)
    - Summary 섹션: total/passed/failed/pending 카운트
    - 사용자 sign-off 날짜 기록
  </acceptance_criteria>
  <what-built>
    Phase 21 의 lifetime 계약 코드 (Plan 01 doc + Plan 02 wire-up + logging instrumentation) 가 빌드 + 자동 grep audit 통과 완료. 이제 사용자가 SIMUL_MODE 에서 다음 4 테스트를 수행하여 Phase 21 종료 판정한다.
  </what-built>
  <how-to-verify>
**Test 1 — AC#1 결과 이미지 표시 (디스크 비접근, D-08 Option B 시각 확인)**
1. `bin\x64\Debug\DatumMeasurement.exe` 실행 (SIMUL_MODE).
2. 임의 recipe 로드 → Inspection Sequence 1회 실행 (FAI 검사 완료까지).
3. InspectionListView 에서 임의 FAI 노드 클릭.
4. **기대 결과:** MainView 의 halconViewer 에 마지막 Shot 이미지가 표시된다 — 디스크 접근 지연 없이 즉시 (메모리 경로).
5. (선택) Process Monitor 를 띄워 클릭 시점 file I/O 이벤트 확인 — recipe 디렉토리 / 이미지 디렉토리 read 이벤트 0.
6. **기록:** result: PASS / FAIL, 비고 (지연 체감 / 모달 누락 등).

**Test 2 — AC#2 dispose 입증 (D-11 ① logging 카운트)**
1. SIMUL_MODE 앱 실행 직전 Trace 로그 파일 위치 확인 (`SystemSetting.GetLogSavePath(ELogType.Trace)` 결과 — 일반적으로 `D:\Trace\` 또는 설정에 따른 경로).
2. 앱 실행 → 빈 로그에서 시작 (또는 시작 라인 기록).
3. Recipe A 로드 → Recipe B 로드 → Recipe A 로드 → Recipe B 로드 → Recipe A 로드 (총 5회 LoadRecipe).
4. 앱 종료 (X 버튼).
5. Trace 로그에서 다음 명령 실행:
   ```
   findstr /c:"[InspectionRecipeManager] ClearShots disposed" <Trace 로그 파일 경로>
   ```
6. **기대 결과:** 매 LoadRecipe 마다 ClearShots 가 2회 (subscriber + LoadPhase6Format 내부) + Release() 1회 → 총 ≥ 11 hits (5×2 + 1). 임계 = 5 hits 이상이면 PASS (보수적).
7. **기록:** result: PASS / FAIL + 실제 hit 카운트 + 로그 마지막 1줄 인용.

**Test 3 — AC#4 SIMUL_MODE 1회 검사 회귀 (D-10 byte-identical)**
1. SIMUL_MODE 앱 실행.
2. Datum 티칭: btn_teachDatum 클릭 → 티칭 ROI 그리기 → Datum 검출 성공 확인.
3. Inspection Sequence 1회 실행 (외부 TCP 또는 UI 트리거).
4. **기대 결과:** Phase 20 sign-off 시점 동작과 동일 — Datum 검출 PASS, FAI 측정 N건 PASS (또는 동일 분포 NG), 결과 이미지 정상 표시.
5. **기록:** result: PASS / FAIL, 비고 (이전 대비 차이 — 없으면 byte-identical OK).

**Test 4 — AC#4 msbuild Debug/x64 PASS**
1. 21-VERIFICATION.md (Task 1) 의 msbuild 결과 인용 — 자동 PASS.
2. 사용자가 추가로 fresh `clean + rebuild` 실행해도 됨 (선택).
3. **기록:** result: PASS (자동 검증 인용).
  </how-to-verify>
  <resume-signal>
    사용자가 4 테스트 결과를 21-UAT.md 에 기록 + frontmatter status 를 `signed_off` (4/4 PASS) 또는 `partial` (gap 명시) 로 마킹 후 "approved" 또는 "Phase 21 sign-off" 로 응답.
  </resume-signal>

**21-UAT.md 형식 (executor 가 Test 결과 빈칸으로 작성, 사용자가 채움):**

```markdown
***
phase: 21-memory-image-buffer
uat_date: 2026-05-10
status: pending  # 사용자 PASS 후 signed_off 로 변경
signed_off_by: heobum0928@gmail.com
ac_coverage: [AC1, AC2, AC4]  # AC3 는 21-VERIFICATION.md 자동 audit 으로 충족
***

# Phase 21 UAT (User Acceptance Test)

## Test 1 — AC#1 결과 이미지 표시 (디스크 비접근)

**Steps:** (위 how-to-verify Test 1 1-5 단계 인용)

**Expected:** halconViewer 에 마지막 Shot 이미지 즉시 표시 (디스크 I/O 0).

**Result:** _____ (PASS / FAIL — 사용자 기입)
**Notes:** _____ (사용자 기입)

## Test 2 — AC#2 dispose 입증 (recipe load × 5 + ClearShots 로그 카운트)

**Steps:** (위 how-to-verify Test 2 1-5 단계 인용)

**Expected:** Trace 로그 `[InspectionRecipeManager] ClearShots disposed N shot buffers` ≥ 5 hits.

**Result:** _____ (PASS / FAIL)
**Hit count:** _____ (실측치)
**Last log line:** _____

## Test 3 — AC#4 SIMUL_MODE 회귀 (1회 검사)

**Steps:** (위 how-to-verify Test 3 1-3 단계 인용)

**Expected:** Phase 20 sign-off 시점과 동일 동작 (byte-identical).

**Result:** _____ (PASS / FAIL)
**Notes:** _____

## Test 4 — AC#4 msbuild

**Steps:** 21-VERIFICATION.md AC#4 섹션 인용 (자동 PASS).

**Expected:** Errors=0, Warnings 신규=0.

**Result:** PASS (자동 검증 인용)

## Summary

- Total: 4
- Passed: ___
- Failed: ___
- Pending: ___

Sign-off: _____ (사용자 서명 + 날짜)
```

**Executor 가 위 템플릿으로 21-UAT.md 를 작성한 후, 체크포인트로 사용자에게 SIMUL UAT 수행 요청 → 사용자가 결과 기입 후 "approved" 응답 → executor 가 frontmatter status 를 `signed_off` 또는 `partial` 로 갱신.**
</task>

</tasks>

<threat_model>
**No new attack surface — UAT/verification documentation only. 코드 변경 0.**

이 plan 은 .planning/ 디렉토리 내 .md 파일 2개만 작성하며 production 코드 미변경. ASVS L1 N/A.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | False sign-off | UAT 사용자가 PASS 표시했지만 실제 dispose 미수행 | mitigate | Test 2 의 로그 카운트 실측 수치 기록 의무화 — 자동 grep 으로 재검증 가능 |
| T-21-03-02 | Verification scope creep | grep audit 결과 0 hits 가 잘못된 경로를 검사 | accept | scope 가 MainView.xaml.cs DisplayShotImage/DisplayFAIImage 로 명시 — PATTERNS.md §"AC#1 grep audit scope" 와 1:1 매핑 |
</threat_model>

<verification>
- 21-VERIFICATION.md 4 AC 섹션 + frontmatter status 작성 완료
- 21-UAT.md 4 테스트 + Summary + 사용자 sign-off frontmatter 작성 완료
- 사용자 SIMUL UAT 4 테스트 PASS (또는 partial — 명시 gap)
- Phase 21 sign-off 또는 carry-over gap 식별
</verification>

<success_criteria>
- AC#1 100% (grep + 시각 확인)
- AC#2 100% (logging 카운트 ≥ 5)
- AC#3 100% (doc/마커 grep)
- AC#4 100% (msbuild PASS + SIMUL 회귀)
- 사용자 sign-off 또는 명시 gap 기록 (Phase 28 / Phase 20 sign-off 와 동일 흐름)
- ROADMAP.md Phase 21 행 업데이트 (사용자 또는 후속 step) — `[x]` 로 마킹
</success_criteria>

<output>
After completion, create `.planning/phases/21-memory-image-buffer/21-03-SUMMARY.md` 에:
- 21-VERIFICATION.md 자동 결과 (4 AC 카운트)
- 21-UAT.md 사용자 결과 (4 테스트 PASS/FAIL 매트릭스)
- 최종 sign-off status (signed_off / partial)
- 미해결 gap (있을 경우 — Phase 22 carry-over 후보)
- Plan 01 + Plan 02 + Plan 03 통합 변경 라인 카운트
</output>
</content>
</invoke>