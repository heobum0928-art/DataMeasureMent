---
phase: 22-image-dual-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
autonomous: true
requirements:
  - IMG-01
must_haves:
  truths:
    - "DatumConfig 인스턴스에 TeachingImagePath 라는 public string 속성이 존재한다 (기본값 빈 문자열)"
    - "TeachingImagePath 는 PropertyGrid 의 Datum|ImageSource 카테고리에 자동 노출된다"
    - "INI 저장 시 [FIXTURE_DATUM_N] 섹션에 TeachingImagePath= 키가 자동 기록된다 (ParamBase reflection)"
    - "INI 로드 시 TeachingImagePath 키 미존재 → 빈 문자열로 안전 폴백 (EnsurePerRoiDefaults 가드)"
    - "msbuild Debug/x64 PASS, 신규 warning 0"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "TeachingImagePath 속성 선언 + EnsurePerRoiDefaults null 가드"
      contains: "public string TeachingImagePath"
  key_links:
    - from: "DatumConfig.TeachingImagePath"
      to: "ParamBase.Save/Load (String case)"
      via: "Reflection 자동 직렬화 — 별도 caller 변경 0"
      pattern: "case \"String\""
    - from: "DatumConfig.EnsurePerRoiDefaults()"
      to: "TeachingImagePath null → \"\" 정규화"
      via: "L514 Circle_RadialDirection 패턴과 동일 위치"
      pattern: "if \\(TeachingImagePath == null\\) TeachingImagePath = \"\";"
---

<objective>
DatumConfig 에 `TeachingImagePath` 속성을 추가하여 Datum 티칭 시 사용한 기준 이미지 경로를 INI 에 영구 보존한다.
ParamBase 의 reflection 직렬화 경로(Save/Load)가 신규 public string 속성을 자동 처리하므로 INI Save/Load 코드는 0 줄 변경한다.
EnsurePerRoiDefaults 에 null 가드 한 줄을 추가하여 기존 INI(키 없음) 로드 시 회귀 0 을 보장한다.

Purpose:
- Phase 22 IMG-01 충족 — TeachingImagePath 필드 + INI 직렬화/역직렬화
- Phase 23 (A시리즈 Simul) 에서 재티칭 기준 이미지 참조 가능
- ShotConfig.SimulImagePath (검사 이미지) 와 코드 레벨에서 역할 분리

Output:
- `DatumConfig.cs` 수정 (2 위치: 속성 선언 + EnsurePerRoiDefaults 가드)
- INI 저장 시 `[FIXTURE_DATUM_N]` 섹션에 `TeachingImagePath=` 키 자동 등장
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-image-dual-structure/22-CONTEXT.md
@.planning/phases/22-image-dual-structure/22-PATTERNS.md

# 디자인 결정 lock-in (planner level)
#
# 1. InspectionImagePath 전략 = (b) 의미적 재해석
#    기존 `ShotConfig.SimulImagePath` 를 그대로 "InspectionImagePath" 역할로 사용한다.
#    이유: zero-regression. INI 키 그대로 유지, Action_FAIMeasurement L111/L226 코드 변경 0.
#    Phase 22 의 "역할 분리" 는 **DatumConfig.TeachingImagePath (티칭) vs ShotConfig.SimulImagePath (검사)**
#    의 명확한 별개 필드로 코드 레벨에서 달성한다.
#    Plan 02 에서 의미 명확화 주석 + UAT 시나리오로 분리를 검증한다.
#
# 2. UI 범위 = PropertyGrid 자동 노출만 (XAML 변경 0)
#    [Category("Datum|ImageSource")] 부착으로 기존 ImageSourceMode/SourceShotName 그룹에
#    자동 합류. Browse 버튼/Open File Dialog 같은 풍부한 UI 는 Phase 22 범위 밖.

@WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
@WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
@WPF_Example/Sequence/Param/ParamBase.cs

<interfaces>
<!-- DatumConfig 의 인접 ImageSource 카테고리 필드들 (참조용 — 동일 패턴 적용) -->

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (L21-31):
```csharp
//260413 hbk Phase 6: 이미지 소스 모드 — "Dedicated" 또는 "ReuseFromShot" (D-07, D-08)
[Category("Datum|ImageSource")]
public string ImageSourceMode { get; set; } = "Dedicated";

//260413 hbk Phase 6: ReuseFromShot 모드일 때 재사용할 Shot 이름 (D-07)
[Category("Datum|ImageSource")]
public string ReuseFromShotName { get; set; } = "";

//260423 hbk Phase 11 D-08 — 카메라/Z/조명을 상속할 Shot 이름 (빈 문자열이면 Sequence 첫 Shot fallback)
[Category("Datum|ImageSource")]
public string SourceShotName { get; set; } = "";
```

From WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs (L15-16) — analog:
```csharp
[Category("Shot|Simulation")]
public string SimulImagePath { get; set; } = "";
```

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (L514) — EnsurePerRoiDefaults null 가드 패턴:
```csharp
if (string.IsNullOrEmpty(Circle_RadialDirection)) Circle_RadialDirection = "Inward"; //260503 hbk Phase 17 D-02 — sentinel "" → "Inward" fallback (INI 하위호환)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DatumConfig 에 TeachingImagePath 속성 선언</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs L1-32 (file header + Datum|ImageSource 카테고리 기존 필드 패턴 = ImageSourceMode L23, ReuseFromShotName L27, SourceShotName L31)
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs L15-16 (analog: `[Category("Shot|Simulation")] public string SimulImagePath { get; set; } = "";`)
    - .planning/phases/22-image-dual-structure/22-PATTERNS.md L40-75 (Field declaration pattern + Why this works)
  </read_first>
  <behavior>
    - 신규 public auto-property `TeachingImagePath` 가 DatumConfig 인스턴스에 존재한다 (PropertyType.Name == "String")
    - 기본값은 빈 문자열 ("") — null 이 아니어야 함 (ParamBase.Save 의 String case L337 가 null 을 (string)cast → SetKey 시 빈 문자열 같은 표현으로 저장되지만 명시적 "" 가 안전)
    - PropertyGrid 노출 시 `Datum|ImageSource` 카테고리 하에 ImageSourceMode/ReuseFromShotName/SourceShotName 와 함께 그룹화된다
    - DatumConfig 인스턴스를 INI 에 Save → `[group]` 섹션에 `TeachingImagePath=` 키가 자동 등장 (ParamBase reflection — 별도 코드 0)
  </behavior>
  <action>
    DatumConfig.cs L31 (SourceShotName 선언 직후) 아래에 **다음 4 줄을 그대로 삽입**한다.
    삽입 위치 = `public string SourceShotName { get; set; } = "";` 라인 바로 다음 빈 줄 + 신규 블록:

    ```csharp
    //260511 hbk Phase 22 IMG-01 — Datum 티칭 시 사용한 기준 이미지 경로. INI 직렬화는 ParamBase reflection 이 자동 처리 (ParamBase.cs L325-339 Save 의 case "String", L385-395 Load 의 case "String"). 검사 실행 시 이미지는 별도 ShotConfig.SimulImagePath 사용 — 역할 분리 유지. 키 미존재 → EnsurePerRoiDefaults 에서 "" 정규화 (T2).
    [Category("Datum|ImageSource")]
    public string TeachingImagePath { get; set; } = "";
    ```

    제약:
    - 들여쓰기는 인접 기존 필드와 정확히 동일하게 (8 spaces — DatumConfig 클래스 본체)
    - `[Category("Datum|ImageSource")]` 의 대괄호 안 문자열은 분리자 `|` 까지 byte-identical 로 ImageSourceMode (L22) 의 것과 일치해야 한다
    - 신규 using 추가 금지 (PropertyTools.DataAnnotations 는 L4 에 이미 import 됨)
    - 다른 필드 수정/삭제 금지 — 순수 4 줄 삽입만
    - 헝가리안 표기법 적용 금지 (QUAL-01 = Phase 26 일괄 처리 — Phase 22 범위 밖)
  </action>
  <acceptance_criteria>
    - `grep -n "public string TeachingImagePath" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 가 정확히 1 라인 매치
    - `grep -n "260511 hbk Phase 22 IMG-01" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 가 ≥ 1 라인 매치 (T2 에서 추가 마커 발생 가능)
    - 신규 선언 라인이 `[Category("Datum|ImageSource")]` 직후에 위치 (`grep -B1 "public string TeachingImagePath"` 결과 첫 행이 `[Category("Datum|ImageSource")]`)
    - 신규 라인의 기본값은 `= ""` 로 끝남 (정확한 문자열 매치: `public string TeachingImagePath { get; set; } = "";`)
    - 파일 전체 라인 수가 baseline 대비 +4 (3 코드 라인 + 1 신규 빈 줄 또는 +4 신규 라인 — 인접 패턴과 일치)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "public string TeachingImagePath { get; set; } = \"\";" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</automated>
  </verify>
  <done>
    - DatumConfig.cs 에 TeachingImagePath public string 속성이 빈 문자열 기본값으로 선언됨
    - Datum|ImageSource 카테고리 부착으로 PropertyGrid 자동 노출 준비 완료
    - 신규 //260511 hbk Phase 22 IMG-01 주석 마커 부착됨
    - 기존 필드/주석 무수정
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: EnsurePerRoiDefaults 에 TeachingImagePath null 가드 추가</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs L472-530 (EnsurePerRoiDefaults 본문 — 특히 L514 `Circle_RadialDirection` sentinel "" 가드 패턴이 동일 위치 analog)
    - WPF_Example/Sequence/Param/ParamBase.cs L385-395 (case "String" 로드 시 null SetValue 가능성 — IniValue.Default.ToString() → null 반환 경로)
    - WPF_Example/Utility/Ini.cs L312-317 (IniValue.ToString defaultStr=null → Value=null 시 null 반환)
    - .planning/phases/22-image-dual-structure/22-PATTERNS.md L137-178 (Analog 1~4: INI 키 미존재 폴백 — 선택지 B "EnsureDefaults" 한 줄 추가)
  </read_first>
  <behavior>
    - DatumConfig.EnsurePerRoiDefaults() 호출 후 TeachingImagePath 가 null 이 아니다 (보장: 빈 문자열 "")
    - 기존 INI (TeachingImagePath 키 없음) 로드 → EnsurePerRoiDefaults() 호출 → TeachingImagePath == ""
    - 이미 값이 설정된 경우 (사용자가 셋업한 경로) 변경되지 않음 — 멱등성 (Phase 17 D-02 패턴)
    - 다른 per-ROI 기본값 로직 (Line1/Line2/Circle/Horizontal_A/B/Vertical) 영향 0
  </behavior>
  <action>
    DatumConfig.cs L514 (`if (string.IsNullOrEmpty(Circle_RadialDirection)) Circle_RadialDirection = "Inward"; //260503 hbk Phase 17 D-02 ...`) 직후에 **다음 1 줄을 정확히 삽입**한다:

    ```csharp
    if (TeachingImagePath == null) TeachingImagePath = ""; //260511 hbk Phase 22 IMG-01 — INI 키 미존재 시 null 가드 (ParamBase.Load String case 가 IniValue.Default → null 로 SetValue 가능 → 소비처 string.IsNullOrEmpty 가드 보완). 멱등성 보장 — 빈 문자열 아닌 사용자 셋업 값은 보존.
    ```

    제약:
    - 들여쓰기는 L514 (`Circle_RadialDirection` 라인) 과 byte-identical 동일 (12 spaces — EnsurePerRoiDefaults 메서드 본체 들여쓰기)
    - L514 의 `Circle_RadialDirection` 가드 형식 (`if (string.IsNullOrEmpty(X)) X = Y; //주석`) 을 따르되, TeachingImagePath 는 빈 문자열도 유효값 (사용자가 의도적으로 클리어한 상태) 이므로 `string.IsNullOrEmpty` 가 아닌 **`== null`** 만 비교 — 빈 문자열은 그대로 두어야 한다
    - 다른 가드 라인 수정 금지
    - 신규 헬퍼 메서드/필드 도입 금지
    - 헝가리안 표기 변경 금지 (Phase 26 범위)
  </action>
  <acceptance_criteria>
    - `grep -n "if (TeachingImagePath == null) TeachingImagePath = \"\";" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 가 정확히 1 라인 매치
    - 해당 라인이 L515 (또는 Circle_RadialDirection 가드 라인 +1) 근방 — `grep -A1 "Circle_RadialDirection = \"Inward\"" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 결과 두 번째 행에 신규 가드가 등장
    - `grep -c "260511 hbk Phase 22 IMG-01" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 가 정확히 2 (T1 의 속성 선언 + T2 의 null 가드)
    - EnsurePerRoiDefaults 의 기존 라인 (L489-523) 무수정 — `grep -c "fbThreshold" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` baseline 동일
  </acceptance_criteria>
  <verify>
    <automated>grep -c "if (TeachingImagePath == null) TeachingImagePath = \"\";" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</automated>
  </verify>
  <done>
    - EnsurePerRoiDefaults() 마지막 (Circle_RadialDirection 가드 직후) 에 TeachingImagePath null 가드 1 줄 추가
    - INI 키 미존재 케이스 → "" 정규화 보장 (success criterion #4 충족)
    - 멱등성 보존 — 비-null 값은 변경 0
    - 신규 //260511 hbk Phase 22 IMG-01 주석 마커 부착
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INI 파일 → DatumConfig 인메모리 | 디스크 INI 의 TeachingImagePath 문자열이 DatumConfig 인스턴스로 역직렬화 |
| DatumConfig.TeachingImagePath → (현재) 미소비 | Plan 22-01 에서는 신규 속성만 추가 — 실 소비는 Phase 23/UI 측 작업 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | INI 파일의 TeachingImagePath 값 | accept | 로컬 디스크 파일, 외부 네트워크 노출 0. ASVS L1 범위 외. |
| T-22-02 | Information Disclosure | TeachingImagePath 에 파일 시스템 경로 노출 | accept | 산업 PC 단독 사용자, PII 미포함, low-value target. |
| T-22-03 | Denial of Service | null/빈 문자열 폴백 누락 시 NullReferenceException | mitigate | Task 2 의 EnsurePerRoiDefaults null 가드 + (Plan 02) 소비처 `string.IsNullOrEmpty` 가드. |
</threat_model>

<verification>
1. msbuild Debug/x64 PASS (Plan 22-02 의 build verification 태스크에서 종합 확인)
2. 신규 warning 0
3. `grep "public string TeachingImagePath" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 매치 1
4. `grep "TeachingImagePath == null" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 매치 1
5. Plan 22-02 Task 4 의 UAT 시나리오 1 (TeachingImagePath INI 라운드트립) PASS
</verification>

<success_criteria>
- DatumConfig.cs 에 TeachingImagePath public string 속성 존재 (기본값 "")
- EnsurePerRoiDefaults() 에 TeachingImagePath null → "" 정규화 가드 존재
- 양 변경 라인에 //260511 hbk Phase 22 IMG-01 주석 마커 부착
- ParamBase reflection 으로 INI 자동 직렬화/역직렬화 (별도 caller 코드 변경 0 — InspectionRecipeManager L117-119/L188-193 무수정)
- PropertyGrid 의 Datum|ImageSource 카테고리에 자동 노출
</success_criteria>

<output>
After completion, create `.planning/phases/22-image-dual-structure/22-01-SUMMARY.md` documenting:
- 정확한 삽입 위치 (DatumConfig.cs L31 직후 / L514 직후 라인 번호)
- ParamBase reflection 자동 직렬화 검증 방법 (테스트 INI 라운드트립 — Plan 02 UAT)
- 디자인 결정 lock-in 결과 (전략 (b) — InspectionImagePath = ShotConfig.SimulImagePath 의미적 재해석, ShotConfig 무수정)
- TeachingImagePath 의 실 소비 시점 = Phase 23 (재티칭 기준 이미지 참조) 또는 후속 UI 작업 (carry-over)
</output>
</content>
</invoke>