---
phase: 22-image-dual-structure
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - .planning/phases/22-image-dual-structure/22-UAT.md
autonomous: false
requirements:
  - IMG-02
must_haves:
  truths:
    - "Simul 모드 검사 실행 시 Datum 찾기 경로(GrabOrLoadDatumImage)와 FAI 측정 경로(EStep.Grab) 양쪽이 ShotConfig.SimulImagePath = InspectionImagePath 역할로 명시적으로 문서화되어 있다"
    - "TeachingImagePath 와 ShotConfig.SimulImagePath 가 같은 파일을 가리켜도 Datum 찾기 + FAI 측정이 회귀 0 으로 동작한다 (Simul UAT)"
    - "TeachingImagePath INI 키 미존재 케이스에서 NullReferenceException 0, 기존 시퀀스 실행 회귀 0"
    - "msbuild Debug/x64 PASS, 신규 warning 0"
    - "22-UAT.md 가 작성되고 사용자 검증 sign-off 가능한 상태로 phase 디렉토리에 존재"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "Simul 이미지 로드 경로의 InspectionImagePath 역할 명시화 주석 + DatumImage = InspectionImagePath 로드 + (선택) TeachingImagePath 로깅"
      contains: "InspectionImagePath"
    - path: ".planning/phases/22-image-dual-structure/22-UAT.md"
      provides: "Simul 모드 검증 시나리오 4개 (SC#1~#5)"
      contains: "Test 1"
  key_links:
    - from: "Action_FAIMeasurement.GrabOrLoadDatumImage"
      to: "ShotParam.SimulImagePath (= InspectionImagePath 역할)"
      via: "SIMUL_MODE 분기 파일 로드 (L226)"
      pattern: "ShotParam.SimulImagePath.*InspectionImagePath"
    - from: "Action_FAIMeasurement EStep.Grab"
      to: "ShotParam.SimulImagePath (= InspectionImagePath 역할)"
      via: "SIMUL_MODE 분기 파일 로드 (L111)"
      pattern: "ShotParam.SimulImagePath.*InspectionImagePath"
    - from: "22-UAT.md"
      to: "Phase 22 sign-off"
      via: "사용자 4 scenarios PASS 후 STATE.md status=phase_complete 갱신"
      pattern: "result: PASS"
---

<objective>
Plan 22-01 에서 추가한 `DatumConfig.TeachingImagePath` 와 기존 `ShotConfig.SimulImagePath` 의 **역할 분리를 코드 레벨에서 명문화** 하고, Simul 검사 경로가 항상 `InspectionImagePath` (= `ShotConfig.SimulImagePath`) 로 해석됨을 주석으로 못박는다.

핵심 디자인 (Plan 22-01 lock-in 재확인):
- **티칭 이미지** = `DatumConfig.TeachingImagePath` (INI 보존, 셋업 시 1회)
- **검사 이미지** = `ShotConfig.SimulImagePath` (의미상 InspectionImagePath — 매 사이클)
- 코드 변경 최소화: ShotConfig 필드 무수정, Action_FAIMeasurement 의 SIMUL 분기 무수정 (회귀 0).
  대신 두 SIMUL 로드 사이트 (L111, L226) 에 **InspectionImagePath 역할 명시 주석** 한 줄씩 추가.

Purpose:
- Phase 22 success criterion #2 (검사 실행 경로의 분리) 코드 레벨 충족
- Phase 22 success criterion #3 (두 경로 동일 파일 케이스 동작) UAT 시나리오로 검증
- Phase 22 success criterion #4 (TeachingImagePath 빈 폴백 회귀 0) UAT 시나리오로 검증
- Phase 22 success criterion #5 (msbuild PASS) blocking 태스크로 강제

Output:
- `Action_FAIMeasurement.cs` 의 2 SIMUL 로드 사이트에 InspectionImagePath 역할 명시 주석 추가
- `22-UAT.md` 신규 작성 — 4 시나리오 (TeachingImagePath INI 라운드트립, 동일 파일 케이스, 누락 키 폴백, msbuild)
- msbuild Debug/x64 PASS + 신규 warning 0 검증
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-image-dual-structure/22-CONTEXT.md
@.planning/phases/22-image-dual-structure/22-PATTERNS.md
@.planning/phases/22-image-dual-structure/22-01-PLAN.md

# Plan 22-01 산출물 참조
- DatumConfig.cs 에 `public string TeachingImagePath { get; set; } = "";` (L32 근방, [Category("Datum|ImageSource")] 직후) 가 존재해야 본 plan 의 UAT 시나리오가 의미를 가진다.
- EnsurePerRoiDefaults 에 `if (TeachingImagePath == null) TeachingImagePath = "";` (L515 근방) 가드 존재.

@WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
@WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs

<interfaces>
<!-- Simul 이미지 로드의 2 현장. 본 plan 은 이 2 라인 주변에 명시 주석만 추가 — 코드 라인 무수정. -->

From WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (L106-123) — Grab step:
```csharp
case EStep.Grab:
    if (ShotParam != null && !ShotParam.HasImage) {
        HImage image = null;
        //260409 hbk Phase 5: SimulImagePath 이미지 로드 (D-10)
        #if SIMUL_MODE
        if (!string.IsNullOrEmpty(ShotParam.SimulImagePath) && File.Exists(ShotParam.SimulImagePath)) {
            try {
                image = new HImage(ShotParam.SimulImagePath);
            } catch {
                image = null;
            }
        }
        if (image == null) {
            image = SystemHandler.Handle.Devices.GrabHalconImage(ShotParam);
        }
        #else
        image = SystemHandler.Handle.Devices.GrabHalconImage(ShotParam);
        #endif
```

From WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (L222-240) — Datum image grab:
```csharp
//260413 hbk Phase 6: Datum 이미지 취득 — Dedicated만 우선 지원 (D-07, D-08)
// ReuseFromShot 모드는 향후 Plan 04 UI 작업과 함께 구현.
private HImage GrabOrLoadDatumImage(InspectionSequence parentSeq) {
    if (ShotParam == null) return null;
    HImage image = null;
    #if SIMUL_MODE
    if (!string.IsNullOrEmpty(ShotParam.SimulImagePath) && File.Exists(ShotParam.SimulImagePath)) {
        try {
            image = new HImage(ShotParam.SimulImagePath);
        } catch {
            image = null;
        }
    }
    if (image == null) {
        image = SystemHandler.Handle.Devices.GrabHalconImage(ShotParam);
    }
    #else
    image = SystemHandler.Handle.Devices.GrabHalconImage(ShotParam);
    #endif
    return image;
}
```

From WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs (L15-16) — 검사 이미지의 진짜 출처:
```csharp
[Category("Shot|Simulation")]
public string SimulImagePath { get; set; } = "";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Action_FAIMeasurement.cs EStep.Grab 사이트에 InspectionImagePath 역할 명시 주석 추가</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs L106-123 (EStep.Grab 케이스 본문 + L109 의 기존 `//260409 hbk Phase 5: SimulImagePath 이미지 로드 (D-10)` 주석)
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs L15-16 (SimulImagePath 선언)
    - .planning/phases/22-image-dual-structure/22-PATTERNS.md L181-247 (Path #2 — Grab 단계 + 분리 전략 설명)
  </read_first>
  <behavior>
    - Action_FAIMeasurement.cs EStep.Grab 사이트에 SimulImagePath 의 **InspectionImagePath 역할** 이 명시 주석으로 문서화되어 있다
    - 기존 코드 라인 (L107-123) 의 의미/실행/동작은 변경 없음 (회귀 0)
    - 기존 주석 L109 는 보존 (Phase 5 의 D-10 트레이스 보존)
    - 신규 주석은 //260511 hbk Phase 22 IMG-02 마커 포함
  </behavior>
  <action>
    Action_FAIMeasurement.cs L109 (`//260409 hbk Phase 5: SimulImagePath 이미지 로드 (D-10)`) 라인 **바로 위에** 다음 1 줄을 정확히 삽입한다 (인접 들여쓰기 = 24 spaces — case 본문 if 블록 내부):

    ```csharp
                        //260511 hbk Phase 22 IMG-02 — ShotParam.SimulImagePath = InspectionImagePath 역할 (검사 사이클 마다 로드). 티칭 기준 이미지는 별도 DatumConfig.TeachingImagePath (셋업 시 1회, INI 보존) 사용 — 역할 분리. Simul 에서 두 경로 동일 파일 가능 (UAT Test 2).
    ```

    제약:
    - L109 기존 주석 `//260409 hbk Phase 5: SimulImagePath 이미지 로드 (D-10)` 보존 — 삭제/수정 금지 (Phase 20 D-12 stacking 패턴 — 신규 마커는 위에 누적)
    - 코드 라인 (L110-123) 무수정 — 단 1 자도 변경 금지 (회귀 0)
    - SimulImagePath → InspectionImagePath 변수 리네이밍 금지 (디자인 lock-in (b): 의미적 재해석만, 식별자 무변경)
    - using 추가 금지
    - 들여쓰기는 L109 와 byte-identical 동일 (24 spaces)
  </action>
  <acceptance_criteria>
    - `grep -n "Phase 22 IMG-02 — ShotParam.SimulImagePath = InspectionImagePath" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 매치 ≥ 1 (T2 에서 1 매치 더 추가됨)
    - `grep -c "260409 hbk Phase 5: SimulImagePath 이미지 로드" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 가 baseline 동일 (1 매치 — L109 보존)
    - `grep -n "new HImage(ShotParam.SimulImagePath)" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 가 정확히 2 라인 매치 (L113, L228 — 코드 무수정 검증)
    - EStep.Grab 케이스의 코드 라인 수 변경량 = 0 (주석 1 라인 + 코드 0 라인)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Phase 22 IMG-02 — ShotParam.SimulImagePath = InspectionImagePath" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</automated>
  </verify>
  <done>
    - EStep.Grab 사이트 L109 위에 InspectionImagePath 역할 명시 주석 1 줄 추가
    - 기존 코드/주석 무수정
    - //260511 hbk Phase 22 IMG-02 마커 부착
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Action_FAIMeasurement.cs GrabOrLoadDatumImage 사이트에 InspectionImagePath 역할 명시 주석 추가</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs L220-240 (GrabOrLoadDatumImage 메서드 헤더 + SIMUL 분기)
    - .planning/phases/22-image-dual-structure/22-PATTERNS.md L183-209 (Path #1 — Datum 이미지 로드 분석)
  </read_first>
  <behavior>
    - GrabOrLoadDatumImage 의 SIMUL 분기 진입부에 SimulImagePath = InspectionImagePath 역할 + Datum 찾기 동안 사용 의미가 주석으로 명시되어 있다
    - 기존 코드 라인 (L222-240) 동작 변경 없음
    - 신규 주석은 //260511 hbk Phase 22 IMG-02 마커 포함
  </behavior>
  <action>
    Action_FAIMeasurement.cs L225 (`#if SIMUL_MODE`) 라인 **바로 위에** 다음 1 줄을 정확히 삽입한다 (인접 들여쓰기 = 12 spaces — 메서드 본체):

    ```csharp
            //260511 hbk Phase 22 IMG-02 — Datum 찾기 단계의 이미지 = InspectionImagePath (= ShotParam.SimulImagePath) 사용. TeachingImagePath (DatumConfig 보존) 는 본 메서드에서 미참조 — 재티칭/UI 셋업 경로에서만 참조 (Phase 23 carry-over 가능).
    ```

    제약:
    - L222 기존 주석 (`//260413 hbk Phase 6: Datum 이미지 취득 — Dedicated만 우선 지원 (D-07, D-08)`) 및 L223 (`// ReuseFromShot 모드는...`) 보존
    - 코드 라인 L224 (`private HImage GrabOrLoadDatumImage(...)`) 시그니처 무수정
    - L225 (`#if SIMUL_MODE`) 위치 무수정 — 신규 주석은 그 위에 추가
    - L226 (`if (!string.IsNullOrEmpty(ShotParam.SimulImagePath) && File.Exists(...)`) 무수정
    - 들여쓰기는 L226 의 if 본체 들여쓰기보다 한 단계 외부 — `#if SIMUL_MODE` 들여쓰기와 동일 (12 spaces)
  </action>
  <acceptance_criteria>
    - `grep -n "Phase 22 IMG-02 — Datum 찾기 단계의 이미지" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 매치 1
    - `grep -c "Phase 22 IMG-02" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 가 정확히 2 (T1 EStep.Grab + T2 GrabOrLoadDatumImage)
    - `grep -B1 "#if SIMUL_MODE" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 결과 중 두 번째 `#if SIMUL_MODE` (GrabOrLoadDatumImage 내부) 의 직전 라인이 신규 주석
    - `grep -n "private HImage GrabOrLoadDatumImage" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 매치 1, 시그니처 byte-identical 보존
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Phase 22 IMG-02" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</automated>
  </verify>
  <done>
    - GrabOrLoadDatumImage 의 SIMUL 분기 위에 InspectionImagePath 역할 명시 주석 1 줄 추가
    - 기존 코드/주석 무수정
    - //260511 hbk Phase 22 IMG-02 마커 부착
    - 파일 전체 신규 주석 2 라인 (T1+T2), 코드 변경 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 [BLOCKING]: msbuild Debug/x64 PASS + 신규 warning 0 검증</name>
  <files>WPF_Example/DatumMeasurement.csproj</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (Plan 22-01 산출물 — TeachingImagePath 선언 + EnsurePerRoiDefaults null 가드)
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (Task 1+2 산출물 — 2 신규 주석)
    - .planning/phases/21-memory-image-buffer/ 의 SUMMARY (이전 Phase msbuild 검증 패턴 참고)
  </read_first>
  <behavior>
    - msbuild Debug/x64 빌드가 0 에러로 완료
    - 신규 warning 발생 0 (baseline 대비 신규 항목 0 — 기존 warning 은 그대로 둠)
    - 산출물 DatumMeasurement.exe 생성 확인
  </behavior>
  <action>
    PowerShell 또는 Bash 에서 다음 명령 실행:

    ```powershell
    $msbuild = "C:\Program Files (x86)\Microsoft Visual Studio\2019\Community\MSBuild\Current\Bin\MSBuild.exe"
    # 또는 환경에 따라 BuildTools/2022 경로 — 이전 phase (Phase 20/21) 에서 사용한 경로 그대로 사용
    & $msbuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo 2>&1 | Tee-Object -FilePath build_22.log
    ```

    검증:
    1. `Build succeeded.` 또는 `0 Error(s)` 라인 존재
    2. `0 Warning(s)` 또는 기존 baseline (이전 Phase 의 build_*.log 와 동일 warning 수) 와 일치
    3. baseline warning 목록 외 신규 항목 0 — `grep -i "warning" build_22.log` 결과를 Phase 21 의 build_21.log (또는 동급 baseline) 와 diff
    4. `bin\x64\Debug\DatumMeasurement.exe` 생성 시각이 빌드 직후 (mtime 갱신)

    제약:
    - 빌드 실패 시 즉시 stop + 사용자 보고 (FinishAction(Error) 패턴) — Task 4 UAT 진행 금지
    - 신규 warning 발생 시 stop + 원인 추적 (대개 TeachingImagePath 미사용 경고 CS0649 가능성 — public 속성이므로 발생하지 않아야 함)
    - Configuration=Release 빌드는 본 Plan 범위 외 (Plan 22-02 는 Debug/x64 만 강제)
  </action>
  <acceptance_criteria>
    - build_22.log 파일이 phase 디렉토리 또는 워크트리 루트에 존재
    - log 내 `0 Error(s)` 문자열 매치 1
    - log 내 `Build succeeded` 또는 동등 PASS 마커 매치 ≥ 1
    - 신규 warning 메시지 0 (직전 phase build log 와 diff 시 추가 warning 없음)
    - `bin\x64\Debug\DatumMeasurement.exe` mtime 이 build_22.log mtime 의 ±60s 이내
  </acceptance_criteria>
  <verify>
    <automated>grep -E "(0 Error\(s\)|Build succeeded)" build_22.log</automated>
  </verify>
  <done>
    - msbuild Debug/x64 PASS 확인
    - 신규 warning 0 확인 (Phase 22 success criterion #5)
    - build_22.log 보관 (UAT 증빙)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 [CHECKPOINT]: 22-UAT.md 사용자 검증 — 4 시나리오 sign-off</name>
  <what-built>
    Plan 22-01: DatumConfig.TeachingImagePath 속성 + EnsurePerRoiDefaults null 가드 추가
    Plan 22-02 T1+T2: Action_FAIMeasurement.cs 2 SIMUL 로드 사이트에 InspectionImagePath 역할 명시 주석 추가
    Plan 22-02 T3: msbuild Debug/x64 PASS + 신규 warning 0 자동 검증 완료
  </what-built>
  <how-to-verify>
    실행 에이전트는 검증 시작 전 `22-UAT.md` 파일을 다음 4 시나리오 템플릿으로 phase 디렉토리에 작성한다:

    파일 경로: `.planning/phases/22-image-dual-structure/22-UAT.md`

    템플릿 (frontmatter: status=pending, updated=2026-05-11, total=4, passed=0):

    ---

    **Test 1 — TeachingImagePath INI 라운드트립 (SC#1)**
    Steps:
    1. DatumMeasurement.exe 실행
    2. 임의 InspectionSequence → Fixture → Datum 노드 선택 → PropertyGrid 의 `Datum|ImageSource` 카테고리에 **TeachingImagePath** 항목이 등장하는지 시각 확인
    3. TeachingImagePath 에 임의 파일 경로 (예: `C:\test\teaching.bmp`) 입력 → 다른 노드 클릭하여 PropertyGrid commit
    4. 메뉴 → 레시피 Save → 저장된 INI 파일 (예: `Recipe/<name>.ini`) 열기
    5. `[FIXTURE_DATUM_0]` 섹션 (또는 해당 Datum 인덱스) 에 `TeachingImagePath=C:\test\teaching.bmp` 라인이 존재하는지 grep/육안 확인
    6. 프로그램 재시작 → 같은 레시피 Load → 같은 Datum 노드의 PropertyGrid 의 TeachingImagePath 값이 복원되는지 확인
    Expected: 3 시점 입력값과 6 시점 복원값이 byte-identical
    Result: __ (PASS/FAIL)

    **Test 2 — 두 경로 동일 파일 케이스 (SC#3)**
    Steps:
    1. ShotConfig.SimulImagePath = `C:\test\same.bmp` 설정 + DatumConfig.TeachingImagePath = `C:\test\same.bmp` (동일 경로) 설정
    2. SIMUL 모드로 시퀀스 1회 실행 → Datum 찾기 + FAI 측정 1 항목 이상 수행
    3. 검사 결과가 오류 없이 완주 (Datum LastTeachSucceeded=true 또는 동등 성공 상태, FAI MeasuredValue > 0 또는 의도된 fail 케이스 — 어쨌든 NullReference/CrashException 0)
    Expected: 동일 경로여도 코드 레벨 분리 유지, 런타임 회귀 0
    Result: __ (PASS/FAIL)

    **Test 3 — TeachingImagePath INI 키 미존재 폴백 (SC#4)**
    Steps:
    1. Phase 22 이전 (또는 수동 편집으로) `TeachingImagePath=` 키가 누락된 INI 파일 준비
    2. 프로그램 로드 → 해당 Datum 노드 선택 → PropertyGrid 에서 TeachingImagePath 값 = `""` (빈 문자열) 표시 확인
    3. EnsurePerRoiDefaults 호출 경로 (예: 시퀀스 시작 직전 Datum 찾기 트리거) 후 NullReference 없이 정상 동작
    Expected: 빈 문자열 폴백 + 회귀 0
    Result: __ (PASS/FAIL)

    **Test 4 — msbuild Debug/x64 PASS + warning 0 (SC#5)**
    Steps:
    1. Task 3 산출물 `build_22.log` 첨부
    2. log 의 `0 Error(s)` 라인 확인
    3. log 의 신규 warning 0 확인 (직전 phase build log diff)
    Result: __ (PASS/FAIL)

    ---

    사용자는 위 4 시나리오를 직접 수행 후 각 Result 를 PASS/FAIL 로 채우고, frontmatter 의 `passed:` 카운트와 `status:` (passed:4 → signed_off) 를 갱신한다.

    `gsd` 에이전트는 이 체크포인트를 절대 건너뛰지 않는다 — 사용자의 명시적 "approved" 또는 "Test N FAIL" 보고를 기다린다.
  </how-to-verify>
  <resume-signal>사용자 입력: "approved" (4/4 PASS 시) 또는 "Test N FAIL: <원인>" (회귀/실패 보고 시 — gap closure plan 발동)</resume-signal>
  <done>
    - 22-UAT.md 가 phase 디렉토리에 작성됨 (frontmatter + 4 Test 본문)
    - 4 시나리오 모두 사용자 검증 PASS → frontmatter status=signed_off, passed=4
    - 신규 발견 결함은 carry-over 항목으로 STATE.md / 22-UAT.md "Carry-overs" 섹션에 기록
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 디스크 INI → DatumConfig | TeachingImagePath 문자열 역직렬화 (Plan 22-01 에서 다룸) |
| TeachingImagePath/SimulImagePath → File.Exists / new HImage(path) | 파일 시스템 호출에 경로 전달 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04 | Tampering | INI 파일의 SimulImagePath/TeachingImagePath 가 경로 트래버설 (예: `..\..\Windows\...`) | accept | 로컬 산업 PC, 단일 사용자, 외부 노출 0. ASVS L1 범위 외. File.Exists + new HImage 가 실패 시 null 반환 → grab fallback. |
| T-22-05 | DoS | `new HImage(path)` 가 매우 큰 파일에 대해 메모리 폭주 | accept | SIMUL_MODE 한정 (개발자 셋업), 운영 모드 미적용. 실 카메라 grab 으로 대체 가능. |
| T-22-06 | Information Disclosure | UAT 시나리오의 build log 에 절대 경로 노출 | accept | 로컬 로그, git 추적 외. Phase 21 build_21.log 와 동일 정책. |
</threat_model>

<verification>
1. (auto) `grep -c "Phase 22 IMG-02" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` == 2
2. (auto) `grep -c "new HImage(ShotParam.SimulImagePath)" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` == 2 (코드 무수정 확인)
3. (auto) `grep -c "260409 hbk Phase 5: SimulImagePath 이미지 로드" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` == 1 (L109 기존 주석 보존)
4. (auto) build_22.log 의 `0 Error(s)` 매치 1, 신규 warning 0
5. (human) 22-UAT.md 4 시나리오 모두 PASS, frontmatter status=signed_off
</verification>

<success_criteria>
- Action_FAIMeasurement.cs 의 2 SIMUL 로드 사이트에 InspectionImagePath 역할 명시 주석 추가 완료 (코드 라인 무수정)
- msbuild Debug/x64 PASS + 신규 warning 0 자동 검증 통과
- 22-UAT.md 작성 + 사용자 4 시나리오 sign-off (SC#1 INI 라운드트립 / SC#3 동일 파일 / SC#4 누락 키 폴백 / SC#5 build)
- Phase 22 success criteria 1~5 (ROADMAP.md L72-76) 모두 충족 — Plan 22-01 (SC#1) + Plan 22-02 (SC#2~5)
- STATE.md 갱신 (Plan 외 작업) 시 `phase 22 signed_off` 마커 추가 권장 — execute-plan 에이전트가 SUMMARY 작성 시 처리
</success_criteria>

<output>
After completion, create `.planning/phases/22-image-dual-structure/22-02-SUMMARY.md` documenting:
- Action_FAIMeasurement.cs 의 2 신규 주석 라인 번호 (T1: L109 위, T2: L225 위)
- build_22.log 의 `0 Error(s)` + `0 Warning(s)` (또는 baseline 동일) 인용
- 22-UAT.md 4 시나리오 결과 인용 (PASS 횟수)
- Carry-over: (있다면) — 예: TeachingImagePath 의 실 소비처 (재티칭 UI / Phase 23 A시리즈 활용) 는 후속 phase
- 디자인 lock-in 결정 (b) 의 효과 검증: ShotConfig.SimulImagePath 코드/INI 키 무수정, Action_FAIMeasurement 코드 무수정 — 회귀 0 달성
</output>
</content>
</invoke>