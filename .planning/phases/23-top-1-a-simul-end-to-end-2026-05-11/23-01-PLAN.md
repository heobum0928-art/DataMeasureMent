---
phase: 23
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs
  - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [ALG-01]
requirements_addressed: [ALG-01]
tags: [measurement, halcon, edge-to-line-distance, signature-extension, datum-relative]

must_haves:
  truths:
    - "EdgeToLineDistanceMeasurement 신규 클래스가 MeasurementBase 를 상속하고 TypeName = 'EdgeToLineDistance' 를 반환한다"
    - "EdgeToLineDistanceMeasurement 가 7 Edge 파라미터 (EdgeThreshold, Sigma, EdgeSampleCount, EdgeTrimCount, EdgePolarity, EdgeDirection, EdgeSelection) 를 모두 PropertyGrid 에 노출한다"
    - "VisionAlgorithmService.TryFitLine 시그니처에 optional `string selection = \"all\"` 파라미터가 추가되어 기존 5 caller 가 모두 default 로 호환된다"
    - "TryFitLine 내부 MeasurePos 호출에서 'all' 하드코딩이 제거되고 selection 인자값이 'first'/'last'/'all' 로 변환되어 전달된다"
    - "csproj Compile ItemGroup 에 EdgeToLineDistanceMeasurement.cs 가 등록되어 msbuild Debug/x64 가 CS0246 없이 PASS 한다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs"
      provides: "신규 Measurement 클래스 (Point ROI fit + datumTransform 적용 + Y 부호 반전)"
      contains: "public class EdgeToLineDistanceMeasurement : MeasurementBase"
    - path: "WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs"
      provides: "TryFitLine 시그니처 확장 (selection 인자 추가)"
      contains: "string selection = \"all\""
    - path: "WPF_Example/DatumMeasurement.csproj"
      provides: "신규 .cs 파일 Compile 등록"
      contains: "Custom\\Sequence\\Inspection\\Measurements\\EdgeToLineDistanceMeasurement.cs"
  key_links:
    - from: "EdgeToLineDistanceMeasurement.TryExecute"
      to: "VisionAlgorithmService.TryFitLine"
      via: "svc.TryFitLine(image, ..., EdgeSelection, out pr1, out pc1, out pr2, out pc2, out error)"
      pattern: "svc\\.TryFitLine\\([\\s\\S]*?EdgeSelection"
    - from: "EdgeToLineDistanceMeasurement.TryExecute"
      to: "HOperatorSet.AffineTransPoint2d"
      via: "datumTransform 적용 후 datumRow 추출"
      pattern: "AffineTransPoint2d\\(datumTransform"
    - from: "VisionAlgorithmService.TryFitLine"
      to: "HOperatorSet.MeasurePos"
      via: "measureSel 변수 (selection PascalCase → Halcon lower-case 변환)"
      pattern: "MeasurePos\\([\\s\\S]*?measureSel"
---

<objective>
EdgeToLineDistance 측정 알고리즘의 신규 인프라를 구축한다.

목적: Phase 23 ALG-01 의 핵심 계약(Datum-relative Y 거리 측정)을 만족하는 신규 Measurement 클래스를 작성하고, HALCON memory feedback (measurePhi + EdgeSelection 명시) 을 충족하도록 VisionAlgorithmService.TryFitLine 시그니처를 확장한다.

산출물:
- 신규 파일 1개: EdgeToLineDistanceMeasurement.cs (Point ROI 1개 fit → AffineTransPoint2d → -datumRow * pixelResolution)
- 수정 파일 2개: VisionAlgorithmService.cs (TryFitLine selection 인자), DatumMeasurement.csproj (Compile 등록)

본 plan 은 Plan 23-02 (Factory + GrabOrLoadDatumImage) 의 의존성 — Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-CONTEXT.md
@.planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-RESEARCH.md
@.planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-PATTERNS.md
@WPF_Example/Custom/Sequence/Inspection/Measurements/PointToLineDistanceMeasurement.cs
@WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
@WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
@WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs

<interfaces>
<!-- Key contracts extracted from existing code — executor MUST use these directly, no exploration. -->

From WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (parent class):
```csharp
public abstract class MeasurementBase
{
    public abstract string TypeName { get; }
    public abstract bool TryExecute(
        HImage image,
        HTuple datumTransform,
        double pixelResolution,
        out double resultValue,
        out string error,
        out List<EdgeInspectionOverlay> overlays);
    // NominalValue, UpperTolerance, LowerTolerance, EvaluateJudgement, IsPass 자동 제공
}
```

From WPF_Example/Custom/Sequence/Inspection/Measurements/PointToLineDistanceMeasurement.cs (patron, L51-L75):
```csharp
public override bool TryExecute(HImage image, HTuple datumTransform, double pixelResolution,
    out double resultValue, out string error, out List<EdgeInspectionOverlay> overlays)
{
    resultValue = 0; error = null;
    overlays = new List<EdgeInspectionOverlay>();
    var svc = new VisionAlgorithmService();
    double pr1, pc1, pr2, pc2;
    if (!svc.TryFitLine(image,
        Point_Row, Point_Col, Point_Phi, Point_Length1, Point_Length2,
        datumTransform,
        EdgeSampleCount, EdgeTrimCount, Sigma, EdgeThreshold,
        EdgeDirection, EdgePolarity,
        out pr1, out pc1, out pr2, out pc2, out error))
    { return false; }
    double pRow = (pr1 + pr2) / 2.0;
    double pCol = (pc1 + pc2) / 2.0;
    // ... (PointToLineDistance 의 Line ROI fit + DistancePointToLine — EdgeToLineDistance 에서는 제거)
}
```

From WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs §TryFitLine (현 시그니처, L18-26):
```csharp
public bool TryFitLine(
    HImage image,
    double roiRow, double roiCol, double roiPhi,
    double roiLength1, double roiLength2,
    HTuple datumTransform,
    int sampleCount, int trimCount, double sigma, int threshold,
    string direction, string polarity,
    out double row1, out double col1, out double row2, out double col2,
    out string error)
```
현 L93-95 `MeasurePos(..., pol, "all", ...)` — 'all' 하드코딩.

From WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs §TryFindCircleByPolarSampling (selection 처리 reference 패턴, L240-264):
```csharp
if (string.IsNullOrEmpty(selection)) selection = "First";
string selectionLower;
if (string.Equals(selection, "Last", StringComparison.OrdinalIgnoreCase)) selectionLower = "last";
else if (string.Equals(selection, "All", StringComparison.OrdinalIgnoreCase)) selectionLower = "all";
else selectionLower = "first";
HOperatorSet.MeasurePos(image, measureHandle, sigma, threshold, polarity, selectionLower, ...);
```

From WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs (L13, L22):
```csharp
public static readonly List<string> Directions    = new List<string> { "LtoR", "RtoL", "TtoB", "BtoT" };
public static readonly List<string> Selections    = new List<string> { "First", "Last", "All" };
public static readonly List<string> FAIPolarities = new List<string> { "DarkToLight", "LightToDark" };
```
</interfaces>

<memory>
- memory/feedback_halcon_measurepos_must_haves.md — HALCON MeasurePos 작성 시 measurePhi 명시 매핑 + EdgeSelection (First/Last/All) 명시 필수
- memory/feedback_comment_convention.md — `//YYMMDD hbk` 마커 (Phase 23: `//260511 hbk Phase 23 ALG-01 — <reason>`)
- Phase 20 D-12 marker stacking — 기존 마커 보존 + 위에 신규 누적
</memory>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend VisionAlgorithmService.TryFitLine signature with selection param</name>
  <files>WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs (시그니처 L18-26 + MeasurePos 호출 L93-95 + TryFindCircleByPolarSampling L222-264 selection 처리 패턴 확인)
    - WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs (Selections L22 = First/Last/All)
    - 5 caller files (변경 영향 없음 확인 — default param 호환):
      - WPF_Example/Custom/Sequence/Inspection/Measurements/PointToLineDistanceMeasurement.cs (L66, L79)
      - WPF_Example/Custom/Sequence/Inspection/Measurements/PointToPointDistanceMeasurement.cs (L65, L78)
      - WPF_Example/Custom/Sequence/Inspection/Measurements/LineToLineAngleMeasurement.cs (L65, L76)
      - WPF_Example/Custom/Sequence/Inspection/Measurements/LineToLineDistanceMeasurement.cs (L64, L75)
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-PATTERNS.md §File 4 — TryFindCircleByPolarSampling 패턴을 그대로 차용
    - memory/feedback_halcon_measurepos_must_haves.md (Selection 명시 필수)
  </read_first>
  <action>
    VisionAlgorithmService.cs §TryFitLine 시그니처 + MeasurePos 호출 변경 (per D-07, D-10, RESEARCH Code Examples Operation 3).

    **변경 1 — 시그니처 (L18-26)에 optional selection 파라미터 추가 (out 파라미터 뒤, default = "all" 로 기존 caller 무수정 호환):**
    ```csharp
    public bool TryFitLine( //260413 hbk
        HImage image,
        double roiRow, double roiCol, double roiPhi,
        double roiLength1, double roiLength2,
        HTuple datumTransform,
        int sampleCount, int trimCount, double sigma, int threshold,
        string direction, string polarity,
        out double row1, out double col1, out double row2, out double col2,
        out string error,
        string selection = "all") //260511 hbk Phase 23 ALG-01 — D-10 EdgeSelection 명시 (optional, 기존 caller 호환)
    ```

    **변경 2 — MeasurePos 호출 (L93-95)에서 "all" 하드코딩 제거 + selection 인자값 사용:**
    호출 직전에 selection → measureSel 변환 (TryFindCircleByPolarSampling 와 동일 패턴, StringComparison.OrdinalIgnoreCase 사용. `using System;` 이 이미 있음을 확인하거나 fully-qualified `System.StringComparison` 사용):
    ```csharp
    //260511 hbk Phase 23 ALG-01 — D-10 EdgeSelection 명시 처리 (TryFindCircleByPolarSampling L240-258 패턴 차용)
    string measureSel;
    if (string.Equals(selection, "First", System.StringComparison.OrdinalIgnoreCase))
    {
        measureSel = "first";
    }
    else if (string.Equals(selection, "Last", System.StringComparison.OrdinalIgnoreCase))
    {
        measureSel = "last";
    }
    else
    {
        measureSel = "all";
    }

    HTuple rows, cols, amp, dist;
    HOperatorSet.MeasurePos(image, measureHandle,
        Math.Max(0.4, sigma), Math.Max(1, threshold),
        pol, measureSel, out rows, out cols, out amp, out dist); //260511 hbk Phase 23 ALG-01
    ```

    **주의:**
    - 기존 5 caller 는 selection 인자 미전달 → default "all" 적용 → 기존 동작 byte-identical 보존 (회귀 0). 본 task 에서 caller 무수정.
    - measurePhi 매핑(EdgeDirection → measurePhi 부호) 은 본 메서드의 기존 처리(현 L60-72 부근)를 byte-identical 보존. 본 변경은 selection 만.
    - Style = Allman (analog L20, L27, L31). 기존 본문 스타일 유지.
    - `//260511 hbk Phase 23 ALG-01` 마커는 시그니처 신규 라인 + measureSel 변환 블록 + MeasurePos 호출 라인에 부착.
  </action>
  <verify>
    <automated>findstr /n /c:"string selection = \"all\"" WPF_Example\Halcon\Algorithms\VisionAlgorithmService.cs &amp;&amp; findstr /n /c:"measureSel" WPF_Example\Halcon\Algorithms\VisionAlgorithmService.cs &amp;&amp; findstr /n /c:"260511 hbk Phase 23 ALG-01" WPF_Example\Halcon\Algorithms\VisionAlgorithmService.cs</automated>
  </verify>
  <acceptance_criteria>
    - VisionAlgorithmService.cs §TryFitLine 시그니처 마지막에 `string selection = "all"` 포함 (grep "string selection = \"all\"")
    - VisionAlgorithmService.cs 본문에 `measureSel` 변수 선언/분기 3개 (First/Last/else→all) 존재
    - VisionAlgorithmService.cs §TryFitLine 의 MeasurePos 호출 인자 4번째 위치에 `measureSel` 사용 (이전 하드코딩 `"all"` 제거). 메서드 영역 한정 2-grep 검증:
      - `findstr /c:'pol, "all",' WPF_Example\Halcon\Algorithms\VisionAlgorithmService.cs` → 0 matches (TryFitLine 본문 내 하드코딩 제거 확인)
      - `findstr /c:'pol, measureSel,' WPF_Example\Halcon\Algorithms\VisionAlgorithmService.cs` → 1 match (신규 measureSel 호출 확인)
    - `//260511 hbk Phase 23 ALG-01` 마커 grep ≥ 3 매치 (시그니처 라인 + measureSel 분기 블록 + MeasurePos 라인)
    - 5 caller 파일 (PointToLineDistance/PointToPointDistance/LineToLineAngle/LineToLineDistance) 의 TryFitLine 호출은 unchanged — grep 으로 본 task 변경 없음 확인
  </acceptance_criteria>
  <done>
    TryFitLine 시그니처 확장 완료. selection 파라미터가 PropertyGrid 의 EdgeSelection 값을 그대로 받아 MeasurePos 4번째 인자로 전달. 기존 5 caller 무수정 호환 (default "all" 자동 적용). HALCON memory feedback 항목 (a) measurePhi 매핑은 기존 본문 보존, (b) EdgeSelection 명시 추가로 충족.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create EdgeToLineDistanceMeasurement.cs (new file) + register in csproj</name>
  <files>
    WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs
    WPF_Example/DatumMeasurement.csproj
  </files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/PointToLineDistanceMeasurement.cs (직접 patron, 전체 95 라인)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (TryExecute 시그니처 + NominalValue/IsPass)
    - WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs (Directions/Selections/FAIPolarities)
    - WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs (overlay 타입 — overlays out 파라미터)
    - WPF_Example/DatumMeasurement.csproj §Compile ItemGroup (L218-222 부근 Measurements 등록 라인)
    - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs §TryFitLine — Task 1 으로 확장된 시그니처 (selection 인자 추가됨)
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-PATTERNS.md §File 1 (신규 파일 골격) + §File 6 (csproj 등록)
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-RESEARCH.md §Code Examples Operation 1 + Pitfalls 2, 4, 5, 6
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-CONTEXT.md (D-06, D-07, D-08, D-09, D-10, D-11)
  </read_first>
  <action>
    **하위작업 A — 신규 파일 생성: `WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs`** (per D-06, D-07, D-10, RESEARCH Code Examples Op#1, PATTERNS File 1).

    PointToLineDistanceMeasurement.cs (analog) 와 동일 namespace (`ReringProject.Sequence`), Allman style 강제. 파일 상단 헤더 마커 = `//260511 hbk Phase 23 ALG-01 — Datum-relative Y 거리 측정 (D-06)`.

    클래스 시그니처: `public class EdgeToLineDistanceMeasurement : MeasurementBase`.

    필수 멤버 (per D-06/D-07/D-08/D-10):
    - `public override string TypeName { get { return "EdgeToLineDistance"; } }`
    - **Point ROI 5필드** (per D-08 Rectangle only, `[Category("Point|ROI")]`): `Point_Row`, `Point_Col`, `Point_Phi`, `Point_Length1`, `Point_Length2` — 모두 `double` getter/setter.
    - **Edge 7파라미터** (per D-07, `[Category("Edge")]`):
      - `public int EdgeThreshold { get; set; } = 10;`
      - `public double Sigma { get; set; } = 1.0;`
      - `public int EdgeSampleCount { get; set; } = 20;`
      - `public int EdgeTrimCount { get; set; } = 10;`
      - `[ItemsSourceProperty(nameof(EdgePolarityList))] public string EdgePolarity { get; set; } = "DarkToLight";`
      - `[ItemsSourceProperty(nameof(EdgeDirectionList))] public string EdgeDirection { get; set; } = "TtoB";` **(default = TtoB per RESEARCH Pitfall 6 — 수평 에지 검출, Y 방향 거리 측정 의도)**
      - `[ItemsSourceProperty(nameof(EdgeSelectionList))] public string EdgeSelection { get; set; } = "First";` **(per D-10, EdgeOptionLists.Selections)**
    - **ItemsSource wrapper 3개** (모두 `[PropertyTools.DataAnnotations.Browsable(false)]`):
      - `EdgeDirectionList { get { return EdgeOptionLists.Directions; } }`
      - `EdgePolarityList { get { return EdgeOptionLists.FAIPolarities; } }`
      - `EdgeSelectionList { get { return EdgeOptionLists.Selections; } }`
    - **Constructor:** `public EdgeToLineDistanceMeasurement(object owner) : base(owner) { } //260511 hbk Phase 23 ALG-01`

    **TryExecute override (D-06 + D-02 + D-10 + D-11):**
    ```csharp
    public override bool TryExecute(
        HImage image,
        HTuple datumTransform,
        double pixelResolution,
        out double resultValue,
        out string error,
        out List<EdgeInspectionOverlay> overlays)
    {
        resultValue = 0;
        error = null;
        overlays = new List<EdgeInspectionOverlay>(); //260511 hbk Phase 23 ALG-01 — PointToLineDistance 패턴 (overlay 빈 리스트, Phase 7-01 D-03)

        //260511 hbk Phase 23 ALG-01 — D-11 Datum 찾기 실패 가드 (literal 구현, upstream gating 은 보조 이중 안전망)
        if (datumTransform == null || datumTransform.Length == 0)
        {
            error = "Datum not found";
            return false;
        }

        var svc = new VisionAlgorithmService();
        double pr1, pc1, pr2, pc2;
        //260511 hbk Phase 23 ALG-01 — TryFitLine selection 인자 전달 (D-10 EdgeSelection 명시)
        if (!svc.TryFitLine(image,
            Point_Row, Point_Col, Point_Phi, Point_Length1, Point_Length2,
            datumTransform,
            EdgeSampleCount, EdgeTrimCount, Sigma, EdgeThreshold,
            EdgeDirection, EdgePolarity,
            out pr1, out pc1, out pr2, out pc2, out error,
            EdgeSelection))
        {
            return false;
        }
        double pRow = (pr1 + pr2) / 2.0;
        double pCol = (pc1 + pc2) / 2.0;

        //260511 hbk Phase 23 ALG-01 — Datum-relative Y 좌표 추출 + D-02 부호 반전 (image row → +Y 위쪽 양수)
        double datumRow = pRow;
        if (datumTransform != null && datumTransform.Length > 0)
        {
            try
            {
                HTuple tRow, tCol;
                HOperatorSet.AffineTransPoint2d(datumTransform, pRow, pCol, out tRow, out tCol);
                datumRow = tRow.D;
            }
            catch
            {
                // transform 실패 시 image-row 좌표 사용 (TryFitLine 패턴 일관성, RESEARCH Pitfall 2)
            }
        }
        resultValue = -datumRow * pixelResolution; //260511 hbk Phase 23 ALG-01 — D-02 +Y 부호 (위쪽 양수)
        return true;
    }
    ```

    using 헤더 (analog L1-7):
    ```csharp
    //260511 hbk Phase 23 ALG-01 — Datum Y 거리 측정 (D-06)
    using System.Collections.Generic;
    using HalconDotNet;
    using PropertyTools.DataAnnotations;
    using ReringProject.Halcon.Algorithms;
    using ReringProject.Halcon.Models;
    ```

    **하위작업 B — csproj 등록: `WPF_Example/DatumMeasurement.csproj`** (per RESEARCH Pitfall 5, PATTERNS File 6).

    Compile ItemGroup 의 Measurements 등록 그룹 (PointToLineDistanceMeasurement.cs 등록 라인 부근) 에 1줄 추가:
    ```xml
    <Compile Include="Custom\Sequence\Inspection\Measurements\EdgeToLineDistanceMeasurement.cs" />
    ```
    위치: 인접한 다른 Measurements 등록 라인들과 같은 ItemGroup 내, 알파벳 순 또는 기존 그룹 끝. csproj 는 plain XML — `//YYMMDD hbk` 주석 부착 안 함.

    **주의:**
    - D-11 (Datum 찾기 실패) — **literal 구현** = `TryExecute` 진입부에 `datumTransform == null || datumTransform.Length == 0` 가드 → `error = "Datum not found"; return false;` (위 TryExecute snippet 의 가드 블록 참조). upstream gating (Action_FAIMeasurement.EStep.DatumPhase 의 FinishAction(Error)) 은 **보조 이중 안전망** — 정상 흐름에서는 Measure 단계 도달 안 하지만, datumTransform 이 null/empty 로 전달되는 경계 케이스 (DatumPhase 통과 후 transform 누락) 도 본 가드로 차단.
    - Overlay = 빈 리스트 (PointToLineDistance 패턴 그대로). SC#2 strip 시각화는 InspectionListView 노드 색상 (기존 CO-05 경로) — Halcon viewer 라인 미표시는 UAT (Plan 23-03) 에서 사용자 확인 (Pitfall 4).
    - Style: 신규 파일 = Allman (analog 일치). csproj = XML.
    - 모든 신규 라인에 `//260511 hbk Phase 23 ALG-01` 마커 부착.
  </action>
  <verify>
    <automated>findstr /n /c:"class EdgeToLineDistanceMeasurement : MeasurementBase" WPF_Example\Custom\Sequence\Inspection\Measurements\EdgeToLineDistanceMeasurement.cs &amp;&amp; findstr /n /c:"EdgeSelection" WPF_Example\Custom\Sequence\Inspection\Measurements\EdgeToLineDistanceMeasurement.cs &amp;&amp; findstr /n /c:"AffineTransPoint2d" WPF_Example\Custom\Sequence\Inspection\Measurements\EdgeToLineDistanceMeasurement.cs &amp;&amp; findstr /n /c:"-datumRow * pixelResolution" WPF_Example\Custom\Sequence\Inspection\Measurements\EdgeToLineDistanceMeasurement.cs &amp;&amp; findstr /n /c:"datumTransform == null" WPF_Example\Custom\Sequence\Inspection\Measurements\EdgeToLineDistanceMeasurement.cs &amp;&amp; findstr /n /c:"Datum not found" WPF_Example\Custom\Sequence\Inspection\Measurements\EdgeToLineDistanceMeasurement.cs &amp;&amp; findstr /n /c:"EdgeToLineDistanceMeasurement.cs" WPF_Example\DatumMeasurement.csproj</automated>
  </verify>
  <acceptance_criteria>
    - 파일 `WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs` 존재
    - 파일 내용에 `public class EdgeToLineDistanceMeasurement : MeasurementBase` 포함
    - 파일 내용에 `TypeName { get { return "EdgeToLineDistance"; } }` 포함
    - 파일 내용에 7 Edge property: `EdgeThreshold`, `Sigma`, `EdgeSampleCount`, `EdgeTrimCount`, `EdgePolarity`, `EdgeDirection`, `EdgeSelection` 모두 존재 (grep 7건 매치)
    - 파일 내용에 `EdgeDirection { get; set; } = "TtoB"` (default per RESEARCH Pitfall 6)
    - 파일 내용에 `EdgeSelection { get; set; } = "First"` (default per RESEARCH A3)
    - 파일 내용에 `[ItemsSourceProperty(nameof(EdgeSelectionList))]` + `EdgeSelectionList` getter 존재
    - 파일 내용에 `svc.TryFitLine(...EdgeSelection)` 호출 — selection 인자 마지막 위치 전달
    - 파일 내용에 `HOperatorSet.AffineTransPoint2d(datumTransform, pRow, pCol, out tRow, out tCol)` 호출
    - 파일 내용에 `resultValue = -datumRow * pixelResolution;` (D-02 부호 반전)
    - 파일 내용에 `try { ... } catch { }` AffineTransPoint2d 보호
    - 파일 내용에 D-11 literal guard: `datumTransform == null || datumTransform.Length == 0` 분기 1회 매치 (grep `datumTransform == null`)
    - 파일 내용에 `error = "Datum not found"` 문자열 정확히 1회 매치 (D-11 literal 메시지)
    - 파일 내용에 `//260511 hbk Phase 23 ALG-01` 마커 grep ≥ 5 매치
    - csproj 에 `<Compile Include="Custom\Sequence\Inspection\Measurements\EdgeToLineDistanceMeasurement.cs" />` 정확히 1줄 추가
    - csproj 의 다른 Compile 라인은 unchanged (grep diff)
  </acceptance_criteria>
  <done>
    EdgeToLineDistanceMeasurement.cs 가 MeasurementBase 를 상속하고 7 Edge 파라미터 (+EdgeSelection per D-10) 를 모두 노출한다. TryExecute 가 Task 1 으로 확장된 TryFitLine 의 selection 인자를 전달하고, AffineTransPoint2d 로 datumTransform 적용 후 -datumRow * pixelResolution 으로 +Y 부호 결과 반환. csproj 등록 완료. msbuild 실패 사유(CS0246) 사전 차단.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: msbuild Debug/x64 Rebuild PASS verification (Phase 21 baseline 6 warnings preserved)</name>
  <files>
    build_23_w1.log (workspace root, gitignored — verification artifact only, no commit)
  </files>
  <read_first>
    - .planning/phases/22-image-dual-structure/22-02-SUMMARY.md §Build Verification — Phase 21 baseline (MSB3884×2 + CS0162×2 + CS0219×2 = 6 occurrences)
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-VALIDATION.md §Sampling Rate
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-CONTEXT.md D-19 (msbuild 검증 정의)
  </read_first>
  <action>
    Task 1 + Task 2 변경을 Debug/x64 Rebuild 로 검증 (per D-19, RESEARCH SC#5).

    명령:
    ```
    msbuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo /t:Rebuild > build_23_w1.log 2>&1
    ```

    PowerShell 환경이면:
    ```powershell
    msbuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo /t:Rebuild *>&1 | Tee-Object -FilePath build_23_w1.log
    ```

    **PASS 기준:**
    - `0 Error(s)`
    - Warning 총 매치 = 6 (Phase 21 baseline, 신규 warning 0):
      - MSB3884 × 2 (MinimumRecommendedRules.ruleset 누락 — pre-existing)
      - CS0162 × 2 (VirtualCamera.cs:266 unreachable code — pre-existing)
      - CS0219 × 2 (VisionAlgorithmService.cs:64 unused 'scanHorizontal' — pre-existing)
    - `bin\x64\Debug\DatumMeasurement.exe` mtime 갱신 확인

    **FAIL 시:**
    - CS0246 (EdgeToLineDistanceMeasurement type 미찾음) → Task 2 csproj 등록 누락 (Pitfall 5) — Task 2 acceptance criteria 재확인
    - CS 신규 error → Task 1 또는 Task 2 코드 수정 (예: out 파라미터와 default param 충돌, 누락 using 등)
    - Warning 신규 추가 → Task 1/2 코드 검토 (CS0219 unused, CS0168 unused exception var 등)
    - 본 task 는 fix-and-retry 가능. msbuild PASS 까지 반복.

    **본 task 는 검증 task — 코드 수정 없음.** build_23_w1.log 는 .log gitignored, commit 안 함.
  </action>
  <verify>
    <automated>findstr /c:"0 Error(s)" build_23_w1.log &amp;&amp; (for /f %a in ('findstr /c:"warning" build_23_w1.log ^| find /c /v ""') do @if %a==6 (echo PASS) else (echo WARN_COUNT=%a EXPECTED=6 &amp; exit /b 1))</automated>
  </verify>
  <acceptance_criteria>
    - `build_23_w1.log` 존재
    - `build_23_w1.log` 내 `0 Error(s)` 정확히 1회 매치 (또는 `Build succeeded`)
    - `build_23_w1.log` 내 warning 매치 정확히 6 (Phase 21 baseline)
    - 신규 warning 0 (Phase 21 baseline 3 unique codes — MSB3884/CS0162/CS0219 외 신규 코드 부재)
    - `bin\x64\Debug\DatumMeasurement.exe` 갱신 (Get-Item mtime 또는 dir 확인)
    - CS0246 부재 (EdgeToLineDistanceMeasurement type 정상 컴파일)
  </acceptance_criteria>
  <done>
    Plan 23-01 의 모든 코드/파일 변경 (Task 1 + Task 2) 이 Debug/x64 빌드 PASS. Phase 21 baseline (6 warning occurrences) 유지, 신규 warning 0. D-19 충족. Wave 2 (Plan 23-02) 진행 가능.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (없음) | Phase 23 신규 코드 = MeasurementBase 파생 + HALCON 헬퍼 시그니처 확장. 외부 입력/세션/네트워크 변경 0. 단일 사용자 데스크탑 WPF 앱. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (없음) | — | — | — | **본 Phase 신규 위협 0건 — Measurement 모듈 내부 + HALCON 헬퍼 시그니처 확장만, 외부 입력/인증/세션/데이터 영속성 변경 0. 기존 try/catch → return false 패턴(CLAUDE.md) 유지로 DoS 위험 없음.** |
</threat_model>

<verification>
- Task 1: VisionAlgorithmService.TryFitLine 시그니처 + measureSel 분기 grep
- Task 2: EdgeToLineDistanceMeasurement.cs 신규 파일 존재 + 7 Edge property + AffineTransPoint2d + -datumRow 부호 반전 + csproj 등록 grep
- Task 3: msbuild Debug/x64 PASS + warning baseline 6 preserved
- 전체 plan: `//260511 hbk Phase 23 ALG-01` 마커 모든 신규/변경 라인 grep (csproj 제외)
</verification>

<success_criteria>
1. `WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs` 존재 + class signature + TypeName 정합
2. 7 Edge property (EdgeThreshold/Sigma/EdgeSampleCount/EdgeTrimCount/EdgePolarity/EdgeDirection/EdgeSelection) 모두 PropertyGrid 노출 가능 (Category + ItemsSourceProperty + Browsable(false) 패턴)
3. TryExecute 가 svc.TryFitLine 의 selection 인자 + AffineTransPoint2d + -datumRow * pixelResolution 호출/계산 포함
4. VisionAlgorithmService.TryFitLine 시그니처에 optional `string selection = "all"` 추가, MeasurePos 호출이 measureSel 변수 사용
5. 5 기존 caller 무수정 (default param 호환)
6. csproj Compile 등록 1줄 추가
7. msbuild Debug/x64 PASS, 신규 warning 0 (Phase 21 baseline 6 보존)
8. `//260511 hbk Phase 23 ALG-01` 마커 grep ≥ 8 매치 (모든 신규/변경 라인)
</success_criteria>

<output>
After completion, create `.planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-01-SUMMARY.md` with:
- Frontmatter (phase, plan, requirements_completed=[ALG-01 partial — measurement infra], provides, affects)
- Performance metrics (duration, files modified, tasks count)
- Accomplishments (3 files modified, msbuild PASS, marker count)
- Task commits (3 commits — Task 1 + Task 2 + verification only Task 3 no-commit)
- Decisions made (TryFitLine optional selection default "all" 채택 / overlay 빈 리스트 / EdgeDirection default TtoB / EdgeSelection default First)
- Carry-overs to Plan 23-02 (MeasurementFactory case 추가 + GrabOrLoadDatumImage TeachingImagePath 분기 + Datum CTH INI seed)
- Self-check passed
</output>
