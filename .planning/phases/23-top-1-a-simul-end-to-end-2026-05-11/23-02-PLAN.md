---
phase: 23
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: true
requirements: [ALG-01]
requirements_addressed: [ALG-01]
tags: [factory, dispatch, teaching-image, simul-mode, wiring]

must_haves:
  truths:
    - "MeasurementFactory.Create('EdgeToLineDistance', owner) 가 새 EdgeToLineDistanceMeasurement 인스턴스를 반환한다"
    - "MeasurementFactory.GetTypeNames() 배열이 'EdgeToLineDistance' 를 포함하여 PropertyGrid 의 EdgeMeasureType ComboBox 와 INI Type dispatch 가 자동 동작한다"
    - "Action_FAIMeasurement.GrabOrLoadDatumImage 가 DatumConfigs[0].TeachingImagePath 가 비어있지 않으면 그 경로를 우선 로드하고, 비어있으면 ShotParam.SimulImagePath 폴백으로 회귀 0"
    - "TeachingImagePath/SimulImagePath 모두 파일 미존재 시 기존 GrabHalconImage 폴백으로 회귀 동작 byte-identical"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs"
      provides: "신규 7번째 case + GetTypeNames 7번째 원소"
      contains: "case \"EdgeToLineDistance\""
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "GrabOrLoadDatumImage TeachingImagePath 우선순위 분기"
      contains: "DatumConfigs[0].TeachingImagePath"
  key_links:
    - from: "MeasurementFactory.Create"
      to: "EdgeToLineDistanceMeasurement constructor"
      via: "switch case \"EdgeToLineDistance\": return new EdgeToLineDistanceMeasurement(owner)"
      pattern: "case \"EdgeToLineDistance\":[\\s\\S]*?new EdgeToLineDistanceMeasurement"
    - from: "FAIConfig.EdgeMeasureType PropertyGrid"
      to: "MeasurementFactory.GetTypeNames"
      via: "FAIConfig L59-60 의 ItemsSource 캐시"
      pattern: "MeasurementFactory\\.GetTypeNames"
    - from: "Action_FAIMeasurement.GrabOrLoadDatumImage"
      to: "DatumConfig.TeachingImagePath"
      via: "parentSeq.DatumConfigs[0].TeachingImagePath 우선 → SimulImagePath 폴백"
      pattern: "DatumConfigs\\[0\\]\\.TeachingImagePath"
---

<objective>
EdgeToLineDistanceMeasurement (Plan 23-01) 를 시스템과 wiring 한다.

목적:
- MeasurementFactory 의 7번째 case + GetTypeNames 7번째 원소 추가로 INI Type=EdgeToLineDistance dispatch + PropertyGrid 드롭다운 자동 노출 (D-13)
- Action_FAIMeasurement.GrabOrLoadDatumImage 에 TeachingImagePath 우선순위 분기 추가 (D-04 Phase 22 carry-over)
- msbuild Debug/x64 PASS 재확인 (신규 warning 0, baseline 6 유지)

산출물:
- 수정 파일 2개: MeasurementFactory.cs (case + GetTypeNames 1줄씩), Action_FAIMeasurement.cs (GrabOrLoadDatumImage 진입부 ~8줄 stacking)

본 plan 의 출력은 Plan 23-03 UAT 에서 검증 — Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-CONTEXT.md
@.planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-RESEARCH.md
@.planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-PATTERNS.md
@.planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-01-PLAN.md
@.planning/phases/22-image-dual-structure/22-CONTEXT.md
@.planning/phases/22-image-dual-structure/22-02-SUMMARY.md
@WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs
@WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
@WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
@WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs

<interfaces>
<!-- Key contracts extracted from existing code. -->

From WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs (L14-44, Allman style):
```csharp
public static MeasurementBase Create(string typeName, object owner)
{
    switch (typeName)
    {
        case "EdgePairDistance":      return new EdgePairDistanceMeasurement(owner);
        case "PointToLineDistance":   return new PointToLineDistanceMeasurement(owner);
        case "PointToPointDistance":  return new PointToPointDistanceMeasurement(owner);
        case "LineToLineAngle":       return new LineToLineAngleMeasurement(owner);
        case "CircleDiameter":        return new CircleDiameterMeasurement(owner);
        case "LineToLineDistance":    return new LineToLineDistanceMeasurement(owner);
        default:                       return null;
    }
}

public static string[] GetTypeNames() //260413 hbk UI ComboBox용
{
    return new string[]
    {
        "EdgePairDistance", "PointToLineDistance", "PointToPointDistance",
        "LineToLineAngle", "CircleDiameter", "LineToLineDistance"
    };
}
```

From WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs §GrabOrLoadDatumImage (L221-242, K&R style — opening brace on same line):
```csharp
//260413 hbk Phase 6: Datum 이미지 취득 — Dedicated만 우선 지원 (D-07, D-08)
// ReuseFromShot 모드는 향후 Plan 04 UI 작업과 함께 구현.
private HImage GrabOrLoadDatumImage(InspectionSequence parentSeq) {
    if (ShotParam == null) return null;
    HImage image = null;
    //260511 hbk Phase 22 IMG-02 — Datum 찾기 단계의 이미지 = InspectionImagePath (= ShotParam.SimulImagePath) 사용. TeachingImagePath (DatumConfig 보존) 는 본 메서드에서 미참조 — 재티칭/UI 셋업 경로에서만 참조 (Phase 23 carry-over 가능).
    #if SIMUL_MODE
    if (!string.IsNullOrEmpty(ShotParam.SimulImagePath) && File.Exists(ShotParam.SimulImagePath)) {
        try {
            image = new HImage(ShotParam.SimulImagePath);
        } catch {
            image = null;
        }
    }
    if (image == null) {
        image = SystemHandler.Handle.Devices.GrabHalconImage(ShotParam);
    }
    #else
    image = SystemHandler.Handle.Devices.GrabHalconImage(ShotParam);
    #endif
    return image;
}
```

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (Phase 22 IMG-01):
```csharp
[Category("Datum|ImageSource")] //260511 hbk Phase 22 IMG-01
public string TeachingImagePath { get; set; } = "";
```

From WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (DatumConfigs collection 노출):
```csharp
public List<DatumConfig> DatumConfigs { get; }  // 또는 protected/internal — 실 시그니처는 read_first 에서 확인
```
</interfaces>

<memory>
- memory/feedback_comment_convention.md — `//260511 hbk Phase 23 ALG-01` 마커, Phase 20 D-12 stacking 패턴 (기존 마커 보존 + 위에 누적)
- Plan 23-01: TryFitLine 시그니처 확장 + EdgeToLineDistanceMeasurement.cs 신규 + csproj 등록 완료
</memory>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: MeasurementFactory — add 7th case + GetTypeNames 7th element</name>
  <files>WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs (전체 — L14-44 switch + GetTypeNames)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs (Plan 23-01 결과물 — class name 확정)
    - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs §L59-60 (EdgeMeasureType ItemsSource = MeasurementFactory.GetTypeNames 캐시 — RESEARCH Finding #4)
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-PATTERNS.md §File 2 (Factory 패턴)
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-CONTEXT.md D-06 (7번째 알고리즘 등록), D-13 (INI + UI 둘 다 채널 자동 충족)
  </read_first>
  <action>
    MeasurementFactory.cs 에 EdgeToLineDistance dispatch 등록 (per D-06, D-13, PATTERNS File 2).

    **변경 1 — switch case 7번째 추가:** `LineToLineDistance` case 와 `default` 사이에 한 라인 삽입.
    ```csharp
    case "EdgeToLineDistance":
        return new EdgeToLineDistanceMeasurement(owner); //260511 hbk Phase 23 ALG-01
    ```

    **변경 2 — GetTypeNames() 배열에 7번째 원소 추가:** `"LineToLineDistance"` 다음에 `"EdgeToLineDistance"` 추가. 이전 라인 끝에 콤마 보충 필요 시 추가.
    ```csharp
    return new string[]
    {
        "EdgePairDistance",
        "PointToLineDistance",
        "PointToPointDistance",
        "LineToLineAngle",
        "CircleDiameter",
        "LineToLineDistance",
        "EdgeToLineDistance" //260511 hbk Phase 23 ALG-01
    };
    ```

    **주의:**
    - 기존 6 case 와 default 무수정 (회귀 0 보장)
    - Style = Allman (analog L11, L14 일치)
    - 변경 라인에 `//260511 hbk Phase 23 ALG-01` 마커 부착
    - FAIConfig.cs 무수정 — ItemsSource 가 GetTypeNames() 단일 소스이므로 자동 갱신 (RESEARCH Finding #4 의 핵심 가치)
  </action>
  <verify>
    <automated>findstr /n /c:"case \"EdgeToLineDistance\":" WPF_Example\Custom\Sequence\Inspection\MeasurementFactory.cs &amp;&amp; findstr /n /c:"new EdgeToLineDistanceMeasurement(owner)" WPF_Example\Custom\Sequence\Inspection\MeasurementFactory.cs &amp;&amp; findstr /n /c:"\"EdgeToLineDistance\"" WPF_Example\Custom\Sequence\Inspection\MeasurementFactory.cs &amp;&amp; findstr /n /c:"260511 hbk Phase 23 ALG-01" WPF_Example\Custom\Sequence\Inspection\MeasurementFactory.cs</automated>
  </verify>
  <acceptance_criteria>
    - `case "EdgeToLineDistance":` 정확히 1회 매치
    - `return new EdgeToLineDistanceMeasurement(owner);` 정확히 1회 매치 (switch 본문)
    - `"EdgeToLineDistance"` 문자열 grep ≥ 2 매치 (case label + GetTypeNames 배열 원소)
    - GetTypeNames 반환 배열 length 7 (코드 검사 — 6 → 7로 증가)
    - 기존 6 case (`EdgePairDistance`, `PointToLineDistance`, `PointToPointDistance`, `LineToLineAngle`, `CircleDiameter`, `LineToLineDistance`) 모두 unchanged grep 매치
    - `default: return null;` (또는 동등) 보존
    - `//260511 hbk Phase 23 ALG-01` 마커 grep ≥ 2 매치 (case 라인 + 배열 원소 라인)
    - **D-09 정밀도 검증 (0.001mm = F3 format):** MeasurementResultRow.cs 의 MeasuredValue 표시 포맷이 `F3` 사용 — `findstr /c:"ToString(""F3"")" WPF_Example\UI\ViewModel\MeasurementResultRow.cs` ≥ 2 매치 (L54 ResultDisplay + L60 MeasuredValueText 확인). **기존 코드에 이미 F3 적용 완료 (Phase 6 Plan 04, 260417)** — Phase 23 신규 코드 변경 없이 D-09 충족. 매치 < 2 시 carry-over 등록 (별도 quick task).
  </acceptance_criteria>
  <done>
    MeasurementFactory 가 INI Type=EdgeToLineDistance 와 PropertyGrid 드롭다운 모두에 EdgeToLineDistance 를 노출한다. FAIConfig.EdgeMeasureType 자동 갱신 (코드 변경 0). D-13 (INI 직접 편집 + UI 'Add FAI' 둘 다 채널) 자동 충족.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Action_FAIMeasurement.GrabOrLoadDatumImage — add TeachingImagePath fallback branch</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (§GrabOrLoadDatumImage L221-242 전체 + 기존 Phase 22 IMG-02 주석 L226)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (DatumConfigs collection 시그니처 — public/protected/internal 확인)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs §TeachingImagePath (Phase 22 IMG-01 결과 — public string 속성 + EnsurePerRoiDefaults null 가드)
    - .planning/phases/22-image-dual-structure/22-02-SUMMARY.md (Phase 22 IMG-02 carry-over 명시)
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-PATTERNS.md §File 3 (Action_FAIMeasurement K&R style + stacking 패턴)
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-RESEARCH.md §Code Examples Pattern 4 + Pitfall 3
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-CONTEXT.md D-04 (TeachingImagePath 자동 로드 구현, Phase 22 carry-over)
  </read_first>
  <action>
    Action_FAIMeasurement.cs §GrabOrLoadDatumImage (L221-242) 진입부에 TeachingImagePath 우선순위 분기 추가 (per D-04, RESEARCH Pattern 4, Pitfall 3).

    **변경 후 본문 (K&R style 유지, 기존 Phase 22 IMG-02 주석 L226 보존 + 위에 stacking):**
    ```csharp
    //260413 hbk Phase 6: Datum 이미지 취득 — Dedicated만 우선 지원 (D-07, D-08)
    // ReuseFromShot 모드는 향후 Plan 04 UI 작업과 함께 구현.
    private HImage GrabOrLoadDatumImage(InspectionSequence parentSeq) {
        if (ShotParam == null) return null;
        HImage image = null;
        //260511 hbk Phase 23 ALG-01 — D-04 TeachingImagePath 자동 로드 (Phase 22 carry-over). 비어있지 않으면 우선, 비어있으면 SimulImagePath 폴백.
        //260511 hbk Phase 22 IMG-02 — Datum 찾기 단계의 이미지 = InspectionImagePath (= ShotParam.SimulImagePath) 사용. TeachingImagePath (DatumConfig 보존) 는 본 메서드에서 미참조 — 재티칭/UI 셋업 경로에서만 참조 (Phase 23 carry-over 가능).
        string teachingPath = null; //260511 hbk Phase 23 ALG-01
        if (parentSeq != null && parentSeq.DatumConfigs != null && parentSeq.DatumConfigs.Count > 0) { //260511 hbk Phase 23 ALG-01
            teachingPath = parentSeq.DatumConfigs[0].TeachingImagePath;
        }
        #if SIMUL_MODE
        if (!string.IsNullOrEmpty(teachingPath) && File.Exists(teachingPath)) { //260511 hbk Phase 23 ALG-01 — TeachingImagePath 우선 (Pitfall 3 - 2-step 가드)
            try {
                image = new HImage(teachingPath);
            } catch {
                image = null;
            }
        }
        if (image == null && !string.IsNullOrEmpty(ShotParam.SimulImagePath) && File.Exists(ShotParam.SimulImagePath)) { //260511 hbk Phase 23 ALG-01 — SimulImagePath 폴백 (회귀 0)
            try {
                image = new HImage(ShotParam.SimulImagePath);
            } catch {
                image = null;
            }
        }
        if (image == null) {
            image = SystemHandler.Handle.Devices.GrabHalconImage(ShotParam);
        }
        #else
        image = SystemHandler.Handle.Devices.GrabHalconImage(ShotParam);
        #endif
        return image;
    }
    ```

    **주의:**
    - **K&R style 강제** — opening brace on same line. analog L223 `private HImage GrabOrLoadDatumImage(InspectionSequence parentSeq) {` 패턴 일치.
    - **기존 Phase 22 IMG-02 주석 (L226 위) 보존** + 위에 Phase 23 ALG-01 주석 stacking (Phase 20 D-12 패턴). 두 주석 모두 존재.
    - **Pitfall 3 가드:** `!string.IsNullOrEmpty(teachingPath) && File.Exists(teachingPath)` 2-step. EnsurePerRoiDefaults 가 null 가드만 처리하므로 빈 문자열도 검사.
    - **parentSeq.DatumConfigs 멤버 시그니처 확인** — `DatumConfigs` 가 `List<DatumConfig>` 면 `.Count` + `[0]` 접근 가능. read_first 에서 InspectionSequence.cs 확인 후 시그니처 일치하지 않으면 (예: 접근 제한자 / collection type 차이) 적합한 방식으로 조정.
    - **회귀 0 검증 경로:** TeachingImagePath="" 인 경우 (Phase 22 EnsurePerRoiDefaults default) → 첫 분기 skip → SimulImagePath 폴백 진입 → 기존 동작 byte-identical (Phase 22 UAT Test 3 회귀 검증 대응).
    - 모든 신규 라인에 `//260511 hbk Phase 23 ALG-01` 마커 부착.
  </action>
  <verify>
    <automated>findstr /n /c:"DatumConfigs[0].TeachingImagePath" WPF_Example\Custom\Sequence\Inspection\Action_FAIMeasurement.cs &amp;&amp; findstr /n /c:"teachingPath" WPF_Example\Custom\Sequence\Inspection\Action_FAIMeasurement.cs &amp;&amp; findstr /n /c:"new HImage(teachingPath)" WPF_Example\Custom\Sequence\Inspection\Action_FAIMeasurement.cs &amp;&amp; findstr /n /c:"new HImage(ShotParam.SimulImagePath)" WPF_Example\Custom\Sequence\Inspection\Action_FAIMeasurement.cs &amp;&amp; findstr /n /c:"260511 hbk Phase 22 IMG-02" WPF_Example\Custom\Sequence\Inspection\Action_FAIMeasurement.cs &amp;&amp; findstr /n /c:"260511 hbk Phase 23 ALG-01" WPF_Example\Custom\Sequence\Inspection\Action_FAIMeasurement.cs</automated>
  </verify>
  <acceptance_criteria>
    - GrabOrLoadDatumImage 본문에 `parentSeq.DatumConfigs[0].TeachingImagePath` (또는 동등 `parentSeq.DatumConfigs.First()` 등 컬렉션 접근) 1회 매치
    - 본문에 `teachingPath` 로컬 변수 선언 + 사용 grep ≥ 3 매치
    - 본문에 `new HImage(teachingPath)` 호출 1회 (try/catch 보호)
    - 본문에 `new HImage(ShotParam.SimulImagePath)` 호출 여전히 존재 (회귀 0 검증 — Phase 22 baseline 보존)
    - 본문에 `!string.IsNullOrEmpty(teachingPath) && File.Exists(teachingPath)` 2-step 가드 (Pitfall 3)
    - 본문에 `image == null && !string.IsNullOrEmpty(ShotParam.SimulImagePath)` 폴백 가드 (회귀 0 경로)
    - 본문에 `image == null` 마지막 가드 (GrabHalconImage 호출) 보존
    - 기존 마커 `//260511 hbk Phase 22 IMG-02` 보존 (grep 1회 매치 — Phase 20 D-12 stacking)
    - 신규 마커 `//260511 hbk Phase 23 ALG-01` 본 메서드 내 grep ≥ 4 매치
    - 메서드 시그니처 `private HImage GrabOrLoadDatumImage(InspectionSequence parentSeq) {` (K&R brace) 보존
  </acceptance_criteria>
  <done>
    GrabOrLoadDatumImage 가 TeachingImagePath 우선 → SimulImagePath 폴백 → GrabHalconImage 폴백 3단계 chain 으로 동작. Phase 22 IMG-02 (SimulImagePath 분기) byte-identical 보존 — TeachingImagePath="" 시 회귀 0. Phase 22 carry-over 해소.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: msbuild Debug/x64 Rebuild PASS verification (post-wiring)</name>
  <files>
    build_23_w2.log (workspace root, gitignored — verification artifact only, no commit)
  </files>
  <read_first>
    - .planning/phases/22-image-dual-structure/22-02-SUMMARY.md §Build Verification — Phase 21 baseline 6 warning occurrences
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-CONTEXT.md D-19
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-01-SUMMARY.md (Plan 23-01 build_23_w1.log 결과 — baseline 동일 6 warnings)
  </read_first>
  <action>
    Plan 23-01 + Plan 23-02 누적 변경을 Debug/x64 Rebuild 로 검증 (per D-19).

    명령 (Plan 23-01 Task 3 와 동일, 로그 파일명만 변경):
    ```
    msbuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo /t:Rebuild > build_23_w2.log 2>&1
    ```

    **PASS 기준:**
    - `0 Error(s)`
    - Warning 매치 = 6 (Phase 21 baseline, 신규 warning 0)
    - `bin\x64\Debug\DatumMeasurement.exe` mtime 갱신

    **FAIL 시:**
    - CS0117 (`DatumConfigs` 멤버 없음) → Task 2 collection 접근 시그니처 mismatch — InspectionSequence.cs read 후 적절히 조정
    - 신규 warning → Task 1/Task 2 코드 검토 (CS0219 unused 등)
    - 본 task fix-and-retry 가능.

    **본 task 는 검증 task — 코드 수정 없음.** build_23_w2.log = .log gitignored.
  </action>
  <verify>
    <automated>findstr /c:"0 Error(s)" build_23_w2.log &amp;&amp; (for /f %a in ('findstr /c:"warning" build_23_w2.log ^| find /c /v ""') do @if %a==6 (echo PASS) else (echo WARN_COUNT=%a EXPECTED=6 &amp; exit /b 1))</automated>
  </verify>
  <acceptance_criteria>
    - `build_23_w2.log` 존재
    - `build_23_w2.log` 내 `0 Error(s)` (또는 `Build succeeded`) 정확히 1회
    - `build_23_w2.log` 내 warning 매치 정확히 6 (Phase 21 baseline 유지)
    - 신규 warning code (MSB3884/CS0162/CS0219 외) 부재
    - `bin\x64\Debug\DatumMeasurement.exe` 갱신
  </acceptance_criteria>
  <done>
    Plan 23-01 + Plan 23-02 누적 변경이 Debug/x64 빌드 PASS. Phase 21 baseline (6 warning occurrences) 유지, 신규 warning 0. D-19 충족. Wave 3 (Plan 23-03 UAT) 진행 가능.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system → HImage | TeachingImagePath / SimulImagePath 두 경로 모두 `File.Exists` + `try { new HImage(...) } catch` 가드 (CLAUDE.md "HImage 객체 dispose" 규칙 + V5 Input Validation 충족 — 기존 패턴 byte-identical) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | Action_FAIMeasurement.GrabOrLoadDatumImage 의 TeachingImagePath 입력 | accept | 단일 사용자 데스크탑 앱 + INI 경로는 사용자가 직접 편집. `File.Exists` + `try/catch new HImage` 가드로 잘못된 경로 → null fallback → SimulImagePath → GrabHalconImage chain. 실 위협 낮음, 기존 패턴 동일. (RESEARCH Security Domain 참조) |
| T-23-02 | DoS | Halcon HImage 생성자 예외 | mitigate | `try { new HImage(...) } catch { image = null; }` 가드 (CLAUDE.md "Wrap all HOperatorSet.* in try/catch → return false" 패턴). 시퀀스 스레드 크래시 방지. Phase 22 기존 분기와 byte-identical. |

**나머지 STRIDE (Spoofing/Repudiation/Information Disclosure/Elevation of Privilege):** 본 Phase 신규 위협 0건 — 인증/세션/네트워크 표면 변경 없음.
</threat_model>

<verification>
- Task 1: MeasurementFactory `case "EdgeToLineDistance":` + `"EdgeToLineDistance"` 배열 원소 grep
- Task 2: Action_FAIMeasurement `DatumConfigs[0].TeachingImagePath` + 2-step 가드 + 회귀 0 폴백 chain grep + Phase 22 IMG-02 마커 보존 grep
- Task 3: msbuild Debug/x64 PASS + baseline 6 warning 유지
- 전체 plan: `//260511 hbk Phase 23 ALG-01` 마커 모든 신규/변경 라인 grep
</verification>

<success_criteria>
1. MeasurementFactory.Create("EdgeToLineDistance", owner) → EdgeToLineDistanceMeasurement 인스턴스 (코드 검사)
2. MeasurementFactory.GetTypeNames() 7개 원소 반환, "EdgeToLineDistance" 포함 (코드 검사)
3. Action_FAIMeasurement.GrabOrLoadDatumImage 가 TeachingImagePath 우선 → SimulImagePath → GrabHalconImage chain (3단계 폴백)
4. TeachingImagePath="" 시 SimulImagePath 분기로 byte-identical 회귀 (Pitfall 3 가드)
5. 기존 Phase 22 IMG-02 마커 보존 (Phase 20 D-12 stacking 패턴)
6. msbuild Debug/x64 PASS, 신규 warning 0 (Phase 21 baseline 6 유지)
7. `//260511 hbk Phase 23 ALG-01` 마커 grep ≥ 6 매치 (Factory 2 + Action_FAIMeasurement 4+)
</success_criteria>

<output>
After completion, create `.planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-02-SUMMARY.md` with:
- Frontmatter (phase, plan, requirements_completed=[ALG-01 wiring complete — pending UAT], provides, affects)
- Performance metrics
- Accomplishments (Factory 2줄 + Action_FAIMeasurement 분기 + build PASS)
- Task commits (3 commits — Task 1, Task 2, Task 3 no-commit verification)
- Decisions made (DatumConfigs[0] 첫 번째만 채택 per RESEARCH A6 / 2-step 가드 / K&R + Allman 혼재 유지)
- Carry-overs to Plan 23-03 (Simul UAT 5 SC scenarios + Datum CTH INI seed + A1~A5 ROI 사용자 셋업 + A6 확장성 검증)
- Self-check passed
</output>
