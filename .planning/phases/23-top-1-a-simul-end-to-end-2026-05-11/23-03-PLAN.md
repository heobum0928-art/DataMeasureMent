---
phase: 23
plan: 03
type: execute
wave: 3
depends_on: [23-01, 23-02]
files_modified:
  - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-UAT.md
autonomous: false
requirements: [ALG-01]
requirements_addressed: [ALG-01]
tags: [uat, sign-off, simul-mode, end-to-end, datum-cth, a-series, extensibility]

must_haves:
  truths:
    - "Simul 이미지 로드 → Datum CTH 자동 찾기 → A1~A5 EdgeToLineDistance 측정값(mm) UI 표시가 오류 없이 완주 (SC#1)"
    - "A1~A5 OK/NG 판정 + InspectionListView strip 녹/적 시각화 동작 (SC#2 — 사용자 확인 + Pitfall 4 명확화)"
    - "TeachingImagePath ≠ InspectionImagePath 분리 동작 + 두 경로 동일 파일 시에도 회귀 0 (SC#3 — Phase 22 carry-over 검증)"
    - "A6 추가 INI 직접 편집 + UI 'Add FAI' 버튼 둘 다 채널로 검증, 코드 변경 0 으로 확장 가능 (SC#4)"
    - "msbuild Debug/x64 PASS, 신규 warning 0 (Phase 21 baseline 6 유지) (SC#5)"
    - "사용자 사인오프 완료, 23-UAT.md status: signed_off, Phase 23 SIGNED_OFF"
  artifacts:
    - path: ".planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-UAT.md"
      provides: "Phase 23 sign-off 문서 (5 scenarios, 사용자 검증 결과)"
      contains: "status: signed_off"
  key_links:
    - from: "사용자 셋업 (DatumConfig CTH INI + A1~A5 FAI INI + Simul 이미지)"
      to: "Simul 실행 → Datum 자동 찾기 → A1~A5 측정값"
      via: "DataMeasurement.exe 기동 + InspectionListView 펼침 + 측정 실행"
      pattern: "UAT manual flow"
    - from: "23-UAT.md status: pending"
      to: "23-UAT.md status: signed_off"
      via: "사용자 5/5 PASS 또는 N/X PASS + carry-over"
      pattern: "status:\\s*signed_off"
---

<objective>
Phase 23 (Top #1 A시리즈 Simul end-to-end) 의 5 Success Criteria 를 Simul 환경 + 정적 검증 조합으로 검증하고 사용자 사인오프를 받는다.

목적:
- 사용자가 Datum CTH INI + A1~A5 FAI INI + Simul 이미지를 사전 셋업
- 5 SC scenarios (SC#1 end-to-end / SC#2 OK/NG strip / SC#3 TeachingImagePath 분리 / SC#4 A6 확장성 / SC#5 msbuild) 인터랙티브 UAT
- 23-UAT.md 사용자 사인오프 (Phase 22 패턴 일치)
- Phase 23 SIGNED_OFF + ALG-01 충족

산출물:
- 신규 문서 1개: 23-UAT.md (frontmatter pending → signed_off, 5 시나리오 본문 + Summary + Carry-overs + Sign-off 블록)

본 plan 은 Wave 3 final — Phase 23 종료 조건.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-CONTEXT.md
@.planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-RESEARCH.md
@.planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-VALIDATION.md
@.planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-01-PLAN.md
@.planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-02-PLAN.md
@.planning/phases/22-image-dual-structure/22-UAT.md
@WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs
@WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs
@WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs

<interfaces>
<!-- 23-UAT.md frontmatter template (Phase 22 patron) -->

From .planning/phases/22-image-dual-structure/22-UAT.md (patron, frontmatter):
```yaml
---
phase: 22
plan: 02
status: signed_off              # initially "pending"
total: 4
passed: 4
pending: 0
sign_off_reviewer: heobum0928
sign_off_date: 2026-05-11
---
```

Body sections (Phase 22 pattern):
1. # Phase XX — Human UAT
2. ## Pre-conditions
3. ## Test 1 ~ Test N (scenario, steps, expected, actual, result, notes)
4. ## Summary table
5. ## Carry-overs
6. ## Sign-off block
</interfaces>

<memory>
- Phase 22 UAT 패턴 — 4 scenarios + trust-based PASS 옵션 (코드 변경 0 영역) + carry-over 별도 quick task 분리
- Phase 17 partial sign-off — 부분 PASS 도 합법 (FAIL 시 carry-over → 후속 phase)
- Validation 사용자 직접 검증 (자동화 테스트 프레임워크 없음)
</memory>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold 23-UAT.md with 5 scenarios (status: pending)</name>
  <files>.planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-UAT.md</files>
  <read_first>
    - .planning/phases/22-image-dual-structure/22-UAT.md (전체 — frontmatter + 4 시나리오 + Sign-off 블록 양식)
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-CONTEXT.md §Phase Boundary + D-12/D-13/D-17 (SC#4 검증 방식, FAI 추가 채널, UI 결과 표시)
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-VALIDATION.md §Manual-Only Verifications (5 SC scenarios 매핑)
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-RESEARCH.md §Open Questions (SC#2 strip 시각화 모호성 + Pitfall 4)
  </read_first>
  <action>
    23-UAT.md 신규 파일 생성 — Phase 22 UAT 패턴 일치.

    **Frontmatter (초기 status: pending):**
    ```yaml
    ---
    phase: 23
    plan: 03
    status: pending
    total: 5
    passed: 0
    pending: 5
    requirement_ids: [ALG-01]
    sign_off_reviewer: ""
    sign_off_date: ""
    ---
    ```

    **본문 구조 — 5 scenarios:**

    ### Pre-conditions (사용자 사전 셋업, D-16 + D-01 + D-13)
    - `D:\TestImg\Datameasurement\` 디렉토리에 Simul 이미지 (PPT 도면 반영본) 비치
    - DatumConfig 1개 작성 — AlgorithmType = `CircleTwoHorizontal` (per D-01 lock)
      - B1 홀 Circle ROI + 2 horizontal tangent line ROI 입력
      - TeachingImagePath = Simul 이미지 경로 또는 빈 문자열 (SC#3 검증용)
    - FAI A1~A5 5개 작성 — EdgeMeasureType = `EdgeToLineDistance` (Plan 23-02 Factory 등록 후 자동 노출)
      - 각각 Point ROI Rectangle 1개 + 7 Edge 파라미터 + NominalValue + UpperTolerance + LowerTolerance
      - 모두 InspectionRecipeManager (IsDynamicFAIMode) 의 기존 INI 포맷 (D-15)
    - msbuild Debug/x64 빌드 산출물 `bin\x64\Debug\DatumMeasurement.exe` 존재 (Plan 23-02 Task 3 출력)

    ### Test 1 — SC#1 Simul end-to-end 완주 (ALG-01)
    - **Scenario:** Simul 이미지 로드 → Datum CTH 자동 찾기 → A1~A5 EdgeToLineDistance 측정값(mm) UI 표시가 오류 없이 완주
    - **Steps:**
      1. DataMeasurement.exe 기동
      2. Recipe 선택 (Pre-conditions 의 INI)
      3. Inspection 시퀀스 1회 실행 (수동 트리거 또는 TCP 명령)
      4. InspectionListView TreeView 펼침 (Datum + A1~A5 노드 가시)
      5. 각 A1~A5 노드의 측정값 컬럼 (mm 단위, 0.001 정밀도 per D-09) 표시 확인
    - **Expected:**
      - 시퀀스 Error 상태 진입 없음 (모든 5 측정 정상 완주)
      - A1~A5 5개 측정값 표시 (mm)
      - 정밀도 3자릿 (예: `12.345 mm`) **(format = F3 확인 완료 — MeasurementResultRow.cs L54/L60 `ToString("F3")` 기존 적용, 23-02 Task 1 acceptance grep)**
      - +Y 부호 적용 확인 (Datum B 위쪽 측정점 = 양수, D-02)
    - **Actual:** (사용자 작성)
    - **Result:** ⬜ pending (PASS/FAIL/PARTIAL 채울 것)
    - **Notes:** (사용자 작성)

    ### Test 2 — SC#2 OK/NG strip 녹/적 (ALG-01)
    - **Scenario:** A1~A5 각각의 측정값 ↔ NominalValue/Tolerance 공차 비교 → OK/NG 판정 → InspectionListView 노드 색상 분기 (CO-05 녹/적)
    - **Steps:**
      1. Test 1 의 시퀀스 실행 결과 유지
      2. NominalValue 또는 Tolerance 를 의도적으로 다르게 설정 → A1~A5 중 일부 OK 일부 NG 만들기
      3. 재실행 → InspectionListView 5 노드 색상 시각 확인
      4. Halcon viewer 오버레이 영역 확인 (RESEARCH Pitfall 4 명확화 — overlay 빈 리스트 채택으로 viewer 라인 미표시 가능, **InspectionListView 노드 색상**으로 분기 검증)
    - **Expected:**
      - OK 판정 노드 = 녹색, NG 판정 노드 = 빨강 (CO-05 패턴 — InspectionListView)
      - Halcon viewer 의 strip 색상 라인은 EdgeToLineDistance overlay 미생성 정책 (Plan 23-01 결정 — PointToLineDistance 패턴 일치) 으로 표시 안 될 수 있음. **trust-based PASS 옵션** — 노드 색상이 정확히 분기되면 PASS, viewer 라인 부재 시 carry-over 등록.
    - **Actual:** (사용자 작성)
    - **Result:** ⬜ pending
    - **Notes:** Pitfall 4 인용 — overlay 추가는 별도 backlog

    ### Test 3 — SC#3 TeachingImagePath ≠ InspectionImagePath 분리 (ALG-01)
    - **Scenario:** TeachingImagePath 와 ShotConfig.SimulImagePath (= InspectionImagePath) 가 다른 파일을 가리켜도 정상 동작, 같은 파일이어도 회귀 0
    - **Steps:**
      1. **Case A (분리):** DatumConfig.TeachingImagePath = `teaching.bmp`, ShotConfig.SimulImagePath = `inspection.bmp` (다른 파일) → 시퀀스 실행 → Datum 찾기는 teaching.bmp 로, A1~A5 측정은 inspection.bmp 로 (Plan 23-02 Task 2 분기 확인)
      2. **Case B (동일):** 두 경로 모두 동일 파일 → 시퀀스 실행 → 회귀 0 (Phase 22 UAT Test 2 패턴)
      3. **Case C (TeachingImagePath 빈 문자열):** TeachingImagePath = `""` → SimulImagePath 폴백으로 Phase 22 baseline byte-identical
    - **Expected:**
      - Case A: Datum 검출 = teaching.bmp 의 좌표, FAI 측정 = inspection.bmp 의 좌표 (각각 분리 동작)
      - Case B: SC#1 동작 동일 (회귀 0)
      - Case C: Phase 22 동작 byte-identical (회귀 0)
    - **Actual:** (사용자 작성)
    - **Result:** ⬜ pending
    - **Notes:** Case A 가 실 시나리오, Case B/C 는 회귀 검증

    ### Test 4 — SC#4 A6 확장성 (D-12 + D-13)
    - **Scenario:** A6 1개 추가가 INI 직접 편집 + UI 'Add FAI' 버튼 두 채널 모두로 가능, 코드 변경 0 으로 확장성 검증
    - **Steps:**
      1. **Channel A (INI 직접 편집):** 프로그램 종료 → INI 파일에 A6 섹션 추가 (FAIName="A6", EdgeMeasureType="EdgeToLineDistance", ROI/Edge 파라미터 입력) → 재기동 → InspectionListView 에 A6 노드 표시 확인 + 시퀀스 실행 시 A6 측정값 출력 확인
      2. **Channel B (UI 'Add FAI'):** InspectionListView 의 Shot 노드 우클릭 또는 'Add FAI' 버튼 → FAIConfig 신규 생성 → EdgeMeasureType ComboBox 에서 `EdgeToLineDistance` 선택 가능 확인 (Plan 23-02 Task 1 GetTypeNames 반영) → ROI/Edge 파라미터 입력 → 시퀀스 실행 → A6 측정값 출력
    - **Expected:**
      - Channel A + B 둘 다 A6 측정값 정상 표시 (= 6개 측정값)
      - EdgeMeasureType ComboBox 의 7번째 항목 = "EdgeToLineDistance" 가시
      - 코드 추가 0 (Phase 5 IsDynamicFAIMode 인프라 + Plan 23-02 Factory 자동 노출)
    - **Actual:** (사용자 작성)
    - **Result:** ⬜ pending
    - **Notes:** D-14 확장 한계 = A23 까지 보장, 검증은 A6 1개

    ### Test 5 — SC#5 msbuild Debug/x64 PASS (D-19)
    - **Scenario:** Plan 23-01 + 23-02 누적 변경 후 msbuild Debug/x64 Rebuild 에서 0 errors + 신규 warning 0 (Phase 21 baseline 6 유지)
    - **Steps:**
      1. `msbuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo /t:Rebuild > build_23_w3.log 2>&1`
      2. log 파일 점검 — error 0, warning 6 (MSB3884×2 + CS0162×2 + CS0219×2)
    - **Expected:**
      - `0 Error(s)`
      - Warning 매치 정확히 6 (Phase 21 baseline)
      - 신규 warning code 부재
    - **Actual:** (build_23_w3.log 인용)
    - **Result:** ⬜ pending — Task 2 가 자동 실행/검증
    - **Notes:** Plan 23-02 Task 3 의 build_23_w2.log 와 동일 결과 기대 (단, Plan 23-03 시점 재검증)

    ### Summary (frontmatter 와 동기화)
    | # | Scenario | Result | Notes |
    |---|----------|--------|-------|
    | 1 | SC#1 Simul end-to-end | ⬜ pending | — |
    | 2 | SC#2 OK/NG strip | ⬜ pending | — |
    | 3 | SC#3 TeachingImagePath 분리 | ⬜ pending | — |
    | 4 | SC#4 A6 확장성 (INI + UI) | ⬜ pending | — |
    | 5 | SC#5 msbuild PASS | ⬜ pending | — |

    ### Carry-overs (초기 빈 — Task 3 사인오프 시 채워짐)

    ### Sign-off (초기 빈)
    - Reviewer:
    - Date:
    - Status: pending

    **주의:**
    - Phase 22 UAT 와 동일 frontmatter schema. status, total, passed, pending 필드.
    - Test 항목별 ⬜/✅/❌ 상태 표시.
    - 본 task 는 scaffold — 사인오프 (Test 결과 채움) 는 Task 3 (checkpoint).
  </action>
  <verify>
    <automated>findstr /c:"phase: 23" .planning\phases\23-top-1-a-simul-end-to-end-2026-05-11\23-UAT.md &amp;&amp; findstr /c:"total: 5" .planning\phases\23-top-1-a-simul-end-to-end-2026-05-11\23-UAT.md &amp;&amp; findstr /c:"Test 1" .planning\phases\23-top-1-a-simul-end-to-end-2026-05-11\23-UAT.md &amp;&amp; findstr /c:"Test 5" .planning\phases\23-top-1-a-simul-end-to-end-2026-05-11\23-UAT.md &amp;&amp; findstr /c:"status: pending" .planning\phases\23-top-1-a-simul-end-to-end-2026-05-11\23-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 파일 `.planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-UAT.md` 존재
    - Frontmatter 에 `phase: 23`, `plan: 03`, `status: pending`, `total: 5`, `passed: 0`, `pending: 5`, `requirement_ids: [ALG-01]` 모두 존재
    - 본문에 `Test 1`, `Test 2`, `Test 3`, `Test 4`, `Test 5` 각각 1회 이상 매치
    - 본문에 `SC#1`, `SC#2`, `SC#3`, `SC#4`, `SC#5` 또는 각 SC 명칭 매핑 매치
    - Pre-conditions 섹션 존재 + `CircleTwoHorizontal` 명시 (D-01 lock)
    - Summary 표 5행 모두 `⬜ pending`
    - Sign-off 블록 존재 + `Status: pending`
  </acceptance_criteria>
  <done>
    23-UAT.md scaffold 완료. 5 SC scenarios 각각 명확한 Steps + Expected + Actual/Result 슬롯 보유. 사용자가 인터랙티브 검증 후 채울 준비 완료. 본 task 는 자동, 다음 Task 2 (msbuild 자동) → Task 3 (사용자 checkpoint).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: msbuild Debug/x64 Rebuild PASS (SC#5 자동 검증) + log 23-UAT.md Test 5 Result 갱신</name>
  <files>
    build_23_w3.log (workspace root, gitignored)
    .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-UAT.md (Test 5 Actual/Result 갱신)
  </files>
  <read_first>
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-UAT.md (Task 1 결과 — Test 5 슬롯)
    - .planning/phases/22-image-dual-structure/22-02-SUMMARY.md §Build Verification (baseline 인용)
  </read_first>
  <action>
    SC#5 (msbuild) 자동 검증 — Phase 22/23-01/23-02 Task 3 와 동일 패턴.

    **명령:**
    ```
    msbuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo /t:Rebuild > build_23_w3.log 2>&1
    ```

    **검증 + 23-UAT.md 갱신:**
    - `0 Error(s)` + warning 매치 6 → 23-UAT.md Test 5 의 Actual = "build_23_w3.log: 0 errors, 6 warnings (MSB3884×2 + CS0162×2 + CS0219×2 = Phase 21 baseline)", Result = `✅ PASS`
    - FAIL 시 Actual = 에러 메시지 + log 인용, Result = `❌ FAIL` — Task 3 checkpoint 에서 사용자 결정 (carry-over vs fix-and-retry)

    **23-UAT.md Summary 표의 Test 5 행도 동기화 갱신** (`⬜ pending` → `✅ PASS` 또는 `❌ FAIL`).
    Frontmatter `passed` 카운트 +1 (PASS 시), `pending` -1.

    **본 task 는 SC#5 만 자동.** SC#1-4 는 사용자 수동 (Task 3 checkpoint).
  </action>
  <verify>
    <automated>findstr /c:"0 Error(s)" build_23_w3.log &amp;&amp; (for /f %a in ('findstr /c:"warning" build_23_w3.log ^| find /c /v ""') do @if %a==6 (echo PASS) else (echo WARN_COUNT=%a EXPECTED=6 &amp; exit /b 1)) &amp;&amp; findstr /c:"Test 5" .planning\phases\23-top-1-a-simul-end-to-end-2026-05-11\23-UAT.md | findstr /c:"PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `build_23_w3.log` 존재 + `0 Error(s)` + warning 매치 6
    - 23-UAT.md Test 5 의 Result 가 `✅ PASS` (또는 `❌ FAIL` + carry-over 메모) 로 갱신
    - 23-UAT.md Summary 표 Test 5 행 동기화
    - PASS 시 frontmatter `passed: 1`, `pending: 4` 갱신
  </acceptance_criteria>
  <done>
    SC#5 자동 검증 완료. 23-UAT.md Test 5 결과 채움. 나머지 SC#1-4 는 사용자 수동 (Task 3).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 [CHECKPOINT]: Human UAT — SC#1~SC#4 + final sign-off</name>
  <what-built>
    Plan 23-01 + 23-02 누적 산출물:
    - EdgeToLineDistanceMeasurement.cs (신규 클래스 — Point ROI fit + datumTransform 적용 + -datumRow * pixelResolution)
    - VisionAlgorithmService.TryFitLine selection 파라미터 확장
    - csproj 등록
    - MeasurementFactory 7번째 case + GetTypeNames
    - Action_FAIMeasurement.GrabOrLoadDatumImage TeachingImagePath 우선순위 분기
    - msbuild Debug/x64 PASS (Task 2 — SC#5 자동 PASS 또는 FAIL 시 carry-over)
  </what-built>
  <how-to-verify>
    **Pre-conditions (사전 셋업, D-16 + D-01 + D-13):**

    1. `D:\TestImg\Datameasurement\` 에 Simul 이미지 비치 (Top Fixture #1 의 PPT 도면 반영본)
    2. DataMeasurement.exe 기동 (`bin\x64\Debug\DatumMeasurement.exe`)
    3. Recipe 작성 또는 기존 Recipe 편집:
       - Shot 1개 — SimulImagePath = `D:\TestImg\Datameasurement\<inspection>.bmp`
       - Datum 1개 — AlgorithmType = `CircleTwoHorizontal` (D-01 lock), B1 홀 Circle ROI + 2 horizontal tangent line ROI, TeachingImagePath = `<teaching>.bmp` 또는 빈 문자열
       - FAI A1~A5 5개 — EdgeMeasureType = `EdgeToLineDistance`, 각각 Point ROI Rectangle 1개 + 7 Edge 파라미터 (EdgeDirection default TtoB, EdgeSelection default First), NominalValue + UpperTolerance + LowerTolerance 입력

    **Test 1 — SC#1 Simul end-to-end 완주:**
    1. Recipe 로드 → InspectionListView 펼침 → Datum + A1~A5 노드 5개 가시
    2. 수동 트리거 또는 TCP `Test` 명령으로 시퀀스 1회 실행
    3. 시퀀스 Error 진입 없음 + A1~A5 노드에 측정값 (mm, 0.001 정밀도) 표시 확인
    4. Datum B 위쪽 측정점 = 양수 값 (D-02 +Y 부호) 확인
    5. 결과 = PASS / FAIL / PARTIAL — 23-UAT.md Test 1 Actual/Result/Notes 채움

    **Test 2 — SC#2 OK/NG strip 녹/적:**
    1. Test 1 결과 유지 또는 NominalValue/Tolerance 조정으로 일부 OK 일부 NG 만들기
    2. 재실행 → InspectionListView 노드 색상 시각 확인 (CO-05 패턴 — OK=녹, NG=적)
    3. Halcon viewer 의 strip 색상 라인 (overlay) 확인 — **EdgeToLineDistance 는 PointToLineDistance 패턴(overlay 빈 리스트) 채택**이므로 viewer 라인 미표시 가능 (RESEARCH Pitfall 4)
    4. 결과 = 노드 색상 정상 분기 시 PASS (trust-based 옵션), viewer 라인 부재는 carry-over 등록
    5. 23-UAT.md Test 2 Actual/Result/Notes 채움

    **Test 3 — SC#3 TeachingImagePath 분리:**
    1. **Case A:** DatumConfig.TeachingImagePath = `teaching.bmp` (다른 파일), ShotConfig.SimulImagePath = `inspection.bmp` → 시퀀스 실행 → Datum 검출 = teaching, FAI 측정 = inspection (분리 동작)
    2. **Case B:** 두 경로 동일 파일 → 시퀀스 실행 → 회귀 0 (Phase 22 Test 2 패턴)
    3. **Case C:** TeachingImagePath = `""` → SimulImagePath 폴백 byte-identical (Phase 22 동작)
    4. 결과 = 3 case 모두 PASS / FAIL 별도 기록
    5. 23-UAT.md Test 3 채움 — trust-based PASS 옵션 사용 가능 (Plan 23-02 Task 2 분기 코드 직접 검증)

    **Test 4 — SC#4 A6 확장성:**
    1. **Channel A (INI 직접 편집):** 프로그램 종료 → INI 에 A6 섹션 추가 (FAIName="A6", EdgeMeasureType="EdgeToLineDistance") → 재기동 → A6 노드 + 측정값 가시
    2. **Channel B (UI 'Add FAI' 버튼):** Shot 노드 우클릭 또는 'Add FAI' → FAIConfig 신규 → EdgeMeasureType ComboBox 에서 `EdgeToLineDistance` 가시 확인 → ROI 입력 → 시퀀스 실행 → A6 측정값
    3. 결과 = 두 채널 모두 동작 시 PASS, 한 채널만 동작 시 PARTIAL + carry-over
    4. 23-UAT.md Test 4 채움

    **Test 5 — SC#5 msbuild (Task 2 자동 PASS):**
    1. Task 2 의 build_23_w3.log 검토 — 0 errors + 6 warnings
    2. 23-UAT.md Test 5 가 이미 ✅ PASS (Task 2 자동) — 확인만

    **Sign-off:**
    1. 23-UAT.md 의 Summary 표 5행 모두 채움 (PASS/FAIL/PARTIAL)
    2. Frontmatter `status: pending` → `signed_off` (모든 5 PASS) 또는 `partial` (일부 FAIL/PARTIAL + carry-over)
    3. Frontmatter `passed`, `pending` 카운트 갱신
    4. Frontmatter `sign_off_reviewer: heobum0928`, `sign_off_date: 2026-05-11` (또는 실제 검증일)
    5. Sign-off 블록의 Status 갱신
    6. Carry-overs 섹션 — FAIL/PARTIAL 항목 별도 quick task / 후속 phase 등록 (Phase 22 CO-22-01 패턴)
    7. Phase 23 SIGNED_OFF 상태 확정

    **합의 옵션:**
    - Trust-based PASS 사용 가능 (코드 변경 0 영역, 회귀 시나리오 부재). Phase 22 Test 2 패턴.
    - Partial sign-off 가능 (Phase 15/17 패턴) — 일부 FAIL 항목 carry-over 등록 후 SIGNED_OFF.

    **23-UAT.md commit + Phase 23 sign-off commit 사용자 명령 시 진행.**
  </how-to-verify>
  <resume-signal>Type "signed_off" (5/5 PASS), "partial" (일부 FAIL/carry-over), "fix-and-retry" (FAIL 후 코드 수정 필요), or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (없음) | Plan 23-03 = UAT 검증 + 문서 작성. 코드 변경 0. 새로운 보안 표면 추가 없음. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (없음) | — | — | — | **본 Plan 신규 위협 0건 — UAT 인터랙티브 검증 + 문서 작성 only. 코드 변경 0. Plan 23-01/23-02 의 threat_model 이 적용 범위 (T-23-01 / T-23-02 — Tampering / DoS for image path + Halcon exception, 모두 기존 try/catch 패턴으로 mitigated 또는 accepted).** |
</threat_model>

<verification>
- Task 1: 23-UAT.md scaffold (5 scenarios + frontmatter pending + Pre-conditions + Summary + Sign-off 블록) grep
- Task 2: build_23_w3.log + 23-UAT.md Test 5 PASS 갱신
- Task 3 (checkpoint): 사용자 SC#1-4 검증 + 23-UAT.md 사인오프 (status: signed_off / partial)
- 전체 plan: 사용자 사인오프 명시적 확인
</verification>

<success_criteria>
1. 23-UAT.md 존재 + 5 scenarios + frontmatter 완비
2. SC#5 (msbuild) 자동 PASS — Task 2 가 검증 + 결과 기록
3. SC#1 ~ SC#4 사용자 수동 검증 + 23-UAT.md Actual/Result/Notes 채워짐
4. 23-UAT.md frontmatter `status: signed_off` 또는 `partial`
5. Sign-off 블록 Reviewer + Date 명시
6. FAIL/PARTIAL 항목 모두 Carry-overs 섹션에 별도 quick task 또는 후속 phase 등록
7. Phase 23 SIGNED_OFF 또는 SIGNED_OFF_PARTIAL 상태 — ALG-01 충족 또는 carry-over 명시
</success_criteria>

<output>
After completion, create `.planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-03-SUMMARY.md` with:
- Frontmatter (phase, plan, requirements_completed=[ALG-01] or [ALG-01 partial], provides, affects)
- Performance metrics (UAT duration, 5 scenarios result distribution)
- Accomplishments (23-UAT.md sign-off + Phase 23 SIGNED_OFF)
- Task commits (Task 1 scaffold + Task 2 build artifact + Task 3 sign-off commits)
- Decisions made (trust-based PASS 채택 여부 / carry-over 항목들)
- Carry-overs (FAIL/PARTIAL 항목 별도 task 등록 — overlay 추가 backlog, viewer strip 라인 미표시 등)
- Self-check passed
- Update .planning/STATE.md: progress 갱신, completed_phases +1, current_position next-phase, last_activity 갱신
- Update .planning/ROADMAP.md: Phase 23 체크박스 [x] + 23-XX-PLAN.md 체크박스 3개 [x] + Progress Table Status → ✅ Complete
- Update .planning/REQUIREMENTS.md: Traceability Status `Phase 23 — ALG-01` → Signed off (2026-05-11)
</output>
