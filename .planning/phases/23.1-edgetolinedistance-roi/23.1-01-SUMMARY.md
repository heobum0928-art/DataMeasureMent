---
phase: 23.1-edgetolinedistance-roi
plan: "01"
subsystem: Halcon/Measurements
tags: [EdgeToLineDistance, ICustomTypeDescriptor, EdgeSelection, CO-23-01, D-08, D-09]
requires: []
provides:
  - EdgeToLineDistanceMeasurement.TryExecute 가 EdgeSelection 필드 무시, "All" 리터럴 전달 (D-08)
  - EdgeToLineDistanceMeasurement ICustomTypeDescriptor 구현 — PropertyGrid EdgeSelection 숨김 (D-09)
affects:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs
tech-stack:
  added: []
  patterns:
    - ICustomTypeDescriptor + DynamicPropertyHelper.FilterProperties (Phase 19 패턴 재사용)
    - sourceNames 화이트리스트 (Phase 18 CO-01 패턴 재사용)
key-files:
  created: []
  modified:
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs
decisions:
  - "D-08: TryExecute 가 EdgeSelection 필드를 완전히 무시하고 리터럴 'All' 전달 — 레거시 INI 값이 로드돼도 측정 동작에 영향 없음"
  - "D-09: ICustomTypeDescriptor 로 EdgeSelection PropertyGrid 숨김 — D-08 고정값에 대한 사용자 재조작 원천 차단"
  - "D-10(유지): EdgeSelection/EdgeSelectionList 필드 보존 — ParamBase reflection INI 직렬화 하위호환"
  - "D-11(유지): EdgeToLineDistanceMeasurement 만 수정 — PointToLineDistanceMeasurement 등 무수정"
metrics:
  duration_minutes: 3
  completed: "2026-05-17T00:12:00Z"
  tasks_completed: 2
  files_modified: 1
---

# Phase 23.1 Plan 01: EdgeSelection 결함 구조적 차단 (D-08/D-09) 요약

`EdgeToLineDistanceMeasurement` 의 CO-23-01 #1 결함(EdgeSelection "First"/"Last" → FitLineContourXld 최소 2점 미충족 → 측정 항상 실패)을 코드 레벨 + UI 레벨 이중으로 영구 차단.

## 완료된 작업

### Task 1: TryExecute — TryFitLine 리터럴 "All" 전달 (D-08)

**커밋:** b1b19b6

`TryExecute` 내 `TryFitLine` 호출 마지막 인자를 `EdgeSelection` (필드 값) → 리터럴 `"All"` 로 교체.

- 레거시 INI 에 `EdgeSelection=First` 가 저장돼 있어도 `TryExecute` 가 무시하므로 측정 실패 재발 불가능.
- `EdgeSelection` 필드 및 `EdgeSelectionList` 래퍼는 INI 하위호환 유지를 위해 보존(D-10).
- 기존 `//260512 hbk Phase 23 ALG-01` 주석 보존 + `//260517 hbk Phase 23.1 D-08` stacking.

**수정 위치:** `WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs` L89

### Task 2: ICustomTypeDescriptor — PropertyGrid EdgeSelection 숨김 (D-09)

**커밋:** 737208b

`EdgeToLineDistanceMeasurement` 에 `System.ComponentModel.ICustomTypeDescriptor` 를 구현해 PropertyGrid 에서 `EdgeSelection` 행을 숨김.

- 클래스 선언: `MeasurementBase, System.ComponentModel.ICustomTypeDescriptor` 다중 상속 추가.
- `GetProperties()` 무인자 오버로드 + `GetProperties(Attribute[])` 오버로드 + `BuildFilteredProperties` 구현.
- `hideFunc`: `name == "EdgeSelection" || name == "EdgeSelectionList"` → 두 프로퍼티 숨김.
- `sourceNames` 화이트리스트: `EdgeDirectionList`, `EdgePolarityList`, `EdgeSelectionList` → 다른 ComboBox 정상 렌더 보장 (Phase 18 CO-01 패턴).
- boilerplate 10개 메서드 fully-qualified `System.ComponentModel.*` — 신규 `using` 0.
- `FAIConfig.cs` L225-248 패턴을 그대로 복제 (Phase 19 산출물 재사용).

**수정 위치:** `WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs` L17-19, L114-150

## 성공 기준 달성 여부

| 기준 | 결과 |
|------|------|
| D-08: TryExecute 가 항상 "All" 로 라인 피팅 | PASS — L91 리터럴 "All" 확인 |
| D-09: PropertyGrid 에 EdgeSelection 행 미노출 | PASS — ICustomTypeDescriptor + hideFunc 구현 |
| D-10: 레거시 INI EdgeSelection 키 보존 | PASS — EdgeSelection/EdgeSelectionList 필드 유지 |
| D-11: 다른 measurement 타입 무수정 | PASS — git diff 단일 파일만 표시 |
| 신규 using 0 | PASS — fully-qualified 타입만 사용 |
| C# 7.2 호환 | PASS — switch expression/record/nullable ref 미사용 |
| Allman 스타일 일관 | PASS — 기존 파일 스타일 유지 |
| //260517 hbk Phase 23.1 마커 | PASS — D-08/D-09 라인 전체 적용 |
| ICustomTypeDescriptor 12개 멤버 완전 구현 | PASS — GetProperties×2 + BuildFilteredProperties + boilerplate 10개 |

## 편차 사항

없음 — 계획대로 정확하게 실행됨.

## 알려진 스텁

없음.

## 위협 표면 스캔

신규 trust boundary 없음. 이 plan 은 in-process measurement 클래스만 수정하며, T-23.1-01(Tampering — 레거시 INI EdgeSelection 조작) 위협을 Task 1(D-08)으로 mitigate 완료. T-23.1-02(DoS — ICustomTypeDescriptor.GetProperties) 는 accept 처리 (DynamicPropertyHelper 기존 null-guard 충족).

## Self-Check: PASSED

- `EdgeToLineDistanceMeasurement.cs` 존재 확인: FOUND
- 커밋 b1b19b6 존재 확인: FOUND (`git log --oneline -5`)
- 커밋 737208b 존재 확인: FOUND (`git log --oneline -5`)
- 두 커밋에서 변경된 파일: `EdgeToLineDistanceMeasurement.cs` 단일 파일만
