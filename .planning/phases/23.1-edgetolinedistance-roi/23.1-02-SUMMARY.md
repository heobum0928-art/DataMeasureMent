---
phase: 23.1-edgetolinedistance-roi
plan: "02"
subsystem: UI/ROI-Teaching
tags: [EdgeToLineDistance, RectRoi, D-01, D-02, D-03, D-04, D-05, MainView, InspectionListView]
requires:
  - 23.1-01 (EdgeToLineDistanceMeasurement.Point_Row/Col/Phi/Length1/Length2 필드 — Plan 01 산출물)
provides:
  - MainView._editingMeasurement 상태 + CommitRectRoi Measurement 분기 → EdgeToLineDistanceMeasurement.Point_* write-back (D-01)
  - MainView.FindSelectedEdgeToLineMeasurement 헬퍼 (트리 SelectedParam 우선, dataGrid fallback)
  - MainView.GetCurrentFAIRois — EdgeToLineDistanceMeasurement Point ROI 동시 수집 (D-03)
  - InspectionListView Measurement 노드 선택 시 btn_rectRoi.IsEnabled 조건부 활성화 (D-01/D-02)
affects:
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
tech-stack:
  added: []
  patterns:
    - CommitCircleRoi + _editingCircleMeasurement 패턴 확장 (동일 파일 analog)
    - btn_teachDatum 조건부 활성화 패턴 (InspectionListView — Datum 노드 선택 analog)
key-files:
  created: []
  modified:
    - WPF_Example/UI/ContentItem/MainView.xaml.cs
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
decisions:
  - "D-01(적용): 기존 Rect ROI 버튼 재사용 — ECanvasMode 신규 값 없음, _editingMeasurement != null 여부로 FAI/Measurement 분기"
  - "D-02(적용): EdgeToLineDistanceMeasurement 만 대상 — FindSelectedEdgeToLineMeasurement 가 타입 캐스트, 다른 타입은 null 반환 → 기존 FAI 경로"
  - "D-03(적용): GetCurrentFAIRois 에서 Measurement Point ROI 추가 수집 — ToRoiDefinition 시그니처 무변경"
  - "D-04/D-05(확인): 다점 치수 Add/Remove UI + INI 직렬화는 Phase 6/23 기존 인프라로 이미 동작 — Task 3 코드 변경 0"
metrics:
  duration_minutes: 8
  completed: "2026-05-17T00:30:00Z"
  tasks_completed: 3
  files_modified: 2
---

# Phase 23.1 Plan 02: EdgeToLineDistanceMeasurement Point ROI 캔버스 티칭 배선 (D-01~D-05) 요약

`EdgeToLineDistanceMeasurement` 의 Point ROI 를 캔버스 드로잉으로 티칭하는 배선을 완성하고, 1개 FAI = N개 measurement(다점 P1/P2/…) 구조의 UI 운용 가능성을 확인.

## 완료된 작업

### Task 1: MainView _editingMeasurement 상태 + RectRoiButton_Click/CommitRectRoi Measurement 분기 + FindSelectedEdgeToLineMeasurement 추가 (D-01)

**커밋:** 9dbd0fe

`MainView.xaml.cs` 에 작동하는 analog(`CommitCircleRoi` + `_editingCircleMeasurement`) 패턴을 그대로 확장하여 `EdgeToLineDistanceMeasurement` Rect ROI 티칭 경로를 추가했다.

**변경 내용:**
- `_editingMeasurement` (EdgeToLineDistanceMeasurement) + `_editingMeasurementFaiName` (string) 상태 변수 추가 (L51-53)
- `ExitCanvasMode()` 에 `_editingMeasurement = null; _editingMeasurementFaiName = null;` 해제 추가
- `RectRoiButton_Click`: `FindSelectedEdgeToLineMeasurement()` 우선 분기 — non-null 이면 `_editingMeasurement` 설정 후 드로잉 진입 및 `return`. 기존 FAIConfig 경로는 그 뒤에서 그대로 동작.
- `CommitRectRoi`: `_canvasMode != ECanvasMode.RectRoi` 가드 분리 → `_editingMeasurement != null` 분기에서 `Point_Row/Col/Phi/Length1/Length2` 5개 write-back (T-23.1-03 null-guard `if (measRoi != null)` 포함) → `ExitCanvasMode() + return`. 기존 `_editingFai` 경로는 이후에 무수정.
- `FindSelectedEdgeToLineMeasurement()` 신규 헬퍼: 트리 `SelectedParam as EdgeToLineDistanceMeasurement` 우선, dataGrid fallback (D-02: 이 타입만 대상)
- `ECanvasMode` enum 신규 값 없음 — `RectRoi` 값 그대로 사용 (D-01)

**수정 위치:** `WPF_Example/UI/ContentItem/MainView.xaml.cs` — 상태 변수 L51-53, ExitCanvasMode L1045-1046, RectRoiButton_Click L1074-1084, CommitRectRoi L1112-1160, FindSelectedEdgeToLineMeasurement L1257-1294

### Task 2: GetCurrentFAIRois Measurement Point ROI 동시 수집 (D-03) + InspectionListView btn_rectRoi 활성화 (D-01)

**커밋:** d68a907

**(A) MainView.GetCurrentFAIRois 확장 (D-03)**

기존 `foreach (var item in dataGrid_faiResults.Items)` 루프 안에서 FAI 당 `RoiDefinition` 수집 직후, `foreach (var m in fai.Measurements)` 루프를 추가하여 `EdgeToLineDistanceMeasurement` Point ROI 를 별도 `RoiDefinition` 으로 수집한다.
- 수집 조건: `etl.Point_Length1 > 0 && etl.Point_Length2 > 0` (미티칭 ROI 제외)
- `RoiDefinition.Id`: `fai.FAIName + "_" + measName` 형식 (선택 강조 매핑용)
- `Row1/Row2 = Point_Row ± Point_Length1`, `Column1/Column2 = Point_Col ± Point_Length2` (Task 1 write-back 역연산 일치)
- `ToRoiDefinition()` 시그니처 무변경 — FAIConfig.cs git diff 0

**(B) InspectionListView Measurement 노드 분기 수정 (D-01)**

`else if (item.NodeType == ENodeType.Measurement)` 분기 안, `button_renameFAI.IsEnabled = true;` 바로 다음에 추가:
```csharp
bool isEdgeToLine = item.Param is EdgeToLineDistanceMeasurement;
if (mParentWindow != null && mParentWindow.mainView != null)
    mParentWindow.mainView.btn_rectRoi.IsEnabled = isEdgeToLine;
```
- `EdgeToLineDistanceMeasurement` 노드 선택 → `btn_rectRoi` 활성화
- 다른 Measurement 타입 선택 → `btn_rectRoi` 비활성화 (D-02 범위 한정)
- analog: `btn_teachDatum.IsEnabled = true` (Datum 노드 선택 L440-442)

**수정 위치:**
- `WPF_Example/UI/ContentItem/MainView.xaml.cs` — GetCurrentFAIRois L183-199
- `WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` — Measurement 노드 분기 L449-452

### Task 3: 다점 치수 Add/Remove Measurement UI 경로 검증 (D-04/D-05 — 코드 변경 0)

**코드 변경:** 0라인

다음 5개 항목을 grep으로 확인하고 모두 기존 코드에 존재함을 확인했다:

1. **N measurement 컨테이너:** `FAIConfig.cs:15` — `public List<MeasurementBase> Measurements { get; private set; }` + `AddMeasurement(string typeName)` L18 + `RemoveMeasurement(int index)` L26
2. **Add Measurement UI:** `InspectionListView.xaml.cs:742` — `private void AddMeasurementToFAI(NodeViewModel faiNode, FAIConfig fai)` (L669, L701 호출점 포함)
3. **Remove Measurement UI:** `InspectionListView.xaml.cs:845` — `faiOwner.RemoveMeasurement(measIdx)` 호출
4. **EdgeToLineDistance 타입 노출:** `MeasurementFactory.cs:45` — `GetTypeNames()` 반환 배열에 `"EdgeToLineDistance"` 포함 (Phase 23-02 산출물)
5. **INI Save/Load:** `ParamBase` reflection 기반 `[SHOT_{s}_FAI_{f}_MEAS_{m}]` 섹션 — N개 measurement 자동 직렬화, 코드 변경 0

D-04/D-05 "1 FAI = N measurement 유동 구조" 는 기존 인프라로 이미 완전히 동작한다.

## 성공 기준 달성 여부

| 기준 | 결과 |
|------|------|
| D-01: 기존 Rect ROI 버튼 재사용, ECanvasMode 신규 값 없음 | PASS — RectRoi 값 그대로, _editingMeasurement 분기만 추가 |
| D-01: CommitRectRoi가 _editingMeasurement 분기로 FAI/Measurement 분기 | PASS — null-guard + 5개 Point_* write-back |
| D-02: EdgeToLineDistanceMeasurement 만 대상 | PASS — FindSelectedEdgeToLineMeasurement 타입 캐스트, 다른 타입 null |
| D-03: FAI 노드 선택 시 다점 Point ROI 동시 렌더 | PASS — GetCurrentFAIRois foreach Measurements 추가 |
| D-04/D-05: 다점 치수 Add/Remove + INI 직렬화 기존 인프라 동작 | PASS — Task 3 grep 5항목 확인, 코드 변경 0 |
| FAI 노드 Rect ROI 경로 회귀 0 | PASS — _editingFai 경로 무수정, 분기는 _editingMeasurement 체크 이후 |
| K&R 스타일 일관 | PASS — MainView.xaml.cs / InspectionListView.xaml.cs 모두 K&R |
| //260517 hbk Phase 23.1 마커 | PASS — 모든 신규 라인 적용 |
| C# 7.2 호환 | PASS — switch expression/record/nullable ref 미사용 |

## 편차 사항

없음 — 계획대로 정확하게 실행됨.

## Task 3 검증 근거 (grep)

| 항목 | 파일:라인 | 내용 |
|------|-----------|------|
| Measurements List | FAIConfig.cs:15 | `List<MeasurementBase> Measurements` |
| AddMeasurement | FAIConfig.cs:18 | `public MeasurementBase AddMeasurement(string typeName)` |
| RemoveMeasurement | FAIConfig.cs:26 | `public bool RemoveMeasurement(int index)` |
| AddMeasurementToFAI | InspectionListView.xaml.cs:742 | `private void AddMeasurementToFAI(...)` |
| RemoveMeasurement 호출 | InspectionListView.xaml.cs:845 | `faiOwner.RemoveMeasurement(measIdx)` |
| EdgeToLineDistance GetTypeNames | MeasurementFactory.cs:45 | `"EdgeToLineDistance"` in GetTypeNames() |

## 알려진 스텁

없음. Plan 02 목표(ROI 티칭 배선)는 코드 레벨에서 완성됐다. 실데이터 측정 검증(A1~A5 측정값 표시 확인)은 Plan 03 범위.

## 위협 표면 스캔

신규 trust boundary 없음. 모든 변경은 로컬 데스크탑 UI in-process 코드(ROI 티칭 배선) + in-process 모델 write-back.

- T-23.1-03 mitigate 완료: `CommitRectRoi` Measurement 분기 `if (measRoi != null)` 가드 후에만 `Point_*` write-back
- T-23.1-04 accept 유지: `mParentWindow != null && mParentWindow.mainView != null` null-guard 모두 적용

## Self-Check: PASSED

- `MainView.xaml.cs` 존재 확인: FOUND
- `InspectionListView.xaml.cs` 존재 확인: FOUND
- 커밋 9dbd0fe 존재 확인: FOUND (git log)
- 커밋 d68a907 존재 확인: FOUND (git log)
- Task 1 변경 파일: MainView.xaml.cs 단일
- Task 2 변경 파일: MainView.xaml.cs + InspectionListView.xaml.cs 2개
- Task 3 코드 변경: 0 라인 (grep-only 검증)
- FAIConfig.cs 무변경 확인: git diff HEAD~2 HEAD --stat 에 FAIConfig.cs 미포함
