---
phase: 23.1-edgetolinedistance-roi
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - build_23_1.log
  - .planning/phases/23.1-edgetolinedistance-roi/23.1-UAT.md
autonomous: false
requirements: [ALG-01]

must_haves:
  truths:
    - "msbuild Debug/x64 Rebuild 가 0 errors 로 통과한다"
    - "Phase 21 baseline 6 warnings (MSB3884x2 + CS0162x2 + CS0219x2) 가 유지되고 신규 warning 0"
    - "사용자가 실데이터 SIMUL 로 측정별 ROI 티칭 / 다점 측정 / A1~A5 Datum-Y(mm) 정확도를 검증한다"
    - "Phase 23 의 blocked SC#1~SC#4 가 23.1-UAT.md 하나로 통합 재검증된다"
  artifacts:
    - path: "build_23_1.log"
      provides: "msbuild Debug/x64 Rebuild 결과 로그 (0 errors / 6 warnings)"
      contains: "0 Error"
    - path: ".planning/phases/23.1-edgetolinedistance-roi/23.1-UAT.md"
      provides: "Phase 23 SC#1~SC#4 + 23.1 신규 검증 통합 UAT 시나리오"
      contains: "23.1-UAT"
  key_links:
    - from: "23.1-UAT.md sign-off"
      to: "Phase 23 최종 sign-off"
      via: "D-14 (23-UAT.md superseded)"
      pattern: "Phase 23"
---

<objective>
Plan 01 + Plan 02 의 누적 변경을 빌드 검증하고, 실데이터 SIMUL UAT 로 ALG-01 carry-over 를 최종 마감한다.

**D-15 (빌드):** Plan 01 (EdgeSelection 구조적 차단) + Plan 02 (ROI 티칭 배선) 의 누적 코드 변경 후 msbuild Debug/x64 Rebuild 가 0 errors 로 통과하고, Phase 21 baseline 6 warnings (MSB3884 x2 + CS0162 x2 + CS0219 x2) 가 정확히 유지되며 신규 warning 이 0 이어야 한다.

**D-12/D-14 (UAT):** 23.1 검증은 실데이터 SIMUL UAT 이다. 사용자가 다점 geometry 가 보이는 Simul 이미지 + Datum CTH INI + A1~A5 FAI INI(다점 포함)를 셋업한 뒤 SIMUL_MODE 로 실행해 측정별 ROI 티칭 / 다점 측정 / A1~A5 측정값을 직접 확인한다. Phase 23 UAT 의 blocked 시나리오(SC#1~SC#4) + 23.1 신규 검증(ROI 티칭/다점)을 `23.1-UAT.md` 하나로 통합 재검증한다. **23.1 sign-off = Phase 23 도 최종 sign-off** (Phase 23 의 `23-UAT.md` 는 23.1 로 superseded).

**D-13 (정확도 판정):** A1~A5 측정값과 OK/NG 판정이 SOP 공차와 일치하는지 확인한다. SOP 기준값:
- A1 = 20.681 +-0.030
- A2 = 19.771 +-0.050
- A3 = 18.500 +0.050/-0.020 (2점 — P1/P2 각각 measurement)
- A4 = 2.180 +0.020/-0.050 (2점 — P1/P2 각각 measurement)
- A5 = 1.079 +-0.050

Purpose: ALG-01 carry-over (Phase 23 미완) 의 최종 마감. 이 plan 의 사용자 sign-off 가 Phase 23 + Phase 23.1 을 동시에 종결한다.
Output: `build_23_1.log` (빌드 로그) + `23.1-UAT.md` (통합 UAT, 사용자 sign-off).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23.1-edgetolinedistance-roi/23.1-CONTEXT.md
@.planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-UAT.md

<interfaces>
<!-- 빌드 명령 + UAT 작성 계약. -->

msbuild 명령 (Phase 23 Test 5 와 동일):
`MSBuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo /t:Rebuild > build_23_1.log 2>&1`
- 기대: `0 Error(s)`, `6 Warning(s)` (MSB3884 x2 + CS0162 x2 + CS0219 x2 — Phase 21 baseline)
- Phase 23 Test 5 결과 (build_23_w3.log): EXIT=0, 0 errors, 6 warnings — baseline preserved. 23.1 변경 후에도 동일해야 함.

Phase 23 UAT 의 blocked SC (23-UAT.md — D-14 로 흡수):
- SC#1: Simul 이미지 로드 -> Datum CTH 자동 찾기 -> A1~A5 EdgeToLineDistance 측정값(mm) UI 표시 완주 (Phase 23 = FAIL — CO-23-01)
- SC#2: A1~A5 측정값 vs 공차 비교 -> OK/NG 판정 -> InspectionListView 노드 색상 분기 (CO-05 녹/적) (Phase 23 = blocked)
- SC#3: TeachingImagePath != InspectionImagePath 분리 동작 + 같은 파일도 회귀 0 (Phase 23 = blocked)
- SC#4: A6 1개 추가가 INI 직접 편집 + UI 'Add FAI' 둘 다로 가능 (Phase 23 = blocked)
- SC#5: msbuild Debug/x64 PASS — Phase 23 에서 이미 PASS (23.1 에서 build_23_1.log 로 재확인)

CO-23-01 원인 (Phase 23 UAT Notes): A1~A5 측정값/판정 컬럼 미표시. 후보 — (a) `FAIConfig.PixelResolutionX = 0` -> `resultValue = -datumRow * 0 = 0`, (b) `MeasurementResultRow` binding/refresh 단절. commit 898a96f 가 부분 수정(EdgeSelection default First->All + MeasurementBase.LastHasResult bool). 23.1 의 Plan 01 D-08 이 EdgeSelection 결함을 구조적으로 완결 — 그러나 PixelResolution 0 가능성은 UAT 셋업에서 사용자가 확인해야 함.

UAT 파일 frontmatter 형식 (23-UAT.md 참조) — `23.1-UAT.md` 최상단에 YAML frontmatter 블록(파일 첫 줄과 끝에 세 개의 하이픈 구분자로 감싼)을 두고 다음 키를 포함한다:
- `phase: 23.1`
- `plan: 03`
- `status: partial` (모든 시나리오 PASS 시 `signed_off`, 아니면 `partial`)
- `total: 8` (Test 총 개수)
- `passed: 0`
- `failed: 0`
- `blocked: 0`
- `pending: 8` (초기값)
- `requirement_ids: [ALG-01]`
- `supersedes: 23-UAT.md` (D-14)
- `sign_off_reviewer: TBD`
- `sign_off_date: TBD`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: msbuild Debug/x64 Rebuild 검증 (D-15)</name>
  <files>build_23_1.log</files>
  <read_first>
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-UAT.md (Test 5 — Phase 23 baseline 빌드 결과: 0 errors / 6 warnings 확인)
  </read_first>
  <action>
Plan 01 + Plan 02 의 누적 코드 변경 후 솔루션을 Rebuild 하고 결과를 검증한다.

저장소 루트에서 다음 명령을 실행한다 (PowerShell):

`MSBuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo /t:Rebuild > build_23_1.log 2>&1`

`MSBuild` 가 PATH 에 없으면 Visual Studio Developer Command Prompt 환경 또는 `vswhere` 로 MSBuild 경로를 찾아 절대 경로로 호출한다.

`build_23_1.log` 를 읽어 다음을 검증한다:

1. **0 errors:** 로그에 `0 Error(s)` 가 있어야 한다. error 가 1개라도 있으면 — Plan 01/02 의 변경에 컴파일 오류가 있는 것이므로 해당 파일을 수정한 뒤 재빌드한다. 흔한 원인: `EdgeToLineDistanceMeasurement` 의 `ICustomTypeDescriptor` 멤버 누락(CS0535), `using` 누락으로 인한 타입 미해결(CS0246).
2. **정확히 6 warnings:** 로그의 warning 매치가 정확히 6개여야 한다 — `MSB3884` x2 + `CS0162` x2 + `CS0219` x2 (Phase 21 baseline). 신규 warning code 가 있으면 안 된다.
3. **신규 warning 0:** `CS` 로 시작하는 warning code 중 `CS0162`/`CS0219` 외의 새 코드가 등장하면 — Plan 01/02 변경이 신규 warning 을 유발한 것이므로 원인을 제거한다 (예: 미사용 변수 -> 제거, 도달 불가 코드 -> 제거).

빌드가 PASS(0 errors + 6 warnings + 신규 0)할 때까지 반복한다. 최종 `build_23_1.log` 를 저장소 루트에 남긴다. SUMMARY 에 EXIT 코드 + error 수 + warning 수 + warning code 목록을 기록한다.

이 task 는 소스 코드를 의도적으로 변경하지 않는다 — 단, 빌드 실패 시 그 원인을 수정하는 것은 허용된다(빌드 통과가 목표).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (-not (Test-Path build_23_1.log)) { 'FAIL: log missing'; exit } $c=Get-Content build_23_1.log -Raw; $errOk = $c -match '0 Error\(s\)'; $warnCount = ([regex]::Matches($c, 'warning (MSB3884|CS0162|CS0219)')).Count; if ($errOk -and $warnCount -eq 6) { 'PASS' } else { 'FAIL: errOk=' + $errOk + ' warnCount=' + $warnCount }"</automated>
  </verify>
  <acceptance_criteria>
    - `build_23_1.log` 파일이 저장소 루트에 존재한다
    - 로그에 `0 Error(s)` 가 포함된다
    - 로그의 `warning (MSB3884|CS0162|CS0219)` 매치 수가 정확히 6 이다
    - `CS0162`/`CS0219`/`MSB3884` 외의 warning code 가 로그에 등장하지 않는다 (신규 warning 0)
    - SUMMARY 에 EXIT 코드 + error 수 + warning 수 + warning code 목록이 기록됐다
  </acceptance_criteria>
  <done>msbuild Debug/x64 Rebuild 가 0 errors / 6 warnings (Phase 21 baseline) 로 통과, 신규 warning 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 23.1-UAT.md 작성 — Phase 23 SC#1~SC#4 + 23.1 신규 검증 통합 (D-12/D-14)</name>
  <files>.planning/phases/23.1-edgetolinedistance-roi/23.1-UAT.md</files>
  <read_first>
    - .planning/phases/23-top-1-a-simul-end-to-end-2026-05-11/23-UAT.md (Phase 23 의 5 SC 시나리오 — 전체. SC#1~SC#4 의 Scenario/Steps/Expected 를 23.1-UAT.md 로 흡수)
    - .planning/phases/23.1-edgetolinedistance-roi/23.1-CONTEXT.md (D-12/D-13/D-14 — UAT 범위 + SOP 공차 기준값)
  </read_first>
  <action>
`.planning/phases/23.1-edgetolinedistance-roi/23.1-UAT.md` 를 신규 작성한다. 이 파일은 Phase 23 UAT 의 blocked SC#1~SC#4 + 23.1 신규 검증(측정별 ROI 티칭 / 다점 측정)을 하나로 통합한 인터랙티브 UAT scaffold 이다 (D-14). 사용자가 각 Test 를 직접 수행 후 Result 를 채운다.

**frontmatter:** 파일 최상단에 YAML frontmatter 블록(파일 첫 줄과 끝에 세 개의 하이픈 구분자)을 두고 `<interfaces>` 의 "UAT 파일 frontmatter 형식" 12개 키를 모두 포함한다 (`phase: 23.1`, `plan: 03`, `status: partial`, `total: 8`, `passed: 0`, `failed: 0`, `blocked: 0`, `pending: 8`, `requirement_ids: [ALG-01]`, `supersedes: 23-UAT.md`, `sign_off_reviewer: TBD`, `sign_off_date: TBD`).

**본문 구성** — 다음 섹션을 Korean 으로 작성한다:

1. **제목 + 개요:** "# Phase 23.1 UAT — EdgeToLineDistance ROI 티칭 배선 + 다점 치수 (ALG-01 carry-over)". 본 UAT 가 Phase 23 의 blocked SC#1~SC#4 를 흡수하며 23.1 sign-off = Phase 23 최종 sign-off 임을 명시 (D-14). 23-UAT.md 는 superseded.

2. **Pre-conditions (사용자 사전 셋업):**
   - Simul 이미지 비치 — `D:\TestImg\Datameasurement\` 에 Top Fixture #1 PPT 도면 반영 + **다점 geometry(A3/A4 의 P1/P2 가 보이는)** Simul 이미지. TeachingImagePath / SimulImagePath 분리 검증용 별도 파일 권장.
   - DatumConfig 1개 — AlgorithmType = `CircleTwoHorizontal` (Phase 23 D-01). B1 홀 Circle ROI + 2 horizontal tangent line ROI.
   - FAI A1~A5 5개 — EdgeMeasureType = `EdgeToLineDistance`. **A3/A4 는 measurement 2개씩(P1/P2 다점), A1/A2/A5 는 measurement 1개씩** — 단, 정확한 점 개수는 사용자가 SOP 도면 기준으로 셋업(D-04 — 코드는 점 개수를 모름).
   - 각 measurement 의 Point ROI 는 Test 6 에서 캔버스 드로잉으로 티칭(또는 INI 직접 입력). NominalValue/UpperTolerance/LowerTolerance 는 SOP 기준값(D-13)으로 입력.
   - `bin\x64\Debug\DatumMeasurement.exe` 존재 (Task 1 산출).

3. **Test 1 — SC#1 Simul end-to-end 완주 (Phase 23 carry-over):** Phase 23 UAT Test 1 의 Scenario/Steps/Expected 를 그대로 흡수. Expected 에 "측정값 컬럼이 `—` 가 아닌 실제 mm 값" 을 명시. Notes 에 CO-23-01 원인 후보(PixelResolutionX=0 / binding 단절)를 적고, 사용자가 측정값이 `—` 또는 `0` 이면 FAIConfig PixelResolution 설정을 먼저 확인하도록 안내.

4. **Test 2 — SC#2 OK/NG strip 녹/적 (Phase 23 carry-over):** Phase 23 UAT Test 2 흡수. InspectionListView 노드 색상 분기 검증. overlay 빈 리스트 정책으로 Halcon viewer 색상 라인은 미표시 가능 — trust-based PASS 옵션 명시.

5. **Test 3 — SC#3 TeachingImagePath != InspectionImagePath 분리 (Phase 23 carry-over):** Phase 23 UAT Test 3 흡수 (Case A 분리 / Case B 동일 / Case C 빈 문자열).

6. **Test 4 — SC#4 A6 확장성 (Phase 23 carry-over):** Phase 23 UAT Test 4 흡수 (Channel A INI 직접 편집 / Channel B UI 'Add FAI'). EdgeMeasureType ComboBox 에 `EdgeToLineDistance` 노출 확인.

7. **Test 5 — SC#5 msbuild Debug/x64 PASS (D-15):** `build_23_1.log` 기준 — Task 1 이 이미 자동 검증. Expected: 0 errors / 6 warnings (Phase 21 baseline) / 신규 0. Result 는 Task 1 결과로 채울 수 있음(자동).

8. **Test 6 — 23.1 신규: 측정별 ROI 티칭 배선 (D-01/D-02/D-03):**
   - Scenario: 트리에서 EdgeToLineDistanceMeasurement 노드 선택 -> Rect ROI 버튼 활성화 -> 캔버스 드로잉 -> 그 measurement 의 Point ROI 로 기록.
   - Steps: (1) InspectionListView 트리에서 A1 의 measurement 노드 선택 -> `btn_rectRoi` 가 활성화되는지 확인. (2) Rect ROI 버튼 클릭 -> 캔버스에 사각형 드로잉 -> 마우스업. (3) 같은 measurement 의 PropertyGrid 에서 `Point_Row/Point_Col/Point_Length1/Point_Length2` 가 드로잉한 사각형 좌표로 채워졌는지 확인. (4) A1 의 **FAI 노드**를 선택 -> 그 FAI 의 모든 measurement Point ROI 가 캔버스에 동시 렌더되는지 확인 (D-03). (5) FAI 노드 선택 후 Rect ROI 드로잉 -> 기존대로 FAIConfig.ROI_* 에 기록되는지 (회귀 0) 확인.
   - Expected: Measurement 노드 선택 시 Rect ROI 버튼 활성 / 드로잉 -> Point_* 기록 / FAI 노드 선택 시 다점 ROI 동시 렌더 / FAI 경로 회귀 0.

9. **Test 7 — 23.1 신규: 다점 측정 (1 FAI = N measurement) (D-04/D-05/D-06/D-07):**
   - Scenario: A3 또는 A4 처럼 1개 FAI 에 measurement 2개(P1/P2)를 두고 각각 ROI 티칭 + 독립 공차 입력 -> 두 점 모두 측정 -> FAI 행은 OK/NG 종합 판정.
   - Steps: (1) A3 FAI 에 measurement 가 2개인지 트리에서 확인 (없으면 UI 'Add Measurement' 로 추가). (2) 각 measurement 의 PropertyGrid 에서 Nominal/Tolerance 를 SOP 기준(A3 = 18.500 +0.050/-0.020)으로 독립 입력. (3) 각 measurement 의 Point ROI 를 Test 6 방식으로 티칭. (4) 시퀀스 실행 -> 각 Measurement 트리 노드에서 점별 측정값 확인. (5) A3 FAI 행이 두 measurement 가 모두 OK 일 때만 OK(종합 판정)인지 확인.
   - Expected: measurement 별 독립 ROI/공차 / 점별 측정값은 각 Measurement 노드에서 확인 / FAI 행은 종합 OK/NG 만.

10. **Test 8 — 23.1 신규: A1~A5 Datum-Y(mm) 측정 정확도 (D-13):**
    - Scenario: A1~A5 측정값과 OK/NG 판정이 SOP nominal/tolerance 와 일치.
    - Steps: 시퀀스 실행 후 A1~A5(다점은 P1/P2 각각) 측정값을 SOP 기준과 대조 — A1=20.681+-0.030 / A2=19.771+-0.050 / A3=18.500 +0.050/-0.020 (2점) / A4=2.180 +0.020/-0.050 (2점) / A5=1.079+-0.050. 측정값이 공차 내면 OK, 벗어나면 NG 로 표시되는지 확인. +Y 부호(Datum B 위쪽 양수, Phase 23 D-02) 적용 확인.
    - Expected: 측정값이 SOP 기준 부근(공차 내 OK 판정) / 공차 위반 시 NG / 0.001mm 정밀도(F3 표기).

11. **Summary 표:** Test 1~8 의 # / Scenario / Result / Notes 컬럼. Total/Passed/Failed/Blocked 집계 행.

12. **Carry-overs 섹션:** CO-ID / Description / Owner / Status 표 (사용자가 FAIL 시 기입).

13. **Sign-off 섹션:** Reviewer / Date / Status. "23.1 sign-off = Phase 23 도 최종 sign-off (D-14)" 명시.

각 Test 는 `**Scenario:** / **Steps:** / **Expected:** / **Actual:** (사용자 기입 — 초기값 `(미수행)`) / **Result:** (초기값 `pending`) / **Notes:**` 구조로 작성한다 — 23-UAT.md 의 Test 형식을 그대로 따른다.

이 task 는 `23.1-UAT.md` 파일만 생성한다 — 코드/다른 파일 변경 없음.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f='.planning/phases/23.1-edgetolinedistance-roi/23.1-UAT.md'; if (-not (Test-Path $f)) { 'FAIL: missing'; exit } $c=Get-Content $f -Raw; $ok = ($c -match 'supersedes: 23-UAT.md') -and ($c -match 'requirement_ids: \[ALG-01\]') -and ($c -match 'Test 6') -and ($c -match 'Test 7') -and ($c -match 'Test 8') -and ($c -match '20.681') -and ($c -match '18.500') -and ($c -match 'CircleTwoHorizontal'); if ($ok) { 'PASS' } else { 'FAIL' }"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/23.1-edgetolinedistance-roi/23.1-UAT.md` 파일이 존재한다
    - frontmatter 에 `phase: 23.1`, `requirement_ids: [ALG-01]`, `supersedes: 23-UAT.md` 가 포함된다
    - Test 1~5 가 Phase 23 SC#1~SC#5 를 흡수한다 (Test 1 = SC#1 Simul end-to-end, Test 3 = SC#3 TeachingImagePath 분리, Test 4 = SC#4 A6 확장성)
    - Test 6 (측정별 ROI 티칭), Test 7 (다점 측정), Test 8 (A1~A5 정확도) 3개 신규 시나리오가 존재한다
    - SOP 기준값 `20.681`, `19.771`, `18.500`, `2.180`, `1.079` 5개가 모두 본문에 등장한다 (D-13)
    - `CircleTwoHorizontal` 가 Pre-conditions 에 명시된다 (Phase 23 D-01 계승)
    - Sign-off 섹션에 "23.1 sign-off = Phase 23 도 최종 sign-off" 가 명시된다 (D-14)
    - 각 Test 가 Scenario/Steps/Expected/Actual/Result/Notes 구조를 갖는다
    - 이 task 로 코드 파일은 변경되지 않았다 (`23.1-UAT.md` 단일 파일만 생성)
  </acceptance_criteria>
  <done>23.1-UAT.md 가 Phase 23 SC#1~SC#4 + 23.1 신규 검증 3개를 통합한 인터랙티브 UAT scaffold 로 작성됨.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 실데이터 SIMUL UAT 사용자 검증 + sign-off (D-12/D-14)</name>
  <files>.planning/phases/23.1-edgetolinedistance-roi/23.1-UAT.md</files>
  <action>
이 task 는 사용자가 직접 수행하는 인터랙티브 검증 checkpoint 이다. 자동화 불가 영역(다점 geometry 가 보이는 실 이미지 셋업 + Halcon 시각 측정 + SOP 공차 대조)이므로 사용자가 SIMUL_MODE 로 직접 검증한다. 실행자(agent)는 이 task 에서 코드를 작성하지 않으며, 사용자 검증 결과(`23.1-UAT.md` 의 Result/Sign-off)를 수령한 뒤 SUMMARY 에 결과를 기록한다.

What was built (사용자 안내용):
Plan 01 (EdgeSelection 구조적 차단 — D-08/D-09) + Plan 02 (ROI 티칭 배선 + 다점 운용 UI — D-01~D-05) 의 코드 변경이 완료됐고, Task 1 에서 msbuild Debug/x64 Rebuild 가 PASS(0 errors / 6 warnings)했으며, Task 2 에서 `23.1-UAT.md` 통합 UAT scaffold(Test 1~8)가 작성됐다.
  </action>
  <what-built>
Plan 01 (EdgeSelection 구조적 차단 — D-08/D-09) + Plan 02 (ROI 티칭 배선 + 다점 운용 UI — D-01~D-05) 의 코드 변경이 완료됐고, Task 1 에서 msbuild Debug/x64 Rebuild 가 PASS(0 errors / 6 warnings)했으며, Task 2 에서 `23.1-UAT.md` 통합 UAT scaffold(Test 1~8)가 작성됐다.

이제 사용자가 실데이터 SIMUL_MODE 로 직접 검증한다 — 자동화 불가 영역(다점 geometry 가 보이는 실 이미지 셋업 + Halcon 시각 측정 + SOP 공차 대조)이므로 checkpoint 가 필요하다.
  </what-built>
  <how-to-verify>
1. **셋업** — `23.1-UAT.md` 의 Pre-conditions 를 따른다:
   - `D:\TestImg\Datameasurement\` 에 Top Fixture #1 + 다점 geometry(A3/A4 의 P1/P2 가 보이는) Simul 이미지를 비치한다.
   - DatumConfig 1개(AlgorithmType = `CircleTwoHorizontal`) + FAI A1~A5 (EdgeMeasureType = `EdgeToLineDistance`, A3/A4 는 measurement 2개) INI 를 셋업한다.
   - NominalValue/Tolerance 는 SOP 기준(D-13)으로 입력한다.

2. **빌드 산출 실행** — `bin\x64\Debug\DatumMeasurement.exe` 를 기동한다.

3. **`23.1-UAT.md` 의 Test 1~8 을 순서대로 수행**한다:
   - Test 1~5: Phase 23 의 blocked SC#1~SC#4 + msbuild — Simul end-to-end 완주 / OK·NG 색상 / TeachingImagePath 분리 / A6 확장성 / 빌드.
   - Test 6: 측정별 ROI 티칭 — Measurement 노드 선택 시 Rect ROI 버튼 활성화 확인 -> 캔버스 드로잉 -> `Point_*` 기록 확인 -> FAI 노드 선택 시 다점 ROI 동시 렌더 확인.
   - Test 7: 다점 측정 — 1 FAI = 2 measurement(P1/P2) 의 독립 ROI/공차 + 점별 측정값 + FAI 종합 판정.
   - Test 8: A1~A5 정확도 — 측정값이 SOP 공차(A1=20.681+-0.030 등) 내 OK 판정인지 대조.

4. **각 Test 의 `Actual:` 와 `Result:` 를 채운다** — PASS / FAIL / blocked. `Summary` 표와 frontmatter 의 `passed`/`failed`/`blocked`/`pending` 카운트, `status` (모두 PASS -> `signed_off`, 아니면 `partial`)를 갱신한다.

5. **Sign-off 섹션**에 Reviewer / Date / Status 를 기입한다. 모든 Test PASS 시 23.1 = `signed_off` 이며 이는 Phase 23 의 최종 sign-off 이기도 하다 (D-14 — 23-UAT.md superseded).

6. **측정값이 `—` 또는 `0` 으로 나오면** (CO-23-01 재발): `FAIConfig.PixelResolutionX` 가 0 인지 먼저 확인한다 (`resultValue = -datumRow * pixelResolution` 이므로 resolution 0 이면 측정값이 조용히 0). resolution 설정 후에도 미표시면 carry-over 로 등록한다.

FAIL 또는 blocked 가 있으면 `Carry-overs` 표에 CO-ID 와 함께 기록하고, 그 항목은 후속 quick task 또는 gap closure 로 처리한다.
  </how-to-verify>
  <verify>
    <automated>powershell -NoProfile -Command "$f='.planning/phases/23.1-edgetolinedistance-roi/23.1-UAT.md'; if (-not (Test-Path $f)) { 'FAIL: missing'; exit } $c=Get-Content $f -Raw; if ($c -match 'sign_off_reviewer:\s*(?!TBD)\S' -and $c -notmatch 'Result:\*\*\s*pending') { 'PASS' } else { 'MANUAL: 사용자 검증 + sign-off 대기' }"</automated>
  </verify>
  <resume-signal>`23.1-UAT.md` 의 모든 Test Result + Summary + Sign-off 가 채워지면 "approved" 또는 발견된 이슈를 기술한다.</resume-signal>
  <done>사용자가 23.1-UAT.md 의 Test 1~8 을 수행하고 Result/Summary/Sign-off 를 채움. 모든 PASS 시 23.1 + Phase 23 동시 sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (없음 — 신규 trust boundary 도입 없음) | 이 plan 은 빌드 검증 + UAT 문서 작성 + 사용자 인터랙티브 검증만 수행한다. 코드 변경 없음(빌드 실패 수정 제외). 신규 네트워크/인증/외부 입력 경로 없음. UAT 셋업의 Simul 이미지/INI 는 기존 로컬 파일 로드 경로(`File.Exists` + try/catch `new HImage` — Phase 23 기존 가드)를 그대로 사용. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23.1-05 | Tampering | UAT 셋업의 Simul 이미지 / INI 파일 경로 | accept | 로컬 단일 사용자 데스크탑 — 사용자가 직접 비치한 검증용 파일. 이미지 로드는 Phase 23 의 기존 `File.Exists` 가드 + try/catch `new HImage` 경로 사용(이 plan 은 그 경로를 변경하지 않음). 실 위협 낮음. |

새 attack surface 없음 — 빌드 검증 + UAT 문서 + 사용자 검증. 코드 경로 변경 0(빌드 실패 수정 제외), 신규 네트워크/인증/외부 입력 추가 없음.
</threat_model>

<verification>
- `build_23_1.log` 가 0 errors / 6 warnings (MSB3884 x2 + CS0162 x2 + CS0219 x2) 로 PASS.
- `23.1-UAT.md` 가 Phase 23 SC#1~SC#5 + 23.1 신규 Test 6~8 을 포함하고 SOP 기준값 5개를 담는다.
- 사용자가 Test 1~8 을 수행하고 Result + Sign-off 를 채운다 (checkpoint).
- 23.1 sign-off 시 Phase 23 도 최종 sign-off — 23-UAT.md superseded (D-14).
</verification>

<success_criteria>
- D-15 충족: msbuild Debug/x64 PASS, Phase 21 baseline 6 warnings 유지, 신규 warning 0.
- D-12 충족: 23.1 검증이 실데이터 SIMUL UAT 로 수행 — checkpoint 로 사용자 인터랙티브 검증.
- D-13 충족: A1~A5 측정 정확도가 SOP nominal/tolerance 와 대조 검증 (Test 8).
- D-14 충족: Phase 23 의 blocked SC#1~SC#4 + 23.1 신규 검증이 `23.1-UAT.md` 하나로 통합 — 23.1 sign-off = Phase 23 최종 sign-off, 23-UAT.md superseded.
- ALG-01 carry-over 완결: Phase 23 + Phase 23.1 동시 종결.
</success_criteria>

<output>
완료 후 `.planning/phases/23.1-edgetolinedistance-roi/23.1-03-SUMMARY.md` 를 생성한다.
</output>
</invoke>
