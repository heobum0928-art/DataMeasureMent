---
phase: 28-fai-circlediameter-datum-circle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs
autonomous: true
requirements: [REQ-28-01, REQ-28-02]
tags: [edgeoption, polarity-mapping, polar-defaults]
must_haves:
  truths:
    - "EdgeOptionLists.MapRadialDirectionToHalconPolarity(string) returns 'negative' for 'Outward' (case-insensitive) and 'positive' otherwise (Inward, empty, null)"
    - "EdgeOptionLists exposes 4 polar default consts (FaiCirclePolarStepDeg=10.0, FaiCircleRectL1Ratio=0.02, FaiCircleRectL2Ratio=0.02, FaiCircleEdgeSelection=\"First\") matching DatumConfig defaults so REQ-28-03 동등성이 결정적으로 보장된다"
    - "Helper signature is callable from both ReringProject.Sequence (CircleDiameterMeasurement.cs) and ReringProject.Halcon.Algorithms (DatumFindingService.cs) without new using directives — both files already import ReringProject.Sequence"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs"
      provides: "Polarity mapping helper + FAI polar default consts"
      contains: "MapRadialDirectionToHalconPolarity"
  key_links:
    - from: "EdgeOptionLists.MapRadialDirectionToHalconPolarity"
      to: "CircleDiameterMeasurement.TryExecute (Plan 02) and DatumFindingService.TryFindCircleTwoHorizontal/TryTeachCircleTwoHorizontal (Plan 03)"
      via: "static method call (single source of polarity mapping)"
      pattern: "EdgeOptionLists\\.MapRadialDirectionToHalconPolarity"
---

<objective>
Add the polarity mapping helper `MapRadialDirectionToHalconPolarity(string)` and 4 FAI-side polar default constants (`FaiCirclePolarStepDeg`, `FaiCircleRectL1Ratio`, `FaiCircleRectL2Ratio`, `FaiCircleEdgeSelection`) to `EdgeOptionLists` so that:

1. Plan 02 (CircleDiameterMeasurement) can branch into the polar path using the same single-source mapping rule used by Datum CTH (REQ-28-02).
2. Plan 03 (DatumFindingService inline cleanup) can replace 2 duplicated inline `string.Equals(...) ? "negative" : "positive"` expressions with a single helper call (D-03).
3. REQ-28-03 동등성 (FAI ↔ Datum CTH 검출 직경 ≤ 0.001 mm) 이 default 일치로 결정적으로 보장된다 (D-04, D-05).

Purpose: Establish the shared, single-source helper that downstream Wave 2 plans depend on. No behaviour change yet — this plan only adds new symbols to a static class.

Output: One file modified. New symbols: 1 static method + 4 const fields. All new lines carry `//260508 hbk Phase 28` per project comment convention.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/28-fai-circlediameter-datum-circle/28-SPEC.md
@.planning/phases/28-fai-circlediameter-datum-circle/28-CONTEXT.md
@WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs

<interfaces>
<!-- Existing EdgeOptionLists members (reference — DO NOT modify): -->
namespace ReringProject.Sequence
public static class EdgeOptionLists
{
    public static readonly List<string> Directions       = { "LtoR", "RtoL", "TtoB", "BtoT" };
    public static readonly List<string> FAIPolarities    = { "DarkToLight", "LightToDark" };
    public static readonly List<string> DatumPolarities  = { "all", "positive", "negative" };
    public static readonly List<string> Selections       = { "First", "Last", "All" };
    public static readonly List<string> RadialDirections = { "Inward", "Outward" }; // Phase 17 D-02
}

<!-- Datum CTH default values that the new FAI consts MUST match (from DatumConfig.cs):
       Circle_PolarStepDeg = 10.0
       Circle_RectL1Ratio  = 0.02 (Quick 260430-hox baseline)
       Circle_RectL2Ratio  = 0.02 (Quick 260430-hox baseline)
       Circle_EdgeSelection default first option = "First" (EdgeOptionLists.Selections[0]) -->

<!-- Datum inline polarity expressions (callers — DO NOT modify here, Plan 03 handles them):
       DatumFindingService.cs:200  string.Equals(config.Circle_RadialDirection, "Outward", System.StringComparison.OrdinalIgnoreCase) ? "negative" : "positive"
       DatumFindingService.cs:730  string.Equals(config.Circle_RadialDirection, "Outward", System.StringComparison.OrdinalIgnoreCase) ? "negative" : "positive"
     The new helper must produce byte-identical output for any input these expressions accept. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MapRadialDirectionToHalconPolarity helper + 4 FAI polar default consts to EdgeOptionLists</name>
  <files>WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs (the file you are editing — full read, only 29 lines)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs lines 195-210 AND 725-735 (the two inline expressions whose semantics the helper MUST preserve byte-for-byte; you are NOT modifying these lines in this plan, only mirroring their behaviour)
    - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs lines 234-245 (sanity-clamp values reference — confirms FaiCirclePolarStepDeg=10.0 / Selection="First" defaults are inside the clamp window so callers do not get reset)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs lines 235-252 (Circle_PolarStepDeg / Circle_RectL1Ratio / Circle_RectL2Ratio default values must match — required for D-04 / REQ-28-03 동등성)
    - .planning/phases/28-fai-circlediameter-datum-circle/28-CONTEXT.md §decisions D-02, D-03, D-04 (helper rule + const values are locked there)
  </read_first>
  <action>
Open `WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs`. After the existing `RadialDirections` declaration (currently the last member, line 27) and before the closing brace of `EdgeOptionLists`, append exactly the following block. Match the file's existing 4-space indentation. Each new line MUST end with the `//260508 hbk Phase 28` comment per project comment convention (CLAUDE.md memory feedback_comment_convention).

```csharp
        //260508 hbk Phase 28 D-04 — FAI CircleDiameter polar 경로 default 상수 (Datum CTH default 와 동일 → REQ-28-03 동등성 결정적 보장)
        public const double FaiCirclePolarStepDeg   = 10.0;     //260508 hbk Phase 28
        public const double FaiCircleRectL1Ratio    = 0.02;     //260508 hbk Phase 28
        public const double FaiCircleRectL2Ratio    = 0.02;     //260508 hbk Phase 28
        public const string FaiCircleEdgeSelection  = "First";  //260508 hbk Phase 28

        //260508 hbk Phase 28 D-02/D-03 — RadialDirection ("Inward"/"Outward") → Halcon polarity ("positive"/"negative") 단일 매핑.
        //  Datum CTH (DatumFindingService.cs:200, :730) 의 inline `string.Equals(..., "Outward", OrdinalIgnoreCase) ? "negative" : "positive"` 와
        //  byte-identical 결과를 보장한다 (null/empty/Inward → "positive", Outward(대소문자무관) → "negative").
        public static string MapRadialDirectionToHalconPolarity(string radial) //260508 hbk Phase 28
        {
            if (string.Equals(radial, "Outward", System.StringComparison.OrdinalIgnoreCase)) //260508 hbk Phase 28
            {
                return "negative"; //260508 hbk Phase 28
            }
            return "positive"; //260508 hbk Phase 28
        }
```

Implementation notes (NON-negotiable):

- **Brace style**: K&R is the file's existing style (open brace on same line as method declaration in `EdgeOptionLists` is N/A — file has none yet; for the new method use Allman braces matching common static-class patterns since the method body has multiple statements). The existing file has no methods to mirror, so Allman is acceptable here. Do NOT introduce expression-bodied members (`=>`) — C# 7.2 allows them but the project preference is explicit `if`/`return` per CLAUDE.md QUAL-02 spirit (and Phase 20 will enforce this globally; do not add new debt).
- **C# version**: Project is C# 7.2. Do NOT use C# 8+ features (no switch expressions, no `is not`, no nullable refs, no pattern-matching enhancements).
- **Namespace**: Existing `using System.Collections.Generic;` is already present. The helper uses `System.StringComparison.OrdinalIgnoreCase` — use fully-qualified `System.StringComparison` (do NOT add `using System;` to keep diff minimal; existing file has no `using System;` and the fully-qualified form matches the inline expressions in DatumFindingService.cs:200/730).
- **Const placement**: The 4 consts go BEFORE the helper method (data first, then behaviour) and AFTER `RadialDirections`. This keeps related symbols grouped (Phase 17 D-02 RadialDirections + Phase 28 polar defaults).
- **No reordering of existing members**: Do NOT touch lines 1-27. Only append after line 27.
- **No new `using` directives** unless strictly required (none are — `System.Collections.Generic` already imported, `System.StringComparison` is fully-qualified).
- **Mapping semantics MUST be byte-identical to the inline expressions in DatumFindingService.cs:200/:730**: any input that returns `"negative"` from those expressions MUST return `"negative"` from the helper, and vice versa. The reference expression is `string.Equals(x, "Outward", OrdinalIgnoreCase) ? "negative" : "positive"` — use the same comparison form.

Do NOT modify CircleDiameterMeasurement.cs or DatumFindingService.cs in this plan — those are Plans 02 and 03 respectively.
  </action>
  <verify>
    <automated>powershell -Command "cd C:\Info\Project\DataMeasurement; $errors = & msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /m /nologo /verbosity:minimal 2>&amp;1 | Select-String -Pattern 'error CS'; if ($errors) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs -Pattern 'public static string MapRadialDirectionToHalconPolarity\(string '` returns exactly 1 match
    - `Select-String -Path WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs -Pattern 'public const double FaiCirclePolarStepDeg\s*=\s*10\.0'` returns exactly 1 match
    - `Select-String -Path WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs -Pattern 'public const double FaiCircleRectL1Ratio\s*=\s*0\.02'` returns exactly 1 match
    - `Select-String -Path WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs -Pattern 'public const double FaiCircleRectL2Ratio\s*=\s*0\.02'` returns exactly 1 match
    - `Select-String -Path WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs -Pattern 'public const string FaiCircleEdgeSelection\s*=\s*"First"'` returns exactly 1 match
    - `Select-String -Path WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs -Pattern '"Outward".*OrdinalIgnoreCase'` returns exactly 1 match (helper body)
    - `(Select-String -Path WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs -Pattern '260508 hbk Phase 28').Matches.Count` returns ≥ 7 (4 const lines + helper signature + helper body lines + section header — every NEW line tagged)
    - `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /m` returns exit 0
    - msbuild output contains 0 new `error CS\d+` and 0 new `warning CS\d+` (5 pre-existing warnings allowed per SPEC REQ-28-06)
    - No edit to lines 1-27 of EdgeOptionLists.cs (verified via `git diff -U0 -- WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs` showing only `+` additions after the original closing of `RadialDirections`)
  </acceptance_criteria>
  <done>
EdgeOptionLists.cs compiles with 1 new static helper + 4 new public consts, all new lines tagged `//260508 hbk Phase 28`. Helper output for `("Outward")`, `("OUTWARD")`, `("Inward")`, `(null)`, `("")` exactly matches the inline expressions at DatumFindingService.cs:200/:730 (negative for any case-insensitive "Outward", positive otherwise). msbuild Debug/x64 PASS, 0 new errors/warnings. Plan 02 and Plan 03 can now reference these symbols.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan only adds public static symbols (consts + pure function) to an in-process static class. No I/O, no user-input parsing, no network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | EdgeOptionLists.MapRadialDirectionToHalconPolarity | accept | Pure function, deterministic output, no state. INI input is already validated by ParamBase string deserialization upstream. No new attack surface. |

Per CONTEXT.md: "no new attack surface — modifies in-process measurement algorithm parameters; INI input already validated by ParamBase string deserialization."
</threat_model>

<verification>
- msbuild Debug/x64 PASS, 0 new errors/warnings
- All 4 const default values exactly match Datum CTH defaults (DatumConfig.Circle_PolarStepDeg=10.0, RectL1Ratio=0.02, RectL2Ratio=0.02, EdgeSelection first option="First") — required for REQ-28-03 동등성
- Helper output byte-identical to existing inline expressions (DatumFindingService.cs:200, :730) for all inputs — required for D-03 cleanup in Plan 03 to be regression-0
</verification>

<success_criteria>
- 1 static method `MapRadialDirectionToHalconPolarity(string)` added to `EdgeOptionLists`
- 4 public const fields (`FaiCirclePolarStepDeg`, `FaiCircleRectL1Ratio`, `FaiCircleRectL2Ratio`, `FaiCircleEdgeSelection`) added with values 10.0 / 0.02 / 0.02 / "First"
- All new lines carry `//260508 hbk Phase 28` comment
- Lines 1-27 of EdgeOptionLists.cs unchanged
- msbuild Debug/x64 PASS, 0 new CS errors/warnings
- Plans 02 and 03 (Wave 2) unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/28-fai-circlediameter-datum-circle/28-01-SUMMARY.md` documenting:
- Helper signature and exact mapping rule
- 4 const values + their Datum CTH default counterparts (cross-reference)
- Confirmation that no Wave 2 caller has been wired yet (this plan is foundation only)
- msbuild result + warning count delta
</output>
