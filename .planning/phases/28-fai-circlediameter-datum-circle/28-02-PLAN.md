---
phase: 28-fai-circlediameter-datum-circle
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs
autonomous: true
requirements: [REQ-28-01, REQ-28-02, REQ-28-03, REQ-28-04, REQ-28-05, REQ-28-06]
tags: [fai, circle-diameter, polar-sampling, propertygrid-combo]
must_haves:
  truths:
    - "CircleDiameterMeasurement exposes Circle_RadialDirection (string) field with [Category(\"Edge\")] + [ItemsSourceProperty(nameof(Circle_RadialDirectionList))], default value = \"\""
    - "PropertyGrid renders Circle_RadialDirection as combo ▼ with options Inward / Outward (sourced from EdgeOptionLists.RadialDirections — single source per Phase 17 D-02 / Phase 18 CO-01)"
    - "TryExecute branches: empty Circle_RadialDirection → existing TryFindCircle path (regression 0 vs v1.0 INI), \"Inward\"/\"Outward\" → VisionAlgorithmService.TryFindCircleByPolarSampling with mapped polarity"
    - "ICustomTypeDescriptor NOT implemented (REQ-28-05) — all fields always visible in PropertyGrid"
    - "EdgePolarity field is preserved but ignored on the polar branch (D-08); fit branch still uses EdgePolarity unchanged"
    - "INI 하위호환: missing Circle_RadialDirection key → default \"\" → fit branch → v1.0 결과 동일 (REQ-28-04)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs"
      provides: "Circle_RadialDirection field + Circle_RadialDirectionList wrapper + TryExecute branch"
      contains: "Circle_RadialDirection"
  key_links:
    - from: "CircleDiameterMeasurement.TryExecute"
      to: "VisionAlgorithmService.TryFindCircleByPolarSampling (line 214) when Circle_RadialDirection is non-empty"
      via: "direct call (D-01 — no wrapper class)"
      pattern: "TryFindCircleByPolarSampling"
    - from: "CircleDiameterMeasurement.TryExecute polar branch"
      to: "EdgeOptionLists.MapRadialDirectionToHalconPolarity (Plan 01)"
      via: "static helper call (single-source polarity mapping)"
      pattern: "EdgeOptionLists\\.MapRadialDirectionToHalconPolarity"
    - from: "CircleDiameterMeasurement.Circle_RadialDirectionList"
      to: "EdgeOptionLists.RadialDirections (Phase 17 D-02 single source)"
      via: "[ItemsSourceProperty] → [Browsable(false)] List<string> wrapper (Phase 18 CO-01 pattern from DatumConfig.cs:242-252)"
      pattern: "EdgeOptionLists\\.RadialDirections"
---

<objective>
Add `Circle_RadialDirection` field + combo binding wrapper + `TryExecute` branch to `CircleDiameterMeasurement.cs` so that:

1. **REQ-28-01**: PropertyGrid exposes `Circle_RadialDirection` as a combo ▼ (Inward / Outward) under `[Category("Edge")]`, default `""`.
2. **REQ-28-02**: `TryExecute` branches — empty → existing `TryFindCircle` (unchanged), `"Inward"`/`"Outward"` → `VisionAlgorithmService.TryFindCircleByPolarSampling` directly (D-01).
3. **REQ-28-03**: Polar branch uses `EdgeOptionLists.MapRadialDirectionToHalconPolarity` (Plan 01) for polarity + the 4 FAI polar default consts (Plan 01) → defaults match Datum CTH → 동등성 결정적 보장.
4. **REQ-28-04**: ParamBase auto string serialization handles INI default `""` — no INI code change.
5. **REQ-28-05**: ICustomTypeDescriptor NOT implemented (정적 노출 only).
6. **REQ-28-06**: msbuild Debug/x64 PASS, 0 new errors/warnings, every new line tagged `//260508 hbk Phase 28`.

Purpose: Wire the FAI CircleDiameter measurement to the same polar-sampling detection path Datum CTH uses, gated by the new combo. Previous default behaviour (fit path) is preserved exactly for empty RadialDirection (REQ-28-04 INI 하위호환).

Output: One file modified. New: 1 field + 1 List wrapper + 1 branch. Existing 6 fields and the fit-path call site are untouched (only relocated inside an `if` block — byte-identical argument list).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/28-fai-circlediameter-datum-circle/28-SPEC.md
@.planning/phases/28-fai-circlediameter-datum-circle/28-CONTEXT.md
@.planning/phases/28-fai-circlediameter-datum-circle/28-01-SUMMARY.md
@WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs
@WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs
@WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
@WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs

<interfaces>
<!-- Combo binding pattern this plan MUST mimic — DatumConfig.cs lines 242-252 (Phase 17 D-02 + Phase 18 CO-01): -->
[ItemsSourceProperty(nameof(Circle_RadialDirectionList))] //260503 hbk Phase 17 D-02
public string Circle_RadialDirection { get; set; } = ""; //260503 hbk Phase 17 D-02

[PropertyTools.DataAnnotations.Browsable(false)] //260503 hbk Phase 17 D-02
public List<string> Circle_RadialDirectionList { get { return EdgeOptionLists.RadialDirections; } } //260503 hbk Phase 17 D-02

<!-- Existing CircleDiameterMeasurement.cs Edge category (lines 23-31) — append the new field IN this category, after EdgePolarity: -->
[Category("Edge")]
public int EdgeThreshold { get; set; } = 10;
public double Sigma { get; set; } = 1.0;
[ItemsSourceProperty(nameof(EdgePolarityList))]
public string EdgePolarity { get; set; } = "DarkToLight";

[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> EdgePolarityList { get { return EdgeOptionLists.FAIPolarities; } }

<!-- VisionAlgorithmService.TryFindCircle signature (line 120, current call site — DO NOT modify the method, only the call site): -->
public bool TryFindCircle(
    HImage image,
    double centerRow, double centerCol, double radius,
    HTuple datumTransform,
    double sigma, int threshold, string polarity,
    out double foundRow, out double foundCol, out double foundRadius,
    out string error)

<!-- VisionAlgorithmService.TryFindCircleByPolarSampling signature (line 214 — exact arg order, copy verbatim): -->
public bool TryFindCircleByPolarSampling(
    HImage image,
    double centerRow, double centerCol, double radius,
    double stepDeg, double rectL1Ratio, double rectL2Ratio,
    double sigma, int threshold, string polarity, string selection,
    HTuple datumTransform,
    out double foundRow, out double foundCol, out double foundRadius,
    out HTuple edgeRows, out HTuple edgeCols,
    out bool[] stripSuccesses,
    out string error)

<!-- Plan 01 — newly available symbols in EdgeOptionLists (must already be merged before this plan executes): -->
EdgeOptionLists.MapRadialDirectionToHalconPolarity(string radial) → string  // "Outward" (case-insensitive) → "negative", else → "positive"
EdgeOptionLists.FaiCirclePolarStepDeg   = 10.0     (double const)
EdgeOptionLists.FaiCircleRectL1Ratio    = 0.02     (double const)
EdgeOptionLists.FaiCircleRectL2Ratio    = 0.02     (double const)
EdgeOptionLists.FaiCircleEdgeSelection  = "First"  (string const)
EdgeOptionLists.RadialDirections        — existing Phase 17 D-02 List<string> { "Inward", "Outward" }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Circle_RadialDirection field + List wrapper to CircleDiameterMeasurement (REQ-28-01, REQ-28-04, REQ-28-05)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs (full read — only 62 lines; this is the file you are editing)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs lines 235-252 (analog combo binding pattern — Phase 18 CO-01 verified)
    - WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs (post-Plan-01 state — confirms RadialDirections + new helper exist)
    - .planning/phases/28-fai-circlediameter-datum-circle/28-CONTEXT.md §decisions D-06, D-07 (Edge category placement + 2-option combo locked)
    - .planning/phases/28-fai-circlediameter-datum-circle/28-SPEC.md REQ-28-01, REQ-28-05 (combo + no ICustomTypeDescriptor locked)
  </read_first>
  <action>
Open `WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs`. Add the new field + list wrapper inside the existing `[Category("Edge")]` group (D-06: same category as EdgeThreshold/Sigma/EdgePolarity), placed AFTER the existing `EdgePolarity` declaration and BEFORE the existing `EdgePolarityList` wrapper, then add the matching `Circle_RadialDirectionList` wrapper alongside `EdgePolarityList`.

**Exact insertion** — after the current line 27 (`public string EdgePolarity { get; set; } = "DarkToLight";`), inject:

```csharp
        [ItemsSourceProperty(nameof(Circle_RadialDirectionList))] //260508 hbk Phase 28 REQ-28-01 (D-06/D-07 — Edge category 2옵션 콤보)
        public string Circle_RadialDirection { get; set; } = ""; //260508 hbk Phase 28 REQ-28-01/REQ-28-04 (default "" → fit 경로 → INI 하위호환)
```

Then, after the existing line 31 (`public List<string> EdgePolarityList { get { return EdgeOptionLists.FAIPolarities; } }`), inject:

```csharp

        //260508 hbk Phase 28 REQ-28-01 — RadialDirection 콤보 옵션 래퍼 (Phase 17 D-02 단일 소스 EdgeOptionLists.RadialDirections 직접 참조)
        [PropertyTools.DataAnnotations.Browsable(false)] //260508 hbk Phase 28
        public List<string> Circle_RadialDirectionList { get { return EdgeOptionLists.RadialDirections; } } //260508 hbk Phase 28
```

Implementation notes:

- **Style**: K&R brace style (CLAUDE.md project convention for CircleDiameterMeasurement.cs — note this file uses the legacy K&R `{ get; set; }` single-line style for properties; mirror it). The wrapper getter uses `{ get { return ...; } }` single-line form like the existing `EdgePolarityList` (line 31).
- **C# version**: 7.2 — no expression-bodied members, no `=>` for the getter (mirror the existing `EdgePolarityList` style verbatim).
- **Default value**: `""` (empty string) per REQ-28-04 — fit path on missing INI key. Do NOT use `null` (ParamBase auto-deserializes missing keys to `""` for string fields, and existing INIs lack this key).
- **Category**: Field falls under the EdgeThreshold-attached `[Category("Edge")]` (D-06). Do NOT add a new `[Category(...)]` attribute on the new field — that would split the category visually.
- **Attribute order**: `[ItemsSourceProperty(...)]` first, then property declaration. Mirror the EdgePolarity pattern at lines 26-27.
- **DO NOT** implement `ICustomTypeDescriptor` (REQ-28-05). DO NOT add hide logic. DO NOT touch the existing 6 fields.
- **DO NOT** add new `using` directives. The file already imports `System.Collections.Generic` (line 2), `PropertyTools.DataAnnotations` (line 4), and the namespace is already `ReringProject.Sequence` so `EdgeOptionLists` resolves directly (it lives in the same namespace).
- **Comment**: Every new line (attribute lines + property declaration line + wrapper getter line) MUST end with `//260508 hbk Phase 28` per project comment convention.

This task only adds the field; the `TryExecute` branch is Task 2 (separated to keep the diff reviewable).
  </action>
  <verify>
    <automated>powershell -Command "cd C:\Info\Project\DataMeasurement; $errors = & msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /m /nologo /verbosity:minimal 2>&amp;1 | Select-String -Pattern 'error CS'; if ($errors) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs -Pattern 'public string Circle_RadialDirection \{ get; set; \} = "";'` returns exactly 1 match (AC-1)
    - `Select-String -Path WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs -Pattern 'ItemsSourceProperty\(nameof\(Circle_RadialDirectionList\)\)'` returns exactly 1 match (AC-1)
    - `Select-String -Path WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs -Pattern 'public List<string> Circle_RadialDirectionList'` returns exactly 1 match (AC-1)
    - `Select-String -Path WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs -Pattern 'EdgeOptionLists\.RadialDirections'` returns exactly 1 match (single-source binding)
    - `(Select-String -Path WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs -Pattern 'ICustomTypeDescriptor').Matches.Count` returns 0 (REQ-28-05)
    - `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /m` returns exit 0 with 0 new `error CS\d+` and 0 new `warning CS\d+` (REQ-28-06)
    - All new lines tagged `//260508 hbk Phase 28` (AC-6)
  </acceptance_criteria>
  <done>
Circle_RadialDirection field declared with combo binding; ItemsSource wrapper returns EdgeOptionLists.RadialDirections; ICustomTypeDescriptor not introduced; existing 6 fields untouched; build passes. PropertyGrid will now show the combo (verified visually in Plan 04 UAT).
  </done>
</task>

<task type="auto">
  <name>Task 2: Branch TryExecute — empty → fit path (existing), Inward/Outward → polar path (REQ-28-02, REQ-28-03)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs (post-Task-1 state — full file, includes the new Circle_RadialDirection field)
    - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs lines 120-200 (TryFindCircle current signature — argument list unchanged, just relocated into `else` branch)
    - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs lines 214-370 (TryFindCircleByPolarSampling signature + sanity clamps — confirms FaiCirclePolarStepDeg=10.0 / Selection="First" pass through the clamp without reset)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs lines 195-215 AND 720-745 (Datum CTH polar call site — argument-order template; replicate the same arg passing style)
    - WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs (post-Plan-01 state — MapRadialDirectionToHalconPolarity + 4 FAI const present)
    - .planning/phases/28-fai-circlediameter-datum-circle/28-CONTEXT.md §code_context Integration Points (verbatim branch code template) + §decisions D-01, D-02, D-04, D-08
  </read_first>
  <action>
Replace the entire `TryExecute` method body (currently lines 35-60) with a branch on `Circle_RadialDirection`. The empty-string branch MUST preserve the existing fit-path argument list byte-for-byte (REQ-28-04 INI 하위호환). The non-empty branch calls `TryFindCircleByPolarSampling` directly (D-01) using `MapRadialDirectionToHalconPolarity` (D-02) + the 4 new FAI defaults (D-04).

**Exact replacement** — replace the existing method body content (from `resultValue = 0;` through `return true;`) with:

```csharp
            resultValue = 0;
            error = null;
            overlays = new List<EdgeInspectionOverlay>(); //260422 hbk Phase 7: 5종 overlay 미구현 — 빈 리스트 반환 (D-03)

            var svc = new VisionAlgorithmService();
            double foundRow, foundCol, foundRadius;

            //260508 hbk Phase 28 REQ-28-02 (D-01) — Circle_RadialDirection 빈값=fit / Inward,Outward=polar 분기
            if (string.IsNullOrEmpty(Circle_RadialDirection)) //260508 hbk Phase 28
            {
                //260508 hbk Phase 28 REQ-28-04 — 기존 fit 경로 (인자 순서/EdgePolarity 사용 규칙 v1.0 동일 → INI 하위호환 회귀 0)
                if (!svc.TryFindCircle(image,
                    Circle_Row, Circle_Col, Circle_Radius,
                    datumTransform,
                    Sigma, EdgeThreshold, EdgePolarity,
                    out foundRow, out foundCol, out foundRadius, out error))
                {
                    return false; //260508 hbk Phase 28
                }
            }
            else //260508 hbk Phase 28
            {
                //260508 hbk Phase 28 REQ-28-02/REQ-28-03 (D-02, D-04, D-08) — polar 경로:
                //  polarity = MapRadialDirectionToHalconPolarity(Circle_RadialDirection) (단일 소스, EdgePolarity 무시)
                //  step/L1/L2/selection = EdgeOptionLists.FaiCircle* defaults (Datum CTH default 와 동일 → 동등성 결정적)
                string polarity = EdgeOptionLists.MapRadialDirectionToHalconPolarity(Circle_RadialDirection); //260508 hbk Phase 28
                HTuple unusedRows, unusedCols; //260508 hbk Phase 28
                bool[] unusedStrips; //260508 hbk Phase 28
                if (!svc.TryFindCircleByPolarSampling(
                    image,
                    Circle_Row, Circle_Col, Circle_Radius,
                    EdgeOptionLists.FaiCirclePolarStepDeg,
                    EdgeOptionLists.FaiCircleRectL1Ratio,
                    EdgeOptionLists.FaiCircleRectL2Ratio,
                    Sigma, EdgeThreshold, polarity,
                    EdgeOptionLists.FaiCircleEdgeSelection,
                    datumTransform,
                    out foundRow, out foundCol, out foundRadius,
                    out unusedRows, out unusedCols, out unusedStrips,
                    out error)) //260508 hbk Phase 28
                {
                    return false; //260508 hbk Phase 28
                }
            }

            resultValue = foundRadius * 2.0 * pixelResolution;
            return true;
```

Implementation notes (NON-negotiable):

- **Argument matching**: The polar arg list MUST match `VisionAlgorithmService.TryFindCircleByPolarSampling` line 214 signature exactly. Re-confirm the signature against the read file (15 args + 7 outs). If the signature read differs from the template above, adjust to match the read source — the source is authoritative.
- **Fit path preserved byte-for-byte**: The fit branch's argument list (`image, Circle_Row, Circle_Col, Circle_Radius, datumTransform, Sigma, EdgeThreshold, EdgePolarity, out ...`) must be identical to the current line 49-53 call. Only the surrounding `if/else` is new. This is the REQ-28-04 hinge — any change here breaks v1.0 INI 회귀.
- **EdgePolarity ignored on polar branch (D-08)**: The polar branch derives polarity from Circle_RadialDirection only. Do NOT pass EdgePolarity. Do NOT hide EdgePolarity from the grid (REQ-28-05).
- **No new `using` directives**: All required types (`HTuple`, `EdgeOptionLists`, `VisionAlgorithmService`, `EdgeInspectionOverlay`, `List<>`) are already imported (lines 1-6 of the file).
- **Brace style**: K&R (mirror existing file style — opening brace on same line as `if`, `else`).
- **C# 7.2**: No `is null`, no switch expressions, no pattern matching enhancements. `string.IsNullOrEmpty` is .NET Framework 1.1+ and matches Phase 18 CO-01 patterns.
- **Comment**: Every line of the new branch (every `if`, `else`, `return false;`, declaration, multi-line method call CONTAINING new code) MUST end with `//260508 hbk Phase 28` per project comment convention. Lines that are byte-identical to v1.0 (the inner fit-path call arguments + the final `resultValue = ...` and `return true;`) keep their original comment state — do NOT re-tag them.
- **Out parameter overlays**: `overlays = new List<EdgeInspectionOverlay>();` line is preserved verbatim from current line 45 (Phase 7 D-03 contract — 5 measurements return empty overlays, untouched).
- **Variable scope**: `foundRow, foundCol, foundRadius` are declared once before the `if`/`else` so both branches assign them; existing pattern (current line 48) is reused.
  </action>
  <verify>
    <automated>powershell -Command "cd C:\Info\Project\DataMeasurement; $errors = & msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /m /nologo /verbosity:minimal 2>&amp;1 | Select-String -Pattern 'error CS'; if ($errors) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs -Pattern 'string\.IsNullOrEmpty\(Circle_RadialDirection\)'` returns exactly 1 match (AC-2/AC-3 branch)
    - `Select-String -Path WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs -Pattern 'TryFindCircleByPolarSampling'` returns exactly 1 match (AC-3)
    - `Select-String -Path WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs -Pattern 'TryFindCircle\b'` returns exactly 1 match (AC-2 — fit path preserved, single call site only)
    - `Select-String -Path WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs -Pattern 'EdgeOptionLists\.MapRadialDirectionToHalconPolarity'` returns exactly 1 match (D-02 single source)
    - `Select-String -Path WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs -Pattern 'EdgeOptionLists\.FaiCirclePolarStepDeg'` returns exactly 1 match (D-04)
    - `Select-String -Path WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs -Pattern 'EdgeOptionLists\.FaiCircleEdgeSelection'` returns exactly 1 match (D-04)
    - `(Select-String -Path WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs -Pattern 'ICustomTypeDescriptor').Matches.Count` returns 0 (REQ-28-05)
    - `(Select-String -Path WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs -Pattern '260508 hbk Phase 28').Matches.Count` returns ≥ count of NEW lines added in Tasks 1+2 (AC-6)
    - `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /m` returns exit 0 with 0 new `error CS\d+` and 0 new `warning CS\d+`
    - Diff shows fit-path argument list (`image, Circle_Row, Circle_Col, Circle_Radius, datumTransform, Sigma, EdgeThreshold, EdgePolarity, out foundRow, out foundCol, out foundRadius, out error`) is BYTE-IDENTICAL to v1.0 — verified by `git diff -U10 -- WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs` showing the call args inside the `if` are unchanged from previous version
    - `resultValue = foundRadius * 2.0 * pixelResolution;` and `return true;` lines remain at the end of TryExecute, unchanged (post-branch — both paths converge)
  </acceptance_criteria>
  <done>
TryExecute branches correctly:
  - Circle_RadialDirection = "" → TryFindCircle (fit) called with v1.0-identical args → REQ-28-04 보장
  - Circle_RadialDirection = "Inward" or "Outward" → TryFindCircleByPolarSampling called with mapped polarity + FAI defaults → REQ-28-02/REQ-28-03 결정적 동등성
  - REQ-28-05 ICustomTypeDescriptor 미도입 (grep count = 0)
  - msbuild Debug/x64 PASS, 0 new errors/warnings (REQ-28-06)
  - Plan 03 (DatumFindingService inline cleanup) and Plan 04 (UAT) unblocked
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INI file → ParamBase deserialization | String fields (Circle_RadialDirection) auto-deserialized — input domain is constrained to file content the user already trusts (recipe files in app local storage). No network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02 | Tampering | CircleDiameterMeasurement.Circle_RadialDirection (INI input) | accept | INI input already validated by ParamBase string deserialization (Phase 5 contract). Unknown values (anything other than "Inward"/"Outward") map to "positive" via MapRadialDirectionToHalconPolarity → behavioural fallback to a valid Halcon polarity. No code execution risk. |
| T-28-03 | Information Disclosure | TryFindCircleByPolarSampling stripSuccesses out param | accept | Discarded via `unusedStrips` local — not logged, not surfaced to UI. No PII or auth secrets handled. |

Per CONTEXT.md: "no new attack surface — modifies in-process measurement algorithm parameters; INI input already validated by ParamBase string deserialization."
</threat_model>

<verification>
- AC-1: PropertyGrid combo ▼ visible (visual UAT in Plan 04)
- AC-2: empty Circle_RadialDirection → grep TryFindCircle 1 / TryFindCircleByPolarSampling 1 (1 = polar branch dead code, runtime takes fit branch)
- AC-3: non-empty → runtime takes polar branch
- AC-6: msbuild Debug/x64 PASS, all new lines tagged
- REQ-28-04: fit-path argument list byte-identical to v1.0 (verified via git diff)
- REQ-28-05: ICustomTypeDescriptor grep count = 0
</verification>

<success_criteria>
- 1 new field (Circle_RadialDirection) + 1 new List wrapper + 1 new branch in TryExecute
- Existing 6 fields unchanged; existing fit-path argument list byte-identical to v1.0
- ICustomTypeDescriptor not introduced
- msbuild Debug/x64 PASS, 0 new CS errors/warnings
- All new lines tagged `//260508 hbk Phase 28`
</success_criteria>

<output>
After completion, create `.planning/phases/28-fai-circlediameter-datum-circle/28-02-SUMMARY.md` documenting:
- Field declaration + combo binding (Tasks 1)
- TryExecute branch logic (Task 2) with both call signatures cited
- Confirmation that v1.0 fit-path args are byte-identical (with git diff -U10 excerpt for the `if`-block contents)
- ICustomTypeDescriptor grep count = 0 (REQ-28-05 evidence)
- msbuild result + warning count delta
- Open: AC-4 / AC-5 SIMUL_MODE UAT pending (Plan 04)
</output>
