---
phase: 28-fai-circlediameter-datum-circle
plan: 03
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
autonomous: true
requirements: [REQ-28-02, REQ-28-06]
tags: [datum, polarity-mapping-cleanup, dry-up]
must_haves:
  truths:
    - "DatumFindingService.TryFindCircleTwoHorizontal (line 200) uses EdgeOptionLists.MapRadialDirectionToHalconPolarity instead of inline string.Equals(...) ? \"negative\" : \"positive\""
    - "DatumFindingService.TryTeachCircleTwoHorizontal (line 730) uses EdgeOptionLists.MapRadialDirectionToHalconPolarity instead of the same inline ternary"
    - "Datum CTH runtime/teaching behaviour regression = 0 (helper output byte-identical to the inline expressions for all inputs — proven in Plan 01)"
    - "After cleanup, polarity mapping logic exists in exactly ONE place (EdgeOptionLists.MapRadialDirectionToHalconPolarity) — DRY single source achieved"
  artifacts:
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "DatumFindingService with helper-call polarity mapping (cleanup)"
      contains: "EdgeOptionLists.MapRadialDirectionToHalconPolarity"
  key_links:
    - from: "DatumFindingService.TryFindCircleTwoHorizontal:200"
      to: "EdgeOptionLists.MapRadialDirectionToHalconPolarity (Plan 01)"
      via: "static helper call (replaces inline ternary)"
      pattern: "EdgeOptionLists\\.MapRadialDirectionToHalconPolarity\\(config\\.Circle_RadialDirection\\)"
    - from: "DatumFindingService.TryTeachCircleTwoHorizontal:730"
      to: "EdgeOptionLists.MapRadialDirectionToHalconPolarity (Plan 01)"
      via: "static helper call (replaces inline ternary)"
      pattern: "EdgeOptionLists\\.MapRadialDirectionToHalconPolarity\\(config\\.Circle_RadialDirection\\)"
---

<objective>
Replace the 2 duplicated inline polarity-mapping ternaries in `DatumFindingService.cs` (lines 200 and 730) with calls to the new `EdgeOptionLists.MapRadialDirectionToHalconPolarity` helper from Plan 01. After this plan:

- The polarity mapping rule (`"Outward" (case-insensitive) → "negative"`, else → `"positive"`) lives in exactly ONE location (Plan 01).
- Datum CTH runtime/teaching behaviour is regression-0 (helper output is byte-identical to the previous inline expressions for all inputs — proven in Plan 01 acceptance).
- Plan 02's polar-path call from CircleDiameterMeasurement and Datum CTH share the same mapping function (D-03 single source achieved).

This is the D-03 "service layer cleanup" — explicitly carved into scope by the planner and CONTEXT.md (it modifies a service class, NOT a Measurement class, so it does NOT violate SPEC §Out-of-scope "다른 Measurement 클래스 변경 금지").

Output: One file modified — exactly 2 line replacements + comment tags. No method signatures change. No semantic change to Datum CTH.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/28-fai-circlediameter-datum-circle/28-SPEC.md
@.planning/phases/28-fai-circlediameter-datum-circle/28-CONTEXT.md
@.planning/phases/28-fai-circlediameter-datum-circle/28-01-SUMMARY.md
@WPF_Example/Halcon/Algorithms/DatumFindingService.cs
@WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs

<interfaces>
<!-- Existing using directives in DatumFindingService.cs (lines 1-7): -->
using System;
using HalconDotNet;
using ReringProject.Sequence;          // ← EdgeOptionLists 가 이 namespace 에 있음 — 추가 using 불필요
using ReringProject.Setting;
using ReringProject.Utility;

<!-- Plan 01 — newly available helper in EdgeOptionLists: -->
EdgeOptionLists.MapRadialDirectionToHalconPolarity(string radial) → string
  // null/empty/anything-not-Outward (case-insensitive) → "positive"
  // "Outward" / "OUTWARD" / "outward" / etc.            → "negative"
  // BYTE-IDENTICAL to: string.Equals(x, "Outward", System.StringComparison.OrdinalIgnoreCase) ? "negative" : "positive"

<!-- Replacement targets (current code — DO NOT keep these inline expressions): -->

// Line 200 (inside TryFindCircleTwoHorizontal):
string circlePolarity = string.Equals(config.Circle_RadialDirection, "Outward", System.StringComparison.OrdinalIgnoreCase) ? "negative" : "positive";

// Line 730 (inside TryTeachCircleTwoHorizontal):
string circlePolarity = string.Equals(config.Circle_RadialDirection, "Outward", System.StringComparison.OrdinalIgnoreCase) ? "negative" : "positive";

<!-- Both lines are functionally equivalent — only their containing method differs. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline polarity ternary at DatumFindingService.cs line 200 (TryFindCircleTwoHorizontal)</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs lines 1-10 (using directives — confirms `using ReringProject.Sequence;` present, no new import needed)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs lines 185-215 (full surrounding context for TryFindCircleTwoHorizontal — to confirm exact line and that the variable `circlePolarity` is only used by the immediately-following `TryFindCircleByPolarSampling` call)
    - WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs (post-Plan-01 state — confirms helper exists with byte-identical mapping)
    - .planning/phases/28-fai-circlediameter-datum-circle/28-CONTEXT.md §decisions D-03 (locked: 2 inline expressions → helper call)
  </read_first>
  <action>
At `WPF_Example/Halcon/Algorithms/DatumFindingService.cs` line 200, replace the existing line:

```csharp
                string circlePolarity = string.Equals(config.Circle_RadialDirection, "Outward", System.StringComparison.OrdinalIgnoreCase) ? "negative" : "positive";
```

with:

```csharp
                string circlePolarity = EdgeOptionLists.MapRadialDirectionToHalconPolarity(config.Circle_RadialDirection); //260508 hbk Phase 28 D-03 — inline ternary → helper 호출 (DRY)
```

Implementation notes:

- **Indentation**: Match the existing line (16 spaces — inside `try { ... }` inside method body inside class). Mirror the leading whitespace of the line being replaced.
- **No new `using` directive**: `EdgeOptionLists` is in `ReringProject.Sequence`, already imported on line 4 of DatumFindingService.cs. Use unqualified `EdgeOptionLists.MapRadialDirectionToHalconPolarity` — do NOT use a fully-qualified name (existing file convention prefers unqualified for already-imported types).
- **Variable name preserved**: `circlePolarity` keeps its name and type (`string`) — the immediately-following call to `TryFindCircleByPolarSampling` (line 206) consumes it as the `polarity` argument; do NOT rename or alter the call site.
- **Comment**: The new line carries `//260508 hbk Phase 28 D-03 — inline ternary → helper 호출 (DRY)` per project convention.
- **DO NOT** modify line 730 in this task (Task 2 handles it). DO NOT modify any other line.
- **DO NOT** modify the `unusedStrips` declaration on line 201 — that is Phase 18 CO-05 code and out of Phase 28 scope.
- **DO NOT** introduce any C# 8+ features.

Verify after edit by re-reading the surrounding `using ReringProject.Sequence;` (line 4) is still present and that the replaced line is now exactly the helper call — `circlePolarity` consumers (the polar-sampling call below) are unchanged.
  </action>
  <verify>
    <automated>powershell -Command "cd C:\Info\Project\DataMeasurement; $errors = & msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /m /nologo /verbosity:minimal 2>&amp;1 | Select-String -Pattern 'error CS'; if ($errors) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - Inside the `TryFindCircleTwoHorizontal` method body (find via `Select-String -Pattern 'TryFindCircleTwoHorizontal' -Context 0,80`), the line `string circlePolarity = string.Equals\(config\.Circle_RadialDirection, "Outward"` no longer appears (D-03 cleanup site #1 closed)
    - `Select-String -Path WPF_Example/Halcon/Algorithms/DatumFindingService.cs -Pattern 'EdgeOptionLists\.MapRadialDirectionToHalconPolarity\(config\.Circle_RadialDirection\)'` returns ≥ 1 match (this task adds 1; Task 2 will add another to reach 2)
    - The variable name `circlePolarity` still appears in the `TryFindCircleTwoHorizontal` body (variable rename not performed)
    - msbuild Debug/x64 PASS, 0 new errors/warnings
  </acceptance_criteria>
  <done>
Line 200 inline ternary replaced with helper call. `circlePolarity` value flows unchanged into the immediately-following `TryFindCircleByPolarSampling` call. Datum CTH runtime behaviour preserved (helper output byte-identical per Plan 01).
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace inline polarity ternary at DatumFindingService.cs line 730 (TryTeachCircleTwoHorizontal)</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs lines 715-745 (full surrounding context for TryTeachCircleTwoHorizontal — confirms the line and that `circlePolarity` is consumed only by the immediately-following polar call)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs around line 200 (post-Task-1 state — to confirm Task 1 changed only its target line and provides a working template for the same edit)
    - .planning/phases/28-fai-circlediameter-datum-circle/28-CONTEXT.md §decisions D-03
  </read_first>
  <action>
At `WPF_Example/Halcon/Algorithms/DatumFindingService.cs` line 730, replace the existing line:

```csharp
                string circlePolarity = string.Equals(config.Circle_RadialDirection, "Outward", System.StringComparison.OrdinalIgnoreCase) ? "negative" : "positive";
```

with:

```csharp
                string circlePolarity = EdgeOptionLists.MapRadialDirectionToHalconPolarity(config.Circle_RadialDirection); //260508 hbk Phase 28 D-03 — inline ternary → helper 호출 (DRY)
```

Implementation notes:

- Identical replacement template to Task 1 — only the file location (line 730 vs 200) differs.
- Match the existing line's leading whitespace (16 spaces — inside `try { }` inside `TryTeachCircleTwoHorizontal`).
- Preserve the existing comment on line 729 (`//260503 hbk Phase 17 D-02 — Circle_RadialDirection ("Inward"/"Outward") → polarity ("positive"/"negative") override (EdgePolarity 무시)`) verbatim — it is now slightly redundant with the new helper, but keeping it preserves the historical decision trail (Phase 17 D-02 origin) and avoids unrelated diff churn.
- Do NOT modify line 731 (`//260426 hbk Phase 14-05 — ...`) or any other line.
- Do NOT modify the `circleStrips` declaration (line 732) or the `TryFindCircleByPolarSampling` call site (lines 733-739) — those are Phase 14-05 + Phase 18 CO-05 code and out of Phase 28 scope.
- Variable name `circlePolarity` preserved.
  </action>
  <verify>
    <automated>powershell -Command "cd C:\Info\Project\DataMeasurement; $errors = & msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /m /nologo /verbosity:minimal 2>&amp;1 | Select-String -Pattern 'error CS'; if ($errors) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path WPF_Example/Halcon/Algorithms/DatumFindingService.cs -Pattern 'string\.Equals\(config\.Circle_RadialDirection.*"Outward".*"negative"' -CaseSensitive` returns 0 matches (both sites D-03 cleaned)
    - `(Select-String -Path WPF_Example/Halcon/Algorithms/DatumFindingService.cs -Pattern 'EdgeOptionLists\.MapRadialDirectionToHalconPolarity\(config\.Circle_RadialDirection\)').Matches.Count` returns exactly 2 (both call sites converted)
    - Both `TryFindCircleTwoHorizontal` and `TryTeachCircleTwoHorizontal` method bodies still contain `circlePolarity` variable reference (variable rename not performed)
    - The line `if (!visionSvc.TryFindCircleByPolarSampling(` near line 733 is unchanged (Task 2 only touches line 730 helper assignment, NOT the polar call site)
    - msbuild Debug/x64 PASS, 0 new errors/warnings
    - `git diff --numstat -- WPF_Example/Halcon/Algorithms/DatumFindingService.cs` shows exactly 2 lines added + 2 lines removed (net 0 line count change) — the cleanup is line-for-line replacement
    - All 2 new lines tagged `//260508 hbk Phase 28 D-03`
  </acceptance_criteria>
  <done>
Both inline ternaries (line 200 + line 730) replaced with helper calls. Polarity mapping logic now exists in exactly one location (EdgeOptionLists). Datum CTH find/teach paths regression-0 (Plan 01 byte-identical mapping guarantee). msbuild Debug/x64 PASS, 0 new errors/warnings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan replaces 2 inline expressions with calls to a deterministic pure function. No I/O, no input parsing, no network surface, no auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Tampering | DatumFindingService polarity mapping | accept | Helper output is byte-identical to the replaced inline expressions for ALL inputs (proven in Plan 01 acceptance). Datum CTH semantics preserved — no new attack surface. |

Per CONTEXT.md: "no new attack surface — modifies in-process measurement algorithm parameters; INI input already validated by ParamBase string deserialization."
</threat_model>

<verification>
- 2 helper call sites in DatumFindingService.cs (grep count exactly 2)
- 0 inline `string.Equals(config.Circle_RadialDirection.*"Outward".*"negative"` remaining
- msbuild Debug/x64 PASS, 0 new errors/warnings
- Datum CTH UAT not required — helper output equivalence is mathematical (Plan 01 acceptance proves it). Plan 04 covers the FAI-side UAT only.
</verification>

<success_criteria>
- 2 inline ternaries → 2 helper calls (line 200 + line 730)
- DRY single source: polarity mapping rule lives in exactly 1 place (EdgeOptionLists.MapRadialDirectionToHalconPolarity)
- msbuild Debug/x64 PASS, 0 new CS errors/warnings
- All new lines tagged `//260508 hbk Phase 28 D-03`
</success_criteria>

<output>
After completion, create `.planning/phases/28-fai-circlediameter-datum-circle/28-03-SUMMARY.md` documenting:
- 2 line replacements (line 200 + line 730)
- Confirmation that helper grep count = 2 in DatumFindingService.cs
- Confirmation that no inline `string.Equals(...)? "negative" : "positive"` remains in DatumFindingService.cs
- msbuild result + warning count delta
- Note: Datum CTH UAT not required (mathematical equivalence) — only FAI-side UAT in Plan 04
</output>
