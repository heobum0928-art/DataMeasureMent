---
phase: 28-fai-circlediameter-datum-circle
plan: 04
type: execute
wave: 3
depends_on: [28-02, 28-03]
files_modified:
  - .planning/phases/28-fai-circlediameter-datum-circle/28-UAT.md
autonomous: false
requirements: [REQ-28-01, REQ-28-03, REQ-28-04, REQ-28-06]
tags: [uat, simul-mode, equivalence-verification]
must_haves:
  truths:
    - "PropertyGrid Circle_RadialDirection 콤보 ▼ 가 Inward/Outward 2옵션으로 표시된다 (AC-1 visual 검증)"
    - "동일 ROI/이미지로 Datum CTH (RadialDirection=Inward) 와 FAI CircleDiameter (RadialDirection=Inward) 실행 시 검출 직경 차이 <= 0.001 mm (AC-4)"
    - "v1.0 시점 INI 레시피 (Circle_RadialDirection 키 없음) 로드 시 CircleDiameter 결과가 v1.0 결과와 동일 (AC-5 / REQ-28-04)"
    - "msbuild Debug/x64 PASS, 0 new errors, 0 new warnings (AC-6 / REQ-28-06)"
  artifacts:
    - path: ".planning/phases/28-fai-circlediameter-datum-circle/28-UAT.md"
      provides: "UAT 시나리오 + 사용자 확인 결과 (PASS/FAIL/INVALID)"
      contains: "Test 1, Test 2, Test 3, Test 4"
  key_links:
    - from: "Plan 02 (CircleDiameterMeasurement field + branch)"
      to: "AC-1 PropertyGrid 콤보 visual + AC-4 동등성 UAT + AC-5 INI 회귀 UAT"
      via: '사용자가 SIMUL_MODE (D:\1.bmp) 에서 직접 검증'
      pattern: "manual UAT — checkpoint:human-verify"
    - from: "Plan 03 (DatumFindingService cleanup)"
      to: "AC-6 msbuild + Datum CTH 회귀 0 (Plan 01 mathematical equivalence + msbuild PASS)"
      via: "no Datum-side UAT required (helper byte-identical guarantee)"
      pattern: "build verification"
---

<objective>
사용자 UAT 로 Phase 28 Acceptance Criteria 4 항목 (AC-1, AC-4, AC-5, AC-6) 을 검증하고 결과를 `28-UAT.md` 에 기록한다.

자동화 가능한 모든 부분 (msbuild PASS, grep verification) 은 Plans 01-03 이 이미 처리. 이 plan 은 SIMUL_MODE (D:\1.bmp) 에서 사용자만이 수행 가능한 검증 — PropertyGrid 콤보 시각 + Datum↔FAI 동등성 + v1.0 INI 회귀 — 을 묶는다.

Purpose: REQ-28-03 동등성 (D-04 default 일치로 결정적이지만 사용자 확인 필요), REQ-28-04 INI 하위호환 (실 INI 로드), AC-1 콤보 시각 노출 모두 사용자 인지 외 자동화 불가능.

Output: `28-UAT.md` 작성 (frontmatter + 4 Test scenario + Summary). 코드 변경 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/28-fai-circlediameter-datum-circle/28-SPEC.md
@.planning/phases/28-fai-circlediameter-datum-circle/28-CONTEXT.md
@.planning/phases/28-fai-circlediameter-datum-circle/28-01-SUMMARY.md
@.planning/phases/28-fai-circlediameter-datum-circle/28-02-SUMMARY.md
@.planning/phases/28-fai-circlediameter-datum-circle/28-03-SUMMARY.md
@.planning/phases/19-propertygrid-dynamic-exposure/19-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate 28-UAT.md skeleton with 4 test scenarios + run msbuild verification (AC-6)</name>
  <files>.planning/phases/28-fai-circlediameter-datum-circle/28-UAT.md</files>
  <read_first>
    - .planning/phases/19-propertygrid-dynamic-exposure/19-UAT.md (analog UAT format — frontmatter, Test header structure, Summary table)
    - .planning/phases/28-fai-circlediameter-datum-circle/28-SPEC.md §Acceptance Criteria (AC-1..AC-6 source of truth)
    - .planning/phases/28-fai-circlediameter-datum-circle/28-CONTEXT.md §decisions D-05 (AC-4 검증 방법: 동일 ROI/Sigma/EdgeThreshold/RadialDirection 설정 후 결과 비교)
  </read_first>
  <action>
Create `.planning/phases/28-fai-circlediameter-datum-circle/28-UAT.md` with the following exact structure (mirror Phase 19 UAT format):

```markdown
***
phase: 28-fai-circlediameter-datum-circle
type: uat
status: pending
created: 2026-05-08
total: 4
passed: 0
failed: 0
pending: 4
***
# Phase 28 — UAT: FAI CircleDiameter + Datum Circle 알고리즘 통합

검증 대상: SPEC AC-1 / AC-4 / AC-5 / AC-6
환경: SIMUL_MODE (D:\1.bmp), Debug/x64 build
실행자: 사용자 (수동 UAT)
***
## Test 1 — AC-1: Circle_RadialDirection 콤보 PropertyGrid 시각 노출

**목적**: REQ-28-01 수용 — CircleDiameterMeasurement.Circle_RadialDirection 이 PropertyGrid 에 콤보 ▼ 으로 노출되고, 옵션이 정확히 Inward / Outward 2개인지 확인.

**Steps**:
1. `DatumMeasurement.exe` Debug/x64 실행
2. 검사 트리에서 임의 Shot 선택 → FAI 추가 (CircleDiameter 타입)
3. 새로 추가된 CircleDiameter FAI 선택 → 우측 PropertyGrid 확인
4. `Edge` 카테고리 펼침 — Circle_RadialDirection 행 존재 + 우측에 콤보 ▼ 표시 확인
5. 콤보 클릭 → 드롭다운 옵션 정확히 `Inward` / `Outward` 2개인지 확인 (3번째 옵션 또는 빈 항목 없음)

**Expected**:
- Circle_RadialDirection 행 표시됨
- 콤보 ▼ 펼침 시 Inward, Outward 만 표시 (Phase 17 D-02 단일 소스)
- 기본값 빈 문자열 (콤보 표시 영역은 비어있음)

**Result**: pending — 사용자 확인 후 PASS/FAIL/INVALID + 일자 기재
***
## Test 2 — AC-4: Datum CTH ↔ FAI CircleDiameter 검출 직경 동등성

**목적**: REQ-28-03 수용 — 동일 ROI / 동일 이미지 / 동일 파라미터에서 Datum CTH 와 FAI CircleDiameter 의 검출 직경 차이가 0.001 mm 이하인지 확인.

**Setup**:
- D:\1.bmp 이미지 로드
- Datum 알고리즘 = CircleTwoHorizontal 로 설정
- Datum Circle ROI 임의 위치/반지름 설정 (예: Row=500, Col=500, Radius=100)
- DatumConfig: Circle_Sigma=1.0, Circle_EdgeThreshold=10, Circle_RadialDirection=Inward, Circle_PolarStepDeg=10.0, Circle_RectL1Ratio=0.02, Circle_RectL2Ratio=0.02, Circle_EdgeSelection=First
- 동일 Shot 에 CircleDiameter FAI 추가, ROI 동일 (Circle_Row=500, Circle_Col=500, Circle_Radius=100), Sigma=1.0, EdgeThreshold=10, Circle_RadialDirection=Inward
- pixelResolution = 동일 캘리브레이션 값

**Steps**:
1. Datum 티칭 → 검출 직경 D_datum 기록 (DatumConfig 의 Circle 검출 결과 mm 환산: foundRadius * 2 * pixelResolution)
2. FAI CircleDiameter 검사 실행 → resultValue (mm) D_fai 기록
3. |D_fai - D_datum| 계산

**Expected**:
- |D_fai - D_datum| <= 0.001 mm (D-04 default 일치 + D-02 helper 단일 소스 → 결정적 동등성)

**Result**: pending — 사용자 확인 후 PASS/FAIL/INVALID + 측정값 기재
***
## Test 3 — AC-5: v1.0 시점 INI 레시피 (RadialDirection 키 없음) 로드 회귀 0

**목적**: REQ-28-04 수용 — Phase 28 변경 전 INI 레시피 (Circle_RadialDirection 키 없음) 로드 시 CircleDiameter 측정 결과가 v1.0 결과와 동일한지 확인.

**Setup**:
- Phase 28 변경 이전에 저장된 INI 레시피 1개 준비 (없으면 현재 INI 에서 `Circle_RadialDirection=` 라인을 텍스트 에디터로 삭제하여 재현)
- D:\1.bmp 이미지

**Steps**:
1. 위 INI 레시피 로드
2. CircleDiameter 측정 1회 실행 → resultValue 기록 (D_after)
3. (선택) git stash 또는 별도 브랜치에서 v1.0 코드로 동일 INI / 이미지 로드 → resultValue 기록 (D_before)
4. PropertyGrid 에서 Circle_RadialDirection 이 빈 값으로 표시되는지 확인 (default = "")
5. resultValue 가 v1.0 출력과 동일한지 확인 (or, D_before 미기록 시 PropertyGrid 콤보를 비운 상태로 측정 → 기존 fit 경로 호출 grep 확인)

**Expected**:
- Circle_RadialDirection 콤보 빈 값 표시
- TryExecute 가 fit 경로(TryFindCircle) 호출 (코드 inspection: 빈 문자열 → string.IsNullOrEmpty true → fit 분기)
- D_after == D_before (회귀 0) 또는 fit 경로 진입 확인 시 v1.0 동등 동작 보장

**Result**: pending — 사용자 확인 후 PASS/FAIL/INVALID + resultValue 기재
***
## Test 4 — AC-6: msbuild Debug/x64 PASS + 0 new errors + 0 new warnings

**목적**: REQ-28-06 수용 — Phase 28 변경 후 빌드 무결성 + 신규 경고/오류 0.

**Steps**:
1. PowerShell 에서 다음 실행:
   ```
   cd C:\Info\Project\DataMeasurement
   msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /m
   ```
2. 출력에서 `error CS\d+` count = 0 확인
3. 출력에서 `warning CS\d+` 새 경고 추가 없음 확인 (5 pre-existing 잔존 OK)
4. exit code = 0 확인

**Expected**:
- exit code 0
- new error CS\d+ count = 0
- new warning CS\d+ count = 0 (pre-existing 5건 그대로)
- DatumMeasurement.exe 생성됨 (`bin\x64\Debug\DatumMeasurement.exe` mtime 갱신)

**Result**: pending — Plan 04 실행자가 자동 명령으로 수행 후 PASS/FAIL 기재
***
## Summary

| Test | AC | Description | Result |
|------|------|-------------|--------|
| 1 | AC-1 | PropertyGrid Circle_RadialDirection 콤보 (Inward/Outward) | pending |
| 2 | AC-4 | Datum ↔ FAI 검출 직경 동등성 (<= 0.001 mm) | pending |
| 3 | AC-5 | v1.0 INI 회귀 0 | pending |
| 4 | AC-6 | msbuild PASS + 0 new errors/warnings | pending |

**Total**: 4 / **Passed**: 0 / **Failed**: 0 / **Pending**: 4

Sign-off 조건: 4/4 PASS 시 frontmatter status=signed_off + 일자 기재.
```

After writing the file, immediately execute Test 4 (msbuild) and update Test 4 result to PASS / FAIL with msbuild output snippet (exit code + new error/warning counts).

Implementation notes:

- **File location**: `.planning/phases/28-fai-circlediameter-datum-circle/28-UAT.md` (matches Phase 19 sibling pattern).
- **Format**: Match Phase 19 UAT (read 19-UAT.md to mirror frontmatter/section structure exactly — total/passed/failed/pending counts in frontmatter must match Summary table).
- **Date placeholder**: Tests 1/2/3 result lines stay as `pending` after writing — these are Task 2 (human-verify checkpoint).
- **Test 4 auto-execution**: This is the only AC verifiable without human intervention — execute msbuild via the verify command and write result inline.
- **No code changes** in this task. Only the UAT.md file is created.
  </action>
  <verify>
    <automated>powershell -Command "cd C:\Info\Project\DataMeasurement; if (-Not (Test-Path '.planning/phases/28-fai-circlediameter-datum-circle/28-UAT.md')) { exit 1 }; $errors = & msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /m /nologo /verbosity:minimal 2>&amp;1 | Select-String -Pattern 'error CS'; if ($errors) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/28-fai-circlediameter-datum-circle/28-UAT.md` exists
    - File contains 4 `## Test \d+` sections (Tests 1, 2, 3, 4)
    - Frontmatter has `status: pending`, `total: 4`, `passed: 0`, `failed: 0`, `pending: 4` (or already updated for Test 4 PASS — see below)
    - Each Test section references its target AC (AC-1, AC-4, AC-5, AC-6)
    - Test 2 includes the parameter list (Sigma, EdgeThreshold, Circle_RadialDirection, etc.) so the user can reproduce identical conditions
    - Test 4 result is PASS (with msbuild exit 0 + 0 new CS errors + 0 new CS warnings) — Test 4 self-completes via the verify command
    - msbuild Debug/x64 returns exit 0
    - All 4 plans 01-04 SUMMARY files referenced in UAT context (cross-link traceability)
  </acceptance_criteria>
  <done>
28-UAT.md created with 4 test scenarios. Test 4 (AC-6 / msbuild) self-verified PASS. Tests 1-3 pending user UAT in Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User SIMUL_MODE UAT — Tests 1, 2, 3 (AC-1, AC-4, AC-5)</name>
  <what-built>
- Plan 01: EdgeOptionLists.MapRadialDirectionToHalconPolarity helper + 4 polar default consts (FaiCirclePolarStepDeg=10.0, FaiCircleRectL1Ratio=0.02, FaiCircleRectL2Ratio=0.02, FaiCircleEdgeSelection="First").
- Plan 02: CircleDiameterMeasurement.Circle_RadialDirection field (combo, default "") + Circle_RadialDirectionList wrapper + TryExecute branch (empty → fit, Inward/Outward → polar via TryFindCircleByPolarSampling).
- Plan 03: DatumFindingService 2 inline polarity ternaries replaced with helper calls (line 200, 730).
- Plan 04 Task 1: 28-UAT.md scaffold + Test 4 (msbuild) auto-PASS.
  </what-built>
  <how-to-verify>

**Test 1 (AC-1) — PropertyGrid 콤보 시각 노출**

1. `bin\x64\Debug\DatumMeasurement.exe` 실행 (Debug/x64 빌드 후)
2. 검사 트리에서 임의 Shot 선택 → FAI 추가 (CircleDiameter 타입 선택)
3. 추가된 CircleDiameter FAI 노드를 트리에서 선택
4. 우측 PropertyGrid `Edge` 카테고리 펼침
5. 확인 사항:
   - Circle_RadialDirection 행 표시되는가?
   - 우측에 콤보 ▼ 가 보이는가?
   - 콤보 펼치면 정확히 `Inward`, `Outward` 2개 옵션만 표시되는가? (3번째 옵션, 빈 항목, 다른 텍스트 없음)
   - 기본 표시는 비어있는가? (default = "")

**Test 2 (AC-4) — Datum ↔ FAI 검출 직경 동등성**

1. D:\1.bmp 로드
2. Datum 알고리즘 = `CircleTwoHorizontal` 설정
3. Datum Circle ROI 설정: Row=500, Col=500, Radius=100 (또는 D:\1.bmp 에 적합한 임의 원 영역)
4. DatumConfig 파라미터:
   - Circle_Sigma = 1.0
   - Circle_EdgeThreshold = 10
   - Circle_RadialDirection = Inward
   - Circle_PolarStepDeg = 10.0
   - Circle_RectL1Ratio = 0.02
   - Circle_RectL2Ratio = 0.02
   - Circle_EdgeSelection = First
5. Datum 티칭 실행 → 검출된 Circle radius (px) 또는 직경 (mm) 기록 → D_datum
6. 동일 Shot 에 CircleDiameter FAI 추가, ROI:
   - Circle_Row = 500, Circle_Col = 500, Circle_Radius = 100
   - Sigma = 1.0, EdgeThreshold = 10
   - Circle_RadialDirection = Inward (Phase 28 신규 콤보 사용)
7. FAI 검사 실행 → resultValue (mm) 기록 → D_fai
8. |D_fai - D_datum| 계산 → 0.001 mm 이하면 PASS

**Test 3 (AC-5) — v1.0 INI 회귀 0**

1. 텍스트 에디터로 INI 레시피 파일을 열고 `Circle_RadialDirection=` 라인 자체를 삭제 (또는 Phase 28 이전 시점 INI 사용)
2. 앱 실행 → 해당 INI 로드
3. CircleDiameter FAI 선택 → PropertyGrid Circle_RadialDirection 표시값 확인 → 빈 값이어야 함
4. CircleDiameter 검사 실행 → resultValue 기록 → D_after
5. 비교 옵션 A: git stash 후 v1.0 commit checkout → 동일 INI/이미지로 측정 → D_before 기록 → D_after == D_before 확인
6. 비교 옵션 B (간이): PropertyGrid 콤보가 빈 값일 때 `string.IsNullOrEmpty(Circle_RadialDirection)` true → fit 경로 진입을 코드 inspection 으로 확인 (CircleDiameterMeasurement.cs 의 `if (string.IsNullOrEmpty(...))` 블록 진입 path)

**결과 기록**:

각 Test 의 result 라인을 다음 중 하나로 갱신:
- `**Result**: PASS — 2026-05-08 사용자 확인` + (Test 2/3 의 경우) 측정값
- `**Result**: FAIL — 2026-05-08 — [이유 / 차이값]` + 발견된 결함 설명
- `**Result**: INVALID — 2026-05-08 — [이유: 이미지 부적합, 환경 미준비 등]`

Summary 테이블의 result 컬럼 + frontmatter passed/failed/pending 카운트 갱신.
  </how-to-verify>
  <resume-signal>
사용자가 다음 중 하나로 응답:
- "approved" — 4/4 PASS 시 → 28-UAT.md frontmatter status=signed_off + 일자 기재 + Phase 28 시그널 컨플리트
- "Test N failed: [상세]" — FAIL 발생 시 → executor 가 Phase 28 carry-over 또는 hotfix plan 결정
- "Test N invalid: [이유]" — INVALID (환경 / 이미지 문제) 시 → 해당 Test 만 재시도 또는 deferred 처리
  </resume-signal>
  <files>(documentation only ??no code files modified; UAT result recorded in .planning/phases/28-fai-circlediameter-datum-circle/28-UAT.md)</files>
  <action>See <how-to-verify> block above. The user MUST execute Tests 1, 2, 3 in SIMUL_MODE per the steps listed there, then update each test's `**Result**:` line in 28-UAT.md to PASS / FAIL / INVALID with date and measured values, and update Summary table + frontmatter passed/failed/pending counts. This task is a blocking checkpoint ??execution does NOT proceed until the user signals via <resume-signal>.</action>
  <verify>
    <automated>powershell -Command "Select-String -Path '.planning/phases/28-fai-circlediameter-datum-circle/28-UAT.md' -Pattern '\*\*Result\*\*: pending' | Measure-Object | ForEach-Object { if (\$_.Count -eq 0) { exit 0 } else { exit 1 } }"</automated>
  </verify>
  <done>All 3 user-facing tests (Tests 1, 2, 3) carry a non-pending Result line (PASS / FAIL / INVALID) in 28-UAT.md. Test 4 already PASS-ed in Task 1. Summary table + frontmatter counts updated. Plan 04 SUMMARY can now be written.</done></task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan is documentation-only (UAT.md) + manual user verification. No code changes, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (n/a) | n/a | n/a | n/a | UAT plan — no code paths added; threats covered by Plans 01-03 dispositions. |
</threat_model>

<verification>
- 28-UAT.md exists with 4 Test scenarios
- Test 4 (msbuild) auto-PASS recorded
- Tests 1, 2, 3 user PASS recorded after checkpoint resume
- Phase 28 sign-off requires 4/4 PASS
</verification>

<success_criteria>
- 28-UAT.md created with full 4-Test scenario coverage (AC-1, AC-4, AC-5, AC-6)
- Test 4 self-PASS via msbuild
- Tests 1-3 PASS via user verification (checkpoint:human-verify)
- Frontmatter passed/pending counts accurate after each result update
</success_criteria>

<output>
After 4/4 PASS, create `.planning/phases/28-fai-circlediameter-datum-circle/28-04-SUMMARY.md` documenting:
- 4 Tests + each Result (PASS/FAIL/INVALID + date + measured values for Tests 2/3)
- Final UAT verdict (signed_off / partial / failed)
- Cross-references to 28-01/02/03 SUMMARYs
- ROADMAP.md Phase 28 row update recommendation (mark `[x]` if signed_off)
- STATE.md update recommendation (Current Position → Phase 29 / next milestone task)
</output>
