---
phase: 31-datum-algorithm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/IDatumOriginConsumer.cs
  - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - WPF_Example/DatumMeasurement.csproj
  - .planning/phases/31-datum-algorithm/31-UAT.md
autonomous: true
requirements: [D-03, D-04, D-10]
must_haves:
  truths:
    - "IDatumOriginConsumer 인터페이스가 존재하고 EdgeToLineDistanceMeasurement 가 이를 구현한다"
    - "Action_FAIMeasurement 가 meas as IDatumOriginConsumer 단일 분기로 Datum 좌표를 주입한다 (구체타입 하드코딩 제거)"
    - "VisionAlgorithmService 에 ComputeProjectionDistance / TryFitArc / TryIntersectCircleLine 정적·인스턴스 메서드가 존재한다"
    - "EdgeToLineDistance 측정이 공용 헬퍼 추출 후에도 회귀 없이 기존 결과를 산출한다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/IDatumOriginConsumer.cs"
      provides: "Datum 절대좌표 주입 인터페이스 (D-03)"
      contains: "interface IDatumOriginConsumer"
    - path: "WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs"
      provides: "공용 거리 헬퍼 + 호 피팅 + 원-직선 교점"
      contains: "ComputeProjectionDistance"
    - path: ".planning/phases/31-datum-algorithm/31-UAT.md"
      provides: "신규 측정 타입 UAT 시나리오 scaffold"
      contains: "31-UAT"
  key_links:
    - from: "Action_FAIMeasurement.cs"
      to: "IDatumOriginConsumer"
      via: "meas as IDatumOriginConsumer 분기"
      pattern: "as IDatumOriginConsumer"
    - from: "EdgeToLineDistanceMeasurement.cs"
      to: "IDatumOriginConsumer"
      via: "클래스 선언 인터페이스 구현"
      pattern: "IDatumOriginConsumer"
---

<objective>
Phase 31 의 공통 기반(foundation)을 구축한다 — Datum 절대좌표 주입 인터페이스(D-03), 거리 계산 공용 헬퍼(D-04), 호 피팅·원-직선 교점 연산을 만들고, EdgeToLineDistanceMeasurement 를 인터페이스로 소급 전환한다. 신규 측정 타입 6종이 전부 이 기반에 의존하므로 가장 먼저 실행한다.

Purpose: 신규 타입 구현 전 공통 계약(인터페이스)·검증된 계산식(헬퍼)을 확정하여 타입별 재구현·하드코딩 분기 확산을 차단한다 (D-03/D-04).
Output: IDatumOriginConsumer.cs 신규 파일, VisionAlgorithmService 3개 메서드, EdgeToLineDistanceMeasurement·Action_FAIMeasurement 일반화, 31-UAT.md scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-datum-algorithm/31-CONTEXT.md
@.planning/phases/31-datum-algorithm/31-RESEARCH.md
@.planning/phases/31-datum-algorithm/31-PATTERNS.md

<interfaces>
<!-- 신규 인터페이스 계약 — 다운스트림 plan(02/03/04)이 이 시그니처를 그대로 사용한다 -->

IDatumOriginConsumer (신규 — ReringProject.Sequence namespace):
  double DatumOriginRow { get; set; }
  double DatumOriginCol { get; set; }
  double DatumAngleRad  { get; set; }

VisionAlgorithmService 신규 메서드 (다운스트림이 호출):
  public static double ComputeProjectionDistance(
      double pointRow, double pointCol,
      double datumOriginRow, double datumOriginCol,
      double datumAngleRad, double pixelResolution, string measureAxis)
  public bool TryFitArc(
      double p1Row, double p1Col, double p2Row, double p2Col, double p3Row, double p3Col,
      out double foundRow, out double foundCol, out double foundRadius, out string error)
  public static bool TryIntersectCircleLine(
      double cRow, double cCol, double radius,
      double lRow1, double lCol1, double lRow2, double lCol2,
      double roiRow, double roiCol, out double intRow, out double intCol)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IDatumOriginConsumer 인터페이스 생성 + csproj 등록</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs (L66~80 transient 3필드 — 인터페이스 멤버 일치 확인)
    - WPF_Example/Device/IAxisController.cs (인터페이스 작성 컨벤션 analog)
    - WPF_Example/DatumMeasurement.csproj (L225 EdgeToLineDistanceMeasurement.cs Compile 항목 — 등록 위치)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/IDatumOriginConsumer.cs, WPF_Example/DatumMeasurement.csproj</files>
  <action>
신규 파일 `WPF_Example/Custom/Sequence/Inspection/IDatumOriginConsumer.cs` 를 아래 내용 그대로 생성한다 (D-03):

```csharp
//260519 hbk Phase 31 D-03 — Datum 절대좌표 주입 인터페이스
namespace ReringProject.Sequence
{
    /// <summary>
    /// Datum 절대좌표를 측정 객체에 주입하는 표준 인터페이스.
    /// Action_FAIMeasurement.EStep.Measure 에서 DatumConfig 를 찾아 주입.
    /// ParamBase INI 직렬화는 이 인터페이스 무관 (public 프로퍼티 reflection 경로).
    /// </summary>
    public interface IDatumOriginConsumer //260519 hbk Phase 31 D-03
    {
        double DatumOriginRow { get; set; }
        double DatumOriginCol { get; set; }
        double DatumAngleRad  { get; set; }
    }
}
```

`DatumMeasurement.csproj` 의 Compile ItemGroup, L225 `<Compile Include="Custom\Sequence\Inspection\Measurements\EdgeToLineDistanceMeasurement.cs" />` 바로 아래 줄에 다음을 추가한다:

```xml
<Compile Include="Custom\Sequence\Inspection\IDatumOriginConsumer.cs" />
```
  </action>
  <verify>
    <automated>findstr /C:"interface IDatumOriginConsumer" WPF_Example\Custom\Sequence\Inspection\IDatumOriginConsumer.cs &amp;&amp; findstr /C:"IDatumOriginConsumer.cs" WPF_Example\DatumMeasurement.csproj</automated>
  </verify>
  <acceptance_criteria>
    - IDatumOriginConsumer.cs 파일이 존재하고 `public interface IDatumOriginConsumer` 를 포함한다
    - 인터페이스가 `double DatumOriginRow`, `double DatumOriginCol`, `double DatumAngleRad` 세 멤버를 get/set 으로 선언한다
    - namespace 가 `ReringProject.Sequence` 다
    - DatumMeasurement.csproj 에 `IDatumOriginConsumer.cs` Compile Include 항목이 존재한다
  </acceptance_criteria>
  <done>IDatumOriginConsumer.cs 생성 + csproj 등록 완료, namespace ReringProject.Sequence, 3 double 멤버.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: VisionAlgorithmService 에 ComputeProjectionDistance / TryFitArc / TryIntersectCircleLine 추가</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs (L126~196 projection_pl 블록 — ComputeProjectionDistance 추출 원본, 그대로 이식)
    - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs (L237~317 TryFindCircle 패턴, L567~592 AngleLineLine, L597~624 IntersectLines, L629 AffineTransformPoint — 삽입 위치)
    - .planning/phases/31-datum-algorithm/31-RESEARCH.md (Pattern 2, Pattern 5 — TryIntersectCircleLine 전체 스켈레톤)
  </read_first>
  <files>WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs</files>
  <action>
`VisionAlgorithmService.cs` 에 신규 메서드 3개를 추가한다. 삽입 위치는 `IntersectLines` (L597) 뒤, `AffineTransformPoint` (L629) 앞.

(1) `ComputeProjectionDistance` — `EdgeToLineDistanceMeasurement.TryExecute` L126~196 의 `datumOriginInjected` 분기(sinT/cosT → cosT≥0 정규화 → axisR1/C1/R2/C2 계산 → ProjectionPl → signedPx → `* pixelResolution`)를 **그대로** 이 정적 메서드 본문으로 이식한다. HOperatorSet 호출은 `try { ... } catch { return 0.0; }` 로 감싼다.

```csharp
//260519 hbk Phase 31 D-04 — projection_pl 거리 공용 헬퍼 (EdgeToLineDistanceMeasurement.TryExecute L126~196 추출)
public static double ComputeProjectionDistance(
    double pointRow, double pointCol,
    double datumOriginRow, double datumOriginCol,
    double datumAngleRad,
    double pixelResolution,
    string measureAxis)
{
    // EdgeToLineDistanceMeasurement.TryExecute L126~196 datumOriginInjected 블록 그대로 이식.
    // measureAxis "X"/"Y" 분기 + cosT 정규화 + ProjectionPl + signedPx → return signedPx * pixelResolution
    // try { ... } catch { return 0.0; }
}
```

(2) `TryFitArc` — 3점 외접원 피팅. `TryFindCircle` (L237) 의 `GenContourPolygonXld → FitCircleContourXld` 패턴을 차용하되 입력은 3점(row/col HTuple) 이다. `try/catch → return false`, `finally` 에서 contour Dispose.

```csharp
//260519 hbk Phase 31 D-01 — 3점 arc 피팅 (GenContourPolygonXld → FitCircleContourXld)
public bool TryFitArc(
    double p1Row, double p1Col, double p2Row, double p2Col, double p3Row, double p3Col,
    out double foundRow, out double foundCol, out double foundRadius, out string error)
{
    foundRow = foundCol = foundRadius = 0; error = null;
    HObject contour = null;
    try
    {
        HTuple rows = new HTuple(new double[] { p1Row, p2Row, p3Row });
        HTuple cols = new HTuple(new double[] { p1Col, p2Col, p3Col });
        HOperatorSet.GenContourPolygonXld(out contour, rows, cols);
        HTuple cR, cC, rad, startPhi, endPhi, pointOrder;
        HOperatorSet.FitCircleContourXld(contour, "algebraic", -1, 0, 0, 3, 2,
            out cR, out cC, out rad, out startPhi, out endPhi, out pointOrder);
        foundRow = cR.D; foundCol = cC.D; foundRadius = rad.D;
        return true;
    }
    catch (System.Exception ex) { error = ex.Message; return false; }
    finally { if (contour != null) { try { contour.Dispose(); } catch { } } }
}
```

(3) `TryIntersectCircleLine` — 31-RESEARCH.md Pattern 5 의 스켈레톤 코드(2차 방정식 → 2해 → ROI 중심 근접 해 선택)를 **그대로** 이식한다. 순수 수학이므로 `try { ... } catch { return false; }`.
  </action>
  <verify>
    <automated>findstr /C:"ComputeProjectionDistance" /C:"TryFitArc" /C:"TryIntersectCircleLine" WPF_Example\Halcon\Algorithms\VisionAlgorithmService.cs</automated>
  </verify>
  <acceptance_criteria>
    - VisionAlgorithmService.cs 가 `public static double ComputeProjectionDistance(` 시그니처를 포함한다
    - VisionAlgorithmService.cs 가 `public bool TryFitArc(` 시그니처를 포함한다
    - VisionAlgorithmService.cs 가 `public static bool TryIntersectCircleLine(` 시그니처를 포함한다
    - 세 메서드 모두 HOperatorSet/수학 호출이 try/catch 로 감싸여 있다 (catch 블록 존재)
    - 모든 신규 라인에 `//260519 hbk Phase 31` 마커가 있다
  </acceptance_criteria>
  <done>ComputeProjectionDistance(static) + TryFitArc(instance) + TryIntersectCircleLine(static) 추가, 전부 try/catch 처리.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: EdgeToLineDistanceMeasurement 인터페이스 소급 + Action_FAIMeasurement D-03 일반화 + 31-UAT.md scaffold</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs (L19 클래스 선언, L66~80 transient 3필드, L126~196 — Task 2 추출 후 ComputeProjectionDistance 호출로 교체할 수 있으면 교체)
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (L140~245 Measure 루프, L161~185 meas as EdgeToLineDistanceMeasurement 하드코딩 블록)
    - .planning/phases/31-datum-algorithm/31-RESEARCH.md (Pattern 1 — Action_FAIMeasurement 교체 코드 전체)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs, .planning/phases/31-datum-algorithm/31-UAT.md</files>
  <action>
(A) `EdgeToLineDistanceMeasurement.cs` 클래스 선언(L19)에 `IDatumOriginConsumer` 를 추가한다:

```csharp
public class EdgeToLineDistanceMeasurement : MeasurementBase,
    System.ComponentModel.ICustomTypeDescriptor,
    IDatumOriginConsumer //260519 hbk Phase 31 D-03 — 소급 구현 (Action_FAIMeasurement 하드코딩 제거 전제조건)
```
기존 transient 3필드(`DatumOriginRow/Col/AngleRad`)가 이미 존재하므로 인터페이스 멤버는 자동 충족된다. 추가 멤버 구현 불필요.

(B) `Action_FAIMeasurement.cs` L161~185 의 `meas as EdgeToLineDistanceMeasurement` 하드코딩 블록 전체를 삭제하고, 삭제 위치에 `//260519 hbk Phase 31 D-03 removed — EdgeToLineDistanceMeasurement 하드코딩 제거` 마커 1줄을 남긴 뒤, 31-RESEARCH.md Pattern 1 의 아래 교체 코드를 삽입한다:

```csharp
//260519 hbk Phase 31 D-03 — IDatumOriginConsumer 일반화 (기존 EdgeToLineDistanceMeasurement 하드코딩 제거)
var consumer = meas as IDatumOriginConsumer;
if (consumer != null)
{
    DatumConfig dc = null;
    if (parentSeq2 != null && parentSeq2.DatumConfigs != null
        && !string.IsNullOrEmpty(meas.DatumRef))
    {
        foreach (var d in parentSeq2.DatumConfigs)
        {
            if (d != null && d.DatumName == meas.DatumRef) { dc = d; break; }
        }
    }
    if (dc != null)
    {
        consumer.DatumOriginRow = dc.DetectedOriginRow;
        consumer.DatumOriginCol = dc.DetectedOriginCol;
        consumer.DatumAngleRad  = dc.DetectedRefAngle;
    }
    else
    {
        consumer.DatumOriginRow = 0.0;
        consumer.DatumOriginCol = 0.0;
        consumer.DatumAngleRad  = 0.0;
    }
}
```
주의: 기존 블록에서 사용하던 지역변수명(`parentSeq2` 등)이 다르면 실제 소스의 변수명에 맞춘다. 기존 블록이 사용하던 DatumConfig 조회 경로를 그대로 따른다. (Pitfall 1 — 하드코딩 블록 잔존 금지: 삭제 후 grep 으로 `as EdgeToLineDistanceMeasurement` 가 Measure 루프에 0건임을 확인.)

(C) EdgeToLineDistanceMeasurement.TryExecute 의 projection_pl 블록(L126~196)을 Task 2 의 `VisionAlgorithmService.ComputeProjectionDistance` 호출로 교체할 수 있으면 교체한다 — 단 교체로 인한 회귀가 의심되면 기존 블록을 유지하고 다운스트림 신규 타입만 헬퍼를 호출한다 (D-04 의 의도는 신규 타입 재사용이 우선, EdgeToLineDistance 자체 회귀 0 이 더 중요). 교체 시 결과값이 byte-identical 인지 코드 검토로 확인한다.

(D) 신규 파일 `.planning/phases/31-datum-algorithm/31-UAT.md` 를 UAT scaffold 로 생성한다. frontmatter `status: pending`, 그리고 신규 측정 타입별 + carry-over 별 시나리오 섹션을 둔다:
- Test 1: E8 (CircleCenterDistance) — 원중심 → Datum B Y 거리, 공차 예 20.201 ±0.030
- Test 2: D1/H5 (EdgeToLineAngle) — Datum A 기준 직선 각도(deg)
- Test 3: I9/I10 (ArcLineIntersectDistance) — 호∩라인 교점 → Datum C X 거리
- Test 4: E2 (CompoundAngle) — Datum B 기준 41.36° ±1.00°
- Test 5: E9/E10 (CompoundCenterC/BDistance) — Datum C/B 거리(mm)
- Test 6: ArcEdgeDistance — G 시리즈 Datum C X 거리
- Test 7: CO-23.1-02 — 신규 타입 선택 시 Rect ROI 버튼 활성화
- Test 8: CO-23.1-01 — 듀얼 이미지 표시
- Test 9: BUILD — MSBuild Debug/x64 PASS
각 Test 는 result 칸을 pending 으로 둔다.
  </action>
  <verify>
    <automated>MSBuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /t:Rebuild</automated>
  </verify>
  <acceptance_criteria>
    - EdgeToLineDistanceMeasurement.cs 클래스 선언에 `IDatumOriginConsumer` 가 포함된다
    - Action_FAIMeasurement.cs 의 Measure 루프에 `as IDatumOriginConsumer` 분기가 존재한다
    - Action_FAIMeasurement.cs 의 Measure 루프에 `as EdgeToLineDistanceMeasurement` Datum 주입 하드코딩 블록이 0건이다
    - `.planning/phases/31-datum-algorithm/31-UAT.md` 파일이 존재하고 Test 1~9 시나리오를 포함한다
    - MSBuild Debug/x64 Rebuild 가 exit 0, 신규 error/warning 0 (Phase 21 baseline 6 warning 유지)
  </acceptance_criteria>
  <done>EdgeToLineDistance 가 IDatumOriginConsumer 구현, Action_FAIMeasurement 하드코딩 제거 + 인터페이스 단일 분기, 31-UAT.md scaffold 생성, 빌드 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INI recipe → measurement 객체 | 사용자가 편집한 INI 의 Datum 좌표/ROI 값이 측정 로직으로 유입 (로컬 파일, 신뢰 가능) |
| Halcon 이미지 → 측정 알고리즘 | SIMUL 이미지 또는 카메라 프레임이 알고리즘 입력 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | INI 의 Datum 좌표/ROI 값 변조 | accept | 로컬 데스크톱 앱, INI 는 신뢰 경계 내부 자산. 인증 표면 없음 |
| T-31-02 | Denial of Service | TryIntersectCircleLine 의 0-나눗셈/음수 판별식 | mitigate | `a < 1e-12` 가드 + `disc < 0` 가드로 NaN/예외 방지, 실패 시 return false |
| T-31-03 | Denial of Service | TryFitArc 의 Halcon FitCircleContourXld 예외 (3점 비수렴) | mitigate | try/catch → return false, finally contour Dispose (메모리 누수 차단) |
| T-31-04 | Information Disclosure | 측정 실패 시 error 문자열 노출 | accept | 로컬 운영자 UI 전용, 외부 노출 없음 |
</threat_model>

<verification>
- `MSBuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /t:Rebuild` exit 0
- IDatumOriginConsumer.cs 존재 + csproj 등록
- Action_FAIMeasurement Measure 루프에 `as EdgeToLineDistanceMeasurement` Datum 주입 하드코딩 0건
- ComputeProjectionDistance / TryFitArc / TryIntersectCircleLine 시그니처 존재
</verification>

<success_criteria>
- IDatumOriginConsumer 인터페이스 + csproj 등록 완료
- EdgeToLineDistanceMeasurement 가 인터페이스 소급 구현
- Action_FAIMeasurement 가 단일 IDatumOriginConsumer 분기로 Datum 주입
- VisionAlgorithmService 에 거리 헬퍼·호 피팅·원-직선 교점 3 메서드 추가
- 31-UAT.md scaffold 생성
- MSBuild Debug/x64 PASS, 신규 warning 0
</success_criteria>

<output>
After completion, create `.planning/phases/31-datum-algorithm/31-01-SUMMARY.md`
</output>
