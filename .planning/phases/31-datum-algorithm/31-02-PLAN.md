---
phase: 31-datum-algorithm
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CircleCenterDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineAngleMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcEdgeDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [D-01, D-04, D-05, D-06, D-08]
must_haves:
  truths:
    - "E8(CircleCenterDistance) 측정 타입이 원중심을 구해 Datum 거리를 산출한다"
    - "D1/H5(EdgeToLineAngle) 측정 타입이 ROI 1개 직선 피팅 후 Datum A 기준 각도를 산출한다"
    - "ArcEdgeDistance(G 시리즈) 측정 타입이 에지 라인 피팅 후 Datum C X 거리를 산출한다"
    - "MeasurementFactory 가 3개 신규 타입 문자열을 인스턴스로 매핑하고 GetTypeNames 에 노출한다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/CircleCenterDistanceMeasurement.cs"
      provides: "E8 — 원중심 → Datum 거리"
      contains: "CircleCenterDistanceMeasurement"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineAngleMeasurement.cs"
      provides: "D1/H5 — Datum A 기준 직선 각도"
      contains: "EdgeToLineAngleMeasurement"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/ArcEdgeDistanceMeasurement.cs"
      provides: "G 시리즈 — 호 에지 Datum 거리"
      contains: "ArcEdgeDistanceMeasurement"
  key_links:
    - from: "MeasurementFactory.cs"
      to: "CircleCenterDistanceMeasurement / EdgeToLineAngleMeasurement / ArcEdgeDistanceMeasurement"
      via: "Create switch case + GetTypeNames 배열"
      pattern: "case \"(CircleCenterDistance|EdgeToLineAngle|ArcEdgeDistance)\""
    - from: "신규 측정 타입"
      to: "VisionAlgorithmService.ComputeProjectionDistance / AngleLineLine"
      via: "TryExecute 내부 호출"
      pattern: "ComputeProjectionDistance|AngleLineLine"
---

<objective>
ROI 1개 패턴의 단순 신규 측정 타입 3종을 구현한다 — E8(CircleCenterDistance, 원중심 거리), D1/H5(EdgeToLineAngle, Datum A 기준 각도), ArcEdgeDistance(G 시리즈, 호 에지 거리). 모두 IDatumOriginConsumer 를 구현하고 Plan 01 의 공용 헬퍼를 호출한다.

Purpose: SOP 신규 측정 항목 중 단일 ROI + Datum 패턴(EdgeToLineDistance 직계 변형)을 먼저 완결한다 (D-01/D-05/D-08).
Output: 신규 측정 클래스 3개 + MeasurementFactory 3 case 등록 + csproj 3 항목.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-datum-algorithm/31-CONTEXT.md
@.planning/phases/31-datum-algorithm/31-RESEARCH.md
@.planning/phases/31-datum-algorithm/31-PATTERNS.md
@.planning/phases/31-datum-algorithm/31-01-SUMMARY.md

<interfaces>
<!-- Plan 01 이 확정한 계약 — 그대로 호출 -->

IDatumOriginConsumer (ReringProject.Sequence):
  double DatumOriginRow/Col/AngleRad { get; set; }

VisionAlgorithmService (호출 대상):
  static double ComputeProjectionDistance(pointRow, pointCol, datumOriginRow, datumOriginCol, datumAngleRad, pixelResolution, measureAxis)
  static double AngleLineLine(r1,c1,r2,c2, r3,c3,r4,c4)   // 기존 — VisionAlgorithmService.cs L567
  bool TryFindCircle(image, row, col, radius, datumTransform, sigma, threshold, polarity, out foundRow, out foundCol, out foundRadius, out error)  // 기존 L237
  bool TryFitLine(image, row, col, phi, len1, len2, datumTransform, sampleCount, trimCount, sigma, threshold, direction, polarity, out pr1, out pc1, out pr2, out pc2, out error, selection)  // 기존 L18

MeasurementBase.TryExecute 시그니처 (override 대상):
  public override bool TryExecute(HImage image, HTuple datumTransform, double pixelResolution,
      out double resultValue, out string error, out List<EdgeInspectionOverlay> overlays)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CircleCenterDistanceMeasurement (E8) 생성 + csproj 등록</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs (전체 — 원 ROI + TryFindCircle 호출 패턴, Circle_* 필드, Edge 필드, ComboBox 래퍼)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs (L59~80 — MeasureAxis 필드 + IDatumOriginConsumer transient 3필드)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (TryExecute 시그니처, 생성자, EvaluateJudgement)
    - .planning/phases/31-datum-algorithm/31-PATTERNS.md (CircleCenterDistanceMeasurement 섹션 — 전체 스켈레톤)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/CircleCenterDistanceMeasurement.cs, WPF_Example/DatumMeasurement.csproj</files>
  <action>
신규 파일 `CircleCenterDistanceMeasurement.cs` 를 생성한다. 31-PATTERNS.md "CircleCenterDistanceMeasurement" 섹션의 스켈레톤을 그대로 따른다:

- namespace `ReringProject.Sequence`, using: System.Collections.Generic / HalconDotNet / PropertyTools.DataAnnotations / ReringProject.Halcon.Algorithms / ReringProject.Halcon.Models
- 클래스 선언: `public class CircleCenterDistanceMeasurement : MeasurementBase, IDatumOriginConsumer`
- `public override string TypeName { get { return "CircleCenterDistance"; } }`
- 생성자: CircleDiameterMeasurement 와 동일한 `(ParamBase owner)` 또는 base 위임 시그니처를 그대로 복사
- ROI 필드: `[Category("Circle|ROI")] public double Circle_Row/Col/Radius { get; set; }`
- Edge 필드: `[Category("Edge")] public int EdgeThreshold = 10; public double Sigma = 1.0; [ItemsSourceProperty(nameof(EdgePolarityList))] public string EdgePolarity = "DarkToLight"; [Browsable(false)] public List<string> EdgePolarityList { get { return EdgeOptionLists.FAIPolarities; } }`
- MeasureAxis 필드: `[Category("Edge")] [ItemsSourceProperty(nameof(MeasureAxisList))] public string MeasureAxis { get; set; } = "Y";` + `[Browsable(false)] public List<string> MeasureAxisList { get { return new List<string> { "Y", "X" }; } }` — E8 은 Datum B Y 방향이므로 기본값 "Y"
- IDatumOriginConsumer transient 3필드: EdgeToLineDistanceMeasurement.cs L69~80 의 `[System.ComponentModel.Browsable(false)] [PropertyTools.DataAnnotations.Browsable(false)] [Newtonsoft.Json.JsonIgnore] public double DatumOriginRow/Col/AngleRad` 3필드를 그대로 복사
- TryExecute override:
  ```csharp
  public override bool TryExecute(HImage image, HTuple datumTransform, double pixelResolution,
      out double resultValue, out string error, out List<EdgeInspectionOverlay> overlays)
  {
      resultValue = 0; error = null;
      overlays = new List<EdgeInspectionOverlay>();
      var svc = new VisionAlgorithmService();
      double foundRow, foundCol, foundRadius;
      if (!svc.TryFindCircle(image, Circle_Row, Circle_Col, Circle_Radius,
          datumTransform, Sigma, EdgeThreshold, EdgePolarity,
          out foundRow, out foundCol, out foundRadius, out error))
      {
          return false;
      }
      resultValue = VisionAlgorithmService.ComputeProjectionDistance(
          foundRow, foundCol, DatumOriginRow, DatumOriginCol, DatumAngleRad,
          pixelResolution, MeasureAxis);
      return true;
  }
  ```
  주의: `TryFindCircle` 의 실제 시그니처를 CircleDiameterMeasurement.cs 의 호출부에서 확인하고 인자 순서를 일치시킨다.
- 모든 신규 라인에 `//260519 hbk Phase 31 D-01` 마커.

csproj Compile ItemGroup 에 `<Compile Include="Custom\Sequence\Inspection\Measurements\CircleCenterDistanceMeasurement.cs" />` 추가.
  </action>
  <verify>
    <automated>findstr /C:"class CircleCenterDistanceMeasurement" WPF_Example\Custom\Sequence\Inspection\Measurements\CircleCenterDistanceMeasurement.cs &amp;&amp; findstr /C:"CircleCenterDistanceMeasurement.cs" WPF_Example\DatumMeasurement.csproj</automated>
  </verify>
  <acceptance_criteria>
    - CircleCenterDistanceMeasurement.cs 가 존재하고 `class CircleCenterDistanceMeasurement : MeasurementBase, IDatumOriginConsumer` 를 포함한다
    - `TypeName` getter 가 `"CircleCenterDistance"` 를 반환한다
    - Circle_Row/Col/Radius, MeasureAxis(기본 "Y"), DatumOriginRow/Col/AngleRad 필드가 존재한다
    - TryExecute 가 `VisionAlgorithmService.ComputeProjectionDistance` 를 호출한다
    - csproj 에 CircleCenterDistanceMeasurement.cs Compile Include 가 존재한다
  </acceptance_criteria>
  <done>E8 측정 클래스 생성, IDatumOriginConsumer 구현, ComputeProjectionDistance 호출, csproj 등록.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: EdgeToLineAngleMeasurement (D1/H5) + ArcEdgeDistanceMeasurement (G 시리즈) 생성 + csproj 등록</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/LineToLineAngleMeasurement.cs (전체 — AngleLineLine 호출, Edge 필드 풀세트)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs (전체 — Point_* ROI 필드, TryFitLine "All" 호출, MeasureAxis, overlay 패턴, IDatumOriginConsumer 3필드)
    - .planning/phases/31-datum-algorithm/31-PATTERNS.md (EdgeToLineAngleMeasurement / ArcEdgeDistanceMeasurement 섹션 — 전체 스켈레톤)
    - .planning/phases/31-datum-algorithm/31-RESEARCH.md (Pattern 4, Pattern 7)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineAngleMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/Measurements/ArcEdgeDistanceMeasurement.cs, WPF_Example/DatumMeasurement.csproj</files>
  <action>
(A) `EdgeToLineAngleMeasurement.cs` 생성 (D1/H5, D-05). 31-PATTERNS.md "EdgeToLineAngleMeasurement" 섹션 스켈레톤을 따른다:
- 클래스 선언: `public class EdgeToLineAngleMeasurement : MeasurementBase, IDatumOriginConsumer`, `TypeName = "EdgeToLineAngle"`
- ROI 필드: EdgeToLineDistanceMeasurement `Point_*` 패턴 — `[Category("Point|ROI")] public double Point_Row/Col/Phi/Length1/Length2`
- Edge 필드: LineToLineAngleMeasurement 의 풀세트 (EdgeThreshold=10 / Sigma=1.0 / EdgeSampleCount=20 / EdgeTrimCount=10 / EdgePolarity="DarkToLight" / EdgeDirection="TtoB" + EdgeDirectionList/EdgePolarityList 래퍼)
- IDatumOriginConsumer transient 3필드 복사
- TryExecute: `svc.TryFitLine(image, Point_Row, Point_Col, Point_Phi, Point_Length1, Point_Length2, datumTransform, EdgeSampleCount, EdgeTrimCount, Sigma, EdgeThreshold, EdgeDirection, EdgePolarity, out pr1, out pc1, out pr2, out pc2, out error, "All")` → 실패 시 return false. 이후 Datum A 기준선 2점 구성:
  ```csharp
  double sinT = System.Math.Sin(DatumAngleRad);
  double cosT = System.Math.Cos(DatumAngleRad);
  double daR1 = DatumOriginRow - 200.0 * sinT;
  double daC1 = DatumOriginCol - 200.0 * cosT;
  double daR2 = DatumOriginRow + 200.0 * sinT;
  double daC2 = DatumOriginCol + 200.0 * cosT;
  resultValue = VisionAlgorithmService.AngleLineLine(pr1, pc1, pr2, pc2, daR1, daC1, daR2, daC2);
  return true;
  ```
  resultValue 는 각도(degree) — pixelResolution 미적용. `AngleLineLine` 의 실제 시그니처(인스턴스 vs static, 반환 단위 degree/rad)를 LineToLineAngleMeasurement.cs 호출부에서 확인하고 일치시킨다. 반환이 rad 이면 degree 변환을 LineToLineAngleMeasurement 와 동일하게 적용한다.
- TryFitLine 의 selection 인자는 반드시 리터럴 `"All"` (memory feedback — EdgeSelection 명시 필수).

(B) `ArcEdgeDistanceMeasurement.cs` 생성 (G 시리즈, D-08). 31-PATTERNS.md / 31-RESEARCH.md Pattern 7 — EdgeToLineDistanceMeasurement 와 알고리즘 동일:
- 클래스 선언: `public class ArcEdgeDistanceMeasurement : MeasurementBase, IDatumOriginConsumer`, `TypeName = "ArcEdgeDistance"`
- ROI 필드: `Point_*` (Category "Point|ROI"), Edge 필드 풀세트, MeasureAxis 기본값 `"X"` (Datum C X 방향)
- IDatumOriginConsumer transient 3필드 복사
- TryExecute: `svc.TryFitLine(... "All")` → 라인 피팅 결과 중점 `pRow = (pr1+pr2)/2.0; pCol = (pc1+pc2)/2.0;` 계산 → `resultValue = VisionAlgorithmService.ComputeProjectionDistance(pRow, pCol, DatumOriginRow, DatumOriginCol, DatumAngleRad, pixelResolution, MeasureAxis);`
- overlay: EdgeToLineDistanceMeasurement 와 동일한 검출 에지 라인(FAI-Edge1 녹/적) + Datum 거리선(FAI-DistLine 청록) overlay 생성 패턴을 복사한다 (EdgeToLineDistanceMeasurement.TryExecute 의 overlay 블록 참조).

두 신규 파일 모두 csproj Compile ItemGroup 에 항목 추가:
```xml
<Compile Include="Custom\Sequence\Inspection\Measurements\EdgeToLineAngleMeasurement.cs" />
<Compile Include="Custom\Sequence\Inspection\Measurements\ArcEdgeDistanceMeasurement.cs" />
```
모든 신규 라인에 `//260519 hbk Phase 31` 마커 (EdgeToLineAngle 은 D-05, ArcEdgeDistance 는 D-08).
  </action>
  <verify>
    <automated>findstr /C:"class EdgeToLineAngleMeasurement" WPF_Example\Custom\Sequence\Inspection\Measurements\EdgeToLineAngleMeasurement.cs &amp;&amp; findstr /C:"class ArcEdgeDistanceMeasurement" WPF_Example\Custom\Sequence\Inspection\Measurements\ArcEdgeDistanceMeasurement.cs &amp;&amp; findstr /C:"EdgeToLineAngleMeasurement.cs" /C:"ArcEdgeDistanceMeasurement.cs" WPF_Example\DatumMeasurement.csproj</automated>
  </verify>
  <acceptance_criteria>
    - EdgeToLineAngleMeasurement.cs 가 `class EdgeToLineAngleMeasurement : MeasurementBase, IDatumOriginConsumer` + `TypeName="EdgeToLineAngle"` 를 포함한다
    - EdgeToLineAngleMeasurement.TryExecute 가 `TryFitLine(... "All")` + `AngleLineLine` 를 호출한다
    - ArcEdgeDistanceMeasurement.cs 가 `class ArcEdgeDistanceMeasurement : MeasurementBase, IDatumOriginConsumer` + `TypeName="ArcEdgeDistance"` 를 포함하고 MeasureAxis 기본값이 "X" 다
    - ArcEdgeDistanceMeasurement.TryExecute 가 `TryFitLine(... "All")` + `ComputeProjectionDistance` 를 호출한다
    - csproj 에 두 파일 Compile Include 가 존재한다
    - 모든 TryFitLine 호출의 selection 인자가 리터럴 "All" 이다
  </acceptance_criteria>
  <done>EdgeToLineAngle + ArcEdgeDistance 측정 클래스 생성, IDatumOriginConsumer 구현, TryFitLine "All" 고정, csproj 등록.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: MeasurementFactory 에 3개 신규 타입 등록 + 빌드 검증</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs (전체 — Create switch case 패턴 L14~46, GetTypeNames 배열 L37~45)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs</files>
  <action>
`MeasurementFactory.cs` 의 `Create` switch 에 3개 case 를 추가한다 (기존 case 패턴과 동일하게 `owner` 인자 전달):

```csharp
case "CircleCenterDistance":   //260519 hbk Phase 31 D-01 E8
    return new CircleCenterDistanceMeasurement(owner);
case "EdgeToLineAngle":        //260519 hbk Phase 31 D-05
    return new EdgeToLineAngleMeasurement(owner);
case "ArcEdgeDistance":        //260519 hbk Phase 31 D-08
    return new ArcEdgeDistanceMeasurement(owner);
```

`GetTypeNames()` 가 반환하는 배열/리스트에 동일하게 `"CircleCenterDistance"`, `"EdgeToLineAngle"`, `"ArcEdgeDistance"` 3개 문자열을 append 한다 (기존 7개 원소 뒤). 각 추가 라인에 `//260519 hbk Phase 31` 마커.

주의 (Pitfall 6): switch case 와 GetTypeNames 배열 **둘 다** 추가해야 한다 — 하나만 추가하면 INI 로드 또는 PropertyGrid 드롭다운에서 누락된다. 생성자 인자(`owner`)의 실제 타입·이름은 기존 case 의 호출 형태를 그대로 따른다.
  </action>
  <verify>
    <automated>MSBuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /t:Rebuild</automated>
  </verify>
  <acceptance_criteria>
    - MeasurementFactory.cs Create switch 에 `case "CircleCenterDistance"`, `case "EdgeToLineAngle"`, `case "ArcEdgeDistance"` 가 존재한다
    - GetTypeNames 반환 컬렉션에 "CircleCenterDistance", "EdgeToLineAngle", "ArcEdgeDistance" 3개 문자열이 포함된다
    - MSBuild Debug/x64 Rebuild 가 exit 0, 신규 error/warning 0
  </acceptance_criteria>
  <done>MeasurementFactory 3 case + GetTypeNames 3 항목 등록, 빌드 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INI recipe → 신규 측정 객체 | 사용자 편집 INI 의 ROI/Edge 파라미터가 측정 로직 입력 (로컬, 신뢰 가능) |
| Halcon 이미지 → 측정 알고리즘 | SIMUL/카메라 이미지가 TryFindCircle·TryFitLine 입력 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Denial of Service | TryFindCircle/TryFitLine 의 Halcon 예외 (검출 실패) | mitigate | 기존 VisionAlgorithmService 가 try/catch → return false 보장. TryExecute 가 false 반환 시 측정 실패로 처리 |
| T-31-06 | Tampering | INI 의 MeasureAxis 문자열 변조 ("X"/"Y" 외 값) | accept | ComputeProjectionDistance 가 비"X" 입력 시 Y 분기로 폴백 — 측정 오류일 뿐 보안 영향 없음. 로컬 자산 |
| T-31-07 | Information Disclosure | 측정 error 문자열 | accept | 로컬 운영자 UI 전용 |
</threat_model>

<verification>
- `MSBuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /t:Rebuild` exit 0
- 3개 신규 측정 클래스 파일 존재 + csproj 등록
- MeasurementFactory Create switch 3 case + GetTypeNames 3 항목
</verification>

<success_criteria>
- CircleCenterDistance(E8) / EdgeToLineAngle(D1·H5) / ArcEdgeDistance(G) 측정 타입 구현 완료
- 모두 IDatumOriginConsumer 구현, 공용 헬퍼(ComputeProjectionDistance/AngleLineLine) 호출
- MeasurementFactory 등록 + csproj 등록
- MSBuild Debug/x64 PASS, 신규 warning 0
</success_criteria>

<output>
After completion, create `.planning/phases/31-datum-algorithm/31-02-SUMMARY.md`
</output>
