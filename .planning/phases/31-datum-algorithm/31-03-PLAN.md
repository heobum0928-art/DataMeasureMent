---
phase: 31-datum-algorithm
plan: 03
type: execute
wave: 3
depends_on: [31-01, 31-02]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundAngleMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterCDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterBDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [D-01, D-04, D-07, D-09, D-10, D-11]
must_haves:
  truths:
    - "I9/I10(ArcLineIntersectDistance) 측정 타입이 3점 호 피팅 + 라인 피팅 + 원-직선 교점으로 Datum 거리를 산출한다"
    - "E2(CompoundAngle) 측정 타입이 다단계 기하 체인을 거쳐 Datum B 기준 각도를 산출한다"
    - "E9(CompoundCenterCDistance)/E10(CompoundCenterBDistance) 측정 타입이 동일 기하 체인으로 Datum C/B 거리를 산출한다"
    - "MeasurementFactory 가 4개 신규 타입을 각각 등록한다 (D-11 별도 등록)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs"
      provides: "I9/I10 — 호∩라인 교점 → Datum 거리"
      contains: "ArcLineIntersectDistanceMeasurement"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundAngleMeasurement.cs"
      provides: "E2 — CompoundAngle 복합 각도"
      contains: "CompoundAngleMeasurement"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterCDistanceMeasurement.cs"
      provides: "E9 — CompoundCenter 거리 (Datum C)"
      contains: "CompoundCenterCDistanceMeasurement"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterBDistanceMeasurement.cs"
      provides: "E10 — CompoundCenter 거리 (Datum B)"
      contains: "CompoundCenterBDistanceMeasurement"
  key_links:
    - from: "ArcLineIntersectDistanceMeasurement.cs"
      to: "VisionAlgorithmService.TryFitArc / TryIntersectCircleLine"
      via: "TryExecute 내부 호출"
      pattern: "TryFitArc|TryIntersectCircleLine"
    - from: "CompoundAngle/CompoundCenter 측정"
      to: "VisionAlgorithmService.TryIntersectCircleLine"
      via: "기하 체인 midline ∩ circle"
      pattern: "TryIntersectCircleLine"
    - from: "MeasurementFactory.cs"
      to: "4개 신규 Compound/ArcLine 타입"
      via: "Create switch case + GetTypeNames"
      pattern: "case .(ArcLineIntersectDistance|CompoundAngle)"
---

<objective>
다단계 기하 구성을 요구하는 복합 신규 측정 타입 4종을 구현한다 — I9/I10(ArcLineIntersectDistance, 호∩라인 교점), E2(CompoundAngle, 복합 각도), E9/E10(CompoundCenterC/BDistance, 복합 중점 거리). 모두 IDatumOriginConsumer 를 구현하고 Plan 01 의 TryFitArc·TryIntersectCircleLine·ComputeProjectionDistance 를 호출한다.

Purpose: SOP 신규 측정 항목 중 가장 복잡한 다단계 기하 체인을 measurement 내부에 캡슐화하여 완결한다 — 사용자는 입력 ROI 만 티칭 (D-09).
Output: 신규 측정 클래스 4개 + MeasurementFactory 4 case 등록 + csproj 4 항목.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-datum-algorithm/31-CONTEXT.md
@.planning/phases/31-datum-algorithm/31-RESEARCH.md
@.planning/phases/31-datum-algorithm/31-PATTERNS.md
@.planning/phases/31-datum-algorithm/31-01-SUMMARY.md
@.planning/phases/31-datum-algorithm/31-02-SUMMARY.md

<interfaces>
<!-- Plan 01/02 가 확정한 계약 — 그대로 호출 -->

VisionAlgorithmService:
  static double ComputeProjectionDistance(pointRow, pointCol, datumOriginRow, datumOriginCol, datumAngleRad, pixelResolution, measureAxis)
  bool TryFitArc(p1Row, p1Col, p2Row, p2Col, p3Row, p3Col, out foundRow, out foundCol, out foundRadius, out error)
  static bool TryIntersectCircleLine(cRow, cCol, radius, lRow1, lCol1, lRow2, lCol2, roiRow, roiCol, out intRow, out intCol)
  static double AngleLineLine(r1,c1,r2,c2, r3,c3,r4,c4)
  bool TryFindCircle(image, row, col, radius, datumTransform, sigma, threshold, polarity, out foundRow, out foundCol, out foundRadius, out error)
  bool TryFitLine(image, row, col, phi, len1, len2, datumTransform, sampleCount, trimCount, sigma, threshold, direction, polarity, out pr1, out pc1, out pr2, out pc2, out error, selection)

IDatumOriginConsumer (ReringProject.Sequence): double DatumOriginRow/Col/AngleRad { get; set; }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ArcLineIntersectDistanceMeasurement (I9/I10) 생성 + csproj 등록</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs (TryFindCircle 호출 패턴 — 참고)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs (Point_* ROI, TryFitLine "All", MeasureAxis, IDatumOriginConsumer 3필드)
    - .planning/phases/31-datum-algorithm/31-PATTERNS.md (ArcLineIntersectDistanceMeasurement 섹션 — ROI 필드 전체 + TryExecute 단계 주석)
    - .planning/phases/31-datum-algorithm/31-RESEARCH.md (Pattern 5 — 3점 arc + 교점 + D-10 ROI 내부 해 선택)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs, WPF_Example/DatumMeasurement.csproj</files>
  <action>
신규 파일 `ArcLineIntersectDistanceMeasurement.cs` 를 생성한다 (I9/I10, D-01/D-10). 31-PATTERNS.md "ArcLineIntersectDistanceMeasurement" 섹션 스켈레톤을 따른다:

- 클래스 선언: `public class ArcLineIntersectDistanceMeasurement : MeasurementBase, IDatumOriginConsumer`, `TypeName = "ArcLineIntersectDistance"`
- ROI 필드 — 3점 arc + 1 라인:
  - `[Category("Arc|ROI")]` 아래 Arc_P1_Row/Col/Phi/Length1/Length2, Arc_P2_Row/Col/Phi/Length1/Length2, Arc_P3_Row/Col/Phi/Length1/Length2 (각 5필드 × 3점 = 15 필드)
  - `[Category("Line|ROI")]` 아래 Line_Row/Col/Phi/Length1/Length2
- Edge 필드: EdgeToLineAngleMeasurement 와 동일 풀세트 (EdgeThreshold/Sigma/EdgeSampleCount/EdgeTrimCount/EdgePolarity/EdgeDirection + List 래퍼)
- MeasureAxis 필드: 기본값 "X" (Datum C X 방향) + MeasureAxisList 래퍼
- IDatumOriginConsumer transient 3필드 복사
- TryExecute:
  1. 3점 에지 추출 — Arc_P1/P2/P3 각 ROI 에서 svc.TryFitLine(..., "All") 호출 → 라인 피팅 결과 중점 ((pr1+pr2)/2, (pc1+pc2)/2) 을 arc 포인트로 사용 (3개 점 획득)
  2. svc.TryFitArc(a1Row,a1Col, a2Row,a2Col, a3Row,a3Col, out arcRow, out arcCol, out arcRadius, out error) → 실패 시 return false
  3. svc.TryFitLine(Line_Row, Line_Col, Line_Phi, Line_Length1, Line_Length2, ..., "All") → 라인 2점 (lr1,lc1,lr2,lc2)
  4. VisionAlgorithmService.TryIntersectCircleLine(arcRow, arcCol, arcRadius, lr1, lc1, lr2, lc2, Line_Row, Line_Col, out intRow, out intCol) — D-10: Line_Row/Line_Col 이 ROI 내부 해 선택 기준점. 실패 시 error 설정 후 return false
  5. resultValue = VisionAlgorithmService.ComputeProjectionDistance(intRow, intCol, DatumOriginRow, DatumOriginCol, DatumAngleRad, pixelResolution, MeasureAxis);
  - overlays 는 빈 리스트로 둔다 (overlay 는 본 plan 범위 밖).
- 모든 TryFitLine 의 selection 인자는 리터럴 "All".
- 모든 신규 라인에 //260519 hbk Phase 31 D-01 마커.

csproj 에 `<Compile Include="Custom\Sequence\Inspection\Measurements\ArcLineIntersectDistanceMeasurement.cs" />` 추가.

주의: 본 타입 인스턴스 1개는 arc 1개 + line 1개 → 교점 1개를 측정한다. I9/I10 의 두 교점(예 Pb, Pd)은 각각 별도 FAI/Measurement 로 티칭한다 (31-RESEARCH.md Open Question 1 권장안).
  </action>
  <verify>
    <automated>MSBuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /t:Build</automated>
  </verify>
  <acceptance_criteria>
    - ArcLineIntersectDistanceMeasurement.cs 가 `class ArcLineIntersectDistanceMeasurement : MeasurementBase, IDatumOriginConsumer` 를 포함한다
    - TypeName getter 가 "ArcLineIntersectDistance" 를 반환한다
    - Arc_P1/P2/P3 각 5필드, Line_* 5필드, MeasureAxis(기본 "X"), DatumOrigin 3필드가 존재한다
    - TryExecute 가 TryFitArc, TryIntersectCircleLine, ComputeProjectionDistance 를 모두 호출한다
    - 모든 TryFitLine 호출의 selection 인자가 리터럴 "All" 이다
    - csproj 에 ArcLineIntersectDistanceMeasurement.cs Compile Include 가 존재한다
  </acceptance_criteria>
  <done>I9/I10 측정 클래스 생성, 3점 호 피팅 + 원-직선 교점 + 공용 거리 헬퍼, csproj 등록.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CompoundAngleMeasurement (E2) + CompoundCenterC/BDistanceMeasurement (E9/E10) 생성 + csproj 등록</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs (TryFindCircle 호출 패턴)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/LineToLineAngleMeasurement.cs (AngleLineLine 호출, Edge 필드 풀세트)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs (IDatumOriginConsumer 3필드, MeasureAxis)
    - .planning/phases/31-datum-algorithm/31-PATTERNS.md (CompoundAngleMeasurement / CompoundCenterCDistanceMeasurement / CompoundCenterBDistanceMeasurement 섹션)
    - .planning/phases/31-datum-algorithm/31-RESEARCH.md (Pattern 6 + Code Examples 의 TryComputeChainPoint 전체 코드)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundAngleMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterCDistanceMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterBDistanceMeasurement.cs, WPF_Example/DatumMeasurement.csproj</files>
  <action>
3개 신규 파일을 생성한다. 모두 IDatumOriginConsumer 를 구현하며 D-09(중간 산출물 내부 캡슐화) / D-11(출력 단위별 별도 타입)을 따른다.

(A) `CompoundAngleMeasurement.cs` (E2, D-11):
- 클래스 선언: `public class CompoundAngleMeasurement : MeasurementBase, IDatumOriginConsumer`, `TypeName = "CompoundAngle"`
- ROI 필드 — 3 원 + 2 라인:
  - `[Category("CL1|ROI")]` Cl1_Row/Col/Radius
  - `[Category("CL2|ROI")]` Cl2_Row/Col/Radius
  - `[Category("CL3|ROI")]` Cl3_Row/Col/Radius
  - `[Category("La|ROI")]` La_Row/Col/Phi/Length1/Length2
  - `[Category("Lb|ROI")]` Lb_Row/Col/Phi/Length1/Length2
- Edge 필드 풀세트 + IDatumOriginConsumer 3필드
- private 헬퍼 메서드 `TryComputeChainPoint(HImage image, HTuple datumTransform, out double pcRow, out double pcCol, out string error)` — 31-RESEARCH.md Code Examples 의 `TryComputeChainPoint` 전체 코드를 그대로 이식 (CL2/CL3 TryFindCircle + La/Lb TryFitLine + midline Lc 계산 + TryIntersectCircleLine ×2 → Pa,Pb → Pc = midpoint). 단 31-RESEARCH.md 코드의 메서드명이 TryComputeChainPc 로 표기된 곳도 동일 메서드를 가리킨다.
- TryExecute:
  1. CL1 TryFindCircle → cl1Center(Pd)
  2. TryComputeChainPoint → pcRow, pcCol
  3. E2 전용: line Ld = (Pd → Pc) — 즉 2점 (cl1Row,cl1Col) 과 (pcRow,pcCol)
  4. Datum B 기준선 2점 구성 (DatumOriginRow/Col 중심 ±200px, EdgeToLineAngleMeasurement 의 daR1/daC1/daR2/daC2 패턴 동일)
  5. resultValue = VisionAlgorithmService.AngleLineLine(cl1Row, cl1Col, pcRow, pcCol, daR1, daC1, daR2, daC2);  — 각도(degree), pixelResolution 미적용

(B) `CompoundCenterCDistanceMeasurement.cs` (E9, D-11):
- 클래스 선언: `public class CompoundCenterCDistanceMeasurement : MeasurementBase, IDatumOriginConsumer`, `TypeName = "CompoundCenterCDistance"`
- ROI 필드: CompoundAngleMeasurement 와 동일하되 **CL1 생략** — CL2/CL3/La/Lb 만 (Category 동일)
- Edge 필드 풀세트 + IDatumOriginConsumer 3필드
- MeasureAxis 필드: 기본값 "X" (Datum C X 방향, D-07)
- private `TryComputeChainPoint` 헬퍼 — CompoundAngleMeasurement 와 동일 코드 복사 (D-09: 각 타입이 자기 헬퍼 보유, ROI 공유 없음)
- TryExecute: TryComputeChainPoint → pcRow,pcCol → `resultValue = VisionAlgorithmService.ComputeProjectionDistance(pcRow, pcCol, DatumOriginRow, DatumOriginCol, DatumAngleRad, pixelResolution, MeasureAxis);`

(C) `CompoundCenterBDistanceMeasurement.cs` (E10, D-11):
- (B) 와 구조 완전 동일. 차이는 `TypeName = "CompoundCenterBDistance"` 와 MeasureAxis 기본값 `"Y"` (Datum B Y 방향, D-07) 두 곳뿐.

모든 TryFitLine 호출의 selection 인자는 리터럴 "All". 모든 신규 라인에 //260519 hbk Phase 31 D-11 (또는 chain 헬퍼는 D-09) 마커.

csproj Compile ItemGroup 에 3 항목 추가:
```xml
<Compile Include="Custom\Sequence\Inspection\Measurements\CompoundAngleMeasurement.cs" />
<Compile Include="Custom\Sequence\Inspection\Measurements\CompoundCenterCDistanceMeasurement.cs" />
<Compile Include="Custom\Sequence\Inspection\Measurements\CompoundCenterBDistanceMeasurement.cs" />
```
  </action>
  <verify>
    <automated>MSBuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /t:Build</automated>
  </verify>
  <acceptance_criteria>
    - 3개 파일 모두 존재하고 각각 `MeasurementBase, IDatumOriginConsumer` 를 구현한다
    - TypeName 이 각각 "CompoundAngle", "CompoundCenterCDistance", "CompoundCenterBDistance" 다
    - CompoundAngleMeasurement 는 CL1/CL2/CL3 + La/Lb ROI 필드를, CompoundCenterC/B 는 CL2/CL3 + La/Lb ROI 필드를 가진다 (CL1 없음)
    - 3개 모두 private TryComputeChainPoint 헬퍼를 포함한다
    - CompoundAngle.TryExecute 는 AngleLineLine 을, CompoundCenterC/B.TryExecute 는 ComputeProjectionDistance 를 호출한다
    - CompoundCenterCDistance MeasureAxis 기본값 "X", CompoundCenterBDistance MeasureAxis 기본값 "Y"
    - csproj 에 3 파일 Compile Include 가 존재한다
  </acceptance_criteria>
  <done>E2/E9/E10 측정 클래스 3개 생성, 기하 체인 내부 캡슐화(D-09), 출력별 별도 타입(D-11), csproj 등록.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: MeasurementFactory 에 4개 신규 타입 등록 + 빌드 검증</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs (전체 — Plan 02 가 추가한 case 3개 포함 현재 상태, Create switch + GetTypeNames)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs</files>
  <action>
`MeasurementFactory.cs` 의 Create switch 에 4개 case 를 추가한다 (Plan 02 가 추가한 3 case 뒤, 기존 case 패턴과 동일하게 `owner` 인자 전달):

```csharp
case "ArcLineIntersectDistance": //260519 hbk Phase 31 D-01
    return new ArcLineIntersectDistanceMeasurement(owner);
case "CompoundAngle":          //260519 hbk Phase 31 D-11
    return new CompoundAngleMeasurement(owner);
case "CompoundCenterCDistance": //260519 hbk Phase 31 D-11
    return new CompoundCenterCDistanceMeasurement(owner);
case "CompoundCenterBDistance": //260519 hbk Phase 31 D-11
    return new CompoundCenterBDistanceMeasurement(owner);
```

`GetTypeNames()` 반환 컬렉션에 동일하게 "ArcLineIntersectDistance", "CompoundAngle", "CompoundCenterCDistance", "CompoundCenterBDistance" 4개 문자열을 append 한다. 각 추가 라인에 //260519 hbk Phase 31 마커.

주의 (Pitfall 6): switch case 와 GetTypeNames 배열 둘 다 추가. 빌드 후 MeasurementFactory 에 Phase 31 전체 7개 신규 타입(CircleCenterDistance/EdgeToLineAngle/ArcEdgeDistance/ArcLineIntersectDistance/CompoundAngle/CompoundCenterCDistance/CompoundCenterBDistance)이 등록되었는지 확인.
  </action>
  <verify>
    <automated>MSBuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /t:Rebuild</automated>
  </verify>
  <acceptance_criteria>
    - MeasurementFactory.cs Create switch 에 ArcLineIntersectDistance, CompoundAngle, CompoundCenterCDistance, CompoundCenterBDistance 4 case 가 존재한다
    - GetTypeNames 반환 컬렉션에 4개 신규 문자열이 포함된다
    - Phase 31 전체 7개 신규 타입이 Create switch + GetTypeNames 양쪽에 등록되어 있다
    - MSBuild Debug/x64 Rebuild 가 exit 0, 신규 error/warning 0
  </acceptance_criteria>
  <done>MeasurementFactory 4 case + GetTypeNames 4 항목 등록, Phase 31 신규 타입 7종 전체 dispatch 가능, 빌드 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INI recipe → 복합 측정 객체 | 사용자 편집 INI 의 다수 ROI 좌표가 기하 체인 입력 (로컬, 신뢰 가능) |
| Halcon 이미지 → 호 피팅/교점 연산 | SIMUL/카메라 이미지가 TryFitArc·TryFitLine 입력 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-08 | Denial of Service | TryComputeChainPoint 의 다단계 연산 중 하나라도 실패 시 미정의 동작 | mitigate | 각 단계 TryFindCircle/TryFitLine/TryIntersectCircleLine 실패 시 즉시 return false + error 전파. 중간 실패가 측정 실패로 안전 종결 |
| T-31-09 | Denial of Service | TryFitArc 3점 비수렴 (I9/I10) | mitigate | Plan 01 TryFitArc 가 try/catch → return false. TryExecute 가 false 시 측정 실패 처리 |
| T-31-10 | Tampering | INI 의 잘못된 ROI 좌표 → 교점 0개 (disc<0) | mitigate | TryIntersectCircleLine 이 disc<0 시 return false — 잘못된 입력이 측정 실패로 귀결, NaN 전파 없음 |
| T-31-11 | Information Disclosure | 측정 error 문자열 | accept | 로컬 운영자 UI 전용 |
</threat_model>

<verification>
- `MSBuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /t:Rebuild` exit 0
- 4개 신규 측정 클래스 파일 존재 + csproj 등록
- MeasurementFactory Create switch + GetTypeNames 에 Phase 31 신규 타입 7종 전체 등록
</verification>

<success_criteria>
- ArcLineIntersectDistance(I9/I10) / CompoundAngle(E2) / CompoundCenterCDistance(E9) / CompoundCenterBDistance(E10) 측정 타입 구현 완료
- 모두 IDatumOriginConsumer 구현, Plan 01 헬퍼(TryFitArc/TryIntersectCircleLine/ComputeProjectionDistance) 호출
- 기하 체인 measurement 내부 캡슐화 (D-09), 출력 단위별 별도 타입 (D-11)
- MeasurementFactory 등록 + csproj 등록
- MSBuild Debug/x64 PASS, 신규 warning 0
</success_criteria>

<output>
After completion, create `.planning/phases/31-datum-algorithm/31-03-SUMMARY.md`
</output>
