---
phase: 31-datum-algorithm
plan: 04
type: execute
wave: 4
depends_on: [31-02, 31-03]
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/UI/ContentItem/MainView.xaml
autonomous: true
requirements: [CO-23.1-01, CO-23.1-02]
must_haves:
  truths:
    - "신규 측정 타입(EdgeToLineAngle/ArcEdgeDistance/ArcLineIntersect/Compound* 등) 선택 시 Rect ROI 버튼이 활성화되어 Point ROI 를 티칭할 수 있다"
    - "CircleCenterDistance 선택 시 Circle ROI 버튼이 활성화되어 Circle ROI 를 티칭할 수 있다"
    - "Datum 티칭 이미지(TeachingImagePath)와 검사 이미지(InspectionImagePath)가 UI 에서 개별 확인 가능하다"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "ROI 버튼 측정 타입 일반화 + 듀얼 이미지 표시 코드비하인드"
      contains: "FindSelectedRectMeasurement"
  key_links:
    - from: "RectRoiButton_Click"
      to: "신규 측정 타입 (Point_* 보유)"
      via: "FindSelectedRectMeasurement 타입 화이트리스트"
      pattern: "FindSelectedRectMeasurement"
    - from: "CommitRectRoi"
      to: "선택된 측정 객체 Point_* 필드"
      via: "타입별 Point_Row/Col/Phi/Length1/Length2 기록"
      pattern: "Point_Row"
---

<objective>
Phase 23.1 carry-over 2건을 흡수한다 — CO-23.1-02(측정 타입별 Rect/Circle ROI 버튼 활성화 일반화)와 CO-23.1-01(듀얼 이미지 뷰어). 신규 측정 타입 7종이 전부 ROI 티칭 가능해지도록 MainView 의 ROI 버튼 분기를 일반화한다.

Purpose: 신규 측정 타입의 ROI 를 사용자가 드로잉 티칭할 수 있게 하고(CO-23.1-02 — 알고리즘 구현의 선행 UI 조건), 티칭/검사 이미지 분리를 UI 에서 확인 가능하게 한다(CO-23.1-01).
Output: MainView.xaml.cs ROI 버튼 일반화 + 듀얼 이미지 표시, MainView.xaml 패널 추가.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-datum-algorithm/31-CONTEXT.md
@.planning/phases/31-datum-algorithm/31-RESEARCH.md
@.planning/phases/31-datum-algorithm/31-PATTERNS.md
@.planning/phases/31-datum-algorithm/31-02-SUMMARY.md
@.planning/phases/31-datum-algorithm/31-03-SUMMARY.md

<interfaces>
<!-- MainView.xaml.cs 현재 상태 (Phase 23.1) — 일반화 대상 -->

MainView.xaml.cs:
  L51:  private EdgeToLineDistanceMeasurement _editingMeasurement;        // → 타입 일반화 필요
  L53:  private string _editingMeasurementFaiName;
  L1187: private void RectRoiButton_Click(object sender, RoutedEventArgs e)
  L1194: EdgeToLineDistanceMeasurement measTarget = FindSelectedEdgeToLineMeasurement();   // → 일반화
  L1239: private void CommitRectRoi()                                     // L1254~1258 Point_* 기록
  L1362: private CircleDiameterMeasurement FindSelectedCircleMeasurement() // → CircleCenterDistance 추가
  L1377: private EdgeToLineDistanceMeasurement FindSelectedEdgeToLineMeasurement()  // → 일반화
  L1398: private void CommitCircleRoi(double centerRow, double centerCol, double radius)

신규 측정 타입 (Plan 02/03 생성):
  Point_* ROI 보유: EdgeToLineAngleMeasurement, ArcEdgeDistanceMeasurement
  Circle_* ROI 보유: CircleCenterDistanceMeasurement
  복합 ROI 보유 (Arc_*/Line_*/CL*_/La_/Lb_): ArcLineIntersectDistanceMeasurement, CompoundAngleMeasurement, CompoundCenterCDistanceMeasurement, CompoundCenterBDistanceMeasurement
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CO-23.1-02 — Rect/Circle ROI 버튼 측정 타입 일반화</name>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (L40~60 필드 선언, L1160~1300 RectRoiButton_Click + CommitRectRoi, L1300~1410 FindSelectedCircleMeasurement + FindSelectedEdgeToLineMeasurement + CommitCircleRoi — 전체 ROI 버튼 영역)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineAngleMeasurement.cs (Point_* 필드명 확인 — Plan 02 산출물)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcEdgeDistanceMeasurement.cs (Point_* 필드명 확인)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CircleCenterDistanceMeasurement.cs (Circle_* 필드명 확인)
    - .planning/phases/31-datum-algorithm/31-PATTERNS.md (MainView.xaml.cs 섹션 — CO-23.1-02 일반화 방향, Option A 타입 화이트리스트)
  </read_first>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <action>
31-RESEARCH.md Pattern 8 의 **Option A (타입 화이트리스트, 최소 변경)** 를 적용한다.

(A) `_editingMeasurement` 필드(L51) 타입을 `EdgeToLineDistanceMeasurement` 에서 `MeasurementBase` 로 변경한다 — Point_* 필드 보유 타입 전체를 담을 수 있게. 마커 //260519 hbk Phase 31 CO-23.1-02.

(B) `FindSelectedEdgeToLineMeasurement()` (L1377) 를 `FindSelectedRectMeasurement()` 로 일반화한다 — 반환 타입 `MeasurementBase`. 선택된 SelectedParam / 트리 fallback 을 검사하되, `as` 캐스트 대상을 Point_* ROI 보유 타입 화이트리스트로 확장:
  - EdgeToLineDistanceMeasurement (기존)
  - EdgeToLineAngleMeasurement
  - ArcEdgeDistanceMeasurement
  - ArcLineIntersectDistanceMeasurement
  - CompoundAngleMeasurement
  - CompoundCenterCDistanceMeasurement
  - CompoundCenterBDistanceMeasurement
  각각 `as` 로 순차 검사 후 non-null 이면 반환. 기존 L1377 메서드의 트리 탐색 로직(SelectedParam → 트리 노드 fallback)을 그대로 유지하되 타입 검사 부분만 화이트리스트로 교체.

(C) `RectRoiButton_Click` (L1187, L1194) — `FindSelectedEdgeToLineMeasurement()` 호출을 `FindSelectedRectMeasurement()` 로 교체. 버튼 활성/비활성 게이팅 로직은 동일하게 유지.

(D) `CommitRectRoi` (L1239, L1254~1258) — `_editingMeasurement` 가 `MeasurementBase` 타입이므로 Point_* 필드 직접 접근이 불가능하다. 화이트리스트 타입별로 `as` 캐스트 후 Point_Row/Col/Phi/Length1/Length2 를 설정하는 분기를 작성한다. 각 타입의 Point_* 필드명이 동일하므로(Plan 02/03 가 EdgeToLineDistance Point_* 패턴 복사) 패턴은 반복적이다. 예:
  ```csharp
  //260519 hbk Phase 31 CO-23.1-02 — 측정 타입별 Point ROI 기록 일반화
  var etld = _editingMeasurement as EdgeToLineDistanceMeasurement;
  if (etld != null) { etld.Point_Row = mCenterRow; etld.Point_Col = mCenterCol; etld.Point_Phi = 0.0; etld.Point_Length1 = mHalfHeight; etld.Point_Length2 = mHalfWidth; }
  var etla = _editingMeasurement as EdgeToLineAngleMeasurement;
  if (etla != null) { etla.Point_Row = mCenterRow; ... }
  // ... ArcEdgeDistanceMeasurement 동일
  ```
  ArcLineIntersect/Compound* 는 Point_* 가 아닌 복합 ROI(Arc_P1_* / Line_* / CL*_)를 가지므로 본 task 의 단순 Point ROI 커밋 대상이 아니다 — 이들은 화이트리스트에서 빼거나, 첫 ROI 슬롯만 커밋하는 별도 처리가 필요하면 31-PATTERNS.md 의 다점 ROI 패턴(Phase 23.1 GetCurrentFAIRois)을 따른다. **본 task 범위:** Point_* 단일 ROI 보유 타입(EdgeToLineDistance/EdgeToLineAngle/ArcEdgeDistance) 만 RectRoiButton 으로 커밋. 복합 ROI 타입의 다점 티칭은 기존 Phase 23.1 다점 운용 구조를 재사용하되, 본 plan 에서 무리하게 확장하지 않고 EdgeToLineAngle/ArcEdgeDistance 활성화를 우선 보장한다.

(E) `FindSelectedCircleMeasurement()` (L1362) — 반환 타입을 `MeasurementBase` 로 변경하고 `CircleDiameterMeasurement` 외 `CircleCenterDistanceMeasurement` 도 `as` 검사에 추가. `CommitCircleRoi` (L1398) 에서 두 타입을 `as` 분기하여 Circle_Row/Col/Radius 를 설정한다 (Point ROI 와 동일 패턴).

모든 변경/신규 라인에 //260519 hbk Phase 31 CO-23.1-02 마커 (기존 마커 위 stacking).
  </action>
  <verify>
    <automated>MSBuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /t:Build</automated>
  </verify>
  <acceptance_criteria>
    - MainView.xaml.cs 에 `FindSelectedRectMeasurement` 메서드가 존재하고 반환 타입이 MeasurementBase 다
    - FindSelectedRectMeasurement 가 EdgeToLineDistanceMeasurement, EdgeToLineAngleMeasurement, ArcEdgeDistanceMeasurement 를 as 캐스트로 검사한다
    - RectRoiButton_Click 이 FindSelectedRectMeasurement 를 호출한다
    - CommitRectRoi 가 EdgeToLineAngleMeasurement / ArcEdgeDistanceMeasurement 의 Point_* 필드를 설정하는 분기를 포함한다
    - FindSelectedCircleMeasurement 가 CircleCenterDistanceMeasurement 를 as 캐스트로 검사한다
    - MSBuild Debug/x64 Build 가 exit 0, 신규 error/warning 0
  </acceptance_criteria>
  <done>Rect ROI 버튼이 Point_* 보유 신규 타입을, Circle ROI 버튼이 CircleCenterDistance 를 활성화. 빌드 PASS.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: CO-23.1-01 듀얼 이미지 뷰어 UI 형태 결정</name>
  <decision>CO-23.1-01 듀얼 이미지 뷰어를 어떤 UI 형태로 구현할지 결정한다.</decision>
  <context>
TeachingImagePath(DatumConfig — Datum 티칭 기준 이미지)와 InspectionImagePath(ShotConfig.SimulImagePath — 검사 실행 이미지)가 Phase 22 에서 코드 레벨 분리되었으나, MainView 는 단일 HalconViewer 만 사용한다. 두 경로가 다를 때 사용자가 어느 이미지를 보고 있는지 알 수 없다. 구현 형태는 31-CONTEXT.md Claude's Discretion 으로 위임되었다.
  </context>
  <options>
    <option id="option-a">
      <name>경로 레이블 표시 (최소 변경)</name>
      <pros>MainView.xaml 변경 최소, HalconViewer 영역 그대로 유지, 현재 표시 중인 이미지 출처(Teaching/Inspection)를 텍스트 레이블로 명시</pros>
      <cons>두 이미지를 동시에 보지는 못함 — 출처 식별만 가능</cons>
    </option>
    <option id="option-b">
      <name>별도 썸네일/Image 패널 추가</name>
      <pros>Datum 티칭 이미지를 작은 보조 패널에 항상 표시, 검사 이미지와 동시 비교 가능</pros>
      <cons>MainView.xaml 레이아웃 변경, 화면 공간 점유</cons>
    </option>
    <option id="option-c">
      <name>듀얼 HalconViewer (분할 뷰)</name>
      <pros>두 이미지를 동일 크기로 나란히 표시 — 완전한 비교</pros>
      <cons>레이아웃 대폭 변경, 기존 뷰어 영역 축소, 구현 비용 최대</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: CO-23.1-01 듀얼 이미지 뷰어 구현 + 빌드 검증</name>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml (HalconViewer 영역 레이아웃)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (이미지 로드/표시 경로 — LoadAndDisplay, TeachingImagePath/SimulImagePath 사용처)
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (GrabOrLoadDatumImage — TeachingImagePath 사용 패턴)
  </read_first>
  <files>WPF_Example/UI/ContentItem/MainView.xaml, WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <action>
Task 2 에서 사용자가 선택한 옵션(option-a / option-b / option-c)에 따라 듀얼 이미지 뷰어를 구현한다.

- **option-a 선택 시:** MainView.xaml 에 TextBlock 레이블을 추가하고, MainView.xaml.cs 의 이미지 표시 경로에서 현재 표시 중인 이미지 출처가 TeachingImagePath 인지 InspectionImagePath(SimulImagePath) 인지 판별하여 레이블 텍스트("티칭 이미지: {경로}" / "검사 이미지: {경로}")를 갱신한다.
- **option-b 선택 시:** MainView.xaml 에 보조 `Image` 컨트롤 + 레이블 패널을 추가하고, Datum 노드 선택/티칭 모드 시 DatumConfig.TeachingImagePath 경로의 이미지를 BitmapImage 로 로드하여 표시한다. 기존 HalconViewer 영역은 검사 이미지 유지.
- **option-c 선택 시:** MainView.xaml 레이아웃을 Grid 분할로 변경하여 HalconViewer 2개(또는 HalconViewer + Image)를 나란히 배치, 좌측=Teaching / 우측=Inspection.

공통:
- TeachingImagePath 가 빈 문자열이면 해당 패널/레이블은 "(티칭 이미지 없음)" 표시 — 회귀 0 (Phase 22 빈 문자열 폴백 호환).
- 모든 신규/변경 라인에 //260519 hbk Phase 31 CO-23.1-01 마커.
- C# 7.2 준수 (switch expression / nullable refs 금지).
  </action>
  <verify>
    <automated>MSBuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /t:Rebuild</automated>
  </verify>
  <acceptance_criteria>
    - MainView.xaml 또는 MainView.xaml.cs 에 TeachingImagePath 와 InspectionImagePath 를 구분 표시하는 UI 요소가 추가되었다
    - TeachingImagePath 가 빈 문자열일 때 폴백 처리(레이블/패널 빈 상태)가 존재한다
    - 신규 라인에 //260519 hbk Phase 31 CO-23.1-01 마커가 있다
    - C# 7.2 위반 구문(switch expression, nullable ref, record) 없음
    - MSBuild Debug/x64 Rebuild 가 exit 0, 신규 error/warning 0
  </acceptance_criteria>
  <done>듀얼 이미지 뷰어(선택 옵션 형태) 구현 완료, TeachingImagePath/InspectionImagePath 개별 확인 가능, 빌드 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INI/레시피 경로 → 이미지 로더 | TeachingImagePath/SimulImagePath 문자열이 파일 로드 경로로 사용 (로컬, 신뢰 가능) |
| 사용자 UI 입력 → ROI 커밋 | 마우스 드로잉 좌표가 측정 객체 ROI 필드로 기록 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-12 | Denial of Service | TeachingImagePath 가 존재하지 않는 파일 경로 | mitigate | 이미지 로드 전 File.Exists 가드 + 실패 시 빈 상태 폴백 (Phase 22/23.1 패턴 재사용) |
| T-31-13 | Tampering | INI 경로 문자열 변조 → 경로 탐색 | accept | 로컬 데스크톱 앱, 운영자 본인이 INI 소유. 경로 탐색 공격 표면 없음 |
| T-31-14 | Information Disclosure | UI 레이블에 전체 파일 경로 노출 | accept | 로컬 운영자 화면 전용, 민감 정보 아님 |
</threat_model>

<verification>
- `MSBuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /t:Rebuild` exit 0
- FindSelectedRectMeasurement 일반화 + 신규 타입 화이트리스트
- 듀얼 이미지 표시 UI 요소 추가
</verification>

<success_criteria>
- CO-23.1-02: Rect ROI 버튼이 Point_* 보유 신규 타입을, Circle ROI 버튼이 CircleCenterDistance 를 활성화
- CO-23.1-01: TeachingImagePath/InspectionImagePath UI 개별 확인 가능
- TeachingImagePath 빈 문자열 폴백 (회귀 0)
- MSBuild Debug/x64 PASS, 신규 warning 0
</success_criteria>

<output>
After completion, create `.planning/phases/31-datum-algorithm/31-04-SUMMARY.md`
</output>
