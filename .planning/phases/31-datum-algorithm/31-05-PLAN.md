---
phase: 31-datum-algorithm
plan: 05
type: execute
wave: 5
depends_on: [31-01, 31-02, 31-03, 31-04]
files_modified:
  - .planning/phases/31-datum-algorithm/31-UAT.md
autonomous: false
requirements: [D-01, D-05, D-07, D-08, CO-23.1-01, CO-23.1-02]
must_haves:
  truths:
    - "신규 측정 타입 7종이 SIMUL_MODE 에서 ROI 티칭 → 측정 실행 → 공차 판정까지 오류 없이 완주한다"
    - "각 측정 타입의 측정값이 SOP 공차 예시 범위 안에서 합리적으로 산출된다"
    - "CO-23.1-01 듀얼 이미지 표시 / CO-23.1-02 ROI 버튼 활성화가 육안 확인된다"
    - "31-UAT.md 전 시나리오가 PASS 로 사인오프된다"
  artifacts:
    - path: ".planning/phases/31-datum-algorithm/31-UAT.md"
      provides: "Phase 31 UAT 결과 + 사인오프"
      contains: "signed_off"
  key_links:
    - from: "31-UAT.md"
      to: "Phase 31 신규 측정 타입 7종 + carry-over 2건"
      via: "시나리오별 PASS/FAIL 기록"
      pattern: "PASS"
---

<objective>
Phase 31 의 신규 측정 타입 7종(E8/D1·H5/I9·I10/E2/E9/E10/ArcEdge)과 carry-over 2건(CO-23.1-01/02)을 SIMUL_MODE 에서 실측 검증하고 31-UAT.md 를 사인오프한다.

Purpose: 측정 알고리즘의 정확성·UI 동작은 Halcon 이미지 처리 결과이므로 자동화 불가 — 사용자 SIMUL UAT 로만 검증된다 (31-VALIDATION.md Manual-Only).
Output: 31-UAT.md 전 시나리오 PASS + status: signed_off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-datum-algorithm/31-CONTEXT.md
@.planning/phases/31-datum-algorithm/31-SOP-REFERENCE.md
@.planning/phases/31-datum-algorithm/31-VALIDATION.md
@.planning/phases/31-datum-algorithm/31-UAT.md
@.planning/phases/31-datum-algorithm/31-01-SUMMARY.md
@.planning/phases/31-datum-algorithm/31-02-SUMMARY.md
@.planning/phases/31-datum-algorithm/31-03-SUMMARY.md
@.planning/phases/31-datum-algorithm/31-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 최종 빌드 검증 + 31-UAT.md 시나리오 확정</name>
  <read_first>
    - .planning/phases/31-datum-algorithm/31-UAT.md (Plan 01 이 생성한 scaffold — Test 1~9)
    - .planning/phases/31-datum-algorithm/31-SOP-REFERENCE.md (각 측정 타입 SOP 공차 예시 — E8 20.201±0.030 / E2 41.36°±1.00° / I9 5.053+0.050 / G2 22.162±0.030 등)
    - .planning/phases/31-datum-algorithm/31-VALIDATION.md (Manual-Only Verifications 표)
  </read_first>
  <files>.planning/phases/31-datum-algorithm/31-UAT.md</files>
  <action>
(A) 최종 통합 빌드를 실행한다: `MSBuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /t:Rebuild` — exit 0, Phase 21 baseline(6 warning) 초과 신규 warning 0 확인.

(B) 31-UAT.md 의 각 Test 시나리오를 실행 가능한 구체 절차로 확정한다. Plan 01 의 scaffold 를 다음 내용으로 채운다:
- 각 Test 에 SIMUL_MODE 실행 단계: 신규 측정 타입을 INI/UI 에서 FAI Measurement 로 추가 → 측정 타입 선택(PropertyGrid Type 드롭다운에 신규 타입 노출 확인) → ROI 티칭(Rect/Circle 버튼) → 측정 실행 → 측정값(mm/deg) + OK/NG 판정 strip 색상 확인.
- 각 Test 에 SOP 공차 예시를 기대값 기준으로 명시: E8 ≈20.201±0.030 / EdgeToLineAngle(D1) ≈90.0°±1.5° / I9 ≈5.053 / E2 ≈41.36°±1.00° / ArcEdge(G2) ≈22.162±0.030.
- Test 7 (CO-23.1-02): 각 신규 타입 선택 시 Rect/Circle ROI 버튼 enable 상태 확인.
- Test 8 (CO-23.1-01): TeachingImagePath ≠ InspectionImagePath 설정 후 두 이미지 개별 표시 확인.
- 각 Test result 칸은 pending 유지 (Task 2 사용자 UAT 에서 채움).
  </action>
  <verify>
    <automated>MSBuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /t:Rebuild</automated>
  </verify>
  <acceptance_criteria>
    - MSBuild Debug/x64 Rebuild 가 exit 0, 신규 error/warning 0
    - 31-UAT.md 의 Test 1~9 가 각각 실행 절차 + SOP 공차 기대값을 포함한다
    - MeasurementFactory GetTypeNames 가 Phase 31 신규 타입 7종을 노출함이 31-UAT.md 절차에 반영됨
  </acceptance_criteria>
  <done>최종 빌드 PASS, 31-UAT.md 시나리오 9건 실행 절차 확정.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SIMUL_MODE UAT 실행 + 31-UAT.md 사인오프</name>
  <what-built>
Phase 31 신규 측정 타입 7종(CircleCenterDistance/EdgeToLineAngle/ArcLineIntersectDistance/CompoundAngle/CompoundCenterCDistance/CompoundCenterBDistance/ArcEdgeDistance)과 carry-over 2건(듀얼 이미지 뷰어, ROI 버튼 일반화)을 구현하고 빌드 검증을 완료했다. 측정값 정확성·UI 동작은 SIMUL 이미지로만 검증 가능하다.
  </what-built>
  <how-to-verify>
1. DatumMeasurement.exe 를 Debug/x64 SIMUL_MODE 로 실행한다.
2. 31-UAT.md Test 1~6 — 각 신규 측정 타입별로:
   a. SIMUL 이미지 로드 (Bottom Fixture #2 / Side Fixture #3 / Top Fixture #2 — 사용자 준비)
   b. Datum 티칭 → DetectedOrigin 확보
   c. FAI 에 신규 Measurement 추가 → PropertyGrid Type 드롭다운에서 신규 타입 선택 (7종 모두 노출 확인)
   d. ROI 티칭 (Rect/Circle 버튼) → 측정 실행
   e. 측정값(mm/deg)이 SOP 공차 예시 범위 안에서 합리적으로 산출되고 OK/NG strip 색상이 표시되는지 확인
3. 31-UAT.md Test 7 (CO-23.1-02) — 신규 타입 선택 시 Rect/Circle ROI 버튼이 활성화되는지 확인.
4. 31-UAT.md Test 8 (CO-23.1-01) — TeachingImagePath 와 InspectionImagePath 를 다른 파일로 설정 후 두 이미지가 UI 에서 개별 확인 가능한지 검증.
5. 각 Test 결과를 31-UAT.md 에 PASS/FAIL 로 기록. 전부 PASS 면 frontmatter status: signed_off 로 갱신.
6. FAIL 시 결함 내용을 기록 — gap closure plan(`/gsd-plan-phase 31 --gaps`)으로 후속 처리.
  </how-to-verify>
  <resume-signal>전 시나리오 PASS 면 "approved" / 결함이 있으면 FAIL 항목과 증상을 기술</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SIMUL 이미지 → 측정 알고리즘 | 사용자 준비 Fixture 이미지가 전 측정 타입 검증 입력 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-15 | Denial of Service | UAT 중 측정 실패가 시퀀스 스레드 크래시로 전파 | mitigate | 전 신규 TryExecute 가 try/catch → return false 보장 (Plan 01~03). 측정 실패는 NG/오류 판정으로 안전 종결 |
| T-31-16 | Repudiation | UAT 결과 사인오프 기록 누락 | accept | 31-UAT.md frontmatter status + 시나리오별 PASS/FAIL 명시로 추적성 확보 |
</threat_model>

<verification>
- `MSBuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /t:Rebuild` exit 0
- 31-UAT.md 전 시나리오 PASS + status: signed_off
</verification>

<success_criteria>
- 최종 통합 빌드 PASS, 신규 warning 0
- 신규 측정 타입 7종 SIMUL UAT 검증 완료
- CO-23.1-01/02 육안 검증 완료
- 31-UAT.md 사인오프 (전 시나리오 PASS)
</success_criteria>

<output>
After completion, create `.planning/phases/31-datum-algorithm/31-05-SUMMARY.md`
</output>
