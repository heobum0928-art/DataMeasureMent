---
phase: 32-sop-i9-i10-e2-e9-e10-e3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "VisionAlgorithmService 가 Rect ROI 1개에서 최대 면적 사각형(LargestRect)의 중심/각도/장단축 길이를 산출하는 메서드를 제공한다"
    - "VisionAlgorithmService 가 두 직선의 교점을 산출하는 public TryIntersectLines 메서드를 제공한다 (평행/근접 시 false)"
    - "VisionAlgorithmService 가 단축 방향 선과 사각형 XLD의 교점 2개를 산출하는 TryIntersectContours 메서드를 제공한다 (0교점 시 false)"
    - "사각형 0개 검출 시 메서드가 예외 throw 없이 error 세팅 후 false 를 반환한다"
  artifacts:
    - path: "WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs"
      provides: "공통 컨투어 알고리즘 + intersection_lines + intersection_contours_xld 래퍼"
      contains: "TryFindLargestContourRect"
  key_links:
    - from: "TryFindLargestContourRect"
      to: "HOperatorSet.EdgesSubPix / UnionAdjacentContoursXld / SmallestRectangle2Xld"
      via: "canny → union → shape_trans → area_center → select_obj 파이프라인"
      pattern: "UnionAdjacentContoursXld"
---

<objective>
VisionAlgorithmService 에 Phase 32 측정 알고리즘 재작성이 의존하는 공통 HALCON 빌딩블록 3개를 신설한다.

목적: E2/E3/E9/E10 의 공통 컨투어 알고리즘(LargestRect 산출)과 ArcLineIntersect 의 2직선 교점 계산은 측정 클래스가 직접 HALCON 을 호출하지 않고 서비스 메서드를 통해 수행해야 한다. 본 plan 이 그 메서드를 먼저 만들어 Wave 2 의 측정 클래스 재작성이 계약(시그니처)에 맞춰 구현되도록 한다.
출력: VisionAlgorithmService.cs 에 public 메서드 3개 추가 (TryFindLargestContourRect, TryIntersectLines, TryIntersectContours).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-CONTEXT.md
@.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md

<interfaces>
<!-- VisionAlgorithmService.cs 기존 메서드 시그니처 — 신규 메서드는 이 패턴을 따른다. 추가 코드베이스 탐색 불필요. -->

기존 TryFindCircle (L249) — datumTransform 적용 + GenCircle→ReduceDomain→EdgesSubPix("canny",...) + try/catch/finally Dispose 패턴:
```csharp
public bool TryFindCircle(
    HImage image, double centerRow, double centerCol, double radius,
    HTuple datumTransform, double sigma, int threshold, string polarity,
    out double foundRow, out double foundCol, out double foundRadius, out string error)
```

기존 static IntersectLines (L615) — 평행/근접 가드 패턴 (isOverlapping.I==1, IsInfinity, IsNaN → false):
```csharp
public static bool IntersectLines(
    double row1a, double col1a, double row1b, double col1b,
    double row2a, double col2a, double row2b, double col2b,
    out double intRow, out double intCol)
```

기존 TryFitArc (L736) — GenContourPolygonXld + try/catch/finally Dispose 골격.

datumTransform 적용 패턴 (TryFitLine L45~60 / TryFindCircle L271~282):
```csharp
double cRow = roiRow, cCol = roiCol, cPhi = roiPhi;
if (datumTransform != null && datumTransform.Length > 0)
{
    try
    {
        HTuple tRow, tCol;
        HOperatorSet.AffineTransPoint2d(datumTransform, roiRow, roiCol, out tRow, out tCol);
        cRow = tRow.D; cCol = tCol.D;
        double rotAngle = System.Math.Atan2(-datumTransform[1].D, datumTransform[0].D);
        cPhi = roiPhi + rotAngle;
    }
    catch { }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TryFindLargestContourRect — 공통 컨투어 알고리즘 (LargestRect 산출)</name>
  <read_first>
    - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs (현재 상태 — 특히 TryFindCircle L249~329 의 ReduceDomain/EdgesSubPix/finally Dispose 패턴, TryFitLine L41~60 의 datumTransform 적용)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-CONTEXT.md (확정 스펙 2번 — 사용자 제공 HALCON 스크립트 9줄)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md (VisionAlgorithmService 섹션 (1) — 메서드 시그니처 골격)
  </read_first>
  <files>WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs</files>
  <action>
VisionAlgorithmService 클래스에 public 인스턴스 메서드 `TryFindLargestContourRect` 를 추가한다 (TryFitArc 다음, L770 부근에 배치). 모든 변경 라인에 `//260521 hbk Phase 32` 코멘트를 부착하고, 파일의 Allman 브레이스 스타일을 따른다.

시그니처 (정확히 이대로):
```csharp
public bool TryFindLargestContourRect(
    HImage image,
    double roiRow, double roiCol, double roiPhi, double roiLength1, double roiLength2,
    HTuple datumTransform,
    double cannyAlpha, int cannyLow, int cannyHigh, double unionDistance,
    out double centerRow, out double centerCol, out double phi,
    out double length1, out double length2, out string error)
```

본문 절차 (CONTEXT.md 확정 스펙 2번의 사용자 HALCON 스크립트를 C# HOperatorSet 으로 이식):
1. out 파라미터 전부 0 초기화, error = null. `image == null` 이면 error = "image is null" 후 false.
2. HObject 변수 선언: rect, imageReduced, edges, unionContours, rectXld, largestRect (전부 null 초기화).
3. try 블록 시작.
4. datumTransform 적용 — <interfaces> 의 datumTransform 패턴 그대로 (roiRow/roiCol/roiPhi → cRow/cCol/cPhi).
5. `HOperatorSet.GenRectangle2(out rect, cRow, cCol, cPhi, roiLength1, roiLength2)` — Rect ROI 영역 생성.
6. `HOperatorSet.ReduceDomain(image, rect, out imageReduced)`.
7. `HOperatorSet.EdgesSubPix(imageReduced, out edges, "canny", cannyAlpha, cannyLow, cannyHigh)` — 사용자 스크립트 `edges_sub_pix(imageReduced, edges, 'canny', 1, 20, 40)` 의 alpha/low/high 를 파라미터로 받음.
8. `HOperatorSet.UnionAdjacentContoursXld(edges, out unionContours, unionDistance, 1, "attr_keep")` — 사용자 스크립트 `union_adjacent_contours_xld(edges, unionContours, 700, 1, 'attr_keep')`.
9. `HOperatorSet.ShapeTransXld(unionContours, out rectXld, "rectangle2")` — 각 컨투어를 최소외접사각형 XLD 로 변환.
10. `HTuple area, rowC, colC, ptOrder; HOperatorSet.AreaCenterXld(rectXld, out area, out rowC, out colC, out ptOrder);`.
11. 검출 0개 안전 종결 (CONTEXT.md 미해결#4): `if (area.Length == 0) { error = "no contour rectangle detected"; return false; }`.
12. `HTuple maxArea, maxIdx; HOperatorSet.TupleMax(area, out maxArea); HOperatorSet.TupleFind(area, maxArea, out maxIdx);` — 최대 면적 인덱스.
13. `HOperatorSet.SelectObj(rectXld, out largestRect, maxIdx + 1);` — HALCON select_obj 는 1-based 이므로 maxIdx 가 HTuple 일 때 `maxIdx.I + 1` 정수 인덱스 사용 (tuple_find 가 복수 반환 시 첫 원소: `maxIdx[0].I + 1`).
14. `HTuple cRowT, cColT, phiT, len1T, len2T; HOperatorSet.SmallestRectangle2Xld(largestRect, out cRowT, out cColT, out phiT, out len1T, out len2T);`.
15. out 파라미터에 대입: centerRow = cRowT.D, centerCol = cColT.D, phi = phiT.D, length1 = len1T.D, length2 = len2T.D. `return true`.
16. `catch (Exception ex) { error = ex.Message; return false; }` — CONTEXT.md 미해결#4 안전 종결.
17. `finally` 블록 — rect/imageReduced/edges/unionContours/rectXld/largestRect 각각 `if (x != null) { try { x.Dispose(); } catch { } }` (TryFindCircle L323~328 패턴).

주의: HALCON 연산자는 전부 try 내부에서 호출 (CLAUDE.md 컨벤션 — 예외 throw 금지, false 반환). C# 7.2 — switch expression / nullable ref / record 사용 금지.
  </action>
  <verify>
    <automated>grep -n "public bool TryFindLargestContourRect" WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "TryFindLargestContourRect" VisionAlgorithmService.cs` 가 시그니처 1건 매치
    - `grep "UnionAdjacentContoursXld" VisionAlgorithmService.cs` 가 1건 이상 매치
    - `grep "no contour rectangle detected" VisionAlgorithmService.cs` 가 1건 매치 (0개 검출 안전 종결)
    - `grep "ShapeTransXld.*rectangle2" VisionAlgorithmService.cs` 가 1건 매치
    - 메서드 본문에 try/catch(Exception)/finally 3-블록 모두 존재
  </acceptance_criteria>
  <done>TryFindLargestContourRect 가 시그니처대로 추가되고, canny→union→shape_trans→area_center→tuple_max/find→select_obj→smallest_rectangle2 파이프라인을 수행하며, 0개 검출과 예외 모두 false 반환으로 안전 종결한다.</done>
</task>

<task type="auto">
  <name>Task 2: TryIntersectLines + TryIntersectContours 래퍼 추가</name>
  <read_first>
    - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs (Task 1 적용 후 상태 — 특히 static IntersectLines L615~642, TryFitArc L736~768)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-CONTEXT.md (미해결#3 평행/근접 안전 종결)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md (VisionAlgorithmService 섹션 (2)/(3))
  </read_first>
  <files>WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs</files>
  <action>
VisionAlgorithmService 에 메서드 2개를 추가한다. 모든 변경 라인에 `//260521 hbk Phase 32` 코멘트, Allman 스타일.

(A) `TryIntersectLines` — ArcLineIntersect 의 2직선 교점용. 기존 static `IntersectLines` (L615) 가 이미 `intersection_lines` 와 동등한 `IntersectionLl` + 평행/근접 가드를 갖추고 있으므로, 동일 시그니처의 public static 래퍼를 신설하여 호출 이름을 명확히 한다 (CONTEXT.md 미해결#3):
```csharp
//260521 hbk Phase 32 — 두 직선 교점 (ArcLineIntersect 호출용). 평행/근접/중첩 시 false (측정값 '—').
public static bool TryIntersectLines(
    double row1a, double col1a, double row1b, double col1b,
    double row2a, double col2a, double row2b, double col2b,
    out double intRow, out double intCol)
{
    return IntersectLines(row1a, col1a, row1b, col1b,
                          row2a, col2a, row2b, col2b,
                          out intRow, out intCol);
}
```
(IntersectLines 본문은 수정하지 않는다 — 이미 isOverlapping.I==1 / IsInfinity / IsNaN 가드 존재. 단순 위임으로 호출 의미를 명확히 함.)

(B) `TryIntersectContours` — E3 단축 거리용. 단축 방향 선분 2점과 사각형 XLD 컨투어의 교점 2개를 산출한다:
```csharp
//260521 hbk Phase 32 — 단축선 ↔ 사각형 XLD 교점 2점 (E3 단축 거리). 0교점/1교점 시 false (안전 종결).
public bool TryIntersectContours(
    HObject rectContour,
    double lineRow1, double lineCol1, double lineRow2, double lineCol2,
    out double iRow1, out double iCol1, out double iRow2, out double iCol2,
    out string error)
```
본문:
1. out 4개 0 초기화, error = null.
2. HObject lineContour = null, intersectionPoints = null 선언.
3. try 블록:
   - `HOperatorSet.GenContourPolygonXld(out lineContour, new HTuple(lineRow1, lineRow2), new HTuple(lineCol1, lineCol2))` — 단축선을 XLD 컨투어로 (TryFitArc L750 의 GenContourPolygonXld 패턴).
   - `HTuple iR, iC; HOperatorSet.IntersectionContoursXld(rectContour, lineContour, "mutual", out iR, out iC);` — 두 컨투어 교점 (HALCON intersection_contours_xld).
   - `if (iR.Length < 2) { error = "short-axis line intersects rectangle at " + iR.Length + " point(s)"; return false; }` — CONTEXT.md 미해결#3 안전 종결.
   - iRow1 = iR[0].D, iCol1 = iC[0].D, iRow2 = iR[1].D, iCol2 = iC[1].D. `return true`.
4. `catch (Exception ex) { error = ex.Message; return false; }`.
5. `finally` — lineContour, intersectionPoints `if (x != null) { try { x.Dispose(); } catch { } }`.

주의: rectContour 는 호출측(E3 측정 클래스 — Plan 05)이 소유/Dispose 하므로 본 메서드에서 Dispose 하지 않는다. HALCON 연산자 try 내부 호출, C# 7.2.
  </action>
  <verify>
    <automated>grep -n "public static bool TryIntersectLines\|public bool TryIntersectContours" WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "public static bool TryIntersectLines" VisionAlgorithmService.cs` 가 1건 매치
    - `grep "public bool TryIntersectContours" VisionAlgorithmService.cs` 가 1건 매치
    - `grep "IntersectionContoursXld" VisionAlgorithmService.cs` 가 1건 매치
    - TryIntersectLines 본문이 기존 IntersectLines 로 위임 (IntersectLines 본문 무수정 — `grep -c "IntersectionLl" VisionAlgorithmService.cs` 가 Task 전 대비 증가 없음)
    - TryIntersectContours 가 `iR.Length < 2` 가드를 포함
  </acceptance_criteria>
  <done>TryIntersectLines (평행/근접 false) 와 TryIntersectContours (0/1교점 false) 가 추가되어 Wave 2 의 ArcLineIntersect 와 E3 측정 클래스가 안전한 교점 계산을 호출할 수 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| camera/recipe → measurement service | 카메라 이미지·레시피 ROI 좌표가 HALCON 알고리즘으로 진입 — 모두 로컬 신뢰 소스, 외부 네트워크 입력 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Denial of Service | TryFindLargestContourRect / TryIntersectContours | mitigate | 모든 HOperatorSet 호출을 try/catch 로 감싸 예외 시 false 반환 — HALCON 예외가 검사 스레드를 크래시시키지 않음 (CLAUDE.md 컨벤션) |
| T-32-02 | Denial of Service | HObject 메모리 누수 | mitigate | finally 블록에서 모든 HObject Dispose — 측정 반복 시 네이티브 메모리 누수 방지 |

본 plan 은 로컬 산업용 데스크톱 앱의 측정 알고리즘 서비스만 다룬다. 외부 신뢰 불가 입력 없음. high-severity 위협 없음.
</threat_model>

<verification>
- msbuild Debug/x64 빌드 시 신규 error 0 (앱 실행 중이면 종료 후 빌드 — MSB3027 exe 잠금 회피)
- VisionAlgorithmService.cs 에 신규 public 메서드 3개 존재
- 기존 메서드(TryFitLine/TryFindCircle/IntersectLines/TryFitArc) 무수정 — 기존 측정 회귀 0
</verification>

<success_criteria>
- TryFindLargestContourRect / TryIntersectLines / TryIntersectContours 3개 메서드 추가
- 각 메서드가 실패(0개 검출/평행/예외) 시 예외 throw 없이 false + error 반환
- 모든 HObject 가 finally 에서 Dispose
- 빌드 PASS, 신규 warning 0
</success_criteria>

<output>
완료 후 `.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-01-SUMMARY.md` 생성.
</output>
