---
phase: 32-sop-i9-i10-e2-e9-e10-e3
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/IDatumOriginConsumer.cs
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineAngleMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CircleCenterDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcEdgeDistanceMeasurement.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "IDatumOriginConsumer 인터페이스가 DatumDetectedCircleRow/Col 2 프로퍼티를 노출한다"
    - "DatumConfig 가 CircleTwoHorizontal 검출 원중심을 담는 transient 필드(검출 원중심)를 제공한다"
    - "Action_FAIMeasurement 가 측정 객체에 datum 검출 원중심을 주입한다 (미주입 시 0)"
    - "기존 IDatumOriginConsumer 구현 클래스 전부가 신규 2 프로퍼티를 구현하여 빌드가 통과한다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/IDatumOriginConsumer.cs"
      provides: "datum 검출 원중심 주입 인터페이스 계약"
      contains: "DatumDetectedCircleRow"
  key_links:
    - from: "Action_FAIMeasurement EStep.Measure 주입 블록"
      to: "IDatumOriginConsumer.DatumDetectedCircleRow"
      via: "consumer.DatumDetectedCircleRow = dc.DetectedCircleRow"
      pattern: "DatumDetectedCircleRow"
---

<objective>
DatumC(CircleTwoHorizontal) 티칭/검출 시 산출되는 B1 홀 원중심을 측정 객체로 전달하는 주입 채널을 신설한다.

목적: Plan 04 의 E2 CompoundAngle 재작성은 "LargestRect 중심 ↔ DatumC 검출 원중심" 대각선이 필요하다. 이 원중심은 측정 클래스가 직접 계산할 수 없고 Datum 검출 결과에서 주입받아야 한다. 본 plan 이 IDatumOriginConsumer 를 확장하여 그 채널을 만들고, 인터페이스 확장으로 영향받는 전 구현 클래스를 함께 갱신한다.
출력: IDatumOriginConsumer 2 프로퍼티 추가, DatumConfig 2 transient 필드 + DatumFindingService write-back, Action_FAIMeasurement 주입 블록 확장, 기존 측정 클래스 4종에 미사용 구현 추가.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-CONTEXT.md
@.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md

<interfaces>
<!-- 인터페이스 확장 영향 범위. grep 으로 확인된 IDatumOriginConsumer 구현 클래스 전체 목록. -->

IDatumOriginConsumer 구현 클래스 (grep "IDatumOriginConsumer" 결과 — 8개):
- EdgeToLineDistanceMeasurement.cs        ← 본 plan 에서 신규 2 프로퍼티 추가 (미사용)
- EdgeToLineAngleMeasurement.cs           ← 본 plan 에서 신규 2 프로퍼티 추가 (미사용)
- CircleCenterDistanceMeasurement.cs      ← 본 plan 에서 신규 2 프로퍼티 추가 (미사용)
- ArcEdgeDistanceMeasurement.cs           ← 본 plan 에서 신규 2 프로퍼티 추가 (미사용)
- ArcLineIntersectDistanceMeasurement.cs  ← Plan 03 재작성 시 동시 추가 (본 plan 범위 밖)
- CompoundAngleMeasurement.cs             ← Plan 04 재작성 시 동시 추가 (E2 = 유일 실사용)
- CompoundCenterCDistanceMeasurement.cs   ← Plan 04 재작성 시 동시 추가 (미사용)
- CompoundCenterBDistanceMeasurement.cs   ← Plan 04 재작성 시 동시 추가 (미사용)

현 IDatumOriginConsumer.cs 전문:
```csharp
public interface IDatumOriginConsumer
{
    double DatumOriginRow { get; set; }
    double DatumOriginCol { get; set; }
    double DatumAngleRad  { get; set; }
    double DatumAngle2Rad { get; set; }
}
```

DatumConfig.cs 기존 DetectedRefAngle2 transient 필드 패턴 (L466~471):
```csharp
//260519 hbk Phase 31 hotfix#3 — datum 2차(수직) 기준선 각도(rad).
[System.ComponentModel.Browsable(false)]
[PropertyTools.DataAnnotations.Browsable(false)]
[Newtonsoft.Json.JsonIgnore]
public double DetectedRefAngle2 { get; set; }
```
DatumConfig 는 이미 CircleCenter_Row / CircleCenter_Col 필드를 보유한다 (L412/L414, [Browsable(false)] visual transient). DatumFindingService.TryFindCircleTwoHorizontal 가 L328~329 에서 `config.CircleCenter_Row = centerRow; config.CircleCenter_Col = centerCol;` 로 검출 원중심을 이미 write-back 한다.

Action_FAIMeasurement.cs IDatumOriginConsumer 주입 블록 (L160~184):
```csharp
var consumer = meas as IDatumOriginConsumer;
if (consumer != null)
{
    if (dc != null) {
        consumer.DatumOriginRow = dc.DetectedOriginRow;
        consumer.DatumOriginCol = dc.DetectedOriginCol;
        consumer.DatumAngleRad  = dc.DetectedRefAngle;
        consumer.DatumAngle2Rad = dc.DetectedRefAngle2;
    }
    else {
        consumer.DatumOriginRow = 0.0;
        consumer.DatumOriginCol = 0.0;
        consumer.DatumAngleRad  = 0.0;
        consumer.DatumAngle2Rad = 0.0;
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IDatumOriginConsumer 인터페이스에 검출 원중심 2 프로퍼티 추가</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/IDatumOriginConsumer.cs (현재 전문 — 4 프로퍼티)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md (DatumC 검출 원중심 주입 채널 섹션 (b))
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/IDatumOriginConsumer.cs</files>
  <action>
IDatumOriginConsumer 인터페이스에 프로퍼티 2개를 추가한다 (DatumAngle2Rad 다음 줄). 변경 라인에 `//260521 hbk Phase 32` 코멘트:
```csharp
//260521 hbk Phase 32 — DatumC(CircleTwoHorizontal) 검출 원(B1 홀) 중심. E2 CompoundAngle 만 사용, 타 타입은 0 주입.
double DatumDetectedCircleRow { get; set; }
double DatumDetectedCircleCol { get; set; }
```
인터페이스 멤버 선언만 추가 — 다른 코드 변경 없음.
  </action>
  <verify>
    <automated>grep -c "DatumDetectedCircle" WPF_Example/Custom/Sequence/Inspection/IDatumOriginConsumer.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "double DatumDetectedCircleRow { get; set; }" IDatumOriginConsumer.cs` 가 1건 매치
    - `grep "double DatumDetectedCircleCol { get; set; }" IDatumOriginConsumer.cs` 가 1건 매치
  </acceptance_criteria>
  <done>인터페이스에 DatumDetectedCircleRow/Col 2 프로퍼티가 추가됨.</done>
</task>

<task type="auto">
  <name>Task 2: DatumConfig 검출 원중심 transient 필드 + DatumFindingService write-back</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (DetectedRefAngle2 L466~471 패턴, CircleCenter_Row/Col L412~414)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (TryFindCircleTwoHorizontal L190~345 — centerRow/centerCol 변수, L314~329 write-back 블록)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md (DatumC 검출 원중심 주입 채널 섹션 (a))
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs, WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <action>
(A) DatumConfig.cs — DetectedRefAngle2 (L471) 다음에 transient 필드 2개를 추가한다. DetectedRefAngle2 와 동일한 3중 attribute (System.ComponentModel.Browsable(false) + PropertyTools.DataAnnotations.Browsable(false) + Newtonsoft.Json.JsonIgnore). 변경 라인에 `//260521 hbk Phase 32` 코멘트:
```csharp
//260521 hbk Phase 32 — CircleTwoHorizontal 검출 원(B1 홀) 중심. E2 CompoundAngle 주입용 (DatumOriginConsumer 채널).
[System.ComponentModel.Browsable(false)]
[PropertyTools.DataAnnotations.Browsable(false)]
[Newtonsoft.Json.JsonIgnore]
public double DetectedCircleRow { get; set; }
[System.ComponentModel.Browsable(false)]
[PropertyTools.DataAnnotations.Browsable(false)]
[Newtonsoft.Json.JsonIgnore]
public double DetectedCircleCol { get; set; }
```

(B) DatumFindingService.cs — `TryFindCircleTwoHorizontal` 의 write-back 블록 (L314~323, `config.DetectedOriginRow = ...` 부근)에 2줄을 추가한다. 이 메서드는 이미 검출 원중심을 `centerRow`/`centerCol` 지역변수로 보유하며 L328~329 에서 `config.CircleCenter_Row = centerRow; config.CircleCenter_Col = centerCol;` 로 사용 중이다. 동일 변수를 신규 transient 필드에도 기록:
```csharp
config.DetectedCircleRow = centerRow; //260521 hbk Phase 32 — E2 CompoundAngle 주입용 원중심
config.DetectedCircleCol = centerCol; //260521 hbk Phase 32
```
DetectedOriginRow/Col write-back 블록(L315~317) 바로 다음, DetectedRefAngle2 대입 근처에 배치한다.

주의: DatumFindingService 의 다른 datum 알고리즘 분기(TwoLineIntersect L159 부근, VerticalTwoHorizontal L498 부근)는 원 검출이 없으므로 DetectedCircleRow/Col 을 write 하지 않는다 — 기본값 0 유지가 정상 (E2 외 타입은 이 값을 안 쓰고, E2 는 DatumC=CircleTwoHorizontal 전제).
  </action>
  <verify>
    <automated>grep -n "DetectedCircleRow\|DetectedCircleCol" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs WPF_Example/Halcon/Algorithms/DatumFindingService.cs</automated>
  </verify>
  <acceptance_criteria>
    - DatumConfig.cs 에 `public double DetectedCircleRow { get; set; }` + `public double DetectedCircleCol { get; set; }` 각 1건
    - DatumConfig.cs 의 두 필드 위에 `[Newtonsoft.Json.JsonIgnore]` attribute 존재
    - DatumFindingService.cs 의 TryFindCircleTwoHorizontal 내부에 `config.DetectedCircleRow = centerRow` + `config.DetectedCircleCol = centerCol` 각 1건
  </acceptance_criteria>
  <done>DatumConfig 가 검출 원중심 transient 필드를 가지며, CircleTwoHorizontal datum 검출이 그 필드를 write-back 한다. INI/PropertyGrid 영향 없음 (3중 attribute).</done>
</task>

<task type="auto">
  <name>Task 3: Action_FAIMeasurement 주입 블록 확장 + 기존 측정 클래스 4종 인터페이스 구현</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (IDatumOriginConsumer 주입 블록 L160~184)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcEdgeDistanceMeasurement.cs (DatumAngle2Rad transient 필드 L65~68 — 3중 attribute 패턴)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs / EdgeToLineAngleMeasurement.cs / CircleCenterDistanceMeasurement.cs (각 파일의 DatumAngle2Rad 필드 위치 — 이 아래에 신규 2필드 추가)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md (DatumC 검출 원중심 주입 채널 섹션 (c) + IDatumOriginConsumer transient 필드 3중 attribute 패턴)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineAngleMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/Measurements/CircleCenterDistanceMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/Measurements/ArcEdgeDistanceMeasurement.cs</files>
  <action>
(A) Action_FAIMeasurement.cs — IDatumOriginConsumer 주입 블록 (L160~184)에 양쪽 분기 모두 2줄씩 추가. dc != null 분기 (L177 `consumer.DatumAngle2Rad = dc.DetectedRefAngle2;` 다음):
```csharp
consumer.DatumDetectedCircleRow = dc.DetectedCircleRow; //260521 hbk Phase 32 — E2 CompoundAngle 원중심 주입
consumer.DatumDetectedCircleCol = dc.DetectedCircleCol; //260521 hbk Phase 32
```
else 분기 (L184 `consumer.DatumAngle2Rad = 0.0;` 다음):
```csharp
consumer.DatumDetectedCircleRow = 0.0; //260521 hbk Phase 32
consumer.DatumDetectedCircleCol = 0.0; //260521 hbk Phase 32
```

(B) 측정 클래스 4종 — EdgeToLineDistanceMeasurement / EdgeToLineAngleMeasurement / CircleCenterDistanceMeasurement / ArcEdgeDistanceMeasurement 각각에 IDatumOriginConsumer 신규 2 프로퍼티를 구현한다. 각 파일에서 기존 `DatumAngle2Rad` transient 필드를 찾아 그 바로 아래에 동일한 3중 attribute 를 붙인 2 프로퍼티를 추가한다 (ArcEdgeDistanceMeasurement.cs L65~68 패턴):
```csharp
//260521 hbk Phase 32 — IDatumOriginConsumer 확장. 본 타입은 검출 원중심 미사용 (E2 만 사용) — 주입만 받고 미참조.
[System.ComponentModel.Browsable(false)] //260521 hbk Phase 32
[PropertyTools.DataAnnotations.Browsable(false)] //260521 hbk Phase 32
[Newtonsoft.Json.JsonIgnore] //260521 hbk Phase 32
public double DatumDetectedCircleRow { get; set; } //260521 hbk Phase 32
[System.ComponentModel.Browsable(false)] //260521 hbk Phase 32
[PropertyTools.DataAnnotations.Browsable(false)] //260521 hbk Phase 32
[Newtonsoft.Json.JsonIgnore] //260521 hbk Phase 32
public double DatumDetectedCircleCol { get; set; } //260521 hbk Phase 32
```
TryExecute 본문은 수정하지 않는다 (이 4종은 검출 원중심을 사용하지 않음 — 인터페이스 계약만 만족시켜 빌드 통과).

주의: ArcLineIntersectDistanceMeasurement / CompoundAngleMeasurement / CompoundCenterCDistanceMeasurement / CompoundCenterBDistanceMeasurement 4종은 Plan 03/04 에서 전면 재작성되므로 본 plan 에서 건드리지 않는다. 본 task 는 재작성되지 않는 4종만 처리한다 (인터페이스 확장으로 CS0535 미구현 오류가 나는 클래스 = 8종 전부지만, 재작성 4종은 Plan 03/04 가 신규 작성 시 함께 구현).
  </action>
  <verify>
    <automated>grep -c "DatumDetectedCircleRow" WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineAngleMeasurement.cs WPF_Example/Custom/Sequence/Inspection/Measurements/CircleCenterDistanceMeasurement.cs WPF_Example/Custom/Sequence/Inspection/Measurements/ArcEdgeDistanceMeasurement.cs WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</automated>
  </verify>
  <acceptance_criteria>
    - 측정 클래스 4종 각 파일에 `public double DatumDetectedCircleRow { get; set; }` 1건 + `DatumDetectedCircleCol` 1건
    - 각 신규 프로퍼티 위에 `[Newtonsoft.Json.JsonIgnore]` 존재
    - Action_FAIMeasurement.cs 에 `consumer.DatumDetectedCircleRow` 가 2건 (dc!=null 분기 + else 분기)
    - Action_FAIMeasurement.cs 에 `dc.DetectedCircleRow` 가 1건
  </acceptance_criteria>
  <done>주입 블록이 검출 원중심을 양 분기에서 전달하고, 재작성되지 않는 측정 클래스 4종이 신규 인터페이스 멤버를 구현한다. (재작성 4종은 Plan 03/04 가 신규 작성 시 구현 — 본 plan 완료 시점에는 그 4종이 아직 CS0535 일 수 있으나 Wave 2 완료 시 해소.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datum 검출 → 측정 객체 | Datum 검출 결과(원중심 좌표)가 측정 객체로 주입 — 모두 로컬 산출, 외부 입력 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03 | Tampering | DetectedCircleRow/Col INI 직렬화 | accept | 3중 attribute (Newtonsoft.Json.JsonIgnore + Browsable false ×2) 로 JSON 직렬화/PropertyGrid 노출 제외 — transient 값. ParamBase reflection 이 double 을 INI 에 쓰더라도 매 검출마다 덮어쓰이므로 무해 (DetectedRefAngle2 와 동일 패턴, 기존 검증됨) |

본 plan 은 인터페이스 확장 + 주입 배선만 다룬다. 외부 신뢰 불가 입력 없음. high-severity 위협 없음.
</threat_model>

<verification>
- IDatumOriginConsumer 2 프로퍼티 추가
- DatumConfig 2 transient 필드 + DatumFindingService write-back
- Action_FAIMeasurement 주입 2분기 각 2줄
- 재작성되지 않는 측정 클래스 4종 인터페이스 구현
- 빌드는 Wave 2 (Plan 03/04 재작성 4종 구현 완료) 후 PASS — 본 plan 단독 빌드는 CompoundAngle 등 미재작성 4종이 CS0535 일 수 있음 (Wave 2 에서 해소, 정상)
</verification>

<success_criteria>
- datum 검출 원중심 주입 채널 완성 (인터페이스 → DatumConfig → DatumFindingService write-back → Action 주입)
- 기존 측정 클래스 4종(EdgeToLineDistance/EdgeToLineAngle/CircleCenterDistance/ArcEdgeDistance)이 신규 멤버 구현 — 회귀 0 (미사용)
</success_criteria>

<output>
완료 후 `.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-02-SUMMARY.md` 생성.
</output>
