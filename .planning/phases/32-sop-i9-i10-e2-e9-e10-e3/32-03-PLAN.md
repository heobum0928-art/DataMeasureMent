---
phase: 32-sop-i9-i10-e2-e9-e10-e3
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "ArcLineIntersect 측정이 Rect ROI 2개(EdgeA 수직, EdgeB 수평)에서 각각 직선을 피팅한다"
    - "두 직선의 교점을 intersection_lines 래퍼로 산출하고, 교점에서 Datum 기준선까지 거리(mm)를 측정한다"
    - "직선이 평행/근접일 때 측정이 예외 없이 false 를 반환한다 (측정값 '—')"
    - "기존 Arc_P1~P3 / Line_* ROI 필드가 제거되고 EdgeA_*/EdgeB_* 5필드 2벌로 교체된다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs"
      provides: "I9/I10 — 2직선 교점 → Datum 거리 측정"
      contains: "TryIntersectLines"
  key_links:
    - from: "ArcLineIntersectDistanceMeasurement.TryExecute"
      to: "VisionAlgorithmService.TryIntersectLines"
      via: "2회 TryFitLine → 교점 계산"
      pattern: "TryIntersectLines"
---

<objective>
ArcLineIntersect (I9/I10) 측정 클래스를 SOP 실무 방식으로 재작성한다: 3점 호 피팅 폐기 → Rect ROI 2개 직선 피팅 + HALCON intersection_lines 교점.

목적: Phase 31 의 ArcLineIntersect 는 MSOP 원문대로 3점 호(arc) 피팅 + 원-직선 교점을 사용했으나, 현장 실무는 "한쪽 수직 에지 직선 ∩ 한쪽 수평 에지 직선"의 단순 2직선 교점을 쓴다 (CONTEXT.md 확정 스펙 1번). 타입 명칭은 유지한다 (사용자 결정).
출력: ArcLineIntersectDistanceMeasurement.cs 전면 재작성 — ROI 필드 세트 교체 + TryExecute 본문 교체.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-CONTEXT.md
@.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md

<interfaces>
<!-- Plan 01 이 추가한 신규 메서드 + 기존 TryFitLine 시그니처. 실행자는 이 계약대로 호출한다. -->

Plan 01 신규 — VisionAlgorithmService.TryIntersectLines (public static):
```csharp
public static bool TryIntersectLines(
    double row1a, double col1a, double row1b, double col1b,
    double row2a, double col2a, double row2b, double col2b,
    out double intRow, out double intCol)
// 평행/근접/중첩 시 false
```

기존 VisionAlgorithmService.TryFitLine (인스턴스 메서드) — selection 인자 "All" 고정 필수:
```csharp
public bool TryFitLine(
    HImage image,
    double roiRow, double roiCol, double roiPhi, double roiLength1, double roiLength2,
    HTuple datumTransform,
    int sampleCount, int trimCount, double sigma, int threshold,
    string direction, string polarity,
    out double row1, out double col1, out double row2, out double col2,
    out string error,
    string selection = "all", ...)
```

기존 ComputeProjectionDistance (static) — 교점 → Datum 거리 (mm):
```csharp
public static double ComputeProjectionDistance(
    double pointRow, double pointCol,
    double datumOriginRow, double datumOriginCol,
    double lineAngleRad, double pixelResolution, string measureAxis)
```

IDatumOriginConsumer (Plan 02 확장 후) — 6 프로퍼티: DatumOriginRow/Col, DatumAngleRad, DatumAngle2Rad, DatumDetectedCircleRow/Col.

ArcEdgeDistanceMeasurement.cs 의 Point_* ROI 필드 패턴 (L21~26) — EdgeA_*/EdgeB_* 의 복제 원본:
```csharp
[Category("Point|ROI")]
public double Point_Row { get; set; }
public double Point_Col { get; set; }
public double Point_Phi { get; set; }
public double Point_Length1 { get; set; }
public double Point_Length2 { get; set; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ArcLineIntersectDistanceMeasurement ROI 필드 세트 교체 (Arc_P1~P3/Line_* → EdgeA_*/EdgeB_*)</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs (현재 전문 — Arc_P1~P3 15필드 L22~40, Line_* 5필드 L43~48, Edge 6필드 L51~59, MeasureAxis L67~73, IDatumOriginConsumer 4필드 L75~92)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcEdgeDistanceMeasurement.cs (Point_* 5필드 L21~26 — 복제 원본)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-CONTEXT.md (확정 스펙 1번 + 미해결#5 — 기존 ROI 필드 제거, INI 하위호환 불필요)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md (ArcLineIntersectDistanceMeasurement 섹션 — 신규 ROI 필드)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs</files>
  <action>
ArcLineIntersectDistanceMeasurement.cs 의 ROI 필드 세트를 교체한다. 변경 라인 전부에 `//260521 hbk Phase 32` 코멘트. 기존 `//260519 hbk Phase 31 ...` 코멘트는 폐기되는 코드와 함께 제거.

(1) 제거: `Arc_P1_*` / `Arc_P2_*` / `Arc_P3_*` 15필드 (현 L22~40), `Line_*` 5필드 (현 L42~48). `[Category("Arc|ROI")]` / `[Category("Line|ROI")]` 도 함께 제거. CONTEXT.md 미해결#5 — INI 하위호환 불필요 (Phase 31 신규 타입).

(2) 추가: ArcEdgeDistanceMeasurement.cs L21~26 의 Point_* 5필드를 2벌 복제 — 수직 에지용 EdgeA_*, 수평 에지용 EdgeB_*:
```csharp
//260521 hbk Phase 32 — 수직 에지 ROI (SOP P4/P5/p6/p8 측). EdgeA Phi 로 스캔 방향 지정.
[Category("EdgeA|ROI")] //260521 hbk Phase 32
public double EdgeA_Row { get; set; } //260521 hbk Phase 32
public double EdgeA_Col { get; set; } //260521 hbk Phase 32
public double EdgeA_Phi { get; set; } //260521 hbk Phase 32
public double EdgeA_Length1 { get; set; } //260521 hbk Phase 32
public double EdgeA_Length2 { get; set; } //260521 hbk Phase 32

//260521 hbk Phase 32 — 수평 에지 ROI (SOP P1/P3/p9/p10 측).
[Category("EdgeB|ROI")] //260521 hbk Phase 32
public double EdgeB_Row { get; set; } //260521 hbk Phase 32
public double EdgeB_Col { get; set; } //260521 hbk Phase 32
public double EdgeB_Phi { get; set; } //260521 hbk Phase 32
public double EdgeB_Length1 { get; set; } //260521 hbk Phase 32
public double EdgeB_Length2 { get; set; } //260521 hbk Phase 32
```

(3) 유지 (수정 금지): 클래스 선언 `: MeasurementBase, IDatumOriginConsumer`, `TypeName` getter `"ArcLineIntersectDistance"`, ctor `(object owner) : base(owner)`, Edge 6필드 (EdgeThreshold/Sigma/EdgeSampleCount/EdgeTrimCount/EdgePolarity/EdgeDirection) + ComboBox 래퍼, MeasureAxis + MeasureAxisList (기본 "X"), IDatumOriginConsumer 4 transient 필드 (DatumOriginRow/Col, DatumAngleRad, DatumAngle2Rad).

(4) IDatumOriginConsumer 신규 2 프로퍼티 추가 — Plan 02 가 인터페이스를 확장했으므로 본 클래스도 DatumAngle2Rad transient 필드 아래에 3중 attribute 부착 2 프로퍼티를 추가 (Plan 02 Task 3 의 측정 클래스 패턴 동일):
```csharp
//260521 hbk Phase 32 — IDatumOriginConsumer 확장. ArcLineIntersect 미사용 (E2 전용) — 주입만 받음.
[System.ComponentModel.Browsable(false)] //260521 hbk Phase 32
[PropertyTools.DataAnnotations.Browsable(false)] //260521 hbk Phase 32
[Newtonsoft.Json.JsonIgnore] //260521 hbk Phase 32
public double DatumDetectedCircleRow { get; set; } //260521 hbk Phase 32
[System.ComponentModel.Browsable(false)] //260521 hbk Phase 32
[PropertyTools.DataAnnotations.Browsable(false)] //260521 hbk Phase 32
[Newtonsoft.Json.JsonIgnore] //260521 hbk Phase 32
public double DatumDetectedCircleCol { get; set; } //260521 hbk Phase 32
```

C# 7.2 — switch expression / record 금지. 클래스 XML doc 주석도 2직선 교점 방식으로 갱신.
  </action>
  <verify>
    <automated>grep -c "EdgeA_Row\|EdgeB_Row\|Arc_P1_Row\|Line_Row" WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Arc_P1_Row\|Arc_P2_Row\|Arc_P3_Row\|Line_Row" ArcLineIntersectDistanceMeasurement.cs` 가 0건 (기존 ROI 필드 완전 제거)
    - `grep "EdgeA_Row\|EdgeA_Col\|EdgeA_Phi\|EdgeA_Length1\|EdgeA_Length2" ` 가 각 1건
    - `grep "EdgeB_Row\|EdgeB_Col\|EdgeB_Phi\|EdgeB_Length1\|EdgeB_Length2" ` 가 각 1건
    - `[Category("EdgeA|ROI")]` + `[Category("EdgeB|ROI")]` 각 1건
    - `public double DatumDetectedCircleRow` + `DatumDetectedCircleCol` 각 1건
    - `TypeName` getter 가 여전히 `"ArcLineIntersectDistance"` 반환
  </acceptance_criteria>
  <done>ROI 필드가 EdgeA/EdgeB 5필드 2벌로 교체되고, 클래스 구조(TypeName/ctor/Edge/MeasureAxis/IDatumOriginConsumer)는 유지된다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TryExecute 본문 재작성 (2직선 피팅 → intersection_lines 교점 → Datum 거리)</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs (Task 1 적용 후 상태 — TryExecute 본문 L96~201)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcEdgeDistanceMeasurement.cs (TryExecute L72~194 — TryFitLine "All" 호출 + ComputeProjectionDistance 패턴)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-CONTEXT.md (확정 스펙 1번, 미해결#3 평행/근접 안전 종결)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md (ArcLineIntersectDistanceMeasurement 섹션 — 새 알고리즘 본문 + EdgeSelection "All" 명시)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs</files>
  <action>
TryExecute 본문 (현 L104~201)을 전면 교체한다. 변경 라인에 `//260521 hbk Phase 32`. 기존 `//260519 hbk Phase 31 ...` 코멘트는 폐기 코드와 함께 제거. TryExecute 시그니처는 유지 (HImage image, HTuple datumTransform, double pixelResolution, out double resultValue, out string error, out List<EdgeInspectionOverlay> overlays).

새 본문:
```csharp
resultValue = 0;
error = null;
overlays = new List<EdgeInspectionOverlay>(); //260521 hbk Phase 32

var svc = new VisionAlgorithmService(); //260521 hbk Phase 32

// (1) EdgeA ROI — 수직 에지 직선 피팅. EdgeSelection "All" 고정 (memory feedback — 단일 MeasurePos 금지)
double a1r1, a1c1, a1r2, a1c2; //260521 hbk Phase 32
if (!svc.TryFitLine(image,
    EdgeA_Row, EdgeA_Col, EdgeA_Phi, EdgeA_Length1, EdgeA_Length2,
    datumTransform,
    EdgeSampleCount, EdgeTrimCount, Sigma, EdgeThreshold,
    EdgeDirection, EdgePolarity,
    out a1r1, out a1c1, out a1r2, out a1c2, out error,
    "All")) //260521 hbk Phase 32 — EdgeSelection "All" 고정
{
    return false;
}

// (2) EdgeB ROI — 수평 에지 직선 피팅
double b1r1, b1c1, b1r2, b1c2; //260521 hbk Phase 32
if (!svc.TryFitLine(image,
    EdgeB_Row, EdgeB_Col, EdgeB_Phi, EdgeB_Length1, EdgeB_Length2,
    datumTransform,
    EdgeSampleCount, EdgeTrimCount, Sigma, EdgeThreshold,
    EdgeDirection, EdgePolarity,
    out b1r1, out b1c1, out b1r2, out b1c2, out error,
    "All")) //260521 hbk Phase 32 — EdgeSelection "All" 고정
{
    return false;
}

// (3) 두 직선 교점 — intersection_lines 래퍼. 평행/근접 시 false (측정값 '—')
double intRow, intCol; //260521 hbk Phase 32
if (!VisionAlgorithmService.TryIntersectLines(
    a1r1, a1c1, a1r2, a1c2,
    b1r1, b1c1, b1r2, b1c2,
    out intRow, out intCol)) //260521 hbk Phase 32
{
    error = "Line intersection failed (parallel or near-parallel edges)"; //260521 hbk Phase 32
    return false;
}

// (4) 교점 → Datum 기준선 투영 거리. X측정=2차(수직)선, Y측정=1차(수평)선
double measureLineAngle = (MeasureAxis == "X") ? DatumAngle2Rad : DatumAngleRad; //260521 hbk Phase 32
resultValue = VisionAlgorithmService.ComputeProjectionDistance(
    intRow, intCol,
    DatumOriginRow, DatumOriginCol, measureLineAngle,
    pixelResolution, MeasureAxis); //260521 hbk Phase 32

return true;
```

overlay 는 빈 리스트 유지 (Phase 31 ArcLineIntersect 도 overlay 없었음 — 본 phase 범위 밖). TryFitArc / TryIntersectCircleLine 호출은 완전히 사라진다.

주의: HALCON 직접 호출 없음 (전부 svc 메서드 위임). C# 7.2 — `(MeasureAxis == "X") ? ...` 삼항은 허용 (switch expression 아님). 클래스에 IDatumOriginConsumer 4+2 transient 필드 전부 존재해야 빌드 통과 (Task 1 에서 완료).
  </action>
  <verify>
    <automated>grep -n "TryIntersectLines\|TryFitArc\|TryIntersectCircleLine" WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "VisionAlgorithmService.TryIntersectLines" ArcLineIntersectDistanceMeasurement.cs` 가 1건
    - `grep "TryFitArc\|TryIntersectCircleLine" ArcLineIntersectDistanceMeasurement.cs` 가 0건 (호 피팅 완전 폐기)
    - `grep "TryFitLine" ArcLineIntersectDistanceMeasurement.cs` 가 2건 (EdgeA + EdgeB)
    - `grep '\"All\"' ArcLineIntersectDistanceMeasurement.cs` 가 2건 (각 TryFitLine 호출에 EdgeSelection 명시)
    - `grep "Line intersection failed" ` 가 1건 (평행/근접 안전 종결)
    - msbuild Debug/x64 시 ArcLineIntersectDistanceMeasurement.cs 관련 신규 error 0
  </acceptance_criteria>
  <done>TryExecute 가 EdgeA/EdgeB 2 ROI 에서 직선을 피팅하고 TryIntersectLines 로 교점을 산출한 뒤 ComputeProjectionDistance 로 Datum 거리를 측정한다. 평행/근접 시 false 로 안전 종결.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레시피 ROI → 측정 알고리즘 | EdgeA/EdgeB ROI 좌표·Edge 파라미터가 HALCON 직선 피팅으로 진입 — 로컬 레시피 신뢰 소스 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Denial of Service | 평행 직선 교점 | mitigate | TryIntersectLines (Plan 01) 의 평행/근접/중첩 가드 → false → 측정값 '—' 표시, 크래시 없음 (CONTEXT.md 미해결#3) |
| T-32-05 | Denial of Service | TryFitLine 에지 부족 | mitigate | TryFitLine 실패 시 false 반환 → TryExecute 즉시 false — 검사 스레드 정상 종결 |

본 plan 은 단일 측정 클래스 재작성. 외부 신뢰 불가 입력 없음. high-severity 위협 없음.
</threat_model>

<verification>
- ArcLineIntersectDistanceMeasurement.cs 가 EdgeA/EdgeB 2 ROI 직선 피팅 + TryIntersectLines 교점 방식으로 동작
- 기존 Arc_P1~P3 / Line_* 필드, TryFitArc / TryIntersectCircleLine 호출 완전 제거
- msbuild Debug/x64 PASS (Wave 2 전체 완료 후 — Plan 04 의 Compound* 재작성 포함)
</verification>

<success_criteria>
- 3점 호 피팅 폐기, 2직선 교점 방식으로 전환 — 타입명 ArcLineIntersectDistance 유지
- EdgeSelection "All" 2회 명시 (memory feedback 준수)
- 평행/근접 안전 종결
</success_criteria>

<output>
완료 후 `.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-03-SUMMARY.md` 생성.
</output>
