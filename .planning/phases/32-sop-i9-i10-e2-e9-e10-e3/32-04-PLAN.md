---
phase: 32-sop-i9-i10-e2-e9-e10-e3
plan: 04
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundAngleMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterCDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterBDistanceMeasurement.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "E2 CompoundAngle 가 Rect ROI 1개의 LargestRect 중심 ↔ DatumC 검출 원중심 대각선과 DatumB 기준선 사이 각도 1개를 산출한다"
    - "E9 CompoundCenterC 가 LargestRect 중심에서 Datum C X 거리(mm)를 산출한다"
    - "E10 CompoundCenterB 가 LargestRect 중심에서 Datum B Y 거리(mm)를 산출한다"
    - "canny(alpha/low/high)와 union 거리가 PropertyGrid 사용자 편집 파라미터로 노출된다"
    - "CL1~3 원피팅 + La/Lb 라인 체인 + TryComputeChainPoint 헬퍼가 완전히 제거된다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundAngleMeasurement.cs"
      provides: "E2 — LargestRect↔DatumC원중심 대각선 vs DatumB 각도"
      contains: "TryFindLargestContourRect"
  key_links:
    - from: "CompoundAngleMeasurement.TryExecute"
      to: "VisionAlgorithmService.TryFindLargestContourRect + DatumDetectedCircleRow"
      via: "LargestRect 중심 ↔ 검출 원중심 대각선 라인"
      pattern: "DatumDetectedCircleRow"
---

<objective>
E2(CompoundAngle) / E9(CompoundCenterCDistance) / E10(CompoundCenterBDistance) 측정 클래스 3종을 공통 컨투어 알고리즘 기반으로 재작성한다.

목적: Phase 31 의 Compound* 3종은 CL1~3 원피팅 + La/Lb 라인 + midline 교점 체인을 사용했으나, 현장 실무는 Rect ROI 1개에서 공통 컨투어 알고리즘(canny→union→LargestRect)으로 최대 면적 사각형을 검출해 그 중심을 측정 기준점으로 쓴다 (CONTEXT.md 확정 스펙 3/5번). E2 는 추가로 DatumC 검출 원중심과의 대각선 각도, E9/E10 은 LargestRect 중심에서 Datum 거리를 측정한다.
출력: CompoundAngle/CompoundCenterC/CompoundCenterB 3 파일 전면 재작성 — ROI 필드 교체 + Contour 파라미터 추가 + TryExecute 본문 교체.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-CONTEXT.md
@.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md

<interfaces>
<!-- Plan 01 신규 메서드 + Plan 02 확장 인터페이스 + 기존 헬퍼. 이 계약대로 호출. -->

Plan 01 신규 — VisionAlgorithmService.TryFindLargestContourRect (인스턴스 메서드):
```csharp
public bool TryFindLargestContourRect(
    HImage image,
    double roiRow, double roiCol, double roiPhi, double roiLength1, double roiLength2,
    HTuple datumTransform,
    double cannyAlpha, int cannyLow, int cannyHigh, double unionDistance,
    out double centerRow, out double centerCol, out double phi,
    out double length1, out double length2, out string error)
// 사각형 0개 검출 시 false + error
```

Plan 02 확장 — IDatumOriginConsumer 6 프로퍼티:
DatumOriginRow/Col, DatumAngleRad, DatumAngle2Rad, DatumDetectedCircleRow, DatumDetectedCircleCol.
DatumDetectedCircleRow/Col 에 Action_FAIMeasurement 가 DatumC(CircleTwoHorizontal) 검출 원중심을 주입한다 (E2 만 사용).

기존 static 헬퍼:
```csharp
public static double AngleLineLine(
    double r1a, double c1a, double r1b, double c1b,
    double r2a, double c2a, double r2b, double c2b) // degree, 0~180 정규화
public static double ComputeProjectionDistance(
    double pointRow, double pointCol, double datumOriginRow, double datumOriginCol,
    double lineAngleRad, double pixelResolution, string measureAxis)
```

ArcEdgeDistanceMeasurement.cs Point_* ROI 필드 패턴 (L21~26) — Rect_* 의 복제 원본 (5필드).
PATTERNS.md 의 신규 Contour 카테고리 파라미터:
```csharp
[Category("Contour")]
public double CannyAlpha { get; set; } = 1.0;
public int CannyLow { get; set; } = 20;
public int CannyHigh { get; set; } = 40;
public double UnionDistance { get; set; } = 700.0;
```

DatumB 기준선 2점 구성 패턴 (CompoundAngleMeasurement 현 L215~220):
```csharp
double sinT = System.Math.Sin(DatumAngleRad), cosT = System.Math.Cos(DatumAngleRad);
double daR1 = DatumOriginRow - 200.0 * sinT; double daC1 = DatumOriginCol - 200.0 * cosT;
double daR2 = DatumOriginRow + 200.0 * sinT; double daC2 = DatumOriginCol + 200.0 * cosT;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CompoundCenterCDistance(E9) + CompoundCenterBDistance(E10) 재작성</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterCDistanceMeasurement.cs (현재 전문 — Cl2/Cl3 6필드 L22~31, La/Lb 10필드 L33~47, Edge 6필드, MeasureAxis L67~73, IDatumOriginConsumer 4필드, TryComputeChainPoint L102~181, TryExecute L183~213)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterBDistanceMeasurement.cs (현재 전문 — E9 와 쌍둥이, TypeName="CompoundCenterBDistance", MeasureAxis 기본 "Y")
    - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcEdgeDistanceMeasurement.cs (Point_* 5필드 L21~26 — Rect_* 복제 원본)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-CONTEXT.md (확정 스펙 5번 + 미해결#4/#5)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md (CompoundCenterC/B 섹션)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterCDistanceMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterBDistanceMeasurement.cs</files>
  <action>
CompoundCenterCDistanceMeasurement.cs 와 CompoundCenterBDistanceMeasurement.cs 를 재작성한다 (둘은 쌍둥이 — 동일 본문, TypeName/MeasureAxis 기본값 2개만 차이). 변경 라인에 `//260521 hbk Phase 32`, 기존 `//260519 hbk Phase 31` 코멘트는 폐기 코드와 함께 제거.

**제거** (양 파일):
- Cl2/Cl3 6필드 + `[Category("CL2|ROI")]` / `[Category("CL3|ROI")]`
- La/Lb 10필드 + `[Category("La|ROI")]` / `[Category("Lb|ROI")]`
- `TryComputeChainPoint` private 헬퍼 메서드 전체

**유지** (수정 금지): 클래스 선언 `: MeasurementBase, IDatumOriginConsumer`, ctor, Edge 6필드 + ComboBox 래퍼, MeasureAxis + MeasureAxisList, IDatumOriginConsumer 4 transient 필드.
- CompoundCenterC: `TypeName` = "CompoundCenterCDistance", MeasureAxis 기본 "X"
- CompoundCenterB: `TypeName` = "CompoundCenterBDistance", MeasureAxis 기본 "Y"

**추가** (양 파일):
1. Rect ROI 5필드 (ArcEdgeDistanceMeasurement Point_* 복제):
```csharp
//260521 hbk Phase 32 — 공통 컨투어 알고리즘 입력 Rect ROI
[Category("Rect|ROI")] //260521 hbk Phase 32
public double Rect_Row { get; set; } //260521 hbk Phase 32
public double Rect_Col { get; set; } //260521 hbk Phase 32
public double Rect_Phi { get; set; } //260521 hbk Phase 32
public double Rect_Length1 { get; set; } //260521 hbk Phase 32
public double Rect_Length2 { get; set; } //260521 hbk Phase 32
```
2. Contour 카테고리 파라미터 4개 (PropertyGrid 노출 — CONTEXT.md Q5):
```csharp
//260521 hbk Phase 32 — 공통 컨투어 알고리즘 파라미터 (PropertyGrid 사용자 편집)
[Category("Contour")] //260521 hbk Phase 32
public double CannyAlpha { get; set; } = 1.0; //260521 hbk Phase 32 — edges_sub_pix canny alpha
public int CannyLow { get; set; } = 20; //260521 hbk Phase 32 — canny low threshold
public int CannyHigh { get; set; } = 40; //260521 hbk Phase 32 — canny high threshold
public double UnionDistance { get; set; } = 700.0; //260521 hbk Phase 32 — union_adjacent_contours_xld 거리
```
3. IDatumOriginConsumer 신규 2 프로퍼티 (DatumAngle2Rad 아래, 3중 attribute — Plan 02 측정 클래스 패턴):
```csharp
//260521 hbk Phase 32 — IDatumOriginConsumer 확장. CompoundCenter 미사용 (E2 전용) — 주입만 받음.
[System.ComponentModel.Browsable(false)] //260521 hbk Phase 32
[PropertyTools.DataAnnotations.Browsable(false)] //260521 hbk Phase 32
[Newtonsoft.Json.JsonIgnore] //260521 hbk Phase 32
public double DatumDetectedCircleRow { get; set; } //260521 hbk Phase 32
[System.ComponentModel.Browsable(false)] //260521 hbk Phase 32
[PropertyTools.DataAnnotations.Browsable(false)] //260521 hbk Phase 32
[Newtonsoft.Json.JsonIgnore] //260521 hbk Phase 32
public double DatumDetectedCircleCol { get; set; } //260521 hbk Phase 32
```

**TryExecute 본문 교체** (양 파일 동일):
```csharp
resultValue = 0;
error = null;
overlays = new List<EdgeInspectionOverlay>(); //260521 hbk Phase 32

var svc = new VisionAlgorithmService(); //260521 hbk Phase 32

// (1) 공통 컨투어 알고리즘 → LargestRect 중심
double centerRow, centerCol, phi, len1, len2; //260521 hbk Phase 32
if (!svc.TryFindLargestContourRect(image,
    Rect_Row, Rect_Col, Rect_Phi, Rect_Length1, Rect_Length2,
    datumTransform,
    CannyAlpha, CannyLow, CannyHigh, UnionDistance,
    out centerRow, out centerCol, out phi, out len1, out len2, out error)) //260521 hbk Phase 32
{
    return false;
}

// (2) LargestRect 중심 → Datum 거리. X측정=2차(수직)선, Y측정=1차(수평)선
double measureLineAngle = (MeasureAxis == "X") ? DatumAngle2Rad : DatumAngleRad; //260521 hbk Phase 32
resultValue = VisionAlgorithmService.ComputeProjectionDistance(
    centerRow, centerCol,
    DatumOriginRow, DatumOriginCol, measureLineAngle,
    pixelResolution, MeasureAxis); //260521 hbk Phase 32

return true;
```
overlay 빈 리스트 유지. 클래스 XML doc 주석을 공통 컨투어 알고리즘 방식으로 갱신.

C# 7.2 — switch expression / record 금지.
  </action>
  <verify>
    <automated>grep -c "TryFindLargestContourRect\|Cl2_Row\|La_Row\|TryComputeChainPoint" WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterCDistanceMeasurement.cs WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterBDistanceMeasurement.cs</automated>
  </verify>
  <acceptance_criteria>
    - 양 파일에서 `grep "Cl2_Row\|Cl3_Row\|La_Row\|Lb_Row"` 가 0건 (CL/La/Lb 필드 완전 제거)
    - 양 파일에서 `grep "TryComputeChainPoint"` 가 0건 (체인 헬퍼 제거)
    - 양 파일에 `svc.TryFindLargestContourRect` 호출 각 1건
    - 양 파일에 `Rect_Row` / `Rect_Col` / `Rect_Phi` / `Rect_Length1` / `Rect_Length2` 각 1건
    - 양 파일에 `CannyAlpha` / `CannyLow` / `CannyHigh` / `UnionDistance` 각 1건, `[Category("Contour")]` 1건
    - CompoundCenterC TypeName = "CompoundCenterCDistance" + MeasureAxis 기본 "X"; CompoundCenterB TypeName = "CompoundCenterBDistance" + MeasureAxis 기본 "Y"
    - 양 파일에 `DatumDetectedCircleRow` / `DatumDetectedCircleCol` 각 1건
  </acceptance_criteria>
  <done>E9/E10 가 공통 컨투어 알고리즘으로 LargestRect 중심을 검출하고 Datum C/B 거리를 측정한다. canny/union 파라미터가 PropertyGrid 에 노출되고, CL/La/Lb 체인이 완전히 제거된다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CompoundAngle(E2) 재작성 — LargestRect↔DatumC원중심 대각선 vs DatumB 각도</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundAngleMeasurement.cs (현재 전문 — Cl1/Cl2/Cl3 9필드 L22~37, La/Lb 10필드 L40~53, Edge 6필드, IDatumOriginConsumer 4필드, TryComputeChainPoint L100~179, TryExecute L181 이후, DatumB 기준선 L215~220, AngleLineLine 호출 L222 부근)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterCDistanceMeasurement.cs (Task 1 재작성 후 상태 — Rect ROI/Contour 파라미터/구조 참조)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-CONTEXT.md (확정 스펙 3번 — E2 CompoundAngle)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md (CompoundAngleMeasurement 섹션 — 새 알고리즘 본문)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundAngleMeasurement.cs</files>
  <action>
CompoundAngleMeasurement.cs 를 재작성한다. 변경 라인에 `//260521 hbk Phase 32`, 기존 `//260519 hbk Phase 31` 코멘트는 폐기 코드와 함께 제거.

**제거**: Cl1/Cl2/Cl3 9필드 + 각 `[Category("CLn|ROI")]`, La/Lb 10필드 + `[Category("La|ROI")]`/`[Category("Lb|ROI")]`, `TryComputeChainPoint` private 헬퍼 전체.

**유지** (수정 금지): 클래스 선언 `: MeasurementBase, IDatumOriginConsumer`, `TypeName` getter `"CompoundAngle"`, ctor, Edge 6필드 + ComboBox 래퍼, IDatumOriginConsumer 4 transient 필드. (CompoundAngle 은 각도 측정이므로 MeasureAxis 가 원래 없음 — 있으면 유지, 없으면 추가 금지.)

**추가** — Task 1 의 CompoundCenterC 와 동일한:
1. Rect ROI 5필드 (`Rect_Row/Col/Phi/Length1/Length2`, `[Category("Rect|ROI")]`)
2. Contour 카테고리 파라미터 4개 (CannyAlpha=1.0, CannyLow=20, CannyHigh=40, UnionDistance=700.0)
3. IDatumOriginConsumer 신규 2 프로퍼티 (`DatumDetectedCircleRow`/`Col`, 3중 attribute). **E2 는 이 2 필드를 실제로 사용한다** — DatumC 검출 원중심.

**TryExecute 본문 교체**:
```csharp
resultValue = 0;
error = null;
overlays = new List<EdgeInspectionOverlay>(); //260521 hbk Phase 32

var svc = new VisionAlgorithmService(); //260521 hbk Phase 32

// (1) 공통 컨투어 알고리즘 → LargestRect 중심
double centerRow, centerCol, phi, len1, len2; //260521 hbk Phase 32
if (!svc.TryFindLargestContourRect(image,
    Rect_Row, Rect_Col, Rect_Phi, Rect_Length1, Rect_Length2,
    datumTransform,
    CannyAlpha, CannyLow, CannyHigh, UnionDistance,
    out centerRow, out centerCol, out phi, out len1, out len2, out error)) //260521 hbk Phase 32
{
    return false;
}

// (2) DatumC 검출 원중심 주입값 검증 — E2 는 CircleTwoHorizontal datum 전제
if (DatumDetectedCircleRow == 0.0 && DatumDetectedCircleCol == 0.0) //260521 hbk Phase 32
{
    error = "DatumC detected circle center not injected (requires CircleTwoHorizontal datum)"; //260521 hbk Phase 32
    return false;
}

// (3) DatumB 기준선 2점 (DatumOrigin 중심 ±200px, 방향 = DatumAngleRad)
double sinT = System.Math.Sin(DatumAngleRad); //260521 hbk Phase 32
double cosT = System.Math.Cos(DatumAngleRad); //260521 hbk Phase 32
double daR1 = DatumOriginRow - 200.0 * sinT; //260521 hbk Phase 32
double daC1 = DatumOriginCol - 200.0 * cosT; //260521 hbk Phase 32
double daR2 = DatumOriginRow + 200.0 * sinT; //260521 hbk Phase 32
double daC2 = DatumOriginCol + 200.0 * cosT; //260521 hbk Phase 32

// (4) 대각선 라인 (LargestRect 중심 ↔ DatumC 검출 원중심) vs DatumB 기준선 각도 1개
resultValue = VisionAlgorithmService.AngleLineLine(
    centerRow, centerCol, DatumDetectedCircleRow, DatumDetectedCircleCol, //260521 hbk Phase 32 — 대각선
    daR1, daC1, daR2, daC2); //260521 hbk Phase 32 — DatumB 기준선

return true;
```
overlay 빈 리스트 유지. 클래스 XML doc 주석을 LargestRect↔DatumC원중심 대각선 각도 방식으로 갱신.

C# 7.2 — switch expression / record 금지.
  </action>
  <verify>
    <automated>grep -n "TryFindLargestContourRect\|AngleLineLine\|DatumDetectedCircleRow\|TryComputeChainPoint\|Cl1_Row" WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundAngleMeasurement.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Cl1_Row\|Cl2_Row\|Cl3_Row\|La_Row\|Lb_Row" CompoundAngleMeasurement.cs` 가 0건
    - `grep "TryComputeChainPoint" CompoundAngleMeasurement.cs` 가 0건
    - `grep "svc.TryFindLargestContourRect" CompoundAngleMeasurement.cs` 가 1건
    - `grep "AngleLineLine" CompoundAngleMeasurement.cs` 가 1건 (각도 1개만 산출)
    - `grep "DatumDetectedCircleRow" CompoundAngleMeasurement.cs` 가 실사용 (TryExecute 본문에서 참조 — 단순 프로퍼티 선언 외 1건 이상)
    - `grep "Rect_Row" + "CannyAlpha" + "UnionDistance"` 각 1건
    - `grep "DatumC detected circle center not injected" ` 가 1건 (검출 원중심 미주입 안전 종결)
    - `TypeName` getter 가 여전히 `"CompoundAngle"` 반환
  </acceptance_criteria>
  <done>E2 가 LargestRect 중심과 DatumC 검출 원중심의 대각선, DatumB 기준선 사이 각도 1개를 산출한다. CL/La/Lb 체인 완전 제거. 검출 원중심 미주입 시 안전 종결.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레시피 ROI + datum 검출 → 측정 알고리즘 | Rect ROI 좌표·Contour 파라미터·주입된 datum 원중심이 측정으로 진입 — 로컬 신뢰 소스 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06 | Denial of Service | 사각형 0개 검출 (canny/union 실패) | mitigate | TryFindLargestContourRect (Plan 01) 가 0개 검출 시 false + error → TryExecute 즉시 false, 측정값 '—' (CONTEXT.md 미해결#4) |
| T-32-07 | Information Disclosure | E2 의 DatumC 원중심 미주입 (잘못된 datum 알고리즘) | mitigate | DatumDetectedCircleRow/Col 모두 0 이면 명시적 error 반환 — TwoLineIntersect/VerticalTwoHorizontal datum 에 E2 를 잘못 연결 시 오측정 대신 안전 종결 |

본 plan 은 측정 클래스 3종 재작성. 외부 신뢰 불가 입력 없음. high-severity 위협 없음.
</threat_model>

<verification>
- CompoundAngle/CompoundCenterC/CompoundCenterB 가 공통 컨투어 알고리즘 기반으로 동작
- CL1~3 / La/Lb 필드, TryComputeChainPoint 헬퍼 완전 제거
- canny/union 4 파라미터 PropertyGrid 노출
- msbuild Debug/x64 PASS (Wave 2 전체 — Plan 03 포함 — 완료 후)
</verification>

<success_criteria>
- E2 = LargestRect↔DatumC원중심 대각선 vs DatumB 각도 1개
- E9 = LargestRect 중심 → Datum C X 거리, E10 = LargestRect 중심 → Datum B Y 거리
- 기하 체인(CL/La/Lb/midline) 폐기
- 사각형 0개 검출 + 원중심 미주입 안전 종결
</success_criteria>

<output>
완료 후 `.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-04-SUMMARY.md` 생성.
</output>
