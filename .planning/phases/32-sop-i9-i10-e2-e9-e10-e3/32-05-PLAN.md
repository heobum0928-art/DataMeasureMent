---
phase: 32-sop-i9-i10-e2-e9-e10-e3
plan: 05
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundShortAxisDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: []
must_haves:
  truths:
    - "E3 신규 측정 타입 CompoundShortAxisDistance 가 존재하며 Rect ROI 1개의 LargestRect 단축 폭을 측정한다"
    - "MeasurementFactory 가 'CompoundShortAxisDistance' 문자열을 CompoundShortAxisDistanceMeasurement 인스턴스로 매핑한다"
    - "FAIConfig Type 드롭다운에 CompoundShortAxisDistance 가 노출된다"
    - "신규 클래스 파일이 csproj 에 등록되어 빌드에 포함된다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundShortAxisDistanceMeasurement.cs"
      provides: "E3 — LargestRect 단축 거리 측정 (공차 0.600±0.030)"
      contains: "CompoundShortAxisDistanceMeasurement"
  key_links:
    - from: "MeasurementFactory.Create"
      to: "CompoundShortAxisDistanceMeasurement"
      via: "case \"CompoundShortAxisDistance\""
      pattern: "CompoundShortAxisDistance"
---

<objective>
E3 신규 측정 타입 CompoundShortAxisDistance 를 추가한다 — SOP p.50 La/Lb 2직선 거리(공차 0.600±0.030)를 LargestRect 단축 폭으로 측정.

목적: Phase 31 에는 E3 타입이 없었다. CONTEXT.md 확정 스펙 4번에 따라 공통 컨투어 알고리즘으로 LargestRect 를 검출하고, 그 사각형의 짧은 변 폭(단축 방향 선과 사각형 컨투어의 교점 2개 거리)을 측정하는 신규 타입을 만든다. TypeName 은 CompoundShortAxisDistance 로 확정한다 (CONTEXT.md 미해결#1 — PATTERNS.md 권장).
출력: CompoundShortAxisDistanceMeasurement.cs 신규 생성, MeasurementFactory 등록, csproj Compile 항목 추가.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-CONTEXT.md
@.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md

<interfaces>
<!-- Plan 01 신규 메서드 + MeasurementBase 표준 멤버. 이 계약대로 구현. -->

Plan 01 신규 — VisionAlgorithmService:
```csharp
// LargestRect 중심/각도/장단축 + (E3 에 필요) — TryFindLargestContourRect 는 shape_trans 된
// 사각형 XLD 를 직접 반환하지 않으므로, 본 plan 의 E3 클래스는 중심/phi/len2 로 단축선을 구성한다.
public bool TryFindLargestContourRect(
    HImage image,
    double roiRow, double roiCol, double roiPhi, double roiLength1, double roiLength2,
    HTuple datumTransform,
    double cannyAlpha, int cannyLow, int cannyHigh, double unionDistance,
    out double centerRow, out double centerCol, out double phi,
    out double length1, out double length2, out string error)
```

MeasurementBase 표준 멤버 (MeasurementBase.cs L46~60):
```csharp
public abstract string TypeName { get; }
public abstract bool TryExecute(HImage image, HTuple datumTransform, double pixelResolution,
    out double resultValue, out string error, out List<EdgeInspectionOverlay> overlays);
// ctor: protected MeasurementBase(object owner)
// 공차 판정 EvaluateJudgement / NominalValue / TolerancePlus / ToleranceMinus 는 base 제공 — 재정의 금지
```

MeasurementFactory.cs Create switch 등록 패턴 (기존):
```csharp
case "ArcEdgeDistance": //260519 hbk Phase 31 D-08
    return new ArcEdgeDistanceMeasurement(owner);
```
GetTypeNames() 배열에도 동일 문자열 추가 필요.

ArcEdgeDistanceMeasurement.cs — 구조 analog (Edge 6필드, ComboBox 래퍼, ctor, frontmatter).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CompoundShortAxisDistanceMeasurement.cs 신규 생성 (E3)</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcEdgeDistanceMeasurement.cs (구조 analog — frontmatter, Edge 6필드 L28~42, ComboBox 래퍼, ctor, TryExecute 시그니처)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (abstract 멤버 TypeName/TryExecute, ctor, EvaluateJudgement)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterCDistanceMeasurement.cs (Plan 04 재작성본 — Rect ROI/Contour 파라미터 패턴 참조)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-CONTEXT.md (확정 스펙 4번 — E3 단축 거리 + 공차 0.600±0.030, 미해결#1 TypeName, #3 교점 안전 종결)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md (CompoundShortAxisDistanceMeasurement 섹션)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundShortAxisDistanceMeasurement.cs</files>
  <action>
신규 파일 `CompoundShortAxisDistanceMeasurement.cs` 를 생성한다. 전 라인에 `//260521 hbk Phase 32 E3` 코멘트. namespace `ReringProject.Sequence`. Allman 브레이스 스타일 (신규 Halcon 측정 클래스 — ArcEdgeDistanceMeasurement 가 K&R 이면 그 파일 스타일을 따르되, 신규 파일이므로 ArcEdgeDistanceMeasurement.cs 와 일관되게).

클래스 골격 (ArcEdgeDistanceMeasurement 구조 복제):
- using: System / System.Collections.Generic / HalconDotNet / PropertyTools.DataAnnotations / ReringProject.Halcon.Algorithms / ReringProject.Halcon.Models
- 선언: `public class CompoundShortAxisDistanceMeasurement : MeasurementBase`
  — **IDatumOriginConsumer 미구현**: E3 단축 폭은 사각형 자체 기하이므로 Datum 의존 불필요 (CONTEXT.md 미해결#1 — Datum 미사용이면 인터페이스 미구현 + 4필드 제거). datum transient 4필드도 추가하지 않는다.
- `TypeName` getter: `return "CompoundShortAxisDistance";` — CONTEXT.md 미해결#1 확정값.
- ctor: `public CompoundShortAxisDistanceMeasurement(object owner) : base(owner) { }`
- Rect ROI 5필드: `[Category("Rect|ROI")]` + `Rect_Row/Col/Phi/Length1/Length2` (double).
- Contour 파라미터 4개: `[Category("Contour")]` + `CannyAlpha=1.0` (double) / `CannyLow=20` (int) / `CannyHigh=40` (int) / `UnionDistance=700.0` (double).
- 공차/NominalValue 는 클래스에 default 설정하지 않는다 — 레시피 INI 값으로 주입 (공차 0.600±0.030 은 레시피에서 설정). base 의 NominalValue/TolerancePlus/ToleranceMinus/EvaluateJudgement 재정의 금지.

TryExecute 본문 — LargestRect 단축 폭 측정:
```csharp
public override bool TryExecute(
    HImage image,
    HTuple datumTransform,
    double pixelResolution,
    out double resultValue,
    out string error,
    out List<EdgeInspectionOverlay> overlays)
{
    resultValue = 0;
    error = null;
    overlays = new List<EdgeInspectionOverlay>(); //260521 hbk Phase 32 E3

    var svc = new VisionAlgorithmService(); //260521 hbk Phase 32 E3

    // 공통 컨투어 알고리즘 → LargestRect 중심/각도/장단축
    double centerRow, centerCol, phi, length1, length2; //260521 hbk Phase 32 E3
    if (!svc.TryFindLargestContourRect(image,
        Rect_Row, Rect_Col, Rect_Phi, Rect_Length1, Rect_Length2,
        datumTransform,
        CannyAlpha, CannyLow, CannyHigh, UnionDistance,
        out centerRow, out centerCol, out phi, out length1, out length2, out error)) //260521 hbk Phase 32 E3
    {
        return false;
    }

    // 단축 폭 = SmallestRectangle2 의 짧은 변. smallest_rectangle2_xld 의 length1/length2 중
    // 작은 쪽이 단축 반길이 → 단축 폭 = 2 * min(length1, length2).
    double shortHalf = System.Math.Min(length1, length2); //260521 hbk Phase 32 E3
    double shortAxisWidthPx = 2.0 * shortHalf; //260521 hbk Phase 32 E3 — 사각형 짧은 변 폭 (px)

    resultValue = shortAxisWidthPx * pixelResolution; //260521 hbk Phase 32 E3 — mm 변환 (SOP La↔Lb 간격 등가)
    return true;
}
```

**구현 결정 근거 (action 에 명시)**: PATTERNS.md 는 intersection_contours_xld 로 단축선↔사각형 교점 2점을 구하라고 제시하나, `TryFindLargestContourRect` 가 반환하는 `smallest_rectangle2_xld` 의 length1/length2 는 정의상 사각형의 장축/단축 반길이이므로 `2 * min(length1, length2)` 가 단축 폭과 수학적으로 동일하다. Plan 01 의 TryFindLargestContourRect 가 사각형 XLD 객체를 반환하지 않으므로 (out 파라미터는 스칼라만), 본 plan 은 length1/length2 기반 단순 계산을 채택한다 — TryIntersectContours 호출 불필요. 이는 사용자 스크립트의 단축 거리와 등가이며 더 단순하고 안전하다 (교점 0개 위험 없음).

C# 7.2 — switch expression / record / nullable ref 금지.
  </action>
  <verify>
    <automated>grep -n "class CompoundShortAxisDistanceMeasurement\|TypeName\|TryFindLargestContourRect" WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundShortAxisDistanceMeasurement.cs</automated>
  </verify>
  <acceptance_criteria>
    - 파일이 존재하고 `public class CompoundShortAxisDistanceMeasurement : MeasurementBase` 선언 포함
    - `TypeName` getter 가 `"CompoundShortAxisDistance"` 반환 (정확히 이 문자열)
    - `grep "svc.TryFindLargestContourRect" ` 가 1건
    - `grep "IDatumOriginConsumer" CompoundShortAxisDistanceMeasurement.cs` 가 0건 (Datum 미의존)
    - Rect_Row/Col/Phi/Length1/Length2 + CannyAlpha/CannyLow/CannyHigh/UnionDistance 각 1건
    - `2.0 * shortHalf` 또는 `2 * System.Math.Min` 단축 폭 계산 존재
  </acceptance_criteria>
  <done>E3 신규 측정 클래스가 생성되어 공통 컨투어 알고리즘으로 LargestRect 단축 폭을 측정한다. Datum 비의존.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MeasurementFactory 등록 + csproj Compile 항목 추가</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs (현재 전문 — Create switch + GetTypeNames 배열)
    - WPF_Example/DatumMeasurement.csproj (Measurements Compile Include 블록 L219~233)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md (MeasurementFactory 섹션)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs, WPF_Example/DatumMeasurement.csproj</files>
  <action>
(A) MeasurementFactory.cs — `Create` switch 와 `GetTypeNames` 배열 양쪽에 E3 타입을 추가한다. 변경 라인에 `//260521 hbk Phase 32 E3` 코멘트.

Create switch — 기존 `case "CompoundCenterBDistance":` 다음에:
```csharp
case "CompoundShortAxisDistance": //260521 hbk Phase 32 E3
    return new CompoundShortAxisDistanceMeasurement(owner); //260521 hbk Phase 32 E3
```

GetTypeNames 배열 — 마지막 원소 `"CompoundCenterBDistance"` 다음에 (콤마 처리 주의 — 기존 마지막 원소에 콤마 추가, 신규 원소 추가):
```csharp
"CompoundCenterBDistance", //260519 hbk Phase 31 D-11 E10
"CompoundShortAxisDistance" //260521 hbk Phase 32 E3
```

문자열 `"CompoundShortAxisDistance"` 는 Task 1 클래스의 `TypeName` getter 값과 **반드시 byte-identical**. 기존 13개 타입 등록은 수정하지 않는다 (회귀 0).

(B) DatumMeasurement.csproj — Measurements Compile Include 블록 (L219~233, `CompoundCenterBDistanceMeasurement.cs` 항목 L233 부근) 다음에 신규 항목 추가:
```xml
<Compile Include="Custom\Sequence\Inspection\Measurements\CompoundShortAxisDistanceMeasurement.cs" /> <!--260521 hbk Phase 32 E3-->
```
csproj 는 classic-style (packages.config) — Compile ItemGroup 에 명시 등록 필수 (SDK-style 자동 포함 아님). 누락 시 CS0246 빌드 차단.
  </action>
  <verify>
    <automated>grep -n "CompoundShortAxisDistance" WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs WPF_Example/DatumMeasurement.csproj</automated>
  </verify>
  <acceptance_criteria>
    - MeasurementFactory.cs Create switch 에 `case "CompoundShortAxisDistance":` 1건 + `return new CompoundShortAxisDistanceMeasurement(owner)` 1건
    - MeasurementFactory.cs GetTypeNames 배열에 `"CompoundShortAxisDistance"` 1건
    - GetTypeNames 배열의 콤마 문법 정상 (이전 마지막 원소에 콤마 추가됨)
    - csproj 에 `CompoundShortAxisDistanceMeasurement.cs` Compile Include 1건
    - 기존 13개 타입 등록 무수정 (`grep -c "case \"" MeasurementFactory.cs` 가 14)
  </acceptance_criteria>
  <done>MeasurementFactory 가 CompoundShortAxisDistance 를 인식하고, csproj 가 신규 파일을 빌드에 포함한다. FAIConfig Type 드롭다운에 자동 노출된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레시피 Type 문자열 → Factory | INI 레시피 Type= 값이 Factory 로 진입 — 로컬 레시피 파일, 미등록 타입은 null 반환 (기존 T-06-01 완화) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-08 | Denial of Service | TypeName 불일치 (클래스 getter ↔ Factory switch ↔ GetTypeNames) | mitigate | 3곳 모두 byte-identical `"CompoundShortAxisDistance"` 사용 — acceptance_criteria grep 으로 검증. 불일치 시 드롭다운 선택값이 Factory 에서 null → 측정 skip (크래시 아님) |
| T-32-09 | Denial of Service | csproj 미등록 → CS0246 | mitigate | csproj Compile Include 명시 추가 — classic-style 프로젝트 필수. acceptance_criteria 로 검증 |

본 plan 은 신규 측정 타입 + 팩토리 등록. 외부 신뢰 불가 입력 없음. high-severity 위협 없음.
</threat_model>

<verification>
- CompoundShortAxisDistanceMeasurement.cs 생성, MeasurementBase 상속, TypeName="CompoundShortAxisDistance"
- MeasurementFactory Create + GetTypeNames 양쪽 등록
- csproj Compile 항목 추가
- msbuild Debug/x64 PASS (Wave 2 전체 완료 후 — Plan 01 의 TryFindLargestContourRect 필요)
</verification>

<success_criteria>
- E3 신규 타입 CompoundShortAxisDistance 추가 — LargestRect 단축 폭 측정
- Factory 3곳(클래스 getter / switch / 배열) TypeName 일치
- csproj 등록 — 빌드 포함
- 공차는 레시피 INI 값 (클래스 default 미설정)
</success_criteria>

<output>
완료 후 `.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-05-SUMMARY.md` 생성.
</output>
