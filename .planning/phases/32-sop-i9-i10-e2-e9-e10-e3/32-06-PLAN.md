---
phase: 32-sop-i9-i10-e2-e9-e10-e3
plan: 06
type: execute
wave: 3
depends_on: ["03", "04", "05"]
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: false
requirements: []
must_haves:
  truths:
    - "E2/E3/E9/E10 측정 노드 선택 후 Rect ROI 버튼으로 단일 Rect ROI 를 티칭하면 Rect_* 필드에 좌표가 기록된다"
    - "ArcLineIntersect 측정 노드 선택 후 순차 2회 드로잉으로 EdgeA/EdgeB 2개 ROI 를 티칭할 수 있다"
    - "티칭된 ROI 가 캔버스에 렌더된다"
    - "재작성된 5종 + E3 측정이 SIMUL 이미지에서 측정값과 공차 판정을 표시한다 (UAT)"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "Phase 32 신규 측정 타입 ROI 티칭 배선"
      contains: "CompoundShortAxisDistanceMeasurement"
  key_links:
    - from: "CommitRectRoi"
      to: "Rect_* / EdgeA_* / EdgeB_* 측정 필드"
      via: "as 캐스트 분기로 ROI 좌표 write-back"
      pattern: "CompoundAngleMeasurement"
---

<objective>
재작성된 Phase 32 측정 타입 5종 + E3 신규 타입의 ROI 티칭을 MainView 에 배선하고, SIMUL 이미지로 전 타입을 UAT 검증한다.

목적: Plan 03/04/05 가 측정 클래스의 ROI 필드명을 변경했다 (ArcLineIntersect: Arc_P*/Line_* → EdgeA_*/EdgeB_*, Compound*: CL/La/Lb → Rect_*). MainView 의 ROI 티칭 코드(FindSelectedRectMeasurement / CommitRectRoi / BuildPointRoiDefinition)는 여전히 구 필드명을 참조하므로 새 필드명에 맞춰 배선해야 사용자가 ROI 를 그릴 수 있다. ArcLineIntersect 는 ROI 2개이므로 순차 2회 드로잉 UX 를 적용한다 (CONTEXT.md 미해결#2 — 순차 2회 드로잉 채택, Datum 위저드 미적용).
출력: MainView.xaml.cs ROI 티칭 배선 갱신 + 전 측정 타입 SIMUL UAT 사인오프.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-CONTEXT.md
@.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md

<interfaces>
<!-- MainView 기존 ROI 티칭 메서드 + Plan 03/04/05 가 정의한 측정 필드명. -->

MainView.xaml.cs 기존 ROI 티칭 메서드:
- `private MeasurementBase FindSelectedRectMeasurement()` (L1440~1469) — `is` 화이트리스트로 Rect ROI 보유 측정 타입 선별. 이미 ArcLineIntersect/CompoundAngle/CompoundCenterC/CompoundCenterB/ArcEdgeDistance 등록됨 (L1446~1450, L1460~1464).
- `private void CommitRectRoi()` (L1287~1348) — `_editingMeasurement != null` 분기에서 `as` 캐스트로 측정 타입별 ROI 필드 write-back. 현재 EdgeToLineDistance/EdgeToLineAngle/ArcEdgeDistance 의 `Point_*` 만 기록 (L1304~1309).
- `private static RoiDefinition BuildPointRoiDefinition(MeasurementBase m, string faiName)` (L222~242) — `as` 캐스트로 ROI 좌표 → RoiDefinition (캔버스 렌더용). 현재 EdgeToLineDistance/EdgeToLineAngle/ArcEdgeDistance 의 `Point_*` 만 처리.
- `_editingMeasurement` (필드 L53), `_editingMeasurementFaiName` (L55).
- `halconViewer.CommitActiveRectangle()` — 그린 사각형을 RoiDefinition (Row1/Col1/Row2/Col2) 으로 반환. mouse-up 시 `HalconViewer_RectDrawingCompleted` (L1282) → `CommitRectRoi()` 자동 호출.
- `halconViewer.StartRectangleDrawing()` — Rect 드로잉 모드 진입.

CommitRectRoi 의 측정 분기 패턴 (L1299~1309):
```csharp
double mCenterRow = (measRoi.Row1 + measRoi.Row2) / 2.0;
double mCenterCol = (measRoi.Column1 + measRoi.Column2) / 2.0;
double mHalfHeight = (measRoi.Row2 - measRoi.Row1) / 2.0;
double mHalfWidth = (measRoi.Column2 - measRoi.Column1) / 2.0;
var aed = _editingMeasurement as ArcEdgeDistanceMeasurement;
if (aed != null) { aed.Point_Row = mCenterRow; aed.Point_Col = mCenterCol; aed.Point_Phi = 0.0; aed.Point_Length1 = mHalfHeight; aed.Point_Length2 = mHalfWidth; }
```

Plan 03/04/05 가 정의한 측정 필드:
- ArcLineIntersectDistanceMeasurement: `EdgeA_Row/Col/Phi/Length1/Length2` + `EdgeB_Row/Col/Phi/Length1/Length2` (ROI 2개)
- CompoundAngleMeasurement / CompoundCenterCDistanceMeasurement / CompoundCenterBDistanceMeasurement / CompoundShortAxisDistanceMeasurement: `Rect_Row/Col/Phi/Length1/Length2` (ROI 1개)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: E2/E3/E9/E10 단일 Rect ROI 배선 + ArcLineIntersect 2-ROI 순차 드로잉</name>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (FindSelectedRectMeasurement L1440~1469, CommitRectRoi L1287~1348, BuildPointRoiDefinition L222~242, RectRoiButton_Click L1234~1279, HalconViewer_RectDrawingCompleted L1282~1285, _editingMeasurement/_editingMeasurementFaiName 필드 L53~55)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundAngleMeasurement.cs (Plan 04 재작성본 — Rect_* 필드명)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs (Plan 03 재작성본 — EdgeA_*/EdgeB_* 필드명)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-CONTEXT.md (미해결#2 — 순차 2회 드로잉 채택)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md (MainView.xaml.cs 섹션)
  </read_first>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <action>
MainView.xaml.cs 의 ROI 티칭 배선을 Phase 32 신규 측정 필드명에 맞춘다. 변경 라인에 `//260521 hbk Phase 32` 코멘트, 파일의 K&R 브레이스 스타일 유지.

**(A) FindSelectedRectMeasurement** (L1440~1469) — `is` 화이트리스트에 `CompoundShortAxisDistanceMeasurement` 추가 (E3 신규 타입). 2곳 모두 (L1450 부근 selParam 분기 + L1464 부근 m 분기):
```csharp
if (selParam is CompoundShortAxisDistanceMeasurement) return (MeasurementBase)selParam; //260521 hbk Phase 32 E3
// ... m 분기에도:
if (m is CompoundShortAxisDistanceMeasurement) return m; //260521 hbk Phase 32 E3
```
ArcLineIntersect/CompoundAngle/CompoundCenterC/CompoundCenterB 는 이미 화이트리스트에 있음 — 유지.

**(B) CommitRectRoi** (L1296~1318 측정 분기) — `as` 캐스트 분기를 신규 필드명에 맞춰 추가/교체한다. ArcEdgeDistance 의 Point_* 분기(L1308~1309)는 유지. 그 다음에 Compound 4종 (Rect_*) 분기 추가:
```csharp
//260521 hbk Phase 32 — Compound 4종 단일 Rect ROI write-back (Rect_* 필드명)
var cAngle = _editingMeasurement as CompoundAngleMeasurement; //260521 hbk Phase 32
if (cAngle != null) { cAngle.Rect_Row = mCenterRow; cAngle.Rect_Col = mCenterCol; cAngle.Rect_Phi = 0.0; cAngle.Rect_Length1 = mHalfHeight; cAngle.Rect_Length2 = mHalfWidth; }
var cCenterC = _editingMeasurement as CompoundCenterCDistanceMeasurement; //260521 hbk Phase 32
if (cCenterC != null) { cCenterC.Rect_Row = mCenterRow; cCenterC.Rect_Col = mCenterCol; cCenterC.Rect_Phi = 0.0; cCenterC.Rect_Length1 = mHalfHeight; cCenterC.Rect_Length2 = mHalfWidth; }
var cCenterB = _editingMeasurement as CompoundCenterBDistanceMeasurement; //260521 hbk Phase 32
if (cCenterB != null) { cCenterB.Rect_Row = mCenterRow; cCenterB.Rect_Col = mCenterCol; cCenterB.Rect_Phi = 0.0; cCenterB.Rect_Length1 = mHalfHeight; cCenterB.Rect_Length2 = mHalfWidth; }
var cShort = _editingMeasurement as CompoundShortAxisDistanceMeasurement; //260521 hbk Phase 32 E3
if (cShort != null) { cShort.Rect_Row = mCenterRow; cShort.Rect_Col = mCenterCol; cShort.Rect_Phi = 0.0; cShort.Rect_Length1 = mHalfHeight; cShort.Rect_Length2 = mHalfWidth; }
```

**(C) ArcLineIntersect 2-ROI 순차 드로잉** — CONTEXT.md 미해결#2 확정: 순차 2회 드로잉. 활성 ROI 인덱스 상태 필드 1개 추가 (L55 `_editingMeasurementFaiName` 근처):
```csharp
private int _editingMeasurementRoiIndex; //260521 hbk Phase 32 — ArcLineIntersect 2-ROI 순차 드로잉 인덱스 (0=EdgeA, 1=EdgeB)
```
- `RectRoiButton_Click` (L1242~1253) 의 measTarget != null 분기 진입 시 `_editingMeasurementRoiIndex = 0;` 으로 초기화. label_drawHint 를 ArcLineIntersect 일 때 "EdgeA(수직 에지) ROI 를 드래그하세요" 로 표시.
- `CommitRectRoi` 의 측정 분기에 ArcLineIntersect 전용 처리 추가 — `as ArcLineIntersectDistanceMeasurement` 캐스트 후, `_editingMeasurementRoiIndex == 0` 이면 EdgeA_* 에 기록 + 인덱스를 1 로 올리고 `halconViewer.StartRectangleDrawing()` + `RectDrawingCompleted` 재구독으로 두 번째 드로잉 재무장 (ExitCanvasMode 호출하지 않고 return), `_editingMeasurementRoiIndex == 1` 이면 EdgeB_* 에 기록 후 정상 종결 (ExitCanvasMode):
```csharp
var ali = _editingMeasurement as ArcLineIntersectDistanceMeasurement; //260521 hbk Phase 32
if (ali != null) {
    if (_editingMeasurementRoiIndex == 0) {
        ali.EdgeA_Row = mCenterRow; ali.EdgeA_Col = mCenterCol; ali.EdgeA_Phi = 0.0;
        ali.EdgeA_Length1 = mHalfHeight; ali.EdgeA_Length2 = mHalfWidth; //260521 hbk Phase 32
        _editingMeasurementRoiIndex = 1; //260521 hbk Phase 32 — 두 번째 ROI 로 진행
        label_drawHint.Content = "EdgeB(수평 에지) ROI 를 드래그하세요"; //260521 hbk Phase 32
        halconViewer.RectDrawingCompleted += HalconViewer_RectDrawingCompleted; //260521 hbk Phase 32 재무장
        halconViewer.StartRectangleDrawing(); //260521 hbk Phase 32
        return; //260521 hbk Phase 32 — ExitCanvasMode 미호출 (두 번째 드로잉 대기)
    }
    else {
        ali.EdgeB_Row = mCenterRow; ali.EdgeB_Col = mCenterCol; ali.EdgeB_Phi = 0.0;
        ali.EdgeB_Length1 = mHalfHeight; ali.EdgeB_Length2 = mHalfWidth; //260521 hbk Phase 32
    }
}
```
주의: `HalconViewer_RectDrawingCompleted` (L1282~1285) 는 mouse-up 시 `RectDrawingCompleted -= ...` 후 `CommitRectRoi()` 호출 — 위 재무장은 `+=` 재구독으로 두 번째 드로잉을 받게 한다. EdgeB 기록 후에는 기존 흐름대로 ExitCanvasMode 로 종결. ArcLineIntersect 가 아닌 측정(Compound 4종/Point 3종)은 인덱스 무관 — 1회 드로잉 후 정상 종결.

**(D) BuildPointRoiDefinition** (L222~242) — `as` 캐스트 분기 추가. Compound 4종은 Rect_* 사용, ArcLineIntersect 는 EdgeA_* 우선 표시 (캔버스 렌더용 — 첫 ROI 만 표시해도 충분, 또는 EdgeA/EdgeB 둘 다 표시 가능하나 메서드가 RoiDefinition 1개 반환이므로 EdgeA 우선):
```csharp
var cAngle = m as CompoundAngleMeasurement; //260521 hbk Phase 32
if (cAngle != null) { pRow = cAngle.Rect_Row; pCol = cAngle.Rect_Col; pLen1 = cAngle.Rect_Length1; pLen2 = cAngle.Rect_Length2; }
var cCenterC = m as CompoundCenterCDistanceMeasurement; //260521 hbk Phase 32
if (cCenterC != null) { pRow = cCenterC.Rect_Row; pCol = cCenterC.Rect_Col; pLen1 = cCenterC.Rect_Length1; pLen2 = cCenterC.Rect_Length2; }
var cCenterB = m as CompoundCenterBDistanceMeasurement; //260521 hbk Phase 32
if (cCenterB != null) { pRow = cCenterB.Rect_Row; pCol = cCenterB.Rect_Col; pLen1 = cCenterB.Rect_Length1; pLen2 = cCenterB.Rect_Length2; }
var cShort = m as CompoundShortAxisDistanceMeasurement; //260521 hbk Phase 32 E3
if (cShort != null) { pRow = cShort.Rect_Row; pCol = cShort.Rect_Col; pLen1 = cShort.Rect_Length1; pLen2 = cShort.Rect_Length2; }
var ali = m as ArcLineIntersectDistanceMeasurement; //260521 hbk Phase 32
if (ali != null) { pRow = ali.EdgeA_Row; pCol = ali.EdgeA_Col; pLen1 = ali.EdgeA_Length1; pLen2 = ali.EdgeA_Length2; }
```

C# 7.2 — switch expression / record 금지. 파일의 K&R 스타일 유지.
  </action>
  <verify>
    <automated>grep -n "CompoundShortAxisDistanceMeasurement\|EdgeA_Row\|_editingMeasurementRoiIndex\|cAngle.Rect_Row" WPF_Example/UI/ContentItem/MainView.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "CompoundShortAxisDistanceMeasurement" MainView.xaml.cs` 가 3건 이상 (FindSelectedRectMeasurement 2곳 + CommitRectRoi + BuildPointRoiDefinition)
    - `grep "_editingMeasurementRoiIndex" MainView.xaml.cs` 가 필드 선언 1건 + 사용 다수
    - CommitRectRoi 에 `cAngle.Rect_Row` / `cCenterC.Rect_Row` / `cCenterB.Rect_Row` / `cShort.Rect_Row` 각 1건
    - CommitRectRoi 에 `ali.EdgeA_Row` + `ali.EdgeB_Row` 각 1건
    - BuildPointRoiDefinition 에 Compound 4종 + ArcLineIntersect `as` 캐스트 분기 존재
    - msbuild Debug/x64 PASS — 신규 error 0
  </acceptance_criteria>
  <done>E2/E3/E9/E10 가 단일 Rect ROI 로, ArcLineIntersect 가 순차 2회 드로잉으로 EdgeA/EdgeB ROI 를 티칭할 수 있다. 티칭된 ROI 가 캔버스에 렌더된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 재작성 측정 5종 + E3 SIMUL UAT</name>
  <what-built>
    Phase 32 측정 알고리즘 SOP 재정합 — 전체:
    - VisionAlgorithmService 공통 컨투어 알고리즘 + intersection_lines / intersection_contours 래퍼 (Plan 01)
    - IDatumOriginConsumer 검출 원중심 주입 채널 (Plan 02)
    - ArcLineIntersect 2직선 교점 재작성 (Plan 03)
    - E2/E9/E10 공통 컨투어 알고리즘 재작성 (Plan 04)
    - E3 CompoundShortAxisDistance 신규 타입 (Plan 05)
    - MainView ROI 티칭 배선 — 단일 Rect + ArcLineIntersect 순차 2-ROI (Plan 06 Task 1)
  </what-built>
  <how-to-verify>
    1. 앱을 종료한 상태에서 msbuild Debug/x64 Rebuild — 빌드 PASS (0 errors, 신규 warning 0) 확인.
    2. 앱 실행 (SIMUL_MODE). FAIConfig Type 드롭다운에 `CompoundShortAxisDistance` 가 새로 보이는지 확인.
    3. SIMUL 이미지로 측정 타입별 검증 (각 타입에 측정 항목 1개씩 구성, ROI 티칭 후 측정 실행):
       - ArcLineIntersect: 측정 노드 선택 → Rect ROI 버튼 → 첫 드로잉(EdgeA 수직 에지) → drawHint 가 "EdgeB" 로 바뀜 → 두 번째 드로잉(EdgeB 수평 에지) → 측정 실행 → 교점 거리(mm) + 공차 판정 표시되는지.
       - E2 CompoundAngle: DatumC(CircleTwoHorizontal) 티칭 완료 상태에서 Rect ROI 1개 티칭 → 측정 → 각도(deg) 1개 + 판정 표시.
       - E3 CompoundShortAxisDistance: Rect ROI 1개 티칭 → 측정 → 단축 폭(mm, 공차 0.600±0.030 레시피 설정) + 판정 표시.
       - E9 CompoundCenterC / E10 CompoundCenterB: Rect ROI 1개 티칭 → 측정 → Datum C X / B Y 거리(mm) + 판정 표시.
    4. PropertyGrid 에서 E2/E3/E9/E10 선택 시 Contour 카테고리에 CannyAlpha/CannyLow/CannyHigh/UnionDistance 4 파라미터가 편집 가능하게 노출되는지 확인.
    5. 사각형 미검출/평행 직선 등 실패 케이스에서 측정값이 '—' 로 표시되고 앱이 크래시하지 않는지 확인.
  </how-to-verify>
  <resume-signal>각 측정 타입별 PASS/FAIL 을 기록하고 "approved" 또는 결함 내용을 알려주세요. FAIL 항목은 gap closure plan 으로 처리합니다.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 ROI 드로잉 → 측정 필드 | 캔버스 드로잉 좌표가 측정 객체 ROI 필드로 write-back — 로컬 UI 조작, 외부 입력 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-10 | Denial of Service | ArcLineIntersect 2-ROI 순차 드로잉 상태 누수 | mitigate | `_editingMeasurementRoiIndex` 를 RectRoiButton_Click 진입 시 0 으로 초기화 — 이전 미완료 티칭의 잔존 인덱스가 다음 티칭을 오염시키지 않음 |
| T-32-11 | Tampering | 잘못된 측정 타입에 ROI 기록 | accept | `as` 캐스트 분기 — 타입 불일치 시 해당 분기 null skip. 화이트리스트 외 타입은 _editingMeasurement 자체가 null (FindSelectedRectMeasurement) — 오기록 불가 |

본 plan 은 UI 배선 + UAT. 외부 신뢰 불가 입력 없음. high-severity 위협 없음.
</threat_model>

<verification>
- MainView ROI 티칭이 EdgeA_*/EdgeB_*/Rect_* 신규 필드명에 배선됨
- ArcLineIntersect 순차 2-ROI 드로잉 동작
- msbuild Debug/x64 PASS
- SIMUL UAT — 측정 6 타입(ArcLineIntersect/E2/E3/E9/E10) 측정값 + 판정 표시
</verification>

<success_criteria>
- 전 측정 타입 ROI 티칭 가능
- SIMUL UAT 사인오프 (각 타입 PASS, FAIL 시 gap closure)
- 빌드 PASS
</success_criteria>

<output>
완료 후 `.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-06-SUMMARY.md` 생성 + UAT 결과를 `.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-UAT.md` 에 기록.
</output>
