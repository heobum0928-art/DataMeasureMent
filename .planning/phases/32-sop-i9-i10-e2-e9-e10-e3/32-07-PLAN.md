---
phase: 32-sop-i9-i10-e2-e9-e10-e3
plan: 07
type: execute
wave: 4
depends_on: ["03", "04", "05"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundAngleMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterCDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterBDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundShortAxisDistanceMeasurement.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "ArcLineIntersect 측정 성공 시 2개의 피팅 에지 라인(FAI-Edge1/FAI-Edge2)과 교점 마커(FAI-Intersection)가 캔버스에 표시된다"
    - "CompoundAngle 측정 성공 시 LargestRect 중심 마커(FAI-Edge1)와 검출 원중심-중심 대각선(FAI-DiagLine)이 캔버스에 표시된다"
    - "CompoundCenterCDistance/CompoundCenterBDistance 측정 성공 시 LargestRect 중심 마커(FAI-Edge1)와 datum 수선 드롭선(FAI-DistLine)이 캔버스에 표시된다"
    - "CompoundShortAxisDistance 측정 성공 시 단축 폭 세그먼트(FAI-ShortAxis)가 캔버스에 표시된다"
    - "측정 실패(false 반환) 시 overlays 는 빈 리스트로 유지되어 캔버스가 잔상 없이 클리어된다"
    - "5종 모두 기존 수치 계산 로직이 변경되지 않아 측정값/공차 판정 결과가 동일하다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs"
      provides: "FAI-Edge1/FAI-Edge2(2개 피팅 라인) + FAI-Intersection(교점 마커) overlay 추가"
      contains: "FAI-Edge1"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundAngleMeasurement.cs"
      provides: "FAI-Edge1(LargestRect 중심 마커) + FAI-DiagLine(대각선) overlay 추가"
      contains: "FAI-DiagLine"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterCDistanceMeasurement.cs"
      provides: "FAI-Edge1(중심 마커) + FAI-DistLine(수선 드롭선) overlay 추가"
      contains: "FAI-DistLine"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterBDistanceMeasurement.cs"
      provides: "FAI-Edge1(중심 마커) + FAI-DistLine(수선 드롭선) overlay 추가"
      contains: "FAI-DistLine"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundShortAxisDistanceMeasurement.cs"
      provides: "FAI-ShortAxis(단축 폭 세그먼트) overlay 추가"
      contains: "FAI-ShortAxis"
  key_links:
    - from: "TryExecute return true 분기"
      to: "overlays 리스트"
      via: "overlays.Add(new EdgeInspectionOverlay { ... }) 호출"
      pattern: "overlays\\.Add"
    - from: "ArcLineIntersectDistanceMeasurement.TryExecute"
      to: "a1r1/a1c1/a1r2/a1c2, b1r1/b1c1/b1r2/b1c2, intRow/intCol"
      via: "TryFitLine/TryIntersectLines 가 이미 계산한 변수 재사용 — HALCON 재호출 없음"
      pattern: "a1r1.*FAI-Edge1"
    - from: "CompoundCenterC/BDistanceMeasurement.TryExecute"
      to: "centerRow/centerCol, footRow/footCol"
      via: "ComputeProjectionDistance foot-반환 오버로드로 교체 — 수치 결과 동일 보장"
      pattern: "out footRow, out footCol, out footOk"
---

<objective>
Phase 32 가 재작성/신설한 5개 측정 클래스의 TryExecute 에 결과 overlay 시각화를 추가한다.

목적: Plan 03/04/05 는 알고리즘 재작성에 집중했고 overlay 채움을 명시적으로 제외했다. 현재 5종 모두 `overlays = new List<EdgeInspectionOverlay>()` 초기화 후 빈 채로 return true 하므로 측정값은 나오지만 캔버스에 아무 것도 표시되지 않는다. 본 plan 은 각 측정 클래스가 이미 계산한 기하(피팅 라인 끝점, 교점, 중심점, 수선의 발)를 overlay 객체로 변환하는 코드만 추가한다. 알고리즘 로직은 전혀 건드리지 않는다.
출력: 5개 .cs 파일 — 각 TryExecute 의 `return true` 직전에 overlays.Add 블록 삽입. msbuild Debug/x64 PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-CONTEXT.md
@.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md

<interfaces>
<!-- EdgeInspectionOverlay 타입 (WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs) -->
```csharp
public class EdgeInspectionOverlay {
    public string RoiId { get; set; }
    public List<EdgeInspectionPoint> Points { get; set; } = new List<EdgeInspectionPoint>();
    public double LineRow1 { get; set; }
    public double LineColumn1 { get; set; }
    public double LineRow2 { get; set; }
    public double LineColumn2 { get; set; }
}
public class EdgeInspectionPoint {
    public double Row { get; set; }
    public double Column { get; set; }
}
```

<!-- RoiId 색상 분기 (HalconDisplayService 규칙, 기존 측정 클래스에서 검증됨) -->
- StartsWith("FAI-Edge") → 녹(PASS) / 적(FAIL) + Action_FAIMeasurement suffix(-OK/-NG) 자동 부여
- "FAI-DistLine"        → cyan(청록), suffix 미부여
- "FAI-Intersection"   → 미정의 RoiId → HalconDisplayService 기본 분기 처리 (흰색 또는 노랑 등). 교점 점 마커로 사용.
- "FAI-DiagLine"       → 위와 동일 (기본 분기). 대각선 라인으로 사용.
- "FAI-ShortAxis"      → 위와 동일 (기본 분기). 단축 폭 세그먼트로 사용.

<!-- VisionAlgorithmService: foot 반환 ComputeProjectionDistance 오버로드 -->
public static double ComputeProjectionDistance(
    double pointRow, double pointCol,
    double datumOriginRow, double datumOriginCol,
    double lineAngleRad,
    double pixelResolution,
    string measureAxis,
    out double footRow, out double footCol, out bool footOk);
// footOk=false 면 projection 실패. 반환값(거리mm)은 기존 단일 오버로드와 동일.

<!-- ArcLineIntersectDistanceMeasurement.TryExecute 현재 계산 변수 (재사용 대상) -->
// (1) TryFitLine 결과 — L110~120
double a1r1, a1c1, a1r2, a1c2;   // EdgeA 피팅 라인 끝점
// (2) TryFitLine 결과 — L123~133
double b1r1, b1c1, b1r2, b1c2;   // EdgeB 피팅 라인 끝점
// (3) TryIntersectLines 결과 — L136~144
double intRow, intCol;            // 두 직선 교점

<!-- CompoundAngleMeasurement.TryExecute 현재 계산 변수 (재사용 대상) -->
// (1) TryFindLargestContourRect 결과 — L101~109
double centerRow, centerCol, phi, len1, len2;  // LargestRect 중심 및 크기
// (2) DatumDetectedCircleRow/DatumDetectedCircleCol — L112 검증 후 사용 가능한 주입값

<!-- CompoundCenterC/BDistanceMeasurement.TryExecute 현재 계산 변수 (재사용 대상) -->
// (1) TryFindLargestContourRect 결과 — L108~116
double centerRow, centerCol, phi, len1, len2;  // LargestRect 중심 및 크기
// (2) resultValue = ComputeProjectionDistance(단일 오버로드) — 이를 foot 반환 오버로드로 교체

<!-- CompoundShortAxisDistanceMeasurement.TryExecute 현재 계산 변수 (재사용 대상) -->
// TryFindLargestContourRect 결과 — L56~63
double centerRow, centerCol, phi, length1, length2;
// shortHalf = Math.Min(length1, length2)  — L69
// 단축 방향 perpendicular: phiPerp = phi + Math.PI / 2.0
// 단축 세그먼트 양 끝점:
//   end1Row = centerRow - shortHalf * Math.Sin(phiPerp)
//   end1Col = centerCol - shortHalf * Math.Cos(phiPerp)
//   end2Row = centerRow + shortHalf * Math.Sin(phiPerp)
//   end2Col = centerCol + shortHalf * Math.Cos(phiPerp)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ArcLineIntersect overlay — FAI-Edge1/FAI-Edge2(피팅 라인) + FAI-Intersection(교점 마커)</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs (TryExecute 전체 — L95~156)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcEdgeDistanceMeasurement.cs (FAI-Edge1 + FAI-DistLine overlay 패턴 참조용)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs</files>
  <action>
ArcLineIntersectDistanceMeasurement.TryExecute 의 `return true;` (현재 L153) 직전에 overlay 생성 블록을 삽입한다. 수치 계산 로직(L107~152)은 일절 변경하지 않는다. 파일의 Allman 브레이스 스타일 유지. 변경 라인에 `//260521 hbk Phase 32 I9/I10-overlay` 주석.

삽입 위치: `resultValue = VisionAlgorithmService.ComputeProjectionDistance(...)` 호출 직후, `return true;` 직전.

삽입 내용:
```csharp
// overlay — 알고리즘이 이미 계산한 변수만 재사용. HALCON 재호출 없음. //260521 hbk Phase 32 I9/I10-overlay
// 1) FAI-Edge1 = EdgeA 피팅 라인 (HalconDisplayService 녹/적 분기 + Action_FAIMeasurement -OK/-NG suffix)
overlays.Add(new EdgeInspectionOverlay //260521 hbk Phase 32 I9/I10-overlay
{
    RoiId = "FAI-Edge1", //260521 hbk Phase 32 I9/I10-overlay
    LineRow1 = a1r1, LineColumn1 = a1c1, //260521 hbk Phase 32 I9/I10-overlay
    LineRow2 = a1r2, LineColumn2 = a1c2, //260521 hbk Phase 32 I9/I10-overlay
    Points = new List<EdgeInspectionPoint> //260521 hbk Phase 32 I9/I10-overlay
    {
        new EdgeInspectionPoint { Row = (a1r1 + a1r2) / 2.0, Column = (a1c1 + a1c2) / 2.0 } //260521 hbk Phase 32 I9/I10-overlay — EdgeA 중점
    }
}); //260521 hbk Phase 32 I9/I10-overlay
// 2) FAI-Edge2 = EdgeB 피팅 라인 (FAI-Edge로 시작하므로 동일 색상 분기 적용)
overlays.Add(new EdgeInspectionOverlay //260521 hbk Phase 32 I9/I10-overlay
{
    RoiId = "FAI-Edge2", //260521 hbk Phase 32 I9/I10-overlay
    LineRow1 = b1r1, LineColumn1 = b1c1, //260521 hbk Phase 32 I9/I10-overlay
    LineRow2 = b1r2, LineColumn2 = b1c2, //260521 hbk Phase 32 I9/I10-overlay
    Points = new List<EdgeInspectionPoint> //260521 hbk Phase 32 I9/I10-overlay
    {
        new EdgeInspectionPoint { Row = (b1r1 + b1r2) / 2.0, Column = (b1c1 + b1c2) / 2.0 } //260521 hbk Phase 32 I9/I10-overlay — EdgeB 중점
    }
}); //260521 hbk Phase 32 I9/I10-overlay
// 3) FAI-Intersection = 교점 마커 (점 마커: LineRow1==LineRow2, LineColumn1==LineColumn2)
overlays.Add(new EdgeInspectionOverlay //260521 hbk Phase 32 I9/I10-overlay
{
    RoiId = "FAI-Intersection", //260521 hbk Phase 32 I9/I10-overlay
    LineRow1 = intRow, LineColumn1 = intCol, //260521 hbk Phase 32 I9/I10-overlay
    LineRow2 = intRow, LineColumn2 = intCol, //260521 hbk Phase 32 I9/I10-overlay — 점 마커 (길이 0 라인)
    Points = new List<EdgeInspectionPoint> //260521 hbk Phase 32 I9/I10-overlay
    {
        new EdgeInspectionPoint { Row = intRow, Column = intCol } //260521 hbk Phase 32 I9/I10-overlay — 교점 X마커
    }
}); //260521 hbk Phase 32 I9/I10-overlay
```

주의사항:
- a1r1/a1c1/a1r2/a1c2, b1r1/b1c1/b1r2/b1c2, intRow/intCol 는 TryExecute 상단에서 이미 선언·계산된 변수 — 그대로 참조한다.
- 실패 경로(TryFitLine/TryIntersectLines false 반환 시 return false)는 변경 없음 — 빈 overlays 리스트 그대로.
- C# 7.2 — switch expression / record 금지.
  </action>
  <verify>
    <automated>findstr /n "FAI-Edge1\|FAI-Edge2\|FAI-Intersection\|I9/I10-overlay" "WPF_Example\Custom\Sequence\Inspection\Measurements\ArcLineIntersectDistanceMeasurement.cs"</automated>
  </verify>
  <done>
    - `FAI-Edge1`, `FAI-Edge2`, `FAI-Intersection` 3개 RoiId 가 파일 내 존재
    - overlay 블록이 `return true;` 직전에 위치 (수치 계산 라인 순서 불변)
    - 파일 내 `260521 hbk Phase 32 I9/I10-overlay` 주석 다수
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CompoundAngle overlay — FAI-Edge1(LargestRect 중심 마커) + FAI-DiagLine(대각선)</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundAngleMeasurement.cs (TryExecute 전체 — L86~133)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundAngleMeasurement.cs</files>
  <action>
CompoundAngleMeasurement.TryExecute 의 `return true;` (현재 L131) 직전에 overlay 생성 블록을 삽입한다. 수치 계산 로직(L98~130)은 일절 변경하지 않는다. 파일의 Allman 스타일 유지. 변경 라인에 `//260521 hbk Phase 32 E2-overlay` 주석.

삽입 위치: `resultValue = VisionAlgorithmService.AngleLineLine(...)` 호출 직후, `return true;` 직전.

삽입 내용:
```csharp
// overlay — TryFindLargestContourRect/DatumDetectedCircle* 가 이미 계산한 변수 재사용. //260521 hbk Phase 32 E2-overlay
// 1) FAI-Edge1 = LargestRect 중심 마커 (점 마커: LineRow1==LineRow2)
overlays.Add(new EdgeInspectionOverlay //260521 hbk Phase 32 E2-overlay
{
    RoiId = "FAI-Edge1", //260521 hbk Phase 32 E2-overlay — HalconDisplayService 녹/적 분기 + Action_FAIMeasurement suffix
    LineRow1 = centerRow, LineColumn1 = centerCol, //260521 hbk Phase 32 E2-overlay
    LineRow2 = centerRow, LineColumn2 = centerCol, //260521 hbk Phase 32 E2-overlay — 점 마커
    Points = new List<EdgeInspectionPoint> //260521 hbk Phase 32 E2-overlay
    {
        new EdgeInspectionPoint { Row = centerRow, Column = centerCol } //260521 hbk Phase 32 E2-overlay
    }
}); //260521 hbk Phase 32 E2-overlay
// 2) FAI-DiagLine = 대각선 (LargestRect 중심 ↔ DatumC 검출 원중심). AngleLineLine 의 첫 번째 라인과 동일.
overlays.Add(new EdgeInspectionOverlay //260521 hbk Phase 32 E2-overlay
{
    RoiId = "FAI-DiagLine", //260521 hbk Phase 32 E2-overlay
    LineRow1 = centerRow, LineColumn1 = centerCol, //260521 hbk Phase 32 E2-overlay — LargestRect 중심
    LineRow2 = DatumDetectedCircleRow, LineColumn2 = DatumDetectedCircleCol, //260521 hbk Phase 32 E2-overlay — DatumC 검출 원중심
    Points = new List<EdgeInspectionPoint> //260521 hbk Phase 32 E2-overlay — 양 끝점 X마커
    {
        new EdgeInspectionPoint { Row = centerRow, Column = centerCol }, //260521 hbk Phase 32 E2-overlay
        new EdgeInspectionPoint { Row = DatumDetectedCircleRow, Column = DatumDetectedCircleCol } //260521 hbk Phase 32 E2-overlay
    }
}); //260521 hbk Phase 32 E2-overlay
```

주의사항:
- `centerRow`, `centerCol` 는 TryFindLargestContourRect out 결과 (L101). `DatumDetectedCircleRow/Col` 는 인터페이스 프로퍼티 (L78/82) — DatumDetectedCircle 검증(L112) 이 이미 통과한 이후이므로 0,0 아님이 보장된다.
- `daR1/daC1/daR2/daC2` (DatumB 기준선 2점) 는 overlay 에 포함하지 않는다 — 대각선만으로 각도 시각화 충분. 판단 기준: EdgeToLineAngleMeasurement 는 에지 라인 1개만 표시하는 패턴.
  </action>
  <verify>
    <automated>findstr /n "FAI-Edge1\|FAI-DiagLine\|E2-overlay" "WPF_Example\Custom\Sequence\Inspection\Measurements\CompoundAngleMeasurement.cs"</automated>
  </verify>
  <done>
    - `FAI-Edge1`, `FAI-DiagLine` 2개 RoiId 가 파일 내 존재
    - overlay 블록이 `return true;` 직전에 위치
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: CompoundCenterC/B overlay — FAI-Edge1(중심 마커) + FAI-DistLine(수선 드롭선)</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterCDistanceMeasurement.cs (TryExecute — L93~127)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterBDistanceMeasurement.cs (TryExecute — L93~127)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcEdgeDistanceMeasurement.cs (FAI-Edge1+FAI-DistLine foot 오버로드 사용 패턴 — L113~200)
  </read_first>
  <files>
    WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterCDistanceMeasurement.cs,
    WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundCenterBDistanceMeasurement.cs
  </files>
  <action>
두 파일 모두 동일한 변경을 적용한다 (TypeName/MeasureAxis 기본값만 다른 쌍둥이). 파일의 Allman 스타일 유지.

**CompoundCenterCDistanceMeasurement.cs** — 변경 라인에 `//260521 hbk Phase 32 E9-overlay`:
**CompoundCenterBDistanceMeasurement.cs** — 변경 라인에 `//260521 hbk Phase 32 E10-overlay`:

각 파일의 TryExecute 에서:

**(A) ComputeProjectionDistance 호출 교체** (현재 L119~123 / L119~123):
기존의 단일 오버로드 호출을 foot 반환 오버로드로 교체해 `footRow`/`footCol`/`footOk` 를 획득한다. 수치 결과(`resultValue`)는 동일하다.

기존:
```csharp
double measureLineAngle = (MeasureAxis == "X") ? DatumAngle2Rad : DatumAngleRad;
resultValue = VisionAlgorithmService.ComputeProjectionDistance(
    centerRow, centerCol,
    DatumOriginRow, DatumOriginCol, measureLineAngle,
    pixelResolution, MeasureAxis);
```

교체 후 (CompoundCenterC 주석 예시 — CompoundCenterB 는 E9 → E10 으로 변경):
```csharp
double measureLineAngle = (MeasureAxis == "X") ? DatumAngle2Rad : DatumAngleRad; //260521 hbk Phase 32 E9-overlay — 유지
double footRow, footCol; //260521 hbk Phase 32 E9-overlay — FAI-DistLine 수선의 발 좌표
bool footOk; //260521 hbk Phase 32 E9-overlay
resultValue = VisionAlgorithmService.ComputeProjectionDistance(
    centerRow, centerCol,
    DatumOriginRow, DatumOriginCol, measureLineAngle,
    pixelResolution, MeasureAxis,
    out footRow, out footCol, out footOk); //260521 hbk Phase 32 E9-overlay — foot 반환 오버로드로 교체 (수치 결과 동일)
```

**(B) overlay 블록 삽입** — ComputeProjectionDistance 호출 직후, `return true;` 직전:
```csharp
// overlay — 이미 계산한 변수만 재사용. HALCON 재호출 없음. //260521 hbk Phase 32 E9-overlay
// 1) FAI-Edge1 = LargestRect 중심 점 마커
overlays.Add(new EdgeInspectionOverlay //260521 hbk Phase 32 E9-overlay
{
    RoiId = "FAI-Edge1", //260521 hbk Phase 32 E9-overlay — HalconDisplayService 녹/적 분기 + Action_FAIMeasurement suffix
    LineRow1 = centerRow, LineColumn1 = centerCol, //260521 hbk Phase 32 E9-overlay
    LineRow2 = centerRow, LineColumn2 = centerCol, //260521 hbk Phase 32 E9-overlay — 점 마커
    Points = new List<EdgeInspectionPoint> //260521 hbk Phase 32 E9-overlay
    {
        new EdgeInspectionPoint { Row = centerRow, Column = centerCol } //260521 hbk Phase 32 E9-overlay
    }
}); //260521 hbk Phase 32 E9-overlay
// 2) FAI-DistLine = LargestRect 중심 → datum 기준선 수선의 발 (수직 드롭선, cyan)
if (footOk) //260521 hbk Phase 32 E9-overlay — projection 실패 시 수치 0 이지만 라인은 skip
{
    overlays.Add(new EdgeInspectionOverlay //260521 hbk Phase 32 E9-overlay
    {
        RoiId = "FAI-DistLine", //260521 hbk Phase 32 E9-overlay — HalconDisplayService cyan 분기
        LineRow1 = footRow, LineColumn1 = footCol, //260521 hbk Phase 32 E9-overlay — 수선의 발 (datum 기준선 위)
        LineRow2 = centerRow, LineColumn2 = centerCol, //260521 hbk Phase 32 E9-overlay — LargestRect 중심
        Points = new List<EdgeInspectionPoint> //260521 hbk Phase 32 E9-overlay — 양 끝점 X마커
        {
            new EdgeInspectionPoint { Row = footRow, Column = footCol }, //260521 hbk Phase 32 E9-overlay
            new EdgeInspectionPoint { Row = centerRow, Column = centerCol } //260521 hbk Phase 32 E9-overlay
        }
    }); //260521 hbk Phase 32 E9-overlay
} //260521 hbk Phase 32 E9-overlay
```

CompoundCenterBDistanceMeasurement.cs 에는 동일 블록을 삽입하되 주석을 `E9-overlay` → `E10-overlay` 로 변경한다.
  </action>
  <verify>
    <automated>findstr /n "FAI-Edge1\|FAI-DistLine\|E9-overlay" "WPF_Example\Custom\Sequence\Inspection\Measurements\CompoundCenterCDistanceMeasurement.cs" && findstr /n "FAI-Edge1\|FAI-DistLine\|E10-overlay" "WPF_Example\Custom\Sequence\Inspection\Measurements\CompoundCenterBDistanceMeasurement.cs"</automated>
  </verify>
  <done>
    - 두 파일 모두 `FAI-Edge1`, `FAI-DistLine`, `footRow`, `footOk` 가 존재
    - ComputeProjectionDistance 가 foot 반환 오버로드(`out footRow, out footCol, out footOk`)로 교체됨
    - overlay 블록이 `return true;` 직전, `if (footOk)` 가드로 감싸진 FAI-DistLine 포함
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: CompoundShortAxis overlay — FAI-ShortAxis(단축 폭 세그먼트)</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundShortAxisDistanceMeasurement.cs (TryExecute 전체 — L40~76)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundShortAxisDistanceMeasurement.cs</files>
  <action>
CompoundShortAxisDistanceMeasurement.TryExecute 의 `return true;` (현재 L73) 직전에 overlay 생성 블록을 삽입한다. 수치 계산 로직(L56~72)은 일절 변경하지 않는다. 파일의 Allman 스타일 유지. 변경 라인에 `//260521 hbk Phase 32 E3-overlay` 주석.

삽입 위치: `resultValue = shortAxisWidthPx * pixelResolution;` 직후, `return true;` 직전.

삽입 내용:
```csharp
// overlay — 단축 폭 세그먼트. TryFindLargestContourRect 결과 변수 재사용. HALCON 재호출 없음. //260521 hbk Phase 32 E3-overlay
// 단축 방향 법선각도: phi 는 장축 방향(rad), 단축은 phi + π/2
double phiPerp = phi + System.Math.PI / 2.0; //260521 hbk Phase 32 E3-overlay — 단축 방향 각도
double sinPerp = System.Math.Sin(phiPerp); //260521 hbk Phase 32 E3-overlay
double cosPerp = System.Math.Cos(phiPerp); //260521 hbk Phase 32 E3-overlay
// 단축 세그먼트 양 끝점 = 중심 ± shortHalf × 단축방향
double sEnd1Row = centerRow - shortHalf * sinPerp; //260521 hbk Phase 32 E3-overlay
double sEnd1Col = centerCol - shortHalf * cosPerp; //260521 hbk Phase 32 E3-overlay
double sEnd2Row = centerRow + shortHalf * sinPerp; //260521 hbk Phase 32 E3-overlay
double sEnd2Col = centerCol + shortHalf * cosPerp; //260521 hbk Phase 32 E3-overlay
// FAI-ShortAxis = 단축 폭 세그먼트 (양 끝 X마커 포함)
overlays.Add(new EdgeInspectionOverlay //260521 hbk Phase 32 E3-overlay
{
    RoiId = "FAI-ShortAxis", //260521 hbk Phase 32 E3-overlay — 기본 HalconDisplayService 분기 (라인 + 점 마커)
    LineRow1 = sEnd1Row, LineColumn1 = sEnd1Col, //260521 hbk Phase 32 E3-overlay — 단축 끝점 1
    LineRow2 = sEnd2Row, LineColumn2 = sEnd2Col, //260521 hbk Phase 32 E3-overlay — 단축 끝점 2
    Points = new List<EdgeInspectionPoint> //260521 hbk Phase 32 E3-overlay — 양 끝점 X마커
    {
        new EdgeInspectionPoint { Row = sEnd1Row, Column = sEnd1Col }, //260521 hbk Phase 32 E3-overlay
        new EdgeInspectionPoint { Row = sEnd2Row, Column = sEnd2Col } //260521 hbk Phase 32 E3-overlay
    }
}); //260521 hbk Phase 32 E3-overlay
```

주의사항:
- `centerRow`, `centerCol`, `phi`, `length1`, `length2` 는 TryFindLargestContourRect out 결과 (L56~63) — 이미 선언된 변수.
- `shortHalf = System.Math.Min(length1, length2)` 는 L69 에서 이미 선언됨 — 재선언 금지, 그대로 참조.
- HALCON smallest_rectangle2_xld 의 `phi` 는 장축 방향각(rad). 단축 방향 = phi + π/2. 이 계산은 순수 C# Math — HALCON 연산자 호출 없음, try/catch 불필요.
  </action>
  <verify>
    <automated>findstr /n "FAI-ShortAxis\|phiPerp\|E3-overlay" "WPF_Example\Custom\Sequence\Inspection\Measurements\CompoundShortAxisDistanceMeasurement.cs"</automated>
  </verify>
  <done>
    - `FAI-ShortAxis`, `phiPerp`, `sEnd1Row`, `sEnd2Row` 가 파일 내 존재
    - overlay 블록이 `resultValue = shortAxisWidthPx * pixelResolution;` 직후, `return true;` 직전에 위치
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: 빌드 검증 — msbuild Debug/x64</name>
  <files></files>
  <action>
Task 1~4 완료 후 msbuild 로 Debug/x64 빌드를 실행해 신규 컴파일 오류가 없음을 확인한다.

```powershell
& "C:\Program Files\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" `
    "C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj" `
    /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal 2>&1 | Select-String "error|warning|Build succeeded"
```

MSBuild 경로가 없으면 아래 대안 사용:
```powershell
& "C:\Windows\Microsoft.NET\Framework64\v4.0.30319\MSBuild.exe" `
    "C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj" `
    /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal
```

빌드 실패 시 오류 메시지를 분석하고 해당 파일만 수정한 뒤 재빌드한다. 일반적 예상 오류:
- `footRow` 미선언: CompoundCenterC/B 에서 foot 오버로드로 교체 미완료 → Task 3 재확인.
- `shortHalf` 재선언 오류: Task 4 에서 L69 의 기존 선언과 충돌 → `double shortHalf =` 를 `shortHalf =` 로 수정.
- `phiPerp` 미선언: Task 4 삽입 블록이 TryExecute 스코프 밖에 삽입된 경우 → 삽입 위치 확인.
  </action>
  <verify>
    <automated>빌드 출력에 "Build succeeded" 포함, "error" 0건</automated>
  </verify>
  <done>
    - msbuild Debug/x64 "Build succeeded" 출력 확인
    - 신규 컴파일 오류 0건 (기존 warning 은 허용)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TryExecute 내부 계산값 → EdgeInspectionOverlay | 측정 알고리즘이 산출한 image 좌표(row/col)를 overlay 객체에 기록 — 외부 입력 없음, 로컬 계산값만 사용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-12 | Denial of Service | CompoundCenterC/B: foot 반환 오버로드 예외 | accept | VisionAlgorithmService.ComputeProjectionDistance 내부가 이미 try/catch 처리 (footOk=false 반환) — footOk 가드로 overlay skip. 수치 결과 0은 기존 동작과 동일. |
| T-32-13 | Tampering | shortHalf 재선언 충돌 | mitigate | Task 4 에서 기존 L69 `double shortHalf =` 선언 이후에 삽입 — `double` 키워드 없이 참조. Task 5 빌드 검증으로 확인. |

본 plan 은 순수 결과 시각화 — 수치 계산 무변경, 외부 신뢰 불가 입력 없음. high-severity 위협 없음.
</threat_model>

<verification>
- 5개 파일 각각 지정된 RoiId 가 포함됨
- 5개 파일 모두 `return true;` 직전에 overlays.Add 블록이 존재함
- 실패 경로(return false 앞)에는 overlay 추가 없음 — 빈 리스트 유지
- CompoundCenterC/B 의 ComputeProjectionDistance 가 foot 반환 오버로드로 교체됨
- msbuild Debug/x64 Build succeeded, 신규 오류 0건
</verification>

<success_criteria>
- ArcLineIntersect: `FAI-Edge1`, `FAI-Edge2`, `FAI-Intersection` 3개 overlay 생성
- CompoundAngle: `FAI-Edge1`, `FAI-DiagLine` 2개 overlay 생성
- CompoundCenterC: `FAI-Edge1`, `FAI-DistLine` 2개 overlay 생성 (footOk=true 시)
- CompoundCenterB: 동일
- CompoundShortAxis: `FAI-ShortAxis` 1개 overlay 생성
- 빌드 PASS
- 알고리즘 수치 계산 결과 무변경 (기존 `return true` 경로의 `resultValue` 동일)
</success_criteria>

<output>
완료 후 `.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-07-SUMMARY.md` 생성.
</output>
