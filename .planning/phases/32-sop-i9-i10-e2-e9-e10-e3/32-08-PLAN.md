---
phase: 32-sop-i9-i10-e2-e9-e10-e3
plan: 08
type: execute
wave: 5
depends_on: ["03", "06", "07"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "ArcLineIntersectDistanceMeasurement 에 4개의 ROI 필드 세트(EdgeA1/EdgeB1/EdgeA2/EdgeB2) 와 4개의 Edge 파라미터 그룹이 존재한다"
    - "TryExecute 가 4개 ROI 에서 직선 4개를 피팅하고, 교점1(A1∩B1)과 교점2(A2∩B2) 를 산출하고, 두 교점의 평균점을 계산한 뒤 Datum C X 방향 거리를 반환한다"
    - "어느 ROI 피팅이라도 실패하거나 교점 산출이 실패(평행/근접)하면 false 를 반환하고 측정값이 '—' 로 표시된다"
    - "TryExecute 성공 시 교점1·2의 2개 피팅 라인 세트(4개 FAI-Edge* overlay), 교점1·2 마커(2개 FAI-Intersection* overlay), 평균점 마커(1개 FAI-AvgPoint overlay), Datum 거리선(1개 FAI-DistLine overlay) 이 overlay 리스트에 추가된다"
    - "MainView 의 ArcLineIntersect 순차 ROI 드로잉이 4회(EdgeA1→EdgeB1→EdgeA2→EdgeB2)로 확장되고 drawHint 가 각 단계에 맞게 바뀐다"
    - "BuildPointRoiDefinitions 의 ArcLineIntersect 분기가 4개 ROI 를 개별 렌더한다"
    - "grep 으로 구 필드명(EdgeA_Row / EdgeB_Row) 이 측정 클래스 내에 더 이상 없음을 확인한다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs"
      provides: "4-ROI 재설계: EdgeA1/EdgeB1/EdgeA2/EdgeB2 ROI+Edge 그룹, 2교점 평균 TryExecute, overlay 8개"
      contains: "EdgeA1_Row"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "4-ROI 순차 드로잉: _editingMeasurementRoiIndex 0→3 확장, drawHint 4단계, BuildPointRoiDefinitions 4 ROI"
      contains: "EdgeA2_Row"
  key_links:
    - from: "ArcLineIntersectDistanceMeasurement.TryExecute"
      to: "VisionAlgorithmService.TryIntersectLines (2회 호출)"
      via: "교점1 = TryIntersectLines(a1*/b1*), 교점2 = TryIntersectLines(a2*/b2*)"
      pattern: "TryIntersectLines"
    - from: "CommitRectRoi"
      to: "EdgeA1_*/EdgeB1_*/EdgeA2_*/EdgeB2_* 필드"
      via: "_editingMeasurementRoiIndex 0/1/2/3 분기"
      pattern: "_editingMeasurementRoiIndex"
---

<objective>
ArcLineIntersectDistanceMeasurement 를 UAT 에서 확정된 설계(4-ROI / 교점 2개 평균)로 전면 재작성하고, MainView 의 ROI 티칭을 4회 순차 드로잉으로 확장한다.

목적: Plan 32-03 이 구현한 2-ROI / 단일 교점 설계는 SOP 실무 요건(I9/I10 측정 = 두 교점의 평균)과 불일치함이 UAT 에서 확인됐다. 교점을 하나만 쓰면 샷마다 두 교점의 위치 편차가 결과에 그대로 반영돼 측정 재현성이 낮아진다. 본 plan 은 4개 ROI(교점1용 A1/B1, 교점2용 A2/B2)에서 각각 직선을 피팅하고, 두 교점의 평균점을 최종 측정 기준점으로 삼는 설계로 교체한다.
출력: ArcLineIntersectDistanceMeasurement.cs (4-ROI 전면 재작성), MainView.xaml.cs (4-ROI 티칭 확장).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-CONTEXT.md
@.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md

<interfaces>
<!-- VisionAlgorithmService 시그니처 (변경 없음, 그대로 사용) -->

```csharp
// TryFitLine — 기존 시그니처 (VisionAlgorithmService.cs)
public bool TryFitLine(
    HImage image,
    double roiRow, double roiCol, double roiPhi, double roiLength1, double roiLength2,
    HTuple datumTransform,
    int sampleCount, int trimCount, double sigma, int threshold,
    string direction, string polarity,
    out double r1, out double c1, out double r2, out double c2, out string error,
    string selection = "All");

// TryIntersectLines — 기존 시그니처 (VisionAlgorithmService.cs L926~)
public static bool TryIntersectLines(
    double row1a, double col1a, double row1b, double col1b,
    double row2a, double col2a, double row2b, double col2b,
    out double intRow, out double intCol);

// ComputeProjectionDistance — foot 반환 오버로드 (VisionAlgorithmService.cs)
public static double ComputeProjectionDistance(
    double pointRow, double pointCol,
    double datumOriginRow, double datumOriginCol,
    double lineAngleRad,
    double pixelResolution,
    string measureAxis,
    out double footRow, out double footCol, out bool footOk);
```

<!-- EdgeInspectionOverlay 타입 (32-07-PLAN.md <interfaces> 에서 확인됨) -->
```csharp
public class EdgeInspectionOverlay {
    public string RoiId { get; set; }
    public List<EdgeInspectionPoint> Points { get; set; } = new List<EdgeInspectionPoint>();
    public double LineRow1 { get; set; }
    public double LineColumn1 { get; set; }
    public double LineRow2 { get; set; }
    public double LineColumn2 { get; set; }
}
public class EdgeInspectionPoint { public double Row { get; set; } public double Column { get; set; } }
```

<!-- IDatumOriginConsumer 인터페이스 필드 (현재 구현 클래스 L87~111) -->
DatumOriginRow, DatumOriginCol, DatumAngleRad, DatumAngle2Rad,
DatumDetectedCircleRow, DatumDetectedCircleCol
— 각각 3중 attribute: [System.ComponentModel.Browsable(false)] / [PropertyTools.DataAnnotations.Browsable(false)] / [Newtonsoft.Json.JsonIgnore]

<!-- MainView 현재 ArcLineIntersect 관련 필드/메서드 (MainView.xaml.cs 에서 확인됨) -->
- _editingMeasurementRoiIndex (L56) — 현재 0=EdgeA / 1=EdgeB; 확장 후 0=EdgeA1 / 1=EdgeB1 / 2=EdgeA2 / 3=EdgeB2
- CommitRectRoi (L1353~) — ali 분기 L1386~1401 (EdgeA 기록 후 index=1, EdgeB 기록 후 ExitCanvasMode)
- BuildPointRoiDefinitions (L235~) — ali 분기 L241~266 (EdgeA + EdgeB 2개 RoiDefinition)
- RectRoiButton_Click (L1295~) — measTarget is ArcLineIntersect → label_drawHint 설정 L1310~1313
- HalconViewer_RectDrawingCompleted (L1348~1350) — mouse-up → CommitRectRoi 호출
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ArcLineIntersectDistanceMeasurement 4-ROI 전면 재작성 (ROI 필드 + Edge 파라미터 + TryExecute + overlay)</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs (전체 — 현재 L1~213)
    - .planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-PATTERNS.md (ArcLineIntersect 섹션 + Shared Patterns)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs</files>
  <action>
파일을 전면 재작성한다. INI 하위호환 제약 없음 (Phase 32 사전-릴리스, CONTEXT.md 미해결#5 확정).
파일 상단 주석 `//260521 hbk Phase 32 — I9/I10-redesign: 4-ROI 두 교점 평균 방식` 으로 갱신.
Allman 브레이스 스타일 유지, C# 7.2 only.

**[A] 제거: 기존 EdgeA_*/EdgeB_* ROI + Edge 필드 전체** (현재 L22~67).

**[B] 신규 ROI 필드 4벌** — `EdgeA1_*/EdgeB1_*/EdgeA2_*/EdgeB2_*`, 각 5필드(Row/Col/Phi/Length1/Length2).
PropertyGrid 카테고리를 교점 단위로 구분:
```csharp
//260521 hbk Phase 32 I9/I10-redesign — 교점1 ROI 필드
[Category("교점1|EdgeA1-ROI")]
public double EdgeA1_Row { get; set; }
public double EdgeA1_Col { get; set; }
public double EdgeA1_Phi { get; set; }
public double EdgeA1_Length1 { get; set; }
public double EdgeA1_Length2 { get; set; }

[Category("교점1|EdgeB1-ROI")]
public double EdgeB1_Row { get; set; }
public double EdgeB1_Col { get; set; }
public double EdgeB1_Phi { get; set; }
public double EdgeB1_Length1 { get; set; }
public double EdgeB1_Length2 { get; set; }

[Category("교점2|EdgeA2-ROI")]
public double EdgeA2_Row { get; set; }
public double EdgeA2_Col { get; set; }
public double EdgeA2_Phi { get; set; }
public double EdgeA2_Length1 { get; set; }
public double EdgeA2_Length2 { get; set; }

[Category("교점2|EdgeB2-ROI")]
public double EdgeB2_Row { get; set; }
public double EdgeB2_Col { get; set; }
public double EdgeB2_Phi { get; set; }
public double EdgeB2_Length1 { get; set; }
public double EdgeB2_Length2 { get; set; }
```

**[C] 신규 Edge 파라미터 4그룹** — EdgeA1/EdgeB1/EdgeA2/EdgeB2, 각 7필드(Threshold/Sigma/SampleCount/TrimCount/Polarity/Direction/Selection).
카테고리: `[Category("교점1|EdgeA1-Edge")]`, `[Category("교점1|EdgeB1-Edge")]`, `[Category("교점2|EdgeA2-Edge")]`, `[Category("교점2|EdgeB2-Edge")]`.
기본값은 기존 EdgeA/EdgeB 와 동일(Threshold=10, Sigma=1.0, SampleCount=20, TrimCount=10, Polarity="DarkToLight", 수직에지Direction="LtoR", 수평에지Direction="TtoB", Selection="All").
EdgeA1/EdgeA2 는 수직 에지 → Direction 기본 "LtoR", EdgeB1/EdgeB2 는 수평 에지 → Direction 기본 "TtoB".
각 그룹의 Polarity/Direction/Selection 에 `[ItemsSourceProperty(nameof(EdgePolarityList))]` 등 ComboBox 래퍼 적용.

**[D] ComboBox 래퍼 리스트** — 기존 `EdgeDirectionList`/`EdgePolarityList`/`EdgeSelectionList` 3개 유지 (4그룹 공유).

**[E] MeasureAxis / IDatumOriginConsumer transient 필드** — 기존 내용 그대로 유지.

**[F] TryExecute 재작성:**
```
(1) EdgeA1 ROI → TryFitLine → a1r1, a1c1, a1r2, a1c2  (실패 시 return false)
(2) EdgeB1 ROI → TryFitLine → b1r1, b1c1, b1r2, b1c2  (실패 시 return false)
(3) EdgeA2 ROI → TryFitLine → a2r1, a2c1, a2r2, a2c2  (실패 시 return false)
(4) EdgeB2 ROI → TryFitLine → b2r1, b2c1, b2r2, b2c2  (실패 시 return false)

(5) 교점1 = TryIntersectLines(a1*, b1*) → int1Row, int1Col  (실패: "교점1 산출 실패" return false)
(6) 교점2 = TryIntersectLines(a2*, b2*) → int2Row, int2Col  (실패: "교점2 산출 실패" return false)

(7) 평균점:
    double avgRow = (int1Row + int2Row) / 2.0;  //260521 hbk Phase 32 I9/I10-redesign
    double avgCol = (int1Col + int2Col) / 2.0;

(8) Datum 거리 (foot 반환 오버로드 사용 — overlay FAI-DistLine 용):
    double measureLineAngle = (MeasureAxis == "X") ? DatumAngle2Rad : DatumAngleRad;
    double footRow, footCol; bool footOk;
    resultValue = VisionAlgorithmService.ComputeProjectionDistance(
        avgRow, avgCol,
        DatumOriginRow, DatumOriginCol, measureLineAngle,
        pixelResolution, MeasureAxis,
        out footRow, out footCol, out footOk);
```

각 TryFitLine 호출은 해당 그룹의 독립 파라미터(EdgeA1_SampleCount 등)를 사용한다.
EdgeSelection 은 각 그룹의 `EdgeA1_Selection` 등 사용자 선택값 전달(기본 "All").
모든 `HOperatorSet.*` 는 try/catch 로 감싸져 있음 — TryFitLine 내부에서 처리되므로 외부 추가 불필요.

**[G] overlay — 알고리즘이 이미 계산한 변수만 재사용, HALCON 재호출 없음:**
```
// 교점1 에지 라인 2개
overlays.Add(RoiId="FAI-Edge1", LineRow1=a1r1, LineColumn1=a1c1, LineRow2=a1r2, LineColumn2=a1c2,
             Points=[중점])   //260521 hbk Phase 32 I9/I10-redesign
overlays.Add(RoiId="FAI-Edge2", LineRow1=b1r1, ... b1c2, Points=[중점])

// 교점2 에지 라인 2개
overlays.Add(RoiId="FAI-Edge3", LineRow1=a2r1, ... a2c2, Points=[중점])
overlays.Add(RoiId="FAI-Edge4", LineRow1=b2r1, ... b2c2, Points=[중점])

// 교점1 마커 (점 마커: LineRow1==LineRow2, LineColumn1==LineColumn2)
overlays.Add(RoiId="FAI-Intersection1", LineRow1=int1Row, LineColumn1=int1Col,
             LineRow2=int1Row, LineColumn2=int1Col, Points=[교점])

// 교점2 마커
overlays.Add(RoiId="FAI-Intersection2", LineRow1=int2Row, LineColumn1=int2Col,
             LineRow2=int2Row, LineColumn2=int2Col, Points=[교점])

// 평균점 마커
overlays.Add(RoiId="FAI-AvgPoint", LineRow1=avgRow, LineColumn1=avgCol,
             LineRow2=avgRow, LineColumn2=avgCol, Points=[평균점])

// Datum 거리선 — 평균점 → 수선의 발 (footOk 가드)
if (footOk) {
    overlays.Add(RoiId="FAI-DistLine",
                 LineRow1=footRow, LineColumn1=footCol,
                 LineRow2=avgRow, LineColumn2=avgCol,
                 Points=[footRow/Col, avgRow/Col]);
}
```
모든 overlay 라인에 `//260521 hbk Phase 32 I9/I10-redesign` 주석.
실패 경로(return false 앞)에는 overlay 추가 없음 — 빈 리스트 유지.
  </action>
  <verify>
    <automated>findstr /n "EdgeA1_Row\|EdgeA2_Row\|FAI-AvgPoint\|FAI-Intersection1\|FAI-Intersection2\|I9/I10-redesign" "WPF_Example\Custom\Sequence\Inspection\Measurements\ArcLineIntersectDistanceMeasurement.cs"</automated>
  </verify>
  <done>
    - `EdgeA1_Row`, `EdgeA2_Row` 각 1건 (필드 선언)
    - `FAI-AvgPoint`, `FAI-Intersection1`, `FAI-Intersection2` 각 1건 (overlay RoiId)
    - 구 필드명 `EdgeA_Row` / `EdgeB_Row` 가 파일 내 0건
    - `TryIntersectLines` 호출이 파일 내 2건 (교점1, 교점2 각 1회)
    - `avgRow` / `avgCol` 선언 1건
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MainView 4-ROI 순차 드로잉 확장 (CommitRectRoi + BuildPointRoiDefinitions + RectRoiButton_Click)</name>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (L50~60 필드, CommitRectRoi L1353~1410, RectRoiButton_Click L1295~1345, BuildPointRoiDefinitions L235~310)
  </read_first>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <action>
MainView.xaml.cs 의 ArcLineIntersect 관련 3곳을 수정한다. 변경 라인에 `//260521 hbk Phase 32 I9/I10-redesign` 주석, K&R 스타일 유지.

**(A) RectRoiButton_Click** — ArcLineIntersect 첫 드로잉 힌트 텍스트 변경:
기존 `"EdgeA(수직 에지) ROI 를 드래그하세요"` → `"교점1 수직 에지(EdgeA1) ROI 를 드래그하세요"`.
```csharp
if (measTarget is ArcLineIntersectDistanceMeasurement) //260521 hbk Phase 32 I9/I10-redesign
    label_drawHint.Content = "교점1 수직 에지(EdgeA1) ROI 를 드래그하세요"; //260521 hbk Phase 32 I9/I10-redesign
```

**(B) CommitRectRoi** — ali 분기를 index 0→1→2→3 4단계로 확장.
기존 L1386~1401 의 ali 분기 전체를 아래로 교체한다:
```csharp
var ali = _editingMeasurement as ArcLineIntersectDistanceMeasurement; //260521 hbk Phase 32 I9/I10-redesign
if (ali != null) {
    if (_editingMeasurementRoiIndex == 0) {
        ali.EdgeA1_Row = mCenterRow; ali.EdgeA1_Col = mCenterCol; ali.EdgeA1_Phi = 0.0;
        ali.EdgeA1_Length1 = mHalfHeight; ali.EdgeA1_Length2 = mHalfWidth; //260521 hbk Phase 32 I9/I10-redesign
        _editingMeasurementRoiIndex = 1; //260521 hbk Phase 32 I9/I10-redesign
        label_drawHint.Content = "교점1 수평 에지(EdgeB1) ROI 를 드래그하세요"; //260521 hbk Phase 32 I9/I10-redesign
        halconViewer.RectDrawingCompleted += HalconViewer_RectDrawingCompleted; //260521 hbk Phase 32 재무장
        halconViewer.StartRectangleDrawing(); //260521 hbk Phase 32 I9/I10-redesign
        return; //260521 hbk Phase 32 I9/I10-redesign — ExitCanvasMode 미호출
    }
    else if (_editingMeasurementRoiIndex == 1) {
        ali.EdgeB1_Row = mCenterRow; ali.EdgeB1_Col = mCenterCol; ali.EdgeB1_Phi = 0.0;
        ali.EdgeB1_Length1 = mHalfHeight; ali.EdgeB1_Length2 = mHalfWidth; //260521 hbk Phase 32 I9/I10-redesign
        _editingMeasurementRoiIndex = 2; //260521 hbk Phase 32 I9/I10-redesign
        label_drawHint.Content = "교점2 수직 에지(EdgeA2) ROI 를 드래그하세요"; //260521 hbk Phase 32 I9/I10-redesign
        halconViewer.RectDrawingCompleted += HalconViewer_RectDrawingCompleted; //260521 hbk Phase 32 재무장
        halconViewer.StartRectangleDrawing(); //260521 hbk Phase 32 I9/I10-redesign
        return; //260521 hbk Phase 32 I9/I10-redesign — ExitCanvasMode 미호출
    }
    else if (_editingMeasurementRoiIndex == 2) {
        ali.EdgeA2_Row = mCenterRow; ali.EdgeA2_Col = mCenterCol; ali.EdgeA2_Phi = 0.0;
        ali.EdgeA2_Length1 = mHalfHeight; ali.EdgeA2_Length2 = mHalfWidth; //260521 hbk Phase 32 I9/I10-redesign
        _editingMeasurementRoiIndex = 3; //260521 hbk Phase 32 I9/I10-redesign
        label_drawHint.Content = "교점2 수평 에지(EdgeB2) ROI 를 드래그하세요"; //260521 hbk Phase 32 I9/I10-redesign
        halconViewer.RectDrawingCompleted += HalconViewer_RectDrawingCompleted; //260521 hbk Phase 32 재무장
        halconViewer.StartRectangleDrawing(); //260521 hbk Phase 32 I9/I10-redesign
        return; //260521 hbk Phase 32 I9/I10-redesign — ExitCanvasMode 미호출
    }
    else { // index == 3: 마지막 ROI — 정상 종결
        ali.EdgeB2_Row = mCenterRow; ali.EdgeB2_Col = mCenterCol; ali.EdgeB2_Phi = 0.0;
        ali.EdgeB2_Length1 = mHalfHeight; ali.EdgeB2_Length2 = mHalfWidth; //260521 hbk Phase 32 I9/I10-redesign
        // index 리셋은 다음 RectRoiButton_Click 진입 시 0 으로 초기화됨 — 명시 불필요
    } //260521 hbk Phase 32 I9/I10-redesign — index 3: EdgeB2 기록 후 아래 ExitCanvasMode 로 종결
}
```
주의: return 문이 없는 else (index==3) 블록 이후 기존 흐름(UpdateDisplayState + ExitCanvasMode 등)으로 정상 진행한다.

**(C) BuildPointRoiDefinitions** — ali 분기(L241~266)를 4 ROI 로 확장.
기존 EdgeA / EdgeB 2 RoiDefinition 블록을 아래 4 RoiDefinition 블록으로 교체한다:
```csharp
var ali = m as ArcLineIntersectDistanceMeasurement; //260521 hbk Phase 32 I9/I10-redesign
if (ali != null) {
    if (ali.EdgeA1_Length1 > 0 && ali.EdgeA1_Length2 > 0) { //260521 hbk Phase 32 I9/I10-redesign
        result.Add(new RoiDefinition {
            Id = faiName + "_" + measName + "_EdgeA1", //260521 hbk Phase 32 I9/I10-redesign
            Name = measName + "_EdgeA1",
            Row1 = ali.EdgeA1_Row - ali.EdgeA1_Length1, Column1 = ali.EdgeA1_Col - ali.EdgeA1_Length2,
            Row2 = ali.EdgeA1_Row + ali.EdgeA1_Length1, Column2 = ali.EdgeA1_Col + ali.EdgeA1_Length2,
            IsTaught = true
        });
    }
    if (ali.EdgeB1_Length1 > 0 && ali.EdgeB1_Length2 > 0) { //260521 hbk Phase 32 I9/I10-redesign
        result.Add(new RoiDefinition {
            Id = faiName + "_" + measName + "_EdgeB1",
            Name = measName + "_EdgeB1",
            Row1 = ali.EdgeB1_Row - ali.EdgeB1_Length1, Column1 = ali.EdgeB1_Col - ali.EdgeB1_Length2,
            Row2 = ali.EdgeB1_Row + ali.EdgeB1_Length1, Column2 = ali.EdgeB1_Col + ali.EdgeB1_Length2,
            IsTaught = true
        });
    }
    if (ali.EdgeA2_Length1 > 0 && ali.EdgeA2_Length2 > 0) { //260521 hbk Phase 32 I9/I10-redesign
        result.Add(new RoiDefinition {
            Id = faiName + "_" + measName + "_EdgeA2",
            Name = measName + "_EdgeA2",
            Row1 = ali.EdgeA2_Row - ali.EdgeA2_Length1, Column1 = ali.EdgeA2_Col - ali.EdgeA2_Length2,
            Row2 = ali.EdgeA2_Row + ali.EdgeA2_Length1, Column2 = ali.EdgeA2_Col + ali.EdgeA2_Length2,
            IsTaught = true
        });
    }
    if (ali.EdgeB2_Length1 > 0 && ali.EdgeB2_Length2 > 0) { //260521 hbk Phase 32 I9/I10-redesign
        result.Add(new RoiDefinition {
            Id = faiName + "_" + measName + "_EdgeB2",
            Name = measName + "_EdgeB2",
            Row1 = ali.EdgeB2_Row - ali.EdgeB2_Length1, Column1 = ali.EdgeB2_Col - ali.EdgeB2_Length2,
            Row2 = ali.EdgeB2_Row + ali.EdgeB2_Length1, Column2 = ali.EdgeB2_Col + ali.EdgeB2_Length2,
            IsTaught = true
        });
    }
    return result; //260521 hbk Phase 32 I9/I10-redesign
}
```
C# 7.2 — switch expression / record 금지.
  </action>
  <verify>
    <automated>findstr /n "EdgeA1_Row\|EdgeB1_Row\|EdgeA2_Row\|EdgeB2_Row\|교점2\|I9/I10-redesign" "WPF_Example\UI\ContentItem\MainView.xaml.cs"</automated>
  </verify>
  <done>
    - `EdgeA1_Row`, `EdgeB1_Row`, `EdgeA2_Row`, `EdgeB2_Row` 각 1건 이상 (CommitRectRoi + BuildPointRoiDefinitions)
    - `_editingMeasurementRoiIndex == 2`, `_editingMeasurementRoiIndex == 3` 각 1건
    - "교점2" 텍스트 2건 이상 (label_drawHint EdgeA2/EdgeB2 메시지)
    - BuildPointRoiDefinitions 내 `_EdgeA1`, `_EdgeB1`, `_EdgeA2`, `_EdgeB2` Id suffix 각 1건
    - 구 필드명 `ali.EdgeA_Row` / `ali.EdgeB_Row` 가 파일 내 0건
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 구 필드명 잔존 grep + 빌드 검증 (msbuild Debug/x64)</name>
  <files></files>
  <action>
**(A) 구 필드명 잔존 확인** — Task 1/2 완료 후 grep 으로 구 필드명이 소스에 남아 있지 않음을 확인한다:
```powershell
# 구 2-ROI 필드명이 소스 파일에 잔존하는지 확인 (0건이어야 정상)
Select-String -Path "WPF_Example\Custom\Sequence\Inspection\Measurements\ArcLineIntersectDistanceMeasurement.cs" `
    -Pattern "EdgeA_Row|EdgeB_Row|EdgeA_Col|EdgeB_Col" | Select-Object LineNumber, Line
Select-String -Path "WPF_Example\UI\ContentItem\MainView.xaml.cs" `
    -Pattern "ali\.EdgeA_Row|ali\.EdgeB_Row|ali\.EdgeA_Col|ali\.EdgeB_Col" | Select-Object LineNumber, Line
```
잔존이 있으면 해당 라인을 수정한다.

**(B) 다른 소비처 grep** — 구 필드명을 참조하는 다른 파일이 있는지 확인한다:
```powershell
Select-String -Path "WPF_Example\**\*.cs" -Recurse `
    -Pattern "\.EdgeA_Row|\.EdgeB_Row" | Select-Object FileName, LineNumber, Line
```
검출 파일이 있으면 항목을 기록하고 해당 파일도 수정한다.

**(C) 빌드 검증:**
```powershell
& "C:\Program Files\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" `
    "C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj" `
    /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal 2>&1 | `
    Select-String "error|warning|Build succeeded"
```
MSBuild 2022 경로 없으면 대안:
```powershell
& "C:\Windows\Microsoft.NET\Framework64\v4.0.30319\MSBuild.exe" `
    "C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj" `
    /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal
```

빌드 실패 시 예상 원인과 처리:
- `EdgeA1_Row` 미선언: Task 1 재확인 — 필드가 `[Category]` attribute 없이 선언됐는지 확인.
- `avgRow` 미선언: TryExecute 에서 `double avgRow` 선언이 교점 산출 after-block 에 있는지 확인.
- `out footRow` 미선언: foot 오버로드 교체에서 `double footRow, footCol; bool footOk;` 선언 누락.
- `EdgeA2_Row` undefined in MainView: Task 2 CommitRectRoi 분기에서 field 이름 오타 확인.
  </action>
  <verify>
    <automated>구 필드명 grep 0건 + "Build succeeded" 출력</automated>
  </verify>
  <done>
    - `EdgeA_Row`/`EdgeB_Row` 구 필드명이 수정된 두 파일 모두에서 0건
    - 다른 소비처 grep 결과 0건 (또는 수정 완료)
    - msbuild Debug/x64 "Build succeeded", 신규 컴파일 오류 0건
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 ROI 드로잉 (4회) → 측정 필드 | 캔버스 드로잉 좌표가 EdgeA1/EdgeB1/EdgeA2/EdgeB2 필드로 write-back — 로컬 UI 조작, 외부 입력 없음 |
| TryExecute 내부 계산값 → overlay | 알고리즘이 계산한 image 좌표 → EdgeInspectionOverlay — 로컬, 외부 입력 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-14 | Denial of Service | 4-ROI 순차 드로잉 상태 누수 | mitigate | `_editingMeasurementRoiIndex` 를 RectRoiButton_Click 진입 시 0 으로 초기화 (기존 L1306 패턴 유지) — 이전 미완료 4회 드로잉의 잔존 인덱스(2/3)가 다음 티칭을 오염시키지 않음 |
| T-32-15 | Denial of Service | TryIntersectLines 2회 호출 중 1회 실패 (평행/근접) | mitigate | 각 TryIntersectLines 호출마다 `return false` 로 즉시 종결. overlays 빈 리스트 유지. 앱 크래시 없음 (기존 단일 교점 처리와 동일 패턴). |
| T-32-16 | Tampering | BuildPointRoiDefinitions 의 4 ROI Id 충돌 | accept | Id = `{faiName}_{measName}_{EdgeA1/EdgeB1/EdgeA2/EdgeB2}` — 측정 항목명(MeasurementName)이 FAI 내 유일하므로 Id 충돌 불가. 기존 EdgeA/EdgeB 2-ROI 패턴과 동일 보장 방법 적용. |

본 plan 은 알고리즘 재작성 + UI 배선 확장. 외부 신뢰 불가 입력 없음.
</threat_model>

<verification>
- ArcLineIntersectDistanceMeasurement.cs: EdgeA1/B1/A2/B2 ROI 필드 4벌 + Edge 파라미터 4그룹 존재
- TryExecute: TryFitLine 4회, TryIntersectLines 2회, avgRow/Col 계산, ComputeProjectionDistance(foot 오버로드) 1회
- overlay: FAI-Edge1/2/3/4, FAI-Intersection1/2, FAI-AvgPoint, FAI-DistLine 8개 (footOk 조건부 1개)
- MainView: CommitRectRoi ali 분기 index 0→1→2→3 4단계, drawHint 4단계 텍스트
- MainView: BuildPointRoiDefinitions ali 분기에서 EdgeA1/B1/A2/B2 4 RoiDefinition 반환
- 구 필드명(EdgeA_Row/EdgeB_Row) 양 파일 모두 0건
- msbuild Debug/x64 Build succeeded, 신규 오류 0건
</verification>

<success_criteria>
- ArcLineIntersectDistanceMeasurement 가 4-ROI / 두 교점 평균 설계로 완전 교체됨
- 측정 항목 1개 = (교점1 + 교점2 평균점) → Datum C X 거리(mm) 출력
- 어느 ROI 피팅 또는 교점 산출이 실패해도 false 반환 + 앱 무크래시
- MainView 에서 ArcLineIntersect 노드 선택 후 4회 순차 드로잉으로 4개 ROI 티칭 가능
- 빌드 PASS (msbuild Debug/x64 "Build succeeded")
</success_criteria>

<output>
완료 후 `.planning/phases/32-sop-i9-i10-e2-e9-e10-e3/32-08-SUMMARY.md` 생성.
</output>
