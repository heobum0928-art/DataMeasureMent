---
phase: 33-side-bottom-inspectionsequence-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/SequenceHandler.cs
  - WPF_Example/Custom/Sequence/Top/Sequence_Top.cs
  - WPF_Example/Custom/Sequence/Bottom/Sequence_Bottom.cs
  - WPF_Example/Custom/Sequence/Top/Action_TopInspection.cs
  - WPF_Example/Custom/Sequence/Bottom/Action_BottomInspection.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Side / Bottom 시퀀스는 InspectionSequence 인스턴스로 등록된다 (grep 검증)"
    - "Side / Bottom 시퀀스가 Datum 추가/티칭 흐름의 진입점 (DatumConfigs 필드 + TryRunDatumPhase) 을 보유한다"
    - "Top 검사 흐름은 1자도 변경되지 않는다 (Top 라인 byte-identical)"
    - "레거시 4 클래스는 [Obsolete] 어트리뷰트로 deprecate 표시된다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/SequenceHandler.cs"
      provides: "Side/Bottom InspectionSequence 인스턴스 등록 + Action 매핑"
      contains: "new InspectionSequence(ESequence.Side"
    - path: "WPF_Example/Custom/Sequence/Top/Sequence_Top.cs"
      provides: "TopSequence [Obsolete] 표시"
      contains: "[System.Obsolete"
    - path: "WPF_Example/Custom/Sequence/Bottom/Sequence_Bottom.cs"
      provides: "BottomSequence [Obsolete] 표시"
      contains: "[System.Obsolete"
  key_links:
    - from: "SequenceHandler.RegisterSequences L30-34"
      to: "InspectionSequence ctor"
      via: "ESequence.Side / ESequence.Bottom 5-arg ctor 호출"
      pattern: "new InspectionSequence\\(ESequence\\.(Side|Bottom)"
    - from: "SequenceHandler.RegisterActions L37-43"
      to: "Action_FAIMeasurement (via RebuildInspectionActions)"
      via: "EAction.Side_Inspection / EAction.Bottom_Inspection placeholder"
      pattern: "EAction\\.(Side|Bottom)_Inspection"
---

<objective>
SequenceHandler 에서 Side / Bottom 의 레거시 TopSequence / BottomSequence 인스턴스를 InspectionSequence 로 교체하고,
연관 Action 매핑을 정리한다. 레거시 4 클래스 (TopSequence, BottomSequence, TopInspectionAction, BottomInspectionAction)
에 [Obsolete] 어트리뷰트를 부여하여 향후 인스턴스화 차단 경고를 명시한다.

Purpose: Phase 33 의 핵심 변경 — 한 줄 교체로 Side/Bottom 도 DatumConfigs + TryRunDatumPhase + Dynamic FAI 흐름 진입 가능.
Output:
  - SequenceHandler.cs L30-34, L37-43, L52-58 수정
  - 4 레거시 클래스에 [Obsolete] 1 라인씩 추가
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-side-bottom-inspectionsequence-migration/33-CONTEXT.md
@.planning/phases/33-side-bottom-inspectionsequence-migration/33-PATTERNS.md

# 변경 금지 (D-06) — 읽기 전용 참조
@WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
@WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs

<interfaces>
<!-- InspectionSequence ctor 시그니처 — Side/Bottom 에서 그대로 재사용 (PATTERNS.md L104-115) -->
From WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs L54:
```csharp
public InspectionSequence(ESequence seqID, string name, int algIndex, string defaultCamera, string defaultLight)
```

<!-- SequenceHandler 의 현재 상태 (L30-34, L37-43, L52-58) -->
From WPF_Example/Custom/Sequence/SequenceHandler.cs L28-43:
```csharp
private void RegisterSequences() {
    //260409 hbk Phase 5: 동적 FAI 모드용 InspectionSequence (D-07)
    SequenceBuilder.RegisterSequence(
        new InspectionSequence(ESequence.Top, SEQ_TOP, Top_Alg_Index, DeviceHandler.CAMERA_TOP, LightHandler.LIGHT_TOP),
        new TopSequence(ESequence.Side, SEQ_SIDE, Side_Alg_Index, DeviceHandler.CAMERA_SIDE, LightHandler.LIGHT_SIDE),
        new BottomSequence(ESequence.Bottom, SEQ_BOTTOM, Bottom_Alg_Index, DeviceHandler.CAMERA_BOTTOM, LightHandler.LIGHT_BOTTOM)
    );
}

private void RegisterActions() {
    SequenceBuilder.RegisterAction(
        new TopInspectionAction(EAction.Top_Inspection, ACT_INSPECT, Top_Alg_Index, Inspection_Model_Index),
        new TopInspectionAction(EAction.Side_Inspection, ACT_INSPECT, Side_Alg_Index, Inspection_Model_Index),
        new BottomInspectionAction(EAction.Bottom_Inspection, ACT_INSPECT, Bottom_Alg_Index, Inspection_Model_Index)
    );
}
```

<!-- 레거시 클래스 선언 위치 (PATTERNS.md L399-403) -->
Sequence_Top.cs L41 — `public class TopSequence : SequenceBase {`
Sequence_Bottom.cs L89 — `public class BottomSequence : SequenceBase {`
Action_TopInspection.cs L194 — `public class TopInspectionAction : ActionBase {`
Action_BottomInspection.cs L283 — `public class BottomInspectionAction : ActionBase {`
</interfaces>

<decisions>
- D-01 (locked): SequenceHandler.RegisterSequences L30-34 의 Side/Bottom 인스턴스 → InspectionSequence 교체
- D-05 (locked): 레거시 4 클래스에 `[System.Obsolete("Phase 33 — InspectionSequence/Action_FAIMeasurement 로 마이그레이션됨", false)]` 부여 (false = warning, not error)
- D-06 (locked): InspectionSequence.cs / Action_FAIMeasurement.cs 1자 변경 금지
- Action 등록 (RegisterActions L37-43) 처리 방침 (planner 결정 — CONTEXT.md "Claude's Discretion"):
  Side_Inspection / Bottom_Inspection 매핑은 InitializeSequences L52-58 의 `AddAction(EAction.Side_Inspection)` 호출 때문에 EAction-→ActionBase 사전 등록이 필요함.
  -> RegisterActions 의 Side/Bottom 라인은 **유지** 하되, 사용 액션 클래스는 placeholder Action_FAIMeasurement 로 대체할 수 없음 (ShotConfig 인자 필수). 따라서 Side/Bottom 의 placeholder 액션 = **TopInspectionAction 그대로 유지** (deprecated 이지만 RebuildInspectionActions 가 동적 FAI 모드 진입 시 ClearChildren+AddAction 으로 즉시 교체됨 — 실제 검사 흐름에서는 사용되지 않음).
  Phase 33 sign-off 시 RebuildInspectionActions(ESequence.Side|Bottom) 호출이 Wave 2 INI 마이그레이션에서 트리거됨.
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SequenceHandler.RegisterSequences L30-34 — Side/Bottom InspectionSequence 교체</name>
  <files>WPF_Example/Custom/Sequence/SequenceHandler.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/SequenceHandler.cs (전체, 132 lines)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (L37-115 ctor + 필드 영역)
    - .planning/phases/33-side-bottom-inspectionsequence-migration/33-PATTERNS.md (L25-95)
  </read_first>
  <action>
SequenceHandler.cs L30-34 의 RegisterSequences 본문을 다음과 같이 교체:

BEFORE (L30-34):
```csharp
SequenceBuilder.RegisterSequence(
    new InspectionSequence(ESequence.Top, SEQ_TOP, Top_Alg_Index, DeviceHandler.CAMERA_TOP, LightHandler.LIGHT_TOP),
    new TopSequence(ESequence.Side, SEQ_SIDE, Side_Alg_Index, DeviceHandler.CAMERA_SIDE, LightHandler.LIGHT_SIDE),
    new BottomSequence(ESequence.Bottom, SEQ_BOTTOM, Bottom_Alg_Index, DeviceHandler.CAMERA_BOTTOM, LightHandler.LIGHT_BOTTOM)
);
```

AFTER (per D-01):
```csharp
//260526 hbk Phase 33 — Side/Bottom 도 InspectionSequence 마이그레이션 (D-01)
SequenceBuilder.RegisterSequence(
    new InspectionSequence(ESequence.Top, SEQ_TOP, Top_Alg_Index, DeviceHandler.CAMERA_TOP, LightHandler.LIGHT_TOP),
    new InspectionSequence(ESequence.Side, SEQ_SIDE, Side_Alg_Index, DeviceHandler.CAMERA_SIDE, LightHandler.LIGHT_SIDE),
    new InspectionSequence(ESequence.Bottom, SEQ_BOTTOM, Bottom_Alg_Index, DeviceHandler.CAMERA_BOTTOM, LightHandler.LIGHT_BOTTOM)
);
```

주의:
- Top 라인 (L31) 은 byte-identical (D-06 회귀 가드)
- 기존 `//260409 hbk Phase 5: 동적 FAI 모드용 InspectionSequence (D-07)` 주석 (L29) 은 보존하고, 위 줄에 신규 마커 누적 (Phase 20 D-12 stacking 패턴)
- 5-arg ctor 시그니처는 InspectionSequence.cs L54 byte-identical — 시그니처 변경 금지
- 다른 메서드 (RegisterActions, InitializeSequences 등) 는 Task 2 에서 처리, 이 Task 에서는 RegisterSequences 만 수정
  </action>
  <verify>
    <automated>grep -c "new InspectionSequence(ESequence.Side" WPF_Example/Custom/Sequence/SequenceHandler.cs returns 1</automated>
    <automated>grep -c "new InspectionSequence(ESequence.Bottom" WPF_Example/Custom/Sequence/SequenceHandler.cs returns 1</automated>
    <automated>grep -c "new TopSequence(" WPF_Example/Custom/Sequence/SequenceHandler.cs returns 0</automated>
    <automated>grep -c "new BottomSequence(" WPF_Example/Custom/Sequence/SequenceHandler.cs returns 0</automated>
    <automated>grep -n "260526 hbk Phase 33" WPF_Example/Custom/Sequence/SequenceHandler.cs has at least 1 match</automated>
  </verify>
  <acceptance_criteria>
    - SequenceHandler.cs L31 Top 라인 = "new InspectionSequence(ESequence.Top, SEQ_TOP, Top_Alg_Index, DeviceHandler.CAMERA_TOP, LightHandler.LIGHT_TOP)," (byte-identical, D-06)
    - SequenceHandler.cs Side 라인 = "new InspectionSequence(ESequence.Side, SEQ_SIDE, Side_Alg_Index, DeviceHandler.CAMERA_SIDE, LightHandler.LIGHT_SIDE),"
    - SequenceHandler.cs Bottom 라인 = "new InspectionSequence(ESequence.Bottom, SEQ_BOTTOM, Bottom_Alg_Index, DeviceHandler.CAMERA_BOTTOM, LightHandler.LIGHT_BOTTOM)"
    - 신규 주석 라인 `//260526 hbk Phase 33` 1개 추가 (RegisterSequences 본문 진입 직후)
  </acceptance_criteria>
  <done>RegisterSequences L30-34 가 InspectionSequence × 3 으로 교체됨. Top 라인 byte-identical 보존.</done>
</task>

<task type="auto">
  <name>Task 2: Action 매핑 + Initialize 부분 처리 + 4 레거시 클래스 [Obsolete] 부여</name>
  <files>
    WPF_Example/Custom/Sequence/SequenceHandler.cs
    WPF_Example/Custom/Sequence/Top/Sequence_Top.cs
    WPF_Example/Custom/Sequence/Bottom/Sequence_Bottom.cs
    WPF_Example/Custom/Sequence/Top/Action_TopInspection.cs
    WPF_Example/Custom/Sequence/Bottom/Action_BottomInspection.cs
  </files>
  <read_first>
    - WPF_Example/Custom/Sequence/SequenceHandler.cs (Task 1 수정 후 상태)
    - WPF_Example/Custom/Sequence/Top/Sequence_Top.cs L1-50
    - WPF_Example/Custom/Sequence/Bottom/Sequence_Bottom.cs L1-100
    - WPF_Example/Custom/Sequence/Top/Action_TopInspection.cs L1-200 (class 선언 라인 찾기)
    - WPF_Example/Custom/Sequence/Bottom/Action_BottomInspection.cs L1-290 (class 선언 라인 찾기)
    - .planning/phases/33-side-bottom-inspectionsequence-migration/33-PATTERNS.md (L395-413)
  </read_first>
  <action>
### Part A — SequenceHandler.cs RegisterActions / InitializeSequences 정리

RegisterActions L37-43 본문은 **유지** (planner 결정 — context decisions 섹션 참조).
이유: `InitializeSequences L52-58` 의 `seq.AddAction(EAction.Side_Inspection)` / `seq.AddAction(EAction.Bottom_Inspection)` 호출이 placeholder 액션을 요구하기 때문.
RebuildInspectionActions(ESequence.Side|Bottom) 가 동적 FAI 모드 진입 시 ClearChildren + AddAction 으로 즉시 교체하므로 placeholder 는 실제 검사 흐름에서 사용되지 않음.

단, **신규 마커 주석만 추가** — L37 위에 (또는 L37 의 라인 끝에) `//260526 hbk Phase 33 — placeholder; RebuildInspectionActions 가 동적 FAI 진입 시 교체` 추가.

수정 형식:
```csharp
private void RegisterActions() {
    //260526 hbk Phase 33 — Side/Bottom placeholder; RebuildInspectionActions(Side|Bottom) 가 동적 FAI 모드 진입 시 Action_FAIMeasurement 로 교체
    SequenceBuilder.RegisterAction(
        new TopInspectionAction(EAction.Top_Inspection, ACT_INSPECT, Top_Alg_Index, Inspection_Model_Index),
        new TopInspectionAction(EAction.Side_Inspection, ACT_INSPECT, Side_Alg_Index, Inspection_Model_Index),
        new BottomInspectionAction(EAction.Bottom_Inspection, ACT_INSPECT, Bottom_Alg_Index, Inspection_Model_Index)
    );
}
```
- L37-43 의 등록 라인 자체는 byte-identical 보존 (Obsolete warning 은 발생하지만 build 차단 안 함 — D-05 의 `false` 인자 의미)
- L52-58 InitializeSequences 는 변경 없음

### Part B — Sequence_Top.cs L41 [Obsolete] 부여

Sequence_Top.cs 의 `public class TopSequence : SequenceBase {` 선언 라인 (L41 추정) 바로 위 라인에 다음 추가:
```csharp
//260526 hbk Phase 33 — InspectionSequence 로 마이그레이션됨 (D-05)
[System.Obsolete("Phase 33 — InspectionSequence/Action_FAIMeasurement 로 마이그레이션됨", false)]
public class TopSequence : SequenceBase {
```

### Part C — Sequence_Bottom.cs L89 [Obsolete] 부여

Sequence_Bottom.cs 의 `public class BottomSequence : SequenceBase {` 선언 라인 (L89 추정) 바로 위 라인에 추가:
```csharp
//260526 hbk Phase 33 — InspectionSequence 로 마이그레이션됨 (D-05)
[System.Obsolete("Phase 33 — InspectionSequence/Action_FAIMeasurement 로 마이그레이션됨", false)]
public class BottomSequence : SequenceBase {
```

### Part D — Action_TopInspection.cs L194 [Obsolete] 부여

Action_TopInspection.cs 의 `public class TopInspectionAction : ActionBase {` 선언 라인 (L194 추정) 바로 위 라인에 추가:
```csharp
//260526 hbk Phase 33 — Action_FAIMeasurement 로 마이그레이션됨 (D-05)
[System.Obsolete("Phase 33 — Action_FAIMeasurement 로 마이그레이션됨", false)]
public class TopInspectionAction : ActionBase {
```

### Part E — Action_BottomInspection.cs L283 [Obsolete] 부여

Action_BottomInspection.cs 의 `public class BottomInspectionAction : ActionBase {` 선언 라인 (L283 추정) 바로 위 라인에 추가:
```csharp
//260526 hbk Phase 33 — Action_FAIMeasurement 로 마이그레이션됨 (D-05)
[System.Obsolete("Phase 33 — Action_FAIMeasurement 로 마이그레이션됨", false)]
public class BottomInspectionAction : ActionBase {
```

주의:
- `BottomDieInfo` 등 helper 클래스에는 [Obsolete] 부여 금지 — Wave 2 의 Bottom Multi-Die → FAI 매핑 작업에서 source 데이터 모델로 재활용
- false 인자 (warning, not error) — 컴파일 차단 회피
- 5 파일 모두 atomic 단일 commit 으로 처리
  </action>
  <verify>
    <automated>grep -c "\[System.Obsolete" WPF_Example/Custom/Sequence/Top/Sequence_Top.cs returns 1</automated>
    <automated>grep -c "\[System.Obsolete" WPF_Example/Custom/Sequence/Bottom/Sequence_Bottom.cs returns 1</automated>
    <automated>grep -c "\[System.Obsolete" WPF_Example/Custom/Sequence/Top/Action_TopInspection.cs returns 1</automated>
    <automated>grep -c "\[System.Obsolete" WPF_Example/Custom/Sequence/Bottom/Action_BottomInspection.cs returns 1</automated>
    <automated>grep -n "260526 hbk Phase 33" WPF_Example/Custom/Sequence/SequenceHandler.cs returns at least 2 matches (RegisterSequences + RegisterActions)</automated>
    <automated>msbuild /p:Configuration=Debug /p:Platform=x64 WPF_Example/DatumMeasurement.csproj — exits 0 (warning CS0618 발생 허용 — RegisterActions 의 TopInspectionAction / BottomInspectionAction 사용에서 발생)</automated>
  </verify>
  <acceptance_criteria>
    - Sequence_Top.cs 의 TopSequence class 선언 바로 위에 [System.Obsolete(...)] 라인 + 260526 hbk Phase 33 주석
    - Sequence_Bottom.cs 의 BottomSequence class 선언 바로 위에 [System.Obsolete(...)] 라인 + 260526 hbk Phase 33 주석
    - Action_TopInspection.cs 의 TopInspectionAction class 선언 바로 위에 [System.Obsolete(...)] 라인 + 260526 hbk Phase 33 주석
    - Action_BottomInspection.cs 의 BottomInspectionAction class 선언 바로 위에 [System.Obsolete(...)] 라인 + 260526 hbk Phase 33 주석
    - SequenceHandler.RegisterActions 본문에 신규 placeholder 설명 주석 1라인 추가
    - msbuild Debug/x64 빌드 성공 (CS0618 warning 4건 발생 — TopSequence(L미사용)/BottomSequence(L미사용)/TopInspectionAction(L40,L41)/BottomInspectionAction(L43) 인스턴스화는 RegisterActions 에서 발생, 이는 Phase 33 의 의도된 deprecation 표시. CS error 0)
    - InspectionSequence.cs / Action_FAIMeasurement.cs / InspectionRecipeManager.cs 변경 0 라인 (D-06)
  </acceptance_criteria>
  <done>4 레거시 클래스에 [Obsolete] 부여 + SequenceHandler placeholder 마커 주석 추가. msbuild Debug/x64 PASS (CS0618 warning 만 발생, error 0).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INI 파일 → RecipeManager | Side/Bottom 레거시 INI 가 Phase 33 변경 후 어떻게 로드되는지 — Plan 33-02 에서 처리 |
| SequenceHandler 등록 → SequenceBuilder | 시퀀스/액션 등록 시점 race condition — 기존 패턴 재사용으로 회귀 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | Top 시퀀스 등록 라인 변경 회귀 | mitigate | RegisterSequences L31 (Top) byte-identical 보존 — grep 으로 검증 (D-06) |
| T-33-02 | Denial-of-Service | RegisterActions 의 EAction.Side/Bottom_Inspection placeholder 제거 시 AddAction 실패로 시퀀스 부트 차단 | accept | placeholder 등록 라인 byte-identical 유지 — RebuildInspectionActions 가 동적 FAI 진입 시 교체 |
| T-33-03 | Tampering | [Obsolete] 잘못된 위치 부여 (method/property 대신 class) | mitigate | acceptance_criteria 에 "class 선언 바로 위" 명시 + grep 검증 |
| T-33-04 | Information Disclosure | InspectionSequence/Action_FAIMeasurement 변경으로 Top 회귀 | mitigate | 두 파일 변경 0 라인 — Plan 33-01/02/03 의 acceptance_criteria 에 명시 가드 (D-06) |
</threat_model>

<verification>
### 자동 검증
- msbuild Debug/x64 PASS (CS error 0, CS0618 warning 발생 허용 — deprecation 의도)
- grep 으로 5 파일의 마커/클래스 표시 확인 (위 acceptance_criteria 참조)
- InspectionSequence.cs / Action_FAIMeasurement.cs / InspectionRecipeManager.cs git diff = 0 lines (Plan 33-01 범위)

### 후속 Wave 인계
- Wave 2 (Plan 33-02): RecipeManager Save/Load 의 Side/Bottom DatumConfigs 라운드트립 + Bottom Multi-Die → FAI 매핑
- Wave 3 (Plan 33-03): SIMUL UAT — Side Datum / Bottom Datum / Top 회귀 / msbuild
</verification>

<success_criteria>
- SequenceHandler.cs L30-34 가 InspectionSequence × 3 으로 변경됨
- Top 시퀀스 라인 byte-identical (D-06)
- 4 레거시 클래스가 [Obsolete] 어트리뷰트로 deprecate 표시됨
- msbuild Debug/x64 PASS, CS error 0 (CS0618 warning 허용)
- 신규 마커 주석 `//260526 hbk Phase 33` 5 파일 모두 존재
- InspectionSequence.cs / Action_FAIMeasurement.cs / InspectionRecipeManager.cs 변경 0 라인
</success_criteria>

<output>
After completion, create `.planning/phases/33-side-bottom-inspectionsequence-migration/33-01-SUMMARY.md` documenting:
  - 5 파일 변경 라인 수
  - msbuild 결과 (CS error 0, CS0618 warning N 건 — 의도된 deprecation)
  - InspectionSequence/Action_FAIMeasurement/RecipeManager git diff = 0 라인 인용 (D-06 회귀 가드 증명)
  - Wave 2 인계 사항: RecipeManager Save/Load 의 Side/Bottom Datum 직렬화 미배선 (PATTERNS.md L470-485 Open Question 1)
</output>
