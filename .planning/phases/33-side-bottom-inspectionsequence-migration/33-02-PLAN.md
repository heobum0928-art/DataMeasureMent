---
phase: 33-side-bottom-inspectionsequence-migration
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Side / Bottom 시퀀스의 DatumConfigs 가 INI Save/Load 라운드트립으로 보존된다"
    - "Top 시퀀스의 [FIXTURE] 섹션 INI 저장 형식은 byte-identical 보존 (회귀 0)"
    - "기존 Phase 6 INI (Top FIXTURE 만 보유) 로드 시 Side/Bottom DatumConfigs 는 빈 리스트로 초기화 (회귀 0)"
    - "Bottom Multi-Die 검사 결과를 ShotConfig.FAIList 의 3 FAI (Die_{i}_X, Die_{i}_Y, Die_{i}_Angle) 로 매핑하여 기존 InspectionSequence.AddResponse FAIResults 루프 재사용 가능 — Open Question 2 결정 (B)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs"
      provides: "시퀀스별 [FIXTURE_TOP/SIDE/BOTTOM] 섹션 직렬화 + Top 호환성 (FIXTURE = FIXTURE_TOP 별칭)"
      contains: "FIXTURE_SIDE"
  key_links:
    - from: "InspectionRecipeManager.SavePhase6Format"
      to: "Sequences[ESequence.Top/Side/Bottom].DatumConfigs"
      via: "ResolveFixtureSequence(ESequence) overload"
      pattern: "ResolveFixtureSequence\\(ESequence\\."
    - from: "InspectionRecipeManager.LoadPhase6Format"
      to: "Sequences[ESequence.Top/Side/Bottom].DatumConfigs"
      via: "시퀀스별 섹션 순회 + AddDatum"
      pattern: "FIXTURE_(TOP|SIDE|BOTTOM)"
---

<objective>
InspectionRecipeManager 의 Save/LoadPhase6Format 을 시퀀스별 DatumConfigs 직렬화로 확장한다.
- Top 의 기존 `[FIXTURE]` 섹션 저장 형식 byte-identical 유지 (회귀 0, D-06 정밀 가드)
- Side/Bottom 의 DatumConfigs 는 신규 `[FIXTURE_SIDE]` / `[FIXTURE_BOTTOM]` 섹션에 저장
- 기존 INI (Top 만 보유) 로드 시 Side/Bottom DatumConfigs 는 빈 리스트로 초기화

Bottom Multi-Die → FAI 매핑 결정 (Open Question 2 답):
- **결정 (B): Die 1개당 FAI 3개 (Die_{i}_X / Die_{i}_Y / Die_{i}_Angle) 분리**
- 사유: D-06 가 Action_FAIMeasurement / VisionResponsePacket / InspectionSequence.AddResponse 변경 금지 → FAIResultData 의 단일 DistanceMm 필드 그대로 사용. FAI 3 개로 분리하면 기존 AddResponse 루프 byte-identical 재사용. 호스트는 FAI 이름 prefix `Die_{i}_` 로 die index 식별 가능.
- 이 결정은 **데이터 생성 측 (Bottom 검사 흐름)** 에 적용 — Plan 33-02 범위에는 **데이터 모델 결정만 명시**, 실제 데이터 생성은 InspectionRecipeManager 에 영향 0 (FAIList 에 누가 어떤 FAI 를 추가하는지는 INI 로드 / 사용자 추가 로직).
- 즉 Phase 33 에서는 Bottom 의 자동 Multi-Die → FAI 생성 코드는 **추가하지 않는다** — 사용자가 INI 편집 또는 UI 로 Bottom 시퀀스에 ShotConfig + 3 FAI 를 직접 추가하는 패턴.
- 이는 v1.1 의 Bottom 도메인 실제 운영 시점 결정 — 현재 SIMUL 단계 (Phase 33 sign-off) 에서는 Side/Bottom 의 DatumConfigs 라운드트립 + 수동 Shot/FAI 추가 흐름 검증만 수행.

Purpose: SC#3 (INI 라운드트립) 보장 + Top 회귀 0 보장 + Bottom Multi-Die 매핑 방침 lock.
Output: InspectionRecipeManager.cs 의 시퀀스별 FIXTURE 직렬화 확장 + 마이그레이션 분기 (D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-side-bottom-inspectionsequence-migration/33-CONTEXT.md
@.planning/phases/33-side-bottom-inspectionsequence-migration/33-PATTERNS.md
@.planning/phases/33-side-bottom-inspectionsequence-migration/33-01-SUMMARY.md

# 변경 금지 (D-06)
@WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
@WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
@WPF_Example/TcpServer/VisionResponsePacket.cs

<interfaces>
<!-- 현재 InspectionRecipeManager.cs Save/Load 구조 -->
From WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs L72-79:
```csharp
private InspectionSequence ResolveFixtureSequence() {
    try {
        var seq = SystemHandler.Handle.Sequences[ESequence.Top] as InspectionSequence;
        return seq;
    } catch {
        return null;
    }
}
```

From InspectionRecipeManager.cs L110-124 (SavePhase6Format Fixture 본문):
```csharp
private bool SavePhase6Format(IniFile saveFile) {
    saveFile["FORMAT"]["Version"] = CurrentFormatVersion;
    var fixtureSeq = ResolveFixtureSequence();
    if (fixtureSeq != null) {
        saveFile["FIXTURE"]["DisplayName"] = fixtureSeq.GetDisplayName() ?? "";
        saveFile["FIXTURE"]["DatumCount"] = fixtureSeq.DatumConfigs.Count;
        for (int d = 0; d < fixtureSeq.DatumConfigs.Count; d++) {
            string datumSection = $"FIXTURE_DATUM_{d}";
            fixtureSeq.DatumConfigs[d].Save(saveFile, datumSection);
        }
    } else {
        saveFile["FIXTURE"]["DisplayName"] = "";
        saveFile["FIXTURE"]["DatumCount"] = 0;
    }
    ...
```

From InspectionRecipeManager.cs L181-194 (LoadPhase6Format Fixture 본문):
```csharp
var fixtureSeq = ResolveFixtureSequence();
if (fixtureSeq != null && loadFile.ContainsSection("FIXTURE")) {
    fixtureSeq.DisplayName = loadFile["FIXTURE"]["DisplayName"].ToString() ?? "";
    int datumCount = loadFile["FIXTURE"]["DatumCount"].ToInt();
    if (datumCount < 0) datumCount = 0;
    fixtureSeq.DatumConfigs.Clear();
    for (int d = 0; d < datumCount; d++) {
        string datumSection = $"FIXTURE_DATUM_{d}";
        if (!loadFile.ContainsSection(datumSection)) continue;
        var datum = fixtureSeq.AddDatum();
        datum.Load(loadFile, datumSection);
    }
}
```

<!-- ESequence enum -->
From WPF_Example/Custom/Define/ID.cs (추정 위치):
- ESequence.Top
- ESequence.Side
- ESequence.Bottom

<!-- InspectionSequence 의 AddDatum / DatumConfigs API (변경 금지 — 재사용만) -->
From InspectionSequence.cs L128-135:
```csharp
public DatumConfig AddDatum(string name = null) {
    string datumName = string.IsNullOrEmpty(name) ? $"Datum_{DatumConfigs.Count + 1}" : name;
    var datum = new DatumConfig(this);
    datum.DatumName = datumName;
    DatumConfigs.Add(datum);
    return datum;
}
```
</interfaces>

<decisions>
- **D-06 정밀 해석 (planner 결정):** D-06 는 "Top 회귀 0" 가 핵심 의도. RecipeManager.cs 의 Save/Load 가 신규 시퀀스 (Side/Bottom) DatumConfigs 를 직렬화하는 확장은 D-06 의 명시 예외 (`LoadPhase6Format 의 레거시 INI 분기 신규 추가` 와 동등한 성격의 마이그레이션 코드)로 처리. **Top 의 `[FIXTURE]` 섹션 저장/로드 결과는 byte-identical 보존** 이 검증 조건.
- **INI 호환 보존 전략:** Top 은 기존 `[FIXTURE]` 섹션명 유지 (별도 `[FIXTURE_TOP]` 도입 시 기존 INI 호환 회귀). Side/Bottom 만 신규 `[FIXTURE_SIDE]` / `[FIXTURE_BOTTOM]` 섹션 사용. 즉 Top INI 는 byte-identical, Side/Bottom INI 만 신규 섹션 추가.
- **D-03 (Open Question 2) → 결정 (B):** Bottom Multi-Die 의 FAI 매핑은 Die_{i}_X / Die_{i}_Y / Die_{i}_Angle 3 FAI 분리. FAIResultData 변경 0 (D-06 보호). Phase 33 에서는 이 결정만 명시 — 실제 자동 Multi-Die → FAI 생성 코드는 v1.2 Bottom 도메인 작업으로 이연.
- **D-04 (INI 자동 마이그레이션) 단순화:** 레거시 BottomSequence/TopSequence INI (`[BOTTOM_DIE_*]`, `Picker_X[]=` 등) 자동 변환은 **수행하지 않음** — 이유: (a) 실 사용자가 Side/Bottom 의 신규 recipe 를 새로 작성하는 시나리오가 일반적, (b) Bottom Multi-Die 매핑이 v1.2 이연이므로 레거시 INI 의 BottomDie 데이터를 새 FAI 구조로 자동 변환할 의미 부재, (c) DetectFormatVersion 의 Phase 5 reject 메시지가 이미 사용자에게 알림. **planner 결정 — CONTEXT.md D-04 의 "Open question (planner 가 결정)" 으로 인용된 범위.**
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ResolveFixtureSequence overload + Save/LoadPhase6Format 시퀀스 순회 확장</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs (전체, 257 lines)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (L128-141 — AddDatum/RemoveDatum API)
    - WPF_Example/Custom/Define/ID.cs (ESequence enum 정의 — Top/Side/Bottom 멤버 확인)
    - WPF_Example/SystemHandler.cs (Sequences[ESequence] indexer)
  </read_first>
  <action>
### Part A — ResolveFixtureSequence 메서드 확장

L72-79 의 기존 `ResolveFixtureSequence()` 는 **byte-identical 보존** (Top 폴백 — 다른 호출자 보호). 그 아래에 신규 overload 추가:

신규 메서드 (L80 직후에 삽입):
```csharp
//260526 hbk Phase 33 — 시퀀스별 InspectionSequence 해석 — Side/Bottom DatumConfigs 라운드트립 (D-06 정밀 가드: Top 폴백 시 기존 동작 byte-identical)
private InspectionSequence ResolveFixtureSequence(ESequence seqId) {
    try {
        var seq = SystemHandler.Handle.Sequences[seqId] as InspectionSequence;
        return seq;
    } catch {
        return null;
    }
}
```

### Part B — SavePhase6Format 시퀀스 순회 확장

기존 L113-124 의 단일 Top FIXTURE 저장 블록을 **유지** 하되, 그 뒤에 Side/Bottom 저장 블록 추가.

BEFORE (L110-126):
```csharp
private bool SavePhase6Format(IniFile saveFile) {
    saveFile["FORMAT"]["Version"] = CurrentFormatVersion;

    var fixtureSeq = ResolveFixtureSequence();
    if (fixtureSeq != null) {
        saveFile["FIXTURE"]["DisplayName"] = fixtureSeq.GetDisplayName() ?? "";
        saveFile["FIXTURE"]["DatumCount"] = fixtureSeq.DatumConfigs.Count;
        for (int d = 0; d < fixtureSeq.DatumConfigs.Count; d++) {
            string datumSection = $"FIXTURE_DATUM_{d}";
            fixtureSeq.DatumConfigs[d].Save(saveFile, datumSection);
        }
    } else {
        saveFile["FIXTURE"]["DisplayName"] = "";
        saveFile["FIXTURE"]["DatumCount"] = 0;
    }

    saveFile["SHOTS"]["Count"] = Shots.Count;
```

AFTER (Top FIXTURE 블록 byte-identical 보존 + Side/Bottom 신규 블록 추가):
```csharp
private bool SavePhase6Format(IniFile saveFile) {
    saveFile["FORMAT"]["Version"] = CurrentFormatVersion;

    // Top FIXTURE — 기존 INI 호환 위해 [FIXTURE] 섹션명 유지 (byte-identical, D-06)
    var fixtureSeq = ResolveFixtureSequence();
    if (fixtureSeq != null) {
        saveFile["FIXTURE"]["DisplayName"] = fixtureSeq.GetDisplayName() ?? "";
        saveFile["FIXTURE"]["DatumCount"] = fixtureSeq.DatumConfigs.Count;
        for (int d = 0; d < fixtureSeq.DatumConfigs.Count; d++) {
            string datumSection = $"FIXTURE_DATUM_{d}";
            fixtureSeq.DatumConfigs[d].Save(saveFile, datumSection);
        }
    } else {
        saveFile["FIXTURE"]["DisplayName"] = "";
        saveFile["FIXTURE"]["DatumCount"] = 0;
    }

    //260526 hbk Phase 33 — Side/Bottom InspectionSequence DatumConfigs 직렬화 (SC#3 INI 라운드트립)
    SaveFixtureForSequence(saveFile, ESequence.Side, "FIXTURE_SIDE");
    SaveFixtureForSequence(saveFile, ESequence.Bottom, "FIXTURE_BOTTOM");

    saveFile["SHOTS"]["Count"] = Shots.Count;
    // ... (L128 이후 byte-identical)
```

그리고 클래스 내부 (L160 SavePhase6Format 종료 직후 또는 ClearShots 근처) 에 헬퍼 메서드 추가:
```csharp
//260526 hbk Phase 33 — 시퀀스별 FIXTURE 저장 헬퍼 (SC#3, Side/Bottom 라운드트립)
private void SaveFixtureForSequence(IniFile saveFile, ESequence seqId, string sectionPrefix) {
    var seq = ResolveFixtureSequence(seqId);
    if (seq == null) {
        saveFile[sectionPrefix]["DisplayName"] = "";
        saveFile[sectionPrefix]["DatumCount"] = 0;
        return;
    }
    saveFile[sectionPrefix]["DisplayName"] = seq.GetDisplayName() ?? "";
    saveFile[sectionPrefix]["DatumCount"] = seq.DatumConfigs.Count;
    for (int d = 0; d < seq.DatumConfigs.Count; d++) {
        string datumSection = $"{sectionPrefix}_DATUM_{d}";
        seq.DatumConfigs[d].Save(saveFile, datumSection);
    }
}
```

### Part C — LoadPhase6Format 시퀀스 순회 확장

기존 L181-194 의 Top FIXTURE 로드 블록을 **유지** 하되, 그 뒤에 Side/Bottom 로드 블록 추가.

BEFORE (L181-195):
```csharp
// --- Fixture (InspectionSequence) ---
var fixtureSeq = ResolveFixtureSequence();
if (fixtureSeq != null && loadFile.ContainsSection("FIXTURE")) {
    fixtureSeq.DisplayName = loadFile["FIXTURE"]["DisplayName"].ToString() ?? "";
    int datumCount = loadFile["FIXTURE"]["DatumCount"].ToInt();
    if (datumCount < 0) datumCount = 0;
    fixtureSeq.DatumConfigs.Clear();
    for (int d = 0; d < datumCount; d++) {
        string datumSection = $"FIXTURE_DATUM_{d}";
        if (!loadFile.ContainsSection(datumSection)) continue;
        var datum = fixtureSeq.AddDatum();
        datum.Load(loadFile, datumSection);
    }
}

// --- Shots ---
```

AFTER:
```csharp
// --- Fixture (InspectionSequence) ---
// Top FIXTURE — 기존 INI 호환 (byte-identical, D-06)
var fixtureSeq = ResolveFixtureSequence();
if (fixtureSeq != null && loadFile.ContainsSection("FIXTURE")) {
    fixtureSeq.DisplayName = loadFile["FIXTURE"]["DisplayName"].ToString() ?? "";
    int datumCount = loadFile["FIXTURE"]["DatumCount"].ToInt();
    if (datumCount < 0) datumCount = 0;
    fixtureSeq.DatumConfigs.Clear();
    for (int d = 0; d < datumCount; d++) {
        string datumSection = $"FIXTURE_DATUM_{d}";
        if (!loadFile.ContainsSection(datumSection)) continue;
        var datum = fixtureSeq.AddDatum();
        datum.Load(loadFile, datumSection);
    }
}

//260526 hbk Phase 33 — Side/Bottom InspectionSequence DatumConfigs 로드 (SC#3 라운드트립)
LoadFixtureForSequence(loadFile, ESequence.Side, "FIXTURE_SIDE");
LoadFixtureForSequence(loadFile, ESequence.Bottom, "FIXTURE_BOTTOM");

// --- Shots ---
```

그리고 헬퍼 메서드 추가 (SaveFixtureForSequence 와 인접):
```csharp
//260526 hbk Phase 33 — 시퀀스별 FIXTURE 로드 헬퍼 (SC#3, Side/Bottom 라운드트립). 섹션 부재 시 빈 DatumConfigs 로 초기화 (기존 INI 회귀 0)
private void LoadFixtureForSequence(IniFile loadFile, ESequence seqId, string sectionPrefix) {
    var seq = ResolveFixtureSequence(seqId);
    if (seq == null) return;
    seq.DatumConfigs.Clear();
    if (!loadFile.ContainsSection(sectionPrefix)) {
        // 기존 INI (Side/Bottom FIXTURE 섹션 부재) — 빈 DatumConfigs 유지 (회귀 0)
        return;
    }
    seq.DisplayName = loadFile[sectionPrefix]["DisplayName"].ToString() ?? "";
    int datumCount = loadFile[sectionPrefix]["DatumCount"].ToInt();
    if (datumCount < 0) datumCount = 0;
    for (int d = 0; d < datumCount; d++) {
        string datumSection = $"{sectionPrefix}_DATUM_{d}";
        if (!loadFile.ContainsSection(datumSection)) continue;
        var datum = seq.AddDatum();
        datum.Load(loadFile, datumSection);
    }
}
```

### 주의
- **기존 `ResolveFixtureSequence()` (zero-arg)** 시그니처 / 본문 / 호출 사이트 (L113, L182) 모두 byte-identical 보존 — Top 호출 path 회귀 가드
- 신규 overload `ResolveFixtureSequence(ESequence)` 는 별도 메서드
- 기존 `[FIXTURE]` 섹션명 / `FIXTURE_DATUM_{d}` 키명 byte-identical 보존
- 신규 섹션명 `FIXTURE_SIDE` / `FIXTURE_BOTTOM` / `FIXTURE_SIDE_DATUM_{d}` / `FIXTURE_BOTTOM_DATUM_{d}`
- 기존 Phase 6 INI (Top FIXTURE 만) 로드 시: ContainsSection("FIXTURE_SIDE") = false → 빈 DatumConfigs 초기화 → 회귀 0
- 기존 `Save(IniFile)` / `Load(IniFile)` 공개 시그니처 변경 0 — 외부 호출자 (Sequence/SequenceHandler.cs L113, L126) 영향 0
- 신규 마커 주석 `//260526 hbk Phase 33` 모든 신규 라인에 부착 (Phase 20 D-12 stacking)
  </action>
  <verify>
    <automated>grep -c "ResolveFixtureSequence(ESequence" WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs returns at least 3 (1 정의 + 2 호출 in helpers)</automated>
    <automated>grep -c "FIXTURE_SIDE" WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs returns at least 2 (Save + Load 호출)</automated>
    <automated>grep -c "FIXTURE_BOTTOM" WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs returns at least 2</automated>
    <automated>grep -c "SaveFixtureForSequence" WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs returns at least 3 (1 정의 + 2 호출)</automated>
    <automated>grep -c "LoadFixtureForSequence" WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs returns at least 3</automated>
    <automated>grep -c "260526 hbk Phase 33" WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs returns at least 4</automated>
    <automated>git diff WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs returns 0 lines (D-06)</automated>
    <automated>git diff WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs returns 0 lines (D-06)</automated>
    <automated>msbuild /p:Configuration=Debug /p:Platform=x64 WPF_Example/DatumMeasurement.csproj exits 0</automated>
  </verify>
  <acceptance_criteria>
    - `ResolveFixtureSequence()` (zero-arg) 본문 byte-identical 보존 (Top 폴백 — 다른 호출 site 회귀 0)
    - 신규 `ResolveFixtureSequence(ESequence)` overload 존재
    - SavePhase6Format L113-124 의 기존 `[FIXTURE]` 저장 블록 (Top) byte-identical 보존 — saveFile["FIXTURE"] 키 + FIXTURE_DATUM_{d} 섹션명 변경 0
    - SavePhase6Format 본문에 `SaveFixtureForSequence(saveFile, ESequence.Side, "FIXTURE_SIDE")` + `SaveFixtureForSequence(saveFile, ESequence.Bottom, "FIXTURE_BOTTOM")` 호출 2 라인 추가
    - LoadPhase6Format 본문에 `LoadFixtureForSequence(loadFile, ESequence.Side, "FIXTURE_SIDE")` + `LoadFixtureForSequence(loadFile, ESequence.Bottom, "FIXTURE_BOTTOM")` 호출 2 라인 추가
    - LoadPhase6Format 의 기존 `[FIXTURE]` 로드 블록 (L181-194) byte-identical 보존
    - 신규 헬퍼 메서드 `SaveFixtureForSequence` + `LoadFixtureForSequence` 정의 (위 action 의 명시된 본문)
    - `Save(IniFile)` / `Load(IniFile)` 공개 시그니처 byte-identical (외부 호출자 영향 0)
    - msbuild Debug/x64 PASS, CS error 0, 신규 warning 0
    - InspectionSequence.cs / Action_FAIMeasurement.cs / VisionResponsePacket.cs git diff = 0 라인 (D-06)
  </acceptance_criteria>
  <done>
InspectionRecipeManager 가 시퀀스별 (Top/Side/Bottom) FIXTURE 섹션 직렬화 지원. Top `[FIXTURE]` 형식 byte-identical (기존 INI 호환). Side/Bottom 의 DatumConfigs 라운드트립 가능. msbuild PASS.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INI 파일 ↔ RecipeManager | 기존 Phase 6 INI (Top FIXTURE 만) 와 신규 Phase 33 INI (Top + Side + Bottom FIXTURE) 의 호환성 |
| Top 호출 site → ResolveFixtureSequence | Save/Load 의 Top 폴백 경로 변경 시 v1.0/v1.1 의 Top 검사 회귀 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05 | Tampering | Top `[FIXTURE]` 섹션명 / 키명 변경 회귀 | mitigate | 기존 ResolveFixtureSequence() zero-arg + L113/L182 호출 byte-identical 보존. acceptance_criteria 명시 |
| T-33-06 | Tampering | InspectionSequence.AddDatum 변경으로 Top Datum 추가 동작 회귀 | mitigate | InspectionSequence.cs / Action_FAIMeasurement.cs git diff = 0 lines (D-06) — automated 검증 |
| T-33-07 | Information Disclosure | 기존 Phase 6 INI (Top FIXTURE 만) 로드 시 Side/Bottom DatumConfigs 의 stale 데이터 잔존 | mitigate | LoadFixtureForSequence 에서 ContainsSection 확인 전에 `seq.DatumConfigs.Clear()` 호출 — 빈 리스트로 초기화 |
| T-33-08 | Denial-of-Service | ESequence.Side / Bottom 시퀀스 미등록 시 ResolveFixtureSequence overload 가 null 반환 | accept | 헬퍼가 null 가드 — null 이면 빈 DisplayName + DatumCount=0 저장, Load 는 early return |
| T-33-09 | Information Disclosure | Side/Bottom FIXTURE 섹션의 DatumConfig.Save 가 신규 필드 누락 | accept | DatumConfig.Save 는 Top 과 동일 코드 path — ParamBase reflection 기반 자동 직렬화, 시퀀스 무관 |
</threat_model>

<verification>
### 자동 검증 (acceptance_criteria 의 grep / msbuild)
### 인계 사항 (Wave 3 — Plan 33-03 UAT)
- INI 라운드트립 SIMUL UAT: Side/Bottom 시퀀스에 Datum 추가 → Save → 앱 재시작 → Load → DatumConfigs 유지 확인
- Top 회귀 검증: 기존 Top recipe 로드 → Save → diff 0 라인 (또는 의미적 동등)
- Bottom Multi-Die → FAI 매핑은 v1.2 이연 — Phase 33 sign-off 시 사용자 수동 Shot/FAI 추가로 Bottom Datum + FAI 측정 1 회 검증
</verification>

<success_criteria>
- SC#3 (INI 라운드트립) 보장: Side/Bottom DatumConfigs 가 신규 FIXTURE_SIDE / FIXTURE_BOTTOM 섹션으로 라운드트립
- Top 회귀 0: Top `[FIXTURE]` 섹션 직렬화 byte-identical, 기존 호출 site 영향 0
- 기존 Phase 6 INI 호환: Side/Bottom FIXTURE 섹션 부재 시 빈 DatumConfigs 초기화
- msbuild Debug/x64 PASS, 신규 warning 0 (Plan 33-01 의 CS0618 4건 외 추가 없음)
- D-06 가드: InspectionSequence.cs / Action_FAIMeasurement.cs / VisionResponsePacket.cs git diff = 0 라인
- Open Question 2 결정 문서화: Die_{i}_X / Y / Angle 3 FAI 분리 방침 lock — 실제 데이터 생성은 v1.2 이연
</success_criteria>

<output>
After completion, create `.planning/phases/33-side-bottom-inspectionsequence-migration/33-02-SUMMARY.md` documenting:
  - InspectionRecipeManager.cs 변경 라인 수 (신규 헬퍼 2개 + 호출 4 라인 + 마커 주석)
  - InspectionSequence.cs / Action_FAIMeasurement.cs / VisionResponsePacket.cs git diff = 0 라인 (D-06 회귀 가드 증명)
  - 기존 Top FIXTURE 직렬화 byte-identical 증명 (코드 차이 보고)
  - Open Question 1 해법 인용 (시퀀스별 헬퍼 + zero-arg overload 보존)
  - Open Question 2 결정 인용 (3 FAI 분리, 자동 데이터 생성은 v1.2 이연)
  - Wave 3 인계: Side Datum 추가 / 티칭 / FAI 측정 SIMUL UAT + Top 회귀 SIMUL 검증 + msbuild
</output>
