---
phase: 33-side-bottom-inspectionsequence-migration
plan: 03
type: execute
wave: 3
depends_on: ["33-01", "33-02"]
files_modified:
  - .planning/phases/33-side-bottom-inspectionsequence-migration/33-UAT.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "msbuild Debug/x64 Rebuild PASS — 신규 CS error 0, 신규 warning 0 (Plan 33-01 의 CS0618 4건 외 추가 없음)"
    - "Side 시퀀스에서 Datum 추가 + 티칭 + FAI 측정이 정상 동작 (사용자 SIMUL 검증)"
    - "Bottom 시퀀스에서 Datum 추가 + 티칭 + FAI 측정이 정상 동작 (사용자 SIMUL 검증)"
    - "Top 시퀀스 검사 동작이 Phase 33 변경 전과 동일 — 회귀 0 (사용자 SIMUL 검증)"
    - "Side/Bottom recipe Save → 재로드 라운드트립 후 DatumConfigs 유지 확인"
  artifacts:
    - path: ".planning/phases/33-side-bottom-inspectionsequence-migration/33-UAT.md"
      provides: "Sign-off 결과 + 5 테스트 진행 / 결과"
      contains: "result: PASS"
  key_links:
    - from: "사용자 UI 조작 (Side 노드 선택 → Datum 추가)"
      to: "InspectionSequence.AddDatum"
      via: "MainView Datum 티칭 흐름"
      pattern: "Phase 13-04 / Phase 12-03 의 Datum 티칭 UI 패턴 재사용"
    - from: "사용자 INI Save → 재로드"
      to: "InspectionRecipeManager.Save/Load + FIXTURE_SIDE/BOTTOM 섹션"
      via: "Plan 33-02 의 신규 헬퍼 메서드"
      pattern: "FIXTURE_SIDE_DATUM_0 / FIXTURE_BOTTOM_DATUM_0"
---

<objective>
Plan 33-01 + 33-02 의 변경이 Phase 33 의 5 success criteria (SC#1~SC#5) 를 모두 충족하는지 SIMUL_MODE 에서 사용자가 직접 검증한다.

테스트 범위:
1. msbuild Debug/x64 Rebuild — 신규 error/warning 0
2. Side 시퀀스 Datum 티칭 + FAI 측정 SIMUL
3. Bottom 시퀀스 Datum 티칭 + FAI 측정 SIMUL
4. Top 시퀀스 회귀 SIMUL (Phase 33 변경 전과 동일 동작)
5. INI 라운드트립 검증 (Side/Bottom recipe Save → 재로드)

Purpose: Phase 33 sign-off — Side/Bottom 도 Datum + Multi-Datum + Dynamic FAI 구조 사용 가능함을 사용자가 1차 검증.
Output: 33-UAT.md 5 테스트 결과 + signed_off 표시.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-side-bottom-inspectionsequence-migration/33-CONTEXT.md
@.planning/phases/33-side-bottom-inspectionsequence-migration/33-PATTERNS.md
@.planning/phases/33-side-bottom-inspectionsequence-migration/33-01-SUMMARY.md
@.planning/phases/33-side-bottom-inspectionsequence-migration/33-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: msbuild Debug/x64 Rebuild 자동 검증</name>
  <files>(read-only — build artifact only)</files>
  <read_first>
    - .planning/phases/33-side-bottom-inspectionsequence-migration/33-01-SUMMARY.md
    - .planning/phases/33-side-bottom-inspectionsequence-migration/33-02-SUMMARY.md
  </read_first>
  <action>
앱이 실행 중이면 종료 (exe 잠금 회피 — MSB3027 방지). 그 후 다음 명령 실행:

```cmd
msbuild WPF_Example\DatumMeasurement.csproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal
```

결과 검증:
- exit code = 0
- "0 Error(s)" 라인 존재
- Warning 수가 **Phase 22 baseline (6 warnings) + Plan 33-01 의 의도된 CS0618 4건 = 최대 10 warnings** 이하
- 신규 CS warning 발생 시 (CS0618 외 카테고리) 원인 분석 후 Plan 33-01 / 33-02 에 hotfix 적용

이 task 는 자동 — 사용자 개입 없음.
  </action>
  <verify>
    <automated>msbuild exit code = 0</automated>
    <automated>build output 에 "0 Error(s)" 라인 존재</automated>
    <automated>warning 카운트 ≤ 10 (Phase 22 baseline 6 + 의도된 CS0618 4)</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 Rebuild exits 0
    - CS error 0
    - 신규 warning 카테고리 = CS0618 (TopSequence / BottomSequence / TopInspectionAction / BottomInspectionAction 인스턴스화 4건) 만, 그 외 신규 카테고리 0
    - 결과를 33-UAT.md Test 1 에 기록
  </acceptance_criteria>
  <done>msbuild 결과가 33-UAT.md Test 1 에 기록되고 PASS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Side 시퀀스 SIMUL UAT — Datum 추가 + 티칭 + FAI 측정</name>
  <what-built>
    Plan 33-01 으로 Side 시퀀스가 InspectionSequence 인스턴스로 등록됨.
    Plan 33-02 로 Side 의 DatumConfigs 가 INI 에 [FIXTURE_SIDE] 섹션으로 직렬화됨.
    이로 인해 Side 노드에서 Datum 추가 / 티칭 / FAI 측정이 Top 과 동일한 흐름으로 가능해야 함.
  </what-built>
  <how-to-verify>
    1. DatumMeasurement.exe 실행 (Debug/x64 빌드)
    2. UI 좌측 트리에서 **Side 시퀀스 노드** 선택
    3. Side 노드에 Shot 추가 — Shot 노드에 SimulImagePath 로 적당한 Side 카메라 SIMUL 이미지 1장 지정
    4. Side 시퀀스 노드에서 Datum 추가 (`Datum_1`) — DatumConfig PropertyGrid 가 노출되는지 확인
    5. AlgorithmType = TwoLineIntersect 설정 후 Line1/Line2 ROI 티칭 (Phase 12-03 흐름)
    6. Datum_1 티칭 성공 → LastTeachSucceeded = true → 시각화 오버레이 표시 확인 (Phase 13-04 흐름)
    7. Side Shot 에 FAI 1개 추가 후 측정 타입 (예: EdgeToLineDistance) 지정, ROI 티칭
    8. **검사 1회 실행** (TCP 또는 UI 트리거) — Datum 자동 보정 → FAI 측정 → MeasuredValue mm 표시 + OK/NG 판정
    
    Expected:
    - Side 노드가 Top 노드와 동일한 UI 흐름 (Datum 추가 버튼 / Shot 추가 / FAI 추가 / 측정 결과 표시) 으로 동작
    - 검사 실행 시 InspectionSequence 의 DatumPhase → Grab → Measure 흐름이 Side 카메라 + 조명으로 실행
    - 측정값 (mm) + 판정 (OK/NG) 이 UI 에 표시
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (예: "Side 노드 PropertyGrid 가 Datum 노드에서 안 뜸" / "검사 실행 시 NullReferenceException" 등 구체적 증상)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Bottom 시퀀스 SIMUL UAT — Datum 추가 + 티칭 + FAI 측정</name>
  <what-built>
    Plan 33-01 으로 Bottom 시퀀스가 InspectionSequence 인스턴스로 등록됨.
    Plan 33-02 로 Bottom 의 DatumConfigs 가 INI 에 [FIXTURE_BOTTOM] 섹션으로 직렬화됨.
    Bottom 도 Side 와 동일하게 Multi-Datum + Dynamic FAI 구조 사용 가능.
    (Multi-Die → FAI 자동 매핑은 v1.2 이연 — 사용자가 수동 Shot/FAI 추가 시나리오로 검증)
  </what-built>
  <how-to-verify>
    1. UI 좌측 트리에서 **Bottom 시퀀스 노드** 선택
    2. Bottom 노드에 Shot 추가 — Bottom 카메라 SIMUL 이미지 1장 지정
    3. Bottom 노드에서 Datum 추가 (`Datum_1`) — AlgorithmType 선택 (TwoLineIntersect / CircleTwoHorizontal / VerticalTwoHorizontal 중 적절한 것)
    4. ROI 티칭 → LastTeachSucceeded = true 확인
    5. Bottom Shot 에 FAI 1개 추가 + 측정 타입 지정, ROI 티칭
    6. **검사 1회 실행** — Datum 자동 보정 → FAI 측정 → 결과 표시
    
    Expected:
    - Bottom 노드가 Top/Side 와 동일한 UI 흐름
    - 검사 실행 시 Bottom 카메라 + 조명으로 InspectionSequence 흐름 실행
    - 측정값 + 판정 UI 표시
    
    주의: Bottom 의 레거시 Multi-Die / Picker / Calibration 흐름은 사용 안 함 (deprecate). v1.2 의 Bottom 도메인 작업에서 자동 Multi-Die → FAI 매핑 (D-03 Option B = Die_i_X/Y/Angle 3 FAI) 구현 예정.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Top 시퀀스 회귀 SIMUL UAT — Phase 33 변경 전과 동일 동작 확인</name>
  <what-built>
    D-06 (Top 회귀 0) 가드.
    Plan 33-01: SequenceHandler L31 (Top InspectionSequence 등록) byte-identical 보존.
    Plan 33-02: ResolveFixtureSequence() zero-arg + L113/L182 호출 byte-identical 보존, `[FIXTURE]` 섹션 직렬화 byte-identical.
    InspectionSequence.cs / Action_FAIMeasurement.cs 0 라인 변경.
    이 모든 가드가 Top 검사 흐름에 회귀 0 임을 사용자가 직접 검증.
  </what-built>
  <how-to-verify>
    1. Top 시퀀스의 기존 recipe (Phase 23 / 23.1 검증된 A1~A5 recipe) 로드
    2. recipe Load 시 Top FIXTURE 섹션의 DatumConfigs (Datum B / Datum C) 정상 로드 확인 — PropertyGrid 에 노출
    3. Top 시퀀스 검사 1회 실행 (Phase 23.1 sign-off 시 PASS 였던 시나리오)
    4. A1~A5 측정값 + OK/NG 판정 표시 확인 — Phase 23.1 결과와 일치
    5. (선택) Save 후 saved INI 의 `[FIXTURE]` 섹션 내용을 Plan 33-01/02 변경 전과 diff — 동일해야 함
    
    Expected:
    - Top 검사 동작이 Phase 33 변경 전 (Phase 23.1 sign-off 시점) 과 100% 동일
    - 측정값 / 판정 / UI 표시 / TCP 응답 모두 동일
    - `[FIXTURE]` 섹션 직렬화 byte-identical
    
    회귀 발견 시: Plan 33-01 또는 33-02 의 변경 라인 검토 → D-06 위반 라인 식별 → hotfix
  </how-to-verify>
  <resume-signal>Type "approved" or describe regression (예: "Top Datum B 티칭 결과 PropertyGrid 에 안 뜸" / "A1 측정값 mm 값이 Phase 23.1 결과와 다름")</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: INI 라운드트립 검증 — Side/Bottom recipe Save → 재로드 후 DatumConfigs 유지</name>
  <what-built>
    Plan 33-02 의 SaveFixtureForSequence / LoadFixtureForSequence 헬퍼 + FIXTURE_SIDE / FIXTURE_BOTTOM 섹션.
    이로 인해 Side/Bottom 의 DatumConfigs 가 INI 저장 후 재로드 시 동일하게 복원되어야 함.
  </what-built>
  <how-to-verify>
    1. Task 2 / 3 에서 작성한 Side + Bottom recipe (Datum + Shot + FAI 포함) 를 명시 이름으로 Save
    2. 저장된 INI 파일 열어서 다음 섹션 존재 확인:
       - `[FIXTURE]` (Top — Top Datum 이 있다면) — byte-identical
       - `[FIXTURE_SIDE]` (DisplayName + DatumCount=N)
       - `[FIXTURE_SIDE_DATUM_0]` (Side Datum_1 의 모든 필드)
       - `[FIXTURE_BOTTOM]` (DisplayName + DatumCount=N)
       - `[FIXTURE_BOTTOM_DATUM_0]` (Bottom Datum_1 의 모든 필드)
       - `[SHOTS]`, `[SHOT_n]`, `[SHOT_n_FAI_m]`, `[SHOT_n_FAI_m_MEAS_k]` 등 신규 형식
    3. 앱 종료 → 재실행 → 동일 recipe 재로드
    4. Side / Bottom 노드 선택 후 Datum / Shot / FAI 가 Save 시점과 동일하게 복원되었는지 확인
    5. (선택) Side 시퀀스 검사 1회 실행 → Datum 티칭 결과 (LastTeachSucceeded = false, 재티칭 필요) — 정상. DatumConfig 자체는 보존, 티칭 결과는 휘발성.
    
    Expected:
    - INI 파일에 FIXTURE_SIDE / FIXTURE_BOTTOM 섹션 + DATUM 서브섹션 존재
    - 재로드 후 Side / Bottom 의 DatumConfigs 가 동일하게 복원 (이름, AlgorithmType, ROI 좌표 등)
    - Top `[FIXTURE]` 섹션 형식 byte-identical (별도 diff 가능하면 수행)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (예: "재로드 시 Side Datum_1 의 AlgorithmType 이 빈 값으로 reset" / "FIXTURE_BOTTOM 섹션이 INI 에 없음")</resume-signal>
</task>

<task type="auto">
  <name>Task 6: 33-UAT.md 작성 + Phase 33 sign-off</name>
  <files>.planning/phases/33-side-bottom-inspectionsequence-migration/33-UAT.md</files>
  <read_first>
    - 이전 phase 의 UAT 파일 (예: .planning/phases/23.1-edgetolinedistance-roi/23.1-UAT.md) 양식 참조
    - 33-01-SUMMARY.md / 33-02-SUMMARY.md
  </read_first>
  <action>
33-UAT.md 작성. 다음 frontmatter + 본문 구조:

```markdown
---
phase: 33-side-bottom-inspectionsequence-migration
status: signed_off    # or partial / pending
created: 2026-05-26
updated: <YYMMDD>
total: 5
passed: <N>
pending: 0
---

# Phase 33 UAT — Side/Bottom InspectionSequence 마이그레이션

## Test 1 — msbuild Debug/x64 Rebuild
- Result: PASS (자동)
- Detail: exit code 0, 0 Error(s), warnings = <N> (baseline + 의도된 CS0618 4건)

## Test 2 — Side 시퀀스 Datum + FAI SIMUL
- Result: PASS / FAIL (사용자 <YYMMDD> 확인)
- Detail: <간단 결과 요약 — 측정값, 판정, 이슈 발견 시 기록>

## Test 3 — Bottom 시퀀스 Datum + FAI SIMUL
- Result: ...

## Test 4 — Top 회귀 SIMUL
- Result: ...
- D-06 가드: InspectionSequence.cs / Action_FAIMeasurement.cs git diff = 0 라인 (자동 검증) + Top FIXTURE 섹션 byte-identical (Plan 33-02 acceptance_criteria)

## Test 5 — INI 라운드트립
- Result: ...

## Summary
- Total: 5 / Passed: <N> / Pending: 0
- carry-over: <필요 시 quick task ID 또는 v1.2 이연 항목>
```

전 테스트 PASS 시 status = signed_off. 일부 FAIL 또는 incomplete 시 status = partial.

신규 carry-over 항목 (예: Bottom Multi-Die 자동 매핑 v1.2 이연 명시):
- **CO-33-01**: Bottom Multi-Die 자동 FAI 매핑 (Die_i_X / Y / Angle 3 FAI 분리, D-03 Option B) — v1.2 Bottom 도메인 작업으로 이연. Phase 33 sign-off 시점에는 사용자 수동 Shot/FAI 추가로 Bottom 검사 검증.
- **CO-33-02** (해당 시): Phase 33 에서 발견된 기타 이슈 — 사용자 보고 사항 반영

git commit:
```
docs(33): Phase 33 UAT sign-off — Side/Bottom InspectionSequence 마이그레이션 (<N>/5 PASS)
```
  </action>
  <verify>
    <automated>cat .planning/phases/33-side-bottom-inspectionsequence-migration/33-UAT.md | grep -c "Result: PASS" returns at least 1 (Test 1)</automated>
    <automated>cat .planning/phases/33-side-bottom-inspectionsequence-migration/33-UAT.md | grep -c "^total: 5" returns 1</automated>
    <automated>git log -1 --pretty=%s contains "Phase 33 UAT" or "Phase 33 sign-off"</automated>
  </verify>
  <acceptance_criteria>
    - 33-UAT.md 파일 존재, frontmatter 5 필드 (phase, status, created, updated, total, passed, pending)
    - 5 Test 섹션 (Test 1 msbuild / Test 2 Side / Test 3 Bottom / Test 4 Top 회귀 / Test 5 INI 라운드트립)
    - 각 Test 에 Result + Detail 기록
    - Summary 섹션 + total/passed/pending 계산
    - carry-over CO-33-01 (Bottom Multi-Die 자동 매핑 v1.2 이연) 명시
    - 사용자 sign-off 확인 시 status = signed_off, git commit 완료
  </acceptance_criteria>
  <done>33-UAT.md 작성 + 사용자 sign-off 시 status=signed_off + git commit. Phase 33 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 사용자 UI 조작 ↔ InspectionSequence | Side/Bottom 시퀀스의 첫 실 사용 — 기존 Top 검증된 흐름을 Side/Bottom 에서도 동일하게 사용 가능한지 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-10 | Tampering | Top 회귀 발견 시 sign-off 차단 | mitigate | Task 4 (Top 회귀 SIMUL) 가 blocking checkpoint — 회귀 시 hotfix 후 재검증 |
| T-33-11 | Information Disclosure | INI 라운드트립 미작동으로 사용자 작업 손실 | mitigate | Task 5 (INI 라운드트립) 가 blocking checkpoint — Side/Bottom DatumConfigs 보존 확인 |
| T-33-12 | Denial-of-Service | exe 잠금 (MSB3027) 으로 msbuild 실패 | accept | Task 1 action 에 "앱 종료 후 빌드" 명시 |
</threat_model>

<verification>
- 5 테스트 모두 PASS 시 phase signed_off
- 일부 FAIL 시 hotfix 또는 partial sign-off + carry-over 명시
- D-06 회귀 가드 자동 검증 (git diff InspectionSequence.cs + Action_FAIMeasurement.cs = 0 라인) — Task 4 사용자 검증 + 자동 grep 양쪽 확인
</verification>

<success_criteria>
Phase 33 sign-off 조건 (CONTEXT.md Success Criteria 5 항목):
1. Side / Bottom 시퀀스가 InspectionSequence 인스턴스 (Task 2, 3 의 노드 선택 후 PropertyGrid 흐름으로 검증)
2. Side / Bottom 노드 Datum 추가 + 티칭 + FAI 측정 동작 (Task 2, 3)
3. INI Save/Load 라운드트립 유지 (Task 5)
4. msbuild Debug/x64 PASS, 신규 warning 0 (Task 1)
5. Top 검사 동작 회귀 0 (Task 4)
</success_criteria>

<output>
After completion, create `.planning/phases/33-side-bottom-inspectionsequence-migration/33-03-SUMMARY.md` documenting:
  - Task 1 msbuild 결과 (warning 카운트, error 카운트)
  - Task 2~5 의 사용자 사인오프 결과 (5 테스트 각각의 PASS/FAIL + 사용자 코멘트 요약)
  - 33-UAT.md status (signed_off / partial)
  - carry-over (CO-33-01 등)
  - Phase 33 → Phase 24 인계 (Side/Bottom 도 Datum + Dynamic FAI 사용 가능 = end-to-end 검증 prerequisite 달성)
</output>
