---
phase: 34-datum-verticaltwohorizontal-2026-05-26
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/EDatumAlgorithm.cs
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
autonomous: true
requirements:
  - SC-1   # 신규 EDatumAlgorithm 값 1개 추가, AlgorithmType 콤보박스 노출
  - SC-2   # DatumConfig 에 TeachingImagePath_Vertical 필드 추가 + ICustomTypeDescriptor 노출
  - SC-6   # INI 호환: TeachingImagePath_Vertical 미존재 시 "" 정규화 (기존 INI 회귀 0)

must_haves:
  truths:
    - "EDatumAlgorithm enum 에 VerticalTwoHorizontalDualImage 값이 추가되어 있다 (string 직렬화 그대로)"
    - "DatumConfig 가 TeachingImagePath_Vertical (string, default \"\") 프로퍼티를 노출한다"
    - "AlgorithmTypeList 콤보박스에 'VerticalTwoHorizontalDualImage' 가 표시된다"
    - "기존 INI(TeachingImagePath_Vertical 키 미존재) 로드 시 \"\" 로 정규화된다 (EnsurePerRoiDefaults)"
    - "ICustomTypeDescriptor 가 algorithm == VerticalTwoHorizontalDualImage 일 때 Line1_*/Line2_*/Circle_* 를 hide 하고, algorithm != DualImage 일 때 TeachingImagePath_Vertical 를 hide 한다"
    - "msbuild Debug/x64 Rebuild PASS, 신규 warning 0"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/EDatumAlgorithm.cs"
      provides: "Datum 알고리즘 enum + 신규 DualImage 값"
      contains: "VerticalTwoHorizontalDualImage"
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "DatumConfig 필드(TeachingImagePath_Vertical) + AlgorithmTypeList 항목 + INI 정규화 + ICustomTypeDescriptor 분기"
      contains: "TeachingImagePath_Vertical"
  key_links:
    - from: "DatumConfig.AlgorithmTypeList getter"
      to: "EDatumAlgorithm.VerticalTwoHorizontalDualImage.ToString()"
      via: "List<string> add"
      pattern: "VerticalTwoHorizontalDualImage\\.ToString"
    - from: "DatumConfig.IsHiddenForAlgorithm switch"
      to: "EDatumAlgorithm.VerticalTwoHorizontalDualImage"
      via: "case 추가 + 3개 기존 case 의 TeachingImagePath_Vertical hide 라인"
      pattern: "case EDatumAlgorithm\\.VerticalTwoHorizontalDualImage"
    - from: "DatumConfig.EnsurePerRoiDefaults() (line 563 부근)"
      to: "TeachingImagePath_Vertical null → \"\" 정규화"
      via: "Phase 22 IMG-01 패턴 1:1 복제"
      pattern: "if \\(TeachingImagePath_Vertical == null\\) TeachingImagePath_Vertical = \"\""
---

<objective>
EDatumAlgorithm.VerticalTwoHorizontalDualImage enum 값 신설 + DatumConfig 에 TeachingImagePath_Vertical 필드/AlgorithmTypeList 항목/INI 정규화/ICustomTypeDescriptor 분기 추가. msbuild Debug/x64 Rebuild PASS 까지 도달.

Purpose: Phase 34 의 데이터 모델 토대. 후속 Plan 02 (DatumFindingService 분기) / Plan 03 (MainView 티칭 UI + Action_FAIMeasurement 분기) / Plan 04 (SIMUL UAT) 가 본 플랜의 enum/필드/콤보박스/INI 정규화/PropertyGrid 분기에 의존한다.
Output: 2 파일 변경 (EDatumAlgorithm.cs 1줄 추가 + DatumConfig.cs 신규 필드 + 콤보박스 + 정규화 + ICustomTypeDescriptor 6 라인 분기), msbuild PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-PATTERNS.md

<!-- Plan 01 은 self-contained: 신규 enum 값 + DatumConfig 필드 추가뿐. 후속 plan 이 본 플랜의 산출물을 참조함 (역방향 없음). 외부 prior SUMMARY 불필요 — PATTERNS.md 가 self-analog 코드 발췌 포함. -->

<interfaces>
<!-- 변경 대상 파일에서 추출한 기존 시그니처 — 변경 금지 (확장만) -->

From WPF_Example/Custom/Sequence/Inspection/EDatumAlgorithm.cs:
```csharp
namespace ReringProject.Sequence
{
    public enum EDatumAlgorithm
    {
        TwoLineIntersect,
        CircleTwoHorizontal,
        VerticalTwoHorizontal,
        // ← 본 Plan: 여기에 VerticalTwoHorizontalDualImage 추가
    }
}
```

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (line 42-44, 기존 TeachingImagePath):
```csharp
[Category("Datum|ImageSource")]
public string TeachingImagePath { get; set; } = "";
```

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (line 74-85, 기존 AlgorithmTypeList):
```csharp
[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> AlgorithmTypeList
{
    get
    {
        return new List<string>
        {
            EDatumAlgorithm.TwoLineIntersect.ToString(),
            EDatumAlgorithm.CircleTwoHorizontal.ToString(),
            EDatumAlgorithm.VerticalTwoHorizontal.ToString(),
        };
    }
}
```

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (line 563, 기존 INI 정규화):
```csharp
if (TeachingImagePath == null) TeachingImagePath = ""; //260511 hbk Phase 22 IMG-01
```

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (line 667-687, IsHiddenForAlgorithm switch — 3 case):
```csharp
private static bool IsHiddenForAlgorithm(string name, EDatumAlgorithm alg) {
    switch (alg) {
        case EDatumAlgorithm.TwoLineIntersect:        ...
        case EDatumAlgorithm.CircleTwoHorizontal:     ...
        case EDatumAlgorithm.VerticalTwoHorizontal:   ...
    }
    return false;
}
```

**ParamBase 직렬화 제약 (Phase 12 D-09 + 본 Plan 적용):**
- AlgorithmType 은 string 으로 저장 (`AlgorithmType { get; set; } = "TwoLineIntersect"`).
- TeachingImagePath_Vertical 은 string → ParamBase reflection 자동 직렬화 case "String" 사용 (Phase 22 IMG-01 와 동일).
- ICustomTypeDescriptor 의 GetProperties() 는 ParamBase INI 직렬화 경로(System.Reflection)와 무관 → hide 해도 INI 저장은 그대로 됨.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: EDatumAlgorithm.VerticalTwoHorizontalDualImage enum 값 추가</name>

  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/EDatumAlgorithm.cs (전체, 13 라인)
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-PATTERNS.md (section "1) EDatumAlgorithm.cs")
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md (D-34-03)
  </read_first>

  <behavior>
    - Test 1: EDatumAlgorithm enum 안에 'VerticalTwoHorizontalDualImage' 식별자가 존재한다.
    - Test 2: 기존 3 값 순서(TwoLineIntersect → CircleTwoHorizontal → VerticalTwoHorizontal)가 보존된다 (INI string 직렬화 하위 호환).
    - Test 3: 신규 값은 enum body 의 마지막 (4번째) 위치에 append 된다.
    - Test 4: 신규 라인에 `//260527 hbk Phase 34 D-34-03` 주석이 포함된다.
  </behavior>

  <action>
WPF_Example/Custom/Sequence/Inspection/EDatumAlgorithm.cs 의 enum body (현재 line 10-12 의 `TwoLineIntersect`, `CircleTwoHorizontal`, `VerticalTwoHorizontal` 3 값) 마지막에 신규 값 1줄을 append 한다.

기존 line 12 (`VerticalTwoHorizontal,      //260423 hbk 수직 ROI ∩ 수평 2-ROI concat`) 바로 다음 줄에 다음 라인을 정확히 삽입:

```csharp
        VerticalTwoHorizontalDualImage, //260527 hbk Phase 34 D-34-03 — 가로축 이미지(H_A+H_B) + 세로축 이미지(V) 분리 (Side fixture). enum 순서 보존(append) — 기존 string 직렬화 INI 회귀 0.
```

**제약 (D-34-03):**
- enum body 안 다른 라인 수정 0. 끝에 append 만.
- ParamBase 가 enum 을 미지원 → DatumConfig.AlgorithmType 은 string 으로 저장됨 (Phase 12 D-09). 신규 값 INI 키 = ToString() = "VerticalTwoHorizontalDualImage".
- AlgorithmTypeEnum 게터의 미지원 문자열 폴백(DatumConfig.cs:95)은 그대로 → 미존재 INI 키 → TwoLineIntersect 폴백 (회귀 0).
- 라인 끝 주석 `//260527 hbk Phase 34 D-34-03` 형식 필수 (memory feedback_comment_convention).
  </action>

  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path 'WPF_Example/Custom/Sequence/Inspection/EDatumAlgorithm.cs' -Pattern 'VerticalTwoHorizontalDualImage'"</automated>
  </verify>

  <acceptance_criteria>
    - EDatumAlgorithm.cs 안에 `VerticalTwoHorizontalDualImage,` 식별자가 정확히 한 번 등장 (grep "VerticalTwoHorizontalDualImage" 카운트 = 1)
    - 기존 3 enum 값 `TwoLineIntersect`, `CircleTwoHorizontal`, `VerticalTwoHorizontal` 의 순서가 보존 (grep 라인 번호 ascending)
    - 신규 라인에 `//260527 hbk Phase 34 D-34-03` 주석 포함 (grep "260527 hbk Phase 34 D-34-03" 매치 ≥ 1)
    - 파일 라인 수 = 기존 14 라인 + 1 = 15 라인 (또는 기존 라인 수 + 1)
  </acceptance_criteria>

  <done>EDatumAlgorithm.cs 에 VerticalTwoHorizontalDualImage 값이 마지막에 추가되어 있고, 주석 컨벤션을 따른다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DatumConfig 에 TeachingImagePath_Vertical 필드 + AlgorithmTypeList 신규 항목 추가</name>

  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (offset=30, limit=130 — 필드 영역 + AlgorithmTypeList)
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-PATTERNS.md (sections "2) DatumConfig.cs — 필드 추가", "3) DatumConfig.cs — AlgorithmTypeOptions 리스트")
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md (D-34-04, D-34-05)
    - .planning/phases/22-image-dual-structure/22-CONTEXT.md (IMG-01 패턴)
  </read_first>

  <behavior>
    - Test 1: DatumConfig 가 `public string TeachingImagePath_Vertical { get; set; } = "";` 자동 프로퍼티를 노출한다.
    - Test 2: 신규 필드는 기존 `TeachingImagePath` 다음 (line 44 직후) 에 배치된다 — PropertyGrid Category("Datum|ImageSource") 그룹화 유지.
    - Test 3: AlgorithmTypeList 안 List<string> 에 `EDatumAlgorithm.VerticalTwoHorizontalDualImage.ToString()` 항목이 추가된다 (기존 3 항목 보존, 마지막 append).
    - Test 4: ParamBase reflection 자동 직렬화 (case "String") → INI 저장/로드 코드 추가 0 (Phase 22 IMG-01 패턴).
  </behavior>

  <action>
WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs 의 두 위치를 동시 수정한다:

**위치 A — line 44 (`public string TeachingImagePath { get; set; } = "";`) 직후에 신규 필드 삽입:**

```csharp
        //260511 hbk Phase 22 IMG-01 — Datum 티칭 시 사용한 기준 이미지 경로. ...
        [Category("Datum|ImageSource")]
        public string TeachingImagePath { get; set; } = "";

        //260527 hbk Phase 34 D-34-04 — 가로축 이미지(TeachingImagePath) 와 분리된 세로축 이미지 경로.
        //  algorithm == VerticalTwoHorizontalDualImage 일 때만 의미가 있으며, 그 외 algorithm 에서는 INI 에 보존되지만 미사용 (ICustomTypeDescriptor 가 hide).
        //  INI 직렬화 = ParamBase reflection String case 자동 (Phase 22 IMG-01 패턴 그대로). 키 미존재 → EnsurePerRoiDefaults 에서 "" 정규화 (D-34-11, Task 3).
        [Category("Datum|ImageSource")]
        public string TeachingImagePath_Vertical { get; set; } = ""; //260527 hbk Phase 34 D-34-04
```

**위치 B — line 78-84 (`AlgorithmTypeList` getter 내부 List<string> initializer) 의 `VerticalTwoHorizontal.ToString(),` 다음 줄에 신규 항목 추가:**

기존:
```csharp
        public List<string> AlgorithmTypeList
        {
            get
            {
                return new List<string>
                {
                    EDatumAlgorithm.TwoLineIntersect.ToString(),
                    EDatumAlgorithm.CircleTwoHorizontal.ToString(),
                    EDatumAlgorithm.VerticalTwoHorizontal.ToString(),
                };
            }
        }
```

변경 후:
```csharp
        public List<string> AlgorithmTypeList
        {
            get
            {
                return new List<string>
                {
                    EDatumAlgorithm.TwoLineIntersect.ToString(),
                    EDatumAlgorithm.CircleTwoHorizontal.ToString(),
                    EDatumAlgorithm.VerticalTwoHorizontal.ToString(),
                    EDatumAlgorithm.VerticalTwoHorizontalDualImage.ToString(), //260527 hbk Phase 34 D-34-05
                };
            }
        }
```

**제약 (D-34-04, D-34-05):**
- 위치 A: PATTERNS.md section #2 의 코드 발췌를 그대로 사용. `[Category("Datum|ImageSource")]` 어트리뷰트 필수 (기존 TeachingImagePath 와 동일 그룹).
- 위치 B: 1줄 append (기존 3 항목 보존). 한글 라벨 매핑 없이 ToString() 그대로 (D-34-05).
- IOfflineImageParam 인터페이스(GetLatestImagePath/SetLatestImagePath, line 51-62)는 변경 0 — 신규 필드는 별도 UI hook (Plan 03 영역).
- 모든 신규 라인에 `//260527 hbk Phase 34 D-34-04` 또는 `//260527 hbk Phase 34 D-34-05` 주석 포함.
  </action>

  <verify>
    <automated>powershell -NoProfile -Command "$f = 'WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs'; $hasField = (Select-String -Path $f -Pattern 'public string TeachingImagePath_Vertical' -Quiet); $hasCombo = (Select-String -Path $f -Pattern 'EDatumAlgorithm\.VerticalTwoHorizontalDualImage\.ToString' -Quiet); if ($hasField -and $hasCombo) { Write-Host 'PASS' } else { Write-Host 'FAIL: field='$hasField' combo='$hasCombo; exit 1 }"</automated>
  </verify>

  <acceptance_criteria>
    - DatumConfig.cs 에 `public string TeachingImagePath_Vertical { get; set; } = "";` 줄이 정확히 1개 존재
    - 신규 필드 위치가 line 45 ~ line 50 사이 (기존 line 44 TeachingImagePath 의 직후 — Category 그룹화 유지)
    - 신규 필드에 `[Category("Datum|ImageSource")]` 어트리뷰트 부착 (직전 줄 또는 같은 줄)
    - AlgorithmTypeList getter 안에 `EDatumAlgorithm.VerticalTwoHorizontalDualImage.ToString()` 정확히 1개 존재
    - 기존 3 항목 (`TwoLineIntersect.ToString()`, `CircleTwoHorizontal.ToString()`, `VerticalTwoHorizontal.ToString()`) 모두 존재 (grep 카운트 ≥ 3)
    - 신규 라인 2곳에 `//260527 hbk Phase 34` 주석 포함 (grep "260527 hbk Phase 34 D-34-04" 매치 ≥ 1 + "260527 hbk Phase 34 D-34-05" 매치 ≥ 1)
  </acceptance_criteria>

  <done>DatumConfig.cs 가 TeachingImagePath_Vertical 필드 + AlgorithmTypeList 신규 항목 2곳을 모두 갖는다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: DatumConfig.EnsurePerRoiDefaults 에 INI null 정규화 추가 + IsHiddenForAlgorithm 분기 6라인 + msbuild Debug/x64 PASS</name>

  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (offset=555, limit=140 — EnsurePerRoiDefaults 끝부분 + ICustomTypeDescriptor IsHiddenForAlgorithm)
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-PATTERNS.md (sections "4) DatumConfig.cs — INI 정규화 hook", "5) DatumConfig.cs — ICustomTypeDescriptor 분기")
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md (D-34-11, D-34-12, D-34-04 의 "DualImage 가 아닌 algorithm 에서 TeachingImagePath_Vertical hide" 요구)
    - .planning/phases/17-datum-teaching-validation-ux/17-CONTEXT.md (ICustomTypeDescriptor 패턴)
  </read_first>

  <behavior>
    - Test 1 (INI 정규화): EnsurePerRoiDefaults 호출 후 `TeachingImagePath_Vertical == null` 인 케이스가 사라지고 "" 로 정규화된다 (Phase 22 IMG-01 패턴 1:1 복제).
    - Test 2 (DualImage case 노출): algorithm = VerticalTwoHorizontalDualImage 일 때 IsHiddenForAlgorithm 가 Line1_*/Line2_*/Circle_* 류 startWith 매치를 true 로 반환 (VTH 와 동일 hide 그룹).
    - Test 3 (DualImage case 의 Vertical/Horizontal 노출): algorithm = VerticalTwoHorizontalDualImage 일 때 `Vertical_*`, `Horizontal_A_*`, `Horizontal_B_*`, `TeachingImagePath_Vertical` 는 hide 되지 않는다 (return false).
    - Test 4 (역방향 hide): algorithm ∈ {TwoLineIntersect, CircleTwoHorizontal, VerticalTwoHorizontal} 일 때 `name == "TeachingImagePath_Vertical"` 이면 true 반환 (PropertyGrid 에서 hide).
    - Test 5: msbuild Debug/x64 Rebuild PASS, 신규 warning 0.
  </behavior>

  <action>
WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs 의 두 위치를 수정한다:

**위치 A — line 563 (`if (TeachingImagePath == null) TeachingImagePath = "";`) 직후에 신규 정규화 1줄 추가:**

기존 (line 563):
```csharp
            if (TeachingImagePath == null) TeachingImagePath = ""; //260511 hbk Phase 22 IMG-01 — INI 키 미존재 시 null 가드 ...
```

변경 후 (line 563 다음 줄 삽입):
```csharp
            if (TeachingImagePath == null) TeachingImagePath = ""; //260511 hbk Phase 22 IMG-01
            if (TeachingImagePath_Vertical == null) TeachingImagePath_Vertical = ""; //260527 hbk Phase 34 D-34-11 — Phase 22 IMG-01 패턴 1:1 복제. algorithm 무관 일률 정규화 — DualImage 가 아니어도 필드 존재. 기존 INI(키 미존재) 로드 → "" 정규화 → 1-image algorithm 회귀 0 (D-34-12).
```

**위치 B — line 667-687 (`IsHiddenForAlgorithm` switch) 의 3가지 변경:**

**B-1: 기존 3 case 안에 `name == "TeachingImagePath_Vertical"` hide 라인 추가** (DualImage 외 algorithm 에서 신규 필드 숨김).

기존 (line 669-684):
```csharp
        private static bool IsHiddenForAlgorithm(string name, EDatumAlgorithm alg) {
            switch (alg) {
                case EDatumAlgorithm.TwoLineIntersect:
                    if (name.StartsWith("Circle_") || name.StartsWith("CircleROI_") || name.StartsWith("CircleCenter_") || name.StartsWith("CircleDetected_")) return true;
                    if (name.StartsWith("Vertical_")) return true;
                    if (name.StartsWith("Horizontal_A_") || name.StartsWith("Horizontal_B_")) return true;
                    return false;
                case EDatumAlgorithm.CircleTwoHorizontal:
                    if (name.StartsWith("Line1_") || name.StartsWith("Line1Detected_")) return true;
                    if (name.StartsWith("Line2_") || name.StartsWith("Line2Detected_")) return true;
                    if (name.StartsWith("Vertical_")) return true;
                    if (name == "Circle_EdgeDirection") return true; //260503 hbk Phase 17 D-03
                    return false;
                case EDatumAlgorithm.VerticalTwoHorizontal:
                    if (name.StartsWith("Line1_") || name.StartsWith("Line1Detected_")) return true;
                    if (name.StartsWith("Line2_") || name.StartsWith("Line2Detected_")) return true;
                    if (name.StartsWith("Circle_") || name.StartsWith("CircleROI_") || name.StartsWith("CircleCenter_") || name.StartsWith("CircleDetected_")) return true;
                    return false;
            }
            return false;
        }
```

각 case 의 첫 번째 라인 직전(또는 적당한 위치) 에 다음 1줄을 추가:
```csharp
                    if (name == "TeachingImagePath_Vertical") return true; //260527 hbk Phase 34 D-34-04 — DualImage 전용 필드 hide
```

수정 후 (예시):
```csharp
        private static bool IsHiddenForAlgorithm(string name, EDatumAlgorithm alg) {
            switch (alg) {
                case EDatumAlgorithm.TwoLineIntersect:
                    if (name == "TeachingImagePath_Vertical") return true; //260527 hbk Phase 34 D-34-04 — DualImage 전용 필드 hide
                    if (name.StartsWith("Circle_") || name.StartsWith("CircleROI_") || name.StartsWith("CircleCenter_") || name.StartsWith("CircleDetected_")) return true;
                    if (name.StartsWith("Vertical_")) return true;
                    if (name.StartsWith("Horizontal_A_") || name.StartsWith("Horizontal_B_")) return true;
                    return false;
                case EDatumAlgorithm.CircleTwoHorizontal:
                    if (name == "TeachingImagePath_Vertical") return true; //260527 hbk Phase 34 D-34-04 — DualImage 전용 필드 hide
                    if (name.StartsWith("Line1_") || name.StartsWith("Line1Detected_")) return true;
                    if (name.StartsWith("Line2_") || name.StartsWith("Line2Detected_")) return true;
                    if (name.StartsWith("Vertical_")) return true;
                    if (name == "Circle_EdgeDirection") return true; //260503 hbk Phase 17 D-03
                    return false;
                case EDatumAlgorithm.VerticalTwoHorizontal:
                    if (name == "TeachingImagePath_Vertical") return true; //260527 hbk Phase 34 D-34-04 — DualImage 전용 필드 hide
                    if (name.StartsWith("Line1_") || name.StartsWith("Line1Detected_")) return true;
                    if (name.StartsWith("Line2_") || name.StartsWith("Line2Detected_")) return true;
                    if (name.StartsWith("Circle_") || name.StartsWith("CircleROI_") || name.StartsWith("CircleCenter_") || name.StartsWith("CircleDetected_")) return true;
                    return false;
                // ↓ B-2 신규 case 가 여기 추가됨
            }
            return false;
        }
```

**B-2: 기존 VTH case (line 680-684) 직후에 신규 DualImage case 추가** (VTH 와 hide 그룹 동일 = Line1_*/Line2_*/Circle_* hide, Vertical_*/Horizontal_A_*/Horizontal_B_* 노출). TeachingImagePath_Vertical 는 hide 하지 않음 → 노출.

```csharp
                case EDatumAlgorithm.VerticalTwoHorizontalDualImage: //260527 hbk Phase 34 D-34-04 — DualImage 변형: VTH 와 동일 hide 그룹 (Vertical_* + Horizontal_A_*/B_* 노출). 차이 = TeachingImagePath_Vertical 추가 노출 (hide 조건 없음 → default fallthrough 후 return false).
                    if (name.StartsWith("Line1_") || name.StartsWith("Line1Detected_")) return true;
                    if (name.StartsWith("Line2_") || name.StartsWith("Line2Detected_")) return true;
                    if (name.StartsWith("Circle_") || name.StartsWith("CircleROI_") || name.StartsWith("CircleCenter_") || name.StartsWith("CircleDetected_")) return true;
                    return false;
```

**제약 (D-34-04, D-34-11, D-34-12):**
- 위치 A: PATTERNS.md section #4 의 코드 발췌. algorithm 분기 없이 일률 정규화 (DualImage 가 아니어도 필드 존재 → INI 라운드트립 보장).
- 위치 B-1: 3 case 각각 첫 줄에 `if (name == "TeachingImagePath_Vertical") return true;` 추가. 기존 라인 0 수정.
- 위치 B-2: VTH case 본문 100% 복제 + case 라벨만 신규 enum 값으로 변경.
- IsHiddenForAlgorithm 의 default fallthrough `return false;` (line 686) 변경 0 — 신규 case 가 함수 안 switch 의 마지막 case 다음에 위치.
- Phase 18 CO-01 학습 (PATTERNS.md section #5 last paragraph): 만약 ICustomTypeDescriptor 동적 hide 가 PropertyGrid 일부 enumeration 에서 안 먹히면 fallback 으로 `TeachingImagePath_Vertical` 에 `[Browsable(false)]` 정적 적용 검토 — 단, 본 Plan 범위는 ICustomTypeDescriptor 분기만. 정적 fallback 은 Plan 03 UAT 후 hotfix 영역.
- 모든 신규 라인에 `//260527 hbk Phase 34 D-34-XX` 주석.

**msbuild 검증:**
- Task 1+2+3 변경 완료 후 다음 명령 실행 (Windows PowerShell):

```powershell
& "C:\Program Files (x86)\Microsoft Visual Studio\2019\BuildTools\MSBuild\Current\Bin\MSBuild.exe" `
  WPF_Example/DatumMeasurement.csproj `
  /p:Configuration=Debug /p:Platform=x64 `
  /t:Rebuild /v:minimal
```

- 종료 코드 0 (PASS)
- 신규 CS error 0
- 신규 warning 0 (기존 baseline 만 출력)

msbuild PASS 까지 도달 시 Plan 01 완료.
  </action>

  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "$f = 'WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs'; $norm = (Select-String -Path $f -Pattern 'TeachingImagePath_Vertical == null' -Quiet); $dualCase = (Select-String -Path $f -Pattern 'case EDatumAlgorithm\.VerticalTwoHorizontalDualImage' -Quiet); $hideCount = (Select-String -Path $f -Pattern 'name == \"TeachingImagePath_Vertical\"' -AllMatches | Measure-Object).Count; if ($norm -and $dualCase -and $hideCount -ge 3) { Write-Host 'PASS norm='$norm' dualCase='$dualCase' hideCount='$hideCount } else { Write-Host 'FAIL norm='$norm' dualCase='$dualCase' hideCount='$hideCount; exit 1 }"</automated>
  </verify>

  <acceptance_criteria>
    - DatumConfig.cs 에 `if (TeachingImagePath_Vertical == null) TeachingImagePath_Vertical = "";` 줄이 정확히 1개 존재 (EnsurePerRoiDefaults 안)
    - 신규 정규화 라인이 기존 `if (TeachingImagePath == null) TeachingImagePath = "";` (line 563) 의 다음 줄에 위치 (라인 차이 = 1)
    - DatumConfig.cs 에 `case EDatumAlgorithm.VerticalTwoHorizontalDualImage:` 식별자가 정확히 1개 존재 (IsHiddenForAlgorithm switch 안)
    - DatumConfig.cs 에 `if (name == "TeachingImagePath_Vertical") return true;` 줄이 정확히 3개 존재 (TwoLineIntersect / CircleTwoHorizontal / VerticalTwoHorizontal case 각각)
    - 신규 DualImage case 본문이 VTH case 와 동일 hide 그룹 (Line1_*/Line2_*/Circle_* hide, Vertical_*/Horizontal_A_*/Horizontal_B_* 비 hide) — grep 으로 4개 startWith 라인 매치
    - 신규 라인에 `//260527 hbk Phase 34` 주석 포함 (grep 카운트 ≥ 5: B-1×3 + B-2×1 + A×1)
    - msbuild Debug/x64 Rebuild 종료 코드 0 (PASS)
    - msbuild 출력에 신규 CS error 0 (`error CS` 라인 = 0)
    - msbuild 출력에 신규 warning 0 (기존 baseline 6 warnings 와 동일 — Phase 21 baseline 참조)
    - git diff 라인 카운트: EDatumAlgorithm.cs ≈ +1, DatumConfig.cs ≈ +8 ~ +12 (필드 4 + 정규화 1 + IsHidden 3+1 + 콤보박스 1)
  </acceptance_criteria>

  <done>DatumConfig.cs 가 INI 정규화 hook + ICustomTypeDescriptor 분기 (DualImage case + 3 case 의 TeachingImagePath_Vertical hide) 를 모두 갖고, msbuild Debug/x64 Rebuild 가 PASS 한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INI 파일 → DatumConfig.Load | 사용자 또는 외부 도구가 작성한 INI 가 ParamBase reflection 으로 역직렬화 (string case). 키 미존재/잘못된 값에 대한 가드 필요. |
| PropertyGrid ↔ DatumConfig | ICustomTypeDescriptor 가 동적 hide 분기 → algorithm 변경 시 잘못된 case fallthrough → 잘못된 필드 노출 → 사용자 혼란 |
| EDatumAlgorithm enum ↔ AlgorithmType string | string → enum 파싱 실패 시 폴백 동작 (TwoLineIntersect) 정확성 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01-01 | Tampering | INI TeachingImagePath_Vertical 키가 임의 문자열 (null 포함) | mitigate | EnsurePerRoiDefaults 의 `if (TeachingImagePath_Vertical == null) TeachingImagePath_Vertical = "";` 일률 정규화 (D-34-11). 빈 문자열은 소비처 (Plan 02/03) 가 IsNullOrEmpty 가드로 처리. |
| T-34-01-02 | Information Disclosure | DualImage 가 아닌 algorithm 에서 TeachingImagePath_Vertical 값이 PropertyGrid 에 노출 → 사용자가 잘못된 필드 입력 | mitigate | IsHiddenForAlgorithm 의 기존 3 case 각각에 `if (name == "TeachingImagePath_Vertical") return true;` 라인 추가 (D-34-04 역방향 hide). |
| T-34-01-03 | Denial of Service | enum 미지원 string ("VerticalTwoHorizontalDualImage" 가 아닌 임의 문자열) 로드 → AlgorithmTypeEnum 파싱 실패 | accept | 기존 AlgorithmTypeEnum 게터(line 89-97) 가 TryParse 실패 시 TwoLineIntersect 폴백 — Phase 12 D-09 보존. 신규 위협 없음. |
| T-34-01-04 | Spoofing | 신규 enum 값이 기존 INI string 충돌 (대소문자/오타) | accept | enum 끝에 append (D-34-03) → 기존 3 값 ordinal 보존. string 직렬화는 정확한 이름 매치 → 충돌 없음. |
| T-34-01-05 | Repudiation | 신규 변경 추적 누락 (어느 변경이 Phase 34 의 것인지 모름) | mitigate | 모든 신규 라인에 `//260527 hbk Phase 34 D-34-XX` 주석 (memory feedback_comment_convention). git diff + grep "260527 hbk Phase 34" 로 변경 범위 정확히 식별. |
| T-34-01-06 | Elevation of Privilege | 신규 코드가 unsafe 블록 / P/Invoke 도입 | mitigate | Plan 01 변경 범위 = enum + auto-property + 리스트 항목 + null guard + switch case. unsafe / native 도입 0 → 위협 없음. |
</threat_model>

<verification>
## Plan 01 검증

**Smoke:**
- DatumConfig.cs 컴파일 통과 — CS0103 (TeachingImagePath_Vertical 미정의) 사라짐 검증 (Plan 02/03 는 본 필드를 참조함).
- AlgorithmTypeEnum getter 가 "VerticalTwoHorizontalDualImage" 입력 시 EDatumAlgorithm.VerticalTwoHorizontalDualImage 반환 (TryParse 성공).

**File diff guards (D-34-13/14):**
- Plan 01 은 EDatumAlgorithm.cs + DatumConfig.cs 2 파일만 수정.
- 다음 파일들은 Plan 01 단계에서 **변경 0 라인** (PowerShell):
  - `git diff --numstat HEAD -- WPF_Example/Halcon/Algorithms/DatumFindingService.cs` → 0,0
  - `git diff --numstat HEAD -- WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` → 0,0
  - `git diff --numstat HEAD -- WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` → 0,0
  - `git diff --numstat HEAD -- WPF_Example/TcpServer/VisionResponsePacket.cs` → 0,0
  - `git diff --numstat HEAD -- WPF_Example/UI/ContentItem/MainView.xaml.cs` → 0,0

**Round-trip:**
- DatumConfig 인스턴스 생성 → Save("test.ini") → Load("test.ini") → TeachingImagePath_Vertical 보존 ("" 또는 사용자 값) 코드 경로 검증 (실제 실행은 Plan 04 UAT).

**msbuild:**
- Debug/x64 Rebuild 종료 코드 0
- Phase 21 baseline warning 수와 동일 (신규 warning 0)
</verification>

<success_criteria>
- [ ] EDatumAlgorithm.VerticalTwoHorizontalDualImage 값이 enum 마지막에 추가됨
- [ ] DatumConfig.TeachingImagePath_Vertical 자동 프로퍼티 노출 + [Category("Datum|ImageSource")] 어트리뷰트
- [ ] AlgorithmTypeList 에 신규 enum.ToString() 항목 1줄 추가
- [ ] EnsurePerRoiDefaults 에 null 정규화 1줄 추가 (D-34-11)
- [ ] IsHiddenForAlgorithm 에 DualImage case 1개 + 기존 3 case 의 TeachingImagePath_Vertical hide 3줄 추가 (D-34-04)
- [ ] 모든 신규 라인에 `//260527 hbk Phase 34 D-34-XX` 주석
- [ ] msbuild Debug/x64 Rebuild PASS, 신규 warning 0
- [ ] D-34-13/14 가드 — DatumFindingService.cs / Action_FAIMeasurement.cs / InspectionSequence.cs / VisionResponsePacket.cs / MainView.xaml.cs 변경 0 라인
</success_criteria>

<output>
After completion, create `.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-01-SUMMARY.md` per template — include msbuild PASS 증거 (출력 발췌) + git diff --numstat 결과 (D-34-13/14 가드 검증) + 변경 라인 수.
</output>
