---
phase: 34-datum-verticaltwohorizontal-2026-05-26
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
autonomous: true
requirements:
  - SC-3   # DatumFindingService 신규 분기 — 가로 ROI 2개 image1, 세로 ROI 1개 image2

must_haves:
  truths:
    - "DatumFindingService 가 VerticalTwoHorizontalDualImage algorithm 에 대해 두 이미지(가로축/세로축)를 입력받는 신규 오버로드를 노출한다 (Find + Teach 양쪽)"
    - "신규 오버로드가 가로축 이미지에서 Horizontal_A + Horizontal_B 에지를 검출하고, 세로축 이미지에서 Vertical 에지를 검출하여 두 결과를 IntersectionLl 로 결합한다"
    - "기존 TryFindDatum(HImage, ...)/TryTeachDatum(HImage, ...) 시그니처는 변경 0 라인 — 기존 3 algorithm 회귀 0"
    - "msbuild Debug/x64 Rebuild PASS, 신규 warning 0"
  artifacts:
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "TryFindVerticalTwoHorizontalDualImage(HImage horiz, HImage vert, ...) + TryTeachVerticalTwoHorizontalDualImage(HImage horiz, HImage vert, ...) + 2-image public 오버로드 TryFindDatum / TryTeachDatum"
      contains: "TryFindVerticalTwoHorizontalDualImage"
      contains_2: "TryTeachVerticalTwoHorizontalDualImage"
  key_links:
    - from: "TryFindDatum(HImage, HImage, DatumConfig, ...) 신규 오버로드"
      to: "TryFindVerticalTwoHorizontalDualImage(imageHorizontal, imageVertical, ...)"
      via: "switch case dispatch"
      pattern: "case EDatumAlgorithm\\.VerticalTwoHorizontalDualImage"
    - from: "TryFindVerticalTwoHorizontalDualImage"
      to: "imageVertical.GetImageSize → TryFindLine(Vertical) + imageHorizontal.GetImageSize → TryExtractEdgePoints(Horizontal_A/B)"
      via: "Vertical ROI 는 imageVertical, Horizontal ROI 2개는 imageHorizontal 사용 (D-34-01/02)"
      pattern: "imageHorizontal|imageVertical"
    - from: "TryTeachDatum(HImage, HImage, DatumConfig, ...) 신규 오버로드"
      to: "TryTeachVerticalTwoHorizontalDualImage(imageHorizontal, imageVertical, ...)"
      via: "switch case dispatch"
      pattern: "case EDatumAlgorithm\\.VerticalTwoHorizontalDualImage"
---

<objective>
DatumFindingService 에 `VerticalTwoHorizontalDualImage` algorithm 분기를 추가한다 — 가로축 이미지에서 Horizontal_A + Horizontal_B 에지 검출 + 세로축 이미지에서 Vertical 에지 검출 → 두 결과 IntersectionLl 로 결합 (기존 TryFindVerticalTwoHorizontal / TryTeachVerticalTwoHorizontal 본문 100% 복제하되 ROI 별 이미지 소스 분기).

Purpose: Side fixture 케이스 (가로축 ROI 와 세로축 ROI 가 동일 이미지에서 모두 잡히지 않음) 를 풀기 위한 비전 알고리즘 코어. Plan 03 (MainView 티칭 UI + Action_FAIMeasurement 분기) 가 본 플랜의 신규 오버로드를 호출함.
Output: DatumFindingService.cs 안에 2개 신규 private 메서드 (Find + Teach) + 2개 신규 public 오버로드 (TryFindDatum / TryTeachDatum) + 기존 switch dispatch 의 신규 case 분기 1줄씩 (기존 단일-이미지 오버로드 본문 unchanged), msbuild PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-PATTERNS.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-01-SUMMARY.md

<!-- 34-01 SUMMARY 참조 이유: Plan 01 에서 추가한 EDatumAlgorithm.VerticalTwoHorizontalDualImage 식별자를 본 Plan 의 switch case 에서 사용. -->

<interfaces>
<!-- DatumFindingService.cs 에서 추출한 기존 시그니처 — 변경 금지 (오버로드 추가만) -->

From WPF_Example/Halcon/Algorithms/DatumFindingService.cs (line 35-43, 기존 TryFindDatum 단일-이미지 시그니처):
```csharp
public bool TryFindDatum(HImage image, DatumConfig config, out HTuple transform, out string error)
{
    error = null;
    HOperatorSet.HomMat2dIdentity(out transform);
    if (image == null || config == null) { error = "image or config is null"; return false; }
    config.EnsurePerRoiDefaults();
    config.LastFindSucceeded = false;
    switch (config.AlgorithmTypeEnum)
    {
        case EDatumAlgorithm.CircleTwoHorizontal:    return TryFindCircleTwoHorizontal(image, config, out transform, out error);
        case EDatumAlgorithm.VerticalTwoHorizontal:  return TryFindVerticalTwoHorizontal(image, config, out transform, out error);
        case EDatumAlgorithm.TwoLineIntersect:
        default:                                     return TryFindTwoLineIntersect(image, config, out transform, out error);
    }
}
```

From WPF_Example/Halcon/Algorithms/DatumFindingService.cs (line 558-582, 기존 TryTeachDatum 시그니처):
```csharp
public bool TryTeachDatum(HImage image, DatumConfig config, out string error)
{
    error = null;
    if (image == null || config == null) { error = "image or config is null"; return false; }
    config.EnsurePerRoiDefaults();
    switch (config.AlgorithmTypeEnum)
    {
        case EDatumAlgorithm.CircleTwoHorizontal:    return TryTeachCircleTwoHorizontal(image, config, out error);
        case EDatumAlgorithm.VerticalTwoHorizontal:  return TryTeachVerticalTwoHorizontal(image, config, out error);
        case EDatumAlgorithm.TwoLineIntersect:
        default:                                     return TryTeachTwoLineIntersect(image, config, out error);
    }
}
```

From WPF_Example/Halcon/Algorithms/DatumFindingService.cs (line 371-546, 기존 TryFindVerticalTwoHorizontal — 본문 ~175 라인, full template):
```csharp
private bool TryFindVerticalTwoHorizontal(HImage image, DatumConfig config, out HTuple transform, out string error)
{
    error = null;
    HOperatorSet.HomMat2dIdentity(out transform);
    HObject contour = null;
    try
    {
        HTuple imageWidth, imageHeight;
        image.GetImageSize(out imageWidth, out imageHeight);

        // Vertical 라인 검출 (TryFindLine, image 입력)
        // Horizontal A 에지점 (TryExtractEdgePoints, image 입력)
        // Horizontal B 에지점 (TryExtractEdgePoints, image 입력)
        // totalEdges 임계값 검사 (MIN_HORIZONTAL_EDGES)
        // TupleConcat (rowEdgeA + rowEdgeB) → GenContourPolygonXld → FitLineContourXld(tukey)
        // IntersectionLl(vertical, horizontal) → curRow, curCol
        // ValidateHorizontalVerticalAngles 게이트
        // hom_mat2d 빌드 (translate + rotate) → transform out
        // DetectedOrigin / DetectedRefAngle / DetectedRefAngle2 transient 기록
        // Line1Detected / Line2Detected / per-ROI DetectedEdge* 시각화 transient 기록
        // config.LastFindSucceeded = true
        return true;
    }
    catch (Exception ex) { ... }
    finally { if (contour != null) { try { contour.Dispose(); } catch { } } }
}
```

From WPF_Example/Halcon/Algorithms/DatumFindingService.cs (line 913-1083, 기존 TryTeachVerticalTwoHorizontal — 본문 ~170 라인, full template):
```csharp
private bool TryTeachVerticalTwoHorizontal(HImage image, DatumConfig config, out string error)
{
    error = null;
    HObject contour = null;
    try
    {
        HTuple imageWidth, imageHeight;
        image.GetImageSize(out imageWidth, out imageHeight);

        // Vertical 라인 검출 (TryFindLine, image 입력) — vertRawRows/vertRawCols 저장
        // Horizontal A 에지점 (TryExtractEdgePoints, image 입력) — rowEdgeA/colEdgeA 저장
        // Horizontal B 에지점 (TryExtractEdgePoints, image 입력) — rowEdgeB/colEdgeB 저장
        // totalEdges 임계값 검사 (MIN_HORIZONTAL_EDGES)
        // TupleConcat (rowEdgeA + rowEdgeB) → GenContourPolygonXld → FitLineContourXld(tukey)
        // IntersectionLl(vertical, horizontal) → curRow, curCol
        // 기준값 저장: config.RefOriginRow/RefOriginCol/RefAngleRad, config.IsConfigured = true
        // Line1Detected (=수직선) / Line2Detected (=수평결합선) 시각화 transient 기록
        // ValidateHorizontalVerticalAngles 게이트 (마지막)
        return true;
    }
    catch (Exception ex) { config.LastTeachSucceeded = false; error = ex.Message; return false; }
    finally { if (contour != null) { try { contour.Dispose(); } catch { } } }
}
```

**ParamBase 직렬화 제약:** Plan 01 의 enum string 직렬화 보존. 신규 메서드는 enum 비교 (AlgorithmTypeEnum) 만 사용.

**브레이스 스타일 (PATTERNS.md Shared Patterns):** DatumFindingService 는 Allman — opening brace 다음 줄.

**MIN_HORIZONTAL_EDGES 상수:** DatumFindingService.cs 내부 상수 (값 = 10, Phase 12 D-15). 신규 메서드는 동일 상수 재사용 (선언 추가 0).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: TryFindVerticalTwoHorizontalDualImage 신규 private 메서드 + TryFindDatum 2-image 오버로드 추가</name>

  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (offset=35, limit=70 — TryFindDatum 본문 + 기존 switch)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (offset=365, limit=180 — TryFindVerticalTwoHorizontal 전체)
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-PATTERNS.md (section "6) DatumFindingService.cs — TryFindDatum dispatch")
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md (D-34-01, D-34-02)
  </read_first>

  <behavior>
    - Test 1 (신규 오버로드 시그니처): `public bool TryFindDatum(HImage imageHorizontal, HImage imageVertical, DatumConfig config, out HTuple transform, out string error)` 가 노출됨.
    - Test 2 (dispatch): 신규 오버로드는 `config.AlgorithmTypeEnum == VerticalTwoHorizontalDualImage` 일 때 `TryFindVerticalTwoHorizontalDualImage(imageHorizontal, imageVertical, config, out transform, out error)` 호출. 그 외 algorithm 입력 시 `error = "Algorithm is not DualImage; use single-image TryFindDatum overload"` + return false.
    - Test 3 (TryFindVerticalTwoHorizontalDualImage 본문):
      - imageVertical 에서 Vertical 라인 검출 (Vertical_* config 필드 그대로)
      - imageHorizontal 에서 Horizontal_A 에지점 검출 (Horizontal_A_* config 필드)
      - imageHorizontal 에서 Horizontal_B 에지점 검출 (Horizontal_B_* config 필드)
      - 두 image GetImageSize 각각 호출 (imageWidth/Height 변수 분리)
      - 이후 totalEdges 가드 / TupleConcat / GenContourPolygonXld / FitLineContourXld / IntersectionLl / ValidateHorizontalVerticalAngles / hom_mat2d 빌드 / DetectedOrigin transient 기록 / Line1Detected/Line2Detected/DetectedEdge* transient 기록 — 모두 기존 TryFindVerticalTwoHorizontal 와 동일 로직 (분기 0).
    - Test 4 (기존 단일-이미지 TryFindDatum 시그니처 변경 0): 기존 line 35-43 의 `TryFindDatum(HImage image, DatumConfig config, out HTuple transform, out string error)` 본문 0 라인 수정.
    - Test 5 (기존 단일-이미지 switch 변경 0): 기존 switch (line 54-63) 본문 0 라인 수정 — DualImage case 추가하지 않음 (단일-이미지 오버로드 의도적으로 DualImage 미지원, 별도 오버로드 사용).
  </behavior>

  <action>
WPF_Example/Halcon/Algorithms/DatumFindingService.cs 의 다음 두 위치를 수정/추가한다:

**위치 A — 기존 TryFindDatum (line 35-43) 직후 (line 64 이후) 에 신규 public 오버로드 추가:**

```csharp
        //260527 hbk Phase 34 D-34-01/02 — VerticalTwoHorizontalDualImage 변형 전용 2-image 오버로드.
        //  imageHorizontal: 가로축 이미지 (Horizontal_A + Horizontal_B ROI 검출 대상, TeachingImagePath 로드)
        //  imageVertical:   세로축 이미지 (Vertical ROI 검출 대상, TeachingImagePath_Vertical 로드)
        //  algorithm != VerticalTwoHorizontalDualImage 일 때는 error 반환 (잘못된 오버로드 호출 가드).
        //  기존 단일-이미지 TryFindDatum 시그니처는 unchanged — 1-image algorithm 3종 회귀 0 (D-34-13).
        public bool TryFindDatum(HImage imageHorizontal, HImage imageVertical, DatumConfig config, out HTuple transform, out string error)
        {
            error = null;
            HOperatorSet.HomMat2dIdentity(out transform);

            if (imageHorizontal == null || imageVertical == null || config == null)
            {
                error = "image(s) or config is null"; //260527 hbk Phase 34 D-34-01
                return false;
            }

            config.EnsurePerRoiDefaults();
            config.LastFindSucceeded = false;

            if (config.AlgorithmTypeEnum != EDatumAlgorithm.VerticalTwoHorizontalDualImage)
            {
                error = "Algorithm is not VerticalTwoHorizontalDualImage; use single-image TryFindDatum overload"; //260527 hbk Phase 34 D-34-02
                return false;
            }

            return TryFindVerticalTwoHorizontalDualImage(imageHorizontal, imageVertical, config, out transform, out error);
        }
```

**위치 B — TryFindVerticalTwoHorizontal (line 371-546) 직후에 신규 private 메서드 추가:**

기존 `TryFindVerticalTwoHorizontal` 의 본문 (대략 line 371-546) 을 그대로 복제하여 `TryFindVerticalTwoHorizontalDualImage` 신규 메서드 생성. **단 다음 3가지 변경:**
1. 시그니처: `private bool TryFindVerticalTwoHorizontal(HImage image, ...)` → `private bool TryFindVerticalTwoHorizontalDualImage(HImage imageHorizontal, HImage imageVertical, ...)`
2. Vertical 라인 검출 부분 (기존 line 386-400) — `image` → `imageVertical` 로 교체. GetImageSize 도 imageVertical 에서 별도 호출:
```csharp
                HTuple imageVerticalWidth, imageVerticalHeight; //260527 hbk Phase 34 D-34-01 — 세로축 이미지 크기
                imageVertical.GetImageSize(out imageVerticalWidth, out imageVerticalHeight);

                // Vertical 라인 검출 — imageVertical 사용 (D-34-01)
                double vrB, vcB, vrE, vcE;
                HTuple vertRawRows, vertRawCols;
                string lineError;
                if (!TryFindLine(
                        imageVertical, imageVerticalWidth, imageVerticalHeight, //260527 hbk Phase 34 D-34-01
                        config.Vertical_Row, config.Vertical_Col, config.Vertical_Phi,
                        config.Vertical_Length1, config.Vertical_Length2,
                        config.Vertical_Sigma, config.Vertical_EdgeThreshold, config.Vertical_EdgePolarity,
                        config.Vertical_EdgeDirection, config.Vertical_EdgeSelection,
                        config.Vertical_EdgeSampleCount, config.Vertical_EdgeTrimCount,
                        out vrB, out vcB, out vrE, out vcE,
                        out vertRawRows, out vertRawCols,
                        out lineError,
                        "Vertical"))
                {
                    error = "Vertical: " + lineError;
                    return false;
                }
```
3. Horizontal A/B 에지점 검출 부분 (기존 line 402-434) — `image` → `imageHorizontal` 로 교체. GetImageSize 도 imageHorizontal 에서 별도 호출:
```csharp
                HTuple imageHorizontalWidth, imageHorizontalHeight; //260527 hbk Phase 34 D-34-01 — 가로축 이미지 크기
                imageHorizontal.GetImageSize(out imageHorizontalWidth, out imageHorizontalHeight);

                // Horizontal A — imageHorizontal 사용 (D-34-01)
                HTuple rowEdgeA, colEdgeA;
                string edgeErrorA;
                if (!TryExtractEdgePoints(
                        imageHorizontal, imageHorizontalWidth, imageHorizontalHeight, //260527 hbk Phase 34 D-34-01
                        config.Horizontal_A_Row, config.Horizontal_A_Col, config.Horizontal_A_Phi,
                        config.Horizontal_A_Length1, config.Horizontal_A_Length2,
                        config.Horizontal_A_Sigma, config.Horizontal_A_EdgeThreshold, config.Horizontal_A_EdgePolarity,
                        config.Horizontal_A_EdgeDirection, config.Horizontal_A_EdgeSelection,
                        config.Horizontal_A_EdgeSampleCount, config.Horizontal_A_EdgeTrimCount,
                        out rowEdgeA, out colEdgeA, out edgeErrorA,
                        "Horizontal_A"))
                {
                    error = "Horizontal_A: " + edgeErrorA;
                    return false;
                }

                // Horizontal B — imageHorizontal 사용 (D-34-01)
                HTuple rowEdgeB, colEdgeB;
                string edgeErrorB;
                if (!TryExtractEdgePoints(
                        imageHorizontal, imageHorizontalWidth, imageHorizontalHeight, //260527 hbk Phase 34 D-34-01
                        config.Horizontal_B_Row, config.Horizontal_B_Col, config.Horizontal_B_Phi,
                        config.Horizontal_B_Length1, config.Horizontal_B_Length2,
                        config.Horizontal_B_Sigma, config.Horizontal_B_EdgeThreshold, config.Horizontal_B_EdgePolarity,
                        config.Horizontal_B_EdgeDirection, config.Horizontal_B_EdgeSelection,
                        config.Horizontal_B_EdgeSampleCount, config.Horizontal_B_EdgeTrimCount,
                        out rowEdgeB, out colEdgeB, out edgeErrorB,
                        "Horizontal_B"))
                {
                    error = "Horizontal_B: " + edgeErrorB;
                    return false;
                }
```
4. 이후 라인 (totalEdges 가드, TupleConcat, GenContourPolygonXld, FitLineContourXld, IntersectionLl, ValidateHorizontalVerticalAngles, hom_mat2d 빌드, DetectedOrigin/DetectedRefAngle/DetectedRefAngle2 transient 기록, Line1Detected/Line2Detected/Vertical_DetectedEdge*/Horizontal_A_DetectedEdge*/Horizontal_B_DetectedEdge* transient 기록, config.LastFindSucceeded = true, catch/finally 블록) 은 기존 TryFindVerticalTwoHorizontal (line 436-545) 와 100% 동일 복제. **단 신규 메서드의 모든 라인 끝에 `//260527 hbk Phase 34 D-34-01` 또는 기존 phase 주석 보존 처리.**

**전체 신규 메서드 구조:**
```csharp
        //260527 hbk Phase 34 D-34-01/02 — VerticalTwoHorizontalDualImage Find 분기.
        //  본문 = TryFindVerticalTwoHorizontal 100% 복제 + ROI 별 이미지 입력 분기 (Vertical=imageVertical, Horizontal A/B=imageHorizontal).
        //  나머지 로직 (totalEdges 가드 / TupleConcat / FitLineContourXld / IntersectionLl / Validate / hom_mat2d / Detected transient) 모두 동일.
        private bool TryFindVerticalTwoHorizontalDualImage(HImage imageHorizontal, HImage imageVertical, DatumConfig config, out HTuple transform, out string error)
        {
            error = null;
            HOperatorSet.HomMat2dIdentity(out transform);

            HObject contour = null;
            try
            {
                // ... (위에 정의한 본문 — 약 175 라인)
                return true;
            }
            catch (Exception ex)
            {
                error = ex.Message;
                HOperatorSet.HomMat2dIdentity(out transform);
                return false;
            }
            finally
            {
                if (contour != null) { try { contour.Dispose(); } catch { } }
            }
        }
```

**제약 (D-34-01/02/13):**
- Allman 스타일 (PATTERNS.md Shared Patterns).
- 기존 TryFindDatum 단일-이미지 시그니처 (line 35-43) 본문 0 라인 수정. switch (line 54-63) 도 0 라인 수정.
- 기존 TryFindVerticalTwoHorizontal (line 371-546) 본문 0 라인 수정.
- 신규 메서드는 기존 사적 헬퍼 (`TryFindLine`, `TryExtractEdgePoints`, `ValidateHorizontalVerticalAngles`, `MIN_HORIZONTAL_EDGES`) 그대로 재사용 — 신규 헬퍼 추가 0.
- 모든 신규 라인에 `//260527 hbk Phase 34 D-34-XX` 주석 (memory feedback_comment_convention).
  </action>

  <verify>
    <automated>powershell -NoProfile -Command "$f = 'WPF_Example/Halcon/Algorithms/DatumFindingService.cs'; $newOverload = (Select-String -Path $f -Pattern 'TryFindDatum\(HImage imageHorizontal' -Quiet); $newMethod = (Select-String -Path $f -Pattern 'TryFindVerticalTwoHorizontalDualImage' -AllMatches | Measure-Object).Count; if ($newOverload -and $newMethod -ge 2) { Write-Host 'PASS overload='$newOverload' methodMatches='$newMethod } else { Write-Host 'FAIL overload='$newOverload' methodMatches='$newMethod; exit 1 }"</automated>
  </verify>

  <acceptance_criteria>
    - DatumFindingService.cs 에 `TryFindDatum(HImage imageHorizontal, HImage imageVertical` 시그니처가 정확히 1개 (신규 public 오버로드)
    - DatumFindingService.cs 에 `TryFindVerticalTwoHorizontalDualImage` 식별자가 정확히 2개 (declaration + dispatch call)
    - 기존 `TryFindDatum(HImage image, DatumConfig config` 단일-이미지 시그니처가 보존 (정확히 1개)
    - 기존 `TryFindVerticalTwoHorizontal` private 메서드 본문 라인 0 변경 (git diff 검증)
    - 신규 메서드 본문 안에 `imageVertical.GetImageSize` 호출 1개 + `imageHorizontal.GetImageSize` 호출 1개
    - 신규 메서드 본문 안에 `TryFindLine(imageVertical` 호출 1개 + `TryExtractEdgePoints(imageHorizontal` 호출 2개 (A + B)
    - 신규 라인에 `//260527 hbk Phase 34 D-34` 주석 포함 (grep 카운트 ≥ 5)
  </acceptance_criteria>

  <done>DatumFindingService.cs 에 TryFindDatum 2-image 오버로드 + TryFindVerticalTwoHorizontalDualImage private 메서드가 모두 추가되었고, 기존 단일-이미지 경로는 0 라인 변경됨.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TryTeachVerticalTwoHorizontalDualImage 신규 private 메서드 + TryTeachDatum 2-image 오버로드 추가 + msbuild Debug/x64 PASS</name>

  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (offset=555, limit=30 — TryTeachDatum 본문 + 기존 switch)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (offset=910, limit=180 — TryTeachVerticalTwoHorizontal 전체)
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-PATTERNS.md (section "7) DatumFindingService.cs — TryTeachDatum dispatch")
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md (D-34-01, D-34-02)
  </read_first>

  <behavior>
    - Test 1 (신규 오버로드 시그니처): `public bool TryTeachDatum(HImage imageHorizontal, HImage imageVertical, DatumConfig config, out string error)` 가 노출됨.
    - Test 2 (dispatch): 신규 오버로드는 `config.AlgorithmTypeEnum == VerticalTwoHorizontalDualImage` 일 때 `TryTeachVerticalTwoHorizontalDualImage(imageHorizontal, imageVertical, config, out error)` 호출. 그 외 algorithm 입력 시 error 반환 + return false.
    - Test 3 (TryTeachVerticalTwoHorizontalDualImage 본문):
      - imageVertical 에서 Vertical 라인 검출 (Vertical_* config 필드, vertRawRows/vertRawCols 저장)
      - imageHorizontal 에서 Horizontal_A 에지점 검출 (Horizontal_A_*, rowEdgeA/colEdgeA 저장)
      - imageHorizontal 에서 Horizontal_B 에지점 검출 (Horizontal_B_*, rowEdgeB/colEdgeB 저장)
      - 두 image GetImageSize 각각 호출 (imageWidth/Height 변수 분리)
      - 이후 totalEdges 가드 / TupleConcat / GenContourPolygonXld / FitLineContourXld / IntersectionLl / 기준값 저장 (RefOriginRow/RefOriginCol/RefAngleRad, IsConfigured=true) / Line1Detected/Line2Detected transient 기록 / ValidateHorizontalVerticalAngles 게이트 — 모두 기존 TryTeachVerticalTwoHorizontal 와 동일 로직.
    - Test 4 (기존 단일-이미지 TryTeachDatum 시그니처 변경 0): 기존 line 558-582 의 `TryTeachDatum(HImage image, DatumConfig config, out string error)` 본문 0 라인 수정.
    - Test 5 (기존 단일-이미지 switch 변경 0): 기존 switch (line 572-581) 본문 0 라인 수정.
    - Test 6: msbuild Debug/x64 Rebuild PASS, 신규 warning 0.
  </behavior>

  <action>
WPF_Example/Halcon/Algorithms/DatumFindingService.cs 의 두 위치를 수정/추가한다:

**위치 A — 기존 TryTeachDatum (line 558-582) 직후에 신규 public 2-image 오버로드 추가:**

```csharp
        //260527 hbk Phase 34 D-34-01/02 — VerticalTwoHorizontalDualImage 변형 전용 2-image Teach 오버로드.
        //  imageHorizontal: 가로축 이미지 (Horizontal_A + Horizontal_B ROI 티칭 대상)
        //  imageVertical:   세로축 이미지 (Vertical ROI 티칭 대상)
        //  algorithm != VerticalTwoHorizontalDualImage 일 때는 error 반환 (잘못된 오버로드 호출 가드).
        //  기존 단일-이미지 TryTeachDatum 시그니처는 unchanged — 1-image algorithm 3종 회귀 0 (D-34-13).
        public bool TryTeachDatum(HImage imageHorizontal, HImage imageVertical, DatumConfig config, out string error)
        {
            error = null;

            if (imageHorizontal == null || imageVertical == null || config == null)
            {
                error = "image(s) or config is null"; //260527 hbk Phase 34 D-34-01
                return false;
            }

            config.EnsurePerRoiDefaults();

            if (config.AlgorithmTypeEnum != EDatumAlgorithm.VerticalTwoHorizontalDualImage)
            {
                error = "Algorithm is not VerticalTwoHorizontalDualImage; use single-image TryTeachDatum overload"; //260527 hbk Phase 34 D-34-02
                return false;
            }

            return TryTeachVerticalTwoHorizontalDualImage(imageHorizontal, imageVertical, config, out error);
        }
```

**위치 B — TryTeachVerticalTwoHorizontal (line 913-1083) 직후에 신규 private 메서드 추가:**

기존 `TryTeachVerticalTwoHorizontal` 의 본문 (대략 line 913-1083, ~170 라인) 을 그대로 복제하여 `TryTeachVerticalTwoHorizontalDualImage` 신규 메서드 생성. **단 다음 3가지 변경 (Task 1 과 동일 패턴):**
1. 시그니처: `private bool TryTeachVerticalTwoHorizontal(HImage image, ...)` → `private bool TryTeachVerticalTwoHorizontalDualImage(HImage imageHorizontal, HImage imageVertical, ...)`
2. Vertical 라인 검출 부분 — `image` / `imageWidth`/`imageHeight` → `imageVertical` / `imageVerticalWidth`/`imageVerticalHeight` 로 교체. GetImageSize 도 imageVertical 에서 별도 호출.
3. Horizontal A/B 에지점 검출 부분 — `image` / `imageWidth`/`imageHeight` → `imageHorizontal` / `imageHorizontalWidth`/`imageHorizontalHeight` 로 교체. GetImageSize 도 imageHorizontal 에서 별도 호출.
4. 이후 라인 (totalEdges 가드, TupleConcat, GenContourPolygonXld, FitLineContourXld, IntersectionLl, 기준값 저장 RefOriginRow/RefOriginCol/RefAngleRad, IsConfigured=true, Line1Detected/Line2Detected transient 기록, ValidateHorizontalVerticalAngles 게이트, config.LastTeachSucceeded = true, catch/finally 블록) 은 기존 TryTeachVerticalTwoHorizontal 와 100% 동일 복제. 단 모든 라인 끝에 `//260527 hbk Phase 34 D-34-01` 또는 기존 phase 주석 보존.

**msbuild 검증 (Task 1 + Task 2 변경 모두 완료 후):**

```powershell
& "C:\Program Files (x86)\Microsoft Visual Studio\2019\BuildTools\MSBuild\Current\Bin\MSBuild.exe" `
  WPF_Example/DatumMeasurement.csproj `
  /p:Configuration=Debug /p:Platform=x64 `
  /t:Rebuild /v:minimal
```

- 종료 코드 0 (PASS)
- 신규 CS error 0
- 신규 warning 0 (Phase 21 baseline 동일)

**제약 (D-34-01/02/13/14):**
- Allman 스타일 (PATTERNS.md Shared Patterns).
- 기존 TryTeachDatum 단일-이미지 시그니처 (line 558-582) 본문 0 라인 수정.
- 기존 TryTeachVerticalTwoHorizontal (line 913-1083) 본문 0 라인 수정.
- 신규 메서드는 기존 사적 헬퍼 그대로 재사용.
- 모든 신규 라인에 `//260527 hbk Phase 34 D-34-XX` 주석.
- `InspectionSequence.cs`, `VisionResponsePacket.cs`, `Action_FAIMeasurement.cs`, `MainView.xaml.cs`, `EDatumAlgorithm.cs`, `DatumConfig.cs` 변경 0 라인 (이 Plan 의 변경은 DatumFindingService.cs 단일 파일만).
  </action>

  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "$f = 'WPF_Example/Halcon/Algorithms/DatumFindingService.cs'; $newOverload = (Select-String -Path $f -Pattern 'TryTeachDatum\(HImage imageHorizontal' -Quiet); $newMethod = (Select-String -Path $f -Pattern 'TryTeachVerticalTwoHorizontalDualImage' -AllMatches | Measure-Object).Count; if ($newOverload -and $newMethod -ge 2) { Write-Host 'PASS overload='$newOverload' methodMatches='$newMethod } else { Write-Host 'FAIL overload='$newOverload' methodMatches='$newMethod; exit 1 }"</automated>
  </verify>

  <acceptance_criteria>
    - DatumFindingService.cs 에 `TryTeachDatum(HImage imageHorizontal, HImage imageVertical` 시그니처가 정확히 1개 (신규 public 오버로드)
    - DatumFindingService.cs 에 `TryTeachVerticalTwoHorizontalDualImage` 식별자가 정확히 2개 (declaration + dispatch call)
    - 기존 `TryTeachDatum(HImage image, DatumConfig config` 단일-이미지 시그니처 보존 (정확히 1개)
    - 기존 `TryTeachVerticalTwoHorizontal` private 메서드 본문 라인 0 변경 (git diff 검증)
    - 신규 메서드 본문 안에 `imageVertical.GetImageSize` 호출 1개 + `imageHorizontal.GetImageSize` 호출 1개
    - 신규 메서드 본문 안에 `TryFindLine(imageVertical` 호출 1개 + `TryExtractEdgePoints(imageHorizontal` 호출 2개
    - msbuild Debug/x64 Rebuild 종료 코드 0 (PASS)
    - msbuild 출력에 신규 CS error 0 (`error CS` 라인 = 0)
    - msbuild 출력에 신규 warning 0 (Phase 21 baseline 6 동일)
    - git diff --numstat: DatumFindingService.cs 변경 라인 ≈ +350 ~ +400 (2 신규 public 오버로드 ≈ 50 라인 + 2 신규 private 메서드 ≈ 350 라인)
    - git diff --numstat: EDatumAlgorithm.cs / DatumConfig.cs / Action_FAIMeasurement.cs / InspectionSequence.cs / VisionResponsePacket.cs / MainView.xaml.cs 모두 변경 0,0
  </acceptance_criteria>

  <done>DatumFindingService.cs 에 TryTeachDatum 2-image 오버로드 + TryTeachVerticalTwoHorizontalDualImage private 메서드가 모두 추가되었고, msbuild Debug/x64 Rebuild 가 PASS 한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 호출처 (Plan 03 MainView/Action_FAIMeasurement) → TryFindDatum/TryTeachDatum 2-image | 호출처가 두 HImage 모두 null 아님을 보장해야 하나, 서비스도 방어 가드 필요 |
| algorithm enum 검증 | 신규 오버로드가 잘못된 algorithm (TwoLineIntersect/CircleTwoHorizontal/VerticalTwoHorizontal) 입력 시 error 반환 (호출처 오류 식별 가능) |
| HObject 자원 해제 | contour HObject 메모리 누수 가능 — finally 블록 dispose 보장 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02-01 | Tampering | 두 HImage 가 서로 다른 해상도 → IntersectionLl 좌표계 불일치 → 잘못된 Datum 좌표 | accept | Phase 34 가정 (CONTEXT D-34 Claude's Discretion #4): 두 이미지가 같은 카메라 해상도/orientation. 실 운영에서 위반 시 Datum 검출 결과는 측정 가능 (검출 좌표가 imageHorizontal frame 기준 이라는 contract 명시). 별도 phase 후보. |
| T-34-02-02 | DoS | contour HObject dispose 누락 → 메모리 누수 누적 | mitigate | 신규 메서드 본문이 기존 TryFindVerticalTwoHorizontal 의 finally 블록 (`if (contour != null) { try { contour.Dispose(); } catch { } }`) 100% 복제 → 누수 위험 없음. |
| T-34-02-03 | DoS | HALCON 예외 (e.g. FitLineContourXld 실패) 발생 시 transform out 이 stale 상태로 반환 | mitigate | 기존 패턴 그대로 catch (Exception ex) 블록에서 `HOperatorSet.HomMat2dIdentity(out transform); return false;` — 호출처가 transform 사용 전 return 값 검증. |
| T-34-02-04 | Spoofing | 신규 오버로드 호출 시 잘못된 algorithm enum 전달 (VTH 등) | mitigate | 신규 오버로드 진입부에 `if (config.AlgorithmTypeEnum != VerticalTwoHorizontalDualImage) { error = "Algorithm is not ..."; return false; }` 명시 가드. |
| T-34-02-05 | Information Disclosure | error 메시지에 내부 파일 경로 / DatumConfig 내부 상태 노출 | accept | 기존 error 패턴 ("Vertical: " + lineError 등) 과 동일 — 진단 목적 의도된 노출. PII 없음. |
| T-34-02-06 | Repudiation | 신규 메서드 변경 추적 | mitigate | 모든 신규 라인 `//260527 hbk Phase 34 D-34-XX` 주석 — git diff + grep 으로 식별. |
</threat_model>

<verification>
## Plan 02 검증

**Smoke:**
- DatumFindingService.cs 컴파일 통과.
- 기존 TryFindDatum / TryTeachDatum 단일-이미지 경로 호출 시 동작 변경 0 (회귀 없음).

**File diff guards (D-34-13/14):**
- Plan 02 의 변경은 DatumFindingService.cs 단일 파일.
- 다음 파일들은 Plan 02 단계에서 **변경 0 라인** (PowerShell):
  - `git diff --numstat HEAD~1 -- WPF_Example/Custom/Sequence/Inspection/EDatumAlgorithm.cs` → 0,0 (Plan 01 변경 외)
  - `git diff --numstat HEAD~1 -- WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` → 0,0 (Plan 01 변경 외)
  - `git diff --numstat HEAD -- WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` → 0,0
  - `git diff --numstat HEAD -- WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` → 0,0
  - `git diff --numstat HEAD -- WPF_Example/TcpServer/VisionResponsePacket.cs` → 0,0
  - `git diff --numstat HEAD -- WPF_Example/UI/ContentItem/MainView.xaml.cs` → 0,0

**기존 단일-이미지 경로 0 라인 변경 검증:**
- DatumFindingService.cs 의 기존 TryFindDatum (line 35-43) / TryTeachDatum (line 558-582) / TryFindVerticalTwoHorizontal (line 371-546) / TryTeachVerticalTwoHorizontal (line 913-1083) 본문 모두 0 라인 변경 (git blame 검증).

**msbuild:**
- Debug/x64 Rebuild 종료 코드 0
- Phase 21 baseline warning 수와 동일 (신규 warning 0)

**구조 검증 (런타임 0 — 다음 Plan 까지 deferred):**
- Plan 03 의 MainView/Action_FAIMeasurement 호출 site 추가 후 실측 검증.
- 본 Plan 단계의 검증은 컴파일 + 시그니처 + 기존 코드 무변경 까지.
</verification>

<success_criteria>
- [ ] TryFindDatum(HImage imageHorizontal, HImage imageVertical, ...) public 오버로드 추가
- [ ] TryTeachDatum(HImage imageHorizontal, HImage imageVertical, ...) public 오버로드 추가
- [ ] TryFindVerticalTwoHorizontalDualImage private 메서드 추가 (기존 TryFindVerticalTwoHorizontal 100% 복제 + 이미지 분기 3곳 교체)
- [ ] TryTeachVerticalTwoHorizontalDualImage private 메서드 추가 (기존 TryTeachVerticalTwoHorizontal 100% 복제 + 이미지 분기 3곳 교체)
- [ ] 기존 단일-이미지 TryFindDatum / TryTeachDatum / TryFindVerticalTwoHorizontal / TryTeachVerticalTwoHorizontal 본문 0 라인 변경
- [ ] D-34-14 가드 — Plan 02 단계에서 InspectionSequence.cs / VisionResponsePacket.cs / Action_FAIMeasurement.cs / MainView.xaml.cs / EDatumAlgorithm.cs / DatumConfig.cs 변경 0 라인
- [ ] 모든 신규 라인에 `//260527 hbk Phase 34 D-34-XX` 주석
- [ ] msbuild Debug/x64 Rebuild PASS, 신규 warning 0
</success_criteria>

<output>
After completion, create `.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-02-SUMMARY.md` per template — include msbuild PASS 증거 + git diff --numstat 결과 (변경 파일 = DatumFindingService.cs 만, 그 외 5 파일 0,0) + 신규 public/private 메서드 시그니처 4개 명시.
</output>
