---
phase: 34-datum-verticaltwohorizontal-2026-05-26
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: true
requirements:
  - SC-1   # AlgorithmType 콤보박스 노출 (Plan 01 에서 데이터 모델 추가, 본 Plan 에서 wizard step 시퀀스 노출)
  - SC-4   # 티칭 UI step 머신에 이미지 전환 step 추가 (Horizontal_A → Horizontal_B → image switch → Vertical)
  - SC-5   # Action_FAIMeasurement GrabOrLoadDatumImage 가 ROI 별 이미지 선택 (algorithm = 신규일 때 vertical ROI 는 image2)
  - SC-7   # 기존 1-image 타입 회귀 0 (msbuild PASS + Top recipe 로드 확인)

must_haves:
  truths:
    - "MainView 의 GetAlgorithmSteps switch 가 VerticalTwoHorizontalDualImage 알고리즘에 대해 [HorizontalA, HorizontalB, Vertical] 순서 step 배열을 반환한다"
    - "Vertical step 진입 시 algorithm == DualImage 이면 halconViewer 가 TeachingImagePath_Vertical 이미지로 자동 swap 된다"
    - "TeachingImagePath_Vertical 가 비어있는 상태로 Vertical step 진입 시 안내 메시지 + 드로잉 차단 (저장 차단 아님)"
    - "MainView.ValidateRoiPresence 가 DualImage 케이스에서 Vertical/Horizontal_A/Horizontal_B ROI + 두 이미지 경로 모두 검증"
    - "InvokeTryTeachDatum 이 algorithm == DualImage 일 때 두 이미지를 파일에서 로드하여 신규 2-image TryTeachDatum 오버로드를 호출"
    - "Action_FAIMeasurement.Run() DatumPhase 가 algorithm == DualImage 일 때 두 이미지 로드 + DatumFindingService 2-image TryFindDatum 직접 호출 (InspectionSequence.TryRunDatumPhase 우회)"
    - "기존 1-image 3종 algorithm 경로는 0 라인 변경 — Top recipe 회귀 0"
    - "msbuild Debug/x64 Rebuild PASS, 신규 warning 0"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "GetAlgorithmSteps DualImage case + StartDatumTeachStep Vertical 이미지 swap + ValidateRoiPresence DualImage case + InvokeTryTeachDatum 2-image 분기"
      contains: "VerticalTwoHorizontalDualImage"
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "EStep.DatumPhase 안 algorithm 분기 (DualImage → 2-image TryFindDatum 직접 호출) + TryGrabOrLoadDualDatumImages 신규 private 함수"
      contains: "TryGrabOrLoadDualDatumImages"
  key_links:
    - from: "MainView.GetAlgorithmSteps switch (line 1911-1922)"
      to: "[EDatumTeachStep.HorizontalA, EDatumTeachStep.HorizontalB, EDatumTeachStep.Vertical]"
      via: "1-case 추가 (D-34-07 순서 — 가로축부터)"
      pattern: "case EDatumAlgorithm\\.VerticalTwoHorizontalDualImage"
    - from: "MainView.StartDatumTeachStep Vertical case (line 1980-1986)"
      to: "halconViewer.LoadImage(TeachingImagePath_Vertical)"
      via: "algorithm == DualImage 이면 진입 직전 자동 swap (D-34-06)"
      pattern: "TeachingImagePath_Vertical"
    - from: "MainView.InvokeTryTeachDatum (line 2106-2146)"
      to: "DatumFindingService.TryTeachDatum(imageHorizontal, imageVertical, ...)"
      via: "algorithm == DualImage 일 때 파일 2개 로드 + 신규 2-image 오버로드 호출"
      pattern: "TryTeachDatum\\(imageHorizontal"
    - from: "Action_FAIMeasurement.Run() EStep.DatumPhase (line 78-104)"
      to: "DatumFindingService.TryFindDatum(imageHorizontal, imageVertical, ...) 직접 호출"
      via: "algorithm == DualImage 일 때 InspectionSequence.TryRunDatumPhase 우회 (D-34-13 + D-34-14 가드)"
      pattern: "TryGrabOrLoadDualDatumImages"
---

<objective>
MainView 의 Datum 티칭 UI 와 Action_FAIMeasurement 의 검사 사이클 양쪽에 VerticalTwoHorizontalDualImage 분기를 추가한다 — UI 측: step 순서 (HA→HB→V) + Vertical step 진입 시 자동 이미지 swap + ValidateRoiPresence + InvokeTryTeachDatum 2-image 분기. 검사 측: EStep.DatumPhase 안에서 algorithm 보고 두 이미지 로드 + DatumFindingService 신규 2-image 오버로드 직접 호출 (InspectionSequence.TryRunDatumPhase 변경 0 — D-34-14 가드 보존).

Purpose: 사용자가 PropertyGrid 에서 algorithm = VerticalTwoHorizontalDualImage 로 선택 → Datum 노드 Load image (가로축) → btn_teachDatum → HorizontalA/B 드로잉 → 자동 swap (세로축) → Vertical 드로잉 → 자동 teach 까지 end-to-end 완주. 그 후 검사 사이클 (Shot Grab → DatumPhase → FAI Measurement) 도 두 이미지로 자동 진행. Plan 04 (UAT) 가 이를 SIMUL_MODE 로 검증.
Output: MainView.xaml.cs 4개 메서드 분기 추가 + Action_FAIMeasurement.cs 1개 신규 함수 + 1개 분기. msbuild PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-PATTERNS.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-01-SUMMARY.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-02-SUMMARY.md

<!-- 34-01 + 34-02 SUMMARY 참조 이유: Plan 01 의 EDatumAlgorithm.VerticalTwoHorizontalDualImage 값 + DatumConfig.TeachingImagePath_Vertical 필드 + Plan 02 의 2-image TryFindDatum/TryTeachDatum 신규 오버로드를 본 Plan 의 호출처에서 사용. -->

<interfaces>
<!-- MainView.xaml.cs 에서 추출한 기존 시그니처 + Action_FAIMeasurement.cs 에서 추출한 호출 site -->

From WPF_Example/UI/ContentItem/MainView.xaml.cs (line 59, EDatumTeachStep enum — 변경 0):
```csharp
private enum EDatumTeachStep { Line1, Line2, Circle, Vertical, HorizontalA, HorizontalB, Done }
```

From WPF_Example/UI/ContentItem/MainView.xaml.cs (line 1911-1922, GetAlgorithmSteps — 1 case 추가):
```csharp
private static EDatumTeachStep[] GetAlgorithmSteps(EDatumAlgorithm alg) {
    switch (alg) {
        case EDatumAlgorithm.TwoLineIntersect:
            return new[] { EDatumTeachStep.Line1, EDatumTeachStep.Line2 };
        case EDatumAlgorithm.CircleTwoHorizontal:
            return new[] { EDatumTeachStep.Circle, EDatumTeachStep.HorizontalA, EDatumTeachStep.HorizontalB };
        case EDatumAlgorithm.VerticalTwoHorizontal:
            return new[] { EDatumTeachStep.Vertical, EDatumTeachStep.HorizontalA, EDatumTeachStep.HorizontalB };
        default:
            return new EDatumTeachStep[0];
    }
}
```

From WPF_Example/UI/ContentItem/MainView.xaml.cs (line 1015-1036, ValidateRoiPresence — 1 case 추가):
```csharp
private static string ValidateRoiPresence(DatumConfig d, EDatumAlgorithm alg) {
    if (d == null) return null;
    switch (alg) {
        case EDatumAlgorithm.TwoLineIntersect: ...
        case EDatumAlgorithm.CircleTwoHorizontal: ...
        case EDatumAlgorithm.VerticalTwoHorizontal: ...
    }
    return null;
}
```

From WPF_Example/UI/ContentItem/MainView.xaml.cs (line 1980-1986, StartDatumTeachStep Vertical case — 진입부 image swap 추가):
```csharp
case EDatumTeachStep.Vertical:
    label_drawHint.Content = "Step 1/3: 수직 ROI를 드래그하세요";
    label_drawHint.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FFAAAAAA"));
    label_drawHint.Visibility = Visibility.Visible;
    halconViewer.RectDrawingCompleted += HalconViewer_DatumRectCompleted;
    halconViewer.StartRectangleDrawing();
    break;
```

From WPF_Example/UI/ContentItem/MainView.xaml.cs (line 2106-2146, InvokeTryTeachDatum — algorithm 분기 추가):
```csharp
private void InvokeTryTeachDatum() {
    if (_editingDatum == null) { ExitCanvasMode(); return; }
    HImage img = halconViewer.CurrentImage; // ← 현재는 단일 image
    ...
    var svc = new ReringProject.Halcon.Algorithms.DatumFindingService();
    string error;
    bool ok = svc.TryTeachDatum(img, _editingDatum, out error); // ← 단일-이미지 오버로드
    ...
}
```

From WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (line 78-104, EStep.DatumPhase 진입 — 분기 추가):
```csharp
case EStep.DatumPhase: {
    var parentSeq = ShotParam != null ? ShotParam.Parent as InspectionSequence : null;
    if (parentSeq != null && parentSeq.DatumConfigs.Count > 0) {
        HImage datumImage = GrabOrLoadDatumImage(parentSeq);  // ← 현재는 단일 image
        if (datumImage == null) { ... FinishAction(EContextResult.Error); break; }
        try {
            string datumError;
            if (!parentSeq.TryRunDatumPhase(datumImage, out datumError)) { ... }
        } finally { datumImage.Dispose(); }
    }
    Step = (int)EStep.Grab;
    break;
}
```

From WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (line 257-288, GrabOrLoadDatumImage — 변경 0):
```csharp
private HImage GrabOrLoadDatumImage(InspectionSequence parentSeq) { ... }
```

**InspectionSequence.TryRunDatumPhase 시그니처 (D-34-14 가드 — 변경 금지):**
```csharp
// InspectionSequence.cs line 144
public bool TryRunDatumPhase(HImage image, out string error)
```

**핵심 설계 결정 (D-34-13 정합):**
InspectionSequence.TryRunDatumPhase 는 0 라인 변경 (D-34-14). 따라서 DualImage 케이스는:
- 옵션 A: Action_FAIMeasurement 가 algorithm 보고 DualImage 면 InspectionSequence.TryRunDatumPhase 우회 → DatumFindingService.TryFindDatum(2-image) 직접 호출 + _datumTransforms 채우는 로직 부분 복제 (InspectionSequence 의 transform 저장 site 접근).
- 옵션 B: InspectionSequence 에 신규 public 메서드 `TryRunDatumPhaseDual(HImage h, HImage v, out string error)` 추가 → D-34-14 가드 위반 (InspectionSequence.cs 변경).

**옵션 A 채택 (D-34-14 가드 보존):**
- Action_FAIMeasurement 가 DualImage 일 때 InspectionSequence 의 단일 DatumConfig 만 처리 (Side fixture 의 단일 Datum 가정 — Phase 33 D-06 와 동일).
- DatumConfig.CurrentTransform / LastFindSucceeded 는 DatumFindingService 가 직접 설정함 (line 49: `config.LastFindSucceeded = false`, line 532: `config.LastFindSucceeded = true`). 따라서 호출처는 transform 저장만 추가하면 됨.
- 다중 Datum (DatumConfigs.Count > 1) 시 DualImage 가 있으면 첫 Datum 만 dual-image 처리 + 나머지는 기존 단일-image 경로 → 또는 더 안전한 모델: DualImage 는 단일 Datum 만 허용 → 첫 번째가 DualImage 면 그 하나만 처리하고 break, 나머지 무시 (Side fixture 가 단일 Datum 가정).
- **결정 (Plan 03):** algorithm 분기는 DatumConfigs[0] 만 검사. DatumConfigs[0].AlgorithmTypeEnum == DualImage 이면 신규 경로 (2-image), else 기존 경로 (TryRunDatumPhase).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: MainView.GetAlgorithmSteps + ValidateRoiPresence + StartDatumTeachStep 자동 이미지 swap + InvokeTryTeachDatum 2-image 분기</name>

  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (offset=1009, limit=40 — ValidateRoiPresence)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (offset=1909, limit=110 — GetAlgorithmSteps + IsStepMissing + StartDatumTeachStep 시작부)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (offset=1965, limit=50 — StartDatumTeachStep switch case 본문 전체)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (offset=2104, limit=50 — InvokeTryTeachDatum 전체)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (offset=140, limit=25 — DisplayDatumImage 패턴 참조, image swap 동일 호출 방식)
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-PATTERNS.md (sections "8) GetAlgorithmSteps", "9) ValidateRoiPresence", "10) StartDatumTeachStep + 자동 이미지 전환")
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md (D-34-06, D-34-07, D-34-08, D-34-09, D-34-10)
  </read_first>

  <behavior>
    - Test 1 (GetAlgorithmSteps): switch 안에 `case EDatumAlgorithm.VerticalTwoHorizontalDualImage:` 추가 → `return new[] { EDatumTeachStep.HorizontalA, EDatumTeachStep.HorizontalB, EDatumTeachStep.Vertical };` (D-34-07 순서 — 가로축 먼저).
    - Test 2 (ValidateRoiPresence): switch 안에 `case EDatumAlgorithm.VerticalTwoHorizontalDualImage:` 추가 → Vertical_Length1 + Horizontal_A_Length1 + Horizontal_B_Length1 + TeachingImagePath (가로축) + TeachingImagePath_Vertical (세로축) 5가지 모두 검증. 빈 경로면 한국어 안내 메시지 반환.
    - Test 3 (StartDatumTeachStep Vertical case 진입): algorithm == DualImage 이면 `halconViewer.LoadImage(_editingDatum.TeachingImagePath_Vertical)` 자동 호출 + 라벨을 "Step 3/3: 수직 ROI를 드래그하세요" 로 갱신. 경로 빈 경우 안내 메시지 + 드로잉 차단.
    - Test 4 (StartDatumTeachStep HorizontalA/B 라벨): algorithm == DualImage 이면 라벨이 "Step 1/3: 수평 A ..." / "Step 2/3: 수평 B ..." 로 분기 (순서 일관성).
    - Test 5 (InvokeTryTeachDatum): algorithm == DualImage 이면 두 파일 경로에서 이미지 2개 로드 + 신규 `svc.TryTeachDatum(imageHorizontal, imageVertical, _editingDatum, out error)` 호출. 빈 경로 또는 파일 없음 시 한국어 에러 모달.
    - Test 6 (회귀): 기존 4 algorithm (TLI/CTH/VTH/default) 의 GetAlgorithmSteps / ValidateRoiPresence / StartDatumTeachStep / InvokeTryTeachDatum 경로는 0 라인 변경.
  </behavior>

  <action>
WPF_Example/UI/ContentItem/MainView.xaml.cs 의 4개 위치를 수정한다:

**위치 A — GetAlgorithmSteps (line 1911-1922) 의 default 직전에 신규 case 추가:**

기존:
```csharp
        private static EDatumTeachStep[] GetAlgorithmSteps(EDatumAlgorithm alg) {
            switch (alg) {
                case EDatumAlgorithm.TwoLineIntersect:
                    return new[] { EDatumTeachStep.Line1, EDatumTeachStep.Line2 };
                case EDatumAlgorithm.CircleTwoHorizontal:
                    return new[] { EDatumTeachStep.Circle, EDatumTeachStep.HorizontalA, EDatumTeachStep.HorizontalB };
                case EDatumAlgorithm.VerticalTwoHorizontal:
                    return new[] { EDatumTeachStep.Vertical, EDatumTeachStep.HorizontalA, EDatumTeachStep.HorizontalB };
                default:
                    return new EDatumTeachStep[0];
            }
        }
```

변경 후 (VerticalTwoHorizontal case 다음 줄에 신규 case):
```csharp
                case EDatumAlgorithm.VerticalTwoHorizontal:
                    return new[] { EDatumTeachStep.Vertical, EDatumTeachStep.HorizontalA, EDatumTeachStep.HorizontalB };
                //260527 hbk Phase 34 D-34-07 — DualImage 변형: 순서 = HA → HB → V (가로축 이미지 먼저 → 자동 swap → 세로축 이미지).
                //  1-image VTH (V → HA → HB) 와 의도적으로 다름.
                case EDatumAlgorithm.VerticalTwoHorizontalDualImage:
                    return new[] { EDatumTeachStep.HorizontalA, EDatumTeachStep.HorizontalB, EDatumTeachStep.Vertical }; //260527 hbk Phase 34 D-34-07
                default:
                    return new EDatumTeachStep[0];
```

**위치 B — ValidateRoiPresence (line 1015-1036) 의 switch 내부 마지막 case (VTH) 다음에 신규 case 추가:**

기존:
```csharp
                case EDatumAlgorithm.VerticalTwoHorizontal:
                    if (d.Vertical_Length1 <= 0)
                        return "Vertical ROI 가 없습니다. 캔버스에 수직 ROI 를 그리고 다시 시도하세요.";
                    if (d.Horizontal_A_Length1 <= 0 || d.Horizontal_B_Length1 <= 0)
                        return "Horizontal A/B ROI 가 없습니다. 캔버스에 ROI 를 그리고 다시 시도하세요.";
                    break;
            }
            return null;
```

변경 후 (VTH 다음에 신규 case 추가):
```csharp
                case EDatumAlgorithm.VerticalTwoHorizontal:
                    if (d.Vertical_Length1 <= 0)
                        return "Vertical ROI 가 없습니다. 캔버스에 수직 ROI 를 그리고 다시 시도하세요.";
                    if (d.Horizontal_A_Length1 <= 0 || d.Horizontal_B_Length1 <= 0)
                        return "Horizontal A/B ROI 가 없습니다. 캔버스에 ROI 를 그리고 다시 시도하세요.";
                    break;
                //260527 hbk Phase 34 D-34-09/10 — DualImage 변형 가드: 3 ROI + 2 이미지 경로 모두 검증.
                case EDatumAlgorithm.VerticalTwoHorizontalDualImage:
                    if (d.Vertical_Length1 <= 0)
                        return "Vertical ROI 가 없습니다. 캔버스에 수직 ROI 를 그리고 다시 시도하세요."; //260527 hbk Phase 34
                    if (d.Horizontal_A_Length1 <= 0 || d.Horizontal_B_Length1 <= 0)
                        return "Horizontal A/B ROI 가 없습니다. 캔버스에 ROI 를 그리고 다시 시도하세요."; //260527 hbk Phase 34
                    if (string.IsNullOrEmpty(d.TeachingImagePath))
                        return "가로축 티칭 이미지 경로가 비어 있습니다. Datum 노드에서 가로축 이미지를 Load 해주세요."; //260527 hbk Phase 34 D-34-10
                    if (string.IsNullOrEmpty(d.TeachingImagePath_Vertical))
                        return "세로축 티칭 이미지 경로가 비어 있습니다. Datum 노드에서 세로축 이미지를 Load 해주세요."; //260527 hbk Phase 34 D-34-10
                    break;
            }
            return null;
```

**위치 C — StartDatumTeachStep (line 1965-2011) 의 Vertical / HorizontalA / HorizontalB case 본문 수정:**

세 case 모두 algorithm == DualImage 여부에 따라 라벨 + 이미지 swap 분기.

기존 Vertical (line 1980-1986):
```csharp
                case EDatumTeachStep.Vertical:
                    label_drawHint.Content = "Step 1/3: 수직 ROI를 드래그하세요"; //260424 hbk Phase 12
                    label_drawHint.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FFAAAAAA"));
                    label_drawHint.Visibility = Visibility.Visible;
                    halconViewer.RectDrawingCompleted += HalconViewer_DatumRectCompleted;
                    halconViewer.StartRectangleDrawing();
                    break;
```

변경 후 (DualImage 분기 추가 — 자동 이미지 swap + 빈 경로 가드 + 라벨 분기):
```csharp
                case EDatumTeachStep.Vertical:
                    //260527 hbk Phase 34 D-34-06 — DualImage 변형이면 진입 직전에 세로축 이미지로 자동 swap.
                    if (_editingDatum != null
                        && _editingDatum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) {
                        string vpath = _editingDatum.TeachingImagePath_Vertical; //260527 hbk Phase 34 D-34-06
                        if (!string.IsNullOrEmpty(vpath) && System.IO.File.Exists(vpath)) {
                            try { halconViewer.LoadImage(vpath); } //260527 hbk Phase 34 D-34-06
                            catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, ex.Message); } //260527 hbk Phase 34
                        } else {
                            //260527 hbk Phase 34 D-34-08 — 빈 경로: 안내 + 드로잉 차단 (저장은 차단하지 않음).
                            label_drawHint.Content = "세로축 이미지를 Load 해주세요"; //260527 hbk Phase 34 D-34-08
                            label_drawHint.Foreground = new SolidColorBrush(Colors.Orange); //260527 hbk Phase 34
                            label_drawHint.Visibility = Visibility.Visible; //260527 hbk Phase 34
                            break; //260527 hbk Phase 34 — 드로잉 시작 안 함 (switch case 종료)
                        }
                        //260527 hbk Phase 34 — DualImage 라벨 (Step 3/3, 순서 = HA→HB→V)
                        label_drawHint.Content = "Step 3/3: 수직 ROI를 드래그하세요"; //260527 hbk Phase 34
                    } else {
                        label_drawHint.Content = "Step 1/3: 수직 ROI를 드래그하세요"; //260424 hbk Phase 12 — 기존 1-image VTH 라벨 보존
                    }
                    label_drawHint.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FFAAAAAA"));
                    label_drawHint.Visibility = Visibility.Visible;
                    halconViewer.RectDrawingCompleted += HalconViewer_DatumRectCompleted;
                    halconViewer.StartRectangleDrawing();
                    break;
```

기존 HorizontalA (line 1987-1993):
```csharp
                case EDatumTeachStep.HorizontalA:
                    label_drawHint.Content = "Step 2/3: 수평 A ROI를 드래그하세요";
                    label_drawHint.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FFAAAAAA"));
                    label_drawHint.Visibility = Visibility.Visible;
                    halconViewer.RectDrawingCompleted += HalconViewer_DatumRectCompleted;
                    halconViewer.StartRectangleDrawing();
                    break;
```

변경 후 (DualImage 분기 추가 — 라벨만 변경, 가로축은 이미 표시되어 있음):
```csharp
                case EDatumTeachStep.HorizontalA:
                    //260527 hbk Phase 34 D-34-07 — DualImage 변형: Step 1/3 (가로축 이미지 표시 상태 가정 — Datum 노드 선택 시 DisplayDatumImage 가 TeachingImagePath 로드).
                    if (_editingDatum != null
                        && _editingDatum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) {
                        label_drawHint.Content = "Step 1/3: 수평 A ROI를 드래그하세요"; //260527 hbk Phase 34 D-34-07
                    } else {
                        label_drawHint.Content = "Step 2/3: 수평 A ROI를 드래그하세요"; //260424 hbk Phase 12 — 기존 1-image VTH/CTH 라벨 보존
                    }
                    label_drawHint.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FFAAAAAA"));
                    label_drawHint.Visibility = Visibility.Visible;
                    halconViewer.RectDrawingCompleted += HalconViewer_DatumRectCompleted;
                    halconViewer.StartRectangleDrawing();
                    break;
```

기존 HorizontalB (line 1994-2000):
```csharp
                case EDatumTeachStep.HorizontalB:
                    label_drawHint.Content = "Step 3/3: 수평 B ROI를 드래그하세요";
                    ...
                    break;
```

변경 후 (DualImage 분기 추가):
```csharp
                case EDatumTeachStep.HorizontalB:
                    //260527 hbk Phase 34 D-34-07 — DualImage 변형: Step 2/3 (가로축 이미지 표시 상태 — Vertical step 진입 시 swap).
                    if (_editingDatum != null
                        && _editingDatum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) {
                        label_drawHint.Content = "Step 2/3: 수평 B ROI를 드래그하세요"; //260527 hbk Phase 34 D-34-07
                    } else {
                        label_drawHint.Content = "Step 3/3: 수평 B ROI를 드래그하세요"; //260424 hbk Phase 12 — 기존 1-image VTH/CTH 라벨 보존
                    }
                    label_drawHint.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FFAAAAAA"));
                    label_drawHint.Visibility = Visibility.Visible;
                    halconViewer.RectDrawingCompleted += HalconViewer_DatumRectCompleted;
                    halconViewer.StartRectangleDrawing();
                    break;
```

**위치 D — InvokeTryTeachDatum (line 2106-2146) 의 algorithm 분기 추가:**

기존 (line 2123): `bool ok = svc.TryTeachDatum(img, _editingDatum, out error);`

변경 후 (algorithm 보고 단일-이미지 또는 2-image 분기):

기존 전체:
```csharp
        private void InvokeTryTeachDatum() {
            if (_editingDatum == null) { ExitCanvasMode(); return; }

            HImage img = halconViewer.CurrentImage;
            if (img == null) { ... CustomMessageBox.Show("티칭 실패", "이미지가 없습니다..."); return; }

            var svc = new ReringProject.Halcon.Algorithms.DatumFindingService();
            string error;
            bool ok = svc.TryTeachDatum(img, _editingDatum, out error);
            if (ok) { ... }
            else { ... }
            ...
        }
```

변경 후 (DualImage 분기 추가 — `var svc = new DatumFindingService();` 위치에서 분기):
```csharp
        private void InvokeTryTeachDatum() {
            if (_editingDatum == null) { ExitCanvasMode(); return; }

            HImage img = halconViewer.CurrentImage;
            if (img == null) {
                label_drawHint.Visibility = Visibility.Collapsed;
                CustomMessageBox.Show("티칭 실패", "이미지가 없습니다. 먼저 Grab 또는 Load Image 를 수행하세요.");
                _canvasMode = ECanvasMode.None;
                btn_teachDatum.IsChecked = false;
                _editingDatum = null;
                halconViewer.IsTeachDatumMode = false;
                return;
            }

            var svc = new ReringProject.Halcon.Algorithms.DatumFindingService();
            string error;
            bool ok;

            //260527 hbk Phase 34 D-34-01/02 — DualImage 변형: 두 파일에서 이미지 2개 로드 + 신규 2-image TryTeachDatum 호출.
            if (_editingDatum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) {
                string pathH = _editingDatum.TeachingImagePath; //260527 hbk Phase 34
                string pathV = _editingDatum.TeachingImagePath_Vertical; //260527 hbk Phase 34
                if (string.IsNullOrEmpty(pathH) || !System.IO.File.Exists(pathH)) { //260527 hbk Phase 34
                    label_drawHint.Visibility = Visibility.Collapsed;
                    CustomMessageBox.Show("티칭 실패", "가로축 티칭 이미지 경로가 비어 있거나 파일이 없습니다."); //260527 hbk Phase 34 D-34-10
                    _canvasMode = ECanvasMode.None; btn_teachDatum.IsChecked = false; _editingDatum = null; halconViewer.IsTeachDatumMode = false;
                    return;
                }
                if (string.IsNullOrEmpty(pathV) || !System.IO.File.Exists(pathV)) { //260527 hbk Phase 34
                    label_drawHint.Visibility = Visibility.Collapsed;
                    CustomMessageBox.Show("티칭 실패", "세로축 티칭 이미지 경로가 비어 있거나 파일이 없습니다."); //260527 hbk Phase 34 D-34-10
                    _canvasMode = ECanvasMode.None; btn_teachDatum.IsChecked = false; _editingDatum = null; halconViewer.IsTeachDatumMode = false;
                    return;
                }
                HImage imgH = null, imgV = null; //260527 hbk Phase 34
                try {
                    try { imgH = new HImage(pathH); } catch (Exception exH) { error = "가로축 이미지 로드 실패: " + exH.Message; ok = false; goto AfterTeach; } //260527 hbk Phase 34
                    try { imgV = new HImage(pathV); } catch (Exception exV) { error = "세로축 이미지 로드 실패: " + exV.Message; ok = false; goto AfterTeach; } //260527 hbk Phase 34
                    ok = svc.TryTeachDatum(imgH, imgV, _editingDatum, out error); //260527 hbk Phase 34 D-34-01/02 — 2-image 오버로드
                } finally {
                    if (imgH != null) { try { imgH.Dispose(); } catch { } } //260527 hbk Phase 34
                    if (imgV != null) { try { imgV.Dispose(); } catch { } } //260527 hbk Phase 34
                }
            } else {
                ok = svc.TryTeachDatum(img, _editingDatum, out error); //기존 단일-이미지 오버로드 (회귀 0)
            }

            AfterTeach: //260527 hbk Phase 34 — 공통 결과 처리 분기
            if (ok) {
                label_drawHint.Content = "Datum 티칭 완료 — Recipe Save 권장";
                label_drawHint.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FF4ADE80"));
                label_drawHint.Visibility = Visibility.Visible;
                halconViewer.SetDatumOverlay(_editingDatum, true);
                PublishDatumRoiCandidates(_editingDatum);
                UpdateDatumRefCoordsLabel(_editingDatum);
            }
            else {
                label_drawHint.Visibility = Visibility.Collapsed;
                CustomMessageBox.Show("티칭 실패", FormatTeachError(_editingDatum, error));
            }

            _canvasMode = ECanvasMode.None;
            btn_teachDatum.IsChecked = false;
            _editingDatum = null;
            halconViewer.IsTeachDatumMode = false;
        }
```

**대안 (goto 회피 권장):** goto 패턴 대신 helper 메서드 추출 또는 inline 분기로 재구성. C# 7.2 에서 goto 는 합법이나 가독성 위해 PostTeachResult(...) 헬퍼 또는 early-return 패턴 권장. 다음과 같이 단순화 가능 — try-catch 안에서 ok/error 만 결정 후 try 밖에서 결과 처리.

**제약 (D-34-06/07/08/09/10/13):**
- K&R 스타일 (PATTERNS.md Shared Patterns — MainView K&R).
- 4 위치 모두 algorithm 분기 추가만, 기존 4 algorithm 의 라벨/로직 0 라인 수정.
- StartDatumTeachStep 의 다른 case (Line1/Line2/Circle/Done) 변경 0 라인.
- ValidateRoiPresence default fallthrough `return null;` (line 1035) 변경 0.
- 모든 신규 라인에 `//260527 hbk Phase 34 D-34-XX` 주석.
- IOfflineImageParam (DatumConfig.GetLatestImagePath/SetLatestImagePath) 변경 0 — TeachingImagePath_Vertical 의 Load UI 는 본 Plan 범위 외 (Plan 04 UAT 시 검증 — 사용자가 PropertyGrid 에서 직접 경로 입력).
  </action>

  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "$f = 'WPF_Example/UI/ContentItem/MainView.xaml.cs'; $caseCount = (Select-String -Path $f -Pattern 'case EDatumAlgorithm\.VerticalTwoHorizontalDualImage' -AllMatches | Measure-Object).Count; $dualMsg = (Select-String -Path $f -Pattern '세로축 티칭 이미지 경로' -Quiet); $swap = (Select-String -Path $f -Pattern 'halconViewer\.LoadImage\(vpath\)' -Quiet); $teach2 = (Select-String -Path $f -Pattern 'svc\.TryTeachDatum\(imgH, imgV' -Quiet); if ($caseCount -ge 2 -and $dualMsg -and $swap -and $teach2) { Write-Host 'PASS caseCount='$caseCount' dualMsg='$dualMsg' swap='$swap' teach2='$teach2 } else { Write-Host 'FAIL caseCount='$caseCount' dualMsg='$dualMsg' swap='$swap' teach2='$teach2; exit 1 }"</automated>
  </verify>

  <acceptance_criteria>
    - MainView.xaml.cs 에 `case EDatumAlgorithm.VerticalTwoHorizontalDualImage:` 식별자가 최소 2개 (GetAlgorithmSteps + ValidateRoiPresence)
    - GetAlgorithmSteps 안 DualImage case 가 `[EDatumTeachStep.HorizontalA, EDatumTeachStep.HorizontalB, EDatumTeachStep.Vertical]` 순서 반환 (grep "HorizontalA, EDatumTeachStep.HorizontalB, EDatumTeachStep.Vertical")
    - ValidateRoiPresence 안 DualImage case 가 "세로축 티칭 이미지 경로가 비어 있습니다" 문구 포함
    - StartDatumTeachStep Vertical case 안에 `halconViewer.LoadImage(vpath)` 호출 + `_editingDatum.TeachingImagePath_Vertical` 참조 1개씩 존재
    - StartDatumTeachStep HorizontalA/HorizontalB case 안에 algorithm == DualImage 라벨 분기 (`if (_editingDatum != null && _editingDatum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage)`) 각 1개
    - InvokeTryTeachDatum 안에 `svc.TryTeachDatum(imgH, imgV, _editingDatum, out error)` 또는 동등 호출 1개 (2-image 오버로드)
    - 기존 1-image 4 algorithm 의 GetAlgorithmSteps return 라인 0 변경 (grep 라인 보존)
    - 기존 ValidateRoiPresence 3 case (TLI/CTH/VTH) 라인 0 변경
    - 신규 라인에 `//260527 hbk Phase 34 D-34` 주석 포함 (grep 카운트 ≥ 10)
  </acceptance_criteria>

  <done>MainView.xaml.cs 의 4 메서드 (GetAlgorithmSteps / ValidateRoiPresence / StartDatumTeachStep / InvokeTryTeachDatum) 가 모두 DualImage 분기를 갖고, 기존 1-image 경로는 0 라인 변경됨.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Action_FAIMeasurement.EStep.DatumPhase 안 DualImage 분기 + TryGrabOrLoadDualDatumImages 신규 함수 + msbuild Debug/x64 PASS</name>

  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (offset=70, limit=120 — EStep.DatumPhase 진입부)
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (offset=255, limit=40 — GrabOrLoadDatumImage 기존 함수)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (offset=140, limit=40 — TryRunDatumPhase 본문 — 변경 0 가드 참조)
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-PATTERNS.md (section "12) Action_FAIMeasurement.cs — GrabOrLoadDatumImage")
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md (D-34-13, D-34-14)
  </read_first>

  <behavior>
    - Test 1 (신규 private 함수 TryGrabOrLoadDualDatumImages): 시그니처 `private bool TryGrabOrLoadDualDatumImages(InspectionSequence parentSeq, out HImage imageHorizontal, out HImage imageVertical)`. 본문은 DatumConfigs[0].TeachingImagePath + TeachingImagePath_Vertical 파일에서 두 HImage 를 로드 (SIMUL_MODE 또는 실 카메라 무관 — Datum 티칭 이미지 = 파일 경로 보존). 빈 경로 시 false + 로그 (D-34-09).
    - Test 2 (EStep.DatumPhase 분기): DatumConfigs[0].AlgorithmTypeEnum == VerticalTwoHorizontalDualImage 면 신규 경로 (TryGrabOrLoadDualDatumImages + DatumFindingService.TryFindDatum 2-image 직접 호출 + DatumConfig.CurrentTransform 저장 + parentSeq 의 transform map 갱신은 InspectionSequence 의 책임이므로 우회). else 기존 경로 (GrabOrLoadDatumImage + parentSeq.TryRunDatumPhase).
    - Test 3 (D-34-14 가드 — InspectionSequence.TryRunDatumPhase 우회): DualImage 경로는 parentSeq.TryRunDatumPhase 호출 안 함. InspectionSequence.cs 변경 0 라인.
    - Test 4 (회귀): 기존 1-image algorithm 3종 경로 (EStep.DatumPhase 안 `parentSeq.TryRunDatumPhase(datumImage, out datumError)`) 0 라인 변경.
    - Test 5 (transform 채움 보장): DualImage 분기에서 DatumFindingService.TryFindDatum 호출 후 transform 을 어디에 저장하는가? — InspectionSequence._datumTransforms 는 private. 우회 방법: DatumConfig.CurrentTransform 만 채움 + parentSeq.TryRunDatumPhase 가 빈 호출이라도 한 번 실행 → DualImage 경로는 별도 transform 등록 site 필요.
    - Test 6 (단순화 결정): DualImage 의 Side fixture 케이스는 **단일 Datum 가정** (Phase 33 D-06 동일). 따라서 DatumConfigs[0] 만 처리. parentSeq.TryRunDatumPhase 우회 후, transform 저장은 DatumConfig.CurrentTransform 만 사용 (FAI Measurement 가 meas.DatumRef 로 lookup 시 InspectionSequence 의 transform map 이 비어있으면 identity fallback — line 152). **따라서 DualImage 케이스에서는 FAI Measurement 의 IDatumOriginConsumer 채움이 안 되는 한계가 있을 수 있음** → Plan 04 UAT 시 검증 필요. 단, 본 Plan 범위는 Datum 검출 자체 PASS 확인까지.
    - Test 7: msbuild Debug/x64 Rebuild PASS, 신규 warning 0.
  </behavior>

  <action>
WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs 의 두 위치를 수정/추가한다:

**위치 A — EStep.DatumPhase (line 78-104) 의 진입 분기 추가:**

기존:
```csharp
                case EStep.DatumPhase: {
                    var parentSeq = ShotParam != null ? ShotParam.Parent as InspectionSequence : null;
                    if (parentSeq != null && parentSeq.DatumConfigs.Count > 0) {
                        HImage datumImage = GrabOrLoadDatumImage(parentSeq);
                        if (datumImage == null) { ... break; }
                        try {
                            string datumError;
                            if (!parentSeq.TryRunDatumPhase(datumImage, out datumError)) {
                                ... FinishAction(EContextResult.Error);
                                break;
                            }
                        } finally { datumImage.Dispose(); }
                    }
                    Step = (int)EStep.Grab;
                    break;
                }
```

변경 후 (algorithm 보고 분기):
```csharp
                case EStep.DatumPhase: {
                    var parentSeq = ShotParam != null ? ShotParam.Parent as InspectionSequence : null;
                    if (parentSeq != null && parentSeq.DatumConfigs.Count > 0) {
                        //260527 hbk Phase 34 D-34-13 — DualImage 변형 분기: DatumConfigs[0] algorithm 검사 후 신규 2-image 경로 또는 기존 1-image 경로 선택.
                        //  D-34-14 가드: InspectionSequence.TryRunDatumPhase 시그니처 변경 0 — DualImage 경로는 본 클래스 안에서 DatumFindingService.TryFindDatum(2-image) 직접 호출하여 우회.
                        if (parentSeq.DatumConfigs[0].AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) { //260527 hbk Phase 34 D-34-13
                            HImage imgH = null, imgV = null; //260527 hbk Phase 34
                            try {
                                if (!TryGrabOrLoadDualDatumImages(parentSeq, out imgH, out imgV)) { //260527 hbk Phase 34 D-34-13
                                    Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] DualImage Datum image acquisition failed"); //260527 hbk Phase 34 D-34-09
                                    pMyContext.AllPass = false;
                                    FinishAction(EContextResult.Error);
                                    break;
                                }
                                var svc = new ReringProject.Halcon.Algorithms.DatumFindingService(); //260527 hbk Phase 34
                                HTuple transform; //260527 hbk Phase 34
                                string datumError; //260527 hbk Phase 34
                                if (!svc.TryFindDatum(imgH, imgV, parentSeq.DatumConfigs[0], out transform, out datumError)) { //260527 hbk Phase 34 D-34-01/02
                                    Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] DualImage Datum failed: " + datumError); //260527 hbk Phase 34
                                    pMyContext.AllPass = false;
                                    FinishAction(EContextResult.Error);
                                    break;
                                }
                                parentSeq.DatumConfigs[0].CurrentTransform = transform; //260527 hbk Phase 34 — DatumConfig transient 만 채움 (InspectionSequence._datumTransforms private 우회). FAI Measurement 의 meas.DatumRef lookup 은 InspectionSequence.TryGetDatumTransform 가 identity fallback 처리 (Action_FAIMeasurement L150-156). 본 Plan 범위는 Datum 자체 검출 PASS — FAI Measurement 의 IDatumOriginConsumer 채움 한계는 Plan 04 UAT 시 검증 후 hotfix 후보.
                            } finally {
                                if (imgH != null) { try { imgH.Dispose(); } catch { } } //260527 hbk Phase 34
                                if (imgV != null) { try { imgV.Dispose(); } catch { } } //260527 hbk Phase 34
                            }
                        } else {
                            //기존 1-image 경로 (TLI/CTH/VTH/default) — 0 라인 변경 (회귀 0, D-34-14)
                            HImage datumImage = GrabOrLoadDatumImage(parentSeq);
                            if (datumImage == null) {
                                Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum image acquisition failed");
                                pMyContext.AllPass = false;
                                FinishAction(EContextResult.Error);
                                break;
                            }
                            try {
                                string datumError;
                                if (!parentSeq.TryRunDatumPhase(datumImage, out datumError)) {
                                    Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum failed: " + datumError);
                                    pMyContext.AllPass = false;
                                    FinishAction(EContextResult.Error);
                                    break;
                                }
                            } finally {
                                datumImage.Dispose();
                            }
                        }
                    }
                    Step = (int)EStep.Grab;
                    break;
                }
```

**위치 B — GrabOrLoadDatumImage (line 257-288) 직후에 신규 private 함수 추가:**

```csharp
        //260527 hbk Phase 34 D-34-13 — DualImage 변형용 두 이미지 동시 로드.
        //  기존 GrabOrLoadDatumImage 시그니처 unchanged. 본 함수만 신규 추가.
        //  imageHorizontal: DatumConfigs[0].TeachingImagePath 에서 로드 (가로축 ROI 검출용)
        //  imageVertical:   DatumConfigs[0].TeachingImagePath_Vertical 에서 로드 (세로축 ROI 검출용)
        //  빈 경로 또는 파일 없음 / HImage 생성 실패 시 false + 로그.
        //  SIMUL_MODE 기준 — Plan 04 UAT 가 Side fixture 이미지 2장으로 검증. 실 카메라 시점에서는 Datum 티칭 이미지 = 파일 경로 보존 모델 유지 (Phase 22 IMG-02 와 동일).
        private bool TryGrabOrLoadDualDatumImages(InspectionSequence parentSeq, out HImage imageHorizontal, out HImage imageVertical) { //260527 hbk Phase 34 D-34-13
            imageHorizontal = null; //260527 hbk Phase 34
            imageVertical = null; //260527 hbk Phase 34
            if (parentSeq == null || parentSeq.DatumConfigs == null || parentSeq.DatumConfigs.Count == 0) { //260527 hbk Phase 34
                Logging.PrintErrLog((int)ELogType.Error, "[Datum] DualImage: DatumConfigs 가 비어 있습니다."); //260527 hbk Phase 34 D-34-09
                return false; //260527 hbk Phase 34
            }
            var datum = parentSeq.DatumConfigs[0]; //260527 hbk Phase 34 — Side fixture 단일 Datum 가정
            string pathH = datum.TeachingImagePath; //260527 hbk Phase 34
            string pathV = datum.TeachingImagePath_Vertical; //260527 hbk Phase 34

            if (string.IsNullOrEmpty(pathH) || !File.Exists(pathH)) { //260527 hbk Phase 34 D-34-09
                Logging.PrintErrLog((int)ELogType.Error, "[Datum] 가로축 티칭 이미지 경로가 비어 있거나 파일이 없습니다 (DualImage)."); //260527 hbk Phase 34 D-34-10
                return false; //260527 hbk Phase 34
            }
            if (string.IsNullOrEmpty(pathV) || !File.Exists(pathV)) { //260527 hbk Phase 34 D-34-09
                Logging.PrintErrLog((int)ELogType.Error, "[Datum] 세로축 티칭 이미지 경로가 비어 있거나 파일이 없습니다 (DualImage)."); //260527 hbk Phase 34 D-34-10
                return false; //260527 hbk Phase 34
            }

            try { imageHorizontal = new HImage(pathH); } catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, "[Datum] 가로축 이미지 로드 실패: " + ex.Message); imageHorizontal = null; } //260527 hbk Phase 34
            try { imageVertical = new HImage(pathV); } catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, "[Datum] 세로축 이미지 로드 실패: " + ex.Message); imageVertical = null; } //260527 hbk Phase 34

            if (imageHorizontal == null || imageVertical == null) { //260527 hbk Phase 34
                if (imageHorizontal != null) { try { imageHorizontal.Dispose(); } catch { } } //260527 hbk Phase 34
                if (imageVertical != null) { try { imageVertical.Dispose(); } catch { } } //260527 hbk Phase 34
                imageHorizontal = null; //260527 hbk Phase 34
                imageVertical = null; //260527 hbk Phase 34
                return false; //260527 hbk Phase 34
            }
            return true; //260527 hbk Phase 34
        }
```

**msbuild 검증 (Task 1 + Task 2 모두 완료 후):**

```powershell
& "C:\Program Files (x86)\Microsoft Visual Studio\2019\BuildTools\MSBuild\Current\Bin\MSBuild.exe" `
  WPF_Example/DatumMeasurement.csproj `
  /p:Configuration=Debug /p:Platform=x64 `
  /t:Rebuild /v:minimal
```

- 종료 코드 0
- 신규 CS error 0
- 신규 warning 0 (Phase 21 baseline 동일)

**제약 (D-34-13/14):**
- K&R 스타일 (Action_FAIMeasurement 는 기존 K&R).
- Action_FAIMeasurement.cs 단일 파일만 변경 (위치 A + 위치 B).
- `InspectionSequence.cs`, `VisionResponsePacket.cs` 변경 0 라인 (D-34-14 가드).
- GrabOrLoadDatumImage (line 257-288) 함수 본문 0 라인 변경 — 신규 함수가 별도 추가만.
- 기존 1-image 경로 (else 블록) 의 본문은 100% 기존 EStep.DatumPhase 본문 보존 (line 80-103) — 라인 단위 동일.
- 모든 신규 라인에 `//260527 hbk Phase 34 D-34-XX` 주석.
- `git diff --stat WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 결과: 추가 라인 ≈ 50 ~ 65, 삭제 라인 0 ~ 5 (else 블록으로 들여쓰기 변경만).
  </action>

  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "$f = 'WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs'; $dualFn = (Select-String -Path $f -Pattern 'private bool TryGrabOrLoadDualDatumImages' -Quiet); $dualCall = (Select-String -Path $f -Pattern 'TryGrabOrLoadDualDatumImages\(parentSeq' -Quiet); $dualFind = (Select-String -Path $f -Pattern 'svc\.TryFindDatum\(imgH, imgV' -Quiet); $algCheck = (Select-String -Path $f -Pattern 'AlgorithmTypeEnum == EDatumAlgorithm\.VerticalTwoHorizontalDualImage' -Quiet); if ($dualFn -and $dualCall -and $dualFind -and $algCheck) { Write-Host 'PASS dualFn='$dualFn' dualCall='$dualCall' dualFind='$dualFind' algCheck='$algCheck } else { Write-Host 'FAIL dualFn='$dualFn' dualCall='$dualCall' dualFind='$dualFind' algCheck='$algCheck; exit 1 }"</automated>
  </verify>

  <acceptance_criteria>
    - Action_FAIMeasurement.cs 에 `private bool TryGrabOrLoadDualDatumImages(InspectionSequence parentSeq, out HImage imageHorizontal, out HImage imageVertical)` 시그니처 정확히 1개
    - Action_FAIMeasurement.cs 에 `TryGrabOrLoadDualDatumImages(parentSeq` 호출이 정확히 1개 (EStep.DatumPhase 안)
    - Action_FAIMeasurement.cs 에 `svc.TryFindDatum(imgH, imgV` 또는 동등 호출 1개 (2-image 오버로드)
    - Action_FAIMeasurement.cs 에 `AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage` 분기 정확히 1개
    - 기존 GrabOrLoadDatumImage 함수 본문 (line 257-288) 변경 0 라인 (git diff 검증)
    - 기존 1-image 경로의 `parentSeq.TryRunDatumPhase(datumImage, out datumError)` 호출이 보존됨 (정확히 1개, else 블록 안)
    - msbuild Debug/x64 Rebuild 종료 코드 0 (PASS)
    - msbuild 출력에 신규 CS error 0
    - msbuild 출력에 신규 warning 0 (Phase 21 baseline 6 동일)
    - git diff --numstat: InspectionSequence.cs 변경 0,0 (D-34-14 가드)
    - git diff --numstat: VisionResponsePacket.cs 변경 0,0 (D-34-14 가드)
    - git diff --numstat: Action_FAIMeasurement.cs 변경 추가 ≈ 50-65 라인, 삭제 0-5 라인 (들여쓰기만)
    - 모든 신규 라인에 `//260527 hbk Phase 34 D-34` 주석 포함 (grep 카운트 ≥ 20)
  </acceptance_criteria>

  <done>Action_FAIMeasurement.cs 가 EStep.DatumPhase 안 algorithm 분기 + TryGrabOrLoadDualDatumImages 신규 함수를 갖고, 기존 1-image 경로는 0 라인 변경, InspectionSequence.cs/VisionResponsePacket.cs 변경 0 라인, msbuild Debug/x64 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 PropertyGrid 입력 → DatumConfig.TeachingImagePath_Vertical | 사용자가 입력한 파일 경로의 존재/유효성. 빈 경로 / 잘못된 파일 / 다른 해상도 가능 |
| 티칭 wizard step 진행 → halconViewer.LoadImage(...) | 파일 시스템 I/O 실패 (권한, 디스크 오류) — try/catch 로 흡수 + 로그 |
| Action_FAIMeasurement 검사 사이클 → 두 파일 로드 | 검사 중 파일 사라짐 / 잠금 — Logging.PrintErrLog + FinishAction(Error) |
| InspectionSequence.TryRunDatumPhase 우회 | DualImage 경로가 _datumTransforms private 우회 → FAI Measurement 의 IDatumOriginConsumer 채움 일관성 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03-01 | Tampering | TeachingImagePath_Vertical 가 빈 문자열로 INI 저장된 상태에서 검사 트리거 | mitigate | Action_FAIMeasurement.TryGrabOrLoadDualDatumImages 진입부 `if (string.IsNullOrEmpty(pathV) \|\| !File.Exists(pathV))` 가드 + 한국어 에러 로그 + return false → FinishAction(EContextResult.Error). 사용자가 UI 에서 누락 인지 가능. |
| T-34-03-02 | Tampering | 두 이미지 파일이 서로 다른 해상도 → IntersectionLl 좌표 의미 불일치 | accept | Phase 34 가정 (CONTEXT D-34 Claude's Discretion #4): 동일 카메라 해상도. Plan 04 UAT 시 동일 카메라 시뮬레이션 이미지 사용 검증. |
| T-34-03-03 | DoS | HImage 객체 dispose 누락 → 메모리 누수 (티칭 + 검사 사이클 매 회) | mitigate | InvokeTryTeachDatum + EStep.DatumPhase 양쪽 모두 `try { ... } finally { imgH.Dispose(); imgV.Dispose(); }` 패턴 사용. TryGrabOrLoadDualDatumImages 실패 시 false 반환 전 imgH/V dispose 안전망. |
| T-34-03-04 | Information Disclosure | 검사 실패 로그에 파일 경로 노출 | accept | 기존 Logging 패턴과 동일 — 진단 목적 의도된 노출. |
| T-34-03-05 | Repudiation | wizard step 자동 swap 동작 추적 | mitigate | StartDatumTeachStep Vertical case 진입 시 swap 시도 + 실패 시 Logging.PrintErrLog. 사용자 시각적 피드백 (label_drawHint 안내 메시지). |
| T-34-03-06 | Tampering | DualImage 케이스에서 다중 Datum (DatumConfigs.Count > 1) 시 첫 Datum 만 처리 → 나머지 Datum 무시 | accept | Side fixture 가 단일 Datum 가정 (Phase 33 D-06 동일). 운영 중 다중 Datum + DualImage 혼합 케이스 발견 시 별도 phase. 본 Plan 은 첫 Datum 만 처리하고 break — 명시적 제약. |
| T-34-03-07 | Elevation of Privilege | unsafe 블록 / native callback 도입 | mitigate | Plan 03 변경 범위 = UI 분기 + 함수 추가만. unsafe / P/Invoke 0. |
| T-34-03-08 | DoS | _datumTransforms private 우회 → IDatumOriginConsumer 채움 누락 → FAI Measurement 결과 stale (이전 Datum transform 사용) | mitigate | DatumConfig.CurrentTransform 채움 (위치 A 의 `parentSeq.DatumConfigs[0].CurrentTransform = transform;`). InspectionSequence.TryGetDatumTransform 가 _datumTransforms 비어있을 때 identity fallback (Action_FAIMeasurement L150-156) → 최악의 경우 identity transform 적용 → 측정값은 절대좌표 (Datum 보정 0). Plan 04 UAT 가 IDatumOriginConsumer 채움까지 검증 — 한계 발견 시 hotfix. |
</threat_model>

<verification>
## Plan 03 검증

**Smoke:**
- MainView.xaml.cs 컴파일 통과.
- Action_FAIMeasurement.cs 컴파일 통과.
- 기존 1-image 4 algorithm 의 GetAlgorithmSteps / ValidateRoiPresence / StartDatumTeachStep / InvokeTryTeachDatum 동작 0 라인 변경.
- 기존 GrabOrLoadDatumImage 함수 본문 0 라인 변경.

**File diff guards (D-34-13/14):**
- Plan 03 변경 파일 = MainView.xaml.cs + Action_FAIMeasurement.cs 2 파일 만.
- 다음 파일들은 Plan 03 단계에서 **변경 0 라인** (PowerShell):
  - `git diff --numstat HEAD -- WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` → 0,0
  - `git diff --numstat HEAD -- WPF_Example/TcpServer/VisionResponsePacket.cs` → 0,0
  - `git diff --numstat HEAD -- WPF_Example/Halcon/Algorithms/DatumFindingService.cs` → 0,0 (Plan 02 변경 외)
  - `git diff --numstat HEAD -- WPF_Example/Custom/Sequence/Inspection/EDatumAlgorithm.cs` → 0,0 (Plan 01 변경 외)
  - `git diff --numstat HEAD -- WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` → 0,0 (Plan 01 변경 외)

**Action_FAIMeasurement.cs 변경 범위 (D-34-13 한정 변경 site 검증):**
- 변경 site 1: EStep.DatumPhase 안 (line 78-104) — algorithm 분기 추가 (들여쓰기 + else 블록 포함)
- 변경 site 2: GrabOrLoadDatumImage 함수 직후 (line 288 이후) — 신규 TryGrabOrLoadDualDatumImages 함수 추가
- 그 외 site (EStep.Init/Grab/Measure/End, Run 메서드 외부 필드, GrabOrLoadDatumImage 본문) 변경 0 라인 → git blame + git diff --stat 검증

**msbuild:**
- Debug/x64 Rebuild 종료 코드 0
- Phase 21 baseline warning 수와 동일 (신규 warning 0)

**기능 검증 (런타임 0 — Plan 04 UAT 에서 검증):**
- 빈 경로 가드 동작은 Plan 04 Test 3 시 검증
- 실제 Datum 검출 PASS 는 Plan 04 Test 3 시 검증
- 기존 1-image 회귀는 Plan 04 Test 2 시 검증
- INI 라운드트립 byte-identical 은 Plan 04 Test 4 시 검증
</verification>

<success_criteria>
- [ ] MainView.GetAlgorithmSteps 에 DualImage case 추가 (순서 = HA→HB→V)
- [ ] MainView.ValidateRoiPresence 에 DualImage case 추가 (Vertical/H_A/H_B Length + 두 이미지 경로 검증)
- [ ] MainView.StartDatumTeachStep Vertical case 안 algorithm 분기 + 자동 image swap + 빈 경로 가드
- [ ] MainView.StartDatumTeachStep HorizontalA/HorizontalB case 안 algorithm 분기 (라벨만)
- [ ] MainView.InvokeTryTeachDatum 안 algorithm 분기 (DualImage 시 두 파일 로드 + 신규 2-image TryTeachDatum 호출)
- [ ] Action_FAIMeasurement.EStep.DatumPhase 안 algorithm 분기 (DualImage 시 TryGrabOrLoadDualDatumImages + DatumFindingService.TryFindDatum 직접 호출)
- [ ] Action_FAIMeasurement 에 신규 private 함수 TryGrabOrLoadDualDatumImages 추가
- [ ] 기존 1-image 4 algorithm 의 모든 UI/검사 경로 0 라인 변경 (회귀 0)
- [ ] D-34-14 가드 — InspectionSequence.cs / VisionResponsePacket.cs 변경 0 라인
- [ ] D-34-13 가드 — Action_FAIMeasurement.cs 변경 site 2개 한정 (EStep.DatumPhase 분기 + 신규 함수)
- [ ] 모든 신규 라인에 `//260527 hbk Phase 34 D-34-XX` 주석
- [ ] msbuild Debug/x64 Rebuild PASS, 신규 warning 0
</success_criteria>

<output>
After completion, create `.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-03-SUMMARY.md` per template — include msbuild PASS 증거 + git diff --numstat 결과 (변경 파일 2개, 나머지 5 파일 0,0) + 4 메서드 분기 site + 1 신규 함수 시그니처 명시 + IDatumOriginConsumer 채움 한계 (T-34-03-08) Plan 04 UAT 검증 대상 명시.
</output>
