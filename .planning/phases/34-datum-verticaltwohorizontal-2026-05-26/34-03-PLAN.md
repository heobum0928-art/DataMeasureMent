---
phase: 34-datum-verticaltwohorizontal-2026-05-26
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: true
requirements:
  - SC-1   # AlgorithmType 콤보박스 노출 (Plan 01 에서 데이터 모델 추가, 본 Plan 에서 wizard step 시퀀스 노출)
  - SC-4   # 티칭 UI step 머신에 이미지 전환 step 추가 (Horizontal_A → Horizontal_B → image switch → Vertical)
  - SC-5   # Action_FAIMeasurement GrabOrLoadDatumImage 가 ROI 별 이미지 선택 (algorithm = 신규일 때 vertical ROI 는 image2) + Datum transform 이 _datumTransforms 에 등록되어 FAI Measurement 가 절대좌표가 아닌 보정좌표를 사용
  - SC-7   # 기존 1-image 타입 회귀 0 (msbuild PASS + Top recipe 로드 확인)

must_haves:
  truths:
    - "InspectionSequence 에 `public bool TryRunDatumPhase(HImage image1, HImage image2, out string error)` 2-image 오버로드가 신규로 노출된다 (D-34-14 정정 — DualImage seam 한정 ≤15 라인 허용)"
    - "신규 오버로드는 `_datumTransforms.Clear()` 후 DatumConfigs 순회 → DualImage algorithm 이면 DatumFindingService.TryFindDatum(image1, image2, ...) 호출, 그 외 algorithm 이면 image1 사용한 기존 단일-image 경로 폴백 → 결과 transform 을 `_datumTransforms[DatumName]` 에 저장 (1-image 오버로드와 동일 transform map 채움 규약)"
    - "기존 `TryRunDatumPhase(HImage)` 단일 시그니처/본문/_datumTransforms 필드/TryGetDatumTransform 변경 0 라인 (호출자 회귀 0)"
    - "MainView 의 GetAlgorithmSteps switch 가 VerticalTwoHorizontalDualImage 알고리즘에 대해 [HorizontalA, HorizontalB, Vertical] 순서 step 배열을 반환한다"
    - "Vertical step 진입 시 algorithm == DualImage 이면 halconViewer 가 TeachingImagePath_Vertical 이미지로 자동 swap 된다"
    - "TeachingImagePath_Vertical 가 비어있는 상태로 Vertical step 진입 시 안내 메시지 + 드로잉 차단 (저장 차단 아님)"
    - "MainView.ValidateRoiPresence 가 DualImage 케이스에서 Vertical/Horizontal_A/Horizontal_B ROI + 두 이미지 경로 모두 검증"
    - "InvokeTryTeachDatum 이 algorithm == DualImage 일 때 두 이미지를 파일에서 로드하여 신규 2-image TryTeachDatum 오버로드를 호출 (goto 패턴 0 — early-return + try/finally 만 사용)"
    - "Action_FAIMeasurement.Run() DatumPhase 가 algorithm == DualImage 일 때 두 이미지 로드 후 InspectionSequence.TryRunDatumPhase 2-image 오버로드를 호출 (DatumFindingService 직접 호출 우회 안 함 — transform 등록은 InspectionSequence 책임)"
    - "DualImage 경로 종료 후 `parentSeq.TryGetDatumTransform(DatumName, out t)` 호출 시 신규 오버로드가 채운 transform 이 반환됨 (identity fallback 회피 — T-34-03-08 해소)"
    - "기존 1-image 3종 algorithm 경로는 0 라인 변경 — Top recipe 회귀 0"
    - "msbuild Debug/x64 Rebuild PASS, 신규 warning 0"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "TryRunDatumPhase 2-image 오버로드 (DualImage seam 한정 ≤15 신규 라인) — _datumTransforms 채우는 dispatch 단일 site"
      contains: "TryRunDatumPhase(HImage image1, HImage image2"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "GetAlgorithmSteps DualImage case + StartDatumTeachStep Vertical 이미지 swap + ValidateRoiPresence DualImage case + InvokeTryTeachDatum 2-image 분기 (goto 0)"
      contains: "VerticalTwoHorizontalDualImage"
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "EStep.DatumPhase 안 algorithm 분기 (DualImage → InspectionSequence.TryRunDatumPhase 2-image 오버로드 호출) + TryGrabOrLoadDualDatumImages 신규 private 함수"
      contains: "TryGrabOrLoadDualDatumImages"
  key_links:
    - from: "InspectionSequence.TryRunDatumPhase 2-image 오버로드 (신규)"
      to: "DatumFindingService.TryFindDatum(image1, image2, ...) (Plan 02 신규 오버로드) → _datumTransforms[DatumName] = transform"
      via: "DatumConfigs foreach + algorithm 분기 dispatch + dict 저장"
      pattern: "_datumTransforms\\[.*\\] = transform"
    - from: "Action_FAIMeasurement EStep.DatumPhase (DualImage 분기, line 78-104)"
      to: "InspectionSequence._datumTransforms (TryRunDatumPhase 2-image 오버로드 경유)"
      via: "transform 등록 — parentSeq.TryRunDatumPhase(imgH, imgV, out error) 호출 후 InspectionSequence 가 dict 채움"
      pattern: "parentSeq.*\\.TryRunDatumPhase\\(\\s*imgH"
    - from: "Action_FAIMeasurement EStep.Measure (line 149)"
      to: "parentSeq2.TryGetDatumTransform(meas.DatumRef, out transform) — _datumTransforms 에서 hit (identity fallback 회피)"
      via: "DualImage 경로가 InspectionSequence 의 transform map 을 채운 후 후속 Measure step 이 lookup"
      pattern: "TryGetDatumTransform\\(meas\\.DatumRef"
    - from: "MainView.GetAlgorithmSteps switch (line 1911-1922)"
      to: "[EDatumTeachStep.HorizontalA, EDatumTeachStep.HorizontalB, EDatumTeachStep.Vertical]"
      via: "1-case 추가 (D-34-07 순서 — 가로축부터)"
      pattern: "case EDatumAlgorithm\\.VerticalTwoHorizontalDualImage"
    - from: "MainView.StartDatumTeachStep Vertical case (line 1980-1986)"
      to: "halconViewer.LoadImage(TeachingImagePath_Vertical)"
      via: "algorithm == DualImage 이면 진입 직전 자동 swap (D-34-06)"
      pattern: "TeachingImagePath_Vertical"
    - from: "MainView.InvokeTryTeachDatum (line 2106-2146)"
      to: "DatumFindingService.TryTeachDatum(imageHorizontal, imageVertical, ...)"
      via: "algorithm == DualImage 일 때 파일 2개 로드 + 신규 2-image 오버로드 호출 (goto 0 — try/finally + early-return)"
      pattern: "TryTeachDatum\\(imgH, imgV"
---

<objective>
InspectionSequence 의 DualImage seam (D-34-14 정정 허용) — 신규 `TryRunDatumPhase(HImage image1, HImage image2, out string error)` 오버로드 1개를 추가하여 `_datumTransforms` 채움 규약을 보존한다. MainView 의 Datum 티칭 UI 와 Action_FAIMeasurement 의 검사 사이클 양쪽에 VerticalTwoHorizontalDualImage 분기를 추가한다.

UI 측: step 순서 (HA→HB→V) + Vertical step 진입 시 자동 이미지 swap + ValidateRoiPresence + InvokeTryTeachDatum 2-image 분기 (goto 0).
검사 측: EStep.DatumPhase 안에서 algorithm 보고 두 이미지 로드 + **InspectionSequence.TryRunDatumPhase 2-image 오버로드 호출** (DatumFindingService 직접 호출 우회 제거). 이로써 `_datumTransforms` 가 채워져 후속 EStep.Measure 의 TryGetDatumTransform 이 정상 동작 → identity fallback 회피 → SC-5 완전 달성 (T-34-03-08 해소).

Purpose: 사용자가 PropertyGrid 에서 algorithm = VerticalTwoHorizontalDualImage 로 선택 → Datum 노드 Load image (가로축) → btn_teachDatum → HorizontalA/B 드로잉 → 자동 swap (세로축) → Vertical 드로잉 → 자동 teach 까지 end-to-end 완주. 그 후 검사 사이클 (Shot Grab → DatumPhase → FAI Measurement) 도 두 이미지로 자동 진행하며 Datum 보정이 실제로 measurement 에 적용됨. Plan 04 (UAT) 가 이를 SIMUL_MODE 로 검증.
Output: InspectionSequence.cs ≤15 신규 라인 (2-image 오버로드 1개) + MainView.xaml.cs 4개 메서드 분기 추가 + Action_FAIMeasurement.cs 1개 신규 함수 + 1개 분기. msbuild PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-PATTERNS.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-01-SUMMARY.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-02-SUMMARY.md

<!-- 34-01 + 34-02 SUMMARY 참조 이유: Plan 01 의 EDatumAlgorithm.VerticalTwoHorizontalDualImage 값 + DatumConfig.TeachingImagePath_Vertical 필드 + Plan 02 의 2-image TryFindDatum/TryTeachDatum 신규 오버로드를 본 Plan 의 호출처에서 사용. -->

<interfaces>
<!-- InspectionSequence.cs / MainView.xaml.cs / Action_FAIMeasurement.cs 에서 추출한 기존 시그니처 -->

From WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (line 143-171, 기존 TryRunDatumPhase 1-image 본문 — 변경 0):
```csharp
//260413 hbk Phase 6: Datum phase 실행 — 모든 DatumConfig 순회 (D-09)
public bool TryRunDatumPhase(HImage image, out string error) {
    error = null;
    _datumTransforms.Clear();

    if (DatumConfigs.Count == 0) {
        return true; // D-10: Datum 미설정 Fixture는 무보정 pass-through
    }

    if (image == null) {
        error = "image is null";
        return false;
    }

    var service = new DatumFindingService();
    foreach (var datum in DatumConfigs) {
        HTuple transform;
        string datumError;
        if (!service.TryFindDatum(image, datum, out transform, out datumError)) {
            error = $"Datum '{datum.DatumName}' failed: {datumError}";
            datum.LastFindSucceeded = false;
            return false;
        }
        datum.LastFindSucceeded = true;
        datum.CurrentTransform = transform;
        _datumTransforms[datum.DatumName ?? ""] = transform;
    }
    return true;
}
```

From WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (line 173-180, TryGetDatumTransform — 변경 0):
```csharp
public bool TryGetDatumTransform(string datumRef, out HTuple transform) {
    if (string.IsNullOrEmpty(datumRef)) {
        HOperatorSet.HomMat2dIdentity(out transform);
        return true;
    }
    return _datumTransforms.TryGetValue(datumRef, out transform);
}
```

From WPF_Example/UI/ContentItem/MainView.xaml.cs (line 59, EDatumTeachStep enum — 변경 0):
```csharp
private enum EDatumTeachStep { Line1, Line2, Circle, Vertical, HorizontalA, HorizontalB, Done }
```

From WPF_Example/UI/ContentItem/MainView.xaml.cs (line 1911-1922, GetAlgorithmSteps — 1 case 추가):
```csharp
private static EDatumTeachStep[] GetAlgorithmSteps(EDatumAlgorithm alg) {
    switch (alg) {
        case EDatumAlgorithm.TwoLineIntersect:
            return new[] { EDatumTeachStep.Line1, EDatumTeachStep.Line2 };
        case EDatumAlgorithm.CircleTwoHorizontal:
            return new[] { EDatumTeachStep.Circle, EDatumTeachStep.HorizontalA, EDatumTeachStep.HorizontalB };
        case EDatumAlgorithm.VerticalTwoHorizontal:
            return new[] { EDatumTeachStep.Vertical, EDatumTeachStep.HorizontalA, EDatumTeachStep.HorizontalB };
        default:
            return new EDatumTeachStep[0];
    }
}
```

From WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (line 78-104, EStep.DatumPhase 진입 — 분기 추가):
```csharp
case EStep.DatumPhase: {
    var parentSeq = ShotParam != null ? ShotParam.Parent as InspectionSequence : null;
    if (parentSeq != null && parentSeq.DatumConfigs.Count > 0) {
        HImage datumImage = GrabOrLoadDatumImage(parentSeq);
        if (datumImage == null) { ... FinishAction(EContextResult.Error); break; }
        try {
            string datumError;
            if (!parentSeq.TryRunDatumPhase(datumImage, out datumError)) { ... }
        } finally { datumImage.Dispose(); }
    }
    Step = (int)EStep.Grab;
    break;
}
```

From WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (line 149, EStep.Measure 의 TryGetDatumTransform lookup — 변경 0):
```csharp
if (parentSeq2 == null || !parentSeq2.TryGetDatumTransform(meas.DatumRef, out transform)) {
    // identity fallback (Datum transform 없음 → 절대좌표 사용)
    HOperatorSet.HomMat2dIdentity(out transform);
}
```

**핵심 설계 결정 (D-34-14 정정 2026-05-27):**
이전 설계 (InspectionSequence.TryRunDatumPhase 우회 + DatumFindingService 직접 호출 + DatumConfig.CurrentTransform 만 채움) 는 SC-5 silent 축소 (identity fallback) 문제로 폐기.

**채택 설계 (Plan-checker iteration 1 BLOCKER #1/#2 해소):**
1. InspectionSequence 에 **2-image 오버로드 1개 신설** (≤15 신규 라인 — D-34-14 정정 허용).
2. Action_FAIMeasurement 가 두 이미지 로드 후 **InspectionSequence.TryRunDatumPhase 2-image 오버로드 호출** → InspectionSequence 가 `_datumTransforms` dict 채움.
3. 후속 EStep.Measure 의 `parentSeq2.TryGetDatumTransform(meas.DatumRef, out transform)` 가 dict 에서 hit → Datum 보정 실제 적용 → SC-5 완전 달성, T-34-03-08 해소.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: InspectionSequence.cs 에 TryRunDatumPhase 2-image 오버로드 신설 (≤15 신규 라인 — D-34-14 정정)</name>

  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (offset=140, limit=45 — TryRunDatumPhase 1-image 본문 + TryGetDatumTransform 전체 + _datumTransforms 필드 사용 패턴)
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md (D-34-14 정정 — DualImage seam 한정 ≤5 신규 라인 허용, 단 시그니처/주석/괄호 포함하여 실용적으로 ≤15)
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-02-SUMMARY.md (Plan 02 신규 DatumFindingService.TryFindDatum 2-image 오버로드 시그니처)
  </read_first>

  <behavior>
    - Test 1 (신규 오버로드 시그니처): `public bool TryRunDatumPhase(HImage image1, HImage image2, out string error)` 가 노출된다. image1 = 가로축, image2 = 세로축 (D-34-01 매핑).
    - Test 2 (transform map 채움 규약): 진입부에서 `_datumTransforms.Clear()` 호출. DatumConfigs 빈 경우 true 반환 (D-10 pass-through). image1 또는 image2 null 시 false + error.
    - Test 3 (algorithm 분기 dispatch):
      - DatumConfigs 순회: 각 DatumConfig 의 `AlgorithmTypeEnum == VerticalTwoHorizontalDualImage` 면 `service.TryFindDatum(image1, image2, datum, out transform, out datumError)` (Plan 02 신규 2-image 오버로드).
      - 그 외 algorithm 이면 `service.TryFindDatum(image1, datum, out transform, out datumError)` 폴백 (1-image 오버로드, image1 사용).
      - 성공 시 `datum.LastFindSucceeded = true; datum.CurrentTransform = transform; _datumTransforms[datum.DatumName ?? ""] = transform;` (1-image 오버로드와 동일 채움 규약).
      - 실패 시 `error = $"Datum '{datum.DatumName}' failed: {datumError}"; datum.LastFindSucceeded = false; return false;`.
    - Test 4 (기존 시그니처 0 라인 변경): `TryRunDatumPhase(HImage image, out string error)` (line 143-171) 본문 0 라인 수정. `TryGetDatumTransform` (line 173-180) 본문 0 라인 수정. `_datumTransforms` 필드 선언 0 라인 수정. RemoveDatum / 다른 메서드 0 라인 수정.
    - Test 5 (diff 가드): `git diff --numstat WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 결과 — 추가 라인 ≤ 15, 삭제 라인 0 (정확히 신규 메서드만 append).
  </behavior>

  <action>
WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs 의 기존 `TryRunDatumPhase(HImage image, out string error)` (line 143-171) 직후, `TryGetDatumTransform` (line 173) 직전에 신규 2-image 오버로드 1개를 append.

**신규 오버로드 (전체 — 시그니처/주석/괄호 포함 총 ≤15 신규 라인):**

```csharp
        //260527 hbk Phase 34 D-34-14 정정 — VerticalTwoHorizontalDualImage 전용 2-image 오버로드. image1=가로축, image2=세로축. _datumTransforms 채움 규약은 1-image 오버로드와 동일 (T-34-03-08 해소).
        public bool TryRunDatumPhase(HImage image1, HImage image2, out string error) {
            error = null;
            _datumTransforms.Clear();
            if (DatumConfigs.Count == 0) return true;
            if (image1 == null || image2 == null) { error = "image1 or image2 is null"; return false; }
            var service = new DatumFindingService();
            foreach (var datum in DatumConfigs) {
                HTuple transform; string datumError; bool ok;
                if (datum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) ok = service.TryFindDatum(image1, image2, datum, out transform, out datumError);
                else ok = service.TryFindDatum(image1, datum, out transform, out datumError);
                if (!ok) { error = $"Datum '{datum.DatumName}' failed: {datumError}"; datum.LastFindSucceeded = false; return false; }
                datum.LastFindSucceeded = true; datum.CurrentTransform = transform; _datumTransforms[datum.DatumName ?? ""] = transform;
            }
            return true;
        }
```

**라인 카운트:** 정확히 15 라인 (주석 1 + 시그니처 1 + 본문 12 + 닫는 괄호 1). D-34-14 정정의 "≤15 신규 라인 (메서드 시그니처/주석/괄호 포함)" 제약 충족.

**제약 (D-34-14 정정):**
- 기존 `TryRunDatumPhase(HImage)` (line 143-171) 본문 0 라인 수정 — line 단위 동일 보존.
- 기존 `TryGetDatumTransform` (line 173-180) 본문 0 라인 수정.
- `_datumTransforms` 필드 선언 0 라인 수정.
- 기존 RemoveDatum / 다른 메서드 0 라인 수정.
- K&R 스타일 (InspectionSequence 는 기존 K&R — line 144 `public bool TryRunDatumPhase(...) {` 동일 줄 brace).
- 모든 신규 라인에 phase 주석 (첫 줄 `//260527 hbk Phase 34 D-34-14`).
- AlgorithmTypeEnum 분기에서 string fallback 아닌 enum 비교 사용 (Plan 01 의 enum 신설).
- `service.TryFindDatum(image1, datum, ...)` (1-image 폴백) 도 동일 service 인스턴스 재사용 — 폴백 호출은 image1 (가로축) 만 사용, image2 는 미사용 (DatumConfigs 안에 DualImage 가 아닌 datum 이 섞여 있을 때를 위한 안전망).
- 단순화 결정: Side fixture 가 단일 Datum 가정 (Phase 33 D-06) 이지만, foreach 는 보수적으로 유지 — 폴백 경로가 image1 만 사용하므로 다중 Datum 혼합 안전.
  </action>

  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "$f = 'WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs'; $newOverload = (Select-String -Path $f -Pattern 'TryRunDatumPhase\(HImage image1, HImage image2' -Quiet); $clear = (Select-String -Path $f -Pattern '_datumTransforms\.Clear\(\)' -AllMatches | Measure-Object).Count; $assign = (Select-String -Path $f -Pattern '_datumTransforms\[datum\.DatumName' -AllMatches | Measure-Object).Count; $dualBranch = (Select-String -Path $f -Pattern 'EDatumAlgorithm\.VerticalTwoHorizontalDualImage' -Quiet); $find2 = (Select-String -Path $f -Pattern 'service\.TryFindDatum\(image1, image2' -Quiet); if ($newOverload -and $clear -ge 2 -and $assign -ge 2 -and $dualBranch -and $find2) { Write-Host 'PASS overload='$newOverload' clearCount='$clear' assignCount='$assign' dual='$dualBranch' find2='$find2 } else { Write-Host 'FAIL overload='$newOverload' clearCount='$clear' assignCount='$assign' dual='$dualBranch' find2='$find2; exit 1 }"</automated>
  </verify>

  <acceptance_criteria>
    - InspectionSequence.cs 에 `TryRunDatumPhase(HImage image1, HImage image2, out string error)` 시그니처 정확히 1개 (신규 오버로드)
    - 기존 `TryRunDatumPhase(HImage image, out string error)` 시그니처 보존 (정확히 1개)
    - `_datumTransforms.Clear()` 호출이 정확히 2개 (기존 1-image + 신규 2-image 오버로드)
    - `_datumTransforms[datum.DatumName` 할당이 정확히 2개 (양쪽 오버로드)
    - 신규 오버로드 안에 `service.TryFindDatum(image1, image2,` 호출 1개 + `service.TryFindDatum(image1, datum,` 폴백 호출 1개
    - 신규 오버로드 안에 `EDatumAlgorithm.VerticalTwoHorizontalDualImage` 분기 1개
    - `git diff --numstat WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 결과: 추가 라인 ≤ 15, 삭제 라인 = 0 (D-34-14 정정 가드)
    - `git diff WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 의 hunk 1개 (신규 메서드 1개 append 만)
    - 기존 `TryRunDatumPhase(HImage image, out string error)` (line 143-171) 본문 라인 0 변경 (git diff hunk 가 line 171 이후만 포함)
    - 기존 `TryGetDatumTransform` (line 173-180) 본문 라인 0 변경
    - 신규 첫 줄에 `//260527 hbk Phase 34 D-34-14` 주석 포함
  </acceptance_criteria>

  <done>InspectionSequence.cs 에 2-image TryRunDatumPhase 오버로드가 추가되어 `_datumTransforms` 채움 규약을 보존하고, 기존 메서드/필드는 0 라인 변경됨. diff 카운트 ≤ 15.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MainView.GetAlgorithmSteps + ValidateRoiPresence + StartDatumTeachStep 자동 이미지 swap + InvokeTryTeachDatum 2-image 분기 (goto 0)</name>

  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (offset=1009, limit=40 — ValidateRoiPresence)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (offset=1909, limit=110 — GetAlgorithmSteps + IsStepMissing + StartDatumTeachStep 시작부)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (offset=1965, limit=50 — StartDatumTeachStep switch case 본문 전체)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (offset=2104, limit=50 — InvokeTryTeachDatum 전체)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (offset=140, limit=25 — DisplayDatumImage 패턴 참조, image swap 동일 호출 방식)
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-PATTERNS.md (sections "8) GetAlgorithmSteps", "9) ValidateRoiPresence", "10) StartDatumTeachStep + 자동 이미지 전환")
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md (D-34-06, D-34-07, D-34-08, D-34-09, D-34-10)
  </read_first>

  <behavior>
    - Test 1 (GetAlgorithmSteps): switch 안에 `case EDatumAlgorithm.VerticalTwoHorizontalDualImage:` 추가 → `return new[] { EDatumTeachStep.HorizontalA, EDatumTeachStep.HorizontalB, EDatumTeachStep.Vertical };` (D-34-07 순서 — 가로축 먼저).
    - Test 2 (ValidateRoiPresence): switch 안에 `case EDatumAlgorithm.VerticalTwoHorizontalDualImage:` 추가 → Vertical_Length1 + Horizontal_A_Length1 + Horizontal_B_Length1 + TeachingImagePath (가로축) + TeachingImagePath_Vertical (세로축) 5가지 모두 검증. 빈 경로면 한국어 안내 메시지 반환.
    - Test 3 (StartDatumTeachStep Vertical case 진입): algorithm == DualImage 이면 `halconViewer.LoadImage(_editingDatum.TeachingImagePath_Vertical)` 자동 호출 + 라벨을 "Step 3/3: 수직 ROI를 드래그하세요" 로 갱신. 경로 빈 경우 안내 메시지 + 드로잉 차단.
    - Test 4 (StartDatumTeachStep HorizontalA/B 라벨): algorithm == DualImage 이면 라벨이 "Step 1/3: 수평 A ..." / "Step 2/3: 수평 B ..." 로 분기.
    - Test 5 (InvokeTryTeachDatum): algorithm == DualImage 이면 두 파일 경로에서 이미지 2개 로드 + 신규 `svc.TryTeachDatum(imageHorizontal, imageVertical, _editingDatum, out error)` 호출. 빈 경로 또는 파일 없음 시 한국어 에러 모달. **goto 패턴 0 — early-return + try/finally 만 사용.**
    - Test 6 (회귀): 기존 4 algorithm (TLI/CTH/VTH/default) 의 GetAlgorithmSteps / ValidateRoiPresence / StartDatumTeachStep / InvokeTryTeachDatum 경로는 0 라인 변경.
  </behavior>

  <action>
WPF_Example/UI/ContentItem/MainView.xaml.cs 의 4개 위치를 수정한다:

**위치 A — GetAlgorithmSteps (line 1911-1922) 의 default 직전에 신규 case 추가:**

```csharp
                case EDatumAlgorithm.VerticalTwoHorizontal:
                    return new[] { EDatumTeachStep.Vertical, EDatumTeachStep.HorizontalA, EDatumTeachStep.HorizontalB };
                //260527 hbk Phase 34 D-34-07 — DualImage 변형: 순서 = HA → HB → V (가로축 이미지 먼저 → 자동 swap → 세로축 이미지). 1-image VTH (V → HA → HB) 와 의도적으로 다름.
                case EDatumAlgorithm.VerticalTwoHorizontalDualImage:
                    return new[] { EDatumTeachStep.HorizontalA, EDatumTeachStep.HorizontalB, EDatumTeachStep.Vertical }; //260527 hbk Phase 34 D-34-07
                default:
                    return new EDatumTeachStep[0];
```

**위치 B — ValidateRoiPresence (line 1015-1036) 의 switch 내부 마지막 case (VTH) 다음에 신규 case 추가:**

```csharp
                case EDatumAlgorithm.VerticalTwoHorizontal:
                    if (d.Vertical_Length1 <= 0)
                        return "Vertical ROI 가 없습니다. 캔버스에 수직 ROI 를 그리고 다시 시도하세요.";
                    if (d.Horizontal_A_Length1 <= 0 || d.Horizontal_B_Length1 <= 0)
                        return "Horizontal A/B ROI 가 없습니다. 캔버스에 ROI 를 그리고 다시 시도하세요.";
                    break;
                //260527 hbk Phase 34 D-34-09/10 — DualImage 변형 가드: 3 ROI + 2 이미지 경로 모두 검증.
                case EDatumAlgorithm.VerticalTwoHorizontalDualImage:
                    if (d.Vertical_Length1 <= 0)
                        return "Vertical ROI 가 없습니다. 캔버스에 수직 ROI 를 그리고 다시 시도하세요."; //260527 hbk Phase 34
                    if (d.Horizontal_A_Length1 <= 0 || d.Horizontal_B_Length1 <= 0)
                        return "Horizontal A/B ROI 가 없습니다. 캔버스에 ROI 를 그리고 다시 시도하세요."; //260527 hbk Phase 34
                    if (string.IsNullOrEmpty(d.TeachingImagePath))
                        return "가로축 티칭 이미지 경로가 비어 있습니다. Datum 노드에서 가로축 이미지를 Load 해주세요."; //260527 hbk Phase 34 D-34-10
                    if (string.IsNullOrEmpty(d.TeachingImagePath_Vertical))
                        return "세로축 티칭 이미지 경로가 비어 있습니다. Datum 노드에서 세로축 이미지를 Load 해주세요."; //260527 hbk Phase 34 D-34-10
                    break;
```

**위치 C — StartDatumTeachStep (line 1965-2011) 의 Vertical / HorizontalA / HorizontalB case 본문 수정:**

Vertical case (line 1980-1986) 변경 후:
```csharp
                case EDatumTeachStep.Vertical:
                    //260527 hbk Phase 34 D-34-06 — DualImage 변형이면 진입 직전에 세로축 이미지로 자동 swap.
                    if (_editingDatum != null
                        && _editingDatum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) {
                        string vpath = _editingDatum.TeachingImagePath_Vertical; //260527 hbk Phase 34 D-34-06
                        if (!string.IsNullOrEmpty(vpath) && System.IO.File.Exists(vpath)) {
                            try { halconViewer.LoadImage(vpath); } //260527 hbk Phase 34 D-34-06
                            catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, ex.Message); } //260527 hbk Phase 34
                        } else {
                            //260527 hbk Phase 34 D-34-08 — 빈 경로: 안내 + 드로잉 차단 (저장은 차단하지 않음).
                            label_drawHint.Content = "세로축 이미지를 Load 해주세요"; //260527 hbk Phase 34 D-34-08
                            label_drawHint.Foreground = new SolidColorBrush(Colors.Orange); //260527 hbk Phase 34
                            label_drawHint.Visibility = Visibility.Visible; //260527 hbk Phase 34
                            break; //260527 hbk Phase 34 — 드로잉 시작 안 함 (switch case 종료)
                        }
                        label_drawHint.Content = "Step 3/3: 수직 ROI를 드래그하세요"; //260527 hbk Phase 34
                    } else {
                        label_drawHint.Content = "Step 1/3: 수직 ROI를 드래그하세요"; //260424 hbk Phase 12 — 기존 1-image VTH 라벨 보존
                    }
                    label_drawHint.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FFAAAAAA"));
                    label_drawHint.Visibility = Visibility.Visible;
                    halconViewer.RectDrawingCompleted += HalconViewer_DatumRectCompleted;
                    halconViewer.StartRectangleDrawing();
                    break;
```

HorizontalA case (line 1987-1993) 변경 후:
```csharp
                case EDatumTeachStep.HorizontalA:
                    //260527 hbk Phase 34 D-34-07 — DualImage 변형: Step 1/3 (가로축 이미지 표시 상태 가정).
                    if (_editingDatum != null
                        && _editingDatum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) {
                        label_drawHint.Content = "Step 1/3: 수평 A ROI를 드래그하세요"; //260527 hbk Phase 34 D-34-07
                    } else {
                        label_drawHint.Content = "Step 2/3: 수평 A ROI를 드래그하세요"; //260424 hbk Phase 12 — 기존 1-image VTH/CTH 라벨 보존
                    }
                    label_drawHint.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FFAAAAAA"));
                    label_drawHint.Visibility = Visibility.Visible;
                    halconViewer.RectDrawingCompleted += HalconViewer_DatumRectCompleted;
                    halconViewer.StartRectangleDrawing();
                    break;
```

HorizontalB case (line 1994-2000) 변경 후:
```csharp
                case EDatumTeachStep.HorizontalB:
                    //260527 hbk Phase 34 D-34-07 — DualImage 변형: Step 2/3.
                    if (_editingDatum != null
                        && _editingDatum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) {
                        label_drawHint.Content = "Step 2/3: 수평 B ROI를 드래그하세요"; //260527 hbk Phase 34 D-34-07
                    } else {
                        label_drawHint.Content = "Step 3/3: 수평 B ROI를 드래그하세요"; //260424 hbk Phase 12 — 기존 1-image VTH/CTH 라벨 보존
                    }
                    label_drawHint.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FFAAAAAA"));
                    label_drawHint.Visibility = Visibility.Visible;
                    halconViewer.RectDrawingCompleted += HalconViewer_DatumRectCompleted;
                    halconViewer.StartRectangleDrawing();
                    break;
```

**위치 D — InvokeTryTeachDatum (line 2106-2146) 의 algorithm 분기 추가 (goto 0 — early-return 패턴):**

기존 (line 2123): `bool ok = svc.TryTeachDatum(img, _editingDatum, out error);`

**핵심 재구성 (goto 패턴 제거 — WARNING #3):** 이미지 로드 실패는 early-return 으로 처리, teach 호출은 try/finally 안에서 ok/error 만 결정, 결과 처리는 try 블록 밖에서.

전체 변경 후:
```csharp
        private void InvokeTryTeachDatum() {
            if (_editingDatum == null) { ExitCanvasMode(); return; }

            HImage img = halconViewer.CurrentImage;
            if (img == null) {
                label_drawHint.Visibility = Visibility.Collapsed;
                CustomMessageBox.Show("티칭 실패", "이미지가 없습니다. 먼저 Grab 또는 Load Image 를 수행하세요.");
                _canvasMode = ECanvasMode.None;
                btn_teachDatum.IsChecked = false;
                _editingDatum = null;
                halconViewer.IsTeachDatumMode = false;
                return;
            }

            var svc = new ReringProject.Halcon.Algorithms.DatumFindingService();
            string error = null;
            bool ok = false;

            //260527 hbk Phase 34 D-34-01/02 — DualImage 변형: 두 파일에서 이미지 2개 로드 + 신규 2-image TryTeachDatum 호출. goto 패턴 0 (early-return + try/finally).
            if (_editingDatum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) {
                string pathH = _editingDatum.TeachingImagePath; //260527 hbk Phase 34
                string pathV = _editingDatum.TeachingImagePath_Vertical; //260527 hbk Phase 34

                //빈 경로 / 파일 없음 가드 — early-return (D-34-10)
                if (string.IsNullOrEmpty(pathH) || !System.IO.File.Exists(pathH)) { //260527 hbk Phase 34
                    ExitTeachWithError("가로축 티칭 이미지 경로가 비어 있거나 파일이 없습니다."); //260527 hbk Phase 34 D-34-10
                    return; //260527 hbk Phase 34
                }
                if (string.IsNullOrEmpty(pathV) || !System.IO.File.Exists(pathV)) { //260527 hbk Phase 34
                    ExitTeachWithError("세로축 티칭 이미지 경로가 비어 있거나 파일이 없습니다."); //260527 hbk Phase 34 D-34-10
                    return; //260527 hbk Phase 34
                }

                //이미지 2개 로드 — try/finally 로 dispose 보장. 로드 실패 시 error 설정 후 try 블록 종료 → 공통 결과 처리.
                HImage imgH = null, imgV = null; //260527 hbk Phase 34
                try {
                    try { imgH = new HImage(pathH); } //260527 hbk Phase 34
                    catch (Exception exH) { error = "가로축 이미지 로드 실패: " + exH.Message; ok = false; } //260527 hbk Phase 34

                    if (error == null) { //260527 hbk Phase 34 — 가로축 로드 성공 시에만 세로축 시도
                        try { imgV = new HImage(pathV); } //260527 hbk Phase 34
                        catch (Exception exV) { error = "세로축 이미지 로드 실패: " + exV.Message; ok = false; } //260527 hbk Phase 34
                    }

                    if (error == null) { //260527 hbk Phase 34 — 두 이미지 모두 성공 시에만 TryTeachDatum 호출
                        ok = svc.TryTeachDatum(imgH, imgV, _editingDatum, out error); //260527 hbk Phase 34 D-34-01/02 — 2-image 오버로드
                    }
                } finally {
                    if (imgH != null) { try { imgH.Dispose(); } catch { } } //260527 hbk Phase 34
                    if (imgV != null) { try { imgV.Dispose(); } catch { } } //260527 hbk Phase 34
                }
            } else {
                ok = svc.TryTeachDatum(img, _editingDatum, out error); //기존 단일-이미지 오버로드 (회귀 0)
            }

            //공통 결과 처리 (DualImage / 1-image 양쪽 공통 — goto 0)
            if (ok) {
                label_drawHint.Content = "Datum 티칭 완료 — Recipe Save 권장";
                label_drawHint.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FF4ADE80"));
                label_drawHint.Visibility = Visibility.Visible;
                halconViewer.SetDatumOverlay(_editingDatum, true);
                PublishDatumRoiCandidates(_editingDatum);
                UpdateDatumRefCoordsLabel(_editingDatum);
            }
            else {
                label_drawHint.Visibility = Visibility.Collapsed;
                CustomMessageBox.Show("티칭 실패", FormatTeachError(_editingDatum, error));
            }

            _canvasMode = ECanvasMode.None;
            btn_teachDatum.IsChecked = false;
            _editingDatum = null;
            halconViewer.IsTeachDatumMode = false;
        }

        //260527 hbk Phase 34 — InvokeTryTeachDatum 의 early-return 헬퍼 (goto 패턴 회피).
        private void ExitTeachWithError(string message) {
            label_drawHint.Visibility = Visibility.Collapsed;
            CustomMessageBox.Show("티칭 실패", message);
            _canvasMode = ECanvasMode.None;
            btn_teachDatum.IsChecked = false;
            _editingDatum = null;
            halconViewer.IsTeachDatumMode = false;
        }
```

**제약 (D-34-06/07/08/09/10/13 + WARNING #3 goto 0):**
- K&R 스타일 (PATTERNS.md Shared Patterns — MainView K&R).
- 4 위치 모두 algorithm 분기 추가만, 기존 4 algorithm 의 라벨/로직 0 라인 수정.
- StartDatumTeachStep 의 다른 case (Line1/Line2/Circle/Done) 변경 0 라인.
- ValidateRoiPresence default fallthrough `return null;` (line 1035) 변경 0.
- **goto 식별자 0 — `Select-String -Pattern '\bgoto\b' MainView.xaml.cs` 가 InvokeTryTeachDatum 영역에서 0 매치.**
- 모든 신규 라인에 `//260527 hbk Phase 34 D-34-XX` 주석.
- IOfflineImageParam (DatumConfig.GetLatestImagePath/SetLatestImagePath) 변경 0.
  </action>

  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "$f = 'WPF_Example/UI/ContentItem/MainView.xaml.cs'; $caseCount = (Select-String -Path $f -Pattern 'case EDatumAlgorithm\.VerticalTwoHorizontalDualImage' -AllMatches | Measure-Object).Count; $dualMsg = (Select-String -Path $f -Pattern '세로축 티칭 이미지 경로' -Quiet); $swap = (Select-String -Path $f -Pattern 'halconViewer\.LoadImage\(vpath\)' -Quiet); $teach2 = (Select-String -Path $f -Pattern 'svc\.TryTeachDatum\(imgH, imgV' -Quiet); $helper = (Select-String -Path $f -Pattern 'private void ExitTeachWithError' -Quiet); $gotoCheck = (Select-String -Path $f -Pattern '\bgoto AfterTeach\b' -Quiet); if ($caseCount -ge 2 -and $dualMsg -and $swap -and $teach2 -and $helper -and -not $gotoCheck) { Write-Host 'PASS caseCount='$caseCount' dualMsg='$dualMsg' swap='$swap' teach2='$teach2' helper='$helper' gotoFree='(-not $gotoCheck) } else { Write-Host 'FAIL caseCount='$caseCount' dualMsg='$dualMsg' swap='$swap' teach2='$teach2' helper='$helper' gotoPresent='$gotoCheck; exit 1 }"</automated>
  </verify>

  <acceptance_criteria>
    - MainView.xaml.cs 에 `case EDatumAlgorithm.VerticalTwoHorizontalDualImage:` 식별자가 최소 2개 (GetAlgorithmSteps + ValidateRoiPresence)
    - GetAlgorithmSteps 안 DualImage case 가 `[EDatumTeachStep.HorizontalA, EDatumTeachStep.HorizontalB, EDatumTeachStep.Vertical]` 순서 반환
    - ValidateRoiPresence 안 DualImage case 가 "세로축 티칭 이미지 경로가 비어 있습니다" 문구 포함
    - StartDatumTeachStep Vertical case 안에 `halconViewer.LoadImage(vpath)` 호출 + `_editingDatum.TeachingImagePath_Vertical` 참조 1개씩 존재
    - StartDatumTeachStep HorizontalA/HorizontalB case 안에 algorithm == DualImage 라벨 분기 각 1개
    - InvokeTryTeachDatum 안에 `svc.TryTeachDatum(imgH, imgV, _editingDatum, out error)` 1개 (2-image 오버로드)
    - **신규 `private void ExitTeachWithError(string message)` 헬퍼 메서드 1개 존재 (goto 회피)**
    - **InvokeTryTeachDatum 영역에 `goto AfterTeach` 식별자 0개 (WARNING #3 — `Select-String -Pattern '\bgoto AfterTeach\b'` 0 매치)**
    - 기존 1-image 4 algorithm 의 GetAlgorithmSteps return 라인 0 변경
    - 기존 ValidateRoiPresence 3 case (TLI/CTH/VTH) 라인 0 변경
    - 신규 라인에 `//260527 hbk Phase 34 D-34` 주석 포함 (grep 카운트 ≥ 10)
  </acceptance_criteria>

  <done>MainView.xaml.cs 의 4 메서드 (GetAlgorithmSteps / ValidateRoiPresence / StartDatumTeachStep / InvokeTryTeachDatum) + 신규 헬퍼 (ExitTeachWithError) 가 모두 DualImage 분기를 갖고, 기존 1-image 경로는 0 라인 변경, goto 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Action_FAIMeasurement.EStep.DatumPhase DualImage 분기 (InspectionSequence.TryRunDatumPhase 2-image 오버로드 호출) + TryGrabOrLoadDualDatumImages 신규 함수 + msbuild Debug/x64 PASS</name>

  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (offset=70, limit=120 — EStep.DatumPhase 진입부)
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (offset=255, limit=40 — GrabOrLoadDatumImage 기존 함수)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (offset=140, limit=45 — Task 1 결과 — TryRunDatumPhase 1-image + 2-image 오버로드 양쪽)
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-PATTERNS.md (section "12) Action_FAIMeasurement.cs — GrabOrLoadDatumImage")
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md (D-34-13, D-34-14 정정)
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-01-SUMMARY.md
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-02-SUMMARY.md
  </read_first>

  <behavior>
    - Test 1 (신규 private 함수 TryGrabOrLoadDualDatumImages): 시그니처 `private bool TryGrabOrLoadDualDatumImages(InspectionSequence parentSeq, out HImage imageHorizontal, out HImage imageVertical)`. 본문은 DatumConfigs[0].TeachingImagePath + TeachingImagePath_Vertical 파일에서 두 HImage 를 로드. 빈 경로 또는 파일 없음 / HImage 생성 실패 시 false + 로그.
    - Test 2 (EStep.DatumPhase 분기): DatumConfigs[0].AlgorithmTypeEnum == VerticalTwoHorizontalDualImage 면 신규 경로 (TryGrabOrLoadDualDatumImages + **parentSeq.TryRunDatumPhase(imgH, imgV, out error) 2-image 오버로드 호출**). else 기존 경로 (GrabOrLoadDatumImage + parentSeq.TryRunDatumPhase(image)).
    - Test 3 (T-34-03-08 해소 — transform map 채움): DualImage 경로의 parentSeq.TryRunDatumPhase(imgH, imgV, ...) 호출이 성공하면 InspectionSequence._datumTransforms 가 채워진다 (Task 1 신규 오버로드 책임). 후속 EStep.Measure 의 TryGetDatumTransform 이 hit → identity fallback 회피.
    - Test 4 (D-34-14 정정 가드): DualImage 경로는 DatumFindingService 직접 호출 안 함. 모든 vision dispatch 는 InspectionSequence 경유.
    - Test 5 (회귀): 기존 1-image algorithm 3종 경로 (EStep.DatumPhase 안 `parentSeq.TryRunDatumPhase(datumImage, out datumError)` 1-image 오버로드) 0 라인 변경.
    - Test 6: msbuild Debug/x64 Rebuild PASS, 신규 warning 0.
  </behavior>

  <action>
WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs 의 두 위치를 수정/추가한다:

**위치 A — EStep.DatumPhase (line 78-104) 의 진입 분기 추가:**

변경 후 (algorithm 보고 분기 — DualImage 시 InspectionSequence 2-image 오버로드 호출, T-34-03-08 해소):
```csharp
                case EStep.DatumPhase: {
                    var parentSeq = ShotParam != null ? ShotParam.Parent as InspectionSequence : null;
                    if (parentSeq != null && parentSeq.DatumConfigs.Count > 0) {
                        //260527 hbk Phase 34 D-34-13 + D-34-14 정정 — DualImage 변형 분기: DatumConfigs[0] algorithm 검사 후 신규 2-image 경로 또는 기존 1-image 경로 선택.
                        //  D-34-14 정정: InspectionSequence 에 2-image TryRunDatumPhase 오버로드 신설 (Plan 03 Task 1) — DualImage 경로도 InspectionSequence 경유로 _datumTransforms 채움 → T-34-03-08 해소 (identity fallback 회피).
                        if (parentSeq.DatumConfigs[0].AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) { //260527 hbk Phase 34 D-34-13
                            HImage imgH = null, imgV = null; //260527 hbk Phase 34
                            try {
                                if (!TryGrabOrLoadDualDatumImages(parentSeq, out imgH, out imgV)) { //260527 hbk Phase 34 D-34-13
                                    Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] DualImage Datum image acquisition failed"); //260527 hbk Phase 34 D-34-09
                                    pMyContext.AllPass = false;
                                    FinishAction(EContextResult.Error);
                                    break;
                                }
                                string datumError; //260527 hbk Phase 34
                                //260527 hbk Phase 34 D-34-14 정정 — InspectionSequence.TryRunDatumPhase 2-image 오버로드 호출 (DatumFindingService 직접 호출 우회 제거).
                                //  InspectionSequence 가 _datumTransforms 채움 → 후속 EStep.Measure 의 TryGetDatumTransform 정상 동작 (T-34-03-08 해소).
                                if (!parentSeq.TryRunDatumPhase(imgH, imgV, out datumError)) { //260527 hbk Phase 34 D-34-14 정정
                                    Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] DualImage Datum failed: " + datumError); //260527 hbk Phase 34
                                    pMyContext.AllPass = false;
                                    FinishAction(EContextResult.Error);
                                    break;
                                }
                            } finally {
                                if (imgH != null) { try { imgH.Dispose(); } catch { } } //260527 hbk Phase 34
                                if (imgV != null) { try { imgV.Dispose(); } catch { } } //260527 hbk Phase 34
                            }
                        } else {
                            //기존 1-image 경로 (TLI/CTH/VTH/default) — 0 라인 변경 (회귀 0, D-34-14)
                            HImage datumImage = GrabOrLoadDatumImage(parentSeq);
                            if (datumImage == null) {
                                Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum image acquisition failed");
                                pMyContext.AllPass = false;
                                FinishAction(EContextResult.Error);
                                break;
                            }
                            try {
                                string datumError;
                                if (!parentSeq.TryRunDatumPhase(datumImage, out datumError)) {
                                    Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum failed: " + datumError);
                                    pMyContext.AllPass = false;
                                    FinishAction(EContextResult.Error);
                                    break;
                                }
                            } finally {
                                datumImage.Dispose();
                            }
                        }
                    }
                    Step = (int)EStep.Grab;
                    break;
                }
```

**위치 B — GrabOrLoadDatumImage (line 257-288) 직후에 신규 private 함수 추가:**

```csharp
        //260527 hbk Phase 34 D-34-13 — DualImage 변형용 두 이미지 동시 로드.
        //  기존 GrabOrLoadDatumImage 시그니처 unchanged. 본 함수만 신규 추가.
        //  imageHorizontal: DatumConfigs[0].TeachingImagePath 에서 로드 (가로축 ROI 검출용)
        //  imageVertical:   DatumConfigs[0].TeachingImagePath_Vertical 에서 로드 (세로축 ROI 검출용)
        //  빈 경로 또는 파일 없음 / HImage 생성 실패 시 false + 로그.
        private bool TryGrabOrLoadDualDatumImages(InspectionSequence parentSeq, out HImage imageHorizontal, out HImage imageVertical) { //260527 hbk Phase 34 D-34-13
            imageHorizontal = null; //260527 hbk Phase 34
            imageVertical = null; //260527 hbk Phase 34
            if (parentSeq == null || parentSeq.DatumConfigs == null || parentSeq.DatumConfigs.Count == 0) { //260527 hbk Phase 34
                Logging.PrintErrLog((int)ELogType.Error, "[Datum] DualImage: DatumConfigs 가 비어 있습니다."); //260527 hbk Phase 34 D-34-09
                return false; //260527 hbk Phase 34
            }
            var datum = parentSeq.DatumConfigs[0]; //260527 hbk Phase 34 — Side fixture 단일 Datum 가정
            string pathH = datum.TeachingImagePath; //260527 hbk Phase 34
            string pathV = datum.TeachingImagePath_Vertical; //260527 hbk Phase 34

            if (string.IsNullOrEmpty(pathH) || !File.Exists(pathH)) { //260527 hbk Phase 34 D-34-09
                Logging.PrintErrLog((int)ELogType.Error, "[Datum] 가로축 티칭 이미지 경로가 비어 있거나 파일이 없습니다 (DualImage)."); //260527 hbk Phase 34 D-34-10
                return false; //260527 hbk Phase 34
            }
            if (string.IsNullOrEmpty(pathV) || !File.Exists(pathV)) { //260527 hbk Phase 34 D-34-09
                Logging.PrintErrLog((int)ELogType.Error, "[Datum] 세로축 티칭 이미지 경로가 비어 있거나 파일이 없습니다 (DualImage)."); //260527 hbk Phase 34 D-34-10
                return false; //260527 hbk Phase 34
            }

            try { imageHorizontal = new HImage(pathH); } catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, "[Datum] 가로축 이미지 로드 실패: " + ex.Message); imageHorizontal = null; } //260527 hbk Phase 34
            try { imageVertical = new HImage(pathV); } catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, "[Datum] 세로축 이미지 로드 실패: " + ex.Message); imageVertical = null; } //260527 hbk Phase 34

            if (imageHorizontal == null || imageVertical == null) { //260527 hbk Phase 34
                if (imageHorizontal != null) { try { imageHorizontal.Dispose(); } catch { } } //260527 hbk Phase 34
                if (imageVertical != null) { try { imageVertical.Dispose(); } catch { } } //260527 hbk Phase 34
                imageHorizontal = null; //260527 hbk Phase 34
                imageVertical = null; //260527 hbk Phase 34
                return false; //260527 hbk Phase 34
            }
            return true; //260527 hbk Phase 34
        }
```

**msbuild 검증 (Task 1 + Task 2 + Task 3 모두 완료 후):**

```powershell
& "C:\Program Files (x86)\Microsoft Visual Studio\2019\BuildTools\MSBuild\Current\Bin\MSBuild.exe" `
  WPF_Example/DatumMeasurement.csproj `
  /p:Configuration=Debug /p:Platform=x64 `
  /t:Rebuild /v:minimal
```

- 종료 코드 0
- 신규 CS error 0
- 신규 warning 0 (Phase 21 baseline 동일)

**제약 (D-34-13/14 정정):**
- K&R 스타일 (Action_FAIMeasurement 는 기존 K&R).
- Action_FAIMeasurement.cs 변경 site = EStep.DatumPhase (위치 A) + 신규 함수 (위치 B) 2 hunks.
- `InspectionSequence.cs` 는 Task 1 의 ≤15 라인 신규 오버로드 1개 외 변경 0 (Plan 03 단계 본 Task 에서는 추가 변경 0).
- `VisionResponsePacket.cs` 변경 0 라인 (D-34-14).
- GrabOrLoadDatumImage (line 257-288) 함수 본문 0 라인 변경 — 신규 함수가 별도 추가만.
- 기존 1-image 경로 (else 블록) 의 본문은 기존 EStep.DatumPhase 본문 보존 (들여쓰기만 +1 단계).
- **DualImage 분기 안에서 DatumFindingService 직접 호출 0 — `Select-String -Pattern 'new.*DatumFindingService' Action_FAIMeasurement.cs` 가 0 매치.**
- 모든 신규 라인에 `//260527 hbk Phase 34 D-34-XX` 주석.
  </action>

  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "$f = 'WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs'; $dualFn = (Select-String -Path $f -Pattern 'private bool TryGrabOrLoadDualDatumImages' -Quiet); $dualCall = (Select-String -Path $f -Pattern 'TryGrabOrLoadDualDatumImages\(parentSeq' -Quiet); $dualRun = (Select-String -Path $f -Pattern 'parentSeq\.TryRunDatumPhase\(imgH, imgV' -Quiet); $algCheck = (Select-String -Path $f -Pattern 'AlgorithmTypeEnum == EDatumAlgorithm\.VerticalTwoHorizontalDualImage' -Quiet); $noDirectSvc = -not (Select-String -Path $f -Pattern 'new ReringProject\.Halcon\.Algorithms\.DatumFindingService' -Quiet); $oneImagePath = (Select-String -Path $f -Pattern 'parentSeq\.TryRunDatumPhase\(datumImage' -Quiet); if ($dualFn -and $dualCall -and $dualRun -and $algCheck -and $noDirectSvc -and $oneImagePath) { Write-Host 'PASS dualFn='$dualFn' dualCall='$dualCall' dualRun='$dualRun' algCheck='$algCheck' noDirectSvc='$noDirectSvc' oneImagePath='$oneImagePath } else { Write-Host 'FAIL dualFn='$dualFn' dualCall='$dualCall' dualRun='$dualRun' algCheck='$algCheck' noDirectSvc='$noDirectSvc' oneImagePath='$oneImagePath; exit 1 }"</automated>
  </verify>

  <acceptance_criteria>
    - Action_FAIMeasurement.cs 에 `private bool TryGrabOrLoadDualDatumImages(InspectionSequence parentSeq, out HImage imageHorizontal, out HImage imageVertical)` 시그니처 정확히 1개
    - Action_FAIMeasurement.cs 에 `TryGrabOrLoadDualDatumImages(parentSeq` 호출이 정확히 1개 (EStep.DatumPhase 안)
    - **Action_FAIMeasurement.cs 에 `parentSeq.TryRunDatumPhase(imgH, imgV` 호출이 정확히 1개 (DualImage 분기 — 2-image 오버로드)**
    - Action_FAIMeasurement.cs 에 `AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage` 분기 정확히 1개
    - **Action_FAIMeasurement.cs 안에 `new ReringProject.Halcon.Algorithms.DatumFindingService` 0개 (D-34-14 정정 — DatumFindingService 직접 호출 우회 제거)**
    - 기존 1-image 경로의 `parentSeq.TryRunDatumPhase(datumImage, out datumError)` 호출이 보존됨 (정확히 1개, else 블록 안)
    - 기존 GrabOrLoadDatumImage 함수 본문 (line 257-288) 변경 0 라인 (git diff 검증)
    - msbuild Debug/x64 Rebuild 종료 코드 0 (PASS)
    - msbuild 출력에 신규 CS error 0
    - msbuild 출력에 신규 warning 0 (Phase 21 baseline 6 동일)
    - git diff --numstat: VisionResponsePacket.cs 변경 0,0 (D-34-14 가드)
    - git diff --numstat: InspectionSequence.cs 변경 추가 ≤ 15, 삭제 0 (Task 1 결과만 — 본 Task 에서 추가 변경 0)
    - git diff --numstat: Action_FAIMeasurement.cs 변경 추가 ≈ 50-65 라인
    - 모든 신규 라인에 `//260527 hbk Phase 34 D-34` 주석 포함 (grep 카운트 ≥ 20)
  </acceptance_criteria>

  <done>Action_FAIMeasurement.cs 가 EStep.DatumPhase 안 algorithm 분기 + TryGrabOrLoadDualDatumImages 신규 함수를 갖고, DualImage 경로가 InspectionSequence.TryRunDatumPhase 2-image 오버로드를 호출하여 _datumTransforms 채움 (T-34-03-08 해소), 기존 1-image 경로 0 라인 변경, VisionResponsePacket.cs 변경 0 라인, msbuild Debug/x64 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 PropertyGrid 입력 → DatumConfig.TeachingImagePath_Vertical | 사용자가 입력한 파일 경로의 존재/유효성. 빈 경로 / 잘못된 파일 / 다른 해상도 가능 |
| 티칭 wizard step 진행 → halconViewer.LoadImage(...) | 파일 시스템 I/O 실패 (권한, 디스크 오류) — try/catch 로 흡수 + 로그 |
| Action_FAIMeasurement 검사 사이클 → 두 파일 로드 → InspectionSequence.TryRunDatumPhase 2-image | InspectionSequence 가 _datumTransforms 채움 책임 보존 (Task 1 신규 오버로드) |
| InspectionSequence.TryRunDatumPhase 2-image 오버로드 호출 | 신규 오버로드가 1-image 오버로드와 동일 채움 규약 보존 (transform map 정합) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03-01 | Tampering | TeachingImagePath_Vertical 가 빈 문자열로 INI 저장된 상태에서 검사 트리거 | mitigate | Action_FAIMeasurement.TryGrabOrLoadDualDatumImages 진입부 `if (string.IsNullOrEmpty(pathV) \|\| !File.Exists(pathV))` 가드 + 한국어 에러 로그 + return false → FinishAction(EContextResult.Error). |
| T-34-03-02 | Tampering | 두 이미지 파일이 서로 다른 해상도 → IntersectionLl 좌표 의미 불일치 | accept | Phase 34 가정 (CONTEXT D-34 Claude's Discretion #4): 동일 카메라 해상도. Plan 04 UAT 시 동일 카메라 시뮬레이션 이미지 사용 검증. |
| T-34-03-03 | DoS | HImage 객체 dispose 누락 → 메모리 누수 (티칭 + 검사 사이클 매 회) | mitigate | InvokeTryTeachDatum (Task 2) + EStep.DatumPhase (Task 3) 양쪽 모두 `try { ... } finally { imgH.Dispose(); imgV.Dispose(); }` 패턴 사용. TryGrabOrLoadDualDatumImages 실패 시 false 반환 전 imgH/V dispose 안전망. |
| T-34-03-04 | Information Disclosure | 검사 실패 로그에 파일 경로 노출 | accept | 기존 Logging 패턴과 동일 — 진단 목적 의도된 노출. |
| T-34-03-05 | Repudiation | wizard step 자동 swap 동작 추적 | mitigate | StartDatumTeachStep Vertical case 진입 시 swap 시도 + 실패 시 Logging.PrintErrLog. 사용자 시각적 피드백 (label_drawHint 안내 메시지). |
| T-34-03-06 | Tampering | DualImage 케이스에서 다중 Datum (DatumConfigs.Count > 1) 시 첫 Datum 만 처리 — 나머지 Datum 무시 | accept | Side fixture 가 단일 Datum 가정 (Phase 33 D-06 동일). Task 3 의 EStep.DatumPhase 분기가 DatumConfigs[0] 만 algorithm 검사. 단, Task 1 의 InspectionSequence.TryRunDatumPhase 2-image 오버로드는 foreach 로 보수적 dispatch (DualImage 가 아닌 datum 도 image1 폴백). 운영 중 다중 Datum + DualImage 혼합 케이스 발견 시 별도 phase. |
| T-34-03-07 | Elevation of Privilege | unsafe 블록 / native callback 도입 | mitigate | Plan 03 변경 범위 = UI 분기 + 함수 추가 + InspectionSequence 오버로드 1개. unsafe / P/Invoke 0. |
| ~~T-34-03-08~~ | ~~DoS~~ | ~~_datumTransforms private 우회 → IDatumOriginConsumer 채움 누락~~ | **resolved (D-34-14 정정)** | **DualImage 경로가 InspectionSequence.TryRunDatumPhase 2-image 오버로드 (Task 1) 를 호출하여 `_datumTransforms` 가 정상 채워짐. 후속 EStep.Measure (Action_FAIMeasurement L149) 의 `parentSeq2.TryGetDatumTransform(meas.DatumRef, out transform)` 가 dict 에서 hit → identity fallback 회피 → Datum 보정 실제 적용. Plan 04 Test 3-e 가 이를 직접 검증 (acceptance: FAI 측정값이 절대좌표 ≠ 보정좌표).** |
</threat_model>

<verification>
## Plan 03 검증

**Smoke:**
- InspectionSequence.cs 컴파일 통과.
- MainView.xaml.cs 컴파일 통과.
- Action_FAIMeasurement.cs 컴파일 통과.
- 기존 1-image 4 algorithm 의 GetAlgorithmSteps / ValidateRoiPresence / StartDatumTeachStep / InvokeTryTeachDatum 동작 0 라인 변경.
- 기존 GrabOrLoadDatumImage 함수 본문 0 라인 변경.
- 기존 InspectionSequence.TryRunDatumPhase(HImage) 1-image 오버로드 본문 0 라인 변경.

**File diff guards (D-34-13/14 정정):**
- Plan 03 변경 파일 = InspectionSequence.cs (≤15 라인) + MainView.xaml.cs + Action_FAIMeasurement.cs 3 파일.
- 다음 파일들은 Plan 03 단계에서 **변경 0 라인** (PowerShell):
  - `git diff --numstat HEAD -- WPF_Example/TcpServer/VisionResponsePacket.cs` → 0,0
  - `git diff --numstat HEAD -- WPF_Example/Halcon/Algorithms/DatumFindingService.cs` → 0,0 (Plan 02 변경 외)
  - `git diff --numstat HEAD -- WPF_Example/Custom/Sequence/Inspection/EDatumAlgorithm.cs` → 0,0 (Plan 01 변경 외)
  - `git diff --numstat HEAD -- WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` → 0,0 (Plan 01 변경 외)

**InspectionSequence.cs 변경 범위 (D-34-14 정정 가드 검증):**
- 변경 site 1: TryGetDatumTransform 직전에 신규 2-image 오버로드 1개 append
- git diff hunk 1개 만 (line 171 이후 append, 기존 라인 0 수정).
- 추가 라인 ≤ 15, 삭제 라인 = 0.

**Action_FAIMeasurement.cs 변경 범위 (D-34-13 한정 변경 site 검증):**
- 변경 site 1: EStep.DatumPhase 안 (line 78-104) — algorithm 분기 추가 (들여쓰기 + else 블록 포함)
- 변경 site 2: GrabOrLoadDatumImage 함수 직후 (line 288 이후) — 신규 TryGrabOrLoadDualDatumImages 함수 추가
- 그 외 site (EStep.Init/Grab/Measure/End, Run 메서드 외부 필드, GrabOrLoadDatumImage 본문) 변경 0 라인.

**msbuild:**
- Debug/x64 Rebuild 종료 코드 0
- Phase 21 baseline warning 수와 동일 (신규 warning 0)

**기능 검증 (런타임 0 — Plan 04 UAT 에서 검증):**
- 빈 경로 가드 동작은 Plan 04 Test 3 시 검증
- 실제 Datum 검출 PASS + transform 적용 검증 (T-34-03-08 해소 증명) 은 Plan 04 Test 3-e 시 검증
- 기존 1-image 회귀는 Plan 04 Test 2 시 검증
- INI 라운드트립 byte-identical 은 Plan 04 Test 4 시 검증
</verification>

<success_criteria>
- [ ] **InspectionSequence.TryRunDatumPhase 2-image 오버로드 추가 (≤15 신규 라인 — D-34-14 정정)**
- [ ] **신규 오버로드가 `_datumTransforms.Clear()` + foreach + algorithm 분기 + `_datumTransforms[name]=transform` 채움 (1-image 오버로드와 동일 규약)**
- [ ] 기존 InspectionSequence.TryRunDatumPhase(HImage) / TryGetDatumTransform / _datumTransforms 필드 변경 0 라인
- [ ] MainView.GetAlgorithmSteps 에 DualImage case 추가 (순서 = HA→HB→V)
- [ ] MainView.ValidateRoiPresence 에 DualImage case 추가 (Vertical/H_A/H_B Length + 두 이미지 경로 검증)
- [ ] MainView.StartDatumTeachStep Vertical case 안 algorithm 분기 + 자동 image swap + 빈 경로 가드
- [ ] MainView.StartDatumTeachStep HorizontalA/HorizontalB case 안 algorithm 분기 (라벨만)
- [ ] MainView.InvokeTryTeachDatum 안 algorithm 분기 (DualImage 시 두 파일 로드 + 신규 2-image TryTeachDatum 호출) — **goto 0 (ExitTeachWithError 헬퍼 사용)**
- [ ] Action_FAIMeasurement.EStep.DatumPhase 안 algorithm 분기 (DualImage 시 **InspectionSequence.TryRunDatumPhase 2-image 호출** — DatumFindingService 직접 호출 0)
- [ ] Action_FAIMeasurement 에 신규 private 함수 TryGrabOrLoadDualDatumImages 추가
- [ ] 기존 1-image 4 algorithm 의 모든 UI/검사 경로 0 라인 변경 (회귀 0)
- [ ] D-34-14 정정 가드 — InspectionSequence.cs 변경 ≤15 라인 (2-image 오버로드 1개) / VisionResponsePacket.cs 변경 0 라인
- [ ] D-34-13 가드 — Action_FAIMeasurement.cs 변경 site 2개 한정
- [ ] T-34-03-08 해소 — DualImage 경로 후 `_datumTransforms[DatumName]` 채워짐 (Plan 04 Test 3-e 가 acceptance 로 확인)
- [ ] 모든 신규 라인에 `//260527 hbk Phase 34 D-34-XX` 주석
- [ ] msbuild Debug/x64 Rebuild PASS, 신규 warning 0
</success_criteria>

<output>
After completion, create `.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-03-SUMMARY.md` per template — include msbuild PASS 증거 + git diff --numstat 결과 (변경 파일 3개: InspectionSequence ≤15 / MainView / Action_FAIMeasurement, 나머지 4 파일 0,0) + InspectionSequence 신규 오버로드 시그니처 + 5 메서드 분기 site + 2 신규 함수 시그니처 (TryGrabOrLoadDualDatumImages + ExitTeachWithError) + T-34-03-08 해소 명시 (DualImage 경로가 _datumTransforms 채움 보장).
</output>
</content>
</invoke>