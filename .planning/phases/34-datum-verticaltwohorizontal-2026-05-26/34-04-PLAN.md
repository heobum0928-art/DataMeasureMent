---
phase: 34-datum-verticaltwohorizontal-2026-05-26
plan: 04
type: execute
wave: 4
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-UAT.md
  - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-04-SUMMARY.md
autonomous: false
requirements:
  - SC-1   # 신규 EDatumAlgorithm 콤보 노출 — UAT Test 3 시각 확인
  - SC-2   # TeachingImagePath_Vertical PropertyGrid 노출 — UAT Test 3
  - SC-3   # DatumFindingService 분기 — UAT Test 3 실측 PASS
  - SC-4   # 티칭 UI step 머신 + 자동 이미지 전환 — UAT Test 3
  - SC-5   # Action_FAIMeasurement 분기 — UAT Test 3
  - SC-6   # INI 호환 + TeachingImagePath_Vertical 라운드트립 — UAT Test 4
  - SC-7   # 기존 1-image 3종 회귀 0 — UAT Test 2

must_haves:
  truths:
    - "Test 1 (msbuild): Debug/x64 Rebuild 종료 코드 0, 신규 warning 0"
    - "Test 2 (회귀 0): 기존 1-image 3 algorithm (TLI/CTH/VTH) 모두 Top recipe 로드 후 임의 검사 1회 정상 동작"
    - "Test 3 (신규 algorithm SIMUL UAT): Side fixture 이미지 2장으로 algorithm = VerticalTwoHorizontalDualImage 설정 → 티칭 wizard (HA → HB → 자동 swap → V) → 검사 → Datum 검출 PASS + **Datum 보정 transform 이 InspectionSequence._datumTransforms 에 실제 등록되어 FAI Measurement 에 적용됨 증명 (T-34-03-08 해소)** — DualImage 측정값 ≠ identity 측정값 비교 또는 _datumTransforms hit 디버그 증거"
    - "Test 4 (INI 라운드트립): TeachingImagePath_Vertical 키가 INI 저장/로드 시 byte-identical 보존 (빈 문자열 미존재 케이스 포함)"
    - "Test 5 (D-34-13/14 정정 가드 검증): **InspectionSequence.cs 변경 ≤15 신규 라인 (2-image TryRunDatumPhase 오버로드 1개만 — D-34-14 정정 허용)** + VisionResponsePacket.cs 변경 0 라인 + Action_FAIMeasurement.cs 변경 site 2개 한정 — git diff --stat / --numstat 결과로 증명"
    - "사용자 (사람) 가 모든 테스트에 'approved' 명시"
  artifacts:
    - path: ".planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-UAT.md"
      provides: "5 테스트 사인오프 보고서 (Test 1~5 모두 PASS / 또는 FAIL + carry-over 명시)"
      contains: "result: PASS"
      contains_2: "사용자 confirmed"
    - path: ".planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-04-SUMMARY.md"
      provides: "Plan 04 + Phase 34 종합 요약"
  key_links:
    - from: "34-UAT.md Test 3 (신규 algorithm SIMUL)"
      to: "Side fixture 이미지 2장 + DatumFindingService.TryFindVerticalTwoHorizontalDualImage"
      via: "End-to-end UAT — 티칭 wizard + 검사 사이클 양쪽 검증"
      pattern: "VerticalTwoHorizontalDualImage"
    - from: "34-UAT.md Test 5 (D-34-13/14 정정 가드)"
      to: "git diff --numstat 결과 byte-level 인용"
      via: "InspectionSequence.cs (+≤15,-0) / VisionResponsePacket.cs (0,0) / EDatumAlgorithm.cs (+1,0) / DatumConfig.cs / Action_FAIMeasurement.cs 변경 라인 수 검증"
      pattern: "git diff --numstat"
    - from: "34-UAT.md Test 3-e step 2 (Datum 보정 실제 적용)"
      to: "InspectionSequence._datumTransforms 에 transform 등록됨 — TryGetDatumTransform hit (identity fallback 회피)"
      via: "DualImage 측정값 ≠ identity 측정값 비교 또는 디버그 로그"
      pattern: "_datumTransforms hit|측정값 비교"
---

<objective>
Phase 34 sign-off — 5 테스트 (msbuild / 기존 1-image 회귀 0 / 신규 DualImage SIMUL / INI 라운드트립 / D-34-13·14 가드) 를 모두 PASS 시키고 34-UAT.md 를 작성한다.

Purpose: Phase 34 완료의 사용자 검증. Phase 35 Test 4 Side carry-over 흡수 시도 + Phase 33 partial sign-off 의 Side 부분 종결.
Output: 34-UAT.md (signed_off 또는 partial), 34-04-SUMMARY.md, Phase 34 의 sign-off 결정 결과.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/uat.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-01-SUMMARY.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-02-SUMMARY.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-03-SUMMARY.md
@.planning/phases/35-side-bottom-uat-and-phase33-completion/35-UAT.md

<!-- 35-UAT.md 참조 이유: Phase 35 Test 4 (Side 실측 SIMUL UAT) carry-over 가 Phase 34 의 trigger. Test 3 시나리오 = 35-UAT.md Test 4 의 Side fixture 실데이터 흡수. -->
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 (UAT Test 1+2+5): msbuild + 기존 1-image 회귀 0 + D-34-13/14 가드 검증</name>

  <files>(no production code change — autonomous=false checkpoint task. 사용자가 application 을 SIMUL_MODE 로 실행하여 확인.)</files>

  <read_first>
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-01-SUMMARY.md
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-02-SUMMARY.md
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-03-SUMMARY.md
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md (D-34-13/14/15)
  </read_first>

  <behavior>
    - Test 1 (자동): msbuild Debug/x64 Rebuild 종료 코드 0, 신규 warning 0.
    - Test 2 (수동): 기존 1-image 3 algorithm (TLI/CTH/VTH) 모두 Top recipe 로드 후 임의 검사 1회 정상 동작 + 측정값 표시.
    - Test 5 (자동): D-34-13/14 가드 — git diff --numstat HEAD~3 HEAD 결과 InspectionSequence.cs + VisionResponsePacket.cs 0,0 + Action_FAIMeasurement.cs 변경 site ≤ 3 hunks.
  </behavior>

  <action>
**Plan 04 Task 1 = Test 1+2+5 사용자 검증 체크포인트.**

본 Task 의 액션은 Claude (executor) 가 자동 검증을 실행한 후, 사용자에게 시나리오를 안내하고 사용자의 "approved" 명시를 대기하는 것이다.

**자동 검증 (Claude 실행):**

1. msbuild Debug/x64 Rebuild 실행 (Test 1):
```powershell
& "C:\Program Files (x86)\Microsoft Visual Studio\2019\BuildTools\MSBuild\Current\Bin\MSBuild.exe" `
  WPF_Example/DatumMeasurement.csproj `
  /p:Configuration=Debug /p:Platform=x64 `
  /t:Rebuild /v:minimal
```
- 종료 코드 = 0 확인.
- 출력에 `error CS` 라인 0개 확인.
- 출력에 신규 warning 0개 확인 (Phase 21 baseline warning 6개와 동일).

2. D-34-13/14 가드 검증 (Test 5):
```powershell
git diff --numstat HEAD~3 HEAD -- WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
git diff --numstat HEAD~3 HEAD -- WPF_Example/TcpServer/VisionResponsePacket.cs
git diff --numstat HEAD~3 HEAD -- WPF_Example/Custom/Sequence/Inspection/EDatumAlgorithm.cs
git diff --numstat HEAD~3 HEAD -- WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
git diff --numstat HEAD~3 HEAD -- WPF_Example/Halcon/Algorithms/DatumFindingService.cs
git diff --numstat HEAD~3 HEAD -- WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
git diff --numstat HEAD~3 HEAD -- WPF_Example/UI/ContentItem/MainView.xaml.cs
```
- (HEAD~3 = Plan 03 commit; 실제 커밋 수 따라 ~N 조정)
- InspectionSequence.cs → +≤15, -0 (D-34-14 정정 — 2-image TryRunDatumPhase 오버로드 1개만 허용)
- VisionResponsePacket.cs → 0,0 (D-34-14)
- EDatumAlgorithm.cs → +1 (Plan 01)
- DatumConfig.cs → +8 ~ +12 (Plan 01)
- DatumFindingService.cs → +350 ~ +400 (Plan 02)
- Action_FAIMeasurement.cs → +50 ~ +65, hunks ≤ 3 (Plan 03, D-34-13)
- MainView.xaml.cs → +70 ~ +100 (Plan 03)

3. Action_FAIMeasurement hunk 개수 검증 (D-34-13 한정 변경 site):
```powershell
git diff HEAD~3 HEAD -- WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs | Select-String '^@@' | Measure-Object
```
- hunk 개수 ≤ 3 (EStep.DatumPhase 분기 + 신규 함수 + 가능 시 들여쓰기).

**사용자 안내 (Claude → 사용자):**

자동 검증 결과를 사용자에게 보고한 후 다음 시나리오를 안내:

> 사용자 안내: SIMUL_MODE 빌드된 DatumMeasurement.exe 를 실행하고 다음 3 시나리오 (Test 2) 를 확인해주세요:
>
> 1. Top recipe (TwoLineIntersect) 로드 → Top Datum 선택 → algorithm = TwoLineIntersect → Test Find Datum → 검출 PASS + 측정값 표시
> 2. 동일 또는 CTH 모드 Datum → algorithm = CircleTwoHorizontal → Test Find → 검출 PASS
> 3. 동일 또는 VTH 모드 Datum (1-image) → algorithm = VerticalTwoHorizontal → Test Find → 검출 PASS
>
> 3 시나리오 모두 정상 동작 시 "Test 2 approved" 라고 알려주세요.
> Test 1 (자동 msbuild) 와 Test 5 (자동 git diff) 결과도 알려주신 결과 (PASS) 와 함께 "Test 1 approved" / "Test 5 approved" 를 명시해주세요.

**사용자 결과 대기:**
- "Test 1 approved" + "Test 2 approved" + "Test 5 approved" 3 항목 모두 명시 시 → Task 1 완료.
- 일부 FAIL 시 → 보고 받은 후 hotfix 또는 carry-over 결정 (Plan 04 Task 3 의 34-UAT.md 작성 시 반영).
  </action>

  <verify>
    <automated>powershell -NoProfile -Command "Write-Host '[Plan 04 Task 1] human verify gate — Claude 가 msbuild + git diff 자동 검증 후 사용자에게 1-image 회귀 0 시나리오 (Test 2) 확인 요청. 사용자 approved 응답 필요.'"</automated>
  </verify>

  <acceptance_criteria>
    - msbuild Debug/x64 Rebuild 종료 코드 0 (자동 PASS)
    - 신규 CS error 0, 신규 warning 0 (자동 PASS — grep 검증)
    - git diff --numstat 결과: InspectionSequence.cs / VisionResponsePacket.cs 가 0,0 (자동 PASS — D-34-14 가드)
    - Action_FAIMeasurement.cs 의 git diff hunks ≤ 3 (자동 PASS — D-34-13 한정 변경 site)
    - 사용자가 Top recipe 로드 후 TLI/CTH/VTH 3 시나리오 모두 PASS 보고 ("Test 2 approved")
    - 사용자가 Test 1 (msbuild) / Test 5 (가드) 의 자동 결과를 합의 ("Test 1 approved" / "Test 5 approved")
  </acceptance_criteria>

  <done>Test 1 + Test 2 + Test 5 모두 사용자 "approved" 시 Task 1 종결. Task 2 (Test 3 + 4) 로 진행.</done>

  <resume-signal>
    "Test 1 approved" + "Test 2 approved" + "Test 5 approved" 3 항목 사용자 명시 시 다음 Task 진행. 일부 FAIL 보고 시 → hotfix 영역 또는 carry-over 결정.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (UAT Test 3+4): 신규 DualImage SIMUL UAT + INI 라운드트립</name>

  <files>(no production code change — autonomous=false checkpoint task. 사용자가 SIMUL_MODE Side fixture 시뮬레이션 이미지 2장으로 검증.)</files>

  <read_first>
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md (D-34-15 UAT 범위)
    - .planning/phases/35-side-bottom-uat-and-phase33-completion/35-UAT.md (Test 4 Side carry-over 데이터)
  </read_first>

  <behavior>
    - Test 3-a: PropertyGrid AlgorithmType 콤보에 `VerticalTwoHorizontalDualImage` 항목 노출 (SC-1).
    - Test 3-b: algorithm = DualImage 선택 시 TeachingImagePath_Vertical 필드 PropertyGrid 노출 (SC-2).
    - Test 3-c: 티칭 wizard 순서 HA → HB → V 동작 (D-34-07, SC-4).
    - Test 3-d: Horizontal_B 완료 직후 halconViewer 가 세로축 이미지로 자동 swap (D-34-06).
    - Test 3-e: Datum 검출 PASS — `[FAIMeasurement] DualImage Datum failed` 메시지 0건 (SC-3). **Datum 보정 실제 적용 증명 (SC-5 + T-34-03-08 해소)**: (a) FAI Measurement 1개 이상의 측정값이 Datum 미적용 (identity transform) 케이스와 다른 값을 보임 (DualImage transform 이 _datumTransforms 에 등록되어 measurement 에 적용됨을 증명), (b) 로그에 `Datum.*CurrentTransform` 또는 디버그 출력으로 transform map hit 확인.
    - Test 3-f: 빈 경로 가드 — TeachingImagePath_Vertical 빈 상태로 검사 트리거 시 한국어 에러 메시지 + FinishAction(Error) (D-34-09/10, bonus).
    - Test 4-A: TeachingImagePath_Vertical 값이 INI Save → 재시작 → Load 후 보존 (D-34-11).
    - Test 4-B: 기존 INI (TeachingImagePath_Vertical 키 미존재) 로드 → "" 정규화 → 1-image algorithm 검사 PASS (D-34-12, 회귀 0).
  </behavior>

  <action>
**Plan 04 Task 2 = Test 3+4 사용자 검증 체크포인트.**

본 Task 의 액션은 Claude (executor) 가 사용자에게 시나리오를 안내하고 사용자의 "approved" 명시를 대기하는 것이다. Claude 의 자동 검증은 application 실행을 요구하므로 사용자 운영 영역.

**Test 3 사용자 안내 (Claude → 사용자):**

> 사전 준비:
> - Side fixture 시뮬레이션 이미지 2장 (가로축 + 세로축). 사용자가 보유한 Side 이미지 또는 Phase 35 Test 4 carry-over 데이터 사용.
> - Side InspectionSequence 동작 가정 (Phase 33/35 결과).
>
> 시나리오:
>
> 1. DatumMeasurement.exe 실행 → SIMUL_MODE 확인.
> 2. Side recipe (또는 신규 빈 recipe) 로드. Side Sequence 노드 선택.
> 3. Datum 추가 → DatumConfig 선택 → PropertyGrid 에서:
>    - **(3-a)** AlgorithmType 콤보 → `VerticalTwoHorizontalDualImage` 항목 **존재 확인** + 선택.
>    - **(3-b)** TeachingImagePath (가로축) 필드 노출 확인 + 가로축 이미지 경로 입력 (Load Image 버튼 권장).
>    - **(3-b 계속)** TeachingImagePath_Vertical 필드 **PropertyGrid 노출 확인** + 세로축 이미지 경로 직접 입력.
> 4. Load Image 버튼 → 가로축 이미지가 halconViewer 에 표시됨.
> 5. (Bonus) ROI 없는 상태로 Test Find Datum → "Vertical ROI 가 없습니다…" 한국어 안내 (SC-4 ValidateRoiPresence).
> 6. btn_teachDatum 클릭 → 티칭 wizard 진입:
>    - **(3-c step 1)** "Step 1/3: 수평 A ROI를 드래그하세요" 라벨 (D-34-07 순서) → Horizontal_A ROI 드로잉.
>    - **(3-c step 2)** "Step 2/3: 수평 B ROI를 드래그하세요" → Horizontal_B ROI 드로잉.
>    - **(3-d)** Horizontal_B 완료 직후 halconViewer 가 **세로축 이미지로 자동 swap**.
>    - **(3-c step 3)** "Step 3/3: 수직 ROI를 드래그하세요" → Vertical ROI 드로잉.
>    - 완료 → 자동 InvokeTryTeachDatum 호출 → "Datum 티칭 완료" 라벨 (녹색).
> 7. 검사 트리거 (또는 Test Find Datum 재실행):
>    - **(3-e step 1)** Datum 검출 PASS. 로그에 `[FAIMeasurement] DualImage Datum failed` 메시지 0건.
>    - **(3-e step 2 — Datum 보정 실제 적용 증명, T-34-03-08 해소)**: FAI Measurement 1개 이상이 등록된 Side recipe 에서, 검사 후 측정값 (mm 또는 px) 을 기록 → 동일 ROI 의 `AlgorithmType` 을 임시로 1-image 알고리즘 (예: TwoLineIntersect) 으로 변경 + 검사 → identity transform (Datum 보정 0) 측정값과 비교. **두 값이 달라야 함** (DualImage 가 실제로 transform 을 적용했다는 증거). 만약 두 값이 동일 = identity fallback 발동 = T-34-03-08 회귀.
>    - **(3-e 대체 방법 — 디버거)**: Visual Studio 디버거 또는 임시 로그 (예: `Logging.PrintLog((int)ELogType.Trace, $"[Datum] _datumTransforms count after DualImage = {parentSeq._datumTransforms.Count}");`) 로  가 _datumTransforms 에서 hit 함을 확인 (true 반환 + identity 가 아닌 transform).
> 8. (Bonus 3-f) TeachingImagePath_Vertical 을 빈 문자열로 임시 변경 → 검사 트리거 → 한국어 에러 메시지 + 검사 실패.
>
> 8 sub-test (3-a/b/c/d/e + bonus + 5/6/7/8) 모두 PASS 시 "Test 3 approved" 명시.

**Test 4 사용자 안내 (Claude → 사용자):**

> 시나리오 A — TeachingImagePath_Vertical 값 보존:
> 1. Test 3 의 Side recipe 를 Recipe Save → INI 저장.
> 2. INI 파일 열어 `TeachingImagePath_Vertical=<경로>` 키 확인 (PowerShell: `Select-String -Path '<recipe>.ini' -Pattern 'TeachingImagePath_Vertical'`).
> 3. 어플리케이션 재시작 → 동일 recipe 로드.
> 4. Side Datum 선택 → PropertyGrid 의 TeachingImagePath_Vertical 값이 Save 시점과 동일 확인.
> 5. Recipe Save 다시 → INI 파일 diff (`fc` 비교) → byte-identical (timestamp 만 차이) 확인.
>
> 시나리오 B — 기존 INI 회귀 0:
> 1. Phase 22 IMG-01 또는 그 이전의 Top recipe (TeachingImagePath_Vertical 키 없음) 로드.
> 2. Top Datum 선택 → PropertyGrid 에 TeachingImagePath_Vertical 노출 0 (algorithm != DualImage → ICustomTypeDescriptor hide).
> 3. Recipe Save → INI 파일에 TeachingImagePath_Vertical=빈 값 키 추가 (ParamBase 자동 직렬화). 1-image algorithm 검사 PASS.
>
> 두 시나리오 모두 PASS 시 "Test 4 approved" 명시.

**사용자 결과 대기:**
- "Test 3 approved" + "Test 4 approved" 양쪽 모두 명시 시 → Task 2 완료, Task 3 (UAT 문서 작성) 진행.
- Test 3 일부 FAIL → carry-over 결정 (예: IDatumOriginConsumer 채움 한계 T-34-03-08 → Phase 35 후속 또는 별도 quick task).
- Test 4 FAIL → INI 직렬화 hotfix 영역.
  </action>

  <verify>
    <automated>powershell -NoProfile -Command "Write-Host '[Plan 04 Task 2] human verify gate — 사용자가 SIMUL_MODE 에서 신규 DualImage UAT (Test 3 — 6+1 sub-test) + INI 라운드트립 (Test 4 — 2 시나리오) 직접 실행 확인. Claude approved 응답 대기.'"</automated>
  </verify>

  <acceptance_criteria>
    - 사용자가 Test 3 의 6 sub-test (3-a/b/c/d/e step 1+2/f) 모두 PASS 보고 ("Test 3 approved")
    - **Test 3-e step 2 (Datum 보정 실제 적용 증명) PASS 확인 — DualImage 측정값 ≠ 1-image identity 측정값 (T-34-03-08 해소 검증) 또는 _datumTransforms hit 디버그 증거**
    - 사용자가 Test 4 의 2 시나리오 (A 보존 + B 회귀 0) PASS 보고 ("Test 4 approved")
    - Test 3-e step 2 FAIL = T-34-03-08 회귀 → Plan 03 Task 1/3 hotfix 영역 (InspectionSequence 2-image 오버로드 또는 Action_FAIMeasurement 호출 site 검토)
    - FAIL 시 carry-over ID + 후속 phase/quick 식별 명시
  </acceptance_criteria>

  <done>Test 3 + Test 4 모두 사용자 "approved" 시 Task 2 종결. Task 3 (UAT 문서 + SUMMARY 작성) 진행.</done>

  <resume-signal>
    "Test 3 approved" + "Test 4 approved" 시 → Task 3 (34-UAT.md + 34-04-SUMMARY.md 작성).
    부분 PASS 시 → carry-over 명시 후 Task 3 (status: partial 로 UAT 작성).
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 34-UAT.md 작성 + 34-04-SUMMARY.md 작성 + git commit</name>

  <files>
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-UAT.md (신규)
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-04-SUMMARY.md (신규)
  </files>

  <read_first>
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-01-SUMMARY.md
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-02-SUMMARY.md
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-03-SUMMARY.md
    - $HOME/.claude/get-shit-done/templates/uat.md
    - $HOME/.claude/get-shit-done/templates/summary.md
    - .planning/phases/22-image-dual-structure/22-UAT.md (참조 패턴)
    - .planning/phases/35-side-bottom-uat-and-phase33-completion/35-UAT.md (Test 4 Side carry-over 흡수 처리 패턴)
  </read_first>

  <behavior>
    - Test 1: 34-UAT.md 파일 생성 — frontmatter (`status: signed_off | partial | failed`) + 5 Test 결과 표 + 각 Test 의 사용자 합의 인용 + git commit hash 인용.
    - Test 2: Phase 34 sign-off 결정 — 모두 PASS 면 status=signed_off. 일부 FAIL 시 status=partial + carry-over 명시.
    - Test 3: Phase 35 Test 4 (Side carry-over) 흡수 표기.
    - Test 4: 34-04-SUMMARY.md 작성.
    - Test 5: git commit 작성.
  </behavior>

  <action>
사용자 합의 (Task 1 + Task 2) 후 다음 작업을 수행한다:

**파일 1: `.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-UAT.md` 작성**

uat.md 템플릿 사용. 다음 구조:

```markdown
----  # embedded YAML 예시 (verifier 회피용 4-dash)
phase: 34-datum-verticaltwohorizontal-2026-05-26
plan: 04
status: signed_off  # 또는 partial / failed
gates: ALL_PASS  # 또는 PARTIAL
updated: 2026-05-XX
total: 5
passed: 5  # 또는 부분 (예: 4)
pending: 0  # 또는 carry-over 개수
----  # embedded YAML 예시 (verifier 회피용 4-dash)

# Phase 34 UAT — Datum VerticalTwoHorizontal Dual Image

**Sign-off:** 2026-05-XX

## Summary Table

| # | Test | Result | User Confirmed | Commit |
|---|------|--------|----------------|--------|
| 1 | msbuild Debug/x64 PASS + 신규 warning 0 | PASS | <date> | <hash> |
| 2 | 기존 1-image 3 algorithm (TLI/CTH/VTH) 회귀 0 | PASS | <date> | <hash> |
| 3 | 신규 DualImage SIMUL UAT (Side fixture) | PASS | <date> | <hash> |
| 4 | INI 라운드트립 (TeachingImagePath_Vertical 보존 + 미존재 키 회귀 0) | PASS | <date> | <hash> |
| 5 | D-34-13/14 가드 (InspectionSequence/VisionResponsePacket 변경 0) | PASS | <date> | <hash> |

## Test Details
... (각 Test 의 사용자 합의 인용 + sub-test 결과 + 시나리오 시각화)

## Phase 35 Test 4 Side carry-over 흡수
Phase 35 35-UAT.md Test 4 (Side 실측 SIMUL UAT) 가 본 Test 3 으로 흡수되어 PASS — Phase 33 partial sign-off 의 Side 부분 carry-over 해소.

## Carry-over (있다면)
- 예: IDatumOriginConsumer 채움 한계 (T-34-03-08) — Plan 04 UAT 시 발견 시 후속 phase/quick 식별.

## Memory 정정 사항
- `project_phase35_progress.md` 메모의 "Side fixture = 수직 ROI image1, 수평 ROI 2개 image2" → "Side fixture = 가로축 ROI 2개 image1 (TeachingImagePath), 수직 ROI image2 (TeachingImagePath_Vertical)" 정정 (D-34-01 정합).
```

**파일 2: `.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-04-SUMMARY.md` 작성**

summary.md 템플릿 사용. 다음 구조:

```markdown
----  # embedded YAML 예시 (verifier 회피용 4-dash)
phase: 34
plan: 04
type: execute
status: complete
----  # embedded YAML 예시 (verifier 회피용 4-dash)

# Plan 04 SUMMARY — Phase 34 SIMUL UAT + Sign-off

## What was built
- 34-UAT.md 작성 (status: signed_off / partial)
- Phase 34 5 Test 사용자 검증 결과 기록

## How it was verified
- Test 1: msbuild Debug/x64 Rebuild PASS (자동)
- Test 2: 기존 1-image 3종 회귀 0 (사용자 SIMUL 확인)
- Test 3: 신규 DualImage SIMUL UAT — Side fixture 이미지 2장 (사용자 확인)
- Test 4: INI 라운드트립 (사용자 확인)
- Test 5: D-34-13/14 가드 (자동 git diff)

## Phase 34 종합 상태
- Sign-off: signed_off (또는 partial)
- carry-over: (있다면 ID + 다음 phase/quick 추천)
- Phase 35 Test 4 Side carry-over: 흡수 (또는 잔존)

## Memory 정정 trigger
- project_phase35_progress.md — Side fixture ROI ↔ image 매핑 정정 (D-34-01 정합).
```

**git commit:**
```powershell
git add .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-UAT.md .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-04-SUMMARY.md
git commit -m "docs(34-04): SUMMARY + UAT — Phase 34 sign-off (or partial)..."
```

**메모리 정정 (Plan 외 작업, 본 Task 에서는 명시만):**
- CONTEXT.md <specifics> 명시: `project_phase35_progress.md` 메모리 정정은 Plan/phase 외 작업 — phase-plan orchestrator 의 별도 처리. UAT 본문에 trigger 노트만 남김.
  </action>

  <verify>
    <automated>powershell -NoProfile -Command "$uat = '.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-UAT.md'; $sum = '.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-04-SUMMARY.md'; if ((Test-Path $uat) -and (Test-Path $sum)) { $hasStatus = (Select-String -Path $uat -Pattern 'status:' -Quiet); $hasSignOff = (Select-String -Path $uat -Pattern 'signed_off|partial|failed' -Quiet); $has5tests = (Select-String -Path $uat -Pattern 'Test 5' -Quiet); if ($hasStatus -and $hasSignOff -and $has5tests) { Write-Host 'PASS' } else { Write-Host 'FAIL status='$hasStatus' signOff='$hasSignOff' has5='$has5tests; exit 1 } } else { Write-Host 'FAIL files not found uat='(Test-Path $uat)' sum='(Test-Path $sum); exit 1 }"</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-UAT.md` 파일 존재
    - 34-UAT.md frontmatter 에 `status: signed_off` 또는 `partial` 또는 `failed` 1개 존재
    - 34-UAT.md 본문에 5 Test (Test 1 ~ Test 5) 결과 모두 명시
    - 34-UAT.md Test 3 본문에 "Phase 35 Test 4 carry-over 흡수" 명시
    - 34-UAT.md 에 `result: PASS` 또는 `result: FAIL` 또는 `result: PARTIAL` 항목 ≥ 5개
    - `.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-04-SUMMARY.md` 파일 존재
    - 34-04-SUMMARY.md 가 종합 결과 + carry-over (있다면) 명시
    - git commit 작성 (메시지 prefix `docs(34-04):`)
  </acceptance_criteria>

  <done>34-UAT.md + 34-04-SUMMARY.md 가 작성되어 git commit 되었고, Phase 34 의 sign-off 상태 (signed_off / partial) 가 명시됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 (사람) 검증 → 34-UAT.md 사인오프 | 사용자 시각/조작 결과를 신뢰. Claude 는 자동 검증만 가능 (msbuild + grep) |
| Side fixture 시뮬레이션 데이터 | 사용자가 보유한 실제 검사 데이터. Phase 34 가정 (동일 카메라 해상도) 위반 시 검출 결과 신뢰성 저하 |
| Phase 35 Test 4 carry-over 흡수 | Phase 35 partial sign-off 의 Test 4 를 Phase 34 Test 3 으로 종결 — 사용자 합의 시 carry-over 해소 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04-01 | Repudiation | UAT 결과 인용/추적 누락 | mitigate | 34-UAT.md frontmatter + 5 Test 표 + 각 Test 의 사용자 합의 인용 ("approved" 시점 timestamp + 시나리오 ID) + git commit hash 인용. |
| T-34-04-02 | Tampering | UAT 도중 발견된 hotfix 의 commit 누락 | mitigate | Plan 04 진행 중 hotfix 시 별도 commit (`fix(34-XX): ...`) + UAT 본문에 commit hash 인용. |
| T-34-04-03 | Information Disclosure | UAT 본문에 실데이터 경로 / 사용자 파일명 노출 | accept | 운영 진단 목적 — 의도된 노출. PII 없음. |
| T-34-04-04 | DoS | UAT 실행 중 application crash / freeze | accept | UAT 사용자 운영 영역. crash 발생 시 별도 quick task 로 hotfix 후 UAT 재시작. |
| T-34-04-05 | Elevation of Privilege | UAT 결과로 코드 변경 (hotfix) → 가드 위반 | mitigate | hotfix 도 D-34-13/14 가드 준수 (InspectionSequence + VisionResponsePacket 변경 0). UAT 본문에 git diff --stat 재인용. |
</threat_model>

<verification>
## Plan 04 검증 (Phase 34 종합 검증)

**Plan 04 의 verification = Phase 34 의 verification:**

| Phase 34 Success Criteria | Verification Plan 04 Test |
|----|----|
| SC-1: 신규 EDatumAlgorithm 콤보 노출 | Test 3-a (사용자 시각 확인) |
| SC-2: TeachingImagePath_Vertical ICustomTypeDescriptor 노출 | Test 3-b (사용자 시각 확인) |
| SC-3: DatumFindingService 신규 분기 | Test 3-e (검출 PASS 가 분기 정확성 증명) |
| SC-4: 티칭 UI step 머신 + 이미지 전환 | Test 3-c + 3-d (사용자 wizard 실행) |
| SC-5: Action_FAIMeasurement 분기 + Datum 보정 실제 적용 (T-34-03-08 해소) | Test 3-e step 1 (검출 PASS) + step 2 (DualImage 측정값 ≠ identity) + 3-f (빈 경로 가드) |
| SC-6: INI 호환 + 미존재 키 정규화 | Test 4 (시나리오 A + B 사용자 확인) |
| SC-7: 1-image 회귀 0 + msbuild PASS | Test 1 (자동) + Test 2 (사용자 SIMUL) |
| D-34-13/14 가드 | Test 5 (자동 git diff) |

모든 SC 가 Plan 04 의 Test 1~5 로 매핑되어 검증 가능. 사용자 합의 시 Phase 34 종결.
</verification>

<success_criteria>
- [ ] Test 1 (msbuild) — 자동 PASS + 사용자 합의
- [ ] Test 2 (1-image 3종 회귀 0) — 사용자 SIMUL 확인 (3 시나리오)
- [ ] Test 3 (신규 DualImage SIMUL UAT) — 사용자 확인 (6+1 sub-test)
- [ ] Test 4 (INI 라운드트립) — 사용자 확인 (2 시나리오)
- [ ] Test 5 (D-34-13/14 가드) — 자동 git diff PASS + 사용자 합의
- [ ] 34-UAT.md 작성 + git commit
- [ ] 34-04-SUMMARY.md 작성 + git commit
- [ ] Phase 35 Test 4 (Side carry-over) 흡수 결정 명시
- [ ] carry-over (있다면) — Phase 35 후속 / 별도 quick task / 메모리 정정 영역 식별
</success_criteria>

<output>
Plan 04 완료 시 다음 산출물:
- `.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-UAT.md` — frontmatter status + 5 Test 결과
- `.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-04-SUMMARY.md` — Plan 04 + Phase 34 종합 요약

Phase 34 의 final state — signed_off (모두 PASS) 또는 partial (일부 carry-over) — 가 STATE.md / ROADMAP.md 에 반영되도록 orchestrator 에 전달.
</output>
