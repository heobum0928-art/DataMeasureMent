---
phase: 34.1-datum-dualimage-swap-ux-2026-05-27
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/Custom/Sequence/Inspection/EImageSource.cs   # 신규 enum 파일
  - WPF_Example/DatumMeasurement.csproj                       # EImageSource.cs Compile 등록
  - Cal_Image/DualImageTest/.gitkeep                          # 신규 폴더 placeholder
  - Cal_Image/DualImageTest/README.md                         # 사용자 페어 배치 가이드
autonomous: true
requirements_addressed:
  - D-34.1-02
  - D-34.1-03
  - D-34.1-07
  - D-34.1-08
  - D-34.1-09
  - D-34.1-10
  - D-34.1-11
  - D-34.1-12
  - D-34.1-13
  - D-34.1-14
  - D-34.1-15
  - D-34.1-17
tags: [phase-34.1, datum, dualimage, swap-ux, ui-only]

must_haves:
  truths:
    - "DualImage 알고리즘 (VerticalTwoHorizontalDualImage) 선택 시 캔버스 우상단에 현재 표시 이미지 배지가 표시된다 (가로축 = Blue700 #1976D2 / 세로축 = Orange800 #F57C00, 폰트 14px, 마진 12px, 텍스트 '가로축'/'세로축')"
    - "DualImage 알고리즘 Datum 노드 선택 시 캔버스 툴바에 [👁 가로] [👁 세로] 토글 버튼 2개가 표시되고 사용자가 클릭하면 즉시 (a) 배지 텍스트 + (b) 배지 색상 + (c) 캔버스 ROI 가시성 (가로 = HorizontalA+HorizontalB / 세로 = Vertical) 3자가 동시 전환된다 (D-34.1-15)"
    - "1-image 알고리즘 (TwoLineIntersect / CircleTwoHorizontal / VerticalTwoHorizontal) 선택 시 토글 버튼 + 배지 모두 Collapsed (D-34.1-09)"
    - "Datum 노드 이동 시 swap 상태 = 기본 가로축으로 초기화된다 (D-34.1-08, 세션 한정, INI 미저장)"
    - "Wizard step 라벨은 swap 시 변경되지 않는다 — step = '편집 의도', 배지 = '현재 화면' 분리 (D-34.1-11)"
    - "자동 swap (기존 StartDatumTeachStep(Vertical) at MainView.xaml.cs L1994-2008) 도 새 헬퍼 `UpdateImageSourceBadge(EImageSource)` 를 통해 동일하게 배지 + ROI 가시성 갱신된다 (D-34.1-15 자동 분기 일관성)"
    - "DatumConfig.cs 의 비의도 working-tree 수정 (현재 깨진 주석 1줄) 이 git checkout 으로 HEAD 상태로 복원되어 D-34-14 / D-34.1-07 가드 (DatumConfig 변경 0) 가 실제로 충족된다"
    - "Cal_Image/DualImageTest/ 폴더가 신규 생성되고, 사용자가 SIMUL 의사 페어 (Top 가로 1장 + Side 세로 1장) 를 배치할 수 있는 README + .gitkeep 가 존재한다 (D-34.1-13/17)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/EImageSource.cs"
      provides: "EImageSource enum (Horizontal, Vertical) — 자동/수동 swap 의 공통 인자 타입"
      contains: "enum EImageSource"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml"
      provides: "[👁 가로] / [👁 세로] 토글 버튼 2개 + 캔버스 우상단 배지 Border+TextBlock"
      contains: "btn_swapHorizontal"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "UpdateImageSourceBadge(EImageSource) 공통 헬퍼 + 토글 핸들러 2개 + Datum 노드 선택 초기화 + AlgorithmType 변경 Visibility 즉시 업데이트"
      contains: "UpdateImageSourceBadge"
    - path: "Cal_Image/DualImageTest/README.md"
      provides: "SIMUL 의사 페어 (Top 가로 1장 + Side 세로 1장) 배치 가이드"
  key_links:
    - from: "MainView.xaml.cs btn_swapHorizontal_Click / btn_swapVertical_Click"
      to: "UpdateImageSourceBadge(EImageSource)"
      via: "직접 호출"
      pattern: "UpdateImageSourceBadge\\("
    - from: "MainView.xaml.cs StartDatumTeachStep(Vertical) L1994~2008 (기존 자동 swap)"
      to: "UpdateImageSourceBadge(EImageSource.Vertical)"
      via: "자동 swap 직후 호출"
      pattern: "UpdateImageSourceBadge\\(EImageSource\\.Vertical\\)"
    - from: "PublishDatumRoiCandidates(DatumConfig)"
      to: "현재 swap 상태 (가로/세로) 별 ROI subset"
      via: "DualImage 분기에서 _currentImageSource 필드 참조하여 가로면 HorizontalA+B 만, 세로면 Vertical 만 list 추가"
      pattern: "_currentImageSource"
---

<objective>
Phase 34 partial sign-off (2026-05-27) 의 CO-34-01 (Datum DualImage swap UX) 을 코드 레벨에서 종결한다.

**Purpose**:
- 사용자 피드백 "이미지를 사용자가 원하는대로 스왑이 필요할 꺼 같아 이렇게 보면 헷갈려" 직접 대응.
- 자동 swap (Phase 34 D-34-06) 만으로는 (a) 현재 어느 축 이미지인지 시각적 구분 불가, (b) 임의 swap 불가 → ROI 그리기 신뢰성 확보 불가능 → DualImage 워크플로우 미완성.
- 본 plan 으로 (a) 캔버스 우상단 배지 + (b) 캔버스 툴바 토글 버튼 2개 + (c) 가로/세로 ROI 가시성 동기 토글 = 3자 동시 전환 (D-34.1-15) 을 단일 헬퍼 `UpdateImageSourceBadge(EImageSource)` 로 일원화.

**Output**:
- MainView 의 XAML 2 사항 (토글 버튼 2개 + 배지 Border) 추가.
- MainView.xaml.cs 의 코드 (1) `UpdateImageSourceBadge(EImageSource)` 공통 헬퍼 + (2) 토글 버튼 Click 핸들러 2개 + (3) `PublishDatumRoiCandidates` 의 DualImage 분기 ROI subset 필터링 + (4) Datum 노드 선택 시 swap 상태 기본값 가로 리셋 + (5) AlgorithmType 변경 시 Visibility 즉시 업데이트 + (6) 기존 자동 swap site (L1994-2008) 가 새 헬퍼 호출 패턴으로 refactor.
- 신규 `EImageSource` enum 파일 (DatumConfig.cs 변경 0 가드 유지를 위해 별도 파일).
- DatumConfig.cs working tree 수정 사전 정리 (git checkout).
- Cal_Image/DualImageTest/ 신규 폴더 + README + .gitkeep.

**Anti-goals (변경 0 가드 — D-34-13/14 + D-34.1-07 유지)**:
- `WPF_Example/TcpServer/VisionResponsePacket.cs` — 변경 0
- `WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` — 변경 0
- `WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` — 변경 0
- `WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` — Phase 34 추가 (TeachingImagePath_Vertical 필드 외 ICustomTypeDescriptor 분기) 유지. 본 plan 에서 **추가 변경 0** + 사전 working-tree 깨진 주석 1줄 복원 (HEAD 상태로 되돌림).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-CONTEXT.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-04-SUMMARY.md

# 변경 대상 파일 (인터페이스/구조 미리 파악)
@WPF_Example/UI/ContentItem/MainView.xaml
@WPF_Example/UI/ContentItem/MainView.xaml.cs

# 참조 전용 (변경 0)
@WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
@WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
@WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs

<interfaces>
<!-- Plan 1 이 의존하는 기존 API. Executor 가 본 plan 안 값만 보고 구현 가능하도록 verbatim 추출. -->

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (L33-51, 변경 금지):
```csharp
[Category("Datum|ImageSource")]
public string ImageSourceMode { get; set; } = "Dedicated";

[Category("Datum|ImageSource")]
public string SourceShotName { get; set; } = "";

[Category("Datum|ImageSource")]
public string TeachingImagePath { get; set; } = "";       // 가로축 이미지 (Phase 22 IMG-01)

[Category("Datum|ImageSource")]
public string TeachingImagePath_Vertical { get; set; } = ""; // 세로축 이미지 (Phase 34 D-34-04)
```

From WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs L80 (AlgorithmTypeList, 변경 금지):
- `EDatumAlgorithm.VerticalTwoHorizontalDualImage` 이미 enum 에 등록됨 (Phase 34 D-34-05). 본 plan 의 Visibility binding 조건 = `datum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage`.

From WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs L627-643:
```csharp
// ROI 가시성 토글 = 개별 IsVisible 노출 없음. 전체 collection swap 만 가능.
public void SetDatumRoiCandidates(IList<RoiDefinition> datumRois);
public void ClearDatumRoiCandidates();
public HImage CurrentImage { get; }
public void LoadImage(string path);
```

→ D-34.1-10 의 "데이터 보존 + 가시성 토글" 은 `SetDatumRoiCandidates` 에 현재 표시 축에 해당하는 ROI subset 만 넘기는 방식으로 구현. DatumConfig 의 ROI 데이터 (Horizontal_A_*, Horizontal_B_*, Vertical_*) 는 항상 보존됨.

From WPF_Example/UI/ContentItem/MainView.xaml.cs L1167-1198 (PublishDatumRoiCandidates, 본 plan 이 수정 대상):
```csharp
public void PublishDatumRoiCandidates(DatumConfig datum) {
    UpdateDatumRefCoordsLabel(datum);
    if (datum == null) { halconViewer.ClearDatumRoiCandidates(); return; }
    var list = new List<RoiDefinition>();
    switch (datum.AlgorithmTypeEnum) {
        case EDatumAlgorithm.TwoLineIntersect: /* Line1, Line2 */ break;
        case EDatumAlgorithm.CircleTwoHorizontal: /* Circle, HA, HB */ break;
        case EDatumAlgorithm.VerticalTwoHorizontal: /* Vertical, HA, HB */ break;
        // ★ DualImage 케이스 누락 — 본 plan 에서 추가 (현재 swap 상태 분기)
    }
    halconViewer.SetDatumRoiCandidates(list);
}
```

From WPF_Example/UI/ContentItem/MainView.xaml.cs L1994-2008 (기존 자동 swap site — 본 plan 이 refactor):
```csharp
case EDatumTeachStep.Vertical:
    if (_editingDatum != null
        && _editingDatum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) {
        string vpath = _editingDatum.TeachingImagePath_Vertical;
        if (!string.IsNullOrEmpty(vpath) && System.IO.File.Exists(vpath)) {
            try { halconViewer.LoadImage(vpath); }
            catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, ex.Message); }
            // ★ 본 plan: 여기 직후 UpdateImageSourceBadge(EImageSource.Vertical) 호출 추가
        } else { /* 빈 경로 안내 */ break; }
        ...
    }
```

From WPF_Example/UI/ControlItem/InspectionListView.xaml.cs L455 (Datum 노드 선택 시):
```csharp
mParentWindow.mainView.PublishDatumRoiCandidates(datumCfg);
// ★ 본 plan: PublishDatumRoiCandidates 내부에서 _currentImageSource = EImageSource.Horizontal 으로 리셋
```

From WPF_Example/UI/ContentItem/MainView.xaml L171:
- `<dmv:MainResultViewerControl x:Name="halconViewer"/>` 가 Grid Row=1 안에 있고, 그 Grid 가 캔버스 영역. 본 plan 의 배지 Border 는 동일 Grid Row=1 에 `HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,12,12,0"` 으로 추가.

From WPF_Example/UI/ContentItem/MainView.xaml L91-96 (기존 btn_teachDatum 패턴 — RoiToggleButtonStyle 재사용):
```xml
<ToggleButton x:Name="btn_teachDatum" Content="Teach Datum"
    Click="TeachDatumButton_Click"
    Style="{StaticResource RoiToggleButtonStyle}"
    IsEnabled="False"
    MinWidth="100" Height="28" Padding="12,4"
    FontSize="13" FontWeight="SemiBold" Margin="0,0,4,0"/>
```
→ btn_swapHorizontal / btn_swapVertical 도 같은 Style 재사용, btn_teachDatum 다음 자리에 배치.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DatumConfig.cs working-tree 정리 + EImageSource enum 신규 + csproj 등록</name>
  <files>
    WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs   (git checkout, HEAD 상태로 복원)
    WPF_Example/Custom/Sequence/Inspection/EImageSource.cs  (신규)
    WPF_Example/DatumMeasurement.csproj                     (Compile ItemGroup 등록)
  </files>
  <read_first>
    - .planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-CONTEXT.md (D-34.1-07/09/14 잠금값)
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-CONTEXT.md (D-34-13/14 가드 4파일)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (현재 상태 확인 — L27~35 working-tree 깨진 주석)
    - WPF_Example/Custom/Sequence/Inspection/EDatumAlgorithm.cs (기존 enum 파일 위치/스타일 참조)
    - WPF_Example/DatumMeasurement.csproj (Compile ItemGroup 패턴)
  </read_first>
  <action>
    **(1) DatumConfig.cs working-tree 정리 (D-34.1-07 가드 실제 충족):**

    먼저 현재 git diff 확인:
    ```bash
    git diff HEAD -- WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
    ```

    현재 working tree 에 의도 없이 깨진 주석 1줄이 있음 (L30 부근, `"Dedicated" 또는 "ReuseFromShot" (D-07, D-08)` 주석이 줄바꿈으로 깨짐). HEAD 상태로 복원:
    ```bash
    git checkout HEAD -- WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
    ```

    복원 후 재확인 — git diff 가 비어 있어야 함 (`git diff --stat HEAD -- WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 출력 0줄).

    **(2) EImageSource enum 신규 파일 생성:**

    파일: `WPF_Example/Custom/Sequence/Inspection/EImageSource.cs`

    내용 (C# 7.2, Allman brace, EDatumAlgorithm.cs 와 동일 namespace + 스타일 차용):

    ```csharp
    //260527 hbk Phase 34.1 D-34.1-15 — 자동/수동 swap 의 공통 인자 타입. EImageSource 단일 enum.
    //  - UpdateImageSourceBadge(EImageSource) 헬퍼 시그니처 단일화 → 자동 swap (L1994-2008) + 수동 토글 (btn_swapHorizontal/Vertical_Click) 양쪽 일관성 보장.
    //  - DatumConfig 변경 0 가드 (D-34-14 / D-34.1-07) 유지 위해 별도 파일.

    namespace ReringProject.Sequence
    {
        public enum EImageSource
        {
            Horizontal = 0,  //260527 hbk Phase 34.1 — 가로축 (TeachingImagePath). 기본값.
            Vertical   = 1   //260527 hbk Phase 34.1 — 세로축 (TeachingImagePath_Vertical, DualImage 전용).
        }
    }
    ```

    **(3) csproj 에 EImageSource.cs 등록 (Rule 3 auto-fix — 미등록 시 CS0246 빌드 차단):**

    `WPF_Example/DatumMeasurement.csproj` 의 `<ItemGroup>` 안 Compile 목록 중 기존 `Custom\Sequence\Inspection\EDatumAlgorithm.cs` 또는 `DatumConfig.cs` 등록 라인 바로 근처에 다음 줄 추가:
    ```xml
    <Compile Include="Custom\Sequence\Inspection\EImageSource.cs" />
    ```

    파일 경로 슬래시는 csproj 패턴 (`\`) 따를 것 (기존 항목 grep 으로 confirm).
  </action>
  <verify>
    <automated>
      cd "C:/Info/Project/DataMeasurement" && \
      git diff --stat HEAD -- WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs | wc -l > /tmp/datum_diff.txt && \
      test "$(cat /tmp/datum_diff.txt)" = "0" && echo "DatumConfig clean" && \
      test -f WPF_Example/Custom/Sequence/Inspection/EImageSource.cs && echo "EImageSource exists" && \
      grep -q "enum EImageSource" WPF_Example/Custom/Sequence/Inspection/EImageSource.cs && echo "enum decl ok" && \
      grep -qE "Horizontal[[:space:]]*=[[:space:]]*0" WPF_Example/Custom/Sequence/Inspection/EImageSource.cs && \
      grep -qE "Vertical[[:space:]]*=[[:space:]]*1" WPF_Example/Custom/Sequence/Inspection/EImageSource.cs && echo "members ok (whitespace-insensitive regex per W2)" && \
      grep -q "Custom\\\\Sequence\\\\Inspection\\\\EImageSource.cs" WPF_Example/DatumMeasurement.csproj && echo "csproj registered"
      # W2 노트 (plan-checker 2026-05-27): 두 enum member grep 모두 `=` 양쪽 공백 수에 둔감한 정규식 사용.
      #   - 기존 verbatim `"Vertical   = 1"` (3-space) 패턴은 executor auto-format (single-space) 시 가짜 BLOCKER 유발 → 정규식으로 교체.
      #   - `[[:space:]]*` 0개 이상 매칭 — `Vertical=1`, `Vertical = 1`, `Vertical   = 1` 모두 PASS.
    </automated>
  </verify>
  <acceptance_criteria>
    - `git diff HEAD -- WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 출력 = 빈 문자열 (DatumConfig.cs 변경 0 가드 D-34-14 / D-34.1-07 충족)
    - `WPF_Example/Custom/Sequence/Inspection/EImageSource.cs` 파일 존재 + `namespace ReringProject.Sequence` + `public enum EImageSource` + `Horizontal = 0` / `Vertical = 1` 멤버 + Phase 34.1 hbk 주석
    - `WPF_Example/DatumMeasurement.csproj` 에 `Custom\Sequence\Inspection\EImageSource.cs` Compile ItemGroup 라인 추가됨
  </acceptance_criteria>
  <done>EImageSource enum 단일 신규 파일 생성 + csproj 등록 + DatumConfig.cs working-tree 청결 복원. Task 2 의 헬퍼/핸들러 시그니처가 컴파일 가능한 인터페이스 확보.</done>
</task>

<task type="auto">
  <name>Task 2: MainView 토글 버튼 + 배지 XAML 추가 + 3자 동시 갱신 헬퍼 + ROI subset 필터링 + 자동 swap site refactor</name>
  <files>
    WPF_Example/UI/ContentItem/MainView.xaml
    WPF_Example/UI/ContentItem/MainView.xaml.cs
  </files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml (전체 1~210 라인 — 캔버스 툴바 L60-167 + 캔버스 Grid L169-206 + 기존 RoiToggleButtonStyle L10-50)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L60-90 (필드 선언부 — _editingDatum 위치)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L1149-1200 (UpdateImageSourceLabel + PublishDatumRoiCandidates 기존 메서드)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L1860-1900 (TeachDatumButton_Click + _editingDatum 할당 패턴)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L1994-2055 (StartDatumTeachStep — 기존 자동 swap site, refactor 대상)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L2150-2180 (InvokeTryTeachDatum 의 DualImage 분기, L2170 부근 — TeachingImagePath_Vertical 사용 site 참조)
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs L450-460 (mParentWindow.mainView.PublishDatumRoiCandidates 호출 site — 본 plan 변경 없음, 진입점 확인 only)
    - WPF_Example/Custom/Sequence/Inspection/EImageSource.cs (Task 1 결과물)
    - .planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-CONTEXT.md (D-34.1-08~15 잠금값)
  </read_first>
  <action>
    **(A) MainView.xaml 변경 — 토글 버튼 2개 + 배지 Border 추가:**

    **A-1) 캔버스 툴바 (Grid Row=0, 기존 btn_teachDatum L91-96 직후 자리)에 토글 버튼 2개 + Separator 추가**:

    위치: `btn_testFindDatum` (L99-117) **직전** (즉 btn_teachDatum 과 btn_testFindDatum 사이) 에 다음 XAML 삽입.

    ```xml
    <!--260527 hbk Phase 34.1 D-34.1-02/09 — DualImage 변형 수동 swap 토글 (가로축/세로축). Visibility 는 code-behind 의 OnDatumSelectionChanged / AlgorithmTypeChanged 에서 제어.-->
    <ToggleButton x:Name="btn_swapHorizontal" Content="👁 가로"
        Click="BtnSwapHorizontal_Click"
        Style="{StaticResource RoiToggleButtonStyle}"
        IsEnabled="True" Visibility="Collapsed"
        MinWidth="80" Height="28" Padding="12,4"
        FontSize="13" FontWeight="SemiBold" Margin="0,0,4,0"
        ToolTip="가로축 이미지로 전환 (HorizontalA + HorizontalB ROI 표시)"/>

    <!--260527 hbk Phase 34.1 D-34.1-02/09 — DualImage 변형 수동 swap 토글 (세로축).-->
    <ToggleButton x:Name="btn_swapVertical" Content="👁 세로"
        Click="BtnSwapVertical_Click"
        Style="{StaticResource RoiToggleButtonStyle}"
        IsEnabled="True" Visibility="Collapsed"
        MinWidth="80" Height="28" Padding="12,4"
        FontSize="13" FontWeight="SemiBold" Margin="0,0,4,0"
        ToolTip="세로축 이미지로 전환 (Vertical ROI 표시)"/>
    ```

    Separator 는 추가하지 않음 (기존 btn_testFindDatum 의 좌측 마진으로 시각 분리 충분 — 기존 L119-120 Separator 는 btn_calibrate 앞만 유지).

    **A-2) 캔버스 Grid (Row=1, L169-206) 안에 배지 Border 추가**:

    위치: `<Border VerticalAlignment="Bottom" ...>` (L178) **직전** (즉 `label_message` L172-176 직후, 같은 Grid Row=1 영역) 에 다음 XAML 삽입.

    ```xml
    <!--260527 hbk Phase 34.1 D-34.1-03/14 — 현재 표시 이미지 배지 (캔버스 우상단, DualImage 변형일 때만 Visible).
         색상/텍스트는 code-behind UpdateImageSourceBadge(EImageSource) 에서 setting.
         가로축 = #1976D2 Material Blue 700 / 세로축 = #F57C00 Material Orange 800 / 14px / 마진 우상단 12px.-->
    <Border x:Name="border_imageSourceBadge"
            HorizontalAlignment="Right" VerticalAlignment="Top"
            Margin="0,12,12,0" Padding="10,4"
            CornerRadius="4" Background="#1976D2"
            Visibility="Collapsed" IsHitTestVisible="False">
        <TextBlock x:Name="txt_imageSourceBadge"
                   Text="가로축" Foreground="White"
                   FontSize="14" FontWeight="SemiBold"/>
    </Border>
    ```

    **(B) MainView.xaml.cs 변경 — 5개 사항:**

    **B-1) 필드 추가 (L60 부근 `_editingDatum` 선언부 근처)**:

    ```csharp
    //260527 hbk Phase 34.1 D-34.1-08/12 — 현재 캔버스에 표시 중인 이미지 축 (가로/세로). DualImage 변형에서만 의미 있음.
    //  세션 한정, INI 미저장 (Datum 노드 이동 시 가로축으로 리셋).
    private ReringProject.Sequence.EImageSource _currentImageSource = ReringProject.Sequence.EImageSource.Horizontal;
    ```

    **B-2) 공통 헬퍼 `UpdateImageSourceBadge(EImageSource)` 신규 추가 (L1149 `UpdateImageSourceLabel` 직후 위치 권장 — 시각화 헬퍼 모여 있음)**:

    ```csharp
    //260527 hbk Phase 34.1 D-34.1-15 — 자동/수동 swap 의 단일 진입점.
    //  3자 동시 갱신: (a) _currentImageSource 필드 + (b) 배지 텍스트/색상 + (c) ROI 가시성 (PublishDatumRoiCandidates 재호출 → 현재 축 ROI subset 만 표시).
    //  자동 swap (StartDatumTeachStep(Vertical) at L1994~) + 수동 swap (BtnSwap*_Click) 모두 본 메서드 경유.
    private void UpdateImageSourceBadge(ReringProject.Sequence.EImageSource source) {
        _currentImageSource = source;

        // (b) 배지 텍스트 + 색상 — D-34.1-14 잠금값
        if (border_imageSourceBadge != null && txt_imageSourceBadge != null) {
            if (source == ReringProject.Sequence.EImageSource.Horizontal) {
                txt_imageSourceBadge.Text = "가로축"; //260527 hbk Phase 34.1 D-34.1-14
                border_imageSourceBadge.Background = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#1976D2")); //260527 hbk Phase 34.1 D-34.1-14
            }
            else {
                txt_imageSourceBadge.Text = "세로축"; //260527 hbk Phase 34.1 D-34.1-14
                border_imageSourceBadge.Background = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#F57C00")); //260527 hbk Phase 34.1 D-34.1-14
            }
        }

        // 토글 버튼 IsChecked 동기화 (수동 클릭 / 자동 swap 양방향) — 한쪽만 체크 (radio 패턴)
        if (btn_swapHorizontal != null) btn_swapHorizontal.IsChecked = (source == ReringProject.Sequence.EImageSource.Horizontal); //260527 hbk Phase 34.1
        if (btn_swapVertical   != null) btn_swapVertical.IsChecked   = (source == ReringProject.Sequence.EImageSource.Vertical);   //260527 hbk Phase 34.1

        // (c) ROI 가시성 — 현재 _editingDatum 가 DualImage 일 때만 subset 필터 적용. 다른 algorithm 은 PublishDatumRoiCandidates 가 알아서 처리.
        if (_editingDatum != null
            && _editingDatum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) { //260527 hbk Phase 34.1 D-34.1-10
            PublishDatumRoiCandidates(_editingDatum); //260527 hbk Phase 34.1 — DualImage 분기가 _currentImageSource 참조하여 subset 만 publish
        }
    }
    ```

    **B-3) `PublishDatumRoiCandidates` 에 DualImage 분기 추가 (L1167-1198 switch 안)**:

    기존 switch 안 마지막 case (VerticalTwoHorizontal) 다음에 DualImage 분기 case 추가:

    ```csharp
    //260527 hbk Phase 34.1 D-34.1-10 — DualImage 변형: 현재 표시 축에 해당하는 ROI subset 만 publish.
    //  가로축 = HorizontalA + HorizontalB / 세로축 = Vertical. ROI 데이터 자체는 DatumConfig 에 항상 보존됨 (가시성만 토글).
    case EDatumAlgorithm.VerticalTwoHorizontalDualImage:
        if (_currentImageSource == ReringProject.Sequence.EImageSource.Horizontal) { //260527 hbk Phase 34.1 D-34.1-10
            if (datum.Horizontal_A_Length1 > 0 && datum.Horizontal_A_Length2 > 0) //260527 hbk Phase 34.1
                list.Add(BuildDatumRectCandidate("Datum.HorizontalA", datum.Horizontal_A_Row, datum.Horizontal_A_Col, datum.Horizontal_A_Length1, datum.Horizontal_A_Length2));
            if (datum.Horizontal_B_Length1 > 0 && datum.Horizontal_B_Length2 > 0) //260527 hbk Phase 34.1
                list.Add(BuildDatumRectCandidate("Datum.HorizontalB", datum.Horizontal_B_Row, datum.Horizontal_B_Col, datum.Horizontal_B_Length1, datum.Horizontal_B_Length2));
        }
        else { //260527 hbk Phase 34.1 — Vertical 표시 중: Vertical ROI 만
            if (datum.Vertical_Length1 > 0 && datum.Vertical_Length2 > 0) //260527 hbk Phase 34.1
                list.Add(BuildDatumRectCandidate("Datum.Vertical", datum.Vertical_Row, datum.Vertical_Col, datum.Vertical_Length1, datum.Vertical_Length2));
        }
        break; //260527 hbk Phase 34.1
    ```

    **B-3-1) `PublishDatumRoiCandidates` 진입부에 swap 상태 리셋 + Visibility 동기화 추가 (D-34.1-08/09/12)**:

    기존 메서드 본문 시작 부분 (UpdateDatumRefCoordsLabel 호출 직후) 에:

    ```csharp
    //260527 hbk Phase 34.1 D-34.1-08/09 — Datum 노드 (재)선택 시: swap 상태 = 기본 가로축 리셋 (세션 한정).
    //  Visibility 동기화: DualImage 변형이면 토글 버튼 + 배지 Visible, 1-image 변형 / null 이면 Collapsed.
    bool isDualImage = (datum != null //260527 hbk Phase 34.1 D-34.1-09
                        && datum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage);
    if (btn_swapHorizontal != null) btn_swapHorizontal.Visibility = isDualImage ? Visibility.Visible : Visibility.Collapsed; //260527 hbk Phase 34.1 D-34.1-09
    if (btn_swapVertical   != null) btn_swapVertical.Visibility   = isDualImage ? Visibility.Visible : Visibility.Collapsed; //260527 hbk Phase 34.1 D-34.1-09
    if (border_imageSourceBadge != null) border_imageSourceBadge.Visibility = isDualImage ? Visibility.Visible : Visibility.Collapsed; //260527 hbk Phase 34.1 D-34.1-09

    if (isDualImage) {
        // Datum 노드 선택 직후 = 가로축 기본 (D-34.1-08). 단, 본 메서드가 자동/수동 swap 도중 재호출되면 _currentImageSource 가 이미 변경되어 있을 수 있음.
        // 진입점 구분: _editingDatum != datum 이면 새 노드 선택 → 리셋. 같으면 swap 진행 중 → 보존.
        if (_editingDatum != datum) { //260527 hbk Phase 34.1 D-34.1-08 — 새 노드 진입만 리셋
            _currentImageSource = ReringProject.Sequence.EImageSource.Horizontal;
            // 배지 텍스트/색상도 가로축으로 동기 (별도 UpdateImageSourceBadge 호출 없이 직접 — 재귀 회피)
            if (txt_imageSourceBadge != null) txt_imageSourceBadge.Text = "가로축";
            if (border_imageSourceBadge != null) border_imageSourceBadge.Background = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#1976D2"));
            if (btn_swapHorizontal != null) btn_swapHorizontal.IsChecked = true;
            if (btn_swapVertical   != null) btn_swapVertical.IsChecked   = false;
        }
    }
    ```

    **B-4) 수동 swap 핸들러 2개 추가 (B-2 헬퍼 메서드 바로 다음 위치 권장)**:

    ```csharp
    //260527 hbk Phase 34.1 D-34.1-02 — 가로축 토글 버튼 Click. 가로축 이미지로 swap + 배지/ROI 갱신.
    private void BtnSwapHorizontal_Click(object sender, RoutedEventArgs e) {
        if (_editingDatum == null) return;
        if (_editingDatum.AlgorithmTypeEnum != EDatumAlgorithm.VerticalTwoHorizontalDualImage) return; //260527 hbk Phase 34.1 D-34.1-09 — DualImage 외 케이스 가드
        string hpath = _editingDatum.TeachingImagePath;
        if (!string.IsNullOrEmpty(hpath) && System.IO.File.Exists(hpath)) {
            try { halconViewer.LoadImage(hpath); } //260527 hbk Phase 34.1 — 자동 swap (L1994~) 와 동일 경로
            catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, ex.Message); }
        }
        UpdateImageSourceBadge(ReringProject.Sequence.EImageSource.Horizontal); //260527 hbk Phase 34.1 D-34.1-15 — 3자 동시 갱신
    }

    //260527 hbk Phase 34.1 D-34.1-02 — 세로축 토글 버튼 Click. 세로축 이미지로 swap + 배지/ROI 갱신.
    private void BtnSwapVertical_Click(object sender, RoutedEventArgs e) {
        if (_editingDatum == null) return;
        if (_editingDatum.AlgorithmTypeEnum != EDatumAlgorithm.VerticalTwoHorizontalDualImage) return; //260527 hbk Phase 34.1 D-34.1-09
        string vpath = _editingDatum.TeachingImagePath_Vertical;
        if (!string.IsNullOrEmpty(vpath) && System.IO.File.Exists(vpath)) {
            try { halconViewer.LoadImage(vpath); }
            catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, ex.Message); }
        }
        UpdateImageSourceBadge(ReringProject.Sequence.EImageSource.Vertical); //260527 hbk Phase 34.1 D-34.1-15
    }
    ```

    **B-5) 기존 자동 swap site refactor (L1994-2008, StartDatumTeachStep(Vertical) 안)**:

    L2001 (`try { halconViewer.LoadImage(vpath); } catch ... }`) **직후** 에 다음 1줄 추가:

    ```csharp
    UpdateImageSourceBadge(ReringProject.Sequence.EImageSource.Vertical); //260527 hbk Phase 34.1 D-34.1-15 — 자동 swap 도 3자 동시 갱신
    ```

    또한 `case EDatumTeachStep.HorizontalA:` (L2018-2030) 진입 시점 = 사용자가 가로축에서 시작해야 하므로 자동 swap 안 함 (Datum 노드 선택 시 가로축 기본). 단, wizard 가 Vertical → HorizontalA 로 역행할 수 있는 경로가 있다면 가로축 자동 swap 추가. **본 plan 범위**: 가로축 자동 swap 은 추가하지 않음 (Phase 34 wizard 는 HA→HB→V 단방향 — D-34-06 verbatim). 사용자가 가로로 되돌리려면 수동 토글 사용.

    **참고 — D-34.1-11 라벨 분리 가드:**
    StartDatumTeachStep 안 `label_drawHint.Content = "Step 3/3: 수직 ROI를 드래그하세요"` (L2009) 는 변경하지 않음. 라벨 = 편집 의도 (Vertical step), 배지 = 현재 화면 (Vertical 이미지). UpdateImageSourceBadge 호출이 배지/ROI 만 갱신.
  </action>
  <verify>
    <automated>
      cd "C:/Info/Project/DataMeasurement" && \
      grep -q 'x:Name="btn_swapHorizontal"' WPF_Example/UI/ContentItem/MainView.xaml && echo "btn_swapHorizontal xaml ok" && \
      grep -q 'x:Name="btn_swapVertical"' WPF_Example/UI/ContentItem/MainView.xaml && echo "btn_swapVertical xaml ok" && \
      grep -q 'x:Name="border_imageSourceBadge"' WPF_Example/UI/ContentItem/MainView.xaml && echo "badge border ok" && \
      grep -q 'x:Name="txt_imageSourceBadge"' WPF_Example/UI/ContentItem/MainView.xaml && echo "badge text ok" && \
      grep -q '#1976D2' WPF_Example/UI/ContentItem/MainView.xaml && echo "blue700 hex ok" && \
      grep -q 'private void UpdateImageSourceBadge(' WPF_Example/UI/ContentItem/MainView.xaml.cs && echo "helper sig ok" && \
      grep -q 'private void BtnSwapHorizontal_Click' WPF_Example/UI/ContentItem/MainView.xaml.cs && echo "manual H handler ok" && \
      grep -q 'private void BtnSwapVertical_Click' WPF_Example/UI/ContentItem/MainView.xaml.cs && echo "manual V handler ok" && \
      grep -q '_currentImageSource' WPF_Example/UI/ContentItem/MainView.xaml.cs && echo "field ok" && \
      grep -q 'EDatumAlgorithm.VerticalTwoHorizontalDualImage' WPF_Example/UI/ContentItem/MainView.xaml.cs | head -1 && \
      grep -c 'UpdateImageSourceBadge(' WPF_Example/UI/ContentItem/MainView.xaml.cs && echo "(expected: 4+ — helper 정의 1 + 자동 1 + 수동 2)" && \
      grep -q '#F57C00' WPF_Example/UI/ContentItem/MainView.xaml.cs && echo "orange800 hex in helper ok" && \
      git diff --stat HEAD -- WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs WPF_Example/TcpServer/VisionResponsePacket.cs WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs && echo "(expected: 위 4파일 모두 0 변경 — D-34-13/14 + D-34.1-07 가드)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `WPF_Example/UI/ContentItem/MainView.xaml` 에 `x:Name="btn_swapHorizontal"`, `x:Name="btn_swapVertical"`, `x:Name="border_imageSourceBadge"`, `x:Name="txt_imageSourceBadge"` 4개 컨트롤 모두 추가됨
    - 배지 Border 의 기본 Background = `#1976D2` (가로축 = Blue 700, D-34.1-14)
    - 배지 TextBlock 의 FontSize = 14, 기본 Text = "가로축"
    - 토글 버튼 2개는 기본 `Visibility="Collapsed"`, `Style="{StaticResource RoiToggleButtonStyle}"`
    - 배지 Border 위치 = HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,12,12,0" (D-34.1-14 우상단 마진 12px)
    - `MainView.xaml.cs` 에 `private void UpdateImageSourceBadge(ReringProject.Sequence.EImageSource source)` 메서드 1개 정의
    - `BtnSwapHorizontal_Click` / `BtnSwapVertical_Click` 핸들러 2개 정의
    - `_currentImageSource` 필드 선언 (기본값 EImageSource.Horizontal)
    - `PublishDatumRoiCandidates` switch 에 `case EDatumAlgorithm.VerticalTwoHorizontalDualImage:` 분기 추가 + _currentImageSource 참조하여 가로/세로 ROI subset 분기
    - `PublishDatumRoiCandidates` 진입부에 isDualImage 계산 + 토글/배지 Visibility 일괄 갱신 + `_editingDatum != datum` 가드 안에서 가로축 리셋
    - 기존 자동 swap site (StartDatumTeachStep(Vertical), L1994~2008 부근) 에 `UpdateImageSourceBadge(ReringProject.Sequence.EImageSource.Vertical)` 호출 추가 (vpath 로드 try-catch 직후)
    - **가드 (D-34-13/14 + D-34.1-07)**: `git diff HEAD -- WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs WPF_Example/TcpServer/VisionResponsePacket.cs WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 출력 빈 문자열 (4파일 0 변경)
    - msbuild Debug/x64 Rebuild PASS, exit=0, 신규 warning 0:

      **PowerShell (권장 — 현재 shell):**
      ```powershell
      # msbuild PATH 자동 탐색 (vswhere fallback 포함)
      $msbuild = (Get-Command msbuild.exe -ErrorAction SilentlyContinue).Source
      if (-not $msbuild) {
          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          if (Test-Path $vswhere) {
              $msbuild = & $vswhere -latest -requires Microsoft.Component.MSBuild `
                          -find "MSBuild\**\Bin\MSBuild.exe" | Select-Object -First 1
          }
      }
      if (-not $msbuild) {
          throw "MSBuild not found. Add msbuild.exe to PATH or install VS Build Tools."
      }
      & $msbuild WPF_Example\DatumMeasurement.csproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /nologo /v:minimal
      Write-Host "Exit: $LASTEXITCODE"
      ```

      **Bash 대체 (msbuild 가 PATH 에 있을 때):**
      ```bash
      MSBUILD="${MSBUILD_PATH:-$(where msbuild.exe 2>/dev/null | head -1)}"
      [ -z "$MSBUILD" ] && { echo "MSBuild not found"; exit 1; }
      "$MSBUILD" WPF_Example/DatumMeasurement.csproj //t:Rebuild //p:Configuration=Debug //p:Platform=x64 //nologo //v:minimal
      ```

      **PASS 기준**: exit code 0 + log 내 `error ` 0건 + `warning ` 카운트가 Phase 34 baseline 과 동일 (신규 0).
      **W3 노트 (plan-checker 2026-05-27)**: 기존 verbatim VS2019 경로 (`/c/Program Files (x86)/.../2019/Community/...`) 는 환경 의존 → vswhere 자동 탐색 + 환경 fallback 안내로 교체. msbuild 가 PATH/vswhere 양쪽 모두 부재 시 사용자가 MSBuild 를 PATH 에 추가하거나 VS Build Tools 설치 필요.
  </acceptance_criteria>
  <done>MainView XAML + code-behind 변경 완료. 토글 버튼 2개 + 배지 1개 + 공통 헬퍼 + ROI subset 분기 + 자동 swap site refactor 모두 적용. 4 가드 파일 변경 0 확인. msbuild PASS.</done>
</task>

<task type="auto">
  <name>Task 3: Cal_Image/DualImageTest/ 폴더 + README 가이드 + .gitkeep 생성</name>
  <files>
    Cal_Image/DualImageTest/.gitkeep    (신규)
    Cal_Image/DualImageTest/README.md   (신규)
  </files>
  <read_first>
    - .planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-CONTEXT.md (D-34.1-13, D-34.1-17, **및 D-34.1-17 정정 2026-05-27**)
    - 현재 Cal_Image/ 디렉터리 상태 확인:
      ```bash
      cd "C:/Info/Project/DataMeasurement" && ls -la Cal_Image/ && git ls-files Cal_Image/ | head -20 && git status --short Cal_Image/
      ```
      → 발견: Cal_Image/ 는 거의 비어 있고, git-tracked 파일 2개 (`Cal_Image/Wafer/LEFT_1227/Left_CAL_1227_late.jpg`, `Cal_Image/Wafer/Right_1227/Right_CAL_1227_late.jpg`) 는 `D` (working tree 에서 삭제됨) 상태. Top/Side 폴더 자체가 존재하지 않음.
      → 결과: CONTEXT D-34.1-17 의 "기존 Cal_Image 의 Top/Side 이미지를 결정적 복사" 전제가 깨졌음 — CONTEXT 의 **D-34.1-17 (정정 2026-05-27)** 가 본 상황 흡수. 실제 SIMUL 페어 이미지 (Top 가로 1장 + Side 세로 1장) 배치는 사용자 수동 단계 (Plan 02 UAT 사전 준비) — 본 task 는 폴더 + 가이드 README 만 생성.
  </read_first>
  <action>
    **(1) 디렉터리 + 빈 placeholder 생성:**

    ```bash
    cd "C:/Info/Project/DataMeasurement"
    mkdir -p Cal_Image/DualImageTest
    touch Cal_Image/DualImageTest/.gitkeep
    ```

    `.gitkeep` 은 빈 파일.

    **(2) README.md 작성 (사용자 페어 배치 가이드 — Plan 2 UAT 가 본 폴더에서 페어를 읽음):**

    파일: `Cal_Image/DualImageTest/README.md`
    한국어, 600자 이하 권장.

    ```markdown
    # Cal_Image/DualImageTest/ — Phase 34.1 SIMUL 의사 페어

    Phase 34.1 (Datum DualImage swap UX) 의 SIMUL UAT 용 의사 듀얼 이미지 페어 배치 폴더.

    ## 배경

    Phase 34 (Datum VerticalTwoHorizontal 듀얼 티칭 이미지 변형) 의 신규 algorithm
    `VerticalTwoHorizontalDualImage` 는 가로축 ROI 2개 (HorizontalA + HorizontalB) 를 이미지 #1 에서,
    세로축 ROI 1개 (Vertical) 를 이미지 #2 에서 검출한다.

    Side fixture 실측 페어는 장비 도착 후 별도 확보 예정 (CO-34.1-01 carry-over).
    Phase 34.1 sign-off 는 SIMUL 의사 페어 (Top 가로 1장 + Side 세로 1장) 로 종결한다 (D-34.1-16).

    ## 정정 컨텍스트 (D-34.1-17 정정 2026-05-27)

    당초 CONTEXT D-34.1-17 잠금 = "Plan 1 의 마지막 task 에서 executor 가 결정적 조작으로 기존
    Cal_Image 의 Top/Side 이미지를 본 폴더로 복사 + 의미 있는 파일명 부여 → 사용자 수동 준비 부담 0."
    그러나 planning 단계 실측 결과 `Cal_Image/Top/` 및 `Cal_Image/Side/` 폴더가 워크트리에 존재하지
    않고 git-tracked 후보 2장도 working-tree deleted 상태로 확인됨. 이에 따라 **D-34.1-17 정정
    (CONTEXT.md L59)** 으로 "사용자 수동 준비 부담 0" 잠금 해제 + Plan 1 = 폴더+README 생성만,
    실제 이미지 페어 배치는 Plan 02 UAT 사전 준비 단계 사용자 책임으로 재정의됨. 본 README 가
    그 가이드 역할.

    ## 사용자 수동 배치 단계 (Phase 34.1 SIMUL UAT 직전)

    1. **가로축 이미지** (1장): Top 카메라 시뮬 이미지 중 가로 에지가 명확한 1장을 복사.
       - 권장 파일명: `top_horizontal.bmp` 또는 `top_horizontal.jpg`
       - 출처 후보: 기존 Top 시뮬 이미지 또는 Wafer/LEFT_1227, Right_1227 의 가로면이 보이는 이미지

    2. **세로축 이미지** (1장): Side 카메라 시뮬 이미지 중 세로 에지가 명확한 1장을 복사.
       - 권장 파일명: `side_vertical.bmp` 또는 `side_vertical.jpg`
       - 출처 후보: Cal_Image/Side/ (없으면 임의의 세로 에지 보이는 시뮬 이미지 1장)

    3. **검증**: 두 이미지가 본 폴더에 존재하면 UAT Test 7 (D-34.1-16) 진입 준비 완료.

    ## UAT 진입 경로 (Plan 2)

    1. 앱 실행 → InspectionListView 에서 Datum 노드 1개 선택.
    2. PropertyGrid 의 AlgorithmType = `VerticalTwoHorizontalDualImage` 로 변경.
    3. PropertyGrid 의 TeachingImagePath = `Cal_Image/DualImageTest/top_horizontal.{ext}` (절대 경로).
    4. PropertyGrid 의 TeachingImagePath_Vertical = `Cal_Image/DualImageTest/side_vertical.{ext}`.
    5. 캔버스 툴바의 `[👁 가로]` 토글 → 가로축 이미지 + HA/HB ROI 표시.
    6. `[👁 세로]` 토글 → 세로축 이미지 + Vertical ROI 표시.
    7. Test Find → Datum 결합 PASS.

    ## 변경 가드

    본 폴더는 Phase 34.1 sign-off 후에도 SIMUL UAT 재현용으로 보존.
    Phase 27 (Side Inspection 확장) 또는 CO-34.1-01 종결 시점에 실측 페어로 교체될 수 있음.
    ```

    **(3) 폴더 commit 준비 — git status 확인:**

    ```bash
    git status --short Cal_Image/DualImageTest/
    ```
    출력에 `?? Cal_Image/DualImageTest/.gitkeep` + `?? Cal_Image/DualImageTest/README.md` 가 보이면 정상.
  </action>
  <verify>
    <automated>
      cd "C:/Info/Project/DataMeasurement" && \
      test -d Cal_Image/DualImageTest && echo "dir ok" && \
      test -f Cal_Image/DualImageTest/.gitkeep && echo ".gitkeep ok" && \
      test -f Cal_Image/DualImageTest/README.md && echo "README ok" && \
      grep -q "Phase 34.1" Cal_Image/DualImageTest/README.md && echo "phase mark ok" && \
      grep -q "VerticalTwoHorizontalDualImage" Cal_Image/DualImageTest/README.md && echo "algo mark ok" && \
      grep -q "top_horizontal" Cal_Image/DualImageTest/README.md && \
      grep -q "side_vertical" Cal_Image/DualImageTest/README.md && echo "filename guide ok" && \
      grep -q "D-34.1-17 정정" Cal_Image/DualImageTest/README.md && echo "correction reference ok"
    </automated>
  </verify>
  <acceptance_criteria>
    - `Cal_Image/DualImageTest/` 디렉터리 존재
    - `.gitkeep` 빈 파일 존재 (git 이 빈 폴더 무시하지 않도록)
    - `README.md` 존재 + "Phase 34.1" / "VerticalTwoHorizontalDualImage" / "top_horizontal" / "side_vertical" / "D-34.1-17 정정" 키워드 포함
    - README 내용은 한국어 사용자 가이드 (CONTEXT D-34.1-13/17 의 "Top 1장 + Side 1장 = SIMUL 의사 페어" 가드 + 사용자 수동 배치 단계 + UAT 진입 경로 + D-34.1-17 정정 reference)
    - **변경 가드**: README 와 .gitkeep 외 신규 파일 0 (D-34-13/14 + D-34.1-07 가드 유지). `git status --short` 출력에 본 Task 신규 파일 2개 외 본 plan 범위 외 신규 변경 없음.
  </acceptance_criteria>
  <done>SIMUL 의사 페어 폴더 + 가이드 생성. Plan 2 의 UAT Test 7 진입 전 사용자가 README 따라 이미지 2장 수동 배치 가능. CONTEXT D-34.1-17 (정정 2026-05-27) — Cal_Image/Top/Side 폴더 부재 + 사용자 수동 책임 정합 — 의 "Plan 1 = 폴더+README 만 결정적 생성" 책임 충족. README 본문에 정정문 reference 포함하여 추적성 확보.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 ↔ MainView UI | 로컬 운영자 마우스 클릭. 신뢰 전제. |
| MainView ↔ halconViewer | 동일 프로세스 in-process 호출. 신뢰 전제. |
| MainView ↔ 파일 시스템 (LoadImage path) | 사용자가 PropertyGrid 에서 입력한 경로. 파일 존재 여부 가드 (`File.Exists`) 적용. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.1-01 | Information Disclosure | UI 배지 텍스트 + 토글 버튼 | accept | 캔버스 우상단 "가로축"/"세로축" 텍스트는 운영자 화면 전용. 외부 노출 없음 (UI surface only, no network endpoint, no log persistence). Phase 31 CO-23.1-01 `UpdateImageSourceLabel` 와 동일 신뢰 모델. |
| T-34.1-02 | Denial of Service | halconViewer.LoadImage(path) 호출 | mitigate | 본 plan 신규 호출 site 2곳 (BtnSwapHorizontal/Vertical_Click) 모두 `!string.IsNullOrEmpty(path) && System.IO.File.Exists(path)` 가드 + `try { ... } catch (Exception ex) { Logging.PrintErrLog(...); }` 으로 wrap (기존 자동 swap L1999-2001 패턴 1:1 복제). 비정상 path → silent fail + 에러 로그. |
| T-34.1-03 | Tampering | Cal_Image/DualImageTest/ 폴더 | accept | 로컬 디스크 placeholder + 한국어 README 가이드. 외부 입력 surface 없음. .gitkeep 은 빈 파일. README 내용은 정적 가이드. |

**Note (Phase 34.1 minimal surface):** UI-only 변경 + 신규 enum 1개 + 신규 placeholder 폴더. 네트워크 endpoint / auth 변경 / 데이터 persistence 변경 없음. DatumConfig 변경 0 가드로 INI 직렬화 attack surface 무변화. 본 threat model 은 새 attack vector 없음을 확인하는 sanity check.
</threat_model>

<verification>
**Plan 단위 자동 검증:**

1. **파일 존재 + 키워드 grep** (Task 1/2/3 의 verify automated 합산):
   ```bash
   cd "C:/Info/Project/DataMeasurement" && \
   test -f WPF_Example/Custom/Sequence/Inspection/EImageSource.cs && \
   grep -q 'enum EImageSource' WPF_Example/Custom/Sequence/Inspection/EImageSource.cs && \
   grep -q 'x:Name="btn_swapHorizontal"' WPF_Example/UI/ContentItem/MainView.xaml && \
   grep -q 'x:Name="btn_swapVertical"' WPF_Example/UI/ContentItem/MainView.xaml && \
   grep -q 'x:Name="border_imageSourceBadge"' WPF_Example/UI/ContentItem/MainView.xaml && \
   grep -q 'private void UpdateImageSourceBadge(' WPF_Example/UI/ContentItem/MainView.xaml.cs && \
   grep -q 'BtnSwapHorizontal_Click' WPF_Example/UI/ContentItem/MainView.xaml.cs && \
   grep -q 'BtnSwapVertical_Click' WPF_Example/UI/ContentItem/MainView.xaml.cs && \
   test -d Cal_Image/DualImageTest && test -f Cal_Image/DualImageTest/README.md
   ```

2. **변경 0 가드 (D-34-13/14 + D-34.1-07)**:
   ```bash
   cd "C:/Info/Project/DataMeasurement" && \
   git diff --stat HEAD -- \
     WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs \
     WPF_Example/TcpServer/VisionResponsePacket.cs \
     WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs \
     WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
   ```
   기대 출력: **빈 문자열** (4파일 모두 변경 0)

3. **msbuild Debug/x64 Rebuild PASS, exit=0, 신규 warning 0** (PowerShell + vswhere fallback, W3 보정):
   ```powershell
   $msbuild = (Get-Command msbuild.exe -ErrorAction SilentlyContinue).Source
   if (-not $msbuild) {
       $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
       if (Test-Path $vswhere) {
           $msbuild = & $vswhere -latest -requires Microsoft.Component.MSBuild `
                       -find "MSBuild\**\Bin\MSBuild.exe" | Select-Object -First 1
       }
   }
   if (-not $msbuild) { throw "MSBuild not found. Add to PATH or install VS Build Tools." }
   & $msbuild WPF_Example\DatumMeasurement.csproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /nologo /v:minimal `
       2>&1 | Tee-Object -FilePath $env:TEMP\build.log
   Write-Host "Exit: $LASTEXITCODE"
   (Select-String -Path $env:TEMP\build.log -Pattern "error " | Measure-Object).Count    # 기대: 0
   (Select-String -Path $env:TEMP\build.log -Pattern "warning " | Measure-Object).Count  # 기대: Phase 34 baseline 과 동일
   ```

4. **UpdateImageSourceBadge 호출 count 검증 (자동/수동 일관성, D-34.1-15)**:
   ```bash
   grep -c "UpdateImageSourceBadge(" WPF_Example/UI/ContentItem/MainView.xaml.cs
   # 기대: 4 이상 (정의 1 + 자동 swap 1 + 수동 H 1 + 수동 V 1)
   ```

본 plan 의 SIMUL 동적 동작 검증 (배지 + ROI 가시성 실시간 토글) 은 **Plan 2 (Wave 2 UAT)** 가 사용자 SIMUL 로 수행. Plan 1 은 자동 정적 검증만 책임.
</verification>

<success_criteria>
- D-34.1-02/03/08/09/10/11/12/14 (수동 swap 토글 + 배지 + ROI 가시성 + Visibility + 라벨 분리 + Datum 노드 리셋) 코드 구현 완료
- D-34.1-15 의 "자동/수동 swap 의 3자 동시 전환" 이 단일 헬퍼 `UpdateImageSourceBadge(EImageSource)` 로 일원화
- D-34.1-07/D-34-14 가드 (DatumConfig.cs 변경 0) 가 사전 working-tree 정리 + Plan 1 의 본 작업으로 실제 충족
- D-34.1-13/17 (+정정 2026-05-27) (Cal_Image/DualImageTest/ 폴더 + 사용자 페어 배치 가이드 README + 정정문 reference) 완료
- msbuild Debug/x64 PASS, 신규 warning 0
- 변경 가드 4파일 (VisionResponsePacket / InspectionSequence / Action_FAIMeasurement / DatumConfig) git diff 0줄
</success_criteria>

<output>
After completion, create `.planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-01-SUMMARY.md` per get-shit-done summary template.

Summary 에 반드시 포함:
- D-34.1-* 결정 ID 별 구현 mapping (어느 파일/메서드에서 충족했는지)
- 변경 0 가드 4파일 git diff 결과 (PASS/FAIL)
- DatumConfig.cs working-tree 청결 복원 확인 (사전 git checkout 결과)
- msbuild Debug/x64 Rebuild 결과 (exit code + warning count delta)
- Cal_Image/DualImageTest/ 폴더 + README + .gitkeep 생성 확인
- D-34.1-17 정정문 (2026-05-27) reference 가 README 본문에 포함되었는지 확인
- Plan 2 UAT 진입 전 사용자 수동 단계 안내 (의사 페어 이미지 배치)
</output>
</output>
