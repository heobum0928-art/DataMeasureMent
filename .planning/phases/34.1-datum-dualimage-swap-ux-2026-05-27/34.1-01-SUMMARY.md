---
phase: 34.1-datum-dualimage-swap-ux-2026-05-27
plan: 01
subsystem: datum/ui
tags: [phase-34.1, datum, dualimage, swap-ux, ui-only, main-view, gap-closure]
requires:
  - phase-34 (VerticalTwoHorizontalDualImage enum + DatumConfig.TeachingImagePath_Vertical 필드)
provides:
  - EImageSource enum (Horizontal/Vertical) — 자동/수동 swap 공통 인자 타입
  - UpdateImageSourceBadge(EImageSource) 단일 헬퍼 — 3자 동시 갱신 (D-34.1-15)
  - MainView 캔버스 툴바 [👁 가로]/[👁 세로] 토글 버튼 2개
  - MainView 캔버스 우상단 이미지 출처 배지 (Border + TextBlock)
  - PublishDatumRoiCandidates VerticalTwoHorizontalDualImage 분기 (ROI subset 필터)
  - Cal_Image/DualImageTest/ 폴더 + 사용자 페어 배치 가이드 README
affects:
  - MainView Datum 티칭 워크플로우 (DualImage variant 만)
  - HalconViewer SetDatumRoiCandidates 호출 시 ROI 가시성 (가시성만 토글, 데이터 보존)
tech-stack:
  added: []
  patterns:
    - WPF ToggleButton IsChecked radio synchronization
    - Single helper for auto/manual swap convergence (D-34.1-15)
    - In-place ROI subset filtering via _currentImageSource field
key-files:
  created:
    - WPF_Example/Custom/Sequence/Inspection/EImageSource.cs
    - Cal_Image/DualImageTest/.gitkeep
    - Cal_Image/DualImageTest/README.md
  modified:
    - WPF_Example/DatumMeasurement.csproj
    - WPF_Example/UI/ContentItem/MainView.xaml
    - WPF_Example/UI/ContentItem/MainView.xaml.cs
decisions:
  - D-34.1-02/03/08/09/10/11/12/14: MainView XAML/code-behind 단일 위치에 토글 + 배지 + Visibility + ROI subset 일원화 (PropertyGrid 우회)
  - D-34.1-15: UpdateImageSourceBadge(EImageSource) 단일 헬퍼로 자동/수동 swap 3자 동시 전환 일원화
  - D-34.1-07: DatumConfig.cs working-tree 깨진 주석 1줄 git checkout 으로 HEAD 복원 → 가드 충족
  - D-34.1-13/17 (+정정 2026-05-27): Cal_Image/DualImageTest/ 폴더 + README + .gitkeep — 사용자 수동 배치 가이드
metrics:
  duration_min: 7
  duration_human: "7 minutes"
  completed_date: "2026-05-27T08:08:00Z"
  tasks_completed: 3
  files_created: 3
  files_modified: 3
  commits: 3
---

# Phase 34.1 Plan 01: Datum DualImage Swap UX 코드 구현 Summary

DualImage 변형용 캔버스 swap 토글 버튼 2개 + 우상단 배지 + 단일 헬퍼 `UpdateImageSourceBadge(EImageSource)` 로 자동/수동 swap 의 3자(배지 텍스트 + 배지 색상 + ROI 가시성) 동시 전환을 일원화 (D-34.1-15). 가드 4파일 변경 0 + msbuild Debug/x64 PASS + Cal_Image/DualImageTest/ 가이드 폴더 신설로 Plan 02 UAT 사전 준비 경로 확보.

## Deliverables

### 신규 (3 files)

1. **`WPF_Example/Custom/Sequence/Inspection/EImageSource.cs`** — `Horizontal=0` / `Vertical=1` enum. 자동/수동 swap 의 공통 인자 타입. DatumConfig 변경 0 가드(D-34.1-07) 유지 위해 별도 파일.
2. **`Cal_Image/DualImageTest/.gitkeep`** — 빈 placeholder.
3. **`Cal_Image/DualImageTest/README.md`** — SIMUL 의사 페어 (Top 가로 1장 + Side 세로 1장) 배치 가이드 + D-34.1-17 정정문 reference 포함.

### 수정 (3 files)

1. **`WPF_Example/DatumMeasurement.csproj`** — EImageSource.cs Compile ItemGroup 등록 (EDatumAlgorithm.cs 와 EdgeOptionLists.cs 사이).
2. **`WPF_Example/UI/ContentItem/MainView.xaml`** — `btn_swapHorizontal`, `btn_swapVertical`, `border_imageSourceBadge`, `txt_imageSourceBadge` 4개 컨트롤 추가.
3. **`WPF_Example/UI/ContentItem/MainView.xaml.cs`** — `_currentImageSource` 필드 + `UpdateImageSourceBadge(EImageSource)` 헬퍼 + `BtnSwapHorizontal_Click` / `BtnSwapVertical_Click` 핸들러 2개 + `PublishDatumRoiCandidates` 진입부 Visibility 동기화 + DualImage switch case + 자동 swap site (StartDatumTeachStep(Vertical)) 1줄 추가.

## D-34.1-* 결정 ID 별 구현 매핑

| 결정 ID | 구현 위치 (파일:메서드/구간) | 확인 방법 |
|--------|---------------------------|----------|
| D-34.1-02 (수동 swap 토글 버튼 2개) | MainView.xaml `btn_swapHorizontal` / `btn_swapVertical`; MainView.xaml.cs `BtnSwapHorizontal_Click` / `BtnSwapVertical_Click` | grep `BtnSwap*_Click` count=2 |
| D-34.1-03 (배지 우상단 + 색상 + 텍스트) | MainView.xaml `border_imageSourceBadge` (HorizontalAlignment=Right, VerticalAlignment=Top, Margin="0,12,12,0"); 기본 Background=#1976D2; FontSize=14 | grep `border_imageSourceBadge` + `#1976D2` + `Margin="0,12,12,0"` |
| D-34.1-07 (DatumConfig.cs 변경 0) | Task 1: `git checkout HEAD -- DatumConfig.cs` 복원 | `git diff HEAD -- DatumConfig.cs` → 빈 출력 |
| D-34.1-08 (Datum 노드 이동 시 가로축 리셋) | MainView.xaml.cs `PublishDatumRoiCandidates` 진입부 `if (_editingDatum != datum)` 분기 — `_currentImageSource = Horizontal` + 배지/IsChecked 직접 동기 (재귀 회피) | grep `if (_editingDatum != datum)` |
| D-34.1-09 (Visibility = DualImage 일 때만) | MainView.xaml.cs `PublishDatumRoiCandidates` 진입부 `isDualImage` bool + 3개 컨트롤 Visibility 일괄 갱신 | grep `isDualImage ? Visibility.Visible` count=3 |
| D-34.1-10 (ROI subset 가시성 토글) | MainView.xaml.cs `PublishDatumRoiCandidates` switch `case VerticalTwoHorizontalDualImage`: `_currentImageSource == Horizontal` → HA+HB / else → Vertical | grep `case EDatumAlgorithm.VerticalTwoHorizontalDualImage:` |
| D-34.1-11 (Wizard step 라벨 ↔ 배지 분리) | `StartDatumTeachStep(Vertical)` 의 기존 `label_drawHint.Content = "Step 3/3: 수직 ROI를 드래그하세요"` 라벨 변경 없음 — 배지 갱신은 1줄 추가 | label_drawHint 라인 회귀 0 (Phase 34 baseline 유지) |
| D-34.1-12 (Idle Datum 노드 선택 상태 자유 토글) | `BtnSwap*_Click` 핸들러는 `_editingDatum != null && AlgorithmTypeEnum == DualImage` 가드만 — wizard mode 비의존 | 핸들러 본문 grep 확인 |
| D-34.1-13 (Cal_Image/DualImageTest/) | Task 3: `Cal_Image/DualImageTest/.gitkeep` + `README.md` | `test -d` + `test -f` |
| D-34.1-14 (배지 색상/폰트/마진 잠금값) | MainView.xaml.cs `UpdateImageSourceBadge` — 가로축 = #1976D2 + "가로축" / 세로축 = #F57C00 + "세로축"; MainView.xaml `FontSize="14"` + `Margin="0,12,12,0"` | grep `#1976D2` + `#F57C00` |
| D-34.1-15 (자동/수동 3자 동시 전환 단일 헬퍼) | MainView.xaml.cs `UpdateImageSourceBadge(EImageSource)` 단일 헬퍼; 호출 site 4개 — 정의 1 + 자동 swap 1 + 수동 H 1 + 수동 V 1 | grep `UpdateImageSourceBadge(` count=4 |
| D-34.1-17 (정정 2026-05-27 reference) | Cal_Image/DualImageTest/README.md "정정 컨텍스트 (D-34.1-17 정정 2026-05-27)" 섹션 | grep `D-34.1-17 정정` |

## 검증 결과

### 1. 가드 4파일 변경 0 (D-34-13/14 + D-34.1-07)

```
$ git diff --stat HEAD -- \
    WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs \
    WPF_Example/TcpServer/VisionResponsePacket.cs \
    WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs \
    WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs

(빈 출력 — PASS)
```

### 2. DatumConfig.cs working-tree 청결 복원

Plan 시작 시점 — `git status --short` 에 `M WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 표시 (L30 부근 깨진 주석 1줄).

Task 1 첫 단계 — `git checkout HEAD -- WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 실행 → `git diff HEAD` 빈 출력 → PASS.

### 3. msbuild Debug/x64 Rebuild

```
MSBuild: C:\Program Files\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe
Exit: 0
Errors: 0
Warnings: 14
```

**Warnings delta:** 0 (Phase 33 baseline 과 동일).
- 14개 모두 기존 baseline: CS0618 deprecated TopSequence/BottomSequence/TopInspectionAction/BottomInspectionAction (Phase 33 마이그레이션 미완) × 2 (wpftmp + main csproj 동시 빌드) + CS0162 VirtualCamera.cs:266 unreachable code × 2 + MSB3884 MinimumRecommendedRules.ruleset 누락 × 2
- 본 plan 신규 warning **0** — MainView, EImageSource, DualImageTest 관련 warning 없음

### 4. UpdateImageSourceBadge( 호출 count

```
$ grep -c 'UpdateImageSourceBadge(' WPF_Example/UI/ContentItem/MainView.xaml.cs
4
```

기대값 ≥ 4 충족 — 정의 1 + 자동 swap 1 (`StartDatumTeachStep(Vertical)`) + 수동 H 1 (`BtnSwapHorizontal_Click`) + 수동 V 1 (`BtnSwapVertical_Click`).

### 5. Cal_Image/DualImageTest/ 생성 확인

| 파일 | 존재 | 키워드 |
|------|------|--------|
| `Cal_Image/DualImageTest/.gitkeep` | ✅ | (빈 파일) |
| `Cal_Image/DualImageTest/README.md` | ✅ | "Phase 34.1" / "VerticalTwoHorizontalDualImage" / "top_horizontal" / "side_vertical" / "D-34.1-17 정정" 모두 포함 |

## Commits

| Task | Commit | Subject |
|------|--------|---------|
| 1 | `789e364` | feat(34.1-01): EImageSource enum + csproj 등록 + DatumConfig.cs working-tree 정리 |
| 2 | `118e8b7` | feat(34.1-01): MainView Datum DualImage swap UX — 토글 버튼 + 배지 + 3자 동시 갱신 헬퍼 |
| 3 | `937f959` | docs(34.1-01): Cal_Image/DualImageTest/ — SIMUL 의사 페어 배치 가이드 |

각 commit 의 deletion check 결과 모두 0 — 의도하지 않은 파일 삭제 없음.

## Deviations from Plan

**None — plan 명세 verbatim 그대로 적용.**

소수 정합 결정 (deviation 아닌 기술적 보정):

1. **자동 swap site `UpdateImageSourceBadge(Vertical)` 호출 위치**: plan 명세는 "L2001 try-catch 직후" 인데, 자동 swap if 블록의 `if (!string.IsNullOrEmpty(vpath) && File.Exists(vpath))` 안 try-catch 직후에 배치. else 블록은 빈 경로 안내 + break 로 종료되므로 의미적으로 try-catch 안 (= 자동 swap 성공 경로 안) 이 정확 — plan 의도와 일치.
2. **csproj 등록 위치**: 알파벳 순서 기존 패턴(EDatumAlgorithm → EdgeOptionLists)에 맞춰 `EDatumAlgorithm.cs` 와 `EdgeOptionLists.cs` 사이에 삽입.

## Auth Gates

None — 본 plan 은 UI-only 변경 + 빌드 검증 + 로컬 파일 시스템 폴더 생성. 외부 인증 요구 0.

## Plan 02 UAT 진입 전 사용자 수동 단계 안내

Phase 34.1 Plan 02 (SIMUL UAT) 진입 직전에 다음 1단계 수동 작업 필요:

1. **`Cal_Image/DualImageTest/top_horizontal.{bmp|jpg}`** 배치 — 사용자 보유 SIMUL 이미지 풀에서 Top 가로 에지 명확한 이미지 1장 복사 + 명명.
2. **`Cal_Image/DualImageTest/side_vertical.{bmp|jpg}`** 배치 — Side 세로 에지 명확한 이미지 1장 복사 + 명명.

배치 완료 시 README 의 "UAT 진입 경로 (Plan 2)" 7-step 사용자 SIMUL UAT 가능.

> **D-34.1-17 정정 (2026-05-27)**: 당초 잠금 = "executor 결정적 복사 → 사용자 부담 0" 이었으나 Cal_Image/Top/Side 폴더가 워크트리에 부재 + git-tracked 후보 working-tree deleted → 본 plan = 폴더 + 가이드 README 만 생성, 실제 배치는 Plan 02 사전 단계 사용자 책임. CONTEXT.md L59 및 본 README "정정 컨텍스트" 섹션에 정정문 명문화 — 추적성 확보.

## Self-Check: PASSED

- ✅ `WPF_Example/Custom/Sequence/Inspection/EImageSource.cs` 존재
- ✅ `WPF_Example/DatumMeasurement.csproj` 에 EImageSource.cs Compile 라인 존재
- ✅ `WPF_Example/UI/ContentItem/MainView.xaml` 에 4개 신규 컨트롤 모두 존재
- ✅ `WPF_Example/UI/ContentItem/MainView.xaml.cs` 에 UpdateImageSourceBadge / BtnSwap*_Click / _currentImageSource 모두 존재
- ✅ `Cal_Image/DualImageTest/.gitkeep` + `README.md` 존재
- ✅ Commits `789e364` / `118e8b7` / `937f959` 모두 `git log` 에 존재
- ✅ 가드 4파일 git diff HEAD 빈 출력
- ✅ msbuild Debug/x64 PASS — Exit=0 / Errors=0 / Warnings=14 (baseline 동일, 신규 0)
- ✅ UpdateImageSourceBadge( count = 4 (helper 정의 1 + 자동 1 + 수동 2)
