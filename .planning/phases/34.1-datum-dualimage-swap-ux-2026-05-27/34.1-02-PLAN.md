---
phase: 34.1-datum-dualimage-swap-ux-2026-05-27
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - .planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-UAT.md   # 신규
autonomous: false
requirements_addressed:
  - D-34.1-01
  - D-34.1-04
  - D-34.1-05
  - D-34.1-06
  - D-34.1-15
  - D-34.1-16
tags: [phase-34.1, uat, sign-off, simul, dualimage]

must_haves:
  truths:
    - "Plan 01 의 모든 변경이 사용자 SIMUL 환경에서 7 Test 모두 통과한다 (또는 사용자 합의로 carry-over 분리)"
    - "Phase 34 의 CO-34-02 (Test 3-c/3-e/3-f) + CO-34-03 (Test 2/4) + CO-34-04 (Phase 35 Test 4 Side 연장) 가 본 UAT 에서 일괄 흡수 종결된다 (D-34.1-06)"
    - "UAT Test 6 (수동 swap 동기화) 합격 기준 = 토글 시 (a) 배지 텍스트 + (b) 배지 색상 + (c) ROI 가시성 3자 동시 전환 + 시각적 지연 없음 (D-34.1-15)"
    - "UAT Test 7 (CO-34-04 Side 종결) = SIMUL 의사 페어 (Cal_Image/DualImageTest/) 로 Datum 결합 PASS 하면 Phase 34.1 종결 (실측은 CO-34.1-01 carry-over 분리, D-34.1-16)"
    - "Phase 34 의 D-34-13/14 가드 4파일 (VisionResponsePacket / InspectionSequence / Action_FAIMeasurement / DatumConfig) 이 Phase 34.1 변경 후에도 0 변경 유지된다"
    - "UAT.md status=signed_off (또는 partial + 명시적 carry-over) + 사용자 'approved' 명시"
  artifacts:
    - path: ".planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-UAT.md"
      provides: "7 Test 결과 + 사용자 sign-off 기록"
      contains: "status: signed_off"
  key_links:
    - from: "사용자 SIMUL 실행"
      to: "Plan 01 의 코드 변경"
      via: "DatumMeasurement.exe 실행 → Datum 노드 + DualImage algorithm 선택 → 토글 + Test Find"
      pattern: "수동"
    - from: "34.1-UAT.md 의 Test 5 (가드 검증)"
      to: "git diff 결과"
      via: "자동 git diff --stat 명령 결과 첨부 (PLAN_01_BASE env var 또는 git tag 기반, W4 보정)"
      pattern: "git diff --stat"
---

<objective>
Plan 01 (수동 swap 토글 + 배지 + ROI 가시성 + Cal_Image 폴더) 의 SIMUL UAT 를 사용자와 함께 수행하고 Phase 34.1 sign-off 완료.

**Purpose**:
- Phase 34 partial sign-off (2026-05-27) 의 4 carry-over (CO-34-01~04) 종결.
- Phase 35 Test 4 Side 실측 연장 (CO-34-04) 의 SIMUL 의사 페어 경유 종결 (D-34.1-16).
- v1.1 milestone 잔여 phase (24 / 25 / 27) 진입 unblock.

**Output**:
- `.planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-UAT.md` (status=signed_off 또는 partial + 명시적 carry-over)
- 사용자가 7 Test 각각에 대해 PASS / FAIL / NOT-TESTED 보고 + Test 6 (D-34.1-15 3자 동시 전환) / Test 7 (D-34.1-16 SIMUL 의사 페어) 명시적 sign-off

**Anti-goals**:
- 실측 Side fixture 페어 검증은 본 plan 범위 외 (CO-34.1-01 carry-over 로 분리, 장비 도착 후 v1.1 다음 회주 또는 Phase 27 안에서 종결).
- 본 plan 안에서 코드 변경 0 — UAT 도중 결함 발견 시 별도 hotfix task 또는 quick task 로 분기.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-CONTEXT.md
@.planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-01-PLAN.md
@.planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-01-SUMMARY.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-UAT.md
@.planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 34.1-UAT.md scaffold 작성 (7 Test 정의 + pending 상태)</name>
  <files>
    .planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-UAT.md
  </files>
  <read_first>
    - .planning/phases/34-datum-verticaltwohorizontal-2026-05-26/34-UAT.md (template + 5 Test 패턴 확인)
    - .planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-CONTEXT.md (D-34.1-06/15/16 UAT 범위 잠금값)
    - .planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-01-SUMMARY.md (Plan 1 결과 — 어느 파일 어느 메서드 변경되었는지 인용)
  </read_first>
  <action>
    `34.1-UAT.md` 파일 신규 작성. 한국어, Phase 34 UAT 의 markdown 패턴 차용.

    **W3/W4 노트 (plan-checker 2026-05-27):** Test 1 의 msbuild 명령은 PowerShell + vswhere fallback 패턴 (verbatim VS2019 경로 회피). Test 5 의 git diff 명령은 `HEAD~1` 가정 회피 — env var (`PLAN_01_BASE`) 또는 git tag (`phase-34.1-pre-exec`) 기반의 2-option 안내로 작성.

    Frontmatter:
    ```yaml
    ---
    phase: 34.1-datum-dualimage-swap-ux-2026-05-27
    type: uat
    status: pending          # Task 2 (사용자 검증) 결과에 따라 signed_off / partial 로 갱신
    created: 2026-05-27
    parent_phase: 34
    carry_over_absorbed: [CO-34-01, CO-34-02, CO-34-03, CO-34-04]
    ---
    ```

    본문 구조 (7 Test):

    ## Test 1: msbuild Debug/x64 Rebuild (자동)
    - **목적**: Plan 01 변경 후 빌드 무결성 확인.
    - **명령 (PowerShell — 환경 비의존, vswhere fallback 포함)**:
      ```powershell
      cd "C:\Info\Project\DataMeasurement"
      $msbuild = (Get-Command msbuild.exe -ErrorAction SilentlyContinue).Source
      if (-not $msbuild) {
          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          if (Test-Path $vswhere) {
              $msbuild = & $vswhere -latest -requires Microsoft.Component.MSBuild `
                          -find "MSBuild\**\Bin\MSBuild.exe" | Select-Object -First 1
          }
      }
      if (-not $msbuild) {
          throw "MSBuild not found. msbuild.exe 를 PATH 에 추가하거나 VS Build Tools 설치 필요."
      }
      & $msbuild WPF_Example\DatumMeasurement.csproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /nologo /v:minimal `
          2>&1 | Tee-Object -FilePath $env:TEMP\build.log
      Write-Host "Exit: $LASTEXITCODE"
      (Select-String -Path $env:TEMP\build.log -Pattern "error " | Measure-Object).Count
      (Select-String -Path $env:TEMP\build.log -Pattern "warning " | Measure-Object).Count
      ```
    - **명령 (Bash 대체 — msbuild 가 PATH 에 있을 때)**:
      ```bash
      cd "C:/Info/Project/DataMeasurement"
      MSBUILD="${MSBUILD_PATH:-$(where msbuild.exe 2>/dev/null | head -1)}"
      [ -z "$MSBUILD" ] && { echo "MSBuild not found"; exit 1; }
      "$MSBUILD" WPF_Example/DatumMeasurement.csproj //t:Rebuild //p:Configuration=Debug //p:Platform=x64 //nologo //v:minimal 2>&1 | tee /tmp/build.log
      echo "Exit: $?"; grep -c "error " /tmp/build.log; grep -c "warning " /tmp/build.log
      ```
    - **PASS 기준**: exit code = 0, errors = 0, 신규 warning = 0 (Phase 34 baseline 대비 delta 0).
    - **결과**: PENDING

    ## Test 2: 기존 1-image 3 algorithm 회귀 0 (사용자 SIMUL, CO-34-03 흡수)
    - **목적**: TwoLineIntersect / CircleTwoHorizontal / VerticalTwoHorizontal (기존 3 algorithm) 가 Phase 34.1 변경 후에도 정상 동작.
    - **단계**:
      1. 앱 실행 → InspectionListView 에서 Datum 노드 1개 선택.
      2. AlgorithmType = TwoLineIntersect 로 변경 → 캔버스 툴바에 `[👁 가로] [👁 세로]` 토글 버튼 + 우상단 배지 모두 **Collapsed** 확인 (D-34.1-09).
      3. Teach Datum → Line1, Line2 ROI 그리기 → Test Find PASS 확인.
      4. AlgorithmType = CircleTwoHorizontal 로 변경 → 토글/배지 Collapsed 확인.
      5. AlgorithmType = VerticalTwoHorizontal 로 변경 → 토글/배지 Collapsed 확인 + 기존 Vertical/HA/HB step 라벨 1/3/2/3/3/3 유지 확인.
    - **PASS 기준**: 3 algorithm 모두 토글/배지 비노출 + 기존 wizard step 라벨 회귀 0.
    - **결과**: PENDING

    ## Test 3: DualImage SIMUL UAT (사용자 SIMUL, CO-34-02 흡수 — 3-a~3-f 전체)
    - **목적**: VerticalTwoHorizontalDualImage 의 자동/수동 swap + 배지 + ROI 가시성 검증.
    - **사전 준비**: `Cal_Image/DualImageTest/README.md` 따라 의사 페어 이미지 2장 배치 (top_horizontal.{ext} + side_vertical.{ext}).
    - **단계**:
      - **3-a**: Datum 노드 선택 → AlgorithmType = VerticalTwoHorizontalDualImage → 토글 버튼 2개 + 배지 모두 **Visible** 확인 + 배지 기본 = `가로축` + `#1976D2` (Blue 700) 확인.
      - **3-b**: PropertyGrid 의 TeachingImagePath / TeachingImagePath_Vertical 에 절대 경로 입력 → `[👁 가로]` 토글 → 캔버스에 가로 이미지 표시 + 배지 = 가로축/파랑 + ROI 영역 비어 있음 (티칭 전).
      - **3-c** (CO-34-02): Teach Datum 시작 → HorizontalA → HorizontalB → 그리기 완료. 배지 = 가로축 (사용자 wizard 상태) 확인. 그 다음 자동 swap 트리거 (Vertical step 진입) 시 캔버스가 세로축 이미지로 자동 전환 + 배지 = 세로축/주황 (`#F57C00`) 자동 갱신 확인 (D-34.1-15 자동 분기).
      - **3-d** (CO-34-01 핵심 회복): 자동 swap 후 사용자가 `[👁 가로]` 토글 클릭 → 가로 이미지 + HA/HB ROI 다시 표시 + 배지 = 가로축/파랑. `[👁 세로]` 토글 → 세로 이미지 + Vertical ROI 표시 + 배지 = 세로축/주황. **수동 swap 가능 확인** (Phase 34 의 swap UX 갭 해소).
      - **3-e** (CO-34-02): Vertical ROI 그리기 → Test Find → Datum 결합 PASS 확인. 자동 LastTeachSucceeded → RefOrigin/Angle 라벨 갱신 확인.
      - **3-f** (CO-34-02): Datum 노드를 다른 노드로 옮긴 후 다시 돌아오면 swap 상태 = **가로축 기본 리셋** 확인 (D-34.1-08, INI 미저장).
    - **PASS 기준**: 3-a ~ 3-f 모두 PASS.
    - **결과**: PENDING

    ## Test 4: INI 라운드트립 (사용자 SIMUL, CO-34-03 흡수)
    - **목적**: DualImage 설정 (TeachingImagePath + TeachingImagePath_Vertical) INI Save/Load 회귀 0.
    - **단계**:
      1. Test 3 환경에서 INI Save → 텍스트 에디터로 INI 열기 → TeachingImagePath / TeachingImagePath_Vertical 키 모두 존재 확인.
      2. 앱 재시작 → 동일 recipe Load → AlgorithmType 자동 복원 + 두 경로 모두 복원 확인.
      3. Teach Datum → Test Find PASS 확인 (Load 후 동작 동등성).
    - **PASS 기준**: INI 키 존재 + Load 후 두 경로 모두 복원 + Test Find PASS.
    - **결과**: PENDING

    ## Test 5: D-34-13/14 + D-34.1-07 가드 (자동 git diff — env var 또는 tag 기반, W4 보정)
    - **목적**: Phase 34.1 변경이 4 가드 파일을 건드리지 않았음을 자동 검증.
    - **사전 준비 (둘 중 하나 선택)**:
      - **옵션 A (env var, 권장)** — Plan 01 첫 commit 직전 hash 를 `PLAN_01_BASE` 환경변수로 export:
        ```bash
        # git log 로 Plan 01 첫 commit 식별 후 그 직전 hash 를 잡기 (예: "feat(34.1-01):" 또는 첫 EImageSource.cs touch commit)
        git log --oneline -20  # Plan 01 첫 commit 식별
        # 예: 첫 commit hash 가 abc1234 면, 그 직전 = abc1234^
        export PLAN_01_BASE=abc1234^   # bash
        # 또는 PowerShell: $env:PLAN_01_BASE = "abc1234^"
        ```
      - **옵션 B (git tag)** — Plan 01 시작 전에 사용자가 미리 tag 를 찍어 두는 방식 (사전 합의 필요):
        ```bash
        # Plan 01 시작 직전 (1회만):
        git tag phase-34.1-pre-exec
        # 본 Test 5 시:
        # PLAN_01_BASE=phase-34.1-pre-exec  (옵션 A 와 동일하게 변수에 담음)
        ```
    - **검증 명령**:
      ```bash
      cd "C:/Info/Project/DataMeasurement" && \
      git diff --stat "${PLAN_01_BASE}"..HEAD -- \
        WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs \
        WPF_Example/TcpServer/VisionResponsePacket.cs \
        WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs \
        WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
      ```
      또는 PowerShell:
      ```powershell
      git diff --stat "$env:PLAN_01_BASE..HEAD" -- `
        WPF_Example\Custom\Sequence\Inspection\DatumConfig.cs `
        WPF_Example\TcpServer\VisionResponsePacket.cs `
        WPF_Example\Custom\Sequence\Inspection\InspectionSequence.cs `
        WPF_Example\Custom\Sequence\Inspection\Action_FAIMeasurement.cs
      ```
    - **PASS 기준**: 출력 빈 문자열 (4파일 모두 변경 0). DatumConfig 의 Phase 34 D-34-04/05/11/12 변경 (TeachingImagePath_Vertical 필드 + ICustomTypeDescriptor 분기) 은 Phase 34 commit 이미 포함되어 있으므로 Phase 34.1 단독 변경 0 만 검증.
    - **W4 노트**: 기존 `HEAD~1` 가정은 Plan 01 이 multi-commit 으로 종결되거나 amend/rebase 시 잘못된 base 가리킴 → env var 또는 tag 기반 base 명시로 대체. 사용자는 환경에 따라 옵션 A/B 선택.
    - **결과**: PENDING

    ## Test 6: 수동 swap 동기화 — 3자 동시 전환 (사용자 SIMUL, D-34.1-15 PASS 기준)
    - **목적**: 수동 토글 시 (a) 배지 텍스트 + (b) 배지 색상 + (c) 캔버스 ROI 가시성 3자 동시 전환 + 시각적 지연 없음.
    - **단계**:
      1. Test 3 환경에서 `[👁 가로]` 클릭 → **즉시** (배지 텍스트 "가로축" + 배지 색상 Blue 700 + 캔버스에 HorizontalA + HorizontalB ROI 만 표시) 3자 동시 확인.
      2. `[👁 세로]` 클릭 → **즉시** (배지 텍스트 "세로축" + 배지 색상 Orange 800 + Vertical ROI 만 표시) 3자 동시 확인.
      3. 반복 5회 — 3자 비동기 (예: 배지는 변경되었는데 ROI 는 가로 그대로) 발생 0회 확인.
    - **PASS 기준**: 3자 동시 전환 + 시각적 지연 없음.
    - **결과**: PENDING

    ## Test 7: SIMUL 의사 페어로 Datum 결합 PASS (사용자 SIMUL, D-34.1-16 PASS 기준, CO-34-04 종결)
    - **목적**: SIMUL 의사 페어 (Cal_Image/DualImageTest/ 안 top_horizontal + side_vertical) 로 DualImage Datum 결합 워크플로우 PASS.
    - **단계**:
      1. PropertyGrid 의 TeachingImagePath / TeachingImagePath_Vertical 에 본 폴더 경로 입력.
      2. Teach Datum → HA → HB → 자동 swap → Vertical → 완료.
      3. Test Find → 가로축 2 라인 + 세로축 1 라인 결합 → Datum origin + angle 산출 PASS.
      4. (선택) FAI 노드 추가 후 측정 1회 수행 → 측정값 표시 확인 (DualImage Datum 기준 거리 측정 회귀 0).
    - **PASS 기준**: Test Find PASS + (선택) FAI 측정 PASS.
    - **실측 Side fixture 페어 검증은 본 Test 범위 외**: 장비 도착 후 CO-34.1-01 (별도 carry-over) 로 처리.
    - **결과**: PENDING

    ## Summary
    - Total: 7
    - Passed: 0 / Failed: 0 / Pending: 7
    - Sign-off date: TBD

    ## Carry-over (Phase 34.1 → v1.1 next)
    - **CO-34.1-01**: Side fixture 실측 이미지 페어 (가로 + 세로) 확보 후 DualImage Datum 결합 실측 검증 → 장비 도착 후 v1.1 다음 회주 또는 Phase 27 안에서 종결.
  </action>
  <verify>
    <automated>
      cd "C:/Info/Project/DataMeasurement" && \
      test -f .planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-UAT.md && echo "UAT scaffold ok" && \
      grep -q 'status: pending' .planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-UAT.md && echo "pending status ok" && \
      grep -q 'Test 1:' .planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-UAT.md && \
      grep -q 'Test 7:' .planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-UAT.md && echo "7 tests defined" && \
      grep -q 'CO-34.1-01' .planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-UAT.md && echo "carry-over listed" && \
      grep -q 'PLAN_01_BASE' .planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-UAT.md && echo "W4 env var pattern present" && \
      grep -q 'vswhere' .planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-UAT.md && echo "W3 vswhere fallback present"
    </automated>
  </verify>
  <acceptance_criteria>
    - 34.1-UAT.md 파일 생성 + frontmatter `status: pending` + `carry_over_absorbed: [CO-34-01..04]`
    - 7 Test 모두 본문에 정의됨 (Test 1 자동 / Test 2~4 + 6~7 사용자 SIMUL / Test 5 자동 git diff)
    - 각 Test 에 PASS 기준 + 단계 + 결과(PENDING) 필드
    - CO-34.1-01 (Side fixture 실측) carry-over 명시
    - **W3 보정**: Test 1 의 msbuild 명령이 verbatim VS2019 경로가 아닌 PowerShell + vswhere fallback (또는 Bash `where msbuild`) 패턴으로 작성됨
    - **W4 보정**: Test 5 의 git diff 명령이 `HEAD~1` 가정 대신 `PLAN_01_BASE` env var 또는 git tag (`phase-34.1-pre-exec`) 기반의 2-option 안내로 작성됨
  </acceptance_criteria>
  <done>UAT 시나리오 문서 scaffold 작성 완료. W3/W4 plan-checker 보정 반영. Task 2 (사용자 검증) 진입 준비 완료.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 사용자 SIMUL UAT 7 Test 수행 + 결과 보고 + 34.1-UAT.md 갱신 + sign-off</name>
  <what-built>
    Plan 01 의 변경:
    - MainView 캔버스 툴바에 `[👁 가로] [👁 세로]` 토글 버튼 2개 추가 (DualImage algorithm 일 때만 Visible).
    - MainView 캔버스 우상단에 현재 표시 이미지 배지 (가로축 = Blue700 / 세로축 = Orange800, 14px, 마진 12px) 추가.
    - 자동/수동 swap 의 공통 헬퍼 `UpdateImageSourceBadge(EImageSource)` — 3자 동시 갱신 (배지 텍스트 + 색상 + ROI 가시성).
    - Datum 노드 이동 시 swap 상태 = 가로축 기본 리셋 (D-34.1-08).
    - `PublishDatumRoiCandidates` 에 DualImage 분기 추가 — 가로축 표시 시 HA+HB ROI 만, 세로축 표시 시 Vertical ROI 만 publish (D-34.1-10).
    - 기존 자동 swap site (L1994~) refactor — `UpdateImageSourceBadge(EImageSource.Vertical)` 호출 추가.
    - Cal_Image/DualImageTest/ 신규 폴더 + README + .gitkeep.
    - DatumConfig.cs working-tree 깨진 주석 1줄 git checkout 으로 HEAD 복원.

    변경 가드 4파일 (DatumConfig / VisionResponsePacket / InspectionSequence / Action_FAIMeasurement) 0 변경 유지.
  </what-built>
  <how-to-verify>
    **0. 사전 준비 (5분):**
    - `Cal_Image/DualImageTest/README.md` 열람.
    - README 가이드 따라 `Cal_Image/DualImageTest/top_horizontal.{bmp|jpg}` (Top 카메라 가로 에지 보이는 이미지 1장) + `Cal_Image/DualImageTest/side_vertical.{bmp|jpg}` (Side 카메라 세로 에지 보이는 이미지 1장) 수동 복사.
    - Cal_Image/ 가 비어 있어 출처 후보가 부족하면 Wafer/LEFT_1227 등 git history 에서 복원 또는 사용자 보유 SIMUL 이미지 풀에서 임의 2장 선택.
    - **W4 사전 준비 (Test 5 가드 검증용)**: Plan 01 첫 commit 직전 hash 를 `PLAN_01_BASE` env var 로 export.
      ```powershell
      # PowerShell — git log 로 Plan 01 첫 commit 식별 후 그 직전 hash 를 $env:PLAN_01_BASE 에 저장
      cd "C:\Info\Project\DataMeasurement"
      git log --oneline -20  # Plan 01 첫 commit hash 확인 (예: abc1234)
      $env:PLAN_01_BASE = "abc1234^"   # ^ 는 직전 commit. 또는 git tag 옵션 B 시 = "phase-34.1-pre-exec"
      ```
      ```bash
      # Bash 대체
      cd "C:/Info/Project/DataMeasurement"
      git log --oneline -20
      export PLAN_01_BASE=abc1234^
      ```

    **1. Test 1 (msbuild) 실행 — 자동 (W3 보정, PowerShell + vswhere fallback):**
    ```powershell
    cd "C:\Info\Project\DataMeasurement"
    $msbuild = (Get-Command msbuild.exe -ErrorAction SilentlyContinue).Source
    if (-not $msbuild) {
        $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
        if (Test-Path $vswhere) {
            $msbuild = & $vswhere -latest -requires Microsoft.Component.MSBuild `
                        -find "MSBuild\**\Bin\MSBuild.exe" | Select-Object -First 1
        }
    }
    if (-not $msbuild) { throw "MSBuild not found. PATH 또는 VS Build Tools 설치 필요." }
    & $msbuild WPF_Example\DatumMeasurement.csproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /nologo /v:minimal `
        2>&1 | Tee-Object -FilePath $env:TEMP\build.log
    Write-Host "Exit: $LASTEXITCODE"
    (Select-String -Path $env:TEMP\build.log -Pattern "error " | Measure-Object).Count
    (Select-String -Path $env:TEMP\build.log -Pattern "warning " | Measure-Object).Count
    ```
    또는 Bash 대체 (msbuild 가 PATH 에 있을 때):
    ```bash
    cd "C:/Info/Project/DataMeasurement"
    MSBUILD="${MSBUILD_PATH:-$(where msbuild.exe 2>/dev/null | head -1)}"
    [ -z "$MSBUILD" ] && { echo "MSBuild not found"; exit 1; }
    "$MSBUILD" WPF_Example/DatumMeasurement.csproj //t:Rebuild //p:Configuration=Debug //p:Platform=x64 //nologo //v:minimal 2>&1 | tee /tmp/build.log
    echo "Exit: $?"; grep -c "error " /tmp/build.log; grep -c "warning " /tmp/build.log
    ```
    → 결과를 34.1-UAT.md Test 1 결과 필드에 PASS / FAIL 로 기록.

    **2. Test 5 (가드) 실행 — 자동 (W4 보정, PLAN_01_BASE 또는 git tag 기반):**
    ```bash
    cd "C:/Info/Project/DataMeasurement"
    # PLAN_01_BASE 이 export 안 되어 있으면 0번 사전 준비 단계로 돌아감
    [ -z "$PLAN_01_BASE" ] && { echo "PLAN_01_BASE not set — go back to step 0"; exit 1; }
    git diff --stat "${PLAN_01_BASE}"..HEAD -- \
      WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs \
      WPF_Example/TcpServer/VisionResponsePacket.cs \
      WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs \
      WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
    ```
    또는 PowerShell:
    ```powershell
    if (-not $env:PLAN_01_BASE) { throw "PLAN_01_BASE not set — go back to step 0" }
    git diff --stat "$env:PLAN_01_BASE..HEAD" -- `
      WPF_Example\Custom\Sequence\Inspection\DatumConfig.cs `
      WPF_Example\TcpServer\VisionResponsePacket.cs `
      WPF_Example\Custom\Sequence\Inspection\InspectionSequence.cs `
      WPF_Example\Custom\Sequence\Inspection\Action_FAIMeasurement.cs
    ```
    → 출력이 빈 문자열이면 PASS, 한 줄이라도 있으면 FAIL.

    **3. Test 2/3/4/6/7 — 사용자 SIMUL 수동:**
    - `DatumMeasurement.exe` (Debug/x64) 실행.
    - 34.1-UAT.md 본문의 각 Test 단계 그대로 따라 수행.
    - 각 Test 별로 PASS / FAIL / NOT-TESTED 와 (FAIL 시) 사유 1~2문장 작성.

    **4. 34.1-UAT.md 갱신:**
    - 각 Test 결과 필드 (PENDING → PASS / FAIL / NOT-TESTED) 갱신.
    - Summary 의 Passed/Failed/Pending 카운트 갱신.
    - frontmatter `status` :
      - 7 Test 모두 PASS → `signed_off`
      - 일부 FAIL/NOT-TESTED → `partial` + 본문 Carry-over 섹션에 명시.
    - Sign-off date 갱신.
    - 사용자 "approved" 명시 (각 Test PASS 분기 옆 또는 본문 말미).

    **5. 결과 보고:**
    채팅으로 다음 형식 응답:
    ```
    Phase 34.1 UAT 결과:
    Test 1 (msbuild): PASS/FAIL
    Test 2 (1-image 회귀): PASS/FAIL/NOT-TESTED
    Test 3 (DualImage 3-a~3-f): PASS/PARTIAL/FAIL
    Test 4 (INI 라운드트립): PASS/FAIL/NOT-TESTED
    Test 5 (가드): PASS/FAIL
    Test 6 (3자 동시 전환): PASS/FAIL
    Test 7 (SIMUL 의사 페어): PASS/FAIL/NOT-TESTED
    status: signed_off / partial
    carry-over: (있으면 명시)
    approved: 예/아니오
    ```
  </how-to-verify>
  <resume-signal>
    "approved" (모든 Test PASS, signed_off) 또는
    "partial — Test X/Y/Z FAIL, carry-over=..." (일부 FAIL, partial + 명시적 carry-over) 또는
    "blocked — <상세>" (실행 자체 막힘 — 별도 hotfix 필요)
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 ↔ DatumMeasurement.exe (Debug/x64 SIMUL) | 로컬 운영자 마우스/키보드. 신뢰 전제. |
| 사용자 ↔ Cal_Image/DualImageTest/ 페어 이미지 | 사용자가 직접 배치. 로컬 디스크. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34.1-04 | Tampering | UAT 결과 기록 | accept | 사용자 직접 작성 + git commit 으로 보존. 외부 attacker surface 없음 (로컬 .md 파일). |
| T-34.1-05 | Repudiation | sign-off | mitigate | 34.1-UAT.md 에 frontmatter `status` + sign-off date + 사용자 "approved" 명시 + git commit hash 로 영구 기록. Phase 34 / 35 UAT 와 동일 패턴. |

**Note**: 본 plan 은 UAT 실행 + 문서 작성. 신규 attack surface 0.
</threat_model>

<verification>
- 34.1-UAT.md 파일 존재 + frontmatter 유효 + 7 Test 결과 모두 채워짐.
- 사용자가 "approved" / "partial — ..." / "blocked — ..." 중 하나로 명시적 응답.
- partial 의 경우 carry-over 가 본문 Carry-over 섹션 + 회고록 메모 (memory) 에 명시.
</verification>

<success_criteria>
- 34.1-UAT.md status = signed_off (또는 partial + 명시적 carry-over + 사용자 합의)
- 모든 Test 결과 PENDING → PASS/FAIL/NOT-TESTED 로 갱신
- D-34.1-16 의 SIMUL 의사 페어 PASS → CO-34-04 (Phase 35 Test 4 Side) 종결
- Phase 34.1 ROADMAP 표 `[ ]` → `[x]` 갱신 (별도 Task 또는 본 sign-off 의 사후 처리)
</success_criteria>

<output>
After completion, create `.planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-02-SUMMARY.md` 에:
- UAT 7 Test 결과 표
- 사용자 sign-off 명시 (date + "approved")
- 4 흡수 carry-over (CO-34-01~04) 종결 상태
- 신규 발생 carry-over (CO-34.1-01 — Side 실측) 명시
- ROADMAP / STATE 갱신 안내 (Phase 34.1 signed_off + Phase 35 Test 4 carry-over 종결 표시)
- memory 갱신 안내 (`project_phase34_1_progress.md` 신설/갱신)
</output>
