# Phase 34.1: Datum DualImage Swap UX — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in 34.1-CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-27
**Phase:** 34.1-datum-dualimage-swap-ux-2026-05-27
**Areas discussed:** A. 회귀 게이트 분기 정책, B. PropertyTools [👁] 아이콘 구현 메커니즘, C. 수동 swap 버튼 개수 + ROI/wizard 상태 보존, D. 배지 디자인 + UAT 6/7 합격 기준 + 테스트 이미지 페어

---

## Pre-context

시드 CONTEXT.md (D-34.1-01~07) 이 Phase 34 partial sign-off (2026-05-27) 시점에 이미 작성되어 있었음. Discuss-phase 는 시드 결정 **유지** + 남은 gray area 보강 모드로 진행.

User mode selection (Q: 어떻게 진행하시겠습니까?): **보강 추가 (Update)**.
User multi-select (Q: 어떤 gray area 를 논의?): **A, B, C, D 전체 선택**.

---

## Area A — 회귀 게이트 분기 정책 (D-34.1-07)

### Q A-1: 게이트 통과 기준은?

| Option | Description | Selected |
|--------|-------------|----------|
| Phase 34 UAT 시나리오 1:1 재현에서 시각적 구분 불가 = 게이트 통과 | 사용자 피드백 원인이 자동 swap 후 이미지 소스 분간 점 자체임을 확인. 재현=Plan 1 진행. | ✓ |
| ROI 그리기 신뢰성 = V step 에서 어느 이미지 위에 그릴지 실수할 용이성 판정 | 소스 배지는 있으나 자동 swap 이후 임의 되돌림 불가하면 게이트 FAIL = Plan 1 진행. | |
| 재현이 안 되어도 사용자의 2026-05-27 UAT 피드백을 1차 증거로 점수 | 게이트는 No-Go 가 아니라 회고록 역할 — 재현 여부 무관 진행. | |

**User's choice:** 첫 번째 (직접 답변).

### Q A-2: 게이트 재현 결과 vs Plan 분기 (사용자 클러리피케이션 후 잠금)

| Option | Description | Selected |
|--------|-------------|----------|
| Plan 1 = 수동 swap [👁] + 배지 라벨 둘 다 진행, 재현 여부 무관, 게이트 = 회고록 | UX 원칙 (자동 swap 의 임의 복귀/되돌림 필요성) 상 Plan 1 우선. | ✓ |
| Plan 1 을 수동 swap 만 (배지는 skip), 배지는 Phase 24 이후 | 일부분 보류 — 사용자 결정 명확하므로 채택 안 함. | |
| 수동 swap + 배지 분리 진행 — 두 task | 의존성 때문에 단일 Plan 안에 함께 진행이 효율적 — 채택 안 함. | |

**User's choice:** 첫 번째 (재차 확인 후 잠금).

### Q A-3: 게이트 실행 주체/시점

| Option | Description | Selected |
|--------|-------------|----------|
| Plan 실행자 (gsd-executor) 가 Plan 1 첫 task 로 자동 수행 | 게이트 작업이 Plan 안 task. | |
| Plan 완료 후 UAT 단계에서 사용자가 SIMUL 도중 확인 | UAT Test 0 로 추가. | |
| 지금 discuss 단계에서 결론 박고 진입 — 별도 게이트 task 없음, D-34.1-07 명문화에만 의미 | git status 청결 이미 확인됨, 별도 실행 불필요. | ✓ |

**User's choice:** 세 번째 (잠금 확정).

**Notes:** 사용자가 "재현 수행하여 Plan 파계. 게이트는 D-34.1-07 명문화에만 의미" 라고 명시. → D-34.1-07 보강 = 게이트 = 회고록.

---

## Area B — PropertyTools [👁] 아이콘 구현 메커니즘

### Pre-discussion finding
- DatumConfig.cs 의 `TeachingImagePath` 는 `[Category("Datum|ImageSource")]` 만 사용. `[DirectoryPath]` 미사용.
- 파일 선택은 `IOfflineImageParam` 인터페이스 + MainView 외부의 별도 "Load Image" 버튼 (MainView.xaml.cs:500, L2314) 담당.
- 시드 CONTEXT 의 "DirectoryPath 다이얼로그 트리거와 충돌 회피" 묘사는 부정확 — 실제로는 PropertyGrid 외부의 Load 버튼 구조.
- PropertyTools 3.1.0 (2020 release) 의 attribute 한계: "필드 우측 임의 아이콘 버튼" 직접 지원 안 됨.

### Q B-1: 수동 swap 트리거 위치 / 구현 접근

| Option | Description | Selected |
|--------|-------------|----------|
| (b) MainView 외부 UI — Load Image 버튼 영역 근처 [👁 가로] [👁 세로] 토글 2개 (권장) | PropertyTools 한계 회피, 자연스러운 외관, 구현 단순, DatumConfig 변경 0 보너스. | ✓ |
| (a) PropertyGrid 안 — bool ShowVerticalImage property + 체크박스 토글 | 외관이 [👁] 아닌 체크박스, INI 의도치 않은 직렬화 회피 작업 필요. | |
| (c) PropertyGrid 안 — PropertyTemplateSelector 커스텀 DataTemplate | 외관 100% 충실 but PropertyTools.Wpf 3.1.0 호환성 리스크 + 회귀 위험. | |

**User's choice:** (b) — MainView 외부 UI.

### Q B-2: Datum 노드 간 이동 시 swap 상태 처리

| Option | Description | Selected |
|--------|-------------|----------|
| Datum 노드 이동 시 swap 상태 초기화 — 기본값 가로축 (TeachingImagePath) (권장) | 단순, 예측 가능, INI 미저장 = 세션 한정. | ✓ |
| Datum 노드당 swap 상태 메모리 유지 | Datum 4개 간 일관성 위험 + 구현 복잡도 증가. | |
| Wizard step 중 = swap 상태 유지, Wizard 없으면 자동 판단 | 잡식 + 예측 어려움 — 채택 안 함. | |

**User's choice:** 첫 번째 — 세션 한정 + 가로축 기본.

### Q B-3: 1-image algorithm (TLI/CTH/VTH) 에서 swap 버튼 UI 표시 여부

| Option | Description | Selected |
|--------|-------------|----------|
| DualImage 일 때만 표시, 1-image 는 Collapsed (권장) | AlgorithmType 변경 시 즉시 Visibility 업데이트. 1-image 는 swap 할 이미지 자체 없음. | ✓ |
| 항상 표시, 1-image 일 때 Disabled + ToolTip 안내 | 컨텍스트 안내 가능하나 화면 공간 낭비. | |
| DualImage 일 때만 Visible + 배지와 동일 조건 동기화 | (사실상 첫 옵션과 동일) | |

**User's choice:** 첫 번째.

---

## Area C — 수동 swap 시 ROI / wizard 상태 보존

### Q C-1: swap 시 그리던 ROI 표시 정책

| Option | Description | Selected |
|--------|-------------|----------|
| 현재 이미지에 속하는 ROI 만 표시 (권장) — 가로=HA/HB, 세로=V | 의미 직관적, ROI 시맨틱 위반 회피, ROI 데이터는 보존. | ✓ |
| 모든 ROI 항상 표시 — 이미지와 무관 | 일관적이지만 사용자 혼동 위험. | |
| swap 시 모든 ROI 일시 숨김 — Wizard step 완료 시에만 표시 | 단순 화면 but ROI 확인 용도 상실 (문제). | |

**User's choice:** 첫 번째.

### Q C-2: wizard step 라벨 vs swap 충돌 시 동기화

| Option | Description | Selected |
|--------|-------------|----------|
| wizard 라벨은 step 진행 중을 나타내고 배지는 현재 이미지를 나타냄 — 그대로 더함 (권장) — swap 시 step 라벨 변경 안 함 | 의미 분리 (라벨=편집 의도, 배지=현재 화면). 사용자 검증 패턴 자유롭게. | ✓ |
| 수동 swap 시 step 자동 되돌림 — [👁 가로]=step HorizontalA, [👁 세로]=step Vertical | 강제 동기화 but HB 직후 [👁 가로] 누르면 step HA 로 되돌림 → 그린 ROI 취소 위험. | |
| 불일치 시 경고 배너만 표시 ("Vertical ROI 는 세로 이미지에서 그립니다") | 동기화 안 함 + 우선 안내만. 추천 안 됨 (배지 자체가 이미 안내 역할). | |

**User's choice:** 첫 번째.

### Q C-3: Idle (wizard 외) Datum 노드 선택에서 swap 동작

| Option | Description | Selected |
|--------|-------------|----------|
| 동일 동작 — 두 이미지 토글 가능, ROI 는 해당 축만 표시 (권장) | 일관성. Wizard 외일 때는 ROI 확인 용도. | ✓ |
| Wizard 외일 때는 swap 불가 (버튼 Disabled) | 제한적, ROI 검증 사용성 막음. | |

**User's choice:** 첫 번째.

---

## Area D — 배지 디자인 + UAT 6/7 합격 기준 + 테스트 이미지 페어

### Mid-discussion user input

> "테스트이미지도 사실 2개가 필요하니 이부분도 고려가 되어야함"

→ Area D 의 핵심 변수로 통합. SIMUL UAT 도 DualImage 알고리즘 입력으로 가로/세로 이미지 페어 필수 = D-1 / D-4 질문에 직접 반영.

### Q D-1: 듀얼 이미지 페어 확보 소스

| Option | Description | Selected |
|--------|-------------|----------|
| 기존 Cal_Image/ 하위 SIMUL 이미지 재활용 — Top 1장 + Cal_Image/Side/ 1장 의사 듀얼 (권장) | SIMUL 범위 안 최단 경로. 해상도 달라도 기능 검증 우선. | ✓ |
| Side fixture 실측 이미지 페어 — 장비 도착 후 실제 촬영 | CO-34-04 본래 일관 조건. 일정 위험 높음. | |
| 의사 듀얼 먼저 + 실측 페어는 carry-over CO-34.1-XX 로 분리 | 이대 완전성. (D-3 합격 기준에서 이 분리 반영됨) | |

**User's choice:** 첫 번째 — 의사 듀얼 우선.

### Q D-2: 배지 디자인 구체화

| Option | Description | Selected |
|--------|-------------|----------|
| 가로축 #1976D2 / 세로축 #F57C00 / 텍스트 "가로축"/"세로축" / 14px / 마진 12px (권장) | Material Design Blue 700 / Orange 800. 채도 높고 색약 구별 가능. 구현 단순. | ✓ |
| 이모지 포함 "🟦 가로축 (HA/HB)" / "🟧 세로축 (V)" | 정보량 최대 but Segoe UI Emoji 폰트 의존. | |
| Claude 재량 — 핵심만 잠그고 디테일 자완료 | (사실상 첫 옵션과 거의 동일, 가드 명시가 더 안전). | |

**User's choice:** 첫 번째.

### Q D-3: UAT Test 6 + Test 7 합격 기준

| Option | Description | Selected |
|--------|-------------|----------|
| Test 6 = swap 토글 시 배지 텍스트/색상/ROI 가시성 3자 동시 전환 시각 지연 없음. Test 7 = SIMUL 의사 페어 Datum 결합 PASS + Side 실측은 CO-34.1-01 로 분리 (권장) | Test 6 = 구체 3자 가드. Test 7 = SIMUL 한계 명확 + 실측 완전성은 carry-over. 장비 도착 불가에 sign-off 묶임 회피. | ✓ |
| Test 6 = 텍스트/색상 일치만 / Test 7 = Side 실측 필수 PASS | 완전성 최대 but v1.1 일정 jeopardy. | |
| Test 7 = 이미지 없는 dry-run | 실측 검증 누락 — 채택 안 함. | |

**User's choice:** 첫 번째.

### Q D-4: SIMUL 의사 페어 이미지 구축 주체 / 시점

| Option | Description | Selected |
|--------|-------------|----------|
| Phase 34.1 Plan 1 의 마지막 task 로 의사 페어 첨부 (executor 가 결정적 조작, Cal_Image/DualImageTest/) (권장) | 사용자 수동 준비 부담 0, UAT 재현 가능성 보장. | ✓ |
| UAT 시작 시 사용자 수동 준비 | UAT 재현성 위협. | |
| Phase 34 의 테스트 이미지 재활용 — 경로만 추적 | Phase 34 의 swap UX 갭을 일관 재현하던 이미지 마이니 한 구조. (옵션 1 이 더 결정적이므로 채택 안 함) | |

**User's choice:** 첫 번째 — Plan 1 마지막 task.

---

## Claude's Discretion

- 배지/swap 버튼의 정확한 ToolTip 텍스트, Hover/Pressed 시각 효과, Disabled 색상
- Cal_Image/DualImageTest/ 안 파일 이름 (D-34.1-13 의 Top 1장 + Side 1장 가드만 준수)
- HalconViewer ROI 가시성 토글 정확한 API (Research #2 가 가드 제공)
- 배지 컨트롤 시각 디테일 (zIndex/Drop Shadow 등, D-34.1-14 의 hex/14px/12px 가드만 준수)

## Deferred Ideas

- 두 이미지 해상도/orientation 불일치 검증 — 별도 phase
- 티칭 중 두 이미지 동시 미리보기 (split-view)
- swap 상태의 Datum 노드별 메모리 유지
- 다른 algorithm 의 dual-image 변형 (CircleTwoHorizontalDualImage 등)
- Side fixture 실측 페어 확보 후 Datum 결합 검증 — **CO-34.1-01 carry-over** (v1.1 다음 회주 또는 Phase 27 흡수)

---

*Discussion log generated: 2026-05-27*
*Workflow: /gsd-discuss-phase 34.1 (Update mode, all 4 areas selected)*
