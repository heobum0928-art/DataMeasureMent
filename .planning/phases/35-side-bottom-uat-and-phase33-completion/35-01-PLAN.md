---
phase: 35-side-bottom-uat-and-phase33-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "HalconViewerControl.LoadImage(string) 와 LoadImage(HImage) 호출 순서에 무관하게 canvas 가 항상 최신 의도된 이미지 표시 (다중-Load 시나리오 PASS)"
    - "ShotConfig._image 캐시는 사용자가 Shot 노드에서 Load 한 경우만 갱신 (Datum 노드 Load 는 ShotConfig 캐시 미오염)"
    - "Datum 노드 → Load Image → ROI 티칭 시 캔버스가 방금 로드한 이미지를 표시 (stale 이미지 위 ROI 작성 차단)"
    - "Top 회귀 0 — Phase 23.1 sign-off 시점 단일-Load 시나리오에서 동작 byte-identical"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs"
      provides: "LoadImage 두 오버로드 일관성 + CurrentImagePath 정규화"
      contains: "260527 hbk Phase 35"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "DisplayShotImage / LoadAndDisplay 캐시 동기화 명시"
      contains: "260527 hbk Phase 35"
  key_links:
    - from: "HalconViewerControl.LoadImage(string) L111-126"
      to: "HalconViewerControl.LoadImage(HImage) L128-136"
      via: "CurrentImagePath 의 일관된 상태 관리"
      pattern: "CurrentImagePath = "
    - from: "MainView.LoadAndDisplay L488-493"
      to: "ShotConfig.SetImage"
      via: "displayParam == pathSinkParam 조건 명시 (Datum Load 시 ShotConfig._image 오염 방지)"
      pattern: "ReferenceEquals\\(displayParam, pathSinkParam\\)"
---

<objective>
CO-33-02 (이미지 갱신 회귀) 의 root cause 인 HalconViewerControl 캐시 로직 일관성 부족을 해소한다.

사용자 다중-Load 시나리오 (Datum Load → Shot Load → Datum 재Load → Shot 재Load → 검사) 에서
Top/Side/Bottom 시퀀스 모두 Datum 검출 실패하는 baseline 회귀의 단일 root cause 가설 검증.

Purpose:
- HalconViewerControl.LoadImage(string) (캐시 hit 가능) vs LoadImage(HImage) (CurrentImagePath = null) 두 오버로드의 상태 불일치 해소
- ShotConfig._image 캐시 무효화 조건 명시 (Datum 노드 Load 가 Shot 캐시 오염 방지)
- 노드 선택 이벤트에서 사용자가 보는 canvas 와 ROI 좌표가 일치하도록 보장

Out of scope:
- OwnerSequenceName 아키텍처 (Plan 35-02)
- 통합 SIMUL UAT (Plan 35-03)
- DatumFindingService 알고리즘 자체 변경
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/35-side-bottom-uat-and-phase33-completion/35-CONTEXT.md
@.planning/phases/33-side-bottom-inspectionsequence-migration/33-UAT.md

# D-06 보호 (Phase 33 계승, Plan 35-01 에서는 변경 0)
@WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
@WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs

<interfaces>
<!-- HalconViewerControl.LoadImage 두 오버로드 현재 상태 -->
From WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs L111-136:
```csharp
public HImage CurrentImage { get; private set; }
public string CurrentImagePath { get; private set; }

public void LoadImage(string imagePath) {
    if (HasImage && string.Equals(CurrentImagePath, imagePath, StringComparison.OrdinalIgnoreCase)) {
        ApplyInitialFitView();
        Render();
        return; //  ← 같은 경로면 새로 로드 안 함
    }
    DisposeImage();
    CurrentImagePath = imagePath;
    CurrentImage = string.IsNullOrWhiteSpace(imagePath) ? null : new HImage(imagePath);
    UpdateImageMetadata();
    ApplyInitialFitView();
    Render();
}

public void LoadImage(HImage image) {
    DisposeImage();
    CurrentImagePath = null;  //  ← HImage 오버로드 호출 후 path 정보 손실
    CurrentImage = HalconImageBridge.Clone(image);
    UpdateImageMetadata();
    ApplyInitialFitView();
    Render();
}
```

<!-- MainView.LoadAndDisplay 2-arg L467-517 (Phase 32 UAT 후 수정 상태) -->
From WPF_Example/UI/ContentItem/MainView.xaml.cs L488-493:
```csharp
if (displayParam is ShotConfig shot && ReferenceEquals(displayParam, pathSinkParam)) {
    HImage currentImg = halconViewer.CurrentImage;
    if (currentImg != null) {
        shot.SetImage(currentImg);
    }
}
```

<!-- 사용자 다중-Load 시나리오 (CO-33-03 재현) -->
1. Datum 노드 → Load Image (path A) → halconViewer.LoadImage(string A)
   → CurrentImagePath = A, CurrentImage = HImage(A)
   → DatumConfig.TeachingImagePath = A
   → shot.SetImage 호출 안 됨 (displayParam=Shot, pathSinkParam=DatumConfig — 참조 불일치)
2. Shot 노드 → Load Image (path B) → halconViewer.LoadImage(string B)
   → CurrentImagePath = B, CurrentImage = HImage(B)
   → ShotConfig.SimulImagePath = B
   → shot.SetImage(HImage(B)) — ShotConfig._image 캐시 갱신
3. Datum 노드 재방문 → DisplayMeasurementImage/DisplayShotImage 안 호출 (Datum 분기에 없음)
   → canvas 는 path B 의 이미지 상태 유지 (stale)
   → Datum 노드 클릭만 했을 때 DatumConfig.TeachingImagePath = A 인 채로 canvas 는 B 표시
4. Datum 노드 → Load Image (path A 다시) → halconViewer.LoadImage(string A)
   → 캐시 hit ! (CurrentImagePath = "B", A 와 다름 → 실제로는 hit 안 됨. 정상)
   → 그러나 다른 path 시나리오에서는 hit 발생 가능
5. Shot 노드 재방문 → DisplayShotImage(shot) → halconViewer.LoadImage(HImage)
   → CurrentImagePath = null
6. 검사 실행 → DatumPhase: GrabOrLoadDatumImage(parentSeq) → TeachingImagePath = A 로드
   → DatumFindingService 가 path A 이미지로 Datum 검출
   → ROI 좌표는 사용자가 path A 위에 잡았을 때라면 일치해야 함 — 그러나 실패

가설 검증 후보:
- 사용자가 5번 Step 5 다음에 Datum 노드 다시 클릭하면 canvas 는 SHOT 이미지가 표시되어 있을 가능성 (Datum 분기에 이미지 재로드 없음)
- 사용자가 그 상태에서 Load Image 누르면 캐시 hit 또는 stale 발생
- 또는 ROI 가 Shot 이미지 위에서 잡혔는데 검출은 Datum 이미지에서 실행됨
</interfaces>

<decisions>
- D-A1, D-B1 (locked, CONTEXT.md): 본 Plan 에서는 변경 없음 (Plan 35-02 에서 처리)
- D-C1 (locked): 광범위 캐시 로직 리팩터링 — 본 Plan 의 핵심
- D-D1 (locked): 본 Plan = Wave 1, 이미지 hotfix 최우선
- D-E1 (locked): InspectionSequence.cs / Action_FAIMeasurement.cs / VisionResponsePacket.cs 변경 0
- D-35-01-01 (planner): LoadImage(HImage) 의 CurrentImagePath = null 정책 → CurrentImagePath = "" 빈 문자열 + CurrentImageSource enum (Path | HImage) 도입으로 변경
  (null vs "" 분리: 명시적 빈 문자열은 "HImage 로드 됨" 상태, null 은 "초기화 전" 상태)
- D-35-01-02 (planner): Datum 노드 선택 시 InspectionListView 가 DisplayDatumImage 호출 추가 — DatumConfig.TeachingImagePath 존재 시 canvas 에 표시. Shot/Measurement 와 일관성 확보 (사용자 다중-Load 시나리오 5의 stale canvas 해소)
- D-35-01-03 (planner): HalconViewerControl.LoadImage(HImage) 호출 시 CurrentImagePath 가 외부에서 명시적으로 전달 가능하도록 overload 추가 (LoadImage(HImage image, string sourceContext = null))
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HalconViewerControl.LoadImage 두 오버로드 일관성 + 캐시 상태 명시</name>
  <files>WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs (전체)
  </read_first>
  <action>
### Part A — LoadImage(HImage) overload 시그니처 확장

기존 L128-136 의 `LoadImage(HImage)` 를 다음으로 교체:

```csharp
//260527 hbk Phase 35 — CO-33-02 hotfix: HImage 오버로드도 sourceContext 보존하여 캐시 일관성 확보
public void LoadImage(HImage image, string sourceContext = null) {
    DisposeImage();
    // 기존 null 대신 sourceContext 보존 — null/empty 이면 빈 문자열로 정규화하여 "HImage 직접 로드 됨" 상태 표현
    CurrentImagePath = sourceContext ?? "";
    CurrentImage = HalconImageBridge.Clone(image);
    UpdateImageMetadata();
    ApplyInitialFitView();
    Render();
}
```

### Part B — LoadImage(string) 캐시 hit 조건 강화

기존 L111-126 의 `LoadImage(string imagePath)` 에서 캐시 hit 조건에 가드 추가:

```csharp
//260527 hbk Phase 35 — CO-33-02 hotfix: 이전 HImage 로드 후에는 캐시 hit 무효화 (CurrentImagePath = "" 였을 때 다른 path 와 잘못된 비교 방지)
public void LoadImage(string imagePath) {
    bool cacheHit = HasImage
                    && !string.IsNullOrEmpty(CurrentImagePath)   //260527 hbk Phase 35 — 빈 문자열(HImage 로드 상태) 시 캐시 hit 차단
                    && string.Equals(CurrentImagePath, imagePath, StringComparison.OrdinalIgnoreCase);
    if (cacheHit) {
        ApplyInitialFitView();
        Render();
        return;
    }
    DisposeImage();
    CurrentImagePath = imagePath ?? "";   //260527 hbk Phase 35 — null 방지
    CurrentImage = string.IsNullOrWhiteSpace(imagePath) ? null : new HImage(imagePath);
    UpdateImageMetadata();
    ApplyInitialFitView();
    Render();
}
```

### Part C — CurrentImagePath 정규화 정책 (이 클래스에서만 적용, 호출 측 영향 0)
- `null` → 초기화 전 상태만
- `""` → HImage 직접 로드 됨 (path 없음)
- non-empty string → path 로 로드 됨
- 외부 호출 측은 기존처럼 `CurrentImagePath` 를 자유롭게 읽기 가능 (null check 만 추가 권장)

### 주의
- `LoadImage(HImage image)` 기존 호출 (DisplayShotImage 등) 는 sourceContext 미지정 → 빈 문자열 폴백 → 기존 동작과 의미적 동등 (단, 캐시 hit 가능성 증가는 Part B 가드로 차단)
- `IsNullOrEmpty(CurrentImagePath)` 가드는 LoadImage(HImage) 호출 후 LoadImage(string) 호출 시 잘못된 캐시 hit 차단
- D-06 가드: 본 Task 는 HalconViewerControl 만 수정, InspectionSequence/Action_FAIMeasurement 변경 0
  </action>
  <verify>
    <automated>grep -c "260527 hbk Phase 35" WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs returns at least 3</automated>
    <automated>grep -c "LoadImage(HImage image, string sourceContext" WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs returns 1</automated>
    <automated>grep -c "IsNullOrEmpty(CurrentImagePath)" WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs returns 1</automated>
    <automated>grep -c "CurrentImagePath = null" WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs returns 0 (모두 "" 로 교체)</automated>
  </verify>
  <acceptance_criteria>
    - LoadImage(HImage) overload 가 optional sourceContext 파라미터 보유
    - LoadImage(string) 의 캐시 hit 조건이 CurrentImagePath 빈 문자열 가드 보유
    - 기존 외부 호출 site (DisplayShotImage, LoadAndDisplay 등) 시그니처 변경 0 (default 인자 활용)
    - 신규 마커 주석 `//260527 hbk Phase 35` 3개 이상
  </acceptance_criteria>
  <done>HalconViewerControl 의 두 LoadImage 오버로드가 일관된 상태 관리. CurrentImagePath null vs "" 의미 분리 완료.</done>
</task>

<task type="auto">
  <name>Task 2: MainView 캐시 동기화 명시 + InspectionListView Datum 노드 이미지 표시 추가</name>
  <files>
    WPF_Example/UI/ContentItem/MainView.xaml.cs
    WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  </files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L100-160, L450-560
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs L370-560 (InspectionList_SelectionChanged 의 Datum 분기 L421-459)
    - Task 1 수정 후 HalconViewerControl 상태
  </read_first>
  <action>
### Part A — MainView 에 DisplayDatumImage 신규 메서드 추가

DisplayShotImage / DisplayMeasurementImage / DisplayFAIImage 와 동일 위치 (L108-151 근처) 에 추가:

```csharp
//260527 hbk Phase 35 — CO-33-02 hotfix: Datum 노드 선택 시 TeachingImagePath 표시 (Shot/Measurement 와 일관성 확보, stale canvas 차단)
/// <summary>Displays the TeachingImagePath image of the given DatumConfig on the canvas.
/// Mirrors DisplayShotImage but uses DatumConfig.TeachingImagePath (Phase 22 IMG-02 분리 구조).</summary>
public void DisplayDatumImage(DatumConfig datum) {
    if (datum == null) {
        return;
    }
    string path = datum.TeachingImagePath;
    if (string.IsNullOrEmpty(path) || !System.IO.File.Exists(path)) {
        // TeachingImagePath 미설정 — 기존 canvas 유지 (사용자가 Load Image 누르면 갱신)
        return;
    }
    try {
        halconViewer.LoadImage(path);
        label_message.Visibility = Visibility.Collapsed;
    } catch (Exception ex) {
        Logging.PrintErrLog((int)ELogType.Error, ex.Message);
    }
}
```

### Part B — LoadAndDisplay 의 ShotConfig._image 캐시 갱신 가드 강화

L488-493 의 캐시 갱신 블록을 다음과 같이 갱신:

```csharp
//260527 hbk Phase 35 — CO-33-02 hotfix: ShotConfig._image 캐시는 Shot 노드 Load 시점에만 갱신 (Datum Load 시 오염 차단)
//  displayParam = Shot AND pathSinkParam = Shot (자기 자신) 인 경우만 SetImage — Datum Load 는 displayParam=Shot, pathSinkParam=DatumConfig 이므로 자동 차단됨
//  Phase 32 UAT 의 9c482dd / 4ea5bcc 패턴 보존 + 의도 명시 강화
if (displayParam is ShotConfig shot && ReferenceEquals(displayParam, pathSinkParam)) {
    HImage currentImg = halconViewer.CurrentImage;
    if (currentImg != null) {
        shot.SetImage(currentImg);
    }
}
```
(기존 코드와 거의 동일 — 의도 강화 주석만 추가. 동작 변경 0)

### Part C — InspectionListView Datum 분기에 DisplayDatumImage 호출 추가

InspectionListView.xaml.cs L421-458 의 Datum 분기 (`item.NodeType == ENodeType.Datum`) 의 SetDatumOverlay / PublishDatumRoiCandidates 호출 다음에 추가:

```csharp
if (itemParam is DatumConfig datumCfg) {
    mParentWindow.mainView.halconViewer.SetDatumOverlay(datumCfg, true);
    mParentWindow.mainView.PublishDatumRoiCandidates(datumCfg);
    //260527 hbk Phase 35 — CO-33-02 hotfix: Datum 노드 선택 시 TeachingImagePath 표시 (Shot/Measurement 와 일관성)
    mParentWindow.mainView.DisplayDatumImage(datumCfg);
    // ... (기존 ParamEditor force rebind 등은 유지)
}
```

### Part D — Measurement 노드 분기는 변경 없음 (Phase 32-06 4ea5bcc/9c482dd 보존)
- `DisplayMeasurementImage(meas)` 호출 (L491) 보존 — Shot 이미지 표시
- 단, 의도 강화 주석 추가 권장:
  ```csharp
  //260527 hbk Phase 35 — Measurement 노드 = 측정 단계 = SHOT 이미지 표시 (Datum 이미지 아님)
  mParentWindow.mainView.DisplayMeasurementImage(meas);
  ```

### 주의
- Datum 노드와 Shot/Measurement 노드의 canvas 상태가 다른 이미지를 표시할 수 있음 → 정상 동작 (Phase 22 IMG-02 dual-image 구조)
- DisplayDatumImage 가 string path 오버로드 사용 → Task 1 의 CurrentImagePath 정규화 혜택 받음
  </action>
  <verify>
    <automated>grep -c "DisplayDatumImage" WPF_Example/UI/ContentItem/MainView.xaml.cs returns at least 2 (정의 + comment)</automated>
    <automated>grep -c "DisplayDatumImage" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs returns at least 1 (호출)</automated>
    <automated>grep -c "260527 hbk Phase 35" WPF_Example/UI/ContentItem/MainView.xaml.cs returns at least 2</automated>
    <automated>grep -c "260527 hbk Phase 35" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs returns at least 1</automated>
  </verify>
  <acceptance_criteria>
    - MainView.DisplayDatumImage 메서드 정의 (TeachingImagePath 기반)
    - InspectionListView 의 Datum 분기에서 DisplayDatumImage 호출 1라인 추가
    - LoadAndDisplay 의 캐시 갱신 가드 의도 강화 주석 추가 (동작 byte-identical)
    - 신규 마커 주석 3개 이상
  </acceptance_criteria>
  <done>Datum 노드 선택 시 canvas 가 TeachingImagePath 자동 표시. 사용자 다중-Load 시나리오의 stale canvas 차단.</done>
</task>

<task type="auto">
  <name>Task 3: msbuild Debug/x64 검증</name>
  <files>(read-only — build artifact)</files>
  <read_first>(none — Task 1, 2 완료 후 빌드만 검증)</read_first>
  <action>
앱이 실행 중이면 종료 (MSB3027 회피). 다음 명령 실행:

```cmd
msbuild WPF_Example\DatumMeasurement.csproj /t:Build /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal
```

검증 조건:
- exit code = 0
- "0 Error(s)" 라인 존재
- 신규 warning 카테고리 = 0 (Phase 33 baseline 의 CS0618 5건 외 추가 없음)

신규 warning 발생 시 (CS0612/CS0618 외 카테고리) 즉시 Task 1/2 재검토.
  </action>
  <verify>
    <automated>msbuild exit code = 0</automated>
    <automated>"0 Error(s)" 라인 존재</automated>
    <automated>신규 warning 카테고리 = 0</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 Build PASS
    - CS error 0
    - 신규 warning 카테고리 0 (CS0618 5건 baseline 외)
  </acceptance_criteria>
  <done>Task 1/2 의 코드 변경이 컴파일 PASS, Wave 1 자체 검증 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MainView ↔ HalconViewerControl | LoadImage 오버로드 변경이 다른 호출 site 회귀 가능성 |
| InspectionListView ↔ MainView | DisplayDatumImage 신규 메서드 호출이 기존 Datum overlay 흐름과 충돌 가능성 |
| Phase 22 IMG-02 dual-image 구조 | TeachingImagePath != SimulImagePath 분리 보존 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01-01 | Tampering | LoadImage 시그니처 변경으로 다른 호출 site 회귀 | mitigate | optional default 파라미터 (`sourceContext = null`) — 기존 호출 site 모두 byte-identical 호환 |
| T-35-01-02 | Tampering | DisplayDatumImage 가 Phase 12-03 Datum 티칭 UI 흐름 회귀 | mitigate | Datum overlay / RoiCandidates publish 흐름 byte-identical 보존, canvas 이미지 표시만 추가 |
| T-35-01-03 | Information Disclosure | LoadImage 캐시 hit 조건 강화로 정상 시나리오 (같은 path 재로드) 회귀 | mitigate | IsNullOrEmpty(CurrentImagePath) 가드는 빈 문자열일 때만 hit 차단 — non-empty path 비교는 byte-identical |
| T-35-01-04 | Denial-of-Service | Datum.TeachingImagePath 가 잘못된 경로일 때 DisplayDatumImage 예외 | mitigate | File.Exists 가드 + try/catch + ELogType.Error 로그 |
</threat_model>

<verification>
### 자동 검증 (Task 3)
- msbuild Debug/x64 Build PASS, CS error 0, 신규 warning 0

### Plan 35-03 UAT 인계
- Top 단일-Load 시나리오: 기존 Phase 23.1 recipe 로드 → 검사 → Phase 23.1 sign-off 시점과 측정값 byte-identical (회귀 0)
- Top 다중-Load 시나리오: Datum Load → Shot Load → Datum 재Load → Shot 재Load → 검사 → Datum 검출 PASS (CO-33-03 해소)
- Side/Bottom 다중-Load 시나리오: 동일 PASS
- canvas 표시 시각 검증: Datum 노드 선택 시 TeachingImagePath 자동 표시, Shot 노드 선택 시 Shot 이미지 표시

### Wave 2 인계
- 이 Wave 가 CO-33-06 (Bottom Shot 재로드 실패) 도 함께 해소했는지 확인 → 해소 시 Plan 35-02 의 핵심 검증만 수행, 미해소 시 OwnerSequenceName 아키텍처 보강 필요
</verification>

<success_criteria>
1. HalconViewerControl.LoadImage 두 오버로드 일관된 상태 관리 (CurrentImagePath null vs "" 의미 분리)
2. MainView.DisplayDatumImage 신규 메서드 — Datum 노드 선택 시 TeachingImagePath 자동 표시
3. InspectionListView Datum 분기 DisplayDatumImage 호출 추가
4. ShotConfig._image 캐시 갱신 가드 의도 강화 주석 (동작 byte-identical)
5. msbuild Debug/x64 Build PASS, CS error 0, 신규 warning 0
6. D-06 가드: InspectionSequence.cs / Action_FAIMeasurement.cs / VisionResponsePacket.cs 변경 0 라인
7. Phase 12-03 Datum 티칭 UI 흐름 회귀 0
</success_criteria>

<output>
After completion, create `.planning/phases/35-side-bottom-uat-and-phase33-completion/35-01-SUMMARY.md` documenting:
  - 3 파일 변경 라인 수 (HalconViewerControl + MainView + InspectionListView)
  - msbuild Build 결과 (CS error 0, warning 카테고리 분석)
  - D-06 가드 git diff 증명 (InspectionSequence.cs / Action_FAIMeasurement.cs / VisionResponsePacket.cs = 0 라인)
  - Wave 2 인계 사항: CO-33-06 (Bottom Shot 재로드) 가 본 Wave 의 hotfix 로 해소되었는지 사용자 검증 필요
  - Wave 3 인계: Phase 33 Test 2~5 통합 UAT 진입 가능 상태
</output>
