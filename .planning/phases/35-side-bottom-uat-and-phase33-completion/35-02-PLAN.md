---
phase: 35-side-bottom-uat-and-phase33-completion
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs
  - WPF_Example/Custom/Sequence/SequenceHandler.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "ShotConfig 가 OwnerSequenceName (string) 필드를 보유하고 ParamBase reflection 자동 직렬화"
    - "신규 Shot 추가 시 AddShotToSequence 가 seqNode.SequenceID 를 OwnerSequenceName 으로 변환하여 ShotConfig 에 설정"
    - "INI 로드 시 OwnerSequenceName 미존재 또는 빈 값 → SEQ_TOP 자동 폴백 (D-B1)"
    - "RebuildInspectionActions(seqId) 가 RecipeManager.Shots 에서 해당 시퀀스 소유 Shot 만 필터링하여 Action 생성"
    - "트리 재구축 시 OwnerSequenceName 기준으로 시퀀스 노드 아래 그룹화"
    - "Top 회귀 0 — Phase 33 이전 INI (OwnerSequenceName 키 부재) 로드 시 모든 Shots 가 Top 소속으로 자동 폴백"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs"
      provides: "OwnerSequenceName public string 필드 + Category 노출"
      contains: "OwnerSequenceName"
    - path: "WPF_Example/Custom/Sequence/SequenceHandler.cs"
      provides: "RebuildInspectionActions(seqId) 의 OwnerSequenceName 필터링"
      contains: "OwnerSequenceName"
  key_links:
    - from: "InspectionListView.AddShotToSequence (L807-843)"
      to: "ShotConfig.OwnerSequenceName"
      via: "seqNode.SequenceID → SEQ_TOP/SIDE/BOTTOM 상수 변환"
      pattern: "shot.OwnerSequenceName ="
    - from: "InspectionRecipeManager.LoadPhase6Format (L201-209)"
      to: "ShotConfig.OwnerSequenceName"
      via: "INI 키 로드 + 빈값 폴백"
      pattern: "OwnerSequenceName"
    - from: "SequenceHandler.RebuildInspectionActions (L66-86)"
      to: "ShotConfig.OwnerSequenceName"
      via: "시퀀스별 Shots 필터링"
      pattern: "OwnerSequenceName"
---

<objective>
CO-33-06 (Bottom Shot 재로드 실패) 의 root cause 인 `RecipeManager.Shots` 글로벌 단일 리스트 + per-sequence ownership 누락을 해소한다.

Phase 33 의 SequenceHandler 마이그레이션이 Side/Bottom 을 InspectionSequence 로 만들었으나
per-sequence Shot ownership 모델이 미해결로 남아 Bottom Shot 이 재로드 시 어느 시퀀스 트리 아래 붙일지 정보 부재로 실패.

Purpose:
- ShotConfig 에 OwnerSequenceName (string, D-A1) 필드 추가
- INI 저장/로드에 OwnerSequenceName 직렬화
- AddShotToSequence UI 핸들러가 시퀀스 정보를 ShotConfig 에 전달
- RebuildInspectionActions 가 시퀀스 소유 Shot 만 필터링
- 트리 재구축 시 OwnerSequenceName 기준 분기
- 기존 INI 폴백 — OwnerSequenceName 미존재 시 SEQ_TOP 자동 (D-B1)

Out of scope:
- 이미지 캐시 hotfix (Plan 35-01)
- 통합 SIMUL UAT (Plan 35-03)
- ShotConfig 의 다른 필드 변경
- ESequence enum 정의 변경
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/35-side-bottom-uat-and-phase33-completion/35-CONTEXT.md
@.planning/phases/35-side-bottom-uat-and-phase33-completion/35-01-SUMMARY.md

# D-06 보호 (Phase 33 계승, Plan 35-02 에서는 변경 0)
@WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
@WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
@WPF_Example/TcpServer/VisionResponsePacket.cs

<interfaces>
<!-- ShotConfig 현재 상태 (L9-44) -->
From WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs L9-44:
```csharp
public class ShotConfig : CameraSlaveParam, IOfflineImageParam {
    [Category("Shot|Setting")]
    public double ZPosition { get; set; }
    public int DelayMs { get; set; }

    [Category("Shot|Simulation")]
    public string SimulImagePath { get; set; } = "";

    private string _shotName;
    [Category("Shot|Identity")]
    public string ShotName { get { ... } set { ... } }
    // ... (조명 8 필드)
}
```

<!-- InspectionRecipeManager.AddShot 현재 시그니처 (L36-42) -->
From InspectionRecipeManager.cs L36-42:
```csharp
public ShotConfig AddShot(string name = null) {
    string shotName = name ?? $"SHOT_{Shots.Count}";
    var shot = new ShotConfig(_owner, shotName);
    Shots.Add(shot);
    return shot;
}
```

<!-- InspectionRecipeManager.LoadPhase6Format Shot 로드 루프 (L201-249, 중점 L201-218) -->
From InspectionRecipeManager.cs L201-218:
```csharp
for (int s = 0; s < shotCount; s++) {
    string shotSection = $"SHOT_{s}";
    if (!loadFile.ContainsSection(shotSection)) continue;
    ShotConfig shot = AddShot();
    shot.ShotName = loadFile[shotSection]["ShotName"].ToString();
    shot.ZPosition = loadFile[shotSection]["ZPosition"].ToDouble();
    shot.DelayMs = loadFile[shotSection]["DelayMs"].ToInt();
    shot.SimulImagePath = loadFile[shotSection]["SimulImagePath"].ToString();
    int faiCount = loadFile[shotSection]["FAICount"].ToInt();
    if (faiCount < 0) faiCount = 0;
    // Camera/ShotConfig 필드 자동 로드 (ParamBase reflection)
    string camSection = shotSection + "_CAM";
    if (loadFile.ContainsSection(camSection)) {
        shot.Load(loadFile, camSection);  //  ← 자동 직렬화에 OwnerSequenceName 자동 포함됨
    }
    // ...
}
```

<!-- SavePhase6Format 현재 (L128-160) — _CAM 섹션에 ShotConfig 자동 직렬화 -->
From InspectionRecipeManager.cs L132-139:
```csharp
saveFile[shotSection]["ShotName"] = shot.ShotName ?? $"SHOT_{s}";
saveFile[shotSection]["ZPosition"] = shot.ZPosition;
saveFile[shotSection]["DelayMs"] = shot.DelayMs;
saveFile[shotSection]["SimulImagePath"] = shot.SimulImagePath ?? "";
saveFile[shotSection]["FAICount"] = shot.FAIList.Count;
shot.Save(saveFile, shotSection + "_CAM");  //  ← ParamBase reflection 자동 직렬화 (OwnerSequenceName 자동 포함됨)
```

<!-- SequenceHandler.RebuildInspectionActions 현재 (L66-86) -->
From SequenceHandler.cs L66-86:
```csharp
public void RebuildInspectionActions(ESequence seqId) {
    SequenceBase seq = this[seqId];
    if (seq == null) return;
    if (seq.Param is CameraMasterParam masterParam) masterParam.ClearChildren();

    var actions = new List<ActionBase>();
    for (int i = 0; i < RecipeManager.ShotCount; i++) {
        ShotConfig shot = RecipeManager.Shots[i];   //  ← 모든 Shot 순회 (시퀀스 무관)
        EAction actionId = (EAction)((int)EAction.FAI_Base + i);
        string actionName = shot.ShotName ?? $"SHOT_{i}";
        var action = new Action_FAIMeasurement(actionId, actionName, shot);
        actions.Add(action);
    }
    if (actions.Count > 0) seq.AddAction(actions.ToArray());
}
```

<!-- InspectionListView.AddShotToSequence (L807-843) -->
From InspectionListView.xaml.cs L807-843:
```csharp
private void AddShotToSequence(NodeViewModel seqNode) {
    var seqHandler = SystemHandler.Handle.Sequences;
    string defaultName = "SHOT_" + seqHandler.RecipeManager.ShotCount;
    if (!TextInputBox.Show("Shot 이름 입력", defaultName, out string shotName)) return;
    ShotConfig shot = seqHandler.RecipeManager.AddShot(shotName);
    FAIConfig fai = shot.AddFAI("FAI_0");
    seqHandler.EnableDynamicFAIMode();
    var shotNode = new CompositeNode {
        Name = shot.ShotName,
        NodeType = ENodeType.Action,
        ParamData = shot,
        SequenceName = seqNode.SequenceName,
        SequenceID = seqNode.SequenceID,  //  ← seqNode.SequenceID 보유 — ShotConfig.OwnerSequenceName 으로 전달 가능
        ActionID = EAction.Unknown
    };
    // ... 트리 추가
}
```

<!-- SEQ 상수 -->
From SequenceHandler.cs L11-13:
```csharp
public const string SEQ_TOP = "TOP";
public const string SEQ_SIDE = "SIDE";
public const string SEQ_BOTTOM = "BOTTOM";
```

<!-- ESequence → SEQ_NAME 매핑 헬퍼 필요 (D-A1 의 string 채택) -->
ESequence.Top → SEQ_TOP
ESequence.Side → SEQ_SIDE
ESequence.Bottom → SEQ_BOTTOM
</interfaces>

<decisions>
- D-A1 (locked, CONTEXT.md): OwnerSequenceName (string) 필드
- D-B1 (locked, Claude's Discretion): 빈값 → SEQ_TOP 자동 폴백
- D-D1 (locked, Wave 2): 본 Plan = 아키텍처 보강
- D-E1 (locked): InspectionSequence.cs / Action_FAIMeasurement.cs / VisionResponsePacket.cs 변경 0
- D-35-02-01 (planner): SequenceHandler 에 `ResolveSequenceName(ESequence)` static helper 추가 — ESequence ↔ SEQ_* 상수 매핑 단일 source
- D-35-02-02 (planner): ShotConfig.OwnerSequenceName 의 `[Category("Shot|Identity")]` 노출 — ShotName 과 동일 그룹. 사용자 디버깅 가능, 그러나 일반적으로 사용자가 직접 편집할 필드는 아님 (PropertyGrid 노출은 가시성만)
- D-35-02-03 (planner): EnsurePerRoiDefaults 패턴 적용 (DatumConfig.EnsurePerRoiDefaults 와 동일 위치 — ShotConfig 에 ApplyShotDefaults() 신규 메서드) — OwnerSequenceName == null/"" 시 SEQ_TOP 폴백. ParamBase.Load 직후 호출 (InspectionRecipeManager.LoadPhase6Format 의 shot.Load 다음 라인)
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ShotConfig.OwnerSequenceName 필드 추가 + ApplyShotDefaults 헬퍼</name>
  <files>WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs (전체)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs L555-580 (EnsurePerRoiDefaults 패턴 참조)
  </read_first>
  <action>
### Part A — OwnerSequenceName public string 필드 추가

ShotName 필드 다음 (L44 직후) 에 추가:

```csharp
//260527 hbk Phase 35 — CO-33-06 per-sequence Shot ownership 보강 (D-A1)
//  값 = SequenceHandler.SEQ_TOP / SEQ_SIDE / SEQ_BOTTOM
//  Phase 33 이전 INI 호환: 빈 문자열 → ApplyShotDefaults 에서 SEQ_TOP 자동 폴백 (D-B1)
//  ParamBase reflection 자동 직렬화 (String case, ParamBase.cs L325-339 Save / L385-395 Load)
[Category("Shot|Identity")]
public string OwnerSequenceName { get; set; } = "";
```

### Part B — ApplyShotDefaults 메서드 신규 추가

ShotConfig 클래스 내 적절한 위치 (메서드 영역) 에 추가:

```csharp
//260527 hbk Phase 35 — CO-33-06: D-B1 빈값 폴백 + 향후 ShotConfig 신규 필드 default 정규화 단일 진입점.
//  DatumConfig.EnsurePerRoiDefaults 패턴 차용. InspectionRecipeManager.LoadPhase6Format 의 shot.Load 다음에 호출.
public void ApplyShotDefaults() {
    if (string.IsNullOrEmpty(OwnerSequenceName)) {
        OwnerSequenceName = "TOP"; // SequenceHandler.SEQ_TOP — Phase 33 이전 INI 호환 (모든 기존 Shots = Top 소속)
    }
    if (SimulImagePath == null) {
        SimulImagePath = "";
    }
}
```

주의:
- `"TOP"` 리터럴 사용 — SequenceHandler.SEQ_TOP 상수 참조 가능하나 namespace 의존 회피 (ShotConfig 는 더 낮은 layer)
- 향후 Plan 에서 SEQ_TOP 상수 import 가능하나 본 Plan 범위 밖
- ParamBase reflection 자동 직렬화 → INI 키 자동 생성 ("OwnerSequenceName=TOP" 형태)
  </action>
  <verify>
    <automated>grep -c "OwnerSequenceName" WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs returns at least 3 (필드 + 폴백 + 주석)</automated>
    <automated>grep -c "ApplyShotDefaults" WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs returns 1 (정의)</automated>
    <automated>grep -c "260527 hbk Phase 35" WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs returns at least 2</automated>
  </verify>
  <acceptance_criteria>
    - ShotConfig.OwnerSequenceName public string 필드 존재, default = "", [Category("Shot|Identity")]
    - ApplyShotDefaults 메서드 존재, 빈값 시 "TOP" 폴백 (D-B1)
    - 기존 ShotConfig 메서드/필드 무수정 (회귀 0)
  </acceptance_criteria>
  <done>ShotConfig 가 OwnerSequenceName 보유 + 폴백 헬퍼 보유. ParamBase reflection 자동 직렬화로 INI 라운드트립 보장.</done>
</task>

<task type="auto">
  <name>Task 2: InspectionRecipeManager 의 AddShot overload + LoadPhase6Format 폴백 호출</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs</files>
  <read_first>
    - Task 1 후 ShotConfig 상태
    - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs L36-42, L201-218
  </read_first>
  <action>
### Part A — AddShot 에 seqName 옵션 추가

L36-42 의 AddShot 을 다음으로 교체:

BEFORE:
```csharp
public ShotConfig AddShot(string name = null) {
    string shotName = name ?? $"SHOT_{Shots.Count}";
    var shot = new ShotConfig(_owner, shotName);
    Shots.Add(shot);
    return shot;
}
```

AFTER:
```csharp
//260527 hbk Phase 35 — CO-33-06: per-sequence Shot ownership. seqName 파라미터 default "" 로 기존 호출 site 호환 (D-B1 폴백 적용 위해 ApplyShotDefaults 호출 유지)
public ShotConfig AddShot(string name = null, string seqName = "") {
    string shotName = name ?? $"SHOT_{Shots.Count}";
    var shot = new ShotConfig(_owner, shotName);
    if (!string.IsNullOrEmpty(seqName)) {
        shot.OwnerSequenceName = seqName;
    }
    Shots.Add(shot);
    return shot;
}
```

### Part B — LoadPhase6Format 의 shot.Load 다음에 ApplyShotDefaults 호출

L218 (또는 `shot.Load(loadFile, camSection)` 호출 직후) 다음 라인에 추가:

```csharp
if (loadFile.ContainsSection(camSection)) {
    shot.Load(loadFile, camSection);
}
//260527 hbk Phase 35 — CO-33-06: 기존 INI (OwnerSequenceName 키 부재) 호환 — 빈값 시 SEQ_TOP 폴백 (D-B1)
shot.ApplyShotDefaults();
```

### Part C — 외부 호출 사이트 영향 0
- 기존 `seqHandler.RecipeManager.AddShot(shotName)` 호출 (InspectionListView.AddShotToSequence L813) 은 default 인자로 호환
- Task 3 에서 AddShotToSequence 가 seqName 명시 전달하도록 수정
  </action>
  <verify>
    <automated>grep -c "AddShot(string name = null, string seqName" WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs returns 1</automated>
    <automated>grep -c "ApplyShotDefaults" WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs returns 1 (호출)</automated>
    <automated>grep -c "260527 hbk Phase 35" WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs returns at least 2</automated>
  </verify>
  <acceptance_criteria>
    - AddShot(string, string) overload 시그니처 (seqName 옵션)
    - LoadPhase6Format 에서 shot.Load 다음에 ApplyShotDefaults() 호출 (기존 INI 폴백)
    - SavePhase6Format 변경 0 (ParamBase reflection 자동 직렬화에 의존)
  </acceptance_criteria>
  <done>AddShot 가 시퀀스 정보 전달 가능. LoadPhase6Format 가 빈값 INI 자동 폴백.</done>
</task>

<task type="auto">
  <name>Task 3: SequenceHandler.RebuildInspectionActions 필터링 + ResolveSequenceName helper + AddShotToSequence UI 핸들러</name>
  <files>
    WPF_Example/Custom/Sequence/SequenceHandler.cs
    WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  </files>
  <read_first>
    - Task 1, 2 후 상태
    - WPF_Example/Custom/Sequence/SequenceHandler.cs L11-13, L66-86
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs L807-843
    - WPF_Example/Custom/Define/ID.cs (ESequence 정의)
  </read_first>
  <action>
### Part A — SequenceHandler 에 ResolveSequenceName static helper 추가

SequenceHandler 클래스 내 (SEQ_* 상수 다음, RegisterSequences 이전 위치) 추가:

```csharp
//260527 hbk Phase 35 — CO-33-06: ESequence ↔ SEQ_* 상수 매핑 단일 source (D-35-02-01)
public static string ResolveSequenceName(ESequence seqId) {
    switch (seqId) {
        case ESequence.Top: return SEQ_TOP;
        case ESequence.Side: return SEQ_SIDE;
        case ESequence.Bottom: return SEQ_BOTTOM;
        default: return SEQ_TOP; // D-B1 폴백 — 예상치 못한 enum 값도 안전한 동작
    }
}
```

### Part B — RebuildInspectionActions 필터링 추가

기존 L66-86 의 `RebuildInspectionActions` 를 다음으로 교체:

BEFORE:
```csharp
public void RebuildInspectionActions(ESequence seqId) {
    SequenceBase seq = this[seqId];
    if (seq == null) return;
    if (seq.Param is CameraMasterParam masterParam) {
        masterParam.ClearChildren();
    }
    var actions = new List<ActionBase>();
    for (int i = 0; i < RecipeManager.ShotCount; i++) {
        ShotConfig shot = RecipeManager.Shots[i];
        EAction actionId = (EAction)((int)EAction.FAI_Base + i);
        string actionName = shot.ShotName ?? $"SHOT_{i}";
        var action = new Action_FAIMeasurement(actionId, actionName, shot);
        actions.Add(action);
    }
    if (actions.Count > 0) {
        seq.AddAction(actions.ToArray());
    }
}
```

AFTER:
```csharp
//260527 hbk Phase 35 — CO-33-06: 시퀀스 소유 Shot 만 필터링 (D-A1 OwnerSequenceName)
public void RebuildInspectionActions(ESequence seqId) {
    SequenceBase seq = this[seqId];
    if (seq == null) return;
    if (seq.Param is CameraMasterParam masterParam) {
        masterParam.ClearChildren();
    }
    string targetSeqName = ResolveSequenceName(seqId);
    var actions = new List<ActionBase>();
    int actionIdx = 0;
    for (int i = 0; i < RecipeManager.ShotCount; i++) {
        ShotConfig shot = RecipeManager.Shots[i];
        //260527 hbk Phase 35 — CO-33-06: OwnerSequenceName 매칭만 추가 (빈값은 Top 폴백 — ApplyShotDefaults 가 보장)
        string shotOwner = string.IsNullOrEmpty(shot.OwnerSequenceName) ? SEQ_TOP : shot.OwnerSequenceName;
        if (shotOwner != targetSeqName) continue;
        EAction actionId = (EAction)((int)EAction.FAI_Base + actionIdx);
        string actionName = shot.ShotName ?? $"SHOT_{actionIdx}";
        var action = new Action_FAIMeasurement(actionId, actionName, shot);
        actions.Add(action);
        actionIdx++;
    }
    if (actions.Count > 0) {
        seq.AddAction(actions.ToArray());
    }
}
```

주의:
- `actionIdx` 별도 사용 — 글로벌 i 가 아닌 시퀀스별 0/1/2 인덱스로 EAction.FAI_Base + N 부여
- Top 의 기존 동작 회귀 0 (Top 만 있던 시점에는 매칭 100%)
- Side/Bottom 도 OwnerSequenceName 매칭으로 정상 분기

### Part C — InspectionListView.AddShotToSequence 가 OwnerSequenceName 전달

L813 의 AddShot 호출을 다음으로 교체:

BEFORE:
```csharp
ShotConfig shot = seqHandler.RecipeManager.AddShot(shotName);
```

AFTER:
```csharp
//260527 hbk Phase 35 — CO-33-06: 신규 Shot 의 OwnerSequenceName 명시 전달 (D-A1)
string ownerSeqName = SequenceHandler.ResolveSequenceName(seqNode.SequenceID);
ShotConfig shot = seqHandler.RecipeManager.AddShot(shotName, ownerSeqName);
```

### Part D — 트리 재구축 시 OwnerSequenceName 필터링 (선택, 진단 후 결정)

현재 InspectionListView 의 트리 재구축 로직 (RecipeManager.Shots 로부터 트리 생성하는 부분) 이 별도 있다면 OwnerSequenceName 으로 필터링 추가 필요.
없으면 (현재 코드에서 Shot 추가 시 즉시 tree append + Load 시 별도 트리 재구축 미존재) — 동작 검증은 Plan 35-03 에서 INI 라운드트립으로 수행.

본 Plan 에서는 Part A/B/C 만 적용, Part D 는 Plan 35-03 UAT 결과에 따라 hotfix 로 처리.
  </action>
  <verify>
    <automated>grep -c "ResolveSequenceName" WPF_Example/Custom/Sequence/SequenceHandler.cs returns at least 2 (정의 + 호출)</automated>
    <automated>grep -c "ResolveSequenceName" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs returns at least 1 (호출)</automated>
    <automated>grep -c "OwnerSequenceName" WPF_Example/Custom/Sequence/SequenceHandler.cs returns at least 2</automated>
    <automated>grep -c "AddShot(shotName, ownerSeqName" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs returns 1</automated>
    <automated>grep -c "260527 hbk Phase 35" WPF_Example/Custom/Sequence/SequenceHandler.cs returns at least 3</automated>
  </verify>
  <acceptance_criteria>
    - SequenceHandler.ResolveSequenceName static helper 정의 (4 case + default)
    - RebuildInspectionActions 가 targetSeqName 매칭으로 필터링
    - actionIdx 별도 사용 — EAction.FAI_Base + 시퀀스별 0/1/2 인덱스
    - InspectionListView.AddShotToSequence 가 ResolveSequenceName + AddShot(string, string) 사용
    - D-06 가드: InspectionSequence.cs / Action_FAIMeasurement.cs / VisionResponsePacket.cs 변경 0
  </acceptance_criteria>
  <done>per-sequence Shot ownership 모델 코드 레벨 완성. Bottom Shot 이 Top Action 으로 attach 되는 회귀 차단.</done>
</task>

<task type="auto">
  <name>Task 4: msbuild Debug/x64 검증 + D-06 가드 자동 검증</name>
  <files>(read-only — build artifact)</files>
  <read_first>(none — Task 1/2/3 완료 후 빌드 + git diff 검증)</read_first>
  <action>
앱이 실행 중이면 종료. 다음 명령 실행:

```cmd
msbuild WPF_Example\DatumMeasurement.csproj /t:Build /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal
```

D-06 가드 자동 검증:
```bash
git diff HEAD~3 HEAD -- \
  WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs \
  WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs \
  WPF_Example/TcpServer/VisionResponsePacket.cs
# (출력이 비어있어야 함 — 3 파일 모두 0 라인 변경)
```

검증 조건:
- msbuild exit code = 0
- "0 Error(s)"
- D-06 보호 3 파일 git diff = 0 라인
- 신규 warning 카테고리 = 0 (Plan 33-01 CS0618 5건 + Plan 35-01 변경 외)
  </action>
  <verify>
    <automated>msbuild exit code = 0</automated>
    <automated>"0 Error(s)" 라인</automated>
    <automated>InspectionSequence.cs / Action_FAIMeasurement.cs / VisionResponsePacket.cs git diff = 0 라인 (D-E1)</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 Build PASS, CS error 0
    - D-06 가드 통과 (3 파일 변경 0 라인)
    - 신규 warning 카테고리 0
  </acceptance_criteria>
  <done>Plan 35-02 코드 변경 PASS, Wave 2 자체 검증 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INI ↔ ShotConfig | OwnerSequenceName INI 키 추가가 기존 INI 호환성 깨뜨릴 가능성 |
| RecipeManager.Shots ↔ RebuildInspectionActions | 시퀀스별 필터링이 Top 의 기존 Shot 매핑 회귀 가능성 |
| AddShotToSequence ↔ AddShot | 시그니처 변경이 다른 호출 site 영향 가능성 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-02-01 | Tampering | 기존 Phase 6 INI 로드 시 OwnerSequenceName 누락 → 빈값 → Side/Bottom 시퀀스가 빈 actions | mitigate | ApplyShotDefaults() 의 SEQ_TOP 폴백 — 기존 Top 만 사용한 INI 는 100% Top 매핑 (회귀 0) |
| T-35-02-02 | Tampering | RebuildInspectionActions 의 actionIdx 가 EAction.FAI_Base + N 부여 → Side 와 Bottom 이 동일 EAction 충돌 | mitigate | actionIdx 가 시퀀스별 로컬 → 같은 시퀀스 내에서만 unique. Side seq 0/1/2 vs Bottom seq 0/1/2 는 SequenceBase 내부에서 분리됨. ResolveRunnableAction 의 seq[shotIdx].ID 매핑이 시퀀스 내부 인덱스 사용 — 회귀 0 |
| T-35-02-03 | Tampering | AddShot 시그니처 변경으로 다른 호출 site 회귀 | mitigate | seqName default = "" 로 기존 호출 호환 (`AddShot(shotName)` 그대로 동작) — Part B 의 InspectionListView 만 명시 전달 |
| T-35-02-04 | Information Disclosure | Bottom Shot 이 Top 시퀀스에 잘못 attach 되어 검사 시 Top 카메라로 잡힘 | mitigate | OwnerSequenceName 매칭 가드 — Top 시퀀스 RebuildInspectionActions 는 OwnerSequenceName=TOP 만 매칭 |
| T-35-02-05 | Denial-of-Service | InspectionSequence.cs / Action_FAIMeasurement.cs 변경 0 보장 위반 (D-06) | mitigate | Task 4 의 git diff 자동 검증 |
</threat_model>

<verification>
### 자동 검증 (Task 4)
- msbuild Debug/x64 Build PASS
- D-06 가드 git diff = 0 라인

### Plan 35-03 UAT 인계
- Bottom Shot 추가 → Save → 재로드 → Bottom 시퀀스 트리 아래 정상 복원 (CO-33-06 핵심 검증)
- Top + Side + Bottom 각 시퀀스 Shot 추가 후 Save → INI 의 SHOT_n 섹션에 OwnerSequenceName=SIDE/BOTTOM 자동 직렬화 확인
- 기존 INI (Phase 33 이전) 로드 시 모든 Shots 가 Top 으로 자동 폴백 (회귀 0)

### 잔여 가능성
- Part D (트리 재구축 시 OwnerSequenceName 필터링) 가 Plan 35-03 UAT 결과로 hotfix 필요할 수 있음
</verification>

<success_criteria>
1. ShotConfig.OwnerSequenceName (string) 필드 + [Category("Shot|Identity")]
2. ShotConfig.ApplyShotDefaults() 헬퍼 (빈값 → "TOP" 폴백)
3. InspectionRecipeManager.AddShot(string, string) overload
4. LoadPhase6Format 의 shot.Load 다음에 ApplyShotDefaults 호출
5. SequenceHandler.ResolveSequenceName(ESequence) static helper
6. RebuildInspectionActions 가 OwnerSequenceName 필터링
7. InspectionListView.AddShotToSequence 가 ResolveSequenceName + AddShot(string, string) 사용
8. msbuild Debug/x64 Build PASS, CS error 0, 신규 warning 0
9. D-06 가드: 3 파일 변경 0 라인 (자동 git diff 검증)
10. Top 회귀 0 — Phase 33 이전 INI 로드 시 모든 Shots 가 Top 폴백, RebuildInspectionActions(Top) 매핑 동일
</success_criteria>

<output>
After completion, create `.planning/phases/35-side-bottom-uat-and-phase33-completion/35-02-SUMMARY.md` documenting:
  - 4 파일 변경 라인 수
  - ShotConfig 신규 필드 + 헬퍼 명세
  - msbuild Build 결과
  - D-06 가드 git diff 증명
  - Plan 35-03 UAT 인계 사항 (Bottom Shot 재로드 핵심 검증)
  - Part D (트리 재구축 필터링) 의 hotfix 가능성 명시
</output>
