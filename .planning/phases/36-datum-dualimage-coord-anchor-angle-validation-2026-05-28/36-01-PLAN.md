---
phase: 36-datum-dualimage-coord-anchor-angle-validation-2026-05-28
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
autonomous: true
requirements:
  - SC-36-5
  - D-36-01
  - D-36-02
  - D-36-03
  - D-36-04
tags: [phase36, datum, dualimage, sameframe, guard, halcon]

must_haves:
  truths:
    - "TryFindDatum DualImage 오버로드 진입부에서 imageHorizontal/imageVertical width·height 불일치 시 명확한 영문 에러 메시지로 false 반환한다"
    - "TryTeachDatum DualImage 오버로드도 Find 와 동일한 SameFrame 가드를 동일 메시지 포맷으로 수행한다"
    - "1-image algorithm (TwoLineIntersect / VerticalTwoHorizontal / CircleTwoHorizontal) 회귀 0 — 단일-이미지 TryFindDatum / TryTeachDatum 오버로드 본문 변경 0 라인"
    - "msbuild Debug/x64 PASS, 신규 warning 0"
  artifacts:
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "SameFrame 진입부 가드 (Find DualImage L71 오버로드 + Teach DualImage L796 오버로드)"
      contains: "DualImage requires same-frame image pair"
  key_links:
    - from: "TryFindDatum(HImage, HImage, DatumConfig, out HTuple, out string)"
      to: "TryFindVerticalTwoHorizontalDualImage 진입 직전"
      via: "SameFrame 가드 7-라인 블록 (GetImageSize × 2 + width/height 비교 + error 반환)"
      pattern: "//260528 hbk Phase 36 D-36-03"
    - from: "TryTeachDatum(HImage, HImage, DatumConfig, out string)"
      to: "TryTeachVerticalTwoHorizontalDualImage 진입 직전"
      via: "Find 와 1:1 대칭 SameFrame 가드"
      pattern: "//260528 hbk Phase 36 D-36-03"
---

<objective>
DualImage 오버로드 (Find / Teach) 진입부에 SameFrame 가드를 1:1 대칭으로 삽입하여 두 이미지의 sensor frame 불일치를 즉시 차단한다.

Purpose: CO-34.1-09 "좌표계 통합 부재" 진단의 진짜 종결 — D-36-01/02 합의 (동일 카메라 + 다른 조명/Z 가정, 좌표 변환 0) 를 코드 가드로 명시 잠금하여 차후 다른 카메라 페어 입력 시 IntersectionLl 의 의미 없는 origin/angle 산출을 방지.
Output: DatumFindingService.cs 의 Find DualImage 오버로드 (L71-92) 와 Teach DualImage 오버로드 (L796-815) 각 진입부에 7-라인 가드 블록 추가 (총 ~14 라인). 1-image 오버로드 (L17-63 Find, L750-790 Teach) 영향 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-CONTEXT.md
@.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- 가드 진입부 패턴 (PATTERNS.md L43-105 추출) -->
<!-- Self-analog: 기존 TryFindDatum DualImage 오버로드 (L71-92) 의 null 가드 + algorithm 가드 직후 위치 -->

기존 본문 (DatumFindingService.cs L71-92):
```csharp
public bool TryFindDatum(HImage imageHorizontal, HImage imageVertical, DatumConfig config, out HTuple transform, out string error) //260527 hbk Phase 34 D-34-01/02
{
    error = null;
    HOperatorSet.HomMat2dIdentity(out transform);

    if (imageHorizontal == null || imageVertical == null || config == null)
    {
        error = "image(s) or config is null";
        return false;
    }

    config.EnsurePerRoiDefaults();
    config.LastFindSucceeded = false;

    if (config.AlgorithmTypeEnum != EDatumAlgorithm.VerticalTwoHorizontalDualImage)
    {
        error = "Algorithm is not VerticalTwoHorizontalDualImage; use single-image TryFindDatum overload";
        return false;
    }

    return TryFindVerticalTwoHorizontalDualImage(imageHorizontal, imageVertical, config, out transform, out error);
}
```

Allman brace style (Halcon 모듈 컨벤션 — PATTERNS.md L543-546)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TryFindDatum DualImage 오버로드 SameFrame 가드 삽입</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs L17-95 (단일-이미지 TryFindDatum 오버로드 본문 + DualImage 오버로드 본문)
    - .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-CONTEXT.md (D-36-01 ~ D-36-04 lock 확인)
    - .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-PATTERNS.md §"1) Find DualImage 진입부 SameFrame 가드" (정확한 가드 라인 추출)
    - PATTERNS.md §"Brace Style — Halcon 모듈 (Allman)" (가드 블록 brace 스타일 확인)
  </read_first>
  <action>
    DualImage Find 오버로드 (`public bool TryFindDatum(HImage imageHorizontal, HImage imageVertical, DatumConfig config, out HTuple transform, out string error)`) 의 `if (config.AlgorithmTypeEnum != EDatumAlgorithm.VerticalTwoHorizontalDualImage)` 가드 **직전** (즉 `config.LastFindSucceeded = false;` 다음 라인) 에 다음 7-라인 SameFrame 가드 블록 삽입 (Allman brace, D-36-03):

    ```csharp
    // 260528 hbk Phase 36 D-36-03 — SameFrame 가드: DualImage 두 입력은 동일 sensor frame 가정 (D-36-01: 동일 카메라 + 다른 조명/Z, D-36-02: 좌표 변환 0).
    //   width/height 불일치 시 IntersectionLl 의 두 픽셀 좌표가 다른 평면이 되어 의미 없는 origin/angle 산출 → 즉시 차단.
    HTuple wH, hH, wV, hV; //260528 hbk Phase 36 D-36-03
    imageHorizontal.GetImageSize(out wH, out hH); //260528 hbk Phase 36 D-36-03
    imageVertical.GetImageSize(out wV, out hV); //260528 hbk Phase 36 D-36-03
    if (wH.I != wV.I || hH.I != hV.I) //260528 hbk Phase 36 D-36-03
    {
        error = "DualImage requires same-frame image pair: horizontal " + wH.I + "x" + hH.I + " vs vertical " + wV.I + "x" + hV.I; //260528 hbk Phase 36 D-36-03
        return false; //260528 hbk Phase 36 D-36-03
    }
    ```

    위치 제약 (D-36-03):
    - null 가드 통과 직후 — 그래야 `imageHorizontal.GetImageSize(...)` 가 NullReferenceException 안전.
    - `config.EnsurePerRoiDefaults()` 와 `config.LastFindSucceeded = false;` 직후, algorithm 가드 직전 — 가드 실패 시 LastFindSucceeded 가 이미 false 인 상태 유지.
    - 단일-이미지 `TryFindDatum(HImage image, ...)` 오버로드 (L17-63) 본문은 **0 라인 변경** — 1-image 회귀 표면 없음 (D-36-04 가드 정신).
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "(Select-String -Path 'WPF_Example/Halcon/Algorithms/DatumFindingService.cs' -Pattern '//260528 hbk Phase 36 D-36-03').Count -ge 7"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "//260528 hbk Phase 36 D-36-03" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 가 ≥ 7 라인 반환 (가드 블록 7라인)
    - `grep -n "DualImage requires same-frame image pair:" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 가 정확히 1라인 반환 (Find 분기)
    - `grep -c "TryFindDatum(HImage image," WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 가 1 (단일-이미지 오버로드 본문 미변경 확인 — 시그니처 그대로)
    - 가드 블록이 `if (config.AlgorithmTypeEnum != EDatumAlgorithm.VerticalTwoHorizontalDualImage)` 직전에 위치 (수동 코드 리뷰)
  </acceptance_criteria>
  <done>
    SameFrame width/height 불일치 입력 시 영문 에러 메시지 ("DualImage requires same-frame image pair: ...") 로 false 반환. 단일-이미지 Find 오버로드 영향 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: TryTeachDatum DualImage 오버로드 SameFrame 가드 미러링</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs L750-820 (단일-이미지 TryTeachDatum + DualImage Teach 오버로드 본문)
    - .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-PATTERNS.md §"2) Teach DualImage 진입부 SameFrame 가드" (Find 미러링 패턴)
    - Task 1 의 변경 라인 (방금 추가한 Find 가드 — Teach 와 1:1 메시지 포맷 동일 보장)
  </read_first>
  <action>
    DualImage Teach 오버로드 (`public bool TryTeachDatum(HImage imageHorizontal, HImage imageVertical, DatumConfig config, out string error)`) 의 algorithm 가드 직전 위치에 Find 와 100% 대칭 7-라인 가드를 미러링 삽입 (D-36-03):

    ```csharp
    // 260528 hbk Phase 36 D-36-03 — SameFrame 가드 (Find 와 대칭). 에러 메시지 포맷 동일 보장 — 사용자 진단 일관성.
    HTuple wH, hH, wV, hV; //260528 hbk Phase 36 D-36-03
    imageHorizontal.GetImageSize(out wH, out hH); //260528 hbk Phase 36 D-36-03
    imageVertical.GetImageSize(out wV, out hV); //260528 hbk Phase 36 D-36-03
    if (wH.I != wV.I || hH.I != hV.I) //260528 hbk Phase 36 D-36-03
    {
        error = "DualImage requires same-frame image pair: horizontal " + wH.I + "x" + hH.I + " vs vertical " + wV.I + "x" + hV.I; //260528 hbk Phase 36 D-36-03
        return false; //260528 hbk Phase 36 D-36-03
    }
    ```

    위치 제약 (D-36-03):
    - null 가드 통과 직후, EnsurePerRoiDefaults 및 LastTeachSucceeded 초기화 직후, algorithm 가드 직전.
    - **에러 메시지 포맷은 Find 와 byte-identical** — 사용자 진단 시 동일 메시지 → 진단 일관성.
    - 단일-이미지 `TryTeachDatum(HImage image, ...)` 오버로드 본문 0 라인 변경.

    msbuild 검증:
    - PowerShell: `& "$env:ProgramFiles (x86)\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build` 또는 vswhere fallback.
    - 신규 warning 0 (Phase 36 baseline).
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "(Select-String -Path 'WPF_Example/Halcon/Algorithms/DatumFindingService.cs' -Pattern 'DualImage requires same-frame image pair:').Count -eq 2"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DualImage requires same-frame image pair:" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 가 정확히 2 (Find + Teach 각 1회)
    - `grep -n "//260528 hbk Phase 36 D-36-03" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 가 ≥ 14 라인 반환 (Find 7 + Teach 7)
    - msbuild Debug/x64 PASS, exit code 0
    - msbuild 출력에 신규 warning 0 (이전 baseline 대비 동일 또는 감소)
    - 단일-이미지 TryTeachDatum 시그니처 (`HImage image,`) grep 결과 라인 본문 변경 0 (git diff 로 확인)
  </acceptance_criteria>
  <done>
    Find/Teach DualImage 양쪽 오버로드가 SameFrame 가드 통과 시에만 본 알고리즘 진입. width/height 불일치 시 동일 에러 메시지로 즉시 false 반환. 1-image 회귀 0. msbuild PASS.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (해당 없음) | 내부 알고리즘/UI 변경, 외부 surface 영향 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01-01 | Information Disclosure | DatumFindingService.cs SameFrame 에러 메시지 | accept | 메시지에는 이미지 width/height 만 노출 (PII/secret 0). 내부 진단 로그 한정. |
| T-36-01-02 | Denial of Service | DualImage 오버로드 진입부 GetImageSize 호출 | accept | HImage.GetImageSize 는 O(1) — 이미지 헤더 metadata 만 읽음. 추가 비용 0. |

(Phase 36 은 내부 알고리즘 변경. 네트워크/인증/외부 파일 입력 영향 0.)
</threat_model>

<verification>
- DualImage Find/Teach 호출 시 width/height 불일치 입력 → "DualImage requires same-frame image pair: ..." 에러 메시지 반환 (Find/Teach 동일 포맷)
- 단일-이미지 TryFindDatum / TryTeachDatum 오버로드 본문 변경 0 라인 (git diff 검증)
- msbuild Debug/x64 PASS, 신규 warning 0
- 가드 블록이 정확한 위치 (null 가드 직후, algorithm 가드 직전) 에 삽입됨 (수동 리뷰)
</verification>

<success_criteria>
SC-36-5 (기존 1-image algorithm 3종 + DualImage 회귀 0 — 가드 4파일 변경 0 유지) 의 DatumFindingService 진입부 가드 표면 확보. CO-34.1-09 종결의 **첫 번째 코드 가드**.
</success_criteria>

<output>
After completion, create `.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-01-SUMMARY.md`
</output>
