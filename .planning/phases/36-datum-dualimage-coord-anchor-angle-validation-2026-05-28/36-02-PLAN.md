---
phase: 36-datum-dualimage-coord-anchor-angle-validation-2026-05-28
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/EAngleValidationStatus.cs
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements:
  - SC-36-2
  - SC-36-4
  - D-36-05
  - D-36-06
  - D-36-07
  - D-36-08
  - D-36-12
  - D-36-13
tags: [phase36, datum, dualimage, expectedangle, tolerance, transient, ini]

must_haves:
  truths:
    - "DatumConfig 에 ExpectedAngleDeg + AngleTolerance public double 필드 2종이 [Category('Datum|Algorithm')] 으로 선언되어 있다"
    - "DatumConfig 에 AngleValidationStatus transient 필드가 3-종 데코 (System.ComponentModel.Browsable(false) + PropertyTools Browsable(false) + JsonIgnore) 와 함께 선언되어 있다"
    - "IsHiddenForAlgorithm switch 의 DualImage 외 3 case (TwoLineIntersect / CircleTwoHorizontal / VerticalTwoHorizontal) 각각이 ExpectedAngleDeg/AngleTolerance 를 hide 처리한다 — DualImage case 만 노출"
    - "TryFindVerticalTwoHorizontalDualImage 가 DetectedAngleDeg write-back 직후 AngleTolerance > 0 sentinel 게이트를 평가해 AngleValidationStatus 를 Pass/Fail/None 으로 write-back 한다"
    - "wrap-around 공식 |((Detected - Expected + 540) % 360) - 180| ≤ Tolerance 으로 179° vs -179° = 2° 정상 판정한다"
    - "INI 라운드트립 — ExpectedAngleDeg=값 / AngleTolerance=값 키가 Save 후 Load 시 보존 (ParamBase Double case 자동)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/EAngleValidationStatus.cs"
      provides: "EAngleValidationStatus enum (None/Pass/Fail) 신규 파일"
      contains: "enum EAngleValidationStatus"
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "ExpectedAngleDeg + AngleTolerance 입력 필드 2종 + AngleValidationStatus transient + Hide 분기 3-라인"
      contains: "public double ExpectedAngleDeg"
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "TryFindVerticalTwoHorizontalDualImage 의 sentinel + wrap-around 평가 블록 (~10 라인)"
      contains: "config.AngleValidationStatus = EAngleValidationStatus.Pass"
    - path: "WPF_Example/DatumMeasurement.csproj"
      provides: "EAngleValidationStatus.cs Compile ItemGroup 등록 (Phase 12 D-Rule3 패턴)"
      contains: "EAngleValidationStatus.cs"
  key_links:
    - from: "DatumConfig.ExpectedAngleDeg / AngleTolerance"
      to: "TryFindVerticalTwoHorizontalDualImage L720 직후 sentinel 평가"
      via: "AngleTolerance > 0 게이트 + wrap-around 정규화"
      pattern: "//260528 hbk Phase 36 D-36-08"
    - from: "AngleValidationStatus transient"
      to: "PropertyGrid 배지 (Plan 03 소비처)"
      via: "Phase 17 D-13 3-종 데코 (Browsable false × 2 + JsonIgnore)"
      pattern: "//260528 hbk Phase 36 D-36-08"
    - from: "IsHiddenForAlgorithm switch (4 case)"
      to: "DatumConfig PropertyGrid 노출 필터"
      via: "DualImage 외 3 case 에 hide 1-라인씩 추가 + DualImage case 0 라인 (default 노출)"
      pattern: "//260528 hbk Phase 36 D-36-05"
---

<objective>
DatumConfig 에 ExpectedAngleDeg / AngleTolerance 입력 필드 2종 + AngleValidationStatus transient 1종을 추가하고, TryFindVerticalTwoHorizontalDualImage 의 DetectedAngleDeg write-back 직후 sentinel + wrap-around 게이트로 PASS/FAIL/None 을 자동 평가한다.

Purpose: SC-36-2 (사용자 기댓값 입력 + PASS/FAIL 비교) + SC-36-4 (INI 라운드트립) 의 모델/알고리즘 표면 구축. CO-34.1-09 "각도 검증 UX 부재" 해결의 데이터 모델 + 평가 로직 단.
Output: 신규 enum 파일 1개 (EAngleValidationStatus.cs) + DatumConfig.cs 의 필드 3개 + Hide 분기 3-라인 + DatumFindingService.cs 의 평가 블록 (~10 라인). PropertyGrid 색상 배지 + 시각화는 Plan 03 분담.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-CONTEXT.md
@.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- DatumConfig 의 Phase 17 D-13 transient 3-종 데코 (PATTERNS.md L209-217 추출) -->
```csharp
[System.ComponentModel.Browsable(false)] //260503 hbk Phase 17 D-13
[PropertyTools.DataAnnotations.Browsable(false)] //260503 hbk Phase 17 D-13
[Newtonsoft.Json.JsonIgnore] //260503 hbk Phase 17 D-13
public double DetectedOriginRow { get; set; } //260503 hbk Phase 17 D-13
```

<!-- TwoLineAngleToleranceDeg sentinel 게이트 패턴 (DatumFindingService.cs L915-933) -->
```csharp
if (config.TwoLineAngleToleranceDeg > 0.0)
{
    double phi1 = Math.Atan2(...);
    double phi2 = Math.Atan2(...);
    double deltaDeg = Math.Abs((phi1 - phi2) * 180.0 / Math.PI);
    while (deltaDeg >= 180.0) deltaDeg -= 180.0;
    ...
}
```

<!-- IsHiddenForAlgorithm switch 4 case (DatumConfig.cs L675-703) -->
각 case 마다 `if (name == "TeachingImagePath_Vertical") return true;` 라인이 존재 — Phase 36 은 동일 위치에 `if (name == "ExpectedAngleDeg" || name == "AngleTolerance") return true;` 추가 (DualImage case 만 제외).

<!-- DatumConfig brace style: K&R (DatumConfig.cs L527-631 EnsurePerRoiDefaults 본문). 필드 선언은 single-line statement -->
<!-- DatumFindingService brace style: Allman (Halcon 모듈 컨벤션) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EAngleValidationStatus enum 신규 파일 + csproj 등록</name>
  <files>
    WPF_Example/Custom/Sequence/Inspection/EAngleValidationStatus.cs,
    WPF_Example/DatumMeasurement.csproj
  </files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/EDatumAlgorithm.cs (분리 enum 파일 패턴 — 신규 파일 컨벤션 답습)
    - WPF_Example/DatumMeasurement.csproj 의 `Custom/Sequence/Inspection/EDatumAlgorithm.cs` Compile 항목 위치 (Phase 12 D-Rule3 auto-fix 학습)
    - .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-PATTERNS.md §"5) transient AngleValidationStatus" Claude's Discretion (None/Pass/Fail 3-값 enum 권장)
    - .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-CONTEXT.md (D-36-08 transient 결정)
  </read_first>
  <action>
    **신규 파일** `WPF_Example/Custom/Sequence/Inspection/EAngleValidationStatus.cs` 작성:

    ```csharp
    //260528 hbk Phase 36 D-36-08 — DualImage Test Find 직후 검출 각도 PASS/FAIL 평가 상태.
    //  None = 미평가 (AngleTolerance == 0.0 sentinel, D-36-13).
    //  Pass = |Detected - Expected| ≤ AngleTolerance (wrap-around 정규화 후).
    //  Fail = 오차 초과.
    //  사용처: DatumConfig.AngleValidationStatus transient (INI/JSON 직렬화 제외 — Phase 17 D-13 3-종 데코).
    //  소비처: HalconDisplayService DrawExpectedAngleArrow (색상 분기) + PropertyGrid 색상 배지 (Plan 03).
    namespace ReringProject.Sequence
    {
        public enum EAngleValidationStatus
        {
            None = 0, //260528 hbk Phase 36 D-36-08 — sentinel: 미평가 (default 값 보장)
            Pass = 1, //260528 hbk Phase 36 D-36-08
            Fail = 2, //260528 hbk Phase 36 D-36-08
        }
    }
    ```

    **csproj 등록** (Phase 12 D-Rule3 학습 — 신규 .cs 파일 누락 시 CS0246 빌드 차단):
    - `WPF_Example/DatumMeasurement.csproj` 안에서 기존 `<Compile Include="Custom\Sequence\Inspection\EDatumAlgorithm.cs" />` 를 찾고, 같은 ItemGroup 의 알파벳 순서에 맞추어 다음 한 줄 추가:

    ```xml
    <Compile Include="Custom\Sequence\Inspection\EAngleValidationStatus.cs" />
    ```

    (백슬래시 사용 — Windows MSBuild 컨벤션. 기존 EDatumAlgorithm 항목과 동일 포맷 답습.)

    제약:
    - namespace `ReringProject.Sequence` — EDatumAlgorithm.cs 와 동일 (DatumConfig 가 zero-import 로 사용 가능, 32-PATTERNS.md `Custom/` 폴더 namespace 답습).
    - enum underlying type 기본 int — 명시 안 함 (CLR 기본).
    - None 을 0 으로 명시 — `default(EAngleValidationStatus) == None` 보장 (transient 필드 미초기화 시 자동 None 상태).
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "if ((Test-Path 'WPF_Example/Custom/Sequence/Inspection/EAngleValidationStatus.cs') -and ((Select-String -Path 'WPF_Example/DatumMeasurement.csproj' -Pattern 'EAngleValidationStatus.cs').Count -ge 1)) { exit 0 } else { exit 1 }"
    </automated>
  </verify>
  <acceptance_criteria>
    - `Test-Path WPF_Example/Custom/Sequence/Inspection/EAngleValidationStatus.cs` 가 True
    - `grep -c "enum EAngleValidationStatus" WPF_Example/Custom/Sequence/Inspection/EAngleValidationStatus.cs` 가 1
    - `grep -c "None = 0" WPF_Example/Custom/Sequence/Inspection/EAngleValidationStatus.cs` 가 1 (default 값 명시)
    - `grep -c "EAngleValidationStatus.cs" WPF_Example/DatumMeasurement.csproj` 가 1 (Compile 항목 등록)
    - `grep -c "namespace ReringProject.Sequence" WPF_Example/Custom/Sequence/Inspection/EAngleValidationStatus.cs` 가 1
    - 신규/변경 라인 모두 `//260528 hbk Phase 36 D-36-08` 주석 부착 — grep 결과 ≥ 3 라인
  </acceptance_criteria>
  <done>
    EAngleValidationStatus enum 신규 파일이 csproj 에 등록되어 CS0246 없이 컴파일 가능. Plan 02 의 나머지 Task 2/3 이 본 enum 을 참조할 수 있다.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DatumConfig 필드 3종 + Hide 분기 3-라인 추가</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs L100-115 (TwoLineAngleToleranceDeg 게이트 sentinel 필드 — ExpectedAngleDeg/AngleTolerance 답습 모델)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs L460-505 (transient 3-종 데코 패턴 — Phase 17 D-13)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs L675-703 (IsHiddenForAlgorithm 4 case switch — Hide 분기 추가 위치)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs L527-631 (EnsurePerRoiDefaults — Plan 02 sentinel 정규화 위치 검토)
    - .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-PATTERNS.md §"4) 입력 필드 2종" + §"5) transient" + §"7) IsHiddenForAlgorithm Hide 분기" (정확한 코드 추출)
    - .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-CONTEXT.md (D-36-05/07/08/12/13)
  </read_first>
  <action>
    **세 군데 변경** (브레이스: DatumConfig K&R 답습).

    **(A) 입력 필드 2종 — `TwoLineAngleToleranceDeg` (L112) 직후 삽입** (D-36-05/07/12):

    ```csharp
    //260528 hbk Phase 36 D-36-05/07/12 — DualImage 검출 각도 검증 기댓값 (도, range hint [-180, 180] atan2 출력과 일치).
    //  알고리즘 무관 일률 직렬화 (Phase 22 IMG-01 / D-34-11 패턴) — INI 키 미존재 시 ParamBase Double case 가 0.0 fallback.
    //  PropertyGrid 노출 = VerticalTwoHorizontalDualImage 한정 (IsHiddenForAlgorithm 의 다른 case 에서 hide).
    //  ParamBase reflection 자동 직렬화 (Double case) — 별도 Save/Load 코드 불필요.
    [Category("Datum|Algorithm")]
    public double ExpectedAngleDeg { get; set; } = 0.0; //260528 hbk Phase 36 D-36-05/07

    //260528 hbk Phase 36 D-36-05/07/13 — 각도 검증 허용 오차 (도).
    //  0.0 = sentinel (게이트 off, AngleValidationStatus=None). > 0.0 = 활성 (TwoLineAngleToleranceDeg L915 패턴과 정렬).
    //  range hint: 0~45°. default 1.0° (D-36-07).
    //  Expected=0.0 + Tolerance>0 도 활성 — 사용자가 0° 검증을 의도한 케이스 (D-36-13 단일 sentinel 모델).
    [Category("Datum|Algorithm")]
    public double AngleTolerance { get; set; } = 1.0; //260528 hbk Phase 36 D-36-05/07/13
    ```

    **(B) transient AngleValidationStatus — `DetectedRefAngle2` (L487) 직후 삽입** (D-36-08, Phase 17 D-13 3-종 데코 1:1):

    ```csharp
    //260528 hbk Phase 36 D-36-08 — Test Find 직후 각도 PASS/FAIL 평가 결과 (transient).
    //  TryFindVerticalTwoHorizontalDualImage 가 DetectedAngleDeg write-back 직후 본 필드 갱신.
    //  None = 미평가 (Tolerance==0 sentinel) / Pass / Fail. PropertyGrid 색상 배지 컨버터 입력 (Plan 03).
    //  INI/JSON 직렬화 제외 — Phase 17 D-13 3-종 데코 답습 (System.ComponentModel + PropertyTools + JsonIgnore).
    //  주의: ParamBase 가 enum 직렬화 미지원 (Phase 12 D-09 / EDatumAlgorithm string-based 회피) — Browsable 데코 없이도 INI write 안 됨. 데코는 PropertyGrid hiding 만 담당.
    [System.ComponentModel.Browsable(false)] //260528 hbk Phase 36 D-36-08
    [PropertyTools.DataAnnotations.Browsable(false)] //260528 hbk Phase 36 D-36-08
    [Newtonsoft.Json.JsonIgnore] //260528 hbk Phase 36 D-36-08
    public ReringProject.Sequence.EAngleValidationStatus AngleValidationStatus { get; set; } //260528 hbk Phase 36 D-36-08
    ```

    (DatumConfig 파일의 기존 `using` 컨벤션에 따라 fully-qualified name 사용 — Phase 28 Plan 01 의 `System.StringComparison` 답습. namespace 가 동일 `ReringProject.Sequence` 면 단순 `EAngleValidationStatus` 도 가능 — DatumConfig 의 namespace 확인 후 적합 형태 채택.)

    **(C) IsHiddenForAlgorithm Hide 분기 3-라인 — L678 / L684 / L691 각 case 에 추가** (D-36-05):

    각 비-DualImage case 의 `if (name == "TeachingImagePath_Vertical") return true;` **직후** 라인에 다음 1줄씩 추가 (총 3 case):

    ```csharp
    if (name == "ExpectedAngleDeg" || name == "AngleTolerance") return true; //260528 hbk Phase 36 D-36-05 — DualImage 전용 필드 hide
    ```

    - `case EDatumAlgorithm.TwoLineIntersect:` 본문에 1줄
    - `case EDatumAlgorithm.CircleTwoHorizontal:` 본문에 1줄
    - `case EDatumAlgorithm.VerticalTwoHorizontal:` 본문에 1줄
    - `case EDatumAlgorithm.VerticalTwoHorizontalDualImage:` 본문은 **0 라인 변경** (default fallthrough 후 `return false` → 노출).

    제약 (D-36-12 / D-36-13):
    - **EnsurePerRoiDefaults 변경 0 라인** — double 은 null 불가, ParamBase Double case 의 IniValue.Default → 0.0 SetValue 가 이미 sentinel 의미 (D-36-13) 와 일치. PATTERNS.md §6 의 "본 라인은 실제로는 추가 안 해도 무방" 권고에 따름.
      - **결정 근거 (checker WARNING-3 반영, 2026-05-28):** double 의 CLR default 0.0 이 D-36-13 sentinel (Tolerance > 0 활성화) 의미와 자동 일치 → EnsurePerRoiDefaults 본문 변경 0 정당화. Phase 22 IMG-01 string 필드 null 정규화의 1:1 답습은 형식적 정합 (string→null 처럼 double→0.0 정규화 불요). INI 키 부재 → ParamBase Double case fallback 0.0 → sentinel None → 검증 비활성 = 의도된 하위호환 동작.
    - **가드 4파일 변경 0 정신 보존** — DatumConfig.cs 는 변경 있음 (D-36-04 는 "메타 필드 추가 0" 의 가드인데 ExpectedAngleDeg/AngleTolerance 는 게이트 입력 필드이지 좌표계 메타가 아님 — PATTERNS.md File Classification 의 변경 라인 카운트 정당화 참조).
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "$f='WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs'; $exp=(Select-String -Path $f -Pattern 'public double ExpectedAngleDeg').Count; $tol=(Select-String -Path $f -Pattern 'public double AngleTolerance').Count; $sta=(Select-String -Path $f -Pattern 'AngleValidationStatus AngleValidationStatus').Count; $hide=(Select-String -Path $f -Pattern 'name == \"ExpectedAngleDeg\" \|\| name == \"AngleTolerance\"').Count; if ($exp -eq 1 -and $tol -eq 1 -and $sta -eq 1 -and $hide -eq 3) { exit 0 } else { Write-Host \"exp=$exp tol=$tol sta=$sta hide=$hide\"; exit 1 }"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public double ExpectedAngleDeg" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 가 1
    - `grep -c "public double AngleTolerance" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 가 1
    - `grep -c "EAngleValidationStatus AngleValidationStatus" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 가 1
    - `grep -c 'name == \"ExpectedAngleDeg\" || name == \"AngleTolerance\"' WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 가 정확히 3 (TLI/CTH/VTH 각 1회)
    - VerticalTwoHorizontalDualImage case 본문에서 위 hide 패턴 grep 결과 0 (수동: `case EDatumAlgorithm.VerticalTwoHorizontalDualImage:` 다음 라인부터 `return false;` 까지 hide 라인 없음)
    - DatumConfig L527-631 (`EnsurePerRoiDefaults`) 본문 변경 0 라인 (git diff 로 검증)
    - 신규/변경 라인 모두 `//260528 hbk Phase 36 D-36-` 주석 부착 (grep ≥ 10)
  </acceptance_criteria>
  <done>
    ExpectedAngleDeg/AngleTolerance 가 DualImage 알고리즘 선택 시 PropertyGrid 에 노출되고 다른 3 알고리즘에서는 숨김. AngleValidationStatus transient 가 INI/JSON 직렬화 제외 상태로 선언. 기존 1-image algorithm PropertyGrid 회귀 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: TryFindVerticalTwoHorizontalDualImage sentinel + wrap-around 평가 블록 삽입</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs L700-740 (TryFindVerticalTwoHorizontalDualImage 의 DetectedAngleDeg write-back 위치 L720 인근)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs L915-933 (TwoLineAngleToleranceDeg sentinel 답습 모델)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs L796-815 (Plan 01 Task 2 에서 추가된 Teach 가드 — 본 평가 블록은 Teach 본문에는 추가 안 함 확인)
    - .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-PATTERNS.md §"3) Find 각도 평가 write-back" (정확한 코드)
    - .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-CONTEXT.md (D-36-08 / D-36-13 wrap-around 공식 권고)
  </read_first>
  <action>
    `TryFindVerticalTwoHorizontalDualImage` 본문 안에서 `config.DetectedAngleDeg = curAngle * 180.0 / Math.PI;` (L720 인근) 라인의 **직후** 위치 (`Line1Detected_RBegin = vrB;` L723 직전) 에 다음 10-라인 평가 블록 삽입 (D-36-08/13, Allman brace):

    ```csharp
    //260528 hbk Phase 36 D-36-08/13 — ExpectedAngleDeg / AngleTolerance 게이트 (TwoLineAngleToleranceDeg L915 sentinel 모델과 정렬).
    //  Tolerance > 0 일 때만 활성. Expected=0 + Tolerance>0 도 활성 (사용자가 0° 검증을 의도) — sentinel 단일 조건.
    //  결과는 transient AngleValidationStatus 에 기록. error 반환 안 함 (Find 자체는 PASS 유지 — UI 색상 배지로만 표시 — Plan 03).
    //  wrap-around: (Detected - Expected) 를 [-180, 180] 정규화 후 절댓값 비교 (179 vs -179 = 2° 차이로 정상 판정).
    if (config.AngleTolerance > 0.0) //260528 hbk Phase 36 D-36-13
    {
        double diff = config.DetectedAngleDeg - config.ExpectedAngleDeg; //260528 hbk Phase 36 D-36-08
        diff = ((diff + 540.0) % 360.0) - 180.0; //260528 hbk Phase 36 D-36-08 — wrap-around 정규화 (CONTEXT 권고 공식)
        double absDiff = System.Math.Abs(diff); //260528 hbk Phase 36 D-36-08
        if (absDiff <= config.AngleTolerance) //260528 hbk Phase 36 D-36-08
            config.AngleValidationStatus = ReringProject.Sequence.EAngleValidationStatus.Pass;
        else //260528 hbk Phase 36 D-36-08
            config.AngleValidationStatus = ReringProject.Sequence.EAngleValidationStatus.Fail;
    }
    else //260528 hbk Phase 36 D-36-13 — sentinel: 검증 비활성
    {
        config.AngleValidationStatus = ReringProject.Sequence.EAngleValidationStatus.None;
    }
    ```

    위치 제약 (D-36-08):
    - 정확한 위치 = `config.DetectedAngleDeg = curAngle * 180.0 / Math.PI;` **직후 다음 라인**. 이 라인이 L720 근방 (Phase 34/34.1 변경으로 행번호 약간 이동 가능 — Grep 으로 정확한 위치 확정).
    - **Teach 본문 (TryTeachVerticalTwoHorizontalDualImage)** 에는 추가 안 함 — D-36-08 = Test Find 직후 자동 평가 한정.
    - **error 반환 안 함** — Find 자체는 PASS 유지. TwoLineAngleToleranceDeg 와의 결정적 차이 (TLI 게이트는 false 반환, Phase 36 게이트는 status 만 write).
    - `EAngleValidationStatus` 사용 형태: fully-qualified `ReringProject.Sequence.EAngleValidationStatus.Pass` — DatumFindingService 가 `using ReringProject.Sequence;` 없으면 필요. 만약 이미 namespace 가 사용 중이면 단순 `EAngleValidationStatus.Pass` 도 OK (file 의 using 확인 후 결정).

    msbuild 검증:
    - PowerShell + vswhere fallback 으로 Debug/x64 Build.
    - 신규 warning 0.
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "$f='WPF_Example/Halcon/Algorithms/DatumFindingService.cs'; $tol=(Select-String -Path $f -Pattern 'config.AngleTolerance > 0.0').Count; $pass=(Select-String -Path $f -Pattern 'EAngleValidationStatus.Pass').Count; $fail=(Select-String -Path $f -Pattern 'EAngleValidationStatus.Fail').Count; $none=(Select-String -Path $f -Pattern 'EAngleValidationStatus.None').Count; $wrap=(Select-String -Path $f -Pattern '\(\(diff \+ 540.0\) % 360.0\) - 180.0').Count; if ($tol -ge 1 -and $pass -ge 1 -and $fail -ge 1 -and $none -ge 1 -and $wrap -ge 1) { exit 0 } else { Write-Host \"tol=$tol pass=$pass fail=$fail none=$none wrap=$wrap\"; exit 1 }"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "config.AngleTolerance > 0.0" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 가 ≥ 1
    - `grep -c "EAngleValidationStatus.Pass" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 가 정확히 1
    - `grep -c "EAngleValidationStatus.Fail" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 가 정확히 1
    - `grep -c "EAngleValidationStatus.None" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 가 정확히 1
    - `grep -c "((diff + 540.0) % 360.0) - 180.0" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 가 정확히 1 (wrap-around 공식 byte-identical)
    - 평가 블록이 `TryFindVerticalTwoHorizontalDualImage` 본문 내부에 위치 (수동 코드 리뷰)
    - **`TryTeachVerticalTwoHorizontalDualImage` 본문에는 평가 블록 없음** (수동 grep + 위치 확인)
    - msbuild Debug/x64 PASS, exit 0
    - msbuild 출력에 신규 warning 0 (Phase 36 baseline 대비)
    - 모든 변경 라인에 `//260528 hbk Phase 36 D-36-08` 또는 `//260528 hbk Phase 36 D-36-13` 주석 부착
  </acceptance_criteria>
  <done>
    Test Find DualImage 성공 시 AngleTolerance > 0 일 때 자동 PASS/FAIL 평가가 AngleValidationStatus 에 기록되며, sentinel (Tolerance==0) 시 None 으로 명시 reset. wrap-around 공식 통과 → 179° vs -179° = 2° 정상 판정. Find 의 PASS 여부 자체는 영향 받지 않음 (status 만 write).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (해당 없음) | 내부 알고리즘/모델 변경, 외부 surface 영향 0. INI 직렬화는 ParamBase reflection 자동 처리 — path traversal/injection 위험 없음 (double 필드 2종만). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02-01 | Tampering | INI 직렬화 ExpectedAngleDeg/AngleTolerance 값 위변조 | accept | INI 는 로컬 디스크. Phase 22 IMG-01 와 동일 신뢰 모델 — 운영자 수동 편집 위협 0 (single-user desktop). |
| T-36-02-02 | Information Disclosure | AngleValidationStatus transient | accept | 3-종 데코 (Browsable false × 2 + JsonIgnore) 가 PropertyGrid/JSON 노출 차단. UI 색상 배지 한정 (Plan 03). |
| T-36-02-03 | Repudiation | 검출 각도 PASS/FAIL 판정 로그 없음 | accept | Phase 36 스코프는 UI 표시 한정. 영구 로그/감사는 v1.2 OUT-* 시리즈 (Phase 25). |
</threat_model>

<verification>
- Plan 01 (SameFrame 가드) 와 본 plan 의 변경이 동시에 컴파일 통과 (Wave 1 → Wave 2 의존성 검증)
- INI 라운드트립 1차 검증: msbuild PASS 후 SIMUL 기동 → DualImage 선택 → ExpectedAngleDeg=5.0 / AngleTolerance=2.0 입력 → 종료 → 재기동 → 두 값 보존 (Plan 04 의 정식 UAT 와는 별도, 자체 smoke)
- 가드 4파일 변경 0 검증: `git diff --name-only` 가 ParamBase.cs / InspectionListView.xaml.cs / InspectionSequence.cs / VisionResponsePacket.cs 를 **포함하지 않음**
- 1-image algorithm 회귀 0: TwoLineIntersect / VerticalTwoHorizontal / CircleTwoHorizontal 의 IsHiddenForAlgorithm case 본문은 새 hide 라인 1줄씩만 증가 (그 외 변경 0)
</verification>

<success_criteria>
- SC-36-2 의 모델 + 평가 로직 단 충족 (PASS/FAIL 색상 배지 UI 표면은 Plan 03 분담)
- SC-36-4 의 INI 직렬화 표면 확보 (라운드트립 정식 UAT 는 Plan 04)
- D-36-05/06/07/08/12/13 모두 코드로 표현됨
- msbuild Debug/x64 PASS, 신규 warning 0
</success_criteria>

<output>
After completion, create `.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-02-SUMMARY.md`
</output>
