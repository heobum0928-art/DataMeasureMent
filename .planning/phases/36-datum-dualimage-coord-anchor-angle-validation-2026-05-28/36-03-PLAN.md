---
phase: 36-datum-dualimage-coord-anchor-angle-validation-2026-05-28
plan: 03
type: execute
wave: 3
depends_on: ["36-02"]
files_modified:
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/UI/ContentItem/MainView.xaml
autonomous: true
requirements:
  - SC-36-1
  - SC-36-3
  - D-36-06
  - D-36-09
  - D-36-10
  - D-36-11
tags: [phase36, datum, dualimage, visualization, fallback, propertygrid, badge]

must_haves:
  truths:
    - "HalconDisplayService 에 DrawOriginFallback 헬퍼가 신설되어 OFF-SCREEN 시 캔버스 중앙 fallback 십자 + (row, col) 텍스트 + 'OFF-SCREEN' 라벨을 렌더한다"
    - "HalconDisplayService 에 DrawExpectedAngleArrow 헬퍼가 신설되어 AngleTolerance > 0 시 ExpectedAngleDeg 방향 점선 화살표 (PASS=green / FAIL=red) 를 렌더한다"
    - "RenderDatumFindResult 가 DetectedOrigin 이 이미지 경계 밖일 때 fallback 헬퍼로 위임하고, 화면 안일 때 기존 purple 십자 + Expected 화살표를 함께 렌더한다"
    - "BtnSwapHorizontal_Click / BtnSwapVertical_Click 각각이 LoadImage 직후 halconViewer.SetDatumOverlay(d, true) 를 재호출하여 양쪽 캔버스 토글 시 시각화가 일관 유지된다"
    - "PropertyGrid 의 DetectedAngleDeg 셀 또는 동등 위치에 AngleValidationStatus 에 따른 색상 배지가 표시된다 (PASS=#43A047 / FAIL=#E53935 / None=default)"
    - "msbuild Debug/x64 PASS, 신규 warning 0"
  artifacts:
    - path: "WPF_Example/Halcon/Display/HalconDisplayService.cs"
      provides: "DrawOriginFallback + DrawExpectedAngleArrow 신규 헬퍼 2개 + RenderDatumFindResult OFF-SCREEN 가드"
      contains: "private void DrawOriginFallback"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "swap 핸들러 2개의 SetDatumOverlay 재호출 2-라인 추가"
      contains: "//260528 hbk Phase 36 D-36-09"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml"
      provides: "PropertyGrid 색상 배지 (옵션 — XAML-only ResourceDictionary 또는 셀 Style)"
      contains: "AngleValidationStatus"
  key_links:
    - from: "RenderDatumFindResult"
      to: "DrawOriginFallback / DrawExpectedAngleArrow"
      via: "OFF-SCREEN 가드 + AngleTolerance > 0 게이트 분기"
      pattern: "//260528 hbk Phase 36 D-36-10/11"
    - from: "BtnSwapHorizontal_Click / BtnSwapVertical_Click"
      to: "halconViewer.SetDatumOverlay(d, true) 재호출"
      via: "swap 직후 RenderDatumOverlay → RenderDatumFindResult chain 자동 재실행"
      pattern: "//260528 hbk Phase 36 D-36-09"
    - from: "AngleValidationStatus transient"
      to: "PropertyGrid 색상 배지 표시"
      via: "XAML ResourceDictionary / IValueConverter / DataTrigger (planner 옵션 선택)"
      pattern: "//260528 hbk Phase 36 D-36-06"
---

<objective>
HalconDisplayService 에 OFF-SCREEN fallback + Expected angle 점선 화살표 헬퍼 2개를 신설하고, MainView swap 토글 시 RenderDatumFindResult 재호출을 트리거하며, PropertyGrid 색상 배지를 가드 4파일 외 경로로 적용한다.

Purpose: SC-36-1 (실측 페어로 DetectedOrigin 이 시각적으로 의미 있는 위치) + SC-36-3 (시각화 강화 / OFF-SCREEN fallback) + D-36-06 (PASS/FAIL 색상 배지) 충족. CO-34.1-09 "정확히 되었는지 안되었는지 판단 불가" 의 시각화 단 종결.
Output: HalconDisplayService.cs 의 헬퍼 2개 (~40 라인) + RenderDatumFindResult 보강 (~5 라인) + MainView.xaml.cs swap 핸들러 2-라인 + MainView.xaml 색상 배지 (XAML-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-CONTEXT.md
@.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- RenderDatumFindResult 현재 본문 시그니처 (HalconDisplayService.cs L307-350, PATTERNS.md §8 추출) -->
```csharp
public void RenderDatumFindResult(HWindow window, DatumConfig datum)
{
    if (window == null || datum == null) return;
    if (!datum.LastFindSucceeded) return;
    try
    {
        HOperatorSet.SetColor(window, "purple");
        HOperatorSet.SetLineWidth(window, 2);
        const double crossHalf = 14.0;
        // purple cross + 좌표 텍스트 + DetectedRefAngle 화살표 (length=20, head=5)
        ...
    }
    catch { /* Suppress display errors */ }
}
```

<!-- swap 핸들러 자체 (MainView.xaml.cs L1240-1264 인근, PATTERNS.md §9) -->
```csharp
private void BtnSwapHorizontal_Click(object sender, RoutedEventArgs e) {
    var d = _selectedDatumForSwap;
    if (d == null) return;
    if (d.AlgorithmTypeEnum != EDatumAlgorithm.VerticalTwoHorizontalDualImage) return;
    string hpath = d.TeachingImagePath;
    if (!string.IsNullOrEmpty(hpath) && System.IO.File.Exists(hpath)) {
        try { halconViewer.LoadImage(hpath); }
        catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, ex.Message); }
    }
    UpdateImageSourceBadge(ReringProject.Sequence.EImageSource.Horizontal);
}
```

<!-- 가드 4파일 (InspectionListView.xaml.cs 등) 변경 0 — MainView.xaml.cs / .xaml 은 가드 외 -->
<!-- HalconDisplayService 본문 brace 스타일: 메서드는 Allman + 본문 mixed (try/catch 후 K&R 라인 혼용) — 기존 RenderDatumFindResult 스타일 답습 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HalconDisplayService 에 DrawOriginFallback + DrawExpectedAngleArrow 헬퍼 신설 + RenderDatumFindResult 보강</name>
  <files>WPF_Example/Halcon/Display/HalconDisplayService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Display/HalconDisplayService.cs L19-50 (Render 메인 진입점 — 시그니처 확인)
    - WPF_Example/Halcon/Display/HalconDisplayService.cs L307-360 (RenderDatumFindResult 본문 — 정확한 코드 위치)
    - WPF_Example/Halcon/Display/HalconDisplayService.cs L680-710 인근 (RenderDatumOverlay DualImage 분기 — CO-34.1-06 hotfix)
    - WPF_Example/Halcon/Display/HalconDisplayService.cs 의 EnsureFontInitialized 메서드 시그니처 (좌표 텍스트 출력에 필요)
    - .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-PATTERNS.md §"8) RenderDatumFindResult 보강" (정확한 코드 + 헬퍼 2개 본문)
    - .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-CONTEXT.md (D-36-09/10/11)
  </read_first>
  <action>
    **(A) 신규 헬퍼 2개 추가** — `RenderDatumFindResult` 메서드 직후 위치에 `private` 메서드로 신설 (Allman brace):

    ```csharp
    //260528 hbk Phase 36 D-36-10 — OFF-SCREEN fallback 십자 (캔버스 중앙) + 좌표 텍스트 + "OFF-SCREEN" 라벨.
    //  DetectedOrigin 이 이미지 경계 밖일 때 사용자에게 검출 결과의 수치를 명시적으로 노출.
    private void DrawOriginFallback(HWindow window, double width, double height, double originRow, double originCol)
    {
        try
        {
            double cRow = height / 2.0; //260528 hbk Phase 36 D-36-10
            double cCol = width  / 2.0; //260528 hbk Phase 36 D-36-10
            const double crossHalf = 14.0;
            HOperatorSet.SetColor(window, "red"); //260528 hbk Phase 36 D-36-10 — fallback 강조 색 (purple 정상 검출 십자와 시각 구분)
            HOperatorSet.SetLineWidth(window, 2);
            HOperatorSet.DispLine(window, cRow - crossHalf, cCol, cRow + crossHalf, cCol); //260528 hbk Phase 36 D-36-10
            HOperatorSet.DispLine(window, cRow, cCol - crossHalf, cRow, cCol + crossHalf); //260528 hbk Phase 36 D-36-10
            EnsureFontInitialized(window); //260528 hbk Phase 36 D-36-10
            HOperatorSet.SetTposition(window, cRow - crossHalf - 22, cCol + crossHalf + 4); //260528 hbk Phase 36 D-36-10
            HOperatorSet.WriteString(window, "OFF-SCREEN (" + originRow.ToString("F1") + ", " + originCol.ToString("F1") + ")"); //260528 hbk Phase 36 D-36-10
        }
        catch { /* Suppress display errors */ }
    }

    //260528 hbk Phase 36 D-36-11 — Expected angle 점선 화살표 (DetectedRefAngle 실선 화살표와 시각 구분).
    //  PASS = 두 화살표 시각적 일치 (green) / FAIL = 시각적 어긋남 (red). status==None 일 때는 본 메서드 호출 안 됨 (호출자 게이트).
    //  Halcon 점선 = HOperatorSet.SetLineStyle(window, new HTuple(10, 5)). 호출 직후 빈 HTuple 로 즉시 해제 (다른 렌더 영향 0).
    private void DrawExpectedAngleArrow(HWindow window, double originRow, double originCol, double expectedAngleRad, ReringProject.Sequence.EAngleValidationStatus status)
    {
        try
        {
            string color; //260528 hbk Phase 36 D-36-11
            if (status == ReringProject.Sequence.EAngleValidationStatus.Pass) color = "green";
            else                                                              color = "red";
            HOperatorSet.SetColor(window, color); //260528 hbk Phase 36 D-36-11
            HOperatorSet.SetLineWidth(window, 2);
            HOperatorSet.SetLineStyle(window, new HTuple(10, 5)); //260528 hbk Phase 36 D-36-11 — 점선 (10px on, 5px off)
            const double aLen = 30.0; //검출 실선 화살표 (20px) 보다 약간 길게 — 시각 구분
            double endRow = originRow + aLen * System.Math.Sin(expectedAngleRad); //260528 hbk Phase 36 D-36-11
            double endCol = originCol + aLen * System.Math.Cos(expectedAngleRad); //260528 hbk Phase 36 D-36-11
            HOperatorSet.DispLine(window, originRow, originCol, endRow, endCol); //260528 hbk Phase 36 D-36-11
            // arrow head (검출 화살표와 동일 패턴)
            double a1 = expectedAngleRad + 2.5; //260528 hbk Phase 36 D-36-11
            double a2 = expectedAngleRad - 2.5; //260528 hbk Phase 36 D-36-11
            const double headLn = 5.0;
            HOperatorSet.DispLine(window, endRow, endCol, endRow + headLn * System.Math.Sin(a1), endCol + headLn * System.Math.Cos(a1)); //260528 hbk Phase 36 D-36-11
            HOperatorSet.DispLine(window, endRow, endCol, endRow + headLn * System.Math.Sin(a2), endCol + headLn * System.Math.Cos(a2)); //260528 hbk Phase 36 D-36-11
            HOperatorSet.SetLineStyle(window, new HTuple()); //260528 hbk Phase 36 D-36-11 — 점선 해제 (다른 렌더 영향 0)
        }
        catch { /* Suppress display errors */ }
    }
    ```

    **(B) RenderDatumFindResult 본문 보강** — `if (!datum.LastFindSucceeded) return;` 다음 라인부터 try 블록 안 진입 직후 OFF-SCREEN 가드 추가, try 블록 마지막에 Expected 화살표 게이트 추가 (D-36-09/10/11):

    OFF-SCREEN 가드 (try 블록 첫 라인):
    ```csharp
    //260528 hbk Phase 36 D-36-10 — OFF-SCREEN 가드. DetectedOrigin 이 이미지 경계 밖이면 캔버스 중앙 fallback 으로 즉시 위임 후 return.
    //  이미지 사이즈는 HOperatorSet.GetWindowExtents 로 조회 (HWindowControlWPF airspace 컨텍스트, 다른 RenderXXX 헬퍼 패턴 답습).
    HTuple winR, winC, winW, winH; //260528 hbk Phase 36 D-36-10
    HOperatorSet.GetWindowExtents(window, out winR, out winC, out winW, out winH); //260528 hbk Phase 36 D-36-10
    bool isOffScreen = (datum.DetectedOriginRow < 0 || datum.DetectedOriginRow >= winH.D //260528 hbk Phase 36 D-36-10
                     || datum.DetectedOriginCol < 0 || datum.DetectedOriginCol >= winW.D);
    if (isOffScreen) //260528 hbk Phase 36 D-36-10
    {
        DrawOriginFallback(window, winW.D, winH.D, datum.DetectedOriginRow, datum.DetectedOriginCol);
        return; //OFF-SCREEN 시 기존 purple 십자/화살표 미렌더 (의미 없는 위치)
    }
    ```

    Expected 화살표 게이트 (DetectedRefAngle 화살표 렌더 직후 추가):
    ```csharp
    //260528 hbk Phase 36 D-36-11/13 — ExpectedAngleDeg 점선 화살표 (AngleTolerance > 0 sentinel 활성 시에만).
    //  status==None 일 때는 호출 안 함 → 점선 화살표 미표시.
    if (datum.AngleTolerance > 0.0) //260528 hbk Phase 36 D-36-11/13
    {
        DrawExpectedAngleArrow(window, datum.DetectedOriginRow, datum.DetectedOriginCol,
                               datum.ExpectedAngleDeg * System.Math.PI / 180.0, //260528 hbk Phase 36 D-36-11 — deg → rad
                               datum.AngleValidationStatus); //260528 hbk Phase 36 D-36-11
    }
    ```

    제약 (D-36-09/10/11):
    - `GetWindowExtents` 미사용 fallback: 만약 헬퍼 호출이 HWindowControlWPF 컨텍스트에서 width/height 를 0 또는 비정상 반환하면, `datum.DetectedOriginRow / Col` 이 무한대가 아닌 한 OFF-SCREEN 판정 false → 기존 동작 유지 (안전한 default).
    - 단일-이미지 algorithm (TwoLineIntersect / VTH / CTH) 도 RenderDatumFindResult 통과 → 위 보강은 모든 algorithm 에 적용되지만, AngleTolerance 가 0.0 이고 OFF-SCREEN 이 발생하지 않으면 회귀 0 (Expected 화살표는 게이트로 skip, OFF-SCREEN 도 normal 케이스에서 false).
    - 회귀 안전 가드: 1-image algorithm 의 기존 사용자 INI 에 AngleTolerance 키 없음 → ParamBase 가 0.0 fallback → Expected 화살표 미렌더 (회귀 0).
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "$f='WPF_Example/Halcon/Display/HalconDisplayService.cs'; $f1=(Select-String -Path $f -Pattern 'private void DrawOriginFallback').Count; $f2=(Select-String -Path $f -Pattern 'private void DrawExpectedAngleArrow').Count; $off=(Select-String -Path $f -Pattern 'OFF-SCREEN \\(').Count; $line=(Select-String -Path $f -Pattern 'SetLineStyle\\(window, new HTuple\\(10, 5\\)\\)').Count; if ($f1 -eq 1 -and $f2 -eq 1 -and $off -ge 1 -and $line -eq 1) { exit 0 } else { Write-Host \"f1=$f1 f2=$f2 off=$off line=$line\"; exit 1 }"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "private void DrawOriginFallback" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 1
    - `grep -c "private void DrawExpectedAngleArrow" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 1
    - `grep -c "OFF-SCREEN" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 ≥ 1
    - `grep -c "SetLineStyle(window, new HTuple(10, 5))" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 정확히 1 (점선 활성)
    - `grep -c "SetLineStyle(window, new HTuple())" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 정확히 1 (점선 해제)
    - `grep -c "datum.AngleTolerance > 0.0" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 ≥ 1
    - `grep -c "GetWindowExtents" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 ≥ 1 (OFF-SCREEN 가드)
    - 모든 변경 라인에 `//260528 hbk Phase 36 D-36-` 주석 부착 (grep ≥ 20)
  </acceptance_criteria>
  <done>
    DetectedOrigin 화면 안 → 기존 purple 십자 + DetectedRefAngle 실선 + (조건부) Expected 점선 화살표 함께 렌더. 화면 밖 → 캔버스 중앙 빨강 십자 + 좌표 텍스트 + "OFF-SCREEN" 라벨 표시. 점선 SetLineStyle 호출 후 즉시 빈 HTuple 로 해제하여 다른 렌더 영향 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: MainView swap 핸들러에 SetDatumOverlay 재호출 2-라인 추가</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L1230-1280 (BtnSwapHorizontal_Click + BtnSwapVertical_Click 정확한 위치)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L2393-2467 (BtnTestFindDatum_Click CO-34.1-08 분기 — SetDatumOverlay chain 패턴 확인)
    - .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-PATTERNS.md §"9) BtnTestFindDatum_Click ... 토글 재호출" (정확한 코드)
    - .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-CONTEXT.md (D-36-09)
  </read_first>
  <action>
    `BtnSwapHorizontal_Click` 핸들러 본문 마지막 (`UpdateImageSourceBadge(...)` 다음 라인) 에 다음 1줄 추가:

    ```csharp
    //260528 hbk Phase 36 D-36-09 — Test Find 결과 양쪽 캔버스에 일관 렌더. swap 직후 RenderDatumFindResult 가 LastFindSucceeded gate 안에서 자동 재실행 (chain: SetDatumOverlay → RenderDatumOverlay → RenderDatumFindResult).
    halconViewer.SetDatumOverlay(_selectedDatumForSwap, true); //260528 hbk Phase 36 D-36-09
    ```

    `BtnSwapVertical_Click` 핸들러 본문 마지막에도 동일 1줄 추가 (Horizontal/Vertical 대칭):

    ```csharp
    //260528 hbk Phase 36 D-36-09 — Vertical 토글 시에도 동일 chain 트리거 (SameFrame 가정 하 양쪽 캔버스 동일 좌표).
    halconViewer.SetDatumOverlay(_selectedDatumForSwap, true); //260528 hbk Phase 36 D-36-09
    ```

    제약 (D-36-09):
    - 변경 라인 총 2 (Horizontal + Vertical 각 1줄, 주석 포함 4 라인).
    - `_selectedDatumForSwap` null 가드는 이미 함수 본문 첫 라인에서 처리됨 — 추가 가드 불요.
    - `SetDatumOverlay(d, true)` 시그니처 = 기존 BtnTestFindDatum_Click 에서 사용하는 형태와 동일 (`true` = find 결과 표시 모드). MainView 의 다른 핸들러 패턴 1:1 답습.
    - 가드 4파일 (InspectionListView.xaml.cs / InspectionSequence.cs / ParamBase.cs / VisionResponsePacket.cs) 변경 0 — MainView.xaml.cs 는 가드 외 파일.
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "$f='WPF_Example/UI/ContentItem/MainView.xaml.cs'; $cnt=(Select-String -Path $f -Pattern '//260528 hbk Phase 36 D-36-09').Count; $set=(Select-String -Path $f -Pattern 'halconViewer.SetDatumOverlay\\(_selectedDatumForSwap, true\\)').Count; if ($cnt -ge 4 -and $set -eq 2) { exit 0 } else { Write-Host \"cnt=$cnt set=$set\"; exit 1 }"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "halconViewer.SetDatumOverlay(_selectedDatumForSwap, true)" WPF_Example/UI/ContentItem/MainView.xaml.cs` 가 정확히 2 (Horizontal + Vertical 핸들러 각 1회)
    - `grep -c "//260528 hbk Phase 36 D-36-09" WPF_Example/UI/ContentItem/MainView.xaml.cs` 가 ≥ 4 (주석 + 코드 라인)
    - 추가된 라인이 BtnSwapHorizontal_Click 과 BtnSwapVertical_Click 핸들러 본문 마지막 (UpdateImageSourceBadge 직후) 에 위치 (수동 코드 리뷰)
    - 다른 swap 외 핸들러는 변경 0 (git diff 로 검증)
    - 가드 4파일 변경 0: `git diff --name-only` 가 InspectionListView.xaml.cs / InspectionSequence.cs / ParamBase.cs / VisionResponsePacket.cs **미포함**
  </acceptance_criteria>
  <done>
    DualImage 가로/세로 swap 토글 시마다 RenderDatumOverlay → RenderDatumFindResult chain 자동 재실행되어 양쪽 캔버스에 일관 시각화 (purple 십자 + 화살표 + Expected 점선 + 색상 배지).
  </done>
</task>

<task type="auto">
  <name>Task 3: PropertyGrid 색상 배지 — XAML-only ResourceDictionary 추가 (D-36-06)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml L131-318 (Grid Row 1 = 캔버스 영역; L280-288 = border_imageSourceBadge 우상단 패턴 — AngleValidationBadge 도 동일 영역 인접 배치 plan-time 확정)
    - WPF_Example/UI/ContentItem/MainView.xaml L131-138 (Grid.RowDefinitions = 4 Row: Row0 toolbar / Row1 캔버스 / Row2 splitter / Row3 dataGrid_faiResults — PropertyGrid 자체는 MainView.xaml 에 없고 InspectionListView 별도 컨테이너)
    - WPF_Example/Custom/Sequence/Inspection/EAngleValidationStatus.cs 의 namespace 선언 (Plan 02 Task 1 에서 ReringProject.Sequence 로 확정 — Plan 03 은 동일 namespace 가정 + xmlns:seq=clr-namespace:ReringProject.Sequence 사전 선언)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L2451-2458 (BtnTestFindDatum_Click 의 RefreshParamEditor 호출 chain — Find 후 PropertyGrid 갱신)
    - .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-PATTERNS.md §"Color Badge 메커니즘 (D-36-06)" (옵션 A/B/C 정리 + Claude's Discretion)
    - .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-CONTEXT.md (D-36-06 색상 코드 PASS=#43A047 / FAIL=#E53935)
  </read_first>
  <action>
    **옵션 C 채택 (D-36-06 갱신 — 2026-05-28 사용자 합의):**

    - **D-36-06 갱신 (2026-05-28 사용자 합의, checker BLOCKER-1 RESOLVED):** PropertyTools 3.1.0 셀 Style API 가 셀 배경 customization 을 지원할 때 1순위 (셀 배경) 채택 가능하나, **Phase 36 은 2순위 (PropertyGrid 외부 별도 Border 라벨, 옵션 C)** 를 채택. 이유: PropertyTools 3.1.0 셀 Style API 제약 회피 + 가드 4파일 변경 0 유지 우선.
    - 옵션 A (PropertyTools CellTemplateSelector) = PropertyTools 3.1.0 API 미검증 → 실행 단계에서 차단 위험.
    - 옵션 B (PropertyGrid 셀 Style override) = Style 적용 가능 여부 사전 검증 불가 + 셀 단위 컨버터 부착 복잡도 높음.
    - 옵션 C = MainView.xaml 의 캔버스 영역 (Grid Row 1) 우상단에 별도 Border + TextBlock 라벨 추가 → DataTrigger 로 AngleValidationStatus 바인딩 → 색상/텍스트 분기. **PropertyGrid 자체 변경 0, 가드 4파일 변경 0**.

    **삽입 위치 (WARNING-1 plan-time 확정):**
    - **부모 컨테이너:** `<Grid Grid.Row="1" Background="#FF303030">` (MainView.xaml L269).
    - **정확한 위치:** L288 의 `</Border>` (border_imageSourceBadge 닫는 태그) **직후**, L290 의 `<Border VerticalAlignment="Bottom" ...>` (하단 좌표 라벨 Border) **직전**.
    - 근거: 이 위치는 캔버스 우상단에 이미 존재하는 `border_imageSourceBadge` (DualImage 가로/세로 배지) 와 동일 영역으로, AngleValidationBadge 도 PropertyGrid 가 아닌 캔버스 영역 우상단에서 한 줄 아래 (Margin 조정) 에 배치하여 사용자 시선 분리. DataContext 는 ElementName Binding 으로 InspectionListView 의 SelectedItem (또는 halconViewer 의 현재 Datum) 에 명시 연결.
    - **대안 후보 (실행 단계에서 위 위치가 부적합 판정 시):**
      - 후보 B: Row 0 (canvasToolbar) 의 Column 1 영역 `label_drawHint` (L240) / `label_testFindResult` (L262) 인접 — 단점: toolbar 폭 제약, 색상 가시성 낮음.
      - 후보 C: Row 3 (dataGrid_faiResults) 상단에 별도 Row 추가 — 단점: Grid RowDefinitions 수정 필요 = 변경 라인 증가.
    - 실행 단계 가이드: 후보 A (border_imageSourceBadge 직후) 가 1순위. msbuild PASS + Plan 04 UAT Test 2/3/4 시각 확인 통과 시 확정. 가시성 문제 발생 시 후보 B/C 로 fallback.

    **enum xmlns 사전 선언 (WARNING-2 plan-time 확정):**
    - MainView.xaml L1-7 의 UserControl 루트에 다음 xmlns 추가 (기존 xmlns:dmv 패턴 답습):
      ```xml
      xmlns:seq="clr-namespace:ReringProject.Sequence"
      ```
    - DataTrigger 의 enum Value 는 처음부터 fully-qualified `{x:Static seq:EAngleValidationStatus.Pass}` 형태로 명시 (런타임 fallback 금지 — checker WARNING-2 반영).

    **삽입할 XAML 블록 (옵션 C, x:Static enum + xmlns:seq):**

    ```xml
    <!--260528 hbk Phase 36 D-36-06 — DualImage 검출 각도 PASS/FAIL 색상 배지 (PropertyGrid 외부 별도 라벨, 캔버스 우상단).
         가드 4파일 변경 0 정합. D-36-06 갱신 (2026-05-28) — 옵션 C (외부 Border) 채택.
         AngleValidationStatus transient 필드 바인딩 → Trigger 로 색상/텍스트 분기. None=숨김 / Pass=#43A047 "검증 OK" / Fail=#E53935 "검증 NG".
         DataContext = 현재 선택된 DatumConfig (halconViewer 또는 InspectionListView SelectedItem chain). -->
    <Border x:Name="border_angleValidationBadge"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Margin="0,52,12,0"
            Padding="10,4"
            CornerRadius="4"
            Visibility="Collapsed"
            IsHitTestVisible="False">
        <Border.Style>
            <Style TargetType="Border">
                <Style.Triggers>
                    <!--260528 hbk Phase 36 D-36-06 — Pass: 연두 배경 + "각도 검증 OK"-->
                    <DataTrigger Binding="{Binding AngleValidationStatus}" Value="{x:Static seq:EAngleValidationStatus.Pass}">
                        <Setter Property="Background" Value="#43A047"/>
                        <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                    <!--260528 hbk Phase 36 D-36-06 — Fail: 연빨 배경 + "각도 검증 NG"-->
                    <DataTrigger Binding="{Binding AngleValidationStatus}" Value="{x:Static seq:EAngleValidationStatus.Fail}">
                        <Setter Property="Background" Value="#E53935"/>
                        <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                    <!--None 또는 unset: Visibility Collapsed (default)-->
                </Style.Triggers>
            </Style>
        </Border.Style>
        <TextBlock Foreground="White" FontWeight="SemiBold" FontSize="14">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Text" Value=""/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding AngleValidationStatus}" Value="{x:Static seq:EAngleValidationStatus.Pass}">
                            <Setter Property="Text" Value="각도 검증 OK"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding AngleValidationStatus}" Value="{x:Static seq:EAngleValidationStatus.Fail}">
                            <Setter Property="Text" Value="각도 검증 NG"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Border>
    ```

    **xmlns 추가 (UserControl 루트):**

    L1-7 의 `<UserControl x:Class="ReringProject.UI.MainView" ...>` 안에 다음 한 줄 추가 (기존 `xmlns:dmv` 다음):
    ```xml
    xmlns:seq="clr-namespace:ReringProject.Sequence"
    ```

    제약 (D-36-06 갱신):
    - **삽입 위치 plan-time 확정:** L288 `</Border>` (border_imageSourceBadge 닫는 태그) 직후, L290 `<Border VerticalAlignment="Bottom" ...>` 직전 — 캔버스 Grid Row 1 영역 우상단 패턴 답습.
    - Margin="0,52,12,0" = border_imageSourceBadge (Margin="0,12,12,0") 와 시각 분리 (40px 아래) — 두 배지 동시 표시 시 겹침 방지.
    - **enum DataTrigger 는 처음부터 {x:Static seq:EAngleValidationStatus.Pass} 형태로 명시** (xmlns:seq 사전 선언 필수 — UserControl 루트에 한 줄 추가). 런타임 fallback 금지.
    - DataContext: 인접 Border 가 halconViewer 의 DataContext (또는 InspectionListView 의 SelectedItem) chain 을 자동 상속 — 별도 ElementName Binding 불필요. 만약 자동 chain 이 끊기면 실행 단계에서 ElementName Binding 으로 명시 fallback 가능 (단, Plan 03 Task 3 의 코드 변경 라인 증가 없음 — XAML 단일 속성만 추가).
    - BtnTestFindDatum_Click 의 기존 `RefreshParamEditor()` chain 이 PropertyGrid 재바인딩 → 인접 Border 의 DataTrigger 도 자동 재평가. **MainView.xaml.cs 코드 0 라인 변경** (Task 2 의 swap chain 만).
    - **가드 4파일 변경 0**: InspectionListView.xaml.cs / InspectionSequence.cs / ParamBase.cs / VisionResponsePacket.cs 모두 변경 0. MainView.xaml 은 가드 외.

    msbuild 검증:
    - XAML 빌드 시 BindingFailure 없으면 OK (WPF binding 은 런타임 평가이므로 빌드 PASS 가 시각 검증 보장 안 함 — Plan 04 UAT 에서 SIMUL 시각 확인 필수).
    - x:Static + xmlns:seq 형태가 컴파일 통과해야 함 (Plan 02 Task 1 EAngleValidationStatus 등록 의존).
    - msbuild Debug/x64 PASS.
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "$f='WPF_Example/UI/ContentItem/MainView.xaml'; $bd=(Select-String -Path $f -Pattern 'border_angleValidationBadge').Count; $pass=(Select-String -Path $f -Pattern '#43A047').Count; $fail=(Select-String -Path $f -Pattern '#E53935').Count; $xst=(Select-String -Path $f -Pattern 'x:Static seq:EAngleValidationStatus').Count; $xmlns=(Select-String -Path $f -Pattern 'xmlns:seq=\"clr-namespace:ReringProject.Sequence\"').Count; if ($bd -ge 1 -and $pass -eq 1 -and $fail -eq 1 -and $xst -ge 4 -and $xmlns -eq 1) { exit 0 } else { Write-Host \"bd=$bd pass=$pass fail=$fail xst=$xst xmlns=$xmlns\"; exit 1 }"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "border_angleValidationBadge" WPF_Example/UI/ContentItem/MainView.xaml` 가 ≥ 1 (Border x:Name — naming convention 기존 border_imageSourceBadge 답습)
    - `grep -c 'xmlns:seq="clr-namespace:ReringProject.Sequence"' WPF_Example/UI/ContentItem/MainView.xaml` 가 정확히 1 (xmlns:seq 사전 선언, WARNING-2 반영)
    - `grep -c "x:Static seq:EAngleValidationStatus" WPF_Example/UI/ContentItem/MainView.xaml` 가 ≥ 4 (Border Style Pass/Fail + TextBlock Style Pass/Fail = 4 DataTrigger Value 모두 x:Static enum 형태)
    - `grep -c "#43A047" WPF_Example/UI/ContentItem/MainView.xaml` 가 정확히 1 (Pass 색상)
    - `grep -c "#E53935" WPF_Example/UI/ContentItem/MainView.xaml` 가 정확히 1 (Fail 색상)
    - `grep -c "Binding AngleValidationStatus" WPF_Example/UI/ContentItem/MainView.xaml` 가 ≥ 4 (Border Pass/Fail + TextBlock Pass/Fail DataTrigger)
    - `grep -c "//260528 hbk Phase 36 D-36-06" WPF_Example/UI/ContentItem/MainView.xaml` 가 ≥ 3 (XAML comment)
    - 가드 4파일 변경 0: `git diff --name-only` 가 InspectionListView.xaml.cs / InspectionSequence.cs / ParamBase.cs / VisionResponsePacket.cs **미포함**
    - msbuild Debug/x64 PASS, exit 0
    - msbuild XAML 컴파일 에러 0 (BindingFailure 런타임은 별도 — Plan 04 UAT)
  </acceptance_criteria>
  <done>
    PropertyGrid 인접에 색상 배지 Border 가 추가됨. DualImage Test Find 후 AngleValidationStatus 가 Pass/Fail/None 으로 바뀔 때 자동으로 색상/텍스트 (또는 숨김) 전환. 시각 검증은 Plan 04 UAT.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (해당 없음) | 내부 UI/시각화 변경, 외부 surface 영향 0. XAML 바인딩은 동일 DataContext chain (MainView.xaml 기존 패턴 답습). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03-01 | Denial of Service | RenderDatumFindResult OFF-SCREEN 가드 GetWindowExtents 호출 | accept | HOperatorSet.GetWindowExtents 는 윈도우 metadata 조회 O(1) — 매 Render 호출 1회. 추가 비용 무시 가능. |
| T-36-03-02 | Information Disclosure | OFF-SCREEN 라벨에 (row, col) 좌표 노출 | accept | 픽셀 좌표는 운영자 진단 정보 — secret 0. 모든 검출 좌표는 이미 PropertyGrid 에 노출됨 (Phase 17 D-16). |
| T-36-03-03 | Tampering | PropertyGrid 색상 배지 우회 (사용자가 AngleValidationStatus 직접 조작) | accept | transient + 3-종 데코로 INI/JSON 직렬화 차단. UI 에서 직접 set 경로 없음 (PropertyGrid 미노출). 코드만 write. |
</threat_model>

<verification>
- DualImage Test Find PASS 시 PropertyGrid 인접에 연두 배지 "각도 검증 OK" 표시 (시각 UAT — Plan 04)
- DualImage Test Find FAIL (Detected vs Expected 차이 > Tolerance) 시 연빨 배지 "각도 검증 NG" 표시
- AngleTolerance == 0.0 시 배지 Collapsed (None status)
- DetectedOrigin 화면 안 좌표 → purple 십자 + 실선 + Expected 점선 (Tolerance>0 시) 함께 표시
- DetectedOrigin 화면 밖 좌표 → 캔버스 중앙 빨강 십자 + "OFF-SCREEN (row, col)" 라벨
- swap Horizontal/Vertical 토글 즉시 양쪽 캔버스 모두 동일 시각화 유지
- 가드 4파일 변경 0
- msbuild Debug/x64 PASS, 신규 warning 0
</verification>

<success_criteria>
- SC-36-1 / SC-36-3 / D-36-06 / D-36-09 / D-36-10 / D-36-11 모두 코드/XAML 로 표현됨
- CO-34.1-09 "정확히 되었는지 안되었는지 판단 불가" 의 시각화 + UI 검증 완료 (실측 UAT 는 Plan 04)
- msbuild PASS
</success_criteria>

<output>
After completion, create `.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-03-SUMMARY.md`
</output>
