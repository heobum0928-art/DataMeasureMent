---
phase: 36-datum-dualimage-coord-anchor-angle-validation-2026-05-28
plan: 04
type: execute
wave: 4
depends_on: ["36-01", "36-02", "36-03"]
files_modified:
  - .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-UAT.md
autonomous: false
requirements:
  - SC-36-1
  - SC-36-4
  - SC-36-5
  - SC-36-6
  - D-36-14
tags: [phase36, uat, ini, roundtrip, regression, guard]

must_haves:
  truths:
    - "가드 4파일 (ParamBase.cs / InspectionListView.xaml.cs / InspectionSequence.cs / VisionResponsePacket.cs) 의 git diff 라인 카운트가 0 — 자동 검증 PASS"
    - "msbuild Debug/x64 Rebuild 가 exit 0 + 신규 warning 0 PASS"
    - "실측 페어 (Cal_Image/DualImageTest/SIDE1_3-1_Datum_A1_A2_backlight_LV30.bmp + SIDE1_3-1_Datum_B1_backlight_LV30.bmp) 로 DualImage Teach + Test Find 시 PropertyGrid PASS/FAIL 색상 배지 + 시각화 fallback 모두 의도대로 작동 (사용자 시각 확인)"
    - "INI 라운드트립 — Recipe 저장 후 종료 → 재기동 → Recipe 로드 → ExpectedAngleDeg / AngleTolerance / TeachingImagePath / TeachingImagePath_Vertical / Vertical_* / Horizontal_A_* / Horizontal_B_* 모두 byte-identical 보존 (CO-34.1-02 Test 4 동시 종결)"
    - "1-image algorithm 회귀 0 — TwoLineIntersect / VerticalTwoHorizontal / CircleTwoHorizontal 각각의 PropertyGrid + Test Find 동작이 Phase 34.1 sign-off 시점과 동일 (3 algorithm × 1 회 smoke 사용자 확인)"
  artifacts:
    - path: ".planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-UAT.md"
      provides: "Phase 36 통합 UAT 시나리오 7건 + 결과 기록"
      contains: "status: signed_off"
  key_links:
    - from: "Plan 01 + Plan 02 + Plan 03 의 모든 코드/XAML 변경"
      to: "통합 SIMUL UAT + 실측 페어 검증"
      via: "사용자 SIMUL 실행 + 7-Test 시나리오 + UAT.md sign-off"
      pattern: "checkpoint:human-verify"
---

<objective>
Plan 01~03 의 모든 변경이 통합된 상태에서 자동 회귀 검증 (가드 4파일 변경 0 + msbuild PASS) 과 실측 페어 사용자 UAT (PropertyGrid 색상 배지 + 시각화 fallback + INI 라운드트립 + 1-image 회귀 0) 을 수행하여 Phase 36 sign-off 한다.

Purpose: SC-36-1 (실측 페어 DetectedOrigin 의미 있는 위치) + SC-36-4 (INI 라운드트립, CO-34.1-02 동시 종결) + SC-36-5 (가드 4파일 변경 0) + SC-36-6 (msbuild PASS) 의 통합 검증. CO-34.1-01 (Side fixture 실측 검증) + CO-34.1-02 (INI Test 4) + CO-34.1-09 (좌표계 + 각도 검증) 의 carry-over 3건 종결.
Output: 36-UAT.md (7-Test 시나리오 + 사용자 결과 + sign-off frontmatter).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-CONTEXT.md
@.planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-UAT.md
@CLAUDE.md

<interfaces>
<!-- Phase 34.1 UAT 구조 답습 — 본 UAT 는 1:1 형식 -->
<!-- 실측 페어 자산: Cal_Image/DualImageTest/SIDE1_3-1_Datum_A1_A2_backlight_LV30.bmp (가로) + SIDE1_3-1_Datum_B1_backlight_LV30.bmp (세로) -->
<!-- 가드 4파일 (변경 0 검증 대상):
     - WPF_Example/Sequence/Param/ParamBase.cs
     - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
     - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
     - WPF_Example/TcpServer/VisionResponsePacket.cs -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 자동 회귀 검증 + 36-UAT.md 스캐폴드 작성</name>
  <files>.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-UAT.md</files>
  <read_first>
    - .planning/phases/34.1-datum-dualimage-swap-ux-2026-05-27/34.1-UAT.md (Phase 34.1 UAT 7-Test 포맷 답습 모델)
    - .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-CONTEXT.md (D-36-14 Test 4 INI 라운드트립 정의)
    - .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-01-PLAN.md, 36-02-PLAN.md, 36-03-PLAN.md (각 Plan 의 must_haves.truths — UAT Test 본문 도출)
  </read_first>
  <action>
    **자동 회귀 검증 3종 (Plan 01~03 완료 후 본 task 진입 시점에 commit 된 상태):**

    1. **가드 4파일 변경 0 검증** (D-34-13/14 + D-34.1-07 정신 보존):
       ```powershell
       # 기준 = Phase 36 시작 시점 (commit 9c1caa8 = "docs(36): capture phase context").
       # 비교 대상 = 현재 HEAD (Plan 01~03 완료 후).
       # Sanity check (checker BLOCKER-2 반영): 9c1caa8 ~ HEAD 사이 WPF_Example/ 변경 commit 이 phase 36 작업 외에 없는지 확인.
       # docs-only commit (예: f1131b6) 은 WPF_Example/ 변경 0 이어야 함. baseline 정합성 보장.
       $preCheck = git log --oneline 9c1caa8..HEAD -- WPF_Example/
       Write-Host ("Sanity: phase 36 commits touching WPF_Example/ = " + ($preCheck.Count) + " (must equal phase 36 work commits only)")
       $base = '9c1caa8'
       $files = @(
           'WPF_Example/Sequence/Param/ParamBase.cs',
           'WPF_Example/UI/ControlItem/InspectionListView.xaml.cs',
           'WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs',
           'WPF_Example/TcpServer/VisionResponsePacket.cs'
       )
       foreach ($f in $files) {
           $diff = git diff $base HEAD -- $f
           if ($diff) { Write-Host "GUARD VIOLATION: $f"; exit 1 }
       }
       Write-Host "Guard 4 files: 0 lines changed (PASS)"
       ```

    2. **msbuild Debug/x64 Rebuild** (SC-36-6):
       ```powershell
       # vswhere 로 MSBuild 경로 확정 → Rebuild → exit code 확인.
       $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
       $msbuild = & $vswhere -latest -products * -requires Microsoft.Component.MSBuild -find "MSBuild\**\Bin\MSBuild.exe" | Select-Object -First 1
       & $msbuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild
       if ($LASTEXITCODE -ne 0) { Write-Host "MSBUILD FAIL exit=$LASTEXITCODE"; exit 1 }
       Write-Host "msbuild Debug/x64 Rebuild: PASS"
       ```

    3. **신규 warning 0 확인** — msbuild 출력에서 `warning CS` / `warning MSB` 라인 수가 Phase 35 baseline (≤ 6) 이하인지 확인 (수동 grep).

    **`.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-UAT.md` 스캐폴드 작성:**

    ```markdown
    ---
    phase: 36-datum-dualimage-coord-anchor-angle-validation-2026-05-28
    type: uat
    status: pending
    updated: 2026-05-28
    auto_checks:
      guard_4_files_changed: 0
      msbuild_debug_x64: pass
      msbuild_new_warnings: 0
    user_tests:
      total: 7
      pass: 0
      fail: 0
      pending: 7
    carry_over_resolves:
      - CO-34.1-01 (Side fixture 실측 검증)
      - CO-34.1-02 (Phase 34.1 Test 4 INI 라운드트립)
      - CO-34.1-09 (좌표계 + 각도 검증 UX)
    ---

    # Phase 36 UAT — Datum DualImage 설계 보강

    ## Auto Checks (Task 1 완료)

    | Check | Status | Detail |
    |---|---|---|
    | Guard 4 files changed = 0 | PASS | git diff 9c1caa8..HEAD vs ParamBase.cs / InspectionListView.xaml.cs / InspectionSequence.cs / VisionResponsePacket.cs = 0 라인 |
    | msbuild Debug/x64 Rebuild | PASS | exit 0 |
    | msbuild 신규 warning | 0 | Phase 35 baseline (≤ 6) 이하 유지 |

    ## User Tests (사용자 SIMUL UAT — 7 시나리오)

    ### Test 1 — SameFrame 가드 동작 검증 (Plan 01)
    **Goal:** DualImage Find/Teach 호출 시 width/height 불일치 입력에 대해 영문 에러 메시지로 즉시 차단.
    **Steps:**
    1. SIMUL 기동 → 임의 Datum 노드 → AlgorithmType = VerticalTwoHorizontalDualImage 선택
    2. TeachingImagePath = (가로 페어, 임의 해상도 A) / TeachingImagePath_Vertical = (세로 페어, 다른 해상도 B) 의도적 mismatch 입력
    3. Test Find 또는 Teach 실행
    **Expected:** "DualImage requires same-frame image pair: horizontal {wH}x{hH} vs vertical {wV}x{hV}" 영문 에러 메시지 노출 (UI 에서는 FormatFindError 변환 한국어 가능)
    **Result:** _pending_

    ### Test 2 — 실측 페어 Teach + Test Find PASS (Plan 02 + Plan 03 / SC-36-1)
    **Goal:** Cal_Image/DualImageTest 실측 페어로 DetectedOrigin 이 시각적으로 의미 있는 위치에 표시.
    **Steps:**
    1. Datum 노드 → DualImage 선택
    2. TeachingImagePath = `Cal_Image/DualImageTest/SIDE1_3-1_Datum_A1_A2_backlight_LV30.bmp` (가로축 페어)
    3. TeachingImagePath_Vertical = `Cal_Image/DualImageTest/SIDE1_3-1_Datum_B1_backlight_LV30.bmp` (세로축 페어)
    4. Vertical_* ROI 1개 + Horizontal_A_* ROI 1개 + Horizontal_B_* ROI 1개 티칭
    5. Teach 실행 → 성공 확인 (LastTeachSucceeded=true)
    6. Test Find 실행 → DetectedOrigin 좌표가 양쪽 캔버스에 purple 십자 + DetectedRefAngle 실선 화살표로 표시
    7. swap Horizontal/Vertical 토글 → 양쪽 캔버스 모두 일관 시각화 유지 (Plan 03 Task 2)
    **Expected:** 십자가 화면 안 의미 있는 위치 (실측 fixture 의 ref 점 근처) 에 표시. 좌표 텍스트 "Find (row, col)" 노출.
    **Result:** _pending_

    ### Test 3 — ExpectedAngleDeg PASS 색상 배지 (Plan 02 + Plan 03 / SC-36-2, D-36-06)
    **Goal:** AngleTolerance > 0 + |Detected-Expected| ≤ Tolerance 시 연두 배지 표시.
    **Steps:**
    1. Test 2 의 Teach + Test Find 결과 상태에서, PropertyGrid 에서 DetectedAngleDeg 값 (예: 0.3°) 확인
    2. ExpectedAngleDeg = 0.0, AngleTolerance = 1.0 입력 (Plan 02 default)
    3. Test Find 재실행
    **Expected:** PropertyGrid 인접 색상 배지 = #43A047 연두 "각도 검증 OK" 표시. 시각화에 녹색 점선 Expected 화살표 추가 표시 (DetectedRefAngle 실선과 거의 일치).
    **Result:** _pending_

    ### Test 4 — ExpectedAngleDeg FAIL 색상 배지 (Plan 02 + Plan 03 / SC-36-2, D-36-06)
    **Goal:** |Detected-Expected| > Tolerance 시 연빨 배지 표시.
    **Steps:**
    1. Test 3 상태에서 ExpectedAngleDeg = 30.0 (Detected 와 차이 크게) 으로 변경
    2. Test Find 재실행
    **Expected:** 배지 = #E53935 연빨 "각도 검증 NG" 표시. 시각화에 빨강 점선 Expected 화살표가 실선과 시각적으로 어긋남.
    **Edge case (INFO-2 — wrap-around 검증):** ExpectedAngleDeg=179, DetectedAngleDeg=-179 → wrap-around 공식 |((Detected - Expected + 540) % 360) - 180| = 2° → AngleTolerance=5 일 때 PASS 판정. 실측 페어로 -179° 검출 유도 어려울 시 단위 추적 (DatumFindingService 의 wrap-around 라인 grep) 으로 대체.
    **Result:** _pending_

    ### Test 5 — OFF-SCREEN fallback (Plan 03 / SC-36-3, D-36-10)
    **Goal:** DetectedOrigin 이 이미지 경계 밖일 때 캔버스 중앙 빨강 십자 + "OFF-SCREEN (row, col)" 표시.
    **Steps:**
    1. 의도적으로 mismatch 한 ROI 위치 또는 sentinel 0 값으로 Teach → DetectedOrigin 이 음수 또는 width/height 초과되도록 유도
    2. Test Find 실행
    **Expected:** purple 십자 미표시. 캔버스 중앙에 빨강 십자 + "OFF-SCREEN (row, col)" 텍스트 노출.
    **Result:** _pending_

    ### Test 6 — INI 라운드트립 (Plan 02 / SC-36-4, D-36-14, CO-34.1-02 종결)
    **Goal:** Recipe 저장 → 재기동 → Recipe 로드 시 신규 2 필드 + 기존 DualImage 6 필드 byte-identical 보존.
    **Steps:**
    1. Test 2~4 시나리오의 DualImage 설정 완성 상태 (ExpectedAngleDeg=30.0 / AngleTolerance=1.0 또는 임의값)
    2. Recipe Save → 메뉴/버튼 통해 Setting.ini 저장
    3. 프로그램 종료 → 재기동
    4. 동일 Recipe Load → Datum 노드 선택 → PropertyGrid 6 + 2 필드 검증:
       - AlgorithmType = VerticalTwoHorizontalDualImage
       - Vertical_Row / Col / Phi / Length1 / Length2 (5 필드)
       - Horizontal_A_Row / Col / Phi / Length1 / Length2 (5 필드)
       - Horizontal_B_Row / Col / Phi / Length1 / Length2 (5 필드)
       - TeachingImagePath (가로 페어 절대경로)
       - TeachingImagePath_Vertical (세로 페어 절대경로)
       - **ExpectedAngleDeg (신규)**
       - **AngleTolerance (신규)**
    **Expected:** 모든 값 저장 직전과 byte-identical. INI 파일에 `ExpectedAngleDeg=` + `AngleTolerance=` 키 각 1회 출현.
    **Verify command (INI 검증):**
    ```powershell
    Select-String -Path 'Setting.ini' -Pattern 'ExpectedAngleDeg='
    Select-String -Path 'Setting.ini' -Pattern 'AngleTolerance='
    ```
    **Result:** _pending_

    ### Test 7 — 1-image algorithm 회귀 smoke (SC-36-5)
    **Goal:** TwoLineIntersect / VerticalTwoHorizontal / CircleTwoHorizontal 3종이 Phase 34.1 sign-off 시점과 동일 동작.
    **Steps:**
    1. AlgorithmType = TwoLineIntersect 선택 → PropertyGrid 에 ExpectedAngleDeg / AngleTolerance **숨김** 확인 (D-36-05). Line1/Line2 ROI 티칭 + Teach + Test Find smoke PASS
    2. AlgorithmType = VerticalTwoHorizontal 동일 — ExpectedAngleDeg/AngleTolerance 숨김 + Vertical/Horizontal_A/B ROI Teach + Test Find smoke PASS
    3. AlgorithmType = CircleTwoHorizontal 동일 — ExpectedAngleDeg/AngleTolerance 숨김 + Circle/Horizontal_A/B ROI Teach + Test Find smoke PASS
    **Expected:** 3종 모두 PropertyGrid 회귀 0 (신규 2필드 숨김 확인) + Teach/Find 동작 정상.
    **Result:** _pending_

    ## Carry-over Resolves

    - **CO-34.1-01** (Side fixture 실측 검증) — Test 2 PASS 시 종결.
    - **CO-34.1-02** (Phase 34.1 Test 4 INI 라운드트립) — Test 6 PASS 시 종결.
    - **CO-34.1-09** (좌표계 + 각도 검증 UX) — Test 1 + 2 + 3 + 4 + 5 모두 PASS 시 종결.

    ## Notes

    - 모든 시각화 검증은 사용자 시각 확인 (SIMUL 화면) 한정. 자동 픽셀 비교 도구 없음.
    - 1-image algorithm 회귀 smoke 는 사용자 합의 시 Phase 34.1 sign-off 시점 동작 신뢰로 대체 가능 (Phase 36 코드 변경 영향 분석 — DatumFindingService 단일-이미지 오버로드 본문 0 라인 변경 + DatumConfig IsHiddenForAlgorithm 4 case 1줄씩만 추가).
    ```

    제약:
    - **본 Task 는 자동 회귀 검증 3종 + UAT.md 스캐폴드 작성까지만 수행** — 사용자 7-Test 실행은 Task 2 (checkpoint:human-verify) 에서 사용자가 진행.
    - 자동 회귀 검증 PASS 가 보장된 경우에만 UAT.md 작성 진입. 가드 위반 또는 msbuild FAIL 시 즉시 stop + 사용자에게 보고.
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "if ((Test-Path '.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-UAT.md') -and ((Select-String -Path '.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-UAT.md' -Pattern 'status: pending').Count -ge 1) -and ((Select-String -Path '.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-UAT.md' -Pattern '### Test ').Count -eq 7)) { exit 0 } else { exit 1 }"
    </automated>
  </verify>
  <acceptance_criteria>
    - 자동 회귀 검증 PASS: 가드 4파일 git diff 0 라인 + msbuild Debug/x64 Rebuild exit 0 + 신규 warning 0
    - **Baseline sanity check (BLOCKER-2):** `git log --oneline 9c1caa8..HEAD -- WPF_Example/` 결과의 모든 commit 이 phase 36 작업 commit 임을 사용자 확인 (docs-only commit 은 WPF_Example/ 변경 0 이어야 함 — 가드 4파일 검증 baseline 9c1caa8 정합성 보장).
    - `Test-Path .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-UAT.md` 가 True
    - `grep -c "### Test " .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-UAT.md` 가 정확히 7
    - frontmatter `status: pending` 및 `auto_checks` 블록 포함
    - `grep -c "CO-34.1" .planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-UAT.md` 가 ≥ 3 (CO-34.1-01/02/09 종결 명시)
    - 자동 회귀 검증 결과 (PASS/FAIL) 가 auto_checks 섹션에 기록됨
  </acceptance_criteria>
  <done>
    가드 4파일 변경 0 + msbuild PASS + 신규 warning 0 자동 검증 완료. 36-UAT.md 가 status=pending 으로 작성되어 사용자 7-Test 진입 준비 완료.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 사용자 7-Test 실측 UAT 실행 + 36-UAT.md sign-off</name>
  <files>.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-UAT.md</files>
  <action>
    Task 1 에서 생성된 36-UAT.md 의 7-Test 시나리오를 사용자가 SIMUL 환경에서 순차 실행하고, 각 Test 의 _pending_ 자리를 PASS/FAIL/SKIP 으로 갱신한 후 frontmatter 의 status 를 signed_off 또는 partial 로 sign-off. 상세 단계는 아래 how-to-verify 참조.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f=''.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-UAT.md''; $signed=(Select-String -Path $f -Pattern ''status: signed_off'' -SimpleMatch).Count; $partial=(Select-String -Path $f -Pattern ''status: partial'' -SimpleMatch).Count; $pending=(Select-String -Path $f -Pattern ''_pending_'' -SimpleMatch).Count; if (($signed -ge 1 -or $partial -ge 1) -and $pending -eq 0) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>
    36-UAT.md frontmatter status 가 signed_off 또는 partial 로 갱신되었고, 7 Test 의 _pending_ 자리가 모두 PASS/FAIL/SKIP 으로 기재됨. user_tests 카운트 갱신. carry-over 종결 또는 신규 carry-over 명시.
  </done>
  <what-built>
    Phase 36 의 모든 코드/XAML 변경 (Plan 01~03) 이 통합되어 빌드 PASS + 가드 4파일 변경 0 확인된 상태. 사용자는 SIMUL 환경에서 36-UAT.md 의 7-Test 시나리오를 순차 실행하고 결과를 기록.
  </what-built>
  <how-to-verify>
    1. `.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-UAT.md` 열기
    2. **Test 1** (SameFrame 가드): DualImage 선택 → 의도적 mismatch 해상도 페어 입력 → Test Find → 영문 에러 메시지 노출 확인
    3. **Test 2** (실측 페어 Teach + Test Find): `Cal_Image/DualImageTest/SIDE1_3-1_Datum_A1_A2_backlight_LV30.bmp` + `SIDE1_3-1_Datum_B1_backlight_LV30.bmp` 페어로 Teach + Test Find → purple 십자 화면 안 의미 있는 위치 + swap 토글 시 양쪽 캔버스 일관 시각화 확인
    4. **Test 3** (PASS 색상 배지): ExpectedAngleDeg=0.0 / AngleTolerance=1.0 → 연두 #43A047 배지 "각도 검증 OK" + 녹색 점선 Expected 화살표 (실선과 거의 일치) 확인
    5. **Test 4** (FAIL 색상 배지): ExpectedAngleDeg=30.0 → 연빨 #E53935 배지 "각도 검증 NG" + 빨강 점선 화살표 (실선과 어긋남) 확인
    6. **Test 5** (OFF-SCREEN fallback): DetectedOrigin 이 음수/초과되도록 유도 → 캔버스 중앙 빨강 십자 + "OFF-SCREEN (row, col)" 텍스트 확인
    7. **Test 6** (INI 라운드트립, CO-34.1-02 종결): Recipe Save → 종료 → 재기동 → Load → 신규 2필드 + 기존 6필드 모두 byte-identical 보존 + Setting.ini 에 `ExpectedAngleDeg=` / `AngleTolerance=` 키 출현 확인
    8. **Test 7** (1-image 회귀 smoke): TwoLineIntersect / VerticalTwoHorizontal / CircleTwoHorizontal 각각 선택 시 PropertyGrid 에서 ExpectedAngleDeg/AngleTolerance **숨김** 확인 + Teach/Test Find 동작 smoke PASS

    각 Test 결과를 `_pending_` 자리에 `PASS` / `FAIL (사유)` / `SKIP (사유)` 기록.

    모두 PASS 시 frontmatter 의 `status: pending` → `status: signed_off`, `updated: 2026-05-28` → 실제 sign-off 날짜로 변경. `user_tests.pass` / `pending` 카운트 갱신.

    일부 FAIL 시: `status: partial` 로 변경. FAIL Test 의 사유 + 재현 단계 기록. 다음 hotfix carry-over 또는 신규 phase 후보 명시.
  </how-to-verify>
  <resume-signal>
    응답 형식:
    - **"approved"** = 7/7 PASS, 전체 sign-off
    - **"partial — Test X/Y/Z FAIL: <사유>"** = 일부 FAIL, carry-over 처리
    - **"blocked — <상세>"** = 환경/실측 문제로 진행 불가, 차단 사유 + 해결 경로 명시
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (해당 없음) | UAT 단계는 사용자 시각 검증 한정. 외부 surface 변경 0. INI 라운드트립도 로컬 디스크 한정. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04-01 | Repudiation | 사용자 UAT 결과 기록 누락 | mitigate | 36-UAT.md 의 7-Test 각각이 명시적 PASS/FAIL/SKIP 기록 필수. checkpoint:human-verify gate 가 sign-off 단계까지 차단. |
| T-36-04-02 | Tampering | Setting.ini 수동 편집으로 INI 라운드트립 false PASS | accept | Phase 22 IMG-01 와 동일 신뢰 모델 (single-user desktop). 사용자가 의도적으로 검증 단계 우회하면 검증 의미 0 — 정직성 가정. |
</threat_model>

<verification>
- Plan 01~03 의 모든 must_haves.truths 가 통합 동작 시 사용자 관점에서 검증됨
- 가드 4파일 변경 0 자동 PASS
- msbuild Debug/x64 PASS + 신규 warning 0
- 7-Test 결과 7/7 PASS 시 status=signed_off / 일부 FAIL 시 status=partial + carry-over 명시
- CO-34.1-01 / CO-34.1-02 / CO-34.1-09 모두 종결 또는 명시적 carry-over 처리
</verification>

<success_criteria>
SC-36-1 ~ SC-36-6 전체 충족 검증 완료. Phase 36 sign-off (전체 또는 partial) 가 36-UAT.md 에 명시. CO-34.1-09 carry-over 종결. Phase 36 마감 → 다음 = ROADMAP §"Current focus" 후속 phase 진입 (Phase 24 검사 워크플로우 end-to-end 후보).
</success_criteria>

<output>
After completion, create `.planning/phases/36-datum-dualimage-coord-anchor-angle-validation-2026-05-28/36-04-SUMMARY.md`
</output>
