---
phase: 37-side-multi-datum-dualimage-2026-05-28
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "datum 4개 중 하나가 검출 실패해도 TryRunDatumPhase 가 전체를 abort 하지 않는다"
    - "성공한 datum 만 _datumTransforms[name] 에 저장된다"
    - "datum 마다 자기 AlgorithmType 으로 DualImage 여부가 판단된다 (DatumConfigs[0] 단일 판단 제거)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "lenient TryRunDatumPhase (1-image + 2-image 오버로드)"
      contains: "TryRunDatumPhase"
  key_links:
    - from: "InspectionSequence.TryRunDatumPhase"
      to: "_datumTransforms"
      via: "성공 datum 만 저장, 실패 datum 은 continue"
      pattern: "_datumTransforms\\["
---

<objective>
Side 검사가 datum 4개를 각자 독립 검출하도록 `InspectionSequence.TryRunDatumPhase` 두 오버로드(1-image, 2-image)를 lenient 로 전환한다. 한 datum 실패가 다른 datum 검출과 후속 측정을 막지 않게 한다 (D-37-03). 동시에 2-image 오버로드의 datum별 DualImage 판단을 loop 안 per-datum 검사로 유지하여 mixed algorithm 을 허용한다 (D-37-04).

Purpose: 현재 코드는 datum 하나만 실패해도 `return false` 로 전체 datum phase 를 중단시켜, 잘 잡힌 datum 의 측정까지 모두 막는다. 이 plan 이 핵심 실행 루프 계약을 lenient 로 바꿔 다중 datum 독립 처리의 토대를 만든다.

Output: lenient 로 전환된 `TryRunDatumPhase` 두 오버로드. 후속 Plan 02 의 호출부가 이 새 계약(부분 성공 = true 반환, 실패 datum 은 _datumTransforms 에 미저장)을 전제한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-side-multi-datum-dualimage-2026-05-28/37-CONTEXT.md

<interfaces>
<!-- 현재 InspectionSequence.cs 의 변경 대상 메서드와 필드 (executor 는 코드 탐색 불필요) -->

InspectionSequence 멤버:
```csharp
// 런타임 transform 캐시 (D-09)
private readonly Dictionary<string, HTuple> _datumTransforms = new Dictionary<string, HTuple>();
public List<DatumConfig> DatumConfigs { get; private set; } = new List<DatumConfig>();

// 1-image 오버로드 (현재: datum 실패 시 return false)
public bool TryRunDatumPhase(HImage image, out string error)

// 2-image 오버로드 (현재: datum 실패 시 return false; per-datum DualImage 판단 이미 존재)
public bool TryRunDatumPhase(HImage image1, HImage image2, out string error)

// DatumRef → transform 조회 (변경 없음)
public bool TryGetDatumTransform(string datumRef, out HTuple transform)
```

DatumConfig 관련 필드 (변경 없음, 참조만):
```csharp
public string DatumName { get; set; }
public EDatumAlgorithm AlgorithmTypeEnum { get; }   // VerticalTwoHorizontalDualImage 등
public HTuple CurrentTransform { get; set; }
public bool LastFindSucceeded { get; set; }
```

DatumFindingService 호출 시그니처 (변경 없음):
```csharp
bool TryFindDatum(HImage image, DatumConfig datum, out HTuple transform, out string error)
bool TryFindDatum(HImage image1, HImage image2, DatumConfig datum, out HTuple transform, out string error)
```

Logging (이 파일에 using 추가 필요 — 현재 InspectionSequence.cs 는 Logging/ELogType using 없음):
```csharp
ReringProject.Utility.Logging.PrintLog((int)ReringProject.Setting.ELogType.Error, "...");
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TryRunDatumPhase 1-image 오버로드 lenient 전환</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (L143-171 — 1-image TryRunDatumPhase 전체)
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (L9 — ELogType using 경로 확인: `using ReringProject.Setting;`)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <action>
1-image 오버로드 `public bool TryRunDatumPhase(HImage image, out string error)` (L144) 의 foreach 루프(L158-169) 를 lenient 로 수정한다:

기존 실패 분기 (L161-165):
```csharp
if (!service.TryFindDatum(image, datum, out transform, out datumError)) {
    error = $"Datum '{datum.DatumName}' failed: {datumError}";
    datum.LastFindSucceeded = false;
    return false;
}
```
를 다음으로 교체 (return false 제거 → continue):
```csharp
if (!service.TryFindDatum(image, datum, out transform, out datumError)) { //260528 hbk Phase 37 D-37-03 — datum 실패 시 abort 안 함, 성공만 저장
    datum.LastFindSucceeded = false; //260528 hbk Phase 37 D-37-03
    Logging.PrintLog((int)ELogType.Error, "[DatumPhase] Datum '" + (datum.DatumName ?? "") + "' find 실패 (skip): " + (datumError ?? "")); //260528 hbk Phase 37 D-37-03
    continue; //260528 hbk Phase 37 D-37-03 — 다음 datum 계속, 이 datum 은 _datumTransforms 미저장
}
```
성공 분기(L166-168 `datum.LastFindSucceeded = true; datum.CurrentTransform = transform; _datumTransforms[...] = transform;`) 는 그대로 유지.

메서드 마지막 `return true;` (L170) 는 유지 — 부분 성공 포함 항상 true 반환 (전체-성공 강제 제거). `out string error` 는 lenient 계약상 더 이상 datum 실패로 설정하지 않으므로, 진입부 `error = null;` (L145) 유지로 충분. image == null 가드(L152-155)는 그대로 유지(여전히 진짜 호출 오류 → return false).

파일 상단 using 절(L3-11)에 `using ReringProject.Utility;` 와 `using ReringProject.Setting;` 가 없으면 추가한다 (Logging / ELogType 참조용). 추가 시 해당 using 라인에 `//260528 hbk Phase 37` 주석. 이미 존재하면 추가 불필요.

이 파일은 K&R 스타일(여는 중괄호 같은 줄)이다 — 그 스타일을 유지한다. 변경/추가한 모든 라인에 `//260528 hbk Phase 37` 주석을 단다.
  </action>
  <verify>
    <automated>MISSING — 수동 grep 검증: 1-image TryRunDatumPhase 본문에 `return false;` 가 datum-find 실패 분기에 더 이상 없고 `continue;` 가 존재하는지 확인</automated>
  </verify>
  <acceptance_criteria>
    - InspectionSequence.cs 의 1-image `TryRunDatumPhase(HImage image, out string error)` 본문 foreach 안에 `continue;` 가 존재한다 (datum-find 실패 분기)
    - 동 메서드 foreach 안에 datum-find 실패 직후의 `return false;` 가 더 이상 없다 (image == null 가드의 return false 는 유지 허용)
    - 동 메서드는 마지막에 `return true;` 로 끝난다
    - 실패 분기 라인들에 `//260528 hbk Phase 37` 주석이 있다
    - Logging.PrintLog / ELogType 참조가 컴파일되도록 using 절이 갖춰져 있다
  </acceptance_criteria>
  <done>1-image TryRunDatumPhase 가 datum 실패를 skip(continue)+log 하고 성공 datum 만 _datumTransforms 에 저장하며 항상 true(부분성공 포함) 반환</done>
</task>

<task type="auto">
  <name>Task 2: TryRunDatumPhase 2-image 오버로드 lenient 전환 (per-datum DualImage 판단 유지)</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (L172-187 — 2-image TryRunDatumPhase 전체, L181 의 per-datum AlgorithmType 분기 패턴)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <action>
2-image 오버로드 `public bool TryRunDatumPhase(HImage image1, HImage image2, out string error)` (L173) 의 foreach 루프(L179-185) 를 lenient 로 수정한다.

기존 (L180-184):
```csharp
HTuple transform; string datumError; bool ok;
if (datum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) ok = service.TryFindDatum(image1, image2, datum, out transform, out datumError);
else ok = service.TryFindDatum(image1, datum, out transform, out datumError);
if (!ok) { error = $"Datum '{datum.DatumName}' failed: {datumError}"; datum.LastFindSucceeded = false; return false; }
datum.LastFindSucceeded = true; datum.CurrentTransform = transform; _datumTransforms[datum.DatumName ?? ""] = transform;
```
를 다음으로 교체:
```csharp
HTuple transform; string datumError; bool ok; //260528 hbk Phase 37 D-37-04 — per-datum DualImage 판단 유지 (DatumConfigs[0] 단일 판단 아님)
if (datum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) ok = service.TryFindDatum(image1, image2, datum, out transform, out datumError); //260528 hbk Phase 37 D-37-04
else ok = service.TryFindDatum(image1, datum, out transform, out datumError); //260528 hbk Phase 37 D-37-04
if (!ok) { datum.LastFindSucceeded = false; Logging.PrintLog((int)ELogType.Error, "[DatumPhase] Datum '" + (datum.DatumName ?? "") + "' find 실패 (skip): " + (datumError ?? "")); continue; } //260528 hbk Phase 37 D-37-03 — abort 제거, skip
datum.LastFindSucceeded = true; datum.CurrentTransform = transform; _datumTransforms[datum.DatumName ?? ""] = transform; //260528 hbk Phase 37 D-37-03
```
per-datum AlgorithmType 분기(L181-182 의 if/else)는 그대로 loop 안에 유지된다 — 이것이 D-37-04 의 "DatumConfigs[0] 단일 판단 제거, 각 datum 의 AlgorithmType 검사" 요구를 이미 충족한다. 즉 2-image 오버로드는 이미 per-datum 판단을 하고 있으므로 그 구조를 보존하면서 실패 시 abort 만 제거하면 된다.

메서드 마지막 `return true;` (L186) 유지. image1/image2 null 가드(L177)는 유지. 진입부 `error = null;` 유지.

K&R 스타일 유지. 변경 라인에 `//260528 hbk Phase 37` 주석.
  </action>
  <verify>
    <automated>MISSING — 수동 grep 검증: 2-image TryRunDatumPhase 본문에 datum-find 실패 분기 `return false;` 가 없고 `continue;` 존재 + per-datum `AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage` 분기가 loop 안에 유지</automated>
  </verify>
  <acceptance_criteria>
    - 2-image `TryRunDatumPhase(HImage image1, HImage image2, out string error)` foreach 안에 `continue;` 가 존재한다
    - 동 메서드 foreach 의 datum-find 실패 직후 `return false;` 가 없다 (image1/image2 null 가드 return false 는 유지 허용)
    - 동 메서드 foreach 안에 `datum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage` per-datum 분기가 그대로 존재한다 (DatumConfigs[0] 참조 아님)
    - 동 메서드는 마지막에 `return true;` 로 끝난다
    - 변경 라인에 `//260528 hbk Phase 37` 주석이 있다
  </acceptance_criteria>
  <done>2-image TryRunDatumPhase 가 per-datum DualImage 판단을 유지하면서 datum 실패를 skip 하고 항상 true 반환</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → recipe(DatumConfig) | 로컬 셋업 시 운영자가 입력한 datum 설정/티칭 이미지 경로. 신규 외부/네트워크 입력 없음. |

이 plan 은 로컬 Windows 산업용 데스크탑 앱의 내부 검사 실행 루프(in-process)만 수정한다. 신규 외부/네트워크 입력 표면 없음, 인증 없음, 신뢰되지 않은 데이터 포맷 파싱 없음.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Denial of Service | TryRunDatumPhase datum-find 실패 처리 | mitigate | datum find 예외/실패를 abort 대신 continue+log 로 처리 — 한 datum 실패가 전체 검사를 막지 않게 함 (D-37-03 이 직접 이 robustness 를 강제). DatumFindingService 내부는 기존 bare-catch 로 예외를 false 로 흡수. |
| T-37-02 | Tampering | _datumTransforms 부분 채움 | accept | 실패 datum 의 transform 미저장 → 해당 DatumRef 측정은 identity fallback 또는 미해결로 식별 (Plan 02 에서 측정별 처리). 로컬 단일 운영자 환경, 악의적 변조 모델 없음. |

block-on 심각도 = high. high-severity threat 없음.
</threat_model>

<verification>
- msbuild Debug/x64 빌드 PASS (0 새 error, 0 새 warning) — 단, 빌드는 Plan 02 의 호출부 변경 없이도 통과해야 한다 (TryRunDatumPhase 시그니처 무변경)
- 1-image + 2-image 오버로드 모두 datum 실패 시 continue, 성공만 _datumTransforms 저장
- per-datum AlgorithmType 분기 보존 (2-image)
</verification>

<success_criteria>
- TryRunDatumPhase 두 오버로드가 lenient (부분 성공 = true, 실패 datum skip)
- 시그니처(`out string error`) 무변경 → 기존 호출부 컴파일 호환
- 변경 라인 전부 `//260528 hbk Phase 37` 주석 보유
- msbuild Debug/x64 PASS
</success_criteria>

<output>
완료 후 `.planning/phases/37-side-multi-datum-dualimage-2026-05-28/37-01-SUMMARY.md` 생성
</output>
