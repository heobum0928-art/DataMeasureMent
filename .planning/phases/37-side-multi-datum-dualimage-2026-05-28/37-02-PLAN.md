---
phase: 37-side-multi-datum-dualimage-2026-05-28
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "EStep.DatumPhase 가 DatumConfigs 전체를 loop 하여 datum 마다 자기 이미지로 검출한다 (DatumConfigs[0] 단일 분기 제거)"
    - "각 DualImage datum 이 자기 TeachingImagePath/TeachingImagePath_Vertical 2장을 로드한다"
    - "datum phase 부분 실패가 action 전체를 abort 하지 않는다 (lenient 호출부)"
    - "측정은 자기 Shot 이미지를 쓰고 datum transform 은 이름(DatumRef)으로 참조한다 — 측정 이미지 ≠ datum 이미지"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "per-datum loop EStep.DatumPhase + per-datum TryGrabOrLoadDualDatumImages"
      contains: "TryGrabOrLoadDualDatumImages"
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "TryRunSingleDatum 누적 검출 경로 + ClearDatumTransforms"
      contains: "TryRunSingleDatum"
  key_links:
    - from: "Action_FAIMeasurement.EStep.DatumPhase"
      to: "InspectionSequence.TryRunSingleDatum"
      via: "datum loop 안에서 per-datum 이미지로 누적 호출 (lenient)"
      pattern: "TryRunSingleDatum"
    - from: "Action_FAIMeasurement.EStep.Measure"
      to: "ShotParam.GetImage()"
      via: "측정은 Shot 이미지 사용, transform 은 TryGetDatumTransform(DatumRef)"
      pattern: "ShotParam.GetImage"
---

<objective>
`Action_FAIMeasurement` 의 `EStep.DatumPhase` 와 `TryGrabOrLoadDualDatumImages` 를 다중 datum 독립 처리로 재작성한다 (D-37-02, D-37-04, D-37-05). `DatumConfigs[0]` 단일 분기를 제거하고 DatumConfigs 전체를 loop 하며, 각 datum 이 자기 TeachingImagePath/TeachingImagePath_Vertical(또는 1-image 경로)로 검출하도록 한다. 호출부는 lenient — datum 부분 실패로 action 전체를 abort 하지 않는다 (D-37-03). 측정 이미지가 datum 이미지와 별개로 동작하는 구조(EStep.Measure 의 ShotParam.GetImage + TryGetDatumTransform 이름 참조)는 이미 존재하므로 보존·검증한다 (D-37-06).

Purpose: 현재 EStep.DatumPhase 는 `DatumConfigs[0]` 하나의 AlgorithmType 으로 전체 datum 의 DualImage 여부를 결정하고, `TryGrabOrLoadDualDatumImages` 는 `DatumConfigs[0]` 한 datum 의 2장만 로드한다("단일 Datum 가정", L329). 이로 인해 datum 4개 시나리오가 불가능하다. 이 plan 이 실행 루프를 datum 단위로 분해한다.

Output: datum 4개를 각자 자기 이미지로 검출하여 InspectionSequence._datumTransforms 를 누적 채운 뒤, EStep.Measure 가 측정별 DatumRef 로 transform 을 이름 참조하는 동작.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-side-multi-datum-dualimage-2026-05-28/37-CONTEXT.md
@.planning/phases/37-side-multi-datum-dualimage-2026-05-28/37-01-SUMMARY.md

<interfaces>
<!-- Plan 01 이 만든 lenient 계약 + 변경 대상 메서드 시그니처 (executor 는 코드 탐색 불필요) -->

InspectionSequence (Plan 01 이후 — 두 오버로드 lenient. 이 Plan 은 누적 경로를 신규 추가):
```csharp
// Plan 01: datum 실패 시 skip+log, 성공만 _datumTransforms 저장, 항상 true(부분성공) 반환 (각 호출 시 Clear)
public bool TryRunDatumPhase(HImage image, out string error)            // 1-image
public bool TryRunDatumPhase(HImage image1, HImage image2, out string error)  // 2-image (DualImage), per-datum 판단 내부 보유
// 변경 없음
public bool TryGetDatumTransform(string datumRef, out HTuple transform)
public List<DatumConfig> DatumConfigs { get; }
// 이 Plan(Task 2)이 신규 추가하는 누적 경로:
//   public bool TryRunSingleDatum(DatumConfig datum, HImage imageH, HImage imageV, out string error)  // Clear 안 함, 해당 datum 만 저장
//   public void ClearDatumTransforms()                                                                // loop 전 1회 초기화
```

DatumConfig (참조만, 변경 없음):
```csharp
public string DatumName { get; set; }
public string TeachingImagePath { get; set; }            // 가로축 이미지
public string TeachingImagePath_Vertical { get; set; }   // 세로축 이미지 (DualImage 전용)
public EDatumAlgorithm AlgorithmTypeEnum { get; }         // VerticalTwoHorizontalDualImage 등
public HTuple CurrentTransform { get; set; }
public bool LastFindSucceeded { get; set; }
```

DatumFindingService (변경 없음, TryRunSingleDatum 이 호출):
```csharp
bool TryFindDatum(HImage image, DatumConfig datum, out HTuple transform, out string error)
bool TryFindDatum(HImage image1, HImage image2, DatumConfig datum, out HTuple transform, out string error)
```

현재 Action_FAIMeasurement 의 변경 대상:
```csharp
// EStep.DatumPhase (L79-131): DatumConfigs[0].AlgorithmTypeEnum 단일 분기 — 제거 대상
// TryGrabOrLoadDualDatumImages (L322-353): parentSeq.DatumConfigs[0] 한정 — datum 인자로 변경 대상
private bool TryGrabOrLoadDualDatumImages(InspectionSequence parentSeq, out HImage imageHorizontal, out HImage imageVertical)
// GrabOrLoadDatumImage (L284-315): 1-image 경로, DatumConfigs[0].TeachingImagePath 한정 — datum 인자 오버로드 추가 대상
private HImage GrabOrLoadDatumImage(InspectionSequence parentSeq)
```

EStep.Measure (L164-273) — 변경 없음, 검증만:
- `using (var image = ShotParam.GetImage())` 로 측정 이미지(자기 Shot 이미지) 사용
- `parentSeq2.TryGetDatumTransform(meas.DatumRef, out transform)` 로 datum transform 을 이름 참조
- 이 두 줄이 "측정 이미지 ≠ datum 이미지" 분리(D-37-06)를 이미 구현한다

Logging / ELogType / HImage / File / SIMUL_MODE 는 이 파일에 이미 using/패턴 존재 (L1-10).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TryGrabOrLoadDualDatumImages 를 datum 인자 받게 변경 + 1-image datum 인자 오버로드 추가</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (L282-353 — GrabOrLoadDatumImage + TryGrabOrLoadDualDatumImages 전체)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (L44-50 — TeachingImagePath / TeachingImagePath_Vertical)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <action>
(A) `TryGrabOrLoadDualDatumImages` (L322) 의 시그니처를 datum 인자를 받도록 변경한다:
```csharp
private bool TryGrabOrLoadDualDatumImages(DatumConfig datum, out HImage imageHorizontal, out HImage imageVertical) //260528 hbk Phase 37 D-37-02 — DatumConfigs[0] 한정 제거, datum 인자
```
본문에서 `parentSeq.DatumConfigs[0]` 참조(L329 `var datum = parentSeq.DatumConfigs[0];`)를 제거하고, 인자로 받은 `datum` 을 직접 사용한다. 진입 null 가드(L325-328 의 parentSeq/DatumConfigs.Count 검사)는 `if (datum == null) { Logging.PrintErrLog((int)ELogType.Error, "[Datum] DualImage: datum 이 null 입니다."); return false; }` 로 교체한다. 나머지 본문(pathH/pathV 로드, File.Exists 2-step 가드, HImage 생성 try/catch, 실패 시 양쪽 Dispose) 로직은 그대로 유지하되 `datum.TeachingImagePath` / `datum.TeachingImagePath_Vertical` 를 인자 datum 에서 읽는다. 변경 라인에 `//260528 hbk Phase 37 D-37-02` 주석.

(B) 1-image 경로용으로 datum 인자를 받는 오버로드를 신규 추가한다 (기존 `GrabOrLoadDatumImage(InspectionSequence parentSeq)` 는 일단 남겨둔다 — Task 2 가 무참조 확인 후 정리):
```csharp
//260528 hbk Phase 37 D-37-02 — per-datum 1-image 로드 (TeachingImagePath → SimulImagePath 폴백 → grab). datum 인자 명시.
private HImage GrabOrLoadDatumImage(DatumConfig datum) {
    if (ShotParam == null) return null; //260528 hbk Phase 37
    HImage image = null; //260528 hbk Phase 37
    string teachingPath = (datum != null) ? datum.TeachingImagePath : null; //260528 hbk Phase 37 D-37-02
    #if SIMUL_MODE
    if (!string.IsNullOrEmpty(teachingPath) && File.Exists(teachingPath)) { //260528 hbk Phase 37
        try { image = new HImage(teachingPath); } catch { image = null; } //260528 hbk Phase 37
    }
    if (image == null && !string.IsNullOrEmpty(ShotParam.SimulImagePath) && File.Exists(ShotParam.SimulImagePath)) { //260528 hbk Phase 37 — 회귀 0 폴백
        try { image = new HImage(ShotParam.SimulImagePath); } catch { image = null; } //260528 hbk Phase 37
    }
    if (image == null) { image = SystemHandler.Handle.Devices.GrabHalconImage(ShotParam); } //260528 hbk Phase 37
    #else
    image = SystemHandler.Handle.Devices.GrabHalconImage(ShotParam); //260528 hbk Phase 37
    #endif
    return image; //260528 hbk Phase 37
}
```
이 파일은 K&R 혼재 스타일이다 — `TryGrabOrLoadDualDatumImages` 가 K&R(여는 중괄호 같은 줄)이므로 신규 메서드도 동일 K&R 로 작성한다. 기존 `GrabOrLoadDatumImage(InspectionSequence parentSeq)` 는 이 Task 에서 삭제하지 않는다.
  </action>
  <verify>
    <automated>MISSING — 수동 grep 검증: `TryGrabOrLoadDualDatumImages(DatumConfig datum` 시그니처 존재 + `parentSeq.DatumConfigs[0]` 가 본 메서드에서 사라짐 + `GrabOrLoadDatumImage(DatumConfig datum)` 오버로드 존재</automated>
  </verify>
  <acceptance_criteria>
    - `TryGrabOrLoadDualDatumImages` 시그니처가 `(DatumConfig datum, out HImage imageHorizontal, out HImage imageVertical)` 이다
    - 동 메서드 본문에 `DatumConfigs[0]` 참조가 없다
    - 동 메서드가 `datum.TeachingImagePath` 와 `datum.TeachingImagePath_Vertical` 를 인자 datum 에서 읽는다
    - `private HImage GrabOrLoadDatumImage(DatumConfig datum)` 오버로드가 존재한다
    - 변경/추가 라인에 `//260528 hbk Phase 37` 주석이 있다
    - HImage 생성 try/catch + 실패 시 Dispose 패턴이 유지된다
  </acceptance_criteria>
  <done>두 datum-이미지 로드 헬퍼가 datum 인자를 받아 per-datum 자기 경로로 이미지를 로드한다 (DatumConfigs[0] 한정 제거)</done>
</task>

<task type="auto">
  <name>Task 2: InspectionSequence 누적 검출 경로 추가 + EStep.DatumPhase 를 datum loop 로 재작성 (lenient, mixed algorithm)</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (L78-131 — EStep.DatumPhase 전체; L164-273 — EStep.Measure 무변경 검증용)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (Plan 01 변경 후 전체 — _datumTransforms 필드, TryRunDatumPhase 두 오버로드, TryGetDatumTransform)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <action>
설계 근거: Plan 01 의 `TryRunDatumPhase` 오버로드들은 호출 시 `_datumTransforms.Clear()` 를 한다. 따라서 datum 마다 그것을 호출하면 직전 datum 의 transform 이 지워진다. 다중 datum 누적을 위해 InspectionSequence 에 **Clear 하지 않고 한 datum 만 검출·저장하는 경로**를 신규로 추가하고, EStep.DatumPhase 가 loop 전에 1회만 Clear 한다 (D-37-05 "모든 datum 먼저 검출 → _datumTransforms 채움").

(A) InspectionSequence.cs 에 두 public 메서드를 신규 추가한다. `TryGetDatumTransform` 메서드 바로 위/아래(클래스 안)에 배치한다. 이 파일은 K&R 스타일이다. `using ReringProject.Halcon.Algorithms;` (DatumFindingService) 는 이미 존재한다(L9). 모든 추가 라인에 `//260528 hbk Phase 37 D-37-05` 주석:
```csharp
//260528 hbk Phase 37 D-37-05 — DatumPhase loop 시작 전 1회 호출 (per-datum 누적 전 초기화)
public void ClearDatumTransforms() {
    _datumTransforms.Clear();
}

//260528 hbk Phase 37 D-37-05 — datum 1개 검출 후 _datumTransforms 누적 저장 (Clear 안 함). 실패 시 false 반환하되 호출부는 abort 안 함 (lenient).
public bool TryRunSingleDatum(DatumConfig datum, HImage imageH, HImage imageV, out string error) {
    error = null;
    if (datum == null) { error = "datum is null"; return false; }
    var service = new DatumFindingService();
    HTuple transform;
    string datumError;
    bool ok;
    if (datum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) {
        if (imageH == null || imageV == null) { error = "DualImage 이미지 null"; datum.LastFindSucceeded = false; return false; }
        ok = service.TryFindDatum(imageH, imageV, datum, out transform, out datumError);
    } else {
        if (imageH == null) { error = "이미지 null"; datum.LastFindSucceeded = false; return false; }
        ok = service.TryFindDatum(imageH, datum, out transform, out datumError);
    }
    if (!ok) { datum.LastFindSucceeded = false; error = datumError; return false; }
    datum.LastFindSucceeded = true;
    datum.CurrentTransform = transform;
    _datumTransforms[datum.DatumName ?? ""] = transform;
    return true;
}
```

(B) Action_FAIMeasurement.cs 의 EStep.DatumPhase 블록(L79-131) 전체를 다음 구조로 교체한다 (K&R, `//260528 hbk Phase 37` 주석). 기존 블록의 `FinishAction(EContextResult.Error)` 호출 4곳(L90, L99, L111, L119)은 전부 제거된다 — datum 실패는 절대 action 을 abort 하지 않는다 (D-37-03 lenient 호출부):
```csharp
case EStep.DatumPhase: {
    var parentSeq = ShotParam != null ? ShotParam.Parent as InspectionSequence : null; //260528 hbk Phase 37
    if (parentSeq != null && parentSeq.DatumConfigs.Count > 0) { //260528 hbk Phase 37
        parentSeq.ClearDatumTransforms(); //260528 hbk Phase 37 D-37-05 — loop 전 1회 초기화
        foreach (var datum in parentSeq.DatumConfigs) { //260528 hbk Phase 37 D-37-04/05 — per-datum loop, mixed algorithm 허용
            if (datum == null) continue; //260528 hbk Phase 37
            if (datum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) { //260528 hbk Phase 37 D-37-04 — datum별 판단
                HImage imgH = null, imgV = null; //260528 hbk Phase 37
                try {
                    if (!TryGrabOrLoadDualDatumImages(datum, out imgH, out imgV)) { //260528 hbk Phase 37 D-37-02 — per-datum
                        Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + (datum.DatumName ?? "") + "' DualImage 취득 실패 (skip)"); //260528 hbk Phase 37 D-37-03
                        continue; //260528 hbk Phase 37 D-37-03 — datum skip, abort 안 함
                    }
                    string derr; //260528 hbk Phase 37
                    if (!parentSeq.TryRunSingleDatum(datum, imgH, imgV, out derr)) { //260528 hbk Phase 37 D-37-05
                        Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + (datum.DatumName ?? "") + "' 검출 실패 (skip): " + (derr ?? "")); //260528 hbk Phase 37 D-37-03
                    }
                } finally {
                    if (imgH != null) { try { imgH.Dispose(); } catch { } } //260528 hbk Phase 37
                    if (imgV != null) { try { imgV.Dispose(); } catch { } } //260528 hbk Phase 37
                }
            } else { //260528 hbk Phase 37 — 1-image datum
                HImage img = GrabOrLoadDatumImage(datum); //260528 hbk Phase 37 D-37-02 — per-datum 오버로드
                if (img == null) { //260528 hbk Phase 37
                    Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + (datum.DatumName ?? "") + "' 이미지 취득 실패 (skip)"); //260528 hbk Phase 37 D-37-03
                    continue; //260528 hbk Phase 37
                }
                try {
                    string derr; //260528 hbk Phase 37
                    if (!parentSeq.TryRunSingleDatum(datum, img, null, out derr)) { //260528 hbk Phase 37 D-37-05
                        Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + (datum.DatumName ?? "") + "' 검출 실패 (skip): " + (derr ?? "")); //260528 hbk Phase 37 D-37-03
                    }
                } finally {
                    img.Dispose(); //260528 hbk Phase 37
                }
            }
        }
    }
    // DatumConfigs 비어있으면 무보정 pass-through (D-10) — abort 없음 (D-37-03 lenient)
    Step = (int)EStep.Grab; //260528 hbk Phase 37 — datum 부분 실패해도 측정 진행
    break;
}
```

(C) EStep.Measure (L164-273) 는 변경하지 않는다. 단, "측정 이미지 ≠ datum 이미지" (D-37-06) 가 이미 성립함을 확인한다: 측정은 `using (var image = ShotParam.GetImage())` (자기 Shot 이미지)를 사용하고, transform 은 `parentSeq2.TryGetDatumTransform(meas.DatumRef, out transform)` 로 이름 참조한다. 이 두 줄이 변경 없이 유지되는지 read_first 로 확인하고 SUMMARY 에 "EStep.Measure 무변경, D-37-06 구조 기존 충족" 으로 기록한다.

(D) Task 1 에서 남겨둔 기존 `GrabOrLoadDatumImage(InspectionSequence parentSeq)` 가 이 변경 후 무참조인지 grep 으로 확인한다. 호출부가 0 이면 제거하고 SUMMARY 에 "무참조 GrabOrLoadDatumImage(InspectionSequence) 제거" 기록. 호출부가 남아있으면(예: 다른 경로) 그대로 둔다 (회귀 0 우선).

이 두 파일은 K&R 혼재 스타일 — 주변 코드 스타일을 따른다. HOperatorSet/Halcon 호출은 절대 Run() 밖으로 throw 하지 않는다(기존 패턴 유지).
  </action>
  <verify>
    <automated>MISSING — 수동: msbuild Debug/x64 빌드 + grep 검증 (아래 acceptance)</automated>
  </verify>
  <acceptance_criteria>
    - InspectionSequence.cs 에 `public bool TryRunSingleDatum(DatumConfig datum, HImage imageH, HImage imageV, out string error)` 와 `public void ClearDatumTransforms()` 가 존재한다
    - TryRunSingleDatum 본문에 `_datumTransforms.Clear()` 호출이 없다 (누적), `_datumTransforms[datum.DatumName ?? ""] = transform;` 가 있다
    - TryRunSingleDatum 에 datum별 `AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage` 분기가 있다 (mixed 허용)
    - Action_FAIMeasurement.cs EStep.DatumPhase 블록에 `foreach (var datum in parentSeq.DatumConfigs)` loop 가 존재한다
    - EStep.DatumPhase 블록에 `DatumConfigs[0]` 참조가 없다
    - EStep.DatumPhase 블록에 `FinishAction(EContextResult.Error)` 호출이 없다
    - EStep.DatumPhase 블록이 `parentSeq.ClearDatumTransforms();` 를 loop 진입 전 1회 호출한다
    - DualImage 경로는 imgH/imgV 를 finally 에서 Dispose; 1-image 경로는 img 를 finally 에서 Dispose 한다
    - EStep.Measure 의 `ShotParam.GetImage()` 와 `TryGetDatumTransform(meas.DatumRef` 호출이 변경 없이 유지된다
    - 변경/추가 라인에 `//260528 hbk Phase 37` 주석이 있다
    - msbuild Debug/x64 PASS (0 새 error, 0 새 warning)
  </acceptance_criteria>
  <done>EStep.DatumPhase 가 모든 datum 을 자기 이미지로 순회 검출하여 _datumTransforms 를 누적 채우고(loop 전 1회 Clear), 한 datum 실패가 action 을 abort 하지 않으며, 측정은 후속 EStep.Measure 에서 자기 Shot 이미지 + DatumRef 이름 참조(D-37-06)로 진행된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → DatumConfig.TeachingImagePath(_Vertical) | 운영자가 셋업 시 입력하는 로컬 티칭 이미지 파일 경로. 로컬 파일, 신규 외부/네트워크 입력 없음. |

이 plan 은 in-process 검사 액션 스텝 머신 + 시퀀스 누적 경로만 수정한다. 신규 외부/네트워크 입력 표면 없음, 인증 없음, 신뢰되지 않은 포맷 파싱 없음.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03 | Denial of Service | EStep.DatumPhase per-datum 이미지 로드 | mitigate | 누락/잘못된 datum 티칭 이미지 경로를 graceful skip(continue)+log 으로 처리 — crash 없이 다음 datum/측정 진행 (D-37-03). 경로 검사는 File.Exists 2-step 가드, HImage 생성은 try/catch. |
| T-37-04 | Denial of Service | HImage 리소스 누수 | mitigate | DualImage 경로 imgH/imgV + 1-image 경로 img 를 finally 에서 Dispose — datum loop 반복 시 네이티브 이미지 핸들 누수 방지. |
| T-37-05 | Tampering | 부분 채워진 _datumTransforms | accept | 미해결 DatumRef 측정은 identity fallback 으로 식별 (EStep.Measure 기존 L176 로직). 로컬 단일 운영자, 변조 모델 없음. |

block-on 심각도 = high. high-severity threat 없음.
</threat_model>

<verification>
- msbuild Debug/x64 빌드 PASS (0 새 error, 0 새 warning)
- EStep.DatumPhase 가 datum loop, DatumConfigs[0] 단일 분기 제거
- datum 부분 실패 시 FinishAction(Error) 미호출 → EStep.Grab 진행
- 측정 이미지(ShotParam.GetImage) 와 datum 이미지(per-datum TeachingImagePath)가 별개로 동작 (D-37-06) — EStep.Measure 무변경 검증
- DualImage / 1-image datum 혼재(mixed) 허용
- _datumTransforms 누적 (loop 전 1회 Clear, datum 마다 누적 저장)
</verification>

<success_criteria>
- EStep.DatumPhase 가 DatumConfigs 전체를 순회하며 datum 마다 자기 이미지로 검출
- 한 datum 실패가 다른 datum/측정을 막지 않음 (lenient, abort 없음)
- TryGrabOrLoadDualDatumImages / GrabOrLoadDatumImage 가 datum 인자 기반
- InspectionSequence 에 TryRunSingleDatum + ClearDatumTransforms 누적 경로 추가
- 변경 라인 전부 `//260528 hbk Phase 37` 주석
- msbuild Debug/x64 PASS
</success_criteria>

<output>
완료 후 `.planning/phases/37-side-multi-datum-dualimage-2026-05-28/37-02-SUMMARY.md` 생성
</output>
