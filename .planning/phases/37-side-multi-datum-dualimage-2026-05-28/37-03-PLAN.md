---
phase: 37-side-multi-datum-dualimage-2026-05-28
plan: 03
type: execute
wave: 3
depends_on: ["37-02"]
files_modified:
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: false
requirements: []
must_haves:
  truths:
    - "운영자가 기존 Datum 추가 흐름으로 Side fixture 에 datum 4개(각 VerticalTwoHorizontalDualImage)를 생성할 수 있다"
    - "각 datum 이 자기 가로/세로 티칭 이미지 경로(TeachingImagePath / TeachingImagePath_Vertical)를 보유한다"
    - "datum 4개 중 일부 실패 시 성공한 datum 의 측정은 정상 진행된다 (한 datum 실패가 다른 측정을 막지 않음)"
    - "측정 이미지가 datum 이미지와 별개임이 SIMUL 실행으로 확인된다"
  artifacts:
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "datum 4개 생성 흐름 확인/보완 (기존 AddDatumToSequence 재활용)"
      contains: "AddDatumToSequence"
  key_links:
    - from: "InspectionListView.AddDatumToSequence"
      to: "InspectionSequence.AddDatum"
      via: "기존 흐름 반복 호출로 datum 4개 생성"
      pattern: "AddDatum"
---

<objective>
기존 DualImage 티칭/Datum 추가 UI 흐름을 재활용하여 Side fixture 에 datum 4개(각 VerticalTwoHorizontalDualImage)를 생성·티칭하는 경로를 확인하고, 필요한 최소 보완만 한다 (D-37-07). 신규 UI 는 최소화한다. 이후 SIMUL_MODE 에서 Plan 01/02 의 다중 datum 독립 실행 + 측정 이미지 분리(D-37-06)를 실제 실행으로 검증한다.

Purpose: 데이터 모델·UI 추가 흐름(`AddDatumToSequence` → `InspectionSequence.AddDatum`)은 이미 datum N개 + per-datum 알고리즘/티칭 이미지 경로를 지원한다(코드 조사 확인). 이 plan 은 그 흐름이 4-datum DualImage 시나리오를 실제로 지원하는지 확인하고, 막힘이 있으면 국소 보완하며, SIMUL UAT 로 phase 목표 달성을 증명한다.

Output: datum 4개 생성·티칭 가능 확인 + SIMUL 다중 datum 독립 실행 UAT 결과. 측정 이미지 ≠ datum 이미지 동작 검증.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-side-multi-datum-dualimage-2026-05-28/37-CONTEXT.md
@.planning/phases/37-side-multi-datum-dualimage-2026-05-28/37-01-SUMMARY.md
@.planning/phases/37-side-multi-datum-dualimage-2026-05-28/37-02-SUMMARY.md

<interfaces>
<!-- 기존 datum 추가 UI 흐름 (코드 조사 확인) — executor 는 이 흐름을 재활용/검증 -->

InspectionListView.xaml.cs (기존):
```csharp
// Datum 노드 추가 진입점들 — 이미 datum N개 생성 지원
private void AddDatumToSequence(NodeViewModel seqNode) {
    // ...
    string defaultName = "Datum_" + (inspSeq.DatumConfigs.Count + 1);
    if (!TextInputBox.Show("Datum 이름 입력", defaultName, out string datumName)) return;
    DatumConfig newDatum = inspSeq.AddDatum(datumName);   // 호출마다 새 DatumConfig 추가
    if (newDatum == null) return;
    ViewModel.AddDatumNode(seqNode, newDatum);
    seqNode.IsExpanded = true;
}
// RemoveDatum 경로도 존재 (L893 inspSeq.RemoveDatum(datumIdx))
```

InspectionSequence (기존, 변경 없음):
```csharp
public DatumConfig AddDatum(string name = null)   // DatumConfigs 에 추가, 호출 횟수만큼 datum 생성
public bool RemoveDatum(int index)
public List<DatumConfig> DatumConfigs { get; }
```

DatumConfig (기존): 각 datum 이 자기 AlgorithmType(VerticalTwoHorizontalDualImage 설정 가능) + TeachingImagePath + TeachingImagePath_Vertical 보유. PropertyGrid 가 DualImage 선택 시 TeachingImagePath_Vertical 를 노출한다(ICustomTypeDescriptor, DatumConfig.cs:723).

결론: datum 4개 생성은 "AddDatum 을 4번 호출" + "각 datum 의 AlgorithmType 을 VerticalTwoHorizontalDualImage 로 설정" + "각 datum 의 두 티칭 이미지 경로 입력" 으로 기존 UI 만으로 가능하다. 신규 UI 불필요할 가능성이 높다 — Task 1 이 이를 확인하고 막힘만 보완한다.

SIMUL 테스트 이미지 (일부 존재):
- Cal_Image/DualImageTest/SIDE1_3-1_Datum_A1_A2_backlight_LV30.bmp
- Cal_Image/DualImageTest/SIDE1_3-1_Datum_B1_backlight_LV30.bmp
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: datum 4개 DualImage 생성/티칭 흐름 확인 및 최소 보완</name>
  <read_first>
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs (L720-810 — AddDatumToSequence + Datum 노드 추가 분기; L880-900 — RemoveDatum)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (L669-730 — ICustomTypeDescriptor hide 규칙, DualImage 시 TeachingImagePath_Vertical 노출 확인)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (L128-141 — AddDatum / RemoveDatum)
  </read_first>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <action>
기존 datum 추가 흐름이 datum 4개(각 VerticalTwoHorizontalDualImage)를 생성·티칭할 수 있는지 코드로 검증한다:

1. `AddDatumToSequence` 가 호출마다 `inspSeq.AddDatum(datumName)` 으로 새 DatumConfig 를 추가하는지 확인 — 4회 반복 호출로 datum 4개 생성 가능한지.
2. 새로 생성된 datum 의 AlgorithmType 을 VerticalTwoHorizontalDualImage 로 변경할 수 있는지(PropertyGrid AlgorithmType 드롭다운). DatumConfig.cs ICustomTypeDescriptor 가 DualImage 선택 시 TeachingImagePath + TeachingImagePath_Vertical 를 모두 노출하는지(L723 default fallthrough → return false 로 두 필드 노출).
3. 각 datum 노드의 Load/티칭 흐름이 자기 DatumConfig.TeachingImagePath(_Vertical) 에 경로를 기록하는지(Datum 노드 IOfflineImageParam = TeachingImagePath backing, DatumConfig.cs:59/67).

검증 결과 **막힘이 없으면 코드 변경 0** 으로 둔다 (D-37-07 "신규 UI 최소화"). 이 경우 InspectionListView.xaml.cs 는 무변경이며, SUMMARY 에 "기존 흐름으로 4-datum DualImage 생성/티칭 가능 — 코드 변경 0, 확인만" 으로 기록하고 frontmatter files_modified 의 실제 변경은 0 임을 명시한다.

**막힘이 발견되면** (예: AddDatumToSequence 가 첫 datum 만 허용, 또는 두 번째+ datum 노드가 티칭 이미지 경로를 못 받는 등) 그 막힘만 국소 보완한다. 보완 시:
- 변경은 InspectionListView.xaml.cs 에 국소화한다.
- 이 파일의 실제 brace 스타일(legacy K&R 또는 주변 코드 스타일)을 따른다.
- 변경 라인에 `//260528 hbk Phase 37 D-37-07` 주석을 단다.
- 신규 데이터 모델/대규모 UI 추가 금지 — 기존 AddDatum/AddDatumNode/PropertyGrid 재활용만.

어느 경우든 빌드(msbuild Debug/x64)가 PASS 해야 한다.
  </action>
  <verify>
    <automated>MISSING — 수동 코드 검증 + msbuild Debug/x64. grep: AddDatumToSequence 가 inspSeq.AddDatum 을 호출하는 경로 존재 확인</automated>
  </verify>
  <acceptance_criteria>
    - AddDatumToSequence → inspSeq.AddDatum(datumName) 호출 경로가 datum 추가를 반복 허용함이 확인됐다 (코드 인용)
    - DatumConfig ICustomTypeDescriptor 가 VerticalTwoHorizontalDualImage 선택 시 TeachingImagePath 와 TeachingImagePath_Vertical 를 둘 다 노출함이 확인됐다 (DatumConfig.cs L723 인용)
    - 코드 변경이 있었다면 InspectionListView.xaml.cs 에 국소화됐고 `//260528 hbk Phase 37` 주석이 있다; 변경이 없었다면 SUMMARY 에 "코드 변경 0 — 기존 흐름 확인" 으로 명시됐다
    - msbuild Debug/x64 PASS (0 새 error, 0 새 warning)
  </acceptance_criteria>
  <done>datum 4개(각 DualImage, 각자 가로/세로 티칭 이미지 경로) 생성·티칭이 기존 UI 흐름으로 가능함이 확인됐고, 필요한 최소 보완만 적용됐다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SIMUL UAT — 다중 datum 독립 실행 + 측정 이미지 분리 검증</name>
  <what-built>
    Plan 01/02 가 InspectionSequence.TryRunDatumPhase 를 lenient 로 전환하고, Action_FAIMeasurement.EStep.DatumPhase 를 datum loop 로 재작성했으며(per-datum 이미지 로드 + TryRunSingleDatum 누적), Plan 03 Task 1 이 datum 4개 생성/티칭 흐름을 확인했다. 측정 이미지(ShotParam.GetImage)는 datum 이미지(per-datum TeachingImagePath/Vertical)와 별개로 동작한다.
  </what-built>
  <how-to-verify>
1. SIMUL_MODE Debug/x64 빌드로 앱을 실행한다 (실행 중 exe 잠금 시 앱 종료 후 재빌드 — MSB3027 전례).
2. Side fixture 에 datum 을 4개 생성하고(기존 Datum 추가 흐름 반복), 각 datum 의 AlgorithmType 을 VerticalTwoHorizontalDualImage 로 설정한다.
3. 각 datum 에 자기 가로축(TeachingImagePath) + 세로축(TeachingImagePath_Vertical) 이미지를 지정한다. 사용 가능한 테스트 이미지:
   - Cal_Image/DualImageTest/SIDE1_3-1_Datum_A1_A2_backlight_LV30.bmp
   - Cal_Image/DualImageTest/SIDE1_3-1_Datum_B1_backlight_LV30.bmp
   (4 datum 분 8장이 모두 없으면, 가진 이미지로 일부 datum 만 정상 경로 지정하고 나머지는 의도적으로 빈/잘못된 경로로 둔다 → lenient skip 검증에 활용.)
4. Shot 의 측정(Measurement)들이 자기 DatumRef(예: Datum_1 등 이름)로 datum 을 참조하도록 둔다. Shot 의 측정 이미지(SimulImagePath)는 datum 티칭 이미지와 다른 파일로 둔다.
5. 검사를 1회 실행한다.

기대 결과:
- (독립 실행) datum 일부가 검출 실패해도 검사가 abort 되지 않고, 성공한 datum 을 참조하는 측정은 측정값/판정이 정상 표시된다. 실패 datum 을 참조하는 측정만 개별 실패(— 또는 NG)로 식별된다.
- (mixed/4-datum) datum 4개가 각자 자기 이미지로 검출되며, DatumConfigs[0] 하나에 묶이지 않는다 (로그에 datum 별 검출/실패 메시지 4건).
- (측정 ≠ datum 이미지) 측정은 Shot 의 SimulImagePath 이미지에서 수행되고, datum 보정은 datum 티칭 이미지로 검출된 transform 으로 적용된다 — 두 이미지가 다른 파일이어도 측정이 정상 동작한다.
- (예외 robustness) 빈/잘못된 datum 티칭 경로가 있어도 앱이 crash 하지 않고 해당 datum 만 skip + 로그 남긴다.

`.planning/phases/37-side-multi-datum-dualimage-2026-05-28/37-UAT.md` 에 각 항목 PASS/FAIL 을 기록한다.
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 발견된 이슈를 기술하세요 (FAIL 항목은 carry-over 또는 gap closure 로 처리)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → recipe(datum 생성/티칭 경로) | 운영자가 UI 로 datum 4개 + 티칭 이미지 경로를 셋업. 로컬 파일, 신규 외부/네트워크 입력 없음. |

이 plan 은 기존 UI 흐름 확인/국소 보완 + SIMUL 실행 검증만 한다. 신규 외부/네트워크 입력 표면 없음.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-06 | Denial of Service | datum 4개 티칭 경로 일부 누락 | mitigate | Plan 01/02 의 lenient skip 으로 누락 경로 datum 을 graceful 처리 — UAT 가 이 robustness 를 명시적으로 검증 (빈/잘못된 경로 시나리오 포함). |

block-on 심각도 = high. high-severity threat 없음.
</threat_model>

<verification>
- Task 1: msbuild Debug/x64 PASS, datum 4개 생성/티칭 흐름 확인 (코드 변경 있으면 국소+주석)
- Task 2: SIMUL UAT 4항목 (독립 실행 / 4-datum mixed / 측정≠datum 이미지 / 예외 robustness) 결과 기록
</verification>

<success_criteria>
- datum 4개 DualImage 생성/티칭이 기존 흐름으로 가능 (신규 UI 최소)
- SIMUL 에서 한 datum 실패가 다른 datum 의 측정을 막지 않음이 확인됨
- 측정 이미지 ≠ datum 이미지 동작 확인 (D-37-06)
- UAT.md 결과 기록
</success_criteria>

<output>
완료 후 `.planning/phases/37-side-multi-datum-dualimage-2026-05-28/37-03-SUMMARY.md` 생성 및 `37-UAT.md` 갱신
</output>
