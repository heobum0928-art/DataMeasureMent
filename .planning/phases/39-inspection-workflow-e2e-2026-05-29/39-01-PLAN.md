---
phase: 39-inspection-workflow-e2e-2026-05-29
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: true
requirements: [WF-01, WF-02]
requirements_addressed: [WF-01, WF-02]
tags: [datum, fai-gate, sequence, halcon]

must_haves:
  truths:
    - "Datum 검출 실패 시 해당 datum 이름이 InspectionSequence._failedDatums 에 기록된다"
    - "Measurement.DatumRef 가 실패 datum 이름과 일치하면 TryExecute 호출 없이 skip 되어 다음 Measurement 로 진행한다"
    - "DatumRef 빈 문자열(무보정) 또는 성공 datum 참조하는 Measurement 는 기존 경로 그대로 동작한다 (회귀 0)"
    - "Skip 된 Measurement 는 fai.WasDatumSkipped=true + meas.LastSkipReason=\"DATUM_FAIL\" 마크된다"
    - "Skip 된 Measurement 는 faiAllPass=false 누적 (skip 도 NG 강도로 처리, D-03 hierarchy 입력)"
    - "Phase 37 lenient 흐름(EStep.DatumPhase L119 Step=Grab) 변경 없이 동작 (D-07 회귀 가드)"
    - "InspectionSequence.TryGetDatumTransform 시그니처 변경 없음 (Phase 37 회귀 가드)"
    - "ClearAllResults / ClearResult 가 호출되면 WasDatumSkipped + LastSkipReason 도 함께 리셋된다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "_failedDatums HashSet + MarkDatumFailed/IsDatumFailed 메서드 + ClearDatumTransforms 에 Clear 통합"
      contains: "_failedDatums"
      contains_alt: "MarkDatumFailed"
      contains_alt2: "IsDatumFailed"
    - path: "WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs"
      provides: "WasDatumSkipped runtime 플래그 + ClearResult 리셋 통합"
      contains: "WasDatumSkipped"
    - path: "WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs"
      provides: "LastSkipReason string 휘발성 필드 + ClearResult 리셋 통합"
      contains: "LastSkipReason"
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "EStep.DatumPhase 실패 분기에 MarkDatumFailed 1줄씩 + EStep.Measure 진입에 IsDatumFailed gate 분기"
      contains: "MarkDatumFailed"
      contains_alt: "IsDatumFailed"
  key_links:
    - from: "Action_FAIMeasurement.cs (EStep.DatumPhase, L80-121)"
      to: "InspectionSequence._failedDatums"
      via: "parentSeq.MarkDatumFailed(datum.DatumName) — 4 실패 분기"
      pattern: "MarkDatumFailed"
    - from: "Action_FAIMeasurement.cs (EStep.Measure 루프, L162-244)"
      to: "InspectionSequence._failedDatums"
      via: "parentSeq2.IsDatumFailed(meas.DatumRef) → skip + LastSkipReason + WasDatumSkipped"
      pattern: "IsDatumFailed"
    - from: "Action_FAIMeasurement.cs (EStep.Init, L60-62)"
      to: "ShotParam.ClearAllResults"
      via: "FAIConfig.ClearResult + MeasurementBase.ClearResult cascade"
      pattern: "ClearAllResults"
---

<objective>
Phase 39 의 핵심 데이터 모델 변경. Datum 검출 실패를 per-FAI 차원에서 게이팅하기 위한 최소 인프라를 4 파일에 분산 구축한다. **Wave 1 foundation** — Plan 02 (TCP wire) + Plan 03 (UI overlay) 모두 본 plan 의 신규 필드/메서드에 의존.

Purpose: Phase 37 D-37-03 lenient 흐름(datum 부분 실패 시 abort 안 함)을 보존하면서, Measurement.DatumRef 기준 per-FAI gate 를 도입해 "다른 datum 의 FAI 는 정상 측정, 실패 datum 의 FAI 만 skip" 정책(WF-01)을 구현. D-01 (per-FAI gate), D-02 (subtype 구분), D-07 (lenient 가드) 한 묶음.

Output:
- `InspectionSequence._failedDatums` HashSet + 2 public 메서드 (`MarkDatumFailed`, `IsDatumFailed`)
- `FAIConfig.WasDatumSkipped` (TCP cycle aggregation 용)
- `MeasurementBase.LastSkipReason` string (UI 라벨 / Excel export 용)
- `Action_FAIMeasurement.EStep.DatumPhase` 실패 4분기 + `EStep.Measure` 게이트 진입

Anti-goals (회귀 가드):
- `TryGetDatumTransform` 시그니처 변경 0
- `EStep.DatumPhase` L119 `Step = (int)EStep.Grab` 변경 0 (Phase 37 lenient 유지)
- `EStep.Measure` try/catch lenient 블록 (L211-219) 변경 0
- 기존 identity fallback 경로(L166-173) 동작 보존 — `IsDatumFailed` 게이트 이후에 그대로 두기
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-CONTEXT.md
@.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- 본 plan 이 만들거나 의존하는 핵심 시그니처. 실행자는 코드베이스 탐색 없이 이 내용을 직접 참조한다. -->

InspectionSequence.cs (기존, 수정 대상 — L54, L192-194):
```csharp
private readonly Dictionary<string, HTuple> _datumTransforms = new Dictionary<string, HTuple>();  // L54 (기존)
public void ClearDatumTransforms() { _datumTransforms.Clear(); }                                   // L192-194 (기존)
public bool TryGetDatumTransform(string datumRef, out HTuple transform);                           // L219-225 (기존 — 변경 금지)
```

InspectionSequence.cs (신규 — 본 task 추가):
```csharp
private readonly HashSet<string> _failedDatums = new HashSet<string>();  // _datumTransforms 옆 신규 필드
public void MarkDatumFailed(string datumName) { if (!string.IsNullOrEmpty(datumName)) _failedDatums.Add(datumName); }
public bool IsDatumFailed(string datumRef) { return !string.IsNullOrEmpty(datumRef) && _failedDatums.Contains(datumRef); }
// ClearDatumTransforms 본문에 _failedDatums.Clear() 1줄 추가
```

FAIConfig.cs (기존, 수정 대상 — L96-101, L126-129):
```csharp
[Browsable(false)] public double MeasuredValue { get; set; }   // L97-98 (기존)
[Browsable(false)] public bool IsPass { get; set; }            // L100-101 (기존)
public void ClearResult() { MeasuredValue = 0; IsPass = false; }  // L126-129 (기존)
```

FAIConfig.cs (신규 — 본 task 추가):
```csharp
[Browsable(false)] public bool WasDatumSkipped { get; set; }   // L101 직후 추가
// ClearResult 본문에 WasDatumSkipped = false; 1줄 추가
```

MeasurementBase.cs (기존, 수정 대상 — L43-52, L90-95):
```csharp
[PropertyTools.DataAnnotations.Browsable(false)] public double LastMeasuredValue { get; set; }  // L43-44
[PropertyTools.DataAnnotations.Browsable(false)] public bool LastJudgement { get; set; }        // L46-47
[PropertyTools.DataAnnotations.Browsable(false)] public bool LastHasResult { get; set; }        // L51-52
public void ClearResult() {
    LastMeasuredValue = 0; LastJudgement = false; LastHasResult = false;
}  // L90-95
```

MeasurementBase.cs (신규 — 본 task 추가):
```csharp
[PropertyTools.DataAnnotations.Browsable(false)] public string LastSkipReason { get; set; }  // L52 직후 추가
// ClearResult 본문에 LastSkipReason = null; 1줄 추가
```

Action_FAIMeasurement.cs (기존 — 게이트 적용 지점 L162-173):
```csharp
foreach (var fai in ShotParam.FAIList) {
    bool faiAllPass = true;
    foreach (var meas in fai.Measurements) {
        HTuple transform;
        if (parentSeq2 == null || !parentSeq2.TryGetDatumTransform(meas.DatumRef, out transform)) {
            try { HOperatorSet.HomMat2dIdentity(out transform); } catch { transform = new HTuple(); }
        }
        // ... (consumer 주입 L177-207, TryExecute L208-243 — 변경 0)
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: InspectionSequence per-FAI gate infrastructure (D-01)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (특히 L54 `_datumTransforms` 필드, L192-194 `ClearDatumTransforms`, L219-225 `TryGetDatumTransform`)
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-CONTEXT.md (특히 D-01, specifics 옵션 A, deferred D-37-03 lenient 가드)
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-PATTERNS.md (특히 InspectionSequence 옵션 A 분석 L213-221, "TryGetDatumTransform 시그니처 변경 금지" 가드)
  </read_first>
  <behavior>
    - `_failedDatums` 필드는 `_datumTransforms` 와 동일 lifecycle: 같이 생성, 같이 Clear.
    - `MarkDatumFailed(null)` / `MarkDatumFailed("")` 무동작 (set 에 추가 안 됨).
    - `IsDatumFailed("")` = false (빈 DatumRef = 무보정 의도, 게이트 무관).
    - `IsDatumFailed("Datum_NonExistent")` = false (실패 기록 없는 datum 은 통과 — _datumTransforms lookup 미스는 별도 처리).
    - `ClearDatumTransforms()` 호출 후 `IsDatumFailed("X")` = false (cleared).
    - `TryGetDatumTransform` 시그니처/본문 변경 0 — Phase 37 회귀 가드.
  </behavior>
  <action>
1. L54 `private readonly Dictionary<string, HTuple> _datumTransforms = new Dictionary<string, HTuple>();` 라인 **바로 아래**에 신규 필드 1줄 삽입:
   ```csharp
   //260529 hbk Phase 39 WF-01 D-01 — datum 검출 실패 datum 이름 집합 (per-FAI gate 신호). _datumTransforms 와 동일 lifecycle.
   private readonly HashSet<string> _failedDatums = new HashSet<string>(); //260529 hbk Phase 39 WF-01 D-01
   ```

2. `using System.Collections.Generic;` (L3) 이미 있으므로 추가 import 불필요.

3. L192-194 `ClearDatumTransforms()` 본문에 1줄 추가 (마지막 줄):
   ```csharp
   //260528 hbk Phase 37 D-37-05 — DatumPhase loop 시작 전 1회 호출 (per-datum 누적 전 초기화)
   public void ClearDatumTransforms() {
       _datumTransforms.Clear();
       _failedDatums.Clear(); //260529 hbk Phase 39 WF-01 D-01 — _datumTransforms 와 동일 lifecycle
   }
   ```

4. L194 `ClearDatumTransforms()` 메서드 **직후, L196 `public bool TryRunSingleDatum` 이전**에 신규 2 메서드 삽입:
   ```csharp
   //260529 hbk Phase 39 WF-01 D-01 — 검출 실패 datum 기록. Action_FAIMeasurement.EStep.DatumPhase 실패 분기에서 호출.
   //  null/empty 가드: PATTERNS.md 옵션 A 분석 L217. _failedDatums 단일 set 에 idempotent add.
   public void MarkDatumFailed(string datumName) //260529 hbk Phase 39 WF-01 D-01
   {
       if (!string.IsNullOrEmpty(datumName)) _failedDatums.Add(datumName); //260529 hbk Phase 39 WF-01 D-01
   }

   //260529 hbk Phase 39 WF-01 D-01 — per-FAI gate 조회. Measurement.DatumRef 가 실패 datum 이름과 일치하면 true.
   //  빈 DatumRef = 무보정 의도이므로 게이트 무관 (false 반환 — TryGetDatumTransform identity fallback 경로로 진행).
   public bool IsDatumFailed(string datumRef) //260529 hbk Phase 39 WF-01 D-01
   {
       return !string.IsNullOrEmpty(datumRef) && _failedDatums.Contains(datumRef); //260529 hbk Phase 39 WF-01 D-01
   }
   ```

5. **변경 금지 — Phase 37 회귀 가드:**
   - L219-225 `TryGetDatumTransform` 시그니처 및 본문
   - L146-189 `TryRunDatumPhase` 두 오버로드
   - L196-216 `TryRunSingleDatum`
   - L17-37 `InspectionSequenceContext`

6. C# 7.2 호환 주의: `nameof`, `string.IsNullOrEmpty` 모두 4.8 + 7.2 안전. switch expression / pattern matching 미사용.

7. 마커 컨벤션: 모든 신규 라인 끝에 `//260529 hbk Phase 39 WF-01 D-01 — <intent>` 부착. 기존 `//260528 hbk Phase 37` 마커 절대 보존.
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_failedDatums" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs | wc -l` >= 4 (선언 + Clear + Mark + Is 4건)
    - `grep -q "private readonly HashSet<string> _failedDatums" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 매치
    - `grep -q "public void MarkDatumFailed(string datumName)" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 매치
    - `grep -q "public bool IsDatumFailed(string datumRef)" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 매치
    - `grep -q "_failedDatums.Clear();" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 매치 (ClearDatumTransforms 내부)
    - `grep -q "//260529 hbk Phase 39 WF-01 D-01" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 매치
    - `grep -q "public bool TryGetDatumTransform(string datumRef, out HTuple transform)" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 매치 (Phase 37 시그니처 보존 — 회귀 가드)
    - msbuild Debug/x64 exit code 0, 신규 warning 0
  </acceptance_criteria>
  <done>
    - 4 변경 적용: 신규 필드 1, ClearDatumTransforms +1줄, MarkDatumFailed 메서드 1, IsDatumFailed 메서드 1
    - TryGetDatumTransform / TryRunDatumPhase / TryRunSingleDatum 시그니처/본문 회귀 0
    - 신규 마커 6개 (필드 1 + Clear 라인 1 + MarkDatumFailed 메서드 4-5개 라인 + IsDatumFailed 메서드 3-4개 라인) 부착
    - msbuild Debug/x64 PASS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: FAIConfig + MeasurementBase skip subtype fields (D-02)</name>
  <files>
    WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs,
    WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
  </files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs (특히 L96-101 MeasuredValue/IsPass 필드, L126-129 ClearResult)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (특히 L43-52 LastMeasuredValue/LastJudgement/LastHasResult, L90-95 ClearResult)
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-CONTEXT.md (특히 D-02 subtype 구분, "FAI 레벨 + Measurement 레벨 둘 다 두기" 가이드)
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-PATTERNS.md (특히 FAIConfig L226-256 D-02 패턴 + MeasurementBase L267-294 D-02 string 채택 근거 + LastHasResult analog 근거)
  </read_first>
  <behavior>
    - `FAIConfig.WasDatumSkipped`: bool, default false. AddResponse 가 cycle aggregation (anyDatumSkip 누적) 에 사용. INI 미직렬화 ([Browsable(false)] 보장).
    - `FAIConfig.ClearResult()`: 기존 (MeasuredValue=0, IsPass=false) + WasDatumSkipped=false 1줄 추가. Action_FAIMeasurement EStep.Init L60-62 `ShotParam.ClearAllResults` 통해 cascade.
    - `MeasurementBase.LastSkipReason`: string, default null. "DATUM_FAIL" 또는 null 만 사용 (현재 phase). 추후 OUT-* 에서 다른 값 가능성 열어둠.
    - `MeasurementBase.ClearResult()`: 기존 (LastMeasuredValue=0, LastJudgement=false, LastHasResult=false) + LastSkipReason=null 1줄 추가.
    - 두 필드 모두 PATTERNS.md `LastHasResult` analog 패턴 정확 차용 — `[PropertyTools.DataAnnotations.Browsable(false)]` 접두 + ClearResult 동기 리셋.
  </behavior>
  <action>
**Part A — FAIConfig.cs**

1. L101 `public bool IsPass { get; set; }` 라인 **직후, L103 빈 줄 사이**에 신규 필드 삽입:
   ```csharp
   //260529 hbk Phase 39 WF-01 D-02 — datum-skip subtype 플래그. true 이면 FAI 가 측정 미실행(datum 검출 실패 원인).
   //  AddResponse 가 EVisionResultType.NotExist ('N') 매핑에 사용 (Plan 02 wire 매핑). INI 미직렬화 ([Browsable(false)] runtime 전용).
   //  ClearResult / Action_FAIMeasurement EStep.Init ShotParam.ClearAllResults 에서 false 로 리셋.
   [Browsable(false)] //260529 hbk Phase 39 WF-01 D-02
   public bool WasDatumSkipped { get; set; } //260529 hbk Phase 39 WF-01 D-02
   ```

2. L126-129 `ClearResult()` 본문에 1줄 추가:
   ```csharp
   public void ClearResult() {
       MeasuredValue = 0;
       IsPass = false;
       WasDatumSkipped = false; //260529 hbk Phase 39 WF-01 D-02
   }
   ```

3. **변경 금지:** L11 ICustomTypeDescriptor 구현, L260-319 GetProperties / FilterProperties / IsHiddenForEdgeMeasureType / IsLegacyEdgeParam (Phase 19/18/38 회귀 가드).

**Part B — MeasurementBase.cs**

4. L52 `public bool LastHasResult { get; set; } //260517 hbk CO-23-01` 라인 **직후, L53 빈 줄 사이**에 신규 필드 삽입:
   ```csharp
   //260529 hbk Phase 39 WF-01 D-02 — measurement 단위 skip reason. null/"" = 정상 또는 측정 NG, "DATUM_FAIL" = datum 검출 실패로 skip.
   //  string 채택 근거 (PATTERNS.md L283): enum 신설 시 INI/직렬화 영향 검토 필요. string 은 ParamBase reflection 의 case "String" 경로 자동 호환.
   //  D-10 (v2.6 순수 유지) — enum 신설 안 함. UI 'DETECT FAIL' 라벨 분기 + Excel export (Phase 40/41) 용.
   [PropertyTools.DataAnnotations.Browsable(false)] //260529 hbk Phase 39 WF-01 D-02
   public string LastSkipReason { get; set; } //260529 hbk Phase 39 WF-01 D-02
   ```

5. L90-95 `ClearResult()` 본문에 1줄 추가:
   ```csharp
   public void ClearResult() //260413 hbk
   {
       LastMeasuredValue = 0;
       LastJudgement = false;
       LastHasResult = false; //260517 hbk CO-23-01: 미측정 상태 복원
       LastSkipReason = null; //260529 hbk Phase 39 WF-01 D-02 — datum-skip subtype 리셋
   }
   ```

6. **변경 금지:** L17-30 (DatumRef / MeasurementName), L32-41 (Tolerance), L63-69 (TryExecute abstract), L76-88 (EvaluateJudgement).

7. C# 7.2 호환: `[Browsable(false)]` namespace 차이 주의 — FAIConfig 는 `using PropertyTools.DataAnnotations;` (L3) 보유 → bare `[Browsable(false)]` 사용 (기존 L97/L100 동일 패턴). MeasurementBase 는 fully-qualified `[PropertyTools.DataAnnotations.Browsable(false)]` (기존 L43/L46/L51 동일 패턴) 사용. **두 파일의 컨벤션이 다르므로 각 파일의 기존 라인을 그대로 따라간다.**
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "public bool WasDatumSkipped { get; set; }" WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs` 매치
    - `grep -q "WasDatumSkipped = false" WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs` 매치 (ClearResult 내부)
    - `grep -q "public string LastSkipReason { get; set; }" WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs` 매치
    - `grep -q "LastSkipReason = null" WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs` 매치 (ClearResult 내부)
    - `grep -q "//260529 hbk Phase 39 WF-01 D-02" WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs` 매치
    - `grep -q "//260529 hbk Phase 39 WF-01 D-02" WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs` 매치
    - `grep -c "//260517 hbk CO-23-01" WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs` >= 3 (기존 마커 보존 — Phase 20 D-12 stacking)
    - `grep -c "//260526 hbk CO-31-01" WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs` >= 1 (기존 마커 보존)
    - msbuild Debug/x64 exit code 0, 신규 warning 0
  </acceptance_criteria>
  <done>
    - FAIConfig: WasDatumSkipped 필드 1 + ClearResult +1줄
    - MeasurementBase: LastSkipReason 필드 1 + ClearResult +1줄
    - 두 파일 모두 기존 마커 (CO-23-01, CO-31-01, Phase 6 등) 100% 보존
    - msbuild Debug/x64 PASS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Action_FAIMeasurement gate wiring (D-01, D-07 가드)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (특히 L80-121 EStep.DatumPhase 4 실패 분기, L154-263 EStep.Measure 루프, L162-173 게이트 적용 지점)
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-CONTEXT.md (특히 D-01 per-FAI gate, D-07 lenient 유지)
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-PATTERNS.md (특히 Action_FAIMeasurement §A L29-69 게이팅 신호 + §B L72-101 게이트 적용 흐름 + Anti-pattern Guards "Phase 37 D-37-03 lenient 회귀 0 가드" L620-625)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (Task 1 에서 추가한 MarkDatumFailed/IsDatumFailed 시그니처 확인)
  </read_first>
  <behavior>
    - **DatumPhase 게이트 신호 기록 (4 분기):**
      - L92 `continue;` (DualImage 이미지 취득 실패) **직전** → `parentSeq.MarkDatumFailed(datum.DatumName);`
      - L96 datum 검출 실패 로그 **직후** (catch finally 진입 전) → `parentSeq.MarkDatumFailed(datum.DatumName);`
      - L105 `continue;` (1-image 이미지 취득 실패) **직전** → `parentSeq.MarkDatumFailed(datum.DatumName);`
      - L111 datum 검출 실패 로그 **직후** (catch finally 진입 전) → `parentSeq.MarkDatumFailed(datum.DatumName);`
    - **Measure 게이트 적용 (1 분기):**
      - L165 `HTuple transform;` 라인 **직후, L166 `if (parentSeq2 == null || ...` 이전** 에 새 게이트 블록 삽입.
      - 게이트 조건: `parentSeq2 != null && parentSeq2.IsDatumFailed(meas.DatumRef)` 만 true 일 때 skip.
      - Skip 시 동작: `meas.ClearResult();` → `meas.LastSkipReason = "DATUM_FAIL";` → fai.WasDatumSkipped 표시는 외부 루프 종료 시 일괄 처리 → `meas.LastJudgement = false;` (`faiAllPass=false` 누적) → `measuredCount++;` → `continue;` (다음 meas).
    - **fai.WasDatumSkipped 집계:** L247-252 `if (fai.Measurements.Count > 0)` 블록 안에서 한 번에 처리:
      - `fai.WasDatumSkipped = fai.Measurements.Any(m => m.LastSkipReason == "DATUM_FAIL");` — System.Linq 추가 필요 → **No**, 명시 for-loop 채용 (`using System.Linq` 추가 회피, CLAUDE.md C# 7.2 컨벤션 + 기존 파일 import 보존).
    - **회귀 0 가드:**
      - L119 `Step = (int)EStep.Grab;` 변경 0 (Phase 37 D-37-03)
      - L166-173 identity fallback 경로 보존 (게이트 분기 안 걸린 경우만 도달)
      - L211-219 try/catch lenient 블록 변경 0 (D-07)
      - L229-238 overlay suffix 부여 블록 변경 0 (Phase 7-02)
      - L240-243 `!meas.LastJudgement → faiAllPass=false` 누적 변경 0
  </behavior>
  <action>
1. **DatumPhase L86-117 의 4 실패 분기에 `MarkDatumFailed` 1줄씩 추가.**

   현재 L88-100 (DualImage 분기):
   ```csharp
   try {
       if (!TryGrabOrLoadDualDatumImages(datum, out imgH, out imgV)) {
           Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + (datum.DatumName ?? "") + "' DualImage 취득 실패 (skip)");
           continue;
       }
       string derr;
       if (!parentSeq.TryRunSingleDatum(datum, imgH, imgV, out derr)) {
           Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + (datum.DatumName ?? "") + "' 검출 실패 (skip): " + (derr ?? ""));
       }
   } finally { ... }
   ```

   변경 후:
   ```csharp
   try {
       if (!TryGrabOrLoadDualDatumImages(datum, out imgH, out imgV)) {
           Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + (datum.DatumName ?? "") + "' DualImage 취득 실패 (skip)");
           parentSeq.MarkDatumFailed(datum.DatumName); //260529 hbk Phase 39 WF-01 D-01 — per-FAI gate 신호 기록
           continue;
       }
       string derr;
       if (!parentSeq.TryRunSingleDatum(datum, imgH, imgV, out derr)) {
           Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + (datum.DatumName ?? "") + "' 검출 실패 (skip): " + (derr ?? ""));
           parentSeq.MarkDatumFailed(datum.DatumName); //260529 hbk Phase 39 WF-01 D-01 — per-FAI gate 신호 기록
       }
   } finally { ... }
   ```

2. **L101-115 (1-image 분기) 동일 패턴 적용:**
   ```csharp
   } else {
       HImage img = GrabOrLoadDatumImage(datum);
       if (img == null) {
           Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + (datum.DatumName ?? "") + "' 이미지 취득 실패 (skip)");
           parentSeq.MarkDatumFailed(datum.DatumName); //260529 hbk Phase 39 WF-01 D-01 — per-FAI gate 신호 기록
           continue;
       }
       try {
           string derr;
           if (!parentSeq.TryRunSingleDatum(datum, img, null, out derr)) {
               Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + (datum.DatumName ?? "") + "' 검출 실패 (skip): " + (derr ?? ""));
               parentSeq.MarkDatumFailed(datum.DatumName); //260529 hbk Phase 39 WF-01 D-01 — per-FAI gate 신호 기록
           }
       } finally { ... }
   }
   ```

3. **EStep.Measure 게이트 — L165 직후 신규 블록 삽입.**

   현재 L164-173:
   ```csharp
   foreach (var meas in fai.Measurements) {
       HTuple transform;
       if (parentSeq2 == null || !parentSeq2.TryGetDatumTransform(meas.DatumRef, out transform)) {
           //260413 hbk Fixture 미존재 또는 미지정 DatumRef → identity fallback
           try {
               HOperatorSet.HomMat2dIdentity(out transform);
           } catch {
               transform = new HTuple();
           }
       }
       ...
   ```

   변경 후 (L164 와 L165 사이에 게이트 블록 삽입):
   ```csharp
   foreach (var meas in fai.Measurements) {
       //260529 hbk Phase 39 WF-01 D-01 — per-FAI gate: 해당 datum 이 검출 실패했으면 측정 skip, NG 누적, 다음 meas 진행.
       //  L119 Step=Grab 변경 안 함 (Phase 37 D-37-03 lenient 유지). 본 게이트는 Measure 루프 안에서만 동작.
       //  빈 DatumRef (무보정) 또는 성공 datum 참조는 IsDatumFailed=false → 기존 identity fallback / transform 경로 진행.
       if (parentSeq2 != null && parentSeq2.IsDatumFailed(meas.DatumRef)) //260529 hbk Phase 39 WF-01 D-01
       {
           meas.ClearResult(); //260529 hbk Phase 39 WF-01 D-01 — runtime 결과 클리어
           meas.LastSkipReason = "DATUM_FAIL"; //260529 hbk Phase 39 WF-01 D-02 — UI 'DETECT FAIL' 라벨 + Excel export 분기 신호
           meas.LastJudgement = false; //260529 hbk Phase 39 WF-01 D-01 — faiAllPass=false 누적 (skip 도 NG 강도)
           Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Measurement '" + (meas.MeasurementName ?? meas.TypeName) + "' skipped — datum '" + (meas.DatumRef ?? "") + "' 검출 실패 (D-01)"); //260529 hbk Phase 39 WF-01 D-01
           faiAllPass = false; //260529 hbk Phase 39 WF-01 D-01 — 외부 if(!meas.LastJudgement) 와 의미 동일이지만 명시
           measuredCount++; //260529 hbk Phase 39 WF-01 D-01 — measuredCount 도 증가 (시도 회수 통계)
           continue; //260529 hbk Phase 39 WF-01 D-01 — 다음 measurement 진행 (TryExecute 호출 안 함)
       }
       HTuple transform;
       if (parentSeq2 == null || !parentSeq2.TryGetDatumTransform(meas.DatumRef, out transform)) {
           //260413 hbk Fixture 미존재 또는 미지정 DatumRef → identity fallback
           try {
               HOperatorSet.HomMat2dIdentity(out transform);
           } catch {
               transform = new HTuple();
           }
       }
       ...
   ```

4. **fai.WasDatumSkipped 집계 — L247-252 변경.**

   현재 L245-253:
   ```csharp
   //260413 hbk FAIConfig legacy 필드(IsPass/MeasuredValue)에 대표 결과 집계 —
   // 첫 Measurement 결과를 사용해 UI/TCP 호환 유지
   if (fai.Measurements.Count > 0) {
       fai.IsPass = faiAllPass;
       fai.MeasuredValue = fai.Measurements[0].LastMeasuredValue;
   } else {
       fai.ClearResult();
   }
   if (!faiAllPass) allPass = false;
   ```

   변경 후:
   ```csharp
   //260413 hbk FAIConfig legacy 필드(IsPass/MeasuredValue)에 대표 결과 집계 —
   // 첫 Measurement 결과를 사용해 UI/TCP 호환 유지
   if (fai.Measurements.Count > 0) {
       fai.IsPass = faiAllPass;
       fai.MeasuredValue = fai.Measurements[0].LastMeasuredValue;
       //260529 hbk Phase 39 WF-01 D-02 — fai 하위 measurement 중 1건이라도 DATUM_FAIL 이면 fai 도 datum-skip 마크.
       //  Plan 02 AddResponse 가 anyDatumSkip 누적용으로 사용 (cycle = NotExist 분기). System.Linq 도입 회피 — for-loop.
       bool wasSkip = false; //260529 hbk Phase 39 WF-01 D-02
       foreach (var m in fai.Measurements) //260529 hbk Phase 39 WF-01 D-02
       {
           if (m != null && m.LastSkipReason == "DATUM_FAIL") { wasSkip = true; break; } //260529 hbk Phase 39 WF-01 D-02
       }
       fai.WasDatumSkipped = wasSkip; //260529 hbk Phase 39 WF-01 D-02
   } else {
       fai.ClearResult();
   }
   if (!faiAllPass) allPass = false;
   ```

5. **변경 금지:**
   - L80-83 (EStep.DatumPhase 진입 + ClearDatumTransforms)
   - L119 `Step = (int)EStep.Grab;` (Phase 37 D-37-03)
   - L166-173 identity fallback try/catch
   - L177-207 IDatumOriginConsumer 주입
   - L211-219 try/catch lenient
   - L221-243 EvaluateJudgement / overlay suffix
   - L60-62 ShotParam.ClearAllResults (cascade 가 Task 2 의 ClearResult 신규 라인 자동 호출)

6. 마커 컨벤션: 모든 신규 라인 끝에 `//260529 hbk Phase 39 WF-01 D-XX — <intent>` 부착. 기존 `//260528 hbk Phase 37` / `//260519 hbk Phase 31` / `//260422 hbk Phase 7` / `//260413 hbk Phase 6` 마커 100% 보존 (Phase 20 D-12 stacking).
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MarkDatumFailed(datum.DatumName)" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` == 4 (4 실패 분기)
    - `grep -q "if (parentSeq2 != null && parentSeq2.IsDatumFailed(meas.DatumRef))" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 매치
    - `grep -q 'meas.LastSkipReason = "DATUM_FAIL"' WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 매치
    - `grep -q "fai.WasDatumSkipped = wasSkip" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 매치
    - `grep -q "Step = (int)EStep.Grab;" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 매치 (Phase 37 D-37-03 lenient 보존)
    - `grep -q "HOperatorSet.HomMat2dIdentity(out transform);" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 매치 (identity fallback 경로 보존)
    - `grep -q "//260528 hbk Phase 37 D-37-03" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 매치 (Phase 37 마커 보존)
    - `grep -q "//260422 hbk Phase 7" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 매치 (Phase 7 마커 보존)
    - `grep -c "//260529 hbk Phase 39 WF-01" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` >= 12 (DatumPhase 4 + Measure 게이트 ~6-8 + 집계 ~5)
    - `grep -q "using System.Linq" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` **미매치** (System.Linq 도입 회피 가드)
    - msbuild Debug/x64 exit code 0, 신규 warning 0
  </acceptance_criteria>
  <done>
    - DatumPhase 4 실패 분기에 MarkDatumFailed 1줄씩 추가
    - Measure 게이트 블록 1 (8-10 줄) 삽입
    - WasDatumSkipped 집계 for-loop 1 (5 줄) 추가
    - Phase 37/31/7/6 모든 기존 마커 보존
    - System.Linq import 추가 안 함 (CLAUDE.md C# 7.2 + 기존 import 컨벤션)
    - msbuild Debug/x64 PASS
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TCP request → InspectionSequence | 외부 핸들러가 TestRequestPacket 으로 사이클 트리거 — 본 plan 영향 없음 (시퀀스 진입 후 데이터 모델만 변경) |
| Sequence thread ↔ UI thread | _datumTransforms / _failedDatums 는 시퀀스 단일 스레드 (SequenceBase.Run) 에서만 접근 — lock 불필요 (Phase 6/37 패턴) |
| Action ↔ Measurement | parentSeq2.IsDatumFailed(meas.DatumRef) 는 동일 스레드 내 read-only — race condition 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Tampering | _failedDatums HashSet | accept | private readonly, 시퀀스 스레드 단일 소유. 외부 setter 없음. |
| T-39-02 | Information Disclosure | LastSkipReason string | accept | runtime 전용, INI 저장 안 됨 (`[Browsable(false)]` + Plan 02 직렬화 분기 검증). PII 없음. |
| T-39-03 | Denial of Service | MarkDatumFailed 무한 호출 | accept | HashSet idempotent add. datum 개수 유한 (멀티샷 N=2-8). 메모리 폭증 불가. |
| T-39-04 | Repudiation | datum skip 사유 추적 부재 | mitigate | Logging.PrintLog((int)ELogType.Error, "...") 기존 패턴 (Phase 37 D-37-03 동일) 으로 모든 4 실패 분기 + Measure 게이트 1 곳 총 5 곳 로깅. |
| T-39-05 | Elevation | Phase 37 lenient 우회 | mitigate | TryGetDatumTransform 시그니처 보존 + L119 Step=Grab 보존 가드 (msbuild 가드는 grep acceptance_criteria 에서 강제). |

</threat_model>

<verification>
**Build verification:**
- msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 exit code 0
- 신규 warning 0 (기존 baseline 유지)

**구조 검증 (Task 1):**
- InspectionSequence._failedDatums 필드 존재
- MarkDatumFailed / IsDatumFailed 2 public 메서드 존재
- TryGetDatumTransform 시그니처 변경 0

**구조 검증 (Task 2):**
- FAIConfig.WasDatumSkipped + ClearResult 리셋 통합
- MeasurementBase.LastSkipReason + ClearResult 리셋 통합

**구조 검증 (Task 3):**
- Action_FAIMeasurement EStep.DatumPhase 4 실패 분기에 MarkDatumFailed 호출
- Action_FAIMeasurement EStep.Measure 진입 게이트 블록 존재
- Step = (int)EStep.Grab (L119, Phase 37 D-37-03) 보존
- try/catch lenient (L211-219) 보존
- System.Linq import 미추가
</verification>

<success_criteria>
- 4 파일 수정, msbuild Debug/x64 PASS
- 모든 acceptance_criteria grep 매치 통과
- Plan 02 (TCP wire) 가 fai.WasDatumSkipped + EVisionResultType.NotExist 매핑 가능한 상태
- Plan 03 (UI overlay) 가 datum.LastFindSucceeded (기존) + measurement.LastSkipReason 신규 필드 모두 참조 가능한 상태
- Phase 37 lenient 흐름 + Phase 7-02 overlay suffix + Phase 31 IDatumOriginConsumer 회귀 0
</success_criteria>

<output>
After completion, create `.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-01-SUMMARY.md` with:
- 신규 API 4종: `_failedDatums` (private field), `MarkDatumFailed(string)`, `IsDatumFailed(string)`, `WasDatumSkipped` (FAIConfig), `LastSkipReason` (MeasurementBase)
- Phase 37 D-37-03 lenient 보존 증거 (L119 Step=Grab + L211-219 try/catch + L166-173 identity fallback)
- 다음 Plan 02 에서 사용할 인터페이스: `fai.WasDatumSkipped` (cycle aggregation), `EVisionResultType.NotExist` 매핑 가능
- 다음 Plan 03 에서 사용할 인터페이스: `meas.LastSkipReason == "DATUM_FAIL"` (UI 'DETECT FAIL' 라벨 분기), `datum.LastFindSucceeded` (기존 — overlay 라벨 게이트)
</output>
