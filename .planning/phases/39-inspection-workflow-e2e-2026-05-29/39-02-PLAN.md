---
phase: 39-inspection-workflow-e2e-2026-05-29
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/TcpServer/VisionResponsePacket.cs
autonomous: true
requirements: [WF-01, WF-02]
requirements_addressed: [WF-01, WF-02]
tags: [tcp, wire-protocol, datum, fai-gate]

must_haves:
  truths:
    - "InspectionSequence.AddResponse 가 3-state 계층(검출실패 > NG > OK)에 따라 cycle Result 를 결정한다"
    - "사이클 내 fai.WasDatumSkipped=true 가 1건이라도 있으면 Result = EVisionResultType.NotExist ('N')"
    - "Skip 없고 fai.IsPass=false 가 1건이라도 있으면 Result = EVisionResultType.NG ('X')"
    - "모든 FAI 가 IsPass=true 이고 skip 없으면 Result = EVisionResultType.OK ('O')"
    - "FAIResults[i] 직렬화가 3-state ('P' / 'F' / 'N') 출력한다 (datum-skip FAI = 'N')"
    - "FAIResultData 신규 ctor (string, EVisionResultType, double) 추가, 기존 ctor (string, bool, double) 보존 (회귀 0)"
    - "TestResultPacket.GetResultString 기존 코드 변경 0 — NotExist→'N' 매핑은 이미 L564-565 에 있음"
    - "v2.6 enum/wire 상수 신규 추가 0 (TEST_RESULT_NOTEXIST='N' 기존 재사용 — D-08/D-10 충족)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "AddResponse 3-state hierarchy + FAIResultData 신규 ctor 호출"
      contains: "anyDatumSkip"
      contains_alt: "EVisionResultType.NotExist"
    - path: "WPF_Example/TcpServer/VisionResponsePacket.cs"
      provides: "FAIResultData 신규 ctor (EVisionResultType 직접) + 동적 FAI 직렬화 3-state 분기"
      contains: "EVisionResultType result"
      contains_alt: "TEST_RESULT_NOTEXIST"
  key_links:
    - from: "InspectionSequence.AddResponse"
      to: "fai.WasDatumSkipped (Plan 01)"
      via: "anyDatumSkip 누적 → responsePacket.Result = NotExist"
      pattern: "fai.WasDatumSkipped"
    - from: "InspectionSequence.AddResponse"
      to: "FAIResultData(string, EVisionResultType, double)"
      via: "신규 ctor 호출로 3-state code 직접 전달"
      pattern: "new FAIResultData"
    - from: "VisionResponsePacket.Convert (동적 FAI 직렬화 L353-365)"
      to: "TEST_RESULT_NOTEXIST 상수"
      via: "faiData.Result == NotExist → 'N' 출력"
      pattern: "TEST_RESULT_NOTEXIST"
---

<objective>
Phase 39 의 TCP wire 매핑. Plan 01 의 신규 `fai.WasDatumSkipped` 플래그를 v2.6 와이어 포맷으로 변환:
- **사이클 결과** = 검출실패(N) > NG(X) > OK(O) 계층 (D-03, D-05)
- **FAIResults[i]** = P (PASS) / F (FAIL) / N (datum skip) 3-state (D-06)
- **wire 포맷 신규 필드 추가 0** (D-08) — 기존 `TEST_RESULT_NOTEXIST="N"` 상수 + `EVisionResultType.NotExist` enum 재사용

Purpose: 제어 측 코드가 이미 `OK/NG/NotExist` 3분기 파싱 가능하므로 NotExist 재사용으로 v2.6 호환성 100% 유지하면서 검출실패 정보를 전달. v2.7 PROTO-* (Phase 48/49) 도입 시에도 의미 충돌 없음.

Output:
- `InspectionSequence.AddResponse` 본문 교체 (3-state hierarchy + FAIResults 신규 ctor 호출)
- `VisionResponsePacket.FAIResultData` 신규 ctor 1 추가 (기존 bool ctor 보존)
- `VisionResponsePacket.Convert` 동적 FAI 직렬화 분기 (L353-365) 3-state switch 로 교체

Anti-goals (D-10 v2.6 순수 유지 가드):
- `EVisionResultType` enum 신규 값 추가 금지 (DetectFail 등)
- `TestResultPacket` 신규 필드 추가 금지 (ngCount, detectFailCount footer 금지 — D-08)
- `TEST_RESULT_*` wire 상수 추가 금지
- `CycleState` / `ECycleResult` enum 도입 금지 (Phase 49 PROTO-* 영역)
- `TestResultPacket.GetResultString` (L560-574), `SetResultFromString` (L552-558) 본문 변경 0 (이미 NotExist 매핑 보유)
- `FAIResultData` 기존 ctor (string, bool, double) 변경 0 (외부 호출 호환)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-CONTEXT.md
@.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-PATTERNS.md
@.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Plan 01 에서 만들어진 인터페이스 + 본 Plan 이 수정/추가하는 인터페이스 -->

Plan 01 산출 (의존):
```csharp
// FAIConfig.cs 신규 — Plan 01 Task 2 추가
[Browsable(false)]
public bool WasDatumSkipped { get; set; }
// AddResponse 가 anyDatumSkip 누적 판정에 사용
```

VisionResponsePacket.cs 기존 (변경 금지):
```csharp
// L22-28 (변경 금지 — D-10 가드)
public enum EVisionResultType : int {
    NG = 0,
    OK = 1,
    NotExist = 2,
    ANG = 3,
    TECHING = 4,
}

// L48-50 (변경 금지 — wire 상수 재사용)
public const string TEST_RESULT_PASS = "P";
public const string TEST_RESULT_FAIL = "F";
public const string TEST_RESULT_NOTEXIST = "N";

// L552-558 (변경 금지 — SetResultFromString)
public void SetResultFromString(string result) {
    if (result == TEST_RESULT_PASS) Result = EVisionResultType.OK;
    else if (result == TEST_RESULT_FAIL) Result = EVisionResultType.NG;
    else if (result == TEST_RESULT_NOTEXIST) Result = EVisionResultType.NotExist;
    ...
}

// L560-574 (변경 금지 — GetResultString 이미 NotExist→'N' 매핑 있음)
public string GetResultString() {
    switch (Result) {
        case EVisionResultType.NG: return TEST_RESULT_FAIL;
        case EVisionResultType.NotExist: return TEST_RESULT_NOTEXIST;
        case EVisionResultType.OK: return TEST_RESULT_PASS;
        ...
    }
    return TEST_RESULT_NOTEXIST;
}
```

VisionResponsePacket.cs 변경 대상:
```csharp
// L510-526 — FAIResultData 신규 ctor 추가 (기존 ctor 보존)
public class FAIResultData {
    public string FAIName { get; set; }
    public EVisionResultType Result { get; set; }
    public double DistanceMm { get; set; }

    public FAIResultData() { ... }                                  // 기존 보존
    public FAIResultData(string name, bool isPass, double distMm) { ... }  // 기존 보존 (회귀 0)
    // 신규: public FAIResultData(string name, EVisionResultType result, double distMm)
}

// L353-365 동적 FAI 직렬화 분기 — 3-state switch 로 교체
if (testPacket.IsDynamicFAI) {
    msg += testPacket.GetResultString();  // 변경 0 — cycle result 송신 (이미 NotExist 대응)
    msg += VisionServer.MSG_CONTENTS_SEPERATOR;
    msg += testPacket.FAICount.ToString();
    for (int i = 0; i < testPacket.FAICount; i++) {
        msg += VisionServer.MSG_CONTENTS_SEPERATOR;
        var faiData = testPacket.FAIResults[i];
        msg += (faiData.Result == EVisionResultType.OK) ? TEST_RESULT_PASS : TEST_RESULT_FAIL;  // ← 2-state, 교체 대상
        ...
    }
}
```

InspectionSequence.cs 변경 대상 (L67-97 AddResponse 전체):
```csharp
protected override void AddResponse() {
    if (RequestPacket == null) return;
    var recipeManager = SystemHandler.Handle.Sequences.RecipeManager;
    bool allPass = true;
    var responsePacket = new TestResultPacket { ... IsDynamicFAI = true };
    foreach (var shot in recipeManager.Shots) {
        foreach (var fai in shot.FAIList) {
            if (!fai.IsPass) allPass = false;
            responsePacket.FAIResults.Add(new FAIResultData(
                fai.FAIName ?? "FAI", fai.IsPass, fai.MeasuredValue));
        }
    }
    responsePacket.Result = allPass ? EVisionResultType.OK : EVisionResultType.NG;  // ← 2-state, 교체 대상
    pMyContext.ResultInfo = responsePacket.Result;
    ResponseQueue.Enqueue(responsePacket);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: FAIResultData 3-state ctor + 동적 FAI 직렬화 3-state 분기 (D-06)</name>
  <files>WPF_Example/TcpServer/VisionResponsePacket.cs</files>
  <read_first>
    - WPF_Example/TcpServer/VisionResponsePacket.cs (특히 L22-28 EVisionResultType enum, L48-50 TEST_RESULT_* 상수, L353-365 동적 FAI 직렬화, L510-526 FAIResultData, L560-574 GetResultString)
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-CONTEXT.md (특히 D-05 NotExist 재사용, D-06 P/F/N 3-state, D-08 footer 추가 안 함, D-10 v2.6 순수 유지)
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-PATTERNS.md (특히 VisionResponsePacket §D-05/D-06 L305-365 시그니처 추가 + 직렬화 수정 패턴 + Cycle Result 'N' 회귀 가드 L367-369)
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-01-PLAN.md (Plan 01 산출 — fai.WasDatumSkipped)
  </read_first>
  <behavior>
    - **신규 ctor 추가:** `FAIResultData(string name, EVisionResultType result, double distMm)` — Result 를 그대로 받아 저장. 변환 로직 없음.
    - **기존 ctor (string, bool, double) 보존:** AddResponse 호출 부에서 신규 ctor 로 교체하지만, FAIResultData 클래스 내부에서 기존 ctor 본문은 변경 안 함 (외부 호출자 안전망 — 회귀 0).
    - **동적 FAI 직렬화 (L353-365) 분기 3-state 교체:**
      - faiData.Result == OK → 'P'
      - faiData.Result == NotExist → 'N'
      - 그 외 (NG, ANG, TECHING 등) → 'F' (fallback)
    - **cycle result 송신 (L355 GetResultString 호출):** 변경 0 — 이미 NotExist → 'N' 자동 매핑.
    - **L367-396 기타 분기 (Bottom InspectionType, 비-DynamicFAI):** 변경 0 (Phase 39 영향 범위 외).
  </behavior>
  <action>
1. **FAIResultData 신규 ctor 추가 — L510-526 클래스 본문 안.**

   현재 L510-526:
   ```csharp
   //260409 hbk Phase 5: FAI별 개별 측정 결과 (D-04, D-05)
   public class FAIResultData {
       public string FAIName { get; set; }
       public EVisionResultType Result { get; set; }
       public double DistanceMm { get; set; }

       public FAIResultData() {
           Result = EVisionResultType.NG;
           DistanceMm = 0;
       }

       public FAIResultData(string name, bool isPass, double distMm) {
           FAIName = name;
           Result = isPass ? EVisionResultType.OK : EVisionResultType.NG;
           DistanceMm = distMm;
       }
   }
   ```

   변경 후 — `(string, bool, double)` ctor **직후, 클래스 닫는 `}` 이전**에 신규 ctor 삽입:
   ```csharp
   //260409 hbk Phase 5: FAI별 개별 측정 결과 (D-04, D-05)
   public class FAIResultData {
       public string FAIName { get; set; }
       public EVisionResultType Result { get; set; }
       public double DistanceMm { get; set; }

       public FAIResultData() {
           Result = EVisionResultType.NG;
           DistanceMm = 0;
       }

       public FAIResultData(string name, bool isPass, double distMm) {
           FAIName = name;
           Result = isPass ? EVisionResultType.OK : EVisionResultType.NG;
           DistanceMm = distMm;
       }

       //260529 hbk Phase 39 WF-02 D-06 — 3-state (P/F/N) 직접 전달용 ctor. 기존 bool ctor 는 호환 유지.
       //  InspectionSequence.AddResponse 가 fai.WasDatumSkipped → NotExist, !fai.IsPass → NG, 그 외 → OK 로 분기 후 호출.
       public FAIResultData(string name, EVisionResultType result, double distMm) //260529 hbk Phase 39 WF-02 D-06
       {
           FAIName = name; //260529 hbk Phase 39 WF-02 D-06
           Result = result; //260529 hbk Phase 39 WF-02 D-06
           DistanceMm = distMm; //260529 hbk Phase 39 WF-02 D-06
       }
   }
   ```

2. **동적 FAI 직렬화 분기 3-state 교체 — L353-365 본문.**

   현재 L353-365:
   ```csharp
   //260409 hbk Phase 5: 동적 FAI 모드 직렬화 (D-05)
   if (testPacket.IsDynamicFAI) {
       msg += testPacket.GetResultString();
       msg += VisionServer.MSG_CONTENTS_SEPERATOR;
       msg += testPacket.FAICount.ToString();
       for (int i = 0; i < testPacket.FAICount; i++) {
           msg += VisionServer.MSG_CONTENTS_SEPERATOR;
           var faiData = testPacket.FAIResults[i];
           msg += (faiData.Result == EVisionResultType.OK) ? TEST_RESULT_PASS : TEST_RESULT_FAIL;
           msg += VisionServer.MSG_CONTENTS_SEPERATOR;
           msg += faiData.DistanceMm.ToString("0.000");
       }
   }
   ```

   변경 후 — `msg += (faiData.Result == EVisionResultType.OK) ? TEST_RESULT_PASS : TEST_RESULT_FAIL;` **단일 라인 교체**:
   ```csharp
   //260409 hbk Phase 5: 동적 FAI 모드 직렬화 (D-05)
   if (testPacket.IsDynamicFAI) {
       msg += testPacket.GetResultString(); //260409 hbk Phase 5 — cycle Result, NotExist→'N' 매핑은 GetResultString 이미 보유 (L564-565)
       msg += VisionServer.MSG_CONTENTS_SEPERATOR;
       msg += testPacket.FAICount.ToString();
       for (int i = 0; i < testPacket.FAICount; i++) {
           msg += VisionServer.MSG_CONTENTS_SEPERATOR;
           var faiData = testPacket.FAIResults[i];
           //260529 hbk Phase 39 WF-02 D-06 — 2-state ternary → 3-state if/else. P (OK) / N (NotExist, datum-skip) / F (NG, fallback).
           //  D-10 v2.6 순수 유지 — TEST_RESULT_* 신규 상수 추가 안 함, EVisionResultType enum 신규 값 추가 안 함.
           //  CLAUDE.md C# 7.2 — switch expression 미사용, if/else (Phase 20 D-04 컨벤션).
           string faiCode; //260529 hbk Phase 39 WF-02 D-06
           if (faiData.Result == EVisionResultType.OK) faiCode = TEST_RESULT_PASS; //260529 hbk Phase 39 WF-02 D-06 — 'P'
           else if (faiData.Result == EVisionResultType.NotExist) faiCode = TEST_RESULT_NOTEXIST; //260529 hbk Phase 39 WF-02 D-06 — 'N' datum-skip
           else faiCode = TEST_RESULT_FAIL; //260529 hbk Phase 39 WF-02 D-06 — 'F' NG 및 fallback (ANG/TECHING 도 'F' 로 매핑 — wire 호환)
           msg += faiCode; //260529 hbk Phase 39 WF-02 D-06
           msg += VisionServer.MSG_CONTENTS_SEPERATOR;
           msg += faiData.DistanceMm.ToString("0.000");
       }
   }
   ```

3. **변경 금지:**
   - L22-28 EVisionResultType enum (신규 값 추가 금지 — D-10)
   - L48-50 TEST_RESULT_* 상수 (신규 추가 금지 — D-08)
   - L353 위의 case CMD_SEND_TEST: 진입부 (L338-352)
   - L367-396 비-DynamicFAI 분기 (Bottom InspectionType + else)
   - L552-558 SetResultFromString (이미 NotExist 매핑 보유)
   - L560-574 TestResultPacket.GetResultString (이미 NotExist 매핑 보유)
   - L528-595 TestResultPacket 다른 멤버 (visionResults, MaxListCount 등)

4. 마커 컨벤션: 모든 신규 라인 끝에 `//260529 hbk Phase 39 WF-02 D-06 — <intent>` 부착. 기존 `//260409 hbk Phase 5` 마커 100% 보존.
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "public FAIResultData(string name, EVisionResultType result, double distMm)" WPF_Example/TcpServer/VisionResponsePacket.cs` 매치
    - `grep -q "public FAIResultData(string name, bool isPass, double distMm)" WPF_Example/TcpServer/VisionResponsePacket.cs` 매치 (기존 ctor 보존 — 회귀 가드)
    - `grep -q "if (faiData.Result == EVisionResultType.OK) faiCode = TEST_RESULT_PASS" WPF_Example/TcpServer/VisionResponsePacket.cs` 매치
    - `grep -q "else if (faiData.Result == EVisionResultType.NotExist) faiCode = TEST_RESULT_NOTEXIST" WPF_Example/TcpServer/VisionResponsePacket.cs` 매치
    - `grep -q "else faiCode = TEST_RESULT_FAIL" WPF_Example/TcpServer/VisionResponsePacket.cs` 매치
    - `grep -c '"P"\|"F"\|"N"' WPF_Example/TcpServer/VisionResponsePacket.cs` >= 3 (TEST_RESULT_* 상수 보존)
    - `grep -q "public enum EVisionResultType : int" WPF_Example/TcpServer/VisionResponsePacket.cs` 매치 (enum 보존)
    - `grep -c "DetectFail\|CycleState\|ECycleResult" WPF_Example/TcpServer/VisionResponsePacket.cs` == 0 (D-10 가드 — v2.7 용어 도입 0)
    - `grep -q "ngCount\|detectFailCount" WPF_Example/TcpServer/VisionResponsePacket.cs` 미매치 (D-08 footer 가드)
    - msbuild Debug/x64 exit code 0, 신규 warning 0
  </acceptance_criteria>
  <done>
    - FAIResultData: 신규 ctor 1 추가, 기존 ctor 2 보존
    - 동적 FAI 직렬화 분기 3-state 교체 (1 ternary → 4 if/else 줄 + 1 변수 선언)
    - GetResultString / SetResultFromString 변경 0
    - EVisionResultType enum 변경 0
    - TEST_RESULT_* 상수 변경 0
    - 마커 Phase 5 보존, Phase 39 신규 마커 부착
    - msbuild Debug/x64 PASS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: InspectionSequence.AddResponse 3-state hierarchy (D-03, D-05)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (특히 L67-97 AddResponse 본문)
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-CONTEXT.md (특히 D-03 hierarchy 검출실패>NG>OK, D-05 NotExist 재사용)
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-PATTERNS.md (특히 InspectionSequence §D-03/D-05/D-06 변경 패턴 L169-200)
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-01-PLAN.md (Plan 01 산출 — fai.WasDatumSkipped, Task 1 신규 ctor)
    - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs (Plan 01 Task 2 가 추가한 WasDatumSkipped 필드 확인)
  </read_first>
  <behavior>
    - **3-state 계층 적용 순서:**
      1. anyDatumSkip = true (1건이라도 fai.WasDatumSkipped) → Result = NotExist ('N')
      2. anyDatumSkip = false && !allPass → Result = NG ('X')
      3. allPass = true → Result = OK ('O')
    - **FAIResults[i] 매핑 (신규 ctor 호출):**
      - fai.WasDatumSkipped → EVisionResultType.NotExist
      - !fai.IsPass (skip 아님) → EVisionResultType.NG
      - 그 외 → EVisionResultType.OK
    - **InspectionSequenceContext.CopyFrom (L27-36) 변경 0** — FAIMeasurementContext.AllPass false 시 NG 마크는 기존대로 유지. AddResponse 가 최종 ResultInfo 덮어쓰기 함 (L94 `pMyContext.ResultInfo = responsePacket.Result`) → InspectionSequenceContext 의 NotExist 분기 도달 가능.
    - **fai.IsPass 가 true 인데 WasDatumSkipped 도 true 인 케이스 처리:** Plan 01 Task 3 의 Action_FAIMeasurement 가 skip 시 `faiAllPass = false` 명시 누적하므로 fai.IsPass = false 강제. 즉 (IsPass=true && WasDatumSkipped=true) 조합은 코드 contract 상 발생 불가. 그래도 안전 가드로 WasDatumSkipped 우선순위 (D-03 hierarchy 검출실패 > NG > OK).
  </behavior>
  <action>
1. **AddResponse 본문 전체 교체 — L67-97.**

   현재 L67-97:
   ```csharp
   //260409 hbk Phase 5: 종합 판정 + FAI별 결과 TCP 전송 (D-07)
   protected override void AddResponse() {
       if (RequestPacket == null) return;

       var recipeManager = SystemHandler.Handle.Sequences.RecipeManager;

       // 종합 판정: 모든 FAI가 Pass여야 OK
       bool allPass = true;
       var responsePacket = new TestResultPacket {
           Target = RequestPacket.Sender,
           Site = RequestPacket.Site,
           InspectionType = RequestPacket.TestType,
           IsDynamicFAI = true,
       };

       foreach (var shot in recipeManager.Shots) {
           foreach (var fai in shot.FAIList) {
               if (!fai.IsPass) allPass = false;
               responsePacket.FAIResults.Add(new FAIResultData(
                   fai.FAIName ?? "FAI",
                   fai.IsPass,
                   fai.MeasuredValue
               ));
           }
       }

       responsePacket.Result = allPass ? EVisionResultType.OK : EVisionResultType.NG;
       pMyContext.ResultInfo = responsePacket.Result;

       ResponseQueue.Enqueue(responsePacket);
   }
   ```

   변경 후:
   ```csharp
   //260409 hbk Phase 5: 종합 판정 + FAI별 결과 TCP 전송 (D-07)
   //260529 hbk Phase 39 WF-02 D-03/D-05/D-06 — 3-state cycle hierarchy + FAIResults P/F/N 분기.
   //  계층: 검출실패(NotExist 'N') > NG ('X') > OK ('O'). datum-skip 1건이라도 있으면 cycle = NotExist.
   //  D-08: TestResultPacket 신규 필드 추가 안 함. FAIResults[i] 표현 레벨에서 끝남. wire 호환 100%.
   //  D-10: EVisionResultType.NotExist (기존 v2.6 enum) 재사용. v2.7 ECycleResult 도입 안 함.
   protected override void AddResponse() {
       if (RequestPacket == null) return;

       var recipeManager = SystemHandler.Handle.Sequences.RecipeManager;

       // 종합 판정: 3-state hierarchy (D-03)
       bool anyDatumSkip = false; //260529 hbk Phase 39 WF-02 D-03 — fai.WasDatumSkipped 1건이라도 있으면 true → cycle=NotExist
       bool allPass = true;
       var responsePacket = new TestResultPacket {
           Target = RequestPacket.Sender,
           Site = RequestPacket.Site,
           InspectionType = RequestPacket.TestType,
           IsDynamicFAI = true,
       };

       foreach (var shot in recipeManager.Shots) {
           foreach (var fai in shot.FAIList) {
               //260529 hbk Phase 39 WF-02 D-06 — FAIResults[i] 3-state (P/F/N) 분기.
               //  Plan 01: Action_FAIMeasurement 가 datum-skip 시 fai.WasDatumSkipped=true + IsPass=false 동시 설정.
               //  계층 우선순위: WasDatumSkipped → NotExist, !IsPass → NG, 그 외 → OK.
               EVisionResultType faiResultCode; //260529 hbk Phase 39 WF-02 D-06
               if (fai.WasDatumSkipped) //260529 hbk Phase 39 WF-02 D-06
               {
                   faiResultCode = EVisionResultType.NotExist; //260529 hbk Phase 39 WF-02 D-06 — 'N'
                   anyDatumSkip = true; //260529 hbk Phase 39 WF-02 D-03 — cycle aggregation
               }
               else if (!fai.IsPass) //260529 hbk Phase 39 WF-02 D-06
               {
                   faiResultCode = EVisionResultType.NG; //260529 hbk Phase 39 WF-02 D-06 — 'F'
                   allPass = false; //260529 hbk Phase 39 WF-02 D-06 — 기존 동작 보존
               }
               else //260529 hbk Phase 39 WF-02 D-06
               {
                   faiResultCode = EVisionResultType.OK; //260529 hbk Phase 39 WF-02 D-06 — 'P'
               }
               responsePacket.FAIResults.Add(new FAIResultData( //260529 hbk Phase 39 WF-02 D-06 — Plan 02 Task 1 신규 ctor 호출
                   fai.FAIName ?? "FAI", //260529 hbk Phase 39 WF-02 D-06
                   faiResultCode, //260529 hbk Phase 39 WF-02 D-06
                   fai.MeasuredValue //260529 hbk Phase 39 WF-02 D-06
               )); //260529 hbk Phase 39 WF-02 D-06
           }
       }

       //260529 hbk Phase 39 WF-02 D-03/D-05 — cycle 계층 판정: 검출실패 > NG > OK.
       if (anyDatumSkip) responsePacket.Result = EVisionResultType.NotExist; //260529 hbk Phase 39 WF-02 D-03/D-05 — 'N' (TestResultPacket.GetResultString 가 자동 매핑)
       else if (!allPass) responsePacket.Result = EVisionResultType.NG; //260529 hbk Phase 39 WF-02 D-03 — 'X'
       else responsePacket.Result = EVisionResultType.OK; //260529 hbk Phase 39 WF-02 D-03 — 'O'
       pMyContext.ResultInfo = responsePacket.Result;

       ResponseQueue.Enqueue(responsePacket);
   }
   ```

2. **변경 금지:**
   - L17-37 InspectionSequenceContext (CopyFrom 의 AllPass 분기는 EStep.End → AddResponse 진입 이전 단계라서 AddResponse 가 최종 덮어씀)
   - L40-65 필드 + 생성자
   - L99-143 OnCreate/OnLoad/OnRelease/GetDisplayName/AddDatum/RemoveDatum
   - L145-189 TryRunDatumPhase 두 오버로드
   - L191-216 ClearDatumTransforms / TryRunSingleDatum (Plan 01 에서 Clear 라인 + 메서드만 추가)
   - L219-225 TryGetDatumTransform

3. C# 7.2 호환: 모두 if/else, 명시 변수 선언. C# 8.0 switch expression 미사용. CLAUDE.md "K&R brace style" (이 파일은 K&R) 컨벤션 따름.

4. 마커 컨벤션: 모든 신규 라인 끝에 `//260529 hbk Phase 39 WF-02 D-XX — <intent>` 부착. 기존 `//260409 hbk Phase 5` 마커 보존 (L67 첫 줄).
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "bool anyDatumSkip = false" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 매치
    - `grep -q "if (fai.WasDatumSkipped)" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 매치
    - `grep -q "faiResultCode = EVisionResultType.NotExist" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 매치
    - `grep -q "faiResultCode = EVisionResultType.NG" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 매치
    - `grep -q "faiResultCode = EVisionResultType.OK" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 매치
    - `grep -q "if (anyDatumSkip) responsePacket.Result = EVisionResultType.NotExist" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 매치
    - `grep -q "new FAIResultData(" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 매치 (신규 ctor 호출)
    - `grep -q "fai.IsPass, fai.MeasuredValue" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` **미매치** (기존 bool ctor 호출 라인 제거 가드 — 신규 ctor 로 교체됨)
    - `grep -q "//260409 hbk Phase 5: 종합 판정 + FAI별 결과 TCP 전송" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 매치 (Phase 5 마커 보존)
    - `grep -c "//260529 hbk Phase 39 WF-02" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` >= 10 (선언 + if-else 3분기 + ctor 호출 + cycle 판정)
    - msbuild Debug/x64 exit code 0, 신규 warning 0
  </acceptance_criteria>
  <done>
    - AddResponse 본문 3-state hierarchy 적용
    - FAIResults.Add 가 신규 EVisionResultType ctor 호출
    - cycle Result = anyDatumSkip → NotExist → !allPass → NG → OK 순서
    - Plan 01 Task 1 의 _failedDatums / MarkDatumFailed / IsDatumFailed 직접 참조 없음 (Plan 01 Task 3 가 fai.WasDatumSkipped 로 변환 완료한 상태에서 본 task 가 읽기만 함)
    - msbuild Debug/x64 PASS
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TCP wire ↔ 제어 측 핸들러 | wire 문자열 'N' 송신 — 제어 측은 이미 NotExist 파싱 가능 (`TestResultPacket.SetResultFromString` L555 자체 검증) |
| InspectionSequence ↔ TestResultPacket | AddResponse 가 단일 시퀀스 스레드에서 Enqueue, MainRun 폴링이 dequeue 후 송신 — race condition 0 |
| FAIResultData ctor 다중 오버로드 | 기존 (bool ctor) 호출자 보존, 신규 (EVisionResultType ctor) 만 AddResponse 사용 — 외부 호출자 회귀 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-06 | Tampering | wire format 'N' 미파싱 | accept | `SetResultFromString` (L552-558) 이미 NotExist→Result 매핑 보유. 제어 측 코드가 'N' 파싱 못하면 v2.6 호환성 실패인데, 이는 본 phase 도입 전부터 존재하던 enum value. 신규 위험 0. |
| T-39-07 | Information Disclosure | datum-skip 사유 wire 노출 | accept | 'N' 코드는 "검출 실패" 의미만 전달, 구체 datum 이름 미노출. 메타데이터 leak 0. |
| T-39-08 | Denial of Service | FAIResults list 무한 증가 | accept | recipeManager.Shots 크기는 recipe 정의 고정. Phase 21 BUF-02 cleanup 패턴으로 lifecycle 관리. |
| T-39-09 | Elevation | v2.7 enum 우발 도입 | mitigate | acceptance_criteria 가 `grep -c "DetectFail\|CycleState\|ECycleResult"` == 0 강제. |
| T-39-10 | Repudiation | cycle 판정 사유 추적 부재 | mitigate | Plan 01 Task 3 가 이미 5 곳 Logging.PrintLog 보장. AddResponse 는 wire 만 책임. |

</threat_model>

<verification>
**Build verification:**
- msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 exit code 0

**Wire 매핑 검증:**
- fai.WasDatumSkipped=true → FAIResults[i].Result = NotExist → 직렬화 'N'
- !fai.IsPass → FAIResults[i].Result = NG → 직렬화 'F'
- 그 외 → FAIResults[i].Result = OK → 직렬화 'P'
- responsePacket.Result = NotExist → cycle 직렬화 'N' (GetResultString L564-565 자동)

**회귀 가드:**
- FAIResultData (string, bool, double) ctor 본문 변경 0 (외부 호출자 보호)
- EVisionResultType enum 변경 0
- TEST_RESULT_* 상수 변경 0
- TestResultPacket.GetResultString / SetResultFromString 변경 0
</verification>

<success_criteria>
- 2 파일 수정, msbuild Debug/x64 PASS
- 모든 acceptance_criteria grep 매치 통과
- Plan 04 (SIMUL UAT) 가 'N' 와이어 송신 검증 가능한 상태
- D-08 wire 포맷 신규 필드 추가 0 — grep 강제
- D-10 v2.6 순수 유지 — grep 강제
</success_criteria>

<output>
After completion, create `.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-02-SUMMARY.md` with:
- 신규 인터페이스: `FAIResultData(string, EVisionResultType, double)` ctor 1
- wire 매핑 표: fai.WasDatumSkipped → 'N', !fai.IsPass → 'F', 그 외 → 'P' / cycle anyDatumSkip → 'N', !allPass → 'X', 그 외 → 'O'
- D-08 footer 가드 + D-10 v2.6 enum 가드 (grep == 0)
- Plan 04 UAT 검증 포인트: Cal_Image/DualImageTest/SIDE1_3-1_Datum_A1_A2 (성공) vs SIDE1_3-1_Datum_B1 (실패 가능) 페어로 검출실패 시나리오 구성
</output>
