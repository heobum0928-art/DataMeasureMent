---
phase: 39-inspection-workflow-e2e-2026-05-29
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/UI/ControlItem/NodeViewModel.cs
autonomous: true
requirements: [WF-01, WF-02]
requirements_addressed: [WF-01, WF-02]
tags: [ui, overlay, halcon, datum, detect-fail]

must_haves:
  truths:
    - "Datum 검출 실패 시 HALCON overlay 에 'DETECT FAIL' 적색 라벨이 RefOrigin 근처에 표시된다"
    - "DatumConfig.LastFindSucceeded 가 INPC (INotifyPropertyChanged) 발화하여 Tree 헤더 갱신 가능"
    - "InspectionListView 의 Datum 노드 헤더에 검출 실패 표시 (HasDetectFail 노출 프로퍼티 — UI 측 바인딩 진입점)"
    - "HALCON SetColor 표준 색상명 'red' 만 사용 (memory feedback_halcon_setcolor_invalid_names 준수)"
    - "기존 RenderDatumOverlay / RenderDatumFindResult 동작 회귀 0 (성공 케이스 + LastTeachSucceeded 분기 보존)"
    - "Phase 37 hotfix B sender-기준 게이트 패턴 따름 (e.Source 비대칭 회피)"
  artifacts:
    - path: "WPF_Example/Halcon/Display/HalconDisplayService.cs"
      provides: "RenderDatumOverlay 내 'DETECT FAIL' 라벨 분기 (IsConfigured && !LastFindSucceeded)"
      contains: "DETECT FAIL"
      contains_alt: "LastFindSucceeded"
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "LastFindSucceeded INPC backing field + HasDetectFail 노출 프로퍼티"
      contains: "HasDetectFail"
      contains_alt: "_lastFindSucceeded"
    - path: "WPF_Example/UI/ControlItem/NodeViewModel.cs"
      provides: "OnParamPropertyChanged 가 LastFindSucceeded 변경 시 HasDetectFail RaisePropertyChanged"
      contains: "case nameof(DatumConfig.LastFindSucceeded)"
  key_links:
    - from: "Action_FAIMeasurement.EStep.DatumPhase (Plan 01 변경)"
      to: "InspectionSequence.TryRunSingleDatum (실패 시 datum.LastFindSucceeded=false)"
      via: "기존 Phase 37 경로 + DatumConfig INPC 발화"
      pattern: "LastFindSucceeded"
    - from: "DatumConfig.LastFindSucceeded setter"
      to: "NodeViewModel.OnParamPropertyChanged"
      via: "INotifyPropertyChanged.PropertyChanged 이벤트"
      pattern: "RaisePropertyChanged(nameof(LastFindSucceeded))"
    - from: "HalconDisplayService.RenderDatumOverlay"
      to: "HALCON HWindow"
      via: "WriteString('DETECT FAIL') with SetColor('red')"
      pattern: "HOperatorSet.WriteString"
---

<objective>
Phase 39 의 UI overlay 변경. Datum 검출 실패 시 사용자 시각 피드백:
- **HALCON overlay 'DETECT FAIL' 적색 라벨** (D-04) — `HalconDisplayService.RenderDatumOverlay` 내 IsConfigured && !LastFindSucceeded 분기 추가
- **InspectionListView Datum 노드 적색 배지 진입점** (D-04) — DatumConfig.LastFindSucceeded INPC 발화 + NodeViewModel 핸들러 분기

Purpose: 사용자가 검사 사이클 직후 어느 datum 이 실패했는지 시각으로 즉시 식별. Phase 22 IMG-02 의 TeachingImagePath vs SimulImagePath 역할 분리 패턴을 활용한 검출 실패 케이스 (UAT 시) 의 가시화.

본 Plan 은 Plan 01 의 데이터 모델(LastFindSucceeded 기존 + 신규 INPC) 만 사용. Plan 02 와 `files_modified` 겹침 0 → Wave 2 병렬 실행 가능.

Output:
- `HalconDisplayService.RenderDatumOverlay`: 'DETECT FAIL' 라벨 1 분기 추가 (8-10 줄)
- `DatumConfig.LastFindSucceeded`: auto-property → backing field + INPC 발화 (3-4 줄)
- `DatumConfig.HasDetectFail`: 노출 프로퍼티 (IsConfigured && !LastFindSucceeded) 1 추가
- `NodeViewModel.OnParamPropertyChanged`: `case nameof(DatumConfig.LastFindSucceeded)` 분기 추가 (3-4 줄)

Anti-goals:
- **WPF XAML DataTemplate 수정 없음** — 본 plan 은 INPC + 노출 프로퍼티까지만 책임 (실제 XAML 적색 dot 추가는 Phase 39 sign-off 후 또는 별도 차회 plan).
  근거: CONTEXT.md "Claude's Discretion — UI 적색 배지/DETECT FAIL 라벨의 시각 디테일(색상값, 폰트, 위치)" — 본 plan 은 데이터 바인딩 진입점만 확보, XAML 디테일은 UAT 시 검증 가능한 최소 surface 로 한정.
- `RenderDatumOverlay` 기존 200+ 줄 본문 (L683-895) 변경 0 — 라벨 분기는 마지막 `RenderDatumFindResult` 호출 (L889) 직후 추가만.
- `RenderDatumFindResult` (L307+) 변경 0.
- HALCON SetColor 비표준 색상명 금지 — "red" 표준명만 사용 (memory feedback_halcon_setcolor_invalid_names).
- DatumConfig 의 다른 100+ 필드 변경 0 — LastFindSucceeded (L413) 단일 필드만 INPC 화.
- NodeViewModel OnParamPropertyChanged 의 기존 4 case (DatumName/ShotName/FAIName/MeasurementName) 변경 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-CONTEXT.md
@.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-PATTERNS.md
@.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Plan 01 산출 + 본 Plan 이 추가/수정하는 인터페이스 -->

DatumConfig.cs 기존 (수정 대상 — L412-413):
```csharp
[PropertyTools.DataAnnotations.Browsable(false)]
public bool LastFindSucceeded { get; set; }  // L412-413 — 현재 auto-property, INPC 미발화
```

DatumConfig.cs 기존 (변경 금지 — L385-388):
```csharp
[Category("Datum|Reference")]
public double RefOriginRow { get; set; } = 0;  // L386
public double RefOriginCol { get; set; } = 0;  // L387
public double RefAngleRad { get; set; } = 0;   // L388
```

DatumConfig.cs 기존 (변경 금지 — L404-406):
```csharp
[Category("Datum|Status")]
public bool IsConfigured { get; set; } = false;  // L406 — D-04 라벨 조건
```

ParamBase.cs (변경 금지 — 모든 ParamBase 자식이 INotifyPropertyChanged 구현 시 사용하는 RaisePropertyChanged):
```csharp
// FAIConfig.cs L106-115 (analog 패턴):
private string _faiName;
public string FAIName {
    get { return _faiName; }
    set {
        if (_faiName == value) return;
        _faiName = value;
        RaisePropertyChanged(nameof(FAIName));
    }
}
```

HalconDisplayService.cs 기존 (변경 금지 — L683-895 RenderDatumOverlay 본문 + L307 RenderDatumFindResult):
- L885-890: 본 plan 의 라벨 분기 삽입 지점 (RenderDatumFindResult 호출 직후)
- L912-938: `DrawRoiLabelAt` analog 패턴 (라벨 렌더 컨벤션)

NodeViewModel.cs 기존 (수정 대상 — L210-233):
```csharp
private void OnParamPropertyChanged(object sender, PropertyChangedEventArgs e) {
    string newName = null;
    switch (e.PropertyName) {
        case nameof(DatumConfig.DatumName): newName = (sender as DatumConfig)?.DatumName; break;
        case nameof(ShotConfig.ShotName):   newName = (sender as ShotConfig)?.ShotName; break;
        case nameof(FAIConfig.FAIName):     newName = (sender as FAIConfig)?.FAIName; break;
        case nameof(MeasurementBase.MeasurementName):
            var m = sender as MeasurementBase;
            if (m != null) newName = string.IsNullOrEmpty(m.MeasurementName) ? m.TypeName : m.MeasurementName;
            break;
    }
    if (newName != null && this.Node != null) {
        this.Node.Name = newName;
        RaisePropertyChanged("Name");
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: HalconDisplayService RenderDatumOverlay 'DETECT FAIL' 라벨 (D-04)</name>
  <files>WPF_Example/Halcon/Display/HalconDisplayService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Display/HalconDisplayService.cs (특히 L307-330 RenderDatumFindResult, L683-895 RenderDatumOverlay 본문, L885-890 RenderDatumFindResult 호출 지점, L912-938 DrawRoiLabelAt analog)
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-CONTEXT.md (특히 D-04 로깅 + UI 표시, Claude's Discretion 시각 디테일)
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-PATTERNS.md (특히 HalconDisplayService §D-04 L420-468 라벨 추가 패턴 + analog 근거 + SetColor 가드 L570-577)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (특히 L386-388 RefOriginRow/Col, L406 IsConfigured, L413 LastFindSucceeded)
  </read_first>
  <behavior>
    - **조건:** `datum.IsConfigured && !datum.LastFindSucceeded` (티칭은 했지만 런타임 Find 실패).
    - **위치:** RefOriginRow - 40, RefOriginCol + 5 (PATTERNS.md L456 가이드 — RefOrigin 십자 위쪽 오프셋).
    - **색상:** "red" 표준명 (memory feedback_halcon_setcolor_invalid_names — "light red" 같은 비표준명 절대 금지).
    - **폰트:** EnsureFontInitialized + 기본 폰트 사용 (DrawRoiLabelAt L426-434 analog 패턴).
    - **호출 시점:** L885-890 `RenderDatumFindResult(window, datum)` 호출 **직후** (Phase 36 hotfix CO-36-03 패턴 — LastTeachSucceeded 블록 밖, z-stack 마지막).
    - **try/catch swallow:** RenderDatumOverlay 기존 catch 컨벤션 (L891 "// Suppress display errors") 그대로 따름.
    - **회귀 0:**
      - L683-885 RenderDatumOverlay 기존 200+ 줄 본문 변경 0 (TwoLineIntersect / VerticalTwoHorizontal / CircleTwoHorizontal 모든 분기 보존)
      - L307+ RenderDatumFindResult 변경 0 (Phase 17 D-13)
      - L912-938 DrawRoiLabelAt 변경 0
  </behavior>
  <action>
1. **위치 파악:** `RenderDatumOverlay` 메서드 본문의 마지막 부분을 찾는다. PATTERNS.md L885-890 에 따르면:
   ```csharp
   //260528 hbk Phase 36 UAT fix (CO-36-03) — RenderDatumFindResult 를 LastTeachSucceeded 블록 밖으로 이동.
   //  ...
   RenderDatumFindResult(window, datum); //260528 hbk Phase 36 UAT fix
   ```

   이 라인 직후, `catch { ... }` 진입 전이 라벨 분기 삽입 지점.

2. **'DETECT FAIL' 라벨 분기 추가.** RenderDatumFindResult 호출 직후:
   ```csharp
   //260528 hbk Phase 36 UAT fix (CO-36-03) — RenderDatumFindResult 를 LastTeachSucceeded 블록 밖으로 이동.
   //  기존엔 검출 십자가 LastTeachSucceeded==true 일 때만 그려져, 레시피 로드/swap 후(teach 미수행) Test Find 결과가 표시 안 되는 버그.
   //  검출 십자는 자체 LastFindSucceeded 게이트(메서드 내부)만 따르면 충분. z-stack last 유지.
   RenderDatumFindResult(window, datum); //260528 hbk Phase 36 UAT fix

   //260529 hbk Phase 39 WF-02 D-04 — Datum 검출 실패 시 'DETECT FAIL' 적색 라벨 렌더.
   //  분기: IsConfigured && !LastFindSucceeded (티칭은 했지만 런타임 Find 실패 = 검출 실패 상태).
   //  위치: RefOrigin 십자 위쪽(L817 "Datum Origin" 라벨과 충돌 없게 -40 row 오프셋).
   //  색상: "red" 표준명 (memory feedback_halcon_setcolor_invalid_names — "light red" 같은 비표준명 catch swallow 로 silent 미표시 1순위 의심).
   //  z-stack: RenderDatumFindResult 직후 — 검출 십자 위에 라벨 표시.
   //  try/catch swallow: 기존 RenderDatumOverlay catch 컨벤션 그대로 (L891 "Suppress display errors").
   if (datum.IsConfigured && !datum.LastFindSucceeded) //260529 hbk Phase 39 WF-02 D-04
   {
       try //260529 hbk Phase 39 WF-02 D-04
       {
           EnsureFontInitialized(window); //260529 hbk Phase 39 WF-02 D-04 — DrawRoiLabelAt analog 패턴 (L427)
           HOperatorSet.SetColor(window, "red"); //260529 hbk Phase 39 WF-02 D-04 — 표준 색상명 (memory feedback_halcon_setcolor_invalid_names)
           HOperatorSet.SetTposition(window, datum.RefOriginRow - 40, datum.RefOriginCol + 5); //260529 hbk Phase 39 WF-02 D-04
           HOperatorSet.WriteString(window, "DETECT FAIL"); //260529 hbk Phase 39 WF-02 D-04 — 영문 라벨 (HALCON 폰트 한글 미지원 회피)
       }
       catch //260529 hbk Phase 39 WF-02 D-04
       {
           // Suppress display errors (기존 RenderDatumOverlay catch 컨벤션) //260529 hbk Phase 39 WF-02 D-04
       }
   }
   ```

3. **변경 금지:**
   - L307 RenderDatumFindResult 메서드 본문 (Phase 17 D-13)
   - L683-885 RenderDatumOverlay 모든 algorithm 분기 (TwoLineIntersect L713-724, VerticalTwoHorizontal/DualImage L726-738, Line2 L742-752, CircleROI L755-771, Horizontal A/B L773-)
   - L789-820 LastTeachSucceeded 분기 (검출 라인/원/중심)
   - L891-893 catch 블록 (기존 컨벤션 보존)
   - L912-938 DrawRoiLabelAt 메서드
   - 다른 모든 메서드 (RenderCircleStripOverlay 등)

4. C# 7.2 호환: try/catch 표준 문법. switch expression 미사용.

5. 마커 컨벤션: 모든 신규 라인 끝에 `//260529 hbk Phase 39 WF-02 D-04 — <intent>` 부착. 기존 `//260528 hbk Phase 36`, `//260503 hbk Phase 17 D-13` 마커 100% 보존.
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "if (datum.IsConfigured && !datum.LastFindSucceeded)" WPF_Example/Halcon/Display/HalconDisplayService.cs` 매치
    - `grep -q 'HOperatorSet.WriteString(window, "DETECT FAIL")' WPF_Example/Halcon/Display/HalconDisplayService.cs` 매치
    - `grep -q 'HOperatorSet.SetColor(window, "red")' WPF_Example/Halcon/Display/HalconDisplayService.cs` 매치 (표준 색상명 "red")
    - `grep -qE 'HOperatorSet.SetColor\(window, "light (red|green|blue)"\)' WPF_Example/Halcon/Display/HalconDisplayService.cs` 미매치 (비표준 색상명 가드)
    - `grep -q "RenderDatumFindResult(window, datum)" WPF_Example/Halcon/Display/HalconDisplayService.cs` 매치 (Phase 36 hotfix 보존)
    - `grep -q "//260528 hbk Phase 36 UAT fix" WPF_Example/Halcon/Display/HalconDisplayService.cs` 매치 (Phase 36 마커 보존)
    - `grep -q "//260503 hbk Phase 17 D-13" WPF_Example/Halcon/Display/HalconDisplayService.cs` 매치 (Phase 17 마커 보존)
    - `grep -c "//260529 hbk Phase 39 WF-02 D-04" WPF_Example/Halcon/Display/HalconDisplayService.cs` >= 5 (분기 + 4 HALCON 호출)
    - msbuild Debug/x64 exit code 0, 신규 warning 0
  </acceptance_criteria>
  <done>
    - 'DETECT FAIL' 라벨 분기 1개 (try/catch + 4 HALCON 호출) 추가
    - RenderDatumOverlay 본문 200+ 줄 회귀 0
    - "red" 표준 색상명만 사용 (memory feedback 준수)
    - Phase 36/17 마커 모두 보존
    - msbuild Debug/x64 PASS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DatumConfig LastFindSucceeded INPC + HasDetectFail 노출 프로퍼티 (D-04)</name>
  <files>
    WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs,
    WPF_Example/UI/ControlItem/NodeViewModel.cs
  </files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (특히 L385-413 RefOrigin/IsConfigured/LastFindSucceeded 필드)
    - WPF_Example/UI/ControlItem/NodeViewModel.cs (특히 L196-233 OnParamPropertyChanged 등록/switch)
    - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs (특히 L103-115 FAIName INPC analog 패턴)
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-CONTEXT.md (특히 D-04 UI 표시, Claude's Discretion)
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-PATTERNS.md (특히 InspectionListView §D-04 권장 구현 경로 L518-523 — backing field + RaisePropertyChanged + nameof case 추가)
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-01-PLAN.md (Plan 01 Task 1 — InspectionSequence.MarkDatumFailed 가 datum.LastFindSucceeded 세팅과 분리되어 있음에 주의)
  </read_first>
  <behavior>
    - **DatumConfig.LastFindSucceeded:** auto-property → backing field `_lastFindSucceeded` + RaisePropertyChanged. 기존 호출자 (Phase 37 D-37-03, Phase 17 D-13) 가 setter 동일 시그니처로 호출 — 호환성 100%.
    - **DatumConfig.HasDetectFail:** computed property `IsConfigured && !LastFindSucceeded` 1줄. setter 없음, get-only.
    - **NodeViewModel.OnParamPropertyChanged:** `case nameof(DatumConfig.LastFindSucceeded)` 추가. 분기 동작: `RaisePropertyChanged("HasDetectFail")` (Node.Name 갱신 아님 — 별도 UI 노출 채널).
    - **INPC 자기 발화:** DatumConfig.LastFindSucceeded setter 가 RaisePropertyChanged(nameof(LastFindSucceeded)) 와 동시에 RaisePropertyChanged(nameof(HasDetectFail)) 도 호출 (computed prop 변경 사실 알림).
    - **회귀 0:**
      - DatumConfig 의 100+ 다른 필드 변경 0
      - NodeViewModel 의 기존 4 switch case 변경 0
      - ParamBase 변경 0
  </behavior>
  <action>
**Part A — DatumConfig.cs**

1. **L412-413 LastFindSucceeded 교체.**

   현재 L412-413:
   ```csharp
   [PropertyTools.DataAnnotations.Browsable(false)]
   public bool LastFindSucceeded { get; set; }
   ```

   변경 후 — backing field + INPC:
   ```csharp
   //260529 hbk Phase 39 WF-02 D-04 — INPC 발화로 NodeViewModel 가 Datum 노드 'DETECT FAIL' 배지 갱신.
   //  기존 auto-property (Phase 4) → backing field 패턴 (FAIConfig.FAIName analog L106-115).
   //  setter 동일 시그니처 유지 → Phase 37 D-37-03 (InspectionSequence.TryRunDatumPhase L164, TryRunSingleDatum L211/L213) 호출 회귀 0.
   //  setter 가 LastFindSucceeded + 파생 HasDetectFail 둘 다 RaisePropertyChanged 발화 → UI computed property 자동 갱신.
   [PropertyTools.DataAnnotations.Browsable(false)]
   private bool _lastFindSucceeded; //260529 hbk Phase 39 WF-02 D-04
   [PropertyTools.DataAnnotations.Browsable(false)]
   public bool LastFindSucceeded //260529 hbk Phase 39 WF-02 D-04
   {
       get { return _lastFindSucceeded; } //260529 hbk Phase 39 WF-02 D-04
       set //260529 hbk Phase 39 WF-02 D-04
       {
           if (_lastFindSucceeded == value) return; //260529 hbk Phase 39 WF-02 D-04 — idempotent 가드
           _lastFindSucceeded = value; //260529 hbk Phase 39 WF-02 D-04
           RaisePropertyChanged(nameof(LastFindSucceeded)); //260529 hbk Phase 39 WF-02 D-04
           RaisePropertyChanged(nameof(HasDetectFail)); //260529 hbk Phase 39 WF-02 D-04 — computed prop 변경 통지
       }
   }

   //260529 hbk Phase 39 WF-02 D-04 — Datum 노드 UI 'DETECT FAIL' 배지 노출용 computed 프로퍼티.
   //  XAML DataTemplate 가 본 프로퍼티 binding 으로 적색 dot 표시 (XAML 구현은 별도 plan/UAT 결정).
   //  IsConfigured = false 이면 (티칭 미수행) 배지 안 보임. IsConfigured && !LastFindSucceeded 이면 배지 표시.
   [PropertyTools.DataAnnotations.Browsable(false)] //260529 hbk Phase 39 WF-02 D-04
   public bool HasDetectFail //260529 hbk Phase 39 WF-02 D-04
   {
       get { return IsConfigured && !_lastFindSucceeded; } //260529 hbk Phase 39 WF-02 D-04
   }
   ```

2. **변경 금지:**
   - L385-388 RefOriginRow/Col/RefAngleRad (D-04 라벨이 RefOrigin 좌표 읽기만 함)
   - L404-406 IsConfigured (D-04 라벨 조건 + HasDetectFail computed 입력)
   - L390-402 Legacy 글로벌 에지 파라미터
   - L408-410 CurrentTransform
   - L415-460+ Line1Detected_* / Line2Detected_* / Circle_* 등 100+ 다른 필드
   - DatumConfig 모든 다른 메서드 (생성자, EnsurePerRoiDefaults, ICustomTypeDescriptor 등)

**Part B — NodeViewModel.cs**

3. **L210-233 OnParamPropertyChanged 의 switch 에 새 case 추가.**

   현재 L210-233:
   ```csharp
   private void OnParamPropertyChanged(object sender, PropertyChangedEventArgs e) {
       string newName = null;
       switch (e.PropertyName) {
           case nameof(DatumConfig.DatumName):
               newName = (sender as DatumConfig)?.DatumName;
               break;
           case nameof(ShotConfig.ShotName):
               newName = (sender as ShotConfig)?.ShotName;
               break;
           case nameof(FAIConfig.FAIName):
               newName = (sender as FAIConfig)?.FAIName;
               break;
           case nameof(MeasurementBase.MeasurementName):
               var m = sender as MeasurementBase;
               if (m != null) {
                   newName = string.IsNullOrEmpty(m.MeasurementName) ? m.TypeName : m.MeasurementName;
               }
               break;
       }
       if (newName != null && this.Node != null) {
           this.Node.Name = newName;
           RaisePropertyChanged("Name");
       }
   }
   ```

   변경 후 — switch 안에 `case nameof(DatumConfig.LastFindSucceeded)` 추가 + 메서드 끝에 별도 통지 분기:
   ```csharp
   private void OnParamPropertyChanged(object sender, PropertyChangedEventArgs e) {
       string newName = null;
       switch (e.PropertyName) {
           case nameof(DatumConfig.DatumName):
               newName = (sender as DatumConfig)?.DatumName;
               break;
           case nameof(ShotConfig.ShotName):
               newName = (sender as ShotConfig)?.ShotName;
               break;
           case nameof(FAIConfig.FAIName):
               newName = (sender as FAIConfig)?.FAIName;
               break;
           case nameof(MeasurementBase.MeasurementName):
               var m = sender as MeasurementBase;
               if (m != null) {
                   newName = string.IsNullOrEmpty(m.MeasurementName) ? m.TypeName : m.MeasurementName;
               }
               break;
           //260529 hbk Phase 39 WF-02 D-04 — Datum 검출 실패 배지 갱신 (Tree 노드 헤더 + XAML DataTemplate 바인딩 진입점).
           //  Node.Name 갱신 아님 — 별도 RaisePropertyChanged("HasDetectFail") 만 발화.
           //  XAML 측은 Node 의 DataContext 가 NodeViewModel 인 상태에서 HasDetectFail 바인딩 가능 — 단, 본 plan 은
           //  데이터 진입점까지만 책임 (XAML DataTemplate 적색 dot 디테일은 UAT 결정).
           case nameof(DatumConfig.LastFindSucceeded): //260529 hbk Phase 39 WF-02 D-04
               RaisePropertyChanged("HasDetectFail"); //260529 hbk Phase 39 WF-02 D-04 — UI 측 NodeViewModel binding 갱신 신호
               return; //260529 hbk Phase 39 WF-02 D-04 — Node.Name 갱신 분기 미진입
       }
       if (newName != null && this.Node != null) {
           this.Node.Name = newName;
           RaisePropertyChanged("Name");
       }
   }
   ```

4. **변경 금지:**
   - L196-206 NodeViewModel 생성자 + INPC 구독 (CO-31-01 패턴)
   - L208-209 OnParamPropertyChanged 메서드 시그니처
   - 기존 4 case 분기 본문
   - L235-264 다른 메서드 (ToString / AddChild / ExpandParents / ExpandAll)

5. C# 7.2 호환: `nameof(DatumConfig.LastFindSucceeded)` C# 6 부터 지원. switch 표준 문법.

6. 마커 컨벤션:
   - DatumConfig.cs: 모든 신규 라인 끝에 `//260529 hbk Phase 39 WF-02 D-04 — <intent>` 부착. 기존 `//260409 hbk Phase 4` 마커 100% 보존.
   - NodeViewModel.cs: 신규 case + return 4 줄에 마커 부착. 기존 `//260526 hbk CO-31-01` 마커 100% 보존.
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /nologo 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "private bool _lastFindSucceeded" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 매치 (backing field)
    - `grep -q "RaisePropertyChanged(nameof(LastFindSucceeded))" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 매치
    - `grep -q "RaisePropertyChanged(nameof(HasDetectFail))" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 매치
    - `grep -q "public bool HasDetectFail" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 매치
    - `grep -q "return IsConfigured && !_lastFindSucceeded" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 매치
    - `grep -q "case nameof(DatumConfig.LastFindSucceeded)" WPF_Example/UI/ControlItem/NodeViewModel.cs` 매치
    - `grep -q 'RaisePropertyChanged("HasDetectFail")' WPF_Example/UI/ControlItem/NodeViewModel.cs` 매치
    - `grep -q "case nameof(DatumConfig.DatumName)" WPF_Example/UI/ControlItem/NodeViewModel.cs` 매치 (기존 case 보존)
    - `grep -q "case nameof(ShotConfig.ShotName)" WPF_Example/UI/ControlItem/NodeViewModel.cs` 매치 (기존 case 보존)
    - `grep -q "case nameof(FAIConfig.FAIName)" WPF_Example/UI/ControlItem/NodeViewModel.cs` 매치 (기존 case 보존)
    - `grep -q "case nameof(MeasurementBase.MeasurementName)" WPF_Example/UI/ControlItem/NodeViewModel.cs` 매치 (기존 case 보존)
    - `grep -q "//260526 hbk CO-31-01" WPF_Example/UI/ControlItem/NodeViewModel.cs` 매치 (CO-31-01 마커 보존)
    - `grep -q "//260409 hbk Phase 4" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 매치 (Phase 4 마커 보존)
    - `grep -c "//260529 hbk Phase 39 WF-02 D-04" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` >= 8 (backing field + 4-5 setter 라인 + HasDetectFail 4-5 라인)
    - msbuild Debug/x64 exit code 0, 신규 warning 0
  </acceptance_criteria>
  <done>
    - DatumConfig.LastFindSucceeded: auto-property → backing field INPC 패턴
    - DatumConfig.HasDetectFail computed 프로퍼티 1 추가
    - NodeViewModel.OnParamPropertyChanged: 신규 case 1 + RaisePropertyChanged("HasDetectFail") + return
    - 기존 4 switch case (CO-31-01) 100% 보존
    - Phase 4 / CO-31-01 마커 보존
    - msbuild Debug/x64 PASS
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HALCON HWindow 렌더 ↔ UI 스레드 | RenderDatumOverlay 는 UI 스레드 (MainView_HalconViewer_Tick) 에서 호출 — try/catch swallow 가 SetColor/WriteString 예외 흡수, UI 응답성 가드. |
| DatumConfig INPC ↔ NodeViewModel | LastFindSucceeded setter 가 UI 스레드 OR 시퀀스 스레드 양쪽에서 호출 가능 (Phase 37 D-37-03). INPC 이벤트는 구독자 스레드에 dispatch 안 함 — 단순 이벤트 발화. NodeViewModel 측이 Dispatcher 가드 필요 시 별도 처리. |
| ParamBase RaisePropertyChanged | ParamBase 가 INotifyPropertyChanged 구현하므로 RaisePropertyChanged 메서드 보유 (FAIConfig.FAIName setter L113 가 동일 패턴 호출). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-11 | Tampering | HALCON SetColor 비표준 색상명 | mitigate | "red" 표준명 강제 + acceptance_criteria 가 비표준명 grep 미매치 강제. memory feedback_halcon_setcolor_invalid_names 의 "light green" 실제 사례 회피. |
| T-39-12 | Denial of Service | LastFindSucceeded INPC 무한 발화 | accept | setter 가 `if (_lastFindSucceeded == value) return;` idempotent 가드 보유. 동일 값 반복 set 무동작. |
| T-39-13 | Information Disclosure | 'DETECT FAIL' 라벨 정보 노출 | accept | 라벨 텍스트는 UI 측에 의도 노출 (사용자가 봐야 할 정보). datum 이름 미포함 → 시스템 내부 식별자 leak 0. |
| T-39-14 | Elevation | NodeViewModel switch fallthrough | mitigate | 신규 case 가 `return;` 명시 → Node.Name 갱신 분기 미진입. C# switch 는 break/return 강제이지만 명시적 return 으로 의도 명확화. |
| T-39-15 | Repudiation | 검출 실패 사용자 인지 부재 | mitigate | 'DETECT FAIL' 적색 라벨 + Tree 노드 배지 진입점 둘 다 보장. Plan 01 Task 3 Logging.PrintLog 와 함께 시각/로그 양 채널. |

</threat_model>

<verification>
**Build verification:**
- msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 exit code 0

**구조 검증 (Task 1):**
- RenderDatumOverlay 내 'DETECT FAIL' 라벨 분기 1개 추가
- HALCON SetColor 표준 색상명 "red" 만 사용
- RenderDatumFindResult 호출 보존 (Phase 36 hotfix)

**구조 검증 (Task 2):**
- DatumConfig.LastFindSucceeded INPC 패턴
- DatumConfig.HasDetectFail computed 프로퍼티
- NodeViewModel switch 5번째 case 추가, 기존 4 case 보존

**회귀 가드:**
- RenderDatumOverlay 본문 200+ 줄 회귀 0
- DatumConfig 100+ 다른 필드 회귀 0
- NodeViewModel 기존 4 case 본문 회귀 0
</verification>

<success_criteria>
- 3 파일 수정, msbuild Debug/x64 PASS
- 모든 acceptance_criteria grep 매치 통과
- Plan 04 (SIMUL UAT) 가 'DETECT FAIL' 라벨 시각 확인 가능한 상태
- D-04 UI overlay + 노드 배지 진입점 둘 다 확보
- XAML DataTemplate 적색 dot 디테일은 본 plan 범위 외 (Claude's Discretion)
</success_criteria>

<output>
After completion, create `.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-03-SUMMARY.md` with:
- 신규 인터페이스: DatumConfig.HasDetectFail (computed) + DatumConfig.LastFindSucceeded (INPC) + NodeViewModel "HasDetectFail" RaisePropertyChanged
- 'DETECT FAIL' 라벨 위치 (RefOriginRow - 40, RefOriginCol + 5) + 색상 ("red")
- Plan 04 UAT 검증 포인트: SIMUL 검출실패 케이스 시각 확인 (라벨 표시 여부 + 적색 색상 + 위치 적정성)
- XAML DataTemplate 적색 dot 추가 여부 (Plan 04 UAT 결과 따라 후속 plan / hotfix 결정)
</output>
