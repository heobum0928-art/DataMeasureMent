---
phase: 39-inspection-workflow-e2e-2026-05-29
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-UAT.md
autonomous: false
requirements: [WF-01, WF-02]
requirements_addressed: [WF-01, WF-02]
tags: [uat, simul, datum, wire-protocol, ui]

must_haves:
  truths:
    - "SIMUL Cal_Image/ 자산 + DualImageTest/SIDE1_3-1_Datum_A1_A2 / B1 페어로 3 시나리오 (OK / NG / 검출실패) 구성 가능"
    - "OK 시나리오: 모든 Datum 검출 성공 + 모든 FAI 측정 PASS → cycle='O' + FAIResults 모두 'P'"
    - "NG 시나리오: Datum 검출 성공 + 1 FAI 측정 FAIL → cycle='X' + 해당 FAI 'F' + 나머지 'P'"
    - "검출실패 시나리오: 1 Datum 검출 FAIL + 영향 받는 FAI skip + 다른 datum 의 FAI 정상 측정 → cycle='N' + skip FAI 'N' + 살아남은 FAI 'P'"
    - "검출실패 시 HALCON overlay 에 'DETECT FAIL' 적색 라벨 표시"
    - "Phase 37 lenient 회귀 0: 다른 datum 의 FAI 가 정상 측정됨 (전면 abort 안 함)"
    - "Phase 7-02 회귀 0: overlay suffix (-OK/-NG) 정상 부여"
    - "NG 2건 이상 누적 케이스: 모든 NG 가 FAIResults 에 반영, cycle='X'"
  artifacts:
    - path: ".planning/phases/39-inspection-workflow-e2e-2026-05-29/39-UAT.md"
      provides: "5 시나리오 UAT 결과 (3 핵심 + 2 회귀 가드)"
      contains: "Test 1: OK"
      contains_alt: "Test 2: NG"
      contains_alt2: "Test 3: 검출실패"
  key_links:
    - from: "UAT Test 3 (검출실패 시나리오)"
      to: "Plan 01 _failedDatums HashSet"
      via: "Action_FAIMeasurement.MarkDatumFailed → IsDatumFailed gate → meas.LastSkipReason"
      pattern: "DATUM_FAIL"
    - from: "UAT Test 3 (검출실패 시나리오)"
      to: "Plan 02 cycle='N' wire 송신"
      via: "AddResponse anyDatumSkip → EVisionResultType.NotExist → TEST_RESULT_NOTEXIST"
      pattern: "TEST_RESULT_NOTEXIST"
    - from: "UAT Test 3 (검출실패 시나리오)"
      to: "Plan 03 'DETECT FAIL' 라벨"
      via: "HalconDisplayService.RenderDatumOverlay IsConfigured && !LastFindSucceeded"
      pattern: "DETECT FAIL"
---

<objective>
Phase 39 의 최종 sign-off 게이트. **SIMUL_MODE 하에서 5 UAT 시나리오 통과** 가 완료 조건 (D-09).

핵심 3 시나리오 (D-03 hierarchy 검증):
- **Test 1: OK** — 정상 케이스, 모든 datum 검출 + 모든 FAI PASS
- **Test 2: NG** — 정상 datum + 1 FAI 측정 실패 (공차 초과 또는 에지 미검출)
- **Test 3: 검출실패** — 1 datum 검출 실패 + 영향 FAI skip + 다른 datum 의 FAI 정상

회귀 가드 2 시나리오:
- **Test 4: NG 2건 이상 누적** — D-07 try/catch lenient 유지 검증
- **Test 5: Phase 37 lenient 회귀 가드** — multi-datum 부분 실패 시 다른 datum 의 FAI 측정 진행 (per-FAI gate vs 전면 abort 구분)

Purpose: POC 시연 전에 1 사이클 정책이 통일됐는지 + 사용자 인지 가능한 시각/로그/wire 출력이 일관되는지 사용자 직접 확인. 실HW UAT 는 Phase 44 CO-38-04 로 분리 (D-09).

Output:
- `39-UAT.md` 파일 (5 Test 케이스, frontmatter status=partial 초기)
- 사용자가 각 Test 를 1회 이상 실제 SIMUL 실행 후 PASS/FAIL/NEEDS_INVESTIGATION 마크
- 모든 Test PASS 시 status=signed_off, summary passed:5/total:5
- 실패 시 root cause 분석 + 후속 hotfix plan 또는 carry-over (CO-39-XX) 등록

Anti-goals:
- 실카메라 HIK 검증 시도 금지 (Phase 44 분리 — D-09)
- v2.7 PROTO 검증 금지 (Phase 48/49 분리 — D-10)
- 신규 코드 작성 금지 — Plan 01-03 으로 충분해야 함. 부족하면 carry-over.
- 결과 분석/Export 검증 금지 (Phase 40/41 — OUT-01~04)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-CONTEXT.md
@.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-PATTERNS.md
@.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-01-PLAN.md
@.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-02-PLAN.md
@.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-03-PLAN.md
@CLAUDE.md

**SIMUL 자산:**
- Cal_Image/DualImageTest/SIDE1_3-1_Datum_A1_A2_backlight_LV30.bmp (Side datum 가로축)
- Cal_Image/DualImageTest/SIDE1_3-1_Datum_B1_backlight_LV30.bmp (Side datum 세로축 또는 다른 워크피스)
- Cal_Image/ 루트의 기타 Top/Side/Bottom 검사 이미지 (사용자가 기존 검사 워크피스 자산 활용)

**검출 실패 케이스 구성 방법 (PATTERNS.md L405-412):**
- 시나리오 A: `TeachingImagePath` + `SimulImagePath` 같은 파일 → 정상 검출 (OK 경로)
- 시나리오 B: `SimulImagePath` 만 다른 워크피스 이미지로 변경 → datum 검출 실패 (NotExist 경로)
- 시나리오 C: Multi-datum 케이스에서 일부 datum 의 TeachingImagePath 만 잘못된 경로 → per-FAI gate 검증

**Memory feedback 준수:**
- 응답 언어 한국어 (feedback_response_language)
- HALCON SetColor "red" 표준명 (feedback_halcon_setcolor_invalid_names) — Plan 03 Task 1 강제
- 마커 컨벤션 //260529 hbk Phase 39 (feedback_comment_convention) — Plan 01-03 강제
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 39-UAT.md 5 시나리오 템플릿 생성</name>
  <files>.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-UAT.md</files>
  <read_first>
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-CONTEXT.md (특히 D-09 SIMUL UAT, Claude's Discretion "UAT 케이스 정확한 입력 데이터")
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-PATTERNS.md (특히 MainView §Phase 22 IMG-02 회귀 가드 L394-412 검출 실패 시나리오 구성 방법)
    - .planning/phases/22-image-dual-source-2026-05-11/ 디렉터리에 UAT.md (가장 최근 sign-off 된 UAT 파일 — 형식 analog)
    - .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-01-PLAN.md / 39-02-PLAN.md / 39-03-PLAN.md (각 Plan 의 must_haves)
  </read_first>
  <behavior>
    - `39-UAT.md` 파일은 사용자가 SIMUL 실행 직후 직접 PASS/FAIL 기록할 템플릿.
    - frontmatter:
      - phase: 39-inspection-workflow-e2e-2026-05-29
      - type: uat
      - status: partial (초기 — 사용자 사인오프 후 signed_off)
      - signed_off_by: TBD
      - signed_off_date: TBD
      - test_environment: SIMUL_MODE (Debug/x64 build)
    - 5 Test 케이스 각각:
      - Test ID + 제목 (예: "Test 1: OK 시나리오 — 정상 datum + 정상 FAI")
      - 사전 조건 (recipe / SimulImagePath / TeachingImagePath 설정)
      - 실행 절차 (단계별 명확한 행동)
      - 기대 결과 (UI / TCP wire / 로그)
      - 결과 기록란: result: TBD (PASS / FAIL / NEEDS_INVESTIGATION)
      - 메모 란
    - Summary 섹션: passed/failed/pending/total + sign-off 결정 (signed_off / partial / failed)
  </behavior>
  <action>
1. **파일 위치 확인.** UAT 파일은 phase 디렉터리 안:
   ```
   .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-UAT.md
   ```

2. **파일 작성.** 아래 정확한 내용으로 작성:

```markdown
---
phase: 39-inspection-workflow-e2e-2026-05-29
type: uat
status: partial
signed_off_by: TBD
signed_off_date: TBD
test_environment: SIMUL_MODE Debug/x64
related_plans: [39-01-PLAN.md, 39-02-PLAN.md, 39-03-PLAN.md]
requirements: [WF-01, WF-02]
---

# Phase 39: 검사 워크플로우 E2E — UAT

**작성일:** 2026-05-29
**범위:** SIMUL 사이클 3분기 (OK/NG/검출실패) + 회귀 가드 2건

D-09 (CONTEXT.md): Phase 39 sign-off = SIMUL UAT 통과. 실카메라 검증은 Phase 44 (CO-38-04) 로 분리.

---

## Test 1: OK 시나리오 — 정상 datum + 정상 FAI

**사전 조건:**
- Recipe 로드: 기존 multi-datum recipe (Top 또는 Side 어느 쪽이든 가능)
- 모든 DatumConfig.TeachingImagePath = 검출 가능한 이미지 (Cal_Image/ 정상 워크피스)
- ShotConfig.SimulImagePath = TeachingImagePath 와 동일 워크피스 (Phase 22 IMG-02 패턴 — 같은 파일 가능)
- 모든 FAI 의 NominalValue + Tolerance 가 실제 측정값을 포함하는 범위로 설정 (PASS 보장)

**실행 절차:**
1. 앱 SIMUL_MODE Debug/x64 실행
2. recipe 로드 + Datum 트리에서 모든 Datum 노드 사전 티칭 확인 (LastTeachSucceeded=true)
3. TCP 클라이언트 또는 메뉴에서 검사 트리거 (`$TEST:site,type,0@` 형식)
4. 검사 완료 대기

**기대 결과:**
- [UI] MainView Halcon Viewer 에 모든 Datum 십자 (purple) + 검출 라인/원 표시
- [UI] 'DETECT FAIL' 라벨 미표시 (모든 datum.LastFindSucceeded=true)
- [UI] InspectionListView Datum 노드에 적색 배지 미표시
- [UI] FAI overlay 에 `-OK` suffix (Phase 7-02 회귀 가드)
- [TCP wire] cycle Result = 'O' (`$RESULT:site;type;O;…@`)
- [TCP wire] 모든 FAIResults[i] = 'P'
- [Log] datum skip 또는 FAI fail 로그 0건

**결과:** TBD
**메모:** (사용자 기록)

---

## Test 2: NG 시나리오 — 정상 datum + 1 FAI 측정 실패

**사전 조건:**
- Test 1 의 recipe 재사용
- 1 FAI 의 Tolerance 범위를 매우 좁게 변경 (예: TolerancePlus=0.001, ToleranceMinus=0.001) → 측정값이 NG 영역에 떨어지도록
- 그 외 datum / FAI 설정은 Test 1 동일

**실행 절차:**
1. Test 1 의 recipe 에서 1 FAI 만 Tolerance 변경
2. 앱 재로드 또는 PropertyGrid 에서 즉시 반영
3. 검사 트리거
4. 검사 완료 대기

**기대 결과:**
- [UI] 모든 Datum 정상 십자 표시 (Test 1 동일)
- [UI] 'DETECT FAIL' 라벨 미표시
- [UI] NG FAI 의 overlay 에 `-NG` suffix 부여 (Phase 7-02 회귀 가드)
- [UI] InspectionListView 결과 행에 NG 표시
- [TCP wire] cycle Result = 'X'
- [TCP wire] NG FAI 의 FAIResults[i] = 'F', 나머지 FAI = 'P'
- [Log] `[FAIMeasurement] Measurement '...' failed: ...` 로그 OR Tolerance 초과 (D-07 try/catch lenient — abort 0)

**결과:** TBD
**메모:** (사용자 기록)

---

## Test 3: 검출실패 시나리오 — 1 Datum 검출 실패 + 영향 FAI skip + 다른 datum FAI 정상 측정

**사전 조건:**
- Test 1 의 recipe 재사용. Multi-datum (예: Datum_1 + Datum_2 두 개) recipe 필수
- Datum_1.TeachingImagePath = 검출 가능 이미지 (Cal_Image/DualImageTest/SIDE1_3-1_Datum_A1_A2_backlight_LV30.bmp)
- Datum_2.TeachingImagePath = 잘못된 워크피스 (Cal_Image/DualImageTest/SIDE1_3-1_Datum_B1_backlight_LV30.bmp) — 알고리즘 (ROI 좌표 + AlgorithmType) 으로 검출 실패 보장
- 또는 더 간단히: Datum_2.TeachingImagePath 를 존재하지 않는 경로 (예: "C:/nonexistent.bmp") 로 설정 → 이미지 취득 실패 분기 트리거
- FAI 구성: 절반의 FAI 는 Measurement.DatumRef = "Datum_1" (정상), 나머지는 DatumRef = "Datum_2" (검출 실패)

**실행 절차:**
1. Datum_1 / Datum_2 둘 다 사전 티칭 시도 후 Datum_1 만 성공 가정 (또는 의도적으로 Datum_2 ROI 를 검출 불가능한 영역에 배치)
2. Recipe save / load 후 검사 트리거
3. 검사 완료 대기

**기대 결과:**
- [UI] Datum_1 십자 정상 표시
- [UI] Datum_2 위치에 'DETECT FAIL' 적색 라벨 표시 (Plan 03 Task 1)
- [UI] InspectionListView Datum_2 노드에 HasDetectFail 바인딩 신호 (XAML 적색 dot 구현 여부와 무관하게 HasDetectFail 프로퍼티 값 확인 가능 — debug breakpoint 또는 후속 plan 의 XAML)
- [UI] Datum_1 기반 FAI overlay 는 `-OK` suffix 정상 부여 (Phase 37 lenient 회귀 가드)
- [UI] Datum_2 기반 FAI 는 overlay 미생성 (TryExecute 호출 안 됨 — Plan 01 Task 3 gate)
- [TCP wire] cycle Result = 'N' (`$RESULT:site;type;N;…@`)
- [TCP wire] Datum_1 기반 FAIResults[i] = 'P'
- [TCP wire] Datum_2 기반 FAIResults[i] = 'N'
- [Log] `[FAIMeasurement] Datum 'Datum_2' ... 실패 (skip):` 로그 (DatumPhase 분기, Plan 01 Task 3)
- [Log] `[FAIMeasurement] Measurement '...' skipped — datum 'Datum_2' 검출 실패 (D-01)` 로그 (Measure 게이트, Plan 01 Task 3)

**결과:** TBD
**메모:** (사용자 기록)

**중요 검증:** Phase 37 D-37-03 lenient 회귀 가드 — Datum_2 실패에도 불구하고 Datum_1 기반 FAI 가 정상 측정되어야 함. 전면 abort 시 즉시 FAIL.

---

## Test 4: NG 2건 이상 누적 — try/catch lenient (D-07) 회귀 가드

**사전 조건:**
- Test 2 와 유사하나 2개 이상 FAI 의 Tolerance 를 좁게 변경
- 또는 2개 FAI 의 ROI 를 에지 검출 불가능한 영역에 배치 (try/catch 분기 트리거)

**실행 절차:**
1. 2개 이상 FAI 가 NG 가 되도록 recipe 변경
2. 검사 트리거
3. 검사 완료 대기

**기대 결과:**
- [UI] 모든 datum 정상 (Test 1 동일)
- [TCP wire] cycle Result = 'X'
- [TCP wire] 2개 이상 FAIResults[i] = 'F' (NG 누적)
- [TCP wire] 정상 FAI 는 'P'
- [Log] 각 NG FAI 마다 별도 fail 로그 (D-07 lenient — 첫 NG 에서 abort 안 함)
- [동작] EStep.Measure 루프가 첫 NG 후에도 계속 진행 (D-07 회귀 가드)

**결과:** TBD
**메모:** (사용자 기록)

---

## Test 5: Multi-datum 부분 실패 — Phase 37 D-37-03 lenient 회귀 가드

**사전 조건:**
- Test 3 의 recipe 와 동일하지만 더 엄격한 검증: 3개 이상 datum 보유 recipe
- 1 datum 만 검출 실패, 2 datum 검출 성공
- 각 datum 별로 최소 1 FAI 보유

**실행 절차:**
1. 3 datum recipe 로드
2. 1 datum (예: 가운데 datum) 의 TeachingImagePath 또는 ROI 를 검출 불가 상태로 변경
3. 검사 트리거
4. 검사 완료 대기

**기대 결과:**
- [UI] 2 datum 정상 십자, 1 datum 'DETECT FAIL' 라벨
- [UI] 정상 datum 기반 FAI overlay 정상 (-OK/-NG suffix)
- [TCP wire] cycle Result = 'N' (1 datum skip 가 hierarchy 최상위)
- [TCP wire] 정상 datum 기반 FAIResults = 'P' 또는 'F' (측정 결과 따라)
- [TCP wire] 실패 datum 기반 FAIResults = 'N'
- [동작] DatumPhase 가 첫 datum 실패에 abort 하지 않고 모든 datum loop 완료 (Phase 37 D-37-03 lenient)
- [동작] Measure 게이트가 datum-skip 만 정확히 차단, 정상 datum 의 FAI 는 영향 없음 (Plan 01 D-01)

**결과:** TBD
**메모:** (사용자 기록)

---

## Summary

| 항목 | 값 |
|------|-----|
| Total | 5 |
| Passed | 0 |
| Failed | 0 |
| Pending | 5 |
| Status | partial |

**Sign-off 결정:**
- 5/5 PASS → status: signed_off, 다음 phase 진행
- 1+ FAIL → root cause 분석 + hotfix plan 또는 carry-over (CO-39-XX) 등록 + status: partial 유지
- 1+ NEEDS_INVESTIGATION → 별도 quick task 또는 carry-over

**Carry-over 후보:**
- 발견 시 여기에 추가 (CO-39-01, CO-39-02, ...)

---

## Phase 39 회귀 가드 체크리스트

UAT 시 다음 회귀 0 확인 (모든 Test 통과 조건):

- [ ] Phase 7-02 overlay suffix (-OK/-NG) 정상 부여 (Test 1, 2, 4)
- [ ] Phase 17 D-13 RenderDatumFindResult (purple 검출 십자) 정상 표시 (Test 1, 2, 4)
- [ ] Phase 36 hotfix CO-36-03 (LastTeachSucceeded 분기 밖에서 RenderDatumFindResult 호출) 동작 (Test 1, 2, 4)
- [ ] Phase 37 D-37-03 lenient — datum 부분 실패 시 다른 datum FAI 정상 측정 (Test 3, 5)
- [ ] Phase 22 IMG-02 — TeachingImagePath ≠ SimulImagePath 역할 분리 유지 (Test 3 시나리오 구성)
- [ ] Phase 20 D-12 marker stacking — 기존 마커 100% 보존, 신규 //260529 hbk Phase 39 누적 (Plan 01-03 task acceptance_criteria)
- [ ] CO-22-01 — Datum↔FAI/SHOT PropertyGrid 전환 정상 (Test 3 시나리오 구성 중 노드 전환)

---

*Last updated: 2026-05-29 — Phase 39 UAT template 생성 (Plan 04). 사용자 SIMUL 실행 후 사인오프 필요.*
```

3. **검증:** 파일이 작성되면 Test 1-5 의 모든 섹션 + Summary + 회귀 가드 체크리스트가 포함되어야 함.

4. **사용자 액션 — 본 task 종료 후 사용자 직접:**
   - SIMUL_MODE Debug/x64 빌드 및 실행
   - 각 Test 시나리오 사전 조건 구성
   - Test 1 → Test 2 → Test 3 → Test 4 → Test 5 순서로 실행 (또는 사용자 선택)
   - 각 Test 의 `결과: TBD` 라인을 PASS/FAIL/NEEDS_INVESTIGATION 으로 교체
   - 메모 란에 관찰 사항 기록
   - 5/5 PASS 시 frontmatter `status: partial → signed_off`, `signed_off_by:` + `signed_off_date:` 채움
   - Summary 표 갱신
  </action>
  <verify>
    <automated>test -f .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-UAT.md && grep -c "## Test" .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 파일 `.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-UAT.md` 존재
    - `grep -c "^## Test" .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-UAT.md` == 5
    - `grep -q "Test 1: OK 시나리오" .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-UAT.md` 매치
    - `grep -q "Test 2: NG 시나리오" .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-UAT.md` 매치
    - `grep -q "Test 3: 검출실패 시나리오" .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-UAT.md` 매치
    - `grep -q "Test 4: NG 2건 이상 누적" .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-UAT.md` 매치
    - `grep -q "Test 5: Multi-datum 부분 실패" .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-UAT.md` 매치
    - `grep -q "status: partial" .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-UAT.md` 매치 (초기 status)
    - `grep -q "DETECT FAIL" .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-UAT.md` 매치 (Test 3 기대 결과)
    - `grep -q "Phase 37 D-37-03 lenient" .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-UAT.md` 매치 (회귀 가드 명시)
    - `grep -q "Phase 7-02 overlay suffix" .planning/phases/39-inspection-workflow-e2e-2026-05-29/39-UAT.md` 매치 (회귀 가드)
  </acceptance_criteria>
  <done>
    - 39-UAT.md 파일 생성 (5 Test 케이스 + Summary + 회귀 가드 체크리스트)
    - frontmatter status: partial (사인오프 전)
    - 사용자가 SIMUL 실행 후 PASS/FAIL 기록 가능한 상태
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (Checkpoint): SIMUL UAT 5 시나리오 사용자 직접 검증 (D-09 sign-off)</name>
  <what-built>
    Plan 01-03 의 모든 코드 변경이 적용된 SIMUL_MODE Debug/x64 빌드.
    Plan 04 Task 1 이 생성한 `.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-UAT.md` 템플릿.
  </what-built>
  <how-to-verify>
    **선행 확인:**
    1. msbuild 으로 SIMUL_MODE Debug/x64 빌드 PASS 확인:
       ```
       msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64
       ```
       exit code 0, 신규 warning 0 (Phase 38 baseline).

    2. 39-UAT.md 파일 5 Test 케이스 확인 (Plan 04 Task 1 acceptance_criteria 통과).

    **Test 1 실행 (OK 시나리오):**
    1. Debug/x64 SIMUL 실행 (앱 기동, recipe 로드)
    2. Top 또는 Side recipe 선택, 모든 Datum + FAI 정상 구성 확인
    3. SimulImagePath = TeachingImagePath 동일 워크피스 (Phase 22 IMG-02 패턴)
    4. 모든 FAI Tolerance 범위 넉넉히 (PASS 보장)
    5. 검사 트리거 (TCP 클라이언트 또는 UI 메뉴)
    6. 결과 확인:
       - UI: 모든 Datum 십자 표시, 'DETECT FAIL' 라벨 미표시
       - UI: FAI overlay 에 `-OK` suffix
       - TCP: cycle = 'O', 모든 FAIResults = 'P'
       - Log: 에러 0
    7. 39-UAT.md 의 Test 1 `결과: TBD` → `결과: PASS` (또는 FAIL)

    **Test 2 실행 (NG 시나리오):**
    1. Test 1 recipe 에서 1 FAI Tolerance 좁게 (예: ±0.001mm)
    2. 검사 트리거
    3. 결과 확인:
       - UI: NG FAI overlay 에 `-NG` suffix
       - TCP: cycle = 'X', NG FAI = 'F', 나머지 = 'P'
       - Log: `[FAIMeasurement] Measurement '...' failed:` 1건
    4. 39-UAT.md Test 2 결과 기록

    **Test 3 실행 (검출실패 시나리오, 핵심):**
    1. Multi-datum recipe (최소 2 datum)
    2. Datum_1 정상, Datum_2 의 TeachingImagePath 를 존재하지 않는 경로로 변경 (예: "C:/nonexistent.bmp")
       또는 Datum_2 의 ROI 를 검출 불가능 영역에 배치
    3. 절반의 FAI 는 DatumRef="Datum_1", 나머지는 DatumRef="Datum_2"
    4. 검사 트리거
    5. 결과 확인:
       - UI: Datum_1 십자 정상, Datum_2 위치에 적색 'DETECT FAIL' 라벨
       - UI: Datum_1 기반 FAI overlay 정상 (`-OK`/`-NG` suffix)
       - UI: Datum_2 기반 FAI overlay 미생성 (skip)
       - TCP: cycle = 'N'
       - TCP: Datum_1 기반 FAIResults = 'P', Datum_2 기반 FAIResults = 'N'
       - Log: `[FAIMeasurement] Datum 'Datum_2' ... 실패 (skip):` (DatumPhase)
       - Log: `[FAIMeasurement] Measurement '...' skipped — datum 'Datum_2' 검출 실패 (D-01)` (Measure gate)
    6. **핵심 회귀 가드:** Datum_2 실패에도 Datum_1 기반 FAI 는 측정 진행 확인 (Phase 37 lenient).
    7. 39-UAT.md Test 3 결과 기록

    **Test 4 실행 (NG 2건 이상 누적):**
    1. 2+ FAI Tolerance 좁게
    2. 검사 트리거
    3. 결과 확인:
       - TCP: cycle = 'X', 2+ FAIResults = 'F'
       - Log: 각 NG FAI 별 별도 로그 (try/catch lenient 회귀 가드)
    4. 39-UAT.md Test 4 결과 기록

    **Test 5 실행 (Phase 37 lenient 회귀 가드):**
    1. 3+ datum recipe, 1 datum 만 검출 실패
    2. 검사 트리거
    3. 결과 확인:
       - 2 datum 정상 십자, 1 datum 'DETECT FAIL' 라벨
       - 정상 datum 기반 FAI 정상 측정
       - TCP: cycle = 'N', 실패 datum FAI = 'N', 정상 datum FAI = 'P' 또는 'F'
    4. 39-UAT.md Test 5 결과 기록

    **Sign-off 절차:**
    1. 5/5 PASS 인 경우:
       - 39-UAT.md frontmatter `status: partial → signed_off`
       - `signed_off_by: {사용자 이름}`, `signed_off_date: 2026-XX-XX`
       - Summary 표 갱신 (passed: 5)
    2. 1+ FAIL 인 경우:
       - root cause 분석
       - hotfix 가능 시 작은 plan/quick task 추가 후 재실행
       - carry-over 필요 시 CO-39-XX 로 등록, status partial 유지

    **Phase 39 회귀 가드 체크리스트** (UAT 종료 시 일괄 확인):
    - Phase 7-02 overlay suffix
    - Phase 17 D-13 RenderDatumFindResult
    - Phase 36 hotfix CO-36-03
    - Phase 37 D-37-03 lenient (Test 3, 5 의 핵심)
    - Phase 22 IMG-02
    - Phase 20 D-12 marker stacking
    - CO-22-01 PropertyGrid 전환
  </how-to-verify>
  <resume-signal>
    PASS 시 입력: "signed_off — UAT 5/5 PASS, status=signed_off 갱신 완료"
    FAIL 시 입력: "partial — UAT N/5 PASS, 실패 분석 + carry-over 결정 필요"
    또는 자유 텍스트로 결과 보고 + 다음 단계 결정
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 UAT 실행 ↔ SIMUL_MODE 빌드 | 사용자가 Debug/x64 빌드 SIMUL 실행 — 실HW 통신 없음, 데이터 안전. |
| Test 3 시나리오 구성 ↔ TeachingImagePath 잘못된 경로 | 의도적 검출 실패 트리거 — File.Exists 가드 (Action_FAIMeasurement GrabOrLoadDatumImage L278 + TryGrabOrLoadDualDatumImages L306-313) 가 안전 분기. |
| 39-UAT.md 사용자 편집 ↔ 사인오프 | 사용자가 직접 PASS/FAIL 기록 — Claude 가 임의 사인오프 금지 (D-09 사용자 결정). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-16 | Tampering | UAT 결과 임의 사인오프 | mitigate | checkpoint:human-verify gate=blocking — 사용자가 명시적 resume-signal 보내기 전에 plan 종료 안 됨. |
| T-39-17 | Repudiation | UAT 시나리오 재현 불가능 | mitigate | 39-UAT.md 각 Test 의 사전 조건 + 실행 절차 + 기대 결과 구체화 (Test 3 의 경우 정확한 파일 경로 명시). |
| T-39-18 | DoS | Test 3 무한 루프 (datum loop) | accept | DatumConfigs.Count 유한, foreach 가 자연 종료 (Phase 37 패턴). |
| T-39-19 | Information Disclosure | SimulImagePath 경로 leak | accept | 로컬 파일 경로만 노출, 외부 송신 없음. |

</threat_model>

<verification>
**파일 생성 검증:**
- 39-UAT.md 존재 + 5 Test 케이스 포함

**사용자 사인오프 검증 (checkpoint 후):**
- 사용자가 5/5 PASS 또는 partial 결정
- 39-UAT.md 의 각 Test 결과 라인이 PASS/FAIL 로 갱신
- Summary 표 갱신
- 1+ FAIL 시 carry-over 등록

**회귀 가드 (UAT 시 확인):**
- Phase 37 D-37-03 lenient (Test 3, 5)
- Phase 7-02 overlay suffix (Test 1, 2, 4)
- Phase 17 D-13 검출 십자 (Test 1, 2, 4)
- Phase 36 hotfix CO-36-03 (Test 1, 2, 4)
- Phase 22 IMG-02 (Test 3 시나리오 구성)
- Phase 20 D-12 marker stacking
- CO-22-01 PropertyGrid 전환
</verification>

<success_criteria>
- 39-UAT.md 파일 생성 (Plan 04 Task 1)
- 사용자 SIMUL UAT 5/5 PASS 사인오프 (Plan 04 Task 2 — checkpoint)
- Phase 39 sign-off 완료 = D-09 충족

**부분 sign-off (partial) 허용 조건 (D-09 fall-back):**
- 핵심 3 시나리오 (Test 1, 2, 3) PASS + 회귀 가드 2건 (Test 4, 5) 중 1건 PASS
- 실패한 1 건은 CO-39-XX carry-over 로 등록 + Phase 40 또는 후속 phase 에 흡수
</success_criteria>

<output>
After completion, create `.planning/phases/39-inspection-workflow-e2e-2026-05-29/39-04-SUMMARY.md` with:
- 39-UAT.md 최종 상태 (status, passed/failed/total)
- Sign-off 결정 (signed_off / partial / failed)
- 각 Test 결과 요약 (PASS/FAIL 사유)
- 회귀 가드 체크리스트 결과
- Carry-over 목록 (CO-39-XX) 있으면 명시
- Phase 39 종료 후 다음 phase 결정 (Phase 40 — OUT-01/02 진행 또는 carry-over 우선 처리)
</output>
