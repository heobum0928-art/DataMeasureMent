---
phase: 39.1-inspection-urgent-fixes-2026-05-29
plan: 01
type: execute
# I1 (260529): D-G1-03 의 의도 "Wave 1 algorithm / Wave 2 UI" 가 file overlap
#   (MainView + InspectionListView 공유, Plan 03 ↔ Plan 04) 으로 4 plan / 3 wave 로
#   조정됨. Plan 01/02 는 algorithm wave 1 그대로. 데이터 의존 없음, 단지 file
#   충돌 회피 목적의 wave 분리. Plan 04 만 wave 3 으로 이동.
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs
autonomous: false
requirements: [WF-01]
tags: [propertygrid, halcon, polar-edge, fai-circle, dynamic-properties]
user_setup: []

must_haves:
  truths:
    - "polar 경로 (Circle_RadialDirection in {Inward, Outward}) 활성 시 사용자가 PolarStepDeg/RectL1Ratio/RectL2Ratio/PolarEdgeSelection 4 필드를 PropertyGrid 에서 즉시 조정"
    - "각 필드 값 변경 시 측정값이 결정적으로 변화 (예: PolarStepDeg 10→5 시 strip 개수 2배 증가, EdgeSelection First→Last 시 검출 위치 변화)"
    - "fit 경로 (Circle_RadialDirection == \"\") 활성 시 polar 4 필드가 PropertyGrid 에서 자동 hide"
    - "기존 INI 레시피 로드 시 4 필드 default 가 EdgeOptionLists.FaiCircle* 상수와 동일 — Datum CTH 동등성 (Phase 28 REQ-28-03) 회귀 0"
    - "Datum CTH polar 경로 (DatumFindingService.TryTeachCircleTwoHorizontal L200/L730) 변경 0 — EdgeOptionLists.FaiCircle* 정적 상수 그대로 참조"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs"
      provides: "4 인스턴스 필드 (Circle_PolarStepDeg/Circle_RectL1Ratio/Circle_RectL2Ratio/Circle_PolarEdgeSelection) + Circle_PolarEdgeSelectionList wrapper + ICustomTypeDescriptor 13 멤버 + idempotent ctor migration"
      contains: "public double Circle_PolarStepDeg, public double Circle_RectL1Ratio, public double Circle_RectL2Ratio, public string Circle_PolarEdgeSelection, public List<string> Circle_PolarEdgeSelectionList, ICustomTypeDescriptor"
  key_links:
    - from: "CircleDiameterMeasurement.TryExecute polar 분기 (L78-89)"
      to: "VisionAlgorithmService.TryFindCircleByPolarSampling 4 인자 위치"
      via: "4 인스턴스 필드를 4 위치 인자로 직접 전달 (기존 정적 상수 교체)"
      pattern: "Circle_PolarStepDeg.*Circle_RectL1Ratio.*Circle_RectL2Ratio.*Circle_PolarEdgeSelection"
    - from: "CircleDiameterMeasurement.GetProperties() 신규"
      to: "DynamicPropertyHelper.FilterProperties (sourceNames 화이트리스트)"
      via: "Circle_RadialDirection 빈 문자열 시 polar 4 필드 + List hide 콜백"
      pattern: "DynamicPropertyHelper\\.FilterProperties.*IsHiddenForRadialDirection"
    - from: "CircleDiameterMeasurement ctor"
      to: "신규 4 필드 sentinel migration"
      via: "0.0 / null / \"\" 감지 시 EdgeOptionLists.FaiCircle* default 채움"
      pattern: "Circle_PolarStepDeg == 0\\.0.*FaiCirclePolarStepDeg"
---

<objective>
CircleDiameterMeasurement polar 경로 (Phase 28 REQ-28-02) 의 4 polar 파라미터 (StepDeg/RectL1Ratio/RectL2Ratio/EdgeSelection) 를 정적 상수 (EdgeOptionLists.FaiCircle*) 참조에서 인스턴스 필드로 교체하여 사용자가 PropertyGrid 에서 직접 조정할 수 있도록 한다. Phase 28 D-04 default-equality 로 INI 회귀 0, ICustomTypeDescriptor 동적 hide/show 로 fit 경로 사용자가 polar 필드를 보지 않도록 한다.

Purpose: Phase 28 의 polar 경로 기본값 고정이 사용자 override 0 로 측정 정밀도 조정 불가. 사용자가 작은 원 / 큰 원 / 노이즈가 많은 이미지에 strip 개수 / 크기 / EdgeSelection 을 적응시켜야 함.
Output: CircleDiameterMeasurement.cs 4 신규 public 필드 + Circle_PolarEdgeSelectionList wrapper + ICustomTypeDescriptor 13 멤버 + idempotent ctor migration. 단일 파일 수정.

**Anti-goal (변경 0, 회귀 0):**
- `DatumFindingService.TryTeachCircleTwoHorizontal` (L200, L730) — Datum CTH polar 경로 `EdgeOptionLists.FaiCircle*` 정적 상수 참조 유지 (Phase 28 D-05 / Phase 39.1 D-G2-05)
- `EdgeOptionLists.FaiCircle*` 4 정적 상수 — single source of truth 보존 (Phase 28 D-04 / Phase 39.1 D-G2-03)
- CircleDiameterMeasurement fit 경로 (Circle_RadialDirection == "") L60-68 — `TryFindCircle` 인자 byte-identical (Phase 28 REQ-28-04)
- PropertyGrid 다른 필드 (Circle_Row/Col/Radius, EdgeThreshold, Sigma, EdgePolarity, Circle_RadialDirection) 노출 동작 변경 0
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-inspection-urgent-fixes-2026-05-29/39.1-CONTEXT.md
@.planning/phases/39.1-inspection-urgent-fixes-2026-05-29/39.1-PATTERNS.md
@WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs
@WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs
@WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs
@WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
@WPF_Example/Halcon/Algorithms/DatumFindingService.cs

<interfaces>
EdgeOptionLists.cs L30-34 (default 상수, 단일 소스 — 변경 0):
```csharp
public const double FaiCirclePolarStepDeg   = 10.0;
public const double FaiCircleRectL1Ratio    = 0.02;
public const double FaiCircleRectL2Ratio    = 0.02;
public const string FaiCircleEdgeSelection  = "First";
```

EdgeOptionLists.cs L22 (Selections 콤보 ItemsSource):
```csharp
public static readonly List<string> Selections = new List<string> { "First", "Last", "All" };
```

VisionAlgorithmService.TryFindCircleByPolarSampling 호출 시그니처 (CircleDiameterMeasurement.cs:78 의 기존 호출부에서 확인):
```csharp
bool TryFindCircleByPolarSampling(
    HImage image,
    double row, double col, double radius,
    double polarStepDeg,        // ← 인스턴스 필드로 교체
    double rectL1Ratio,         // ← 인스턴스 필드로 교체
    double rectL2Ratio,         // ← 인스턴스 필드로 교체
    double sigma, int edgeThreshold, string polarity,
    string edgeSelection,       // ← 인스턴스 필드로 교체
    HTuple datumTransform,
    out double foundRow, out double foundCol, out double foundRadius,
    out HTuple stripRows, out HTuple stripCols, out bool[] stripSuccess,
    out string error);
```

FAIConfig.cs L267-296 (ICustomTypeDescriptor 패턴 — exact analog):
```csharp
public System.ComponentModel.PropertyDescriptorCollection GetProperties(System.Attribute[] attributes) {
    return BuildFilteredProperties(attributes);
}
public System.ComponentModel.PropertyDescriptorCollection GetProperties() {
    return BuildFilteredProperties(null);
}
private System.ComponentModel.PropertyDescriptorCollection BuildFilteredProperties(System.Attribute[] attrs) {
    var sourceNames = new System.Collections.Generic.HashSet<string> {
        nameof(EdgeMeasureTypeList), nameof(EdgeDirectionList), nameof(EdgePolarityList),
    };
    bool hasDynamicMeasurements = Measurements != null && Measurements.Count > 0;
    return DynamicPropertyHelper.FilterProperties(this, attrs,
        name => IsHiddenForEdgeMeasureType(name, EdgeMeasureType)
                || (hasDynamicMeasurements && IsLegacyEdgeParam(name)),
        sourceNames);
}
// + 11 stub: GetAttributes / GetClassName / GetComponentName / GetConverter / GetDefaultEvent / GetDefaultProperty / GetEditor / GetEvents(Attribute[]) / GetEvents() / GetPropertyOwner
```

DynamicPropertyHelper.FilterProperties 시그니처:
```csharp
public static System.ComponentModel.PropertyDescriptorCollection FilterProperties(
    object obj,
    System.Attribute[] attrs,
    System.Func<string, bool> hideFunc,
    System.Collections.Generic.HashSet<string> sourceNames);
```

DatumFindingService.cs L196-205, L726-735 (Datum CTH polar 호출 — **변경 0**, 본 plan anti-goal):
```csharp
EdgeOptionLists.FaiCirclePolarStepDeg, EdgeOptionLists.FaiCircleRectL1Ratio,
EdgeOptionLists.FaiCircleRectL2Ratio, ..., EdgeOptionLists.FaiCircleEdgeSelection,
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CircleDiameterMeasurement 에 4 polar 필드 + Circle_PolarEdgeSelectionList wrapper 추가 + ctor idempotent migration</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs (전체 — 100 라인)
    - WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs L22, L29-33 (Selections + FaiCircle* 상수)
    - 39.1-PATTERNS.md §1.1 §1.2 (CircleCenterDistance 4 필드 노출 analog) §1.7 (sentinel migration 패턴)
  </read_first>
  <action>
    `WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs` 를 다음과 같이 수정한다 (per D-G2-01/02/03):

    **A) 신규 인스턴스 필드 4 추가 — `using` 직후의 클래스 본문 안, 기존 `Circle_RadialDirection` 선언(L29-30) 직후 위치:**
    ```csharp
    //260529 hbk Phase 39.1 G2-01 — polar 4 필드 인스턴스화 (PropertyGrid 노출, ICustomTypeDescriptor 가 Circle_RadialDirection 빈값 시 hide). default = EdgeOptionLists.FaiCircle* 상수 byte-identical → Datum CTH 동등성 회귀 0 (Phase 28 D-04).
    [Category("Edge")] //260529 hbk Phase 39.1 G2-01 — Edge 카테고리
    public double Circle_PolarStepDeg { get; set; } = 10.0; //260529 hbk Phase 39.1 G2-01 — default = EdgeOptionLists.FaiCirclePolarStepDeg

    [Category("Edge")] //260529 hbk Phase 39.1 G2-01
    public double Circle_RectL1Ratio { get; set; } = 0.02; //260529 hbk Phase 39.1 G2-01 — default = EdgeOptionLists.FaiCircleRectL1Ratio

    [Category("Edge")] //260529 hbk Phase 39.1 G2-01
    public double Circle_RectL2Ratio { get; set; } = 0.02; //260529 hbk Phase 39.1 G2-01 — default = EdgeOptionLists.FaiCircleRectL2Ratio

    [Category("Edge")] //260529 hbk Phase 39.1 G2-01
    [ItemsSourceProperty(nameof(Circle_PolarEdgeSelectionList))] //260529 hbk Phase 39.1 — Circle_RadialDirectionList 패턴 동형
    public string Circle_PolarEdgeSelection { get; set; } = "First"; //260529 hbk Phase 39.1 G2-01 — default = EdgeOptionLists.FaiCircleEdgeSelection
    ```

    **B) Circle_PolarEdgeSelectionList wrapper 추가 — Circle_RadialDirectionList 선언(L37-38) 직후 위치:**
    ```csharp
    //260529 hbk Phase 39.1 G2-01 — Selections 옵션 래퍼 (EdgeOptionLists 단일 소스)
    [PropertyTools.DataAnnotations.Browsable(false)] //260529 hbk Phase 39.1
    public List<string> Circle_PolarEdgeSelectionList { get { return EdgeOptionLists.Selections; } } //260529 hbk Phase 39.1
    ```

    **C) 기존 polar 분기 (L70-93) 의 4 정적 상수 참조를 4 인스턴스 필드로 교체:**
    ```csharp
    if (!svc.TryFindCircleByPolarSampling(
        image,
        Circle_Row, Circle_Col, Circle_Radius,
        Circle_PolarStepDeg,    //260529 hbk Phase 39.1 G2-01 — was EdgeOptionLists.FaiCirclePolarStepDeg
        Circle_RectL1Ratio,     //260529 hbk Phase 39.1 G2-01 — was EdgeOptionLists.FaiCircleRectL1Ratio
        Circle_RectL2Ratio,     //260529 hbk Phase 39.1 G2-01 — was EdgeOptionLists.FaiCircleRectL2Ratio
        Sigma, EdgeThreshold, polarity,
        Circle_PolarEdgeSelection, //260529 hbk Phase 39.1 G2-01 — was EdgeOptionLists.FaiCircleEdgeSelection
        datumTransform,
        out foundRow, out foundCol, out foundRadius,
        out unusedRows, out unusedCols, out unusedStrips,
        out error))
    ```
    (기존 `//260508 hbk Phase 28 REQ-28-02` 주석 위에 `//260529 hbk Phase 39.1 G2-01` 마커 누적 추가 — Phase 20 D-12 stacking).

    **D) ctor idempotent migration 추가 — 기존 `public CircleDiameterMeasurement(object owner) : base(owner) { }` (L40) 을 다음으로 교체:**
    ```csharp
    //260529 hbk Phase 39.1 G2-03 — idempotent migration: 기존 INI 로드 후 sentinel (0/"") 감지 시 EdgeOptionLists 단일 소스에서 default 채움.
    public CircleDiameterMeasurement(object owner) : base(owner) //260529 hbk Phase 39.1 G2-03
    {
        if (Circle_PolarStepDeg == 0.0) Circle_PolarStepDeg = EdgeOptionLists.FaiCirclePolarStepDeg; //260529 hbk Phase 39.1 G2-03
        if (Circle_RectL1Ratio  == 0.0) Circle_RectL1Ratio  = EdgeOptionLists.FaiCircleRectL1Ratio;  //260529 hbk Phase 39.1 G2-03
        if (Circle_RectL2Ratio  == 0.0) Circle_RectL2Ratio  = EdgeOptionLists.FaiCircleRectL2Ratio;  //260529 hbk Phase 39.1 G2-03
        if (string.IsNullOrEmpty(Circle_PolarEdgeSelection)) Circle_PolarEdgeSelection = EdgeOptionLists.FaiCircleEdgeSelection; //260529 hbk Phase 39.1 G2-03
    }
    ```

    **Brace style (K&R):** 파일 전체 K&R 컨벤션 유지 (CLAUDE.md Code Style).

    **Anti-action:** `EdgeOptionLists.FaiCircle*` 4 정적 상수 그대로 보존. Datum CTH 변경 0. fit 경로 (L60-68) 호출 인자 byte-identical.

    **빌드:** msbuild Debug/x64. 0 errors, 신규 warning 0.
  </action>
  <verify>
    <automated>
      Grep 검증:
      - `grep -n "Circle_PolarStepDeg" WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs` → 최소 3 hit
      - `Circle_RectL1Ratio` `Circle_RectL2Ratio` `Circle_PolarEdgeSelection` 각 3 hit 이상
      - `Circle_PolarEdgeSelectionList` 2 hit (선언 + ItemsSourceProperty)
      - `EdgeOptionLists.FaiCirclePolarStepDeg` 가 CircleDiameterMeasurement.cs 안에 ctor 에서만 등장, polar 분기 호출부에서 등장 0
      - Anti-goal: `grep -n "EdgeOptionLists.FaiCircle" WPF_Example/Halcon/Algorithms/DatumFindingService.cs` → 기존 hit 수 유지 (변경 0)
      - msbuild Debug/x64 = exit code 0, 0 new errors/warnings
    </automated>
  </verify>
  <done>
    - 4 신규 인스턴스 필드 + [Category("Edge")] 어노테이션
    - default = EdgeOptionLists.FaiCircle* byte-identical (10.0 / 0.02 / 0.02 / "First")
    - polar 분기 호출이 4 인스턴스 필드 사용 (정적 상수 참조 제거)
    - ctor 안에 sentinel migration 4 라인 (idempotent)
    - Circle_PolarEdgeSelectionList wrapper ([Browsable(false)])
    - msbuild Debug/x64 PASS — 0 errors, 0 new warnings
    - DatumFindingService.cs 변경 0 (anti-goal)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CircleDiameterMeasurement 에 ICustomTypeDescriptor 13 멤버 구현 (polar 4 필드 동적 hide/show)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs (Task 1 적용 후 상태)
    - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs L11, L263-309 (전체 ICustomTypeDescriptor 패턴)
    - WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs L21-50 (FilterProperties 시그니처)
    - 39.1-PATTERNS.md §1.3 §1.4 (ICustomTypeDescriptor 패턴 + DynamicPropertyHelper 위임)
  </read_first>
  <action>
    `WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs` 에 ICustomTypeDescriptor 13 멤버를 atomic 으로 추가한다 (per D-G2-04).

    **A) 클래스 선언 변경 — L14:**
    ```csharp
    // 변경:
    //260529 hbk Phase 39.1 G2-04 — ICustomTypeDescriptor 추가: Circle_RadialDirection 빈값 시 polar 4 필드 PropertyGrid hide.
    public class CircleDiameterMeasurement : MeasurementBase, System.ComponentModel.ICustomTypeDescriptor //260529 hbk Phase 39.1 G2-04
    ```

    **B) ICustomTypeDescriptor 13 멤버 추가 — 클래스 본문 마지막 (`}` 직전, ctor 다음) 위치:**
    ```csharp
    //260529 hbk Phase 39.1 G2-04 — PropertyGrid 동적 노출. FAIConfig L267-296 패턴 동형.
    public System.ComponentModel.PropertyDescriptorCollection GetProperties(System.Attribute[] attributes) { return BuildFilteredProperties(attributes); }
    public System.ComponentModel.PropertyDescriptorCollection GetProperties() { return BuildFilteredProperties(null); }
    private System.ComponentModel.PropertyDescriptorCollection BuildFilteredProperties(System.Attribute[] attrs) {
        var sourceNames = new System.Collections.Generic.HashSet<string> {
            nameof(EdgePolarityList),
            nameof(Circle_RadialDirectionList),
            nameof(Circle_PolarEdgeSelectionList), // 신규 List
        };
        return DynamicPropertyHelper.FilterProperties(this, attrs,
            name => IsHiddenForRadialDirection(name, Circle_RadialDirection),
            sourceNames);
    }
    //260529 hbk Phase 39.1 G2-04 — Circle_RadialDirection 빈값 (fit 경로) 시 polar 4 필드 + List hide
    private static bool IsHiddenForRadialDirection(string name, string radialDir) {
        if (string.IsNullOrEmpty(radialDir)) {
            if (name == "Circle_PolarStepDeg") return true;
            if (name == "Circle_RectL1Ratio")  return true;
            if (name == "Circle_RectL2Ratio")  return true;
            if (name == "Circle_PolarEdgeSelection") return true;
            if (name == "Circle_PolarEdgeSelectionList") return true;
        }
        return false;
    }
    public System.ComponentModel.AttributeCollection GetAttributes() { return System.ComponentModel.TypeDescriptor.GetAttributes(this, true); }
    public string GetClassName() { return System.ComponentModel.TypeDescriptor.GetClassName(this, true); }
    public string GetComponentName() { return System.ComponentModel.TypeDescriptor.GetComponentName(this, true); }
    public System.ComponentModel.TypeConverter GetConverter() { return System.ComponentModel.TypeDescriptor.GetConverter(this, true); }
    public System.ComponentModel.EventDescriptor GetDefaultEvent() { return System.ComponentModel.TypeDescriptor.GetDefaultEvent(this, true); }
    public System.ComponentModel.PropertyDescriptor GetDefaultProperty() { return System.ComponentModel.TypeDescriptor.GetDefaultProperty(this, true); }
    public object GetEditor(System.Type editorBaseType) { return System.ComponentModel.TypeDescriptor.GetEditor(this, editorBaseType, true); }
    public System.ComponentModel.EventDescriptorCollection GetEvents(System.Attribute[] attributes) { return System.ComponentModel.TypeDescriptor.GetEvents(this, attributes, true); }
    public System.ComponentModel.EventDescriptorCollection GetEvents() { return System.ComponentModel.TypeDescriptor.GetEvents(this, true); }
    public object GetPropertyOwner(System.ComponentModel.PropertyDescriptor pd) { return this; }
    ```

    **Brace style (K&R):** FAIConfig 의 ICustomTypeDescriptor 패턴 그대로 복사하되 마커만 `260529 hbk Phase 39.1 G2-04`.

    **Atomic commit (Phase 19 SUMMARY — PATTERNS.md §1.3):** Task 1 + Task 2 단일 commit 또는 Task 2 commit 안에 13 멤버 전부 포함. 단독 인터페이스 선언 + 멤버 미구현 시 CS0535 빌드 깨짐.

    **빌드:** msbuild Debug/x64 PASS.
  </action>
  <verify>
    <automated>
      Grep 검증:
      - `grep -n "class CircleDiameterMeasurement" CircleDiameterMeasurement.cs` → `: MeasurementBase, System.ComponentModel.ICustomTypeDescriptor` 포함
      - 13 멤버: `grep -nE "GetProperties|GetAttributes|GetClassName|GetComponentName|GetConverter|GetDefaultEvent|GetDefaultProperty|GetEditor|GetEvents|GetPropertyOwner|BuildFilteredProperties|IsHiddenForRadialDirection" CircleDiameterMeasurement.cs` → 최소 13 라인 hit
      - DynamicPropertyHelper.FilterProperties 호출 존재
      - 화이트리스트에 nameof(Circle_PolarEdgeSelectionList) 포함
      - msbuild Debug/x64 = exit code 0, 0 new errors/warnings
    </automated>
  </verify>
  <done>
    - CircleDiameterMeasurement 가 System.ComponentModel.ICustomTypeDescriptor 구현
    - 13 멤버 모두 존재
    - BuildFilteredProperties → DynamicPropertyHelper.FilterProperties 위임
    - IsHiddenForRadialDirection 가 Circle_RadialDirection 빈 문자열 시 polar 4 필드 + List 1 hide
    - msbuild Debug/x64 PASS
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Item #1 SIMUL UAT — CircleDiameter polar 4 필드 노출 + 측정값 변화 + Datum CTH 회귀 0</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs, WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <action>
    사용자 SIMUL 모드에서 5 시나리오 수동 실행 + 결과 보고. 본 task 는 코드 변경 0, 검증만 수행.
  </action>
  <what-built>
    CircleDiameterMeasurement.cs 에 polar 경로 4 인스턴스 필드 + Circle_PolarEdgeSelectionList wrapper + ICustomTypeDescriptor 13 멤버 + ctor idempotent migration. fit 경로 (`Circle_RadialDirection == ""`) 시 polar 4 필드 자동 hide. Datum CTH 폴라 변경 0.
  </what-built>
  <how-to-verify>
    **SIMUL UAT 5 시나리오 (D-G1-04 sign-off):**

    1. **노출 검증 (fit→polar):**
       - 앱 실행, INI 레시피 로드, FAI 노드 선택, EdgeMeasureType=CircleDiameter
       - Circle_RadialDirection 빈값 → PropertyGrid 에 polar 4 필드 모두 미노출
       - Circle_RadialDirection "Inward" → 4 필드 즉시 노출 (default: 10.0 / 0.02 / 0.02 / "First")
       - Circle_RadialDirection 다시 빈값 → 4 필드 재 hide

    2. **측정값 변화 (PolarStepDeg):**
       - polar 활성 후 검사 → 측정값 A
       - PolarStepDeg 10 → 5 → 검사 → 측정값 B. A ≠ B 확인

    3. **측정값 변화 (RectL1Ratio):**
       - StepDeg=10 복귀 + RectL1Ratio 0.02 → 0.05 → 검사 → 측정값 C. A ≠ C 확인

    4. **측정값 변화 (EdgeSelection):**
       - PolarEdgeSelection "First" → "Last" → 검사 → 측정값 D. A ≠ D 확인

    5. **Datum CTH 회귀 0:**
       - Datum CTH 가 있는 레시피: Test Find Datum 실행
       - 검출 결과 (DetectedOrigin / fitting 원) 가 39.1 적용 *전* 결과와 동일
       - INI 파일 byte-level diff 0 (CircleDiameter FAI 외 라인 무변경)
       - DatumFindingService.cs grep: `EdgeOptionLists.FaiCircle*` 참조 4 hit 유지
  </how-to-verify>
  <verify>
    사용자 시나리오 1~5 PASS 보고 + DatumFindingService.cs grep 결과 확인 (변경 0).
  </verify>
  <done>
    사용자가 "approved" 또는 시나리오 1~5 모두 PASS 보고.
  </done>
  <resume-signal>
    사용자가 "approved" 또는 시나리오 1~5 모두 PASS 보고 시 진행. 측정값 변화 없거나 Datum CTH 회귀 발생 시 (CO 신설) 보고 → 재작업.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INI 파일 ↔ ParamBase | 디스크 INI 가 신뢰 영역. 신규 4 필드 sentinel 0 / "" 로드 시 ctor 가 default 채움 (PATTERNS.md §1.7) |
| PropertyGrid ↔ Measurement | UI 입력값 → 인스턴스 필드 → Halcon 호출. ItemsSourceProperty 콤보로 제약된 값 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-01-01 | T (Tampering) | CircleDiameterMeasurement.Circle_PolarStepDeg 0 또는 음수 입력 | accept | PropertyGrid double — TryFindCircleByPolarSampling 내부 가드 (Phase 28). UI 추가 검증 회피 |
| T-39.1-01-02 | I (Info Disclosure) | ICustomTypeDescriptor 동적 hide 로 polar 필드 정보 누출 | accept | risk 0 — 같은 호스트 / 같은 프로세스. UX 명확화 목적 |
| T-39.1-01-03 | T (Tampering) | DatumFindingService 정적 상수 우회 시 Datum CTH polar 결과 변경 | mitigate | DatumFindingService 변경 0 anti-goal (D-G2-05). Acceptance: grep 검증 |
</threat_model>

<verification>
**자동 검증:**
- [ ] msbuild Debug/x64 PASS — 0 errors, 0 new warnings
- [ ] 4 신규 필드 grep
- [ ] ICustomTypeDescriptor 13 멤버 grep
- [ ] polar 분기 정적 상수 제거 + 인스턴스 필드 사용
- [ ] DatumFindingService.cs `EdgeOptionLists.FaiCircle*` 참조 보존
- [ ] EdgeOptionLists.cs L29-33 4 정적 상수 보존

**Anti-goal 회귀:**
- [ ] DatumFindingService.cs 변경 0
- [ ] EdgeOptionLists.cs 변경 0
- [ ] fit 경로 호출 인자 byte-identical

**SIMUL UAT:**
- [ ] 5 시나리오 모두 PASS
</verification>

<success_criteria>
**M1:**
- Circle_PolarStepDeg / RectL1Ratio / RectL2Ratio / PolarEdgeSelection 4 인스턴스 필드 ✓
- ICustomTypeDescriptor 로 Circle_RadialDirection 빈값 시 자동 hide ✓
- 변경 시 측정값 결정적 변화 (SIMUL UAT) ✓

**M5 (Phase 28 anti-goal 회귀 0):**
- DatumFindingService.cs 변경 0 ✓
- EdgeOptionLists.cs 4 정적 상수 보존 ✓
- Datum CTH 검출 결과 사전/사후 동일 ✓
</success_criteria>

<output>
After completion, create `.planning/phases/39.1-inspection-urgent-fixes-2026-05-29/39.1-01-SUMMARY.md`.

Include:
- Tasks executed (Task 1 + Task 2 atomic commit 권장)
- msbuild Debug/x64 PASS 결과
- Grep 검증 결과 (4 필드 / 13 멤버 / Datum CTH 미변경)
- SIMUL UAT 5 시나리오 결과
- Phase 28 anti-goal 회귀 0 증거
</output>
