---
phase: 39.1-inspection-urgent-fixes-2026-05-29
plan: 01
type: summary
status: tasks_complete_pending_uat
date: 2026-05-29
wave: 1
requirements: [WF-01]
commits:
  - 6344ac8 — feat(39.1-01): CircleDiameter polar 4 필드 PropertyGrid 노출 + ICustomTypeDescriptor (atomic Task 1 + Task 2)
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs (+69 / -6)
files_unchanged_anti_goal:
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (0 diff — anti-goal D-G2-05)
  - WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs (0 diff — anti-goal 단일 소스 보존)
---

# Plan 39.1-01 SUMMARY — CircleDiameter polar 파라미터 노출

## Objective Recap

CircleDiameterMeasurement polar 경로 (Phase 28 REQ-28-02) 의 4 polar 파라미터를 `EdgeOptionLists.FaiCircle*` 정적 상수에서 인스턴스 필드로 교체하여 PropertyGrid 에서 사용자 직접 조정 가능. `Circle_RadialDirection` 빈값 (fit 경로) 시 polar 4 필드 자동 hide.

## Tasks Executed

### Task 1 — 4 인스턴스 필드 추가 + ctor migration + 호출부 교체 (atomic with Task 2)

A) 4 신규 인스턴스 필드 `[Category("Edge")]` 추가:
- `Circle_PolarStepDeg` (double, default 10.0)
- `Circle_RectL1Ratio` (double, default 0.02)
- `Circle_RectL2Ratio` (double, default 0.02)
- `Circle_PolarEdgeSelection` (string, default "First") + `[ItemsSourceProperty]`

B) `Circle_PolarEdgeSelectionList` wrapper 추가 — `[Browsable(false)]`, `EdgeOptionLists.Selections` 반환.

C) polar 분기 (L98-121) 의 4 정적 상수 참조를 4 인스턴스 필드로 교체. polarity 매핑 (MapRadialDirectionToHalconPolarity) + fit 경로 변경 0.

D) ctor idempotent sentinel migration (4 라인) — INI 로드 후 0/"" 감지 시 EdgeOptionLists 단일 소스에서 default 채움.

### Task 2 — ICustomTypeDescriptor 13 멤버 atomic 구현

- 클래스 선언 변경: `: MeasurementBase, System.ComponentModel.ICustomTypeDescriptor`
- `GetProperties()` / `GetProperties(Attribute[])` / `BuildFilteredProperties()` — `DynamicPropertyHelper.FilterProperties` 위임
- `IsHiddenForRadialDirection(name, radialDir)` — `Circle_RadialDirection` 빈값 시 polar 4 필드 + `Circle_PolarEdgeSelectionList` hide
- 10 stub 멤버 (GetAttributes/GetClassName/GetComponentName/GetConverter/GetDefaultEvent/GetDefaultProperty/GetEditor/GetEvents x2/GetPropertyOwner) — TypeDescriptor passthrough
- 화이트리스트 sourceNames = `{ EdgePolarityList, Circle_RadialDirectionList, Circle_PolarEdgeSelectionList }`

**Commit 전략:** Phase 19 SUMMARY 패턴 (PATTERNS §1.3) — 단독 인터페이스 선언 + 멤버 미구현 시 CS0535 발생 위험 → Task 1 + Task 2 단일 atomic commit (6344ac8).

## Build Verification

**Command:** `msbuild DatumMeasurement.sln -p:Configuration=Debug -p:Platform=x64`

**Result:** PASS
- 0 errors
- 0 new warnings
- 기존 warning 만 (CS0618 deprecated TopSequence/BottomSequence/TopInspectionAction/BottomInspectionAction, CS0162 unreachable code in VirtualCamera.cs, MSB3884 MinimumRecommendedRules.ruleset)
- 출력: `DatumMeasurement -> .../bin/x64/Debug/DatumMeasurement.exe`

**Worktree 빌드 환경 메모:** worktree 의 `bin/x64/Debug/` 가 비어 있어 HintPath 기반 reference (PropertyTools, PropertyTools.Wpf, WPF.MDI, Basler.Pylon, MvCamCtrl.Net, halcondotnet, ImageGlass.ImageBox) 가 미해결 → 빌드 검증을 위해 메인 디렉토리의 DLL 을 임시로 복사. 복사된 DLL 은 commit 대상이 아니며 빌드 검증 목적만으로 사용.

## Grep Verification

| 검증 항목 | grep 패턴 | 결과 |
|----------|----------|------|
| `Circle_PolarStepDeg` 출현 | `grep -c Circle_PolarStepDeg` | 4 (선언/default 주석/ctor migration/polar 호출) |
| `Circle_RectL1Ratio` 출현 | `grep -c Circle_RectL1Ratio` | 4 |
| `Circle_RectL2Ratio` 출현 | `grep -c Circle_RectL2Ratio` | 4 |
| `Circle_PolarEdgeSelection` 출현 | `grep -c Circle_PolarEdgeSelection` | 8 (List wrapper 포함) |
| `Circle_PolarEdgeSelectionList` 출현 | `grep -c Circle_PolarEdgeSelectionList` | 4 (선언/ItemsSourceProperty/sourceNames/IsHidden) |
| ICustomTypeDescriptor 13 멤버 (+helper 2) | regex 13 멤버명 | 15 hit |
| `EdgeOptionLists.FaiCirclePolarStepDeg` 사용 위치 | `grep -n` | ctor + default 주석만 (polar 호출부 0) |
| **Anti-goal:** DatumFindingService.cs diff | `git diff --stat` | 0 (변경 없음) |
| **Anti-goal:** EdgeOptionLists.cs diff | `git diff --stat` | 0 (변경 없음) |

## Anti-Goal 회귀 0 증거

- **DatumFindingService.cs 변경 0**: `git diff --stat` = empty. Phase 28 D-04 Datum CTH polar 경로 보존.
- **EdgeOptionLists.cs 변경 0**: 4 정적 상수 (`FaiCirclePolarStepDeg=10.0`, `FaiCircleRectL1Ratio=0.02`, `FaiCircleRectL2Ratio=0.02`, `FaiCircleEdgeSelection="First"`) 단일 소스 보존.
- **fit 경로 byte-identical**: `if (string.IsNullOrEmpty(Circle_RadialDirection))` 분기 (L85-95) 무수정. `TryFindCircle` 인자 변경 0.
- **default-equality**: 신규 4 필드 default 값이 `EdgeOptionLists.FaiCircle*` 상수와 byte-identical → 기존 INI 로드 시 기존 동작 그대로 재현 (Phase 28 REQ-28-03 동등성).

## STRIDE Threat Mitigations

- **T-39.1-01-01 (Tampering, polar 입력 0/음수):** accept — PropertyGrid double, `TryFindCircleByPolarSampling` 내부 가드 (Phase 28). UI 검증 회피 채택.
- **T-39.1-01-02 (Info Disclosure, polar 필드 동적 hide):** accept — 같은 호스트/같은 프로세스, risk 0. UX 명확화 목적.
- **T-39.1-01-03 (Tampering, Datum CTH 정적 상수 우회):** mitigate — DatumFindingService 변경 0 anti-goal grep PASS.

## Checkpoint (Pending Human UAT)

Plan 마지막 task = `checkpoint:human-verify`. 본 worktree 실행에서는 코드 변경만 수행. SIMUL UAT 5 시나리오는 사용자/orchestrator 단계에서 수행 예정:

1. **노출 검증 (fit→polar 전환):** RadialDirection 빈값 → polar 4 필드 hide / "Inward" → 노출 (default 10.0/0.02/0.02/"First") / 빈값 복귀 → 재 hide
2. **PolarStepDeg 변화:** 10→5 검사 시 측정값 변경 확인
3. **RectL1Ratio 변화:** 0.02→0.05 검사 시 측정값 변경
4. **EdgeSelection 변화:** "First"→"Last" 검사 시 측정값 변경
5. **Datum CTH 회귀 0:** Datum CTH 가 있는 레시피 Find 결과 사전/사후 동일, DatumFindingService.cs `EdgeOptionLists.FaiCircle*` 참조 보존 (현 0 hit — config.Circle_* 사용)

## Success Criteria Status

| Criterion | Status |
|-----------|--------|
| 4 신규 인스턴스 필드 + `[Category("Edge")]` | DONE |
| default = EdgeOptionLists.FaiCircle* byte-identical | DONE |
| polar 분기 호출 4 인스턴스 필드 사용 (정적 상수 참조 제거) | DONE |
| ctor sentinel migration 4 라인 (idempotent) | DONE |
| Circle_PolarEdgeSelectionList wrapper `[Browsable(false)]` | DONE |
| ICustomTypeDescriptor 13 멤버 atomic 구현 | DONE |
| DynamicPropertyHelper.FilterProperties 위임 | DONE |
| IsHiddenForRadialDirection 빈값 시 polar 4 + List hide | DONE |
| msbuild Debug/x64 PASS (0 errors, 0 new warnings) | DONE |
| DatumFindingService.cs 변경 0 (anti-goal) | DONE |
| EdgeOptionLists.cs 변경 0 (anti-goal) | DONE |
| SIMUL UAT 5 시나리오 | PENDING (orchestrator/사용자) |

## Notes

- Phase 20 D-12 hbk marker stacking 패턴 적용 — 기존 `260508 hbk Phase 28` 마커 보존, 변환 라인에 `260529 hbk Phase 39.1 G2-01/03/04` 누적.
- K&R brace style 유지 (파일 컨벤션).
- C# 7.2 제한 준수 (no nullable refs, no switch expressions, no records). `[ItemsSourceProperty]` + `[Browsable]` PropertyTools 어노테이션은 기존 패턴 동형.
- Plan에서 명시된 PATTERNS.md §1.3 referenced atomic commit 권장 — 13 멤버 단독 commit 시 CS0535 위험 회피.

---
*Generated: 2026-05-29*
*Worktree: agent-a517b8b3db4f5c232*
*Base commit: 5fd2207 (hard reset 적용)*
