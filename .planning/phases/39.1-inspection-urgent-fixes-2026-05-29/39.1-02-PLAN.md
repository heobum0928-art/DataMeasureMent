---
phase: 39.1-inspection-urgent-fixes-2026-05-29
plan: 02
type: execute
# I1 (260529): D-G1-03 의 의도 "Wave 1 algorithm / Wave 2 UI" 가 file overlap
#   (Plan 03 ↔ Plan 04 의 MainView + InspectionListView 공유) 으로 4 plan / 3 wave
#   로 조정됨. Plan 02 는 algorithm wave 1 그대로 유지. 데이터 의존 없음, 단지
#   file 충돌 회피 목적의 wave 분리 (Plan 04 만 wave 3 으로 이동).
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs
autonomous: false
requirements: [WF-01]
tags: [halcon, edge-measurement, datum-relative, projection-pl, axis-fix]
user_setup: []

must_haves:
  truths:
    - "EdgeToLineDistance MeasureAxis='X' + Datum CTH/VTH (DatumAngle2Rad != 0) 시 실제 datum 수직선(DatumAngle2Rad) 기준 정사영 — 부정확한 직각 케이스에서 측정값 정확화"
    - "MeasureAxis='X' + Datum TLI 등 DatumAngle2Rad sentinel 0 시 폴백 = 기존 로직 (DatumAngleRad+90° 가정) → TLI Datum 사용 시 회귀 0"
    - "MeasureAxis='Y' (default) 분기 변경 0 — datum 수평선까지 거리 측정 (회귀 0)"
    - "DatumOriginRow/Col 미주입 폴백 (L193-208 AffineTransPoint2d 레거시 경로) 변경 0 — Datum 미설정 측정 회귀 0"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs"
      provides: "measureX 분기에서 DatumAngle2Rad 사용 + 폴백 분기 추가 + signedPx 계산 재구성"
      contains: "DatumAngle2Rad != 0.0, axisR1/C1/R2/C2 신규 공식, signedPx 부호 정사영"
  key_links:
    - from: "EdgeToLineDistanceMeasurement.TryExecute measureX 분기 (L156-161 → 신규)"
      to: "DatumConfig.DetectedRefAngle2 (Action_FAIMeasurement L217 주입)"
      via: "consumer.DatumAngle2Rad = dc.DetectedRefAngle2 흐름 활용 — 인터페이스 신규 추가 0"
      pattern: "DatumAngle2Rad.*!=.*0\\.0"
    - from: "EdgeToLineDistanceMeasurement.TryExecute signedPx (L183-189)"
      to: "axis 정사영 부호 공식 (measureX 분기만)"
      via: "(sinT, cosT) 가 DatumAngle2Rad 기반으로 재계산되므로 신규 axis vector 와 일관성 유지"
      pattern: "signedPx.*pRow.*footRow"
---

<objective>
EdgeToLineDistance MeasureAxis='X' 분기에서 가상 수직선(DatumAngleRad+90°) 가정을 폐기하고 실제 datum 수직 기준선(DatumAngle2Rad) 을 사용한다.
Datum CTH/VTH 사용 시 부품이 정확한 직각이 아닌 케이스에서 정사영 결과가 정확해진다.
Phase 31 hotfix#3 이 IDatumOriginConsumer 에 `DatumAngle2Rad` 멤버를 이미 추가했고 Action_FAIMeasurement L217 이 이미 주입 중이지만, EdgeToLineDistance 가 실제 사용하지 않던 carry-over 를 해결.

Purpose: 사용자 보고 "Datum 수직선으로 X축 거리... 못 미치거나 넘어감" — Datum CTH/VTH 가 부품의 실측 수직 기준선을 검출하지만, EdgeToLineDistance 가 datum 수평선 각도 + 90° 가상 수직선으로 정사영 → 부품이 정확한 직각 아닐 때 거리 오차 발생.
Output: EdgeToLineDistanceMeasurement.cs measureX 분기만 수정. 단일 파일 수정.

**Anti-goal (변경 0, 회귀 0):**
- `EdgeToLineDistanceMeasurement.cs` measureY 분기 L163-169 변경 0 (Phase 23 ALG-01 + 23.1 l5e-03 결과 안정화)
- `EdgeToLineDistanceMeasurement.cs` DatumOriginRow/Col 미주입 폴백 L193-208 변경 0 (Datum 미설정/TLI 케이스 회귀 0)
- `Action_FAIMeasurement.cs` L214-217 주입 경로 변경 0 (Phase 31 D-03)
- `IDatumOriginConsumer.cs` 인터페이스 변경 0
- Phase 31 D-04 `ComputeProjectionDistance` 헬퍼 통합 0 (D-G3-03 인라인 유지 — overlay footRow/footCol 회귀 방지)
- TLI Datum 사용 시 `DatumAngle2Rad == 0` 폴백 → 기존 로직 동작 (D-G3-04)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-inspection-urgent-fixes-2026-05-29/39.1-CONTEXT.md
@.planning/phases/39.1-inspection-urgent-fixes-2026-05-29/39.1-PATTERNS.md
@WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs
@WPF_Example/Custom/Sequence/Inspection/IDatumOriginConsumer.cs
@WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
@WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs

<interfaces>
IDatumOriginConsumer.cs 전체 (인터페이스 — 본 plan 변경 0):
```csharp
public interface IDatumOriginConsumer
{
    double DatumOriginRow { get; set; }
    double DatumOriginCol { get; set; }
    double DatumAngleRad  { get; set; }
    // CircleTwoHorizontal=π/2, TwoLineIntersect=Line2 실제각, VerticalTwoHorizontal=수직에지 실제각.
    double DatumAngle2Rad { get; set; }
    double DatumDetectedCircleRow { get; set; }
    double DatumDetectedCircleCol { get; set; }
}
```

EdgeToLineDistanceMeasurement.cs L86 (DatumAngle2Rad 이미 선언, 현재 미사용):
```csharp
[System.ComponentModel.Browsable(false)]
[PropertyTools.DataAnnotations.Browsable(false)]
[Newtonsoft.Json.JsonIgnore]
public double DatumAngle2Rad { get; set; } //260519 hbk Phase 31 hotfix#3
```

Action_FAIMeasurement.cs L214-217 (DatumAngle2Rad 주입 — 본 plan 변경 0):
```csharp
consumer.DatumOriginRow = dc.DetectedOriginRow;
consumer.DatumOriginCol = dc.DetectedOriginCol;
consumer.DatumAngleRad  = dc.DetectedRefAngle;
consumer.DatumAngle2Rad = dc.DetectedRefAngle2; //260519 hbk Phase 31 hotfix#3 — 수직 기준선 각도
```

EdgeToLineDistanceMeasurement.cs L151-191 현재 (수정 대상):
```csharp
bool measureX = (MeasureAxis == "X");
double sinT = System.Math.Sin(DatumAngleRad);
double cosT = System.Math.Cos(DatumAngleRad);
if (cosT < 0.0) { sinT = -sinT; cosT = -cosT; }
double axisR1, axisC1, axisR2, axisC2;
if (measureX) //  ← 문제: DatumAngleRad+90° 가정
{
    axisR1 = DatumOriginRow - 200.0 * cosT;
    axisC1 = DatumOriginCol + 200.0 * sinT;
    axisR2 = DatumOriginRow + 200.0 * cosT;
    axisC2 = DatumOriginCol - 200.0 * sinT;
}
else // measureY — datum 수평선(x축), 변경 0
{
    axisR1 = DatumOriginRow - 200.0 * sinT;
    axisC1 = DatumOriginCol - 200.0 * cosT;
    axisR2 = DatumOriginRow + 200.0 * sinT;
    axisC2 = DatumOriginCol + 200.0 * cosT;
}
// ProjectionPl + try/catch
double signedPx;
if (measureX) {
    signedPx = (pRow - footRow) * sinT + (pCol - footCol) * cosT;
} else {
    signedPx = (pRow - footRow) * (-cosT) + (pCol - footCol) * sinT;
}
resultValue = signedPx * pixelResolution;
```

**핵심 invariant:** DatumConfig 가 CTH/VTH 검출 시 DetectedRefAngle2 ≠ 0 으로 write-back. TLI 등 2차 기준선 없는 알고리즘 시 미 write-back → default 0 (sentinel). 폴백 가드 = `DatumAngle2Rad != 0.0`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="soft">
  <name>Task 1: 사용자 SIMUL 이미지로 현재 결함 재현 + 기대값 측정 (수정 전 baseline)</name>
  <files>(no code changes — baseline measurement only)</files>
  <read_first>
    - 39.1-CONTEXT.md `<specifics>` 섹션 Item #2 사용자 보고 시나리오
    - 39.1-PATTERNS.md §2.1 "Current measureX branch (L151-162, problematic)"
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs L151-191
  </read_first>
  <action>
    **Note (I3, 260529):** task type = `checkpoint:human-verify` (gate="soft") — 사용자 SIMUL 이미지 수집 + baseline 측정 단계로 사용자 상호작용 필수. soft gate 로 코드 변경 없이 측정 데이터만 수집 후 Task 2 진행.

    **Goal:** 사용자가 보고한 X축 거리 부정확 케이스를 SIMUL 모드에서 재현하고, 수정 전 측정값과 기대값(실측 또는 SOP 도면값) 차이를 정량 기록한다. Task 2 수정 후 동일 케이스 재측정으로 정확화 검증.

    **A) 사용자 와 함께 SIMUL 시나리오 준비 (interactive checkpoint 성격):**
    - 사용자에게 다음 정보 요청:
      1. SIMUL 이미지 경로 (Cal_Image/ 또는 다른 폴더)
      2. Datum 알고리즘 = CircleTwoHorizontal (CTH) 또는 VerticalTwoHorizontal (VTH)
      3. EdgeToLineDistance FAI 위치 + MeasureAxis='X' 설정 확인
      4. 기대 측정값 (SOP 도면 수치 또는 실측)
      5. 현재 출력되는 잘못된 측정값 (못 미침 또는 넘어감 방향 + 오차 크기)
    - 사용자가 정보 제공 불가 시: 임시 SIMUL 케이스 구성 — `Cal_Image/DualImageTest/SIDE1_3-1_Datum_A1_A2_backlight_LV30.bmp` (현재 untracked 파일) 사용 또는 기존 Phase 23.1 UAT 케이스 재활용.

    **B) baseline 측정 (수정 전, Task 2 미적용 상태):**
    - 위 시나리오로 검사 실행 → 측정값 R0 기록
    - 기대값 E 와 차이 |R0 - E| = Δ0 기록
    - DatumConfig.DetectedRefAngle (θ1, 수평선) vs DetectedRefAngle2 (θ2, 수직선) 차이가 90° 가 아님을 확인 (Logging.PrintLog 임시 추가 또는 디버그 캔버스) → 부정확 직각 케이스 검증
    - 디버그 정보를 본 plan SUMMARY 또는 메모로 보관

    **본 task 는 코드 변경 0. 측정 데이터 수집만 수행.**
  </action>
  <what-built>
    코드 변경 0 — 사용자 SIMUL 이미지로 EdgeToLineDistance measureX 분기의 부정확 직각 케이스를 baseline 측정만 수집.
  </what-built>
  <how-to-verify>
    사용자 SIMUL 시나리오 구성 후 baseline R0/E/Δ0 + θ1/θ2 차이 데이터 보고. soft gate 이므로 baseline 데이터만 inline 으로 기록되면 Task 2 진행.
  </how-to-verify>
  <verify>
    <automated>
      코드 변경 0 — 자동 빌드/grep 검증 항목 없음.
      검증: SIMUL UAT 결과 메모/로그 (baseline 측정값 R0 + 기대값 E + 차이 Δ0 + DatumRefAngle θ1 vs θ2 데이터) 가 존재 — 본 task 완료 보고에 inline 으로 기록.
    </automated>
  </verify>
  <done>
    - 사용자 또는 임시 SIMUL 시나리오 구성 완료
    - 수정 전 baseline 측정값 R0 + 기대값 E + 오차 Δ0 정량 기록
    - DatumConfig.DetectedRefAngle vs DetectedRefAngle2 가 정확히 ±90° 가 아님을 데이터로 확인
    - 사용자가 baseline 차이 인지 + Task 2 적용 후 재측정 동의
  </done>
  <resume-signal>
    사용자가 baseline 데이터 보고 + "proceed" / "approved" 신호 시 Task 2 진행. SIMUL 케이스 구성 불가 시 alternative 케이스 (Phase 23.1 UAT 재활용) 합의 후 진행.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: EdgeToLineDistance measureX 분기에서 DatumAngle2Rad 사용 + 폴백 분기 + signedPx 재구성</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs L99-208 (TryExecute 전체)
    - 39.1-PATTERNS.md §2.1 "Pattern to apply" 신규 분기 구조
    - Task 1 의 baseline 측정 데이터 (R0, E, Δ0)
  </read_first>
  <action>
    `WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs` 의 measureX 분기를 다음과 같이 수정한다 (per D-G3-02, D-G3-04).

    **수정 위치:** L151-191 (sinT/cosT 계산 + measureX axis vector 정의 + signedPx 부호 계산 블록). measureY 분기 + 폴백 (L193-208) 변경 0.

    **A) sinT/cosT 계산 블록 (L152-154) 을 다음으로 교체 — measureX 시 DatumAngle2Rad 폴백 가드:**
    ```csharp
    //260529 hbk Phase 39.1 G3-02 — measureX 만 DatumAngle2Rad 사용 (실 datum 수직 기준선 각도). measureY 는 DatumAngleRad 유지 (회귀 0).
    //  근본 원인 (D-G3-01): 기존 코드는 DatumAngleRad+90° 가정 — DetectedRefAngle2 ≠ DetectedRefAngle+90° 일 때 정사영 오차.
    //  폴백 (D-G3-04): DatumAngle2Rad==0 (TLI Datum 등 2차 기준선 미주입) 시 기존 로직 유지 → TLI 회귀 0.
    bool measureX = (MeasureAxis == "X"); //260517 hbk l5e-03
    bool useAngle2 = measureX && (DatumAngle2Rad != 0.0); //260529 hbk Phase 39.1 G3-02/G3-04
    double angleSrc; //260529 hbk Phase 39.1 G3-02
    if (useAngle2)
    {
        angleSrc = DatumAngle2Rad; //260529 hbk Phase 39.1 G3-02 — 실 수직 기준선
    }
    else
    {
        angleSrc = DatumAngleRad; //260529 hbk Phase 39.1 G3-04 — 1차 수평선 (회귀 0)
    }
    double sinT = System.Math.Sin(angleSrc); //260517 hbk l5e-03 //260529 hbk Phase 39.1 G3-02
    double cosT = System.Math.Cos(angleSrc); //260517 hbk l5e-03 //260529 hbk Phase 39.1 G3-02
    if (cosT < 0.0) { sinT = -sinT; cosT = -cosT; } //260517 hbk l5e-03 — cosθ≥0 정규화
    ```

    **B) measureX 분기 axis vector 정의 (L155-162) 를 다음으로 교체:**
    ```csharp
    double axisR1, axisC1, axisR2, axisC2; //260517 hbk l5e-03
    if (measureX) //260517 hbk l5e-03
    {
        if (useAngle2) //260529 hbk Phase 39.1 G3-02 — 실 수직 기준선: 방향벡터 (sinθ2,cosθ2) — measureY 와 같은 공식 (각도만 θ→θ2)
        {
            axisR1 = DatumOriginRow - 200.0 * sinT; //260529 hbk Phase 39.1 G3-02
            axisC1 = DatumOriginCol - 200.0 * cosT; //260529 hbk Phase 39.1 G3-02
            axisR2 = DatumOriginRow + 200.0 * sinT; //260529 hbk Phase 39.1 G3-02
            axisC2 = DatumOriginCol + 200.0 * cosT; //260529 hbk Phase 39.1 G3-02
        }
        else //260529 hbk Phase 39.1 G3-04 — 폴백: 기존 가상 수직선 (DatumAngleRad+90° 가정)
        {
            axisR1 = DatumOriginRow - 200.0 * cosT; //260517 hbk l5e-03
            axisC1 = DatumOriginCol + 200.0 * sinT; //260517 hbk l5e-03
            axisR2 = DatumOriginRow + 200.0 * cosT; //260517 hbk l5e-03
            axisC2 = DatumOriginCol - 200.0 * sinT; //260517 hbk l5e-03
        }
    }
    else //260517 hbk l5e-03 — measureY 변경 0
    {
        axisR1 = DatumOriginRow - 200.0 * sinT; //260517 hbk l5e-03
        axisC1 = DatumOriginCol - 200.0 * cosT; //260517 hbk l5e-03
        axisR2 = DatumOriginRow + 200.0 * sinT; //260517 hbk l5e-03
        axisC2 = DatumOriginCol + 200.0 * cosT; //260517 hbk l5e-03
    }
    ```

    **C) signedPx 부호 계산 블록 (L182-191) 을 다음으로 교체:**
    ```csharp
    double signedPx; //260517 hbk l5e-03
    if (measureX) //260517 hbk l5e-03 — +X 오른쪽 양수
    {
        if (useAngle2) //260529 hbk Phase 39.1 G3-02 — axis (sinθ2,cosθ2) 의 우측 법선 (cosθ2,-sinθ2)
        {
            signedPx = (pRow - footRow) * cosT - (pCol - footCol) * sinT; //260529 hbk Phase 39.1 G3-02
        }
        else //260529 hbk Phase 39.1 G3-04 — 폴백: 기존 (sinθ, cosθ) 공식
        {
            signedPx = (pRow - footRow) * sinT + (pCol - footCol) * cosT; //260517 hbk l5e-03
        }
    }
    else //260517 hbk l5e-03 — measureY +Y 위쪽 양수 변경 0
    {
        signedPx = (pRow - footRow) * (-cosT) + (pCol - footCol) * sinT; //260517 hbk l5e-03
    }
    resultValue = signedPx * pixelResolution; //260517 hbk l5e-03
    ```

    **부호 공식 유도 메모 (주석으로 남길 것):**
    - axis 방향벡터 d = (sinθ, cosθ) → 우측 법선 n_right = (cosθ, -sinθ) (시계방향 90°)
    - +X(이미지 오른쪽) 양수 의도 = pixel col 증가 방향 → axis 가 위(row 감소) 방향일 때 우측은 +col
    - signedPx = (Δrow * n_right_row) + (Δcol * n_right_col) = Δrow*cosθ - Δcol*sinθ ← useAngle2 시 적용
    - **W1 (260529): useAngle2 분기 signedPx 공식과 폴백 공식의 정규화 후 동치 증명** — 정확한 직각 케이스 (θ2 = θ + π/2) → cosθ2 < 0 정규화 후 부호 반전 적용 시 폴백 공식과 수학적 동치 (Δ1 = 0). 이는 UAT 시나리오 2 "정확한 직각 회귀 0" 검증의 수학적 근거.

    **Brace style (Allman):** EdgeToLineDistanceMeasurement.cs 는 Allman style. 모든 신규 if/else 의 `{` 별도 라인. 기존 K&R 표현 (`if (cosT < 0.0) { ... }`) 보존.

    **빌드:** msbuild Debug/x64 PASS.

    **사용자 SIMUL 사후 점검:** Task 1 baseline 시나리오 재실행 → 수정 후 측정값 R1 기록. Δ1 < Δ0 검증.
  </action>
  <verify>
    <automated>
      Grep 검증:
      - `grep -n "useAngle2" EdgeToLineDistanceMeasurement.cs` → 최소 4 hit (선언 + 3 분기 사용)
      - `grep -n "DatumAngle2Rad" EdgeToLineDistanceMeasurement.cs` → 최소 2 hit
      - measureY 분기 보존: 기존 L163-169 axis 라인 보존 (sinT 사용)
      - 폴백 분기 보존: 기존 L158-161 axis 라인 보존 (cosT 사용)
      - msbuild Debug/x64 = exit code 0, 0 new errors/warnings
      - Action_FAIMeasurement.cs / IDatumOriginConsumer.cs / DatumConfig.cs 변경 0
    </automated>
  </verify>
  <done>
    - measureX 분기에서 DatumAngle2Rad 사용 + 폴백 (DatumAngle2Rad==0 → DatumAngleRad)
    - measureY 분기 변경 0
    - DatumOriginRow/Col 미주입 폴백 L193-208 변경 0
    - signedPx 공식 useAngle2 / 폴백 / measureY 3 분기 분리
    - msbuild Debug/x64 PASS
    - Task 1 baseline (R0) 대비 Task 2 (R1) 의 기대값 (E) 일치도 향상 (Δ1 < Δ0)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Item #2 SIMUL UAT — EdgeToLineDistance MeasureAxis='X' 정확화 + Phase 23/23.1 회귀 0</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs</files>
  <action>
    사용자 SIMUL 모드에서 5 시나리오 수동 실행 + 결과 보고. 본 task 는 코드 변경 0.
  </action>
  <what-built>
    EdgeToLineDistanceMeasurement.cs measureX 분기에서 DatumAngle2Rad (실 datum 수직 기준선 각도) 사용. measureY 분기 + DatumOriginRow/Col 미주입 폴백 변경 0. TLI Datum (DatumAngle2Rad==0) 사용 시 폴백 = 기존 로직.
  </what-built>
  <how-to-verify>
    **SIMUL UAT 5 시나리오 (D-G1-04 sign-off):**

    1. **부정확 직각 케이스 정확화 (핵심):**
       - Task 1 baseline 시나리오 재실행
       - |R1 - E| < |R0 - E| 확인 (정확도 향상)
       - 사용자가 "정확해졌다" 확인

    2. **정확한 직각 케이스 회귀 0:**
       - CTH Datum + MeasureAxis='X' + 부품이 정확한 직각
       - 수정 전후 측정값 동일

    3. **MeasureAxis='Y' 분기 회귀 0:**
       - CTH/VTH Datum + MeasureAxis='Y' 5 종
       - 수정 전후 측정값 동일

    4. **TLI Datum 폴백 회귀 0:**
       - TLI Datum + EdgeToLineDistance MeasureAxis='X'
       - DatumAngle2Rad == 0 폴백 → 수정 전후 측정값 동일

    5. **Datum 미주입 폴백 회귀 0:**
       - EdgeToLineDistance + DatumRef="" 또는 매칭 없음 (L193-208 폴백)
       - 수정 전후 측정값 동일

    **자동 점검:**
    - msbuild Debug/x64 PASS
    - Action_FAIMeasurement.cs / IDatumOriginConsumer.cs / DatumConfig.cs 변경 0
  </how-to-verify>
  <verify>
    사용자 시나리오 1~5 PASS + Δ1 < Δ0 검증.
  </verify>
  <done>
    사용자가 "approved" 또는 시나리오 1~5 모두 PASS 보고.
  </done>
  <resume-signal>
    사용자가 "approved" 시 진행. 정확화 실패 (Δ1 ≥ Δ0) 또는 회귀 (시나리오 2~5) 발생 시 (CO 신설) 보고.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DatumConfig ↔ Action_FAIMeasurement | DatumConfig.DetectedRefAngle2 가 신뢰 영역. 본 plan 은 주입된 값을 사용만 |
| consumer.DatumAngle2Rad ↔ TryExecute | 미주입 시 default 0 → 폴백 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-02-01 | T (Tampering) | DatumAngle2Rad 잘못 주입 (NaN, ±π 경계) | mitigate | (1) `DatumAngle2Rad != 0.0` 폴백 가드. (2) HOperatorSet.ProjectionPl try/catch → 예외 시 footOk=false |
| T-39.1-02-02 | T (Tampering) | measureY 분기 회귀 (코드 변경 실수) | mitigate | grep 검증 (`axisR1 = DatumOriginRow - 200.0 * sinT` 기존 라인 보존). SIMUL UAT 시나리오 3 |
| T-39.1-02-03 | T (Tampering) | TLI Datum 폴백 분기 회귀 | mitigate | useAngle2 가드 (`DatumAngle2Rad != 0.0`). SIMUL UAT 시나리오 4 |
</threat_model>

<verification>
**자동 검증:**
- [ ] msbuild Debug/x64 PASS
- [ ] EdgeToLineDistanceMeasurement.cs measureX 분기에 useAngle2 + DatumAngle2Rad 사용
- [ ] measureX useAngle2 axis 공식 = measureY 공식과 동일 (각도만 θ→θ2)
- [ ] measureX 폴백 axis 공식 = 기존 (DatumAngleRad+90° 가정) 보존
- [ ] measureY 분기 grep (변경 0)
- [ ] DatumOriginRow/Col 미주입 폴백 L193-208 grep (변경 0)
- [ ] Action_FAIMeasurement.cs / IDatumOriginConsumer.cs / DatumConfig.cs 변경 0

**Anti-goal:**
- [ ] Action_FAIMeasurement.cs L214-217 주입 경로 변경 0
- [ ] measureY 분기 L163-169 변경 0
- [ ] DatumOriginRow/Col 미주입 폴백 L193-208 변경 0
- [ ] Phase 31 D-04 헬퍼 미통합

**SIMUL UAT:**
- [ ] 5 시나리오 모두 PASS
</verification>

<success_criteria>
**M2:**
- measureX 분기에서 DatumAngle2Rad 사용 + 폴백 ✓
- 부정확 직각 케이스 정확화 (Δ1 < Δ0) ✓
- 폴백 → 기존 로직 ✓

**M5 (Phase 23/23.1/31 anti-goal 회귀 0):**
- measureY 변경 0 ✓
- DatumOriginRow/Col 미주입 폴백 변경 0 ✓
- Phase 31 D-04 헬퍼 미통합 ✓
- IDatumOriginConsumer 변경 0 ✓
</success_criteria>

<output>
After completion, create `.planning/phases/39.1-inspection-urgent-fixes-2026-05-29/39.1-02-SUMMARY.md`.

Include:
- Tasks executed (Task 1 baseline + Task 2 implementation)
- Task 1 의 R0/E/Δ0 baseline 데이터
- Task 2 의 R1/Δ1 측정 + Δ1 < Δ0 검증
- 부호 공식 유도 메모 + W1 정규화 후 동치 증명 추가
- msbuild Debug/x64 PASS 결과
- Grep 검증 결과
- SIMUL UAT 5 시나리오 결과
- Anti-goal 회귀 0 증거
</output>
