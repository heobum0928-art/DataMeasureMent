---
phase: 39.1-inspection-urgent-fixes-2026-05-29
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: false
requirements: [WF-01]
tags: [ui, inspection-result, overlay, fai-node, post-inspection]
user_setup: []

# 주: Wave 1 (Plan 01 + 02) 와 데이터 의존 0 (algorithm vs UI 분리). Plan 04 (Datum CTH Edit)
#     도 MainView.xaml.cs 수정 → file overlap 으로 Plan 04 는 wave 3 로 분리.

must_haves:
  truths:
    - "검사 완료 후 InspectionListView 트리에서 Measurement 노드 클릭 → 해당 측정의 결과 + 소유 Shot 이미지 + overlay (FAI-Edge*/FAI-DistLine) 캔버스 재현"
    - "검사 완료 후 InspectionListView 트리에서 FAI 노드 클릭 → 해당 FAI 하위 Measurement 들의 결과 + Shot 이미지 + overlay (전체 measurement overlay 합산) 캔버스 재현"
    - "Sequence 동작 변경 0 — overlay 데이터는 검사 시점에 FAIConfig.LastOverlays 에 누적, 노드 클릭 시 재 호출 없이 재 렌더"
    - "전 FAI 타입 공통 동작 — CircleDiameter / EdgeToLineDistance / PointToLineDistance / EdgePairDistance / EdgeToLineAngle / ArcEdgeDistance 6 타입 동일"
    - "동일 노드 다시 클릭 → 동일 재현 (멱등)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs"
      provides: "FAIConfig.LastOverlays neue List<EdgeInspectionOverlay> — 검사 결과 overlay 저장"
      contains: "public List<EdgeInspectionOverlay> LastOverlays"
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "EStep.Measure 루프 안에서 per-FAI overlay 누적 → fai.LastOverlays write-back"
      contains: "fai.LastOverlays = faiOverlays"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "RenderInspectionResultForNode(ParamBase param) 신규 통합 진입점 + RenderStoredOverlaysForFai/Measurement helper"
      contains: "public void RenderInspectionResultForNode, halconViewer.SetInspectionOverlays"
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "Measurement/FAI 분기에서 RenderInspectionResultForNode 호출 추가"
      contains: "mParentWindow.mainView.RenderInspectionResultForNode"
  key_links:
    - from: "Action_FAIMeasurement.cs EStep.Measure (L261 overlayAcc.AddRange)"
      to: "fai.LastOverlays = faiOverlays (per-FAI scope)"
      via: "기존 overlayAcc 와 별도로 per-FAI overlay 누적 → 검사 종료 시 각 FAIConfig 객체에 저장"
      pattern: "fai\\.LastOverlays\\s*="
    - from: "InspectionListView.InspectionList_SelectionChanged (L489-547)"
      to: "MainView.RenderInspectionResultForNode (신규)"
      via: "Measurement / FAI 분기에서 통합 진입점 호출"
      pattern: "RenderInspectionResultForNode"
    - from: "MainView.RenderInspectionResultForNode"
      to: "halconViewer.SetInspectionOverlays + UpdateDisplayState"
      via: "기존 SetInspectionOverlays API 활용 — 신규 overlay API 0"
      pattern: "halconViewer\\.SetInspectionOverlays|UpdateDisplayState"
---

<objective>
검사 완료 후 InspectionListView 트리에서 Measurement 또는 FAI 노드 클릭 시 해당 항목의 측정 결과 + 소유 Shot 이미지 + overlay 를 캔버스에 재현한다.
overlay 데이터는 검사 시점에 FAIConfig.LastOverlays 에 per-FAI 단위로 누적 저장 → 노드 클릭 시 측정 재 실행 없이 저장된 overlay 만 재 렌더 → Sequence 동작 변경 0 보장.

Purpose: POC 시연 시 사용자가 검사 결과 노드별 시각 검증 필요 — 측정값 + 검출 에지/거리선/원이 동시 확인되어야 정합성 판단 가능.
Output: 4 파일 수정 — FAIConfig.cs (필드 추가), Action_FAIMeasurement.cs (write-back), MainView.xaml.cs (재현 진입점), InspectionListView.xaml.cs (호출 분기).

**Anti-goal (변경 0, 회귀 0):**
- Sequence 실행 동작 변경 0 — TryExecute 호출 / 측정 흐름 / overlayAcc → InspectionOverlays 흐름 변경 0
- Phase 37 D-37-03 lenient 동작 보존
- Phase 39 D-01 gate (`_failedDatums` HashSet + IsDatumFailed) 변경 0
- Phase 37 hotfix A/B FindFAIContainingMeasurement (object reference round-trip) 변경 0
- 기존 `pMyContext.InspectionOverlays = overlayAcc` (L291) 동작 변경 0
- `InspectionViewModel.OnFAISelected/ClearResults` 변경 0
- PropertyGrid SelectedObject 재할당 force rebind 패턴 (L507-515, L534-542) 변경 0
- DataGrid_faiResults FAIResults_SelectionChanged 핸들러 (L201) 변경 0
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-inspection-urgent-fixes-2026-05-29/39.1-CONTEXT.md
@.planning/phases/39.1-inspection-urgent-fixes-2026-05-29/39.1-PATTERNS.md
@WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
@WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
@WPF_Example/UI/ContentItem/MainView.xaml.cs
@WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
@WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs
@WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs

<interfaces>
FAIConfig.cs L97-108 (기존 결과 필드 — 본 plan 변경 0):
```csharp
[Browsable(false)]
public double MeasuredValue { get; set; }
[Browsable(false)]
public bool IsPass { get; set; }
//260529 hbk Phase 39 WF-01 D-02 — datum-skip subtype 플래그.
[Browsable(false)]
public bool WasDatumSkipped { get; set; }
```

Action_FAIMeasurement.cs L164-294 (EStep.Measure 전체 — overlayAcc 누적 패턴):
```csharp
case EStep.Measure: {
    var overlayAcc = new List<EdgeInspectionOverlay>(); // Shot 단위 누적
    foreach (var fai in ShotParam.FAIList) {
        bool faiAllPass = true;
        foreach (var meas in fai.Measurements) {
            // ... DATUM_FAIL gate / datum transform / consumer.DatumOrigin* 주입 / TryExecute / EvaluateJudgement
            if (measOverlays != null) {
                string suffix = meas.LastJudgement ? "-OK" : "-NG";
                foreach (var ov in measOverlays) {
                    if (ov.RoiId.StartsWith("FAI-Edge", ...)) ov.RoiId = ov.RoiId + suffix;
                }
                overlayAcc.AddRange(measOverlays);
            }
        }
        if (fai.Measurements.Count > 0) {
            fai.IsPass = faiAllPass;
            fai.MeasuredValue = fai.Measurements[0].LastMeasuredValue;
            // WasDatumSkipped 계산
        }
    }
    pMyContext.InspectionOverlays = overlayAcc; // 변경 0
    Step = (int)EStep.End;
}
```

MainView.xaml.cs L181-189 (DisplayMeasurementImage):
```csharp
public void DisplayMeasurementImage(MeasurementBase measurement) {
    if (measurement == null) return;
    FAIConfig fai = FindFAIContainingMeasurement(measurement);
    if (fai == null) return;
    ShotConfig shot = fai.Owner as ShotConfig;
    DisplayShotImage(shot);
}
```

MainView.xaml.cs L124-132 (DisplayFAIImage):
```csharp
public void DisplayFAIImage(FAIConfig fai) {
    if (fai == null) { /* ... */ return; }
    ShotConfig shot = fai.Owner as ShotConfig;
    DisplayShotImage(shot);
}
```

HalconViewerControl.xaml.cs L161 (SetInspectionOverlays — 기존 API):
```csharp
public void SetInspectionOverlays(IEnumerable<EdgeInspectionOverlay> overlays)
```

InspectionListView.xaml.cs L488-547 (Measurement / FAI 분기 — 수정 위치):
```csharp
else if (item.NodeType == ENodeType.Measurement) {
    // force rebind ParamEditor
    if (_inspectionVm != null) _inspectionVm.ClearResults();
    if (mParentWindow != null && mParentWindow.mainView != null && itemParam is MeasurementBase meas)
        mParentWindow.mainView.DisplayMeasurementImage(meas);  // ← 이미지만, overlay 없음
    if (mParentWindow != null && mParentWindow.mainView != null)
        mParentWindow.mainView.HighlightSelectedRoi(itemParam as ParamBase);
}
else if (item.NodeType == ENodeType.FAI) {
    if (_inspectionVm != null && itemParam is FAIConfig faiConfig) {
        _inspectionVm.OnFAISelected(faiConfig);
        mParentWindow.mainView.DisplayFAIImage(faiConfig);  // ← 이미지만, overlay 없음
        // force rebind
    }
    if (mParentWindow != null && mParentWindow.mainView != null)
        mParentWindow.mainView.HighlightSelectedRoi(itemParam as ParamBase);
}
```

**핵심 인사이트:** overlay 데이터는 검사 시점에 measOverlays 변수에 fai 단위로 한 번씩 들어옴 (L233 + L251-262). 별도 List 에 per-FAI 단위 누적 후 fai.LastOverlays 에 write-back → 노드 클릭 시 SetInspectionOverlays 호출만 추가하면 재현 가능.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: FAIConfig.LastOverlays 필드 추가 + Action_FAIMeasurement 에서 per-FAI write-back</name>
  <files>WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs, WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs L1-108 (헤더 + 결과 필드 위치)
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs L164-294 (EStep.Measure 전체)
    - WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs (Overlay 타입 정의)
    - 39.1-PATTERNS.md §3.4 (FAIConfig 결과 저장 필드 패턴)
  </read_first>
  <action>
    **A) FAIConfig.cs 에 LastOverlays 필드 추가:**
    `WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs` L97-108 의 `WasDatumSkipped` 직후 신규 필드 추가:

    ```csharp
    //260529 hbk Phase 39.1 G4-01 — 검사 결과 overlay 저장. 노드 클릭 시 재 렌더용.
    //  검사 시점에 Action_FAIMeasurement.EStep.Measure 가 per-FAI 누적 write-back. ParamBase INI 직렬화 회피 (transient runtime 결과).
    [System.ComponentModel.Browsable(false)] //260529 hbk Phase 39.1 G4-01
    [PropertyTools.DataAnnotations.Browsable(false)] //260529 hbk Phase 39.1 G4-01
    [Newtonsoft.Json.JsonIgnore] //260529 hbk Phase 39.1 G4-01
    public System.Collections.Generic.List<ReringProject.Halcon.Models.EdgeInspectionOverlay> LastOverlays { get; set; } = new System.Collections.Generic.List<ReringProject.Halcon.Models.EdgeInspectionOverlay>(); //260529 hbk Phase 39.1 G4-01
    ```

    **주의:** FAIConfig.cs 가 이미 `using System.Collections.Generic;` 와 `using ReringProject.Halcon.Models;` 사용 중인지 확인 — 미사용 시 fully-qualified 그대로 사용 (Phase 28 D-01 패턴).

    **B) Action_FAIMeasurement.cs EStep.Measure 에 per-FAI overlay 누적 + write-back 추가:**

    수정 위치 1 — L173 `bool faiAllPass = true;` 다음 라인:
    ```csharp
    bool faiAllPass = true;
    var faiOverlays = new List<EdgeInspectionOverlay>(); //260529 hbk Phase 39.1 G4-01 — per-FAI overlay 누적 (LastOverlays write-back 용)
    ```

    수정 위치 2 — L261 `overlayAcc.AddRange(measOverlays);` 직후:
    ```csharp
    overlayAcc.AddRange(measOverlays); //260422 hbk Phase 7: Shot 단위 누적 (D-04)
    faiOverlays.AddRange(measOverlays); //260529 hbk Phase 39.1 G4-01 — per-FAI 누적 (노드 클릭 재현용)
    ```

    수정 위치 3 — L268-283 `if (fai.Measurements.Count > 0) { ... } else { fai.ClearResult(); }` 블록 안 write-back:
    ```csharp
    if (fai.Measurements.Count > 0) {
        fai.IsPass = faiAllPass;
        fai.MeasuredValue = fai.Measurements[0].LastMeasuredValue;
        // ... WasDatumSkipped 계산 ...
        fai.WasDatumSkipped = wasSkip;
        fai.LastOverlays = faiOverlays; //260529 hbk Phase 39.1 G4-01 — per-FAI overlay 저장
    } else {
        fai.ClearResult();
        if (fai.LastOverlays != null) fai.LastOverlays.Clear(); //260529 hbk Phase 39.1 G4-01 — Measurements 0 케이스 명시적 클리어
    }
    ```

    **Anti-action:**
    - `overlayAcc.AddRange(measOverlays)` (L261) 그대로 유지
    - `pMyContext.InspectionOverlays = overlayAcc;` (L291) 그대로 유지
    - L243-249 측정 성공/실패 분기 변경 0
    - L251-262 suffix 부여 (`-OK`/`-NG`) 분기 변경 0

    **Brace style:** 양 파일 모두 K&R.

    **빌드:** msbuild Debug/x64 PASS.
  </action>
  <verify>
    <automated>
      Grep 검증:
      - `grep -n "LastOverlays" FAIConfig.cs` → 1 hit (필드 선언)
      - `grep -n "LastOverlays" Action_FAIMeasurement.cs` → 최소 2 hit (write-back + clear)
      - `grep -n "faiOverlays" Action_FAIMeasurement.cs` → 최소 3 hit
      - `overlayAcc.AddRange(measOverlays);` (L261) 라인 보존
      - `pMyContext.InspectionOverlays = overlayAcc;` (L291) 라인 보존
      - msbuild Debug/x64 PASS — 0 errors, 0 new warnings
    </automated>
  </verify>
  <done>
    - FAIConfig.LastOverlays 필드 존재 ([Browsable(false)] + [JsonIgnore])
    - Action_FAIMeasurement faiOverlays per-FAI 누적 + write-back
    - 기존 overlayAcc / InspectionOverlays 흐름 변경 0
    - msbuild Debug/x64 PASS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MainView.RenderInspectionResultForNode 신규 메서드 + InspectionListView 분기 호출 추가</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs, WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs L120-200 (Display*Image), L370-404 (HighlightSelectedRoi), L1732-1762 (FindFAIContainingMeasurement)
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs L488-547 (Measurement / FAI 분기)
    - WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs L161 (SetInspectionOverlays API)
    - 39.1-PATTERNS.md §3.2 §3.5
    - Task 1 적용 후의 FAIConfig.LastOverlays 필드 존재 확인
  </read_first>
  <action>
    **A) MainView.xaml.cs 에 RenderInspectionResultForNode + 헬퍼 메서드 추가:**

    `WPF_Example/UI/ContentItem/MainView.xaml.cs` 의 `DisplayMeasurementImage` (L181-189) 직후에 신규 통합 진입점 + 헬퍼 추가:

    ```csharp
    //260529 hbk Phase 39.1 G4-01/G4-02 — 검사 후 FAI/Measurement 노드 클릭 시 측정 결과 + 이미지 + overlay 재현 통합 진입점.
    //  Sequence 동작 변경 0: 측정 재 호출 없이 저장된 overlay 만 재 렌더.
    //  전 FAI 타입 공통 (D-G4-02).
    public void RenderInspectionResultForNode(ParamBase param) //260529 hbk Phase 39.1 G4-01
    {
        if (param == null) return;
        if (param is MeasurementBase meas) {
            DisplayMeasurementImage(meas);
            HighlightSelectedRoi(meas);
            RenderStoredOverlaysForMeasurement(meas);
        }
        else if (param is FAIConfig fai) {
            DisplayFAIImage(fai);
            HighlightSelectedRoi(fai);
            RenderStoredOverlaysForFai(fai);
        }
    }

    //260529 hbk Phase 39.1 G4-01 — FAI 노드 클릭 시 fai.LastOverlays 전체 재 렌더
    private void RenderStoredOverlaysForFai(FAIConfig fai) //260529 hbk Phase 39.1 G4-01
    {
        if (fai == null || fai.LastOverlays == null || fai.LastOverlays.Count == 0) {
            halconViewer.SetInspectionOverlays(new System.Collections.Generic.List<ReringProject.Halcon.Models.EdgeInspectionOverlay>());
            return;
        }
        halconViewer.SetInspectionOverlays(fai.LastOverlays);
    }

    //260529 hbk Phase 39.1 G4-01 — Measurement 노드 클릭 시 소유 FAI 의 LastOverlays 재 렌더 (D-G4-02 전 타입 공통, 타입별 분기 없음)
    private void RenderStoredOverlaysForMeasurement(MeasurementBase meas) //260529 hbk Phase 39.1 G4-01
    {
        if (meas == null) {
            halconViewer.SetInspectionOverlays(new System.Collections.Generic.List<ReringProject.Halcon.Models.EdgeInspectionOverlay>());
            return;
        }
        FAIConfig fai = FindFAIContainingMeasurement(meas); //260528 hbk Phase 37 hotfix A/B
        RenderStoredOverlaysForFai(fai);
    }
    ```

    **B) InspectionListView.xaml.cs Measurement / FAI 분기에서 호출 통합:**

    Measurement 분기 (L517-523) 수정:
    ```csharp
    //260529 hbk Phase 39.1 G4-01 — DisplayMeasurementImage + HighlightSelectedRoi 호출을 RenderInspectionResultForNode 로 통합. overlay 재 렌더 추가.
    if (mParentWindow != null && mParentWindow.mainView != null && itemParam is MeasurementBase meas) //260529 hbk Phase 39.1 G4-01
        mParentWindow.mainView.RenderInspectionResultForNode(meas); //260529 hbk Phase 39.1 G4-01
    ```
    (기존 `DisplayMeasurementImage(meas)` + `HighlightSelectedRoi(itemParam as ParamBase)` 호출 제거 — RenderInspectionResultForNode 안에서 모두 호출.)

    FAI 분기 (L525-547) 수정:
    ```csharp
    else if (item.NodeType == ENodeType.FAI) {
        button_addFAI.IsEnabled = true;
        button_removeFAI.IsEnabled = true;
        button_renameFAI.IsEnabled = true;
        if (_inspectionVm != null && itemParam is FAIConfig faiConfig) {
            _inspectionVm.OnFAISelected(faiConfig);
            //260529 hbk Phase 39.1 G4-01 — DisplayFAIImage + HighlightSelectedRoi 통합 → RenderInspectionResultForNode
            mParentWindow.mainView.RenderInspectionResultForNode(faiConfig); //260529 hbk Phase 39.1 G4-01
            if (ParamEditor != null) {
                _isRebinding = true;
                try {
                    ParamEditor.SelectedObject = null;
                    ParamEditor.SelectedObject = faiConfig;
                } finally {
                    _isRebinding = false;
                }
            }
        }
        //260529 hbk Phase 39.1 G4-01 — HighlightSelectedRoi 가 RenderInspectionResultForNode 안에 통합 → 별도 호출 제거
    }
    ```

    **Anti-action:**
    - Datum / Action / Sequence / else 분기 (L548-602) 변경 0
    - force rebind 패턴 변경 0
    - `_inspectionVm.OnFAISelected/OnActionSelected/ClearResults` 변경 0
    - FAIResults_SelectionChanged 핸들러 (L201) 변경 0
    - FindFAIContainingMeasurement (Phase 37 hotfix A/B) 변경 0

    **Brace style:** K&R 양 파일.

    **빌드:** msbuild Debug/x64 PASS.
  </action>
  <verify>
    <automated>
      Grep 검증:
      - `grep -n "RenderInspectionResultForNode" MainView.xaml.cs` → 1 hit (정의)
      - `grep -n "RenderInspectionResultForNode" InspectionListView.xaml.cs` → 최소 2 hit (Measurement + FAI 분기)
      - `grep -nE "RenderStoredOverlaysForFai|RenderStoredOverlaysForMeasurement" MainView.xaml.cs` → 최소 4 hit
      - `grep -n "halconViewer.SetInspectionOverlays" MainView.xaml.cs` → 최소 2 hit
      - FAIResults_SelectionChanged 핸들러 (L201) grep — 본문 변경 0
      - force rebind 패턴 grep — 변경 0
      - msbuild Debug/x64 PASS
    </automated>
  </verify>
  <done>
    - MainView.RenderInspectionResultForNode 신규 public 메서드 (Measurement + FAI 분기)
    - RenderStoredOverlaysForFai + RenderStoredOverlaysForMeasurement 헬퍼
    - InspectionListView Measurement / FAI 분기에서 호출
    - Datum / Action / Sequence / else 분기 변경 0
    - msbuild Debug/x64 PASS
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Item #3 SIMUL UAT — 검사 후 노드 클릭 결과 재현 (6 FAI 타입 공통)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs, WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs, WPF_Example/UI/ContentItem/MainView.xaml.cs, WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <action>
    사용자 SIMUL 모드에서 6 시나리오 수동 실행 + 결과 보고. 본 task 는 코드 변경 0.
  </action>
  <what-built>
    FAIConfig 에 LastOverlays 신규 필드 + Action_FAIMeasurement per-FAI overlay write-back. MainView 의 RenderInspectionResultForNode 통합 진입점 + 2 헬퍼. InspectionListView 의 Measurement / FAI 분기에서 통합 진입점 호출. Sequence 동작 변경 0.
  </what-built>
  <how-to-verify>
    **SIMUL UAT 6 시나리오 (D-G1-04 sign-off):**

    1. **6 FAI 타입 공통 동작 (PASS 케이스):**
       - CircleDiameter FAI: 검출 원 (FAI-Edge1-OK 녹색)
       - EdgeToLineDistance FAI: 검출 에지 (FAI-Edge1-OK 녹색) + 거리선 (FAI-DistLine 청록)
       - PointToLineDistance FAI: 검출 에지
       - EdgePairDistance FAI: 2 에지 + 거리선
       - EdgeToLineAngle FAI: 2 에지 + 각도 라벨
       - ArcEdgeDistance FAI: 호 + 라인 + 거리선

    2. **NG 케이스 — 적색 overlay:**
       - EdgeToLineDistance NG (공차 초과) → FAI-Edge1-NG (적색) 재현

    3. **FAI vs Measurement 노드 분리:**
       - FAI 노드 클릭 → 해당 FAI 의 모든 measurement overlay 재현
       - Measurement 노드 클릭 → 소유 FAI 전체 overlay 재현 (D-G4-02)
       - 동일 노드 재 클릭 → 동일 재현 (멱등)
       - 다른 FAI 노드 → 이전 overlay 클리어 + 새 overlay

    4. **Datum 노드 클릭 시 overlay 미렌더:**
       - Datum 노드 클릭 → RenderInspectionResultForNode 미호출
       - Datum 의 fitting 원 / origin 십자만 표시 (HalconDisplayService 기존 동작)

    5. **Sequence 회귀 0:**
       - 검사 5 cycle → 측정값 / 판정 / TCP 응답 변경 0 (Phase 39 UAT 재현)
       - grep: `pMyContext.InspectionOverlays = overlayAcc;` + `overlayAcc.AddRange(measOverlays);` 보존
       - Phase 37 D-37-03 lenient 회귀 0
       - Phase 39 D-01 gate 회귀 0

    6. **PropertyGrid + DataGrid 회귀 0:**
       - FAI 노드 클릭 시 PropertyGrid force rebind 정상
       - DataGrid_faiResults SelectionChanged 동작 그대로

    **자동 점검:**
    - msbuild Debug/x64 PASS
    - FAIConfig.LastOverlays / Action_FAIMeasurement faiOverlays + LastOverlays write-back
    - InspectionListView Datum/Action/Sequence/else 분기 변경 0
  </how-to-verify>
  <verify>
    사용자 시나리오 1~6 PASS + grep 검증.
  </verify>
  <done>
    사용자가 "approved" 또는 시나리오 1~6 모두 PASS 보고.
  </done>
  <resume-signal>
    사용자 "approved" 시 진행. overlay 재현 실패 / NG 색상 회귀 / Sequence 회귀 / PropertyGrid stale 발생 시 (CO 신설) 보고.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Action_FAIMeasurement ↔ FAIConfig.LastOverlays | sequence 스레드가 검사 시점에 write, UI 스레드 (InspectionListView click) 가 read. 검사 종료 후 클릭 정상 워크플로우 가정 |
| FAIConfig.LastOverlays ↔ INI 직렬화 | [JsonIgnore] + [Browsable(false)] 로 INI 회피 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-03-01 | D (DoS) | LastOverlays 메모리 누적 | accept | overlay 객체 light-weight. cycle 마다 새 List → GC 회수 |
| T-39.1-03-02 | T (Tampering) | 검사 중 노드 클릭 시 race | accept | 정상 워크플로우 = 종료 후 클릭. reference assignment atomic, worst case = 이전 cycle overlay 표시 |
| T-39.1-03-03 | I (Info Disclosure) | overlay 좌표/RoiId 노출 | accept | 같은 호스트 / 같은 프로세스. UI 표시만 |
</threat_model>

<verification>
**자동 검증:**
- [ ] msbuild Debug/x64 PASS
- [ ] FAIConfig.LastOverlays 필드 존재
- [ ] Action_FAIMeasurement.cs faiOverlays + LastOverlays write-back grep
- [ ] MainView.RenderInspectionResultForNode + 2 헬퍼 grep
- [ ] InspectionListView Measurement / FAI 분기 호출 grep
- [ ] 기존 overlayAcc / pMyContext.InspectionOverlays 보존
- [ ] FAIResults_SelectionChanged (L201) 변경 0

**Anti-goal:**
- [ ] Sequence 동작 (TryExecute / 측정 / overlayAcc → InspectionOverlays) 변경 0
- [ ] Phase 37 D-37-03 lenient 변경 0
- [ ] Phase 39 D-01 gate 변경 0
- [ ] FindFAIContainingMeasurement 변경 0
- [ ] InspectionListView Datum/Action/Sequence/else 변경 0

**SIMUL UAT:**
- [ ] 6 시나리오 모두 PASS
</verification>

<success_criteria>
**M3:**
- FAIConfig.LastOverlays 필드 추가 ✓
- per-FAI overlay 누적 + write-back ✓
- MainView.RenderInspectionResultForNode 통합 진입점 ✓
- 6 FAI 타입 공통 (D-G4-02) ✓

**M5 (Phase 37 / 39 anti-goal):**
- Sequence 동작 변경 0 ✓
- Phase 37 D-37-03 lenient 회귀 0 ✓
- Phase 39 D-01 gate 회귀 0 ✓
- FindFAIContainingMeasurement 변경 0 ✓
</success_criteria>

<output>
After completion, create `.planning/phases/39.1-inspection-urgent-fixes-2026-05-29/39.1-03-SUMMARY.md`.

Include:
- Tasks executed (Task 1 + Task 2)
- 4 파일 수정 그래프
- msbuild Debug/x64 PASS 결과
- Grep 검증 결과
- SIMUL UAT 6 시나리오 결과
- Phase 37 lenient + Phase 39 gate 회귀 0 증거
- Sequence 5 cycle 측정값 회귀 0 증거
</output>
