---
phase: 39.1-inspection-urgent-fixes-2026-05-29
plan: 03
type: summary
status: completed
mode: inline (orchestrator 직접 실행 — gsd-executor 2회 연속 실패 후 사용자 결정)
date: 2026-05-29
commits:
  - 7c1ef70 — feat(39.1-03): FAIConfig.LastOverlays + per-FAI overlay write-back (Task 1)
  - b1ecc17 — feat(39.1-03): InspectionListView 노드 클릭 결과 재현 통합 진입점 (Task 2)
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs   # 스코프 초과 (불가피, 아래 deviation 참조)
build_result: PASS (msbuild Debug/x64, 0 new errors, 0 new warnings — Phase 33 baseline 만 유지)
---

# Plan 39.1-03 SUMMARY

## Tasks executed

### Task 1 — FAIConfig.LastOverlays + Action_FAIMeasurement per-FAI overlay write-back
Atomic commit **7c1ef70**.

**FAIConfig.cs (+7 라인):**
- `WasDatumSkipped` (L107) 직후 `List<EdgeInspectionOverlay> LastOverlays` 추가.
- `[System.ComponentModel.Browsable(false)]` + `[PropertyTools.DataAnnotations.Browsable(false)]` + `[Newtonsoft.Json.JsonIgnore]` 3 attribute 로 INI/JSON 직렬화 회피.
- 기본값 `new List<EdgeInspectionOverlay>()` (null-safety).

**Action_FAIMeasurement.cs (+4 라인):**
- `EStep.Measure` foreach(fai) 진입부 `bool faiAllPass = true;` 뒤에 `var faiOverlays = new List<EdgeInspectionOverlay>();` (per-FAI 누적 컨테이너).
- `overlayAcc.AddRange(measOverlays);` (L261) 직후 `faiOverlays.AddRange(measOverlays);` (per-FAI 누적).
- `fai.WasDatumSkipped = wasSkip;` 직후 `fai.LastOverlays = faiOverlays;` (Measurements.Count > 0 분기 write-back).
- `else { fai.ClearResult(); }` 분기에 `if (fai.LastOverlays != null) fai.LastOverlays.Clear();` (Measurements.Count == 0 명시적 클리어).
- 기존 `overlayAcc` / `pMyContext.InspectionOverlays = overlayAcc;` (L291) 흐름 변경 0.

### Task 2 — MainView.RenderInspectionResultForNode + 헬퍼 + InspectionListView 호출 통합 + MainResultViewerControl.SetInspectionOverlays wrapper
Atomic commit **b1ecc17**.

**MainView.xaml.cs (+35 라인):**
- `RenderInspectionResultForNode(ParamBase param)` 신규 public 통합 진입점:
  - `MeasurementBase` → `DisplayMeasurementImage` → `HighlightSelectedRoi` → `RenderStoredOverlaysForMeasurement`.
  - `FAIConfig` → `DisplayFAIImage` → `HighlightSelectedRoi` → `RenderStoredOverlaysForFai`.
- `RenderStoredOverlaysForFai(FAIConfig)` 헬퍼 (null/빈 케이스 빈 List 호출 → REPLACE 의미로 prior overlay 안전 클리어).
- `RenderStoredOverlaysForMeasurement(MeasurementBase)` 헬퍼 → 소유 FAI 객체 참조 해석 (FindFAIContainingMeasurement, Phase 37 hotfix A/B) → RenderStoredOverlaysForFai 위임 (D-G4-02 전 타입 공통).

**InspectionListView.xaml.cs (변경 −8 / +4):**
- Measurement 분기 (L519-523): `DisplayMeasurementImage(meas)` + 외부 `HighlightSelectedRoi(itemParam as ParamBase)` 호출 두 줄을 `RenderInspectionResultForNode(meas)` 단일 호출로 교체.
- FAI 분기 (L531/L544-546): `DisplayFAIImage(faiConfig)` 호출 위치 `RenderInspectionResultForNode(faiConfig)` 로 교체 + 외부 `HighlightSelectedRoi` 호출 제거 (통합 진입점이 호출).
- PropertyGrid force rebind 패턴 (L507-515, L534-542) 변경 0.
- Datum (L484-487) / Action (L548-570) / Sequence (L571-587) / else (L588-603) 분기 변경 0.

**MainResultViewerControl.xaml.cs (+13 라인, 스코프 초과 deviation):**
- `SetInspectionOverlays(IEnumerable<EdgeInspectionOverlay>)` thin wrapper 추가 — `_inspectionOverlays.Clear() + AddRange + Render()`.
- 기존 `UpdateDisplayState` 가 `_rois` 도 함께 Clear 하므로 HighlightSelectedRoi 직후에 ROI 보존 + overlay 만 갱신 필요. 신규 메서드는 HalconViewerControl L161 의 동명 메서드와 동일 REPLACE 패턴.

## Deviation

- **스코프 초과 — MainResultViewerControl.xaml.cs**: 본래 Plan 03 files_modified 에 미포함. 그러나 plan key_links 에 `halconViewer.SetInspectionOverlays` 호출 패턴이 명시되어 있었고 (`MainView.RenderInspectionResultForNode → halconViewer.SetInspectionOverlays + UpdateDisplayState`), `halconViewer` 는 `MainResultViewerControl` 인스턴스인데 `MainResultViewerControl` 에 `SetInspectionOverlays` 메서드 부재 → 빌드 CS1061 3건. 회피 옵션 분석:
  - Option A (선택): MainResultViewerControl 에 thin wrapper 신규 추가 (+13 라인, 추가만, 기존 API 변경 0). HalconViewerControl 동명 메서드 패턴과 byte-equivalent.
  - Option B (미선택): UpdateDisplayState(GetCurrentFAIRois(), overlays, null) 호출 — 매 호출마다 ROI 재 수집/Clone 발생, _selectedRoiId 매 호출마다 null 로 clobber 가능.
- **Wave 2 worktree 실패 → inline 실행 전환**: Wave 2 의 첫 번째 gsd-executor (a703884932dc569f1) 가 worktree base 오류 (v1.0 milestone 시점 5727bd8 분기), 두 번째 (a0e1bcb1737b2ff68) 가 PreToolUse Edit hook 반복 차단. 사용자 결정 (Inline 직접 실행) 으로 orchestrator 가 main repo 에 직접 4 atomic edit + 2 commit + msbuild + SUMMARY 진행.

## 자동 검증 (verify)

| 항목 | 결과 |
|------|------|
| msbuild Debug/x64 | **PASS** (0 errors, 0 new warnings — Phase 33 baseline CS0618×5 + CS0162×1 + MSB3884 만 유지) |
| `grep -n LastOverlays FAIConfig.cs` | 1 hit (L109 declaration) — 통과 |
| `grep -n LastOverlays Action_FAIMeasurement.cs` | 2 hit (write-back + Clear) — 통과 |
| `grep -n faiOverlays Action_FAIMeasurement.cs` | 3 hit (선언 + AddRange + write-back) — 통과 |
| `overlayAcc.AddRange(measOverlays);` 보존 | 통과 |
| `pMyContext.InspectionOverlays = overlayAcc;` 보존 | 통과 |
| `grep -n RenderInspectionResultForNode MainView.xaml.cs` | 1 hit (정의) — 통과 |
| `grep -n RenderInspectionResultForNode InspectionListView.xaml.cs` | 2 hit (Measurement + FAI 분기) — 통과 |
| `grep -nE RenderStoredOverlaysForFai|RenderStoredOverlaysForMeasurement MainView.xaml.cs` | 4 hit (정의 2 + 호출 2) — 통과 |
| `grep -n halconViewer.SetInspectionOverlays MainView.xaml.cs` | 3 hit (RenderStoredOverlaysForFai null-fallback, success, RenderStoredOverlaysForMeasurement null-fallback) — 통과 |
| FAIResults_SelectionChanged (L201) 본문 변경 0 | 통과 (변경 없음, force rebind 무관) |
| Datum/Action/Sequence/else 분기 변경 0 | 통과 (L484-487 / 548-587 / 588-603 무수정) |
| force rebind 패턴 (L507-515, L534-542) 변경 0 | 통과 (PropertyGrid stale 회귀 0) |
| FindFAIContainingMeasurement (Phase 37 hotfix A/B) 변경 0 | 통과 (RenderStoredOverlaysForMeasurement 가 호출만, 정의 무변경) |
| HalconViewerControl.cs SetInspectionOverlays 변경 0 | 통과 (L161-170 무변경) |

### I2 — INI/JSON 직렬화 무영향
- ParamBase.Save() reflection 경로 가 LastOverlays 필드를 인식하더라도 `[Browsable(false)]` 2종 + 타입이 `List<EdgeInspectionOverlay>` (ParamBase switch-case 미지원 타입) → INI 키 미작성. Setting.ini 로드/저장 후 `LastOverlays` 키 미존재 (직렬화 차단 다중 안전망 — Phase 5 D-04 패턴).
- Newtonsoft.Json 직렬화 시 `[JsonIgnore]` → JSON 미포함.

### W2 — SetInspectionOverlays 캐시 동작
- HalconViewerControl.SetInspectionOverlays (L161-170): `_inspectionOverlays.Clear()` 직후 `AddRange(overlays.Select(Clone))` → REPLACE 의미.
- 새로 추가한 MainResultViewerControl.SetInspectionOverlays (L268-279): 동일 패턴.
- 빈 List 호출 → `_inspectionOverlays.Clear()` 만 실행 → prior overlay 안전 클리어.

## SIMUL UAT 6 시나리오 (D-G1-04 sign-off — 사용자 검증 대기)

- [ ] **Scenario 1** — 6 FAI 타입 공통 PASS overlay (CircleDiameter / EdgeToLineDistance / PointToLineDistance / EdgePairDistance / EdgeToLineAngle / ArcEdgeDistance)
- [ ] **Scenario 2** — NG (적색) overlay 재현 (EdgeToLineDistance 공차 초과)
- [ ] **Scenario 3** — FAI vs Measurement 노드 분리 + 멱등 + 다른 FAI 클리어
- [ ] **Scenario 4** — Datum 노드 클릭 시 overlay 미렌더 (HalconDisplayService 기존 동작 유지)
- [ ] **Scenario 5** — Sequence 5 cycle 회귀 0 (Phase 39 UAT 재현 — 측정값/판정/TCP 응답 변경 0)
- [ ] **Scenario 6** — PropertyGrid + DataGrid 회귀 0 (force rebind / FAIResults_SelectionChanged 정상)

검증 절차: 사용자 SIMUL 모드 실행 후 위 6 시나리오 결과 보고 → "approved" 또는 회귀 발견 시 (CO 신설).

## Phase 37 / 39 anti-goal 충족 증거

- **Phase 37 D-37-03 lenient (datum find 실패 = continue + log, 항상 true)**: TryRunSingleDatum / TryGetDatumTransform 무수정, EStep.DatumPhase 무수정, L119 Step=Grab 무수정. EStep.Measure D-01 게이트 (`IsDatumFailed`) 무수정 — Plan 03 변경은 모두 게이트 이후 overlay 누적 로직.
- **Phase 39 D-01 gate (_failedDatums + IsDatumFailed)**: InspectionSequence 무수정. L178 `IsDatumFailed(meas.DatumRef)` skip 분기 무변경. 본 plan 의 faiOverlays 누적은 skip 분기 이후 정상 측정 분기 (`overlayAcc.AddRange` 직후) 에서만 발생 → skip 된 measurement 의 overlay 는 누적되지 않음 (기존 의미 동일).
- **FindFAIContainingMeasurement (Phase 37 hotfix A/B)**: 호출만, 정의 변경 0. RenderStoredOverlaysForMeasurement 가 활용.

## Sequence 회귀 0 증거 (코드 인스펙션)

1. `overlayAcc.AddRange(measOverlays)` (L261) 보존 → Shot 단위 누적 unchanged.
2. `pMyContext.InspectionOverlays = overlayAcc;` (L291) 보존 → Sequence 종료 시 Action overlay 흐름 unchanged.
3. `fai.IsPass = faiAllPass; fai.MeasuredValue = ...` (L270-272) 보존 → TCP 응답 결정 인자 unchanged.
4. `fai.WasDatumSkipped = wasSkip;` (L280) 보존 → AddResponse NotExist 매핑 unchanged.
5. `fai.ClearResult();` (L282) 보존 → Measurements.Count == 0 케이스 unchanged.
6. Try/Catch 측정 흐름 (L235-249) + suffix 부여 (L251-260) 보존.
7. 본 plan 추가 라인은 모두 ADDITIVE (faiOverlays 선언 + AddRange + 분기별 LastOverlays write-back/Clear) — 측정/판정/누적/응답 결정에 영향 0.

→ Sequence 5 cycle UAT 는 SIMUL UAT Scenario 5 에서 실측 확인 예정 (사용자 검증).
