---
phase: 39.1-inspection-urgent-fixes-2026-05-29
plan: 04
type: execute
wave: 3
depends_on: ["39.1-03"]
files_modified:
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
autonomous: false
requirements: [WF-01]
tags: [ui, datum-cth, edit-mode, halcon-overlay, render-datum]
user_setup: []

# 주: Wave 3 로 분리 — MainView.xaml.cs / InspectionListView.xaml.cs 가 Plan 03 의 files_modified 와 겹침.
#     CONTEXT.md D-G1-03 의 "Wave 2 = Plan 03 + 04" 의도는 file overlap 으로 무산. Sequential 의존 도입.

must_haves:
  truths:
    - "Datum CTH 가 LastTeachSucceeded==true 인 상태에서 btn_teachDatum.IsChecked==false (평소) → fitting 원 + DetectedOrigin 십자 만 표시"
    - "Datum CTH 가 LastTeachSucceeded==true + btn_teachDatum.IsChecked==true (Edit 모드) → fitting 원 + Horizontal_A/B + Circle ROI 사각형 핸들 동시 표시"
    - "Datum CTH 가 LastTeachSucceeded==false 인 상태 → 기존 동작 (Horizontal_A/B + Circle ROI 사각형 핸들, 티칭 미완료)"
    - "DETECT FAIL 라벨 분기 (Phase 39 hotfix CO-39-02/03) 변경 0"
    - "Datum CTH 외 algorithm (TwoLineIntersect / VerticalTwoHorizontal / VerticalTwoHorizontalDualImage) 동작 변경 0"
  artifacts:
    - path: "WPF_Example/Halcon/Display/HalconDisplayService.cs"
      provides: "RenderDatumOverlay 시그니처에 isEditMode 옵션 인자 + cthHideRois 가드 분기"
      contains: "bool isEditMode = false, cthHideRois"
    - path: "WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs"
      provides: "SetDatumOverlay 시그니처 + _datumIsEditMode 필드 + RenderDatumOverlay 호출 isEditMode 전달"
      contains: "SetDatumOverlay(DatumConfig datum, bool isSelected, bool isEditMode = false), _datumIsEditMode"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "btn_teachDatum.IsChecked 기반 isEditMode 계산 + SetDatumOverlay 11 호출처 인자 전달"
      contains: "isEditMode = (btn_teachDatum != null && btn_teachDatum.IsChecked == true)"
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "Datum 노드 선택 시 SetDatumOverlay 2 호출처 isEditMode 인자 전달"
  key_links:
    - from: "MainView.btn_teachDatum.IsChecked toggle"
      to: "HalconDisplayService.RenderDatumOverlay isEditMode 분기"
      via: "MainResultViewerControl.SetDatumOverlay 시그니처에 옵션 인자 + RenderDatumOverlay 까지 propagate"
      pattern: "isEditMode"
    - from: "HalconDisplayService.RenderDatumOverlay cthHideRois 가드"
      to: "L755-770 Circle ROI 그리기 + L774-797 Horizontal_A/B 그리기"
      via: "cthHideRois==true 시 두 분기 skip (CircleTwoHorizontal + LastTeachSucceeded + !isEditMode)"
      pattern: "cthHideRois|!cthHideRois"
---

<objective>
Datum CircleTwoHorizontal (CTH) 의 Edit 모드를 btn_teachDatum 토글 기반으로 분리한다.
현재는 LastTeachSucceeded==true 인 CTH datum 노드 선택 시 fitting 원 + ROI 사각형 핸들이 항상 동시 표시.
사용자가 평소에는 fitting 원만, btn_teachDatum 토글 ON 시에만 ROI 핸들 표시 → UX 명확화.

Purpose: 사용자 보고 "CTH Datum 원이 계속 Edit 모드처럼 보임" — fitting 원이 ROI 사각형 핸들과 동시 표시되어 시각적 혼란.
Output: 4 파일 수정 — HalconDisplayService.cs (RenderDatumOverlay 분기), MainResultViewerControl.xaml.cs (SetDatumOverlay 시그니처), MainView.xaml.cs (11 호출처 인자 전달), InspectionListView.xaml.cs (2 호출처 인자 전달).

**Anti-goal (변경 0, 회귀 0):**
- HalconDisplayService.RenderDatumOverlay L822-884 (LastTeachSucceeded 검출 라인 + fitting 원 + raw points + center cross) 변경 0 — Phase 11/13/16
- HalconDisplayService.RenderDatumOverlay L886 RenderDatumFindResult 호출 변경 0 — Phase 36 CO-36-03
- HalconDisplayService.RenderDatumOverlay L891-919 DETECT FAIL 라벨 분기 변경 0 — Phase 39 hotfix CO-39-02/03
- HalconDisplayService.RenderDatumOverlay TwoLineIntersect 분기 (L713-724) 변경 0
- HalconDisplayService.RenderDatumOverlay VerticalTwoHorizontal / VTH-DualImage 분기 (L726-738) 변경 0
- HalconDisplayService.RenderDatumOverlay TwoLineIntersect Line2 분기 (L742-752) 변경 0
- HalconDisplayService.RenderDatumOverlay RefOrigin magenta 십자 (L800-819) 변경 0
- DatumFindingService.cs 변경 0
- DatumConfig.cs 변경 0
- project_roi_edit_mode_deferred 3 항목 통합 X (D-G4-04)
- Plan 03 의 RenderInspectionResultForNode 변경 0 — Datum 노드 미호출
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-inspection-urgent-fixes-2026-05-29/39.1-CONTEXT.md
@.planning/phases/39.1-inspection-urgent-fixes-2026-05-29/39.1-PATTERNS.md
@WPF_Example/Halcon/Display/HalconDisplayService.cs
@WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
@WPF_Example/UI/ContentItem/MainView.xaml.cs
@WPF_Example/UI/ControlItem/InspectionListView.xaml.cs

<interfaces>
HalconDisplayService.RenderDatumOverlay 현재 시그니처 (L683):
```csharp
public void RenderDatumOverlay(HWindow window, DatumConfig datum, bool isSelected)
```

MainResultViewerControl.SetDatumOverlay 현재 시그니처 (L595):
```csharp
public void SetDatumOverlay(DatumConfig datum, bool isSelected) {
    _datumConfig = datum;
    _datumSelected = isSelected;
    Render();
}
```

MainResultViewerControl.RenderNow 의 RenderDatumOverlay 호출 (L726-729):
```csharp
if (_datumConfig != null) {
    _displayService.RenderDatumOverlay(ViewerHost.HalconWindow, _datumConfig, _datumSelected);
}
```

HalconDisplayService.RenderDatumOverlay L755-770 (CTH Circle ROI 그리기 — 가드 대상):
```csharp
if (datum.AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal
    && datum.CircleROI_Radius > 0)
{
    HOperatorSet.SetColor(window, color);
    HOperatorSet.SetLineWidth(window, lineWidth);
    HOperatorSet.DispCircle(window, datum.CircleROI_Row, datum.CircleROI_Col, datum.CircleROI_Radius);
    DrawRoiLabelAt(window, /* ... */, "Circle");
    RenderCircleStripOverlay(window, datum);
}
```

HalconDisplayService.RenderDatumOverlay L773-798 (Horizontal_A/B 그리기 — CTH+VTH 공용, 가드 대상):
```csharp
if (datum.AlgorithmTypeEnum != EDatumAlgorithm.TwoLineIntersect)
{
    HOperatorSet.SetColor(window, color);
    HOperatorSet.SetLineWidth(window, lineWidth);
    if (datum.Horizontal_A_Length1 > 0 && datum.Horizontal_A_Length2 > 0) {
        HOperatorSet.DispRectangle2(/* ... */);
        DrawRoiLabel(/* ... */, "H-A");
    }
    if (datum.Horizontal_B_Length1 > 0 && datum.Horizontal_B_Length2 > 0) {
        HOperatorSet.DispRectangle2(/* ... */);
        DrawRoiLabel(/* ... */, "H-B");
    }
}
```

HalconDisplayService.RenderDatumOverlay L822-884 (LastTeachSucceeded 분기 — **변경 0**, fitting 원 + raw points 그대로):
```csharp
if (datum.LastTeachSucceeded) {
    // Line1Detected / Line2Detected 외삽 — TLI 만 의미 있음
    // Intersection cross (red)
    // raw edge points (cyan/magenta/gray/green/lime green/orange)
    if (datum.AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal
        && datum.CircleDetected_Radius > 0) {
        HOperatorSet.SetColor(window, "#90EE90"); // light green
        HOperatorSet.DispCircle(window, datum.CircleCenter_Row, datum.CircleCenter_Col, datum.CircleDetected_Radius);
        // yellow center cross size=12
    }
}
```

HalconDisplayService.RenderDatumOverlay L886-919 (RenderDatumFindResult + DETECT FAIL 라벨 — **변경 0**):
```csharp
RenderDatumFindResult(window, datum); // L889 Phase 36 CO-36-03
if (datum.RuntimeDetectFailed || (datum.IsConfigured && !datum.LastFindSucceeded)) {
    try {
        EnsureFontInitialized(window);
        HOperatorSet.SetColor(window, "red");
        // GetPart + hash stagger + "DETECT FAIL: " + datum.DatumName
    } catch { /* suppress */ }
}
```

MainView.xaml.cs SetDatumOverlay 11 호출처 grep:
- L97 (`_editingDatum, false` — 모드 해제 시)
- L831, L930, L962, L1139 (`datum, true`)
- L1254, L1270 (`_selectedDatumForSwap, true`) — Phase 36 swap
- L2313, L2331, L2408 (`_editingDatum, true`)
- L2498 (`datum, true`)

InspectionListView.xaml.cs SetDatumOverlay 2 호출처 grep:
- L135 (Datum 노드 선택 시)
- L459 (Datum 노드 선택 시 다른 분기)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: HalconDisplayService.RenderDatumOverlay 에 isEditMode 옵션 인자 + cthHideRois 가드 분기 추가</name>
  <files>WPF_Example/Halcon/Display/HalconDisplayService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Display/HalconDisplayService.cs L683-925 (RenderDatumOverlay 전체)
    - 39.1-PATTERNS.md §4.1 (RenderDatumOverlay pattern + critical preserve blocks L822-884 / L886-919)
    - 39.1-PATTERNS.md "Shared Patterns" HALCON SetColor 표준명 / Try/Catch Swallow Convention
    - memory feedback_halcon_setcolor_invalid_names — "light green" → hex "#90EE90" 보존
  </read_first>
  <action>
    `WPF_Example/Halcon/Display/HalconDisplayService.cs` 의 `RenderDatumOverlay` 메서드를 다음과 같이 수정한다 (per D-G4-05).

    **A) 시그니처 수정 (L683) — isEditMode 옵션 인자 추가:**
    ```csharp
    //260529 hbk Phase 39.1 G4-03/G4-05 — Datum CTH Edit 모드 분리: isEditMode 인자 추가. 기본값 false 로 기존 호출자 호환.
    public void RenderDatumOverlay(HWindow window, DatumConfig datum, bool isSelected, bool isEditMode = false) //260529 hbk Phase 39.1 G4-03/G4-05
    ```

    **B) cthHideRois 가드 변수 도입 (try 블록 진입 후, L701-704 SetColor/SetLineWidth 다음, L706 첫 분기 직전):**
    ```csharp
    try
    {
        HOperatorSet.SetColor(window, color);
        HOperatorSet.SetLineWidth(window, lineWidth);

        //260529 hbk Phase 39.1 G4-05 — CTH 평소 모드 (LastTeachSucceeded + !isEditMode) 시 Horizontal_A/B + Circle ROI 사각형 핸들 hide.
        //  fitting 원 + DetectedOrigin 십자는 L822-884 LastTeachSucceeded 블록 + L800-819 RefOrigin 블록에서 별도 그림 — 본 가드 영향 없음.
        //  TwoLineIntersect / VerticalTwoHorizontal 등 다른 algorithm 에는 영향 0 (CircleTwoHorizontal 한정).
        bool cthHideRois = (datum.AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal) //260529 hbk Phase 39.1 G4-05
                        && datum.LastTeachSucceeded //260529 hbk Phase 39.1 G4-05
                        && !isEditMode; //260529 hbk Phase 39.1 G4-05

        // 기존 L706 W4-A 후속 분기 (TwoLineIntersect / VTH) 시작...
    ```

    **C) Circle ROI 그리기 분기 (L755-770) 에 가드 추가 — 기존 if 조건에 `&& !cthHideRois` 추가:**
    ```csharp
    //260424 hbk Phase 12 D-10 — Circle ROI 검색 영역 (CircleTwoHorizontal 일 때만 렌더)
    //260529 hbk Phase 39.1 G4-05 — cthHideRois 가드: Edit 모드 OFF + 티칭 완료 시 Circle ROI + Strip 시각화 hide
    if (datum.AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal
        && datum.CircleROI_Radius > 0
        && !cthHideRois) //260529 hbk Phase 39.1 G4-05
    {
        HOperatorSet.SetColor(window, color);
        HOperatorSet.SetLineWidth(window, lineWidth);
        HOperatorSet.DispCircle(window, datum.CircleROI_Row, datum.CircleROI_Col, datum.CircleROI_Radius);
        DrawRoiLabelAt(window, datum.CircleROI_Row - datum.CircleROI_Radius - 22, datum.CircleROI_Col - datum.CircleROI_Radius, "Circle");
        RenderCircleStripOverlay(window, datum);
    }
    ```

    **D) Horizontal_A/B 그리기 분기 (L773-798) 에 가드 추가:**
    ```csharp
    //260424 hbk Phase 12 D-11 — Horizontal A/B ROI Rectangle2 (CircleTwoHorizontal + VerticalTwoHorizontal 공용)
    //260529 hbk Phase 39.1 G4-05 — cthHideRois 가드: CTH Edit 모드 OFF + 티칭 완료 시 Horizontal_A/B hide.
    //  VTH 는 cthHideRois 가 false (AlgorithmTypeEnum != CircleTwoHorizontal) → 영향 0
    if (datum.AlgorithmTypeEnum != EDatumAlgorithm.TwoLineIntersect
        && !cthHideRois) //260529 hbk Phase 39.1 G4-05
    {
        HOperatorSet.SetColor(window, color);
        HOperatorSet.SetLineWidth(window, lineWidth);
        if (datum.Horizontal_A_Length1 > 0 && datum.Horizontal_A_Length2 > 0)
        {
            HOperatorSet.DispRectangle2(window, datum.Horizontal_A_Row, datum.Horizontal_A_Col, datum.Horizontal_A_Phi, datum.Horizontal_A_Length1, datum.Horizontal_A_Length2);
            DrawRoiLabel(window, datum.Horizontal_A_Row, datum.Horizontal_A_Col, datum.Horizontal_A_Phi, datum.Horizontal_A_Length1, datum.Horizontal_A_Length2, "H-A");
        }
        if (datum.Horizontal_B_Length1 > 0 && datum.Horizontal_B_Length2 > 0)
        {
            HOperatorSet.DispRectangle2(window, datum.Horizontal_B_Row, datum.Horizontal_B_Col, datum.Horizontal_B_Phi, datum.Horizontal_B_Length1, datum.Horizontal_B_Length2);
            DrawRoiLabel(window, datum.Horizontal_B_Row, datum.Horizontal_B_Col, datum.Horizontal_B_Phi, datum.Horizontal_B_Length1, datum.Horizontal_B_Length2, "H-B");
        }
    }
    ```

    **Anti-action (verbatim preserve):**
    - L713-724 TwoLineIntersect Line1 분기 (변경 0)
    - L726-738 VerticalTwoHorizontal / VTH-DualImage Vertical 분기 (변경 0)
    - L742-752 TwoLineIntersect Line2 분기 (변경 0)
    - L800-819 RefOrigin magenta 십자 + IsConfigured 분기 (변경 0)
    - L822-884 LastTeachSucceeded 분기 (fitting 원 hex "#90EE90" + yellow center cross + raw points) 변경 0
    - L886 RenderDatumFindResult 호출 (Phase 36 CO-36-03)
    - L891-919 DETECT FAIL 라벨 분기 (Phase 39 hotfix CO-39-02/03)
    - L921-924 외곽 try/catch swallow

    **Brace style (Allman):** HalconDisplayService.cs Allman style.

    **HALCON SetColor 표준명:** 신규 SetColor 호출 0 — 모든 색상은 기존 분기에서 처리 (color 변수 / "yellow" / "#90EE90" / "red"). 비표준 색상명 도입 0 (memory feedback).

    **빌드:** msbuild Debug/x64 PASS.
  </action>
  <verify>
    <automated>
      Grep 검증:
      - 시그니처: `grep -n "public void RenderDatumOverlay" HalconDisplayService.cs` → `bool isEditMode = false` 포함
      - cthHideRois: `grep -n "cthHideRois" HalconDisplayService.cs` → 최소 3 hit
      - Circle ROI 가드: `&& !cthHideRois` 포함 라인 1 hit (CircleTwoHorizontal 분기)
      - Horizontal A/B 가드: `&& !cthHideRois` 포함 라인 1 hit (Horizontal 분기)
      - L822 LastTeachSucceeded 보존: `if (datum.LastTeachSucceeded)` grep
      - L867 hex 색상 보존: `grep -n "#90EE90" HalconDisplayService.cs` → 1 hit
      - L889 RenderDatumFindResult 보존
      - DETECT FAIL 분기 보존: `RuntimeDetectFailed` grep → 최소 1 hit
      - msbuild Debug/x64 PASS
    </automated>
  </verify>
  <done>
    - RenderDatumOverlay 시그니처에 `bool isEditMode = false`
    - cthHideRois 가드 변수 (CircleTwoHorizontal && LastTeachSucceeded && !isEditMode)
    - Circle ROI / Horizontal_A/B 분기에 `&& !cthHideRois` 가드
    - L822-884 / L886 / L891-919 보존
    - TwoLineIntersect / VTH / VTH-DualImage 변경 0
    - msbuild Debug/x64 PASS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SetDatumOverlay 시그니처 + RenderDatumOverlay 호출 isEditMode 인자 전달 (MainResultViewerControl + MainView + InspectionListView)</name>
  <files>WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs, WPF_Example/UI/ContentItem/MainView.xaml.cs, WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs L592-621 (SetDatumOverlay + ClearDatumOverlay + RenderNow)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs SetDatumOverlay 11 호출처 (interfaces 섹션 grep 결과)
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs L135, L459 (Datum 노드 선택)
    - Task 1 적용 후 HalconDisplayService.RenderDatumOverlay 시그니처 확인
  </read_first>
  <action>
    **A) MainResultViewerControl.xaml.cs SetDatumOverlay 시그니처 + RenderDatumOverlay 호출 수정:**

    클래스 본문 위쪽 (`_datumFindResultOverlay` 근처) 에 신규 필드 추가:
    ```csharp
    //260529 hbk Phase 39.1 G4-03 — Datum CTH Edit 모드 트리거. btn_teachDatum.IsChecked 기반 호출자가 전달.
    private bool _datumIsEditMode = false; //260529 hbk Phase 39.1 G4-03
    ```

    SetDatumOverlay 시그니처 변경 (L595):
    ```csharp
    //260410 hbk Phase 4 gap fix: set Datum for overlay rendering
    //260529 hbk Phase 39.1 G4-03 — isEditMode 옵션 인자 추가 (기본 false). btn_teachDatum.IsChecked 기반 전달.
    public void SetDatumOverlay(DatumConfig datum, bool isSelected, bool isEditMode = false) //260529 hbk Phase 39.1 G4-03
    {
        _datumConfig = datum;
        _datumSelected = isSelected;
        _datumIsEditMode = isEditMode; //260529 hbk Phase 39.1 G4-03
        Render();
    }
    ```

    ClearDatumOverlay (L603) 에 리셋 추가:
    ```csharp
    public void ClearDatumOverlay()
    {
        _datumConfig = null;
        _datumSelected = false;
        _datumIsEditMode = false; //260529 hbk Phase 39.1 G4-03 — Edit 모드 리셋
    }
    ```

    RenderNow 의 RenderDatumOverlay 호출 (L726-729) 수정:
    ```csharp
    //260410 hbk Phase 4 gap fix: render Datum Line ROI overlay
    //260529 hbk Phase 39.1 G4-03 — isEditMode 인자 전달
    if (_datumConfig != null)
    {
        _displayService.RenderDatumOverlay(ViewerHost.HalconWindow, _datumConfig, _datumSelected, _datumIsEditMode); //260529 hbk Phase 39.1 G4-03
    }
    ```

    **B) MainView.xaml.cs SetDatumOverlay 11 호출처에 isEditMode 인자 전달:**

    공통 인자 결정 규칙:
    ```csharp
    bool isEditMode = (btn_teachDatum != null && btn_teachDatum.IsChecked == true); //260529 hbk Phase 39.1 G4-03
    ```

    호출처별 변경:

    L97 (`_editingDatum, false` — 모드 해제):
    ```csharp
    halconViewer.SetDatumOverlay(_editingDatum, false, false); //260505 hbk Phase 18 CO-04 //260529 hbk Phase 39.1 G4-03 — 모드 해제 → isEditMode=false 명시
    ```

    L831/L930/L962/L1139/L2313/L2331/L2408/L2498 (`datum, true` 또는 `_editingDatum, true`):
    각 호출처에 isEditMode 계산 라인 + 3-인자 호출 형태로 변경:
    ```csharp
    bool isEditMode = (btn_teachDatum != null && btn_teachDatum.IsChecked == true); //260529 hbk Phase 39.1 G4-03
    halconViewer.SetDatumOverlay(datum, true, isEditMode); //260529 hbk Phase 39.1 G4-03
    ```

    L1254/L1270 (Phase 36 swap):
    ```csharp
    bool isEditMode = (btn_teachDatum != null && btn_teachDatum.IsChecked == true); //260529 hbk Phase 39.1 G4-03
    halconViewer.SetDatumOverlay(_selectedDatumForSwap, true, isEditMode); //260528 hbk Phase 36 D-36-09 //260529 hbk Phase 39.1 G4-03
    ```

    **마커 누적 (Phase 20 D-12 stacking):** 기존 마커 보존 + `//260529 hbk Phase 39.1 G4-03` 누적.

    **C) InspectionListView.xaml.cs L135, L459 호출처에 isEditMode 인자 전달:**

    각 호출처에 isEditMode 계산 + 3-인자 호출:
    ```csharp
    //260424 hbk Phase 12 D-01 — Datum 노드 선택 시 SetDatumOverlay 호출
    //260529 hbk Phase 39.1 G4-03 — isEditMode 인자 전달
    bool isEditMode = (mParentWindow != null && mParentWindow.mainView != null
                       && mParentWindow.mainView.btn_teachDatum != null
                       && mParentWindow.mainView.btn_teachDatum.IsChecked == true); //260529 hbk Phase 39.1 G4-03
    mParentWindow.mainView.halconViewer.SetDatumOverlay(datum, true, isEditMode); //260529 hbk Phase 39.1 G4-03
    ```
    (실제 호출 형태가 grep 라인에 따라 다를 수 있으므로 — 호출 형태 유지하며 isEditMode 인자만 추가.)

    **Anti-action (verbatim preserve):**
    - btn_teachDatum_Click 핸들러 (MainView.xaml.cs L2044-2111) 로직 변경 0
    - _canvasMode == TeachDatum 분기 변경 0
    - DatumConfig.cs / DatumFindingService.cs 변경 0
    - Plan 03 의 RenderInspectionResultForNode 변경 0

    **사용자 SIMUL 사전 점검:**
    - Datum CTH teaching 완료 상태에서 노드 선택 → fitting 원 + 십자만, ROI 사각형 핸들 미표시
    - btn_teachDatum 클릭 (toggle ON) → fitting 원 + ROI 사각형 핸들 동시
    - btn_teachDatum 재 클릭 (OFF) → fitting 원만
    - DETECT FAIL 케이스 → "DETECT FAIL: Datum_X" 라벨 표시 (변경 0)
    - VTH / TLI datum 동작 변경 0

    **빌드:** msbuild Debug/x64 PASS.
  </action>
  <verify>
    <automated>
      Grep 검증:
      - MainResultViewerControl: `SetDatumOverlay.*bool isEditMode = false` 포함
      - MainResultViewerControl: `_datumIsEditMode` 최소 4 hit
      - MainResultViewerControl RenderDatumOverlay 호출: `, _datumIsEditMode` 포함
      - MainView SetDatumOverlay 11 호출처: 3-인자 형태로 변경
      - MainView isEditMode 계산: `btn_teachDatum.*IsChecked` hit 수 증가
      - InspectionListView: 2 호출처 3-인자 형태
      - btn_teachDatum_Click 핸들러 본문 변경 0
      - DatumConfig.cs / DatumFindingService.cs 변경 0
      - HalconDisplayService L822-884 / L886-919 변경 0 (Task 1 외)
      - msbuild Debug/x64 PASS
    </automated>
  </verify>
  <done>
    - MainResultViewerControl.SetDatumOverlay 시그니처 + _datumIsEditMode 필드 + RenderDatumOverlay 호출 인자 전달
    - ClearDatumOverlay 가 _datumIsEditMode 리셋
    - MainView SetDatumOverlay 11 호출처 모두 3-arg
    - InspectionListView SetDatumOverlay 2 호출처 모두 3-arg
    - btn_teachDatum_Click 변경 0
    - Phase 36 swap 흐름 (L1254/L1270) 변경 0 — isEditMode 인자만 추가
    - msbuild Debug/x64 PASS
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Item #4 SIMUL UAT — Datum CTH Edit 모드 분리 + Phase 39/36/11/13/16 회귀 0</name>
  <files>WPF_Example/Halcon/Display/HalconDisplayService.cs, WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs, WPF_Example/UI/ContentItem/MainView.xaml.cs, WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <action>
    사용자 SIMUL 모드에서 7 시나리오 수동 실행 + 결과 보고. 본 task 는 코드 변경 0.
  </action>
  <what-built>
    HalconDisplayService.RenderDatumOverlay 에 isEditMode 옵션 인자 + cthHideRois 가드. MainResultViewerControl SetDatumOverlay 시그니처 + _datumIsEditMode 필드. MainView 11 호출처 + InspectionListView 2 호출처 isEditMode 인자 전달. fitting 원 + DetectedOrigin 십자 + DETECT FAIL 라벨 변경 0.
  </what-built>
  <how-to-verify>
    **SIMUL UAT 7 시나리오 (D-G1-04 sign-off):**

    1. **CTH 평소 모드 — fitting 원 만:**
       - Datum CTH teaching 완료 (LastTeachSucceeded==true), btn_teachDatum.IsChecked==false
       - 캔버스: fitting 원 (#90EE90) + yellow center cross + DetectedOrigin magenta 십자 + Datum Origin 라벨
       - 캔버스: Horizontal_A 사각형 / Horizontal_B 사각형 / Circle ROI 사각형 / Strip 사각형 미표시

    2. **CTH Edit 모드 — fitting 원 + ROI 핸들 동시:**
       - btn_teachDatum 클릭 (toggle ON)
       - 캔버스: 시나리오 1 의 fitting 원 + 십자 그대로 + Horizontal_A (H-A) + Horizontal_B (H-B) + Circle ROI (Circle) + Strip 표시
       - btn_teachDatum 재 클릭 (OFF) → 시나리오 1 복귀

    3. **CTH 티칭 미완료 — 기존 동작:**
       - 새 Datum 노드 또는 teaching 실패 직후 (LastTeachSucceeded==false)
       - btn_teachDatum 상태 무관: Horizontal_A/B 사각형 + Circle ROI + Strip 표시
       - fitting 원 / center cross 미표시 (LastTeachSucceeded 블록 미진입)

    4. **DETECT FAIL 라벨 회귀 0 (Phase 39):**
       - RuntimeDetectFailed=true 또는 IsConfigured && !LastFindSucceeded
       - 캔버스: "DETECT FAIL: Datum_X" 적색 라벨 우상단 표시 (Phase 39 hotfix CO-39-03)
       - isEditMode 무관

    5. **VTH / VTH-DualImage 회귀 0:**
       - VTH datum 노드 선택 → Vertical (Vert) + Horizontal_A/B (H-A/H-B) 표시
       - btn_teachDatum 토글 무관 (cthHideRois 미적용)
       - LastTeachSucceeded==true 시 raw points (Vertical=orange, H_A=green, H_B=lime green) 표시

    6. **TLI 회귀 0:**
       - TLI datum 노드 선택 → Line1 (L1) + Line2 (L2) 사각형 표시
       - btn_teachDatum 토글 무관
       - LastTeachSucceeded==true 시 yellow Line1 외삽 + cyan Line2 외삽 + red intersection cross + raw points

    7. **Phase 36 swap 회귀 0:**
       - VTH-DualImage + 2-image swap 흐름 (L1254/L1270)
       - swap 직후 SetDatumOverlay isEditMode 인자 정상 전달
       - Test Find Datum + RenderDatumFindResult (L889) 변경 0

    **자동 점검:**
    - msbuild Debug/x64 PASS
    - HalconDisplayService L822-884 / L886-919 변경 0
    - "#90EE90" hex 색상 유지
    - DatumConfig.cs / DatumFindingService.cs 변경 0
  </how-to-verify>
  <verify>
    사용자 시나리오 1~7 PASS + grep 검증.
  </verify>
  <done>
    사용자가 "approved" 또는 시나리오 1~7 모두 PASS 보고.
  </done>
  <resume-signal>
    사용자 "approved" 시 진행. fitting 원 미표시 / DETECT FAIL 회귀 / VTH/TLI 동작 변경 / Phase 36 swap 회귀 발생 시 (CO 신설) 보고.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MainView.btn_teachDatum.IsChecked ↔ SetDatumOverlay | UI thread → MainResultViewerControl |
| _datumIsEditMode 필드 ↔ Render | UI thread 단일 — race condition 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-04-01 | T (Tampering) | btn_teachDatum.IsChecked == null → NRE | mitigate | 모든 호출처에서 `(btn_teachDatum != null && btn_teachDatum.IsChecked == true)` null-guard |
| T-39.1-04-02 | I (Info Disclosure) | cthHideRois 가드 우회 시 ROI 좌표 노출 | accept | INI 평문 — UI hide 는 UX 명확화 |
| T-39.1-04-03 | T (Tampering) | DETECT FAIL 라벨 분기 회귀 → Phase 39 hotfix 무력화 | mitigate | grep 검증 (`RuntimeDetectFailed`). SIMUL UAT 시나리오 4 |
| T-39.1-04-04 | T (Tampering) | Phase 36 swap SetDatumOverlay 인자 누락 | mitigate | grep 검증 (L1254/L1270 3-arg). SIMUL UAT 시나리오 7 |
</threat_model>

<verification>
**자동 검증:**
- [ ] msbuild Debug/x64 PASS
- [ ] HalconDisplayService.RenderDatumOverlay 시그니처에 `bool isEditMode = false`
- [ ] cthHideRois 가드 + Circle ROI / Horizontal_A/B 분기 가드
- [ ] MainResultViewerControl.SetDatumOverlay 시그니처 + _datumIsEditMode 필드
- [ ] MainView 11 호출처 3-arg
- [ ] InspectionListView 2 호출처 3-arg

**Anti-goal 회귀:**
- [ ] HalconDisplayService L822-884 (fitting 원 + raw points) 변경 0
- [ ] L886 RenderDatumFindResult 변경 0 (Phase 36 CO-36-03)
- [ ] L891-919 DETECT FAIL 라벨 변경 0 (Phase 39 CO-39-02/03)
- [ ] L867 "#90EE90" hex 색상 보존
- [ ] TwoLineIntersect / VTH / VTH-DualImage 변경 0
- [ ] RefOrigin magenta 십자 (L800-819) 변경 0
- [ ] DatumConfig.cs / DatumFindingService.cs 변경 0
- [ ] btn_teachDatum_Click 변경 0
- [ ] project_roi_edit_mode_deferred 미통합 (D-G4-04)

**SIMUL UAT:**
- [ ] 7 시나리오 모두 PASS
</verification>

<success_criteria>
**M4:**
- RenderDatumOverlay isEditMode + cthHideRois ✓
- SetDatumOverlay 13 호출처 인자 전달 ✓
- CTH 평소 (LastTeachSucceeded + !isEditMode) → fitting 원 + 십자 만 ✓
- CTH Edit (LastTeachSucceeded + isEditMode) → fitting 원 + ROI 핸들 동시 ✓
- CTH 티칭 미완료 → 기존 동작 ✓

**M5 (Phase 39 / 36 / 11 / 13 / 16 회귀 0):**
- DETECT FAIL 라벨 변경 0 ✓
- RenderDatumFindResult 변경 0 ✓
- LastTeachSucceeded fitting 원 + raw points 변경 0 ✓
- TwoLineIntersect / VTH / VTH-DualImage 변경 0 ✓
- "#90EE90" hex 보존 ✓
- btn_teachDatum_Click 변경 0 ✓
- DatumConfig / DatumFindingService 변경 0 ✓
</success_criteria>

<output>
After completion, create `.planning/phases/39.1-inspection-urgent-fixes-2026-05-29/39.1-04-SUMMARY.md`.

Include:
- Tasks executed (Task 1 + Task 2)
- 4 파일 수정 그래프
- msbuild Debug/x64 PASS 결과
- Grep 검증 결과 (isEditMode / cthHideRois / 13 호출처 / fitting 원 / DETECT FAIL 보존)
- SIMUL UAT 7 시나리오 결과
- Phase 39 / 36 / 14-03 W4-A / 11 / 13 / 16 회귀 0 증거
</output>
