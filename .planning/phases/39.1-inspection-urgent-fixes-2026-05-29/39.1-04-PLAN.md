---
phase: 39.1-inspection-urgent-fixes-2026-05-29
plan: 04
type: execute
# I1 (260529): D-G1-03 의 의도 "Wave 1 algorithm / Wave 2 UI" 가 file overlap
#   (MainView + InspectionListView 가 Plan 03 의 files_modified 와 겹침) 으로
#   4 plan / 3 wave 로 조정됨. Plan 04 = wave 3 (UI 두 번째 슬롯). 데이터 의존
#   없음, 단지 file 충돌 회피 목적의 sequential 의존 도입.
wave: 3
depends_on: ["39.1-03"]
files_modified:
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
  - WPF_Example/UI/ContentItem/MainView.xaml
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
autonomous: false
requirements: [WF-01]
tags: [ui, datum-cth, edit-mode, halcon-overlay, render-datum]
user_setup: []

must_haves:
  truths:
    - "Datum CTH 가 LastTeachSucceeded==true 인 상태에서 btn_teachDatum.IsChecked==false (평소) → fitting 원 + DetectedOrigin 십자 만 표시"
    - "Datum CTH 가 LastTeachSucceeded==true + btn_teachDatum.IsChecked==true (Edit 모드) → fitting 원 + Horizontal_A/B + Circle ROI 사각형 핸들 동시 표시"
    - "Datum CTH 가 LastTeachSucceeded==false 인 상태 → 기존 동작 (Horizontal_A/B + Circle ROI 사각형 핸들, 티칭 미완료)"
    - "DETECT FAIL 라벨 분기 (Phase 39 hotfix CO-39-02/03) 변경 0"
    - "Datum CTH 외 algorithm (TwoLineIntersect / VerticalTwoHorizontal / VerticalTwoHorizontalDualImage) 동작 변경 0"
  artifacts:
    - path: "WPF_Example/Halcon/Display/HalconDisplayService.cs"
      provides: "RenderDatumOverlay 시그니처에 isEditMode 옵션 인자 + cthHideRois 가드 분기"
      contains: "bool isEditMode = false, cthHideRois"
    - path: "WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs"
      provides: "SetDatumOverlay 시그니처 + _datumIsEditMode 필드 + RenderDatumOverlay 호출 isEditMode 전달"
      contains: "SetDatumOverlay(DatumConfig datum, bool isSelected, bool isEditMode = false), _datumIsEditMode"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "IsDatumTeachActive 프로퍼티 wrapper + GetDatumEditMode helper + 11 호출처 helper 호출로 통합"
      contains: "public bool IsDatumTeachActive, private bool GetDatumEditMode()"
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "Datum 노드 선택 시 mParentWindow.mainView.IsDatumTeachActive 프로퍼티 사용한 isEditMode 인자 전달"
  key_links:
    - from: "MainView.btn_teachDatum.IsChecked toggle"
      to: "HalconDisplayService.RenderDatumOverlay isEditMode 분기"
      via: "MainView.GetDatumEditMode helper (intra) + IsDatumTeachActive 프로퍼티 (extra) → MainResultViewerControl.SetDatumOverlay 옵션 인자 → RenderDatumOverlay propagate"
      pattern: "isEditMode|IsDatumTeachActive|GetDatumEditMode"
    - from: "HalconDisplayService.RenderDatumOverlay cthHideRois 가드"
      to: "Circle ROI 그리기 분기 + Horizontal_A/B 그리기 분기"
      via: "cthHideRois==true 시 두 분기 skip (CircleTwoHorizontal + LastTeachSucceeded + !isEditMode)"
      pattern: "cthHideRois|!cthHideRois"
---

<objective>
Datum CircleTwoHorizontal (CTH) 의 Edit 모드를 btn_teachDatum 토글 기반으로 분리한다.
현재는 LastTeachSucceeded==true 인 CTH datum 노드 선택 시 fitting 원 + ROI 사각형 핸들이 항상 동시 표시.
사용자가 평소에는 fitting 원만, btn_teachDatum 토글 ON 시에만 ROI 핸들 표시 → UX 명확화.

Purpose: 사용자 보고 "CTH Datum 원이 계속 Edit 모드처럼 보임" — fitting 원이 ROI 사각형 핸들과 동시 표시되어 시각적 혼란.
Output: 5 파일 수정 — HalconDisplayService.cs (RenderDatumOverlay 분기), MainResultViewerControl.xaml.cs (SetDatumOverlay 시그니처), MainView.xaml (btn_teachDatum access modifier 확정), MainView.xaml.cs (IsDatumTeachActive 프로퍼티 wrapper + GetDatumEditMode helper + 11 호출처 helper 호출), InspectionListView.xaml.cs (2 호출처 IsDatumTeachActive 프로퍼티 사용).

**Anti-goal (변경 0, 회귀 0):**
- HalconDisplayService.RenderDatumOverlay **LastTeachSucceeded 블록 (Phase 11/13/16 검출 라인 + fitting 원 + raw points + center cross)** 변경 0
- HalconDisplayService.RenderDatumOverlay **RenderDatumFindResult 호출 블록 (Phase 36 CO-36-03)** 변경 0
- HalconDisplayService.RenderDatumOverlay **DETECT FAIL 라벨 분기 (Phase 39 hotfix CO-39-02/03)** 변경 0
- HalconDisplayService.RenderDatumOverlay TwoLineIntersect Line1/Line2 분기 변경 0
- HalconDisplayService.RenderDatumOverlay VerticalTwoHorizontal / VTH-DualImage 분기 변경 0
- HalconDisplayService.RenderDatumOverlay RefOrigin magenta 십자 분기 변경 0
- DatumFindingService.cs 변경 0
- DatumConfig.cs 변경 0
- project_roi_edit_mode_deferred 3 항목 통합 X (D-G4-04)
- Plan 03 의 RenderInspectionResultForNode 변경 0 — Datum 노드 미호출

**I4 (260529, 라인 번호 → 의미 기반 표현):**
Phase 39 hotfix CO-39-02/03 적용 후 라인 시프트 가능성. 본 plan 은 의미 기반 anchor 사용:
- "L867 hex 색상" → "LastTeachSucceeded 블록 안 hex 색상 `#90EE90`"
- "L822-884" → "RenderDatumFindResult 호출 직전 LastTeachSucceeded 블록"
- "L886-919" → "DETECT FAIL 라벨 분기 (Phase 39 CO-39-02/03)"
- "L755-770 Circle ROI" → "CircleTwoHorizontal && CircleROI_Radius > 0 분기"
- "L773-798 Horizontal_A/B" → "AlgorithmType != TwoLineIntersect Horizontal_A/B 분기"
- "L800-819 RefOrigin" → "IsConfigured RefOrigin magenta 십자 분기"
- 코드 안 anchor 식별은 grep 패턴 (예: `if (datum.LastTeachSucceeded)`, `RenderDatumFindResult(window, datum)`, `RuntimeDetectFailed`, `DispCircle.*CircleROI_Radius`, `DispRectangle2.*Horizontal_A`, `#90EE90`) 으로 수행.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-inspection-urgent-fixes-2026-05-29/39.1-CONTEXT.md
@.planning/phases/39.1-inspection-urgent-fixes-2026-05-29/39.1-PATTERNS.md
@WPF_Example/Halcon/Display/HalconDisplayService.cs
@WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
@WPF_Example/UI/ContentItem/MainView.xaml
@WPF_Example/UI/ContentItem/MainView.xaml.cs
@WPF_Example/UI/ControlItem/InspectionListView.xaml.cs

<interfaces>
HalconDisplayService.RenderDatumOverlay 현재 시그니처:
```csharp
public void RenderDatumOverlay(HWindow window, DatumConfig datum, bool isSelected)
```

MainResultViewerControl.SetDatumOverlay 현재 시그니처:
```csharp
public void SetDatumOverlay(DatumConfig datum, bool isSelected) {
    _datumConfig = datum;
    _datumSelected = isSelected;
    Render();
}
```

MainResultViewerControl.RenderNow 의 RenderDatumOverlay 호출:
```csharp
if (_datumConfig != null) {
    _displayService.RenderDatumOverlay(ViewerHost.HalconWindow, _datumConfig, _datumSelected);
}
```

HalconDisplayService.RenderDatumOverlay CTH Circle ROI 그리기 분기 (의미 anchor — 가드 대상):
```csharp
if (datum.AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal
    && datum.CircleROI_Radius > 0)
{
    HOperatorSet.SetColor(window, color);
    HOperatorSet.SetLineWidth(window, lineWidth);
    HOperatorSet.DispCircle(window, datum.CircleROI_Row, datum.CircleROI_Col, datum.CircleROI_Radius);
    DrawRoiLabelAt(window, /* ... */, "Circle");
    RenderCircleStripOverlay(window, datum);
}
```

HalconDisplayService.RenderDatumOverlay Horizontal_A/B 그리기 분기 (CTH+VTH 공용, 가드 대상):
```csharp
if (datum.AlgorithmTypeEnum != EDatumAlgorithm.TwoLineIntersect)
{
    HOperatorSet.SetColor(window, color);
    HOperatorSet.SetLineWidth(window, lineWidth);
    if (datum.Horizontal_A_Length1 > 0 && datum.Horizontal_A_Length2 > 0) {
        HOperatorSet.DispRectangle2(/* ... */);
        DrawRoiLabel(/* ... */, "H-A");
    }
    if (datum.Horizontal_B_Length1 > 0 && datum.Horizontal_B_Length2 > 0) {
        HOperatorSet.DispRectangle2(/* ... */);
        DrawRoiLabel(/* ... */, "H-B");
    }
}
```

HalconDisplayService.RenderDatumOverlay LastTeachSucceeded 블록 (**변경 0**, fitting 원 + raw points 그대로):
```csharp
if (datum.LastTeachSucceeded) {
    // Line1Detected / Line2Detected 외삽 — TLI 만 의미 있음
    // Intersection cross (red)
    // raw edge points (cyan/magenta/gray/green/lime green/orange)
    if (datum.AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal
        && datum.CircleDetected_Radius > 0) {
        HOperatorSet.SetColor(window, "#90EE90"); // light green hex (memory feedback_halcon_setcolor_invalid_names)
        HOperatorSet.DispCircle(window, datum.CircleCenter_Row, datum.CircleCenter_Col, datum.CircleDetected_Radius);
        // yellow center cross size=12
    }
}
```

HalconDisplayService.RenderDatumOverlay RenderDatumFindResult + DETECT FAIL 라벨 분기 (**변경 0**):
```csharp
RenderDatumFindResult(window, datum); // Phase 36 CO-36-03
if (datum.RuntimeDetectFailed || (datum.IsConfigured && !datum.LastFindSucceeded)) {
    try {
        EnsureFontInitialized(window);
        HOperatorSet.SetColor(window, "red");
        // GetPart + hash stagger + "DETECT FAIL: " + datum.DatumName  (Phase 39 hotfix CO-39-02/03 우상단 + datum 이름)
    } catch { /* suppress */ }
}
```

MainView.xaml.cs SetDatumOverlay 11 호출처 grep:
- `_editingDatum, false` (모드 해제 시, 1곳)
- `datum, true` (일반 노드 선택, 4곳)
- `_selectedDatumForSwap, true` (Phase 36 swap, 2곳)
- `_editingDatum, true` (편집 진입, 3곳)
- `datum, true` (Test Find 진입, 1곳)

InspectionListView.xaml.cs SetDatumOverlay 2 호출처 grep:
- Datum 노드 선택 시 호출 (2곳)

**W4 (260529) — btn_teachDatum 외부 접근 안전화:**
InspectionListView 가 `mParentWindow.mainView.btn_teachDatum.IsChecked` 를 직접 참조하는 형태는 access modifier (internal/public) 의존 + 노출 과다. 본 plan 은 MainView 에 **`public bool IsDatumTeachActive => btn_teachDatum?.IsChecked == true`** 프로퍼티 wrapper 를 신설하고, InspectionListView 의 2 호출처가 `mParentWindow.mainView.IsDatumTeachActive` 를 사용하도록 한다. → encapsulation + access modifier 안전.

**W3 (260529) — 11 호출처 변수 shadowing 회피 (옵션 B helper 메서드 채택):**
MainView.xaml.cs 의 11 호출처에 각각 `bool isEditMode = ...` 로컬 변수 도입 시 동일 메서드 내 중복 호출 발견되면 CS0136 (변수 이름 중복) 가능. **옵션 B 채택:** `private bool GetDatumEditMode() => btn_teachDatum?.IsChecked == true;` helper 메서드를 신설하고 11 호출처는 `GetDatumEditMode()` 호출. 변수 중복/shadowing 위험 0.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: HalconDisplayService.RenderDatumOverlay 에 isEditMode 옵션 인자 + cthHideRois 가드 분기 추가</name>
  <files>WPF_Example/Halcon/Display/HalconDisplayService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Display/HalconDisplayService.cs §RenderDatumOverlay 전체 (grep anchor: `public void RenderDatumOverlay`)
    - 39.1-PATTERNS.md §4.1 (RenderDatumOverlay pattern + critical preserve blocks: LastTeachSucceeded 블록 / RenderDatumFindResult / DETECT FAIL)
    - 39.1-PATTERNS.md "Shared Patterns" HALCON SetColor 표준명 / Try/Catch Swallow Convention
    - memory feedback_halcon_setcolor_invalid_names — `"light green"` → hex `"#90EE90"` 보존
  </read_first>
  <action>
    `WPF_Example/Halcon/Display/HalconDisplayService.cs` 의 `RenderDatumOverlay` 메서드를 다음과 같이 수정한다 (per D-G4-05).

    **A) 시그니처 수정 — isEditMode 옵션 인자 추가:**
    ```csharp
    //260529 hbk Phase 39.1 G4-03/G4-05 — Datum CTH Edit 모드 분리: isEditMode 인자 추가. 기본값 false 로 기존 호출자 호환.
    public void RenderDatumOverlay(HWindow window, DatumConfig datum, bool isSelected, bool isEditMode = false) //260529 hbk Phase 39.1 G4-03/G4-05
    ```

    **B) cthHideRois 가드 변수 도입 — try 블록 진입 후, SetColor/SetLineWidth 다음, 첫 algorithm 분기 직전:**
    ```csharp
    try
    {
        HOperatorSet.SetColor(window, color);
        HOperatorSet.SetLineWidth(window, lineWidth);

        //260529 hbk Phase 39.1 G4-05 — CTH 평소 모드 (LastTeachSucceeded + !isEditMode) 시 Horizontal_A/B + Circle ROI 사각형 핸들 hide.
        //  fitting 원 + DetectedOrigin 십자는 LastTeachSucceeded 블록 + RefOrigin 블록에서 별도 그림 — 본 가드 영향 없음.
        //  TwoLineIntersect / VerticalTwoHorizontal 등 다른 algorithm 에는 영향 0 (CircleTwoHorizontal 한정).
        bool cthHideRois = (datum.AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal) //260529 hbk Phase 39.1 G4-05
                        && datum.LastTeachSucceeded //260529 hbk Phase 39.1 G4-05
                        && !isEditMode; //260529 hbk Phase 39.1 G4-05

        // 기존 algorithm 분기 (TwoLineIntersect / VTH) 시작...
    ```

    **C) Circle ROI 그리기 분기 (의미 anchor: `CircleTwoHorizontal && CircleROI_Radius > 0`) 에 가드 추가:**
    ```csharp
    //260424 hbk Phase 12 D-10 — Circle ROI 검색 영역 (CircleTwoHorizontal 일 때만 렌더)
    //260529 hbk Phase 39.1 G4-05 — cthHideRois 가드: Edit 모드 OFF + 티칭 완료 시 Circle ROI + Strip 시각화 hide
    if (datum.AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal
        && datum.CircleROI_Radius > 0
        && !cthHideRois) //260529 hbk Phase 39.1 G4-05
    {
        HOperatorSet.SetColor(window, color);
        HOperatorSet.SetLineWidth(window, lineWidth);
        HOperatorSet.DispCircle(window, datum.CircleROI_Row, datum.CircleROI_Col, datum.CircleROI_Radius);
        DrawRoiLabelAt(window, datum.CircleROI_Row - datum.CircleROI_Radius - 22, datum.CircleROI_Col - datum.CircleROI_Radius, "Circle");
        RenderCircleStripOverlay(window, datum);
    }
    ```

    **D) Horizontal_A/B 그리기 분기 (의미 anchor: `AlgorithmType != TwoLineIntersect Horizontal_A/B`) 에 가드 추가:**
    ```csharp
    //260424 hbk Phase 12 D-11 — Horizontal A/B ROI Rectangle2 (CircleTwoHorizontal + VerticalTwoHorizontal 공용)
    //260529 hbk Phase 39.1 G4-05 — cthHideRois 가드: CTH Edit 모드 OFF + 티칭 완료 시 Horizontal_A/B hide.
    //  VTH 는 cthHideRois 가 false (AlgorithmTypeEnum != CircleTwoHorizontal) → 영향 0
    if (datum.AlgorithmTypeEnum != EDatumAlgorithm.TwoLineIntersect
        && !cthHideRois) //260529 hbk Phase 39.1 G4-05
    {
        HOperatorSet.SetColor(window, color);
        HOperatorSet.SetLineWidth(window, lineWidth);
        if (datum.Horizontal_A_Length1 > 0 && datum.Horizontal_A_Length2 > 0)
        {
            HOperatorSet.DispRectangle2(window, datum.Horizontal_A_Row, datum.Horizontal_A_Col, datum.Horizontal_A_Phi, datum.Horizontal_A_Length1, datum.Horizontal_A_Length2);
            DrawRoiLabel(window, datum.Horizontal_A_Row, datum.Horizontal_A_Col, datum.Horizontal_A_Phi, datum.Horizontal_A_Length1, datum.Horizontal_A_Length2, "H-A");
        }
        if (datum.Horizontal_B_Length1 > 0 && datum.Horizontal_B_Length2 > 0)
        {
            HOperatorSet.DispRectangle2(window, datum.Horizontal_B_Row, datum.Horizontal_B_Col, datum.Horizontal_B_Phi, datum.Horizontal_B_Length1, datum.Horizontal_B_Length2);
            DrawRoiLabel(window, datum.Horizontal_B_Row, datum.Horizontal_B_Col, datum.Horizontal_B_Phi, datum.Horizontal_B_Length1, datum.Horizontal_B_Length2, "H-B");
        }
    }
    ```

    **Anti-action (verbatim preserve, I4 의미 anchor):**
    - TwoLineIntersect Line1 분기 (변경 0)
    - VerticalTwoHorizontal / VTH-DualImage Vertical 분기 (변경 0)
    - TwoLineIntersect Line2 분기 (변경 0)
    - IsConfigured RefOrigin magenta 십자 분기 (변경 0)
    - **LastTeachSucceeded 블록 안 hex 색상 `#90EE90` + yellow center cross + raw points (변경 0)**
    - **RenderDatumFindResult 호출 블록 (Phase 36 CO-36-03, 변경 0)**
    - **DETECT FAIL 라벨 분기 (Phase 39 hotfix CO-39-02/03 — 우상단 + datum 이름 포함, 변경 0)**
    - 외곽 try/catch swallow (변경 0)

    **Brace style (Allman):** HalconDisplayService.cs Allman style.

    **HALCON SetColor 표준명:** 신규 SetColor 호출 0 — 모든 색상은 기존 분기에서 처리. 비표준 색상명 도입 0 (memory feedback).

    **빌드:** msbuild Debug/x64 PASS.
  </action>
  <verify>
    <automated>
      Grep 검증 (의미 anchor 기반, I4):
      - 시그니처: `grep -n "public void RenderDatumOverlay" HalconDisplayService.cs` → `bool isEditMode = false` 포함
      - cthHideRois: `grep -n "cthHideRois" HalconDisplayService.cs` → 최소 3 hit
      - Circle ROI 가드: `&& !cthHideRois` 포함 라인 1 hit (CircleTwoHorizontal && CircleROI_Radius 분기)
      - Horizontal A/B 가드: `&& !cthHideRois` 포함 라인 1 hit (AlgorithmType != TwoLineIntersect 분기)
      - LastTeachSucceeded 블록 보존: `grep -n "if (datum.LastTeachSucceeded)" HalconDisplayService.cs` → 1 hit
      - LastTeachSucceeded 블록 안 hex 색상 `#90EE90` 보존: `grep -n "#90EE90" HalconDisplayService.cs` → 1 hit
      - RenderDatumFindResult 호출 블록 보존: `grep -n "RenderDatumFindResult(window, datum)" HalconDisplayService.cs` → 1 hit
      - DETECT FAIL 라벨 분기 보존 (Phase 39 CO-39-02/03): `grep -n "RuntimeDetectFailed" HalconDisplayService.cs` → 최소 1 hit + `grep -n "DETECT FAIL" HalconDisplayService.cs` → 최소 1 hit
      - msbuild Debug/x64 PASS
    </automated>
  </verify>
  <done>
    - RenderDatumOverlay 시그니처에 `bool isEditMode = false`
    - cthHideRois 가드 변수 (CircleTwoHorizontal && LastTeachSucceeded && !isEditMode)
    - Circle ROI / Horizontal_A/B 분기에 `&& !cthHideRois` 가드
    - LastTeachSucceeded 블록 / RenderDatumFindResult 호출 / DETECT FAIL 라벨 분기 보존
    - TwoLineIntersect / VTH / VTH-DualImage 변경 0
    - msbuild Debug/x64 PASS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SetDatumOverlay 시그니처 + GetDatumEditMode helper + IsDatumTeachActive 프로퍼티 + RenderDatumOverlay 호출 isEditMode 인자 전달 (MainResultViewerControl + MainView + InspectionListView)</name>
  <files>WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs, WPF_Example/UI/ContentItem/MainView.xaml, WPF_Example/UI/ContentItem/MainView.xaml.cs, WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs §SetDatumOverlay §ClearDatumOverlay §RenderNow (grep anchor: `SetDatumOverlay`, `ClearDatumOverlay`, `RenderDatumOverlay`)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs SetDatumOverlay 11 호출처 (grep anchor: `SetDatumOverlay\(`) — 함수별 분포 확인 (각 호출처가 어느 메서드 내에 위치하는지). 동일 메서드 내 중복 호출 식별 시 W3 helper 메서드로 통합.
    - **W4 (260529)**: `WPF_Example/UI/ContentItem/MainView.xaml` §btn_teachDatum x:Name + access modifier 확인 (internal/public). 외부 UserControl(InspectionListView) 접근 가능 여부 컴파일 시점 결정. 외부 접근 불가 시 MainView.xaml.cs 에 `public bool IsDatumTeachActive` 프로퍼티 신설 후 InspectionListView 호출처에서 IsDatumTeachActive 사용.
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs SetDatumOverlay 2 호출처 (grep anchor: `SetDatumOverlay\(`)
    - Task 1 적용 후 HalconDisplayService.RenderDatumOverlay 시그니처 확인
  </read_first>
  <action>
    **A) MainResultViewerControl.xaml.cs SetDatumOverlay 시그니처 + RenderDatumOverlay 호출 수정:**

    클래스 본문 위쪽 (`_datumFindResultOverlay` 근처) 에 신규 필드 추가:
    ```csharp
    //260529 hbk Phase 39.1 G4-03 — Datum CTH Edit 모드 트리거. btn_teachDatum.IsChecked 기반 호출자가 전달.
    private bool _datumIsEditMode = false; //260529 hbk Phase 39.1 G4-03
    ```

    SetDatumOverlay 시그니처 변경:
    ```csharp
    //260410 hbk Phase 4 gap fix: set Datum for overlay rendering
    //260529 hbk Phase 39.1 G4-03 — isEditMode 옵션 인자 추가 (기본 false). MainView.GetDatumEditMode() / IsDatumTeachActive 기반 전달.
    public void SetDatumOverlay(DatumConfig datum, bool isSelected, bool isEditMode = false) //260529 hbk Phase 39.1 G4-03
    {
        _datumConfig = datum;
        _datumSelected = isSelected;
        _datumIsEditMode = isEditMode; //260529 hbk Phase 39.1 G4-03
        Render();
    }
    ```

    ClearDatumOverlay 에 리셋 추가:
    ```csharp
    public void ClearDatumOverlay()
    {
        _datumConfig = null;
        _datumSelected = false;
        _datumIsEditMode = false; //260529 hbk Phase 39.1 G4-03 — Edit 모드 리셋
    }
    ```

    RenderNow 의 RenderDatumOverlay 호출 수정:
    ```csharp
    //260410 hbk Phase 4 gap fix: render Datum Line ROI overlay
    //260529 hbk Phase 39.1 G4-03 — isEditMode 인자 전달
    if (_datumConfig != null)
    {
        _displayService.RenderDatumOverlay(ViewerHost.HalconWindow, _datumConfig, _datumSelected, _datumIsEditMode); //260529 hbk Phase 39.1 G4-03
    }
    ```

    **B) MainView.xaml.cs GetDatumEditMode helper + IsDatumTeachActive 프로퍼티 + 11 호출처 helper 호출로 통합:**

    **B-1) Helper + 프로퍼티 신설 (intra-class helper + extra-class wrapper):**
    ```csharp
    //260529 hbk Phase 39.1 G4-03 — W3 (260529): 11 호출처 변수 shadowing 회피 옵션 B (helper 메서드 채택).
    //  intra-class 호출처는 GetDatumEditMode() 호출. 변수 중복/shadowing 위험 0.
    private bool GetDatumEditMode() //260529 hbk Phase 39.1 G4-03 — W3
    {
        return btn_teachDatum != null && btn_teachDatum.IsChecked == true;
    }

    //260529 hbk Phase 39.1 G4-03 — W4 (260529): InspectionListView 등 외부 UserControl 에서 btn_teachDatum 직접 접근 회피.
    //  encapsulation + access modifier 안전. InspectionListView 의 2 호출처에서 mParentWindow.mainView.IsDatumTeachActive 사용.
    public bool IsDatumTeachActive //260529 hbk Phase 39.1 G4-03 — W4
    {
        get { return btn_teachDatum != null && btn_teachDatum.IsChecked == true; }
    }
    ```

    **B-2) MainView.xaml — btn_teachDatum access modifier 안전화 (W4 가드):**
    `WPF_Example/UI/ContentItem/MainView.xaml` 의 `btn_teachDatum` x:Name 위치 확인. WPF 기본은 internal — `IsDatumTeachActive` 프로퍼티 wrapper 가 외부 접근을 안전 우회. **MainView.xaml 자체 수정은 불필요 (프로퍼티 신설로 해결)** — 단, read_first 단계에서 access modifier 확인 결과 `x:FieldModifier="public"` 가 명시되어 있으면 그대로 유지 (이전 phase 호환).

    **B-3) 11 호출처 helper 호출로 통합:**

    공통 인자 결정 규칙: `GetDatumEditMode()` helper 직접 호출 — 로컬 변수 도입 0 (W3 변수 shadowing 회피).

    호출처별 변경 예:

    "모드 해제 시" 호출처 (`_editingDatum, false`):
    ```csharp
    halconViewer.SetDatumOverlay(_editingDatum, false, false); //260505 hbk Phase 18 CO-04 //260529 hbk Phase 39.1 G4-03 — 모드 해제 → isEditMode=false 명시
    ```

    "일반 노드 선택 / Test Find 진입 / 편집 진입" 호출처 (`datum, true` 또는 `_editingDatum, true`):
    ```csharp
    halconViewer.SetDatumOverlay(datum, true, GetDatumEditMode()); //260529 hbk Phase 39.1 G4-03 — W3 helper 호출
    ```

    "Phase 36 swap" 호출처 (`_selectedDatumForSwap, true`):
    ```csharp
    halconViewer.SetDatumOverlay(_selectedDatumForSwap, true, GetDatumEditMode()); //260528 hbk Phase 36 D-36-09 //260529 hbk Phase 39.1 G4-03 — W3 helper 호출
    ```

    **마커 누적 (Phase 20 D-12 stacking):** 기존 마커 보존 + `//260529 hbk Phase 39.1 G4-03` 누적.

    **C) InspectionListView.xaml.cs 2 호출처에 IsDatumTeachActive 프로퍼티 사용 (W4 encapsulation):**

    각 호출처에 `mParentWindow.mainView.IsDatumTeachActive` 프로퍼티 사용 — `btn_teachDatum` 직접 접근 회피:
    ```csharp
    //260424 hbk Phase 12 D-01 — Datum 노드 선택 시 SetDatumOverlay 호출
    //260529 hbk Phase 39.1 G4-03 — W4: mainView.IsDatumTeachActive 프로퍼티 wrapper 사용 (btn_teachDatum 직접 접근 회피, encapsulation)
    bool isEditMode = (mParentWindow != null && mParentWindow.mainView != null
                       && mParentWindow.mainView.IsDatumTeachActive); //260529 hbk Phase 39.1 G4-03 — W4
    mParentWindow.mainView.halconViewer.SetDatumOverlay(datum, true, isEditMode); //260529 hbk Phase 39.1 G4-03
    ```
    (호출 형태 유지하며 isEditMode 인자만 추가, IsDatumTeachActive 프로퍼티로 가져옴.)

    **Anti-action (verbatim preserve):**
    - btn_teachDatum_Click 핸들러 로직 변경 0 (grep anchor: `TeachDatumButton_Click` 또는 `btn_teachDatum_Click`)
    - _canvasMode == TeachDatum 분기 변경 0
    - DatumConfig.cs / DatumFindingService.cs 변경 0
    - Plan 03 의 RenderInspectionResultForNode 변경 0

    **사용자 SIMUL 사전 점검:**
    - Datum CTH teaching 완료 상태에서 노드 선택 → fitting 원 + 십자만, ROI 사각형 핸들 미표시
    - btn_teachDatum 클릭 (toggle ON) → fitting 원 + ROI 사각형 핸들 동시
    - btn_teachDatum 재 클릭 (OFF) → fitting 원만
    - DETECT FAIL 케이스 → "DETECT FAIL: Datum_X" 라벨 표시 (변경 0)
    - VTH / TLI datum 동작 변경 0

    **빌드:** msbuild Debug/x64 PASS.
  </action>
  <verify>
    <automated>
      Grep 검증:
      - MainResultViewerControl: `SetDatumOverlay.*bool isEditMode = false` 포함
      - MainResultViewerControl: `_datumIsEditMode` 최소 4 hit
      - MainResultViewerControl RenderDatumOverlay 호출: `, _datumIsEditMode` 포함
      - MainView GetDatumEditMode helper: `grep -n "GetDatumEditMode" MainView.xaml.cs` → 최소 12 hit (정의 1 + 11 호출처)
      - MainView IsDatumTeachActive 프로퍼티: `grep -n "IsDatumTeachActive" MainView.xaml.cs` → 1 hit (정의)
      - MainView SetDatumOverlay 11 호출처: 3-인자 형태로 변경 (`SetDatumOverlay\(.*,.*,.*\)` 패턴)
      - MainView 로컬 변수 `bool isEditMode = ...` 형태 0 hit (W3 변수 shadowing 회피 확인)
      - InspectionListView: `IsDatumTeachActive` 최소 2 hit (호출처)
      - InspectionListView: `mainView.btn_teachDatum` 직접 참조 0 hit (W4 encapsulation 확인)
      - InspectionListView SetDatumOverlay 2 호출처 3-인자 형태
      - btn_teachDatum_Click 핸들러 본문 변경 0
      - DatumConfig.cs / DatumFindingService.cs 변경 0
      - HalconDisplayService LastTeachSucceeded 블록 / RenderDatumFindResult / DETECT FAIL 라벨 변경 0 (Task 1 외)
      - msbuild Debug/x64 PASS
    </automated>
  </verify>
  <done>
    - MainResultViewerControl.SetDatumOverlay 시그니처 + _datumIsEditMode 필드 + RenderDatumOverlay 호출 인자 전달
    - ClearDatumOverlay 가 _datumIsEditMode 리셋
    - MainView GetDatumEditMode helper + IsDatumTeachActive 프로퍼티 신설
    - MainView SetDatumOverlay 11 호출처 모두 3-arg + GetDatumEditMode() helper 호출 (변수 도입 0)
    - InspectionListView SetDatumOverlay 2 호출처 모두 3-arg + IsDatumTeachActive 프로퍼티 사용 (btn_teachDatum 직접 접근 0)
    - btn_teachDatum_Click 변경 0
    - Phase 36 swap 흐름 변경 0 — isEditMode 인자만 추가
    - msbuild Debug/x64 PASS
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Item #4 SIMUL UAT — Datum CTH Edit 모드 분리 + Phase 39/36/11/13/16 회귀 0</name>
  <files>WPF_Example/Halcon/Display/HalconDisplayService.cs, WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs, WPF_Example/UI/ContentItem/MainView.xaml.cs, WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <action>
    사용자 SIMUL 모드에서 7 시나리오 수동 실행 + 결과 보고. 본 task 는 코드 변경 0.
  </action>
  <what-built>
    HalconDisplayService.RenderDatumOverlay 에 isEditMode 옵션 인자 + cthHideRois 가드. MainResultViewerControl SetDatumOverlay 시그니처 + _datumIsEditMode 필드. MainView GetDatumEditMode helper + IsDatumTeachActive 프로퍼티 + 11 호출처 helper 호출. InspectionListView 2 호출처 IsDatumTeachActive 프로퍼티 사용. fitting 원 + DetectedOrigin 십자 + DETECT FAIL 라벨 변경 0.
  </what-built>
  <how-to-verify>
    **SIMUL UAT 7 시나리오 (D-G1-04 sign-off):**

    1. **CTH 평소 모드 — fitting 원 만:**
       - Datum CTH teaching 완료 (LastTeachSucceeded==true), btn_teachDatum.IsChecked==false
       - 캔버스: fitting 원 (#90EE90) + yellow center cross + DetectedOrigin magenta 십자 + Datum Origin 라벨
       - 캔버스: Horizontal_A 사각형 / Horizontal_B 사각형 / Circle ROI 사각형 / Strip 사각형 미표시

    2. **CTH Edit 모드 — fitting 원 + ROI 핸들 동시:**
       - btn_teachDatum 클릭 (toggle ON)
       - 캔버스: 시나리오 1 의 fitting 원 + 십자 그대로 + Horizontal_A (H-A) + Horizontal_B (H-B) + Circle ROI (Circle) + Strip 표시
       - btn_teachDatum 재 클릭 (OFF) → 시나리오 1 복귀

    3. **CTH 티칭 미완료 — 기존 동작:**
       - 새 Datum 노드 또는 teaching 실패 직후 (LastTeachSucceeded==false)
       - btn_teachDatum 상태 무관: Horizontal_A/B 사각형 + Circle ROI + Strip 표시
       - fitting 원 / center cross 미표시 (LastTeachSucceeded 블록 미진입)

    4. **DETECT FAIL 라벨 회귀 0 (Phase 39 CO-39-02/03):**
       - RuntimeDetectFailed=true 또는 IsConfigured && !LastFindSucceeded
       - 캔버스: "DETECT FAIL: Datum_X" 적색 라벨 우상단 표시 (datum 이름 포함)
       - isEditMode 무관

    5. **VTH / VTH-DualImage 회귀 0:**
       - VTH datum 노드 선택 → Vertical (Vert) + Horizontal_A/B (H-A/H-B) 표시
       - btn_teachDatum 토글 무관 (cthHideRois 미적용)
       - LastTeachSucceeded==true 시 raw points (Vertical=orange, H_A=green, H_B=lime green) 표시

    6. **TLI 회귀 0:**
       - TLI datum 노드 선택 → Line1 (L1) + Line2 (L2) 사각형 표시
       - btn_teachDatum 토글 무관
       - LastTeachSucceeded==true 시 yellow Line1 외삽 + cyan Line2 외삽 + red intersection cross + raw points

    7. **Phase 36 swap 회귀 0:**
       - VTH-DualImage + 2-image swap 흐름
       - swap 직후 SetDatumOverlay isEditMode 인자 정상 전달
       - Test Find Datum + RenderDatumFindResult 변경 0

    **자동 점검:**
    - msbuild Debug/x64 PASS
    - HalconDisplayService LastTeachSucceeded 블록 / RenderDatumFindResult / DETECT FAIL 라벨 변경 0
    - LastTeachSucceeded 블록 안 `#90EE90` hex 색상 유지
    - DatumConfig.cs / DatumFindingService.cs 변경 0
    - InspectionListView 의 btn_teachDatum 직접 참조 0 (IsDatumTeachActive 프로퍼티 사용)
    - MainView 의 로컬 `bool isEditMode = ...` 변수 0 (GetDatumEditMode helper 호출만)
  </how-to-verify>
  <verify>
    사용자 시나리오 1~7 PASS + grep 검증.
  </verify>
  <done>
    사용자가 "approved" 또는 시나리오 1~7 모두 PASS 보고.
  </done>
  <resume-signal>
    사용자 "approved" 시 진행. fitting 원 미표시 / DETECT FAIL 회귀 / VTH/TLI 동작 변경 / Phase 36 swap 회귀 발생 시 (CO 신설) 보고.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MainView.btn_teachDatum.IsChecked ↔ SetDatumOverlay | UI thread → MainResultViewerControl. W4 IsDatumTeachActive 프로퍼티 wrapper 가 외부 접근 안전화 |
| _datumIsEditMode 필드 ↔ Render | UI thread 단일 — race condition 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-04-01 | T (Tampering) | btn_teachDatum.IsChecked == null → NRE | mitigate | GetDatumEditMode() / IsDatumTeachActive 둘 다 `btn_teachDatum != null && IsChecked == true` null-guard |
| T-39.1-04-02 | I (Info Disclosure) | cthHideRois 가드 우회 시 ROI 좌표 노출 | accept | INI 평문 — UI hide 는 UX 명확화 |
| T-39.1-04-03 | T (Tampering) | DETECT FAIL 라벨 분기 회귀 → Phase 39 hotfix 무력화 | mitigate | grep 검증 (`RuntimeDetectFailed`, `DETECT FAIL`). SIMUL UAT 시나리오 4 |
| T-39.1-04-04 | T (Tampering) | Phase 36 swap SetDatumOverlay 인자 누락 | mitigate | grep 검증 (swap 호출처 3-arg). SIMUL UAT 시나리오 7 |
| T-39.1-04-05 | E (Elevation) | InspectionListView 가 btn_teachDatum 직접 접근 시 access modifier 위반 | mitigate | W4 IsDatumTeachActive 프로퍼티 wrapper. grep 검증 (`mainView.btn_teachDatum` 0 hit) |
</threat_model>

<verification>
**자동 검증:**
- [ ] msbuild Debug/x64 PASS
- [ ] HalconDisplayService.RenderDatumOverlay 시그니처에 `bool isEditMode = false`
- [ ] cthHideRois 가드 + Circle ROI / Horizontal_A/B 분기 가드
- [ ] MainResultViewerControl.SetDatumOverlay 시그니처 + _datumIsEditMode 필드
- [ ] MainView GetDatumEditMode helper (12 hit) + IsDatumTeachActive 프로퍼티 (1 hit)
- [ ] MainView 11 호출처 3-arg + GetDatumEditMode() 호출 (변수 도입 0)
- [ ] InspectionListView 2 호출처 3-arg + IsDatumTeachActive 프로퍼티 사용 (btn_teachDatum 직접 접근 0)

**Anti-goal 회귀:**
- [ ] HalconDisplayService LastTeachSucceeded 블록 (fitting 원 + raw points) 변경 0
- [ ] RenderDatumFindResult 호출 블록 변경 0 (Phase 36 CO-36-03)
- [ ] DETECT FAIL 라벨 분기 변경 0 (Phase 39 CO-39-02/03)
- [ ] LastTeachSucceeded 블록 안 `#90EE90` hex 색상 보존
- [ ] TwoLineIntersect / VTH / VTH-DualImage 변경 0
- [ ] RefOrigin magenta 십자 변경 0
- [ ] DatumConfig.cs / DatumFindingService.cs 변경 0
- [ ] btn_teachDatum_Click 변경 0
- [ ] project_roi_edit_mode_deferred 미통합 (D-G4-04)

**SIMUL UAT:**
- [ ] 7 시나리오 모두 PASS
</verification>

<success_criteria>
**M4:**
- RenderDatumOverlay isEditMode + cthHideRois ✓
- SetDatumOverlay 13 호출처 인자 전달 (W3 helper + W4 프로퍼티) ✓
- CTH 평소 (LastTeachSucceeded + !isEditMode) → fitting 원 + 십자 만 ✓
- CTH Edit (LastTeachSucceeded + isEditMode) → fitting 원 + ROI 핸들 동시 ✓
- CTH 티칭 미완료 → 기존 동작 ✓

**M5 (Phase 39 / 36 / 11 / 13 / 16 회귀 0):**
- DETECT FAIL 라벨 변경 0 ✓
- RenderDatumFindResult 변경 0 ✓
- LastTeachSucceeded fitting 원 + raw points 변경 0 ✓
- TwoLineIntersect / VTH / VTH-DualImage 변경 0 ✓
- `#90EE90` hex 보존 ✓
- btn_teachDatum_Click 변경 0 ✓
- DatumConfig / DatumFindingService 변경 0 ✓
- W3 변수 shadowing 회피 (helper 메서드) ✓
- W4 encapsulation (IsDatumTeachActive 프로퍼티) ✓
</success_criteria>

<output>
After completion, create `.planning/phases/39.1-inspection-urgent-fixes-2026-05-29/39.1-04-SUMMARY.md`.

Include:
- Tasks executed (Task 1 + Task 2)
- 5 파일 수정 그래프 (HalconDisplayService / MainResultViewerControl / MainView.xaml / MainView.xaml.cs / InspectionListView)
- msbuild Debug/x64 PASS 결과
- Grep 검증 결과 (isEditMode / cthHideRois / GetDatumEditMode 12 hit / IsDatumTeachActive / 13 호출처 / fitting 원 / DETECT FAIL 보존)
- W3 helper / W4 프로퍼티 적용 확인
- SIMUL UAT 7 시나리오 결과
- Phase 39 / 36 / 14-03 W4-A / 11 / 13 / 16 회귀 0 증거
</output>
