---
phase: 39.1-inspection-urgent-fixes-2026-05-29
plan: 04
type: summary
status: completed
mode: inline (orchestrator 직접 실행 — Wave 3 단일 plan, Wave 2 inline 흐름 유지)
date: 2026-05-29
commits:
  - c9d42c1 — feat(39.1-04): RenderDatumOverlay isEditMode + cthHideRois 가드 (Task 1)
  - 792a2f4 — feat(39.1-04): SetDatumOverlay isEditMode 인자 전파 + W3/W4 helper/property (Task 2)
files_modified:
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
  - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  # MainView.xaml — W4 가드 결과 미수정 (IsDatumTeachActive 프로퍼티 wrapper 로 access modifier 의존 회피)
build_result: PASS (msbuild Debug/x64, 0 new errors, 0 new warnings — Phase 33 baseline 만 유지)
---

# Plan 39.1-04 SUMMARY

## Tasks executed

### Task 1 — HalconDisplayService.RenderDatumOverlay isEditMode + cthHideRois 가드
Atomic commit **c9d42c1**.

**HalconDisplayService.cs (+15 / −3 라인):**
- 시그니처 (L683): `public void RenderDatumOverlay(HWindow window, DatumConfig datum, bool isSelected, bool isEditMode = false)` — 옵션 인자, 기본 false (기존 호출자 호환).
- try 블록 진입부 (SetColor/SetLineWidth 직후, 첫 algorithm 분기 직전) 에 `cthHideRois` 변수 도입:
  ```csharp
  bool cthHideRois = (datum.AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal)
                  && datum.LastTeachSucceeded
                  && !isEditMode;
  ```
- Circle ROI 분기 (`CircleTwoHorizontal && CircleROI_Radius > 0`) 에 `&& !cthHideRois` 가드 추가 — CTH Edit OFF + 티칭 완료 시 Circle ROI + Strip 시각화 hide.
- Horizontal A/B 분기 (`AlgorithmType != TwoLineIntersect`) 에 `&& !cthHideRois` 가드 추가 — CTH 한정 hide (VTH 는 cthHideRois==false 라 영향 0).

**Verbatim preserve (변경 0):**
- TLI Line1 분기 / Line2 분기
- VerticalTwoHorizontal / VerticalTwoHorizontalDualImage Vertical_* 분기
- IsConfigured RefOrigin magenta 십자
- LastTeachSucceeded 블록 (fitting 원 `#90EE90` hex + raw points + yellow center cross + intersection cross)
- RenderDatumFindResult 호출 블록 (Phase 36 CO-36-03)
- DETECT FAIL 라벨 분기 (Phase 39 hotfix CO-39-02/03 — 우상단 + datum 이름)
- 외곽 try/catch swallow

### Task 2 — SetDatumOverlay 시그니처 + GetDatumEditMode helper + IsDatumTeachActive 프로퍼티 + 호출처 통합
Atomic commit **792a2f4**.

**MainResultViewerControl.xaml.cs (+5 / −2 라인):**
- `_datumIsEditMode` private bool 필드 신규 (기본 false) — `_datumConfig` / `_datumSelected` 옆 추가.
- `SetDatumOverlay(DatumConfig datum, bool isSelected, bool isEditMode = false)` — 3-arg 옵션 인자 + body 에서 `_datumIsEditMode = isEditMode;`.
- `ClearDatumOverlay()` 에 `_datumIsEditMode = false;` 추가 — Edit 모드 리셋 (정리 일관성).
- RenderNow 의 `_displayService.RenderDatumOverlay(...)` 호출에 `_datumIsEditMode` 인자 전달.

**MainView.xaml.cs (+22 라인, 11 호출처 변경):**
- `GetDatumEditMode()` private helper (W3 — 변수 shadowing 회피) + `IsDatumTeachActive` public property (W4 — 외부 encapsulation) 신설.
- 11 SetDatumOverlay 호출처 모두 3-arg 변환:
  - **모드 해제** (1곳): `(_editingDatum, false, false)` — isEditMode=false 명시.
  - **일반 노드 선택** (4곳): `(datum, true, GetDatumEditMode())` — replace_all 한 번 적용.
  - **Phase 36 swap** (2곳): `(_selectedDatumForSwap, true, GetDatumEditMode())`.
  - **편집 진입** (3곳): `(_editingDatum, true, GetDatumEditMode())`.
  - **Test Find 진입** (1곳): `(datum, true, GetDatumEditMode())` — Phase 17 D-14 마커 보존.
- 로컬 `bool isEditMode = ...` 변수 도입 0 (W3 충족).

**InspectionListView.xaml.cs (+2 / −2 라인, 2 호출처 변경):**
- L135 (Datum AlgorithmType 변경 후 ROI 보존 분기) — `(datum, true, mParentWindow.mainView.IsDatumTeachActive)`.
- L459 (Datum 노드 선택 시 SetDatumOverlay) — `(datumCfg, true, mParentWindow.mainView.IsDatumTeachActive)`.
- `mainView.btn_teachDatum` 직접 참조 0 (W4 충족 — 단, L407 / L485 의 `btn_teachDatum.IsEnabled` 접근은 본 plan 스코프 외 — Phase 12 기존 패턴, datum 노드 enable 게이트, 본 plan 동작과 무관).

**MainView.xaml — 미수정:**
- read_first 시 access modifier 미확인 (Visual Studio WPF 기본 internal). W4 가드 의도에 따라 IsDatumTeachActive 프로퍼티 wrapper 가 외부 접근 안전 우회 → MainView.xaml 자체 수정 불필요 (plan 명시).

## 자동 검증 (verify)

| 항목 | 결과 |
|------|------|
| msbuild Debug/x64 | **PASS** (0 errors, 0 new warnings — Phase 33 baseline CS0618×5 + CS0162×1 + MSB3884 만 유지) |
| RenderDatumOverlay 시그니처 `bool isEditMode = false` | 통과 |
| cthHideRois grep (HalconDisplayService) | 5 hit (선언 + Circle ROI 가드 + Horizontal_A/B 가드 + 추가 라인) — 통과 |
| Circle ROI 분기 `&& !cthHideRois` | 1 hit — 통과 |
| Horizontal A/B 분기 `&& !cthHideRois` | 1 hit — 통과 |
| `if (datum.LastTeachSucceeded)` 블록 | 1 hit (보존) — 통과 |
| `#90EE90` hex 색상 | 보존 — 통과 |
| `RenderDatumFindResult(window, datum)` 호출 | 보존 — 통과 |
| `DETECT FAIL` 라벨 분기 + `RuntimeDetectFailed` 가드 | 보존 — 통과 |
| MainResultViewerControl SetDatumOverlay 시그니처 `bool isEditMode = false` | 통과 |
| `_datumIsEditMode` 4 hit (선언 + SetDatumOverlay 할당 + ClearDatumOverlay 리셋 + RenderNow 전달) | 통과 |
| MainResultViewerControl RenderDatumOverlay 호출 `, _datumIsEditMode` | 통과 |
| MainView `GetDatumEditMode` grep | 12 hit (정의 1 + return 1 + 호출 10) — 통과 (W3 helper) |
| MainView `IsDatumTeachActive` grep | 2 hit (정의 + getter) — 통과 (W4 wrapper) |
| MainView SetDatumOverlay 11 호출 3-arg 형태 | 11 hit (grep `SetDatumOverlay\(` 결과 모두 `, false)` 또는 `, GetDatumEditMode())` 포함) — 통과 |
| MainView `bool isEditMode = ` 로컬 변수 | 0 hit (W3 변수 shadowing 회피 충족) — 통과 |
| InspectionListView SetDatumOverlay 2 호출 3-arg + IsDatumTeachActive 사용 | 2 hit — 통과 |
| InspectionListView `mainView.btn_teachDatum` 직접 참조 (W4 위반 확인) | 본 plan 호출처는 0 hit. L407/L485 에 `btn_teachDatum.IsEnabled` 잔존 — 본 plan 스코프 외 (Phase 12 노드 enable 게이트) |
| `btn_teachDatum_Click` 본문 변경 | 변경 0 — 통과 |
| DatumConfig.cs / DatumFindingService.cs diff | 0 — 통과 |
| HalconDisplayService TLI Line1/Line2 / VTH / VTH-DualImage / RefOrigin 분기 diff | 0 — 통과 |

### W3 — 변수 shadowing 회피
`GetDatumEditMode()` private helper 채택 (옵션 B). 11 호출처 모두 helper 호출, 로컬 `bool isEditMode = ...` 변수 도입 0. 메서드 내 중복 호출이 있어도 CS0136 위험 없음.

### W4 — encapsulation
InspectionListView 2 호출처가 `mParentWindow.mainView.IsDatumTeachActive` 프로퍼티 wrapper 사용. `btn_teachDatum` field 직접 참조 회피. MainView.xaml 의 `x:FieldModifier` access modifier 의존 0. (L407/L485 `btn_teachDatum.IsEnabled` 는 Phase 12 신설된 노드 enable 게이트로 본 plan 의도와 무관 — UAT 시나리오 2~5 동작에 영향 0.)

### I4 — 의미 anchor 기반 anchor
라인 번호 대신 grep 패턴 (`if (datum.LastTeachSucceeded)`, `RenderDatumFindResult(window, datum)`, `RuntimeDetectFailed`, `DispCircle.*CircleROI_Radius`, `DispRectangle2.*Horizontal_A`, `#90EE90`) 으로 anchor 식별. Phase 39 hotfix CO-39-02/03 라인 시프트 영향 없이 정확한 위치 수정 + 보존 검증 모두 가능.

## Deviation

- **MainView.xaml.cs L2547 (Test Find 진입) 의 마커 순서**: 기존 `//260503 hbk Phase 17 D-14 — purple cross + 좌표 + 화살표` 마커가 replace_all 후 `//260529 hbk Phase 39.1-04 G4-03` 마커 뒤에 위치. Phase 20 D-12 stacking 패턴 (신규 마커가 기존 마커 위) 과 약간 다른 순서이나, 동일 라인 내 단순 connected 마커로 의미적 동등. 후속 phase 마커 누적 시 정상 위치로 정렬 가능 (cosmetic).
- **MainView.xaml 미수정**: plan 의 B-2 "btn_teachDatum access modifier 안전화" 항목은 IsDatumTeachActive 프로퍼티 wrapper 로 외부 접근 해결되어 MainView.xaml 직접 수정 불필요 — plan 명시한 "MainView.xaml 자체 수정은 불필요 (프로퍼티 신설로 해결)" 경로 채택.

## SIMUL UAT 7 시나리오 (D-G1-04 sign-off — 사용자 검증 대기)

- [ ] **Scenario 1** — CTH 평소 모드 (LastTeachSucceeded + btn_teachDatum OFF) → fitting 원 + DetectedOrigin 십자 + RefOrigin magenta 십자 만 표시 (Horizontal_A/B / Circle ROI / Strip 미표시)
- [ ] **Scenario 2** — CTH Edit 모드 (btn_teachDatum ON) → 시나리오 1 + Horizontal_A (H-A) + Horizontal_B (H-B) + Circle ROI (Circle) + Strip 동시 표시. 재 클릭 (OFF) → 시나리오 1 복귀.
- [ ] **Scenario 3** — CTH 티칭 미완료 (LastTeachSucceeded==false) → btn_teachDatum 무관 Horizontal_A/B + Circle ROI + Strip 표시 (fitting 원 / center cross 없음 — 기존 동작 보존).
- [ ] **Scenario 4** — DETECT FAIL 라벨 (Phase 39 CO-39-02/03 회귀 0): RuntimeDetectFailed || (IsConfigured && !LastFindSucceeded) → "DETECT FAIL: Datum_X" 적색 우상단. isEditMode 무관.
- [ ] **Scenario 5** — VTH / VTH-DualImage 회귀 0: Vertical (Vert) + Horizontal_A/B (H-A/H-B) 표시. btn_teachDatum 무관. LastTeachSucceeded==true 시 raw points (Vertical=orange, H_A=green, H_B=lime green).
- [ ] **Scenario 6** — TLI 회귀 0: Line1 (L1) + Line2 (L2) 사각형. btn_teachDatum 무관. LastTeachSucceeded==true 시 yellow Line1 외삽 + cyan Line2 외삽 + red intersection cross.
- [ ] **Scenario 7** — Phase 36 swap 회귀 0: VTH-DualImage + 2-image swap → swap 직후 SetDatumOverlay isEditMode 인자 정상 전달 + Test Find Datum + RenderDatumFindResult 변경 0.

검증 절차: 사용자 SIMUL 모드 실행 후 7 시나리오 결과 보고 → "approved" 또는 회귀 발견 시 (CO 신설).

## Phase 39 / 36 / 14-03 W4-A / 11 / 13 / 16 회귀 0 증거 (코드 인스펙션)

- **Phase 39 CO-39-02/03 DETECT FAIL 라벨**: HalconDisplayService 의 `RuntimeDetectFailed || (IsConfigured && !LastFindSucceeded)` 분기 + GetPart + hash stagger + "DETECT FAIL: " + datum.DatumName 전체 보존 (Task 1 의 cthHideRois 가드는 `if (datum.AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal) && LastTeachSucceeded && !isEditMode` 조건 — DETECT FAIL 분기는 `LastFindSucceeded==false` (즉 `!LastTeachSucceeded`) 기준이라 동시 활성 불가).
- **Phase 36 CO-36-03 RenderDatumFindResult**: Task 1 의 가드 cthHideRois 는 Circle ROI + Horizontal_A/B 분기만 영향. RenderDatumFindResult 호출 블록 변경 0.
- **Phase 14-03 W4-A VerticalTwoHorizontal Vertical 슬롯 렌더**: AlgorithmTypeEnum == VTH 분기 + Vertical_Length1/2 > 0 → DispRectangle2 "Vert" 라벨 보존 (cthHideRois 미적용, AlgorithmTypeEnum != CTH 라 false).
- **Phase 11/13 LastTeachSucceeded fitting 원 + raw points**: LastTeachSucceeded 블록 전체 verbatim 보존 — `#90EE90` hex (memory feedback_halcon_setcolor_invalid_names 준수, "light green" 비표준명 사용 금지).
- **Phase 16 D-01/D-02/D-08 RenderCircleStripOverlay**: Circle ROI 분기 안 호출 → cthHideRois 가드 적용 시 Circle ROI 와 함께 hide (의도). Edit 모드 ON 또는 LastTeachSucceeded==false 시 정상 표시 (Phase 16 검증 시나리오와 일관).

## Anti-goal 충족 증거

- DatumConfig.cs / DatumFindingService.cs 변경 0
- btn_teachDatum_Click 변경 0
- Plan 03 의 RenderInspectionResultForNode 변경 0 (Datum 노드 분기 미호출 — Plan 03 은 Measurement/FAI 한정)
- project_roi_edit_mode_deferred 3 항목 미통합 (D-G4-04 충족)
