# Phase 39.1: 검사 워크플로우 긴급 fixes — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `39.1-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-05-29
**Phase:** 39.1-inspection-urgent-fixes-2026-05-29
**Areas discussed:** G1 범위/분할, G2 Item #1 CircleDiameter, G3 Item #2 EdgeToLineDistance, G4 Item #3+#4 UI

---

## Pre-discussion: 시드 + 코드 스카웃 결과

기존 seed CONTEXT.md (status=seed) 가 4 항목 + 7 placeholder (A1~A7) 보유. 코드 스카웃을 통해 다음 발견:
- Item #1: `EdgeOptionLists.FaiCircle*` 상수 4종이 `CircleDiameterMeasurement.cs` polar 분기에서 사용자 override 0 으로 사용 중
- Item #2: `projection_pl` 은 이미 구현됨. `DatumAngle2Rad` 가 주입 경로 (Action_FAIMeasurement L217) 에 있으나 EdgeToLineDistance 가 미사용
- Item #3: `HighlightSelectedRoi` + `FindFAIContainingMeasurement` 부분 구현 (Phase 37 hotfix A/B)
- Item #4: `RenderDatumOverlay` L822 `LastTeachSucceeded` 분기가 fitting 원 + ROI 핸들 동시 표시. Edit 분리 모드 없음

7 placeholder 를 코드 컨텍스트로 4 그룹 축약: G1 범위/분할 (A5+A6), G2 #1 파라미터 (A1), G3 #2 수정 (A2), G4 #3+#4 UI (A3+A4+A7).

---

## G1: 범위/분할

| Option | Description | Selected |
|--------|-------------|----------|
| 4 항목 일괄 (추천) | #1+#2+#3+#4 모두 39.1, plan 4개 예상 | ✓ |
| #1+#2 만 39.1 (#3/#4 이월) | POC 직접영향만 39.1, 나머지 Phase 40 이후 | |
| 분할 (39.1=#1+#2, 39.2=#3+#4) | 단위 명확, 단기 출시 + 순차 병행 | |

**User's choice (Q1):** 4 항목 일괄
**Notes:** POC 시연 전 단일 처리로 컨텍스트 일관성 + UAT 일괄 sign-off 우선.

| Option | Description | Selected |
|--------|-------------|----------|
| 4 plan (추천) | 항목당 1 plan, SUMMARY 추적성 명확 | ✓ |
| 2 plan (algorithm/UI) | 관련성 기반 병합 | |
| 3 plan (#1 / #2 / #3+#4) | POC 우선 분리, UI 합침 | |

**User's choice (Q2):** 4 plan
**Notes:** 항목당 1 plan = SUMMARY 추적성 명확.

| Option | Description | Selected |
|--------|-------------|----------|
| 2 wave (추천): wave1=#1+#2 / wave2=#3+#4 + UAT | algorithm 먼저, UI 장착, UAT 통합 | ✓ |
| 3 wave: wave1=#1+#2 / wave2=#3+#4 / wave3=UAT | UAT 독립 wave | |
| 4 wave (항목당 1 wave) | 완전 순차, 병렬 이득 0 | |

**User's choice (Q3):** 2 wave
**Notes:** Wave 간 데이터 의존 0 → Wave 1 결과 영향 없이 Wave 2 동시 진행 가능.

| Option | Description | Selected |
|--------|-------------|----------|
| 4 항목 각자 SIMUL UAT + sign-off (추천) | POC 안전성 장치 | ✓ |
| #1+#2 SIMUL UAT 만 / #3+#4 시각 확인 | POC 직접영향만 정식, UI 약식 | |
| 실HW 대기 | Simul 먼저 sign-off, 실HW carry-over | |

**User's choice (Q4):** 4 항목 각자 SIMUL UAT
**Notes:** Phase 39 와 동일한 UAT 정합성 패턴.

---

## G2: Item #1 CircleDiameter 파라미터 노출

| Option | Description | Selected |
|--------|-------------|----------|
| polar step + L1Ratio + L2Ratio + EdgeSelection (추천) | 4 필드 모두 인스턴스 필드화 | ✓ |
| polar step + L1 + L2 (3 필드) | EdgeSelection 상수 유지 | |
| polar step 만 (1 필드) | 사용자 원문 'L1, L2 누락' 충족 불가 | |

**User's choice (Q1):** 4 필드 노출
**Notes:** memory `feedback_halcon_measurepos_must_haves` 3 필수 (strip-loop + measurePhi + EdgeSelection) 중 measurePhi 는 내부, polar step/L1/L2/selection 은 사용자 제어.

| Option | Description | Selected |
|--------|-------------|----------|
| Ratio (0.0~1.0, 원 지름 대비, 추천) | 현 구현 일치, INI byte-identical | ✓ |
| px 절대값 | 사용자 직관적, 원 크기 따라 수동 조정 필요 | |
| 둘 다 (Ratio 기본 + AbsPx override) | 유연하지만 UI 복잡 | |

**User's choice (Q2):** Ratio
**Notes:** 일관 polar strip 크기가 원 지름에 자동 비례 → 큰 원/작은 원 동일 동작.

| Option | Description | Selected |
|--------|-------------|----------|
| sentinel 0/"" + EnsurePerRoiDefaults idempotent migration (추천) | Phase 13/22 패턴 차용 | ✓ |
| default 값 상수 직접 설정 | 코드 간결하지만 INI 0 덮어쓰기 위험 | |
| 다중 소스 (Datum 수동 일치 요구) | Datum 정적 상수 + FAI 인스턴스 필드 분리 | |

**User's choice (Q3):** sentinel + EnsurePerRoiDefaults
**Notes:** Phase 28 D-04 default-equality 동등성 가드 적용으로 INI 회귀 0 byte-identical.

| Option | Description | Selected |
|--------|-------------|----------|
| polar 전용 필드는 RadialDirection 설정 시만 노출 (추천) | FAIConfig.GetProperties 패턴 | ✓ |
| 항상 노출 (Phase 28 REQ-28-05 정책) | 구현 간단, fit 경로 사용자 혼원 위험 | |
| Category 분리 (Edge / Polar) 만 | 단순 그룹화, 투자 적은 중간안 | |

**User's choice (Q4):** RadialDirection 설정 시만 polar 필드 노출
**Notes:** Phase 19 QUAL-03 동적 노출 패턴 차용.

---

## G3: Item #2 EdgeToLineDistance 수정 방향

| Option | Description | Selected |
|--------|-------------|----------|
| DatumAngleRad+90° 가정 파괴 (추천) | measureX 분기에서 실제 DetectedRefAngle2 와 다를 때 오차 | ✓ |
| DatumOriginRow/Col 주입 잘못 | Action_FAIMeasurement 주입 로직 의심 | |
| projection_pl 인자 부호 오류 | axis 방향벡터 (cosθ,-sinθ) 오류 가능성 | |
| 모르겠습니다 (Phase 안에서 재현 우선) | discuss 단계 원인 확정 어려움 | |

**User's choice (Q1):** DatumAngleRad+90° 가정 파괴
**Notes:** DatumAngle2Rad 가 이미 주입 경로 있음 (Action_FAIMeasurement L217) → 사용하도록 변경.

| Option | Description | Selected |
|--------|-------------|----------|
| measureX 분기만 DatumAngle2Rad 사용 (추천) | measureY = DatumAngleRad 유지, 회귀 0 | ✓ |
| axis 정의 완전 재설계 (두 축 모두) | 구현 큼, 회귀 위험 | |
| 클라이언트측 boundary 검증 추가 | 증상 완화, 근본 해결 아님 | |

**User's choice (Q2):** measureX 분기만 DatumAngle2Rad 사용
**Notes:** measureY (수평선까지 거리) 분기는 회귀 0 보장.

| Option | Description | Selected |
|--------|-------------|----------|
| 인라인 바로 수정 (현 위치) | Phase 31 Task 3(C) 보류 가드 유지 | ✓ |
| ComputeProjectionDistance 로 통합 (Phase 31 패턴, 추천) | static 헬퍼 공용화 | |
| 둘 공존 (인라인 유지 + 신규 케이스 헬퍼) | 이중 유지 부담 | |

**User's choice (Q3):** 인라인 (현 위치)
**Notes:** Phase 31 D-04 보류 결정 유지 (overlay footRow/footCol 회귀 방지).

| Option | Description | Selected |
|--------|-------------|----------|
| DatumAngle2Rad==0 이면 현재 로직 (추천) | 레거시/TLI Datum 회귀 0 가드 | ✓ |
| DatumAngle2Rad 없으면 measureX 자체 실패 | 명시적 error, Phase 31 TLI 회귀 위험 | |
| DatumConfig.AlgorithmType 기반 분기 | 명시적이지만 EdgeToLineDistance 에 DatumConfig 참조 추가 필요 | |

**User's choice (Q4):** DatumAngle2Rad==0 시 현재 로직 폴백
**Notes:** CTH/VTH Datum 사용 시에만 실제 변경 적용. sentinel 0 = datum 2차 기준선 없는 상황 안전 처리.

---

## G4: Item #3+#4 UI 설계

| Option | Description | Selected |
|--------|-------------|----------|
| B (검사 후 FAI 노드 클릭 조회, 추천) | Sequence 동작 변경 0, 부분 구현 확장 | ✓ |
| C (Measure Selected FAI 컨텍스트 메뉴) | 디버그 단축, sequence 우회 큰 변경 | |
| A (FAI 단위 sequence 우회 API) | anti-goal, POC 회귀 위험 | |

**User's choice (Q1):** B 검사 후 FAI 노드 클릭 조회
**Notes:** Phase 37 lenient + Phase 39 gate 회귀 0 = POC 안전 우선.

| Option | Description | Selected |
|--------|-------------|----------|
| 전 FAI 타입 공통 (추천) | 모든 Measurement 타입 동일 동작 | ✓ |
| POC 핵심 타입만 (CircleDiameter + EdgeToLineDistance) | POC 시연 필수 2 타입만, 나머지 carry-over | |
| Selected Measurement 노드만 (이미 부분 구현) | Quick 260519-c08 패턴, FAI 노드는 안 건드림 | |

**User's choice (Q2):** 전 FAI 타입 공통
**Notes:** 타입별 분기 없이 일반화.

| Option | Description | Selected |
|--------|-------------|----------|
| btn_teachDatum 재사용 (추천) | 기존 토글 활용, 새 UI 0 | ✓ |
| 이미지 더블클릭 (새 일관성 패턴) | project_roi_edit_mode_deferred 와 일관 | |
| PropertyGrid 안 'Edit' 버튼 필드 | 명시 트리거, 컨트롤 이원화 단점 | |

**User's choice (Q3):** btn_teachDatum 재사용
**Notes:** 새 버튼/UI 추가 0, 기존 사용자 워크플로우와 자연 일치.

| Option | Description | Selected |
|--------|-------------|----------|
| 통합 X — Item #4 는 CTH 원 한정 (추천) | 범위 명확, POC 일정 안전 | ✓ |
| Polygon 이동 만 추가 통합 | deferred 3건 중 1건만 흡수 | |
| Edit 모드 전면 재설계 일괄 | POC 일정 위험 + 범위 폭발 | |

**User's choice (Q4):** 통합 X
**Notes:** ROI 이동 전면 재설계는 별도 phase (v1.3 후보) 로 분리.

---

## Claude's Discretion

다음 항목들은 Plan 단계 implementation 판단에 위임:
- `EnsurePerRoiDefaults` migration 안에서 신규 4 필드 default 채움 위치 (FAIConfig vs CircleDiameterMeasurement ctor)
- ICustomTypeDescriptor 구현 시 DynamicPropertyHelper.FilterProperties 위임 vs 직접 구현
- `_canvasMode = TeachDatum` 진입/탈출 로직은 기존 패턴 유지 (변경 0)
- `260529 hbk Phase 39.1` 마커 사용 (memory feedback_comment_convention)
- HALCON SetColor 표준 색상명 (memory feedback_halcon_setcolor_invalid_names) 점검

## Deferred Ideas

- `project_roi_edit_mode_deferred` 3 항목 (Rect 회귀 / Edit 모드 전면 재설계 / Polygon 이동) — 별도 phase 후보 (v1.3 또는 POC 후)
- Phase 31 Task 3(C) `EdgeToLineDistance` 의 `ComputeProjectionDistance` 헬퍼 통합 — 신규 측정 타입과 함께 별도 phase 재검토
- Item #3 옵션 C (Measure Selected FAI 컨텍스트 메뉴) — 디버그 빌드 한정 기능 후보
- Item #4 더블클릭 / PropertyGrid Edit 버튼 진입 트리거 — `btn_teachDatum` 재사용 채택으로 보류
