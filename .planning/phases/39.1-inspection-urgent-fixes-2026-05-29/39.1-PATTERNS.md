---
phase: 39.1-inspection-urgent-fixes-2026-05-29
type: patterns
status: ready_for_planning
date: 2026-05-29
upstream: 39.1-CONTEXT.md
downstream: 39.1-{01..04}-PLAN.md
---

# Phase 39.1: 검사 워크플로우 긴급 fixes — Pattern Map

**Mapped:** 2026-05-29
**Files analyzed:** 13 (modify 5 + read-only-ref 8)
**Analogs found:** 13 / 13 (모두 within-codebase, 신규 파일 0)

## File Classification (전 4 항목 합산)

| 파일 | 역할 | Role | Item | Closest Analog | Match Quality |
|------|------|------|------|----------------|---------------|
| `WPF_Example/Custom/Sequence/Inspection/Measurements/CircleDiameterMeasurement.cs` | modify | measurement(impl) | #1 | `CircleCenterDistanceMeasurement.cs` (Phase 31) | exact (polar+fit 2분기 동형) |
| `WPF_Example/Custom/Sequence/Inspection/EdgeOptionLists.cs` | read-only-ref | constants(static-source) | #1 | (self — default 상수 소스) | n/a |
| `WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs` | read-only-ref | service(halcon) | #1 | (호출 시그니처만 참조) | n/a |
| `WPF_Example/Halcon/Algorithms/DatumFindingService.cs` | **read-only ANTI-GOAL** | service(halcon) | #1 | (Datum CTH polar 경로 변경 0) | n/a |
| `WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs` | read-only-ref | model+ICustomTypeDescriptor | #1 | (자체 패턴 참고, 변경 X) | exact |
| `WPF_Example/Custom/Sequence/Inspection/DynamicPropertyHelper.cs` | read-only-ref | helper(static) | #1 | (Phase 19 헬퍼 위임 패턴) | exact |
| `WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs` | modify | measurement(impl) | #2 | `EdgeToLineDistanceMeasurement.cs` (self, measureY 분기 분석) | exact |
| `WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` | read-only-ref | action(controller) | #2 | (주입 경로 L214-217 정상 동작) | exact |
| `WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` | read-only-ref | model | #2, #4 | (DetectedRefAngle2 sentinel 0 확인) | exact |
| `WPF_Example/Custom/Sequence/Inspection/Measurements/IDatumOriginConsumer.cs` | read-only-ref | interface | #2 | (DatumAngle2Rad 인터페이스 정의 보유) | exact |
| `WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` | modify | view-codebehind(UI) | #3 | (자체 SelectionChanged 4 분기) | exact |
| `WPF_Example/UI/ContentItem/MainView.xaml.cs` | modify | view-codebehind(UI) | #3, #4 | (자체 Display*+HighlightSelectedRoi+RenderImage 컨벤션) | exact |
| `WPF_Example/Halcon/Display/HalconDisplayService.cs` | modify | service(halcon-display) | #4 | (RenderDatumOverlay L683~966 self-analog) | exact |

**Convention notes (전 항목 공통):**
- `260529 hbk Phase 39.1` 마커 — Phase 20 D-12 stacking 패턴 (변환 라인 마커 교체, 비변환 라인 보존, 누적 marker)
- **K&R brace style** — 모든 수정 대상 파일이 K&R 일관 (Logging 컨벤션, `FAIConfig`/`Action_FAIMeasurement`/`InspectionListView`/`MainView` 동일). HalconDisplayService 만 Allman — 그 파일 안에서는 Allman 유지.
- C# 7.2 제한 — `nullable reference types`/`switch expressions`/`record` 사용 금지. `is` 패턴 매칭 (`is FAIConfig fai`) 은 허용.
- HALCON `SetColor` — 표준 색상명만 사용 (memory `feedback_halcon_setcolor_invalid_names`). `"light green"` 같은 비표준 → catch swallow → silent 미표시 1순위 의심.
- HALCON `MeasurePos` — strip-loop + measurePhi + EdgeSelection 3 필수 (memory `feedback_halcon_measurepos_must_haves`).
- 기존 `try { } catch { ... }` swallow 컨벤션 유지 — Halcon display 에러는 silent 처리 (RenderDatumOverlay 관습).
- `//YYMMDD hbk` 주석 컨벤션 (memory `feedback_comment_convention`).

---

## Item #1 — CircleDiameter 파라미터 노출

**Phase decision anchors:** D-G2-01 (4 필드 노출) / D-G2-02 (Ratio 채택) / D-G2-03 (sentinel + EnsurePerRoiDefaults migration) / D-G2-04 (ICustomTypeDescriptor 동적 노출) / D-G2-05 (anti-goal — Datum CTH 변경 0)

### 1.1 `CircleDiameterMeasurement.cs` (수정 대상)

**Role:** measurement(impl) — `MeasurementBase` 파생
**Closest analog:** `CircleCenterDistanceMeasurement.cs` (Phase 31 CO-23.1-02 hotfix — 동일 polar/fit 2-분기 구조이며 Phase 28 default 와 동일 4-필드 보유)

**Current state (L70-93, polar 분기 — Phase 28 D-04 정적 상수 참조):**
```csharp
//260508 hbk Phase 28 REQ-28-02/REQ-28-03 (D-02, D-04, D-08) — polar 경로:
//  polarity = MapRadialDirectionToHalconPolarity(Circle_RadialDirection) (단일 소스, EdgePolarity 무시)
//  step/L1/L2/selection = EdgeOptionLists.FaiCircle* defaults (Datum CTH default 와 동일 → 동등성 결정적)
string polarity = EdgeOptionLists.MapRadialDirectionToHalconPolarity(Circle_RadialDirection);
HTuple unusedRows, unusedCols;
bool[] unusedStrips;
if (!svc.TryFindCircleByPolarSampling(
    image,
    Circle_Row, Circle_Col, Circle_Radius,
    EdgeOptionLists.FaiCirclePolarStepDeg,   // ← 인스턴스 필드로 교체
    EdgeOptionLists.FaiCircleRectL1Ratio,    // ← 인스턴스 필드로 교체
    EdgeOptionLists.FaiCircleRectL2Ratio,    // ← 인스턴스 필드로 교체
    Sigma, EdgeThreshold, polarity,
    EdgeOptionLists.FaiCircleEdgeSelection,  // ← 인스턴스 필드로 교체
    datumTransform,
    out foundRow, out foundCol, out foundRadius,
    out unusedRows, out unusedCols, out unusedStrips,
    out error))
{
    return false;
}
```

**Pattern to apply (4 신규 인스턴스 필드 + Circle_RadialDirectionList ComboBox 패턴 동형):**
```csharp
//260529 hbk Phase 39.1 G2-01/G2-04 — polar 4 필드 인스턴스화 (PropertyGrid 노출, ICustomTypeDescriptor 가 Circle_RadialDirection 빈값 시 hide)
[Category("Edge")] //260529 hbk Phase 39.1 G2-01 — Edge 카테고리, polar 분기 활성 시에만 표시
public double Circle_PolarStepDeg { get; set; } = 10.0;   //260529 hbk Phase 39.1 — default = EdgeOptionLists.FaiCirclePolarStepDeg 동일 (Phase 28 D-04 byte-identical)
public double Circle_RectL1Ratio  { get; set; } = 0.02;   //260529 hbk Phase 39.1 — default = EdgeOptionLists.FaiCircleRectL1Ratio
public double Circle_RectL2Ratio  { get; set; } = 0.02;   //260529 hbk Phase 39.1 — default = EdgeOptionLists.FaiCircleRectL2Ratio

[ItemsSourceProperty(nameof(Circle_PolarEdgeSelectionList))] //260529 hbk Phase 39.1 — ComboBox 처리 (Circle_RadialDirectionList 패턴 동형)
public string Circle_PolarEdgeSelection { get; set; } = "First"; //260529 hbk Phase 39.1 — default = EdgeOptionLists.FaiCircleEdgeSelection
[PropertyTools.DataAnnotations.Browsable(false)] //260529 hbk Phase 39.1 — Selections 옵션 래퍼 (EdgeOptionLists 단일 소스)
public List<string> Circle_PolarEdgeSelectionList { get { return EdgeOptionLists.Selections; } } //260529 hbk Phase 39.1
```

**Existing wrapper pattern reference (L36-38, Circle_RadialDirectionList — neue Liste 동형):**
```csharp
//260508 hbk Phase 28 REQ-28-01 — RadialDirection 콤보 옵션 래퍼 (Phase 17 D-02 단일 소스 EdgeOptionLists.RadialDirections 직접 참조)
[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> Circle_RadialDirectionList { get { return EdgeOptionLists.RadialDirections; } }
```

### 1.2 `CircleCenterDistanceMeasurement.cs` (Phase 31 CO-23.1-02 reference analog, read-only)

**기존 polar 4 필드 노출 패턴 — Phase 31 hotfix#4 에서 이미 구현됨:**
- `FAIConfig.ToRoiDefinition` L167-173 가 `cc.Circle_PolarStepDeg / Circle_RectL1Ratio / Circle_RectL2Ratio` 를 그대로 RoiDefinition 으로 전달 (strip 시각화 파라미터). **즉, CircleCenterDistance 는 이미 39.1 #1 의 분야 패턴 보유** — CircleDiameter 도 동일 4 필드 노출 + `FAIConfig.ToRoiDefinition` 의 `var c = m as CircleDiameterMeasurement;` 블록(L156-163) 에 strip 파라미터 전달 추가 검토.

**Match quality justification:** CircleDiameter 와 CircleCenterDistance 모두 `MeasurementBase` 파생, polar 분기에서 `TryFindCircleByPolarSampling` 호출, `Circle_RadialDirection` 으로 polar/fit 분기 — 1:1 대응.

### 1.3 `FAIConfig.cs` (ICustomTypeDescriptor 동적 노출 패턴 reference, 본 phase 미변경)

**Pattern reference — Phase 19 QUAL-03 (L270-310):**
```csharp
//260507 hbk Phase 19 QUAL-03 — PropertyGrid 동적 노출 (EdgeMeasureType 별 필터)
public System.ComponentModel.PropertyDescriptorCollection GetProperties() {
    return BuildFilteredProperties(null);
}
private System.ComponentModel.PropertyDescriptorCollection BuildFilteredProperties(System.Attribute[] attrs) {
    var sourceNames = new System.Collections.Generic.HashSet<string> {
        nameof(EdgeMeasureTypeList),
        nameof(EdgeDirectionList),
        nameof(EdgePolarityList),
    };
    bool hasDynamicMeasurements = Measurements != null && Measurements.Count > 0;
    return DynamicPropertyHelper.FilterProperties(this, attrs,
        name => IsHiddenForEdgeMeasureType(name, EdgeMeasureType)
                || (hasDynamicMeasurements && IsLegacyEdgeParam(name)),
        sourceNames);
}

private static bool IsHiddenForEdgeMeasureType(string name, string edgeMeasureType) {
    if (edgeMeasureType == "CircleDiameter") {
        if (name == "EdgeDirection" || name == "EdgeDirectionList") return true;
        // ...
    }
    return false;
}
```

**Apply pattern in #1 (CircleDiameterMeasurement.cs 새 hide rule):**
```csharp
//260529 hbk Phase 39.1 G2-04 — Circle_RadialDirection 빈값 (fit 경로) 시 polar 4 필드 hide.
//  Circle_RadialDirection 콤보가 PropertyGrid 에서 변경되면 InspectionListView.OnParamEditorSelectionChanged
//  → TryTriggerMeasurementRoiRefresh → Background defer → SelectedObject 재할당 → GetProperties 재호출 → hide 갱신.
public System.ComponentModel.PropertyDescriptorCollection GetProperties() { //260529 hbk Phase 39.1
    var sourceNames = new System.Collections.Generic.HashSet<string> {
        nameof(EdgePolarityList),
        nameof(Circle_RadialDirectionList),
        nameof(Circle_PolarEdgeSelectionList), //260529 hbk Phase 39.1 — 신규 List
    };
    return DynamicPropertyHelper.FilterProperties(this, null,
        name => IsHiddenForRadialDirection(name, Circle_RadialDirection),
        sourceNames);
}
private static bool IsHiddenForRadialDirection(string name, string radialDir) { //260529 hbk Phase 39.1
    if (string.IsNullOrEmpty(radialDir)) { // fit 경로 — polar 4 필드 hide
        if (name == "Circle_PolarStepDeg") return true;
        if (name == "Circle_RectL1Ratio")  return true;
        if (name == "Circle_RectL2Ratio")  return true;
        if (name == "Circle_PolarEdgeSelection" || name == "Circle_PolarEdgeSelectionList") return true;
    }
    return false;
}
// + ICustomTypeDescriptor stub 12개 (FAIConfig.cs L287-296 참고: GetAttributes/GetClassName/GetComponentName/GetConverter/GetDefaultEvent/GetDefaultProperty/GetEditor/GetEvents x 2/GetPropertyOwner + GetProperties(Attribute[]))
```

**Reference — Phase 19 Plan 02 SUMMARY:** ICustomTypeDescriptor 단독 commit 시 CS0535 (미구현 멤버) → Task 1+2 atomic commit 필수. Phase 39.1 의 CircleDiameterMeasurement 에 ICustomTypeDescriptor 추가 시 **단일 commit 으로 12 stub 전부 한 번에** 적용 필요.

### 1.4 `DynamicPropertyHelper.cs` (위임 헬퍼, 본 phase 미변경)

**Reference signature (L21-25):**
```csharp
public static System.ComponentModel.PropertyDescriptorCollection FilterProperties(
    object obj,
    System.Attribute[] attrs,
    System.Func<string, bool> hideFunc,
    System.Collections.Generic.HashSet<string> sourceNames) {
    // hideFunc(name) == true → 해당 프로퍼티 hide
    // sourceNames 포함 [Browsable(false)] 프로퍼티는 화이트리스트로 강제 포함
}
```

**Match quality justification:** Phase 19/28 가 이미 검증한 단일 위임 패턴 — DatumConfig (L569 부근) + FAIConfig 양쪽 모두 동형 사용. CircleDiameterMeasurement 도 동일 위임 호출만 추가하면 됨.

### 1.5 `VisionAlgorithmService.TryFindCircleByPolarSampling` (read-only, 호출 시그니처 참조)

**Existing signature — CircleDiameterMeasurement.cs L78-89 호출부에서 명확. 4 필드를 위치 인자로 직접 받음.** 인스턴스 필드로 교체해도 시그니처 변경 0.

### 1.6 `DatumFindingService.TryTeachCircleTwoHorizontal` (read-only, **ANTI-GOAL**)

**D-G2-05 anti-goal:** L200 + L730 의 inline `string.Equals(..., "Outward", OrdinalIgnoreCase) ? "negative" : "positive"` 패턴 (Phase 28 D-02 helper 단일 소스 통일 commit `84affbb`+`a894c36`) 은 **Phase 39.1 변경 0**. CircleDiameter 인스턴스 필드는 FAI 경로 전용 — Datum CTH polar 경로는 `EdgeOptionLists.FaiCircle*` 정적 상수 그대로 사용. 사용자 검증 핵심 = INI byte-identical + Datum CTH 검출 동등성 회귀 0 (Phase 28 REQ-28-03).

### 1.7 `EnsurePerRoiDefaults` idempotent migration 패턴 (Phase 13 D-PRP-LOOP / Phase 22 IMG-01)

**Pattern reference:** sentinel 0 / "" → default 채움. INI 하위호환 + 신규 필드 추가 동시 처리.

```csharp
//260529 hbk Phase 39.1 G2-03 — idempotent migration: 기존 INI 로드 후 0/"" sentinel 검출 시 EdgeOptionLists 단일 소스에서 default 채움.
//  적용 위치 후보: FAIConfig.EnsurePerRoiDefaults (전역 hook) 또는 CircleDiameterMeasurement ctor (per-instance) — implementation 측 판단 (D-discretion).
//  핵심: default 값이 EdgeOptionLists.FaiCircle* 상수와 동일 → Phase 28 byte-identical 회귀 0 보장.
if (m.Circle_PolarStepDeg == 0.0)         m.Circle_PolarStepDeg        = EdgeOptionLists.FaiCirclePolarStepDeg;
if (m.Circle_RectL1Ratio  == 0.0)         m.Circle_RectL1Ratio         = EdgeOptionLists.FaiCircleRectL1Ratio;
if (m.Circle_RectL2Ratio  == 0.0)         m.Circle_RectL2Ratio         = EdgeOptionLists.FaiCircleRectL2Ratio;
if (string.IsNullOrEmpty(m.Circle_PolarEdgeSelection)) m.Circle_PolarEdgeSelection = EdgeOptionLists.FaiCircleEdgeSelection;
```

---

## Item #2 — EdgeToLineDistance `measureX` projection_pl axis fix

**Phase decision anchors:** D-G3-01 (근본 원인 lock) / D-G3-02 (measureX 분기만 DatumAngle2Rad) / D-G3-03 (인라인 유지, Phase 31 Task 3(C) 보류) / D-G3-04 (DatumAngle2Rad==0 폴백)

### 2.1 `EdgeToLineDistanceMeasurement.cs` (수정 대상)

**Role:** measurement(impl) — `MeasurementBase, IDatumOriginConsumer` 파생
**Closest analog:** self (measureY 분기 L163-169 가 정사영 패턴의 정답 reference). measureX 만 동일 각도 일관성 적용.

**Current measureX branch (L151-162, problematic):**
```csharp
bool measureX = (MeasureAxis == "X"); //260517 hbk l5e-03 — null/""/"Y" → false (레거시 INI·미설정 안전)
double sinT = System.Math.Sin(DatumAngleRad); //260517 hbk l5e-03
double cosT = System.Math.Cos(DatumAngleRad); //260517 hbk l5e-03
if (cosT < 0.0) { sinT = -sinT; cosT = -cosT; } //260517 hbk l5e-03 — Atan2 방향(좌→우/우→좌) 무관 부호 일관성: datum x축 cosθ≥0 정규화
double axisR1, axisC1, axisR2, axisC2; //260517 hbk l5e-03 — projection_pl 대상 직선의 2점 (교점 ±200px, 길이는 직선 정의에 무관)
if (measureX) //260517 hbk l5e-03 — datum 수직선(y축): 방향벡터 (cosθ,-sinθ), 각도 θ+90°  ← PROBLEMATIC: θ+90° 가정 파괴 시 오차
{
    axisR1 = DatumOriginRow - 200.0 * cosT;
    axisC1 = DatumOriginCol + 200.0 * sinT;
    axisR2 = DatumOriginRow + 200.0 * cosT;
    axisC2 = DatumOriginCol - 200.0 * sinT;
}
```

**Pattern to apply (D-G3-02 — measureX 만 DatumAngle2Rad, D-G3-04 폴백):**
```csharp
//260529 hbk Phase 39.1 G3-02 — measureX 만 DatumAngle2Rad 사용 (실 datum 수직선 각도). measureY 는 DatumAngleRad 유지 (회귀 0).
//  근본 원인 (D-G3-01): 기존 코드는 DatumAngleRad+90° 가정으로 가상 수직선 구성 — DetectedRefAngle2 와 다를 때(부품이 직각 아닐 때) 정사영 오차.
//  D-G3-04 폴백: DatumAngle2Rad==0 (TLI Datum 등 2차 기준선 미주입) 시 현재 로직 유지 → CTH/VTH 만 실제 변경 적용, TLI 회귀 0.
bool measureX = (MeasureAxis == "X");
double sinT, cosT;
if (measureX && DatumAngle2Rad != 0.0) //260529 hbk Phase 39.1 G3-02 — 폴백 가드 (D-G3-04)
{
    sinT = System.Math.Sin(DatumAngle2Rad); //260529 hbk Phase 39.1 — 수직 기준선 실제 각도
    cosT = System.Math.Cos(DatumAngle2Rad); //260529 hbk Phase 39.1
}
else //260529 hbk Phase 39.1 — measureY 또는 DatumAngle2Rad sentinel 0 → 기존 동작 (DatumAngleRad)
{
    sinT = System.Math.Sin(DatumAngleRad);
    cosT = System.Math.Cos(DatumAngleRad);
}
if (cosT < 0.0) { sinT = -sinT; cosT = -cosT; } // datum x축 cosθ≥0 정규화 (각도 부호 일관)

double axisR1, axisC1, axisR2, axisC2;
if (measureX)
{
    if (DatumAngle2Rad != 0.0) //260529 hbk Phase 39.1 G3-02 — 실 수직선: 방향벡터 (sinθ2,cosθ2) — measureY 분기와 동일 공식 (θ2 사용)
    {
        axisR1 = DatumOriginRow - 200.0 * sinT; //260529 hbk Phase 39.1 — measureY 와 같은 sin/cos 조합 (각도만 θ→θ2)
        axisC1 = DatumOriginCol - 200.0 * cosT;
        axisR2 = DatumOriginRow + 200.0 * sinT;
        axisC2 = DatumOriginCol + 200.0 * cosT;
    }
    else //260529 hbk Phase 39.1 G3-04 — 폴백: 기존 가상 수직선 (DatumAngleRad+90° 가정)
    {
        axisR1 = DatumOriginRow - 200.0 * cosT; // 기존 L158-161 동일
        axisC1 = DatumOriginCol + 200.0 * sinT;
        axisR2 = DatumOriginRow + 200.0 * cosT;
        axisC2 = DatumOriginCol - 200.0 * sinT;
    }
}
else //260517 hbk l5e-03 — measureY: datum 수평선(x축), 변경 0 (회귀 0)
{
    axisR1 = DatumOriginRow - 200.0 * sinT;
    axisC1 = DatumOriginCol - 200.0 * cosT;
    axisR2 = DatumOriginRow + 200.0 * sinT;
    axisC2 = DatumOriginCol + 200.0 * cosT;
}
// ... signedPx 계산 — measureX 시 부호 공식도 (sinT,cosT) 기반으로 재검토 필요 (현 L185 동일 형식 사용 가능)
```

**Reference — measureY 분기 (L163-169, 변경 0, 회귀 0 보존 대상):**
```csharp
else //260517 hbk l5e-03 — datum 수평선(x축): 방향벡터 (sinθ,cosθ), 각도 θ
{
    axisR1 = DatumOriginRow - 200.0 * sinT;
    axisC1 = DatumOriginCol - 200.0 * cosT;
    axisR2 = DatumOriginRow + 200.0 * sinT;
    axisC2 = DatumOriginCol + 200.0 * cosT;
}
```

**Match quality justification:** measureY 분기는 이미 datum 1차 기준선 (θ) 의 sin/cos 직접 사용으로 정확. measureX 분기를 동일 공식 적용 — 단지 각도만 θ → θ2 로 교체.

### 2.2 `Action_FAIMeasurement.cs` (주입 경로, read-only)

**Reference L214-217 (이미 정상 동작, 변경 0):**
```csharp
//260519 hbk Phase 31 D-03 — IDatumOriginConsumer 일반화 (기존 EdgeToLineDistanceMeasurement 하드코딩 제거)
consumer.DatumOriginRow = dc.DetectedOriginRow;
consumer.DatumOriginCol = dc.DetectedOriginCol;
consumer.DatumAngleRad  = dc.DetectedRefAngle;
consumer.DatumAngle2Rad = dc.DetectedRefAngle2; //260519 hbk Phase 31 hotfix#3 — 수직 기준선 각도
```

**즉 `DatumAngle2Rad` 는 이미 EdgeToLineDistance 에 흐름 — 단지 measureX 분기가 미사용했을 뿐.**

L223-228 의 미주입 분기 (DatumRef 없음/매칭 실패 시 `consumer.DatumAngle2Rad = 0.0`) 가 G3-04 폴백의 sentinel — 이 경로 회귀 0 보장이 핵심.

### 2.3 `DatumConfig.DetectedRefAngle2` (read-only, sentinel 0 확인)

**Reference L534-539:**
```csharp
//260519 hbk Phase 31 hotfix#3 — datum 2차(수직) 기준선 각도(rad). DetectedRefAngle 은 1차(수평) 각도.
//  X축 측정이 Line1+90° 가 아닌 실제 datum 수직선을 기준하도록 DatumFindingService 가 write-back.
[System.ComponentModel.Browsable(false)]
[PropertyTools.DataAnnotations.Browsable(false)]
[Newtonsoft.Json.JsonIgnore]
public double DetectedRefAngle2 { get; set; }
```

**Invariant:** `DetectedRefAngle2` 미 write-back 시 default 0 (sentinel) — G3-04 폴백 분기 조건 `DatumAngle2Rad != 0.0` 와 정합.

### 2.4 `IDatumOriginConsumer.cs` (read-only, 인터페이스 정의)

**Reference (전체 22 lines):**
```csharp
public interface IDatumOriginConsumer
{
    double DatumOriginRow { get; set; }
    double DatumOriginCol { get; set; }
    double DatumAngleRad  { get; set; }
    //260519 hbk Phase 31 hotfix#3 — datum 2차(수직) 기준선 각도. Y측정=DatumAngleRad(수평선), X측정=DatumAngle2Rad(수직선).
    //  CircleTwoHorizontal=π/2(원중심 통과 수직 가상선), TwoLineIntersect=Line2 실제각, VerticalTwoHorizontal=수직에지 실제각.
    double DatumAngle2Rad { get; set; }
    //260521 hbk Phase 32 — DatumC(CircleTwoHorizontal) 검출 원(B1 홀) 중심. E2 CompoundAngle 만 사용, 타 타입은 0 주입.
    double DatumDetectedCircleRow { get; set; }
    double DatumDetectedCircleCol { get; set; }
}
```

**즉, 인터페이스 멤버 추가 0. EdgeToLineDistance 구현체에서 `DatumAngle2Rad` 를 사용 코드만 추가.** 이전 phase 의 carry-over (L81-86 주석: "EdgeToLineDistance 인라인 투영은 현행 유지(carry-over)") 가 Phase 39.1 G3 의 정확한 해결 대상.

---

## Item #3 — FAI 노드 조회 (post-inspection result display)

**Phase decision anchors:** D-G4-01 (옵션 B 채택 — 검사 후 노드 클릭 시 결과 재현) / D-G4-02 (전 FAI 타입 공통) / G3 옵션 A·C 비채택

### 3.1 `InspectionListView.xaml.cs` (수정 대상)

**Role:** view-codebehind(UI) — `UserControl` partial class
**Closest analog:** self (InspectionList_SelectionChanged L396 — 이미 모든 노드 타입 (Datum/Measurement/FAI/Action/Sequence) 분기 처리. 단지 검사 후 결과/이미지/overlay 재현 분기가 누락.)

**Current state (L488-547, Measurement/FAI 노드 분기 — 이미지 표시는 있으나 *측정 결과 재현* 누락):**
```csharp
else if (item.NodeType == ENodeType.Measurement) {
    button_addFAI.IsEnabled = true;
    button_removeFAI.IsEnabled = true;
    button_renameFAI.IsEnabled = true;
    bool isRectRoiType = item.Param is EdgeToLineDistanceMeasurement
        || item.Param is EdgeToLineAngleMeasurement
        || /* ... 5 more types ... */
        || item.Param is CompoundShortAxisDistanceMeasurement;
    if (mParentWindow != null && mParentWindow.mainView != null)
        mParentWindow.mainView.btn_rectRoi.IsEnabled = isRectRoiType;
    if (ParamEditor != null && itemParam != null) {
        _isRebinding = true;
        try {
            ParamEditor.SelectedObject = null;
            ParamEditor.SelectedObject = itemParam;
        } finally { _isRebinding = false; }
    }
    if (_inspectionVm != null) _inspectionVm.ClearResults(); //260509 hbk Phase 20  ← Phase 39.1 G4-01: 검사 후엔 결과 보존 필요
    //260521 hbk Phase 32 UAT — Measurement 노드 선택 시 소유 Shot 이미지 표시 (이미지 회귀 결함 수정)
    if (mParentWindow != null && mParentWindow.mainView != null && itemParam is MeasurementBase meas)
        mParentWindow.mainView.DisplayMeasurementImage(meas);  ← 이미지는 이미 표시
    //260518 hbk #6 — 선택 Measurement 노드의 ROI 캔버스 하이라이트 + 명칭 라벨
    if (mParentWindow != null && mParentWindow.mainView != null)
        mParentWindow.mainView.HighlightSelectedRoi(itemParam as ParamBase);  ← ROI 하이라이트도 이미
}
else if (item.NodeType == ENodeType.FAI) {
    button_addFAI.IsEnabled = true;
    button_removeFAI.IsEnabled = true;
    button_renameFAI.IsEnabled = true;
    if (_inspectionVm != null && itemParam is FAIConfig faiConfig) {
        _inspectionVm.OnFAISelected(faiConfig);  ← FAI 결과 모델 갱신 이미 있음
        mParentWindow.mainView.DisplayFAIImage(faiConfig);  ← 이미지 이미 표시
        if (ParamEditor != null) { /* force rebind */ }
    }
    if (mParentWindow != null && mParentWindow.mainView != null)
        mParentWindow.mainView.HighlightSelectedRoi(itemParam as ParamBase);
}
```

**Pattern to apply (G4-01 — 검사 후 overlay 재현 추가):**
```csharp
//260529 hbk Phase 39.1 G4-01/G4-02 — 검사 후 FAI/Measurement 노드 클릭 → 측정 결과 + 이미지 + overlay 재현.
//  Phase 37 hotfix A/B (FindFAIContainingMeasurement) round-trip 으로 객체 참조 round-trip 확정 — meas.LastJudgement / meas.LastMeasuredValue / fai.MeasuredValue / fai.IsPass 가 이미 객체에 잔존.
//  현 코드에서 누락된 부분: overlay 재현 (FAI-Edge*/FAI-DistLine — Action_FAIMeasurement 가 overlayAcc 에 누적했던 것을 노드 클릭 시 재 렌더).
//  옵션 A/C (sequence 우회) 미채택 — 단순 재현만으로 충분 (D-G4-01).
//  적용 분기: ENodeType.Measurement + ENodeType.FAI 양쪽 (D-G4-02 전 FAI 타입 공통).
//  구현 측 판단 사항: overlay 데이터 저장 위치 — (a) FAIConfig.LastOverlays 신규 List + Action_FAIMeasurement 에서 write-back, 또는 (b) Shot 단위 overlayAcc 캐시 — implementation 측 결정.
mParentWindow.mainView.RenderInspectionResultForNode(itemParam); //260529 hbk Phase 39.1 — 신규 MainView 메서드 (overlay 재현 + 결과 라벨)
```

**Match quality justification:** SelectionChanged 핸들러는 이미 4 NodeType 분기를 가지고 각 분기에서 `DisplayShotImage/DisplayFAIImage/DisplayMeasurementImage` + `HighlightSelectedRoi` 호출 패턴이 정형화됨. overlay 재현 호출만 추가하면 됨.

### 3.2 `MainView.xaml.cs` (수정 대상)

**Role:** view-codebehind(UI) — `UserControl` partial class
**Closest analog:** self (DisplayFAIImage L124 / DisplayShotImage L136 / DisplayDatumImage L161 / DisplayMeasurementImage L181 / HighlightSelectedRoi L370 모두 동일 패턴)

**Pattern reference — `DisplayMeasurementImage` (L181-189):**
```csharp
//260521 hbk Phase 32 UAT — Measurement 노드 선택 시 소유 Shot 이미지 표시 진입점.
//  MeasurementBase → FAI(FindFaiNameContainingMeasurement) → ShotConfig(FAIConfig.Owner) → DisplayShotImage.
public void DisplayMeasurementImage(MeasurementBase measurement) {
    if (measurement == null) return;
    //260528 hbk Phase 37 — 이름 round-trip(FindFAIByName) 제거: 여러 Shot 의 FAI 이름이 같으면(기본 FAI_0 등)
    //  첫 Shot 의 FAI 가 반환돼 잘못된 Shot 이미지가 표시됨. 소유 FAIConfig 를 객체 참조로 직접 해석.
    FAIConfig fai = FindFAIContainingMeasurement(measurement);
    if (fai == null) return;
    ShotConfig shot = fai.Owner as ShotConfig;
    DisplayShotImage(shot);
}
```

**Pattern reference — `HighlightSelectedRoi` (L370-404):**
```csharp
public void HighlightSelectedRoi(ParamBase param) {
    string selRoiId = null;
    string faiNameForFallback = null;
    FAIConfig anchorFai = null;
    if (param is FAIConfig faiSel) {
        anchorFai = faiSel;
    }
    else if (param is MeasurementBase measSel) {
        string faiName = FindFaiNameContainingMeasurement(measSel);
        faiNameForFallback = faiName;
        string mName = measSel.MeasurementName;
        if (string.IsNullOrEmpty(mName)) mName = measSel.TypeName;
        if (!string.IsNullOrEmpty(faiName)) {
            selRoiId = faiName + "_" + mName;
            anchorFai = FindFAIContainingMeasurement(measSel); //260528 hbk Phase 37
        }
    }
    var rois = CollectShotRois(anchorFai);
    if (!string.IsNullOrEmpty(selRoiId) && !string.IsNullOrEmpty(faiNameForFallback)) {
        bool matched = false;
        foreach (var r in rois) {
            if (r != null && r.Id == selRoiId) { matched = true; break; }
        }
        if (!matched) selRoiId = faiNameForFallback;
    }
    halconViewer.UpdateDisplayState(rois, selRoiId, null, null);
}
```

**Pattern to apply (G4-01 — 신규 `RenderInspectionResultForNode`):**
```csharp
//260529 hbk Phase 39.1 G4-01/G4-02 — 검사 후 FAI/Measurement 노드 클릭 시 측정 결과 + overlay 재현 통합 진입점.
//  안에서 (a) DisplayMeasurementImage/DisplayFAIImage 호출 (이미지) + (b) HighlightSelectedRoi 호출 (ROI 하이라이트) + (c) overlay 재현 (FAI-Edge*/FAI-DistLine).
//  Phase 37 hotfix A/B 의 FindFAIContainingMeasurement round-trip 활용 — 이름 충돌 없는 정확한 객체 참조 도달.
//  Item #2 (EdgeToLineDistance projection_pl) 회귀 0: overlay 재현은 저장된 데이터만 사용 — 측정 재실행 0.
public void RenderInspectionResultForNode(ParamBase param) { //260529 hbk Phase 39.1
    if (param == null) return;
    if (param is MeasurementBase meas) {
        DisplayMeasurementImage(meas);
        HighlightSelectedRoi(meas);
        RenderStoredOverlays(meas); //260529 hbk Phase 39.1 — overlay 재현 (Action_FAIMeasurement 가 측정 시 저장한 데이터)
    }
    else if (param is FAIConfig fai) {
        DisplayFAIImage(fai);
        HighlightSelectedRoi(fai);
        RenderStoredOverlays(fai); //260529 hbk Phase 39.1 — fai 의 모든 measurement overlay 합산
    }
}
```

**Implementation 측 판단 사항:**
- overlay 데이터 저장 위치 — (a) `FAIConfig.LastOverlays` 신규 List + `Action_FAIMeasurement.EStep.Measure` L261 `overlayAcc.AddRange(measOverlays)` 직후 fai 에 write-back, 또는 (b) Shot 단위 캐시.
- 또는 단순화 — overlay 재현 없이 *측정 결과 라벨만 재표시* (FAIConfig.MeasuredValue + LastJudgement 가 객체에 잔존 → DataGrid 갱신만 추가).
- D-G4-01 의 "이미지 + overlay + 결과 재현" 표현 의도와 구현 비용 사이 implementation 측 결정.

### 3.3 `InspectionRecipeManager.cs` (read-only, FAI round-trip)

**Phase 37 hotfix A/B canonical ref:** `FindFAIContainingMeasurement` (MainView.xaml.cs L1732) — 객체 참조 round-trip, RecipeManager.Shots + pSeq fallback 2-pass 탐색. 이미 정상 동작 — 본 phase 변경 0.

**Reference (MainView.xaml.cs L1732-1762):**
```csharp
//260528 hbk Phase 37 — FindFaiNameContainingMeasurement 의 참조 버전: 이름 충돌(여러 Shot 동일 FAI 명) 시에도
//  실제 소유 FAIConfig 객체를 ReferenceEquals 로 정확히 반환. 이미지/ROI 해석이 첫 Shot 으로 잘못 묶이는 결함 차단.
private FAIConfig FindFAIContainingMeasurement(MeasurementBase measurement) {
    if (measurement == null) return null;
    //260528 hbk Phase 37 — 우선 RecipeManager.Shots(동적 FAI 단일 소스, 신규 Shot 즉시 반영) 에서 탐색.
    var recipeManager = (SystemHandler.Handle != null && SystemHandler.Handle.Sequences != null)
        ? SystemHandler.Handle.Sequences.RecipeManager : null;
    if (recipeManager != null && recipeManager.Shots != null) {
        foreach (ShotConfig rmShot in recipeManager.Shots) {
            if (rmShot == null) continue;
            foreach (FAIConfig fai in rmShot.FAIList) {
                foreach (var m in fai.Measurements) {
                    if (ReferenceEquals(m, measurement)) return fai;
                }
            }
        }
    }
    // ... pSeq fallback ...
}
```

### 3.4 `FAIConfig.cs` (read-only, 결과 저장 필드)

**Reference L97-108:**
```csharp
[Browsable(false)]
public double MeasuredValue { get; set; }

[Browsable(false)]
public bool IsPass { get; set; }

//260529 hbk Phase 39 WF-01 D-02 — datum-skip subtype 플래그.
[Browsable(false)]
public bool WasDatumSkipped { get; set; }
```

**Invariant:** FAIConfig 의 결과 필드는 검사 후에도 잔존 (`ClearResult` 호출 전까지). `Action_FAIMeasurement.EStep.Measure` L271 `fai.MeasuredValue = fai.Measurements[0].LastMeasuredValue;` 가 라스트 측정 결과 기록 — 노드 클릭 시 그대로 재사용 가능.

### 3.5 `HalconDisplayService.cs` (read-only, overlay 렌더)

**Reference:** L181 부근 `FAI-DistLine` cyan 분기 + L256 `FAI-Edge*` 녹/적 + suffix `-OK`/`-NG` (Action_FAIMeasurement L257 `ov.RoiId = ov.RoiId + suffix;`). overlay 렌더 핵심은 `HalconDisplayService.UpdateDisplayState` 가 이미 처리 — 본 phase 는 *재현 시 동일 overlay 데이터 전달*만 필요.

---

## Item #4 — Datum CTH Edit 모드 분리

**Phase decision anchors:** D-G4-03 (btn_teachDatum 재사용) / D-G4-04 (project_roi_edit_mode_deferred 미통합) / D-G4-05 (3분기 재구성)

### 4.1 `HalconDisplayService.cs` (수정 대상)

**Role:** service(halcon-display)
**Closest analog:** self (RenderDatumOverlay L683-925 — `LastTeachSucceeded` 분기 패턴은 이미 정형. 단지 `isEditMode` 인자 분기 추가.)

**Current signature (L683):**
```csharp
public void RenderDatumOverlay(HWindow window, DatumConfig datum, bool isSelected)
{
    // ...
}
```

**Pattern to apply (D-G4-05 — isEditMode 인자 추가 + 3분기 재구성):**
```csharp
//260529 hbk Phase 39.1 G4-03/G4-05 — Datum CTH Edit 모드 분리: isEditMode 인자 추가 + LastTeachSucceeded 분기 재구성.
//  D-G4-05 분기:
//    LastTeachSucceeded==true && !isEditMode → fitting 원 + DetectedOrigin 십자만 (사용자가 평소 보는 화면)
//    LastTeachSucceeded==true &&  isEditMode → fitting 원 + 십자 + Horizontal_A/B + Circle ROI 사각형 핸들 (편집 시)
//    LastTeachSucceeded==false                 → 기존 동작 (Horizontal_A/B ROI 만, 티칭 미완료 상태)
//  호출자 MainView.RenderImage 가 isEditMode = btn_teachDatum.IsChecked == true 인자 전달.
//  호환성: 호출처 5건(L97/831/930/962/1139/1254/1270/2313/2331/2408 등) 시그니처 변경 필요 — 기본값 false 인 옵션 인자로 추가 또는 오버로드 신설 (implementation 측 판단).
public void RenderDatumOverlay(HWindow window, DatumConfig datum, bool isSelected, bool isEditMode = false) //260529 hbk Phase 39.1
{
    if (datum == null) return;
    // ... 색상 설정 등 기존 진입부 (L688-704) 변경 0 ...

    try
    {
        HOperatorSet.SetColor(window, color);
        HOperatorSet.SetLineWidth(window, lineWidth);

        //260529 hbk Phase 39.1 G4-05 — CTH 에서는 *티칭 후 평소 모드* 일 때 ROI 사각형 핸들 + Circle ROI 비표시.
        //  CircleTwoHorizontal && LastTeachSucceeded && !isEditMode → ROI 도형 skip (fitting 원 + 십자만 L822 분기에서 표시).
        bool cthHideRois = (datum.AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal)
                        && datum.LastTeachSucceeded
                        && !isEditMode; //260529 hbk Phase 39.1 G4-05

        //260428 hbk W4-A 후속 — RenderDatumOverlay 슬롯 분기 수정 (TwoLineIntersect L713-724, VTH L726-738 — 본 phase 변경 0)
        if (datum.AlgorithmTypeEnum == EDatumAlgorithm.TwoLineIntersect) { /* ... */ }
        else if (datum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontal
              || datum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) { /* ... */ }
        // CTH: 기존 L754-770 Circle ROI 그리기 — cthHideRois 가드 추가
        if (datum.AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal
            && datum.CircleROI_Radius > 0
            && !cthHideRois) //260529 hbk Phase 39.1 G4-05
        {
            HOperatorSet.SetColor(window, color);
            HOperatorSet.SetLineWidth(window, lineWidth);
            HOperatorSet.DispCircle(window, datum.CircleROI_Row, datum.CircleROI_Col, datum.CircleROI_Radius);
            // ... 라벨 + RenderCircleStripOverlay 동일 ...
        }
        // Horizontal A/B: 기존 L773-798 — cthHideRois 가드 추가
        if (datum.AlgorithmTypeEnum != EDatumAlgorithm.TwoLineIntersect && !cthHideRois) //260529 hbk Phase 39.1 G4-05
        {
            // ... Horizontal_A/B ROI 사각형 렌더 (기존 L778-797 동일) ...
        }

        // L800-819 RefOrigin 십자 + L822 LastTeachSucceeded 분기 (검출 라인 + fitting 원 + raw points) — 기본 동작 보존, 변경 0.
        // L886 RenderDatumFindResult + L898 DETECT FAIL 라벨 — 변경 0 (Phase 39 CO-39-02/03 회귀 0).
    }
    catch
    {
        // Suppress display errors
    }
}
```

**Critical existing block to preserve (L822-884, LastTeachSucceeded 검출 라인/원/십자 — 변경 0):**
```csharp
//260423 hbk Phase 11 D-11 — 검출 라인 2개 + 교점 오버레이 (TryTeachDatum 성공 시에만, 기존 cyan/blue/magenta 팔레트는 건드리지 않음)
if (datum.LastTeachSucceeded)
{
    // Line1Detected/Line2Detected 외삽 — 변경 0
    // raw edge points (cyan/magenta/gray/green/lime green/orange) — 변경 0
    // CircleTwoHorizontal 검출 원 (#90EE90 light-green-hex Quick 260430 fix) + center cross (yellow size=12) — 변경 0
    if (datum.AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal
        && datum.CircleDetected_Radius > 0)
    {
        HOperatorSet.SetColor(window, "#90EE90"); //260430 hbk Quick — 비표준 "light green" → hex (memory feedback_halcon_setcolor_invalid_names)
        HOperatorSet.SetLineWidth(window, 2);
        HOperatorSet.DispCircle(window,
            datum.CircleCenter_Row, datum.CircleCenter_Col, datum.CircleDetected_Radius);
        // ... center cross (yellow, size=12, line width=3) ...
    }
}
```

**즉, fitting 원 + center cross 는 L822-884 안에 이미 있음. Phase 39.1 G4 의 핵심은 그 외부 L755-797 의 ROI 사각형 도형들을 `cthHideRois` 로 가드.**

**Critical existing block to preserve (L886-919, RenderDatumFindResult + DETECT FAIL 라벨 — Phase 39 hotfix CO-39-02/03 회귀 0):**
- RenderDatumFindResult 는 LastTeachSucceeded 블록 *밖*. Phase 36 CO-36-03 fix 의 결정.
- DETECT FAIL 라벨 분기 `RuntimeDetectFailed || (IsConfigured && !LastFindSucceeded)` 은 Phase 39 hotfix — 본 phase 변경 0.

### 4.2 `MainView.xaml.cs` (수정 대상 — `RenderImage` 호출자)

**Role:** view-codebehind(UI)
**Closest analog:** self (TeachDatumButton_Click L2044 + `_canvasMode` enum L43 + SetDatumOverlay 호출 11곳)

**Current `TeachDatumButton_Click` (L2044-2111, 변경 0):**
```csharp
private void TeachDatumButton_Click(object sender, RoutedEventArgs e) {
    if (btn_teachDatum.IsChecked == true) {
        ExitCanvasMode();
        _canvasMode = ECanvasMode.TeachDatum;
        btn_teachDatum.IsChecked = true;
        halconViewer.IsTeachDatumMode = true;
        // ... ROI 누락 가드 + StartDatumTeachStep ...
    }
    else {
        halconViewer.IsTeachDatumMode = false;
        ExitCanvasMode();
    }
}
```

**Pattern to apply (G4-03 — SetDatumOverlay 호출처 모두 isEditMode 인자 전달):**
```csharp
//260529 hbk Phase 39.1 G4-03/G4-05 — SetDatumOverlay 호출 시 isEditMode = btn_teachDatum.IsChecked == true 전달.
//  CO-39-02 RuntimeDetectFailed 분기는 isEditMode 와 무관 (Phase 39 hotfix CO-39-02/03 동작 회귀 0 보장).
bool isEditMode = (btn_teachDatum != null && btn_teachDatum.IsChecked == true); //260529 hbk Phase 39.1
halconViewer.SetDatumOverlay(datum, true, isEditMode); //260529 hbk Phase 39.1 — 신규 3-인자 오버로드 또는 옵션 인자
```

**11 SetDatumOverlay 호출처 (Grep 결과):**
- `MainView.xaml.cs:97` (`_editingDatum, false` — 모드 해제 시)
- `MainView.xaml.cs:831/930/962/1139/1254/1270/2313/2331/2408` (`datum, true`)
- `InspectionListView.xaml.cs:135/459` (Datum 노드 선택 시)

**각 호출처에서 `isEditMode` 결정 규칙:**
- Datum 노드 선택 후 평상 시: `btn_teachDatum.IsChecked == false` → `isEditMode = false` → fitting 원만 표시 (D-G4-05 분기 1)
- 사용자가 `btn_teachDatum` 클릭하여 ON 으로 토글: `isEditMode = true` → fitting 원 + ROI 사각형 핸들 (D-G4-05 분기 2)
- 새 Datum / 티칭 미완료: `LastTeachSucceeded==false` → 기존 동작 (Horizontal A/B ROI 만, D-G4-05 분기 3)

**InspectionListView.xaml.cs L135/459 호출처는 mainView.btn_teachDatum 참조 필요 (직접 접근 가능 — Grep 결과 L1484 `btn_teachDatum.IsChecked = false;` 가 이미 외부 접근 가능 입증).**

### 4.3 `DatumConfig.cs` (read-only, CTH 알고리즘 + ROI 좌표)

**Reference fields (canonical_refs):**
- `AlgorithmTypeEnum == EDatumAlgorithm.CircleTwoHorizontal` 분기 (HalconDisplayService L755/L862)
- `Horizontal_A_Row/Col/Phi/Length1/Length2`, `Horizontal_B_*`, `CircleROI_Row/Col/Radius` — ROI 사각형 핸들 그리는 좌표
- `LastTeachSucceeded`, `LastFindSucceeded`, `RuntimeDetectFailed` — 분기 조건 필드
- `CircleCenter_Row/Col`, `CircleDetected_Radius` — fitting 원 좌표

**본 phase 변경 0** — 모든 분기는 HalconDisplayService 내부에서 처리.

### 4.4 `DatumFindingService.TryTeachCircleTwoHorizontal` (read-only, fitting 원 계산 경로)

**본 phase 변경 0** — fitting 원 (`CircleCenter_Row/Col`, `CircleDetected_Radius`) 결과 write-back 만 사용. Item #1 의 anti-goal 과 동일 — polar 경로 변경 0.

---

## Shared Patterns (전 4 항목 공통)

### Comment Marker Stacking (Phase 20 D-12)
**Source:** memory `feedback_comment_convention` + Phase 20 D-12
**Apply to:** 모든 수정 파일
```csharp
// 기존:  //260508 hbk Phase 28 REQ-28-02 (D-02)
// 추가:  //260508 hbk Phase 28 REQ-28-02 (D-02) + //260529 hbk Phase 39.1 G2-01
// 또는:  변환 라인 위에 새 마커 추가 — 누적 패턴 유지
```

### HALCON SetColor 표준명 (memory feedback)
**Source:** memory `feedback_halcon_setcolor_invalid_names` + Phase 16/30/HalconDisplayService L867
**Apply to:** Item #4 RenderDatumOverlay 분기 재구성
- `"red"`, `"yellow"`, `"cyan"`, `"magenta"`, `"blue"`, `"green"`, `"orange"`, `"gray"` 표준명만 사용
- `"light green"` 같은 비표준 → HALCON 예외 → catch swallow → silent 미표시. hex `"#90EE90"` 사용 (RenderDatumOverlay L867 검증된 패턴)
- "안 보임" 증상 시 1순위 의심 — Item #4 신규 색상 도입 시 즉시 점검

### HALCON MeasurePos 3 필수 (memory feedback)
**Source:** memory `feedback_halcon_measurepos_must_haves`
**Apply to:** Item #1 polar 경로 분석
- strip-loop (sampleCount for-loop 누적) + measurePhi 명시 매핑 + EdgeSelection 명시
- `TryFindCircleByPolarSampling` 내부에서 이미 충족 — Item #1 의 새 4 필드 (StepDeg/L1/L2/Selection) 가 strip-loop 파라미터 사용자 제어 지점

### K&R Brace Style (수정 대상 4 파일 중 3개)
**Apply to:** `CircleDiameterMeasurement.cs`(K&R), `EdgeToLineDistanceMeasurement.cs`(Allman), `InspectionListView.xaml.cs`(K&R), `MainView.xaml.cs`(K&R), `HalconDisplayService.cs`(Allman)
- **K&R:** `if (...) { ... }`, `void Foo() { ... }`
- **Allman:** `if (...)\n{ ... }`, `void Foo()\n{ ... }`
- 각 파일의 기존 스타일 유지 — 혼용 금지 (Phase 19 SUMMARY 결정 재사용)

### ICustomTypeDescriptor + DynamicPropertyHelper 위임 패턴 (Phase 19 single-source)
**Source:** Phase 19 plan 02 SUMMARY (`19-02-SUMMARY.md`)
**Apply to:** Item #1 CircleDiameterMeasurement 신규 ICustomTypeDescriptor
- Task 1 (필드 추가) + Task 2 (ICustomTypeDescriptor 구현) **atomic commit 필수** — Task 1 단독 commit 시 CS0535 (12 stub 미구현) 빌드 깨짐
- `DynamicPropertyHelper.FilterProperties` 단일 위임 — 직접 구현 안 함
- `sourceNames` 화이트리스트에 신규 List 이름 (`Circle_PolarEdgeSelectionList` 등) 포함

### ParamBase INI 직렬화 비간섭
**Source:** Phase 19 SUMMARY (L120 검증)
- `ParamBase.Save/Load` 는 `GetType().GetProperties()` Reflection 경로 사용 — ICustomTypeDescriptor 영향 0
- 신규 public 필드 4개 (Item #1) 는 자동 INI 직렬화 — 추가 코드 0

### Sentinel 0/"" Default + Idempotent Migration (Phase 13 D-PRP-LOOP / Phase 22 IMG-01)
**Apply to:** Item #1 EnsurePerRoiDefaults
- 기존 INI 의 누락 키 → 로드 시 default 0/"" → migration 함수가 EdgeOptionLists.FaiCircle* 상수로 채움
- byte-identical (Phase 28 REQ-28-04) 보장 — 기존 INI 회귀 0

### Try/Catch Swallow Convention (RenderDatumOverlay)
**Source:** HalconDisplayService L921-924 + L965 기존 패턴
**Apply to:** Item #4 신규 분기
- `try { ... } catch { /* Suppress display errors */ }`
- Halcon display 에러는 silent 처리 — 검사 흐름 차단 안 함
- 비표준 색상 catch swallow 가 silent 미표시의 1순위 원인 (memory feedback) — Item #4 신규 색상 도입 시 표준명 점검 필수

### Force Rebind Pattern (Phase 16 D-09/D-10 / Phase 19 fix / CO-22-01)
**Source:** InspectionListView.xaml.cs L472-481, L507-515, L534-542, L561-569
**Apply to:** Item #3 노드 클릭 시 PropertyGrid 갱신
```csharp
_isRebinding = true;
try {
    ParamEditor.SelectedObject = null;
    ParamEditor.SelectedObject = itemParam;
} finally { _isRebinding = false; }
```
**중요:** Item #3 의 RenderInspectionResultForNode 호출은 force rebind *후* 또는 *별도* 경로로 — _isRebinding 가드 안에서 호출 시 PropertyGrid SelectedObject 충돌 가능.

---

## No Analog Found

전 13 파일 모두 within-codebase analog 보유. 신규 패턴 도입 0.

---

## Anti-Goal Files (변경 0, 회귀 0 보장 대상)

| 파일 | 보존 이유 | Origin Phase |
|------|-----------|--------------|
| `DatumFindingService.TryTeachCircleTwoHorizontal` (L200, L730) | Datum CTH polar 경로 INI byte-identical | Phase 28 D-04 + Phase 39.1 D-G2-05 |
| `Action_FAIMeasurement.EStep.DatumPhase` L214-217 주입 경로 | DatumAngle2Rad 정상 흐름 (Phase 31 hotfix#3) | Phase 31 D-03 |
| `Action_FAIMeasurement.EStep.Measure` lenient 동작 | datum fail 시 measurement skip | Phase 37 D-37-03 |
| `InspectionSequence._failedDatums` + `MarkDatumFailed` | datum gate 동작 | Phase 39 D-01 |
| `HalconDisplayService.RenderDatumOverlay L822-884` (LastTeachSucceeded 검출 라인/원/십자) | fitting 원 + raw points 컨벤션 | Phase 11/13/16 |
| `HalconDisplayService.RenderDatumOverlay L886-919` (RenderDatumFindResult + DETECT FAIL 라벨) | Phase 36 CO-36-03 + Phase 39 CO-39-02/03 | Phase 36/39 |
| `EdgeToLineDistanceMeasurement.cs` measureY 분기 L163-169 | datum 1차 수평선 정사영 정확성 | Phase 23 ALG-01 + 23.1 l5e-03 |
| `EdgeToLineDistanceMeasurement.cs` DatumOriginRow/Col 미주입 폴백 L193-208 | AffineTransPoint2d 레거시 경로 (TLI Datum 케이스) | Phase 23.1 l5e-03 |

---

## Cross-Plan Anchor Table

| Plan | Item | Modify 파일 (1차) | Modify 파일 (2차) | Read-only Reference |
|------|------|-------------------|-------------------|----------------------|
| 39.1-01-PLAN | #1 CircleDiameter | `CircleDiameterMeasurement.cs` | (impl 측 판단: `FAIConfig.cs` migration hook 또는 ctor) | `EdgeOptionLists.cs`, `VisionAlgorithmService.cs`, `DatumFindingService.cs` (ANTI), `DynamicPropertyHelper.cs`, `FAIConfig.cs` (ICustomTypeDescriptor 패턴) |
| 39.1-02-PLAN | #2 EdgeToLineDistance | `EdgeToLineDistanceMeasurement.cs` | — | `Action_FAIMeasurement.cs` L214-217, `DatumConfig.cs` L534-539, `IDatumOriginConsumer.cs` |
| 39.1-03-PLAN | #3 FAI 노드 조회 | `MainView.xaml.cs`, `InspectionListView.xaml.cs` | (impl 측 판단: `FAIConfig.cs` LastOverlays 신규 필드 또는 Shot 캐시) | `InspectionRecipeManager.cs`, `FAIConfig.cs`, `HalconDisplayService.cs` |
| 39.1-04-PLAN | #4 Datum CTH Edit | `HalconDisplayService.cs`, `MainView.xaml.cs` | `InspectionListView.xaml.cs` (SetDatumOverlay 호출처 2건) | `DatumConfig.cs`, `DatumFindingService.cs` (ANTI) |

**Wave 구성 (D-G1-03):**
- Wave 1 = Plan 01 (#1) + Plan 02 (#2) — algorithm, MeasurementBase 파생만
- Wave 2 = Plan 03 (#3) + Plan 04 (#4) — UI, MainView/HalconDisplayService/InspectionListView
- Wave 간 데이터 의존 0 → 병렬 가능. UAT 는 Wave 2 안에 통합.

---

## Metadata

**Analog search scope:**
- `WPF_Example/Custom/Sequence/Inspection/` (12 .cs)
- `WPF_Example/UI/ContentItem/` + `WPF_Example/UI/ControlItem/` (5 .cs)
- `WPF_Example/Halcon/Algorithms/` + `WPF_Example/Halcon/Display/` (3 .cs)
- `.planning/phases/19/`, `.planning/phases/28/`, `.planning/phases/37/`, `.planning/phases/39/` SUMMARY

**Files scanned:** 13 source files + 4 prior phase SUMMARY
**Analogs ranked:** 모두 self-analog 또는 prior-phase analog (exact role+data flow 1:1 매칭)
**Pattern extraction date:** 2026-05-29

*Phase: 39.1-inspection-urgent-fixes-2026-05-29*
*Patterns mapped: 2026-05-29*
*Next: /gsd-plan-phase 39.1*
