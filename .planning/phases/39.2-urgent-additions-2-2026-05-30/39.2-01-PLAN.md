---
phase: 39.2-urgent-additions-2-2026-05-30
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: ["WF-01"]
tags: ["fai", "dualimage", "halcon", "measurement"]

must_haves:
  truths:
    - "FAI Type 콤보에 'DualImageEdgeDistance' 항목이 노출된다 (MeasurementFactory.GetTypeNames 11개)"
    - "DualImageEdgeDistance Measurement 는 INI 에서 Type=DualImageEdgeDistance 로 로드되면 MeasurementFactory.Create 가 인스턴스를 반환한다"
    - "Action_FAIMeasurement.EStep.Measure 에서 DualImageEdgeDistance 타입은 양 이미지(SimulImagePath + TeachingImagePath_Vertical) 를 별도 로드한다"
    - "PointROI 의 fit_line 중점 → LineROI 의 fit_line 으로 projection_pl 정사영 거리(mm) 가 resultValue 에 기록된다"
    - "기존 10 measurement 타입의 TryExecute 호출 경로는 변경되지 않는다 (회귀 0)"
    - "Phase 39 D-01 IsDatumFailed 게이트 + D-02 WasDatumSkipped + Phase 39.1-03 LastOverlays write-back 동작이 보존된다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs"
      provides: "MeasurementBase + IDatumOriginConsumer 신규 클래스, TypeName=DualImageEdgeDistance"
      contains: "class DualImageEdgeDistanceMeasurement"
      exports: ["PointROI_Row/Col/Phi/Length1/Length2", "LineROI_Row/Col/Phi/Length1/Length2", "PointROI_/LineROI_ Edge 7 params each", "TeachingImagePath_Vertical", "RuntimeImageA/B (transient)"]
    - path: "WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs"
      provides: "DualImageEdgeDistance dispatch + GetTypeNames 추가"
      contains: 'case "DualImageEdgeDistance":'
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "TryGrabOrLoadFaiDualImages helper + EStep.Measure DualImage 분기"
      contains: "TryGrabOrLoadFaiDualImages"
    - path: "WPF_Example/DatumMeasurement.csproj"
      provides: "신규 .cs 파일 Compile ItemGroup 등록"
      contains: "DualImageEdgeDistanceMeasurement.cs"
  key_links:
    - from: "MeasurementFactory.Create (INI Type 키)"
      to: "DualImageEdgeDistanceMeasurement instance"
      via: 'switch case "DualImageEdgeDistance"'
      pattern: 'case "DualImageEdgeDistance":\s*return new DualImageEdgeDistanceMeasurement'
    - from: "Action_FAIMeasurement.EStep.Measure (meas is DualImageEdgeDistanceMeasurement)"
      to: "TryGrabOrLoadFaiDualImages → RuntimeImageA/B 주입 → meas.TryExecute"
      via: "타입 분기 helper"
      pattern: "is DualImageEdgeDistanceMeasurement"
    - from: "DualImageEdgeDistanceMeasurement.TryExecute"
      to: "VisionAlgorithmService.TryFitLine ×2 (RuntimeImageA, RuntimeImageB)"
      via: "strip-loop fit_line on each ROI"
      pattern: "svc.TryFitLine\\(.*RuntimeImage"
---

<objective>
Phase 39.2 D-G1 — Bottom E5 듀얼 이미지 FAI 측정용 신규 `DualImageEdgeDistanceMeasurement` 타입 신설.
PointROI(점 형태, TeachingImagePath 이미지) 의 fit_line 중점 → LineROI(라인 형태, TeachingImagePath_Vertical 이미지) 의 fit_line 으로 projection_pl 수직 정사영 거리(mm) 를 측정한다.

Purpose: POC 2026-06-30 시연 Bottom E5 측정 항목 충족. 두 별도 이미지(서로 다른 조명/시야) 에서 각각 ROI 에지 추출 후 distance 산출 (Datum DualImage 패턴을 FAI 레이어로 이식).
Output: 신규 .cs (MeasurementBase 서브타입) + MeasurementFactory 등록 + Action_FAIMeasurement DualImage 로드 helper + EStep.Measure 분기 + csproj 등록.

핵심 lock (CONTEXT D-G1 / PATTERNS D-G1-A~C 반영):
- 옵션 B (transient HImage RuntimeImageA / RuntimeImageB 주입) 채택 — abstract TryExecute 시그니처 변경 0, Action_FAIMeasurement L237 호출 회귀 0
- 점 선택 = midpoint 고정 (D-G1-02)
- ROI 슬롯 컨벤션: PointROI ← TeachingImagePath, LineROI ← TeachingImagePath_Vertical (D-G1-03 Claude 기본값)
- Per-ROI Edge 7 파라미터 독립 (D-G1-04)
- Overlay: FAI-Edge1(Point 라인 + 중점 마커) + FAI-Edge2(Line 라인) + FAI-DistLine(청록 거리선)
- 단일 이미지 표시: PointROI 이미지(TeachingImagePath) 기본 background — SameFrame 계약 (planner 추가 lock)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.2-urgent-additions-2-2026-05-30/39.2-CONTEXT.md
@.planning/phases/39.2-urgent-additions-2-2026-05-30/39.2-PATTERNS.md

# Analog 파일 — 신규 클래스 구조 + projection_pl + overlay 패턴
@WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs
@WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs
@WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
@WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs
@WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
@WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
@WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs

<interfaces>
<!-- Critical contracts the executor needs. Do not re-explore the codebase. -->

From MeasurementBase.cs (abstract — DO NOT change signature):
```csharp
public abstract class MeasurementBase
{
    public string TypeName { get; }                       // override per type
    public string MeasurementName { get; set; }
    public string DatumRef { get; set; }
    public double LastMeasuredValue { get; set; }
    public bool LastJudgement { get; set; }
    public string LastSkipReason { get; set; }            // Phase 39 D-02
    public void EvaluateJudgement(double value);
    public void ClearResult();

    public abstract bool TryExecute(
        HImage image,                                     // ← DualImage 타입은 무시 (RuntimeImageA/B 사용)
        HTuple datumTransform,
        double pixelResolution,
        out double resultValue,
        out string error,
        out List<EdgeInspectionOverlay> overlays);
}
```

From IDatumOriginConsumer (ReringProject.Sequence):
```csharp
public interface IDatumOriginConsumer
{
    double DatumOriginRow { get; set; }
    double DatumOriginCol { get; set; }
    double DatumAngleRad { get; set; }
    double DatumAngle2Rad { get; set; }
    double DatumDetectedCircleRow { get; set; }
    double DatumDetectedCircleCol { get; set; }
}
```

From VisionAlgorithmService.cs:
```csharp
public bool TryFitLine(
    HImage image,
    double row, double col, double phi, double length1, double length2,
    HTuple datumTransform,
    int sampleCount, int trimCount, double sigma, int threshold,
    string edgeDirection, string edgePolarity,
    out double r1, out double c1, out double r2, out double c2,
    out string error,
    string selection = "all");
```

From EdgeInspectionOverlay.cs:
```csharp
public class EdgeInspectionOverlay
{
    public string RoiId { get; set; }                     // "FAI-Edge1" / "FAI-Edge2" / "FAI-DistLine"
    public double LineRow1, LineColumn1, LineRow2, LineColumn2;
    public List<EdgeInspectionPoint> Points;
}
public class EdgeInspectionPoint { public double Row, Column; }
```

From HalconDisplayService.cs convention (HALCON RoiId 라우팅):
- StartsWith("FAI-Edge") → 녹/적 분기 + Action_FAIMeasurement 가 -OK/-NG suffix 자동 부여
- "FAI-DistLine" → 청록(cyan) 거리선, suffix 미부여
- HALCON SetColor 표준 색상명만 (memory feedback)

From Action_FAIMeasurement.cs L237 (DO NOT change signature):
```csharp
ok = meas.TryExecute(image, transform, fai.PixelResolutionX, out resultValue, out measError, out measOverlays);
```

From Phase 37 TryGrabOrLoadDualDatumImages (L331-361 pattern — ADAPT to FAI):
- 2 path validation (IsNullOrEmpty + File.Exists)
- HImage 2개 생성 + 한쪽 실패 시 양쪽 dispose
- bool 반환 + out 2 HImage
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DualImageEdgeDistanceMeasurement 신규 클래스 + csproj 등록</name>
  <read_first>
    - @WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs (메인 analog: per-ROI 7 params + projection_pl + overlay 3종)
    - @WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs (다중 ROI 그룹 명명 패턴: ROIgroup_Row/Col/Phi/Length1/Length2 + ROIgroup_Threshold/Sigma/SampleCount/TrimCount/Polarity/Direction/Selection)
    - @WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (abstract TryExecute 시그니처)
    - @WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs (TryFitLine + ProjectionPl 호출 패턴)
    - @WPF_Example/DatumMeasurement.csproj (Compile ItemGroup 등록 위치 — 다른 Measurements/*.cs 인접 위치)
  </read_first>
  <behavior>
    - Test 1: typeof(DualImageEdgeDistanceMeasurement).BaseType == typeof(MeasurementBase)
    - Test 2: typeof(DualImageEdgeDistanceMeasurement).GetInterfaces() contains IDatumOriginConsumer
    - Test 3: new DualImageEdgeDistanceMeasurement(null).TypeName == "DualImageEdgeDistance"
    - Test 4: PointROI_/LineROI_ 각각 5 geometry(Row/Col/Phi/Length1/Length2) + 7 edge(Threshold/Sigma/SampleCount/TrimCount/Polarity/Direction/Selection) = 12 × 2 = 24 properties
    - Test 5: TeachingImagePath_Vertical (string, default "") + RuntimeImageA/B (HImage, [Browsable false] + [JsonIgnore]) 존재
    - Test 6: TryExecute 가 RuntimeImageA/B 사용 (인자 image 무시) — Action_FAIMeasurement L237 호출 시그니처 호환
    - Test 7 (W2 fix): IDatumOriginConsumer 6 필드 (DatumOriginRow/Col + DatumAngleRad/Angle2Rad + DatumDetectedCircleRow/Col) 모두 public double + 3중 attribute 존재
    - Test 8 (W2 fix): Newtonsoft.Json.JsonIgnore attribute 가 8개 라인 (6 datum transient + 2 RuntimeImage) 적용됨
  </behavior>
  <action>
[A] **신규 파일 작성** — `WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs`

스타일: **Allman brace** (Measurements/ 폴더 컨벤션, EdgeToLineDistance.cs 와 동일).
주석 마커: `//260530 hbk Phase 39.2 D-G1` (memory feedback_comment_convention).

```csharp
//260530 hbk Phase 39.2 D-G1 — Bottom E5 DualImage FAI Measurement
using System.Collections.Generic;
using HalconDotNet;
using PropertyTools.DataAnnotations;
using ReringProject.Halcon.Algorithms;
using ReringProject.Halcon.Models;

namespace ReringProject.Sequence
{
    /// <summary>
    /// 두 별도 이미지에서 각각 ROI 에지를 추출해 거리(mm)를 측정한다.
    ///  - PointROI: TeachingImagePath(또는 ShotParam.SimulImagePath) 이미지 → fit_line 중점 1점 ("Point").
    ///  - LineROI:  TeachingImagePath_Vertical 이미지 → fit_line 결과 라인 자체 ("Line").
    /// 거리 = HALCON projection_pl(Point → Line) 의 수직 정사영 거리 × pixelResolution.
    /// 두 이미지 좌표계는 동일 평면 가정 (Phase 36 SameFrame 계약).
    /// Action_FAIMeasurement 가 EStep.Measure 진입 시 RuntimeImageA / RuntimeImageB 를 주입한다 — 본 TryExecute 는 인자 image 를 무시.
    /// </summary>
    public class DualImageEdgeDistanceMeasurement : MeasurementBase, IDatumOriginConsumer //260530 hbk Phase 39.2 D-G1
    {
        public override string TypeName { get { return "DualImageEdgeDistance"; } } //260530 hbk Phase 39.2 D-G1

        // ── 듀얼 이미지 — 가로축(점 ROI) 이미지는 ShotParam.SimulImagePath 재사용, 세로축(라인 ROI) 이미지는 별도 경로 ──
        //260530 hbk Phase 39.2 D-G1-06 — LineROI 이미지 경로 (Action_FAIMeasurement.TryGrabOrLoadFaiDualImages 가 로드)
        [Category("Image|DualImage")] //260530 hbk Phase 39.2 D-G1
        [System.ComponentModel.Description("LineROI 검출용 별도 이미지 경로 — Bottom E5 패턴 (Phase 37 VTH-DualImage 슬롯 재사용)")] //260530 hbk Phase 39.2 D-G1
        public string TeachingImagePath_Vertical { get; set; } = ""; //260530 hbk Phase 39.2 D-G1

        // ── PointROI (점 형태, 1차 이미지 = ShotParam.SimulImagePath) ──
        [Category("PointROI|ROI")] //260530 hbk Phase 39.2 D-G1
        public double PointROI_Row { get; set; }
        public double PointROI_Col { get; set; }
        public double PointROI_Phi { get; set; }
        public double PointROI_Length1 { get; set; }
        public double PointROI_Length2 { get; set; }

        [Category("PointROI|Edge")] //260530 hbk Phase 39.2 D-G1-04
        public int PointROI_EdgeThreshold { get; set; } = 10;
        public double PointROI_Sigma { get; set; } = 1.0;
        public int PointROI_EdgeSampleCount { get; set; } = 20;
        public int PointROI_EdgeTrimCount { get; set; } = 10;
        [ItemsSourceProperty(nameof(EdgePolarityList))]
        public string PointROI_EdgePolarity { get; set; } = "DarkToLight";
        [ItemsSourceProperty(nameof(EdgeDirectionList))]
        public string PointROI_EdgeDirection { get; set; } = "TtoB";
        [ItemsSourceProperty(nameof(EdgeSelectionList))]
        public string PointROI_EdgeSelection { get; set; } = "All";

        // ── LineROI (라인 형태, 2차 이미지 = TeachingImagePath_Vertical) ──
        [Category("LineROI|ROI")] //260530 hbk Phase 39.2 D-G1
        public double LineROI_Row { get; set; }
        public double LineROI_Col { get; set; }
        public double LineROI_Phi { get; set; }
        public double LineROI_Length1 { get; set; }
        public double LineROI_Length2 { get; set; }

        [Category("LineROI|Edge")] //260530 hbk Phase 39.2 D-G1-04
        public int LineROI_EdgeThreshold { get; set; } = 10;
        public double LineROI_Sigma { get; set; } = 1.0;
        public int LineROI_EdgeSampleCount { get; set; } = 20;
        public int LineROI_EdgeTrimCount { get; set; } = 10;
        [ItemsSourceProperty(nameof(EdgePolarityList))]
        public string LineROI_EdgePolarity { get; set; } = "DarkToLight";
        [ItemsSourceProperty(nameof(EdgeDirectionList))]
        public string LineROI_EdgeDirection { get; set; } = "TtoB";
        [ItemsSourceProperty(nameof(EdgeSelectionList))]
        public string LineROI_EdgeSelection { get; set; } = "All";

        // ── ComboBox 옵션 래퍼 (EdgeToLineDistance L49-54 동형) ──
        [PropertyTools.DataAnnotations.Browsable(false)]
        public List<string> EdgeDirectionList { get { return EdgeOptionLists.Directions; } }
        [PropertyTools.DataAnnotations.Browsable(false)]
        public List<string> EdgePolarityList { get { return EdgeOptionLists.FAIPolarities; } }
        [PropertyTools.DataAnnotations.Browsable(false)]
        public List<string> EdgeSelectionList { get { return EdgeOptionLists.Selections; } }

        // ── IDatumOriginConsumer transient 6 필드 (3중 attribute — EdgeToLineDistance L69-95 패턴) ──
        [System.ComponentModel.Browsable(false)]
        [PropertyTools.DataAnnotations.Browsable(false)]
        [Newtonsoft.Json.JsonIgnore]
        public double DatumOriginRow { get; set; }
        [System.ComponentModel.Browsable(false)]
        [PropertyTools.DataAnnotations.Browsable(false)]
        [Newtonsoft.Json.JsonIgnore]
        public double DatumOriginCol { get; set; }
        [System.ComponentModel.Browsable(false)]
        [PropertyTools.DataAnnotations.Browsable(false)]
        [Newtonsoft.Json.JsonIgnore]
        public double DatumAngleRad { get; set; }
        [System.ComponentModel.Browsable(false)]
        [PropertyTools.DataAnnotations.Browsable(false)]
        [Newtonsoft.Json.JsonIgnore]
        public double DatumAngle2Rad { get; set; }
        [System.ComponentModel.Browsable(false)]
        [PropertyTools.DataAnnotations.Browsable(false)]
        [Newtonsoft.Json.JsonIgnore]
        public double DatumDetectedCircleRow { get; set; }
        [System.ComponentModel.Browsable(false)]
        [PropertyTools.DataAnnotations.Browsable(false)]
        [Newtonsoft.Json.JsonIgnore]
        public double DatumDetectedCircleCol { get; set; }

        // ── Action_FAIMeasurement 가 EStep.Measure 진입 시 주입 (옵션 B 패턴) ──
        //260530 hbk Phase 39.2 D-G1 — RuntimeImageA = PointROI 이미지 (TeachingImagePath / SimulImagePath)
        //                              RuntimeImageB = LineROI 이미지 (TeachingImagePath_Vertical)
        //  TryExecute 호출 후 Action_FAIMeasurement 가 dispose + 본 필드 null 리셋.
        [System.ComponentModel.Browsable(false)]
        [PropertyTools.DataAnnotations.Browsable(false)]
        [Newtonsoft.Json.JsonIgnore]
        public HImage RuntimeImageA { get; set; }
        [System.ComponentModel.Browsable(false)]
        [PropertyTools.DataAnnotations.Browsable(false)]
        [Newtonsoft.Json.JsonIgnore]
        public HImage RuntimeImageB { get; set; }

        public DualImageEdgeDistanceMeasurement(object owner) : base(owner) { }

        public override bool TryExecute(
            HImage image,                                       // 무시 — RuntimeImageA/B 사용
            HTuple datumTransform,
            double pixelResolution,
            out double resultValue,
            out string error,
            out List<EdgeInspectionOverlay> overlays)
        {
            resultValue = 0;
            error = null;
            overlays = new List<EdgeInspectionOverlay>();

            // (0) Runtime 이미지 가드 — Action_FAIMeasurement 가 주입 실패 시 명시적 에러
            if (RuntimeImageA == null || RuntimeImageB == null)
            {
                error = "DualImage 이미지 미주입 (RuntimeImageA/B null)";
                return false;
            }

            var svc = new VisionAlgorithmService();

            // (1) PointROI — RuntimeImageA 에서 fit_line → midpoint
            double pa_r1, pa_c1, pa_r2, pa_c2;
            if (!svc.TryFitLine(RuntimeImageA,
                PointROI_Row, PointROI_Col, PointROI_Phi, PointROI_Length1, PointROI_Length2,
                datumTransform,
                PointROI_EdgeSampleCount, PointROI_EdgeTrimCount, PointROI_Sigma, PointROI_EdgeThreshold,
                PointROI_EdgeDirection, PointROI_EdgePolarity,
                out pa_r1, out pa_c1, out pa_r2, out pa_c2, out error,
                PointROI_EdgeSelection))
            {
                return false;
            }
            double pointRow = (pa_r1 + pa_r2) / 2.0;            //260530 hbk Phase 39.2 D-G1-02 midpoint 고정
            double pointCol = (pa_c1 + pa_c2) / 2.0;

            // (2) LineROI — RuntimeImageB 에서 fit_line → 라인 그대로 (시작점/끝점 사용)
            double lb_r1, lb_c1, lb_r2, lb_c2;
            if (!svc.TryFitLine(RuntimeImageB,
                LineROI_Row, LineROI_Col, LineROI_Phi, LineROI_Length1, LineROI_Length2,
                datumTransform,
                LineROI_EdgeSampleCount, LineROI_EdgeTrimCount, LineROI_Sigma, LineROI_EdgeThreshold,
                LineROI_EdgeDirection, LineROI_EdgePolarity,
                out lb_r1, out lb_c1, out lb_r2, out lb_c2, out error,
                LineROI_EdgeSelection))
            {
                return false;
            }

            // (3) projection_pl(point → line) — EdgeToLineDistance L193-204 패턴 동형
            double footRow = pointRow;
            double footCol = pointCol;
            bool footOk = false;
            try
            {
                HTuple prRow, prCol;
                HOperatorSet.ProjectionPl(pointRow, pointCol, lb_r1, lb_c1, lb_r2, lb_c2, out prRow, out prCol);
                footRow = prRow.D;
                footCol = prCol.D;
                footOk = true;
            }
            catch
            {
                error = "projection_pl 실패";
                return false;
            }
            double dRow = pointRow - footRow;
            double dCol = pointCol - footCol;
            double distPx = System.Math.Sqrt(dRow * dRow + dCol * dCol);
            resultValue = distPx * pixelResolution;             // mm

            // (4) Overlay 생성 — RoiId 컨벤션 강제 (HalconDisplayService 분기 충족)
            //   FAI-Edge1 = PointROI 검출 라인 + 중점 마커 (녹/적 + suffix 자동)
            //   FAI-Edge2 = LineROI 검출 라인 (녹/적 + suffix 자동)
            //   FAI-DistLine = projection 거리선 (청록 고정, suffix 미부여)
            overlays.Add(new EdgeInspectionOverlay
            {
                RoiId = "FAI-Edge1",
                LineRow1 = pa_r1, LineColumn1 = pa_c1,
                LineRow2 = pa_r2, LineColumn2 = pa_c2,
                Points = new List<EdgeInspectionPoint>
                {
                    new EdgeInspectionPoint { Row = pointRow, Column = pointCol }
                }
            });
            overlays.Add(new EdgeInspectionOverlay
            {
                RoiId = "FAI-Edge2",
                LineRow1 = lb_r1, LineColumn1 = lb_c1,
                LineRow2 = lb_r2, LineColumn2 = lb_c2
            });
            if (footOk)
            {
                overlays.Add(new EdgeInspectionOverlay
                {
                    RoiId = "FAI-DistLine",
                    LineRow1 = footRow, LineColumn1 = footCol,
                    LineRow2 = pointRow, LineColumn2 = pointCol,
                    Points = new List<EdgeInspectionPoint>
                    {
                        new EdgeInspectionPoint { Row = footRow, Column = footCol },
                        new EdgeInspectionPoint { Row = pointRow, Column = pointCol }
                    }
                });
            }

            return true;
        }
    }
}
```

[B] **csproj 등록** — `WPF_Example/DatumMeasurement.csproj`:
Compile ItemGroup 의 `Measurements/EdgeToLineDistanceMeasurement.cs` 또는 `Measurements/CompoundShortAxisDistanceMeasurement.cs` 인접 위치 (Measurements 라인이 알파벳 그룹화되어 있음 — 그 인근) 에 아래 1줄 추가:

```xml
<Compile Include="Custom\Sequence\Inspection\Measurements\DualImageEdgeDistanceMeasurement.cs" />
```

Build 가 CS0246 / CS2001 없이 통과해야 한다 (Rule 3 auto-fix — blocking error).

[C] **HALCON 정책 준수 (memory feedback)** :
- 모든 HOperatorSet 호출은 try { } catch — projection_pl 만 본 코드에 직접 호출, fit_line 은 svc.TryFitLine wrapper (내부에 strip-loop + 안전 catch 포함)
- SetColor 직접 호출 0 — RoiId 만 지정, 색상은 HalconDisplayService 가 prefix 라우팅
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /v:m</automated>
  </verify>
  <acceptance_criteria>
    - File exists: WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs
    - `Grep -F "class DualImageEdgeDistanceMeasurement : MeasurementBase, IDatumOriginConsumer" matches 1` (W3 fix: -F)
    - `Grep -F "return \"DualImageEdgeDistance\"" matches 1` (TypeName getter)
    - `Grep -F "public double PointROI_Row" matches 1`
    - `Grep -F "public double LineROI_Row" matches 1`
    - `Grep -F "public string PointROI_EdgeSelection" matches 1`
    - `Grep -F "public string LineROI_EdgeSelection" matches 1`
    - `Grep -F "public string TeachingImagePath_Vertical" matches 1`
    - `Grep -F "public HImage RuntimeImageA" matches 1`
    - `Grep -F "public HImage RuntimeImageB" matches 1`
    - `Grep -F "HOperatorSet.ProjectionPl" matches 1`
    - `Grep -F "svc.TryFitLine" matches 2` (PointROI + LineROI)
    - `Grep -F "\"FAI-Edge1\"" matches 1`
    - `Grep -F "\"FAI-Edge2\"" matches 1`
    - `Grep -F "\"FAI-DistLine\"" matches 1`
    - **W2 fix — IDatumOriginConsumer 6 필드 명시 grep:**
      - `Grep -F "public double DatumOriginRow" matches 1`
      - `Grep -F "public double DatumOriginCol" matches 1`
      - `Grep -F "public double DatumAngleRad" matches 1`
      - `Grep -F "public double DatumAngle2Rad" matches 1`
      - `Grep -F "public double DatumDetectedCircleRow" matches 1`
      - `Grep -F "public double DatumDetectedCircleCol" matches 1`
    - **W2 fix — JsonIgnore 총 8 라인 검증 (6 datum transient + 2 RuntimeImage):**
      - `Grep -F "Newtonsoft.Json.JsonIgnore" in DualImageEdgeDistanceMeasurement.cs matches 8`
    - csproj contains: `<Compile Include="Custom\Sequence\Inspection\Measurements\DualImageEdgeDistanceMeasurement.cs" />`
    - msbuild Rebuild Debug/x64 PASS (0 errors)
    - 신규 warning 0 (Phase 39 baseline 대비)
    - 모든 신규/수정 라인에 //260530 hbk Phase 39.2 D-G1 마커
  </acceptance_criteria>
  <done>
    DualImageEdgeDistance 신규 타입 빌드 통과, MeasurementBase 시그니처 0 변경, IDatumOriginConsumer 6 필드 구현 (W2 fix grep verified), RuntimeImageA/B transient 주입 패턴 준비, JsonIgnore 8 라인 적용 (W2 fix grep verified).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MeasurementFactory dispatch + GetTypeNames 등록</name>
  <read_first>
    - @WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs (전체 — 16 case + 10 GetTypeNames)
  </read_first>
  <behavior>
    - Test 1: MeasurementFactory.Create("DualImageEdgeDistance", null) returns non-null DualImageEdgeDistanceMeasurement instance
    - Test 2: MeasurementFactory.GetTypeNames().Length == 11 (was 10)
    - Test 3: GetTypeNames() last element == "DualImageEdgeDistance"
    - Test 4: 기존 15 case dispatch 결과 byte-identical (회귀 0)
    - Test 5: 기존 10 GetTypeNames 원소 순서/문자 변경 0
  </behavior>
  <action>
**파일:** `WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs`

스타일: 기존 파일 K&R brace (one-line case) 보존.

**[A] switch 분기 추가** — `case "CompoundShortAxisDistance":` 와 `default:` 사이 (L45-46 부근):

```csharp
case "CompoundShortAxisDistance": //260523 hbk Phase 32 — E3 단축 환원
    return new CompoundShortAxisDistanceMeasurement(owner); //260523 hbk Phase 32 — E3 단축 환원
case "DualImageEdgeDistance": //260530 hbk Phase 39.2 D-G1 — Bottom E5
    return new DualImageEdgeDistanceMeasurement(owner); //260530 hbk Phase 39.2 D-G1
default:
    return null;
```

**[B] GetTypeNames 배열 마지막 원소 추가** — `"CompoundShortAxisDistance"` 뒤에 comma 추가 + 신규 원소 추가:

```csharp
return new string[]
{
    "CircleDiameter",
    "EdgeToLineDistance", //260512 hbk Phase 23 ALG-01
    "CircleCenterDistance", //260519 hbk Phase 31 D-01 E8
    "EdgeToLineAngle", //260519 hbk Phase 31 D-05
    "ArcEdgeDistance", //260519 hbk Phase 31 D-08
    "ArcLineIntersectDistance", //260519 hbk Phase 31 D-01 I9/I10
    "CompoundAngle", //260519 hbk Phase 31 D-11 E2
    "CompoundCenterCDistance", //260519 hbk Phase 31 D-11 E9
    "CompoundCenterBDistance", //260519 hbk Phase 31 D-11 E10
    "CompoundShortAxisDistance", //260523 hbk Phase 32 — E3 단축 환원
    "DualImageEdgeDistance" //260530 hbk Phase 39.2 D-G1 — Bottom E5
};
```

⚠️ **Anti-goal (CONTEXT D-G1 마지막):** 기존 15 case + 10 GetTypeNames 원소 순서/문자 변경 0 — 단일 case + 단일 원소 추가만 허용. Comma 추가는 변경 0 으로 간주 (이전 마지막 원소 뒤 trailing comma 없었으므로 새로 1개 추가됨).
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /v:m</automated>
  </verify>
  <acceptance_criteria>
    - `Grep -F "case \"DualImageEdgeDistance\":" in MeasurementFactory.cs matches 1` (W3 fix: -F)
    - `Grep -F "return new DualImageEdgeDistanceMeasurement(owner);" matches 1`
    - `Grep -F "\"DualImageEdgeDistance\"" in MeasurementFactory.cs matches 2` (case + GetTypeNames)
    - GetTypeNames 의 마지막 원소 = "DualImageEdgeDistance" (위치 11번째)
    - 기존 15 case의 case literal 문자열 변경 0 (`Grep -F "case \"CircleDiameter\":" matches 1` / `Grep -F "case \"EdgePairDistance\":" matches 1` 등 모두 보존)
    - 기존 10 GetTypeNames 원소 (CircleDiameter ~ CompoundShortAxisDistance) 순서/문자 변경 0
    - msbuild Rebuild Debug/x64 PASS (0 errors)
    - 신규 warning 0
    - 신규/수정 라인에 //260530 hbk Phase 39.2 D-G1 마커
  </acceptance_criteria>
  <done>
    MeasurementFactory.Create + GetTypeNames 가 DualImageEdgeDistance 를 dispatch + 노출. INI Type=DualImageEdgeDistance 로드 + PropertyGrid FAI Type 콤보 추가 항목 가시화 baseline 충족.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Action_FAIMeasurement TryGrabOrLoadFaiDualImages helper + EStep.Measure DualImage 분기</name>
  <read_first>
    - @WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (전체, 특히 L233-298 EStep.Measure 루프 + L307-361 GrabOrLoad helpers)
    - @WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs (Task 1 산물 — RuntimeImageA/B 주입 컨벤션)
  </read_first>
  <behavior>
    - Test 1: Action_FAIMeasurement.cs 에 private bool TryGrabOrLoadFaiDualImages(MeasurementBase, out HImage, out HImage) 헬퍼 1개 존재
    - Test 2: EStep.Measure 의 meas.TryExecute 호출 직전에 `meas is DualImageEdgeDistanceMeasurement` 분기 1개 추가
    - Test 3: DualImage 분기에서 TryGrabOrLoadFaiDualImages → RuntimeImageA/B 주입 → meas.TryExecute → finally 에서 양 HImage Dispose + RuntimeImageA/B null 리셋
    - Test 4: 기존 1-image 경로 (`else` 블록) 의 TryExecute 호출 시그니처 L237 변경 0
    - Test 5: Phase 39 D-01 IsDatumFailed 게이트 (L179-188), D-02 WasDatumSkipped (L277-282), Phase 39.1-03 LastOverlays write-back (L263, L283) 변경 0
  </behavior>
  <action>
**파일:** `WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs`

스타일: 기존 파일 K&R brace 보존. 주석 마커 `//260530 hbk Phase 39.2 D-G1`.

**[A] 신규 helper 메서드 추가** — 클래스 끝부분 (L361 `TryGrabOrLoadDualDatumImages` 메서드 직후) 에 다음 메서드 추가:

```csharp
//260530 hbk Phase 39.2 D-G1-06 — DualImageEdgeDistanceMeasurement 측정용 양 이미지 로드.
//  imageA: ShotParam.SimulImagePath (1차) — PointROI 검출용 (FAI 1차 이미지 = Shot 검사 이미지 재사용, 회귀 0 baseline).
//  imageB: meas.TeachingImagePath_Vertical (2차) — LineROI 검출용 (D-G1-03 슬롯 컨벤션).
//  P37 TryGrabOrLoadDualDatumImages 동형 패턴 — 경로 빈/파일없음 → false + 로그.
//  HImage 한쪽 생성 실패 시 양쪽 Dispose + false (메모리 누수 방지).
private bool TryGrabOrLoadFaiDualImages(MeasurementBase meas, out HImage imageA, out HImage imageB) { //260530 hbk Phase 39.2 D-G1-06
    imageA = null; //260530 hbk Phase 39.2 D-G1-06
    imageB = null; //260530 hbk Phase 39.2 D-G1-06
    if (ShotParam == null) { //260530 hbk Phase 39.2 D-G1-06
        Logging.PrintErrLog((int)ELogType.Error, "[FAI DualImage] ShotParam null"); //260530 hbk Phase 39.2 D-G1-06
        return false; //260530 hbk Phase 39.2 D-G1-06
    }
    var dualMeas = meas as DualImageEdgeDistanceMeasurement; //260530 hbk Phase 39.2 D-G1-06
    if (dualMeas == null) { //260530 hbk Phase 39.2 D-G1-06
        Logging.PrintErrLog((int)ELogType.Error, "[FAI DualImage] meas 가 DualImageEdgeDistanceMeasurement 가 아닙니다"); //260530 hbk Phase 39.2 D-G1-06
        return false; //260530 hbk Phase 39.2 D-G1-06
    }
    string pathA = ShotParam.SimulImagePath; //260530 hbk Phase 39.2 D-G1-06 — PointROI 이미지 = Shot baseline
    string pathB = dualMeas.TeachingImagePath_Vertical; //260530 hbk Phase 39.2 D-G1-06 — LineROI 이미지 = meas 별도 경로

    if (string.IsNullOrEmpty(pathA) || !File.Exists(pathA)) { //260530 hbk Phase 39.2 D-G1-06
        Logging.PrintErrLog((int)ELogType.Error, "[FAI DualImage] PointROI 이미지 경로 비어 있거나 파일 없음 (SimulImagePath)"); //260530 hbk Phase 39.2 D-G1-06
        return false; //260530 hbk Phase 39.2 D-G1-06
    }
    if (string.IsNullOrEmpty(pathB) || !File.Exists(pathB)) { //260530 hbk Phase 39.2 D-G1-06
        Logging.PrintErrLog((int)ELogType.Error, "[FAI DualImage] LineROI 이미지 경로 비어 있거나 파일 없음 (TeachingImagePath_Vertical)"); //260530 hbk Phase 39.2 D-G1-06
        return false; //260530 hbk Phase 39.2 D-G1-06
    }
    try { imageA = new HImage(pathA); } catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, "[FAI DualImage] PointROI 이미지 로드 실패: " + ex.Message); imageA = null; } //260530 hbk Phase 39.2 D-G1-06
    try { imageB = new HImage(pathB); } catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, "[FAI DualImage] LineROI 이미지 로드 실패: " + ex.Message); imageB = null; } //260530 hbk Phase 39.2 D-G1-06
    if (imageA == null || imageB == null) { //260530 hbk Phase 39.2 D-G1-06
        if (imageA != null) { try { imageA.Dispose(); } catch { } } //260530 hbk Phase 39.2 D-G1-06
        if (imageB != null) { try { imageB.Dispose(); } catch { } } //260530 hbk Phase 39.2 D-G1-06
        imageA = null; imageB = null; //260530 hbk Phase 39.2 D-G1-06
        return false; //260530 hbk Phase 39.2 D-G1-06
    }
    return true; //260530 hbk Phase 39.2 D-G1-06
}
```

**[B] EStep.Measure TryExecute 호출 분기 추가** — 정확한 위치: L233-243 `try { ok = meas.TryExecute(...); } catch (Exception ex) { ... }` 블록 교체.

**현재 코드 (L233-243):**
```csharp
double resultValue;
string measError;
List<EdgeInspectionOverlay> measOverlays; //260422 hbk Phase 7: (D-01)
bool ok = false;
try {
    ok = meas.TryExecute(image, transform, fai.PixelResolutionX, out resultValue, out measError, out measOverlays); //260422 hbk Phase 7: 6-param (D-01)
} catch (Exception ex) {
    ok = false;
    resultValue = 0;
    measError = ex.Message;
    measOverlays = null; //260422 hbk Phase 7: 예외 경로 null-safe (D-02)
}
```

**교체 (DualImage 분기 추가):**
```csharp
double resultValue;
string measError;
List<EdgeInspectionOverlay> measOverlays; //260422 hbk Phase 7: (D-01)
bool ok = false;
//260530 hbk Phase 39.2 D-G1 — DualImage 타입 분기: 양 이미지 별도 로드 → RuntimeImageA/B 주입 → TryExecute → dispose
if (meas is DualImageEdgeDistanceMeasurement dualMeas) { //260530 hbk Phase 39.2 D-G1
    HImage imgA = null, imgB = null; //260530 hbk Phase 39.2 D-G1
    try {
        if (TryGrabOrLoadFaiDualImages(meas, out imgA, out imgB)) { //260530 hbk Phase 39.2 D-G1
            dualMeas.RuntimeImageA = imgA; //260530 hbk Phase 39.2 D-G1 — transient property, TryExecute 가 image 인자 무시
            dualMeas.RuntimeImageB = imgB; //260530 hbk Phase 39.2 D-G1
            try {
                ok = meas.TryExecute(image, transform, fai.PixelResolutionX, out resultValue, out measError, out measOverlays); //260530 hbk Phase 39.2 D-G1 — 시그니처 변경 0
            } catch (Exception ex) {
                ok = false; resultValue = 0; measError = ex.Message; measOverlays = null;
            }
        } else {
            ok = false; resultValue = 0; measError = "DualImage 이미지 로드 실패"; measOverlays = null; //260530 hbk Phase 39.2 D-G1
        }
    } finally {
        if (imgA != null) { try { imgA.Dispose(); } catch { } } //260530 hbk Phase 39.2 D-G1
        if (imgB != null) { try { imgB.Dispose(); } catch { } } //260530 hbk Phase 39.2 D-G1
        dualMeas.RuntimeImageA = null; //260530 hbk Phase 39.2 D-G1
        dualMeas.RuntimeImageB = null; //260530 hbk Phase 39.2 D-G1
    }
} else { //260530 hbk Phase 39.2 D-G1 — 기존 1-image 경로 (회귀 0)
    try {
        ok = meas.TryExecute(image, transform, fai.PixelResolutionX, out resultValue, out measError, out measOverlays); //260422 hbk Phase 7: 6-param (D-01)
    } catch (Exception ex) {
        ok = false;
        resultValue = 0;
        measError = ex.Message;
        measOverlays = null; //260422 hbk Phase 7: 예외 경로 null-safe (D-02)
    }
}
```

⚠️ **Anti-goal (CONTEXT D-G1 마지막):**
- EStep.Measure D-01 게이트 (L179-188 `IsDatumFailed`) 변경 0
- L263 `faiOverlays.AddRange(measOverlays)` (Phase 39.1-03 LastOverlays write-back) 변경 0
- L277-282 `WasDatumSkipped` 누적 (Phase 39 D-02) 변경 0
- L283 `fai.LastOverlays = faiOverlays;` 변경 0
- IDatumOriginConsumer 주입 (L202-231) 변경 0 — DualImage 타입도 IDatumOriginConsumer 구현이므로 동일 경로 통과
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /v:m</automated>
  </verify>
  <acceptance_criteria>
    - `Grep -F "private bool TryGrabOrLoadFaiDualImages" in Action_FAIMeasurement.cs matches 1` (W3 fix: -F)
    - `Grep -F "is DualImageEdgeDistanceMeasurement dualMeas" matches 1`
    - `Grep -F "dualMeas.RuntimeImageA = imgA" matches 1`
    - `Grep -F "dualMeas.RuntimeImageB = imgB" matches 1`
    - `Grep -F "dualMeas.RuntimeImageA = null" matches 1` (finally)
    - `Grep -F "dualMeas.RuntimeImageB = null" matches 1`
    - L179-188 IsDatumFailed 게이트 블록 텍스트 변경 0 (`Grep -F "parentSeq2.IsDatumFailed(meas.DatumRef)" matches 1`)
    - L263 `faiOverlays.AddRange(measOverlays)` 보존 (`Grep -F "faiOverlays.AddRange(measOverlays)" matches 1`)
    - L283 `fai.LastOverlays = faiOverlays;` 보존 (`Grep -F "fai.LastOverlays = faiOverlays;" matches 1`)
    - meas.TryExecute 호출 시그니처 변경 0 — 두 호출 위치 모두 `(image, transform, fai.PixelResolutionX, out resultValue, out measError, out measOverlays)` (`Grep -F "meas.TryExecute(image, transform, fai.PixelResolutionX" matches 2` — DualImage 분기 + else 분기)
    - msbuild Rebuild Debug/x64 PASS (0 errors)
    - 신규 warning 0
    - 모든 신규/수정 라인에 //260530 hbk Phase 39.2 D-G1 마커
  </acceptance_criteria>
  <done>
    Action_FAIMeasurement 가 DualImage 타입 감지 → 양 이미지 로드 → RuntimeImageA/B 주입 → TryExecute → finally dispose 까지 자동 처리. Phase 39 / 39.1 lenient + gate + write-back 회귀 0.
  </done>
</task>

</tasks>

<verification>
**End-to-end smoke check (msbuild PASS 시 자동 충족):**
1. `MeasurementFactory.Create("DualImageEdgeDistance", null)` 가 null 이 아님 (Task 2)
2. FAI Type 콤보에 11번째 항목 'DualImageEdgeDistance' 노출 (Task 2 — PropertyGrid 측 UAT 에서 확인)
3. INI Type=DualImageEdgeDistance 로 저장한 measurement 가 다음 로드 시 정상 복원 (PointROI_/LineROI_ 필드 자동 직렬화)
4. Action_FAIMeasurement.cs `is DualImageEdgeDistanceMeasurement` 분기 단일 도입 (Task 3)

**회귀 0 가드:**
- 기존 10 measurement 타입 TryExecute 경로 변경 0 (Task 3 else 분기)
- Phase 39 IsDatumFailed gate / WasDatumSkipped / LastOverlays write-back 보존
- INI 파일 내 Type=다른값 측정은 기존 dispatch + 결과 byte-identical
</verification>

<success_criteria>
- [ ] Task 1: DualImageEdgeDistanceMeasurement.cs 신규 작성 + csproj 등록 → msbuild PASS + W2 fix grep (6 datum 필드 + JsonIgnore 8 matches)
- [ ] Task 2: MeasurementFactory 11번째 case + GetTypeNames 11번째 원소 → msbuild PASS
- [ ] Task 3: TryGrabOrLoadFaiDualImages helper + EStep.Measure DualImage 분기 → msbuild PASS
- [ ] 전체 msbuild Rebuild Debug/x64 0 errors / 0 신규 warnings
- [ ] Action_FAIMeasurement L179-188 IsDatumFailed gate / L263 faiOverlays / L283 LastOverlays 텍스트 변경 0
- [ ] 모든 신규/수정 라인에 `//260530 hbk Phase 39.2 D-G1` 마커
- [ ] HALCON 정책: SetColor 직접 호출 0, projection_pl try/catch, fit_line 은 svc wrapper 사용
</success_criteria>

<output>
After completion, create `.planning/phases/39.2-urgent-additions-2-2026-05-30/39.2-01-SUMMARY.md` covering:
- DualImageEdgeDistanceMeasurement.cs 신설 결과 (line count, IDatumOriginConsumer 6 필드 확인 + W2 fix JsonIgnore 8 matches 입증)
- MeasurementFactory case + GetTypeNames 추가 결과 (11번째 위치 확인)
- TryGrabOrLoadFaiDualImages helper + EStep.Measure 분기 line count
- msbuild Rebuild Debug/x64 결과 (errors / warnings)
- 회귀 0 가드 grep 결과 (IsDatumFailed / faiOverlays / LastOverlays)
- INI Type=DualImageEdgeDistance round-trip 검증 (manual/dev — SIMUL UAT 는 Plan 05 에서)
</output>
