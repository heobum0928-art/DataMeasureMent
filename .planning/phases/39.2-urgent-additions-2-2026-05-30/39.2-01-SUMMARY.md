---
phase: 39.2-urgent-additions-2-2026-05-30
plan: 01
subsystem: algorithm / fai-measurement
tags: [fai, dualimage, halcon, measurement, bottom-e5]
requires:
  - Phase 39 D-01 IsDatumFailed gate (preserved)
  - Phase 39 D-02 WasDatumSkipped (preserved)
  - Phase 39.1-03 LastOverlays write-back (preserved)
  - Phase 37 TryGrabOrLoadDualDatumImages pattern (analog)
provides:
  - DualImageEdgeDistanceMeasurement (new MeasurementBase subtype)
  - MeasurementFactory dispatch (16th case + 11th GetTypeNames entry)
  - Action_FAIMeasurement TryGrabOrLoadFaiDualImages helper + EStep.Measure DualImage branch
affects:
  - FAI Type ComboBox (PropertyGrid + InspectionListView) → adds 'DualImageEdgeDistance'
  - INI recipe round-trip — new Type=DualImageEdgeDistance supported
tech-stack:
  added: []
  patterns:
    - "Option B transient HImage injection (RuntimeImageA/B) — abstract TryExecute signature unchanged"
    - "Per-ROI 5 geometry + 7 edge params (PointROI / LineROI independent)"
    - "projection_pl midpoint→line distance (EdgeToLineDistance L193-204 analog)"
    - "3x attribute IDatumOriginConsumer transient (System.ComponentModel.Browsable + PropertyTools.Browsable + Newtonsoft.Json.JsonIgnore)"
key-files:
  created:
    - WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs
  modified:
    - WPF_Example/DatumMeasurement.csproj
    - WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
decisions:
  - "D-G1-01 신규 타입 DualImageEdgeDistanceMeasurement (직접 MeasurementBase 상속, EdgeToLineDistance 서브타입 X)"
  - "D-G1-02 midpoint 고정 (PointSelection enum 미도입)"
  - "D-G1-03 ROI 슬롯 컨벤션 — PointROI ← SimulImagePath, LineROI ← TeachingImagePath_Vertical (Claude 기본값)"
  - "D-G1-04 per-ROI 7 edge 파라미터 독립"
  - "옵션 B (transient HImage 주입) 채택 — abstract TryExecute 시그니처 변경 0, Action_FAIMeasurement L237 호출 회귀 0"
metrics:
  duration: ~50 minutes (single agent worktree)
  completed: 2026-05-30
  commits: 3 task commits (6a671c0 / d982ea4 / b76ef86)
---

# Phase 39.2 Plan 01: Bottom E5 DualImage FAI Measurement (D-G1) Summary

Bottom E5 양 이미지 FAI 측정용 신규 `DualImageEdgeDistanceMeasurement` 타입 + 팩토리 dispatch + Action_FAIMeasurement 양-이미지 로드 helper + EStep.Measure 분기를 3 task 로 원자 commit. msbuild PASS, 회귀 가드 7건 grep 검증, Option B 패턴(transient HImage 주입)으로 MeasurementBase abstract 시그니처 변경 0.

## Tasks

### Task 1 — DualImageEdgeDistanceMeasurement 신규 클래스 + csproj 등록 (commit `6a671c0`)

- 신규 파일: `WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` (227 lines)
- Style: Allman brace (Measurements/ 폴더 컨벤션, EdgeToLineDistance.cs 동형)
- Inherits: `MeasurementBase, IDatumOriginConsumer`
- `TypeName` = "DualImageEdgeDistance"
- Properties:
  - `TeachingImagePath_Vertical` (string, "" default) — LineROI 이미지 경로
  - `PointROI_Row/Col/Phi/Length1/Length2` (5 doubles) + `PointROI_EdgeThreshold/Sigma/EdgeSampleCount/EdgeTrimCount/EdgePolarity/EdgeDirection/EdgeSelection` (7 edge params)
  - `LineROI_*` 동형 5 + 7
  - 3x attribute IDatumOriginConsumer transient 6 필드 (DatumOriginRow/Col + DatumAngleRad/Angle2Rad + DatumDetectedCircleRow/Col)
  - `RuntimeImageA/B` (HImage, 3x attribute transient) — Action_FAIMeasurement 주입
- TryExecute 흐름:
  1. RuntimeImageA/B null guard → 명시 error
  2. `svc.TryFitLine(RuntimeImageA, PointROI_*)` → 라인 시작/끝점 → midpoint 1점 (D-G1-02)
  3. `svc.TryFitLine(RuntimeImageB, LineROI_*)` → 라인 시작/끝점 (라인 그대로)
  4. `HOperatorSet.ProjectionPl(point → line)` try/catch → footRow/Col
  5. `distPx = sqrt(dRow² + dCol²)` × pixelResolution → mm
  6. Overlays 3종: FAI-Edge1 (PointROI 라인 + 중점), FAI-Edge2 (LineROI 라인), FAI-DistLine (청록 projection drop)
- csproj: `Custom\Sequence\Inspection\Measurements\DualImageEdgeDistanceMeasurement.cs` Compile ItemGroup 등록 (CompoundShortAxisDistance 다음 줄)

**Grep verification:**
- `class DualImageEdgeDistanceMeasurement : MeasurementBase, IDatumOriginConsumer` = 1 ✓
- `Newtonsoft.Json.JsonIgnore` = **8 lines** ✓ (W2 fix: 6 datum transient + 2 RuntimeImage)
- `public double DatumOriginRow/Col/AngleRad/Angle2Rad/DetectedCircleRow/Col` = 6 fields ✓ (W2 fix)
- `svc.TryFitLine` = 2 (PointROI + LineROI) ✓
- `HOperatorSet.ProjectionPl` = 1 ✓
- "FAI-Edge1" / "FAI-Edge2" / "FAI-DistLine" = 1 each ✓
- csproj contains `DualImageEdgeDistanceMeasurement.cs` = 1 ✓

### Task 2 — MeasurementFactory dispatch + GetTypeNames 등록 (commit `d982ea4`)

- `Create()` switch: 16th case "DualImageEdgeDistance" → `new DualImageEdgeDistanceMeasurement(owner)` (CompoundShortAxisDistance 다음 + default 직전, 5 lines 추가)
- `GetTypeNames()`: 11th array element "DualImageEdgeDistance" (was 10) — CompoundShortAxisDistance 뒤 trailing comma + 신규 1 element

**Grep verification:**
- `case "DualImageEdgeDistance":` = 1 ✓
- `return new DualImageEdgeDistanceMeasurement(owner);` = 1 ✓
- `"DualImageEdgeDistance"` (case + array) = 2 ✓
- 기존 case 보존 (CircleDiameter / EdgePairDistance / CompoundShortAxisDistance) = 3 ✓ (regression 0)

### Task 3 — Action_FAIMeasurement TryGrabOrLoadFaiDualImages helper + EStep.Measure DualImage 분기 (commit `b76ef86`)

- 신규 helper `TryGrabOrLoadFaiDualImages(MeasurementBase meas, out HImage imageA, out HImage imageB)`:
  - ShotParam null guard
  - meas as DualImageEdgeDistanceMeasurement cast guard
  - pathA = ShotParam.SimulImagePath (PointROI = Shot baseline, 회귀 0)
  - pathB = dualMeas.TeachingImagePath_Vertical (LineROI = meas-specific)
  - 2-tier path/file guard (IsNullOrEmpty + File.Exists)
  - HImage 2개 try/catch 생성 + 한쪽 실패 시 양쪽 Dispose + return false
  - Phase 37 TryGrabOrLoadDualDatumImages 동형 패턴 (단, datum-arg → meas-arg + DatumConfig → DualImageEdgeDistanceMeasurement 캐스트)
- EStep.Measure 분기 (L237 영역 교체, 12 lines → 30 lines):
  - `if (meas is DualImageEdgeDistanceMeasurement dualMeas)` 분기 추가
  - try → TryGrabOrLoadFaiDualImages → RuntimeImageA/B 주입 → meas.TryExecute (signature unchanged!) → catch
  - finally → 양 HImage Dispose + RuntimeImageA/B null reset (메모리 위생)
  - else 분기 → 기존 1-image 호출 byte-identical (회귀 0)

**Grep verification (회귀 가드):**
- `private bool TryGrabOrLoadFaiDualImages` = 1 ✓
- `is DualImageEdgeDistanceMeasurement dualMeas` = 1 ✓
- `dualMeas.RuntimeImageA = imgA` = 1 ✓ / `dualMeas.RuntimeImageA = null` = 1 ✓ (finally)
- `parentSeq2.IsDatumFailed(meas.DatumRef)` = 1 ✓ (Phase 39 D-01 gate 보존)
- `faiOverlays.AddRange(measOverlays)` = 1 ✓ (Phase 39.1-03 LastOverlays write-back 보존)
- `fai.LastOverlays = faiOverlays;` = 1 ✓
- `meas.TryExecute(image, transform, fai.PixelResolutionX` = 2 ✓ (DualImage 분기 + else 분기)

## Build Result

- Tool: `MSBuild 17.14.40` (VS2022 Community)
- Command: `MSBuild WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 -v:m`
- Result: **PASS — 0 errors**
- Warnings: 7 (모두 Phase 33 baseline — TopSequence/BottomSequence CS0618 obsolete 6 + VirtualCamera.cs CS0162 unreachable 1 + MSB3884 ruleset)
- 신규 warning: **0**
- Output: `WPF_Example/bin/x64/Debug/DatumMeasurement.exe`

## Decisions / Deviations

- Plan written 그대로 실행 — 자동 수정 deviation 0.
- 모든 신규/수정 라인에 `//260530 hbk Phase 39.2 D-G1` 마커 (memory feedback_comment_convention) 적용.
- C# 7.2 호환 — `is X x` declaration pattern (C# 7.0+), record/switch expression 미사용.
- HALCON 정책 (memory feedback):
  - `HOperatorSet.ProjectionPl` try/catch 적용 (1 위치)
  - `fit_line` 은 `svc.TryFitLine` wrapper 사용 — 내부에 strip-loop + 안전 catch (단일 MeasurePos 호출 0)
  - `HOperatorSet.SetColor` 직접 호출 0 — RoiId 만 지정, 색상은 HalconDisplayService 가 prefix 라우팅

## End-to-end Smoke (코드 검증)

1. `MeasurementFactory.Create("DualImageEdgeDistance", null)` 가 null 이 아닌 인스턴스 반환 — Task 2 switch 16th case ✓
2. INI Type=DualImageEdgeDistance 로 저장된 measurement 가 다음 로드 시 정상 복원 — ParamBase reflection 의 string/double 케이스로 PointROI_/LineROI_ 자동 직렬화 ✓
3. Action_FAIMeasurement.EStep.Measure DualImage 분기 진입 → 양 이미지 로드 → RuntimeImageA/B 주입 → TryExecute → finally dispose — Task 3 ✓
4. FAI Type ComboBox 11번째 항목 "DualImageEdgeDistance" 노출 — Task 2 GetTypeNames ✓ (실제 UI 가시 확인은 SIMUL UAT 단계 — Plan 05)

## Regression Guard (회귀 0)

- 기존 10 measurement 타입 TryExecute 호출 경로 = else 분기 byte-identical (Task 3)
- Phase 39 IsDatumFailed gate / WasDatumSkipped 누적 / LastOverlays write-back 코드 변경 0 (grep 검증)
- INI 파일 내 Type=다른값 측정은 기존 dispatch + 결과 byte-identical
- 기존 15 case + 10 GetTypeNames 원소 순서/문자 변경 0 (1 case + 1 element 단순 추가만)

## Self-Check: PASSED

**Files exist:**
- `WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` FOUND ✓
- `WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs` (modified) FOUND ✓
- `WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` (modified) FOUND ✓
- `WPF_Example/DatumMeasurement.csproj` (modified) FOUND ✓

**Commits exist (git log):**
- `6a671c0` (Task 1) FOUND ✓
- `d982ea4` (Task 2) FOUND ✓
- `b76ef86` (Task 3) FOUND ✓

## Next

- 39.2-02 (D-G2 I10 close-point) — Wave 1 algorithm (depends_on: none, same wave)
- 39.2-05 SIMUL UAT — Plan 01 결과 시각 가시화 (FAI Type 콤보 노출 + INI round-trip + 듀얼 이미지 측정 결과)
