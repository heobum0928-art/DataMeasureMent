---
phase: 39.2-urgent-additions-2-2026-05-30
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs
autonomous: true
requirements: ["WF-01"]
tags: ["fai", "i10", "close-point", "intersection"]

must_haves:
  truths:
    - "ArcLineIntersectDistanceMeasurement 에 IntersectionPointSelection 문자열 파라미터가 노출된다 (콤보: Far / Close)"
    - "PropertyGrid 의 Measurement|Measure 카테고리에 'IntersectionPointSelection' 항목이 표시되고 값은 Far / Close 만 선택 가능"
    - "IntersectionPointSelection 기본값 = 'Far' (INI 하위호환 — 기존 측정값 byte-identical)"
    - "IntersectionPointSelection='Close' 일 때 측정점이 교점1(가까운 점) 좌표를 사용한다"
    - "MeasureAxis='X' / 'Y' 둘 다 Close/Far 분기가 동작한다"
    - "ParamBase 자동 직렬화로 INI 파일에 IntersectionPointSelection=Far|Close 가 저장/복원된다 (case String)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs"
      provides: "IntersectionPointSelection string property + IntersectionPointSelectionList combo wrapper + TryExecute close/far 분기"
      contains: "public string IntersectionPointSelection"
      exports: ["IntersectionPointSelection", "IntersectionPointSelectionList"]
  key_links:
    - from: "PropertyGrid Measurement|Measure 카테고리"
      to: "IntersectionPointSelection 콤보"
      via: "[ItemsSourceProperty(nameof(IntersectionPointSelectionList))]"
      pattern: 'ItemsSourceProperty\(nameof\(IntersectionPointSelectionList\)\)'
    - from: "TryExecute (7) 측정점 보정 블록"
      to: "useClose 분기 (int1 vs int2 좌표 선택)"
      via: "if (IntersectionPointSelection == \"Close\")"
      pattern: 'IntersectionPointSelection == "Close"'
---

<objective>
Phase 39.2 D-G2 — `ArcLineIntersectDistanceMeasurement` 에 `IntersectionPointSelection` 파라미터를 추가하여 두 교점 중 Far(기본, 멀리) / Close(가까이) 를 선택할 수 있게 한다.

Purpose: POC 2026-06-30 시연 Top I10 close-point variant — SOP 도면이 가까운 교점 P1 의 거리 측정을 요구하는 케이스 충족. 신규 Measurement 타입 추가 없이 기존 type 의 enum 파라미터로 단일 소스 유지.
Output: 1 파일 (ArcLineIntersectDistanceMeasurement.cs) 수정 — string property 1 + List wrapper 1 + TryExecute (7) 측정점 보정 블록 if/else 분기 1.

핵심 lock (CONTEXT D-G2 / PATTERNS D-G2-A 반영):
- ParamBase 직렬화 제약상 enum 직접 저장 불가 → **string 으로 저장** ("Far" / "Close")
- ItemsSourceProperty + Browsable(false) List wrapper 콤보 (EdgeToLineDistance L36-54 동형)
- ICustomTypeDescriptor 미도입 (옵션 A — 단순 콤보만, 코드 변경 최소)
- default = "Far" → INI 회귀 0 (기존 INI 에 키 없으면 자동 빈/null → 본 "Far" default 와 같은 결과)
- TryComputeBothIntersections 헬퍼 신설 X — 기존 인라인 양 교점 산출 (L226-245) 후 보정 블록 (L247-261) 만 close/far 분기
- 신규 Measurement 타입 도입 0 → MeasurementFactory 변경 0
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.2-urgent-additions-2-2026-05-30/39.2-CONTEXT.md
@.planning/phases/39.2-urgent-additions-2-2026-05-30/39.2-PATTERNS.md

# Analog 파일
@WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs
@WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs

<interfaces>
<!-- ParamBase 직렬화 제약 — switch case 만 지원 -->

From ParamBase.cs L331-365 (DO NOT modify):
- 자동 직렬화 type: Int32 / Double / String / Boolean / Rect / Line / Circle / PropertyItem[] / ModelFinderViewModel
- enum 직접 저장 ✗ → string 저장 + ItemsSourceProperty 콤보 패턴 강제

From ArcLineIntersectDistanceMeasurement.cs current shape:
```csharp
// L121-127 MeasureAxis 패턴 (analog for IntersectionPointSelection)
[Category("Measurement|Measure")]
[ItemsSourceProperty(nameof(MeasureAxisList))]
public string MeasureAxis { get; set; } = "X";

[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> MeasureAxisList { get { return new List<string> { "X", "Y" }; } }

// L226-245 인라인 교점 산출 (변경 X, 보존)
double int1Row, int1Col;  // 교점1 (close 후보)
if (!VisionAlgorithmService.TryIntersectLines(a1r1, a1c1, a1r2, a1c2, b1r1, b1c1, b1r2, b1c2, out int1Row, out int1Col)) { ... return false; }
double int2Row, int2Col;  // 교점2 (far 후보, 기존 default 사용)
if (!VisionAlgorithmService.TryIntersectLines(a2r1, a2c1, a2r2, a2c2, b2r1, b2c1, b2r2, b2c2, out int2Row, out int2Col)) { ... return false; }

// L247-261 (보정 블록 — 본 plan 에서 close/far 분기 추가)
double measurePointRow, measurePointCol;
if (MeasureAxis == "X") {
    measurePointCol = int2Col;  // 기존 = 교점2 (Far)
    measurePointRow = (int1Row + int2Row) / 2.0;
} else {
    measurePointRow = int2Row;  // 기존 = 교점2 (Far)
    measurePointCol = (int1Col + int2Col) / 2.0;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ArcLineIntersectDistanceMeasurement 에 IntersectionPointSelection 파라미터 + TryExecute close/far 분기</name>
  <read_first>
    - @WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs (전체 — L1-50, L121-127 MeasureAxis 패턴, L226-261 교점 산출 + 측정점 보정 블록)
    - @WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs L36-54 (ItemsSourceProperty + Browsable(false) List wrapper 패턴)
  </read_first>
  <behavior>
    - Test 1: ArcLineIntersectDistanceMeasurement 인스턴스의 IntersectionPointSelection 기본값 = "Far"
    - Test 2: IntersectionPointSelectionList getter 가 ["Far", "Close"] 반환
    - Test 3: PropertyGrid 카테고리 "Measurement|Measure" 에 IntersectionPointSelection 항목 표시 (Description attribute 포함)
    - Test 4: IntersectionPointSelection 미지정 (기존 INI 파일 로드) → default "Far" 적용 → 기존 측정 결과 byte-identical
    - Test 5: IntersectionPointSelection="Close" + MeasureAxis="X" 일 때 measurePointCol = int1Col (교점1)
    - Test 6: IntersectionPointSelection="Close" + MeasureAxis="Y" 일 때 measurePointRow = int1Row (교점1)
    - Test 7: IntersectionPointSelection="Far" 또는 빈 문자열 일 때 기존 동작 (교점2 사용) 유지
  </behavior>
  <action>
**파일:** `WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs`

스타일: 기존 파일 Allman brace 보존. 주석 마커 `//260530 hbk Phase 39.2 D-G2`.

**[A] IntersectionPointSelection 프로퍼티 추가** — L121-127 MeasureAxis 블록 **직후** (L128) 에 다음 6 줄 삽입:

```csharp
// ── IntersectionPointSelection (D-G2 close-point variant) ────────────────────
//260530 hbk Phase 39.2 D-G2-01 — 두 교점 중 어느 것을 측정점으로 사용할지: Far(기본, INI 하위호환) 또는 Close
[Category("Measurement|Measure")] //260530 hbk Phase 39.2 D-G2
[System.ComponentModel.Description("교점 선택 — Far: Datum 수직선 기준 더 먼 점(기본), Close: 더 가까운 점")] //260530 hbk Phase 39.2 D-G2
[ItemsSourceProperty(nameof(IntersectionPointSelectionList))] //260530 hbk Phase 39.2 D-G2
public string IntersectionPointSelection { get; set; } = "Far"; //260530 hbk Phase 39.2 D-G2 — default Far = INI 회귀 0
[PropertyTools.DataAnnotations.Browsable(false)] //260530 hbk Phase 39.2 D-G2
public List<string> IntersectionPointSelectionList { get { return new List<string> { "Far", "Close" }; } } //260530 hbk Phase 39.2 D-G2
```

**[B] TryExecute (7) 측정점 보정 블록 교체** — L247-261 의 `double measurePointRow, measurePointCol;` ~ `}` 블록을 전부 교체:

**현재 코드 (L247-261):**
```csharp
// (7) 측정점 보정 — 측정축 방향 좌표는 교점2(거리 끝점), 수직축 좌표는 두 교점 평균.
// MeasureAxis X(수평=Col): measurePointCol = 교점2.Col, measurePointRow = (교점1.Row+교점2.Row)/2
// MeasureAxis Y(수직=Row): measurePointRow = 교점2.Row, measurePointCol = (교점1.Col+교점2.Col)/2
//260521 hbk Phase 32 UAT — 단순 양축 평균 → 측정축은 교점2, 수직축만 평균으로 정정
double measurePointRow, measurePointCol;
if (MeasureAxis == "X")
{
    measurePointCol = int2Col; //260521 hbk Phase 32 UAT — 측정축(X=Col)은 교점2 값
    measurePointRow = (int1Row + int2Row) / 2.0; //260521 hbk Phase 32 UAT — 수직축(Row)은 두 교점 평균
}
else
{
    measurePointRow = int2Row; //260521 hbk Phase 32 UAT — 측정축(Y=Row)은 교점2 값
    measurePointCol = (int1Col + int2Col) / 2.0; //260521 hbk Phase 32 UAT — 수직축(Col)은 두 교점 평균
}
```

**교체 (close/far 분기 추가):**
```csharp
// (7) 측정점 보정 — 측정축 방향 좌표는 교점1(Close) 또는 교점2(Far, 기본), 수직축 좌표는 두 교점 평균.
// MeasureAxis X(수평=Col): measurePointCol = (Close: int1Col / Far: int2Col), measurePointRow = (교점1.Row+교점2.Row)/2
// MeasureAxis Y(수직=Row): measurePointRow = (Close: int1Row / Far: int2Row), measurePointCol = (교점1.Col+교점2.Col)/2
//260521 hbk Phase 32 UAT — 단순 양축 평균 → 측정축은 교점2, 수직축만 평균으로 정정
//260530 hbk Phase 39.2 D-G2-01 — IntersectionPointSelection 분기 추가: "Close" → 교점1, "Far" 또는 다른 값 → 교점2 (회귀 0)
double measurePointRow, measurePointCol;
bool useClose = (IntersectionPointSelection == "Close"); //260530 hbk Phase 39.2 D-G2-01 — null/""/"Far" 모두 false (INI 하위호환)
if (MeasureAxis == "X")
{
    if (useClose) //260530 hbk Phase 39.2 D-G2-01
    {
        measurePointCol = int1Col; //260530 hbk Phase 39.2 D-G2-01 — Close: 교점1.Col
    }
    else
    {
        measurePointCol = int2Col; //260521 hbk Phase 32 UAT — Far: 측정축(X=Col)은 교점2 값 (기존 default 회귀 0)
    }
    measurePointRow = (int1Row + int2Row) / 2.0; //260521 hbk Phase 32 UAT — 수직축(Row)은 두 교점 평균
}
else
{
    if (useClose) //260530 hbk Phase 39.2 D-G2-01
    {
        measurePointRow = int1Row; //260530 hbk Phase 39.2 D-G2-01 — Close: 교점1.Row
    }
    else
    {
        measurePointRow = int2Row; //260521 hbk Phase 32 UAT — Far: 측정축(Y=Row)은 교점2 값 (기존 default 회귀 0)
    }
    measurePointCol = (int1Col + int2Col) / 2.0; //260521 hbk Phase 32 UAT — 수직축(Col)은 두 교점 평균
}
```

⚠️ **Anti-goals (CONTEXT D-G2 마지막):**
- ArcLineIntersectDistance Far 기본 결과값 byte-identical (default 사용 시 — useClose=false → 기존 int2 분기 = 회귀 0)
- Phase 32 P03 / I9/I10 알고리즘 흐름 변경 0 (4 ROI fit_line + TryIntersectLines + ComputeProjectionDistance + overlay 7종 변경 0)
- MeasurementFactory 등록 카운트 변경 0 (신규 type 도입 X — Plan 01 의 +1 만 별도 = 합산 11)
- TryComputeBothIntersections 헬퍼 신설 X (인라인 분기로 충분 — VisionAlgorithmService 변경 0)
- ICustomTypeDescriptor 도입 X (단순 ItemsSourceProperty 콤보만)
- 4 ROI / 4 Edge 그룹 / DatumOriginConsumer transient 6 필드 / overlay 7개 (FAI-Edge1~4 + FAI-Intersection1/2 + FAI-DistLine) 변경 0
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /v:m</automated>
  </verify>
  <acceptance_criteria>
    # W3 fix: 모든 grep 라인을 `Grep -F` (fixed-string) 로 표기 — `[...]` / `{...}` / `<...>` 가 regex 토큰으로 오인되어 unclosed-class 에러 나는 케이스 회피.
    - `Grep -F "public string IntersectionPointSelection { get; set; } = \"Far\";" matches 1`
    - `Grep -F "public List<string> IntersectionPointSelectionList" matches 1`
    - `Grep -F "new List<string> { \"Far\", \"Close\" }" matches 1`
    - `Grep -F "[ItemsSourceProperty(nameof(IntersectionPointSelectionList))]" matches 1` (W3 fix: -F 로 `[...]` 토큰화 회피)
    - `Grep -F "IntersectionPointSelection == \"Close\"" matches 1` (useClose 변수 초기화)
    - `Grep -F "bool useClose" matches 1`
    - `Grep -F "measurePointCol = int1Col" matches 1` (MeasureAxis=X + Close)
    - `Grep -F "measurePointRow = int1Row" matches 1` (MeasureAxis=Y + Close)
    - `Grep -F "measurePointCol = int2Col" matches 1` (MeasureAxis=X + Far, 기존 보존)
    - `Grep -F "measurePointRow = int2Row" matches 1` (MeasureAxis=Y + Far, 기존 보존)
    - L121-127 MeasureAxis 블록 텍스트 변경 0 (`Grep -F "public string MeasureAxis { get; set; } = \"X\";" matches 1`)
    - L129-155 IDatumOriginConsumer 6 필드 변경 0
    - L226-245 TryIntersectLines 호출 2회 변경 0 (`Grep -F "VisionAlgorithmService.TryIntersectLines" matches 2`)
    - L267-271 ComputeProjectionDistance 호출 변경 0
    - L275-330 overlay 7개 (FAI-Edge1~4, FAI-Intersection1, FAI-Intersection2, FAI-DistLine) 변경 0
    - MeasurementFactory.cs 변경 0 (이 plan 에서)
    - msbuild Rebuild Debug/x64 PASS (0 errors)
    - 신규 warning 0
    - 모든 신규/수정 라인에 //260530 hbk Phase 39.2 D-G2 마커

    # W5 INFO (info-level — 수정 불요, 명시만):
    # ParamBase reflection 의 String case dispatch 는 base class MeasurementBase → ParamBase 의 Save/Load 에서 자동 처리됨 (PATTERNS.md L297, ParamBase.cs L331-365). INI round-trip 회귀 검증은 정적 grep 으로 어려우므로 Plan 05 Test 2 의 SIMUL UAT 에 위임.
  </acceptance_criteria>
  <done>
    ArcLineIntersectDistanceMeasurement 가 IntersectionPointSelection (string, Far/Close, default Far) 콤보로 close-point variant 를 지원. INI 하위호환 보장 (default Far → 기존 byte-identical). MeasureAxis X/Y 양쪽 모두 close/far 분기 동작.
  </done>
</task>

</tasks>

<verification>
**End-to-end smoke check (msbuild PASS 시 자동 충족):**
1. PropertyGrid 가 ArcLineIntersectDistance 선택 시 Measurement|Measure 카테고리에 IntersectionPointSelection 콤보 노출 (Far / Close)
2. INI Type=ArcLineIntersectDistance + IntersectionPointSelection 키 없음 → default "Far" 적용 → 기존 측정값 byte-identical (Phase 32 sign-off 회귀 0)
3. IntersectionPointSelection=Close + 실데이터 시 측정점이 교점1 좌표 사용 (Plan 05 SIMUL UAT 에서 확인)
4. ParamBase 자동 직렬화: IntersectionPointSelection=Close 저장 후 재로드 시 값 보존 (case String)

**회귀 0 가드:**
- 4 ROI fit_line / TryIntersectLines 2회 호출 / ComputeProjectionDistance / overlay 7개 변경 0
- MeasureAxis="X" (default) + IntersectionPointSelection="Far" (default) → 기존 측정 byte-identical
- Phase 32 UAT 4-ROI 두 교점 평균 방식 보존 (수직축 좌표 (int1+int2)/2 변경 0)
</verification>

<success_criteria>
- [ ] Task 1: IntersectionPointSelection 프로퍼티 + List wrapper 추가 + TryExecute (7) close/far 분기 → msbuild PASS
- [ ] msbuild Rebuild Debug/x64 0 errors / 0 신규 warnings
- [ ] 신규 Measurement 타입 0 (MeasurementFactory 변경 0 in this plan)
- [ ] VisionAlgorithmService 변경 0 (TryComputeBothIntersections 헬퍼 신설 X)
- [ ] INI 하위호환 default "Far" 적용
- [ ] 모든 신규/수정 라인에 `//260530 hbk Phase 39.2 D-G2` 마커
</success_criteria>

<output>
After completion, create `.planning/phases/39.2-urgent-additions-2-2026-05-30/39.2-02-SUMMARY.md` covering:
- IntersectionPointSelection 프로퍼티 + List wrapper 추가 결과 (line 위치, default 값)
- TryExecute (7) close/far 분기 교체 결과 (변경 라인 수, useClose 분기 로직)
- 회귀 0 가드 grep 결과 (MeasureAxis 보존, 4 ROI ROI/Edge 그룹 보존, overlay 7개 보존, IDatumOriginConsumer 6 필드 보존)
- msbuild Rebuild Debug/x64 결과 (errors / warnings)
- INI 직렬화 round-trip 검증 (수동 — Plan 05 SIMUL UAT 에서)
</output>
</content>
