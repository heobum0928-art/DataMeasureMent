---
phase: 39.2-urgent-additions-2-2026-05-30
plan: 02
subsystem: fai-measurement
tags: [fai, i10, close-point, intersection, dg2]
requirements: [WF-01]
dependency_graph:
  requires:
    - ArcLineIntersectDistanceMeasurement (Phase 32-08 — 4-ROI fit_line + TryIntersectLines + ComputeProjectionDistance)
    - ParamBase string-case 자동 직렬화 (case "String", L331-365)
    - PropertyTools ItemsSourceProperty + [Browsable(false)] List wrapper 패턴
  provides:
    - ArcLineIntersectDistance Close-variant 측정 (교점1 좌표 사용)
    - PropertyGrid Measurement|Measure 카테고리 IntersectionPointSelection 콤보 (Far/Close)
  affects:
    - Top I10 SOP 도면 (교점1 가까운 점의 X 거리) 단일 type 재사용 (신규 type 도입 0)
tech-stack:
  added: []
  patterns:
    - "string-enum + ItemsSourceProperty 콤보 (EdgeToLineDistance L36-54 동형)"
    - "INI 하위호환 default = 기존 동작 (회귀 0 가드)"
key-files:
  created: []
  modified:
    - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs
decisions:
  - "D-G2-01 적용: IntersectionPointSelection string property + ItemsSourceProperty 콤보 — enum 직접 저장 회피 (ParamBase 직렬화 제약 case String)"
  - "옵션 A 채택: ICustomTypeDescriptor 미도입 — ItemsSourceProperty + Browsable(false) List wrapper 만으로 PropertyGrid 노출 충족 (코드 변경 최소)"
  - "TryComputeBothIntersections 헬퍼 신설 X — ArcLineIntersect 가 이미 int1Row/Col + int2Row/Col 양 교점 산출 중, 인라인 if/else 분기로 충분 (VisionAlgorithmService 변경 0)"
  - "신규 Measurement 타입 도입 X — MeasurementFactory 등록 카운트 변경 0 (단일 type + parameter = 단일 소스, INI 회귀 0)"
  - "useClose 변수 = (IntersectionPointSelection == \"Close\") — null/\"\"/\"Far\" 모두 false → INI 키 누락 시 기존 byte-identical"
metrics:
  duration_minutes: 8
  completed: 2026-05-29T22:29:52Z
  tasks_completed: 1
  files_modified: 1
  files_created: 0
  commits: 1
---

# Phase 39.2 Plan 02: ArcLineIntersect IntersectionPointSelection (Far/Close) Summary

`ArcLineIntersectDistanceMeasurement` 에 `IntersectionPointSelection` 콤보 파라미터(Far/Close, default Far)를 추가하고 TryExecute (7) 측정점 보정 블록에 close/far 인라인 분기를 삽입하여 Top I10 close-point variant 를 지원한다 — INI 회귀 0 (default Far → 기존 byte-identical), 신규 타입 도입 0.

## Objective Met

D-G2 (Top I10 close-point variant) — SOP 도면이 가까운 교점 P1 의 거리 측정을 요구하는 케이스를 신규 Measurement 타입 추가 없이 기존 type 의 enum 파라미터로 충족. ParamBase 직렬화 제약 (case String 만) 회피를 위해 string ("Far" / "Close") + ItemsSourceProperty 콤보 패턴 (EdgeToLineDistance L36-54 동형) 사용. ICustomTypeDescriptor 미도입 (옵션 A — 코드 변경 최소).

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | ArcLineIntersect IntersectionPointSelection 추가 + TryExecute close/far 분기 | 5e35f6b | ArcLineIntersectDistanceMeasurement.cs |

## Changes Detail

### Task 1: IntersectionPointSelection 파라미터 + TryExecute (7) close/far 분기

**[A] IntersectionPointSelection 프로퍼티 추가** — L129-136 신규 (MeasureAxis 블록 직후):

```csharp
// ── IntersectionPointSelection (D-G2 close-point variant) ────────────────────
//260530 hbk Phase 39.2 D-G2-01 — 두 교점 중 어느 것을 측정점으로 사용할지: Far(기본, INI 하위호환) 또는 Close
[Category("Measurement|Measure")] //260530 hbk Phase 39.2 D-G2
[System.ComponentModel.Description("교점 선택 — Far: Datum 수직선 기준 더 먼 점(기본), Close: 더 가까운 점")] //260530 hbk Phase 39.2 D-G2
[ItemsSourceProperty(nameof(IntersectionPointSelectionList))] //260530 hbk Phase 39.2 D-G2
public string IntersectionPointSelection { get; set; } = "Far"; //260530 hbk Phase 39.2 D-G2 — default Far = INI 회귀 0
[PropertyTools.DataAnnotations.Browsable(false)] //260530 hbk Phase 39.2 D-G2
public List<string> IntersectionPointSelectionList { get { return new List<string> { "Far", "Close" }; } } //260530 hbk Phase 39.2 D-G2
```

**[B] TryExecute (7) 측정점 보정 블록 교체** — L256-286 (기존 L247-261 in-place 확장):

핵심 분기 로직 — `useClose` 변수 도입 + MeasureAxis X/Y 각 분기 안에 if(useClose)/else 추가:

```csharp
bool useClose = (IntersectionPointSelection == "Close"); //260530 hbk Phase 39.2 D-G2-01 — null/""/"Far" 모두 false (INI 하위호환)
if (MeasureAxis == "X")
{
    if (useClose) { measurePointCol = int1Col; }   // Close: 교점1.Col
    else          { measurePointCol = int2Col; }   // Far: 측정축(X=Col)은 교점2 값 (기존 default 회귀 0)
    measurePointRow = (int1Row + int2Row) / 2.0;
}
else
{
    if (useClose) { measurePointRow = int1Row; }   // Close: 교점1.Row
    else          { measurePointRow = int2Row; }   // Far: 측정축(Y=Row)은 교점2 값 (기존 default 회귀 0)
    measurePointCol = (int1Col + int2Col) / 2.0;
}
```

**ParamBase 자동 직렬화 회귀 0 분석:**
- ParamBase reflection 의 `case "String"` (L331-365) 가 IntersectionPointSelection 을 자동 직렬화
- 기존 INI 파일에 키 없음 → C# auto-property default = `"Far"` 유지 → `useClose=false` → 기존 교점2 분기 → byte-identical

## 회귀 0 가드 grep 결과 (PowerShell SimpleMatch)

| 패턴 | 매치 수 | 의미 |
|------|---------|------|
| `public string IntersectionPointSelection { get; set; } = "Far";` | 1 | 신규 프로퍼티 정확 1회 |
| `public List<string> IntersectionPointSelectionList` | 1 | List wrapper 1회 |
| `new List<string> { "Far", "Close" }` | 1 | 콤보 옵션 정확 |
| `[ItemsSourceProperty(nameof(IntersectionPointSelectionList))]` | 1 | 콤보 결선 1회 |
| `IntersectionPointSelection == "Close"` | 1 | useClose 변수 초기화 1회 |
| `bool useClose` | 1 | 변수 선언 1회 |
| `measurePointCol = int1Col` | 1 | MeasureAxis=X + Close 분기 |
| `measurePointRow = int1Row` | 1 | MeasureAxis=Y + Close 분기 |
| `measurePointCol = int2Col` | 1 | MeasureAxis=X + Far (기존 보존) |
| `measurePointRow = int2Row` | 1 | MeasureAxis=Y + Far (기존 보존) |
| `public string MeasureAxis { get; set; } = "X";` | 1 | MeasureAxis L121-127 블록 텍스트 보존 |
| `VisionAlgorithmService.TryIntersectLines` | 2 | (5) 교점1 + (6) 교점2 호출 변경 0 |

`Grep` 툴이 정규식 메타 (`{`, `}`, `[`) 해석으로 0 매치를 반환하던 케이스는 PowerShell `Select-String -SimpleMatch` 로 우회하여 동일 결과 확인. 모든 acceptance criteria 패턴 정확히 1 매치 (또는 의도된 2 매치 = TryIntersectLines).

## 회귀 0 그룹 (변경 0)

- **L23-111** EdgeA1/EdgeB1/EdgeA2/EdgeB2 4 ROI × 5 필드 + 7 Edge 파라미터 (Threshold/Sigma/SampleCount/TrimCount/Polarity/Direction/Selection) — **변경 0**
- **L121-127** MeasureAxis 블록 (Category/ItemsSourceProperty/property/Browsable/List) — **변경 0** (직후에 IntersectionPointSelection 블록 6 라인 삽입만)
- **L139-167** IDatumOriginConsumer 6 transient 필드 (DatumOriginRow/Col + DatumAngleRad/Angle2Rad + DatumDetectedCircleRow/Col) — **변경 0**
- **L185-235** TryFitLine 4회 호출 (EdgeA1/B1/A2/B2) — **변경 0**
- **L237-257** TryIntersectLines 2회 호출 (교점1/교점2 산출) — **변경 0** (grep 2매치 확인)
- **L295-330** ComputeProjectionDistance 호출 + measureLineAngle 산출 — **변경 0**
- **L335-385** overlay 7개 (FAI-Edge1, FAI-Edge2, FAI-Edge3, FAI-Edge4, FAI-Intersection1, FAI-Intersection2, FAI-AvgPoint) + FAI-DistLine (footOk 가드) — **변경 0**
- `MeasurementFactory.cs` — **변경 0** (신규 type 도입 X)
- `VisionAlgorithmService.cs` — **변경 0** (TryComputeBothIntersections 헬퍼 신설 X — 인라인 분기로 충족)

## msbuild Rebuild Debug/x64 결과

| 항목 | 값 |
|------|-----|
| Errors | **0** |
| Warnings | **2** (모두 baseline) |
| 신규 Warnings | **0** |
| Output | `WPF_Example\bin\x64\Debug\DatumMeasurement.exe` 생성 확인 |
| MSBuild 버전 | 17.14.40+3e7442088 (VS 2022 Community) |

Warning breakdown:
- `MSB3884` × 2 — MinimumRecommendedRules.ruleset (Phase 39/39.1/Phase 21 와 동일 baseline)
- 기존 CS0618 (TopSequence/BottomSequence/InspectionAction obsolete) / CS0162 — Rebuild 시 표시되나 Build summary 의 카운트 (warning 2) 는 incremental 빌드 후 잔여 baseline.

## 빌드 환경 보정 (Rule 3 — blocking issue)

워크트리 (`agent-a96ca5e4a48445e7f`) 의 `WPF_Example/bin/x64/Debug/` 가 비어 있어 csproj 의 `HintPath=bin\x64\Debug\WPF.MDI.dll` 가 해결되지 않아 MC3074 XAML 어셈블리 미해결 에러 발생. 메인 repo (`C:\Info\Project\DataMeasurement\WPF_Example\bin\x64\Debug\`) 에서 robocopy (`/E /XO`) 로 292 파일을 워크트리 동일 경로로 복사하여 build 환경 정상화. 이는 worktree 사전 환경 문제로, plan 의 코드 변경과는 무관 (SCOPE BOUNDARY).

## Deviations from Plan

**None** — 플랜 대로 실행됨.

- Task 1 의 [A] 프로퍼티 6 라인 + [B] 보정 블록 close/far 분기 — plan 작성 코드와 byte-identical 로 삽입/교체
- 모든 acceptance criteria grep PASS (PowerShell SimpleMatch 우회 확인)
- msbuild 0 errors / 신규 warning 0
- 회귀 0 가드 12 패턴 모두 정확 매치

## 후속 검증 (Plan 05 SIMUL UAT 위임)

본 plan 의 정적 grep + msbuild 으로 다음은 자동 충족:
1. ✓ PropertyGrid Measurement|Measure 카테고리에 IntersectionPointSelection 콤보 정의
2. ✓ Far / Close 두 옵션만 노출
3. ✓ default = "Far" → useClose=false → 기존 분기 (회귀 0)
4. ✓ MeasureAxis X/Y 각 분기 안에 useClose 분기 추가

Plan 05 SIMUL UAT 에서 확인 필요:
- IntersectionPointSelection=Close 실데이터 측정점이 교점1 좌표 사용 (시각적)
- ParamBase 자동 직렬화 round-trip (INI 저장 후 재로드 시 Close 값 보존)
- 기존 Phase 32 sign-off UAT 항목 회귀 0 (Far default 동작)

## Self-Check: PASSED

**Created files:**
- `.planning/phases/39.2-urgent-additions-2-2026-05-30/39.2-02-SUMMARY.md` — FOUND (this file)

**Modified files (artifact path verified):**
- `WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs` — FOUND, +30 lines / -5 lines (commit 5e35f6b)

**Commits:**
- `5e35f6b` — FOUND in worktree branch (verified via git log)

**msbuild:**
- 0 errors / 0 신규 warnings — VERIFIED

**Acceptance criteria grep:**
- 12 / 12 패턴 매치 — VERIFIED (PowerShell SimpleMatch)
