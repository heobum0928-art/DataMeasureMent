---
phase: 39.2-urgent-additions-2-2026-05-30
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]  # B1 fix: Plan 01 DualImageEdgeDistance TypeName + Plan 02 IntersectionPointSelection 빌드 후 실행 (wave 2 일관성)
files_modified:
  - WPF_Example/Utility/NaturalStringComparer.cs
  - WPF_Example/UI/ControlItem/InspectionListViewModel.cs
  - WPF_Example/UI/ControlItem/NodeViewModel.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: ["WF-01"]
tags: ["ui", "tree", "natural-sort"]

must_haves:
  truths:
    - "TreeView 의 Shot/FAI/Datum/Measurement 노드가 Name 자연정렬로 표시된다 (Shot2 < Shot10)"
    - "Add{Shot,FAI,Datum,Measurement}Node 호출 직후 정렬된 위치에 노드가 삽입된다"
    - "RebuildTree (recipe 로드) 직후 전 트리가 자연정렬 상태가 된다"
    - "노드 Rename (DatumName/ShotName/FAIName/MeasurementName 변경) 시 부모 children 이 즉시 재정렬된다"
    - "Btn_AddShot_Click (또는 AddShotToSequence) 후에도 부모 ActionNode children 이 자연정렬된다 (B2 fix)"
    - "ParamBase INI 직렬화 순서 / ShotConfig.FAIList / FAIConfig.Measurements 컬렉션 순서 변경 0 (view 정렬만)"
    - "TreeView SelectionChanged / RenderInspectionResultForNode / DataGrid_faiResults / PropertyGrid 동작 변경 0"
  artifacts:
    - path: "WPF_Example/Utility/NaturalStringComparer.cs"
      provides: "IComparer<string> 자연정렬 구현 (regex chunk 분리, 숫자는 int 비교)"
      contains: "class NaturalStringComparer : IComparer<string>"
      exports: ["Compare(string x, string y)"]
    - path: "WPF_Example/UI/ControlItem/InspectionListViewModel.cs"
      provides: "SortNodeChildren(NodeViewModel) + SortAllLevels() + Add* 메서드 내 정렬 트리거 + RebuildTree 끝 정렬"
      contains: "SortNodeChildren"
    - path: "WPF_Example/UI/ControlItem/NodeViewModel.cs"
      provides: "OnParamPropertyChanged Name 갱신 분기 후 부모 SortNodeChildren 트리거"
      contains: "SortNodeChildren(this.Parent)"
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "Btn_AddShot_Click (또는 AddShotToSequence) 끝에 SortNodeChildren 트리거 (B2 fix)"
      contains: "SortNodeChildren"
    - path: "WPF_Example/DatumMeasurement.csproj"
      provides: "NaturalStringComparer.cs Compile ItemGroup 등록"
      contains: "NaturalStringComparer.cs"
  key_links:
    - from: "InspectionListViewModel.AddFAINode / AddDatumNode / AddMeasurementNode / RebuildTree"
      to: "SortNodeChildren(parent) / SortAllLevels()"
      via: "메서드 끝에서 정렬 호출"
      pattern: "SortNodeChildren\\(.*\\);|SortAllLevels\\(\\);"
    - from: "NodeViewModel.OnParamPropertyChanged Name 분기 끝"
      to: "InspectionListViewModel.SortNodeChildren(this.Parent)"
      via: "static 호출"
      pattern: "InspectionListViewModel.SortNodeChildren\\(this.Parent\\)"
    - from: "Btn_AddShot_Click / AddShotToSequence (Shot add path)"
      to: "InspectionListViewModel.SortNodeChildren(actionNode/parent)"
      via: "Shot add 직후 정렬 트리거 (B2 fix)"
      pattern: "SortNodeChildren\\("
---

<objective>
Phase 39.2 D-G3 — TreeView 의 Shot / FAI / Datum / Measurement 노드를 Name 기반 자연정렬 (Natural Sort) 로 표시한다.

Purpose: POC 2026-06-30 시연 — Shot 생성 순서와 무관하게 사용자가 트리에서 "Shot2 < Shot10" 으로 예측 가능한 순서로 보이게 함. ParamBase 직렬화 순서는 보존 (view-only 정렬).
Output: 신규 NaturalStringComparer.cs (Utility) + InspectionListViewModel.cs 정렬 헬퍼 + Add* 트리거 + RebuildTree 끝 호출 + NodeViewModel Rename hook + InspectionListView.xaml.cs Btn_AddShot_Click 트리거 + csproj 등록.

핵심 lock (CONTEXT D-G3 / PATTERNS D-G3-A~D 반영):
- 전 레벨 적용 (Shot / FAI / Datum / Measurement) — Recipe / Sequence / Action 도 자연정렬 적용 (전 레벨 SortAllLevels)
- 정렬 방향 asc 고정 (toggle UI 미도입)
- 정렬 트리거: Add* / Rename / RebuildTree / Btn_AddShot_Click
- NaturalStringComparer = regex `(\d+|\D+)` chunk 분리, 숫자는 int 비교, 그 외 OrdinalIgnoreCase
- ObservableCollection in-place 정렬: 새 리스트 생성 → Sort → Clear → re-Add (TreeListBox 이벤트 안전)
- ParamBase Save/Load INI 순서 변경 0 (Anti-goal)
- TreeView SelectionChanged / RenderInspectionResultForNode 변경 0 (Anti-goal)
- B2 fix: PATTERNS.md L26 D-G3-C analog 의 AddShotToSequence 경로에도 명시적 트리거 보장 (FAI/Datum/Measurement 와 동등하게 Shot add 경로도 정렬)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.2-urgent-additions-2-2026-05-30/39.2-CONTEXT.md
@.planning/phases/39.2-urgent-additions-2-2026-05-30/39.2-PATTERNS.md

# Analog 파일
@WPF_Example/UI/ControlItem/InspectionListViewModel.cs
@WPF_Example/UI/ControlItem/NodeViewModel.cs
@WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
@WPF_Example/Utility/Ini.cs
@WPF_Example/Utility/Logging.cs

<interfaces>
<!-- Key contracts the executor needs -->

From NodeViewModel.cs (key members):
```csharp
public class NodeViewModel : Observable
{
    public NodeViewModel Parent { get; private set; }
    public ObservableCollection<NodeViewModel> Children { get; }    // backing field "children"
    public string Name { get; set; }                                 // Node.Name proxy
    public ENodeType NodeType { get; }
    public void ReloadChildren();                                    // L94-104 — clear + re-add pattern
    private void OnParamPropertyChanged(object sender, PropertyChangedEventArgs e);  // L210-240, Name 분기 L236-239
}
```

From InspectionListViewModel.cs (key methods to extend):
```csharp
public class InspectionListViewModel : Observable
{
    public NodeViewModel RootModel { get; set; }
    public void AddFAINode(NodeViewModel actionNode, FAIConfig fai, ESequence seqID, EAction actID);  // L117-133
    public void AddDatumNode(NodeViewModel seqNode, DatumConfig datum);                                // L136-147
    public void AddMeasurementNode(NodeViewModel faiNode, MeasurementBase meas);                      // L150-162
    public void RebuildTree();                                                                         // L167-174
    private void HookSequenceDisplayNameUpdates();                                                     // L177-190
}
```

From InspectionListView.xaml.cs (Shot add path — B2 fix 대상):
```csharp
// PATTERNS.md L26 analog 인용: AddShotToSequence (L833+) — Shot add 경로
// Btn_AddShot_Click (또는 동등 메서드) 가 새 ShotConfig 추가 + tree 노드 생성 후 호출됨
// 본 plan Task 2 [C] 는 Shot add 직후 부모 (actionNode 또는 seqNode) children 자연정렬 트리거 필요
```

Existing patterns to mimic:
- ObservableCollection clear + re-add: NodeViewModel.ReloadChildren L94-104
- static helper from sibling type: InspectionListViewModel.Sort* will be public/static
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: NaturalStringComparer.cs 신규 작성 + csproj 등록</name>
  <read_first>
    - @WPF_Example/Utility/Logging.cs (Utility 폴더 컨벤션: namespace ReringProject.Utility, file header 컨벤션)
    - @WPF_Example/Utility/Ini.cs (Utility 폴더 PascalCase 컨벤션)
  </read_first>
  <behavior>
    - Test 1: NaturalStringComparer 가 IComparer<string> 구현
    - Test 2: cmp.Compare("Shot2", "Shot10") < 0 (Shot2 가 앞)
    - Test 3: cmp.Compare("Shot10", "Shot2") > 0
    - Test 4: cmp.Compare("Shot1", "Shot1") == 0
    - Test 5: cmp.Compare(null, "Shot1") < 0
    - Test 6: cmp.Compare("Shot1", null) > 0
    - Test 7: cmp.Compare("FAI_A", "FAI_B") < 0 (사전식 fallback)
    - Test 8: cmp.Compare("Datum_C1", "Datum_C2") < 0 (숫자 chunk + 사전식 prefix)
  </behavior>
  <action>
**파일 신규 작성:** `WPF_Example/Utility/NaturalStringComparer.cs`

스타일: **Allman brace** (신규 Utility 표준). 주석 마커 `//260530 hbk Phase 39.2 D-G3`.

```csharp
//260530 hbk Phase 39.2 D-G3 — Tree 노드 자연정렬 비교자 (Shot2 < Shot10)
using System.Collections.Generic;
using System.Text.RegularExpressions;

namespace ReringProject.Utility
{
    /// <summary>
    /// 자연정렬(Natural Sort) IComparer&lt;string&gt; 구현.
    /// 사전식 비교는 "Shot10" &lt; "Shot2" 가 되는 문제를 회피하기 위해
    /// regex 로 숫자 chunk 와 텍스트 chunk 를 분리한 후 chunk 단위로 비교한다.
    ///  - 양쪽 chunk 모두 숫자 → int 로 변환해 수치 비교
    ///  - 그 외 → System.StringComparison.OrdinalIgnoreCase 로 사전식 비교
    /// chunk 수 동일까지 끝나면 chunk 수 큰 쪽이 뒤.
    /// </summary>
    public class NaturalStringComparer : IComparer<string>
    {
        // 숫자 연속 또는 비숫자 연속을 chunk 로 분리
        private static readonly Regex _chunkRegex = new Regex(@"(\d+|\D+)", RegexOptions.Compiled);

        public int Compare(string x, string y)
        {
            if (x == null && y == null) return 0;
            if (x == null) return -1;
            if (y == null) return 1;

            var xChunks = _chunkRegex.Matches(x);
            var yChunks = _chunkRegex.Matches(y);
            int n = System.Math.Min(xChunks.Count, yChunks.Count);
            for (int i = 0; i < n; i++)
            {
                string xc = xChunks[i].Value;
                string yc = yChunks[i].Value;
                int cmp;
                if (xc.Length > 0 && yc.Length > 0 && char.IsDigit(xc[0]) && char.IsDigit(yc[0]))
                {
                    int xi, yi;
                    if (int.TryParse(xc, out xi) && int.TryParse(yc, out yi))
                    {
                        cmp = xi.CompareTo(yi);
                    }
                    else
                    {
                        // overflow 등 int.TryParse 실패 시 사전식 fallback
                        cmp = string.Compare(xc, yc, System.StringComparison.OrdinalIgnoreCase);
                    }
                }
                else
                {
                    cmp = string.Compare(xc, yc, System.StringComparison.OrdinalIgnoreCase);
                }
                if (cmp != 0) return cmp;
            }
            return xChunks.Count.CompareTo(yChunks.Count);
        }
    }
}
```

**csproj 등록 (`WPF_Example/DatumMeasurement.csproj`):**
Compile ItemGroup 의 `Utility\Logging.cs` 또는 `Utility\Ini.cs` 인접 위치에 다음 1줄 추가:

```xml
<Compile Include="Utility\NaturalStringComparer.cs" />
```

**컨벤션 준수:**
- PascalCase 클래스명 (CLAUDE.md)
- `Try` prefix 없음 (CompareTo 표준 시그니처)
- C# 7.2 / .NET 4.8 (nullable refs / switch expression / record 미사용)
- Allman brace (신규 파일 표준)
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /v:m</automated>
  </verify>
  <acceptance_criteria>
    - File exists: WPF_Example/Utility/NaturalStringComparer.cs
    - `Grep -F "class NaturalStringComparer : IComparer<string>" matches 1` (W3 fix: -F fixed-string 사용 — `<` 가 ripgrep regex 토큰으로 오인되지 않음)
    - `Grep -F "namespace ReringProject.Utility" matches 1`
    - `Grep -F "private static readonly Regex _chunkRegex" matches 1`
    - `Grep -F "@\"(\d+|\D+)\"" matches 1` (W3 fix: -F 로 regex pattern literal 검증)
    - `Grep -F "public int Compare(string x, string y)" matches 1`
    - csproj contains `<Compile Include="Utility\NaturalStringComparer.cs" />`
    - msbuild Rebuild Debug/x64 PASS (0 errors)
    - 신규 warning 0
    - //260530 hbk Phase 39.2 D-G3 마커 포함
  </acceptance_criteria>
  <done>
    NaturalStringComparer IComparer<string> 구현 완료, msbuild PASS. Shot2/Shot10 자연정렬 baseline 충족.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: InspectionListViewModel 정렬 헬퍼 + Add* / RebuildTree / Shot 트리거 (B2 fix 포함)</name>
  <read_first>
    - @WPF_Example/UI/ControlItem/InspectionListViewModel.cs (전체 — L55-114 CreateSequenceNode, L117-133 AddFAINode, L136-147 AddDatumNode, L150-162 AddMeasurementNode, L167-174 RebuildTree)
    - @WPF_Example/UI/ControlItem/InspectionListView.xaml.cs (Btn_AddShot_Click / AddShotToSequence — PATTERNS.md L26 analog 메서드 실제 위치 확인 — Shot add 후 actionNode children 정렬 트리거 위치)
    - @WPF_Example/UI/ControlItem/NodeViewModel.cs (L70-104 Children getter + ReloadChildren in-place 갱신 패턴)
  </read_first>
  <behavior>
    - Test 1: SortNodeChildren(null) 안전 (NoOp)
    - Test 2: SortNodeChildren(parent with 1 child) NoOp (이미 정렬됨)
    - Test 3: SortNodeChildren(parent with [Shot10, Shot2, Shot1]) → 정렬 후 children = [Shot1, Shot2, Shot10]
    - Test 4: SortAllLevels 호출 시 Recipe → Sequence → Action → FAI → Measurement / Datum 까지 모든 children 정렬
    - Test 5: AddFAINode 끝에 SortNodeChildren(actionNode) 호출
    - Test 6: AddDatumNode 끝에 SortNodeChildren(seqNode) 호출
    - Test 7: AddMeasurementNode 끝에 SortNodeChildren(faiNode) 호출
    - Test 8: RebuildTree 끝에 SortAllLevels 호출 (HookSequenceDisplayNameUpdates 뒤)
    - Test 9 (B2 fix): Btn_AddShot_Click 또는 AddShotToSequence (PATTERNS.md L26 analog) 의 Shot add 직후 부모 actionNode children 자연정렬
  </behavior>
  <action>
**파일:** `WPF_Example/UI/ControlItem/InspectionListViewModel.cs`

스타일: 기존 파일 K&R brace (one-line method) 보존. 주석 마커 `//260530 hbk Phase 39.2 D-G3`.

**[A] using 추가 (L1-9 영역):**
```csharp
using ReringProject.Utility; //260530 hbk Phase 39.2 D-G3 — NaturalStringComparer
```

**[B] 정렬 헬퍼 + comparer static 필드 추가** — 클래스 안 (L13 `public class InspectionListViewModel : Observable {` 직후 또는 L43 `public int Count { get; set; }` 직후):

```csharp
//260530 hbk Phase 39.2 D-G3 — 자연정렬 비교자 (Shot2 < Shot10)
private static readonly NaturalStringComparer _naturalComparer = new NaturalStringComparer(); //260530 hbk Phase 39.2 D-G3

/// <summary>지정한 부모 노드의 직속 children 만 Name 기반 자연정렬 (asc).
/// ObservableCollection 이벤트로 TreeListBox 안전 갱신 (clear + re-add).</summary>
//260530 hbk Phase 39.2 D-G3 — Add 시 즉시 정렬 + Rename hook 호출 진입점
public static void SortNodeChildren(NodeViewModel parent) { //260530 hbk Phase 39.2 D-G3
    if (parent == null) return; //260530 hbk Phase 39.2 D-G3
    var list = parent.Children; //260530 hbk Phase 39.2 D-G3 — LoadChildren 보장
    if (list == null || list.Count <= 1) return; //260530 hbk Phase 39.2 D-G3
    var sorted = new List<NodeViewModel>(list); //260530 hbk Phase 39.2 D-G3
    sorted.Sort((a, b) => _naturalComparer.Compare(a != null ? a.Name : null, b != null ? b.Name : null)); //260530 hbk Phase 39.2 D-G3
    bool dirty = false; //260530 hbk Phase 39.2 D-G3 — 변경 감지 (no-op skip 으로 RaisePropertyChanged 폭발 방지)
    for (int i = 0; i < sorted.Count; i++) { //260530 hbk Phase 39.2 D-G3
        if (!ReferenceEquals(sorted[i], list[i])) { dirty = true; break; } //260530 hbk Phase 39.2 D-G3
    }
    if (!dirty) return; //260530 hbk Phase 39.2 D-G3
    list.Clear(); //260530 hbk Phase 39.2 D-G3
    foreach (var item in sorted) list.Add(item); //260530 hbk Phase 39.2 D-G3
}

//260530 hbk Phase 39.2 D-G3 — 전 트리 재귀 정렬 (RebuildTree 후 또는 외부 일괄 정렬용)
public void SortAllLevels() { //260530 hbk Phase 39.2 D-G3
    SortRecursive(RootModel); //260530 hbk Phase 39.2 D-G3
}
private static void SortRecursive(NodeViewModel node) { //260530 hbk Phase 39.2 D-G3
    if (node == null) return; //260530 hbk Phase 39.2 D-G3
    SortNodeChildren(node); //260530 hbk Phase 39.2 D-G3
    foreach (var child in node.Children) //260530 hbk Phase 39.2 D-G3
        SortRecursive(child); //260530 hbk Phase 39.2 D-G3
}
```

**[C] Add* 메서드 끝에 SortNodeChildren 호출 추가 + B2 fix: Shot add 경로 트리거:**

L132 `cn.Add(faiVm);` 직후에 추가:
```csharp
SortNodeChildren(actionNode); //260530 hbk Phase 39.2 D-G3-03 — Add 시 즉시 정렬
```

L146 `seqNode.Children.Add(datumVm);` 직후에 추가:
```csharp
SortNodeChildren(seqNode); //260530 hbk Phase 39.2 D-G3-03 — Add 시 즉시 정렬
```

L161 `faiNode.Children.Add(measVm);` 직후에 추가:
```csharp
SortNodeChildren(faiNode); //260530 hbk Phase 39.2 D-G3-03 — Add 시 즉시 정렬
```

**B2 fix — Shot add 경로 트리거 추가:**

PATTERNS.md L26 D-G3-C analog 가 `AddShotToSequence` (L833+) 를 명시한다. 본 plan 의 InspectionListViewModel.cs 에 `AddShotNode(NodeViewModel actionNode, ShotConfig shot)` 또는 동등 메서드가 존재할 경우:
- 해당 메서드 끝부분 (Shot 노드를 actionNode 또는 seqNode children 에 Add 한 직후) 에 다음 호출 추가:

```csharp
SortNodeChildren(actionNode); //260530 hbk Phase 39.2 D-G3-03 B2 fix — Shot add 시 즉시 정렬 (PATTERNS.md L26 D-G3-C analog 충족)
```

⚠️ InspectionListViewModel.cs 에 AddShotNode 가 없는 경우 (Shot add 가 InspectionListView.xaml.cs 측 code-behind 에서만 처리되는 경우), 본 트리거는 Task 2 [D] (xaml.cs 직접 수정) 에서 처리. read_first 단계에서 코드베이스 실제 메서드 존재 여부 확인 후 한쪽에만 추가 (중복 호출 회피).

**[D] InspectionListView.xaml.cs Btn_AddShot_Click 트리거 (B2 fix — 본 plan 신규 범위):**

`WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` 의 `Btn_AddShot_Click` 또는 `AddShotToSequence` (PATTERNS.md L26 명시 메서드명) 의 Shot 노드 add 직후 라인에 다음 호출 추가:

```csharp
//260530 hbk Phase 39.2 D-G3-03 B2 fix — Shot add 후 부모 children 자연정렬
InspectionListViewModel.SortNodeChildren(actionNode); //260530 hbk Phase 39.2 D-G3
// (actionNode 가 직접 가용 변수가 아닌 경우 parentNode / shotNode.Parent 사용)
```

⚠️ B2 fix 트리거는 InspectionListViewModel.AddShotNode 또는 InspectionListView.xaml.cs Btn_AddShot_Click **둘 중 하나**에만 추가 (중복 정렬 호출 회피). read_first 단계에서 Shot add 경로의 실제 메서드 위치 확인 후 결정.

**[E] RebuildTree 끝에 SortAllLevels 호출 추가** — L173 `HookSequenceDisplayNameUpdates();` 직후:
```csharp
SortAllLevels(); //260530 hbk Phase 39.2 D-G3 — recipe 로드 후 전 트리 자연정렬
```

**[F] InspectionListViewModel 생성자 끝 (L52 `HookSequenceDisplayNameUpdates();` 직후) 에도 추가:**
```csharp
SortAllLevels(); //260530 hbk Phase 39.2 D-G3 — 초기 트리 생성 후 자연정렬
```

⚠️ **Anti-goal (CONTEXT D-G3 마지막):**
- CreateSequenceNode (L55-114) 본문 변경 0 — model.Children.Add(...) 순서는 그대로, ParamBase 컬렉션 순서 그대로
- ShotConfig.FAIList / FAIConfig.Measurements / InspectionSequence.DatumConfigs 변경 0
- ParamBase Save/Load INI 직렬화 순서 변경 0 — 본 정렬은 NodeViewModel.Children (UI 레이어) 에서만 동작
- HookSequenceDisplayNameUpdates 본문 변경 0
- OnSequenceMasterPropertyChanged 본문 변경 0 — Name 갱신 자체는 정렬에 영향 (자동) — but DisplayName 변경 시점에도 부모 children 재정렬 안전: NodeViewModel.OnParamPropertyChanged 의 ShotName/DatumName/FAIName/MeasurementName 분기에서 처리 (Task 3)

**InspectionListView.xaml.cs 변경 0 (CONTEXT 명시) → B2 fix 로 인해 1 라인 추가 허용** — 단, 변경 영역은 Shot add 직후 정렬 호출 1줄 + 주석만, 다른 본문 변경 0.
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /v:m</automated>
  </verify>
  <acceptance_criteria>
    - `Grep -F "private static readonly NaturalStringComparer _naturalComparer" in InspectionListViewModel.cs matches 1` (W3 fix: -F)
    - `Grep -F "public static void SortNodeChildren(NodeViewModel parent)" matches 1`
    - `Grep -F "public void SortAllLevels()" matches 1`
    - `Grep -F "private static void SortRecursive(NodeViewModel node)" matches 1`
    - `Grep -F "SortNodeChildren(actionNode);" matches 2` (B2 fix: AddFAINode + Shot add 경로 — 2 호출 합산)
    - `Grep -F "SortNodeChildren(seqNode);" matches 1` (in AddDatumNode)
    - `Grep -F "SortNodeChildren(faiNode);" matches 1` (in AddMeasurementNode)
    - `Grep -F "SortAllLevels();" matches 2` (constructor + RebuildTree)
    - `Grep -F "using ReringProject.Utility;" matches 1`
    - B2 fix Shot add 트리거: `Grep -F "InspectionListViewModel.SortNodeChildren(" in InspectionListView.xaml.cs matches >= 1` OR `Grep -F "SortNodeChildren(actionNode)" in InspectionListViewModel.cs matches 2` (Shot 트리거 위치는 read_first 단계 결과에 따라 한쪽에만)
    - CreateSequenceNode (L55-114 영역) `model.Children.Add` / `seqNode.Children.Add` / `actNode.Children.Add` / `faiNode.Children.Add` 호출 보존 (count 변경 0)
    - HookSequenceDisplayNameUpdates / OnSequenceMasterPropertyChanged 본문 변경 0 (line count 비교)
    - msbuild Rebuild Debug/x64 PASS (0 errors)
    - 신규 warning 0
    - 모든 신규/수정 라인에 //260530 hbk Phase 39.2 D-G3 마커
  </acceptance_criteria>
  <done>
    SortNodeChildren / SortAllLevels 헬퍼 도입 + AddFAINode / AddDatumNode / AddMeasurementNode / RebuildTree / Constructor 5 곳 트리거 + B2 fix Shot add 경로 트리거 1 추가. ParamBase 컬렉션 순서 변경 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: NodeViewModel Rename → 부모 SortNodeChildren 트리거</name>
  <read_first>
    - @WPF_Example/UI/ControlItem/NodeViewModel.cs (전체 — L210-240 OnParamPropertyChanged, 특히 L236-239 Name 갱신 분기)
    - @WPF_Example/UI/ControlItem/InspectionListViewModel.cs Task 2 산물 (SortNodeChildren static)
  </read_first>
  <behavior>
    - Test 1: PropertyGrid 에서 DatumName="Datum_C10" → "Datum_A1" 변경 시 부모 children (Datum 노드 컬렉션) 재정렬
    - Test 2: ShotName / FAIName / MeasurementName 변경 시도 동일
    - Test 3: this.Parent == null (Root node) 인 경우 재정렬 호출 skip (안전 분기)
    - Test 4: HasDetectFail 분기 (L232-234) 는 변경 0 — Name 갱신 분기 아니므로 정렬 트리거 없음
    - Test 5: MeasurementName 폴백 (TypeName) 동작 보존
  </behavior>
  <action>
**파일:** `WPF_Example/UI/ControlItem/NodeViewModel.cs`

스타일: 기존 파일 K&R brace 보존. 주석 마커 `//260530 hbk Phase 39.2 D-G3`.

**[A] L236-239 Name 갱신 분기에 정렬 트리거 추가** — 현재 코드:
```csharp
if (newName != null && this.Node != null) {
    this.Node.Name = newName;
    RaisePropertyChanged("Name");
}
```

**교체:**
```csharp
if (newName != null && this.Node != null) {
    this.Node.Name = newName;
    RaisePropertyChanged("Name");
    //260530 hbk Phase 39.2 D-G3-03 — Rename 후 부모 children 재정렬 (즉시 트리 정렬 갱신)
    if (this.Parent != null) { //260530 hbk Phase 39.2 D-G3
        InspectionListViewModel.SortNodeChildren(this.Parent); //260530 hbk Phase 39.2 D-G3
    }
}
```

⚠️ **Anti-goal:**
- L210-235 switch 본문 변경 0 (DatumName / ShotName / FAIName / MeasurementName / LastFindSucceeded 분기 모두 보존)
- L232-234 HasDetectFail 분기 (Phase 39 WF-02 D-04) 변경 0 — return 만 발생, 정렬 트리거 X (이 분기는 Name 변경이 아님)
- OnParamPropertyChanged 시그니처 변경 0
- L196-206 생성자 (Param INPC 구독) 변경 0

**Why SortNodeChildren is `static`?**
NodeViewModel 은 InspectionListViewModel 인스턴스 참조 없음 (의도된 분리). Task 2 에서 `public static void SortNodeChildren(NodeViewModel parent)` 로 노출했으므로 ViewModel 인스턴스 없이 직접 호출 가능.
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /v:m</automated>
  </verify>
  <acceptance_criteria>
    - `Grep -F "InspectionListViewModel.SortNodeChildren(this.Parent);" in NodeViewModel.cs matches 1` (W3 fix: -F)
    - `Grep -F "if (this.Parent != null)" matches 1` (안전 분기)
    - L210-235 switch 본문 보존 (`Grep -F "case nameof(DatumConfig.DatumName):" matches 1` / `Grep -F "case nameof(ShotConfig.ShotName):" matches 1` / `Grep -F "case nameof(FAIConfig.FAIName):" matches 1` / `Grep -F "case nameof(MeasurementBase.MeasurementName):" matches 1` / `Grep -F "case nameof(DatumConfig.LastFindSucceeded):" matches 1`)
    - `Grep -F "RaisePropertyChanged(\"HasDetectFail\")" matches 1` (변경 0)
    - `Grep -F "this.Node.Name = newName;" matches 1`
    - msbuild Rebuild Debug/x64 PASS (0 errors)
    - 신규 warning 0
    - 모든 신규/수정 라인에 //260530 hbk Phase 39.2 D-G3 마커
  </acceptance_criteria>
  <done>
    Rename (4 가지 Name 프로퍼티) 시 부모 children 즉시 자연정렬 재실행. Phase 39 HasDetectFail 분기 + 기존 LastFindSucceeded 회귀 0.
  </done>
</task>

</tasks>

<verification>
**End-to-end smoke check (msbuild PASS 시 자동 충족):**
1. NaturalStringComparer.Compare("Shot2", "Shot10") < 0 단위 검증 (Plan 05 SIMUL UAT 수동)
2. RebuildTree (recipe 로드) 후 RootModel.Children → Sequence → Action → FAI → Measurement 가 자연정렬 (사용자 visible)
3. PropertyGrid 에서 DatumName/ShotName/FAIName/MeasurementName 변경 시 트리 노드가 새 위치로 이동 (즉시 보임)
4. Add(Shot|FAI|Datum|Measurement)Node 호출 시 정렬된 위치에 삽입 (B2 fix: Shot 경로 포함 4 레벨 add 트리거)

**회귀 0 가드:**
- ParamBase Save/Load INI 직렬화 순서 변경 0 — 본 정렬은 NodeViewModel.Children 만 변경
- ShotConfig.FAIList / FAIConfig.Measurements / InspectionSequence.DatumConfigs / pSystemHandle.Sequences 변경 0
- InspectionListView.xaml.cs 변경 최소화 (B2 fix Shot add 트리거 1 라인만 — 본문 다른 부분 변경 0)
- TreeView SelectionChanged / RenderInspectionResultForNode / DataGrid_faiResults / PropertyGrid 본문 변경 0
- Phase 39 HasDetectFail 분기 변경 0
- HookSequenceDisplayNameUpdates / OnSequenceMasterPropertyChanged 변경 0
</verification>

<success_criteria>
- [ ] Task 1: NaturalStringComparer.cs 신규 + csproj 등록 → msbuild PASS
- [ ] Task 2: InspectionListViewModel.cs Sort 헬퍼 + Add*/RebuildTree/Constructor 5 트리거 + B2 fix Shot 경로 트리거 1 추가 → msbuild PASS
- [ ] Task 3: NodeViewModel.cs Rename hook → msbuild PASS
- [ ] msbuild Rebuild Debug/x64 0 errors / 0 신규 warnings
- [ ] InspectionListView.xaml.cs 변경 최소화 (B2 fix Shot add 트리거만)
- [ ] ParamBase 컬렉션 순서 변경 0 (view-only 정렬)
- [ ] 모든 신규/수정 라인에 `//260530 hbk Phase 39.2 D-G3` 마커
</success_criteria>

<output>
After completion, create `.planning/phases/39.2-urgent-additions-2-2026-05-30/39.2-03-SUMMARY.md` covering:
- NaturalStringComparer.cs 신설 결과 (line count, regex pattern 확인, csproj 등록)
- InspectionListViewModel.cs 정렬 헬퍼 + 5 트리거 + B2 fix Shot 경로 트리거 추가 결과 (라인 위치)
- NodeViewModel.cs Rename hook 추가 결과 (L236-239 → L240-243 영역)
- InspectionListView.xaml.cs B2 fix Shot add 트리거 1 라인 추가 결과
- msbuild Rebuild Debug/x64 결과 (errors / warnings)
- 회귀 0 가드 grep 결과 (CreateSequenceNode 보존, HookSequenceDisplayNameUpdates 보존, HasDetectFail 보존, InspectionListView.xaml.cs 본문 변경 최소화)
- 자연정렬 단위 동작 (NaturalStringComparer.Compare unit tests — Plan 05 SIMUL UAT 에서 visible)
</output>
</content>
