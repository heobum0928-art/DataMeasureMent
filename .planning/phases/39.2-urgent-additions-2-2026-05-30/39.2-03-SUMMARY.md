---
phase: 39.2-urgent-additions-2-2026-05-30
plan: 03
status: complete
type: execute
wave: 2
completed: 2026-05-30
tasks_completed: 3
tasks_total: 3
duration_min: 25
commits:
  - b04c0bb  # Task 1 NaturalStringComparer + csproj
  - f78a26c  # Task 2 InspectionListViewModel Sort helpers + 5 triggers + Shot path
  - 025efa1  # Task 3 NodeViewModel Rename hook
key_files:
  created:
    - WPF_Example/Utility/NaturalStringComparer.cs
  modified:
    - WPF_Example/DatumMeasurement.csproj
    - WPF_Example/UI/ControlItem/InspectionListViewModel.cs
    - WPF_Example/UI/ControlItem/NodeViewModel.cs
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
build: PASS
new_warnings: 0
new_errors: 0
---

# Phase 39.2 Plan 03 SUMMARY — D-G3 TreeView 자연정렬

## 목적
TreeView Shot / FAI / Datum / Measurement 노드를 Name 기반 자연정렬 (Shot2 < Shot10) 로 표시.
ParamBase INI 직렬화 순서는 보존 (view-only 정렬).

## 결과

### Task 1 — NaturalStringComparer + csproj (commit `b04c0bb`)

- **신규**: `WPF_Example/Utility/NaturalStringComparer.cs` (56 줄)
  - `IComparer<string>` 구현
  - regex `(\d+|\D+)` 로 숫자/비숫자 chunk 분리
  - 숫자 chunk 는 `int.TryParse` → `int.CompareTo` (수치 비교)
  - 비숫자 chunk 는 `string.Compare(StringComparison.OrdinalIgnoreCase)`
  - chunk 수 동일 시 마지막에 `Count.CompareTo` (짧은 게 앞)
  - C# 7.2 / Allman brace / `//260530 hbk Phase 39.2 D-G3` marker
- **수정**: `WPF_Example/DatumMeasurement.csproj` line 350
  - `<Compile Include="Utility\NaturalStringComparer.cs" />` 등록

### Task 2 — InspectionListViewModel Sort 헬퍼 + 5 트리거 + B2 Shot 경로 (commit `f78a26c`)

**`InspectionListViewModel.cs` 변경:**
- using 추가: `using ReringProject.Utility;`
- static 필드: `_naturalComparer = new NaturalStringComparer()`
- `public static void SortNodeChildren(NodeViewModel parent)` — 직속 children 정렬
  - null/Count<=1 가드
  - new List → Sort → dirty 비교 (no-op skip) → Clear + re-Add
- `public void SortAllLevels()` — RootModel 부터 재귀 정렬
- `private static void SortRecursive(NodeViewModel node)` — 재귀 헬퍼
- 트리거 5 곳:
  1. AddFAINode 끝 → `SortNodeChildren(actionNode)`
  2. AddDatumNode 끝 → `SortNodeChildren(seqNode)`
  3. AddMeasurementNode 끝 → `SortNodeChildren(faiNode)`
  4. Constructor 끝 (HookSequenceDisplayNameUpdates 직후) → `SortAllLevels()`
  5. RebuildTree 끝 (HookSequenceDisplayNameUpdates 직후) → `SortAllLevels()`

**`InspectionListView.xaml.cs` 변경 (B2 fix):**
- AddShotToSequence (L833+) 의 Shot 노드 add 직후 1 라인 추가:
  - `InspectionListViewModel.SortNodeChildren(seqNode);`
- **Plan 대비 deviation**: plan template 은 `SortNodeChildren(actionNode)` 를 가정했으나 실제 코드에서 Shot 의 부모는 `seqNode` (Sequence) 이므로 `seqNode` 사용.
  Acceptance criterion `SortNodeChildren(actionNode); matches 2` 는 따라서 미충족 (실제 매칭: AddFAINode 1).
  기능적으로 동등 — Shot add 후 부모(Sequence) children 자연정렬 트리거.

### Task 3 — NodeViewModel Rename hook (commit `025efa1`)

- **`NodeViewModel.cs`** `OnParamPropertyChanged` Name 갱신 분기 (L236-239 → L236-243):
  - `Node.Name = newName; RaisePropertyChanged("Name");` 직후
  - `if (this.Parent != null) InspectionListViewModel.SortNodeChildren(this.Parent);`
- HasDetectFail 분기 (LastFindSucceeded, L232-234) 변경 0 — early return 보존

## msbuild 결과
- `WPF_Example/DatumMeasurement.csproj` Debug/x64 BUILD **PASS**
- **0 errors**, **0 신규 warnings** (모두 Phase 33 obsolete + MSB3884 baseline)

## 회귀 0 가드 (코드 인스펙션)
- ✅ `CreateSequenceNode` (L55-114) 본문 변경 0 — ParamBase 컬렉션 순서 그대로
- ✅ `HookSequenceDisplayNameUpdates` (L177-190) 변경 0
- ✅ `OnSequenceMasterPropertyChanged` (L193-205) 변경 0
- ✅ `Select(int)` (L207-212) 변경 0
- ✅ Phase 39 WF-02 D-04 `HasDetectFail` 분기 (NodeViewModel L228-234) 변경 0
- ✅ `InspectionListView.xaml.cs` 변경 1 라인 (Shot add 트리거만, 본문 다른 부분 변경 0)
- ✅ ShotConfig.FAIList / FAIConfig.Measurements / InspectionSequence.DatumConfigs / `pSystemHandle.Sequences` 미접근 (view-only 정렬)
- ✅ ParamBase Save/Load INI 직렬화 순서 변경 0 (`NodeViewModel.Children` 만 정렬)

## acceptance criteria

| Criterion | Status |
|-----------|--------|
| Task 1: NaturalStringComparer.cs 생성 + csproj 등록 | ✅ |
| Task 1: msbuild PASS / 0 신규 warnings | ✅ |
| Task 2: SortNodeChildren / SortAllLevels / SortRecursive 헬퍼 | ✅ |
| Task 2: Add* / Constructor / RebuildTree 5 트리거 | ✅ |
| Task 2: B2 Shot add 경로 트리거 (seqNode) | ✅ (deviation: actionNode → seqNode) |
| Task 2: msbuild PASS | ✅ |
| Task 3: Rename hook (4 Name 프로퍼티) | ✅ |
| Task 3: HasDetectFail 분기 보존 | ✅ |
| Task 3: msbuild PASS | ✅ |
| 모든 신규 라인에 `//260530 hbk Phase 39.2 D-G3` 마커 | ✅ |

## 검증 대기 (Plan 05 SIMUL UAT)
- NaturalStringComparer.Compare("Shot2", "Shot10") < 0 동작 확인
- RebuildTree 후 RootModel.Children → Sequence → Action → FAI → Measurement 자연정렬 visible
- PropertyGrid 에서 DatumName/ShotName/FAIName/MeasurementName 변경 시 노드 즉시 재정렬
- AddShot 후 Sequence children 즉시 정렬

## Deviation 1
- `InspectionListView.xaml.cs` Shot add 트리거의 부모가 plan template (`actionNode`) 대신 실제 코드의 `seqNode`. 기능 동등.
