---
phase: 39.2-urgent-additions-2-2026-05-30
plan: 04
type: execute
wave: 2
depends_on: ["01", "02"]  # B1 fix: Plan 04 Icon.Meas.DualImageEdgeDistance resource key 가 Plan 01 의 TypeName 등록 후 실행되어야 함 (wave 2 일관성)
files_modified:
  - WPF_Example/UI/ControlItem/Node.cs
  - WPF_Example/UI/ControlItem/NodeViewModel.cs
  - WPF_Example/UI/ControlItem/IconKeyToGeometryConverter.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: ["WF-01"]
tags: ["ui", "tree", "icon", "geometry"]

must_haves:
  truths:
    - "TreeView 의 각 노드가 NodeType + Measurement TypeName 기반의 Geometry icon 으로 표시된다"
    - "FAI 노드와 Measurement 노드의 아이콘이 시각적으로 다르다"
    - "12 Measurement TypeName 별로 다른 Geometry path 가 적용된다 (CircleDiameter, EdgeToLineDistance, ..., DualImageEdgeDistance 포함)"
    - "노드 텍스트 (Name) 표시는 변경 없음 (`{Binding Name}` 보존)"
    - "기존 PNG ImageSource 프로퍼티는 보존되어 다른 사용처 회귀 0"
    - "Phase 39.1-03 RenderInspectionResultForNode / TreeView SelectionChanged / DataGrid_faiResults / PropertyGrid 동작 변경 0"
  artifacts:
    - path: "WPF_Example/UI/ControlItem/Node.cs"
      provides: "IconKey 프로퍼티 (NodeType + Measurement TypeName 기반 string key)"
      contains: "public string IconKey"
    - path: "WPF_Example/UI/ControlItem/NodeViewModel.cs"
      provides: "IconKey 프로퍼티 proxy"
      contains: "public string IconKey"
    - path: "WPF_Example/UI/ControlItem/IconKeyToGeometryConverter.cs"
      provides: "IValueConverter: IconKey string → Application.Current.Resources lookup → Geometry"
      contains: "class IconKeyToGeometryConverter : IValueConverter"
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml"
      provides: "Geometry 리소스 17 종 (5 NodeType + 12 Measurement + Default fallback) + DataTemplate Path 교체"
      contains: "Geometry.Icon."
    - path: "WPF_Example/DatumMeasurement.csproj"
      provides: "IconKeyToGeometryConverter.cs Compile ItemGroup 등록"
      contains: "IconKeyToGeometryConverter.cs"
  key_links:
    - from: "Node.IconKey (NodeType switch + Measurement TypeName)"
      to: "NodeViewModel.IconKey (proxy)"
      via: "this.Node.IconKey getter delegation"
      pattern: "this.Node.IconKey"
    - from: "InspectionListView.xaml DataTemplate Path.Data"
      to: "IconKeyToGeometryConverter.Convert"
      via: "Binding IconKey, Converter={StaticResource iconKeyToGeometry}"
      pattern: 'Converter=\{StaticResource iconKeyToGeometry\}'
    - from: "IconKeyToGeometryConverter.Convert(key)"
      to: "Application.Current.Resources[\"Geometry.{key}\"] 또는 \"Geometry.Icon.Default\""
      via: "ResourceDictionary lookup"
      pattern: "Application.Current.Resources\\["
---

<objective>
Phase 39.2 D-G4 — TreeView 노드 아이콘을 NodeType + Measurement TypeName 기반 Geometry path 로 차별화한다.

Purpose: POC 2026-06-30 시연 — 사용자가 트리에서 노드 종류 + 측정 타입을 시각적으로 즉시 식별 (FAI vs Measurement 분리 + 12 Measurement TypeName 별 차별). 외부 NuGet 의존 0, monochrome theme foreground.
Output: Node.cs IconKey 프로퍼티 + NodeViewModel.cs IconKey proxy + IconKeyToGeometryConverter.cs IValueConverter + InspectionListView.xaml Geometry resources 17 종 + DataTemplate Path 교체 + csproj 등록.

핵심 lock (CONTEXT D-G4 / PATTERNS D-G4-A~B 반영 + planner 추가 lock):
- 자원 = Geometry Path inline XAML (Material Icons SVG path 발췌, monochrome)
- 적용 범위 = 5 NodeType (Recipe / Sequence / Action / Datum / FAI) + 12 Measurement TypeName (CircleDiameter / EdgeToLineDistance / PointToLineDistance / EdgePairDistance / EdgeToLineAngle / ArcEdgeDistance / ArcLineIntersectDistance / CompoundAngle / CompoundCenterCDistance / CompoundCenterBDistance / CompoundShortAxisDistance / DualImageEdgeDistance) + Default fallback + Measurement fallback (총 17 + 1 = 18 키)
- 구현 방식 = **IValueConverter + DynamicResource lookup** (단순 1 DataTemplate + Geometry resource 17, DataTemplateSelector 미도입)
- 기존 ImageSource 프로퍼티 보존 (다른 사용처 - context menu MenuItem.Icon 등 영향 X)
- 노드 텍스트 (Name) 변경 0 (Anti-goal)
- Phase 39.1-03 RenderInspectionResultForNode 호출 흐름 변경 0 (Anti-goal)
- TreeView SelectionChanged 핸들러 (InspectionListView) 본문 변경 0 (Anti-goal)
- DataGrid_faiResults / PropertyGrid 변경 0 (Anti-goal)

**SVG Geometry path data (Material Icons 발췌 + 개발자 디자인) — plan-phase 에서 lock:**

(W1 fix: CompoundCenterBDistance path 의 중복 segment 제거 — 좌측 화살표 mirror 정상화)

| IconKey | 모티프 | Geometry path (compact) |
|---------|--------|-------------------------|
| Icon.Recipe | folder | `M10,4H4C2.89,4 2,4.89 2,6V18A2,2 0 0,0 4,20H20A2,2 0 0,0 22,18V8C22,6.89 21.1,6 20,6H12L10,4Z` |
| Icon.Sequence | flow chart | `M5,4H7V8H9V4H11V14H9V10H7V14H5V4M13,4H15V8H17V4H19V14H17V10H15V14H13V4M5,16H19V18H5V16M5,20H19V22H5V20Z` |
| Icon.Action | layers | `M12,16L19.36,10.27L21,9L12,2L3,9L4.63,10.27L12,16M12,18.54L4.62,12.81L3,14.07L12,21.07L21,14.07L19.37,12.81L12,18.54Z` |
| Icon.Datum | crosshair | `M11,2V4.07C7.38,4.53 4.53,7.38 4.07,11H2V13H4.07C4.53,16.62 7.38,19.47 11,19.93V22H13V19.93C16.62,19.47 19.47,16.62 19.93,13H22V11H19.93C19.47,7.38 16.62,4.53 13,4.07V2M11,6.08V8H13V6.09C15.5,6.5 17.5,8.5 17.92,11H16V13H17.91C17.5,15.5 15.5,17.5 13,17.92V16H11V17.91C8.5,17.5 6.5,15.5 6.08,13H8V11H6.09C6.5,8.5 8.5,6.5 11,6.08M12,11A1,1 0 0,0 11,12A1,1 0 0,0 12,13A1,1 0 0,0 13,12A1,1 0 0,0 12,11Z` |
| Icon.FAI | document with chart | `M19,3H5C3.89,3 3,3.89 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V5C21,3.89 20.1,3 19,3M9,17H7V10H9V17M13,17H11V7H13V17M17,17H15V13H17V17Z` |
| Icon.Measurement (fallback) | empty square | `M3,3H21V21H3V3M5,5V19H19V5H5Z` |
| Icon.Meas.CircleDiameter | circle with diameter line | `M12,2A10,10 0 0,1 22,12A10,10 0 0,1 12,22A10,10 0 0,1 2,12A10,10 0 0,1 12,2M12,4A8,8 0 0,0 4,12A8,8 0 0,0 12,20A8,8 0 0,0 20,12A8,8 0 0,0 12,4M3,12L21,12Z` |
| Icon.Meas.EdgeToLineDistance | point with perpendicular line | `M4,18H20V20H4V18M12,2L5,12H9V16H15V12H19L12,2Z` |
| Icon.Meas.PointToLineDistance | dot above line | `M12,7A2,2 0 1,0 12,11A2,2 0 0,0 12,7M4,18H20V20H4V18M12,14L8,18H16L12,14Z` |
| Icon.Meas.EdgePairDistance | two parallel lines | `M4,4H20V6H4V4M4,18H20V20H4V18M4,11H20V13H4V11Z` |
| Icon.Meas.EdgeToLineAngle | angle | `M21,21H3L21,3V21M5.83,19H19V5.83L5.83,19Z` |
| Icon.Meas.ArcEdgeDistance | arc with line | `M2,17H22V19H2V17M12,3A6,6 0 0,1 18,9A6,6 0 0,1 12,15A6,6 0 0,1 6,9A6,6 0 0,1 12,3M12,5A4,4 0 0,0 8,9A4,4 0 0,0 12,13A4,4 0 0,0 16,9A4,4 0 0,0 12,5Z` |
| Icon.Meas.ArcLineIntersectDistance | arc intersect line | `M3,11L21,11V13L3,13V11M12,3A8,8 0 0,1 20,11H18A6,6 0 0,0 12,5A6,6 0 0,0 6,11H4A8,8 0 0,1 12,3Z` |
| Icon.Meas.CompoundAngle | nested angle | `M21,21H3L21,3V21M5.83,19H19V5.83L5.83,19M14,17L17,14L17,17L14,17Z` |
| Icon.Meas.CompoundCenterCDistance | circle C + arrow (right) | `M12,2A10,10 0 0,1 22,12A10,10 0 0,1 12,22A10,10 0 0,1 2,12A10,10 0 0,1 12,2M12,4A8,8 0 0,0 4,12A8,8 0 0,0 12,20A8,8 0 0,0 20,12A8,8 0 0,0 12,4M22,12L18,8V11H14V13H18V16L22,12Z` |
| Icon.Meas.CompoundCenterBDistance | circle B + arrow (left mirror) | `M12,2A10,10 0 0,1 22,12A10,10 0 0,1 12,22A10,10 0 0,1 2,12A10,10 0 0,1 12,2M12,4A8,8 0 0,0 4,12A8,8 0 0,0 12,20A8,8 0 0,0 20,12A8,8 0 0,0 12,4M2,12L6,16V13H10V11H6V8L2,12Z` |
| Icon.Meas.CompoundShortAxisDistance | horizontal arrows | `M2,12L6,8V11H10V13H6V16L2,12M22,12L18,16V13H14V11H18V8L22,12Z` |
| Icon.Meas.DualImageEdgeDistance | two boxes + arrow | `M3,3H10V10H3V3M14,3H21V10H14V3M3,14H10V21H3V14M14,14H21V21H14V14M11,12L13,12L13,11L11,11L11,12Z` |
| Icon.Default | small circle | `M12,8A4,4 0 0,1 16,12A4,4 0 0,1 12,16A4,4 0 0,1 8,12A4,4 0 0,1 12,8Z` |

**Note (W1 fix):** CompoundCenterBDistance path 는 정확히 좌측 화살표 mirror (`M2,12L6,16V13H10V11H6V8L2,12Z`). 이전 plan 의 중복 `A8,8 0 0,0 12,4` segment 는 좌측 mirror 의도와 맞지 않아 정상 path 로 동기화. CompoundCenterCDistance (우측 화살표) 와 좌우 대칭.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.2-urgent-additions-2-2026-05-30/39.2-CONTEXT.md
@.planning/phases/39.2-urgent-additions-2-2026-05-30/39.2-PATTERNS.md

# Analog 파일
@WPF_Example/UI/ControlItem/Node.cs
@WPF_Example/UI/ControlItem/NodeViewModel.cs
@WPF_Example/UI/ControlItem/InspectionListView.xaml

<interfaces>
<!-- Key contracts -->

From Node.cs L20-54:
```csharp
public enum ENodeType { Recipe, Sequence, Action, SubSequence, FAI, Datum, Measurement }

public class Node
{
    public ENodeType NodeType { get; set; }
    public object ParamData { get; set; }                      // can be MeasurementBase, FAIConfig, ShotConfig, DatumConfig, etc.
    public string Name { get; set; }
    public string ImageSource { get; }                          // 기존 PNG path getter (보존)
}
```

From NodeViewModel.cs L106-109:
```csharp
public string ImageSource {
    get { return this.Node.ImageSource; }
    set { this.Node.ImageSource = value; RaisePropertyChanged("ImageSource"); }
}
```

From InspectionListView.xaml L43-53 (current DataTemplate — to replace):
```xml
<DataTemplate DataType="{x:Type local:NodeViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Image Grid.Column="0" Source="{Binding ImageSource}" Width="32" Height="32" Margin="0 0 4 0"/>
        <propToolsWpf:EditableTextBlock Grid.Column="1" Text="{Binding Name}" IsEditing="{Binding IsEditing}"/>
    </Grid>
</DataTemplate>
```

From MeasurementBase.cs:
```csharp
public abstract class MeasurementBase
{
    public string TypeName { get; }                            // e.g. "CircleDiameter", "EdgeToLineDistance", ...
    // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Node.IconKey 프로퍼티 + NodeViewModel.IconKey proxy 추가</name>
  <read_first>
    - @WPF_Example/UI/ControlItem/Node.cs (전체)
    - @WPF_Example/UI/ControlItem/NodeViewModel.cs L106-109 (ImageSource proxy 패턴)
    - @WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (TypeName getter)
  </read_first>
  <behavior>
    - Test 1: Node.IconKey 가 NodeType 별 string 반환 ("Icon.Recipe", "Icon.Sequence", ..., "Icon.FAI", "Icon.Datum")
    - Test 2: NodeType==Measurement + ParamData is MeasurementBase → "Icon.Meas." + meas.TypeName 반환 (e.g. "Icon.Meas.DualImageEdgeDistance")
    - Test 3: NodeType==Measurement + ParamData null/non-Measurement → "Icon.Measurement" fallback
    - Test 4: 알 수 없는 NodeType → "Icon.Default"
    - Test 5: NodeViewModel.IconKey proxy 가 this.Node.IconKey 위임
    - Test 6: 기존 ImageSource 프로퍼티는 보존 (변경 0)
  </behavior>
  <action>
**[A] 파일:** `WPF_Example/UI/ControlItem/Node.cs`

기존 K&R brace 보존, 주석 마커 `//260530 hbk Phase 39.2 D-G4`.

L54 `set { }` (ImageSource setter) 직후, `}` (class 닫기 전) 에 IconKey 프로퍼티 추가:

```csharp
//260530 hbk Phase 39.2 D-G4 — NodeType + Measurement TypeName 기반 Geometry ResourceKey
//  Recipe/Sequence/Action/Datum/FAI = NodeType 별 단일 키.
//  Measurement = "Icon.Meas." + TypeName (12 종 + DualImageEdgeDistance Plan 01 포함).
//  미지정 / 알 수 없는 ParamData = "Icon.Measurement" fallback.
//  최종 fallback (알 수 없는 NodeType) = "Icon.Default".
public string IconKey { //260530 hbk Phase 39.2 D-G4
    get {
        switch (NodeType) {
            case ENodeType.Recipe:      return "Icon.Recipe"; //260530 hbk Phase 39.2 D-G4
            case ENodeType.Sequence:    return "Icon.Sequence"; //260530 hbk Phase 39.2 D-G4
            case ENodeType.Action:      return "Icon.Action"; //260530 hbk Phase 39.2 D-G4
            case ENodeType.SubSequence: return "Icon.Action"; //260530 hbk Phase 39.2 D-G4 — SubSequence = Action 과 동일 아이콘
            case ENodeType.Datum:       return "Icon.Datum"; //260530 hbk Phase 39.2 D-G4
            case ENodeType.FAI:         return "Icon.FAI"; //260530 hbk Phase 39.2 D-G4
            case ENodeType.Measurement: //260530 hbk Phase 39.2 D-G4
                var meas = ParamData as ReringProject.Sequence.MeasurementBase; //260530 hbk Phase 39.2 D-G4
                if (meas != null && !string.IsNullOrEmpty(meas.TypeName)) //260530 hbk Phase 39.2 D-G4
                    return "Icon.Meas." + meas.TypeName; //260530 hbk Phase 39.2 D-G4
                return "Icon.Measurement"; //260530 hbk Phase 39.2 D-G4 — fallback (ParamData 미상)
        }
        return "Icon.Default"; //260530 hbk Phase 39.2 D-G4
    }
}
```

⚠️ 기존 `ImageSource` 프로퍼티 (L33-54) 변경 0 — Context menu / 다른 사용처에서 계속 사용.

**[B] 파일:** `WPF_Example/UI/ControlItem/NodeViewModel.cs`

기존 K&R brace 보존. L106-109 `public string ImageSource { ... }` 블록 **직후** (L110 부근) 에 다음 추가:

```csharp
//260530 hbk Phase 39.2 D-G4 — IconKey proxy (Node.IconKey 위임)
public string IconKey { //260530 hbk Phase 39.2 D-G4
    get { return this.Node.IconKey; } //260530 hbk Phase 39.2 D-G4
}
```

⚠️ ImageSource 프로퍼티 (L106-109) / 다른 모든 프로퍼티 변경 0.
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /v:m</automated>
  </verify>
  <acceptance_criteria>
    - `Grep -F "public string IconKey" in Node.cs matches 1` (W3 fix: -F)
    - `Grep -F "case ENodeType.Recipe:" matches 1`
    - `Grep -F "return \"Icon.Recipe\"" matches 1`
    - `Grep -F "Icon.Meas." in Node.cs matches 1` (concat 위치)
    - `Grep -F "ParamData as ReringProject.Sequence.MeasurementBase" matches 1`
    - `Grep -F "return \"Icon.Default\"" matches 1`
    - `Grep -F "public string IconKey" in NodeViewModel.cs matches 1`
    - `Grep -F "return this.Node.IconKey;" matches 1`
    - Node.cs ImageSource 프로퍼티 (L33-54) 보존 (`Grep -F "/Resource/folder.png" matches 1`, `Grep -F "/Resource/process.png" matches 2`, `Grep -F "/Resource/chart.png" matches 2`, `Grep -F "/Resource/layout.png" matches 2`, `Grep -F "/Resource/split.png" matches 1`)
    - NodeViewModel.cs ImageSource proxy (L106-109) 보존
    - msbuild Rebuild Debug/x64 PASS (0 errors)
    - 신규 warning 0
    - 모든 신규/수정 라인에 //260530 hbk Phase 39.2 D-G4 마커
  </acceptance_criteria>
  <done>
    Node.IconKey / NodeViewModel.IconKey 가 5 NodeType + Measurement TypeName 기반 string key 반환. 기존 ImageSource 보존.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: IconKeyToGeometryConverter (IValueConverter) 신규 + csproj 등록</name>
  <read_first>
    - @WPF_Example/UI/ControlItem/Node.cs (Task 1 산물 — IconKey getter)
    - @WPF_Example/UI/ControlItem/NodeViewModel.cs (Task 1 산물 — IconKey proxy)
  </read_first>
  <behavior>
    - Test 1: typeof(IconKeyToGeometryConverter).GetInterfaces() contains IValueConverter
    - Test 2: Convert("Icon.Recipe", ...) → Application.Current.Resources["Geometry.Icon.Recipe"] 반환 (XAML 등록 시)
    - Test 3: Convert(null, ...) → "Geometry.Icon.Default" 폴백
    - Test 4: Convert("Icon.Unknown.Foo", ...) → "Geometry.Icon.Default" 폴백 (resource 미존재 시)
    - Test 5: ConvertBack(...) → Binding.DoNothing (one-way only)
  </behavior>
  <action>
**파일 신규 작성:** `WPF_Example/UI/ControlItem/IconKeyToGeometryConverter.cs`

스타일: Allman brace (신규 파일 표준), 주석 마커 `//260530 hbk Phase 39.2 D-G4`.

```csharp
//260530 hbk Phase 39.2 D-G4 — IconKey (string) → Geometry resource lookup converter
using System;
using System.Globalization;
using System.Windows;
using System.Windows.Data;
using System.Windows.Media;

namespace ReringProject.UI
{
    /// <summary>
    /// NodeViewModel.IconKey → Application.Current.Resources["Geometry.{key}"] 으로 Geometry 룩업.
    /// 리소스가 InspectionListView.xaml UserControl.Resources 에 정의되어 있어도 동작 (WPF resource lookup chain).
    /// 매칭 실패 시 "Geometry.Icon.Default" 폴백, 그것마저 없으면 null (Path.Data null 안전).
    /// One-way only — ConvertBack 미지원.
    /// </summary>
    public class IconKeyToGeometryConverter : IValueConverter
    {
        public object Convert(object value, Type targetType, object parameter, CultureInfo culture)
        {
            string key = value as string;
            if (string.IsNullOrEmpty(key))
                key = "Icon.Default";

            string resourceKey = "Geometry." + key;

            // 1) Application.Current.Resources 우선 (전역 ResourceDictionary)
            if (Application.Current != null && Application.Current.Resources != null)
            {
                if (Application.Current.Resources.Contains(resourceKey))
                    return Application.Current.Resources[resourceKey] as Geometry;
            }

            // 2) FrameworkElement 까지 chain 못 들어와도 안전 — 폴백
            string defaultKey = "Geometry.Icon.Default";
            if (Application.Current != null && Application.Current.Resources != null
                && Application.Current.Resources.Contains(defaultKey))
            {
                return Application.Current.Resources[defaultKey] as Geometry;
            }

            return null;        // Path.Data = null 안전 (WPF가 빈 영역 표시)
        }

        public object ConvertBack(object value, Type targetType, object parameter, CultureInfo culture)
        {
            return Binding.DoNothing;       // one-way only
        }
    }
}
```

**csproj 등록** (`WPF_Example/DatumMeasurement.csproj`):
Compile ItemGroup 의 `UI\ControlItem\Node.cs` 또는 `UI\ControlItem\NodeViewModel.cs` 인접 위치에 추가:

```xml
<Compile Include="UI\ControlItem\IconKeyToGeometryConverter.cs" />
```

**컨벤션 준수:**
- PascalCase, namespace ReringProject.UI
- IValueConverter 표준 시그니처 (Convert / ConvertBack)
- C# 7.2 / .NET 4.8 호환
- Allman brace
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /v:m</automated>
  </verify>
  <acceptance_criteria>
    - File exists: WPF_Example/UI/ControlItem/IconKeyToGeometryConverter.cs
    - `Grep -F "class IconKeyToGeometryConverter : IValueConverter" matches 1` (W3 fix: -F)
    - `Grep -F "namespace ReringProject.UI" matches 1`
    - `Grep -F "Application.Current.Resources[resourceKey]" matches 1`
    - `Grep -F "\"Geometry.Icon.Default\"" matches 1`
    - `Grep -F "return Binding.DoNothing;" matches 1` (ConvertBack)
    - csproj contains `<Compile Include="UI\ControlItem\IconKeyToGeometryConverter.cs" />`
    - msbuild Rebuild Debug/x64 PASS (0 errors)
    - 신규 warning 0
    - //260530 hbk Phase 39.2 D-G4 마커 포함
  </acceptance_criteria>
  <done>
    IValueConverter 가 IconKey string → Geometry resource lookup 처리. fallback 안전 (resource 미존재 시 null).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: InspectionListView.xaml Geometry 리소스 18 종 + DataTemplate Path 교체</name>
  <read_first>
    - @WPF_Example/UI/ControlItem/InspectionListView.xaml L1-127 (UserControl.Resources + 현재 DataTemplate L43-53)
    - @WPF_Example/UI/ControlItem/IconKeyToGeometryConverter.cs (Task 2 산물)
  </read_first>
  <behavior>
    - Test 1: UserControl.Resources 에 IconKeyToGeometryConverter 인스턴스 (x:Key="iconKeyToGeometry") 1개
    - Test 2: Geometry resources 18 종 등록 (Icon.Recipe / Icon.Sequence / Icon.Action / Icon.Datum / Icon.FAI / Icon.Measurement (fallback) / Icon.Meas.* 12 + Icon.Default = 18)
    - Test 3: DataTemplate DataType="{x:Type local:NodeViewModel}" 가 Image 대신 Path 사용
    - Test 4: Path Data="{Binding IconKey, Converter={StaticResource iconKeyToGeometry}}"
    - Test 5: EditableTextBlock Text="{Binding Name}" 보존 (노드 텍스트 변경 0)
    - Test 6: Image Source="{Binding ImageSource}" 사용처 (Context menu 등) 보존 — DataTemplate 외에서 영향 0
    - Test 7 (W1 fix): CompoundCenterBDistance path 가 objective table 의 정상 path 와 byte-identical (`M12,2A10,10 0 0,1 22,12A10,10 0 0,1 12,22A10,10 0 0,1 2,12A10,10 0 0,1 12,2M12,4A8,8 0 0,0 4,12A8,8 0 0,0 12,20A8,8 0 0,0 20,12A8,8 0 0,0 12,4M2,12L6,16V13H10V11H6V8L2,12Z`)
  </behavior>
  <action>
**파일:** `WPF_Example/UI/ControlItem/InspectionListView.xaml`

주석은 XAML `<!-- -->` 컨벤션 + `260530 hbk Phase 39.2 D-G4` 표기.

**[A] xmlns 확인** — L7 `xmlns:local="clr-namespace:ReringProject.UI"` 가 이미 있음. IconKeyToGeometryConverter 가 동일 namespace 라 추가 xmlns 불필요.

**[B] UserControl.Resources 에 Converter 인스턴스 + Geometry 18 종 추가** — L12 `<UserControl.Resources>` 와 L13 `<Style ...>` **사이에** 다음 블록 삽입:

```xml
        <!-- 260530 hbk Phase 39.2 D-G4 — Tree 노드 아이콘 Geometry resources + converter -->
        <local:IconKeyToGeometryConverter x:Key="iconKeyToGeometry"/>

        <!-- 5 NodeType -->
        <Geometry x:Key="Geometry.Icon.Recipe">M10,4H4C2.89,4 2,4.89 2,6V18A2,2 0 0,0 4,20H20A2,2 0 0,0 22,18V8C22,6.89 21.1,6 20,6H12L10,4Z</Geometry>
        <Geometry x:Key="Geometry.Icon.Sequence">M5,4H7V8H9V4H11V14H9V10H7V14H5V4M13,4H15V8H17V4H19V14H17V10H15V14H13V4M5,16H19V18H5V16M5,20H19V22H5V20Z</Geometry>
        <Geometry x:Key="Geometry.Icon.Action">M12,16L19.36,10.27L21,9L12,2L3,9L4.63,10.27L12,16M12,18.54L4.62,12.81L3,14.07L12,21.07L21,14.07L19.37,12.81L12,18.54Z</Geometry>
        <Geometry x:Key="Geometry.Icon.Datum">M11,2V4.07C7.38,4.53 4.53,7.38 4.07,11H2V13H4.07C4.53,16.62 7.38,19.47 11,19.93V22H13V19.93C16.62,19.47 19.47,16.62 19.93,13H22V11H19.93C19.47,7.38 16.62,4.53 13,4.07V2M11,6.08V8H13V6.09C15.5,6.5 17.5,8.5 17.92,11H16V13H17.91C17.5,15.5 15.5,17.5 13,17.92V16H11V17.91C8.5,17.5 6.5,15.5 6.08,13H8V11H6.09C6.5,8.5 8.5,6.5 11,6.08M12,11A1,1 0 0,0 11,12A1,1 0 0,0 12,13A1,1 0 0,0 13,12A1,1 0 0,0 12,11Z</Geometry>
        <Geometry x:Key="Geometry.Icon.FAI">M19,3H5C3.89,3 3,3.89 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V5C21,3.89 20.1,3 19,3M9,17H7V10H9V17M13,17H11V7H13V17M17,17H15V13H17V17Z</Geometry>

        <!-- Measurement fallback (TypeName 매칭 실패 시 또는 ParamData 미상) -->
        <Geometry x:Key="Geometry.Icon.Measurement">M3,3H21V21H3V3M5,5V19H19V5H5Z</Geometry>

        <!-- 12 Measurement TypeName -->
        <Geometry x:Key="Geometry.Icon.Meas.CircleDiameter">M12,2A10,10 0 0,1 22,12A10,10 0 0,1 12,22A10,10 0 0,1 2,12A10,10 0 0,1 12,2M12,4A8,8 0 0,0 4,12A8,8 0 0,0 12,20A8,8 0 0,0 20,12A8,8 0 0,0 12,4M3,12L21,12Z</Geometry>
        <Geometry x:Key="Geometry.Icon.Meas.EdgeToLineDistance">M4,18H20V20H4V18M12,2L5,12H9V16H15V12H19L12,2Z</Geometry>
        <Geometry x:Key="Geometry.Icon.Meas.PointToLineDistance">M12,7A2,2 0 1,0 12,11A2,2 0 0,0 12,7M4,18H20V20H4V18M12,14L8,18H16L12,14Z</Geometry>
        <Geometry x:Key="Geometry.Icon.Meas.EdgePairDistance">M4,4H20V6H4V4M4,18H20V20H4V18M4,11H20V13H4V11Z</Geometry>
        <Geometry x:Key="Geometry.Icon.Meas.EdgeToLineAngle">M21,21H3L21,3V21M5.83,19H19V5.83L5.83,19Z</Geometry>
        <Geometry x:Key="Geometry.Icon.Meas.ArcEdgeDistance">M2,17H22V19H2V17M12,3A6,6 0 0,1 18,9A6,6 0 0,1 12,15A6,6 0 0,1 6,9A6,6 0 0,1 12,3M12,5A4,4 0 0,0 8,9A4,4 0 0,0 12,13A4,4 0 0,0 16,9A4,4 0 0,0 12,5Z</Geometry>
        <Geometry x:Key="Geometry.Icon.Meas.ArcLineIntersectDistance">M3,11L21,11V13L3,13V11M12,3A8,8 0 0,1 20,11H18A6,6 0 0,0 12,5A6,6 0 0,0 6,11H4A8,8 0 0,1 12,3Z</Geometry>
        <Geometry x:Key="Geometry.Icon.Meas.CompoundAngle">M21,21H3L21,3V21M5.83,19H19V5.83L5.83,19M14,17L17,14L17,17L14,17Z</Geometry>
        <Geometry x:Key="Geometry.Icon.Meas.CompoundCenterCDistance">M12,2A10,10 0 0,1 22,12A10,10 0 0,1 12,22A10,10 0 0,1 2,12A10,10 0 0,1 12,2M12,4A8,8 0 0,0 4,12A8,8 0 0,0 12,20A8,8 0 0,0 20,12A8,8 0 0,0 12,4M22,12L18,8V11H14V13H18V16L22,12Z</Geometry>
        <!-- W1 fix: CompoundCenterBDistance — 중복 segment 제거, 좌측 화살표 mirror 정상화 (CompoundCenterCDistance 와 좌우 대칭) -->
        <Geometry x:Key="Geometry.Icon.Meas.CompoundCenterBDistance">M12,2A10,10 0 0,1 22,12A10,10 0 0,1 12,22A10,10 0 0,1 2,12A10,10 0 0,1 12,2M12,4A8,8 0 0,0 4,12A8,8 0 0,0 12,20A8,8 0 0,0 20,12A8,8 0 0,0 12,4M2,12L6,16V13H10V11H6V8L2,12Z</Geometry>
        <Geometry x:Key="Geometry.Icon.Meas.CompoundShortAxisDistance">M2,12L6,8V11H10V13H6V16L2,12M22,12L18,16V13H14V11H18V8L22,12Z</Geometry>
        <Geometry x:Key="Geometry.Icon.Meas.DualImageEdgeDistance">M3,3H10V10H3V3M14,3H21V10H14V3M3,14H10V21H3V14M14,14H21V21H14V14M11,12L13,12L13,11L11,11L11,12Z</Geometry>

        <!-- Default fallback -->
        <Geometry x:Key="Geometry.Icon.Default">M12,8A4,4 0 0,1 16,12A4,4 0 0,1 12,16A4,4 0 0,1 8,12A4,4 0 0,1 12,8Z</Geometry>

```

**[C] DataTemplate L43-53 교체** — 기존 `<DataTemplate DataType="{x:Type local:NodeViewModel}">` 블록을 다음으로 교체:

```xml
        <!-- 260530 hbk Phase 39.2 D-G4 — NodeType + Measurement TypeName 별 Geometry icon (was Image / ImageSource PNG) -->
        <DataTemplate DataType="{x:Type local:NodeViewModel}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <!-- 260530 hbk Phase 39.2 D-G4: Image PNG → Path Geometry (Material Icons inline path data). Fill = TextElement foreground (theme 호환). -->
                <Path Grid.Column="0" Width="24" Height="24" Margin="0 0 4 0"
                      Stretch="Uniform"
                      Fill="{Binding Path=(TextElement.Foreground), RelativeSource={RelativeSource Self}}"
                      Data="{Binding IconKey, Converter={StaticResource iconKeyToGeometry}}"/>
                <propToolsWpf:EditableTextBlock Grid.Column="1" Text="{Binding Name}" IsEditing="{Binding IsEditing}"/>
            </Grid>
        </DataTemplate>
```

⚠️ **Anti-goal:**
- 노드 텍스트 (Name) 변경 0 — `{Binding Name}` 보존
- Phase 39.1-03 RenderInspectionResultForNode 호출 흐름 변경 0 — XAML SelectionChanged 핸들러 미수정
- TreeView SelectionChanged 핸들러 본문 변경 0 (InspectionListView.xaml.cs 변경 0)
- DataGrid_faiResults / PropertyGrid 변경 0
- ContextMenu MenuItem.Icon 의 `<Image Source="/Resource/save.png">` 등 (L104-120) 변경 0 — 기존 ImageSource 다른 사용처 회귀 0
- HeaderTemplate1/PageHeaderTemplate1/HeaderTemplate2 등 (L56-86) 도 모두 보존 (PropertyGrid 헤더 패턴, NodeViewModel 와 무관)

**색상 fallback note (Fill binding):**
- `(TextElement.Foreground)` RelativeSource Self 는 Path 자신의 inherited foreground 사용
- 미배선 시 black/SystemColor 가 기본 → 다크 테마 회귀 가능 — fallback 으로 `Fill="Black"` 직접 지정도 안전 (단, 다크 테마 호환 X)
- 본 plan 권장: `Fill="{Binding Path=(TextElement.Foreground), RelativeSource={RelativeSource Self}}"` 유지 → 차후 다크 테마 도입 시 자동 호환
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /v:m</automated>
  </verify>
  <acceptance_criteria>
    - `Grep -F "<local:IconKeyToGeometryConverter x:Key=\"iconKeyToGeometry\"/>" matches 1` (W3 fix: -F fixed-string, `{` 도 토큰화 회피)
    - `Grep -F "x:Key=\"Geometry.Icon.Recipe\"" matches 1`
    - `Grep -F "x:Key=\"Geometry.Icon.Sequence\"" matches 1`
    - `Grep -F "x:Key=\"Geometry.Icon.Action\"" matches 1`
    - `Grep -F "x:Key=\"Geometry.Icon.Datum\"" matches 1`
    - `Grep -F "x:Key=\"Geometry.Icon.FAI\"" matches 1`
    - `Grep -F "x:Key=\"Geometry.Icon.Measurement\"" matches 1` (W4 fix: fallback 별도 라인)
    - `Grep -F "x:Key=\"Geometry.Icon.Meas.CircleDiameter\"" matches 1`
    - `Grep -F "x:Key=\"Geometry.Icon.Meas.DualImageEdgeDistance\"" matches 1`
    - `Grep -F "x:Key=\"Geometry.Icon.Meas." in InspectionListView.xaml matches 12` (W4 fix: `Geometry.Icon.Meas.` prefix 의 12 Measurement TypeName, `Geometry.Icon.Measurement` 와는 명확히 별개 — Meas. (dot 포함) 로 검색하여 fallback 과 구분)
    - `Grep -F "x:Key=\"Geometry.Icon.Default\"" matches 1`
    - `Grep -F "Path Grid.Column=\"0\"" matches 1` (was Image)
    - `Grep -F "Data=\"{Binding IconKey, Converter={StaticResource iconKeyToGeometry}}\"" matches 1` (W3 fix: -F — `{` quantifier 토큰화 회피)
    - `Grep -F "<Image Grid.Column=\"0\" Source=\"{Binding ImageSource}\"" matches 0` (교체됨)
    - `Grep -F "{Binding Name}" in InspectionListView.xaml matches at least 1` (EditableTextBlock 보존)
    - ContextMenu MenuItem 보존 (`Grep -F "/Resource/save.png" matches 1`, `Grep -F "/Resource/saveas.png" matches 1`, `Grep -F "/Resource/folder.png" matches 1`)
    - **W1 fix CompoundCenterBDistance path byte-identical 검증:** `Grep -F "M12,2A10,10 0 0,1 22,12A10,10 0 0,1 12,22A10,10 0 0,1 2,12A10,10 0 0,1 12,2M12,4A8,8 0 0,0 4,12A8,8 0 0,0 12,20A8,8 0 0,0 20,12A8,8 0 0,0 12,4M2,12L6,16V13H10V11H6V8L2,12Z" matches 1` (CompoundCenterBDistance path 가 objective table L91 값과 정확히 일치 — 중복 segment 없음)
    - InspectionListView.xaml.cs 변경 0 (file mtime 또는 git diff 비교)
    - msbuild Rebuild Debug/x64 PASS (0 errors)
    - 신규 warning 0
  </acceptance_criteria>
  <done>
    InspectionListView XAML 이 IconKeyToGeometryConverter + 18 Geometry resources 등록 + DataTemplate Path 교체로 NodeType + Measurement TypeName 별 차별화된 아이콘 표시. CompoundCenterBDistance path 가 objective table 정상값과 byte-identical (W1 fix). ContextMenu 및 기타 Image 사용처 회귀 0.
  </done>
</task>

</tasks>

<verification>
**End-to-end smoke check (msbuild PASS 시 자동 충족):**
1. 트리에서 Sequence / Action / FAI / Datum 노드가 각자 다른 Geometry 아이콘 표시 (Plan 05 SIMUL UAT 에서 시각 확인)
2. Measurement 노드는 TypeName 별로 12 종 + Default fallback 아이콘 표시
3. DualImageEdgeDistance (Plan 01 신규 type) 도 별도 아이콘 (2 boxes + arrow)
4. 노드 텍스트 (Name) 그대로 표시 (`{Binding Name}` 변경 0)
5. CompoundCenterBDistance / CompoundCenterCDistance 좌우 대칭 아이콘 (W1 fix)

**회귀 0 가드:**
- Phase 39.1-03 노드 클릭 시 RenderInspectionResultForNode 호출 흐름 변경 0
- TreeView SelectionChanged 핸들러 (InspectionListView.xaml.cs) 변경 0
- DataGrid_faiResults / PropertyGrid 변경 0
- ContextMenu MenuItem.Icon 의 PNG 아이콘 보존
- Node.ImageSource / NodeViewModel.ImageSource 프로퍼티 보존 (다른 사용처 영향 0)
- HasDetectFail / LastFindSucceeded 분기 (Phase 39 WF-02) 변경 0
</verification>

<success_criteria>
- [ ] Task 1: Node.IconKey + NodeViewModel.IconKey proxy → msbuild PASS
- [ ] Task 2: IconKeyToGeometryConverter.cs IValueConverter + csproj 등록 → msbuild PASS
- [ ] Task 3: InspectionListView.xaml Geometry 18 종 + DataTemplate Path 교체 + W1 fix CompoundCenterBDistance path 동기화 → msbuild PASS
- [ ] msbuild Rebuild Debug/x64 0 errors / 0 신규 warnings
- [ ] InspectionListView.xaml.cs 변경 0
- [ ] 노드 텍스트 (Name) 변경 0 — `{Binding Name}` 보존
- [ ] 기존 ImageSource 프로퍼티 보존 — ContextMenu / 다른 사용처 회귀 0
- [ ] 모든 신규/수정 라인에 `//260530 hbk Phase 39.2 D-G4` 또는 `260530 hbk Phase 39.2 D-G4` (XAML) 마커
</success_criteria>

<output>
After completion, create `.planning/phases/39.2-urgent-additions-2-2026-05-30/39.2-04-SUMMARY.md` covering:
- Node.IconKey + NodeViewModel.IconKey proxy 추가 결과 (라인 위치, 반환 키 18 종)
- IconKeyToGeometryConverter.cs 신설 결과 (line count, fallback chain)
- InspectionListView.xaml Geometry 18 종 등록 + DataTemplate Path 교체 결과 + W1 fix CompoundCenterBDistance path byte-identical 검증
- msbuild Rebuild Debug/x64 결과 (errors / warnings)
- 회귀 0 가드 grep 결과 (xaml.cs 변경 0, ContextMenu PNG 보존, ImageSource 프로퍼티 보존, RenderInspectionResultForNode 보존)
- 시각 확인 결과 (Plan 05 SIMUL UAT 에서 — 본 plan 은 빌드 검증까지만 책임)
</output>
