---
phase: 39.2-urgent-additions-2-2026-05-30
plan: 04
status: complete
type: execute
wave: 2
completed: 2026-05-30
tasks_completed: 3
tasks_total: 3
duration_min: 20
commits:
  - 7307d28  # Task 1 Node.IconKey + NodeViewModel.IconKey proxy
  - b855314  # Task 2 IconKeyToGeometryConverter + csproj
  - dfb7ae0  # Task 3 InspectionListView.xaml 18 Geometry + Path DataTemplate
key_files:
  created:
    - WPF_Example/UI/ControlItem/IconKeyToGeometryConverter.cs
  modified:
    - WPF_Example/UI/ControlItem/Node.cs
    - WPF_Example/UI/ControlItem/NodeViewModel.cs
    - WPF_Example/UI/ControlItem/InspectionListView.xaml
    - WPF_Example/DatumMeasurement.csproj
build: PASS
new_warnings: 0
new_errors: 0
---

# Phase 39.2 Plan 04 SUMMARY — D-G4 TreeView Geometry 아이콘 차별화

## 목적
TreeView 노드를 NodeType + Measurement TypeName 기반 Geometry path 로 차별화 — Material Icons SVG path inline + IValueConverter + ResourceDictionary lookup.

## 결과

### Task 1 — Node.IconKey + NodeViewModel.IconKey proxy (commit `7307d28`)

**`Node.cs`** — `ImageSource` setter 직후 `IconKey` getter 추가 (8 NodeType case + Measurement TypeName 분기 + 2 fallback):
- Recipe / Sequence / Action / SubSequence / Datum / FAI → "Icon.{NodeType}"
- Measurement + ParamData is MeasurementBase → "Icon.Meas." + TypeName
- ParamData 미상 → "Icon.Measurement" fallback
- 알 수 없는 NodeType → "Icon.Default"
- 기존 `ImageSource` 프로퍼티 변경 0 (ContextMenu PNG 보존)

**`NodeViewModel.cs`** — `ImageSource` proxy 직후 `IconKey` proxy 추가:
- `public string IconKey { get { return this.Node.IconKey; } }`

### Task 2 — IconKeyToGeometryConverter (commit `b855314`)

**신규**: `WPF_Example/UI/ControlItem/IconKeyToGeometryConverter.cs` (49 줄)
- `class IconKeyToGeometryConverter : IValueConverter`
- `Convert(...)`:
  1. `string key = value as string;` (null/empty → "Icon.Default")
  2. `resourceKey = "Geometry." + key`
  3. `Application.Current.Resources.Contains(resourceKey)` 검사 → 매치 시 Geometry 반환
  4. 폴백 1: `"Geometry.Icon.Default"` lookup
  5. 폴백 2: `null` (Path.Data null 안전)
- `ConvertBack(...)` → `Binding.DoNothing` (one-way only)
- C# 7.2 / Allman brace / `//260530 hbk Phase 39.2 D-G4` marker

**csproj 등록** — `WPF_Example/DatumMeasurement.csproj`:
- `<Compile Include="UI\ControlItem\IconKeyToGeometryConverter.cs" />` (line 306, NodeViewModel.cs 직후)

### Task 3 — InspectionListView.xaml Geometry 18 종 + DataTemplate Path 교체 (commit `dfb7ae0`)

**`InspectionListView.xaml`** `UserControl.Resources` 시작 부분에 추가:
- `<local:IconKeyToGeometryConverter x:Key="iconKeyToGeometry"/>` (converter 인스턴스 1)
- 5 NodeType Geometry resources (Recipe / Sequence / Action / Datum / FAI)
- Measurement fallback Geometry (Icon.Measurement)
- 12 Measurement TypeName Geometry resources:
  - CircleDiameter / EdgeToLineDistance / PointToLineDistance / EdgePairDistance / EdgeToLineAngle
  - ArcEdgeDistance / ArcLineIntersectDistance / CompoundAngle
  - **CompoundCenterCDistance** (우측 화살표) / **CompoundCenterBDistance** (좌측 화살표 mirror — W1 fix path 정상화)
  - CompoundShortAxisDistance / DualImageEdgeDistance (Plan 01 신규 타입 포함)
- Default fallback Geometry (Icon.Default)
- **총 18 Geometry resource keys** (5 NodeType + 1 Meas fallback + 12 Meas TypeName + 1 Icon.Default)

DataTemplate 교체 (L43-53 영역):
- `<Image Grid.Column="0" Source="{Binding ImageSource}" Width="32" Height="32"/>` →
- `<Path Grid.Column="0" Width="24" Height="24" Stretch="Uniform" Fill="{Binding Path=(TextElement.Foreground), RelativeSource={RelativeSource Self}}" Data="{Binding IconKey, Converter={StaticResource iconKeyToGeometry}}"/>`
- `{Binding Name}` 보존 (EditableTextBlock 변경 0)

**W1 fix 검증**: CompoundCenterBDistance path
- 정상값: `M12,2A10,10 0 0,1 22,12...M2,12L6,16V13H10V11H6V8L2,12Z` (좌측 화살표 mirror)
- byte-identical 적용 완료 (Plan 작성 path 와 일치)

## msbuild 결과
- `WPF_Example/DatumMeasurement.csproj` Debug/x64 BUILD **PASS**
- **0 errors**, **0 신규 warnings** (모두 Phase 33 obsolete + MSB3884 baseline)

## 회귀 0 가드
- ✅ `Node.ImageSource` 프로퍼티 (L33-54) 보존 — ContextMenu / 기타 사용처 회귀 0
- ✅ `NodeViewModel.ImageSource` proxy (L106-109) 보존
- ✅ `InspectionListView.xaml.cs` 변경 0 (XAML 만 수정)
- ✅ TreeView SelectionChanged / RenderInspectionResultForNode (Phase 39.1-03) 호출 흐름 변경 0
- ✅ DataGrid_faiResults / PropertyGrid 변경 0
- ✅ ContextMenu MenuItem.Icon PNG 보존 (XAML L104-120 등 별도 영역 변경 0)
- ✅ HeaderTemplate1 / PageHeaderTemplate1 등 PropertyGrid 헤더 보존
- ✅ `{Binding Name}` 보존 — 노드 텍스트 변경 0
- ✅ Phase 39 WF-02 D-04 HasDetectFail / LastFindSucceeded 분기 변경 0

## acceptance criteria

| Criterion | Status |
|-----------|--------|
| Task 1: Node.IconKey getter 8 NodeType + Measurement TypeName + 2 fallback | ✅ |
| Task 1: NodeViewModel.IconKey proxy | ✅ |
| Task 1: ImageSource 프로퍼티 보존 | ✅ |
| Task 1: msbuild PASS | ✅ |
| Task 2: IconKeyToGeometryConverter (IValueConverter) | ✅ |
| Task 2: csproj 등록 | ✅ |
| Task 2: msbuild PASS | ✅ |
| Task 3: 18 Geometry resources + converter 인스턴스 | ✅ |
| Task 3: DataTemplate Path with Data binding + converter | ✅ |
| Task 3: W1 fix CompoundCenterBDistance path byte-identical | ✅ |
| Task 3: InspectionListView.xaml.cs 변경 0 | ✅ |
| Task 3: msbuild PASS | ✅ |
| 모든 신규/수정 라인에 `//260530 hbk Phase 39.2 D-G4` 마커 | ✅ |

## 검증 대기 (Plan 05 SIMUL UAT)
- Sequence / Action / FAI / Datum 노드 시각 차별 확인
- Measurement TypeName 12 종 + DualImageEdgeDistance 별 아이콘 시각 확인
- CompoundCenterB / CompoundCenterC 좌우 대칭 아이콘 확인
- 노드 텍스트 (Name) 그대로 표시 확인
- ContextMenu PNG 아이콘 보존 확인

## Deviation
- 없음 — plan 작성 코드와 byte-identical 적용
