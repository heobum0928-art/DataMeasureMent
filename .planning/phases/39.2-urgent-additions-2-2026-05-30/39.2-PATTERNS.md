---
phase: 39.2-urgent-additions-2-2026-05-30
type: patterns
date: 2026-05-30
mapper: gsd-pattern-mapper
files_classified: 11
analogs_found: 10
no_analog: 1 (NaturalStringComparer — regex IComparer 표준 패턴, 외부 일반론 적용)
---

# Phase 39.2 PATTERNS — 긴급 추가건2 (D-G1~D-G4 패턴 맵)

CONTEXT.md 의 4 항목(D-G1 DualImageEdgeDistance / D-G2 I10 close-point / D-G3 Tree 자연정렬 / D-G4 Tree 아이콘) 별로 신규/수정 파일의 closest analog 와 코드 excerpt 를 지정한다.

## File Classification

| 신규/수정 파일 | 영역 | 역할 | Data flow | Closest Analog | Match |
|----------|-----|-----|-----------|----------------|-------|
| **D-G1-NEW** `WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` | algorithm | Measurement subclass | per-ROI HALCON strip-loop + projection_pl | `EdgeToLineDistanceMeasurement.cs` (구조 동형 + projection_pl 패턴) + `ArcLineIntersectDistanceMeasurement.cs` (다중 ROI 그룹 패턴) | exact (구성 합성) |
| **D-G1-EDIT** `WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs` | algorithm | factory dispatch | switch (typeName) + GetTypeNames[] | 자기 자신 (`MeasurementFactory.cs` 기존 16 case) | exact |
| **D-G1-EDIT** `WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` | sequence | action step (image source helper) | per-meas image 분기 + dual image 로드 | `GrabOrLoadDatumImage` (L308-324) + `TryGrabOrLoadDualDatumImages` (L331-361) | exact (재사용 패턴) |
| **D-G2-EDIT** `WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs` | algorithm | enum 파라미터 + dispatch | string property + ItemsSourceProperty 콤보 + ICustomTypeDescriptor 항상-노출 | 자기 자신 (`MeasureAxis = "X"` 패턴, L121-127) + `CircleDiameterMeasurement` ICustomTypeDescriptor 8 메서드 (L179-211) | exact |
| **D-G2-EDIT** `WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs` | algorithm helper | 직선 교점 헬퍼 신설 (TryComputeBothIntersections) | static bool + out 2점 | `TryIntersectLines` (L926-935) wrapper 패턴 | role-match |
| **D-G3-NEW** `WPF_Example/Utility/NaturalStringComparer.cs` | utility | IComparer<string> | regex 숫자 chunk 분리 | (codebase 내 IComparer 구현 없음 — 표준 패턴 적용) | no-analog |
| **D-G3-EDIT** `WPF_Example/UI/ControlItem/InspectionListViewModel.cs` | UI viewmodel | NodeViewModel children 정렬 | CompositeNode.Children 정렬 + ReloadChildren 호출 | `CreateSequenceNode` (L55-114) + `RebuildTree` (L167-174) + `AddFAINode` (L117-133) | exact |
| **D-G3-EDIT** `WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` | UI code-behind | Add* 후 정렬 트리거 | AddFAINode / AddDatumNode / AddMeasurementNode 직후 sort 호출 | `Btn_AddFAI_Click` (L718-787) + `AddDatumToSequence` (L790-805) + `AddMeasurementToFAI` (L808-829) + `AddShotToSequence` (L833+) | exact |
| **D-G3-EDIT** `WPF_Example/UI/ControlItem/NodeViewModel.cs` | UI viewmodel | Rename → 정렬 hook | OnParamPropertyChanged 의 Name 갱신 분기 끝에 정렬 신호 | `OnParamPropertyChanged` (L210-240) | exact |
| **D-G4-EDIT** `WPF_Example/UI/ControlItem/Node.cs` | UI model | ImageSource switch | NodeType 별 PNG 경로 → 이번에 Path Geometry resource key 로 교체 (또는 신규 IconKey 프로퍼티 추가) | `Node.ImageSource` getter switch (L33-54) | exact (확장 대상) |
| **D-G4-EDIT** `WPF_Example/UI/ControlItem/InspectionListView.xaml` | UI resource | DataTemplate 의 Image → ContentControl + Path | DataTemplate L43-53 — `<Image Source="{Binding ImageSource}" />` 분기 추가 | InspectionListView.xaml L12-127 UserControl.Resources + L43-53 NodeViewModel DataTemplate | exact (구조 확장) |

## Pattern Assignments

### D-G1-A — `DualImageEdgeDistanceMeasurement.cs` (신규)

**Analog 1 (메인 구조):** `WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs`
**Analog 2 (다중 ROI 그룹 + Edge 파라미터 명명 규칙):** `Measurements/ArcLineIntersectDistanceMeasurement.cs`

**Imports/헤더 패턴** (EdgeToLineDistance L1-8):
```csharp
//260530 hbk Phase 39.2 D-G1 — Bottom E5 DualImage FAI Measurement
using System.Collections.Generic;
using HalconDotNet;
using PropertyTools.DataAnnotations;
using ReringProject.Halcon.Algorithms;
using ReringProject.Halcon.Models;

namespace ReringProject.Sequence
{
    public class DualImageEdgeDistanceMeasurement : MeasurementBase, IDatumOriginConsumer
    {
        public override string TypeName { get { return "DualImageEdgeDistance"; } }
```
→ **그대로 차용:** ReringProject.Sequence namespace + MeasurementBase 상속 + IDatumOriginConsumer 구현 (Datum-relative 측정).

**ROI 그룹 명명 — per-ROI 7+5 파라미터 (ArcLineIntersect L23-111 동형, 단 2 그룹: Point/Line):**
```csharp
// Phase 39.2 D-G1-05: ROI A (점 형태, TeachingImagePath)
[Category("PointROI|ROI")]
public double PointROI_Row { get; set; }
public double PointROI_Col { get; set; }
public double PointROI_Phi { get; set; }
public double PointROI_Length1 { get; set; }
public double PointROI_Length2 { get; set; }

// Phase 39.2 D-G1-04: PointROI per-ROI Edge 파라미터 (EdgeToLineDistance L32-46 7개 동형)
[Category("PointROI|Edge")]
public int PointROI_EdgeThreshold { get; set; } = 10;
public double PointROI_Sigma { get; set; } = 1.0;
public int PointROI_EdgeSampleCount { get; set; } = 20;
public int PointROI_EdgeTrimCount { get; set; } = 10;
[ItemsSourceProperty(nameof(EdgePolarityList))]
public string PointROI_EdgePolarity { get; set; } = "DarkToLight";
[ItemsSourceProperty(nameof(EdgeDirectionList))]
public string PointROI_EdgeDirection { get; set; } = "TtoB";
[ItemsSourceProperty(nameof(EdgeSelectionList))]
public string PointROI_EdgeSelection { get; set; } = "All";

// LineROI (TeachingImagePath_Vertical) — 동형 7+5 (생략)
```

**ComboBox 옵션 래퍼 (EdgeToLineDistance L49-54 동형):**
```csharp
[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> EdgeDirectionList { get { return EdgeOptionLists.Directions; } }
[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> EdgePolarityList { get { return EdgeOptionLists.FAIPolarities; } }
[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> EdgeSelectionList { get { return EdgeOptionLists.Selections; } }
```

**IDatumOriginConsumer transient 6 필드** — EdgeToLineDistance L69-95 또는 ArcLineIntersect L131-155 동형 (Browsable(false) + JsonIgnore 3중 attr):
```csharp
[System.ComponentModel.Browsable(false)]
[PropertyTools.DataAnnotations.Browsable(false)]
[Newtonsoft.Json.JsonIgnore]
public double DatumOriginRow { get; set; }
// ... DatumOriginCol / DatumAngleRad / DatumAngle2Rad / DatumDetectedCircleRow / DatumDetectedCircleCol
```

**TryExecute 시그니처** (MeasurementBase L69-75 강제 + EdgeToLineDistance L99-105):
```csharp
public override bool TryExecute(
    HImage image,                  // ← 의의 변경: 본 타입은 DualImage 이므로 image 는 **무시** (Action_FAIMeasurement 가 imageH / imageV 별도 전달)
    HTuple datumTransform,
    double pixelResolution,
    out double resultValue,
    out string error,
    out List<EdgeInspectionOverlay> overlays)
```

⚠️ **구조 결정 필요 (planner lock):** TryExecute 시그니처는 abstract 강제. 두 HImage 를 받는 분기는:
- 옵션 A: `TryExecuteDual(HImage imgH, HImage imgV, ...)` 신규 메서드 + Action_FAIMeasurement 가 본 타입 감지 시 분기
- 옵션 B: 본 타입 인스턴스에 `HImage RuntimeImageA / RuntimeImageB` transient 속성 주입 후 TryExecute 가 그것 참조 (IDatumOriginConsumer 패턴 동형)
→ 옵션 B 가 abstract 시그니처 변경 0 + Action_FAIMeasurement L237 호출 회귀 0 (Plan-phase lock 권장 옵션).

**핵심 알고리즘 — 양 이미지 각자 fit_line → midpoint/line → projection_pl 거리** (EdgeToLineDistance L119-222 강력 차용):
```csharp
var svc = new VisionAlgorithmService();

// (1) PointROI — TeachingImagePath 이미지에서 fit_line → midpoint
double pa_r1, pa_c1, pa_r2, pa_c2;
if (!svc.TryFitLine(imageA,
    PointROI_Row, PointROI_Col, PointROI_Phi, PointROI_Length1, PointROI_Length2,
    datumTransform,
    PointROI_EdgeSampleCount, PointROI_EdgeTrimCount, PointROI_Sigma, PointROI_EdgeThreshold,
    PointROI_EdgeDirection, PointROI_EdgePolarity,
    out pa_r1, out pa_c1, out pa_r2, out pa_c2, out error,
    PointROI_EdgeSelection))
{
    return false;
}
double pointRow = (pa_r1 + pa_r2) / 2.0;  // D-G1-02 midpoint 고정
double pointCol = (pa_c1 + pa_c2) / 2.0;

// (2) LineROI — TeachingImagePath_Vertical 이미지에서 fit_line → 라인 그대로
double lb_r1, lb_c1, lb_r2, lb_c2;
if (!svc.TryFitLine(imageB, ... LineROI_*, out lb_r1, out lb_c1, out lb_r2, out lb_c2, out error,
    LineROI_EdgeSelection))
{
    return false;
}

// (3) projection_pl(point → line) — EdgeToLineDistance L193-204 동형
try {
    HTuple prRow, prCol;
    HOperatorSet.ProjectionPl(pointRow, pointCol, lb_r1, lb_c1, lb_r2, lb_c2, out prRow, out prCol);
    double footRow = prRow.D;
    double footCol = prCol.D;
    double dRow = pointRow - footRow;
    double dCol = pointCol - footCol;
    double distPx = System.Math.Sqrt(dRow*dRow + dCol*dCol);
    resultValue = distPx * pixelResolution;  // mm
}
catch {
    error = "projection_pl 실패";
    return false;
}
```

**Overlay 생성 — EdgeToLineDistance L240-300 동형 (FAI-Edge1/Edge2/DistLine):**
```csharp
// 검사 결과 overlay 3종 — RoiId 컨벤션 강제 (HalconDisplayService 분기 충족)
overlays.Add(new EdgeInspectionOverlay {
    RoiId = "FAI-Edge1",  // ← Point ROI 검출 라인 (StartsWith("FAI-Edge") → 녹/적 + -OK/-NG suffix)
    LineRow1 = pa_r1, LineColumn1 = pa_c1,
    LineRow2 = pa_r2, LineColumn2 = pa_c2,
    Points = new List<EdgeInspectionPoint> {
        new EdgeInspectionPoint { Row = pointRow, Column = pointCol }  // ← 중점 십자 마커
    }
});
overlays.Add(new EdgeInspectionOverlay {
    RoiId = "FAI-Edge2",  // ← Line ROI 검출 라인
    LineRow1 = lb_r1, LineColumn1 = lb_c1,
    LineRow2 = lb_r2, LineColumn2 = lb_c2
});
overlays.Add(new EdgeInspectionOverlay {
    RoiId = "FAI-DistLine",  // ← projection_pl 거리선 (청록, HalconDisplayService L181 분기 충족)
    LineRow1 = footRow, LineColumn1 = footCol,
    LineRow2 = pointRow, LineColumn2 = pointCol
});
```

**HALCON 에러 정책** (memory feedback_halcon_measurepos + EdgeToLineDistance L193-204):
- 모든 HOperatorSet 호출은 try { } catch { return false; } 또는 catch 후 error 문자열 기록
- TryFitLine 은 svc.TryFitLine wrapper 사용 (단일 MeasurePos 호출 금지 — strip-loop 누적이 wrapper 내부에서 처리됨)
- SetColor 표준 색상명만 (memory feedback_halcon_setcolor_invalid_names — overlay는 RoiId 만 지정, 색상은 HalconDisplayService 가 RoiId prefix 로 분기)

---

### D-G1-B — `MeasurementFactory.cs` (수정)

**Analog:** 자기 자신 (L14-49 switch + L53-67 GetTypeNames).

**Switch 케이스 추가** (L44-45 패턴 동형):
```csharp
case "CompoundShortAxisDistance":
    return new CompoundShortAxisDistanceMeasurement(owner);
case "DualImageEdgeDistance":  //260530 hbk Phase 39.2 D-G1
    return new DualImageEdgeDistanceMeasurement(owner);  //260530 hbk Phase 39.2 D-G1
default:
    return null;
```

**GetTypeNames 배열 추가** (L66 끝에):
```csharp
return new string[]
{
    "CircleDiameter",
    ...
    "CompoundShortAxisDistance",
    "DualImageEdgeDistance"  //260530 hbk Phase 39.2 D-G1 — Bottom E5
};
```

⚠️ **anti-goal lock (CONTEXT D-G1 마지막 줄):** 기존 15 case + 10 GetTypeNames 원소 순서/문자 변경 0.

---

### D-G1-C — `Action_FAIMeasurement.cs` (수정 — DualImage 분기 helper 신설)

**Analog 1 (헬퍼 함수):** `Action_FAIMeasurement.cs` L308-324 `GrabOrLoadDatumImage(DatumConfig)` — 1-image 로드 + Simul/grab fallback.
**Analog 2 (DualImage 로드):** `Action_FAIMeasurement.cs` L331-361 `TryGrabOrLoadDualDatumImages(DatumConfig, out HImage, out HImage)` — 2-image 로드 패턴.

**FAI DualImage helper 신설 — TryGrabOrLoadFaiDualImages (L331-361 동형):**

```csharp
//260530 hbk Phase 39.2 D-G1-06 — DualImageEdgeDistance 측정용 양 이미지 로드.
//  imageA: ShotParam.SimulImagePath (1차) — PointROI 검출 (datum.TeachingImagePath 와 동일 의미)
//  imageB: meas.TeachingImagePath_Vertical (2차) — LineROI 검출
//  P37 TryGrabOrLoadDualDatumImages 동형 패턴 — 경로 빈/파일없음 → false + 로그.
private bool TryGrabOrLoadFaiDualImages(MeasurementBase meas, out HImage imageA, out HImage imageB)
{
    imageA = null;
    imageB = null;
    if (ShotParam == null) return false;
    // pathA: 기존 Shot SimulImagePath 재사용 (1-image FAI 측정 baseline 회귀 0)
    string pathA = ShotParam.SimulImagePath;
    // pathB: measurement 에 TeachingImagePath_Vertical 추가 필요 (Plan-phase lock)
    string pathB = (meas as DualImageEdgeDistanceMeasurement)?.TeachingImagePath_Vertical;

    if (string.IsNullOrEmpty(pathA) || !File.Exists(pathA)) {
        Logging.PrintErrLog((int)ELogType.Error, "[FAI DualImage] PointROI 이미지 경로 비어 있거나 파일 없음");
        return false;
    }
    if (string.IsNullOrEmpty(pathB) || !File.Exists(pathB)) {
        Logging.PrintErrLog((int)ELogType.Error, "[FAI DualImage] LineROI 이미지 경로 비어 있거나 파일 없음");
        return false;
    }
    try { imageA = new HImage(pathA); } catch { imageA = null; }
    try { imageB = new HImage(pathB); } catch { imageB = null; }
    if (imageA == null || imageB == null) {
        if (imageA != null) { try { imageA.Dispose(); } catch { } }
        if (imageB != null) { try { imageB.Dispose(); } catch { } }
        imageA = null; imageB = null;
        return false;
    }
    return true;
}
```

**EStep.Measure 의 meas.TryExecute 호출 분기** — L232-243 영역 위에 분기 추가:

```csharp
// 기존 (L237)
ok = meas.TryExecute(image, transform, fai.PixelResolutionX, out resultValue, out measError, out measOverlays);

// Phase 39.2 D-G1: DualImage 타입 분기 (Plan-phase lock — 옵션 B 패턴 권장)
if (meas is DualImageEdgeDistanceMeasurement dualMeas) {
    HImage imgA = null, imgB = null;
    try {
        if (TryGrabOrLoadFaiDualImages(meas, out imgA, out imgB)) {
            dualMeas.RuntimeImageA = imgA;  // ← transient property, TryExecute 가 image 무시하고 RuntimeImageA/B 사용
            dualMeas.RuntimeImageB = imgB;
            ok = meas.TryExecute(image, transform, fai.PixelResolutionX, out resultValue, out measError, out measOverlays);
        } else {
            ok = false; resultValue = 0; measError = "DualImage 로드 실패"; measOverlays = null;
        }
    } finally {
        if (imgA != null) { try { imgA.Dispose(); } catch { } }
        if (imgB != null) { try { imgB.Dispose(); } catch { } }
        dualMeas.RuntimeImageA = null;
        dualMeas.RuntimeImageB = null;
    }
} else {
    // 기존 1-image 경로 (회귀 0)
    ok = meas.TryExecute(image, transform, fai.PixelResolutionX, out resultValue, out measError, out measOverlays);
}
```

⚠️ **anti-goal (CONTEXT D-G1 마지막 줄):** EStep.Measure D-01 게이트 (`IsDatumFailed`, L179-188) 변경 0 + L283 LastOverlays write-back 변경 0.

---

### D-G2-A — `ArcLineIntersectDistanceMeasurement.cs` (수정 — enum 파라미터)

**Analog:** 자기 자신 + `CircleDiameterMeasurement` ICustomTypeDescriptor.

**ParamBase 직렬화 제약 (ParamBase.cs L331-365):**
- switch case 만 — `Int32 / Double / String / Boolean / Rect / Line / Circle / PropertyItem[] / ModelFinderViewModel`
- **enum 직접 저장 불가** → **string 으로 저장** (EdgeToLineDistance `string EdgeDirection = "TtoB"` 패턴, L36-46 동형).

**파라미터 추가 — IntersectionPointSelection 신규** (ArcLineIntersect L121-127 MeasureAxis 패턴 동형):
```csharp
// Phase 39.2 D-G2-01: 교점 선택 — Far(기존 default) 또는 Close(가까운 점)
[Category("Measurement|Measure")]  //260530 hbk Phase 39.2 D-G2
[System.ComponentModel.Description("교점 선택 — Far: Datum 수직선에서 더 먼 점, Close: 더 가까운 점")]
[ItemsSourceProperty(nameof(IntersectionPointSelectionList))]
public string IntersectionPointSelection { get; set; } = "Far";  // ← default Far = INI 하위호환

[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> IntersectionPointSelectionList { get { return new List<string> { "Far", "Close" }; } }
```

⚠️ INI 회귀 0 검증: 기존 INI 파일에 IntersectionPointSelection 키 없음 → `ToString()` 빈/null → default = "Far" 유지 → 기존 측정값 byte-identical.

**TryComputeBothIntersections — 분리 X (이미 ArcLineIntersect L226-245 에 교점1/교점2 모두 산출 중):**
- 코드 보면 이미 `int1Row, int1Col` (교점1) + `int2Row, int2Col` (교점2) 양쪽 산출 후 L247-261 에서 `measurePointRow/Col` 보정 시 교점2 만 사용
- D-G2 는 IntersectionPointSelection==Close 일 때 교점1 을 사용하는 분기 추가만 필요

**분기 추가 (L247-261 보정 블록 교체):**
```csharp
// Phase 39.2 D-G2-01: IntersectionPointSelection 분기 — Far(default) / Close
double measurePointRow, measurePointCol;
bool useClose = (IntersectionPointSelection == "Close");  //260530 hbk Phase 39.2 D-G2
if (MeasureAxis == "X")
{
    if (useClose) {
        measurePointCol = int1Col;  // ← Close = 교점1 (수직 ROI 가까운 쪽)
        measurePointRow = (int1Row + int2Row) / 2.0;
    } else {
        measurePointCol = int2Col;  // ← Far = 기존 (회귀 0)
        measurePointRow = (int1Row + int2Row) / 2.0;
    }
}
else
{
    if (useClose) {
        measurePointRow = int1Row;
        measurePointCol = (int1Col + int2Col) / 2.0;
    } else {
        measurePointRow = int2Row;  // ← Far = 기존
        measurePointCol = (int1Col + int2Col) / 2.0;
    }
}
```

**ICustomTypeDescriptor 8 메서드 보일러 (CircleDiameterMeasurement L179-211 그대로 차용):**
- 단, ArcLineIntersect 는 모든 파라미터 항상 노출이라 GetProperties 필터링 없이 단순 보일러 (BuildFilteredProperties 도 hideFunc null 반환).
- 또는 ICustomTypeDescriptor 미도입 — `ItemsSourceProperty` + `[Browsable(false)] List<string>` 콤보만으로도 충분 (EdgeToLineDistance L36-54 패턴은 ICustomTypeDescriptor 없이도 PropertyGrid 정상 노출).
→ **planner lock 권장: 옵션 A — ICustomTypeDescriptor 미도입, ItemsSourceProperty 콤보만 추가** (코드 변경 최소).

---

### D-G2-B — `VisionAlgorithmService.cs` (수정 — 헬퍼 신설 옵션)

**Analog:** `TryIntersectLines` L926-935 wrapper 패턴.

**TryComputeBothIntersections 신설 — 옵션 (planner 결정):**
ArcLineIntersect 가 이미 인라인으로 양 교점 산출 중 → 별도 헬퍼 불필요. CONTEXT.md D-G2-01 의 "TryComputeBothIntersections 헬퍼 신설" 은 **인라인 분기 단순화 옵션**으로만 의미.

**옵션 A: 헬퍼 신설 안 함 (권장)** — ArcLineIntersect 내부 분기만으로 충족, VisionAlgorithmService 변경 0.
**옵션 B: 헬퍼 신설** — `TryIntersectLines` L926-935 동형 패턴 (static bool + out 다중):
```csharp
//260530 hbk Phase 39.2 D-G2 — 두 짝의 직선 교점 (4-ROI ArcLineIntersect 용 wrapper)
public static bool TryComputeBothIntersections(
    double a1r1, double a1c1, double a1r2, double a1c2,   // line A1
    double b1r1, double b1c1, double b1r2, double b1c2,   // line B1
    double a2r1, double a2c1, double a2r2, double a2c2,   // line A2
    double b2r1, double b2c1, double b2r2, double b2c2,   // line B2
    out double int1Row, out double int1Col,
    out double int2Row, out double int2Col)
{
    int1Row = int1Col = int2Row = int2Col = 0;
    if (!TryIntersectLines(a1r1, a1c1, a1r2, a1c2, b1r1, b1c1, b1r2, b1c2, out int1Row, out int1Col))
        return false;
    if (!TryIntersectLines(a2r1, a2c1, a2r2, a2c2, b2r1, b2c1, b2r2, b2c2, out int2Row, out int2Col))
        return false;
    return true;
}
```

---

### D-G3-A — `NaturalStringComparer.cs` (신규)

**No analog in codebase** — IComparer<string> 구현은 새로 작성. 표준 패턴 차용.

**파일 위치 결정:** `WPF_Example/Utility/NaturalStringComparer.cs` — `Logging.cs`, `Ini.cs` 와 동일 폴더 (모든 Utility 의 표준 위치).

**구조:**
```csharp
//260530 hbk Phase 39.2 D-G3 — Tree 노드 자연정렬 비교자
using System.Collections.Generic;
using System.Text.RegularExpressions;

namespace ReringProject.Utility
{
    /// <summary>
    /// "Shot10" > "Shot2" (사전식 = Shot10 < Shot2 회피).
    /// regex 로 숫자 chunk 와 텍스트 chunk 를 분리한 후 chunk 단위로 비교한다.
    /// 숫자 chunk 는 int 로 비교, 텍스트 chunk 는 OrdinalIgnoreCase 로 비교.
    /// </summary>
    public class NaturalStringComparer : IComparer<string>
    {
        private static readonly Regex _chunkRegex = new Regex(@"(\d+|\D+)", RegexOptions.Compiled);

        public int Compare(string x, string y)
        {
            if (x == null && y == null) return 0;
            if (x == null) return -1;
            if (y == null) return 1;

            var xChunks = _chunkRegex.Matches(x);
            var yChunks = _chunkRegex.Matches(y);
            int n = System.Math.Min(xChunks.Count, yChunks.Count);
            for (int i = 0; i < n; i++)
            {
                string xc = xChunks[i].Value;
                string yc = yChunks[i].Value;
                int cmp;
                if (char.IsDigit(xc[0]) && char.IsDigit(yc[0]))
                {
                    int xi, yi;
                    if (int.TryParse(xc, out xi) && int.TryParse(yc, out yi))
                        cmp = xi.CompareTo(yi);
                    else
                        cmp = string.Compare(xc, yc, System.StringComparison.OrdinalIgnoreCase);
                }
                else
                {
                    cmp = string.Compare(xc, yc, System.StringComparison.OrdinalIgnoreCase);
                }
                if (cmp != 0) return cmp;
            }
            return xChunks.Count.CompareTo(yChunks.Count);
        }
    }
}
```

**컨벤션 적용 (CLAUDE.md):**
- PascalCase / `Try` prefix 없음 (CompareTo 표준 시그니처)
- C# 7.2 / .NET 4.8 (nullable refs 미사용)
- 주석: //260530 hbk Phase 39.2 D-G3

---

### D-G3-B — `InspectionListViewModel.cs` (수정 — 정렬 메서드 추가)

**Analog:** `CreateSequenceNode` (L55-114) + `RebuildTree` (L167-174) + `AddFAINode` (L117-133).

**정렬 헬퍼 신설 — SortNodeChildren:**
```csharp
//260530 hbk Phase 39.2 D-G3 — 자연정렬 (지정 레벨 children 만 정렬)
private static readonly NaturalStringComparer _comparer = new NaturalStringComparer();

public static void SortNodeChildren(NodeViewModel parent)
{
    if (parent == null) return;
    var list = parent.Children;
    if (list == null || list.Count <= 1) return;
    // ObservableCollection 직접 Sort 불가 → list 생성 → Clear → 정렬 후 재삽입
    var sorted = new List<NodeViewModel>(list);
    sorted.Sort((a, b) => _comparer.Compare(a.Name, b.Name));
    list.Clear();
    foreach (var item in sorted) list.Add(item);
}

//260530 hbk Phase 39.2 D-G3 — 전 트리 재정렬 (RebuildTree 후 또는 다단계 정렬용)
public void SortAllLevels()
{
    SortRecursive(RootModel);
}
private static void SortRecursive(NodeViewModel node)
{
    if (node == null) return;
    SortNodeChildren(node);
    foreach (var child in node.Children)
        SortRecursive(child);
}
```

**`AddFAINode` / `AddDatumNode` / `AddMeasurementNode` 끝에 정렬 호출 추가** (L132/146/161 직전):
```csharp
public void AddFAINode(NodeViewModel actionNode, FAIConfig fai, ESequence seqID, EAction actID) {
    // ... 기존 본문 (L120-132)
    cn.Add(faiVm);
    SortNodeChildren(actionNode);  //260530 hbk Phase 39.2 D-G3-03 — Add 시 즉시 정렬
}
```

**`RebuildTree` 끝에 추가** (L173 끝):
```csharp
public void RebuildTree() {
    // ... 기존 L168-172
    HookSequenceDisplayNameUpdates();
    SortAllLevels();  //260530 hbk Phase 39.2 D-G3 — recipe 로드 후 전 트리 자연정렬
}
```

⚠️ **anti-goal (CONTEXT D-G3 마지막 줄):** ParamBase Save/Load INI 직렬화 순서 변경 0 — 본 정렬은 ViewModel 레이어에서만 동작, ShotConfig.FAIList / FAIConfig.Measurements 등 ParamBase 컬렉션 순서 변경 0.

---

### D-G3-C — `InspectionListView.xaml.cs` (수정 — Add 후 정렬 트리거)

**Analog:** `Btn_AddFAI_Click` (L718-787), `AddDatumToSequence` (L790-805), `AddMeasurementToFAI` (L808-829), `AddShotToSequence` (L833+).

InspectionListViewModel.AddFAINode 가 내부에서 SortNodeChildren 호출 → InspectionListView.xaml.cs 자체 수정 사실상 불필요 (Plan-phase 결정).

**Optional refresh — Add 후 IsExpanded 유지 + 정렬 결과 가시화:**
- 이미 L804 `seqNode.IsExpanded = true` / L828 `faiNode.IsExpanded = true` 호출 중 → 정렬이 cascade 됨.
- ListView_Loaded (L11) 의 트리 expansion + RebuildTree → SortAllLevels 이 RebuildTree 안에 포함되므로 일관성 보장.

⚠️ **planner lock:** InspectionListView.xaml.cs 직접 수정 0 (정렬은 ViewModel 책임).

---

### D-G3-D — `NodeViewModel.cs` (수정 — Rename → 정렬 hook)

**Analog:** `OnParamPropertyChanged` (L210-240) — 기존 Name 갱신 분기.

**변경 위치 (L236-239 — newName 갱신 분기):**
```csharp
if (newName != null && this.Node != null) {
    this.Node.Name = newName;
    RaisePropertyChanged("Name");
    // Phase 39.2 D-G3-03: Rename 후 부모 children 재정렬 (즉시 트리 정렬 갱신)
    if (this.Parent != null) {  //260530 hbk Phase 39.2 D-G3
        InspectionListViewModel.SortNodeChildren(this.Parent);  //260530 hbk Phase 39.2 D-G3
    }
}
```

⚠️ **anti-goal:** Phase 39.1-03 RenderInspectionResultForNode 호출 흐름 변경 0 + TreeView SelectionChanged 핸들러 변경 0 + DataGrid_faiResults / PropertyGrid 변경 0 (CONTEXT D-G3 마지막).

---

### D-G4-A — `Node.cs` (수정 — Geometry resource key)

**Analog:** 자기 자신 — `ImageSource` getter switch L33-54.

**현재 코드 (L33-54):**
```csharp
public string ImageSource {
    get {
        switch (NodeType) {
            case ENodeType.Recipe:      return "/Resource/folder.png";
            case ENodeType.Sequence:    return "/Resource/process.png";
            case ENodeType.Action:      return "/Resource/layout.png";
            case ENodeType.SubSequence: return "/Resource/split.png";
            case ENodeType.FAI:         return "/Resource/chart.png";
            case ENodeType.Datum:       return "/Resource/layout.png";
            case ENodeType.Measurement: return "/Resource/chart.png";
        }
        return "/Resource/process.png";
    }
    set { }
}
```

**옵션 A (권장 — 기존 ImageSource 보존 + IconKey 신규 프로퍼티):**
```csharp
// Phase 39.2 D-G4-04: NodeType + Measurement TypeName 기반 Geometry ResourceKey
public string IconKey {  //260530 hbk Phase 39.2 D-G4
    get {
        switch (NodeType) {
            case ENodeType.Recipe:      return "Icon.Recipe";
            case ENodeType.Sequence:    return "Icon.Sequence";
            case ENodeType.Action:      return "Icon.Action";
            case ENodeType.Datum:       return "Icon.Datum";
            case ENodeType.FAI:         return "Icon.FAI";
            case ENodeType.Measurement:
                // TypeName 별 추가 분기 (D-G4-03 12종)
                var meas = ParamData as ReringProject.Sequence.MeasurementBase;
                if (meas != null) return "Icon.Meas." + meas.TypeName;
                return "Icon.Measurement";
        }
        return "Icon.Default";
    }
}
```

⚠️ **회귀 회피:** 기존 `ImageSource` 프로퍼티 유지 → 옛 DataTemplate(L43-53 `<Image Source="{Binding ImageSource}" />`) 의 Image 컨트롤도 동작 유지 (옵션 A) — Plan-phase 가 DataTemplate 을 ContentControl + Path 로 전환 결정 시에만 ImageSource 빈 문자열/null 처리 결정.

---

### D-G4-B — `InspectionListView.xaml` (수정 — DataTemplate + Geometry resources)

**Analog:** 자기 자신 — L43-53 NodeViewModel DataTemplate + L12-127 UserControl.Resources.

**현재 DataTemplate (L43-53):**
```xml
<DataTemplate DataType="{x:Type local:NodeViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Image Grid.Column="0" Source="{Binding ImageSource}" Width="32" Height="32" Margin="0 0 4 0"/>
        <propToolsWpf:EditableTextBlock Grid.Column="1" Text="{Binding Name}" IsEditing="{Binding IsEditing}"/>
    </Grid>
</DataTemplate>
```

**변경 패턴 — Image 를 ContentControl + Path 로 교체:**
```xml
<!-- Phase 39.2 D-G4: NodeType + Measurement TypeName 별 Geometry icon -->
<UserControl.Resources>
    <!-- 기존 리소스 ... -->

    <!-- Phase 39.2 D-G4-03: Geometry 리소스 (Material Icons SVG path 발췌, monochrome) -->
    <Geometry x:Key="Geometry.Icon.Sequence">M9,5A4,4 0 0,1 13,9V14L17,18V21H3V18L7,14V9A4,4 0 0,1 9,5Z</Geometry>
    <Geometry x:Key="Geometry.Icon.Datum">M12,2L4,5V11C4,16.55 7.84,21.74 12,23C16.16,21.74 20,16.55 20,11V5L12,2Z</Geometry>
    <Geometry x:Key="Geometry.Icon.FAI">M3,3H21V21H3V3M5,5V19H19V5H5M7,7H17V9H7V7M7,11H17V13H7V11M7,15H14V17H7V15Z</Geometry>
    <!-- ... 12 Measurement TypeName 별 Geometry 추가 ... -->

    <!-- DataTemplateSelector 인스턴스 -->
    <local:TreeNodeIconTemplateSelector x:Key="iconSelector"/>

    <!-- 수정 DataTemplate -->
    <DataTemplate DataType="{x:Type local:NodeViewModel}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <!-- 옵션: ContentControl + Resource key 동적 lookup -->
            <Path Grid.Column="0" Width="20" Height="20" Margin="0 0 4 0"
                  Stretch="Uniform"
                  Fill="{DynamicResource MaterialDesignBody}"
                  Data="{Binding IconKey, Converter={StaticResource KeyToGeometryConverter}}"/>
            <propToolsWpf:EditableTextBlock Grid.Column="1" Text="{Binding Name}" IsEditing="{Binding IsEditing}"/>
        </Grid>
    </DataTemplate>
</UserControl.Resources>
```

**대안 — DataTemplateSelector (CONTEXT D-G4-04 권장):**
- DataTemplateSelector 신설 (`TreeNodeIconTemplateSelector.cs`) → NodeType + Measurement TypeName 분기 → 각 케이스별 DataTemplate 반환
- HierarchicalDataTemplate ItemContainerStyle 의 ContentTemplateSelector 로 등록
- 12 Measurement + 5 NodeType = 17 DataTemplate 자원 등록 (XAML)

**또는 IValueConverter 패턴 (단순 — 권장):**
- `KeyToGeometryConverter : IValueConverter` 신설 — IconKey string → Application.Current.Resources[$"Geometry.{key}"] 반환
- DataTemplate 단일 + Path Data binding 만으로 17 분기 처리 (코드 변경 1 + XAML 분기 0)

⚠️ **anti-goal (CONTEXT D-G4 마지막 줄):**
- 트리 노드 텍스트 (FAIName / MeasurementName) 변경 0 — Text binding (`{Binding Name}`) 보존
- Phase 39.1-03 RenderInspectionResultForNode 호출 흐름 변경 0
- TreeView SelectionChanged 핸들러 (InspectionListView) 본문 변경 0
- DataGrid_faiResults / PropertyGrid 변경 0

---

## Cross-cutting Patterns

### 1. HALCON strip-loop fit_line (D-G1 핵심 — memory feedback_halcon_measurepos)
**Source:** `VisionAlgorithmService.TryFitLine` (호출자에서 단일 MeasurePos 금지). EdgeToLineDistance L125-131 / ArcLineIntersect L175-184 동형 호출 패턴.
**적용:** D-G1 DualImageEdgeDistance 의 PointROI / LineROI 각 fit_line — strip-loop 가 wrapper 내부에서 처리. 호출자는 EdgeSampleCount / EdgeTrimCount 만 전달.

### 2. projection_pl 수직거리 (D-G1)
**Source:** EdgeToLineDistanceMeasurement L193-204.
```csharp
try {
    HTuple prRow, prCol;
    HOperatorSet.ProjectionPl(pRow, pCol, axisR1, axisC1, axisR2, axisC2, out prRow, out prCol);
    footRow = prRow.D; footCol = prCol.D; footOk = true;
} catch { /* foot=점 자신 유지 */ }
```
**적용:** D-G1 DualImage 의 거리 계산 — Point 를 Line 으로 정사영, distPx = sqrt((pR-fR)^2 + (pC-fC)^2).

### 3. ParamBase 자동 직렬화 — string-only enum 패턴 (D-G2)
**Source:** ParamBase.cs L331-365 switch — Int32/Double/String/Boolean/Rect/Line/Circle/PropertyItem[]/ModelFinderViewModel 만.
**적용:** D-G2 `IntersectionPointSelection` 은 enum 대신 string ("Far" / "Close") + ItemsSourceProperty 콤보 — INI 자동 직렬화 (case "String").

### 4. IDatumOriginConsumer transient 6 필드 (D-G1)
**Source:** EdgeToLineDistance L69-95 / ArcLineIntersect L131-155.
**3중 attribute 패턴:**
```csharp
[System.ComponentModel.Browsable(false)]
[PropertyTools.DataAnnotations.Browsable(false)]
[Newtonsoft.Json.JsonIgnore]
public double DatumOriginRow { get; set; }
```
- PropertyGrid 노출 차단 (PropertyTools.Browsable + System.ComponentModel.Browsable 동시 — PropertyTools 가 system 도 본다)
- Newtonsoft.Json 직렬화 차단 (JSON recipe 저장 회피)
- ParamBase reflection 의 `case "Double"` 은 catch — INI 에 0 으로 저장됨 (DatumConfig 와 동일 수용, runtime 주입으로 덮어쓰기)

### 5. ItemsSourceProperty + List<string> 콤보 (D-G2)
**Source:** EdgeToLineDistance L36-46 (EdgeDirection/Polarity/Selection) + L49-54 (List 래퍼).
```csharp
[ItemsSourceProperty(nameof(EdgePolarityList))]
public string EdgePolarity { get; set; } = "DarkToLight";

[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> EdgePolarityList { get { return EdgeOptionLists.FAIPolarities; } }
```
**적용:** D-G2 IntersectionPointSelection 신규 enum 콤보. ICustomTypeDescriptor 미도입 옵션 (옵션 A).

### 6. ObservableCollection in-place 정렬 (D-G3)
**Source:** NodeViewModel.LoadChildren (L77-89) + ReloadChildren (L94-104) — ObservableCollection 직접 갱신 패턴.
**적용:** SortNodeChildren — list 복사 → Sort → Clear → re-Add. TreeListBox ObservableCollection 이벤트로 안전 갱신.
**주의:** RaisePropertyChanged("HasItems") 호출은 ReloadChildren 의 정렬 후 호출 패턴 동형 적용.

### 7. //YYMMDD hbk 주석 컨벤션 (memory feedback_comment_convention)
**Format:** `//260530 hbk Phase 39.2 D-G1` (날짜 / 작성자 / phase / 결정 ID).
**적용 대상:** 모든 신규/수정 라인. 기존 marker 스택 보존 (Phase 20 D-12 패턴 — 기존 marker 위에 누적).

### 8. Allman vs K&R brace 스타일 — per-file 보존 (CLAUDE.md)
- EdgeToLineDistanceMeasurement.cs / ArcLineIntersectDistanceMeasurement.cs / MeasurementFactory.cs / MeasurementBase.cs → **Allman** (신규 파일은 Allman).
- Action_FAIMeasurement.cs / InspectionListView.xaml.cs / InspectionListViewModel.cs / NodeViewModel.cs / Node.cs → **K&R** (기존 파일은 K&R 보존).
- 신규 `DualImageEdgeDistanceMeasurement.cs` / `NaturalStringComparer.cs` → **Allman** (Measurements / 새 Utility 표준).

## No Analog Found

| 파일 | Role | 이유 | 권장 대안 |
|------|------|------|----------|
| `WPF_Example/Utility/NaturalStringComparer.cs` | utility (IComparer) | codebase 내 IComparer 구현 없음 | 외부 표준 regex 패턴 (D-G3-A 섹션 참조), Utility 폴더 컨벤션은 `Logging.cs` / `Ini.cs` 참조 |

DataTemplateSelector 도 codebase 에 분명한 예 없음 — `WPF_Example/UI/ControlItem/InspectionListView.xaml` L43-53 의 `DataTemplate DataType="{x:Type local:NodeViewModel}"` 단일 인라인 패턴이 유일. DataTemplateSelector 신설보다는 **IValueConverter + DynamicResource Geometry lookup** 패턴이 변경 최소 (planner 결정).

## Metadata

**Analog search scope:**
- `WPF_Example/Custom/Sequence/Inspection/Measurements/*.cs` (12 files — 모든 Measurement 타입 확인)
- `WPF_Example/Custom/Sequence/Inspection/` (MeasurementBase, MeasurementFactory, Action_FAIMeasurement)
- `WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs` (TryFitLine, TryIntersectLines, ComputeProjectionDistance)
- `WPF_Example/UI/ControlItem/` (InspectionListView.xaml, .xaml.cs, ViewModel, NodeViewModel, Node)
- `WPF_Example/Sequence/Param/ParamBase.cs` (직렬화 switch case)
- `WPF_Example/Resource/` (기존 PNG 자원 32개)
- `WPF_Example/App.xaml` (ResourceDictionary 패턴 검색 — 결과: ResourceDictionary 미사용, 직접 Application.Resources 사용)

**Files scanned:** 15
**Pattern extraction date:** 2026-05-30
