---
phase: 39.2-urgent-additions-2-2026-05-30
type: uat
status: pending
date: 2026-05-30
mode: SIMUL_MODE (Debug/x64)
prerequisites:
  - msbuild Debug/x64 PASS (Plan 01~04 빌드 완료)
  - 신규 SIMUL 이미지: Cal_Image/DualImageTest/SIDE1_3-1_Datum_A1_A2_backlight_LV30.bmp + Cal_Image/DualImageTest/SIDE1_3-1_Datum_B1_backlight_LV30.bmp
total: 5
passed: 0
failed: 0
pending: 5
---

# Phase 39.2 UAT — 긴급 추가건2 (4 항목 + 회귀)

## 검증 시나리오

### Test 1: Item #1 — Bottom E5 DualImage FAI 측정 (D-G1)

**대상:** `DualImageEdgeDistanceMeasurement` (Plan 01 신규)

**Setup:**
1. SIMUL_MODE 빌드 실행
2. Bottom Sequence > Shot > FAI > Measurement 신규 추가 → Type = `DualImageEdgeDistance` 선택
3. ShotConfig.SimulImagePath = `Cal_Image/DualImageTest/SIDE1_3-1_Datum_A1_A2_backlight_LV30.bmp` (PointROI 이미지)
4. Measurement.TeachingImagePath_Vertical = `Cal_Image/DualImageTest/SIDE1_3-1_Datum_B1_backlight_LV30.bmp` (LineROI 이미지)
5. PointROI / LineROI ROI 좌표 설정 (사용자 자체 — 각 이미지의 에지 영역)
6. Edge 파라미터 (PointROI_/LineROI_) 기본값 그대로 (Sigma=1.0 / Threshold=10 / SampleCount=20 / TrimCount=10 / Polarity=DarkToLight / EdgeSelection=All)
7. 사이클 실행 (Inspect 버튼 또는 TCP 트리거)

**Verification steps:**
1. EStep.Measure 진입 시 Action_FAIMeasurement 가 DualImage 분기 진입 — `is DualImageEdgeDistanceMeasurement` 트루
2. TryGrabOrLoadFaiDualImages 가 양 이미지 (SimulImagePath + TeachingImagePath_Vertical) 로드 성공 (로그 "PointROI 이미지 경로 비어 있거나" / "LineROI 이미지 경로 비어 있거나" 출력 0)
3. PointROI 의 fit_line → 중점 1점 산출 ("FAI-Edge1" overlay 의 Points[0])
4. LineROI 의 fit_line → 라인 (시작/끝점) 산출 ("FAI-Edge2" overlay)
5. projection_pl 거리 = sqrt((pRow-footRow)^2 + (pCol-footCol)^2) × pixelResolution → 측정값 (mm) FAIResultRow.MeasuredValue 에 표시
6. UI 캔버스 = PointROI 이미지 (SimulImagePath) 가 background, 양 에지 라인 + 중점 마커 + 청록 거리선 표시

**Expected outcome:**
- 측정값 (mm) 이 0 이 아니고 일관성 있음 (반복 실행 시 동일)
- Action_FAIMeasurement 종료 후 DualImage 인스턴스의 RuntimeImageA/B == null (메모리 정리)
- INI 저장 후 재로드 시 PointROI_/LineROI_ 전 30+ 파라미터 round-trip 보존
- Phase 39 D-01 IsDatumFailed 게이트는 datum 미설정 시도 정상 동작 (식별자 = 빈 DatumRef → identity fallback)

**Result:** _pending_

---

### Test 2: Item #2 — Top I10 close-point variant (D-G2)

**대상:** `ArcLineIntersectDistanceMeasurement.IntersectionPointSelection` (Plan 02 추가)

**Setup:**
1. SIMUL_MODE 빌드 실행
2. Top Sequence > Shot > FAI > Measurement Type=`ArcLineIntersectDistance` 1건 선택 (Phase 32 P03 기존 측정 인스턴스 — 새로 만들지 말고 기존 INI 재사용 권장)
3. 4 ROI (EdgeA1/B1/A2/B2) + 4 Edge 그룹 설정 (Phase 32 sign-off 시점 값 그대로)
4. Datum (DatumConfigs[0]) 적절히 셋업 (DatumAngle2Rad 주입 위해 CTH 또는 VTH datum 결과 사용)

**Verification steps:**
1. **Default Far (default 값):**
   - IntersectionPointSelection 미설정 또는 "Far" → 사이클 실행 → 측정값 V_far 기록
   - Phase 32 sign-off (commit cccd543 또는 본 Phase 직전 빌드) 시 동일 INI 측정값과 비교
   - **I1 fix 정량 tolerance:** byte-identical = 소수점 4자리 이내 차이 (|V_far_now - V_far_phase32| ≤ 0.0001 mm) → PASS. 그 외 → FAIL (회귀)
2. **Close 분기:**
   - PropertyGrid 에서 IntersectionPointSelection = "Close" 변경 → INI 저장 → 사이클 실행 → 측정값 V_close 기록
   - V_close ≠ V_far (두 교점이 다른 좌표 → 다른 결과, |V_close - V_far| > 0.0001 mm)
   - MeasureAxis="X" 일 때: V_close 는 교점1.Col 기준, V_far 는 교점2.Col 기준
3. PropertyGrid 카테고리 "Measurement|Measure" 에 IntersectionPointSelection 콤보 (Far / Close) 시각 노출

**Expected outcome:**
- 기본값 Far 동작이 Phase 32 sign-off 시점과 byte-identical (소수점 4자리 이내, ≤ 0.0001 mm)
- Close 변경 시 측정값 결정적 변화 (Far 값과 0.0001 mm 초과 차이)
- INI round-trip 보존 (IntersectionPointSelection=Close 저장/로드)
- MeasurementFactory.GetTypeNames() 11 개 (CompoundShortAxisDistance 까지 10 + DualImageEdgeDistance 1)

**Result:** _pending_

---

### Test 3: Item #3 — Tree 노드 자연정렬 (D-G3)

**대상:** `NaturalStringComparer` + `InspectionListViewModel.SortNodeChildren/SortAllLevels` (Plan 03)

**Setup:**
1. SIMUL_MODE 빌드 실행 → 신규 빈 recipe 생성
2. Shot 노드 추가 순서: Shot10 → Shot2 → Shot1 → Shot20 (Shot 이름은 PropertyGrid 에서 ShotName 변경)
3. Shot1 아래 FAI 노드 3개 추가: FAI10 / FAI2 / FAI1 (FAIName 변경)
4. Shot1 아래 Datum 노드 3개: Datum_C → Datum_B → Datum_A
5. FAI1 아래 Measurement 3개: Meas10 / Meas2 / Meas1

**Verification steps:**
1. 트리에서 Shot 노드 표시 순서 확인: **Shot1 / Shot2 / Shot10 / Shot20** (자연정렬 asc) — B2 fix: Shot add 직후 즉시 정렬됨
2. Shot1 아래 FAI 노드: FAI1 / FAI2 / FAI10
3. Shot1 아래 Datum 노드: Datum_A / Datum_B / Datum_C (사전식 fallback)
4. FAI1 아래 Measurement: Meas1 / Meas2 / Meas10
5. **Rename 즉시 재정렬:** Shot1 → "Shot99" 변경 (PropertyGrid) → Shot 노드 순서 = Shot2 / Shot10 / Shot20 / Shot99
6. **INI 저장 후 재로드:** INI 파일을 외부 텍스트 편집기로 확인 → `[Shot]` 섹션 순서가 **트리 표시 순서와 무관** (ParamBase 직렬화 순서 변경 0 — 원래 등록 순서 유지)
7. RebuildTree (recipe Reload) → 트리 다시 자연정렬 상태

**Expected outcome:**
- Shot/FAI/Datum/Measurement 4 레벨 모두 자연정렬 가시화 (B2 fix: Shot add 트리거 포함)
- Rename 시 즉시 재정렬
- INI 파일 순서 변경 0 (view-only 정렬)
- Phase 39.1-03 노드 클릭 → RenderInspectionResultForNode 동작 회귀 0 (선택된 노드의 결과 재현 정상)

**Result:** _pending_

---

### Test 4: Item #4 — Tree 노드 아이콘 차별화 (D-G4)

**대상:** `Node.IconKey` + `IconKeyToGeometryConverter` + InspectionListView.xaml Geometry 18 종 (Plan 04)

**Setup:**
1. SIMUL_MODE 빌드 실행 → 기존 recipe 로드 (다양한 Measurement TypeName 포함된 것)
2. 트리에 다음 노드가 보이도록: Recipe / Sequence / Action / Datum / FAI / Measurement (12 TypeName 중 최소 5 종)

**Verification steps:**
1. **NodeType 별 아이콘 시각 확인:**
   - Recipe (루트): folder 아이콘 (Icon.Recipe)
   - Sequence: flow-chart 아이콘 (Icon.Sequence)
   - Action: layers 아이콘 (Icon.Action)
   - Datum: crosshair 아이콘 (Icon.Datum)
   - FAI: document with chart 아이콘 (Icon.FAI)
2. **Measurement TypeName 별 차별 아이콘:**
   - CircleDiameter: 원 + 지름선
   - EdgeToLineDistance: 점 + 수직선
   - ArcLineIntersectDistance: 호 + 라인 교차
   - **W1 fix CompoundCenterBDistance:** 좌측 화살표 mirror (CompoundCenterCDistance 우측 화살표와 좌우 대칭). 화살표 path 의 중복 segment 없음 (objective table L91 path 와 byte-identical)
   - DualImageEdgeDistance (Plan 01 신규): 2 boxes + arrow (별도 아이콘)
   - 다른 TypeName 도 모두 다른 모양
3. **노드 텍스트 (Name) 보존:** 모든 노드 텍스트가 변경 없이 표시 (DatumName / ShotName / FAIName / MeasurementName)
4. **Phase 39 DETECT FAIL 회귀 0:** Datum 검출 실패 시 적색 라벨 (RenderDatumOverlay) 표시 동작 보존
5. **ContextMenu 아이콘 회귀 0:** 트리 우클릭 → context menu (Save / Save As / Open Recipe) 의 아이콘 (PNG: save.png / saveas.png / folder.png) 보존

**Expected outcome:**
- 5 NodeType + 12 Measurement TypeName 별 차별 아이콘 가시화
- CompoundCenterBDistance 좌측 화살표 mirror 정상 (W1 fix)
- 노드 텍스트 변경 0
- ContextMenu PNG 아이콘 보존
- Phase 39 DETECT FAIL 라벨 + Phase 39.1-03 LastOverlays 재현 보존

**Result:** _pending_

---

### Test 5: 회귀 검증 — Phase 28/31/36/37/39/39.1 SIMUL UAT 핵심 시나리오 재현

**대상:** Phase 39.2 의 4 변경이 직전 phase sign-off 시나리오에 회귀 0 임을 입증

**Setup:**
1. SIMUL_MODE 빌드 실행
2. Phase 39.1 sign-off 시점의 recipe 또는 동일 측정 항목 셋업

**Verification steps (I1 fix tolerance — 모든 byte-identical 비교는 소수점 4자리 / ≤ 0.0001 mm):**
1. **Phase 28 (CircleDiameter polar):** FAI CircleDiameter 측정값이 Phase 28 sign-off 결과와 비교, |Δ| ≤ 0.0001 mm
2. **Phase 31 (E8/D1/I9/I10):** 기존 ArcLineIntersect (default IntersectionPointSelection=Far) 측정값이 Phase 31/32 sign-off 결과와 비교, |Δ| ≤ 0.0001 mm
3. **Phase 36 (Datum DualImage):** Datum VerticalTwoHorizontalDualImage 동작 (Action_FAIMeasurement.EStep.DatumPhase TryGrabOrLoadDualDatumImages 분기) 회귀 0
4. **Phase 37 (Side multi-datum):** Side Sequence DatumConfigs 전체 per-datum loop + lenient 분기 회귀 0
5. **Phase 39 (WF E2E):** Datum 검출 실패 시 후속 FAI skip + DETECT FAIL 라벨 + LastSkipReason="DATUM_FAIL" 회귀 0
6. **Phase 39.1 (CircleDiameter polar 4 필드 + EdgeToLineDistance measureX + FAI 노드 클릭 overlay 재현 + Datum CTH Edit 모드):** 4 시나리오 모두 회귀 0 (측정값 비교 시 |Δ| ≤ 0.0001 mm)

**Expected outcome:**
- 5 회귀 시나리오 모두 PASS (sign-off 동등성 유지, |Δ| ≤ 0.0001 mm)
- INI 하위호환 100%
- Anti-goal 위반 0

**Result:** _pending_

---

## Summary

| Test | Item | Decision | Status |
|------|------|----------|--------|
| 1 | Bottom E5 DualImage FAI | D-G1 | pending |
| 2 | Top I10 close-point variant | D-G2 | pending |
| 3 | Tree 자연정렬 | D-G3 | pending |
| 4 | Tree 아이콘 차별화 | D-G4 | pending |
| 5 | Phase 28/31/36/37/39/39.1 회귀 | (cross-phase) | pending |

**Total:** 5 / **Passed:** 0 / **Failed:** 0 / **Pending:** 5

**측정값 비교 정밀도 (I1 fix):** 소수점 4자리 (0.0001 mm 이하 차이 PASS, 그 외 FAIL)

---

## Sign-off

(사용자 4/4 + 회귀 1 = 5/5 PASS 시 status=signed_off + 갱신)

- [ ] Test 1 — D-G1 DualImage FAI: _pending user verification_
- [ ] Test 2 — D-G2 I10 close-point: _pending user verification_
- [ ] Test 3 — D-G3 자연정렬: _pending user verification_
- [ ] Test 4 — D-G4 Tree 아이콘: _pending user verification_
- [ ] Test 5 — 회귀: _pending user verification_

**Sign-off:** _pending_
**Date:** _pending_
**Commit:** _pending_
