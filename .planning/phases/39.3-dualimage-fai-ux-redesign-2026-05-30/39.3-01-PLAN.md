---
phase: 39.3-dualimage-fai-ux-redesign-2026-05-30
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: true
requirements: [WF-01]
tags: [ui, wpf, dualimage, rectroi]

must_haves:
  truths:
    - "DualImage FAI Measurement 노드 선택 시 btn_rectRoi 가 활성화된다 (isRectRoiType=true)"
    - "btn_rectRoi 클릭 → 드래그 시 FindSelectedRectMeasurement 가 DualImage 객체를 반환한다 (트리/dataGrid 양 경로)"
    - "캔버스 드래그 후 마우스 업 시 _currentImageSource 슬롯에 따라 PointROI 또는 LineROI 필드가 write-back 된다"
    - "BuildPointRoiDefinitions 가 DualImage 에 대해 2개 RoiDefinition (Point + Line) 을 반환한다"
    - "기존 7 Measurement 타입의 동작이 변경되지 않는다 (회귀 0)"
    - "_currentImageSource 의 default = Horizontal 이므로 Plan 02 미진입 상태에서도 PointROI 라우팅이 동작한다 (빌드 안전)"
  artifacts:
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "isRectRoiType 화이트리스트 8번째 타입 추가"
      contains: "item.Param is DualImageEdgeDistanceMeasurement"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "FindSelectedRectMeasurement + CommitRectRoi + BuildPointRoiDefinitions 3 메서드 DualImage 분기"
      contains: "_editingMeasurement as DualImageEdgeDistanceMeasurement"
  key_links:
    - from: "InspectionListView.xaml.cs:496-503 isRectRoiType"
      to: "MainView.btn_rectRoi.IsEnabled"
      via: "mParentWindow.mainView.btn_rectRoi.IsEnabled = isRectRoiType"
      pattern: "btn_rectRoi\\.IsEnabled = isRectRoiType"
    - from: "MainView.btn_rectRoi.Click → FindSelectedRectMeasurement"
      to: "_editingMeasurement (MeasurementBase)"
      via: "트리 SelectedParam 우선, dataGrid fallback"
      pattern: "selParam is DualImageEdgeDistanceMeasurement"
    - from: "halconViewer.RectDrawingCompleted → CommitRectRoi"
      to: "DualImage.PointROI_* / LineROI_* 필드"
      via: "_currentImageSource 분기 라우팅"
      pattern: "_currentImageSource == .*EImageSource\\.Vertical"
    - from: "BuildPointRoiDefinitions(DualImage)"
      to: "List<RoiDefinition> {Point, Line}"
      via: "ArcLineIntersect 4-RoiDefinition 패턴 차용 + early return"
      pattern: "faiName \\+ \"_\" \\+ measName \\+ \"_(Point|Line)\""
---

<objective>
**Plan 01 — RectROI 활성화 baseline (D-G1 + D-G5)**

Phase 39.2 D-G1 UAT FAIL 의 직접 원인 (`isRectRoiType` 8 타입 화이트리스트에 `DualImageEdgeDistanceMeasurement` 누락) 을 해소하고, RectROI 드로잉이 활성화되었을 때 캔버스 write-back 이 `_currentImageSource` 슬롯에 따라 PointROI / LineROI 양쪽으로 라우팅되도록 4 지점 (분기 A, B, C, D) 을 추가한다.

Purpose: Plan 02 (swap UX wiring) 진입 전에 RectROI 드로잉 자체를 baseline 으로 완성. `_currentImageSource` default = Horizontal 이므로 Plan 02 미진입 상태에서도 PointROI 1개 ROI 셋팅이 가능한 부분 동작 상태로 빌드 안전.
Output: 8번째 타입 (`DualImageEdgeDistanceMeasurement`) 이 기존 7 타입과 동일하게 RectROI 드로잉 → write-back → 재 렌더 파이프라인에 통합. ArcLineIntersect 4-RoiDefinition 패턴을 차용한 2-RoiDefinition (Point/Line) 반환.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-CONTEXT.md
@.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-RESEARCH.md
@.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-PATTERNS.md
@WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
@WPF_Example/UI/ContentItem/MainView.xaml.cs
@WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs
@WPF_Example/Custom/Sequence/Inspection/EImageSource.cs

<interfaces>
<!-- Key types and contracts for executor reference -->

DualImageEdgeDistanceMeasurement 필드 (write-back 대상):
- public double PointROI_Row / PointROI_Col / PointROI_Phi / PointROI_Length1 / PointROI_Length2
- public double LineROI_Row / LineROI_Col / LineROI_Phi / LineROI_Length1 / LineROI_Length2
- public string TeachingImagePath_Vertical
- 본체는 ReringProject.Sequence namespace (DualImageEdgeDistanceMeasurement.cs 확인)

EImageSource enum:
- namespace ReringProject.Sequence
- enum EImageSource { Horizontal = 0, Vertical = 1 }

MainView.xaml.cs 기존 상태 필드 (Plan 02 와 공유):
- private ReringProject.Sequence.EImageSource _currentImageSource = ReringProject.Sequence.EImageSource.Horizontal; (L64)
- 본 Plan 01 은 _currentImageSource 의 default = Horizontal 만 참조 (set 안 함)

RoiDefinition 컨벤션 (SP-5):
- 단일: Id = faiName + "_" + measName
- 다중 (ArcLineIntersect 패턴): Id = faiName + "_" + measName + "_EdgeA1" 등 suffix
- DualImage 적용: "_Point" / "_Line" suffix
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 01-01: InspectionListView.xaml.cs — isRectRoiType 화이트리스트 8번째 타입 추가 (분기 A)</name>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs (L480-525 — isRectRoiType 분기 본체 + Measurement 노드 처리 전체)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-PATTERNS.md (분기 A 섹션 — L75-95)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-RESEARCH.md (RT-4 표 A 항)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs (타입 namespace + using 확인)
  </read_first>
  <action>
    //260530 hbk Phase 39.3 D-G1 주석 의무.

    PATTERNS.md L77-86 분기 A 의 기존 isRectRoiType 블록 (L495-505) 끝, `is CompoundShortAxisDistanceMeasurement;` 가 OR 체인 종료 (`;`) 인 라인에서:

    1. `CompoundShortAxisDistanceMeasurement` 라인의 끝 세미콜론 `;` 을 제거하고 OR 연산자 `` || `` 로 교체.
    2. 신규 라인 1줄 추가:
       ```
       || item.Param is DualImageEdgeDistanceMeasurement; //260530 hbk Phase 39.3 D-G1
       ```
    3. 결과 OR 체인 끝부분 (수정 후 형태):
       ```csharp
       bool isRectRoiType = item.Param is EdgeToLineDistanceMeasurement
           || item.Param is EdgeToLineAngleMeasurement       //260519 hbk Phase 31 D-05
           || item.Param is ArcEdgeDistanceMeasurement        //260519 hbk Phase 31 D-08
           || item.Param is ArcLineIntersectDistanceMeasurement //260519 hbk Phase 31 D-01
           || item.Param is CompoundAngleMeasurement          //260519 hbk Phase 31 D-11
           || item.Param is CompoundCenterCDistanceMeasurement //260519 hbk Phase 31 D-11
           || item.Param is CompoundCenterBDistanceMeasurement //260519 hbk Phase 31 D-11
           || item.Param is CompoundShortAxisDistanceMeasurement //260523 hbk Phase 32 — E3 단축 환원
           || item.Param is DualImageEdgeDistanceMeasurement; //260530 hbk Phase 39.3 D-G1
       ```

    using/namespace 추가 0 — `DualImageEdgeDistanceMeasurement` 은 동일 `ReringProject.Sequence` namespace 이고 InspectionListView.xaml.cs 의 기존 using 으로 해결 (분기 A 의 다른 7 타입과 동일 namespace).

    회귀 가드: 기존 8개 항 (Compound 4종 + Edge 3종 + ArcLineIntersect 1종 = 8) 의 OR 절 텍스트 1줄도 수정 X. 추가만 허용.
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 — PASS 0 new errors/warnings</automated>
    추가 grep 검증:
    - `grep -n "is DualImageEdgeDistanceMeasurement" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` → 1+ 라인 hit
    - `grep -n "Phase 39.3 D-G1" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` → 1+ 라인 hit
    - `grep -c "is .*Measurement" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` 결과가 기존 대비 +1 (= 9 OR 항)
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 PASS, 0 new errors, 0 new warnings (Phase 39.2 baseline 대비)
    - InspectionListView.xaml.cs 의 isRectRoiType 블록이 정확히 9 OR 항 (기존 8 + DualImage 1)
    - 마지막 OR 라인이 `|| item.Param is DualImageEdgeDistanceMeasurement; //260530 hbk Phase 39.3 D-G1` 텍스트 완전 일치
    - 기존 8 OR 항 라인의 텍스트가 수정되지 않음 (line-for-line diff 1 라인만 변경 + 1 라인 추가 또는 2 라인 변경)
    - using 추가 0 (DualImageEdgeDistanceMeasurement = 동일 namespace 으로 해결)
  </acceptance_criteria>
  <done>
    isRectRoiType 화이트리스트가 8번째 타입 (`DualImageEdgeDistanceMeasurement`) 을 포함. DualImage FAI Measurement 노드 선택 시 `mainView.btn_rectRoi.IsEnabled = true` 가 설정됨.
  </done>
</task>

<task type="auto">
  <name>Task 01-02: MainView.xaml.cs — FindSelectedRectMeasurement DualImage 분기 추가 (분기 B)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (L1860-1900 — FindSelectedRectMeasurement 본체)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-PATTERNS.md (분기 B 섹션 — L106-144)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-RESEARCH.md (RT-4 표 B 항)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs
  </read_first>
  <action>
    //260530 hbk Phase 39.3 D-G1 주석 의무. PATTERNS.md L137-142 가 적용 코드 블록.

    `FindSelectedRectMeasurement()` 메서드 (현재 L1865-1896 부근) 의 **2 분기 경로** 양쪽에 DualImage 분기 추가:

    **1) 트리 분기 (`selParam is *` 블록)** — 기존 마지막 항 `if (selParam is CompoundShortAxisDistanceMeasurement) return (MeasurementBase)selParam;` (L1876 부근) 직후 1 라인 추가:

    ```csharp
    if (selParam is DualImageEdgeDistanceMeasurement) return (MeasurementBase)selParam; //260530 hbk Phase 39.3 D-G1
    ```

    **2) dataGrid fallback (`foreach (var m in fai.Measurements)` 내부 `m is *` 블록)** — 기존 마지막 항 `if (m is CompoundShortAxisDistanceMeasurement) return m;` (L1891 부근) 직후 1 라인 추가:

    ```csharp
    if (m is DualImageEdgeDistanceMeasurement) return m; //260530 hbk Phase 39.3 D-G1
    ```

    회귀 가드:
    - 기존 7 타입의 각 `if` 라인 텍스트 변경 0 (line-for-line 차분 = 정확히 +2 라인)
    - return 순서가 정의 순서 (8번째 OR 매치) → FAI 안에 DualImage + EdgeToLineDistance 공존 시 EdgeToLineDistance 가 먼저 매치되어 우선 (의도된 우선순위)
    - using 추가 0 (`DualImageEdgeDistanceMeasurement` 동일 namespace, MainView.xaml.cs 기존 using 으로 해결)
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 — PASS 0 new errors/warnings</automated>
    grep 검증:
    - `grep -n "DualImageEdgeDistanceMeasurement" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 2+ 라인 hit (트리 + dataGrid)
    - `grep -n "Phase 39.3 D-G1" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 2+ 라인 hit (Task 01-02 만 기준; Task 01-03/04 후엔 더 늘어남)
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 PASS, 0 new errors, 0 new warnings
    - FindSelectedRectMeasurement 내부에 `DualImageEdgeDistanceMeasurement` 라는 텍스트가 정확히 2 라인 등장 (트리 + dataGrid)
    - 추가 라인 텍스트가 PATTERNS.md L137-142 와 byte-identical (`return (MeasurementBase)selParam;` / `return m;` 차이 + 주석)
    - 기존 7 타입 분기 (Edge 3 + Compound 4) + ArcLineIntersect 분기 1 라인 변경 0
    - using 추가 0
  </acceptance_criteria>
  <done>
    트리에서 DualImage Measurement 노드 선택 또는 dataGrid 에서 DualImage 결과 행 선택 시 `FindSelectedRectMeasurement()` 가 DualImage 객체를 반환. 후속 `_editingMeasurement` set 으로 RectROI 드로잉 가능.
  </done>
</task>

<task type="auto">
  <name>Task 01-03: MainView.xaml.cs — CommitRectRoi 슬롯-종속 write-back 분기 추가 (분기 C, D-G1 핵심)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (L1627-1700 — CommitRectRoi 본체, 타입별 as 캐스트 블록 7+1 타입)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-PATTERNS.md (분기 C 섹션 — L172-193)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-RESEARCH.md (RT-2 #3 + Code Examples 의 CommitRectRoi 패턴)
    - WPF_Example/Custom/Sequence/Inspection/EImageSource.cs (enum 정의)
  </read_first>
  <action>
    //260530 hbk Phase 39.3 D-G1 주석 의무. PATTERNS.md L173-184 가 적용 코드 블록.

    `CommitRectRoi()` 메서드 (현재 L1627-1735 부근) 내부의 7+1 타입 as 캐스트 분기 블록 (L1644-1658 + Compound 4종) 의 끝부분 — `CompoundShortAxisDistanceMeasurement` 분기 (`cShort` 변수) 직후, `ArcLineIntersectDistanceMeasurement` 분기 (`ali` 변수, L1660 부근) 직전 — 에 신규 블록 삽입:

    ```csharp
    //260530 hbk Phase 39.3 D-G1 — DualImage: _currentImageSource 슬롯에 따라 PointROI vs LineROI 라우팅 (사용자 인지 부담 0 — 보이는 이미지의 ROI 만 그림)
    var dualMeas = _editingMeasurement as DualImageEdgeDistanceMeasurement; //260530 hbk Phase 39.3 D-G1
    if (dualMeas != null) { //260530 hbk Phase 39.3 D-G1
        if (_currentImageSource == ReringProject.Sequence.EImageSource.Vertical) { //260530 hbk Phase 39.3 D-G1
            // 세로 슬롯 표시 중 → LineROI 라우팅
            dualMeas.LineROI_Row = mCenterRow; dualMeas.LineROI_Col = mCenterCol; dualMeas.LineROI_Phi = 0.0;
            dualMeas.LineROI_Length1 = mHalfHeight; dualMeas.LineROI_Length2 = mHalfWidth;
        } else { //260530 hbk Phase 39.3 D-G1
            // 가로 슬롯 표시 중 (기본 Horizontal) → PointROI 라우팅
            dualMeas.PointROI_Row = mCenterRow; dualMeas.PointROI_Col = mCenterCol; dualMeas.PointROI_Phi = 0.0;
            dualMeas.PointROI_Length1 = mHalfHeight; dualMeas.PointROI_Length2 = mHalfWidth;
        }
    }
    ```

    **변수명 검증:** 기존 7 타입 분기에서 사용한 로컬 변수명 (`mCenterRow / mCenterCol / mHalfHeight / mHalfWidth`) 을 그대로 차용. 본 메서드 본체에서 이미 계산되어 있으므로 (L1644 분기 이전) 재계산 0.

    회귀 가드:
    - ArcLineIntersect 4-ROI 순차 분기 (L1660-1693 부근) **진입 전**에 위치 → DualImage 분기가 `_editingMeasurementRoiIndex` 를 건드리지 않음
    - 기존 7+1 (Edge 3 + Compound 4 + ArcLineIntersect 1) 분기 코드 라인 1줄도 수정 X
    - `_currentImageSource` default = Horizontal → Plan 02 미진입 상태에서도 PointROI 라우팅이 항상 동작 → 빌드 + 부분 UAT 가능
    - C# 7.2 호환: `var x = obj as T; if (x != null)` 패턴 (SP-2)
    - K&R 중괄호 스타일 (SP-4) — `if (...) {` 같은 줄
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 — PASS 0 new errors/warnings</automated>
    grep 검증:
    - `grep -n "_editingMeasurement as DualImageEdgeDistanceMeasurement" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 라인 hit
    - `grep -n "dualMeas.PointROI_Row" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 라인 hit
    - `grep -n "dualMeas.LineROI_Row" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 라인 hit
    - `grep -n "_currentImageSource == ReringProject.Sequence.EImageSource.Vertical" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 라인 hit (CommitRectRoi 위치)
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 PASS, 0 new errors, 0 new warnings
    - CommitRectRoi 메서드 안에 `dualMeas` 변수 캐스트 + `if (dualMeas != null)` 블록이 정확히 1번 등장
    - 슬롯 분기 if/else 양쪽에 PointROI / LineROI 5필드 (Row/Col/Phi/Length1/Length2) 모두 set
    - 기존 7+1 타입 분기 (Edge 3 + Compound 4 + ArcLineIntersect 1) 라인 변경 0 (line-for-line diff = 신규 블록 추가만)
    - 위치: cShort 분기 직후, ali 분기 직전 (ArcLineIntersect 의 RoiIndex 사이드 이펙트 방지)
  </acceptance_criteria>
  <done>
    캔버스에서 RectROI 드래그 → 마우스 업 시 `_currentImageSource` 가 Vertical 이면 LineROI_*, Horizontal (기본) 이면 PointROI_* 필드에 5값 (Row/Col/Phi/Length1/Length2) 라우팅 write-back. Plan 02 미진입 시에도 default = Horizontal 이므로 PointROI 셋팅이 항상 동작.
  </done>
</task>

<task type="auto">
  <name>Task 01-04: MainView.xaml.cs — BuildPointRoiDefinitions DualImage 2-RoiDefinition 분기 추가 (분기 D)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (L343-420 — BuildPointRoiDefinitions 본체, ArcLineIntersect 4-RoiDefinition 분기 L348-388 포함)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-PATTERNS.md (분기 D 섹션 — L222-248)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-RESEARCH.md (RT-2 #4 + Risk R3 가드)
  </read_first>
  <action>
    //260530 hbk Phase 39.3 D-G1 주석 의무. PATTERNS.md L222-244 가 적용 코드 블록.

    `BuildPointRoiDefinitions(MeasurementBase m, string faiName)` 메서드 (현재 L343-416 부근) 내부, ArcLineIntersect 분기 `return result;` (L388 부근) **직후**, 단일 RoiDefinition 분기 (`double pRow = 0;` 또는 동등 시작점, L390 부근) **직전** 에 신규 블록 삽입:

    ```csharp
    //260530 hbk Phase 39.3 D-G1 — DualImage: PointROI + LineROI 2개 독립 RoiDefinition (ArcLineIntersect 패턴 차용, RoiId suffix "_Point"/"_Line", HighlightSelectedRoi L466-470 fallback 호환)
    var dual = m as DualImageEdgeDistanceMeasurement; //260530 hbk Phase 39.3 D-G1
    if (dual != null) { //260530 hbk Phase 39.3 D-G1
        if (dual.PointROI_Length1 > 0 && dual.PointROI_Length2 > 0) { //260530 hbk Phase 39.3 D-G1
            result.Add(new RoiDefinition {
                Id = faiName + "_" + measName + "_Point", //260530 hbk Phase 39.3 D-G1 — SP-5 RoiId suffix
                Name = measName + "_Point",
                Row1 = dual.PointROI_Row - dual.PointROI_Length1, Column1 = dual.PointROI_Col - dual.PointROI_Length2,
                Row2 = dual.PointROI_Row + dual.PointROI_Length1, Column2 = dual.PointROI_Col + dual.PointROI_Length2,
                IsTaught = true
            });
        }
        if (dual.LineROI_Length1 > 0 && dual.LineROI_Length2 > 0) { //260530 hbk Phase 39.3 D-G1
            result.Add(new RoiDefinition {
                Id = faiName + "_" + measName + "_Line", //260530 hbk Phase 39.3 D-G1
                Name = measName + "_Line",
                Row1 = dual.LineROI_Row - dual.LineROI_Length1, Column1 = dual.LineROI_Col - dual.LineROI_Length2,
                Row2 = dual.LineROI_Row + dual.LineROI_Length1, Column2 = dual.LineROI_Col + dual.LineROI_Length2,
                IsTaught = true
            });
        }
        return result; //260530 hbk Phase 39.3 D-G1 — ArcLineIntersect 와 동일하게 조기 종료 (단일 RoiDefinition 분기 건너뜀)
    }
    ```

    **변수명 검증:**
    - `result` = 메서드 본체에서 이미 선언된 `List<RoiDefinition>` (ArcLineIntersect 분기에서도 동일 변수 사용)
    - `measName` = ArcLineIntersect 분기에서 사용된 로컬 변수 (L350 부근 `string measName = m.MeasurementName;` 또는 동등 라인) — 동일 변수 그대로 사용. 만약 ArcLineIntersect 분기 안에서만 선언되어 scope 밖이라면, 메서드 진입부에서 `string measName = m.MeasurementName;` 한 줄 추가 또는 ArcLineIntersect 와 동일 위치 선언 패턴 참조.
    - `RoiDefinition` 타입 = 동일 파일 기존 import 으로 해결

    회귀 가드:
    - ArcLineIntersect 분기 (L348-388) 의 4 RoiDefinition + `return result;` 라인 변경 0
    - 단일 RoiDefinition 분기 (L390+) 의 모든 라인 변경 0 — 본 추가 블록의 `return result;` 가 DualImage 가 거기 빠져나가 빈 ROI 반환되는 결함 회피
    - RoiId 컨벤션 `_Point` / `_Line` suffix → HighlightSelectedRoi L466-470 fallback (composite ID 미매칭 시 부모 FAI ROI fallback) 정상 동작 (Risk R3 회피)
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 — PASS 0 new errors/warnings</automated>
    grep 검증:
    - `grep -n "as DualImageEdgeDistanceMeasurement" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 2 라인 hit (Task 01-03 + 01-04)
    - `grep -n "_Point\"" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1+ 라인 hit (RoiId suffix)
    - `grep -n "_Line\"" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1+ 라인 hit
    - `grep -n "dual.PointROI_Length1 > 0" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 라인 hit
    - `grep -n "dual.LineROI_Length1 > 0" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 라인 hit
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 PASS, 0 new errors, 0 new warnings
    - BuildPointRoiDefinitions 메서드 안에 `var dual = m as DualImageEdgeDistanceMeasurement` 블록 + `return result;` (early return) 정확히 1회 등장
    - DualImage 분기 안에 PointROI 와 LineROI 각각 `result.Add(new RoiDefinition { ... })` 정확히 1회씩 (총 2회 추가)
    - RoiId 컨벤션: `faiName + "_" + measName + "_Point"` / `"_Line"`
    - 위치: ArcLineIntersect 분기 `return result;` 직후, 단일 RoiDefinition 분기 시작 직전
    - 기존 ArcLineIntersect + 단일 RoiDefinition 분기 라인 변경 0
  </acceptance_criteria>
  <done>
    BuildPointRoiDefinitions 가 DualImage 에 대해 2개 RoiDefinition (Point + Line, suffix 컨벤션) 을 반환. 캔버스 재 렌더 시 양 ROI 가 동시에 그려짐. 부분 티칭 (PointROI 만 또는 LineROI 만) 도 Length1/Length2 > 0 가드로 정상 처리.
  </done>
</task>

</tasks>

<verification>
**Plan 01 전체 검증 (Anti-Goal 가드):**

1. **빌드 PASS 검증** (각 task 후 누적):
   - `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64` → 0 new errors, 0 new warnings (Phase 39.2 baseline 대비)

2. **분기 추가량 정확성** (grep):
   - InspectionListView.xaml.cs: `is DualImageEdgeDistanceMeasurement` 1 라인 hit
   - MainView.xaml.cs: `DualImageEdgeDistanceMeasurement` 텍스트 4 라인 hit (트리 분기 1 + dataGrid 분기 1 + CommitRectRoi 1 + BuildPointRoiDefinitions 1)
   - MainView.xaml.cs: `Phase 39.3 D-G1` 마커 그루핑 (Task 01-02/03/04 합산 = 다수 라인)

3. **Anti-Goal 가드 (코드 인스펙션)**:
   - `DualImageEdgeDistanceMeasurement.cs` 변경 0 (`git diff WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` 결과 0 라인)
   - `MeasurementFactory.cs` 변경 0
   - `Action_FAIMeasurement.cs` 변경 0
   - 다른 7 Measurement 타입 (Edge 3 + Compound 4 + ArcLineIntersect) 분기 라인 변경 0
   - Datum 관련 코드 (`_selectedDatumForSwap` / `PublishDatumRoiCandidates` / `BtnSwap*_Click` Datum 경로) 변경 0

4. **빌드 안전성 검증**:
   - Plan 02 미진입 상태에서도 `_currentImageSource` default = Horizontal 이므로 CommitRectRoi 의 DualImage 분기 PointROI 라우팅이 동작 (`else` 절)
</verification>

<success_criteria>
- 4 task 모두 msbuild Debug/x64 PASS (0 new errors, 0 new warnings)
- DualImage FAI Measurement 노드 선택 시 `btn_rectRoi.IsEnabled = true` 활성
- RectROI 드로잉 → write-back 시 `_currentImageSource` default = Horizontal 이므로 PointROI 5필드 set
- BuildPointRoiDefinitions 가 PointROI Length>0 인 경우 1 RoiDefinition 반환
- 기존 7 Measurement 타입 회귀 0 (코드 인스펙션)
- Plan 02 / Plan 03 와 독립적으로 빌드 가능 — Wave 1 병렬 진입 보장
</success_criteria>

<output>
After completion, create `.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-01-SUMMARY.md` with:
- 4 task 의 changes (file:line + 추가 라인 수)
- Anti-Goal 가드 검증 결과 (위 verification 4개 항)
- msbuild Debug/x64 결과 (errors/warnings 카운트)
- Plan 02 와의 인터페이스 (`_currentImageSource` 의 default 의존)
</output>
</content>
</invoke>