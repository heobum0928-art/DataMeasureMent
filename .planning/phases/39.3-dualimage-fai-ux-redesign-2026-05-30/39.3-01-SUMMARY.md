---
phase: 39.3-dualimage-fai-ux-redesign-2026-05-30
plan: 01
subsystem: ui-wpf
tags: [ui, wpf, dualimage, rectroi, d-g1]
one-liner: "DualImageEdgeDistanceMeasurement (Phase 39.2 8번째 타입) 을 RectROI baseline UX 파이프라인 (isRectRoiType / FindSelectedRectMeasurement / CommitRectRoi / BuildPointRoiDefinitions) 에 4 분기 (A/B/C/D) 로 통합 — Plan 02 swap UX 미진입 상태에서도 _currentImageSource default = Horizontal 로 PointROI 라우팅 always 동작 (빌드 안전)"
requires: [WF-01]
provides: [D-G1-baseline-rectroi-routing]
affects:
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
tech-stack:
  added: []
  patterns:
    - "ArcLineIntersect 4-RoiDefinition early-return 패턴 차용 → DualImage 2-RoiDefinition (_Point / _Line suffix)"
    - "_currentImageSource (EImageSource enum, Phase 34.1 도입) 슬롯-종속 라우팅 (Vertical → LineROI, default Horizontal → PointROI)"
    - "is OR 화이트리스트 (Phase 31 CO-23.1-02 / Phase 32 E3 단축 환원 패턴 확장 → 9 OR 항)"
    - "SP-5 RoiId 컨벤션 (단일 = faiName + '_' + measName, 다중 = + '_Suffix')"
key-files:
  created: []
  modified:
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs (L503-504 isRectRoiType OR 9번째 항 추가)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (L389-414 BuildPointRoiDefinitions DualImage 분기 / L1659-1672 CommitRectRoi 슬롯 라우팅 분기 / L1877 FindSelectedRectMeasurement 트리 분기 / L1893 dataGrid fallback 분기)
decisions:
  - "DualImageEdgeDistanceMeasurement / MeasurementFactory / Action_FAIMeasurement 변경 0 — RectROI baseline 은 UI 4 분기 통합만으로 충족 (Anti-Goal #1~#3 가드)"
  - "_currentImageSource 의 default = Horizontal 보장 → Plan 02 swap UX 미진입 상태에서도 PointROI 5필드 셋팅이 always 동작 → Wave 1 (Plan 01/03) 병렬 빌드 안전"
  - "RoiId suffix '_Point' / '_Line' (ArcLineIntersect '_EdgeA1' 4-suffix 패턴 차용) → HighlightSelectedRoi L466-470 composite ID fallback 자동 호환"
metrics:
  duration: "~12 minutes"
  completed: "2026-05-30"
  tasks_completed: 4
  files_modified: 2
  commits: 4
---

# Phase 39.3 Plan 01: DualImage RectROI Baseline (D-G1) Summary

## Objective

Phase 39.2 D-G1 UAT FAIL 의 직접 원인 (isRectRoiType 8 타입 화이트리스트에 `DualImageEdgeDistanceMeasurement` 누락) 을 해소하고, RectROI 드로잉이 활성화되었을 때 캔버스 write-back 이 `_currentImageSource` 슬롯에 따라 PointROI / LineROI 양쪽으로 라우팅되도록 4 분기 (A, B, C, D) 를 추가. Plan 02 (swap UX wiring) 진입 전에 RectROI 드로잉 자체를 baseline 으로 완성.

## Task-Level Changes

### Task 01-01 — InspectionListView.xaml.cs (분기 A): isRectRoiType 9번째 OR 항 추가

**Commit:** `0a27dd2`

| Item | Detail |
| ---- | ------ |
| File | WPF_Example/UI/ControlItem/InspectionListView.xaml.cs |
| Lines | L503 (수정 — 세미콜론 → OR), L504 (추가 — 신규 OR 항) |
| Net change | +2 -1 (1 file, 2 insertions, 1 deletion) |
| Added text | `\|\| item.Param is DualImageEdgeDistanceMeasurement; //260530 hbk Phase 39.3 D-G1` |

기존 8 OR 항 (Edge 3 + Compound 4 + ArcLineIntersect 1) 라인 변경 0. using 추가 0 (DualImageEdgeDistanceMeasurement = ReringProject.Sequence 동일 namespace, 기존 `using ReringProject.Sequence;` 로 해결).

**효과:** DualImage FAI Measurement 노드 선택 시 `mParentWindow.mainView.btn_rectRoi.IsEnabled = true` 가 설정됨.

### Task 01-02 — MainView.xaml.cs (분기 B): FindSelectedRectMeasurement 양 분기 추가

**Commit:** `d3087f4`

| Item | Detail |
| ---- | ------ |
| File | WPF_Example/UI/ContentItem/MainView.xaml.cs |
| Lines | L1877 (트리 분기 +1), L1893 (dataGrid fallback +1) |
| Net change | +2 (1 file, 2 insertions) |
| Added text 트리 | `if (selParam is DualImageEdgeDistanceMeasurement) return (MeasurementBase)selParam; //260530 hbk Phase 39.3 D-G1` |
| Added text dataGrid | `if (m is DualImageEdgeDistanceMeasurement) return m; //260530 hbk Phase 39.3 D-G1` |

기존 7+1 (Edge 3 + Compound 4 + ArcLineIntersect 1) 분기 라인 변경 0. using 추가 0.

**효과:** 트리에서 DualImage Measurement 노드 또는 dataGrid 결과 행 선택 시 FindSelectedRectMeasurement() 가 DualImage 객체 반환 → `_editingMeasurement` set 가능.

### Task 01-03 — MainView.xaml.cs (분기 C, D-G1 핵심): CommitRectRoi 슬롯-종속 write-back

**Commit:** `7d02766`

| Item | Detail |
| ---- | ------ |
| File | WPF_Example/UI/ContentItem/MainView.xaml.cs |
| Lines | L1659-1672 (cShort 분기 직후, ali 분기 직전) |
| Net change | +13 (1 file, 13 insertions) |
| Logic | `var dualMeas = _editingMeasurement as DualImageEdgeDistanceMeasurement` → `if (dualMeas != null)` → `_currentImageSource == EImageSource.Vertical` ? LineROI_* 5필드 set : PointROI_* 5필드 set |

위치 가드: ArcLineIntersect 4-ROI 순차 분기 (`_editingMeasurementRoiIndex`) **진입 전** 에 위치 → DualImage 분기가 RoiIndex 사이드 이펙트 0. 기존 7+1 분기 라인 변경 0.

**Plan 02 인터페이스 (의존):**
- `_currentImageSource` 필드 (private, L64, EImageSource enum) — Plan 01 은 read-only 참조 (set 안 함).
- default value = `EImageSource.Horizontal` 보장 → Plan 02 미진입 상태에서도 else 절 (PointROI 라우팅) 이 always 동작 → 빌드 안전.

**효과:** 캔버스에서 RectROI 드래그 → 마우스 업 시 `_currentImageSource` 가 Vertical 이면 LineROI_*, Horizontal (기본) 이면 PointROI_* 필드에 5값 (Row/Col/Phi/Length1/Length2) 라우팅 write-back.

### Task 01-04 — MainView.xaml.cs (분기 D): BuildPointRoiDefinitions DualImage 2-RoiDefinition

**Commit:** `fc3b854`

| Item | Detail |
| ---- | ------ |
| File | WPF_Example/UI/ContentItem/MainView.xaml.cs |
| Lines | L389-414 (ArcLineIntersect `return result;` 직후, 단일 RoiDefinition 분기 직전) |
| Net change | +24 (1 file, 24 insertions) |
| RoiId suffix | `_Point` / `_Line` (SP-5 컨벤션, ArcLineIntersect `_EdgeA1` 4-suffix 패턴 차용) |
| 부분 티칭 가드 | `dual.PointROI_Length1 > 0 && dual.PointROI_Length2 > 0` (Point 만 또는 Line 만 티칭된 상태도 정상 처리) |

`return result;` 조기 종료로 단일 RoiDefinition 분기 (L390+, `pRow/pCol/pLen1/pLen2`) 진입 차단 → DualImage 가 빈 ROI 로 반환되는 결함 회피. ArcLineIntersect 분기 (L348-388) + 단일 RoiDefinition 분기 (L390-415) 라인 변경 0.

**효과:** BuildPointRoiDefinitions 가 DualImage 에 대해 2개 RoiDefinition (Point + Line) 을 반환 → 캔버스 재 렌더 시 양 ROI 가 동시에 그려짐.

## Verification Results

### msbuild Debug/x64 (각 task 후 누적)

| Task | Errors | New Warnings | Status |
| ---- | ------ | ------------ | ------ |
| Task 01-01 | 0 | 0 | PASS |
| Task 01-02 | 0 | 0 | PASS |
| Task 01-03 | 0 | 0 | PASS |
| Task 01-04 | 0 | 0 | PASS |

빌드 출력: `WPF_Example/bin/x64/Debug/DatumMeasurement.exe`

**Pre-existing warnings (Phase 39.2 baseline, 본 plan 무관):**
- MSB3884 — MinimumRecommendedRules.ruleset 미존재 (Phase 33 도입)
- CS0618 — Phase 33 deprecation (TopSequence / BottomSequence / TopInspectionAction / BottomInspectionAction, 6건)
- CS0162 — VirtualCamera.cs L266 unreachable code
- MSB3088 — GenerateResource.cache 입력 스트림 (transient, 사전 존재)

### Anti-Goal 가드 (코드 인스펙션)

| Anti-Goal | File | Result |
| --------- | ---- | ------ |
| #1 | `WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` | 변경 0 lines (PASS) |
| #2 | `WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs` | 변경 0 lines (PASS) |
| #3 | `WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` | 변경 0 lines (PASS) |
| 7 타입 회귀 | Edge 3 + Compound 4 + ArcLineIntersect 분기 라인 변경 0 (분기 A/B/C/D 모두 추가만, 수정 0) | PASS |
| Datum 경로 무관 | `_selectedDatumForSwap` / `PublishDatumRoiCandidates` / `BtnSwap*_Click` Datum 경로 변경 0 | PASS |

### grep 검증

| Pattern | File | Expected | Actual | Result |
| ------- | ---- | -------- | ------ | ------ |
| `is DualImageEdgeDistanceMeasurement` | InspectionListView.xaml.cs | 1+ | 1 (L504) | PASS |
| `DualImageEdgeDistanceMeasurement` | MainView.xaml.cs | 4 (트리 + dataGrid + CommitRectRoi + BuildPointRoiDefinitions) | 4 (L391, L1684, L1877, L1893) | PASS |
| `Phase 39.3 D-G1` | MainView.xaml.cs | 다수 (3 task 합) | 15 hits | PASS |
| `_currentImageSource == ReringProject.Sequence.EImageSource.Vertical` | MainView.xaml.cs (CommitRectRoi) | 1+ | 2 (L599 기존 + L1662 신규) | PASS |
| `dualMeas.PointROI_Row` | MainView.xaml.cs | 1 | 1 (L1668) | PASS |
| `dualMeas.LineROI_Row` | MainView.xaml.cs | 1 | 1 (L1664) | PASS |
| `dual.PointROI_Length1 > 0` | MainView.xaml.cs | 1 | 1 (L393) | PASS |
| `dual.LineROI_Length1 > 0` | MainView.xaml.cs | 1 | 1 (L402) | PASS |

### CLAUDE.md / Project Skills 컴플라이언스

- 코멘트 컨벤션 (`//YYMMDD hbk` 필수): 모든 신규 라인에 `//260530 hbk Phase 39.3 D-G1` 마커 — PASS
- C# 7.2 호환 (`var x = obj as T; if (x != null)` 패턴, switch expression 미사용) — PASS
- 파일 스타일 일치 (K&R brace — 두 파일 모두 K&R) — PASS
- using 추가 0 (DualImageEdgeDistanceMeasurement = 기존 `using ReringProject.Sequence;` 으로 해결) — PASS

## Plan 02 Interface (Hand-off)

본 Plan 01 이 다음 Plan 02 에 제공하는 인터페이스:

1. **`_currentImageSource` 의 default = Horizontal 의존성:** CommitRectRoi 의 DualImage 분기 (Task 01-03) 가 `_currentImageSource` 를 read-only 참조. Plan 02 의 swap UX (Vertical / Horizontal 토글) 가 도착하지 않은 상태에서도 PointROI 5필드 셋팅이 always 동작 (else 절). Plan 02 가 도착하면 Vertical 슬롯에서 LineROI 5필드 셋팅 자동 활성.

2. **RectROI 활성화 baseline 완료:** Plan 02 는 swap 토글 + RectROI 활성화 진입점 wiring 만 추가하면 됨 — RectROI 자체의 드로잉 / write-back / 재 렌더 파이프라인은 Plan 01 으로 완성.

3. **2-RoiDefinition 렌더링 baseline 완료:** Plan 03 (DualImageEdgeDistanceMeasurement 실행 로직) 이 PointROI / LineROI 5필드를 read 만 하므로 본 plan 의 write-back 만으로 Plan 03 진입 후 즉시 실측 가능 (단, Plan 03 의 fit_line + projection_pl 구현 도착 후).

## Deviations from Plan

None — plan 이 PATTERNS.md 의 코드 블록을 byte-identical 로 인용했고 4 task 모두 1차 commit 으로 통과.

## Self-Check

### File existence
- WPF_Example/UI/ControlItem/InspectionListView.xaml.cs — FOUND (L503-504 변경 확인)
- WPF_Example/UI/ContentItem/MainView.xaml.cs — FOUND (L389-414, L1659-1672, L1877, L1893 변경 확인)

### Commit existence
- 0a27dd2 (Task 01-01) — FOUND
- d3087f4 (Task 01-02) — FOUND
- 7d02766 (Task 01-03) — FOUND
- fc3b854 (Task 01-04) — FOUND

### Anti-Goal #1~#3 (DualImageEdgeDistanceMeasurement / MeasurementFactory / Action_FAIMeasurement 변경 0)
- 모두 `git diff HEAD <file>` 0 lines — FOUND

## Self-Check: PASSED
