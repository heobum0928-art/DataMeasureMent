---
phase: 39.3-dualimage-fai-ux-redesign-2026-05-30
plan: 02
type: execute
wave: 2
depends_on: ["39.3-01"]
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: true
requirements: [WF-01]
tags: [ui, wpf, dualimage, swap, mutex]

must_haves:
  truths:
    - "DualImage FAI Measurement 노드 선택 시 btn_swapHorizontal / btn_swapVertical / border_imageSourceBadge 가 Visible 로 전환된다"
    - "Datum DualImage 노드 ↔ Measurement DualImage 노드 전환 시 _selectedDatumForSwap 와 _selectedDualImageMeasurement 가 둘 다 non-null 인 순간이 0 (mutex 보장)"
    - "btn_swapHorizontal 클릭 시 Measurement 가 selected 면 ShotConfig 이미지 로드 + 배지 가로축 전환 후 return (Datum 코드 진입 X)"
    - "btn_swapVertical 클릭 시 Measurement 가 selected 면 meas.TeachingImagePath_Vertical 로드 + 배지 세로축 전환 후 return"
    - "Measurement 노드 새 선택 시 _currentImageSource = Horizontal 로 리셋된다 (Risk R6 회피)"
    - "Plan 01 의 CommitRectRoi 분기가 Plan 02 의 _currentImageSource Vertical 토글과 연동되어 LineROI 라우팅 동작"
    - "Datum DualImage swap 동작 (Phase 34.1 / 36 / 37) 회귀 0"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "_selectedDualImageMeasurement 필드 + PublishMeasurementDualImageSelection public 메서드 + BtnSwap*_Click Measurement 우선순위 분기 + UpdateImageSourceBadge publish 분기 + PublishDatumRoiCandidates mutex clear"
      contains: "_selectedDualImageMeasurement"
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "Measurement 노드 분기에서 mainView.PublishMeasurementDualImageSelection 호출"
      contains: "PublishMeasurementDualImageSelection"
  key_links:
    - from: "InspectionListView 트리 Measurement 노드 선택"
      to: "MainView._selectedDualImageMeasurement (set or null)"
      via: "mainView.PublishMeasurementDualImageSelection(itemParam as DualImageEdgeDistanceMeasurement)"
      pattern: "PublishMeasurementDualImageSelection"
    - from: "BtnSwapHorizontal_Click / BtnSwapVertical_Click"
      to: "_currentImageSource 갱신 + 이미지 로드"
      via: "_selectedDualImageMeasurement != null 우선순위 분기 → return"
      pattern: "var meas = _selectedDualImageMeasurement"
    - from: "PublishDatumRoiCandidates 진입"
      to: "_selectedDualImageMeasurement = null (mutex clear)"
      via: "datum != null 가드 직전"
      pattern: "_selectedDualImageMeasurement = null"
    - from: "UpdateImageSourceBadge 진입 (publish 분기)"
      to: "RenderInspectionResultForNode(_selectedDualImageMeasurement)"
      via: "Datum publish 가드 직후 Measurement 가드"
      pattern: "RenderInspectionResultForNode\\(_selectedDualImageMeasurement\\)"
---

<objective>
**Plan 02 — 가로/세로 이미지 슬롯 swap UX wiring (D-G2)**

Side Datum DualImage (Phase 34.1 / 36 / 37) 의 swap UX 인프라 (`btn_swapHorizontal/Vertical`, `_currentImageSource`, `UpdateImageSourceBadge`, `BadgeBrush*`) 를 그대로 재사용하면서, Measurement DualImage 도 동일 swap 동작이 가능하도록 wiring E~J (6 지점) 를 추가한다. **Datum/Measurement 상호 배제 (mutex)** 가 본 Plan 의 핵심 설계.

Purpose: Plan 01 의 RectROI 분기 C (`_currentImageSource` 종속 라우팅) 가 실제 swap 토글로 Vertical 전환 가능해져야 LineROI 드로잉이 가능. D-G2 의 "사용자 인지 부담 0 — 이미지 보면서 그 이미지의 ROI 만 그림" 메커니즘 완성.
Output: Datum DualImage 와 Measurement DualImage 가 swap 인프라를 공유하면서 mutex 로 안전 격리. Datum 회귀 0 + Measurement DualImage 풀 UX 활성.

**Wave 2 단독 실행** — Plan 01 의 분기 C/D 의존 (`_currentImageSource` 참조 + 2 RoiDefinition 반환). Plan 03 와는 파일 충돌 0 (Plan 03 = Measurements/DualImageEdgeDistanceMeasurement.cs 만).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-CONTEXT.md
@.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-RESEARCH.md
@.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-PATTERNS.md
@.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-01-PLAN.md
@WPF_Example/UI/ContentItem/MainView.xaml.cs
@WPF_Example/UI/ContentItem/MainView.xaml
@WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
@WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs
@WPF_Example/Custom/Sequence/Inspection/EImageSource.cs

<interfaces>
<!-- Key types and contracts (Datum 측 패턴 대칭) -->

MainView.xaml.cs 기존 swap 인프라 (재사용):
- private ReringProject.Sequence.EImageSource _currentImageSource = ReringProject.Sequence.EImageSource.Horizontal; (L64)
- private DatumConfig _selectedDatumForSwap; (L68)
- private static readonly System.Windows.Media.Brush BadgeBrushHorizontal / BadgeBrushVertical; (L71-77, frozen)
- private void UpdateImageSourceBadge(ReringProject.Sequence.EImageSource source); (L1265-1296)
- private void BtnSwapHorizontal_Click(object sender, RoutedEventArgs e); (L1311-1325)
- private void BtnSwapVertical_Click(object sender, RoutedEventArgs e); (L1327-1341)
- private void PublishDatumRoiCandidates(DatumConfig datum); (L1343-1430+)

MainView.xaml 토글 버튼 (재사용, XAML 변경 0):
- <ToggleButton x:Name="btn_swapHorizontal" Click="BtnSwapHorizontal_Click" Visibility="Collapsed" />
- <ToggleButton x:Name="btn_swapVertical" Click="BtnSwapVertical_Click" Visibility="Collapsed" />
- <Border x:Name="border_imageSourceBadge" Visibility="Collapsed" /> — 배지

DualImageEdgeDistanceMeasurement 인터페이스:
- public string TeachingImagePath_Vertical { get; set; } — Plan 02 BtnSwapVertical_Click 에서 로드 대상
- 본체는 ReringProject.Sequence namespace

기존 MainView helper (재사용):
- private FAIConfig FindFAIContainingMeasurement(MeasurementBase meas); (L1802 부근 — hotfix C, b 추적용)
- public void RenderInspectionResultForNode(object node); (또는 동등 메서드명 — MainView 코드 내 검색)

ShotConfig / Halcon (재사용):
- ShotConfig.HasImage / GetImage() — Phase 37 hotfix 패턴
- halconViewer.LoadImage(string path) / LoadImage(HImage img)

신규 추가 (Plan 02 만 의무):
- private DualImageEdgeDistanceMeasurement _selectedDualImageMeasurement; (필드 1)
- public void PublishMeasurementDualImageSelection(DualImageEdgeDistanceMeasurement meas); (public 메서드 1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 02-01: MainView.xaml.cs — _selectedDualImageMeasurement 필드 + PublishMeasurementDualImageSelection 신규 public 메서드 (wiring E + F)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (L60-80 — `_currentImageSource` + `_selectedDatumForSwap` 필드 선언 위치, L1343-1430 — `PublishDatumRoiCandidates` 본체)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-PATTERNS.md (분기 E + F 섹션 — L263-322)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-RESEARCH.md (RT-4 표 E + F + Risk R1 mutex + Risk R6 슬롯 리셋)
    - **사전 grep lock-in (Warning #3):** `grep -n "OnSelectedDatumPropertyChanged\|_selectedDatum.*PropertyChanged" WPF_Example/UI/ContentItem/MainView.xaml.cs` 으로 정확한 핸들러 메서드명을 lock-in. 결과 라인을 SUMMARY 에 기록.
    - WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs (필드 타입 확인)
  </read_first>
  <action>
    //260530 hbk Phase 39.3 D-G2 주석 의무.

    **0) 사전 grep lock-in (Warning #3 — 메서드명 확정 후 코드 삽입):**
    위 read_first 의 grep 결과로 `OnSelectedDatumPropertyChanged` (또는 동등 핸들러명) 의 정확한 메서드명을 확인. 본 task 의 mutex unsubscribe 블록 (`_selectedDatumForSwap.PropertyChanged -= XXX;`) 의 XXX 자리에 정확 메서드명을 채워서 코드 삽입. 만약 grep 결과 핸들러가 0 hit 또는 WeakEventManager 등 다른 메커니즘이면 RESEARCH.md / PATTERNS.md 의 Datum 측 unsubscribe 패턴 재확인 후 동일 패턴 적용.

    **A) 신규 필드 추가** — MainView.xaml.cs L68 `_selectedDatumForSwap` 선언 직후 (L69 위치):

    ```csharp
    //260530 hbk Phase 39.3 D-G2 — 현재 선택된 DualImage Measurement (Datum 의 _selectedDatumForSwap 대칭, mutex 페어).
    //  세션 한정, INI 미저장. InspectionListView Measurement 노드 선택 시 set, Datum 노드 선택 시 clear.
    private DualImageEdgeDistanceMeasurement _selectedDualImageMeasurement;
    ```

    **B) 신규 public 메서드 추가** — `PublishDatumRoiCandidates` 메서드 (L1343 부근) **직전** 또는 직후 (소스 순서 일관성 위해 직후 권장):

    ```csharp
    //260530 hbk Phase 39.3 D-G2 — PublishDatumRoiCandidates 대칭. Measurement DualImage 노드 선택 시 swap UI owner set + 가로축 리셋 + Visibility 제어.
    //  mutex: 본 메서드와 PublishDatumRoiCandidates 는 _selectedDatumForSwap / _selectedDualImageMeasurement 중 하나만 non-null 임을 보장.
    public void PublishMeasurementDualImageSelection(DualImageEdgeDistanceMeasurement meas) {
        _selectedDualImageMeasurement = meas; //260530 hbk Phase 39.3 D-G2

        //260530 hbk Phase 39.3 D-G2 — mutex: Measurement set 시 Datum clear (Risk R1 회피)
        if (meas != null && _selectedDatumForSwap != null) {
            _selectedDatumForSwap.PropertyChanged -= OnSelectedDatumPropertyChanged; //260530 hbk Phase 39.3 D-G2
            _selectedDatumForSwap = null; //260530 hbk Phase 39.3 D-G2
        }

        //260530 hbk Phase 39.3 D-G2 — Visibility 제어 (PublishDatumRoiCandidates L1363-1365 패턴 차용)
        bool isDualImage = (meas != null);
        if (btn_swapHorizontal != null) btn_swapHorizontal.Visibility = isDualImage ? Visibility.Visible : Visibility.Collapsed;
        if (btn_swapVertical   != null) btn_swapVertical.Visibility   = isDualImage ? Visibility.Visible : Visibility.Collapsed;
        if (border_imageSourceBadge != null) border_imageSourceBadge.Visibility = isDualImage ? Visibility.Visible : Visibility.Collapsed;

        if (isDualImage) {
            //260530 hbk Phase 39.3 D-G2 — 새 노드 진입 시 가로축 리셋 (Datum D-34.1-08 대칭, Risk R6 회피)
            _currentImageSource = ReringProject.Sequence.EImageSource.Horizontal;
            UpdateImageSourceBadge(ReringProject.Sequence.EImageSource.Horizontal); //260530 hbk Phase 39.3 D-G2
        }
    }
    ```

    **검증 사항:**
    - `OnSelectedDatumPropertyChanged` 이벤트 핸들러는 기존 Datum 측 코드 (Phase 34.1 hotfix CO-34.1-02) 에서 정의됨 → MainView.xaml.cs 내 검색하여 정확한 메서드명 확인. 만약 다른 이름이면 PATTERNS.md L401-405 의 `_selectedDatumForSwap.PropertyChanged -= XXX` 패턴에서 X 부분만 교체. 만약 PropertyChanged 핸들러가 unsubscribe 형태가 아닌 다른 메커니즘 (e.g. WeakEventManager 등) 이면 동일 패턴 따름.
    - `btn_swapHorizontal` / `btn_swapVertical` / `border_imageSourceBadge` 이름이 MainView.xaml 의 x:Name 과 일치 (RESEARCH.md RT-1.1 verified)
    - `Visibility` = `System.Windows.Visibility` — 이미 using 으로 해결됨
    - 본 메서드는 InspectionListView.xaml.cs (Task 02-02) 에서 호출되므로 **public**

    회귀 가드:
    - 기존 Datum 측 필드 / 메서드 / 핸들러 변경 0
    - mutex unsubscribe 패턴은 PATTERNS.md L401-405 Datum 측 패턴과 동일
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 — PASS 0 new errors/warnings</automated>
    grep 검증:
    - `grep -n "_selectedDualImageMeasurement" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 3+ 라인 hit (필드 1 + 메서드 set 1 + mutex clear/null check 등)
    - `grep -n "public void PublishMeasurementDualImageSelection" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 라인 hit
    - `grep -n "border_imageSourceBadge" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 메서드 안 1+ 라인 hit
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 PASS, 0 new errors, 0 new warnings
    - `private DualImageEdgeDistanceMeasurement _selectedDualImageMeasurement;` 라인이 L68 부근에 정확히 1개 존재
    - `public void PublishMeasurementDualImageSelection(DualImageEdgeDistanceMeasurement meas)` 시그니처가 정확히 1개 존재
    - 메서드 본체에 mutex clear 블록 (`_selectedDatumForSwap.PropertyChanged -=` + `_selectedDatumForSwap = null`) 존재
    - Visibility 3종 (btn_swapHorizontal / btn_swapVertical / border_imageSourceBadge) 모두 set
    - `_currentImageSource = Horizontal` + `UpdateImageSourceBadge(Horizontal)` 가 `isDualImage` 분기 안에 위치
  </acceptance_criteria>
  <done>
    `_selectedDualImageMeasurement` 필드 + `PublishMeasurementDualImageSelection` 메서드가 추가되어 Task 02-02 (InspectionListView 콜아웃) 의 진입점 확보. mutex (Datum clear) 가 본 메서드 안에서 보장.
  </done>
</task>

<task type="auto">
  <name>Task 02-02: InspectionListView.xaml.cs — Measurement 노드 분기에서 mainView.PublishMeasurementDualImageSelection 호출 (wiring F)</name>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs (L480-525 — Measurement 노드 처리 분기 전체, Task 01-01 에서 isRectRoiType 추가 후 상태)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-PATTERNS.md (분기 F 섹션 — L283-299)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-RESEARCH.md (RT-4 표 F)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (Task 02-01 완료 후 `PublishMeasurementDualImageSelection` 메서드 존재 확인)
  </read_first>
  <action>
    //260530 hbk Phase 39.3 D-G2 주석 의무.

    InspectionListView.xaml.cs 의 Measurement 노드 분기 (L491-522 부근, Task 01-01 의 isRectRoiType 화이트리스트 추가 위치와 같은 블록) **끝부분** 에 PATTERNS.md L294-298 의 콜아웃 추가:

    ```csharp
    //260530 hbk Phase 39.3 D-G2 — Measurement DualImage 선택 시 swap UI owner set (mutex). 비-DualImage Measurement 는 null 로 clear.
    if (mParentWindow != null && mParentWindow.mainView != null) {
        mParentWindow.mainView.PublishMeasurementDualImageSelection(itemParam as DualImageEdgeDistanceMeasurement);
    }
    ```

    **변수명 검증:**
    - `itemParam` 또는 `item.Param` — Task 01-01 의 isRectRoiType 분기에서 사용한 변수명과 동일 변수 사용. 만약 로컬 변수 `itemParam` 이 없으면 `item.Param` 으로 교체.
    - `mParentWindow.mainView` — 동 파일의 다른 메서드 콜아웃 (e.g. L485-488 의 `btn_teachDatum.IsEnabled` 패턴) 과 동일 접근 패턴

    **위치 정확화:** Task 01-01 의 `btn_rectRoi.IsEnabled = isRectRoiType;` 라인 (L505 부근) 직후가 가장 자연스러움. 같은 Measurement 분기 안에 있어야 함.

    **로직 의미:** `as DualImageEdgeDistanceMeasurement` 캐스트 결과가:
    - non-null (= DualImage Measurement 선택) → MainView 에서 set + Visibility Visible + Horizontal 리셋
    - null (= 비-DualImage Measurement 또는 다른 타입) → MainView 에서 null 로 clear + Visibility Collapsed → swap UI 숨김

    회귀 가드:
    - 기존 Measurement 노드 처리 분기 라인 변경 0 (위 콜아웃 추가만)
    - Task 01-01 의 isRectRoiType 추가 라인 변경 0
    - using 추가 0 (`DualImageEdgeDistanceMeasurement` 동일 namespace)
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 — PASS 0 new errors/warnings</automated>
    grep 검증:
    - `grep -n "PublishMeasurementDualImageSelection" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` → 1 라인 hit
    - `grep -n "Phase 39.3 D-G2" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` → 1+ 라인 hit
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 PASS, 0 new errors, 0 new warnings
    - InspectionListView.xaml.cs 의 Measurement 분기 안에 `mParentWindow.mainView.PublishMeasurementDualImageSelection(... as DualImageEdgeDistanceMeasurement)` 콜아웃 정확히 1회
    - 콜아웃이 `mParentWindow != null && mParentWindow.mainView != null` null 가드 안에 위치 (기존 다른 콜아웃 패턴 일치)
    - 비-DualImage Measurement 선택 시도 메서드가 호출되어 swap UI 가 자동 Collapsed (`as` 캐스트 결과 null 로 인한 clear) — 의도된 동작
  </acceptance_criteria>
  <done>
    Measurement 노드 트리 선택 시 즉시 `_selectedDualImageMeasurement` 가 set 또는 null clear. DualImage 면 swap UI 표시, 비-DualImage 면 swap UI 자동 숨김.
  </done>
</task>

<task type="auto">
  <name>Task 02-03: MainView.xaml.cs — PublishDatumRoiCandidates 진입 시 Measurement clear (wiring I, mutex 양방향)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (L1343-1380 — PublishDatumRoiCandidates 본체 진입부, `priorSelected = _selectedDatumForSwap` 캐싱 위치 L1352-1357 부근)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-PATTERNS.md (분기 I 섹션 — L398-413)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-RESEARCH.md (Risk R1 mutex)
  </read_first>
  <action>
    //260530 hbk Phase 39.3 D-G2 주석 의무. PATTERNS.md L409-412 가 적용 코드 블록.

    `PublishDatumRoiCandidates(DatumConfig datum)` 메서드 (L1343 부근) 의 본문 진입부, `priorSelected = _selectedDatumForSwap` 캐싱 블록 (L1352-1357 부근, `if (_selectedDatumForSwap != datum)` 가 시작되는 라인) **직전** 에 mutex clear 블록 추가:

    ```csharp
    //260530 hbk Phase 39.3 D-G2 — mutex: Datum 노드 선택 시 Measurement clear (Risk R1 회피, PublishMeasurementDualImageSelection 와 대칭)
    if (datum != null && _selectedDualImageMeasurement != null) {
        _selectedDualImageMeasurement = null;
    }
    ```

    **로직 의미:**
    - `datum != null` = Datum 노드 진입 (clear 가 아닌 set 케이스)
    - `_selectedDualImageMeasurement != null` = Measurement DualImage 가 이전에 선택돼 있던 상태
    - 두 조건 모두 true → mutex 위반 → Measurement clear
    - clear 만 함 (Visibility 는 직후 PublishDatumRoiCandidates 본체에서 Datum 기준 재설정됨)

    회귀 가드:
    - PublishDatumRoiCandidates 의 나머지 코드 (Datum 측 priorSelected 캐싱 + ROI candidates 생성) 변경 0
    - Task 02-01 의 PublishMeasurementDualImageSelection 의 Datum clear 와 대칭 (양방향 mutex)
    - PublishDatumRoiCandidates(null) 호출 (Datum 선택 해제) 시도 영향 0 (`datum != null` 가드)
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 — PASS 0 new errors/warnings</automated>
    grep 검증:
    - `grep -n "_selectedDualImageMeasurement = null" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 2+ 라인 hit (Task 02-01 mutex clear + Task 02-03 mutex clear)
    - `grep -n "datum != null && _selectedDualImageMeasurement != null" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 라인 hit
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 PASS, 0 new errors, 0 new warnings
    - PublishDatumRoiCandidates 메서드 안에 mutex clear 블록 (`datum != null && _selectedDualImageMeasurement != null`) 정확히 1회 등장
    - 위치: `if (_selectedDatumForSwap != datum)` 블록 직전
    - 기존 Datum 측 priorSelected / unsubscribe / subscribe 로직 변경 0
  </acceptance_criteria>
  <done>
    Datum 노드 선택 시 자동으로 `_selectedDualImageMeasurement` clear → mutex 양방향 보장 → swap 핸들러가 안전하게 owner 분기 가능.
  </done>
</task>

<task type="auto">
  <name>Task 02-04: MainView.xaml.cs — BtnSwapHorizontal_Click / BtnSwapVertical_Click Measurement 우선순위 분기 추가 (wiring G)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (L1311-1341 — BtnSwapHorizontal_Click + BtnSwapVertical_Click 본체 + L1802 부근 FindFAIContainingMeasurement helper)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-PATTERNS.md (분기 G 섹션 — L328-368)
    - **사전 pre-flight grep (Warning #4 — ShotConfig.GetImage / halconViewer.LoadImage 시그니처 lock-in):** `grep -n "ShotConfig.*HasImage\|ShotConfig.*GetImage\|halconViewer.*LoadImage" WPF_Example/` 으로 ShotConfig.GetImage() 의 반환 타입 (HImage 직접 반환인지 string 경로 반환인지) 과 halconViewer.LoadImage(HImage) 오버로드 존재 여부를 확인. main 분기 (HImage 로드) vs fallback 분기 (SimulImagePath 경로 로드) 결정에 사용.
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-RESEARCH.md (Code Examples 의 Phase 39.3 swap handler 권장 패턴 L443-463)
  </read_first>
  <action>
    //260530 hbk Phase 39.3 D-G2 주석 의무. PATTERNS.md L330-366 가 적용 코드 블록.

    **0) 사전 pre-flight grep 결과로 분기 결정 (Warning #4):**
    위 read_first 의 grep 결과를 검토하여:
    - `ShotConfig.GetImage()` 가 `HImage` 직접 반환 + `halconViewer.LoadImage(HImage)` 오버로드 존재 → **main 분기** 적용 (아래 A 의 권장 코드 그대로)
    - 둘 중 하나라도 없음 → **fallback 분기** 적용 (`halconViewer.LoadImage(shot.SimulImagePath)` 등 string 경로 로드 — Fallback 옵션 섹션 참조)
    적용한 분기 (main / fallback) 를 SUMMARY 에 기록 의무.

    **A) `BtnSwapHorizontal_Click(object sender, RoutedEventArgs e)`** (L1311 부근) — 메서드 진입 **직후** (첫 라인, `var d = _selectedDatumForSwap;` 보다 위) 에 Measurement 우선순위 분기 삽입:

    ```csharp
    //260530 hbk Phase 39.3 D-G2 — Measurement DualImage 가 Datum 보다 우선 (mutex)
    {
        var meas = _selectedDualImageMeasurement;
        if (meas != null) {
            FAIConfig fai = FindFAIContainingMeasurement(meas); //260530 hbk Phase 39.3 D-G2 — 기존 helper (Phase 37 hotfix C, b)
            ShotConfig shot = fai != null ? fai.Owner as ShotConfig : null;
            if (shot != null && shot.HasImage) {
                HImage img = null;
                try {
                    img = shot.GetImage();
                    if (img != null) halconViewer.LoadImage(img);
                }
                catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, ex.Message); }
                finally { if (img != null) img.Dispose(); }
            }
            UpdateImageSourceBadge(ReringProject.Sequence.EImageSource.Horizontal); //260530 hbk Phase 39.3 D-G2
            return; //260530 hbk Phase 39.3 D-G2 — Measurement 경로 종결, Datum 코드 진입 X
        }
    }
    ```

    **B) `BtnSwapVertical_Click(object sender, RoutedEventArgs e)`** (L1327 부근) — 동일 위치 (메서드 진입 직후) 에 Measurement 분기 삽입:

    ```csharp
    //260530 hbk Phase 39.3 D-G2 — Measurement DualImage 가 Datum 보다 우선
    {
        var meas = _selectedDualImageMeasurement;
        if (meas != null) {
            string vpath = meas.TeachingImagePath_Vertical; //260530 hbk Phase 39.3 D-G2 — Measurement 필드
            if (!string.IsNullOrEmpty(vpath) && System.IO.File.Exists(vpath)) {
                try { halconViewer.LoadImage(vpath); }
                catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, ex.Message); }
            }
            UpdateImageSourceBadge(ReringProject.Sequence.EImageSource.Vertical); //260530 hbk Phase 39.3 D-G2
            return; //260530 hbk Phase 39.3 D-G2 — Measurement 경로 종결
        }
    }
    ```

    **변수명 검증:**
    - `FindFAIContainingMeasurement(meas)` — Phase 37 hotfix C, b 에서 추가된 helper (L1802 부근). 메서드명이 일치하지 않으면 grep 으로 정확한 메서드명 확인 후 교체.
    - `FAIConfig.Owner` 는 `ShotConfig` 로 캐스트 가능 (Phase 5/37 모델 검증)
    - `ShotConfig.HasImage` / `ShotConfig.GetImage()` — Phase 37 hotfix 패턴 (verified)
    - `HImage` = HalconDotNet 타입, 이미 using 으로 해결됨 (BtnSwapHorizontal_Click 기존 코드에 등장 안 함 — 만약 using 누락이면 파일 상단의 기존 using HalconDotNet; 확인)
    - `halconViewer.LoadImage(HImage)` 오버로드 존재 확인 — Datum 측 L1318 의 `LoadImage(string)` 와는 다른 오버로드. halconViewer 컨트롤 (HalconViewer 클래스) 의 시그니처 확인. 만약 `LoadImage(HImage)` 가 없으면 임시 파일 저장 → `LoadImage(path)` fallback 필요 (이 경우 ShotConfig.SimulImagePath 로 대체).

    **위치 정확화:** 메서드 진입 후 첫 코드 라인. 기존 Datum 코드 (`var d = _selectedDatumForSwap;`) 가 위치한 곳보다 **위**. Measurement 분기 안에서 `return` 으로 조기 종료하면 기존 Datum 코드는 변경 0.

    **Fallback 옵션 (assumption A3 검증 실패 시):**
    - `shot.GetImage()` 가 null 이거나 HImage 오버로드 없으면 → `ShotConfig.SimulImagePath` 활용 (Phase 22 IMG-01 패턴 — 이 필드는 ShotConfig 가 보유, Action_FAIMeasurement 의 InspectionImagePath 와 동의어).
    - 만약 GetImage(HImage) 호출이 빌드 에러나면, 임시 fallback: `halconViewer.LoadImage(shot.SimulImagePath);` 와 같은 string 경로 로드로 교체. SUMMARY 에 fallback 사유 기록.

    회귀 가드:
    - 기존 Datum 측 코드 (`var d = _selectedDatumForSwap;` 이하 전부) 변경 0
    - Measurement 분기는 `return` 으로 조기 종료 → Datum 코드 도달 X
    - mutex 가 Task 02-01/02-03 에서 보장됐으므로 `meas != null && _selectedDatumForSwap != null` 상태는 발생 X
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 — PASS 0 new errors/warnings</automated>
    grep 검증:
    - `grep -n "var meas = _selectedDualImageMeasurement" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 2 라인 hit (Horizontal + Vertical 각 1)
    - `grep -n "FindFAIContainingMeasurement(meas)" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 라인 hit (Horizontal 분기)
    - `grep -n "meas.TeachingImagePath_Vertical" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 라인 hit (Vertical 분기)
    - `grep -n "UpdateImageSourceBadge(ReringProject.Sequence.EImageSource.Vertical)" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 2+ 라인 hit (Task 02-04 Vertical + 기존 Datum 코드 사용처)
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 PASS, 0 new errors, 0 new warnings
    - BtnSwapHorizontal_Click 의 메서드 본문 첫 라인 그룹이 Measurement 우선순위 분기 (var meas + if (meas != null) + return)
    - BtnSwapVertical_Click 동일 구조 (var meas + if (meas != null) + return) — 가로/세로 분기 차이는 이미지 소스만 (ShotConfig 이미지 vs meas.TeachingImagePath_Vertical)
    - 두 핸들러 모두 `return` 으로 조기 종료 → 기존 Datum 코드 변경 0
    - try/catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, ex.Message); } 패턴 (SP-3) 준수
    - K&R 중괄호 스타일 (SP-4)
  </acceptance_criteria>
  <done>
    swap 토글 클릭 시 Measurement DualImage 가 selected 면 Measurement 경로 (ShotConfig 이미지 또는 TeachingImagePath_Vertical 로드) + 배지 갱신 후 return. Datum 선택 시는 기존 Datum 코드 그대로 동작 (회귀 0). Plan 01 의 CommitRectRoi 분기가 이 swap 동작과 연동되어 PointROI / LineROI 양쪽 라우팅 완성.
  </done>
</task>

<task type="auto">
  <name>Task 02-05: MainView.xaml.cs — UpdateImageSourceBadge 의 Measurement publish 분기 추가 (wiring H)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (L1265-1296 — UpdateImageSourceBadge 본체, 끝부분 publish 가드 L1289-1295)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-PATTERNS.md (분기 H 섹션 — L374-392)
    - **사전 grep lock-in (Warning #5 — RenderInspectionResultForNode 메서드명 확정):** `grep -n "RenderInspectionResultForNode\|DisplayMeasurementResultForNode\|RefreshResultDisplay" WPF_Example/UI/ContentItem/MainView.xaml.cs` 으로 ROI/결과 재 렌더 메서드의 정확한 이름을 확인. 0 hit 이면 PATTERNS.md L386 의 의도 ("ROI 재 렌더") 에 부합하는 후보 메서드를 추가 grep 으로 식별.
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-RESEARCH.md (RT-4 표 H + RT-1.2 UpdateImageSourceBadge 동작)
  </read_first>
  <action>
    //260530 hbk Phase 39.3 D-G2 주석 의무. PATTERNS.md L386-389 가 적용 코드 블록.

    **0) 사전 grep lock-in (Warning #5 — RenderInspectionResultForNode 정확 메서드명 확정):**
    위 read_first 의 grep 결과로 ROI/결과 재 렌더 메서드의 정확한 이름을 lock-in. 본 task 의 publish 분기 안 `RenderInspectionResultForNode(_selectedDualImageMeasurement);` 호출의 메서드명을 확정된 이름으로 교체. 사용한 메서드명을 SUMMARY 에 기록 의무.

    `UpdateImageSourceBadge(EImageSource source)` 메서드 (L1265-1296) **끝부분**, 기존 Datum publish 가드 (L1289-1295, `var datumForRoi = _selectedDatumForSwap;` ~ `PublishDatumRoiCandidates(datumForRoi);`) **직후** 에 Measurement publish 분기 추가:

    ```csharp
    //260530 hbk Phase 39.3 D-G2 — Measurement DualImage 선택 시 RenderInspectionResultForNode 재호출 (Datum 와 동일 컨셉, mutex 가 있어 둘 다 fire X)
    if (_selectedDualImageMeasurement != null) {
        RenderInspectionResultForNode(_selectedDualImageMeasurement);
    }
    ```

    **`RenderInspectionResultForNode` 메서드명 검증:**
    - MainView.xaml.cs 안에서 grep `RenderInspectionResultForNode` 또는 동등 메서드 (e.g. `DisplayMeasurementResultForNode`, `RefreshResultDisplay` 등) 검색.
    - 만약 정확한 메서드명이 다르면 정확 메서드명으로 교체 + SUMMARY 에 사용한 메서드명 기록.
    - 기능 요구사항: Measurement 노드 선택 시 트리에서 호출되는 결과/ROI 표시 메서드. Phase 37 hotfix A (1c11c35) 에서 `FindFAIContainingMeasurement` 와 짝을 이루는 표시 메서드.
    - **대체 옵션:** 만약 단일 메서드가 없고 여러 단계 (예: `BuildPointRoiDefinitions(meas, faiName)` + `halconViewer.SetMeasurementOverlay(...)`) 로 흩어져 있다면, swap 시 Plan 01 의 BuildPointRoiDefinitions 분기 D 가 호출되는 경로 (e.g. UpdateDisplayState, CollectShotRois 등) 를 호출. PATTERNS.md L386 의 의도는 "ROI 재 렌더" — 정확한 메서드는 코드베이스 grep 으로 결정.

    **위치 정확화:** 기존 Datum publish 가드 (`if (datumForRoi != null && ... AlgorithmType == VerticalTwoHorizontalDualImage) { PublishDatumRoiCandidates(datumForRoi); }`) 의 **직후**. mutex 가 보장된 상태 → Datum 분기와 Measurement 분기는 상호 배타.

    회귀 가드:
    - 기존 UpdateImageSourceBadge 본체 (a) (b) (c) 3 갱신 로직 변경 0
    - 기존 Datum publish 가드 변경 0
    - mutex 보장 (Task 02-01/02-03) — Datum 와 Measurement 동시 publish 시도 발생 X
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 — PASS 0 new errors/warnings</automated>
    grep 검증:
    - `grep -n "if (_selectedDualImageMeasurement != null)" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 2+ 라인 hit (Task 02-01 PublishMeasurementDualImageSelection mutex 가드 + Task 02-05 UpdateImageSourceBadge publish 가드)
    - `grep -n "RenderInspectionResultForNode\|DisplayMeasurementResultForNode" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 기존 메서드 정의 + 신규 호출 모두 hit (정확한 메서드명 확인됨)
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 PASS, 0 new errors, 0 new warnings
    - UpdateImageSourceBadge 메서드 안에 Measurement publish 분기 (`if (_selectedDualImageMeasurement != null)` + `RenderInspectionResultForNode` 또는 정확 메서드 호출) 정확히 1회 등장
    - 위치: 기존 Datum publish 가드 직후 (소스 라인 기준 인접)
    - Datum publish 가드 (`if (datumForRoi != null && ...)` 블록) 변경 0
    - 호출된 메서드명이 코드베이스에 실제 존재 (CS0103 컴파일 에러 방지)
  </acceptance_criteria>
  <done>
    swap 토글 시 `UpdateImageSourceBadge(EImageSource)` 가 (c) 단계에서 Measurement DualImage 가 selected 면 ROI/결과 재 렌더 → swap 직후 화면이 새 이미지에 맞는 ROI 표시. Plan 01 의 BuildPointRoiDefinitions 분기 D 의 2 RoiDefinition 반환과 연동되어 가로 슬롯에서는 Point ROI / 세로 슬롯에서도 동일 2 RoiDefinition (둘 다 표시).
  </done>
</task>

</tasks>

<verification>
**Plan 02 전체 검증 (Anti-Goal 가드 + Risk):**

1. **빌드 PASS 검증** (각 task 후 누적):
   - `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64` → 0 new errors, 0 new warnings

2. **신규 추가 코드 정확성** (grep):
   - `grep -c "_selectedDualImageMeasurement" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 5+ hit (필드 1 + Task 02-01 메서드 안 3 + Task 02-03 mutex clear 1 + Task 02-04 핸들러 2 + Task 02-05 publish 가드 1)
   - `grep -c "PublishMeasurementDualImageSelection" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` → 1 hit
   - `grep -c "PublishMeasurementDualImageSelection" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 hit (메서드 정의)

3. **Anti-Goal 가드 (코드 인스펙션)**:
   - `DualImageEdgeDistanceMeasurement.cs` 변경 0 (Plan 03 의 영역)
   - `MeasurementFactory.cs` 변경 0
   - `Action_FAIMeasurement.cs` 변경 0
   - 다른 7 Measurement 타입 라인 변경 0
   - **Datum 측 코드 (BtnSwap*_Click Datum 경로 / PublishDatumRoiCandidates Datum 측 로직 / OnSelectedDatumPropertyChanged) 변경 0** — Measurement 분기는 모두 `return` 또는 추가 블록으로 격리
   - XAML 파일 (MainView.xaml) 변경 0 — 기존 toggle button x:Name + Click 핸들러 재사용
   - INI 직렬화 / TeachingImagePath_Vertical property 이름 변경 0

4. **Mutex 검증 (Risk R1)**:
   - PublishMeasurementDualImageSelection 안에 `_selectedDatumForSwap = null` clear 존재
   - PublishDatumRoiCandidates 안에 `_selectedDualImageMeasurement = null` clear 존재
   - 양방향 mutex 보장

5. **Risk R6 검증**:
   - PublishMeasurementDualImageSelection 안에 `_currentImageSource = Horizontal` 리셋 + UpdateImageSourceBadge(Horizontal) 호출
</verification>

<success_criteria>
- 5 task 모두 msbuild Debug/x64 PASS (0 new errors, 0 new warnings)
- DualImage FAI Measurement 노드 선택 시 swap UI (btn_swapHorizontal/Vertical/border_imageSourceBadge) Visible
- 비-DualImage Measurement / 일반 FAI / Shot / Sequence 노드 선택 시 swap UI Collapsed (자동)
- mutex 양방향 보장 — `_selectedDatumForSwap` 와 `_selectedDualImageMeasurement` 동시 non-null 0
- swap 토글 → Measurement 우선순위 분기로 이미지 로드 + 배지 + ROI 재 렌더 3자 동시 갱신
- Plan 01 의 CommitRectRoi 분기 C 와 연동되어 가로 슬롯 = PointROI 라우팅, 세로 슬롯 = LineROI 라우팅
- Datum DualImage swap (Phase 34.1 / 36 / 37) 동작 회귀 0
</success_criteria>

<output>
After completion, create `.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-02-SUMMARY.md` with:
- 5 task changes (file:line + 추가 라인 수)
- Anti-Goal 가드 검증 결과 (위 verification 3~5 항)
- mutex 양방향 보장 확인 (Datum/Measurement clear 위치)
- assumption A3 (ShotConfig.GetImage HImage 오버로드) / RenderInspectionResultForNode 메서드명 확인 결과 — fallback 적용 여부 기록
- msbuild Debug/x64 결과
</output>
</content>
</invoke>