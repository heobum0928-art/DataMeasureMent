---
phase: 39.3-dualimage-fai-ux-redesign-2026-05-30
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs
autonomous: true
requirements: [WF-01]
tags: [propertygrid, attribute, dualimage, browse]

must_haves:
  truths:
    - "DualImage Measurement 의 TeachingImagePath_Vertical 셀에 PropertyGrid 의 [Browse...] 버튼이 노출된다"
    - "Browse 버튼 클릭 시 OpenFileDialog 가 표시되어 이미지 파일 (tiff/bmp/jpg/jpeg/png/All) 선택 가능"
    - "선택한 경로가 셀에 반영되고 INI 직렬화에 정상 저장"
    - "Datum 측 TeachingImagePath_Vertical (DatumConfig.cs) 에는 변경 0 (Anti-Goal)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs"
      provides: "TeachingImagePath_Vertical 에 [InputFilePath] + [AutoUpdateText] attribute 추가 + using ReringProject.Device;"
      contains: "InputFilePath(DeviceHandler.EXTENSION_IMAGE, DeviceHandler.FILTER_IMAGE)"
  key_links:
    - from: "DualImageEdgeDistanceMeasurement.TeachingImagePath_Vertical"
      to: "PropertyGrid Browse 버튼 → OpenFileDialog"
      via: "[InputFilePath(...)] PropertyTools attribute"
      pattern: "\\[InputFilePath\\("
    - from: "TeachingImagePath_Vertical setter"
      to: "PropertyGrid 즉시 갱신"
      via: "[AutoUpdateText] attribute"
      pattern: "\\[AutoUpdateText\\]"
---

<objective>
**Plan 03 — TeachingImagePath_Vertical Browse 버튼 (D-G3)**

`DualImageEdgeDistanceMeasurement.TeachingImagePath_Vertical` 에 PropertyTools `[InputFilePath]` + `[AutoUpdateText]` attribute 를 추가하여 PropertyGrid 셀에 [Browse...] 버튼 노출. `DeviceHandler.EXTENSION_IMAGE` / `DeviceHandler.FILTER_IMAGE` 상수 재사용 (`ModelFinderViewModel` 와 동일 패턴).

Purpose: 사용자가 string 직접 입력 (오타 가능) 대신 파일 다이얼로그로 이미지 파일 선택. Datum 측 동일 적용은 후속 phase (CONTEXT.md Deferred Ideas).
Output: 단일 파일 (Measurements/DualImageEdgeDistanceMeasurement.cs) 의 attribute 2개 + using 1개 추가. 코드 추가량 ~5 라인.

**Wave 1 병렬 실행** — Plan 01 과 다른 파일 + 의존성 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-CONTEXT.md
@.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-RESEARCH.md
@.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-PATTERNS.md
@WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs
@WPF_Example/UI/ViewModel/ModelFinderViewModel.cs
@WPF_Example/Custom/Device/DeviceHandler.cs

<interfaces>
<!-- Key contracts -->

PropertyTools 3.1.0 attribute (검증된 사용 사례):
- [InputFilePath(string defaultExt, string filter)] → PropertyGrid Browse 버튼 + Microsoft.Win32.OpenFileDialog
- [AutoUpdateText] → setter 시 즉시 INPC fire (입력 중에도 반영)
- 양 attribute 모두 PropertyTools.DataAnnotations namespace

DeviceHandler 상수 (재사용, 신규 0):
- public const string EXTENSION_IMAGE = ".tiff";
- public const string FILTER_IMAGE = "tiff Files(*.tiff)|*.tiff|bmp Files(*.bmp)|*.bmp|jpg Files(*.jpg)|*.jpg|jpeg Files(*.jpeg)|*.jpeg|png Files(*.png)|*.png|All Files(*.*)|*.*";
- namespace ReringProject.Device

ModelFinderViewModel.cs 의 use site (재사용 패턴):
```csharp
using PropertyTools.DataAnnotations;
using ReringProject.Device;

[InputFilePath(DeviceHandler.EXTENSION_MODEL, DeviceHandler.FILTER_MODEL)]
[AutoUpdateText]
public string ModelFile { get; set; }
```

수정 대상 (Before):
```csharp
[Category("Image|DualImage")] //260530 hbk Phase 39.2 D-G1
[System.ComponentModel.Description("LineROI 검출용 별도 이미지 경로 — Bottom E5 패턴 (Phase 37 VTH-DualImage 슬롯 재사용)")]
public string TeachingImagePath_Vertical { get; set; } = "";
```

Anti-Goal (변경 0):
- TeachingImagePath_Vertical property 이름 / get/set body / default value `""`
- INI 직렬화 순서
- DatumConfig.cs:46 의 TeachingImagePath_Vertical (별도 phase)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 03-01: DualImageEdgeDistanceMeasurement.cs — [InputFilePath] + [AutoUpdateText] attribute + using 추가</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs (전체 — 특히 L1-30 의 using 블록 + L24-28 의 TeachingImagePath_Vertical 선언)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-PATTERNS.md (Plan 03 섹션 — L421-479)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-RESEARCH.md (RT-3 PropertyTools attribute 검증)
    - WPF_Example/UI/ViewModel/ModelFinderViewModel.cs (L1-40 — using 블록 + L28-37 의 use site 패턴)
    - WPF_Example/Custom/Device/DeviceHandler.cs (L70-75 — EXTENSION_IMAGE / FILTER_IMAGE 상수 정의)
  </read_first>
  <action>
    //260530 hbk Phase 39.3 D-G3 주석 의무. PATTERNS.md L462-466 가 적용 코드 블록.

    **A) 파일 상단 using 추가** — 기존 using 블록 끝부분 (`using PropertyTools.DataAnnotations;` 가 이미 존재하면 추가 0, 없으면 추가):

    1. 먼저 `using PropertyTools.DataAnnotations;` 가 파일 상단에 이미 존재하는지 확인 (`[Category]` 가 동 파일에 이미 사용되므로 거의 확실히 존재).
    2. `using ReringProject.Device;` 가 존재하는지 확인. 없으면 using 블록 끝에 추가:
       ```csharp
       using ReringProject.Device; //260530 hbk Phase 39.3 D-G3 — DeviceHandler 상수 참조
       ```

    **B) TeachingImagePath_Vertical 에 attribute 2개 추가** — 기존 선언 (L24-26 부근):

    **Before**:
    ```csharp
    [Category("Image|DualImage")] //260530 hbk Phase 39.2 D-G1
    [System.ComponentModel.Description("LineROI 검출용 별도 이미지 경로 — Bottom E5 패턴 (Phase 37 VTH-DualImage 슬롯 재사용)")]
    public string TeachingImagePath_Vertical { get; set; } = "";
    ```

    **After**:
    ```csharp
    [Category("Image|DualImage")] //260530 hbk Phase 39.2 D-G1
    [System.ComponentModel.Description("LineROI 검출용 별도 이미지 경로 — Bottom E5 패턴 (Phase 37 VTH-DualImage 슬롯 재사용)")]
    [InputFilePath(DeviceHandler.EXTENSION_IMAGE, DeviceHandler.FILTER_IMAGE)] //260530 hbk Phase 39.3 D-G3
    [AutoUpdateText] //260530 hbk Phase 39.3 D-G3 — 즉시 INPC fire (ModelFinderViewModel L28-29 패턴)
    public string TeachingImagePath_Vertical { get; set; } = "";
    ```

    **변경 사항 요약 (정확히 3 라인 추가):**
    1. `using ReringProject.Device;` (이미 있으면 0)
    2. `[InputFilePath(DeviceHandler.EXTENSION_IMAGE, DeviceHandler.FILTER_IMAGE)]` 라인
    3. `[AutoUpdateText]` 라인

    **회귀 가드 (Anti-Goal):**
    - Property 이름 `TeachingImagePath_Vertical` 변경 0
    - get/set body (`{ get; set; } = "";`) 변경 0 → ParamBase reflection serializer (Phase 22 IMG-01 패턴) 회귀 0
    - INI 직렬화 순서 변경 0 (attribute 추가는 INI 순서 영향 없음 — ParamBase 가 정렬에 의존 X)
    - `[Category]` / `[Description]` 기존 attribute 변경 0
    - 동 파일의 다른 필드 / 메서드 변경 0
    - DatumConfig.cs:46 의 동명 property 변경 0 (Plan 03 의 영역은 Measurements/ 만)

    **Fallback (Risk R4 - LOW — `[InputFilePath]` + `[Category]` 호환성):**
    - 만약 빌드 PASS 후 PropertyGrid 에서 Browse 버튼이 미표시 (Plan 04 UAT 4 에서 검출) → 4 가지 대응 후보 (SUMMARY 에 기록):
      (a) `[Browsable(true)]` 명시 추가
      (b) `[Category]` 제거 (ModelFinderViewModel 패턴)
      (c) `[InputFilePath]` 가 `[Category]` 앞에 오도록 순서 교체
      (d) PropertyTools 3.1.0 의 다른 버전 attribute 사용
    - **Plan 03 의 의무는 attribute 적용 + 빌드 PASS 까지**. UI 동작 검증은 Plan 04 UAT 4 에서.
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 — PASS 0 new errors/warnings</automated>
    grep 검증:
    - `grep -n "InputFilePath(DeviceHandler.EXTENSION_IMAGE" WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` → 1 라인 hit
    - `grep -n "AutoUpdateText" WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` → 1 라인 hit
    - `grep -n "using ReringProject.Device" WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` → 1 라인 hit (이미 있었으면 변경 0)
    - `grep -n "TeachingImagePath_Vertical" WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` → 1 라인 hit (property 선언만, 다른 위치 변경 0)
    - `grep -n "TeachingImagePath_Vertical" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` → 기존 라인 수와 동일 (Anti-Goal 검증)
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 PASS, 0 new errors, 0 new warnings
    - DualImageEdgeDistanceMeasurement.cs 안에 `[InputFilePath(DeviceHandler.EXTENSION_IMAGE, DeviceHandler.FILTER_IMAGE)]` 정확히 1 라인
    - 동 파일에 `[AutoUpdateText]` 정확히 1 라인 (TeachingImagePath_Vertical 위)
    - `using ReringProject.Device;` 가 파일 상단 using 블록에 존재 (이미 있었으면 변경 0, 없었으면 추가)
    - `TeachingImagePath_Vertical` property 이름 / 타입 / get/set/default 변경 0 (line-for-line diff = 정확히 +2~3 라인 추가만, 기존 라인 수정 0)
    - `using PropertyTools.DataAnnotations;` 이 파일 상단에 존재 (이미 있으므로 변경 0)
    - DatumConfig.cs 변경 0 (CONTEXT.md Deferred Ideas — Datum 측 attribute 적용은 후속 phase)
  </acceptance_criteria>
  <done>
    DualImageEdgeDistanceMeasurement 의 TeachingImagePath_Vertical 필드에 PropertyTools `[InputFilePath]` + `[AutoUpdateText]` attribute 가 적용됨. 빌드 PASS. PropertyGrid Browse 버튼 동작 확인은 Plan 04 UAT 4 에서 (Risk R4 가드).
  </done>
</task>

</tasks>

<verification>
**Plan 03 전체 검증:**

1. **빌드 PASS 검증**:
   - `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64` → 0 new errors, 0 new warnings

2. **변경 정확성 (grep)**:
   - DualImageEdgeDistanceMeasurement.cs: `[InputFilePath` 1 hit, `[AutoUpdateText]` 1 hit, `using ReringProject.Device` 1 hit (이미 있으면 변경 0)

3. **Anti-Goal 가드 (코드 인스펙션)**:
   - DualImageEdgeDistanceMeasurement.cs 의 `TryExecute` 본문 변경 0 (`git diff` 결과는 L24-28 부근 attribute + 상단 using 만)
   - `MeasurementFactory.cs` 변경 0
   - `Action_FAIMeasurement.cs` 변경 0 (TryGrabOrLoadFaiDualImages 회귀 0)
   - 다른 7 Measurement 타입 변경 0
   - DatumConfig.cs 변경 0 (Datum 측 동일 attribute 미적용 — Deferred)
   - INI 직렬화 / property 이름 변경 0

4. **소요 라인 추정**: 
   - 추가 +2~3 라인 (using 1 추가 또는 0 + attribute 2)
   - 수정 0 라인
</verification>

<success_criteria>
- 1 task msbuild Debug/x64 PASS (0 new errors, 0 new warnings)
- `[InputFilePath]` + `[AutoUpdateText]` attribute 가 TeachingImagePath_Vertical 에 적용
- `DeviceHandler.EXTENSION_IMAGE` / `DeviceHandler.FILTER_IMAGE` 재사용 (ModelFinderViewModel L28 패턴 일치)
- DatumConfig.cs / Action_FAIMeasurement.cs / MeasurementFactory.cs 변경 0
- INI 직렬화 회귀 0 (property 이름 + get/set body 보존)
- Plan 01 / Plan 02 와 독립적으로 빌드 가능 — Wave 1 병렬 진입
</success_criteria>

<output>
After completion, create `.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-03-SUMMARY.md` with:
- 변경 파일 (DualImageEdgeDistanceMeasurement.cs) + 추가 라인 수
- using 추가 여부 (`using ReringProject.Device;` 가 이미 있었는지)
- Anti-Goal 가드 결과 (Datum 측 변경 0 / TryExecute 본문 변경 0)
- msbuild Debug/x64 결과
- Plan 04 UAT 4 에서 검증할 Risk R4 (Browse 버튼 표시 여부) 메모
</output>
</content>
</invoke>