---
phase: 39.3-dualimage-fai-ux-redesign-2026-05-30
plan: 03
subsystem: ui
tags: [propertygrid, attribute, dualimage, browse, propertytools, inputfilepath]

# Dependency graph
requires:
  - phase: 39.2-urgent-additions-2-2026-05-30
    provides: DualImageEdgeDistanceMeasurement (D-G1) + TeachingImagePath_Vertical 슬롯 (재사용)
  - phase: 37-side-dualimage-multi-datum
    provides: VTH-DualImage TeachingImagePath_Vertical 의 슬롯 패턴 + ModelFinderViewModel [InputFilePath] 적용 패턴
provides:
  - "DualImageEdgeDistanceMeasurement.TeachingImagePath_Vertical 셀에 PropertyGrid Browse 버튼 노출 (`[InputFilePath]`)"
  - "AutoUpdateText attribute 로 setter 즉시 INPC fire 보장 (입력 중에도 반영)"
  - "DeviceHandler.EXTENSION_IMAGE/FILTER_IMAGE 상수 재사용 — ModelFinderViewModel L28-29 패턴 일치"
affects:
  - 39.3-04 (UAT — Test 4 R4 Risk: Browse 버튼 PropertyGrid 표시 여부 시각 검증)
  - 후속 phase (Deferred — DatumConfig.TeachingImagePath / TeachingImagePath_Vertical 에 동일 attribute 적용)

# Tech tracking
tech-stack:
  added: []  # 신규 라이브러리 0 — PropertyTools.DataAnnotations 기존 사용 중 + DeviceHandler 기존 상수 재사용
  patterns:
    - "PropertyGrid [InputFilePath(ext, filter)] + [AutoUpdateText] 조합 — Browse 버튼 + Microsoft.Win32.OpenFileDialog 자동 노출"
    - "DeviceHandler.EXTENSION_IMAGE / FILTER_IMAGE 상수 재사용으로 이미지 경로 다이얼로그 필터 단일 소스"

key-files:
  created: []
  modified:
    - WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs

key-decisions:
  - "[InputFilePath(DeviceHandler.EXTENSION_IMAGE, DeviceHandler.FILTER_IMAGE)] + [AutoUpdateText] 2 attribute 추가 — ModelFinderViewModel L28-29 패턴 그대로"
  - "using ReringProject.Device; 신규 추가 — DeviceHandler 상수 참조 (기존 using 블록 끝부분)"
  - "TeachingImagePath_Vertical property 이름/타입/get/set/default 변경 0 — ParamBase reflection serializer 회귀 0"
  - "DatumConfig.cs 변경 0 — Datum 측 동일 attribute 적용은 후속 phase Deferred (CONTEXT.md Deferred Ideas)"

patterns-established:
  - "PropertyGrid 파일 경로 셀 = [Category] + [Description] + [InputFilePath] + [AutoUpdateText] 4-attribute 스택 (Phase 22 IMG-01 의 단순 string 슬롯에 비해 사용자 입력 편의 보강)"

requirements-completed: [WF-01]

# Metrics
duration: 2min
completed: 2026-05-30
---

# Phase 39.3 Plan 03: DualImage TeachingImagePath_Vertical Browse 버튼 (D-G3) Summary

**DualImageEdgeDistanceMeasurement.TeachingImagePath_Vertical 에 PropertyTools `[InputFilePath]` + `[AutoUpdateText]` attribute 2개 추가로 PropertyGrid 셀에 [Browse...] 버튼 노출 — ModelFinderViewModel 패턴 일치 + DeviceHandler 상수 재사용**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-05-30T03:34:23Z
- **Completed:** 2026-05-30T03:35:51Z
- **Tasks:** 1/1
- **Files modified:** 1 (DualImageEdgeDistanceMeasurement.cs)
- **Lines changed:** +3 / -0 (using 1 + attribute 2, 기존 라인 수정 0)

## Accomplishments
- `DualImageEdgeDistanceMeasurement.TeachingImagePath_Vertical` 에 `[InputFilePath(DeviceHandler.EXTENSION_IMAGE, DeviceHandler.FILTER_IMAGE)]` + `[AutoUpdateText]` attribute 추가 — PropertyGrid Browse 버튼 노출 준비 완료
- `using ReringProject.Device;` 추가 — DeviceHandler 상수 참조 (ModelFinderViewModel L3 패턴 일치, 기존 using 블록은 `using ReringProject.Halcon.Algorithms;` / `using ReringProject.Halcon.Models;` 만 존재하여 신규 추가)
- msbuild Debug/x64 PASS, 0 new errors, 0 new warnings (baseline warning 7건 — CS0618 Phase 33 obsolete migration + CS0162 VirtualCamera + MSB3884 ruleset, 모두 pre-existing)

## Task Commits

Each task was committed atomically:

1. **Task 03-01: DualImageEdgeDistanceMeasurement.cs — [InputFilePath] + [AutoUpdateText] attribute + using 추가** — `f260cf0` (feat)

_단일 task plan — 1 commit._

## Files Created/Modified
- `WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` — using ReringProject.Device 추가 (L5) + TeachingImagePath_Vertical 위 `[InputFilePath]` + `[AutoUpdateText]` 2 attribute 추가 (L27-L28). +3 lines, -0 modifications.

## using 추가 여부

**`using ReringProject.Device;` — 추가됨 (신규)**

기존 using 블록 (L2-6) 에는 `System.Collections.Generic`, `HalconDotNet`, `PropertyTools.DataAnnotations`, `ReringProject.Halcon.Algorithms`, `ReringProject.Halcon.Models` 만 존재 — DeviceHandler 참조가 없었으므로 신규 추가. ModelFinderViewModel.cs L3 (`using ReringProject.Device;`) 패턴 일치.

`using PropertyTools.DataAnnotations;` 는 기존 L4 에 존재 (`[Category]` / `[ItemsSourceProperty]` 등 동 파일에서 이미 사용 중) — 변경 0.

## Anti-Goal 가드 결과

| Anti-Goal 항목 | 검증 방법 | 결과 |
|----------------|-----------|------|
| `TeachingImagePath_Vertical` property 이름 변경 0 | grep 결과 4 hit (xml-doc L14 + 선언 L29 + 코멘트 L51 + 코멘트 L107) — 선언 L29 byte-identical | PASS |
| get/set body / default `""` 변경 0 | L29 `public string TeachingImagePath_Vertical { get; set; } = "";` 유지 | PASS |
| `[Category]` / `[Description]` 기존 attribute 변경 0 | L24-25 line-identical | PASS |
| `TryExecute` 본문 변경 0 | git diff 결과 attribute 2 + using 1 만 표시 — L117-224 무수정 | PASS |
| `DatumConfig.cs` 변경 0 (Datum 측 동일 attribute 미적용) | grep 결과 L46 `TeachingImagePath_Vertical { get; set; } = "";` 변경 0, ICustomTypeDescriptor 분기 L741~756 보존 | PASS |
| `MeasurementFactory.cs` 변경 0 | git diff 미포함 | PASS |
| `Action_FAIMeasurement.cs` 변경 0 (TryGrabOrLoadFaiDualImages 회귀 0) | git diff 미포함 | PASS |
| 다른 7 Measurement 타입 변경 0 | git diff 단일 파일만 | PASS |
| INI 직렬화 순서 변경 0 (attribute 추가는 ParamBase reflection 순서 무관) | property 이름/타입/default 모두 보존 → ParamBase 회귀 0 | PASS |

## msbuild Debug/x64 결과

```
msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64
→ DatumMeasurement -> C:\Info\Project\DataMeasurement\WPF_Example\bin\x64\Debug\DatumMeasurement.exe
→ Build succeeded
→ 0 Error(s)
→ 7 Warning(s) (모두 baseline / pre-existing — 0 new)
```

**Baseline warnings (변경 전부터 존재):**
- CS0618 (5건): Sequence_Top/Sequence_Bottom/SequenceHandler — Phase 33 obsolete migration markers
- CS0162 (1건): VirtualCamera.cs:266 — unreachable code (Phase 39.2 baseline)
- MSB3884 (1건): MinimumRecommendedRules.ruleset missing (환경 noise)

**신규 warning 0 / error 0.**

## Decisions Made
- ModelFinderViewModel L28-29 의 `[InputFilePath(EXTENSION_MODEL, FILTER_MODEL)] + [AutoUpdateText]` 조합을 그대로 차용 — 패턴 동형성으로 PropertyGrid 동작 결정적
- `using ReringProject.Device;` 위치는 알파벳 순 정렬상 `ReringProject.Halcon.*` 앞 — 기존 ModelFinderViewModel L3 패턴과 일치 (모듈 import 우선)
- 주석은 `//260530 hbk Phase 39.3 D-G3` 3건 (using 1 + attribute 2) — feedback_comment_convention 준수
- AutoUpdateText 주석에 `ModelFinderViewModel L28-29 패턴` 명시 — 후속 유지보수 시 reference 위치 즉시 노출

## Deviations from Plan

None - plan executed exactly as written. 추가 +3 라인 / 수정 0 / 삭제 0 — 계획 예측 (+2~3 라인) 정확 매치.

## Issues Encountered

None.

## Plan 04 UAT 4 Risk R4 Fallback 메모

**Risk R4 (LOW) — `[InputFilePath]` + `[Category]` 호환성**: 빌드 PASS 됐으나 Plan 04 Test 4 시각 검증에서 PropertyGrid Browse 버튼이 미표시될 가능성. 4 가지 fallback 후보 (우선순위 순):

1. **Fallback (a) — `[Browsable(true)]` 명시 추가**: `[PropertyTools.DataAnnotations.Browsable(true)]` attribute 를 InputFilePath 위에 추가. ModelFinderViewModel 에서는 Browsable 명시 없이도 동작하므로 차선책.
2. **Fallback (b) — `[Category]` 제거**: ModelFinderViewModel 패턴은 `[Category]` 없이 attribute 만 적용 → DualImage 도 동일 시도. 단, Phase 39.2 D-G1 `[Category("Image|DualImage")]` 의 그룹화 의도와 충돌.
3. **Fallback (c) — attribute 순서 교체**: `[InputFilePath]` → `[Category]` → `[Description]` 순서로 변경. PropertyTools 3.1.0 의 attribute 처리 우선순위 의존성 회피.
4. **Fallback (d) — PropertyTools 3.1.0 의 다른 버전 attribute**: `[FilePath]` (별도 attribute 존재 여부 확인 필요) 또는 ICustomTypeDescriptor 직접 구현으로 셀 에디터 강제 지정.

Plan 04 UAT 4 에서 사용자 시각 확인 결과 PASS 면 위 fallback 미적용. FAIL 시 (a) → (c) → (b) → (d) 순서로 시도 권장.

## Next Phase Readiness

- **Plan 04 UAT 4 진입 가능**: 본 plan 의 attribute 적용 + 빌드 PASS 까지 의무 완료. PropertyGrid 시각 동작 검증은 Plan 04 Test 4 (Risk R4 가드) 에서.
- **Wave 1 병렬 완결**: Plan 01 (4 commits 0a27dd2..fc3b854) + Plan 03 (1 commit f260cf0) 독립 — 파일 충돌 0.
- **Plan 02 (D-G2) 의존성 0**: Plan 02 가 _currentImageSource 분기 확장 중이지만 본 plan 의 변경 영역 (Measurement 클래스 attribute) 과 직교 — 동시 진입 안전.
- **Deferred (후속 phase 후보)**: DatumConfig.TeachingImagePath / TeachingImagePath_Vertical 에 동일 `[InputFilePath]` + `[AutoUpdateText]` 적용 — CONTEXT.md Deferred Ideas 등재 권장.

## Self-Check: PASSED

- File modified: `WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` → FOUND
- Commit `f260cf0` → FOUND (`git rev-parse --short HEAD` = f260cf0)
- msbuild PASS, 0 new errors/warnings
- grep `[InputFilePath(DeviceHandler.EXTENSION_IMAGE` → 1 hit (L27)
- grep `[AutoUpdateText]` → 1 hit (L28)
- grep `using ReringProject.Device` → 1 hit (L5)
- grep `TeachingImagePath_Vertical` (declaration only) → property 선언 1 hit (L29) + xml-doc/comments 3 hit (L14/L51/L107) = 변경 없음
- DatumConfig.cs `TeachingImagePath_Vertical` → 기존 라인 수 보존 (Anti-Goal verified)

---
*Phase: 39.3-dualimage-fai-ux-redesign-2026-05-30*
*Plan: 03*
*Completed: 2026-05-30*
