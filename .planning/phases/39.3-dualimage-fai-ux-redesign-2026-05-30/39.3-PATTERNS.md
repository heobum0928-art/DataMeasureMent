---
phase: 39.3-dualimage-fai-ux-redesign-2026-05-30
type: patterns
date: 2026-05-30
status: patterns_complete
depends_on: 39.3-CONTEXT.md / 39.3-RESEARCH.md
---

# Phase 39.3 — Pattern Map

**Mapped:** 2026-05-30
**Files analyzed:** 3 수정 (분기 A~J 총 10 지점) + 0 신규
**Analogs found:** 5 / 5 (전부 in-repo 분기 인용)
**Confidence:** HIGH — RESEARCH.md 가 이미 모든 file:line 을 검증, 본 문서는 planner-가용 코드 excerpt 로 변환

## 의도

Phase 39.3 = **순수 UI/UX phase** (D-G4). 신규 알고리즘 0, 신규 파일 0. 동일 파일 내 기존 7 화이트리스트 분기 패턴을 **그대로 차용**하여 8번째 (`DualImageEdgeDistanceMeasurement`) 분기를 추가. swap UI 는 Datum DualImage (Phase 34.1) 의 `_selectedDatumForSwap` 패턴을 대칭 복제 (`_selectedDualImageMeasurement`). PATTERNS.md = "복사 대상 코드 + 적용 위치 + 회귀 0 가드" 단일 문서.

## File Classification

| 수정 대상 파일 | 역할 | 데이터 흐름 | 가장 가까운 in-file Analog | Match Quality |
|----------------|------|-------------|----------------------------|---------------|
| `WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` | UI binding (트리 selection 분기) | 트리 노드 클릭 → Param 타입 분기 → 버튼 IsEnabled | 동 파일 L496-503 `isRectRoiType` 7 타입 화이트리스트 | exact (같은 파일, 동일 패턴) |
| `WPF_Example/UI/ContentItem/MainView.xaml.cs` (`FindSelectedRectMeasurement`) | UI 해석 (Measurement 객체 lookup) | 트리/dataGrid → MeasurementBase 캐스트 | 동 파일 L1865-1896 7 타입 분기 | exact |
| `WPF_Example/UI/ContentItem/MainView.xaml.cs` (`CommitRectRoi`) | write-back (drag bbox → Measurement 필드) | RoiDefinition bbox → Point_*/Rect_*/EdgeA*B* 라우팅 | 동 파일 L1644-1693 7+1 타입 as 캐스트 분기 | exact (DualImage = 슬롯 종속 분기 추가) |
| `WPF_Example/UI/ContentItem/MainView.xaml.cs` (`BuildPointRoiDefinitions`) | drawing (Measurement → RoiDefinition list) | Measurement 필드 → bbox 변환 → RoiDefinition list | 동 파일 L343-416 — `ArcLineIntersect` 가 4 RoiDefinition 반환 (다중 RoiDefinition 분기 패턴) | role-match (단일 RoiDefinition → 다중) |
| `WPF_Example/UI/ContentItem/MainView.xaml.cs` (swap wiring E~J) | UI state machine (Datum/Meas 상호 배제 mutex) | 트리 selection → 활성 owner → swap 핸들러 처리 | 동 파일 L62-68/L1311-1341/L1345-1422 — `_selectedDatumForSwap` 전체 라이프사이클 | exact (대칭 복제) |
| `WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` | model attribute (PropertyGrid Browse) | PropertyGrid 셀 → OpenFileDialog → string | `WPF_Example/UI/ViewModel/ModelFinderViewModel.cs:28-37` `[InputFilePath] + [AutoUpdateText]` | exact (DataMeasurement 의 유일 use site) |

## Shared Patterns (cross-cutting)

### SP-1 — 헝가리안 주석 마커 강제 (CLAUDE.md / feedback_comment_convention.md)
**적용:** 본 phase 모든 추가/수정 라인
**형식:** `//260530 hbk Phase 39.3 D-GX` (X = 1/2/3/4/5)
**예시 (in-repo):** L1311-1341 모든 swap 핸들러 라인의 `//260527 hbk Phase 34.1 D-34.1-XX`

### SP-2 — C# 7.2 호환 캐스트 (NULL CHECK 패턴)
**Source:** `MainView.xaml.cs:1644-1658` (CommitRectRoi 7 타입 분기)
```csharp
var etld = _editingMeasurement as EdgeToLineDistanceMeasurement;
if (etld != null) { etld.Point_Row = mCenterRow; /* ... */ }
```
**Apply to:** Phase 39.3 의 모든 DualImage 분기 추가 — `is T x` pattern matching 도 C# 7.0 에서 가능하므로 사용 가능하지만 in-file 일관성 위해 `as T + null check` 유지.

### SP-3 — Halcon LoadImage try/catch 패턴
**Source:** `MainView.xaml.cs:1318-1321`
```csharp
try { halconViewer.LoadImage(hpath); }
catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, ex.Message); }
```
**Apply to:** Plan 02 Measurement swap 핸들러 (ShotConfig 이미지 로드 / TeachingImagePath_Vertical 로드 양쪽).

### SP-4 — K&R 중괄호 스타일 (MainView.xaml.cs 전체)
**확인:** L1313-1325 — `if (...) {` / `else {` 모두 같은 줄. 신규 코드 동일 유지.

### SP-5 — RoiDefinition ID 컨벤션 = `faiName + "_" + measName + ["_" + suffix]`
**Source:** `MainView.xaml.cs:353/362/371/380` (ArcLineIntersect 4 RoiDefinition) + L407 (단일 RoiDefinition)
- 단일: `Id = faiName + "_" + measName`
- 다중: `Id = faiName + "_" + measName + "_EdgeA1"` (suffix 필수)
**Apply to:** Plan 01 Task 01-04 — DualImage 의 2 RoiDefinition → `"_Point"` / `"_Line"` suffix.
**HighlightSelectedRoi fallback (L466-470):** composite ID 매칭 실패 시 부모 FAI ROI 로 자동 fallback → `_Point` 가 기본 매칭 대상이 됨 (Risk R3 회피).

---

## Pattern Assignments

### 분기 A — `InspectionListView.xaml.cs:496-503` (`isRectRoiType` 화이트리스트)

**Role:** UI binding — 트리 Measurement 노드 선택 시 `btn_rectRoi.IsEnabled` 결정.
**Data flow:** `OnInspectionTreeSelectedItemChanged` → `item.NodeType == Measurement` → `bool isRectRoiType = item.Param is T1 || ... || item.Param is T7;` → `mainView.btn_rectRoi.IsEnabled = isRectRoiType;`

**Analog (in-file, exact):**
```csharp
// Source: WPF_Example/UI/ControlItem/InspectionListView.xaml.cs:495-505
//260519 hbk Phase 31 CO-23.1-02 — Rect ROI 버튼 활성화: Point_* ROI 보유 타입 7종 (Phase 31 신규 포함)
bool isRectRoiType = item.Param is EdgeToLineDistanceMeasurement
    || item.Param is EdgeToLineAngleMeasurement       //260519 hbk Phase 31 D-05
    || item.Param is ArcEdgeDistanceMeasurement        //260519 hbk Phase 31 D-08
    || item.Param is ArcLineIntersectDistanceMeasurement //260519 hbk Phase 31 D-01
    || item.Param is CompoundAngleMeasurement          //260519 hbk Phase 31 D-11
    || item.Param is CompoundCenterCDistanceMeasurement //260519 hbk Phase 31 D-11
    || item.Param is CompoundCenterBDistanceMeasurement //260519 hbk Phase 31 D-11
    || item.Param is CompoundShortAxisDistanceMeasurement; //260523 hbk Phase 32 — E3 단축 환원
if (mParentWindow != null && mParentWindow.mainView != null)
    mParentWindow.mainView.btn_rectRoi.IsEnabled = isRectRoiType; //260519 hbk Phase 31 CO-23.1-02
```

**적용 (Phase 39.3):** 1 줄 추가
```csharp
    || item.Param is CompoundShortAxisDistanceMeasurement //260523 hbk Phase 32
    || item.Param is DualImageEdgeDistanceMeasurement; //260530 hbk Phase 39.3 D-G1
```

**회귀 가드:** 기존 7 타입의 `IsEnabled = true` 동작 변경 없음 (OR 추가). using/namespace 추가 0 (`DualImageEdgeDistanceMeasurement` = 동일 `ReringProject.Sequence` namespace + 기존 using 으로 해결).

---

### 분기 B — `MainView.xaml.cs:1865-1896` (`FindSelectedRectMeasurement`)

**Role:** UI 해석 — `btn_rectRoi` 클릭 시 어느 MeasurementBase 를 편집 대상으로 잡을지 결정.
**Data flow:** 트리 `SelectedParam` 우선 → 미해당 시 dataGrid 행 → 해당 FAI 의 `Measurements` 순회 → 화이트리스트 타입 첫 매치 반환.

**Analog (in-file, exact):**
```csharp
// Source: WPF_Example/UI/ContentItem/MainView.xaml.cs:1865-1896
private MeasurementBase FindSelectedRectMeasurement() {
    // 트리 노드 선택 우선 (FAI 미생성 신규 measurement 케이스 포함)
    if (mParentWindow != null && mParentWindow.inspectionList != null) {
        var selParam = mParentWindow.inspectionList.SelectedParam;
        if (selParam is EdgeToLineDistanceMeasurement) return (MeasurementBase)selParam;
        if (selParam is EdgeToLineAngleMeasurement) return (MeasurementBase)selParam;
        if (selParam is ArcEdgeDistanceMeasurement) return (MeasurementBase)selParam;
        if (selParam is ArcLineIntersectDistanceMeasurement) return (MeasurementBase)selParam;
        if (selParam is CompoundAngleMeasurement) return (MeasurementBase)selParam;
        if (selParam is CompoundCenterCDistanceMeasurement) return (MeasurementBase)selParam;
        if (selParam is CompoundCenterBDistanceMeasurement) return (MeasurementBase)selParam;
        if (selParam is CompoundShortAxisDistanceMeasurement) return (MeasurementBase)selParam; //260523 hbk Phase 32
    }
    // fallback — dataGrid 행 선택 경로
    var selectedRow = dataGrid_faiResults.SelectedItem as MeasurementResultRow;
    if (selectedRow != null) {
        FAIConfig fai = FindFAIByName(selectedRow.FAIName);
        if (fai != null) {
            foreach (var m in fai.Measurements) {
                if (m is EdgeToLineDistanceMeasurement) return m;
                /* ... 7 타입 ... */
                if (m is CompoundShortAxisDistanceMeasurement) return m;
            }
        }
    }
    return null;
}
```

**적용 (Phase 39.3):** 2 줄 추가 (트리 분기 끝 + dataGrid fallback 끝)
```csharp
// 트리 분기 끝부분 (L1876 다음):
if (selParam is DualImageEdgeDistanceMeasurement) return (MeasurementBase)selParam; //260530 hbk Phase 39.3 D-G1

// dataGrid fallback 끝부분 (L1891 다음):
if (m is DualImageEdgeDistanceMeasurement) return m; //260530 hbk Phase 39.3 D-G1
```

**회귀 가드:** 기존 7 타입 lookup 동작 변경 없음. 추가 분기는 **8번째 OR 매치** — 우선순위는 정의 순서 (8번째). FAI 내에 DualImage 와 EdgeToLineDistance 가 공존하면 EdgeToLineDistance 가 우선 — 의도된 우선순위.

---

### 분기 C — `MainView.xaml.cs:1644-1693` (`CommitRectRoi` 타입별 write-back)

**Role:** write-back — 캔버스 드래그 bbox 의 center/halfH/halfW 를 `_editingMeasurement` 의 `Point_*` / `Rect_*` / `EdgeA*_*` 필드에 라우팅.
**Data flow:** `halconViewer.CommitActiveRectangle()` → `RoiDefinition (Row1/Row2/Col1/Col2)` → `mCenterRow/Col + mHalfHeight/Width` → 타입별 as 캐스트 분기 → 해당 필드에 5값 set.

**Analog (in-file, exact — 단일 RoiDefinition 패턴):**
```csharp
// Source: WPF_Example/UI/ContentItem/MainView.xaml.cs:1644-1658
//260519 hbk Phase 31 CO-23.1-02 — 측정 타입별 Point ROI 기록 일반화
var etld = _editingMeasurement as EdgeToLineDistanceMeasurement;
if (etld != null) { etld.Point_Row = mCenterRow; etld.Point_Col = mCenterCol; etld.Point_Phi = 0.0; etld.Point_Length1 = mHalfHeight; etld.Point_Length2 = mHalfWidth; }
var etla = _editingMeasurement as EdgeToLineAngleMeasurement;
if (etla != null) { etla.Point_Row = mCenterRow; etla.Point_Col = mCenterCol; etla.Point_Phi = 0.0; etla.Point_Length1 = mHalfHeight; etla.Point_Length2 = mHalfWidth; }
var aed = _editingMeasurement as ArcEdgeDistanceMeasurement;
if (aed != null) { aed.Point_Row = mCenterRow; aed.Point_Col = mCenterCol; aed.Point_Phi = 0.0; aed.Point_Length1 = mHalfHeight; aed.Point_Length2 = mHalfWidth; }
//260521 hbk Phase 32 — Compound 4종 단일 Rect ROI write-back (Rect_* 필드명)
var cAngle = _editingMeasurement as CompoundAngleMeasurement;
if (cAngle != null) { cAngle.Rect_Row = mCenterRow; cAngle.Rect_Col = mCenterCol; cAngle.Rect_Phi = 0.0; cAngle.Rect_Length1 = mHalfHeight; cAngle.Rect_Length2 = mHalfWidth; }
// ... (3 more Compound 타입 동일 패턴)
```

**적용 (Phase 39.3) — D-G1 핵심 라우팅:**
```csharp
//260530 hbk Phase 39.3 D-G1 — DualImage: _currentImageSource 슬롯에 따라 PointROI vs LineROI 라우팅
var dualMeas = _editingMeasurement as DualImageEdgeDistanceMeasurement; //260530 hbk Phase 39.3 D-G1
if (dualMeas != null) { //260530 hbk Phase 39.3 D-G1
    if (_currentImageSource == ReringProject.Sequence.EImageSource.Vertical) { //260530 hbk Phase 39.3 D-G1
        // 세로 슬롯 표시 중 → LineROI 라우팅
        dualMeas.LineROI_Row = mCenterRow; dualMeas.LineROI_Col = mCenterCol; dualMeas.LineROI_Phi = 0.0;
        dualMeas.LineROI_Length1 = mHalfHeight; dualMeas.LineROI_Length2 = mHalfWidth;
    } else { //260530 hbk Phase 39.3 D-G1
        // 가로 슬롯 표시 중 (기본 Horizontal) → PointROI 라우팅
        dualMeas.PointROI_Row = mCenterRow; dualMeas.PointROI_Col = mCenterCol; dualMeas.PointROI_Phi = 0.0;
        dualMeas.PointROI_Length1 = mHalfHeight; dualMeas.PointROI_Length2 = mHalfWidth;
    }
}
```

**위치:** L1658 (cShort 분기 후) ~ L1659 (ali 분기 전) 사이.

**회귀 가드:**
- ArcLineIntersect 4-ROI 순차 분기 (L1660-1693) 진입 전에 위치 → DualImage 가 `_editingMeasurementRoiIndex` 를 건드리지 않음 (회귀 0).
- `_currentImageSource` default = Horizontal → Plan 02 미진입 상태에서도 PointROI 라우팅 항상 동작 (빌드 안전).
- `EImageSource` 임포트 = `using ReringProject.Sequence;` 이미 존재 (L62 `_currentImageSource` 선언이 동 namespace).

---

### 분기 D — `MainView.xaml.cs:343-416` (`BuildPointRoiDefinitions` — 다중 RoiDefinition 반환)

**Role:** drawing — Measurement 필드를 RoiDefinition list 로 변환해 캔버스 재 렌더 시 표시.
**Data flow:** `MeasurementBase + faiName` → as 캐스트로 (Row/Col/Length1/Length2) 추출 → bbox (Row1=Row-Length1, Row2=Row+Length1, ...) → `List<RoiDefinition>`.

**Analog (in-file, role-match — 다중 RoiDefinition 패턴 = ArcLineIntersect):**
```csharp
// Source: WPF_Example/UI/ContentItem/MainView.xaml.cs:348-388
//260521 hbk Phase 32 I9/I10-redesign — ArcLineIntersect: EdgeA1/EdgeB1/EdgeA2/EdgeB2 4개 독립 RoiDefinition. 미티칭 ROI 는 개별 skip.
var ali = m as ArcLineIntersectDistanceMeasurement;
if (ali != null) {
    if (ali.EdgeA1_Length1 > 0 && ali.EdgeA1_Length2 > 0) {
        result.Add(new RoiDefinition {
            Id = faiName + "_" + measName + "_EdgeA1",
            Name = measName + "_EdgeA1",
            Row1 = ali.EdgeA1_Row - ali.EdgeA1_Length1, Column1 = ali.EdgeA1_Col - ali.EdgeA1_Length2,
            Row2 = ali.EdgeA1_Row + ali.EdgeA1_Length1, Column2 = ali.EdgeA1_Col + ali.EdgeA1_Length2,
            IsTaught = true
        });
    }
    // ... EdgeB1 / EdgeA2 / EdgeB2 동일 패턴
    return result; //260521 hbk Phase 32 I9/I10-redesign — 나머지 타입 분기 통과 불필요
}
```

**적용 (Phase 39.3) — DualImage 2 RoiDefinition:**
```csharp
//260530 hbk Phase 39.3 D-G1 — DualImage: PointROI + LineROI 2개 독립 RoiDefinition (ArcLineIntersect 패턴 차용, RoiId suffix "_Point"/"_Line")
var dual = m as DualImageEdgeDistanceMeasurement; //260530 hbk Phase 39.3 D-G1
if (dual != null) { //260530 hbk Phase 39.3 D-G1
    if (dual.PointROI_Length1 > 0 && dual.PointROI_Length2 > 0) { //260530 hbk Phase 39.3 D-G1
        result.Add(new RoiDefinition {
            Id = faiName + "_" + measName + "_Point", //260530 hbk Phase 39.3 D-G1 — SP-5
            Name = measName + "_Point",
            Row1 = dual.PointROI_Row - dual.PointROI_Length1, Column1 = dual.PointROI_Col - dual.PointROI_Length2,
            Row2 = dual.PointROI_Row + dual.PointROI_Length1, Column2 = dual.PointROI_Col + dual.PointROI_Length2,
            IsTaught = true
        });
    }
    if (dual.LineROI_Length1 > 0 && dual.LineROI_Length2 > 0) { //260530 hbk Phase 39.3 D-G1
        result.Add(new RoiDefinition {
            Id = faiName + "_" + measName + "_Line", //260530 hbk Phase 39.3 D-G1
            Name = measName + "_Line",
            Row1 = dual.LineROI_Row - dual.LineROI_Length1, Column1 = dual.LineROI_Col - dual.LineROI_Length2,
            Row2 = dual.LineROI_Row + dual.LineROI_Length1, Column2 = dual.LineROI_Col + dual.LineROI_Length2,
            IsTaught = true
        });
    }
    return result; //260530 hbk Phase 39.3 D-G1 — ArcLineIntersect 와 동일하게 조기 종료
}
```

**위치:** L388 (ArcLineIntersect 의 `return result;`) 직후, L390 (`double pRow = 0` 단일 RoiDefinition 분기 시작) 이전.

**회귀 가드:**
- ArcLineIntersect 의 `return result;` 패턴 동일 차용 → 단일 RoiDefinition 7 타입 분기 통과 X (DualImage 가 거기 빠져나가 빈 ROI 반환되는 결함 회피).
- RoiId 컨벤션 `_Point` / `_Line` → HighlightSelectedRoi L466-470 fallback (composite ID 미매칭 → 부모 FAI ROI) 가 정상 동작 (Risk R3 회피).

---

### 분기 E~J — swap UX wiring (Plan 02 의 핵심)

**Role:** UI state machine — Datum/Measurement DualImage 의 mutual exclusion + 토글 핸들러 우선순위 + 배지/Visibility 제어.

#### E-1: 신규 필드 (`MainView.xaml.cs` L68 직후)

**Analog (in-file, exact):**
```csharp
// Source: WPF_Example/UI/ContentItem/MainView.xaml.cs:62-68
//260527 hbk Phase 34.1 D-34.1-08/12 — 현재 캔버스에 표시 중인 이미지 축. DualImage 변형에서만 의미 있음.
//  세션 한정, INI 미저장 (Datum 노드 이동 시 가로축으로 리셋).
private ReringProject.Sequence.EImageSource _currentImageSource = ReringProject.Sequence.EImageSource.Horizontal;
//260527 hbk Phase 34.1 CO-34.1-02 hotfix — 현재 선택된 Datum (teach 모드 무관, swap UI 의 대상).
private DatumConfig _selectedDatumForSwap;
```

**적용 (Phase 39.3) — 대칭 신규 필드:**
```csharp
//260530 hbk Phase 39.3 D-G2 — 현재 선택된 DualImage Measurement (Datum 의 _selectedDatumForSwap 대칭, mutex 페어).
//  세션 한정, INI 미저장. InspectionListView Measurement 노드 선택 시 set, Datum 노드 선택 시 clear.
private DualImageEdgeDistanceMeasurement _selectedDualImageMeasurement;
```

**회귀 가드:** mutex 컨벤션 — `_selectedDatumForSwap` 와 `_selectedDualImageMeasurement` 는 **둘 중 하나만 non-null**. 둘 다 non-null 이면 Risk R1 (swap 핸들러 모호) 발생.

#### F: InspectionListView 의 Measurement 분기에서 mutex set (신규 public 메서드 호출)

**Analog (in-file, exact — Datum 측 패턴):**
```csharp
// Source: WPF_Example/UI/ControlItem/InspectionListView.xaml.cs:485-488 (Datum 노드 선택 시 mainView 콜아웃)
//260424 hbk Phase 12 D-01 — Datum 노드 선택 시 btn_teachDatum 활성화
if (mParentWindow != null && mParentWindow.mainView != null) {
    mParentWindow.mainView.btn_teachDatum.IsEnabled = true;
}
```
+ Datum 선택 시 `PublishDatumRoiCandidates(datum)` 호출 (별도 위치) → `_selectedDatumForSwap` set.

**적용 (Phase 39.3):** InspectionListView 의 Measurement 분기 (L491-522) 끝부분에 추가
```csharp
//260530 hbk Phase 39.3 D-G2 — Measurement DualImage 선택 시 swap UI owner set (mutex). 비-DualImage 는 null 로 clear.
if (mParentWindow != null && mParentWindow.mainView != null) {
    mParentWindow.mainView.PublishMeasurementDualImageSelection(itemParam as DualImageEdgeDistanceMeasurement);
}
```

**+ MainView 신규 public 메서드** (Plan 02 Task 02-01):
```csharp
//260530 hbk Phase 39.3 D-G2 — PublishDatumRoiCandidates 대칭. Measurement DualImage 노드 선택 시 swap UI owner set + 가로축 리셋 + Visibility 제어.
public void PublishMeasurementDualImageSelection(DualImageEdgeDistanceMeasurement meas) {
    _selectedDualImageMeasurement = meas; //260530 hbk Phase 39.3 D-G2
    //260530 hbk Phase 39.3 D-G2 — mutex: Measurement set 시 Datum clear (Risk R1 회피)
    if (meas != null && _selectedDatumForSwap != null) {
        _selectedDatumForSwap.PropertyChanged -= OnSelectedDatumPropertyChanged; //260530 hbk Phase 39.3 D-G2
        _selectedDatumForSwap = null; //260530 hbk Phase 39.3 D-G2
    }
    //260530 hbk Phase 39.3 D-G2 — Visibility 제어 (PublishDatumRoiCandidates L1363-1365 패턴)
    bool isDualImage = (meas != null);
    if (btn_swapHorizontal != null) btn_swapHorizontal.Visibility = isDualImage ? Visibility.Visible : Visibility.Collapsed;
    if (btn_swapVertical   != null) btn_swapVertical.Visibility   = isDualImage ? Visibility.Visible : Visibility.Collapsed;
    if (border_imageSourceBadge != null) border_imageSourceBadge.Visibility = isDualImage ? Visibility.Visible : Visibility.Collapsed;
    if (isDualImage) {
        //260530 hbk Phase 39.3 D-G2 — 새 노드 진입 시 가로축 리셋 (Datum D-34.1-08 대칭, Risk R6 회피)
        _currentImageSource = ReringProject.Sequence.EImageSource.Horizontal;
        UpdateImageSourceBadge(ReringProject.Sequence.EImageSource.Horizontal); //260530 hbk Phase 39.3 D-G2
    }
}
```

#### G: `BtnSwapHorizontal_Click` / `BtnSwapVertical_Click` 우선순위 분기

**Analog (in-file, exact):** `MainView.xaml.cs:1311-1325` / `1327-1341` (위 RESEARCH.md 1.2 인용 그대로).

**적용 (Phase 39.3) — Measurement 우선순위 가드 핸들러 진입 직후:**
```csharp
private void BtnSwapHorizontal_Click(object sender, RoutedEventArgs e) {
    //260530 hbk Phase 39.3 D-G2 — Measurement DualImage 가 Datum 보다 우선 (mutex)
    var meas = _selectedDualImageMeasurement;
    if (meas != null) {
        FAIConfig fai = FindFAIContainingMeasurement(meas); //260530 hbk Phase 39.3 — 기존 helper (L1802)
        ShotConfig shot = fai != null ? fai.Owner as ShotConfig : null;
        if (shot != null && shot.HasImage) {
            HImage img = null;
            try {
                img = shot.GetImage();
                if (img != null) halconViewer.LoadImage(img);
            } catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, ex.Message); }
            finally { if (img != null) img.Dispose(); }
        }
        UpdateImageSourceBadge(ReringProject.Sequence.EImageSource.Horizontal);
        return; //260530 hbk Phase 39.3 D-G2 — Measurement 경로 종결, Datum 코드 진입 X
    }
    //260527 hbk Phase 34.1 — 기존 Datum 경로 (변경 0)
    var d = _selectedDatumForSwap;
    if (d == null) return;
    /* ... 기존 코드 ... */
}

private void BtnSwapVertical_Click(object sender, RoutedEventArgs e) {
    //260530 hbk Phase 39.3 D-G2 — Measurement DualImage 가 Datum 보다 우선
    var meas = _selectedDualImageMeasurement;
    if (meas != null) {
        string vpath = meas.TeachingImagePath_Vertical; //260530 hbk Phase 39.3 D-G2 — Measurement 필드
        if (!string.IsNullOrEmpty(vpath) && System.IO.File.Exists(vpath)) {
            try { halconViewer.LoadImage(vpath); }
            catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, ex.Message); }
        }
        UpdateImageSourceBadge(ReringProject.Sequence.EImageSource.Vertical);
        return;
    }
    /* ... 기존 Datum 경로 ... */
}
```

**회귀 가드:** Datum 경로 코드는 **단 1 자도 변경 0**. Measurement 분기는 `return` 으로 조기 종료.

#### H: `UpdateImageSourceBadge` 의 publish 분기 확장

**Analog (in-file, exact):**
```csharp
// Source: WPF_Example/UI/ContentItem/MainView.xaml.cs:1289-1295
// (c) ROI 가시성 — 현재 선택된 datum 이 DualImage 일 때만 subset 필터 적용.
var datumForRoi = _selectedDatumForSwap;
if (datumForRoi != null
    && datumForRoi.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage) {
    PublishDatumRoiCandidates(datumForRoi);
}
```

**적용 (Phase 39.3) — 위 if 블록 직후 추가:**
```csharp
//260530 hbk Phase 39.3 D-G2 — Measurement DualImage 선택 시 RenderInspectionResultForNode 재호출로 ROI 재 렌더 (Datum 와 동일 컨셉, mutex 가 있어 둘 다 fire X)
if (_selectedDualImageMeasurement != null) {
    RenderInspectionResultForNode(_selectedDualImageMeasurement);
}
```

**회귀 가드:** mutex 가 보장된 상태에서 Datum 분기와 Measurement 분기는 **상호 배타** — 둘 다 fire 되지 않음.

#### I: `PublishDatumRoiCandidates` 진입 시 Measurement clear (mutex)

**Analog (in-file, exact):**
```csharp
// Source: WPF_Example/UI/ContentItem/MainView.xaml.cs:1352-1357 (Datum 측 prior reference 캐싱)
DatumConfig priorSelected = _selectedDatumForSwap;
if (_selectedDatumForSwap != datum) {
    if (_selectedDatumForSwap != null) _selectedDatumForSwap.PropertyChanged -= OnSelectedDatumPropertyChanged;
    _selectedDatumForSwap = datum;
    if (_selectedDatumForSwap != null) _selectedDatumForSwap.PropertyChanged += OnSelectedDatumPropertyChanged;
}
```

**적용 (Phase 39.3) — 위 블록 직전에 추가:**
```csharp
//260530 hbk Phase 39.3 D-G2 — mutex: Datum 노드 선택 시 Measurement clear (Risk R1 회피)
if (datum != null && _selectedDualImageMeasurement != null) {
    _selectedDualImageMeasurement = null;
}
```

#### J: `btn_swapHorizontal/Vertical.Visibility` 제어

이미 분기 F 의 `PublishMeasurementDualImageSelection` 신규 메서드에 포함 (Visibility 3종 설정 — `btn_swapHorizontal/Vertical/border_imageSourceBadge`). Datum 측 L1363-1365 와 mutex 관계 — Datum/Measurement 둘 중 active 인 쪽이 Visible 결정. mutex 보장 하 충돌 0.

---

### Plan 03 — `DualImageEdgeDistanceMeasurement.cs:24-26` (PropertyTools attribute)

**Role:** model attribute — PropertyGrid 셀에 [Browse...] 버튼 + OpenFileDialog.
**Data flow:** PropertyGrid 사용자 입력 → `[InputFilePath]` attribute → PropertyTools Editor → OpenFileDialog → 선택 경로 string → property setter → `[AutoUpdateText]` 즉시 INPC fire.

**Analog (exact use site in repo):**
```csharp
// Source: WPF_Example/UI/ViewModel/ModelFinderViewModel.cs:28-38
[InputFilePath(DeviceHandler.EXTENSION_MODEL, DeviceHandler.FILTER_MODEL)]
[AutoUpdateText]
public string ModelFile {
    get { return modelFile; }
    set { this.SetValue(ref this.modelFile, value); }
}
private string modelFile;
```

**Required imports (이미 ModelFinderViewModel L2-3 에 존재):**
```csharp
using PropertyTools.DataAnnotations;
using ReringProject.Device; //260530 hbk Phase 39.3 D-G3 — DeviceHandler 상수 참조 (DualImageEdgeDistanceMeasurement.cs 신규 추가 필요)
```

**DeviceHandler 상수 (재사용, 신규 0):**
```csharp
// Source: WPF_Example/Custom/Device/DeviceHandler.cs:71-72
public const string FILTER_IMAGE = "tiff Files(*.tiff)|*.tiff|bmp Files(*.bmp)|*.bmp|jpg Files(*.jpg)|*.jpg|jpeg Files(*.jpeg)|*.jpeg|png Files(*.png)|*.png|All Files(*.*)|*.*";
public const string EXTENSION_IMAGE = ".tiff";
```

**적용 (Phase 39.3) — `DualImageEdgeDistanceMeasurement.cs:24-26`:**

**Before** (현재):
```csharp
[Category("Image|DualImage")] //260530 hbk Phase 39.2 D-G1
[System.ComponentModel.Description("LineROI 검출용 별도 이미지 경로 — Bottom E5 패턴 (Phase 37 VTH-DualImage 슬롯 재사용)")]
public string TeachingImagePath_Vertical { get; set; } = "";
```

**After:**
```csharp
[Category("Image|DualImage")] //260530 hbk Phase 39.2 D-G1
[System.ComponentModel.Description("LineROI 검출용 별도 이미지 경로 — Bottom E5 패턴 (Phase 37 VTH-DualImage 슬롯 재사용)")]
[InputFilePath(DeviceHandler.EXTENSION_IMAGE, DeviceHandler.FILTER_IMAGE)] //260530 hbk Phase 39.3 D-G3
[AutoUpdateText] //260530 hbk Phase 39.3 D-G3 — 즉시 INPC fire (ModelFinderViewModel L28-29 패턴)
public string TeachingImagePath_Vertical { get; set; } = "";
```

**+ 파일 상단 using 추가** (Risk R5 회피):
```csharp
using PropertyTools.DataAnnotations; // 이미 L4 에 존재 — 변경 0
using ReringProject.Device; //260530 hbk Phase 39.3 D-G3 — DeviceHandler 상수 참조
```

**회귀 가드:**
- Property 이름 `TeachingImagePath_Vertical` 변경 0 → INI 직렬화 / `Action_FAIMeasurement.TryGrabOrLoadFaiDualImages` reference 회귀 0 (Anti-Goal).
- get/set body 변경 0 → ParamBase reflection serializer (Phase 22 IMG-01 패턴) 회귀 0.
- DatumConfig.cs:46 의 `TeachingImagePath_Vertical` 에는 attribute 추가 X (CONTEXT.md Deferred Ideas — Datum 측은 후속 phase 후보).

---

## No Analog Found

해당 없음. Phase 39.3 의 모든 추가/수정 코드는 in-file 또는 in-repo 의 기존 패턴을 직접 차용 (HIGH confidence).

---

## Wave-Plan 매핑 (RESEARCH.md 권장 그대로)

| Wave | Plan | 분기 적용 | 의존성 |
|------|------|-----------|--------|
| Wave 1 | Plan 01 (D-G1) | A + B + C + D | 독립 |
| Wave 1 | Plan 03 (D-G3) | DualImageEdgeDistanceMeasurement.cs attribute + using | 독립 (Plan 01 과 다른 파일) |
| Wave 2 | Plan 02 (D-G2) | E + F + G + H + I + J | Plan 01 분기 C 의 `_currentImageSource` 참조에 의존 |
| Wave 3 | Plan 04 (D-G1~G3 + Anti-Goals) | SIMUL UAT 4 시나리오 + 회귀 1 | 모든 코드 wave 완료 |

## 검증 명령

| 단계 | 명령 |
|------|------|
| Per task | `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64` + 코드 인스펙션 (Anti-Goal 3 파일 변경 0 확인) |
| Per wave | SIMUL UAT 해당 wave 시나리오 부분 실행 |
| Phase gate | UAT 1~4 + 회귀 1 ALL PASS → `/gsd-verify-work` |

## Anti-Goal 가드 체크리스트 (planner 가 각 PLAN.md 의 verification 에 인용)

- [ ] `DualImageEdgeDistanceMeasurement.cs` `TryExecute` 본문 변경 0 (Plan 03 만 L24-26 attribute 추가)
- [ ] `MeasurementFactory.cs` 변경 0
- [ ] `Action_FAIMeasurement.cs` 변경 0 (DualImage 분기 + `TryGrabOrLoadFaiDualImages` 본문 보존)
- [ ] 다른 7 Measurement 타입 `isRectRoiType` / `FindSelectedRectMeasurement` / `CommitRectRoi` / `BuildPointRoiDefinitions` 분기 변경 0 (DualImage 분기는 **추가만**, 기존 분기 1줄도 수정 X)
- [ ] Datum DualImage (Phase 34.1 / 36 / 37) — `_selectedDatumForSwap`/`PublishDatumRoiCandidates`/`BtnSwap*_Click` 의 Datum 경로 코드 변경 0
- [ ] INI 직렬화 순서 / `TeachingImagePath_Vertical` property 이름 보존
- [ ] mutex 검증: Datum 노드 ↔ Measurement DualImage 노드 전환 시 둘 다 non-null 인 순간 0 (Risk R1)
- [ ] `_currentImageSource` 리셋 정책: Measurement 노드 새 선택 시 Horizontal (Risk R6)
- [ ] RoiId `_Point` / `_Line` suffix 컨벤션 (Risk R3 / SP-5)
- [ ] `using ReringProject.Device;` import (Risk R5 — Plan 03)

## Metadata

**Analog search scope:**
- `WPF_Example/UI/ContentItem/MainView.xaml.cs` (전 swap UX + RectROI 파이프라인)
- `WPF_Example/UI/ContentItem/MainView.xaml` (toolbar 토글 버튼 XAML)
- `WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` (isRectRoiType 화이트리스트)
- `WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` (model body)
- `WPF_Example/UI/ViewModel/ModelFinderViewModel.cs` (`[InputFilePath]` use site)
- `WPF_Example/Custom/Sequence/Inspection/EImageSource.cs` (enum)
- `WPF_Example/Custom/Device/DeviceHandler.cs` (FILTER_IMAGE 상수)

**Pattern extraction date:** 2026-05-30
**Source confidence:** HIGH — 모든 코드 excerpt 는 직접 file:line 인용, RESEARCH.md 의 [VERIFIED] 마커 일치.
