---
phase: 39.3-dualimage-fai-ux-redesign-2026-05-30
type: uat
status: partial
source:
  - 39.3-04-PLAN.md
started: 2026-05-30
updated: 2026-05-30
auto_checks:
  msbuild_debug_x64: pass
  msbuild_errors: 0
  msbuild_warnings_total: 7
  msbuild_warnings_baseline: 7   # Phase 39.2 baseline (MSB3884 + CS0618 x5 + CS0162)
  msbuild_new_warnings: 0
  anti_goal_pass: 10
  anti_goal_fail: 0
user_tests:
  total: 5
  pass: 0
  fail: 0
  partial: 0
  pending: 5
phase_commits_range:
  first: 0a27dd2   # Plan 01 Task 01-01
  last: 3d9e15e    # Plan 02 Task 02-05
covers:
  - "D-G1 — RectROI 활성화 (Plan 01, 4 분기)"
  - "D-G2 — Swap UX wiring (Plan 02, 5 wiring 지점)"
  - "D-G3 — TeachingImagePath_Vertical Browse 버튼 (Plan 03, 2 attribute)"
  - "WF-01 (Phase 39 carry-over CO-39.2-01-01 종결 후보)"
---

# Phase 39.3 UAT — DualImage FAI Measurement UX 재설계

> **Sign-off: PENDING (2026-05-30).** Task 04-01 baseline 완료 (msbuild PASS + Anti-Goal 10/10 ✅ + UAT 시나리오 정의). 사용자 SIMUL UAT (Test 1~4 + 회귀 1) 대기.
> Plan 01 (RectROI baseline) + Plan 02 (Swap UX wiring) + Plan 03 (Browse 버튼) 통합 동작 검증.
> CO-39.2-01-01 (D-G1 carry-over) 종결 후보.

## Results Summary (2026-05-30 baseline)

| Total | Passed | Issues | Pending |
|-------|--------|--------|---------|
| 5 | 0 | 0 | 5 |

| Test | 항목 | 결과 | 비고 |
|---|---|---|---|
| 1 | RectROI 활성화 (D-G1) | PENDING | btn_rectRoi 활성 + swap UI Visible + 배지 가로축 |
| 2 | Swap UX (D-G2) | PENDING | btn_swapVertical/Horizontal 클릭 → 이미지/배지/토글 3자 동기 + 회수 |
| 3 | 슬롯 종속 RectROI (D-G1+G2 연동) | PENDING | 가로 슬롯 = PointROI 변경 + LineROI 보존 / 세로 슬롯 = LineROI 변경 + PointROI 보존 |
| 4 | Browse 버튼 (D-G3) | PENDING | TeachingImagePath_Vertical [Browse...] → OpenFileDialog → INI 저장 |
| 회귀 1 | Datum + 기존 7 Measurement + 측정값 + Phase 39.1/39.2 smoke | PENDING | Verify A~E ALL OK |

## Auto Checks

| Check | Status | Detail |
|---|---|---|
| msbuild Debug/x64 rebuild | PASS | 0 errors. 7 warnings (MSB3884 + CS0618 x5 + CS0162) = Phase 39.2 baseline 과 동일, 신규 warning 0. |
| Anti-Goal 10/10 자동 검증 | PASS | 하단 Anti-Goal 체크리스트 전체 ✅. |
| 39.3 phase commits range | OK | 0a27dd2 (Plan 01 Task 01-01) ~ 3d9e15e (Plan 02 Task 02-05) — Plan 03 commit f260cf0 포함됨. |

## Anti-Goal 체크리스트 (Task 04-01 자동 검증, 10/10 ✅)

검증 범위 = `git diff 0a27dd2~1..3d9e15e`. PATTERNS.md L505-516 가드 10항.

| # | 가드 항목 | 결과 | 근거 |
|---|---|---|---|
| 1 | `DualImageEdgeDistanceMeasurement.cs` TryExecute 본문 변경 0 (Plan 03 attribute + using 만 허용) | ✅ | git diff 결과 +3 라인만 = L5 `using ReringProject.Device;` + L27 `[InputFilePath(...)]` + L28 `[AutoUpdateText]`. TryExecute / Try* 메서드 본문 0 hit. |
| 2 | `MeasurementFactory.cs` 변경 0 | ✅ | `git diff 0a27dd2~1..3d9e15e -- WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs` → 빈 출력 (변경 0). |
| 3 | `Action_FAIMeasurement.cs` 변경 0 | ✅ | `git diff 0a27dd2~1..3d9e15e -- WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` → 빈 출력 (변경 0). |
| 4 | 다른 7 Measurement 타입 (Edge 3 + Compound 4 + ArcLineIntersect 1) UI 분기 변경 0 | ✅ | MainView.xaml.cs diff = +112 라인 (제거 0 라인). 모두 신규 분기 (PublishMeasurementDualImageSelection / BuildPointRoiDefinitions DualImage 2-RoiDefinition / Btn*Swap* Measurement 우선순위 분기 등). InspectionListView.xaml.cs diff = isRectRoiType 화이트리스트 끝에 `|| DualImageEdgeDistanceMeasurement` 1줄 append (기존 7 분기 조건 보존) + Measurement 콜아웃 1 if-block 추가. |
| 5 | Datum 측 코드 변경 0 (BtnSwap*_Click Datum 경로 / PublishDatumRoiCandidates Datum 측 로직 / OnSelectedDatumPropertyChanged / DatumConfig.cs) | ✅ | `git diff 0a27dd2~1..3d9e15e -- WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` → 빈 출력. MainView.xaml.cs 의 PublishDatumRoiCandidates 변경은 Task 02-03 진입 라인 `_selectedDualImageMeasurement = null` (L1417) 의 *추가* 만 (Datum 측 로직 보존, mutex clear 만 prepend). BtnSwap*_Click 의 Datum 경로는 Measurement 우선순위 early-return 으로 격리됨. |
| 6 | INI 직렬화 / `TeachingImagePath_Vertical` property 이름 보존 | ✅ | DualImageEdgeDistanceMeasurement.cs L29: `public string TeachingImagePath_Vertical { get; set; } = "";` — 이름 / 타입 / default 보존. attribute 만 추가 (L27-28). |
| 7 | mutex 양방향 (Risk R1) | ✅ | `_selectedDualImageMeasurement = null` MainView.xaml.cs L1417 (PublishDatumRoiCandidates 진입, Task 02-03 mutex clear) + PublishMeasurementDualImageSelection 안에서 비-DualImage 시 null assignment (Plan 02 Task 02-01 메서드 본문). `_selectedDatumForSwap = null` MainView.xaml.cs L1504 (PublishMeasurementDualImageSelection 진입, Task 02-01 mutex clear). 양방향 mutex 확보. |
| 8 | `_currentImageSource = ReringProject.Sequence.EImageSource.Horizontal` 리셋 (Risk R6) | ✅ | MainView.xaml.cs L1443 (PublishMeasurementDualImageSelection 안) + L1515 (PublishMeasurementDualImageSelection 또 다른 진입) — Measurement 선택 시 가로축 baseline 으로 리셋. 필드 선언 L64. |
| 9 | RoiId `_Point` / `_Line` suffix (Risk R3 / SP-5) | ✅ | MainView.xaml.cs L398 `Id = faiName + "_" + measName + "_Point"` + L399 `Name = measName + "_Point"` + L407 `Id = faiName + "_" + measName + "_Line"` + L408 `Name = measName + "_Line"` (BuildPointRoiDefinitions DualImage 분기, Plan 01 Task 01-04). |
| 10 | `using ReringProject.Device;` import (Risk R5 — Plan 03) | ✅ | DualImageEdgeDistanceMeasurement.cs L5: `using ReringProject.Device; //260530 hbk Phase 39.3 D-G3 — DeviceHandler 상수 참조`. |

## User Tests (사용자 SIMUL UAT — 4 시나리오 + 1 회귀)

### Test 1 — RectROI 활성화 (D-G1, Plan 01)

**Setup:**
- 앱 실행: `bin/x64/Debug/DatumMeasurement.exe` (Debug 빌드 = SIMUL_MODE 자동 활성).
- Bottom 시퀀스 → E5 Shot → DualImage FAI Measurement 노드 클릭.

**확인 4 항목:**
- (a) `btn_rectRoi` 시각적으로 활성 (`IsEnabled = true`).
- (b) `btn_swapHorizontal` Visibility = Visible.
- (c) `btn_swapVertical` Visibility = Visible.
- (d) 배지 = "가로축" / 파란색 (BadgeBrushHorizontal).

**Expected:** 4 항목 ALL OK = D-G1 (CO-39.2-01-01) 직접 원인 해소.

**Result:** **PENDING** (사용자 SIMUL UAT 대기)

---

### Test 2 — Swap UX (D-G2, Plan 02)

**Setup:** Test 1 후 상태 (DualImage FAI Measurement 선택 + 배지 가로축 + swap UI Visible).

**Action 1: `btn_swapVertical` 클릭**
- 캔버스 이미지 = `TeachingImagePath_Vertical` (Vertical 슬롯 이미지).
- 배지 = "세로축" / 오렌지 (BadgeBrushVertical).
- 토글 상태 갱신 (btn_swapVertical IsChecked = true 또는 강조).

**Action 2: `btn_swapHorizontal` 다시 클릭 (회수)**
- 캔버스 이미지 = ShotConfig.SimulImagePath (Horizontal 슬롯 이미지).
- 배지 = "가로축" / 파란색.
- 토글 복귀 (btn_swapHorizontal IsChecked = true).

**Expected:** 3자 동기 (이미지 + 배지 + 토글) + 회수 ALL OK = Datum DualImage 와 동일 UX.

**Result:** **PENDING** (사용자 SIMUL UAT 대기)

---

### Test 3 — 슬롯 종속 RectROI (D-G1 + G2 연동)

**Setup:** Test 1 상태 (가로 슬롯 = `_currentImageSource = Horizontal`).

**Action 1 (가로 슬롯에서 RectROI 그리기):**
- `btn_rectRoi` 클릭 → 캔버스 위 드래그.
- 결과: `PointROI_*` 5필드 (X/Y/Width/Height/Angle) 변경됨.
- 보존: `LineROI_*` 5필드 unchanged.

**Action 2 (세로 슬롯 전환 후 RectROI 그리기):**
- `btn_swapVertical` 클릭 → `_currentImageSource = Vertical`.
- `btn_rectRoi` 클릭 → 캔버스 위 드래그.
- 결과: `LineROI_*` 5필드 변경됨.
- 보존: `PointROI_*` 5필드 (Action 1 의 셋팅값) unchanged.

**Expected:** 슬롯 종속 라우팅 (CommitRectRoi 분기 C) + 비활성 ROI 보존 ALL OK.

**Result:** **PENDING** (사용자 SIMUL UAT 대기)

---

### Test 4 — Browse 버튼 (D-G3, Plan 03)

**Setup:**
- DualImage FAI Measurement 선택.
- PropertyGrid 의 "Image|DualImage" 카테고리 펼침.

**Action 1: `TeachingImagePath_Vertical` 셀에 [Browse...] 버튼 표시**
- 셀 우측에 `[Browse...]` 버튼이 시각적으로 노출되어야 함.

**Action 2: [Browse...] 클릭**
- OpenFileDialog 표시.
- 필터 = `*.tiff;*.bmp;*.jpg;*.jpeg;*.png;*.*` (DeviceHandler.FILTER_IMAGE).

**Action 3: 파일 선택 → OK**
- 셀에 선택한 파일 경로 표시.
- INI 저장 시 `TeachingImagePath_Vertical=<경로>` 직렬화 확인 (앱 재시작 후 복원).

**Expected:** ALL OK = D-G3 충족.

**Fallback (Risk R4 — Browse 버튼 미표시 시):**
다음 4개 hotfix 중 하나를 순차 적용 후 재테스트:
- (a) `[Browsable(true)]` 명시 추가 — PropertyTools 의 PropertyDescriptor 표시 가드 명시화.
- (b) `[Category]` 제거 — ModelFinderViewModel 패턴 차용 (Category 와 InputFilePath 호환성 이슈 회피).
- (c) attribute 순서 교체 — `[InputFilePath(...)]` 가 `[Category(...)]` 앞에 오도록 reorder.
- (d) PropertyTools 다른 attribute / `ICustomTypeDescriptor` — 마지막 수단, ITypeDescriptor 구현으로 우회.

**Result:** **PENDING** (사용자 SIMUL UAT 대기)

---

### 회귀 1 — Datum + 기존 FAI + 측정값 + Phase 39.1/39.2 smoke

#### Verify A — Datum DualImage swap 회귀 (Phase 34.1 / 36 / 37 보존)
- Side 시퀀스 4 Datum DualImage 노드 선택.
- swap 토글 / 배지 / ROI 정상 동작.
- "Test Find Datum" 버튼 정상 동작.
- Datum ↔ Measurement 전환 시 mutex 정상 (양방향 clear).

#### Verify B — 기존 7 Measurement 타입 회귀 (UI 분기 보존)
다음 각 1개씩 (총 7) FAI 측정 노드 선택 → `btn_rectRoi` 활성 + ROI 드로잉 정상:
- EdgeToLineDistance
- EdgeToLineAngle
- ArcEdgeDistance
- CompoundAngle
- CompoundCenterCDistance
- CompoundCenterBDistance
- CompoundShortAxisDistance
- ArcLineIntersect
- (참고: 위 8개 중 7+1 변경 0 가드, DualImage 추가 1 + 화이트리스트 보존)

#### Verify C — DualImage 측정값 byte-identical
- Bottom E5 DualImage 검사 SIMUL 실행.
- 측정값 (mm) 이 Phase 39.2-01 baseline 과 일치 (TryExecute 본문 변경 0 가드 = Anti-Goal 1).

#### Verify D — Phase 39.1 4항목 smoke test (Anti-Goal #7 가드)
- (1) CircleDiameter — Bottom CircleDiameter Measurement 1개 클릭 → PropertyGrid 파라미터 (Radius/Center) 정상 표시 (1 클릭).
- (2) EdgeToLineDistance projection_pl — EdgeToLineDistance Measurement 1개 클릭 → SIMUL 검사 1회 실행 → 측정값 표시 (1 클릭 + 1 실행).
- (3) FAI 개별 검사 토글 — FAI 노드 우클릭 메뉴 또는 토글 버튼 1회 클릭 → 개별 검사 동작 (1 클릭).
- (4) Datum CircleTwoHorizontal Edit 모드 — Side CircleTwoHorizontal Datum 1개 선택 → Edit 모드 진입 1회 → ROI 표시 정상 (1 클릭).

#### Verify E — Phase 39.2 D-G2~G4 smoke test (Anti-Goal #7 가드)
- (1) I10 close-point variant — Bottom I10 close-point variant Measurement 1개로 SIMUL 검사 1 시퀀스 실행 → 측정값 정상 (1 실행).
- (2) Tree Move ▲▼ 버튼 — InspectionListView 트리에서 임의 노드 선택 → Move ▲ 또는 ▼ 버튼 1회 클릭 → 노드 순서 변경 동작 (1 클릭).
- (3) Tree 18 Geometry 아이콘 — InspectionListView 트리 18 Geometry 아이콘 시각 확인 (각 Measurement 타입별 아이콘 정상 표시, 1 회 시각 확인).

**Expected:** Verify A~E 모두 PASS = 회귀 0 보장.

**Result:** **PENDING** (사용자 SIMUL UAT 대기)

---

## Sign-off 절차 (Task 04-02 사용자 보고 후)

**ALL PASS 시:**
- frontmatter `status: signed_off` / `passed: 5` / `pending: 0` 갱신.
- ROADMAP.md Phase 39.3 체크박스 ✅.
- STATE.md `status: SIGNED_OFF` + 다음 phase = 40 (OUT-01/02) 또는 carry-over.
- CO-39.2-01-01 carry-over 종결.

**부분 FAIL 시:**
- frontmatter `status: partial` + 실패 Test 의 result 필드에 FAIL 상세 + carry-over 후보 식별.
- Test 4 (Browse 버튼) FAIL 시 → Risk R4 fallback (a/b/c/d) 적용 후 재테스트.
- 잔여 결함 = 신규 hotfix Quick 또는 Phase 39.4 신설 합의.

## Notes

- 모든 검증은 사용자 SIMUL_MODE 화면 시각/동작 확인 한정. 자동 픽셀 비교 도구 없음.
- Plan 01 (Wave 1) / Plan 02 (Wave 2) / Plan 03 (Wave 1, 병렬) → 모두 Wave 3 진입 전 완료. Wave 3 = Plan 04 단독.
- Anti-Goal 5 (Datum 코드 변경 0) 는 PublishDatumRoiCandidates 의 mutex clear 1줄 *추가* 만 허용 (Datum 측 로직은 그대로). 양방향 mutex 가드 (Anti-Goal 7) 와 일치.
- Phase 39.2-01-SUMMARY 의 "기존 7+1 분기 변경 0" 가드와 일관 — 8번째 분기 (DualImage) 만 화이트리스트에 append.
