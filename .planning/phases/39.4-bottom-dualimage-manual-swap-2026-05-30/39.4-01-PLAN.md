---
phase: 39.4-bottom-dualimage-manual-swap-2026-05-30
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs
autonomous: true
requirements: [WF-01]
tags: [model, dualimage, propertygrid, attribute, hangul]

must_haves:
  truths:
    - "DualImageEdgeDistanceMeasurement 에 새 string property `TeachingImagePath_Horizontal` 가 default `\"\"` 로 선언된다"
    - "신규 필드에 5종 attribute ([Category(\"Image|DualImage\")] + [Description] + [DisplayName(\"가로축 티칭 이미지\")] + [InputFilePath(EXTENSION_IMAGE,FILTER_IMAGE)] + [AutoUpdateText]) 가 부여된다"
    - "기존 `TeachingImagePath_Vertical` 의 attribute 위에 `[DisplayName(\"세로축 티칭 이미지\")]` 1줄만 추가된다 (기존 attribute 4종 그대로 유지)"
    - "ParamBase reflection 의 `case \"String\"` 분기가 신규 필드를 자동 픽업 — INI Save/Load 코드 변경 0 (Phase 22 IMG-01 baseline 패턴)"
    - "`TryExecute` 본문 / `IDatumOriginConsumer` 6 필드 / `RuntimeImageA/B` / PointROI · LineROI 필드 변경 0 (Anti-Goal #1)"
    - "using 블록 변경 0 (`PropertyTools.DataAnnotations` 의 `[DisplayName]` 활용, 추가 import 불필요 — Research RT-1.1/1.2 검증)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs"
      provides: "`TeachingImagePath_Horizontal` 신규 property + 한글 [DisplayName] attribute 양측"
      contains: "TeachingImagePath_Horizontal"
  key_links:
    - from: "DualImageEdgeDistanceMeasurement.TeachingImagePath_Horizontal"
      to: "ParamBase.Save/Load (case \"String\")"
      via: "reflection auto-pickup — Phase 22 IMG-01 baseline (default \"\")"
      pattern: "case \"String\""
    - from: "[DisplayName(\"가로축 티칭 이미지\")]"
      to: "PropertyGrid 셀 헤더 한글 표시"
      via: "PropertyTools.DataAnnotations.DisplayNameAttribute (Action_WaferScanCalibration.cs:56 11 use site 검증)"
      pattern: "\\[DisplayName\\(\"가로축"
    - from: "[InputFilePath(DeviceHandler.EXTENSION_IMAGE, DeviceHandler.FILTER_IMAGE)]"
      to: "PropertyGrid Browse 버튼 노출 + OpenFileDialog"
      via: "Phase 39.3 Plan 03 (commit f260cf0) 검증 패턴 mirror"
      pattern: "InputFilePath\\(DeviceHandler\\.EXTENSION_IMAGE"
---

<objective>
**Plan 01 — Wave 1: DualImageEdgeDistanceMeasurement 신규 필드 + attribute (D-G1 + D-G3)**

`DualImageEdgeDistanceMeasurement` 모델에 `TeachingImagePath_Horizontal` 신규 string 필드를 추가하여 가로축 이미지를 **Measurement 단위 명시 경로** 로 보관할 수 있게 한다. 기존 `TeachingImagePath_Vertical` 에는 한글 라벨 일관성 차원에서 `[DisplayName("세로축 티칭 이미지")]` 1줄만 추가.

Purpose: Plan 02 (Action_FAIMeasurement fallback) 와 Plan 03 (MainView swap + 배지) 의 진입점 — 두 Plan 모두 본 필드 존재에 컴파일 의존. ParamBase reflection 자동 직렬화 (Phase 22 IMG-01 baseline 패턴) 로 INI 호환성 보장.
Output: PropertyGrid 의 `Image|DualImage` 카테고리에 한글 라벨 가로축/세로축 2 셀 양측이 [Browse...] 버튼과 함께 노출. 기존 INI 레시피는 default `""` + reflection 자동 픽업으로 byte-identical 호환.

**Wave 1 단독 실행** — 다른 Plan 의존성 없음. Plan 02/03 의 Wave 2 진입을 위한 baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-CONTEXT.md
@.planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-RESEARCH.md
@.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-PATTERNS.md
@.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-03-PLAN.md
@WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs
@WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
@WPF_Example/UI/ViewModel/ModelFinderViewModel.cs
@WPF_Example/Custom/Device/DeviceHandler.cs
@WPF_Example/Sequence/Param/ParamBase.cs
@WPF_Example/Custom/Sequence/Wafer/Action_WaferScanCalibration.cs

<interfaces>
<!-- Key types and contracts for executor reference -->

DualImageEdgeDistanceMeasurement (현재 L1-50 검증):
- namespace ReringProject.Sequence
- public class DualImageEdgeDistanceMeasurement : MeasurementBase, IDatumOriginConsumer
- 기존 using 블록 (L2-7):
    using System.Collections.Generic;
    using HalconDotNet;
    using PropertyTools.DataAnnotations;          // [DisplayName] / [Category] / [InputFilePath] / [AutoUpdateText] 출처
    using ReringProject.Device;                   // DeviceHandler.EXTENSION_IMAGE / FILTER_IMAGE 출처 (Phase 39.3 D-G3 도입)
    using ReringProject.Halcon.Algorithms;
    using ReringProject.Halcon.Models;

기존 TeachingImagePath_Vertical 선언 (L23-29) — Plan 01 변경 대상:
    [Category("Image|DualImage")]                                                              // L25
    [System.ComponentModel.Description("LineROI 검출용 별도 이미지 경로 — Bottom E5 패턴 ...")] // L26
    [InputFilePath(DeviceHandler.EXTENSION_IMAGE, DeviceHandler.FILTER_IMAGE)]                 // L27 (Phase 39.3 D-G3)
    [AutoUpdateText]                                                                            // L28 (Phase 39.3 D-G3)
    public string TeachingImagePath_Vertical { get; set; } = "";                               // L29

DeviceHandler 상수 (WPF_Example/Custom/Device/DeviceHandler.cs:71-72):
- public const string FILTER_IMAGE = "tiff Files(*.tiff)|*.tiff|bmp Files(*.bmp)|*.bmp|jpg Files(*.jpg)|*.jpg|jpeg Files(*.jpeg)|*.jpeg|png Files(*.png)|*.png|All Files(*.*)|*.*";
- public const string EXTENSION_IMAGE = ".tiff";

ParamBase reflection (WPF_Example/Sequence/Param/ParamBase.cs:324-430):
- Save 의 case "String" L337-339: saveFile[group][name] = (string)prop.GetValue(this);
- Load 의 case "String" L385-395: prop.SetValue(this, loadFile[group][name].ToString());
- 신규 string property 추가 시 attribute 외 코드 변경 0 — auto-pickup 보장 (Phase 22 IMG-01 + Phase 34 D-34-04 baseline)

PropertyTools DisplayName (RT-1.1 검증):
- FullName = PropertyTools.DataAnnotations.DisplayNameAttribute
- using PropertyTools.DataAnnotations; 만으로 PropertyGrid 셀 헤더 매핑 (Action_WaferScanCalibration.cs:56 + 10 추가 use site)
- System.ComponentModel.DisplayName 사용 X — 추가 import 불필요

브레이스 스타일 (CLAUDE.md + 파일 현황):
- DualImageEdgeDistanceMeasurement.cs = Allman (Halcon 신규 코드 컨벤션)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 01-01: DualImageEdgeDistanceMeasurement.cs — TeachingImagePath_Horizontal 신규 필드 + 5종 attribute (D-G1 + D-G3)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs (전체 L1-50 — 기존 using + Vertical 선언 블록 확인)
    - .planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-CONTEXT.md (D-G1 fallback 정책 + D-G3 한글 라벨)
    - .planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-RESEARCH.md (RT-1 PropertyTools DisplayName + RT-2.4 declaration 순서 lock-in: Vertical 뒤 추가)
    - WPF_Example/Custom/Sequence/Wafer/Action_WaferScanCalibration.cs (L1-15, L55-70 — `using PropertyTools.DataAnnotations;` 단일 import 으로 `[DisplayName]` 정상 동작 검증 패턴)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (L38-46 — Phase 22 IMG-01 baseline: `public string TeachingImagePath { get; set; } = "";` default 패턴)
    - WPF_Example/Custom/Device/DeviceHandler.cs (L71-72 — EXTENSION_IMAGE/FILTER_IMAGE 상수 정의 확인)
  </read_first>
  <action>
    //260530 hbk Phase 39.4 D-G1 + D-G3 주석 의무. RESEARCH RT-1.3 의 Plan 01 적용 패턴이 baseline.

    **위치 lock-in (RT-2.4):** 기존 `TeachingImagePath_Vertical` (L29) **다음 줄** 에 신규 `TeachingImagePath_Horizontal` 블록 추가. 절대 위치 보존 (Anti-Goal #6 — Vertical 위치 보존, Horizontal 은 신규 추가만).

    **수정 1 (Vertical attribute 블록에 [DisplayName] 1줄 삽입):**

    기존 `[Description]` (L26) 다음 줄 + `[InputFilePath]` (L27) 이전 줄에 `[DisplayName("세로축 티칭 이미지")]` 1줄 삽입 (정확히 L27 위치에 신규 라인 추가, 기존 L27 `[InputFilePath]` 는 L28 로 이동). attribute 그룹 순서는 PropertyTools 컨벤션 (Action_WaferScanCalibration.cs L55-69) 을 따라 `[Category]` → `[Description]` → `[DisplayName]` → `[InputFilePath]` → `[AutoUpdateText]` 순.

    Before (L25-29):
    ```csharp
            [Category("Image|DualImage")] //260530 hbk Phase 39.2 D-G1
            [System.ComponentModel.Description("LineROI 검출용 별도 이미지 경로 — Bottom E5 패턴 (Phase 37 VTH-DualImage 슬롯 재사용)")] //260530 hbk Phase 39.2 D-G1
            [InputFilePath(DeviceHandler.EXTENSION_IMAGE, DeviceHandler.FILTER_IMAGE)] //260530 hbk Phase 39.3 D-G3
            [AutoUpdateText] //260530 hbk Phase 39.3 D-G3 — 즉시 INPC fire (ModelFinderViewModel L28-29 패턴)
            public string TeachingImagePath_Vertical { get; set; } = ""; //260530 hbk Phase 39.2 D-G1
    ```

    After (L25-30, 1줄 추가):
    ```csharp
            [Category("Image|DualImage")] //260530 hbk Phase 39.2 D-G1
            [System.ComponentModel.Description("LineROI 검출용 별도 이미지 경로 — Bottom E5 패턴 (Phase 37 VTH-DualImage 슬롯 재사용)")] //260530 hbk Phase 39.2 D-G1
            [DisplayName("세로축 티칭 이미지")] //260530 hbk Phase 39.4 D-G3 — PropertyTools.DataAnnotations.DisplayName (Action_WaferScanCalibration L56 패턴)
            [InputFilePath(DeviceHandler.EXTENSION_IMAGE, DeviceHandler.FILTER_IMAGE)] //260530 hbk Phase 39.3 D-G3
            [AutoUpdateText] //260530 hbk Phase 39.3 D-G3 — 즉시 INPC fire (ModelFinderViewModel L28-29 패턴)
            public string TeachingImagePath_Vertical { get; set; } = ""; //260530 hbk Phase 39.2 D-G1
    ```

    **수정 2 (Vertical 선언 직후, L29 다음 줄에 신규 Horizontal 블록 6줄 추가):**

    `TeachingImagePath_Vertical` (After 기준 L30) 다음에 빈 줄 1줄 + 신규 블록 6줄 추가:

    ```csharp
            //260530 hbk Phase 39.4 D-G1 — PointROI 검출용 가로축 이미지 경로. 명시 시 우선 사용, 빈 문자열/파일 부재 시 ShotConfig.SimulImagePath 로 fallback (Action_FAIMeasurement.TryGrabOrLoadFaiDualImages L403 분기).
            [Category("Image|DualImage")] //260530 hbk Phase 39.4 D-G1
            [System.ComponentModel.Description("PointROI 검출용 가로축 이미지 경로 — 명시 시 우선 사용, 빈 문자열/파일 부재 시 ShotConfig.SimulImagePath 로 fallback (D-G1)")] //260530 hbk Phase 39.4 D-G1
            [DisplayName("가로축 티칭 이미지")] //260530 hbk Phase 39.4 D-G3 — PropertyTools.DataAnnotations.DisplayName (Action_WaferScanCalibration L56 패턴, RT-1.1/1.2 검증)
            [InputFilePath(DeviceHandler.EXTENSION_IMAGE, DeviceHandler.FILTER_IMAGE)] //260530 hbk Phase 39.4 D-G3 — Phase 39.3 Plan 03 mirror
            [AutoUpdateText] //260530 hbk Phase 39.4 D-G3 — 즉시 INPC fire (ModelFinderViewModel L28-29 패턴)
            public string TeachingImagePath_Horizontal { get; set; } = ""; //260530 hbk Phase 39.4 D-G1
    ```

    **회귀 가드 (Anti-Goal):**
    - using 블록 (L2-7) 변경 0 — `PropertyTools.DataAnnotations` + `ReringProject.Device` 이미 import (Phase 39.3 D-G3) → 추가 import 0
    - `TypeName` (L21) / `TryExecute` 본문 / PointROI · LineROI 필드 (L33-) / `IDatumOriginConsumer` 인터페이스 멤버 / RuntimeImageA · RuntimeImageB / EdgePolarityList · EdgeDirectionList · EdgeSelectionList 변경 0
    - Vertical property 자체의 `[Category]` / `[Description]` / `[InputFilePath]` / `[AutoUpdateText]` 4종 attribute 라인 변경 0 — `[DisplayName]` 1줄 삽입만
    - default `""` 양측 일관 → INI 호환 (RT-2.2/2.3, 기존 INI 레시피 Load 시 byte-identical)
    - INI 직렬화 순서: Vertical 위치 절대 보존, Horizontal 은 Vertical 뒤 신규 추가 (Anti-Goal #6 + RT-2.4 lock)

    **브레이스/스타일:** Allman 유지 (해당 파일 컨벤션). attribute 는 각 1줄, `public string ... { get; set; } = "";` 한 줄.
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 — 0 new errors / 0 new warnings (Phase 39.3 baseline 대비)</automated>
    grep 검증:
    - `grep -n "TeachingImagePath_Horizontal" WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` → 정확히 1 라인 hit (declaration)
    - `grep -n "TeachingImagePath_Vertical" WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` → 정확히 1 라인 hit (declaration, 기존 보존)
    - `grep -n "\[DisplayName(\"가로축 티칭 이미지\")\]" WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` → 1 라인 hit
    - `grep -n "\[DisplayName(\"세로축 티칭 이미지\")\]" WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` → 1 라인 hit
    - `grep -cn "Phase 39.4" WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` → 8+ 라인 hit (Vertical [DisplayName] 1 + Horizontal 7줄)
    - `grep -n "TryExecute" WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` → Phase 39.2 baseline 과 동일 hit (본문 변경 0)
    - `git diff WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` → using 블록 (L2-7) 0 변경 + Vertical 이외 영역 변경 0
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 PASS, 0 new errors, 0 new warnings
    - 신규 `TeachingImagePath_Horizontal` property 가 default `""` 로 선언됨
    - `TeachingImagePath_Horizontal` 에 5종 attribute (`[Category]` + `[Description]` + `[DisplayName("가로축 티칭 이미지")]` + `[InputFilePath]` + `[AutoUpdateText]`) 모두 부여
    - `TeachingImagePath_Vertical` attribute 블록의 `[Description]` 와 `[InputFilePath]` 사이에 `[DisplayName("세로축 티칭 이미지")]` 1줄 추가 (기존 4종 attribute 그대로)
    - Horizontal 선언 위치 = Vertical 다음 줄 (RT-2.4 lock — Vertical 절대 위치 보존)
    - using 블록 추가 0 (`PropertyTools.DataAnnotations` 의 `[DisplayName]` 사용 — RT-1.1/1.2 검증)
    - `TryExecute` / `IDatumOriginConsumer` 6 필드 / `RuntimeImageA/B` / PointROI · LineROI 필드 변경 0 (git diff 검증)
    - INI 직렬화 순서: Vertical 위치 보존, Horizontal 은 Vertical 뒤 신규 추가 (Anti-Goal #6)
  </acceptance_criteria>
  <done>
    `DualImageEdgeDistanceMeasurement` 가 PointROI 검출용 `TeachingImagePath_Horizontal` (default `""`) 을 Measurement 단위 명시 경로로 보관 가능. ParamBase reflection 이 신규 string property 를 INI Save/Load 자동 직렬화 (Phase 22 IMG-01 baseline). PropertyGrid 의 `Image|DualImage` 카테고리에 한글 라벨 "가로축 티칭 이미지" / "세로축 티칭 이미지" 2 셀이 양측 [Browse...] 버튼과 함께 노출 (Plan 04 Test 1 검증). Plan 02 (Action_FAIMeasurement fallback) + Plan 03 (MainView swap/배지) 컴파일 진입점 확보.
  </done>
</task>

</tasks>

<verification>
**Plan 01 전체 검증 (Anti-Goal 가드):**

1. **빌드 PASS 검증**:
   - `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64` → 0 new errors, 0 new warnings (Phase 39.3 baseline 대비)

2. **필드/attribute 추가 정확성** (grep):
   - `TeachingImagePath_Horizontal` declaration = 1 라인
   - `TeachingImagePath_Vertical` declaration = 1 라인 (기존 보존)
   - `[DisplayName("가로축 티칭 이미지")]` = 1 라인
   - `[DisplayName("세로축 티칭 이미지")]` = 1 라인
   - `[InputFilePath(DeviceHandler.EXTENSION_IMAGE, DeviceHandler.FILTER_IMAGE)]` = 2 라인 (Vertical 기존 + Horizontal 신규)
   - `[AutoUpdateText]` = 2 라인 (Vertical 기존 + Horizontal 신규)
   - `[Category("Image|DualImage")]` = 2 라인 (Vertical 기존 + Horizontal 신규)

3. **Anti-Goal 가드 (코드 인스펙션)**:
   - using 블록 (L2-7) `git diff` 결과 0 라인 변경
   - `TryExecute` 본문 변경 0 (Anti-Goal #1)
   - `IDatumOriginConsumer` 6 필드 + RuntimeImageA/B + PointROI · LineROI · Edge* 필드 변경 0
   - 다른 7 Measurement 타입 파일 (EdgeToLineDistance / EdgeToLineAngle / ArcEdgeDistance / Compound 4종 / ArcLineIntersect) 변경 0 (Anti-Goal #4)
   - MeasurementFactory.cs 변경 0 (Anti-Goal #2)
   - DatumConfig.cs (Phase 22/34/37) 변경 0 (Anti-Goal #3 — Datum DualImage 회귀 0, D-G2)
   - INI 직렬화 순서: Vertical 위치 절대 보존, Horizontal 뒤 신규 추가 (Anti-Goal #6 + RT-2.4)

4. **컴파일 의존 진입점**:
   - Plan 02 (Action_FAIMeasurement) 가 `dualMeas.TeachingImagePath_Horizontal` 참조 가능
   - Plan 03 (MainView.BtnSwapHorizontal_Click / UpdateImageSourceBadge) 가 `meas.TeachingImagePath_Horizontal` 참조 가능
</verification>

<success_criteria>
- msbuild Debug/x64 PASS (0 new errors, 0 new warnings)
- 신규 필드 `TeachingImagePath_Horizontal` 가 5종 attribute 와 함께 default `""` 로 선언
- `TeachingImagePath_Vertical` 에 `[DisplayName("세로축 티칭 이미지")]` 1줄만 추가 (기존 4종 attribute 보존)
- using 추가 0 (Anti-Goal — RT-1.1/1.2 검증)
- Plan 02/03 의 Wave 2 진입을 위한 컴파일 의존 진입점 확보
- ParamBase reflection 자동 직렬화 + INI 호환 (Phase 22 IMG-01 baseline) — Plan 04 Test 4 회귀 검증 대상
</success_criteria>

<output>
After completion, create `.planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-01-SUMMARY.md` with:
- Task 01-01 changes (file:line + 추가 라인 수 = 신규 7줄 + Vertical [DisplayName] 1줄 = 8줄)
- Anti-Goal 가드 검증 결과 (위 verification 4개 항)
- msbuild Debug/x64 결과 (errors/warnings 카운트)
- Plan 02 / Plan 03 와의 인터페이스 (`TeachingImagePath_Horizontal` property 존재 의존)
</output>
</content>
