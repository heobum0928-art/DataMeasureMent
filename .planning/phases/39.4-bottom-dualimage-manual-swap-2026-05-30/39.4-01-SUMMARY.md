---
phase: 39.4-bottom-dualimage-manual-swap-2026-05-30
plan: "01"
subsystem: measurement-model
tags: [model, dualimage, propertygrid, attribute, hangul, d-g1, d-g3]
dependency_graph:
  requires: []
  provides:
    - DualImageEdgeDistanceMeasurement.TeachingImagePath_Horizontal (string property, default "")
  affects:
    - Plan 02 (Action_FAIMeasurement.TryGrabOrLoadFaiDualImages — TeachingImagePath_Horizontal 참조)
    - Plan 03 (MainView.BtnSwapHorizontal_Click — TeachingImagePath_Horizontal 참조)
    - Plan 04 (UAT Test 1 PropertyGrid 시각 검증)
tech_stack:
  added: []
  patterns:
    - PropertyTools.DataAnnotations.DisplayName (RT-1.1/1.2 검증, Action_WaferScanCalibration L56 패턴)
    - ParamBase reflection case "String" auto-pickup (Phase 22 IMG-01 baseline)
    - [InputFilePath + AutoUpdateText] Browse 버튼 패턴 (Phase 39.3 Plan 03 mirror)
key_files:
  created: []
  modified:
    - WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs
decisions:
  - D-G1 fallback 정책: TeachingImagePath_Horizontal 미설정 시 ShotConfig.SimulImagePath fallback (회귀 0)
  - D-G3 PropertyGrid 라벨: PropertyTools.DataAnnotations.DisplayName 사용 확정 (System.ComponentModel.DisplayName 불필요)
  - RT-2.4 선언 순서: Vertical 다음(뒤)에 Horizontal 추가 — INI Vertical 절대 위치 보존
metrics:
  duration_sec: 111
  completed_date: "2026-05-30"
  tasks_completed: 1
  tasks_total: 1
  files_changed: 1
---

# Phase 39.4 Plan 01: DualImageEdgeDistanceMeasurement 신규 필드 + attribute Summary

`DualImageEdgeDistanceMeasurement` 에 가로축 이미지 경로 `TeachingImagePath_Horizontal` (default `""`) 신규 필드 + 5종 attribute + Vertical 한글 라벨 `[DisplayName("세로축 티칭 이미지")]` 추가 — D-G1 ShotConfig fallback + D-G3 PropertyGrid 한글 라벨 2개 셀 양측 Browse 버튼 노출 기반 확보.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 01-01 | DualImageEdgeDistanceMeasurement.cs 수정 — TeachingImagePath_Horizontal 신규 필드 + 5종 attribute + Vertical [DisplayName] 추가 | 5cf4fb3 | DualImageEdgeDistanceMeasurement.cs (+9줄) |

## Changes Made

### Task 01-01: DualImageEdgeDistanceMeasurement.cs

**파일:** `WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs`

**변경 요약 (+9줄, 삭제 0줄):**

1. **수정 1 — Vertical attribute 블록에 [DisplayName] 1줄 삽입 (L27)**
   - `[Description]` (L26) 다음, `[InputFilePath]` (구 L27) 앞에 삽입:
   - `[DisplayName("세로축 티칭 이미지")] //260530 hbk Phase 39.4 D-G3`
   - 기존 4종 attribute (`[Category]` + `[Description]` + `[InputFilePath]` + `[AutoUpdateText]`) 변경 0

2. **수정 2 — TeachingImagePath_Vertical (L30) 직후 Horizontal 블록 8줄 추가 (L32-38)**
   - 주석 1줄 (D-G1 fallback 정책 설명)
   - `[Category("Image|DualImage")]` 1줄
   - `[System.ComponentModel.Description("PointROI 검출용 가로축 이미지 경로...")]` 1줄
   - `[DisplayName("가로축 티칭 이미지")]` 1줄
   - `[InputFilePath(DeviceHandler.EXTENSION_IMAGE, DeviceHandler.FILTER_IMAGE)]` 1줄
   - `[AutoUpdateText]` 1줄
   - `public string TeachingImagePath_Horizontal { get; set; } = "";` 1줄
   - 빈 줄 1줄 (블록 구분)

## Verification Results

### 1. msbuild Debug/x64 빌드 검증

**결과: PASS**
- 빌드 출력: `DatumMeasurement -> ...\bin\x64\Debug\DatumMeasurement.exe`
- **신규 오류:** 0
- **신규 경고:** 0
- 기존 경고 6개 (Phase 33 CS0618 × 5 + VirtualCamera CS0162 × 1 + MSB3884 × 2) = Phase 39.3 baseline 동일, 신규 0

### 2. grep 검증 결과 (8가지 모두 PASS)

| 검증 항목 | 결과 |
|-----------|------|
| `TeachingImagePath_Horizontal` declaration = 1 라인 hit | PASS (L38) |
| `TeachingImagePath_Vertical` declaration = 1 라인 hit | PASS (L30, 기존 보존) |
| `[DisplayName("가로축 티칭 이미지")]` = 1 라인 hit | PASS (L35) |
| `[DisplayName("세로축 티칭 이미지")]` = 1 라인 hit | PASS (L27) |
| `[InputFilePath(DeviceHandler...)]` = 2 라인 (Vertical 기존 + Horizontal 신규) | PASS |
| `[AutoUpdateText]` = 2 라인 | PASS |
| `[Category("Image\|DualImage")]` = 2 라인 | PASS |
| Phase 39.4 주석 = 8 라인 이상 | PASS (8라인) |

### 3. Anti-Goal 가드 (git diff 검증)

| Anti-Goal | 결과 |
|-----------|------|
| using 블록 (L2-7) 변경 0 | PASS — git diff 확인, L2-7 무변경 |
| `TryExecute` 본문 변경 0 | PASS — diff에 TryExecute 영역 0줄 변경 |
| `IDatumOriginConsumer` 6 필드 변경 0 | PASS — DatumOriginRow/Col/AngleRad/Angle2Rad/DetectedCircleRow/Col 변경 0 |
| `RuntimeImageA/B` 변경 0 | PASS — 변경 0 |
| `PointROI` / `LineROI` 필드 변경 0 | PASS — 변경 0 |
| `EdgePolarityList/DirectionList/SelectionList` 변경 0 | PASS — 변경 0 |
| INI 직렬화 순서: Vertical 위치 보존 | PASS — Vertical L30 절대 위치 보존, Horizontal L32-38 후(뒤) 추가 |
| using 추가 0 | PASS — `PropertyTools.DataAnnotations` + `ReringProject.Device` 기존 import 재사용 |

### 4. 컴파일 의존 진입점 확보

- Plan 02 (`Action_FAIMeasurement`) 가 `dualMeas.TeachingImagePath_Horizontal` 참조 가능 — 필드 존재 확인
- Plan 03 (`MainView.BtnSwapHorizontal_Click`) 가 `meas.TeachingImagePath_Horizontal` 참조 가능 — 필드 존재 확인

## Deviations from Plan

PLAN.md 의 `before/after` 코드 블록에서 Horizontal 블록 순서가 Vertical **위** 위치 예시로 기술되어 있으나, RESEARCH RT-2.4 lock-in 및 Plan `<action>` 섹션 "위치 lock-in (RT-2.4): 기존 TeachingImagePath_Vertical (L29) 다음 줄에 신규 TeachingImagePath_Horizontal 블록 추가" 지시에 따라 **Vertical 뒤에 추가** 실행. Anti-Goal #6 ("Vertical 위치 절대 보존") 준수.

이는 Rule 1 수준의 명세 불일치 해결 — PLAN.md 본문 `<action>` 지시와 RT-2.4 lock-in이 Vertical 뒤 추가를 명시적으로 지정하므로 deviation이 아닌 정상 실행.

## Known Stubs

없음 — `TeachingImagePath_Horizontal` 필드 자체는 완전히 선언됨. Plan 02 (Action_FAIMeasurement fallback 분기) + Plan 03 (MainView swap 배지 텍스트) 에서 실제 사용 배선 예정.

## Threat Flags

없음 — 모델 property 선언만. 파일 경로 참조 로직은 Plan 02/03에서 추가.

## Plan 02 / Plan 03 인터페이스

- **Plan 02 의존:** `DualImageEdgeDistanceMeasurement.TeachingImagePath_Horizontal` (string) — `Action_FAIMeasurement.TryGrabOrLoadFaiDualImages` 에서 `dualMeas.TeachingImagePath_Horizontal` 으로 참조. D-G1 fallback ternary 진입점.
- **Plan 03 의존:** `DualImageEdgeDistanceMeasurement.TeachingImagePath_Horizontal` (string) — `MainView.BtnSwapHorizontal_Click` Measurement 분기에서 `meas.TeachingImagePath_Horizontal` 으로 참조. D-G4 배지 텍스트 분기 진입점.
- **INI 직렬화:** ParamBase reflection `case "String"` auto-pickup — Plan 04 Test 4 (INI 라운드트립 회귀 검증) 대상.

## Self-Check: PASSED

- [x] `WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` 존재 확인
- [x] commit `5cf4fb3` 존재 확인 (`git rev-parse --short HEAD`)
- [x] msbuild PASS (0 new errors, 0 new warnings)
- [x] grep 8가지 PASS
- [x] Anti-Goal git diff PASS
