---
phase: 39.4-bottom-dualimage-manual-swap-2026-05-30
plan: 02
type: execute
wave: 2
depends_on: ["39.4-01"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: true
requirements: [WF-01]
tags: [sequence, fai, dualimage, fallback, runtime]

must_haves:
  truths:
    - "Action_FAIMeasurement.TryGrabOrLoadFaiDualImages 의 pathA 계산이 명시적 if/else 로 교체되어 dualMeas.TeachingImagePath_Horizontal 우선 + ShotConfig.SimulImagePath fallback 동작"
    - "TeachingImagePath_Horizontal 명시 + 파일 존재 → pathA = dualMeas.TeachingImagePath_Horizontal"
    - "TeachingImagePath_Horizontal 빈 문자열 또는 파일 부재 → pathA = ShotParam.SimulImagePath (Phase 39.2 baseline 회귀 0)"
    - "L404 pathB = dualMeas.TeachingImagePath_Vertical 라인 변경 0 (Vertical 은 항상 Measurement 단위 경로 baseline 유지)"
    - "L394-L425 함수 본체 (시그니처 / null guard / dualMeas 캐스트 / pathB / file load / HImage Dispose 가드 / return 분기) 변경 0 (Anti-Goal — TryGrabOrLoadFaiDualImages 동형 패턴 보존)"
    - "ternary 회피 — 명시적 if/else (RESEARCH R3 권장, 회귀 표면 최소화)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "TryGrabOrLoadFaiDualImages pathA 계산 분기 (D-G1 fallback)"
      contains: "dualMeas.TeachingImagePath_Horizontal"
  key_links:
    - from: "Action_FAIMeasurement.TryGrabOrLoadFaiDualImages L403"
      to: "DualImageEdgeDistanceMeasurement.TeachingImagePath_Horizontal (Plan 01)"
      via: "dualMeas 캐스트 후 string property read"
      pattern: "dualMeas\\.TeachingImagePath_Horizontal"
    - from: "pathA = ShotParam.SimulImagePath (fallback)"
      to: "Phase 39.2 baseline 동작 보존 (기존 INI 레시피 byte-identical)"
      via: "if (string.IsNullOrEmpty || !File.Exists) → fallback else 명시 경로"
      pattern: "ShotParam\\.SimulImagePath"
    - from: "L406 pathA 가드"
      to: "fallback 결과로 set 된 SimulImagePath 가 같은 가드 통과"
      via: "RESEARCH R3 — IsNullOrEmpty || !File.Exists 동일 패턴"
      pattern: "string\\.IsNullOrEmpty\\(pathA\\) \\|\\| !File\\.Exists\\(pathA\\)"
---

<objective>
**Plan 02 — Wave 2 (Plan 01 의존): Action_FAIMeasurement pathA fallback if/else (D-G1)**

`Action_FAIMeasurement.TryGrabOrLoadFaiDualImages` 의 L403 `string pathA = ShotParam.SimulImagePath;` (Phase 39.2 baseline) 를 **D-G1 fallback 정책** 에 맞춰 명시적 if/else 로 교체. `dualMeas.TeachingImagePath_Horizontal` 이 명시되고 파일이 존재하면 그 경로를, 아니면 기존 baseline 인 `ShotParam.SimulImagePath` 를 사용.

Purpose: Plan 03 (MainView swap UX 가로축 분기 — Measurement 우선 → fallback ShotConfig) 의 검사 시점 대응. 검사 실행 시 RuntimeImageA 의 소스가 UI swap 동작과 동일 우선순위 (Measurement 명시 > ShotConfig fallback) 를 따르도록 통일. 기존 INI 레시피 (Horizontal 부재) 는 fallback 으로 byte-identical 회귀 0.
Output: Action_FAIMeasurement 가 DualImage Measurement 의 가로축 이미지 소스를 Measurement 단위 명시 경로 → ShotConfig fallback 순으로 결정. `TryExecute` 본문 / RuntimeImageA·B 처리 / 측정 알고리즘 변경 0.

**Wave 2 단독 — Plan 01 의 TeachingImagePath_Horizontal 컴파일 의존.** Plan 03 와 파일 충돌 0 (Plan 03 = MainView.xaml.cs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-CONTEXT.md
@.planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-RESEARCH.md
@.planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-01-PLAN.md
@.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-PATTERNS.md
@WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
@WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs

<interfaces>
<!-- Key types and contracts for executor reference -->

TryGrabOrLoadFaiDualImages 현재 상태 (Action_FAIMeasurement.cs L386-423 검증):

```csharp
//260530 hbk Phase 39.2 D-G1-06 — DualImageEdgeDistanceMeasurement 측정용 양 이미지 로드.
//  imageA: ShotParam.SimulImagePath (1차) — PointROI 검출용 (FAI 1차 이미지 = Shot 검사 이미지 재사용, 회귀 0 baseline).
//  imageB: meas.TeachingImagePath_Vertical (2차) — LineROI 검출용 (D-G1-03 슬롯 컨벤션).
//  P37 TryGrabOrLoadDualDatumImages 동형 패턴 — 경로 빈/파일없음 → false + 로그.
//  HImage 한쪽 생성 실패 시 양쪽 Dispose + false (메모리 누수 방지).
private bool TryGrabOrLoadFaiDualImages(MeasurementBase meas, out HImage imageA, out HImage imageB) { // L391
    imageA = null;
    imageB = null;
    if (ShotParam == null) {
        Logging.PrintErrLog((int)ELogType.Error, "[FAI DualImage] ShotParam null");
        return false;
    }
    var dualMeas = meas as DualImageEdgeDistanceMeasurement;          // L398
    if (dualMeas == null) {
        Logging.PrintErrLog((int)ELogType.Error, "[FAI DualImage] meas 가 DualImageEdgeDistanceMeasurement 가 아닙니다");
        return false;
    }
    string pathA = ShotParam.SimulImagePath; //260530 hbk Phase 39.2 D-G1-06 — PointROI 이미지 = Shot baseline   ← L403 (Plan 02 교체 대상)
    string pathB = dualMeas.TeachingImagePath_Vertical; //260530 hbk Phase 39.2 D-G1-06 — LineROI 이미지       ← L404 (변경 0)

    if (string.IsNullOrEmpty(pathA) || !File.Exists(pathA)) { // L406 — fallback 결과도 통과하는 가드
        Logging.PrintErrLog((int)ELogType.Error, "[FAI DualImage] PointROI 이미지 경로 비어 있거나 파일 없음 (SimulImagePath)");
        return false;
    }
    if (string.IsNullOrEmpty(pathB) || !File.Exists(pathB)) { // L410
        Logging.PrintErrLog((int)ELogType.Error, "[FAI DualImage] LineROI 이미지 경로 비어 있거나 파일 없음 (TeachingImagePath_Vertical)");
        return false;
    }
    try { imageA = new HImage(pathA); } catch (Exception ex) { ... } // L414
    try { imageB = new HImage(pathB); } catch (Exception ex) { ... } // L415
    if (imageA == null || imageB == null) {                          // L416 — Dispose 가드
        if (imageA != null) { try { imageA.Dispose(); } catch { } }
        if (imageB != null) { try { imageB.Dispose(); } catch { } }
        imageA = null; imageB = null;
        return false;
    }
    return true;
}
```

DualImageEdgeDistanceMeasurement.TeachingImagePath_Horizontal (Plan 01 산출물):
- public string TeachingImagePath_Horizontal { get; set; } = "";
- namespace ReringProject.Sequence
- Action_FAIMeasurement.cs 의 기존 using 으로 해결 (Plan 01 = `DualImageEdgeDistanceMeasurement` 타입을 이미 L398 에서 캐스트 중)

브레이스 스타일 (CLAUDE.md + 파일 현황):
- Action_FAIMeasurement.cs = K&R (`if (...) {` 같은 줄, `}` 다음 줄)
- 신규 if/else 도 동일 K&R 유지
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 02-01: Action_FAIMeasurement.cs — TryGrabOrLoadFaiDualImages L403 pathA 분기 교체 (D-G1 fallback if/else)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (L386-425 — TryGrabOrLoadFaiDualImages 전체 본문)
    - .planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-CONTEXT.md (D-G1 fallback 정책 + Anti-Goal #1/#5)
    - .planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-RESEARCH.md (RT-3.2 파일 2 line 매핑 + Risk R3 ternary 회피 if/else 권장)
    - .planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-01-PLAN.md (Plan 01 산출물 — TeachingImagePath_Horizontal property 존재 확인)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs (Plan 01 적용 후 L30 직후 신규 Horizontal 필드)
  </read_first>
  <action>
    //260530 hbk Phase 39.4 D-G1 주석 의무. RESEARCH R3 fallback (ternary 회피 → 명시적 if/else).

    **위치 (RT-3.2):** L403 `string pathA = ShotParam.SimulImagePath;` 한 줄을 명시적 if/else 7줄 블록으로 교체. L404 pathB 라인 + L394-L402 진입부 + L406-L422 후속 가드 모두 **변경 0**.

    Before (L403, 1줄):
    ```csharp
                string pathA = ShotParam.SimulImagePath; //260530 hbk Phase 39.2 D-G1-06 — PointROI 이미지 = Shot baseline
    ```

    After (L403 자리에 7줄 블록):
    ```csharp
                //260530 hbk Phase 39.4 D-G1 — PointROI 이미지 = Measurement 명시 경로 우선, 빈/파일 부재 시 ShotConfig.SimulImagePath fallback (Phase 39.2 baseline 회귀 0). ternary 회피 (RESEARCH R3 — 명시적 if/else 로 회귀 표면 최소화).
                string pathA;
                if (!string.IsNullOrEmpty(dualMeas.TeachingImagePath_Horizontal) && File.Exists(dualMeas.TeachingImagePath_Horizontal)) {
                    pathA = dualMeas.TeachingImagePath_Horizontal; //260530 hbk Phase 39.4 D-G1 — Measurement 명시 경로
                }
                else {
                    pathA = ShotParam.SimulImagePath; //260530 hbk Phase 39.4 D-G1 — fallback (Phase 39.2 baseline)
                }
    ```

    **회귀 가드 (Anti-Goal):**
    - L391 함수 시그니처 변경 0 (`TryGrabOrLoadFaiDualImages(MeasurementBase meas, out HImage imageA, out HImage imageB)` 그대로)
    - L392-L402 진입부 (imageA/imageB null 초기화 + ShotParam null guard + dualMeas 캐스트 + null guard) 변경 0
    - L404 `string pathB = dualMeas.TeachingImagePath_Vertical;` 변경 0 — Vertical 은 항상 Measurement 단위 경로 (Anti-Goal #5 Phase 39.2/39.3 baseline 보존)
    - L406-L412 pathA/pathB 가드 (IsNullOrEmpty || !File.Exists → 로그 + return false) 변경 0 — fallback 결과 (pathA = ShotParam.SimulImagePath) 도 같은 L406 가드를 통과해야 함 (RESEARCH R3: SimulImagePath 자체가 빈/부재면 기존과 동일하게 L406 가드에서 거름)
    - L414-L422 HImage 로드 + Dispose 가드 + return 분기 변경 0
    - using 블록 변경 0 — `string.IsNullOrEmpty` (mscorlib) / `File.Exists` (System.IO) / `dualMeas.TeachingImagePath_Horizontal` (이미 캐스트된 DualImageEdgeDistanceMeasurement) 모두 기존 import 으로 해결
    - 변수명 `pathA` 그대로 (재선언 X) — 기존 `string pathA = ...` 한 줄을 `string pathA;` + if/else 블록으로 분리 (C# 7.2 호환, 같은 scope 재선언 X)
    - 들여쓰기: TryGrabOrLoadFaiDualImages 본체 4 space 단위 (12 space — 메서드 안 if 블록 깊이)

    **C# 7.2 호환 + K&R 브레이스:**
    - `if (...) {` 같은 줄 + `}` 다음 줄 + `else {` 별도 줄
    - C# 7.2 ternary OK (RESEARCH R3 fallback) 이지만 본 Plan 은 명시적 if/else 채택 — 회귀 표면 최소화 + 가독성

    **dualMeas 변수 가용성:** L398 에서 이미 `var dualMeas = meas as DualImageEdgeDistanceMeasurement;` 캐스트 + L399 null guard 통과 후 → L403 시점에 `dualMeas != null` 보장됨. 신규 if 의 `dualMeas.TeachingImagePath_Horizontal` 접근 safe.
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 — 0 new errors / 0 new warnings (Phase 39.3 baseline 대비)</automated>
    grep 검증:
    - `grep -n "dualMeas.TeachingImagePath_Horizontal" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` → 2 라인 hit (if 조건 + pathA 할당)
    - `grep -n "dualMeas.TeachingImagePath_Vertical" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` → 1 라인 hit (L404 보존)
    - `grep -n "string pathA;" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` → 1 라인 hit (declaration only)
    - `grep -cn "Phase 39.4 D-G1" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` → 4+ 라인 hit (블록 주석 1 + if 분기 + else 분기 + 본 task 주석)
    - `grep -n "string pathA = ShotParam.SimulImagePath" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` → 0 hit (기존 L403 한 줄 사라짐)
    - `grep -n "string pathB = dualMeas.TeachingImagePath_Vertical" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` → 1 라인 hit (L404 보존)
    - `git diff WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` → 변경량 = L403 부근 만 (1줄 삭제 + 7줄 추가 = net +6 라인), 그 외 변경 0
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 PASS, 0 new errors, 0 new warnings
    - L403 자리에 if/else 블록 정확히 7줄 추가 (주석 1 + `string pathA;` 1 + if 조건 1 + then-branch 2 + else 2)
    - L404 `string pathB = dualMeas.TeachingImagePath_Vertical;` byte-identical 보존
    - L391 함수 시그니처 + L392-L402 진입부 + L406-L422 후속 본체 변경 0
    - if 조건 = `!string.IsNullOrEmpty(dualMeas.TeachingImagePath_Horizontal) && File.Exists(dualMeas.TeachingImagePath_Horizontal)` (NOT prefix + AND — RESEARCH R3 패턴)
    - then-branch: `pathA = dualMeas.TeachingImagePath_Horizontal;`
    - else-branch: `pathA = ShotParam.SimulImagePath;` (Phase 39.2 baseline 와 동일 값)
    - using 추가 0
    - K&R 브레이스 + C# 7.2 호환
  </acceptance_criteria>
  <done>
    `TryGrabOrLoadFaiDualImages` 의 pathA 가 D-G1 fallback 정책에 따라 결정. Measurement 가 `TeachingImagePath_Horizontal` 을 명시하고 파일이 존재하면 그 경로를 RuntimeImageA 로, 아니면 기존 `ShotParam.SimulImagePath` 로 fallback. 기존 INI 레시피 (Horizontal 부재) byte-identical 회귀 0. Plan 03 (MainView swap 가로축 분기) 와 검사 시점이 동일 우선순위 적용.
  </done>
</task>

</tasks>

<verification>
**Plan 02 전체 검증 (Anti-Goal 가드):**

1. **빌드 PASS 검증**:
   - `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64` → 0 new errors, 0 new warnings (Phase 39.3 baseline 대비)

2. **분기 교체 정확성** (grep + diff):
   - `dualMeas.TeachingImagePath_Horizontal` = 2 라인 hit (if 조건 + then-branch)
   - `dualMeas.TeachingImagePath_Vertical` = 1 라인 hit (L404 보존)
   - `string pathA;` (declaration only) = 1 라인 hit
   - 기존 `string pathA = ShotParam.SimulImagePath;` 한 줄 = 0 hit (교체됨)
   - `Phase 39.4 D-G1` 주석 마커 = 4+ hit (block 주석 1 + if/then/else 3)
   - `git diff` 변경 범위 = L403 부근만 (정확히 1줄 삭제 + 7줄 추가, net +6)

3. **Anti-Goal 가드 (코드 인스펙션)**:
   - `TryGrabOrLoadFaiDualImages` 함수 시그니처 변경 0 (Anti-Goal — 함수 명/파라미터 보존)
   - L404 pathB 라인 변경 0 (Vertical = Measurement 단위 경로 baseline 보존)
   - L406-L422 후속 가드 + HImage 로드 + Dispose 가드 변경 0 (Anti-Goal — 이미지 로딩 로직 보존)
   - 다른 Action_*.cs 변경 0 (Anti-Goal #4 — 다른 7 Measurement)
   - `TryExecute` (DualImageEdgeDistanceMeasurement) 변경 0 (Anti-Goal #1)
   - MeasurementFactory.cs 변경 0 (Anti-Goal #2)
   - DatumConfig.cs 변경 0 (Anti-Goal #3 — D-G2)
   - using 블록 변경 0

4. **회귀 0 의미적 검증**:
   - 기존 INI 레시피 (Horizontal 부재, default `""`) Load → if 조건 false → else → pathA = ShotParam.SimulImagePath → Phase 39.2 baseline 와 byte-identical pathA
   - Horizontal 명시 + 파일 존재 → if 조건 true → pathA = TeachingImagePath_Horizontal → 새 동작 (D-G1)
   - Horizontal 명시 + 파일 부재 → if 조건 false → else → fallback (D-G1)
   - 모든 경로의 pathA 값이 L406 가드 (`IsNullOrEmpty || !File.Exists`) 통과 가능
</verification>

<success_criteria>
- msbuild Debug/x64 PASS (0 new errors, 0 new warnings)
- pathA 분기가 명시적 if/else 로 D-G1 fallback 정책 구현
- L404 pathB + L391 시그니처 + L392-L402 진입부 + L406-L422 후속 본체 변경 0
- TeachingImagePath_Horizontal 부재 시 baseline byte-identical (회귀 C 보장)
- using 추가 0 + K&R 브레이스 + C# 7.2 호환
- Plan 03 와 파일 충돌 0 (Wave 2 병렬 가능)
- Plan 04 회귀 C (Phase 39.2-01 baseline 측정값 byte-identical) 의 기반 동작 확정
</success_criteria>

<output>
After completion, create `.planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-02-SUMMARY.md` with:
- Task 02-01 changes (file:line + 변경량 = -1 줄 / +7 줄 = net +6)
- Anti-Goal 가드 검증 결과 (위 verification 4개 항)
- msbuild Debug/x64 결과 (errors/warnings 카운트)
- L404 pathB + L391 시그니처 + L406-L422 후속 본체 변경 0 (byte-correct diff 명시)
- Plan 03 와의 동작 일관성 (검사 시점 = swap UX 시점 동일 우선순위)
</output>
</content>
