---
phase: 39.4-bottom-dualimage-manual-swap-2026-05-30
plan: "02"
subsystem: sequence-fai-dualimage
tags: [sequence, fai, dualimage, fallback, runtime, d-g1]
dependency_graph:
  requires:
    - Plan 01 (DualImageEdgeDistanceMeasurement.TeachingImagePath_Horizontal 필드)
  provides:
    - Action_FAIMeasurement.TryGrabOrLoadFaiDualImages pathA D-G1 fallback if/else
  affects:
    - Plan 03 (MainView.BtnSwapHorizontal_Click — 검사 시점과 swap UX 동일 우선순위)
    - Plan 04 (UAT Test 2 — pathA 분기 시뮬 동작 검증)
tech_stack:
  added: []
  patterns:
    - D-G1 fallback 정책: Measurement 명시 경로 우선 → ShotConfig.SimulImagePath fallback
    - RESEARCH R3: ternary 회피 → 명시적 if/else (회귀 표면 최소화)
    - K&R 브레이스 (파일 기존 스타일 유지)
key_files:
  created: []
  modified:
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
decisions:
  - D-G1 fallback 정책: TeachingImagePath_Horizontal 명시+존재 → 명시 경로, 아니면 ShotParam.SimulImagePath (Phase 39.2 baseline 회귀 0)
  - ternary 회피 채택 (RESEARCH R3) — 명시적 if/else 7줄, 회귀 표면 최소화
metrics:
  duration_sec: 360
  completed_date: "2026-05-30"
  tasks_completed: 1
  tasks_total: 1
  files_changed: 1
---

# Phase 39.4 Plan 02: Action_FAIMeasurement pathA D-G1 fallback if/else Summary

`Action_FAIMeasurement.TryGrabOrLoadFaiDualImages` L403 `string pathA = ShotParam.SimulImagePath;` (Phase 39.2 baseline 1줄) 를 D-G1 fallback 정책 명시적 if/else 7줄 블록으로 교체 — `dualMeas.TeachingImagePath_Horizontal` 이 명시되고 파일이 존재하면 그 경로를, 아니면 기존 `ShotParam.SimulImagePath` fallback.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 02-01 | Action_FAIMeasurement.cs — TryGrabOrLoadFaiDualImages L403 pathA 분기 교체 (D-G1 fallback if/else) | 484819e | Action_FAIMeasurement.cs (-1줄 / +7줄 = net +6) |

## Changes Made

### Task 02-01: Action_FAIMeasurement.cs

**파일:** `WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs`

**변경 요약 (-1줄 / +7줄 = net +6, 변경 L403 부근만):**

**Before (L403, 1줄):**
```csharp
            string pathA = ShotParam.SimulImagePath; //260530 hbk Phase 39.2 D-G1-06 — PointROI 이미지 = Shot baseline
```

**After (L403 자리 → 7줄 블록):**
```csharp
            //260530 hbk Phase 39.4 D-G1 — PointROI 이미지 = Measurement 명시 경로 우선, 빈/파일 부재 시 ShotConfig.SimulImagePath fallback (Phase 39.2 baseline 회귀 0). ternary 회피 (RESEARCH R3 — 명시적 if/else 로 회귀 표면 최소화).
            string pathA;
            if (!string.IsNullOrEmpty(dualMeas.TeachingImagePath_Horizontal) && File.Exists(dualMeas.TeachingImagePath_Horizontal)) { //260530 hbk Phase 39.4 D-G1
                pathA = dualMeas.TeachingImagePath_Horizontal; //260530 hbk Phase 39.4 D-G1 — Measurement 명시 경로
            }
            else {
                pathA = ShotParam.SimulImagePath; //260530 hbk Phase 39.4 D-G1 — fallback (Phase 39.2 baseline)
            }
```

## Verification Results

### 1. msbuild Debug/x64 빌드 검증

**결과: PASS**
- 빌드 출력: `DatumMeasurement -> ...\bin\x64\Debug\DatumMeasurement.exe`
- **신규 오류:** 0
- **신규 경고:** 0
- 기존 경고 8개 (CS0618 × 5, CS0162 × 1, MSB3884 × 2) = Plan 01 baseline 동일, 신규 0

### 2. grep 검증 결과 (7가지 모두 PASS)

| 검증 항목 | 결과 |
|-----------|------|
| `dualMeas.TeachingImagePath_Horizontal` = 2 라인 hit (if 조건 L405 + then-branch L406) | PASS |
| `dualMeas.TeachingImagePath_Vertical` = 1 라인 hit (L411 보존) | PASS |
| `string pathA;` (declaration only) = 1 라인 hit (L404) | PASS |
| `Phase 39.4 D-G1` 주석 마커 = 4 라인 hit (블록 주석 + if 조건 + then-branch + else-branch) | PASS |
| `string pathA = ShotParam.SimulImagePath` = 0 hit (기존 1줄 사라짐) | PASS |
| `string pathB = dualMeas.TeachingImagePath_Vertical` = 1 라인 hit (L411 byte-identical 보존) | PASS |
| `git diff` 변경 범위 = L403 부근만 (정확히 1줄 삭제 + 7줄 추가 = net +6) | PASS |

### 3. Anti-Goal 가드 검증

| Anti-Goal | 결과 |
|-----------|------|
| TryGrabOrLoadFaiDualImages 시그니처 변경 0 (L391) | PASS — diff 확인 |
| L392-L402 진입부 (imageA/imageB null 초기화 + ShotParam null guard + dualMeas 캐스트 + null guard) 변경 0 | PASS — diff 확인 |
| L404 `string pathB = dualMeas.TeachingImagePath_Vertical` 변경 0 (Vertical = Measurement 단위 경로 baseline) | PASS — byte-identical 보존 |
| L406-L422 후속 가드 + HImage 로드 + Dispose 가드 + return 분기 변경 0 | PASS — diff 확인 |
| using 블록 변경 0 (`string.IsNullOrEmpty` = mscorlib, `File.Exists` = System.IO, `dualMeas.TeachingImagePath_Horizontal` = 기존 캐스트 후 참조) | PASS |
| 다른 Action_*.cs 변경 0 | PASS — Action_FAIMeasurement.cs 단일 파일만 변경 |
| TryExecute (DualImageEdgeDistanceMeasurement) 변경 0 | PASS |
| MeasurementFactory.cs 변경 0 | PASS |
| DatumConfig.cs 변경 0 | PASS |

### 4. 회귀 0 의미적 검증 (3 경로)

| 경로 | if 조건 | pathA 값 | L406 가드 통과 |
|------|---------|---------|----------------|
| 기존 INI 레시피 (Horizontal 미설정 = default `""`) | `IsNullOrEmpty("")` = true → false | `ShotParam.SimulImagePath` | Phase 39.2 baseline 동일 ✅ |
| Horizontal 명시 + 파일 존재 | false → true | `TeachingImagePath_Horizontal` | 명시 경로 파일 통과 ✅ |
| Horizontal 명시 + 파일 부재 | `File.Exists(...)` = false → false | `ShotParam.SimulImagePath` | fallback → Phase 39.2 baseline ✅ |

## Plan 03 와의 동작 일관성

- **검사 시점 (Plan 02):** `TryGrabOrLoadFaiDualImages` → `TeachingImagePath_Horizontal` 명시+존재 → 명시 경로 / 아니면 fallback
- **swap UX 시점 (Plan 03):** `BtnSwapHorizontal_Click` → Measurement 분기 → 동일 `TeachingImagePath_Horizontal` 참조 예정
- **우선순위 일치:** Measurement 명시 > ShotConfig fallback — 검사 실행 시 이미지 소스와 UI swap 배지가 동일 결정 로직 적용

## Deviations from Plan

없음 — PLAN.md 명세 정확히 실행.

## Known Stubs

없음 — `pathA` 분기 완전히 배선. Plan 04 UAT Test 2 에서 실제 SIMUL 동작 검증 예정.

## Threat Flags

없음 — 파일 경로 읽기(File.Exists)는 기존 L406 패턴과 동일. 신규 네트워크/스키마/신규 신뢰 경계 없음.

## Self-Check: PASSED

- [x] `WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 존재 확인
- [x] commit `484819e` 존재 확인 (`git rev-parse --short HEAD`)
- [x] msbuild Debug/x64 PASS (0 new errors, 0 new warnings)
- [x] grep 7가지 PASS
- [x] Anti-Goal git diff PASS — L403 부근 1줄 삭제 + 7줄 추가만, 나머지 변경 0
