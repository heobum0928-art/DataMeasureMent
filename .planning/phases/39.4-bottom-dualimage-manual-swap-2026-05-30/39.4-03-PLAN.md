---
phase: 39.4-bottom-dualimage-manual-swap-2026-05-30
plan: 03
type: execute
wave: 2
depends_on: ["39.4-01"]
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: true
requirements: [WF-01]
tags: [ui, wpf, mainview, dualimage, swap, badge, fallback]

must_haves:
  truths:
    - "BtnSwapHorizontal_Click Measurement 분기 (L1347-1365) 가 TeachingImagePath_Horizontal 우선 + ShotConfig fallback 으로 이미지 로드"
    - "TeachingImagePath_Horizontal 명시 + 파일 존재 → halconViewer.LoadImage(hpathMeas)"
    - "TeachingImagePath_Horizontal 빈/부재 → 기존 Phase 39.3 ShotConfig.GetImage() 경로 사용 (fallback)"
    - "UpdateImageSourceBadge Horizontal 분기 (L1300-1310) 가 Measurement 가 선택된 경우 fallback 여부에 따라 '가로축 (Measurement)' vs '가로축 (Shot fallback)' 동적 텍스트 set"
    - "_selectedDualImageMeasurement == null (Datum 또는 비-DualImage) → 기존 '가로축' / '세로축' 텍스트 보존 (mutex 가드 — Phase 34.1/36/37 회귀 0)"
    - "Vertical 분기 (L1382-1395 BtnSwapVertical_Click + UpdateImageSourceBadge Vertical) 변경 0 — Vertical 은 항상 Measurement 단위 경로 (D-G4 텍스트 = 그대로 '세로축')"
    - "Datum 측 코드 (BtnSwap*_Click Datum 경로 L1366-1377 / PublishDatumRoiCandidates Datum 측 로직 / OnSelectedDatumPropertyChanged) 변경 0 (Anti-Goal #3)"
    - "TextBlock.SetCurrentValue(TextProperty) + Border.InvalidateVisual 패턴 보존 (CO-34.1-03 hotfix)"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "BtnSwapHorizontal_Click Measurement 분기 D-G1 fallback + UpdateImageSourceBadge Horizontal 텍스트 D-G4 동적 분기"
      contains: "meas.TeachingImagePath_Horizontal"
  key_links:
    - from: "MainView.BtnSwapHorizontal_Click Measurement 분기"
      to: "halconViewer.LoadImage(hpathMeas) (Measurement) 또는 shot.GetImage() (fallback)"
      via: "string hpathMeas + IsNullOrEmpty + File.Exists 가드"
      pattern: "hpathMeas|TeachingImagePath_Horizontal"
    - from: "MainView.UpdateImageSourceBadge L1300-1310 Horizontal 분기"
      to: "txt_imageSourceBadge.SetCurrentValue(TextProperty, ...)"
      via: "_selectedDualImageMeasurement + File.Exists 분기"
      pattern: "가로축 \\(Measurement\\)|가로축 \\(Shot fallback\\)"
    - from: "_selectedDualImageMeasurement 필드 (Phase 39.3 L69-71)"
      to: "Plan 03 의 fallback 분기 진입 가드"
      via: "PublishMeasurementDualImageSelection 가 set / PublishDatumRoiCandidates 가 clear (mutex)"
      pattern: "_selectedDualImageMeasurement"
---

<objective>
**Plan 03 — Wave 2 병렬 (Plan 01 의존, Plan 02 와 파일 충돌 0): MainView swap UX + 배지 텍스트 (D-G1 + D-G4)**

`MainView.xaml.cs` 의 두 지점을 변경:

1. **BtnSwapHorizontal_Click Measurement 분기 (L1347-1365)** — Phase 39.3 의 ShotConfig 직접 로드 단일 경로를 **D-G1 fallback 분기** 로 교체. `meas.TeachingImagePath_Horizontal` 명시 + 파일 존재 → `halconViewer.LoadImage(hpathMeas)`, 아니면 기존 ShotConfig 경로 fallback.
2. **UpdateImageSourceBadge Horizontal 분기 (L1300-1310)** — RT-4 옵션 A 채택 (single source of truth). `_selectedDualImageMeasurement` 가 set 일 때 fallback 여부에 따라 배지 텍스트를 `"가로축 (Measurement)"` vs `"가로축 (Shot fallback)"` 으로 동적 set. Datum 또는 null 분기 → 기존 `"가로축"` 보존 (mutex 가드).

Purpose: D-G4 (사용자 즉시 인지 — 추가 클릭 0) + D-G1 fallback 정책의 UI 가시화. Plan 02 (검사 시점 fallback) 와 동일 우선순위로 swap UX 시점 통일. Datum DualImage (Phase 34.1/36/37) 측은 mutex 가드로 회귀 0.
Output: 사용자가 가로축 swap 토글 클릭 시 (a) Measurement 명시 → 가로축 (Measurement) 배지 + 명시 이미지 로드, (b) Measurement 미명시 → 가로축 (Shot fallback) 배지 + ShotConfig 이미지 로드, (c) Datum 선택 → 기존 '가로축' 배지 + Datum 이미지 (Phase 34.1 baseline).

**Wave 2 단독 — Plan 01 의 TeachingImagePath_Horizontal 컴파일 의존.** Plan 02 와 파일 충돌 0 (Plan 02 = Action_FAIMeasurement.cs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-CONTEXT.md
@.planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-RESEARCH.md
@.planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-01-PLAN.md
@.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-PATTERNS.md
@.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-02-PLAN.md
@WPF_Example/UI/ContentItem/MainView.xaml.cs
@WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs
@WPF_Example/Custom/Sequence/Inspection/EImageSource.cs

<interfaces>
<!-- Key types and contracts (RT-3.1 line 매핑 검증) -->

**파일: WPF_Example/UI/ContentItem/MainView.xaml.cs**

기존 필드 (변경 0):
- L62-64: private ReringProject.Sequence.EImageSource _currentImageSource = ReringProject.Sequence.EImageSource.Horizontal;
- L69-71: private DualImageEdgeDistanceMeasurement _selectedDualImageMeasurement; (Phase 39.3 Plan 02 commit 2e28a76)
- L72-80: BadgeBrushHorizontal / BadgeBrushVertical (frozen brush, CO-34.1-03)

**변경 대상 1 — UpdateImageSourceBadge (L1295-1329):**

현재 (검증된 L1300-1310):
```csharp
if (border_imageSourceBadge != null && txt_imageSourceBadge != null) {
    if (source == ReringProject.Sequence.EImageSource.Horizontal) {
        txt_imageSourceBadge.SetCurrentValue(TextBlock.TextProperty, "가로축"); //260527 hbk Phase 34.1 D-34.1-14   ← L1302 (D-G4 동적 분기 추가 대상)
        border_imageSourceBadge.SetCurrentValue(Border.BackgroundProperty, BadgeBrushHorizontal);
    }
    else {
        txt_imageSourceBadge.SetCurrentValue(TextBlock.TextProperty, "세로축"); //260527 hbk Phase 34.1 D-34.1-14   ← L1306 (변경 0 — Vertical 은 항상 Measurement 경로)
        border_imageSourceBadge.SetCurrentValue(Border.BackgroundProperty, BadgeBrushVertical);
    }
    border_imageSourceBadge.InvalidateVisual();
}
```

**변경 대상 2 — BtnSwapHorizontal_Click Measurement 분기 (L1346-1377):**

현재 (검증된 L1347-1365, Phase 39.3 Plan 02 Task 02-04 commit a8facf9):
```csharp
private void BtnSwapHorizontal_Click(object sender, RoutedEventArgs e) {
    //260530 hbk Phase 39.3 D-G2 — Measurement DualImage 가 Datum 보다 우선 (mutex)
    {
        var meas = _selectedDualImageMeasurement; //260530 hbk Phase 39.3 D-G2
        if (meas != null) {
            FAIConfig fai = FindFAIContainingMeasurement(meas);                        // L1351
            ShotConfig shot = fai != null ? fai.Owner as ShotConfig : null;            // L1352
            if (shot != null && shot.HasImage) {                                        // L1353 (변경 대상: 이 분기를 D-G1 fallback 분기로 교체)
                HImage img = null;
                try {
                    img = shot.GetImage();
                    if (img != null) halconViewer.LoadImage(img);
                }
                catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, ex.Message); }
                finally { if (img != null) img.Dispose(); }
            }
            UpdateImageSourceBadge(ReringProject.Sequence.EImageSource.Horizontal);   // L1362
            return;                                                                    // L1363 — Datum 코드 진입 X
        }
    }
    var d = _selectedDatumForSwap;                                                     // L1366 (변경 0 — Datum 경로)
    ...
    halconViewer.SetDatumOverlay(...);                                                 // L1376 (변경 0)
}
```

**변경 안 함 (Anti-Goal #3 D-G2 — Datum 측):**
- L1366-1377 Datum 경로 (`var d = _selectedDatumForSwap;` 이후)
- L1381-1407 BtnSwapVertical_Click 전체 (Vertical 은 항상 Measurement 경로 → fallback 불필요)
- L1316-1322 UpdateImageSourceBadge Datum publish 가드
- L1323-1328 UpdateImageSourceBadge Measurement publish 가드 (`RenderInspectionResultForNode` 호출)
- L1411-1418 PublishDatumRoiCandidates mutex clear
- L1496-1518 PublishMeasurementDualImageSelection 메서드

DualImageEdgeDistanceMeasurement.TeachingImagePath_Horizontal (Plan 01 산출물):
- public string TeachingImagePath_Horizontal { get; set; } = ""; (namespace ReringProject.Sequence)
- MainView.xaml.cs 기존 using 으로 해결 (Phase 39.3 Plan 02 이미 _selectedDualImageMeasurement 의 타입으로 사용 중)

브레이스 스타일 (CLAUDE.md + 파일 현황):
- MainView.xaml.cs = K&R (`if (...) {` 같은 줄, Phase 39.3 Plan 02 동일 스타일)

EImageSource enum (WPF_Example/Custom/Sequence/Inspection/EImageSource.cs):
- namespace ReringProject.Sequence
- enum EImageSource { Horizontal = 0, Vertical = 1 }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 03-01: MainView.xaml.cs — BtnSwapHorizontal_Click Measurement 분기 D-G1 fallback 교체</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (L1346-1377 BtnSwapHorizontal_Click 전체 + L1381-1407 BtnSwapVertical_Click 참조)
    - .planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-CONTEXT.md (D-G1 fallback 정책 + Anti-Goal #3/#9)
    - .planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-RESEARCH.md (RT-3 L1347-1365 매핑 + R7 CS0136 회피 `hpathMeas` 명명)
    - .planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-01-PLAN.md (TeachingImagePath_Horizontal property 존재 확인)
    - .planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-02-PLAN.md (Task 02-04 vpathMeas rename 패턴 참조)
  </read_first>
  <action>
    //260530 hbk Phase 39.4 D-G1 주석 의무. RESEARCH RT-3.2 적용 + R7 CS0136 회피 (`hpathMeas` 명명).

    **위치:** BtnSwapHorizontal_Click 안의 Measurement 분기 (L1350-1364). `if (meas != null) { ... }` 블록 안의 ShotConfig 직접 로드 (L1353-1361) 를 D-G1 fallback 분기로 교체. `UpdateImageSourceBadge(...Horizontal)` 호출 (L1362) + `return` (L1363) 는 변경 0.

    Before (L1350-1364):
    ```csharp
            if (meas != null) {
                FAIConfig fai = FindFAIContainingMeasurement(meas); //260530 hbk Phase 39.3 D-G2 — 기존 helper (Phase 37 hotfix A/B)
                ShotConfig shot = fai != null ? fai.Owner as ShotConfig : null; //260530 hbk Phase 39.3 D-G2
                if (shot != null && shot.HasImage) { //260530 hbk Phase 39.3 D-G2
                    HImage img = null;
                    try {
                        img = shot.GetImage();
                        if (img != null) halconViewer.LoadImage(img); //260530 hbk Phase 39.3 D-G2
                    }
                    catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, ex.Message); }
                    finally { if (img != null) img.Dispose(); }
                }
                UpdateImageSourceBadge(ReringProject.Sequence.EImageSource.Horizontal); //260530 hbk Phase 39.3 D-G2
                return; //260530 hbk Phase 39.3 D-G2 — Measurement 경로 종결, Datum 코드 진입 X
            }
    ```

    After (L1350-1372 부근, D-G1 fallback 분기 추가):
    ```csharp
            if (meas != null) {
                //260530 hbk Phase 39.4 D-G1 — Measurement 명시 경로 우선, 없으면 ShotConfig fallback. CS0136 회피 위해 `hpathMeas` 명명 (Phase 39.3 Task 02-04 vpathMeas 패턴 mirror).
                string hpathMeas = meas.TeachingImagePath_Horizontal; //260530 hbk Phase 39.4 D-G1
                if (!string.IsNullOrEmpty(hpathMeas) && System.IO.File.Exists(hpathMeas)) {
                    try { halconViewer.LoadImage(hpathMeas); } //260530 hbk Phase 39.4 D-G1 — Measurement 명시 경로
                    catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, ex.Message); }
                }
                else {
                    //260530 hbk Phase 39.4 D-G1 — fallback: ShotConfig 이미지 (Phase 39.3 baseline)
                    FAIConfig fai = FindFAIContainingMeasurement(meas); //260530 hbk Phase 39.3 D-G2 — 기존 helper (Phase 37 hotfix A/B)
                    ShotConfig shot = fai != null ? fai.Owner as ShotConfig : null; //260530 hbk Phase 39.3 D-G2
                    if (shot != null && shot.HasImage) { //260530 hbk Phase 39.3 D-G2
                        HImage img = null;
                        try {
                            img = shot.GetImage();
                            if (img != null) halconViewer.LoadImage(img); //260530 hbk Phase 39.3 D-G2
                        }
                        catch (Exception ex) { Logging.PrintErrLog((int)ELogType.Error, ex.Message); }
                        finally { if (img != null) img.Dispose(); }
                    }
                }
                UpdateImageSourceBadge(ReringProject.Sequence.EImageSource.Horizontal); //260530 hbk Phase 39.3 D-G2
                return; //260530 hbk Phase 39.3 D-G2 — Measurement 경로 종결, Datum 코드 진입 X
            }
    ```

    **회귀 가드 (Anti-Goal):**
    - L1346 함수 시그니처 `private void BtnSwapHorizontal_Click(object sender, RoutedEventArgs e)` 변경 0
    - L1347-1349 outer 블록 + `var meas = _selectedDualImageMeasurement;` 변경 0
    - L1362 `UpdateImageSourceBadge(...Horizontal);` 호출 변경 0 — Task 03-02 에서 호출되는 메서드 본체를 갱신
    - L1363 `return;` 변경 0 — Datum 코드 진입 가드 유지
    - L1366-1377 Datum 경로 (`var d = _selectedDatumForSwap;` 이후) 변경 0 (Anti-Goal #3 D-G2)
    - 변수명 `hpathMeas` 사용 (CS0136 회피, RT R7) — Datum 분기의 `hpath` (L1369) 와 outer scope 충돌 회피
    - `FAIConfig` + `ShotConfig` + `HImage` 등 fallback 분기 안에서만 declare (then-branch 안에선 미선언 → 사용하지 않음)
    - using 추가 0 — `System.IO.File`, `FAIConfig`, `ShotConfig`, `HImage` 모두 기존 MainView.xaml.cs import 으로 해결 (Phase 39.3 Plan 02 검증)
    - K&R 브레이스 + C# 7.2 호환

    **변수 scope 검증:**
    - `hpathMeas` = if (meas != null) 블록 안에서 declare → outer scope (Datum 분기) 에서 보이지 않음
    - else 블록의 `fai` / `shot` / `img` = else 블록 안에서만 declare → if 분기와 충돌 X
    - 기존 `FAIConfig fai = ...` / `ShotConfig shot = ...` 가 if 분기 → else 분기로 이동
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 — 0 new errors / 0 new warnings (Phase 39.3 baseline 대비)</automated>
    grep 검증:
    - `grep -n "string hpathMeas = meas.TeachingImagePath_Horizontal" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 라인 hit
    - `grep -n "halconViewer.LoadImage(hpathMeas)" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 라인 hit
    - `grep -n "string hpath = d.TeachingImagePath" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 라인 hit (Datum 분기 L1369 보존)
    - `grep -cn "Phase 39.4 D-G1" WPF_Example/UI/ContentItem/MainView.xaml.cs` (Task 03-01 만) → 3+ 라인 hit (block 주석 + hpathMeas declare + LoadImage + else 주석)
    - `grep -n "var d = _selectedDatumForSwap" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 라인 hit (L1366 보존)
    - `grep -n "SetDatumOverlay(_selectedDatumForSwap" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 라인 hit (L1376 보존)
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 PASS, 0 new errors, 0 new warnings
    - `if (meas != null)` 블록 안에 `string hpathMeas = meas.TeachingImagePath_Horizontal;` declare 1 라인
    - then-branch: `halconViewer.LoadImage(hpathMeas);` + try/catch
    - else-branch: 기존 ShotConfig 직접 로드 코드 (`FAIConfig fai` / `ShotConfig shot` / `HImage img`) 그대로 이동
    - `UpdateImageSourceBadge(...Horizontal);` + `return;` 양쪽 분기 후 공통 실행 위치 보존
    - Datum 경로 (L1366-1377) 변경 0
    - CS0136 회피 (`hpathMeas` 명명) — `hpath` (Datum 분기) 와 다른 변수명
    - using 추가 0
  </acceptance_criteria>
  <done>
    BtnSwapHorizontal_Click Measurement 분기가 Plan 02 와 동일한 D-G1 fallback 우선순위 (Measurement 명시 > ShotConfig fallback) 로 이미지 로드. Datum 경로 (mutex 가드 via return) 변경 0. Task 03-02 에서 배지 텍스트 분기 추가 후 동일 swap 토글 클릭에서 이미지 + 배지가 동시 갱신됨.
  </done>
</task>

<task type="auto">
  <name>Task 03-02: MainView.xaml.cs — UpdateImageSourceBadge Horizontal 분기 D-G4 동적 텍스트 (RT-4 옵션 A)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (L1295-1329 UpdateImageSourceBadge 전체)
    - .planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-CONTEXT.md (D-G4 배지 텍스트 정책 + Anti-Goal #9 XAML 변경 0)
    - .planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-RESEARCH.md (RT-4 옵션 A — UpdateImageSourceBadge 안 single source of truth + Risk R4 회피)
    - .planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-01-PLAN.md (TeachingImagePath_Horizontal property 존재)
  </read_first>
  <action>
    //260530 hbk Phase 39.4 D-G4 주석 의무. RESEARCH RT-4 옵션 A 적용 — UpdateImageSourceBadge 안에서 결정 (single source of truth + Datum mutex 가드).

    **위치:** UpdateImageSourceBadge (L1295-1329) 의 Horizontal 분기 (L1301-1304). 기존 고정 `"가로축"` 텍스트 set 1줄 (L1302) 을 동적 결정 블록으로 교체. 색상 set (L1303 `border_imageSourceBadge.SetCurrentValue(Border.BackgroundProperty, BadgeBrushHorizontal)`) + `InvalidateVisual()` (L1309) + Vertical 분기 (L1305-1308) + (c) ROI publish 가드 (L1316-1328) 변경 0.

    Before (L1300-1310):
    ```csharp
                // (b) 배지 텍스트 + 색상 — D-34.1-14 잠금값
                //260527 hbk Phase 34.1 CO-34.1-03 hotfix — 정적 frozen brush + SetCurrentValue 로 WPF 갱신 보장.
                if (border_imageSourceBadge != null && txt_imageSourceBadge != null) {
                    if (source == ReringProject.Sequence.EImageSource.Horizontal) {
                        txt_imageSourceBadge.SetCurrentValue(TextBlock.TextProperty, "가로축"); //260527 hbk Phase 34.1 D-34.1-14
                        border_imageSourceBadge.SetCurrentValue(Border.BackgroundProperty, BadgeBrushHorizontal); //260527 hbk Phase 34.1 CO-34.1-03
                    }
                    else {
                        txt_imageSourceBadge.SetCurrentValue(TextBlock.TextProperty, "세로축"); //260527 hbk Phase 34.1 D-34.1-14
                        border_imageSourceBadge.SetCurrentValue(Border.BackgroundProperty, BadgeBrushVertical); //260527 hbk Phase 34.1 CO-34.1-03
                    }
                    border_imageSourceBadge.InvalidateVisual(); //260527 hbk Phase 34.1 CO-34.1-03 — 즉시 재렌더 강제
                }
    ```

    After (L1300-1318 부근, Horizontal 분기 안에서 동적 텍스트 결정):
    ```csharp
                // (b) 배지 텍스트 + 색상 — D-34.1-14 잠금값 + Phase 39.4 D-G4 (Measurement 명시 vs fallback 명시)
                //260527 hbk Phase 34.1 CO-34.1-03 hotfix — 정적 frozen brush + SetCurrentValue 로 WPF 갱신 보장.
                if (border_imageSourceBadge != null && txt_imageSourceBadge != null) {
                    if (source == ReringProject.Sequence.EImageSource.Horizontal) {
                        //260530 hbk Phase 39.4 D-G4 — Measurement DualImage 가 선택된 상태에선 명시 경로 vs fallback 여부에 따라 배지 텍스트 보강. Datum 또는 null 분기 → 기존 "가로축" 보존 (mutex 가드).
                        string horizontalBadgeText = "가로축"; //260530 hbk Phase 39.4 D-G4 — 기본값 (Datum 또는 비-DualImage selection)
                        var measForBadge = _selectedDualImageMeasurement; //260530 hbk Phase 39.4 D-G4
                        if (measForBadge != null) {
                            bool hasExplicitHorizontal = !string.IsNullOrEmpty(measForBadge.TeachingImagePath_Horizontal)
                                && System.IO.File.Exists(measForBadge.TeachingImagePath_Horizontal); //260530 hbk Phase 39.4 D-G4
                            horizontalBadgeText = hasExplicitHorizontal ? "가로축 (Measurement)" : "가로축 (Shot fallback)"; //260530 hbk Phase 39.4 D-G4
                        }
                        txt_imageSourceBadge.SetCurrentValue(TextBlock.TextProperty, horizontalBadgeText); //260530 hbk Phase 39.4 D-G4 (Phase 34.1 D-34.1-14 텍스트 확장)
                        border_imageSourceBadge.SetCurrentValue(Border.BackgroundProperty, BadgeBrushHorizontal); //260527 hbk Phase 34.1 CO-34.1-03
                    }
                    else {
                        txt_imageSourceBadge.SetCurrentValue(TextBlock.TextProperty, "세로축"); //260527 hbk Phase 34.1 D-34.1-14
                        border_imageSourceBadge.SetCurrentValue(Border.BackgroundProperty, BadgeBrushVertical); //260527 hbk Phase 34.1 CO-34.1-03
                    }
                    border_imageSourceBadge.InvalidateVisual(); //260527 hbk Phase 34.1 CO-34.1-03 — 즉시 재렌더 강제
                }
    ```

    **회귀 가드 (Anti-Goal):**
    - L1295 함수 시그니처 + L1296 `_currentImageSource = source;` 변경 0
    - Vertical else 분기 (L1305-1308 in Before, 동일 위치 in After) 변경 0 — Vertical 은 항상 Measurement 단위 경로 (D-G4 후속 — 텍스트 그대로 `"세로축"` 유지)
    - L1303 `border_imageSourceBadge.SetCurrentValue(Border.BackgroundProperty, BadgeBrushHorizontal);` byte-identical 보존 (색상 = Phase 34.1 CO-34.1-03)
    - L1309 `border_imageSourceBadge.InvalidateVisual();` 보존 (텍스트 + 색상 양쪽 갱신 보장)
    - L1312-1314 토글 IsChecked 동기화 변경 0
    - L1316-1322 Datum publish 가드 변경 0 (Anti-Goal #3 D-G2)
    - L1323-1328 Measurement publish 가드 (`RenderInspectionResultForNode`) 변경 0 (Phase 39.3 Plan 02 baseline)
    - `_selectedDualImageMeasurement` 필드 (Phase 39.3 L69-71) 참조만, 변경 0
    - File.Exists I/O = swap 토글 시점만 발생 (LOW cost, RT-4 검증)
    - using 추가 0 (`System.IO.File`, `TextBlock`, `Border` 모두 기존 import)

    **Datum 측 회귀 0 검증:**
    - Datum DualImage selection → `_selectedDualImageMeasurement == null` (mutex via PublishDatumRoiCandidates clear, Phase 39.3 Plan 02 Task 02-03 ffd139c L1416-1418) → `measForBadge == null` → `horizontalBadgeText = "가로축"` (기본값) → Phase 34.1/36/37 UAT 회귀 0
    - 비-DualImage Measurement selection → 동일 흐름

    **변수명 `horizontalBadgeText` / `measForBadge` / `hasExplicitHorizontal`:**
    - 모두 if 블록 안에서만 declare → outer scope (Datum 분기 / Vertical 분기) 와 충돌 X
    - `meas` (BtnSwap*_Click 의 변수명) 와 다른 명명 → 동일 메서드 안 혼동 회피
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 — 0 new errors / 0 new warnings (Phase 39.3 baseline 대비)</automated>
    grep 검증:
    - `grep -n "가로축 (Measurement)" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 라인 hit
    - `grep -n "가로축 (Shot fallback)" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 라인 hit
    - `grep -n "horizontalBadgeText" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 3+ 라인 hit (declare + ternary + SetCurrentValue 인자)
    - `grep -n "measForBadge" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 3+ 라인 hit (declare + null check + TeachingImagePath_Horizontal 접근)
    - `grep -n "hasExplicitHorizontal" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 2 라인 hit (declare + ternary)
    - `grep -n "txt_imageSourceBadge.SetCurrentValue(TextBlock.TextProperty, \"가로축\")" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 0 hit (기존 고정 텍스트 사라짐)
    - `grep -n "txt_imageSourceBadge.SetCurrentValue(TextBlock.TextProperty, \"세로축\")" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 1 라인 hit (Vertical 분기 보존)
    - `grep -n "BadgeBrushHorizontal" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 변경 전과 동일 hit (색상 set 보존)
    - `grep -n "InvalidateVisual()" WPF_Example/UI/ContentItem/MainView.xaml.cs` → 변경 전과 동일 hit (즉시 재렌더 보존)
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 PASS, 0 new errors, 0 new warnings
    - Horizontal 분기 안에 `horizontalBadgeText` 변수 declare + `measForBadge` null 가드 + `hasExplicitHorizontal` ternary
    - 텍스트 리터럴 `"가로축 (Measurement)"` + `"가로축 (Shot fallback)"` + `"가로축"` (기본값) 3종 등장
    - `_selectedDualImageMeasurement == null` 경로 → `horizontalBadgeText = "가로축"` (Datum/null 회귀 0)
    - Vertical 분기 (else) 변경 0 — `"세로축"` 텍스트 그대로
    - 색상 set (`BadgeBrushHorizontal` / `BadgeBrushVertical`) + `InvalidateVisual()` 보존
    - (c) ROI publish 가드 (L1316-1328) 변경 0
    - File.Exists 가 swap 토글 시점만 발생 (LOW cost)
    - using 추가 0
    - XAML 파일 (MainView.xaml) 변경 0 — 텍스트만 코드 비하인드 동적 set
  </acceptance_criteria>
  <done>
    UpdateImageSourceBadge 가 Horizontal 분기에서 `_selectedDualImageMeasurement` 상태 + `TeachingImagePath_Horizontal` 존재 여부에 따라 배지 텍스트를 `"가로축 (Measurement)"` / `"가로축 (Shot fallback)"` / `"가로축"` 3가지 중 동적 결정. Datum 측은 mutex 가드 (measForBadge == null) 로 기존 `"가로축"` 보존. swap UX 토글 1회 클릭으로 이미지 + 배지 + 토글 IsChecked + ROI publish 4자 동시 갱신.
  </done>
</task>

</tasks>

<verification>
**Plan 03 전체 검증 (Anti-Goal 가드):**

1. **빌드 PASS 검증** (Task 03-01 + Task 03-02 누적):
   - `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64` → 0 new errors, 0 new warnings (Phase 39.3 baseline 대비)

2. **분기 추가 정확성** (grep):
   - MainView.xaml.cs 내 `TeachingImagePath_Horizontal` 텍스트 = 2+ 라인 hit (Task 03-01: `string hpathMeas = meas.TeachingImagePath_Horizontal;` + Task 03-02: `measForBadge.TeachingImagePath_Horizontal` File.Exists 인자 1+ + IsNullOrEmpty 인자 1+ — 총 3+)
   - `가로축 (Measurement)` 리터럴 = 1 hit
   - `가로축 (Shot fallback)` 리터럴 = 1 hit
   - `hpathMeas` (CS0136 회피 명명) = 2+ hit (declare + LoadImage)
   - `Phase 39.4 D-G1` 마커 (Task 03-01) = 3+ hit
   - `Phase 39.4 D-G4` 마커 (Task 03-02) = 5+ hit

3. **Anti-Goal 가드 (코드 인스펙션)**:
   - L1295 UpdateImageSourceBadge 시그니처 + L1296 `_currentImageSource = source;` 변경 0
   - L1305-1308 Vertical 분기 (`"세로축"` 텍스트) 변경 0 (Anti-Goal — Vertical 은 항상 Measurement 단위 경로)
   - L1303 `BadgeBrushHorizontal` + L1307 `BadgeBrushVertical` + L1309 `InvalidateVisual()` 변경 0
   - L1312-1314 토글 IsChecked 동기화 변경 0
   - L1316-1322 Datum publish 가드 (`_selectedDatumForSwap` + `AlgorithmTypeEnum == VerticalTwoHorizontalDualImage` + `PublishDatumRoiCandidates`) 변경 0
   - L1323-1328 Measurement publish 가드 (`_selectedDualImageMeasurement != null` + `RenderInspectionResultForNode`) 변경 0
   - L1346 BtnSwapHorizontal_Click 시그니처 + L1347-1349 outer 블록 + L1362 UpdateImageSourceBadge 호출 + L1363 return 변경 0
   - L1366-1377 Datum 경로 (`var d = _selectedDatumForSwap;` 이후 모든 라인 — Phase 34.1/36/37 baseline) 변경 0 (Anti-Goal #3 D-G2)
   - L1381-1407 BtnSwapVertical_Click 전체 변경 0
   - L1411-1418 PublishDatumRoiCandidates mutex clear 변경 0 (Anti-Goal #7 Phase 39.3 mutex 보존)
   - L1496-1518 PublishMeasurementDualImageSelection 변경 0 (Anti-Goal #7 Horizontal 리셋 보존)
   - MainView.xaml (XAML 파일) 변경 0 — 텍스트만 코드 비하인드 동적 set (Anti-Goal #8)
   - using 블록 변경 0

4. **회귀 0 의미적 검증**:
   - Datum DualImage selection (Side Phase 34.1/36/37) → `_selectedDualImageMeasurement == null` (mutex via PublishDatumRoiCandidates) → `measForBadge == null` → `horizontalBadgeText = "가로축"` (Phase 34.1 baseline 텍스트 동일) + Datum 분기 BtnSwapHorizontal_Click L1366-1377 코드 그대로 실행 → 회귀 0
   - 기존 7 Measurement (비-DualImage) selection → btn_swapHorizontal 자체 Visibility=Collapsed (Phase 39.3 baseline) → Click 호출 X → 회귀 0
</verification>

<success_criteria>
- Task 03-01 + Task 03-02 모두 msbuild Debug/x64 PASS (0 new errors, 0 new warnings)
- BtnSwapHorizontal_Click Measurement 분기 = D-G1 fallback if/else 분기 (Measurement 명시 우선 + ShotConfig fallback)
- UpdateImageSourceBadge Horizontal 분기 = D-G4 동적 텍스트 (`"가로축 (Measurement)"` / `"가로축 (Shot fallback)"` / `"가로축"` 3종)
- Vertical 분기 변경 0 (Anti-Goal — Vertical 은 항상 Measurement 단위 경로)
- Datum 경로 + mutex 가드 + ROI publish + 토글 IsChecked + XAML 파일 변경 0
- CS0136 회피 (`hpathMeas` / `measForBadge` / `hasExplicitHorizontal` / `horizontalBadgeText` 명명)
- Plan 02 (검사 시점 fallback) 와 swap UX 시점 동일 우선순위 통일 (회귀 0 의미적 일관성)
</success_criteria>

<output>
After completion, create `.planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-03-SUMMARY.md` with:
- Task 03-01 + Task 03-02 changes (file:line + 추가/수정 라인 수)
- Anti-Goal 가드 검증 결과 (위 verification 4개 항 — 특히 Vertical 분기 변경 0 + XAML 파일 변경 0)
- msbuild Debug/x64 결과 (errors/warnings 카운트)
- Datum DualImage 회귀 0 의미적 검증 (mutex 가드 흐름 추적)
- Plan 02 와의 동작 일관성 (검사 시점 = swap UX 시점 동일 우선순위)
</output>
</content>
