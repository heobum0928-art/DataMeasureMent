---
phase: 39.4-bottom-dualimage-manual-swap-2026-05-30
plan: "03"
subsystem: ui
tags: [wpf, mainview, dualimage, swap, badge, fallback, halcon]

requires:
  - phase: 39.4-bottom-dualimage-manual-swap-2026-05-30
    plan: "01"
    provides: "DualImageEdgeDistanceMeasurement.TeachingImagePath_Horizontal 필드"

provides:
  - "BtnSwapHorizontal_Click Measurement 분기: TeachingImagePath_Horizontal 명시+존재 → LoadImage(hpathMeas), 아니면 ShotConfig fallback (D-G1)"
  - "UpdateImageSourceBadge Horizontal 분기: '가로축 (Measurement)' / '가로축 (Shot fallback)' / '가로축' 동적 배지 텍스트 (D-G4)"

affects:
  - "Phase 39.4 Plan 04 UAT (Test 2 swap 동작 + Test 3 배지 텍스트 시각 검증)"
  - "Datum DualImage swap UX (Phase 34.1/36/37) — mutex 가드 회귀 0"

tech-stack:
  added: []
  patterns:
    - "hpathMeas 명명 패턴 — CS0136 outer-scope 충돌 회피 (vpathMeas Phase 39.3 Task 02-04 mirror)"
    - "RT-4 옵션 A — UpdateImageSourceBadge 안 single source of truth (D-G4 배지 텍스트 결정)"
    - "_selectedDualImageMeasurement null 가드로 Datum 경로 mutex 보존"

key-files:
  created: []
  modified:
    - "WPF_Example/UI/ContentItem/MainView.xaml.cs (L1350-1379 BtnSwapHorizontal_Click Measurement 분기 + L1300-1320 UpdateImageSourceBadge Horizontal 분기)"

key-decisions:
  - "RT-4 옵션 A 채택: UpdateImageSourceBadge 안에서 배지 텍스트 결정 (single source of truth, Datum mutex 가드 자동 적용)"
  - "CS0136 회피: hpathMeas 명명 (Datum 분기 L1383 hpath 와 outer scope 분리)"
  - "Datum 경로 (L1383-1393) + Vertical 분기 + PublishDatumRoiCandidates mutex + XAML 파일 변경 0 (Anti-Goal #3/#8/#9)"

patterns-established:
  - "D-G1 fallback 분기: IsNullOrEmpty+File.Exists → 명시 경로 / else ShotConfig.GetImage() — Plan 02 와 동일 우선순위"
  - "D-G4 배지 텍스트: _selectedDualImageMeasurement null 가드 → Datum 회귀 0 mutex 패턴"

requirements-completed: [WF-01]

duration: 15min
completed: 2026-05-30
---

# Phase 39.4 Plan 03: MainView swap UX + 배지 텍스트 Summary

**BtnSwapHorizontal_Click에 D-G1 fallback if/else 분기(TeachingImagePath_Horizontal 명시 우선 → ShotConfig fallback)와 UpdateImageSourceBadge에 D-G4 동적 배지 텍스트("가로축 (Measurement)"/"가로축 (Shot fallback)"/"가로축") 추가 — Datum mutex 가드와 XAML 변경 0 유지**

## Performance

- **Duration:** 15 min
- **Started:** 2026-05-30
- **Completed:** 2026-05-30
- **Tasks:** 2 (Task 03-01 + Task 03-02, 동일 파일 단일 commit)
- **Files modified:** 1

## Accomplishments

- BtnSwapHorizontal_Click Measurement 분기: `hpathMeas = meas.TeachingImagePath_Horizontal` → 명시+존재 시 `halconViewer.LoadImage(hpathMeas)`, else ShotConfig.GetImage() fallback (Plan 02 D-G1 우선순위와 swap UX 시점 동일 통일)
- UpdateImageSourceBadge Horizontal 분기: `measForBadge` null 가드 + `hasExplicitHorizontal` 판단으로 배지 텍스트 3종 동적 결정 (D-G4)
- Datum 경로(L1383-1393) + BtnSwapVertical_Click 전체 + PublishDatumRoiCandidates mutex + XAML(MainView.xaml) 변경 0 — Anti-Goal 10항 준수

## Task Commits

1. **Task 03-01 + 03-02: MainView swap UX + 배지 텍스트 D-G1 + D-G4** - `85d8c92` (feat)

## Files Created/Modified

- `WPF_Example/UI/ContentItem/MainView.xaml.cs` — BtnSwapHorizontal_Click Measurement 분기 D-G1 fallback if/else 교체 (L1350-1379) + UpdateImageSourceBadge Horizontal 분기 D-G4 동적 배지 텍스트 (L1300-1320)

## Decisions Made

- RT-4 옵션 A 채택: UpdateImageSourceBadge 안에서 배지 텍스트 결정 — `_selectedDualImageMeasurement` 상태를 단일 진입점에서 읽어 Datum 경로(null 가드)와 일관 처리
- CS0136 회피: `hpathMeas` 명명 — Datum 분기 outer scope의 `hpath` (L1383)와 변수명 분리 (Phase 39.3 Task 02-04 `vpathMeas` 패턴 mirror)
- File.Exists I/O 는 swap 토글 클릭 시점만 발생 → LOW cost 수용 (RT-4 검증)

## Deviations from Plan

없음 — 플랜 그대로 실행.

## Anti-Goal 가드 검증 결과

| 항목 | 결과 |
|------|------|
| BtnSwapHorizontal_Click 시그니처 + outer 블록 (`var meas = ...`) 변경 0 | PASS |
| `UpdateImageSourceBadge(...Horizontal)` 호출 (L1379) + `return` (L1380) 위치 변경 0 | PASS |
| Datum 경로 (`var d = _selectedDatumForSwap;` L1383 이후 전체) 변경 0 | PASS |
| BtnSwapVertical_Click 전체 (L1397-1423) 변경 0 | PASS |
| Vertical else 분기 — `"세로축"` 텍스트 그대로 (D-G4 후속 Vertical 변경 0) | PASS |
| BadgeBrushHorizontal + BadgeBrushVertical 색상 set 보존 | PASS |
| `border_imageSourceBadge.InvalidateVisual()` 보존 (CO-34.1-03) | PASS |
| 토글 IsChecked 동기화 (L1330-1331) 변경 0 | PASS |
| Datum publish 가드 (L1334-1338) 변경 0 (Anti-Goal #3 D-G2) | PASS |
| Measurement publish 가드 (L1339-1341 `RenderInspectionResultForNode`) 변경 0 | PASS |
| PublishDatumRoiCandidates mutex clear (L1428-1434) 변경 0 (Anti-Goal #7) | PASS |
| PublishMeasurementDualImageSelection (L1512-1534) 변경 0 | PASS |
| MainView.xaml (XAML 파일) 변경 0 (Anti-Goal #9) | PASS |
| using 추가 0 | PASS |
| CS0136 회피 (`hpathMeas` / `measForBadge` / `hasExplicitHorizontal` / `horizontalBadgeText` 명명) | PASS |

## msbuild Debug/x64 결과

- **신규 error:** 0
- **신규 warning:** 0
- **빌드 결과:** PASS (`DatumMeasurement.exe` 생성 확인)
- 기존 baseline 경고 (CS0618 Phase 33 마이그레이션 / CS0162 VirtualCamera / MSB3884 ruleset) 신규 추가 0

## Datum DualImage 회귀 0 의미적 검증

Datum DualImage 선택 시 (`_selectedDualImageMeasurement == null` — mutex via `PublishDatumRoiCandidates` L1434 clear):

1. `UpdateImageSourceBadge` Horizontal 분기 진입 → `measForBadge = _selectedDualImageMeasurement` → `null`
2. `measForBadge == null` → `horizontalBadgeText = "가로축"` (기본값, Phase 34.1 baseline 텍스트 동일)
3. `txt_imageSourceBadge.SetCurrentValue(..., "가로축")` → 배지 Phase 34.1/36/37 UAT 결과와 byte-identical
4. `BtnSwapHorizontal_Click` Measurement 분기 → `meas = null` → outer 블록 skip → Datum 경로 (L1383-1393) 진입 → 기존 동작 그대로

결론: Datum DualImage (Side Phase 34.1/36/37) 회귀 **0**.

## Plan 02 와의 동작 일관성

| 경로 | Plan 02 (검사 시점) | Plan 03 (swap UX 시점) |
|------|-------------------|-----------------------|
| 명시 경로 있음 | `pathA = dualMeas.TeachingImagePath_Horizontal` → TryLoadHalconImage | `halconViewer.LoadImage(hpathMeas)` |
| 명시 경로 없음 | `pathA = ShotParam.SimulImagePath` (fallback) | ShotConfig.GetImage() (fallback) |
| 판단 조건 | `IsNullOrEmpty || !File.Exists` | `IsNullOrEmpty || !File.Exists` |

검사 시점과 swap UX 시점 모두 **동일 우선순위** — D-G1 계약 일관성 달성.

## grep 검증 결과

| grep 패턴 | 결과 |
|-----------|------|
| `string hpathMeas = meas.TeachingImagePath_Horizontal` | 1 hit (L1360) |
| `halconViewer.LoadImage(hpathMeas)` | 1 hit (L1362) |
| `string hpath = d.TeachingImagePath` | 1 hit (L1383 Datum 분기 보존) |
| `Phase 39.4 D-G1` 마커 | 4 hit |
| `Phase 39.4 D-G4` 마커 | 7 hit |
| `가로축 (Measurement)` 리터럴 | 1 hit (L1308) |
| `가로축 (Shot fallback)` 리터럴 | 1 hit (L1308) |
| `horizontalBadgeText` 변수 | 3 hit |
| `measForBadge` 변수 | 3 hit |
| `hasExplicitHorizontal` 변수 | 2 hit |
| `"세로축"` 텍스트 set (Vertical 보존) | 1 hit (L1314) |
| `BadgeBrushHorizontal` | 기존과 동일 hit |
| `InvalidateVisual()` | 기존과 동일 hit |

## Known Stubs

없음.

## Threat Flags

없음 (코드 비하인드 UI 로직만 변경, 신규 네트워크/파일시스템/인증 경로 0).

## Next Phase Readiness

- Plan 04 (SIMUL UAT): Test 2 (swap 동작 D-G1) + Test 3 (배지 텍스트 D-G4) 검증 준비 완료
- Plan 03 산출물이 Plan 04 의 유일한 의존 대상 — 즉시 UAT 진행 가능

---
*Phase: 39.4-bottom-dualimage-manual-swap-2026-05-30*
*Completed: 2026-05-30*

## Self-Check: PASSED

- [x] `WPF_Example/UI/ContentItem/MainView.xaml.cs` 존재 및 변경 확인
- [x] commit `85d8c92` 존재 확인 (`git log --oneline` 첫 번째 항목)
- [x] SUMMARY.md 파일 생성 완료
