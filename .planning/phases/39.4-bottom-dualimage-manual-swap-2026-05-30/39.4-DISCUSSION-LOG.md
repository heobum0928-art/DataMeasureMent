# Phase 39.4: Bottom DualImage 수동 swap UX 재설계 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-30
**Phase:** 39.4-bottom-dualimage-manual-swap-2026-05-30
**Areas discussed:** Fallback 정책, Datum DualImage 일괄 적용 범위, PropertyGrid 라벨 한글화 방식, Swap UX 세부 동작

---

## Fallback 정책 (TeachingImagePath_Horizontal 부재 시)

| Option | Description | Selected |
|--------|-------------|----------|
| ShotConfig fallback (추천) | Phase 39.2/39.3 baseline 동작 보존 — 39.3 구조 대안서 기존 INI 레시피 자동 호환 + 점진적 마이그레이션 가능. 작업자가 안 설정해도 계속 검사 동작. 인지 명확성은 Area 3/4 에서 보완. | ✓ |
| 명시적 에러 | TeachingImagePath_Horizontal 필수 강제. 총 작업자가 편의는 떨어지지만 계정 혼동 원체 차단. 기존 INI 레시피는 첨감 자동 멈춤 — 수동 마이그레이션 필요. | |
| 사용자 선택 다이얼로그 | Swap 시도 때 다이얼로그 열어 사용자가 파일 선택. UI 조작 증가. 자동 검사 흐름에 멈춤 발생. | |

**User's choice:** ShotConfig fallback
**Notes:** 회귀 0 + INI 호환 자동 충족. INI 호환 결정과 통합 (별도 Open Question 불필요).

---

## Datum DualImage 일괄 적용 범위

| Option | Description | Selected |
|--------|-------------|----------|
| 별도 phase (추천) | 39.4 = Measurement 에만 집중 (Anti-Goal 'Datum 회귀 0' 일관). Datum 일괄 적용은 후속 phase (39.5 또는 v1.3) 로 이관. 회귀 표면 격리, UAT 부담 최소화. | ✓ |
| 같이 적용 | 39.4 안에서 Datum 측까지 attribute + 라벨 활성 — 한번에 일관성 확보. 단 Phase 36/37 있는 Datum 코드 회귀 표면을 변화하는 리스크 추가 명시 필요. | |

**User's choice:** 별도 phase
**Notes:** Deferred Ideas 섹션에 Datum 일관화 후속 phase 명시.

---

## PropertyGrid 라벨 한글화 방식

| Option | Description | Selected |
|--------|-------------|----------|
| [DisplayName] + [Category] 조합 (추천) | [DisplayName("가로축 티칭 이미지")] 로 셀 헤더 자체를 한글화 + [Category("Image|DualImage")] 그룹 유지. 가독성 최고, 명확함 + 그룹화. | ✓ |
| [Category] 텍스트만 한글화 | [Category("이미지|DualImage 가로축")] 식 한글 그룹 헤더. 적은 코드 변경. 그룹 펼침 시 보이지만 property 이름은 영문 유지. | |
| [Description] 보강만 | tooltip (hover 시 표시) 에 한글 설명 추가. 셀 이름은 변경 안 함. 이미있는 [Description] 활용, 추가 코드 몇 줄 — 가장 가볍지만 출처 명시는 약함. | |

**User's choice:** [DisplayName] + [Category] 조합
**Notes:** PropertyTools 3.1.0 [DisplayName] namespace 검증 필요 (research-phase). Fallback = [Description] 만.

---

## Swap UX 세부 동작 (하이라이트)

| Option | Description | Selected |
|--------|-------------|----------|
| 배지 라벨에 소스 명시 (추천) | 배지 텍스트 동적 변경 — '가로축 (Measurement)' (명시 경로) vs '가로축 (Shot fallback)' (ShotConfig). 색상 동일. Swap 누를 때 즉시 인지, 추가 클릭 부담 0. | ✓ |
| 티팅 바 메시지 | 하단 상태 표시줄에 'Shot 공용 이미지 사용 중 (TeachingImagePath_Horizontal 닿면 Measurement 이미지)' 메시지. 2-3초 자동 소멸. | |
| 변경 안함 (코드 최소) | 배지/티팅 바 장환 없이 기존 'Horizontal' 라벨 그대로. 별도 UI 구축 부담 0. 인지는 PropertyGrid 의 TeachingImagePath_Horizontal 셀이 채워졌는지 적용 작업자 책임으로 이관. | |

**User's choice:** 배지 라벨에 소스 명시
**Notes:** UpdateImageSourceBadge 또는 BtnSwap*_Click 안에서 배지 텍스트 동적 set. Measurement 가 selected 일 때만 fallback 분기 적용 (Datum DualImage 는 기존 텍스트 보존).

---

## Claude's Discretion

없음 — 4 area 모두 명시적 결정 lock.

## Deferred Ideas

- Datum DualImage 일관화 (Phase 36/37 의 TeachingImagePath/Vertical 에 동일 attribute 적용) → 후속 phase
- Browse 버튼 Risk R4 시각 검증 (Phase 39.3 Test 4 NOT_TESTED) → Phase 39.4 Plan 04 UAT 에 흡수
- 회귀 D/E 종합 검증 (Phase 39.3 NOT_TESTED) → Phase 39.4 Plan 04 UAT 에 흡수
- HALCON SetColor 비표준 색상명 정적 분석 도구 → backlog
