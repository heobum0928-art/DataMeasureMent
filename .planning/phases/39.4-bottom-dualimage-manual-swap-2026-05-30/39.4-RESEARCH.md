---
phase: 39.4-bottom-dualimage-manual-swap-2026-05-30
type: research
date: 2026-05-30
status: complete
depends_on: 39.4-CONTEXT.md (4 결정 D-G1~G4 lock) + 39.3 PARTIAL_SIGNED_OFF (Plan 02 commits a8facf9 / 3d9e15e / Plan 03 commit f260cf0)
confidence: HIGH
---

# Phase 39.4 RESEARCH — Bottom DualImage 수동 swap UX 재설계

**Researched:** 2026-05-30
**Confidence:** HIGH — 4 RT 모두 직접 코드 인스펙션 + PropertyTools 3.1.0 어셈블리 reflection 로드 검증 + Phase 22 IMG-01 baseline 재확인 완료.

---

## Phase Boundary (CONTEXT.md 재인용)

DualImage Measurement (`DualImageEdgeDistanceMeasurement`) 의 **가로/세로 양측 모두 Measurement 단위 명시 경로** 로 재설계.
- 가로축 = `TeachingImagePath_Horizontal` 신규 필드 (default `""`)
- 세로축 = 기존 `TeachingImagePath_Vertical` 유지 (Phase 22/37/39.3 baseline 보존)
- ShotConfig 이미지 = **fallback 으로 강등** (D-G1)
- Datum DualImage (Phase 36/37) 일관화는 별도 phase 로 이관 (D-G2)

작업 단위 = Bottom DualImage Measurement 한 종류 집중. Anti-Goal 10항 = 회귀 0 가드.

---

## Research Targets — 4건 (RT-1 ~ RT-4)

### RT-1 — PropertyTools 3.1.0 `[DisplayName]` namespace 검증

#### 1.1 PropertyTools 어셈블리 reflection 확인 (검증된 ground truth)

PowerShell `Add-Type -Path 'packages/PropertyTools.3.1.0/lib/net45/PropertyTools.dll'` 후 `[PropertyTools.DataAnnotations.DisplayNameAttribute] | Format-List FullName` 결과:

```
FullName : PropertyTools.DataAnnotations.DisplayNameAttribute
```

**결론:** `PropertyTools.DataAnnotations.DisplayNameAttribute` **존재 확인**. `using PropertyTools.DataAnnotations;` 하 `[DisplayName("...")]` 시 PropertyTools 측 attribute 가 우선 매칭됨 (`System.ComponentModel.DisplayName` 가 아닌 PropertyTools 측).

[VERIFIED: packages/PropertyTools.3.1.0/lib/net45/PropertyTools.dll reflection 로드]

#### 1.2 코드베이스 내 use site 검증 (in-repo proof)

`grep "\[DisplayName"` 결과 — DataMeasurement 코드베이스 광범위 사용 (Wafer + Bottom 외 다수):
- `WPF_Example/Custom/Sequence/Wafer/Action_WaferScanCalibration.cs:56` — `[DisplayName("Calibration Enable")]` + `using PropertyTools.DataAnnotations;` (L5) 단일 import + `[Category("Grid Calibration")]` (L55) + `[Spinnable]` (L69) 와 같은 PropertyTools attribute 와 **혼용 정상 동작 중**
- `WPF_Example/Custom/Sequence/Bottom/Action_BottomCalibration.cs:83` — `[DisplayName("Process Name")]` 동일 패턴
- `Action_WaferScanCalibration.cs` L70/87/103/118/134/150/166/182/202/223 — 11 use site 모두 PropertyGrid 셀 헤더 영문 라벨로 정상 표시 (사용자 환경 검증된 코드)

**핵심:** Action_WaferScanCalibration.cs L5 의 import 는 단 한 줄 `using PropertyTools.DataAnnotations;` 이며 `System.ComponentModel` import 0 — 그래도 `[DisplayName("Calibration Enable")]` 으로 PropertyGrid 셀 헤더에 "Calibration Enable" 로 노출됨 → **PropertyTools.DataAnnotations.DisplayName 가 PropertyGrid 셀 헤더에 실제 반영되는 attribute**.

[VERIFIED: WPF_Example/Custom/Sequence/Wafer/Action_WaferScanCalibration.cs:1-15, 55-70 직접 인용]
[VERIFIED: WPF_Example 전체 `[DisplayName` grep 결과 — 0 컴파일 에러 baseline]

#### 1.3 Plan 01 적용 (lock-in 결론)

**적용 패턴 — 추가 import 0 (기존 `using PropertyTools.DataAnnotations;` (DualImageEdgeDistanceMeasurement.cs L4) 그대로 사용):**

```csharp
// DualImageEdgeDistanceMeasurement.cs L23-29 변경 (Plan 01)
[Category("Image|DualImage")]
[System.ComponentModel.Description("PointROI 검출용 가로축 이미지 경로 — 명시 시 우선 사용, 빈 문자열/파일 부재 시 ShotConfig.SimulImagePath 로 fallback (D-G1)")] //260530 hbk Phase 39.4 D-G1
[DisplayName("가로축 티칭 이미지")] //260530 hbk Phase 39.4 D-G3 — PropertyTools.DataAnnotations.DisplayName (Action_WaferScanCalibration L56 패턴)
[InputFilePath(DeviceHandler.EXTENSION_IMAGE, DeviceHandler.FILTER_IMAGE)] //260530 hbk Phase 39.4 D-G3
[AutoUpdateText] //260530 hbk Phase 39.4 D-G3 — 즉시 INPC fire
public string TeachingImagePath_Horizontal { get; set; } = ""; //260530 hbk Phase 39.4 D-G1

[Category("Image|DualImage")]
[System.ComponentModel.Description("LineROI 검출용 별도 이미지 경로 — Bottom E5 패턴 (Phase 37 VTH-DualImage 슬롯 재사용)")]
[DisplayName("세로축 티칭 이미지")] //260530 hbk Phase 39.4 D-G3 — Phase 39.3 D-G3 에 한글 라벨 보강
[InputFilePath(DeviceHandler.EXTENSION_IMAGE, DeviceHandler.FILTER_IMAGE)] //260530 hbk Phase 39.3 D-G3
[AutoUpdateText] //260530 hbk Phase 39.3 D-G3
public string TeachingImagePath_Vertical { get; set; } = "";
```

#### 1.4 Fallback Plan (D-G3 fallback 불필요 — 검증된 패턴)

CONTEXT.md D-G3 의 fallback ("`[Description]` 만으로 tooltip 한글화 + 셀 이름 영문 유지") = **불필요**. RT-1.2 의 11 use site 가 이미 PropertyGrid 에서 한글/영문 셀 헤더 정상 노출 검증된 패턴 → Plan 01 적용 시 fallback 분기 0.

**잔여 LOW risk:** PropertyTools 의 attribute 처리 순서가 `[DisplayName]` + `[InputFilePath]` + `[Category]` 조합에서 PropertyGrid 그룹화 + Browse 버튼 + 셀 헤더 3종을 모두 정상 표시하는지는 Phase 39.3 Plan 03 의 `[InputFilePath]` + `[Category]` 조합 빌드 PASS + Phase 39.3 NOT_TESTED Test 4 회수 시점에 시각 검증 — Plan 04 Test 1 (PropertyGrid 노출 시각 확인) 에 포함.

[VERIFIED: PropertyTools.3.1.0 어셈블리 reflection + 11 in-repo use site 인용]
[RESOLVED: CONTEXT.md D-G3 Open Question — PropertyTools.DataAnnotations.DisplayName 가 정답, System.ComponentModel.DisplayName 미적용. Fallback 분기 불필요.]

---

### RT-2 — ParamBase reflection auto-pickup (string field INI 직렬화)

#### 2.1 ParamBase.Save/Load 메커니즘 (verified)

`WPF_Example/Sequence/Param/ParamBase.cs:324-430` 의 Save/Load 본체:

**Save (L324-367):**
```csharp
public virtual bool Save(IniFile saveFile, string group) {
    PropertyInfo[] props = this.GetType().GetProperties(BindingFlags.Instance | BindingFlags.Public);
    foreach (var prop in props) {
        string name = prop.Name;
        string type = prop.PropertyType.Name;
        switch (type) {
            ...
            case "String":
                saveFile[group][name] = (string)prop.GetValue(this); // L338
                break;
            ...
        }
    }
    return true;
}
```

**Load (L369-430):**
```csharp
public virtual bool Load(IniFile loadFile, string group) {
    PropertyInfo[] props = GetType().GetProperties();
    foreach (var prop in props) {
        string name = prop.Name;
        string type = prop.PropertyType.Name;
        try {
            switch (type) {
                ...
                case "String":
                    string sValue = loadFile[group][name].ToString(); // L386
                    prop.SetValue(this, sValue);                       // L387
                    break;
                ...
            }
        }
        catch(Exception e) { /* log */ }
    }
    return true;
}
```

**핵심:** reflection `GetType().GetProperties()` 가 base class (`MeasurementBase : ParamBase`) + 파생 클래스 (`DualImageEdgeDistanceMeasurement : MeasurementBase`) 의 모든 public property 를 자동 픽업 → `case "String"` 분기 (L337-339 / L385-395) 가 모든 `string` property 를 INI key-value 로 직렬화.

[VERIFIED: WPF_Example/Sequence/Param/ParamBase.cs:324-430 직접 인용]
[VERIFIED: WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs:15 `public abstract class MeasurementBase : ParamBase` 상속 체인 확인]

#### 2.2 Phase 22 IMG-01 baseline 재확인 (TeachingImagePath_Vertical INI 호환)

`WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs:38-46` 인용:
```csharp
//260511 hbk Phase 22 IMG-01 — Datum 티칭 시 사용한 기준 이미지 경로. INI 직렬화는 ParamBase reflection 이 자동 처리 (ParamBase.cs L325-339 Save 의 case "String", L385-395 Load 의 case "String"). 키 미존재 → EnsurePerRoiDefaults 에서 "" 정규화 (T2).
[Category("Datum|ImageSource")]
public string TeachingImagePath { get; set; } = "";

//260527 hbk Phase 34 D-34-04 — 가로축 이미지(TeachingImagePath) 와 분리된 세로축 이미지 경로.
//  INI 직렬화 = ParamBase reflection String case 자동 (Phase 22 IMG-01 패턴 그대로). 키 미존재 → EnsurePerRoiDefaults 에서 "" 정규화 (D-34-11).
[Category("Datum|ImageSource")]
public string TeachingImagePath_Vertical { get; set; } = ""; //260527 hbk Phase 34 D-34-04
```

**검증된 Phase 22/34 baseline:**
- Default value `""` + `ParamBase.Load` 의 `case "String"` 분기 → INI 키 미존재 시 `loadFile[group][name].ToString()` 이 empty string 반환 → 기존 INI 레시피와 byte-identical 호환 (회귀 0).
- 신규 property 추가 시 attribute 변경 0, get/set body 변경 0 → reflection 자동 픽업.

#### 2.3 Phase 39.4 Plan 01 적용 검증

신규 `TeachingImagePath_Horizontal` (string, default `""`) 추가 시:

1. **자동 픽업:** `DualImageEdgeDistanceMeasurement.GetType().GetProperties()` 가 신규 property 를 reflection 으로 자동 검색 → `case "String"` 분기 (Save L338 / Load L386) 자동 적용.
2. **INI 호환:**
   - 기존 INI 레시피 (TeachingImagePath_Horizontal 부재) Load 시 `loadFile[group]["TeachingImagePath_Horizontal"].ToString()` → empty string 반환 → property = `""` (default 와 동일) → byte-identical 동작.
   - Save 시 신규 key `TeachingImagePath_Horizontal=` (빈 값) 추가됨 — Phase 22 IMG-01 baseline 과 동일 패턴 (신규 string property 추가 시 INI 키 자동 출현).
3. **순서:** Reflection `GetProperties()` 의 반환 순서는 .NET Framework 4.x 에서 **declaration order 와 유사하지만 보장은 안 됨** (MS 문서). 그러나 INI key 순서는 `loadFile[group][name]` lookup-by-name 이므로 순서 무관 — `TeachingImagePath_Vertical` 의 INI 내 위치 변경 X (Anti-Goal "TeachingImagePath_Vertical 위치 보존" 충족).
4. **EnsurePerRoiDefaults 정규화:** DatumConfig (Phase 34) 와 달리 Measurement 측은 EnsurePerRoiDefaults 같은 정규화 helper 미적용 — default `""` 만으로 충분 (Action_FAIMeasurement.TryGrabOrLoadFaiDualImages L406 의 `string.IsNullOrEmpty(pathA)` 가드가 이미 처리).

#### 2.4 INI 직렬화 순서 결정 (lock-in)

**Plan 01 의 `TeachingImagePath_Horizontal` 선언 위치:**
- DualImageEdgeDistanceMeasurement.cs L29 의 기존 `TeachingImagePath_Vertical` **위쪽** 에 신규 `TeachingImagePath_Horizontal` 선언 (declaration order 기준 알파벳 무관, 의미 순서 = 가로 → 세로).
- INI 내 key 순서 = property declaration 순서 → 신규 key `TeachingImagePath_Horizontal` 가 `TeachingImagePath_Vertical` 앞에 추가됨 (Anti-Goal "신규 추가만, Vertical 위치 보존" — Vertical 의 상대 위치는 보존됨, 단지 Horizontal 이 앞에 삽입).

**대안:** Vertical 뒤에 추가 (`TeachingImagePath_Vertical` L29 다음 줄에 `TeachingImagePath_Horizontal` 선언) — Anti-Goal "Vertical 위치 보존" 의 절대 위치 (INI line number) 까지 보장. 단, 의미 순서 (가로 → 세로) 가 깨짐.

**Plan-phase 에서 lock 추천:** **Vertical 뒤 추가** (절대 위치 보존). INI diff 최소화 (테스트 baseline 비교 용이). 사용자에게 의미 순서 어색함은 PropertyGrid `[DisplayName]` 한글 라벨이 흡수 — PropertyGrid 셀 헤더 표시는 attribute 순서가 아닌 reflection 순서.

[VERIFIED: WPF_Example/Sequence/Param/ParamBase.cs:324-430 + DatumConfig.cs:38-46 + MeasurementBase.cs:15 상속 체인]
[RESOLVED: CONTEXT.md "TeachingImagePath_Vertical 위치 보존" 요구사항 — Plan 01 의 신규 필드 위치 = Vertical 뒤 추가 권장 (lock-in, plan-phase 에서 최종 확정).]

---

### RT-3 — Phase 39.3 Plan 02 코드 위치 정확화 (line numbers)

#### 3.1 현재 MainView.xaml.cs 의 정확한 line 번호 (검증)

`grep "_selectedDualImageMeasurement|PublishMeasurementDualImageSelection|BadgeBrush"` + Read 직접 인스펙션 결과:

| Phase 39.3 Plan 02 구성요소 | 파일 | 정확 line | 변경 commit | Phase 39.4 변경 필요? |
|------------------------------|------|----------|-------------|----------------------|
| `_currentImageSource` 필드 선언 | MainView.xaml.cs | L62-64 | Phase 34.1 (변경 0) | NO |
| `_selectedDatumForSwap` 필드 선언 | MainView.xaml.cs | L65-68 | Phase 34.1 (변경 0) | NO |
| `_selectedDualImageMeasurement` 필드 선언 | MainView.xaml.cs | L69-71 | Phase 39.3 Plan 02 Task 02-01 (2e28a76) | NO (재사용) |
| `BadgeBrushHorizontal/Vertical` (frozen brush) | MainView.xaml.cs | L72-80 | Phase 34.1 CO-34.1-03 hotfix (변경 0) | NO |
| `UpdateImageSourceBadge` 본체 | MainView.xaml.cs | L1295-1329 | Phase 34.1 D-34.1-15 + Phase 39.3 Plan 02 Task 02-05 (3d9e15e) | **YES (D-G4)** |
| `UpdateImageSourceBadge` 배지 텍스트 set | MainView.xaml.cs | L1300-1310 | Phase 34.1 D-34.1-14 + CO-34.1-03 hotfix | **YES (D-G4)** |
| `UpdateImageSourceBadge` Datum publish 가드 | MainView.xaml.cs | L1316-1322 | Phase 34.1 + Phase 36 D-36-09 | NO (변경 0 가드) |
| `UpdateImageSourceBadge` Measurement publish 분기 | MainView.xaml.cs | L1323-1328 | Phase 39.3 Plan 02 Task 02-05 (3d9e15e) | NO (재사용 + D-G4 텍스트 분기 추가만) |
| `BtnSwapHorizontal_Click` 본체 | MainView.xaml.cs | L1346-1377 | Phase 34.1 + Phase 39.3 Plan 02 Task 02-04 (a8facf9) | **YES (D-G1 fallback)** |
| `BtnSwapHorizontal_Click` Measurement 분기 (Phase 39.3) | MainView.xaml.cs | L1347-1365 | Phase 39.3 Plan 02 Task 02-04 (a8facf9) | **YES (D-G1 우선순위 교체)** |
| `BtnSwapVertical_Click` 본체 | MainView.xaml.cs | L1381-1407 | Phase 34.1 + Phase 39.3 Plan 02 Task 02-04 (a8facf9) | NO (Measurement 분기는 이미 Vertical Measurement 경로 사용) |
| `BtnSwapVertical_Click` Measurement 분기 (Phase 39.3) | MainView.xaml.cs | L1382-1395 | Phase 39.3 Plan 02 Task 02-04 (a8facf9) | NO (D-G4 옵션 텍스트 일관화만 — "세로축 (Measurement)") |
| `PublishMeasurementDualImageSelection` 메서드 | MainView.xaml.cs | L1496-1518 | Phase 39.3 Plan 02 Task 02-01 (2e28a76) | NO (재사용) |
| `PublishDatumRoiCandidates` 진입 시 mutex clear | MainView.xaml.cs | L1411-1418 | Phase 39.3 Plan 02 Task 02-03 (ffd139c) | NO (변경 0) |

#### 3.2 Phase 39.4 변경 대상 line summary (3 파일)

**파일 1: `WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs`** (Plan 01)
- L4 (기존 `using PropertyTools.DataAnnotations;`) — 변경 0
- L5 (기존 `using ReringProject.Device;` Phase 39.3 D-G3) — 변경 0
- L29 (`TeachingImagePath_Vertical` 선언) — `[DisplayName("세로축 티칭 이미지")]` 1줄 추가 (D-G3)
- L29 직전 또는 직후 — **신규 `TeachingImagePath_Horizontal` 필드 선언 + 5종 attribute** (D-G1 + D-G3)
  - Plan-phase lock 권장: Vertical 다음 줄에 추가 (INI line number 절대 위치 보존, RT-2.4)

**파일 2: `WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs`** (Plan 02)
- L403 (`string pathA = ShotParam.SimulImagePath;`) — D-G1 fallback ternary 로 교체:
  ```csharp
  string pathA = string.IsNullOrEmpty(dualMeas.TeachingImagePath_Horizontal) || !File.Exists(dualMeas.TeachingImagePath_Horizontal)
      ? ShotParam.SimulImagePath
      : dualMeas.TeachingImagePath_Horizontal; //260530 hbk Phase 39.4 D-G1 — Measurement 명시 경로 우선, 부재 시 ShotConfig fallback
  ```
- L404 (`string pathB = dualMeas.TeachingImagePath_Vertical;`) — 변경 0 (Vertical = 기존 baseline)
- L394~L425 사이 함수 본체 = 변경 0 (Anti-Goal)

**파일 3: `WPF_Example/UI/ContentItem/MainView.xaml.cs`** (Plan 03)
- L1349-1364 (`BtnSwapHorizontal_Click` Measurement 분기) — Phase 39.3 의 ShotConfig 직접 로드를 **D-G1 fallback 분기 + 배지 텍스트 D-G4** 로 교체:
  - 신규: `string hpathMeas = meas.TeachingImagePath_Horizontal;` (CS0136 회피 — Phase 39.3 Task 02-04 가 `vpathMeas` 패턴 채택, 동일 컨벤션)
  - Fallback 1: `IsNullOrEmpty + !File.Exists` → ShotConfig.GetImage() HImage 경로 + 배지 텍스트 = `"가로축 (Shot fallback)"`
  - Fallback 0 (정상): TeachingImagePath_Horizontal 명시 + 파일 존재 → `halconViewer.LoadImage(hpathMeas)` + 배지 텍스트 = `"가로축 (Measurement)"`
- L1323-1328 (`UpdateImageSourceBadge` Measurement publish 분기) — 변경 0 또는 mini 변경 (D-G4 배지 텍스트 결정 위치는 BtnSwap*_Click 안에서 set 권장, RT-4 참조)
- L1300-1310 (`UpdateImageSourceBadge` 배지 텍스트 set) — D-G4 텍스트 분기 추가 위치 후보 (RT-4 결정)
- L1382-1395 (`BtnSwapVertical_Click` Measurement 분기) — 배지 텍스트 D-G4 일관성 위해 `"세로축 (Measurement)"` 텍스트 set (선택, 단순 `"세로축"` 유지도 가능)

[VERIFIED: WPF_Example/UI/ContentItem/MainView.xaml.cs 직접 인스펙션 (L62-80, 1295-1518)]
[VERIFIED: WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs:386-425 직접 인용]
[VERIFIED: 39.3-02-SUMMARY.md commit a8facf9 (Task 02-04) + 3d9e15e (Task 02-05) 매핑]

---

### RT-4 — UpdateImageSourceBadge 배지 텍스트 동적 set 호환성 (D-G4)

#### 4.1 현재 UpdateImageSourceBadge 본체 분석 (verified)

`MainView.xaml.cs:1295-1329` 의 텍스트 + 색상 set 메커니즘:

```csharp
private void UpdateImageSourceBadge(ReringProject.Sequence.EImageSource source) {
    _currentImageSource = source;

    // (b) 배지 텍스트 + 색상 — D-34.1-14 잠금값
    //260527 hbk Phase 34.1 CO-34.1-03 hotfix — 정적 frozen brush + SetCurrentValue 로 WPF 갱신 보장.
    if (border_imageSourceBadge != null && txt_imageSourceBadge != null) {
        if (source == ReringProject.Sequence.EImageSource.Horizontal) {
            txt_imageSourceBadge.SetCurrentValue(TextBlock.TextProperty, "가로축"); // L1302 — 고정 텍스트
            border_imageSourceBadge.SetCurrentValue(Border.BackgroundProperty, BadgeBrushHorizontal); // L1303
        }
        else {
            txt_imageSourceBadge.SetCurrentValue(TextBlock.TextProperty, "세로축"); // L1306 — 고정 텍스트
            border_imageSourceBadge.SetCurrentValue(Border.BackgroundProperty, BadgeBrushVertical); // L1307
        }
        border_imageSourceBadge.InvalidateVisual(); // L1309 — 즉시 재렌더 강제
    }

    // 토글 버튼 IsChecked 동기화 (L1313-1314)
    if (btn_swapHorizontal != null) btn_swapHorizontal.IsChecked = (source == ...Horizontal);
    if (btn_swapVertical   != null) btn_swapVertical.IsChecked   = (source == ...Vertical);

    // (c) ROI publish 가드 — Datum (L1318-1322) + Measurement (L1326-1328, Phase 39.3)
    var datumForRoi = _selectedDatumForSwap;
    if (datumForRoi != null && datumForRoi.AlgorithmTypeEnum == ...VerticalTwoHorizontalDualImage) {
        PublishDatumRoiCandidates(datumForRoi);
    }
    if (_selectedDualImageMeasurement != null) {
        RenderInspectionResultForNode(_selectedDualImageMeasurement); // Phase 39.3 Plan 02 Task 02-05 (3d9e15e)
    }
}
```

**핵심 발견:**
- 배지 텍스트 set 방법 = **`TextBlock.SetCurrentValue(TextBlock.TextProperty, "...")`** (CO-34.1-03 hotfix 패턴 — XAML binding 없이 코드 비하인드 직접 set, WPF 즉시 반영 보장).
- 색상 set 방법 = **`Border.SetCurrentValue(Border.BackgroundProperty, frozen brush)`** (CO-34.1-03 동일 패턴).
- 텍스트 + 색상 = **분기 안에서 동시 set** (Horizontal / Vertical 양분).
- 토글 IsChecked = `source` enum 비교 (텍스트 무관).
- `InvalidateVisual()` 호출로 즉시 재렌더 강제 — 텍스트 동적 변경 0 cost.

#### 4.2 D-G4 배지 텍스트 분기 위치 결정

**옵션 A — `UpdateImageSourceBadge` 안에서 결정 (single source of truth)**
- L1302/L1306 의 고정 텍스트 `"가로축"` / `"세로축"` 를 동적 결정으로 교체:
  ```csharp
  string badgeText;
  if (source == EImageSource.Horizontal) {
      // D-G4: Measurement 가 active + 명시 경로 있음 → "가로축 (Measurement)" / 없음 → "가로축 (Shot fallback)" / Datum 또는 null → "가로축"
      if (_selectedDualImageMeasurement != null) {
          var meas = _selectedDualImageMeasurement;
          bool hasExplicit = !string.IsNullOrEmpty(meas.TeachingImagePath_Horizontal) && System.IO.File.Exists(meas.TeachingImagePath_Horizontal);
          badgeText = hasExplicit ? "가로축 (Measurement)" : "가로축 (Shot fallback)";
      } else {
          badgeText = "가로축"; // Datum 또는 비-DualImage selection
      }
      txt_imageSourceBadge.SetCurrentValue(TextBlock.TextProperty, badgeText);
      border_imageSourceBadge.SetCurrentValue(Border.BackgroundProperty, BadgeBrushHorizontal);
  }
  ```
- **장점:** 모든 진입점 (자동 swap + 수동 swap + 노드 전환) 공통 적용.
- **단점:** UpdateImageSourceBadge 메서드 복잡도 증가 + File.Exists I/O 가 swap 토글 매 호출마다 발생 (LOW cost, 단일 파일).
- **Datum 보존:** `_selectedDualImageMeasurement == null` 분기에서 기존 `"가로축"` / `"세로축"` 그대로 — Datum DualImage 회귀 0.

**옵션 B — `BtnSwap*_Click` 안에서 결정 + 배지 직접 set (분산)**
- `BtnSwapHorizontal_Click` 의 Measurement 분기 안에서 fallback 여부 판단 → `txt_imageSourceBadge.SetCurrentValue(...)` 직접 set 후 `UpdateImageSourceBadge` 호출.
- **장점:** 결정 위치가 fallback 결정 위치와 동일 (file load 분기 옆) → File.Exists 중복 제거.
- **단점:** 자동 swap 경로 (Datum 의 StartDatumTeachStep L1994~) 가 Measurement 대상 분기 미지원 → 비대칭. 향후 자동 swap 도입 시 수정 필요.
- **mutex 가드:** `UpdateImageSourceBadge` 안의 (c) ROI publish 가드가 Measurement publish 호출 → 텍스트 set 위치와 ROI publish 위치 분리 → 일관성 risk.

**권장 (lock-in 후보):** **옵션 A** — UpdateImageSourceBadge 안에서 결정.
- single source of truth
- File.Exists I/O 는 swap 토글 시점만 발생 (LOW cost — UI 클릭 빈도)
- Datum 경로 보존 (mutex 가 보장)
- Phase 39.3 Task 02-05 의 RenderInspectionResultForNode 호출과 동일 publish 가드 위치 → 일관성 유지

**최종 lock 은 plan-phase 에서.** Plan 03 Task task 분할 시점에 옵션 A/B 중 택일.

#### 4.3 색상 분기 보존 (BadgeBrush 충돌 0)

옵션 A 채택 시:
- Horizontal 분기 안에서 텍스트 (`"가로축 (Measurement)"` / `"가로축 (Shot fallback)"` / `"가로축"`) 만 동적, 색상 = `BadgeBrushHorizontal` (Blue 700) 고정.
- Vertical 분기 안에서 텍스트 (`"세로축"` 또는 옵션 `"세로축 (Measurement)"`) 만 동적, 색상 = `BadgeBrushVertical` (Orange 800) 고정.
- BadgeBrush frozen brush 정적 인스턴스 (L74-75) 재사용 0 변경.

**Datum 배지 텍스트 보존 검증:**
- `_selectedDualImageMeasurement == null` 분기 = Datum 또는 비-DualImage selection.
- Datum DualImage 케이스 → 기존 `"가로축"` / `"세로축"` 그대로 → Phase 34.1/36/37 UAT 회귀 0.
- mutex 가드 (Phase 39.3 Plan 02 Task 02-03 ffd139c, L1416-1418) → Datum/Measurement 동시 non-null 불가 → 텍스트 분기 충돌 0.

#### 4.4 Vertical 배지 텍스트 결정 (선택)

CONTEXT.md D-G4: "세로축 (`btn_swapVertical`) 은 항상 Measurement 단위 경로이므로 변경 0 → 배지 = `"세로축"` (또는 일관성 위해 `"세로축 (Measurement)"`)"

**plan-phase 에서 lock:** **두 옵션 모두 valid**. 일관성 우선 시 `"세로축 (Measurement)"` (가로축 와 동일 suffix 패턴), 단순성 우선 시 `"세로축"` (텍스트 변경 0). UAT 사용자 피드백으로 결정.

[VERIFIED: WPF_Example/UI/ContentItem/MainView.xaml.cs:1295-1329 직접 인용]
[RESOLVED: CONTEXT.md D-G4 fallback 분기 위치 — 옵션 A (UpdateImageSourceBadge 안) 권장, plan-phase 에서 task 분할 시 최종 lock.]

---

## Validation Architecture (Nyquist 8 Dimensions)

> Phase 39.4 = UI/모델 중심 phase. 알고리즘 변경 0 (DualImageEdgeDistanceMeasurement.TryExecute Anti-Goal). Test framework 없음 (CLAUDE.md). Plan 04 SIMUL UAT 가 단일 검증 게이트.

### Test Framework

| Property | Value |
|----------|-------|
| Framework | None (CLAUDE.md — Test/ 디렉토리는 Python mock 스크립트만) |
| Config file | none |
| Quick run command | `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64` |
| Full suite | 빌드 + `bin/x64/Debug/DatumMeasurement.exe` 실행 → SIMUL UAT |

### Phase 39.4 Plan 04 UAT 의 8 검증 Dimensions

| Dim | 검증 항목 | 측정 방법 | 자동/수동 | Plan 04 Test 매핑 |
|-----|----------|----------|----------|------------------|
| **Dim 1** | msbuild PASS (Debug/x64) — 0 new errors / 0 new warnings | shell exit code | **자동** | Per-task 빌드 (Plan 01/02/03 wave gate) |
| **Dim 2** | PropertyGrid 양측 Browse 노출 + 한글 라벨 (D-G3) | 시각 확인 (PropertyGrid `Image|DualImage` 카테고리 펼침) | 수동 | Plan 04 Test 1 |
| **Dim 3** | Horizontal swap 동작 — TeachingImagePath_Horizontal 우선 + fallback ShotConfig (D-G1) | (a) 명시 경로 set → 가로축 swap → 캔버스 = 명시 이미지 (b) 경로 빈/부재 → 가로축 swap → 캔버스 = ShotConfig 이미지 | 시각 + 파일 비교 | Plan 04 Test 2 |
| **Dim 4** | 배지 텍스트 동적 변경 (D-G4) | 명시 경로 시 = "가로축 (Measurement)" / fallback 시 = "가로축 (Shot fallback)" / 세로축 swap 시 = "세로축" 또는 "세로축 (Measurement)" | 시각 | Plan 04 Test 3 |
| **Dim 5** | 측정값 (mm) byte-identical to Phase 39.2-01 baseline (Anti-Goal TryExecute 변경 0) | 동일 ROI/이미지 입력으로 검사 실행 → resultValue 비교 | 검사 결과 숫자 비교 | Plan 04 회귀 C (Phase 39.3 Verify C 일관) |
| **Dim 6** | INI 호환 (TeachingImagePath_Horizontal 부재 = byte-identical 동작) | (a) 기존 INI 레시피 Load → byte-identical 동작 (b) 신규 INI Save → TeachingImagePath_Horizontal 키 등장 (값 = `""` default 또는 사용자 set 값) | INI diff + Load/Save 라운드트립 | Plan 04 Test 4 (회귀 6) |
| **Dim 7** | Datum DualImage / 기존 7 Measurement / Phase 39.1~39.3 회귀 0 | (a) Side Datum DualImage swap 동작 검증 (Phase 34.1/36/37 baseline) (b) 다른 7 Measurement (EdgeToLineDistance 등) RectROI 드로잉 정상 (c) Phase 39.1 RectROI baseline (d) Phase 39.2 DualImage 알고리즘 baseline (e) Phase 39.3 Verify A/B/D/E 종합 smoke | 시각 + 측정 결과 | Plan 04 Smoke Verify A/B/D/E |
| **Dim 8** | Anti-Goal 10항 검증 (grep + git diff) | (a) DualImageEdgeDistanceMeasurement.TryExecute 본문 변경 0 (git diff) (b) MeasurementFactory.cs 변경 0 (c) DatumConfig.cs 변경 0 (D-G2) (d) Datum DualImage Phase 34.1/36/37 회귀 0 (e) 기존 7 Measurement 변경 0 (f) Phase 39.1/39.2/39.3 회귀 0 (g) INI 직렬화 순서 — TeachingImagePath_Vertical 위치 보존 (h) Phase 39.3 mutex (R1) + 가로축 리셋 (R6) 보존 (i) XAML 파일 변경 0 — 텍스트 동적 set 만 (j) TeachingImagePath_Horizontal default `""` + fallback = 회귀 0 | git diff + grep + INI baseline 비교 | Plan 04 Anti-Goal Checklist |

### Sampling Rate

- **Per task commit:** msbuild 빌드 PASS + 코드 인스펙션 (Anti-Goal 파일 변경 0 확인) — Dim 1 + Dim 8 부분.
- **Per wave merge:** Wave 1 (Plan 01) = Dim 1 + Dim 6 (INI Save/Load 라운드트립), Wave 2 (Plan 02+03) = Dim 1 + Dim 3 부분.
- **Phase gate:** Plan 04 SIMUL UAT 8 Dim 모두 PASS → `/gsd-verify-work`.

### Wave 0 Gaps

- None — 빌드 인프라 + SIMUL_MODE 기존 동작 (Debug 빌드 자동 활성). Test framework 신규 도입 0 (POC 6월 말 기준, Phase 39.4 = UI hotfix 성격).
- Phase 39.3 NOT_TESTED Test 4 (Browse 버튼 시각) + 회귀 A/B/D/E 가 Plan 04 Smoke 에 흡수 (CONTEXT.md Deferred Ideas "회귀 D/E 종합 검증").

---

## Risk Register

### R1 — PropertyTools `[DisplayName]` 미적용 (LOW)

**증상:** `[DisplayName("가로축 티칭 이미지")]` 추가 후 PropertyGrid 셀 헤더가 한글로 안 바뀌고 property 이름 `TeachingImagePath_Horizontal` 그대로 표시.

**예방:**
- RT-1.2 의 11 use site (Action_WaferScanCalibration.cs L56-223) 가 정확히 동일 패턴 (`using PropertyTools.DataAnnotations;` 단일 import + `[DisplayName]` + `[Category]` + 기타 PropertyTools attribute 혼용) 으로 정상 동작 검증됨 → risk 매우 낮음.
- `[InputFilePath]` + `[DisplayName]` + `[Category]` 3 attribute 조합은 본 phase 가 첫 use site → Plan 04 Test 1 에서 시각 검증.

**검출:** Plan 04 Test 1 (PropertyGrid 셀 헤더 한글 노출 확인).

**Fallback (D-G3 fallback 1순위):** `[DisplayName]` 제거 + `[Description]` 만 적용 (tooltip 한글화) + 셀 이름 영문 유지.

### R2 — ParamBase reflection 신규 string field 미픽업 (VERY LOW)

**증상:** `TeachingImagePath_Horizontal` 추가 후 INI Save 시 키 미등장 또는 Load 시 default `""` 유지 안 됨.

**예방:**
- RT-2.1-2.3 의 baseline (Phase 22 IMG-01 + Phase 34 D-34-04) 가 동일 패턴 (string + default `""` + reflection auto-pickup) 으로 검증됨 → risk 거의 0.
- `case "String"` 분기 (ParamBase.cs L337-339 / L385-395) 가 모든 public string property 를 처리.

**검출:** Plan 04 Test 4 (회귀 6) — INI Save/Load 라운드트립 + 신규 키 등장 확인.

**Fallback:** 없음 — 발생 시 ParamBase reflection 자체 회귀 (전체 시스템 영향) → 별도 phase 필요.

### R3 — D-G1 fallback ternary 회귀 (MEDIUM)

**증상:** `Action_FAIMeasurement.TryGrabOrLoadFaiDualImages` L403 ternary 교체 후 기존 INI 레시피 검사 시 `pathA = ""` (Empty Horizontal + ShotConfig fallback) 가 작동 안 함 → "[FAI DualImage] PointROI 이미지 경로 비어 있거나 파일 없음" 에러.

**예방:**
- ternary 의 첫 조건 `IsNullOrEmpty(TeachingImagePath_Horizontal) || !File.Exists(TeachingImagePath_Horizontal)` 이 true 일 때 `ShotParam.SimulImagePath` 로 fallback → 기존 baseline (L403 = `ShotParam.SimulImagePath`) 와 동등.
- Phase 39.2 baseline (D-G1-06) 의 `string.IsNullOrEmpty(pathA) || !File.Exists(pathA)` 가드 (L406) 가 그대로 작동 → fallback 결과로 ShotParam.SimulImagePath 가 set 된 후 동일 가드 통과.

**검출:** Plan 04 Test 2 (D-G1 fallback 동작 검증) + 회귀 C (Dim 5 측정값 byte-identical).

**Fallback:** ternary 교체 대신 명시적 if/else 분기 (가독성 + 명시성):
```csharp
string pathA;
if (string.IsNullOrEmpty(dualMeas.TeachingImagePath_Horizontal) || !File.Exists(dualMeas.TeachingImagePath_Horizontal)) {
    pathA = ShotParam.SimulImagePath;
} else {
    pathA = dualMeas.TeachingImagePath_Horizontal;
}
```

### R4 — 배지 텍스트 동적 set 충돌 (LOW)

**증상:** `UpdateImageSourceBadge` 안에서 텍스트 동적 결정 후 `SetCurrentValue(TextBlock.TextProperty, ...)` 호출이 기존 `BadgeBrushHorizontal/Vertical` 색상 set 와 충돌 → WPF rendering 비동기 → 텍스트만 갱신 + 색상 안 바뀜 또는 반대.

**예방:**
- CO-34.1-03 hotfix 의 `SetCurrentValue` + `InvalidateVisual()` 패턴 (L1300-1309) 이 이미 텍스트 + 색상 동시 set 검증된 패턴 → 동일 분기 안에서 텍스트만 동적 결정으로 교체해도 안전.
- 색상 set 는 `BadgeBrushHorizontal/Vertical` 정적 frozen brush — 충돌 0.

**검출:** Plan 04 Test 3 (D-G4 배지 텍스트 + 색상 동시 시각 확인).

**Fallback:** `InvalidateVisual()` 호출 2회 (텍스트 set 직후 + 색상 set 직후) — 비용 0 cost, 안전 강화.

### R5 — Phase 39.3 NOT_TESTED 회귀 잔존 (MEDIUM)

**증상:** Phase 39.3 partial sign-off 시 NOT_TESTED 였던 Test 4 (Browse 버튼 시각) + 회귀 A (Datum DualImage swap) + 회귀 B (다른 7 Measurement RectROI) + 회귀 D + 회귀 E 가 Phase 39.4 변경에서 회귀 일으킴.

**예방:**
- Plan 04 Smoke Verify A/B/D/E 가 Phase 39.3 NOT_TESTED 모두 흡수 (CONTEXT.md Deferred Ideas).
- Phase 39.3 의 mutex (R1) + 가로축 리셋 (R6) 보존 (Anti-Goal).
- Phase 39.4 의 변경 영역 (D-G1 fallback + D-G3 attribute + D-G4 배지 텍스트) 은 Phase 39.3 의 Datum 경로 / mutex 본체 / `_currentImageSource` 리셋 코드와 직교 → 회귀 surface 0.

**검출:** Plan 04 Smoke (Verify A/B/D/E + Test 4 Browse 양측 시각 확인).

**Fallback:** 회귀 발견 시 git revert Phase 39.4 commit → Phase 39.3 baseline 복귀.

### R6 — Plan 04 Smoke 누락 — 회귀 발견 지연 (LOW)

**증상:** Plan 04 UAT 가 Plan 01~03 코드 변경 직후만 검증 + Smoke 검증 skip → Phase 40 진입 후 회귀 발견.

**예방:** Plan 04 task 분할 시 Smoke 항목 (Verify A/B/D/E + Test 4) 필수 task 로 lock — gsd-plan-phase 시 명시.

**검출:** Phase 39.4 sign-off 후 Phase 40 첫 UAT.

**Fallback:** Phase 39.4 sign-off 가 PARTIAL_SIGNED_OFF 로 종결되어도, Phase 39.3 NOT_TESTED 흡수만으로 valid (사용자 명시 + CONTEXT.md Success Criteria 충족).

### R7 — CS0136 outer-scope 변수 충돌 (LOW)

**증상:** `BtnSwapHorizontal_Click` Measurement 분기 안에 신규 `string hpathMeas = meas.TeachingImagePath_Horizontal;` 추가 시, outer scope 의 Datum 분기 (L1369) `string hpath = d.TeachingImagePath;` 와 변수명 충돌.

**예방:** Phase 39.3 Plan 02 Task 02-04 의 `vpathMeas` rename 패턴 (`hpathMeas` 명명) — 동일 컨벤션 적용. `meas` 변수도 `_selectedDualImageMeasurement` alias 로 동일 패턴.

**검출:** msbuild 빌드 — CS0136 즉시 발견.

**Fallback:** rename (`hpathMeas`, `bMeas`, `fallbackMeas` 등) 또는 별도 helper 메서드 추출.

### R8 — INI 라인 순서 변경으로 인한 baseline 비교 어려움 (LOW)

**증상:** `TeachingImagePath_Horizontal` 신규 추가 시 INI 출력 line 순서 변경 → 기존 INI 와 byte-by-byte diff 가 noisy.

**예방:** RT-2.4 권장 — Vertical 뒤 추가 (절대 위치 보존).

**검출:** Plan 04 Test 4 (회귀 6) — INI Save/Load 라운드트립.

**Fallback:** 신규 키는 INI 끝부분에 추가 — 기존 baseline diff 최소화 (단, declaration order 충돌 시 reflection 순서가 declaration 순서를 정확히 따르지 않을 수 있어 lock 어려움).

---

## Open Questions (RESOLVED)

### Q1 — PropertyTools 의 `[DisplayName]` namespace 확인 → **RESOLVED (RT-1)**

**Question:** PropertyTools.DataAnnotations.DisplayName vs System.ComponentModel.DisplayName — 어느 쪽이 PropertyGrid 셀 헤더에 실제 반영되는지?

**Resolution:**
- PropertyTools 어셈블리 reflection 로드 결과 `PropertyTools.DataAnnotations.DisplayNameAttribute` 존재 확인.
- 코드베이스 내 11 use site (Action_WaferScanCalibration.cs + Action_BottomCalibration.cs) 가 `using PropertyTools.DataAnnotations;` 단일 import + `[DisplayName]` 정상 동작 검증.
- **결론:** PropertyTools.DataAnnotations.DisplayName 사용 (`using PropertyTools.DataAnnotations;` 만 import). Fallback (D-G3 fallback 1순위 `[Description]` 만 적용) = 불필요.

### Q2 — ParamBase reflection 신규 string field auto-pickup 보장 → **RESOLVED (RT-2)**

**Question:** 신규 `TeachingImagePath_Horizontal` (string, default `""`) 가 ParamBase reflection 으로 자동 INI 직렬화되는지 + 기존 INI 레시피 byte-identical 호환 보장?

**Resolution:**
- ParamBase.Save/Load (L324-430) 의 `case "String"` 분기가 reflection auto-pickup → 자동 직렬화.
- Phase 22 IMG-01 (DatumConfig.TeachingImagePath) + Phase 34 D-34-04 (DatumConfig.TeachingImagePath_Vertical) 두 baseline 이 동일 패턴 검증됨.
- 기존 INI 레시피 Load 시 키 미존재 → empty string → default `""` 와 동일 → byte-identical.
- **결론:** Plan 01 의 신규 field 는 attribute 5종만 추가, get/set body + default `""` 보존 → 회귀 0. INI 직렬화 위치 = Vertical 뒤 추가 권장 (lock-in, plan-phase 에서 최종).

### Q3 — Phase 39.3 Plan 02 코드 정확 line 번호 → **RESOLVED (RT-3)**

**Question:** Phase 39.4 변경 대상 (BtnSwapHorizontal_Click Measurement 분기 + UpdateImageSourceBadge + Action_FAIMeasurement L403) 의 정확 line 번호?

**Resolution:**
- MainView.xaml.cs L1346-1377 BtnSwapHorizontal_Click + L1347-1365 Measurement 분기 (commit a8facf9, Phase 39.3 Plan 02 Task 02-04)
- MainView.xaml.cs L1295-1329 UpdateImageSourceBadge + L1323-1328 Measurement publish 분기 (commit 3d9e15e, Task 02-05)
- MainView.xaml.cs L1496-1518 PublishMeasurementDualImageSelection 메서드 (commit 2e28a76, Task 02-01)
- MainView.xaml.cs L1411-1418 PublishDatumRoiCandidates 진입 시 mutex clear (commit ffd139c, Task 02-03)
- Action_FAIMeasurement.cs L391-423 TryGrabOrLoadFaiDualImages + L403 pathA = ShotParam.SimulImagePath (Phase 39.2 D-G1-06)
- **결론:** Phase 39.4 변경 = MainView.xaml.cs (BtnSwapHorizontal_Click Measurement 분기 + UpdateImageSourceBadge 배지 텍스트 분기) + Action_FAIMeasurement.cs L403 ternary + DualImageEdgeDistanceMeasurement.cs L29 신규 field + Vertical attribute 보강.

### Q4 — 배지 텍스트 동적 set 위치 → **RESOLVED (RT-4)**

**Question:** D-G4 배지 텍스트 ("가로축 (Measurement)" / "가로축 (Shot fallback)") 결정 위치 = `UpdateImageSourceBadge` 안 vs `BtnSwap*_Click` 안?

**Resolution:**
- 옵션 A (`UpdateImageSourceBadge` 안) 권장 — single source of truth, Datum 경로 보존 (mutex 가드).
- BadgeBrush 색상 set + InvalidateVisual() 패턴은 변경 0 — 텍스트만 분기 추가.
- Vertical 텍스트 = `"세로축"` 또는 `"세로축 (Measurement)"` (plan-phase task 분할 시 UAT 우선순위로 결정).
- **결론:** Plan 03 task 분할 시 옵션 A 의 텍스트 분기 위치 = L1300-1310 UpdateImageSourceBadge 본체. File.Exists I/O LOW cost (swap 토글 시점만).

---

## Plan Structure (CONTEXT.md 4-plan 구조 검증 → 그대로 유지)

| Plan | Wave | Scope | 변경 파일 | 의존 |
|------|------|-------|----------|------|
| **Plan 01** | Wave 1 | DualImageEdgeDistanceMeasurement 모델 보강 — `TeachingImagePath_Horizontal` 신규 field + 5종 attribute + Vertical 에 `[DisplayName]` 보강 (D-G3) | DualImageEdgeDistanceMeasurement.cs | 독립 |
| **Plan 02** | Wave 2 (Plan 01 의존) | Action_FAIMeasurement.TryGrabOrLoadFaiDualImages L403 ternary 교체 (D-G1 fallback) + Phase 39.3 D-G4 anti-goal 의도적 해제 SUMMARY 명시 | Action_FAIMeasurement.cs | Plan 01 (TeachingImagePath_Horizontal field 존재) |
| **Plan 03** | Wave 2 (병렬, Plan 01 의존) | MainView.BtnSwapHorizontal_Click Measurement 분기 교체 (D-G1 fallback) + UpdateImageSourceBadge 배지 텍스트 동적 분기 (D-G4) + BtnSwapVertical_Click 배지 텍스트 일관성 (선택) | MainView.xaml.cs | Plan 01 (TeachingImagePath_Horizontal field) |
| **Plan 04** | Wave 3 | SIMUL UAT — Test 1 (PropertyGrid Browse + 한글 라벨 D-G3) + Test 2 (Horizontal Measurement + fallback 모드 swap D-G1) + Test 3 (배지 텍스트 D-G4) + Test 4 (INI 회귀 Dim 6) + Smoke Verify A/B/D/E (Phase 39.3 NOT_TESTED 흡수) + 측정값 byte-identical 회귀 C | UAT only (코드 변경 0) | Plan 01/02/03 모두 |

**Wave 구조:**
- Wave 1 = Plan 01 단독 (모델 field 추가)
- Wave 2 = Plan 02 + Plan 03 병렬 (서로 다른 파일, Plan 01 의 field 만 read)
- Wave 3 = Plan 04 (SIMUL UAT)

**총 변경 파일:** 3 + 1 UAT = 3 코드 파일 (DualImageEdgeDistanceMeasurement.cs + Action_FAIMeasurement.cs + MainView.xaml.cs).

---

## Anti-Goals 가드 체크리스트 (Plan-phase 가 각 PLAN.md verification 에 인용)

- [ ] `DualImageEdgeDistanceMeasurement.TryExecute` 본문 변경 0 (Plan 01 = field 추가 + attribute 만)
- [ ] `MeasurementFactory.cs` 변경 0
- [ ] Datum DualImage (Phase 34.1 / 36 / 37) 회귀 0 — DatumConfig.cs 변경 0 (D-G2)
- [ ] 기존 7 Measurement 타입 (Edge 3 + Compound 4 + ArcLineIntersect) 변경 0
- [ ] Phase 39.1 / 39.2 / 39.3 회귀 0 — Plan 04 Smoke Verify A/B/D/E 로 검증
- [ ] INI 직렬화 순서 — `TeachingImagePath_Vertical` 위치 보존, `TeachingImagePath_Horizontal` 신규 추가만 (Vertical 뒤 권장)
- [ ] Phase 39.3 Plan 02 mutex (R1) + Horizontal 리셋 (R6) 보존 — `PublishMeasurementDualImageSelection` / `PublishDatumRoiCandidates` 본체 변경 0
- [ ] 기존 INI 레시피 byte-identical 호환 (TeachingImagePath_Horizontal default `""` + fallback) — Plan 04 Test 4
- [ ] XAML 파일 (MainView.xaml) 변경 0 — toggle button x:Name + Click 핸들러 + Border/TextBlock x:Name 변경 0, 텍스트만 코드 비하인드 동적 set
- [ ] BtnSwapVertical_Click 의 Vertical Measurement 경로 (Phase 39.3 Plan 02 Task 02-04 a8facf9) 변경 0 — D-G1 은 Horizontal 만 fallback, Vertical 은 항상 Measurement 단위 경로

---

## Project Constraints (CLAUDE.md 재인용)

- **Tech stack:** .NET Framework 4.8 + WPF + Halcon 24.11 — 변경 불가
- **Architecture:** SystemHandler 싱글턴 + SequenceBase/ActionBase 패턴 유지 (Phase 39.4 = UI/모델 only, sequence 변경 0)
- **Compatibility:** 기존 INI 레시피 포맷 하위 호환 — `TeachingImagePath_Vertical` property 이름 + ParamBase reflection serializer 보존 (Phase 22 IMG-01 패턴), 신규 `TeachingImagePath_Horizontal` default `""` 로 byte-identical
- **Hardware:** SIMUL_MODE Debug 빌드로 UAT (실 카메라 미요구)
- **C# 7.2:** nullable / switch expression / record 금지 (ternary OK, `as T + null check` OK, `is T x` pattern matching OK)
- **Naming:**
  - 신규 field prefix `_` (newer code) — 본 phase 신규 instance field 0 (모델 property 만 추가)
  - PascalCase public property — `TeachingImagePath_Horizontal`
  - Comment marker `//260530 hbk Phase 39.4 D-GX` 형식 강제 (feedback_comment_convention)
- **Brace style:**
  - MainView.xaml.cs = K&R (예: `if (...) {` 같은 줄, Phase 39.3 Plan 02 동일 스타일)
  - Action_FAIMeasurement.cs = K&R (L391-423 동일 스타일)
  - DualImageEdgeDistanceMeasurement.cs = Allman (Halcon 신규 코드 스타일, namespace + class 본체) — Plan 01 신규 attribute/field 는 Allman 유지
- **Error handling:** Halcon 호출 시 `try/catch (Exception ex) { Logging.PrintErrLog }` 패턴, swap 핸들러 (BtnSwap*_Click L1359/L1372) 사례 그대로

---

## Sources

### Primary (HIGH confidence — 직접 코드/파일 확인)
- `.planning/phases/39.4-bottom-dualimage-manual-swap-2026-05-30/39.4-CONTEXT.md` — 4 lock decisions (D-G1~G4) + Anti-Goal 10항
- `.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-CONTEXT.md` — 39.3 6 lock decisions baseline
- `.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-RESEARCH.md` — 39.3 research targets (특히 RT-1 swap UX inventory + RT-3 PropertyTools 검증)
- `.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-PATTERNS.md` — 39.3 분기 A~J pattern excerpt + 회귀 가드 컨벤션
- `.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-01-SUMMARY.md` — Phase 39.3 Plan 01 commits 0a27dd2 / d3087f4 / 7d02766 / fc3b854
- `.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-02-SUMMARY.md` — Phase 39.3 Plan 02 commits 2e28a76 / 80d04b3 / ffd139c / a8facf9 / 3d9e15e + mutex 양방향 검증
- `.planning/phases/39.3-dualimage-fai-ux-redesign-2026-05-30/39.3-03-SUMMARY.md` — Phase 39.3 Plan 03 commit f260cf0 + `[InputFilePath]` + `[AutoUpdateText]` 적용 패턴
- `.planning/REQUIREMENTS.md` — WF-01 (검사 워크플로우 E2E) + v1.2 우선순위 1
- `.planning/STATE.md` — Phase 39.3 PARTIAL_SIGNED_OFF + Phase 39.4 discuss_complete (commit 8f85eba)
- `WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs` (전체 229 lines) — Plan 01 변경 대상
- `WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs:386-425` — Plan 02 변경 대상 (L403 pathA)
- `WPF_Example/UI/ContentItem/MainView.xaml.cs` L60-80 (필드) + L1295-1518 (swap UX wiring) — Plan 03 변경 대상
- `WPF_Example/UI/ViewModel/ModelFinderViewModel.cs:28-38` — `[InputFilePath]` + `[AutoUpdateText]` 패턴
- `WPF_Example/Custom/Device/DeviceHandler.cs:71-72` — `EXTENSION_IMAGE` / `FILTER_IMAGE` 상수
- `WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs:30-100` — Phase 22 IMG-01 + Phase 34 D-34-04 baseline (reference only, 변경 0)
- `WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs` (전체 104 lines) — MeasurementBase : ParamBase 상속 체인
- `WPF_Example/Sequence/Param/ParamBase.cs:324-430` — Save/Load reflection `case "String"` 본체
- `WPF_Example/Custom/Sequence/Wafer/Action_WaferScanCalibration.cs:1-15, 55-70` — `[DisplayName]` in-repo use site (11 hit)

### Secondary (HIGH confidence — 어셈블리 reflection)
- `packages/PropertyTools.3.1.0/lib/net45/PropertyTools.dll` — `[PropertyTools.DataAnnotations.DisplayNameAttribute]` reflection 로드 확인 (PowerShell `Add-Type` + `[type].FullName`)

### Tertiary (검증 미필요)
- 없음 — 모든 claim 은 직접 코드 인스펙션 또는 어셈블리 reflection 으로 검증됨.

---

## Metadata

**Confidence breakdown:**
- RT-1 PropertyTools `[DisplayName]`: HIGH — 어셈블리 reflection + 11 in-repo use site 검증
- RT-2 ParamBase reflection: HIGH — Phase 22/34 baseline 직접 인용 + `case "String"` 코드 직접 확인
- RT-3 Phase 39.3 line 번호: HIGH — MainView.xaml.cs / Action_FAIMeasurement.cs 직접 인스펙션 + 39.3-02/03 SUMMARY commit 매핑
- RT-4 배지 텍스트 동적 set: HIGH — UpdateImageSourceBadge 본체 직접 인용 + CO-34.1-03 hotfix 패턴 확인

**Research date:** 2026-05-30
**Valid until:** 2026-06-29 (30일 — Phase 39.4 의 모든 코드 진입점은 Phase 39.3 sign-off 직후 stable, Phase 40+ 의존성 0)

## RESEARCH COMPLETE
