---
phase: 39.4-bottom-dualimage-manual-swap-2026-05-30
type: uat
status: partial
source:
  - 39.4-04-PLAN.md
started: 2026-05-30
updated: 2026-05-31
date: 2026-05-30
auto_checks:
  msbuild_debug_x64: pass
  msbuild_errors: 0
  msbuild_warnings_total: 7
  msbuild_warnings_baseline: 7   # Phase 39.2 baseline (MSB3884 x1 + CS0618 x5 + CS0162 x1)
  msbuild_new_warnings: 0
  anti_goal_pass: 10
  anti_goal_fail: 0
user_tests:
  total: 8
  pass: 5
  fail: 0
  partial: 0
  pending: 0
  skipped: 3   # Verify B/D/E 사용자 합의 이월 (carry-over CO-39.4-02)
  ini_compat: skipped   # 사용자 합의 이월
phase_commits_range:
  first: 5cf4fb3   # Plan 01 Task 01-01 — DualImageEdgeDistanceMeasurement TeachingImagePath_Horizontal
  last: 6843c0d    # CO-39.4-01 hotfix — UpdateImageSourceBadge swap 결과 덮어쓰기 회귀
covers:
  - "D-G1 — TeachingImagePath_Horizontal 신규 필드 + fallback 정책 — Plan 01/02/03"
  - "D-G3 — PropertyGrid 한글 라벨 [DisplayName] — Plan 01"
  - "D-G4 — 배지 텍스트 동적 (Measurement / Shot fallback) — Plan 03"
  - "CO-39.3-01 — DualImage Measurement 가로축 Shot 이미지 점유 혼동 해소"
hotfixes:
  - id: CO-39.4-01
    commit: 6843c0d
    date: 2026-05-31
    found_in: "UAT Test 2/3 사용자 1차 보고 — swap 클릭 시 배지/색상은 변경되나 이미지 뷰어 미반영"
    root_cause: "UpdateImageSourceBadge L1334-1335 의 RenderInspectionResultForNode(meas) 호출이 DisplayMeasurementImage → DisplayShotImage 로 swap 직후 명시 이미지를 Shot 이미지로 덮어씌움. Phase 39.3 baseline 에서는 swap 도 Shot 이미지를 로드 → 가시 회귀 0 으로 잠복."
    fix: "L1334-1335 단일 호출을 HighlightSelectedRoi + RenderStoredOverlaysForMeasurement + UpdateFaiCirclePreview 3개로 교체 (DisplayMeasurementImage 제외). 초기 노드 선택 시 full render 는 InspectionListView L522 가 담당."
    retest: "Test 2/3 재테스트 필요"
carry_overs:
  - id: CO-39.4-02
    title: "Verify B/D/E + INI 호환 회귀 smoke — Phase 39.4 사용자 합의 이월"
    rationale: "Phase 39.4 핵심 가치 (D-G1/G3/G4 + CO-39.3-01 해소) 는 Test 1~4 + Verify A 로 검증 완료. 나머지 회귀 smoke 4 항목은 사용자 합의로 후속 phase 에 일괄 처리 (Anti-Goal 자동 검증 10/10 PASS 로 회귀 위험 자체는 낮음)."
    items:
      - "Verify B — 기존 7 Measurement (Edge 3 + Compound 4 + ArcLineIntersect) RectROI 드로잉 회귀 smoke"
      - "Verify D — Phase 39.1 4 smoke (CircleDiameter / EdgeToLineDistance projection_pl / FAI 개별 검사 / Datum CircleTwoHorizontal Edit)"
      - "Verify E — Phase 39.2 3 smoke (I10 close-point variant / Tree Move ▲▼ / Tree 18 Geometry 아이콘)"
      - "INI 호환 byte-identical — Phase 22 baseline 레시피 Load/Save 라운드트립"
---

# Phase 39.4 UAT — Bottom DualImage 수동 swap UX 재설계

> **Sign-off: PARTIAL_SIGNED_OFF (2026-05-31).** Task 04-01 자동 검증 PASS (msbuild + Anti-Goal 10/10) + 사용자 UAT Test 1~4 + Verify A 5/5 PASS (1 hotfix CO-39.4-01 적용).
> Plan 01 (TeachingImagePath_Horizontal 신규 필드) + Plan 02 (pathA D-G1 fallback if/else) + Plan 03 (swap UX D-G1 + D-G4 배지) 통합 동작 검증 완료.
> Verify B/D/E + INI 호환 4 항목은 사용자 합의로 후속 phase 이월 (CO-39.4-02).

## Results Summary (2026-05-31 — 사용자 UAT 완료, PARTIAL_SIGNED_OFF)

| Total | Passed | Issues | Skipped (carry-over) |
|-------|--------|--------|---------|
| 9 | 5 | 0 | 4 |

| Test | 항목 | 결과 | 비고 |
|---|---|---|---|
| 1 | PropertyGrid 양측 Browse + 한글 라벨 (D-G3, Plan 01) | ✅ PASS | 한글 라벨 + Browse 양측 정상 |
| 2 | TeachingImagePath_Horizontal 명시 시 swap (D-G1+G4, Plan 03) | ✅ PASS | CO-39.4-01 hotfix 후 PASS (1차 보고 시 배지만 변경, 이미지 미반영 → fix 완료) |
| 3 | TeachingImagePath_Horizontal 부재 시 swap fallback (D-G1+G4, Plan 03) | ✅ PASS | "가로축 (Shot fallback)" 배지 + Shot 이미지 fallback 정상 |
| 4 | 검사 실행 RuntimeImageA 우선/fallback (D-G1, Plan 02) | ✅ PASS | 사용자 단순 보고 "pass" |
| Verify A | Side Datum DualImage swap 회귀 (Phase 34.1/36/37) | ✅ PASS | 사용자 단순 보고 "pass" — Datum 경로 mutex 회귀 0 |
| Verify B | 기존 7 Measurement RectROI 드로잉 회귀 | ⏭ SKIPPED | CO-39.4-02 — 후속 phase 이월 (사용자 합의) |
| Verify D | Phase 39.1 4 smoke test | ⏭ SKIPPED | CO-39.4-02 — 후속 phase 이월 |
| Verify E | Phase 39.2 3 smoke test | ⏭ SKIPPED | CO-39.4-02 — 후속 phase 이월 |
| INI 호환 | INI byte-identical (TeachingImagePath_Horizontal 부재 = 기존 레시피 호환) | ⏭ SKIPPED | CO-39.4-02 — 후속 phase 이월 |

## Auto Checks (Task 04-01 자동 검증)

| Check | Status | Detail |
|---|---|---|
| msbuild Debug/x64 rebuild | PASS | 0 errors. 7 warnings (MSB3884 x1 + CS0618 x5 + CS0162 x1) = Phase 39.2 baseline 과 동일, 신규 warning 0. |
| Anti-Goal 10/10 자동 검증 | PASS | 하단 Anti-Goal 체크리스트 전체 ✅ (grep + git diff 8f85eba). |
| 39.4 phase commits range | OK | 5cf4fb3 (Plan 01) ~ 85d8c92 (Plan 03) — Plan 01/02/03 모두 완료. |

## Anti-Goal 체크리스트 (Task 04-01 자동 검증, 10/10 ✅)

검증 범위 = `git diff 8f85eba..HEAD`. baseline = `8f85eba` (Phase 39.4 discuss complete 이전 = Phase 39.3 + 시드 마지막).

| # | 가드 항목 | 결과 | 근거 |
|---|---|---|---|
| 1 | `DualImageEdgeDistanceMeasurement.TryExecute` 본문 변경 0 (field + attribute 추가만) | ✅ | `git diff 8f85eba -- DualImageEdgeDistanceMeasurement.cs` 결과: +[DisplayName("세로축")] 1줄 + Horizontal 신규 7줄 (attribute 4 + property 1 + comments 2). hunk 범위 = L24-38 (property 블록). TryExecute 본문 (L60+) hunk 0. |
| 2 | `MeasurementFactory.cs` 변경 0 | ✅ | `git diff 8f85eba -- MeasurementFactory.cs` → 빈 출력 (변경 0). |
| 3 | Datum DualImage 회귀 0 — `DatumConfig.cs` 변경 0 (D-G2) | ✅ | `git diff 8f85eba -- DatumConfig.cs` → 빈 출력 (변경 0). `git diff 8f85eba --name-only` 에 DatumConfig.cs 없음. |
| 4 | 기존 7 Measurement 타입 (Edge 3 + Compound 4 + ArcLineIntersect) 변경 0 | ✅ | `git diff 8f85eba --name-only -- WPF_Example/Custom/Sequence/Inspection/Measurements/` 결과 = `DualImageEdgeDistanceMeasurement.cs` 단일. EdgeToLine*/ArcEdge*/Compound*/ArcLineIntersect* 변경 0. |
| 5 | Phase 39.1 / 39.2 / 39.3 회귀 0 | ✅ | `git diff 8f85eba --name-only` 결과 = DualImageEdgeDistanceMeasurement.cs + Action_FAIMeasurement.cs + MainView.xaml.cs + planning 문서 4종. Phase 39.1~39.3 영향 파일 변경 0. UAT Verify D/E 에서 시각 smoke 검증 예정. |
| 6 | INI 직렬화 순서 — `TeachingImagePath_Vertical` 위치 보존, `TeachingImagePath_Horizontal` Vertical 뒤 추가 | ✅ | DualImageEdgeDistanceMeasurement.cs: `TeachingImagePath_Vertical` = L30 선언. `TeachingImagePath_Horizontal` = L38 선언 (Vertical 뒤). `ParamBase.cs` 변경 0 (`git diff 8f85eba -- ParamBase.cs` 빈 출력) — reflection 자동 픽업. |
| 7 | Phase 39.3 Plan 02 mutex (R1) + Horizontal 리셋 (R6) 보존 | ✅ | `_selectedDualImageMeasurement = null` L1434 (PublishDatumRoiCandidates) 보존. `_selectedDatumForSwap = null` L1521 (PublishMeasurementDualImageSelection) 보존. `_currentImageSource = EImageSource.Horizontal` L1460 + L1532 (PublishMeasurementDualImageSelection 안) 보존. |
| 8 | 기존 INI 레시피 byte-identical 호환 (`TeachingImagePath_Horizontal` default `""` + D-G1 fallback 보장) | ✅ | `ParamBase.cs` 변경 0 (reflection 자동 픽업). 신규 필드 default `""` → 기존 INI (Horizontal 부재) Load 시 자동 fallback, 동작 byte-identical. UAT Test 4 + INI 호환 항목에서 시각 검증 예정. |
| 9 | XAML 파일 (`MainView.xaml`) 변경 0 | ✅ | `git diff 8f85eba -- WPF_Example/UI/ContentItem/MainView.xaml` → 빈 출력 (변경 0). toggle button x:Name + Click 핸들러 + Border/TextBlock x:Name 모두 보존. 텍스트만 코드 비하인드에서 동적 set (D-G4). |
| 10 | `BtnSwapVertical_Click` Vertical Measurement 경로 변경 0 — D-G1 = Horizontal 만 fallback | ✅ | `git diff 8f85eba -- MainView.xaml.cs` hunk 범위 = L1295 (UpdateImageSourceBadge Horizontal 분기) + L1348 (BtnSwapHorizontal_Click Measurement 분기). `BtnSwapVertical_Click` = L1398 → diff hunk 에 포함되지 않음 (변경 0). Vertical = 항상 Measurement 단위 경로 보존. |

---

## User Tests (사용자 SIMUL UAT — 4 시나리오 + Verify A/B/D/E + INI 호환)

**앱 실행:** `bin\x64\Debug\DatumMeasurement.exe` (Debug 빌드 = `SIMUL_MODE` 자동 활성)

---

### Test 1 — PropertyGrid 양측 Browse + 한글 라벨 (D-G3, Plan 01)

**Goal:** DualImage Measurement PropertyGrid 에 "가로축 티칭 이미지" / "세로축 티칭 이미지" 셀 양측 Browse 버튼 + 한글 라벨 노출 확인.

**Setup:**
- Bottom 시퀀스 → E5 Shot → DualImage FAI Measurement 노드 클릭 → PropertyGrid 의 `Image|DualImage` 카테고리 펼침.

**확인 5 항목:**
- (a) "세로축 티칭 이미지" 라벨 셀 노출 + `[Browse...]` 버튼.
- (b) "가로축 티칭 이미지" 라벨 셀 노출 + `[Browse...]` 버튼.
- (c) "가로축 티칭 이미지" `[Browse...]` 클릭 → OpenFileDialog 표시 + 필터 = `tiff/bmp/jpg/jpeg/png/All`.
- (d) 임의 이미지 파일 선택 → OK → 셀에 경로 표시 (TeachingImagePath_Horizontal 반영).
- (e) "세로축 티칭 이미지" 도 동일 동작 (c/d 반복 — TeachingImagePath_Vertical 반영).

**Expected:** 5 항목 ALL OK = D-G3 충족 (Phase 39.3 Test 4 NOT_TESTED 흡수).

**Fallback (R1 — 한글 라벨 미표시 시):**
- (a) `[DisplayName]` 제거 + `[Description]` 만 적용 (tooltip 한글화, 셀명 영문 유지) → 재테스트.
- (b) PropertyTools attribute 처리 순서 조정 (`[InputFilePath]` 앞으로 교체) → 재테스트.

**Result:** ✅ **PASS** (2026-05-31) — 사용자 보고 "test 1,2 pass". 한글 라벨 + Browse 양측 정상.

> **Hotfix history (CO-39.4-01, 2026-05-31, commit 6843c0d):** Test 2 1차 보고 시 swap 클릭 → 배지 텍스트/색상은 정상 변경 but 캔버스 이미지 미반영. 원인 = UpdateImageSourceBadge L1334-1335 의 RenderInspectionResultForNode 가 swap 직후 Shot 이미지로 덮어씌움 (Plan 03 Anti-Goal 누락 — Phase 39.3 baseline 에서는 swap 도 Shot 이미지 로드라 잠복). Fix: L1334-1335 호출 분해 — DisplayMeasurementImage 제외, HighlightSelectedRoi + RenderStoredOverlaysForMeasurement + UpdateFaiCirclePreview 만 호출. Test 2 재테스트 PASS.

---

### Test 2 — TeachingImagePath_Horizontal 명시 시 swap (D-G1 + D-G4, Plan 03)

**Goal:** TeachingImagePath_Horizontal 에 별도 이미지 명시 후 가로축 swap → Measurement 명시 이미지 로드 + 배지 "가로축 (Measurement)" 확인.

**Setup:** Test 1 후 상태. TeachingImagePath_Horizontal 에 유효한 이미지 파일 set (Browse 로 선택).

**Action 1:** `btn_swapHorizontal` 클릭.

**확인 4 항목:**
- (a) 캔버스 이미지 = TeachingImagePath_Horizontal 명시 이미지 (ShotConfig 이미지가 아님).
- (b) 배지 텍스트 = `"가로축 (Measurement)"`.
- (c) 배지 색상 = BadgeBrushHorizontal (파란색 보존).
- (d) `btn_swapHorizontal.IsChecked = true`.

**Expected:** 4 항목 ALL OK.

**Fallback (R3/R4):**
- (a) if 조건 (`!IsNullOrEmpty && File.Exists`) 추적 — BtnSwapHorizontal_Click 의 `hpathMeas` 분기 진입 확인.
- (b) UpdateImageSourceBadge 안의 File.Exists I/O 를 BtnSwap*_Click 안으로 이동 (옵션 B).

**Result:** ✅ **PASS** (2026-05-31, CO-39.4-01 hotfix 후) — 1차 보고 시 swap 클릭 → 배지 정상 변경 but 캔버스 이미지 미반영. 원인 추적 → CO-39.4-01 hotfix (commit `6843c0d`) 적용 → 재테스트 PASS.

---

### Test 3 — TeachingImagePath_Horizontal 부재 시 swap fallback (D-G1 + D-G4, Plan 03)

**Goal:** TeachingImagePath_Horizontal 빈 문자열/파일 부재 시 → ShotConfig fallback + 배지 "가로축 (Shot fallback)" 확인.

**Setup:** Test 2 후 PropertyGrid 에서 TeachingImagePath_Horizontal 셀을 빈 문자열로 clear (또는 INI 직접 편집 후 재로드).

**Action 1:** `btn_swapHorizontal` 클릭 (또는 노드 재선택 → 가로축으로 자동 reset).

**확인 4 항목:**
- (a) 캔버스 이미지 = ShotConfig.GetImage() (FAI 공통 Shot 이미지 — Phase 39.3 baseline).
- (b) 배지 텍스트 = `"가로축 (Shot fallback)"`.
- (c) 배지 색상 = BadgeBrushHorizontal (파란색 보존).
- (d) `btn_swapHorizontal.IsChecked = true`.

**Expected:** 4 항목 ALL OK = D-G1 fallback 정책 동작 확인.

**Fallback (R3):**
- (a) UpdateImageSourceBadge 분기 추적 — `_selectedDualImageMeasurement` null 여부 + `hasExplicitHorizontal = false` 경로 확인.

**Result:** ✅ **PASS** (2026-05-31) — 사용자 보고 "test 3 가로이미지 경로 없애고 샷이미지가 가로에 나옴" + 배지 = "가로축 (Shot fallback)" 확인. D-G1 fallback 정책 의도 동작.

---

### Test 4 — 검사 실행 RuntimeImageA 우선/fallback + 측정값 byte-identical (D-G1, Plan 02 + 회귀 C)

**Goal:** 검사 실행 시 RuntimeImageA = TeachingImagePath_Horizontal 우선 / fallback = ShotConfig 확인 + 측정값 byte-identical (Phase 39.2-01 baseline).

**Setup A (Horizontal 명시):**
- Test 2 의 TeachingImagePath_Horizontal 명시 + Phase 39.2-01 baseline 과 다른 이미지로 검사 실행.
- 측정값 (mm) 확인 — 명시 이미지 기준 값 (ShotConfig baseline 과 달라야 함).

**Setup B (Horizontal 부재):**
- Test 3 의 빈 상태에서 동일 ROI/Shot 이미지로 검사 실행 → Phase 39.2-01 baseline 측정값과 비교.

**확인 3 항목:**
- (a) Setup A: RuntimeImageA = TeachingImagePath_Horizontal 이미지 사용됨 (측정값 = 명시 이미지 기준).
- (b) Setup B: 측정값 (mm) = Phase 39.2-01 baseline 과 byte-identical (회귀 C — Phase 39.3 Verify C 동일).
- (c) PointROI 측정값 + LineROI 측정값 양쪽 정상 출력.

**Expected:** 3 항목 ALL OK.

**Fallback (R2):**
- (a) `ParamBase.Load` 시 default `""` 보장 확인 (Case "String" 분기).
- (b) `Action_FAIMeasurement L406` 가드 통과 추적 (ELogType.Error 로그 확인).

**Result:** ✅ **PASS** (2026-05-31) — 사용자 단순 보고 "pass". 검사 시점 RuntimeImageA 분기 + 측정값 회귀 0 확인 (Setup A/B 사용자 시각 검증).

---

### Verify A — Side Datum DualImage swap 회귀 (Phase 34.1/36/37)

**Goal:** Phase 39.4 변경 (BtnSwapHorizontal_Click 교체 + UpdateImageSourceBadge 배지 분기) 이 Datum DualImage 경로에 회귀 0 확인.

**Setup:** Side 시퀀스 → VerticalTwoHorizontalDualImage Datum (예: 3-1 Datum_A1_A2_backlight) 1개 선택.

**확인 5 항목:**
- (a) `btn_swapHorizontal` / `btn_swapVertical` 동작 (Datum 측 경로 — Phase 34.1 baseline).
- (b) 배지 텍스트 = `"가로축"` / `"세로축"` (Datum 분기 → 기존 텍스트 보존, `"가로축 (Measurement)"` 같은 D-G4 텍스트 등장 X).
- (c) 배지 색상 = BadgeBrushHorizontal / Vertical 보존.
- (d) "Test Find Datum" 실행 → 정상 결과.
- (e) Datum 노드 → Measurement DualImage 노드 → Datum 노드 전환 시 mutex 정상 (배지 텍스트 `"가로축 (Measurement)"` ↔ `"가로축"` 정확히 토글).

**Expected:** 5 항목 ALL OK (Phase 34.1/36/37 baseline 회귀 0).

**Result:** ✅ **PASS** (2026-05-31) — 사용자 단순 보고 "pass". Datum 경로 mutex 회귀 0 + 배지 "가로축"/"세로축" 보존.

---

### Verify B — 기존 7 Measurement RectROI 드로잉 회귀

**Goal:** 기존 7 Measurement 타입 UI 분기 변경 0 확인.

**확인 (각 타입 1 노드씩 클릭 후 RectROI 드로잉 1회):**
- (a) EdgeToLineDistance — `btn_rectRoi` 활성 + ROI 드로잉 → 측정값 출력.
- (b) EdgeToLineAngle — 동일.
- (c) ArcEdgeDistance — 동일.
- (d) CompoundAngle — 동일.
- (e) CompoundCenterCDistance — 동일.
- (f) CompoundCenterBDistance — 동일.
- (g) CompoundShortAxisDistance — 동일.
- (h) ArcLineIntersect — 동일.

**Expected:** 8 항목 ALL OK (Phase 39.1~39.3 baseline 회귀 0).

**Result:** ⏭ **SKIPPED — CO-39.4-02 carry-over** (2026-05-31, 사용자 합의 후속 phase 이월). 회귀 위험 = LOW (Anti-Goal #4 자동 검증 PASS — 7 Measurement 타입 파일 변경 0).

---

### Verify D — Phase 39.1 4 smoke test

**Goal:** Phase 39.1 (CircleDiameter / EdgeToLineDistance projection_pl / FAI 개별 검사 / Datum CircleTwoHorizontal Edit 모드) 회귀 0 확인.

**확인 4 항목:**
- (1) CircleDiameter — Bottom CircleDiameter Measurement 1개 클릭 → PropertyGrid 파라미터 (Radius/Center) 정상 표시.
- (2) EdgeToLineDistance projection_pl — EdgeToLineDistance 1개 클릭 → SIMUL 검사 1회 실행 → 측정값 표시.
- (3) FAI 개별 검사 — FAI 노드 우클릭 메뉴 또는 토글 → 개별 검사 동작 확인.
- (4) Datum CircleTwoHorizontal Edit 모드 — Side CircleTwoHorizontal Datum 1개 선택 → Edit 모드 진입 → ROI 표시 정상.

**Expected:** 4 항목 ALL OK (Phase 39.1 baseline 회귀 0).

**Result:** ⏭ **SKIPPED — CO-39.4-02 carry-over** (2026-05-31, 사용자 합의 후속 phase 이월).

---

### Verify E — Phase 39.2 3 smoke test

**Goal:** Phase 39.2 (I10 close-point variant / Tree Move ▲▼ / Tree 18 Geometry 아이콘) 회귀 0 확인.

**확인 3 항목:**
- (1) I10 close-point variant — Bottom I10 close-point variant Measurement 1개로 SIMUL 검사 1 시퀀스 실행 → 측정값 정상.
- (2) Tree Move ▲▼ — InspectionListView 트리에서 임의 노드 → Move ▲ 또는 Move ▼ 1회 → 노드 순서 변경.
- (3) Tree 18 Geometry 아이콘 — 트리 18 Geometry 아이콘 시각 확인 (각 Measurement 타입별 아이콘 정상).

**Expected:** 3 항목 ALL OK (Phase 39.2 baseline 회귀 0).

**Result:** ⏭ **SKIPPED — CO-39.4-02 carry-over** (2026-05-31, 사용자 합의 후속 phase 이월).

---

### INI 호환 — byte-identical (Phase 22 baseline Load/Save 라운드트립)

**Goal:** 기존 INI 레시피 (TeachingImagePath_Horizontal 부재) 를 앱에서 Load/Save 시 byte-identical 확인 (D-G1 fallback 보장 + Anti-Goal #6 INI 순서 보존).

**확인 4 항목:**
- (a) Load 시 TeachingImagePath_Horizontal = `""` (default 적용, 에러 0).
- (b) Save 후 INI 안에 `TeachingImagePath_Horizontal=` 신규 키 등장 (값 = `""` 또는 사용자 설정 경로).
- (c) TeachingImagePath_Vertical 라인 위치 보존 (Anti-Goal #6 INI 순서).
- (d) 다른 모든 키 byte-identical.

**Expected:** 4 항목 ALL OK.

**Result:** ⏭ **SKIPPED — CO-39.4-02 carry-over** (2026-05-31, 사용자 합의 후속 phase 이월). 회귀 위험 = LOW (Anti-Goal #8 자동 검증 PASS — ParamBase.cs 변경 0, reflection 자동 픽업).

---

## Risk Fallback 메모 (단계 D)

### Test 1 FAIL — Browse 버튼 미표시 / 한글 라벨 미표시 (RESEARCH RT-1.4)

PropertyGrid 셀 헤더가 영문 (`TeachingImagePath_Horizontal`) 그대로 표시되거나 `[Browse...]` 버튼 미노출 시:

- **(a)** `[DisplayName]` 제거 + `[Description]` 만 적용 (tooltip 한글화, 셀명 영문 유지). attribute 2개 제거 → 재빌드 → Test 1 재수행.
- **(b)** PropertyTools attribute 처리 순서 조정 — `[InputFilePath]` 가 `[DisplayName]` 보다 앞에 오도록 reorder → 재빌드 → Test 1 재수행.

Note: RT-1.2 에서 `Action_WaferScanCalibration.cs` L56-223 의 11 use site 가 동일 패턴으로 정상 동작 검증됨 → risk 낮음.

### Test 2/3 FAIL — swap UX 배지 텍스트 오류 / 캔버스 이미지 미변경 (RESEARCH R3/R4)

- **(a)** Plan 03 if/else 변수 scope 충돌 (CS0136) 확인 → `hpathMeas` 변수명 재명명 → 재빌드.
- **(b)** `UpdateImageSourceBadge` 안의 `File.Exists` I/O 를 `BtnSwapHorizontal_Click` 안으로 이동 (옵션 B — RT-4 fallback) → 재설계 + 재빌드.

### Test 4 FAIL — 측정값 mismatch (RESEARCH R2)

Setup B (Horizontal 부재) 측정값이 Phase 39.2-01 baseline 과 다를 경우:

- **(a)** `ParamBase.cs` reflection Load 에서 Case `"String"` 분기 — `TeachingImagePath_Horizontal` 가 `""` 로 load 되는지 추적.
- **(b)** `Action_FAIMeasurement L406` 가드 통과 추적 — `ELogType.Error` 로그 `"[FAI DualImage] pathA 파일 없음"` 등장 시 fallback 경로 진입 미확인.

### Verify A~E 중 일부 FAIL

Phase 39.4 PARTIAL_SIGNED_OFF 처리 → 신규 carry-over phase 합의.

---

## Carry-over

### CO-39.4-02 — Verify B/D/E + INI 호환 회귀 smoke (사용자 합의 후속 phase 이월)

**Rationale:** Phase 39.4 핵심 가치 (D-G1/G3/G4 + CO-39.3-01 해소) 는 Test 1~4 + Verify A 5/5 PASS 로 검증 완료. 나머지 4 항목 은 회귀 위험 자체가 낮으므로 (Anti-Goal 자동 검증 10/10 PASS) 후속 phase 에 일괄 처리 합의 (2026-05-31).

**이월 항목:**
- Verify B — 기존 7 Measurement RectROI 드로잉 회귀 smoke (회귀 위험 LOW — Anti-Goal #4 자동 검증 PASS, 7 Measurement 타입 파일 변경 0)
- Verify D — Phase 39.1 4 smoke (CircleDiameter / EdgeToLineDistance projection_pl / FAI 개별 검사 / Datum CircleTwoHorizontal Edit)
- Verify E — Phase 39.2 3 smoke (I10 close-point variant / Tree Move ▲▼ / Tree 18 Geometry 아이콘)
- INI 호환 byte-identical — Phase 22 baseline Load/Save 라운드트립 (회귀 위험 LOW — Anti-Goal #8 자동 검증 PASS, ParamBase.cs 변경 0)

**Resolution:** 차후 phase (예: Phase 39.5 또는 v1.2 carry-over cleanup phase) 에서 SIMUL UAT 또는 통합 회귀 swoke 로 일괄 처리.

---

## Sign-off (2026-05-31 — PARTIAL_SIGNED_OFF)

- [x] **Test 1 — D-G3 PropertyGrid Browse + 한글 라벨:** ✅ PASS
- [x] **Test 2 — D-G1+G4 Horizontal 명시 swap:** ✅ PASS (CO-39.4-01 hotfix 후)
- [x] **Test 3 — D-G1+G4 Horizontal fallback swap:** ✅ PASS
- [x] **Test 4 — D-G1 검사 RuntimeImageA + 측정값 byte-identical:** ✅ PASS
- [x] **Verify A — Datum DualImage 회귀 (Phase 34.1/36/37):** ✅ PASS
- [ ] **Verify B — 기존 7 Measurement RectROI 회귀:** ⏭ SKIPPED → CO-39.4-02
- [ ] **Verify D — Phase 39.1 4 smoke:** ⏭ SKIPPED → CO-39.4-02
- [ ] **Verify E — Phase 39.2 3 smoke:** ⏭ SKIPPED → CO-39.4-02
- [ ] **INI 호환 byte-identical:** ⏭ SKIPPED → CO-39.4-02

**Sign-off:** PARTIAL_SIGNED_OFF (Test 1~4 + Verify A 5/5 PASS · Verify B/D/E + INI = CO-39.4-02 이월)
**Date:** 2026-05-31
**Hotfix commits:** `6843c0d` (CO-39.4-01 — UpdateImageSourceBadge swap 결과 Shot 이미지 덮어쓰기 회귀)
**Carry-over:** CO-39.4-02 (Verify B/D/E + INI 호환 smoke 후속 phase 이월)

## Notes

- 모든 검증은 사용자 SIMUL_MODE 화면 시각/동작 확인 한정. 자동 픽셀 비교 도구 없음.
- Plan 01 (Wave 1) + Plan 02/03 (Wave 2 sequential) → 모두 Wave 3 진입 전 완료. Wave 3 = Plan 04 단독.
- Anti-Goal #3 (Datum 코드 변경 0) — D-G2 deferral 유지. Datum DualImage 일관화는 별도 후속 phase 후보.
- INI 호환 total 에 포함 (별도 보충 항목 → user_tests.total = 9, pass = 5, skipped = 4 [CO-39.4-02]).
- CO-39.4-01 hotfix: Plan 03 의 Anti-Goal 누락 (UpdateImageSourceBadge L1334-1335 의 RenderInspectionResultForNode 가 swap 직후 Shot 이미지 덮어씌움). Phase 39.3 baseline 에서는 swap 도 Shot 이미지를 로드라서 잠복. Test 2 1차 사용자 보고로 발견 → 즉시 fix 적용 → 재테스트 PASS.
