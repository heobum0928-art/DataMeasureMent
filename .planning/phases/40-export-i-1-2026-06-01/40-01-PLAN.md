---
phase: 40-export-i-1-2026-06-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ViewModel/CycleResultDto.cs
  - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [OUT-01, OUT-02]
must_haves:
  truths:
    - "검사 cycle 완료 시 ./Result/{YYYYMMDD}/{HHmmss}_cycle/cycle.json 파일이 생성된다"
    - "cycle.json 에 측정값(mm) + 판정 + nominal/tol + overlay 기하 + 이미지 경로 + 메타(일시/레시피명/종합판정)가 모두 포함된다"
    - "JsonConvert.SerializeObject(dto) → DeserializeObject<CycleResultDto>(json) round-trip 이 데이터 손실 없이 동작한다"
    - "직렬화 예외가 발생해도 TCP 응답(ResponseQueue.Enqueue)은 계속 진행된다"
  artifacts:
    - path: "WPF_Example/UI/ViewModel/CycleResultDto.cs"
      provides: "cycle 결과 JSON 직렬화 DTO 5종 (CycleResultDto/ShotResultDto/FaiResultDto/MeasurementResultDto)"
      contains: "public class CycleResultDto"
    - path: "WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs"
      provides: "BuildDto + SaveAsync + Load static 서비스 (날짜폴더 저장 + 예외격리)"
      contains: "public static void SaveAsync"
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "AddResponse() cycle 완료 wiring (CycleResultSerializer 호출)"
      contains: "CycleResultSerializer"
  key_links:
    - from: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      to: "CycleResultSerializer.SaveAsync"
      via: "AddResponse() responsePacket.Result 확정 직후, ResponseQueue.Enqueue 직전"
      pattern: "CycleResultSerializer\\.(BuildDto|SaveAsync)"
    - from: "WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs"
      to: "SystemSetting.ResultSavePath"
      via: "Path.Combine(ResultSavePath, yyyyMMdd, HHmmss_cycle)"
      pattern: "ResultSavePath"
---

<objective>
검사 결과 영속화 토대를 구축한다. cycle 단위 구조화 JSON DTO 를 정의하고, 검사 완료 시점(`InspectionSequence.AddResponse`)에서 전 Shot/FAI/Measurement 결과 + overlay 기하 + 메타를 모아 `./Result/{YYYYMMDD}/{HHmmss}_cycle/cycle.json` 로 직렬화 저장한다.

Purpose: 이 영속화 계층은 OUT-01(리뷰어 재렌더) 과 OUT-02(xlsx export) 의 **공통 단일 소스** 다 (D-01). 리뷰어와 xlsx 가 동일 JSON 을 소비한다.
Output: CycleResultDto.cs (신규), CycleResultSerializer.cs (신규), InspectionSequence.cs 수정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-export-i-1-2026-06-01/40-CONTEXT.md
@.planning/phases/40-export-i-1-2026-06-01/40-RESEARCH.md
@.planning/phases/40-export-i-1-2026-06-01/40-PATTERNS.md

<interfaces>
<!-- 직렬화 대상 필드 (PATTERNS.md / RESEARCH.md 에서 확인됨) -->

From WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs:22-48 (모든 필드 CLR 타입 → JSON 안전):
```csharp
public class EdgeInspectionOverlay {
    public string RoiId { get; set; }
    public List<EdgeInspectionPoint> Points { get; set; }
    public double LineRow1 { get; set; }
    public double LineColumn1 { get; set; }
    public double LineRow2 { get; set; }
    public double LineColumn2 { get; set; }
}
public class EdgeInspectionPoint { public double Row { get; set; } public double Column { get; set; } }
```

From WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs:43-58:
```csharp
public double NominalValue, TolerancePlus, ToleranceMinus;
public double LastMeasuredValue;     // 측정값 mm
public bool   LastJudgement;         // true=OK
public bool   LastHasResult;         // CO-23-01: 0.0 도 정상 결과
public string LastSkipReason;        // null or "DATUM_FAIL"
public string MeasurementName;       // TypeName 폴백
```

From WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs:97-114:
```csharp
public double MeasuredValue;          // [Browsable(false)]
public bool   IsPass;                 // [Browsable(false)]
public bool   WasDatumSkipped;        // [Browsable(false)]
public List<EdgeInspectionOverlay> LastOverlays;  // [JsonIgnore] — 반드시 DTO 로 복사
```

From WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs:17-31:
```csharp
public string SimulImagePath;                 // 측정에 사용된 소스 이미지 경로
public string GetLatestImagePath();           // → SimulImagePath 반환 (리뷰어 재로드용 이미지)
public string ShotName;
public string OwnerSequenceName;
public List<FAIConfig> FAIList;
```

cycle 완료 wiring 진입점 — WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs:118-124:
```csharp
if (anyDatumSkip) responsePacket.Result = EVisionResultType.NotExist;
else if (!allPass) responsePacket.Result = EVisionResultType.NG;
else responsePacket.Result = EVisionResultType.OK;
pMyContext.ResultInfo = responsePacket.Result;
// [신규 삽입 위치] ← 여기
ResponseQueue.Enqueue(responsePacket);
```

레시피명 / 저장경로:
- `SystemHandler.Handle.Setting.CurrentRecipeName` (string, default "A") — D-02 메타 레시피명
- `SystemHandler.Handle.Setting.ResultSavePath` (string, default `BaseDirectory + "Result"`) — D-03 저장 루트
- `SystemHandler.Handle.Sequences.RecipeManager.Shots` — 전 Shot 결과 (AddResponse 에서 이미 사용 중)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CycleResultDto JSON 직렬화 DTO 정의</name>
  <files>WPF_Example/UI/ViewModel/CycleResultDto.cs</files>
  <read_first>
    - WPF_Example/UI/ViewModel/MeasurementResultRow.cs (아날로그 — 네임스페이스 ReringProject.UI, DTO 컬럼 구성)
    - WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs:22-48 (직렬화 대상 타입)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs:43-58
    - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs:97-114
    - .planning/phases/40-export-i-1-2026-06-01/40-RESEARCH.md (Pattern 1)
  </read_first>
  <behavior>
    - JsonConvert.SerializeObject(new CycleResultDto { ... }) 가 예외 없이 직렬화 문자열 반환
    - JsonConvert.DeserializeObject<CycleResultDto>(json) 가 모든 필드를 손실 없이 복원 (overlay Points 포함)
    - 빈 Shots / null Measurements 도 직렬화/역직렬화 안전 (NRE 없음)
  </behavior>
  <action>
namespace `ReringProject.UI`, `using System;` + `using System.Collections.Generic;` + `using ReringProject.Halcon.Models;` (EdgeInspectionOverlay 참조). `Observable` 상속 없이 순수 직렬화 DTO. `[JsonIgnore]` 사용 금지 — DTO 계층은 전 필드를 직렬화 노출 (FAIConfig.LastOverlays 의 [JsonIgnore] 는 INI 용이므로 DTO 복사 시 무관).

5개 클래스를 한 파일에 정의:

```csharp
public class CycleResultDto
{
    // D-02 메타
    public DateTime InspectionTime { get; set; }
    public string RecipeName { get; set; }
    public string OverallJudgement { get; set; }   // "OK" / "NG" / "DETECT_FAIL"
    // D-03 cycle 폴더 경로 (절대 경로)
    public string CycleFolderPath { get; set; }
    // D-01/D-05 측정 데이터
    public List<ShotResultDto> Shots { get; set; } = new List<ShotResultDto>();
}

public class ShotResultDto
{
    public string ShotName { get; set; }
    public string OwnerSequenceName { get; set; }
    public string ResultImagePath { get; set; }    // ShotConfig.GetLatestImagePath() = SimulImagePath (측정 소스 이미지, 리뷰어 재로드용 — 절대 경로)
    public List<FaiResultDto> FAIs { get; set; } = new List<FaiResultDto>();
}

public class FaiResultDto
{
    public string FAIName { get; set; }
    public bool IsPass { get; set; }
    public bool WasDatumSkipped { get; set; }
    public List<MeasurementResultDto> Measurements { get; set; } = new List<MeasurementResultDto>();
    public List<EdgeInspectionOverlay> LastOverlays { get; set; } = new List<EdgeInspectionOverlay>();
}

public class MeasurementResultDto
{
    public string MeasurementName { get; set; }
    public string TypeName { get; set; }
    public double NominalValue { get; set; }
    public double TolerancePlus { get; set; }
    public double ToleranceMinus { get; set; }
    public double LastMeasuredValue { get; set; }
    public bool LastJudgement { get; set; }     // true=OK
    public bool LastHasResult { get; set; }     // CO-23-01: 0.0 정상 결과 구분
    public string LastSkipReason { get; set; }  // null or "DATUM_FAIL"
}
```

각 신규 클래스 위에 `//260601 hbk Phase 40 OUT-01/OUT-02` 주석 마커. C# 7.2 준수 (switch expression / record / nullable refs 금지). csproj Compile ItemGroup 에 이 파일 등록 (CS0246 차단).
  </action>
  <verify>
    <automated>findstr /C:"public class CycleResultDto" /C:"public class FaiResultDto" /C:"List<EdgeInspectionOverlay> LastOverlays" WPF_Example\UI\ViewModel\CycleResultDto.cs</automated>
  </verify>
  <acceptance_criteria>
    - CycleResultDto.cs 에 `public class CycleResultDto` / `ShotResultDto` / `FaiResultDto` / `MeasurementResultDto` 4개 클래스 존재
    - FaiResultDto 에 `public List<EdgeInspectionOverlay> LastOverlays` 존재 (JsonIgnore 없음)
    - MeasurementResultDto 에 `LastHasResult`, `LastSkipReason`, `NominalValue`, `TolerancePlus`, `ToleranceMinus`, `LastMeasuredValue`, `LastJudgement` 7필드 존재
    - csproj 에 `<Compile Include="UI\ViewModel\CycleResultDto.cs" />` 등록
    - msbuild Debug/x64 exits 0, 신규 warning 0
  </acceptance_criteria>
  <done>CycleResultDto 5종 정의 + csproj 등록 + 빌드 PASS</done>
</task>

<task type="auto">
  <name>Task 2: CycleResultSerializer 서비스 (BuildDto + SaveAsync + Load) + 저장 디렉터리 변경</name>
  <files>WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs, WPF_Example/DatumMeasurement.csproj</files>
  <read_first>
    - WPF_Example/Utility/RawImageSaveService.cs:71-93 (날짜폴더 저장 + 예외격리 아날로그 — exact)
    - WPF_Example/Sequence/Sequence/SequenceBase.cs:382-415 (Task.Factory.StartNew 비동기 예외격리 패턴)
    - WPF_Example/Setting/SystemSetting.cs:45,63,137-166 (CurrentRecipeName, ResultSavePath, GetLogSavePath)
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs:17-31 (GetLatestImagePath, FAIList)
    - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs (Shots 컬렉션 — RecipeManager.Shots 순회)
    - WPF_Example/UI/ViewModel/CycleResultDto.cs (Task 1 산출물)
    - .planning/phases/40-export-i-1-2026-06-01/40-RESEARCH.md (Code Examples, Pitfall 4/5)
  </read_first>
  <action>
신규 파일 `WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs`. namespace `ReringProject.Sequence` (Custom 폴더 규칙). `using ReringProject.Setting;` + `using System;` + `using System.Collections.Generic;` + `using System.IO;` + `using System.Linq;` + `using System.Threading.Tasks;` + `using Newtonsoft.Json;` + `using ReringProject.UI;` (CycleResultDto). 클래스 상단 `//260601 hbk Phase 40 OUT-01/OUT-02 — cycle 결과 JSON 영속화` 마커.

3개 static 메서드:

1. `public static CycleResultDto BuildDto(InspectionRecipeManager recipeManager, EVisionResultType cycleResult, DateTime when, string recipeName)`:
   - 새 CycleResultDto 생성: `InspectionTime = when`, `RecipeName = recipeName`, `OverallJudgement = MapJudgement(cycleResult)`.
   - `MapJudgement`: `EVisionResultType.NotExist → "DETECT_FAIL"`, `EVisionResultType.NG → "NG"`, 그 외 → `"OK"` (InspectionSequence 3-state hierarchy 와 일치, D-02/specifics).
   - `recipeManager.Shots` 순회 → ShotResultDto 생성: `ShotName`, `OwnerSequenceName`, `ResultImagePath = shot.GetLatestImagePath()` (= SimulImagePath, 측정 소스 이미지 — SaveResultImage 의 SaveFailImage 게이트에 의존하지 말 것, RESEARCH Pitfall + SequenceBase.cs:383 확인).
   - 각 shot.FAIList 순회 → FaiResultDto: `FAIName`, `IsPass`, `WasDatumSkipped`, `LastOverlays = new List<EdgeInspectionOverlay>(fai.LastOverlays ?? new List<EdgeInspectionOverlay>())` (DTO 로 복사 — FAIConfig.LastOverlays 는 [JsonIgnore]).
   - 각 fai 의 Measurements 순회 (FAIConfig 가 보유한 MeasurementBase 컬렉션 — 코드 확인 후 정확한 프로퍼티명 사용) → MeasurementResultDto: MeasurementName, TypeName, NominalValue, TolerancePlus, ToleranceMinus, LastMeasuredValue, LastJudgement, LastHasResult, LastSkipReason 복사.

2. `public static void SaveAsync(CycleResultDto dto)`:
   - dto null 가드. `dto.CycleFolderPath` 를 BuildDto 또는 여기서 계산: `string dateDir = Path.Combine(SystemHandler.Handle.Setting.ResultSavePath, dto.InspectionTime.ToString("yyyyMMdd")); string cycleDir = Path.Combine(dateDir, dto.InspectionTime.ToString("HHmmss") + "_cycle");` — `dto.CycleFolderPath = cycleDir` 설정.
   - `Task.Factory.StartNew(() => { try { Directory.CreateDirectory(cycleDir); string jsonPath = Path.Combine(cycleDir, "cycle.json"); string json = JsonConvert.SerializeObject(dto, Formatting.Indented); File.WriteAllText(jsonPath, json, System.Text.Encoding.UTF8); } catch (Exception ex) { try { Logging.PrintErrLog((int)ELogType.Error, "[CycleResultSerializer] Save failed: " + ex.Message); } catch { } } });`
   - 보안 T-40-01 mitigation: cycleDir 계산은 ResultSavePath + DateTime 포맷팅만 사용 → 외부 입력 없음 (path traversal 불가).

3. `public static CycleResultDto Load(string jsonPath)`:
   - 보안 T-40-02 mitigation: `try { if (string.IsNullOrEmpty(jsonPath) || !File.Exists(jsonPath)) return null; string json = File.ReadAllText(jsonPath, System.Text.Encoding.UTF8); var settings = new JsonSerializerSettings { TypeNameHandling = TypeNameHandling.None }; return JsonConvert.DeserializeObject<CycleResultDto>(json, settings); } catch { return null; }`
   - **TypeNameHandling.None 명시** (절대 None 외 설정 금지 — security_requirement). 손상/악성 JSON → null 반환, unhandled exception 0.

csproj Compile ItemGroup 에 CycleResultSerializer.cs 등록. Logging/ELogType 는 `using ReringProject.Utility;` 또는 fully-qualified 로 해소.
  </action>
  <verify>
    <automated>findstr /C:"public static void SaveAsync" /C:"TypeNameHandling.None" /C:"GetLatestImagePath" /C:"cycle.json" WPF_Example\Custom\Sequence\Inspection\CycleResultSerializer.cs</automated>
  </verify>
  <acceptance_criteria>
    - CycleResultSerializer.cs 에 `BuildDto`, `SaveAsync`, `Load` 3개 public static 메서드 존재
    - Load 에 `TypeNameHandling.None` 명시 존재 (security)
    - SaveAsync 가 `ResultSavePath` + `yyyyMMdd` + `HHmmss` + `"cycle.json"` 경로 사용
    - SaveAsync 가 `Task.Factory.StartNew` + try/catch Logging.PrintErrLog 예외 격리 적용
    - BuildDto 가 `shot.GetLatestImagePath()` 로 ResultImagePath 채움 (SaveResultImage 비의존)
    - FaiResultDto.LastOverlays 를 `new List<EdgeInspectionOverlay>(fai.LastOverlays)` 로 복사
    - csproj 에 CycleResultSerializer.cs Compile 등록
    - msbuild Debug/x64 exits 0, 신규 warning 0
  </acceptance_criteria>
  <done>CycleResultSerializer 3메서드 + 예외격리 + TypeNameHandling.None + csproj 등록 + 빌드 PASS</done>
</task>

<task type="auto">
  <name>Task 3: InspectionSequence.AddResponse cycle 완료 wiring</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs:75-125 (AddResponse 전체 — 삽입 지점)
    - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs (Task 2 산출물 — BuildDto/SaveAsync 시그니처)
    - .planning/phases/40-export-i-1-2026-06-01/40-PATTERNS.md (cycle 완료 wiring 위치 섹션)
    - .planning/phases/40-export-i-1-2026-06-01/40-RESEARCH.md (Pitfall 4 — 직렬화 예외 격리)
  </read_first>
  <action>
`AddResponse()` 내부, `pMyContext.ResultInfo = responsePacket.Result;` (L122) 직후 + `ResponseQueue.Enqueue(responsePacket);` (L124) 직전에 cycle JSON 저장 wiring 삽입:

```csharp
//260601 hbk Phase 40 OUT-01/OUT-02 — cycle 완료 → 구조화 JSON 영속화 (리뷰어/xlsx 공통 토대, D-01/D-02/D-03)
try
{
    var cycleDto = CycleResultSerializer.BuildDto(
        recipeManager,
        responsePacket.Result,
        DateTime.Now,
        SystemHandler.Handle.Setting.CurrentRecipeName);
    CycleResultSerializer.SaveAsync(cycleDto);
}
catch (Exception ex)
{
    try { Logging.PrintErrLog((int)ELogType.Error, "[Phase40] cycle 직렬화 실패(무시): " + ex.Message); } catch { }
}
```

`recipeManager` 변수는 L78 에 이미 선언됨 (`var recipeManager = SystemHandler.Handle.Sequences.RecipeManager;`) — 재사용. `DateTime` / `Exception` 은 기존 using 으로 해소되는지 확인, 부족 시 `using System;` 추가.

핵심: BuildDto 는 동기적으로 스냅샷을 만들고 (Shots 가 이미 채워진 시점), SaveAsync 가 비동기 파일 쓰기 — AddResponse 스레드 블로킹 최소화. 직렬화 예외가 TCP 응답(ResponseQueue.Enqueue)을 차단하지 않도록 try/catch 격리 (RESEARCH Pitfall 4). 삽입 외 기존 L75-124 로직 무수정 (Phase 39 WF-02 3-state hierarchy 회귀 0).
  </action>
  <verify>
    <automated>findstr /C:"CycleResultSerializer.BuildDto" /C:"CycleResultSerializer.SaveAsync" /C:"Phase 40" WPF_Example\Custom\Sequence\Inspection\InspectionSequence.cs</automated>
  </verify>
  <acceptance_criteria>
    - AddResponse 내 `pMyContext.ResultInfo` 와 `ResponseQueue.Enqueue` 사이에 `CycleResultSerializer.BuildDto(...)` + `SaveAsync(...)` 호출 존재
    - 호출이 try/catch 로 감싸짐 (직렬화 실패 시 ResponseQueue.Enqueue 계속 진행)
    - 기존 Phase 39 WF-02 3-state hierarchy 라인 (anyDatumSkip/allPass/responsePacket.Result) 무수정
    - msbuild Debug/x64 exits 0, 신규 warning 0
  </acceptance_criteria>
  <done>AddResponse wiring + 예외격리 + 기존 로직 회귀 0 + 빌드 PASS</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 검사 엔진 → 디스크 | cycle 결과를 JSON 파일로 직렬화 저장 (내부 생성 데이터, 외부 입력 아님) |
| 디스크 → 역직렬화 | 저장된 cycle.json 을 다시 읽음 (변조/손상 가능성 존재) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | CycleResultSerializer.SaveAsync 경로 구성 | mitigate | cycleDir = ResultSavePath + DateTime 포맷팅만 사용 — 외부 입력 없음, path traversal 원천 차단 |
| T-40-02 | Tampering | CycleResultSerializer.Load 역직렬화 | mitigate | `TypeNameHandling.None` 명시 + try/catch → null 반환. 손상/악성 JSON → unhandled exception 0 (RCE 방지) |
| T-40-03 | Denial of Service | AddResponse 내 직렬화 예외 | mitigate | BuildDto/SaveAsync 호출을 try/catch 로 격리 — 직렬화 실패가 TCP 응답/sequence 진행을 차단하지 않음 |
| T-40-04 | Information Disclosure | cycle.json 평문 측정 데이터 | accept | 로컬 디스크 산업용 검사 데이터, PII 없음, 오프라인 단일 PC — 암호화 불필요 (ASVS V6 해당 없음) |
</threat_model>

<verification>
- msbuild Debug/x64 Rebuild PASS (0 errors, 신규 warning 0)
- SIMUL_MODE 검사 1회 실행 후 `./Result/{오늘날짜}/{시각}_cycle/cycle.json` 파일 생성 확인
- cycle.json 을 텍스트 에디터로 열어 Shots/FAIs/Measurements/LastOverlays/메타 필드 모두 존재 확인
</verification>

<success_criteria>
- CycleResultDto 5종 + CycleResultSerializer 3메서드 정의 + InspectionSequence wiring 완료
- 검사 cycle 완료 시 cycle.json 자동 생성 (D-01/D-02/D-03)
- 직렬화 예외 시에도 TCP 응답 계속 진행 (회귀 0)
- TypeNameHandling.None 보안 제어 적용 (T-40-02)
</success_criteria>

<output>
After completion, create `.planning/phases/40-export-i-1-2026-06-01/40-01-SUMMARY.md`
</output>
