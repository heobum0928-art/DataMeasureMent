---
phase: 40-export-i-1-2026-06-01
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/packages.config
  - WPF_Example/App.config
  - WPF_Example/DatumMeasurement.csproj
autonomous: false
requirements: [OUT-02]
must_haves:
  truths:
    - "ClosedXML 0.105.0 + 전이 의존성이 packages.config 에 등록된다"
    - "msbuild Debug/x64 가 0 errors 로 빌드된다 (참조 해소)"
    - "런타임에 new XLWorkbook() 이 FileNotFoundException 없이 생성된다 (.NET 4.8 어셈블리 로드 검증)"
  artifacts:
    - path: "WPF_Example/packages.config"
      provides: "ClosedXML + 전이 의존성 package 항목"
      contains: "ClosedXML"
    - path: "WPF_Example/DatumMeasurement.csproj"
      provides: "ClosedXML 등 신규 어셈블리 Reference"
      contains: "ClosedXML"
  key_links:
    - from: "WPF_Example/DatumMeasurement.csproj"
      to: "packages/ClosedXML.0.105.0/lib/netstandard2.0/ClosedXML.dll"
      via: "<Reference><HintPath>"
      pattern: "ClosedXML"
---

<objective>
xlsx export(OUT-02)의 선행 의존성을 구축한다 (D-04 ClosedXML, MIT). packages.config(classic NuGet) 환경에 ClosedXML 0.105.0 + 전이 의존성(DocumentFormat.OpenXml, SixLabors.Fonts, ClosedXML.Parser, ExcelNumberFormat, RBush.Signed, Microsoft.Bcl.HashCode)을 수동 등록하고, csproj `<Reference>` + App.config binding redirect 를 정합한 뒤, `new XLWorkbook()` smoke test 로 .NET 4.8 런타임 로드를 검증한다.

Purpose: classic NuGet 은 전이 의존성을 자동 복원하지 않으므로 수동 등록 + 빌드/런타임 검증이 필수다. **이 plan 은 Plan 04(ExcelExportService) 의 BLOCKING 선행이다.**
Output: packages.config / App.config / csproj 갱신 + smoke test 통과.

[BLOCKING] — Plan 04 (xlsx export) 는 이 plan 완료 후에만 실행 가능.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-export-i-1-2026-06-01/40-CONTEXT.md
@.planning/phases/40-export-i-1-2026-06-01/40-RESEARCH.md
@.planning/phases/40-export-i-1-2026-06-01/40-PATTERNS.md

<interfaces>
<!-- RESEARCH.md Standard Stack 섹션 — 버전 확정값 -->
ClosedXML            0.105.0    (MIT, .NET Standard 2.0 → net48 호환) [VERIFIED nuget.org]
DocumentFormat.OpenXml  >=3.1.1 <4.0.0  (권장 3.4.1 최신 stable)
SixLabors.Fonts     2.1.3 (stable — prerelease 이슈 해소)
ClosedXML.Parser    >=2.0.0   [ASSUMED — 설치 후 packages 폴더 검증]
ExcelNumberFormat   >=1.1.0   [ASSUMED]
RBush.Signed        >=4.0.0   [ASSUMED]
Microsoft.Bcl.HashCode  >=1.1.1  [ASSUMED]

기존 packages.config 보유 (재추가 금지): Newtonsoft.Json 13.0.3, System.Memory 4.5.5,
System.Buffers 4.5.1, System.Runtime.CompilerServices.Unsafe 6.0.0, Ookii.Dialogs.Wpf 5.0.1
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ClosedXML + 전이 의존성 설치 및 packages.config/csproj 등록</name>
  <files>WPF_Example/packages.config, WPF_Example/DatumMeasurement.csproj, WPF_Example/App.config</files>
  <read_first>
    - WPF_Example/packages.config (현재 의존성 목록 — 중복 추가 금지 항목 확인)
    - WPF_Example/App.config (기존 assemblyBinding redirect 현황)
    - WPF_Example/DatumMeasurement.csproj (Reference / HintPath 패턴 — OpenCvSharp4 등 기존 NuGet Reference 형식 답습)
    - .planning/phases/40-export-i-1-2026-06-01/40-RESEARCH.md (Standard Stack, Pitfall 1/2, Open Questions 1/2)
  </read_first>
  <action>
PM Console 또는 nuget.exe 로 ClosedXML 0.105.0 설치. SixLabors.Fonts prerelease 이슈 회피 위해 stable 먼저 명시 (RESEARCH Standard Stack 설치 방법):
```
PM> Install-Package SixLabors.Fonts -Version 2.1.3
PM> Install-Package ClosedXML -Version 0.105.0
```
PM Console 사용 불가 시 nuget.exe CLI: `nuget install ClosedXML -Version 0.105.0 -OutputDirectory packages`.

설치 후 **실제 복원된 packages 폴더를 확인** (RESEARCH Pitfall 1 — classic NuGet 은 전이 의존성 자동 등록 안 됨). 다음 패키지가 packages.config 에 `<package id="..." version="..." targetFramework="net48" />` 형태로 모두 등록됐는지 검증, 누락 시 수동 추가:
- ClosedXML (0.105.0)
- DocumentFormat.OpenXml (3.1.1~3.4.1 — Open Question 1: 빌드 통과하는 버전으로 고정)
- SixLabors.Fonts (2.1.3)
- ClosedXML.Parser, ExcelNumberFormat, RBush.Signed, Microsoft.Bcl.HashCode (복원된 실제 버전)

csproj `<ItemGroup>` 에 각 어셈블리 `<Reference>` 추가 — 기존 OpenCvSharp4 Reference 형식 답습 (`<Reference Include="ClosedXML"><HintPath>packages\ClosedXML.0.105.0\lib\netstandard2.0\ClosedXML.dll</HintPath></Reference>` 처럼 실제 lib 경로 확인 후 net48 또는 netstandard2.0 폴더 선택).

App.config binding redirect (RESEARCH Pitfall 2): DocumentFormat.OpenXml 3.x 가 System.Memory/System.Buffers 다른 버전 요구 시 `<assemblyBinding>` 에 redirect 추가. 기존 System.Memory 4.0.1.2 / System.Runtime.CompilerServices.Unsafe 6.0.0 redirect 는 보존. 충돌 어셈블리 발견 시에만 추가, 무충돌이면 그대로.

x64 빌드이므로 SixLabors 등 네이티브 의존성 없는 순수 managed 어셈블리인지 확인 (managed-only → 플랫폼 무관).
  </action>
  <verify>
    <automated>findstr /C:"ClosedXML" /C:"DocumentFormat.OpenXml" /C:"SixLabors.Fonts" WPF_Example\packages.config && msbuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild</automated>
  </verify>
  <acceptance_criteria>
    - packages.config 에 `ClosedXML` version="0.105.0" 항목 존재
    - packages.config 에 `DocumentFormat.OpenXml`, `SixLabors.Fonts` 항목 존재
    - csproj 에 ClosedXML `<Reference>` + `<HintPath>` 존재
    - 전이 의존성(ClosedXML.Parser/ExcelNumberFormat/RBush.Signed/Microsoft.Bcl.HashCode) 모두 packages.config 등록
    - msbuild Debug/x64 Rebuild exits 0
  </acceptance_criteria>
  <done>ClosedXML + 전이 의존성 등록 + csproj Reference + 빌드 PASS</done>
</task>

<task type="auto">
  <name>Task 2: new XLWorkbook() 런타임 로드 smoke test</name>
  <files>WPF_Example/Custom/Export/ExcelExportSmokeTest.cs</files>
  <read_first>
    - .planning/phases/40-export-i-1-2026-06-01/40-RESEARCH.md (Open Question 2, Wave 0 Gaps, Assumptions A1/A2/A3)
    - WPF_Example/DatumMeasurement.csproj (Task 1 갱신 결과 — Reference 확인)
  </read_first>
  <action>
ClosedXML + 전이 의존성(특히 SixLabors.Fonts 2.1.3) 이 .NET 4.8 런타임에서 실제 로드되는지 검증하는 최소 smoke test (RESEARCH Assumptions A2 — nuget.org 가 net8.0 타겟만 명시하여 net48 로드 미확인). 테스트 프레임워크 없으므로(MEMORY: MSTest/xUnit 없음) 임시 정적 진입 메서드로 구현:

신규 파일 `WPF_Example/Custom/Export/ExcelExportSmokeTest.cs`, namespace `ReringProject.Export`, `using ClosedXML.Excel;` + `using System;` + `using System.IO;`. 클래스 상단 `//260601 hbk Phase 40 OUT-02 — ClosedXML 런타임 로드 smoke test (의존성 검증 후 제거 가능)` 마커.

```csharp
public static class ExcelExportSmokeTest
{
    public static bool TryCreateWorkbook(out string error)
    {
        error = null;
        try
        {
            using (var wb = new XLWorkbook())
            {
                var ws = wb.Worksheets.Add("Smoke");
                ws.Cell(1, 1).Value = "OK";
                string tmp = Path.Combine(Path.GetTempPath(), "closedxml_smoke.xlsx");
                wb.SaveAs(tmp);
                bool ok = File.Exists(tmp);
                try { File.Delete(tmp); } catch { }
                return ok;
            }
        }
        catch (Exception ex) { error = ex.Message; return false; }
    }
}
```

csproj Compile ItemGroup 에 등록. App.xaml.cs 또는 MainWindow Loaded 에서 `#if SIMUL_MODE` 가드 하에 1회 호출하여 Logging.PrintLog 로 결과 출력하는 것은 선택 — checkpoint 에서 수동 호출/디버거 평가로 검증해도 됨. **smoke test 가 true 반환 = SixLabors.Fonts net48 로드 성공 = A2 검증 완료.**
  </action>
  <verify>
    <automated>findstr /C:"new XLWorkbook" /C:"TryCreateWorkbook" WPF_Example\Custom\Export\ExcelExportSmokeTest.cs</automated>
  </verify>
  <acceptance_criteria>
    - ExcelExportSmokeTest.cs 에 `TryCreateWorkbook` + `new XLWorkbook()` + `wb.SaveAs` 존재
    - csproj 에 Compile 등록
    - msbuild Debug/x64 exits 0
  </acceptance_criteria>
  <done>smoke test 메서드 작성 + 빌드 PASS</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>ClosedXML 0.105.0 + 전이 의존성을 packages.config 에 등록하고 csproj Reference + App.config redirect 를 정합했습니다. ExcelExportSmokeTest.TryCreateWorkbook() 으로 .NET 4.8 런타임에서 XLWorkbook 생성 + xlsx 저장이 동작하는지 검증할 수 있습니다.</what-built>
  <how-to-verify>
    1. Debug/x64 로 빌드 후 앱을 실행 (또는 디버거에서 `ExcelExportSmokeTest.TryCreateWorkbook(out var err)` 즉시 실행 평가)
    2. 반환값이 true 인지 확인 (false 면 err 메시지 = 어떤 어셈블리가 로드 실패했는지 — 보통 SixLabors.Fonts 또는 DocumentFormat.OpenXml)
    3. `%TEMP%\closedxml_smoke.xlsx` 가 생성되었다가 삭제되는지(또는 생성 시점에) 확인
    4. 앱 시작 시 `Could not load file or assembly 'SixLabors.Fonts'` 류 예외가 없는지 확인 (RESEARCH Pitfall 1 경고 신호)
  </how-to-verify>
  <resume-signal>smoke test true 확인 시 "approved", 어셈블리 로드 실패 시 err 메시지 공유</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NuGet 패키지 → 빌드/런타임 | 외부 라이브러리(ClosedXML 및 전이 의존성) 도입 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05 | Tampering | NuGet 패키지 무결성 | accept | nuget.org 공식 피드 + 버전 핀(0.105.0/2.1.3) 고정. MIT 라이선스 검증됨 (D-04). 사내 산업 PC 오프라인 환경 |
| T-40-06 | Elevation of Privilege | 전이 의존성 어셈블리 binding redirect | mitigate | 기존 System.Memory/Unsafe redirect 보존 + 충돌 시에만 추가. version conflict 예외 = smoke test 에서 사전 검출 |
</threat_model>

<verification>
- msbuild Debug/x64 Rebuild PASS (0 errors)
- ExcelExportSmokeTest.TryCreateWorkbook() == true (런타임 어셈블리 로드 검증)
- 앱 시작 시 assembly load 예외 0
</verification>

<success_criteria>
- ClosedXML + 전이 의존성 packages.config/csproj 등록 완료
- new XLWorkbook() 런타임 생성 성공 (.NET 4.8 호환 입증)
- Plan 04(xlsx export) 의 선행 의존성 해소
</success_criteria>

<output>
After completion, create `.planning/phases/40-export-i-1-2026-06-01/40-02-SUMMARY.md`
</output>
