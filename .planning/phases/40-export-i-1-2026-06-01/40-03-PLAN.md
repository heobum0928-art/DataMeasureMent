---
phase: 40-export-i-1-2026-06-01
plan: 03
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs
  - WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs
  - WPF_Example/MainWindow.xaml.cs
  - WPF_Example/UI/MenuBar.xaml
  - WPF_Example/UI/MenuBar.xaml.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: false
requirements: [OUT-01]
must_haves:
  truths:
    - "메뉴 버튼 클릭 시 별도 리뷰어 Window 가 비모달로 열린다"
    - "날짜 폴더 선택 시 그 안의 cycle 목록이 시각 + 종합판정으로 표시된다"
    - "cycle 선택 시 결과 이미지 + overlay 가 라이브 검사와 동일하게 재현된다"
    - "cycle 선택 시 측정표(Shot/FAI/측정명/nominal/tol/측정값/판정)가 표시된다"
    - "손상/빈 cycle.json 선택 시 크래시 없이 빈 상태로 처리된다"
  artifacts:
    - path: "WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs"
      provides: "리뷰어 Window — 폴더 선택 + cycle 목록 + 이미지/overlay 재렌더 + 측정표"
      contains: "VistaFolderBrowserDialog"
    - path: "WPF_Example/UI/Reviewer/ReviewerWindow.xaml"
      provides: "리뷰어 레이아웃 (HalconViewerControl + ListBox + DataGrid)"
      contains: "HalconViewerControl"
    - path: "WPF_Example/MainWindow.xaml.cs"
      provides: "EPageType.Reviewer + PopupView 비모달 Show 분기"
      contains: "Reviewer"
  key_links:
    - from: "WPF_Example/UI/MenuBar.xaml.cs"
      to: "MainWindow.PopupView(EPageType.Reviewer)"
      via: "Button_Reviewer_Click"
      pattern: "PopupView\\(EPageType.Reviewer\\)"
    - from: "WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs"
      to: "HalconViewerControl.SetInspectionOverlays"
      via: "LoadImage → SetInspectionOverlays 순서"
      pattern: "SetInspectionOverlays"
    - from: "WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs"
      to: "CycleResultSerializer.Load"
      via: "cycle 선택 시 cycle.json 역직렬화"
      pattern: "CycleResultSerializer\\.Load"
---

<objective>
결과 리뷰어를 구축한다 (OUT-01, D-08/D-09). 메뉴 버튼으로 여는 독립 WPF Window 에서 Ookii 폴더 다이얼로그로 날짜 폴더를 선택 → cycle 목록(시각·종합판정)을 표시 → cycle 선택 시 Plan 01 의 cycle.json 을 역직렬화하여 결과 이미지 + overlay 를 라이브 경로(`HalconDisplayService`)와 동일하게 재렌더하고 측정표를 표시한다.

Purpose: 검사 완료 결과를 사후에 다시 불러와 검토 (POC "검사 → 결과 리뷰" 흐름). 라이브 MainView 방해 없는 비모달 별도 Window (D-08).
Output: ReviewerWindow.xaml(.cs) (신규), ReviewMeasurementRow.cs (신규 그리드 행 DTO), MainWindow/MenuBar 수정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-export-i-1-2026-06-01/40-CONTEXT.md
@.planning/phases/40-export-i-1-2026-06-01/40-RESEARCH.md
@.planning/phases/40-export-i-1-2026-06-01/40-PATTERNS.md

<interfaces>
<!-- Plan 01 산출물 (이미 정의됨) -->
WPF_Example/UI/ViewModel/CycleResultDto.cs:
  CycleResultDto { DateTime InspectionTime; string RecipeName; string OverallJudgement; string CycleFolderPath; List<ShotResultDto> Shots; }
  ShotResultDto  { string ShotName; string OwnerSequenceName; string ResultImagePath; List<FaiResultDto> FAIs; }
  FaiResultDto   { string FAIName; bool IsPass; bool WasDatumSkipped; List<MeasurementResultDto> Measurements; List<EdgeInspectionOverlay> LastOverlays; }
  MeasurementResultDto { string MeasurementName; string TypeName; double NominalValue/TolerancePlus/ToleranceMinus/LastMeasuredValue; bool LastJudgement/LastHasResult; string LastSkipReason; }

WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs:
  public static CycleResultDto Load(string jsonPath);   // 손상 JSON → null (TypeNameHandling.None)

<!-- 기존 컨트롤/패턴 (코드 확인됨) -->
HalconViewerControl 공개 API (HalconViewerControl.xaml.cs:112-170):
  void LoadImage(string imagePath);                                     // 경로 기반 로드
  void SetInspectionOverlays(IEnumerable<EdgeInspectionOverlay> overlays); // Clear+AddRange (REPLACE)
  // 순서 반드시: LoadImage → SetInspectionOverlays (RESEARCH Pitfall 6)

Ookii 폴더 다이얼로그 (DeviceSelector.xaml.cs:250-263):
  var dlg = new Ookii.Dialogs.Wpf.VistaFolderBrowserDialog();
  dlg.Multiselect = false; dlg.SelectedPath = SystemHandler.Handle.Setting.ResultSavePath;
  if ((bool)dlg.ShowDialog()) { ... }

MainWindow PopupView 비모달 패턴 (MainWindow.xaml.cs:20-30, 327-337):
  enum EPageType { ..., ProcessMonitor }    // ← Reviewer 추가
  private ProcessMonitorWindow mProcMonitorWindow;   // ← mReviewerWindow 추가
  case EPageType.ProcessMonitor:
      if (mProcMonitorWindow != null && mProcMonitorWindow.IsLoaded) { mProcMonitorWindow.Show(); return; }
      mProcMonitorWindow = new ProcessMonitorWindow();
      mProcMonitorWindow.Owner = this; mProcMonitorWindow.Show(); break;   // Show() = 비모달

MenuBar 버튼 → PopupView (MenuBar.xaml.cs:96-101):
  private void Button_CI_Click(object sender, RoutedEventArgs e) { mParentWindow.PopupView(EPageType.ProcessMonitor); }

MeasurementResultRow 포맷 로직 (MeasurementResultRow.cs:40-66):
  HasResult ? (IsPass ? "OK" : "NG") : "—"     // JudgeText
  단위 분기 "deg"/"mm"                          // ResultDisplay
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReviewMeasurementRow 그리드 행 DTO + ReviewerWindow.xaml 레이아웃</name>
  <files>WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs, WPF_Example/UI/Reviewer/ReviewerWindow.xaml, WPF_Example/DatumMeasurement.csproj</files>
  <read_first>
    - WPF_Example/UI/ViewModel/MeasurementResultRow.cs:40-66 (JudgeText/ResultDisplay 포맷 로직 아날로그)
    - WPF_Example/UI/ProcessMonitor/ProcessMonitorWindow.xaml (Window 선언 + DataGrid 구조 아날로그)
    - WPF_Example/UI/ContentItem/HalconViewerControl.xaml (컨트롤 XAML namespace 확인 — 리뷰어에 인스턴스 배치)
    - WPF_Example/UI/ViewModel/CycleResultDto.cs (MeasurementResultDto 필드)
    - .planning/phases/40-export-i-1-2026-06-01/40-PATTERNS.md (ReviewerWindow XAML DataGrid 패턴)
  </read_first>
  <action>
**ReviewMeasurementRow.cs** (신규, namespace `ReringProject.UI`): cycle.json 의 MeasurementResultDto 를 그리드 행으로 래핑. ShotName/FAIName 은 부모 컨텍스트에서 받아 합성. `using ReringProject.Sequence;` 불요 — DTO 만 참조. 클래스 상단 `//260601 hbk Phase 40 OUT-01` 마커.
```csharp
public class ReviewMeasurementRow
{
    public string ShotName { get; set; }
    public string FAIName { get; set; }
    public string MeasurementName { get; set; }
    public double NominalValue { get; set; }
    public double TolerancePlus { get; set; }
    public double ToleranceMinus { get; set; }
    public string ResultDisplay { get; set; }   // LastHasResult ? LastMeasuredValue.ToString("F4") : "—"
    public string JudgeText { get; set; }        // LastSkipReason=="DATUM_FAIL"?"DETECT FAIL":(LastHasResult?(LastJudgement?"OK":"NG"):"—")
    public ReviewMeasurementRow(string shotName, MeasurementResultDto m) {
        ShotName = shotName; FAIName = ...; MeasurementName = m.MeasurementName;
        NominalValue = m.NominalValue; TolerancePlus = m.TolerancePlus; ToleranceMinus = m.ToleranceMinus;
        ResultDisplay = m.LastHasResult ? m.LastMeasuredValue.ToString("F4") : "—";
        JudgeText = m.LastSkipReason == "DATUM_FAIL" ? "DETECT FAIL"
                    : (m.LastHasResult ? (m.LastJudgement ? "OK" : "NG") : "—");
    }
}
```
(FAIName 은 생성자에 faiName 인자 추가하여 주입 — Task 2 가 Shot/FAI 순회 시 전달.)

**ReviewerWindow.xaml** (신규, `WPF_Example/UI/Reviewer/`): `<Window x:Class="ReringProject.UI.ReviewerWindow" ... Title="결과 리뷰어" Height="800" Width="1200" WindowStartupLocation="CenterOwner">`. ProcessMonitorWindow.xaml 구조 답습. 레이아웃 (Grid 2열):
- 좌측 패널: `[날짜 폴더 열기]` Button (x:Name=btn_loadFolder, Click=Button_LoadFolder_Click) + cycle `ListBox` (x:Name=listBox_cycles, SelectionChanged=CycleList_SelectionChanged, ItemTemplate=TextBlock Binding DisplayText)
- 중앙: `HalconViewerControl` 인스턴스 (x:Name=halconViewer) — xmlns 등록 후 배치 (라이브 경로와 독립 인스턴스, RESEARCH Anti-Pattern: MainView halconViewer 재사용 금지)
- 우측/하단: 측정 `DataGrid` (x:Name=dataGrid_measurements, AutoGenerateColumns=False, CanUserAddRows=False) 컬럼 = Shot/FAI/측정명(MeasurementName)/Nominal(NominalValue)/Tol+(TolerancePlus)/Tol-(ToleranceMinus)/측정값(ResultDisplay)/판정(JudgeText)
- (Plan 04 가 추가할 `[엑셀 export]` 버튼 자리 비워둘 필요 없음 — Plan 04 가 XAML 에 추가)

ReviewMeasurementRow.cs + ReviewerWindow.xaml(.cs) 를 csproj 에 등록 (Compile + Page).
  </action>
  <verify>
    <automated>findstr /C:"HalconViewerControl" /C:"listBox_cycles" /C:"dataGrid_measurements" WPF_Example\UI\Reviewer\ReviewerWindow.xaml</automated>
  </verify>
  <acceptance_criteria>
    - ReviewMeasurementRow.cs 에 ShotName/FAIName/MeasurementName/NominalValue/TolerancePlus/ToleranceMinus/ResultDisplay/JudgeText 8필드 + MeasurementResultDto 받는 생성자 존재
    - JudgeText 에 "DETECT FAIL" 3분기 로직 존재
    - ReviewerWindow.xaml 에 `x:Class="ReringProject.UI.ReviewerWindow"`, HalconViewerControl, listBox_cycles, dataGrid_measurements 존재
    - DataGrid 컬럼 8개 (Shot~판정)
    - csproj 에 ReviewMeasurementRow.cs Compile + ReviewerWindow.xaml Page + ReviewerWindow.xaml.cs Compile 등록
  </acceptance_criteria>
  <done>그리드 행 DTO + 리뷰어 XAML 레이아웃 + csproj 등록</done>
</task>

<task type="auto">
  <name>Task 2: ReviewerWindow.xaml.cs — 폴더 선택 + cycle 스캔 + 재렌더 로직</name>
  <files>WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/Device/DeviceSelector.xaml.cs:250-263 (Ookii VistaFolderBrowserDialog 패턴 — exact 복사)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs:243-262 (RenderStoredOverlaysForFai 재렌더 패턴 — exact)
    - WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs:112-170 (LoadImage/SetInspectionOverlays 시그니처)
    - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs (Load 시그니처 — Plan 01)
    - WPF_Example/UI/ViewModel/CycleResultDto.cs
    - .planning/phases/40-export-i-1-2026-06-01/40-RESEARCH.md (Code Examples: cycle 폴더 스캔, Pitfall 6, Security Domain V5)
  </read_first>
  <action>
namespace `ReringProject.UI`, `public partial class ReviewerWindow : Window`. `using System;` + `using System.Collections.Generic;` + `using System.IO;` + `using System.Linq;` + `using System.Windows;` + `using ReringProject.Halcon.Models;` (EdgeInspectionOverlay) + `using ReringProject.Sequence;` (CycleResultSerializer). 클래스 상단 `//260601 hbk Phase 40 OUT-01` 마커.

필드: `private CycleResultDto _currentCycle;`. 생성자 `ReviewerWindow() { InitializeComponent(); }`.

cycle 목록 항목 클래스 (파일 내 nested 또는 동일 파일):
```csharp
public class CycleListItem { public string FolderPath { get; set; } public string DisplayText { get; set; } public override string ToString(){return DisplayText;} }
```

1. **Button_LoadFolder_Click**: DeviceSelector 패턴 — `VistaFolderBrowserDialog { Multiselect=false, SelectedPath=SystemHandler.Handle.Setting.ResultSavePath }`. `if ((bool)dlg.ShowDialog()) LoadCycleFolders(dlg.SelectedPath);`

2. **LoadCycleFolders(string dateFolderPath)** (security T-40-07 V5 — 경로 검증):
```csharp
try {
    if (string.IsNullOrEmpty(dateFolderPath) || !Directory.Exists(dateFolderPath)) { listBox_cycles.ItemsSource = null; return; }
    var items = Directory.GetDirectories(dateFolderPath)
        .Where(d => File.Exists(Path.Combine(d, "cycle.json")))
        .OrderByDescending(d => d)
        .Select(d => { var dto = CycleResultSerializer.Load(Path.Combine(d, "cycle.json"));
                       return new CycleListItem { FolderPath = d,
                           DisplayText = dto != null ? dto.InspectionTime.ToString("HH:mm:ss") + "  " + dto.OverallJudgement : Path.GetFileName(d) }; })
        .ToList();
    listBox_cycles.ItemsSource = items;
} catch (Exception ex) { try { Logging.PrintErrLog((int)ELogType.Error, "[Reviewer] LoadCycleFolders: " + ex.Message); } catch { } }
```
손상 cycle.json → Load 가 null 반환 → DisplayText 폴백 (크래시 없음). 빈/없는 폴더 → 빈 목록.

3. **CycleList_SelectionChanged**: 선택된 CycleListItem → `_currentCycle = CycleResultSerializer.Load(Path.Combine(item.FolderPath, "cycle.json"));` null 가드 시 빈 상태 (이미지 클리어 + 빈 그리드). 그 후 첫 Shot/첫 FAI 자동 표시 또는 측정표 전체 빌드.

4. **측정표 + 재렌더 빌드** (MainView.xaml.cs:243-262 패턴):
```csharp
private void DisplayCycle(CycleResultDto cycle) {
    if (cycle == null || cycle.Shots == null) { halconViewer.SetInspectionOverlays(new List<EdgeInspectionOverlay>()); dataGrid_measurements.ItemsSource = null; return; }
    // 측정표: 전 Shot/FAI/Measurement flatten → ReviewMeasurementRow
    var rows = new List<ReviewMeasurementRow>();
    foreach (var shot in cycle.Shots) foreach (var fai in shot.FAIs) foreach (var m in (fai.Measurements ?? new List<MeasurementResultDto>()))
        rows.Add(new ReviewMeasurementRow(shot.ShotName, fai.FAIName, m));
    dataGrid_measurements.ItemsSource = rows;
    // 이미지 + overlay 재렌더 — 첫 Shot/첫 FAI 기준 (LoadImage → SetInspectionOverlays 순서, Pitfall 6)
    var firstShot = cycle.Shots.FirstOrDefault();
    if (firstShot != null && !string.IsNullOrEmpty(firstShot.ResultImagePath) && File.Exists(firstShot.ResultImagePath))
        halconViewer.LoadImage(firstShot.ResultImagePath);
    var allOverlays = cycle.Shots.SelectMany(s => s.FAIs).Where(f => f.LastOverlays != null).SelectMany(f => f.LastOverlays).ToList();
    halconViewer.SetInspectionOverlays(allOverlays);
}
```
(Shot 별 이미지 전환을 더 정교하게 하고 싶으면 listBox_cycles 옆에 Shot 선택 ListBox 추가는 discretion — POC 범위에서는 첫 Shot 이미지 + 전 overlay 재렌더로 충분. 단 측정표는 전 Shot 포함.)

ReviewMeasurementRow 생성자에 faiName 인자가 필요하므로 Task 1 의 생성자 시그니처를 `(string shotName, string faiName, MeasurementResultDto m)` 로 맞출 것.

Logging/ELogType 는 `using ReringProject.Utility;` 또는 fully-qualified.
  </action>
  <verify>
    <automated>findstr /C:"VistaFolderBrowserDialog" /C:"CycleResultSerializer.Load" /C:"SetInspectionOverlays" /C:"Directory.Exists" WPF_Example\UI\Reviewer\ReviewerWindow.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - Button_LoadFolder_Click 에 VistaFolderBrowserDialog + SelectedPath=ResultSavePath
    - LoadCycleFolders 에 `Directory.Exists` 가드 + cycle.json 필터 + OrderByDescending
    - CycleList_SelectionChanged → CycleResultSerializer.Load → DisplayCycle
    - DisplayCycle 에 LoadImage → SetInspectionOverlays 순서 (Pitfall 6) + File.Exists 가드
    - cycle==null / Shots==null 분기에서 빈 overlay/null 그리드 (크래시 없음)
    - msbuild Debug/x64 exits 0, 신규 warning 0
  </acceptance_criteria>
  <done>폴더 선택 + cycle 스캔 + 재렌더 + 측정표 + 예외격리 + 빌드 PASS</done>
</task>

<task type="auto">
  <name>Task 3: MainWindow EPageType.Reviewer + MenuBar 리뷰어 버튼 wiring</name>
  <files>WPF_Example/MainWindow.xaml.cs, WPF_Example/UI/MenuBar.xaml, WPF_Example/UI/MenuBar.xaml.cs</files>
  <read_first>
    - WPF_Example/MainWindow.xaml.cs:20-30 (EPageType enum), 68 (mProcMonitorWindow 필드), 327-337 (ProcessMonitor case 비모달 Show)
    - WPF_Example/UI/MenuBar.xaml.cs:96-101 (Button_CI_Click → PopupView)
    - WPF_Example/UI/MenuBar.xaml (버튼 배치 — 기존 ProcessMonitor/CI 버튼 인접)
    - .planning/phases/40-export-i-1-2026-06-01/40-PATTERNS.md (MainWindow EPageType + PopupView 패턴, MenuBar 패턴)
  </read_first>
  <action>
**MainWindow.xaml.cs**:
- EPageType enum (L20-30) 에 `Reviewer,` 추가 (ProcessMonitor 다음). `//260601 hbk Phase 40 OUT-01 D-08` 마커.
- 필드 추가 (L68 인접): `private UI.ReviewerWindow mReviewerWindow; //260601 hbk Phase 40 OUT-01`
- PopupView switch 에 case 추가 (ProcessMonitor case 답습, 비모달 Show):
```csharp
case EPageType.Reviewer:   //260601 hbk Phase 40 OUT-01 D-08
    if (mReviewerWindow != null && mReviewerWindow.IsLoaded) { mReviewerWindow.Show(); return; }
    mReviewerWindow = new UI.ReviewerWindow();
    mReviewerWindow.Owner = this;
    mReviewerWindow.Show();   // ShowDialog 아님 — 비모달, 라이브 검사 방해 안 함 (D-08)
    break;
```

**MenuBar.xaml**: 기존 ProcessMonitor/Setting 버튼 인접에 리뷰어 버튼 추가 — `<Button x:Name="Button_Reviewer" Content="결과 리뷰어" Click="Button_Reviewer_Click" .../>` (기존 버튼 스타일/크기 답습).

**MenuBar.xaml.cs**: Button_CI_Click 패턴 답습 (L96-101):
```csharp
private void Button_Reviewer_Click(object sender, RoutedEventArgs e) {   //260601 hbk Phase 40 OUT-01
    mParentWindow.PopupView(EPageType.Reviewer);
}
```
리뷰어는 읽기 전용 뷰어이므로 IsEditable 게이팅에 추가하지 않음 (검사 중에도 접근 가능 — PATTERNS.md IsEditable 섹션).

ReviewerWindow 네임스페이스가 `ReringProject.UI` 이므로 MainWindow(`ReringProject`)에서 `UI.ReviewerWindow` 로 참조 (using 충돌 회피).
  </action>
  <verify>
    <automated>findstr /C:"EPageType.Reviewer" WPF_Example\MainWindow.xaml.cs && findstr /C:"Button_Reviewer_Click" WPF_Example\UI\MenuBar.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - MainWindow.xaml.cs EPageType enum 에 `Reviewer` 멤버 존재
    - MainWindow.xaml.cs 에 `mReviewerWindow` 필드 + `case EPageType.Reviewer` 비모달 Show 분기 (ShowDialog 아님)
    - MenuBar.xaml 에 Button_Reviewer (Click=Button_Reviewer_Click)
    - MenuBar.xaml.cs 에 `Button_Reviewer_Click` → `PopupView(EPageType.Reviewer)`
    - msbuild Debug/x64 exits 0, 신규 warning 0
  </acceptance_criteria>
  <done>메뉴 버튼 → 비모달 리뷰어 Window wiring + 빌드 PASS</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>결과 리뷰어 Window 를 구축했습니다. 메뉴의 [결과 리뷰어] 버튼으로 비모달 창을 열고, 날짜 폴더 선택 → cycle 목록 → cycle 선택 시 Plan 01 의 cycle.json 을 역직렬화하여 결과 이미지 + overlay 를 라이브 검사와 동일하게 재렌더하고 측정표를 표시합니다.</what-built>
  <how-to-verify>
    1. SIMUL_MODE 빌드 후 앱 실행 → 먼저 검사 1회 실행하여 `./Result/{오늘날짜}/{시각}_cycle/cycle.json` 생성 (Plan 01)
    2. 메뉴 [결과 리뷰어] 버튼 클릭 → 별도 창이 열리고 라이브 화면이 가려지지 않는지(비모달) 확인
    3. [날짜 폴더 열기] → 오늘 날짜 폴더 선택 → cycle 목록에 "시각 + OK/NG/DETECT_FAIL" 표시 확인
    4. cycle 선택 → 중앙 뷰어에 결과 이미지 + 에지/overlay 가 라이브 검사 화면과 동일하게 재현되는지 확인 (녹/적 에지 + 청록 DistLine 등)
    5. 우측/하단 측정표에 Shot/FAI/측정명/nominal/tol/측정값/판정 행이 표시되는지 확인
    6. (엣지) 손상된 cycle.json(임의 텍스트로 덮어쓰기) 선택 시 크래시 없이 빈 상태로 처리되는지 확인
  </how-to-verify>
  <resume-signal>재현이 라이브와 동일하면 "approved", 차이/문제 발견 시 구체적으로 기술</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 → 폴더 선택 | Ookii 다이얼로그로 디스크 폴더 경로 입력 |
| 디스크 cycle.json → 리뷰어 | 저장된 결과 파일 역직렬화 (손상/변조 가능) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-07 | Tampering | LoadCycleFolders 폴더 경로 | mitigate | Ookii 다이얼로그(OS 네이티브) + `Directory.Exists` 가드. 잘못된/없는 경로 → 빈 목록, 크래시 없음 (ASVS V5) |
| T-40-08 | Denial of Service | 손상 cycle.json 역직렬화 | mitigate | CycleResultSerializer.Load 가 try/catch → null (Plan 01 TypeNameHandling.None). 리뷰어는 null/빈 Shots 분기로 빈 상태 처리 — unhandled exception 0 |
| T-40-09 | Tampering | 결과 이미지 경로 로드 | mitigate | LoadImage 전 `File.Exists` 가드 — 누락 이미지 시 overlay 만 렌더, 크래시 없음 |
| T-40-10 | Information Disclosure | 리뷰어 접근 권한 | accept | 리뷰어는 읽기 전용 뷰어. 기존 IsEditable 권한 체계 하 — 검사 중에도 결과 검토 허용 (POC 의도) |
</threat_model>

<verification>
- msbuild Debug/x64 Rebuild PASS (0 errors, 신규 warning 0)
- 메뉴 버튼 → 비모달 리뷰어 Window 열림
- 날짜 폴더 → cycle 목록 → cycle 선택 → 이미지 + overlay + 측정표 재현 (체크포인트 육안 검증)
- 손상 cycle.json 크래시 없음
</verification>

<success_criteria>
- 리뷰어 Window 가 cycle.json 을 라이브 검사와 동일하게 재현 (OUT-01 success criteria)
- 날짜 폴더 → cycle 목록(시각·종합판정) → 선택 UX (D-09)
- 비모달 별도 Window, 라이브 방해 없음 (D-08)
</success_criteria>

<output>
After completion, create `.planning/phases/40-export-i-1-2026-06-01/40-03-SUMMARY.md`
</output>
