---
phase: 40.1-uat-ui-3-overlay-on-off-shot-polygon-roi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
  - WPF_Example/UI/ContentItem/MainView.xaml
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: false
requirements: []
must_haves:
  truths:
    - "이미지 뷰어 툴바에 '측정 overlay' 와 'Datum 라인' 체크박스 2개가 있고, 둘 다 기본 ON 이다"
    - "'측정 overlay' 체크 해제 시 FAI 측정 overlay(녹/적 에지 + 청록 DistLine) 가 캔버스에서 사라진다"
    - "'Datum 라인' 체크 해제 시 Datum 검출 수평/수직 라인 + 십자가 캔버스에서 사라진다"
    - "두 토글은 독립적이다 (4가지 조합 모두 정상)"
    - "토글 변경 시 현재 화면이 즉시 재렌더된다 (노드 재선택 불필요)"
    - "Polygon ROI 생성 버튼이 UI 에서 보이지 않는다"
    - "기존 Polygon ROI 보유 레시피 로드 시 측정/렌더 회귀가 0 이다 (RoiShape.Polygon enum + HalconDisplayService Polygon 분기 보존)"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs"
      provides: "측정/Datum overlay 가시성 게이트 플래그 2개 + 공개 setter 2개 + RenderNow 게이팅"
      contains: "SetMeasurementOverlayVisible"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml"
      provides: "overlay 토글 체크박스 2개 (기본 IsChecked=true) + btn_polygonRoi Visibility=Collapsed"
      contains: "chk_overlayMeasure"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "체크박스 Checked/Unchecked 핸들러 → halconViewer setter 호출"
      contains: "Chk_overlayMeasure"
  key_links:
    - from: "MainView.xaml chk_overlayMeasure"
      to: "MainResultViewerControl.SetMeasurementOverlayVisible"
      via: "Checked/Unchecked event handler"
      pattern: "SetMeasurementOverlayVisible"
    - from: "MainResultViewerControl.RenderNow"
      to: "_displayService.Render / RenderDatumOverlay"
      via: "게이트 플래그 분기"
      pattern: "_measurementOverlayVisible|_datumOverlayVisible"
---

<objective>
이미지 뷰어 overlay On/Off 토글 (#2) 과 Polygon ROI UI 숨김 (#4) 을 구현한다. 둘 다 MainView 툴바 영역의 라이브 검사 화면 UI 변경이며 같은 파일(MainView.xaml)을 건드리므로 한 plan 으로 묶는다.

Purpose: 라이브 검사 화면에서 측정 overlay / Datum 라인을 각각 켜고 끌 수 있게 하고, 더 이상 쓰지 않는 Polygon ROI 생성 진입점을 숨겨 작업자 혼동을 줄인다.
Output: MainResultViewerControl 게이트 플래그 + setter, MainView 체크박스 2개 + 핸들러, btn_polygonRoi Collapsed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40.1-uat-ui-3-overlay-on-off-shot-polygon-roi/40.1-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- 실제 코드에서 추출 — executor 는 이 계약을 직접 사용, 추가 탐색 불필요 -->

MainResultViewerControl.xaml.cs (= MainView 의 halconViewer 필드 타입):
- RenderNow() (private, L740~) 에서 두 overlay 군이 렌더된다:
  - 측정 overlay: L753-760 `_displayService.Render(ViewerHost.HalconWindow, CurrentImage, _rois, _selectedRoiId, _rectDraftRoi, _inspectionOverlays.Concat(BuildTransientOverlays()).ToList(), _displayMessages.Concat(BuildTransientMessages()).ToList());`
  - Datum 라인: L778-781 `if (_datumConfig != null) { _displayService.RenderDatumOverlay(ViewerHost.HalconWindow, _datumConfig, _datumSelected, _datumIsEditMode); }`
- private void Render() (L729) 는 _renderPending 가드 후 Dispatcher.BeginInvoke(RenderNow) — 모든 SetXxx setter 가 이 Render() 호출로 즉시 갱신.
- 기존 setter 패턴 (L618 SetDatumOverlay, L661 SetDatumFindResultOverlay): 필드 대입 후 Render() 호출. 신규 setter 도 이 패턴 따름.
- 파일 brace 스타일 = Allman (opening brace 자기 줄).

MainView.xaml.cs:
- `halconViewer` 는 MainResultViewerControl 인스턴스 (XAML x:Name).
- RenderInspectionResultForNode / HighlightSelectedRoi / RenderStoredOverlaysForFai 등 모든 overlay 경로가 결국 halconViewer.SetInspectionOverlays/UpdateDisplayState/SetDatumOverlay → Render() → RenderNow() 로 수렴. → RenderNow 게이팅 한 곳이 라이브 + 노드 재현 경로 양쪽 모두 커버.
- 파일 brace 스타일 = Allman.

MainView.xaml:
- btn_polygonRoi: L156-160 `<ToggleButton x:Name="btn_polygonRoi" Content="Polygon ROI" Click="PolygonRoiButton_Click" Style="{StaticResource RoiToggleButtonStyle}" .../>` — canvasToolbar Border(L141) > Grid > StackPanel Grid.Column="0" (L149) 안에 위치.
- 토글 체크박스를 추가할 위치: 같은 StackPanel(Grid.Column="0") 내 btn_calibrate 뒤, 또는 Separator 뒤. 기존 다크 테마(Foreground 흰색 계열) 답습.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MainResultViewerControl 에 overlay 가시성 게이트 플래그 + setter 추가 및 RenderNow 게이팅</name>
  <read_first>
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs — L599-624 (_datumConfig/_datumSelected/_datumIsEditMode 필드 + SetDatumOverlay setter 패턴), L729-797 (Render/RenderNow 구조, 측정 overlay L753-760 + Datum 라인 L778-781).
    - CLAUDE.md — Allman 스타일 (이 파일), 주석 //YYMMDD hbk, C# 7.2.
  </read_first>
  <action>
    1. 필드 2개 추가 (기존 _datumIsEditMode 필드 근처, L602 인근). 기본값 true (기본 ON):
       `private bool _measurementOverlayVisible = true; //260601 hbk Phase 40.1 #2 — 측정 overlay 토글 게이트 (기본 ON)`
       `private bool _datumOverlayVisible = true; //260601 hbk Phase 40.1 #2 — Datum 라인 토글 게이트 (기본 ON)`
    2. 공개 setter 2개 추가 (기존 SetDatumOverlay L618 근처, Allman 스타일, 필드 대입 후 Render() 호출 — 즉시 재렌더):
       ```
       //260601 hbk Phase 40.1 #2 — 측정 overlay 가시성 토글 (MainView 체크박스에서 호출). 즉시 재렌더.
       public void SetMeasurementOverlayVisible(bool visible)
       {
           _measurementOverlayVisible = visible;
           Render();
       }
       //260601 hbk Phase 40.1 #2 — Datum 라인 가시성 토글 (MainView 체크박스에서 호출). 즉시 재렌더.
       public void SetDatumOverlayVisible(bool visible)
       {
           _datumOverlayVisible = visible;
           Render();
       }
       ```
    3. RenderNow 게이팅 — 측정 overlay (L753-760 의 _displayService.Render 호출): inspectionOverlays 인자를 게이트로 분기. OFF 시 빈 리스트 전달하여 측정 overlay 만 미렌더 (rois/messages/draft 는 보존 — ROI 하이라이트/메시지는 유지). 구현:
       ```
       //260601 hbk Phase 40.1 #2 — 측정 overlay 토글 OFF 시 빈 리스트 (rois/messages 보존)
       var measOverlays = _measurementOverlayVisible
           ? _inspectionOverlays.Concat(BuildTransientOverlays()).ToList()
           : new System.Collections.Generic.List<EdgeInspectionOverlay>();
       _displayService.Render(
           ViewerHost.HalconWindow,
           CurrentImage,
           _rois,
           _selectedRoiId,
           _rectDraftRoi,
           measOverlays,
           _displayMessages.Concat(BuildTransientMessages()).ToList());
       ```
       (기존 L759 의 인라인 Concat 인자를 measOverlays 지역변수로 교체. C# 7.2 — 삼항 OK. EdgeInspectionOverlay 의 fully-qualified 네임스페이스는 기존 파일 using 으로 단순명 사용 가능하면 단순명, 아니면 ReringProject.Halcon.Models.EdgeInspectionOverlay 사용.)
    4. RenderNow 게이팅 — Datum 라인 (L778-781): 기존 `if (_datumConfig != null)` 조건에 `&& _datumOverlayVisible` 추가:
       `if (_datumConfig != null && _datumOverlayVisible) //260601 hbk Phase 40.1 #2 — Datum 라인 토글`
    5. anti-goal 준수: SetInspectionOverlays/UpdateDisplayState/RenderDatumOverlay 내부 로직 무변경, 측정 알고리즘 무변경. 게이트는 RenderNow 표시 시점에서만 적용 (데이터는 그대로 유지 → 토글 ON 복귀 시 즉시 재표시).
  </action>
  <verify>
    <automated>& 'C:\Program Files\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe' 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' /t:Build /p:Configuration=Debug /p:Platform=x64</automated>
  </verify>
  <acceptance_criteria>
    - MainResultViewerControl.xaml.cs 에 `private bool _measurementOverlayVisible = true;` 및 `private bool _datumOverlayVisible = true;` 두 필드 존재 (grep).
    - `public void SetMeasurementOverlayVisible(bool` 및 `public void SetDatumOverlayVisible(bool` 두 메서드 존재, 각 본문에 `Render();` 호출 포함 (grep).
    - RenderNow 의 Datum 라인 분기가 `if (_datumConfig != null && _datumOverlayVisible)` 로 변경됨 (grep).
    - 측정 overlay 게이트가 `_measurementOverlayVisible ?` 삼항으로 _displayService.Render 의 overlays 인자에 적용됨 (grep `measOverlays`).
    - msbuild Debug/x64 exits 0, 신규 warning 0 (기존 baseline 6 warnings 유지).
    - SetInspectionOverlays / UpdateDisplayState / RenderDatumOverlay 메서드 본문 무변경 (게이트는 RenderNow 에서만).
  </acceptance_criteria>
  <done>두 게이트 플래그 + setter 가 존재하고 RenderNow 가 두 overlay 군을 독립 게이팅하며 빌드 통과.</done>
</task>

<task type="auto">
  <name>Task 2: MainView.xaml 에 overlay 토글 체크박스 2개 추가(기본 ON) + 핸들러 + btn_polygonRoi 숨김</name>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml — L140-239 (canvasToolbar Border + Grid.Column="0" StackPanel, btn_polygonRoi L156-160, btn_calibrate L222 끝 L239 StackPanel 닫힘).
    - WPF_Example/UI/ContentItem/MainView.xaml.cs — L200-223 (halconViewer 호출 패턴, RenderInspectionResultForNode), 파일 Allman 스타일.
    - Task 1 의 setter 시그니처: halconViewer.SetMeasurementOverlayVisible(bool) / halconViewer.SetDatumOverlayVisible(bool).
    - CLAUDE.md — XAML 다크 테마, 주석 //YYMMDD hbk.
  </read_first>
  <action>
    #4 Polygon 숨김:
    1. MainView.xaml btn_polygonRoi (L156-160) 에 `Visibility="Collapsed"` 속성 추가 + 라인에 주석 `<!--260601 hbk Phase 40.1 #4 — Polygon ROI 생성 진입점 숨김 (RoiShape.Polygon enum + 렌더 분기 보존, 데이터 호환)-->`. PolygonRoiButton_Click 핸들러 / CompletePolygon / HalconViewer_Polygon* code-behind 는 보존 (anti-goal: 완전 제거 아님, Rule 3 회귀 방지 — label_pointCount L244 선례와 동일).

    #2 overlay 토글 체크박스:
    2. MainView.xaml 의 Grid.Column="0" StackPanel(L149) 내, btn_calibrate(L222-238) 뒤 또는 그 앞 Separator 인근에 CheckBox 2개 추가. 다크 테마(Foreground 흰색 계열) 답습. 기본 IsChecked="True":
       ```
       <!--260601 hbk Phase 40.1 #2 — overlay On/Off 토글 (기본 ON). 측정 overlay / Datum 라인 독립.-->
       <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="4,4"/>
       <CheckBox x:Name="chk_overlayMeasure" Content="측정 overlay"
           IsChecked="True"
           Checked="Chk_overlayMeasure_Changed" Unchecked="Chk_overlayMeasure_Changed"
           Foreground="White" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="12"/>
       <CheckBox x:Name="chk_overlayDatum" Content="Datum 라인"
           IsChecked="True"
           Checked="Chk_overlayDatum_Changed" Unchecked="Chk_overlayDatum_Changed"
           Foreground="White" VerticalAlignment="Center" Margin="0,0,4,0" FontSize="12"/>
       ```
       (정확한 배치/스타일/라벨 문구는 Claude 재량 — 기존 MainView 스타일 답습. 라벨은 한글 "측정 overlay" / "Datum 라인" 권장.)
    3. MainView.xaml.cs 에 핸들러 4개(또는 공유 2개) 추가, Allman 스타일. halconViewer null 가드 후 setter 호출. 단일 핸들러를 Checked/Unchecked 공용으로 사용 (CheckBox.IsChecked nullable bool):
       ```
       //260601 hbk Phase 40.1 #2 — 측정 overlay 토글 → 뷰어 게이트 갱신 (즉시 재렌더)
       private void Chk_overlayMeasure_Changed(object sender, RoutedEventArgs e)
       {
           if (halconViewer == null) return;
           halconViewer.SetMeasurementOverlayVisible(chk_overlayMeasure.IsChecked == true);
       }
       //260601 hbk Phase 40.1 #2 — Datum 라인 토글 → 뷰어 게이트 갱신 (즉시 재렌더)
       private void Chk_overlayDatum_Changed(object sender, RoutedEventArgs e)
       {
           if (halconViewer == null) return;
           halconViewer.SetDatumOverlayVisible(chk_overlayDatum.IsChecked == true);
       }
       ```
    4. 상태 보존: 세션 내 메모리 플래그(MainResultViewerControl 필드)로 충분 — INI 영속화 없음 (CONTEXT Claude 재량, 단순 우선). 별도 초기화 불요 — 게이트 필드 기본값 true 가 기본 ON 을 보장하고, 체크박스 IsChecked="True" 와 일치.
  </action>
  <verify>
    <automated>& 'C:\Program Files\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe' 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' /t:Build /p:Configuration=Debug /p:Platform=x64</automated>
  </verify>
  <acceptance_criteria>
    - MainView.xaml 에 `x:Name="chk_overlayMeasure"` 및 `x:Name="chk_overlayDatum"` CheckBox 2개 존재, 둘 다 `IsChecked="True"` (grep).
    - 두 CheckBox 모두 Checked + Unchecked 이벤트가 핸들러에 wiring (grep `Chk_overlayMeasure_Changed`, `Chk_overlayDatum_Changed`).
    - MainView.xaml 의 btn_polygonRoi 요소에 `Visibility="Collapsed"` 존재 (grep, btn_polygonRoi 인근).
    - MainView.xaml.cs 에 `Chk_overlayMeasure_Changed` 및 `Chk_overlayDatum_Changed` 메서드 존재, 각각 halconViewer.SetMeasurementOverlayVisible / SetDatumOverlayVisible 호출 + null 가드 포함 (grep).
    - PolygonRoiButton_Click 핸들러 + CompletePolygon 메서드는 MainView.xaml.cs 에 그대로 존재 (제거 아님 — grep 으로 잔존 확인).
    - msbuild Debug/x64 exits 0, 신규 warning 0.
  </acceptance_criteria>
  <done>툴바에 체크박스 2개(기본 ON) + 핸들러 wiring 완료, btn_polygonRoi Collapsed, 빌드 통과.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    이미지 뷰어 툴바에 '측정 overlay' / 'Datum 라인' 체크박스 2개 (기본 ON) 추가 — 각각 OFF 시 해당 overlay 군만 캔버스에서 사라지고 즉시 재렌더. Polygon ROI 생성 버튼은 UI 에서 숨김 (데이터/렌더 분기는 보존).
  </what-built>
  <how-to-verify>
    1. 앱(.exe) 실행 중이면 종료 후 빌드 (MSB3027 잠금 방지), DatumMeasurement.exe 실행.
    2. SIMUL_MODE 로 검사 레시피 로드 → Shot/FAI/Measurement 노드 선택하여 측정 overlay(녹/적 에지 + 청록 DistLine) + Datum 검출 라인이 캔버스에 보이는지 확인 (기본 둘 다 ON).
    3. 툴바의 '측정 overlay' 체크 해제 → FAI 측정 overlay 만 사라지고 Datum 라인은 유지되는지 확인. 즉시 갱신(노드 재선택 불필요) 확인.
    4. '측정 overlay' 다시 체크 → overlay 즉시 복귀 확인.
    5. 'Datum 라인' 체크 해제 → Datum 수평/수직 라인 + 십자만 사라지고 측정 overlay 는 유지되는지 확인.
    6. 4가지 조합 (둘 다 ON / 측정만 / Datum만 / 둘 다 OFF) 모두 정상인지 확인.
    7. 라이브 검사(오토/Test Find) 실행 후 결과 overlay 에도 토글이 적용되는지 + 노드 클릭 재현 경로에도 적용되는지 확인.
    8. 툴바에 'Polygon ROI' 버튼이 더 이상 보이지 않는지 확인. (기존 Polygon ROI 보유 레시피가 있으면 로드 시 측정/렌더 정상인지 — 회귀 0)
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 발견된 문제 기술</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No external trust boundaries — UI-only local rendering toggle + 버튼 Visibility 변경. 신규 입력/직렬화/네트워크 경로 없음. overlay 데이터는 기존 인-메모리 객체이며 게이트는 표시 시점 분기일 뿐 데이터 변형 없음.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.1-01 | (N/A) | RenderNow overlay 게이트 | accept | 순수 로컬 렌더 토글 — 외부 입력/영속화 없음, 공격면 0 |
</threat_model>

<verification>
- msbuild Debug/x64 exits 0, 신규 warning 0 (baseline 6 유지).
- grep: chk_overlayMeasure / chk_overlayDatum / SetMeasurementOverlayVisible / SetDatumOverlayVisible / _measurementOverlayVisible / _datumOverlayVisible / btn_polygonRoi Visibility="Collapsed" 존재.
- anti-goal: RoiShape.Polygon enum + HalconDisplayService Polygon 분기 무변경, PolygonRoiButton_Click/CompletePolygon 잔존, 측정 알고리즘 무변경, 리뷰어(ReviewerWindow/별도 HalconViewerControl) 무변경.
- SIMUL UAT 4가지 토글 조합 + Polygon 버튼 미표시 사용자 승인.
</verification>

<success_criteria>
- 측정 overlay / Datum 라인 토글 2개가 독립적으로 동작하고 기본 ON, 변경 시 즉시 재렌더.
- 라이브 검사 렌더 경로 + 노드 클릭 재현 경로 양쪽에 토글 적용 (RenderNow 단일 게이트).
- Polygon ROI 생성 버튼 미표시, 데이터/렌더 호환 보존.
</success_criteria>

<output>
After completion, create `.planning/phases/40.1-uat-ui-3-overlay-on-off-shot-polygon-roi/40.1-01-SUMMARY.md`
</output>
