---
phase: 40.1-uat-ui-3-overlay-on-off-shot-polygon-roi
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ControlItem/NodeViewModel.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: false
requirements: []
must_haves:
  truths:
    - "라이브 검사 트리가 로드되면 기본적으로 Sequence > Shot/Datum 레벨까지만 펼쳐지고 그 하위 FAI/Measurement 는 접혀 있다"
    - "사용자가 Shot 노드를 펼치면 FAI/Measurement 상세가 표준 TreeView 펼치기로 표시된다"
    - "신규 Shot 추가/RebuildInspectionActions 후에도 기본 Shot 레벨 접기가 일관 적용된다 (또는 신규 Shot 펼침은 명시적 의도 유지)"
    - "노드 선택 기반 ROI 표시/하이라이트/PropertyGrid 전환은 펼침 상태와 무관하게 동작한다 (선택 로직 무변경)"
  artifacts:
    - path: "WPF_Example/UI/ControlItem/NodeViewModel.cs"
      provides: "Shot 레벨까지만 펼치는 ExpandToShotLevel() 메서드"
      contains: "ExpandToShotLevel"
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "ListView_Loaded / OnLoadRecipe 의 전체 ExpandAll → ExpandToShotLevel 교체 (라이브 경로)"
      contains: "ExpandToShotLevel"
  key_links:
    - from: "InspectionListView.ListView_Loaded"
      to: "NodeViewModel.ExpandToShotLevel"
      via: "트리 로드 후 기본 펼침 호출"
      pattern: "ExpandToShotLevel"
    - from: "InspectionListView.OnLoadRecipe"
      to: "NodeViewModel.ExpandToShotLevel"
      via: "레시피 재로드 후 기본 펼침 호출"
      pattern: "ExpandToShotLevel"
---

<objective>
Shot→FAI→Measurement 트리의 기본 펼침을 전체(ExpandAll)에서 Shot 레벨까지로 제한한다 (#3). 라이브 검사 화면 한정.

Purpose: 100개+ 검사 항목 트리가 처음부터 전부 펼쳐져 길어지는 문제를 해소 — 기본은 Sequence > Shot/Datum 까지만 보이고, 사용자가 필요한 Shot 만 펼쳐 FAI/Measurement 상세를 본다.
Output: NodeViewModel.ExpandToShotLevel() 신규 + InspectionListView 라이브 경로 2곳(ListView_Loaded, OnLoadRecipe)의 ExpandAll 교체.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40.1-uat-ui-3-overlay-on-off-shot-polygon-roi/40.1-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- 실제 코드에서 추출 -->

NodeViewModel.cs (namespace ReringProject.UI, K&R 브레이스 스타일 — 이 파일):
- public ENodeType NodeType { get; } — Node.NodeType 위임 (L152-156).
- public ObservableCollection<NodeViewModel> Children { get; } — lazy LoadChildren (L70-75).
- public bool IsExpanded { get; set; } — INPC (L160-170).
- public void ExpandAll() (L275-280): `this.IsExpanded = true; foreach (var child in this.Children) child.ExpandAll();` — 무조건 전부 펼침.
- public void ExpandParents() (L268-273): 부모만 펼침.
- 트리 계층: 루트(recipe) > Sequence > {Shot(=ENodeType.Action, Param is ShotConfig) | Datum(=ENodeType.Datum)} > FAI(=ENodeType.FAI) > Measurement(=ENodeType.Measurement).
  → "Shot 레벨까지만 펼침" = 루트 + Sequence 노드만 IsExpanded=true. Shot/Datum 노드는 보이되 IsExpanded=false (그 하위 FAI/Measurement 접힘). Shot/Datum 자체는 Sequence 가 펼쳐져 있으므로 가시.

ENodeType (ReringProject.Define): Sequence, Action(=Shot 포함), Datum, FAI, Measurement 등. (InspectionListView.xaml.cs 가 ENodeType.Sequence / Action / Datum / FAI / Measurement 사용 — L272, L448, L491, L528, L550, L573 참조.)

InspectionListView.xaml.cs (namespace ReringProject.UI, K&R 스타일):
- ListView_Loaded (L187-217): L205 `ViewModel.RootModel.ExpandAll();` — 라이브 트리 로드 후 전체 펼침. ← 교체 대상.
- OnLoadRecipe (L253-264): L263 `if (ViewModel.RootModel != null) ViewModel.RootModel.ExpandAll();` — 레시피 재로드 후 전체 펼침. ← 교체 대상.
- IsEditable setter (L161-185): L168 `ViewModel.RootModel.ExpandAll();` — 편집 모드 진입 시. ← 라이브 검사 화면 아님 (편집 모드) → CONTEXT 범위 "라이브 검사 화면 한정" 이므로 무변경 권장 (편집 시 전체 펼침 편의 유지).
- ViewModel.RootModel 타입 = NodeViewModel.
- 선택/하이라이트 로직 (InspectionList_SelectionChanged L397~) 은 펼침 상태와 독립 — 무변경 (회귀 가드).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NodeViewModel.ExpandToShotLevel() 추가 + InspectionListView 라이브 경로 2곳 교체</name>
  <read_first>
    - WPF_Example/UI/ControlItem/NodeViewModel.cs — L152-156 (NodeType), L268-280 (ExpandParents/ExpandAll 패턴), 파일 K&R 스타일.
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs — L187-217 (ListView_Loaded, L205 ExpandAll), L253-264 (OnLoadRecipe, L263 ExpandAll), L161-185 (IsEditable setter, L168 ExpandAll — 무변경 대상), L397-624 (InspectionList_SelectionChanged — 선택 로직, 무변경 회귀 가드).
    - ReringProject.Define ENodeType — Sequence / Action 멤버 확인 (Shot = Action 타입).
    - CLAUDE.md — K&R 스타일 (이 두 파일), 주석 //YYMMDD hbk, C# 7.2.
  </read_first>
  <action>
    1. NodeViewModel.cs 에 ExpandToShotLevel() 추가 (ExpandAll L275-280 인근, K&R 스타일). 동작: Sequence(및 루트) 레벨까지만 IsExpanded=true 로 펼치고, Shot/Datum 노드 이하는 펼치지 않음. 구현 방식 — "이 노드를 펼치되, 자식이 Shot/Measurement 상세 레벨이면 더 내려가지 않는다":
       ```
       //260601 hbk Phase 40.1 #3 — 기본 펼침을 Shot 레벨까지로 제한.
       //  루트(recipe) + Sequence 노드만 펼치고, 그 하위 Shot(Action)/Datum 노드는 접힘 상태 유지
       //  (FAI/Measurement 상세는 사용자가 Shot 을 펼쳐야 보임). 라이브 검사 트리 한정.
       public void ExpandToShotLevel() {
           // Sequence 노드 이하(Shot/Datum)는 펼치지 않는다 — 자기 자신만 펼치고 재귀 중단
           if (this.NodeType == ENodeType.Sequence) {
               this.IsExpanded = true;
               foreach (var child in this.Children) {
                   child.IsExpanded = false; // Shot/Datum 노드는 접힘
               }
               return;
           }
           // 루트(recipe) 등 Sequence 상위: 펼치고 자식(Sequence) 으로 재귀
           this.IsExpanded = true;
           foreach (var child in this.Children) {
               child.ExpandToShotLevel();
           }
       }
       ```
       (ENodeType 사용을 위해 파일 상단 `using ReringProject.Define;` 가 이미 있는지 확인 — L7 에 존재. 없으면 추가.)
    2. InspectionListView.xaml.cs L205 `ViewModel.RootModel.ExpandAll();` → `ViewModel.RootModel.ExpandToShotLevel(); //260601 hbk Phase 40.1 #3 — 기본 Shot 레벨 접기 (전체 ExpandAll → Shot 레벨)`.
    3. InspectionListView.xaml.cs L263 `if (ViewModel.RootModel != null) ViewModel.RootModel.ExpandAll();` → `if (ViewModel.RootModel != null) ViewModel.RootModel.ExpandToShotLevel(); //260601 hbk Phase 40.1 #3 — 레시피 재로드 후 Shot 레벨 접기`.
    4. IsEditable setter L168 의 ExpandAll 은 무변경 (편집 모드는 CONTEXT "라이브 검사 화면 한정" 범위 밖 — 편집 시 전체 펼침 편의 유지). SetSelectionChange(L382)/선택 로직(L397~) 무변경.
    5. anti-goal/회귀 가드: 노드 선택 기반 ROI 표시/하이라이트/PropertyGrid 전환은 펼침 상태와 무관하게 동작해야 함 — InspectionList_SelectionChanged / RenderInspectionResultForNode / HighlightSelectedRoi 경로 무변경. 접힌 하위 노드는 사용자가 펼친 뒤 선택.
    6. 신규 Shot 추가 시점(AddShotToSequence L877 `seqNode.IsExpanded = true;`): 이는 사용자가 방금 추가한 Shot 의 부모 Sequence 를 펼치는 명시적 의도 — 무변경 (신규 추가 가시성 유지). Shot 자체 하위는 기본 접힘 유지됨.
  </action>
  <verify>
    <automated>& 'C:\Program Files\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe' 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' /t:Build /p:Configuration=Debug /p:Platform=x64</automated>
  </verify>
  <acceptance_criteria>
    - NodeViewModel.cs 에 `public void ExpandToShotLevel()` 메서드 존재, 본문에 `ENodeType.Sequence` 분기 + `child.IsExpanded = false` 포함 (grep).
    - InspectionListView.xaml.cs 의 ListView_Loaded 와 OnLoadRecipe 두 경로가 `ExpandToShotLevel()` 호출로 변경됨 (grep, 정확히 2곳).
    - InspectionListView.xaml.cs IsEditable setter(L168 인근)의 `ExpandAll()` 은 그대로 존재 (편집 모드 무변경 — grep 으로 ExpandAll 잔존 확인).
    - InspectionList_SelectionChanged / HighlightSelectedRoi 관련 라인 무변경 (선택 로직 회귀 0).
    - msbuild Debug/x64 exits 0, 신규 warning 0.
  </acceptance_criteria>
  <done>ExpandToShotLevel() 신규 + 라이브 경로 2곳 교체 완료, 편집 모드/선택 로직 무변경, 빌드 통과.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    라이브 검사 트리의 기본 펼침을 Shot 레벨까지로 제한 — Sequence > Shot/Datum 노드만 보이고 FAI/Measurement 는 접힘. 사용자가 Shot 을 펼치면 상세 표시. 편집 모드/선택 로직은 무변경.
  </what-built>
  <how-to-verify>
    1. 앱(.exe) 실행 중이면 종료 후 빌드, DatumMeasurement.exe 실행.
    2. 검사 레시피 로드 → 트리가 Sequence > Shot/Datum 레벨까지만 펼쳐지고 FAI/Measurement 가 접혀 있는지 확인 (기본).
    3. 임의 Shot 노드를 펼쳐 FAI/Measurement 상세가 표시되는지 확인.
    4. 다른 레시피로 전환(OnLoadRecipe) 후에도 동일하게 Shot 레벨 접기가 적용되는지 확인.
    5. 신규 Shot 추가 후 트리 일관성 확인.
    6. 회귀 가드: 펼친 후 FAI/Measurement/Datum 노드 선택 시 ROI 하이라이트 / 측정 결과 표시 / PropertyGrid 전환이 정상 동작하는지 확인.
    7. (선택) 편집 모드(IsEditable) 진입 시 기존처럼 전체 펼침이 유지되는지 확인.
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 발견된 문제 기술</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No external trust boundaries — UI-only TreeView 기본 펼침 상태 변경. 신규 입력/직렬화/네트워크 경로 없음.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.1-02 | (N/A) | ExpandToShotLevel / 트리 기본 펼침 | accept | 순수 로컬 UI 상태 토글 — 데이터 변형/외부 입력 없음, 공격면 0 |
</threat_model>

<verification>
- msbuild Debug/x64 exits 0, 신규 warning 0.
- grep: ExpandToShotLevel 정의(NodeViewModel.cs) + 호출 2곳(InspectionListView.xaml.cs ListView_Loaded/OnLoadRecipe).
- IsEditable setter 의 ExpandAll 잔존 (편집 모드 무변경).
- 선택 로직(InspectionList_SelectionChanged / HighlightSelectedRoi / RenderInspectionResultForNode) 무변경.
- SIMUL UAT: 기본 Shot 레벨 접기 + Shot 펼침 시 상세 + 선택 동작 정상 사용자 승인.
</verification>

<success_criteria>
- 라이브 트리가 기본 Shot 레벨까지만 펼쳐지고 사용자 펼침 시 상세 표시.
- 노드 선택 ROI/하이라이트/PropertyGrid 동작 회귀 0.
- 신규 Shot/레시피 재로드 후 일관 적용, 편집 모드 무변경.
</success_criteria>

<output>
After completion, create `.planning/phases/40.1-uat-ui-3-overlay-on-off-shot-polygon-roi/40.1-02-SUMMARY.md`
</output>
