---
phase: 40.1-uat-ui-3-overlay-on-off-shot-polygon-roi
plan: "02"
subsystem: live-inspection-tree
tags: [wpf, treeview, expand-state, login-logout, uat]
dependency_graph:
  requires: []
  provides:
    - NodeViewModel.ExpandToShotLevel() (Sequence 레벨까지만 펼침)
    - NodeViewModel.CollapseAll() (트리 전체 접기)
    - InspectionListView 첫 로드 1회 가드 + 로그인/로그아웃 연동 펼침
  affects:
    - WPF_Example/UI/ControlItem/NodeViewModel.cs
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
tech_stack:
  added: []
  patterns:
    - _initialExpandApplied 가드로 세션 첫 로드 1회만 기본 펼침
    - IsEditable setter = 로그인/로그아웃 훅 (MainWindow.IsEditable ← OnLoginStateChanged)
key_files:
  created: []
  modified:
    - WPF_Example/UI/ControlItem/NodeViewModel.cs
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
decisions:
  - "ExpandToShotLevel(): NodeType==Sequence 분기에서 자기만 펼치고 자식(Shot/Datum) IsExpanded=false 후 재귀 중단, 상위(루트)는 펼치고 자식으로 재귀"
  - "첫 로드 1회만 적용(_initialExpandApplied 가드), OnLoadRecipe 강제 재접기 제거 → 재로드 시 사용자 펼침 상태 유지 (UAT 1차 피드백)"
  - "로그인(IsEditable=true)=ExpandToShotLevel(기존 ExpandAll 제거), 로그아웃(false)=CollapseAll (UAT 2차 피드백)"
  - "선택/하이라이트(InspectionList_SelectionChanged/HighlightSelectedRoi/RenderInspectionResultForNode) 무변경 — 회귀 가드"
metrics:
  completed_date: "2026-06-02"
  tasks_completed: 1
  files_changed: 2
status: complete
---

# Phase 40.1 Plan 02: 트리 기본 펼침 Shot 레벨 제한 Summary

**한 줄 요약:** 라이브 검사 트리 기본 펼침을 ExpandAll → Shot 레벨(ExpandToShotLevel)로 제한하고, 첫 로드 1회 가드 + 로그인 시 Shot 레벨/로그아웃 시 전체 접기(CollapseAll) 연동으로 100+ 항목 트리 가독성 개선. UAT PASS.

## Completed Tasks

| Task | 이름 | Commit | 핵심 파일 |
|------|------|--------|-----------|
| 1 | ExpandToShotLevel() 신규 + 라이브 경로 2곳 교체 | `b8cbaf6` | NodeViewModel.cs, InspectionListView.xaml.cs |
| 1+ (UAT) | 첫 로드 1회 가드 + 재로드 시 상태 유지 | `ab470eb` | InspectionListView.xaml.cs |
| 1+ (UAT) | 로그인 Shot레벨 / 로그아웃 전체 접기 + CollapseAll() | `de1dfe4` | NodeViewModel.cs, InspectionListView.xaml.cs |

## UAT 결과 (2026-06-02, SIMUL)

- ✅ 시작 시 Top/Bottom/Side Shot 레벨까지만, FAI/Measurement 접힘
- ✅ 로그인(관리자) 시 Shot 레벨 유지 (자동 ExpandAll 안 함)
- ✅ 로그인 상태에서 Shot 수동 펼침 → 하위 표시
- ✅ 로그아웃 시 트리 전체 접힘
- ✅ 레시피 재로드 시 펼침 상태 유지 (강제 재접기 없음)

## Self-Check: PASS

- msbuild Debug/x64 exit 0, 신규 warning 0 (baseline 유지)
- grep 확인: NodeViewModel.ExpandToShotLevel()/CollapseAll() 정의, InspectionListView 첫로드 가드 + IsEditable setter 로그인=ExpandToShotLevel/로그아웃=CollapseAll
- 선택 로직(InspectionList_SelectionChanged/HighlightSelectedRoi) 무변경 — 회귀 0
