---
phase: 40.2-fai-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ViewModel/CycleResultDto.cs
  - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
  - WPF_Example/Utility/CaptureImageSaveService.cs
  - WPF_Example/SystemHandler.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "검사 결과 DTO(FaiResultDto)가 원본 파일명·캡쳐 파일명 2필드를 보유한다"
    - "FAIConfig 가 캡쳐 파일명을 검사 시점에 write-back 받을 transient 필드 2개를 보유한다"
    - "캡쳐 PNG 를 비동기로 디스크에 저장하는 워커 서비스가 존재하고 SystemHandler 가 라이프사이클을 관리한다"
  artifacts:
    - path: "WPF_Example/UI/ViewModel/CycleResultDto.cs"
      provides: "FaiResultDto.OriginImageFileName / CaptureImageFileName"
      contains: "OriginImageFileName"
    - path: "WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs"
      provides: "FAIConfig.LastOriginImageFileName / LastCaptureImageFileName transient 필드"
      contains: "LastCaptureImageFileName"
    - path: "WPF_Example/Utility/CaptureImageSaveService.cs"
      provides: "비동기 캡쳐 저장 워커 + CaptureImageSaveRequest"
      contains: "class CaptureImageSaveService"
    - path: "WPF_Example/SystemHandler.cs"
      provides: "CaptureImageSaver 싱글턴 필드 + Start/Dispose 배선"
      contains: "CaptureImageSaver"
  key_links:
    - from: "SystemHandler.Initialize"
      to: "CaptureImageSaver.Start"
      via: "RawImageSaver.Start() 직후 미러"
      pattern: "CaptureImageSaver.*Start"
    - from: "CaptureImageSaveService.SaveRequest"
      to: "ResultSavePath\\Image\\{yyMMdd}\\{HHmm}\\original|capture"
      via: "Path.Combine(ResultSavePath, \"Image\", ...)"
      pattern: "ResultSavePath"
---

<objective>
Phase 40.2 의 foundation 계층을 구축한다. 검사 결과를 담는 DTO 에 원본/캡쳐 파일명 필드를 추가하고, FAIConfig 에 검사 시점에 파일명을 write-back 받을 transient 필드를 추가하며, 캡쳐 PNG 를 비동기로 디스크에 저장하는 워커 서비스(RawImageSaveService 복제)를 신규 작성하여 SystemHandler 라이프사이클에 배선한다.

Purpose: Wave 2(캡쳐 생성/enqueue)와 Wave 3(직렬화/엑셀)이 소비할 계약(DTO 필드, FAIConfig 필드, 저장 서비스 API)을 먼저 확정한다 (interface-first ordering).
Output: CycleResultDto.cs 수정, FAIConfig.cs 수정, CaptureImageSaveService.cs 신규, SystemHandler.cs 수정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40.2-fai-2/40.2-CONTEXT.md
@.planning/phases/40.2-fai-2/40.2-PATTERNS.md

<decisions_resolved>
<!-- planner 위임 결정 2건 해소 (코드 grep 근거) -->
- **저장 루트 = ResultSavePath** (코드 확인: SystemSetting.cs:63 `ResultSavePath = BaseDirectory\Result`, L60 `ImageSavePath = BaseDirectory\Image`). CONTEXT 폴더 `Result\Image\...` 는 ResultSavePath 하위에 "Image" 를 직접 Path.Combine 해야만 조립 가능. `GetLogSavePath(ELogType.Image,...)` 는 ImageSavePath 를 반환하므로 사용 금지.
  최종 baseDir = `Path.Combine(Setting.ResultSavePath, "Image", yyMMdd, HHmm, isCapture ? "capture" : "original")`.
- **파일명 타이밍 = 동기 결정 + 비동기 write 분리.** 파일명(origin_/capture_)은 호출 스레드(Action_FAIMeasurement)에서 동기 생성되어 fai.LastOriginImageFileName/LastCaptureImageFileName 에 즉시 write-back 된다(Wave 2). PNG write 만 이 서비스 워커가 비동기 수행. 따라서 BuildDto(AddResponse 시점)는 파일명이 이미 확정된 상태에서 읽는다. 이 plan 의 서비스는 파일명 생성 헬퍼(public static)도 제공하여 호출부가 동일 규칙으로 파일명을 만들 수 있게 한다.
</decisions_resolved>

<interfaces>
<!-- 복제 대상 원본 서비스 (그대로 골격 복제) -->
From WPF_Example/Utility/RawImageSaveService.cs (전체 116줄):
- RawImageSaveRequest : IDisposable { HImage Image; ... DateTime Timestamp; void Dispose(){Image?.Dispose();} }
- RawImageSaveService : IDisposable
  - ConcurrentQueue<RawImageSaveRequest> _queue + AutoResetEvent _signal + Thread _workerThread + volatile bool _isStopping/_isStarted
  - ctor: Thread(WorkLoop){ IsBackground=true, Priority=ThreadPriority.BelowNormal }
  - Start(): _workerThread.Start() (idempotent via _isStarted)
  - Enqueue(req): null/Image null 가드 → _queue.Enqueue + _signal.Set()
  - WorkLoop(): while(!_isStopping){ TryDequeue→SaveRequest; else _signal.WaitOne(100) } 종료 시 잔여 큐 flush
  - SaveRequest(req): try{ Directory.CreateDirectory; req.Image.WriteImage("png",0,filePath); } catch{ Logging.PrintErrLog } finally{ req.Dispose(); }
  - SanitizeFilePart(value, fallback): Path.GetInvalidFileNameChars() 치환
  - Dispose(): _isStopping=true; _signal.Set(); Join(1000); _signal.Dispose();

From WPF_Example/SystemHandler.cs (배선 미러 대상):
- Line 55: `public RawImageSaveService RawImageSaver { get; private set; }`
- Line 130-132: `RawImageSaver = new RawImageSaveService(); RawImageSaver.Start(); Logging.PrintLog(...)`  (Initialize 내부)
- Line 204-205: `RawImageSaver?.Dispose(); RawImageSaver = null;`  (Release 내부)
- `Setting` 프로퍼티: `public SystemSetting Setting { get; private set; }` (L31)

From WPF_Example/Setting/SystemSetting.cs:
- `public string ResultSavePath { get; set; } = AppDomain.CurrentDomain.BaseDirectory + @"Result";` (L63)

From WPF_Example/UI/ViewModel/CycleResultDto.cs (수정 대상 FaiResultDto, L49-62):
- public class FaiResultDto { string FAIName; bool IsPass; bool WasDatumSkipped; List<MeasurementResultDto> Measurements; List<EdgeInspectionOverlay> LastOverlays; }

From WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs:
- L111-114: LastOverlays 가 `[Browsable(false)] [PropertyTools.DataAnnotations.Browsable(false)] [Newtonsoft.Json.JsonIgnore]` 3 어트리뷰트 + transient 패턴 (복제 모델)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: FaiResultDto + FAIConfig 파일명 필드 추가</name>
  <files>WPF_Example/UI/ViewModel/CycleResultDto.cs, WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs</files>
  <read_first>
    - WPF_Example/UI/ViewModel/CycleResultDto.cs (FaiResultDto L49-62 — 기존 필드 스타일·주석 패턴)
    - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs (L96-114 — runtime transient 필드 패턴 + LastOverlays 3-attribute 모델)
  </read_first>
  <action>
    (1) CycleResultDto.cs 의 `FaiResultDto` 클래스(L49-62)에 public auto-property 2개를 `WasDatumSkipped` 와 `Measurements` 사이에 추가한다. 기존 XML doc + `//260610 hbk Phase 40.2` 주석 스타일 답습:
    ```csharp
    //260610 hbk Phase 40.2 — FAI별 캡쳐 이미지 파일명(경로 아님, 파일명만). 엑셀 2컬럼 + JSON 직렬화 소스.
    /// <summary>원본 이미지 파일명. 예: origin_Top_FAI_A1_P1P2_153012345.png (경로 미포함).</summary>
    public string OriginImageFileName { get; set; }
    /// <summary>측정 오버레이 캡쳐 이미지 파일명. 예: capture_Top_FAI_A1_P1P2_153012345.png (경로 미포함).</summary>
    public string CaptureImageFileName { get; set; }
    ```
    (2) FAIConfig.cs 의 LastOverlays 필드(L109-114) 바로 아래에 transient runtime 필드 2개를 추가한다. LastOverlays 와 동일하게 `[System.ComponentModel.Browsable(false)]` + `[PropertyTools.DataAnnotations.Browsable(false)]` + `[Newtonsoft.Json.JsonIgnore]` 3 어트리뷰트를 붙여 INI/PropertyGrid 노출 차단 + JSON 직렬화 제외(DTO 가 별도 복사):
    ```csharp
    //260610 hbk Phase 40.2 — 검사 시점에 Action_FAIMeasurement 가 write-back 하는 캡쳐 파일명(transient).
    //  CycleResultSerializer.BuildDto 가 FaiResultDto 로 복사. INI/PropertyGrid 미노출 + JSON 직렬화 제외(LastOverlays 패턴 일치).
    [System.ComponentModel.Browsable(false)] //260610 hbk Phase 40.2
    [PropertyTools.DataAnnotations.Browsable(false)] //260610 hbk Phase 40.2
    [Newtonsoft.Json.JsonIgnore] //260610 hbk Phase 40.2
    public string LastOriginImageFileName { get; set; } = ""; //260610 hbk Phase 40.2
    [System.ComponentModel.Browsable(false)] //260610 hbk Phase 40.2
    [PropertyTools.DataAnnotations.Browsable(false)] //260610 hbk Phase 40.2
    [Newtonsoft.Json.JsonIgnore] //260610 hbk Phase 40.2
    public string LastCaptureImageFileName { get; set; } = ""; //260610 hbk Phase 40.2
    ```
    파일별 기존 brace 스타일 유지(CycleResultDto=Allman, FAIConfig=Allman 확인). C# 7.2 범위 내(auto-property initializer 허용).
  </action>
  <verify>
    <automated>MISSING — 자동 단위 테스트 프레임워크 없음. grep 으로 필드 존재 확인: Grep("OriginImageFileName", "WPF_Example/UI/ViewModel/CycleResultDto.cs") 와 Grep("LastCaptureImageFileName", "WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs") 각 1+ 매치. 빌드 검증은 Task 3 의 msbuild 에 통합.</automated>
  </verify>
  <acceptance_criteria>
    - Grep "public string OriginImageFileName" in CycleResultDto.cs → 1 match
    - Grep "public string CaptureImageFileName" in CycleResultDto.cs → 1 match
    - Grep "public string LastOriginImageFileName" in FAIConfig.cs → 1 match
    - Grep "public string LastCaptureImageFileName" in FAIConfig.cs → 1 match
    - FAIConfig 신규 필드 2개 각각 위에 `[Newtonsoft.Json.JsonIgnore]` 라인 존재 (Grep "JsonIgnore" count 가 기존 1 → 3 으로 증가)
    - 모든 신규 라인에 `//260610 hbk Phase 40.2` 주석 포함
  </acceptance_criteria>
  <done>FaiResultDto 에 파일명 2필드, FAIConfig 에 transient 파일명 2필드 추가됨. FAIConfig 필드는 INI/PropertyGrid/JSON 직렬화 모두 제외.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CaptureImageSaveService 비동기 워커 신규 작성</name>
  <files>WPF_Example/Utility/CaptureImageSaveService.cs</files>
  <read_first>
    - WPF_Example/Utility/RawImageSaveService.cs (전체 116줄 — 복제 골격: ConcurrentQueue/AutoResetEvent/volatile/WorkLoop/SaveRequest/SanitizeFilePart/Dispose)
    - WPF_Example/Setting/SystemSetting.cs (L63 ResultSavePath, L146-177 GetLogSavePath — 사용 안 함, 비교 참조용)
    - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs (L143-150 ResultSavePath + DateTime Path.Combine 패턴)
  </read_first>
  <action>
    `WPF_Example/Utility/CaptureImageSaveService.cs` 신규 생성. RawImageSaveService.cs 를 거의 그대로 복제하되 request DTO 필드와 경로/파일명 규칙만 교체. namespace `ReringProject.Utility`. K&R brace 스타일(복제 원본 RawImageSaveService 가 K&R 이므로 일치). using: HalconDotNet, ReringProject.Network, ReringProject.Setting, System, System.Collections.Concurrent, System.IO, System.Threading.

    (A) request DTO — `CaptureImageSaveRequest : IDisposable`:
    ```csharp
    public sealed class CaptureImageSaveRequest : IDisposable {
        public HImage Image { get; set; }       // 워커가 저장 후 Dispose 할 소유 객체
        public string FileName { get; set; }     // 동기 결정된 완성 파일명 (origin_... 또는 capture_...)
        public bool IsCapture { get; set; }       // true=capture 폴더, false=original 폴더
        public DateTime Timestamp { get; set; } = DateTime.Now;  // yyMMdd/HHmm 폴더 계산용
        public void Dispose() { Image?.Dispose(); Image = null; }
    }
    ```
    (B) 워커 골격 — RawImageSaveService 의 _queue/_signal/_workerThread/_isStopping/_isStarted/ctor(BelowNormal,IsBackground,Name="CaptureImageSaveService")/Start()/Enqueue()/WorkLoop()/Dispose() 를 타입명만 CaptureImageSaveRequest 로 바꿔 그대로 복제.

    (C) SaveRequest(req) — 경로/파일명만 교체 (위임 결정 해소):
    ```csharp
    private static void SaveRequest(CaptureImageSaveRequest request) {
        try {
            string baseDirectory = Path.Combine(
                SystemHandler.Handle.Setting.ResultSavePath, "Image",
                request.Timestamp.ToString("yyMMdd"),
                request.Timestamp.ToString("HHmm"),
                request.IsCapture ? "capture" : "original");   //260610 hbk Phase 40.2 — ResultSavePath\Image\{yyMMdd}\{HHmm}\original|capture
            Directory.CreateDirectory(baseDirectory);
            string fileName = SanitizeFileName(request.FileName);
            string filePath = Path.Combine(baseDirectory, fileName);
            request.Image.WriteImage("png", 0, filePath);   // RawImageSaveService.cs:85 동일 HImage→PNG API
        }
        catch (Exception ex) {
            Logging.PrintErrLog((int)ELogType.Error, string.Format("Capture image save failed: {0}", ex.Message));
        }
        finally {
            request.Dispose();
        }
    }
    ```
    (D) 파일명 생성 public static 헬퍼 — 호출부(Wave 2 Action_FAIMeasurement)가 동기로 파일명을 만들어 fai 에 write-back 하고 동일 파일명으로 enqueue 하도록 single source of truth 제공. T-40.2-01 mitigation: 각 segment(sequence/fai/measurePointSegment) 를 Path.GetInvalidFileNameChars() 로 sanitize:
    ```csharp
    // prefix = "origin" 또는 "capture". 결과: {prefix}_{시퀀스}_{FAI}_{측정점}_{HHmmssfff}.png
    public static string BuildFileName(string prefix, string sequence, string faiName, string measurePointSegment, DateTime ts) {
        string seq = SanitizeFilePart(sequence, "SEQ");
        string fai = SanitizeFilePart(faiName, "FAI");
        string seg = SanitizeFilePart(measurePointSegment, "");   // 빈 segment 허용
        string time = ts.ToString("HHmmssfff");
        if (string.IsNullOrEmpty(seg)) {
            return string.Format("{0}_{1}_{2}_{3}.png", prefix, seq, fai, time);
        }
        return string.Format("{0}_{1}_{2}_{3}_{4}.png", prefix, seq, fai, seg, time);
    }
    ```
    SanitizeFilePart(value, fallback) 는 RawImageSaveService.cs:95-105 와 동일 복제. SanitizeFileName(name) 은 완성 파일명 전체에 대해 Path.GetInvalidFileNameChars() 치환(이중 방어). 모든 신규 메서드/필드에 `//260610 hbk Phase 40.2` 주석.
  </action>
  <verify>
    <automated>MISSING — 단위 테스트 없음. 빌드는 Task 3 에서 csproj 등록 후 msbuild 로 검증. grep 으로 구조 확인: Grep("class CaptureImageSaveService", file) 1 match, Grep("ResultSavePath", file) 1+ match, Grep("BuildFileName", file) 1 match.</automated>
  </verify>
  <acceptance_criteria>
    - 파일 WPF_Example/Utility/CaptureImageSaveService.cs 존재
    - Grep "public sealed class CaptureImageSaveRequest : IDisposable" → 1 match
    - Grep "public sealed class CaptureImageSaveService : IDisposable" → 1 match
    - Grep "volatile bool _isStopping" → 1 match (워커 골격 복제 확인)
    - Grep "ThreadPriority.BelowNormal" → 1 match
    - Grep "Path.Combine(\\s*SystemHandler.Handle.Setting.ResultSavePath, \"Image\"" (또는 ResultSavePath + \"Image\" 동일 의미) → SaveRequest 가 ImageSavePath 가 아닌 ResultSavePath\Image 사용 확인
    - Grep "request.IsCapture ? \"capture\" : \"original\"" → 1 match
    - Grep "public static string BuildFileName" → 1 match
    - Grep "WriteImage(\"png\", 0," → 1 match
    - Grep "Path.GetInvalidFileNameChars" → 1+ match (sanitize)
    - GetLogSavePath 미사용: Grep "GetLogSavePath" in file → 0 match
  </acceptance_criteria>
  <done>RawImageSaveService 패턴을 복제한 비동기 캡쳐 저장 서비스 완성. ResultSavePath\Image\{yyMMdd}\{HHmm}\original|capture 경로 + origin_/capture_ 파일명 규칙 + BuildFileName public 헬퍼 제공.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: SystemHandler 에 CaptureImageSaver 등록 + 빌드 검증</name>
  <files>WPF_Example/SystemHandler.cs</files>
  <read_first>
    - WPF_Example/SystemHandler.cs (L55 RawImageSaver 필드 선언, L130-132 Initialize 내 Start, L204-205 Release 내 Dispose)
    - WPF_Example/DatumMeasurement.csproj (Compile ItemGroup — 신규 .cs 등록 필요 여부 확인. classic-style packages.config 프로젝트는 새 파일 수동 등록 필수)
  </read_first>
  <action>
    (1) SystemHandler.cs 필드 선언(L55 RawImageSaver 바로 아래)에 추가:
    ```csharp
    //260610 hbk Phase 40.2 — FAI별 캡쳐 이미지 비동기 저장 서비스 (RawImageSaver 와 동일 라이프사이클).
    public CaptureImageSaveService CaptureImageSaver { get; private set; }
    ```
    (2) Initialize 의 RawImageSaver.Start() 직후(L132 Logging 라인 다음)에 미러:
    ```csharp
    CaptureImageSaver = new CaptureImageSaveService(); //260610 hbk Phase 40.2
    CaptureImageSaver.Start(); //260610 hbk Phase 40.2
    ```
    (3) Release 의 RawImageSaver?.Dispose() 직후(L205 다음)에 미러:
    ```csharp
    CaptureImageSaver?.Dispose(); //260610 hbk Phase 40.2
    CaptureImageSaver = null; //260610 hbk Phase 40.2
    ```
    using 절에 `ReringProject.Utility` 가 이미 있으면 추가 불필요(RawImageSaveService 사용 중이므로 존재). 없으면 추가.
    (4) csproj 에 신규 파일 등록: DatumMeasurement.csproj 의 Compile ItemGroup 에서 RawImageSaveService.cs 등록 라인을 찾아, 동일 형식으로 `<Compile Include="Utility\CaptureImageSaveService.cs" />` 를 추가(없으면 빌드 시 CS 미컴파일). Grep("RawImageSaveService.cs", csproj) 로 기존 등록 형식 확인 후 미러.
    (5) 빌드 검증: Wave 1 전체(Task 1+2+3) 가 컴파일되는지 msbuild Debug/x64 로 확인.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /nologo /v:minimal — exit 0, 신규 error 0 (경로 미설치 시 vswhere 로 MSBuild 탐색 후 실행)</automated>
  </verify>
  <acceptance_criteria>
    - Grep "public CaptureImageSaveService CaptureImageSaver" in SystemHandler.cs → 1 match
    - Grep "CaptureImageSaver = new CaptureImageSaveService" → 1 match
    - Grep "CaptureImageSaver?.Dispose" → 1 match
    - Grep "CaptureImageSaveService.cs" in DatumMeasurement.csproj → 1 match (Compile 등록)
    - msbuild Debug/x64 빌드 성공 (exit 0), Phase 41 baseline 대비 신규 error 0
  </acceptance_criteria>
  <done>CaptureImageSaver 가 SystemHandler 싱글턴에 RawImageSaver 와 동일 패턴으로 등록되고 Initialize/Release 라이프사이클에 배선됨. Wave 1 전체 msbuild Debug/x64 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FAI 설정명/시퀀스명 → 파일 경로 | FAIName / SequenceName / 측정점 segment 가 파일명 문자열로 합성되어 디스크에 기록됨. 사용자가 PropertyGrid 에서 편집 가능한 문자열이 파일명에 들어감 (untrusted-ish 로컬 입력). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.2-01 | Tampering / Path traversal | CaptureImageSaveService.BuildFileName + SaveRequest | mitigate | 모든 segment(sequence/fai/seg)를 `SanitizeFilePart` 에서 `Path.GetInvalidFileNameChars()` 치환(`\`, `/`, `..` 부분문자 무력화 — 디렉토리 분리자 제거로 traversal 차단). 완성 파일명도 `SanitizeFileName` 으로 2차 방어. baseDir 는 ResultSavePath + DateTime 포맷팅만으로 조립(외부 입력 0, CycleResultSerializer T-40-01 동일 근거). |
| T-40.2-02 | Denial of Service | 비동기 워커 큐 무한 적재 | accept | FAI별 1장 + BelowNormal 백그라운드 스레드 + 검사 사이클당 유한 enqueue. 로컬 단일 장비, 외부 트리거 없음 → 저위험. RawImageSaveService 기존 운영 실적과 동일 구조. |
</threat_model>

<verification>
- msbuild Debug/x64 빌드 성공, 신규 error/warning 0 (Phase 41 baseline 대비)
- Grep 으로 4 파일 모든 acceptance_criteria 패턴 매치 확인
- ResultSavePath\Image 경로 사용 확인(ImageSavePath/GetLogSavePath 오용 0)
</verification>

<success_criteria>
- FaiResultDto 가 OriginImageFileName/CaptureImageFileName 보유
- FAIConfig 가 LastOriginImageFileName/LastCaptureImageFileName transient 필드 보유(3-attribute 차단)
- CaptureImageSaveService 가 RawImageSaveService 패턴으로 비동기 PNG 저장 + BuildFileName 헬퍼 제공
- SystemHandler 가 CaptureImageSaver 를 Initialize/Release 라이프사이클에 배선
- 전체 msbuild Debug/x64 통과
</success_criteria>

<output>
완료 후 `.planning/phases/40.2-fai-2/40.2-01-SUMMARY.md` 생성.
</output>
