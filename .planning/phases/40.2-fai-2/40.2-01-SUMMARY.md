---
phase: 40.2-fai-2
plan: "01"
subsystem: inspection-capture-foundation
tags: [dto, fai-config, async-worker, capture-image, system-handler]
dependency_graph:
  requires: []
  provides:
    - FaiResultDto.OriginImageFileName
    - FaiResultDto.CaptureImageFileName
    - FAIConfig.LastOriginImageFileName
    - FAIConfig.LastCaptureImageFileName
    - CaptureImageSaveService (async PNG worker + BuildFileName helper)
    - SystemHandler.CaptureImageSaver
  affects:
    - WPF_Example/UI/ViewModel/CycleResultDto.cs
    - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
    - WPF_Example/Utility/CaptureImageSaveService.cs
    - WPF_Example/SystemHandler.cs
    - WPF_Example/DatumMeasurement.csproj
tech_stack:
  added:
    - CaptureImageSaveService (RawImageSaveService 패턴 복제, BelowNormal 비동기 워커)
  patterns:
    - ConcurrentQueue + AutoResetEvent + volatile 비동기 저장 워커
    - BuildFileName public static 헬퍼 (단일 파일명 규칙 source of truth)
    - 3-attribute transient 필드 차단 (Browsable×2 + JsonIgnore)
key_files:
  created:
    - WPF_Example/Utility/CaptureImageSaveService.cs
  modified:
    - WPF_Example/UI/ViewModel/CycleResultDto.cs
    - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
    - WPF_Example/SystemHandler.cs
    - WPF_Example/DatumMeasurement.csproj
decisions:
  - "저장 경로 = ResultSavePath\\Image\\{yyMMdd}\\{HHmm}\\original|capture (ImageSavePath/GetLogSavePath 미사용)"
  - "파일명 타이밍 = 동기 결정(BuildFileName) + 비동기 write 분리"
  - "FAIConfig transient 필드 2개는 LastOverlays 3-attribute 패턴 그대로 복제"
metrics:
  duration: 25
  completed_date: "2026-06-10"
  tasks: 3
  files: 5
---

# Phase 40.2 Plan 01: FAI 캡쳐 이미지 저장 Foundation Summary

## One-liner

RawImageSaveService 패턴으로 CaptureImageSaveService 신규 작성 + FaiResultDto/FAIConfig에 파일명 필드 추가 + SystemHandler 라이프사이클 배선 — Wave 2/3 소비 계약 확정.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | FaiResultDto + FAIConfig 파일명 필드 추가 | ec249a7 | CycleResultDto.cs, FAIConfig.cs |
| 2 | CaptureImageSaveService 비동기 워커 신규 작성 | aec2f65 | CaptureImageSaveService.cs |
| 3 | SystemHandler CaptureImageSaver 등록 + 빌드 검증 | fbbbcea | SystemHandler.cs, DatumMeasurement.csproj |

## Changes Made

### CycleResultDto.cs
- `FaiResultDto` 클래스에 `OriginImageFileName` / `CaptureImageFileName` public auto-property 2개 추가
- `WasDatumSkipped` 와 `Measurements` 사이에 배치 (Plan 지정 위치)
- XML doc 주석 포함 (경로 아닌 파일명 명시)

### FAIConfig.cs
- `LastOverlays` 아래에 `LastOriginImageFileName` / `LastCaptureImageFileName` transient 필드 2개 추가
- 각 필드에 3-attribute 차단 적용: `[System.ComponentModel.Browsable(false)]` + `[PropertyTools.DataAnnotations.Browsable(false)]` + `[Newtonsoft.Json.JsonIgnore]`
- 초기값 `""` — C# 7.2 auto-property initializer

### CaptureImageSaveService.cs (신규)
- `CaptureImageSaveRequest : IDisposable` DTO — Image/FileName/IsCapture/Timestamp 4 필드
- `CaptureImageSaveService : IDisposable` 비동기 워커 — RawImageSaveService 골격 그대로 복제
- `SaveRequest`: `ResultSavePath\Image\{yyMMdd}\{HHmm}\original|capture` 경로 조립
- `BuildFileName` public static 헬퍼 — `{prefix}_{seq}_{fai}_{seg}_{HHmmssfff}.png` 규칙
- `SanitizeFilePart` + `SanitizeFileName` 2중 방어 (T-40.2-01 path traversal 차단)
- K&R brace 스타일 (복제 원본 RawImageSaveService 일치)

### SystemHandler.cs
- `CaptureImageSaver` 싱글턴 필드 추가 (RawImageSaver 바로 아래)
- `Initialize()` — `CaptureImageSaver = new CaptureImageSaveService(); CaptureImageSaver.Start();` (RawImageSaver.Start 직후)
- `Release()` — `CaptureImageSaver?.Dispose(); CaptureImageSaver = null;` (RawImageSaver?.Dispose 직후)

### DatumMeasurement.csproj
- `<Compile Include="Utility\CaptureImageSaveService.cs" />` RawImageSaveService.cs 바로 아래 등록

## Build Verification

msbuild Debug/x64 실행 결과:
- **CS 컴파일 에러: 0개**
- MSB3027/MSB3021: 실행 중인 앱이 `bin\x64\Debug\DatumMeasurement.exe` 잠금 → 복사 실패 (기존 환경 이슈, 신규 오류 아님)
- 기존 CS0618/CS0162 warnings: Phase 41 baseline 동일, 신규 0

## Acceptance Criteria Verification

| 항목 | 결과 |
|------|------|
| FaiResultDto.OriginImageFileName | PASS (1 match) |
| FaiResultDto.CaptureImageFileName | PASS (1 match) |
| FAIConfig.LastOriginImageFileName | PASS (1 match) |
| FAIConfig.LastCaptureImageFileName | PASS (1 match) |
| FAIConfig JsonIgnore 3개 (기존 1→3) | PASS |
| CaptureImageSaveRequest : IDisposable | PASS (1 match) |
| CaptureImageSaveService : IDisposable | PASS (1 match) |
| volatile bool _isStopping | PASS (1 match) |
| ThreadPriority.BelowNormal | PASS (1 match) |
| ResultSavePath + "Image" 경로 사용 | PASS (3 match) |
| IsCapture ? "capture" : "original" | PASS (1 match) |
| BuildFileName public static | PASS (1 match) |
| WriteImage("png", 0, ...) | PASS (1 match) |
| Path.GetInvalidFileNameChars (sanitize) | PASS (3 match) |
| GetLogSavePath 미사용 | PASS (0 match) |
| SystemHandler.CaptureImageSaver 필드 | PASS (1 match) |
| CaptureImageSaver = new CaptureImageSaveService | PASS (1 match) |
| CaptureImageSaver?.Dispose | PASS (1 match) |
| csproj Compile 등록 | PASS (1 match) |

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

없음. 이 Plan은 계약 레이어(DTO 필드, FAIConfig transient 필드, 저장 서비스 API)를 확정하는 목적이며, 실제 캡쳐 생성(Wave 2)과 직렬화/엑셀(Wave 3)은 후속 Plan에서 구현된다. 파일명 필드 초기값은 `""` (비어있음)이며 Wave 2가 채우기 전까지는 빈 문자열로 유지 — 이는 의도적 초기 상태임.

## Threat Surface Scan

T-40.2-01 (Path traversal) mitigation 구현 확인:
- `BuildFileName`: sequence/faiName/measurePointSegment 각 segment를 `SanitizeFilePart`로 sanitize
- `SaveRequest`: 완성 파일명을 `SanitizeFileName`으로 2차 방어
- `baseDirectory`: `ResultSavePath` + DateTime 포맷팅만으로 조립 (외부 입력 0)

신규 네트워크 엔드포인트, auth 경로, DB 스키마 변경 없음.

## Self-Check: PASSED

- `WPF_Example/Utility/CaptureImageSaveService.cs` — FOUND
- `WPF_Example/UI/ViewModel/CycleResultDto.cs` — FOUND (수정)
- `WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs` — FOUND (수정)
- `WPF_Example/SystemHandler.cs` — FOUND (수정)
- Commits ec249a7, aec2f65, fbbbcea — FOUND
