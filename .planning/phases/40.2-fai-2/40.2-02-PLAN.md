---
phase: 40.2-fai-2
plan: 02
type: execute
wave: 2
depends_on: ["40.2-01"]
files_modified:
  - WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: []
must_haves:
  truths:
    - "검사 시점에 각 FAI 의 원본 이미지가 origin_ PNG 로, 측정 오버레이가 입혀진 캡쳐 이미지가 capture_ PNG 로 디스크에 저장된다"
    - "캡쳐 이미지는 off-screen 버퍼 윈도우에서 HalconDisplayService.Render 를 헤드리스 재사용하여 생성된다(UI HWND 의존 없음, 시퀀스 스레드 안전)"
    - "FAI 의 측정점 개수에 따라 파일명 segment 가 P1/P1P2/빈값으로 구성되고 fai 에 write-back 된다"
  artifacts:
    - path: "WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs"
      provides: "off-screen 버퍼 윈도우 렌더→dump→HImage 헤드리스 캡쳐"
      contains: "OpenWindow"
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "FAI별 origin/capture enqueue + 파일명 write-back"
      contains: "QueueFaiCapture"
  key_links:
    - from: "Action_FAIMeasurement EStep.Measure (fai.LastOverlays = faiOverlays 직후)"
      to: "CaptureImageSaver.Enqueue"
      via: "QueueFaiCapture(fai, image, faiOverlays, seq)"
      pattern: "QueueFaiCapture"
    - from: "OverlayCaptureRenderer.RenderToHImage"
      to: "HalconDisplayService.Render(bufferWindow,...)"
      via: "buffer 모드 OpenWindow + Render 재사용 + DumpWindowImage"
      pattern: "DumpWindowImage"
    - from: "QueueFaiCapture"
      to: "fai.LastOriginImageFileName / LastCaptureImageFileName"
      via: "동기 write-back (BuildDto 가 읽을 수 있도록)"
      pattern: "LastCaptureImageFileName ="
---

<objective>
검사 시점에 FAI별로 원본 이미지(origin_)와 측정 오버레이가 입혀진 캡쳐 이미지(capture_)를 각각 PNG 로 디스크에 저장한다. off-screen 버퍼 윈도우에서 기존 HalconDisplayService.Render 를 헤드리스로 재사용하여 캡쳐 HImage 를 생성하는 신규 렌더러를 작성하고, Action_FAIMeasurement 의 Measure step 에 FAI별 캡쳐 enqueue + 파일명 write-back 을 삽입한다.

Purpose: Wave 1 이 만든 CaptureImageSaveService/BuildFileName/FAIConfig 파일명 필드를 소비하여 실제 이미지 산출물을 생성한다. CONTEXT 핵심 기능(FAI별 1장, original/capture 분리).
Output: OverlayCaptureRenderer.cs 신규, Action_FAIMeasurement.cs 수정, csproj 등록.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40.2-fai-2/40.2-CONTEXT.md
@.planning/phases/40.2-fai-2/40.2-PATTERNS.md
@.planning/phases/40.2-fai-2/40.2-01-SUMMARY.md

<interfaces>
<!-- Wave 1 산출물 (소비 대상) -->
From WPF_Example/Utility/CaptureImageSaveService.cs (Plan 01):
- class CaptureImageSaveRequest : IDisposable { HImage Image; string FileName; bool IsCapture; DateTime Timestamp; }
- CaptureImageSaveService.Enqueue(CaptureImageSaveRequest)
- public static string BuildFileName(string prefix, string sequence, string faiName, string measurePointSegment, DateTime ts)  // "origin"/"capture" prefix → {prefix}_{seq}_{fai}_{seg}_{HHmmssfff}.png
- SystemHandler.Handle.CaptureImageSaver (싱글턴 접근)

From WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs (Plan 01):
- public string LastOriginImageFileName { get; set; }   // write-back 대상
- public string LastCaptureImageFileName { get; set; }

<!-- 렌더 재사용 대상 -->
From WPF_Example/Halcon/Display/HalconDisplayService.cs (L19-26):
- public void Render(HWindow window, HImage image, IEnumerable<RoiDefinition> rois, string selectedRoiId, RoiDefinition draftRoi = null, IEnumerable<EdgeInspectionOverlay> inspectionOverlays = null, IEnumerable<string> displayMessages = null)
  - window null 가드 → image 를 먼저 DispObj 후 오버레이 렌더 (순서 계약: image → overlays)

From WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs:
- class EdgeInspectionOverlay { string RoiId; List<EdgeInspectionPoint> Points; double LineRow1/Column1/Row2/Column2; }
- class EdgeInspectionPoint { double Row; double Column; }
  - 측정점 식별: RoiId.StartsWith("FAI-Edge") 인 오버레이의 Points 개수 (Action_FAIMeasurement.cs:280 에서 suffix 부여, HalconDisplayService.cs:217 렌더)

<!-- 삽입 지점 -->
From WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs:
- L168 overlayAcc, L170 `using (var image = ShotParam.GetImage())`, L172 `foreach (var fai in ShotParam.FAIList)`
- L174 faiOverlays per-FAI 누적, L306 `fai.LastOverlays = faiOverlays;` ← **삽입 지점(직후)**
- ShotParam.OwnerSequenceName (시퀀스명, 빈값 시 "TOP" 폴백 — ShotConfig.cs:51/145)
- image.CopyImage() 로 소유권 사본 전달 (Action_TopInspection.cs:290 패턴)
</interfaces>

<headless_capture_snippet>
<!-- 검증됨: dump_window_image / "buffer" OpenWindow 설치본 존재 (PATTERNS.md §2) -->
off-screen 버퍼 윈도우 표준 패턴 (코드베이스 신규, HALCON 24.11):
```csharp
image.GetImageSize(out HTuple w, out HTuple h);
HTuple win;
HOperatorSet.OpenWindow(0, 0, w, h, 0, "buffer", "", out win);   // "buffer" = off-screen, HWND 불필요
HWindow hwin = new HWindow(win.IP);   // HalconDisplayService.Render 가 HWindow 요구
try {
    HOperatorSet.SetPart(win, 0, 0, h - 1, w - 1);
    _displayService.Render(hwin, image, null, null, null, faiOverlays, null);  // 기존 렌더 재사용 (rois/selected/draft null)
    HObject dumped;
    HOperatorSet.DumpWindowImage(out dumped, win);
    captureHImage = new HImage(dumped);   // 호출부로 소유권 이전
}
finally {
    HOperatorSet.CloseWindow(win);   // 누수 방지 필수 (finally)
}
```
모든 HOperatorSet 블록은 try/catch swallow (렌더 실패 = PNG 누락, 검사 스레드 생존 — CLAUDE.md + HalconDisplayService 전역 컨벤션).
</headless_capture_snippet>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: OverlayCaptureRenderer 헤드리스 캡쳐 렌더러 신규 작성</name>
  <files>WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs, WPF_Example/DatumMeasurement.csproj</files>
  <read_first>
    - WPF_Example/Halcon/Display/HalconDisplayService.cs (L19-27 Render 시그니처, L28-31 window null 가드, L41 image DispObj 순서 — 헤드리스도 동일). **추가로 stateful 여부 판정**: 클래스에 HWindow/HTuple 인스턴스 필드가 있는지, Render 가 인스턴스 필드를 읽거나 쓰는지(호출 간 상태 누적/공유)를 정독하여 W1 분기 결정에 사용.
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs (L155-171 DisplayCycle — LoadImage→SetInspectionOverlays 순서 계약 참조)
    - WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs (Points 구조 — 측정점 카운트)
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (L270-310 FAI-Edge 오버레이 **생성부** — W4 근거: 검출점 1개당 오버레이가 1개로 생성되는지, 아니면 단일 오버레이의 Points 에 다점이 누적되는지 확인. EdgeInspectionOverlay.Points 가 단일점인지 다점인지 이 생성부에서 확정)
    - .planning/phases/40.2-fai-2/40.2-PATTERNS.md (§2 검증된 버퍼 윈도우 스니펫)
  </read_first>
  <action>
    `WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs` 신규 생성. namespace `ReringProject.Halcon.Display`(HalconDisplayService 와 동일 폴더/namespace). Allman brace 스타일(Halcon 계열 일치). using: HalconDotNet, ReringProject.Halcon.Models, ReringProject.Network(Logging/ELogType), System, System.Collections.Generic.

    ### W1 — 스레드 안전성 분기 결정 (필수, read_first 에서 확정)
    `OverlayCaptureRenderer` 는 Task 2 에서 `_captureRenderer` **단일 인스턴스**로 Action_FAIMeasurement(시퀀스 스레드)에서 FAI 루프마다 반복 호출된다. RenderToHImage 내부가 `_displayService.Render` 를 호출하므로, **HalconDisplayService 의 stateful 여부에 따라 동시성 처리를 분기**한다(read_first 의 HalconDisplayService.cs 정독으로 확정):
    - HalconDisplayService 가 **stateful**(HWindow/HTuple 등 인스턴스 필드 보유 OR Render 가 인스턴스 필드를 읽거나 변경 OR 호출 간 상태 누적) → 둘 중 하나 채택:
      - (a) RenderToHImage 호출마다 **지역 `new HalconDisplayService()`** 를 사용(인스턴스 필드 공유 금지), 또는
      - (b) `_displayService.Render(...)` 구간을 **`lock (_renderLock)`** 으로 직렬화 — 클래스에 `private readonly object _renderLock = new object();` 추가.
    - HalconDisplayService 가 **stateless**(메서드 인자만으로 동작, 가변 인스턴스 필드 없음) → `_displayService` 인스턴스 필드 **재사용 허용**(별도 lock 불필요).
    - 비고: OpenWindow/CloseWindow 는 호출당 지역 `win` 으로 생성·해제되므로 윈도우 핸들 자체는 이미 호출-지역이다. 동시성 위험은 오직 `_displayService` 상태 공유에 국한된다.
    선택한 분기를 코드에 `//260610 hbk Phase 40.2 — 스레드 안전성: <stateful→lock | stateful→지역new | stateless→재사용>` 주석으로 명시한다(grep 추적용).

    클래스 `OverlayCaptureRenderer` — HalconDisplayService 인스턴스를 ctor 주입(또는 new HalconDisplayService() 내부 생성; HalconDisplayService 가 stateless 면 내부 생성 권장. Read 로 확인 후 결정):
    ```csharp
    public class OverlayCaptureRenderer
    {
        private readonly HalconDisplayService _displayService;
        // (stateful 이면서 lock 분기 채택 시) private readonly object _renderLock = new object();
        public OverlayCaptureRenderer(HalconDisplayService displayService) //260610 hbk Phase 40.2
        {
            _displayService = displayService ?? new HalconDisplayService();
        }
    ```
    (A) `public HImage RenderToHImage(HImage image, List<EdgeInspectionOverlay> overlays)` — off-screen 버퍼 윈도우에 원본 + overlays 렌더 후 DumpWindowImage 로 HImage 반환. 실패 시 null 반환(검사 스레드 보호). <headless_capture_snippet> 의 패턴을 그대로 구현하되 전체를 try/catch 로 감싸 catch 에서 Logging.PrintErrLog + return null. **W1 분기에서 lock 을 채택한 경우 `_displayService.Render(...)` 호출을 `lock (_renderLock) { ... }` 으로 감쌀 것**:
    ```csharp
    public HImage RenderToHImage(HImage image, List<EdgeInspectionOverlay> overlays) //260610 hbk Phase 40.2
    {
        if (image == null) return null;
        HTuple win = null;
        try
        {
            image.GetImageSize(out HTuple w, out HTuple h);
            HOperatorSet.OpenWindow(0, 0, w, h, 0, "buffer", "", out win);
            HWindow hwin = new HWindow(win.IP);
            HOperatorSet.SetPart(win, 0, 0, h - 1, w - 1);
            // W1 stateless: 아래 한 줄 그대로. W1 stateful+lock: lock (_renderLock) { ... } 로 감쌈. W1 stateful+지역new: new HalconDisplayService().Render(...)
            _displayService.Render(hwin, image, null, null, null, overlays, null); //260610 hbk Phase 40.2 — 기존 오버레이 렌더 재사용 (순서: image→overlays)
            HObject dumped;
            HOperatorSet.DumpWindowImage(out dumped, win);
            return new HImage(dumped);
        }
        catch (Exception ex)
        {
            Logging.PrintErrLog((int)ELogType.Error, "[OverlayCaptureRenderer] capture render failed: " + ex.Message); //260610 hbk Phase 40.2 — 캡쳐 실패 = PNG 누락만, 검사 계속
            return null;
        }
        finally
        {
            if (win != null) //260610 hbk Phase 40.2 — 버퍼 윈도우 누수 방지 (필수)
            {
                try { HOperatorSet.CloseWindow(win); } catch { }
            }
        }
    }
    ```
    (B) `public static string BuildMeasurePointSegment(List<EdgeInspectionOverlay> overlays)` — 측정점 segment 식별(CONTEXT Claude's Discretion). **W4 — 측정점 카운트 근거**: read_first 에서 확인한 Action_FAIMeasurement 의 FAI-Edge 오버레이 생성부 구조에 따라 분기한다.
    - **1점 = 1오버레이 구조**(검출점 1개당 EdgeInspectionOverlay 1개 생성, 각 오버레이 Points 가 단일점)이면 → RoiId 가 "FAI-Edge" 로 시작하고 Points 가 1개 이상인 **오버레이 개수**를 카운트.
    - **다점 오버레이 구조**(단일 오버레이의 Points 에 P1/P2 가 함께 누적)이면 → 해당 오버레이들의 **Points.Count 합산**으로 보정하여 카운트.
    아래 골격은 1점=1오버레이 가정이며, 다점 구조로 확인되면 `pointCount++` 를 `pointCount += ov.Points.Count;` 로 교체한다. 점 0개면 빈 문자열 반환:
    ```csharp
    public static string BuildMeasurePointSegment(List<EdgeInspectionOverlay> overlays) //260610 hbk Phase 40.2
    {
        if (overlays == null) return "";
        int pointCount = 0;
        foreach (var ov in overlays)
        {
            if (ov == null || ov.Points == null) continue;
            if (string.IsNullOrEmpty(ov.RoiId)) continue;
            if (!ov.RoiId.StartsWith("FAI-Edge", StringComparison.OrdinalIgnoreCase)) continue;
            if (ov.Points.Count > 0) pointCount++;   //260610 hbk Phase 40.2 — 1점=1오버레이 가정. 다점 오버레이면 pointCount += ov.Points.Count 로 교체(read_first 근거)
        }
        if (pointCount <= 0) return "";
        var sb = new System.Text.StringBuilder();
        for (int i = 1; i <= pointCount; i++) { sb.Append("P"); sb.Append(i); }  //260610 hbk Phase 40.2 — 1→"P1", 2→"P1P2"
        return sb.ToString();
    }
    ```
    > 주의: RoiId 는 Action_FAIMeasurement.cs:281 에서 "-OK"/"-NG" suffix 가 붙으므로 StartsWith 매칭만 사용(Contains 아님). C# 7.2 — `is` pattern/switch expression 금지, StringBuilder/foreach 사용.
    > W4 검증 안전망: P1P2(다점) 케이스의 segment 정확성은 Plan 04 Test 2(파일명 규칙 + origin/capture 쌍, P1P2 케이스)의 SIMUL 실측에서 최종 확인된다.

    (C) csproj 등록: DatumMeasurement.csproj Compile ItemGroup 에 `<Compile Include="Halcon\Display\OverlayCaptureRenderer.cs" />` 추가(HalconDisplayService.cs 등록 라인 형식 미러).
  </action>
  <verify>
    <automated>MISSING — 단위 테스트 없음. 빌드 검증은 Task 2 의 msbuild 에 통합. grep: Grep("class OverlayCaptureRenderer", file) 1, Grep("DumpWindowImage", file) 1, Grep("\"buffer\"", file) 1, Grep("CloseWindow", file) 1.</automated>
  </verify>
  <acceptance_criteria>
    - 파일 WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs 존재
    - Grep "public class OverlayCaptureRenderer" → 1 match
    - Grep "OpenWindow(0, 0, w, h, 0, \"buffer\"" (또는 \"buffer\" 인자 포함 OpenWindow) → 1 match
    - Grep "DumpWindowImage" → 1 match
    - Grep "CloseWindow" → 1 match, 그리고 그 라인이 finally 블록 내에 위치 (Read 로 확인)
    - Grep "_displayService.Render(hwin, image, null, null, null" → 1 match (기존 렌더 재사용, 순서 계약)
    - **W1 스레드 안전성 분기 적용 증거(grep 가능)**: OverlayCaptureRenderer.cs 가 다음 중 **정확히 하나**를 포함 — (a) `lock (_renderLock)` (+ `private readonly object _renderLock`) [stateful+lock], 또는 (b) `new HalconDisplayService()` 가 RenderToHImage **메서드 본문 내부**에 존재 [stateful+지역new], 또는 (c) `//260610 hbk Phase 40.2 — 스레드 안전성: stateless` 주석으로 재사용 명시 [stateless]. 셋 다 부재면 FAIL.
    - **W1 분기 결정 주석**: Grep "스레드 안전성:" → 1 match (선택 근거 명시)
    - Grep "public static string BuildMeasurePointSegment" → 1 match
    - Grep "StartsWith(\"FAI-Edge\"" → 1 match (측정점 카운트)
    - **W4 측정점 카운트 근거 주석**: Grep "1점=1오버레이" 또는 다점 보정 채택 시 "ov.Points.Count" 합산 라인 → 1 match (read_first 근거 반영)
    - Grep "OverlayCaptureRenderer.cs" in DatumMeasurement.csproj → 1 match
    - C# 8+ 문법 부재: Grep "switch expression(=>)" / "is .* pattern" 수동 확인 — 신규 파일에 `=>` 표현식 본문 메서드 0
  </acceptance_criteria>
  <done>off-screen 버퍼 윈도우에서 HalconDisplayService.Render 를 헤드리스 재사용해 캡쳐 HImage 를 생성하는 렌더러 완성. HalconDisplayService stateful 여부에 따른 스레드 안전성 분기 적용(lock/지역new/재사용 중 하나). 측정점 segment 식별 헬퍼는 오버레이 구조(1점=1오버레이 vs 다점) 근거로 카운트. CloseWindow finally 보장.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Action_FAIMeasurement 에 FAI별 캡쳐 enqueue + 파일명 write-back 삽입</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (L164-318 EStep.Measure, 특히 L170 using image, L172 FAIList loop, L306 fai.LastOverlays = faiOverlays 삽입 지점, L283-296 영역 — QueueRawImageSave 미존재 시 Action_TopInspection 참조)
    - WPF_Example/Custom/Sequence/Top/Action_TopInspection.cs (L283-296 QueueRawImageSave — image.CopyImage() 소유권 + saver null 가드 패턴)
  </read_first>
  <action>
    (1) FAI 루프 안, `fai.LastOverlays = faiOverlays;`(L306) **직후** 한 줄 삽입 — image 와 faiOverlays 가 모두 유효한 시점:
    ```csharp
    fai.LastOverlays = faiOverlays; // 기존 L306
    //260610 hbk Phase 40.2 — FAI별 origin/capture 캡쳐 enqueue + 파일명 write-back (오버레이+소스 이미지 확정 시점)
    QueueFaiCapture(fai, image, faiOverlays, ShotParam != null ? ShotParam.OwnerSequenceName : "");
    ```
    삽입 위치는 `if (fai.Measurements.Count > 0)` 블록 안(L295-306) — Measurements 0 케이스(L307-310 else)는 캡쳐 생략(측정 오버레이 없음). 즉 L306 (Measurements>0 분기 내부) 직후가 정확.

    (2) 클래스에 private 헬퍼 메서드 `QueueFaiCapture` 신규 추가(EStep enum/Run 메서드 밖, 클래스 레벨). 파일 기존 brace 스타일(K&R/Allman은 Read 로 확인 후 일치). 동기 파일명 생성 + write-back + 비동기 enqueue:
    ```csharp
    //260610 hbk Phase 40.2 — FAI별 원본/캡쳐 이미지를 비동기 저장 큐에 넣고, 파일명을 fai 에 동기 write-back.
    //  파일명은 BuildDto(AddResponse) 가 읽으므로 enqueue 전에 동기 확정. PNG write 만 워커가 비동기 수행.
    private void QueueFaiCapture(FAIConfig fai, HImage sourceImage, List<EdgeInspectionOverlay> faiOverlays, string sequenceName)
    {
        if (fai == null || sourceImage == null) return; //260610 hbk Phase 40.2
        var saver = SystemHandler.Handle.CaptureImageSaver;
        DateTime ts = DateTime.Now; //260610 hbk Phase 40.2 — origin/capture 동일 timestamp 공유 (쌍)
        string seg = OverlayCaptureRenderer.BuildMeasurePointSegment(faiOverlays); //260610 hbk Phase 40.2 — P1/P1P2/빈값
        string originName = CaptureImageSaveService.BuildFileName("origin", sequenceName, fai.FAIName, seg, ts); //260610 hbk Phase 40.2
        string captureName = CaptureImageSaveService.BuildFileName("capture", sequenceName, fai.FAIName, seg, ts); //260610 hbk Phase 40.2
        // 동기 write-back — BuildDto 가 즉시 읽을 수 있도록 (PNG write 실패와 무관하게 파일명은 확정)
        fai.LastOriginImageFileName = originName; //260610 hbk Phase 40.2
        fai.LastCaptureImageFileName = captureName; //260610 hbk Phase 40.2
        if (saver == null) return; //260610 hbk Phase 40.2 — 서비스 미기동 시 파일명만 기록, PNG skip

        // 원본 enqueue — 호출 스레드 원본 보호 위해 CopyImage() 사본 전달 (using image 가 dispose)
        saver.Enqueue(new CaptureImageSaveRequest //260610 hbk Phase 40.2
        {
            Image = sourceImage.CopyImage(),
            FileName = originName,
            IsCapture = false,
            Timestamp = ts
        });

        // 캡쳐 enqueue — 버퍼 윈도우에서 오버레이 렌더 (실패 시 null → enqueue skip)
        HImage captured = null; //260610 hbk Phase 40.2
        try { captured = _captureRenderer.RenderToHImage(sourceImage, faiOverlays); } //260610 hbk Phase 40.2
        catch { captured = null; }
        if (captured != null) //260610 hbk Phase 40.2 — captured 는 새 객체이므로 CopyImage 불필요, 워커가 Dispose
        {
            saver.Enqueue(new CaptureImageSaveRequest
            {
                Image = captured,
                FileName = captureName,
                IsCapture = true,
                Timestamp = ts
            });
        }
    }
    ```
    (3) 클래스에 `OverlayCaptureRenderer _captureRenderer` 필드 추가(생성자 또는 inline init). Action_FAIMeasurement 의 기존 필드 선언부에:
    ```csharp
    private readonly OverlayCaptureRenderer _captureRenderer = new OverlayCaptureRenderer(null); //260610 hbk Phase 40.2 — 헤드리스 캡쳐 렌더러 (내부서 HalconDisplayService 생성)
    ```
    (null 전달 → OverlayCaptureRenderer ctor 가 new HalconDisplayService() 폴백. HalconDisplayService 가 ctor 인자 필요하면 Read 로 확인 후 맞춰 전달.)

    (4) using 절 확인: `ReringProject.Halcon.Display`(OverlayCaptureRenderer), `ReringProject.Utility`(CaptureImageSaveService/Request), `ReringProject.Halcon.Models`(EdgeInspectionOverlay) 이 없으면 추가.

    (5) Wave 1+2 전체 msbuild Debug/x64 빌드 검증.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /nologo /v:minimal — exit 0, 신규 error 0 (MSBuild 경로 vswhere 로 탐색 가능)</automated>
  </verify>
  <acceptance_criteria>
    - Grep "QueueFaiCapture(fai, image, faiOverlays" in Action_FAIMeasurement.cs → 1 match (호출부, L306 직후)
    - Grep "private void QueueFaiCapture" → 1 match (정의)
    - Grep "fai.LastOriginImageFileName = originName" → 1 match
    - Grep "fai.LastCaptureImageFileName = captureName" → 1 match
    - Grep "CaptureImageSaveService.BuildFileName(\"origin\"" → 1 match, "capture" → 1 match
    - Grep "OverlayCaptureRenderer.BuildMeasurePointSegment" → 1 match
    - Grep "sourceImage.CopyImage()" → 1 match (원본 소유권 사본)
    - Grep "_captureRenderer.RenderToHImage" → 1 match
    - Grep "private readonly OverlayCaptureRenderer _captureRenderer" → 1 match (필드 — 필드명 정확히 `_captureRenderer`)
    - origin/capture 가 동일 ts 변수 공유: QueueFaiCapture 내 `DateTime ts = DateTime.Now` 1회 선언 + 두 BuildFileName/Request 가 ts 사용 (Read 확인)
    - msbuild Debug/x64 빌드 성공 (exit 0), 신규 error 0
  </acceptance_criteria>
  <done>Action_FAIMeasurement 가 FAI별로 원본 PNG + 측정 오버레이 캡쳐 PNG 를 비동기 enqueue 하고, 파일명을 fai 에 동기 write-back 한다. origin/capture 가 동일 timestamp·segment 로 쌍을 이룬다. Wave 1+2 msbuild 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 시퀀스 스레드 → off-screen HALCON 윈도우 | UI HWND 없는 시퀀스 스레드에서 buffer 윈도우 생성/dump. 리소스(윈도우 핸들) 누수 + 단일 _captureRenderer/_displayService 상태 공유 가능. |
| FAI 설정명/시퀀스명 → 파일명 | Plan 01 T-40.2-01 과 동일 경계 (BuildFileName 가 sanitize). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.2-03 | Denial of Service / resource leak | OverlayCaptureRenderer 버퍼 윈도우 핸들 | mitigate | `HOperatorSet.CloseWindow(win)` 를 finally 블록에서 무조건 호출(win null 가드 + try/catch). 렌더 예외가 검사 스레드를 죽이지 않도록 RenderToHImage 전체 try/catch → null 반환. enqueue 측도 captured null 가드. |
| T-40.2-04 | Tampering / Path traversal | 파일명 합성 (QueueFaiCapture) | mitigate | 파일명 생성은 전적으로 Plan 01 `CaptureImageSaveService.BuildFileName` 에 위임 — segment sanitize 가 그 안에서 수행됨(중복 구현 0, single source). QueueFaiCapture 는 raw 문자열을 직접 Path.Combine 하지 않음. |
| T-40.2-05 | Tampering / 동시성 상태 손상 | 단일 _captureRenderer→_displayService 가 시퀀스 스레드에서 FAI 루프 반복 호출 | mitigate | W1 — HalconDisplayService stateful 여부 판정 후 stateful 이면 호출당 지역 인스턴스 또는 lock 직렬화, stateless 면 재사용. 분기 결정을 코드 주석 + acceptance grep 로 강제. (현재 단일 시퀀스 스레드 내 직렬 호출이라 즉시 손상 위험은 낮으나, 향후 다중 시퀀스 공유 대비 방어.) |
</threat_model>

<verification>
- Wave 1+2 msbuild Debug/x64 빌드 성공, 신규 error/warning 0
- Grep 으로 호출부/정의/필드/소유권 패턴 매치 확인 (필드명 `_captureRenderer` 정확)
- W1 스레드 안전성 분기(lock/지역new/재사용 중 하나) grep 증거 확인
- W4 측정점 카운트가 오버레이 구조(1점=1오버레이 또는 다점 합산) 근거로 작성됨 — Plan 04 Test 2(P1P2)가 실측 안전망
- CloseWindow 가 finally 에 위치(윈도우 누수 0)
- 실제 PNG 생성은 Plan 04 SIMUL UAT 에서 디스크 확인
</verification>

<success_criteria>
- OverlayCaptureRenderer 가 buffer 윈도우 + Render 재사용 + DumpWindowImage 로 캡쳐 HImage 생성
- HalconDisplayService stateful 여부에 따른 스레드 안전성 분기 적용
- Action_FAIMeasurement 가 FAI별 origin/capture enqueue + 파일명 write-back
- origin/capture 가 동일 timestamp·segment 쌍
- 측정점 segment 가 오버레이 구조 근거로 P1/P1P2/빈값 구성
- Wave 1+2 msbuild Debug/x64 통과
</success_criteria>

<output>
완료 후 `.planning/phases/40.2-fai-2/40.2-02-SUMMARY.md` 생성.
</output>
</content>
</invoke>
