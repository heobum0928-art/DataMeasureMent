---
phase: 40.2-fai-2
plan: "02"
subsystem: inspection-capture-renderer
tags: [overlay-capture, headless-render, off-screen-window, fai-measurement, action]
dependency_graph:
  requires:
    - CaptureImageSaveService (Wave 1: async PNG worker + BuildFileName)
    - FAIConfig.LastOriginImageFileName / LastCaptureImageFileName (Wave 1: transient write-back fields)
    - SystemHandler.CaptureImageSaver (Wave 1: singleton)
  provides:
    - OverlayCaptureRenderer.RenderToHImage (off-screen buffer window headless capture)
    - OverlayCaptureRenderer.BuildMeasurePointSegment (P1/P1P2/empty segment helper)
    - Action_FAIMeasurement: QueueFaiCapture (per-FAI origin+capture enqueue + filename write-back)
  affects:
    - WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
    - WPF_Example/DatumMeasurement.csproj
tech_stack:
  added:
    - HALCON OpenWindow("buffer") + SetPart + DumpWindowImage off-screen pattern (codebase 최초)
  patterns:
    - stateful→지역new 분기: HalconDisplayService 인스턴스 상태 공유 방지
    - FAI-Edge 1점=1오버레이 구조 기반 측정점 카운트 (FAIEdgeMeasurementService 생성부 확인)
    - origin/capture 동일 timestamp 공유 (쌍 보장)
    - sourceImage.CopyImage() 소유권 사본 전달 (Action_TopInspection 패턴 동일)
key_files:
  created:
    - WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs
  modified:
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
    - WPF_Example/DatumMeasurement.csproj
decisions:
  - "HalconDisplayService stateful 판정: _isFontInitialized/_normalFontName 인스턴스 필드 + EnsureFontInitialized(window) 호출 확인 → stateful→지역new 분기 채택"
  - "측정점 카운트: FAIEdgeMeasurementService.BuildOverlaysBoth/Single 에서 FAI-Edge1/2 각각 별도 오버레이(Points 단일점) 확인 → 1점=1오버레이 구조, pointCount++ 방식"
  - "Logging namespace 오류: ReringProject.Network 가 아닌 ReringProject.Utility 에 위치 → Rule 1 auto-fix 적용"
  - "QueueFaiCapture 삽입 위치: fai.LastOverlays = faiOverlays 직후 (Measurements>0 분기 내부) — overlays+image 모두 확정된 시점"
metrics:
  duration: 30
  completed_date: "2026-06-10"
  tasks: 2
  files: 3
---

# Phase 40.2 Plan 02: OverlayCaptureRenderer + Action_FAIMeasurement 캡쳐 enqueue Summary

## One-liner

HALCON off-screen 버퍼 윈도우 + HalconDisplayService.Render 헤드리스 재사용으로 FAI별 측정 오버레이 캡쳐 HImage 생성 렌더러 신규 작성 + Action_FAIMeasurement에 origin/capture 쌍 enqueue 및 파일명 동기 write-back 삽입.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | OverlayCaptureRenderer 헤드리스 캡쳐 렌더러 신규 작성 | 87a5b7f | OverlayCaptureRenderer.cs, DatumMeasurement.csproj |
| 2 | Action_FAIMeasurement FAI별 캡쳐 enqueue + 파일명 write-back 삽입 | 41bbab1 | Action_FAIMeasurement.cs |

## Changes Made

### OverlayCaptureRenderer.cs (신규)

- `namespace ReringProject.Halcon.Display` — HalconDisplayService 와 동일 폴더/namespace, Allman brace 스타일
- `RenderToHImage(HImage, List<EdgeInspectionOverlay>)` — off-screen buffer 윈도우 open → SetPart → 지역 new HalconDisplayService().Render() → DumpWindowImage → HImage 반환. 실패 시 null (검사 스레드 생존 보장)
- **stateful→지역new 분기**: HalconDisplayService 가 `_isFontInitialized`/`_normalFontName` 인스턴스 필드 보유 + `EnsureFontInitialized(window)` 호출 확인 → 호출마다 지역 `new HalconDisplayService()` 로 인스턴스 상태 공유 방지
- `CloseWindow` finally 블록 보장 (T-40.2-03 버퍼 윈도우 누수 방지)
- `BuildMeasurePointSegment(List<EdgeInspectionOverlay>)` — FAI-Edge* RoiId 오버레이의 개수로 P1/P1P2/빈값 segment 구성 (1점=1오버레이 구조 확인)

### Action_FAIMeasurement.cs (수정)

- `using ReringProject.Halcon.Display;` 추가
- `private readonly OverlayCaptureRenderer _captureRenderer` 필드 추가 (inline init, `new OverlayCaptureRenderer()`)
- `fai.LastOverlays = faiOverlays;` 직후 `QueueFaiCapture(fai, image, faiOverlays, ...)` 호출 삽입 (Measurements>0 분기 내부)
- `QueueFaiCapture` private 메서드 신규: origin/capture 동일 ts 공유 → BuildFileName 동기 확정 → fai write-back → saver null 가드 → 원본 CopyImage 사본 enqueue → RenderToHImage 캡쳐 enqueue (null 가드)

### DatumMeasurement.csproj (수정)

- `<Compile Include="Halcon\Display\OverlayCaptureRenderer.cs" />` HalconDisplayService.cs 바로 아래 등록

## Build Verification

msbuild Debug/x64 실행 결과:
- **CS 컴파일 에러: 0개** (신규 0)
- MSB3027/MSB3021: 실행 중인 앱이 `bin\x64\Debug\DatumMeasurement.exe` 잠금 → 복사 실패 (기존 환경 이슈, Wave 1 SUMMARY 동일 결과)
- 기존 CS0618/CS0162 warnings: Wave 1 baseline 동일, 신규 0

## Acceptance Criteria Verification

| 항목 | 결과 |
|------|------|
| OverlayCaptureRenderer.cs 존재 | PASS |
| public class OverlayCaptureRenderer | PASS (1 match) |
| OpenWindow ... "buffer" | PASS (1 match) |
| DumpWindowImage | PASS (1 match) |
| CloseWindow (finally 블록 내) | PASS (1 match, finally) |
| new HalconDisplayService().Render(hwin, image, null, null, null ...) [stateful→지역new] | PASS |
| 스레드 안전성: 주석 1 match | PASS (line 16) |
| public static string BuildMeasurePointSegment | PASS (1 match) |
| StartsWith("FAI-Edge" | PASS (1 match) |
| 1점=1오버레이 근거 주석 | PASS (line 73) |
| OverlayCaptureRenderer.cs csproj 등록 | PASS (1 match) |
| QueueFaiCapture(fai, image, faiOverlays | PASS (1 match, 호출부) |
| private void QueueFaiCapture | PASS (1 match, 정의) |
| fai.LastOriginImageFileName = originName | PASS (1 match) |
| fai.LastCaptureImageFileName = captureName | PASS (1 match) |
| CaptureImageSaveService.BuildFileName("origin" | PASS (1 match) |
| CaptureImageSaveService.BuildFileName("capture" | PASS (1 match) |
| OverlayCaptureRenderer.BuildMeasurePointSegment | PASS (1 match) |
| sourceImage.CopyImage() | PASS (1 match) |
| _captureRenderer.RenderToHImage | PASS (1 match) |
| private readonly OverlayCaptureRenderer _captureRenderer | PASS (필드명 정확) |
| ts 1회 선언 + BuildFileName/Request 양쪽 사용 | PASS (Read 확인) |
| msbuild Debug/x64 CS 에러 0 | PASS |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] OverlayCaptureRenderer.cs Logging namespace 오류 수정**
- **Found during:** Task 1 → msbuild 에러 CS0103 발생
- **Issue:** `using ReringProject.Network;` 를 사용했으나 `Logging` 클래스는 `ReringProject.Utility` namespace에 있음
- **Fix:** `using ReringProject.Network;` → `using ReringProject.Utility;` 교체
- **Files modified:** WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs
- **Commit:** 87a5b7f (같은 커밋에 포함, 최종 커밋 전 수정)

## Known Stubs

없음. 모든 기능이 완전히 구현됨:
- `RenderToHImage`: off-screen 버퍼 윈도우 + HalconDisplayService.Render + DumpWindowImage 실제 구현
- `QueueFaiCapture`: 파일명 동기 확정 + write-back + 비동기 enqueue 실제 구현
- 실제 PNG 디스크 생성은 Plan 04 SIMUL UAT에서 확인

## Threat Surface Scan

T-40.2-03 (버퍼 윈도우 핸들 누수) mitigation 확인:
- `CloseWindow(win)` finally 블록에서 무조건 호출 (win null 가드 + try/catch)
- `RenderToHImage` 전체 try/catch → null 반환으로 검사 스레드 생존 보장
- `captured != null` 가드로 enqueue 측 보호

T-40.2-04 (파일명 합성 path traversal) mitigation 확인:
- `QueueFaiCapture` 는 파일명을 `CaptureImageSaveService.BuildFileName` 에 전적으로 위임
- `QueueFaiCapture` 에서 직접 `Path.Combine` 사용 0

T-40.2-05 (동시성 상태 손상) mitigation 확인:
- HalconDisplayService stateful 확인 → stateful→지역new 분기 적용
- RenderToHImage 내 `new HalconDisplayService()` 로 인스턴스 필드 공유 0

신규 네트워크 엔드포인트, auth 경로, DB 스키마 변경 없음.

## Self-Check: PASSED

- `WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs` — FOUND
- `WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` — FOUND (수정)
- `WPF_Example/DatumMeasurement.csproj` — FOUND (수정)
- Commits 87a5b7f, 41bbab1 — FOUND
