---
phase: 40.2-fai-2
plan: 03
type: execute
wave: 3
depends_on: ["40.2-01", "40.2-02"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs
  - WPF_Example/Custom/Export/ExcelExportService.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "CycleResultSerializer.BuildDto 가 검사 시점에 write-back 된 FAI 파일명을 FaiResultDto 로 복사한다"
    - "엑셀 export 에서 '이미지' 하이퍼링크 컬럼이 제거되고 '원본이미지 파일명'·'캡쳐이미지 파일명' 텍스트 2컬럼으로 교체된다"
    - "엑셀의 파일명 컬럼은 경로 아닌 파일명만 표시하고 하이퍼링크가 없다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs"
      provides: "faiDto 에 OriginImageFileName/CaptureImageFileName 주입"
      contains: "OriginImageFileName = fai.LastOriginImageFileName"
    - path: "WPF_Example/Custom/Export/ExcelExportService.cs"
      provides: "10컬럼 헤더 + 파일명 텍스트 2컬럼 (하이퍼링크 제거)"
      contains: "원본이미지 파일명"
  key_links:
    - from: "CycleResultSerializer.BuildDto (faiDto 생성)"
      to: "fai.LastOriginImageFileName / LastCaptureImageFileName"
      via: "DTO 복사"
      pattern: "fai.LastOriginImageFileName"
    - from: "ExcelExportService.Export (테이블 행)"
      to: "fai.OriginImageFileName / CaptureImageFileName"
      via: "ws.Cell(row,9/10).Value 텍스트"
      pattern: "fai.CaptureImageFileName"
---

<objective>
Wave 2 가 검사 시점에 fai 에 write-back 한 캡쳐 파일명을 직렬화/엑셀 export 경로에 흘려보낸다. CycleResultSerializer.BuildDto 의 faiDto 생성부에서 파일명 2필드를 복사하고, ExcelExportService 에서 기존 "이미지" 하이퍼링크 컬럼(D-07, 3385f7c)을 제거하여 "원본이미지 파일명"·"캡쳐이미지 파일명" 텍스트 2컬럼으로 교체한다.

Purpose: Wave 1(DTO 필드) + Wave 2(write-back) 를 소비하여 사용자가 보는 최종 산출물(cycle.json + xlsx)에 파일명을 노출한다. CONTEXT 엑셀 변경 + DTO 변경 결정 완성.
Output: CycleResultSerializer.cs 수정, ExcelExportService.cs 수정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40.2-fai-2/40.2-CONTEXT.md
@.planning/phases/40.2-fai-2/40.2-PATTERNS.md
@.planning/phases/40.2-fai-2/40.2-01-SUMMARY.md
@.planning/phases/40.2-fai-2/40.2-02-SUMMARY.md

<interfaces>
<!-- Wave 1 DTO 필드 -->
From WPF_Example/UI/ViewModel/CycleResultDto.cs (Plan 01):
- FaiResultDto.OriginImageFileName { get; set; }
- FaiResultDto.CaptureImageFileName { get; set; }

<!-- Wave 2 write-back 소스 -->
From WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs (Plan 01 + Plan 02 write):
- fai.LastOriginImageFileName  (검사 시점 채워짐, 미검사 시 "")
- fai.LastCaptureImageFileName

<!-- 수정 대상 -->
From WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs:
- L78-89: faiDto 생성 (foreach var fai in shot.FAIList) — FAIName/IsPass/WasDatumSkipped/LastOverlays 채움. ← 여기에 파일명 2필드 추가
- BuildDto 는 AddResponse 종합판정 확정 직후 호출 (L25-27 주석) — 파일명 이미 확정 시점

From WPF_Example/Custom/Export/ExcelExportService.cs:
- L46: `string[] headers = { "Shot", "FAI", "측정명", "Nominal", "Tol+", "Tol-", "측정값", "판정", "이미지" };` (9컬럼)
- L53-104: 3중 foreach (shot → fai → measurement), row 단위 ws.Cell 채움
- L90-99: D-07 이미지 하이퍼링크 블록 (shot.ResultImagePath 기반 SetHyperlink) ← 제거 대상
- 판정 컬럼은 row,8 까지 사용 — 이미지는 row,9
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CycleResultSerializer 에 파일명 2필드 복사 주입</name>
  <files>WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs (L78-89 faiDto 생성부 — 기존 필드 복사 패턴)
    - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs (Plan 01 추가 필드 LastOriginImageFileName/LastCaptureImageFileName 존재 확인)
  </read_first>
  <action>
    BuildDto 의 faiDto object initializer(L80-89)에 파일명 2필드 복사를 추가한다. 기존 WasDatumSkipped 다음 줄에:
    ```csharp
    var faiDto = new FaiResultDto
    {
        FAIName = fai.FAIName ?? "",
        IsPass = fai.IsPass,
        WasDatumSkipped = fai.WasDatumSkipped,
        OriginImageFileName = fai.LastOriginImageFileName ?? "",   //260610 hbk Phase 40.2 — 검사 시점 write-back 파일명 복사
        CaptureImageFileName = fai.LastCaptureImageFileName ?? "", //260610 hbk Phase 40.2
        LastOverlays = new List<EdgeInspectionOverlay>(
            fai.LastOverlays != null ? fai.LastOverlays : new List<EdgeInspectionOverlay>())
    };
    ```
    파일 brace 스타일(Allman) 유지. C# 7.2 범위.
  </action>
  <verify>
    <automated>MISSING — 단위 테스트 없음. 빌드는 Task 2 의 msbuild 에 통합. grep: Grep("OriginImageFileName = fai.LastOriginImageFileName", file) 1 match.</automated>
  </verify>
  <acceptance_criteria>
    - Grep "OriginImageFileName = fai.LastOriginImageFileName" → 1 match
    - Grep "CaptureImageFileName = fai.LastCaptureImageFileName" → 1 match
    - 두 라인 모두 `//260610 hbk Phase 40.2` 주석 포함
    - 두 필드가 faiDto object initializer 안(LastOverlays 와 동일 블록)에 위치 (Read 확인)
  </acceptance_criteria>
  <done>BuildDto 가 fai 의 write-back 파일명을 FaiResultDto 로 복사. cycle.json 에 파일명 직렬화됨.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ExcelExportService 하이퍼링크 컬럼 → 파일명 텍스트 2컬럼 교체 + 빌드 검증</name>
  <files>WPF_Example/Custom/Export/ExcelExportService.cs</files>
  <read_first>
    - WPF_Example/Custom/Export/ExcelExportService.cs (L44-104 — 헤더 배열 L46, 3중 foreach, 하이퍼링크 블록 L90-99)
  </read_first>
  <action>
    (1) 헤더 배열(L46) 교체 — "이미지"(9번째) 제거, "원본이미지 파일명"·"캡쳐이미지 파일명" 2개 추가 (총 10컬럼):
    ```csharp
    //260610 hbk Phase 40.2 — "이미지" 하이퍼링크 컬럼(D-07, 3385f7c) 폐기 → 파일명 텍스트 2컬럼으로 교체.
    string[] headers = { "Shot", "FAI", "측정명", "Nominal", "Tol+", "Tol-", "측정값", "판정", "원본이미지 파일명", "캡쳐이미지 파일명" };
    ```
    (2) 하이퍼링크 블록(L90-99) **전체 삭제** → fai 레벨 파일명 텍스트 2컬럼으로 교체. 파일명은 FAI 레벨이므로 (measurement 루프 안이지만) `fai` 에서 읽음. 같은 FAI 의 여러 measurement 행이 동일 파일명을 반복 표시(FAI별 1장이므로 정상). row,8 판정 다음:
    ```csharp
    //260610 hbk Phase 40.2 — D-07 하이퍼링크 컬럼 폐기(3385f7c 한글경로 file:/// 퍼센트인코딩 이슈도 동시 해소), 파일명 텍스트로 대체. 경로 아닌 파일명만, 하이퍼링크 없음.
    ws.Cell(row, 9).Value  = fai.OriginImageFileName ?? "";
    ws.Cell(row, 10).Value = fai.CaptureImageFileName ?? "";
    ```
    (3) 삭제 확인: `SetHyperlink` / `XLHyperlink` / `shot.ResultImagePath` 의 하이퍼링크 사용이 Export 의 테이블 셀 채우기에서 완전히 제거됨(File.Exists/Path.GetFullPath 의 하이퍼링크 가드도 함께 제거). using System.IO 가 다른 곳에서 안 쓰이면 그대로 둬도 무방(경고 아님).
    (4) try/catch + false 반환 + Logging + using(XLWorkbook) + AdjustToContents + SaveAs 골격은 무변경 유지.
    (5) Wave 1+2+3 전체 msbuild Debug/x64 빌드 검증.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /nologo /v:minimal — exit 0, 신규 error 0</automated>
  </verify>
  <acceptance_criteria>
    - Grep "원본이미지 파일명" in ExcelExportService.cs → 1 match
    - Grep "캡쳐이미지 파일명" → 1 match
    - Grep "ws.Cell(row, 9).Value  = fai.OriginImageFileName" (공백 무관) → 1 match
    - Grep "ws.Cell(row, 10).Value = fai.CaptureImageFileName" → 1 match
    - Grep "SetHyperlink" in ExcelExportService.cs → 0 match (하이퍼링크 완전 제거)
    - Grep "XLHyperlink" → 0 match
    - Grep "이미지 열기" → 0 match
    - 헤더 배열 원소 10개 (Read 확인: "판정" 다음 "원본이미지 파일명","캡쳐이미지 파일명")
    - msbuild Debug/x64 빌드 성공 (exit 0), 신규 error 0
  </acceptance_criteria>
  <done>엑셀에서 하이퍼링크 컬럼 제거 + 파일명 텍스트 2컬럼(원본/캡쳐) 추가. 경로 아닌 파일명만, 하이퍼링크 없음. Wave 1+2+3 전체 msbuild 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fai 파일명 문자열 → xlsx 셀 | 파일명이 엑셀 셀 텍스트로 기록됨. 텍스트 값만(하이퍼링크/수식 없음) → injection 표면 없음. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.2-05 | Injection (CSV/formula) | ExcelExportService 파일명 셀 | accept | 파일명은 Plan 01 sanitize 를 거친 영숫자+밑줄+.png. `=`/`+`/`@`/`-` 선두 위험 문자는 sanitize 로 제거되며 셀은 `.Value =` 텍스트 대입(수식 평가 없음). 로컬 단일 사용자가 자신의 데이터를 여는 시나리오 → 저위험. 하이퍼링크 제거로 기존 file:/// 경로 노출 표면도 축소. |
</threat_model>

<verification>
- Wave 1+2+3 msbuild Debug/x64 빌드 성공, 신규 error/warning 0
- Grep 으로 헤더/셀/하이퍼링크 제거 패턴 확인
- cycle.json 에 파일명 직렬화 + xlsx 에 텍스트 2컬럼은 Plan 04 SIMUL UAT 에서 산출물 확인
</verification>

<success_criteria>
- BuildDto 가 fai 파일명을 FaiResultDto 로 복사
- 엑셀 헤더 10컬럼 (이미지 하이퍼링크 → 원본/캡쳐 파일명 텍스트)
- SetHyperlink/XLHyperlink 완전 제거
- Wave 1+2+3 msbuild Debug/x64 통과
</success_criteria>

<output>
완료 후 `.planning/phases/40.2-fai-2/40.2-03-SUMMARY.md` 생성.
</output>
