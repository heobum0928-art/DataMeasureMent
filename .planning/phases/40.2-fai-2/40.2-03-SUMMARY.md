---
phase: 40.2-fai-2
plan: "03"
subsystem: inspection-serializer-excel
tags: [cycle-result-serializer, excel-export, dto-wiring, hyperlink-removal, filename-columns]
dependency_graph:
  requires:
    - FaiResultDto.OriginImageFileName / CaptureImageFileName (Wave 1: DTO 필드)
    - FAIConfig.LastOriginImageFileName / LastCaptureImageFileName (Wave 2: write-back 필드)
  provides:
    - CycleResultSerializer.BuildDto: faiDto 에 파일명 2필드 복사
    - ExcelExportService: 10컬럼 헤더 + 파일명 텍스트 2컬럼 (하이퍼링크 제거)
  affects:
    - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs
    - WPF_Example/Custom/Export/ExcelExportService.cs
tech_stack:
  added: []
  patterns:
    - faiDto object initializer 에 transient write-back 필드 복사 (null-coalescing ?? "" 방어)
    - 하이퍼링크 컬럼 폐기 + 텍스트 셀 대체 (SetHyperlink/XLHyperlink 완전 제거)
key_files:
  created: []
  modified:
    - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs
    - WPF_Example/Custom/Export/ExcelExportService.cs
decisions:
  - "faiDto 복사 위치: WasDatumSkipped 다음, LastOverlays 앞 — object initializer 순서 유지"
  - "ExcelExportService 파일명 컬럼: FAI 레벨 (measurement 루프 내, fai 참조) — 같은 FAI의 여러 measurement 행이 동일 파일명 반복 표시 (FAI별 1장 정책 정상)"
  - "System.IO using 보존: File.Exists 등 다른 용도로 사용 중이 아니어도 경고가 아니므로 그대로 유지"
metrics:
  duration: 15
  completed_date: "2026-06-10"
  tasks: 2
  files: 2
---

# Phase 40.2 Plan 03: CycleResultSerializer + ExcelExportService 파일명 배선 Summary

## One-liner

Wave 2 write-back 파일명을 CycleResultSerializer.BuildDto 에서 FaiResultDto 로 복사하고 ExcelExportService에서 D-07 하이퍼링크 컬럼을 제거하여 "원본이미지 파일명"·"캡쳐이미지 파일명" 텍스트 2컬럼으로 교체.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | CycleResultSerializer FAI 파일명 2필드 DTO 복사 | af735b0 | CycleResultSerializer.cs |
| 2 | ExcelExportService 하이퍼링크 컬럼 → 파일명 텍스트 2컬럼 교체 + 빌드 검증 | a46807f | ExcelExportService.cs |

## Changes Made

### CycleResultSerializer.cs (수정)

- `BuildDto` 의 faiDto object initializer(L80-89) 에 파일명 2필드 복사 추가
- `OriginImageFileName = fai.LastOriginImageFileName ?? ""` (Wave 2 write-back 소비)
- `CaptureImageFileName = fai.LastCaptureImageFileName ?? ""` (Wave 2 write-back 소비)
- WasDatumSkipped 와 LastOverlays 사이에 배치 (Plan 지정 위치 준수)
- 두 라인 모두 `//260610 hbk Phase 40.2` 주석 포함

### ExcelExportService.cs (수정)

- 헤더 배열 9컬럼 → 10컬럼: `"이미지"` 제거, `"원본이미지 파일명"`, `"캡쳐이미지 파일명"` 추가
- D-07 하이퍼링크 블록(L90-99, commit 3385f7c) 전체 제거:
  - `SetHyperlink` 호출 0
  - `XLHyperlink` 사용 0
  - `shot.ResultImagePath` 기반 `File.Exists`/`Path.GetFullPath` 가드 0
  - `"이미지 열기"` 텍스트 0
- 교체: `ws.Cell(row, 9).Value = fai.OriginImageFileName ?? ""` + `ws.Cell(row, 10).Value = fai.CaptureImageFileName ?? ""`
- 두 라인 모두 `//260610 hbk Phase 40.2` 주석 포함
- 한글경로 file:/// 퍼센트인코딩 이슈(3385f7c hotfix 근본 원인) 동시 해소

## Build Verification

msbuild Debug/x64 실행 결과:
- **CS 컴파일 에러: 0개** (신규 0)
- MSB3027/MSB3021: 실행 중인 앱이 `bin\x64\Debug\DatumMeasurement.exe` 잠금 → 복사 실패 (기존 환경 이슈, Wave 1/2 SUMMARY 동일 결과)
- 기존 CS0618/CS0162 warnings: Wave 1/2 baseline 동일, 신규 0

## Acceptance Criteria Verification

| 항목 | 결과 |
|------|------|
| Grep "OriginImageFileName = fai.LastOriginImageFileName" → 1 match | PASS |
| Grep "CaptureImageFileName = fai.LastCaptureImageFileName" → 1 match | PASS |
| 두 라인 모두 `//260610 hbk Phase 40.2` 주석 포함 | PASS |
| 두 필드 faiDto object initializer 안 위치 (WasDatumSkipped 다음) | PASS |
| Grep "원본이미지 파일명" in ExcelExportService.cs → 1 match | PASS |
| Grep "캡쳐이미지 파일명" → 1 match | PASS |
| ws.Cell(row, 9).Value = fai.OriginImageFileName → 1 match | PASS |
| ws.Cell(row, 10).Value = fai.CaptureImageFileName → 1 match | PASS |
| Grep "SetHyperlink" in ExcelExportService.cs → 0 match | PASS |
| Grep "XLHyperlink" → 0 match | PASS |
| Grep "이미지 열기" → 0 match | PASS |
| 헤더 배열 원소 10개 | PASS |
| msbuild Debug/x64 CS 에러 0 | PASS |

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

없음. 이 Plan은 Wave 1(DTO 필드) + Wave 2(write-back) 를 소비하여 cycle.json 직렬화 + xlsx export 경로에 파일명을 실제 흘려보내는 완전 구현이다. 실제 파일명 값 확인은 Plan 04 SIMUL UAT에서 수행.

## Threat Surface Scan

T-40.2-05 (Injection — CSV/formula) mitigation 확인:
- `ws.Cell(row, 9/10).Value = fai.OriginImageFileName ?? ""` / `fai.CaptureImageFileName ?? ""`
- 셀은 `.Value =` 텍스트 대입(수식 평가 없음)
- 파일명은 Plan 01 CaptureImageSaveService.BuildFileName/SanitizeFilePart 를 거친 값 (Wave 2 write-back 경로)
- `=`/`+`/`@`/`-` 선두 위험 문자는 sanitize 에서 제거됨
- 하이퍼링크 제거로 기존 file:/// 경로 노출 표면도 축소

신규 네트워크 엔드포인트, auth 경로, DB 스키마 변경 없음.

## Self-Check: PASSED

- `WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs` — FOUND (수정)
- `WPF_Example/Custom/Export/ExcelExportService.cs` — FOUND (수정)
- Commits af735b0, a46807f — FOUND (git log 확인)
