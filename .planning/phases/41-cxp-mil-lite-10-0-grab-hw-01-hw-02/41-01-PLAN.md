---
phase: 41-cxp-mil-lite-10-0-grab-hw-01-hw-02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/DatumMeasurement.csproj
  - WPF_Example/Device/Camera/VirtualCamera.cs
autonomous: true
requirements: [HW-01]
must_haves:
  truths:
    - "솔루션이 Matrox.MatroxImagingLibrary.dll 참조 하에 msbuild Debug/x64 0 errors 빌드된다"
    - "ECameraType enum 이 MIL 멤버를 포함한다 (DeviceHandler/MilCamera 가 case/생성자에서 사용 가능)"
  artifacts:
    - path: "WPF_Example/DatumMeasurement.csproj"
      provides: "Matrox MIL .NET DLL HintPath 참조"
      contains: "Matrox.MatroxImagingLibrary"
    - path: "WPF_Example/Device/Camera/VirtualCamera.cs"
      provides: "ECameraType.MIL enum 멤버"
      contains: "MIL"
  key_links:
    - from: "WPF_Example/DatumMeasurement.csproj"
      to: "C:\\Program Files\\Matrox Imaging\\MIL\\MIL.NET\\Matrox.MatroxImagingLibrary.dll"
      via: "Reference HintPath"
      pattern: "Matrox.MatroxImagingLibrary"
---

<objective>
MIL Lite 10.0 .NET 어셈블리(`Matrox.MatroxImagingLibrary.dll`)를 csproj 에 로컬 DLL 참조로 추가하고, `ECameraType` enum 에 `MIL` 멤버를 추가하여 후속 MilCamera 드라이버와 DeviceHandler switch case 의 컴파일 기반(foundation)을 마련한다. (HW-01: RAP4G4C12 CXP SDK = MIL Lite 10.0 확정/참조)

Purpose: MilCamera.cs(Plan 02)와 DeviceHandler.case MIL(Plan 03)이 이 enum 값과 DLL 참조 없이는 컴파일되지 않는다. 이 plan 은 가장 작은 foundation 변경만 수행한다.
Output: MIL DLL 참조가 추가된 csproj + MIL 멤버를 가진 ECameraType enum. 빌드는 여전히 0 errors (아직 MilCamera 미사용).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-RESEARCH.md
@.planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-PATTERNS.md

<interfaces>
<!-- VirtualCamera.cs L18-22 현재 enum (verbatim) -->
namespace ReringProject.Device {
    public enum ECameraType {
        Virtual,
        Basler,
        HIK,
    }

<!-- csproj 기존 로컬 DLL HintPath 선례 (halcondotnet = 절대경로, MvCamCtrl.Net = bin/x64) -->
<!-- MIL DLL 실제 경로 확인됨: C:\Program Files\Matrox Imaging\MIL\MIL.NET\Matrox.MatroxImagingLibrary.dll [VERIFIED] -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ECameraType enum 에 MIL 멤버 추가</name>
  <read_first>
    - WPF_Example/Device/Camera/VirtualCamera.cs (L18-22 enum 정의 — 편집 대상)
    - .planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-PATTERNS.md (VirtualCamera.cs enum 확장 섹션)
  </read_first>
  <files>WPF_Example/Device/Camera/VirtualCamera.cs</files>
  <action>
VirtualCamera.cs L18-22 의 `ECameraType` enum 정의를 다음으로 변경. `HIK,` 다음 줄에 `MIL,` 멤버 한 줄만 추가한다 (다른 enum/메서드/필드 변경 금지):

```csharp
    public enum ECameraType {
        Virtual,
        Basler,
        HIK,
        MIL,   //260602 hbk Phase 41 — CXP 카메라 MIL Lite 10.0
    }
```

CLAUDE.md 주석 관습 `//260602 hbk` 적용. 파일의 K&R brace 스타일(여는 중괄호 같은 줄)을 따른다.
  </action>
  <verify>
    <automated>Select-String -Path "WPF_Example\Device\Camera\VirtualCamera.cs" -Pattern "MIL,"</automated>
  </verify>
  <acceptance_criteria>
    - VirtualCamera.cs 의 ECameraType enum 에 `MIL,` 멤버가 존재한다 (Select-String "MIL," 매치)
    - 기존 멤버 Virtual/Basler/HIK 모두 보존 (3개 + MIL = 4개)
    - enum 외 다른 라인 변경 0
  </acceptance_criteria>
  <done>ECameraType enum 이 Virtual/Basler/HIK/MIL 4개 멤버를 가진다</done>
</task>

<task type="auto">
  <name>Task 2: csproj 에 Matrox MIL .NET DLL HintPath 참조 추가</name>
  <read_first>
    - WPF_Example/DatumMeasurement.csproj (기존 Reference ItemGroup — MvCamCtrl.Net / halcondotnet 블록을 찾아 그 인접에 추가)
    - .planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-PATTERNS.md (csproj MIL DLL 참조 추가 패턴 섹션)
    - .planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-RESEARCH.md (Standard Stack — DLL 경로)
  </read_first>
  <files>WPF_Example/DatumMeasurement.csproj</files>
  <action>
csproj 의 `<Reference Include="halcondotnet">` 블록 또는 `MvCamCtrl.Net` Reference 블록이 있는 `<ItemGroup>` 안에, 다음 Reference 를 추가한다 (halcondotnet 과 동일한 절대경로 HintPath 패턴):

```xml
<!-- 260602 hbk Phase 41 — Matrox MIL Lite 10.0 .NET binding (CXP grab) -->
<Reference Include="Matrox.MatroxImagingLibrary">
  <HintPath>C:\Program Files\Matrox Imaging\MIL\MIL.NET\Matrox.MatroxImagingLibrary.dll</HintPath>
  <Private>False</Private>
</Reference>
```

`<Private>False</Private>` 는 MIL 런타임이 PC 에 설치돼 있으므로 출력 디렉토리 미복사. 실제 DLL 존재 확인됨: `C:\Program Files\Matrox Imaging\MIL\MIL.NET\Matrox.MatroxImagingLibrary.dll` [VERIFIED].

이 task 는 Compile Include 를 추가하지 않는다 (MilCamera.cs 미존재 — Plan 02 에서 파일 작성과 함께 atomic 추가). 따라서 이 plan 종료 시점에도 빌드는 0 errors 여야 한다.
  </action>
  <verify>
    <automated>msbuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild /v:m</automated>
  </verify>
  <acceptance_criteria>
    - csproj 에 `Include="Matrox.MatroxImagingLibrary"` Reference 가 존재한다
    - HintPath 가 `C:\Program Files\Matrox Imaging\MIL\MIL.NET\Matrox.MatroxImagingLibrary.dll` 이다
    - msbuild Debug/x64 Rebuild 가 0 errors 로 성공한다 (신규 warning 0)
    - Compile Include 라인은 추가하지 않음 (MilCamera.cs 는 Plan 02 범위)
  </acceptance_criteria>
  <done>MIL DLL 참조가 csproj 에 추가되고 솔루션이 0 errors 빌드된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

이 phase 는 로컬 카메라 드라이버 통합으로 외부 입력 표면이 거의 없다. 네트워크/인증/직렬화 변경 없음. 이 plan(01)은 빌드 구성(csproj 참조 + enum 값)만 변경 — 런타임 동작 0.

| Boundary | Description |
|----------|-------------|
| (없음) | 이 plan 은 빌드 메타데이터만 변경, 런타임/입력 경계 미접촉 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering | csproj HintPath (절대경로 MIL DLL) | accept | 빌드 머신 로컬 DLL 경로, 외부 입력 아님. halcondotnet 동일 패턴 선례. high severity 없음 |

**위협 표면 LOW — high severity 없음.**
</threat_model>

<verification>
- msbuild Debug/x64 Rebuild 0 errors / 신규 warning 0
- ECameraType enum = Virtual/Basler/HIK/MIL
- MIL DLL 참조 해석됨 (빌드 성공으로 입증)
</verification>

<success_criteria>
- csproj 가 Matrox.MatroxImagingLibrary 를 참조하고 0 errors 빌드
- ECameraType.MIL 사용 가능 (후속 plan foundation)
</success_criteria>

<output>
After completion, create `.planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-01-SUMMARY.md`
</output>
