---
phase: 41-cxp-mil-lite-10-0-grab-hw-01-hw-02
plan: 03
type: execute
wave: 3
depends_on: [41-01, 41-02]
files_modified:
  - WPF_Example/Device/DeviceHandler.cs
  - WPF_Example/Custom/Device/DeviceHandler.cs
  - WPF_Example/Custom/SystemSetting.cs
autonomous: true
requirements: [HW-02]
must_haves:
  truths:
    - "DeviceHandler.InitializeDevices switch 가 ECameraType.MIL 케이스에서 new MilCamera 를 인스턴스화한다"
    - "MIL Open 실패 시 SIMUL_MODE 에서 AddVirtualCamera 폴백으로 초기화가 계속된다"
    - "RegisterRequiredDevices 가 SystemSetting CameraRole(TopBottom/Side) 기반으로 PC별 CXP 1대를 등록한다"
    - "SystemSetting 이 CameraRole 을 INI 직렬화 가능한 타입(int)으로 영속화한다"
    - "솔루션이 msbuild Debug/x64 0 errors 빌드된다"
  artifacts:
    - path: "WPF_Example/Device/DeviceHandler.cs"
      provides: "ECameraType.MIL switch case (MilCamera 인스턴스화 + SIMUL 폴백)"
      contains: "case ECameraType.MIL"
    - path: "WPF_Example/Custom/Device/DeviceHandler.cs"
      provides: "PC별 역할 기반 CXP 1대 등록"
      contains: "ECameraType.MIL"
    - path: "WPF_Example/Custom/SystemSetting.cs"
      provides: "CameraRole INI 영속 설정 + ECameraRole 변환"
      contains: "CameraRole"
  key_links:
    - from: "WPF_Example/Device/DeviceHandler.cs"
      to: "MilCamera"
      via: "case ECameraType.MIL: new MilCamera(Config, id)"
      pattern: "new MilCamera"
    - from: "WPF_Example/Custom/Device/DeviceHandler.cs"
      to: "SystemSetting.Handle.CameraRole"
      via: "RegisterRequiredDevices 역할 분기"
      pattern: "CameraRole"
---

<objective>
DeviceHandler 가 `ECameraType.MIL` 케이스에서 `MilCamera` 를 인스턴스화하도록 factory switch 에 case 를 추가하고, `RegisterRequiredDevices` 를 "HIK 3대 고정"에서 "PC별 CXP 1대 + 역할(시퀀스) 설정"으로 재구성한다. 역할 선택은 SystemSetting INI 의 `CameraRole`(TopBottom/Side) 기반. (HW-02: HIK 와 동일 GrabHalconImage 경로 + D-03 PC별 역할)

Purpose: Plan 02 의 MilCamera 가 실제로 DeviceHandler 를 통해 등록/Open 되어야 시퀀스 grab 경로가 CXP 로 동작한다. HIK case/enum 은 잔존(D-01) — 회귀 최소화.
Output: MIL switch case + 역할 기반 등록 + CameraRole INI 설정. SIMUL_MODE 에서 Open 실패 시 VirtualCamera 폴백.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-RESEARCH.md
@.planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-PATTERNS.md

<interfaces>
<!-- DeviceHandler.InitializeDevices switch (verbatim 구조, DeviceHandler.cs L103-222) -->
switch (IDList[i].CamType) {
    case ECameraType.Virtual: { ... AddVirtualCamera(id); } break;
    case ECameraType.Basler:  { ... new BaslerCamera(Config, id); ... Devices.Add(devName, newCam); ... } break;
    case ECameraType.HIK:     { ... new HikCamera(Config, id); ... Devices.Add(devName, newCam); ... } break;
    // ← 여기에 case ECameraType.MIL 추가
}

<!-- SetRequiredDevice 시그니처 (verbatim, DeviceHandler.cs L263) -->
public void SetRequiredDevice(ECameraType type, ECaptureImageType imageType, ETriggerSource triggerSource,
    string identifier, int width, int height, bool reverseX, bool reverseY, ERotateAngleType rotateAngle = 0);

<!-- AddVirtualCamera (verbatim, DeviceHandler.cs L272-281) — SIMUL 폴백에 사용 -->
private void AddVirtualCamera(DeviceInfo id) { ... VirtualCamera vCam = new VirtualCamera(Config, id); vCam.Open(...); Devices.Add(id.Identifier, vCam); }

<!-- 현재 RegisterRequiredDevices (verbatim, Custom/Device/DeviceHandler.cs L86-121) — HIK 3대 -->
private void RegisterRequiredDevices() {
    SetRequiredDevice(ECameraType.HIK, ECaptureImageType.Gray8, ETriggerSource.Software, CAMERA_TOP, WIDTH_TOP, HEIGHT_TOP, REVERSE_X_TOP, REVERSE_Y_TOP, ROTATE_TOP);
    SetRequiredDevice(ECameraType.HIK, ..., CAMERA_SIDE, ...);
    SetRequiredDevice(ECameraType.HIK, ..., CAMERA_BOTTOM, ...);
}

<!-- 상수 (Custom/Device/DeviceHandler.cs L13-15) -->
public const string CAMERA_TOP = "CAM_TOP";
public const string CAMERA_SIDE = "CAM_SIDE";
public const string CAMERA_BOTTOM = "CAM_BOTTOM";

<!-- ⚠ CRITICAL: SystemSetting.Save/Load (SystemSetting.cs L179-308) 는 reflection 기반이지만
     switch(type) 가 Int32/Double/String/Boolean/Rect/Line/Circle/PropertyItem[]/ModelFinderViewModel 만 처리한다.
     ENUM 타입은 직렬화 케이스가 없어 INI 저장/로드가 동작하지 않는다.
     → Phase 12 D-12 선례(AlgorithmType 을 enum 대신 string 저장)와 동일하게,
       CameraRole 은 반드시 int(또는 string)로 저장하고 enum 변환 헬퍼를 제공해야 한다. -->

<!-- SystemSetting 프로퍼티 + Category 선례 (SystemSetting.cs L35-36) -->
[Category("Connection|Server")]
public int ServerPort { get; set; } = 2505;

<!-- Custom/SystemSetting.cs 현재 (verbatim 전체) -->
namespace ReringProject.Setting {
    public partial class SystemSetting {
    }
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SystemSetting 에 CameraRole INI 설정 + ECameraRole enum 추가</name>
  <read_first>
    - WPF_Example/Custom/SystemSetting.cs (편집 대상 — 현재 빈 partial)
    - WPF_Example/Setting/SystemSetting.cs (L179-308 Save/Load switch(type) — enum 미지원 확인)
    - .planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-PATTERNS.md (ECameraRole enum + SystemSetting CameraRole 패턴)
  </read_first>
  <files>WPF_Example/Custom/SystemSetting.cs</files>
  <action>
`Custom/SystemSetting.cs` 의 `public partial class SystemSetting {}` 본문에 다음을 추가한다. CLAUDE.md `//260602 hbk Phase 41` 주석 적용.

**INI 직렬화 제약 대응 (CRITICAL):** SystemSetting.Save/Load 의 `switch(type)` 는 enum 을 처리하지 않으므로(read_first 확인), CameraRole 을 **int 백킹 프로퍼티 `CameraRoleValue`** 로 저장하고, `ECameraRole CameraRole` 은 `[Browsable(false)]` non-직렬화 변환 프로퍼티로 노출한다 (Phase 12 D-12 AlgorithmType string 저장 선례와 동일 패턴):

```csharp
using System.ComponentModel;
using ReringProject.Device;   // ECameraRole

namespace ReringProject.Setting {
    //260602 hbk Phase 41 — PC별 CXP 카메라 역할 (TopBottom / Side)
    public enum ECameraRole {
        TopBottom = 0,   // PC1: Top + Bottom 시퀀스
        Side      = 1,   // PC2: Side 시퀀스
    }

    public partial class SystemSetting {
        // INI 직렬화용 int 백킹 (SystemSetting.Save/Load switch(type) 가 Int32 지원)
        [Category("System|Camera")]
        public int CameraRoleValue { get; set; } = 0;   // 0 = TopBottom

        // 코드 사용용 enum 변환 프로퍼티 (직렬화 제외)
        [Browsable(false)]
        public ECameraRole CameraRole {
            get { return (ECameraRole)CameraRoleValue; }
            set { CameraRoleValue = (int)value; }
        }
    }
}
```

주의: `ECameraRole` 의 배치 위치는 `ReringProject.Device` namespace 가 자연스러우나(카메라 도메인), Custom/SystemSetting.cs 가 `ReringProject.Setting` 이므로 위처럼 Setting namespace 에 두고 `using ReringProject.Device` 없이 사용하거나, RESEARCH/PATTERNS 의 권고대로 Device namespace 에 두고 using 추가 중 택1. Plan 02 의 MilCamera 와 Custom/Device/DeviceHandler 가 동일 enum 을 참조하므로 **단일 정의 위치**를 유지하고 두 곳에서 같은 fully-qualified 이름으로 접근한다. Rule 3: 중복 정의 시 CS0101 → 한 곳만 남긴다.
  </action>
  <verify>
    <automated>Select-String -Path "WPF_Example\Custom\SystemSetting.cs" -Pattern "CameraRole|ECameraRole|CameraRoleValue"</automated>
  </verify>
  <acceptance_criteria>
    - Custom/SystemSetting.cs 에 `ECameraRole` enum (TopBottom/Side) 정의 존재
    - `int CameraRoleValue` 프로퍼티 (INI 직렬화용, [Category("System|Camera")]) 존재
    - `ECameraRole CameraRole` 변환 프로퍼티 ([Browsable(false)]) 존재
    - enum 을 직접 직렬화 프로퍼티로 노출하지 않음 (Save/Load switch(type) 미지원 회피)
  </acceptance_criteria>
  <done>CameraRole 이 int 로 INI 영속되고 ECameraRole 로 변환 노출된다</done>
</task>

<task type="auto">
  <name>Task 2: DeviceHandler.InitializeDevices 에 case ECameraType.MIL 추가</name>
  <read_first>
    - WPF_Example/Device/DeviceHandler.cs (L103-222 switch, L167-219 HIK case 구조, L272-281 AddVirtualCamera)
    - .planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-PATTERNS.md (DeviceHandler switch case 추가 패턴 + "MIL enumerate 불필요 이유")
  </read_first>
  <files>WPF_Example/Device/DeviceHandler.cs</files>
  <action>
DeviceHandler.cs 의 switch 문에서 `case ECameraType.HIK: { ... } break;` (L167-220) 블록 **뒤에** 다음 case 를 추가한다. CLAUDE.md `//260602 hbk Phase 41` 주석 적용. 파일 K&R 스타일 따름.

MIL 은 HIK 처럼 EnumerateDevice/ContainsDevice 사전 조회가 없다 (MdigAlloc 이 직접 보드/채널 연결). Open() 성공/실패로만 판별:

```csharp
case ECameraType.MIL: {
    //260602 hbk Phase 41 — CXP 카메라 MIL grab. enumerate 없음, Open() 으로 판별.
    MilCamera newCam = new MilCamera(Config, id);
    if (!newCam.Open()) {
        result &= ~EInitializeResult.Success;
        result |= EInitializeResult.OpenFail;
#if SIMUL_MODE
        AddVirtualCamera(id);   // 보드 미설치 시 VirtualCamera 파일 grab 폴백 (Pitfall 1)
#endif
        continue;
    }
    Devices.Add(id.Identifier, newCam);
}
break;
```

주의: `id` 와 `devName` 변수가 switch 진입부에 이미 선언돼 있음(L99-101 확인). HIK case 의 `Devices.Add(devName, newCam)` 패턴을 보되, MIL 은 enumerate 가 없으므로 `id.Identifier`(= devName)를 키로 사용한다. devName 이 null 가능하면 HIK 패턴 동일하게 처리. Rule 1: 변수 스코프/중복 선언 CS0136 발생 시 즉시 교정.
  </action>
  <verify>
    <automated>Select-String -Path "WPF_Example\Device\DeviceHandler.cs" -Pattern "case ECameraType.MIL|new MilCamera"</automated>
  </verify>
  <acceptance_criteria>
    - DeviceHandler.cs switch 에 `case ECameraType.MIL:` 존재
    - `new MilCamera(Config, id)` 인스턴스화
    - Open() 실패 시 `#if SIMUL_MODE` 블록에서 `AddVirtualCamera(id)` 폴백 + continue
    - 성공 시 Devices.Add 등록
    - 기존 Virtual/Basler/HIK case 무변경 (회귀 0)
  </acceptance_criteria>
  <done>MIL 카메라가 DeviceHandler factory 에서 인스턴스화/등록되고 실패 시 SIMUL 폴백</done>
</task>

<task type="auto">
  <name>Task 3: RegisterRequiredDevices PC별 역할 재구성 + CXP 상수 + 빌드</name>
  <read_first>
    - WPF_Example/Custom/Device/DeviceHandler.cs (L13-15 카메라 이름 상수, L86-121 RegisterRequiredDevices — 편집 대상)
    - .planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-PATTERNS.md (RegisterRequiredDevices 재구성 패턴 + WIDTH_CXP/HEIGHT_CXP)
    - .planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-RESEARCH.md (Open Q2/Q3 — PC1 Top+Bottom 카메라 공유, 해상도 TBD)
  </read_first>
  <files>WPF_Example/Custom/Device/DeviceHandler.cs</files>
  <action>
Custom/Device/DeviceHandler.cs 의 카메라 이름 상수(L13-15) 부근에 CXP 해상도 상수를 추가한다 (실물 도착 후 MdigInquire 로 확정 — TBD 주석):

```csharp
//260602 hbk Phase 41 — CXP ViewWorks 128MP 해상도 (실물 도착 후 MdigInquire M_SIZE_X/Y 로 확정, RESEARCH Open Q3)
public const int WIDTH_CXP  = 14192;   // TBD: 실측 후 교정
public const int HEIGHT_CXP = 10640;   // TBD: 실측 후 교정
```

그리고 `RegisterRequiredDevices()` (L86-121) 본문을 다음으로 교체한다 (RESEARCH Pattern 3, D-02/D-03). PC1(TopBottom)은 카메라 1대로 Top+Bottom 두 시퀀스를 담당 — **동일 물리 카메라이지만 시퀀스 분기를 위해 CAMERA_TOP/CAMERA_BOTTOM 두 이름으로 등록하되 둘 다 MIL** (Open Q2 권고: 별도 등록). PC2(Side)는 CAMERA_SIDE 1대만:

```csharp
private void RegisterRequiredDevices() {
    //260602 hbk Phase 41 — D-03 PC별 CXP 1대 + 역할(시퀀스) 설정. HIK 3대 고정 → 역할 분기.
    ECameraRole role = SystemSetting.Handle.CameraRole;

    if (role == ECameraRole.TopBottom) {
        // PC1: CXP 카메라 — Top + Bottom 시퀀스 담당 (D-02)
        SetRequiredDevice(ECameraType.MIL, ECaptureImageType.Gray8, ETriggerSource.Software,
            CAMERA_TOP, WIDTH_CXP, HEIGHT_CXP, REVERSE_X_TOP, REVERSE_Y_TOP, ROTATE_TOP);
        SetRequiredDevice(ECameraType.MIL, ECaptureImageType.Gray8, ETriggerSource.Software,
            CAMERA_BOTTOM, WIDTH_CXP, HEIGHT_CXP, REVERSE_X_BOTTOM, REVERSE_Y_BOTTOM, ROTATE_BOTTOM);
    }
    else { // ECameraRole.Side — PC2
        SetRequiredDevice(ECameraType.MIL, ECaptureImageType.Gray8, ETriggerSource.Software,
            CAMERA_SIDE, WIDTH_CXP, HEIGHT_CXP, REVERSE_X_SIDE, REVERSE_Y_SIDE, ROTATE_SIDE);
    }
}
```

주의: `ECameraRole` 는 Task 1 에서 정의한 enum. namespace 가 다르면 `using` 추가 (Custom/Device/DeviceHandler.cs 는 `ReringProject.Device`, Task 1 이 Setting namespace 에 enum 을 뒀다면 `using ReringProject.Setting;` 추가). Rule 1: CS0246 발생 시 enum 정의 namespace 에 맞춰 using 교정.
SIMUL_MODE 에서는 MIL Open 이 M_SYSTEM_HOST 로 시도되고 실패 시 Task 2 의 AddVirtualCamera 폴백이 동작하여 기존 SIMUL 파일 grab 경로가 유지된다 (D-11 회귀 0).

이 task 마지막에 솔루션 전체를 빌드하여 0 errors 확인.
  </action>
  <verify>
    <automated>msbuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild /v:m</automated>
  </verify>
  <acceptance_criteria>
    - Custom/Device/DeviceHandler.cs 에 WIDTH_CXP / HEIGHT_CXP 상수 존재
    - RegisterRequiredDevices 가 `SystemSetting.Handle.CameraRole` 분기 사용
    - TopBottom 분기 = CAMERA_TOP + CAMERA_BOTTOM 모두 ECameraType.MIL 등록
    - Side 분기 = CAMERA_SIDE 1대 ECameraType.MIL 등록
    - HIK 등록 라인 제거됨 (CXP 로 대체, D-01 — enum/드라이버 클래스는 잔존)
    - msbuild Debug/x64 Rebuild 0 errors / 신규 warning 0
  </acceptance_criteria>
  <done>RegisterRequiredDevices 가 PC별 역할 기반 CXP 등록으로 재구성되고 솔루션이 빌드된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INI 파일 → SystemSetting.CameraRoleValue | 로컬 설정 파일 입력 (int 파싱). 외부 네트워크 아님 |

로컬 카메라/설정 통합 — 네트워크/인증 변경 없음. 위협 표면 LOW.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-05 | Tampering (잘못된 설정값) | CameraRoleValue INI int | mitigate | `(ECameraRole)CameraRoleValue` 캐스팅 — 정의 외 정수는 else 분기(Side) 로 안전 폴백. RegisterRequiredDevices 가 if/else 로 미정의값도 한 경로 보장 (크래시 없음) |
| T-41-06 | Denial of Service (전체 초기화 실패) | MIL Open 실패 시 IsInitializeFail | mitigate | SIMUL_MODE 에서 AddVirtualCamera 폴백(Task 2)으로 초기화 계속. 실 HW 미설치가 전체 앱 기동을 막지 않음 (Pitfall 5) |

**high severity 보안 위협 없음.**
</threat_model>

<verification>
- msbuild Debug/x64 Rebuild 0 errors / 신규 warning 0
- case ECameraType.MIL + new MilCamera + SIMUL 폴백 존재
- RegisterRequiredDevices = CameraRole 역할 분기
- CameraRole int INI 영속 + enum 변환
</verification>

<success_criteria>
- MIL 카메라가 DeviceHandler 를 통해 등록/Open/grab
- PC별 역할(TopBottom/Side) 기반 CXP 1대 등록
- SIMUL_MODE 폴백으로 기존 3 시퀀스 경로 회귀 0
- 솔루션 0 errors 빌드
</success_criteria>

<output>
After completion, create `.planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-03-SUMMARY.md`
</output>
