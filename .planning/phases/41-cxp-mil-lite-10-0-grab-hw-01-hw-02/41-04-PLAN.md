---
phase: 41-cxp-mil-lite-10-0-grab-hw-01-hw-02
plan: 04
type: execute
wave: 4
depends_on: [41-01, 41-02, 41-03]
files_modified:
  - .planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-UAT.md
autonomous: false
requirements: [HW-01, HW-02]
must_haves:
  truths:
    - "SIMUL_MODE 빌드에서 앱이 정상 기동하고 CameraRole=TopBottom 으로 Top/Bottom 시퀀스가 grab 한다"
    - "MIL Open 실패가 AddVirtualCamera 폴백으로 흡수되어 기존 SIMUL 파일 grab 경로가 회귀 없이 동작한다"
    - "msbuild Debug/x64 0 errors 가 전 plan 통합 후에도 유지된다"
  artifacts:
    - path: ".planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-UAT.md"
      provides: "SIMUL UAT 결과 + sign-off"
      contains: "Test"
  key_links:
    - from: "41-UAT.md"
      to: "MilCamera SIMUL grab 경로"
      via: "수동 UAT 결과 기록"
      pattern: "PASS|FAIL"
---

<objective>
Plan 01~03 통합 후 SIMUL_MODE 에서 falsifiable UAT 를 수행한다. 실 HW(RAP4G4C12 보드 + ViewWorks 카메라) 미도착이므로, SIMUL_MODE 파일 grab 경로 + MIL 코드 구조/컴파일 + 회귀 0 을 검증한다. 실 HW grab(MsysAlloc 시스템 디스크립터, MdigInquire 해상도)은 보드 도착 후 별도 재측정으로 격리한다. (HW-01/HW-02 SIMUL 기준 sign-off)

Purpose: HW 의존 코드라 자동 테스트가 구조적으로 어렵다(RESEARCH Validation Architecture). 빌드 + SIMUL 런타임 + 회귀가 phase gate.
Output: 41-UAT.md (5 테스트 결과 + sign-off). 코드 변경 0 (검증 전용 plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-RESEARCH.md
@.planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-01-SUMMARY.md
@.planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-02-SUMMARY.md
@.planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 통합 빌드 + 정적 검증 + 41-UAT.md 스캐폴드 생성</name>
  <read_first>
    - .planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-01-SUMMARY.md
    - .planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-02-SUMMARY.md
    - .planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-03-SUMMARY.md
    - .planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-RESEARCH.md (Validation Architecture — Phase Requirements → Test Map)
  </read_first>
  <files>.planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-UAT.md</files>
  <action>
먼저 솔루션 통합 빌드를 실행하여 0 errors 확인:
`msbuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild /v:m`

다음 정적 검증을 Select-String 으로 수행하여 결과를 기록:
- VirtualCamera.cs ECameraType.MIL 존재
- MilCamera.cs `class MilCamera : VirtualCamera` + MIL.MdigGrab + GenImage1("byte" + #if SIMUL_MODE 폴백
- DeviceHandler.cs `case ECameraType.MIL` + new MilCamera + AddVirtualCamera 폴백
- Custom/Device/DeviceHandler.cs RegisterRequiredDevices CameraRole 분기 + WIDTH_CXP/HEIGHT_CXP
- Custom/SystemSetting.cs CameraRole + CameraRoleValue (int 직렬화)
- csproj Matrox.MatroxImagingLibrary Reference + MilCamera.cs Compile Include

`41-UAT.md` 를 생성한다. frontmatter status=partial. 5 테스트(아래 Task 2 의 항목)를 result=pending 으로 스캐폴드. Test 1/2/5 는 자동/코드 검증 결과를 이 task 에서 채울 수 있으면 채운다(빌드+정적). Test 3/4 는 human-verify 대기로 둔다.
  </action>
  <verify>
    <automated>msbuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild /v:m</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 Rebuild 0 errors / 신규 warning 0
    - 정적 검증 6 항목 전부 매치 확인 (위 grep 목록)
    - 41-UAT.md 생성, 5 테스트 스캐폴드 (frontmatter status=partial)
  </acceptance_criteria>
  <done>통합 빌드 PASS + 정적 검증 기록 + UAT 스캐폴드 존재</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SIMUL_MODE 런타임 UAT (사용자 육안 확인)</name>
  <files>(없음 — 사용자 육안 검증, 코드 변경 0)</files>
  <action>
이 태스크는 사용자 육안 검증 체크포인트다. 실행자는 자동화할 수 있는 것(빌드/정적 검증)을 Task 1 에서 이미 완료했으며, 여기서는 사용자에게 아래 how-to-verify 절차를 제시하고 SIMUL_MODE 런타임 결과를 수집한다. 실행자가 직접 수행할 추가 작업은 없다 — 사용자 응답(PASS/FAIL)을 기다린 뒤 Task 3 으로 진행한다.
  </action>
  <what-built>
Plan 01~03 으로 MIL Lite 10.0 기반 `MilCamera : VirtualCamera` 드라이버가 추가되고, DeviceHandler 가 ECameraType.MIL 로 인스턴스화하며, RegisterRequiredDevices 가 CameraRole(TopBottom/Side) 기반 CXP 1대 등록으로 재구성되었다. SIMUL_MODE 에서 MIL Open 은 M_SYSTEM_HOST 로 시도되고 보드 미설치 시 AddVirtualCamera 파일 grab 폴백이 동작한다.
  </what-built>
  <how-to-verify>
SIMUL_MODE Debug/x64 로 앱을 실행하고 다음을 확인한다 (실 CXP 보드 없이):

**Test 1 (빌드/정적):** 통합 빌드 0 errors — Task 1 에서 자동 확인됨. PASS 여부 확인.

**Test 2 (앱 기동):** Debug/x64(SIMUL_MODE) 앱이 크래시 없이 기동하는가? IsInitializeFail 모달이 뜨지 않는가? (MIL Open 실패가 AddVirtualCamera 폴백으로 흡수되어야 함)

**Test 3 (SIMUL grab — TopBottom 역할):** Setting.ini 의 CameraRoleValue=0 (TopBottom). 앱 실행 → Top/Bottom 시퀀스가 SIMUL 이미지(Cal_Image/ 또는 SimulatedImagePath)를 grab 하여 MainView 에 표시되는가? 측정/검사 1 사이클이 기존과 동일하게 동작하는가? (회귀 0)

**Test 4 (SIMUL grab — Side 역할):** Setting.ini 의 CameraRoleValue=1 (Side) 로 변경 후 재기동 → Side 시퀀스가 SIMUL grab/표시되는가? (Top/Bottom 미등록 — 역할 분리 동작 확인)

**Test 5 (회귀):** 기존 Datum 검출 / FAI 측정 / 결과 표시 경로가 SIMUL 에서 Phase 40.1 baseline 과 동일하게 동작하는가? (MilCamera 가 VirtualCamera 파일 grab 으로 폴백하므로 grab 결과는 SIMUL 동일)

실 HW grab(MdigGrab, M_SYSTEM_DEFAULT, 실 해상도)은 RAP4G4C12 보드 + ViewWorks 카메라 도착 후 별도 검증 — 이번 sign-off 범위 밖(RESEARCH Open Q1/Q3, Pitfall 1).
  </how-to-verify>
  <verify>사용자가 5 Test 의 PASS/FAIL 을 회신할 때까지 대기 (blocking gate)</verify>
  <done>사용자가 5 Test 각각에 PASS/FAIL 을 회신하고 "approved" 또는 결함 내용을 기술한다</done>
  <resume-signal>각 Test 의 PASS/FAIL 을 알려주세요. "approved" 또는 결함 내용을 기술. FAIL 시 carry-over(CO-41-xx)로 기록 후 hotfix 또는 후속 phase 결정.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 41-UAT.md sign-off 기록</name>
  <read_first>
    - .planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-UAT.md (Task 1 스캐폴드)
  </read_first>
  <files>.planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-UAT.md</files>
  <action>
Task 2 의 사용자 결과를 41-UAT.md 5 테스트에 기록한다. 각 Test 에 result(PASS/FAIL) + 근거(자동 빌드 / 사용자 육안 확인 일자 / 코드 검증 인용). frontmatter status 를 결과에 맞춰 갱신:
- 전부 PASS → status=signed_off, Summary total:5/passed:5
- 일부 FAIL → status=partial, FAIL 항목을 CO-41-xx carry-over 로 명시 (실 HW 의존 항목은 보드 도착 후 별도 phase 로 격리)

HW-01/HW-02 가 SIMUL 기준 충족됨을 명시하고, 실 HW grab 검증은 보드 도착 후 재측정(RESEARCH Open Q1/Q3)이 잔여임을 기록.
  </action>
  <verify>
    <automated>Select-String -Path ".planning\phases\41-cxp-mil-lite-10-0-grab-hw-01-hw-02\41-UAT.md" -Pattern "status:|PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - 41-UAT.md 의 5 테스트 모두 result 기록 (PASS/FAIL, pending 0)
    - frontmatter status = signed_off 또는 partial (결과 일치)
    - FAIL 시 CO-41-xx carry-over 명시
    - 실 HW grab 잔여(보드 도착 후) 명시
  </acceptance_criteria>
  <done>SIMUL UAT 결과가 sign-off 로 기록되고 HW-01/HW-02 SIMUL 충족이 명시된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

이 plan 은 검증/문서(UAT.md) 전용 — 코드/런타임 변경 0. 위협 표면 없음.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-07 | (해당 없음) | UAT 문서 | accept | 검증 전용 plan, 코드 변경 0. 위협 표면 없음 |

**high severity 위협 없음 — 검증 전용.**
</threat_model>

<verification>
- 통합 msbuild Debug/x64 0 errors
- SIMUL 앱 기동 + grab + 회귀 0 (사용자 육안)
- 41-UAT.md 5 테스트 result 기록 + sign-off
</verification>

<success_criteria>
- SIMUL_MODE 5/5 UAT PASS (또는 FAIL carry-over 명시)
- HW-01/HW-02 SIMUL 기준 충족, 실 HW grab 잔여 격리
</success_criteria>

<output>
After completion, create `.planning/phases/41-cxp-mil-lite-10-0-grab-hw-01-hw-02/41-04-SUMMARY.md`
</output>
