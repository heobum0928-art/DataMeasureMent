---
phase: 41.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Export/RepeatExcelExportService.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements:
  - OUT-03
  - OUT-04

must_haves:
  truths:
    - "시트1 '반복도 통계': 상단 메타 + Shot/FAI/측정명/N/Mean/StdDev/Range/Cpk/Nominal/Tol+/Tol-/OK/NG/DETECT_FAIL 컬럼 포함"
    - "시트2 '알고리즘 통계': AlgorithmCategory/TypeName/N측정수/성공률/Mean/StdDev 컬럼 포함"
    - "TLI/CTH/VTH/EdgeToLine/ArcEdge/Compound/DualImage/Other 카테고리 매핑 동작"
    - "생성된 xlsx 가 Excel 에서 정상 열림"
  artifacts:
    - path: "WPF_Example/Custom/Export/RepeatExcelExportService.cs"
      provides: "반복도 + 알고리즘 통계 xlsx 2시트 export"
      exports: ["RepeatExcelExportService.Export"]
  key_links:
    - from: "RepeatExcelExportService.Export"
      to: "RepeatMeasurementStats.ComputeAll"
      via: "MeasurementStat 목록 소비하여 시트1 행 작성"
      pattern: "stats\\.ComputeAll"
    - from: "RepeatExcelExportService.Export"
      to: "XLWorkbook (ClosedXML)"
      via: "ExcelExportService 패턴 그대로 답습"
      pattern: "new XLWorkbook"
---

<objective>
OUT-03 반복도 통계 시트 + OUT-04 알고리즘 통계 시트를 하나의 xlsx 파일로 export 하는 RepeatExcelExportService 를 구현한다.

Purpose: Phase 40 ExcelExportService 의 ClosedXML 패턴을 재사용하여 코드 복제 최소화. 2-시트 구조로 OUT-03/OUT-04 동시 충족.
Output: RepeatExcelExportService.cs (신규) + csproj Compile 등록
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/41.1-export-ii-50cycle-algorithm-stats/

<interfaces>
<!-- 재사용할 Phase 40 패턴 -->

From WPF_Example/Custom/Export/ExcelExportService.cs:
```csharp
// ClosedXML 패턴: XLWorkbook → ws.Cell(row, col).Value = ... → wb.SaveAs(path)
using ClosedXML.Excel;
using (var wb = new XLWorkbook())
{
    var ws = wb.Worksheets.Add("Result");
    ws.Cell(1, 1).Value = "모델명";
    // ...
    ws.Columns().AdjustToContents();
    wb.SaveAs(outputPath);
}
```

From WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs (Plan 01 생성):
```csharp
// MeasurementStat 필드
public class MeasurementStat {
    public string ShotName;  public string FAIName;  public string MeasurementName;
    public string TypeName;
    public int N;
    public double Mean, StdDev, Range, Cpk;
    public double NominalValue, TolerancePlus, ToleranceMinus;
    public int OkCount, NgCount, DetectFailCount;
}

public class RepeatMeasurementStats {
    public void AddSample(CycleResultDto dto);
    public Dictionary<string, MeasurementStat> ComputeAll();
}
```

<!-- D-05 알고리즘 카테고리 매핑 (CONTEXT.md 설계 결정 lock) -->
TypeName → AlgorithmCategory 매핑:
- "TwoLineIntersect", "PointToPoint", "LineToLineAngle", "LineToLineDistance", "PointToLineDistance" → "TLI"
- "CircleTwoHorizontal", "CircleDiameterMeasurement" (CircleDiameter 포함) → "CTH"
- "VerticalTwoHorizontal" → "VTH"
- "EdgeToLineDistance", "EdgeToLineAngle" → "EdgeToLine"
- "ArcEdgeDistance", "ArcLineIntersectDistance", "CircleCenterDistance" → "ArcEdge"
- "CompoundAngle", "CompoundCenterCDistance", "CompoundCenterBDistance", "CompoundShortAxisDistance" → "Compound"
- "DualImageEdgeDistanceMeasurement" → "DualImage"
- 그 외 → "Other"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RepeatExcelExportService — 2시트 xlsx export 신규 작성</name>
  <files>WPF_Example/Custom/Export/RepeatExcelExportService.cs</files>
  <read_first>
    - WPF_Example/Custom/Export/ExcelExportService.cs (ClosedXML XLWorkbook 패턴, namespace ReringProject.Export, using 목록)
    - WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs (MeasurementStat 필드 확인)
  </read_first>
  <action>
신규 파일 `WPF_Example/Custom/Export/RepeatExcelExportService.cs` 생성.

namespace `ReringProject.Export` (ExcelExportService 와 동일).

```csharp
//260612 hbk Phase 41.1 OUT-03/OUT-04 반복도+알고리즘 통계 xlsx export
public static class RepeatExcelExportService
{
    /// <summary>
    /// 수집된 CycleResultDto 목록으로 2-시트 xlsx 를 생성한다.
    /// 시트1 "반복도 통계" (OUT-03): MeasurementStat 1행 1측정 테이블.
    /// 시트2 "알고리즘 통계" (OUT-04): TypeName → 카테고리 집계.
    /// 실패 시 false + Logging (T-40-12 패턴 동일).
    /// </summary>
    public static bool Export(
        List<CycleResultDto> cycles,
        string recipeName,
        string outputPath)
}
```

Export 구현:
1. null/빈값 guard: cycles==null || cycles.Count==0 || string.IsNullOrEmpty(outputPath) → return false

2. RepeatMeasurementStats stats = new RepeatMeasurementStats();
   foreach (var c in cycles) stats.AddSample(c);
   var statDict = stats.ComputeAll();

3. using (var wb = new XLWorkbook())
   {
       // === 시트1: 반복도 통계 ===
       var ws1 = wb.Worksheets.Add("반복도 통계");

       // 메타 헤더 (행 1~4)
       ws1.Cell(1, 1).Value = "모델명";
       ws1.Cell(1, 2).Value = recipeName ?? "";
       ws1.Cell(2, 1).Value = "측정일시";
       ws1.Cell(2, 2).Value = DateTime.Now.ToString("yyyy-MM-dd HH:mm:ss");
       ws1.Cell(3, 1).Value = "반복횟수";
       ws1.Cell(3, 2).Value = cycles.Count;

       // 테이블 헤더 (행 5)
       string[] h1 = { "Shot", "FAI", "측정명", "N", "Mean", "StdDev", "Range", "Cpk",
                        "Nominal", "Tol+", "Tol-", "OK수", "NG수", "DETECT_FAIL수" };
       for (int i = 0; i < h1.Length; i++) ws1.Cell(5, i + 1).Value = h1[i];

       // 데이터 행 (행 6~)
       int r = 6;
       foreach (var kv in statDict)  // statDict = Dictionary<string, MeasurementStat>
       {
           var s = kv.Value;
           ws1.Cell(r, 1).Value = s.ShotName ?? "";
           ws1.Cell(r, 2).Value = s.FAIName ?? "";
           ws1.Cell(r, 3).Value = s.MeasurementName ?? "";
           ws1.Cell(r, 4).Value = s.N;
           ws1.Cell(r, 5).Value = Math.Round(s.Mean, 6);
           ws1.Cell(r, 6).Value = Math.Round(s.StdDev, 6);
           ws1.Cell(r, 7).Value = Math.Round(s.Range, 6);
           ws1.Cell(r, 8).Value = double.IsInfinity(s.Cpk) ? 999.0 : Math.Round(s.Cpk, 4);
           ws1.Cell(r, 9).Value = s.NominalValue;
           ws1.Cell(r, 10).Value = s.TolerancePlus;
           ws1.Cell(r, 11).Value = s.ToleranceMinus;
           ws1.Cell(r, 12).Value = s.OkCount;
           ws1.Cell(r, 13).Value = s.NgCount;
           ws1.Cell(r, 14).Value = s.DetectFailCount;
           r++;
       }
       ws1.Columns().AdjustToContents();

       // === 시트2: 알고리즘 통계 ===
       var ws2 = wb.Worksheets.Add("알고리즘 통계");

       // 알고리즘 카테고리별 집계: TypeName → AlgorithmCategory 매핑 후 그룹화
       // 유효 측정(LastHasResult=true)만 집계, DETECT_FAIL/미측정 제외
       // 집계 구조: Dictionary<string, AlgoGroupData> (key = AlgoCat + "|" + TypeName)
       // AlgoGroupData: Category, TypeName, List<double> values (LastMeasuredValue), int totalN
       // 성공률 = (유효 측정수 / 전체 시도수) * 100

       // cycles 전체를 다시 순회하여 TypeName 별 집계
       var algoMap = new Dictionary<string, AlgoAggData>();
       foreach (var cycle in cycles)
       {
           if (cycle.Shots == null) continue;
           foreach (var shot in cycle.Shots)
           {
               if (shot.FAIs == null) continue;
               foreach (var fai in shot.FAIs)
               {
                   if (fai.Measurements == null) continue;
                   foreach (var m in fai.Measurements)
                   {
                       string typeName = m.TypeName ?? "Other";
                       string cat = MapAlgorithmCategory(typeName);
                       string algoKey = cat + "|" + typeName;
                       AlgoAggData agg;
                       if (!algoMap.TryGetValue(algoKey, out agg))
                       {
                           agg = new AlgoAggData { Category = cat, TypeName = typeName };
                           algoMap[algoKey] = agg;
                       }
                       agg.TotalCount++;
                       if (m.LastHasResult)
                       {
                           agg.Values.Add(m.LastMeasuredValue);
                       }
                   }
               }
           }
       }

       // 시트2 헤더 (행 1)
       string[] h2 = { "알고리즘 분류", "TypeName", "N(측정수)", "성공률(%)", "Mean", "StdDev" };
       for (int i = 0; i < h2.Length; i++) ws2.Cell(1, i + 1).Value = h2[i];

       // 시트2 데이터 (행 2~)
       int r2 = 2;
       foreach (var kv2 in algoMap)
       {
           var a = kv2.Value;
           int validN = a.Values.Count;
           double successRate = a.TotalCount > 0 ? (validN * 100.0 / a.TotalCount) : 0.0;
           double mean2 = validN > 0 ? a.Values.Sum() / validN : 0.0;
           double stddev2 = 0.0;
           if (validN > 1)
           {
               double sumSq = 0;
               foreach (var v in a.Values) sumSq += (v - mean2) * (v - mean2);
               stddev2 = Math.Sqrt(sumSq / (validN - 1));
           }
           ws2.Cell(r2, 1).Value = a.Category;
           ws2.Cell(r2, 2).Value = a.TypeName;
           ws2.Cell(r2, 3).Value = validN;
           ws2.Cell(r2, 4).Value = Math.Round(successRate, 1);
           ws2.Cell(r2, 5).Value = Math.Round(mean2, 6);
           ws2.Cell(r2, 6).Value = Math.Round(stddev2, 6);
           r2++;
       }
       ws2.Columns().AdjustToContents();

       wb.SaveAs(outputPath);
   }
   return true;

내부 헬퍼 클래스(private, 파일 내부):
```csharp
private class AlgoAggData
{
    public string Category;
    public string TypeName;
    public int TotalCount;
    public List<double> Values = new List<double>();
}
```

MapAlgorithmCategory(string typeName) private static 메서드:
- switch/if-else 체인으로 D-05 매핑 구현
- "TwoLineIntersect", "PointToPoint", "LineToLineAngle", "LineToLineDistance", "PointToLineDistance" → "TLI"
- "CircleTwoHorizontal", "CircleDiameterMeasurement" → "CTH"
- "VerticalTwoHorizontal" → "VTH"
- "EdgeToLineDistance", "EdgeToLineAngle" → "EdgeToLine"
- "ArcEdgeDistance", "ArcLineIntersectDistance", "CircleCenterDistance" → "ArcEdge"
- "CompoundAngle", "CompoundCenterCDistance", "CompoundCenterBDistance", "CompoundShortAxisDistance" → "Compound"
- "DualImageEdgeDistanceMeasurement" → "DualImage"
- 기본값 → "Other"

using 목록 (파일 상단):
- using ClosedXML.Excel;
- using ReringProject.Sequence;
- using ReringProject.UI;
- using ReringProject.Utility;
- using System;
- using System.Collections.Generic;
- using System.Linq;

전체 try/catch → false + Logging.PrintErrLog (ExcelExportService 패턴 동일).
주석 마커: `//260612 hbk Phase 41.1`
Allman brace style.
  </action>
  <verify>
    <automated>grep -n "반복도 통계\|알고리즘 통계\|MapAlgorithmCategory\|AlgoAggData\|RepeatExcelExportService" "WPF_Example/Custom/Export/RepeatExcelExportService.cs"</automated>
  </verify>
  <done>
    - RepeatExcelExportService.cs 파일 생성
    - Export(List&lt;CycleResultDto&gt;, string, string) 메서드 존재
    - 시트명 "반복도 통계" 및 "알고리즘 통계" 문자열 존재
    - MapAlgorithmCategory private static 메서드 존재
    - "TLI", "CTH", "VTH", "EdgeToLine", "ArcEdge", "Compound", "DualImage" 카테고리 문자열 존재
    - ClosedXML XLWorkbook using 존재
    - //260612 hbk Phase 41.1 마커 존재
  </done>
</task>

<task type="auto">
  <name>Task 2: csproj Compile 등록 + msbuild 빌드 검증</name>
  <files>WPF_Example/DatumMeasurement.csproj</files>
  <read_first>
    - WPF_Example/DatumMeasurement.csproj (Custom\Export 섹션 위치, ExcelExportService.cs 항목 위치)
  </read_first>
  <action>
DatumMeasurement.csproj 에 신규 파일 Compile 등록.

추가 위치: `<Compile Include="Custom\Export\ExcelExportService.cs" />` 바로 다음 줄.

추가 내용:
```xml
<Compile Include="Custom\Export\RepeatExcelExportService.cs" />
```

삽입 후 msbuild Debug/x64 빌드:
```
msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal
```

에러 발생 시 수정 (Plan 01 Task 3 동일 패턴).
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal 2>&1 | Select-String "error|Error" | Select-Object -First 10</automated>
  </verify>
  <done>
    - csproj 에 RepeatExcelExportService.cs Compile 항목 존재
    - msbuild Debug/x64 0 errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Export → File System | outputPath 는 SaveFileDialog 경유 (UI 에서 사용자 선택) — path traversal 위험 낮음 |
| Export → ClosedXML | cycles 리스트 내 string 값 그대로 셀에 기록 — XSS 없음 (xlsx 포맷) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-04 | Denial of Service | Export(null cycles) | mitigate | 진입부 null/빈 guard → return false |
| T-41.1-05 | Denial of Service | double.IsInfinity(Cpk) → xlsx 기록 오류 | mitigate | IsInfinity 시 999.0 대체값 기록 |
</threat_model>

<verification>
- RepeatExcelExportService.cs: 2시트 이름 + MapAlgorithmCategory + XLWorkbook grep 확인
- csproj: RepeatExcelExportService.cs Compile 항목 grep 확인
- msbuild Debug/x64: 0 errors
</verification>

<success_criteria>
- RepeatExcelExportService.cs 신규 1파일 생성
- csproj Compile 등록 1건
- msbuild Debug/x64 PASS (0 errors)
- 시트1 "반복도 통계": 14컬럼 헤더 포함
- 시트2 "알고리즘 통계": 8카테고리 매핑 포함
- //260612 hbk Phase 41.1 마커 존재
</success_criteria>

<output>
완료 후 `.planning/phases/41.1-export-ii-50cycle-algorithm-stats/41.1-02-SUMMARY.md` 생성.
</output>
