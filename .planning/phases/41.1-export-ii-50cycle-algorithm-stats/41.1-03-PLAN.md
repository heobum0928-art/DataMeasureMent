---
phase: 41.1
plan: 03
type: execute
wave: 2
depends_on:
  - "41.1-01"
  - "41.1-02"
files_modified:
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs
autonomous: false
requirements:
  - OUT-03
  - OUT-04

must_haves:
  truths:
    - "ReviewerWindow 좌측 패널 하단에 '50회 반복 실행' 버튼이 존재한다"
    - "버튼 클릭 시 진행 표시 레이블이 '진행 중: N/50' 형태로 갱신된다"
    - "50회 완료 후 '반복도 엑셀 export' 버튼이 활성화된다"
    - "반복도 엑셀 export 버튼 클릭 시 SaveFileDialog → RepeatExcelExportService.Export 호출"
  artifacts:
    - path: "WPF_Example/UI/Reviewer/ReviewerWindow.xaml"
      provides: "50회 반복 실행 버튼 + 진행 레이블 + 반복도 export 버튼 XAML"
    - path: "WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs"
      provides: "RepeatRunService 인스턴스 소유 + 버튼 핸들러"
  key_links:
    - from: "Button_RepeatRun_Click"
      to: "RepeatRunService.Start(seq, 50)"
      via: "SequenceHandler.GetSequence 로 활성 시퀀스 획득"
      pattern: "_repeatService\\.Start"
    - from: "RepeatRunService.OnRepeatComplete"
      to: "btn_repeatExport.IsEnabled = true"
      via: "Dispatcher.Invoke 로 UI 스레드 전환"
      pattern: "btn_repeatExport\\.IsEnabled"
---

<objective>
ReviewerWindow 에 50회 반복 실행 버튼과 진행 표시, 완료 후 반복도 엑셀 export 버튼을 추가하여 D-06 UI 진입점을 구현한다.

Purpose: OUT-03/OUT-04 의 사용자 트리거 진입점. RepeatRunService(Plan 01)와 RepeatExcelExportService(Plan 02)를 UI 에 배선.
Output: ReviewerWindow.xaml + ReviewerWindow.xaml.cs 수정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/41.1-export-ii-50cycle-algorithm-stats/

<interfaces>
<!-- Plan 01/02 에서 생성된 인터페이스 -->

From RepeatRunService.cs:
```csharp
public class RepeatRunService
{
    public const int DEFAULT_REPEAT_COUNT = 50;
    public event Action<List<CycleResultDto>> OnRepeatComplete;
    public event Action<int, int> OnProgressChanged;  // (현재, 목표)
    public bool IsRunning { get; }
    public void Start(InspectionSequence seq, int targetCount = DEFAULT_REPEAT_COUNT);
    public void Stop();
}
```

From RepeatExcelExportService.cs:
```csharp
public static bool Export(List<CycleResultDto> cycles, string recipeName, string outputPath);
```

From SequenceHandler (기존 코드):
```csharp
// SequenceHandler 에서 시퀀스 이름으로 InspectionSequence 획득
// SystemHandler.Handle.Sequences 는 SequenceHandler
// SequenceHandler.GetSequence(string name) 또는 Sequences["TOP"] 인덱서
// → Custom/SequenceHandler.cs 에서 SEQ_TOP/SEQ_SIDE/SEQ_BOTTOM 상수 확인 필요
```

From ReviewerWindow.xaml 기존 좌측 패널:
```xml
<!-- 좌측 패널 헤더: Row0, Col0 -->
<Border Grid.Row="0" Grid.Column="0" ...>
    <StackPanel Orientation="Vertical">
        <Button x:Name="btn_loadFolder" Content="날짜 폴더 열기" .../>
        <Button x:Name="btn_exportExcel" Content="엑셀 export" IsEnabled="False" .../>
    </StackPanel>
</Border>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReviewerWindow.xaml — 50회 반복 실행 UI 요소 추가</name>
  <files>WPF_Example/UI/Reviewer/ReviewerWindow.xaml</files>
  <read_first>
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml (좌측 패널 StackPanel 전체 구조, 기존 버튼 위치)
  </read_first>
  <action>
ReviewerWindow.xaml 의 좌측 패널 StackPanel (`<Compile Include="UI\Reviewer\...">` 에 해당하는 Border Grid.Row="0" Grid.Column="0" 내부 StackPanel) 에 기존 `btn_exportExcel` 버튼 아래에 다음 요소를 추가한다.

추가 내용 (btn_exportExcel Button 태그 닫힘 직후):
```xml
<!-- 260612 hbk Phase 41.1 OUT-03/OUT-04 반복도 실행 UI -->
<Separator Margin="0,6,0,6"/>
<Button x:Name="btn_repeatRun" Content="50회 반복 실행"
        Click="Button_RepeatRun_Click"
        Padding="8,4" HorizontalAlignment="Stretch"/>
<TextBlock x:Name="lbl_repeatProgress" Text=""
           FontSize="11" Foreground="#334155"
           Margin="0,4,0,0" HorizontalAlignment="Center"/>
<Button x:Name="btn_repeatExport" Content="반복도 엑셀 export"
        Click="Button_RepeatExport_Click"
        IsEnabled="False"
        Padding="8,4" Margin="0,4,0,0" HorizontalAlignment="Stretch"/>
```

삽입 위치: `</StackPanel>` 닫는 태그 바로 위.

기존 요소(btn_loadFolder, btn_exportExcel) 는 변경하지 않는다.
  </action>
  <verify>
    <automated>grep -n "btn_repeatRun\|btn_repeatExport\|lbl_repeatProgress\|Button_RepeatRun_Click\|Button_RepeatExport_Click" "WPF_Example/UI/Reviewer/ReviewerWindow.xaml"</automated>
  </verify>
  <done>
    - btn_repeatRun 버튼 존재 (Content="50회 반복 실행")
    - lbl_repeatProgress TextBlock 존재
    - btn_repeatExport 버튼 존재 (IsEnabled="False")
    - 기존 btn_loadFolder, btn_exportExcel 변경 없음
  </done>
</task>

<task type="auto">
  <name>Task 2: ReviewerWindow.xaml.cs — RepeatRunService 배선 + 핸들러 구현</name>
  <files>WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs (전체 — _currentCycle 필드, Button_ExportExcel_Click 패턴, using 목록)
    - WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs (Plan 01 생성, Start/Stop/이벤트 시그니처)
    - WPF_Example/Custom/SequenceHandler.cs (GetSequence 메서드 또는 시퀀스 이름 상수 확인)
  </read_first>
  <action>
ReviewerWindow.xaml.cs 에 다음 변경을 적용한다.

**1. using 추가** (파일 상단 기존 using 목록 끝에 추가, 중복 없으면):
```csharp
using ReringProject.Sequence;
```
(이미 존재하면 추가 불필요)

**2. 필드 추가** (기존 `private CycleResultDto _currentCycle;` 아래):
```csharp
//260612 hbk Phase 41.1 OUT-03 반복도 실행 서비스 (UI 레이어 소유 인스턴스)
private RepeatRunService _repeatService;
private List<CycleResultDto> _repeatCycles;
```

**3. Button_RepeatRun_Click 핸들러 추가** (파일 끝 `}` 내부, Button_ExportExcel_Click 다음에 추가):
```csharp
//260612 hbk Phase 41.1 OUT-03 50회 반복 실행 버튼 핸들러
private void Button_RepeatRun_Click(object sender, RoutedEventArgs e)
{
    if (_repeatService != null && _repeatService.IsRunning)
    {
        _repeatService.Stop();
        lbl_repeatProgress.Text = "중단됨";
        btn_repeatRun.Content = "50회 반복 실행";
        return;
    }

    // 활성 InspectionSequence 획득 — SequenceHandler 에서 이름으로 탐색
    // Custom/SequenceHandler.cs 의 SEQ_TOP 등 상수 이름 확인 후 일치시킬 것
    // 없으면 첫 번째 InspectionSequence 를 반환
    InspectionSequence activeSeq = null;
    var seqHandler = SystemHandler.Handle.Sequences;
    if (seqHandler != null)
    {
        // GetSequence(string) 시그니처 확인 후 사용, 없으면 직접 탐색
        var seqs = seqHandler.GetAllSequences();   // 또는 seqHandler.Sequences
        if (seqs != null)
        {
            foreach (var s in seqs)
            {
                var inspSeq = s as InspectionSequence;
                if (inspSeq != null)
                {
                    activeSeq = inspSeq;
                    break;
                }
            }
        }
    }

    if (activeSeq == null)
    {
        CustomMessageBox.Show("반복 실행", "활성 검사 시퀀스를 찾을 수 없습니다.", MessageBoxImage.Warning);
        return;
    }

    _repeatCycles = null;
    btn_repeatExport.IsEnabled = false;
    btn_repeatRun.Content = "중단";
    lbl_repeatProgress.Text = "진행 중: 0/" + RepeatRunService.DEFAULT_REPEAT_COUNT;

    _repeatService = new RepeatRunService();
    _repeatService.OnProgressChanged += (current, total) =>
    {
        Dispatcher.Invoke(() =>
        {
            lbl_repeatProgress.Text = "진행 중: " + current + "/" + total;
        });
    };
    _repeatService.OnRepeatComplete += (cycles) =>
    {
        Dispatcher.Invoke(() =>
        {
            _repeatCycles = cycles;
            lbl_repeatProgress.Text = "완료: " + (cycles != null ? cycles.Count : 0) + "회";
            btn_repeatRun.Content = "50회 반복 실행";
            btn_repeatExport.IsEnabled = (cycles != null && cycles.Count > 0);
        });
    };

    _repeatService.Start(activeSeq, RepeatRunService.DEFAULT_REPEAT_COUNT);
}
```

**4. Button_RepeatExport_Click 핸들러 추가** (Button_RepeatRun_Click 다음에):
```csharp
//260612 hbk Phase 41.1 OUT-03/OUT-04 반복도 엑셀 export 핸들러
private void Button_RepeatExport_Click(object sender, RoutedEventArgs e)
{
    if (_repeatCycles == null || _repeatCycles.Count == 0)
    {
        CustomMessageBox.Show("반복도 export", "반복 실행 완료 후 사용하세요.", MessageBoxImage.Warning);
        return;
    }

    string initialDir = SystemHandler.Handle.Setting.ResultSavePath;
    string recipeName = SystemHandler.Handle.Setting.CurrentRecipeName ?? "";

    var dlg = new Microsoft.Win32.SaveFileDialog
    {
        Filter = "Excel 파일 (*.xlsx)|*.xlsx",
        FileName = "repeat_" + DateTime.Now.ToString("yyyyMMdd_HHmmss") + ".xlsx",
        InitialDirectory = initialDir
    };

    if (dlg.ShowDialog() == true)
    {
        bool ok = ReringProject.Export.RepeatExcelExportService.Export(
            _repeatCycles, recipeName, dlg.FileName);
        string msg;
        if (ok) msg = "저장 완료:\n" + dlg.FileName; else msg = "export 실패 (로그 확인)";
        MessageBoxImage icon;
        if (ok) icon = MessageBoxImage.Information; else icon = MessageBoxImage.Error;
        CustomMessageBox.Show("반복도 엑셀 export", msg, icon);
    }
}
```

**주의**: seqHandler.GetAllSequences() 또는 seqHandler.Sequences 는 Custom/SequenceHandler.cs 실제 API 에 맞춰 조정한다. 파일을 먼저 읽어 실제 존재하는 메서드/프로퍼티 이름을 확인할 것 (Sequences 인덱서, GetSequence(string) 등). 빌드 에러 CS1061 발생 시 즉시 수정.

파일 스타일: ReviewerWindow.xaml.cs 기존 스타일 확인 후 일치 (K&R 또는 Allman).
  </action>
  <verify>
    <automated>grep -n "Button_RepeatRun_Click\|Button_RepeatExport_Click\|_repeatService\|_repeatCycles\|RepeatRunService\|RepeatExcelExportService" "WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs"</automated>
  </verify>
  <done>
    - _repeatService, _repeatCycles 필드 존재
    - Button_RepeatRun_Click 핸들러 존재
    - Button_RepeatExport_Click 핸들러 존재
    - RepeatRunService.Start 호출 존재
    - RepeatExcelExportService.Export 호출 존재
    - OnProgressChanged/OnRepeatComplete 이벤트 구독 + Dispatcher.Invoke 존재
    - //260612 hbk Phase 41.1 마커 존재
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Plan 01: RepeatMeasurementStats.cs + RepeatRunService.cs 신규
    - Plan 02: RepeatExcelExportService.cs 신규
    - Plan 03: ReviewerWindow 버튼 3종 추가 + 핸들러 배선
    - msbuild Debug/x64 PASS (Plan 01/02 각 검증 완료)
  </what-built>
  <how-to-verify>
1. msbuild Debug/x64 Rebuild 최종 확인:
   `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild /v:minimal`
   → 0 errors 확인

2. 앱 실행 (SIMUL_MODE):
   - 메인 화면에서 레시피 로드 → 1회 검사 실행 (측정값 정상 출력 확인)
   - [결과 리뷰어] 열기 → 좌측 패널 하단에 "50회 반복 실행" 버튼 + 진행 레이블 + "반복도 엑셀 export" 버튼(비활성) 확인

3. 반복 실행 테스트 (SIMUL):
   - "50회 반복 실행" 클릭 → 진행 레이블이 "진행 중: 1/50", "진행 중: 2/50" ... 형태로 갱신 확인
   - 50회 완료 후 "완료: 50회" 표시 + "반복도 엑셀 export" 버튼 활성화 확인

4. 엑셀 export 테스트:
   - "반복도 엑셀 export" 클릭 → SaveFileDialog → 저장 → xlsx 파일 Excel 에서 열기
   - 시트1 "반복도 통계": Shot/FAI/측정명/N/Mean/StdDev/Range/Cpk 컬럼 + 데이터 행 확인
   - 시트2 "알고리즘 통계": AlgorithmCategory/TypeName/N/성공률(%) 컬럼 + 데이터 행 확인
  </how-to-verify>
  <resume-signal>UAT 결과를 "approved" 또는 발견한 이슈 설명으로 응답하세요.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI 스레드 → RepeatRunService | Start/Stop 은 UI 스레드, OnFinish 핸들러는 시퀀스 스레드 |
| Dispatcher.Invoke | OnProgressChanged/OnRepeatComplete 는 Dispatcher.Invoke 로 UI 스레드 전환 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41.1-06 | Denial of Service | GetAllSequences null 반환 | mitigate | null 체크 후 MessageBox (activeSeq == null 분기) |
| T-41.1-07 | Denial of Service | 반복 실행 중 btn_repeatRun 재클릭 | mitigate | IsRunning=true 이면 Stop() 전환 (토글 UX) |
</threat_model>

<verification>
- XAML: btn_repeatRun, lbl_repeatProgress, btn_repeatExport 3종 grep 확인
- code-behind: RepeatRunService.Start + RepeatExcelExportService.Export 호출 grep 확인
- msbuild Debug/x64 Rebuild: 0 errors
- 체크포인트: 사용자 SIMUL UAT — 50회 반복 + 2시트 xlsx 열기 확인
</verification>

<success_criteria>
- XAML 3종 요소 추가 (btn_repeatRun, lbl_repeatProgress, btn_repeatExport)
- 핸들러 2종 구현 (RepeatRun_Click, RepeatExport_Click)
- msbuild Debug/x64 Rebuild PASS
- SIMUL UAT: 50회 반복 완료 후 xlsx 생성, Excel 에서 2시트 정상 열림
- OUT-03 (반복도 통계) + OUT-04 (알고리즘 통계) 동시 충족
</success_criteria>

<output>
완료 후 `.planning/phases/41.1-export-ii-50cycle-algorithm-stats/41.1-03-SUMMARY.md` 생성.
</output>
