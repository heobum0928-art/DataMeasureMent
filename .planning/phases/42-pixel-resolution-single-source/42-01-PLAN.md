---
phase: 42-pixel-resolution-single-source
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgePairDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
autonomous: true
requirements: []   # phase_req_ids is null — must_haves derived from ROADMAP success criteria + locked CONTEXT decisions (D-01~D-07)

must_haves:
  truths:
    - "측정 경로가 Shot 단일 소스(ShotParam.PixelResolution)에서 픽셀분해능을 읽는다 — 항목별 fai.PixelResolutionX 소비 0"
    - "EdgePair 측정도 넘어온 pixelResolution 파라미터(=shot 단일소스)를 소비한다 — self.PixelResolutionX/Y 소비 0"
    - "Shot 단일값을 편집하면 다음 검사부터 별도 cascade 없이 전체 FAI에 반영된다 (구조적 — 검사 시점에 shot 값 직접 읽음)"
    - "PropertyGrid에서 항목별(FAI/EdgePair) PixelResolutionX/Y가 노출되지 않는다"
    - "ShotConfig.PixelResolution은 유일한 편집 소스로 PropertyGrid 노출이 유지된다 (변경 없음)"
    - "정규화된 레시피의 측정 mm 값이 Rewire 전후 동일하다 (회귀 0)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "EStep.Measure 루프가 ShotParam.PixelResolution을 1회 캡처하여 두 호출부(269,284)에 전달"
      contains: "ShotParam.PixelResolution"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/EdgePairDistanceMeasurement.cs"
      provides: "temp FAIConfig 구성 시 pixelResolution 파라미터(shot 단일소스) 소비 + 항목 필드 Browsable(false)"
      contains: "[PropertyTools.DataAnnotations.Browsable(false)]"
    - path: "WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs"
      provides: "PixelResolutionX/Y에 Browsable(false) 부여, 필드 자체는 INI 호환 위해 유지"
      contains: "[PropertyTools.DataAnnotations.Browsable(false)]"
  key_links:
    - from: "Action_FAIMeasurement.cs EStep.Measure"
      to: "ShotConfig.PixelResolution"
      via: "double pixRes = ShotParam.PixelResolution 캡처 후 meas.TryExecute 인자 전달"
      pattern: "TryExecute\\(image, transform, pixRes"
    - from: "EdgePairDistanceMeasurement.cs temp FAIConfig"
      to: "pixelResolution 파라미터 (= shot 단일소스)"
      via: "ownerShot.PixelResolution 또는 pixelResolution 파라미터 대입"
      pattern: "PixelResolutionX = resolvedPixelRes"
---

<objective>
산재된 픽셀분해능(mm/pixel) 소비 경로를 카메라(Shot) 단일 소스로 통합한다. 측정 코드가 항목별(FAIConfig / EdgePairDistanceMeasurement) PixelResolutionX/Y 대신 ShotConfig.PixelResolution을 읽도록 소비 지점만 Rewire(D-01, D-06)하고, 소비되지 않게 된 항목별 필드는 PropertyGrid에서 숨긴다(D-04, D-05). 필드 자체는 INI 하위 호환(D-07)을 위해 보존한다.

Purpose: ROADMAP 성공기준 3종 충족 — (1) Shot 단일값 편집 시 재시작 없이 전체 FAI 반영, (2) PropertyGrid 항목별 노출 정리, (3) 측정 경로 단일 소스. D-03 확정에 따라 별도 cascade/트리거 없이 "검사 시점에 shot 값 직접 읽기"만으로 라이브 반영이 구조적으로 충족된다.

Output: 3개 파일 수정 (측정 소비 Rewire 2곳 + PropertyGrid 숨김 4 필드). 신규 파일 0, INI 포맷 키 변경 0, 측정 알고리즘 로직 변경 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-pixel-resolution-single-source/42-CONTEXT.md
@.planning/phases/42-pixel-resolution-single-source/42-PATTERNS.md

<interfaces>
<!-- 단일 소스 기준값 — CameraSlaveParam.cs:22-25 (변경 없음, 참조용) -->
<!-- ShotConfig extends CameraSlaveParam → ShotParam.PixelResolution 이 모든 Rewire의 최종 단일소스 -->

From WPF_Example/Sequence/Param/CameraSlaveParam.cs:24-25:
```csharp
[System.ComponentModel.Description("mm/pixel calibration factor for this camera")]
public double PixelResolution { get; set; } = 1.0;  //260408 hbk mm/pixel (per D-12)
```

From WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs:42:
```csharp
public ShotConfig ShotParam => Param as ShotConfig;   // Action 이 직접 보유 — Owner chain walk 불필요
```

From WPF_Example/Custom/Sequence/Inspection/Measurements/EdgePairDistanceMeasurement.cs (TryExecute signature):
```csharp
public override bool TryExecute(HImage image, HTuple datumTransform, double pixelResolution,
    out double resultValue, out string error, out List<EdgeInspectionOverlay> overlays)
// Owner chain: var ownerFai = Owner as FAIConfig;  →  ownerFai.Owner as ShotConfig
```

<!-- 숨김 패턴 선례 (정적 무조건 hide): DatumConfig.cs:263-265, EdgePairDistanceMeasurement.cs:33-36 -->
<!-- ICustomTypeDescriptor(IsHiddenForAlgorithm)는 조건부 hide 전용 — 여기선 사용하지 말 것 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 측정 소비 경로 Rewire — Action_FAIMeasurement + EdgePairDistanceMeasurement (D-01, D-06)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/Measurements/EdgePairDistanceMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (수정 대상; ShotParam 프로퍼티 line 42, EStep.Measure case line 177, foreach line 195, 측정 호출부 line 269 + 284)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgePairDistanceMeasurement.cs (수정 대상; Owner chain analog line 63, temp FAIConfig 구성 line 72-89)
    - .planning/phases/42-pixel-resolution-single-source/42-PATTERNS.md §1(D-01), §4(D-06) — 정확한 변경 코드 블록
  </read_first>
  <action>
A) Action_FAIMeasurement.cs (K&R 브레이스 스타일 — 이 파일 스타일 유지):
   1. `case EStep.Measure:` 블록(line 177) 내부, `foreach (var fai in ShotParam.FAIList)` (line 195) **진입 직전**에 픽셀분해능을 1회 캡처하는 지역 변수를 추가한다:
      ```csharp
      double pixRes = ShotParam != null ? ShotParam.PixelResolution : 1.0; //260615 hbk Phase 42 D-01 Shot 단일소스
      ```
      (ShotParam null 가드는 이 파일의 기존 `if (ShotParam != null)` 패턴과 일관 — Conditional 연산자 1줄 채택. line 195 foreach 는 이미 `if (ShotParam != null)` 블록 내부이므로 진입 직전 위치가 안전.)
   2. line 269 의 `meas.TryExecute(image, transform, fai.PixelResolutionX, ...)` 에서 `fai.PixelResolutionX` 를 `pixRes` 로 교체하고 줄 끝에 `//260615 hbk Phase 42 D-01` 주석을 추가한다 (DualImage 경로).
   3. line 284 의 `meas.TryExecute(image, transform, fai.PixelResolutionX, ...)` 에서 동일하게 `fai.PixelResolutionX` 를 `pixRes` 로 교체하고 `//260615 hbk Phase 42 D-01` 주석을 추가한다 (1-image 경로).
   - `fai` 루프 변수 자체는 그대로 둔다 (다른 용도로 계속 사용됨). `fai.PixelResolutionX` 참조는 이 두 호출부에서만 제거된다.

B) EdgePairDistanceMeasurement.cs (Allman 브레이스 스타일 — 이 파일 스타일 유지):
   1. line 70-89 의 temp FAIConfig 구성 직전에 Owner 체인으로 shot 단일소스를 해석하는 코드를 추가한다 (analog: line 63 `var ownerFai = Owner as FAIConfig;`):
      ```csharp
      //260615 hbk Phase 42 D-06 — PixelResolution 은 shot 단일소스(파라미터 경유). self 필드 소비 제거
      var ownerShot = ownerFai.Owner as ShotConfig;
      double resolvedPixelRes = (ownerShot != null) ? ownerShot.PixelResolution : pixelResolution;
      ```
      (`ownerShot != null` 분기는 방어 코드 — D-01 Rewire 후 넘어오는 `pixelResolution` 파라미터도 동일하게 shot 단일소스를 가리키므로 두 경로 값이 일치한다.)
   2. temp object initializer 내부 line 86 `PixelResolutionX = PixelResolutionX,` 를 `PixelResolutionX = resolvedPixelRes,` 로, line 87 `PixelResolutionY = PixelResolutionY,` 를 `PixelResolutionY = resolvedPixelRes,` 로 교체하고 각 줄 끝에 `//260615 hbk Phase 42 D-06` 주석을 추가한다 (X=Y 정방형 가정 D-09 — 동일 값).
   - 만약 `ShotConfig` 타입이 이 파일 using 범위에 없으면 fully-qualified `ReringProject.Sequence.ShotConfig` 또는 적절한 using 을 추가한다 (FAIConfig 가 동일 네임스페이스이므로 추가 불필요할 가능성이 높음 — 빌드 에러 시에만 조치).
  </action>
  <verify>
    <automated>MISSING — 정적 검증으로 대체: grep + msbuild. (1) Action_FAIMeasurement.cs 측정 호출 2곳에 fai.PixelResolutionX 가 더 이상 존재하지 않고 pixRes 가 전달됨; (2) EdgePairDistanceMeasurement.cs temp 구성에 resolvedPixelRes 사용; (3) msbuild Debug/x64 PASS 신규 warning 0</automated>
  </verify>
  <acceptance_criteria>
    - Grep `fai\.PixelResolutionX` in Action_FAIMeasurement.cs → 측정 호출부(EStep.Measure)에서 0 매치 (다른 위치의 잔존이 있다면 측정 호출부 라인만 제거되었는지 확인)
    - Grep `double pixRes = ShotParam` in Action_FAIMeasurement.cs → 1 매치, EStep.Measure foreach 진입 직전
    - Grep `TryExecute\(image, transform, pixRes` in Action_FAIMeasurement.cs → 2 매치 (line ~269, ~284)
    - Grep `resolvedPixelRes` in EdgePairDistanceMeasurement.cs → 3 매치 (선언 1 + temp 대입 2)
    - Grep `PixelResolutionX = PixelResolutionX` in EdgePairDistanceMeasurement.cs → 0 매치 (self-필드 직접 대입 제거됨)
    - Grep `//260615 hbk` in 두 파일 → 각 파일 ≥ 2 매치
    - msbuild Debug/x64 빌드 PASS, 신규 error 0 / 신규 warning 0 (Phase 41 baseline = 6 warnings)
  </acceptance_criteria>
  <done>측정 두 경로(Action 1/2-image + EdgePair) 모두 픽셀분해능을 ShotConfig.PixelResolution 단일소스에서 소비. fai/self 항목별 필드 소비 0. 빌드 PASS.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PropertyGrid 항목별 PixelResolution 숨김 — FAIConfig + EdgePairDistanceMeasurement (D-04, D-05)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs, WPF_Example/Custom/Sequence/Inspection/Measurements/EdgePairDistanceMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs (수정 대상; PixelResolutionX/Y line 78-80, [Category("Calibration")] 포함)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgePairDistanceMeasurement.cs (수정 대상; PixelResolutionX/Y line 38-40; 동일 파일 Browsable(false) 선례 line 33-36)
    - .planning/phases/42-pixel-resolution-single-source/42-PATTERNS.md §2(D-04), §3(D-05), "Shared Patterns: PropertyGrid 숨김" — 정적 [Browsable(false)] 가 올바른 선택임 확정
  </read_first>
  <action>
공통 규칙: 무조건 숨김이므로 **정적 어트리뷰트** `[PropertyTools.DataAnnotations.Browsable(false)]` 를 사용한다. ICustomTypeDescriptor / IsHiddenForAlgorithm 동적 필터는 사용하지 않는다(조건부 hide 전용). 필드 자체(getter/setter/타입/기본값 1.0)는 INI 하위 호환(D-07)을 위해 **삭제하지 않고 그대로 유지**한다. ParamBase.Save/Load 는 Reflection 경로라 Browsable 어트리뷰트와 무관하므로 INI 직렬화에 영향 없음.

A) FAIConfig.cs (line 76-80 — 이 파일 스타일 유지):
   - 기존 `[Category("Calibration")]` (line 78) 어트리뷰트를 제거하고, PixelResolutionX 와 PixelResolutionY 각 프로퍼티 바로 위에 `[PropertyTools.DataAnnotations.Browsable(false)]` 를 부여한다. 결과 형태:
     ```csharp
     // Calibration: INI 호환 잔존 저장용. 소비 없음 — Shot 단일소스(D-01). PropertyGrid 숨김. //260615 hbk Phase 42 D-04/D-05
     [PropertyTools.DataAnnotations.Browsable(false)]
     public double PixelResolutionX { get; set; } = 1.0;  // mm/pixel — INI 키 보존(D-07)
     [PropertyTools.DataAnnotations.Browsable(false)]
     public double PixelResolutionY { get; set; } = 1.0;  // mm/pixel — INI 키 보존(D-07)
     ```
   - 주의: FAIConfig 는 ICustomTypeDescriptor 구현 클래스이나, 무조건 숨김이므로 정적 Browsable 이 올바르다(IsHiddenForAlgorithm 분기 추가 금지). GetProperties 필터 로직은 변경하지 않는다.

B) EdgePairDistanceMeasurement.cs (line 38-40 — Allman 스타일 유지):
   - 기존 `[Category("EdgePair|Calibration")]` (line 38) 어트리뷰트를 제거하고, PixelResolutionX 와 PixelResolutionY 각 프로퍼티 바로 위에 `[PropertyTools.DataAnnotations.Browsable(false)]` 를 부여한다 (동일 파일 line 33-36 선례와 동일 패턴). 결과 형태:
     ```csharp
     // INI 호환 잔존 저장용 — D-06 재배선 후 TryExecute 에서 소비 안 함. //260615 hbk Phase 42 D-05
     [PropertyTools.DataAnnotations.Browsable(false)]
     public double PixelResolutionX { get; set; } = 1.0;
     [PropertyTools.DataAnnotations.Browsable(false)]
     public double PixelResolutionY { get; set; } = 1.0;
     ```
   - EdgePairDistanceMeasurement 는 ICustomTypeDescriptor 미구현이므로 정적 어트리뷰트가 유일한 경로다.

ShotConfig.PixelResolution(CameraSlaveParam.cs:25, `[Category("General|AOI")]` 노출)은 **변경하지 않는다** — 유일한 편집 소스로 노출 유지.
  </action>
  <verify>
    <automated>MISSING — 정적 검증으로 대체: grep + msbuild. (1) FAIConfig.cs PixelResolutionX/Y 위에 Browsable(false) 존재 + [Category("Calibration")] 제거됨; (2) EdgePairDistanceMeasurement.cs PixelResolutionX/Y 위에 Browsable(false) 존재 + [Category("EdgePair|Calibration")] 제거됨; (3) CameraSlaveParam.cs PixelResolution 무변경; (4) msbuild Debug/x64 PASS 신규 warning 0</automated>
  </verify>
  <acceptance_criteria>
    - Grep `[PropertyTools.DataAnnotations.Browsable(false)]` 직후 `public double PixelResolutionX` in FAIConfig.cs → 인접 매치 1
    - Grep `[PropertyTools.DataAnnotations.Browsable(false)]` 직후 `public double PixelResolutionY` in FAIConfig.cs → 인접 매치 1
    - Grep `[Category("Calibration")]` in FAIConfig.cs PixelResolution 인근 → 0 매치 (제거됨)
    - Grep `[PropertyTools.DataAnnotations.Browsable(false)]` 가 PixelResolutionX/Y 위에 in EdgePairDistanceMeasurement.cs → 각 1
    - Grep `[Category("EdgePair|Calibration")]` in EdgePairDistanceMeasurement.cs → 0 매치 (제거됨)
    - CameraSlaveParam.cs line 25 `public double PixelResolution` + `[Category` 노출 무변경 (git diff 에 CameraSlaveParam.cs 없음)
    - Grep `//260615 hbk` in 두 파일 → 각 ≥ 1 매치
    - msbuild Debug/x64 PASS 신규 error 0 / 신규 warning 0
  </acceptance_criteria>
  <done>FAIConfig + EdgePairDistanceMeasurement 의 항목별 PixelResolutionX/Y 4개가 PropertyGrid에서 숨겨짐. 필드/INI 직렬화는 유지. ShotConfig.PixelResolution 만 노출. 빌드 PASS.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 회귀 검증 — 정규화 레시피 측정값 불변 + 단일소스 정합 문서화</name>
  <files>(검증 전용 — 코드 변경 없음. SUMMARY에 결과 기록)</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs:325-334 (로딩 정규화 D-10 — fai = camRes 덮어쓰기 확인, 유지)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs:2187-2208 (ApplyCalibrationResult — D-02 편집 진입점, shot.PixelResolution 수렴 확인, 변경 불필요)
    - .planning/phases/42-pixel-resolution-single-source/42-CONTEXT.md <specifics> — "Rewire 전후 동일 레시피의 측정 mm 값 회귀 0" 검증 핵심
  </read_first>
  <action>
이 태스크는 코드를 변경하지 않는다. Task 1/2 완료 후 다음을 검증하고 SUMMARY에 근거를 기록한다:

1. **회귀 0 논증 (정규화 레시피):**
   - InspectionRecipeManager.cs:325-334 의 로딩 정규화(D-10)가 CAM 섹션 존재 시 `fai.PixelResolutionX/Y = camRes` 로 덮어쓰는지 코드로 확인한다. 즉 정상 레시피는 로딩 후 `fai.PixelResolutionX == shot.PixelResolution` 이 성립한다.
   - 따라서 Task 1 Rewire(`fai.PixelResolutionX` → `ShotParam.PixelResolution`)는 정규화된 레시피에서 **동일 값**을 전달한다 → 측정 mm 값 회귀 0. 이 등가성을 SUMMARY에 명시한다.
   - 만약 CAM 섹션이 없는 구형 레시피(fai 값 ≠ shot 값)에서는 값이 달라질 수 있으나, 이는 D-01 의 의도(shot 단일소스 수렴)이며 의도적 보정으로 SUMMARY에 문서화한다(Phase 38 성공기준 #3 연속선).

2. **편집 진입점 정합 확인 (D-02):**
   - MainView.xaml.cs:2187-2208 ApplyCalibrationResult 가 `shot.PixelResolution = mmPerPixel` 로 단일소스에 기록하는지 확인한다. Shot 노드 PropertyGrid 편집 + 2점 캘리브레이션 두 진입점이 모두 shot.PixelResolution 으로 수렴함을 확인 (코드 변경 불필요, 확인만).

3. **라이브 반영 구조 확인 (D-03):**
   - Task 1 적용 후 EStep.Measure 가 검사 시점에 `ShotParam.PixelResolution` 을 직접 읽으므로, Shot 값 편집 → 다음 검사부터 자동 반영됨을 코드 경로로 확인하고 SUMMARY에 기록한다 (별도 cascade/트리거 미구현이 정상).

4. **로딩 정규화 확장 불필요 확인 (Claude's Discretion):**
   - (B) Rewire 하에서 measurement 자체 PixelResolution 값은 더 이상 소비되지 않으므로 InspectionRecipeManager 정규화 확장은 불필요함을 확인하고 SUMMARY에 기록한다 (변경 0).

5. **최종 빌드 게이트:** msbuild Debug/x64 Rebuild PASS, 신규 error 0 / 신규 warning 0 (Phase 41 baseline 6 warnings 유지).
  </action>
  <verify>
    <automated>MISSING — 검증 전용 태스크. msbuild Debug/x64 Rebuild PASS (신규 warning 0) + grep 로 InspectionRecipeManager 정규화/ApplyCalibrationResult shot.PixelResolution 기록 존재 확인. 실측 UAT(SIMUL_MODE)는 사용자 검증 단계로 이관.</automated>
  </verify>
  <acceptance_criteria>
    - Grep `PixelResolution` in InspectionRecipeManager.cs (line 325-334 범위) → fai 정규화(camRes 덮어쓰기) 코드 존재 확인
    - Grep `shot.PixelResolution = mmPerPixel` in MainView.xaml.cs → 1 매치 (편집 진입점 단일소스 수렴 확인)
    - msbuild Debug/x64 Rebuild PASS, 신규 error 0 / 신규 warning 0
    - SUMMARY에 다음 4 항목 기록: (a) 정규화 레시피 회귀 0 등가성 논증, (b) 구형(CAM 미존재) 레시피 의도적 보정 문서화, (c) D-03 라이브 반영 구조 확인, (d) 정규화 확장 불필요 확인
  </acceptance_criteria>
  <done>정규화 레시피 측정값 회귀 0 이 등가성으로 논증되고, 편집 진입점/라이브 반영/정규화 확장 불필요가 코드로 확인되어 SUMMARY에 문서화됨. 최종 빌드 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (해당 없음 — 신규 경계 0) | 본 phase는 오프라인 산업용 WPF 앱의 순수 내부 리팩토링. 신규 네트워크 표면 없음, 신규 외부 입력 없음, 인증/직렬화 포맷 변경 없음(INI 키 불변 D-07). 측정값 소비 경로 단일소스화 + PropertyGrid 노출 정리만 수행. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | INI 레시피 라운드트립 (PixelResolutionX/Y 필드 보존) | accept | 필드 삭제 0 — ParamBase Reflection 직렬화 무영향(Browsable 어트리뷰트는 PropertyGrid 전용). 신규 외부 입력 표면 없음. 하위 호환 유지로 데이터 손상 위험 미증가. |
| T-42-02 | Denial of Service | EStep.Measure pixRes 캡처 / EdgePair Owner 체인 walk | accept | ShotParam null 가드(Conditional 1.0 fallback) + ownerShot null 가드(파라미터 fallback) 모두 적용 — NRE 로 인한 측정 스레드 크래시 방지. 기존 패턴(this file `if (ShotParam != null)`)과 일관. |

신규 고위험(High) 위협 0. 본 리팩토링은 공격 표면을 증가시키지 않으며, 기존 null-가드 패턴을 유지·강화한다.
</threat_model>

<verification>
- msbuild Debug/x64 Rebuild PASS, 신규 error 0 / 신규 warning 0 (baseline 6 warnings)
- Grep: Action_FAIMeasurement.cs 측정 호출부 `fai.PixelResolutionX` 0 매치 → `pixRes` 전달 2 매치
- Grep: EdgePairDistanceMeasurement.cs `PixelResolutionX = PixelResolutionX` 0 매치 → `resolvedPixelRes` 사용
- Grep: FAIConfig.cs + EdgePairDistanceMeasurement.cs PixelResolutionX/Y 위 `[PropertyTools.DataAnnotations.Browsable(false)]` 존재
- Grep: CameraSlaveParam.cs PixelResolution 무변경 (git diff 미포함)
- SIMUL_MODE 실측 UAT(Shot 값 편집 → 다음 검사 반영, PropertyGrid 항목별 미노출, 측정 mm 회귀 0)는 사용자 검증 단계로 이관
</verification>

<success_criteria>
- [측정 경로 단일 소스] 모든 측정이 ShotConfig.PixelResolution 단일소스에서 픽셀분해능을 소비 — 항목별 fai/self 필드 소비 0
- [PropertyGrid 항목별 노출 정리] FAIConfig/EdgePair PixelResolutionX/Y 4개 숨김, ShotConfig.PixelResolution 만 노출
- [재시작 없이 전체 FAI 반영] EStep.Measure 가 검사 시점에 shot 값 직접 읽음 → Shot 편집 후 다음 검사부터 자동 반영 (구조적 충족, cascade 미구현 정상)
- [회귀 0] 정규화 레시피 측정 mm 값 Rewire 전후 동일 (로딩 정규화 등가성으로 논증)
- [하위 호환] INI 키 PixelResolutionX/Y 보존, 포맷 변경/마이그레이션 0
- [out of scope 미구현 확인] X≠Y 비정방형 실지원 / INI 키 변경 / 정식 Gage R&R / 측정 알고리즘 로직 변경 — 전부 미수행
</success_criteria>

<output>
After completion, create `.planning/phases/42-pixel-resolution-single-source/42-01-SUMMARY.md`
</output>
