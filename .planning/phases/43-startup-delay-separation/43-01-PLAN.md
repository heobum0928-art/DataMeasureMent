---
phase: 43-startup-delay-separation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Login/LoginManager.cs
  - WPF_Example/SystemHandler.cs
  - WPF_Example/UI/Login/LoginWindow.xaml.cs
autonomous: false
requirements: [CO-38-02, CO-38-03]

must_haves:
  truths:
    - "앱 기동 시 LoginManager.Load() (account.db AES decrypt + JSON deserialize)가 SystemHandler.Initialize() 동기 임계 경로에서 실행되지 않는다 (백그라운드 thread)"
    - "[STARTUP] READY 마커가 Step 7 CollectRecipe 완료 직후 1회 출력되어 '첫 $TEST 수용 가능 시점'을 단일 기준 지표로 기록한다"
    - "로그인 다이얼로그를 열면 AccountList가 항상 완전히 로드된 상태다 (half-loaded race 없음)"
    - "백그라운드 프리로드 미완 상태에서 첫 $TEST가 수신되어도 안전하게 처리된다 (Login.IsLogin 기본 false → AutoLogout 분기 미진입)"
    - "기존 LoginManager 인증 동작/계정 모델은 변경되지 않는다 (Load() 본문 무수정, IsLogin/LoginAccount/AccountList 의미 동일)"
  artifacts:
    - path: "WPF_Example/Login/LoginManager.cs"
      provides: "백그라운드 프리로드 (Preload/EnsureLoaded/PreloadWorker) + volatile _isPreloaded ready 신호"
      contains: "volatile bool _isPreloaded"
    - path: "WPF_Example/SystemHandler.cs"
      provides: "Step 5 동기 Load 제거 → 백그라운드 프리로드 기동 + [STARTUP] READY 마커"
      contains: "[STARTUP] READY"
    - path: "WPF_Example/UI/Login/LoginWindow.xaml.cs"
      provides: "로그인 진입 시 프리로드 완료 보장 (EnsureLoaded 호출이 GetIDList보다 먼저)"
      contains: "EnsureLoaded"
  key_links:
    - from: "WPF_Example/SystemHandler.cs Initialize() Step 5"
      to: "LoginManager.Preload()"
      via: "백그라운드 thread 기동 (Login = LoginManager.Handle 직후)"
      pattern: "LoginManager\\.Handle\\.Preload\\(\\)"
    - from: "WPF_Example/UI/Login/LoginWindow.xaml.cs ctor"
      to: "LoginManager.EnsureLoaded()"
      via: "comboBox_id.ItemsSource = Login.GetIDList() 호출 이전 동기 대기"
      pattern: "Login\\.EnsureLoaded\\(\\)"
---

<objective>
앱 기동 시 `SystemHandler.Initialize()`의 Step 5 LoginManager 동기 로드(SIMUL 1회 실측 808ms delta = account.db AES decrypt + Newtonsoft.Json deserialize의 첫-접근 JIT/crypto warmup)를 측정 임계 경로 밖으로 이동하여, "첫 $TEST 수용 가능 시점"까지의 시간을 ≥30% 단축한다.

핵심 메커니즘 (CONTEXT D-01~D-10):
- **D-03/D-04:** `LoginManager` 생성자에서 동기 `Load()` 호출을 제거하고, 기동 직후 백그라운드 thread로 프리로드한다. `RawImageSaveService` 워커 패턴(volatile flag + Thread + Start guard)을 그대로 차용한다.
- **D-01/D-02:** Step 7 CollectRecipe 완료 직후 = recipe 스캔 완료 + SystemThread(Step 4) alive + Sequences(Step 2) 구성 완료 → 첫 $TEST 수용 가능 시점에 `[STARTUP] READY` 마커를 신규 출력. Before/After 비교의 단일 기준 지표.
- **D-06/D-07:** SequenceHandler(Step 2)는 측정 임계 경로이므로 이동하지 않는다. CO-38-03의 해석 = "시퀀스 준비에 불필요하게 끼어 있는 동기 의존성만 분리". 본 phase에서 분리 대상으로 식별된 동기 의존성은 LoginManager(Step 5) 하나이며, SequenceHandler 구간 계측은 유지한다.
- **D-04/D-10:** 로그인 UI 진입 시 `EnsureLoaded()`로 프리로드 완료를 보장(완료 시 즉시 반환, 미완 시 Join) → half-loaded AccountList race 없음.

Purpose: POC 시연 시 체감 기동 속도 개선 + v1.1 carry-over CO-38-02/03 종결.
Output: LoginManager 백그라운드 프리로드 + [STARTUP] READY 마커 + 로그인 UI readiness wait.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-startup-delay-separation/43-CONTEXT.md
@.planning/phases/43-startup-delay-separation/43-PATTERNS.md

<conventions>
- C# 7.2 / .NET Framework 4.8 — switch expression / nullable reference type / record 금지.
- 신규/수정 모든 라인에 `//260615 hbk Phase 43: ...` 주석 필수 (CLAUDE.md feedback 규칙). 기존 `//YYMMDD hbk` 마커는 보존하고 위에 누적(Phase 20 D-12 stacking 패턴).
- brace 스타일은 편집 대상 파일 스타일을 따른다 — LoginManager.cs / SystemHandler.cs / LoginWindow.xaml.cs 모두 K&R(여는 중괄호 같은 줄).
- `Logging.PrintLog` 호출 시 `ELogType`을 `(int)` 캐스팅 필수. 계측 로그는 `(int)ELogType.Trace`, prefix는 `[STARTUP]` / `[LOGIN]`.
- 스레드 안전: ready 신호는 `volatile bool`. account.db는 단일 read이므로 lock 불필요(RawImageSaveService 선례와 동일).
- startup 경로에서 throw 금지 — Load() 실패는 기존대로 내부 흡수(기본 admin 추가).
</conventions>

<interfaces>
<!-- 실코드 확인 완료 (2026-06-15). executor는 아래 시그니처를 그대로 사용 — 코드베이스 재탐색 불필요. -->

From WPF_Example/Login/LoginManager.cs (현재 상태):
```csharp
public static LoginManager Handle { get; } = new LoginManager();           // L88 — 변경 없음
public ObservableCollection<AccountInfo> AccountList { get; private set; }  // L94
public bool IsLogin { get; private set; }                                   // L92

private LoginManager() {                                                    // L98-103 — Load() 동기 호출 제거 대상
    ACCOUNT_FILE = AppDomain.CurrentDomain.BaseDirectory + @"account.db";
    if (!Load()) { /* occurs error or nothing */ }                         // L100 — 이 호출을 백그라운드로 이동
}

public bool Load() { ... }   // L164-208 — 본문 무수정. PreloadWorker 가 호출하는 대상.
public string[] GetIDList()  // L112-118 — AccountList 를 읽어 콤보박스 채움. EnsureLoaded 가 선행돼야 안전.
```

From WPF_Example/Utility/RawImageSaveService.cs (차용할 백그라운드 워커 패턴):
```csharp
private readonly Thread _workerThread;
private volatile bool _isStarted;
public RawImageSaveService() {
    _workerThread = new Thread(WorkLoop) { IsBackground = true, Name = "...", Priority = ThreadPriority.BelowNormal };
}
public void Start() { if (!_isStarted) { _workerThread.Start(); _isStarted = true; } }
```

From WPF_Example/SystemHandler.cs Initialize() (현재 Step 5, L149-152 — 교체 대상):
```csharp
// 5) Login manager
Login = LoginManager.Handle;   // <-- 첫 접근 시 생성자 Load() 동기 실행 = 808ms
Logging.PrintLog((int)ELogType.Trace, "[STARTUP] Step 5 LoginManager: {0} ms (cumulative), delta {1} ms", sw.ElapsedMilliseconds, sw.ElapsedMilliseconds - prev);
prev = sw.ElapsedMilliseconds;
```
Step 7 CollectRecipe 는 L163-167 (READY 마커 삽입 지점은 L167 prev 갱신 직후, Step 8 Localize L169 직전).

From WPF_Example/UI/Login/LoginWindow.xaml.cs (현재 ctor, L18-24):
```csharp
public LoginWindow() {
    InitializeComponent();
    Login = SystemHandler.Handle.Login;
    comboBox_id.ItemsSource = Login.GetIDList();   // L23 — 이 호출 전에 EnsureLoaded() 필요
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LoginManager 백그라운드 프리로드 인프라 추가 (Preload/EnsureLoaded/PreloadWorker + volatile _isPreloaded)</name>
  <files>WPF_Example/Login/LoginManager.cs</files>
  <read_first>
    - WPF_Example/Login/LoginManager.cs (L80~208 — 필드 블록 L84-96, 생성자 L98-103, Load() L164-208)
    - WPF_Example/Utility/RawImageSaveService.cs (volatile flag + Thread + Start guard 패턴 — 43-PATTERNS.md lines 82-105 에 발췌됨)
    - .planning/phases/43-startup-delay-separation/43-PATTERNS.md (lines 117-177 — LoginManager 추가 패턴 명세)
  </read_first>
  <action>
    `LoginManager.cs`에 백그라운드 프리로드 인프라를 추가한다. Load() 본문은 한 줄도 수정하지 않는다(인증 동작 불변 = D-03 고정 범위).

    1) **필드 추가** (기존 `private readonly string ACCOUNT_FILE;` L84 아래에):
    ```csharp
    private readonly Thread _preloadThread;          //260615 hbk Phase 43: D-03 백그라운드 프리로드 워커
    private volatile bool _isPreloaded;              //260615 hbk Phase 43: D-04 Load() 완료 ready 신호
    ```
    `System.Threading` using 이 없으면 파일 상단 using 블록에 `using System.Threading;` 추가(`//260615 hbk Phase 43`).

    2) **생성자 수정** (L98-103): 현재 `if (!Load()) { ... }` 동기 호출을 제거하고, `_preloadThread` 객체만 생성한다(아직 Start 하지 않음):
    ```csharp
    private LoginManager() {
        ACCOUNT_FILE = AppDomain.CurrentDomain.BaseDirectory + @"account.db";
        //260615 hbk Phase 43: D-03 — 생성자 동기 Load() 제거 → Preload() 백그라운드 이동 (기동 임계경로 외부)
        _preloadThread = new Thread(PreloadWorker) {
            IsBackground = true,
            Name = "LoginManagerPreload",
            Priority = ThreadPriority.BelowNormal
        };
    }
    ```

    3) **Preload() / PreloadWorker() / EnsureLoaded() 신규 추가** (Load() 메서드 근처, 예: Load() 정의 위 또는 아래):
    ```csharp
    //260615 hbk Phase 43: D-03 — Initialize() Step 5 에서 1회 호출. _isStarted 대신 IsAlive+_isPreloaded 이중 guard 로 재기동 방지.
    public void Preload() {
        if (!_isPreloaded && !_preloadThread.IsAlive) {
            _preloadThread.Start();
        }
    }

    private void PreloadWorker() {
        //260615 hbk Phase 43: Load() 본문 무수정 — 백그라운드 thread 에서 호출만 함
        if (!Load()) {
            //occurs error or nothing (Load() 내부에서 기본 admin 추가)
        }
        _isPreloaded = true; //260615 hbk Phase 43: D-04 완료 신호 (EnsureLoaded 가 확인)
        Logging.PrintLog((int)ELogType.Trace, "[LOGIN] Preload complete: {0} accounts", AccountList.Count); //260615 hbk Phase 43
    }

    //260615 hbk Phase 43: D-04/D-10 — 로그인 UI 가 호출. 완료면 즉시 반환(대기 0), 미완이면 Join 으로 half-loaded race 차단.
    public void EnsureLoaded() {
        if (_isPreloaded) return;
        if (_preloadThread.IsAlive) {
            _preloadThread.Join();
        }
        else if (!_isPreloaded) {
            //프리로드가 한 번도 기동되지 않은 방어 경로 — 동기 Load() 폴백 (UI 진입 시 항상 AccountList 보장)
            Load();
            _isPreloaded = true;
        }
    }
    ```
    참고: `ELogType` / `Logging` 가 이미 이 파일에서 사용 가능한지 확인하고, 없으면 해당 namespace using 추가(`//260615 hbk Phase 43`). brace 스타일은 LoginManager.cs 기존 K&R 유지.
  </action>
  <verify>
    <automated>MISSING — 컴파일 검증은 Task 3 통합 후 msbuild 으로 일괄 수행. 본 task 는 grep 구조 검증: Select-String -Path WPF_Example/Login/LoginManager.cs -Pattern 'volatile bool _isPreloaded','public void Preload','public void EnsureLoaded','private void PreloadWorker' 가 4건 모두 매치, 'if (!Load())' 가 생성자 블록(L98-103)에서 제거됐는지(PreloadWorker 내부에만 존재) 확인</automated>
  </verify>
  <acceptance_criteria>
    - LoginManager.cs 가 `volatile bool _isPreloaded` 를 포함한다
    - LoginManager.cs 가 `public void Preload()` 를 포함한다
    - LoginManager.cs 가 `public void EnsureLoaded()` 를 포함한다
    - LoginManager.cs 가 `private void PreloadWorker()` 를 포함한다
    - LoginManager.cs 가 `_preloadThread = new Thread(PreloadWorker)` 를 포함한다
    - LoginManager 생성자(L98 인근)에 더 이상 `Load()` 직접 호출이 없다 (Load 호출은 PreloadWorker/EnsureLoaded 폴백 내부에만 존재)
    - Load() 메서드 본문(L164-208 상당)은 무수정 (AES decrypt + JsonConvert.DeserializeObject 라인 보존)
    - 신규/수정 라인에 `//260615 hbk Phase 43` 마커 존재
  </acceptance_criteria>
  <done>백그라운드 프리로드 인프라 추가 완료, Load() 인증 동작 불변, 생성자 동기 Load 제거.</done>
</task>

<task type="auto">
  <name>Task 2: SystemHandler.Initialize() Step 5 백그라운드 기동 교체 + [STARTUP] READY 마커 삽입</name>
  <files>WPF_Example/SystemHandler.cs</files>
  <read_first>
    - WPF_Example/SystemHandler.cs (L109~177 — Initialize() 전체, Step 5 = L149-152, Step 7 = L163-167, Step 8 = L169-173)
    - .planning/phases/43-startup-delay-separation/43-PATTERNS.md (lines 36-74 — Step 5 교체 패턴 + READY 마커 삽입 위치)
  </read_first>
  <action>
    Task 1 의 `Preload()` / `_isPreloaded` 가 존재한다는 전제로 Step 5 를 교체하고 READY 마커를 추가한다. Step 1~8 의 기존 `[STARTUP]` 계측 라인은 모두 보존한다(Phase 38 #11 baseline). Step 2 SequenceHandler(L124)는 손대지 않는다 (D-06: 측정 임계 경로 이동 금지).

    1) **Step 5 교체** (현재 L149-152):
    ```csharp
    // 5) Login manager — background preload (측정 임계 경로 외부)
    //260615 hbk Phase 43: D-03 — 동기 Load() 제거 → 백그라운드 프리로드로 교체 (Step 5 delta 808ms → ~0)
    Login = LoginManager.Handle;           // Handle getter(인스턴스 취득)만 — 생성자에서 Load() 제거됨(Task 1)
    LoginManager.Handle.Preload();         //260615 hbk Phase 43: 백그라운드 Thread 기동 (내부 IsAlive+_isPreloaded guard)
    Logging.PrintLog((int)ELogType.Trace, "[STARTUP] Step 5 LoginManager preload started: {0} ms (cumulative), delta {1} ms", sw.ElapsedMilliseconds, sw.ElapsedMilliseconds - prev); //260615 hbk Phase 43
    prev = sw.ElapsedMilliseconds; //260615 hbk Phase 43
    ```

    2) **[STARTUP] READY 마커 삽입** (D-01/D-02): Step 7 CollectRecipe 의 `prev = sw.ElapsedMilliseconds;`(L167) 직후, Step 8 Localize 주석(L169 `// 8) Localization resource`) 직전에 삽입:
    ```csharp
    //260615 hbk Phase 43: [STARTUP] READY — recipe ready + SystemThread alive + Sequences 구성 완료
    //  = 첫 $TEST 수용 가능 시점 (D-01). Before/After 30% 비교의 단일 기준 지표 (D-02).
    Logging.PrintLog((int)ELogType.Trace, "[STARTUP] READY: {0} ms", sw.ElapsedMilliseconds);
    ```
    `// 8) Localization resource` 이하 Step 8 + `[STARTUP] Total Initialize` 라인은 무수정 유지.
  </action>
  <verify>
    <automated>MISSING — 컴파일 검증은 Task 3 통합 후 일괄. 본 task grep 검증: Select-String -Path WPF_Example/SystemHandler.cs -Pattern '\[STARTUP\] READY','LoginManager.Handle.Preload\(\)','Step 5 LoginManager preload started' 3건 매치 + Step 2 SequenceHandler 라인 'Sequences = SequenceHandler.Handle;' 보존 확인 + 'Step 1' ~ 'Step 8' + 'Total Initialize' 계측 라인 9건 보존 확인</automated>
  </verify>
  <acceptance_criteria>
    - SystemHandler.cs 가 `[STARTUP] READY` 를 포함한다
    - SystemHandler.cs 가 `LoginManager.Handle.Preload()` 를 포함한다
    - Step 5 라인이 `Login = LoginManager.Handle;` 단독 동기 접근이 아니라 Preload() 기동을 동반한다
    - Step 2 `Sequences = SequenceHandler.Handle;` 라인 무수정 보존 (D-06)
    - 기존 Step 1~8 + Total `[STARTUP]` 계측 라인(`//260528 hbk Phase 38 #11`)이 모두 보존된다
    - READY 마커가 Step 7 CollectRecipe 이후·Step 8 Localize 이전에 위치한다
    - 신규 라인에 `//260615 hbk Phase 43` 마커 존재
  </acceptance_criteria>
  <done>Step 5 백그라운드 기동 교체 완료, READY 마커 삽입(첫 $TEST 수용 가능 시점), SequenceHandler·기존 계측 무손상.</done>
</task>

<task type="auto">
  <name>Task 3: LoginWindow 진입 시 EnsureLoaded readiness wait + 통합 빌드</name>
  <files>WPF_Example/UI/Login/LoginWindow.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/Login/LoginWindow.xaml.cs (L15-24 — ctor)
    - .planning/phases/43-startup-delay-separation/43-PATTERNS.md (lines 222-250 — LoginWindow ctor 수정 패턴 + UI 차단 허용 근거)
  </read_first>
  <action>
    1) **LoginWindow 생성자 수정** (L18-24): `comboBox_id.ItemsSource = Login.GetIDList();`(L23) 호출 **이전**에 `Login.EnsureLoaded();` 를 삽입한다. 이는 half-loaded AccountList race(D-10)를 차단한다:
    ```csharp
    public LoginWindow() {
        InitializeComponent();
        Login = SystemHandler.Handle.Login;
        Login.EnsureLoaded(); //260615 hbk Phase 43: D-04/D-10 — 프리로드 미완 시 완료까지 대기(완료면 즉시 반환). half-loaded AccountList race 차단

        //init combobox
        comboBox_id.ItemsSource = Login.GetIDList();
    }
    ```
    `EnsureLoaded()`는 모달 `ShowDialog()` 컨텍스트의 UI thread 차단이지만, 다이얼로그 자체가 모달 차단이므로 허용 가능(43-PATTERNS.md L250 근거).

    2) **통합 빌드** (Task 1/2/3 컴파일 게이트): SIMUL_MODE Debug/x64 로 빌드하여 0 new error/warning 확인.
    ```powershell
    & "C:\Program Files (x86)\Microsoft Visual Studio\2019\...\MSBuild.exe" WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build
    ```
    (실제 MSBuild 경로는 환경에서 확인. 빌드 명령이 환경별로 다르면 기존 phase SUMMARY 의 빌드 명령 차용.)
  </action>
  <verify>
    <automated>msbuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 → "0 Error" + 신규 warning 0 (Phase 42 baseline 대비). 추가 grep: Select-String -Path WPF_Example/UI/Login/LoginWindow.xaml.cs -Pattern 'Login.EnsureLoaded\(\)' 가 GetIDList 호출 라인보다 앞 라인에 매치</automated>
  </verify>
  <acceptance_criteria>
    - LoginWindow.xaml.cs 가 `Login.EnsureLoaded()` 를 포함한다
    - `EnsureLoaded()` 호출 라인이 `comboBox_id.ItemsSource = Login.GetIDList();` 보다 앞에 위치한다
    - msbuild Debug/x64 PASS: 0 errors, 신규 warning 0
    - 신규 라인에 `//260615 hbk Phase 43` 마커 존재
  </acceptance_criteria>
  <done>로그인 UI readiness wait 완료, 3 파일 통합 빌드 PASS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [STARTUP] 3~5회 평균 30% 단축 입증 + 회귀 (첫 로그인 / 첫 $TEST race)</name>
  <action>
    런타임 검증 전용 체크포인트(코드 변경 없음). executor는 빌드 산출물을 실행하고 아래 <how-to-verify> 3항목을 수행한 뒤 사용자에게 결과를 제시하고 정지한다. 수치/로그는 사용자 관측이 필요하므로 executor가 자체 PASS 판정하지 않는다.
  </action>
  <what-built>
    LoginManager 백그라운드 프리로드(Step 5 동기 808ms 제거) + Step 7 직후 `[STARTUP] READY` 마커 + LoginWindow EnsureLoaded readiness wait. 코드 구조/빌드는 Task 1~3에서 자동 검증 완료. 본 체크포인트는 런타임 수치/회귀(앱 실행 필요, executor 자체 검증 불가)를 확인한다.
  </what-built>
  <how-to-verify>
    SIMUL_MODE(Debug/x64) 빌드된 앱으로 검증한다. 각 항목 결과를 알려주세요.

    1. **30% 단축 입증 (D-09 — 3~5회 평균):**
       - 앱을 3~5회 기동하고 매 기동마다 Trace 로그에서 `[STARTUP] READY: N ms` 값을 기록.
       - Before 기준선 = Phase 38 SIMUL Total 1509ms 중 READY 상당 구간(Step 5 LoginManager 808ms가 READY 이전에 포함되어 있던 값). 비교 편의를 위해 동일 빌드에서 Step 5 교체 전/후가 어렵다면, 최소한 `[STARTUP] READY` 평균값이 Phase 38 기준선(≈1509ms에서 LoginManager 808ms 제거 ≈ 700ms 수준)으로 ≥30% 감소했는지 확인.
       - 기대: READY 평균이 Before 대비 ≥30% 단축. (JIT 첫-실행 편차 흡수를 위해 1회가 아닌 3~5회 평균)
    2. **회귀 (a) 첫 로그인 정상 (D-10):**
       - 앱 기동 직후 곧바로 로그인 다이얼로그를 연다(프리로드 미완 타이밍 유도).
       - 기대: 계정 콤보박스가 정상적으로 채워지고(빈 목록 아님), admin 로그인 성공. `[LOGIN] Preload complete` 로그가 EnsureLoaded 대기 후 또는 그 전에 출력됨.
    3. **회귀 (b) 첫 $TEST 정상 — race 없음 (D-10):**
       - 핸들러/목 클라이언트로 기동 직후 첫 `$TEST` 를 전송.
       - 기대: 검사가 정상 수행되고 readiness race(NRE/half-loaded) 없음. `[STARTUP] READY` 출력 이후 TEST가 정상 처리됨.
  </how-to-verify>
  <verify>
    <automated>MISSING — 런타임 human-verify. [STARTUP] READY 로그 3~5회 평균 30% 이상 단축 + 첫 로그인/첫 TEST 회귀는 앱 실행 후 사용자 확인.</automated>
  </verify>
  <done>READY 평균 30% 이상 단축 입증 + 회귀 (a)(b) PASS, 사용자 approved.</done>
  <resume-signal>"approved" (3항목 모두 PASS) 또는 발견된 이슈(수치/로그/스택트레이스) 기술</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 기동 thread ↔ 백그라운드 프리로드 thread | `_isPreloaded`(write: PreloadWorker) ↔ (read: EnsureLoaded). `AccountList` 교체(Load() L199)와 read(GetIDList) 간 가시성 경계 |
| 외부 핸들러 → MainRun $TEST | 기존 TCP 경계 — 본 phase에서 신규 입력/엔드포인트 추가 없음 |

## STRIDE Threat Register

본 phase는 내부 Windows 데스크톱 앱의 startup 타이밍 리팩토링으로, 신규 외부 입력·네트워크 엔드포인트·인증 변경이 없다(D-03 인증 동작/계정 모델 불변). 표면은 낮음.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering (race) | LoginManager 프리로드 thread vs LoginWindow read | mitigate | `_isPreloaded` 를 `volatile` 로 선언(가시성 보장) + LoginWindow.ctor 가 `GetIDList()` 이전 `EnsureLoaded()`(Join 또는 동기 폴백) 호출 → half-loaded AccountList 절대 read 안 됨. account.db 는 단일 read 라 추가 lock 불필요(RawImageSaveService 선례) |
| T-43-02 | Elevation (auth bypass) | 첫 $TEST 가 프리로드 미완 상태에서 인증 게이트 우회 | accept | MainRun `case Test` 의 `Login.IsLogin` 체크는 `IsLogin` 기본값 false → AutoLogout 분기 미진입(안전 통과). $TEST 처리는 본래 인증 게이트가 아니라 핸들러 신뢰 경계이며 동작 불변. 신규 우회 경로 도입 없음 |
| T-43-03 | DoS (startup hang) | EnsureLoaded Join 이 무한 대기 | mitigate | PreloadWorker 는 throw 금지 경로(Load 실패 = 내부 흡수 + 기본 admin) → thread 항상 종료 → Join 무한 대기 불가. `_isPreloaded` 미설정 방어 경로에 동기 Load() 폴백 추가로 thread 미기동 시에도 AccountList 보장 |
</threat_model>

<verification>
- 정적(자동, executor 수행): Task 1~3 grep acceptance_criteria 전건 + msbuild Debug/x64 0 error / 신규 warning 0.
- 런타임(human-verify, Task 4): [STARTUP] READY 3~5회 평균 ≥30% 단축 + 첫 로그인 정상 + 첫 $TEST race 없음.
- 회귀 가드: Load() 본문 무수정(인증 동작 불변), Step 2 SequenceHandler 라인 무수정(D-06), 기존 [STARTUP] 9줄 계측 보존.
</verification>

<success_criteria>
- `[STARTUP] READY` 마커가 Step 7 직후 출력되고, 3~5회 평균이 Phase 38 기준선 대비 ≥30% 단축됨 (D-01/D-02/D-09).
- LoginManager.Load() 가 기동 동기 임계 경로에서 실행되지 않음 (백그라운드 thread) (CO-38-02).
- SequenceHandler 동기 의존성 검토 완료 — 측정 임계 경로 유지, LoginManager 동기 의존성만 분리 (CO-38-03, D-06/D-07).
- 첫 로그인·첫 $TEST 정상, half-loaded race 없음 (D-10).
- 인증 동작/계정 모델 불변, msbuild PASS.
</success_criteria>

<output>
After completion, create `.planning/phases/43-startup-delay-separation/43-01-SUMMARY.md`
</output>
