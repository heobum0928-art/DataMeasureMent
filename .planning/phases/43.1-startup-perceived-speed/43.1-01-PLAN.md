---
phase: 43.1-startup-perceived-speed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/App.xaml.cs
  - WPF_Example/MainWindow.xaml.cs
  - WPF_Example/Resource/splash.png
  - WPF_Example/DatumMeasurement.csproj
autonomous: false
requirements: [CO-43-01]

must_haves:
  truths:
    - "앱 더블클릭 후 ≤1s 이내에 정적 스플래시(프로그램명 + '기동 중...')가 표시되어 18~20초 흰 화면(체감 정지)이 시각적으로 제거된다 (관리 UI/JIT 이전 WPF 네이티브 SplashScreen)"
    - "흰 화면 구간이 5개 [STARTUP-WHITE] 마커로 분해 계측되어, 18~20초 중 어느 구간(콜드 JIT / Initialize / InitializeComponent / Show→첫 paint)이 지배적인지 사용자 실행 로그로 수치화된다"
    - "스플래시는 MainWindow 의 첫 실제 paint(ContentRendered)까지 유지된 뒤 fade 로 닫혀, 닫힌 직후 흰 화면이 재노출되지 않는다 (auto-close 금지)"
    - "SystemHandler.Initialize 동작 의미·순서가 불변이고, Phase 43 [STARTUP] READY 마커 의미(첫 $TEST 수용 가능 시점)가 보존된다 (계측+마스킹만, async 리팩토링 없음 = D-05)"
    - "회귀 0: 기동 후 첫 로그인 / 첫 $TEST / MainView 정상 동작 (스플래시 추가가 기존 기동 흐름을 깨지 않음)"
  artifacts:
    - path: "WPF_Example/App.xaml.cs"
      provides: "App 정적 Stopwatch + [STARTUP-WHITE] 마커 (a)(e) + Application_Startup 진입 스플래시 Show + ContentRendered 시 splash.Close(fade)"
      contains: "[STARTUP-WHITE]"
    - path: "WPF_Example/MainWindow.xaml.cs"
      provides: "[STARTUP-WHITE] 마커 (b)(c)(d) — ctor 진입 / Initialize 전후 / InitializeComponent 전후"
      contains: "[STARTUP-WHITE]"
    - path: "WPF_Example/Resource/splash.png"
      provides: "정적 스플래시 PNG (프로그램명 DataMeasurement + '기동 중...', 애니메이션 없음)"
      contains: "PNG binary"
    - path: "WPF_Example/DatumMeasurement.csproj"
      provides: "splash.png 을 Build Action=Resource 로 등록 (기존 Resource\\*.png 블록과 동일)"
      contains: "Resource\\splash.png"
  key_links:
    - from: "WPF_Example/App.xaml.cs Application_Startup 진입"
      to: "new SplashScreen(\"Resource/splash.png\").Show(false)"
      via: "관리 UI(new MainWindow()) 생성 이전, autoClose=false 즉시 표시"
      pattern: "new SplashScreen\\("
    - from: "WPF_Example/App.xaml.cs view.Show() 이후"
      to: "MainWindow.ContentRendered → splash.Close(TimeSpan)"
      via: "첫 실제 paint 이벤트 구독 후 fade close"
      pattern: "ContentRendered"
    - from: "WPF_Example/MainWindow.xaml.cs ctor + App 정적 Stopwatch"
      to: "Logging.PrintLog((int)ELogType.Trace, \"[STARTUP-WHITE] ...\")"
      via: "흰 화면 구간 (a)~(e) 절대시각 분해 로그"
      pattern: "\\[STARTUP-WHITE\\]"
---

<objective>
앱 더블클릭 → 창 표시까지의 **18~20초 흰 화면**(체감 기동 정지)을, (1) 구간 분해 계측으로 지배 원인을 수치화하고 (2) 즉시 표시되는 WPF 네이티브 스플래시로 가려 POC 시연 체감 속도를 개선한다.

핵심 메커니즘 (CONTEXT D-01~D-06):
- **D-03 (계측 우선):** 마스킹 **전에** 흰 화면 구간을 분해하는 계측을 먼저 추가한다(Task 1). App 레벨 정적 `Stopwatch` + `[STARTUP-WHITE]` 마커 5종 — (a) App 정적 진입, (b) MainWindow ctor 진입, (c) Initialize() 전후, (d) InitializeComponent() 전후, (e) ctor 종료→Show()→ContentRendered 첫 paint. 기존 `[STARTUP]`(Phase 38/43, Initialize 내부) 로그와 같은 `Logging.PrintLog((int)ELogType.Trace, ...)` 형식으로 연계해, 18~20초가 어느 구간에서 소모되는지 사용자 실행 로그로 입증한다.
- **D-01 (마스킹 메커니즘):** WPF 네이티브 `System.Windows.SplashScreen`(정적 PNG, 관리 UI/JIT **이전** 표시)으로 흰 화면을 가린다. 별도 WPF `Window` 로딩 화면은 그 자체가 JIT+inflation 비용을 유발해 기각.
- **D-02 (수동 제어):** `Application_Startup` 진입 즉시 `new SplashScreen("Resource/splash.png").Show(false)`(autoClose=false) → `MainWindow.ContentRendered`(첫 실제 paint) 시 `splash.Close(fade)`. auto-close 는 MainWindow 생성 전 너무 일찍 닫혀 흰 화면 재노출 → 금지.
- **D-04 (자산):** 정적 PNG 1개(프로그램명 "DataMeasurement" + "기동 중..." 정적 텍스트, 애니메이션 없음)를 `WPF_Example/Resource/splash.png` 에 추가, csproj Build Action=Resource 등록(기존 `Resource\*.png` 패턴 동일).
- **D-05 (범위 가드):** 본 phase = **계측 + 마스킹(저위험)** ONLY. `Initialize()`/`InitializeComponent()` 의 실제 async 재구성은 **out-of-scope**(후속 phase). `SystemHandler.Initialize` 동작 의미 불변, Phase 43 `[STARTUP] READY` 마커 의미(첫 `$TEST` 수용 가능 시점) 절대 보존.

Purpose: POC 시연 체감 기동 속도 개선 + v1.2 carry-over CO-43-01 종결.
Output: App/MainWindow 흰 화면 구간 [STARTUP-WHITE] 분해 계측 + 즉시 표시 스플래시(흰 화면 마스킹) + splash.png 자산.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43.1-startup-perceived-speed/43.1-CONTEXT.md
@.planning/phases/43-startup-delay-separation/43-01-SUMMARY.md

<conventions>
- C# 7.2 / .NET Framework 4.8 — switch expression / nullable reference type / record 금지.
- 신규/수정 모든 라인에 `//260615 hbk Phase 43.1: ...` 주석 필수 (CLAUDE.md feedback 규칙). 기존 `//YYMMDD hbk` 마커는 보존하고 위에 누적(stacking).
- brace 스타일은 편집 대상 파일 스타일을 따른다 — App.xaml.cs / MainWindow.xaml.cs 모두 K&R(여는 중괄호 같은 줄).
- `Logging.PrintLog` 호출 시 `ELogType` 을 `(int)` 캐스팅 필수. 계측 로그는 `(int)ELogType.Trace`, 흰 화면 구간 prefix 는 `[STARTUP-WHITE]`(기존 Initialize 내부 `[STARTUP]` 와 구분).
- startup 경로에서 throw 금지 — 스플래시 Show/Close 코드는 try/catch 로 감싸 예외가 기동을 막지 않게 한다(App.Dispatcher_UnhandledException 흡수 선례).
- 빌드 게이트: msbuild Debug/x64 SIMUL_MODE → 0 error, 0 신규 warning. Phase 42/43 baseline = 6 warning(CS0618 x4 + CS0162 + MSB3884) 유지. 빌드 명령은 43-01-SUMMARY.md 빌드 절차 차용(Task 1~3 PASS 확인됨).
</conventions>

<interfaces>
<!-- 실코드 확인 완료 (2026-06-15). executor 는 아래 시그니처/라인을 그대로 사용 — 코드베이스 재탐색 불필요. -->

From WPF_Example/App.xaml (L7):
```xml
<Application ... Startup="Application_Startup">   <!-- StartupUri / SplashScreen 속성 없음 — 코드로 제어 -->
```

From WPF_Example/App.xaml.cs (현재):
```csharp
// L20  Mutex mMutex;
// L22-25  public App() { this.Dispatcher.UnhandledException += this.Dispatcher_UnhandledException; }
// L27-39  Application_Startup — 스플래시 표시 + 계측 (a)(e) 삽입 대상:
private void Application_Startup(object sender, StartupEventArgs e) {
    var resource = App.Current.Resources["DR"] as LocalizationResource;   // L28
    if (resource != null) { resource.ChangeLanguage("ko-KR"); }
    CultureInfo c = new CultureInfo("ko-KR"); ...                          // L33-35 언어/메타데이터
    var view = new MainWindow();   // L37 — 흰 화면 유발(ctor 안에서 Initialize+InitializeComponent)
    view.Show();                   // L38 — 첫 paint 직전
}
// L41-51  OnStartup — mutex 단일 인스턴스(중복이면 Shutdown). Application_Startup 보다 먼저 호출됨.
// L55-71  Dispatcher_UnhandledException — 예외 흡수 선례
```

From WPF_Example/MainWindow.xaml.cs (현재 ctor, L75-99):
```csharp
public MainWindow() {
    mSystemHandler = SystemHandler.Handle;   // L77
    mSystemHandler.Initialize();             // L78 — Phase 43 [STARTUP] Step1~8 계측(내부)
    InitializeComponent();                   // L81 — 전체 MDI XAML inflation(무거움)
    mSystemHandler.Sequences.OnRecipeChanged += this.OnLoadRecipe;   // L84~ 이벤트 와이어링
    ...
    mTimer.Tick += TimerTick;                // L97
}
// ContentRendered 핸들러 없음 — 본 plan 에서 추가하지 않고(App 가 구독), App.Application_Startup 가 view.ContentRendered 를 구독해 splash 를 닫는다.
// MainWindow.xaml.cs using 블록에 ReringProject.Utility(Logging/ELogType) L10, System L? 존재.
```

From WPF_Example/DatumMeasurement.csproj (기존 PNG 리소스 패턴, L605~658 인근):
```xml
<Resource Include="Resource\home.png" />
<Resource Include="Resource\login.png" />
<!-- ... splash.png 를 같은 ItemGroup 에 동일 형식으로 추가 -->
```
`System.Windows.SplashScreen` 은 Build Action=Resource 인 이미지를 어셈블리 상대경로 문자열("Resource/splash.png")로 로드한다(.NET 3.5+ 기본 제공, 별도 패키지 불필요).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 흰 화면 구간 분해 계측 — App 정적 Stopwatch + [STARTUP-WHITE] 마커 (a)~(e)</name>
  <files>WPF_Example/App.xaml.cs, WPF_Example/MainWindow.xaml.cs</files>
  <read_first>
    - WPF_Example/App.xaml.cs (L1-72 — using 블록, App ctor L22-25, Application_Startup L27-39, Dispatcher_UnhandledException L55-71)
    - WPF_Example/MainWindow.xaml.cs (L1-99 — using 블록 L1-16, ctor L75-99: Initialize L78, InitializeComponent L81)
    - WPF_Example/SystemHandler.cs (L110-181 — 기존 [STARTUP] Step 마커 형식 참고. 이 파일은 본 task 에서 수정하지 않음)
  </read_first>
  <action>
    흰 화면(process 진입 → 첫 paint) 구간을 5개 절대시각 마커로 분해 계측한다. 마스킹(Task 2)보다 **먼저** 추가한다(D-03). 모든 로그는 기존 `[STARTUP]` 과 구분되도록 prefix `[STARTUP-WHITE]` 를 쓰고 `(int)ELogType.Trace` 캐스팅을 지킨다. Initialize 내부 기존 `[STARTUP]` 9줄(SystemHandler.cs)은 손대지 않는다.

    1) **App.xaml.cs — 정적 Stopwatch 필드 추가** (using 블록에 `System.Diagnostics` 없으면 추가):
    파일 상단 using 블록에 (없을 경우):
    ```csharp
    using System.Diagnostics; //260615 hbk Phase 43.1: 흰 화면 구간 계측 Stopwatch
    ```
    `App` 클래스 필드 영역(`Mutex mMutex;` L20 아래)에:
    ```csharp
    //260615 hbk Phase 43.1: D-03 — 흰 화면(process→첫 paint) 구간 분해용 절대시각 기준. App 정적 진입부터 측정.
    internal static readonly Stopwatch StartupWatch = Stopwatch.StartNew();
    ```
    참고: 정적 필드 이니셜라이저는 App 타입 최초 접근(= 프로세스 기동 직후 WPF 가 App 생성) 시점에 실행되므로, 흰 화면 시작에 가장 가까운 절대 0점이 된다.

    2) **App.xaml.cs — 마커 (a) App.Application_Startup 진입** (L27 `Application_Startup` 본문 첫 줄):
    ```csharp
    private void Application_Startup(object sender, StartupEventArgs e) {
        Logging.PrintLog((int)ELogType.Trace, "[STARTUP-WHITE] (a) App.Startup entry: {0} ms", StartupWatch.ElapsedMilliseconds); //260615 hbk Phase 43.1
        var resource = App.Current.Resources["DR"] as LocalizationResource;
        ...
    ```
    (`Logging`/`ELogType` 는 App.xaml.cs 에서 `ReringProject.Utility` using L10 으로 이미 사용 가능 — Dispatcher_UnhandledException L63 가 `Logging.PrintErrLog` 사용 중. 추가 using 불필요.)

    3) **App.xaml.cs — 마커 (e) 첫 paint** 는 Task 2 의 ContentRendered 구독부에서 함께 출력한다(중복 ContentRendered 구독 회피). 본 task 에서는 (e) 출력 라인을 남기지 않고, Task 2 가 splash.Close 와 같은 핸들러에서 `[STARTUP-WHITE] (e) ContentRendered first paint` 를 출력하도록 위임한다. (acceptance 는 (a)(b)(c)(d) 4종 + 빌드, (e) 는 Task 2 검증.)

    4) **MainWindow.xaml.cs — 마커 (b)(c)(d)** (ctor L75-99). `App.StartupWatch` 절대시각을 그대로 읽어 같은 시간축으로 분해한다:
    ```csharp
    public MainWindow() {
        Logging.PrintLog((int)ELogType.Trace, "[STARTUP-WHITE] (b) MainWindow ctor entry: {0} ms", App.StartupWatch.ElapsedMilliseconds); //260615 hbk Phase 43.1
        //initialize
        mSystemHandler = SystemHandler.Handle;
        mSystemHandler.Initialize();
        Logging.PrintLog((int)ELogType.Trace, "[STARTUP-WHITE] (c) after Initialize: {0} ms", App.StartupWatch.ElapsedMilliseconds); //260615 hbk Phase 43.1

        //update ui
        InitializeComponent();
        Logging.PrintLog((int)ELogType.Trace, "[STARTUP-WHITE] (d) after InitializeComponent: {0} ms", App.StartupWatch.ElapsedMilliseconds); //260615 hbk Phase 43.1

        mSystemHandler.Sequences.OnRecipeChanged += this.OnLoadRecipe;
        ... (이하 기존 와이어링/타이머 무수정)
    }
    ```
    (`Logging`/`ELogType` 는 MainWindow.xaml.cs `ReringProject.Utility` using L10 으로 사용 가능. `App` 은 같은 `ReringProject` 네임스페이스 — 추가 using 불필요.)

    주의: 마커 (c) 는 Initialize() 직후, (d) 는 InitializeComponent() 직후에 위치해 각각의 delta 가 콜드 JIT + native DLL 로딩 + XAML inflation 중 어느 비용을 먹는지 드러낸다. (c)-(b) = Initialize 구간(Phase 43 [STARTUP] Total 과 교차검증 가능), (d)-(c) = InitializeComponent(XAML inflation) 구간.
  </action>
  <verify>
    <automated>grep 구조 검증(컴파일은 Task 3 통합 빌드에서 일괄):
    Select-String -Path WPF_Example/App.xaml.cs -Pattern 'internal static readonly Stopwatch StartupWatch','\[STARTUP-WHITE\] \(a\)' → 2건 매치 +
    Select-String -Path WPF_Example/MainWindow.xaml.cs -Pattern '\[STARTUP-WHITE\] \(b\)','\[STARTUP-WHITE\] \(c\)','\[STARTUP-WHITE\] \(d\)','App.StartupWatch.ElapsedMilliseconds' → 4건 매치 +
    Select-String -Path WPF_Example/SystemHandler.cs -Pattern '\[STARTUP\] Total Initialize' → 1건 보존(무변경)</automated>
  </verify>
  <acceptance_criteria>
    - App.xaml.cs 가 `internal static readonly Stopwatch StartupWatch` 를 포함한다
    - App.xaml.cs Application_Startup 첫 줄에 `[STARTUP-WHITE] (a)` 마커가 있다
    - MainWindow.xaml.cs ctor 에 `[STARTUP-WHITE] (b)` (ctor 진입), `(c)` (Initialize 직후), `(d)` (InitializeComponent 직후) 3종 마커가 순서대로 있다
    - (b)(c)(d) 마커가 `App.StartupWatch.ElapsedMilliseconds` 를 읽어 동일 시간축을 쓴다
    - 기존 SystemHandler.cs `[STARTUP]` Step 1~8 + Total + READY 계측 라인이 무변경 보존된다 (D-05 의미 불변)
    - 신규 라인에 `//260615 hbk Phase 43.1` 마커 존재, brace K&R 유지
  </acceptance_criteria>
  <done>흰 화면 (a)(b)(c)(d) 구간 분해 계측 추가 완료, App 정적 Stopwatch 단일 시간축, 기존 [STARTUP] 무손상. ((e) 는 Task 2 ContentRendered 핸들러에서 출력.)</done>
</task>

<task type="auto">
  <name>Task 2: 스플래시 자산 생성 + Application_Startup 즉시 표시 + ContentRendered fade close + 마커 (e)</name>
  <files>WPF_Example/Resource/splash.png, WPF_Example/DatumMeasurement.csproj, WPF_Example/App.xaml.cs</files>
  <read_first>
    - WPF_Example/App.xaml.cs (Task 1 수정 후 상태 — using 블록, Application_Startup 본문)
    - WPF_Example/DatumMeasurement.csproj (L605~658 — 기존 `<Resource Include="Resource\*.png" />` ItemGroup. splash.png 를 같은 ItemGroup 에 추가)
    - WPF_Example/Camera_DDA.ico (브랜드 참고 — D-04)
  </read_first>
  <action>
    Task 1 의 `App.StartupWatch` 가 존재한다는 전제로 흰 화면 마스킹 스플래시를 추가한다(D-01/D-02/D-04).

    1) **splash.png 자산 생성** (`WPF_Example/Resource/splash.png`, 정적 PNG, 애니메이션 없음 — D-04):
    내용 = 프로그램명 "DataMeasurement" + 부제 "기동 중..." 정적 텍스트. 권장 크기 ≈ 480x270, 어두운 배경(예: #1E1E1E) + 밝은 텍스트(가독성). 기존 `WPF_Example/Camera_DDA.ico` 브랜드 톤 참고(레이아웃/색 디테일은 재량 — D-04 충족 시).
    실제 PNG 생성은 PowerShell + System.Drawing(.NET 4.8 기본 가용)으로 한다. 아래 스니펫을 그대로 사용(경로/문구만 확인):
    ```powershell
    Add-Type -AssemblyName System.Drawing
    $bmp = New-Object System.Drawing.Bitmap(480, 270)
    $g = [System.Drawing.Graphics]::FromImage($bmp)
    $g.SmoothingMode = [System.Drawing.Drawing2D.SmoothingMode]::AntiAlias
    $g.TextRenderingHint = [System.Drawing.Text.TextRenderingHint]::AntiAlias
    $g.Clear([System.Drawing.Color]::FromArgb(30, 30, 30))
    $f1 = New-Object System.Drawing.Font("Segoe UI", 28, [System.Drawing.FontStyle]::Bold)
    $f2 = New-Object System.Drawing.Font("Segoe UI", 14)
    $brush = New-Object System.Drawing.SolidBrush([System.Drawing.Color]::White)
    $g.DrawString("DataMeasurement", $f1, $brush, 70, 95)
    $g.DrawString("기동 중...", $f2, $brush, 200, 150)
    $g.Dispose()
    $bmp.Save("WPF_Example\Resource\splash.png", [System.Drawing.Imaging.ImageFormat]::Png)
    $bmp.Dispose()
    ```
    생성 후 파일이 유효한 PNG 인지 확인(헤더 매직 `\x89PNG`, Test-Path). 디자인 완성도는 Task 4 사용자 확인 대상이므로, 가독성 있는 2줄 텍스트면 충분하다. (좌표/폰트는 재량 — D-04 충족 시.)

    2) **csproj 등록** (Build Action=Resource — D-04): 기존 `<Resource Include="Resource\login.png" />` 등이 있는 ItemGroup(L605~658 인근)에 동일 형식으로 추가:
    ```xml
    <Resource Include="Resource\splash.png" /> <!--260615 hbk Phase 43.1: 흰 화면 마스킹 스플래시 (D-04)-->
    ```
    (Build Action=Content 가 아니라 Resource 로 등록 — `System.Windows.SplashScreen` 은 어셈블리 임베드 Resource 이미지를 상대경로 문자열로 로드한다.)

    3) **App.xaml.cs — Application_Startup 진입 즉시 스플래시 Show** (D-01/D-02): Task 1 의 `[STARTUP-WHITE] (a)` 로그 **직후**, `new MainWindow()`(L37) **이전**에 삽입. autoClose=false 로 수동 제어. 필드로 splash 참조 보관:
    `App` 클래스 필드(StartupWatch 아래):
    ```csharp
    private System.Windows.SplashScreen _splash; //260615 hbk Phase 43.1: D-02 수동 제어용 splash 참조 (ContentRendered 에서 Close)
    ```
    `Application_Startup` 본문:
    ```csharp
    Logging.PrintLog((int)ELogType.Trace, "[STARTUP-WHITE] (a) App.Startup entry: {0} ms", StartupWatch.ElapsedMilliseconds); //260615 hbk Phase 43.1 (Task 1)
    //260615 hbk Phase 43.1: D-01/D-02 — 관리 UI(new MainWindow) 이전 즉시 스플래시 표시(autoClose=false). 흰 화면 마스킹.
    try {
        _splash = new System.Windows.SplashScreen("Resource/splash.png");
        _splash.Show(false); // autoClose=false — ContentRendered 까지 유지
    }
    catch (Exception ex) {
        Logging.PrintErrLog((int)ELogType.Error, "[STARTUP-WHITE] splash show fail: " + ex.Message); //260615 hbk Phase 43.1 — 스플래시 실패는 기동을 막지 않음
        _splash = null;
    }
    ```

    4) **App.xaml.cs — ContentRendered 구독 → splash fade close + 마커 (e)** (D-02): ContentRendered 핸들러는 반드시 `view.Show();`(L38) **이전**에 구독한다(`new MainWindow()` 직후 ~ `view.Show()` 직전). Show() 이후 구독은 첫 paint 누락 위험이 있으므로 금지. 첫 실제 paint 시점에 (e) 로그 출력 + splash 를 fade 로 닫는다:
    ```csharp
    var view = new MainWindow();
    //260615 hbk Phase 43.1: D-02 — 첫 실제 paint(ContentRendered)까지 splash 유지 후 fade close. auto-close 금지(너무 일찍 닫혀 흰 화면 재노출).
    view.ContentRendered += (s, ev) => {
        Logging.PrintLog((int)ELogType.Trace, "[STARTUP-WHITE] (e) ContentRendered first paint: {0} ms", StartupWatch.ElapsedMilliseconds); //260615 hbk Phase 43.1
        if (_splash != null) {
            _splash.Close(TimeSpan.FromSeconds(0.3)); // 0.3s fade
            _splash = null;
        }
    };
    view.Show();
    ```
    참고: ContentRendered 는 첫 렌더 후 1회 발생하므로 중복 close 안전. fade 시간(0.3s)은 D-02 계약(첫 paint 까지 유지) 만족 내 재량.

    빌드 게이트는 Task 3 에서 일괄 수행.
  </action>
  <verify>
    <automated>grep 구조 검증(컴파일은 Task 3 통합 빌드에서 일괄):
    Test-Path WPF_Example/Resource/splash.png = True (유효 PNG) +
    Select-String -Path WPF_Example/DatumMeasurement.csproj -Pattern 'Resource\\splash.png' 1건 +
    Select-String -Path WPF_Example/App.xaml.cs -Pattern 'new System.Windows.SplashScreen\(','_splash.Show\(false\)','ContentRendered','_splash.Close\(','\[STARTUP-WHITE\] \(e\)' 5건 매치 +
    'new System.Windows.SplashScreen' 라인이 'var view = new MainWindow' 라인보다 앞에 위치</automated>
  </verify>
  <acceptance_criteria>
    - WPF_Example/Resource/splash.png 가 존재하고 유효한 PNG 이다 ("DataMeasurement" + "기동 중..." 텍스트, 정적)
    - DatumMeasurement.csproj 가 `<Resource Include="Resource\splash.png" />` 를 포함한다 (Build Action=Resource)
    - App.xaml.cs 가 `new System.Windows.SplashScreen("Resource/splash.png")` 를 `.Show(false)` 로 표시한다
    - 스플래시 Show 가 `new MainWindow()` 생성 **이전**에 위치한다 (D-01 관리 UI 이전 표시)
    - App.xaml.cs 가 `view.ContentRendered` 를 구독해 `_splash.Close(TimeSpan...)` 로 fade close 한다 (auto-close 미사용)
    - ContentRendered 핸들러가 `[STARTUP-WHITE] (e)` 마커를 출력한다 (Task 1 의 (a)~(d) 와 동일 시간축)
    - splash Show/Close 코드가 try/catch 로 감싸져 실패해도 기동을 막지 않는다
    - 신규 라인에 `//260615 hbk Phase 43.1` 마커 존재
  </acceptance_criteria>
  <done>스플래시 자산 + 즉시 표시(관리 UI 이전) + ContentRendered fade close + (e) 마커 완료. 흰 화면 마스킹 구조 완성, async 리팩토링 없음(D-05).</done>
</task>

<task type="auto">
  <name>Task 3: 통합 빌드 게이트 (Task 1+2 컴파일 검증)</name>
  <files>WPF_Example/DatumMeasurement.csproj</files>
  <read_first>
    - .planning/phases/43-startup-delay-separation/43-01-SUMMARY.md (Task 3 통합 빌드 절차 + Phase 42/43 baseline 6 warning)
  </read_first>
  <action>
    Task 1(계측) + Task 2(스플래시 자산/표시) 통합 후 SIMUL_MODE Debug/x64 로 빌드하여 컴파일 게이트를 통과시킨다. 코드 신규 작성 없음 — 빌드 + 결과 판정만.

    1) msbuild Debug/x64 빌드 실행 (43-01-SUMMARY.md 에서 PASS 확인된 빌드 명령 차용. MSBuild 경로는 환경에서 확인):
    ```powershell
    & "<MSBuild.exe 경로>" WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build
    ```
    2) 결과 확인:
       - 0 Error.
       - 신규 warning 0 (Phase 42/43 baseline 6 warning = CS0618 x4 + CS0162 + MSB3884 유지, 그 외 추가 warning 없음).
       - splash.png 가 Resource 로 어셈블리에 임베드됐는지(빌드 산출물에 포함) 빌드 로그/산출물로 확인.
    3) 만약 `SplashScreen` 경로 로드 관련 컴파일/리소스 경고가 새로 뜨면, csproj 의 splash.png 가 `<Resource>`(Content 아님)로 등록됐는지 재확인.
  </action>
  <verify>
    <automated>msbuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 → "Build succeeded" + "0 Error(s)" + 신규 warning 0 (baseline 6 대비 증가 없음)</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 PASS: 0 errors
    - 신규 warning 0 (Phase 42/43 baseline 6 warning 유지)
    - splash.png 가 Resource 로 빌드 산출물에 임베드됨
  </acceptance_criteria>
  <done>3 파일(App.xaml.cs / MainWindow.xaml.cs / csproj) + splash.png 통합 빌드 PASS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 흰 화면 제거(≤1s 스플래시) + 구간 분해 수치 + 회귀 (첫 로그인 / 첫 $TEST / MainView) — D-06 UAT</name>
  <action>
    런타임 검증 전용 체크포인트(코드 변경 없음). executor 는 빌드 산출물을 실행하고 아래 <how-to-verify> 3항목을 수행한 뒤 사용자에게 결과를 제시하고 정지한다. 스플래시 시각·로그 수치·회귀는 사용자 관측이 필요하므로 executor 가 자체 PASS 판정하지 않는다.
  </action>
  <what-built>
    App 정적 Stopwatch + `[STARTUP-WHITE]` (a)~(e) 흰 화면 구간 분해 계측(Task 1) + 즉시 표시 WPF 네이티브 스플래시(관리 UI 이전, autoClose=false → ContentRendered fade close)로 흰 화면 마스킹(Task 2) + splash.png 자산. 코드 구조/빌드는 Task 1~3 에서 자동 검증 완료. 본 체크포인트는 런타임 체감/수치/회귀(앱 실행 필요, executor 자체 검증 불가)를 확인한다.
  </what-built>
  <how-to-verify>
    SIMUL_MODE(Debug/x64) 빌드된 앱으로 검증한다. 각 항목 결과를 알려주세요.

    1. **흰 화면 제거 — 스플래시 ≤1s 표시 (D-06 a):**
       - 앱을 더블클릭으로 콜드 기동한다.
       - 기대: 기동 직후(≤1s) "DataMeasurement / 기동 중..." 스플래시가 즉시 나타나고, 이전의 18~20초 흰 화면(체감 정지)이 사라진다. 스플래시는 MainWindow 가 실제로 그려질 때(ContentRendered)까지 유지되다가 0.3s fade 로 닫힌다. 스플래시가 너무 일찍 닫혀 흰 화면이 재노출되지 않아야 한다.

    2. **흰 화면 구간 분해 수치 확보 (D-06 b):**
       - 기동 후 Trace 로그(예: D:\Data\Trace\YYYY-MM-DD_Trace.log)에서 `[STARTUP-WHITE]` (a)~(e) 5줄을 확인한다.
       - 기대: (a) App.Startup entry → (b) MainWindow ctor entry → (c) after Initialize → (d) after InitializeComponent → (e) ContentRendered first paint 절대시각이 순서대로 기록되어, 18~20초 중 지배 구간((d)-(c) InitializeComponent inflation vs (c)-(b) Initialize vs (b)-(a) 콜드 JIT 등)이 수치로 드러난다. 기존 `[STARTUP] Total Initialize` 값과 (c)-(b) 가 교차검증된다.
       - 이 수치는 후속 async 리팩토링 phase 의 판단 근거로 기록(메모리/SUMMARY).

    3. **회귀 0 (D-06 c):**
       - (a) 기동 후 첫 로그인 다이얼로그 정상(계정 콤보 채워짐, admin 로그인 성공).
       - (b) 핸들러/목 클라이언트로 기동 직후 첫 `$TEST` 전송 → 검사 정상 수행, `[STARTUP] READY` 의미(첫 $TEST 수용 가능 시점) 보존.
       - (c) MainView(카메라 이미지/검사 결과 화면) 정상 표시.
       - 기대: 스플래시 추가로 인한 기동 흐름 깨짐/NRE/표시 이상 없음.
  </how-to-verify>
  <verify>
    <automated>MISSING — 런타임 human-verify. 스플래시 ≤1s 표시 + [STARTUP-WHITE] (a)~(e) 분해 수치 + 첫 로그인/첫 $TEST/MainView 회귀는 앱 실행 후 사용자 확인.</automated>
  </verify>
  <done>스플래시 ≤1s 표시(흰 화면 제거) + (a)~(e) 구간 분해 수치 확보 + 회귀 (a)(b)(c) PASS, 사용자 approved.</done>
  <resume-signal>"approved" (3항목 모두 PASS) 또는 발견된 이슈(스플래시 미표시/조기 닫힘/수치 누락/회귀 스택트레이스) 기술</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App UI thread ↔ SplashScreen 네이티브 윈도우 | `_splash`(Show: Application_Startup) ↔ Close(ContentRendered 핸들러). 동일 UI thread 이벤트 경계 — 동시성 없음 |
| 외부 핸들러 → MainRun $TEST | 기존 TCP 경계 — 본 phase 에서 신규 입력/엔드포인트 추가 없음 (계측/스플래시만) |

## STRIDE Threat Register

본 phase 는 내부 Windows 데스크톱 앱의 기동 시각 피드백(스플래시) + 계측 로그 추가로, 신규 외부 입력·네트워크 엔드포인트·인증 변경·측정 흐름 변경이 없다(D-05 동작 의미 불변). 표면은 낮음.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43.1-01 | DoS (startup hang) | SplashScreen Show/Close 가 예외/리소스 누락으로 기동 차단 | mitigate | splash Show/Close 를 try/catch 로 감싸 실패 시 `_splash=null` + 에러 로그만 남기고 기동 계속(D-02). ContentRendered 는 첫 paint 후 1회 발생 보장 → Close 무한 대기 불가. splash.png 누락 시 SplashScreen 생성자 예외 흡수 |
| T-43.1-02 | Tampering (race) | `_splash` 가 ContentRendered 전에 다른 경로로 Close | accept | `_splash` Show/Close 모두 동일 UI thread(Application_Startup → ContentRendered 핸들러)에서만 접근, 백그라운드 thread 미관여 → race 불가. Close 후 `_splash=null` 로 중복 Close 방어 |
| T-43.1-03 | Information disclosure | `[STARTUP-WHITE]` 로그가 민감정보 노출 | accept | 로그는 절대시각(ms) 수치만 — 계정/경로/PII 미포함. 기존 `[STARTUP]` 계측과 동일 수준. Trace 로그 파일은 로컬 디스크 한정 |
</threat_model>

<verification>
- 정적(자동, executor 수행): Task 1~2 grep acceptance_criteria 전건 + Task 3 msbuild Debug/x64 0 error / 신규 warning 0.
- 런타임(human-verify, Task 4): 스플래시 ≤1s 표시(흰 화면 제거) + `[STARTUP-WHITE]` (a)~(e) 구간 분해 수치 확보 + 첫 로그인/첫 $TEST/MainView 회귀 0.
- 회귀 가드(D-05): SystemHandler.Initialize `[STARTUP]` Step 1~8 + Total + READY 9줄 무변경(동작 의미·순서·READY 마커 의미 불변), Initialize/InitializeComponent async 재구성 없음.
</verification>

<success_criteria>
- 앱 기동 시 흰 화면 대신 즉시(≤1s) 스플래시 표시 — 18~20초 체감 "멈춤" 제거 (D-01/D-02).
- `[STARTUP-WHITE]` (a)~(e) 마커로 흰 화면 구간이 분해 계측되어 지배 구간이 수치화됨 (D-03).
- 스플래시가 첫 실제 paint(ContentRendered)까지 유지 후 fade close — 조기 닫힘에 의한 흰 화면 재노출 없음 (D-02).
- SystemHandler.Initialize 동작 의미·순서 불변, Phase 43 [STARTUP] READY 마커 의미 보존, async 리팩토링 없음 (D-05).
- 회귀 0: 첫 로그인·첫 $TEST·MainView 정상 (D-06), msbuild PASS.
</success_criteria>

<output>
After completion, create `.planning/phases/43.1-startup-perceived-speed/43.1-01-SUMMARY.md`
</output>
