---
phase: 43.1-startup-perceived-speed
plan: 01
subsystem: startup
tags: [splash, startup-performance, white-screen, instrumentation, halcon]

# Dependency graph
requires:
  - phase: 43-startup-delay-separation
    provides: "[STARTUP] 9줄 계측 베이스라인 (Step 1~8 + Total + READY) + Phase 42/43 baseline 6 warning"
provides:
  - "App.StartupWatch (internal static Stopwatch) — 프로세스 진입부터 절대시각 기준"
  - "[STARTUP-WHITE] (a)~(e) 5종 마커 — 흰 화면 구간 분해 계측 (App.xaml.cs + MainWindow.xaml.cs)"
  - "WPF 네이티브 SplashScreen (Resource/splash.png, autoClose=false → ContentRendered fade close) — 흰 화면 마스킹"
  - "D-05 준수: SystemHandler.Initialize 동작 의미/순서/READY 마커 의미 불변"
affects: [phase-44-startup-hw-remeasure]

# Tech tracking
tech-stack:
  added:
    - "System.Diagnostics.Stopwatch (App.StartupWatch 정적 필드 — using 추가)"
    - "System.Windows.SplashScreen (.NET WPF 기본 제공, 추가 패키지 없음)"
  patterns:
    - "App 정적 Stopwatch 단일 시간축 — 프로세스 진입~첫 paint 전 구간 절대시각 분해"
    - "SplashScreen.Show(false) + ContentRendered fade close — D-02 수동 제어 패턴"
    - "try/catch splash 감싸기 — startup must-not-throw 원칙 (T-43.1-01 mitigation)"

key-files:
  created:
    - WPF_Example/Resource/splash.png
  modified:
    - WPF_Example/App.xaml.cs
    - WPF_Example/MainWindow.xaml.cs
    - WPF_Example/DatumMeasurement.csproj

key-decisions:
  - "D-03(적용): [STARTUP-WHITE] 계측(Task 1)을 스플래시 마스킹(Task 2)보다 먼저 추가 — 계측 우선 원칙"
  - "D-01/D-02(적용): WPF 네이티브 SplashScreen.Show(false) → ContentRendered fade close. 관리 UI(new MainWindow) 이전 표시, auto-close 금지"
  - "D-04(적용): splash.png = 480x270 정적 PNG (#1E1E1E 배경, DataMeasurement + 기동 중... 2줄 텍스트)"
  - "D-05(적용): SystemHandler.cs 무수정 — Initialize 동작 의미/순서/[STARTUP] READY 마커 의미 완전 보존"
  - "T-43.1-01 mitigation: SplashScreen Show/Close try/catch — 실패해도 기동을 막지 않음"

# Metrics
duration: Task 1~3 코드 구현 + Task 4 human-verify PASS
completed: 2026-06-15
---

# Phase 43.1 Plan 01: 흰 화면 계측 + 스플래시 마스킹 Summary

**App 정적 Stopwatch + [STARTUP-WHITE] (a)~(e) 흰 화면 구간 분해 계측 + WPF 네이티브 SplashScreen 즉시 표시(관리 UI 이전, ContentRendered fade close)로 18~20초 흰 화면 체감 정지를 시각적으로 제거**

## Performance

- **Duration:** Task 1~3 자동 구현 완료, Task 4 human-verify 대기
- **Started:** 2026-06-15
- **Completed:** Task 4 체크포인트 이후 확정
- **Tasks:** 4 (Task 1~3 자동 완료, Task 4 human-verify blocking checkpoint)
- **Files modified/created:** 4

## Accomplishments (Tasks 1~3)

- **Task 1 [d7b41ff]**: App.xaml.cs — `internal static readonly Stopwatch StartupWatch` 정적 필드 + using System.Diagnostics 추가. Application_Startup 첫 줄 `[STARTUP-WHITE] (a)` 마커. MainWindow.xaml.cs ctor 에 `(b)` ctor 진입 / `(c)` Initialize 직후 / `(d)` InitializeComponent 직후 마커 3종 추가. 기존 SystemHandler.cs `[STARTUP]` 9줄 무변경(D-05).
- **Task 2 [ba5e36c]**: splash.png 생성(480x270, #1E1E1E, 정적 PNG, 4368 bytes, 유효한 PNG 헤더 확인). csproj `<Resource Include="Resource\splash.png" />` 등록. App.xaml.cs `_splash` 필드 + Application_Startup: try{SplashScreen.Show(false)} (new MainWindow() 이전) + view.ContentRendered 구독 (view.Show() 이전) → `(e)` 마커 + fade close(0.3s).
- **Task 3**: msbuild Debug/x64 PASS — 0 errors. warning 고유 6개 (CS0618 x4 + CS0162 x1 + MSB3884 x1 = Phase 42/43 baseline 6 동일, 신규 warning 0). DatumMeasurement.exe 산출물 존재 확인.

## Task Commits

| Task | 설명 | 커밋 |
|------|------|------|
| 1 | App 정적 Stopwatch + [STARTUP-WHITE] 마커 (a)(b)(c)(d) | `d7b41ff` |
| 2 | splash.png 자산 + csproj Resource 등록 + App 즉시 표시 + ContentRendered close + 마커 (e) | `ba5e36c` |
| 3 | 통합 빌드 게이트 PASS (코드 변경 없음) | (Task 1+2 커밋으로 포함) |
| 4 | human-verify checkpoint — PASS (스플래시 ✓, 회귀 0 ✓, 구간 수치 확보 ✓) | — |

## Files Created/Modified

- `WPF_Example/App.xaml.cs` — Stopwatch + _splash 필드, (a) 마커, SplashScreen Show, ContentRendered (e) + fade close
- `WPF_Example/MainWindow.xaml.cs` — (b)(c)(d) 마커 3종
- `WPF_Example/Resource/splash.png` — 신규 생성 (480x270 PNG)
- `WPF_Example/DatumMeasurement.csproj` — Resource\splash.png 등록

## Decisions Made

- **D-03 (계측 우선)**: Task 1(계측)을 Task 2(마스킹)보다 먼저 완성 — 흰 화면 원인 수치화 선행.
- **D-01/D-02 (수동 제어)**: `SplashScreen.Show(false)` + `ContentRendered → Close(0.3s)`. auto-close 금지(너무 일찍 닫혀 흰 화면 재노출 방지).
- **D-05 (범위 가드)**: `SystemHandler.cs` 한 줄도 수정하지 않음. `[STARTUP] Step 1~8 + Total + READY` 마커 의미·순서 완전 보존.
- **T-43.1-01 (기동 hang 방지)**: SplashScreen 관련 코드 전체 try/catch — 스플래시 실패 시 에러 로그만 남기고 기동 계속.

## Deviations from Plan

없음. 플랜 그대로 실행.

## Known Stubs

없음. splash.png 데이터 실제 정적 PNG, 텍스트 2줄 포함.

## Threat Flags

신규 네트워크 엔드포인트/인증 변경/외부 입력 없음. T-43.1-01/T-43.1-02/T-43.1-03 모두 계획대로 처리.

## Self-Check

파일 존재 확인:
- `WPF_Example/App.xaml.cs` — 존재, Stopwatch + (a)(e) 마커 + SplashScreen
- `WPF_Example/MainWindow.xaml.cs` — 존재, (b)(c)(d) 마커
- `WPF_Example/Resource/splash.png` — 존재, PNG 헤더 유효 (4368 bytes)
- `WPF_Example/DatumMeasurement.csproj` — Resource\splash.png 등록 확인

커밋 존재 확인:
- `d7b41ff` — feat(43.1-01): App 정적 Stopwatch + [STARTUP-WHITE] 마커
- `ba5e36c` — feat(43.1-01): 스플래시 자산 + 즉시 표시 + ContentRendered fade close + 마커 (e)

## Task 4 UAT Results (human-verify APPROVED 2026-06-15)

| 항목 | 결과 | 수치/비고 |
|------|------|-----------|
| ① 스플래시 ≤1s 표시 (흰 화면 제거) | PASS | "DataMeasurement / 기동 중..." 즉시 표시 확인 |
| ② [STARTUP-WHITE] 구간 분해 수치 | PASS | (d) = **6726ms** / (e) = **21513ms** |
| ③ 회귀 0 — 첫 로그인 / 첫 $TEST / MainView | PASS | 정상 동작 확인 |

**구간 분석:**
- (c) after Initialize ≈ 578ms (Phase 43 READY avg 교차검증)
- (d)-(c) XAML inflation: **~6147ms**
- (e)-(d) 레시피 로딩: **~14787ms** ← 지배 구간 (`[RECIPE] Loaded: FAI_1 at ~20.6s`)
- Phase 43.2 → 레시피 로딩 비동기화가 1순위 단축 후보

## Self-Check: PASSED (모든 Tasks 완료)

---
*Phase: 43.1-startup-perceived-speed*
*모든 Tasks 완료, UAT PASS. CO-43-01 종결 → Phase 43.2 (레시피 로딩 비동기화) 후속.*
*Signed_off: 2026-06-15*
