---
phase: 43.2-startup-recipe-async
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/SystemHandler.cs
autonomous: true
requirements:
  - CO-43-01

must_haves:
  truths:
    - "SystemHandler 에 volatile bool _isRecipeReady 필드가 존재한다"
    - "IsRecipeReady public 프로퍼티가 _isRecipeReady 를 래핑한다"
    - "[STARTUP-WHITE] (f) 마커가 레시피 로드 시작 시점에 기록된다"
    - "[STARTUP-WHITE] (g) 마커가 레시피 로드 완료(또는 실패) 시점에 기록된다"
  artifacts:
    - path: "WPF_Example/SystemHandler.cs"
      provides: "IsRecipeReady 프로퍼티 + _isRecipeReady volatile 필드 + LoadRecipe 마커 2종"
      contains: "_isRecipeReady"
  key_links:
    - from: "WPF_Example/Custom/SystemHandler.cs ProcessTest"
      to: "SystemHandler.IsRecipeReady"
      via: "프로퍼티 참조"
      pattern: "IsRecipeReady"
---

<objective>
SystemHandler 에 레시피 준비 상태 신호 필드를 추가하고, LoadRecipe 에 [STARTUP-WHITE] (f)/(g) 계측 마커를 삽입한다.

Purpose: Wave 2(MainWindow 비동기 이동)와 Wave 3(TCP guard)가 참조하는 준비 상태 계약을 먼저 정의한다. 인터페이스 우선 원칙.
Output: `volatile bool _isRecipeReady` + `IsRecipeReady` 프로퍼티 + `LoadRecipe` 마커 2종 (f)(g).
</objective>

<execution_context>
@C:/Users/tech/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Info/Project/DataMeasurement/.planning/PROJECT.md
@C:/Info/Project/DataMeasurement/.planning/ROADMAP.md

<!-- Phase 43.1 계측 결과: (d)=6726ms, (e)=21513ms, 레시피 로딩 ~14787ms -->
@C:/Info/Project/DataMeasurement/.planning/phases/43.1-startup-perceived-speed/43.1-01-SUMMARY.md
@C:/Info/Project/DataMeasurement/.planning/phases/43.2-startup-recipe-async/43.2-PATTERNS.md

<interfaces>
<!-- SystemHandler.cs 현재 필드 선언 위치 (lines 60-72) -->
// WPF_Example/SystemHandler.cs (lines 60-72)
public LocalizationResource Localize { get; set; }

private Thread mSystemThread;
private bool IsTerminated = false;

public bool IsInitializeFail { get; private set; } = false;
public bool IsReleased { get; private set; } = false;

// LoadRecipe (lines 185-197) — 수정 대상
public bool LoadRecipe(string recipeName) {
    bool result = Sequences.LoadRecipe(recipeName, ERecipeFileType.Ini);
    if (result) {
        Setting.Save();
        Logging.PrintLog((int)ELogType.Trace, "[RECIPE] Loaded : {0}", recipeName);
    }
    else {
        Logging.PrintLog((int)ELogType.Trace, "[RECIPE] Load fail : {0}", recipeName);
    }
    return result;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SystemHandler — _isRecipeReady 필드 + IsRecipeReady 프로퍼티 선언</name>
  <files>WPF_Example/SystemHandler.cs</files>
  <read_first>
    - WPF_Example/SystemHandler.cs lines 60-75 (IsInitializeFail 선언 위치 — 같은 블록에 추가)
  </read_first>
  <action>
`IsReleased` 선언(line 71) 바로 아래에 아래 두 줄을 추가한다:

```csharp
//260615 hbk Phase 43.2: 레시피 비동기 로드 완료 신호 — ProcessTest guard 참조용 (D-B)
private volatile bool _isRecipeReady = false;
public bool IsRecipeReady { get { return _isRecipeReady; } set { _isRecipeReady = value; } }
```

`volatile bool` 선택 이유: UI 스레드(Dispatcher.Background)에서 쓰기, MainRun 폴링 스레드(1ms)에서 읽기 → 가시성 보장 필요. RawImageSaveService._isStopping 과 동일 패턴.

**주의:** 이 필드는 Wave 1 에서 `false` 초기값이다. `true` 로의 쓰기는 Wave 2 의 MainWindow 핸들러에서 LoadRecipe 완료 후 수행한다.
  </action>
  <verify>
    <automated>grep -n "_isRecipeReady" C:/Info/Project/DataMeasurement/WPF_Example/SystemHandler.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "_isRecipeReady" WPF_Example/SystemHandler.cs` → 2줄 이상 (필드 선언 + 프로퍼티 getter)
    - `grep "volatile bool _isRecipeReady" WPF_Example/SystemHandler.cs` → 1줄 정확히 매칭
    - `grep "IsRecipeReady" WPF_Example/SystemHandler.cs` → 프로퍼티 선언 포함 1줄 이상
    - `grep "260615 hbk Phase 43.2" WPF_Example/SystemHandler.cs` → 주석 존재
  </acceptance_criteria>
  <done>SystemHandler.cs 에 `volatile bool _isRecipeReady = false` + `IsRecipeReady` 프로퍼티 선언 완료. Wave 2/3 가 이 프로퍼티를 참조 가능.</done>
</task>

<task type="auto">
  <name>Task 2: LoadRecipe — (f) 시작 / (g) 완료 계측 마커 삽입</name>
  <files>WPF_Example/SystemHandler.cs</files>
  <read_first>
    - WPF_Example/SystemHandler.cs lines 185-197 (LoadRecipe 전체 본문)
    - WPF_Example/App.xaml.cs lines 23-25 (App.StartupWatch 참조 방법 확인)
    - WPF_Example/MainWindow.xaml.cs lines 76-84 ([STARTUP-WHITE] 마커 형식 확인)
  </read_first>
  <action>
`LoadRecipe` 메서드(lines 185-197)를 아래와 같이 수정한다:

```csharp
public bool LoadRecipe(string recipeName) {
    //260615 hbk Phase 43.2: [STARTUP-WHITE] (f) — 레시피 로드 시작. Dispatcher.Background 지연 후 실제 실행 시점 계측 (D-D)
    Logging.PrintLog((int)ELogType.Trace, "[STARTUP-WHITE] (f) recipe load start: {0} ms", App.StartupWatch.ElapsedMilliseconds);

    // Delegate recipe load to sequence handler.
    // ERecipeFileType.Ini implies recipe config stored in INI format.
    bool result = Sequences.LoadRecipe(recipeName, ERecipeFileType.Ini);
    if (result) {
        Setting.Save();
        Logging.PrintLog((int)ELogType.Trace, "[RECIPE] Loaded : {0}", recipeName);
    }
    else {
        Logging.PrintLog((int)ELogType.Trace, "[RECIPE] Load fail : {0}", recipeName);
    }

    //260615 hbk Phase 43.2: [STARTUP-WHITE] (g) — 레시피 로드 완료(성공/실패 무관). 창 표시(e)~레시피 완료(g) 구간 = 비동기 지연 확인 (D-D)
    Logging.PrintLog((int)ELogType.Trace, "[STARTUP-WHITE] (g) recipe load done (result={0}): {1} ms", result, App.StartupWatch.ElapsedMilliseconds);
    return result;
}
```

`App.StartupWatch` 는 App.xaml.cs 에 `internal static readonly Stopwatch` 로 선언되어 있다. SystemHandler.cs 는 같은 `ReringProject` 네임스페이스에 있으므로 `using` 추가 없이 `App.StartupWatch` 직접 참조 가능.

기존 `[RECIPE] Loaded` / `[RECIPE] Load fail` 로그는 그대로 보존한다.
  </action>
  <verify>
    <automated>grep -n "STARTUP-WHITE" C:/Info/Project/DataMeasurement/WPF_Example/SystemHandler.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "[STARTUP-WHITE].*\(f\)" WPF_Example/SystemHandler.cs` → LoadRecipe 본문 첫 줄에 존재
    - `grep "[STARTUP-WHITE].*\(g\)" WPF_Example/SystemHandler.cs` → return 직전에 존재
    - `grep "App.StartupWatch" WPF_Example/SystemHandler.cs` → 2줄 (f + g 마커)
    - 기존 `[RECIPE] Loaded` / `[RECIPE] Load fail` 로그 줄 보존 (`grep "RECIPE.*Loaded" WPF_Example/SystemHandler.cs` → 1줄)
    - `grep "260615 hbk Phase 43.2" WPF_Example/SystemHandler.cs` → Task 1 + Task 2 합산 3줄 이상
  </acceptance_criteria>
  <done>LoadRecipe 에 (f) 시작 + (g) 완료 마커 삽입 완료. 기존 로그 보존. App.StartupWatch 참조로 절대시각 기록.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 내부 필드 읽기/쓰기 | `_isRecipeReady` 는 UI 스레드(쓰기)와 MainRun 폴링 스레드(읽기)가 접근 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43.2-01 | Information Disclosure | _isRecipeReady 공개 프로퍼티 | accept | IsRecipeReady 는 내부 상태 노출이지만 UI 바인딩 목적 없음; 프로세스 내부 읽기 전용 용도 |
| T-43.2-02 | Denial of Service | _isRecipeReady=false 중 TCP TEST 수신 | mitigate | Wave 3 (Plan 03) 의 ProcessTest guard 로 NG 반환 — Plan 01 의 필드 선언이 선행 조건 |
</threat_model>

<verification>
빌드 게이트:
```
cd C:/Info/Project/DataMeasurement
msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build 2>&1 | tail -5
```
- 0 errors 확인
- 신규 warning 0 (baseline: CS0618 x4 + CS0162 x1 + MSB3884 x1 = 6개 동일)
</verification>

<success_criteria>
- SystemHandler.cs 에 `volatile bool _isRecipeReady = false` 필드 선언 존재
- `IsRecipeReady` public 프로퍼티 getter/setter 존재
- `LoadRecipe` 본문에 `[STARTUP-WHITE] (f)` 시작 마커 존재 (return 이전 최초 줄)
- `LoadRecipe` 본문에 `[STARTUP-WHITE] (g)` 완료 마커 존재 (return 직전)
- `msbuild` 0 errors
- Wave 2 (43.2-02-PLAN.md) 가 `IsRecipeReady = true` 쓰기 및 `Dispatcher.BeginInvoke(Background)` 이동 가능한 상태
</success_criteria>

<output>
완료 후 `.planning/phases/43.2-startup-recipe-async/43.2-01-SUMMARY.md` 생성.
</output>
