---
plan: 43.2-01
phase: 43.2-startup-recipe-async
status: complete
wave: 1
completed: 2026-06-15
commits:
  - 624c778
---

# Plan 43.2-01 Summary: SystemHandler IsRecipeReady 신호 필드 + LoadRecipe 마커

## What Was Built

Wave 2(MainWindow 비동기 이동)와 Wave 3(TCP guard)가 참조하는 준비 상태 계약을 정의했다.

1. `volatile bool _isRecipeReady = false` + `IsRecipeReady` public 프로퍼티 (line 73-74)
2. `LoadRecipe` 에 `[STARTUP-WHITE] (f)` 시작 마커 + `[STARTUP-WHITE] (g)` 완료 마커 삽입

## Files Modified

- `WPF_Example/SystemHandler.cs` — 필드 선언 2줄 + 마커 2줄 + 주석 2줄 (총 +9 lines)

## Key Decisions

- `volatile bool` 채택: UI 스레드(쓰기)와 MainRun 폴링 스레드(읽기) 간 가시성 보장 (RawImageSaveService._isStopping 동일 패턴)
- `App.StartupWatch` 직접 참조: 동일 네임스페이스(ReringProject) — using 추가 0

## Self-Check: PASSED

- `grep "_isRecipeReady"` → 2줄 (필드 + 프로퍼티)
- `grep "volatile bool _isRecipeReady"` → 1줄 정확히 매칭
- `grep "STARTUP-WHITE.*\(f\)"` → 1줄
- `grep "STARTUP-WHITE.*\(g\)"` → 1줄
- Wave 2 (43.2-02) 가 `IsRecipeReady = true` 쓰기 가능한 상태
