---
phase: 43.2-startup-recipe-async
plan: 02
type: execute
wave: 2
depends_on:
  - 43.2-01
files_modified:
  - WPF_Example/MainWindow.xaml.cs
autonomous: true
requirements:
  - CO-43-01

must_haves:
  truths:
    - "Window_Loaded 에서 LoadRecipe 동기 호출이 제거된다"
    - "ContentRendered 이후 Dispatcher.BeginInvoke(Background)로 LoadRecipe 가 실행된다"
    - "LoadRecipe 완료 후 IsRecipeReady = true 가 설정된다"
    - "OnLoadRecipe 핸들러가 Dispatcher.BeginInvoke 로 래핑된다 (향후 스레드 안전)"
  artifacts:
    - path: "WPF_Example/MainWindow.xaml.cs"
      provides: "Window_Loaded 동기 LoadRecipe 제거 + ContentRendered 핸들러 + BeginInvoke(Background)"
      contains: "Window_ContentRendered_LoadRecipe"
  key_links:
    - from: "App.xaml.cs ContentRendered 구독 (line 55)"
      to: "MainWindow.xaml.cs ContentRendered 핸들러"
      via: "이벤트 += 순서 (App 가 먼저, Window_Loaded 가 나중 — 모두 UI 스레드)"
      pattern: "ContentRendered"
    - from: "Window_ContentRendered_LoadRecipe"
      to: "mSystemHandler.LoadRecipe"
      via: "Dispatcher.BeginInvoke(DispatcherPriority.Background)"
      pattern: "BeginInvoke.*Background.*LoadRecipe"
---

<objective>
MainWindow.Window_Loaded 에서 14787ms 블로킹 LoadRecipe 호출을 제거하고, ContentRendered 이후 `Dispatcher.BeginInvoke(DispatcherPriority.Background)` 로 이동한다.

Purpose: ContentRendered(첫 paint, ~6726ms)에서 스플래시가 닫히고 사용자가 창을 보게 된 뒤 레시피를 로드하여 체감 기동 시간을 ~14787ms 단축한다.
Output: Window_Loaded 동기 블로킹 제거 + Window_ContentRendered_LoadRecipe 핸들러 신설 + IsRecipeReady=true 쓰기.
</objective>

<execution_context>
@C:/Users/tech/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Info/Project/DataMeasurement/.planning/PROJECT.md
@C:/Info/Project/DataMeasurement/.planning/ROADMAP.md

<!-- Wave 1 결과: IsRecipeReady 프로퍼티 + LoadRecipe (f)/(g) 마커 완성 -->
@C:/Info/Project/DataMeasurement/.planning/phases/43.2-startup-recipe-async/43.2-01-SUMMARY.md
@C:/Info/Project/DataMeasurement/.planning/phases/43.2-startup-recipe-async/43.2-PATTERNS.md

<interfaces>
<!-- MainWindow.xaml.cs 현재 Window_Loaded (lines 361-376) — 수정 대상 -->
// WPF_Example/MainWindow.xaml.cs (lines 361-376)
private void Window_Loaded(object sender, RoutedEventArgs e) {
    CustomMessageBox.Parent = this;
    mTimer.Start();

    //register custom ui
    RegisterCustomUI();

    //load recipe
    if (mSystemHandler.Setting.CurrentRecipeName != null)
    {
        mSystemHandler.LoadRecipe(mSystemHandler.Setting.CurrentRecipeName);
    }

    IsEditable = false;
}

// OnLoadRecipe (lines 253-256) — Dispatcher 래핑 추가 대상
public void OnLoadRecipe(object sender, RecipeChangedEventArgs args) {
    //args.RecipeName;
    inspectionList.OnLoadRecipe(args.RecipeName);
}

<!-- App.xaml.cs ContentRendered 구독 (lines 55-61) — 이미 존재, 충돌 없음 -->
view.ContentRendered += (s, ev) => {
    Logging.PrintLog((int)ELogType.Trace, "[STARTUP-WHITE] (e) ContentRendered first paint: ...");
    if (_splash != null) { _splash.Close(TimeSpan.FromSeconds(0.3)); _splash = null; }
};

<!-- MainWindow 기존 Dispatcher.BeginInvoke 패턴 (lines 141-145) — 동일 패턴 적용 -->
private void OnSequenceStart(SequenceContext context) {
    Dispatcher.BeginInvoke(System.Windows.Threading.DispatcherPriority.Normal, new Action(() => {
        mainView.SetManualToolsEnabled(false);
        statusBar.Model.SetText(string.Format("[DIAG] Start fired: {0}", context.Source.Name));
    }));
}

<!-- Wave 1 에서 추가된 IsRecipeReady 프로퍼티 -->
// SystemHandler.IsRecipeReady { get; set; }  — volatile bool _isRecipeReady 래퍼
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Window_Loaded 동기 LoadRecipe 제거 + ContentRendered 핸들러 신설</name>
  <files>WPF_Example/MainWindow.xaml.cs</files>
  <read_first>
    - WPF_Example/MainWindow.xaml.cs lines 361-376 (Window_Loaded 전체 — 제거 대상 블록 확인)
    - WPF_Example/MainWindow.xaml.cs lines 75-102 (ctor — using/namespace 블록 확인)
    - WPF_Example/App.xaml.cs lines 53-63 (ContentRendered 구독 패턴 — 동일 이벤트 중복 구독 확인)
  </read_first>
  <action>
**Step 1 — Window_Loaded 수정:**

`Window_Loaded` 에서 `//load recipe` 블록(3줄) 을 제거하고, ContentRendered 1회 구독을 추가한다:

```csharp
private void Window_Loaded(object sender, RoutedEventArgs e) {
    CustomMessageBox.Parent = this;
    mTimer.Start();

    //register custom ui
    RegisterCustomUI();

    //260615 hbk Phase 43.2: 레시피 로드를 ContentRendered(첫 paint) 이후 Dispatcher.Background 로 후퇴 (D-A)
    //  기존 동기 LoadRecipe(14787ms 블로킹) 제거 → ContentRendered(~6726ms) 후 실행으로 체감 시간 단축
    this.ContentRendered += Window_ContentRendered_LoadRecipe;

    IsEditable = false;
}
```

제거되는 코드:
```csharp
//load recipe
if (mSystemHandler.Setting.CurrentRecipeName != null)
{
    mSystemHandler.LoadRecipe(mSystemHandler.Setting.CurrentRecipeName);
}
```

**Step 2 — Window_ContentRendered_LoadRecipe 신설:**

`Window_Loaded` 메서드 바로 다음(line 377 이후, `Window_Closing` 이전)에 새 메서드를 추가한다:

```csharp
//260615 hbk Phase 43.2: ContentRendered(첫 paint) 이후 레시피를 Background 우선순위로 로드 (D-A/D-B/D-D)
//  App.xaml.cs ContentRendered 핸들러(스플래시 close)와 동일 이벤트 — 순서는 구독 순으로 App 먼저, 여기서 나중.
//  Dispatcher.BeginInvoke(Background): UI 스레드에서 실행하되 렌더링 유휴 시점으로 지연.
//  LoadPhase6Format 내 CustomMessageBox.Show 가 존재하므로 별도 Thread 사용 불가 (PATTERNS.md 결론).
private void Window_ContentRendered_LoadRecipe(object sender, EventArgs e) {
    this.ContentRendered -= Window_ContentRendered_LoadRecipe; // 1회 실행 후 구독 해제
    if (mSystemHandler.Setting.CurrentRecipeName == null) {
        mSystemHandler.IsRecipeReady = true; // 레시피 없어도 guard 해제
        return;
    }
    Dispatcher.BeginInvoke(System.Windows.Threading.DispatcherPriority.Background, new Action(() => {
        mSystemHandler.LoadRecipe(mSystemHandler.Setting.CurrentRecipeName);
        mSystemHandler.IsRecipeReady = true; //260615 hbk Phase 43.2: 로드 완료(성공/실패 무관) → TCP guard 해제 (D-B)
    }));
}
```

**App.xaml.cs ContentRendered 충돌 없음:**
App.xaml.cs 의 `view.ContentRendered += (s, ev) => { ... }` 는 스플래시를 닫는다 (line 55). 이 메서드는 레시피를 로드한다. 동일 이벤트에 구독자 2개 = WPF 표준 멀티캐스트 델리게이트. 호출 순서: App 이 `view.ContentRendered +=` 를 `view.Show()` 이전 (line 55, view 생성 직후)에 구독, `Window_ContentRendered_LoadRecipe` 구독은 `Window_Loaded` 이벤트(Show 이후) 에서 실행되므로 App 핸들러가 먼저 호출되고 스플래시가 먼저 닫힌다. 순서 충돌 없음.
  </action>
  <verify>
    <automated>grep -n "Window_ContentRendered_LoadRecipe\|BeginInvoke.*Background\|IsRecipeReady" C:/Info/Project/DataMeasurement/WPF_Example/MainWindow.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Window_ContentRendered_LoadRecipe" WPF_Example/MainWindow.xaml.cs` → 3줄 (Window_Loaded 에서 += 구독 / 메서드 정의 내 -= 해제 / 메서드 시그니처)
    - `grep "BeginInvoke.*Background" WPF_Example/MainWindow.xaml.cs` → 1줄 이상 (Window_ContentRendered_LoadRecipe 내부)
    - `grep "IsRecipeReady = true" WPF_Example/MainWindow.xaml.cs` → 2줄 (레시피 없을 때 폴백 + BeginInvoke 내부 완료 후)
    - `grep "mSystemHandler.LoadRecipe.*CurrentRecipeName" WPF_Example/MainWindow.xaml.cs` 에서 Window_Loaded 본문(line ~361-376) 에 LoadRecipe 직접 호출 없음
    - `grep "260615 hbk Phase 43.2" WPF_Example/MainWindow.xaml.cs` → 주석 2개 이상 (Window_Loaded 변경 + 핸들러 메서드)
  </acceptance_criteria>
  <done>Window_Loaded 에서 동기 LoadRecipe 제거 완료. ContentRendered 이후 BeginInvoke(Background)로 레시피 로드 이동. 레시피 없는 경우 즉시 IsRecipeReady=true 설정.</done>
</task>

<task type="auto">
  <name>Task 2: OnLoadRecipe 핸들러 Dispatcher 래핑 + 빌드 게이트</name>
  <files>WPF_Example/MainWindow.xaml.cs</files>
  <read_first>
    - WPF_Example/MainWindow.xaml.cs lines 253-256 (OnLoadRecipe 현재 코드)
    - WPF_Example/MainWindow.xaml.cs lines 141-145 (OnSequenceStart — Dispatcher.BeginInvoke Normal 패턴)
    - C:/Info/Project/DataMeasurement/.planning/phases/43.2-startup-recipe-async/43.2-PATTERNS.md lines 248-274 (OnLoadRecipe Dispatcher 래핑 분석)
  </read_first>
  <action>
`OnLoadRecipe` (lines 253-256) 를 Dispatcher.BeginInvoke(Normal) 로 래핑한다.

**현재:**
```csharp
public void OnLoadRecipe(object sender, RecipeChangedEventArgs args) {
    //args.RecipeName;
    inspectionList.OnLoadRecipe(args.RecipeName);
}
```

**수정 후:**
```csharp
//260615 hbk Phase 43.2: OnLoadRecipe — Dispatcher.BeginInvoke 래핑 (D-A 비동기 방식 대비 스레드 안전)
//  Dispatcher.BeginInvoke(Background) 방식은 UI 스레드 실행이라 현재 래핑 불필요하나,
//  향후 별도 Thread 방식 전환 시 이 핸들러가 비 UI 스레드에서 호출될 수 있으므로 방어적 래핑.
//  inspectionList.OnLoadRecipe → ViewModel.RebuildTree() → UI 컨트롤 접근 → UI 스레드 필요.
public void OnLoadRecipe(object sender, RecipeChangedEventArgs args) {
    Dispatcher.BeginInvoke(System.Windows.Threading.DispatcherPriority.Normal, new Action(() => {
        inspectionList.OnLoadRecipe(args.RecipeName);
    }));
}
```

**이후 — 빌드 게이트:**

아래 명령을 실행하고 결과를 확인한다:
```
msbuild C:/Info/Project/DataMeasurement/WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /verbosity:minimal 2>&1
```

성공 기준: `0 Error(s)`. warning 수 = 기존 baseline 6개 ± 0 (신규 warning 없음).
  </action>
  <verify>
    <automated>grep -n "Dispatcher.BeginInvoke.*Normal.*new Action" C:/Info/Project/DataMeasurement/WPF_Example/MainWindow.xaml.cs | grep -i "OnLoadRecipe\|inspectionList"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A3 "public void OnLoadRecipe" WPF_Example/MainWindow.xaml.cs` → 본문에 `Dispatcher.BeginInvoke` 포함
    - `grep "inspectionList.OnLoadRecipe" WPF_Example/MainWindow.xaml.cs` → BeginInvoke 람다 내부에 위치 (들여쓰기 패턴 확인)
    - msbuild 출력에 `0 Error(s)` 포함
    - msbuild 출력의 `Warning(s)` 수 <= 6 (신규 warning 없음)
    - `grep "260615 hbk Phase 43.2" WPF_Example/MainWindow.xaml.cs` → Task 1 + Task 2 합산 3줄 이상
  </acceptance_criteria>
  <done>OnLoadRecipe Dispatcher 래핑 완료. 빌드 0 errors. Phase 43.2 핵심 코드 변경 완료 — Wave 3(TCP guard + UAT)으로 진행 가능.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ContentRendered 이벤트 구독 순서 | App.xaml.cs 구독(스플래시 close) vs MainWindow.Window_Loaded 구독(레시피 로드) — 순서 의존성 |
| Dispatcher.BeginInvoke(Background) | UI 스레드 실행, 렌더링 유휴 시 실행 — 예외 발생 시 Dispatcher.UnhandledException 로 흡수됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43.2-03 | Denial of Service | ContentRendered 이벤트 1회 보장 누락 | mitigate | `this.ContentRendered -= Window_ContentRendered_LoadRecipe` 로 1회 실행 후 즉시 구독 해제 |
| T-43.2-04 | Denial of Service | 레시피 null 케이스에서 IsRecipeReady 미설정 | mitigate | `CurrentRecipeName == null` 분기에서도 `IsRecipeReady = true` 로 TCP guard 해제 |
| T-43.2-05 | Elevation of Privilege | BeginInvoke 내 LoadRecipe 예외가 Dispatcher.UnhandledException 로 노출 | accept | App.xaml.cs Dispatcher_UnhandledException 가 이미 전역 처리 — 기존 패턴과 동일 위험 수준 |
</threat_model>

<verification>
1. `grep -n "Window_ContentRendered_LoadRecipe" WPF_Example/MainWindow.xaml.cs` — 3줄 이상
2. `grep -n "BeginInvoke.*Background" WPF_Example/MainWindow.xaml.cs` — 1줄
3. `grep -n "IsRecipeReady = true" WPF_Example/MainWindow.xaml.cs` — 2줄 (null 폴백 + BeginInvoke 완료)
4. `msbuild ... /t:Build` → `0 Error(s)`
</verification>

<success_criteria>
- Window_Loaded 에서 동기 `mSystemHandler.LoadRecipe` 호출 라인 0개
- `Window_ContentRendered_LoadRecipe` 메서드 존재 (시그니처: `private void Window_ContentRendered_LoadRecipe(object sender, EventArgs e)`)
- `Dispatcher.BeginInvoke(DispatcherPriority.Background, ...)` 내부에서 `LoadRecipe` + `IsRecipeReady = true` 순서
- `OnLoadRecipe` 내부가 `Dispatcher.BeginInvoke(Normal, ...)` 로 래핑됨
- msbuild 0 errors
</success_criteria>

<output>
완료 후 `.planning/phases/43.2-startup-recipe-async/43.2-02-SUMMARY.md` 생성.
</output>
