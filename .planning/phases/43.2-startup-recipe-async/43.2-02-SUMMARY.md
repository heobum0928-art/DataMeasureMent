---
plan: 43.2-02
phase: 43.2-startup-recipe-async
status: complete
wave: 2
completed: 2026-06-15
commits:
  - b78b22a
---

# Plan 43.2-02 Summary: MainWindow 비동기 레시피 로드 이동

## What Was Built

ContentRendered(첫 paint, ~6726ms) 이후로 14787ms 블로킹 LoadRecipe를 후퇴시켰다.

1. **Window_Loaded**: `//load recipe` 블록(동기 LoadRecipe 3줄) 제거 → `ContentRendered += Window_ContentRendered_LoadRecipe` 구독으로 교체
2. **Window_ContentRendered_LoadRecipe** (신설): 1회 해제 + null 분기 IsRecipeReady=true + BeginInvoke(Background, LoadRecipe + IsRecipeReady=true)
3. **OnLoadRecipe**: Dispatcher.BeginInvoke(Normal) 방어적 래핑 (향후 별도 Thread 전환 대비)

## Files Modified

- `WPF_Example/MainWindow.xaml.cs` — +26 lines / -8 lines

## Key Decisions

- `DispatcherPriority.Background`: UI 스레드에서 실행하되 렌더링 유휴 시 실행 → 창 표시 블로킹 0
- App.xaml.cs ContentRendered(스플래시 close)가 먼저 구독됐으므로 App→Window 순서 보장
- CustomMessageBox.Show 포함으로 별도 Thread 방식 불가 → Dispatcher.Background 방식 확정(PATTERNS.md)

## Self-Check: PASSED

- `grep "Window_ContentRendered_LoadRecipe"` → 3줄 (Window_Loaded += / 메서드 내 -= / 메서드 시그니처)
- `grep "BeginInvoke.*Background"` → 1줄
- `grep "IsRecipeReady = true"` → 2줄 (null 폴백 + BeginInvoke 내부)
- Window_Loaded 본문에 `mSystemHandler.LoadRecipe.*CurrentRecipeName` 직접 호출 없음
- msbuild 0 errors, 6 warnings = baseline
