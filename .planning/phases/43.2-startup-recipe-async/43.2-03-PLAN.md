---
phase: 43.2-startup-recipe-async
plan: 03
type: execute
wave: 3
depends_on:
  - 43.2-01
  - 43.2-02
files_modified:
  - WPF_Example/Custom/SystemHandler.cs
autonomous: false
requirements:
  - CO-43-01

must_haves:
  truths:
    - "ProcessTest 가 IsRecipeReady=false 일 때 TCP TEST 를 거부하고 NG 응답을 반환한다"
    - "ContentRendered(~6726ms) 이후 창이 즉시 사용자에게 표시된다"
    - "레시피 로드 완료(~21513ms) 이후 TCP TEST 가 정상 수용된다"
    - "기존 검사 흐름 회귀가 없다 (첫 로그인, 첫 $TEST, MainView 정상)"
  artifacts:
    - path: "WPF_Example/Custom/SystemHandler.cs"
      provides: "ProcessTest IsRecipeReady guard"
      contains: "IsRecipeReady"
  key_links:
    - from: "Custom/SystemHandler.cs ProcessTest"
      to: "SystemHandler.IsRecipeReady"
      via: "if (!IsRecipeReady) return false → SendTestError"
      pattern: "!IsRecipeReady"
---

<objective>
`Custom/SystemHandler.cs` `ProcessTest` 에 `IsRecipeReady` guard 를 추가하고, UAT(SIMUL) 로 Before/After 수치 비교 + TCP TEST 거부 동작 + 회귀 0을 검증한다.

Purpose: 레시피 로드 전 TCP TEST 수신 시 NG 거부 응답으로 핸들러가 오작동하지 않도록 보호. UAT 로 Phase 43.2 목표(~6726ms 창 표시) 달성을 입증.
Output: ProcessTest guard 코드 + UAT 체크포인트 PASS.
</objective>

<execution_context>
@C:/Users/tech/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Info/Project/DataMeasurement/.planning/PROJECT.md
@C:/Info/Project/DataMeasurement/.planning/ROADMAP.md

@C:/Info/Project/DataMeasurement/.planning/phases/43.2-startup-recipe-async/43.2-01-SUMMARY.md
@C:/Info/Project/DataMeasurement/.planning/phases/43.2-startup-recipe-async/43.2-02-SUMMARY.md
@C:/Info/Project/DataMeasurement/.planning/phases/43.2-startup-recipe-async/43.2-PATTERNS.md

<interfaces>
<!-- Custom/SystemHandler.cs ProcessTest (lines 232-241) — 수정 대상 -->
// WPF_Example/Custom/SystemHandler.cs (lines 232-241)
//260409 hbk Phase 5: IsDynamicFAIMode 분기 (D-03)
private bool ProcessTest(TestPacket packet) {
    if (Sequences.IsDynamicFAIMode) {
        string seqName = packet.Identifier;
        SequenceBase seq = Sequences[seqName];
        if (seq == null) return false;
        return seq.StartAll(packet);
    }
    return Sequences.Start(packet);
}

// SendTestError (lines 243-253) — 거부 응답 생성 메서드 (이미 존재)
private TestResultPacket SendTestError(TestPacket packet) {
    TestResultPacket resultPacket = new TestResultPacket();
    TestPacket sendPacket = packet.AsTest();
    resultPacket.Target = sendPacket.Sender;
    resultPacket.Site = sendPacket.Site;
    resultPacket.InspectionType = sendPacket.TestType;
    resultPacket.Result = EVisionResultType.NG;
    return resultPacket;
}

// MainRun VisionRequestType.Test 처리 (lines 51-58) — ProcessTest false 반환 시 SendTestError 호출됨
case VisionRequestType.Test:
    if (Setting.AutoLogoutWhenRecvTest && Login.IsLogin) { Login.LogOut(); }
    if (!ProcessTest(packet.AsTest())) {
        Logging.PrintLog(..., "Client {0} : Fail to Start Sequence...");
        responsePacket = SendTestError(packet.AsTest());
    }
    break;

// Wave 1 에서 추가된 IsRecipeReady — SystemHandler.IsRecipeReady { get; set; }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProcessTest — IsRecipeReady guard 추가</name>
  <files>WPF_Example/Custom/SystemHandler.cs</files>
  <read_first>
    - WPF_Example/Custom/SystemHandler.cs lines 232-255 (ProcessTest + SendTestError 전체)
    - WPF_Example/SystemHandler.cs lines 68-72 (IsInitializeFail 선언 — 동일 guard 패턴 참조)
  </read_first>
  <action>
`ProcessTest` 메서드(lines 232-241) 첫 줄에 `IsRecipeReady` guard 를 추가한다.

**현재:**
```csharp
//260409 hbk Phase 5: IsDynamicFAIMode 분기 (D-03)
private bool ProcessTest(TestPacket packet) {
    if (Sequences.IsDynamicFAIMode) {
        string seqName = packet.Identifier;
        SequenceBase seq = Sequences[seqName];
        if (seq == null) return false;
        return seq.StartAll(packet);
    }
    return Sequences.Start(packet);
}
```

**수정 후:**
```csharp
//260409 hbk Phase 5: IsDynamicFAIMode 분기 (D-03)
//260615 hbk Phase 43.2: IsRecipeReady guard — 레시피 비동기 로드 완료 전 TEST 수신 시 NG 거부 (D-C)
//  IsInitializeFail 패턴과 동일. false 반환 → MainRun 에서 SendTestError(NG) 응답.
private bool ProcessTest(TestPacket packet) {
    if (!IsRecipeReady) {
        Logging.PrintLog((int)ELogType.Error, "[RECIPE] TEST rejected — recipe not yet loaded (IsRecipeReady=false)");
        return false;
    }
    if (Sequences.IsDynamicFAIMode) {
        string seqName = packet.Identifier;
        SequenceBase seq = Sequences[seqName];
        if (seq == null) return false;
        return seq.StartAll(packet);
    }
    return Sequences.Start(packet);
}
```

`return false` 반환 시 `MainRun` (lines 51-58) 에서 자동으로 `SendTestError(packet.AsTest())` 를 호출하여 TCP 클라이언트에 NG 응답을 전송한다. `SendTestError` 수정 불필요.
  </action>
  <verify>
    <automated>grep -n "IsRecipeReady\|recipe not yet loaded" C:/Info/Project/DataMeasurement/WPF_Example/Custom/SystemHandler.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "IsRecipeReady" WPF_Example/Custom/SystemHandler.cs` → 1줄 (`!IsRecipeReady` guard)
    - `grep "recipe not yet loaded" WPF_Example/Custom/SystemHandler.cs` → 1줄 (거부 로그)
    - `grep "260615 hbk Phase 43.2" WPF_Example/Custom/SystemHandler.cs` → 주석 1줄 이상
    - ProcessTest 본문에서 `IsRecipeReady` guard 가 `IsDynamicFAIMode` 체크보다 먼저 위치 (guard-first 패턴)
  </acceptance_criteria>
  <done>ProcessTest 에 IsRecipeReady guard 추가 완료. 레시피 미로드 상태에서 TCP TEST 수신 시 NG 거부 응답 전송.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT — Before/After 수치 비교 + TCP TEST 거부 + 회귀 0 (D-E)</name>
  <what-built>
    Phase 43.2 전체 변경 사항:
    1. SystemHandler.cs: `_isRecipeReady` 필드 + `IsRecipeReady` 프로퍼티 + LoadRecipe (f)/(g) 마커
    2. MainWindow.xaml.cs: Window_Loaded 동기 블로킹 제거 + ContentRendered→BeginInvoke(Background) 이동 + OnLoadRecipe Dispatcher 래핑
    3. Custom/SystemHandler.cs: ProcessTest IsRecipeReady guard
  </what-built>
  <how-to-verify>
앱을 빌드하고 SIMUL 모드로 실행하여 아래 4가지를 순서대로 확인한다.

**사전 준비:**
```
msbuild C:/Info/Project/DataMeasurement/WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /verbosity:minimal
```
→ 0 errors 확인 후 `bin\x64\Debug\DatumMeasurement.exe` 실행

---

**Test 1: Before/After 창 표시 시점 비교 (D-E 핵심)**

Phase 43.1 기준:
- Before(동기): ContentRendered `(e)` = 21513ms (창이 21.5초에 표시)
- After(비동기) 목표: ContentRendered `(e)` ≈ 6726ms (레시피 로딩 없으므로 (d) 직후)

실행 후 로그 파일 (`Trace_YYYYMMDD.log`) 에서 확인:
```
[STARTUP-WHITE] (d) after InitializeComponent: XXX ms
[STARTUP-WHITE] (e) ContentRendered first paint: YYY ms  ← 기존 21513ms → 목표 ≤7500ms
[STARTUP-WHITE] (f) recipe load start: ZZZ ms            ← (e) 이후 실행 확인
[STARTUP-WHITE] (g) recipe load done (result=True): WWW ms ← (f) + ~14787ms
```

**PASS 기준:**
- `(e)` 수치 ≤ 7500ms (기존 21513ms 대비 ≥50% 단축)
- `(f)` 수치 > `(e)` 수치 (레시피 로드가 창 표시 이후에 시작됨)
- `(g)` - `(f)` ≈ 14787ms (레시피 로드 소요 시간 — 비동기 이동으로 변화 없음)

---

**Test 2: TCP TEST 거부 확인 (D-C)**

앱 실행 후 `(e)` ContentRendered 가 표시된 직후(`(f)` 이전) TCP TEST 패킷을 전송한다:
```python
# Test/mock_vision_client.py 또는 직접 TCP 전송 (포트 2505)
# $TEST,TOP,1 형식 TEST 패킷 전송
```

또는 로그에서 `[RECIPE] TEST rejected` 메시지 확인 (정상 수신 후 NG 거부):
```
[RECIPE] TEST rejected — recipe not yet loaded (IsRecipeReady=false)
```

`(g)` 이후 동일 TEST 패킷 재전송 → 정상 검사 시작 (NG 거부 없음).

---

**Test 3: 레시피 로드 완료 후 정상 동작 (회귀 0)**

`(g)` 마커 로그 확인 후:
1. 첫 로그인 시도 → 로그인 성공 (MainView 권한 활성)
2. 메뉴 → Recipe → 현재 레시피 정상 로드됨 (InspectionList 트리 표시)
3. SIMUL 모드 $TEST → 검사 시퀀스 정상 시작

**PASS 기준:** 3가지 모두 Phase 43.1 PASS 기준과 동일한 동작.

---

**Test 4: 앱 종료 후 재기동 회귀**

종료 후 재실행 → Test 1 반복 확인 (첫 번째 실행과 동일한 (e) 단축 수치).
  </how-to-verify>
  <resume-signal>
4가지 Test 결과를 아래 형식으로 보고:

Test 1 [PASS/FAIL]: (e)=XXXms, (f)=YYYms, (g)=ZZZms
Test 2 [PASS/FAIL]: 거부 로그 확인 여부
Test 3 [PASS/FAIL]: 로그인/레시피/검사 회귀 0
Test 4 [PASS/FAIL]: 재기동 수치 동일

또는 실패 시 실패 케이스 + 로그 스니펫 보고.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TCP 클라이언트 → ProcessTest | 외부 핸들러가 레시피 로드 전 TEST 패킷 전송 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43.2-06 | Denial of Service | 레시피 로드 실패 시 IsRecipeReady=true 미설정 | mitigate | BeginInvoke 내부 `IsRecipeReady = true` 는 `LoadRecipe` 반환값 무관하게 실행 (성공/실패 양쪽 처리됨) |
| T-43.2-07 | Spoofing | 레시피 로드 완료 전 TEST 수신 → 오작동 | mitigate | ProcessTest IsRecipeReady guard (Task 1) — false 반환 → NG 응답 전송 |
| T-43.2-08 | Denial of Service | 앱 종료 중 BeginInvoke 람다 실행 | accept | WPF Dispatcher.BeginInvoke 는 Dispatcher.ShutdownStarted 이후 무시됨 — .NET 4.8 기본 동작 |
</threat_model>

<verification>
최종 상태 검증:
```
grep -n "IsRecipeReady" C:/Info/Project/DataMeasurement/WPF_Example/Custom/SystemHandler.cs
grep -n "Window_ContentRendered_LoadRecipe" C:/Info/Project/DataMeasurement/WPF_Example/MainWindow.xaml.cs
grep -n "volatile bool _isRecipeReady" C:/Info/Project/DataMeasurement/WPF_Example/SystemHandler.cs
```

전체 3개 파일 변경 확인 후 UAT PASS 시 sign-off.
</verification>

<success_criteria>
- ProcessTest 에 `!IsRecipeReady` guard 존재 (IsDynamicFAIMode 보다 먼저)
- UAT Test 1: ContentRendered (e) ≤ 7500ms (기존 21513ms 대비 단축 입증)
- UAT Test 2: 레시피 로드 전 TCP TEST → `[RECIPE] TEST rejected` 로그 + NG 응답
- UAT Test 3: 레시피 로드 완료 후 첫 로그인/레시피/검사 정상 (회귀 0)
- UAT Test 4: 재기동 수치 일관성
</success_criteria>

<output>
완료 후 `.planning/phases/43.2-startup-recipe-async/43.2-03-SUMMARY.md` 생성.

Phase 43.2 전체 완료 시 `.planning/phases/43.2-startup-recipe-async/` 에 sign-off 기록 포함.
</output>
