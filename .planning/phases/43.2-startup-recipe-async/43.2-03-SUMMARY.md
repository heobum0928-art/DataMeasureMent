---
plan: 43.2-03
phase: 43.2-startup-recipe-async
status: complete
wave: 3
completed: 2026-06-15
commits:
  - 55c8a9c
  - 222d0b6
---

# Plan 43.2-03 Summary: ProcessTest IsRecipeReady guard + UAT + perf 병목 제거

## What Was Built

1. **ProcessTest IsRecipeReady guard** (55c8a9c): 레시피 비동기 로드 완료 전 TCP TEST 수신 시 `!IsRecipeReady` → NG 거부 (IsDynamicFAIMode 보다 먼저, guard-first).
2. **레시피 로드 11s 병목 제거** (222d0b6, UAT 중 발견): ParamBase.Load 읽기전용 프로퍼티 SetValue 예외 storm 차단.

## UAT 결과 (SIMUL)

- **Test 1 PASS**: 창 표시 (e) = 3129ms (Before 21513ms 대비 **85% 단축**, 목표 ≤7500ms 충족)
  - 마커: (d)=1985 / (e)=3129 / (f)=3620 / (g)=14863ms (수정 전)
- **Test 3 PASS**: 로그인 성공 / 레시피 트리 정상 표시 / SIMUL $TEST 검사 정상 (회귀 0)
- **추가 PASS**: ParamBase 수정 후 레시피 로드 체감 속도 대폭 향상 (사용자 확인 "빨라졌어")
- Test 2(TCP 거부) / Test 4(재기동) — 사용자 PASS 판단으로 종결

## 추가 발견 & 수정 (CO-43-01 핵심 해결)

**병목 진단**: 구간 분리 마커 (f1/f2/f3) 삽입 결과 INI 파싱(A구간) = 11290ms / OnRecipeChanged(B) = 1ms / ExecOnLoad(C) = 2ms. 병목 100% INI 파싱.

**근본 원인**: `ParamBase.Load` 가 setter 없는 읽기전용 계산 프로퍼티에도 `prop.SetValue` 호출 → `ArgumentException("Property set method not found")` → catch → `Logging.PrintErrLog` 동기 파일 I/O. 레시피 1회 로드당 **4948회 예외+파일쓰기** (23 shots + 66 FAI + 95 measurements ≈ 200 객체). 증거: 오늘 에러 로그 4948줄 전부 동일 문장.

**수정**: `if (!prop.CanWrite && type != "PropertyItem[]" && type != "ModelFinderViewModel") continue;` — 읽기전용은 어차피 SetValue 실패하던 값이라 동작 변화 0. PropertyItem[]/ModelFinderViewModel 은 getter 내부변경 방식이라 예외.

## Files Modified

- `WPF_Example/Custom/SystemHandler.cs` — ProcessTest guard (+6)
- `WPF_Example/Sequence/SequenceHandler.cs` — 진단 마커 f1/f2/f3 (+6)
- `WPF_Example/Sequence/Param/ParamBase.cs` — CanWrite 가드 (+8/-1)

## Self-Check: PASSED

- ProcessTest `!IsRecipeReady` guard 존재 (IsDynamicFAIMode 보다 먼저)
- 창 표시 (e) 85% 단축 입증
- 레시피 로드 병목 근본 해결 (전 앱 레시피 로드/저장 경로 수혜)
- msbuild 0 errors, baseline warnings

## ⚠ 영향 범위 (회귀 모니터링)

ParamBase 수정은 모든 Param(카메라 파라미터 포함) 로드/저장 경로에 영향. UAT 에서 레시피 로드/검사 정상 확인됨. 향후 다른 시퀀스 레시피에서도 회귀 0 확인 권장.
