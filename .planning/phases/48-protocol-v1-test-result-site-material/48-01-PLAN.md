---
phase: 48-protocol-v1-test-result-site-material
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Setting/SystemSetting.cs
  - WPF_Example/Custom/SystemSetting.cs
  - WPF_Example/TcpServer/VisionRequestPacket.cs
autonomous: true
requirements: [PROTO-01]
must_haves:
  truths:
    - "TEST 파서가 고정 매직 인덱스(dataList[숫자리터럴]) 대신 헝가리언 변수 경유 가변길이 파싱을 수행한다"
    - "자재번호 미수신($TEST 필드 부족 또는 'null' 문자열)이면 IndexNumber 가 -1 sentinel 로 폴백된다"
    - "SystemSetting 에 UseProtocolV1 / PcRole / ServerPortV1 설정 속성이 존재한다"
    - "기존 v2.6 TEST 파싱 경로가 UseProtocolV1=false 분기로 보존된다 (회귀 0)"
  artifacts:
    - path: "WPF_Example/Setting/SystemSetting.cs"
      provides: "UseProtocolV1(bool=false), PcRole(int=1), ServerPortV1(int=7701) 설정 속성"
      contains: "UseProtocolV1"
    - path: "WPF_Example/Custom/SystemSetting.cs"
      provides: "PcRole==0 → 1 복원 Load 가드 (구 INI 0 로드 방어)"
      contains: "PcRole"
    - path: "WPF_Example/TcpServer/VisionRequestPacket.cs"
      provides: "TestPacket.IndexNumber 속성 + TryParseTestFieldsV1 유연 파서 + v2.6/v1.0 분기"
      contains: "IndexNumber"
  key_links:
    - from: "VisionRequestPacket.Convert(string) CMD_RECV_TEST"
      to: "SystemSetting.Handle.UseProtocolV1"
      via: "bUseV1 분기로 v1.0 유연 파서 vs v2.6 레거시 파서 선택"
      pattern: "UseProtocolV1"
    - from: "TryParseTestFieldsV1"
      to: "TestPacket.IndexNumber"
      via: "site 다음 2번째 필드 파싱, 누락/null 시 SENTINEL_NO_MATERIAL(-1) 대입"
      pattern: "IndexNumber"
---

<objective>
디팜스테크 v1.0 프로토콜의 **토대(foundation)**를 구축한다:
1. `SystemSetting` 에 v1.0/v2.6 공존 플래그(`UseProtocolV1`)·PC 역할(`PcRole`)·v1.0 포트(`ServerPortV1`)를 추가한다 (D-03/D-06). 이 플래그들은 Wave 2 의 Site 매핑·RESULT 직렬화·포트 설정이 공유하는 공통 토대이다.
2. `$TEST:site,자재번호,null,z_index@` 를 파싱하는 **유연 파서**(`TryParseTestFieldsV1`)를 구현한다 — 고정 매직 인덱스(`dataList[1]`,`dataList[2]`) 의존을 탈피하고, 필드 누락 시 sentinel 폴백한다 (D-01/D-02). `TestPacket` 에 자재번호 보관용 `IndexNumber` 속성을 신설한다 (자재번호 전파 체인의 출발점 — Wave 2 Plan 04 가 소비).
3. 기존 v2.6 파서는 `UseProtocolV1=false` 분기 뒤에 보존한다 (D-06, 회귀 0).

Purpose: TEST 파싱 + 자재번호 필드 = PROTO-01 의 핵심. 후속 plan(Site/RESULT/전파)이 모두 이 plan 의 산출물(플래그, IndexNumber)에 의존하므로 가장 먼저 실행한다.
Output: SystemSetting 3 속성 + Custom SystemSetting Load 가드 + TestPacket.IndexNumber + 유연 파서 메서드 + v2.6/v1.0 분기.

🔒 **이 plan 의 모든 신규/수정 제어·프로토콜 코드(SystemSetting 신규 속성, VisionRequestPacket 파서/속성)는 `control-sequence-coding-guideline.md`(D-00) 를 따른다 — 헝가리언 접두사, `if/else if/else` 만(삼항 `?:`·null병합 `??` 금지), 매직넘버 금지(상수/enum), 조건식 bool 변수화, 함수 30줄 한도, 동사+목적어 함수명.** 이 지침이 CLAUDE.md "파일 스타일 따르기"보다 우선이다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-protocol-v1-test-result-site-material/48-CONTEXT.md
@.planning/phases/48-protocol-v1-test-result-site-material/48-PATTERNS.md
@.planning/refs/Vision-Protocol-v1.0.md
@.planning/refs/control-sequence-coding-guideline.md

<interfaces>
<!-- executor 가 직접 사용. 코드베이스 재탐색 불필요. -->

VisionServer 구분자 상수 (WPF_Example/TcpServer/VisionServer.cs lines 9-12):
```csharp
public const char MSG_STX = '$';
public const char MSG_ETX = '@';
public const char MSG_CONTENTS_SEPERATOR = ',';   // TEST 필드 구분자(현행)
public const char MSG_CMD_SEPERATOR = ':';
```

현행 TestPacket (WPF_Example/TcpServer/VisionRequestPacket.cs lines 360-368):
```csharp
public class TestPacket : VisionRequestPacket {
    public int TestType { get; set; }
    public string TestID { get; set; }       // z_index 문자열 보관
    public string Identifier2 { get; set; }
    public TestPacket() : base(VisionRequestType.Test) { }
}
```

현행 CMD_RECV_TEST 파서 (VisionRequestPacket.cs lines 259-288) — 교체 대상, 매직 인덱스:
```csharp
case CMD_RECV_TEST:
    packet = new TestPacket();
    TestPacket testPacket = packet.AsTest();
    dataList = msgList[1].Split(VisionServer.MSG_CONTENTS_SEPERATOR);
    if (dataList.Length < 3) return null;                          // 매직 3
    if (Int32.TryParse(dataList[0], out siteNum) == false) { return null; }
    testPacket.Site = siteNum;
    if (Int32.TryParse(dataList[1], out testKind) == false) { return null; }  // 매직 1
    testPacket.TestType = testKind;
    testID = dataList[2];                                          // 매직 2
    testPacket.TestID = testID;
    break;
```

현행 SystemSetting 속성 패턴 (WPF_Example/Setting/SystemSetting.cs):
```csharp
[Category("Connection|Server")]
public int ServerPort { get; set; } = 2505;        // line 36

public bool SaveFailImage { get; set; } = false;   // line 100
```

Custom SystemSetting partial 클래스 위치: WPF_Example/Custom/SystemSetting.cs (Load 오버라이드 가드 추가 지점).
SystemSetting.Load() 는 reflection 기반 INI 파싱 — 신규 public 속성은 자동 저장/로드되나, 기본값 ≠ 0 인 PcRole(=1) 은 구 INI 에서 0 으로 로드되는 기지 문제(reference_parambase_missing_key_zeroes_default.md) 가 있어 Load 가드 필요.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SystemSetting v1.0 플래그 + PC 역할 + v1.0 포트 속성 추가 (+ Custom Load 가드)</name>
  <read_first>
    - WPF_Example/Setting/SystemSetting.cs (수정 대상 — lines 28-110 속성 영역 + lines 186-253 Load 영역 확인)
    - WPF_Example/Custom/SystemSetting.cs (수정 대상 — partial 클래스, Load 가드 추가 지점)
    - .planning/phases/48-protocol-v1-test-result-site-material/48-PATTERNS.md (SystemSetting 섹션 lines 399-434)
    - .planning/phases/48-protocol-v1-test-result-site-material/48-CONTEXT.md (D-03/D-06)
    - .planning/refs/control-sequence-coding-guideline.md (헝가리언/매직넘버 금지)
  </read_first>
  <files>WPF_Example/Setting/SystemSetting.cs, WPF_Example/Custom/SystemSetting.cs</files>
  <action>
**WPF_Example/Setting/SystemSetting.cs** — 기존 `ServerPort`(line 36) 속성 아래(또는 `SaveFailImage` line 100 근처)에 신규 속성 3개를 추가한다. ServerPort 패턴(자동 INI 직렬화 [Category] 속성)을 그대로 따른다:

```csharp
// 260622 hbk Phase 48 PROTO-01: v1.0 프로토콜 활성화 플래그 (D-06 v2.6/v1.0 공존). 기본 false → 구 INI 0 로드돼도 v2.6 유지.
[Category("Connection|Protocol")]
public bool UseProtocolV1 { get; set; } = false;

// 260622 hbk Phase 48 PROTO-01: PC 역할 (D-03 빌드 상수 대신 설정 지정). 1=PC1(TOP/BOTTOM), 2=PC2(SIDE_1/SIDE_2).
[Category("Connection|Server")]
public int PcRole { get; set; } = 1;

// 260622 hbk Phase 48 PROTO-01: v1.0 전용 포트 (엑셀 규격 7701). ServerPort(2505) 는 v2.6 호환 유지 — v1.0 포트는 별도 속성으로 분리.
[Category("Connection|Server")]
public int ServerPortV1 { get; set; } = 7701;
```

규약: 위 속성 선언은 PropertyGrid/INI 직렬화용 표준 C# 자동 프로퍼티이므로 헝가리언 접두사 비대상(기존 ServerPort/SaveFailImage 와 동일 형식 유지 — 일관성). **매직넘버(7701, 2505)는 속성 기본값으로서 명명된 설정값**이므로 D-00 매직넘버 금지 위반 아님.

**WPF_Example/Custom/SystemSetting.cs** — PcRole 기본값(1)이 구 INI 에서 0 으로 로드되는 문제(reference_parambase_missing_key_zeroes_default.md) 방어. Custom partial 클래스에 Load 후처리 가드를 추가한다. 기존 Load 오버라이드/후크가 있으면 그 안에, 없으면 적절한 후처리 지점에 다음 헬퍼를 추가하고 호출한다. **D-00 준수: 헝가리언 + if/else + 매직넘버 상수화.**

```csharp
// 260622 hbk Phase 48 PROTO-01: 구 INI 에 PcRole 키 부재 시 0 로드 → 기본 PC1(=1) 복원 (reference_parambase_missing_key_zeroes_default.md 방어).
private const int PC_ROLE_DEFAULT = 1;

private void RestorePcRoleDefault()
{
    bool bPcRoleMissing = PcRole == 0;
    if (bPcRoleMissing)
    {
        PcRole = PC_ROLE_DEFAULT;
    }
}
```

Custom/SystemSetting.cs 의 기존 구조(partial 클래스, Load 후크 존재 여부)를 read_first 로 확인한 뒤, Load 완료 직후 `RestorePcRoleDefault()` 가 1회 호출되도록 연결한다. 기존 Load 후크가 없으면 가장 안전한 후처리 지점(Custom 초기화/Load 오버라이드)에 배치하되, **신규 v2.6 회귀를 피하기 위해 기존 메서드 시그니처는 변경하지 않는다**.
  </action>
  <verify>
    <automated>solution 빌드 (msbuild Debug/x64) — 신규 error/warning 0</automated>
  </verify>
  <acceptance_criteria>
    - `WPF_Example/Setting/SystemSetting.cs` 에 `UseProtocolV1`, `PcRole`, `ServerPortV1` 3개 public 속성 정의 존재 (grep `UseProtocolV1`, `ServerPortV1`)
    - `UseProtocolV1` 기본값 `= false`, `PcRole` 기본값 `= 1`, `ServerPortV1` 기본값 `= 7701` (grep)
    - `WPF_Example/Custom/SystemSetting.cs` 에 `PcRole == 0` 검사 후 1 복원하는 가드 코드 존재 (grep `PcRole`)
    - 가드 코드에 매직넘버 1 대신 명명 상수(`PC_ROLE_DEFAULT`) 사용 (grep `PC_ROLE_DEFAULT`)
    - 가드 코드에 삼항 `?:`·null병합 `??` 0건 (grep `?:`, `??` 해당 메서드 범위)
    - 솔루션 빌드 PASS, 신규 error/warning 0
  </acceptance_criteria>
  <done>SystemSetting 에 3개 v1.0 속성 추가 + Custom 에 PcRole 0→1 복원 가드 연결, 빌드 PASS.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TestPacket.IndexNumber 속성 + 유연 TEST 파서(V1) + v2.6/v1.0 분기</name>
  <read_first>
    - WPF_Example/TcpServer/VisionRequestPacket.cs (수정 대상 전체 — 특히 lines 137-292 Convert(string), lines 360-368 TestPacket)
    - .planning/phases/48-protocol-v1-test-result-site-material/48-PATTERNS.md (VisionRequestPacket 섹션 lines 53-184 — 유연 파서 목표 패턴 verbatim)
    - .planning/phases/48-protocol-v1-test-result-site-material/48-CONTEXT.md (D-01/D-02/D-06)
    - .planning/refs/Vision-Protocol-v1.0.md (lines 36-39 TEST 포맷 정의)
    - .planning/refs/control-sequence-coding-guideline.md (전체)
  </read_first>
  <files>WPF_Example/TcpServer/VisionRequestPacket.cs</files>
  <behavior>
    유연 파서 `TryParseTestFieldsV1(string[] dataList, TestPacket testPacket)` 입력→출력:
    - 입력 `"1,42,null,1"` → Site=1, IndexNumber=42, TestID="1" (z_index), return true
    - 입력 `"1,null,null,0"` → Site=1, IndexNumber=-1(sentinel), TestID="0", return true
    - 입력 `"1"` (site만) → Site=1, IndexNumber=-1, TestID="-1"(sentinel), return true (필드 누락 폴백)
    - 입력 `"1,42"` (자재번호까지) → Site=1, IndexNumber=42, TestID="-1"(z_index 누락 폴백), return true
    - 입력 `""` (빈) 또는 site 파싱 실패 → return false
    - 입력 `"abc,42,null,1"` (site 비정수) → return false
    - 입력 `"1,xyz,null,1"` (자재번호 비정수) → Site=1, IndexNumber=-1(sentinel), return true (견고성: 자재번호 파싱 실패는 치명적 아님)
  </behavior>
  <action>
**(a) TestPacket 클래스(lines 360-368)에 IndexNumber 속성 + sentinel 상수 추가:**

```csharp
public class TestPacket : VisionRequestPacket {
    public int TestType { get; set; }
    public string TestID { get; set; }       // z_index 문자열 보관
    public string Identifier2 { get; set; }

    // 260622 hbk Phase 48 PROTO-01: 자재번호 (자재 IndexNumber). 미수신/null/파싱실패 → SENTINEL_NO_MATERIAL(-1).
    public int IndexNumber { get; set; } = SENTINEL_NO_MATERIAL;

    public TestPacket() : base(VisionRequestType.Test) { }
}
```

**(b) VisionRequestPacket 클래스(static 컨텍스트)에 sentinel/필드 인덱스 명명 상수 추가** (CMD_RECV_* 상수들 근처, lines 21-25 영역). **D-00 매직넘버 금지 → 모든 필드 인덱스를 명명 상수로:**

```csharp
// 260622 hbk Phase 48 PROTO-01: v1.0 TEST 유연 파서 상수 ($TEST:site,MaterialNumber,null,z_index@).
public const int SENTINEL_NO_MATERIAL = -1;          // 자재번호 미수신 sentinel
public const string SENTINEL_Z_INDEX_STR = "-1";     // z_index 미수신 sentinel
public const string TEST_NULL_PLACEHOLDER = "null";  // 예약 'null' 문자열
private const int TEST_FIELD_SITE = 0;               // 필드 인덱스: site
private const int TEST_FIELD_MATERIAL = 1;           // 필드 인덱스: 자재번호
private const int TEST_FIELD_ZINDEX = 3;             // 필드 인덱스: z_index (v1.0 4번째)
private const int TEST_MIN_FIELD_SITE = 1;           // site 만 있으면 파싱 시작
private const int TEST_MIN_FIELD_MATERIAL = 2;       // 자재번호 필드 존재 최소 길이
private const int TEST_MIN_FIELD_ZINDEX = 4;         // z_index 필드 존재 최소 길이
```

**(c) 유연 파서 메서드 신설** (Convert(string) 아래 또는 클래스 내 private static). 48-PATTERNS.md lines 89-150 의 목표 패턴을 따른다. **헝가리언 + if/else 전용 + 조건 bool 변수화 + 30줄 한도(초과 시 자재번호 파싱을 `ParseMaterialField` 헬퍼로 분리)**:

```csharp
// 260622 hbk Phase 48 PROTO-01: v1.0 유연 TEST 파서. 고정 매직 인덱스 의존 탈피 — 필드 누락 시 sentinel 폴백.
//  향후 필드 추가/순서 변경 시 이 메서드(+상수)만 수정. D-02.
private static bool TryParseTestFieldsV1(string[] dataList, TestPacket testPacket)
{
    bool bHasSite = dataList.Length >= TEST_MIN_FIELD_SITE;
    if (!bHasSite) { return false; }

    int nSiteNum = 0;
    bool bSiteValid = Int32.TryParse(dataList[TEST_FIELD_SITE], out nSiteNum);
    if (!bSiteValid) { return false; }
    testPacket.Site = nSiteNum;

    testPacket.IndexNumber = ParseMaterialField(dataList);
    testPacket.TestID = ParseZIndexField(dataList);
    return true;
}

// 260622 hbk Phase 48 PROTO-01: 자재번호 필드 파싱. 누락/'null'/비정수 → SENTINEL_NO_MATERIAL.
private static int ParseMaterialField(string[] dataList)
{
    bool bHasMaterial = dataList.Length >= TEST_MIN_FIELD_MATERIAL;
    if (!bHasMaterial) { return SENTINEL_NO_MATERIAL; }

    string szRaw = dataList[TEST_FIELD_MATERIAL];
    bool bIsNullPlaceholder = string.IsNullOrEmpty(szRaw) || szRaw == TEST_NULL_PLACEHOLDER;
    if (bIsNullPlaceholder) { return SENTINEL_NO_MATERIAL; }

    int nMaterial = 0;
    bool bMaterialValid = Int32.TryParse(szRaw, out nMaterial);
    if (!bMaterialValid) { return SENTINEL_NO_MATERIAL; }
    return nMaterial;
}

// 260622 hbk Phase 48 PROTO-01: z_index 필드 파싱. 누락/비정수 → SENTINEL_Z_INDEX_STR.
private static string ParseZIndexField(string[] dataList)
{
    bool bHasZIndex = dataList.Length >= TEST_MIN_FIELD_ZINDEX;
    if (!bHasZIndex) { return SENTINEL_Z_INDEX_STR; }

    int nZIndex = 0;
    bool bZValid = Int32.TryParse(dataList[TEST_FIELD_ZINDEX], out nZIndex);
    if (!bZValid) { return SENTINEL_Z_INDEX_STR; }
    return nZIndex.ToString();
}
```

> 주의: v1.0 포맷에서 TestType(z_index 와 별개)은 명시 필드가 없다. 현행 v2.6 은 dataList[1]=TestType 으로 썼으나 v1.0 은 [1]=자재번호다. v1.0 분기에서 `testPacket.TestType` 은 기존 기본값(0=ETestType.Default)을 유지하거나, ResourceMap 이 Inspection 으로 처리하도록 둔다 — **이 plan 에서는 TestType 을 강제 변경하지 말고 기본값 유지**(Site→Sequence 매핑은 Wave 2 Plan 02 가 PcRole 기반으로 처리). 단, 현행 ResourceMap.SetIdentifier(Test) 가 `(ETestType)testPacket.TestType == ETestType.Calibration` 분기로 Calibration 을 걸러내므로, v1.0 에서 TestType=0(Default)이면 Inspection 으로 흐른다(정상). 이 동작을 SUMMARY 에 명시한다.

**(d) v2.6 레거시 파서 추출 + 분기:** 현행 CMD_RECV_TEST 블록(lines 259-288)의 본문을 private static `TryParseTestFieldsV26(string[] dataList, TestPacket testPacket)` 로 **byte-identical 추출**(매직 인덱스·`Length < 3`·return 동작 그대로 — 회귀 0)한 뒤, switch 의 CMD_RECV_TEST case 를 분기로 교체. **D-00 헝가리언 + if/else:**

```csharp
case CMD_RECV_TEST: //test
    packet = new TestPacket();
    TestPacket testPacket = packet.AsTest();
    dataList = msgList[1].Split(VisionServer.MSG_CONTENTS_SEPERATOR);

    bool bUseV1 = SystemSetting.Handle.UseProtocolV1;
    if (bUseV1)
    {
        bool bParseV1Ok = TryParseTestFieldsV1(dataList, testPacket);
        if (!bParseV1Ok) { return null; }
    }
    else
    {
        bool bParseV26Ok = TryParseTestFieldsV26(dataList, testPacket);
        if (!bParseV26Ok) { return null; }
    }
    break;
```

`TryParseTestFieldsV26` 본문은 현행 lines 264-286 로직(`if (dataList.Length < 3) return null;` 포함)을 그대로 옮기되 return 타입을 bool 로(`return null` → `return false`, 끝에 `return true`). **레거시 파서는 D-00 적용 대상이나 회귀 0 우선 — 매직 인덱스/스타일은 현행 보존하고, 신규 코드(V1 파서·상수·분기)만 D-00 엄격 적용**. (CONTEXT.md D-06 = 교체 아닌 보존.)
  </action>
  <verify>
    <automated>solution 빌드 (msbuild Debug/x64) — 신규 error/warning 0</automated>
  </verify>
  <acceptance_criteria>
    - `TestPacket` 클래스에 `public int IndexNumber` 속성 정의 존재, 기본값 `SENTINEL_NO_MATERIAL` (grep `IndexNumber`)
    - `TryParseTestFieldsV1` 메서드 정의 존재 (grep)
    - 유연 파서(V1)가 `dataList[1]`,`dataList[2]` 등 **숫자 리터럴 고정 인덱스 미사용** — 명명 상수(`TEST_FIELD_MATERIAL`, `TEST_FIELD_ZINDEX` 등) 경유 (grep `TEST_FIELD_` 다수 출현, V1 파서 본문에 `dataList[1]`/`dataList[2]`/`dataList[3]` 숫자 리터럴 0건)
    - V1 파서·헬퍼(`ParseMaterialField`/`ParseZIndexField`)·분기 코드에 삼항 `?:` 및 null병합 `??` 0건 (grep `?:`, `??` 해당 메서드 범위)
    - sentinel 상수 `SENTINEL_NO_MATERIAL = -1` 정의 존재 (grep)
    - v2.6 분기 보존: `UseProtocolV1` false 시 `TryParseTestFieldsV26` 호출 경로 존재, 레거시 `if (dataList.Length < 3) return` 로직 보존 (grep `TryParseTestFieldsV26`)
    - 각 신규 메서드 30줄 이하 (TryParseTestFieldsV1 본문이 자재번호/z_index 헬퍼로 분리됨 — grep `ParseMaterialField`, `ParseZIndexField`)
    - 솔루션 빌드 PASS, 신규 error/warning 0
  </acceptance_criteria>
  <done>TestPacket.IndexNumber 신설 + 유연 V1 파서(헝가리언/if-else/명명상수/30줄) + v2.6 분기 보존, 빌드 PASS. behavior 8 케이스 코드 경로 충족.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 제어 PC(Client) → Vision Server TCP 입력 | `$TEST:...@` 패킷은 신뢰 경계를 넘는 untrusted 네트워크 입력. 변형/불완전/초과길이 가능. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Denial of Service | VisionRequestPacket.TryParseTestFieldsV1 | mitigate | 가변길이 폴백 — 필드 부족(`dataList.Length` 체크)·비정수(`Int32.TryParse` 실패) 시 sentinel 반환 또는 return false, IndexOutOfRange/FormatException 미발생. site 필수 외 모든 필드 누락 허용. |
| T-48-02 | Tampering | TestPacket.IndexNumber | mitigate | 자재번호 비정수/변형 입력은 SENTINEL_NO_MATERIAL(-1) 로 정규화 — 후속 전파(파일명/xlsx)에 정수만 흐름. 임의 문자열이 저장 경로로 전파되지 않음. |
| T-48-03 | Denial of Service | VisionServer.GetRecvPacket (호출부) | accept | ArgumentOutOfRange/IndexOutOfRange catch 가 이미 VisionServer 에 존재(lines 50-56) → 파서 예외도 상위에서 흡수. 산업용 로컬 신뢰망. 본 plan 은 파서 자체 견고성으로 1차 방어. |

high severity 미해결 위협: 0건.
</threat_model>

<verification>
- msbuild Debug/x64 솔루션 빌드 PASS, 신규 error/warning 0 (v2.6 경로 회귀 0).
- grep: `UseProtocolV1`, `PcRole`, `ServerPortV1`, `IndexNumber`, `TryParseTestFieldsV1`, `TryParseTestFieldsV26`, `SENTINEL_NO_MATERIAL` 모두 존재.
- grep: V1 파서 본문 숫자 리터럴 인덱스(`dataList[1]`/`dataList[2]`/`dataList[3]`) 0건, 삼항/`??` 0건.
- mock 입력 검증(코드 경로 추적): behavior 8 케이스가 분기로 커버됨 — `"1,42,null,1"`→IndexNumber=42, `"1,null,null,0"`→-1, `"1"`→폴백, `"abc,..."`→false.
</verification>

<success_criteria>
- SystemSetting 3 속성(UseProtocolV1=false/PcRole=1/ServerPortV1=7701) + Custom Load 가드 추가.
- TestPacket.IndexNumber 신설 (자재번호 전파 출발점 — Plan 04 가 소비).
- 유연 V1 파서: 고정 매직 인덱스 탈피, 자재번호 미수신 -1 폴백, 필드 누락/추가 견고.
- v2.6 경로 UseProtocolV1=false 분기로 보존 (회귀 0).
- 신규 제어 코드 D-00 준수 (헝가리언/if-else/명명상수/30줄/삼항·??0건).
</success_criteria>

<output>
완료 후 `.planning/phases/48-protocol-v1-test-result-site-material/48-01-SUMMARY.md` 생성.
SUMMARY 에 기록: (1) v1.0 분기에서 TestType 기본값(0=Default) 유지 결정 + ResourceMap Inspection 흐름, (2) sentinel 값 -1 채택, (3) 추가된 명명 상수 목록, (4) Custom Load 가드 연결 지점.
</output>
