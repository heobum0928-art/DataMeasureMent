---
phase: 48-protocol-v1-test-result-site-material
plan: 02
type: execute
wave: 2
depends_on: [48-01]
files_modified:
  - WPF_Example/Custom/TcpServer/ResourceMap.cs
  - WPF_Example/TcpServer/VisionServer.cs
  - WPF_Example/TcpServer/TcpServer.cs
autonomous: true
requirements: [PROTO-01]
must_haves:
  truths:
    - "UseProtocolV1=true 시 ResourceMap 이 2-PC 체계로 Site→Sequence/Camera/Light/Action 을 매핑한다 (PC1: Site1=TOP/Site2=BOTTOM, PC2: Site1=SIDE/Site2=SIDE)"
    - "PcRole 설정값(1=PC1, 2=PC2)에 따라 Site 번호의 의미가 동적으로 결정된다 (재빌드 불필요)"
    - "UseProtocolV1=false 시 기존 ResourceMap(Site1=Top/2=Side/3=Bottom) 매핑이 보존된다 (회귀 0)"
    - "UseProtocolV1=true 시 VisionServer 가 UTF-8 인코딩과 ServerPortV1(7701) 포트를 사용한다"
  artifacts:
    - path: "WPF_Example/Custom/TcpServer/ResourceMap.cs"
      provides: "EPcRole enum + InitializeV1/InitializeV26 분기 + SetIdentifier v1.0 Site 변환"
      contains: "EPcRole"
    - path: "WPF_Example/TcpServer/VisionServer.cs"
      provides: "UseProtocolV1 시 UTF-8 인코딩 적용 (생성자)"
      contains: "UseProtocolV1"
    - path: "WPF_Example/TcpServer/TcpServer.cs"
      provides: "EncodingType/PortNum 를 파생 클래스가 v1.0 설정할 수 있는 접근 경로"
      contains: "EncodingType"
  key_links:
    - from: "ResourceMap.Initialize"
      to: "SystemSetting.Handle.UseProtocolV1 / PcRole"
      via: "bUseV1 분기 → InitializeV1(PcRole) vs InitializeV26"
      pattern: "UseProtocolV1"
    - from: "ResourceMap.SetIdentifier(Test)"
      to: "Find(EResource.Sequence, mapped ESite)"
      via: "v1.0 Site 정수 → PcRole-aware ESite 키 변환 후 Find"
      pattern: "PcRole"
    - from: "VisionServer 생성자"
      to: "TcpServer EncodingType / PortNum"
      via: "UseProtocolV1 시 Utf8 + ServerPortV1 적용"
      pattern: "Utf8"
---

<objective>
디팜스테크 v1.0 의 **2-PC Site 체계**(D-04)와 **전송 계층 설정**(Port 7701/UTF-8)을 재정합한다:
1. 현행 `ResourceMap`(Site1=Top/2=Side/3=Bottom)을 2-PC 체계로 재정합한다. PC 역할은 `SystemSetting.PcRole`(D-03, Plan 01 산출) 로 지정 — **빌드 상수/컴파일 분기 없이** 한 바이너리로 PC1/PC2 모두 동작. PC1: Site1=TOP/Site2=BOTTOM, PC2: Site1=SIDE_1/Site2=SIDE_2 (둘 다 물리 SIDE 자원 공유 — 시스템에 SIDE 자원 1종).
2. `VisionServer` 가 `UseProtocolV1=true` 시 UTF-8 인코딩 + `ServerPortV1`(7701) 포트를 사용하도록 한다. `@` 종료(MSG_ETX)는 이미 적용됨.
3. v2.6 경로(`UseProtocolV1=false`)는 기존 `ESite.Top/Side/Bottom` 매핑·기존 포트(2505)·기존 인코딩으로 보존한다 (D-06, 회귀 0).

Purpose: Site 재정합 = PROTO-01 의 일부(ROADMAP "ResourceMap z_index↔Shot/Site 매핑"). Plan 01 의 `UseProtocolV1`/`PcRole` 플래그에 의존하므로 Wave 2.
Output: ResourceMap 2-PC 분기 + EPcRole enum + VisionServer/TcpServer 전송 설정.

🔒 **이 plan 의 모든 신규/수정 제어 코드(ResourceMap 신규 메서드/enum, VisionServer 생성자, TcpServer 접근자)는 `control-sequence-coding-guideline.md`(D-00) 를 따른다 — 헝가리언, `if/else if/else` 만(삼항·??금지), 매직넘버 금지(상수/enum), 조건식 bool 변수화, 함수 30줄 한도, 동사+목적어 함수명.** v2.6 보존 블록은 byte-identical 추출로 회귀 0 우선.

⚠ **설계 제약(반드시 준수):** framework `WPF_Example/TcpServer/ResourceMap.cs` 의 `Add`/`Find`/`SiteMap`/`ActionList` 는 `ESite` enum 키에 강결합(`Dictionary<ESite, ...>`)되어 있고, `SetIdentifier` 는 `(ESite)testPacket.Site` 캐스팅을 쓴다. 따라서 **별도 ESiteV1 enum 을 Add/Find 인자로 넘기면 시그니처 불일치로 컴파일 실패**한다. → v1.0 매핑은 **기존 ESite 키(Top/Side/Bottom)를 재사용**하되 PcRole-aware Site 정수→ESite 변환으로 구현한다 (아래 action 참조). framework ResourceMap.cs(`WPF_Example/TcpServer/ResourceMap.cs`) 는 **무수정**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/48-protocol-v1-test-result-site-material/48-CONTEXT.md
@.planning/phases/48-protocol-v1-test-result-site-material/48-PATTERNS.md
@.planning/refs/Vision-Protocol-v1.0.md
@.planning/refs/control-sequence-coding-guideline.md

<interfaces>
<!-- executor 가 직접 사용. 코드베이스 재탐색 불필요. -->

framework ResourceMap (WPF_Example/TcpServer/ResourceMap.cs) — **무수정**, 시그니처만 참조:
```csharp
public enum EResource : int { Camera=0, Light=1, Sequence=2, Action=3 }
public enum ESite : int { Top=1, Side=2, Bottom=3 }   // Custom/TcpServer/ResourceMap.cs 에 정의

// Add/Find 는 ESite 강타입 — ESiteV1 같은 새 enum 불가:
private bool Add(EResource res, ESite site, string name);
private bool Add(EResource res, ESite site, ETestType testType, string name);
public string Find(EResource res, ESite site);
public string Find(EResource res, ESite site, ETestType testType);
// SiteMap/ActionList 키 = ESite
```

현행 Custom ResourceMap.Initialize (WPF_Example/Custom/TcpServer/ResourceMap.cs lines 33-55) — 교체 대상:
```csharp
public void Initialize() {
    Add(EResource.Camera, ESite.Top, DeviceHandler.CAMERA_TOP);
    Add(EResource.Camera, ESite.Side, DeviceHandler.CAMERA_SIDE);
    Add(EResource.Camera, ESite.Bottom, DeviceHandler.CAMERA_BOTTOM);
    Add(EResource.Light, ESite.Top, LightHandler.LIGHT_TOP);
    Add(EResource.Light, ESite.Side, LightHandler.LIGHT_SIDE);
    Add(EResource.Light, ESite.Bottom, LightHandler.LIGHT_BOTTOM);
    Add(EResource.Sequence, ESite.Top, SequenceHandler.SEQ_TOP);
    Add(EResource.Sequence, ESite.Side, SequenceHandler.SEQ_SIDE);
    Add(EResource.Sequence, ESite.Bottom, SequenceHandler.SEQ_BOTTOM);
    Add(EResource.Action, ESite.Top, ETestType.Inspection, SequenceHandler.ACT_INSPECT);
    Add(EResource.Action, ESite.Side, ETestType.Inspection, SequenceHandler.ACT_INSPECT);
    Add(EResource.Action, ESite.Bottom, ETestType.Inspection, SequenceHandler.ACT_INSPECT);
}
```

현행 SetIdentifier(Test) 분기 (WPF_Example/Custom/TcpServer/ResourceMap.cs lines 77-86):
```csharp
case VisionRequestType.Test:
    TestPacket testPacket = packet.AsTest();
    if ((ETestType)testPacket.TestType == ETestType.Calibration) { ... return false; }
    testPacket.Identifier  = Find(EResource.Sequence, (ESite)testPacket.Site);
    testPacket.Identifier2 = Find(EResource.Action, (ESite)testPacket.Site, (ETestType)testPacket.TestType);
    break;
```

시스템 자원 상수 (확인됨 — 추가 자원 없음):
```
DeviceHandler.CAMERA_TOP/CAMERA_SIDE/CAMERA_BOTTOM   (CAM_TOP/CAM_SIDE/CAM_BOTTOM)
LightHandler.LIGHT_TOP/LIGHT_SIDE/LIGHT_BOTTOM       (TOP/SIDE/BOTTOM)
SequenceHandler.SEQ_TOP/SEQ_SIDE/SEQ_BOTTOM          (TOP/SIDE/BOTTOM)
SequenceHandler.ACT_INSPECT                          (Inspect)
```
> SIDE_1/SIDE_2 전용 자원은 존재하지 않는다 — PC2 의 Site1/Site2 는 동일 물리 SIDE 자원(SEQ_SIDE/CAMERA_SIDE/LIGHT_SIDE)에 매핑한다. (엑셀 Index Table 에서 SIDE_1/SIDE_2 는 검사 위치 구분일 뿐 별도 카메라 아님.)

TcpServer 인코딩/포트 (WPF_Example/TcpServer/TcpServer.cs) — **private static 문제 주의**:
```csharp
public enum MessageEncodingType { Default, Ascii, Utf8 }   // line 71
private static MessageEncodingType EncodingType { get; set; } = MessageEncodingType.Default;  // line 76 ← private!
protected int PortNum = 9000;                              // line 319
public TcpServer() {                                       // line 340 — 생성자에서 즉시 mListener.Start()
    PortNum = SystemSetting.Handle.ServerPort;             // line 341
    mListener = new TcpListener(IPAddress.Any, PortNum);   // line 343
    mListener.Start();                                     // line 344 ← 생성자 안에서 listen 시작
}
```

VisionServer 생성자 (WPF_Example/TcpServer/VisionServer.cs lines 18-21):
```csharp
public VisionServer() : base() {        // base() = TcpServer() 가 먼저 실행 → Port/listen 이미 시작됨
    Header = (byte)MSG_STX;
    Trailer = (byte)MSG_ETX;            // '@' 종료 이미 적용
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ResourceMap 2-PC 재정합 (EPcRole enum + InitializeV1/V26 분기 + SetIdentifier v1.0 Site 변환)</name>
  <read_first>
    - WPF_Example/Custom/TcpServer/ResourceMap.cs (수정 대상 전체)
    - WPF_Example/TcpServer/ResourceMap.cs (framework — 무수정, Add/Find/SiteMap 시그니처 확인용 lines 56-107)
    - .planning/phases/48-protocol-v1-test-result-site-material/48-PATTERNS.md (ResourceMap 섹션 lines 304-394)
    - .planning/phases/48-protocol-v1-test-result-site-material/48-CONTEXT.md (D-03/D-04/D-06)
    - .planning/refs/Vision-Protocol-v1.0.md (lines 11-18 Site 정의, lines 50-56 Index Table)
    - .planning/refs/control-sequence-coding-guideline.md (전체)
  </read_first>
  <files>WPF_Example/Custom/TcpServer/ResourceMap.cs</files>
  <action>
**(a) EPcRole enum 추가** (ESite enum 근처, lines 10-14 영역). D-00 enum 으로 매직넘버 회피:

```csharp
// 260622 hbk Phase 48 PROTO-01: PC 역할 (D-03). SystemSetting.PcRole 값과 일치 (1=PC1, 2=PC2).
public enum EPcRole : int {
    PC1_TopBottom = 1,
    PC2_Side = 2,
}
```

**(b) Initialize 를 분기로 교체.** 현행 Initialize 본문(lines 34-54)을 private `InitializeV26()` 로 **byte-identical 이동**(회귀 0)한 뒤, Initialize 는 플래그 분기만. **D-00 헝가리언 + if/else:**

```csharp
public void Initialize()
{
    bool bUseV1 = SystemSetting.Handle.UseProtocolV1;
    if (bUseV1)
    {
        InitializeV1();
    }
    else
    {
        InitializeV26();
    }
}

// v2.6 보존 — 현행 Initialize 본문 그대로 (Site1=Top/2=Side/3=Bottom). 회귀 0.
private void InitializeV26()
{
    Add(EResource.Camera, ESite.Top, DeviceHandler.CAMERA_TOP);
    Add(EResource.Camera, ESite.Side, DeviceHandler.CAMERA_SIDE);
    Add(EResource.Camera, ESite.Bottom, DeviceHandler.CAMERA_BOTTOM);
    Add(EResource.Light, ESite.Top, LightHandler.LIGHT_TOP);
    Add(EResource.Light, ESite.Side, LightHandler.LIGHT_SIDE);
    Add(EResource.Light, ESite.Bottom, LightHandler.LIGHT_BOTTOM);
    Add(EResource.Sequence, ESite.Top, SequenceHandler.SEQ_TOP);
    Add(EResource.Sequence, ESite.Side, SequenceHandler.SEQ_SIDE);
    Add(EResource.Sequence, ESite.Bottom, SequenceHandler.SEQ_BOTTOM);
    Add(EResource.Action, ESite.Top, ETestType.Inspection, SequenceHandler.ACT_INSPECT);
    Add(EResource.Action, ESite.Side, ETestType.Inspection, SequenceHandler.ACT_INSPECT);
    Add(EResource.Action, ESite.Bottom, ETestType.Inspection, SequenceHandler.ACT_INSPECT);
}
```

**(c) InitializeV1() — PcRole 분기.** framework Add 는 ESite 키 강타입이므로, **2-PC Site 정수를 기존 ESite 키로 매핑**한다. PC1: Site1→ESite.Top 키(TOP 자원), Site2→ESite.Side 키(BOTTOM 자원). PC2: Site1→ESite.Top 키(SIDE 자원), Site2→ESite.Side 키(SIDE 자원). **핵심: ESite 키는 슬롯(Site1=ESite.Top, Site2=ESite.Side)으로만 쓰고, 실제 매핑되는 자원 이름은 PcRole 로 결정.** 30줄 초과 시 PC1/PC2 를 각각 `MapPc1Resources()`/`MapPc2Resources()` 헬퍼로 분리. D-00 헝가리언 + if/else + 조건 변수화:

```csharp
// 260622 hbk Phase 48 PROTO-01: v1.0 2-PC 매핑 (D-04). Site1=ESite.Top 슬롯, Site2=ESite.Side 슬롯.
//  PcRole 로 슬롯이 가리키는 실제 자원(TOP/BOTTOM vs SIDE/SIDE)을 결정. framework ESite 키 재사용(시그니처 호환).
private void InitializeV1()
{
    int nPcRole = (int)SystemSetting.Handle.PcRole;
    bool bIsPc1 = nPcRole == (int)EPcRole.PC1_TopBottom;
    if (bIsPc1)
    {
        MapPc1Resources();
    }
    else
    {
        MapPc2Resources();
    }
}

// PC1: Site1(ESite.Top 슬롯)=TOP, Site2(ESite.Side 슬롯)=BOTTOM.
private void MapPc1Resources()
{
    Add(EResource.Camera, ESite.Top,  DeviceHandler.CAMERA_TOP);
    Add(EResource.Camera, ESite.Side, DeviceHandler.CAMERA_BOTTOM);
    Add(EResource.Light, ESite.Top,  LightHandler.LIGHT_TOP);
    Add(EResource.Light, ESite.Side, LightHandler.LIGHT_BOTTOM);
    Add(EResource.Sequence, ESite.Top,  SequenceHandler.SEQ_TOP);
    Add(EResource.Sequence, ESite.Side, SequenceHandler.SEQ_BOTTOM);
    Add(EResource.Action, ESite.Top,  ETestType.Inspection, SequenceHandler.ACT_INSPECT);
    Add(EResource.Action, ESite.Side, ETestType.Inspection, SequenceHandler.ACT_INSPECT);
}

// PC2: Site1·Site2 모두 SIDE 자원 (물리 SIDE 1종 공유).
private void MapPc2Resources()
{
    Add(EResource.Camera, ESite.Top,  DeviceHandler.CAMERA_SIDE);
    Add(EResource.Camera, ESite.Side, DeviceHandler.CAMERA_SIDE);
    Add(EResource.Light, ESite.Top,  LightHandler.LIGHT_SIDE);
    Add(EResource.Light, ESite.Side, LightHandler.LIGHT_SIDE);
    Add(EResource.Sequence, ESite.Top,  SequenceHandler.SEQ_SIDE);
    Add(EResource.Sequence, ESite.Side, SequenceHandler.SEQ_SIDE);
    Add(EResource.Action, ESite.Top,  ETestType.Inspection, SequenceHandler.ACT_INSPECT);
    Add(EResource.Action, ESite.Side, ETestType.Inspection, SequenceHandler.ACT_INSPECT);
}
```

**(d) SetIdentifier(Test) v1.0 Site 변환.** v1.0 에서 들어온 Site 정수(1/2)를 위 슬롯 키(ESite.Top=Site1, ESite.Side=Site2)로 변환해 Find 해야 한다. 현행은 `(ESite)testPacket.Site` 직접 캐스팅(Site1→ESite.Top=1, Site2→ESite.Side=2 — 우연히 일치하지만 Site3 등 방어 필요). v1.0 분기를 추가하되 **회귀 0 위해 v2.6 은 기존 캐스팅 보존**. SetIdentifier 의 Test case(lines 77-86)를 다음으로 교체. 30줄/중첩 한도 위해 Site→ESite 변환을 `ResolveSiteSlot(int)` 헬퍼로 분리. D-00 헝가리언 + if/else:

```csharp
// 260622 hbk Phase 48 PROTO-01: v1.0 Site 정수 → ESite 슬롯 변환 (Site1=Top, Site2=Side). 범위 밖 → Top 폴백.
private ESite ResolveSiteSlot(int nSite)
{
    bool bIsSite2 = nSite == 2;
    if (bIsSite2)
    {
        return ESite.Side;
    }
    return ESite.Top;
}
```

Test case 본문:
```csharp
case VisionRequestType.Test:
    TestPacket testPacket = packet.AsTest();
    bool bIsCalibration = (ETestType)testPacket.TestType == ETestType.Calibration;
    if (bIsCalibration)
    {
        testPacket.Identifier = null;
        testPacket.Identifier2 = null;
        return false;
    }

    bool bUseV1 = SystemSetting.Handle.UseProtocolV1;
    if (bUseV1)
    {
        ESite eSlot = ResolveSiteSlot(testPacket.Site);
        testPacket.Identifier  = Find(EResource.Sequence, eSlot);
        testPacket.Identifier2 = Find(EResource.Action, eSlot, ETestType.Inspection);
    }
    else
    {
        testPacket.Identifier  = Find(EResource.Sequence, (ESite)testPacket.Site);
        testPacket.Identifier2 = Find(EResource.Action, (ESite)testPacket.Site, (ETestType)testPacket.TestType);
    }
    break;
```

> 주의: v1.0 의 TestType 은 Plan 01 에서 기본값 0(Default) 유지하기로 했다. v1.0 분기는 Action Find 에 `ETestType.Inspection` 을 명시 전달(Default 가 아닌 Inspection 슬롯으로 매핑) — InitializeV1 이 ETestType.Inspection 으로 Add 했으므로 일치. **이 결정(v1.0 은 항상 Inspection action)을 SUMMARY 에 명시.**

> framework ResourceMap.cs(`WPF_Example/TcpServer/ResourceMap.cs`)는 **건드리지 않는다.** ESite enum 은 Custom/TcpServer/ResourceMap.cs 에 정의되어 있으므로 EPcRole 도 같은 파일에 추가.
  </action>
  <verify>
    <automated>solution 빌드 (msbuild Debug/x64) — 신규 error/warning 0</automated>
  </verify>
  <acceptance_criteria>
    - `EPcRole` enum 정의 존재 (PC1_TopBottom=1, PC2_Side=2) (grep `EPcRole`)
    - `InitializeV1`, `InitializeV26`, `MapPc1Resources`, `MapPc2Resources` 메서드 정의 존재 (grep 각각)
    - `InitializeV26` 본문이 현행 매핑(ESite.Top=TOP, ESite.Side=SIDE, ESite.Bottom=BOTTOM)을 보존 (grep — 12 Add 호출 + Top/Side/Bottom 모두 출현)
    - `MapPc1Resources` 에 `CAMERA_BOTTOM`, `SEQ_BOTTOM`, `LIGHT_BOTTOM` 가 ESite.Side 슬롯에 매핑 (grep `ESite.Side, DeviceHandler.CAMERA_BOTTOM` 또는 동등)
    - `ResolveSiteSlot` 헬퍼 존재, Site2→ESite.Side 반환 (grep `ResolveSiteSlot`)
    - SetIdentifier 에 `UseProtocolV1` 분기 존재, v2.6 캐스팅 `(ESite)testPacket.Site` 보존 (grep)
    - 신규 메서드(InitializeV1/MapPc1/MapPc2/ResolveSiteSlot/SetIdentifier 신규 분기)에 삼항 `?:`·null병합 `??` 0건 (grep `?:`, `??` 범위)
    - framework `WPF_Example/TcpServer/ResourceMap.cs` git diff 0 (무수정)
    - 솔루션 빌드 PASS, 신규 error/warning 0
  </acceptance_criteria>
  <done>ResourceMap 2-PC 분기(EPcRole + InitializeV1/V26 + MapPc1/Pc2 + SetIdentifier v1.0 Site 변환), framework 무수정, 빌드 PASS, v2.6 회귀 0.</done>
</task>

<task type="auto">
  <name>Task 2: VisionServer Port 7701 + UTF-8 인코딩 (v1.0 전송 계층)</name>
  <read_first>
    - WPF_Example/TcpServer/VisionServer.cs (수정 대상 — lines 8-25 생성자)
    - WPF_Example/TcpServer/TcpServer.cs (수정 대상 — lines 68-76 EncodingType, lines 319-352 생성자/PortNum)
    - .planning/phases/48-protocol-v1-test-result-site-material/48-PATTERNS.md (VisionServer 섹션 lines 265-300)
    - .planning/phases/48-protocol-v1-test-result-site-material/48-CONTEXT.md (D-04/D-06)
    - .planning/refs/Vision-Protocol-v1.0.md (lines 7-9 Port 7701/UTF-8/@ 종료)
    - .planning/refs/control-sequence-coding-guideline.md (전체)
  </read_first>
  <files>WPF_Example/TcpServer/VisionServer.cs, WPF_Example/TcpServer/TcpServer.cs</files>
  <action>
**문제:** `TcpServer.EncodingType` 은 `private static`(line 76) 이라 파생 VisionServer 가 설정 불가. 또한 `TcpServer()` 생성자가 `mListener.Start()` 를 **base 생성자 안에서 즉시** 호출하므로(line 344), VisionServer 생성자 본문(base() 이후)에서 Port 를 바꿔도 이미 listen 이 시작된 뒤다. 따라서 Port 결정은 base 생성자가 ServerPort 를 읽는 지점(line 341)에서 v1.0 분기해야 한다.

**(a) TcpServer 인코딩 setter 노출.** `EncodingType` private setter 를 파생 클래스가 설정할 수 있도록 `protected static` 메서드를 추가한다 (속성 자체는 캡슐화 유지). D-00 헝가리언 + 동사+목적어 함수명:

```csharp
// 260622 hbk Phase 48 PROTO-01: 파생 클래스(VisionServer)가 v1.0 UTF-8 인코딩을 설정하는 진입점.
protected static void ApplyEncoding(MessageEncodingType eEncoding)
{
    EncodingType = eEncoding;
}
```

**(b) TcpServer 생성자 Port 결정 v1.0 분기.** 생성자(line 340-352)에서 `PortNum = SystemSetting.Handle.ServerPort;`(line 341) 를 v1.0-aware 로 교체. **base 생성자가 listen 전에 올바른 포트를 잡도록** 여기서 분기. D-00 헝가리언 + if/else + 조건 변수화, 매직넘버 회피(포트는 SystemSetting 명명 속성 경유):

```csharp
// 260622 hbk Phase 48 PROTO-01: v1.0 활성 시 ServerPortV1(7701), 아니면 기존 ServerPort(2505). D-04/D-06.
bool bUseV1 = SystemSetting.Handle.UseProtocolV1;
if (bUseV1)
{
    PortNum = SystemSetting.Handle.ServerPortV1;
}
else
{
    PortNum = SystemSetting.Handle.ServerPort;
}
```

> 이 분기는 TcpServer 생성자에 들어가지만 TcpServer 는 v2.6 기본 false 면 기존 동작 그대로(ServerPort=2505) → 회귀 0. UseProtocolV1 은 Plan 01 에서 추가됨(빌드 의존 순서 OK — depends_on: 48-01).

**(c) VisionServer 생성자에서 UTF-8 적용.** VisionServer 생성자(lines 18-21)에 v1.0 인코딩 적용 추가. base() 실행 후이지만 인코딩은 메시지 송수신 시점(GetBytes 호출)에 읽히므로 생성자 본문에서 설정해도 유효. D-00 헝가리언 + if/else:

```csharp
public VisionServer() : base() {
    Header = (byte)MSG_STX;
    Trailer = (byte)MSG_ETX;          // '@' 종료 (이미 적용)
    // 260622 hbk Phase 48 PROTO-01: v1.0 활성 시 UTF-8 인코딩 강제 (D-04). v2.6 면 기존 Default 유지.
    bool bUseV1 = SystemSetting.Handle.UseProtocolV1;
    if (bUseV1)
    {
        ApplyEncoding(MessageEncodingType.Utf8);
    }
}
```

> `using ReringProject.Setting;` 가 VisionServer.cs/TcpServer.cs 에 필요하면 추가 (TcpServer.cs 는 이미 SystemSetting 사용 중 — line 341). VisionServer.cs 에 SystemSetting 참조 추가 필요 시 using 추가.
  </action>
  <verify>
    <automated>solution 빌드 (msbuild Debug/x64) — 신규 error/warning 0</automated>
  </verify>
  <acceptance_criteria>
    - `TcpServer.cs` 에 `ApplyEncoding(MessageEncodingType` setter 메서드 정의 존재 (grep `ApplyEncoding`)
    - `TcpServer` 생성자에 `UseProtocolV1` 분기로 `ServerPortV1` 선택 경로 존재 (grep `ServerPortV1`)
    - `VisionServer` 생성자에 `UseProtocolV1` true 시 `ApplyEncoding(MessageEncodingType.Utf8)` 호출 존재 (grep `Utf8`)
    - 신규 분기 코드에 삼항 `?:`·null병합 `??` 0건 (grep `?:`, `??` 범위)
    - v2.6 경로 보존: UseProtocolV1=false 시 PortNum=ServerPort(2505), 인코딩 Default 유지 (grep `ServerPort` else 분기)
    - 솔루션 빌드 PASS, 신규 error/warning 0
  </acceptance_criteria>
  <done>TcpServer ApplyEncoding setter + Port v1.0 분기(7701) + VisionServer UTF-8 적용, v2.6 회귀 0, 빌드 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 제어 PC(Client) → Vision Server TCP | Site 번호(정수)가 신뢰 경계를 넘어옴. 범위 밖 Site(0, 3, 음수) 가능. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04 | Denial of Service | ResourceMap.ResolveSiteSlot / SetIdentifier | mitigate | v1.0 Site 변환은 Site2 외 모든 값을 ESite.Top 슬롯으로 폴백 → `Dictionary[ESite]` KeyNotFoundException 회피. 범위 밖 Site 도 안전한 기본 슬롯으로 처리. |
| T-48-05 | Tampering | ResourceMap.Find (v2.6 캐스팅) | accept | v2.6 분기는 기존 `(ESite)testPacket.Site` 캐스팅 보존 — 기존 동작과 동일 위험 수준(변경 없음). v1.0 분기가 ResolveSiteSlot 으로 견고화. 산업용 로컬 신뢰망. |
| T-48-06 | Information Disclosure | VisionServer 포트 7701 노출 | accept | 포트 변경은 규격 준수(엑셀 7701). 로컬 신뢰망, IP 로 PC 구분. 인증/암호화는 본 프로토콜 범위 밖(제어팀 동기화 — Phase 50). |

high severity 미해결 위협: 0건.
</threat_model>

<verification>
- msbuild Debug/x64 PASS, 신규 error/warning 0 (v2.6 회귀 0).
- grep: `EPcRole`, `InitializeV1`, `InitializeV26`, `MapPc1Resources`, `MapPc2Resources`, `ResolveSiteSlot`, `ApplyEncoding`, `ServerPortV1`, `Utf8` 모두 존재.
- git diff: framework `WPF_Example/TcpServer/ResourceMap.cs` 변경 0.
- grep: 신규 제어 코드 삼항/`??` 0건.
- 코드 경로 추적: UseProtocolV1=true + PcRole=1 → Site1→TOP 시퀀스/Site2→BOTTOM 시퀀스; PcRole=2 → Site1/Site2→SIDE 시퀀스. UseProtocolV1=false → 기존 Top/Side/Bottom.
</verification>

<success_criteria>
- ResourceMap 2-PC 재정합: PcRole(1/2)로 Site→자원 매핑 동적 결정, 재빌드 불필요 (D-03).
- PC1: Site1=TOP/Site2=BOTTOM, PC2: Site1=SIDE/Site2=SIDE (D-04).
- VisionServer v1.0: UTF-8 + Port 7701 (`@` 종료는 기존 MSG_ETX).
- v2.6 경로(매핑/포트/인코딩) 전부 UseProtocolV1=false 분기로 보존 (회귀 0).
- framework ResourceMap.cs 무수정. 신규 제어 코드 D-00 준수.
</success_criteria>

<output>
완료 후 `.planning/phases/48-protocol-v1-test-result-site-material/48-02-SUMMARY.md` 생성.
SUMMARY 에 기록: (1) ESite 슬롯 재사용 설계(Site1=ESite.Top/Site2=ESite.Side, 자원은 PcRole 결정) 근거 — framework Dictionary<ESite> 키 호환, (2) PC2 Site1/Site2 동일 SIDE 자원 공유 결정, (3) v1.0 은 항상 ETestType.Inspection action 매핑, (4) Port 결정을 TcpServer 생성자에서 분기한 이유(base 생성자 listen 즉시 시작).
</output>
