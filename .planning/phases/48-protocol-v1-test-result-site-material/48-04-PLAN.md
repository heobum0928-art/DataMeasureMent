---
phase: 48-protocol-v1-test-result-site-material
plan: 04
type: execute
wave: 2
depends_on: [48-01]
files_modified:
  - WPF_Example/UI/ViewModel/CycleResultDto.cs
  - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Utility/CaptureImageSaveService.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - WPF_Example/Custom/Export/ExcelExportService.cs
autonomous: true
requirements: [PROTO-01]
must_haves:
  truths:
    - "TestPacket.IndexNumber(자재번호)가 InspectionSequence 검사 결과를 통해 CycleResultDto.IndexNumber 로 전파된다"
    - "자재번호(>=0)가 캡쳐/원본 이미지 파일명에 _M{번호} 형태로 포함된다 (미수신 -1 이면 생략)"
    - "자재번호가 xlsx export 메타 헤더에 '자재번호' 행으로 기록된다 (미수신 -1 이면 '-')"
    - "자재번호 미전파 경로(수동/Batch/Repeat)에서도 기본값 -1 로 안전 동작한다 (회귀 0)"
  artifacts:
    - path: "WPF_Example/UI/ViewModel/CycleResultDto.cs"
      provides: "CycleResultDto.IndexNumber(int=-1) 필드"
      contains: "IndexNumber"
    - path: "WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs"
      provides: "BuildDto 에 nIndexNumber optional 파라미터(-1 기본) + dto 대입"
      contains: "nIndexNumber"
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "AddResponse 에서 RequestPacket.IndexNumber 를 BuildDto 로 전달"
      contains: "IndexNumber"
    - path: "WPF_Example/Utility/CaptureImageSaveService.cs"
      provides: "BuildFileName 자재번호 오버로드 (_M{번호} 삽입, -1 생략)"
      contains: "nIndexNumber"
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "QueueFaiCapture 가 부모 시퀀스 RequestPacket.IndexNumber 를 파일명에 전파"
      contains: "IndexNumber"
    - path: "WPF_Example/Custom/Export/ExcelExportService.cs"
      provides: "xlsx 메타 헤더 '자재번호' 행 + 테이블 헤더 오프셋 조정"
      contains: "자재번호"
  key_links:
    - from: "InspectionSequence.AddResponse"
      to: "CycleResultSerializer.BuildDto(nIndexNumber)"
      via: "RequestPacket.IndexNumber 추출 후 전달"
      pattern: "RequestPacket.*IndexNumber"
    - from: "Action_FAIMeasurement.QueueFaiCapture"
      to: "CaptureImageSaveService.BuildFileName(...nIndexNumber)"
      via: "ShotParam.Parent as InspectionSequence → RequestPacket.IndexNumber"
      pattern: "BuildFileName"
    - from: "CycleResultDto.IndexNumber"
      to: "ExcelExportService 자재번호 행"
      via: "cycle.IndexNumber → ws.Cell 자재번호"
      pattern: "IndexNumber"
---

<objective>
자재번호(IndexNumber, Plan 01 의 `TestPacket.IndexNumber`)를 **결과 저장(파일명 + xlsx export)까지 한 번에 전파**한다 (D-05). 전파 경로:

```
TestPacket.IndexNumber (Plan 01)
  → SequenceBase.RequestPacket.IndexNumber (TestPacket, 기존 멤버)
  → [경로 A: xlsx] InspectionSequence.AddResponse → BuildDto(nIndexNumber) → CycleResultDto.IndexNumber → ExcelExportService 자재번호 행
  → [경로 B: 파일명] Action_FAIMeasurement.QueueFaiCapture → BuildFileName(...nIndexNumber) → _M{번호} 파일명
```

"자재가 몇 번인지" 가 데이터 저장(파일명/xlsx)에 기록되어 데이터 식별이 원활해진다.

Purpose: 자재 IndexNumber 전파 = phase 핵심 추가 요구(정식 ID 미부여, ROADMAP "자재 IndexNumber 가 결과 저장(파일명/xlsx)에 기록됨"). Plan 01 의 `TestPacket.IndexNumber` 에 의존(Wave 2). PROTO-01 묶음(z_index/자재번호 파싱·전파).
Output: CycleResultDto.IndexNumber + BuildDto 파라미터 + AddResponse 전달 + BuildFileName 오버로드 + QueueFaiCapture 전파 + xlsx 자재번호 행.

🔒 **신규/수정 코드 스타일:** CaptureImageSaveService.BuildFileName 오버로드, QueueFaiCapture 자재번호 추출은 제어/저장 경계 코드 — `control-sequence-coding-guideline.md`(D-00) 의 헝가리언 + `if/else`(삼항·`??`·`?.` 금지) + 매직넘버 금지를 적용한다. CycleResultDto/CycleResultSerializer/ExcelExportService 는 기존 비-제어 코드 스타일(CLAUDE.md, 삼항 허용)을 유지하되, **자재번호 신규 분기는 일관성 위해 if/else 권장**(48-PATTERNS.md lines 532-535 지침).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/48-protocol-v1-test-result-site-material/48-CONTEXT.md
@.planning/phases/48-protocol-v1-test-result-site-material/48-PATTERNS.md
@.planning/refs/control-sequence-coding-guideline.md

<interfaces>
<!-- executor 가 직접 사용. 코드베이스 재탐색 불필요. -->

TestPacket.IndexNumber (Plan 01 산출, WPF_Example/TcpServer/VisionRequestPacket.cs):
```csharp
public int IndexNumber { get; set; } = -1;   // SENTINEL_NO_MATERIAL; 미수신/null/파싱실패 = -1
```

SequenceBase.RequestPacket (WPF_Example/Sequence/Sequence/SequenceBase.cs line 69) — 자재번호 전파 출발점:
```csharp
public TestPacket RequestPacket { get; private set; } = null;   // Start/StartAll 에서 set, Finish 후 null
```

현행 CycleResultDto (WPF_Example/UI/ViewModel/CycleResultDto.cs lines 11-26):
```csharp
public class CycleResultDto {
    public DateTime InspectionTime { get; set; }
    public string RecipeName { get; set; }
    public string OverallJudgement { get; set; }
    public string CycleFolderPath { get; set; }
    public List<ShotResultDto> Shots { get; set; } = new List<ShotResultDto>();
}
```

현행 BuildDto 시그니처 (WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs lines 35-50):
```csharp
public static CycleResultDto BuildDto(
    InspectionRecipeManager recipeManager, EVisionResultType cycleResult,
    DateTime when, string recipeName, string ownerSequenceName = null)
{
    var dto = new CycleResultDto { InspectionTime = when, RecipeName = recipeNameStr,
        OverallJudgement = MapJudgement(cycleResult) };
    ...
}
```
BuildDto 호출부 4곳:
- InspectionSequence.cs line 141 (AddResponse — TCP 경로, RequestPacket 접근 가능) ← 자재번호 전달 지점
- InspectionSequence.cs line 166 (HandleManualCyclePersist — 수동, RequestPacket==null 가드됨) ← -1 기본
- BatchRunService.cs line 127 (RequestPacket 없음) ← -1 기본
- RepeatRunService.cs line 203 (RequestPacket 없음) ← -1 기본

InspectionSequence.AddResponse BuildDto 호출 (WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs lines 139-147):
```csharp
var cycleDto = CycleResultSerializer.BuildDto(
    recipeManager, responsePacket.Result, System.DateTime.Now,
    SystemHandler.Handle.Setting.CurrentRecipeName, Name);
CycleResultSerializer.SaveAsync(cycleDto);
```
> AddResponse 는 InspectionSequence(: SequenceBase) 인스턴스 메서드 → `RequestPacket`(TestPacket) 직접 접근 가능. RequestPacket 은 이 시점 non-null(TCP 경로).

현행 BuildFileName (WPF_Example/Utility/CaptureImageSaveService.cs lines 174-184):
```csharp
public static string BuildFileName(string prefix, string sequence, string faiName,
    string measurePointSegment, string judgement, DateTime ts) {
    string seq = SanitizeFilePart(sequence, "SEQ");
    string fai = SanitizeFilePart(faiName, "FAI");
    string seg = SanitizeFilePart(measurePointSegment, "");
    string judge = SanitizeFilePart(judgement, "");
    string time = ts.ToString("HHmmssfff");
    string name = prefix + "_" + seq + "_" + fai;
    if (!string.IsNullOrEmpty(seg)) { name += "_" + seg; }
    if (!string.IsNullOrEmpty(judge)) { name += "_" + judge; }
    return name + "_" + time + ".jpg";
}
```

현행 QueueFaiCapture (WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs lines 592-605) — 호출은 line 395:
```csharp
private void QueueFaiCapture(FAIConfig fai, SharedHImage sharedSrc, List<EdgeInspectionOverlay> faiOverlays, List<DatumCaptureOverlay> datumSnapshot, string sequenceName) {
    if (fai == null) return;
    ...
    string originName = CaptureImageSaveService.BuildFileName("origin", sequenceName, fai.FAIName, seg, judge, ts);
    string captureName = CaptureImageSaveService.BuildFileName("capture", sequenceName, fai.FAIName, seg, judge, ts);
    fai.LastOriginImageFileName = CaptureImageSaveService.BuildFilePath(false, originName, ts);
    fai.LastCaptureImageFileName = CaptureImageSaveService.BuildFilePath(true, captureName, ts);
    ...
}
```
부모 시퀀스 접근 패턴 (이미 Action_FAIMeasurement 내 사용 중, lines 83/393):
```csharp
InspectionSequence parentSeq;
if (ShotParam != null) parentSeq = ShotParam.Parent as InspectionSequence;
else parentSeq = null;
```

현행 ExcelExportService 메타 헤더 (WPF_Example/Custom/Export/ExcelExportService.cs lines 35-52):
```csharp
ws.Cell(1, 1).Value = "모델명";
ws.Cell(1, 2).Value = cycle.RecipeName != null ? cycle.RecipeName : "";
ws.Cell(2, 1).Value = "검사일시";
ws.Cell(2, 2).Value = cycle.InspectionTime.ToString("yyyy-MM-dd HH:mm:ss");
ws.Cell(3, 1).Value = "종합판정";
ws.Cell(3, 2).Value = cycle.OverallJudgement != null ? cycle.OverallJudgement : "";
int hr = 5;   // 테이블 헤더 행
string[] headers = { "Shot", "FAI", ... };
int row = hr + 1;
```
> RepeatExcelExportService 도 존재하나 본 plan 범위 밖(반복도 export). cycle export(ExcelExportService) 만 자재번호 행 추가.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CycleResultDto.IndexNumber 필드 + BuildDto 파라미터 + AddResponse 전파</name>
  <read_first>
    - WPF_Example/UI/ViewModel/CycleResultDto.cs (수정 대상 — lines 11-26)
    - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs (수정 대상 — lines 35-55 BuildDto 진입)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (수정 대상 — lines 139-147 AddResponse BuildDto 호출)
    - .planning/phases/48-protocol-v1-test-result-site-material/48-PATTERNS.md (CycleResultDto 섹션 lines 438-459 + 전파 체인 lines 555-617)
    - .planning/phases/48-protocol-v1-test-result-site-material/48-CONTEXT.md (D-05)
  </read_first>
  <files>WPF_Example/UI/ViewModel/CycleResultDto.cs, WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs, WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <action>
**(a) CycleResultDto 에 IndexNumber 필드 추가** (RecipeName line 16 패턴 따라, lines 11-26). 기본값 -1 = 미수신 sentinel (TestPacket.IndexNumber 와 일관):

```csharp
// 260622 hbk Phase 48 PROTO-01: 자재번호 (TestPacket.IndexNumber 에서 전파). -1 = 미수신(sentinel).
public int IndexNumber { get; set; } = -1;
```

**(b) BuildDto 에 nIndexNumber optional 파라미터 추가** (CycleResultSerializer.cs lines 35-50). **기존 호출부 4곳 무수정 호환 위해 끝에 optional 추가(기본 -1).** D-00 헝가리언:

```csharp
public static CycleResultDto BuildDto(
    InspectionRecipeManager recipeManager,
    EVisionResultType cycleResult,
    DateTime when,
    string recipeName,
    string ownerSequenceName = null,
    int nIndexNumber = -1)        // 260622 hbk Phase 48 PROTO-01: 자재번호 전파 (기본 -1 미수신)
{
    string recipeNameStr = recipeName;
    if (recipeNameStr == null) recipeNameStr = "";
    var dto = new CycleResultDto
    {
        InspectionTime = when,
        RecipeName = recipeNameStr,
        OverallJudgement = MapJudgement(cycleResult),
        IndexNumber = nIndexNumber      // 260622 hbk Phase 48 PROTO-01
    };
    ...  // 이하 기존 본문 무변경
}
```

**(c) InspectionSequence.AddResponse 에서 RequestPacket.IndexNumber 전달** (lines 139-147). AddResponse 는 SequenceBase 상속 인스턴스 메서드 → `RequestPacket` 직접 접근. **D-00 헝가리언 + if/else (null 체크 `?.` 금지 → if/else):**

```csharp
// 260622 hbk Phase 48 PROTO-01: 자재번호 추출 (RequestPacket = 이번 TCP TEST 패킷). null 이면 -1 폴백.
int nIndexNumber = -1;
bool bHasRequest = RequestPacket != null;
if (bHasRequest)
{
    nIndexNumber = RequestPacket.IndexNumber;
}

var cycleDto = CycleResultSerializer.BuildDto(
    recipeManager,
    responsePacket.Result,
    System.DateTime.Now,
    SystemHandler.Handle.Setting.CurrentRecipeName,
    Name,
    nIndexNumber);     // 260622 hbk Phase 48 PROTO-01: 자재번호 전파
CycleResultSerializer.SaveAsync(cycleDto);
```

> 다른 BuildDto 호출부(HandleManualCyclePersist line 166, BatchRunService line 127, RepeatRunService line 203)는 **무수정** — optional 기본 -1 적용(수동/배치/반복 경로는 자재번호 없음, 회귀 0). HandleManualCyclePersist 는 `RequestPacket != null` 시 early-return 이므로 자재번호 전파 대상 아님(TCP 경로는 AddResponse 가 처리).
  </action>
  <verify>
    <automated>solution 빌드 (msbuild Debug/x64) — 신규 error/warning 0</automated>
  </verify>
  <acceptance_criteria>
    - `CycleResultDto` 에 `public int IndexNumber` 필드 정의, 기본값 `= -1` (grep `IndexNumber`)
    - `BuildDto` 시그니처에 `int nIndexNumber = -1` 파라미터 존재, dto 에 `IndexNumber = nIndexNumber` 대입 (grep `nIndexNumber`)
    - `InspectionSequence.AddResponse` 가 `RequestPacket.IndexNumber` 를 추출해 BuildDto 6번째 인자로 전달 (grep `RequestPacket` + `IndexNumber` 동일 메서드)
    - RequestPacket null 체크가 `?.`(null조건) 아닌 `if (RequestPacket != null)` 형태 (grep — AddResponse 신규 코드에 `?.` 0건)
    - 다른 BuildDto 호출부 3곳(HandleManualCyclePersist/BatchRunService/RepeatRunService) 시그니처 변경 없음 — optional 기본값으로 컴파일 (빌드 PASS 로 검증)
    - 솔루션 빌드 PASS, 신규 error/warning 0
  </acceptance_criteria>
  <done>CycleResultDto.IndexNumber + BuildDto nIndexNumber 파라미터 + AddResponse 자재번호 전달. 다른 호출부 무수정 호환. 빌드 PASS.</done>
</task>

<task type="auto">
  <name>Task 2: BuildFileName 자재번호 오버로드 + QueueFaiCapture 전파 (파일명 경로)</name>
  <read_first>
    - WPF_Example/Utility/CaptureImageSaveService.cs (수정 대상 — lines 167-184 BuildFileName)
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (수정 대상 — lines 83-84 parentSeq 패턴, lines 388-395 QueueFaiCapture 호출, lines 592-606 QueueFaiCapture 본문)
    - .planning/phases/48-protocol-v1-test-result-site-material/48-PATTERNS.md (CaptureImageSaveService 섹션 lines 463-513 + 전파 체인 lines 572-588)
    - .planning/phases/48-protocol-v1-test-result-site-material/48-CONTEXT.md (D-05)
    - .planning/refs/control-sequence-coding-guideline.md (전체)
  </read_first>
  <files>WPF_Example/Utility/CaptureImageSaveService.cs, WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <action>
**(a) BuildFileName 자재번호 오버로드 추가** (CaptureImageSaveService.cs lines 174-184 아래). **기존 6-인자 시그니처 보존(무수정) + 7번째 자재번호 받는 오버로드 신설.** 자재번호 -1 이면 파일명에 미포함. SanitizeFilePart 패턴 재사용. **D-00 헝가리언 + if/else, 매직넘버 회피(sentinel 상수):**

```csharp
// 260622 hbk Phase 48 PROTO-01: 자재번호 포함 파일명 오버로드. nIndexNumber >= 0 이면 _M{번호} 삽입, -1 이면 생략.
//  prefix_seq_fai[_M{자재번호}][_seg][_judge]_time.jpg — 자재번호는 FAI 앞(식별 우선).
private const int FILENAME_NO_MATERIAL = -1;

public static string BuildFileName(string prefix, string sequence, string faiName, string measurePointSegment, string judgement, DateTime ts, int nIndexNumber) {
    string seq = SanitizeFilePart(sequence, "SEQ");
    string fai = SanitizeFilePart(faiName, "FAI");
    string seg = SanitizeFilePart(measurePointSegment, "");
    string judge = SanitizeFilePart(judgement, "");
    string time = ts.ToString("HHmmssfff");

    string szMat = "";
    bool bHasMaterial = nIndexNumber > FILENAME_NO_MATERIAL;
    if (bHasMaterial)
    {
        szMat = nIndexNumber.ToString();
    }

    string name = prefix + "_" + seq + "_" + fai;
    if (!string.IsNullOrEmpty(szMat)) { name += "_M" + szMat; }
    if (!string.IsNullOrEmpty(seg)) { name += "_" + seg; }
    if (!string.IsNullOrEmpty(judge)) { name += "_" + judge; }
    return name + "_" + time + ".jpg";
}
```

> 기존 6-인자 BuildFileName(lines 174-184)은 **그대로 둔다** — 다른 호출부 호환. 신규 오버로드만 추가.

**(b) QueueFaiCapture 가 자재번호를 추출해 7-인자 오버로드 호출** (Action_FAIMeasurement.cs lines 592-605). QueueFaiCapture 내부에서 부모 시퀀스 RequestPacket.IndexNumber 추출 (lines 83-84 패턴 재사용). **D-00 헝가리언 + if/else (`?.` 금지):**

QueueFaiCapture 본문 상단에 자재번호 추출 추가:
```csharp
private void QueueFaiCapture(FAIConfig fai, SharedHImage sharedSrc, List<EdgeInspectionOverlay> faiOverlays, List<DatumCaptureOverlay> datumSnapshot, string sequenceName) {
    if (fai == null) return;
    var saver = SystemHandler.Handle.CaptureImageSaver;
    DateTime ts = DateTime.Now;
    string seg = OverlayCaptureRenderer.BuildMeasurePointSegment(faiOverlays);
    string judge;
    if (fai.IsPass) judge = "OK";
    else judge = "NG";

    // 260622 hbk Phase 48 PROTO-01: 자재번호 추출 — 부모 시퀀스 RequestPacket(TCP TEST 패킷). null 이면 -1.
    int nIndexNumber = -1;
    InspectionSequence parentSeq;
    if (ShotParam != null) parentSeq = ShotParam.Parent as InspectionSequence;
    else parentSeq = null;
    bool bHasRequest = parentSeq != null && parentSeq.RequestPacket != null;
    if (bHasRequest)
    {
        nIndexNumber = parentSeq.RequestPacket.IndexNumber;
    }

    string originName = CaptureImageSaveService.BuildFileName("origin", sequenceName, fai.FAIName, seg, judge, ts, nIndexNumber);
    string captureName = CaptureImageSaveService.BuildFileName("capture", sequenceName, fai.FAIName, seg, judge, ts, nIndexNumber);
    fai.LastOriginImageFileName = CaptureImageSaveService.BuildFilePath(false, originName, ts);
    fai.LastCaptureImageFileName = CaptureImageSaveService.BuildFilePath(true, captureName, ts);
    ...  // 이하 saver enqueue 블록 무변경
}
```

> `parentSeq.RequestPacket` 접근 가능 여부: RequestPacket 은 SequenceBase public 속성(get; private set;) — 외부 읽기 가능. InspectionSequence 캐스팅 후 접근 OK. ShotParam.Parent 가 null 이거나 InspectionSequence 아니면 -1 폴백(파일명에 자재번호 생략) — 회귀 0.
  </action>
  <verify>
    <automated>solution 빌드 (msbuild Debug/x64) — 신규 error/warning 0</automated>
  </verify>
  <acceptance_criteria>
    - `CaptureImageSaveService` 에 7-인자 BuildFileName 오버로드(`...DateTime ts, int nIndexNumber`) 정의 존재 (grep `int nIndexNumber`)
    - 기존 6-인자 BuildFileName 시그니처 보존(grep — `DateTime ts) {` 6-인자 버전 잔존)
    - 자재번호 오버로드가 `nIndexNumber > FILENAME_NO_MATERIAL` 시 `_M` + 번호 삽입 (grep `"_M"`)
    - 매직넘버 -1 대신 명명 상수 `FILENAME_NO_MATERIAL` 사용 (grep)
    - `QueueFaiCapture` 가 `parentSeq.RequestPacket.IndexNumber` 추출 후 7-인자 BuildFileName 호출 (grep `RequestPacket` + `IndexNumber` in QueueFaiCapture)
    - null 체크가 `?.` 아닌 `if (parentSeq != null && parentSeq.RequestPacket != null)` 형태 (grep — QueueFaiCapture 신규 코드 `?.` 0건)
    - 신규 코드(BuildFileName 오버로드 + QueueFaiCapture 자재번호 블록)에 삼항 `?:`·null병합 `??` 0건 (grep `?:`, `??` 범위)
    - 솔루션 빌드 PASS, 신규 error/warning 0
  </acceptance_criteria>
  <done>BuildFileName 자재번호 오버로드(_M{번호}, -1 생략) + QueueFaiCapture 부모 RequestPacket.IndexNumber 전파. 기존 6-인자 보존. 빌드 PASS.</done>
</task>

<task type="auto">
  <name>Task 3: xlsx export 자재번호 메타 헤더 행 추가</name>
  <read_first>
    - WPF_Example/Custom/Export/ExcelExportService.cs (수정 대상 — lines 35-52 메타 헤더 + 테이블 헤더 오프셋)
    - .planning/phases/48-protocol-v1-test-result-site-material/48-PATTERNS.md (ExcelExportService 섹션 lines 518-551)
    - .planning/phases/48-protocol-v1-test-result-site-material/48-CONTEXT.md (D-05)
  </read_first>
  <files>WPF_Example/Custom/Export/ExcelExportService.cs</files>
  <action>
**메타 헤더 블록(lines 35-52)에 자재번호 행 추가 + 테이블 헤더 오프셋 조정.** 현행 행 1=모델명, 2=검사일시, 3=종합판정, 5=테이블 헤더. 자재번호를 행 4로 추가하고 테이블 헤더를 행 6으로 이동. **자재번호 신규 분기는 if/else 권장(48-PATTERNS.md lines 532-535):**

현행:
```csharp
ws.Cell(3, 1).Value = "종합판정";
ws.Cell(3, 2).Value = cycle.OverallJudgement != null ? cycle.OverallJudgement : "";

// D-05 테이블 헤더 (행 5)
int hr = 5;
```

교체:
```csharp
ws.Cell(3, 1).Value = "종합판정";
ws.Cell(3, 2).Value = cycle.OverallJudgement != null ? cycle.OverallJudgement : "";

// 260622 hbk Phase 48 PROTO-01: 자재번호 메타 행 (D-05). -1 = 미수신 → "-".
ws.Cell(4, 1).Value = "자재번호";
if (cycle.IndexNumber >= 0)
{
    ws.Cell(4, 2).Value = cycle.IndexNumber;
}
else
{
    ws.Cell(4, 2).Value = "-";
}

// D-05 테이블 헤더 (행 6 — 자재번호 행 추가로 5→6 이동)
int hr = 6;
```

> `hr` 변경(5→6)으로 테이블 데이터 행(`row = hr + 1`)도 자동 이동 — 헤더/데이터 오프셋 일관 유지. 테이블 헤더 배열·루프(headers, foreach)는 무변경 (hr 변수 참조).
> 다른 export 경로(RepeatExcelExportService)는 본 plan 범위 밖 — cycle export(ExcelExportService.Export)만 자재번호 행 추가.
  </action>
  <verify>
    <automated>solution 빌드 (msbuild Debug/x64) — 신규 error/warning 0</automated>
  </verify>
  <acceptance_criteria>
    - ExcelExportService 메타 헤더에 `"자재번호"` 셀(행 4) 추가 (grep `자재번호`)
    - `cycle.IndexNumber >= 0` 분기로 값 vs "-" 출력 (grep `cycle.IndexNumber`)
    - 테이블 헤더 오프셋 `int hr = 6` 으로 변경 (자재번호 행 추가 반영) (grep `int hr = 6`)
    - 테이블 헤더 배열(`headers`)·데이터 루프 무변경 (hr 변수 참조 유지)
    - 솔루션 빌드 PASS, 신규 error/warning 0
  </acceptance_criteria>
  <done>xlsx 메타 헤더 자재번호 행(행 4) + 테이블 헤더 행 6 이동. IndexNumber -1 시 "-". 빌드 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TestPacket.IndexNumber → 파일시스템(파일명) | 자재번호(int)가 파일명 세그먼트로 흐름. Plan 01 에서 이미 int 정규화(비정수→-1)되어 path traversal 문자 불가. |
| CycleResultDto.IndexNumber → xlsx 셀 | 자재번호(int)가 셀 값으로 흐름. int 이므로 수식 주입 불가. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-10 | Tampering | CaptureImageSaveService.BuildFileName (자재번호 세그먼트) | mitigate | 자재번호는 int(Plan 01 에서 비정수→-1 정규화) → `_M{int}` 만 파일명에 삽입. `/`,`\`,`..` 등 traversal 문자 불가. 기존 SanitizeFilePart 도 다른 세그먼트(seq/fai)에 적용 유지(T-40.2-01). |
| T-48-11 | Tampering | ExcelExportService 자재번호 셀 (CSV/수식 주입) | mitigate | cycle.IndexNumber 는 int → ws.Cell.Value 에 정수로 대입(`=`/`+` 등 수식 문자 불가). 미수신 시 리터럴 "-". 외부 문자열 미경유. |
| T-48-12 | Information Disclosure | 파일명/xlsx 자재번호 노출 | accept | 자재 식별은 저장 본연의 목적(데이터 추적). 로컬 디스크, 클라우드 미전송. |

high severity 미해결 위협: 0건.
</threat_model>

<verification>
- msbuild Debug/x64 PASS, 신규 error/warning 0 (수동/배치/반복 경로 회귀 0 — optional 기본 -1).
- grep: `CycleResultDto.IndexNumber`, `BuildDto nIndexNumber`, `RequestPacket.IndexNumber`(AddResponse+QueueFaiCapture), `BuildFileName int nIndexNumber`, `"_M"`, `"자재번호"`, `int hr = 6` 존재.
- grep: 신규 제어 경계 코드(BuildFileName 오버로드/QueueFaiCapture/AddResponse 자재번호 블록) 삼항/`??`/`?.` 0건.
- 전파 경로 추적: TestPacket.IndexNumber=42 → RequestPacket.IndexNumber=42 → (A) CycleResultDto.IndexNumber=42 → xlsx "자재번호" 행=42; (B) 파일명 `origin_TOP_FAI_A1_M42_..._OK_..._.jpg`.
- 미수신(-1) 경로: 파일명에 `_M` 미포함, xlsx "자재번호"="-".
</verification>

<success_criteria>
- 자재번호 전파 완성: TestPacket.IndexNumber → CycleResultDto.IndexNumber(xlsx) + 파일명(_M{번호}).
- xlsx 메타 헤더에 "자재번호" 행 기록 (미수신 -1 → "-").
- 캡쳐/원본 파일명에 _M{번호} 포함 (미수신 -1 → 생략).
- 수동/Batch/Repeat 경로 기본값 -1 안전 동작 (회귀 0).
- 신규 제어 경계 코드 D-00 준수 (헝가리언/if-else/명명상수/삼항·??·?.0건).
</success_criteria>

<output>
완료 후 `.planning/phases/48-protocol-v1-test-result-site-material/48-04-SUMMARY.md` 생성.
SUMMARY 에 기록: (1) 전파 2경로(xlsx via AddResponse/BuildDto, 파일명 via QueueFaiCapture) 연결 지점, (2) 파일명 자재번호 위치(_M{번호}, FAI 뒤 seg 앞) 결정, (3) 무수정 BuildDto 호출부 3곳(수동/배치/반복) 기본 -1, (4) xlsx 테이블 헤더 5→6 오프셋, (5) RepeatExcelExportService 미포함(범위 밖).
</output>
