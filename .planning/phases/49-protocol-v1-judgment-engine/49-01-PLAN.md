---
phase: 49-protocol-v1-judgment-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/TcpServer/VisionResponsePacket.cs
  - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
autonomous: true
requirements: [PROTO-05]
must_haves:
  truths:
    - "ECycleResult enum 이 { Buffer, Pass, Fail } 3 멤버로 존재한다"
    - "ShotConfig 가 z_index 를 보유하여 어떤 Index 에 속한 Shot 인지 식별 가능하다"
    - "InspectionSequence 가 사이클 누적 상태(NG/Datum실패/현재·마지막 Index)를 멤버로 보유한다"
    - "레시피 Shot 들의 z_index 최댓값(=마지막 Index)을 산출하는 헬퍼가 존재한다"
    - "기존 v2.6/측정 동작 회귀 0 — 빌드 PASS"
  artifacts:
    - path: "WPF_Example/TcpServer/VisionResponsePacket.cs"
      provides: "ECycleResult enum { Buffer, Pass, Fail } (PROTO-05 enum 요구)"
      contains: "enum ECycleResult"
    - path: "WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs"
      provides: "ZIndex 멤버 (z_index↔Shot 매핑 기반, default 0)"
      contains: "ZIndex"
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "사이클 상태 멤버 m_bCycleHasNG/m_bCycleDatumFailed/m_nCurrentZIndex/m_nLastZIndex + ComputeLastZIndex 헬퍼"
      contains: "m_bCycleHasNG"
  key_links:
    - from: "InspectionSequence cycle 멤버"
      to: "AddResponse (49-02)"
      via: "멤버 필드 읽기/쓰기"
      pattern: "m_bCycleHasNG"
    - from: "ShotConfig.ZIndex"
      to: "AddResponse Index-scoped 필터 (49-02)"
      via: "z_index 비교"
      pattern: "ZIndex"
---

<objective>
Phase 49 판정 엔진의 **상태 토대**를 깐다. 코드 흐름(판정 로직)은 49-02에서 채우고, 본 plan 은 그 흐름이 읽고 쓸 enum·멤버·매핑 필드만 정의한다.

3가지를 만든다:
1. `ECycleResult { Buffer, Pass, Fail }` enum 1개 신설 (D-07, PROTO-05 enum 요구 충족) — VisionResponsePacket.cs.
2. `ShotConfig.ZIndex` 멤버 신설 (D-01 z_index↔Shot 매핑 기반) — 어떤 Index($TEST z_index)에 이 Shot 이 속하는지 식별. INI 하위 호환(default 0).
3. InspectionSequence 사이클 누적 상태 멤버 4개(D-02, 신규 클래스 미도입 — 기존 `_datumTransforms`/`_failedDatums` 와 동일 lifecycle) + 레시피 z_index 최댓값 산출 헬퍼(D-03).

Purpose: 49-02의 판정 흐름이 "상태를 어디서 읽나"를 구조적으로 확정하여, 49-02가 로직만 집중하게 한다.
Output: enum 1개, ShotConfig 멤버 1개, InspectionSequence 멤버 4개 + 헬퍼 메서드 1개.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-protocol-v1-judgment-engine/49-CONTEXT.md
@.planning/refs/control-sequence-coding-guideline.md
@.planning/refs/Vision-Protocol-v1.0.md

<interfaces>
<!-- 기존 코드에서 확인된 계약 (executor 가 탐색 없이 사용). -->

VisionResponsePacket.cs (기존):
  public enum EVisionResultType : int { NG=0, OK=1, NotExist=2, ANG=3, TECHING=4 }
  public class TestResultPacket : VisionResponsePacket {
    public EVisionResultType Result { get; set; }
    public int FAICount => FAIResults.Count;
    public bool IsBuffer { get; set; } = false;  // Phase 48 hook — Phase 49 가 채울 자리
  }

ShotConfig.cs (기존, ReringProject.Sequence 네임스페이스, Allman 스타일):
  public class ShotConfig : CameraSlaveParam, IOfflineImageParam
  public string ShotName { ... }
  public string OwnerSequenceName { get; set; } = "";
  public int DelayMs { get; set; }
  // ParamBase reflection 자동 INI 직렬화 — public 프로퍼티는 자동 저장/로드됨.

InspectionSequence.cs (기존, ReringProject.Sequence):
  private readonly Dictionary<string, HTuple> _datumTransforms = ...;
  private readonly HashSet<string> _failedDatums = ...;   // 요청 간 캐시 멤버 — 동일 lifecycle 에 신규 멤버 추가
  public TestPacket RequestPacket { get; ... }   // SequenceBase 상속 — RequestPacket.TestID = z_index 문자열, RequestPacket.IndexNumber = 자재번호
  var recipeManager = SystemHandler.Handle.Sequences.RecipeManager;  // recipeManager.Shots = List<ShotConfig>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ECycleResult enum 신설 (D-07, PROTO-05)</name>
  <files>WPF_Example/TcpServer/VisionResponsePacket.cs</files>
  <read_first>
    - WPF_Example/TcpServer/VisionResponsePacket.cs (라인 23~29 EVisionResultType enum 위치 확인 — 같은 패턴으로 형제 enum 추가)
    - .planning/refs/control-sequence-coding-guideline.md (enum/상수화 규칙)
    - .planning/phases/49-protocol-v1-judgment-engine/49-CONTEXT.md (D-07: enum 신설 범위 = ECycleResult 1개만)
  </read_first>
  <action>
VisionResponsePacket.cs 의 `EVisionResultType` enum(라인 23~29) **바로 아래**에 형제 enum 으로 `ECycleResult` 를 추가한다. 정확한 정의:

```csharp
//260623 hbk Phase 49 PROTO-05: 멀티샷 사이클 판정 결과 (D-07). enum 신설 범위 = 이 1개만.
//  Buffer = 중간 Index 진행 중(NG 포함 가능) / Pass = 마지막 Index 전체 OK / Fail = 마지막 Index NG 있음 or Datum 실패.
//  라이프사이클 상태는 멤버 bool(m_bCycleHasNG 등)로 표현 — A-2(멤버 상태) 결정과 일관. CycleState 라이프사이클 enum 미도입.
public enum ECycleResult : int {
    Buffer = 0,
    Pass = 1,
    Fail = 2,
}
```

이 enum 은 본 plan 에서 정의만 한다 — 소비(IsBuffer/Result 매핑)는 49-02. D-10 코딩지침: enum 멤버는 PascalCase 유지(타입명 접두사 E 는 기존 EVisionResultType 패턴 일치). 매직넘버 없음.
  </action>
  <verify>
    <automated>grep -n "enum ECycleResult" WPF_Example/TcpServer/VisionResponsePacket.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "enum ECycleResult" WPF_Example/TcpServer/VisionResponsePacket.cs` 가 1 출력
    - `grep -n "Buffer = 0" WPF_Example/TcpServer/VisionResponsePacket.cs` 매치 (3 멤버 Buffer/Pass/Fail 존재)
    - 변경 라인에 `//260623 hbk Phase 49` 주석 존재
  </acceptance_criteria>
  <done>ECycleResult { Buffer, Pass, Fail } enum 이 VisionResponsePacket.cs 에 존재.</done>
</task>

<task type="auto">
  <name>Task 2: ShotConfig.ZIndex 멤버 신설 (D-01 z_index↔Shot 매핑 기반)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs (라인 9~70 — ShotName/OwnerSequenceName/DelayMs public 프로퍼티 패턴 + Allman 스타일 + ParamBase 자동 직렬화 확인)
    - .planning/refs/control-sequence-coding-guideline.md (헝가리언 — 단, public 프로퍼티는 ParamBase reflection 직렬화 대상이라 INI 키 = 프로퍼티명. 기존 PascalCase 프로퍼티 컨벤션 보존 = INI 호환 우선)
    - .planning/refs/Vision-Protocol-v1.0.md (§Index Table — Idx0=Datum 샷 / 중간 / 마지막. z_index=Z축 인덱스, 0=Datum)
    - .planning/phases/49-protocol-v1-judgment-engine/49-CONTEXT.md (D-01: z_index 에 매핑된 Shot/FAI 그룹만 검사)
  </read_first>
  <action>
ShotConfig.cs 에 `ZIndex` public int 프로퍼티를 추가한다. 위치: `OwnerSequenceName`(라인 50) 바로 아래.

```csharp
//260623 hbk Phase 49 PROTO-03/05 (D-01): 이 Shot 이 속한 $TEST z_index. 0=Datum 샷, 1+=측정 Index.
//  AddResponse(49-02) 가 RequestPacket z_index 와 비교하여 해당 Index Shot 만 판정 집계(Index-scoped, D-01).
//  ParamBase reflection 자동 직렬화 — INI 키 = "ZIndex". 기존 레시피엔 키 부재 → 0 로드(=Datum/Idx0 폴백, 하위 호환).
//  ※ INI 호환 위해 PascalCase 프로퍼티명 유지(ParamBase 키=프로퍼티명) — 헝가리언 예외(직렬화 필드, D-10 적용범위 밖).
public int ZIndex { get; set; } = 0;
```

**중요 — 누락 키 0 로드 회귀 주의 (MEMORY reference_parambase_missing_key_zeroes_default):** 기존 레시피엔 ZIndex 키가 없어 ParamBase.Load 가 0 으로 채운다. 본 phase 에서 0 = "Index 0(Datum) 또는 미지정" 의미이므로 0 폴백이 **의도된 안전값**이다(별도 Load 오버라이드 불요). 단, 49-02 판정 흐름은 "ZIndex 미지정(전부 0) 레시피"에서도 회귀 없이 동작해야 한다 — 이 전제를 주석으로 못박는다(위 주석에 포함).

INI 미직렬화가 아님(직렬화 대상)을 명확히: 측정 결과 필드(WasDatumSkipped 등 runtime 전용)와 달리 ZIndex 는 레시피 구성값이므로 직렬화 대상이 맞다.
  </action>
  <verify>
    <automated>grep -n "public int ZIndex" WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public int ZIndex" WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs` 가 1 출력
    - 기본값 `= 0` 포함 (`grep -n "ZIndex { get; set; } = 0"` 매치)
    - 변경 라인에 `//260623 hbk Phase 49` 주석 존재
  </acceptance_criteria>
  <done>ShotConfig 가 ZIndex (default 0) 를 보유하고 ParamBase 로 자동 직렬화된다.</done>
</task>

<task type="auto">
  <name>Task 3: InspectionSequence 사이클 상태 멤버 4개 + ComputeLastZIndex 헬퍼 (D-02, D-03)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (라인 38~64 멤버 선언부 + 라인 86~163 AddResponse + 라인 219~236 ComputeOverallResult — 멤버 lifecycle/헬퍼 패턴 확인)
    - .planning/refs/control-sequence-coding-guideline.md (헝가리언 m_ 멤버 접두사 + 함수 30줄 한도 + 조건식 변수화)
    - .planning/phases/49-protocol-v1-judgment-engine/49-CONTEXT.md (D-02 멤버 상태 / D-03 z_index 최댓값 / Claude's Discretion: 멤버명 planner 재량 하 헝가리언 준수)
  </read_first>
  <action>
InspectionSequence.cs 에 사이클 누적 상태 멤버 4개를 추가한다. 위치: 라인 61 `_alignFailedDatums` 선언 바로 아래(요청 간 캐시 멤버 군집과 동일 위치 = 동일 lifecycle 명시).

```csharp
//260623 hbk Phase 49 PROTO-03/05 (D-02): 멀티샷 사이클 누적 상태 — 신규 클래스 미도입, _failedDatums 와 동일 lifecycle 멤버.
//  ClearDatumTransforms() 와 별도로 Index 0 수신 시 ResetCycleState() 로 리셋(D-08).
private bool m_bCycleHasNG = false;          // 사이클 중 NG 1건이라도 발견 → 마지막 Index 종합 F (D-02)
private bool m_bCycleDatumFailed = false;    // Index 0 Datum 검출 실패 → 즉시 F 마킹 (D-04/D-05)
private int m_nCurrentZIndex = 0;            // 이번 $TEST z_index (RequestPacket.TestID 파싱 결과)
private int m_nLastZIndex = 0;               // 레시피 Shot z_index 최댓값 = 마지막 Index (D-03, ComputeLastZIndex 산출)
```

그리고 D-03 "마지막 Index = 레시피 z_index 최댓값" 산출 헬퍼를 추가한다. 위치: ComputeOverallResult(라인 219~236) 바로 아래. 30줄 한도 준수, 헝가리언, if/else only, 삼항·null병합 금지:

```csharp
//260623 hbk Phase 49 PROTO-03 (D-03): 레시피 Shot 들의 z_index 최댓값 = "마지막 Index".
//  별도 설정/플래그 불필요 — 레시피 단일 진실원. PLC Index Table = 레시피 Shot 구성 일치 전제(D-03 코드 주석 고정).
//  이 시퀀스 소유 Shot 만 대상(Name == OwnerSequenceName) — Top/Bottom/Side 병렬 간섭 차단.
private int ComputeLastZIndex(InspectionRecipeManager recipeManager)
{
    int nMax = 0;
    bool bHasManager = recipeManager != null;
    if (!bHasManager)
    {
        return nMax;
    }
    foreach (var shot in recipeManager.Shots)
    {
        bool bIsNull = shot == null;
        if (bIsNull)
        {
            continue;
        }
        bool bOwnedByThisSeq = shot.OwnerSequenceName == Name;
        if (!bOwnedByThisSeq)
        {
            continue;
        }
        bool bIsLarger = shot.ZIndex > nMax;
        if (bIsLarger)
        {
            nMax = shot.ZIndex;
        }
    }
    return nMax;
}
```

추가로, 사이클 리셋 헬퍼도 본 plan 에서 정의한다(D-08 의 호출은 49-02, 정의는 여기 — 멤버와 함께 두어 응집). 위치: ComputeLastZIndex 바로 아래:

```csharp
//260623 hbk Phase 49 PROTO-05 (D-08): Index 0(Datum 샷) 수신 = 사이클 시작 → 누적 상태 클린 슬레이트.
//  비정상 종료(중단 F) 후에도 다음 사이클 시작이 항상 깨끗 — 마지막 Index 후 리셋(누락 위험) 미채택.
private void ResetCycleState()
{
    m_bCycleHasNG = false;
    m_bCycleDatumFailed = false;
    m_nCurrentZIndex = 0;
    m_nLastZIndex = 0;
}
```

본 plan 에서는 멤버·헬퍼 **정의만** 한다 — AddResponse 에서의 호출/판정 흐름은 49-02. (49-02 가 같은 파일을 수정하므로 wave 2 에 격리 — 파일 경합 회피.)
  </action>
  <verify>
    <automated>grep -n "m_bCycleHasNG\|ComputeLastZIndex\|ResetCycleState" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "m_bCycleHasNG" InspectionSequence.cs` >= 2 (선언 + ResetCycleState 대입)
    - `grep -c "m_bCycleDatumFailed" InspectionSequence.cs` >= 2
    - `grep -n "private int ComputeLastZIndex" InspectionSequence.cs` 매치
    - `grep -n "private void ResetCycleState" InspectionSequence.cs` 매치
    - 삼항 `?:` / null병합 `??` 신규 0건: `grep -nE "\?\?|\? .* :" InspectionSequence.cs` 결과에 본 plan 신규 라인 미포함
    - 빌드 PASS — msbuild Debug/x64, 0 errors, 0 new warnings (DatumMeasurement.exe 생성)
  </acceptance_criteria>
  <done>InspectionSequence 가 m_bCycleHasNG/m_bCycleDatumFailed/m_nCurrentZIndex/m_nLastZIndex 멤버 + ComputeLastZIndex/ResetCycleState 헬퍼를 보유하고 빌드 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 핸들러(PLC)→Vision TCP | z_index/자재번호는 외부 입력. 단 본 plan 은 enum/멤버/필드 **정의만** — 외부 입력 파싱/소비는 49-02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | ShotConfig.ZIndex INI 직렬화 | accept | 레시피 파일은 로컬 신뢰 자원(클라우드/네트워크 DB 없음). ParamBase Load 누락 키 → 0 폴백(의도된 안전값). 신규 외부 공격면 없음. |
| T-49-02 | DoS | ComputeLastZIndex 루프 | accept | Shots 수는 레시피 구성(100개+ 상한, 유한). 외부 입력이 루프 횟수를 좌우하지 않음 — 저위험. |

본 plan 은 로컬 산업 레시피 구성 필드 추가만으로 신규 외부 공격면 추가 없음. ASVS L1 high 심각도 해당 없음.
</threat_model>

<verification>
- msbuild Debug/x64 PASS (0 errors, 0 new warnings) — DatumMeasurement.exe 생성
- ECycleResult enum 3 멤버 존재 (grep)
- ShotConfig.ZIndex default 0 존재 (grep)
- InspectionSequence 사이클 멤버 4개 + 헬퍼 2개 존재 (grep)
- 기존 측정/v2.6 경로 무변경 — 본 plan 은 정의만, 호출처 0 (grep 으로 AddResponse 본문 미변경 확인)
</verification>

<success_criteria>
ECycleResult enum + ShotConfig.ZIndex + InspectionSequence 사이클 상태 멤버/헬퍼가 모두 존재하고 빌드 PASS. 49-02 가 이 토대 위에 판정 흐름을 채울 수 있는 상태.
</success_criteria>

<output>
완료 후 `.planning/phases/49-protocol-v1-judgment-engine/49-01-SUMMARY.md` 생성.
</output>
