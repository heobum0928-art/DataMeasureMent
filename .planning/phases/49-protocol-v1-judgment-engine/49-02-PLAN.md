---
phase: 49-protocol-v1-judgment-engine
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
autonomous: true
requirements: [PROTO-03, PROTO-04, PROTO-05]
must_haves:
  truths:
    - "중간 Index 응답은 항상 B(IsBuffer=true), NG 가 있어도 B"
    - "마지막 Index(z_index 최댓값)에서만 종합 P/F 1회 산출"
    - "사이클 중 발견된 NG 가 누적되어 마지막 Index 종합 F 에 반영된다"
    - "Index 0(Datum 샷) 정상 시 빈 응답 RESULT:site;B;0; (FAICount=0)"
    - "Index 0 Datum 검출 실패 시 즉시 F (UseProtocolV1 분기에서만, v2.6 은 N 유지)"
    - "Index 0 수신 시 사이클 상태가 자동 리셋(이전 NG 미잔류)"
    - "ZIndex 매칭 0건(전부 0 레시피서 측정 Index 수신)이면 빈 B 를 조용히 반환하지 않고 PrintErrLog 경고를 남긴다"
    - "AddResponseV1Cycle 메서드 본문이 D-10 30줄 한도 이하"
    - "기존 v2.6 경로(UseProtocolV1=false) 회귀 0 — 빌드 PASS"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "Index-scoped 판정 엔진 — AddResponse 가 z_index 별 B/P/F + Datum 빈응답/즉시F + 리셋"
      contains: "ResetCycleState"
  key_links:
    - from: "AddResponse"
      to: "TestResultPacket.IsBuffer (48-03 직렬화)"
      via: "IsBuffer = true/false 설정"
      pattern: "IsBuffer"
    - from: "AddResponse"
      to: "RequestPacket.TestID (z_index 문자열)"
      via: "Int32.TryParse → m_nCurrentZIndex"
      pattern: "RequestPacket.TestID"
    - from: "AddResponse"
      to: "ShotConfig.ZIndex (49-01)"
      via: "Index-scoped FAI 집계 필터"
      pattern: "shot.ZIndex"
---

<objective>
Phase 49 의 **핵심**: InspectionSequence.AddResponse 를 z_index 멀티샷 사이클 판정 엔진으로 확장한다. Phase 48 이 직렬화(BuildResultMessageV1/MapCycleJudgement)를 완성했으므로 **IsBuffer/Result 를 채우기만** 한다 — 직렬화 재구현 금지.

판정 흐름 골격 (자재 1개, CONTEXT <specifics>):
- **Index 0(Datum 샷) 수신** → `ResetCycleState()`(D-08, 49-01 정의) → Datum 검출 결과 확인 → 실패 시 **즉시 F**(UseProtocolV1 분기에서만, D-04/D-05) + `m_bCycleDatumFailed=true` / 정상 시 **빈 응답** `RESULT:site;B;0;`(IsBuffer=true, FAIResults 비움, D-06).
- **중간 Index** → 해당 z_index 매핑 Shot/FAI 그룹만 집계(D-01) → NG 발견 시 `m_bCycleHasNG=true`(D-02) → 응답 **IsBuffer=true(→B)**, NG 있어도 B.
- **마지막 Index**(z_index 최댓값 = ComputeLastZIndex, D-03) → 해당 Shot 집계 + `m_bCycleHasNG` 반영 → 종합 **P/F 1회**(IsBuffer=false).
- **불변식:** NG 발견돼도 마지막 Index까지 측정 진행(데이터 수집), 종료 판정은 마지막 Index에서만.

Purpose: 멀티 Index 를 가로지르는 P/F/B 판정 주체를 추가. PROTO-03(3-state)/PROTO-04(Datum)/PROTO-05(사이클 NG mark + 리셋) 모두 충족.
Output: InspectionSequence.AddResponse 재구조화 + Index-scoped 집계 헬퍼 + Datum 빈응답/즉시F 헬퍼.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-protocol-v1-judgment-engine/49-CONTEXT.md
@.planning/refs/Vision-Protocol-v1.0.md
@.planning/refs/control-sequence-coding-guideline.md
@.planning/phases/48-protocol-v1-test-result-site-material/48-03-SUMMARY.md
@.planning/phases/49-protocol-v1-judgment-engine/49-01-PLAN.md

<interfaces>
<!-- 49-01 이 만든 토대 + 기존 코드 계약. executor 는 탐색 없이 사용. -->

49-01 이 추가한 것 (반드시 먼저 존재해야 함):
  // InspectionSequence 멤버
  private bool m_bCycleHasNG = false;
  private bool m_bCycleDatumFailed = false;
  private int m_nCurrentZIndex = 0;
  private int m_nLastZIndex = 0;
  // InspectionSequence 헬퍼
  private int ComputeLastZIndex(InspectionRecipeManager recipeManager);  // 이 시퀀스 소유 Shot z_index 최댓값
  private void ResetCycleState();                                        // 4 멤버 0/false 리셋 (호출부는 직후 m_nLastZIndex 재산출 의무)
  // ShotConfig 멤버
  public int ZIndex { get; set; } = 0;
  // VisionResponsePacket
  public enum ECycleResult : int { Buffer=0, Pass=1, Fail=2 }

기존 InspectionSequence (수정 대상):
  protected override void AddResponse()   // 라인 86~163 — 현재 전체 Shot 집계 + anyDatumSkip>NG>OK 계층
  public TestPacket RequestPacket { get; ... }   // RequestPacket.TestID = z_index 문자열("-1"=미수신 sentinel), RequestPacket.IndexNumber = 자재번호, RequestPacket.Sender/Site/TestType
  public bool IsDatumFailed(string datumRef);    // _failedDatums.Contains — Datum 검출 실패 게이트
  var recipeManager = SystemHandler.Handle.Sequences.RecipeManager;  // recipeManager.Shots = List<ShotConfig> (shot.FAIList, shot.ZIndex, shot.OwnerSequenceName)
  Logging.PrintErrLog((int)ELogType.Error, "...");   // 기존 에러 로깅 — 운용 경고에 사용 (ELogType 캐스트 필수)

기존 FAIConfig (집계 대상):
  public bool WasDatumSkipped { get; set; }   // datum 검출 실패로 측정 skip
  public bool IsPass { get; set; }
  public double MeasuredValue { get; set; }
  public string FAIName { get; ... }

기존 TestResultPacket (48-03):
  public EVisionResultType Result { get; set; }
  public bool IsBuffer { get; set; } = false;   // ★ Phase 49 가 채울 hook
  public bool IsDynamicFAI { get; set; }
  public List<FAIResultData> FAIResults { get; set; }   // .Add(new FAIResultData(name, EVisionResultType, value))
  public int FAICount => FAIResults.Count;
  // 직렬화(BuildResultMessageV1): IsBuffer=true → B / Result==OK → P / 그 외 → F. FAICount=0 → "RESULT:site;B;0;"

기존 SystemSetting:
  SystemHandler.Handle.Setting.UseProtocolV1   // bool — D-04 분기 가드
  SystemHandler.Handle.Setting.CurrentRecipeName
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AddResponse 진입부 — z_index 파싱 + Index 0 리셋 + Datum 빈응답/즉시F 분기 (D-04/D-06/D-08)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (라인 86~163 AddResponse 전체 — 현재 흐름 + try/catch CycleResultSerializer 영속화 블록 보존 대상)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (49-01 이 추가한 멤버/헬퍼 — m_nCurrentZIndex/ResetCycleState/ComputeLastZIndex)
    - .planning/refs/Vision-Protocol-v1.0.md (§판정 P/F/B 표 — F 발생 시점 ②Datum 실패 즉시 / B=Index 진행 중 / Datum 샷 빈응답 RESULT:site;B;0;)
    - .planning/refs/control-sequence-coding-guideline.md (헝가리언 + if/else + 함수 분리 30줄 + 조건식 변수화)
    - .planning/phases/49-protocol-v1-judgment-engine/49-CONTEXT.md (D-04 UseProtocolV1만 F / D-06 빈응답 / D-08 Index 0 리셋 / 핵심 불변식)
  </read_first>
  <action>
AddResponse(라인 86~) 진입부를 재구조화한다. **기존 try/catch CycleResultSerializer.BuildDto/SaveAsync 영속화 블록(라인 139~160)과 ResponseQueue.Enqueue(라인 162)는 보존** — 그 위에 z_index 사이클 분기를 얹는다.

**1) z_index 파싱 헬퍼** (AddResponse 위에 신규, 30줄 한도):
```csharp
//260623 hbk Phase 49 PROTO-03 (D-08): RequestPacket.TestID(=z_index 문자열, "-1"=미수신)를 정수 파싱.
//  파싱 실패/미수신 → 0(Datum/Idx0 폴백) 으로 안전 처리.
private int ParseCurrentZIndex()
{
    int nZ = 0;
    bool bHasRequest = RequestPacket != null;
    if (!bHasRequest)
    {
        return nZ;
    }
    string szTestId = RequestPacket.TestID;
    bool bHasId = !string.IsNullOrEmpty(szTestId);
    if (!bHasId)
    {
        return nZ;
    }
    int nParsed = 0;
    bool bValid = int.TryParse(szTestId, out nParsed);
    if (bValid && nParsed >= 0)
    {
        nZ = nParsed;
    }
    return nZ;
}
```

**2) Datum 빈응답/즉시F 헬퍼** (Index 0 전용, D-06/D-04). 신규:
```csharp
//260623 hbk Phase 49 PROTO-04 (D-04/D-06): Index 0(Datum 샷) 응답 생성.
//  정상 → 빈 응답 RESULT:site;B;0; (IsBuffer=true, FAIResults 비움). 실패 → 즉시 F (UseProtocolV1 분기에서만).
//  Datum 검출 실패 여부 = 이 시퀀스 소유 Datum 중 _failedDatums 1건이라도 = IsDatumFailed. (Action_FAIMeasurement.DatumPhase 가 MarkDatumFailed 호출함.)
private TestResultPacket BuildDatumShotResponse()
{
    var packet = new TestResultPacket
    {
        Target = RequestPacket.Sender,
        Site = RequestPacket.Site,
        InspectionType = RequestPacket.TestType,
        IsDynamicFAI = true,
    };
    bool bUseV1 = SystemHandler.Handle.Setting.UseProtocolV1;
    bool bDatumFailed = m_bCycleDatumFailed;
    if (bUseV1 && bDatumFailed)
    {
        // 즉시 F — 후속 Index skip 은 핸들러 주도(D-05). IsBuffer=false + Result=NG → 직렬화 'F'.
        packet.IsBuffer = false;
        packet.Result = EVisionResultType.NG;
        return packet;
    }
    // 정상 Datum 샷 → 빈 응답 RESULT:site;B;0; (FAIResults 비어있음 → FAICount=0).
    packet.IsBuffer = true;
    packet.Result = EVisionResultType.OK;
    return packet;
}
```

**3) Datum 실패 감지** (AddResponse 진입부에서 m_bCycleDatumFailed 설정). 헬퍼:
```csharp
//260623 hbk Phase 49 PROTO-04: 이 시퀀스 소유 Datum 중 검출 실패가 있으면 true.
private bool DetectDatumFailure()
{
    bool bAnyFailed = false;
    foreach (var datum in DatumConfigs)
    {
        bool bIsNull = datum == null;
        if (bIsNull)
        {
            continue;
        }
        bool bFailed = IsDatumFailed(datum.DatumName);
        if (bFailed)
        {
            bAnyFailed = true;
        }
    }
    return bAnyFailed;
}
```

**4) AddResponse 진입부** — 라인 87 `if (RequestPacket == null) return;` 직후에 z_index 분기 골격을 삽입한다:
```csharp
if (RequestPacket == null) return;

//260623 hbk Phase 49 PROTO-03/04/05: v1.0 활성 시에만 z_index 사이클 엔진 적용. v2.6 면 기존 전체-Shot 경로(아래)로 폴백(회귀 0).
bool bUseV1Cycle = SystemHandler.Handle.Setting.UseProtocolV1;
if (bUseV1Cycle)
{
    AddResponseV1Cycle();
    return;
}
// ↓ 이하 기존 v2.6 전체-Shot 집계 블록 그대로 (회귀 0) — 라인 89~162 무변경 보존.
```

기존 AddResponse 본문(라인 89~162: recipeManager/foreach Shot/anyDatumSkip/cycleDto/Enqueue)은 **그대로 둔다** — v2.6 폴백 경로. v1.0 흐름은 Task 2 의 신규 `AddResponseV1Cycle()` 에 집중.

D-10 준수: 모든 신규 변수 헝가리언(b/n/sz), if/else only, 삼항·null병합 금지, 조건식 bool 변수화, 각 함수 30줄 이하.
  </action>
  <verify>
    <automated>grep -n "ParseCurrentZIndex\|BuildDatumShotResponse\|DetectDatumFailure\|AddResponseV1Cycle\|bUseV1Cycle" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "private int ParseCurrentZIndex" InspectionSequence.cs` 매치
    - `grep -n "BuildDatumShotResponse" InspectionSequence.cs` 매치
    - `grep -n "DetectDatumFailure" InspectionSequence.cs` 매치
    - `grep -n "bUseV1Cycle" InspectionSequence.cs` 매치 (진입부 v2.6 폴백 분기)
    - 기존 v2.6 블록 보존: `grep -c "anyDatumSkip" InspectionSequence.cs` >= 4 (기존 라인 미삭제)
    - 신규 라인에 삼항 `?:`/null병합 `??` 0건
  </acceptance_criteria>
  <done>AddResponse 가 UseProtocolV1 분기로 v1.0 사이클 경로/v2.6 폴백 경로를 가르고, Datum 빈응답·즉시F·z_index 파싱 헬퍼가 존재.</done>
</task>

<task type="auto">
  <name>Task 2: AddResponseV1Cycle — Index-scoped 집계 + B/P/F 분기 + 사이클 NG 누적 + 영속화 (D-01/D-02/D-03/D-05)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (라인 139~162 기존 CycleResultSerializer.BuildDto/SaveAsync/Enqueue 블록 — v1.0 경로도 동일 영속화 재사용)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (Task 1 이 추가한 ParseCurrentZIndex/BuildDatumShotResponse/DetectDatumFailure + 49-01 ComputeLastZIndex/ResetCycleState/m_* 멤버)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (AddResponse 기존 전체-Shot 폴백 경로 — 폴백 분기(전체 재검사) 채택 금지 근거 확인)
    - .planning/phases/48-protocol-v1-test-result-site-material/48-03-SUMMARY.md (IsBuffer 최우선 직렬화 계약 — IsBuffer=true→B, Result==OK→P, 그 외→F)
    - .planning/refs/Vision-Protocol-v1.0.md (§Index Table — 같은 Site 에서 Index마다 Shot 그룹 다름 / 중간=B / 마지막=종합)
    - .planning/phases/49-protocol-v1-judgment-engine/49-CONTEXT.md (D-01 Index-scoped only(전체 재검사 금지) / D-02 멤버 NG 누적 / D-03 z_index 최댓값 / D-10 함수 30줄 / 핵심 불변식: NG 있어도 마지막까지)
  </read_first>
  <action>
신규 메서드 `AddResponseV1Cycle()` 를 추가한다(AddResponse 바로 아래). 이것이 v1.0 판정 흐름 전체다. **D-10 함수 30줄 한도 엄수** → Index 0 분기 본문을 별도 헬퍼로 추출하고, ComputeLastZIndex 이중 호출을 제거하여 AddResponseV1Cycle 본문을 30줄 이하로 유지한다.

**메인 흐름** (z_index 별 분기 — 본문 30줄 이하, Index 0 분기는 헬퍼로 추출):
```csharp
//260623 hbk Phase 49 PROTO-03/04/05: v1.0 z_index 멀티샷 사이클 판정 엔진.
//  Index 0 = 사이클 시작(리셋, D-08) + Datum 빈응답/즉시F(D-04/D-06) → HandleDatumIndexResponse() 헬퍼 위임.
//  중간 Index = 해당 z_index Shot 집계(D-01) + NG 누적(D-02) + B(IsBuffer=true).
//  마지막 Index(z_index 최댓값, D-03) = 집계 + m_bCycleHasNG 반영 종합 P/F.
private void AddResponseV1Cycle()
{
    var recipeManager = SystemHandler.Handle.Sequences.RecipeManager;
    m_nCurrentZIndex = ParseCurrentZIndex();

    bool bIsDatumShot = m_nCurrentZIndex == 0;
    if (bIsDatumShot)
    {
        HandleDatumIndexResponse(recipeManager);   // D-08 리셋 + 재산출 + Datum 응답 + 영속화
        return;
    }

    // 측정 Index (중간 or 마지막) — m_nLastZIndex 는 측정 경로에서 1회만 산출(이중 호출 제거).
    m_nLastZIndex = ComputeLastZIndex(recipeManager);
    bool bIsLastIndex = m_nCurrentZIndex >= m_nLastZIndex;
    TestResultPacket packet = BuildScopedResponse(recipeManager, m_nCurrentZIndex, bIsLastIndex);
    PersistAndEnqueueV1(recipeManager, packet);
}
```

**Index 0 분기 추출 헬퍼** `HandleDatumIndexResponse(recipeManager)` (D-08, BLOCKER 2 (b) — AddResponseV1Cycle 30줄 유지 위해 추출). 신규:
```csharp
//260623 hbk Phase 49 PROTO-04/05 (D-08): Index 0(Datum 샷) 처리 — 사이클 리셋 + Datum 검출 감지 + 응답 + 영속화.
//  ResetCycleState() 가 m_nLastZIndex=0 으로 덮으므로 직후 단 1회 재산출(이중 호출 제거 — 측정 경로는 자체 산출).
private void HandleDatumIndexResponse(InspectionRecipeManager recipeManager)
{
    ResetCycleState();                                  // D-08: 사이클 시작 = 클린 슬레이트
    m_nCurrentZIndex = 0;
    m_nLastZIndex = ComputeLastZIndex(recipeManager);   // 리셋 직후 재산출(호출부 의무, 49-01 ResetCycleState 주석)
    m_bCycleDatumFailed = DetectDatumFailure();         // D-04: Datum 검출 실패 감지
    TestResultPacket datumPacket = BuildDatumShotResponse();
    PersistAndEnqueueV1(recipeManager, datumPacket);
}
```

**Index-scoped 집계 + 응답 조립 헬퍼** `BuildScopedResponse(recipeManager, nZIndex, bIsLastIndex)`:
- 응답 패킷 생성(Target/Site/InspectionType/IsDynamicFAI=true — 기존 AddResponse 라인 94~99 패턴 그대로).
- `recipeManager.Shots` 순회하되 **이 시퀀스 소유(shot.OwnerSequenceName == Name) AND shot.ZIndex == nZIndex 인 Shot 만**(D-01 Index-scoped). 그 Shot 의 FAIList 만 집계. 매칭된 Shot 개수를 `int nMatchedShots` 헝가리언 변수로 카운트한다.
- 각 FAI 3-state(기존 라인 105~126 패턴 차용): WasDatumSkipped → NotExist 'N' + 그 FAI 도 NG 취급으로 `m_bCycleHasNG=true`; !IsPass → NG + `m_bCycleHasNG=true`; 그 외 → OK. FAIResults.Add(name, code, MeasuredValue).
- **사이클 NG 누적(D-02):** 이 Index 집계 중 NG/검출실패 1건이라도 → `m_bCycleHasNG = true` (멤버, 리셋 전까지 유지).
- **★ BLOCKER 1 — 빈 결과 경고 (조용한 빈 B 금지, D-01 정합):** 집계 후 `packet.FAIResults` 가 비어있는데 매칭 Shot 도 0건(`nMatchedShots == 0`)이면, 이는 "ZIndex 매칭 0건"(기존 레시피 전부 ZIndex=0 인데 측정 Index 1+ 수신)인 운용 오류 상황이다. 이 때 `Logging.PrintErrLog((int)ELogType.Error, ...)` 로 경고를 남긴다(시퀀스명/nZIndex/m_nLastZIndex 포함). **폴백(전체-Shot 재검사) 분기는 D-01(Index-scoped only, 전체 재검사 금지) 위반이므로 채택 금지** — 경고 로그만 남기고 빈 B 응답은 그대로 유지(spec 정합). 헝가리언 + if/else, 삼항 금지:
  ```csharp
  bool bNoResults = packet.FAIResults.Count == 0;
  bool bNoMatch = nMatchedShots == 0;
  if (bNoResults && bNoMatch)
  {
      // ZIndex 매칭 0건 — 정상 B 와 구분 불가한 빈 B. 운용 오류(레시피 ZIndex 미설정 + 측정 Index 수신) 가시화.
      Logging.PrintErrLog((int)ELogType.Error,
          "[V1Cycle] BuildScopedResponse 빈 결과: ZIndex 매칭 0건 (Seq=" + Name + ", z=" + nZIndex + ", last=" + m_nLastZIndex + "). 레시피 ZIndex 설정 확인 필요. //260623 hbk");
  }
  else
  {
      // 정상 집계 — 추가 처리 없음.
  }
  ```
- **B vs P/F 분기(D-03/불변식):**
  - `bIsLastIndex == false` → `packet.IsBuffer = true; packet.Result = EVisionResultType.OK;` (직렬화 B — NG 있어도 B).
  - `bIsLastIndex == true` → `packet.IsBuffer = false;` + 종합: `bool bCycleFail = m_bCycleHasNG || m_bCycleDatumFailed; if (bCycleFail) packet.Result = NG; else packet.Result = OK;` (직렬화 F/P).
- `pMyContext.ResultInfo = packet.Result;` (기존 라인 134 패턴).
- return packet.

**영속화+Enqueue 헬퍼** `PersistAndEnqueueV1(recipeManager, packet)`:
- 기존 라인 139~162 의 try/catch CycleResultSerializer.BuildDto/SaveAsync(nIndexNumber=RequestPacket.IndexNumber)/ResponseQueue.Enqueue 를 그대로 이식(v1.0 경로 동일 영속화). BuildDto 에 전달하는 result 는 `packet.Result`.
- **주의:** 중간 Index(B)도 cycle.json 영속화하면 마지막 Index 가 다시 덮으므로, 기존 BuildDto 의미(시퀀스 소유 shot 스냅샷)와 충돌 없음 — 매 Index 스냅샷 저장이 의도. 단 try/catch 격리로 직렬화 예외가 Enqueue 차단 안 하게(기존 패턴 보존).

**불변식 검증(주석으로 못박기):** "중간 Index 에서 NG(m_bCycleHasNG=true) 가 나도 응답 B, 종료 판정은 마지막 Index 에서만. NG 발견돼도 마지막까지 측정 진행(각 Index 의 측정은 Action_FAIMeasurement 가 수행, AddResponse 는 집계만)."

D-10 준수: **AddResponseV1Cycle 본문 30줄 이하**(Index 0 분기를 HandleDatumIndexResponse 로 추출하여 달성), 각 헬퍼 30줄 이하(BuildScopedResponse 초과 시 FAI 3-state 분기를 별도 `ClassifyFai(fai)` 헬퍼로 추가 분리), 헝가리언 b/n/sz, if/else only, 삼항·null병합 금지, 조건식 bool 변수화, 매직넘버 없음(0 = Datum 은 의미 명확 — 필요 시 `const int DATUM_Z_INDEX = 0;` 상수화 권장). **ComputeLastZIndex 는 Index 0 경로(HandleDatumIndexResponse) 와 측정 경로(AddResponseV1Cycle) 가 각각 1회씩만 산출 — 동일 호출에서 이중 산출 금지.**
  </action>
  <verify>
    <automated>grep -n "AddResponseV1Cycle\|HandleDatumIndexResponse\|BuildScopedResponse\|PersistAndEnqueueV1\|m_bCycleHasNG = true\|bIsLastIndex" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "private void AddResponseV1Cycle" InspectionSequence.cs` 매치
    - `grep -n "private void HandleDatumIndexResponse" InspectionSequence.cs` 매치 (Index 0 분기 추출 — BLOCKER 2 (b))
    - `grep -n "BuildScopedResponse" InspectionSequence.cs` 매치 (Index-scoped 집계)
    - `grep -n "shot.ZIndex == " InspectionSequence.cs` 또는 `nZIndex` 비교 매치 (D-01 Index 필터)
    - `grep -c "m_bCycleHasNG = true" InspectionSequence.cs` >= 1 (D-02 NG 누적)
    - `grep -n "bIsLastIndex" InspectionSequence.cs` 매치 (D-03 마지막 Index 분기)
    - `grep -n "IsBuffer = true" InspectionSequence.cs` 매치 (중간 Index B) AND `grep -n "IsBuffer = false" InspectionSequence.cs` 매치 (마지막/즉시F)
    - `grep -n "ResetCycleState()" InspectionSequence.cs` 매치 (Index 0 호출 — D-08)
    - **★ BLOCKER 1:** BuildScopedResponse 빈 결과 시 PrintErrLog 호출 확인 — `grep -n "BuildScopedResponse 빈 결과\|nMatchedShots == 0" InspectionSequence.cs` 매치 AND `grep -c "PrintErrLog" InspectionSequence.cs` 가 v1.0 경로 신규 호출 1건 이상 포함
    - **★ BLOCKER 2 (c):** AddResponseV1Cycle 메서드 본문 30줄 이하 — `private void AddResponseV1Cycle` 시작 `{` 부터 매칭 `}` 까지 본문 라인 수가 30 이하(주석 제외). 수동 확인 또는 awk 로 메서드 블록 추출 후 wc -l
    - **★ BLOCKER 2 (a):** ComputeLastZIndex 가 AddResponseV1Cycle 의 단일 호출 경로에서 1회만 호출 — `grep -c "ComputeLastZIndex(recipeManager)" InspectionSequence.cs` 가 AddResponseV1Cycle(측정경로 1) + HandleDatumIndexResponse(1) = 메서드별 1회씩(동일 호출 중복 0)
    - **D-10 헬퍼 한도:** BuildScopedResponse 본문도 30줄 이하 — 시작 `{` 부터 매칭 `}` 까지 본문 라인 수 30 이하(주석 제외). 초과 시 FAI 3-state 분기를 `ClassifyFai(FAIConfig fai)` 헬퍼로 **분리 필수** → `grep -n "private .* ClassifyFai" InspectionSequence.cs` 로 분리 확인
    - 신규 라인 삼항 `?:`/null병합 `??` 0건
    - 빌드 PASS — msbuild Debug/x64, 0 errors, 0 new warnings (DatumMeasurement.exe 생성)
  </acceptance_criteria>
  <done>AddResponseV1Cycle(30줄 이하) 가 Index 0 분기를 HandleDatumIndexResponse 로 위임 / 중간 Index B+NG누적 / 마지막 Index 종합 P/F 를 구현하고, ZIndex 매칭 0건 시 PrintErrLog 경고를 남기며, ComputeLastZIndex 이중 호출 없음, v2.6 폴백 경로 회귀 0, 빌드 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 핸들러(PLC)→Vision TCP | z_index(RequestPacket.TestID)/자재번호는 외부 입력. AddResponseV1Cycle 가 소비. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-03 | Tampering | RequestPacket.TestID(z_index) 파싱 | mitigate | ParseCurrentZIndex 가 int.TryParse + `>= 0` 가드 → 비정수/음수/미수신 모두 0(Datum 폴백) 으로 안전 정규화. 임의 큰 값이 와도 ComputeLastZIndex(레시피 최댓값)과 비교 시 "마지막 Index 이상" 으로 종합 판정만 수행(루프 폭주 없음). |
| T-49-04 | DoS | BuildScopedResponse Shots 루프 | accept | Shots 수는 레시피 구성(유한). 외부 입력(z_index)은 필터 값일 뿐 루프 횟수 불변 — 저위험. |
| T-49-05 | Info Disclosure | RESULT 응답 항목(FAIName=val=judge) | accept | 응답은 신뢰된 로컬 핸들러 대상(2-PC 폐쇄망, 인증된 persistent TCP). FAIName 은 내부 레시피 영숫자 코드(외부 주입 불가, 48-03 T-48-07 기록). 신규 노출 없음. |

본 plan 은 외부 입력(z_index)을 소비하나 TryParse+범위 가드로 정규화 — 신규 high 위협 없음. ASVS L1: 입력 검증(V5) 충족(TryParse 가드).
</threat_model>

<verification>
- msbuild Debug/x64 PASS (0 errors, 0 new warnings) — DatumMeasurement.exe 생성
- v1.0 경로(UseProtocolV1=true): Index 0 → 빈 B(또는 Datum 실패 시 F) / 중간 Index → B / 마지막 Index → 종합 P/F (코드 경로 추적)
- ZIndex 매칭 0건 → PrintErrLog 경고 (grep BLOCKER 1)
- AddResponseV1Cycle 본문 30줄 이하 (BLOCKER 2)
- v2.6 경로(UseProtocolV1=false): 기존 전체-Shot 집계 블록 무변경 → 회귀 0 (grep anyDatumSkip 보존 확인)
- IsBuffer true/false 양쪽 설정 존재 (grep)
- ResetCycleState 가 Index 0 분기(HandleDatumIndexResponse)에서 호출 (grep)
- 삼항/null병합 신규 0건, 헝가리언 멤버 m_*/지역 b/n/sz 준수 (grep + 코드 인스펙션)

**human_needed (UAT — 실 핸들러/SIMUL 시나리오, 코드 검증으로 대체 불가):**
- UAT-1: $TEST z_index=0 정상 → `RESULT:site;B;0;` byte 확인
- UAT-2: $TEST z_index=0 Datum 실패 → 즉시 `RESULT:site;F;...` (UseProtocolV1=true)
- UAT-3: 중간 Index NG 발생 → 응답 `B`, 사이클 계속 진행
- UAT-4: 마지막 Index → 사이클 누적 NG 있으면 `F`, 없으면 `P` 1회
- UAT-5: 다음 자재 Index 0 재수신 → 이전 NG 미잔류(리셋 확인)
- UAT-6: UseProtocolV1=false 회귀 — v2.6 응답 포맷 무변경
- UAT-7: ZIndex 미설정(전부 0) 레시피서 측정 Index 1+ 수신 → 빈 B + PrintErrLog 경고 로그 확인
</verification>

<success_criteria>
중간 Index=B / 마지막 Index 종합 P/F 1회 / Datum 샷 빈응답 + Datum 실패 즉시 F / Index 0 자동 리셋 / ZIndex 매칭 0건 시 PrintErrLog 경고 / AddResponseV1Cycle 30줄 이하가 코드로 구현되고, v2.6 경로 회귀 0, 빌드 PASS. UAT 항목은 human_needed.
</success_criteria>

<output>
완료 후 `.planning/phases/49-protocol-v1-judgment-engine/49-02-SUMMARY.md` 생성. UAT 항목은 49-HUMAN-UAT.md 후보로 SUMMARY 에 명시.
</output>
</content>
