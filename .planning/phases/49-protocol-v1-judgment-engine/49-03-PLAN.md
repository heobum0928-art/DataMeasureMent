---
phase: 49-protocol-v1-judgment-engine
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/TcpServer/TcpServer.cs
autonomous: true
requirements: [PROTO-05]
must_haves:
  truths:
    - "TcpServer.EncodingType 가 instance 필드(static 아님)"
    - "ApplyEncoding 이 instance 메서드(static 아님)"
    - "ConvertMessage(string)/ConvertMessage(byte[]) 가 instance EncodingType 를 읽는다"
    - "VisionServer 생성자의 ApplyEncoding(Utf8) 호출이 그대로 동작(v1.0 UTF-8 인코딩 회귀 0)"
    - "빌드 PASS — 인코딩 동작 회귀 0"
  artifacts:
    - path: "WPF_Example/TcpServer/TcpServer.cs"
      provides: "EncodingType instance 필드 + ApplyEncoding instance 메서드 (CO-48-01 / D-09)"
      contains: "private MessageEncodingType EncodingType"
  key_links:
    - from: "VisionServer 생성자 ApplyEncoding(Utf8)"
      to: "TcpServer.EncodingType (instance)"
      via: "instance 메서드 호출"
      pattern: "ApplyEncoding"
    - from: "ConvertMessage"
      to: "EncodingType (instance)"
      via: "switch (EncodingType)"
      pattern: "switch (EncodingType)"
---

<objective>
**CO-48-01 흡수 (D-09, Phase 48 review CR-01):** `TcpServer.EncodingType` static 필드 → instance 필드, `ApplyEncoding` static → instance 메서드로 전환한다. 다중 인스턴스 전역 인코딩 오염 시한폭탄 제거. 제어 코드를 터치하는 김에 동반 정리.

핵심: **인코딩 동작 회귀 0**. 현재 프로세스 내 TcpServer 인스턴스가 1개뿐이라 동작 변화는 없어야 하며, 구조만 안전하게 인스턴스화된다.

Purpose: Phase 49 가 제어 코드(VisionResponsePacket/InspectionSequence)를 만지는 phase 이므로, 동일 제어 스택의 구조적 결함(CR-01)을 함께 닫는다.
Output: TcpServer.cs — EncodingType/ApplyEncoding 인스턴스화 (1 파일, 최소 변경).

**범위 밖(기록만):** 같은 파일의 `Header`/`Trailer` static 필드(동일 문제, Phase 48 이전부터 존재) — CO-48-01 범위 밖(CONTEXT deferred 기록). 본 plan 은 EncodingType/ApplyEncoding 만 전환.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-protocol-v1-judgment-engine/49-CONTEXT.md
@.planning/refs/control-sequence-coding-guideline.md
@.planning/phases/48-protocol-v1-test-result-site-material/48-REVIEW.md

<interfaces>
<!-- TcpServer.cs 현재 상태 (수정 대상). executor 는 탐색 없이 사용. -->

TcpServer.cs (라인 76, 80~83):
  private static MessageEncodingType EncodingType { get; set; } = MessageEncodingType.Default;   // ★ static 제거 대상
  protected static void ApplyEncoding(MessageEncodingType eEncoding)   // ★ static 제거 대상
  {
      EncodingType = eEncoding;
  }

ConvertMessage 소비처 (라인 158, 177 — ConnectedClient 중첩 클래스 내부):
  switch (EncodingType) { case Ascii / Default / Utf8 ... }
  ※ EncodingType 가 instance 가 되면, ConnectedClient(중첩 클래스)에서 부모 TcpServer 인스턴스의 필드 접근이 필요 — ConnectedClient 는 이미 `private TcpServer Parent { get; }` 보유(라인 87). switch(EncodingType) → switch(Parent.EncodingType) 로 한정 필요.

VisionServer.cs (라인 27 — 유일한 호출처):
  ApplyEncoding(MessageEncodingType.Utf8);   // VisionServer 생성자 — base TcpServer 의 protected 메서드 호출. instance 전환 후에도 동일 호출 구문 유효(생성자는 instance 컨텍스트).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EncodingType/ApplyEncoding 인스턴스화 + ConvertMessage 접근 한정 (CO-48-01 / D-09)</name>
  <files>WPF_Example/TcpServer/TcpServer.cs</files>
  <read_first>
    - WPF_Example/TcpServer/TcpServer.cs (라인 60~120 클래스 선언/EncodingType/ApplyEncoding/ConnectedClient.Parent + 라인 152~192 ConvertMessage 2개 — switch(EncodingType) 소비처)
    - WPF_Example/TcpServer/VisionServer.cs (라인 19~29 생성자 — ApplyEncoding(Utf8) 유일 호출처)
    - .planning/phases/48-protocol-v1-test-result-site-material/48-REVIEW.md (CR-01 Fix 코드 — instance 필드 전환 가이드)
    - .planning/refs/control-sequence-coding-guideline.md (헝가리언 — 단, EncodingType/ApplyEncoding 은 기존 시그니처/이름 보존 = VisionServer 호출처 무변경 우선. eEncoding 파라미터는 기존 명명 유지)
    - .planning/phases/49-protocol-v1-judgment-engine/49-CONTEXT.md (D-09: 인코딩 동작 회귀 0)
  </read_first>
  <action>
TcpServer.cs 3곳을 변경한다. **인코딩 동작 회귀 0** 이 절대 기준 — 이름/동작 보존, static 키워드만 제거 + 접근 한정.

**(1) 필드 static 제거** (라인 76):
```csharp
//260623 hbk Phase 49 CO-48-01 (D-09): static → instance. 다중 인스턴스 전역 인코딩 오염 시한폭탄 제거.
//  현재 인스턴스 1개라 동작 변화 0 — 구조만 안전화. (Header/Trailer static 은 CO-48-01 범위 밖, 기록만.)
private MessageEncodingType EncodingType { get; set; } = MessageEncodingType.Default;
```

**(2) ApplyEncoding static 제거** (라인 80~83) — `protected static` → `protected`:
```csharp
//260623 hbk Phase 49 CO-48-01 (D-09): static 제거. 파생 클래스(VisionServer) 생성자가 instance 컨텍스트에서 호출.
protected void ApplyEncoding(MessageEncodingType eEncoding)
{
    EncodingType = eEncoding;
}
```

**(3) ConvertMessage 접근 한정** (라인 158, 177 — ConnectedClient 중첩 클래스 내부 `switch (EncodingType)` 2곳):
EncodingType 이 instance 가 되면 ConnectedClient(중첩 클래스)는 부모 인스턴스 필드를 직접 못 읽는다. ConnectedClient 는 이미 `private TcpServer Parent { get; }`(라인 87) 를 보유하므로, 2곳 모두 `switch (EncodingType)` → `switch (Parent.EncodingType)` 로 변경:
```csharp
//260623 hbk Phase 49 CO-48-01 (D-09): EncodingType instance 화 — 부모 인스턴스(Parent) 경유 접근.
switch (Parent.EncodingType) {
    case MessageEncodingType.Ascii: ...
    case MessageEncodingType.Default: ...
    case MessageEncodingType.Utf8: ...
}
```
**주의:** `EncodingType` 이 `private` 이면 중첩 클래스 ConnectedClient 에서 Parent.EncodingType 접근 가능(C# 중첩 클래스는 외곽 클래스 private 멤버 접근 허용). 컴파일 OK. 만약 빌드 시 접근성 오류가 나면 EncodingType getter 를 그대로 두고(private set 유지) 읽기만 하므로 문제 없음 — Rule 1 즉시 수정.

**VisionServer.cs 무변경:** 라인 27 `ApplyEncoding(MessageEncodingType.Utf8);` 는 생성자(instance 컨텍스트) 내 호출이므로 instance 메서드로 전환돼도 구문 그대로 유효 — 수정 불요. (확인만.)

D-10 준수: EncodingType/ApplyEncoding/eEncoding 기존 명명 보존(호출처 호환 + 회귀 0 우선 — 이름 변경은 회귀 위험). 신규 변수 없음. 삼항·null병합 0건(switch 유지).
  </action>
  <verify>
    <automated>grep -n "static.*EncodingType\|static void ApplyEncoding\|switch (Parent.EncodingType)\|private MessageEncodingType EncodingType" WPF_Example/TcpServer/TcpServer.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "static" WPF_Example/TcpServer/TcpServer.cs` 결과에서 EncodingType/ApplyEncoding 라인에 static **없음**: `grep -n "static.*EncodingType" TcpServer.cs` 0 매치, `grep -n "static void ApplyEncoding" TcpServer.cs` 0 매치
    - `grep -n "private MessageEncodingType EncodingType" TcpServer.cs` 매치 (instance 필드)
    - `grep -n "protected void ApplyEncoding" TcpServer.cs` 매치 (instance 메서드, static 없음)
    - `grep -c "switch (Parent.EncodingType)" TcpServer.cs` 가 2 (ConvertMessage 2곳 모두 한정)
    - VisionServer.cs 무변경: `grep -n "ApplyEncoding(MessageEncodingType.Utf8)" WPF_Example/TcpServer/VisionServer.cs` 매치 유지
    - 변경 라인에 `//260623 hbk Phase 49 CO-48-01` 주석 존재
    - 빌드 PASS — msbuild Debug/x64, 0 errors, 0 new warnings (DatumMeasurement.exe 생성)
  </acceptance_criteria>
  <done>EncodingType/ApplyEncoding 이 instance 화되고, ConvertMessage 가 Parent.EncodingType 를 읽으며, VisionServer UTF-8 설정이 그대로 동작, 빌드 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 핸들러(PLC)→Vision TCP | 인코딩(UTF-8/Default)은 메시지 직렬화 경로. 본 plan 은 인코딩 필드의 **저장 위치(static→instance)** 만 변경 — 외부 입력 처리 로직 무변경. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-06 | Tampering | EncodingType 전역 상태 오염 | mitigate | **이 plan 자체가 mitigation** — static 필드를 instance 로 전환하여 다중 인스턴스 간 전역 인코딩 오염(CR-01 시한폭탄) 구조적 제거. 공격면 축소. |
| T-49-07 | Repudiation/기타 | — | accept | 인코딩 동작 자체 무변경(회귀 0) — 신규 위협 없음. Header/Trailer static 잔존은 범위 밖 기록. |

본 plan 은 기존 결함(전역 가변 상태)을 닫는 보안-개선 성격. 신규 공격면 추가 없음(오히려 제거). ASVS L1 해당 없음(인코딩 동작 불변).
</threat_model>

<verification>
- msbuild Debug/x64 PASS (0 errors, 0 new warnings) — DatumMeasurement.exe 생성
- EncodingType/ApplyEncoding static 키워드 제거 확인 (grep)
- ConvertMessage 2곳 Parent.EncodingType 한정 (grep count = 2)
- VisionServer UTF-8 설정 호출 무변경 (grep)
- 인코딩 동작 회귀 0: UseProtocolV1=true → UTF-8, false → Default (코드 경로 추적 — VisionServer 생성자 분기 무변경)

**human_needed (선택 — 인코딩 회귀 확증):**
- UAT: UseProtocolV1=true 로 실제 $TEST/$RESULT 송수신 시 한글/UTF-8 메시지 정상 인코딩(48 baseline 과 동일) 확인
</verification>

<success_criteria>
EncodingType/ApplyEncoding 인스턴스화 후 인코딩 동작 회귀 0, 빌드 PASS. CO-48-01 / D-09 종결.
</success_criteria>

<output>
완료 후 `.planning/phases/49-protocol-v1-judgment-engine/49-03-SUMMARY.md` 생성. CO-48-01 종결을 STATE/carry-over 갱신 후보로 명시.
</output>
