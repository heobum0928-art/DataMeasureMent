---
phase: 51-export-2026-06-16-poc-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Sequence/Sequence/SequenceBase.cs
  - WPF_Example/Custom/Sequence/Inspection/BatchRunService.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [BATCH-01]

must_haves:
  truths:
    - "선택된 SHOT 인덱스 집합만 1사이클 실행할 수 있다 (전체 StartAll 대체)"
    - "선택 SHOT 실행 결과가 1 CycleResultDto 로 누적되어 호출자에게 전달된다"
    - "BatchRunService 가 SaveAsync 를 호출하지 않아 같은 사이클이 두 번 저장되지 않는다 (HandleManualCyclePersist 위임)"
    - "기존 StartAll / 단일 SHOT 검사 경로는 변경 없이 동작한다 (회귀 0)"
  artifacts:
    - path: "WPF_Example/Sequence/Sequence/SequenceBase.cs"
      provides: "StartSubset(int[] actionIndices, TestPacket packet) 부분 실행 진입점"
      contains: "public bool StartSubset("
    - path: "WPF_Example/Custom/Sequence/Inspection/BatchRunService.cs"
      provides: "선택 SHOT 일괄 실행 + 결과 누적 서비스 (RepeatRunService 파생 패턴)"
      contains: "public void StartBatch("
    - path: "WPF_Example/DatumMeasurement.csproj"
      provides: "BatchRunService.cs Compile 등록"
      contains: "BatchRunService.cs"
  key_links:
    - from: "BatchRunService.TriggerNext"
      to: "SequenceBase.StartSubset"
      via: "Dispatcher.BeginInvoke(Background) 안에서 _seq.StartSubset(_selectedIndices.ToArray(), null) 호출"
      pattern: "StartSubset\\("
    - from: "BatchRunService.HandleFinish"
      to: "CycleResultSerializer.BuildDto"
      via: "OnFinish 구독 → recipeManager 순회 → BuildDto → _collected.Add"
      pattern: "CycleResultSerializer\\.BuildDto"
---

<objective>
선택된 SHOT 인덱스 집합만 1사이클 실행하는 부분 실행 경로(`SequenceBase.StartSubset`)와, 그 결과를 누적하여 호출자(UI)에게 전달하는 `BatchRunService` 서비스를 추가한다. `BatchRunService` 는 `RepeatRunService` 와 동일한 Start → OnFinish → HandleFinish → 누적(_collected) 패턴을 쓰되, "N회 반복" 대신 "선택 SHOT 1사이클" 로 동작한다. 누적/Export 경로는 Phase 41.1(Gage R&R)가 그대로 재사용할 수 있게 설계한다 (D-08, 코드 중복 0).

Purpose: Phase 51 일괄 검사 백엔드. UI 가 의존하는 계약(StartSubset + BatchRunService)을 먼저 확정하여 Wave 2(UI) 가 탐색 없이 구현하게 한다 (interface-first).
Output: SequenceBase.StartSubset 메서드 + BatchRunService.cs 신규 파일 + csproj 등록.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-export-2026-06-16-poc-3/51-CONTEXT.md
@.planning/phases/51-export-2026-06-16-poc-3/51-PATTERNS.md
@./CLAUDE.md

<interfaces>
<!-- 실행자는 아래 계약을 직접 사용한다 — 코드베이스 추가 탐색 불필요. -->

기존 SequenceBase.StartAll (SequenceBase.cs L341-359, K&R 스타일, 복사 기준):
```csharp
//260409 hbk Phase 5: 모든 Action 순차 실행 (D-01)
public bool StartAll(TestPacket packet) {
    if (State != EContextState.Idle) return false;
    if (Actions == null || Actions.Length == 0) return false;
    RequestPacket = packet;
    CurrentActionIndex = 0;
    EndActionIndex = Actions.Length - 1;
    Context.Clear();
    IsFinished = false;
    if (OnStart != null) {
        OnStart.Invoke(Context);
    }
    Command = ESequenceCommmand.Start; //260517 hbk
    return true;
}
```

기존 RepeatRunService (Custom/Sequence/Inspection/RepeatRunService.cs, Allman 스타일) 의 핵심 시그니처:
- `public class RepeatRunService` — namespace ReringProject.Sequence
- `public event Action<List<CycleResultDto>> OnRepeatComplete;`
- `public event Action<int, int> OnProgressChanged;`
- `public bool IsRunning { get; private set; }`
- `public int CompletedCount { get; private set; }` / `public int TargetCount { get; private set; }`
- private fields: `InspectionSequence _seq; List<CycleResultDto> _collected; EventSequenceStateChanged _onFinishHandler; readonly object _lock`
- `private void HandleFinish(SequenceContext ctx)` — recipeManager.Shots 순회 → anySkip/allPass → EVisionResultType → `CycleResultSerializer.BuildDto(recipeManager, resultType, DateTime.Now, recipeName, seqName)` → `CycleResultSerializer.SaveAsync(dto)` → `_collected.Add(dto)` → CompletedCount++ → 도달 시 OnRepeatComplete
- `private void TriggerNext()` — `Application.Current.Dispatcher.BeginInvoke(DispatcherPriority.Background, ...)` 안에서 State==Idle 이면 `_seq.StartAll(null)`, 아니면 `Task.Delay(50).ContinueWith(_ => TriggerNext())`
- `public void Stop()` — OnFinish 구독 해제 + IsRunning=false

중복 저장 계약 (InspectionSequence.cs L150-171):
- `HandleManualCyclePersist(SequenceContext)` 는 생성자에서 OnFinish 에 1회 등록되어 있고, `RequestPacket == null` (수동 경로) 일 때 이미 `CycleResultSerializer.SaveAsync` 를 호출한다.
- 따라서 BatchRunService 가 수동 경로(packet=null)로 실행하면 SaveAsync 가 이미 호출된다. BatchRunService.HandleFinish 는 SaveAsync 를 **호출하지 않는다** (BuildDto 로 _collected 누적만).

CycleResultDto / EVisionResultType / EContextState / EventSequenceStateChanged 는 모두 ReringProject.Sequence 네임스페이스 (using 추가 불필요, 동일 namespace).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SequenceBase.StartSubset 부분 실행 진입점 추가</name>
  <read_first>
    - WPF_Example/Sequence/Sequence/SequenceBase.cs (L341-359 StartAll 메서드 — 복사 기준 + 파일 K&R 스타일 확인)
  </read_first>
  <files>WPF_Example/Sequence/Sequence/SequenceBase.cs</files>
  <action>
StartAll 메서드(L359 닫는 중괄호) 바로 아래에 `StartSubset` public 메서드를 추가한다. K&R 브레이스 스타일(여는 중괄호 같은 줄)을 유지한다. 변경 라인에 `//260616 hbk Phase 51` 주석을 단다.

구현 전략 = **연속 범위(CurrentActionIndex/EndActionIndex)** 방식 — StartAll 과 동일하게 시퀀스 엔진이 first→last 인덱스를 순차 실행한다. 정렬 후 첫/끝 인덱스를 경계로 설정한다.

```csharp
//260616 hbk Phase 51: 선택 Action(SHOT) 인덱스 집합으로 부분 실행 (D-01 SHOT 다중 선택, StartAll 변형)
public bool StartSubset(int[] actionIndices, TestPacket packet) {
    if (State != EContextState.Idle) return false;
    if (Actions == null || Actions.Length == 0) return false;
    if (actionIndices == null || actionIndices.Length == 0) return false;

    Array.Sort(actionIndices);
    int first = actionIndices[0];
    int last = actionIndices[actionIndices.Length - 1];
    if (first < 0 || last >= Actions.Length) return false;

    RequestPacket = packet;
    CurrentActionIndex = first;
    EndActionIndex = last;

    Context.Clear();
    IsFinished = false;

    if (OnStart != null) {
        OnStart.Invoke(Context);
    }
    Command = ESequenceCommmand.Start; //260616 hbk Phase 51 OnStart 이후 Command 설정 (StartAll 동일 순서)
    return true;
}
```

`System` (Array.Sort) 는 파일 상단 using 에 이미 존재할 가능성이 높다 — 없으면 `Array.Sort` 를 `System.Array.Sort` 로 fully-qualify 하여 using 추가 0 으로 처리한다. StartAll 본문은 절대 수정하지 않는다 (회귀 0).

NOTE(설계 한계 — SUMMARY 에 기록): 연속 범위 방식은 first~last 사이의 비선택 SHOT 도 실행한다. Phase 51 의 UI(51-02)는 "동일 시퀀스 내 SHOT 선택" 을 강제하고, 일반적 운용(시퀀스 전체 또는 연속 SHOT 선택)에서 정확하다. 불연속 SHOT 선택의 엄밀 필터링이 향후 필요하면 마스크 필터 전략으로 교체한다 (별도 phase). 이 한계를 SUMMARY 의 "주의/한계" 섹션에 명시한다.
  </action>
  <verify>
    <automated>grep -n "public bool StartSubset(" WPF_Example/Sequence/Sequence/SequenceBase.cs</automated>
  </verify>
  <acceptance_criteria>
    - `SequenceBase.cs` 가 `public bool StartSubset(int[] actionIndices, TestPacket packet) {` 를 정확히 포함한다
    - `SequenceBase.cs` 가 `CurrentActionIndex = first;` 와 `EndActionIndex = last;` 를 포함한다
    - 기존 `public bool StartAll(TestPacket packet) {` 줄과 그 본문은 그대로 존재한다 (StartAll diff 0줄)
    - 추가된 모든 라인에 `//260616 hbk Phase 51` 주석이 1개 이상 존재한다
  </acceptance_criteria>
  <done>StartSubset 메서드가 StartAll 아래에 추가되고, StartAll 본문은 무변경이다.</done>
</task>

<task type="auto">
  <name>Task 2: BatchRunService.cs 신규 + csproj 등록</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs (전체 — Start/HandleFinish/TriggerNext/Stop 복사 기준, Allman 스타일)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (L150-171 HandleManualCyclePersist — 중복 저장 계약 확인)
    - WPF_Example/DatumMeasurement.csproj (Compile ItemGroup — RepeatRunService.cs 등록 줄 위치 확인)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/BatchRunService.cs, WPF_Example/DatumMeasurement.csproj</files>
  <action>
`WPF_Example/Custom/Sequence/Inspection/BatchRunService.cs` 를 신규 생성한다. RepeatRunService.cs 를 베이스로, Allman 브레이스 스타일을 유지하고, 모든 라인에 `//260616 hbk Phase 51` 주석을 단다.

차이점 (RepeatRunService 대비):
1. 클래스명 = `BatchRunService`, namespace = `ReringProject.Sequence`.
2. using 블록은 RepeatRunService.cs L2-8 와 동일 (System, System.Collections.Generic, System.Threading.Tasks, ReringProject.Network, ReringProject.Setting, ReringProject.UI, ReringProject.Utility).
3. 진입점 = `StartBatch(InspectionSequence seq, List<int> selectedShotIndices)` (반복 횟수 인자 없음, targetCount 고정 1).
4. 새 필드 `private List<int> _selectedIndices;` 추가.
5. `TriggerNext()` 의 Dispatcher.BeginInvoke 안에서 `_seq.StartAll(null)` 대신 `_seq.StartSubset(_selectedIndices.ToArray(), null)` 호출.
6. `HandleFinish` 에서 **CycleResultSerializer.SaveAsync 를 호출하지 않는다** (InspectionSequence.HandleManualCyclePersist 가 packet==null 수동 경로에서 이미 저장 — 중복 방지, 위 interfaces 계약). BuildDto → `_collected.Add(dto)` 만 수행.

파일 전체 내용:

```csharp
//260616 hbk Phase 51 BATCH-01 선택 SHOT 일괄 검사 실행 서비스 (RepeatRunService 파생 패턴, 코드 중복 최소화)
using System;
using System.Collections.Generic;
using System.Threading.Tasks;
using ReringProject.Network;
using ReringProject.Setting;
using ReringProject.UI;
using ReringProject.Utility;

namespace ReringProject.Sequence
{
    /// <summary>
    //260616 hbk Phase 51: 트리에서 선택된 SHOT 인덱스 집합을 1사이클 일괄 실행하고 결과를 누적한다.
    /// RepeatRunService 와 동일한 Start → OnFinish → HandleFinish → 누적 패턴.
    /// 차이: N회 반복 대신 선택 SHOT 1사이클. SaveAsync 미호출 (InspectionSequence.HandleManualCyclePersist 위임 — 중복 저장 방지).
    /// 누적/Export 경로는 Phase 41.1 (Gage R&R) 가 재사용 가능하도록 RepeatRunService 패턴 정합 (D-08).
    /// </summary>
    public class BatchRunService
    {
        //260616 hbk Phase 51: 1사이클 완료 시 발화. arg = 누적된 CycleResultDto 목록 (수동 모드 = 1개).
        public event Action<List<CycleResultDto>> OnBatchComplete;

        //260616 hbk Phase 51: 사이클 완료마다 발화. (완료, 목표).
        public event Action<int, int> OnProgressChanged;

        public bool IsRunning { get; private set; }
        public int CompletedCount { get; private set; }
        public int TargetCount { get; private set; }

        private InspectionSequence _seq;
        private List<int> _selectedIndices; //260616 hbk Phase 51: 실행 대상 로컬 SHOT 인덱스
        private List<CycleResultDto> _collected;
        private EventSequenceStateChanged _onFinishHandler;
        private readonly object _lock = new object();

        //260616 hbk Phase 51: 선택 SHOT 1사이클 일괄 검사 시작. IsRunning 또는 입력 부재 시 즉시 반환.
        public void StartBatch(InspectionSequence seq, List<int> selectedShotIndices)
        {
            if (IsRunning)
            {
                return;
            }

            if (seq == null || selectedShotIndices == null || selectedShotIndices.Count == 0)
            {
                return;
            }

            IsRunning = true;
            _seq = seq;
            _selectedIndices = selectedShotIndices;
            TargetCount = 1;
            CompletedCount = 0;
            _collected = new List<CycleResultDto>();

            _onFinishHandler = (ctx) => HandleFinish(ctx);
            _seq.OnFinish += _onFinishHandler;

            TriggerNext();
        }

        //260616 hbk Phase 51: 강제 중단. OnFinish 구독 해제 + IsRunning=false.
        public void Stop()
        {
            if (_seq != null && _onFinishHandler != null)
            {
                _seq.OnFinish -= _onFinishHandler;
            }

            IsRunning = false;
            _onFinishHandler = null;
            _seq = null;
            _selectedIndices = null;
        }

        //260616 hbk Phase 51: OnFinish 핸들러 — recipeManager 순회로 종합판정 산출 후 BuildDto 누적.
        //  SaveAsync 미호출 (InspectionSequence.HandleManualCyclePersist 가 packet==null 수동 경로에서 이미 저장 — 중복 방지).
        private void HandleFinish(SequenceContext ctx)
        {
            lock (_lock)
            {
                var seqHandler = SystemHandler.Handle.Sequences;
                if (seqHandler == null)
                {
                    return;
                }

                var recipeManager = seqHandler.RecipeManager;
                if (recipeManager == null)
                {
                    return;
                }

                bool anySkip = false;
                bool allPass = true;
                foreach (var shot in recipeManager.Shots)
                {
                    foreach (var fai in shot.FAIList)
                    {
                        if (fai.WasDatumSkipped)
                        {
                            anySkip = true;
                        }
                        else if (!fai.IsPass)
                        {
                            allPass = false;
                        }
                    }
                }

                EVisionResultType resultType;
                if (anySkip)
                {
                    resultType = EVisionResultType.NotExist;
                }
                else if (!allPass)
                {
                    resultType = EVisionResultType.NG;
                }
                else
                {
                    resultType = EVisionResultType.OK;
                }

                string recipeName = SystemHandler.Handle.Setting.CurrentRecipeName;
                string seqName = _seq != null ? _seq.Name : null;
                CycleResultDto dto = CycleResultSerializer.BuildDto(
                    recipeManager, resultType, DateTime.Now, recipeName, seqName);

                //260616 hbk Phase 51: SaveAsync 미호출 — HandleManualCyclePersist 위임 (중복 저장 방지). 누적만.
                _collected.Add(dto);
                CompletedCount++;

                OnProgressChanged?.Invoke(CompletedCount, TargetCount);

                if (CompletedCount >= TargetCount)
                {
                    var finalList = new List<CycleResultDto>(_collected);
                    Stop();
                    OnBatchComplete?.Invoke(finalList);
                }
                else
                {
                    TriggerNext();
                }
            }
        }

        //260616 hbk Phase 51: 시퀀스 Idle 대기 후 StartSubset 호출. Background 우선순위로 이전 OnFinish 핸들러 선행 완료 보장.
        private void TriggerNext()
        {
            if (!IsRunning || _seq == null)
            {
                return;
            }

            if (_seq.State == EContextState.Idle)
            {
                System.Windows.Application.Current.Dispatcher.BeginInvoke(
                    System.Windows.Threading.DispatcherPriority.Background,
                    new Action(() =>
                    {
                        if (!IsRunning || _seq == null || _selectedIndices == null)
                        {
                            return;
                        }

                        if (_seq.State == EContextState.Idle)
                        {
                            _seq.StartSubset(_selectedIndices.ToArray(), null);
                        }
                        else
                        {
                            Task.Delay(50).ContinueWith(_ => TriggerNext());
                        }
                    }));
            }
            else
            {
                Task.Delay(50).ContinueWith(_ => TriggerNext());
            }
        }
    }
}
```

그 후 `DatumMeasurement.csproj` 의 Compile ItemGroup 에서 `RepeatRunService.cs` 가 등록된 `<Compile Include="..." />` 줄을 찾아, 그 바로 아래에 동일 패턴으로 BatchRunService.cs 등록 줄을 추가한다:
```xml
<Compile Include="Custom\Sequence\Inspection\BatchRunService.cs" />
```
(경로 구분자는 csproj 기존 줄과 동일하게 백슬래시 사용. RepeatRunService.cs 등록 줄의 정확한 형식을 복사하여 파일명만 교체한다.)
  </action>
  <verify>
    <automated>grep -n "StartSubset\|SaveAsync\|public void StartBatch" WPF_Example/Custom/Sequence/Inspection/BatchRunService.cs && grep -n "BatchRunService.cs" WPF_Example/DatumMeasurement.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `BatchRunService.cs` 가 `public class BatchRunService` 와 `public void StartBatch(InspectionSequence seq, List<int> selectedShotIndices)` 를 포함한다
    - `BatchRunService.cs` 가 `_seq.StartSubset(_selectedIndices.ToArray(), null);` 를 포함한다
    - `BatchRunService.cs` 가 `CycleResultSerializer.SaveAsync` 를 **포함하지 않는다** (grep `SaveAsync` 결과 0건)
    - `BatchRunService.cs` 가 `OnBatchComplete` 이벤트와 `CycleResultSerializer.BuildDto(` 를 포함한다
    - `DatumMeasurement.csproj` 가 `Custom\Sequence\Inspection\BatchRunService.cs` Compile 등록 줄을 포함한다
    - namespace 가 `ReringProject.Sequence` 이다
  </acceptance_criteria>
  <done>BatchRunService.cs 가 생성되고 csproj 에 등록되며, SaveAsync 미호출(중복 저장 방지)이다.</done>
</task>

<task type="auto">
  <name>Task 3: Debug|x64 빌드 검증 (SIMUL_MODE)</name>
  <read_first>
    - WPF_Example/DatumMeasurement.csproj (빌드 설정 확인)
  </read_first>
  <files>WPF_Example/Sequence/Sequence/SequenceBase.cs, WPF_Example/Custom/Sequence/Inspection/BatchRunService.cs</files>
  <action>
MSBuild 로 Debug|x64 구성(SIMUL_MODE 활성)을 빌드하여 컴파일 0 errors 를 확인한다. C# 7.2 호환(switch expression / nullable ref / record / range 미사용)을 점검한다 — 본 플랜 코드는 `?.Invoke`, `?:` 삼항, ternary 만 사용하며 C# 7.2 합법이다.

PowerShell 빌드 명령:
```
msbuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /verbosity:minimal
```
빌드 출력에서 `0 Error(s)` 를 확인한다. 신규 warning 0 (Phase 43.2 baseline 대비) 을 목표로 한다.
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /verbosity:minimal</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug|x64 빌드가 `0 Error(s)` 로 종료한다 (exit 0)
    - 본 플랜 추가 코드로 인한 신규 warning 0
  </acceptance_criteria>
  <done>Debug|x64 빌드가 0 errors 로 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → BatchRunService | 사용자 선택 SHOT 인덱스 목록(메모리 내, 신뢰 가능) — 외부 네트워크 입력 아님 |
| BatchRunService → SequenceBase | 인덱스 배열 경계 (Actions.Length 초과 시 OOR 가능) |

이 plan 은 로컬 WPF 인메모리 경로만 다룬다. 네트워크 입력/인증/직렬화 외부 입력 없음 (xlsx export 는 51-02 의 파일 다이얼로그 경로). 위협 표면 최소.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Denial of Service (인덱스 경계) | SequenceBase.StartSubset | mitigate | `first < 0 || last >= Actions.Length` 가드로 IndexOutOfRange 차단; null/empty 배열 early-return |
| T-51-02 | Tampering (동시 재진입) | BatchRunService.StartBatch | mitigate | `IsRunning` 가드 + `lock(_lock)` HandleFinish 로 중복 시작/경합 방지 (RepeatRunService 동일 패턴) |
| T-51-03 | Repudiation/Data integrity (이중 저장) | BatchRunService.HandleFinish | mitigate | SaveAsync 미호출 — HandleManualCyclePersist 단일 저장 경로 위임으로 cycle.json 중복 차단 |
</threat_model>

<verification>
- `grep "public bool StartSubset(" SequenceBase.cs` → 1건
- `grep "SaveAsync" BatchRunService.cs` → 0건 (중복 저장 방지 확인)
- `grep "BatchRunService.cs" DatumMeasurement.csproj` → 1건
- msbuild Debug|x64 → 0 errors
</verification>

<success_criteria>
- StartSubset 가 선택 인덱스 범위만 실행하고 StartAll 은 무변경(회귀 0)
- BatchRunService 가 RepeatRunService 패턴 정합 + SaveAsync 미호출
- Debug|x64 빌드 0 errors, C# 7.2 호환
</success_criteria>

<output>
After completion, create `.planning/phases/51-export-2026-06-16-poc-3/51-01-SUMMARY.md`.
SUMMARY 에 반드시 기록할 항목: (1) StartSubset 연속범위 전략의 한계(비선택 중간 SHOT 실행) 및 51-02 UI 가 동일 시퀀스 SHOT 선택을 강제한다는 점, (2) SaveAsync 미호출로 HandleManualCyclePersist 위임한 중복 저장 방지 계약, (3) BatchRunService 의 OnBatchComplete 시그니처(List&lt;CycleResultDto&gt;)를 51-02 가 소비.
</output>
