---
phase: 51-export-2026-06-16-poc-3
plan: 02
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - WPF_Example/UI/ControlItem/NodeViewModel.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: false
requirements: [BATCH-01]

must_haves:
  truths:
    - "사용자가 트리에서 SHOT 노드에 체크박스로 여러 SHOT 을 다중 선택할 수 있다 (SHOT 노드만 체크박스 노출)"
    - "체크된 SHOT 들이 모두 동일 시퀀스 소속일 때만 일괄 검사가 실행된다 (Top끼리/Bottom끼리, 교차 차단)"
    - "일괄 검사 버튼이 선택 SHOT 들을 1사이클 일괄 실행하고 결과를 누적한다"
    - "수동 Export 버튼이 누적된 전 SHOT/FAI 결과를 단일 xlsx 로 추출한다 (Phase 40 포맷 재사용)"
    - "기존 단일 SHOT RUN(btn_start) 경로는 변경 없이 동작한다 (회귀 0)"
  artifacts:
    - path: "WPF_Example/UI/ControlItem/NodeViewModel.cs"
      provides: "IsChecked / IsCheckboxVisible 다중 선택 프로퍼티"
      contains: "public bool IsChecked"
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml"
      provides: "SHOT 체크박스 + 일괄검사/Export 버튼 UI"
      contains: "btn_batchRun"
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "Btn_batchRun_Click / Btn_batchExport_Click 핸들러 + 누적 보관"
      contains: "Btn_batchRun_Click"
  key_links:
    - from: "InspectionListView.Btn_batchRun_Click"
      to: "BatchRunService.StartBatch"
      via: "체크된 SHOT 수집 → 동일 시퀀스 검증 → ComputeLocalShotIndex → StartBatch(seq, indices)"
      pattern: "StartBatch\\("
    - from: "InspectionListView.Btn_batchExport_Click"
      to: "RepeatExcelExportService.Export"
      via: "_batchAccumulated → SaveFileDialog → RepeatExcelExportService.Export(list, recipeName, path)"
      pattern: "RepeatExcelExportService\\.Export"
    - from: "BatchRunService.OnBatchComplete"
      to: "InspectionListView._batchAccumulated"
      via: "Dispatcher.Invoke → _batchAccumulated.AddRange(cycles) → btn_batchExport.IsEnabled"
      pattern: "_batchAccumulated\\.AddRange"
---

<objective>
트리에서 SHOT 다중 선택(체크박스) UI 를 추가하고, 선택 SHOT 들을 한 시퀀스 내에서 일괄 검사(`btn_batchRun`)한 뒤 누적 결과를 수동 Export 버튼(`btn_batchExport`)으로 단일 xlsx 추출하는 경로를 구현한다. 51-01 의 `BatchRunService` 와 `SequenceBase.StartSubset` 을 소비하고, Export 는 기존 `RepeatExcelExportService.Export(List<CycleResultDto>, recipeName, path)` (이미 존재) 를 재사용한다 (D-06 포맷 재사용 + D-08 코드 중복 0).

Purpose: Phase 51 의 사용자 진입점. 샷 개별 트리거 대신 시퀀스 단위 일괄 데이터 추출(POC 2026-06-30 산출물).
Output: NodeViewModel 다중선택 프로퍼티 + InspectionListView XAML 체크박스/버튼 + code-behind 핸들러. 마지막 SIMUL UAT 체크포인트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-export-2026-06-16-poc-3/51-CONTEXT.md
@.planning/phases/51-export-2026-06-16-poc-3/51-PATTERNS.md
@.planning/phases/51-export-2026-06-16-poc-3/51-01-PLAN.md
@./CLAUDE.md

<interfaces>
<!-- 51-01 이 제공한 계약 (소비) -->
- `BatchRunService` (ReringProject.Sequence): `void StartBatch(InspectionSequence seq, List<int> selectedShotIndices)`, `event Action<List<CycleResultDto>> OnBatchComplete`, `event Action<int,int> OnProgressChanged`, `bool IsRunning`, `void Stop()`
- `SequenceBase.StartSubset(int[], TestPacket)` — BatchRunService 내부에서 호출 (UI 는 직접 호출 안 함)

<!-- 기존 코드베이스 계약 (소비) -->
- `RepeatExcelExportService.Export(List<CycleResultDto> cycles, string recipeName, string path)` → bool — ReringProject.Export 네임스페이스. 이미 존재(ReviewerWindow.xaml.cs L433 사용). 다중 CycleResultDto → xlsx, Phase 40 포맷.
- `NodeViewModel` (ReringProject.UI): `object Param` (=Node.ParamData), `ENodeType NodeType`, `ESequence SequenceID`, `ObservableCollection<NodeViewModel> Children`, `void RaisePropertyChanged(string)` (Observable 베이스). bool 프로퍼티 패턴 = IsSelected (L170-182): private 백킹 + `if (x==value) return;` + RaisePropertyChanged. 파일 스타일 = K&R.
- `ENodeType.Action` + `Param is ShotConfig` = SHOT 노드 식별.
- `ComputeLocalShotIndex(InspectionRecipeManager mgr, ShotConfig target, ESequence seqId)` → int (InspectionListView.xaml.cs L419, private static). 시퀀스 소유 로컬 SHOT 인덱스 반환.
- `SystemHandler.Handle.Sequences[seqID]` → SequenceBase. `as InspectionSequence` 캐스트로 BatchRunService 인자.
- `SystemHandler.Handle.Sequences.IsIdle`, `.RecipeManager`.
- `SystemHandler.Handle.Setting.CurrentRecipeName`, `.ResultSavePath`.
- `CustomMessageBox.Show(title, msg, MessageBoxImage)`.
- 기존 트리 컨트롤: `treeListBox_sequence` (PropertyTools.Wpf.TreeListBox, HierarchySource={Binding Root}), DataTemplate (XAML L46-60, Grid 3열: 아이콘/이름/Column2 빈칸).
- 버튼 추가 위치: Recipe Name Grid (XAML L149-168) — btn_start(Col1)/btn_RecipeSelect(Col2).

다중 선택 UI 방식 = **NodeViewModel.IsChecked 체크박스** 채택. 근거: PropertyTools.Wpf.TreeListBox 의 SelectionMode="Extended" 지원이 불확실(PATTERNS.md 핵심 설계질문 #1) — 체크박스 방식은 컨트롤 의존성 없이 확정적. SHOT 노드(Action + ShotConfig)에만 체크박스 노출.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NodeViewModel 다중 선택 프로퍼티 추가</name>
  <read_first>
    - WPF_Example/UI/ControlItem/NodeViewModel.cs (L124-182 Param/NodeType/IsSelected 패턴 — 복사 기준, K&R 스타일)
  </read_first>
  <files>WPF_Example/UI/ControlItem/NodeViewModel.cs</files>
  <action>
`IsSelected` 프로퍼티(L170-182) 바로 아래에 `IsChecked` 와 `IsCheckboxVisible` 프로퍼티를 추가한다. K&R 스타일 유지, 변경 라인에 `//260616 hbk Phase 51` 주석.

```csharp
//260616 hbk Phase 51: SHOT 다중 선택용 체크박스 상태 (일괄 검사 대상)
private bool isChecked;

public bool IsChecked {
    get {
        return this.isChecked;
    }
    set {
        if (isChecked == value) return;
        this.isChecked = value;
        RaisePropertyChanged("IsChecked");
    }
}

//260616 hbk Phase 51: SHOT 노드(Action + ShotConfig param)만 체크박스 노출
public bool IsCheckboxVisible {
    get {
        return NodeType == ENodeType.Action && Param is ShotConfig;
    }
}
```

`ShotConfig` 타입 참조를 위해 파일 상단 using 에 `ReringProject.Sequence` 가 이미 있는지 확인한다(L8 `using ReringProject.Sequence;` 존재). 없으면 추가한다. `ShotConfig` 가 다른 namespace 면 fully-qualify 한다.
  </action>
  <verify>
    <automated>grep -n "public bool IsChecked\|public bool IsCheckboxVisible" WPF_Example/UI/ControlItem/NodeViewModel.cs</automated>
  </verify>
  <acceptance_criteria>
    - `NodeViewModel.cs` 가 `public bool IsChecked {` 와 `public bool IsCheckboxVisible {` 를 포함한다
    - IsChecked setter 가 `RaisePropertyChanged("IsChecked")` 를 호출한다
    - IsCheckboxVisible 가 `NodeType == ENodeType.Action && Param is ShotConfig` 를 반환한다
    - 기존 IsSelected 프로퍼티는 무변경
  </acceptance_criteria>
  <done>IsChecked / IsCheckboxVisible 프로퍼티가 추가되고 기존 프로퍼티 무변경이다.</done>
</task>

<task type="auto">
  <name>Task 2: XAML 체크박스 + 일괄검사/Export 버튼 추가</name>
  <read_first>
    - WPF_Example/UI/ControlItem/InspectionListView.xaml (L46-60 DataTemplate, L149-168 Recipe Name Grid — 버튼/체크박스 추가 지점)
  </read_first>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml</files>
  <action>
**(A) DataTemplate 에 SHOT 체크박스 추가** — DataTemplate(L46-60) 의 Grid.Column="2"(현재 빈칸)에 CheckBox 를 삽입한다. SHOT 노드만 보이도록 `IsCheckboxVisible` 를 BooleanToVisibility 로 바인딩한다.

먼저 UserControl.Resources(L37 근처, DataTemplate 위)에 BooleanToVisibilityConverter 가 이미 등록돼 있는지 확인한다. 없으면 Resources 에 추가:
```xml
<BooleanToVisibilityConverter x:Key="boolToVis"/>
```
(System.Windows.Controls 기본 컨버터 — 추가 using 불필요. xmlns 기본 네임스페이스에 존재.)

DataTemplate 의 Grid.Column="1" EditableTextBlock 다음, Grid.Column="2" 위치에 추가:
```xml
<!-- 260616 hbk Phase 51: SHOT 다중 선택 체크박스 (SHOT 노드만 노출) -->
<CheckBox Grid.Column="2" VerticalAlignment="Center" Margin="4 0 4 0"
          IsChecked="{Binding IsChecked, Mode=TwoWay}"
          Visibility="{Binding IsCheckboxVisible, Converter={StaticResource boolToVis}}"/>
```

**(B) Recipe Name Grid 에 일괄검사/Export 버튼 추가** — 현재 Recipe Name Grid(L149-168)는 3열(6*/3*/2*)이며 btn_start(Col1), btn_RecipeSelect(Col2)가 있다. 일괄 검사/Export 버튼 2개를 위한 열을 확장한다.

ColumnDefinitions 를 5열로 변경:
```xml
<Grid.ColumnDefinitions>
    <ColumnDefinition Width="4*"></ColumnDefinition>  <!-- RecipeName -->
    <ColumnDefinition Width="2*"></ColumnDefinition>  <!-- btn_start (RUN) -->
    <ColumnDefinition Width="3*"></ColumnDefinition>  <!-- btn_batchRun (일괄검사) -->
    <ColumnDefinition Width="3*"></ColumnDefinition>  <!-- btn_batchExport (Export) -->
    <ColumnDefinition Width="1*"></ColumnDefinition>  <!-- btn_RecipeSelect (...) -->
</Grid.ColumnDefinitions>
```
- tb_RecipeName: Grid.Column="0" 유지
- btn_start: Grid.Column="1" 유지
- btn_RecipeSelect: Grid.Column="1" → Grid.Column="4" 로 변경
- 그 사이 Col2/Col3 에 신규 버튼 2개 추가:
```xml
<!-- 260616 hbk Phase 51 BATCH-01: 선택 SHOT 일괄 검사 -->
<Button x:Name="btn_batchRun" Grid.Column="2" Margin="2,2,2,2" FontSize="16" Click="Btn_batchRun_Click">
    <TextBlock Text="일괄검사" HorizontalAlignment="Center" VerticalAlignment="Center"></TextBlock>
</Button>
<!-- 260616 hbk Phase 51 BATCH-01: 누적분 수동 엑셀 Export (D-05) -->
<Button x:Name="btn_batchExport" Grid.Column="3" Margin="2,2,2,2" FontSize="16" IsEnabled="False" Click="Btn_batchExport_Click">
    <TextBlock Text="일괄Export" HorizontalAlignment="Center" VerticalAlignment="Center"></TextBlock>
</Button>
```
btn_batchExport 는 초기 비활성(IsEnabled="False"), 누적 후 code-behind 에서 활성화한다.
  </action>
  <verify>
    <automated>grep -n "btn_batchRun\|btn_batchExport\|IsCheckboxVisible" WPF_Example/UI/ControlItem/InspectionListView.xaml</automated>
  </verify>
  <acceptance_criteria>
    - `InspectionListView.xaml` 가 `x:Name="btn_batchRun"` 와 `Click="Btn_batchRun_Click"` 를 포함한다
    - `InspectionListView.xaml` 가 `x:Name="btn_batchExport"` 와 `Click="Btn_batchExport_Click"` 와 `IsEnabled="False"` 를 포함한다
    - DataTemplate 가 `IsChecked="{Binding IsChecked, Mode=TwoWay}"` 와 `Visibility="{Binding IsCheckboxVisible,` 를 포함한다
    - btn_RecipeSelect 가 `Grid.Column="4"` 로 이동했고, btn_start 는 `Grid.Column="1"` 유지
  </acceptance_criteria>
  <done>체크박스 + 일괄검사/Export 버튼이 XAML 에 추가되고 기존 버튼 레이아웃이 보존된다.</done>
</task>

<task type="auto">
  <name>Task 3: code-behind 일괄검사/Export 핸들러 + 누적 보관</name>
  <read_first>
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs (L323-435 Btn_start_Click / ResolveRunnableAction / ComputeLocalShotIndex — 재사용 패턴, K&R 스타일)
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs (L412-457 Button_RepeatExport_Click — Export 다이얼로그 + RepeatExcelExportService.Export 패턴)
  </read_first>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <action>
InspectionListView 클래스에 필드 2개 + 핸들러 2개 + 트리 순회 헬퍼 1개를 추가한다. K&R 스타일 유지, 변경 라인에 `//260616 hbk Phase 51` 주석. 파일 상단 using 에 `ReringProject.Sequence`(BatchRunService/CycleResultDto), `System.Collections.Generic`(List) 가 있는지 확인하고 없으면 추가한다.

**(A) 필드 추가** (클래스 멤버 영역, 기존 필드 근처):
```csharp
//260616 hbk Phase 51 BATCH-01: 일괄 검사 누적 결과 + 서비스 인스턴스 (UI 소유, static 금지)
private System.Collections.Generic.List<CycleResultDto> _batchAccumulated = new System.Collections.Generic.List<CycleResultDto>();
private BatchRunService _batchService;
```

**(B) 체크된 SHOT 노드 수집 헬퍼** — 트리를 재귀 순회하여 IsChecked && IsCheckboxVisible 인 NodeViewModel 을 수집한다:
```csharp
//260616 hbk Phase 51: 트리에서 체크된 SHOT 노드 수집 (재귀)
private void CollectCheckedShots(NodeViewModel node, System.Collections.Generic.List<NodeViewModel> acc) {
    if (node == null) return;
    if (node.IsChecked && node.IsCheckboxVisible) {
        acc.Add(node);
    }
    foreach (NodeViewModel child in node.Children) {
        CollectCheckedShots(child, acc);
    }
}
```

**(C) Btn_batchRun_Click 핸들러** — 체크 SHOT 수집 → 동일 시퀀스 검증(D-02) → 로컬 인덱스 수집 → BatchRunService.StartBatch:
```csharp
//260616 hbk Phase 51 BATCH-01: 선택 SHOT 일괄 검사 (D-01/D-02/D-03)
private void Btn_batchRun_Click(object sender, RoutedEventArgs e) {
    var root = treeListBox_sequence.Items.Count > 0 ? treeListBox_sequence.Items[0] as NodeViewModel : null;
    var checkedShots = new System.Collections.Generic.List<NodeViewModel>();
    if (root != null) {
        CollectCheckedShots(root, checkedShots);
    }

    if (checkedShots.Count == 0) {
        CustomMessageBox.Show("일괄 검사", "검사할 SHOT 을 체크하세요.", MessageBoxImage.Warning);
        return;
    }

    // D-02: 모든 체크 SHOT 이 동일 시퀀스 소속이어야 함 (Top끼리 / Bottom끼리)
    ESequence seqID = checkedShots[0].SequenceID;
    foreach (NodeViewModel n in checkedShots) {
        if (n.SequenceID != seqID) {
            CustomMessageBox.Show("일괄 검사",
                "한 시퀀스 내의 SHOT 만 함께 선택할 수 있습니다.\n(Top 끼리 / Bottom 끼리)",
                MessageBoxImage.Error);
            return;
        }
    }

    if (!SystemHandler.Handle.Sequences.IsIdle) {
        CustomMessageBox.Show("일괄 검사", "시퀀스가 이미 실행 중입니다.", MessageBoxImage.Error);
        return;
    }

    var seqBase = SystemHandler.Handle.Sequences[seqID];
    InspectionSequence inspSeq = seqBase as InspectionSequence;
    if (inspSeq == null) {
        CustomMessageBox.Show("일괄 검사", "InspectionSequence 를 찾을 수 없습니다.", MessageBoxImage.Error);
        return;
    }

    var mgr = SystemHandler.Handle.Sequences.RecipeManager;
    var indices = new System.Collections.Generic.List<int>();
    foreach (NodeViewModel n in checkedShots) {
        ShotConfig shot = n.Param as ShotConfig;
        if (shot == null) continue;
        int localIdx = ComputeLocalShotIndex(mgr, shot, seqID);
        if (localIdx >= 0) indices.Add(localIdx);
    }

    if (indices.Count == 0) {
        CustomMessageBox.Show("일괄 검사", "선택 SHOT 의 인덱스를 해석할 수 없습니다.", MessageBoxImage.Error);
        return;
    }

    if (_batchService != null && _batchService.IsRunning) {
        CustomMessageBox.Show("일괄 검사", "일괄 검사가 이미 진행 중입니다.", MessageBoxImage.Warning);
        return;
    }

    _batchService = new BatchRunService();
    _batchService.OnBatchComplete += OnBatchComplete;
    _batchService.StartBatch(inspSeq, indices);
}

//260616 hbk Phase 51 BATCH-01: 일괄 검사 1사이클 완료 → 누적 + Export 버튼 활성 (D-04 append, D-05 수동 Export)
private void OnBatchComplete(System.Collections.Generic.List<CycleResultDto> cycles) {
    Dispatcher.Invoke(new Action(delegate {
        if (cycles != null) {
            _batchAccumulated.AddRange(cycles);
        }
        btn_batchExport.IsEnabled = (_batchAccumulated.Count > 0);
    }));
}
```
NOTE: D-04(실모드 검사 사이클마다 append)는 동일 _batchAccumulated 에 매 일괄검사 결과가 AddRange 로 누적되므로 자연 충족된다. 사용자가 재검사할 때마다 누적분이 늘어난다. _batchAccumulated 초기화는 Export 성공 후 또는 레시피 변경 시점에 하지 않는다(누적 유지) — 명시적 초기화 버튼은 Phase 51 범위 밖(누적 우선).

**(D) Btn_batchExport_Click 핸들러** — ReviewerWindow.Button_RepeatExport_Click 이식, _batchAccumulated 사용:
```csharp
//260616 hbk Phase 51 BATCH-01: 누적분 수동 엑셀 Export (D-05/D-06 Phase 40 포맷 재사용)
private void Btn_batchExport_Click(object sender, RoutedEventArgs e) {
    if (_batchAccumulated == null || _batchAccumulated.Count == 0) {
        CustomMessageBox.Show("일괄 Export", "먼저 일괄 검사를 실행하세요.", MessageBoxImage.Warning);
        return;
    }

    string initialDir = SystemHandler.Handle.Setting.ResultSavePath;
    string recipeName = SystemHandler.Handle.Setting.CurrentRecipeName;
    if (recipeName == null) recipeName = "";

    var dlg = new Microsoft.Win32.SaveFileDialog();
    dlg.Filter = "Excel 파일 (*.xlsx)|*.xlsx";
    dlg.FileName = "batch_" + DateTime.Now.ToString("yyyyMMdd_HHmmss") + ".xlsx";
    dlg.InitialDirectory = initialDir;

    if (dlg.ShowDialog() == true) {
        bool ok = ReringProject.Export.RepeatExcelExportService.Export(
            _batchAccumulated, recipeName, dlg.FileName);
        string msg;
        if (ok) msg = "저장 완료:\n" + dlg.FileName; else msg = "export 실패 (로그 확인)";
        MessageBoxImage icon;
        if (ok) icon = MessageBoxImage.Information; else icon = MessageBoxImage.Error;
        CustomMessageBox.Show("일괄 엑셀 Export", msg, icon);
    }
}
```
`DateTime` / `Microsoft.Win32` 는 ReviewerWindow 에서 fully-qualify 또는 using 으로 해결된 패턴 — 본 파일에 `using System;` 존재 확인, `Microsoft.Win32.SaveFileDialog` 는 fully-qualify 로 using 추가 0.
  </action>
  <verify>
    <automated>grep -n "Btn_batchRun_Click\|Btn_batchExport_Click\|_batchAccumulated.AddRange\|StartBatch\|RepeatExcelExportService.Export" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - `InspectionListView.xaml.cs` 가 `private void Btn_batchRun_Click(` 와 `private void Btn_batchExport_Click(` 를 포함한다
    - 동일 시퀀스 검증 분기 `if (n.SequenceID != seqID)` 가 존재한다 (D-02)
    - `_batchService.StartBatch(inspSeq, indices)` 호출이 존재한다
    - `_batchAccumulated.AddRange(cycles)` 가 OnBatchComplete 에 존재한다
    - `ReringProject.Export.RepeatExcelExportService.Export(_batchAccumulated, recipeName, dlg.FileName)` 호출이 존재한다
    - 기존 `Btn_start_Click` / `ResolveRunnableAction` / `ComputeLocalShotIndex` 메서드 본문 무변경
  </acceptance_criteria>
  <done>일괄검사/Export 핸들러와 누적 보관이 추가되고 기존 단일 RUN 경로 무변경이다.</done>
</task>

<task type="auto">
  <name>Task 4: Debug|x64 빌드 검증 (SIMUL_MODE)</name>
  <read_first>
    - WPF_Example/DatumMeasurement.csproj
  </read_first>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <action>
MSBuild Debug|x64(SIMUL_MODE 활성) 빌드로 컴파일 0 errors 를 확인한다. C# 7.2 호환 점검: 본 플랜은 `delegate { }` 익명 메서드(C# 2+), 명시적 if/else, fully-qualify 호출만 사용 — switch expression / nullable ref / record / range 미사용. XAML BooleanToVisibilityConverter 는 WPF 기본 컨버터.

```
msbuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /verbosity:minimal
```
`0 Error(s)` 확인. 신규 warning 0 목표.
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /verbosity:minimal</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug|x64 빌드가 `0 Error(s)` 로 종료한다 (exit 0)
    - 본 플랜 추가 코드로 인한 신규 warning 0
  </acceptance_criteria>
  <done>Debug|x64 빌드가 0 errors 로 통과한다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: SIMUL 일괄 검사 + Export 육안 검증</name>
  <what-built>
    트리 SHOT 체크박스 다중 선택 + "일괄검사" 버튼(선택 SHOT 만 1사이클 실행 + 누적) + "일괄Export" 버튼(누적분 단일 xlsx, Phase 40 포맷 재사용). 동일 시퀀스 검증(Top끼리/Bottom끼리), SaveAsync 중복 저장 방지(51-01).
  </what-built>
  <how-to-verify>
1. 앱 실행(SIMUL_MODE, Ctrl+F5). 레시피 로드 후 InspectionListView 트리에서 한 시퀀스(예: Top)의 SHOT 노드 2개 이상에 체크박스 표시를 확인 → 체크.
2. "일괄검사" 버튼 클릭 → 체크한 SHOT 들이 각자 SimulImagePath 로 1회씩 실행되고 측정 결과가 트리/뷰어에 반영되는지 확인 (D-03). 검사 완료 후 "일괄Export" 버튼이 활성화되는지 확인.
3. (D-02 검증) Top SHOT 1개 + Bottom SHOT 1개를 동시 체크 후 "일괄검사" → "한 시퀀스 내의 SHOT 만…" 오류 모달이 뜨고 실행되지 않는지 확인.
4. "일괄Export" 버튼 클릭 → SaveFileDialog 에서 xlsx 저장 → 생성된 파일을 열어 선택 SHOT 들의 전 FAI/측정값이 SHOT 이름 컬럼으로 구분되어 한 시트에 행으로 들어갔는지 확인 (D-06 Phase 40 포맷).
5. (회귀) 단일 SHOT 노드 선택 후 기존 "RUN"(btn_start) 버튼 → 단일 검사가 그대로 동작하는지 확인 (회귀 0).
6. (중복 저장 확인) ResultSavePath 의 cycle 저장 폴더에 동일 사이클이 1건만 생성되는지 확인 (51-01 SaveAsync 미호출).
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 발견된 이슈 설명</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 → 파일 시스템 (Export) | SaveFileDialog 로 사용자가 지정한 xlsx 경로 — 로컬 신뢰 사용자, 외부 입력 아님 |
| 사용자 → UI (SHOT 선택) | 체크박스 인메모리 상태 — 외부 네트워크 입력 없음 |

로컬 WPF 데스크탑 앱. 네트워크/인증/외부 직렬화 입력 없음. xlsx write 는 기존 RepeatExcelExportService(ClosedXML, Phase 40 검증됨) 재사용 — 신규 위협 표면 없음.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-04 | Tampering (경로) | Btn_batchExport_Click SaveFileDialog | accept | SaveFileDialog 는 OS 표준 다이얼로그로 경로를 사용자가 선택; .xlsx 필터 적용; 로컬 신뢰 사용자 단독 사용 — 인젝션/권한 상승 표면 없음 |
| T-51-05 | Denial of Service (재진입) | Btn_batchRun_Click | mitigate | `Sequences.IsIdle` + `_batchService.IsRunning` 가드로 동시 실행 차단 |
| T-51-06 | Information disclosure | xlsx export 내용 | accept | 측정 결과는 비민감 산업 데이터(거리/판정); 로컬 디스크 저장; 신규 노출 경로 없음 |
</threat_model>

<verification>
- `grep "Btn_batchRun_Click\|Btn_batchExport_Click" InspectionListView.xaml.cs` → 각 1건
- `grep "btn_batchRun\|btn_batchExport" InspectionListView.xaml` → 존재
- `grep "public bool IsChecked" NodeViewModel.cs` → 1건
- msbuild Debug|x64 → 0 errors
- 육안 UAT (Task 5) PASS
</verification>

<success_criteria>
- SHOT 다중 선택(체크박스) → 동일 시퀀스 일괄 검사 → 누적 → 단일 xlsx Export 동작
- Top+Bottom 교차 선택 차단(D-02), 단일 RUN 회귀 0, 중복 저장 0
- Debug|x64 빌드 0 errors, C# 7.2 호환
</success_criteria>

<output>
After completion, create `.planning/phases/51-export-2026-06-16-poc-3/51-02-SUMMARY.md`.
SUMMARY 에 기록: (1) 다중 선택 = IsChecked 체크박스 방식 채택 사유(TreeListBox Extended 불확실), (2) Export = RepeatExcelExportService.Export 재사용(D-06/D-08, 신규 Export 코드 0), (3) _batchAccumulated 누적 유지 정책(명시 초기화 버튼 없음 — 누적 우선, 향후 41.1 재사용 가능), (4) UAT 결과.
</output>
