---
phase: 52-datum-2026-06-16-poc-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs
autonomous: true
requirements: [LEVEL-01]

must_haves:
  truths:
    - "시퀀스마다 레벨링 on/off 토글이 레시피(INI)에 저장되고 기본값은 off 다"
    - "기존 레시피(INI 키 없음)를 로드하면 레벨링이 off 로 폴백되어 회귀가 0 이다"
    - "Datum 1개를 '레벨링 기준'으로 지정하는 플래그가 PropertyGrid 에 노출되고 INI 에 저장된다"
    - "CameraRole 전환 시 비활성 시퀀스의 레벨링 키가 소실되지 않는다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "LevelingEnabled bool 멤버 + _levelingAngleRad 캐시 + LevelingComputed 플래그"
      contains: "public bool LevelingEnabled"
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "IsLevelingReference per-datum bool 플래그"
      contains: "IsLevelingReference"
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs"
      provides: "FIXTURE 섹션 LevelingEnabled INI save/load"
      contains: "LevelingEnabled"
  key_links:
    - from: "InspectionRecipeManager.SaveFixtureForSequence"
      to: "InspectionSequence.LevelingEnabled"
      via: "saveFile[sectionPrefix][\"LevelingEnabled\"]"
      pattern: "LevelingEnabled"
    - from: "InspectionRecipeManager.LoadFixtureForSequence"
      to: "InspectionSequence.LevelingEnabled"
      via: ".ToBool() 미존재 폴백 false"
      pattern: "LevelingEnabled.*ToBool"
---

<objective>
시퀀스 단위 레벨링 토글(D-04)과 기준 Datum 지정 플래그(D-01)를 위한 데이터 모델 + INI 영속 토대를 구축한다.

Purpose: 회전 각도 산출(Plan 02)과 전처리 회전 삽입(Plan 03)이 소비할 설정/캐시 멤버를 먼저 정의한다 (interface-first). 기본 off + INI 미존재 폴백 off 로 기존 레시피 회귀를 0 으로 보장한다.
Output: InspectionSequence 레벨링 멤버 3종, DatumConfig.IsLevelingReference 플래그, FIXTURE 섹션 INI save/load 2키.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-datum-2026-06-16-poc-1/52-CONTEXT.md
@.planning/phases/52-datum-2026-06-16-poc-1/52-PATTERNS.md

<interfaces>
<!-- 실제 코드베이스에서 추출한 계약. 실행자는 codebase 탐색 없이 아래 시그니처를 직접 사용한다. -->

InspectionSequence.cs (수정 대상, 기존 멤버):
```csharp
// L46 — DisplayName 와 같은 자리에 레벨링 멤버 추가
public string DisplayName { get; set; } = "";
// L50
public List<DatumConfig> DatumConfigs { get; private set; } = new List<DatumConfig>();
// L250
public string GetDisplayName() { ... }
```

DatumConfig.cs (수정 대상, ParamBase 상속 — bool 자동 INI 직렬화):
```csharp
// L36-38 선례 (string 필드 INI 자동 직렬화)
[Category("Datum|ImageSource")]
public string TeachingImagePath { get; set; } = "";
// MeasurementBase.InvertSign 선례 (bool 기본 false, 회귀 0)
[Category("Measurement|Tolerance")]
public bool InvertSign { get; set; } = false;
```

InspectionRecipeManager.cs (수정 대상, FIXTURE 섹션 수동 키 직렬화):
```csharp
// SaveFixtureForSequence (L92-95)
string displayName = seq.GetDisplayName();
saveFile[sectionPrefix]["DisplayName"] = displayName;
saveFile[sectionPrefix]["DatumCount"] = seq.DatumConfigs.Count;

// LoadFixtureForSequence (L131-135)
string displayName = loadFile[sectionPrefix]["DisplayName"].ToString();
seq.DisplayName = displayName;
int datumCount = loadFile[sectionPrefix]["DatumCount"].ToInt();
// IniFile[..][..].ToBool() = 키 미존재 시 false 폴백 (DatumCount .ToInt() = 0 폴백과 동일 메커니즘)

// PreserveFixtureFromExisting (L105-121) — 비활성 시퀀스 섹션 통째 복사
// L112: saveFile[sectionPrefix] = existingFile[sectionPrefix]; ← 신규 키도 섹션 단위 복사라 자동 보존됨
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: InspectionSequence 레벨링 멤버 + DatumConfig 기준 플래그</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (L40-60 멤버 영역, L250 GetDisplayName, L338 ClearDatumTransforms)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (L1-50 헤더 + 기존 string 필드 패턴)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (InvertSign bool 선례 — Category/Description/= false 3종 세트)
  </read_first>
  <behavior>
    - InspectionSequence 에 public bool LevelingEnabled 멤버가 존재하고 기본값 false (grep "public bool LevelingEnabled" → 1건, "= false" 포함)
    - InspectionSequence 에 런타임 캐시 필드 private double _levelingAngleRad = 0.0 + private bool _levelingComputed = false + public ResetLeveling() 메서드가 존재 (D-03 시퀀스당 1회 산출 캐시)
    - DatumConfig 에 public bool IsLevelingReference 멤버가 존재하고 기본값 false (회귀 0, ParamBase bool 자동 직렬화)
  </behavior>
  <action>
InspectionSequence.cs L46 (DisplayName 선언 바로 아래) 에 D-04 시퀀스 레벨링 토글 멤버 추가:
```csharp
//260617 hbk Phase 52 LEVEL-01 시퀀스 단위 레벨링 토글 (D-04, 기본 off → INI 미존재 폴백 off 회귀 0)
public bool LevelingEnabled { get; set; } = false;
```

같은 멤버 영역(L53 `_datumTransforms` 선언 근처)에 D-03 "시퀀스당 1회 산출 → 전 SHOT 공유" 캐시 필드 추가:
```csharp
//260617 hbk Phase 52 LEVEL-01 레벨링 각도 캐시 (D-03 시퀀스당 1회 산출, 전 SHOT 공유)
private double _levelingAngleRad = 0.0;
private bool _levelingComputed = false;
public double LevelingAngleRad { get { return _levelingAngleRad; } }
public bool LevelingComputed { get { return _levelingComputed; } }
public void SetLevelingAngle(double angleRad) { _levelingAngleRad = angleRad; _levelingComputed = true; }
public void ResetLeveling() { _levelingAngleRad = 0.0; _levelingComputed = false; }
```

ClearDatumTransforms() (L338-346) 본문 끝에 ResetLeveling() 호출 추가 — datum transform 과 동일 lifecycle 로 사이클마다 레벨링 캐시 리셋:
```csharp
//260617 hbk Phase 52 레벨링 캐시도 datum transform 과 동일 lifecycle 리셋
ResetLeveling();
```
주의: ResetLeveling() 은 _datumTransforms.Clear() 와 같은 사이클 리셋이나, D-03 "시퀀스당 1회 산출 캐시" 는 Plan 03 에서 시퀀스 검사 시작 시점에 1회 산출하도록 사용. 여기서는 멤버 정의 + 리셋만 제공.

DatumConfig.cs L38 (TeachingImagePath 선언 아래, ImageSource 카테고리 인근) 에 D-01 per-datum 기준 플래그 추가:
```csharp
//260617 hbk Phase 52 LEVEL-01 레벨링 기준 Datum 지정 (D-01). 켜면 이 Datum 의 수평 에지로 이미지를 회전 정렬. 시퀀스당 1개만 지정 권장.
[Category("Datum|Leveling")]
[System.ComponentModel.Description("레벨링 기준. 켜면 이 Datum 의 수평 2-ROI 에지로 이미지 회전 정렬한다. 시퀀스당 1개만 지정.")]
public bool IsLevelingReference { get; set; } = false;
```
ParamBase reflection 이 Boolean case 를 자동 직렬화하므로 Save/Load 코드 추가 0. CLAUDE.md C# 7.2 / 파일 실제 brace 스타일 따름.
  </action>
  <verify>
    <automated>grep -n "public bool LevelingEnabled" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs && grep -n "IsLevelingReference" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs && grep -n "ResetLeveling\|_levelingComputed" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</automated>
  </verify>
  <acceptance_criteria>
    - grep "public bool LevelingEnabled" InspectionSequence.cs → 1건 매치 + 같은 줄/인근 "= false"
    - grep "_levelingAngleRad\|_levelingComputed\|SetLevelingAngle\|ResetLeveling" InspectionSequence.cs → 각 1건 이상
    - grep "public bool IsLevelingReference" DatumConfig.cs → 1건 + "= false"
    - ClearDatumTransforms 본문에 ResetLeveling() 호출 존재
    - 모든 신규 라인에 //260617 hbk Phase 52 주석 포함
  </acceptance_criteria>
  <done>InspectionSequence 에 LevelingEnabled + 레벨링 각도 캐시 멤버, DatumConfig 에 IsLevelingReference 가 추가되고 기본값 false. ClearDatumTransforms 가 레벨링 캐시도 리셋.</done>
</task>

<task type="auto">
  <name>Task 2: FIXTURE 섹션 LevelingEnabled INI save/load + CameraRole 보존 검증</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs (L85-142 Save/Load/Preserve FixtureForSequence)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (Task 1 에서 추가한 LevelingEnabled — 같은 파일)
  </read_first>
  <action>
InspectionRecipeManager.cs SaveFixtureForSequence (L92-95, DisplayName/DatumCount 직렬화 바로 뒤) 에 D-04 시퀀스 레벨링 토글 저장 추가:
```csharp
//260617 hbk Phase 52 LEVEL-01 시퀀스 레벨링 토글 저장 (D-04). DisplayName 과 같은 FIXTURE 섹션.
saveFile[sectionPrefix]["LevelingEnabled"] = seq.LevelingEnabled ? 1 : 0;
```

LoadFixtureForSequence (L131-135, DisplayName/DatumCount 로드 인근) 에 로드 추가 — 키 미존재 시 .ToBool() = false 폴백 (회귀 0):
```csharp
//260617 hbk Phase 52 LEVEL-01 시퀀스 레벨링 토글 로드 (D-04). 키 미존재 → ToBool() false 폴백 = 기존 레시피 회귀 0.
seq.LevelingEnabled = loadFile[sectionPrefix]["LevelingEnabled"].ToBool();
```

PreserveFixtureFromExisting (L105-121) 는 수정 불필요 — L112 `saveFile[sectionPrefix] = existingFile[sectionPrefix]` 가 섹션 통째 복사라 LevelingEnabled 키도 자동 보존. 단, L108-109 의 신규 레시피 빈값 분기에는 명시 추가:
```csharp
//260617 hbk Phase 52 신규 레시피 보존 분기 — LevelingEnabled 기본 off 명시
saveFile[sectionPrefix]["LevelingEnabled"] = 0;
```
(L108 `saveFile[sectionPrefix]["DisplayName"] = ""` 직후, return 전)

회귀 주의 (MEMORY project_recipe_datum_loss_camerarole): IsLevelingReference 는 DatumConfig 내부 ParamBase 직렬화로 _DATUM_{d} 서브섹션에 저장되며, PreserveFixtureFromExisting L115-119 의 datumSection 통째 복사 경로로 보존됨 — 추가 코드 0. 단, 이 보존 경로가 datum 서브섹션을 복사하는지 코드로 확인하고 SUMMARY 에 명시.

CLAUDE.md: 파일 실제 brace 스타일(K&R) 따름. IniFile .ToBool() API 가 존재하는지 확인(DatumConfig 등에서 .ToBool 사용 선례 grep). 없으면 `.ToInt() == 1` 대체.
  </action>
  <verify>
    <automated>grep -n "LevelingEnabled" WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs</automated>
  </verify>
  <acceptance_criteria>
    - grep "LevelingEnabled" InspectionRecipeManager.cs → 3건 이상 (Save 1 + Load 1 + Preserve 빈값분기 1)
    - Save 라인이 `seq.LevelingEnabled ? 1 : 0` 형태 (또는 bool 직접)
    - Load 라인이 `.ToBool()` 또는 `.ToInt() == 1` 폴백 형태
    - msbuild Debug/x64 Rebuild 시 신규 컴파일 에러 0 (IniFile API 호환 확인)
    - 모든 신규 라인에 //260617 hbk Phase 52 주석
  </acceptance_criteria>
  <done>FIXTURE 섹션에 LevelingEnabled 가 save/load 되고, 기존 INI 키 미존재 시 false 폴백. CameraRole 전환 시 비활성 시퀀스 레벨링 키 보존 경로 SUMMARY 에 확인 기록.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recipe INI 파일 → InspectionRecipeManager.Load | 사용자/외부 편집 가능한 INI 텍스트가 시퀀스 설정으로 역직렬화 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | InspectionRecipeManager.LoadFixtureForSequence | mitigate | INI 키 미존재/malformed → `.ToBool()` false 폴백, 예외 없이 안전한 off 상태로 로드 (회귀 0, 비활성 안전) |
| T-52-02 | Denial of Service | CameraRole 전환 시 PreserveFixtureFromExisting | accept | 오프라인 단일 사용자 데스크톱 앱, 네트워크 공격면 없음. 섹션 통째 복사로 데이터 손실 회귀만 코드로 검증 |
</threat_model>

<verification>
- msbuild Debug/x64 Rebuild → 신규 에러/경고 0
- grep 으로 LevelingEnabled (3 파일) + IsLevelingReference (DatumConfig) 존재 확인
- 기존 레시피 INI 로드 시 LevelingEnabled = false 폴백 (코드 경로 검증)
</verification>

<success_criteria>
- InspectionSequence.LevelingEnabled + 레벨링 각도 캐시 멤버 + DatumConfig.IsLevelingReference 추가, 모두 기본 false
- FIXTURE 섹션 INI save/load 동작, 키 미존재 폴백 off
- msbuild Debug/x64 PASS, 회귀 0
</success_criteria>

<output>
After completion, create `.planning/phases/52-datum-2026-06-16-poc-1/52-01-SUMMARY.md`
</output>
