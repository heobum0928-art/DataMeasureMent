---
phase: 52-datum-2026-06-16-poc-1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
  - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
autonomous: true
requirements: [LEVEL-01]

must_haves:
  truths:
    - "기준 Datum 의 수평 2-ROI concat 피팅 라인에서 수평선 대비 각도(radian)를 산출한다"
    - "임의 각도로 입력 HImage 를 회전한 새 HImage 를 반환한다 (90 배수 제약 없음)"
    - "각도 산출/회전 실패 시 throw 하지 않고 false + 무회전(원본/identity) 폴백한다"
  artifacts:
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "TryGetLevelingAngle — 수평 2-ROI 피팅 각도 산출 (angle_lx 등가 Math.Atan2)"
      contains: "TryGetLevelingAngle"
    - path: "WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs"
      provides: "RotateImageByAngle — affine_trans_image 임의각 회전 유틸"
      contains: "RotateImageByAngle"
  key_links:
    - from: "TryGetLevelingAngle"
      to: "FitLineContourXld + Math.Atan2"
      via: "수평 2-ROI concat 피팅 후 angleRad = Math.Atan2(hrE-hrB, hcE-hcB)"
      pattern: "Math\\.Atan2"
    - from: "RotateImageByAngle"
      to: "HOperatorSet.AffineTransImage"
      via: "hom_mat2d_rotate(angle, centerRow, centerCol) → affine_trans_image"
      pattern: "AffineTransImage"
---

<objective>
레벨링 각도 산출(DatumFindingService.TryGetLevelingAngle)과 임의각 이미지 회전 유틸(VisionAlgorithmService.RotateImageByAngle)을 추가한다.

Purpose: D-01 "기존 수평 피팅 재사용·중복 구현 금지" 를 충족 — 각도는 100% 기존 자산(FitLineContourXld + Math.Atan2 curAngle) 재사용. 회전만 신규(affine_trans_image, HImage.RotateImage 는 90 배수 전용이라 부적합). Plan 03 통합이 이 두 메서드를 호출한다.
Output: TryGetLevelingAngle 1메서드, RotateImageByAngle 1유틸.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-datum-2026-06-16-poc-1/52-CONTEXT.md
@.planning/phases/52-datum-2026-06-16-poc-1/52-PATTERNS.md

<interfaces>
<!-- 코드베이스에서 추출한 정확한 시그니처. 실행자는 이 계약을 직접 사용한다. -->

DatumFindingService.cs (수정 대상 — 수평 2-ROI 피팅 전 구간 재사용):
```csharp
// L407 — 재사용 기준 메서드 (수평 A/B concat 피팅 포함)
private bool TryFindVerticalTwoHorizontal(HImage image, DatumConfig config, out HTuple transform, out string error)

// L441-470 — Horizontal A/B 에지점 추출 (그대로 재사용)
private bool TryExtractEdgePoints(
    HImage image, HTuple imageWidth, HTuple imageHeight,
    double roiRow, double roiCol, double roiPhi, double roiLength1, double roiLength2,
    double sigma, int threshold, string polarity,
    string direction, string selection,
    int sampleCount, int trimCount,
    out HTuple rowEdge, out HTuple colEdge,
    out string error, string roiLabel)

// L480-516 — concat → 피팅 → 각도 산출 (핵심 재사용 블록)
HTuple allRows = rowEdgeA.TupleConcat(rowEdgeB);
HTuple allCols = colEdgeA.TupleConcat(colEdgeB);
HOperatorSet.GenContourPolygonXld(out contour, allRows, allCols);
HTuple hrB, hcB, hrE, hcE, nr, nc, df;
HOperatorSet.FitLineContourXld(contour, "tukey", -1, 0, 5, 2,
    out hrB, out hcB, out hrE, out hcE, out nr, out nc, out df);
double curAngle = Math.Atan2(hrE.D - hrB.D, hcE.D - hcB.D);  // ← angle_lx 등가. 레벨링 각도 정답 소스.

// MIN_HORIZONTAL_EDGES = 10 (const, 에지 부족 가드)
// 에러/dispose 패턴 (L571-580):
//   catch (Exception ex) { error = ex.Message; ...; return false; }
//   finally { if (contour != null) { try { contour.Dispose(); } catch { } } }
```

VisionAlgorithmService.cs (수정 대상 — static 유틸 템플릿):
```csharp
// L1136 — 점-변환 유틸 (구조 템플릿: identity 가드 + try/catch swallow)
public static void AffineTransformPoint(HTuple homMat2D, double row, double col,
    out double transRow, out double transCol)
{
    transRow = row; transCol = col;
    if (homMat2D == null || homMat2D.Length == 0) return;
    try { HOperatorSet.AffineTransPoint2d(homMat2D, row, col, out HTuple tr, out HTuple tc); ... }
    catch { }
}
// ← 이 메서드 바로 위/아래에 RotateImageByAngle 추가
```

HALCON operator 참고 (HImage.RotateImage 는 90 배수 전용 — 임의각 불가, affine 채택 근거):
- HOperatorSet.HomMat2dIdentity(out HTuple mat)
- HOperatorSet.HomMat2dRotate(HTuple mat, HTuple phi, HTuple row, HTuple col, out HTuple matRotated)  // 회전중심 지정
- HOperatorSet.AffineTransImage(HObject image, out HObject imageTrans, HTuple homMat2D, HTuple interpolation, HTuple adaptImageSize)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DatumFindingService.TryGetLevelingAngle — 수평 피팅 각도 산출</name>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (L407-581 TryFindVerticalTwoHorizontal 전체 — 수평 A/B 피팅 + curAngle 산출, L1728 TryExtractEdgePoints 시그니처)
    - .planning/phases/52-datum-2026-06-16-poc-1/52-PATTERNS.md (DatumFindingService 섹션 — angle_lx 등가 = Math.Atan2 결론)
  </read_first>
  <behavior>
    - TryGetLevelingAngle(image, config, out angleRad, out error) 가 config 의 Horizontal_A / Horizontal_B 2-ROI 에지점을 추출 → concat → FitLineContourXld → angleRad = Math.Atan2(hrE-hrB, hcE-hcB) 반환하고 true
    - 에지점 합산이 MIN_HORIZONTAL_EDGES(10) 미만이면 false + error "insufficient edges", angleRad = 0.0
    - 예외 발생 시 throw 안 함 — catch → false + error = ex.Message + angleRad = 0.0 (CLAUDE.md "Never throw")
    - contour XLD 는 finally 에서 dispose
  </behavior>
  <action>
DatumFindingService.cs 의 TryFindVerticalTwoHorizontal (L581) 직후에 신규 public 메서드 추가. 본문은 TryFindVerticalTwoHorizontal 의 **수평 A/B 피팅 구간만** 떼어내 각도 반환 (Vertical 검출 / IntersectionLl / ValidateHorizontalVerticalAngles / hom_mat2d 빌드 / DetectedOrigin transient 는 레벨링에 불필요 — 제외):

```csharp
//260617 hbk Phase 52 LEVEL-01 레벨링 각도 산출 (D-01). 기준 Datum 의 수평 2-ROI concat 피팅 라인의
//  수평선(0°) 대비 각도(radian). TryFindVerticalTwoHorizontal 의 수평 피팅 구간만 재사용 — 중복 구현 금지.
//  angle_lx 등가 = Math.Atan2 (코드베이스 관용, PATTERNS.md 확인). 회전각 부호 규약은 호출부(Plan 03)에서 -angleRad 확정.
public bool TryGetLevelingAngle(HImage image, DatumConfig config, out double angleRad, out string error)
{
    angleRad = 0.0;
    error = null;
    HObject contour = null;
    try
    {
        HTuple imageWidth, imageHeight;
        image.GetImageSize(out imageWidth, out imageHeight);

        // Horizontal A 에지점 추출 (TryFindVerticalTwoHorizontal L441-453 와 동일 인자)
        HTuple rowEdgeA, colEdgeA;
        string edgeErrorA;
        if (!TryExtractEdgePoints(
                image, imageWidth, imageHeight,
                config.Horizontal_A_Row, config.Horizontal_A_Col, config.Horizontal_A_Phi,
                config.Horizontal_A_Length1, config.Horizontal_A_Length2,
                config.Horizontal_A_Sigma, config.Horizontal_A_EdgeThreshold, config.Horizontal_A_EdgePolarity,
                config.Horizontal_A_EdgeDirection, config.Horizontal_A_EdgeSelection,
                config.Horizontal_A_EdgeSampleCount, config.Horizontal_A_EdgeTrimCount,
                out rowEdgeA, out colEdgeA, out edgeErrorA, "Horizontal_A"))
        {
            error = "Horizontal_A: " + edgeErrorA;
            return false;
        }

        // Horizontal B 에지점 추출
        HTuple rowEdgeB, colEdgeB;
        string edgeErrorB;
        if (!TryExtractEdgePoints(
                image, imageWidth, imageHeight,
                config.Horizontal_B_Row, config.Horizontal_B_Col, config.Horizontal_B_Phi,
                config.Horizontal_B_Length1, config.Horizontal_B_Length2,
                config.Horizontal_B_Sigma, config.Horizontal_B_EdgeThreshold, config.Horizontal_B_EdgePolarity,
                config.Horizontal_B_EdgeDirection, config.Horizontal_B_EdgeSelection,
                config.Horizontal_B_EdgeSampleCount, config.Horizontal_B_EdgeTrimCount,
                out rowEdgeB, out colEdgeB, out edgeErrorB, "Horizontal_B"))
        {
            error = "Horizontal_B: " + edgeErrorB;
            return false;
        }

        int totalEdges = rowEdgeA.TupleLength() + rowEdgeB.TupleLength();
        if (totalEdges < MIN_HORIZONTAL_EDGES)
        {
            error = "Leveling line fit failed: insufficient edges (" + totalEdges + ")";
            return false;
        }

        // A+B concat → 라인 fit (TryFindVerticalTwoHorizontal L480-489 와 동일)
        HTuple allRows = rowEdgeA.TupleConcat(rowEdgeB);
        HTuple allCols = colEdgeA.TupleConcat(colEdgeB);
        HOperatorSet.GenContourPolygonXld(out contour, allRows, allCols);

        HTuple hrB, hcB, hrE, hcE, nr, nc, df;
        HOperatorSet.FitLineContourXld(
            contour, "tukey", -1, 0, 5, 2,
            out hrB, out hcB, out hrE, out hcE, out nr, out nc, out df);

        // 수평선(0°) 대비 라인 각도. HDevelop angle_lx 와 동일 의미 (L516 curAngle 패턴).
        angleRad = Math.Atan2(hrE.D - hrB.D, hcE.D - hcB.D);
        return true;
    }
    catch (Exception ex)
    {
        error = ex.Message;
        angleRad = 0.0;
        return false;
    }
    finally
    {
        if (contour != null) { try { contour.Dispose(); } catch { } }
    }
}
```
config 의 Horizontal_A_* / Horizontal_B_* 프로퍼티명이 정확한지 TryFindVerticalTwoHorizontal L441-466 에서 확인 후 그대로 복사. CLAUDE.md: DatumFindingService 는 Allman 스타일 — 위 코드 그대로. C# 7.2 준수 (out var 인라인 허용되나 기존 파일 패턴인 명시 선언 따름).
  </action>
  <verify>
    <automated>grep -n "public bool TryGetLevelingAngle" WPF_Example/Halcon/Algorithms/DatumFindingService.cs && grep -n "Math.Atan2(hrE.D - hrB.D, hcE.D - hcB.D)" WPF_Example/Halcon/Algorithms/DatumFindingService.cs</automated>
  </verify>
  <acceptance_criteria>
    - grep "public bool TryGetLevelingAngle(HImage image, DatumConfig config, out double angleRad, out string error)" → 1건
    - grep "Math.Atan2(hrE.D - hrB.D, hcE.D - hcB.D)" → 1건 이상 (재사용된 각도 산출)
    - grep "MIN_HORIZONTAL_EDGES" → 메서드 내 1건 (에지 부족 가드)
    - finally 블록에 contour.Dispose() 존재
    - 메서드 본문에 IntersectionLl / hom_mat2d / DetectedOrigin 코드 없음 (각도만 산출 — 불필요 구간 제외)
    - msbuild Debug/x64 신규 에러 0
    - //260617 hbk Phase 52 주석 포함
  </acceptance_criteria>
  <done>TryGetLevelingAngle 가 수평 2-ROI 피팅으로 angleRad 를 산출, 에지 부족/예외 시 false + 0.0 폴백, contour dispose.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VisionAlgorithmService.RotateImageByAngle — 임의각 회전 유틸</name>
  <read_first>
    - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs (L1133-1152 AffineTransformPoint — static 유틸 + identity 가드 + try/catch 템플릿)
    - .planning/phases/52-datum-2026-06-16-poc-1/52-PATTERNS.md (VisionAlgorithmService 섹션 — affine_trans_image 채택 근거, adapt_image_size 경계처리)
  </read_first>
  <behavior>
    - RotateImageByAngle(src, angleRad) 가 src 의 중심을 회전중심으로 angleRad 만큼 회전한 새 HImage 를 반환 (90 배수 제약 없음, affine_trans_image 사용)
    - angleRad 가 0 에 가까우면(|angleRad| < 1e-6) 무회전 — src.CopyImage() 반환 (불필요 보간 방지)
    - 예외 발생 시 throw 안 함 — catch → src.CopyImage() 폴백 반환 (무회전, 호출부 측정 계속)
    - 반환된 HImage 는 src 와 독립 객체 (호출부가 dispose 책임)
  </behavior>
  <action>
VisionAlgorithmService.cs 의 AffineTransformPoint (L1136-1151) 바로 아래(L1151 닫는 중괄호 뒤, 클래스 닫기 L1152 전) 에 신규 static 유틸 추가:

```csharp
//260617 hbk Phase 52 LEVEL-01 임의각 이미지 회전 (D-02 grab 직후 전처리 실제 회전).
//  HImage.RotateImage 는 90 배수 전용이라 레벨링 부적합 → affine_trans_image + hom_mat2d_rotate(회전중심=이미지 중심).
//  경계처리: adapt_image_size='false' (고정 크기) — taught 측정 ROI 좌표 정합 보존. 잘림 영역은 배경.
//  실패/근사0 → 원본 복사 폴백 (무회전, 측정 계속, AffineTransformPoint identity 폴백과 동일 정신).
public static HImage RotateImageByAngle(HImage src, double angleRad)
{
    if (src == null) return null;
    // 근사 0 회전 → 보간 생략, 원본 복사
    if (System.Math.Abs(angleRad) < 1e-6) return src.CopyImage();
    try
    {
        HTuple width, height;
        src.GetImageSize(out width, out height);
        double centerRow = (height.D - 1.0) / 2.0;
        double centerCol = (width.D - 1.0) / 2.0;

        HTuple mat;
        HOperatorSet.HomMat2dIdentity(out mat);
        HOperatorSet.HomMat2dRotate(mat, angleRad, centerRow, centerCol, out mat);

        HObject rotatedObj;
        HOperatorSet.AffineTransImage(src, out rotatedObj, mat, "constant", "false");
        // HObject → HImage 변환 (기존 코드베이스 변환 선례 확인 후 적용)
        HImage rotated = new HImage(rotatedObj);
        rotatedObj.Dispose();
        return rotated;
    }
    catch
    {
        // 무회전 폴백 (throw 금지)
        return src.CopyImage();
    }
}
```

주의 1 — HObject ↔ HImage 변환: `new HImage(rotatedObj)` 가 HalconDotNet 에서 동작하는지 확인. 동작 안 하면 대안:
  (a) `HImage rotated = src.AffineTransImage(mat, "constant", "false");` — HImage 인스턴스 메서드 오버로드 존재 시 (HImage.RotateImage 처럼 인스턴스 메서드 가능). HikCamera.cs L460 `HImage.RotateImage` 선례 참고. 이 경로가 더 깔끔하면 채택.
  (b) HOperatorSet.AffineTransImage 의 out 이 HObject 이면 `rotatedObj.HObjectToHImage()` 또는 캐스팅 헬퍼 확인.
실행자는 HImage 인스턴스 메서드 `src.AffineTransImage(mat, "constant", "false")` 를 1순위로 시도(가장 단순, dispose 단순) → 컴파일 안 되면 HObject 경로.

주의 2 — angleRad 부호: 여기서는 입력 angleRad 그대로 회전. 회전 방향(레벨링 = 기울기 상쇄) 부호 확정은 Plan 03 호출부에서 `-levelingAngle` 전달로 처리. 이 유틸은 부호 중립.

CLAUDE.md: VisionAlgorithmService 파일 실제 스타일(Allman) 따름. C# 7.2.
  </action>
  <verify>
    <automated>grep -n "public static HImage RotateImageByAngle" WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs && grep -n "HomMat2dRotate\|AffineTransImage" WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs</automated>
  </verify>
  <acceptance_criteria>
    - grep "public static HImage RotateImageByAngle(HImage src, double angleRad)" → 1건
    - grep "HomMat2dRotate" 메서드 내 1건 (회전중심 지정)
    - grep "AffineTransImage" 메서드 내 1건 (인스턴스 또는 HOperatorSet 형태)
    - 근사 0 분기 `System.Math.Abs(angleRad) < 1e-6` 존재 → src.CopyImage() 반환
    - catch 블록이 src.CopyImage() 폴백 반환 (throw 없음)
    - msbuild Debug/x64 PASS — HObject/HImage 변환 컴파일 성공 (실행자가 동작 경로 확정)
    - //260617 hbk Phase 52 주석 포함
  </acceptance_criteria>
  <done>RotateImageByAngle 가 affine_trans_image 로 임의각 회전 HImage 반환, 근사0/예외 시 원본 복사 폴백. 컴파일 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recipe DatumConfig 각도 파라미터 → HALCON 연산 | 레시피에서 온 ROI/threshold 값이 에지 검출/피팅 입력 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-03 | Tampering | TryGetLevelingAngle (malformed 각도/NaN) | mitigate | 에지 부족 가드(MIN_HORIZONTAL_EDGES) + try/catch → false + angleRad 0.0 폴백. 회전 유틸은 근사0/예외 시 원본 복사 (무회전) — corrupt 각도가 측정 파괴 못함 |
| T-52-04 | Denial of Service | RotateImageByAngle (대용량 152MP 이미지 보간) | accept | 단일 사용자 오프라인 앱, 시퀀스당 1회 회전 (D-03). 성능 부담은 측정 throughput 범위 내 |
</threat_model>

<verification>
- msbuild Debug/x64 Rebuild → 신규 에러 0
- grep TryGetLevelingAngle + RotateImageByAngle 존재
- 각도 산출 = Math.Atan2 재사용 (신규 angle_lx 호출 0건)
</verification>

<success_criteria>
- TryGetLevelingAngle 가 수평 2-ROI 피팅으로 angleRad 산출 (D-01 기존 자산 재사용)
- RotateImageByAngle 가 임의각 회전 HImage 반환 (affine_trans_image, 90 배수 제약 없음)
- 둘 다 실패 시 무회전/false 폴백, throw 0
- msbuild PASS
</success_criteria>

<output>
After completion, create `.planning/phases/52-datum-2026-06-16-poc-1/52-02-SUMMARY.md`
</output>
