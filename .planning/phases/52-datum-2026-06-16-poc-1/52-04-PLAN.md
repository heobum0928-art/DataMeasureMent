---
phase: 52-datum-2026-06-16-poc-1
plan: 04
type: execute
wave: 3
depends_on: [52-01, 52-02, 52-03]
files_modified:
  - .planning/phases/52-datum-2026-06-16-poc-1/52-UAT.md
autonomous: false
requirements: [LEVEL-01]

must_haves:
  truths:
    - "기울어진 입력 이미지가 LevelingEnabled=on 시 수평 정렬된 뒤 측정된다 (육안 확인)"
    - "LevelingEnabled=off 또는 기존 레시피에서 회귀가 0 이다 (측정값 동일)"
    - "회전 방향 부호(-angle)가 올바르다 (기울기 상쇄 방향)"
  artifacts:
    - path: ".planning/phases/52-datum-2026-06-16-poc-1/52-UAT.md"
      provides: "SIMUL UAT 결과 + sign-off"
      contains: "sign"
  key_links:
    - from: "UAT"
      to: "LEVEL-01"
      via: "ROADMAP Success Criteria 충족 확인"
      pattern: "LEVEL-01"
---

<objective>
SIMUL 모드에서 이미지 수평 보정(레벨링) 전 경로를 사용자 육안 검증하고 Phase 52 를 sign-off 한다.

Purpose: 회전 정렬이 실제로 동작하는지(부호/방향), off 회귀 0, 측정값 변화를 사용자가 시각 확인. ROADMAP Success Criteria("기울어진 입력 이미지가 수평 정렬된 후 측정 / 기존 측정 회귀 0") 충족 확인.
Output: 52-UAT.md sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-datum-2026-06-16-poc-1/52-CONTEXT.md
@.planning/phases/52-datum-2026-06-16-poc-1/52-01-SUMMARY.md
@.planning/phases/52-datum-2026-06-16-poc-1/52-02-SUMMARY.md
@.planning/phases/52-datum-2026-06-16-poc-1/52-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 빌드 검증 + UAT 문서 골격 작성</name>
  <read_first>
    - .planning/phases/52-datum-2026-06-16-poc-1/52-01-SUMMARY.md
    - .planning/phases/52-datum-2026-06-16-poc-1/52-02-SUMMARY.md
    - .planning/phases/52-datum-2026-06-16-poc-1/52-03-SUMMARY.md
  </read_first>
  <action>
msbuild Debug/x64 Rebuild 를 실행해 전 Plan 통합 후 빌드가 PASS 하고 신규 에러/경고가 0 인지 확인 (baseline = Phase 51 warning 수).

빌드 명령 (PowerShell):
```
msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild
```
HALCON/카메라 DLL 경로는 기존 csproj 참조 사용. 빌드 결과(errors/warnings 수)를 52-UAT.md 에 기록.

`.planning/phases/52-datum-2026-06-16-poc-1/52-UAT.md` 를 frontmatter(status: partial) + 5 Test 골격으로 작성:
- Test 1: LevelingEnabled=off 기본 회귀 (기존 레시피 측정값 동일)
- Test 2: 기준 Datum(IsLevelingReference) 지정 + LevelingEnabled=on → 기울어진 이미지 수평 정렬 시각 확인
- Test 3: 회전 방향 부호 확인 (기울기 상쇄 = 올바른 방향, 반대로 더 기울면 부호 반전 hotfix)
- Test 4: 기준 Datum 미지정/검출 실패 시 무회전 폴백 (측정 정상 진행, abort 없음)
- Test 5: INI save/load — LevelingEnabled/IsLevelingReference 저장 후 재로드 시 유지 + 기존 레시피 폴백 off
  </action>
  <verify>
    <automated>test -f .planning/phases/52-datum-2026-06-16-poc-1/52-UAT.md && grep -c "Test" .planning/phases/52-datum-2026-06-16-poc-1/52-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 Rebuild PASS, 신규 에러 0, 신규 경고 0 (baseline 대비)
    - 52-UAT.md 존재 + 5 Test 항목 + frontmatter status: partial
    - 빌드 결과(errors/warnings) 52-UAT.md 에 기록
  </acceptance_criteria>
  <done>통합 빌드 PASS, 52-UAT.md 5 Test 골격 작성.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 52 이미지 수평 보정 (Datum 에지 기반 회전 정렬) 전 경로:
    - 시퀀스 단위 LevelingEnabled 토글 + INI 저장 (기본 off)
    - DatumConfig.IsLevelingReference 기준 Datum 지정 플래그
    - 기준 Datum 수평 2-ROI 피팅 각도 산출 (DatumFindingService.TryGetLevelingAngle)
    - grab 직후 SHOT 이미지 임의각 회전 (VisionAlgorithmService.RotateImageByAngle, 시퀀스당 1회 산출 → 전 SHOT 공유)
  </what-built>
  <how-to-verify>
    SIMUL_MODE 빌드로 앱 실행 후:

    1. **Test 1 (off 회귀):** 기존 레시피 로드 → 임의 SHOT 검사 → 측정값을 기록. LevelingEnabled 미설정(기본 off) 상태에서 측정값이 Phase 51 baseline 과 동일한지 확인. (기존 레시피 INI 에 레벨링 키 없음 → off 폴백)

    2. **Test 2 (레벨링 동작):** 한 시퀀스(예: Top)의 Datum 1개를 PropertyGrid 에서 IsLevelingReference = true 로 지정 + 시퀀스 LevelingEnabled = on (FIXTURE 토글). 기울어진 입력 이미지로 검사 실행 → 결과 화면에서 이미지가 수평 정렬되어 표시되는지 육안 확인. Datum 수평 라인이 수평에 가깝게 정렬되는지 확인.

    3. **Test 3 (부호/방향):** Test 2 에서 회전 방향이 기울기를 **상쇄**하는 방향인지 확인. 만약 반대로 더 기울어지면 → Action_FAIMeasurement Grab 의 `-rotSeq.LevelingAngleRad` 부호를 `+` 로 반전하는 hotfix 필요 (보고).

    4. **Test 4 (무회전 폴백):** IsLevelingReference 를 전부 끄거나 기준 Datum 검출이 실패하도록 ROI 를 잘못 둔 뒤 LevelingEnabled=on 으로 검사 → 앱이 abort/crash 없이 무회전으로 측정 진행하는지 + 로그에 "[Leveling] ... 무회전 진행" 출력되는지 확인.

    5. **Test 5 (INI 영속):** LevelingEnabled=on + IsLevelingReference 설정 후 레시피 저장 → 앱 재시작 → 재로드 시 설정 유지 확인. CameraRole 전환(TopBottom ↔ Side) 후 저장 시 비활성 시퀀스 Datum/레벨링 키 소실 없는지 확인 (MEMORY recipe_datum_loss_camerarole 회귀 가드).

    각 Test 결과(PASS/FAIL + 비고)를 알려주세요. FAIL 시 hotfix 후 재검증.
  </how-to-verify>
  <resume-signal>각 Test 결과(PASS/FAIL)를 보고하거나 "approved" 입력 시 sign-off 진행</resume-signal>
</task>

<task type="auto">
  <name>Task 2: UAT 결과 기록 + sign-off + ROADMAP/STATE 갱신</name>
  <read_first>
    - .planning/phases/52-datum-2026-06-16-poc-1/52-UAT.md (Task 1 골격)
    - .planning/ROADMAP.md (Phase 52 항목)
  </read_first>
  <action>
사용자 검증 결과를 52-UAT.md 각 Test 에 PASS/FAIL + 날짜로 기록. 전 항목 PASS 또는 사용자 합의 시 frontmatter status: signed_off.

ROADMAP.md Phase 52 항목을 `[x] **Phase 52: 이미지 수평 보정** ... SIGNED_OFF {날짜}` 로 갱신 + Progress Table Status = SIGNED_OFF. REQUIREMENTS.md Traceability LEVEL-01 행이 있으면 signed_off.

carry-over 가 발생하면(예: Datum 검출 이미지 회전 미적용, 부호 hotfix) CO-52-XX 로 등재.
  </action>
  <verify>
    <automated>grep -in "signed_off\|sign-off\|SIGNED" .planning/phases/52-datum-2026-06-16-poc-1/52-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 52-UAT.md 각 Test 에 결과(PASS/FAIL) + 날짜 기록
    - status: signed_off (또는 partial_signed_off + carry-over 명시)
    - ROADMAP.md Phase 52 = SIGNED_OFF + Progress Table 갱신
  </acceptance_criteria>
  <done>52-UAT.md sign-off, ROADMAP/STATE 갱신, carry-over(있으면) 등재.</done>
</task>

</tasks>

<threat_model>
보안 표면 신규 없음 (오프라인 산업용 WPF 앱, UAT 검증 phase). Plan 01~03 의 T-52-01~06 dispositions 가 적용됨.
</threat_model>

<verification>
- msbuild Debug/x64 통합 PASS
- 사용자 SIMUL 육안 검증 5 Test
- ROADMAP/STATE Phase 52 SIGNED_OFF
</verification>

<success_criteria>
- 기울어진 입력 이미지가 수평 정렬된 후 측정 (Test 2 PASS)
- 기존 측정 회귀 0 (Test 1 PASS)
- LEVEL-01 충족, Phase 52 SIGNED_OFF
</success_criteria>

<output>
After completion, update `.planning/phases/52-datum-2026-06-16-poc-1/52-UAT.md` (sign-off) and create `.planning/phases/52-datum-2026-06-16-poc-1/52-04-SUMMARY.md`
</output>
