---
phase: 53-2026-06-16-poc-2
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - WPF_Example/UI/Dialog/CalibrationWindow.xaml
  - WPF_Example/UI/Dialog/CalibrationWindow.xaml.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: false
requirements: [CAL-01]
must_haves:
  truths:
    - "별도 캘리브 창에서 칸 크기(mm)를 입력하고 이미지 로드/라이브 촬상으로 체커보드를 띄울 수 있다"
    - "[검출] 시 코너 검출 + mm/px 산출 + 외곽 편차% 리포트가 표시되고 임계 초과 시 경고 라벨이 뜬다"
    - "SIMUL_MODE 에서는 라이브 촬상 버튼이 비활성화된다 (이미지 로드만)"
    - "산출 결과를 외부(MainView)가 [적용]에 쓸 수 있도록 노출한다"
  artifacts:
    - path: "WPF_Example/UI/Dialog/CalibrationWindow.xaml"
      provides: "캘리브 창 레이아웃 (입력/버튼/리포트/뷰어)"
      contains: "CalibrationViewer"
    - path: "WPF_Example/UI/Dialog/CalibrationWindow.xaml.cs"
      provides: "입력모드/검출/리포트 핸들러 + 결과 노출"
      contains: "CheckerboardCalibrationService"
  key_links:
    - from: "CalibrationWindow.DetectButton_Click"
      to: "CheckerboardCalibrationService.TryCalibrate"
      via: "현재 표시 HImage + knownMm 전달"
      pattern: "TryCalibrate"
---

<objective>
체커보드 캘리브 전용 WPF Window 를 신규 작성한다(TeachingWindow 패턴 복사). 좌측: 칸 크기(mm) 입력(D-01 직전값 기억) + saddle Sigma/Threshold/왜곡임계% 튜닝 입력(open question 2 해소) + [이미지 로드]/[라이브 촬상]/[검출] 버튼 + 리포트/왜곡 경고 라벨(D-05). 우측: HalconViewerControl. plan 01 서비스를 호출해 산출하고, 결과를 외부가 [적용]에 쓰도록 public 으로 노출한다(반영/저장 wiring 은 plan 03).

Purpose: CAL-01 의 입력·검출·리포트 UI 계층(D-04/D-05/D-01). 반영은 D-06 게이트로 plan 03.
Output: `CalibrationWindow.xaml` + `.xaml.cs`, csproj Page/Compile 등록.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-2026-06-16-poc-2/53-PATTERNS.md
@.planning/phases/53-2026-06-16-poc-2/53-01-SUMMARY.md

<interfaces>
<!-- plan 01 산출 계약 (53-01-SUMMARY 참조). 서비스 호출만 — 알고리즘 재구현 금지 -->
From WPF_Example/Halcon/Algorithms/CheckerboardCalibrationService.cs:
  public bool TryCalibrate(HImage image, double knownMmPerCell,
      double saddleSigma, double saddleThreshold, double distortionWarnPct,
      out CalibrationResult result, out string error)
  public const double DefaultSaddleSigma / DefaultSaddleThreshold / DistortionWarnThresholdPct
  CalibrationResult { MmPerPixel, MmPerPixelX, MmPerPixelY, MeanSpacingPx,
      CenterOuterDeviationPct, IsDistortionWarn, CornerCount }

<!-- analog: TeachingWindow (별도 Window 선례) -->
From WPF_Example/UI/Dialog/TeachingWindow.xaml.cs:
  public Func<string> ImageGrabber { get; set; }  // caller(MainView) 주입, grab→imagePath
  생성자: InitializeComponent(); TeachingViewer.PointerChanged += ...
From WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs:
  public void LoadImage(string imagePath)               // 파일 경로 표시
  public void LoadImage(HImage image, string sourceContext = null)  // grab HImage 직접 표시
  public HImage CurrentImage { get; }  // 표시 중 이미지 접근 (검출 입력) — 없으면 LoadImage 시 보관 필드 추가
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CalibrationWindow.xaml 레이아웃 (TeachingWindow 패턴 복사)</name>
  <read_first>
    - WPF_Example/UI/Dialog/TeachingWindow.xaml (Window 선언/2-column Grid/버튼 스타일 리소스/HalconViewerControl/StatusBar)
    - .planning/phases/53-2026-06-16-poc-2/53-PATTERNS.md (CalibrationWindow.xaml 섹션 라인 82-120)
  </read_first>
  <action>
신규 `WPF_Example/UI/Dialog/CalibrationWindow.xaml`. TeachingWindow.xaml 통째 차용:
- `<Window x:Class="ReringProject.UI.CalibrationWindow"` + 동일 xmlns + `xmlns:viewers="clr-namespace:ReringProject.UI"` + `Title="픽셀 캘리브레이션 (체커보드)"` + `Height="860" Width="1400"` + `WindowStartupLocation="CenterOwner"`.
- `Window.Resources` 에 TeachingWindow 의 `TeachingActionButtonStyle` 복사 → `CalibrationActionButtonStyle` 로 rename.
- 2-column Grid: 좌(`Width="430"`)/우(`Width="*"`). 좌측 StackPanel 구성(위→아래):
  1. 칸 크기(mm) TextBox `x:Name="txt_cellMm"` (D-01 — 직전값 코드비하인드서 채움)
  2. saddle Sigma TextBox `x:Name="txt_sigma"`, Threshold `x:Name="txt_threshold"`, 왜곡임계% `x:Name="txt_warnPct"` (튜닝 — 기본값 코드비하인드서 const 로 채움)
  3. 버튼 WrapPanel: `<Button x:Name="btn_loadImage" Style="{StaticResource CalibrationActionButtonStyle}" Content="이미지 로드" Click="LoadImageButton_Click"/>`, `btn_liveCapture` Content="라이브 촬상" Click="GrabImageButton_Click", `btn_detect` Content="검출" Click="DetectButton_Click", `btn_apply` Content="적용" Click="ApplyButton_Click" IsEnabled="False" (D-06 — 검출 성공 후 활성)
  4. 리포트: `<TextBlock x:Name="txt_report"/>` (mm/px, X/Y, 평균간격, 코너수)
  5. 왜곡 경고 라벨: `<TextBlock x:Name="lbl_distortionWarn" Foreground="Red" Visibility="Collapsed"/>` (D-05)
- 우측: `<viewers:HalconViewerControl x:Name="CalibrationViewer" Grid.Column="1"/>` + 하단 `<StatusBar Grid.Row="1"><StatusBarItem><TextBlock x:Name="CalibrationStatusTextBlock" Text="Ready"/></StatusBarItem></StatusBar>` (TeachingWindow Grid.Row 구조 복사).
모든 UI 텍스트 한국어.
  </action>
  <verify>
    <automated>MISSING — build-verify Task 3. grep: xaml 에 "x:Class=\"ReringProject.UI.CalibrationWindow\"" AND "CalibrationViewer" AND "btn_detect" AND "btn_apply" AND "lbl_distortionWarn"</automated>
  </verify>
  <acceptance_criteria>
    - grep xaml `x:Class="ReringProject.UI.CalibrationWindow"` 매치
    - grep `x:Name="txt_cellMm"`, `txt_sigma`, `txt_threshold`, `txt_warnPct` 모두 매치
    - grep `btn_loadImage`/`btn_liveCapture`/`btn_detect`/`btn_apply` 4 버튼 + Click 핸들러 바인딩 존재
    - grep `lbl_distortionWarn` + `CalibrationViewer` 매치
    - btn_apply 가 IsEnabled="False" (D-06 게이트)
  </acceptance_criteria>
  <done>2-column 캘리브 창 레이아웃 (입력 4 + 버튼 4 + 리포트 + 경고 라벨 + 뷰어 + StatusBar)</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CalibrationWindow.xaml.cs — 입력모드/검출/리포트/결과 노출</name>
  <read_first>
    - WPF_Example/UI/Dialog/TeachingWindow.xaml.cs (using/partial class/생성자/LoadImageButton_Click OpenFileDialog/ImageGrabber Func 주입/GrabImageButton_Click)
    - WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs (LoadImage(string), LoadImage(HImage,string), 표시 이미지 접근)
    - .planning/phases/53-2026-06-16-poc-2/53-RESEARCH.md §"Pattern 4"(SIMUL 분기), §"Common Pitfalls" 1/3
    - 53-01-SUMMARY.md (TryCalibrate 시그니처 + CalibrationResult)
  </read_first>
  <action>
신규 `WPF_Example/UI/Dialog/CalibrationWindow.xaml.cs`. `using System; using System.Globalization; using System.Windows; using Microsoft.Win32; using HalconDotNet; using ReringProject.Halcon.Algorithms;`. namespace `ReringProject.UI`. `public partial class CalibrationWindow : Window`. Allman? — TeachingWindow 스타일 따름(편집 분기: TeachingWindow.xaml.cs 의 K&R 여부 확인 후 동일 적용). `//260623 hbk Phase 53`.

필드/생성자:
```csharp
private readonly CheckerboardCalibrationService _calibService = new CheckerboardCalibrationService();
private HImage _currentImage;            // 검출 입력
private CalibrationResult _lastResult;   // [적용]이 소비 (plan 03)
public Func<string> ImageGrabber { get; set; }   // caller(MainView) 주입, grab→imagePath
public CalibrationResult LastResult { get { return _lastResult; } }   // 외부 노출 (plan 03)
public CalibrationWindow()
{
    InitializeComponent();
    txt_cellMm.Text = Properties.Settings... ;  // 직전값 — 아래 D-01 참조
    txt_sigma.Text = CheckerboardCalibrationService.DefaultSaddleSigma.ToString(CultureInfo.InvariantCulture);
    txt_threshold.Text = CheckerboardCalibrationService.DefaultSaddleThreshold.ToString(CultureInfo.InvariantCulture);
    txt_warnPct.Text = CheckerboardCalibrationService.DistortionWarnThresholdPct.ToString(CultureInfo.InvariantCulture);
#if SIMUL_MODE
    btn_liveCapture.IsEnabled = false;
    btn_liveCapture.ToolTip = "SIMUL 모드에서는 이미지 로드만 가능합니다.";
#endif
}
```
D-01 직전값 기억: Properties.Settings 가 없으면 static 필드 `private static string _lastCellMm = "1.0";` 로 보관(앱 수명 내 기억) + 검출 성공 시 갱신. (간단·INI 의존 없음.)

(a) `LoadImageButton_Click`: OpenFileDialog(Filter "Image Files (*.bmp;*.jpg;*.jpeg;*.png;*.tif;*.tiff)|...|All Files (*.*)|*.*") → ShowDialog(this) != true return → `CalibrationViewer.LoadImage(path)` + 동시에 `_currentImage = new HImage(path);` (검출 입력 확보, try/catch 가드) → status "로드: {path}".

(b) `GrabImageButton_Click` (라이브, Pattern 4): `if (ImageGrabber == null) { status="라이브 촬상 불가"; return; }` → `string p = ImageGrabber();` → IsNullOrWhiteSpace 가드 → `CalibrationViewer.LoadImage(p); _currentImage = new HImage(p);`. (SIMUL 은 버튼 비활성이라 진입 안 함.)

(c) `DetectButton_Click`:
```csharp
if (_currentImage == null) { CalibrationStatusTextBlock.Text = "이미지를 먼저 로드하세요."; return; }
double mm, sigma, thr, warnPct;
if (!double.TryParse(txt_cellMm.Text, NumberStyles.Float, CultureInfo.InvariantCulture, out mm) || mm <= 0)
    { CustomMessageBox.Show("칸 크기(mm)에 0보다 큰 숫자를 입력하세요.", "캘리브레이션"); return; }   // V5 입력검증
// sigma/thr/warnPct 파싱 실패 시 const 기본값 폴백
if (!double.TryParse(txt_sigma.Text, NumberStyles.Float, CultureInfo.InvariantCulture, out sigma)) sigma = CheckerboardCalibrationService.DefaultSaddleSigma;
... thr, warnPct 동일 ...
CalibrationResult result; string err;
if (!_calibService.TryCalibrate(_currentImage, mm, sigma, thr, warnPct, out result, out err))
    { CalibrationStatusTextBlock.Text = "검출 실패: " + err; btn_apply.IsEnabled = false; return; }
_lastResult = result;
_lastCellMm = txt_cellMm.Text;   // D-01 직전값 갱신
txt_report.Text = string.Format("1 px = {0:F5} mm (X {1:F5} / Y {2:F5})\n평균 간격 {3:F2} px · 코너 {4}개\n중앙↔외곽 편차 {5:F2}%",
    result.MmPerPixel, result.MmPerPixelX, result.MmPerPixelY, result.MeanSpacingPx, result.CornerCount, result.CenterOuterDeviationPct);
if (result.IsDistortionWarn)
    { lbl_distortionWarn.Text = string.Format("[경고] 외곽 왜곡 {0:F2}% — undistort 검토 필요", result.CenterOuterDeviationPct); lbl_distortionWarn.Visibility = Visibility.Visible; }
else lbl_distortionWarn.Visibility = Visibility.Collapsed;
btn_apply.IsEnabled = true;   // D-06: 검출 성공 후에만 적용 가능
CalibrationStatusTextBlock.Text = "검출 완료";
```

(d) `ApplyButton_Click`: 본 plan 에선 stub — `//260623 hbk Phase 53: 반영/저장 wiring 은 plan 03 (ApplyRequested 이벤트로 위임)`. 외부 위임용 `public event Action<CalibrationResult> ApplyRequested;` 선언 + 핸들러는 `if (_lastResult != null && ApplyRequested != null) ApplyRequested(_lastResult);`. (plan 03 이 MainView 에서 이 이벤트를 구독해 반영+저장.)

`CustomMessageBox` 가 같은 namespace 접근 가능한지 확인(TeachingWindow 에서 쓰는 동일 타입). HImage 생성 실패는 try/catch + status 메시지(throw 금지, CLAUDE.md).
  </action>
  <verify>
    <automated>MISSING — build-verify Task 3. grep: cs 에 "_calibService.TryCalibrate" AND "IsDistortionWarn" AND "btn_apply.IsEnabled = true" AND "#if SIMUL_MODE" AND "event Action<CalibrationResult> ApplyRequested"</automated>
  </verify>
  <acceptance_criteria>
    - grep "partial class CalibrationWindow : Window" + namespace "ReringProject.UI"
    - grep "_calibService.TryCalibrate(" 매치 (서비스 호출)
    - grep "#if SIMUL_MODE" + "btn_liveCapture.IsEnabled = false" (D-04 SIMUL 분기)
    - grep "double.TryParse" + "mm <= 0" 입력검증 (V5)
    - grep "result.IsDistortionWarn" + "lbl_distortionWarn.Visibility = Visibility.Visible" (D-05)
    - grep "btn_apply.IsEnabled = true" 검출 성공 후만 (D-06)
    - grep "public event Action<CalibrationResult> ApplyRequested" + "public Func<string> ImageGrabber"
    - grep -c "set_calibration_data|undistort|map_image" == 0 (알고리즘은 서비스 위임, UI 에 미존재)
  </acceptance_criteria>
  <done>입력 검증 + 이미지 로드/라이브 + [검출]→서비스 호출→리포트+경고 + D-06 적용 게이트 + ApplyRequested 이벤트 노출</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: csproj Page/Compile 등록 + Debug/x64 빌드</name>
  <read_first>
    - WPF_Example/DatumMeasurement.csproj (기존 UI\Dialog\TeachingWindow.xaml `<Page>` + TeachingWindow.xaml.cs `<Compile>` 항목 패턴)
  </read_first>
  <action>
csproj 에 TeachingWindow 등록 인접에 추가 (백슬래시 경로):
```xml
<Page Include="UI\Dialog\CalibrationWindow.xaml">
  <Generator>MSBuild:Compile</Generator>
  <SubType>Designer</SubType>
</Page>
...
<Compile Include="UI\Dialog\CalibrationWindow.xaml.cs">
  <DependentUpon>CalibrationWindow.xaml</DependentUpon>
</Compile>
```
정확한 `<Page>`/`<Compile>` 자식 요소는 기존 TeachingWindow 항목을 그대로 미러링. 그 후 Debug/x64 빌드.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && MSBuild WPF_Example/DatumMeasurement.csproj //p:Configuration=Debug //p:Platform=x64 //t:Build //v:minimal — exit 0</automated>
  </verify>
  <acceptance_criteria>
    - grep csproj `UI\\Dialog\\CalibrationWindow.xaml` (Page) AND `UI\\Dialog\\CalibrationWindow.xaml.cs` (Compile, DependentUpon)
    - MSBuild Debug/x64 exit 0, 신규 error 0
  </acceptance_criteria>
  <done>창 csproj 등록 + Debug/x64 빌드 PASS</done>
</task>

</tasks>

<verification>
- Debug/x64 빌드 PASS (Task 3)
- **SIMUL 수동 UAT (실행 단계서 사용자):**
  1. 앱 실행 → 캘리브 창 진입(plan 03 wiring 후) → 라이브 촬상 버튼 비활성 확인 (D-04 SIMUL)
  2. 인터넷 체커보드 이미지 [이미지 로드] → 우측 뷰어 표시
  3. 칸 크기(mm) 입력 → [검출] → 리포트(mm/px·X/Y·코너수·편차%) 표시
  4. 외곽 왜곡 큰 이미지 → 1% 초과 시 빨강 경고 라벨 표시 (D-05)
  5. 검출 전 [적용] 비활성 → 검출 성공 후 활성 (D-06)
</verification>

<success_criteria>
별도 캘리브 창에서 칸 크기 입력 + 이미지 로드/라이브(실HW) + [검출] → mm/px·X/Y·편차% 리포트 + 왜곡 경고가 동작하고, 산출 결과를 ApplyRequested/LastResult 로 외부에 노출한다.
</success_criteria>

<output>
완료 후 `.planning/phases/53-2026-06-16-poc-2/53-02-SUMMARY.md` 생성 (CalibrationWindow public 멤버: ImageGrabber, LastResult, ApplyRequested 이벤트 — plan 03 launch wiring 이 참조).
</output>
