---
phase: 54-datum-align-01-x-y-tilt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
autonomous: true
requirements: [ALIGN-01]

must_haves:
  truths:
    - "Datum 노드 PropertyGrid 에 패턴매칭 활성 토글(IsPatternAlignEnabled)이 노출되고 기본 off"
    - "PatternEngine 드롭다운에 Shape/NCC 두 항목이 보이고 기본 Shape"
    - "INI 키가 없는 기존 레시피 로드 시 IsPatternAlignEnabled=false 폴백 → off 회귀 0"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "ALIGN-01 per-Datum 영속 필드 + PatternEngine 드롭다운 + EnsurePerRoiDefaults 폴백 + ICustomTypeDescriptor hide"
      contains: "IsPatternAlignEnabled"
  key_links:
    - from: "DatumConfig.PatternEngine"
      to: "PatternEngineList ItemsSource"
      via: "ItemsSourceProperty"
      pattern: "ItemsSourceProperty\\(nameof\\(PatternEngineList\\)\\)"
    - from: "DatumConfig.EnsurePerRoiDefaults"
      to: "IsPatternAlignEnabled / Pattern* sentinel"
      via: "INI 키 미존재 폴백"
      pattern: "PatternMinScore"
---

<objective>
DatumConfig 에 ALIGN-01 per-Datum 영속 모델을 추가한다: 활성 토글(IsPatternAlignEnabled, 기본 false), 엔진 선택자(PatternEngine Shape|NCC), ref pose(RefMatchRow/Col/AngleDeg), 매칭 파라미터(PatternMinScore/PatternAngleExtentDeg/PatternSearchMarginPx), 패턴 ROI(PatternRoi_*). 기존 AlgorithmType 드롭다운 + IsLevelingReference 토글 + EnsurePerRoiDefaults 멱등 폴백 + ICustomTypeDescriptor hide 패턴을 그대로 미러한다.

Purpose: 패턴매칭 보정의 영속 계약(off 회귀 0 + 모델 경로 비저장)을 확정하여 Wave 2/3 이 의존하는 필드를 먼저 정의한다.
Output: DatumConfig.cs 단일 파일 수정. ParamBase reflection 직렬화로 caller 무수정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-datum-align-01-x-y-tilt/54-CONTEXT.md
@.planning/phases/54-datum-align-01-x-y-tilt/54-PATTERNS.md

LOCKED 결정(재결정 금지): D-01(PatternEngine Shape|NCC 기본 Shape), D-07(모델 경로 DatumConfig 저장 금지 — 이름 기반 재계산), D-09(RefMatch* ref pose), D-11(IsPatternAlignEnabled 기본 false + INI 폴백).
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: PatternEngine 드롭다운 + ALIGN 영속 필드 추가</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (수정 대상 — 특히 AlgorithmType 드롭다운 L73-103, IsLevelingReference 토글 L40-43, ExpectedAngleDeg/AngleTolerance 영속 필드 L105-123, Line1_* ROI 필드 + _PhiDeg wrapper L132-145)
  </read_first>
  <action>
DatumConfig.cs 에 아래 멤버를 AlgorithmType 드롭다운 패턴(L73-103)을 미러하여 추가한다. 모든 신규 라인에 `//260618 hbk Phase 54 ALIGN-01` 주석. Allman 브레이스(파일 규약 — DatumConfig 은 Allman 사용). C# 7.2 (switch expression / record / nullable ref 금지).

1) 엔진 선택자 (string-backed — ParamBase 가 enum 직렬화 미지원이므로 반드시 string):
```csharp
//260618 hbk Phase 54 ALIGN-01 매칭 엔진 선택자 (D-01) — AlgorithmType 드롭다운 미러
[Category("Datum|PatternAlign")]
[ItemsSourceProperty(nameof(PatternEngineList))]
public string PatternEngine { get; set; } = "Shape";

[PropertyTools.DataAnnotations.Browsable(false)]
public List<string> PatternEngineList { get { return new List<string> { "Shape", "NCC" }; } }
```

2) 활성 토글 (IsLevelingReference 미러, D-11 기본 false):
```csharp
//260618 hbk Phase 54 ALIGN-01 패턴매칭 위치보정 활성 (D-11) — 기본 false → off 회귀 0
[Category("Datum|PatternAlign")]
public bool IsPatternAlignEnabled { get; set; } = false;
```

3) ref pose + 매칭 파라미터 (double, sentinel 0 — ExpectedAngleDeg 미러). PatternModelPath 절대 추가 금지(D-07 이름 기반 재계산):
```csharp
//260618 hbk Phase 54 ALIGN-01 ref pose (D-09) — 티칭 시 find 결과 1회 기록
[Category("Datum|PatternAlign")]
public double RefMatchRow { get; set; } = 0.0;
[Category("Datum|PatternAlign")]
public double RefMatchCol { get; set; } = 0.0;
[Category("Datum|PatternAlign")]
public double RefMatchAngleDeg { get; set; } = 0.0;
//260618 hbk Phase 54 ALIGN-01 매칭 파라미터 (D-06/D-06a) — sentinel 0 → EnsurePerRoiDefaults 기본값 복원
[Category("Datum|PatternAlign")]
public double PatternMinScore { get; set; } = 0.0;
[Category("Datum|PatternAlign")]
public double PatternAngleExtentDeg { get; set; } = 0.0;
[Category("Datum|PatternAlign")]
public double PatternSearchMarginPx { get; set; } = 0.0;
```

4) 패턴 ROI(Rect) 필드 — Line1_* ROI 필드(L132-145) + _PhiDeg wrapper 패턴 복사:
```csharp
//260618 hbk Phase 54 ALIGN-01 패턴 템플릿 ROI (D-08) — Line1_* ROI 패턴 미러
[Category("Datum|PatternAlign")]
public double PatternRoi_Row { get; set; } = 0.0;
[Category("Datum|PatternAlign")]
public double PatternRoi_Col { get; set; } = 0.0;
[Category("Datum|PatternAlign")]
public double PatternRoi_Phi { get; set; } = 0.0;
[Category("Datum|PatternAlign")]
public double PatternRoi_Length1 { get; set; } = 0.0;
[Category("Datum|PatternAlign")]
public double PatternRoi_Length2 { get; set; } = 0.0;
```
(파일에 Line1_*_PhiDeg wrapper 가 있으면 동일 형태로 PatternRoi_PhiDeg wrapper 도 추가. 없으면 생략.)
  </action>
  <verify>
    <automated>grep -c "260618 hbk Phase 54 ALIGN-01" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</automated>
  </verify>
  <acceptance_criteria>
    - DatumConfig.cs contains `public string PatternEngine { get; set; } = "Shape";`
    - DatumConfig.cs contains `ItemsSourceProperty(nameof(PatternEngineList))`
    - DatumConfig.cs PatternEngineList getter contains both `"Shape"` and `"NCC"`
    - DatumConfig.cs contains `public bool IsPatternAlignEnabled { get; set; } = false;`
    - DatumConfig.cs contains `RefMatchRow`, `RefMatchCol`, `RefMatchAngleDeg`, `PatternMinScore`, `PatternAngleExtentDeg`, `PatternSearchMarginPx`
    - DatumConfig.cs contains `PatternRoi_Row`, `PatternRoi_Col`, `PatternRoi_Phi`, `PatternRoi_Length1`, `PatternRoi_Length2`
    - DatumConfig.cs does NOT contain `PatternModelPath` (D-07 경로 비저장)
  </acceptance_criteria>
  <done>모든 ALIGN 필드가 [Category("Datum|PatternAlign")] 로 PropertyGrid 노출, PatternEngine 드롭다운 Shape/NCC, IsPatternAlignEnabled 기본 false, PatternModelPath 부재.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: EnsurePerRoiDefaults 폴백 + ICustomTypeDescriptor hide</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (수정 대상 — EnsurePerRoiDefaults 멱등 폴백 L574-677 특히 null 가드 L617-618, IsHiddenForAlgorithm L721-753, BuildFilteredProperties + sourceNames HashSet L690-705)
  </read_first>
  <action>
1) EnsurePerRoiDefaults(L574-677) 에 ALIGN sentinel 폴백을 멱등(이미 의미값이면 미변경)으로 추가. INI 키 미존재 시 ParamBase 가 기본값을 채우지만, sentinel 0 → 기본값 복원 패턴을 명시 추가(다운샘플/마진 SIMUL 튜닝 가능 기본값). `//260618 hbk Phase 54 ALIGN-01` 주석:
```csharp
//260618 hbk Phase 54 ALIGN-01 매칭 파라미터 sentinel 0 → 기본값 복원 (D-11 멱등 폴백)
if (PatternMinScore <= 0.0) PatternMinScore = 0.6;          // SIMUL 튜닝 기본 (Claude's Discretion)
if (PatternAngleExtentDeg <= 0.0) PatternAngleExtentDeg = 10.0; // coarse x,y 전용 → 작은 angle range (D-01b)
if (PatternSearchMarginPx <= 0.0) PatternSearchMarginPx = 100.0; // template ROI ± margin (D-06)
```
주의: IsPatternAlignEnabled 는 bool — INI 키 미존재 시 자동 false(D-11). 별도 폴백 라인 불필요하나, PatternEngine null 가드는 추가:
```csharp
//260618 hbk Phase 54 ALIGN-01 PatternEngine null 가드 (INI 키 미존재)
if (PatternEngine == null) PatternEngine = "Shape";
```

2) ICustomTypeDescriptor 동적 hide: IsHiddenForAlgorithm(L721-753) 에 PatternEngine=="NCC" 분기로 Shape 전용 파라미터를 hide 하는 로직을 추가(또는 그 반대). 1차 최소: PatternAngleExtentDeg 는 양쪽 공통 노출, 엔진별 hide 가 불명확하면 hide 0(전부 노출)로 두되, BuildFilteredProperties 의 sourceNames HashSet(L690-705)에 반드시 `PatternEngineList` 를 추가하여 드롭다운 ItemsSource 가 필터에서 누락되지 않도록 한다(CO-01 ItemsSource fallback 버그 선례).
```csharp
//260618 hbk Phase 54 ALIGN-01 PatternEngineList 를 sourceNames 화이트리스트에 강제 포함 (CO-01 ItemsSource fallback 가드)
sourceNames.Add("PatternEngineList");
```
주의(L13, L680-683): ICustomTypeDescriptor 는 PropertyGrid hide 만 — INI 직렬화는 Reflection 경로라 hide 해도 값 보존(하위호환).
  </action>
  <verify>
    <automated>grep -n "PatternMinScore\|PatternEngineList\|PatternEngine == null" WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</automated>
  </verify>
  <acceptance_criteria>
    - DatumConfig.cs EnsurePerRoiDefaults body contains `if (PatternMinScore <= 0.0) PatternMinScore =`
    - DatumConfig.cs contains `if (PatternAngleExtentDeg <= 0.0) PatternAngleExtentDeg =`
    - DatumConfig.cs contains `if (PatternSearchMarginPx <= 0.0) PatternSearchMarginPx =`
    - DatumConfig.cs contains `if (PatternEngine == null) PatternEngine = "Shape";`
    - DatumConfig.cs BuildFilteredProperties / sourceNames region contains `sourceNames.Add("PatternEngineList")`
  </acceptance_criteria>
  <done>INI 키 미존재 레시피 로드 시 PatternMinScore/AngleExtent/SearchMargin 기본값 복원 + IsPatternAlignEnabled false 폴백; PatternEngine 드롭다운 ItemsSource 가 필터에서 유지.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INI 레시피 파일 → DatumConfig | 디스크의 레시피 INI 가 신뢰되지 않을 수 있는 입력으로 역직렬화됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Tampering | DatumConfig INI 역직렬화 | accept | ParamBase reflection 은 double/int/string/bool 만 처리(타입 주입 불가). sentinel 폴백이 비정상값(<=0)을 기본값으로 정규화 |
| T-54-02 | Denial of Service | EnsurePerRoiDefaults | mitigate | 멱등 폴백 — 음수/0 값을 결정적 기본값으로 클램프하여 다운스트림 매칭 파라미터 무한루프/예외 방지 |
</threat_model>

<verification>
- msbuild Debug/x64 빌드 PASS (신규 error 0)
- 기존 레시피(ALIGN 키 부재) 로드 시 IsPatternAlignEnabled=false → 측정 byte-identical(off 회귀 0)
</verification>

<success_criteria>
- DatumConfig 에 ALIGN per-Datum 필드 전부 존재 + PatternModelPath 부재(D-07)
- PatternEngine 드롭다운 Shape/NCC 노출 + 기본 Shape
- EnsurePerRoiDefaults 멱등 폴백 + IsPatternAlignEnabled 기본 false (D-11)
</success_criteria>

<output>
완료 후 `.planning/phases/54-datum-align-01-x-y-tilt/54-01-SUMMARY.md` 생성.
</output>
