---
phase: 54-datum-align-01-x-y-tilt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Device/DeviceHandler.cs
  - WPF_Example/Utility/RecipeFileHelper.cs
autonomous: true
requirements: [ALIGN-01]

must_haves:
  truths:
    - "HALCON shape/ncc 모델 확장자 상수(.shm/.ncm)가 정의되어 있다"
    - "엔진(Shape|NCC)에 따라 .shm/.ncm 경로를 이름 기반으로 결정적으로 재계산한다(절대경로 저장 없음)"
    - "모델 파일이 레시피 폴더 하위에 위치하여 Copy/Delete 가 자동 동반한다"
  artifacts:
    - path: "WPF_Example/Custom/Device/DeviceHandler.cs"
      provides: "EXTENSION_SHAPE_MODEL / EXTENSION_NCC_MODEL 상수"
      contains: "EXTENSION_SHAPE_MODEL"
    - path: "WPF_Example/Utility/RecipeFileHelper.cs"
      provides: "engine-aware GetModelFilePath 변형 (확장자 인자)"
      contains: "GetPatternModelFilePath"
  key_links:
    - from: "GetPatternModelFilePath"
      to: "RecipeSavePath/recipe/seq/act"
      via: "Path.Combine + engine 확장자"
      pattern: "EXTENSION_SHAPE_MODEL|EXTENSION_NCC_MODEL"
---

<objective>
HALCON shape/ncc 모델 파일의 신규 확장자 상수(.shm/.ncm)를 DeviceHandler 에 추가하고(기존 MIL .mmf 미재사용), RecipeFileHelper 에 엔진별 확장자를 받는 모델 경로 재계산 변형(GetPatternModelFilePath)을 추가한다. D-07 이름 기반 결정적 경로(절대경로 저장 0) + D-07a 레시피 폴더 하위 저장(Copy/Delete 자동 동반, stale 0).

Purpose: PatternMatchService(54-03)와 통합단계(54-04)·티칭 UI(54-05)가 모델 read/write 시 호출할 단일 경로 소스를 확정한다.
Output: DeviceHandler.cs 상수 2개 + RecipeFileHelper.cs 메서드 1개.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-datum-align-01-x-y-tilt/54-CONTEXT.md
@.planning/phases/54-datum-align-01-x-y-tilt/54-PATTERNS.md

LOCKED: D-07(이름 기반 재계산, 절대경로 저장 금지), D-07a(레시피 폴더 하위), D-07b(.shm/.ncm 신규 상수, .mmf 미재사용).

<interfaces>
기존 RecipeFileHelper.GetModelFilePath (수정 대상 분석용 — 호출 시그니처):
```csharp
public string GetModelFilePath(string recipeName, string seqName, string actName, string propertyName) {
    string saveFile = Path.Combine(SystemHandler.Handle.Setting.RecipeSavePath, recipeName, seqName, actName);
    saveFile += propertyName + DeviceHandler.EXTENSION_MODEL;   // ← MIL .mmf 하드코딩
    string savePath = Path.GetDirectoryName(saveFile);
    if (Directory.Exists(savePath) == false) Directory.CreateDirectory(savePath);
    return saveFile;
}
```
기존 DeviceHandler 상수:
```csharp
public const string EXTENSION_MODEL = ".mmf";   // MIL — 미재사용
public const string FILTER_MODEL = "mmf Files(*.mmf)|*.mmf";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DeviceHandler .shm/.ncm 상수 추가</name>
  <files>WPF_Example/Custom/Device/DeviceHandler.cs</files>
  <read_first>
    - WPF_Example/Custom/Device/DeviceHandler.cs (수정 대상 — EXTENSION_MODEL/FILTER_MODEL/EXTENSION_CALIBRATION 상수 블록 L77-85)
  </read_first>
  <action>
DeviceHandler 의 EXTENSION_MODEL 상수 인근(L83 부근)에 HALCON shape/ncc 모델 확장자 상수를 추가한다. 기존 .mmf 는 미재사용(MIL Alligator 레거시). `//260618 hbk Phase 54 ALIGN-01` 주석. 파일 실제 스타일 따름.
```csharp
//260618 hbk Phase 54 ALIGN-01 HALCON shape/ncc 모델 확장자 (D-07b) — 기존 .mmf(MIL) 미재사용
public const string EXTENSION_SHAPE_MODEL = ".shm";
public const string EXTENSION_NCC_MODEL = ".ncm";
```
  </action>
  <verify>
    <automated>grep -n "EXTENSION_SHAPE_MODEL\|EXTENSION_NCC_MODEL" WPF_Example/Custom/Device/DeviceHandler.cs</automated>
  </verify>
  <acceptance_criteria>
    - DeviceHandler.cs contains `public const string EXTENSION_SHAPE_MODEL = ".shm";`
    - DeviceHandler.cs contains `public const string EXTENSION_NCC_MODEL = ".ncm";`
    - EXTENSION_MODEL = ".mmf" 라인 무수정 (미재사용 보존)
  </acceptance_criteria>
  <done>.shm/.ncm 상수 존재, .mmf 보존.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: RecipeFileHelper engine-aware 모델 경로 재계산</name>
  <files>WPF_Example/Utility/RecipeFileHelper.cs</files>
  <read_first>
    - WPF_Example/Utility/RecipeFileHelper.cs (수정 대상 — GetModelFilePath L86-93 미러 대상, GetPatternImageFilePath L104-111 동형 패턴, Copy/CopyFilesRecursively L122-149, Delete L113-119)
    - WPF_Example/Custom/Device/DeviceHandler.cs (.shm/.ncm 상수 참조 — Task 1 결과)
  </read_first>
  <action>
RecipeFileHelper 에 엔진별 확장자를 받는 모델 경로 재계산 메서드를 GetModelFilePath(L86-93) 패턴 그대로 미러하여 신규 추가한다. 기존 GetModelFilePath 는 무수정(MIL .mmf 호환 보존). `//260618 hbk Phase 54 ALIGN-01` 주석.

engine 문자열("Shape"|"NCC")을 받아 확장자를 분기:
```csharp
//260618 hbk Phase 54 ALIGN-01 패턴 모델 경로 이름 기반 재계산 (D-07/D-07a/D-07b) — 절대경로 저장 안 함
public string GetPatternModelFilePath(string recipeName, string seqName, string actName, string propertyName, string engine)
{
    string ext;
    if (string.Equals(engine, "NCC", System.StringComparison.OrdinalIgnoreCase))
        ext = DeviceHandler.EXTENSION_NCC_MODEL;
    else
        ext = DeviceHandler.EXTENSION_SHAPE_MODEL;
    string saveFile = Path.Combine(SystemHandler.Handle.Setting.RecipeSavePath, recipeName, seqName, actName);
    saveFile += propertyName + ext;
    string savePath = Path.GetDirectoryName(saveFile);
    if (Directory.Exists(savePath) == false) Directory.CreateDirectory(savePath);
    return saveFile;
}
```
호출 규약(다운스트림 54-03/04/05 용): propertyName = datumName(이름 기반 결정성, D-07), engine = DatumConfig.PatternEngine. 레시피 폴더 하위(RecipeSavePath/recipe/seq/act)라 기존 Copy(CopyFilesRecursively)/Delete(폴더째)가 모델 파일 자동 동반(D-07a, stale 0). DatumConfig 에 경로 미저장.
  </action>
  <verify>
    <automated>grep -n "GetPatternModelFilePath\|EXTENSION_NCC_MODEL\|EXTENSION_SHAPE_MODEL" WPF_Example/Utility/RecipeFileHelper.cs</automated>
  </verify>
  <acceptance_criteria>
    - RecipeFileHelper.cs contains `public string GetPatternModelFilePath(string recipeName, string seqName, string actName, string propertyName, string engine)`
    - method body contains `DeviceHandler.EXTENSION_NCC_MODEL` and `DeviceHandler.EXTENSION_SHAPE_MODEL`
    - method body contains `StringComparison.OrdinalIgnoreCase` for engine compare
    - method body contains `Directory.CreateDirectory(savePath)` (디렉터리 자동 생성)
    - existing `GetModelFilePath` (.mmf) 무수정
  </acceptance_criteria>
  <done>engine "NCC" → .ncm, 그 외 → .shm 경로 반환; 레시피 폴더 하위; 절대경로 비저장; 디렉터리 자동 생성.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recipe/seq/datum 이름 → 파일 경로 | 사용자 입력 이름이 파일 시스템 경로에 합성됨 (path traversal 우려) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-03 | Tampering | GetPatternModelFilePath path traversal | accept | recipeName/seqName/datumName 은 내부 레시피 트리에서 생성된 통제된 이름 — 외부 네트워크 입력 아님. 기존 GetModelFilePath/GetPatternImageFilePath 와 동일 신뢰 모델(선례 일치) |
| T-54-04 | Information Disclosure | stale 모델 파일 | mitigate | D-07 이름 기반 재계산 — 절대경로 비저장으로 Rename/Copy/Delete 후 stale 참조 0. 레시피 폴더 하위 저장으로 폴더 단위 정합 |
</threat_model>

<verification>
- msbuild Debug/x64 빌드 PASS (신규 error 0)
- GetPatternModelFilePath("R","Top","FAI","DatumA","NCC") → ...\DatumA.ncm; engine="Shape" → ...\DatumA.shm
</verification>

<success_criteria>
- .shm/.ncm 상수 존재 (D-07b)
- engine-aware 경로 재계산 메서드 존재, 레시피 폴더 하위, 절대경로 비저장 (D-07/D-07a)
</success_criteria>

<output>
완료 후 `.planning/phases/54-datum-align-01-x-y-tilt/54-02-SUMMARY.md` 생성.
</output>
