---
phase: 54-datum-align-01-x-y-tilt
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Halcon/Algorithms/PatternMatchService.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [ALIGN-01]

must_haves:
  truths:
    - "Shape 와 NCC 모델을 생성·저장·로드할 수 있다"
    - "모델로 런타임 이미지에서 find 하여 match pose(row,col,angle,score)를 얻는다"
    - "ref pose 와 cur pose 로 rigid transform(x,y) 행렬을 산출한다"
    - "모델 로드/매칭 실패 시 throw 없이 false + identity 반환(lenient)"
  artifacts:
    - path: "WPF_Example/Halcon/Algorithms/PatternMatchService.cs"
      provides: "HALCON shape/ncc 매칭 + rigid transform 산출 서비스"
      contains: "TryCreateModel"
      min_lines: 120
  key_links:
    - from: "PatternMatchService.TryFindPose"
      to: "find_shape_model / find_ncc_model"
      via: "engine 분기"
      pattern: "FindShapeModel|FindNccModel"
    - from: "PatternMatchService.TryBuildAlignRigid"
      to: "vector_angle_to_rigid"
      via: "ref pose - cur pose"
      pattern: "VectorAngleToRigid|HomMat2dTranslate"
---

<objective>
신규 PatternMatchService(HALCON shape/ncc 매칭 — 코드베이스 매칭 greenfield)를 작성한다. 모델 생성/저장/로드(create/write/read_{shape,ncc}_model), 검색영역 제한(reduce_domain) + 다운샘플 coarse(zoom_image_factor) find, ref/cur pose → rigid transform(vector_angle_to_rigid 또는 수동 translate). 전 메서드 try/catch(return false) + HObject/HImage dispose 규약 준수. DatumFindingService/VisionAlgorithmService 의 구조·규약만 복사, 매칭 본문은 D-01/D-06/§3 설계대로 신규.

Purpose: Wave 2 통합단계가 호출할 매칭 엔진. coarse x,y 전용(정밀 θ는 line-fit 이 담당).
Output: PatternMatchService.cs 신규 + csproj Compile 등록.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-datum-align-01-x-y-tilt/54-CONTEXT.md
@.planning/phases/54-datum-align-01-x-y-tilt/54-PATTERNS.md
@.planning/ALIGN-01-pattern-align-analysis.md

LOCKED: D-01(Shape/NCC 엔진), D-01b(매칭=coarse x,y 전용), D-05(입력=보정 전 원본 grab), D-06(reduce_domain template ROI ± margin), D-06a(다운샘플 coarse), D-09(ref pose 기반 변위).
Claude's Discretion: vector_angle_to_rigid vs 수동 translate+rotate 합성(§3 권고=vector_angle_to_rigid), 다운샘플 비율 기본값, NumLevels/Greediness, 메서드 시그니처/내부 구조.

<interfaces>
복사 대상 규약 (from DatumFindingService.cs / VisionAlgorithmService.cs):
- namespace ReringProject.Halcon.Algorithms
- public class PatternMatchService — try { HOperatorSet.* } catch (Exception ex) { error = ex.Message; ...; return false; } finally { if (x != null) { try { x.Dispose(); } catch { } } }
- HImage 단명: using (var image = new HImage(...))
- rigid 산출 형식 (DatumFindingService.cs:185-193):
  HOperatorSet.HomMat2dIdentity(out mat);
  HOperatorSet.HomMat2dTranslate(mat, dRow, dCol, out mat);
  HOperatorSet.HomMat2dRotate(mat, dAngle, curRow.D, curCol.D, out transform);
- 부호 주의(§5): find_shape_model angle = 반시계+rad. 기존 코드 각도 = Math.Atan2(rEnd-rBegin, cEnd-cBegin).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 모델 생성/저장/로드 + ref pose find (create/write/read + teach find)</name>
  <files>WPF_Example/Halcon/Algorithms/PatternMatchService.cs, WPF_Example/DatumMeasurement.csproj</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs (구조/주석 규약 L5-13, try/catch+finally dispose L18-60)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (try/catch(return false) + error out L214-218, detected pose write-back 미러 L196-210)
    - WPF_Example/Halcon/Algorithms/MeasurementAlgorithm.cs (using(var image = new HImage) 단명 dispose L21-31)
    - .planning/ALIGN-01-pattern-align-analysis.md (§3 엔진/산출, §8 하이브리드)
  </read_first>
  <action>
PatternMatchService.cs 를 namespace ReringProject.Halcon.Algorithms 에 신규 작성. 파일 전체 `//260618 hbk Phase 54 ALIGN-01`. Allman 브레이스(신규 Halcon 코드 규약). C# 7.2. 전 HOperatorSet 호출 try/catch(return false) + HObject/HImage finally dispose.

메서드 1 — 모델 생성+저장 (engine 분기):
```csharp
// template ROI(Rect2)로 reduce_domain 한 영역에서 모델 생성 후 engine 별 파일 저장.
// engine "NCC" → create_ncc_model/write_ncc_model, 그 외 → create_shape_model/write_shape_model.
// angleExtentDeg = 0 → angle off (작은 range, D-01b). modelPath = 54-02 GetPatternModelFilePath 결과(호출부 전달).
public bool TryCreateModel(HImage templateImage, double roiRow, double roiCol, double roiPhi,
    double roiLen1, double roiLen2, string engine, double angleExtentDeg, string modelPath, out string error)
```
구현 핵심:
- gen_rectangle2(roiRow, roiCol, roiPhi, roiLen1, roiLen2) → reduce_domain(templateImage, rect) → reducedImage.
- Shape: create_shape_model(reducedImage, NumLevels='auto', AngleStart=-rad(angleExtentDeg/2), AngleExtent=rad(angleExtentDeg), ...defaults Claude's Discretion) → write_shape_model(modelId, modelPath).
- NCC: create_ncc_model(reducedImage, 'auto', -rad(angleExtentDeg/2), rad(angleExtentDeg), 'auto', 'use_polarity') → write_ncc_model(modelId, modelPath).
- 모델 핸들(HTuple/HShapeModel/long)·HObject(rect/reducedImage) dispose/clear. shape/ncc 모델은 clear_shape_model/clear_ncc_model.

메서드 2 — ref pose find (티칭 시 1회, D-09):
```csharp
// 방금 생성한 모델로 templateImage 자체에서 find → ref pose 반환. (런타임과 동일 연산 → 부호 일관성, D-09)
public bool TryFindRefPose(HImage templateImage, string engine, string modelPath, double minScore,
    out double refRow, out double refCol, out double refAngleDeg, out double refScore, out string error)
```
- read_{shape,ncc}_model(modelPath) → find_{shape,ncc}_model(templateImage, ...) → 최고 score match 의 Row/Col/Angle(rad)/Score. refAngleDeg = HTuple angle * 180/PI. NumMatches=1. 실패(빈 결과/score<minScore) → false + out 0.

csproj: PatternMatchService.cs 를 Compile ItemGroup 에 등록(CS0246 차단, Phase 12 D-12 선례).
  </action>
  <verify>
    <automated>grep -n "TryCreateModel\|TryFindRefPose\|CreateShapeModel\|CreateNccModel\|ReduceDomain" WPF_Example/Halcon/Algorithms/PatternMatchService.cs</automated>
  </verify>
  <acceptance_criteria>
    - PatternMatchService.cs starts with `namespace ReringProject.Halcon.Algorithms`
    - contains `public class PatternMatchService`
    - contains method `public bool TryCreateModel(`
    - contains method `public bool TryFindRefPose(`
    - body contains both `HOperatorSet.CreateShapeModel` and `HOperatorSet.CreateNccModel`
    - body contains both `HOperatorSet.WriteShapeModel` and `HOperatorSet.WriteNccModel`
    - body contains `HOperatorSet.ReduceDomain` and `HOperatorSet.GenRectangle2`
    - every HOperatorSet call group wrapped in try/catch with `return false` on catch
    - DatumMeasurement.csproj contains `PatternMatchService.cs` in a Compile element
  </acceptance_criteria>
  <done>Shape/NCC 모델 생성+저장+ref find 동작; 실패 시 throw 없이 false; 모델/HObject dispose.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 런타임 coarse find(다운샘플) + rigid transform 산출</name>
  <files>WPF_Example/Halcon/Algorithms/PatternMatchService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/PatternMatchService.cs (Task 1 결과 — 동일 파일 누적)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (hom_mat2d 빌드 L185-193, 각도 부호 Atan2 L181-183/648)
    - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs (AffineTransPoint2d 좌표변환 채널 L45-60 — 산출 transform 의 소비처 확인용)
  </read_first>
  <action>
PatternMatchService.cs 에 메서드 2개 추가. 동일 규약.

메서드 3 — 런타임 coarse find (D-06/D-06a, x,y 전용):
```csharp
// 모델 로드 후 검색영역(template ROI ± marginPx)으로 reduce_domain, 다운샘플(downsampleFactor)에서 coarse find → x,y 획득 후 스케일 복원.
// minScore 미달 → false (호출부가 MarkDatumFailed, D-10). angle 은 거칠어 측정에 쓰지 않음(정밀 θ는 line-fit, D-01b) — out 으로 반환만.
public bool TryFindPose(HImage runtimeImage, string engine, string modelPath,
    double roiRow, double roiCol, double roiLen1, double roiLen2, double marginPx,
    double minScore, double downsampleFactor,
    out double curRow, out double curCol, out double curAngleDeg, out double curScore, out string error)
```
구현 핵심:
- 검색영역: gen_rectangle1/2 중심(roiRow,roiCol), len1+margin, len2+margin → reduce_domain(runtimeImage).
- 다운샘플: downsampleFactor>1 이면 zoom_image_factor(reduced, 1/factor, 1/factor) 또는 피라미드 상위 레벨에서 find → 결과 Row/Col 에 factor 곱하여 원본 스케일 복원(Claude's Discretion: zoom 방식). downsampleFactor<=1 → 원본 find.
- find_{shape,ncc}_model(NumMatches=1, MinScore=minScore, Greediness 높게). 빈 결과 또는 score<minScore → false + identity 의도(out 0).
- read_{shape,ncc}_model 핸들·HObject 전부 dispose/clear.

메서드 4 — rigid transform 산출 (D-02 ③, §3):
```csharp
// ref pose - cur pose 변위로 rigid 행렬 산출. theta 는 line-fit 으로 따로 주입되므로 이 행렬은 x,y(+선택 angle) translate 중심.
// thetaRad 인자 = line-fit 정밀 θ (호출부가 별도 산출 후 전달). vector_angle_to_rigid 또는 수동 translate+rotate (Discretion).
public bool TryBuildAlignRigid(double refRow, double refCol, double refAngleDeg,
    double curRow, double curCol, double thetaRad, out HTuple transform, out string error)
```
구현 핵심:
- §3 권고 = vector_angle_to_rigid(refRow, refCol, refAngleRad, curRow, curCol, curAngleRad) → out transform. 단 angle 은 line-fit thetaRad 사용(매칭 angle 은 거침). 또는 수동: HomMat2dIdentity → HomMat2dTranslate(dRow=refRow-curRow, dCol=refCol-curCol) → HomMat2dRotate(thetaRad, pivot).
- 부호 캘리브: ref − cur 방향(D-09). catch → HomMat2dIdentity(out transform) + return false (lenient identity 폴백, §5 부호 SIMUL 검증 필수 — SUMMARY 에 부호 규약 명시).
  </action>
  <verify>
    <automated>grep -n "TryFindPose\|TryBuildAlignRigid\|ZoomImageFactor\|FindShapeModel\|FindNccModel\|HomMat2d" WPF_Example/Halcon/Algorithms/PatternMatchService.cs</automated>
  </verify>
  <acceptance_criteria>
    - PatternMatchService.cs contains method `public bool TryFindPose(`
    - PatternMatchService.cs contains method `public bool TryBuildAlignRigid(`
    - TryFindPose body contains both `HOperatorSet.FindShapeModel` and `HOperatorSet.FindNccModel`
    - TryFindPose body contains `HOperatorSet.ReduceDomain` (검색영역 제한)
    - TryFindPose body contains downsample call (`HOperatorSet.ZoomImageFactor` or pyramid level handling) gated by downsampleFactor
    - TryBuildAlignRigid body contains `HOperatorSet.VectorAngleToRigid` OR (`HOperatorSet.HomMat2dTranslate` and `HOperatorSet.HomMat2dRotate`)
    - TryBuildAlignRigid catch branch contains `HOperatorSet.HomMat2dIdentity(out transform)` (lenient identity 폴백)
    - every HOperatorSet call group wrapped in try/catch with `return false`
  </acceptance_criteria>
  <done>런타임 coarse find(검색영역 제한+다운샘플) → x,y; minScore 미달 false; ref-cur+θ rigid 산출; 실패 시 identity false.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 디스크 모델 파일(.shm/.ncm) → read_*_model | 레시피 폴더의 모델 파일이 in-memory 매칭으로 로드됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-05 | Tampering | read_shape_model/read_ncc_model 손상 파일 | mitigate | read_*_model 호출을 try/catch 로 래핑, 실패 시 false + identity → 호출부 MarkDatumFailed(D-10). 손상/누락 모델이 throw 로 시퀀스 abort 하지 않음 |
| T-54-06 | Denial of Service | 전체 이미지 find 152MP tact | mitigate | reduce_domain(template ROI ± margin) + 다운샘플 coarse(D-06/D-06a)로 검색 비용 제한 — 전체 이미지 find 금지 |
</threat_model>

<verification>
- msbuild Debug/x64 빌드 PASS (신규 error 0, csproj 등록 확인)
- 손상/누락 모델 경로 → TryFindPose false + out 0 (throw 없음, lenient)
- SIMUL: 동일 이미지 ref find vs 평행이동 이미지 find → curRow/Col 차이 = 이동량 (부호 규약 SUMMARY 기록)
</verification>

<success_criteria>
- Shape/NCC create/write/read/find 동작 + reduce_domain 검색영역 제한 + 다운샘플 coarse
- rigid transform 산출(ref-cur 변위 + line-fit θ)
- 전 메서드 try/catch(return false) + dispose 규약 + lenient identity 폴백
</success_criteria>

<output>
완료 후 `.planning/phases/54-datum-align-01-x-y-tilt/54-03-SUMMARY.md` 생성 (부호/좌표계 규약 명시).
</output>
