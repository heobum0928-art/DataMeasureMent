---
phase: 54-datum-align-01-x-y-tilt
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: true
requirements: [ALIGN-01]

must_haves:
  truths:
    - "IsPatternAlignEnabled=true Datum 은 보정 전 원본 grab 이미지로 매칭하여 x,y(curRow,curCol)를 얻는다"
    - "(curRow-RefMatchRow, curCol-RefMatchCol) 만큼 이동한 line-fit ROI 에서 정밀 θ 를 얻어 (x,y+θ) rigid 를 _datumTransforms[DatumName] 에 합성한다"
    - "측정은 ROI 좌표변환으로 자동 추종한다(Measure 본문/라우팅 무수정)"
    - "매칭 실패 Datum 은 시퀀스 abort 없이 측정 NG(LastSkipReason=ALIGN_FAIL)로 강제된다(D-10)"
    - "IsPatternAlignEnabled=false → align=identity → 측정 byte-identical (off 회귀 0)"
    - "RotateImageByAngle 호출이 파일 전체에서 0회로 폐기된다(D-03 측정=원본 픽셀, D-05 warp 0회)"
    - "모델 경로 키는 ResolveDatumModelPath(datum) 단일 헬퍼로 도출되어 54-04(load)/54-05(save) 가 동일 경로를 보장한다(D-07)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "TryComposeAlign + ResolveDatumModelPath(공유 키 헬퍼) + _alignFailedDatums/MarkAlignFailed/IsAlignFailed"
      contains: "TryComposeAlign"
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "TryGetLevelingAngle (dRow,dCol) offset 오버로드 — line-fit ROI 이동 후 θ"
      contains: "double dRow, double dCol"
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "DatumPhase 매칭 삽입(원본 이미지) + ALIGN_FAIL 게이트 + RotateImageByAngle 전면 폐기(L151/153/184/239)"
      contains: "ALIGN_FAIL"
  key_links:
    - from: "Action_FAIMeasurement.DatumPhase 1-image 분기"
      to: "parentSeq.TryComposeAlign"
      via: "IsPatternAlignEnabled 가드 + 보정 전 원본 grab 이미지(img) + ResolveDatumModelPath 경로"
      pattern: "IsPatternAlignEnabled"
    - from: "TryComposeAlign 이동 line-fit"
      to: "DatumFindingService.TryGetLevelingAngle(refImage, datum, dRow, dCol, ...)"
      via: "(curRow-RefMatchRow, curCol-RefMatchCol) 이동"
      pattern: "dRow, dCol"
    - from: "align rigid"
      to: "_datumTransforms[DatumName]"
      via: "HomMat2dCompose"
      pattern: "HomMat2dCompose"
    - from: "매칭 실패 Datum"
      to: "측정 NG (LastSkipReason ALIGN_FAIL)"
      via: "MarkAlignFailed + IsAlignFailed 게이트 분기"
      pattern: "ALIGN_FAIL"
    - from: "ResolveDatumModelPath"
      to: "Recipes.GetPatternModelFilePath"
      via: "SourceShotName→ShotConfig.OwnerSequenceName + 결정적 actName 상수"
      pattern: "ResolveDatumModelPath"
---

<objective>
DatumPhase per-datum 루프에 패턴매칭 보정을 통합한다(D-02 흐름): IsPatternAlignEnabled 가드 하에 ① 보정 전 원본 grab 이미지로 PatternMatchService.TryFindPose → x,y(curRow,curCol), ② (curRow-RefMatchRow, curCol-RefMatchCol) 만큼 이동한 line-fit ROI 에서 TryGetLevelingAngle(offset 오버로드)로 정밀 θ, ③ TryBuildAlignRigid 로 (x,y+θ) rigid, ④ _datumTransforms[DatumName] 에 HomMat2dCompose 합성. 매칭 실패 = lenient(MarkAlignFailed + 측정 LastSkipReason=ALIGN_FAIL NG, D-10). RotateImageByAngle 호출을 파일 전체에서 폐기(L151/153/184 datum 검출 회전 + L239 측정 이미지 회전 — D-03 측정=원본 픽셀, D-05 warp 0회). off → identity(byte-identical, D-11). 모델 경로는 ResolveDatumModelPath(datum) 단일 헬퍼로 도출(54-05 와 동일 키 → D-07 결정성).

Purpose: 보정 채널의 실제 결선. 측정 라우팅(meas.DatumRef → TryGetDatumTransform → AffineTransPoint2d)은 무수정으로 자동 추종.
Output: InspectionSequence.cs(TryComposeAlign + ResolveDatumModelPath + align-fail set) + DatumFindingService.cs(offset 오버로드) + Action_FAIMeasurement.cs(매칭 삽입 + ALIGN_FAIL 게이트 + RotateImageByAngle 전면 폐기).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-datum-align-01-x-y-tilt/54-CONTEXT.md
@.planning/phases/54-datum-align-01-x-y-tilt/54-PATTERNS.md
@.planning/phases/54-datum-align-01-x-y-tilt/54-01-SUMMARY.md
@.planning/phases/54-datum-align-01-x-y-tilt/54-02-SUMMARY.md
@.planning/phases/54-datum-align-01-x-y-tilt/54-03-SUMMARY.md

LOCKED: D-02(하이브리드 흐름 x,y 매칭 + line-fit θ), D-03(측정=원본 픽셀, ROI 좌표변환, Measure 무수정), D-04(Datum 당 1회, DatumPhase 확장), D-05(보정 전 원본 grab + 이미지회전 폐기 warp 0회), D-07(이름 기반 결정적 경로), D-10(lenient + LastSkipReason=ALIGN_FAIL), D-11(off identity 회귀 0).

NOTE(research): 54 는 RESEARCH/VALIDATION 문서 미생성 — orchestrator 결정으로 의도적 생략(.planning/ALIGN-01-pattern-align-analysis.md 가 설계 근거 대체). Nyquist 는 grep-검증 acceptance_criteria 로 충족.

<verified_accessors>
<!-- 아래는 소스에서 직접 확인된 실제 접근자/시그니처. 추정 금지. -->
- 레시피 인스턴스: SystemHandler.Handle.Recipes  (SystemHandler.cs:43 — 타입 RecipeFiles. 멤버명이 'Recipes'. ※'RecipeFiles' 라는 프로퍼티는 존재하지 않음 — CS1061.)
- 레시피명: SystemHandler.Handle.Setting.CurrentRecipeName  (SystemHandler.cs:31 Setting; SystemSetting.cs:45. ParamBase.cs:117 / InspectionSequence.cs:149 동일 사용 선례.)
- 모델 경로: SystemHandler.Handle.Recipes.GetPatternModelFilePath(recipeName, seqName, actName, propertyName, engine)  (54-02 가 RecipeFiles 클래스에 추가하는 인스턴스 메서드. RecipeFiles.Handle.GetPatternModelFilePath 도 동일 인스턴스 — ParamBase.cs:121 선례.)
- datum→Shot 역추적: SystemHandler.Handle.Sequences.RecipeManager.Shots 에서 ShotName==datum.SourceShotName 매칭 (InspectionListView.cs:866-874 ResolveDatumCameraParam 선례). 미매칭 → Shots[0] 폴백.
- ShotConfig.OwnerSequenceName (ShotConfig.cs:50, 빈값은 "TOP" 폴백 ShotConfig.cs:138-139).
- DatumConfig.SourceShotName (DatumConfig.cs:34) + DatumConfig.DatumName (DatumConfig.cs:19) — 양쪽(load/save) 동일 인스턴스 필드.
- line-fit ROI 필드(TryGetLevelingAngle 내부): config.Horizontal_A_Row/Col/Phi/Length1/Length2 + config.Horizontal_B_Row/Col/Phi/Length1/Length2 (DatumFindingService.cs:601-624).
- 기존 TryGetLevelingAngle 시그니처: public bool TryGetLevelingAngle(HImage image, DatumConfig config, out double angleRad, out string error) (DatumFindingService.cs:586). 내부 angleRad = Math.Atan2(hrE.D-hrB.D, hcE.D-hcB.D) (L648).
- RotateImageByAngle 호출 4곳: Action_FAIMeasurement.cs L151/L153(DualImage), L184(1-image datum), L239(EStep.Grab 측정 이미지).
</verified_accessors>

<key_convention>
<!-- 54-04(런타임 load) 와 54-05(티칭 save) 가 IDENTICAL 키로 모델 경로를 도출해야 saved path == loaded path (D-07 결정성). -->
<!-- ★해결책: 단일 공유 헬퍼 InspectionSequence.ResolveDatumModelPath(DatumConfig) 를 Task 1 에서 추가하고, 54-04(런타임)·54-05(티칭) 가 모두 이 한 메서드만 호출한다. 키 도출 로직이 한 곳에만 존재 → 불일치 구조적으로 불가. -->
<!-- 런타임의 this.Name(ActionBase) 은 동적 attach 로 datum 컨텍스트와 어긋날 수 있어 키 소스로 부적합 → datum 자기완결 필드(SourceShotName + DatumName)만 사용. -->
public static string ResolveDatumModelPath(DatumConfig datum)
{
    recipeName   = SystemHandler.Handle.Setting.CurrentRecipeName ?? "";
    seqName      = (datum.SourceShotName 으로 Shots 매칭한 ShotConfig.OwnerSequenceName, 빈값/미매칭 → "TOP");
    actName      = "Datum";                  // 결정적 상수 — datum 모델 전용 폴더 (propertyName=DatumName 이 유일성 보장)
    propertyName = datum.DatumName ?? "";    // 양쪽 동일 (D-07 이름 기반)
    engine       = datum.PatternEngine;      // 양쪽 동일 (.shm/.ncm 분기)
    return SystemHandler.Handle.Recipes.GetPatternModelFilePath(recipeName, seqName, actName, propertyName, engine);
}
→ 54-05 Task 1 은 직접 경로를 도출하지 않고 InspectionSequence.ResolveDatumModelPath(_editingDatum) 를 호출한다(동일 헬퍼 강제).
</key_convention>

<interfaces>
54-03 PatternMatchService (소비): TryFindPose(runtimeImage, engine, modelPath, roiRow, roiCol, roiLen1, roiLen2, marginPx, minScore, downsampleFactor, out curRow, out curCol, out curAngleDeg, out curScore, out error); TryBuildAlignRigid(refRow, refCol, refAngleDeg, curRow, curCol, thetaRad, out transform, out error).
54-02 RecipeFiles (소비): GetPatternModelFilePath(recipeName, seqName, actName, propertyName=datumName, engine).
기존 InspectionSequence: _datumTransforms[datumKey] 누적 (L395); TryGetDatumTransform (L400); MarkDatumFailed/IsDatumFailed (L363-373); ClearDatumTransforms (L348-359) — _datumTransforms.Clear()+_failedDatums.Clear()+RuntimeDetectFailed 리셋+ResetLeveling(); TryGetLevelingAngle line-fit θ 소스(DatumFindingService).
기존 Action_FAIMeasurement DatumPhase 루프 (L122-207): ClearDatumTransforms → datumLevelOn/datumLevelAngle 산출(L129-130) → foreach datum → (1-image L170-201: GrabOrLoadDatumImage(datum) → datumLevelOn 시 RotateImageByAngle(L184) → TryRunSingleDatum → finally Dispose). DualImage 분기 L133-169 (RotateImageByAngle L151/153). EStep.Grab 측정 이미지 회전 L234-244 (RotateImageByAngle L239). 측정 게이트 L280-293: IsDatumFailed → ClearResult + LastSkipReason="DATUM_FAIL" + continue. transform 적용 L294-302 무수정.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DatumFindingService offset 오버로드 + InspectionSequence (ResolveDatumModelPath + TryComposeAlign + align-fail set)</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs, WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (수정 대상 — TryGetLevelingAngle L586-661: TryExtractEdgePoints(image, w, h, config.Horizontal_A_Row, config.Horizontal_A_Col, ...) 2회 + FitLineContourXld + Atan2. ※offset 오버로드는 동일 본문에 ROI Row/Col 에만 dRow/dCol 가산.)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (수정 대상 — _datumTransforms 누적 L375-397, TryGetDatumTransform L400-406, ClearDatumTransforms L348-359, MarkDatumFailed/IsDatumFailed L361-373, _failedDatums 필드 선언부)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (RefMatchRow/Col/AngleDeg / PatternEngine / PatternRoi_* / PatternMinScore / PatternSearchMarginPx / IsPatternAlignEnabled / SourceShotName / DatumName — 54-01 결과)
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs (OwnerSequenceName L50, ShotName, "TOP" 폴백 L138-139)
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs (ResolveDatumCameraParam L866-874 — SourceShotName→ShotConfig 역추적 선례)
  </read_first>
  <action>
**(A) DatumFindingService.cs — TryGetLevelingAngle offset 오버로드 추가 (D-02 step② line-fit ROI 이동).**
기존 TryGetLevelingAngle(image, config, out angleRad, out error) (L586-661) 는 무수정. 그 인근에 dRow/dCol 을 받아 line-fit ROI 중심을 이동시키는 오버로드를 신규 추가한다. 본문은 기존과 동일하되 TryExtractEdgePoints 호출의 ROI Row/Col 인자에 dRow/dCol 을 가산한다(Phi/Length/edge 파라미터는 그대로). `//260618 hbk Phase 54 ALIGN-01` 주석. Allman(이 파일 스타일). HOperatorSet try/catch.
```csharp
//260618 hbk Phase 54 ALIGN-01 line-fit ROI 이동 오버로드 (D-02 ② — 큰 X,Y 변위 시 고정 티칭 ROI 가 에지 이탈하는 문제 해소).
//  dRow,dCol = 매칭 변위(curRow-RefMatchRow, curCol-RefMatchCol). ROI 중심만 이동, Phi/Length/edge 파라미터 무변경.
//  dRow=dCol=0 이면 기존 오버로드와 동일 결과(회귀 0).
public bool TryGetLevelingAngle(HImage image, DatumConfig config, double dRow, double dCol, out double angleRad, out string error)
{
    angleRad = 0.0;
    error = null;
    HObject contour = null;
    try
    {
        HTuple imageWidth, imageHeight;
        image.GetImageSize(out imageWidth, out imageHeight);

        HTuple rowEdgeA, colEdgeA;
        string edgeErrorA;
        if (!TryExtractEdgePoints(
                image, imageWidth, imageHeight,
                config.Horizontal_A_Row + dRow, config.Horizontal_A_Col + dCol, config.Horizontal_A_Phi,
                config.Horizontal_A_Length1, config.Horizontal_A_Length2,
                config.Horizontal_A_Sigma, config.Horizontal_A_EdgeThreshold, config.Horizontal_A_EdgePolarity,
                config.Horizontal_A_EdgeDirection, config.Horizontal_A_EdgeSelection,
                config.Horizontal_A_EdgeSampleCount, config.Horizontal_A_EdgeTrimCount,
                out rowEdgeA, out colEdgeA, out edgeErrorA, "Horizontal_A"))
        {
            error = "Horizontal_A: " + edgeErrorA; return false;
        }
        HTuple rowEdgeB, colEdgeB;
        string edgeErrorB;
        if (!TryExtractEdgePoints(
                image, imageWidth, imageHeight,
                config.Horizontal_B_Row + dRow, config.Horizontal_B_Col + dCol, config.Horizontal_B_Phi,
                config.Horizontal_B_Length1, config.Horizontal_B_Length2,
                config.Horizontal_B_Sigma, config.Horizontal_B_EdgeThreshold, config.Horizontal_B_EdgePolarity,
                config.Horizontal_B_EdgeDirection, config.Horizontal_B_EdgeSelection,
                config.Horizontal_B_EdgeSampleCount, config.Horizontal_B_EdgeTrimCount,
                out rowEdgeB, out colEdgeB, out edgeErrorB, "Horizontal_B"))
        {
            error = "Horizontal_B: " + edgeErrorB; return false;
        }
        int totalEdges = rowEdgeA.TupleLength() + rowEdgeB.TupleLength();
        if (totalEdges < MIN_HORIZONTAL_EDGES)
        {
            error = "Leveling line fit failed: insufficient edges (" + totalEdges + ")"; return false;
        }
        HTuple allRows = rowEdgeA.TupleConcat(rowEdgeB);
        HTuple allCols = colEdgeA.TupleConcat(colEdgeB);
        HOperatorSet.GenContourPolygonXld(out contour, allRows, allCols);
        HTuple hrB, hcB, hrE, hcE, nr, nc, df;
        HOperatorSet.FitLineContourXld(contour, "tukey", -1, 0, 5, 2, out hrB, out hcB, out hrE, out hcE, out nr, out nc, out df);
        angleRad = Math.Atan2(hrE.D - hrB.D, hcE.D - hcB.D);
        return true;
    }
    catch (Exception ex) { error = ex.Message; angleRad = 0.0; return false; }
    finally { if (contour != null) { try { contour.Dispose(); } catch { } } }
}
```
(memory feedback_halcon_measurepos_must_haves 는 TryExtractEdgePoints 내부 strip-loop/measurePhi/EdgeSelection 에 이미 구현 — 무수정 재사용.)

**(B) InspectionSequence.cs — ResolveDatumModelPath 공유 키 헬퍼 (D-07).**
54-04/54-05 가 공유할 단일 모델 경로 도출 메서드 추가. <key_convention> 그대로 구현. `//260618 hbk Phase 54 ALIGN-01` 주석. Allman. System.Linq 도입 회피(for-loop).
```csharp
//260618 hbk Phase 54 ALIGN-01 datum 모델 경로 단일 소스 (D-07) — 54-04(런타임 load)·54-05(티칭 save) 공유. 키 도출 로직 단일화 → 경로 불일치 구조적 차단.
public static string ResolveDatumModelPath(DatumConfig datum)
{
    if (datum == null) return null;
    string recipeName = SystemHandler.Handle.Setting.CurrentRecipeName;
    if (recipeName == null) recipeName = "";
    // datum.SourceShotName 으로 ShotConfig 역추적 → OwnerSequenceName (InspectionListView.ResolveDatumCameraParam 선례)
    string seqName = "TOP"; // 미매칭/빈값 폴백 (ShotConfig 정책 일치)
    var shots = SystemHandler.Handle.Sequences.RecipeManager.Shots;
    if (shots != null && shots.Count > 0)
    {
        ShotConfig matched = null;
        if (!string.IsNullOrEmpty(datum.SourceShotName))
        {
            foreach (var s in shots) { if (s != null && s.ShotName == datum.SourceShotName) { matched = s; break; } }
        }
        if (matched == null) matched = shots[0];
        if (matched != null && !string.IsNullOrEmpty(matched.OwnerSequenceName)) seqName = matched.OwnerSequenceName;
    }
    string actName = "Datum"; // 결정적 상수 — propertyName=DatumName 이 유일성 보장
    string propertyName = datum.DatumName; if (propertyName == null) propertyName = "";
    return SystemHandler.Handle.Recipes.GetPatternModelFilePath(recipeName, seqName, actName, propertyName, datum.PatternEngine);
}
```

**(C) InspectionSequence.cs — align-fail set + MarkAlignFailed/IsAlignFailed.**
_failedDatums 선언 인근에 align 전용 실패 set 을 추가하고(게이트가 ALIGN_FAIL vs DATUM_FAIL 구분, D-10), ClearDatumTransforms 에 Clear 1줄 추가. `//260618 hbk Phase 54 ALIGN-01` 주석. Allman.
```csharp
//260618 hbk Phase 54 ALIGN-01 패턴매칭(align) 실패 datum set — 검출 실패(_failedDatums)와 구분하여 측정 게이트가 LastSkipReason=ALIGN_FAIL 표기 (D-10).
private readonly System.Collections.Generic.HashSet<string> _alignFailedDatums = new System.Collections.Generic.HashSet<string>();

//260618 hbk Phase 54 ALIGN-01
public void MarkAlignFailed(string datumName)
{
    if (!string.IsNullOrEmpty(datumName)) { _alignFailedDatums.Add(datumName); MarkDatumFailed(datumName); } // 측정 NG 강제 위해 기존 게이트 set 에도 add
}

//260618 hbk Phase 54 ALIGN-01 — 게이트가 ALIGN_FAIL 표기할지 판정
public bool IsAlignFailed(string datumRef)
{
    return !string.IsNullOrEmpty(datumRef) && _alignFailedDatums.Contains(datumRef);
}
```
ClearDatumTransforms(L348-359) 에 `_alignFailedDatums.Clear();` 1줄 추가(_failedDatums.Clear() 인근, 동일 lifecycle 주석).

**(D) InspectionSequence.cs — TryComposeAlign (D-02 ②③④).**
align rigid 산출 + 합성 저장 메서드 신규. `//260618 hbk Phase 54 ALIGN-01` 주석. Allman. HOperatorSet try/catch.
```csharp
//260618 hbk Phase 54 ALIGN-01 패턴매칭 보정 합성 (D-02 ①②③④).
//  refImage = 보정 전 원본 grab 이미지(D-05). modelPath = 호출부가 ResolveDatumModelPath 로 산출 전달.
//  ① 원본 매칭 → curRow,curCol  ② (curRow-RefMatchRow,curCol-RefMatchCol) 이동 line-fit θ  ③ rigid  ④ _datumTransforms 합성.
//  합성 순서: HomMat2dCompose(alignRigid, existing) = "기존 검출 transform 을 먼저 적용 후 align 보정" (right-to-left 적용 규약).
//   ※TryComposeAlign 은 DatumPhase 에서 TryRunSingleDatum(검출) 호출 없이 align 단독 경로로 호출됨(Task 2 (2) 참조) → 통상 existing 없음 → alignRigid 단독 저장. existing 존재 시(혼용)만 1회 합성 — align 1회 적용 보장(W4).
//  실패(매칭 score 미달/모델 로드 실패/rigid 실패) → false (호출부가 MarkAlignFailed, lenient D-10). _datumTransforms 미변경.
public bool TryComposeAlign(DatumConfig datum, HImage refImage, string modelPath, out string error)
{
    error = null;
    if (datum == null) { error = "datum null"; return false; }
    if (refImage == null) { error = "refImage null"; return false; }
    var svc = new ReringProject.Halcon.Algorithms.PatternMatchService();
    double curRow, curCol, curAngleDeg, curScore;
    // ① 매칭 (보정 전 원본) → x,y
    if (!svc.TryFindPose(refImage, datum.PatternEngine, modelPath,
            datum.PatternRoi_Row, datum.PatternRoi_Col, datum.PatternRoi_Length1, datum.PatternRoi_Length2,
            datum.PatternSearchMarginPx, datum.PatternMinScore, /*downsampleFactor*/ 2.0,
            out curRow, out curCol, out curAngleDeg, out curScore, out error))
    {
        return false; // ALIGN_FAIL — 호출부 MarkAlignFailed
    }
    // ② line-fit ROI 를 매칭 변위만큼 이동 후 정밀 θ (D-02 ②). dRow/dCol = cur - ref.
    double dRow = curRow - datum.RefMatchRow;
    double dCol = curCol - datum.RefMatchCol;
    double thetaRad = 0.0;
    var dfs = new ReringProject.Halcon.Algorithms.DatumFindingService();
    string thErr;
    if (!dfs.TryGetLevelingAngle(refImage, datum, dRow, dCol, out thetaRad, out thErr))
    {
        thetaRad = 0.0; // line-fit 실패 → θ=0 폴백 (x,y 보정 유지, lenient)
        string te = thErr; if (te == null) te = "";
        Logging.PrintLog((int)ELogType.Trace, "[ALIGN] Datum '" + (datum.DatumName ?? "") + "' line-fit θ 실패 → θ=0 폴백: " + te);
    }
    // ③ rigid 산출
    HTuple alignRigid;
    if (!svc.TryBuildAlignRigid(datum.RefMatchRow, datum.RefMatchCol, datum.RefMatchAngleDeg,
            curRow, curCol, thetaRad, out alignRigid, out error))
    {
        return false;
    }
    // ④ 기존 검출 transform(있으면)과 1회 합성 — 없으면 alignRigid 단독 저장
    string datumKey = datum.DatumName; if (datumKey == null) datumKey = "";
    HTuple existing;
    HTuple composed;
    if (_datumTransforms.TryGetValue(datumKey, out existing) && existing != null && existing.Length > 0)
    {
        try { HOperatorSet.HomMat2dCompose(alignRigid, existing, out composed); }
        catch (Exception ex) { error = ex.Message; return false; }
    }
    else
    {
        composed = alignRigid;
    }
    _datumTransforms[datumKey] = composed;
    datum.LastFindSucceeded = true;
    return true;
}
```
주의: dRow/dCol 부호(cur-ref) + alignRigid 합성 순서 + TryBuildAlignRigid 내부 부호(54-03)는 §5 중리스크 — SIMUL 평행이동 페어로 방향 검증 필수. SUMMARY 에 "dRow=curRow-RefMatchRow, 합성=HomMat2dCompose(alignRigid, existing), TryComposeAlign 은 align 단독 경로(검출 미수행) → align 1회 적용" 규약 + SIMUL 부호 검증 결과(W3) 명시.
  </action>
  <verify>
    <automated>grep -n "double dRow, double dCol" WPF_Example/Halcon/Algorithms/DatumFindingService.cs; grep -n "ResolveDatumModelPath\|TryComposeAlign\|HomMat2dCompose\|TryFindPose\|TryBuildAlignRigid\|MarkAlignFailed\|IsAlignFailed\|_alignFailedDatums" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</automated>
  </verify>
  <acceptance_criteria>
    - DatumFindingService.cs contains overload `public bool TryGetLevelingAngle(HImage image, DatumConfig config, double dRow, double dCol, out double angleRad, out string error)`
    - overload body adds `dRow` to `config.Horizontal_A_Row` and `config.Horizontal_B_Row`, and `dCol` to `config.Horizontal_A_Col` and `config.Horizontal_B_Col` (grep: `config.Horizontal_A_Row + dRow`, `config.Horizontal_A_Col + dCol`)
    - 기존 4-arg TryGetLevelingAngle (L586) 무수정 보존
    - InspectionSequence.cs contains `public static string ResolveDatumModelPath(DatumConfig datum)` calling `SystemHandler.Handle.Recipes.GetPatternModelFilePath(` with `actName = "Datum"` constant and `datum.DatumName` and `datum.PatternEngine`
    - ResolveDatumModelPath uses `SystemHandler.Handle.Setting.CurrentRecipeName` and resolves seqName via `datum.SourceShotName` → `ShotConfig.OwnerSequenceName`
    - InspectionSequence.cs contains `public bool TryComposeAlign(DatumConfig datum, HImage refImage, string modelPath, out string error)`
    - TryComposeAlign body computes `double dRow = curRow - datum.RefMatchRow;` and `double dCol = curCol - datum.RefMatchCol;`
    - TryComposeAlign body calls `dfs.TryGetLevelingAngle(refImage, datum, dRow, dCol,` (이동 좌표 line-fit 호출 — D-02 ②)
    - TryComposeAlign body calls `svc.TryFindPose(` with `datum.PatternEngine` and `datum.PatternMinScore`
    - TryComposeAlign body calls `svc.TryBuildAlignRigid(` with `datum.RefMatchRow`, `datum.RefMatchCol`
    - TryComposeAlign body contains `HOperatorSet.HomMat2dCompose(alignRigid, existing` wrapped in try/catch with `return false`
    - 매칭/rigid 실패 분기 모두 `return false` (lenient — _datumTransforms 미변경)
    - InspectionSequence.cs contains `_alignFailedDatums`, `public void MarkAlignFailed(`, `public bool IsAlignFailed(`
    - ClearDatumTransforms contains `_alignFailedDatums.Clear();`
    - InspectionSequence.cs 에 `SystemHandler.Handle.RecipeFiles` 문자열 미존재 (grep 0 — Recipes 로 정정 확인)
  </acceptance_criteria>
  <done>offset 오버로드가 이동 ROI 에서 θ 산출; ResolveDatumModelPath 단일 키 헬퍼; TryComposeAlign 가 원본 매칭 → 이동 line-fit θ → rigid → 합성; 실패 시 false + 미변경; align-fail set 으로 게이트 구분 가능.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DatumPhase 매칭 삽입 + ALIGN_FAIL 게이트 + RotateImageByAngle 전면 폐기</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (수정 대상 — DatumPhase per-datum 루프 L122-207; datumLevelOn/datumLevelAngle L129-130; RotateImageByAngle 호출 L151/153(DualImage)/L184(1-image)/L239(EStep.Grab 측정 이미지) — 4곳 전부; 1-image 분기 L170-201; EStep.Grab L209-249 측정 이미지 회전 블록 L234-244; 측정 게이트 L280-293 DATUM_FAIL 패턴; WasDatumSkipped 누적 L408-414 ("DATUM_FAIL" 비교); transform 적용 L294-302 무수정 보존)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (ResolveDatumModelPath + TryComposeAlign + MarkAlignFailed + IsAlignFailed — Task 1 결과)
  </read_first>
  <action>
파일 실제 스타일(K&R) 따름. `//260618 hbk Phase 54 ALIGN-01` 주석.

**1) RotateImageByAngle 전면 폐기 (D-03 측정=원본 픽셀 + D-05 warp 0회) — BLOCKER.**
   `VisionAlgorithmService.RotateImageByAngle` 호출 4곳을 전부 제거한다:
   - L184: 1-image datum 검출 회전 블록(L181-186 `if (datumLevelOn) { ... RotateImageByAngle(img, datumLevelAngle) ... }`) 제거. datum 검출 입력은 보정 전 원본 `img` 그대로.
   - L151/L153: DualImage datum 회전 블록(L148-155 `if (datumLevelOn) { ... imgH/imgV RotateImageByAngle ... }`) 제거.
   - L239: EStep.Grab 측정 이미지 회전 블록(L234-244 `if (rotSeq != null && rotSeq.LevelingEnabled && rotSeq.LevelingComputed) { ... rotated = RotateImageByAngle(image, -rotSeq.LevelingAngleRad) ... image = rotated; }`) 제거 → 측정은 원본 grab `image` 를 그대로 `ShotParam.SetImage(image)` (D-03 측정=원본 픽셀, ROI 좌표변환이 추종).
   - L129-130 datumLevelOn/datumLevelAngle 지역변수는 더 이상 참조되지 않으면 제거. EStep.Grab 의 rotSeq 지역변수도 회전 제거 후 미사용이면 제거.
   주석으로 폐기 사유 명시(레벨링 이미지회전 → 패턴매칭 ROI 좌표변환으로 대체, warp 0회).
   ※ 검증: grep -c "RotateImageByAngle" Action_FAIMeasurement.cs == 0 (무조건 blocker 기준, DatumPhase 한정 아님).

**2) 1-image datum 분기에 align 삽입 (D-02/D-04/D-05).**
   GrabOrLoadDatumImage 직후, img 살아있는 동안(img.Dispose() 전, finally 진입 전), TryRunSingleDatum 호출을 enabled/disabled 분기로 대체한다. enabled → TryComposeAlign(align 단독 경로 — 검출 미수행, align 이중적용 방지 W4), disabled → 기존 TryRunSingleDatum(무수정 보존 → off 회귀 0, D-11). 기존 try/finally(img.Dispose()) 구조 유지, 분기를 try 블록 안에 둔다. modelPath 는 ResolveDatumModelPath 공유 헬퍼로 도출(key_convention):
```csharp
//260618 hbk Phase 54 ALIGN-01 패턴매칭 위치보정 (D-02/D-04/D-05). enabled → align 단독 경로(검출 미수행, 이중적용 방지). disabled → 기존 검출 경로 유지(off 회귀 0, D-11).
if (datum.IsPatternAlignEnabled)
{
    string modelPath = InspectionSequence.ResolveDatumModelPath(datum); // 54-05 티칭과 동일 키 헬퍼 (D-07)
    string alignErr;
    if (!parentSeq.TryComposeAlign(datum, img, modelPath, out alignErr))
    {
        string dn = datum.DatumName; if (dn == null) dn = "";
        string ae = alignErr; if (ae == null) ae = "";
        Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + dn + "' 패턴매칭 실패 (ALIGN_FAIL, skip): " + ae);
        datum.RuntimeDetectFailed = true;
        parentSeq.MarkAlignFailed(datum.DatumName); // D-10 lenient — 측정 NG(ALIGN_FAIL) 강제, abort 안 함
    }
}
else
{
    string derr;
    if (!parentSeq.TryRunSingleDatum(datum, img, null, out derr)) // 기존 검출 경로 무수정
    {
        string datumName = datum.DatumName; if (datumName == null) datumName = "";
        string derrStr = derr; if (derrStr == null) derrStr = "";
        Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + datumName + "' 검출 실패 (skip): " + derrStr);
        datum.RuntimeDetectFailed = true;
        parentSeq.MarkDatumFailed(datum.DatumName);
    }
}
```
   (SIMUL 에서 GrabOrLoadDatumImage 가 TeachingImagePath/SimulImagePath 사용 → modelPath 도 동일 recipe 컨텍스트.)

**3) 측정 게이트 ALIGN_FAIL 사유 표기 (D-10) — L280-293.**
   기존 게이트(L280 `if (parentSeq2 != null && parentSeq2.IsDatumFailed(meas.DatumRef))`)는 그대로 측정 NG 를 강제한다(MarkAlignFailed 가 _failedDatums 에도 add 하므로). 단 LastSkipReason 을 align 실패 datum 은 "ALIGN_FAIL", 검출 실패 datum 은 "DATUM_FAIL" 로 구분 표기한다:
```csharp
if (parentSeq2 != null && parentSeq2.IsDatumFailed(meas.DatumRef))
{
    meas.ClearResult();
    //260618 hbk Phase 54 ALIGN-01 align 실패와 검출 실패 구분 표기 (D-10) — Excel/UI 식별.
    meas.LastSkipReason = parentSeq2.IsAlignFailed(meas.DatumRef) ? "ALIGN_FAIL" : "DATUM_FAIL";
    meas.LastJudgement = false;
    ... (기존 로그/faiAllPass/measuredCount/continue 유지)
}
```
   ※ L412 의 `m.LastSkipReason == "DATUM_FAIL"` (WasDatumSkipped 누적, anyDatumSkip→cycle NotExist) 분기도 ALIGN_FAIL 을 포함하도록 `(m.LastSkipReason == "DATUM_FAIL" || m.LastSkipReason == "ALIGN_FAIL")` 로 확장(skip 통계 누락 방지).

**4) DualImage datum 분기(L133-169)는 1차 out of scope** (Side DualImage 후속 phase, deferred) — IsPatternAlignEnabled 무관하게 align 미삽입, 기존 TryRunSingleDatum 검출 경로 유지(단 회전 블록 L148-155 는 위 (1)에서 제거). 주석으로 deferred 명시.
  </action>
  <verify>
    <automated>echo "RotateImageByAngle count (must be 0):"; grep -c "RotateImageByAngle" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs; grep -n "IsPatternAlignEnabled\|TryComposeAlign\|ResolveDatumModelPath\|MarkAlignFailed\|IsAlignFailed\|ALIGN_FAIL" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs; echo "RecipeFiles property misuse (must be 0):"; grep -c "Handle.RecipeFiles" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RotateImageByAngle" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` returns `0` (BLOCKER — L151/153/184/239 전부 제거, 측정/datum 모두 원본 픽셀)
    - Action_FAIMeasurement.cs contains `if (datum.IsPatternAlignEnabled)` guard in the 1-image datum branch
    - enabled 분기 calls `parentSeq.TryComposeAlign(datum, img,` with the raw grab image
    - modelPath uses `InspectionSequence.ResolveDatumModelPath(datum)` (54-05 티칭과 동일 키 헬퍼, D-07)
    - align 실패 분기 calls `parentSeq.MarkAlignFailed(datum.DatumName)`
    - align 실패 로그 contains string `ALIGN_FAIL`
    - 측정 게이트 sets `LastSkipReason = parentSeq2.IsAlignFailed(meas.DatumRef) ? "ALIGN_FAIL" : "DATUM_FAIL"` (D-10 구분 표기)
    - WasDatumSkipped 누적(L412)이 `"ALIGN_FAIL"` 포함하도록 확장
    - disabled datum 은 기존 `TryRunSingleDatum` 검출 경로 유지 (off identity, D-11)
    - transform 적용 블록(L294-302 TryGetDatumTransform → TryExecute) 무수정 (D-03)
    - `grep -c "Handle.RecipeFiles"` returns `0` (CS1061 방지 — Recipes 로 정정 확인)
  </acceptance_criteria>
  <done>RotateImageByAngle 0회(원본 픽셀 측정); enabled datum 은 원본 이미지 매칭+이동 line-fit → _datumTransforms 합성; 실패 lenient NG(ALIGN_FAIL 표기); disabled/off → 기존 검출 identity 무변경; modelPath = ResolveDatumModelPath 공유 헬퍼.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grab 이미지 → 매칭/측정 보정 | 카메라/시뮬 이미지가 보정 transform 산출에 사용됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-07 | Tampering | 잘못된 매칭 → 양품 오판 | mitigate | D-10 lenient — 매칭 score 미달/실패 시 가짜 transform 안 쓰고 MarkAlignFailed → 해당 Datum 측정 NG(ClearResult+LastJudgement=false+LastSkipReason=ALIGN_FAIL). 양품 오판 0 |
| T-54-08 | Denial of Service | 매칭 예외로 시퀀스 abort | mitigate | TryComposeAlign 실패 = false 반환(throw 없음) → continue. FinishAction(Error) 미호출, 시퀀스 진행 보장 |
| T-54-11 | Tampering | line-fit ROI 미이동 시 큰 변위에서 에지 이탈 → θ 오산 | mitigate | TryGetLevelingAngle offset 오버로드로 (curRow-RefMatchRow,curCol-RefMatchCol) 이동 후 line-fit (D-02 ②). 실패 시 θ=0 폴백(x,y 보정 유지) |
| T-54-12 | Spoofing | load/save 모델 경로 불일치(stale) → 미검증 모델 매칭 | mitigate | ResolveDatumModelPath 단일 헬퍼로 54-04/54-05 동일 키 도출 (D-07). 키 로직 단일화로 불일치 구조적 차단 |
</threat_model>

<verification>
- msbuild Debug/x64 빌드 PASS (신규 error 0; CS1061 'RecipeFiles'/'ActionName' 부재 — Recipes/ResolveDatumModelPath 로 정정 확인)
- grep -c RotateImageByAngle Action_FAIMeasurement.cs == 0 (warp 0회)
- off(IsPatternAlignEnabled=false): 기존 검출 경로 유지 → 측정 byte-identical (회귀 0)
- SIMUL 평행이동 페어: on → 측정 추종 / off → 에지 이탈 대조; 부호(dRow=cur-ref) 방향 검증
- 모델 누락 datum: 시퀀스 abort 0 + 해당 Datum 측정 NG(LastSkipReason=ALIGN_FAIL)
</verification>

<success_criteria>
- RotateImageByAngle 전면 폐기(grep 0, D-03/D-05 warp 0회)
- DatumPhase enabled-가드 매칭(보정 전 원본) + 이동 line-fit θ → _datumTransforms 합성 (D-02/D-04/D-05)
- 매칭 실패 lenient NG + LastSkipReason=ALIGN_FAIL (D-10)
- off/disabled identity 회귀 0 (D-11), 측정 라우팅 무수정 (D-03)
- 접근자 정정(Recipes/Setting.CurrentRecipeName) + ResolveDatumModelPath 단일 키 (D-07) — 빌드 PASS
</success_criteria>

<output>
완료 후 `.planning/phases/54-datum-align-01-x-y-tilt/54-04-SUMMARY.md` 생성 (합성 순서/부호 규약 + SIMUL 부호 검증 결과 + ResolveDatumModelPath 키 규약 명시).
</output>
