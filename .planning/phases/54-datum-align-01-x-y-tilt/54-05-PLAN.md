---
phase: 54-datum-align-01-x-y-tilt
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: false
requirements: [ALIGN-01]

must_haves:
  truths:
    - "Datum 노드에서 패턴 ROI(Rect)를 전용 그리기 모드로 그리면 PatternRoi_* 가 즉시 write-back 된다"
    - "[패턴 모델 생성/저장] 버튼이 모델 파일(.shm/.ncm)을 생성·저장한다"
    - "모델 생성은 PatternRoi_* 가 채워진 경우에만 진행하고 미확보 시 모달로 차단한다(silent 실패 0)"
    - "모델 생성 시 티칭 이미지에서 1회 find 하여 ref pose(RefMatchRow/Col/AngleDeg)를 DatumConfig 에 기록한다"
    - "런타임(54-04)과 동일한 ResolveDatumModelPath 키로 모델을 저장하여 저장-로드 경로가 일치한다(D-07)"
    - "재시작 후에도 모델 파일 + ref pose 로 런타임 매칭이 동작한다"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "패턴 ROI 전용 그리기 모드 + write-back + 모델 생성/저장 + ref pose 기록 핸들러"
      contains: "InvokeCreatePatternModel"
  key_links:
    - from: "패턴 ROI(Rect) 그리기 모드"
      to: "DatumConfig.PatternRoi_*"
      via: "HalconViewer_PatternRectCompleted write-back"
      pattern: "PatternRoi_Row"
    - from: "[모델 생성] 버튼"
      to: "PatternMatchService.TryCreateModel + TryFindRefPose"
      via: "InspectionSequence.ResolveDatumModelPath 경로(54-04 와 동일 키)"
      pattern: "TryCreateModel"
    - from: "ref pose find"
      to: "DatumConfig.RefMatch*"
      via: "write-back"
      pattern: "RefMatchRow ="
---

<objective>
패턴 티칭 UI(D-08 최소 완결형)를 추가한다: Datum 노드에서 패턴 ROI(Rect)를 **전용 그리기 모드**(기존 HalconViewer_DatumRectCompleted 패턴 미러)로 그려 DatumConfig.PatternRoi_Row/Col/Phi/Length1/Length2 에 즉시 write-back + [패턴 모델 생성/저장] 버튼 → (PatternRoi 가드) → PatternMatchService.TryCreateModel(.shm/.ncm 파일 저장) + TryFindRefPose(ref pose 1회 기록, D-09) → DatumConfig.RefMatchRow/Col/AngleDeg write-back. 모델 경로는 54-04 와 **동일한 InspectionSequence.ResolveDatumModelPath(_editingDatum)** 헬퍼로 도출(D-07 저장-로드 경로 일치). 검색영역 별도 그리기·score 미리보기는 후속(deferred).

Purpose: 모델 생성·SIMUL 검증을 가능케 함(없으면 Phase 52 전철 — 백엔드만으론 검증 불가).
Output: MainView.xaml(버튼) + MainView.xaml.cs(패턴 ROI 모드 + 핸들러).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-datum-align-01-x-y-tilt/54-CONTEXT.md
@.planning/phases/54-datum-align-01-x-y-tilt/54-PATTERNS.md
@.planning/phases/54-datum-align-01-x-y-tilt/54-01-SUMMARY.md
@.planning/phases/54-datum-align-01-x-y-tilt/54-02-SUMMARY.md
@.planning/phases/54-datum-align-01-x-y-tilt/54-03-SUMMARY.md
@.planning/phases/54-datum-align-01-x-y-tilt/54-04-SUMMARY.md

LOCKED: D-07(이름 기반 결정적 경로 — 54-04 와 동일 키), D-08(최소 완결형 — ROI 그리기 + 모델 생성/저장 버튼 + ref pose 기록), D-09(티칭 시 find 1회 → ref pose). 검색영역 별도 그리기·score 미리보기·재티칭 미리보기 = deferred(후속).

<verified_accessors>
<!-- 소스 직접 확인. 추정 금지. -->
- 레시피명: SystemHandler.Handle.Setting.CurrentRecipeName  (SystemHandler.cs:31/SystemSetting.cs:45). ※SystemHandler.Handle.RecipeFiles 프로퍼티는 존재하지 않음(멤버명은 Recipes) — 직접 경로 도출 금지, 아래 헬퍼만 사용.
- ★모델 경로: InspectionSequence.ResolveDatumModelPath(_editingDatum)  (54-04 Task 1 이 추가하는 public static 헬퍼 — 54-04 런타임 load 와 동일 키. 직접 GetPatternModelFilePath 호출 금지 → 키 불일치 방지 D-07.)
- _editingDatum = mParentWindow.inspectionList.SelectedParam as DatumConfig  (MainView.cs:2218/2583 선례).
- 기존 Rect write-back 패턴: HalconViewer_DatumRectCompleted (MainView.cs:2407-2456) — halconViewer.CommitActiveRectangle() → bbox → centerRow/Col + halfW/halfH → switch(_datumTeachStep) 필드 기록. (centerRow=(Row1+Row2)/2, halfW=(Col2-Col1)/2 X축절반=Length1, halfH=(Row2-Row1)/2 Y축절반=Length2.)
- RectDrawingCompleted 이벤트 구독/해제 패턴: MainView.cs:2322/2330/2407-2408.
- InvokeTryTeachDatum (MainView.cs:2494-2568) — _editingDatum + halconViewer.CurrentImage + svc + 결과 모달 + write-back 미러 대상.
- ECanvasMode enum (MainView.cs:40): { None, RectRoi, PolygonRoi, CircleRoi, TeachDatum, Calibration } — 패턴 ROI 전용 모드 추가 대상.
- TeachDatumButton_Click (MainView.cs:2210-) — Datum 노드 선택 가드 + 모드 진입 선례.
</verified_accessors>

<interfaces>
54-03 PatternMatchService: TryCreateModel(templateImage, roiRow, roiCol, roiPhi, roiLen1, roiLen2, engine, angleExtentDeg, modelPath, out error); TryFindRefPose(templateImage, engine, modelPath, minScore, out refRow, out refCol, out refAngleDeg, out refScore, out error).
54-04 InspectionSequence: public static string ResolveDatumModelPath(DatumConfig datum) — recipe/seq/act/datumName/engine 단일 키. 54-05 는 이 헬퍼만 호출(직접 경로 도출 금지).
54-01 DatumConfig: PatternRoi_Row/Col/Phi/Length1/Length2, RefMatchRow/Col/AngleDeg, PatternEngine, PatternAngleExtentDeg, PatternMinScore, DatumName, SourceShotName.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 패턴 ROI 전용 그리기 모드 + write-back + 모델 생성/저장 + ref pose 기록</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml, WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (수정 대상 — ECanvasMode enum L40, HalconViewer_DatumRectCompleted L2407-2456 write-back 패턴, RectDrawingCompleted 구독 L2322/2330, TeachDatumButton_Click L2210 Datum 노드 가드, InvokeTryTeachDatum L2494-2568 모달/결과 미러, btn_teachDatum 활성화 로직)
    - WPF_Example/UI/ContentItem/MainView.xaml (수정 대상 — btn_teachDatum 버튼 배치 영역, Datum 패널)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (PatternRoi_*/RefMatch*/PatternEngine/PatternAngleExtentDeg/PatternMinScore — 54-01 결과)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (ResolveDatumModelPath — 54-04 Task 1 결과)
  </read_first>
  <action>
1) MainView.xaml: btn_teachDatum 인근에 버튼 2개 추가:
   - [패턴 ROI 그리기] x:Name="btn_drawPatternRoi", Content="패턴 ROI", Click="DrawPatternRoiButton_Click"
   - [패턴 모델 생성/저장] x:Name="btn_createPatternModel", Content="패턴 모델 생성", Click="CreatePatternModelButton_Click"
   둘 다 기본 IsEnabled=false(Datum 노드 선택 시 활성, btn_teachDatum 와 동일 게이팅). `<!--260618 hbk Phase 54 ALIGN-01-->` 주석.

2) MainView.xaml.cs: 패턴 ROI 전용 그리기 모드 (W2 — write-back 메커니즘 구체화, silent 실패 차단).
   - ECanvasMode enum(L40)에 `PatternRoi` 추가.
   - DrawPatternRoiButton_Click: _editingDatum = SelectedParam as DatumConfig (null 이면 "Datum 노드를 먼저 선택" 모달 + return). ExitCanvasMode 후 _canvasMode=ECanvasMode.PatternRoi, halconViewer Rect 그리기 모드 진입, RectDrawingCompleted += HalconViewer_PatternRectCompleted (TeachDatumButton_Click 진입 패턴 미러).
   - HalconViewer_PatternRectCompleted: HalconViewer_DatumRectCompleted(L2407-2456) 패턴 그대로 미러하되 PatternRoi_* 에 write-back:
```csharp
//260618 hbk Phase 54 ALIGN-01 패턴 ROI write-back (D-08) — DatumRectCompleted 패턴 미러
private void HalconViewer_PatternRectCompleted(object sender, EventArgs e)
{
    halconViewer.RectDrawingCompleted -= HalconViewer_PatternRectCompleted;
    var roi = halconViewer.CommitActiveRectangle();
    if (roi == null || _editingDatum == null) { ExitCanvasMode(); return; }
    double centerRow = (roi.Row1 + roi.Row2) / 2.0;
    double centerCol = (roi.Column1 + roi.Column2) / 2.0;
    double halfH = (roi.Row2 - roi.Row1) / 2.0;
    double halfW = (roi.Column2 - roi.Column1) / 2.0;
    _editingDatum.PatternRoi_Row = centerRow;
    _editingDatum.PatternRoi_Col = centerCol;
    _editingDatum.PatternRoi_Phi = 0.0;
    _editingDatum.PatternRoi_Length1 = halfW; // X축 절반
    _editingDatum.PatternRoi_Length2 = halfH; // Y축 절반
    _canvasMode = ECanvasMode.None;
    halconViewer.SetDatumOverlay(_editingDatum, _editingDatum.LastTeachSucceeded, GetDatumEditMode()); // 오버레이 갱신(있으면)
}
```
   (CommitActiveRectangle/이벤트 해제/ExitCanvasMode 시그니처는 read_first 로 확인 — HalconViewer_DatumRectCompleted 와 동일 호출.)

3) CreatePatternModelButton_Click → InvokeCreatePatternModel() (InvokeTryTeachDatum L2494-2568 패턴 미러):
```csharp
//260618 hbk Phase 54 ALIGN-01 패턴 모델 생성/저장 + ref pose 기록 (D-08/D-09) — InvokeTryTeachDatum 미러
private void InvokeCreatePatternModel()
{
    if (_editingDatum == null) { CustomMessageBox.Show("모델 생성 실패", "Datum 노드를 먼저 선택하세요."); return; }
    HImage img = halconViewer.CurrentImage;
    if (img == null) { CustomMessageBox.Show("모델 생성 실패", "이미지가 없습니다. 먼저 Grab 또는 Load Image."); return; }
    // W2: PatternRoi 미확보 시 모델 생성 차단 (silent 실패 0)
    if (_editingDatum.PatternRoi_Length1 <= 0.0 || _editingDatum.PatternRoi_Length2 <= 0.0)
    { CustomMessageBox.Show("모델 생성 실패", "패턴 ROI(Rect)를 먼저 그리세요. ([패턴 ROI] 버튼)"); return; }

    // 54-04 런타임 load 와 동일 키 (D-07) — 직접 경로 도출 금지
    string modelPath = ReringProject.Sequence.InspectionSequence.ResolveDatumModelPath(_editingDatum);
    if (string.IsNullOrEmpty(modelPath)) { CustomMessageBox.Show("모델 생성 실패", "모델 경로 도출 실패(레시피/Shot 확인)."); return; }

    var svc = new ReringProject.Halcon.Algorithms.PatternMatchService();
    string error;
    bool ok = svc.TryCreateModel(img,
        _editingDatum.PatternRoi_Row, _editingDatum.PatternRoi_Col, _editingDatum.PatternRoi_Phi,
        _editingDatum.PatternRoi_Length1, _editingDatum.PatternRoi_Length2,
        _editingDatum.PatternEngine, _editingDatum.PatternAngleExtentDeg, modelPath, out error);

    if (ok)
    {
        // D-09: 티칭 이미지에서 1회 find → ref pose 기록 (런타임과 동일 연산 → 부호 일관성)
        double rr, rc, ra, rs;
        if (svc.TryFindRefPose(img, _editingDatum.PatternEngine, modelPath, _editingDatum.PatternMinScore, out rr, out rc, out ra, out rs, out error))
        {
            _editingDatum.RefMatchRow = rr;
            _editingDatum.RefMatchCol = rc;
            _editingDatum.RefMatchAngleDeg = ra;
            CustomMessageBox.Show("모델 생성 완료", "패턴 모델 생성·ref pose 기록 완료 (score " + rs.ToString("F3") + ") — Recipe Save 권장");
        }
        else { CustomMessageBox.Show("ref pose 기록 실패", error); }
    }
    else { CustomMessageBox.Show("모델 생성 실패", error); }
}
```

4) Datum 노드 선택 시 btn_drawPatternRoi.IsEnabled / btn_createPatternModel.IsEnabled = (선택 노드가 DatumConfig) — btn_teachDatum 활성화 분기와 동일 위치.
  </action>
  <verify>
    <automated>grep -n "InvokeCreatePatternModel\|TryCreateModel\|TryFindRefPose\|RefMatchRow =\|PatternRoi_Row =\|ResolveDatumModelPath\|HalconViewer_PatternRectCompleted\|ECanvasMode.PatternRoi" WPF_Example/UI/ContentItem/MainView.xaml.cs; echo "RecipeFiles property misuse (must be 0):"; grep -c "Handle.RecipeFiles\|Handle.Recipes.GetPatternModelFilePath" WPF_Example/UI/ContentItem/MainView.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - MainView.xaml contains a button with `Click="CreatePatternModelButton_Click"` and a button with `Click="DrawPatternRoiButton_Click"`
    - MainView.xaml.cs ECanvasMode enum contains `PatternRoi`
    - MainView.xaml.cs contains `private void HalconViewer_PatternRectCompleted(` assigning `_editingDatum.PatternRoi_Row =`, `PatternRoi_Col =`, `PatternRoi_Length1 =`, `PatternRoi_Length2 =`
    - MainView.xaml.cs contains `private void InvokeCreatePatternModel(`
    - InvokeCreatePatternModel guards `_editingDatum.PatternRoi_Length1 <= 0.0 || _editingDatum.PatternRoi_Length2 <= 0.0` before TryCreateModel (W2 silent 실패 차단)
    - modelPath derived via `InspectionSequence.ResolveDatumModelPath(_editingDatum)` (54-04 와 동일 키, D-07) — NOT via direct GetPatternModelFilePath
    - body calls `svc.TryCreateModel(` with `_editingDatum.PatternEngine` and `_editingDatum.PatternRoi_Length1`
    - body calls `svc.TryFindRefPose(` and on success assigns `_editingDatum.RefMatchRow =`, `RefMatchCol =`, `RefMatchAngleDeg =`
    - 모델/ref pose 실패 시 CustomMessageBox 안내 (throw 없음)
    - `grep -c "Handle.RecipeFiles\|Handle.Recipes.GetPatternModelFilePath"` returns `0` (직접 경로 도출 금지 — 헬퍼만 사용)
  </acceptance_criteria>
  <done>Datum 노드에서 [패턴 ROI] 전용 모드로 그리면 PatternRoi_* write-back + [패턴 모델 생성] → ResolveDatumModelPath 동일 키로 .shm/.ncm 생성 + ref pose write-back; ROI 미확보/실패 시 모달 차단.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SIMUL 패턴매칭 보정 UAT (off 회귀 0 + x/y 추종 + tilt + ALIGN_FAIL + 영속 + Side per-Datum + 부호)</name>
  <files>—</files>
  <action>checkpoint:human-verify — 자동화 코드 작업 없음. 아래 how-to-verify 8개 항목을 사용자가 SIMUL 빌드에서 검증하고 PASS/FAIL 보고한다.</action>
  <what-built>
    Phase 54 전체 결선 — DatumConfig ALIGN 필드(54-01) + .shm/.ncm 경로(54-02) + PatternMatchService(54-03) + DatumPhase 매칭 통합/RotateImageByAngle 전면 폐기/ResolveDatumModelPath 키(54-04) + 패턴 티칭 UI(54-05). SIMUL 합성 변형 이미지 페어로 검증.
  </what-built>
  <how-to-verify>
    빌드: Debug/x64 (SIMUL_MODE) Rebuild → 앱 실행.

    1) off 회귀 0: IsPatternAlignEnabled=false 유지 상태로 기존 레시피 검사 → 측정값이 Phase 53 이전과 동일(byte-identical). _datumTransforms 무변경. (※RotateImageByAngle 폐기로 레벨링 회전 경로도 사라졌으니, 레벨링 기존 사용 레시피가 있었다면 측정 변화 여부도 함께 확인 — 변화 시 보고.)
    2) 패턴 티칭: Datum 노드 선택 → [패턴 ROI] 버튼으로 Rect 그리기 → [패턴 모델 생성] → "모델 생성 완료 + ref pose" 모달 → Recipe Save. 레시피 폴더에 .shm(또는 .ncm) 파일 생성 확인. (ROI 안 그리고 [패턴 모델 생성] 누르면 "패턴 ROI 를 먼저 그리세요" 모달 확인.)
    3) X,Y 이동 케이스: 평행이동된 합성 이미지로 검사. IsPatternAlignEnabled=true → 측정 ROI 가 에지 추종(측정 정상). off 로 토글 → 동일 이미지에서 에지 이탈(오측) 대조.
    4) tilt 케이스: 회전+이동 합성 이미지 → x,y(매칭) + 이동 line-fit θ 합성 보정 후 측정 정상.
    5) 매칭 실패 폴백: score 미달 이미지 또는 모델 파일 삭제 → 시퀀스 abort 0(진행) + 해당 Datum 측정 NG(LastSkipReason=ALIGN_FAIL 로그 + 값 클리어). 가짜 양품 0. (Excel/UI 에서 ALIGN_FAIL 이 DATUM_FAIL 과 구분 표기되는지 확인.)
    6) 영속: 앱 재시작 → 레시피 로드 → 재티칭 없이 런타임 매칭 동작(모델 파일 + ref pose 복원, 저장-로드 경로 일치).
    7) Side per-Datum: Side 4 Datum 각각 독립 보정(한 Datum 실패가 다른 Datum 보정에 영향 없음).
    8) 부호/좌표계(BLOCKING): X 이동 vs Y 이동 시 ROI 가 올바른 방향으로 추종(부호 반전 없음). dRow=curRow-RefMatchRow 부호가 line-fit ROI 를 매칭 방향과 같은 쪽으로 이동시키는지 SUMMARY 규약과 대조. 반전 시 FAIL 보고(54-03/54-04 SUMMARY 부호 규약 수정 필요).
  </how-to-verify>
  <verify>UAT — 사용자 육안/기능 검증 (자동 명령 없음)</verify>
  <done>8개 UAT 항목 사용자 sign-off (off 회귀 0 / x,y 추종 / tilt / ALIGN_FAIL 구분 / 영속 / Side per-Datum / 부호 BLOCKING)</done>
  <resume-signal>각 항목 PASS/FAIL 보고. "approved" 또는 결함 기술.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 티칭 이미지 → 모델 파일 쓰기 | 사용자 선택 이미지에서 모델 생성 후 레시피 폴더에 .shm/.ncm 기록 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-09 | Tampering | 모델 생성 실패/ROI 미확보 silent | mitigate | PatternRoi_Length 가드 + TryCreateModel/TryFindRefPose 실패 시 CustomMessageBox 안내 + ref pose 미기록 → 미검증 모델로 런타임 진입 차단 |
| T-54-10 | Repudiation | ref pose 부호 불일치 | mitigate | D-09 — 티칭 find 가 런타임 find 와 동일 연산이라 부호 일관. UAT 8항(BLOCKING)으로 방향 검증 |
| T-54-13 | Spoofing | 저장-로드 경로 불일치(stale) | mitigate | ResolveDatumModelPath 단일 헬퍼 강제(54-04 와 동일 키) — 직접 경로 도출 금지 (D-07) |
</threat_model>

<verification>
- msbuild Debug/x64 빌드 PASS
- UAT Task 2 사용자 sign-off (SIMUL 합성 변형 페어)
</verification>

<success_criteria>
- 패턴 ROI 전용 모드 그리기 + write-back + 가드 + 모델 생성/저장 + ref pose 기록 동작 (D-08/D-09)
- ResolveDatumModelPath 동일 키로 저장 (D-07)
- UAT: off 회귀 0 / x,y 추종 / tilt / ALIGN_FAIL 구분 / 영속 / Side per-Datum / 부호(BLOCKING) 전부 PASS
</success_criteria>

<output>
완료 후 `.planning/phases/54-datum-align-01-x-y-tilt/54-05-SUMMARY.md` + UAT 결과 기록.
</output>
