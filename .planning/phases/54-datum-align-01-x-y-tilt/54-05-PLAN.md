---
phase: 54-datum-align-01-x-y-tilt
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: false
requirements: [ALIGN-01]

must_haves:
  truths:
    - "Datum 노드에서 패턴 ROI(Rect)를 그릴 수 있다"
    - "[패턴 모델 생성/저장] 버튼이 모델 파일(.shm/.ncm)을 생성·저장한다"
    - "모델 생성 시 티칭 이미지에서 1회 find 하여 ref pose(RefMatchRow/Col/AngleDeg)를 DatumConfig 에 기록한다"
    - "재시작 후에도 모델 파일 + ref pose 로 런타임 매칭이 동작한다"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "패턴 ROI write-back + 모델 생성/저장 + ref pose 기록 핸들러"
      contains: "InvokeCreatePatternModel"
  key_links:
    - from: "패턴 ROI(Rect)"
      to: "DatumConfig.PatternRoi_*"
      via: "ECanvasMode write-back"
      pattern: "PatternRoi_Row"
    - from: "[모델 생성] 버튼"
      to: "PatternMatchService.TryCreateModel + TryFindRefPose"
      via: "GetPatternModelFilePath 경로"
      pattern: "TryCreateModel"
    - from: "ref pose find"
      to: "DatumConfig.RefMatch*"
      via: "write-back"
      pattern: "RefMatchRow ="
---

<objective>
패턴 티칭 UI(D-08 최소 완결형)를 추가한다: Datum 노드에서 패턴 ROI(Rect) 그리기(기존 ECanvasMode.TeachDatum / Rect write-back 재사용) + [패턴 모델 생성/저장] 버튼 → PatternMatchService.TryCreateModel(.shm/.ncm 파일 저장) + TryFindRefPose(ref pose 1회 기록, D-09) → DatumConfig.RefMatchRow/Col/AngleDeg write-back. 검색영역 별도 그리기·score 미리보기는 후속(deferred).

Purpose: 모델 생성·SIMUL 검증을 가능케 함(없으면 Phase 52 전철 — 백엔드만으론 검증 불가).
Output: MainView.xaml(버튼) + MainView.xaml.cs(핸들러).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-datum-align-01-x-y-tilt/54-CONTEXT.md
@.planning/phases/54-datum-align-01-x-y-tilt/54-PATTERNS.md
@.planning/phases/54-datum-align-01-x-y-tilt/54-01-SUMMARY.md
@.planning/phases/54-datum-align-01-x-y-tilt/54-02-SUMMARY.md
@.planning/phases/54-datum-align-01-x-y-tilt/54-03-SUMMARY.md

LOCKED: D-08(최소 완결형 — ROI 그리기 + 모델 생성/저장 버튼 + ref pose 기록), D-09(티칭 시 find 1회 → ref pose). 검색영역 별도 그리기·score 미리보기·재티칭 미리보기 = deferred(후속).

<interfaces>
54-03 PatternMatchService: TryCreateModel(templateImage, roiRow, roiCol, roiPhi, roiLen1, roiLen2, engine, angleExtentDeg, modelPath, out error); TryFindRefPose(templateImage, engine, modelPath, minScore, out refRow, out refCol, out refAngleDeg, out refScore, out error).
54-02 RecipeFileHelper: GetPatternModelFilePath(recipeName, seqName, actName, propertyName=datumName, engine).
레시피명 접근자: SystemHandler.Handle.Setting.CurrentRecipeName.
기존 티칭 UX (MainView.xaml.cs): private enum ECanvasMode { None, RectRoi, PolygonRoi, CircleRoi, TeachDatum, Calibration } (L40-41); InvokeTryTeachDatum() (L2494-2568) — _editingDatum + halconViewer.CurrentImage + svc.TryTeachDatum → 결과 모달 + SetDatumOverlay + write-back 패턴; HalconViewer_DatumRect/Completed step write-back; btn_teachDatum 활성화는 Datum 노드 선택 시.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 패턴 ROI write-back + 모델 생성/저장 + ref pose 기록</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml, WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (수정 대상 — ECanvasMode enum L40-41, InvokeTryTeachDatum L2494-2568 결과처리/write-back/모달 패턴, btn_teachDatum 활성화 로직, HalconViewer Rect completed write-back 패턴)
    - WPF_Example/UI/ContentItem/MainView.xaml (수정 대상 — btn_teachDatum 버튼 배치 영역, Datum 패널)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (PatternRoi_*/RefMatch*/PatternEngine/PatternAngleExtentDeg/PatternMinScore 필드 — 54-01 결과)
  </read_first>
  <action>
1) MainView.xaml: btn_teachDatum 인근에 [패턴 모델 생성/저장] 버튼 추가(예: x:Name="btn_createPatternModel", Content="패턴 모델 생성", Click="CreatePatternModelButton_Click"). 기본 IsEnabled=false(Datum 노드 선택 시 활성, btn_teachDatum 활성화 로직과 동일 게이팅). `<!--260618 hbk Phase 54 ALIGN-01-->` 주석.

2) MainView.xaml.cs: 패턴 ROI(Rect) write-back — 기존 Rect ROI 그리기(ECanvasMode.RectRoi 또는 TeachDatum 의 Rect step) 완료 시 _editingDatum 에 PatternRoi_Row/Col/Phi/Length1/Length2 를 write-back 하는 경로 추가. 1차 최소: btn_createPatternModel 클릭 시 현재 halconViewer 의 마지막 그린 Rect ROI 를 PatternRoi_* 로 읽어오거나(뷰어 API 확인), TeachDatum Rect write-back 패턴을 재사용. write-back 라인에 `//260618 hbk Phase 54 ALIGN-01` + `PatternRoi_Row = ...`.

3) CreatePatternModelButton_Click → InvokeCreatePatternModel() (InvokeTryTeachDatum L2494-2568 패턴 미러):
```csharp
//260618 hbk Phase 54 ALIGN-01 패턴 모델 생성/저장 + ref pose 기록 (D-08/D-09) — InvokeTryTeachDatum 미러
private void InvokeCreatePatternModel()
{
    if (_editingDatum == null) { return; }
    HImage img = halconViewer.CurrentImage;
    if (img == null) { CustomMessageBox.Show("모델 생성 실패", "이미지가 없습니다. 먼저 Grab 또는 Load Image."); return; }
    if (_editingDatum.PatternRoi_Length1 <= 0.0 || _editingDatum.PatternRoi_Length2 <= 0.0)
    { CustomMessageBox.Show("모델 생성 실패", "패턴 ROI(Rect)를 먼저 그리세요."); return; }

    string recipeName = SystemHandler.Handle.Setting.CurrentRecipeName;
    string seqName = /* 현 시퀀스명 — read_first 확인 (_editingDatum 소유 시퀀스 또는 선택 노드 시퀀스) */;
    string actName = /* 액션명 — read_first 확인 */;
    string modelPath = SystemHandler.Handle.RecipeFiles.GetPatternModelFilePath(recipeName, seqName, actName, _editingDatum.DatumName, _editingDatum.PatternEngine);

    var svc = new ReringProject.Halcon.Algorithms.PatternMatchService();
    string error;
    bool ok = svc.TryCreateModel(img,
        _editingDatum.PatternRoi_Row, _editingDatum.PatternRoi_Col, _editingDatum.PatternRoi_Phi,
        _editingDatum.PatternRoi_Length1, _editingDatum.PatternRoi_Length2,
        _editingDatum.PatternEngine, _editingDatum.PatternAngleExtentDeg, modelPath, out error);

    if (ok)
    {
        // D-09: 티칭 이미지에서 1회 find → ref pose 기록 (런타임과 동일 연산 → 부호 일관성)
        double rr, rc, ra, rs;
        if (svc.TryFindRefPose(img, _editingDatum.PatternEngine, modelPath, _editingDatum.PatternMinScore, out rr, out rc, out ra, out rs, out error))
        {
            _editingDatum.RefMatchRow = rr;
            _editingDatum.RefMatchCol = rc;
            _editingDatum.RefMatchAngleDeg = ra;
            CustomMessageBox.Show("모델 생성 완료", "패턴 모델 생성·ref pose 기록 완료 (score " + rs.ToString("F3") + ") — Recipe Save 권장");
        }
        else { CustomMessageBox.Show("ref pose 기록 실패", error); }
    }
    else { CustomMessageBox.Show("모델 생성 실패", error); }
}
```
   (seqName/actName 접근자는 read_first 로 정확히 확인 — InvokeTryTeachDatum 가 _editingDatum 컨텍스트에서 시퀀스/액션을 어떻게 얻는지 동일 소스 재사용. 없으면 선택 노드 트리에서 부모 시퀀스/액션명 도출.)

4) Datum 노드 선택 시 btn_createPatternModel.IsEnabled = (_editingDatum != null) — btn_teachDatum 활성화 분기와 동일 위치.
  </action>
  <verify>
    <automated>grep -n "InvokeCreatePatternModel\|TryCreateModel\|TryFindRefPose\|RefMatchRow =\|PatternRoi_Row" WPF_Example/UI/ContentItem/MainView.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - MainView.xaml contains a button with `Click="CreatePatternModelButton_Click"` (or equivalent named handler)
    - MainView.xaml.cs contains `private void InvokeCreatePatternModel(`
    - body calls `svc.TryCreateModel(` with `_editingDatum.PatternEngine` and `_editingDatum.PatternRoi_Length1`
    - body calls `svc.TryFindRefPose(` and on success assigns `_editingDatum.RefMatchRow =`, `RefMatchCol =`, `RefMatchAngleDeg =`
    - body calls `GetPatternModelFilePath(` with `_editingDatum.DatumName` and `_editingDatum.PatternEngine`
    - PatternRoi_* write-back present (grep `PatternRoi_Row` assignment in xaml.cs)
    - 모델/ref pose 실패 시 CustomMessageBox 안내 (throw 없음)
  </acceptance_criteria>
  <done>Datum 노드에서 패턴 ROI 그리기 + 버튼 클릭 → .shm/.ncm 생성 + ref pose write-back; 실패 시 모달 안내.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SIMUL 패턴매칭 보정 UAT (off 회귀 0 + x/y 추종 + tilt + ALIGN_FAIL + 영속 + Side per-Datum)</name>
  <files>—</files>
  <action>checkpoint:human-verify — 자동화 코드 작업 없음. 아래 how-to-verify 8개 항목을 사용자가 SIMUL 빌드에서 검증하고 PASS/FAIL 보고한다.</action>
  <what-built>
    Phase 54 전체 결선 — DatumConfig ALIGN 필드(54-01) + .shm/.ncm 경로(54-02) + PatternMatchService(54-03) + DatumPhase 매칭 통합/RotateImageByAngle 폐기(54-04) + 패턴 티칭 UI(54-05). SIMUL 합성 변형 이미지 페어로 검증.
  </what-built>
  <how-to-verify>
    빌드: Debug/x64 (SIMUL_MODE) Rebuild → 앱 실행.

    1) off 회귀 0: IsPatternAlignEnabled=false 유지 상태로 기존 레시피 검사 → 측정값이 Phase 53 이전과 동일(byte-identical). _datumTransforms 무변경.
    2) 패턴 티칭: Datum 노드 선택 → 패턴 ROI(Rect) 그리기 → [패턴 모델 생성] → "모델 생성 완료 + ref pose" 모달 → Recipe Save. 레시피 폴더에 .shm(또는 .ncm) 파일 생성 확인.
    3) X,Y 이동 케이스: 평행이동된 합성 이미지로 검사. IsPatternAlignEnabled=true → 측정 ROI 가 에지 추종(측정 정상). off 로 토글 → 동일 이미지에서 에지 이탈(오측) 대조.
    4) tilt 케이스: 회전+이동 합성 이미지 → x,y(매칭) + line-fit θ 합성 보정 후 측정 정상.
    5) 매칭 실패 폴백: score 미달 이미지 또는 모델 파일 삭제 → 시퀀스 abort 0(진행) + 해당 Datum 측정 NG(ALIGN_FAIL 로그 + 값 클리어). 가짜 양품 0.
    6) 영속: 앱 재시작 → 레시피 로드 → 재티칭 없이 런타임 매칭 동작(모델 파일 + ref pose 복원).
    7) Side per-Datum: Side 4 Datum 각각 독립 보정(한 Datum 실패가 다른 Datum 보정에 영향 없음).
    8) 부호/좌표계: X 이동 vs Y 이동 시 ROI 가 올바른 방향으로 추종(부호 반전 없음).
  </how-to-verify>
  <verify>UAT — 사용자 육안/기능 검증 (자동 명령 없음)</verify>
  <done>8개 UAT 항목 사용자 sign-off (off 회귀 0 / x,y 추종 / tilt / ALIGN_FAIL / 영속 / Side per-Datum / 부호)</done>
  <resume-signal>각 항목 PASS/FAIL 보고. "approved" 또는 결함 기술.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 티칭 이미지 → 모델 파일 쓰기 | 사용자 선택 이미지에서 모델 생성 후 레시피 폴더에 .shm/.ncm 기록 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-09 | Tampering | 모델 생성 실패 silent | mitigate | TryCreateModel/TryFindRefPose 실패 시 CustomMessageBox 안내 + ref pose 미기록 → 미검증 모델로 런타임 진입 차단 |
| T-54-10 | Repudiation | ref pose 부호 불일치 | accept | D-09 — 티칭 find 가 런타임 find 와 동일 연산이라 부호 일관. UAT 8항으로 방향 검증 |
</threat_model>

<verification>
- msbuild Debug/x64 빌드 PASS
- UAT Task 2 사용자 sign-off (SIMUL 합성 변형 페어)
</verification>

<success_criteria>
- 패턴 ROI 그리기 + 모델 생성/저장 + ref pose 기록 동작 (D-08/D-09)
- UAT: off 회귀 0 / x,y 추종 / tilt / ALIGN_FAIL 폴백 / 영속 / Side per-Datum 전부 PASS
</success_criteria>

<output>
완료 후 `.planning/phases/54-datum-align-01-x-y-tilt/54-05-SUMMARY.md` + UAT 결과 기록.
</output>
