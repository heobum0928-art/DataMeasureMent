---
phase: 57-pattern-roi-ux-datum-align-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionMasterParam.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs
autonomous: true
requirements:
  - "#6 leveling 완전 제거 (코드+상태+INI, off 레시피 회귀 0)"

must_haves:
  truths:
    - "코드베이스에 leveling 관련 멤버/메서드/INI 키가 0건 (grep 0)"
    - "측정 상태머신 전이가 MoveZ → DatumPhase 로 직결 (Level step 부재)"
    - "leveling 미사용(off) 기존 레시피 로드 시 크래시 없이 동작 변화 없음"
    - "SIMUL_MODE Debug/x64 빌드가 신규 error/warning 0 으로 통과"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "EStep.Level 제거된 상태머신 (MoveZ→DatumPhase 직결)"
      contains: "Step = (int)EStep.DatumPhase"
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "leveling 멤버/메서드 제거된 시퀀스"
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "TryGetLevelingAngle 양 오버로드 제거된 서비스"
  key_links:
    - from: "Action_FAIMeasurement.EStep.MoveZ"
      to: "EStep.DatumPhase"
      via: "Step 전이"
      pattern: "Step = \\(int\\)EStep\\.DatumPhase"
---

<objective>
미사용 leveling(레벨링/기울기 보정) 기능의 모든 잔재를 코드·측정 상태·INI 직렬화에서 완전 제거한다. ALIGN(Phase 54/55) 의 패턴매칭 위치/tilt 보정이 leveling 을 완전히 대체하므로 중복 코드다.

Purpose: 죽은 코드 경로(특히 측정 상태머신의 EStep.Level)를 제거하여 유지보수성을 높이고, 측정 흐름을 단순화한다. CONTEXT D-12/D-13/D-14.

Output: leveling 멤버/메서드/INI 키가 0건인 코드베이스. MoveZ → DatumPhase 직결 상태머신. off 레시피 회귀 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-pattern-roi-ux-datum-align-hardening/57-CONTEXT.md
@.planning/phases/57-pattern-roi-ux-datum-align-hardening/57-PATTERNS.md

<notes>
- .NET Framework 4.8, C# 7.2 — switch expressions / nullable refs / record 금지.
- 모든 변경 라인에 `//260619 hbk Phase 57 #6 ...` 주석 필수 (MEMORY feedback_comment_convention).
- brace 스타일은 편집 대상 파일의 기존 스타일을 따른다 (Action_FAIMeasurement/InspectionSequence = K&R, DatumFindingService = Allman).
- **off-회귀 0 핵심 (D-14):** 옛 INI 의 `IsLevelingReference`/`LevelingEnabled` 키는 매칭 프로퍼티 제거 후 stale 가 되며, ParamBase.Load 는 매칭 프로퍼티 부재 시 키를 무시하므로 로드 크래시 없음. InspectionRecipeManager 의 명시 read(`LevelingEnabled`) 도 동시 제거하므로 무관.
- **순서 권장:** Task 1(상태머신 재배선)을 먼저, Task 2(멤버/메서드/INI 제거)를 그 다음. 호출처(EStep.Level)가 사라진 뒤 호출 대상(TryComputeLevelingAngle/TryGetLevelingAngle)을 제거하면 CS0103/CS1501 빌드 차단 위험이 줄어든다.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EStep.Level 제거 + 상태머신 MoveZ→DatumPhase 재배선</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (전체 — EStep enum :30-38, MoveZ 종료 :75, EStep.Level 케이스 :80-118, DatumPhase :122~)
    - .planning/phases/57-pattern-roi-ux-datum-align-hardening/57-PATTERNS.md (#6 상태머신 재배선 패턴 섹션)
  </read_first>
  <action>
    Action_FAIMeasurement.cs 에서 leveling 상태머신을 제거한다 (최우선 위험 — D-14):

    1. EStep enum(:30-38) 에서 `Level,` 멤버 라인(:33) 을 삭제. 남는 멤버: `Init, MoveZ, DatumPhase, Grab, Measure, End`. switch 가 명칭 기반이라 정수값 변동은 안전하지만, enum 멤버 순서는 유지(혼동 방지).

    2. EStep.MoveZ 케이스 종료 라인(:75) 을 변경:
       - 현재: `Step = (int)EStep.Level;`
       - 변경: `Step = (int)EStep.DatumPhase;`
       - 변경 라인 위에 `//260619 hbk Phase 57 #6 leveling 제거 — MoveZ→DatumPhase 직결 (EStep.Level 폐기, D-13/D-14)` 주석 추가.

    3. `case EStep.Level:` 블록 전체(:80-118, 중괄호 포함 닫는 `}` 까지)를 완전히 삭제. 이 블록 안의 SIMUL_MODE 진단 로그(:97-106), `lvlSeq.TryComputeLevelingAngle(...)` 호출(:108), `lvlSeq.LevelingEnabled`/`LevelingComputed`/`IsLevelingReference` 참조가 모두 제거되어야 한다.

    4. 블록 삭제 후 case 라벨 위에 있던 leveling 설명 주석(:78-79) 도 함께 제거.

    제거 후 switch 가 `case EStep.MoveZ` 다음에 `case EStep.DatumPhase` 로 바로 이어지는지 확인.
  </action>
  <verify>
    <automated>grep -rn "EStep.Level\|TryComputeLevelingAngle\|IsLevelingReference\|LevelingEnabled\|LevelingComputed" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs | wc -l  # → 0 이어야 함</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Level" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 결과에 `EStep.Level` / `case EStep.Level` / `Leveling` 문자열 0건.
    - `grep -n "Step = (int)EStep.DatumPhase" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 가 MoveZ 케이스 내에서 매치 (1건 이상).
    - EStep enum 에 `Level,` 멤버 라인 부재.
    - 변경 라인에 `//260619 hbk Phase 57 #6` 주석 존재.
  </acceptance_criteria>
  <done>EStep.Level 및 그 케이스 블록 완전 제거, MoveZ 가 DatumPhase 로 직결. leveling 참조 0건.</done>
</task>

<task type="auto">
  <name>Task 2: InspectionSequence/DatumFindingService/DatumConfig/InspectionMasterParam/InspectionRecipeManager 의 leveling 멤버·메서드·INI 키 제거</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (:48-49 LevelingEnabled, :64-70 캐시 멤버/메서드, :355-363 ClearDatumTransforms 내 ResetLeveling 호출, :600-639 TryComputeLevelingAngle)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (:606-684 TryGetLevelingAngle 4-arg, :686-755 TryGetLevelingAngle 6-arg dRow/dCol 오버로드)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (:40-43 IsLevelingReference)
    - WPF_Example/Custom/Sequence/Inspection/InspectionMasterParam.cs (:34-50 LevelingEnabled PropertyGrid 프로퍼티)
    - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs (:96-97 save, :112-113 신규보존, :138-139 load)
    - .planning/phases/57-pattern-roi-ux-datum-align-hardening/57-PATTERNS.md (#6 제거 footprint 표)
  </read_first>
  <action>
    leveling 멤버/메서드/INI 키를 전수 제거한다. **Task 1 완료 후** 실행(호출처 EStep.Level 제거 선행). 각 변경 라인에 `//260619 hbk Phase 57 #6 leveling 제거` 주석.

    **InspectionSequence.cs:**
    1. `public bool LevelingEnabled { get; set; } = false;` (:48-49, 주석 포함) 삭제.
    2. leveling 캐시 멤버/메서드 블록(:64-70) 전체 삭제:
       - `private double _levelingAngleRad = 0.0;`
       - `private bool _levelingComputed = false;`
       - `public double LevelingAngleRad { ... }`
       - `public bool LevelingComputed { ... }`
       - `public void SetLevelingAngle(double angleRad) { ... }`
       - `public void ResetLeveling() { ... }`
    3. ClearDatumTransforms 내부의 `ResetLeveling();` 호출(:363) + 그 위 주석(:362) 삭제. 나머지 ClearDatumTransforms 로직(_datumTransforms.Clear / _failedDatums.Clear / _alignFailedDatums.Clear / RuntimeDetectFailed 리셋)은 무손상 보존.
    4. `public bool TryComputeLevelingAngle(HImage refImage, out double angleRad)` 메서드(:600-639, 주석 포함) 전체 삭제.

    **DatumFindingService.cs:**
    5. `public bool TryGetLevelingAngle(HImage image, DatumConfig config, out double angleRad, out string error)` 4-arg 오버로드(:606-684, 주석 포함) 전체 삭제.
    6. `public bool TryGetLevelingAngle(HImage image, DatumConfig config, double dRow, double dCol, out double angleRad, out string error)` 6-arg 오버로드(:686-755, 주석 포함) 전체 삭제.
    7. AlignPreTransform 프로퍼티(:26-29) 및 TryFindDatum / TryFindVerticalTwoHorizontalDualImage / TryExtractEdgePoints 등 ALIGN 경로는 절대 건드리지 말 것 (#4 = 57-02 범위, Wave 2 충돌 회피).

    **DatumConfig.cs:**
    8. `public bool IsLevelingReference { get; set; } = false;` + `[Category("Datum|Leveling")]` + `[Description(...)]` 속성(:40-43, 주석 포함) 전체 삭제. PatternRoi*/PatternRoi2*/TeachingImagePath 등 다른 필드는 보존.

    **InspectionMasterParam.cs:**
    9. `public bool LevelingEnabled { ... }` PropertyGrid 프로퍼티(:34-50, `[Category("Fixture|Leveling")]` + Description + getter/setter 포함) 전체 삭제. DisplayName 프로퍼티는 보존.

    **InspectionRecipeManager.cs:**
    10. save 분기: `saveFile[sectionPrefix]["LevelingEnabled"] = seq.LevelingEnabled ? 1 : 0;` (:97) + 위 주석(:96) 삭제.
    11. 신규 레시피 보존 분기: `saveFile[sectionPrefix]["LevelingEnabled"] = 0;` (:113) + 위 주석(:112) 삭제.
    12. load 분기: `seq.LevelingEnabled = loadFile[sectionPrefix]["LevelingEnabled"].ToBool();` (:139) + 위 주석(:138) 삭제. **옛 INI 의 stale `LevelingEnabled` 키는 더 이상 read 하지 않으므로 무시됨 — 로드 크래시 0 (D-14).**

    제거 후 컴파일러가 미해결 참조(CS0103/CS1501)를 보고하면 잔여 호출처를 grep 으로 추적해 제거.
  </action>
  <verify>
    <automated>grep -rn "LevelingEnabled\|IsLevelingReference\|TryGetLevelingAngle\|TryComputeLevelingAngle\|LevelingAngleRad\|LevelingComputed\|SetLevelingAngle\|ResetLeveling\|_levelingAngleRad\|_levelingComputed" WPF_Example/ | wc -l  # → 0 이어야 함</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "Leveling\|leveling" WPF_Example/Custom/Sequence/Inspection/ WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 결과 0건 (주석 포함 모든 잔재 제거).
    - InspectionRecipeManager.cs 에 `["LevelingEnabled"]` 인덱서 접근 0건 (save/load/preserve 3곳 모두 제거).
    - DatumFindingService.cs 에 `TryGetLevelingAngle` 메서드 정의 0건.
    - DatumFindingService.cs 의 `AlignPreTransform` 프로퍼티(:29) 및 `TryFindVerticalTwoHorizontalDualImage` 메서드는 그대로 존재 (#4 범위 — 미변경 확인).
    - DatumConfig.cs 에 `IsLevelingReference` 0건, 단 `PatternRoi_Row`/`TeachingImagePath` 는 존재.
  </acceptance_criteria>
  <done>모든 leveling 멤버/메서드/INI 키 제거. grep "Leveling" 0건. ALIGN(#4) 경로 무손상.</done>
</task>

<task type="auto">
  <name>Task 3: SIMUL_MODE Debug/x64 빌드 검증 + off 레시피 회귀 확인</name>
  <read_first>
    - WPF_Example/DatumMeasurement.csproj (빌드 타깃 확인)
  </read_first>
  <action>
    MSBuild Debug/x64 로 SIMUL_MODE 빌드를 수행하여 leveling 제거가 컴파일 깨짐 없이 완료되었는지 확인한다.

    1. MSBuild 실행 (PATH 에 없으면 Visual Studio 의 MSBuild.exe 절대경로 사용; 통상 경로 예: `"C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"` 또는 vswhere 로 탐색):
       `MSBuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild /v:minimal`
    2. 출력에서 `error CS` 0건, 신규 `warning CS` 0건 확인 (Phase 21 baseline 6 warning 은 허용 — 신규만 0).
    3. 미해결 참조(CS0103 'LevelingEnabled' / CS1501 등)가 나오면 해당 파일을 다시 grep 하여 잔여 leveling 참조를 제거하고 재빌드.

    **off 레시피 회귀 검증 (D-14):** 기존 레시피 INI 에 stale `LevelingEnabled`/`IsLevelingReference` 키가 남아 있어도 ParamBase.Load 가 매칭 프로퍼티 부재 시 무시함을 코드로 확증한다. 빌드 PASS 자체가 명시 read 제거를 보장하며, 실데이터 로드 크래시 0 은 후속 UAT 에서 확인(이 task 는 빌드 게이트까지).
  </action>
  <verify>
    <automated>MSBuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild /v:minimal 2>&1 | grep -c "error CS"  # → 0 이어야 함</automated>
  </verify>
  <acceptance_criteria>
    - MSBuild Debug/x64 Rebuild 출력에 `error CS` 0건.
    - 신규 `warning CS` 0건 (기존 baseline 외 추가 경고 없음).
    - 빌드 산출물 `DatumMeasurement.exe` 가 생성됨.
  </acceptance_criteria>
  <done>SIMUL_MODE Debug/x64 빌드 PASS, leveling 제거로 인한 컴파일 깨짐 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레시피 INI → ParamBase.Load | 파일에서 읽은 stale leveling 키가 메모리 객체로 진입 |
| 측정 상태머신 전이 | Step 정수 캐스팅 — enum 멤버 제거 시 전이 누락 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Denial of Service | ParamBase.Load(옛 INI) | mitigate | ParamBase 가 매칭 프로퍼티 부재 시 stale 키 무시 — 로드 크래시 없음 (D-14). 명시 read(InspectionRecipeManager :139) 동시 제거로 KeyNotFound 회피 |
| T-57-02 | Tampering | EStep 상태머신 전이 | mitigate | MoveZ → DatumPhase 명시 재배선. switch 명칭 기반이라 정수값 변동 안전. Task 3 빌드 게이트로 미해결 전이 차단 |
</threat_model>

<verification>
- grep "Leveling|leveling" WPF_Example/Custom/Sequence/Inspection/ + DatumFindingService.cs → 0건
- MSBuild Debug/x64 Rebuild → error CS 0, 신규 warning CS 0
- 측정 상태머신: MoveZ → DatumPhase 직결 (EStep.Level 부재)
- ALIGN(#4) 경로(AlignPreTransform/TryFindVerticalTwoHorizontalDualImage) 무손상
</verification>

<success_criteria>
- leveling 관련 멤버/메서드/INI 키 0건 (코드+상태+INI 완전 제거, D-12)
- 상태머신 MoveZ→DatumPhase 직결 (D-13)
- SIMUL_MODE Debug/x64 빌드 PASS (신규 error/warning 0)
- 옛 INI 의 stale leveling 키는 무시됨 — 명시 read 제거로 회귀 0 (D-14)
</success_criteria>

<output>
After completion, create `.planning/phases/57-pattern-roi-ux-datum-align-hardening/57-01-SUMMARY.md`
</output>
