---
phase: 57-pattern-roi-ux-datum-align-hardening
plan: 02
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
autonomous: true
requirements:
  - "#4 Side DualImage 단일 공유 transform align (가로축 매칭 → 가로/세로 ROI 모두 적용)"
  - "#5 매칭 실패 lenient 검증/보강 (ALIGN_FAIL → NG, abort 없음)"

must_haves:
  truths:
    - "Side VerticalTwoHorizontalDualImage datum 이 IsPatternAlignEnabled 시 패턴매칭 align 을 수행한다"
    - "가로축(imgH)에서 산출한 단일 rigid transform 이 가로축 ROI(Horizontal_A/B)와 세로축 ROI(Vertical) 모두에 적용된다"
    - "DualImage align 실패 시 abort 없이 MarkAlignFailed → 측정 NG(ALIGN_FAIL) 처리"
    - "SIMUL_MODE Debug/x64 빌드가 신규 error/warning 0 으로 통과"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "DualImage 분기의 IsPatternAlignEnabled align 배선 (단일이미지 분기 미러)"
      contains: "TryComposeAlign"
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "DualImage 인지 TryComposeAlign (2-image 검출 분기)"
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "TryFindLine 의 AlignPreTransform 소비 (Vertical ROI transform 적용)"
  key_links:
    - from: "Action_FAIMeasurement.EStep.DatumPhase DualImage 분기"
      to: "InspectionSequence.TryComposeAlign"
      via: "IsPatternAlignEnabled 게이트"
      pattern: "TryComposeAlign"
    - from: "DatumFindingService.TryFindLine"
      to: "AlignPreTransform"
      via: "ROI 중심 이동 + strip θ 회전"
      pattern: "AlignPreTransform"
---

<objective>
Side 의 `VerticalTwoHorizontalDualImage` datum 에 ALIGN(패턴매칭 위치/tilt 보정)을 적용한다. 현재 DualImage 경로는 align 미적용(검출-only 폴백)이다. 가로축 이미지(imgH, TeachingImagePath)에서만 패턴매칭으로 단일 rigid transform `(x,y,θ)` 를 산출하고, 그 동일 transform 을 가로축 검출 ROI **와** 세로축 검출 ROI(imgV) **모두에** 적용한다.

Purpose: 텔레센트릭 렌즈 + Z축(포커스)만 이동하는 셋업에서 가로/세로 이미지는 같은 픽셀 좌표계이므로, 가로에서 구한 rigid transform 이 세로 ROI 에도 글로벌하게 유효하다 (D-01/D-02). 세로 이미지 별도 패턴은 불필요(과설계 폐기, D-02). 동시에 #5 lenient 정책(매칭 실패 → NG, abort 없음)을 검증·보강한다.

Output: DualImage datum 이 align-enabled 시 두 이미지에 단일 transform 으로 보정 검출. align 실패 시 측정 진행(NG). 빌드 PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-pattern-roi-ux-datum-align-hardening/57-CONTEXT.md
@.planning/phases/57-pattern-roi-ux-datum-align-hardening/57-PATTERNS.md

<interfaces>
<!-- 핵심 계약. executor 는 이 시그니처/패턴을 직접 사용 — 코드 탐색 불필요. -->

InspectionSequence.cs (Wave 1=57-01 에서 leveling 멤버 제거됨, 아래 ALIGN 멤버는 보존):
```csharp
// 단일 refImage 패턴매칭 → rigid transform 산출 + datum 검출 + _datumTransforms[datumKey] = alignRigid 저장
public bool TryComposeAlign(DatumConfig datum, HImage refImage, string modelPath, out string error);
// 모델 경로 단일 소스 (티칭·런타임 공유) — 직접 경로 도출 금지
public static string ResolveDatumModelPath(DatumConfig datum);
// align 실패 기록 (lenient — 측정 NG 강제, abort 없음). 내부에서 MarkDatumFailed 도 호출.
public void MarkAlignFailed(string datumName);
public void MarkDatumFailed(string datumName);
// datum 검출 (Clear 안 함, 누적). DualImage 면 imageV != null 로 2-image 분기.
public bool TryRunSingleDatum(DatumConfig datum, HImage imageH, HImage imageV, out string error);
```

DatumFindingService.cs:
```csharp
// set 되면 검출 ROI 중심을 이 hom_mat2d 로 이동/회전 후 검출. null/빈 = 무보정.
public HTuple AlignPreTransform { get; set; } = null;
// 1-image 오버로드 (TwoLineIntersect/CircleTwoHorizontal/VerticalTwoHorizontal)
public bool TryFindDatum(HImage image, DatumConfig config, out HTuple transform, out string error);
// 2-image 오버로드 (VerticalTwoHorizontalDualImage 전용) — imageHorizontal/imageVertical
public bool TryFindDatum(HImage imageHorizontal, HImage imageVertical, DatumConfig config, out HTuple transform, out string error);
```

AlignPreTransform 소비 패턴 (TryExtractEdgePoints :1932-1944 — Horizontal_A/B 는 이미 소비):
```csharp
double alignRot = 0.0;
if (AlignPreTransform != null && AlignPreTransform.Length > 0)
{
    try
    {
        HTuple atRow, atCol;
        HOperatorSet.AffineTransPoint2d(AlignPreTransform, roiRow, roiCol, out atRow, out atCol);
        roiRow = atRow.D;
        roiCol = atCol.D;
        alignRot = Math.Atan2(AlignPreTransform[3].D, AlignPreTransform[0].D); // h10/h00
    }
    catch { /* 변환 실패 시 원본 ROI 좌표/회전 유지 */ }
}
```
</interfaces>

<critical_findings>
<!-- planner 가 코드 분석으로 확정한 핵심 사실 — executor 는 이를 전제로 작업 -->

1. **게이트 해제만으로는 D-01 불충족.** Action_FAIMeasurement.cs:147-158 의 deferred 게이트를 풀어 TryComposeAlign 을 호출해도, 두 곳에서 transform 이 세로 ROI 에 닿지 않는다:
   - (a) TryComposeAlign 의 ④단계(InspectionSequence.cs:537) 가 `detectSvc.TryFindDatum(refImage, datum, ...)` **1-image 오버로드**를 호출 → DualImage datum 의 세로 ROI(imageVertical) 를 검출하지 못함.
   - (b) `TryFindVerticalTwoHorizontalDualImage`(DatumFindingService.cs:762) 의 Vertical 라인 검출은 `TryFindLine`(:1747) 을 호출하는데, **TryFindLine 은 AlignPreTransform 을 소비하지 않는다**(ROI 중심 이동·strip θ회전 없음). Horizontal_A/B 는 `TryExtractEdgePoints`(:1904, transform 소비함) 를 타므로 이미 적용됨.

2. **측정 transform = alignRigid(패턴 pose).** TryComposeAlign 은 검출-datum transform(datumTransform) 대신 `alignRigid` 를 `_datumTransforms[datumKey]` 에 저장한다(InspectionSequence.cs:558). 검출-datum 은 먼 측정점에서 ~130px 오차가 있어 패턴 pose 를 측정 ROI 위치로 사용. DualImage 도 동일 정책 유지 — 이 plan 은 검출 경로에 transform 을 전파해 DetectedOrigin/기준선만 정확화하고, 측정 transform 은 alignRigid 그대로.

3. **#5 lenient 는 이미 구현됨.** Action_FAIMeasurement.cs:279-293(Measure 루프 IsDatumFailed → skip+NG+continue) + MarkAlignFailed(InspectionSequence.cs:382). EStep.DatumPhase 종료가 무조건 Step=Grab(:208). 이 plan 은 DualImage 분기에서 실패 시 동일 lenient 호출만 추가 + 전반 검증.
</critical_findings>

<notes>
- .NET Framework 4.8, C# 7.2 — switch expressions / nullable refs 금지.
- 모든 변경 라인에 `//260619 hbk Phase 57 #4 ...` 또는 `#5` 주석 필수.
- brace 스타일: Action_FAIMeasurement/InspectionSequence = K&R, DatumFindingService = Allman.
- HImage dispose: 추가되는 어떤 HImage 도 try/finally dispose (CLAUDE.md). 단 이 plan 은 기존 imgH/imgV finally(:159-162) 재사용 — 신규 grab 최소.
- **이중적용 가드 (확립 패턴):** TryComposeAlign 은 align 단독 경로로 호출되어 통상 existing transform 없음 → alignRigid 단독 저장. transform 1회만 적용 보장.
- **57-01 의존:** Wave 1(57-01) 이 동일 3파일에서 leveling 을 제거한 뒤 이 plan 이 ALIGN 을 보강한다. depends_on=["57-01"] — 같은 파일 충돌 회피.
</notes>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: TryFindLine 에 AlignPreTransform 소비 이식 (Vertical ROI transform 적용)</name>
  <read_first>
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (:1747-1898 TryFindLine 전체, :1904-1944 TryExtractEdgePoints 의 AlignPreTransform 소비 패턴 — 미러 원본, :762-907 TryFindVerticalTwoHorizontalDualImage 의 Vertical 호출부)
    - .planning/phases/57-pattern-roi-ux-datum-align-hardening/57-PATTERNS.md (#4 AlignPreTransform 소비 패턴 + 회전분 추출 패턴)
  </read_first>
  <action>
    DatumFindingService.cs 의 `TryFindLine`(:1747) 에 `TryExtractEdgePoints`(:1927-1944) 와 동형의 AlignPreTransform 소비 로직을 이식한다. 이로써 DualImage 의 Vertical 라인 검출 ROI 도 가로축에서 산출한 transform 으로 이동/회전된다 (D-01 "두 이미지 동일 transform" 충족의 마지막 누락 고리).

    1. TryFindLine 의 sanity clamp 블록(:1767-1774) 직후, bounding box 계산(:1776, `double halfW = roiLength1;`) **직전** 에 AlignPreTransform 소비 블록을 삽입. TryExtractEdgePoints 의 미러 패턴 그대로:
    ```csharp
    //260619 hbk Phase 57 #4 DualImage align — Vertical ROI 도 가로축 transform 소비(TryExtractEdgePoints :1932 미러).
    //  현재 TryFindLine 은 AlignPreTransform 미소비라 DualImage 세로 ROI 가 보정 누락 → 이식. alignRot=0(비-align) 이면 기존 축정렬 동작과 정확히 동일(회귀 0).
    double alignRot = 0.0;
    if (AlignPreTransform != null && AlignPreTransform.Length > 0)
    {
        try
        {
            HTuple atRow, atCol;
            HOperatorSet.AffineTransPoint2d(AlignPreTransform, roiRow, roiCol, out atRow, out atCol);
            roiRow = atRow.D;
            roiCol = atCol.D;
            alignRot = Math.Atan2(AlignPreTransform[3].D, AlignPreTransform[0].D);
        }
        catch { /* 변환 실패 시 원본 ROI 좌표/회전 유지 */ }
    }
    ```

    2. bounding box 계산(:1776-1784) 을 alignRot 회전 반영 enlarged AABB 로 교체. TryExtractEdgePoints :1946-1957 과 동일 공식:
    ```csharp
    // bounding box: alignRot 회전 반영 enlarged AABB. alignRot=0 → halfW=length1, halfH=length2 (기존 축정렬 정확 복원).
    double cosT     = Math.Cos(alignRot);
    double sinT     = Math.Sin(alignRot);
    double halfW    = Math.Abs(roiLength1 * cosT) + Math.Abs(roiLength2 * sinT);
    double halfH    = Math.Abs(roiLength1 * sinT) + Math.Abs(roiLength2 * cosT);
    double top      = roiRow - halfH;
    double bottom   = roiRow + halfH;
    double left     = roiCol - halfW;
    double right    = roiCol + halfW;
    double widthPx  = right - left;
    double heightPx = bottom - top;
    ```
    **주의 — datum 규약 length1=col(가로)/length2=row(세로):** TryExtractEdgePoints 는 halfCol/halfRow 명칭을 쓰지만 TryFindLine 의 기존 명칭은 halfW(=col)/halfH(=row) 이다. 위 코드는 TryFindLine 의 기존 명칭(halfW/halfH)을 유지하되 halfW↔length1, halfH↔length2 매핑이 기존(:1777-1778)과 동일하므로 alignRot=0 시 byte-identical. **명칭만 다르고 의미는 TryExtractEdgePoints :1950-1951 의 halfCol/halfRow 와 동일**하니 공식의 length1/length2 배치를 그대로 옮긴다.

    3. strip 샘플링 루프(:1810-1839) 의 `AppendEdgePointsFromStrip` 호출은 alignRot 을 추가로 전달할 필요가 있는지 TryExtractEdgePoints 와 비교 확인. **TryExtractEdgePoints 의 strip 루프가 alignRot 을 어떻게 소비하는지(:1959 이후) 확인 후 동일하게 적용** — measurePhi/strip 회전을 measurePhi 인자로 넘기는 구조라면 TryFindLine 의 AppendEdgePointsFromStrip 시그니처에도 동일 인자가 있어야 한다. AppendEdgePointsFromStrip 이 이미 strip 별 SmallestRectangle2 Phi 를 자동 산출한다면 enlarged AABB 만으로 충분할 수 있으니, **TryExtractEdgePoints 의 동작과 1:1 동형**이 되도록 맞춘다(불일치 시 Vertical 만 다르게 동작하는 회귀 위험).

    alignRot=0(비-align, 기존 모든 호출) 시 결과가 기존과 정확히 동일해야 한다(회귀 0). 이를 위해 enlarged AABB 공식이 alignRot=0 에서 halfW=length1/halfH=length2 로 복원됨을 확인.
  </action>
  <verify>
    <automated>grep -c "AlignPreTransform" WPF_Example/Halcon/Algorithms/DatumFindingService.cs  # → 5 (기존 :252/:1932 패턴 2블록 4건 + 프로퍼티 1건) 이 6+ 로 증가 (TryFindLine 신규 소비 추가)</automated>
  </verify>
  <acceptance_criteria>
    - TryFindLine 메서드 본문 내에 `if (AlignPreTransform != null && AlignPreTransform.Length > 0)` 블록 1건 존재 (`grep -n "AlignPreTransform" DatumFindingService.cs` 가 TryFindLine 범위 :1747-1898 내에서 매치).
    - TryFindLine 의 bounding box 가 `Math.Abs(roiLength1 * cosT)` 형태 enlarged AABB 사용.
    - `Math.Atan2(AlignPreTransform[3].D, AlignPreTransform[0].D)` 형태의 alignRot 추출이 TryFindLine 내 존재.
    - 변경 라인에 `//260619 hbk Phase 57 #4` 주석.
  </acceptance_criteria>
  <done>TryFindLine 이 AlignPreTransform 을 TryExtractEdgePoints 와 동형으로 소비. alignRot=0 시 회귀 0.</done>
</task>

<task type="auto">
  <name>Task 2: TryComposeAlign 의 DualImage 인지 검출 분기 + Action_FAIMeasurement DualImage align 배선</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (:447-565 TryComposeAlign, 특히 ④단계 :530-541 의 1-image TryFindDatum 호출)
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (:122-210 EStep.DatumPhase, DualImage 분기 :132-162, 단일이미지 align 분기 :176-203 = 미러 원본)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs (:75-107 2-image TryFindDatum 오버로드)
    - .planning/phases/57-pattern-roi-ux-datum-align-hardening/57-PATTERNS.md (#4 실행 지침)
  </read_first>
  <action>
    DualImage datum 의 align 경로를 단일이미지 분기와 구조 동형으로 배선한다. 두 군데 변경:

    **(A) InspectionSequence.TryComposeAlign — DualImage 면 2-image 검출 분기 (④단계 보정):**
    TryComposeAlign 의 ④단계(:530-541) 는 현재 항상 1-image 검출을 호출한다:
    ```csharp
    var detectSvc = new DatumFindingService();
    detectSvc.AlignPreTransform = alignRigid;
    HTuple datumTransform;
    string detErr;
    if (!detectSvc.TryFindDatum(refImage, datum, out datumTransform, out detErr))
    {
        error = "datum 검출 실패(패턴 보정 후): " + (detErr ?? "");
        return false;
    }
    ```
    이를 DualImage 인지 분기로 교체. TryComposeAlign 시그니처는 1개의 refImage 만 받으므로, **DualImage 의 두 번째 이미지(imgV)를 전달할 경로가 필요**하다. 다음 중 하나로 구현(executor 재량, 회귀 0 우선):
      - 옵션 A (권장): TryComposeAlign 에 optional 파라미터 `HImage refImageVertical = null` 를 추가 (`public bool TryComposeAlign(DatumConfig datum, HImage refImage, string modelPath, out string error)` → `(..., HImage refImageVertical = null, ...)` — 단 out 파라미터가 마지막이어야 하므로 `public bool TryComposeAlign(DatumConfig datum, HImage refImage, HImage refImageVertical, string modelPath, out string error)` 새 오버로드를 만들고 기존 4-arg 는 `refImageVertical=null` 로 위임). 기존 단일이미지 호출처(Action_FAIMeasurement.cs:180) 무변경 보장.
      - ④단계에서:
      ```csharp
      bool detectOk;
      if (datum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage && refImageVertical != null)
      {
          //260619 hbk Phase 57 #4 DualImage align — 가로/세로 두 이미지에 동일 alignRigid 적용 후 2-image 검출 (D-01)
          detectOk = detectSvc.TryFindDatum(refImage, refImageVertical, datum, out datumTransform, out detErr);
      }
      else
      {
          detectOk = detectSvc.TryFindDatum(refImage, datum, out datumTransform, out detErr);
      }
      if (!detectOk) { error = "datum 검출 실패(패턴 보정 후): " + (detErr ?? ""); return false; }
      ```
      - `detectSvc.AlignPreTransform = alignRigid;` 는 단일 인스턴스에 1회 set 되어 두 이미지 검출(Vertical=TryFindLine, Horizontal=TryExtractEdgePoints)에 모두 적용됨 (Task 1 으로 TryFindLine 도 소비). **패턴 매칭(① TryFindPose)은 refImage=imgH 에서만 수행** (D-04 세로엔 패턴 없음) — 이 부분 무변경.

    **(B) Action_FAIMeasurement.cs — DualImage 분기 align 배선:**
    DualImage 분기(:147-158) 의 deferred 게이트를 풀고 단일이미지 분기(:176-200) 와 동형으로 IsPatternAlignEnabled 분기를 추가한다. 현재:
    ```csharp
    //260618 hbk ... DualImage align 삽입은 후속 phase(deferred).
    string derr;
    if (!parentSeq.TryRunSingleDatum(datum, imgH, imgV, out derr)) { ... MarkDatumFailed(...); }
    ```
    이를:
    ```csharp
    //260619 hbk Phase 57 #4 DualImage align 배선 (deferred 게이트 해제, 단일이미지 분기 :176 미러). enabled → align 단독 경로, disabled → 기존 2-image 검출(off 회귀 0).
    if (datum.IsPatternAlignEnabled) {
        string modelPath = InspectionSequence.ResolveDatumModelPath(datum);
        string alignErr;
        if (!parentSeq.TryComposeAlign(datum, imgH, imgV, modelPath, out alignErr)) {
            string dn = datum.DatumName; if (dn == null) dn = "";
            string ae = alignErr; if (ae == null) ae = "";
            Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + dn + "' DualImage 패턴매칭 실패 (ALIGN_FAIL, skip): " + ae);
            datum.RuntimeDetectFailed = true;
            parentSeq.MarkAlignFailed(datum.DatumName); //260619 hbk Phase 57 #5 lenient — NG 강제, abort 안 함
        }
    } else {
        string derr;
        if (!parentSeq.TryRunSingleDatum(datum, imgH, imgV, out derr)) {
            string datumName = datum.DatumName; if (datumName == null) datumName = "";
            string derrStr = derr; if (derrStr == null) derrStr = "";
            Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + datumName + "' 검출 실패 (skip): " + derrStr);
            datum.RuntimeDetectFailed = true;
            parentSeq.MarkDatumFailed(datum.DatumName);
        }
    }
    ```
    finally 블록(imgH/imgV dispose :159-162) 은 그대로 유지. refImage=imgH 사용(가로축 패턴, D-04). TryComposeAlign 호출은 (A) 의 새 5-arg(refImageVertical 포함) 오버로드 사용.
  </action>
  <verify>
    <automated>grep -c "TryComposeAlign(datum, imgH, imgV" WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs  # → 1 (DualImage align 호출 추가됨)</automated>
  </verify>
  <acceptance_criteria>
    - Action_FAIMeasurement.cs DualImage 분기(AlgorithmTypeEnum == VerticalTwoHorizontalDualImage)에 `if (datum.IsPatternAlignEnabled)` 분기 존재.
    - DualImage align 실패 분기가 `MarkAlignFailed(datum.DatumName)` 호출 (MarkDatumFailed 아님 — ALIGN_FAIL 구분).
    - InspectionSequence.cs 에 DualImage 인지 2-image `TryFindDatum(refImage, refImageVertical, datum, ...)` 호출 존재.
    - 기존 단일이미지 호출처(Action_FAIMeasurement.cs:180 의 `TryComposeAlign(datum, img, modelPath, out alignErr)`) 가 무변경 (4-arg 오버로드 보존).
    - `grep "deferred" Action_FAIMeasurement.cs` 에서 DualImage align deferred 주석 0건 (게이트 해제됨).
    - 변경 라인에 `//260619 hbk Phase 57 #4` 주석.
  </acceptance_criteria>
  <done>DualImage datum 이 align-enabled 시 imgH 패턴매칭 → 단일 transform 으로 imgH/imgV 두 검출에 적용. 실패 시 lenient NG.</done>
</task>

<task type="auto">
  <name>Task 3: #5 lenient 정책 검증·보강 + SIMUL_MODE Debug/x64 빌드 검증</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (:122-210 EStep.DatumPhase 전체 실패 분기들 + :208 Step=Grab 종료, :275-293 Measure 루프 IsDatumFailed skip 게이트)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (:368-395 MarkDatumFailed/MarkAlignFailed/IsDatumFailed/IsAlignFailed)
    - .planning/phases/57-pattern-roi-ux-datum-align-hardening/57-PATTERNS.md (Shared Patterns — lenient)
  </read_first>
  <action>
    **(A) #5 lenient 검증·보강 (D-16):** EStep.DatumPhase 의 **모든** 실패 분기가 abort(throw / FinishAction(Error)) 없이 continue/skip 으로 끝나고, EStep.DatumPhase 종료가 무조건 `Step = (int)EStep.Grab;`(:208) 임을 확인한다. 코드 검증 체크리스트:
    1. DualImage 이미지 취득 실패 분기(:135-145) → continue (abort 없음) ✓ 확인.
    2. DualImage 검출/align 실패 분기(Task 2 에서 추가/수정) → MarkDatumFailed/MarkAlignFailed 후 continue 없이 try-finally 종료, 다음 datum 진행 → abort 없음 확인.
    3. 1-image 취득 실패(:165-172) / align 실패(:185-187) / 검출 실패(:196-198) → 모두 Mark*Failed + continue/진행, abort 없음 확인.
    4. EStep.DatumPhase case 종료가 `Step = (int)EStep.Grab;`(:208) 단일 경로임 확인 — datum 실패 여부와 무관하게 측정 진행.
    5. Measure 루프(:279-293) 의 `IsDatumFailed(meas.DatumRef)` → ClearResult + LastSkipReason(ALIGN_FAIL/DATUM_FAIL) + LastJudgement=false + continue 가 NG 처리를 보장함 확인.
    **보강이 필요한 경우만 코드 추가** (예: 어떤 실패 분기가 FinishAction(Error) 나 throw 로 끝나면 그것을 Mark*Failed + skip 으로 교체). 현 코드가 이미 lenient 면 코드 변경 없이 검증만 기록하고, SUMMARY 에 "DatumPhase 전 실패 분기 abort 0 / 종료 무조건 Grab" 증거를 남긴다. 변경 시 `//260619 hbk Phase 57 #5` 주석.

    **(B) 빌드 검증:** MSBuild Debug/x64 Rebuild 로 SIMUL_MODE 빌드:
    `MSBuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild /v:minimal`
    error CS 0건, 신규 warning CS 0건 확인. TryComposeAlign 오버로드 추가/TryFindLine 변경으로 인한 미해결 참조가 없는지 확인.
  </action>
  <verify>
    <automated>MSBuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild /v:minimal 2>&1 | grep -c "error CS"  # → 0</automated>
  </verify>
  <acceptance_criteria>
    - EStep.DatumPhase case 내에 `FinishAction(EContextResult.Error)` 또는 `throw` 가 datum 실패 경로에 0건 (lenient 보장).
    - EStep.DatumPhase 종료가 `Step = (int)EStep.Grab;` 단일 경로 (`grep -n "Step = (int)EStep.Grab" Action_FAIMeasurement.cs` 가 DatumPhase case 내 매치).
    - MSBuild Debug/x64 Rebuild → error CS 0, 신규 warning CS 0.
    - DualImage align 실패 시 LastSkipReason 이 "ALIGN_FAIL" 로 분기 가능 (IsAlignFailed → _alignFailedDatums 경유).
  </acceptance_criteria>
  <done>#5 lenient 정책 검증 완료(abort 0, 종료 무조건 Grab). DualImage align 실패도 NG 처리. 빌드 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 검사 이미지 → 패턴매칭 | 가로/세로 이미지가 같은 좌표계가 아니면 transform 오적용 (텔레센트릭 가정 위반) |
| align transform → 두 이미지 검출 | 단일 transform 이 두 이미지에 글로벌 유효해야 정확 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-03 | Tampering | DualImage transform 글로벌 유효성 | accept | 텔레센트릭 + Z축만 이동 셋업에서 두 이미지 동일 픽셀 좌표계 (D-02, 사용자 확인). SameFrame 가드(DatumFindingService.cs:89-98) 가 width/height 불일치 즉시 차단 |
| T-57-04 | Denial of Service | align 실패 → 검사 중단 | mitigate | lenient — MarkAlignFailed 후 측정 진행(NG), abort 없음. EStep.DatumPhase 종료 무조건 Grab (#5 검증) |
| T-57-05 | Tampering | TryFindLine 회귀 (비-align 호출) | mitigate | alignRot=0 시 enlarged AABB 가 기존 축정렬 bbox byte-identical 복원 → 회귀 0. 빌드 게이트 + TryExtractEdgePoints 동형 |
</threat_model>

<verification>
- DualImage datum align-enabled → imgH 패턴매칭 → 단일 transform 이 Vertical(TryFindLine) + Horizontal_A/B(TryExtractEdgePoints) 검출 ROI 모두 적용
- align 실패 → MarkAlignFailed → 측정 NG(ALIGN_FAIL), abort 없음
- 비-align(alignRot=0) 경로 회귀 0 (TryFindLine bbox byte-identical)
- MSBuild Debug/x64 → error CS 0, 신규 warning CS 0
</verification>

<success_criteria>
- #4: Side DualImage datum 이 가로축 단일 transform 으로 가로/세로 ROI 모두 보정 검출 (D-01~D-05)
- #5: 매칭/검출 실패 lenient (NG, abort 없음) 검증·보강 완료 (D-16)
- 기존 단일이미지 align 경로 + 비-align 검출 경로 회귀 0
- SIMUL_MODE Debug/x64 빌드 PASS
</success_criteria>

<output>
After completion, create `.planning/phases/57-pattern-roi-ux-datum-align-hardening/57-02-SUMMARY.md`
</output>
