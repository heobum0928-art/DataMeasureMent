---
phase: 57-pattern-roi-ux-datum-align-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
autonomous: true
requirements:
  - "#3 Datum 시각화 slate blue 통일 (긴 기준선 유지, recolor only)"

must_haves:
  truths:
    - "Datum 검출 기준선(수평/수직)이 slate blue 단일색으로 표시된다 (magenta 제거)"
    - "긴 관통 기준선(이미지 전체 걸치는 datum 축선)은 길이/좌표 무변경으로 유지된다"
    - "datum 무관 yellow(teach ROI 선택색/메인 십자/측정 raw 에지)는 절대 변경되지 않는다"
    - "SIMUL_MODE Debug/x64 빌드가 신규 error/warning 0 으로 통과"
  artifacts:
    - path: "WPF_Example/Halcon/Display/HalconDisplayService.cs"
      provides: "slate blue 통일된 RenderDatumFindResult 기준선"
      contains: "slate blue"
  key_links:
    - from: "RenderDatumFindResult 기준선 SetColor"
      to: "slate blue"
      via: "magenta → slate blue recolor"
      pattern: "SetColor\\(window, \"slate blue\"\\)"
---

<objective>
Datum 시각화의 색 중복(slate blue origin 십자 + magenta 긴 기준선)을 slate blue 단일색으로 통일한다. magenta 기준선을 **제거가 아니라 slate blue 로 recolor** — 정보(긴 관통 기준선)는 보존하고 색만 통일한다 (D-09/D-10).

Purpose: 큰 이미지(14208px) 확대 시 색이 중복되어 혼란스럽다는 사용자 피드백. slate blue 30px 화살표만으로는 큰 이미지에서 안 보이므로 긴 기준선은 유지하되 색만 origin 십자와 통일한다.

Output: RenderDatumFindResult 의 기준선이 slate blue. 길이/좌표 로직 무변경. datum 무관 yellow 미변경.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-pattern-roi-ux-datum-align-hardening/57-CONTEXT.md
@.planning/phases/57-pattern-roi-ux-datum-align-hardening/57-PATTERNS.md

<critical_findings>
<!-- planner 가 코드 분석으로 확정 — CONTEXT D-11 와 PATTERNS 의 불일치 명시 -->

**CONTEXT D-11 은 recolor 대상으로 magenta(:346) + legacy yellow(:52/:70/:166/:200) 를 인용했으나, PATTERNS.md 분석 + planner 코드 확인 결과 yellow 라인은 datum 무관이다:**
- `:52`/`:70` = teach 모드 ROI 선택 강조색(Circle/Rect ROI selection) — datum 시각화 아님.
- `:166` = Main-Crosshair 메인 십자선 — datum 아님.
- `:200` = FAI-EdgeRaw 측정 raw 에지점 — datum 아님.

**따라서 #3 의 실제 recolor 대상은 `RenderDatumFindResult`(:301) 내부 magenta 기준선(:346) 한 곳뿐이다.** datum 무관 yellow 는 절대 건드리지 말 것. executor 는 read_first 로 실제 대상을 확인하고, datum 관련 잔여 magenta/yellow 가 추가 발견되면 동일 recolor, datum 무관 색은 변경 금지.
</critical_findings>

<interfaces>
RenderDatumFindResult 의 recolor 대상 (HalconDisplayService.cs:343-347):
```csharp
//260619 hbk Phase 56 — datum 기준선 magenta 굵게...
HOperatorSet.SetColor(window, "magenta");   // ← "slate blue" 로 recolor
HOperatorSet.SetLineWidth(window, 3);
```
이 magenta SetColor 1회가 그 아래 수평 기준선(:360-363) + 수직 기준선(:381-383) 둘 다에 적용된다. 길이/좌표 로직(GetPart 전체관통 :348-358, hSin/hCos 방향 :360, 수직 vDir :366-384)은 그대로.

검증된 slate blue 패턴 (:311 — origin 십자, 동작 중):
```csharp
HOperatorSet.SetColor(window, "slate blue");
```
</interfaces>

<notes>
- .NET Framework 4.8, C# 7.2.
- 변경 라인에 `//260619 hbk Phase 57 #3 ...` 주석 필수.
- HalconDisplayService.cs = Allman brace 스타일.
- **HALCON SetColor 함정 (MEMORY feedback_halcon_setcolor_invalid_names):** 유효 색상명만. "slate blue"(공백, 소문자) 검증됨. 비표준명("purple" 류) → SetColor 예외 → catch swallow(:395-398) → 렌더 블록 전체 silent 미표시. "안 보임" 시 색상명 1순위 의심.
- 길이/두께 미세 조정은 Claude's Discretion (현 전체관통 유지가 기본). 색만 바꾸는 것이 사용자 의도 — 선 자체 삭제·길이 변경 금지 (D-10).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RenderDatumFindResult 기준선 magenta → slate blue recolor</name>
  <read_first>
    - WPF_Example/Halcon/Display/HalconDisplayService.cs (:301-399 RenderDatumFindResult 전체 — :311 slate blue origin 십자, :343-347 magenta 기준선 SetColor, :360-363 수평선, :381-383 수직선, :395-398 catch swallow)
    - .planning/phases/57-pattern-roi-ux-datum-align-hardening/57-PATTERNS.md (#3 섹션 + ⚠ yellow 무관 경고)
  </read_first>
  <action>
    HalconDisplayService.cs 의 RenderDatumFindResult(:301) 에서 datum 기준선 색을 통일한다:

    1. magenta 기준선 SetColor(:346) 를 변경:
       - 현재: `HOperatorSet.SetColor(window, "magenta");`
       - 변경: `HOperatorSet.SetColor(window, "slate blue");`
       - 위 주석(:343-345)의 "magenta 굵게" 표현을 "slate blue 굵게" 로 갱신하고 끝에 `//260619 hbk Phase 57 #3 datum 색상 slate blue 통일 (magenta→slate blue recolor, 길이/좌표 무변경 D-10)` 추가.

    2. **변경 금지 (회귀 가드):**
       - SetLineWidth(window, 3) (:347) — 두께 유지 (현 기본).
       - GetPart 전체관통 길이 로직(:348-358) — 무변경 (D-10 14208px 시인성).
       - 수평 기준선 좌표(:360-363) / 수직 기준선 좌표(:366-384) — 무변경.
       - slate blue origin 십자(:311) — 이미 slate blue, 무변경.
       - datum 무관 yellow(파일 내 :52/:70/:166/:200 류 teach ROI 선택색/메인 십자/측정 raw 에지) — **절대 변경 금지** (PATTERNS ⚠ 경고).

    3. **datum 관련 잔여 색 확인:** RenderDatumFindResult 및 datum 관련 렌더 메서드(RenderDatumOverlay 등) 안에서 magenta 또는 datum 기준선/origin 에 쓰인 yellow 가 추가로 있으면 동일하게 slate blue 로 recolor. 단 ROI 검색 박스 orange(RenderResultRoiBoxes 호출 인자) / 측정 green / FAI-EdgeRaw yellow / teach 선택 yellow 는 datum 기준선이 아니므로 변경 금지. 판단 기준: "datum 검출 origin/기준 축선 렌더" 인가.
  </action>
  <verify>
    <automated>grep -c "SetColor(window, \"magenta\")" WPF_Example/Halcon/Display/HalconDisplayService.cs  # → RenderDatumFindResult 의 magenta 가 0 이 되어야 함 (다른 메서드 magenta 있으면 그 수만큼; RenderDatumFindResult 범위 :301-399 내 magenta 0 확인)</automated>
  </verify>
  <acceptance_criteria>
    - RenderDatumFindResult(:301-399) 범위 내에 `SetColor(window, "magenta")` 0건.
    - RenderDatumFindResult 범위 내에 `SetColor(window, "slate blue")` 2건 이상 (origin 십자 :311 + 기준선 recolor).
    - GetPart 길이 로직(`HOperatorSet.GetPart`) + 수평/수직 기준선 DispLine 좌표 인자 무변경 (diff 에서 좌표 숫자 변경 0).
    - datum 무관 yellow(`SetColor(window, "yellow")` 의 teach 선택/메인 십자/EdgeRaw 사이트)는 변경 0건.
    - 변경 라인에 `//260619 hbk Phase 57 #3` 주석.
  </acceptance_criteria>
  <done>datum 기준선이 slate blue. 길이/좌표 무변경. datum 무관 색 미변경.</done>
</task>

<task type="auto">
  <name>Task 2: SIMUL_MODE Debug/x64 빌드 검증</name>
  <read_first>
    - WPF_Example/DatumMeasurement.csproj
  </read_first>
  <action>
    MSBuild Debug/x64 Rebuild 로 SIMUL_MODE 빌드를 수행하여 recolor 변경이 컴파일 깨짐 없이 완료되었는지 확인한다:
    `MSBuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild /v:minimal`
    error CS 0건, 신규 warning CS 0건 확인. (이 변경은 문자열 리터럴 1건 교체이므로 빌드 영향 최소.)
  </action>
  <verify>
    <automated>MSBuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild /v:minimal 2>&1 | grep -c "error CS"  # → 0</automated>
  </verify>
  <acceptance_criteria>
    - MSBuild Debug/x64 Rebuild → error CS 0, 신규 warning CS 0.
    - DatumMeasurement.exe 생성됨.
  </acceptance_criteria>
  <done>SIMUL_MODE Debug/x64 빌드 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 색상명 문자열 → HALCON SetColor | 비표준 색상명 시 예외 → catch swallow → silent 미표시 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-06 | Denial of Service | SetColor 색상명 | mitigate | "slate blue"(공백/소문자) 검증된 유효명 사용. :311 에서 이미 동작 중. 비표준명 금지 (MEMORY feedback_halcon_setcolor_invalid_names) |
| T-57-07 | Tampering | datum 무관 yellow 오변경 | mitigate | recolor 대상을 RenderDatumFindResult 내 magenta 기준선 1곳으로 한정. read_first 로 실제 대상 확인. acceptance_criteria 가 datum 무관 yellow 변경 0 강제 |
</threat_model>

<verification>
- RenderDatumFindResult 기준선이 slate blue (magenta 0)
- 길이/좌표 로직 무변경 (DispLine 좌표 인자 diff 0)
- datum 무관 yellow 미변경
- MSBuild Debug/x64 → error CS 0
</verification>

<success_criteria>
- #3: datum 시각화 slate blue 통일, 긴 기준선 유지(recolor only, D-09~D-11)
- datum 무관 색(yellow/orange/green) 회귀 0
- SIMUL_MODE Debug/x64 빌드 PASS
</success_criteria>

<output>
After completion, create `.planning/phases/57-pattern-roi-ux-datum-align-hardening/57-03-SUMMARY.md`
</output>
