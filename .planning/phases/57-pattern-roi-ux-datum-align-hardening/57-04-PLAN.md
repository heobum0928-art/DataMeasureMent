---
phase: 57-pattern-roi-ux-datum-align-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: true
requirements:
  - "#1 패턴 ROI1/ROI2 버튼 나란히 배치 + 단일 패턴 경고+override"

must_haves:
  truths:
    - "패턴1/패턴2 ROI 그리기 버튼이 UI 상 나란히(인접) 배치된다"
    - "패턴2 미확보 상태로 모델 생성 시 경고 후 사용자 OK 시 단일 패턴으로 진행(override), Cancel 시 중단"
    - "패턴1 미확보는 하드 블록(기존 동작 유지)"
    - "SIMUL_MODE Debug/x64 빌드가 신규 error/warning 0 으로 통과"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml"
      provides: "패턴1/패턴2 버튼 인접 배치"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "패턴2 경고+override (InvokeCreatePatternModel)"
      contains: "ShowConfirmation"
  key_links:
    - from: "InvokeCreatePatternModel"
      to: "단일 패턴 경고"
      via: "PatternRoi2_Length 판정 → ShowConfirmation OK/Cancel"
      pattern: "PatternRoi2_Length"
---

<objective>
패턴 매칭 ROI 입력 UX 를 명확/안전하게 한다: (1) 패턴1/패턴2 ROI 그리기 버튼을 UI 상 나란히 배치하고, (2) 패턴을 1개만 그린 채 모델 생성 시 경고 후 override 허용한다.

Purpose: 현재 버튼 배치가 분리되어 있고(패턴1 → 모델생성 → 패턴2), 단일 패턴은 회전 정밀도가 낮음에도 경고가 없다. 단, 좋은 패턴 피처가 1개뿐인 부품의 탈출구(override)는 유지 — 하드 블록 아님 (D-06/D-07/D-08).

Output: 패턴1/패턴2 버튼 인접. 패턴2 미설정 모델 생성 시 경고+override.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-pattern-roi-ux-datum-align-hardening/57-CONTEXT.md
@.planning/phases/57-pattern-roi-ux-datum-align-hardening/57-PATTERNS.md

<interfaces>
현재 버튼 순서 (MainView.xaml):
- `btn_drawPatternRoi` (패턴 1, :181-200)
- `btn_createPatternModel` (패턴 모델 생성, :203-222)
- `btn_drawPatternRoi2` (패턴 2, :225-244)
→ 목표: 패턴1, 패턴2, [패턴 모델 생성] 순 (패턴1/패턴2 인접).

CustomMessageBox 확인 다이얼로그 (UI/Dialog/CustomMessageBox.cs:68):
```csharp
public static MessageBoxResult ShowConfirmation(string title, string message, MessageBoxButton buttons);
// 반환: MessageBoxResult.OK / Cancel / Yes / No / None
```

InvokeCreatePatternModel 경고 삽입 지점 (MainView.xaml.cs:2796-2799 — 패턴1 가드 직후):
```csharp
// W2: PatternRoi 미확보 시 모델 생성 차단 (silent 실패 0)
if (datum.PatternRoi_Length1 <= 0.0 || datum.PatternRoi_Length2 <= 0.0) {
    CustomMessageBox.Show("모델 생성 실패", "패턴 ROI(Rect) 를 먼저 그리세요. ([패턴 ROI] 버튼)");
    return;   // ← 패턴1 미확보는 하드 블록 유지
}
```

패턴2 판정 조건 (InspectionSequence.cs:490 폴백 조건 미러):
```csharp
datum.PatternRoi2_Length1 <= 0.0 || datum.PatternRoi2_Length2 <= 0.0   // 패턴2 미설정
```

단일 패턴 폴백 경로 (TryComposeAlign InspectionSequence.cs:490-514) — **변경 없음** (D-08): 패턴2 없으면 단일 패턴 θ 사용.
</interfaces>

<notes>
- .NET Framework 4.8, C# 7.2.
- 변경 라인에 `//260619 hbk Phase 57 #1 ...` 주석 필수.
- MainView.xaml.cs = K&R brace 스타일.
- 버튼 재배치는 **XAML 요소 순서만 이동** — 스타일/Click 핸들러/x:Name 무변경 (회귀 0). `Margin="0,0,4,0"` 간격 관습 유지.
- 경고 문구는 Claude's Discretion (D-06 취지: "패턴 2 권장 — 단일은 회전 정밀도 저하").
- ShowConfirmation 의 정확한 MessageBoxResult 반환값(OK vs Yes)은 CustomMessageBox.cs 를 read_first 로 확인 후 분기.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MainView.xaml 패턴1/패턴2 버튼 인접 재배치</name>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml (:180-244 btn_drawPatternRoi / btn_createPatternModel / btn_drawPatternRoi2 3버튼 블록 전체)
    - .planning/phases/57-pattern-roi-ux-datum-align-hardening/57-PATTERNS.md (#1 버튼 나란히 배치 섹션)
  </read_first>
  <action>
    MainView.xaml 에서 패턴 버튼 순서를 재배치한다 (D-07). 현재: `btn_drawPatternRoi`(패턴1, :181-200) → `btn_createPatternModel`(:203-222) → `btn_drawPatternRoi2`(패턴2, :225-244).

    목표 순서: **패턴1 → 패턴2 → 패턴 모델 생성**.

    `btn_drawPatternRoi2`(패턴2) 의 `<Button ...>...</Button>` 요소 블록 전체(:225-244, 위 주석 :224 포함)를 잘라내어 `btn_drawPatternRoi`(패턴1, :200 닫는 `</Button>`) 직후, `btn_createPatternModel`(패턴 모델 생성, :202 주석) 직전 위치로 이동한다.

    **변경 금지:**
    - 각 버튼의 x:Name / Click 핸들러 / Style / Background / Template / ToolTip — 무변경 (요소 순서만 이동).
    - Margin 은 기존 `Margin="0,0,4,0"` 유지 (인접 버튼 간격 관습).

    이동한 패턴2 버튼 블록 위 주석에 `//260619 hbk Phase 57 #1 패턴2 버튼을 패턴1 옆으로 이동 (나란히 배치, D-07)` 형태 XAML 주석(`<!-- ... -->`) 추가.

    재배치 후 StackPanel 내 순서가: ... btn_teachDatum → btn_drawPatternRoi(패턴1) → btn_drawPatternRoi2(패턴2) → btn_createPatternModel(패턴 모델 생성) → btn_swapHorizontal ... 인지 확인.
  </action>
  <verify>
    <automated>grep -n "x:Name=\"btn_drawPatternRoi\"\|x:Name=\"btn_drawPatternRoi2\"\|x:Name=\"btn_createPatternModel\"" WPF_Example/UI/ContentItem/MainView.xaml  # → 라인번호 순서가 패턴1 < 패턴2 < 모델생성 이어야 함</automated>
  </verify>
  <acceptance_criteria>
    - MainView.xaml 에서 `btn_drawPatternRoi`(패턴1) 라인번호 < `btn_drawPatternRoi2`(패턴2) 라인번호 < `btn_createPatternModel`(모델생성) 라인번호.
    - 세 버튼의 x:Name/Click/Style/ToolTip 속성 문자열 무변경 (diff 에서 속성 변경 0, 위치만 이동).
    - XAML 이 well-formed (빌드 PASS — Task 3 게이트).
  </acceptance_criteria>
  <done>패턴1/패턴2 버튼 인접 배치. 속성 무변경.</done>
</task>

<task type="auto">
  <name>Task 2: InvokeCreatePatternModel 에 패턴2 미설정 경고+override 삽입</name>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (:2776-2860 InvokeCreatePatternModel 전체 — :2796-2799 패턴1 가드, :2826 패턴2 판정)
    - WPF_Example/UI/Dialog/CustomMessageBox.cs (:24 Show, :68 ShowConfirmation 시그니처 + MessageBoxResult 반환값 확인)
    - .planning/phases/57-pattern-roi-ux-datum-align-hardening/57-PATTERNS.md (#1 실행 지침)
  </read_first>
  <action>
    InvokeCreatePatternModel(:2776) 에서 패턴2 미설정 시 경고+override 를 추가한다.

    1. 패턴1 가드(:2796-2799)는 **유지** — 패턴1 은 필수 하드 블록 (변경 금지).

    2. 패턴1 가드 직후(:2799 `}` 다음, :2801 modelPath 도출 직전) 에 패턴2 경고+override 삽입:
    ```csharp
    //260619 hbk Phase 57 #1 패턴2 미설정 경고+override (D-06 — 하드 블록 아님, 회전 정밀도 안전장치).
    //  단일 패턴 폴백 경로(TryComposeAlign InspectionSequence.cs:490-514) 는 그대로 — 패턴2 없으면 단일 패턴 θ 사용 (D-08).
    if (datum.PatternRoi2_Length1 <= 0.0 || datum.PatternRoi2_Length2 <= 0.0) {
        MessageBoxResult confirm = CustomMessageBox.ShowConfirmation(
            "패턴 2 미설정",
            "패턴 2 를 그리지 않았습니다. 단일 패턴은 회전(tilt) 보정 정밀도가 낮습니다.\n\n그래도 단일 패턴으로 모델을 생성하시겠습니까?\n([취소] 후 [패턴 2] 버튼으로 반대 대각 끝에 패턴을 추가 권장)",
            MessageBoxButton.OKCancel);
        if (confirm != MessageBoxResult.OK) {
            return; // 사용자가 취소 → 모델 생성 중단 (하드 블록 아님 — 단순 중단)
        }
        // OK → 단일 패턴으로 진행 (override)
    }
    ```
    **주의:** ShowConfirmation 의 실제 반환 enum 값(OK / Yes 등)을 read_first 로 CustomMessageBox.cs 에서 확인하고, `MessageBoxButton.OKCancel` 전달 시 긍정 응답이 `MessageBoxResult.OK` 인지 검증. 다르면(예: YesNo 형식만 지원) `MessageBoxButton.YesNo` + `!= MessageBoxResult.Yes` 로 맞춘다. `using System.Windows;` (MessageBoxResult/MessageBoxButton) 가 필요하면 파일 상단 using 확인 후 추가.

    3. 나머지 모델 생성 로직(:2801~2859, modelPath 도출 / TryCreateModel / TryFindRefPose / 패턴2 모델 생성 :2826~) 은 무변경. 패턴2 미설정 시 단일 패턴 alignMsg(:2845-2846) 가 이미 안내 문구를 보여주므로 그대로 둔다.
  </action>
  <verify>
    <automated>grep -c "ShowConfirmation" WPF_Example/UI/ContentItem/MainView.xaml.cs  # → 1 이상 (패턴2 경고 추가)</automated>
  </verify>
  <acceptance_criteria>
    - InvokeCreatePatternModel 내에 `datum.PatternRoi2_Length1 <= 0.0 || datum.PatternRoi2_Length2 <= 0.0` 판정 + `CustomMessageBox.ShowConfirmation(...)` 호출 존재.
    - 부정 응답(취소) 시 `return;` (모델 생성 중단), 긍정 응답 시 진행(override) — 패턴1 가드처럼 무조건 return 아님.
    - 패턴1 가드(:2796-2799)는 무변경 (여전히 하드 블록).
    - 패턴2 모델 생성 분기(:2826~) 무변경.
    - 변경 라인에 `//260619 hbk Phase 57 #1` 주석.
  </acceptance_criteria>
  <done>패턴2 미설정 시 경고 후 OK=진행(override)/Cancel=중단. 패턴1 하드 블록 유지. 단일 패턴 폴백 무변경.</done>
</task>

<task type="auto">
  <name>Task 3: SIMUL_MODE Debug/x64 빌드 검증</name>
  <read_first>
    - WPF_Example/DatumMeasurement.csproj
  </read_first>
  <action>
    MSBuild Debug/x64 Rebuild 로 SIMUL_MODE 빌드를 수행하여 XAML 재배치 + 경고 추가가 컴파일/XAML 깨짐 없이 완료되었는지 확인한다:
    `MSBuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild /v:minimal`
    error CS / XAML 파싱 오류 0건, 신규 warning CS 0건 확인. MessageBoxResult/MessageBoxButton using 누락 시 CS0103/CS0246 으로 드러나므로 추가.
  </action>
  <verify>
    <automated>MSBuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild /v:minimal 2>&1 | grep -c "error CS\|error MC\|error MarkupCompile"  # → 0</automated>
  </verify>
  <acceptance_criteria>
    - MSBuild Debug/x64 Rebuild → error CS / XAML markup error 0, 신규 warning CS 0.
    - DatumMeasurement.exe 생성됨.
  </acceptance_criteria>
  <done>SIMUL_MODE Debug/x64 빌드 PASS (XAML 재배치 + 경고 추가 정상).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 입력(패턴 ROI) → 모델 생성 | 패턴 미확보 상태로 생성 시 무효 모델 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-08 | Tampering | 단일 패턴 모델 생성 | accept | 단일 패턴은 회전 정밀도 저하 위험이나 좋은 피처 1개 부품 탈출구 필요 (D-06). 경고+override 로 사용자 인지 후 진행. 패턴1 미확보는 하드 블록 유지 |
</threat_model>

<verification>
- 패턴1/패턴2 버튼 인접 배치 (라인번호 순서 패턴1 < 패턴2 < 모델생성)
- 패턴2 미설정 모델 생성 → 경고 → OK=override / Cancel=중단
- 패턴1 미확보 하드 블록 유지
- MSBuild Debug/x64 → error 0
</verification>

<success_criteria>
- #1: 패턴1/패턴2 버튼 나란히 배치 (D-07) + 단일 패턴 경고+override (D-06) + 단일 폴백 보존 (D-08)
- SIMUL_MODE Debug/x64 빌드 PASS
</success_criteria>

<output>
After completion, create `.planning/phases/57-pattern-roi-ux-datum-align-hardening/57-04-SUMMARY.md`
</output>
