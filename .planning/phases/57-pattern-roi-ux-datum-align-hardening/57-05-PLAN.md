---
phase: 57-pattern-roi-ux-datum-align-hardening
plan: 05
type: execute
wave: 2
depends_on: ["57-03", "57-04"]
files_modified:
  - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
  - WPF_Example/UI/ContentItem/MainView.xaml
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
autonomous: true
requirements:
  - "#2 패턴 ROI 표시/숨김 토글 (SetDatumOverlayVisible 미러)"

must_haves:
  truths:
    - "결과화면에 패턴 ROI 가시성 토글 체크박스가 있다"
    - "토글 ON 시 datum 의 패턴1/패턴2 ROI 박스가 결과화면에 렌더된다"
    - "토글 OFF 시 패턴 ROI 박스가 숨겨지고 즉시 재렌더된다"
    - "SIMUL_MODE Debug/x64 빌드가 신규 error/warning 0 으로 통과"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs"
      provides: "SetPatternRoiOverlayVisible + 패턴 ROI 렌더 게이트 (SetDatumOverlayVisible 미러)"
      contains: "SetPatternRoiOverlayVisible"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml"
      provides: "패턴 ROI 토글 체크박스 chk_overlayPattern"
      contains: "chk_overlayPattern"
  key_links:
    - from: "MainView.chk_overlayPattern"
      to: "MainResultViewerControl.SetPatternRoiOverlayVisible"
      via: "Checked/Unchecked 핸들러"
      pattern: "SetPatternRoiOverlayVisible"
---

<objective>
결과화면에 패턴 매칭 ROI(패턴1/패턴2) 의 표시/숨김 토글을 추가한다. 현재 패턴 ROI 는 teach 피드백만 있고 결과화면 렌더/토글이 없다. 기존 datum/측정 토글(SetDatumOverlayVisible) 미러 패턴으로 구현한다 (D-15).

Purpose: 작업자가 결과화면에서 패턴 매칭 ROI 위치를 확인/숨김 전환할 수 있게 하여, datum 검출 ROI(orange)·측정 ROI(green)·datum 기준선(slate blue)과 구분되는 패턴 ROI 시각 확인 제공.

Output: chk_overlayPattern 체크박스 + SetPatternRoiOverlayVisible 토글 + 패턴 ROI 렌더 게이트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-pattern-roi-ux-datum-align-hardening/57-CONTEXT.md
@.planning/phases/57-pattern-roi-ux-datum-align-hardening/57-PATTERNS.md

<interfaces>
**미러 원본 = datum 토글 (MainResultViewerControl.xaml.cs):**
```csharp
private bool _datumOverlayVisible = true;                 // :584 게이트 필드
public void SetDatumOverlayVisible(bool visible)          // :614 토글 setter
{
    _datumOverlayVisible = visible;
    Render();                                             // 즉시 재렌더
}
```
RenderNow 게이트 (MainResultViewerControl.xaml.cs:831-834):
```csharp
if (_datumOverlayVisible && _resultDatumRoiOverlays != null && _resultDatumRoiOverlays.Count > 0)
{
    _displayService.RenderResultRoiBoxes(ViewerHost.HalconWindow, _resultDatumRoiOverlays, "orange", 2);
}
```

**패턴 ROI 박스 렌더 (HalconDisplayService.cs):**
```csharp
// rects = List<double[]> 각 원소 {row, col, phi, l1, l2}
public void RenderResultRoiBoxes(HWindow window, List<double[]> rects, string color, int lineWidth);
```

**패턴 ROI 좌표 (DatumConfig):**
```csharp
PatternRoi_Row / PatternRoi_Col / PatternRoi_Phi / PatternRoi_Length1 / PatternRoi_Length2
PatternRoi2_Row / PatternRoi2_Col / PatternRoi2_Phi / PatternRoi2_Length1 / PatternRoi2_Length2
```

**체크박스 미러 (MainView.xaml:314-317 chk_overlayDatum):**
```xml
<CheckBox x:Name="chk_overlayDatum" Content="Datum 라인"
    IsChecked="True"
    Checked="Chk_overlayDatum_Changed" Unchecked="Chk_overlayDatum_Changed"
    Foreground="White" VerticalAlignment="Center" Margin="0,0,4,0" FontSize="12"/>
```
**핸들러 미러 (MainView.xaml.cs:2197-2200):**
```csharp
private void Chk_overlayDatum_Changed(object sender, RoutedEventArgs e) {
    if (halconViewer == null) return;
    halconViewer.SetDatumOverlayVisible(chk_overlayDatum.IsChecked == true);
}
```

**결과 datum 리스트 소스 (MainResultViewerControl):** `_resultDatumOverlays` (List<DatumConfig>) — 패턴 ROI 좌표를 가진 datum 들. RenderNow 의 패턴 게이트는 이 리스트를 순회하며 각 datum 의 PatternRoi*/PatternRoi2* 를 double[] 로 구성해 렌더.
</interfaces>

<notes>
- .NET Framework 4.8, C# 7.2.
- 변경 라인에 `//260619 hbk Phase 57 #2 ...` 주석 필수.
- MainResultViewerControl.xaml.cs / MainView.xaml.cs = K&R brace 스타일.
- **색상명 유효성 (#3 함정 공유, MEMORY feedback_halcon_setcolor_invalid_names):** 패턴 ROI 색은 유효 HALCON 색상명만. 측정(green)/datum 검색(orange)/datum 기준선(slate blue, 57-03 후) 과 구분되는 유효명 사용 — 권장 `"cyan"` (검증된 유효명). "slate blue" 는 datum 기준선과 겹치므로 패턴엔 cyan 권장.
- **57-03/57-04 의존 (Wave 2):** 57-03 이 HalconDisplayService 의 datum 색상을 변경하고, 57-04 가 MainView.xaml/.xaml.cs 의 패턴 버튼/InvokeCreatePatternModel 을 변경한다. 이 plan 은 동일 파일들을 추가 편집하므로 depends_on=["57-03","57-04"] — Wave 2 충돌 회피. RenderResultRoiBoxes 시그니처는 57-03 이 변경하지 않음(색상 인자만 호출부에서 다름).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MainResultViewerControl 에 패턴 ROI 토글 게이트 + 렌더 추가 (SetDatumOverlayVisible 미러)</name>
  <read_first>
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs (:575-660 오버레이 필드/setter 영역, :803-844 RenderNow 게이트 영역, 특히 :831-834 datum ROI 게이트 = 미러 원본, :812-823 _resultDatumOverlays 순회)
    - WPF_Example/Halcon/Display/HalconDisplayService.cs (RenderResultRoiBoxes 시그니처 + DatumConfig PatternRoi* 필드 — 57-03 변경분 반영된 최신 상태)
    - .planning/phases/57-pattern-roi-ux-datum-align-hardening/57-PATTERNS.md (#2 실행 지침)
  </read_first>
  <action>
    MainResultViewerControl.xaml.cs 에 패턴 ROI 토글을 datum 토글 동형으로 추가한다:

    1. 게이트 필드 추가 (datum 필드 :584 옆):
    ```csharp
    //260619 hbk Phase 57 #2 패턴 ROI 토글 게이트 (기본 ON, _datumOverlayVisible 미러)
    private bool _patternRoiOverlayVisible = true;
    ```

    2. 토글 setter 추가 (SetDatumOverlayVisible :614 옆, 동형):
    ```csharp
    //260619 hbk Phase 57 #2 패턴 ROI 가시성 토글 (MainView 체크박스에서 호출). 즉시 재렌더.
    public void SetPatternRoiOverlayVisible(bool visible)
    {
        _patternRoiOverlayVisible = visible;
        Render();
    }
    ```

    3. RenderNow 에 패턴 ROI 렌더 게이트 추가 (datum ROI 게이트 :831-834 직후, 동형):
    ```csharp
    //260619 hbk Phase 57 #2 패턴 매칭 ROI (cyan, datum orange / 측정 green / datum 기준선 slate blue 와 구분). 패턴 토글 게이트.
    if (_patternRoiOverlayVisible && _resultDatumOverlays != null && _resultDatumOverlays.Count > 0)
    {
        var patternRects = new List<double[]>();
        foreach (DatumConfig d in _resultDatumOverlays)
        {
            if (d == null) continue;
            if (d.PatternRoi_Length1 > 0.0 && d.PatternRoi_Length2 > 0.0)
                patternRects.Add(new double[] { d.PatternRoi_Row, d.PatternRoi_Col, d.PatternRoi_Phi, d.PatternRoi_Length1, d.PatternRoi_Length2 });
            if (d.PatternRoi2_Length1 > 0.0 && d.PatternRoi2_Length2 > 0.0)
                patternRects.Add(new double[] { d.PatternRoi2_Row, d.PatternRoi2_Col, d.PatternRoi2_Phi, d.PatternRoi2_Length1, d.PatternRoi2_Length2 });
        }
        if (patternRects.Count > 0)
            _displayService.RenderResultRoiBoxes(ViewerHost.HalconWindow, patternRects, "cyan", 2);
    }
    ```
    **double[] 원소 순서는 RenderResultRoiBoxes 가 기대하는 {row,col,phi,l1,l2} 와 일치해야 한다 — read_first 로 RenderResultRoiBoxes 의 인덱싱(rect[0]=row 등)을 확인하고 맞춘다.** _resultDatumRoiOverlays(orange, 보정 검색 ROI)의 double[] 구성과 동일 규약인지 대조.

    4. `using System.Collections.Generic;` (List<double[]>) 가 이미 있는지 확인 (datum 코드가 이미 사용 중이므로 보통 존재).
  </action>
  <verify>
    <automated>grep -c "SetPatternRoiOverlayVisible\|_patternRoiOverlayVisible" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs  # → 4+ (필드 1 선언 + setter 2 + 게이트 1)</automated>
  </verify>
  <acceptance_criteria>
    - `private bool _patternRoiOverlayVisible = true;` 필드 존재.
    - `public void SetPatternRoiOverlayVisible(bool visible)` 메서드가 `_patternRoiOverlayVisible = visible; Render();` 본문으로 존재.
    - RenderNow 내 `if (_patternRoiOverlayVisible ...)` 게이트가 PatternRoi_Length1/PatternRoi2_Length1 sentinel 판정 + RenderResultRoiBoxes(..., "cyan", 2) 호출 포함.
    - 패턴 ROI double[] 가 {row,col,phi,l1,l2} 순서로 RenderResultRoiBoxes 규약과 일치.
    - 변경 라인에 `//260619 hbk Phase 57 #2` 주석.
  </acceptance_criteria>
  <done>패턴 ROI 토글 게이트/setter/렌더가 datum 토글과 동형으로 추가됨.</done>
</task>

<task type="auto">
  <name>Task 2: MainView 패턴 ROI 토글 체크박스 + 핸들러 추가 (chk_overlayDatum 미러)</name>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml (:308-318 chk_overlayMeasure / chk_overlayDatum 체크박스 영역 — 미러 원본. 57-04 가 같은 파일 패턴 버튼 영역을 편집했으므로 최신 상태 확인)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (:2192-2200 Chk_overlayMeasure_Changed / Chk_overlayDatum_Changed 핸들러 — 미러 원본)
  </read_first>
  <action>
    MainView 에 패턴 ROI 토글 체크박스를 datum 토글 동형으로 추가한다:

    1. MainView.xaml: chk_overlayDatum(:314-317) 직후에 패턴 토글 체크박스 추가 (동형):
    ```xml
    <!--260619 hbk Phase 57 #2 패턴 매칭 ROI 표시/숨김 토글 (기본 ON, chk_overlayDatum 미러)-->
    <CheckBox x:Name="chk_overlayPattern" Content="패턴 ROI"
        IsChecked="True"
        Checked="Chk_overlayPattern_Changed" Unchecked="Chk_overlayPattern_Changed"
        Foreground="White" VerticalAlignment="Center" Margin="0,0,4,0" FontSize="12"/>
    ```
    같은 StackPanel(:318 `</StackPanel>` 직전) 안에 두어 측정/Datum 토글과 나란히 배치.

    2. MainView.xaml.cs: Chk_overlayDatum_Changed(:2197-2200) 직후에 핸들러 추가 (동형):
    ```csharp
    //260619 hbk Phase 57 #2 패턴 ROI 토글 핸들러 (Chk_overlayDatum_Changed 미러)
    private void Chk_overlayPattern_Changed(object sender, RoutedEventArgs e) {
        if (halconViewer == null) return;
        halconViewer.SetPatternRoiOverlayVisible(chk_overlayPattern.IsChecked == true);
    }
    ```

    체크박스 x:Name(chk_overlayPattern) 과 핸들러명(Chk_overlayPattern_Changed) 이 XAML/code-behind 간 정확히 일치하는지 확인 (불일치 시 XAML markup error).
  </action>
  <verify>
    <automated>grep -c "chk_overlayPattern\|Chk_overlayPattern_Changed" WPF_Example/UI/ContentItem/MainView.xaml WPF_Example/UI/ContentItem/MainView.xaml.cs  # → XAML 2+ (x:Name + Checked/Unchecked) , cs 2+ (핸들러 정의 + 호출)</automated>
  </verify>
  <acceptance_criteria>
    - MainView.xaml 에 `x:Name="chk_overlayPattern"` CheckBox 존재 (Content "패턴 ROI", IsChecked="True", Checked/Unchecked="Chk_overlayPattern_Changed").
    - MainView.xaml.cs 에 `private void Chk_overlayPattern_Changed(object sender, RoutedEventArgs e)` 핸들러가 `halconViewer.SetPatternRoiOverlayVisible(chk_overlayPattern.IsChecked == true);` 호출 포함.
    - 체크박스가 측정/Datum 토글과 같은 StackPanel 내 배치.
    - 변경 라인에 `//260619 hbk Phase 57 #2` 주석.
  </acceptance_criteria>
  <done>chk_overlayPattern 체크박스 + Chk_overlayPattern_Changed 핸들러가 datum 토글과 동형으로 추가됨.</done>
</task>

<task type="auto">
  <name>Task 3: SIMUL_MODE Debug/x64 빌드 검증</name>
  <read_first>
    - WPF_Example/DatumMeasurement.csproj
  </read_first>
  <action>
    MSBuild Debug/x64 Rebuild 로 SIMUL_MODE 빌드를 수행하여 패턴 토글 추가가 컴파일/XAML 깨짐 없이 완료되었는지 확인한다:
    `MSBuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild /v:minimal`
    error CS / XAML markup error 0건, 신규 warning CS 0건 확인. chk_overlayPattern x:Name ↔ Chk_overlayPattern_Changed 핸들러 불일치 시 XAML 컴파일 오류로 드러나므로 수정.
  </action>
  <verify>
    <automated>MSBuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild /v:minimal 2>&1 | grep -c "error CS\|error MC\|error MarkupCompile"  # → 0</automated>
  </verify>
  <acceptance_criteria>
    - MSBuild Debug/x64 Rebuild → error CS / XAML markup error 0, 신규 warning CS 0.
    - DatumMeasurement.exe 생성됨.
  </acceptance_criteria>
  <done>SIMUL_MODE Debug/x64 빌드 PASS (패턴 토글 wiring 정상).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 패턴 ROI 색상 문자열 → HALCON SetColor | 비표준 색상명 시 silent 미표시 |
| XAML x:Name ↔ code-behind 핸들러 | 명칭 불일치 시 markup error / null ref |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-09 | Denial of Service | 패턴 ROI SetColor 색상명 | mitigate | "cyan" 검증된 유효명 사용 (RenderResultRoiBoxes 가 orange/green 으로 이미 동작). 비표준명 금지 |
| T-57-10 | Information Disclosure | 패턴 ROI 색 중복 | accept | datum(orange)/측정(green)/기준선(slate blue) 과 cyan 으로 구분 — 시각 구별 가능 |
</threat_model>

<verification>
- 결과화면에 chk_overlayPattern 체크박스 존재
- 토글 ON → datum 의 패턴1/패턴2 ROI 박스 cyan 렌더, OFF → 숨김 + 즉시 재렌더
- datum/측정 토글(기존) 회귀 0
- MSBuild Debug/x64 → error 0
</verification>

<success_criteria>
- #2: 패턴 ROI 표시/숨김 토글 (SetDatumOverlayVisible 미러, D-15)
- 기존 datum/측정 토글 회귀 0
- SIMUL_MODE Debug/x64 빌드 PASS
</success_criteria>

<output>
After completion, create `.planning/phases/57-pattern-roi-ux-datum-align-hardening/57-05-SUMMARY.md`
</output>
