---
phase: 57.1-pattern-roi-verification-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: true
requirements: [D-03, D-01]
must_haves:
  truths:
    - "Datum 노드 단독 선택 시 cyan 패턴 보정 ROI 박스가 결과 화면에 표시된다 (Top/Side/Bottom 무차별)"
    - "Datum 노드 선택 시 chk_overlayPattern 토글로 cyan 패턴 ROI 표시/숨김이 일관 동작한다"
    - "Measurement/Shot/FAI 노드 선택의 기존 패턴/Datum 오버레이 표시는 회귀 0"
  artifacts:
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "Datum 노드 분기에서 ShowResultDatumOverlays 호출로 _resultDatumOverlays 채움"
      contains: "ShowResultDatumOverlays"
  key_links:
    - from: "InspectionListView.InspectionList_SelectionChanged (Datum 분기)"
      to: "MainView.ShowResultDatumOverlays"
      via: "selected DatumConfig 단일-원소 List 전달"
      pattern: "ShowResultDatumOverlays"
    - from: "MainView.ShowResultDatumOverlays"
      to: "MainResultViewerControl.SetResultDatumOverlays → cyan 렌더 게이트 (_resultDatumOverlays.Count > 0)"
      via: "_resultDatumOverlays 채워짐"
      pattern: "SetResultDatumOverlays"
---

<objective>
D-03 + D-01: 패턴 ROI(cyan) 시각화 렌더 조건을 안정화한다.

현상(코드 확인): MainResultViewerControl.Render() 의 cyan 패턴 ROI 렌더 게이트(`MainResultViewerControl.xaml.cs:846`)는
`_patternRoiOverlayVisible && _resultDatumOverlays != null && _resultDatumOverlays.Count > 0` 조건이다.
`_resultDatumOverlays` 는 Measurement/Shot/FAI 노드 분기에서만 `ShowResultDatumOverlays(...)` 로 채워진다
(`InspectionListView.xaml.cs:695 / :712 / :737`). **Datum 노드 분기(:624~:661)에는 ShowResultDatumOverlays 호출이 없다**
→ Datum 노드 단독 선택/티칭 시 `_resultDatumOverlays` 가 비어 cyan 패턴 ROI 가 안 보인다("보일 때/안 보일 때" 증상).

D-01 은 신규 배선 불필요 — 본 plan 으로 Top/Bottom 보정 ROI(cyan) 가 Datum 노드에서도 가시화되어 보정 적용 여부를 육안 확인 가능해진다.

Purpose: 어느 노드를 선택해도 패턴 ROI 표시가 일관 동작하도록 렌더 입력을 안정화.
Output: InspectionListView 의 Datum 분기에서 선택 Datum 을 ShowResultDatumOverlays 로 전달.

**중요 제약:** 알고리즘/측정값 무변경. MainResultViewerControl 렌더 게이트(:846)는 이미 정상 — 수정하지 않는다. 입력(`_resultDatumOverlays`)만 Datum 분기에서 채운다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/57.1-pattern-roi-verification-safety/57.1-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- 코드베이스에서 추출 — 실행자는 탐색 없이 이 계약을 직접 사용한다. -->

MainView.xaml.cs (이미 존재, 무수정):
  public void ShowResultDatumOverlays(List<DatumConfig> datums)
    // null/빈 → halconViewer.ClearResultDatumOverlays() + ClearResultRoiOverlays()
    // 비어있지 않으면 각 datum 휘발 좌표 silent 복원(LastFindSucceeded 가드) 후
    //   halconViewer.SetResultDatumOverlays(datums) + SetResultRoiOverlays(보정 measRects, datumRects)

MainResultViewerControl.xaml.cs (이미 존재, 무수정):
  // Render() 내 cyan 패턴 ROI 게이트 (:846):
  if (_patternRoiOverlayVisible && _resultDatumOverlays != null && _resultDatumOverlays.Count > 0) { ... cyan ... }
  public void SetResultDatumOverlays(List<DatumConfig> datums)   // _resultDatumOverlays = datums; Render();

InspectionListView.xaml.cs (수정 대상):
  // InspectionList_SelectionChanged 내 분기 구조 (앵커):
  //   :606  mParentWindow.mainView.halconViewer.ClearResultDatumOverlays();   // 모든 노드 진입 시 클리어
  //   :624  if (item.NodeType == ENodeType.Datum) { ... SetDatumOverlay(datumCfg, true, ...); RestoreDatumOverlayFromTeach(datumCfg); ... }   // ← ShowResultDatumOverlays 누락
  //   :662  else if (item.NodeType == ENodeType.Measurement) { ... ShowResultDatumOverlays(datumsForMeas); }
  //   :702  else if (item.NodeType == ENodeType.FAI) { ... ShowResultDatumOverlays(datumsForFai); }
  //   :726  else if (item.NodeType == ENodeType.Action) { ... if (...ShotConfig) ShowResultDatumOverlays(datumsForShot); }
  // 헬퍼 (이미 존재):
  //   private List<DatumConfig> ResolveSequenceDatums(ESequence seqId)   // 시퀀스 전체 datum
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Datum 노드 분기에서 ShowResultDatumOverlays 로 선택 Datum 전달</name>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs (InspectionList_SelectionChanged Datum 분기 :624~:661, Measurement 분기 :690~:696 의 ShowResultDatumOverlays 호출 패턴 참조)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (ShowResultDatumOverlays 시그니처/동작 :1302~:1318)
    - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs (cyan 렌더 게이트 :846~:859 — 무수정, 동작 확인용)
  </read_first>
  <behavior>
    - Datum 노드 선택 → cyan 패턴 ROI 가 렌더된다 (PatternRoi_*/PatternRoi2_* 가 설정된 경우)
    - chk_overlayPattern 체크 해제 → cyan 미표시, 재체크 → 표시 (SetPatternRoiOverlayVisible 게이트 일관)
    - PatternRoi 미설정 Datum 선택 → cyan 없음 (정상, 회귀 아님)
  </behavior>
  <action>
    `InspectionListView.xaml.cs` 의 `InspectionList_SelectionChanged` Datum 분기(`if (item.NodeType == ENodeType.Datum)`, 약 :624)에서
    `RestoreDatumOverlayFromTeach(datumCfg);` 호출(약 :638) **직후**, force-rebind 블록 진입 전에,
    선택된 Datum 을 결과 오버레이로 전달하는 호출을 추가한다:

    ```csharp
    //260622 hbk Phase 57.1 D-03/D-01: Datum 노드 단독 선택 시에도 _resultDatumOverlays 를 채워 cyan 패턴 ROI 렌더 게이트 충족.
    //  Measurement/FAI/Action 분기와 동일 경로(ShowResultDatumOverlays) — 단일-원소 List. SetDatumOverlay(편집 채널)와 무관(공존).
    var datumOverlayList = new List<DatumConfig> { datumCfg };
    mParentWindow.mainView.ShowResultDatumOverlays(datumOverlayList);
    ```

    제약:
    - `ShowResultDatumOverlays` 는 단일-원소 `List<DatumConfig>` 를 받는다 (Measurement 분기의 `datumsForMeas` 와 동일 타입).
    - `mParentWindow != null && mParentWindow.mainView != null` 가드는 Datum 분기 진입 시 이미 보장됨(:633 에서 `mParentWindow.mainView.halconViewer` 직접 사용). 동일 가드 패턴 유지.
    - `System.Collections.Generic.List<DatumConfig>` 는 파일 상단 using 에 이미 존재(ResolveSequenceDatums 가 List<DatumConfig> 반환) — using 추가 불필요. `DatumConfig` 도 동일.
    - SetDatumOverlay(편집용 _datumConfig 채널)는 **건드리지 않는다** — 패턴 cyan 은 _resultDatumOverlays 채널 전용.
    - MainResultViewerControl/MainView/HalconDisplayService 무수정.
    - 파일 brace 스타일: InspectionListView 는 K&R(여는 중괄호 같은 줄) — 추가 코드도 K&R 유지.
  </action>
  <verify>
    <automated>MISSING — 빌드 검증으로 대체: msbuild Debug/x64 PASS + grep 앵커 확인 (테스트 프레임워크 없음, CLAUDE.md)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ShowResultDatumOverlays" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` 가 Datum 분기 내 1건 + 기존 Measurement/FAI/Action 분기 3건 = 총 4건 이상 매칭
    - `grep -n "260622 hbk Phase 57.1" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` 가 추가 주석 매칭
    - 추가 호출이 `if (item.NodeType == ENodeType.Datum)` 블록 내부에 위치 (Measurement 분기 :662 이전)
    - msbuild Debug/x64 Rebuild: 0 errors, 신규 warning 0
  </acceptance_criteria>
  <done>
    Datum 노드 선택 시 ShowResultDatumOverlays 가 선택 Datum 단일-원소 List 로 호출되어 _resultDatumOverlays 가 채워지고, MainResultViewerControl 의 cyan 게이트(:846)가 충족된다. 빌드 PASS.
  </done>
</task>

</tasks>

<verification>
- msbuild Debug/x64 Rebuild: 0 errors, 신규 warning 0
- Datum 분기 ShowResultDatumOverlays 호출이 Measurement/FAI/Action 분기와 동일 헬퍼 사용 (코드 일관성)
- MainResultViewerControl.xaml.cs / HalconDisplayService.cs / MainView.xaml.cs 무수정 (회귀 표면 최소화)
</verification>

<success_criteria>
- [SIMUL UAT] Cal_Image 셋 로드 → PatternRoi 설정된 Datum 노드 선택 → 결과 화면에 cyan 박스 표시 (이전엔 안 보임)
- [SIMUL UAT] chk_overlayPattern 토글 → cyan 표시/숨김 일관 동작
- [회귀] Measurement/Shot/FAI 노드 선택 시 기존 패턴/Datum 오버레이 표시 변화 없음
</success_criteria>

<output>
완료 후 `.planning/phases/57.1-pattern-roi-verification-safety/57.1-01-SUMMARY.md` 생성.
</output>
