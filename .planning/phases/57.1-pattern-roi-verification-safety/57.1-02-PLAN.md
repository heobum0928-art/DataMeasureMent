---
phase: 57.1-pattern-roi-verification-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs
autonomous: true
requirements: [D-02]
must_haves:
  truths:
    - "검사 시 각 측정 ROI 의 적용 회전각(roiPhi)과 baseline 가산각(rotAngle)이 Trace 로그로 남아 90° 어긋남 여부를 UAT 시 육안 확인 가능하다"
    - "length1=phi 방향 반장축, length2=phi 수직 반장축 HALCON 규약이 코드 주석으로 명시된다"
    - "측정 결과값(거리 mm/판정)은 본 plan 으로 변경되지 않는다 (진단 전용)"
  artifacts:
    - path: "WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs"
      provides: "회전각 확증 Trace 로그 + length 장축 매핑 주석"
      contains: "[ALIGN-ROI]"
  key_links:
    - from: "FAIEdgeMeasurementService.TryMeasure (transform != null 분기)"
      to: "Logging.PrintLog(ELogType.Trace)"
      via: "roiPhi/rotAngle deg 확증 로그"
      pattern: "ALIGN-ROI"
---

<objective>
D-02 진단: gen_rectangle2 회전(analytic 방식)의 회전각과 length 장축 매핑을 **확증·문서화**한다.

🔒 LOCKED 제약 (CONTEXT.md D-02): **length1/length2 를 swap 하는 코드 수정 절대 금지.**
`FAIEdgeMeasurementService.cs:51~64` 의 analytic 회전(중심 transform 이동 + 회전각을 phi 에 가산, length1/length2 유지)은
강체회전상 정상이며, AABB 계산(:111~:118)도 회전 사각형 기준으로 정확하다.
사용자 우려("90° 뒤바뀜")의 진짜 원인 후보는 (a) baseline 회전각, (b) 티칭 시 length 매핑 직관 불일치이며,
**둘 다 진단/로그/주석으로 확증**한다 — 측정 결과값(거리/판정)을 바꾸는 변경은 없다.

본 plan 산출물 2가지:
- (a) **회전각 확증 Trace 로그** — transform 적용 분기에서 `fai.ROI_Phi`(deg), 가산된 `rotAngle`(deg), 최종 `roiPhi`(deg)를 Trace 로그로 남긴다.
  InspectionSequence 의 `[ALIGN]`/`[ALIGN2]` thetaRad 로그(`InspectionSequence.cs:482/:503`)와 함께 보면 baseline θ → ROI 적용각 일치 여부(90° 어긋남 없음)를 UAT 시 확인 가능.
- (b) **length 장축 매핑 주석** — HALCON gen_measure_rectangle2 규약(length1 = phi 방향 반장축, length2 = phi 수직 반장축)을 length 사용 지점(:40, :113~:114) 주석에 명시.

Purpose: 회전 정상성을 코드 변경 없이 로그+주석으로 확증하여 사용자 90° 우려를 UAT 로 판정.
Output: FAIEdgeMeasurementService 에 진단 Trace 로그 1건 + length 장축 매핑 주석.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/57.1-pattern-roi-verification-safety/57.1-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- 코드베이스에서 추출 — 실행자는 탐색 없이 이 계약을 사용한다. -->

FAIEdgeMeasurementService.cs (수정 대상) — TryMeasure(HImage, FAIConfig, HTuple transform, out result):
  // :47~:49  double roiRow = fai.ROI_Row; double roiCol = fai.ROI_Col; double roiPhi = fai.ROI_Phi;
  // :54~:70  if (transform != null && transform.Length > 0) {
  //   AffineTransPoint2d(transform, ROI_Row, ROI_Col, out transRow, out transCol);  roiRow/roiCol = 이동값;
  //   double rotAngle = Math.Atan2(transform[3].D, transform[0].D);                 // h10/h00
  //   roiPhi = fai.ROI_Phi + rotAngle;                                              // ← 회전 가산 (정상)
  // }
  // :111~:118  AABB: dRow = |L1·cos(roiPhi)| + |L2·sin(roiPhi)|, dCol = |L1·sin(roiPhi)| + |L2·cos(roiPhi)|

Logging (이미 다른 곳에서 사용 — InspectionSequence.cs):
  Logging.PrintLog((int)ELogType.Trace, "....");   // ELogType 캐스트 (int) 필수 (CLAUDE.md)
  // FAIEdgeMeasurementService.cs 현재 using 에 Logging/ELogType 없음 → 추가 필요:
  //   using ReringProject.Utility;  (Logging 정의)  — 정확 네임스페이스는 read_first 로 Logging.cs 확인
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 회전각 확증 Trace 로그 추가 (transform 적용 분기)</name>
  <files>WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs (TryMeasure transform 분기 :45~:70, 상단 using 블록 :1~:5)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (Logging.PrintLog((int)ELogType.Trace,...) 사용 패턴 :482, "[ALIGN]" 포맷 참고)
    - WPF_Example/Utility/Logging.cs (PrintLog 시그니처 + Logging 클래스 네임스페이스 확인 → using 정확값)
  </read_first>
  <behavior>
    - transform != null 인 검사(보정 활성) 시 측정마다 Trace 로그 1건: ROI 이름/id, ROI_Phi(deg), rotAngle(deg), roiPhi(deg)
    - transform == null (보정 미적용) 시 로그 없음 (잡음 0)
    - rotAngle 이 baseline θ(InspectionSequence [ALIGN] thetaDeg)와 동일 부호·크기여야 함 (90° 어긋남 없음) → UAT 확인 포인트
  </behavior>
  <action>
    `FAIEdgeMeasurementService.cs` TryMeasure 의 transform 적용 분기(`if (transform != null && transform.Length > 0)`, 약 :54)
    안의 try 블록 끝(`roiPhi = fai.ROI_Phi + rotAngle;` 직후, catch 진입 전)에 Trace 로그를 추가한다:

    ```csharp
    //260622 hbk Phase 57.1 D-02(a): 회전각 확증 로그 — baseline θ(InspectionSequence [ALIGN] thetaDeg)와
    //  이 ROI 적용각(rotAngle)이 90° 어긋남 없이 일치하는지 UAT 시 확인. 측정값 무변경 — 진단 전용.
    try
    {
        string roiId = fai.RoiId;
        if (string.IsNullOrEmpty(roiId)) roiId = fai.Name;
        Logging.PrintLog((int)ELogType.Trace, "[ALIGN-ROI] " + (roiId ?? "")
            + " roiPhiDeg=" + (fai.ROI_Phi * 180.0 / Math.PI).ToString("F3")
            + " rotAngleDeg=" + (rotAngle * 180.0 / Math.PI).ToString("F3")
            + " appliedPhiDeg=" + (roiPhi * 180.0 / Math.PI).ToString("F3"));
    }
    catch { /* 로그 실패 무시 — 측정 흐름 보호 */ }
    ```

    제약/주의:
    - `fai.RoiId` / `fai.Name` 의 정확한 프로퍼티명은 read_first 로 FAIConfig 확인 후 존재하는 식별자만 사용. RoiId 없으면 Name, 둘 다 없으면 빈 문자열 폴백(위 패턴). **존재하지 않는 프로퍼티 참조 금지(CS0117 방지)** — 확인 후 실제 식별자로 교체.
    - `Logging.PrintLog((int)ELogType.Trace, ...)` — `ELogType` 은 반드시 `(int)` 캐스트 (CLAUDE.md).
    - 파일 상단 using 에 `Logging`/`ELogType` 네임스페이스가 없으면 추가 (read_first 로 Logging.cs 네임스페이스 확인 — 통상 `ReringProject.Utility`).
    - `Math` 는 이미 사용 중(:63 Math.Atan2) — System using 존재.
    - **transform 분기 안에만** 로그 — 보정 미적용 검사에 로그 누수 금지.
    - 파일 brace 스타일: 이 파일은 Allman(여는 중괄호 새 줄) — 추가 코드도 Allman 유지.
    - 측정 로직(roiRow/roiCol/roiPhi 산출, AABB, MeasurePos)은 **한 줄도 수정 금지** — 로그만 삽입.
  </action>
  <verify>
    <automated>MISSING — 빌드 검증으로 대체: msbuild Debug/x64 PASS + grep 앵커 확인 (테스트 프레임워크 없음, CLAUDE.md)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\[ALIGN-ROI\]" WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs` 가 1건 매칭
    - `grep -n "Logging.PrintLog((int)ELogType.Trace" WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs` 가 1건 매칭
    - 로그 호출이 `if (transform != null && transform.Length > 0)` 블록 내부에 위치 (transform 미적용 경로엔 로그 없음)
    - `git diff` 상 측정 산출 라인(AffineTransPoint2d, rotAngle 계산, AABB :111~:118, MeasurePos)은 무변경 — 추가 라인만 존재
    - msbuild Debug/x64 Rebuild: 0 errors, 신규 warning 0
  </acceptance_criteria>
  <done>
    transform 적용 분기에서 ROI_Phi/rotAngle/적용 phi(deg) Trace 로그가 측정마다 남고, 측정 산출 코드는 무변경. 빌드 PASS.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: length1/length2 장축 매핑 HALCON 규약 주석 명시</name>
  <files>WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs (ROI_Length1/Length2 사용 지점 :40, AABB :111~:118, gen_measure_rectangle2 호출 TryMeasureBoth/Single)
  </read_first>
  <behavior>
    - length1=phi 방향 반장축, length2=phi 수직 반장축 규약이 코드 주석으로 명시됨 (티칭 드로잉 직관과의 매핑 문서화)
    - 코드 동작/측정값 무변경
  </behavior>
  <action>
    `FAIEdgeMeasurementService.cs` 의 ROI length 사용 핵심 지점에 HALCON gen_measure_rectangle2 규약 주석을 추가한다.
    AABB 계산 직전(약 :110, `// ROI 바운딩 박스 (Rectangle2 -> AABB)` 주석 라인 근처)에 다음 주석을 보강:

    ```csharp
    //260622 hbk Phase 57.1 D-02(b): HALCON gen_measure_rectangle2 규약 명시 — length1 = phi(roiPhi) 방향 반장축,
    //  length2 = phi 수직 방향 반장축. 티칭 시 드래그한 bbox 의 X절반→Length1·Y절반→Length2 매핑(ROI_Phi=0 기준,
    //  HalconViewer_*RectCompleted)과 일치. analytic 회전은 phi 만 가산하고 length1/length2 는 보존(강체회전 정상).
    //  ※ length1/length2 swap 금지 — swap 시 장축/단축 뒤바뀌어 회귀(CONTEXT.md D-02 LOCKED).
    ```

    제약:
    - **주석만 추가** — length1/length2 사용 표현식(:113~:114 의 cosPhi/sinPhi 곱), 변수 대입 전부 무변경.
    - 기존 주석 라인은 보존하고 위/아래에 누적(Phase 20 D-12 marker stacking 패턴).
    - 파일 brace 스타일 Allman 유지.
  </action>
  <verify>
    <automated>MISSING — 빌드 검증으로 대체: msbuild Debug/x64 PASS + grep 앵커 확인</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "length1 = phi" WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs` (또는 "D-02(b)") 가 매칭
    - `grep -n "swap 금지" WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs` 가 매칭
    - `git diff` 상 AABB 계산식(dRow/dCol/top/bottom/left/right)은 무변경 — 주석 라인만 추가
    - msbuild Debug/x64 Rebuild: 0 errors, 신규 warning 0
  </acceptance_criteria>
  <done>
    length 장축 매핑 HALCON 규약 + swap 금지 사유가 코드 주석으로 명시되고, 측정 코드는 무변경. 빌드 PASS.
  </done>
</task>

</tasks>

<verification>
- msbuild Debug/x64 Rebuild: 0 errors, 신규 warning 0
- `git diff WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs` — 추가 라인은 (a) Trace 로그 블록 (b) length 규약 주석뿐. 측정 산출/AABB/MeasurePos 라인 무변경 (D-02 LOCKED 준수)
- length1/length2 swap 코드 0건 (grep 으로 변수 재대입 부재 확인)
</verification>

<success_criteria>
- [SIMUL UAT] IsPatternAlignEnabled on Datum 검사 → Trace 로그에 [ALIGN-ROI] roiPhiDeg/rotAngleDeg/appliedPhiDeg 출력, [ALIGN] thetaDeg 와 rotAngleDeg 부호·크기 일치(90° 어긋남 없음) 확인
- [확증] length1 장축 매핑 주석이 티칭 드로잉 직관과 일치함을 코드 리뷰로 확인
- [무변경] 측정 거리(mm)/판정 결과가 본 plan 적용 전후 동일 (회귀 0)
</success_criteria>

<output>
완료 후 `.planning/phases/57.1-pattern-roi-verification-safety/57.1-02-SUMMARY.md` 생성.
</output>
