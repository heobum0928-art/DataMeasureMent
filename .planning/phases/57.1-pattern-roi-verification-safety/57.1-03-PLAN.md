---
phase: 57.1-pattern-roi-verification-safety
plan: 03
type: execute
wave: 2
depends_on: [57.1-01]
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: true
requirements: [D-04]
must_haves:
  truths:
    - "비-Datum 노드(Measurement/Shot/FAI/Sequence/Recipe) 선택 시 패턴1/패턴2/패턴모델생성 3버튼이 IsEnabled=false 다"
    - "Datum 노드 선택 시 3버튼이 IsEnabled=true 다 (회귀 0)"
    - "3버튼 클릭 핸들러가 선택 노드 비-Datum 시 CustomMessageBox 알림 후 early-return 한다"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "패턴 버튼 클릭 핸들러의 비-Datum 가드 (CustomMessageBox + return)"
      contains: "Datum 티칭 존을 먼저 선택"
  key_links:
    - from: "InspectionListView.InspectionList_SelectionChanged (분기 진입부 default-disable)"
      to: "btn_drawPatternRoi/btn_drawPatternRoi2/btn_createPatternModel.IsEnabled = false"
      via: "모든 노드 진입 시 비활성화 → Datum 분기만 재활성화"
      pattern: "btn_drawPatternRoi.IsEnabled = false"
    - from: "MainView.DrawPatternRoiButton_Click / DrawPatternRoi2Button_Click / InvokeCreatePatternModel"
      to: "CustomMessageBox.Show + return"
      via: "SelectedParam as DatumConfig == null 가드"
      pattern: "Datum 티칭 존을 먼저 선택"
---

<objective>
D-04: 패턴 ROI 버튼 안전장치 — 비-Datum 노드 비활성화 + 클릭 가드 알림.

**현상 확인(코드 정독 결과):**
- (a) 비활성화: `InspectionListView.xaml.cs:588~590` 에서 SelectionChanged 진입 시 3버튼(btn_drawPatternRoi/btn_drawPatternRoi2/btn_createPatternModel)을
  **이미 default-disable** 하고, Datum 분기(:657~659)에서만 재활성화한다 → 비-Datum 노드 비활성화는 **이미 구조적으로 충족**.
- (b) 클릭 가드: 3개 클릭 핸들러(`DrawPatternRoiButton_Click:2685`, `DrawPatternRoi2Button_Click:2734`, `InvokeCreatePatternModel:2786`)는
  **이미** `SelectedParam as DatumConfig == null` 시 `CustomMessageBox.Show(...)` + `return` 가드를 가진다.
  단, 현재 메시지는 "Datum 노드를 먼저 선택하세요." 로 CONTEXT.md D-04 의 권고 문구("Datum 티칭 존을 먼저 선택하세요")와 미세 불일치.

⚠ 주의: "Datum 노드를 먼저 선택" 문자열은 MainView.xaml.cs 에 **4회** 등장 — 패턴 핸들러 3개(:2686/:2735/:2787, **본 plan 수정 대상**) +
Teach Datum 핸들러(:2321, **패턴 무관 — 범위 밖, 무변경 유지**). 본 plan 은 패턴 3개만 신규 문구로 교체하므로 교체 후에도 :2321 은 구 문구로 남아 잔존 count=1 이 정상이다.

따라서 본 plan = **안전장치 확증 + 패턴 핸들러 메시지 문구 통일 + 회귀 가드**. depends_on 57.1-01 (동일 파일 InspectionListView 충돌 방지 → Wave 2).

Purpose: 부주의 클릭/노드 이동 시 패턴 버튼이 안전하게 동작함을 명시·확증.
Output: 3개 패턴 클릭 핸들러 메시지 문구 통일 + 비활성화 경로 회귀 가드 확인.

**제약:** 알고리즘/측정값 무변경. 버튼 활성/비활성 로직은 이미 정상 — 메시지 통일 + 확증만.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/57.1-pattern-roi-verification-safety/57.1-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- 코드베이스에서 추출 — 실행자는 탐색 없이 이 계약을 사용한다. -->

InspectionListView.xaml.cs (이미 존재 — 비활성화 경로, 확증 대상/무수정 권장):
  // InspectionList_SelectionChanged 진입부 (:584~:591): 모든 노드 진입 시
  //   mParentWindow.mainView.btn_drawPatternRoi.IsEnabled = false;       // :588
  //   mParentWindow.mainView.btn_createPatternModel.IsEnabled = false;   // :589
  //   mParentWindow.mainView.btn_drawPatternRoi2.IsEnabled = false;      // :590
  // Datum 분기 (:657~659): 재활성화
  //   btn_drawPatternRoi.IsEnabled = true; btn_createPatternModel.IsEnabled = true; btn_drawPatternRoi2.IsEnabled = true;

MainView.xaml.cs (수정 대상 — 패턴 핸들러 메시지 문구 통일):
  // DrawPatternRoiButton_Click (:2681~): datum==null → CustomMessageBox.Show("패턴 ROI 그리기", "Datum 노드를 먼저 선택하세요."); return;  (:2685~2688)
  // DrawPatternRoi2Button_Click (:2730~): datum==null → CustomMessageBox.Show("패턴 2 ROI 그리기", "Datum 노드를 먼저 선택하세요."); return; (:2734~2737)
  // InvokeCreatePatternModel (:2782~): datum==null → CustomMessageBox.Show("모델 생성 실패", "Datum 노드를 먼저 선택하세요."); return; (:2786~2789)
  //   datum = mParentWindow.inspectionList.SelectedParam as DatumConfig;
  // ⚠ Teach Datum 핸들러 (:2321): 동일 문구 "Datum 노드를 먼저 선택" 존재 — 패턴과 무관, 본 plan 범위 밖, 무변경.

CustomMessageBox (프로젝트 알림 패턴, 이미 사용 중):
  CustomMessageBox.Show(string title, string message);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 패턴 버튼 3개 클릭 가드 메시지 문구 통일 + 가드 확증</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (DrawPatternRoiButton_Click :2681~2697, DrawPatternRoi2Button_Click :2730~2746, InvokeCreatePatternModel :2782~2799, ※ Teach Datum :2321 은 무변경 — 패턴 핸들러만 수정)
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs (비활성화 default-disable :584~591, Datum 분기 재활성화 :654~660 — 확증용, 수정 불필요 시 무변경)
  </read_first>
  <behavior>
    - 비-Datum 노드 선택 후 패턴 버튼 클릭(만일 활성 상태) → "Datum 티칭 존을 먼저 선택하세요" 알림 + early-return (캔버스 모드 진입/모델 생성 없음)
    - Datum 노드 선택 후 클릭 → 정상 동작 (회귀 0)
  </behavior>
  <action>
    `MainView.xaml.cs` 의 **3개 패턴 핸들러**에서 `datum == null` 가드의 `CustomMessageBox.Show(...)` 메시지 본문을
    CONTEXT.md D-04 권고 문구로 통일한다. **타이틀은 각 버튼별 기존 값 유지**, 본문만 교체:

    1. `DrawPatternRoiButton_Click` (:2686):
       `CustomMessageBox.Show("패턴 ROI 그리기", "Datum 노드를 먼저 선택하세요.");`
       → `CustomMessageBox.Show("패턴 ROI 그리기", "Datum 티칭 존을 먼저 선택하세요.");`
    2. `DrawPatternRoi2Button_Click` (:2735):
       `CustomMessageBox.Show("패턴 2 ROI 그리기", "Datum 노드를 먼저 선택하세요.");`
       → `CustomMessageBox.Show("패턴 2 ROI 그리기", "Datum 티칭 존을 먼저 선택하세요.");`
    3. `InvokeCreatePatternModel` (:2787):
       `CustomMessageBox.Show("모델 생성 실패", "Datum 노드를 먼저 선택하세요.");`
       → `CustomMessageBox.Show("모델 생성 실패", "Datum 티칭 존을 먼저 선택하세요.");`

    ⚠ **Teach Datum 핸들러(:2321)의 동일 문구는 패턴과 무관 — 절대 건드리지 말 것.** 교체 후에도 :2321 은 구 문구로 남는다.

    각 교체 라인 위에 마커 주석 1줄 추가:
    ```csharp
    //260622 hbk Phase 57.1 D-04(b): 비-Datum 클릭 가드 메시지 통일 (CustomMessageBox 알림 후 early-return).
    ```

    제약:
    - `return;` early-out 은 **이미 존재** — 제거/이동 금지, 그대로 유지.
    - `datum = ... SelectedParam as DatumConfig` 가드 로직 무변경 — 메시지 본문 문자열만 교체.
    - 캔버스 모드 진입/모델 생성 코드 무변경.
    - 패턴 3개 핸들러 외(특히 Teach Datum :2321)의 동일 문구는 무변경.
    - 파일 brace 스타일 유지(MainView 는 K&R).
  </action>
  <verify>
    <automated>MISSING — 빌드 검증으로 대체: msbuild Debug/x64 PASS + grep 앵커 확인</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Datum 티칭 존을 먼저 선택" WPF_Example/UI/ContentItem/MainView.xaml.cs` 가 3 (패턴 핸들러 3개)
    - `grep -c "Datum 노드를 먼저 선택" WPF_Example/UI/ContentItem/MainView.xaml.cs` 가 **1** (Teach Datum :2321 핸들러 — 패턴 무관 범위 밖, 무변경 유지가 정상. 패턴 핸들러 3개의 구 문구는 모두 제거됨)
    - 패턴 핸들러 3개(:2686/:2735/:2787)에 구 문구 "Datum 노드를 먼저 선택" 잔존 0 — 잔존 1건은 반드시 :2321(Teach Datum)이어야 함 (`grep -n` 으로 라인 번호 = 2321대 확인)
    - 각 패턴 핸들러의 `return;` early-out 보존 (grep 으로 datum==null 블록 내 return 확인)
    - msbuild Debug/x64 Rebuild: 0 errors, 신규 warning 0
  </acceptance_criteria>
  <done>
    3개 패턴 핸들러의 비-Datum 가드 메시지가 "Datum 티칭 존을 먼저 선택하세요" 로 통일되고 early-return 보존. Teach Datum :2321 무변경. 빌드 PASS.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 비-Datum 비활성화 경로 회귀 확증 (코드 검증)</name>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs (default-disable :584~591, Datum 분기 재활성화 :654~660, Measurement/FAI/Action/Sequence/else 분기 — 패턴 버튼 재활성화 부재 확인)
  </read_first>
  <behavior>
    - SelectionChanged 진입 시 패턴 3버튼이 항상 먼저 false → Datum 분기만 true → 비-Datum 노드는 false 유지
  </behavior>
  <action>
    `InspectionListView.xaml.cs` 의 `InspectionList_SelectionChanged` 에서 비활성화 경로가 정상임을 확증하고, 회귀 가드 주석을 보강한다.

    1. default-disable 블록(:587 의 기존 주석 근처)에 확증 주석 누적:
    ```csharp
    //260622 hbk Phase 57.1 D-04(a): 패턴 3버튼 진입 시 무조건 비활성화 → Datum 분기에서만 재활성. 비-Datum 노드 = 비활성 유지.
    ```

    2. **검증 only (코드 수정 없음)**: Measurement(:662)/FAI(:702)/Action(:726)/Sequence(:751)/else(:767) 분기에
       `btn_drawPatternRoi`/`btn_drawPatternRoi2`/`btn_createPatternModel`.IsEnabled = true 재활성화가 **없음**을 grep 으로 확인.
       만약 어떤 비-Datum 분기에서 재활성화가 발견되면(회귀 위험) 그 라인을 제거하고 SUMMARY 에 기록.

    제약:
    - Datum 분기의 재활성화(:657~659)는 **유지** — D-04(a) 의 정상 동작.
    - 57.1-01 이 같은 파일 Datum 분기에 ShowResultDatumOverlays 를 추가했으므로(depends_on), 그 변경과 충돌 없이 주석만 추가.
    - 파일 brace 스타일 유지(K&R).
  </action>
  <verify>
    <automated>MISSING — 빌드 검증 + grep 으로 대체</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "btn_drawPatternRoi.IsEnabled = true" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` 가 **Datum 분기 1건만** 매칭 (비-Datum 분기 0건)
    - `grep -n "btn_createPatternModel.IsEnabled = true" ...` / `btn_drawPatternRoi2.IsEnabled = true` 동일하게 각 1건만 (Datum 분기)
    - `grep -c "btn_drawPatternRoi.IsEnabled = false" ...` ≥ 1 (default-disable 존재)
    - `grep -n "260622 hbk Phase 57.1 D-04(a)" ...` 매칭 (확증 주석)
    - msbuild Debug/x64 Rebuild: 0 errors, 신규 warning 0
  </acceptance_criteria>
  <done>
    비-Datum 노드에서 패턴 3버튼이 비활성 유지됨이 grep 으로 확증되고, 회귀 가드 주석 보강. 빌드 PASS.
  </done>
</task>

</tasks>

<verification>
- msbuild Debug/x64 Rebuild: 0 errors, 신규 warning 0
- 패턴 3버튼 재활성화(IsEnabled=true)는 Datum 분기에만 존재 (grep 검증)
- 3개 패턴 클릭 핸들러 가드 메시지 통일 + early-return 보존. Teach Datum :2321 무변경 (구 문구 1건 잔존 정상)
</verification>

<success_criteria>
- [SIMUL UAT] Measurement/Shot/FAI/Sequence 노드 선택 → 패턴1/패턴2/패턴모델생성 버튼 비활성(회색) 표시
- [SIMUL UAT] Datum 노드 선택 → 3버튼 활성, 클릭 시 정상 그리기/생성 진입 (회귀 0)
- [SIMUL UAT] (가능 시) 비-Datum 상태에서 강제 클릭 → "Datum 티칭 존을 먼저 선택하세요" 알림 + 동작 없음
</success_criteria>

<output>
완료 후 `.planning/phases/57.1-pattern-roi-verification-safety/57.1-03-SUMMARY.md` 생성.
</output>
</content>
