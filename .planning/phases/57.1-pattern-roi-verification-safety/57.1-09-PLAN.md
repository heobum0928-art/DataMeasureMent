---
plan: "57.1-09"
phase: "57.1"
wave: 7
depends_on: ["57.1-08"]
autonomous: true
files_modified:
  - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
  - WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs
  - WPF_Example/Halcon/Algorithms/MeasurementAlgorithm.cs
requirements: ["Phase 57 후속 (ALIGN-01/02 UAT 피드백)"]
gap_closure: true
---

# 57.1-09 — 에지 trim "정렬+% 절사" 전 사이트 통일 (Datum 포함)

<objective>
57.1-08 에서 TryFitLine 에만 적용한 "위치축 정렬 → 양끝 각 % 절사" 를 **Datum 검출 + 모든 측정 trim 경로에 통일**한다. 공유 static 헬퍼 1개로 단일 소스화한다. EdgeTrimCount/trimCount 는 전 사이트에서 "양끝 각 백분율(%)" 로 일관 해석.
</objective>

<background>
조사 결과 trim 사이트 5곳 (현재 상태):
1. `VisionAlgorithmService.TryFitLine` — 정렬+% 절사 **완료**(57.1-08). → 헬퍼로 리팩터(단일 소스).
2. `DatumFindingService.TryFindLine` (~:1718) — 정렬 불확실 + **개수** trim(TupleSelectRange). Line1/Line2/Vertical 검출.
3. `DatumFindingService.TryExtractEdgePoints` (~:1893) — **개수** trim. Horizontal_A/B 에지.
4. `FAIEdgeMeasurementService.TrimExtremePoints` (~:572) — **정렬 있음** + **개수** trim.
5. `MeasurementAlgorithm.TrimExtremePoints` (~:250) — **정렬 있음** + **개수** trim.

전부 동일 namespace(ReringProject.Halcon.Algorithms) + 동일 assembly → VisionAlgorithmService 에 public static 헬퍼 두면 전 사이트 호출 가능(신규 파일 불필요 → classic csproj <Compile> 편집 회피).
</background>

<tasks>
<task id="1" type="execute">
<name>VisionAlgorithmService 에 공유 SortAndTrimPercent 헬퍼 추가 + TryFitLine 리팩터</name>

<read_first>
- WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs (TryFitLine trim 블록 57.1-08 적용분, 클래스 구조)
</read_first>

<action>
VisionAlgorithmService 클래스에 public static 헬퍼 추가 (`//260622 hbk Phase 57.1` 마커, Allman):
```csharp
// 에지점 (rows,cols) 을 위치축으로 정렬 후 양끝 각 trimPercent%(0~49) 절사. scanHorizontal=true→row, false→col 기준.
//  trimPercent<=0 또는 점<4 → 무변경. 절사 후 남는 점<2 면 절사 skip(원본 유지). 점 순서는 FitLineContourXld 결과 무관 — 정렬은 절사 대상 선정용.
public static void SortAndTrimPercent(ref HTuple rows, ref HTuple cols, bool scanHorizontal, int trimPercent)
{
    if (rows == null || cols == null) return;
    int n = rows.TupleLength();
    if (trimPercent <= 0 || n < 4) return;
    HTuple key = scanHorizontal ? rows : cols;
    HTuple order = key.TupleSortIndex();
    rows = rows.TupleSelect(order);
    cols = cols.TupleSelect(order);
    int pct = trimPercent; if (pct > 49) pct = 49;
    int removeEach = (int)(n * pct / 100.0);
    if (removeEach > 0 && (n - 2 * removeEach) >= 2)
    {
        rows = rows.TupleSelectRange(removeEach, n - removeEach - 1);
        cols = cols.TupleSelectRange(removeEach, n - removeEach - 1);
    }
}
```
그 후 TryFitLine 의 57.1-08 인라인 정렬+절사 블록을 다음 한 줄로 교체:
```csharp
//260622 hbk Phase 57.1 trim 통일 — 공유 헬퍼로 단일 소스화
SortAndTrimPercent(ref allRows, ref allCols, scanHorizontal, trimCount);
int edgeCount = allRows.TupleLength();
```
(이후 `if (edgeCount < 2)` 검사·fit·collectedEdges 무변경)
</action>

<acceptance_criteria>
- VisionAlgorithmService 에 `public static void SortAndTrimPercent(ref HTuple, ref HTuple, bool, int)` 존재
- TryFitLine 이 인라인 trim 대신 SortAndTrimPercent 호출, 이후 edgeCount 재계산
- MSBuild 0 errors
</acceptance_criteria>
</task>

<task id="2" type="execute">
<name>DatumFindingService TryFindLine/TryExtractEdgePoints 를 헬퍼로 교체</name>

<read_first>
- WPF_Example/Halcon/Algorithms/DatumFindingService.cs (TryFindLine 의 strip 누적 후 trim 지점 ~:1700~1730, TryExtractEdgePoints trim ~:1880~1910, 각 메서드의 direction/scanHorizontal 결정부)
</read_first>

<action>
TryFindLine 과 TryExtractEdgePoints 각각의 "누적 후 개수 trim" 블록(TupleSelectRange(trimCount, edgeCount-trimCount-1) 패턴)을 다음으로 교체:
```csharp
//260622 hbk Phase 57.1 trim 통일 — 정렬+% 절사 공유 헬퍼 사용(개수 trim → 양끝 각 %)
VisionAlgorithmService.SortAndTrimPercent(ref allRows, ref allCols, scanHorizontal, trimCount);
```
(allRows/allCols 변수명, scanHorizontal 판별 변수는 read_first 로 실제명 확인 후 일치. scanHorizontal 변수가 없으면 해당 메서드의 direction→가로/세로 판별식을 동일 규약으로 산출 후 전달.)
trim 후 edgeCount 재계산이 필요하면 `allRows.TupleLength()` 로 갱신. 이후 "에지 부족(<2)" 검사·fit 로직 무변경.
</action>

<acceptance_criteria>
- TryFindLine, TryExtractEdgePoints 둘 다 `VisionAlgorithmService.SortAndTrimPercent` 호출로 교체 (기존 TupleSelectRange 개수 trim 제거)
- scanHorizontal(또는 동등 축 판별) 올바르게 전달
- Datum 검출 후속 로직(에지 부족 검사/fit) 무변경
- MSBuild 0 errors
</acceptance_criteria>
</task>

<task id="3" type="execute">
<name>FAIEdgeMeasurementService / MeasurementAlgorithm 의 TrimExtremePoints 를 헬퍼로 위임</name>

<read_first>
- WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs (TrimExtremePoints ~:572~605, 호출처 ~:254/:428)
- WPF_Example/Halcon/Algorithms/MeasurementAlgorithm.cs (TrimExtremePoints ~:250~283)
</read_first>

<action>
두 파일의 `TrimExtremePoints`(정렬+개수 trim) 본문을 공유 헬퍼 위임으로 교체 — 시그니처/호출처는 유지하고 내부만 SortAndTrimPercent 호출로:
```csharp
//260622 hbk Phase 57.1 trim 통일 — 정렬+% 절사 공유 헬퍼 위임(개수 → 양끝 각 %)
private static void TrimExtremePoints(ref HTuple rows, ref HTuple cols, bool scanHorizontal, int trimCount)
{
    VisionAlgorithmService.SortAndTrimPercent(ref rows, ref cols, scanHorizontal, trimCount);
}
```
(실제 시그니처(파라미터 순서/이름, ref 여부)는 read_first 로 확인 후 일치. 호출처는 무변경. trimCount 가 이제 % 의미임을 본문 주석에 명시.)
</action>

<acceptance_criteria>
- FAIEdgeMeasurementService.TrimExtremePoints, MeasurementAlgorithm.TrimExtremePoints 본문이 SortAndTrimPercent 위임으로 교체 (자체 정렬/개수 trim 코드 제거)
- 호출처 시그니처 무변경 (컴파일 유지)
- MSBuild 0 errors, 신규 warning 0
</acceptance_criteria>
</task>
</tasks>

<must_haves>
- Datum 검출(Line/Vertical/Horizontal) + 전 측정 trim 이 동일한 SortAndTrimPercent(정렬+양끝 % 절사) 사용
- trimCount/EdgeTrimCount 전 사이트 % 일관 해석
- 빌드 0 errors, 검출/측정 후속 로직 회귀 0
</must_haves>

<uat>
SIMUL: Datum 검출(TwoLineIntersect/CircleTwoHorizontal/VerticalTwoHorizontal) + 측정 모두에서 에지 trim 이 정렬+% 절사로 동작(끝 노이즈 제거, 피팅 깔끔). EdgeTrimCount 값은 % 로 해석됨(기존 레시피 값 재확인).
</uat>

<notes>
- 전 사이트 trim 의미 "개수"→"%(양끝 각)" 변경 — 기존 레시피 영향. 57.1-10 UI 라벨(%)과 함께 사용자 안내.
- DualImage 경로(TryFindVerticalTwoHorizontalDualImage)도 TryFindLine/TryExtractEdgePoints 를 거치면 자동 통일됨 — read_first 로 확인.
</notes>
