---
plan: "57.1-11"
phase: "57.1"
wave: 9
depends_on: ["57.1-06"]
autonomous: true
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
requirements: ["Phase 57 후속 (ALIGN-01/02 UAT 피드백)"]
gap_closure: true
---

# 57.1-11 — 재티칭 시 패턴 기준(RefMatch) 동기화 (Teach=Find 일치)

<objective>
UAT: align ON datum 재티칭(Teach Datum) 후 Test Find 하면 slate blue(Find)가 yellow(Teach)와 어긋남. 원인 = "datum 재티칭"이 패턴 기준 RefMatch 를 갱신하지 않아, Find 의 align 보정(alignRigid)이 이미 현재 위치에 놓인 datum ROI 에 0 이 아닌 보정을 또 적용(이중보정). 재티칭 성공 시 같은 티칭 이미지에서 RefMatch(2-패턴이면 RefMatch2)를 재산출해 동기화한다 → 그 이미지에서 alignRigid≈identity → Find=Teach.
</objective>

<root_cause>
- Teach(TryTeachVerticalTwoHorizontal:1342/1357)와 Find(TryFindVerticalTwoHorizontal:539/542)의 각도 산출식은 **동일** → 같은 이미지면 겹쳐야 함.
- Find(align ON)는 datum 검출 ROI 에 alignRigid 적용. alignRigid = (현재 패턴 pose − RefMatch). RefMatch 는 "패턴 모델 생성"(InvokeCreatePatternModel, MainView.xaml.cs:2854 TryFindRefPose) 에서만 set.
- "Teach Datum"(InvokeTryTeachDatum:2597)은 datum ROI/RefOrigin/RefAngle 만 갱신, RefMatch 미갱신 → 재티칭 이미지와 RefMatch 기준 이미지가 desync → alignRigid≠identity → Find 가 teach 위치에서 어긋남.
- 해결: 재티칭 성공 직후, align ON + 패턴 모델 파일 존재 시 같은 티칭 이미지에서 TryFindRefPose 재실행 → RefMatch 갱신(패턴 ROI 는 재티칭서 불변이므로 현재 이미지의 패턴 위치로 재앵커).
</root_cause>

<tasks>
<task id="1" type="execute">
<name>RefreshPatternRefPoseAfterTeach 헬퍼 + 재티칭 성공 경로에 호출</name>

<read_first>
- WPF_Example/UI/ContentItem/MainView.xaml.cs (InvokeTryTeachDatum:2597~2660 — DualImage 분기 TryTeachDatum(imgH,imgV):2640 / 단일 TryTeachDatum(img):2647 / 공통 성공블록:2651; InvokeTryTeachDatumForEdit:1212~1234 의 TryTeachDatum(img):1219; InvokeCreatePatternModel:2842~2885 의 TryFindRefPose/ResolveDatumModelPath/ResolveDatumModelPath2 사용례; pSeq/using)
- WPF_Example/Halcon/Algorithms/PatternMatchService.cs (TryFindRefPose 시그니처)
- WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (IsPatternAlignEnabled, RefMatchRow/Col/AngleDeg, RefMatch2Row/Col, PatternEngine, PatternMinScore, PatternRoi2_Length1/2)
</read_first>

<action>
1. private 헬퍼 추가 (MainView, K&R, `//260622 hbk Phase 57.1` 마커):
```csharp
//260622 hbk Phase 57.1 — 재티칭 시 패턴 기준(RefMatch) 동기화. align ON + 패턴 모델 존재 시 같은 티칭 이미지에서
//  TryFindRefPose 재실행해 RefMatch(2-패턴이면 RefMatch2) 갱신 → Test Find alignRigid≈identity → Find=Teach.
//  실패/모델 없음 → 기존 RefMatch 유지(teach 성공은 유지, silent).
private void RefreshPatternRefPoseAfterTeach(DatumConfig datum, HImage patternImage) {
    if (datum == null || patternImage == null) return;
    if (!datum.IsPatternAlignEnabled) return;
    string modelPath = ReringProject.Sequence.InspectionSequence.ResolveDatumModelPath(datum);
    if (string.IsNullOrEmpty(modelPath) || !System.IO.File.Exists(modelPath)) return;
    var svc = new ReringProject.Halcon.Algorithms.PatternMatchService();
    double rr, rc, ra, rs; string refErr;
    if (svc.TryFindRefPose(patternImage, datum.PatternEngine, modelPath, datum.PatternMinScore,
            out rr, out rc, out ra, out rs, out refErr)) {
        datum.RefMatchRow = rr; datum.RefMatchCol = rc; datum.RefMatchAngleDeg = ra;
        if (datum.PatternRoi2_Length1 > 0.0 && datum.PatternRoi2_Length2 > 0.0) {
            string modelPath2 = ReringProject.Sequence.InspectionSequence.ResolveDatumModelPath2(datum);
            double rr2, rc2, ra2, rs2; string refErr2;
            if (!string.IsNullOrEmpty(modelPath2) && System.IO.File.Exists(modelPath2)
                && svc.TryFindRefPose(patternImage, datum.PatternEngine, modelPath2, datum.PatternMinScore,
                       out rr2, out rc2, out ra2, out rs2, out refErr2)) {
                datum.RefMatch2Row = rr2; datum.RefMatch2Col = rc2;
            }
        }
        Logging.PrintLog((int)ELogType.Trace, "[ALIGN-REFRESH] " + (datum.DatumName ?? "")
            + " RefMatch re-anchored on teach image: (" + rr.ToString("F1") + "," + rc.ToString("F1") + ") score=" + rs.ToString("F3"));
    }
}
```
(TryFindRefPose 실제 시그니처/파라미터는 InvokeCreatePatternModel:2853 사용례와 일치시킬 것. Logging/ELogType using 확인.)

2. 호출 삽입:
- **단일-이미지 teach** (InvokeTryTeachDatum:2647 `ok = svc.TryTeachDatum(img, _editingDatum, out error);` 직후, ok 일 때): `if (ok) { RefreshPatternRefPoseAfterTeach(_editingDatum, img); }`
- **DualImage teach** (InvokeTryTeachDatum:2640 `ok = svc.TryTeachDatum(imgH, imgV, ...)` 직후, **finally 의 imgH.Dispose() 전**, imgH 살아있을 때): `if (ok) { RefreshPatternRefPoseAfterTeach(_editingDatum, imgH); }` (패턴은 가로축 이미지 기준)
- **편집 모드 재티칭** (InvokeTryTeachDatumForEdit:1219 `bool ok = svc.TryTeachDatum(img, datum, out error);` 직후, ok 일 때): `if (ok) { RefreshPatternRefPoseAfterTeach(datum, img); }`
(각 지점의 실제 변수명/ok 플래그/이미지 변수는 read_first 로 확인 후 일치. 공통 성공블록의 SetDatumOverlay 등 기존 로직 무변경.)
</action>

<acceptance_criteria>
- RefreshPatternRefPoseAfterTeach 헬퍼 존재 (IsPatternAlignEnabled 게이트 + 모델 파일 존재 체크 + TryFindRefPose 로 RefMatch/RefMatch2 갱신)
- InvokeTryTeachDatum 단일/DualImage 경로 + InvokeTryTeachDatumForEdit 의 teach 성공 직후 호출 (DualImage 는 imgH dispose 전)
- align OFF datum 은 헬퍼가 즉시 return (회귀 0), 모델 없으면 skip(크래시 없음)
- 기존 teach 성공 후속(SetDatumOverlay/PublishDatumRoiCandidates 등) 무변경
- MSBuild Debug/x64: 0 errors, 신규 warning 0
</acceptance_criteria>
</task>
</tasks>

<must_haves>
- align ON datum 재티칭 후 RefMatch 가 같은 이미지로 재앵커 → Test Find 시 그 이미지서 alignRigid≈identity → slate blue(Find)가 yellow(Teach)와 일치
- align OFF / 모델 없음 회귀 0, 빌드 0 errors
</must_haves>

<uat>
SIMUL: align ON VTH datum → Teach Datum 재티칭 → Test Find → slate blue 검출 기준선이 yellow 티칭선과 일치(어긋남 해소). [ALIGN-REFRESH] + [ALIGN] thetaDeg≈0 로그로 확인. align OFF datum 은 기존과 동일.
</uat>

<notes>
- 의미: "Teach Datum" 이 datum 기준 + 패턴 기준을 같은 이미지에서 함께 앵커. 별도 golden 패턴 기준을 원하면 패턴 모델 생성으로 따로 set 가능(드문 워크플로).
- TryRestoreDatumGeometry(silent 복원)는 RefMatch 갱신 대상 아님 — 건드리지 말 것.
</notes>
