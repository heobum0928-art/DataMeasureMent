---
phase: "57.1"
plan: "57.1-11"
subsystem: UI / Datum Pattern Align
tags: [datum, pattern-match, align, refmatch, teach]
requires: ["57.1-06"]
provides: ["RefMatch re-anchor on datum re-teach (Teach=Find consistency)"]
affects: ["WPF_Example/UI/ContentItem/MainView.xaml.cs"]
tech-stack:
  added: []
  patterns: ["align-gate + model-exists guard + TryFindRefPose re-anchor (mirrors InvokeCreatePatternModel)"]
key-files:
  created: []
  modified:
    - WPF_Example/UI/ContentItem/MainView.xaml.cs
decisions:
  - "헬퍼는 InvokeCreatePatternModel:2856 의 TryFindRefPose 사용례와 byte-identical 시그니처/파라미터로 일치 — 런타임 부호 일관성"
  - "DualImage 경로는 패턴=가로축 imgH 기준으로 RefMatch 재산출, finally 의 imgH.Dispose() 전에 호출"
  - "align OFF / 모델 파일 없음 → 즉시 return (회귀 0). TryFindRefPose 실패 → 기존 RefMatch 유지 silent"
  - "TryRestoreDatumGeometry(silent 복원)는 RefMatch 대상 아님 — 무변경"
metrics:
  duration: 8
  completed: 2026-06-22
---

# Phase 57.1 Plan 11: 재티칭 시 패턴 기준(RefMatch) 동기화 Summary

align ON datum 재티칭(Teach Datum) 직후 같은 티칭 이미지에서 `TryFindRefPose` 를 재실행해 `RefMatch`(2-패턴이면 `RefMatch2`)를 재앵커 → Test Find 의 alignRigid≈identity → slate blue(Find)가 yellow(Teach)와 일치하도록 desync/이중보정을 해소.

## What Changed

`MainView.xaml.cs` 에 private 헬퍼 `RefreshPatternRefPoseAfterTeach(DatumConfig, HImage)` 추가:

- 게이트: `datum/patternImage null` → return, `!IsPatternAlignEnabled` → return, `ResolveDatumModelPath` 결과 빈값 또는 모델 파일 미존재 → return.
- `PatternMatchService.TryFindRefPose` 로 `RefMatchRow/Col/AngleDeg` 갱신.
- `PatternRoi2_Length1/2 > 0` 이면 `ResolveDatumModelPath2` + 파일 존재 + `TryFindRefPose` 성공 시 `RefMatch2Row/Col` 갱신.
- `[ALIGN-REFRESH]` Trace 로그.

3 teach 성공 경로에 `if (ok) { RefreshPatternRefPoseAfterTeach(...); }` 삽입:

1. `InvokeTryTeachDatum` 단일-이미지 (`TryTeachDatum(img, ...)` 직후, `img`)
2. `InvokeTryTeachDatum` DualImage (`TryTeachDatum(imgH, imgV, ...)` 직후, **imgH dispose(finally) 전**, 패턴=가로축 `imgH`)
3. `InvokeTryTeachDatumForEdit` (`TryTeachDatum(img, datum, ...)` 직후, `img`)

## Verification

- grep: 헬퍼 정의 1 + 호출 3 = 4건 확인 (MainView.xaml.cs:2682 정의 / :1224 edit / :2644 DualImage / :2653 단일).
- MSBuild Debug/x64 Build: **0 errors**. 신규 warning **0** (잔존 warning 은 전부 Phase 33 CS0618 마이그레이션 deprecation + VirtualCamera CS0162 unreachable + MSB3884 ruleset = 기존 baseline, MainView.xaml.cs 무관).
- align OFF 회귀: 헬퍼 진입 즉시 `!IsPatternAlignEnabled` return → 기존 teach 동작 무변경.
- 기존 teach 성공 후속(SetDatumOverlay/PublishDatumRoiCandidates/UpdateDatumRefCoordsLabel) 무변경.

## Deviations from Plan

None - plan executed exactly as written. Plan 헬퍼 코드와 byte-identical (TryFindRefPose 시그니처는 InvokeCreatePatternModel:2856 사용례와 이미 일치 확인).

## UAT (사용자 검증 대기)

SIMUL: align ON VTH datum → Teach Datum 재티칭 → Test Find → slate blue 검출 기준선이 yellow 티칭선과 일치(어긋남 해소). `[ALIGN-REFRESH]` + `[ALIGN] thetaDeg≈0` 로그로 확인. align OFF datum 은 기존과 동일.

## Commits

- 7e2d896: fix(57.1-11): sync pattern RefMatch on datum re-teach (Teach=Find)

## Self-Check: PASSED
- FOUND: WPF_Example/UI/ContentItem/MainView.xaml.cs (modified, RefreshPatternRefPoseAfterTeach 헬퍼 + 3 호출)
- FOUND: commit 7e2d896
