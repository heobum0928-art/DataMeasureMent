---
phase: "57.1"
name: pattern-roi-verification-safety
status: pending_uat
created: 2026-06-22
mode: SIMUL (Cal_Image 셋)
---

# Phase 57.1 UAT — 패턴 ROI 검증 & 안전장치

코드 4/4 완료(빌드 0 errors). 아래는 SIMUL 모드 + 기존 Cal_Image 셋으로 수행할 육안 판정.

## Test 1 — (D-03/D-01) Datum 노드 cyan 패턴 ROI 표시
- [ ] Cal_Image 로드 → PatternRoi 설정된 Datum 노드 **단독 선택** → 결과 화면에 cyan 보정 ROI 박스 표시
- [ ] `chk_overlayPattern` 토글 on/off 시 cyan 박스 일관 표시/숨김
- [ ] **Top/Bottom Datum** 에 IsPatternAlignEnabled on → 검사 → 보정된 위치에 cyan ROI 육안 확인(= Top/Bottom 도 보정 적용됨을 확인)
- [ ] Measurement/Shot/FAI 노드 선택 시 기존 표시 회귀 없음

## Test 2 — (D-02) 회전각 진단 (90° 어긋남 여부)
- [ ] 패턴 보정 켠 Datum 검사 실행 → Trace 로그에 `[ALIGN-ROI]` 항목(ROI명/ROI_Phi/rotAngle/roiPhi deg) 출력 확인
- [ ] `[ALIGN] thetaDeg`(baseline 각)와 `[ALIGN-ROI] rotAngleDeg` 부호·크기 일치 → 90° 어긋남 없음 확인
- [ ] 측정 결과값(mm)이 Phase 57 대비 무변경(회귀 0) — swap 없음 확증
- [ ] length1 축이 phi 방향(장축)으로 ROI 정렬과 일치하는지 육안

## Test 3 — (D-04) 패턴 버튼 안전장치
- [ ] **비-Datum 노드**(Measurement/Shot 등) 선택 → 패턴1/패턴2/패턴모델생성 버튼 **회색(비활성)**
- [ ] **Datum 노드** 선택 → 3버튼 활성 + 정상 진입
- [ ] (혹 활성 상태로) 비-Datum 에서 클릭 시 "Datum 티칭 존을 먼저 선택하세요" 알림 후 early-out

## Test 4 — (57.1-04) Test Find 가 패턴 보정을 따라가나 (Top/Bottom/Side 전부)
- [ ] 패턴 보정 켠(IsPatternAlignEnabled) Datum 선택 → 부품이 이동/회전한 테스트 이미지로 **Test Find** → ROI(cyan/DetectedOrigin 십자)가 부품을 **따라 이동**(공칭 고정 아님)
- [ ] Trace 에 `[ALIGN]`/`[ALIGN-ROI]` 로그 출력 (검사 안 돌리고 티칭서 확인)
- [ ] Top / Bottom / Side 각각에서 동작
- [ ] align off Datum 은 기존 Test Find 동작과 동일(회귀 0)

## Test 5 — (57.1-05) 패턴 ROI 그리기 버튼 진행 확인창
- [ ] Datum 노드 선택 → [패턴 1] 클릭 → "새로 그리시겠습니까?" OK/Cancel 표시
- [ ] [취소] → 무동작(그리기 모드 미진입), [확인] → 기존대로 드래그 그리기 진입
- [ ] [패턴 2] 동일 동작
- [ ] 비-Datum 노드 → 기존 "Datum 티칭 존을 먼저 선택하세요" 유지

## Test 6 — (57.1-07 #2) 측정 ROI 표시 90° 정상화 + (#1) 패턴 ROI 위치보정
- [ ] 보정 검사/Test Find → 측정 ROI 박스(green)가 가로/세로 비율 정상(90° 안 돌아감)
- [ ] 측정값(mm)은 Phase 57 대비 무변경 (표시만 수정, 측정 회귀 0)
- [ ] cyan 패턴 ROI 가 보정 시 매칭된(부품 따라간) 위치로 이동 표시
- [ ] 비보정 datum 은 패턴 ROI 공칭 위치 그대로 (회귀 0)

## Test 7 — (57.1-08) EdgeTrimCount 정렬 + % 절사
- [ ] EdgeToLineAngle 등 라인피팅 측정 → 마젠타 raw 에지점이 위치 정렬 후 상/하위 % 절사되어 중간만 남음, 피팅선 깔끔
- [ ] EdgeTrimCount=20 → 상위 20% + 하위 20% 제거 (양끝 각 %)
- [ ] ⚠ EdgeTrimCount 의미 변경(개수→%) — 기존 레시피 값 재확인
- [ ] (양쪽 에지 섞이면 First/Last 권장 — 위치절사론 반대쪽 에지 미제거)

## Test 8 — (57.1-09/10) trim 통일(Datum 포함) + UI %
- [ ] Datum 검출(TwoLineIntersect/CircleTwoHorizontal/VerticalTwoHorizontal) 에지 trim 이 정렬+% 절사로 동작 (측정과 동일)
- [ ] 전 측정 trim 도 동일 SortAndTrimPercent 사용 (단일소스)
- [ ] PropertyGrid trim 라벨이 "Edge Trim (%)" (DualImage 는 Point/Line 구분) 표시
- [ ] 기존 레시피 로드/저장 정상 (프로퍼티명/INI 키 보존)
- [ ] ⚠ EdgeTrimCount 값 % 재해석 — 전 항목(측정+Datum) 값 재확인

## Test 9 — (57.1-11) 재티칭 시 RefMatch 동기화 (Teach=Find)
- [ ] align ON VTH datum → Teach Datum 재티칭 → Test Find → slate blue(find)가 yellow(teach)와 일치
- [ ] Trace 로그 `[ALIGN-REFRESH]` 출력 + `[ALIGN] thetaDeg ≈ 0`
- [ ] align OFF datum 은 기존과 동일(회귀 0)

## 결과
- Test 1 (90°회전): PASS (사용자 확인)
- Test 2: 
- Test 3: 
- Test 4: (57.1-06) 
- Test 5: 
- Test 6: (57.1-07) 90°=PASS, 위치보정=정상(Viewer 표현차이, 측정정상)
- Test 7: (57.1-08) 
- Test 8: (57.1-09/10) 
