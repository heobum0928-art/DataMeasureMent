---
phase: 59-vision-algorithm-b-2026-06-23
plan: 03
type: execute
wave: 3
depends_on: [59-01, 59-02]
files_modified:
  - WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs
autonomous: true
requirements: [AV-03, AV-04]
must_haves:
  truths:
    - "EthernetVisionHandler exposes the matcher: public AlignShapeMatchService Matcher { get; private set; }"
    - "Matcher is created in Initialize() for ALL paths (None, connected, connect-failed, exception) so it is never null after Initialize"
    - "The whole solution builds (msbuild Debug/x64) with all three new Phase-59 files compiled"
    - "PatternMatchService.cs and RecipeFileHelper.cs are unchanged (reuse-by-call, not edited)"
    - "Existing Grabber files (HikCamera.cs, DeviceHandler.cs, Sequence/*, Action_*, VirtualCamera.cs, SystemHandler.cs) are unchanged"
  artifacts:
    - path: "WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs"
      provides: "Matcher property (lazy AlignShapeMatchService) on the phase-58 singleton"
      contains: "public AlignShapeMatchService Matcher"
  key_links:
    - from: "EthernetVisionHandler.Initialize()"
      to: "AlignShapeMatchService (Plan 02)"
      via: "Matcher = new AlignShapeMatchService() on every path"
      pattern: "Matcher = new AlignShapeMatchService"
    - from: "EthernetVisionHandler.Matcher"
      to: "Phase 61 UI / Phase 62 TCP (downstream consumers)"
      via: "public getter exposing TryTeach/Run/HasTemplate"
      pattern: "public AlignShapeMatchService Matcher"
---

<objective>
Wire the Plan 02 service into the phase-58 singleton and prove the whole subsystem integrates without touching any prohibited file.

1. Add `public AlignShapeMatchService Matcher { get; private set; }` to `EthernetVisionHandler` (D-02) and create it in `Initialize()` on EVERY path (mode==None early-return, connect success, connect failure, and the catch) so downstream callers (Phase 61 UI, Phase 62 TCP) always get a non-null, stateless matcher. `EthernetVisionHandler.cs` is OUR phase-58 code — editing it is explicitly allowed; SystemHandler and all framework files are NOT touched (D-02: the phase-58 one-line init already starts the handler).
2. Build the full solution (Debug/x64) to prove all three new Phase-59 files (`AlignResult.cs`, `AlignRefPose.cs`, `AlignShapeMatchService.cs`) compile and integrate.
3. Anti-goal git verification: prove `PatternMatchService.cs`, `RecipeFileHelper.cs`, and every Grabber inspection file are byte-unchanged — the ONLY modified existing file in Phase 59 is `EthernetVisionHandler.cs`; the csproj compile-list additions come from Plans 01/02.

Purpose: AV-03/AV-04 integration — expose the teach/run service on the singleton + guarantee the HARD CONSTRAINT (no Grabber edits, no PatternMatchService edit) held.
Output: `EthernetVisionHandler.cs` (+ Matcher property and 2 creation points), build pass, anti-goal proof. ADD-ONLY edits to our own handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-vision-algorithm-b-2026-06-23/59-CONTEXT.md
@.planning/phases/59-vision-algorithm-b-2026-06-23/59-PATTERNS.md
@WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs

<interfaces>
<!-- The exact phase-58 Initialize() this plan edits (current committed content). Insert Matcher creation on every path. -->
EthernetVisionHandler.cs (namespace ReringProject, K&R braces). Current Initialize():
```csharp
public void Initialize() {
    try {
        bool bModeOff = SystemSetting.Handle.EthernetVisionMode == EEthernetVisionMode.None;
        if (bModeOff) {
            Logging.PrintLog((int)ELogType.Camera, "[ETHERNET] mode = None, skip connect");
            IsInitialized = false;
            return;                                   // ← Matcher must already be created before this return
        }

        Camera = new EthernetAlignCamera();
        string camIp = SystemSetting.Handle.EthernetCameraIp;
        bool bConnected = Camera.Connect(camIp);

        IsInitialized = bConnected;
        if (bConnected) {
            Logging.PrintLog((int)ELogType.Camera, "[ETHERNET] connected: {0}", camIp);
        }
        else {
            Logging.PrintLog((int)ELogType.Camera, "[ETHERNET] connect failed (fallback active): {0}", camIp);
        }
    }
    catch (Exception ex) {
        IsInitialized = false;                        // ← Matcher null-guard also created in catch
        Logging.PrintLog((int)ELogType.Error, "[ETHERNET] EthernetVisionHandler.Initialize error: {0}", ex.Message);
    }
}
```

From Plan 02 (namespace ReringProject — same as this handler, NO using needed):
```csharp
public class AlignShapeMatchService {
    public AlignShapeMatchService();
    public bool TryTeach(HImage img, double roiRow, double roiCol, double roiPhi, double roiLen1, double roiLen2, EEthernetVisionMode mode, out string error);
    public AlignResult Run(HImage img, EEthernetVisionMode mode);
    public bool HasTemplate(EEthernetVisionMode mode);
    public bool TryLoadTemplate(EEthernetVisionMode mode);
}
```

Build command (the SIMUL_MODE config used by phase 58):
`msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /m`
Phase 38 baseline ≈ 6 pre-existing warnings; Phase-59 files must add 0 new warnings.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Matcher property to EthernetVisionHandler and create it on every Initialize() path</name>
  <files>WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs</files>
  <read_first>
    - WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs (current phase-58 content — the Camera property declaration line 18 + the full Initialize() lines 28-53, the paths to cover)
    - WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs (Plan 02 — the type being instantiated; confirm parameterless ctor)
    - .planning/phases/59-vision-algorithm-b-2026-06-23/59-PATTERNS.md (EthernetVisionHandler.cs modification section lines 303-352 — the Matcher property + Initialize() insertion pattern, including the None-path and catch-path creation)
    - .planning/phases/59-vision-algorithm-b-2026-06-23/59-CONTEXT.md (D-02 ownership/lazy creation; D-06 stateless service)
  </read_first>
  <action>
    Edit `WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs` (OUR phase-58 file — editing allowed per D-02). Keep K&R braces, C# 7.2. Mark every added line with `//260624 hbk Phase 59`.

    EDIT 1 — declare the property. Immediately AFTER the existing `Camera` property (currently line 18: `public EthernetAlignCamera Camera { get; private set; }`), add:
    ```csharp
        //260624 hbk Phase 59 — D-02: Shape matching align 서비스 (handler 소유, stateless). Mode 무관 항상 생성.
        public AlignShapeMatchService Matcher { get; private set; }
    ```
    No `using` is needed — `AlignShapeMatchService` is in namespace `ReringProject`, same as this handler.

    EDIT 2 — create the matcher so it is never null after Initialize() (D-02). Because the service is stateless and cheap, create it ONCE as the FIRST statement inside the `try`, before the mode gate — that single placement covers the None early-return, the connected path, and the connect-failed path. Then add a SECOND creation inside the `catch` as a null-guard (covers an exception thrown before the first assignment). Concretely:

    - As the FIRST line inside `try {` (before `bool bModeOff = ...`), insert:
      ```csharp
                //260624 hbk Phase 59 — D-02: Matcher 는 stateless → 모드/연결 결과 무관하게 항상 생성
                Matcher = new AlignShapeMatchService();
      ```
    - Inside the existing `catch (Exception ex) {` block, after `IsInitialized = false;`, insert:
      ```csharp
                //260624 hbk Phase 59 — 예외 경로에서도 Matcher null 방지
                if (Matcher == null) {
                    Matcher = new AlignShapeMatchService();
                }
      ```

    Constraints:
    - Do NOT change any existing line of Initialize() (the mode gate, Camera creation, Connect call, IsInitialized assignments, and all logs stay byte-identical). Your diff must be PURELY ADDITIVE: the one property declaration + the two inserted creation blocks. No removed or edited existing lines.
    - Do NOT touch SystemHandler.cs — D-02 states the phase-58 single init line already starts this handler; the Matcher is created internally, so no new call site is needed.
    - The handler must remain a sealed singleton (do not change `sealed` / `Handle` / the private ctor).
  </action>
  <verify>
    <automated>grep -q "public AlignShapeMatchService Matcher" "WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs" && grep -c "Matcher = new AlignShapeMatchService" "WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs" | grep -qx "2" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `EthernetVisionHandler.cs` declares `public AlignShapeMatchService Matcher { get; private set; }`.
    - `Initialize()` assigns `Matcher = new AlignShapeMatchService();` as the first statement inside the `try` (covering None / connected / connect-failed) AND assigns it again inside the `catch` under an `if (Matcher == null)` guard — exactly two `Matcher = new AlignShapeMatchService` occurrences.
    - The edit is purely additive: the property line + two creation blocks; no existing Initialize() line removed or altered (mode gate, Camera, Connect, IsInitialized, logs unchanged).
    - Handler remains `sealed`, keeps `static Handle` and the private constructor.
    - No `using` added (AlignShapeMatchService is in namespace ReringProject); no C# 8+ constructs.
  </acceptance_criteria>
  <done>The phase-58 singleton now owns a never-null, stateless AlignShapeMatchService exposed as Matcher for Phase 61/62 consumers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build the full solution (Debug/x64) and confirm all three Phase-59 files compile</name>
  <files>(build-only — no files modified)</files>
  <read_first>
    - WPF_Example/DatumMeasurement.csproj (confirm the three new compile entries from Plans 01/02 are present: AlignResult.cs, AlignRefPose.cs, AlignShapeMatchService.cs)
    - .planning/phases/58-config-camera-a-2026-06-23/58-03-PLAN.md (Task 2 build pattern — the exact msbuild invocation + 0-error / 0-new-warning expectation this repo uses)
  </read_first>
  <action>
    Build the entire solution in the Debug/x64 (SIMUL_MODE) configuration to prove the Phase-59 additions compile and integrate with the phase-58 handler. Run the repo-standard msbuild invocation (full path to MSBuild.exe or a Developer Command Prompt, same as phase 58):
    `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /m`

    The build MUST succeed with 0 errors. Pre-existing warnings (the ~6-warning Phase 38 baseline) are acceptable; the three new Phase-59 files (AlignResult.cs, AlignRefPose.cs, AlignShapeMatchService.cs) and the EthernetVisionHandler.cs edit must introduce 0 NEW warnings.

    If the build fails, the most likely causes (fix and rebuild):
    - Missing csproj `<Compile Include>` for one of the three new files (Plans 01/02) → add the entry in the Custom\EthernetVision ItemGroup.
    - `CS0246` on `AlignResult` / `AlignRefPose` / `AlignShapeMatchService` → the referencing file is missing a `using` or the type's file is unregistered.
    - `CS1501`/`CS1502` (wrong argument count/type) on a `_matcher.Try*` call → re-check the call against the REAL PatternMatchService signature in Plan 02's `<interfaces>` block (TryFindPose has 7 value args before the 4 out-params; angles are already in degrees).
    - Any C# 8+ construct (switch expression / record / nullable) → rewrite as C# 7.2.
  </action>
  <verify>
    <automated>msbuild "WPF_Example/DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /m /nologo /clp:ErrorsOnly && echo BUILD_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64` completes with 0 errors.
    - All three new files (AlignResult.cs, AlignRefPose.cs, AlignShapeMatchService.cs) are compiled (present in the csproj `<Compile>` list and error-free).
    - No NEW compiler warnings originate from the Phase-59 files or the EthernetVisionHandler.cs edit (Phase 38 ~6-warning baseline unchanged).
  </acceptance_criteria>
  <done>The whole DataMeasurement solution builds in Debug/x64 with the Phase-59 align service compiled and wired into the handler.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Anti-goal verification — PatternMatchService.cs, RecipeFileHelper.cs, and all Grabber files unchanged</name>
  <files>(verification-only — no files modified)</files>
  <read_first>
    - .planning/phases/59-vision-algorithm-b-2026-06-23/59-CONTEXT.md (HARD CONSTRAINT: PatternMatchService reuse-by-call only — NOT modified; RecipeFileHelper NOT edited; existing Grabber Sequence/Action/SystemHandler/HikCamera/VirtualCamera/DeviceHandler ADD-ONLY; only EthernetVisionHandler.cs may be edited)
    - .planning/ROADMAP.md (Phase 59 + v1.3 common constraint line — existing Grabber inspection paths unmodified)
  </read_first>
  <action>
    Verification-only (no code changes) that the HARD CONSTRAINT held. Using git, confirm the ONLY modified existing files in Phase 59 are:
      - `WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs` (Plan 03 — Matcher property, our own phase-58 code, additive)
      - `WPF_Example/DatumMeasurement.csproj` (Plans 01/02 — three additive `<Compile>` entries)
    Everything else in Phase 59 is NEW (AlignResult.cs, AlignRefPose.cs, AlignShapeMatchService.cs).

    Run:
    `git status --porcelain`
    and
    `git diff --stat`

    Confirm specifically that NONE of these prohibited files appear in the modified set:
      - WPF_Example/Halcon/Algorithms/PatternMatchService.cs   (reuse-by-call ONLY — must be byte-unchanged)
      - WPF_Example/Utility/RecipeFileHelper.cs                (path pattern referenced, NOT edited)
      - WPF_Example/Device/Camera/Hik/HikCamera.cs
      - WPF_Example/Device/DeviceHandler.cs  and  WPF_Example/Custom/Device/DeviceHandler.cs
      - WPF_Example/Device/Camera/VirtualCamera.cs
      - WPF_Example/SystemHandler.cs  and  WPF_Example/Custom/SystemHandler.cs
      - any file under WPF_Example/Sequence/ or WPF_Example/Custom/Sequence/ (Sequence_*, Action_*)

    Also confirm the `EthernetVisionHandler.cs` diff is purely additive (only ADDED lines: the Matcher property + the two `//260624 hbk Phase 59` creation blocks; no removed lines, no edits to the phase-58 Camera/Connect/mode-gate logic).

    If any prohibited file appears modified, STOP and report it as a HARD-CONSTRAINT violation — the executor must revert that file to its committed state before this plan is complete. This task makes NO edits itself; it is the gate that proves the anti-goal.
  </action>
  <verify>
    <automated>git diff --name-only | grep -E "Halcon/Algorithms/PatternMatchService.cs|Utility/RecipeFileHelper.cs|Device/Camera/Hik/HikCamera.cs|Device/DeviceHandler.cs|Custom/Device/DeviceHandler.cs|Device/Camera/VirtualCamera.cs|SystemHandler.cs|/Sequence/|Sequence_|Action_" && echo "VIOLATION — prohibited file modified" || echo "ANTIGOAL_PASS — no prohibited file modified"</automated>
    <automated>git diff -- WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs | grep -E "^-" | grep -v "^---" | grep -q . && echo "WARN — EthernetVisionHandler has removed/changed lines (expected ADD-ONLY)" || echo "HANDLER_ADDONLY_PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` does NOT list PatternMatchService.cs, RecipeFileHelper.cs, HikCamera.cs, DeviceHandler.cs (framework or Custom), VirtualCamera.cs, SystemHandler.cs (framework or Custom), or any Sequence_*/Action_*/Sequence-folder file.
    - `git diff WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs` shows only ADDED lines (the Matcher property + the two Phase-59 creation blocks) — no deletions, no edits to the phase-58 logic.
    - The full modified-existing-file set is limited to: `EthernetVisionHandler.cs` (Plan 03) and `DatumMeasurement.csproj` (Plans 01/02 compile entries).
  </acceptance_criteria>
  <done>The HARD CONSTRAINT is proven: PatternMatchService.cs + RecipeFileHelper.cs + every Grabber inspection file are byte-unchanged; the only existing-file touches are the additive EthernetVisionHandler.cs edit and the csproj compile registrations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local config (SystemSetting) → EthernetVisionHandler.Initialize() | mode read from local INI; gates Matcher/Camera creation (Matcher is created regardless of mode) |
| (no new untrusted input) | Matcher creation is a local object instantiation; the matcher's own file-I/O surface is covered by Plan 02's threat model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-05 | Denial of Service | `Matcher = new AlignShapeMatchService()` throwing during startup Initialize() | mitigate | The matcher ctor only constructs a composed `PatternMatchService` (no I/O, no HALCON). It runs inside the existing Initialize() try-catch; an exception is logged and `IsInitialized=false`, and the catch re-creates the matcher under a null-guard. Per Phase 58 design, SystemHandler calls Initialize() inside its own try-catch after Grabber init, so this cannot disturb Grabber. |
| T-59-06 | Tampering | EthernetVisionMode selecting connect behavior | accept | Local operator-controlled INI; Matcher is created irrespective of mode, so mode tampering only affects camera connect (already accepted in Phase 58 T-58-05). No remote trigger. |

No new untrusted-input attack surface in this plan: object wiring + build + git verification only. The file-I/O / deserialization surface lives in Plan 02 (mitigated there with TypeNameHandling.None).
</threat_model>

<verification>
- Maps to ROADMAP Success Criterion 1-4 (integration): exposing `Matcher` makes the Plan 02 TryTeach (.shm save/load), Run (find Row/Col/Angle/Score → Tray X/Y, Bottom X/Y/Theta), and per-mode template logic reachable by Phase 61/62 consumers.
- Whole-phase build verification (msbuild Debug/x64, 0 errors, 0 new warnings) performed in Task 2 — proves AlignResult.cs + AlignRefPose.cs + AlignShapeMatchService.cs + the handler edit all compile together.
- HARD CONSTRAINT (no Grabber edits, PatternMatchService + RecipeFileHelper unmodified) proven by the Task 3 git gate — the v1.3 common constraint "기존 Grabber 검사 코드 절대 수정 금지" and the phase intent "do NOT modify PatternMatchService.cs".
</verification>

<success_criteria>
- `EthernetVisionHandler` exposes `Matcher` (never null after Initialize, stateless, created on all paths).
- Solution builds in Debug/x64 with 0 errors and 0 new warnings from Phase-59 files.
- Anti-goal proven: PatternMatchService.cs, RecipeFileHelper.cs, and all Grabber files byte-unchanged; only existing-file touches are EthernetVisionHandler.cs (additive) + csproj compile entries.
</success_criteria>

<output>
After completion, create `.planning/phases/59-vision-algorithm-b-2026-06-23/59-03-SUMMARY.md`
</output>
