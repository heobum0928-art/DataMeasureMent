---
phase: 60-calibration-bottom-c-2026-06-23
plan: 03
type: execute
wave: 3
depends_on: ["60-01", "60-02"]
files_modified:
  - WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs
  - WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [AV-05]
must_haves:
  truths:
    - "Each calibration step extracts the Cal jig's circular feature center via fit_circle_contour_xld (edges→atukey fit), independent of the 2-pattern shape match"
    - "Accumulating ≥MIN_STEPS per-step jig centers (ideally 36, eccentric trajectory) then fitting a second circle yields the picker rotation center via robust least-squares"
    - "The fitted picker center is stored to SystemSetting [ETHERNET_VISION] PickerCenterRow/Col"
    - "EthernetVisionHandler owns the calibration service via a PickerCal property (mirrors Matcher)"
    - "The solution builds (msbuild Debug/x64) with the new file registered in csproj"
    - "VisionAlgorithmService.cs / PatternMatchService.cs / RecipeFileHelper.cs / all Grabber files are UNCHANGED"
  artifacts:
    - path: "WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs"
      provides: "Stateful Reset/TryAddStep(jig circle fit)/TryComputePickerCenter(eccentric fit); fit_circle_contour_xld used TWICE"
      contains: "FitCircleContourXld"
    - path: "WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs"
      provides: "PickerCal property + lazy creation"
      contains: "PickerCal"
    - path: "WPF_Example/DatumMeasurement.csproj"
      provides: "Compile Include for PickerCenterCalibrationService.cs"
      contains: "PickerCenterCalibrationService.cs"
  key_links:
    - from: "PickerCenterCalibrationService.TryAddStep"
      to: "HOperatorSet.FitCircleContourXld (per-step Cal jig circle)"
      via: "GenCircle search ROI → ReduceDomain → EdgesSubPix → FitCircleContourXld → jig center"
      pattern: "FitCircleContourXld"
    - from: "PickerCenterCalibrationService.TryComputePickerCenter"
      to: "SystemSetting.PickerCenterRow/Col + Save"
      via: "GenContourPolygonXld(accumulated jig centers) → FitCircleContourXld → picker center → store"
      pattern: "PickerCenterRow"
---

<objective>
Build the calibration core (D-01, D-02 corrected, D-03) and wire it in (D-01), then prove the
build and the anti-goal.

The new `PickerCenterCalibrationService` is a STATEFUL accumulator. External callers (Phase 61
UI / PLC trigger) call `TryAddStep` once per picker step (36×). CORRECTED per-step extraction
(D-02, 2026-06-24): each step's center is the **Cal jig's circular feature**, fitted with
`fit_circle_contour_xld` (edges→atukey) INSIDE this service — independent of the production
part's 2-pattern shape match. The jig moves as it rotates, so the caller passes a search ROI
(circle) covering the sweep (or whole image). `TryComputePickerCenter` then fits a SECOND
robust circle (`FitCircleContourXld` "atukey") through the accumulated jig centers — that
eccentric-trajectory circle's center IS the picker's true rotation center — and stores it.

So `fit_circle_contour_xld` is used TWICE: (1) per-step jig circle → jig center, (2) the 36
accumulated jig centers → picker center. There is NO dependency on AlignShapeMatchService /
`TryFindCenter` (that approach is dropped); calibration is fully independent of Phase 59.

Purpose: closes AV-05 (per-step Cal jig circle + 36-step eccentric circle LS → picker center)
at the service layer. The 36-step rotation is external hardware; this service only fits a
circle per grab and accumulates.
Output: PickerCenterCalibrationService.cs + EthernetVisionHandler.PickerCal + csproj entry +
green Debug/x64 build + git proof the anti-goal files are untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-calibration-bottom-c-2026-06-23/60-CONTEXT.md

<read_first>
- WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs L253-336 (`TryFindCircle`) — the EXACT
  GenCircle → ReduceDomain → EdgesSubPix("canny") → FitCircleContourXld("atukey", -1, 2, 0, 5, 2)
  pattern to REPLICATE for the per-step Cal jig circle. Call HOperatorSet directly in the new
  service; do NOT call into or modify VisionAlgorithmService — keep the new service
  Grabber-independent. Note the edge params: `sigma`, `threshold/2` (low), `threshold` (high)
  and the `row.Length == 0` guard.
- WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs — the `Matcher` property
  (L21) + its lazy creation in Initialize (L34) + the exception-path null guard (L58-60).
  PickerCal mirrors Matcher exactly.
- WPF_Example/Custom/SystemSetting.cs — PickerCenterRow/Col (Plan 60-01).
- WPF_Example/Setting/SystemSetting.cs L281 — `public void Save()` is the real persistence
  entry point. Call `SystemSetting.Handle.Save()` after writing the center.
- WPF_Example/DatumMeasurement.csproj L239-244 — the EthernetVision Compile Include block
  to add the new file to.
</read_first>

<interfaces>
The per-step Cal jig circle-fit pattern to REPLICATE (VisionAlgorithmService.cs L294-322):
```csharp
HObject circleRegion, imageReduced, edges;
HOperatorSet.GenCircle(out circleRegion, searchRow, searchCol, searchRadius);
HOperatorSet.ReduceDomain(image, circleRegion, out imageReduced);
HOperatorSet.EdgesSubPix(imageReduced, out edges,
    "canny", Math.Max(0.4, sigma), Math.Max(1, threshold / 2), Math.Max(2, threshold));
HTuple row, column, rad, startPhi, endPhi, pointOrder;
HOperatorSet.FitCircleContourXld(edges, "atukey", -1, 2, 0, 5, 2,
    out row, out column, out rad, out startPhi, out endPhi, out pointOrder);
// guard row.Length == 0 ; jig center = (row[0].D, column[0].D)
```

The eccentric-fit pattern (accumulated jig centers → picker center):
```csharp
HObject contour;
HOperatorSet.GenContourPolygonXld(out contour, allRows, allCols); // HTuple of accumulated doubles
HTuple row, column, rad, startPhi, endPhi, pointOrder;
HOperatorSet.FitCircleContourXld(contour, "atukey", -1, 2, 0, 5, 2,
    out row, out column, out rad, out startPhi, out endPhi, out pointOrder);
// picker center = (row[0].D, column[0].D) ; guard row.Length == 0
```

From EthernetVisionHandler.cs (existing Matcher pattern to mirror):
```csharp
public AlignShapeMatchService Matcher { get; private set; }
// in Initialize(): Matcher = new AlignShapeMatchService();
// in catch: if (Matcher == null) { Matcher = new AlignShapeMatchService(); }
```

From SystemSetting.cs (Plan 60-01) + Setting/SystemSetting.cs:
```csharp
public double PickerCenterRow { get; set; } = 0.0;
public double PickerCenterCol { get; set; } = 0.0;
public void Save();   // L281 — real persistence entry point
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PickerCenterCalibrationService (per-step jig circle fit + eccentric atukey fit + store)</name>
  <files>WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs</files>
  <behavior>
    - TryAddStep on an image with a clean circle inside the search ROI: fits the jig circle,
      accumulates its center, returns true; StepCount increments.
    - TryAddStep with img==null or no edges/no circle in ROI: returns false, StepCount unchanged.
    - TryComputePickerCenter with < MIN_STEPS accumulated: returns false ("누적 점 부족").
    - TryComputePickerCenter with ≥ MIN_STEPS valid eccentric centers: fits the second circle,
      writes SystemSetting.PickerCenterRow/Col, calls Save(), returns true.
    - Degenerate eccentric fit (radius out of [MIN,MAX] / NaN): returns false, does NOT persist.
    - Any HALCON exception: returns false (no throw); all HObject/HTuple disposed in finally.
  </behavior>
  <action>
Create a new file `WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs`
in namespace `ReringProject`. K&R brace, C# 7.2, all HALCON in try-catch + HTuple/HObject
dispose in finally (D-03/D-06).

CORRECTED D-02: the per-step center = the Cal jig circle fitted by THIS service via
`fit_circle_contour_xld` (edges→atukey), NOT the 2-pattern midpoint. Do NOT call
`AlignShapeMatchService` / `EthernetVisionHandler.Handle.Matcher` anywhere in this file.

`TryAddStep` takes a search ROI (circle: searchRow/searchCol/searchRadius) covering the jig
sweep, plus edge params (sigma/threshold) so callers can tune. Provide sane const defaults.

```csharp
//260624 hbk Phase 60 — AV-05 picker center calibration (D-01/D-02 corrected/D-03)
using System;
using System.Collections.Generic;
using HalconDotNet;
using ReringProject.Setting;
using ReringProject.Utility;

namespace ReringProject {

    /// <summary>
    /// D-01: 피커 센터 캘리브레이션 — 상태형 누적 서비스.
    /// 피커가 전용 Cal 지그(깨끗한 원)를 픽업한 채 10°×36스텝 회전 → 각 스텝의 지그 원 중심이
    /// 편심원을 그림 → 그 편심원 중심 = 피커 실제 회전중심.
    /// D-02(정정): 스텝별 중심 = Cal 지그 원을 fit_circle_contour_xld 로 피팅한 중심 (2-패턴 아님).
    ///   → 이 서비스가 직접 에지→원 피팅 (VisionAlgorithmService.TryFindCircle 패턴 독립 복제, 무수정).
    /// fit_circle_contour_xld 2회 사용: ① 스텝별 지그 원, ② 36 지그 중심(편심) → 피커 중심.
    /// 외부(Phase 61 UI / PLC 트리거)가 스텝마다 TryAddStep 호출, ≥MIN_STEPS 후 TryComputePickerCenter.
    /// 비전은 회전을 제어하지 않음. 전 메서드 try-catch → false (D-06, Grabber 무영향).
    /// </summary>
    public class PickerCenterCalibrationService {

        // D-03: 편심원 피팅 최소 누적 점 수. 권장 36(=10°×36스텝). 일부 스텝 미검출 허용하되 안정 피팅 하한.
        private const int MIN_STEPS = 6;

        // D-03: 반경 정상 범위 가드(px). 피팅 결과가 이 범위 밖이면 비정상 → false.
        private const double MIN_RADIUS_PX = 1.0;
        private const double MAX_RADIUS_PX = 100000.0;

        // D-02: 지그 원 에지 추출 기본 파라미터 (TryFindCircle 패턴 — canny sigma / hysteresis threshold).
        private const double EDGE_SIGMA_DEFAULT     = 1.0;
        private const int    EDGE_THRESHOLD_DEFAULT = 30;

        // 누적 지그 중심점 (절대 row/col)
        private readonly List<double> _rows = new List<double>();
        private readonly List<double> _cols = new List<double>();

        // D-01: 누적 초기화.
        public void Reset() {
            _rows.Clear();
            _cols.Clear();
        }

        /// <summary>누적된 스텝 수.</summary>
        public int StepCount {
            get { return _rows.Count; }
        }

        // D-01/D-02: 한 스텝 이미지에서 검색 ROI(원) 내 Cal 지그 원을 fit_circle_contour_xld 로 피팅 → 중심 누적.
        // 기본 에지 파라미터 오버로드 (Phase 61 UI 가 ROI 만 전달).
        public bool TryAddStep(HImage img,
            double searchRow, double searchCol, double searchRadius,
            out string error) {
            return TryAddStep(img, searchRow, searchCol, searchRadius,
                EDGE_SIGMA_DEFAULT, EDGE_THRESHOLD_DEFAULT, out error);
        }

        // D-02 정정: 스텝별 지그 원 중심 추출 = 이 서비스 내부 에지→FitCircleContourXld (독립 구현).
        public bool TryAddStep(HImage img,
            double searchRow, double searchCol, double searchRadius,
            double sigma, int threshold,
            out string error) {
            error = null;
            if (img == null) {
                error = "img is null";
                return false;
            }

            HObject circleRegion = null;
            HObject imageReduced = null;
            HObject edges = null;
            HTuple fitRow = null;
            HTuple fitCol = null;
            HTuple fitRad = null;
            HTuple startPhi = null;
            HTuple endPhi = null;
            HTuple pointOrder = null;
            try {
                // 검색 ROI(원)로 도메인 축소 — 지그 sweep 커버 (또는 전체 이미지면 큰 radius)
                HOperatorSet.GenCircle(out circleRegion, searchRow, searchCol, searchRadius);
                HOperatorSet.ReduceDomain(img, circleRegion, out imageReduced);

                // 서브픽셀 에지 (TryFindCircle 과 동일 canny + hysteresis)
                HOperatorSet.EdgesSubPix(imageReduced, out edges,
                    "canny", Math.Max(0.4, sigma), Math.Max(1, threshold / 2), Math.Max(2, threshold));

                // ① fit_circle_contour_xld — 스텝별 지그 원
                HOperatorSet.FitCircleContourXld(edges, "atukey", -1, 2, 0, 5, 2,
                    out fitRow, out fitCol, out fitRad, out startPhi, out endPhi, out pointOrder);
                if (fitRow.Length == 0) {
                    error = "지그 원 피팅 실패 (에지/원 없음)";
                    return false;
                }

                double jigRow = fitRow[0].D;
                double jigCol = fitCol[0].D;
                _rows.Add(jigRow);
                _cols.Add(jigCol);

                Logging.PrintLog((int)ELogType.Camera,
                    "[PICKER_CAL] step {0} jig=({1:F2},{2:F2}) r={3:F2}",
                    _rows.Count, jigRow, jigCol, fitRad[0].D);
                return true;
            }
            catch (Exception ex) {
                error = "TryAddStep exception: " + ex.Message;
                Logging.PrintLog((int)ELogType.Error, "[PICKER_CAL] TryAddStep exception: {0}", ex.Message);
                return false;
            }
            finally {
                if (circleRegion != null) { try { circleRegion.Dispose(); } catch { } }
                if (imageReduced != null) { try { imageReduced.Dispose(); } catch { } }
                if (edges != null) { try { edges.Dispose(); } catch { } }
                if (fitRow != null) { try { fitRow.Dispose(); } catch { } }
                if (fitCol != null) { try { fitCol.Dispose(); } catch { } }
                if (fitRad != null) { try { fitRad.Dispose(); } catch { } }
                if (startPhi != null) { try { startPhi.Dispose(); } catch { } }
                if (endPhi != null) { try { endPhi.Dispose(); } catch { } }
                if (pointOrder != null) { try { pointOrder.Dispose(); } catch { } }
            }
        }

        // D-01/D-03: 누적 지그 중심들로 ② fit_circle_contour_xld atukey → 피커센터(row,col)+radius + SystemSetting 저장.
        public bool TryComputePickerCenter(out double row, out double col, out double radius, out string error) {
            row = 0.0;
            col = 0.0;
            radius = 0.0;
            error = null;

            if (_rows.Count < MIN_STEPS) {
                error = "누적 점 부족: " + _rows.Count + " < " + MIN_STEPS;
                return false;
            }

            HObject contour = null;
            HTuple allRows = null;
            HTuple allCols = null;
            HTuple fitRow = null;
            HTuple fitCol = null;
            HTuple fitRad = null;
            HTuple startPhi = null;
            HTuple endPhi = null;
            HTuple pointOrder = null;
            try {
                allRows = new HTuple(_rows.ToArray());
                allCols = new HTuple(_cols.ToArray());
                HOperatorSet.GenContourPolygonXld(out contour, allRows, allCols);
                HOperatorSet.FitCircleContourXld(contour, "atukey", -1, 2, 0, 5, 2,
                    out fitRow, out fitCol, out fitRad, out startPhi, out endPhi, out pointOrder);

                if (fitRow.Length == 0) {
                    error = "편심원 피팅 실패 (점 없음)";
                    return false;
                }

                double fittedRow = fitRow[0].D;
                double fittedCol = fitCol[0].D;
                double fittedRad = fitRad[0].D;

                bool bRadiusBad = (fittedRad < MIN_RADIUS_PX) || (fittedRad > MAX_RADIUS_PX)
                               || double.IsNaN(fittedRad) || double.IsInfinity(fittedRad);
                if (bRadiusBad) {
                    error = "피팅 반경 비정상: " + fittedRad.ToString("F2");
                    return false;
                }

                row = fittedRow;
                col = fittedCol;
                radius = fittedRad;

                // D-04: 피커센터 저장 (머신 단위 HW 캘 → SystemSetting [ETHERNET_VISION])
                SystemSetting.Handle.PickerCenterRow = fittedRow;
                SystemSetting.Handle.PickerCenterCol = fittedCol;
                SystemSetting.Handle.Save();

                Logging.PrintLog((int)ELogType.Camera,
                    "[PICKER_CAL] computed: center=({0:F2},{1:F2}) r={2:F2} from {3} steps",
                    fittedRow, fittedCol, fittedRad, _rows.Count);
                return true;
            }
            catch (Exception ex) {
                error = "TryComputePickerCenter exception: " + ex.Message;
                Logging.PrintLog((int)ELogType.Error, "[PICKER_CAL] compute exception: {0}", ex.Message);
                return false;
            }
            finally {
                if (contour != null) { try { contour.Dispose(); } catch { } }
                if (allRows != null) { try { allRows.Dispose(); } catch { } }
                if (allCols != null) { try { allCols.Dispose(); } catch { } }
                if (fitRow != null) { try { fitRow.Dispose(); } catch { } }
                if (fitCol != null) { try { fitCol.Dispose(); } catch { } }
                if (fitRad != null) { try { fitRad.Dispose(); } catch { } }
                if (startPhi != null) { try { startPhi.Dispose(); } catch { } }
                if (endPhi != null) { try { endPhi.Dispose(); } catch { } }
                if (pointOrder != null) { try { pointOrder.Dispose(); } catch { } }
            }
        }
    }
}
```

IMPORTANT while writing:
- Confirm `SystemSetting.Handle.Save()` exists (Setting/SystemSetting.cs L281 — it does). Use
  it verbatim. Do NOT invent a different persist method.
- Do NOT call into VisionAlgorithmService — replicate the HOperatorSet pattern inline only.
- Do NOT reference AlignShapeMatchService / Matcher / TryFindCenter anywhere — the per-step
  center is the Cal jig circle fitted here (corrected D-02).
  </action>
  <verify>
  <automated>F=WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs; grep -q "FitCircleContourXld" $F && [ "$(grep -c "FitCircleContourXld" $F)" -ge 2 ] && grep -q "TryAddStep" $F && grep -q "TryComputePickerCenter" $F && grep -q "void Reset" $F && grep -q "EdgesSubPix" $F && ! grep -q "TryFindCenter\|Matcher\|AlignShapeMatchService\|VisionAlgorithmService" $F && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - PickerCenterCalibrationService.cs contains `FitCircleContourXld` with `"atukey"` — appearing at least TWICE (per-step jig circle in TryAddStep + eccentric fit in TryComputePickerCenter)
    - TryAddStep extracts the jig circle via `GenCircle`→`ReduceDomain`→`EdgesSubPix`→`FitCircleContourXld` (replicating TryFindCircle); it does NOT call AlignShapeMatchService/Matcher/TryFindCenter
    - contains `void Reset`, `bool TryAddStep`, `bool TryComputePickerCenter`
    - TryComputePickerCenter writes `SystemSetting.Handle.PickerCenterRow` / `PickerCenterCol` and calls `Save()`
    - MIN_STEPS guard + radius guard present; all HALCON in try-catch with finally dispose of every HObject/HTuple
    - does NOT reference VisionAlgorithmService, AlignShapeMatchService, Matcher, or TryFindCenter
    - `//260624 hbk Phase 60` marker present
  </acceptance_criteria>
  <done>Stateful calibration service fits the Cal jig circle per step (fit_circle_contour_xld #1) and fits the eccentric trajectory of accumulated jig centers (fit_circle_contour_xld #2) to derive + store the picker center — fully independent of the 2-pattern shape match.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PickerCal into EthernetVisionHandler + register file in csproj</name>
  <files>WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs, WPF_Example/DatumMeasurement.csproj</files>
  <action>
1. EthernetVisionHandler.cs — add a `PickerCal` property mirroring `Matcher`:
   - After the `Matcher` property (L21), add:
     ```csharp
     //260624 hbk Phase 60 — D-01: 피커센터 캘 서비스 (handler 소유, stateful). Mode 무관 항상 생성.
     public PickerCenterCalibrationService PickerCal { get; private set; }
     ```
   - In `Initialize()`, right after `Matcher = new AlignShapeMatchService();` (L34), add:
     ```csharp
     //260624 hbk Phase 60 — D-01: PickerCal stateful → 모드/연결 결과 무관 항상 생성
     PickerCal = new PickerCenterCalibrationService();
     ```
   - In the `catch` block, after the existing `if (Matcher == null) { ... }` guard (L58-60), add:
     ```csharp
     //260624 hbk Phase 60 — 예외 경로에서도 PickerCal null 방지
     if (PickerCal == null) {
         PickerCal = new PickerCenterCalibrationService();
     }
     ```
   K&R brace (match the file). Do not modify any other line.

2. DatumMeasurement.csproj — register the new file in the EthernetVision Compile block.
   After `<Compile Include="Custom\EthernetVision\AlignShapeMatchService.cs" />` (L243) and
   before `EthernetVisionHandler.cs` (L244), add:
   ```xml
    <Compile Include="Custom\EthernetVision\PickerCenterCalibrationService.cs" />
   ```
   (any position inside the ItemGroup is valid; keep it grouped with the EthernetVision files).
  </action>
  <verify>
  <automated>grep -q "PickerCal" WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs && grep -q "PickerCenterCalibrationService.cs" WPF_Example/DatumMeasurement.csproj && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - EthernetVisionHandler.cs contains `public PickerCenterCalibrationService PickerCal`
    - Initialize() creates `PickerCal = new PickerCenterCalibrationService()` and catch-path null-guards it (mirrors Matcher)
    - csproj contains `<Compile Include="Custom\EthernetVision\PickerCenterCalibrationService.cs" />`
    - `//260624 hbk Phase 60` marker present in handler
  </acceptance_criteria>
  <done>EthernetVisionHandler owns PickerCal (mirrors Matcher); the new service compiles into the project.</done>
</task>

<task type="auto">
  <name>Task 3: Build Debug/x64 + prove anti-goal files unchanged vs phase-60 start</name>
  <files>WPF_Example/DatumMeasurement.csproj</files>
  <action>
1. Build the solution in Debug/x64 and confirm zero new errors:
   ```
   "C:/Program Files (x86)/Microsoft Visual Studio/2019/Community/MSBuild/Current/Bin/MSBuild.exe" "C:/Info/Project/DataMeasurement/WPF_Example/DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal /nologo
   ```
   If that MSBuild path is absent, locate via `vswhere` or use `dotnet msbuild` / `msbuild` on
   PATH. Build MUST succeed (0 errors). Warning baseline = Phase 59 (no NEW warnings).

2. Prove the anti-goal. CRITICAL baseline note: InspectionSequence.cs and other Grabber-area
   files were modified by a SEPARATE Phase 63 protocol track that is now COMMITTED (HEAD =
   48f2c49). Phase 60 started from that committed HEAD. Therefore the anti-goal proof must
   compare the WORKING TREE against the phase-60 START commit (HEAD at the time this phase
   began), NOT against any older commit — so Phase 63's already-committed changes are NOT
   falsely attributed to phase 60.

   a) Capture the phase-60 start commit ONCE (the commit before any 60-* code change). Since no
      phase-60 code is committed yet, that is current HEAD:
      ```
      git -C "C:/Info/Project/DataMeasurement" rev-parse HEAD
      ```
      (Record it in the SUMMARY as PHASE60_START.)

   b) Confirm the named anti-goal files have NO working-tree modification relative to HEAD:
      ```
      git -C "C:/Info/Project/DataMeasurement" status --porcelain -- \
        WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs \
        WPF_Example/Halcon/Algorithms/PatternMatchService.cs \
        WPF_Example/Utility/RecipeFileHelper.cs
      ```
      (Each printed line = an unwanted modification. Expect NO output.)

   c) Confirm NO Grabber file changed in the working tree (Sequence_* / Action_* /
      SystemHandler / HikCamera / VirtualCamera / DeviceHandler / InspectionSequence):
      ```
      git -C "C:/Info/Project/DataMeasurement" status --porcelain | grep -iE "Sequence_|Action_|SystemHandler|HikCamera|VirtualCamera|DeviceHandler|InspectionSequence|VisionAlgorithmService|PatternMatchService|RecipeFileHelper" || echo "ANTI-GOAL CLEAN"
      ```
      Expect `ANTI-GOAL CLEAN`. The ONLY files this phase touches:
      SystemSetting.cs (60-01), AlignShapeMatchService.cs (60-02),
      PickerCenterCalibrationService.cs + EthernetVisionHandler.cs + DatumMeasurement.csproj (60-03).

If RecipeFileHelper.cs lives at a different path, adjust the path but keep the intent: prove
the named anti-goal files have NO working-tree modification.
  </action>
  <verify>
  <automated>git -C "C:/Info/Project/DataMeasurement" status --porcelain | grep -iE "Sequence_|Action_|SystemHandler|HikCamera|VirtualCamera|DeviceHandler|InspectionSequence|VisionAlgorithmService|PatternMatchService|RecipeFileHelper" || echo "ANTI-GOAL CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 succeeds with 0 errors and no new warnings vs Phase 59 baseline
    - git status shows VisionAlgorithmService.cs / PatternMatchService.cs / RecipeFileHelper.cs UNMODIFIED in the working tree
    - no Grabber file (Sequence_*/Action_*/SystemHandler/HikCamera/VirtualCamera/DeviceHandler/InspectionSequence) modified by phase 60
    - verify command prints `ANTI-GOAL CLEAN`
    - PHASE60_START commit hash recorded in SUMMARY
  </acceptance_criteria>
  <done>The full solution builds in Debug/x64 and git proves every anti-goal file is untouched by phase 60 (compared against the phase-60 start HEAD, excluding Phase 63's pre-committed changes).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local step images + local INI → calibration service | inputs are operator-local (grabbed frames, SystemSetting INI); no remote/untrusted surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-03 | Denial of Service | TryAddStep / TryComputePickerCenter on bad frames or degenerate point sets | mitigate | `row.Length == 0` jig-fit guard + MIN_STEPS + radius guards reject degenerate fits; every method try-catch → false; HObject/HTuple disposed in finally (D-03/D-06) — Grabber isolation preserved. |
| T-60-04 | Tampering | stored PickerCenterRow/Col | accept | Local machine config, re-derivable by recalibration; same disposition as T-60-01. |
</threat_model>

<verification>
- msbuild Debug/x64 PASS (0 errors, no new warnings).
- grep: PickerCenterCalibrationService.cs has FitCircleContourXld ×2 (per-step jig + eccentric),
  EdgesSubPix, TryAddStep/TryComputePickerCenter/Reset; NO reference to AlignShapeMatchService/
  Matcher/TryFindCenter/VisionAlgorithmService; handler has PickerCal; csproj registers the file.
- git: anti-goal files (VisionAlgorithmService/PatternMatchService/RecipeFileHelper/Grabber/
  InspectionSequence) unchanged by phase 60 → `ANTI-GOAL CLEAN`.
</verification>

<success_criteria>
Per-step Cal jig circle fit (fit_circle_contour_xld #1) → 36-step accumulation → atukey
eccentric-circle LS (fit_circle_contour_xld #2) → picker center, stored to SystemSetting and
owned by EthernetVisionHandler.PickerCal; solution builds; anti-goal files untouched
(SC-1 + SC-3 integration, build-pass gate). No Phase 59 / 2-pattern dependency for the center.
</success_criteria>

<output>
After completion, create `.planning/phases/60-calibration-bottom-c-2026-06-23/60-03-SUMMARY.md`
</output>
