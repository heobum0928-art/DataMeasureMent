---
phase: 61-ui-tabcontrol-d-2026-06-23
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/UI/TrayVisionView.xaml
  - WPF_Example/Custom/UI/TrayVisionView.xaml.cs
autonomous: true
requirements: [AV-08]
must_haves:
  truths:
    - "Tray 비전 뷰는 Grab/Live/Stop 툴바 버튼을 가지며 각각 EthernetVisionHandler.Handle.Camera 의 Grab/Live/Stop 을 호출한다"
    - "Grab 클릭 시 취득 HImage 가 공유 뷰어(MainResultViewerControl)에 LoadImage 로 표시된다"
    - "티칭 버튼이 ROI 2개 드로잉(StartRectangleDrawing×2 → CommitActiveRectangle)을 받아 Matcher.TryTeach(img, roi1.., roi2.., Tray, out err) 를 호출한다"
    - "검사(Run) 버튼이 Matcher.Run(img, Tray) 를 호출하고 AlignResult 의 Found/Score/OffsetXmm/OffsetYmm 를 표시한다 (Tray 는 Theta 없음)"
    - "상태 라벨이 대기/LIVE/검사중/미연결 중 하나를 표시한다 (EthernetVisionHandler.IsInitialized 기반)"
  artifacts:
    - path: "WPF_Example/Custom/UI/TrayVisionView.xaml"
      provides: "Airspace-safe layout: 툴바 행 + 뷰어 호스트 Border(별도 행) + 티칭/결과 패널 + 상태 라벨"
      contains: "x:Name=\"ViewerHostBorder\""
    - path: "WPF_Example/Custom/UI/TrayVisionView.xaml.cs"
      provides: "Grab/Live/Stop/Teach/Run 핸들러 + AttachSharedViewer(MainResultViewerControl) 공개 계약 + SetViewMode"
      contains: "EthernetVisionHandler.Handle.Camera.Grab"
  key_links:
    - from: "WPF_Example/Custom/UI/TrayVisionView.xaml.cs"
      to: "EthernetVisionHandler.Handle.Matcher"
      via: "TryTeach + Run 직접 호출"
      pattern: "Matcher\\.(TryTeach|Run)"
    - from: "WPF_Example/Custom/UI/TrayVisionView.xaml.cs"
      to: "MainResultViewerControl"
      via: "AttachSharedViewer 로 주입된 공유 뷰어 참조에 LoadImage/StartRectangleDrawing/CommitActiveRectangle 호출"
      pattern: "_viewer\\.(LoadImage|StartRectangleDrawing|CommitActiveRectangle)"
---

<objective>
Tray Align 비전 뷰(`TrayVisionView` UserControl)를 신규 생성한다. Phase 61 TabControl 의 [Tray 비전] 탭에 들어갈 뷰로, 툴바(Grab/Live/Stop) + 2-ROI Shape 티칭 + 검사(Run) 결과(X/Y Offset) + 상태 표시를 제공한다. 모든 비전 로직은 phase 58/59 서비스(`EthernetVisionHandler.Handle.{Camera,Matcher}`)에 위임하는 **thin facade** 이며, HALCON 뷰어는 소유하지 않고 외부(MainWindow)가 주입하는 **공유 `MainResultViewerControl`** 를 사용한다 (D-03 공유 뷰어 계약).

Purpose: AV-08 의 Tray 측 UI — 밀린 Tray UAT(59 매칭) 를 실측할 버튼 제공.
Output: `WPF_Example/Custom/UI/TrayVisionView.xaml` + `.xaml.cs` (2 신규 파일). csproj 등록 + MainWindow 배선은 Plan 61-03 이 담당.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-ui-tabcontrol-d-2026-06-23/61-CONTEXT.md

<read_first>
프로덕션 코드 작성 전 반드시 읽어 계약/패턴을 확인한다 (한 번씩만 읽고 패턴 추출):
- `WPF_Example/UI/Dialog/CalibrationWindow.xaml` — **airspace-safe 레이아웃의 in-project 정본**. 좌측 컨트롤 패널 + 우측 뷰어(Row0=`*`) / 상태(Row1=`Auto`) 분리. 툴바/버튼은 뷰어와 다른 Grid 셀(네이티브 HWND 밖). 동일 구조를 차용하되 뷰어는 외부 주입.
- `WPF_Example/UI/Dialog/CalibrationWindow.xaml.cs` — Grab/Load → 뷰어 표시, ROI 드로잉(StartRectangleDrawing→CommitActiveRectangle) 사용 흐름. **단 CalibrationWindow 는 `HalconViewerControl`(다른 클래스)를 쓰므로 메서드 시그니처가 다름** — 본 plan 은 `MainResultViewerControl` 시그니처(아래 interfaces)를 따른다.
- `WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs` — `Handle.Camera`/`Handle.Matcher`/`Handle.IsInitialized` 싱글턴.
- `WPF_Example/Custom/EthernetVision/EthernetAlignCamera.cs` — `Grab()`→`HImage`(미연결 시 D:\align_test.bmp 폴백, null 가능), `Live()`→`bool`(미연결 false), `Stop()`→void.
- `WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs` + `AlignResult.cs` — TryTeach/Run 시그니처 + AlignResult 필드(아래 interfaces).
- `WPF_Example/Custom/EthernetVision/EEthernetVisionMode.cs` — `ReringProject.Setting.EEthernetVisionMode { None=0, Tray=1, Bottom=2 }`.
</read_first>

<interfaces>
<!-- 코드베이스에서 추출한 정확한 계약. 탐색 없이 이 시그니처를 그대로 사용할 것. -->

EEthernetVisionMode (namespace ReringProject.Setting):
```csharp
public enum EEthernetVisionMode { None = 0, Tray = 1, Bottom = 2 }
```

EthernetVisionHandler (namespace ReringProject — 싱글턴):
```csharp
EthernetVisionHandler.Handle.Camera      // EthernetAlignCamera, Mode==None 이면 null
EthernetVisionHandler.Handle.Matcher     // AlignShapeMatchService, 항상 non-null
EthernetVisionHandler.Handle.IsInitialized  // bool (연결 성공 시 true)
```

EthernetAlignCamera (namespace ReringProject.Device):
```csharp
public HImage Grab();   // 미연결/실패 시 D:\align_test.bmp 폴백, 폴백도 실패면 null. 반환 HImage 는 호출자 Dispose 책임.
public bool Live();     // 미연결이면 false
public void Stop();
public bool IsOpen { get; }
```

AlignShapeMatchService (namespace ReringProject):
```csharp
public bool TryTeach(
    HImage img,
    double roi1Row, double roi1Col, double roi1Phi, double roi1Len1, double roi1Len2,
    double roi2Row, double roi2Col, double roi2Phi, double roi2Len1, double roi2Len2,
    EEthernetVisionMode mode, out string error);
public AlignResult Run(HImage img, EEthernetVisionMode mode);  // 실패 시 Found=false (throw 없음)
public bool HasTemplate(EEthernetVisionMode mode);
```

AlignResult (namespace ReringProject — POCO):
```csharp
public bool Found; public double Score;
public double OffsetXmm; public double OffsetYmm;
public double ThetaDeg; public bool HasTheta;   // Tray: HasTheta=false, ThetaDeg=0
```

MainResultViewerControl (namespace ReringProject.UI — 공유 뷰어, 무수정 재사용):
```csharp
public void LoadImage(HImage image);                 // HImage 오버로드 — 내부에서 Clone
public void StartRectangleDrawing();                 // 인자 없음
public RoiDefinition CommitActiveRectangle();        // 인자 없음, 그린 Rect 반환(없으면 null)
public event EventHandler RectDrawingCompleted;      // 드래그 끝나면 발생
public void FitImage();
public HImage CurrentImage { get; }                  // 현재 표시 이미지(Clone본)
// 주의: SetRois(...) 는 이 클래스에 없음(HalconViewerControl 의 메서드). 사용 금지.
```

RoiDefinition (namespace ReringProject.Halcon.Models) — CommitActiveRectangle 반환형:
```csharp
public string Id; public bool IsTaught; public RoiShape Shape;   // enum RoiShape { Rect, Polygon, Circle }
public double Row1, Column1, Row2, Column2;   // Rect 끝점 (Row1<Row2, Col1<Col2 정규화됨)
```

Rect ROI → TryTeach 파라미터 변환 규약 (HALCON gen_rectangle2: Length1=hwidth=Column 반폭, Length2=hheight=Row 반폭):
```
Row  = (Row1 + Row2) / 2.0
Col  = (Column1 + Column2) / 2.0
Phi  = 0.0
Len1 = (Column2 - Column1) / 2.0   // 열 반폭
Len2 = (Row2 - Row1) / 2.0         // 행 반폭
```
</interfaces>

<analog>
StatusBarModel.SetText(string) 은 MainWindow.statusBar 전용이라 뷰 내부에서 접근 불가 → 뷰 자체 `TextBlock x:Name="lbl_status"` 로 상태 표시 (CalibrationWindow 의 `CalibrationStatusTextBlock` 패턴과 동일).
</analog>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TrayVisionView.xaml — airspace-safe 레이아웃 (툴바 + 뷰어 호스트 + 티칭/결과 패널 + 상태)</name>
  <files>WPF_Example/Custom/UI/TrayVisionView.xaml</files>
  <read_first>
    - `WPF_Example/UI/Dialog/CalibrationWindow.xaml` (좌측 패널 / 우측 뷰어+상태 2분할 + 버튼 스타일 패턴 — 차용)
    - `WPF_Example/MainWindow.xaml` (XAML 스타일 톤: 색/Margin)
  </read_first>
  <action>
`WPF_Example/Custom/UI/` 폴더에 `TrayVisionView.xaml` 신규 생성.

- Root: `<UserControl x:Class="ReringProject.Custom.UI.TrayVisionView" ...>` — namespace = `ReringProject.Custom.UI`. 표준 WPF xmlns 선언(presentation/x/d/mc) 포함. `mc:Ignorable="d"`, `d:DesignWidth/Height` 부여.
- **AIRSPACE 제약 필수**: HALCON 뷰어(`HWindowControlWPF`)는 네이티브 HWND 라 그 위에 WPF 오버레이가 가려진다. 따라서 **툴바/티칭/결과/상태는 뷰어 호스트와 반드시 별도 Grid 셀**에 둔다. CalibrationWindow 와 동일하게 2-컬럼 분할:
  - 루트 `<Grid>` 2 컬럼: `ColumnDefinition Width="380"`(좌측 컨트롤 패널) + `Width="*"`(우측 뷰어 영역).
  - **좌측 컬럼(컨트롤 패널)** — `<Grid>` 행 구성(모두 `Auto` + 마지막 `*`):
    - 헤더 `TextBlock` "Tray 비전".
    - 툴바 `WrapPanel` 또는 `StackPanel(Orientation=Horizontal)`: `<Button x:Name="btn_grab" Content="Grab" Click="GrabButton_Click"/>`, `btn_live`("Live", Click=`LiveButton_Click`), `btn_stop`("Stop", Click=`StopButton_Click`).
    - 티칭 `GroupBox` Header="티칭 (ROI 2개)": `btn_drawRoi1`("ROI 1 그리기", Click=`DrawRoi1Button_Click`), `btn_drawRoi2`("ROI 2 그리기", Click=`DrawRoi2Button_Click`), `btn_teach`("티칭 저장", Click=`TeachButton_Click`), 상태 `TextBlock x:Name="lbl_teachStatus"` (HasTemplate 표시).
    - 결과 `GroupBox` Header="검사 결과": `btn_run`("검사", Click=`RunButton_Click`), 결과 `TextBlock x:Name="lbl_result"` (Found/Score/OffsetX/OffsetY 텍스트).
    - 상태 `TextBlock x:Name="lbl_status"` Text="대기" (마지막 행 또는 헤더 아래).
  - **우측 컬럼(뷰어 영역)** — `<Border x:Name="ViewerHostBorder"/>` 단일 자식. **이 Border 가 외부에서 주입되는 공유 `MainResultViewerControl` 의 호스트** (D-03). XAML 에서 뷰어를 직접 인스턴스화하지 말 것 — MainWindow 가 `AttachSharedViewer` 로 자식 주입.
- 버튼 스타일은 CalibrationWindow 의 `CalibrationActionButtonStyle` 톤을 인라인 또는 간단 Setter 로 맞추되, 과도하게 복사하지 말 것(가독 우선). XAML 스타일은 기존 톤과 일관.
- C# 8+/신규 디자인 도입 금지 — 순수 WPF 레이아웃.

검증 가능 사실: 파일이 `x:Name="ViewerHostBorder"` 와 5개 핵심 버튼(btn_grab/btn_live/btn_stop/btn_teach/btn_run)을 포함하고, 뷰어를 직접 인스턴스화하지 않는다.
  </action>
  <verify>
    <automated>grep -q 'x:Name="ViewerHostBorder"' "WPF_Example/Custom/UI/TrayVisionView.xaml" && grep -q 'GrabButton_Click' "WPF_Example/Custom/UI/TrayVisionView.xaml" && grep -q 'TeachButton_Click' "WPF_Example/Custom/UI/TrayVisionView.xaml" && grep -q 'RunButton_Click' "WPF_Example/Custom/UI/TrayVisionView.xaml" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 파일 `WPF_Example/Custom/UI/TrayVisionView.xaml` 가 존재한다.
    - `x:Class="ReringProject.Custom.UI.TrayVisionView"` 선언.
    - `x:Name="ViewerHostBorder"` 인 `<Border>` 가 우측 컬럼에 존재한다 (뷰어 호스트, 자식 미선언 — 외부 주입).
    - 툴바 버튼 `btn_grab`/`btn_live`/`btn_stop` (Click=GrabButton_Click/LiveButton_Click/StopButton_Click) 존재.
    - 티칭 버튼 `btn_drawRoi1`/`btn_drawRoi2`/`btn_teach` + `lbl_teachStatus` 존재.
    - 결과 버튼 `btn_run` + `lbl_result` + 상태 `lbl_status` 존재.
    - 툴바/패널/상태가 뷰어 호스트 Border 와 **다른 Grid 셀**에 위치 (airspace — 뷰어 위 WPF 오버레이 없음).
    - XAML 에서 `MainResultViewerControl`/`HWindowControlWPF` 를 직접 인스턴스화하지 않는다.
  </acceptance_criteria>
  <done>airspace-safe Tray 뷰 레이아웃 XAML 완성 — 뷰어 호스트 Border + 분리된 툴바/티칭/결과/상태.</done>
</task>

<task type="auto">
  <name>Task 2: TrayVisionView.xaml.cs — 서비스 배선 facade (Grab/Live/Stop/Teach/Run/상태 + 공유 뷰어 계약)</name>
  <files>WPF_Example/Custom/UI/TrayVisionView.xaml.cs</files>
  <read_first>
    - `WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs`, `AlignShapeMatchService.cs`, `AlignResult.cs`, `EthernetAlignCamera.cs` (시그니처 확정 — context interfaces 와 대조)
    - `WPF_Example/UI/Dialog/CalibrationWindow.xaml.cs` (try-catch + 상태 텍스트 갱신 흐름 패턴)
  </read_first>
  <action>
`TrayVisionView.xaml.cs` 신규 생성. `namespace ReringProject.Custom.UI`, `public partial class TrayVisionView : UserControl`. using: `System`, `System.Windows`, `System.Windows.Controls`, `HalconDotNet`, `ReringProject.Setting`, `ReringProject.UI`, `ReringProject.Halcon.Models`. 생성자에서 `InitializeComponent()` 호출.

**공유 뷰어 계약 (Plan 61-03 이 의존 — 정확히 이 시그니처로 구현):**
- private 필드 `private MainResultViewerControl _viewer;` (외부 주입, 소유하지 않음).
- `public void AttachSharedViewer(MainResultViewerControl viewer)`: `_viewer = viewer;` 그리고 `ViewerHostBorder.Child = viewer;` 로 호스트에 부착. 호출 전 viewer 가 다른 부모에 붙어 있을 수 있으나 그 detach 는 **MainWindow(Plan 61-03)** 책임 — 이 메서드는 `ViewerHostBorder.Child = viewer` 만 수행. viewer==null 가드.
- 모드 상수: 이 뷰는 Tray 전용 → `private const EEthernetVisionMode VIEW_MODE = EEthernetVisionMode.Tray;` (C# 7.2 에서 enum const 가능).

**핸들러 (모두 try-catch — 실패해도 throw 금지, UI/Grabber 무영향; 매직넘버 const):**
- `GrabButton_Click`: `EthernetVisionHandler.Handle.Camera` 가 null 이면 상태=미연결 후 return. `HImage img = EthernetVisionHandler.Handle.Camera.Grab();` 가 null 이면 상태=미연결/취득 실패. non-null 이면 `_viewer?.LoadImage(img); img.Dispose();` (LoadImage 가 내부 Clone 하므로 즉시 Dispose 안전 — interfaces 주석 근거). 상태 갱신.
- `LiveButton_Click`: Camera null 가드. `bool ok = EthernetVisionHandler.Handle.Camera.Live();` ok 면 상태="LIVE", 아니면 상태="미연결". (실시간 프레임 뷰어 push 루프는 Camera.Live 의 StartStream 위임 — 본 plan 은 시작 트리거만; 프레임 표시 루프는 Discretion/후속 UAT.)
- `StopButton_Click`: Camera null 가드. `Camera.Stop();` 상태="대기".
- `DrawRoi1Button_Click` / `DrawRoi2Button_Click`: `_viewer` null 가드. `_viewer.StartRectangleDrawing();` 후 상태="ROI{n} 드래그 → 그린 뒤 다음/티칭". 그린 결과는 RectDrawingCompleted 또는 티칭 시점에 `CommitActiveRectangle()` 로 수거. **2-ROI 슬롯 관리(Discretion):** private 필드 `RoiDefinition _roi1, _roi2;` 와 진행 슬롯 인덱스를 두고, Draw1/Draw2 가 어느 슬롯을 채울지 표시. `RectDrawingCompleted` 이벤트(생성자에서 구독) 또는 각 Draw 후 다음 Draw 클릭 시 직전 `CommitActiveRectangle()` 결과를 해당 슬롯에 저장하는 단순 흐름 중 택1 — 구현은 결정적이고 2개 ROI 가 확실히 수거되면 됨. 권장: Draw1 클릭→StartRectangleDrawing, Draw2 클릭 시 `_roi1 = _viewer.CommitActiveRectangle()` 저장 후 다시 StartRectangleDrawing(슬롯2), Teach 클릭 시 `_roi2 = _viewer.CommitActiveRectangle()` 저장.
- `TeachButton_Click`: `_viewer`/`CurrentImage` null 가드. 두 ROI(`_roi1`,`_roi2`) 가 모두 유효(IsTaught + 폭/높이>1px) 한지 검증, 미흡 시 상태 경고 후 return. 각 ROI → (Row,Col,Phi,Len1,Len2) 변환(interfaces 의 규약 사용; 변환은 ≤30줄 private 헬퍼 `RectToTeachParams(RoiDefinition, out r,out c,out phi,out l1,out l2)` 로 분리). `bool ok = EthernetVisionHandler.Handle.Matcher.TryTeach(_viewer.CurrentImage, r1.., c1.., ..., r2.., ..., VIEW_MODE, out err);` ok 면 `lbl_teachStatus.Text` = "티칭 OK (HasTemplate=" + Matcher.HasTemplate(VIEW_MODE) + ")", 실패면 err 표시.
- `RunButton_Click`: `_viewer`/`CurrentImage` null 가드. 상태="검사중". `AlignResult res = EthernetVisionHandler.Handle.Matcher.Run(_viewer.CurrentImage, VIEW_MODE);` `res.Found` 면 `lbl_result.Text` = X/Y Offset + Score 포맷(Tray 는 Theta 미표시), 아니면 "검출 실패". 상태="대기".
- `RefreshStatus()` private 헬퍼(생성자/Loaded 에서 1회 호출): `EthernetVisionHandler.Handle.IsInitialized` false 면 `lbl_status.Text="미연결"`, true 면 "대기". HasTemplate 로 `lbl_teachStatus` 초기화.

각 핸들러 함수 30줄 이하 유지(변환/포맷은 헬퍼 분리). 주석 `//260624 hbk Phase 61`. C# 7.2 — switch expression/nullable ref/record 금지. `EthernetVisionHandler`/`AlignResult` 는 `ReringProject` 네임스페이스라 `using ReringProject;` 불필요(같은 루트). `MainResultViewerControl`/`RoiDefinition` 은 각각 `ReringProject.UI`/`ReringProject.Halcon.Models`.
  </action>
  <verify>
    <automated>grep -q "EthernetVisionHandler.Handle.Camera.Grab" "WPF_Example/Custom/UI/TrayVisionView.xaml.cs" && grep -q "Matcher.TryTeach" "WPF_Example/Custom/UI/TrayVisionView.xaml.cs" && grep -q "Matcher.Run" "WPF_Example/Custom/UI/TrayVisionView.xaml.cs" && grep -q "public void AttachSharedViewer" "WPF_Example/Custom/UI/TrayVisionView.xaml.cs" && grep -q "EEthernetVisionMode.Tray" "WPF_Example/Custom/UI/TrayVisionView.xaml.cs" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 파일 `WPF_Example/Custom/UI/TrayVisionView.xaml.cs` 가 존재, `namespace ReringProject.Custom.UI`, `partial class TrayVisionView : UserControl`.
    - `public void AttachSharedViewer(MainResultViewerControl viewer)` 가 존재하고 `ViewerHostBorder.Child = viewer` 를 수행.
    - GrabButton_Click 이 `EthernetVisionHandler.Handle.Camera.Grab()` 호출 → 결과 HImage 를 `_viewer.LoadImage(...)` 로 표시.
    - LiveButton_Click → `Camera.Live()`, StopButton_Click → `Camera.Stop()` 호출.
    - TeachButton_Click 이 `Matcher.TryTeach(...)` 를 **2개 ROI 파라미터**(roi1 Row/Col/Phi/Len1/Len2 + roi2 동일)와 `EEthernetVisionMode.Tray` 로 호출.
    - RunButton_Click 이 `Matcher.Run(img, EEthernetVisionMode.Tray)` 호출 후 AlignResult.Found/Score/OffsetXmm/OffsetYmm 를 lbl_result 에 표시.
    - 모든 서비스 호출이 try-catch 로 감싸짐 (Camera/Matcher null 또는 예외 시 상태 라벨만 갱신, throw 없음).
    - 상태 라벨(lbl_status)이 IsInitialized 기반 미연결/대기 + Live 시 LIVE + Run 시 검사중 을 표시.
    - C# 8+ 구문 없음. 주석 `//260624 hbk Phase 61` 포함.
  </acceptance_criteria>
  <done>TrayVisionView 코드비하인드가 Camera/Matcher 싱글턴에 위임하는 thin facade 로 완성 — 공유 뷰어 주입 계약 + Grab/Live/Stop/2-ROI Teach/Run/상태, 전 호출 try-catch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (없음 — 신규 untrusted-input 경계 없음) | 로컬 UI 컨트롤 ↔ 로컬 인-프로세스 싱글턴 서비스(Camera/Matcher)만. 외부 입력/네트워크/파일 파싱 신규 도입 없음. 이미지 폴백 경로(D:\align_test.bmp)는 phase 58 가 이미 try-catch 격리. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Denial of Service | TrayVisionView 버튼 핸들러 (Camera/Matcher 예외) | mitigate | 전 서비스 호출 try-catch + null 가드 → UI/Grabber 스레드 무중단 (D-05). |
| T-61-02 | Tampering | 공유 HImage 수명 | accept | LoadImage 가 내부 Clone, Grab HImage 는 호출 직후 Dispose — 신규 공유 가변 상태 없음, 로컬 단일 스레드 UI. |
</threat_model>

<verification>
- TrayVisionView.xaml + .xaml.cs 2 파일 신규 생성, 위 grep 5종 PASS.
- 빌드 검증은 Plan 61-03(csproj 등록 후) 에서 일괄 — 본 plan 단독은 csproj 미등록이라 컴파일되지 않음(의도된 wave 분리).
- 안티골: 본 plan 은 신규 파일 2개만 생성, 기존 파일 0 수정.
</verification>

<success_criteria>
- `TrayVisionView` 가 Grab/Live/Stop(Camera) + 2-ROI Teach(Matcher.TryTeach) + Run(Matcher.Run→AlignResult X/Y) + 상태 라벨을 제공.
- 공유 뷰어 계약 `AttachSharedViewer(MainResultViewerControl)` 노출 (Plan 61-03 소비).
- airspace-safe(툴바/패널이 뷰어 호스트와 별도 셀), thin facade(로직 0, 전부 서비스 위임), 전 호출 try-catch.
</success_criteria>

<output>
After completion, create `.planning/phases/61-ui-tabcontrol-d-2026-06-23/61-01-SUMMARY.md`
</output>
