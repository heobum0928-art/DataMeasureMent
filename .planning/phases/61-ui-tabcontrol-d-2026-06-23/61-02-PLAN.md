---
phase: 61-ui-tabcontrol-d-2026-06-23
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/UI/BottomVisionView.xaml
  - WPF_Example/Custom/UI/BottomVisionView.xaml.cs
autonomous: true
requirements: [AV-08]
must_haves:
  truths:
    - "Bottom 비전 뷰는 Grab/Live/Stop 툴바 + 2-ROI 티칭(Matcher.TryTeach, Bottom) + 검사(Matcher.Run, Bottom)를 제공한다 (Tray 와 동일 facade)"
    - "검사 결과가 OffsetXmm/OffsetYmm + ThetaDeg + Score 를 표시한다 (Bottom 은 Theta 유효, HasTheta=true)"
    - "캘리브레이션 패널이 PickerCal.Reset → (스텝마다 Grab + TryAddStep) → TryComputePickerCenter 흐름으로 피커 센터를 산출·표시한다"
    - "캘 스텝 ROI 는 원(Circle) 검색 ROI 로 지정되어 TryAddStep(img, searchRow, searchCol, searchRadius, out err) 에 전달된다"
    - "상태 라벨이 대기/LIVE/검사중/미연결 + 누적 스텝 수(PickerCal.StepCount)를 표시한다"
  artifacts:
    - path: "WPF_Example/Custom/UI/BottomVisionView.xaml"
      provides: "Airspace-safe 레이아웃: 툴바 + 뷰어 호스트 Border + 티칭/결과 패널 + 캘 패널(Reset/스텝추가/계산) + 상태"
      contains: "x:Name=\"ViewerHostBorder\""
    - path: "WPF_Example/Custom/UI/BottomVisionView.xaml.cs"
      provides: "Tray facade + Theta 표시 + PickerCal 캘 패널 핸들러 + AttachSharedViewer 계약"
      contains: "PickerCal.TryAddStep"
  key_links:
    - from: "WPF_Example/Custom/UI/BottomVisionView.xaml.cs"
      to: "EthernetVisionHandler.Handle.PickerCal"
      via: "Reset/TryAddStep/TryComputePickerCenter 직접 호출"
      pattern: "PickerCal\\.(Reset|TryAddStep|TryComputePickerCenter)"
    - from: "WPF_Example/Custom/UI/BottomVisionView.xaml.cs"
      to: "EthernetVisionHandler.Handle.Matcher"
      via: "TryTeach + Run (Bottom 모드)"
      pattern: "Matcher\\.(TryTeach|Run)"
---

<objective>
Bottom Align 비전 뷰(`BottomVisionView` UserControl)를 신규 생성한다. Phase 61 TabControl 의 [Bottom 비전] 탭에 들어갈 뷰. 구성은 Tray 뷰(Plan 61-01)와 동일한 facade(툴바 Grab/Live/Stop + 2-ROI 티칭 + Run) 에 **두 가지 Bottom 전용 추가**: (1) 검사 결과에 **Theta(deg)** 표시(AV-04: Bottom = X/Y/Theta), (2) **피커센터 캘리브레이션 패널**(AV-05: PickerCal.Reset → 스텝마다 Grab+TryAddStep → TryComputePickerCenter → 피커센터 표시). 비전/캘 로직은 전부 phase 59/60 서비스에 위임하는 thin facade.

Purpose: AV-08 의 Bottom 측 UI + Bottom 캘 패널 — 밀린 Bottom UAT(59 매칭 + 60 캘) 실측 버튼 제공.
Output: `WPF_Example/Custom/UI/BottomVisionView.xaml` + `.xaml.cs` (2 신규 파일). csproj 등록 + MainWindow 배선은 Plan 61-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-ui-tabcontrol-d-2026-06-23/61-CONTEXT.md

<read_first>
- `WPF_Example/UI/Dialog/CalibrationWindow.xaml` + `.xaml.cs` — airspace-safe 레이아웃 정본 + ROI 드로잉/상태 흐름 (단 `HalconViewerControl` 사용 — 시그니처는 본 plan interfaces 우선).
- `WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs` — `Handle.Camera`/`Matcher`/`PickerCal`/`IsInitialized` 싱글턴.
- `WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs` — `Reset()`/`TryAddStep(...)`/`TryComputePickerCenter(...)`/`StepCount` (아래 interfaces).
- `WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs` + `AlignResult.cs` — TryTeach/Run + AlignResult.ThetaDeg/HasTheta.
- `WPF_Example/Custom/EthernetVision/EthernetAlignCamera.cs` — Grab/Live/Stop.
- `WPF_Example/Custom/EthernetVision/EEthernetVisionMode.cs` — None/Tray/Bottom.
- **`.planning/phases/61-ui-tabcontrol-d-2026-06-23/61-01-PLAN.md`** — Tray 뷰 계약(동일 패턴 차용: AttachSharedViewer, RectToTeachParams, RefreshStatus, 핸들러 구조). 본 plan 은 Tray facade 부분을 동일하게 구현 + Theta/캘 추가.
</read_first>

<interfaces>
<!-- Tray plan(61-01) 의 interfaces 블록(EEthernetVisionMode/EthernetAlignCamera/AlignShapeMatchService/AlignResult/MainResultViewerControl/RoiDefinition/Rect→Teach 변환)을 그대로 적용. Bottom 추가분만 아래 명시. -->

AlignResult — Bottom 추가 소비 필드:
```csharp
public double ThetaDeg;   // Bottom: 유효 (curAngleDeg - refAngleDeg)
public bool HasTheta;     // Bottom: true
```

PickerCenterCalibrationService (namespace ReringProject — EthernetVisionHandler.Handle.PickerCal):
```csharp
public void Reset();                       // 누적 초기화
public int StepCount { get; }              // 누적 스텝 수
// 기본 에지 파라미터 오버로드 (UI 는 검색 ROI(원)만 전달):
public bool TryAddStep(HImage img,
    double searchRow, double searchCol, double searchRadius, out string error);
public bool TryComputePickerCenter(out double row, out double col, out double radius, out string error);
// MIN_STEPS=6 (내부) — 미만이면 TryComputePickerCenter 가 false + error.
```

MainResultViewerControl — Circle 드로잉(캘 검색 ROI 용):
```csharp
public void StartCircleDrawing();
public void CommitActiveCircle();                       // void (좌표는 이벤트로)
public event EventHandler<CircleDrawCompletedArgs> CircleDrawingCompleted;  // args.CenterRow/CenterCol/Radius
```
CircleDrawCompletedArgs (namespace ReringProject.UI): `public double CenterRow, CenterCol, Radius;`

VIEW_MODE = EEthernetVisionMode.Bottom (이 뷰 전용 const).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BottomVisionView.xaml — Tray 레이아웃 + Theta 결과 + 캘리브레이션 패널 (airspace-safe)</name>
  <files>WPF_Example/Custom/UI/BottomVisionView.xaml</files>
  <read_first>
    - `.planning/phases/61-ui-tabcontrol-d-2026-06-23/61-01-PLAN.md` (Tray.xaml 레이아웃 — 동일 차용)
    - `WPF_Example/UI/Dialog/CalibrationWindow.xaml` (GroupBox/버튼/상태 패턴)
  </read_first>
  <action>
`WPF_Example/Custom/UI/BottomVisionView.xaml` 신규 생성. Plan 61-01 의 TrayVisionView.xaml 과 **동일한 airspace-safe 2-컬럼 구조**(좌측 컨트롤 패널 / 우측 `<Border x:Name="ViewerHostBorder"/>` 뷰어 호스트). `x:Class="ReringProject.Custom.UI.BottomVisionView"`.

좌측 컨트롤 패널 행 구성 (Tray 와 동일 + Bottom 추가):
- 헤더 `TextBlock` "Bottom 비전".
- 툴바 `StackPanel`: `btn_grab`(GrabButton_Click)/`btn_live`(LiveButton_Click)/`btn_stop`(StopButton_Click).
- 티칭 `GroupBox` "티칭 (ROI 2개)": `btn_drawRoi1`/`btn_drawRoi2`/`btn_teach` + `lbl_teachStatus`.
- 결과 `GroupBox` "검사 결과": `btn_run`(RunButton_Click) + `lbl_result` (X/Y **+ Theta** 표시 텍스트).
- **캘리브레이션 `GroupBox` Header="피커센터 캘 (36-스텝)"** (Bottom 전용):
  - `btn_calReset`("초기화", Click=`CalResetButton_Click`).
  - `btn_calDrawRoi`("검색 ROI(원) 지정", Click=`CalDrawRoiButton_Click`).
  - `btn_calAddStep`("스텝 추가 (Grab+검출)", Click=`CalAddStepButton_Click`).
  - `btn_calCompute`("피커센터 계산", Click=`CalComputeButton_Click`).
  - `lbl_calStatus` (누적 스텝 수 / 검출 상태) + `lbl_pickerCenter` (산출된 피커센터 row/col/radius).
- 상태 `TextBlock x:Name="lbl_status"` Text="대기".

우측: `<Border x:Name="ViewerHostBorder"/>` 단일(외부 주입). 뷰어/HWindowControlWPF 직접 인스턴스화 금지. 버튼 톤은 CalibrationWindow 차용, 과복사 금지. C# 8+/신규 디자인 금지.
  </action>
  <verify>
    <automated>grep -q 'x:Name="ViewerHostBorder"' "WPF_Example/Custom/UI/BottomVisionView.xaml" && grep -q 'CalResetButton_Click' "WPF_Example/Custom/UI/BottomVisionView.xaml" && grep -q 'CalAddStepButton_Click' "WPF_Example/Custom/UI/BottomVisionView.xaml" && grep -q 'CalComputeButton_Click' "WPF_Example/Custom/UI/BottomVisionView.xaml" && grep -q 'RunButton_Click' "WPF_Example/Custom/UI/BottomVisionView.xaml" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 파일 `WPF_Example/Custom/UI/BottomVisionView.xaml` 존재, `x:Class="ReringProject.Custom.UI.BottomVisionView"`.
    - `x:Name="ViewerHostBorder"` Border (뷰어 호스트, 자식 미선언) 가 우측 컬럼에 존재.
    - 툴바 btn_grab/btn_live/btn_stop + 티칭 btn_drawRoi1/btn_drawRoi2/btn_teach + 결과 btn_run + lbl_result 존재.
    - 캘 패널: btn_calReset/btn_calDrawRoi/btn_calAddStep/btn_calCompute + lbl_calStatus + lbl_pickerCenter 존재.
    - 상태 lbl_status 존재. 모든 버튼/패널/상태가 뷰어 호스트와 다른 Grid 셀(airspace).
    - XAML 에서 뷰어/HWindowControlWPF 직접 인스턴스화 없음.
  </acceptance_criteria>
  <done>Bottom 뷰 레이아웃 XAML 완성 — Tray facade 레이아웃 + Theta 결과 라벨 + 피커센터 캘 패널, airspace-safe.</done>
</task>

<task type="auto">
  <name>Task 2: BottomVisionView.xaml.cs — Tray facade + Theta 표시 + PickerCal 캘 배선</name>
  <files>WPF_Example/Custom/UI/BottomVisionView.xaml.cs</files>
  <read_first>
    - `WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs` (Reset/TryAddStep/TryComputePickerCenter/StepCount — interfaces 대조)
    - `WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs` + `AlignResult.cs` (Run → ThetaDeg/HasTheta)
    - `.planning/phases/61-ui-tabcontrol-d-2026-06-23/61-01-PLAN.md` (Tray.xaml.cs facade 구조 — Grab/Live/Stop/Teach/Run/RefreshStatus/RectToTeachParams/AttachSharedViewer 동일 구현)
  </read_first>
  <action>
`BottomVisionView.xaml.cs` 신규 생성. `namespace ReringProject.Custom.UI`, `public partial class BottomVisionView : UserControl`. using: `System`, `System.Windows`, `System.Windows.Controls`, `HalconDotNet`, `ReringProject.Setting`, `ReringProject.UI`, `ReringProject.Halcon.Models`. 생성자 `InitializeComponent()`.

**Tray facade 부분 (Plan 61-01 Task 2 와 동일하게 구현)** — `_viewer` 필드 + `AttachSharedViewer(MainResultViewerControl)` (`ViewerHostBorder.Child = viewer`) + Grab/Live/Stop/Draw1/Draw2/Teach/Run/RefreshStatus/RectToTeachParams. 단 `private const EEthernetVisionMode VIEW_MODE = EEthernetVisionMode.Bottom;`. 전부 try-catch.

**Bottom 추가 1 — Run 결과 Theta:**
- `RunButton_Click`: `AlignResult res = EthernetVisionHandler.Handle.Matcher.Run(_viewer.CurrentImage, VIEW_MODE);` Found 면 `lbl_result.Text` = X/Y Offset + **Theta(deg)** + Score 포맷 (res.HasTheta 가 true 일 때 Theta 표시 — Bottom 은 항상 true). 실패 시 "검출 실패".

**Bottom 추가 2 — 피커센터 캘 패널 (PickerCal 위임, 전부 try-catch):**
- 캘 검색 ROI 슬롯: `private double _calSearchRow, _calSearchCol, _calSearchRadius; private bool _calRoiSet;`.
- 생성자에서 `_viewer` 가 주입된 후 CircleDrawingCompleted 를 구독해야 하므로, 이벤트 구독은 `AttachSharedViewer` 안에서 수행: `viewer.CircleDrawingCompleted -= OnCalCircleDrawn; viewer.CircleDrawingCompleted += OnCalCircleDrawn;` (중복 구독 방지 위해 -= 후 +=). 핸들러 `private void OnCalCircleDrawn(object sender, CircleDrawCompletedArgs e)`: `_calSearchRow=e.CenterRow; _calSearchCol=e.CenterCol; _calSearchRadius=e.Radius; _calRoiSet=true;` 상태="검색 ROI 설정됨".
- `CalResetButton_Click`: `EthernetVisionHandler.Handle.PickerCal.Reset();` `lbl_calStatus.Text="누적 0"`, `lbl_pickerCenter.Text=""`. (PickerCal null 가드.)
- `CalDrawRoiButton_Click`: `_viewer` 가드. `_viewer.StartCircleDrawing();` 상태="검색 원 ROI 를 드래그하세요". (좌표는 OnCalCircleDrawn 으로 수거.)
- `CalAddStepButton_Click`: `_calRoiSet` 미설정 시 경고 후 return. `EthernetVisionHandler.Handle.Camera` null 이면 미연결 후 return. `HImage img = Camera.Grab();` null 가드. `bool ok = EthernetVisionHandler.Handle.PickerCal.TryAddStep(img, _calSearchRow, _calSearchCol, _calSearchRadius, out err);` img.Dispose(). 추가로 `_viewer?.LoadImage(img)` 는 Dispose 전에(원하면) — 단 LoadImage 가 Clone 하므로 순서: `_viewer.LoadImage(img); ... TryAddStep(img, ...); img.Dispose();`. ok 면 `lbl_calStatus.Text = "누적 " + PickerCal.StepCount` , 실패면 err 표시. (외부 회전=PLC; UI 는 스텝 트리거만 — 한 번 클릭 = 한 스텝.)
- `CalComputeButton_Click`: `bool ok = PickerCal.TryComputePickerCenter(out double r, out double c, out double rad, out err);` ok 면 `lbl_pickerCenter.Text = string.Format("피커센터 ({0:F2},{1:F2}) r={2:F2}", r, c, rad)`, 실패(스텝 부족 등) 면 err 표시. (저장은 서비스 내부에서 SystemSetting.Save — UI 무관.)
- `RefreshStatus()`: IsInitialized 기반 미연결/대기 + `lbl_calStatus.Text = "누적 " + (PickerCal?.StepCount ?? 0)`.

함수 30줄 이하(포맷/변환 헬퍼 분리). 주석 `//260624 hbk Phase 61`. C# 7.2 (switch expression/nullable ref/record 금지 — `?.`/`??` 는 null-conditional/coalescing 으로 C# 7.2 허용). 캘 결과/매칭/회전 로직 0 — 전부 PickerCal/Matcher 위임.
  </action>
  <verify>
    <automated>grep -q "PickerCal.Reset" "WPF_Example/Custom/UI/BottomVisionView.xaml.cs" && grep -q "PickerCal.TryAddStep" "WPF_Example/Custom/UI/BottomVisionView.xaml.cs" && grep -q "PickerCal.TryComputePickerCenter" "WPF_Example/Custom/UI/BottomVisionView.xaml.cs" && grep -q "Matcher.Run" "WPF_Example/Custom/UI/BottomVisionView.xaml.cs" && grep -q "public void AttachSharedViewer" "WPF_Example/Custom/UI/BottomVisionView.xaml.cs" && grep -q "EEthernetVisionMode.Bottom" "WPF_Example/Custom/UI/BottomVisionView.xaml.cs" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 파일 `WPF_Example/Custom/UI/BottomVisionView.xaml.cs` 존재, `namespace ReringProject.Custom.UI`, `partial class BottomVisionView : UserControl`.
    - `public void AttachSharedViewer(MainResultViewerControl viewer)` 가 `ViewerHostBorder.Child = viewer` 수행 + CircleDrawingCompleted 구독(-= 후 +=).
    - Grab/Live/Stop 이 `EthernetVisionHandler.Handle.Camera` Grab/Live/Stop 호출 (Tray facade 동일).
    - TeachButton_Click 이 `Matcher.TryTeach(...)` 2-ROI + `EEthernetVisionMode.Bottom` 로 호출.
    - RunButton_Click 이 `Matcher.Run(img, EEthernetVisionMode.Bottom)` 후 OffsetXmm/OffsetYmm + **ThetaDeg** + Score 를 lbl_result 에 표시 (HasTheta 분기).
    - CalResetButton_Click → `PickerCal.Reset()`, CalAddStepButton_Click → `Camera.Grab()` + `PickerCal.TryAddStep(img, searchRow, searchCol, searchRadius, out err)`, CalComputeButton_Click → `PickerCal.TryComputePickerCenter(out r,out c,out rad,out err)` → lbl_pickerCenter 표시.
    - 캘 검색 ROI 가 Circle 드로잉(StartCircleDrawing → CircleDrawingCompleted)으로 수거되어 TryAddStep 에 전달.
    - 전 서비스 호출 try-catch + null 가드. lbl_calStatus 가 PickerCal.StepCount(누적 스텝) 표시.
    - C# 8+ 구문 없음(switch expression/record/nullable ref 금지). 주석 `//260624 hbk Phase 61`.
  </acceptance_criteria>
  <done>BottomVisionView 코드비하인드 완성 — Tray facade + Theta 표시 + PickerCal 36-스텝 캘(Reset/스텝 Grab+TryAddStep/Compute) 위임, 공유 뷰어 주입, 전 호출 try-catch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (없음 — 신규 untrusted-input 경계 없음) | 로컬 UI ↔ 로컬 인-프로세스 싱글턴(Camera/Matcher/PickerCal). 외부 입력/네트워크/파일 파싱 신규 도입 없음. PickerCal 의 SystemSetting.Save 는 phase 60 가 try-catch 격리. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-03 | Denial of Service | Bottom 뷰 캘/매칭 핸들러 (PickerCal/Matcher 예외) | mitigate | 전 호출 try-catch + null 가드 → UI/Grabber 무중단 (D-05). |
| T-61-04 | Tampering | 캘 누적 상태(PickerCal stateful) | accept | 누적은 PickerCal 싱글턴 내부 — UI 는 Reset/AddStep/Compute 트리거만, 신규 가변 공유 상태 없음. CircleDrawingCompleted 중복 구독은 -= 후 += 로 방지. |
</threat_model>

<verification>
- BottomVisionView.xaml + .xaml.cs 2 파일 신규 생성, 위 grep 6종 PASS.
- 빌드 검증은 Plan 61-03(csproj 등록 후) 일괄.
- 안티골: 본 plan 신규 파일 2개만, 기존 파일 0 수정.
</verification>

<success_criteria>
- `BottomVisionView` = Tray facade(Grab/Live/Stop + 2-ROI Teach + Run) + Theta 표시 + 피커센터 캘(Reset/TryAddStep/TryComputePickerCenter).
- 공유 뷰어 계약 `AttachSharedViewer` + CircleDrawingCompleted 캘 ROI 수거.
- thin facade(로직 0), airspace-safe, 전 호출 try-catch.
</success_criteria>

<output>
After completion, create `.planning/phases/61-ui-tabcontrol-d-2026-06-23/61-02-SUMMARY.md`
</output>
