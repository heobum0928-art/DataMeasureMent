---
phase: 61-ui-tabcontrol-d-2026-06-23
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - WPF_Example/MainWindow.xaml
  - WPF_Example/MainWindow.xaml.cs
  - WPF_Example/Custom/UI/MainWindow.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [AV-07, AV-08]
must_haves:
  truths:
    - "MainWindow 의 Contents 영역(Grid.Row=2/Col=0, Row0)에 3-탭 TabControl([검사]/[Tray 비전]/[Bottom 비전])이 존재한다"
    - "[검사] 탭이 기존 MainView 를 x:Name=\"mainView\" 보존 상태로 호스팅한다 (MainView.xaml/.cs 0 수정)"
    - "GridSplitter + LogView 가 Row1~2 에 그대로 유지된다 (회귀 0)"
    - "EthernetVisionMode 가 None 이면 [Tray]/[Bottom] 탭 Collapsed, Tray 면 [검사]+[Tray], Bottom 이면 [검사]+[Bottom] 만 표시된다"
    - "단일 공유 MainResultViewerControl 인스턴스가 활성 align 뷰(Tray 또는 Bottom)의 ViewerHostBorder 에 부착된다 (AV-08 공용 뷰어)"
    - "msbuild Debug/x64 빌드가 PASS 한다"
  artifacts:
    - path: "WPF_Example/MainWindow.xaml"
      provides: "Contents 영역 TabControl 래퍼 (line 70 MainView → TabControl[3 TabItem])"
      contains: "<TabControl"
    - path: "WPF_Example/Custom/UI/MainWindow.cs"
      provides: "RegisterCustomUI() 내 탭 Visibility 게이트 + 공유 뷰어 attach (RefreshEthernetVisionTabs)"
      contains: "EthernetVisionMode"
    - path: "WPF_Example/DatumMeasurement.csproj"
      provides: "TrayVisionView/BottomVisionView .xaml(Page) + .xaml.cs(Compile DependentUpon) 등록"
      contains: "Custom\\UI\\TrayVisionView.xaml"
  key_links:
    - from: "WPF_Example/Custom/UI/MainWindow.cs"
      to: "SystemSetting.Handle.EthernetVisionMode"
      via: "모드 읽어 TabItem.Visibility 설정"
      pattern: "EthernetVisionMode"
    - from: "WPF_Example/Custom/UI/MainWindow.cs"
      to: "TrayVisionView / BottomVisionView"
      via: "AttachSharedViewer(_alignViewer) 로 공유 뷰어 주입"
      pattern: "AttachSharedViewer"
    - from: "WPF_Example/MainWindow.xaml"
      to: "ReringProject.Custom.UI.TrayVisionView / BottomVisionView"
      via: "TabItem 내 뷰 인스턴스화 (xmlns 매핑)"
      pattern: "TrayVisionView|BottomVisionView"
---

<objective>
MainWindow 에 3-탭 TabControl 을 추가해 기존 MainView 를 [검사] 탭으로 재부모화하고, [Tray 비전]/[Bottom 비전] 탭에 Plan 61-01/61-02 의 뷰를 호스팅한다. `EthernetVisionMode` 에 따라 Tray/Bottom 탭 Visibility 를 게이트하고(D-04), **단일 공유 `MainResultViewerControl`** 를 활성 align 뷰에 부착한다(D-03/AV-08). 신규 뷰 파일을 csproj 에 등록하고 msbuild Debug/x64 빌드 PASS 를 확인하며, **anti-goal(MainView/뷰어/Grabber/58~60 서비스 무수정)을 git 으로 증명**한다.

Purpose: AV-07(TabControl 통합 + 모드 게이트 + MainView 이동) + AV-08(공유 뷰어 호스트 실재화) 의 통합 지점.
Output: MainWindow.xaml(TabControl 래퍼) + Custom/UI/MainWindow.cs(탭 게이트/뷰어 attach) + MainWindow.xaml.cs(설정창 후 1줄 refresh) + csproj(뷰 등록) 수정. 빌드 PASS + 안티골 git 증명.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-ui-tabcontrol-d-2026-06-23/61-CONTEXT.md

<read_first>
- `WPF_Example/MainWindow.xaml` — **line 60~73 Contents Grid 가 래핑 지점**. line 70 `<ui:MainView x:Name="mainView" Grid.Column="0" Grid.Row="0"/>` 가 TabControl 로 교체 대상. line 71(GridSplitter)/72(LogView, Grid.Row=1/2)는 그대로 유지. `xmlns:ui="clr-namespace:ReringProject.UI"` 이미 선언됨.
- `WPF_Example/MainWindow.xaml.cs` — `Window_Loaded`(line 366) 가 `RegisterCustomUI()`(line 371) 호출. `PopupView(EPageType.Setting)`(line 338~342) 는 `mModalWindow.ShowDialog()` **블로킹** 후 복귀 → 그 직후가 모드 변경 반영 지점. `mainView` 참조(line 59,142,153,165,193,304~305 등 다수) 는 재부모화 후에도 x:Name 동일이라 유효.
- `WPF_Example/Custom/UI/MainWindow.cs` — `public partial class MainWindow` 의 **빈 `RegisterCustomUI()`** — 탭 게이트/뷰어 attach 코드를 여기 채운다 (MainWindow 클래스 partial = MainWindow 통합 코드 범주, 안티골 허용 확장).
- `.planning/phases/61-ui-tabcontrol-d-2026-06-23/61-01-PLAN.md` + `61-02-PLAN.md` — 뷰 공개 계약 `public void AttachSharedViewer(MainResultViewerControl viewer)` + `ViewerHostBorder`.
- `WPF_Example/DatumMeasurement.csproj` — `<Page Include="...xaml"><SubType>Designer</SubType><Generator>MSBuild:Compile</Generator></Page>` + `<Compile Include="...xaml.cs"><DependentUpon>...xaml</DependentUpon></Compile>` 등록 포맷 (line 424~459 참조). 기존 EthernetVision Compile 그룹은 line 241~245.
</read_first>

<interfaces>
신규 뷰 공개 계약 (Plan 61-01/02 가 보장):
```csharp
namespace ReringProject.Custom.UI {
  public partial class TrayVisionView : UserControl {
      public void AttachSharedViewer(MainResultViewerControl viewer);  // ViewerHostBorder.Child = viewer
  }
  public partial class BottomVisionView : UserControl {
      public void AttachSharedViewer(MainResultViewerControl viewer);  // + CircleDrawingCompleted 구독
  }
}
```

SystemSetting / 모드:
```csharp
SystemSetting.Handle.EthernetVisionMode   // EEthernetVisionMode { None=0, Tray=1, Bottom=2 } (namespace ReringProject.Setting)
```

MainResultViewerControl: `namespace ReringProject.UI`, 기본 생성자 `new MainResultViewerControl()`. WPF 컨트롤은 한 번에 하나의 부모만 가짐 → 재부모화 시 기존 부모(Border)에서 detach 후 attach.

MainWindow.xaml 기존 Contents Grid (line 60~73) — **변경 후 목표 구조**:
```
<Grid Grid.Row="2" Grid.Column="0" Background="Beige">  (3 RowDefinition 8*/5/1* 유지)
  <TabControl x:Name="tabMain" Grid.Column="0" Grid.Row="0">   ← line 70 MainView 자리
     [검사] TabItem      → <ui:MainView x:Name="mainView"/>   (x:Name 보존!)
     [Tray 비전] TabItem → <customui:TrayVisionView x:Name="trayVisionView"/>
     [Bottom 비전] TabItem → <customui:BottomVisionView x:Name="bottomVisionView"/>
  </TabControl>
  <GridSplitter Grid.Row="1" .../>   (line 71 그대로)
  <ui:LogView x:Name="logView" Grid.Row="2"/>  (line 72 그대로)
</Grid>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MainWindow.xaml — Contents 영역에 3-탭 TabControl 래퍼 (MainView 재부모화, x:Name 보존)</name>
  <files>WPF_Example/MainWindow.xaml</files>
  <read_first>
    - `WPF_Example/MainWindow.xaml` (line 1~14 헤더 xmlns + line 60~73 Contents Grid)
    - `D:\Backup\파이널비전\WPF_Example_260604\UI\ContentItem\MainView.xaml` (참조 TabControl + ItemContainerStyle Visibility + TabItem 헤더 패턴 — 구조만 차용)
  </read_first>
  <action>
`WPF_Example/MainWindow.xaml` 의 **Contents Grid(Grid.Row="2" Grid.Column="0", line 60~73)만** 수정. 그 외 영역(TitleBar/MenuBar/우측 InspectionListView/StatusBar/RowDefinitions/ColumnDefinitions) 무변경.

1. 루트 `<Window>` 헤더에 Custom.UI 네임스페이스 xmlns 추가:
   `xmlns:customui="clr-namespace:ReringProject.Custom.UI"` (기존 `xmlns:ui="clr-namespace:ReringProject.UI"` 옆).
2. line 70 `<ui:MainView x:Name="mainView" Grid.Column="0" Grid.Row="0"/>` 를 **TabControl 로 교체** (Contents Grid 의 RowDefinitions 8*/5/1* 는 그대로 유지):
```xml
<TabControl x:Name="tabMain" Grid.Column="0" Grid.Row="0">
    <TabItem x:Name="tabInspection" Header="검사">
        <ui:MainView x:Name="mainView"/>
    </TabItem>
    <TabItem x:Name="tabTray" Header="Tray 비전">
        <customui:TrayVisionView x:Name="trayVisionView"/>
    </TabItem>
    <TabItem x:Name="tabBottom" Header="Bottom 비전">
        <customui:BottomVisionView x:Name="bottomVisionView"/>
    </TabItem>
</TabControl>
```
   - **`x:Name="mainView"` 반드시 보존** — MainWindow.xaml.cs 의 다수 mainView 참조가 무손상이어야 함(회귀 0 핵심). MainView 는 Grid.Row/Column attached property 불필요(TabItem 자식이므로 제거).
3. line 71 GridSplitter(Grid.Row=1) + line 72 LogView(Grid.Row=2) 는 **그대로 유지** (TabControl 아래 동일 위치).

MainView.xaml / MainView.xaml.cs 는 절대 열어 수정하지 말 것 — XAML 트리에서 부모만 Grid→TabItem 으로 바뀐다. TabControl 에 SelectionChanged 는 본 task 에서 불필요(탭 가시성은 코드비하인드 게이트). C# 8+/신규 디자인 없음.
  </action>
  <verify>
    <automated>grep -q "<TabControl x:Name=\"tabMain\"" "WPF_Example/MainWindow.xaml" && grep -q "x:Name=\"mainView\"" "WPF_Example/MainWindow.xaml" && grep -q "TrayVisionView" "WPF_Example/MainWindow.xaml" && grep -q "BottomVisionView" "WPF_Example/MainWindow.xaml" && grep -q 'x:Name="logView"' "WPF_Example/MainWindow.xaml" && grep -q "clr-namespace:ReringProject.Custom.UI" "WPF_Example/MainWindow.xaml" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `WPF_Example/MainWindow.xaml` 에 `<TabControl x:Name="tabMain"` 가 Contents Grid(Grid.Row="2" Grid.Column="0")의 Grid.Row="0" 위치에 존재.
    - TabControl 이 3 TabItem(tabInspection "검사" / tabTray "Tray 비전" / tabBottom "Bottom 비전") 보유.
    - `<ui:MainView x:Name="mainView"/>` 가 [검사] TabItem 안에 **x:Name 보존**된 채 존재.
    - `<customui:TrayVisionView x:Name="trayVisionView"/>` + `<customui:BottomVisionView x:Name="bottomVisionView"/>` 가 각 탭에 존재.
    - `xmlns:customui="clr-namespace:ReringProject.Custom.UI"` 선언됨.
    - GridSplitter(Grid.Row=1) + `<ui:LogView x:Name="logView" ... Grid.Row="2">` 유지.
    - TitleBar/MenuBar/우측 InspectionListView/StatusBar 영역 무변경.
  </acceptance_criteria>
  <done>MainWindow.xaml Contents 영역이 3-탭 TabControl 로 래핑되고 MainView 가 x:Name 보존하며 [검사] 탭으로 이동, LogView/Splitter 유지.</done>
</task>

<task type="auto">
  <name>Task 2: 탭 Visibility 게이트 + 공유 뷰어 attach (Custom/UI/MainWindow.cs) + 설정창 후 refresh (MainWindow.xaml.cs)</name>
  <files>WPF_Example/Custom/UI/MainWindow.cs, WPF_Example/MainWindow.xaml.cs</files>
  <read_first>
    - `WPF_Example/Custom/UI/MainWindow.cs` (빈 RegisterCustomUI — 채울 대상)
    - `WPF_Example/MainWindow.xaml.cs` (Window_Loaded line 366 → RegisterCustomUI 호출 line 371; PopupView Setting case line 338~342 ShowDialog)
    - `.planning/phases/61-ui-tabcontrol-d-2026-06-23/61-01-PLAN.md` / `61-02-PLAN.md` (AttachSharedViewer 계약)
  </read_first>
  <action>
**(A) `WPF_Example/Custom/UI/MainWindow.cs`** — partial `MainWindow` 의 `RegisterCustomUI()` 를 채우고 헬퍼 추가:
- using 추가: `System.Windows`, `System.Windows.Controls`, `ReringProject.Setting`, `ReringProject.UI`. (이미 `using ReringProject.UI;` 있음.)
- private 필드: `private MainResultViewerControl _alignViewer;` (단일 공유 뷰어 — D-03).
- `RegisterCustomUI()` 본문:
  - `_alignViewer = new MainResultViewerControl();` (한 번만).
  - `RefreshEthernetVisionTabs();` 호출 (Loaded 시점 초기 게이트).
- 신규 헬퍼 `public void RefreshEthernetVisionTabs()` (≤30줄, try-catch):
  - `EEthernetVisionMode mode = SystemSetting.Handle.EthernetVisionMode;` (SystemSetting 접근은 `mSystemHandler.Setting` 도 가능하나 싱글턴 직접 `SystemSetting.Handle` 사용 — D-05).
  - 게이트 (TabItem.Visibility):
    - `bool bTray = (mode == EEthernetVisionMode.Tray);`
    - `bool bBottom = (mode == EEthernetVisionMode.Bottom);`
    - `tabTray.Visibility = bTray ? Visibility.Visible : Visibility.Collapsed;`
    - `tabBottom.Visibility = bBottom ? Visibility.Visible : Visibility.Collapsed;`
    - ([검사] 탭 tabInspection 은 항상 Visible — 미변경.)
  - **공유 뷰어 attach**: 활성 align 뷰에 단일 `_alignViewer` 부착. WPF 단일 부모 제약 → 먼저 현재 부모에서 detach:
    - 헬퍼 `DetachAlignViewer()`: `if (_alignViewer != null && _alignViewer.Parent is Border) { ((Border)_alignViewer.Parent).Child = null; }` (또는 ContentControl 등 — Plan 61-01/02 가 ViewerHostBorder=Border 이므로 Border 캐스팅).
    - `if (bTray) { DetachAlignViewer(); trayVisionView.AttachSharedViewer(_alignViewer); }`
    - `else if (bBottom) { DetachAlignViewer(); bottomVisionView.AttachSharedViewer(_alignViewer); }`
    - `else { DetachAlignViewer(); }` (None — 어디에도 부착 안 함.)
  - 전체 try-catch(Exception ex) → Logging.PrintLog Error (UI 무중단). 주석 `//260624 hbk Phase 61`.
- **C# 7.2 주의**: `is Border` 패턴 매칭 변수 선언(`is Border b`)은 C# 7.0+ 허용. switch expression 금지. `?:` 삼항 허용(CONVENTIONS).

**(B) `WPF_Example/MainWindow.xaml.cs`** — 설정창 닫힌 후 모드 변경 반영 (D-04):
- `PopupView` 의 `case EPageType.Setting:` 블록에서 `mModalWindow.ShowDialog();` **직후**에 한 줄 추가: `RefreshEthernetVisionTabs();` (ShowDialog 블로킹 복귀 = 설정 저장 완료 시점 → 모드 변경 즉시 탭 재게이트). 주석 `//260624 hbk Phase 61: 설정창 종료 후 EthernetVisionMode 탭 재게이트`.
- 그 외 MainWindow.xaml.cs 변경 없음 (mainView 참조는 재부모화로 무손상 — 손대지 않음).

**불변식**: MainView/MainResultViewerControl 클래스 자체는 수정 0. `_alignViewer` 는 새 인스턴스로 align 전용 — MainView 내부 뷰어와 무관.
  </action>
  <verify>
    <automated>grep -q "RefreshEthernetVisionTabs" "WPF_Example/Custom/UI/MainWindow.cs" && grep -q "SystemSetting.Handle.EthernetVisionMode" "WPF_Example/Custom/UI/MainWindow.cs" && grep -q "AttachSharedViewer" "WPF_Example/Custom/UI/MainWindow.cs" && grep -q "tabTray.Visibility" "WPF_Example/Custom/UI/MainWindow.cs" && grep -q "new MainResultViewerControl" "WPF_Example/Custom/UI/MainWindow.cs" && grep -q "RefreshEthernetVisionTabs" "WPF_Example/MainWindow.xaml.cs" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `Custom/UI/MainWindow.cs` 의 `RegisterCustomUI()` 가 `_alignViewer = new MainResultViewerControl()` 생성 + `RefreshEthernetVisionTabs()` 호출.
    - `public void RefreshEthernetVisionTabs()` 가 `SystemSetting.Handle.EthernetVisionMode` 읽어 tabTray/tabBottom 의 `Visibility` 를 Collapsed/Visible 로 게이트 (None→둘 다 Collapsed / Tray→tabTray Visible / Bottom→tabBottom Visible).
    - 활성 모드에서 `_alignViewer` 가 detach 후 `trayVisionView.AttachSharedViewer(_alignViewer)` 또는 `bottomVisionView.AttachSharedViewer(_alignViewer)` 로 부착 (단일 인스턴스 공유).
    - `MainWindow.xaml.cs` 의 Setting case ShowDialog 직후 `RefreshEthernetVisionTabs()` 호출 (모드 변경 반영).
    - 전 로직 try-catch (실패 시 Logging 만, UI 무중단).
    - C# 8+ 구문 없음. `//260624 hbk Phase 61` 주석.
  </acceptance_criteria>
  <done>Loaded(RegisterCustomUI)+설정창 종료 시 EthernetVisionMode 로 Tray/Bottom 탭 Visibility 게이트 + 단일 공유 뷰어를 활성 뷰에 attach.</done>
</task>

<task type="auto">
  <name>Task 3: csproj 등록 + msbuild Debug/x64 빌드 PASS + 안티골 git 증명</name>
  <files>WPF_Example/DatumMeasurement.csproj</files>
  <read_first>
    - `WPF_Example/DatumMeasurement.csproj` (Page ItemGroup line 455~459 + Compile DependentUpon line 424~429 + EthernetVision Compile 그룹 line 241~245 — 등록 포맷 확인)
  </read_first>
  <action>
**(A) csproj 등록** — 4 신규 항목 추가 (기존 포맷 일치):
- `<Page>` ItemGroup(기존 .xaml Page 들이 모인 그룹)에 추가:
```xml
<Page Include="Custom\UI\TrayVisionView.xaml">
  <SubType>Designer</SubType>
  <Generator>MSBuild:Compile</Generator>
</Page>
<Page Include="Custom\UI\BottomVisionView.xaml">
  <SubType>Designer</SubType>
  <Generator>MSBuild:Compile</Generator>
</Page>
```
- `<Compile>` ItemGroup(코드비하인드 DependentUpon 그룹)에 추가:
```xml
<Compile Include="Custom\UI\TrayVisionView.xaml.cs">
  <DependentUpon>TrayVisionView.xaml</DependentUpon>
</Compile>
<Compile Include="Custom\UI\BottomVisionView.xaml.cs">
  <DependentUpon>BottomVisionView.xaml</DependentUpon>
</Compile>
```
경로 구분자는 백슬래시(`\`) — 기존 csproj 관례. `Custom/UI/MainWindow.cs` 는 이미 등록되어 있으면 재추가 금지(중복 빌드 오류) — 등록 여부 grep 확인 후 없을 때만 추가.

**(B) msbuild Debug/x64 빌드** — 전 plan(01/02/03) 산출물이 함께 컴파일되는 통합 빌드:
```
msbuild "WPF_Example/DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /m /nologo /v:minimal
```
빌드 PASS(에러 0) 확인. XAML 컴파일 오류(x:Name 매핑, customui 네임스페이스, AttachSharedViewer 시그니처 불일치 등) 발생 시 수정 후 재빌드. **빌드가 통과해야 본 task 완료.**

**(C) 안티골 git 증명** — 다음 파일들이 본 phase 에서 무수정임을 git 으로 확인 (HEAD 베이스라인 = phase 61 작업 시작 커밋):
- 무수정이어야 할 파일: `WPF_Example/UI/ContentItem/MainView.xaml`, `WPF_Example/UI/ContentItem/MainView.xaml.cs`, `WPF_Example/UI/ContentItem/MainResultViewerControl.xaml`, `WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs`, `WPF_Example/Custom/EthernetVision/*.cs`(5 서비스), Grabber(`WPF_Example/Custom/Sequence/**`, `WPF_Example/Device/**`, `WPF_Example/SystemHandler.cs`).
- 확인 명령: `git status --porcelain` 결과의 수정/추가 파일이 **오직** {MainWindow.xaml, MainWindow.xaml.cs, Custom/UI/MainWindow.cs, DatumMeasurement.csproj, 신규 TrayVisionView.xaml/.cs, 신규 BottomVisionView.xaml/.cs, .planning/**} 에 한정되는지 검증.
- 명령: `git diff --name-only HEAD -- WPF_Example/UI/ContentItem/MainView.xaml WPF_Example/UI/ContentItem/MainView.xaml.cs WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs WPF_Example/Custom/EthernetVision` 의 출력이 **비어 있어야** PASS (이 파일들 0 변경). 비어있지 않으면 안티골 위반 → 해당 변경 되돌림.

3 검증(csproj 등록 / 빌드 PASS / 안티골 0변경)이 모두 통과해야 phase 통합 완료.
  </action>
  <verify>
    <automated>grep -q "Custom..UI..TrayVisionView.xaml" "WPF_Example/DatumMeasurement.csproj" && grep -q "Custom..UI..BottomVisionView.xaml" "WPF_Example/DatumMeasurement.csproj" && test -z "$(git diff --name-only HEAD -- WPF_Example/UI/ContentItem/MainView.xaml WPF_Example/UI/ContentItem/MainView.xaml.cs WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs WPF_Example/Custom/EthernetVision)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `DatumMeasurement.csproj` 에 TrayVisionView.xaml/BottomVisionView.xaml 의 `<Page>` 등록 + TrayVisionView.xaml.cs/BottomVisionView.xaml.cs 의 `<Compile ... DependentUpon>` 등록 (백슬래시 경로, 기존 포맷 일치, 중복 없음).
    - `msbuild /p:Configuration=Debug /p:Platform=x64` 빌드가 **에러 0 으로 PASS** (전 3 plan 산출물 통합 컴파일).
    - `git diff --name-only HEAD -- WPF_Example/UI/ContentItem/MainView.xaml WPF_Example/UI/ContentItem/MainView.xaml.cs WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs WPF_Example/Custom/EthernetVision` 출력이 **빈 문자열** (안티골: MainView/뷰어/58~60 서비스 0 변경).
    - Grabber(Sequence/Device/SystemHandler) 도 0 변경 (git status 로 확인 — 변경 목록이 허용 화이트리스트 내).
  </acceptance_criteria>
  <done>신규 뷰 csproj 등록 + Debug/x64 빌드 PASS + MainView/뷰어/Grabber/58~60 서비스 무수정 git 증명 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (없음 — 신규 untrusted-input 경계 없음) | MainWindow 통합 코드 ↔ 로컬 UI(TabControl/뷰)·로컬 싱글턴(SystemSetting). 외부 입력/네트워크 신규 도입 없음. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-05 | Denial of Service | RefreshEthernetVisionTabs (탭 게이트/뷰어 attach) | mitigate | try-catch → Logging only, UI 무중단. 뷰어 재부모화는 detach 후 attach 로 WPF 단일 부모 위반 방지. |
| T-61-06 | Elevation of Privilege | MainView/Grabber 회귀 | mitigate | 재부모화만(MainView 0수정) + 안티골 git diff 0 검증 → 기존 검사 경로 무영향. |
</threat_model>

<verification>
- Task1 grep 6종 + Task2 grep 6종 + Task3 grep/git 검증 PASS.
- **msbuild Debug/x64 빌드 PASS** (통합 게이트).
- **안티골 git 증명**: MainView.xaml/.cs + MainResultViewerControl + Custom/EthernetVision/* + Grabber = 0 변경. 변경 화이트리스트 = {MainWindow.xaml, MainWindow.xaml.cs, Custom/UI/MainWindow.cs, csproj, 신규 4 뷰 파일, .planning/**}.
- 4 ROADMAP SC 매핑: (1) 3-탭 TabControl + MainView 회귀0 = Task1 + 안티골 git, (2) 모드 visibility = Task2 RefreshEthernetVisionTabs, (3) Tray·Bottom 툴바+teach+result+cal+공유뷰어 = Plan 61-01/02 + Task2 attach, (4) 상태 = Plan 61-01/02 상태 라벨.
</verification>

<success_criteria>
- MainWindow 에 [검사]/[Tray 비전]/[Bottom 비전] 3-탭 TabControl, MainView 가 [검사] 탭에 x:Name 보존 이동(회귀 0).
- EthernetVisionMode 게이트(None/Tray/Bottom)로 Tray/Bottom 탭 Visibility 제어 (Loaded + 설정창 종료 후).
- 단일 공유 MainResultViewerControl 가 활성 align 뷰에 부착 (AV-08 공용).
- msbuild Debug/x64 PASS + 안티골(MainView/뷰어/Grabber/58~60 서비스 무수정) git 증명.
</success_criteria>

<output>
After completion, create `.planning/phases/61-ui-tabcontrol-d-2026-06-23/61-03-SUMMARY.md`
</output>
