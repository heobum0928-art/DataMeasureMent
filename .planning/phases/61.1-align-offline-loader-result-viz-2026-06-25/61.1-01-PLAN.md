---
phase: 61.1-align-offline-loader-result-viz-2026-06-25
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/EthernetVision/AlignResult.cs
  - WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs
autonomous: true
requirements: [AV-08]
must_haves:
  truths:
    - "AlignShapeMatchService.Run 이 검출 성공 시 두 패턴의 검출 좌표(Row1/Col1, Row2/Col2)와 중심을 AlignResult 로 반환한다"
    - "Run 이 검출 패턴별 보정 ROI 박스 좌표(row,col,phi,len1,len2)를 AlignResult 리스트로 반환한다"
    - "Run 이 검출된 모델 에지 contour 점(Row/Col 리스트)을 AlignResult 로 반환한다"
    - "기존 Run(img, mode) 시그니처와 AlignResult 의 Found/Score/OffsetXmm/OffsetYmm/ThetaDeg/HasTheta 필드는 그대로 유지된다 (하위호환)"
  artifacts:
    - path: "WPF_Example/Custom/EthernetVision/AlignResult.cs"
      provides: "검출 좌표/보정 ROI/에지 contour 시각화 필드 (기존 POCO 필드에 추가)"
      contains: "DetectedRow1"
    - path: "WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs"
      provides: "Run 내 검출 pose/ROI/contour 산출 + AlignResult write"
      contains: "get_shape_model_contours"
  key_links:
    - from: "AlignShapeMatchService.Run"
      to: "AlignResult (검출 필드)"
      via: "result.DetectedRow1 = f1Row 등 대입"
      pattern: "result\\.Detected"
---

<objective>
AlignShapeMatchService.Run 을 확장하여 매칭 결과 시각화에 필요한 데이터(검출 위치 + 보정 ROI 박스 + 검출 에지 contour)를 AlignResult 로 반환한다. D-02: 시각화 범위 = 검출 + 보정 ROI + 에지 전부. D-04: anti-goal 일부 해제 — AlignShapeMatchService + AlignResult 만 수정, PatternMatchService 는 무수정 재사용.

Purpose: Phase 61 UAT 갭 G-61.1-02(매칭 결과가 좌측 텍스트로만 표시, 이미지 위 검출 위치/ROI/에지 미시각화) 해소의 데이터 계층. 시각화 배선(Plan 03)이 이 반환값을 소비한다.
Output: AlignResult 에 시각화 필드 추가 + Run 이 검출 성공 시 해당 필드를 채워 반환 (기존 Run/AlignResult 계약 하위호환).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61.1-align-offline-loader-result-viz-2026-06-25/61.1-CONTEXT.md

<interfaces>
<!-- AlignShapeMatchService.Run 내부에서 이미 산출되는 검출 값 (확장 시 그대로 노출). 별도 재계산 불필요. -->
<!-- 현 Run 시그니처/계약 (하위호환 필수 유지): -->
public AlignResult Run(HImage img, EEthernetVisionMode mode)
  - Step 1: _matcher.TryFindPose(img, ENGINE, shmPath1, 0.0, 0.0, FULL_SEARCH_LEN, FULL_SEARCH_LEN, 0.0, MIN_SCORE, 1.0,
              out f1Row, out f1Col, out f1AngleDeg, out f1Score, out findErr1)   // TL 모델 검출
  - Step 2: 동일 패턴으로 shmPath2 → f2Row, f2Col, f2AngleDeg, f2Score   // BR 모델 검출
  - midFRow=(f1Row+f2Row)/2, midFCol=(f1Col+f2Col)/2   // 검출 중심 (이미 계산됨)
  - refPose.Ref1Row/Ref1Col/Ref2Row/Ref2Col   // LoadRefPose 로 로드된 레퍼런스 중심

<!-- AlignResult 현 필드 (POCO, 절대 제거/이름변경 금지): -->
public bool Found; public double Score; public double OffsetXmm; public double OffsetYmm;
public double ThetaDeg; public bool HasTheta;

<!-- 경로 헬퍼 (Run 이 이미 사용): BuildShmPath(mode, 1) → TL .shm 경로, BuildShmPath(mode, 2) → BR .shm 경로. -->
<!-- PatternMatchService 상수: PatternMatchService.EXTENSION_SHAPE_MODEL = ".shm". -->

<!-- HALCON 에지 contour 추출 표준 흐름 (이 서비스 내부에 직접 작성 — PatternMatchService 무수정): -->
HOperatorSet.ReadShapeModel(shmPath, out modelId);
HOperatorSet.GetShapeModelContours(out modelContours, modelId, 1);   // 모델 좌표계 contour (level 1)
HOperatorSet.VectorAngleToRigid(0, 0, 0, detRow, detCol, detAngleRad, out homMat);  // 모델원점→검출pose 강체변환
HOperatorSet.AffineTransContourXld(modelContours, out movedContours, homMat);       // 검출 위치로 이동된 contour
HOperatorSet.GetContourXld(movedContours, out contRows, out contCols);              // contour 점 추출 (HTuple)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AlignResult 에 시각화 필드 추가 (하위호환)</name>
  <read_first>
    - WPF_Example/Custom/EthernetVision/AlignResult.cs (전체 — 기존 POCO 6필드 보존 확인)
  </read_first>
  <files>WPF_Example/Custom/EthernetVision/AlignResult.cs</files>
  <action>
    AlignResult 클래스에 시각화 전용 필드를 ADDITIVE 로 추가한다. 기존 6필드(Found/Score/OffsetXmm/OffsetYmm/ThetaDeg/HasTheta)는 한 글자도 수정 금지 (Plan 62/TCP 소비 — 회귀 0). 주석 `//260625 hbk Phase 61.1` 를 추가 블록 위에 1회 표기.

    추가 필드 (모두 public auto-property, C# 7.2 — record/switch expression 금지):
    1) 검출 위치(십자용):
       - `public bool HasDetection { get; set; }`  // 검출 좌표 유효 여부 (Found 와 별개 — 시각화 게이트)
       - `public double DetectedRow1 { get; set; }` / `public double DetectedCol1 { get; set; }`  // TL 패턴 검출 중심
       - `public double DetectedRow2 { get; set; }` / `public double DetectedCol2 { get; set; }`  // BR 패턴 검출 중심
       - `public double DetectedCenterRow { get; set; }` / `public double DetectedCenterCol { get; set; }`  // 두 패턴 midpoint
    2) 보정 ROI 박스(검출 위치로 이동한 패턴 박스):
       - `public System.Collections.Generic.List<double[]> DetectedRoiBoxes { get; set; }`
         // 각 항목 = {row, col, phi, len1, len2} — MainResultViewerControl.RenderResultRoiBoxes 규약(길이 5 사각). 기본 new List<double[]>() 로 초기화.
    3) 검출 에지 contour:
       - `public System.Collections.Generic.List<double> EdgeContourRows { get; set; }`  // 검출 모델 contour 점 Row (기본 new List<double>())
       - `public System.Collections.Generic.List<double> EdgeContourCols { get; set; }`  // 동일 점 Col (Rows 와 인덱스 1:1, 기본 new List<double>())

    각 List 필드는 선언부에서 `= new System.Collections.Generic.List<double[]>();` 처럼 즉시 초기화하여 null 소비 위험 제거 (소비측 null 가드 부담 완화). XML doc summary 1줄씩(검출 좌표/보정 ROI/에지 의미) 붙인다.
  </action>
  <verify>
    <automated>grep -c "DetectedRow1\|DetectedRoiBoxes\|EdgeContourRows\|HasDetection" WPF_Example/Custom/EthernetVision/AlignResult.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "public bool Found" WPF_Example/Custom/EthernetVision/AlignResult.cs` 가 여전히 1건 (기존 필드 보존)
    - `grep -n "public double OffsetXmm\|public double OffsetYmm\|public double ThetaDeg\|public bool HasTheta\|public double Score" WPF_Example/Custom/EthernetVision/AlignResult.cs` 가 5건 모두 존재
    - `grep -n "HasDetection\|DetectedRow1\|DetectedCol1\|DetectedRow2\|DetectedCol2\|DetectedCenterRow\|DetectedCenterCol" AlignResult.cs` 가 7개 식별자 모두 존재
    - `grep -n "DetectedRoiBoxes\|EdgeContourRows\|EdgeContourCols" AlignResult.cs` 가 3건 존재 + `= new System.Collections.Generic.List` 초기화 3건
    - `grep -n "record \|switch.*=>\|?[^.?:]*:" AlignResult.cs` 에 삼항/record/switch-expression 0건 (단순 POCO)
    - 주석 `grep "//260625 hbk Phase 61.1" AlignResult.cs` 1건 이상
  </acceptance_criteria>
  <done>AlignResult 에 검출 좌표 7필드 + 보정 ROI 리스트 1필드 + 에지 contour 2리스트가 추가되고 기존 6필드는 무수정. List 필드는 즉시 초기화.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AlignShapeMatchService.Run 검출 pose/ROI/에지 산출 + AlignResult write</name>
  <read_first>
    - WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs (전체 — 특히 Run() L320~435, BuildShmPath L62~87, ComputeAngleLx L117~131)
    - WPF_Example/Halcon/Algorithms/PatternMatchService.cs (L304~ TryFindPose 시그니처, L22 EXTENSION_SHAPE_MODEL — 무수정 확인)
    - WPF_Example/Custom/EthernetVision/AlignResult.cs (Task 1 결과 — 신규 필드명)
  </read_first>
  <files>WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs</files>
  <action>
    Run() 내에서 검출 성공(Step 1/2 의 f1Row/Col/Angle, f2Row/Col/Angle 확보) 이후, 최종 `result` 를 반환하기 직전(L404 `AlignResult result = new AlignResult();` 이후 ~ L427 return 사이)에서 시각화 필드를 채운다. 주석 `//260625 hbk Phase 61.1` 를 추가 블록마다 표기. 모든 HALCON 호출은 try-catch 로 감싸 실패해도 result.Found/Offset 등 기존 동작은 throw 없이 유지 (D-05 무중단). 삼항 `?:` 금지 → if-else. 함수 30~40줄 — 헬퍼로 분리.

    (a) 검출 위치 대입 (단순, HALCON 호출 없음 — Run 본문 인라인):
        result.HasDetection = true;
        result.DetectedRow1 = f1Row; result.DetectedCol1 = f1Col;
        result.DetectedRow2 = f2Row; result.DetectedCol2 = f2Col;
        result.DetectedCenterRow = midFRow; result.DetectedCenterCol = midFCol;
        (midFRow/midFCol 는 이미 Step 4 에서 계산됨 — 그 변수 재사용. 계산 순서상 result 대입 블록을 midFRow 계산 이후로 배치.)

    (b) 보정 ROI 박스 산출 — 신규 private 헬퍼 `BuildDetectedRoiBox(double detRow, double detCol, double detAngleDeg, EEthernetVisionMode mode, int modelIndex, out double[] box)`:
        - 목적: 검출 pose 를 중심으로 패턴 박스를 그린다. 박스 크기는 .shm 자체엔 직접 노출 안 되므로, ROI 박스는 "검출 중심 + 고정 표시 크기" 로 산출한다 (시각화 목적). const `DETECTED_ROI_HALF_LEN = 60.0;` (px, 표시용 사각 반폭) 클래스 상단에 추가.
        - box = new double[] { detRow, detCol, detAngleDeg * Math.PI / 180.0, DETECTED_ROI_HALF_LEN, DETECTED_ROI_HALF_LEN };
          (phi = 검출 각도 rad — DispRectangle2 규약. len1/len2 = 표시 반폭.)
        - 반환 true. (단순 좌표 산출 — HALCON 호출 없음. try-catch 불필요하나 호출부에서 전체 블록 try-catch 안에 둠.)
        Run 본문에서 두 패턴에 대해 호출하여 result.DetectedRoiBoxes 에 Add (f1/f2 각각, f1AngleDeg/f2AngleDeg 사용).

    (c) 검출 에지 contour 산출 — 신규 private 헬퍼 `bool TryExtractDetectedContour(string shmPath, double detRow, double detCol, double detAngleDeg, List<double> outRows, List<double> outCols, out string error)`:
        - try-catch 로 전체 감싸기. 실패 시 outRows/outCols 비우고 error 세팅 후 return false (throw 금지).
        - HOperatorSet.ReadShapeModel(shmPath, out modelId);
        - HOperatorSet.GetShapeModelContours(out modelContours, modelId, 1);   // level 1 = 최상위 contour
        - double detAngleRad = detAngleDeg * Math.PI / 180.0;
        - HOperatorSet.VectorAngleToRigid(0.0, 0.0, 0.0, detRow, detCol, detAngleRad, out homMat);  // 모델원점(0,0,0)→검출pose 강체변환
        - HOperatorSet.AffineTransContourXld(modelContours, out movedContours, homMat);
        - HOperatorSet.GetContourXld(movedContours, out contRows, out contCols);  // HTuple
        - contour 점이 많을 수 있으므로 const `EDGE_CONTOUR_MAX_POINTS = 400;` 로 다운샘플(stride = max(1, count/MAX)) — outRows/outCols 에 stride 간격으로 Add. (D-50 discretion: 에지 단순화 수준 = 최대 400점.)
        - finally 에서 modelContours/movedContours(HObject) + homMat/contRows/contCols(HTuple) 모두 Dispose (기존 ComputeAngleLx/ApplyPickerCenterCorrection 의 dispose 관습 동일 — try{ .Dispose(); }catch{}).
        Run 본문에서 두 패턴 .shm(shmPath1, shmPath2) 각각 호출하여 result.EdgeContourRows/Cols 에 누적 (두 패턴 contour 합쳐 표시). 실패해도 무시(로그만).

    배치 위치: 위 (a)(b)(c) 전체를 하나의 try-catch 블록으로 감싸 Run 의 기존 성공 경로(L404~L427) 안에 삽입. 예외 시 Logging.PrintLog(Error) 후 시각화 필드만 비고 result.Found 등 기존 반환은 유지.

    Logging: 성공 시 기존 "[ALIGN_SVC] run OK" 로그(L424)에 검출 contour 점 수를 1개 토큰 추가 가능 (선택). 필수는 아님.
  </action>
  <verify>
    <automated>grep -n "GetShapeModelContours\|AffineTransContourXld\|result.HasDetection\|DetectedRoiBoxes.Add\|TryExtractDetectedContour" WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "result.HasDetection = true" AlignShapeMatchService.cs` 1건
    - `grep -n "result.DetectedRow1\|result.DetectedRow2\|result.DetectedCenterRow" AlignShapeMatchService.cs` 3건 이상 (검출 좌표 대입)
    - `grep -n "DetectedRoiBoxes.Add\|BuildDetectedRoiBox" AlignShapeMatchService.cs` 가 헬퍼 정의 1 + 호출 2건 이상
    - `grep -n "TryExtractDetectedContour" AlignShapeMatchService.cs` 가 정의 1 + 호출 2건 (shmPath1, shmPath2)
    - `grep -n "GetShapeModelContours\|VectorAngleToRigid\|AffineTransContourXld\|GetContourXld\|ReadShapeModel" AlignShapeMatchService.cs` 가 contour 추출 HALCON 호출 모두 존재
    - `grep -n "DETECTED_ROI_HALF_LEN\|EDGE_CONTOUR_MAX_POINTS" AlignShapeMatchService.cs` 가 2개 const 존재
    - `grep -n "public AlignResult Run(HImage img, EEthernetVisionMode mode)" AlignShapeMatchService.cs` 1건 (시그니처 무변경 — 하위호환)
    - contour 헬퍼에 `finally` 블록 + `.Dispose()` 존재 (`grep -n "finally" AlignShapeMatchService.cs` 가 기존 ComputeAngleLx/ApplyPickerCenterCorrection 외 신규 1건 이상)
    - 삼항 검증: `grep -nE "\?[^.?]" AlignShapeMatchService.cs` 결과에 신규 라인의 `condition ? a : b` 0건 (if-else 사용)
    - 주석 `grep "//260625 hbk Phase 61.1" AlignShapeMatchService.cs` 1건 이상
  </acceptance_criteria>
  <done>Run 검출 성공 경로에서 HasDetection/검출좌표/보정 ROI 박스(DetectedRoiBoxes)/에지 contour(EdgeContourRows/Cols)가 채워지고, 모든 신규 HALCON 호출이 try-catch+finally dispose 로 감싸지며, Run 시그니처와 기존 Found/Offset/Theta 반환은 무변경.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 .shm 파일 → AlignShapeMatchService | 레시피 폴더의 shape model 파일 읽기 (사용자 로컬, 신뢰). 신규 untrusted 입력 없음. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61.1-01 | Denial of Service | AlignShapeMatchService.TryExtractDetectedContour (ReadShapeModel/GetShapeModelContours) | mitigate | contour 추출 전체를 try-catch+finally Dispose 로 감싸 손상 .shm/HALCON 오류 시에도 throw 없이 result.Found 기존 반환 유지 (UI 무중단). EDGE_CONTOUR_MAX_POINTS=400 다운샘플로 비정상 대용량 contour 메모리 폭주 차단. |
| T-61.1-02 | Tampering | .shm 파일 변조 | accept | 로컬 파일, 레시피 권한 사용자 소유. 변조 시 매칭 실패→Found=false 로 안전 degrade. PII/네트워크 노출 없음. |
</threat_model>

<verification>
- `grep -n "public AlignResult Run(HImage img, EEthernetVisionMode mode)" AlignShapeMatchService.cs` — Run 시그니처 무변경 (하위호환).
- AlignResult 기존 6필드(Found/Score/OffsetXmm/OffsetYmm/ThetaDeg/HasTheta) 전부 잔존.
- 신규 HALCON contour 추출이 try-catch+finally Dispose 로 감싸짐.
- 최종 빌드 검증은 Plan 04 의 msbuild 게이트에서 수행.
</verification>

<success_criteria>
- AlignResult 에 검출 좌표/보정 ROI/에지 contour 시각화 필드 추가 (기존 필드 하위호환).
- AlignShapeMatchService.Run 이 검출 성공 시 시각화 필드를 채워 반환, 실패/예외 시 기존 동작 유지(무중단).
- ROADMAP Success Criteria #3(AlignShapeMatchService 검출 좌표·에지 반환 확장, 기존 계약 하위호환) 충족.
</success_criteria>

<output>
After completion, create `.planning/phases/61.1-align-offline-loader-result-viz-2026-06-25/61.1-01-SUMMARY.md`
</output>
