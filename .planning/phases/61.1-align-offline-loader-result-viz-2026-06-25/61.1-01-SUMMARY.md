---
phase: 61.1-align-offline-loader-result-viz-2026-06-25
plan: "01"
subsystem: EthernetVision / AlignShapeMatch
tags: [align, shape-match, visualization, halcon, result-model]
dependency_graph:
  requires: []
  provides: [AlignResult.DetectedRow1/Col1/Row2/Col2/CenterRow/CenterCol, AlignResult.DetectedRoiBoxes, AlignResult.EdgeContourRows/Cols, AlignShapeMatchService.BuildDetectedRoiBox, AlignShapeMatchService.TryExtractDetectedContour]
  affects: [AlignShapeMatchService.Run, AlignResult (POCO)]
tech_stack:
  added: [System.Collections.Generic (List<double[]>, List<double>)]
  patterns: [HALCON-try-catch-finally-dispose, additive-POCO-extension, stride-downsample]
key_files:
  created: []
  modified:
    - WPF_Example/Custom/EthernetVision/AlignResult.cs
    - WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs
decisions:
  - AlignResult 확장 방식 = 필드 추가(out 파라미터 없음) — 하위호환 최대화, Plan 62 TCP 소비 회귀 0
  - DetectedRoiBoxes 박스 크기 = DETECTED_ROI_HALF_LEN(60px) 고정 — .shm 직접 크기 노출 없음, 시각화 목적 표시
  - 에지 contour 다운샘플 = stride=totalPts/EDGE_CONTOUR_MAX_POINTS(400) — T-61.1-01 메모리 폭주 차단
  - List 필드 선언부 즉시 초기화 — 소비측 null 가드 부담 제거
metrics:
  duration: 15
  completed_date: "2026-06-25"
  task_count: 2
  file_count: 2
---

# Phase 61.1 Plan 01: AlignResult/AlignShapeMatchService 시각화 필드 확장 Summary

**One-liner:** AlignResult POCO에 검출 좌표 7필드+ROI박스+에지 contour 3필드를 추가하고, AlignShapeMatchService.Run에서 HALCON GetShapeModelContours+AffineTransContourXld로 채워 반환하는 시각화 데이터 계층 구현.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | AlignResult 시각화 필드 추가 | 36abe29 | AlignResult.cs |
| 2 | AlignShapeMatchService.Run 검출 pose/ROI/에지 산출 | 7d23b7b | AlignShapeMatchService.cs |

## What Was Built

### Task 1 — AlignResult.cs 시각화 필드 (ADDITIVE)

기존 6필드(Found/Score/OffsetXmm/OffsetYmm/ThetaDeg/HasTheta) 완전 보존.  
다음을 추가:

- `HasDetection` — 시각화 게이트 (bool)
- `DetectedRow1/Col1`, `DetectedRow2/Col2` — TL/BR 패턴 검출 중심 (px)
- `DetectedCenterRow/CenterCol` — 두 패턴 midpoint (px)
- `DetectedRoiBoxes` — `List<double[]>` 각 항목 `{row,col,phi,len1,len2}`, DispRectangle2 규약
- `EdgeContourRows`, `EdgeContourCols` — `List<double>` contour 점 쌍, 즉시 초기화

### Task 2 — AlignShapeMatchService.cs 확장

- 클래스 상수: `DETECTED_ROI_HALF_LEN=60.0`, `EDGE_CONTOUR_MAX_POINTS=400`
- `using System.Collections.Generic` 추가
- `BuildDetectedRoiBox` private 헬퍼: 검출 각도 → phi 변환, 고정 크기 박스 배열 반환 (HALCON 호출 없음)
- `TryExtractDetectedContour` private 헬퍼:
  - `ReadShapeModel` → `GetShapeModelContours` → `VectorAngleToRigid` → `AffineTransContourXld` → `GetContourXld`
  - stride 다운샘플(totalPts>400 시 간격 추출)
  - try-catch+finally: modelId/modelContours/movedContours/homMat/contRows/contCols 전부 Dispose
- `Run()` 성공 경로에 시각화 블록 삽입(기존 로그 직전):
  - (a) HasDetection=true + 검출 좌표 6개 대입
  - (b) BuildDetectedRoiBox × 2 → DetectedRoiBoxes.Add
  - (c) TryExtractDetectedContour × 2 → EdgeContourRows/Cols 누적
  - 전체 try-catch — 예외 시 로그만, result.Found/Offset 반환 유지 (D-05 무중단)

## Deviations from Plan

None — plan 이 지정한 대로 정확히 구현. anti-goal 준수(PatternMatchService/기타 파일 0 수정).

## Known Stubs

없음. 모든 필드는 Run() 검출 성공 경로에서 실제 값으로 채워짐.

## Threat Flags

없음 — 신규 네트워크 엔드포인트/인증 경로/외부 입력 없음. T-61.1-01(손상 .shm DoS) try-catch+finally+400점 다운샘플로 mitigate 완료.

## Self-Check: PASSED

- AlignResult.cs 존재: yes (36abe29)
- AlignShapeMatchService.cs 존재: yes (7d23b7b)
- `grep -n "public bool Found" AlignResult.cs` → L11 (기존 필드 보존)
- `grep -n "public AlignResult Run(HImage img, EEthernetVisionMode mode)"` → L328 (시그니처 무변경)
- `grep -n "GetShapeModelContours\|VectorAngleToRigid"` → L526/L530 (contour 추출 HALCON 호출 존재)
- `grep -n "finally"` → L134/L564/L621 (신규 finally 1건 이상 확인)
- 삼항 `?:` 신규 사용: 0건
