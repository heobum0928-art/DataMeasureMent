---
phase: 61.1-align-offline-loader-result-viz-2026-06-25
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/UI/TrayVisionView.xaml
  - WPF_Example/Custom/UI/TrayVisionView.xaml.cs
  - WPF_Example/Custom/UI/BottomVisionView.xaml
  - WPF_Example/Custom/UI/BottomVisionView.xaml.cs
autonomous: true
requirements: [AV-08]
must_haves:
  truths:
    - "Tray/Bottom 뷰 카메라 패널에 [폴더 열기] + [◀ 이전] [다음 ▶] 버튼이 있다"
    - "[폴더 열기] 클릭 시 폴더 선택 → 폴더 내 이미지(bmp/png/jpg/tif) 목록을 로드하고 인덱스 0 의 이미지를 뷰어에 표시한다"
    - "[이전]/[다음] 클릭 시 목록을 인덱스 순회하며 해당 이미지를 뷰어에 로드한다"
    - "로드한 이미지가 _viewer.CurrentImage 가 되어 기존 티칭/Run/캘 핸들러의 입력으로 사용된다 (핸들러 코드 무변경)"
    - "기존 Grab/Live/Stop + D:\\align_test.bmp 폴백 동작은 회귀 없이 유지된다"
  artifacts:
    - path: "WPF_Example/Custom/UI/TrayVisionView.xaml.cs"
      provides: "폴더 열기/이전/다음 핸들러 + 이미지 목록/인덱스 상태"
      contains: "VistaFolderBrowserDialog"
    - path: "WPF_Example/Custom/UI/BottomVisionView.xaml.cs"
      provides: "폴더 열기/이전/다음 핸들러 + 이미지 목록/인덱스 상태"
      contains: "VistaFolderBrowserDialog"
    - path: "WPF_Example/Custom/UI/TrayVisionView.xaml"
      provides: "로더 버튼 3개 (폴더 열기/이전/다음)"
      contains: "OpenFolderButton_Click"
    - path: "WPF_Example/Custom/UI/BottomVisionView.xaml"
      provides: "로더 버튼 3개 (폴더 열기/이전/다음)"
      contains: "OpenFolderButton_Click"
  key_links:
    - from: "OpenFolderButton_Click / Prev / Next"
      to: "_viewer.LoadImage(path)"
      via: "현재 인덱스 이미지 경로 로드 → CurrentImage 갱신"
      pattern: "_viewer.LoadImage\\("
---

<objective>
Tray/Bottom 비전 뷰에 Offline 다중 이미지 로더([폴더 열기] + [◀ 이전][다음 ▶])를 추가한다. D-01: 폴더 내 이미지(bmp/png/jpg/tif) 목록 로드 → 인덱스 순회. 로드한 이미지가 "현재 이미지"(_viewer.CurrentImage)가 되어 티칭/Run/캘 입력으로 사용. 기존 Grab/Live/Stop + D:\align_test.bmp 폴백은 회귀 0. 폴더 다이얼로그 = Ookii.Dialogs.Wpf.

Purpose: Phase 61 UAT 갭 G-61.1-01(Grab 이 D:\align_test.bmp 한 장만 폴백 → 이동/회전 다양 케이스로 offset/theta 검증 불가) 해소. 16세트 등 실 Tray 이미지를 빠르게 순회하며 매칭 검증.
Output: 두 뷰 XAML 에 로더 버튼 3개 + 코드비하인드에 폴더 선택/목록/인덱스/로드 핸들러. 기존 핸들러는 무변경(LoadImage 가 CurrentImage 를 갱신하므로 Teach/Run/Cal 이 자동으로 로드 이미지 사용).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61.1-align-offline-loader-result-viz-2026-06-25/61.1-CONTEXT.md

<interfaces>
<!-- 공유 뷰어 API (MainResultViewerControl — consume only, 무수정): -->
public HImage CurrentImage { get; private set; }      // 로드/Grab 된 현재 이미지
public string CurrentImagePath { get; private set; }
public void LoadImage(string imagePath)               // 파일 경로 로드 → CurrentImage = new HImage(path) + Render + ApplyInitialFitView
public void LoadImage(HImage image)                   // HImage 로드 (내부 Clone)

<!-- 두 뷰의 기존 멤버 (이미 존재): -->
private MainResultViewerControl _viewer;               // AttachSharedViewer 로 주입됨
// 기존 핸들러 GrabButton_Click / TeachButton_Click / RunButton_Click 모두 _viewer.CurrentImage 를 입력으로 사용.
// → 로더가 LoadImage(path) 호출하면 CurrentImage 가 갱신되어 기존 핸들러가 자동으로 로드 이미지 사용 (핸들러 코드 변경 불필요).

<!-- 폴더+이미지 목록 로드 정본 패턴 (WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs L334~361): -->
var folderDlg = new Ookii.Dialogs.Wpf.VistaFolderBrowserDialog();
folderDlg.Multiselect = false;
folderDlg.SelectedPath = <마지막 폴더 또는 기본 경로>;
if (folderDlg.ShowDialog() != true) { return; }
string folder = folderDlg.SelectedPath;
imagePaths = Directory.GetFiles(folder)
    .Where(f => 확장자집합.Contains(Path.GetExtension(f).ToLowerInvariant()))
    .OrderBy(f => f, StringComparer.OrdinalIgnoreCase)
    .ToList();

<!-- 기존 XAML 레이아웃 (Row2 = 카메라 GroupBox, WrapPanel 안에 Grab/Live/Stop 버튼): -->
<!-- TrayVisionView.xaml: 버튼 스타일 = {StaticResource TrayActionButtonStyle} -->
<!-- BottomVisionView.xaml: 버튼 스타일 = {StaticResource BottomActionButtonStyle} -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tray/Bottom XAML 에 로더 버튼 3개 추가 (카메라 패널)</name>
  <read_first>
    - WPF_Example/Custom/UI/TrayVisionView.xaml (Row2 카메라 GroupBox L87~103, 버튼 스타일 TrayActionButtonStyle)
    - WPF_Example/Custom/UI/BottomVisionView.xaml (Row2 카메라 GroupBox L90~106, 버튼 스타일 BottomActionButtonStyle)
  </read_first>
  <files>WPF_Example/Custom/UI/TrayVisionView.xaml, WPF_Example/Custom/UI/BottomVisionView.xaml</files>
  <action>
    각 뷰 Row2 "카메라" GroupBox 의 WrapPanel(기존 Grab/Live/Stop 버튼 아래) 또는 Row2 GroupBox 헤더를 "카메라 / 이미지 로더" 로 바꾸고 같은 WrapPanel 에 로더 버튼 3개를 추가한다. airspace 준수 — 이미 좌측 컨트롤 패널(HWND 밖)이라 안전. XAML 주석 `<!--260625 hbk Phase 61.1 오프라인 이미지 로더-->` 표기.

    TrayVisionView.xaml — Row2 WrapPanel 안 Stop 버튼 뒤에 추가:
    ```
    <!--260625 hbk Phase 61.1 오프라인 이미지 로더-->
    <Button x:Name="btn_openFolder" Content="폴더 열기"
            Style="{StaticResource TrayActionButtonStyle}" Click="OpenFolderButton_Click"/>
    <Button x:Name="btn_prevImage" Content="◀ 이전"
            Style="{StaticResource TrayActionButtonStyle}" Click="PrevImageButton_Click"/>
    <Button x:Name="btn_nextImage" Content="다음 ▶"
            Style="{StaticResource TrayActionButtonStyle}" Click="NextImageButton_Click"/>
    ```
    그리고 카메라 GroupBox 바로 아래(또는 WrapPanel 아래)에 로더 상태 라벨 1개 추가:
    ```
    <TextBlock x:Name="lbl_loaderStatus" Margin="8,0,0,4" FontSize="12"
               Foreground="#475569" Text="로드된 이미지 없음" TextWrapping="Wrap"/>
    ```
    (라벨은 WrapPanel 과 같은 StackPanel 컨텍스트에 들어가도록 — 필요 시 GroupBox 내부를 WrapPanel 단독에서 StackPanel{WrapPanel + TextBlock} 으로 감싸기. 기존 Grab/Live/Stop 버튼 x:Name/Click 핸들러는 무변경.)

    BottomVisionView.xaml — 동일하게 Row2 WrapPanel 에 동일 3버튼 + lbl_loaderStatus 추가하되 Style 은 `{StaticResource BottomActionButtonStyle}` 사용. 핸들러명은 두 뷰 동일(OpenFolderButton_Click / PrevImageButton_Click / NextImageButton_Click).

    Grab/Live/Stop 의 기존 x:Name(btn_grab/btn_live/btn_stop)·Click 핸들러는 절대 수정하지 말 것 (회귀 0).
  </action>
  <verify>
    <automated>grep -c "OpenFolderButton_Click\|PrevImageButton_Click\|NextImageButton_Click\|lbl_loaderStatus" WPF_Example/Custom/UI/TrayVisionView.xaml WPF_Example/Custom/UI/BottomVisionView.xaml</automated>
  </verify>
  <acceptance_criteria>
    - TrayVisionView.xaml: `grep -n "btn_openFolder\|btn_prevImage\|btn_nextImage\|lbl_loaderStatus"` 4개 모두 존재
    - TrayVisionView.xaml: `grep -n "OpenFolderButton_Click\|PrevImageButton_Click\|NextImageButton_Click"` 3개 Click 바인딩 존재
    - BottomVisionView.xaml: 위와 동일 4개 x:Name + 3개 Click 바인딩 존재
    - 두 XAML 모두 기존 `grep -n "GrabButton_Click\|LiveButton_Click\|StopButton_Click"` 가 각 1건씩 잔존 (기존 핸들러 무변경)
    - 두 XAML 모두 신규 버튼 Style 이 각 뷰 고유 스타일(TrayActionButtonStyle / BottomActionButtonStyle) 참조
    - 주석 `grep "260625 hbk Phase 61.1"` 각 XAML 1건 이상
  </acceptance_criteria>
  <done>두 뷰 카메라 패널에 [폴더 열기][◀ 이전][다음 ▶] 버튼 + 로더 상태 라벨이 추가되고, 기존 Grab/Live/Stop 버튼은 무변경.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tray/Bottom 코드비하인드 로더 핸들러 + 목록/인덱스 상태</name>
  <read_first>
    - WPF_Example/Custom/UI/TrayVisionView.xaml.cs (전체 — using 블록 L1~9, 멤버 L20~35, GrabButton_Click L65~88 패턴, RefreshStatus)
    - WPF_Example/Custom/UI/BottomVisionView.xaml.cs (전체 — using 블록 L1~8, 멤버 L20~41)
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs (L334~361 Ookii 폴더+GetFiles+확장자 필터 정본)
  </read_first>
  <files>WPF_Example/Custom/UI/TrayVisionView.xaml.cs, WPF_Example/Custom/UI/BottomVisionView.xaml.cs</files>
  <action>
    두 뷰 코드비하인드에 동일 구조의 로더를 추가한다. 주석 `//260625 hbk Phase 61.1` 표기. 삼항 `?:` 금지(if-else), 헝가리언, C# 7.2, 함수 30~40줄, try-catch(파일 I/O 실패해도 throw 금지 — 상태 라벨만 갱신).

    using 추가 (없으면): `using System.Collections.Generic;`, `using System.IO;`, `using System.Linq;`.

    멤버 추가 (클래스 필드):
    ```
    //260625 hbk Phase 61.1 오프라인 이미지 로더 상태
    private const string LOADER_IMAGE_EXTS = ".bmp;.png;.jpg;.jpeg;.tif;.tiff";  // 지원 확장자 (discretion)
    private List<string> _loadedImagePaths = new List<string>();
    private int _loadedImageIndex = -1;   // -1 = 미로드
    private static string _lastImageFolder = null;   // 폴더 마지막 위치 기억 (static — 뷰 재생성/탭 전환에도 유지, discretion)
    ```

    핸들러 1 — OpenFolderButton_Click:
    - try-catch.
    - `var dlg = new Ookii.Dialogs.Wpf.VistaFolderBrowserDialog(); dlg.Multiselect = false;`
    - if (!string.IsNullOrEmpty(_lastImageFolder)) dlg.SelectedPath = _lastImageFolder;
    - if (dlg.ShowDialog() != true) return;
    - string folder = dlg.SelectedPath; if (string.IsNullOrEmpty(folder) || !Directory.Exists(folder)) { lbl_loaderStatus.Text = "폴더 없음"; return; }
    - _lastImageFolder = folder;
    - 확장자 HashSet 구성: `var exts = new HashSet<string>(LOADER_IMAGE_EXTS.Split(';'), System.StringComparer.OrdinalIgnoreCase);`
    - `_loadedImagePaths = Directory.GetFiles(folder).Where(f => exts.Contains(Path.GetExtension(f))).OrderBy(f => f, System.StringComparer.OrdinalIgnoreCase).ToList();`
    - if (_loadedImagePaths.Count == 0) { _loadedImageIndex = -1; lbl_loaderStatus.Text = "이미지 없음 (bmp/png/jpg/tif)"; return; }
    - _loadedImageIndex = 0; LoadCurrentLoaderImage();  (헬퍼)

    헬퍼 — LoadCurrentLoaderImage (private, 30줄 이내):
    - if (_viewer == null) { lbl_loaderStatus.Text = "뷰어 미연결"; return; }
    - if (_loadedImageIndex < 0 || _loadedImageIndex >= _loadedImagePaths.Count) return;
    - string path = _loadedImagePaths[_loadedImageIndex];
    - try { _viewer.LoadImage(path); ... } catch (Exception ex) { lbl_loaderStatus.Text = "로드 오류: " + ex.Message; return; }
    - lbl_loaderStatus.Text = string.Format("{0}/{1}  {2}", _loadedImageIndex + 1, _loadedImagePaths.Count, Path.GetFileName(path));
    - lbl_status.Text = "대기";   // 로드 성공 — 기존 상태 라벨도 대기로

    핸들러 2 — PrevImageButton_Click:
    - if (_loadedImagePaths.Count == 0) { lbl_loaderStatus.Text = "폴더 먼저 열기"; return; }
    - if (_loadedImageIndex > 0) { _loadedImageIndex = _loadedImageIndex - 1; LoadCurrentLoaderImage(); } else { lbl_loaderStatus.Text = "첫 이미지"; }

    핸들러 3 — NextImageButton_Click:
    - if (_loadedImagePaths.Count == 0) { lbl_loaderStatus.Text = "폴더 먼저 열기"; return; }
    - if (_loadedImageIndex < _loadedImagePaths.Count - 1) { _loadedImageIndex = _loadedImageIndex + 1; LoadCurrentLoaderImage(); } else { lbl_loaderStatus.Text = "마지막 이미지"; }

    중요(회귀 0): 기존 GrabButton_Click / LiveButton_Click / StopButton_Click / TeachButton_Click / RunButton_Click (Bottom 은 추가로 Cal* 핸들러)을 절대 수정하지 말 것. 로더는 _viewer.LoadImage 로 CurrentImage 만 갱신 → 기존 핸들러가 그 CurrentImage 를 자동 사용. D:\align_test.bmp 폴백은 EthernetAlignCamera.Grab 경로(무수정)에 그대로 존재.

    두 뷰는 동일 코드 — BottomVisionView.xaml.cs 도 동일 멤버/핸들러/헬퍼를 추가 (Bottom 의 Cal/Theta 핸들러와 충돌 없음, 별도 메서드).
  </action>
  <verify>
    <automated>grep -n "OpenFolderButton_Click\|PrevImageButton_Click\|NextImageButton_Click\|LoadCurrentLoaderImage\|_loadedImagePaths\|VistaFolderBrowserDialog" WPF_Example/Custom/UI/TrayVisionView.xaml.cs WPF_Example/Custom/UI/BottomVisionView.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - 두 .cs 각각: `grep -n "private void OpenFolderButton_Click\|private void PrevImageButton_Click\|private void NextImageButton_Click"` 3개 핸들러 정의 존재
    - 두 .cs 각각: `grep -n "LoadCurrentLoaderImage"` 가 정의 1 + 호출 3건(OpenFolder/Prev/Next)
    - 두 .cs 각각: `grep -n "VistaFolderBrowserDialog\|Directory.GetFiles\|_viewer.LoadImage(path)"` 모두 존재
    - 두 .cs 각각: `grep -n "_loadedImagePaths\|_loadedImageIndex\|_lastImageFolder\|LOADER_IMAGE_EXTS"` 4개 멤버 존재
    - 두 .cs 각각: 기존 `grep -n "private void GrabButton_Click\|private void RunButton_Click\|private void TeachButton_Click"` 가 각 1건씩 잔존 (수정 없음 — 줄 수/시그니처 동일)
    - BottomVisionView.xaml.cs: 기존 `grep -n "CalAddStepButton_Click\|CalComputeButton_Click"` 잔존 (Cal 핸들러 무변경)
    - 삼항 검증: `grep -nE "\?[^.?]" TrayVisionView.xaml.cs BottomVisionView.xaml.cs` 의 신규 라인에 `cond ? a : b` 0건 (if-else)
    - 주석 `grep "//260625 hbk Phase 61.1"` 각 .cs 1건 이상
  </acceptance_criteria>
  <done>두 뷰에 폴더 열기/이전/다음 핸들러 + 목록·인덱스·마지막폴더 상태 + LoadCurrentLoaderImage 헬퍼가 추가되고, 로드 이미지가 _viewer.LoadImage 로 CurrentImage 를 갱신하며, 기존 Grab/Live/Stop/Teach/Run/Cal 핸들러는 무변경.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 이미지 폴더 → 뷰 로더 | 사용자가 폴더 다이얼로그로 선택한 로컬 이미지 파일(bmp/png/jpg/tif) 읽기. 사용자 본인 선택 = 신뢰 경계, 신규 네트워크/원격 입력 없음. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61.1-03 | Denial of Service | LoadCurrentLoaderImage / OpenFolderButton_Click (Directory.GetFiles, _viewer.LoadImage) | mitigate | 폴더 열기/로드 전체를 try-catch 로 감싸 손상 이미지/접근 거부 시 throw 없이 lbl_loaderStatus 갱신만 (UI 무중단). 확장자 화이트리스트(LOADER_IMAGE_EXTS)로 비이미지 파일 로드 시도 차단. |
| T-61.1-04 | Tampering | 로컬 이미지 파일 | accept | 사용자 로컬 파일, 본인 선택. 손상 시 HImage 생성 예외→catch→상태 라벨, 안전 degrade. PII/네트워크 노출 없음. |
</threat_model>

<verification>
- 두 뷰 카메라 패널에 로더 버튼 3개 + 상태 라벨 존재.
- OpenFolder→목록 로드→index 0 표시, Prev/Next→인덱스 순회→LoadImage 호출.
- 기존 Grab/Live/Stop/Teach/Run/Cal 핸들러 무수정 (회귀 0).
- _viewer.LoadImage(path) 가 CurrentImage 를 갱신 → 기존 Teach/Run/Cal 이 로드 이미지 자동 사용.
- 최종 빌드 검증은 Plan 04 의 msbuild 게이트에서 수행.
</verification>

<success_criteria>
- Tray/Bottom 뷰 툴바에 [폴더 열기] + [◀ 이전][다음 ▶] 로 폴더 내 이미지 연속 로드/전환.
- 로드한 이미지가 티칭/Run/캘 입력으로 사용 (CurrentImage 갱신 경유).
- 기존 Grab/폴백 동작 유지 (회귀 0).
- ROADMAP Success Criteria #1 충족.
</success_criteria>

<output>
After completion, create `.planning/phases/61.1-align-offline-loader-result-viz-2026-06-25/61.1-02-SUMMARY.md`
</output>
