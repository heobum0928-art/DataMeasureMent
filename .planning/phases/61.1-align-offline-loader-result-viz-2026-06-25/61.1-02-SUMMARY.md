---
phase: 61.1-align-offline-loader-result-viz-2026-06-25
plan: 02
subsystem: UI
tags: [offline-loader, image-navigation, tray-vision, bottom-vision, ookii-dialogs]
dependency_graph:
  requires: []
  provides: [offline-image-loader-tray, offline-image-loader-bottom]
  affects: [TrayVisionView, BottomVisionView]
tech_stack:
  added: []
  patterns: [Ookii.Dialogs.Wpf.VistaFolderBrowserDialog, Directory.GetFiles + HashSet 확장자 화이트리스트, 인덱스 순회 + LoadImage]
key_files:
  created: []
  modified:
    - WPF_Example/Custom/UI/TrayVisionView.xaml
    - WPF_Example/Custom/UI/TrayVisionView.xaml.cs
    - WPF_Example/Custom/UI/BottomVisionView.xaml
    - WPF_Example/Custom/UI/BottomVisionView.xaml.cs
decisions:
  - "로더 폴더 마지막 위치 = static _lastImageFolder (탭 전환/뷰 재생성에도 유지)"
  - "지원 확장자 = LOADER_IMAGE_EXTS const (.bmp;.png;.jpg;.jpeg;.tif;.tiff)"
  - "XAML: 기존 WrapPanel 단독 → StackPanel{WrapPanel+TextBlock} 구조로 변경 (lbl_loaderStatus 삽입)"
metrics:
  duration: 25min
  completed_date: 2026-06-25
  tasks_completed: 2
  tasks_total: 2
  files_modified: 4
---

# Phase 61.1 Plan 02: Tray/Bottom Offline 이미지 로더 Summary

**One-liner:** Tray/Bottom 뷰에 [폴더 열기]+[◀ 이전][다음 ▶] 로더 추가 — VistaFolderBrowserDialog + 확장자 화이트리스트 + 인덱스 순회로 폴더 내 이미지를 LoadImage(path) 경유 CurrentImage 갱신.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Tray/Bottom XAML 로더 버튼 3개 + 상태 라벨 추가 | 529ea80 | TrayVisionView.xaml, BottomVisionView.xaml |
| 2 | Tray/Bottom 코드비하인드 로더 핸들러 + 목록/인덱스 상태 | 9c99f6e | TrayVisionView.xaml.cs, BottomVisionView.xaml.cs |

## What Was Built

### Task 1 — XAML 변경

두 뷰 Row2 카메라 GroupBox:
- Header "카메라" → "카메라 / 이미지 로더"
- 기존 WrapPanel 단독 → `StackPanel { WrapPanel + TextBlock }` 구조
- WrapPanel 내 기존 Grab/Live/Stop 뒤에 버튼 3개 추가:
  - `btn_openFolder` (Click="OpenFolderButton_Click", 각 뷰 고유 스타일)
  - `btn_prevImage` (Click="PrevImageButton_Click")
  - `btn_nextImage` (Click="NextImageButton_Click")
- TextBlock `lbl_loaderStatus` (Text="로드된 이미지 없음") WrapPanel 아래 삽입
- 주석 `<!--260625 hbk Phase 61.1 오프라인 이미지 로더-->` 표기
- 기존 btn_grab/btn_live/btn_stop x:Name·Click 핸들러 무변경

### Task 2 — 코드비하인드 변경

두 .cs 공통 추가:
- using: `System.Collections.Generic`, `System.IO`, `System.Linq`
- 멤버 필드:
  - `const string LOADER_IMAGE_EXTS = ".bmp;.png;.jpg;.jpeg;.tif;.tiff"`
  - `List<string> _loadedImagePaths` (이미지 경로 목록)
  - `int _loadedImageIndex = -1` (-1 = 미로드)
  - `static string _lastImageFolder` (뷰 재생성에도 유지)
- `OpenFolderButton_Click`: VistaFolderBrowserDialog → Directory.GetFiles + HashSet 확장자 필터 → OrdinalIgnoreCase 정렬 → 인덱스 0 → `LoadCurrentLoaderImage()`
- `PrevImageButton_Click` / `NextImageButton_Click`: 경계 가드 + 인덱스 이동 → `LoadCurrentLoaderImage()`
- `LoadCurrentLoaderImage()`: `_viewer.LoadImage(path)` 호출 → CurrentImage 갱신 → lbl_loaderStatus "N/total  filename.bmp" + lbl_status "대기"
  - 파일 I/O 실패 → catch → lbl_loaderStatus 갱신만, throw 금지 (T-61.1-03)
  - _viewer null 가드 포함

**회귀 0 보장:** 기존 GrabButton_Click/LiveButton_Click/StopButton_Click/TeachButton_Click/RunButton_Click (Bottom: CalAddStep/CalCompute) 메서드 무변경. 로더는 _viewer.LoadImage 경유 CurrentImage만 갱신 → 기존 핸들러가 코드 변경 없이 로드 이미지 자동 사용.

## Deviations from Plan

None - 계획된 대로 정확히 실행.

## Verification Results

### Plan `<verify>` grep PASS

```
grep -n "OpenFolderButton_Click|PrevImageButton_Click|NextImageButton_Click|LoadCurrentLoaderImage|_loadedImagePaths|VistaFolderBrowserDialog" TrayVisionView.xaml.cs BottomVisionView.xaml.cs
→ Tray: 17건, Bottom: 17건 (정의+호출 전부 존재)
```

### Acceptance Criteria PASS

| 항목 | 결과 |
|------|------|
| 두 XAML: btn_openFolder/prevImage/nextImage/lbl_loaderStatus 4개 x:Name | PASS |
| 두 XAML: OpenFolder/Prev/Next Click 바인딩 3개 | PASS |
| 두 XAML: 기존 GrabButton_Click/LiveButton_Click/StopButton_Click 각 1건 잔존 | PASS |
| 두 XAML: 버튼 스타일 각 뷰 고유 스타일 참조 | PASS |
| 두 XAML: 주석 "260625 hbk Phase 61.1" 각 1건 이상 | PASS |
| 두 .cs: 핸들러 3개 정의 존재 | PASS |
| 두 .cs: LoadCurrentLoaderImage 정의 1 + 호출 3건 | PASS |
| 두 .cs: VistaFolderBrowserDialog/Directory.GetFiles/_viewer.LoadImage 존재 | PASS |
| 두 .cs: 4개 멤버 (_loadedImagePaths/_loadedImageIndex/_lastImageFolder/LOADER_IMAGE_EXTS) | PASS |
| 두 .cs: 기존 Grab/Run/Teach 핸들러 잔존 (무변경) | PASS |
| Bottom .cs: CalAddStep/CalCompute 잔존 | PASS |
| 삼항 ?: 신규 라인 0건 (if-else) | PASS |
| 수정 파일 범위: Tray/Bottom 4파일 외 0 수정 | PASS |

## Threat Flags

None - 신규 네트워크/원격 입력 없음. 로컬 파일 I/O만 추가, try-catch로 완화(T-61.1-03).

## Self-Check

### Created files exist:
(신규 파일 없음)

### Modified files committed:
- `529ea80` TrayVisionView.xaml, BottomVisionView.xaml — FOUND
- `9c99f6e` TrayVisionView.xaml.cs, BottomVisionView.xaml.cs — FOUND

## Self-Check: PASSED
