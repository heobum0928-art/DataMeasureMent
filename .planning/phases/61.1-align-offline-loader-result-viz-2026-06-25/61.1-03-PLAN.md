---
phase: 61.1-align-offline-loader-result-viz-2026-06-25
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - WPF_Example/Custom/UI/TrayVisionView.xaml
  - WPF_Example/Custom/UI/TrayVisionView.xaml.cs
  - WPF_Example/Custom/UI/BottomVisionView.xaml
  - WPF_Example/Custom/UI/BottomVisionView.xaml.cs
autonomous: true
requirements: [AV-08]
must_haves:
  truths:
    - "검사(Run) 성공 후 이미지 위에 검출 위치 십자가 표시된다"
    - "검사 성공 후 이미지 위에 검출 패턴 보정 ROI 박스가 표시된다"
    - "검사 성공 후 이미지 위에 검출 에지 contour 가 표시된다"
    - "[ROI 표시] 체크박스로 보정 ROI 박스를 on/off 토글한다"
    - "[에지 표시] 체크박스로 검출 에지를 on/off 토글한다"
    - "오버레이는 HALCON 창 내부에, 체크박스는 좌측 컨트롤 패널(뷰어 호스트 밖)에 있다 (airspace 준수)"
  artifacts:
    - path: "WPF_Example/Custom/UI/TrayVisionView.xaml.cs"
      provides: "Run 결과 → 검출 십자/보정 ROI/에지 오버레이 변환 + 체크박스 토글 핸들러"
      contains: "SetDatumFindResultOverlay"
    - path: "WPF_Example/Custom/UI/BottomVisionView.xaml.cs"
      provides: "동일 시각화 배선 (Bottom)"
      contains: "SetDatumFindResultOverlay"
    - path: "WPF_Example/Custom/UI/TrayVisionView.xaml"
      provides: "ROI 표시 / 에지 표시 체크박스"
      contains: "chk_showRoi"
    - path: "WPF_Example/Custom/UI/BottomVisionView.xaml"
      provides: "ROI 표시 / 에지 표시 체크박스"
      contains: "chk_showRoi"
  key_links:
    - from: "RunButton_Click (AlignResult.HasDetection)"
      to: "_viewer.SetResultRoiOverlays / SetInspectionOverlays / SetDatumFindResultOverlay"
      via: "검출 좌표→DatumConfig 십자, DetectedRoiBoxes→datumRects, EdgeContour→EdgeInspectionOverlay"
      pattern: "_viewer\\.(SetResultRoiOverlays|SetInspectionOverlays|SetDatumFindResultOverlay)"
    - from: "chk_showRoi / chk_showEdge"
      to: "_viewer.SetDatumOverlayVisible / SetMeasurementOverlayVisible"
      via: "Checked/Unchecked → 가시성 토글"
      pattern: "_viewer\\.Set(Datum|Measurement)OverlayVisible"
---

<objective>
검사(Run) 결과를 MainResultViewerControl 기존 오버레이 API 로 시각화한다. D-02: 검출 위치 십자 + 보정 ROI 박스 + 검출 에지 전부 표시. D-03: 신규 렌더러 만들지 말고 기존 API 재사용 + 좌측 패널에 [ROI 표시][에지 표시] 체크박스 추가(airspace). Plan 01(AlignResult 검출 필드)과 Plan 02(로더, 같은 4파일)에 의존하므로 Wave 2.

Purpose: Phase 61 UAT 갭 G-61.1-02(매칭 결과가 좌측 텍스트로만 표시) 해소의 UI 계층. Plan 01 이 반환한 검출 좌표/보정 ROI/에지를 화면에 그린다.
Output: 두 뷰 RunButton_Click 에 시각화 호출 추가 + XAML 에 체크박스 2개 + 토글 핸들러. 모두 MainResultViewerControl public API 호출(consume only — D-04 무수정).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61.1-align-offline-loader-result-viz-2026-06-25/61.1-CONTEXT.md
@.planning/phases/61.1-align-offline-loader-result-viz-2026-06-25/61.1-01-PLAN.md

<interfaces>
<!-- MainResultViewerControl 오버레이 API (consume only — 무수정, D-04). Render() 의 토글 게이트 매핑(검증 완료): -->
<!--   보정 ROI 박스(datumRects, orange) → _datumOverlayVisible 게이트 (line 844~846) -->
<!--   에지(_inspectionOverlays) → _measurementOverlayVisible 게이트 (line 786~787) -->
<!--   검출 십자(_datumFindResultOverlay) → 토글 게이트 없음, Clear 로만 제어 (line 899~901) -->
public void SetResultRoiOverlays(List<double[]> measRects, List<double[]> datumRects)
  // 각 rect 항목 = {row,col,phi,len1,len2}(사각, len≥5) 또는 {row,col,radius}(원, len3). RenderResultRoiBoxes 규약.
  // measRects → green(_measurementOverlayVisible), datumRects → orange(_datumOverlayVisible).
public void ClearResultRoiOverlays()
public void SetInspectionOverlays(IEnumerable<EdgeInspectionOverlay> overlays)   // _measurementOverlayVisible 게이트
public void SetDatumFindResultOverlay(DatumConfig datum)   // slate blue 검출 십자 + "Find (row,col)" 텍스트
public void ClearDatumFindResultOverlay()
public void SetMeasurementOverlayVisible(bool visible)     // 에지 토글
public void SetDatumOverlayVisible(bool visible)           // 보정 ROI 박스 토글
public HImage CurrentImage { get; private set; }

<!-- AlignResult 신규 필드 (Plan 01 산출): -->
bool HasDetection; double DetectedRow1/Col1, DetectedRow2/Col2, DetectedCenterRow/Col;
List<double[]> DetectedRoiBoxes;   // 각 {row,col,phi,len1,len2}
List<double> EdgeContourRows; List<double> EdgeContourCols;   // 인덱스 1:1

<!-- EdgeInspectionOverlay (WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs) — 에지 점 표시용: -->
public string RoiId; public List<EdgeInspectionPoint> Points;   // EdgeInspectionPoint { double Row; double Column; }
public double LineRow1/LineColumn1/LineRow2/LineColumn2;

<!-- DatumConfig 검출 십자용 필드 (WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs) — set 가능: -->
public double DetectedOriginRow/DetectedOriginCol;   // 십자 중심
public bool LastFindSucceeded;   // true 세팅 시 RenderDatumFindResult 게이트 통과 (line 333)
public string DatumName;   // "Find (row,col)" 텍스트와 함께 표시되는 라벨
// 주의: SetDatumFindResultOverlay 경로는 RenderDatumFindResult 직접 호출 → 'DETECT FAIL' 라벨(RenderDatumOverlay 내부)과 무관.

<!-- 두 뷰 기존 RunButton_Click (Plan 02 무변경 — 여기서 시각화 호출만 추가): -->
//   AlignResult res = EthernetVisionHandler.Handle.Matcher.Run(_viewer.CurrentImage, VIEW_MODE);
//   if (res.Found) { lbl_result.Text = FormatAlignResult(res); } else { lbl_result.Text = "검출 실패"; }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tray/Bottom XAML 에 [ROI 표시][에지 표시] 체크박스 추가 (검사 결과 패널)</name>
  <read_first>
    - WPF_Example/Custom/UI/TrayVisionView.xaml (Row4 검사 결과 GroupBox L133~147)
    - WPF_Example/Custom/UI/BottomVisionView.xaml (Row4 검사 결과 GroupBox L136~150)
  </read_first>
  <files>WPF_Example/Custom/UI/TrayVisionView.xaml, WPF_Example/Custom/UI/BottomVisionView.xaml</files>
  <action>
    각 뷰 Row4 "검사 결과" GroupBox 의 StackPanel 안(btn_run 아래, lbl_result 위 또는 아래)에 체크박스 2개를 추가한다. airspace 준수 — 좌측 컨트롤 패널(HWND 밖)이라 안전. 기본 IsChecked="True"(오버레이 기본 ON, MainResultViewerControl 기본 게이트 true 와 일치). 주석 `<!--260625 hbk Phase 61.1 시각화 토글-->`.

    두 뷰 동일 (StackPanel 컨텍스트, 핸들러명 동일):
    ```
    <!--260625 hbk Phase 61.1 시각화 토글 (airspace-safe: 좌측 패널)-->
    <CheckBox x:Name="chk_showRoi" Content="ROI 표시" IsChecked="True" Margin="0,8,0,0"
              Checked="ShowRoiCheckBox_Changed" Unchecked="ShowRoiCheckBox_Changed"/>
    <CheckBox x:Name="chk_showEdge" Content="에지 표시" IsChecked="True" Margin="0,4,0,0"
              Checked="ShowEdgeCheckBox_Changed" Unchecked="ShowEdgeCheckBox_Changed"/>
    ```
    기존 btn_run(Click="RunButton_Click")·lbl_result 는 무변경.
  </action>
  <verify>
    <automated>grep -c "chk_showRoi\|chk_showEdge\|ShowRoiCheckBox_Changed\|ShowEdgeCheckBox_Changed" WPF_Example/Custom/UI/TrayVisionView.xaml WPF_Example/Custom/UI/BottomVisionView.xaml</automated>
  </verify>
  <acceptance_criteria>
    - 두 XAML 각각: `grep -n "chk_showRoi\|chk_showEdge"` 2개 CheckBox x:Name 존재
    - 두 XAML 각각: `grep -n "ShowRoiCheckBox_Changed\|ShowEdgeCheckBox_Changed"` 가 Checked+Unchecked 4개 바인딩 존재
    - 두 XAML 각각: `IsChecked="True"` 가 체크박스 2개에 존재 (기본 ON)
    - 두 XAML 각각: 기존 `grep -n "RunButton_Click"` 1건 잔존 (검사 버튼 무변경)
    - 주석 `grep "260625 hbk Phase 61.1"` 각 XAML 에 (Plan 02 로더 주석 외에) 시각화 토글 주석 추가 존재
  </acceptance_criteria>
  <done>두 뷰 검사 결과 패널에 [ROI 표시][에지 표시] 체크박스(기본 ON)가 추가되고 기존 검사 버튼/결과 라벨은 무변경.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tray/Bottom RunButton_Click 시각화 호출 + 체크박스 토글 핸들러 + 변환 헬퍼</name>
  <read_first>
    - WPF_Example/Custom/UI/TrayVisionView.xaml.cs (RunButton_Click L219~242, using 블록)
    - WPF_Example/Custom/UI/BottomVisionView.xaml.cs (RunButton_Click L231~254, using 블록)
    - WPF_Example/Custom/EthernetVision/AlignResult.cs (Plan 01 신규 필드)
    - WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs (EdgeInspectionOverlay/EdgeInspectionPoint)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (L485~609 DetectedOriginRow/Col, LastFindSucceeded, DatumName)
  </read_first>
  <files>WPF_Example/Custom/UI/TrayVisionView.xaml.cs, WPF_Example/Custom/UI/BottomVisionView.xaml.cs</files>
  <action>
    두 뷰에 동일 구조의 시각화 배선을 추가한다. 주석 `//260625 hbk Phase 61.1`. 삼항 `?:` 금지(if-else), 헝가리언, C# 7.2, 함수 30~40줄, try-catch(시각화 실패해도 throw 금지 — 결과 텍스트는 유지).

    using 추가(없으면): `using System.Collections.Generic;`, `using ReringProject.Sequence;`(DatumConfig namespace 확인 후 — DatumConfig 의 실제 namespace 를 read_first 로 확인하여 정확히 추가). EdgeInspectionOverlay 는 이미 `using ReringProject.Halcon.Models;` 존재.

    (A) RunButton_Click 확장 — 기존 본문은 보존하고, res.Found 분기 내부 lbl_result 갱신 직후에 시각화 호출을 추가 (기존 try-catch 블록 안):
    ```
    if (res.Found) {
        lbl_result.Text = FormatAlignResult(res);
        ApplyAlignVisualization(res);          //260625 hbk Phase 61.1 검출 시각화
    }
    else {
        lbl_result.Text = "검출 실패";
        ClearAlignVisualization();             //260625 hbk Phase 61.1 이전 오버레이 제거
    }
    ```
    (Run 시그니처/기존 흐름은 무변경 — 호출 2줄만 추가.)

    (B) 신규 private 헬퍼 ApplyAlignVisualization(AlignResult res) (try-catch, 30~40줄 — 필요 시 3개 작은 헬퍼로 분리):
    - if (_viewer == null) return;
    - if (!res.HasDetection) { ClearAlignVisualization(); return; }
    - 1) 검출 십자: DatumConfig 임시 객체 생성 → DetectedOriginRow=res.DetectedCenterRow; DetectedOriginCol=res.DetectedCenterCol; LastFindSucceeded=true; DatumName="Align"; → _viewer.SetDatumFindResultOverlay(dc);
         (중심 십자. 두 패턴 개별 십자가 필요하면 추가 가능하나 D-02 는 "검출 위치(pose 중심 십자)" — 중심 1개로 충족. discretion.)
    - 2) 보정 ROI 박스: List<double[]> datumRects = res.DetectedRoiBoxes (null 가드 → 빈 리스트); _viewer.SetResultRoiOverlays(null, datumRects);
         (datumRects 채널 = orange = _datumOverlayVisible 게이트 = [ROI 표시] 체크박스. measRects=null 로 두어 green 채널 미사용 → 에지 토글과 게이트 분리.)
    - 3) 에지 contour: List<EdgeInspectionOverlay> edgeOverlays = BuildEdgeOverlays(res); _viewer.SetInspectionOverlays(edgeOverlays);
         (_measurementOverlayVisible 게이트 = [에지 표시] 체크박스.)

    (C) 신규 private 헬퍼 List<EdgeInspectionOverlay> BuildEdgeOverlays(AlignResult res):
    - var list = new List<EdgeInspectionOverlay>();
    - if (res.EdgeContourRows == null || res.EdgeContourCols == null) return list;
    - int n = Math.Min(res.EdgeContourRows.Count, res.EdgeContourCols.Count); if (n == 0) return list;
    - var ov = new EdgeInspectionOverlay(); ov.RoiId = "AlignEdge";
    - for (int i = 0; i < n; i++) { var p = new EdgeInspectionPoint(); p.Row = res.EdgeContourRows[i]; p.Column = res.EdgeContourCols[i]; ov.Points.Add(p); }
    - list.Add(ov); return list;
    (EdgeInspectionOverlay 의 Points 가 점 마커로 렌더됨 — HalconDisplayService 기존 에지 점 렌더 재사용.)

    (D) 신규 private 헬퍼 ClearAlignVisualization():
    - if (_viewer == null) return;
    - try { _viewer.ClearDatumFindResultOverlay(); _viewer.ClearResultRoiOverlays(); _viewer.SetInspectionOverlays(null); } catch { }

    (E) 체크박스 토글 핸들러 (sender 무시, chk_*.IsChecked 사용 — bool? → == true 비교, 삼항 금지):
    - ShowRoiCheckBox_Changed(object sender, RoutedEventArgs e):
        if (_viewer == null) return;
        bool bShow = (chk_showRoi.IsChecked == true);
        try { _viewer.SetDatumOverlayVisible(bShow); } catch { }     // 보정 ROI(orange) = datum 게이트
    - ShowEdgeCheckBox_Changed(object sender, RoutedEventArgs e):
        if (_viewer == null) return;
        bool bShow = (chk_showEdge.IsChecked == true);
        try { _viewer.SetMeasurementOverlayVisible(bShow); } catch { }  // 에지 = measurement 게이트

    토글 게이트 매핑 근거(주석으로 명시): MainResultViewerControl.Render() 에서 datumRects(보정 ROI orange)는 _datumOverlayVisible, _inspectionOverlays(에지)는 _measurementOverlayVisible 게이트 → 두 체크박스가 독립 토글. (검출 십자는 게이트 없음 — 항상 표시, Clear 로만 제거.)

    회귀 0: 기존 GrabButton_Click/TeachButton_Click/LiveButton_Click/StopButton_Click + Plan 02 로더 핸들러 + (Bottom) Cal* 핸들러 무수정. FormatAlignResult 무수정.
    Bottom 도 동일 — Bottom 의 추가 Theta 결과는 lbl_result(FormatAlignResult)에서 처리되며 시각화는 Tray 와 동일(검출 십자/ROI/에지).
  </action>
  <verify>
    <automated>grep -n "ApplyAlignVisualization\|BuildEdgeOverlays\|ClearAlignVisualization\|ShowRoiCheckBox_Changed\|ShowEdgeCheckBox_Changed\|SetDatumFindResultOverlay" WPF_Example/Custom/UI/TrayVisionView.xaml.cs WPF_Example/Custom/UI/BottomVisionView.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - 두 .cs 각각: `grep -n "private void ApplyAlignVisualization\|private List<.*EdgeInspectionOverlay> BuildEdgeOverlays\|private void ClearAlignVisualization"` 3개 헬퍼 정의 존재
    - 두 .cs 각각: `grep -n "ApplyAlignVisualization(res)"` 가 RunButton_Click 의 res.Found 분기에서 호출 1건
    - 두 .cs 각각: `grep -n "_viewer.SetDatumFindResultOverlay\|_viewer.SetResultRoiOverlays(null,\|_viewer.SetInspectionOverlays"` 3개 시각화 호출 존재
    - 두 .cs 각각: `grep -n "private void ShowRoiCheckBox_Changed\|private void ShowEdgeCheckBox_Changed"` 2개 토글 핸들러 정의
    - 두 .cs 각각: `grep -n "_viewer.SetDatumOverlayVisible\|_viewer.SetMeasurementOverlayVisible"` 토글 호출 각 1건
    - 두 .cs 각각: `grep -n "res.DetectedCenterRow\|res.DetectedRoiBoxes\|res.EdgeContourRows" ` 신규 필드 소비 존재
    - 두 .cs 각각: `grep -n "EdgeInspectionPoint\|new EdgeInspectionOverlay"` 에지 변환 존재
    - 두 .cs 각각: 기존 `grep -n "private void GrabButton_Click\|private void TeachButton_Click\|FormatAlignResult"` 잔존 (무수정)
    - 삼항 검증: `grep -nE "\?[^.?]" TrayVisionView.xaml.cs BottomVisionView.xaml.cs` 신규 라인에 `cond ? a : b` 0건 (IsChecked == true 비교 사용)
    - DatumConfig using/네임스페이스: 컴파일 의존 — `grep -n "DatumConfig" TrayVisionView.xaml.cs BottomVisionView.xaml.cs` 가 존재하고 해당 namespace using 추가됨 (빌드는 Plan 04 게이트에서 확정)
    - 주석 `grep "//260625 hbk Phase 61.1"` 각 .cs 1건 이상
  </acceptance_criteria>
  <done>두 뷰 Run 성공 시 검출 십자(SetDatumFindResultOverlay) + 보정 ROI 박스(SetResultRoiOverlays datumRects) + 에지(SetInspectionOverlays) 표시, [ROI 표시]→SetDatumOverlayVisible / [에지 표시]→SetMeasurementOverlayVisible 토글, 검출 실패 시 ClearAlignVisualization, 기존 핸들러 무수정.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AlignResult(인-프로세스) → 뷰 시각화 | Plan 01 서비스가 반환한 검출 좌표/contour 를 뷰가 오버레이로 변환. 인-프로세스 데이터, 신규 외부 경계 없음. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61.1-05 | Denial of Service | ApplyAlignVisualization / BuildEdgeOverlays / 토글 핸들러 | mitigate | 모든 시각화 변환·뷰어 호출을 try-catch 로 감싸 null/대용량 contour/HALCON 렌더 오류 시 throw 없이 결과 텍스트 유지(UI 무중단). EdgeContour 는 Plan 01 에서 이미 400점 다운샘플 → 마커 렌더 부하 한정. |
| T-61.1-06 | Information Disclosure | 검출 좌표 화면 표시 | accept | 로컬 검사 화면에 좌표/십자 표시뿐. PII/비밀 없음, 네트워크 송신 없음. |
</threat_model>

<verification>
- Run 성공 → 검출 십자 + 보정 ROI 박스 + 에지 표시 (세 오버레이 API 호출).
- [ROI 표시]→SetDatumOverlayVisible, [에지 표시]→SetMeasurementOverlayVisible 독립 토글 (Render 게이트 매핑 근거).
- 체크박스는 좌측 패널(airspace-safe), 오버레이는 HALCON 창 내부.
- MainResultViewerControl 무수정 (consume only) — 호출만 추가.
- 기존 Grab/Teach/Run/Cal/로더 핸들러 무수정.
- 최종 빌드 검증은 Plan 04 의 msbuild 게이트에서 수행.
</verification>

<success_criteria>
- 매칭 결과 시각화: 검출 위치 + 보정 ROI + 검출 에지, View on/off 체크박스 토글 (MainResultViewerControl 기존 오버레이 API 재사용).
- ROADMAP Success Criteria #2 충족.
</success_criteria>

<output>
After completion, create `.planning/phases/61.1-align-offline-loader-result-viz-2026-06-25/61.1-03-SUMMARY.md`
</output>
