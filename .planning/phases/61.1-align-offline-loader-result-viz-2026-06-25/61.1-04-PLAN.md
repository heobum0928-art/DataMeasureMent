---
phase: 61.1-align-offline-loader-result-viz-2026-06-25
plan: 04
type: execute
wave: 3
depends_on: ["01", "02", "03"]
files_modified: []
autonomous: true
requirements: [AV-08]
must_haves:
  truths:
    - "msbuild Debug/x64 빌드가 에러 0 으로 PASS 한다"
    - "anti-goal 무수정 파일(MainView/MainResultViewerControl/EthernetAlignCamera/PickerCenterCalibrationService/Grabber/TcpServer/ShotConfig/InspectionSequence)의 git diff 가 0 줄이다"
    - "Phase 61.1 의 4 Success Criteria 가 코드 증거(grep/빌드)로 추적된다"
  artifacts:
    - path: ".planning/phases/61.1-align-offline-loader-result-viz-2026-06-25/61.1-04-SUMMARY.md"
      provides: "빌드 PASS 로그 + anti-goal git diff 증명 + Success Criteria 추적표"
      contains: "anti-goal"
  key_links:
    - from: "msbuild Debug/x64"
      to: "DatumMeasurement.exe"
      via: "전체 빌드 에러 0"
      pattern: "Build succeeded"
---

<objective>
Phase 61.1 의 모든 코드 변경(Plan 01~03)을 통합 빌드로 검증하고, D-04 anti-goal(무수정 파일 git diff 0)을 git 으로 증명하며, ROADMAP 4 Success Criteria 를 코드 증거로 추적한다. Phase 61 의 빌드 게이트 패턴 — PowerShell msbuild Debug/x64.

Purpose: 회귀 0 보장(빌드 + anti-goal). 검증 게이트 plan (코드 무수정 — 검증/증명만).
Output: 61.1-04-SUMMARY.md 에 빌드 PASS 로그 요약 + anti-goal git diff 증명 + Success Criteria 추적표.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61.1-align-offline-loader-result-viz-2026-06-25/61.1-CONTEXT.md

<interfaces>
<!-- 빌드 게이트 (Phase 61 패턴 — PowerShell 로 실행. Git Bash 는 /p: 인자 깨짐 주의): -->
<!-- 작업 디렉토리: C:\Info\Project\DataMeasurement -->
<!-- msbuild "WPF_Example\DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build -->

<!-- D-04 anti-goal 무수정 파일 목록 (git diff 0 줄이어야 함): -->
<!--   MainView (실제 경로 확인: WPF_Example/UI/MainView.xaml(.cs) 또는 Custom/UI/MainView.xaml(.cs)) -->
<!--   WPF_Example/UI/ContentItem/MainResultViewerControl.xaml(.cs)  (consume only) -->
<!--   WPF_Example/Custom/EthernetVision/EthernetAlignCamera.cs -->
<!--   WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs -->
<!--   WPF_Example/Custom/Sequence/**  (Grabber 검사 경로, InspectionSequence.cs 포함) -->
<!--   WPF_Example/Device/**  (Grabber 카메라) -->
<!--   WPF_Example/SystemHandler.cs, WPF_Example/Custom/SystemHandler.cs -->
<!--   WPF_Example/TcpServer/**, WPF_Example/Custom/TcpServer/** -->
<!--   ShotConfig.cs -->
<!-- 허용 수정(diff 존재 정상): AlignResult.cs, AlignShapeMatchService.cs, TrayVisionView.xaml(.cs), BottomVisionView.xaml(.cs) -->

<!-- ROADMAP Success Criteria (4) — .planning/ROADMAP.md L646~650: -->
<!--   #1 Tray/Bottom 로더 [폴더 열기]+[이전][다음], 로드 이미지=티칭/Run/캘 입력, 기존 Grab/폴백 유지 -->
<!--   #2 매칭 결과 시각화(검출+보정ROI+에지) + View on/off 체크박스 (MainResultViewerControl 기존 API 재사용) -->
<!--   #3 AlignShapeMatchService 검출 좌표·에지 반환 확장 (기존 Run/AlignResult 하위호환) -->
<!--   #4 msbuild Debug/x64 PASS, MainView/Grabber/검사 경로 회귀 0 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: msbuild Debug/x64 통합 빌드 게이트 (에러 0)</name>
  <read_first>
    - WPF_Example/DatumMeasurement.csproj (빌드 대상 — AlignResult/AlignShapeMatchService/TrayVisionView/BottomVisionView 등록 확인, 신규 파일 0 이므로 csproj 무수정 예상)
  </read_first>
  <files>(코드 무수정 — 빌드 검증만)</files>
  <action>
    PowerShell 로 Debug/x64 전체 빌드를 실행한다 (Phase 61 빌드 게이트 패턴). 작업 디렉토리 = C:\Info\Project\DataMeasurement. Bash 로 실행 시 /p: 인자가 깨질 수 있으므로 powershell -Command 형태로 호출.

    실행:
    powershell -Command "msbuild 'WPF_Example\DatumMeasurement.csproj' /p:Configuration=Debug /p:Platform=x64 /t:Build"
    (msbuild PATH 미설정 시 Phase 61 SUMMARY 가 사용한 MSBuild 전체 경로 사용 — VS BuildTools 경로.)

    결과 판정:
    - "Build succeeded" + "0 Error(s)" 면 PASS.
    - 에러가 있으면 Plan 01~03 의 수정 파일 범위 내에서만 수정(anti-goal 파일은 절대 손대지 말 것). 흔한 후보:
      * AlignResult 신규 List 필드 → using/타입 미스 → AlignResult.cs.
      * DatumConfig namespace using 누락(Plan 03) → TrayVisionView/BottomVisionView.xaml.cs using 추가.
      * EdgeInspectionOverlay/EdgeInspectionPoint → using ReringProject.Halcon.Models 확인.
      * HALCON contour 연산자 인자 수 불일치(GetShapeModelContours/AffineTransContourXld/GetContourXld) → AlignShapeMatchService.cs.
    - 빌드 로그의 에러/경고 수를 SUMMARY 에 기록. 신규 warning 0 목표(불가피 시 사유 기록).

    빌드 산출물(bin/) 은 gitignore 라 커밋 대상 아님 — 빌드 성공 사실만 SUMMARY 에 기록.
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && powershell -Command "msbuild 'WPF_Example\DatumMeasurement.csproj' /p:Configuration=Debug /p:Platform=x64 /t:Build /nologo /verbosity:minimal" 2>&1 | grep -iE "Build succeeded|error CS|error MSB"</automated>
  </verify>
  <acceptance_criteria>
    - 빌드 출력에 "Build succeeded" 포함 + error CS/error MSB 0건
    - 빌드 에러 발생 시 수정은 허용 파일(AlignResult.cs/AlignShapeMatchService.cs/TrayVisionView.xaml(.cs)/BottomVisionView.xaml(.cs))로 한정 — anti-goal 파일 0 변경
  </acceptance_criteria>
  <done>msbuild Debug/x64 가 에러 0 으로 PASS (ROADMAP Success Criteria #4 빌드 부분 충족).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: anti-goal git diff 증명 + Success Criteria 추적표 작성</name>
  <read_first>
    - .planning/phases/61.1-align-offline-loader-result-viz-2026-06-25/61.1-CONTEXT.md (D-04 anti-goal 무수정 목록)
    - .planning/ROADMAP.md (L643~652 Phase 61.1 4 Success Criteria)
  </read_first>
  <files>.planning/phases/61.1-align-offline-loader-result-viz-2026-06-25/61.1-04-SUMMARY.md</files>
  <action>
    git 으로 anti-goal 무수정 파일이 0 줄 변경임을 증명하고, 4 Success Criteria 를 코드 증거로 추적하여 SUMMARY 에 기록한다. 코드 파일 무수정 — 검증/문서화만.

    (A) anti-goal git diff 증명:
    - 먼저 MainView 실제 경로를 확인 (glob WPF_Example/**/MainView.xaml.cs — 단일 경로 확정).
    - Phase 61.1 변경 파일 목록 수집: `git diff --stat <phase시작SHA>..HEAD -- WPF_Example/` (phase 시작 SHA = Plan 01 실행 직전 커밋. STATE 의 직전 HEAD 또는 git log 로 확인).
    - 변경 파일이 정확히 다음 6종에 한정되는지 확인 (csproj 는 신규파일 0 → 무변경 예상):
      허용: Custom/EthernetVision/AlignResult.cs, Custom/EthernetVision/AlignShapeMatchService.cs,
            Custom/UI/TrayVisionView.xaml, Custom/UI/TrayVisionView.xaml.cs,
            Custom/UI/BottomVisionView.xaml, Custom/UI/BottomVisionView.xaml.cs.
    - anti-goal 파일별 0-diff 확인 (각각 빈 출력 = PASS). phase 시작 SHA 대비:
      `git diff <SHA>..HEAD -- WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs WPF_Example/UI/ContentItem/MainResultViewerControl.xaml`
      `git diff <SHA>..HEAD -- WPF_Example/Custom/EthernetVision/EthernetAlignCamera.cs`
      `git diff <SHA>..HEAD -- WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs`
      `git diff <SHA>..HEAD -- WPF_Example/Custom/Sequence/`   (InspectionSequence.cs/ShotConfig.cs 포함)
      `git diff <SHA>..HEAD -- WPF_Example/Device/`
      `git diff <SHA>..HEAD -- WPF_Example/SystemHandler.cs WPF_Example/Custom/SystemHandler.cs`
      `git diff <SHA>..HEAD -- WPF_Example/TcpServer/ WPF_Example/Custom/TcpServer/`
      `git diff <SHA>..HEAD -- <MainView 실제경로>`
    - 각 경로 diff 가 0 줄(빈 출력)임을 SUMMARY 에 파일별로 명시. 한 줄이라도 있으면 FAIL → 해당 파일 원복(git checkout) 후 빌드 재확인.

    (B) Success Criteria 추적표 — SUMMARY 에 4행 표, 각 행 코드 증거(grep/파일/빌드):
    | # | Success Criteria | 증거 |
    | 1 | 로더 [폴더 열기]+[이전][다음], 로드=입력, Grab/폴백 유지 | Tray/Bottom xaml/.cs grep(OpenFolderButton_Click/Prev/Next/_viewer.LoadImage), 기존 Grab 핸들러 잔존 |
    | 2 | 시각화(검출+보정ROI+에지) + View 토글 | RunButton_Click→ApplyAlignVisualization, SetDatumFindResultOverlay/SetResultRoiOverlays/SetInspectionOverlays, chk_showRoi/chk_showEdge→Set*OverlayVisible |
    | 3 | AlignShapeMatchService 검출좌표·에지 반환(하위호환) | AlignResult 신규 필드 + Run write, 기존 Run 시그니처/6필드 잔존 |
    | 4 | msbuild PASS + 회귀 0 | Task 1 Build succeeded + (A) anti-goal 0-diff |

    (C) SUMMARY 기록 항목: 빌드 결과(에러/경고 수), anti-goal 0-diff 결과(파일별 PASS/FAIL), 허용 변경 파일 목록, Success Criteria 추적표, carry-over(있으면). human UAT(실 이미지 폴더 순회 + 시각화 육안)는 Phase 61 패턴대로 /gsd-verify-work 로 분리됨을 명시 (SIMUL/실 이미지 검증 = 다음 단계).
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && git diff --stat HEAD -- WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs WPF_Example/Custom/EthernetVision/EthernetAlignCamera.cs WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs WPF_Example/TcpServer/ WPF_Example/Device/ WPF_Example/Custom/Sequence/ | cat</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY 에 anti-goal 파일별 0-diff 결과가 명시됨 (MainResultViewerControl/EthernetAlignCamera/PickerCenterCalibrationService/Custom/Sequence/Device/SystemHandler/TcpServer/MainView 각각 0 줄)
    - SUMMARY 의 변경 파일 목록이 허용 6파일(AlignResult/AlignShapeMatchService/Tray·Bottom xaml·cs)에만 한정 (anti-goal 파일 미포함)
    - SUMMARY 에 4행 Success Criteria 추적표 + 각 행 grep/빌드 증거 존재
    - SUMMARY 에 빌드 결과(Build succeeded, 에러/경고 수) 기록
    - `git diff --stat HEAD -- <anti-goal 경로들>` verify 출력이 빈 결과(변경 0) — anti-goal 미수정 확정
  </acceptance_criteria>
  <done>anti-goal 무수정 파일이 git diff 0 줄로 증명되고, 4 Success Criteria 가 코드 증거로 추적되며, SUMMARY 에 빌드 PASS + anti-goal 증명 + 추적표가 기록됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (없음 — 코드 무수정 검증 게이트) | 빌드 실행 + git diff 검증만. 신규 런타임 경계 없음. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61.1-07 | Tampering | anti-goal 무수정 파일(검사/Grabber/TCP 경로) | mitigate | git diff 0-줄 증명으로 의도치 않은 회귀 변경을 게이트. 한 줄이라도 변경 시 FAIL→원복. (이 plan 의 핵심 목적 = anti-goal 강제.) |
</threat_model>

<verification>
- msbuild Debug/x64 PASS (에러 0).
- anti-goal 무수정 파일 git diff 0 줄 (파일별 증명).
- 변경 파일이 허용 6파일에만 한정.
- 4 Success Criteria 코드 증거 추적.
</verification>

<success_criteria>
- msbuild Debug/x64 PASS, MainView/Grabber/검사 경로 회귀 0 (anti-goal git 증명).
- ROADMAP Success Criteria #4 충족 + #1/#2/#3 추적 확인.
</success_criteria>

<output>
After completion, create `.planning/phases/61.1-align-offline-loader-result-viz-2026-06-25/61.1-04-SUMMARY.md`
</output>
