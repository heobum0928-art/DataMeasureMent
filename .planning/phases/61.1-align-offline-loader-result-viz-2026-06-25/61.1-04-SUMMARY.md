---
phase: 61.1-align-offline-loader-result-viz-2026-06-25
plan: "04"
subsystem: EthernetVision / AlignView
tags: [build-gate, anti-goal-proof, integration-verification]
dependency_graph:
  requires: ["61.1-01", "61.1-02", "61.1-03"]
  provides: ["build-gate-pass", "anti-goal-proof", "sc-traceability"]
  affects: []
tech_stack:
  added: []
  patterns: ["msbuild Debug/x64 build gate (PowerShell)", "git diff anti-goal proof"]
key_files:
  created: []
  modified:
    - WPF_Example/Custom/UI/TrayVisionView.xaml.cs
    - WPF_Example/Custom/UI/BottomVisionView.xaml.cs
decisions:
  - "[Rule 1 - Bug] DatumConfig(object owner) 필수 생성자 인수 Plan 03 누락 → new DatumConfig(this) 수정 (허용 파일 내, anti-goal 0-변경)"
metrics:
  duration: "20min"
  completed_date: "2026-06-25"
  tasks_completed: 2
  tasks_total: 2
  files_changed: 2
---

# Phase 61.1 Plan 04: msbuild 빌드 게이트 + anti-goal git 증명 Summary

**One-liner:** msbuild Debug/x64 에러 0 PASS + Phase 61.1 전체 변경이 허용 6파일에만 한정됨을 git diff로 증명, 4 Success Criteria 코드 추적 완료.

---

## Build Gate Result

| 항목 | 결과 |
|------|------|
| 명령 | `msbuild DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /nologo /v:minimal` |
| 결과 | **Build succeeded** |
| 에러 수 | **0** |
| 경고 수 | 6 (기존 baseline — Phase 61.1 신규 경고 없음) |
| 산출물 | `WPF_Example\bin\x64\Debug\DatumMeasurement.exe` 최신화 |

### 기존 baseline 경고 (Phase 61.1 무관)

| 경고 | 위치 | 사유 |
|------|------|------|
| MSB3884 | MinimumRecommendedRules.ruleset | 기존 베이스라인 — ruleset 파일 부재 |
| CS0618 × 3 | TopSequence, BottomSequence, TopInspectionAction | Phase 33 마이그레이션 deprecated 표시 |
| CS0162 | VirtualCamera.cs | SIMUL_MODE 분기 접근불가 코드 |

---

## Anti-Goal git diff 증명

**Phase 61.1 시작 SHA:** `ccf8f33`  
**비교 범위:** `ccf8f33..HEAD`

### 허용 변경 파일 (6종)

| 파일 | 변경 내용 |
|------|-----------|
| `Custom/EthernetVision/AlignResult.cs` | 시각화 필드 추가 (HasDetection/DetectedCenter/ROI/Edge) |
| `Custom/EthernetVision/AlignShapeMatchService.cs` | Run 검출 좌표·ROI·에지 contour 반환 확장 |
| `Custom/UI/TrayVisionView.xaml` | 오프라인 로더 버튼 3개 + 체크박스 2개 추가 |
| `Custom/UI/TrayVisionView.xaml.cs` | 로더 핸들러 + 시각화 배선 추가 |
| `Custom/UI/BottomVisionView.xaml` | 오프라인 로더 버튼 3개 + 체크박스 2개 추가 |
| `Custom/UI/BottomVisionView.xaml.cs` | 로더 핸들러 + 시각화 배선 추가 |

### Anti-Goal 무수정 파일 (0-diff PASS)

| 파일/경로 | git diff 출력 | 결과 |
|-----------|--------------|------|
| `UI/ContentItem/MainResultViewerControl.xaml(.cs)` | (빈 출력) | **PASS** |
| `Custom/EthernetVision/EthernetAlignCamera.cs` | (빈 출력) | **PASS** |
| `Custom/EthernetVision/PickerCenterCalibrationService.cs` | (빈 출력) | **PASS** |
| `Custom/Sequence/**` (InspectionSequence/ShotConfig 포함) | (빈 출력) | **PASS** |
| `Device/**` | (빈 출력) | **PASS** |
| `SystemHandler.cs` / `Custom/SystemHandler.cs` | (빈 출력) | **PASS** |
| `TcpServer/**` / `Custom/TcpServer/**` | (빈 출력) | **PASS** |
| `UI/MainView.xaml(.cs)` / `Custom/UI/MainView.xaml(.cs)` | (빈 출력) | **PASS** |

모든 anti-goal 파일 git diff = 0줄. D-04 제약 전항목 충족.

---

## Success Criteria 추적표

| # | Success Criteria | 코드 증거 |
|---|-----------------|-----------|
| 1 | Tray/Bottom 뷰 툴바에 [폴더 열기] + [이전][다음] — 로드 이미지를 티칭/Run/캘 입력으로 사용, 기존 Grab/폴백 유지 | `TrayVisionView.xaml.cs:470` `OpenFolderButton_Click` + `PrevImageButton_Click:514` + `NextImageButton_Click:530`; `_viewer.LoadImage(path)` 호출; `GrabButton_Click:75`/`LiveButton_Click:100`/`StopButton_Click:120` 잔존 확인. Bottom 동일 패턴 (`624/668/684`). |
| 2 | 매칭 결과 시각화: 검출 위치 + 보정 ROI + 검출 에지, View on/off 체크박스 토글 (MainResultViewerControl 기존 오버레이 API 재사용) | `ApplyAlignVisualization:296` → `SetDatumFindResultOverlay:312` + `SetResultRoiOverlays:324` + `SetInspectionOverlays:333`; `chk_showRoi(XAML:152)→ShowRoiCheckBox_Changed:258`→`SetDatumOverlayVisible:265`; `chk_showEdge(XAML:154)→ShowEdgeCheckBox_Changed:272`→`SetMeasurementOverlayVisible:279`. |
| 3 | AlignShapeMatchService 검출 좌표·에지 반환 확장, 기존 Run/AlignResult 계약 하위호환 | `AlignResult.cs`: 기존 `Found/Score/OffsetXmm/OffsetYmm/ThetaDeg/HasTheta` 보존 + 신규 `HasDetection/DetectedCenterRow/DetectedCenterCol/DetectedRoiBoxes` 추가. `AlignShapeMatchService.Run` 시그니처 `public AlignResult Run(HImage img, EEthernetVisionMode mode)` 불변. |
| 4 | msbuild Debug/x64 PASS, MainView/Grabber/검사 경로 회귀 0 | Build succeeded 에러 0 (위 Build Gate Result 참조) + anti-goal 0-diff PASS 전항목 (위 anti-goal 표 참조). |

---

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] DatumConfig(object owner) 필수 생성자 인수 누락**

- **Found during:** Task 1 (msbuild 빌드 게이트)
- **Issue:** Plan 03에서 생성된 `TrayVisionView.xaml.cs:307`, `BottomVisionView.xaml.cs:319` 에서 `new DatumConfig()` 호출 — `DatumConfig` 생성자는 `public DatumConfig(object owner)` 만 존재하여 `error CS7036` 발생
- **Fix:** `new DatumConfig(this)` 로 수정 (허용 파일 내, anti-goal 파일 0 변경)
- **Files modified:** `TrayVisionView.xaml.cs`, `BottomVisionView.xaml.cs`
- **Commit:** `80b4ea8`

---

## Human UAT (별도 단계)

실 Align 이미지 폴더 순회 + 시각화 육안 검증은 Phase 61.1 패턴대로 `/gsd-verify-work 61.1` 로 분리.  
검증 항목: [폴더 열기] → 이미지 로드 → [이전]/[다음] 순회, Run → 검출 십자/보정 ROI/에지 시각화, 체크박스 토글 on/off, 기존 Grab/폴백 동작 회귀 확인.

---

## Self-Check: PASSED

- [x] `WPF_Example\bin\x64\Debug\DatumMeasurement.exe` 최신화 (Build succeeded 확인)
- [x] 커밋 `80b4ea8` 존재 (git log 확인)
- [x] anti-goal 전파일 0-diff (위 표 참조)
- [x] 변경 파일 6종 화이트리스트 내 한정
- [x] 4 Success Criteria 추적표 완성
