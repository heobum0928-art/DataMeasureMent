---
status: partial
phase: 61.1-align-offline-loader-result-viz-2026-06-25
source: [61.1-VERIFICATION.md, 61.1-REVIEW.md]
started: 2026-06-25
updated: 2026-06-25
---

## Current Test

[2차 실측: 에지 polyline 대각선 버그 발견 → F4(XLD object 직접 disp) 적용 → 재실측 대기(앱 재빌드/실행)]

## Tests

### 1. Offline 이미지 로더 — 폴더 열기 / 이전 / 다음
expected: Tray/Bottom 뷰에서 [폴더 열기] → 목록 로드, [◀이전][다음▶] 순회, lbl_loaderStatus 표시.
result: PASS (2026-06-25 사용자 실측)

### 2. 결과 시각화 — 검출 윤곽(XLD object) + 보정 ROI
expected: 검사(Run) 후 검출 위치 십자는 표시 안 하고(F1), 검출 윤곽을 두 패턴 contour XLD object 그대로 녹색으로 표시(F4). 패턴1↔패턴2 사이를 잇는 대각선이 없어야 함. 보정 ROI 박스는 실제 티칭 ROI 크기로 표시(F2, 60px 고정 아님).
result: PARTIAL → **F4 재설계 적용, 재실측 대기**. 2차 실측서 polyline 방식이 패턴1 끝점→패턴2 시작점을 대각선으로 잘못 연결하는 버그 발견 → 점 추출/polyline 폐기, 검출 contour XLD 를 concat_obj 로 합쳐 window.DispObj 직접 표시(대각선 원천 차단 + 다운샘플 곡선손상 제거). (F2 는 재티칭해야 ROI len 저장 — 구 레시피는 60px 폴백)

### 3. ROI/에지 체크박스 독립 토글
expected: [ROI 표시]/[에지 표시] 각각 독립 on/off.
result: PASS (2026-06-25 "에지표시 보정됨")

### 4. SIMUL 모드 Grab = 이미지 로드 (F3)
expected: SIMUL 빌드에서 Grab 버튼 클릭 시 파일 선택 다이얼로그로 이미지 1개 로드. (실 빌드는 기존 Camera.Grab.)
result: 보완 적용, 재실측 대기 (F3 #if SIMUL_MODE VistaOpenFileDialog)

### 5. 다중 이미지로 offset/theta 변화
expected: 다른 이미지 로드 후 Run → offset/theta 변화.
result: PASS (2026-06-25 사용자 실측)

## Summary

total: 5
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0
partial: 2

## Gaps

### 보완 적용 완료 (2026-06-25, 재실측 대기)
- **F1 (Test 2)**: 검출 십자(SetDatumFindResultOverlay) 제거 + 에지를 contour 로 표시(기존 FAI 회귀 0). commit d3d17de.
- **F2 (Test 2)**: 보정 ROI 박스 60px 고정 → 실제 티칭 ROI 크기(AlignRefPose Roi*Len 저장/로드). **재티칭 필요**(구 .json 엔 len 없음 → 60px 폴백).
- **F3 (Test 4)**: SIMUL_MODE Grab = 파일 선택 다이얼로그 로드(Tray+Bottom). 실 빌드 Camera.Grab 무손상.
- **F4 (Test 2, 2차 실측 후)**: 에지 시각화 "점→polyline" → "XLD object 직접 disp" 재설계. 2차 실측서 polyline 이 패턴1 끝점→패턴2 시작점을 대각선으로 잘못 연결. 수정 = AlignShapeMatchService.Run 이 두 패턴 contour 를 affine_trans_contour_xld + concat_obj 로 단일 XLD 생성 → AlignResult.DetectedContourXld → MainResultViewerControl.SetAlignContourXld(소유권 이전·교체/clear/Dispose 시 HObject.Dispose) → HalconDisplayService.RenderAlignContourXld(window.DispObj). 점 추출/다운샘플/EdgeContourRows·Cols/BuildEdgeOverlays/AlignEdge polyline 분기 전부 제거. **대각선 원천 차단 + 곡선손상 제거**. 검사(MainView) 는 _alignContourXld=null → 분기 미진입 회귀 0.
- 코드리뷰 WR-01/02 (다중 segment contour / catch 공유 누적) 도 앞서 수정 완료(F4 에서 해당 경로 자체 제거).

> 비고: 빌드 Debug/x64 PASS(에러 0). 재실측 시 **Tray/Bottom 재티칭 후** F2 ROI 크기 + F4 에지 윤곽(대각선 無) 확인 권장.
