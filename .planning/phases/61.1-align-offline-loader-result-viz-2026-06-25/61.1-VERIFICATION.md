---
phase: 61.1-align-offline-loader-result-viz-2026-06-25
verified: 2026-06-25T00:00:00Z
status: human_needed
score: 4/4 must-haves verified
overrides_applied: 0
gaps: []
human_verification:
  - test: "Offline 이미지 로더 — 폴더 열기 + 이전/다음 순회 육안 확인"
    expected: "[폴더 열기] 클릭 시 폴더 선택 다이얼로그 표시 → 이미지 로드(뷰어 표시) → [◀ 이전][다음 ▶]로 목록 순회, lbl_loaderStatus 에 'N/total filename' 표시, 첫/마지막 경계에서 메시지 표시"
    why_human: "UI 다이얼로그·이미지 렌더·라벨 갱신은 런타임 앱 실행 없이 코드 grep 으로 완전 검증 불가"
  - test: "Align Run → 시각화 오버레이 표시 (검출 십자 + 보정 ROI + 에지)"
    expected: "Run 성공 시 이미지 위에 검출 midpoint 십자(DatumConfig), 두 패턴 ROI 박스(orange), 에지 contour 점들이 표시됨. WR-01(GetContourXld 다중 contour 누락)로 인해 복잡 모델의 에지가 끊겨 보일 수 있음"
    why_human: "HALCON 렌더 결과(HWindowControlWPF 내부 airspace) 는 화면 육안 확인 필요. WR-01 버그 실제 영향 범위도 실 .shm 파일로만 판단 가능"
  - test: "[ROI 표시] / [에지 표시] 체크박스 토글 동작 확인"
    expected: "체크박스 OFF 시 해당 오버레이 소멸, ON 시 재표시. Tray/Bottom 각각 독립 동작"
    why_human: "SetDatumOverlayVisible / SetMeasurementOverlayVisible 의 MainResultViewerControl 내 실제 렌더 게이트 작동은 런타임 확인 필요"
  - test: "기존 Grab/Live/Stop 동작 회귀 없음 확인"
    expected: "Grab 클릭 시 D:\\align_test.bmp 폴백(이더넷 카메라 미연결 상태) 정상 로드, lbl_loaderStatus 와 충돌 없음. Tray/Bottom 뷰 양쪽 확인"
    why_human: "SIMUL_MODE 에서 실제 EthernetAlignCamera 폴백 경로 동작은 앱 실행 필요"
  - test: "WR-02 실제 영향 평가 — 두 번째 contour 실패 시 첫 번째 누적 삭제 여부"
    expected: "shmPath2 TryExtractDetectedContour 실패 시 catch 블록이 result.EdgeContourRows/Cols.Clear() 호출 → shmPath1 에서 누적된 점 전부 삭제. 에러 로그 '[ALIGN_SVC] contour[2] 추출 실패' 는 출력되지만 화면에 에지가 전혀 안 보임. 수정 전 발생 시나리오: 두 .shm 중 하나만 손상된 경우"
    why_human: "정상 레시피에서 shmPath2 가 항상 유효하면 WR-02 미발현 — 실 .shm 손상 케이스로만 재현 가능"
---

# Phase 61.1: Align Offline 이미지 로더 + 결과 시각화 Verification Report

**Phase Goal:** Phase 61 갭 보완 — (1) Offline 다중 이미지 로더(폴더+이전/다음), (2) Align 매칭 결과 시각화(검출+보정 ROI+에지 + View on/off 토글). 신규 검사 로직 0.
**Verified:** 2026-06-25
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Tray/Bottom 뷰 툴바에 [폴더 열기] + [◀ 이전][다음 ▶] — 로드 이미지를 티칭/Run/캘 입력으로 사용, 기존 Grab/폴백 유지 | ✓ VERIFIED | TrayVisionView.xaml L105/107/109: btn_openFolder/PrevImage/NextImage. TrayVisionView.xaml.cs L470/514/530: OpenFolderButton_Click/PrevImageButton_Click/NextImageButton_Click. LoadCurrentLoaderImage() L551→_viewer.LoadImage(path). 기존 GrabButton_Click(L75)/LiveButton_Click(L100)/StopButton_Click(L120) 잔존. BottomVisionView 동일 패턴 17건 확인 |
| 2 | 매칭 결과 시각화: 검출 위치 + 보정 ROI + 검출 에지, View on/off 체크박스 토글 (MainResultViewerControl 기존 오버레이 API 재사용) | ✓ VERIFIED | ApplyAlignVisualization(L296): SetDatumFindResultOverlay(L312)+SetResultRoiOverlays(L324)+SetInspectionOverlays(L333). 체크박스 chk_showRoi(XAML L152)/chk_showEdge(L154) → ShowRoiCheckBox_Changed(L258)→SetDatumOverlayVisible, ShowEdgeCheckBox_Changed(L272)→SetMeasurementOverlayVisible. ClearAlignVisualization(L370) 3개 API 일괄 제거. MainResultViewerControl 무수정 |
| 3 | AlignShapeMatchService 검출 좌표·에지 반환 확장, 기존 Run/AlignResult 계약 하위호환 | ✓ VERIFIED | AlignResult.cs: 기존 6필드(Found/Score/OffsetXmm/OffsetYmm/ThetaDeg/HasTheta) 완전 보존 + 신규 10필드(HasDetection/DetectedRow1/Col1/Row2/Col2/CenterRow/Col/DetectedRoiBoxes/EdgeContourRows/Cols) 추가. AlignShapeMatchService.Run 시그니처 L328 public AlignResult Run(HImage img, EEthernetVisionMode mode) 불변. result.HasDetection=true L436, DetectedRoiBoxes.Add L447/451, TryExtractDetectedContour L455/465 |
| 4 | msbuild Debug/x64 PASS, MainView/Grabber/검사 경로 회귀 0 | ✓ VERIFIED | 61.1-04-SUMMARY: Build succeeded 에러 0, 경고 6건(기존 baseline). git diff ccf8f33..HEAD anti-goal 파일(MainResultViewerControl/EthernetAlignCamera/PickerCenterCalibrationService/Custom/Sequence/Device/SystemHandler/TcpServer/MainView) 전부 0-diff PASS. 변경 파일: 6파일(AlignResult.cs/AlignShapeMatchService.cs/TrayVisionView.xaml(.cs)/BottomVisionView.xaml(.cs))에만 한정 — 직접 git diff 확인으로 증명 |

**Score:** 4/4 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `WPF_Example/Custom/EthernetVision/AlignResult.cs` | 시각화 필드 추가 (기존 6필드 보존) | ✓ VERIFIED | 기존 6필드 무손상 + HasDetection/DetectedRow1/Col1/Row2/Col2/CenterRow/CenterCol/DetectedRoiBoxes/EdgeContourRows/EdgeContourCols 추가. List 필드 선언부 즉시 초기화. 주석 `//260625 hbk Phase 61.1` 존재(L28) |
| `WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs` | Run 내 검출 pose/ROI/에지 산출 | ✓ VERIFIED | DETECTED_ROI_HALF_LEN=60.0(L50), EDGE_CONTOUR_MAX_POINTS=400(L53) const 존재. BuildDetectedRoiBox(L497), TryExtractDetectedContour(L509) 헬퍼 정의. GetShapeModelContours(L526)/VectorAngleToRigid(L530)/AffineTransContourXld(L534)/GetContourXld(L535) HALCON 호출 존재. finally Dispose 전항목(L564~571) |
| `WPF_Example/Custom/UI/TrayVisionView.xaml` | 로더 버튼 3개 + 체크박스 2개 | ✓ VERIFIED | btn_openFolder(L105)/PrevImage(L107)/NextImage(L109), lbl_loaderStatus(L111), chk_showRoi(L152)/chk_showEdge(L154) |
| `WPF_Example/Custom/UI/TrayVisionView.xaml.cs` | 로더 핸들러 + 시각화 배선 | ✓ VERIFIED | OpenFolderButton_Click(L470)/PrevImageButton_Click(L514)/NextImageButton_Click(L530)/LoadCurrentLoaderImage(L551). ApplyAlignVisualization(L296)/BuildEdgeOverlays(L344)/ClearAlignVisualization(L370)/ShowRoiCheckBox_Changed(L258)/ShowEdgeCheckBox_Changed(L272) |
| `WPF_Example/Custom/UI/BottomVisionView.xaml` | 로더 버튼 3개 + 체크박스 2개 | ✓ VERIFIED | btn_openFolder/lbl_loaderStatus/chk_showRoi/chk_showEdge grep 4건 확인 |
| `WPF_Example/Custom/UI/BottomVisionView.xaml.cs` | 로더 핸들러 + 시각화 배선 | ✓ VERIFIED | 로더 핸들러 17건, 시각화 배선 9건 grep 확인. CalAddStep/CalCompute 잔존 |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| OpenFolderButton_Click | _viewer.LoadImage(path) | LoadCurrentLoaderImage() | ✓ WIRED | L507→L551→L563: _viewer.LoadImage(path) 직접 호출 |
| TryExtractDetectedContour (실패) | result.EdgeContourRows/Cols 보존 | catch 블록 outRows.Clear() | ⚠️ WIRED(버그 있음) | WR-02 실제 버그 존재: 실패 시 누적 분 Clear() 발생. 상세는 아래 버그 분석 섹션 참조 |
| AlignShapeMatchService.Run | AlignResult 시각화 필드 | result.HasDetection=true + DetectedRoiBoxes.Add + EdgeContourRows 누적 | ✓ WIRED | L432~L478: 검출 성공 경로에 try-catch 전체 포괄, 시각화 필드 모두 대입 |
| ApplyAlignVisualization | MainResultViewerControl 오버레이 API | SetDatumFindResultOverlay/SetResultRoiOverlays/SetInspectionOverlays | ✓ WIRED | TrayVisionView.xaml.cs L312/L324/L333 |
| chk_showRoi | SetDatumOverlayVisible | ShowRoiCheckBox_Changed | ✓ WIRED | L258~L270 |
| chk_showEdge | SetMeasurementOverlayVisible | ShowEdgeCheckBox_Changed | ✓ WIRED | L272~L284 |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| TrayVisionView.RunButton_Click | AlignResult res | AlignShapeMatchService.Run(img, VIEW_MODE) | Yes — TryFindPose → 검출 좌표 → 시각화 필드 | ✓ FLOWING |
| ApplyAlignVisualization | res.EdgeContourRows | TryExtractDetectedContour → GetContourXld | Yes (단, WR-01 다중 contour 누락 버그 있음) | ⚠️ FLOWING (부분 에지 누락 가능) |
| LoadCurrentLoaderImage | _viewer.CurrentImage | LoadImage(path) → HImage | Yes — 파일 경로 직접 로드 | ✓ FLOWING |

---

### Behavioral Spot-Checks

Step 7b: SKIPPED (HALCON 런타임 / 카메라 하드웨어 의존 — 단독 명령으로 검증 불가)

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|------------|-------------|-------------|--------|----------|
| AV-08 | 61.1-01/02/03/04 | Tray/BottomVisionView 툴바+티칭+결과 패널+HalconViewer 공용 | ✓ SATISFIED | ROADMAP SC 1~4 전항목 코드 검증 완료. Phase 61.1 이 Phase 61 AV-08 보완으로 신설된 범위 — 로더 및 시각화 제공 |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| AlignShapeMatchService.cs | 535 | GetContourXld 단일 호출 — 다중 contour 지원 없음 (WR-01) | ⚠️ Warning | movedContours 에 복수 XLD segment 존재 시 첫 번째 contour 좌표만 추출. 복잡 모델 에지가 끊겨 보임. 단일 segment 모델(현재 Tray/Bottom .shm)에서는 미발현 가능 |
| AlignShapeMatchService.cs | 560~561 | outRows.Clear(); outCols.Clear() in catch (WR-02) | ⚠️ Warning | shmPath2 실패 시 shmPath1 누적 점 전부 삭제 → 에지 시각화 전체 소멸. 에러 로그와 실제 효과 불일치. 두 .shm 모두 정상이면 미발현 |
| AlignShapeMatchService.cs | 117~118 | BuildJsonPath Replace("_1","") 전체 치환 (WR-03) | ℹ️ Info | 현재 "Tray"/"Bottom" 고정 파일명에서 미발현. 미래 파일명에 _1 포함 시 오동작 위험 |

---

### WR-01 / WR-02 버그 목표 달성 영향 분석

**WR-01 (GetContourXld 다중 contour 누락):**

REVIEW.md 에서 "에지가 끊어 보임" 증상 가능 → SC#2 "검출 에지" 시각화의 시각적 완성도에 영향. 단, 현재 Tray/Bottom .shm 이 단순 윤곽(단일 segment)이면 contours 객체에 XLD 1개만 존재 → 실제 미발현 가능. 코드 grep 으로 실 .shm 구조를 확인할 수 없으므로 **런타임 UAT 필요**.

**WR-02 (catch outRows.Clear() 누적 삭제):**

두 패턴 모두 .shm 정상이면 TryExtractDetectedContour 가 모두 성공 → catch 미진입 → WR-02 미발현. 레시피 정상 운영 환경에서 데모/POC 에는 즉각 영향 없음. 단 .shm 하나라도 읽기 실패하면 에지 전체 소멸 + 오해 유발 로그 → **향후 핫픽스 필요**. 목표 달성을 즉시 block 하지는 않음(정상 .shm 기준).

두 버그 모두 must_have(SC#2 에지 시각화)를 FAILED 로 전환할 근거가 확실하지 않음 — 실 .shm 구조와 실행 경로에 따라 미발현 가능. human_verification 으로 분류.

---

### Human Verification Required

**1. Offline 이미지 로더 순회 동작**

**Test:** Tray 뷰 → [폴더 열기] 클릭 → 폴더 선택 → 이미지 로드 확인 → [◀ 이전][다음 ▶] 순회
**Expected:** lbl_loaderStatus 에 "1/16 filename.bmp" 형식 표시, 이미지 뷰어에 각 이미지 표시. 첫/마지막 경계 메시지 표시.
**Why human:** UI 다이얼로그·뷰어 렌더 확인 불가

**2. Run 후 시각화 오버레이 표시 (에지 관련 WR-01 확인 포함)**

**Test:** Offline 이미지 로드 → [Teach] → [Run] → 이미지 위 오버레이 확인
**Expected:** 검출 midpoint 십자 + 두 패턴 ROI 박스(주황) + 에지 contour 점. 에지가 완전하게 표시되면 WR-01 미발현; 끊기면 WR-01 발현 → 핫픽스 필요
**Why human:** HALCON 창 내부 렌더(airspace) + 실 .shm 구조 확인 필요

**3. 체크박스 토글 on/off 동작**

**Test:** [ROI 표시] 체크박스 OFF → ROI 박스 소멸 확인. [에지 표시] 체크박스 OFF → 에지 소멸 확인
**Expected:** 각 체크박스 독립 제어, SetDatumOverlayVisible/SetMeasurementOverlayVisible 실제 동작
**Why human:** MainResultViewerControl 게이트 실제 렌더 동작 확인 필요

**4. 기존 Grab/Live/Stop 회귀 없음**

**Test:** 로더로 이미지 로드 후 [Grab] 클릭
**Expected:** EthernetAlignCamera 폴백(D:\align_test.bmp) 정상 동작, 로더 상태와 간섭 없음
**Why human:** SIMUL_MODE 실제 실행 필요

**5. WR-02 버그 재현 여부 확인 (선택적)**

**Test:** .shm 파일 중 하나를 임시로 손상(또는 경로 변경) 후 Run
**Expected:** "[ALIGN_SVC] contour[2] 추출 실패" 로그 출력 + 에지 전혀 표시 안 됨(WR-02 발현). 정상이면 contour[1] 점 유지
**Why human:** 비정상 .shm 시뮬레이션 필요. 정상 운영 시 미발현이므로 선택적 확인

---

### Gaps Summary

코드 수준 자동 검증에서 발견된 gaps: 없음.

4개 Success Criteria 전항목이 실제 코드에서 확인됨:
- SC#1 이미지 로더: XAML 버튼 3개 + CS 핸들러 완전 배선
- SC#2 결과 시각화: ApplyAlignVisualization 3-API 배선 + 체크박스 토글
- SC#3 AlignShapeMatchService 확장 + 하위호환: AlignResult 기존 6필드 보존 + Run 시그니처 불변
- SC#4 빌드 PASS + anti-goal: Build succeeded 에러 0, 변경 파일 6파일 한정 검증

코드 리뷰(REVIEW.md)에서 발견된 WR-01/WR-02 실제 버그는 정상 레시피 환경에서 즉각 미발현 가능하나 향후 핫픽스 권장. 목표 달성을 현재 즉시 block 하지는 않으나 실 .shm 구조 및 에지 시각화 품질은 런타임 UAT 에서 확인 필요.

자동 검증 통과 — 시각화 화면 동작·이미지 로더 UX·WR-01 에지 완전성은 human_verification 으로 이관.

---

_Verified: 2026-06-25_
_Verifier: Claude (gsd-verifier)_
