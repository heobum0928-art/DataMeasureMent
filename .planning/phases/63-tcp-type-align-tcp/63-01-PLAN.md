---
phase: 63-tcp-type-align-tcp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/TcpServer/VisionRequestPacket.cs
autonomous: true
requirements: [PROTO-Type, AV-09]
must_haves:
  truths:
    - "UseProtocolV1=true 일 때 $TEST:site,Type,자재번호,null,z_index@ 를 파싱하면 Type=[1], 자재번호=[2], z_index=[4] 가 정확히 추출된다"
    - "TestPacket.Type 에 SIDE_3 같은 토큰 문자열이 담긴다"
    - "$ALIGN_TEST / $ALIGN_CALIB 수신 메시지가 AlignTestPacket / AlignCalibPacket 으로 파싱된다"
    - "UseProtocolV1=false(v2.6) 경로의 site/type/testID 인덱스는 무변경 — 회귀 0"
    - "필드 부족/비정수 site/과다 토큰 입력에 대해 예외 없이 null 또는 sentinel 로 방어적 처리된다"
  artifacts:
    - path: "WPF_Example/TcpServer/VisionRequestPacket.cs"
      provides: "TEST_FIELD_TYPE 상수, TestPacket.Type 필드, ALIGN 요청 패킷, V1 Type 파싱"
      contains: "TEST_FIELD_TYPE"
  key_links:
    - from: "VisionRequestPacket.Convert(string)"
      to: "TryParseTestFieldsV1"
      via: "UseProtocolV1=true 분기에서 Type/자재번호/z_index 시프트 파싱"
      pattern: "TEST_FIELD_TYPE"
    - from: "VisionRequestPacket.Convert(string)"
      to: "AlignTestPacket / AlignCalibPacket"
      via: "CMD_RECV_ALIGN_TEST / CMD_RECV_ALIGN_CALIB switch case"
      pattern: "CMD_RECV_ALIGN_TEST"
---

<objective>
디팜스테크 Vision Protocol v3.0 수신측을 구현한다. UseProtocolV1=true(V1 파서) 한정으로 $TEST 에 Type 필드를 추가(자재번호/z_index 인덱스를 +1 시프트)하고, Align 전용 커맨드($ALIGN_TEST/$ALIGN_CALIB)를 기존 파싱 프레임워크에 통합한다.

Purpose: site(=PC독립번호)와 별개로 검사 대상(TOP/BOTTOM/SIDE_1~4)을 Type 토큰으로 구분하여 라우팅 가능하게 한다. Align 커맨드를 기존 Grabber 파서와 동일한 switch 프레임워크로 받는다.
Output: VisionRequestPacket.cs 에 TEST_FIELD_TYPE 상수 + TestPacket.Type 필드 + ALIGN 요청 패킷 클래스 2종 + V1 Type 파싱.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

<interfaces>
<!-- 수정 대상 파일의 기존 상수/메서드 (이미 코드베이스에 존재 — 탐색 불필요) -->

From WPF_Example/TcpServer/VisionRequestPacket.cs (현행, Phase 48 작성):
```csharp
// 기존 V1 필드 인덱스 상수 (이 plan 이 +1 시프트):
private const int TEST_FIELD_SITE = 0;        // 무변경
private const int TEST_FIELD_MATERIAL = 1;    // → 2 로 시프트 (Type 삽입)
private const int TEST_FIELD_ZINDEX = 3;      // → 4 로 시프트
private const int TEST_MIN_FIELD_SITE = 1;
private const int TEST_MIN_FIELD_MATERIAL = 2;// → 3
private const int TEST_MIN_FIELD_ZINDEX = 4;  // → 5
public const int SENTINEL_NO_MATERIAL = -1;
public const string SENTINEL_Z_INDEX_STR = "-1";
public const string TEST_NULL_PLACEHOLDER = "null";

// 기존 수신 커맨드 상수:
public const string CMD_RECV_TEST = "TEST";   // 기존 Grabber

// 기존 V1 파서 (이 plan 이 Type 파싱 추가):
private static bool TryParseTestFieldsV1(string[] dataList, TestPacket testPacket)
// 기존 헬퍼 (인덱스 상수만 참조 — 시프트 후 자동 반영):
private static int ParseMaterialField(string[] dataList)
private static string ParseZIndexField(string[] dataList)

// V2.6 파서 (절대 무변경 — 인덱스 0/1/2 하드코딩):
private static bool TryParseTestFieldsV26(string[] dataList, TestPacket testPacket)

// 기존 패킷 클래스:
public class TestPacket : VisionRequestPacket {
    public int TestType { get; set; }
    public string TestID { get; set; }       // z_index 가 여기 저장됨
    public int IndexNumber { get; set; } = SENTINEL_NO_MATERIAL;  // 자재번호
}
public enum VisionRequestType { RecipeChange, RecipeGet, SiteStatus, Light, Test, Unknown = 999 }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: V1 파서에 Type 필드 추가 + 자재번호/z_index 인덱스 시프트 (V1 한정)</name>
  <read_first>
    - WPF_Example/TcpServer/VisionRequestPacket.cs lines 27-39 (필드 인덱스 상수), 301-364 (TryParseTestFieldsV1 / TryParseTestFieldsV26 / ParseMaterialField / ParseZIndexField), 432-445 (TestPacket 클래스).
    - 패턴 참조: 기존 ParseMaterialField/ParseZIndexField 의 bHas* bool 변수화 + sentinel 폴백 구조를 그대로 따른다.
  </read_first>
  <action>
    VisionRequestPacket.cs 수정. 모든 신규/수정 라인에 `//260624 hbk Phase 63` 주석.

    1) 필드 인덱스 상수 시프트 (V1 파서 전용 — TryParseTestFieldsV26 의 하드코딩 0/1/2 는 절대 무변경):
       - `TEST_FIELD_MATERIAL = 1` → `2`
       - `TEST_FIELD_ZINDEX = 3` → `4`
       - `TEST_MIN_FIELD_MATERIAL = 2` → `3`
       - `TEST_MIN_FIELD_ZINDEX = 4` → `5`
       - 신규 상수 추가: `private const int TEST_FIELD_TYPE = 1;` 와 `private const int TEST_MIN_FIELD_TYPE = 2;`
    2) TestPacket 클래스에 `public string Type { get; set; } = "";` 필드 추가 (기본값 빈 문자열 — INI/미수신 안전).
    3) TryParseTestFieldsV1 에 Type 파싱 추가. site 파싱 직후, ParseMaterialField 호출 전에:
       ```csharp
       testPacket.Type = ParseTypeField(dataList);   //260624 hbk Phase 63
       ```
    4) 신규 헬퍼 ParseTypeField 추가 (30줄 이내, if/else, bool 변수화):
       ```csharp
       //260624 hbk Phase 63 PROTO-Type: Type 필드 파싱. 누락/'null'/빈값 → "" 폴백.
       private static string ParseTypeField(string[] dataList)
       {
           bool bHasType = dataList.Length >= TEST_MIN_FIELD_TYPE;
           if (!bHasType) { return ""; }
           string szRaw = dataList[TEST_FIELD_TYPE];
           bool bIsNullPlaceholder = string.IsNullOrEmpty(szRaw) || szRaw == TEST_NULL_PLACEHOLDER;
           if (bIsNullPlaceholder) { return ""; }
           return szRaw;
       }
       ```
    제약: ParseMaterialField/ParseZIndexField 본문은 수정하지 않는다(상수 시프트만으로 자동 반영). TryParseTestFieldsV26 전체 무변경. C# 7.2, 삼항/null병합 금지.
  </action>
  <verify>
    <automated>grep -n "TEST_FIELD_TYPE = 1" WPF_Example/TcpServer/VisionRequestPacket.cs && grep -n "TEST_FIELD_MATERIAL = 2" WPF_Example/TcpServer/VisionRequestPacket.cs && grep -n "TEST_FIELD_ZINDEX = 4" WPF_Example/TcpServer/VisionRequestPacket.cs && grep -n "public string Type" WPF_Example/TcpServer/VisionRequestPacket.cs && grep -n "ParseTypeField" WPF_Example/TcpServer/VisionRequestPacket.cs</automated>
  </verify>
  <acceptance_criteria>
    - VisionRequestPacket.cs contains `TEST_FIELD_TYPE = 1`
    - VisionRequestPacket.cs contains `TEST_FIELD_MATERIAL = 2` (시프트됨)
    - VisionRequestPacket.cs contains `TEST_FIELD_ZINDEX = 4` (시프트됨)
    - VisionRequestPacket.cs contains `TEST_MIN_FIELD_MATERIAL = 3` and `TEST_MIN_FIELD_ZINDEX = 5`
    - TestPacket 클래스에 `public string Type { get; set; }` 존재
    - ParseTypeField 메서드 존재, TryParseTestFieldsV1 가 testPacket.Type 에 대입
    - TryParseTestFieldsV26 본문에 `dataList[0]`/`dataList[1]`/`dataList[2]` 하드코딩 인덱스가 그대로 유지 (회귀 0)
  </acceptance_criteria>
  <done>UseProtocolV1=true 에서 $TEST:1,SIDE_3,12345,null,2@ 파싱 시 Site=1, Type="SIDE_3", IndexNumber=12345, TestID="2". V2.6 경로 인덱스 무변경.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: $ALIGN_TEST / $ALIGN_CALIB 수신 커맨드 + 패킷 클래스 통합</name>
  <read_first>
    - WPF_Example/TcpServer/VisionRequestPacket.cs lines 8-16 (VisionRequestType enum), 21-25 (CMD_RECV_* 상수), 129-296 (Convert(string) switch — 패킷 생성 패턴), 144-145 (msgList 길이 가드), 400-453 (패킷 서브클래스 정의 패턴).
    - 패턴 참조: 기존 case CMD_RECV_SITE_STATUS 의 `Int32.TryParse(dataList[0], out siteNum) == false → return null` 방어 파싱.
  </read_first>
  <action>
    VisionRequestPacket.cs 수정. 모든 신규 라인에 `//260624 hbk Phase 63` 주석.

    1) VisionRequestType enum 에 멤버 2개 추가 (Unknown=999 앞):
       `AlignTest,` 와 `AlignCalib,`
    2) 수신 커맨드 상수 2개 추가 (기존 CMD_RECV_* 옆):
       `public const string CMD_RECV_ALIGN_TEST = "ALIGN_TEST";`
       `public const string CMD_RECV_ALIGN_CALIB = "ALIGN_CALIB";`
    3) Convert(string msg) 의 switch(msgList[0]) 에 case 2개 추가 (기존 case 무손상 — 추가만):
       ```csharp
       case CMD_RECV_ALIGN_TEST:
           packet = new AlignTestPacket();
           AlignTestPacket alignTestPacket = packet.AsAlignTest();
           dataList = msgList[1].Split(VisionServer.MSG_CONTENTS_SEPERATOR);
           bool bAlignTestOk = TryParseAlignTestFields(dataList, alignTestPacket); //260624 hbk Phase 63
           if (!bAlignTestOk) { return null; }
           break;
       case CMD_RECV_ALIGN_CALIB:
           packet = new AlignCalibPacket();
           AlignCalibPacket alignCalibPacket = packet.AsAlignCalib();
           dataList = msgList[1].Split(VisionServer.MSG_CONTENTS_SEPERATOR);
           bool bAlignCalibOk = TryParseAlignCalibFields(dataList, alignCalibPacket); //260624 hbk Phase 63
           if (!bAlignCalibOk) { return null; }
           break;
       ```
    4) 방어 파서 헬퍼 2개 (각 30줄 이내, if/else, bool 변수화). 가정 명시 주석 필수:
       페이로드 = `$ALIGN_TEST:target@` / `$ALIGN_CALIB:target@` 로 가정(최소 1필드 target 문자열, Phase 62 Align 결과 모델 미확정 — Target 토큰만 라우팅용으로 추출).
       ```csharp
       //260624 hbk Phase 63 PROTO-Type/AV-09: ALIGN_TEST 수신 파서. [가정] 페이로드 = target 토큰(TRAY/BOTTOM 등).
       //  Phase 62(Align 결과 모델) 미확정 → 라우팅용 Target 만 추출, 상세 필드는 향후 확장.
       private static bool TryParseAlignTestFields(string[] dataList, AlignTestPacket alignPacket)
       {
           bool bHasTarget = dataList != null && dataList.Length >= 1;
           if (!bHasTarget) { return false; }
           alignPacket.AlignTarget = dataList[0];
           return true;
       }
       ```
       (TryParseAlignCalibFields 도 동일 구조, AlignCalibPacket.AlignTarget 대입.)
    5) As* 헬퍼 2개 추가 (기존 AsTest 패턴):
       `public AlignTestPacket AsAlignTest() { if (RequestType != VisionRequestType.AlignTest) return null; return this as AlignTestPacket; }`
       (AsAlignCalib 동일.)
    6) 패킷 서브클래스 2개 추가 (기존 TestPacket 패턴):
       ```csharp
       public class AlignTestPacket : VisionRequestPacket {
           public string AlignTarget { get; set; } = "";   //260624 hbk Phase 63 라우팅 대상(TRAY/BOTTOM)
           public AlignTestPacket() : base(VisionRequestType.AlignTest) { }
       }
       public class AlignCalibPacket : VisionRequestPacket {
           public string AlignTarget { get; set; } = "";
           public AlignCalibPacket() : base(VisionRequestType.AlignCalib) { }
       }
       ```
    제약: 기존 CMD_RECV_TEST/LIGHT/RECIPE/SITE_STATUS case 무변경. msgList.Length<2 가드(line 145)는 ALIGN 에도 적용됨(공통 진입부) — 추가 가드 불필요.
  </action>
  <verify>
    <automated>grep -n "CMD_RECV_ALIGN_TEST" WPF_Example/TcpServer/VisionRequestPacket.cs && grep -n "CMD_RECV_ALIGN_CALIB" WPF_Example/TcpServer/VisionRequestPacket.cs && grep -n "class AlignTestPacket" WPF_Example/TcpServer/VisionRequestPacket.cs && grep -n "class AlignCalibPacket" WPF_Example/TcpServer/VisionRequestPacket.cs && grep -n "AlignTest," WPF_Example/TcpServer/VisionRequestPacket.cs</automated>
  </verify>
  <acceptance_criteria>
    - VisionRequestPacket.cs contains `CMD_RECV_ALIGN_TEST = "ALIGN_TEST"` and `CMD_RECV_ALIGN_CALIB = "ALIGN_CALIB"`
    - VisionRequestType enum 에 `AlignTest` 와 `AlignCalib` 멤버 존재
    - `class AlignTestPacket` 와 `class AlignCalibPacket` 정의 존재, 각각 AlignTarget 프로퍼티 보유
    - Convert(string) switch 에 `case CMD_RECV_ALIGN_TEST` 와 `case CMD_RECV_ALIGN_CALIB` 존재
    - TryParseAlignTestFields / TryParseAlignCalibFields 가 dataList null/길이<1 시 false 반환 (방어 파싱)
    - AsAlignTest / AsAlignCalib 헬퍼 존재
  </acceptance_criteria>
  <done>$ALIGN_TEST:TRAY@ 수신 시 AlignTestPacket(AlignTarget="TRAY") 생성. 필드 0개면 null 반환(예외 없음). 기존 4개 CMD case 무손상.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 핸들러/호스트 PC → VisionServer TCP | 로컬 산업망의 신뢰 호스트지만 형식이 깨진 페이로드 가능(필드 부족, 비숫자 site, 과다 토큰) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Tampering/DoS | TryParseTestFieldsV1 / ParseTypeField | mitigate | dataList.Length 가드(TEST_MIN_FIELD_TYPE) + bHas* bool 변수화 후 분기 — 인덱스 범위 밖 접근 차단, 미수신 시 "" 폴백 |
| T-63-02 | Tampering/DoS | TryParseAlignTestFields / TryParseAlignCalibFields | mitigate | dataList null + Length>=1 가드 후 false 반환 → Convert 가 null 패킷 반환(상위 dispatch 무시). 예외 throw 금지 |
| T-63-03 | Tampering | V2.6 파서 회귀 | mitigate | TryParseTestFieldsV26 무변경 — 인덱스 시프트는 V1 분기 한정. v2.6 site/type/testID 위치 보존 |
| T-63-04 | DoS | 과다 토큰 입력 | accept | dataList.Length 초과 토큰은 무시(필요 인덱스만 접근) — 추가 토큰은 무해, 저위험 신뢰망 |
</threat_model>

<verification>
- grep 으로 모든 신규 상수/필드/메서드/패킷 클래스 존재 확인 (각 task acceptance_criteria).
- TryParseTestFieldsV26 의 dataList[0/1/2] 하드코딩 인덱스 보존 확인 (회귀 0).
- 컴파일 검증은 Plan 05(빌드)에서 통합 수행.
</verification>

<success_criteria>
- Success Criteria 1(수신 Type 파싱) 충족: TEST_FIELD_TYPE=1, 자재번호[1]→[2], z_index[3]→[4] (V1 한정).
- Success Criteria 3(부분 — 수신측) 충족: $ALIGN_TEST/$ALIGN_CALIB 파싱 통합.
- Success Criteria 4(부분 — V2.6 회귀 0) 충족: V26 파서 무변경.
</success_criteria>

<output>
완료 후 `.planning/phases/63-tcp-type-align-tcp/63-01-SUMMARY.md` 생성.
</output>
