---
phase: 63-tcp-type-align-tcp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/TcpServer/VisionResponsePacket.cs
autonomous: true
requirements: [PROTO-Type, AV-09]
must_haves:
  truths:
    - "UseProtocolV1=true 송신 시 $RESULT:site;Type;P|F|B;count;id=val=judge,...@ 형식으로 Type 이 헤더 두 번째 토큰에 echo 된다"
    - "TestResultPacket.Type 에 값이 없으면(빈값) 빈 토큰으로라도 자리(;;)가 보존되어 자재번호/판정 인덱스가 어긋나지 않는다"
    - "$ALIGN_RESULT / $ALIGN_CALIB 응답 메시지를 빌더가 생성한다"
    - "UseProtocolV1=false(v2.6) RESULT 출력 포맷은 byte-identical — 회귀 0"
  artifacts:
    - path: "WPF_Example/TcpServer/VisionResponsePacket.cs"
      provides: "TestResultPacket.Type 필드, BuildResultMessageV1 Type echo, Align 응답 빌더"
      contains: "MSG_RESULT_HEADER_SEP"
  key_links:
    - from: "BuildResultMessageV1"
      to: "TestResultPacket.Type"
      via: "site 토큰 뒤 ;Type; 삽입"
      pattern: "testPacket.Type"
    - from: "Convert(VisionResponsePacket)"
      to: "BuildAlignResultMessage / BuildAlignCalibMessage"
      via: "EVisionResponseType.AlignResult / AlignCalib switch case"
      pattern: "BuildAlignResultMessage"
---

<objective>
디팜스테크 Vision Protocol v3.0 송신측을 구현한다. UseProtocolV1=true(V1 직렬화) 한정으로 $RESULT 헤더에 Type 을 echo 하고, Align 전용 응답($ALIGN_RESULT/$ALIGN_CALIB)을 빌더로 생성한다.

Purpose: 호스트가 어떤 검사 대상(TOP/BOTTOM/SIDE_1~4)의 결과인지 Type echo 로 식별하게 한다. Align 결과를 기존 RESULT 프레임워크와 동형으로 송신.
Output: VisionResponsePacket.cs 에 TestResultPacket.Type 필드 + BuildResultMessageV1 의 ;Type; 삽입 + Align 응답 빌더/타입.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

<interfaces>
<!-- 수정 대상 파일의 기존 상수/메서드 (이미 코드베이스에 존재 — 탐색 불필요) -->

From WPF_Example/TcpServer/VisionResponsePacket.cs (현행, Phase 48/49 작성):
```csharp
public const string CMD_SEND_TEST = "RESULT";
public const char MSG_RESULT_HEADER_SEP = ';';   // 헤더 구분자 (site/판정/count 사이)
public const char MSG_RESULT_ITEM_SEP   = ',';
public const char MSG_RESULT_INNER_SEP  = '=';
public const string TEST_RESULT_PASS = "P";
public const string TEST_RESULT_FAIL = "F";
public const string TEST_RESULT_BUFFER = "B";
public const string RESULT_OK = "OK";
public const string RESULT_NG = "NG";

public enum EVisionResponseType { RecipeChange, RecipeGet, SiteStatus, Light, Test, GrabStatus, Unknown = 999 }

// V1 직렬화 빌더 (이 plan 이 ;Type; 삽입):
private static string BuildResultMessageV1(TestResultPacket testPacket)
//  현행: RESULT : site ; P|F|B ; count ; id=val=judge,...
private static string MapCycleJudgement(TestResultPacket testPacket)  // P|F|B
private static string BuildFaiItemsV1(TestResultPacket testPacket)    // id=val=judge,...

// TestResultPacket (이 plan 이 Type 필드 추가):
public class TestResultPacket : VisionResponsePacket {
    public int InspectionType { get; set; }
    public EVisionResultType Result { get; set; }
    public bool IsBuffer { get; set; } = false;
    public List<FAIResultData> FAIResults { get; set; }
    public int FAICount => FAIResults.Count;
    // ... (V1 빌더가 사용)
}

// Convert(VisionResponsePacket) switch — V2.6 RESULT 블록은 line 365-430,
//   BuildResultMessageV1 분기는 line 358-363 (UseProtocolV1=true 시 early break).
//   ⚠ EVisionResponseType.Test 의 v2.6 블록 (Site==Bottom 하드코딩 분기 포함) 절대 무변경.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: TestResultPacket.Type 필드 + BuildResultMessageV1 에 ;Type; echo 삽입</name>
  <read_first>
    - WPF_Example/TcpServer/VisionResponsePacket.cs lines 437-452 (BuildResultMessageV1 — 헤더 조립 순서), 640-665 (TestResultPacket 클래스).
    - 패턴 참조: BuildResultMessageV1 의 `szMsg += testPacket.Site.ToString(); szMsg += MSG_RESULT_HEADER_SEP;` 토큰 조립 순서를 그대로 따른다.
  </read_first>
  <action>
    VisionResponsePacket.cs 수정. 모든 신규/수정 라인에 `//260624 hbk Phase 63` 주석.

    1) TestResultPacket 클래스에 필드 추가:
       `public string Type { get; set; } = "";   //260624 hbk Phase 63 PROTO-Type: 검사 대상 echo (TOP/BOTTOM/SIDE_1~4)`
       (기본값 "" — Type 미설정/v2.6 경로에서 안전.)
    2) BuildResultMessageV1 에서 site 토큰 직후 ;Type; 삽입. 현행:
       ```
       szMsg += testPacket.Site.ToString();
       szMsg += MSG_RESULT_HEADER_SEP;               // ';'
       szMsg += MapCycleJudgement(testPacket);       // P|F|B
       ```
       을 다음으로 변경 (site 와 판정 사이에 Type 토큰 + 구분자 1개 추가):
       ```
       szMsg += testPacket.Site.ToString();
       szMsg += MSG_RESULT_HEADER_SEP;               // ';'
       szMsg += testPacket.Type;                     //260624 hbk Phase 63 Type echo (빈값이면 빈 토큰)
       szMsg += MSG_RESULT_HEADER_SEP;               // ';'  //260624 hbk Phase 63
       szMsg += MapCycleJudgement(testPacket);       // P|F|B
       ```
       결과 포맷: `RESULT:site;Type;P|F|B;count;id=val=judge,...`
    제약: MapCycleJudgement / BuildFaiItemsV1 본문 무변경. Convert 의 v2.6 Test 블록(line 365-430) 무변경(Type 은 V1 빌더에만). C# 7.2, 삼항/null병합 금지.
  </action>
  <verify>
    <automated>grep -n "public string Type" WPF_Example/TcpServer/VisionResponsePacket.cs && grep -n "testPacket.Type" WPF_Example/TcpServer/VisionResponsePacket.cs</automated>
  </verify>
  <acceptance_criteria>
    - TestResultPacket 클래스에 `public string Type { get; set; }` 존재
    - BuildResultMessageV1 가 `szMsg += testPacket.Type;` 와 그 뒤 `MSG_RESULT_HEADER_SEP` 를 site 토큰과 MapCycleJudgement 사이에 삽입
    - BuildResultMessageV1 의 site→Type→판정→count→items 순서가 보존됨 (Type 이 site 바로 뒤, 판정 앞)
    - Convert 의 v2.6 Test 블록(`testPacket.Site == (int)ESequence.Bottom` 하드코딩 분기 포함)에 Type 관련 변경 없음 (회귀 0)
  </acceptance_criteria>
  <done>Type="SIDE_3", Site=2, IsBuffer=true, count=0 → `RESULT:2;SIDE_3;B;0;`. Type="" → `RESULT:2;;B;0;` (자리 보존).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: $ALIGN_RESULT / $ALIGN_CALIB 응답 타입 + 빌더 골격 통합</name>
  <read_first>
    - WPF_Example/TcpServer/VisionResponsePacket.cs lines 11-21 (EVisionResponseType enum), 46-72 (CMD_SEND_* / RESULT 상수), 275-435 (Convert(VisionResponsePacket) switch — 빌더 패턴), 437-452 (BuildResultMessageV1 토큰 조립 패턴), 565-589 (응답 서브클래스 정의 패턴).
    - 패턴 참조: BuildResultMessageV1 의 `CMD + MSG_CMD_SEPERATOR + ...` 조립과 As* 헬퍼 패턴.
  </read_first>
  <action>
    VisionResponsePacket.cs 수정. 모든 신규 라인에 `//260624 hbk Phase 63` 주석.
    [가정] Phase 62(Align 결과 모델) 미완 — Align 페이로드 필드를 다음으로 가정하고 빌더 골격 작성:
      Tray: `ALIGN_RESULT:target;P|F;OffsetX=val=judge,OffsetY=val=judge`
      Bottom: `ALIGN_RESULT:target;P|F;OffsetX=...,OffsetY=...,Theta=...`
    Align 항목은 가변 리스트(FAIResultData 와 동형)로 모델링하여 Tray(2항목)/Bottom(3항목) 모두 수용.

    1) EVisionResponseType enum 에 멤버 2개 추가 (Unknown 앞): `AlignResult,` 와 `AlignCalib,`.
    2) 송신 커맨드 상수 추가:
       `public const string CMD_SEND_ALIGN_RESULT = "ALIGN_RESULT";`
       `public const string CMD_SEND_ALIGN_CALIB = "ALIGN_CALIB";`
    3) 응답 서브클래스 2개 + 항목 DTO 추가 (기존 FAIResultData/TestResultPacket 패턴):
       ```csharp
       //260624 hbk Phase 63 AV-09: Align 결과 항목 (OffsetX/Y/Theta 공용). id=val=judge 직렬화.
       public class AlignResultItem {
           public string ItemName { get; set; } = "";   // OffsetX / OffsetY / Theta
           public double Value { get; set; }
           public bool IsPass { get; set; } = true;
       }
       public class AlignResultPacket : VisionResponsePacket {
           public string AlignTarget { get; set; } = "";   // TRAY / BOTTOM
           public bool IsPass { get; set; } = true;
           public List<AlignResultItem> Items { get; set; } = new List<AlignResultItem>();
           public AlignResultPacket() : base(EVisionResponseType.AlignResult) { }
       }
       public class AlignCalibPacket : VisionResponsePacket {
           public string AlignTarget { get; set; } = "";
           public bool IsPass { get; set; } = true;
           public AlignCalibPacket() : base(EVisionResponseType.AlignCalib) { }
       }
       ```
    4) As* 헬퍼 2개 추가 (기존 AsTestResult 패턴):
       `public AlignResultPacket AsAlignResult() { if (ResponseType != EVisionResponseType.AlignResult) return null; return this as AlignResultPacket; }`
       (AsAlignCalib 동일.)
    5) Convert(VisionResponsePacket) switch 에 case 2개 추가 (기존 case 무손상):
       ```csharp
       case EVisionResponseType.AlignResult:
           msg += BuildAlignResultMessage(packet.AsAlignResult());   //260624 hbk Phase 63
           break;
       case EVisionResponseType.AlignCalib:
           msg += BuildAlignCalibMessage(packet.AsAlignCalib());     //260624 hbk Phase 63
           break;
       ```
    6) 빌더 메서드 2개 (각 30~40줄 이내, if/else, bool 변수화, 항목 루프는 BuildFaiItemsV1 의 i>0 분리자 패턴 차용):
       ```csharp
       //260624 hbk Phase 63 AV-09: $ALIGN_RESULT:target;P|F;Name=val=OK|NG,... 직렬화.
       //  [가정] Tray=2항목(OffsetX/Y), Bottom=3항목(+Theta). Phase 62 모델 확정 시 항목 채움.
       private static string BuildAlignResultMessage(AlignResultPacket packet)
       {
           string szMsg = "";
           szMsg += CMD_SEND_ALIGN_RESULT;
           szMsg += VisionServer.MSG_CMD_SEPERATOR;
           szMsg += packet.AlignTarget;
           szMsg += MSG_RESULT_HEADER_SEP;
           bool bIsPass = packet.IsPass;
           if (bIsPass) { szMsg += TEST_RESULT_PASS; } else { szMsg += TEST_RESULT_FAIL; }
           szMsg += MSG_RESULT_HEADER_SEP;
           szMsg += BuildAlignItems(packet);
           return szMsg;
       }
       ```
       BuildAlignItems = packet.Items 순회, i>0 시 MSG_RESULT_ITEM_SEP, 각 항목 `Name=val.ToString("0.000")=OK|NG`.
       BuildAlignCalibMessage = `ALIGN_CALIB:target;P|F` (항목 없음 — 캘리브 ack).
    제약: 기존 Convert switch case(GrabStatus/Light/RecipeChange/RecipeGet/SiteStatus/Test) 무변경. v2.6 RESULT 무영향. C# 7.2.
  </action>
  <verify>
    <automated>grep -n "CMD_SEND_ALIGN_RESULT" WPF_Example/TcpServer/VisionResponsePacket.cs && grep -n "class AlignResultPacket" WPF_Example/TcpServer/VisionResponsePacket.cs && grep -n "BuildAlignResultMessage" WPF_Example/TcpServer/VisionResponsePacket.cs && grep -n "AlignResult," WPF_Example/TcpServer/VisionResponsePacket.cs</automated>
  </verify>
  <acceptance_criteria>
    - VisionResponsePacket.cs contains `CMD_SEND_ALIGN_RESULT = "ALIGN_RESULT"` and `CMD_SEND_ALIGN_CALIB = "ALIGN_CALIB"`
    - EVisionResponseType enum 에 `AlignResult` 와 `AlignCalib` 멤버 존재
    - `class AlignResultPacket`, `class AlignCalibPacket`, `class AlignResultItem` 정의 존재
    - Convert(VisionResponsePacket) switch 에 `case EVisionResponseType.AlignResult` 와 `case EVisionResponseType.AlignCalib` 존재
    - BuildAlignResultMessage / BuildAlignCalibMessage / BuildAlignItems 메서드 존재
    - 기존 6개 Convert case 무변경 (GrabStatus/Light/RecipeChange/RecipeGet/SiteStatus/Test)
  </acceptance_criteria>
  <done>AlignResultPacket(AlignTarget="TRAY", IsPass=true, Items=[OffsetX,OffsetY]) → `ALIGN_RESULT:TRAY;P;OffsetX=..=OK,OffsetY=..=OK`. AlignCalibPacket → `ALIGN_CALIB:TRAY;P`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VisionServer → 핸들러/호스트 PC | 송신측 — 직렬화 출력. 입력 검증보다 포맷 무결성(구분자/자리 보존)이 핵심 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-05 | Tampering/포맷 오염 | BuildResultMessageV1 Type echo | mitigate | Type=="" 여도 토큰 자리(;;) 보존 → count/판정 인덱스 어긋남 방지. 호스트 파싱 오해석 차단 |
| T-63-06 | Information disclosure | BuildAlignItems double 직렬화 | accept | OffsetX/Y/Theta 측정값은 비민감 공정 데이터, 로컬 신뢰망 송신 |
| T-63-07 | Tampering | v2.6 RESULT 회귀 | mitigate | Type echo/Align 빌더는 V1/Align 경로 한정 — Convert v2.6 Test 블록 무변경 |
</threat_model>

<verification>
- grep 으로 신규 상수/필드/메서드/패킷/enum 멤버 존재 확인.
- Convert v2.6 Test 블록(Site==Bottom 하드코딩) 무변경 확인 (회귀 0).
- 컴파일 검증은 Plan 05(빌드)에서 통합 수행.
</verification>

<success_criteria>
- Success Criteria 2(송신 Type echo) 충족: $RESULT:site;Type;P|F|B;count;... 형식.
- Success Criteria 3(부분 — 송신측) 충족: $ALIGN_RESULT/$ALIGN_CALIB 빌더.
- Success Criteria 4(부분 — V2.6 회귀 0) 충족: Convert v2.6 블록 무변경.
</success_criteria>

<output>
완료 후 `.planning/phases/63-tcp-type-align-tcp/63-02-SUMMARY.md` 생성.
</output>
