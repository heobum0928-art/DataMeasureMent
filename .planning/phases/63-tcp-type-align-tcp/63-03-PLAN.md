---
phase: 63-tcp-type-align-tcp
plan: 03
type: execute
wave: 2
depends_on: [63-01]
files_modified:
  - WPF_Example/Custom/TcpServer/ResourceMap.cs
autonomous: true
requirements: [PROTO-Type]
must_haves:
  truths:
    - "UseProtocolV1=true 일 때 TestPacket.Type(TOP/BOTTOM/SIDE_1~4) 으로 시퀀스/액션 자원을 라우팅한다"
    - "Type 토큰이 빈값/미인식이면 기존 Site 슬롯 매핑으로 안전하게 폴백한다(예외 없음)"
    - "UseProtocolV1=false(v2.6) ResolveSiteSlot/Find 경로 무변경 — 회귀 0"
  artifacts:
    - path: "WPF_Example/Custom/TcpServer/ResourceMap.cs"
      provides: "Type 토큰 → ESite 슬롯 매핑 헬퍼"
      contains: "ResolveSlotByType"
  key_links:
    - from: "SetIdentifier (Test case, V1 분기)"
      to: "ResolveSlotByType(testPacket.Type, testPacket.Site)"
      via: "Type 우선, 빈값 시 ResolveSiteSlot 폴백"
      pattern: "ResolveSlotByType"
---

<objective>
수신 TestPacket.Type(검사 대상 토큰)을 기존 ESite 슬롯/자원 매핑으로 변환하여 시퀀스·액션을 라우팅한다. Type 부재 시 기존 Site 기반 폴백 보존.

Purpose: site(=PC독립번호)만으로는 검사 대상을 구분 못 하므로 Type 토큰으로 TOP/BOTTOM/SIDE_n 을 자원 슬롯에 매핑한다.
Output: ResourceMap.cs 에 Type→슬롯 변환 헬퍼 + SetIdentifier V1 분기의 Type 우선 라우팅.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

<dependency_note>
Plan 63-01 이 TestPacket.Type(string) 필드를 추가함. 이 plan 은 그 필드를 소비한다.
파일 무겹침(VisionRequestPacket.cs vs ResourceMap.cs)이나 컴파일 의존 → Wave 2.
</dependency_note>

<interfaces>
<!-- 수정 대상 파일의 기존 멤버 (이미 코드베이스에 존재 — 탐색 불필요) -->

From WPF_Example/Custom/TcpServer/ResourceMap.cs (현행, Phase 48 작성):
```csharp
public enum ESite : int { Top = 1, Side = 2, Bottom = 3 }
public enum ETestType : int { Default=0, Calibration=1, Inspection=2, InspectionLeft=3, InspectionRight=4, Unknown=999 }
public enum EPcRole : int { PC1_TopBottom = 1, PC2_Side = 2 }

// 현행 V1 Site→슬롯 변환 (Site2=Side, 그 외=Top):
private ESite ResolveSiteSlot(int nSite)

// 자원 등록: V1 은 ESite.Top/ESite.Side 2개 슬롯만 사용 (PC1: Top슬롯=TOP/Side슬롯=BOTTOM, PC2: 양 슬롯=SIDE).
//   MapPc1Resources / MapPc2Resources 가 Add(EResource.Sequence/Action/Camera/Light, ESite.Top|Side, ...) 등록.
public string Find(EResource res, ESite site)
public string Find(EResource res, ESite site, ETestType type)

// SetIdentifier Test case V1 분기 (line 162-169) — 이 plan 이 ResolveSiteSlot → ResolveSlotByType 로 교체:
//   bUseV1 → ESite eSlot = ResolveSiteSlot(testPacket.Site);
//            testPacket.Identifier = Find(EResource.Sequence, eSlot);
//            testPacket.Identifier2 = Find(EResource.Action, eSlot, ETestType.Inspection);
```

From WPF_Example/TcpServer/VisionRequestPacket.cs (Plan 63-01 이 추가):
```csharp
public class TestPacket { public string Type { get; set; } = ""; /* TOP/BOTTOM/SIDE_1~4 */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Type 토큰 → ESite 슬롯 매핑 헬퍼 + 토큰 상수</name>
  <read_first>
    - WPF_Example/Custom/TcpServer/ResourceMap.cs lines 11-33 (ESite/ETestType/EPcRole enum), 78-128 (MapPc1Resources/MapPc2Resources/ResolveSiteSlot — 슬롯 매핑 구조).
    - 패턴 참조: ResolveSiteSlot 의 `bool bIsSite2 = ...; if (bIsSite2) return ESite.Side; return ESite.Top;` if/else + bool 변수화.
  </read_first>
  <action>
    ResourceMap.cs 수정. 모든 신규 라인에 `//260624 hbk Phase 63` 주석.
    [가정] Type 토큰 정확 문자열 = "TOP"/"BOTTOM"/"SIDE_1"/"SIDE_2"/"SIDE_3"/"SIDE_4" (대문자, 언더스코어).
    슬롯 매핑 정책(현행 V1 자원 등록과 정합):
      - "TOP" → ESite.Top 슬롯
      - "BOTTOM" → ESite.Side 슬롯 (PC1 의 Side 슬롯 = BOTTOM 자원, MapPc1Resources 와 일치)
      - "SIDE_1"~"SIDE_4" → ESite.Top 슬롯 (PC2 는 양 슬롯이 SIDE 자원 동일 — Top 슬롯 채택)
      - 빈값/미인식 → ESite.Top 폴백(폴백은 호출부에서 ResolveSiteSlot 로 위임, 아래 Task 2 참조)

    1) Type 토큰 const 추가 (매직 스트링 금지):
       ```csharp
       private const string TYPE_TOKEN_TOP = "TOP";       //260624 hbk Phase 63
       private const string TYPE_TOKEN_BOTTOM = "BOTTOM";
       private const string TYPE_TOKEN_SIDE_PREFIX = "SIDE_";  // SIDE_1~4 공통 접두
       ```
    2) 매핑 헬퍼 추가 (30줄 이내, if/else, bool 변수화, out 패턴으로 인식 여부 반환):
       ```csharp
       //260624 hbk Phase 63 PROTO-Type: Type 토큰 → ESite 슬롯. 인식 실패 시 false 반환(호출부가 Site 폴백).
       private bool TryResolveSlotByType(string szType, out ESite eSlot)
       {
           eSlot = ESite.Top;
           bool bHasType = !string.IsNullOrEmpty(szType);
           if (!bHasType) { return false; }
           bool bIsTop = szType == TYPE_TOKEN_TOP;
           if (bIsTop) { eSlot = ESite.Top; return true; }
           bool bIsBottom = szType == TYPE_TOKEN_BOTTOM;
           if (bIsBottom) { eSlot = ESite.Side; return true; }
           bool bIsSide = szType.StartsWith(TYPE_TOKEN_SIDE_PREFIX);
           if (bIsSide) { eSlot = ESite.Top; return true; }
           return false;
       }
       ```
    제약: ResolveSiteSlot / MapPc1Resources / MapPc2Resources / InitializeV1 / InitializeV26 본문 무변경. C# 7.2, 삼항/null병합/switch expression 금지.
  </action>
  <verify>
    <automated>grep -n "TYPE_TOKEN_TOP" WPF_Example/Custom/TcpServer/ResourceMap.cs && grep -n "TryResolveSlotByType" WPF_Example/Custom/TcpServer/ResourceMap.cs</automated>
  </verify>
  <acceptance_criteria>
    - ResourceMap.cs contains `TYPE_TOKEN_TOP = "TOP"`, `TYPE_TOKEN_BOTTOM = "BOTTOM"`, `TYPE_TOKEN_SIDE_PREFIX = "SIDE_"`
    - TryResolveSlotByType(string, out ESite) 메서드 존재, 빈값/미인식 시 false 반환
    - TOP→ESite.Top, BOTTOM→ESite.Side, SIDE_*→ESite.Top 매핑
    - ResolveSiteSlot / MapPc1Resources / MapPc2Resources 본문 무변경
  </acceptance_criteria>
  <done>TryResolveSlotByType("BOTTOM", out s) → true, s=ESite.Side. TryResolveSlotByType("", out s) → false. TryResolveSlotByType("SIDE_3", out s) → true, s=ESite.Top.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SetIdentifier V1 분기에서 Type 우선 라우팅 (Site 폴백 보존)</name>
  <read_first>
    - WPF_Example/Custom/TcpServer/ResourceMap.cs lines 150-181 (SetIdentifier Test case — bIsCalibration 가드, bUseV1 분기 V1/v2.6).
    - 패턴 참조: 기존 bUseV1 분기의 `ESite eSlot = ResolveSiteSlot(...); testPacket.Identifier = Find(...);` 흐름.
  </read_first>
  <action>
    ResourceMap.cs 의 SetIdentifier Test case, bUseV1=true 분기만 수정. 신규/수정 라인에 `//260624 hbk Phase 63` 주석.

    현행 V1 분기:
    ```csharp
    if (bUseV1)
    {
        ESite eSlot = ResolveSiteSlot(testPacket.Site);
        testPacket.Identifier  = Find(EResource.Sequence, eSlot);
        testPacket.Identifier2 = Find(EResource.Action, eSlot, ETestType.Inspection);
    }
    ```
    을 다음으로 교체 (Type 우선, 미인식 시 ResolveSiteSlot 폴백):
    ```csharp
    if (bUseV1)
    {
        ESite eSlot;                                                  //260624 hbk Phase 63
        bool bTypeResolved = TryResolveSlotByType(testPacket.Type, out eSlot);  //260624 hbk Phase 63
        if (!bTypeResolved)                                          //260624 hbk Phase 63
        {
            eSlot = ResolveSiteSlot(testPacket.Site);               //260624 hbk Phase 63 폴백
        }
        testPacket.Identifier  = Find(EResource.Sequence, eSlot);
        testPacket.Identifier2 = Find(EResource.Action, eSlot, ETestType.Inspection);
    }
    ```
    제약: bIsCalibration 가드(line 153-159) 무변경. else(v2.6) 분기 무변경 — Type 미참조. 함수 전체 40줄 한도 준수(분기만 교체, 라인 +4).
  </action>
  <verify>
    <automated>grep -n "TryResolveSlotByType(testPacket.Type" WPF_Example/Custom/TcpServer/ResourceMap.cs && grep -n "ResolveSiteSlot(testPacket.Site)" WPF_Example/Custom/TcpServer/ResourceMap.cs</automated>
  </verify>
  <acceptance_criteria>
    - SetIdentifier V1 분기가 `TryResolveSlotByType(testPacket.Type, out eSlot)` 를 먼저 호출
    - bTypeResolved==false 시 `ResolveSiteSlot(testPacket.Site)` 폴백 호출
    - bIsCalibration 가드 무변경 (testPacket.Identifier=null; return false; 보존)
    - else(v2.6) 분기에 Type 참조 없음 — `Find(EResource.Sequence, (ESite)testPacket.Site)` 보존 (회귀 0)
  </acceptance_criteria>
  <done>V1 에서 Type="BOTTOM" 수신 → Identifier=SEQ_BOTTOM(PC1). Type="" → ResolveSiteSlot(Site) 폴백. v2.6 경로 무변경.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TestPacket.Type (파서 산출) → ResourceMap Find | Type 토큰은 외부 호스트 유래 문자열 — 미인식/임의값 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-08 | Tampering/DoS | TryResolveSlotByType | mitigate | 미인식 토큰 → false → ResolveSiteSlot(Site) 안전 폴백. KeyNotFoundException 회피(등록된 ESite.Top/Side 슬롯만 Find) |
| T-63-09 | Elevation/오라우팅 | Type 기반 시퀀스 선택 | mitigate | 인식 토큰만 슬롯 매핑, 그 외 기존 Site 폴백 — 미정의 동작 없음 |
| T-63-10 | Tampering | v2.6 SetIdentifier 회귀 | mitigate | else 분기 + ResolveSiteSlot/Map* 무변경, Type 은 V1 분기 한정 |
</threat_model>

<verification>
- grep 으로 TryResolveSlotByType 호출 + ResolveSiteSlot 폴백 확인.
- bIsCalibration 가드 및 v2.6 else 분기 무변경 확인.
- 컴파일 검증은 Plan 05(빌드)에서 통합 (TestPacket.Type 의존 — Plan 01 선행 필요).
</verification>

<success_criteria>
- Success Criteria 1(부분 — Type 라우팅) 충족: Type 토큰 → 시퀀스/액션 자원 매핑.
- Success Criteria 4(부분 — V2.6 회귀 0) 충족: ResolveSiteSlot/Map*/v2.6 분기 무변경.
</success_criteria>

<output>
완료 후 `.planning/phases/63-tcp-type-align-tcp/63-03-SUMMARY.md` 생성.
</output>
