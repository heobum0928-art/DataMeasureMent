---
phase: 63-tcp-type-align-tcp
plan: 04
type: execute
wave: 2
depends_on: [63-01, 63-02]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Custom/SystemHandler.cs
autonomous: true
requirements: [PROTO-Type, AV-09]
must_haves:
  truths:
    - "InspectionSequence 가 생성하는 모든 TestResultPacket 에 수신 RequestPacket.Type 이 echo 된다(응답 생성 3곳)"
    - "$ALIGN_TEST / $ALIGN_CALIB 수신 시 SystemHandler 가 ProcessAlignTest / ProcessAlignCalib 로 분기한다"
    - "기존 Grabber 검사 시퀀스/Action/SequenceBase 본문 무변경 — 추가만"
    - "UseProtocolV1=false(v2.6) 응답 경로 무영향(Type echo 는 V1 직렬화에서만 출력)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "TestResultPacket.Type echo 전파 (3 응답 생성 지점)"
      contains: "Type = RequestPacket.Type"
    - path: "WPF_Example/Custom/SystemHandler.cs"
      provides: "ALIGN_TEST/ALIGN_CALIB dispatch 분기"
      contains: "ProcessAlignTest"
  key_links:
    - from: "InspectionSequence AddResponse/BuildScopedResponse/BuildDatumShotResponse"
      to: "TestResultPacket.Type"
      via: "RequestPacket.Type echo"
      pattern: "Type = RequestPacket.Type"
    - from: "SystemHandler MainRun switch"
      to: "ProcessAlignTest / ProcessAlignCalib"
      via: "VisionRequestType.AlignTest / AlignCalib case"
      pattern: "case VisionRequestType.AlignTest"
---

<objective>
수신 TestPacket.Type 을 응답 TestResultPacket.Type 으로 전파(echo)하고, $ALIGN_TEST/$ALIGN_CALIB 커맨드를 SystemHandler 디스패치에 통합한다.

Purpose: 송신 RESULT 에 Type echo 가 실제로 채워지려면 응답 생성 지점에서 RequestPacket.Type 을 복사해야 한다(3곳). Align 커맨드는 기존 dispatch switch 에 case 추가로 라우팅.
Output: InspectionSequence.cs 응답 생성 3곳 Type echo + SystemHandler.cs ALIGN 분기 메서드.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

<dependency_note>
Plan 63-01 → TestPacket.Type + AlignTestPacket/AlignCalibPacket(요청) + VisionRequestType.AlignTest/AlignCalib.
Plan 63-02 → TestResultPacket.Type + AlignResultPacket/AlignCalibPacket(응답) + 빌더.
이 plan 은 둘 다 소비 → Wave 2, depends_on [63-01, 63-02].
파일 무겹침: InspectionSequence.cs / SystemHandler.cs (Plan 01~03 파일과 겹치지 않음 → Plan 03 과 병렬 가능).
</dependency_note>

<interfaces>
From WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (현행, Phase 49):
```csharp
// RequestPacket = TestPacket (base 필드 + Plan 01 추가 Type). 응답 생성 3곳:
// (A) AddResponse() v2.6 폴백 블록 (~line 112):
var responsePacket = new TestResultPacket {
    Target = RequestPacket.Sender, Site = RequestPacket.Site,
    InspectionType = RequestPacket.TestType, IsDynamicFAI = true,
};
// (B) BuildDatumShotResponse() (~line 329):
var packet = new TestResultPacket {
    Target = RequestPacket.Sender, Site = RequestPacket.Site,
    InspectionType = RequestPacket.TestType, IsDynamicFAI = true,
};
// (C) BuildScopedResponse() (~line 412):
var packet = new TestResultPacket {
    Target = RequestPacket.Sender, Site = RequestPacket.Site,
    InspectionType = RequestPacket.TestType, IsDynamicFAI = true,
};
// RequestPacket 타입은 TestPacket → .Type(string) 접근 가능 (Plan 01).
```

From WPF_Example/Custom/SystemHandler.cs (현행, MainRun dispatch ~line 37-63):
```csharp
switch (packet.RequestType) {
    case VisionRequestType.Light:       responsePacket = ProcessLightSet(packet.AsLight()); break;
    case VisionRequestType.RecipeChange:responsePacket = ProcessRecipeChange(packet.AsRecipeChange()); break;
    case VisionRequestType.RecipeGet:   responsePacket = ProcessRecipeGet(packet.AsRecipeGet()); break;
    case VisionRequestType.SiteStatus:  responsePacket = ProcessSiteStatus(packet.AsSiteStatus()); break;
    case VisionRequestType.Test:        ... ProcessTest(packet.AsTest()) ...; break;
    case VisionRequestType.Unknown:     break;
}
// responsePacket != null 이면 Server.SendPacket(i, responsePacket). null 이면 미송신.
```

From WPF_Example/TcpServer/VisionRequestPacket.cs (Plan 01): AlignTestPacket/AlignCalibPacket (AlignTarget), AsAlignTest()/AsAlignCalib(), VisionRequestType.AlignTest/AlignCalib.
From WPF_Example/TcpServer/VisionResponsePacket.cs (Plan 02): AlignResultPacket/AlignCalibPacket(응답, AlignTarget/IsPass/Items), CMD_SEND_ALIGN_*.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: InspectionSequence 응답 생성 3곳에 RequestPacket.Type echo 추가</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs lines 112-117 (AddResponse v2.6 폴백 TestResultPacket), 327-335 (BuildDatumShotResponse), 410-418 (BuildScopedResponse).
    - 패턴 참조: 세 곳 모두 동일한 객체 초기화자(Target/Site/InspectionType/IsDynamicFAI). Type 을 같은 초기화자에 추가.
  </read_first>
  <action>
    InspectionSequence.cs 의 세 TestResultPacket 객체 초기화자 각각에 Type echo 1줄 추가. 모든 신규 라인에 `//260624 hbk Phase 63` 주석.

    세 곳 모두 객체 초기화자에 다음 줄을 추가 (IsDynamicFAI 줄 옆/아래):
    ```csharp
    Type = RequestPacket.Type,   //260624 hbk Phase 63 PROTO-Type: 수신 Type echo
    ```
    적용 대상:
      (A) AddResponse() v2.6 폴백 블록의 `var responsePacket = new TestResultPacket { ... }` (~line 112)
      (B) BuildDatumShotResponse() 의 `var packet = new TestResultPacket { ... }` (~line 329)
      (C) BuildScopedResponse() 의 `var packet = new TestResultPacket { ... }` (~line 412)

    제약:
      - RequestPacket 은 TestPacket → .Type 접근 가능(Plan 01). RequestPacket null 가드는 각 메서드 진입부에 이미 존재(AddResponse line 96 `if (RequestPacket == null) return;`; Build* 는 RequestPacket.Sender 를 이미 참조하므로 non-null 전제 동일).
      - 기존 Target/Site/InspectionType/IsDynamicFAI 줄 무변경 — Type 줄만 추가.
      - 검사 본문(집계/판정 로직) 무변경. SequenceBase/ActionBase 무변경.
      - v2.6 경로에서도 Type 이 채워지지만 v2.6 직렬화(Convert)는 Type 을 출력하지 않으므로 무영향(회귀 0).
  </action>
  <verify>
    <automated>grep -c "Type = RequestPacket.Type" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</automated>
  </verify>
  <acceptance_criteria>
    - `Type = RequestPacket.Type` 가 InspectionSequence.cs 에 정확히 3회 출현 (3 응답 생성 지점)
    - 세 곳 모두 기존 TestResultPacket 객체 초기화자 내부에 위치 (Target/Site/InspectionType 와 동일 블록)
    - 집계/판정 로직(AggregateIndexFais/ApplyCycleJudgement/MapCycleJudgement) 무변경
    - SequenceBase.cs / ActionBase.cs 미수정 (이 plan files_modified 에 없음)
  </acceptance_criteria>
  <done>V1 검사 사이클에서 RequestPacket.Type="SIDE_3" → 3개 응답 경로(Datum/중간/마지막 Index) 모두 TestResultPacket.Type="SIDE_3" → BuildResultMessageV1 이 ;SIDE_3; echo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SystemHandler 디스패치에 ALIGN_TEST/ALIGN_CALIB 분기 추가</name>
  <read_first>
    - WPF_Example/Custom/SystemHandler.cs lines 37-72 (MainRun dispatch switch + responsePacket 송신), 209-259 (ProcessSiteStatus/ProcessTest/SendTestError — Process* 메서드 패턴).
    - 패턴 참조: ProcessSiteStatus(SiteStatusPacket) → SiteStatusResultPacket 반환 구조(Target/Site 세팅 후 return).
    - Plan 02 interfaces: AlignResultPacket(AlignTarget/IsPass/Items), AlignCalibPacket(응답, AlignTarget/IsPass).
  </read_first>
  <action>
    SystemHandler.cs 수정. 모든 신규 라인에 `//260624 hbk Phase 63` 주석.

    1) MainRun dispatch switch(packet.RequestType) 에 case 2개 추가 (Unknown case 앞, 기존 case 무변경):
       ```csharp
       case VisionRequestType.AlignTest:                                  //260624 hbk Phase 63
           responsePacket = ProcessAlignTest(packet.AsAlignTest());
           break;
       case VisionRequestType.AlignCalib:                                 //260624 hbk Phase 63
           responsePacket = ProcessAlignCalib(packet.AsAlignCalib());
           break;
       ```
       (기존 송신 분기 `else if (!Server.SendPacket(...))` 가 non-null responsePacket 을 자동 송신 — 추가 작업 불필요.)
    2) Process 메서드 2개 추가 (각 30~40줄 이내, if/else, bool 변수화). 골격 + 가정 주석:
       ```csharp
       //260624 hbk Phase 63 AV-09: $ALIGN_TEST 처리. [가정] Phase 62 Align 결과 모델 미확정 →
       //  현재는 ack 응답 골격(AlignTarget echo + Pass). 실제 Align 측정 트리거/결과 채움은 Phase 62 연계 시 확장.
       private AlignResultPacket ProcessAlignTest(AlignTestPacket packet)
       {
           AlignResultPacket resultPacket = new AlignResultPacket();
           bool bHasPacket = packet != null;
           if (!bHasPacket) { return null; }
           resultPacket.Target = packet.Sender;
           resultPacket.AlignTarget = packet.AlignTarget;
           resultPacket.IsPass = true;   // [가정] 측정 연계 전까지 ack
           return resultPacket;
       }
       //260624 hbk Phase 63 AV-09: $ALIGN_CALIB 처리 — 캘리브 ack 응답.
       private AlignCalibPacket ProcessAlignCalib(AlignCalibPacket packet)
       {
           AlignCalibPacket resultPacket = new AlignCalibPacket();
           bool bHasPacket = packet != null;
           if (!bHasPacket) { return null; }
           resultPacket.Target = packet.Sender;
           resultPacket.AlignTarget = packet.AlignTarget;
           resultPacket.IsPass = true;
           return resultPacket;
       }
       ```
       주의: 요청측 AlignTestPacket/AlignCalibPacket(VisionRequestPacket 파생, Plan 01)과 응답측 AlignResultPacket/AlignCalibPacket(VisionResponsePacket 파생, Plan 02)은 다른 타입. 응답측 AlignCalibPacket 은 ReringProject.Network 네임스페이스에 동명 클래스가 양쪽에 존재할 수 있으므로(요청 AlignCalibPacket vs 응답 AlignCalibPacket), 빌드 충돌 발견 시 응답측을 `AlignCalibResultPacket` 으로 개명 권고 — Plan 05 빌드에서 확인 후 필요 시 Plan 02 산출물명과 동기화한다.
    제약: 기존 dispatch case(Light/RecipeChange/RecipeGet/SiteStatus/Test/Unknown) 무변경. ProcessTest/ProcessLightSet 등 무변경. 검사 시퀀스/Action 무변경.
  </action>
  <verify>
    <automated>grep -n "case VisionRequestType.AlignTest" WPF_Example/Custom/SystemHandler.cs && grep -n "ProcessAlignTest" WPF_Example/Custom/SystemHandler.cs && grep -n "ProcessAlignCalib" WPF_Example/Custom/SystemHandler.cs</automated>
  </verify>
  <acceptance_criteria>
    - SystemHandler.cs MainRun switch 에 `case VisionRequestType.AlignTest` 와 `case VisionRequestType.AlignCalib` 존재
    - ProcessAlignTest / ProcessAlignCalib 메서드 존재, packet null 시 null 반환(방어)
    - 두 메서드 모두 packet.AlignTarget 을 응답에 echo + Target=packet.Sender 세팅
    - 기존 dispatch case(Light/RecipeChange/RecipeGet/SiteStatus/Test) 무변경
    - ProcessTest / ProcessLightSet / SendTestError 무변경
  </acceptance_criteria>
  <done>$ALIGN_TEST:TRAY@ 수신 → ProcessAlignTest → AlignResultPacket(AlignTarget="TRAY",IsPass=true) → Server.SendPacket → ALIGN_RESULT:TRAY;P;. packet null → null 반환(미송신).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VisionRequestPacket(파서) → SystemHandler dispatch | AsAlignTest()/AsAlignCalib() 가 null 반환 가능(타입 불일치) |
| RequestPacket.Type → TestResultPacket.Type | 외부 유래 Type 문자열이 응답에 echo — 임의값 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-11 | DoS/NRE | ProcessAlignTest/ProcessAlignCalib | mitigate | packet==null bool 변수화 후 null 반환 → 상위 dispatch 미송신. AsAlignTest 가 타입 불일치 시 null 반환 → 가드 적용 |
| T-63-12 | Tampering/echo | TestResultPacket.Type = RequestPacket.Type | accept | Type 은 라우팅/식별 토큰 echo 일 뿐 권한/판정에 영향 없음. 임의 문자열은 호스트 자기책임(신뢰망) |
| T-63-13 | Tampering | 기존 dispatch/검사 경로 회귀 | mitigate | case 추가 only, 기존 case/Process* 무변경. Type echo 는 객체 초기화자 1줄 추가 — 집계 로직 불변 |
</threat_model>

<verification>
- grep: `Type = RequestPacket.Type` 3회 + ProcessAlignTest/ProcessAlignCalib + dispatch case 확인.
- 기존 dispatch case 및 검사 본문 무변경 확인.
- 컴파일 검증(특히 요청/응답 AlignCalibPacket 동명 충돌 여부)은 Plan 05 에서 수행 — 충돌 시 응답측 개명 + Plan 02 동기화.
</verification>

<success_criteria>
- Success Criteria 2(완성 — Type echo 실제 채움) 충족: 응답 생성 3곳 Type 전파.
- Success Criteria 3(완성 — Align dispatch) 충족: ALIGN_TEST/ALIGN_CALIB 분기.
- Success Criteria 4(부분 — 검사 코드 무변경) 충족: 추가만, 본문 무변경.
</success_criteria>

<output>
완료 후 `.planning/phases/63-tcp-type-align-tcp/63-04-SUMMARY.md` 생성.
</output>
