---
phase: 63-tcp-type-align-tcp
plan: 05
type: execute
wave: 3
depends_on: [63-01, 63-02, 63-03, 63-04]
files_modified: []
autonomous: true
requirements: [PROTO-Type, AV-09]
must_haves:
  truths:
    - "Debug/x64 msbuild 가 0 errors 로 PASS 한다"
    - "신규 컴파일 경고 0 (Phase 49 baseline 대비)"
    - "요청측/응답측 동명 패킷 클래스 충돌이 없다(또는 개명으로 해소됨)"
    - "전 5 Success Criteria 가 코드 grep + 빌드로 추적 가능하다"
  artifacts:
    - path: ".planning/phases/63-tcp-type-align-tcp/63-05-SUMMARY.md"
      provides: "통합 빌드 검증 + Success Criteria 추적표"
      contains: "msbuild"
  key_links:
    - from: "VisionRequestPacket.Type / TestResultPacket.Type"
      to: "BuildResultMessageV1 / SetIdentifier"
      via: "엔드투엔드 Type echo 체인"
      pattern: "Type"
---

<objective>
Plan 01~04 의 5개 TCP 경계 파일 변경을 통합 빌드로 검증하고, 동명 패킷 충돌·신규 경고를 점검한 뒤 Success Criteria 1~5 추적표를 작성한다.

Purpose: 5개 파일이 컴파일 의존(Type 필드 → ResourceMap/InspectionSequence, Align 패킷 → SystemHandler)을 가지므로 통합 시점에 단일 빌드로 검증해야 한다.
Output: Debug/x64 msbuild PASS 확인 + 63-05-SUMMARY.md 추적표.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

<note>
빌드는 메인 워크트리에서 순차 실행한다. bin/ 이 gitignore 라 worktree 격리 빌드는 SDK/HALCON DLL 누락으로 실패하는 전례(Phase 48)가 있다. 이 plan 은 Wave 3 단독 실행 — Plan 01~04 가 메인 트리에 머지된 상태를 전제.
</note>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Debug/x64 통합 빌드 + 동명 패킷 충돌/신규 경고 점검</name>
  <read_first>
    - WPF_Example/DatumMeasurement.csproj (빌드 대상)
    - WPF_Example/TcpServer/VisionRequestPacket.cs (Plan 01 산출 — AlignTestPacket/AlignCalibPacket 요청측)
    - WPF_Example/TcpServer/VisionResponsePacket.cs (Plan 02 산출 — Align 응답측 패킷; 요청측과 동명 AlignCalibPacket 충돌 가능성)
  </read_first>
  <action>
    1) 메인 워크트리에서 빌드 실행:
       ```
       msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal
       ```
       (PowerShell 환경이면 msbuild 경로 확인 — 기존 Phase 빌드와 동일 호출.)
    2) 빌드 출력에서 확인:
       - `0 Error(s)` 여부.
       - 신규 경고 0 (Phase 49 baseline 대비 — CS0436/CS0101 동명 타입 경고 특히 주시).
    3) 동명 패킷 충돌 점검: 요청측(VisionRequestPacket.cs)과 응답측(VisionResponsePacket.cs) 모두 `AlignCalibPacket` 클래스를 ReringProject.Network 네임스페이스에 선언하면 CS0101(중복 정의) 발생. 발생 시:
       - 응답측 클래스를 `AlignCalibResultPacket` 으로 개명 (VisionResponsePacket.cs 의 class 선언 + ctor + EVisionResponseType.AlignCalib base + AsAlignCalib 반환형 + Convert case 빌더 인자형).
       - SystemHandler.cs(Plan 04 Task 2) 의 ProcessAlignCalib 반환형/지역변수도 `AlignCalibResultPacket` 으로 동기화.
       - 개명 라인에 `//260624 hbk Phase 63` 주석.
    4) 빌드가 다른 사유로 실패하면 오류 메시지를 근거로 최소 수정(시그니처/네임스페이스/using). 검사 시퀀스/Action/SequenceBase 본문은 수정 금지.
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal</automated>
  </verify>
  <acceptance_criteria>
    - msbuild 출력에 `0 Error(s)` 포함 (Debug/x64 PASS)
    - CS0101/CS0111 동명 타입 충돌 경고/오류 0 (충돌 시 응답측 AlignCalibResultPacket 개명으로 해소됨이 확인됨)
    - Phase 49 baseline 대비 신규 경고 0
    - SequenceBase.cs / ActionBase.cs / 검사 Action 본문 미수정
  </acceptance_criteria>
  <done>Debug/x64 빌드 0 errors. 동명 패킷 충돌 없음(또는 응답측 개명으로 해소). 신규 경고 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Success Criteria 1~5 추적표 작성 (grep 근거)</name>
  <read_first>
    - .planning/ROADMAP.md Phase 63 Success Criteria 1~5.
    - Plan 01~04 SUMMARY (생성되었으면) 또는 직접 grep 으로 산출물 확인.
  </read_first>
  <action>
    63-05-SUMMARY.md 에 Success Criteria 추적표를 작성. 각 항목에 grep 근거(파일:상수/메서드/포맷)를 명시.

    | SC | 내용 | 충족 근거(grep/빌드) | Plan |
    |----|------|---------------------|------|
    | 1 | 수신 Type 파싱 (Type=[1],자재=[2],z=[4]) | VisionRequestPacket.cs: TEST_FIELD_TYPE=1, TEST_FIELD_MATERIAL=2, TEST_FIELD_ZINDEX=4, ParseTypeField | 01 |
    | 2 | 송신 RESULT Type echo | VisionResponsePacket.cs: BuildResultMessageV1 `szMsg += testPacket.Type` + ;구분자; / InspectionSequence: Type=RequestPacket.Type ×3 | 02,04 |
    | 3 | ALIGN_TEST/CALIB 수신+응답 빌더 | Request: CMD_RECV_ALIGN_TEST/CALIB + AlignTestPacket/AlignCalibPacket / Response: CMD_SEND_ALIGN_RESULT/CALIB + BuildAlignResultMessage / SystemHandler: ProcessAlignTest/Calib | 01,02,04 |
    | 4 | v2.6 회귀 0 + 검사코드 무변경 | TryParseTestFieldsV26 무변경(dataList[0/1/2]) + Convert v2.6 Test 블록 무변경 + SequenceBase/Action 미수정 | 01,02,03,04 |
    | 5 | Debug/x64 msbuild PASS | Task 1 빌드 0 errors | 05 |

    추가 기재: Phase 62(Align 결과 모델) 미완으로 ALIGN 빌더/Process 가 골격(ack)임을 명시 — 측정 트리거/결과 채움은 Phase 62 연계 시 확장. [가정] 목록(Type 토큰 문자열, ALIGN 페이로드 필드, 응답측 패킷 개명 여부) 기록.
  </action>
  <verify>
    <automated>grep -n "Success Criteria" .planning/phases/63-tcp-type-align-tcp/63-05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - 63-05-SUMMARY.md 에 SC 1~5 추적표 존재, 각 행에 grep/빌드 근거 + Plan 번호
    - Phase 62 미완에 따른 ALIGN 골격(ack) 한계 + 가정 목록 명시
    - 응답측 동명 패킷 개명 여부(있었으면) 기록
  </acceptance_criteria>
  <done>SC 1~5 전부 추적 근거와 함께 SUMMARY 에 기록. Phase 62 연계 carry-over(Align 결과 실측 채움) 명시.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (해당 없음 — 빌드/검증 plan) | 코드 변경 없음. 충돌 발견 시 최소 개명만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-14 | Tampering | 빌드 충돌 해소 시 응답측 개명 | mitigate | 개명은 타입명/시그니처 한정 — 직렬화 포맷/파서 로직 무변경. 빌드 grep 으로 회귀 0 확인 |
</threat_model>

<verification>
- Debug/x64 msbuild 0 errors + 신규 경고 0.
- Success Criteria 1~5 추적표 grep 근거 검증.
</verification>

<success_criteria>
- Success Criteria 5(msbuild PASS) 충족.
- Success Criteria 1~4 의 통합 검증(추적표) 완료.
</success_criteria>

<output>
완료 후 `.planning/phases/63-tcp-type-align-tcp/63-05-SUMMARY.md` 생성 (추적표 포함).
</output>
