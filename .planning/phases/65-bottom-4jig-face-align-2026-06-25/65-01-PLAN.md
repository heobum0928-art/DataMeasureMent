---
phase: 65-bottom-4jig-face-align-2026-06-25
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/EthernetVision/EBottomAlignSlot.cs
  - WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [AV-08]

must_haves:
  truths:
    - "AlignShapeMatchService 가 6슬롯(3D_Top/3D_Bottom/2D_TOP/2D_BOTTOM/2D_SIDE_1/2D_SIDE_2) 별로 독립 모델/레퍼런스를 티칭·검색한다"
    - "슬롯 미지정(None) 시 기존 Bottom 단일 경로(Bottom_1/2.shm + Bottom.json)로 폴백한다 (회귀 0)"
    - "AlignFace 0~5 정수를 슬롯으로 매핑할 수 있다 (0=3D_Top … 5=2D_SIDE_2)"
  artifacts:
    - path: "WPF_Example/Custom/EthernetVision/EBottomAlignSlot.cs"
      provides: "6값 슬롯 enum + 파일 토큰 매퍼 + AlignFace 0~5 매퍼"
      contains: "enum EBottomAlignSlot"
    - path: "WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs"
      provides: "slot 파라미터를 받는 TryTeach/Run/HasTemplate + Bottom_{slot} 경로"
      contains: "EBottomAlignSlot"
  key_links:
    - from: "AlignShapeMatchService.BuildShmPath"
      to: "Bottom_{slot}_{1|2}.shm 파일명"
      via: "slot 토큰 삽입"
      pattern: "Bottom_.*slot|SlotToken"
    - from: "EBottomAlignSlot.FromAlignFace"
      to: "0~5 → 슬롯"
      via: "switch/if-else 매핑"
      pattern: "FromAlignFace"
---

<objective>
AlignShapeMatchService 를 단일 Bottom 경로에서 **6슬롯**(면별) 경로로 확장한다. 슬롯 차원을 `EEthernetVisionMode mode` 와 **나란히(orthogonal)** 추가하여, 각 지그(3D_Top/3D_Bottom/2D_TOP/2D_BOTTOM/2D_SIDE_1/2D_SIDE_2)가 독립 모델 2개(`Bottom_{slot}_1/2.shm`) + 레퍼런스 1개(`Bottom_{slot}.json`)를 갖도록 한다.

Purpose: Bottom 비전이 6종 안착지그를 면별로 정합할 수 있는 데이터/서비스 기반을 만든다. UI(Plan 02)와 TCP Run 라우팅(Plan 03)이 이 slot API 를 소비한다.
Output: `EBottomAlignSlot` enum + 토큰/AlignFace 매퍼, slot 파라미터로 확장된 AlignShapeMatchService (기존 단일/Tray 경로 폴백 보존).

**잠금 결정(CONTEXT.md):** D-01(6슬롯 2그룹), D-02(`Bottom_{slot}_1/2.shm`+`.json`), D-03(AlignFace 0~5 매핑 = 0=3D_Top/1=3D_Bottom/2=2D_TOP/3=2D_BOTTOM/4=2D_SIDE_1/5=2D_SIDE_2), D-09(slot 미지정/None → 기존 Bottom 단일 폴백, 회귀 0), D-10(Tray 무변경).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-bottom-4jig-face-align-2026-06-25/65-CONTEXT.md

# 핵심 변경 대상 — 반드시 먼저 읽을 것
@WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs
@WPF_Example/Custom/EthernetVision/EEthernetVisionMode.cs

<interfaces>
<!-- 실행자가 코드베이스를 탐색하지 않도록 핵심 계약을 그대로 전달. -->

AlignShapeMatchService.cs 의 slot-ification 대상 멤버 (현재 시그니처 — mode 만 받음):

```csharp
// 경로 헬퍼 (private) — modeFileName 이 "Bottom"/"Tray" 로 고정. 여기에 slot 토큰을 끼워야 함.
private string BuildShmPath(EEthernetVisionMode mode, int modelIndex);   // L67 — IO 없음(읽기 경로)
private string GetShmPath(EEthernetVisionMode mode, int modelIndex);     // L96 — Directory.CreateDirectory 포함(쓰기 경로)
private string BuildJsonPath(EEthernetVisionMode mode);                  // L106 — _1.shm 에서 .json 유도

// 공개 API (slot 파라미터 추가 대상)
public bool HasTemplate(EEthernetVisionMode mode);                       // L198
public bool TryLoadTemplate(EEthernetVisionMode mode);                   // L212 — HasTemplate 위임
public bool TryTeach(HImage img,
    double roi1Row, double roi1Col, double roi1Phi, double roi1Len1, double roi1Len2,
    double roi2Row, double roi2Col, double roi2Phi, double roi2Len1, double roi2Len2,
    EEthernetVisionMode mode, out string error);                         // L220
public AlignResult Run(HImage img, EEthernetVisionMode mode);            // L332

// 모델 파일명 조립 핵심 (BuildShmPath 내부, L77-91):
//   modeFileName = (mode==Bottom) ? "Bottom" : "Tray";
//   suffix       = (modelIndex==2) ? "_2" : "_1";
//   return Path.Combine(folder, modeFileName + suffix + PatternMatchService.EXTENSION_SHAPE_MODEL);
// → slot 이 지정되고 mode==Bottom 이면 modeFileName = "Bottom_" + slotToken (예: "Bottom_3D_Top").
//   slot==None 또는 mode!=Bottom 이면 기존 그대로 "Bottom"/"Tray" (폴백, 회귀 0).

// 상수 (이미 존재 — 재사용):
private const string ETHERNET_ALIGN_FOLDER = "ETHERNET_ALIGN";
private const string MODEL_SUFFIX_1 = "_1";
private const string MODEL_SUFFIX_2 = "_2";
private const string REF_POSE_EXT = ".json";
// PatternMatchService.EXTENSION_SHAPE_MODEL (.shm 확장자 상수)
```

EEthernetVisionMode.cs (무변경 — slot 은 별도 enum):
```csharp
public enum EEthernetVisionMode { None = 0, Tray = 1, Bottom = 2 }
```

AlignResultPacket / VisionResponsePacket.cs 의 AlignFace 매핑 주석(권위, 이미 코드 반영):
//  0=G1_TOP=3D_Top / 1=G1_BOT=3D_Bottom / 2=G2_TOP=2D_TOP / 3=G2_BOT=2D_BOTTOM / 4=G2_SIDE1=2D_SIDE_1 / 5=G2_SIDE2=2D_SIDE_2
</interfaces>
</context>

<coding_rules>
- 모든 추가/수정 라인 끝에 `//260626 hbk` + 한국어 사유 1줄 주석.
- 삼항 연산자 `?:` 금지 → `if-else`. 헝가리언/기존 명명 유지(`b` prefix bool, PascalCase 메서드).
- C# 7.2 (.NET 4.8) — switch expression / nullable reference / record 등 8.0+ 문법 금지. 일반 `switch` 문 또는 if-else 사용.
- 모든 HALCON/Shape matching 호출은 try/catch (기존 AlignShapeMatchService 패턴 유지: 실패 시 return false / Found=false, throw 금지).
- 함수 30~40줄 유지. 슬롯 분기 때문에 길어지면 헬퍼로 분할.
- **회귀 0**: slot==None 또는 mode!=Bottom 경로는 byte-identical 동작 (기존 Bottom_1/2.shm / Tray_*.shm 그대로). Tray 경로 코드 무수정.
</coding_rules>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: EBottomAlignSlot enum + 토큰/AlignFace 매퍼 파일 생성</name>
  <files>WPF_Example/Custom/EthernetVision/EBottomAlignSlot.cs, WPF_Example/DatumMeasurement.csproj</files>
  <read_first>
    - WPF_Example/Custom/EthernetVision/EEthernetVisionMode.cs (네임스페이스 `ReringProject.Setting` + 주석 스타일 참고)
    - WPF_Example/TcpServer/VisionResponsePacket.cs:531-545,882-886 (AlignFace 0~5 권위 매핑 주석)
  </read_first>
  <action>
    신규 파일 `WPF_Example/Custom/EthernetVision/EBottomAlignSlot.cs` 생성 (네임스페이스 `ReringProject.Setting` — EEthernetVisionMode 와 동일).

    (1) **enum EBottomAlignSlot** — 7값 (D-01 6슬롯 + None 폴백 센티넬):
    ```
    None      = -1,  // 슬롯 미지정 → 기존 Bottom 단일 경로 폴백 (D-09)
    Slot3DTop    = 0,  // 3D_Top    (AlignFace 0 = G1_TOP)
    Slot3DBottom = 1,  // 3D_Bottom (AlignFace 1 = G1_BOT)
    Slot2DTop    = 2,  // 2D_TOP    (AlignFace 2 = G2_TOP)
    Slot2DBottom = 3,  // 2D_BOTTOM (AlignFace 3 = G2_BOT)
    Slot2DSide1  = 4,  // 2D_SIDE_1 (AlignFace 4 = G2_SIDE1)
    Slot2DSide2  = 5   // 2D_SIDE_2 (AlignFace 5 = G2_SIDE2)
    ```
    (정수값을 AlignFace 0~5 와 1:1 정렬 — D-03 권위 매핑 고정. None=-1 은 폴백.)

    (2) **static class EBottomAlignSlotMap** (또는 enum 옆 static helper) — 매퍼 메서드:
    - `public static string ToFileToken(EBottomAlignSlot slot)` — 파일명 토큰 반환 (D-02). switch 문:
      `Slot3DTop`→`"3D_Top"`, `Slot3DBottom`→`"3D_Bottom"`, `Slot2DTop`→`"2D_TOP"`, `Slot2DBottom`→`"2D_BOTTOM"`, `Slot2DSide1`→`"2D_SIDE_1"`, `Slot2DSide2`→`"2D_SIDE_2"`, `None`(default)→`""`(빈 문자열 = 폴백 신호). 삼항 금지, switch 또는 if-else.
    - `public static EBottomAlignSlot FromAlignFace(int alignFace)` — TCP AlignFace 0~5 → 슬롯. switch 문: 0→Slot3DTop … 5→Slot2DSide2, 그 외(default, 음수/6이상)→`None`. **0~5 외 값은 None 반환**(Plan 03 이 안전 거부에 사용 — threat model T-65-01).
    - `public static string ToDisplayLabel(EBottomAlignSlot slot)` — UI 라벨 (Plan 02 소비). 토큰과 동일 문자열 반환해도 됨(`3D_Top` 등). None→`"(단일)"`.

    모든 매퍼는 헝가리언 + if-else/switch, 삼항 금지. 각 메서드 30줄 이내.

    (3) **csproj 등록 (Rule 3 — CS0246 방지):** `WPF_Example/DatumMeasurement.csproj` 의 `<Compile Include=...>` ItemGroup 에 `<Compile Include="Custom\EthernetVision\EBottomAlignSlot.cs" />` 추가. EEthernetVisionMode.cs / AlignShapeMatchService.cs 등록 라인 근처에 알파벳/인접 위치로 삽입. (Phase 12-01 선례: 새 파일 csproj Compile 누락 시 빌드 블로커.)
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal /nologo 2>&1 | grep -iE "error|Build succeeded"</automated>
  </verify>
  <done>
    EBottomAlignSlot.cs 존재, enum 7값 + ToFileToken/FromAlignFace/ToDisplayLabel 매퍼 컴파일. csproj 에 Compile Include 추가. FromAlignFace(6) 및 FromAlignFace(-1) 은 None 반환(코드 검증). msbuild Debug/x64 PASS.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AlignShapeMatchService 경로 헬퍼 + 공개 API 에 slot 파라미터 추가 (폴백 보존)</name>
  <files>WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs</files>
  <read_first>
    - WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs (전체 — L63-116 경로 헬퍼, L198/212/220/332 공개 API)
    - WPF_Example/Custom/EthernetVision/EBottomAlignSlot.cs (Task 1 산출물 — ToFileToken)
  </read_first>
  <action>
    AlignShapeMatchService 에 slot 차원을 mode 와 나란히 추가. **시그니처 보호 전략 = 기본값 파라미터** (`EBottomAlignSlot slot = EBottomAlignSlot.None`) — 기존 호출자(없거나 Plan 02/03 이 갱신)와 폴백(None) 동시 보호.

    (1) **BuildShmPath** (L67) 시그니처 → `private string BuildShmPath(EEthernetVisionMode mode, int modelIndex, EBottomAlignSlot slot)`.
        내부 modeFileName 조립부(L77-83) 변경:
        - `mode==Bottom` 이고 `slot != None` 이고 `ToFileToken(slot)` 이 비어있지 않으면 → `modeFileName = "Bottom_" + token` (예: `"Bottom_3D_Top"`). //260626 hbk 6슬롯 면별 모델명
        - 그 외(Tray, 또는 Bottom+slot==None) → 기존 그대로 `"Bottom"` / `"Tray"`. (폴백, 회귀 0)
        삼항 금지 — if-else 블록.

    (2) **GetShmPath** (L96) 시그니처 → `(EEthernetVisionMode mode, int modelIndex, EBottomAlignSlot slot)`, 내부 `BuildShmPath(mode, modelIndex, slot)` 전달. Directory.CreateDirectory 로직 그대로.

    (3) **BuildJsonPath** (L106) 시그니처 → `(EEthernetVisionMode mode, EBottomAlignSlot slot)`, 내부 `BuildShmPath(mode, 1, slot)` 전달. `_1` 접미사 제거 + `.json` 교체 로직 그대로 (slot 토큰 포함된 파일명에서 `_1`만 제거 — `Bottom_3D_Top_1.shm` → `Bottom_3D_Top.json` 확인. **주의: Replace(MODEL_SUFFIX_1,"")=`_1` 제거가 토큰 내부 다른 `_1`을 건드리지 않는지 검증** — 예 `2D_SIDE_1` 토큰. 안전하게 `Path.GetFileNameWithoutExtension` 결과 끝의 `_1`만 제거하도록 변경: 현재 `Replace(MODEL_SUFFIX_1, "")` 가 `Bottom_2D_SIDE_1_1` 에서 두 `_1` 모두 치환하면 버그 → **EndsWith 체크 후 마지막 `_1`만 Substring 제거**로 교정. //260626 hbk 토큰 내부 _1 오치환 방지).

    (4) **HasTemplate** (L198) 시그니처 → `public bool HasTemplate(EEthernetVisionMode mode, EBottomAlignSlot slot = EBottomAlignSlot.None)`. 내부 `BuildShmPath(mode,1,slot)`/`BuildShmPath(mode,2,slot)`/`BuildJsonPath(mode,slot)` 전달. 나머지 존재확인 로직 그대로.

    (5) **TryLoadTemplate** (L212) 시그니처 → `(EEthernetVisionMode mode, EBottomAlignSlot slot = EBottomAlignSlot.None)`, `HasTemplate(mode, slot)` 위임.

    (6) **TryTeach** (L220) 시그니처에 `EBottomAlignSlot slot = EBottomAlignSlot.None` 를 `mode` 다음·`out string error` 앞에 추가. 내부 `GetShmPath(mode,1,slot)`/`GetShmPath(mode,2,slot)`/`BuildJsonPath(mode,slot)` 전달. angleExtent 분기(Bottom/Tray) 그대로. 로그 문구에 slot 토큰 추가(`teach OK ({0}/{slot})`). //260626 hbk 슬롯 로그.

    (7) **Run** (L332) 시그니처 → `public AlignResult Run(HImage img, EEthernetVisionMode mode, EBottomAlignSlot slot = EBottomAlignSlot.None)`. 내부 `BuildShmPath(mode,1,slot)`/`BuildShmPath(mode,2,slot)`/`BuildJsonPath(mode,slot)` 전달. 나머지 (TryFindPose×2, angle_lx, offset, 피커보정, 시각화 XLD) 전부 그대로. Run 내부 `TryBuildDetectedContourXld` 가 받는 `shmPath1/shmPath2` 는 이미 slot 반영된 경로 → 추가 변경 불필요. 로그 문구에 slot 추가.

    **회귀 가드:** 위 7개 외 메서드(TrySaveRefPose/LoadRefPose/ComputeAngleLx/BuildDetectedRoiBox/TryBuildDetectedContourXld/TryBuildMovedContour/ApplyPickerCenterCorrection) 시그니처·본문 **무변경**. slot==None 호출 시 산출 경로가 기존과 100% 동일해야 함(기존 Bottom_1/2.shm, Bottom.json / Tray_1/2.shm, Tray.json).
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal /nologo 2>&1 | grep -iE "error|Build succeeded"</automated>
  </verify>
  <done>
    BuildShmPath/GetShmPath/BuildJsonPath/HasTemplate/TryLoadTemplate/TryTeach/Run 모두 slot 파라미터 수용(기본 None). mode==Bottom+slot!=None 시 `Bottom_{token}_1/2.shm`+`Bottom_{token}.json` 경로, slot==None 또는 Tray 시 기존 경로 그대로. `2D_SIDE_1` 토큰의 `_1` 오치환 없음(json 경로 = `Bottom_2D_SIDE_1.json`). msbuild Debug/x64 PASS. 비-slot 메서드 무변경.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLC→Vision TCP ($ALIGN_TEST) | AlignFace 정수 외부 입력. 0~5 외 값(음수/6이상)이 슬롯 배열/매핑 인덱스로 들어가면 OOB 위험. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Tampering/DoS | EBottomAlignSlot.FromAlignFace | mitigate | FromAlignFace 가 0~5 외 정수에 대해 `None` 반환(switch default). 인덱스 산술 없이 enum 매핑만 사용 → OOB 불가. Plan 03 이 None 수신 시 안전 거부+로그. |
| T-65-02 | Information Disclosure | 슬롯 파일 경로 | accept | RecipeSavePath 하위 고정 폴더(ETHERNET_ALIGN). 슬롯 토큰은 내부 상수 집합에서만 생성(외부 문자열 미사용) → path traversal 불가. 저위험. |
</threat_model>

<verification>
- msbuild Debug/x64 PASS (CS 오류 0).
- EBottomAlignSlot.FromAlignFace: 0→Slot3DTop, 5→Slot2DSide2, 6→None, -1→None (코드 grep 확인).
- AlignShapeMatchService 의 slot==None 경로가 기존 단일/Tray 경로와 동일 파일명 산출 (코드 리뷰).
- 비-slot 메서드 7종 무변경 (git diff 범위 = 경로 헬퍼 3 + 공개 API 4 한정).
</verification>

<success_criteria>
- 6슬롯 각각 독립 `Bottom_{slot}_1/2.shm` + `Bottom_{slot}.json` 경로가 산출된다.
- slot 미지정(None) 호출은 기존 Bottom 단일 동작과 byte-identical (D-09 폴백, 회귀 0).
- Tray 경로 코드 무변경 (D-10).
- AlignFace 0~5 → 슬롯 매핑 + 범위 외 거부(None) 동작.
- Debug/x64 빌드 PASS.
</success_criteria>

<output>
완료 후 `.planning/phases/65-bottom-4jig-face-align-2026-06-25/65-01-SUMMARY.md` 생성.
</output>
