---
phase: 65-bottom-4jig-face-align-2026-06-25
plan: 02
type: execute
wave: 2
depends_on: ["65-01"]
files_modified:
  - WPF_Example/Custom/UI/BottomVisionView.xaml
  - WPF_Example/Custom/UI/BottomVisionView.xaml.cs
autonomous: true
requirements: [AV-08]

must_haves:
  truths:
    - "사용자가 BottomVisionView 에서 6슬롯(3D_Top/3D_Bottom/2D_TOP/2D_BOTTOM/2D_SIDE_1/2D_SIDE_2) 중 하나를 선택할 수 있다"
    - "선택 슬롯에 면별 이미지를 로드하고 티칭하면 해당 슬롯의 Bottom_{slot}_1/2.shm + .json 이 생성된다"
    - "각 슬롯의 HasTemplate 상태(티칭 유무)가 UI 에 표시된다"
    - "슬롯 전환 시 그 슬롯의 ROI/티칭상태로 컨텍스트가 바뀐다"
  artifacts:
    - path: "WPF_Example/Custom/UI/BottomVisionView.xaml"
      provides: "6슬롯 선택 위젯(ComboBox) + 그룹(3D/2D) 구분 UI"
      contains: "cmb_slot"
    - path: "WPF_Example/Custom/UI/BottomVisionView.xaml.cs"
      provides: "선택 슬롯을 TryTeach/Run/HasTemplate 에 전달, 슬롯별 ROI 보관"
      contains: "EBottomAlignSlot"
  key_links:
    - from: "BottomVisionView.TeachButton_Click"
      to: "Matcher.TryTeach(..., VIEW_MODE, _selectedSlot, ...)"
      via: "선택 슬롯 전달"
      pattern: "TryTeach.*_selectedSlot|TryTeach.*CurrentSlot"
    - from: "BottomVisionView.RefreshStatus"
      to: "Matcher.HasTemplate(VIEW_MODE, _selectedSlot)"
      via: "슬롯별 티칭 상태 조회"
      pattern: "HasTemplate.*Slot"
---

<objective>
BottomVisionView 를 단일 Bottom 티칭/검사에서 **6슬롯 면별** 워크플로우로 확장한다. 좌측 컨트롤 패널에 슬롯 선택 위젯을 추가하고, Teach/Run/HasTemplate 호출에 현재 선택 슬롯을 전달한다. 면별 이미지는 기존 Phase 61.1 오프라인 로더(폴더 열기/이전/다음)로 슬롯마다 따로 로드한다 (D-04).

Purpose: 작업자가 6종 지그 각각을 독립 티칭(6번)하고 슬롯별 상태를 확인할 수 있게 한다.
Output: 슬롯 선택 ComboBox + 슬롯 전환 핸들러 + 슬롯별 ROI 보관 + slot 인자를 전달하는 Teach/Run/HasTemplate.

**잠금 결정(CONTEXT.md):** D-01(6슬롯 2그룹 라벨), D-04(티칭=면별 이미지 파일 로드, Phase 61.1 로더 재사용), D-09(폴백 — 슬롯 메커니즘은 Plan 01 이 보장). **Claude 재량(CONTEXT):** 슬롯 위젯 = ComboBox(3D/2D 그룹 라벨), 슬롯별 ROI = `_roi1/_roi2` 단일쌍 → 슬롯별 Dictionary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/65-bottom-4jig-face-align-2026-06-25/65-CONTEXT.md

# 변경 대상 — 반드시 먼저 읽을 것
@WPF_Example/Custom/UI/BottomVisionView.xaml.cs
@WPF_Example/Custom/UI/BottomVisionView.xaml

<interfaces>
<!-- Plan 01 산출 slot API + 기존 BottomVisionView 의 변경 지점. 탐색 불필요. -->

Plan 01 산출 (WPF_Example/Custom/EthernetVision/EBottomAlignSlot.cs):
```csharp
namespace ReringProject.Setting
public enum EBottomAlignSlot {
  None=-1, Slot3DTop=0, Slot3DBottom=1, Slot2DTop=2, Slot2DBottom=3, Slot2DSide1=4, Slot2DSide2=5
}
public static class EBottomAlignSlotMap {   // (또는 enum 옆 static)
  public static string ToFileToken(EBottomAlignSlot slot);    // "3D_Top" 등, None→""
  public static EBottomAlignSlot FromAlignFace(int alignFace);
  public static string ToDisplayLabel(EBottomAlignSlot slot); // "3D_Top" 등, None→"(단일)"
}
```

Plan 01 산출 — 확장된 Matcher 시그니처 (slot 기본값 None):
```csharp
bool TryTeach(HImage img, double r1,c1,phi1,l1_1,l1_2, double r2,c2,phi2,l2_1,l2_2,
              EEthernetVisionMode mode, EBottomAlignSlot slot, out string error);
AlignResult Run(HImage img, EEthernetVisionMode mode, EBottomAlignSlot slot = EBottomAlignSlot.None);
bool HasTemplate(EEthernetVisionMode mode, EBottomAlignSlot slot = EBottomAlignSlot.None);
```

기존 BottomVisionView.xaml.cs 변경 지점 (현재 코드):
```csharp
private const EEthernetVisionMode VIEW_MODE = EEthernetVisionMode.Bottom;   // L26 — 유지
private RoiDefinition _roi1;   // L42 — 슬롯별로 분리 대상
private RoiDefinition _roi2;   // L43
private int _drawingSlot;      // L46 — (주의) 이건 ROI 드로잉 슬롯(1/2), 면 슬롯과 다른 개념. 이름 충돌 주의.

// TeachButton_Click (L208~255) 의 호출:
EthernetVisionHandler.Handle.Matcher.TryTeach(_viewer.CurrentImage,
    r1,c1,phi1,l1_1,l1_2, r2,c2,phi2,l2_1,l2_2, VIEW_MODE, out error);    // L236 → slot 추가
bool bHas = EthernetVisionHandler.Handle.Matcher.HasTemplate(VIEW_MODE);  // L244 → slot 추가

// RunButton_Click (L259~284) 의 호출:
AlignResult res = EthernetVisionHandler.Handle.Matcher.Run(_viewer.CurrentImage, VIEW_MODE);  // L268 → slot 추가

// RefreshStatus (L508~537):
bHasTemplate = EthernetVisionHandler.Handle.Matcher.HasTemplate(VIEW_MODE);  // L518 → slot 추가
```

기존 XAML Row 구조 (BottomVisionView.xaml):
```
Row 0: 헤더 / Row 1: lbl_status / Row 2: 카메라+로더(GroupBox) /
Row 3: 티칭(ROI 2개) GroupBox / Row 4: 검사 결과 / Row 5: 피커센터 캘 / Row 6: 여백
```
ScrollViewer > Grid(RowDefinitions 7행). 슬롯 선택은 Row 2(로더) 위 또는 Row 3(티칭) 헤더에 삽입.
ComboBox 다크 패널 스타일 — 기존 BottomActionButtonStyle 인접 배치, 라벨 흰색 대비 명시.
</interfaces>
</context>

<coding_rules>
- 모든 추가/수정 라인 끝 `//260626 hbk` + 한국어 사유.
- 삼항 `?:` 금지 → if-else. 헝가리언/기존 명명(`btn_`, `lbl_`, `cmb_`, `_camelCase` 필드).
- C# 7.2 (.NET 4.8) — 8.0+ 문법 금지.
- 전 서비스 호출 try/catch (기존 BottomVisionView 패턴: 예외 시 lbl_* 텍스트만 갱신, throw 금지).
- 함수 30~40줄.
- **회귀 0**: TrayVisionView 무변경. 기존 Grab/Live/Stop/피커캘/오프라인 로더 핸들러 동작 보존. MainView 검사 무관.
</coding_rules>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: XAML 에 6슬롯 선택 ComboBox 추가 (3D/2D 그룹 구분)</name>
  <files>WPF_Example/Custom/UI/BottomVisionView.xaml</files>
  <read_first>
    - WPF_Example/Custom/UI/BottomVisionView.xaml (전체 — Row 구조 + BottomActionButtonStyle)
  </read_first>
  <action>
    좌측 컨트롤 패널에 슬롯 선택 GroupBox 를 추가 (Row 2 카메라/로더 GroupBox 직전 또는 직후). RowDefinitions 에 행 1개 추가(`<RowDefinition Height="Auto"/>`) 하고 이후 Grid.Row 인덱스를 1씩 시프트 — **또는** 기존 Row 2(카메라/이미지 로더) GroupBox 내부 StackPanel 상단에 슬롯 선택 블록을 끼워 행 시프트를 피한다(권장 — 시프트 리스크 최소화).

    슬롯 선택 UI:
    ```xml
    <!-- 면 슬롯 선택 (D-01: 3D 2개 / 2D 4개) //260626 hbk -->
    <StackPanel Orientation="Horizontal" Margin="8,0,0,6">
      <TextBlock Text="면 슬롯:" VerticalAlignment="Center" Foreground="#334155" Margin="0,0,8,0"/>
      <ComboBox x:Name="cmb_slot" Width="180" SelectionChanged="SlotComboBox_SelectionChanged"/>
    </StackPanel>
    <TextBlock x:Name="lbl_slotStatus" Margin="8,0,0,4" FontSize="12"
               Foreground="#475569" Text="슬롯 선택 필요" TextWrapping="Wrap"/>
    ```
    ComboBox 항목은 코드비하인드(Task 2)에서 6개 슬롯을 그룹 라벨과 함께 채운다 (예: 표시 텍스트 `[3D] 3D_Top`, `[2D] 2D_SIDE_1`). XAML 에서는 빈 ComboBox + x:Name 만 선언.

    티칭 GroupBox(현 Row 3) 헤더 텍스트는 `"티칭 (선택 슬롯 · ROI 2개)"` 로 갱신해 슬롯 종속을 명시. //260626 hbk

    **회귀 가드:** 기존 위젯(btn_grab/btn_live/btn_stop/btn_openFolder/btn_prevImage/btn_nextImage/btn_drawRoi1/btn_drawRoi2/btn_teach/btn_run/chk_showRoi/chk_showEdge/캘 패널/ViewerHostBorder) x:Name·이벤트 핸들러 무변경. Grid 우측 뷰어 컬럼 무변경.
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal /nologo 2>&1 | grep -iE "error|Build succeeded"</automated>
  </verify>
  <done>
    cmb_slot ComboBox + lbl_slotStatus + SlotComboBox_SelectionChanged 핸들러 참조가 XAML 에 존재. 기존 위젯 x:Name 전부 보존. XAML 파싱/빌드 PASS (이벤트 핸들러는 Task 2 에서 코드비하인드 구현 — 같은 plan 내 동시 산출이므로 빌드는 Task 2 후 최종 PASS).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 코드비하인드 — 슬롯 선택/전환 + slot 인자 전달 + 슬롯별 ROI 보관</name>
  <files>WPF_Example/Custom/UI/BottomVisionView.xaml.cs</files>
  <read_first>
    - WPF_Example/Custom/UI/BottomVisionView.xaml.cs (전체)
    - WPF_Example/Custom/EthernetVision/EBottomAlignSlot.cs (Plan 01)
  </read_first>
  <action>
    (1) **필드 추가**:
    - `private EBottomAlignSlot _selectedSlot = EBottomAlignSlot.None;` //260626 hbk 현재 선택 면 슬롯
    - 슬롯별 ROI 보관: `private Dictionary<EBottomAlignSlot, RoiDefinition[]> _slotRois = new Dictionary<EBottomAlignSlot, RoiDefinition[]>();` //260626 hbk 슬롯별 ROI쌍 ( [0]=roi1, [1]=roi2 ). 기존 `_roi1/_roi2` 는 "현재 드로잉 중 임시" 로 유지하되, 티칭 확정 시 `_slotRois[_selectedSlot]` 에 저장.
    - `using System.Collections.Generic;` 이미 있음(확인). `ReringProject.Setting` using 이미 있음.

    (2) **ComboBox 채우기** (BottomVisionView_Loaded 또는 별도 PopulateSlotComboBox 헬퍼, Loaded 에서 호출):
    6개 슬롯을 순서대로 ItemsSource 또는 Items.Add. 표시 텍스트는 그룹 접두사 포함. 권장: `cmb_slot.Items.Add(new ComboBoxItem{ Content="[3D] 3D_Top", Tag=EBottomAlignSlot.Slot3DTop });` … 6개 (3D 2개 먼저, 2D 4개). Tag 에 enum 보관 → SelectionChanged 에서 꺼냄. 삼항 금지.
    초기 선택 없음(슬롯 미선택 = None) 또는 첫 항목 자동 선택 — 미선택을 기본으로(작업자가 명시 선택). lbl_slotStatus="슬롯 선택 필요".

    (3) **SlotComboBox_SelectionChanged 핸들러**:
    - 선택된 ComboBoxItem.Tag → `_selectedSlot` 갱신 (try/catch).
    - `_slotRois` 에 해당 슬롯 ROI 가 있으면 `_roi1/_roi2` 복원, 없으면 둘 다 null (새 티칭 대기). `_drawingSlot=0` 리셋.
    - `RefreshStatus()` 호출(슬롯별 HasTemplate 라벨 갱신). lbl_slotStatus 에 선택 슬롯 표시(`EBottomAlignSlotMap.ToDisplayLabel`).

    (4) **TeachButton_Click 수정** (L208):
    - 시작에 슬롯 가드: `if (_selectedSlot == EBottomAlignSlot.None) { lbl_teachStatus.Text="면 슬롯을 먼저 선택하세요"; return; }` //260626 hbk
    - TryTeach 호출(L236)에 `_selectedSlot` 인자 추가: `...VIEW_MODE, _selectedSlot, out error);`
    - HasTemplate(L244) → `HasTemplate(VIEW_MODE, _selectedSlot)`.
    - 티칭 성공 시 `_slotRois[_selectedSlot] = new RoiDefinition[]{ _roi1, _roi2 };` 저장. //260626 hbk 슬롯 ROI 보관

    (5) **RunButton_Click 수정** (L268): Run 호출에 `_selectedSlot` 추가: `Run(_viewer.CurrentImage, VIEW_MODE, _selectedSlot)`. (슬롯 None 이면 Plan 01 폴백 = 기존 단일 — UI 에서 None 도 허용하되 lbl 안내. 가드는 선택사항: None 일 때 "단일(폴백) 검사" 로그만.)

    (6) **RefreshStatus 수정** (L518): `HasTemplate(VIEW_MODE, _selectedSlot)`. 슬롯 None 이면 단일 경로 상태 표시. lbl_teachStatus 문구에 슬롯 라벨 포함(`"[3D_Top] 티칭 OK"` 등).

    **회귀 가드:** Grab/Live/Stop/오프라인 로더(OpenFolder/Prev/Next/LoadCurrentLoaderImage)/피커캘(Cal*) 핸들러 본문 무변경. ApplyAlignVisualization/ClearAlignVisualization 무변경(시각화는 Phase 61.1 그대로). RectToTeachParams/ValidateRois/FormatAlignResult 무변경. `_drawingSlot`(ROI 드로잉 1/2 슬롯) 의미 유지 — 면 슬롯 `_selectedSlot` 과 혼동 금지.
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal /nologo 2>&1 | grep -iE "error|Build succeeded"</automated>
  </verify>
  <done>
    `_selectedSlot` + `_slotRois` 필드 존재. ComboBox 6슬롯 채워지고 SelectionChanged 가 _selectedSlot 갱신+ROI 복원+RefreshStatus 호출. TryTeach/Run/HasTemplate 4개 호출 전부 _selectedSlot 전달. 슬롯 미선택 시 티칭 가드. 기존 핸들러(로더/캘/Grab) 본문 무변경. msbuild Debug/x64 PASS.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 작업자→UI (슬롯 선택) | 신뢰 입력(로컬 운영자). ComboBox enum Tag 만 수용 → 임의 슬롯값 불가. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-03 | Tampering | cmb_slot 선택 | accept | UI ComboBox 는 6개 고정 enum Tag 만 노출. 외부 입력 경로 아님(로컬 운영). 저위험 — 추가 검증 불필요. |
| T-65-04 | DoS | 슬롯 미선택 상태 티칭 | mitigate | TeachButton_Click 이 `_selectedSlot==None` 가드로 조기 반환 → 의도치 않은 단일경로 덮어쓰기 방지. |
</threat_model>

<verification>
- msbuild Debug/x64 PASS.
- ComboBox 에 6슬롯(3D 2 + 2D 4) 항목 + 그룹 라벨 표시.
- 슬롯 전환 시 _selectedSlot 갱신 + 해당 슬롯 ROI/HasTemplate 컨텍스트 전환 (코드 리뷰).
- TryTeach/Run/HasTemplate 호출 4곳 모두 _selectedSlot 전달 (grep).
- 기존 핸들러/TrayVisionView 무변경.
</verification>

<success_criteria>
- 6슬롯 선택 + 슬롯별 면 이미지 로드(기존 로더) + 슬롯별 독립 티칭/Run/HasTemplate.
- 슬롯 전환 시 ROI/티칭상태 컨텍스트 분리.
- 기존 Bottom 단일/Tray/로더/피커캘 회귀 0.
- Debug/x64 빌드 PASS.
</success_criteria>

<output>
완료 후 `.planning/phases/65-bottom-4jig-face-align-2026-06-25/65-02-SUMMARY.md` 생성.
</output>
