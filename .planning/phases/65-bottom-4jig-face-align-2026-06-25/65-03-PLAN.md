---
phase: 65-bottom-4jig-face-align-2026-06-25
plan: 03
type: execute
wave: 2
depends_on: ["65-01"]
files_modified:
  - WPF_Example/Custom/SystemHandler.cs
autonomous: true
requirements: [AV-08]

must_haves:
  truths:
    - "$ALIGN_TEST(BOTTOM, AlignFace 0~5) 수신 시 해당 슬롯 모델로 grab + Matcher.Run 을 수행한다"
    - "Run 결과 pose(OffsetX/OffsetY/Theta)가 AlignResultPacket.Items 에 채워지고 IsPass 가 Found 로 설정된다"
    - "AlignFace 가 0~5 외(음수/6이상)이거나 슬롯 미티칭이면 안전하게 NG(IsPass=false)로 거부+로그한다"
    - "TRAY 경로 및 AlignFace echo / $ALIGN_RESULT 와이어 포맷은 무변경 (회귀 0)"
  artifacts:
    - path: "WPF_Example/Custom/SystemHandler.cs"
      provides: "ProcessAlignTest 의 실측 grab+Run+pose 채움 (stub 교체)"
      contains: "Matcher.Run"
  key_links:
    - from: "SystemHandler.ProcessAlignTest"
      to: "EthernetVisionHandler.Handle.Matcher.Run(img, Bottom, slot)"
      via: "AlignFace→슬롯 매핑 후 grab+Run"
      pattern: "Matcher\\.Run"
    - from: "AlignResult"
      to: "AlignResultPacket.Items (OffsetX/OffsetY/Theta)"
      via: "pose 직렬화 채움"
      pattern: "Items\\.Add"
---

<objective>
SystemHandler.ProcessAlignTest 의 stub(현재 `IsPass=true` echo만)을 **실측 결과**로 교체한다. `$ALIGN_TEST(BOTTOM, AlignFace 0~5)` 수신 시: AlignFace → 슬롯 매핑 → 이더넷 카메라 grab(SIMUL=폴백) → 해당 슬롯 모델로 `Matcher.Run` → 보정 pose(X/Y/Theta) + pass/fail 을 `AlignResultPacket` 에 채워 반환. PLC 가 이 pose 로 자재를 ideal 안착시킨다 (D-06).

Purpose: 6슬롯 면별 align 의 런타임 경로(Phase 64 가 미룬 "면별 라우팅")를 완성한다.
Output: grab+Run+pose 를 채우는 ProcessAlignTest. **TCP 파서/echo/$ALIGN_RESULT 포맷은 이미 완료 → 무변경** (D-03/D-08).

**잠금 결정(CONTEXT.md):** D-03(AlignFace 0~5 매핑 권위·고정), D-05(런타임 Run=라이브 grab, SIMUL=파일 폴백), D-06(grab→Run→pose+pass/fail), D-07(반환 그릇=AlignResultPacket.Items + IsPass + AlignFace echo), D-08(와이어 포맷 v3.0 준수, 발명 금지). **AlignFace echo + $ALIGN_RESULT pose 포맷은 이미 구현됨 — 이 plan 은 Items 채움 + IsPass=Found 만 추가.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/65-bottom-4jig-face-align-2026-06-25/65-CONTEXT.md

# 변경 대상 — 반드시 먼저 읽을 것
@WPF_Example/Custom/SystemHandler.cs

<interfaces>
<!-- ProcessAlignTest 가 호출할 모든 계약을 그대로 전달. 탐색 불필요. -->

현재 ProcessAlignTest (WPF_Example/Custom/SystemHandler.cs L227-248) — 교체 대상:
```csharp
private AlignResultPacket ProcessAlignTest(AlignTestPacket packet) {
    AlignResultPacket resultPacket = new AlignResultPacket();
    bool bHasPacket = packet != null;
    if (!bHasPacket) { return null; }
    resultPacket.Target = packet.Sender;
    resultPacket.AlignTarget = packet.AlignTarget;
    resultPacket.MaterialNo = packet.MaterialNo;
    resultPacket.AlignFace = packet.AlignFace;     // echo (무변경)
    resultPacket.IsPass = true;                     // [stub] ← 실측 결과로 교체
    bool bIsBottom = packet.AlignTarget == "BOTTOM";
    if (bIsBottom) { Logging.PrintLog(...); }
    return resultPacket;
}
```

수신 패킷 (VisionRequestPacket.cs):
```csharp
class AlignTestPacket { string AlignTarget; int MaterialNo=-1; int AlignFace=-1; string Sender; }
//  AlignTarget = "TRAY" / "BOTTOM". AlignFace 0~5 (BOTTOM 전용, TRAY=-1).
```

응답 패킷 (VisionResponsePacket.cs L883-892) — Items 가 직렬화 소스:
```csharp
class AlignResultPacket : VisionResponsePacket {
    string AlignTarget; int MaterialNo=-1; int AlignFace=-1; bool IsPass=true;
    List<AlignResultItem> Items = new List<AlignResultItem>();
}
class AlignResultItem { string ItemName=""; double Value; bool IsPass=true; }
// BuildAlignItems (L563-580) 가 Items 를 "ItemName=Value" 로 직렬화:
//   $ALIGN_RESULT:BOTTOM,Mat,AlignFace,OK|NG,OffsetX=val,OffsetY=val,Theta=val@
//   → Items 에 ItemName="OffsetX"/"OffsetY"/"Theta" + Value 채우면 됨. 순서 = X,Y,Theta.
```

Run 결과 POCO (AlignResult.cs):
```csharp
class AlignResult {
    bool Found; double Score;
    double OffsetXmm; double OffsetYmm; double ThetaDeg; bool HasTheta;
    // (시각화 필드 HasDetection/DetectedContourXld 등 — TCP 경로에서는 미사용. XLD 는 Dispose 책임 주의)
}
```

서비스 진입점 (EthernetVisionHandler.cs):
```csharp
EthernetVisionHandler.Handle.Camera   // EthernetAlignCamera (Mode==None 이면 null)
EthernetVisionHandler.Handle.Matcher  // AlignShapeMatchService (항상 non-null)

// EthernetAlignCamera.Grab() (L78) — HImage 반환. IsOpen 이면 라이브 grab, 아니면 LoadFallbackImage()
//   → SIMUL/미연결 시 폴백 이미지 자동 반환 (D-05 충족, 별도 #if SIMUL 분기 불필요).
HImage img = EthernetVisionHandler.Handle.Camera.Grab();   // null 가능 → 가드
```

Plan 01 산출 (EBottomAlignSlot.cs):
```csharp
EBottomAlignSlot slot = EBottomAlignSlotMap.FromAlignFace(packet.AlignFace);  // 0~5→슬롯, 그외→None
AlignResult res = EthernetVisionHandler.Handle.Matcher.Run(img, EEthernetVisionMode.Bottom, slot);
bool bHas = EthernetVisionHandler.Handle.Matcher.HasTemplate(EEthernetVisionMode.Bottom, slot);
```
</interfaces>
</context>

<coding_rules>
- 모든 추가/수정 라인 끝 `//260626 hbk` + 한국어 사유.
- 삼항 `?:` 금지 → if-else. 헝가리언(`b` prefix bool, PascalCase). C# 7.2 (.NET 4.8) — 8.0+ 문법 금지.
- HALCON/grab/Run 호출 try/catch. **throw 금지** — 실패 시 IsPass=false + 빈/0 pose 반환(SequenceBase 패턴: TCP 스레드 크래시 방지).
- 함수 30~40줄 — ProcessAlignTest 가 길어지면 private 헬퍼(`FillAlignPose`, `RunBottomAlign`)로 분할.
- **회귀 0**: TRAY 경로(AlignFace=-1) 는 grab/Run 미수행 — 기존 ack(echo) 동작 그대로 유지. AlignFace echo / MaterialNo echo / $ALIGN_RESULT 포맷 코드 무변경. VisionRequestPacket/VisionResponsePacket 무수정.
- **HImage 수명**: grab 한 HImage 는 try/finally 에서 Dispose. Matcher.Run 이 내부 Clone 하지 않으면 Run 반환 후 Dispose. AlignResult.DetectedContourXld 가 non-null 이면 TCP 경로에서 미사용이므로 즉시 Dispose(누수 방지) — Phase 61.1 WR-01/02 HALCON 핸들누수 선례.
</coding_rules>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ProcessAlignTest 를 grab + 슬롯 Matcher.Run + pose 채움으로 교체</name>
  <files>WPF_Example/Custom/SystemHandler.cs</files>
  <read_first>
    - WPF_Example/Custom/SystemHandler.cs (L225-265 ProcessAlignTest/ProcessAlignCalib + L60-70 dispatch)
    - WPF_Example/Custom/EthernetVision/EBottomAlignSlot.cs (Plan 01 — FromAlignFace)
    - WPF_Example/Custom/EthernetVision/AlignResult.cs (pose 필드)
  </read_first>
  <action>
    ProcessAlignTest 본문을 교체 (시그니처/dispatch L64 무변경).

    골격:
    ```
    private AlignResultPacket ProcessAlignTest(AlignTestPacket packet) {
        AlignResultPacket resultPacket = new AlignResultPacket();
        if (packet == null) { return null; }
        resultPacket.Target      = packet.Sender;
        resultPacket.AlignTarget = packet.AlignTarget;
        resultPacket.MaterialNo  = packet.MaterialNo;   // echo (무변경)
        resultPacket.AlignFace   = packet.AlignFace;    // echo (무변경)

        bool bIsBottom = packet.AlignTarget == "BOTTOM";
        if (!bIsBottom) {
            // TRAY: 기존 동작 유지 = ack(echo). grab/Run 미수행 (Tray 면 라우팅은 범위 밖). //260626 hbk 회귀 0
            resultPacket.IsPass = true;
            return resultPacket;
        }

        // BOTTOM: AlignFace → 슬롯 매핑 (T-65-01 안전 거부) //260626 hbk
        EBottomAlignSlot slot = EBottomAlignSlotMap.FromAlignFace(packet.AlignFace);
        bool bSlotValid = (slot != EBottomAlignSlot.None);
        if (!bSlotValid) {
            Logging.PrintLog((int)ELogType.Error,
                "[ALIGN_TEST] AlignFace 범위 외 거부: {0} //260626 hbk", packet.AlignFace);
            resultPacket.IsPass = false;   // NG 안전 거부
            return resultPacket;
        }

        // 슬롯 모델 RunBottomAlign 위임 (grab+Run+pose 채움) //260626 hbk
        bool bPass = RunBottomAlign(slot, packet.AlignFace, resultPacket);
        resultPacket.IsPass = bPass;
        return resultPacket;
    }
    ```

    신규 private 헬퍼 `RunBottomAlign(EBottomAlignSlot slot, int alignFace, AlignResultPacket resultPacket)` → bool:
    - 슬롯 미티칭 가드: `if (!Matcher.HasTemplate(Bottom, slot)) { log; return false; }` //260626 hbk 미티칭 NG
    - Camera null 가드: `if (EthernetVisionHandler.Handle.Camera == null) { log "미연결"; return false; }` (SIMUL 에서도 Camera 객체는 Mode!=None 일 때 생성 — null 이면 진짜 미연결).
    - `HImage img = null;` try/finally:
      - `img = EthernetVisionHandler.Handle.Camera.Grab();` (Grab 내부가 SIMUL/미연결 폴백 처리 — D-05). null 이면 log+return false.
      - `AlignResult res = EthernetVisionHandler.Handle.Matcher.Run(img, EEthernetVisionMode.Bottom, slot);`
      - `if (!res.Found) { log; FillAlignPoseZero(resultPacket); return false; }`
      - 성공: `FillAlignPose(resultPacket, res);` → Items 에 OffsetX/OffsetY/Theta 추가. return true.
      - **HImage Dispose** (finally). **res.DetectedContourXld 가 non-null 이면 Dispose** (TCP 경로 미사용 — 누수 방지). //260626 hbk
    - 전체 try/catch (Exception) → log + return false (throw 금지).

    신규 private 헬퍼 `FillAlignPose(AlignResultPacket pkt, AlignResult res)`:
    - `pkt.Items.Clear();`
    - `pkt.Items.Add(new AlignResultItem{ ItemName="OffsetX", Value=res.OffsetXmm });`
    - `pkt.Items.Add(new AlignResultItem{ ItemName="OffsetY", Value=res.OffsetYmm });`
    - Theta 추가 (Bottom 은 res.HasTheta=true): `pkt.Items.Add(new AlignResultItem{ ItemName="Theta", Value=res.ThetaDeg });`
    - (ItemName 문자열은 BuildAlignItems 가 그대로 직렬화 → v3.0 스펙 `OffsetX=val,OffsetY=val,Theta=val` 와 정합. D-08 발명 금지 준수.)

    `FillAlignPoseZero(AlignResultPacket pkt)` (검출 실패 시): Items 에 OffsetX=0/OffsetY=0/Theta=0 추가 (PLC 가 형식 일관 수신 — pkt.IsPass=false 로 불량 신호). 또는 Items 비움 — **기존 스펙 확인 후** PLC 가 NG 시 pose 미수신을 허용하면 빈 Items, 아니면 0 채움. 보수적으로 0 채움 권장.

    Logging: 성공 시 `[ALIGN_TEST] BOTTOM slot={token} off=(X,Y) theta=Z pass` Trace 로그.

    **회귀 가드:** ProcessAlignCalib/ProcessAlive/ProcessPrep 등 인접 메서드 무변경. dispatch switch(L60-70) 무변경. AlignFace/MaterialNo echo 라인 의미 보존.
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal /nologo 2>&1 | grep -iE "error|Build succeeded"</automated>
  </verify>
  <done>
    ProcessAlignTest 가 BOTTOM+AlignFace 0~5 → FromAlignFace → grab → Matcher.Run(Bottom,slot) → FillAlignPose(Items=OffsetX/OffsetY/Theta) + IsPass=Found. AlignFace 범위 외/미티칭/미연결/검출실패 → IsPass=false 안전 거부+로그. HImage 및 DetectedContourXld Dispose. TRAY 경로·echo·$ALIGN_RESULT 포맷·dispatch 무변경. msbuild Debug/x64 PASS.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLC→Vision TCP ($ALIGN_TEST) | AlignFace 정수 외부 입력. 0~5 외 값이 슬롯 모델 로드/Run 으로 전달되면 잘못된 모델 매칭 또는 OOB. |
| Vision→PLC ($ALIGN_RESULT) | 보정 pose 신뢰 경계 — 검출 실패값을 유효 pose 로 오인하면 PLC 가 자재를 잘못 안착(물리 충돌 위험). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Tampering/DoS | ProcessAlignTest AlignFace 입력 | mitigate | FromAlignFace(0~5 외)=None → bSlotValid=false → IsPass=false 즉시 반환. 인덱스 산술 없음. enum 매핑만 → 배열 OOB 불가. |
| T-65-05 | Integrity | 검출 실패 pose 반환 | mitigate | res.Found=false 면 IsPass=false + pose=0 (또는 빈 Items). PLC 가 NG 시 안착 미실행하도록 신호 — 잘못된 보정값 미전송. |
| T-65-06 | DoS | grab/Run 예외 → TCP 스레드 크래시 | mitigate | RunBottomAlign 전체 try/catch → 예외 시 false 반환(throw 금지). HImage/XLD finally Dispose 로 누수 방지. |
</threat_model>

<verification>
- msbuild Debug/x64 PASS.
- BOTTOM AlignFace 0~5 → slot Run → Items(OffsetX/OffsetY/Theta) + IsPass=Found (코드 리뷰).
- AlignFace 6/-2 → IsPass=false + 로그, grab/Run 미수행 (코드 리뷰).
- 미티칭 슬롯 → HasTemplate=false → IsPass=false.
- TRAY 경로 grab/Run 미수행 = 기존 echo ack 유지.
- HImage + DetectedContourXld Dispose (누수 0).
- VisionRequestPacket/VisionResponsePacket/dispatch 무변경.
</verification>

<success_criteria>
- $ALIGN_TEST(BOTTOM, 0~5) → 슬롯 모델 실측 Run → pose+pass/fail 반환 (D-06/D-07).
- 와이어 포맷 v3.0 준수 (Items 채움만, BuildAlignItems 무변경 — D-08).
- AlignFace 범위 외/미티칭/실패 안전 거부 (T-65-01/05).
- TRAY/echo/포맷 회귀 0.
- Debug/x64 빌드 PASS.
</success_criteria>

<output>
완료 후 `.planning/phases/65-bottom-4jig-face-align-2026-06-25/65-03-SUMMARY.md` 생성.
</output>
