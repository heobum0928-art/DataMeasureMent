---
phase: 65-bottom-4jig-face-align-2026-06-25
plan: 04
type: execute
wave: 3
depends_on: ["65-01", "65-02", "65-03"]
files_modified: []
autonomous: false
requirements: [AV-08]

must_haves:
  truths:
    - "전체 솔루션이 Debug/x64 로 빌드된다 (3개 plan 통합 후 CS 오류 0)"
    - "6슬롯 면별 티칭 → 슬롯별 Bottom_{slot}_1/2.shm + .json 생성 → $ALIGN_TEST Run pose 반환이 실HW 에서 동작한다 (작업자 확인)"
    - "기존 Bottom 단일 / Tray align / MainView 검사가 회귀 없이 동작한다 (작업자 확인)"
  artifacts: []
  key_links: []
---

<objective>
Phase 65 통합 검증. Plan 01~03(서비스 6슬롯화 + UI + ProcessAlignTest Run)을 합친 뒤 Debug/x64 빌드를 확정하고, 실HW 6슬롯 티칭/Run/PLC 연동 + 회귀(단일 Bottom/Tray/검사)를 작업자가 실측 확인한다.

Purpose: 코드 통합 무결성(빌드)과 실 동작(UAT)을 분리 검증한다. 빌드는 자동, 실측 align/PLC 는 실 카메라·실 PLC·실 지그가 필요하므로 작업자 게이트.
Output: 통합 빌드 PASS 확인 + UAT 결과 기록.

**잠금 결정(CONTEXT.md):** D-09(기존 Bottom 단일 회귀 0), D-10(Tray 무변경). 빌드 PASS = 코드 task done 조건, 실측 UAT = 작업자/실HW 분리.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/65-bottom-4jig-face-align-2026-06-25/65-CONTEXT.md
@.planning/phases/65-bottom-4jig-face-align-2026-06-25/65-01-PLAN.md
@.planning/phases/65-bottom-4jig-face-align-2026-06-25/65-02-PLAN.md
@.planning/phases/65-bottom-4jig-face-align-2026-06-25/65-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 통합 빌드 확정 (Debug/x64) + 변경 파일 인벤토리</name>
  <files></files>
  <action>
    Plan 01~03 통합 후 전체 솔루션 빌드를 확정한다.
    (1) msbuild Debug/x64 클린 리빌드 — CS 오류 0 확인. 경고는 기존 baseline 수준 허용(신규 CS 경고 발생 시 원인 기록).
    (2) Phase 65 변경 파일 목록을 git diff 로 확인 — 예상 범위 내인지 검증:
        - 신규: WPF_Example/Custom/EthernetVision/EBottomAlignSlot.cs
        - 수정: AlignShapeMatchService.cs (경로헬퍼3 + 공개API4), BottomVisionView.xaml(.cs), Custom/SystemHandler.cs (ProcessAlignTest), DatumMeasurement.csproj (Compile Include)
        - 범위 밖 파일 변경 시 경고 — TrayVisionView / VisionRequestPacket / VisionResponsePacket / MainView 는 무변경이어야 함(회귀 가드).
    (3) 모든 변경 라인에 //260626 hbk 주석 존재 여부 스팟 확인.
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Rebuild /v:minimal /nologo 2>&1 | grep -iE "error|Build succeeded"</automated>
  </verify>
  <done>
    msbuild Debug/x64 Rebuild PASS (CS 오류 0). git diff 변경 파일이 5개 예상 범위 내(EBottomAlignSlot 신규 + AlignShapeMatchService/BottomVisionView/SystemHandler/csproj 수정). 범위 밖 변경 없음. 변경 라인 //260626 hbk 주석 확인.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 실HW 6슬롯 면별 Align UAT (작업자)</name>
  <what-built>
    - AlignShapeMatchService 6슬롯화: 슬롯별 Bottom_{slot}_1/2.shm + Bottom_{slot}.json (Bottom_3D_Top_*, Bottom_3D_Bottom_*, Bottom_2D_TOP_*, Bottom_2D_BOTTOM_*, Bottom_2D_SIDE_1_*, Bottom_2D_SIDE_2_*). slot 미지정 시 기존 Bottom 단일 폴백.
    - BottomVisionView: 면 슬롯 선택 ComboBox(3D 2 / 2D 4) + 슬롯별 면 이미지 로드(폴더/이전/다음) + 슬롯별 티칭/Run/HasTemplate.
    - SystemHandler.ProcessAlignTest: $ALIGN_TEST(BOTTOM, AlignFace 0~5) → 슬롯 grab + Matcher.Run → pose(OffsetX/OffsetY/Theta) + pass/fail 반환. AlignFace echo / $ALIGN_RESULT 포맷은 기존 그대로.
  </what-built>
  <how-to-verify>
    앱 종료 → 재빌드된 DatumMeasurement.exe 실행 (EthernetVisionMode=Bottom, FAI_1 레시피).

    **Test 1 — 6슬롯 면별 티칭 (D-01/D-02/D-04):**
    1. Bottom 비전 탭 → "면 슬롯" ComboBox 에서 `[3D] 3D_Top` 선택.
    2. "폴더 열기" 로 3D_Top 면 이미지 로드 (이전/다음 으로 적절한 프레임 선택).
    3. "ROI 1 그리기" → 드래그 → "ROI 2 그리기" → 드래그 → "티칭 저장". lbl_teachStatus 에 "[3D_Top] 티칭 OK (HasTemplate=True)" 표시 확인.
    4. 슬롯을 `[3D] 3D_Bottom` 으로 전환 → HasTemplate 라벨이 "티칭 없음" 으로 바뀌는지(슬롯 컨텍스트 분리) 확인. 다시 3D_Top 선택 시 "티칭 OK" 복원 확인.
    5. 6슬롯 전부 반복 티칭 (3D_Top/3D_Bottom/2D_TOP/2D_BOTTOM/2D_SIDE_1/2D_SIDE_2).
    6. 파일 확인: `D:\Data\Recipe\FAI_1\ETHERNET_ALIGN\` 에 `Bottom_3D_Top_1.shm`/`_2.shm`/`Bottom_3D_Top.json` … `Bottom_2D_SIDE_2_*` 6세트(18파일) 생성 확인. **특히 `Bottom_2D_SIDE_1.json` 파일명이 정상(`_1` 오치환 없음)인지 확인.**

    **Test 2 — 슬롯별 Run (D-06):**
    7. 각 슬롯 선택 → 자재 안착 후 "검사" → lbl_result 에 X/Y/Theta/Score 표시, 검출 ROI/에지 시각화 정상 확인.

    **Test 3 — PLC $ALIGN_TEST Run pose (D-03/D-06/D-07/D-08):**
    8. PLC 가 `$ALIGN_TEST:BOTTOM,<자재번호>,<모드>,<AlignFace 0~5>@` 송신.
    9. 비전 응답 `$ALIGN_RESULT:BOTTOM,<자재>,<AlignFace>,OK|NG,OffsetX=val,OffsetY=val,Theta=val@` 확인 — AlignFace echo 정확, pose 가 Test 2 의 UI Run 값과 일치, PLC 가 보정값으로 자재 ideal 안착.
    10. (음성 테스트) AlignFace=6 또는 미티칭 슬롯 송신 → NG 응답 + 로그 확인.

    **Test 4 — 회귀 (D-09/D-10):**
    11. Tray 비전 탭: 기존 Tray 티칭/Run 정상(무변경) 확인.
    12. (가능 시) 기존 Bottom 단일 레시피(Bottom_1/2.shm)로 slot 미지정 동작 회귀 확인.
    13. MainView 검사(Top/Side/Bottom 측정) 정상 동작 확인.
  </how-to-verify>
  <resume-signal>각 Test 결과(PASS/FAIL + 실측 pose 값)를 알려주세요. FAIL 이면 증상/로그 기재 → 핫픽스 또는 carry-over 결정.</resume-signal>
</task>

</tasks>

<verification>
- Task 1: msbuild Debug/x64 Rebuild PASS, 변경 파일 5개 범위 내, 범위 밖 0.
- Task 2: 작업자 실측 — 6슬롯 티칭 18파일 생성 / 슬롯 컨텍스트 분리 / 슬롯별 Run pose / PLC $ALIGN_RESULT 정합 / Tray·단일 Bottom·MainView 회귀 0.
</verification>

<success_criteria>
- 통합 Debug/x64 빌드 PASS.
- 6슬롯 면별 티칭 + 슬롯별 Run + PLC AlignFace 0~5 Run pose 반환 실HW 동작 (작업자 PASS).
- Tray / 기존 Bottom 단일 / MainView 검사 회귀 0 (작업자 PASS).
</success_criteria>

<output>
완료 후 `.planning/phases/65-bottom-4jig-face-align-2026-06-25/65-04-SUMMARY.md` 생성.
실측 UAT 미수행/일부 FAIL 시 carry-over 기록.
</output>
