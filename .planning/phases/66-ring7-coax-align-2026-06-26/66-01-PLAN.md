---
phase: 66-ring7-coax-align-2026-06-26
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
autonomous: true
requirements: [LIGHT-01]

must_haves:
  truths:
    - "검사 Shot>Light PropertyGrid 에 Ring7 조명(ON/OFF + 밝기)이 노출되어 Ring/Back/Bar/Ring7 4종을 자유 조합할 수 있다"
    - "검사 Shot>Light PropertyGrid 에 Coax 조명이 더 이상 노출되지 않는다 (숨김)"
    - "Coax INI 키(CoaxLight_Enabled/CoaxLight_Brightness)는 보존되어 기존 레시피 로드/저장이 깨지지 않는다 (하위호환)"
    - "$PREP(ApplyShotLights) 수신 시 Ring7Light_Enabled 인 Shot 은 LIGHT_RING7 그룹이 켜지고 밝기가 적용된다"
    - "$PREP Op==0(사이클 종료, TurnOffShotLights) 수신 시 RING7 을 포함한 검사 조명이 모두 소등된다 (점등/소등 대칭 — RING7 만 켜진 채 잔존하지 않음)"
    - "기존 Ring/Back/Side(BAR)/Coax 매핑 동작은 변경되지 않는다 (회귀 0)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs"
      provides: "Ring7Light_Enabled/Brightness 프로퍼티 + CoaxLight_* [Browsable(false)] 숨김"
      contains: "Ring7Light_Enabled"
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "ApplyShotLightsInternal 에 Ring7Light → LIGHT_RING7 점등 매핑 + TurnOffShotLights 에 RING7 소등 (점등/소등 대칭)"
      contains: "LIGHT_RING7"
  key_links:
    - from: "ShotConfig.Ring7Light_Enabled/Brightness"
      to: "InspectionSequence.ApplyShotLightsInternal"
      via: "shot.Ring7Light_Enabled 분기 → SetOnOff/SetLevel(LIGHT_RING7)"
      pattern: "Ring7Light_Enabled"
    - from: "$PREP Op==0 (InspectionSequence.TurnOffShotLights)"
      to: "LightHandler.LIGHT_RING7 소등"
      via: "SetOnOff(LIGHT_RING7, false) — 점등/소등 대칭"
      pattern: "LIGHT_RING7, false"
    - from: "ShotConfig.CoaxLight_Enabled"
      to: "PropertyGrid 숨김"
      via: "[Browsable(false)] 어트리뷰트"
      pattern: "Browsable\\(false\\)\\]\\s*\\n\\s*\\[Category\\(\"Light\\|Coax\"\\)\\]"
---

<objective>
검사(inspection) ShotConfig 의 조명 세트를 하드웨어 사양(Ring/Back/Bar/Ring7, shot별 자유 조합)과 정합시킨다. 두 가지 변경:
1. **Ring7 조명 추가** — ShotConfig 에 `Ring7Light_Enabled`(bool) + `Ring7Light_Brightness`(int) 프로퍼티를 추가하고, InspectionSequence.ApplyShotLightsInternal 에 `Ring7Light → LIGHT_RING7` 매핑 블록을 추가한다 (기존 4종과 동일 if-else 패턴).
2. **Coax 검사 숨김** — 검사 ShotConfig 의 `CoaxLight_*` 프로퍼티에 `[Browsable(false)]` 를 추가하여 검사 PropertyGrid(Shot>Light 탭)에서 숨긴다. INI 키는 보존되어 하위호환을 유지하고, 런타임 매핑 코드(CoaxLight → ALIGN_COAX)는 그대로 둔다.

Purpose: 검사 Shot>Light 탭이 HW 이미지(Ring/Back/Bar/Ring7)와 1:1 정합되고, 얼라인 카메라 전용 동축은 검사 화면에서 사라진다(동축 제어는 Plan 03 Align 창으로 이동). 이 Plan 은 검사 조명 워크스트림(LIGHT-01)으로, Align 동축 워크스트림(Plan 02/03)과 파일이 겹치지 않아 병렬 실행 가능하다.
Output: Ring7 필드 2개가 추가되고 Coax 가 숨겨진 ShotConfig + Ring7 매핑이 추가된 InspectionSequence.

**잠금 결정(CONTEXT.md):** D-01(Ring7Light_Enabled/Brightness 추가, `[Category("Light|Ring7")]`, 기존 4종 무변경), D-02(ApplyShotLights Ring7→LIGHT_RING7 매핑, Enabled→SetOnOff, Brightness→SetLevel, 기존 if-else 패턴), D-03(CoaxLight_* `[Browsable(false)]` 숨김, INI 키 보존, 필드/매핑 코드 유지).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-ring7-coax-align-2026-06-26/66-CONTEXT.md
@.planning/phases/66-ring7-coax-align-2026-06-26/66-PATTERNS.md

# 핵심 변경 대상 — 반드시 먼저 읽을 것
@WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
@WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs

<interfaces>
<!-- 실행자가 코드베이스를 탐색하지 않도록 핵심 계약을 그대로 전달. 코드 검증 완료(2026-06-26). -->

ShotConfig.cs 현재 조명 4종 필드 (L60-75 — 정확히 확인됨):
```csharp
// Multi-Light — Ring/Back/Coax/Side 조명 필드 8개
[Category("Light|Ring")]
public bool RingLight_Enabled { get; set; }
public int RingLight_Brightness { get; set; }

[Category("Light|Back")]
public bool BackLight_Enabled { get; set; }
public int BackLight_Brightness { get; set; }

[Category("Light|Coax")]
public bool CoaxLight_Enabled { get; set; }
public int CoaxLight_Brightness { get; set; }

[Category("Light|Side")]
public bool SideLight_Enabled { get; set; }
public int SideLight_Brightness { get; set; }
```

ShotConfig.cs 기존 [Browsable(false)] 사용 선례 (L30, L85 — Category 없이 단독):
```csharp
[Browsable(false)]
public List<FAIConfig> FAIList { get; private set; } = new List<FAIConfig>();
```

InspectionSequence.cs — 조명 메서드 위치 (2026-06-26 **재확인·라인 보정**. v3.0 TurnOffShotLights 신설로 기존 plan 라인번호가 ~10줄 밀려 있었음):

(A) **TurnOffShotLights()** — public, **L337-343**. $PREP Op==0(사이클 종료) → SystemHandler.TurnOffPrepLights 가 호출. 현재 **4종만 소등(RING7 누락)** → Task 3 에서 RING7 1줄 추가:
```csharp
//260626 hbk v3.0: $PREP Op==0(사이클 종료) → 전 조명 그룹 소등.
public void TurnOffShotLights()
{
    LightHandler.Handle.SetOnOff(LightHandler.LIGHT_RING, false);
    LightHandler.Handle.SetOnOff(LightHandler.LIGHT_BACK, false);
    LightHandler.Handle.SetOnOff(LightHandler.LIGHT_ALIGN_COAX, false);
    LightHandler.Handle.SetOnOff(LightHandler.LIGHT_BAR, false);
}   // ← LIGHT_BAR 소등 뒤, 메서드 닫는 } 앞에 RING7 소등 1줄 추가 = Task 3
```

(B) **ApplyShotLightsInternal(ShotConfig shot)** — private, **L349-390**. 주석 L345-348 + 4종 if-else(Enabled→SetOnOff(true)+SetLevel / else→SetOnOff(false)):
```csharp
//260625 hbk Phase 64 LIGHT-01 (D-10): ShotConfig 4종 조명 → LightHandler 5종 그룹 적용.
//  RingLight → RING / BackLight → BACK / CoaxLight → ALIGN_COAX / SideLight → BAR
private void ApplyShotLightsInternal(ShotConfig shot)
{
    if (shot.RingLight_Enabled) { SetOnOff(LIGHT_RING,true); SetLevel(LIGHT_RING, RingLight_Brightness); } else { SetOnOff(LIGHT_RING,false); }   // L351-359
    if (shot.BackLight_Enabled) { ...LIGHT_BACK... } else { ... }     // L361-369
    if (shot.CoaxLight_Enabled) { ...LIGHT_ALIGN_COAX... } else { ... }  // L371-379
    if (shot.SideLight_Enabled) { ...LIGHT_BAR... } else { SetOnOff(LIGHT_BAR,false); }   // L381-389 (SideLight if=L381, 닫는 }=L389)
}   // 메서드 닫는 } = L390 — Ring7 점등 블록은 L389 뒤·L390 앞 = Task 2
```

⚠ **라인 드리프트 주의(필수):** 위 절대 라인번호는 2026-06-26 기준이다. Task 2(ApplyShotLightsInternal 점등)와 Task 3(TurnOffShotLights 소등)이 **같은 파일**을 수정하므로 먼저 실행한 Task 의 삽입으로 이후 Task 의 라인이 밀린다. **절대 라인번호를 신뢰하지 말고 반드시 content-anchor**(메서드명 + "기존 마지막 라인 뒤, 메서드 닫는 } 앞")로 위치를 잡을 것.

LightHandler 그룹 상수 (이미 등록됨 — 변경 없음, 확인용):
```csharp
public const string LIGHT_RING7 = "RING7";   // WPF_Example/Custom/Device/LightHandler.cs:28, 등록 L69
// SetOnOff(string groupName, bool onOff) / SetLevel(string groupName, int level) — 기존 시그니처
```
</interfaces>
</context>

<coding_rules>
- 모든 추가/수정 라인 끝(또는 위)에 `//260626 hbk` + 한국어 사유 1줄 주석 필수.
- 삼항 연산자 `?:` 금지 → `if-else` (초보 가독성). 헝가리언/기존 명명 유지.
- ShotConfig 조명 프로퍼티는 **PascalCase + 언더스코어 패턴**(`Ring7Light_Enabled`/`Ring7Light_Brightness`) 유지 — ParamBase 리플렉션 INI 키 = 프로퍼티명. 기존 4종과 동일.
- C# 7.2 (.NET 4.8) — switch expression / nullable reference / record / using 선언 등 8.0+ 문법 금지.
- Allman 중괄호(InspectionSequence.cs 기존 스타일) — ShotConfig 는 기존 K&R 프로퍼티 한 줄 스타일 유지(파일 인접 코드 관행).
- **회귀 0**: 기존 Ring/Back/Side/Coax 필드와 매핑 동작은 byte-identical 유지. CoaxLight 필드 자체와 ALIGN_COAX 매핑 코드는 삭제 금지(숨김만).
- 함수 30~40줄 권장. ApplyShotLightsInternal 은 Ring7 블록 추가로 5종 ~50줄이 되나, 기존 4종 블록은 회귀 0(byte-identical) 의무가 있어 분할하지 않는다 — 동일 패턴의 평면 매핑 나열이라 가독성 영향 없음(예외 허용). TurnOffShotLights 는 RING7 1줄 추가 후 ~7줄로 유지.
</coding_rules>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ShotConfig 에 Ring7 필드 추가 + CoaxLight [Browsable(false)] 숨김 (D-01, D-03)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs (전체 — 특히 L30-31, L60-75, L85-86 의 [Browsable]/[Category] 패턴)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs:335-380 (CoaxLight_* 가 ALIGN_COAX 로 매핑되는 런타임 소비처 — 숨겨도 런타임은 동작함을 확인)
  </read_first>
  <action>
    ShotConfig.cs 의 조명 4종 블록(L60-75)을 두 군데 수정한다.

    (1) **D-01 — Ring7 필드 추가:** 현재 SideLight 블록(L73-75) **뒤**에 Ring7 블록을 삽입한다. (Coax 뒤가 아니라 4종 블록 마지막 = SideLight 뒤에 추가 — 그룹 표시 순서가 Ring/Back/Coax(숨김)/Side/Ring7 가 되며, Coax 가 숨겨지므로 노출 순서는 Ring/Back/Side/Ring7):
    ```csharp
    [Category("Light|Ring7")]   //260626 hbk Phase 66 D-01: Ring7 조명 추가(자유 조합) — Ring/Back/Bar/Ring7 4종
    public bool Ring7Light_Enabled { get; set; }   //260626 hbk Ring7 ON/OFF
    public int Ring7Light_Brightness { get; set; }   //260626 hbk Ring7 밝기 0~255
    ```

    (2) **D-03 — CoaxLight 숨김:** 기존 `[Category("Light|Coax")]`(L69) **위 줄**에 `[Browsable(false)]` 를 추가한다. Category 어트리뷰트와 필드 2개(CoaxLight_Enabled/CoaxLight_Brightness)는 **그대로 유지**(삭제 금지 — INI 키 보존 + ALIGN_COAX 매핑 코드가 계속 참조):
    ```csharp
    [Browsable(false)]   //260626 hbk Phase 66 D-03: 검사 PropertyGrid 에서 동축 숨김. INI 키/매핑 코드는 보존(하위호환). 동축 제어는 Align 창(Plan 03).
    [Category("Light|Coax")]
    public bool CoaxLight_Enabled { get; set; }
    public int CoaxLight_Brightness { get; set; }
    ```

    주의(PATTERNS.md 검증 완료): PropertyTools PropertyGrid 는 `[Browsable(false)]` 를 우선 적용하여 Category 와 무관하게 숨긴다. ParamBase.Save/Load 는 리플렉션으로 INI 를 별도 처리하므로 `[Browsable]` 과 무관하게 직렬화가 계속 동작한다 → 하위호환 유지.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && grep -c "Ring7Light_Enabled\|Ring7Light_Brightness" WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Ring7Light_Enabled" ShotConfig.cs` → `public bool Ring7Light_Enabled { get; set; }` 존재.
    - `grep "Ring7Light_Brightness" ShotConfig.cs` → `public int Ring7Light_Brightness { get; set; }` 존재.
    - `grep "Light|Ring7" ShotConfig.cs` → `[Category("Light|Ring7")]` 존재.
    - `[Browsable(false)]` 가 `[Category("Light|Coax")]` 바로 위 줄에 존재 (Coax 만 — Ring/Back/Side/Ring7 Category 에는 Browsable(false) 없음). 검증: `grep -B1 'Category("Light|Coax")' ShotConfig.cs` 출력에 `Browsable(false)` 포함.
    - CoaxLight_Enabled / CoaxLight_Brightness 프로퍼티가 **여전히 존재**(삭제되지 않음): `grep -c "CoaxLight_Enabled\|CoaxLight_Brightness" ShotConfig.cs` ≥ 2.
    - 추가/수정 라인에 `//260626 hbk` 주석 존재: `grep -c "260626 hbk" ShotConfig.cs` ≥ 3.
    - 삼항 `?:` 미사용(이 변경분에). switch expression / record 등 C# 8.0+ 문법 없음.
  </acceptance_criteria>
  <done>Ring7Light_Enabled/Ring7Light_Brightness 프로퍼티 + `[Category("Light|Ring7")]` 추가. CoaxLight 필드 위에 `[Browsable(false)]` 추가(필드 자체 유지). `//260626 hbk` 주석 ≥3개. 빌드는 Task 3 에서 통합 검증.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: InspectionSequence.ApplyShotLightsInternal 에 Ring7 → LIGHT_RING7 매핑 추가 (D-02)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs:318-390 (ApplyShotLights L321 / TurnOffShotLights L337-343 / ApplyShotLightsInternal L349-390 — 세 메서드 구분 확인)
    - WPF_Example/Custom/Device/LightHandler.cs:28,69 (LIGHT_RING7 상수 + 그룹 등록 — 이미 존재, 변경 없음)
  </read_first>
  <action>
    ApplyShotLightsInternal 메서드(현재 L349-390 — 드리프트 가능)에 Ring7 점등 매핑 블록을 추가한다.

    (1) **Ring7 if-else 블록 추가:** content-anchor = **SideLight(LIGHT_BAR) if-else 블록의 닫는 `}`(현재 L389) 뒤, ApplyShotLightsInternal 메서드 닫는 `}`(현재 L390) 앞**. ⚠ 절대 라인 신뢰 금지 — "SideLight/LIGHT_BAR else 블록 직후, 메서드 닫는 } 직전"으로 위치 확정. 기존 4종과 동일한 Enabled→SetOnOff+SetLevel / Disabled→SetOnOff(false) 패턴 (삼항 금지, Allman 중괄호):
    ```csharp

    if (shot.Ring7Light_Enabled)   //260626 hbk Phase 66 D-02: Ring7Light → LIGHT_RING7 매핑 추가(검사 조명 정합)
    {
        LightHandler.Handle.SetOnOff(LightHandler.LIGHT_RING7, true);   //260626 hbk Ring7 ON
        LightHandler.Handle.SetLevel(LightHandler.LIGHT_RING7, shot.Ring7Light_Brightness);   //260626 hbk Ring7 밝기
    }
    else
    {
        LightHandler.Handle.SetOnOff(LightHandler.LIGHT_RING7, false);   //260626 hbk Ring7 OFF
    }
    ```

    (2) **주석 라인 업데이트:** ApplyShotLightsInternal 메서드 위 주석(현재 L346, "RingLight → RING ..." 라인)의 매핑 목록에 Ring7 추가. 기존:
    `//  RingLight → RING / BackLight → BACK / CoaxLight → ALIGN_COAX / SideLight → BAR`
    →
    `//  RingLight → RING / BackLight → BACK / CoaxLight → ALIGN_COAX / SideLight → BAR / Ring7Light → RING7   //260626 hbk Phase 66 Ring7 추가`

    기존 Ring/Back/Coax/Side 4종 블록은 **한 글자도 수정하지 말 것** (회귀 0). CoaxLight → ALIGN_COAX 매핑도 그대로 유지 (Coax 는 검사 PropertyGrid 에서만 숨겨질 뿐 런타임 매핑은 유지 — D-03).
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && grep -c "LIGHT_RING7" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Ring7Light_Enabled" InspectionSequence.cs` → `if (shot.Ring7Light_Enabled)` 존재.
    - ApplyShotLightsInternal 내에 `LIGHT_RING7` 3회: `SetOnOff(LightHandler.LIGHT_RING7, true)` + `SetLevel(LightHandler.LIGHT_RING7, shot.Ring7Light_Brightness)` + else 의 `SetOnOff(LightHandler.LIGHT_RING7, false)`. (TurnOffShotLights 소등 1회는 Task 3 에서 추가 → 파일 전체 합계는 Task 3 후 ≥4회.)
    - 기존 LIGHT_RING / LIGHT_BACK / LIGHT_ALIGN_COAX / LIGHT_BAR 블록 4개 모두 그대로 존재(회귀 0): `grep -c "LIGHT_ALIGN_COAX" InspectionSequence.cs` 변경 전과 동일(≥1).
    - 추가 라인에 `//260626 hbk` 주석 존재: 이 변경분에서 `260626 hbk` ≥ 4개.
    - 삼항 `?:` 미사용. C# 8.0+ 문법 없음. Allman 중괄호.
  </acceptance_criteria>
  <done>ApplyShotLightsInternal 에 Ring7Light → LIGHT_RING7 if-else 블록 추가 + 주석 매핑 목록 갱신. 기존 4종 블록 무변경. 빌드는 Task 3 에서 통합 검증.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: InspectionSequence.TurnOffShotLights 에 RING7 소등 추가 — 점등/소등 대칭 (v3.0 $PREP OFF 갭)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs:335-390 (TurnOffShotLights L337-343 소등 경로 + ApplyShotLightsInternal L349-390 점등 경로 — 양 경로 대조)
    - WPF_Example/Custom/Device/LightHandler.cs:28 (LIGHT_RING7 상수 — 이미 존재, 변경 없음)
  </read_first>
  <action>
    **배경:** v3.0 프로토콜에서 `$LIGHT` 명령이 폐기되고 조명 ON/OFF 가 `$PREP` Op 필드로 통합됨(커밋 7e8f7c6). `$PREP:site,z_index,0@`(사이클 종료) 수신 시 `SystemHandler.TurnOffPrepLights` → `InspectionSequence.TurnOffShotLights()` 가 조명을 소등한다. 현재 TurnOffShotLights 는 RING/BACK/ALIGN_COAX/BAR **4종만** 소등한다. Task 2 가 점등 경로(ApplyShotLightsInternal)에 RING7 을 추가하므로, 소등 경로에 RING7 이 빠지면 **사이클 종료($PREP Op==0) 후 RING7 만 켜진 채 잔존하는 ON/OFF 비대칭 버그**가 발생한다.

    **수정:** TurnOffShotLights() 본문에서 기존 `LIGHT_BAR` 소등 라인 **뒤**, 메서드 닫는 `}` **앞**에 RING7 소등 1줄을 추가한다. ⚠ content-anchor 사용("TurnOffShotLights 본문 LIGHT_BAR, false 라인 직후") — Task 2 가 같은 파일을 수정해 라인이 드리프트할 수 있으므로 절대 라인 신뢰 금지. 기존 4종 소등 라인은 무변경(회귀 0):
    ```csharp
            LightHandler.Handle.SetOnOff(LightHandler.LIGHT_RING7, false);   //260626 hbk Phase 66: Ring7 소등 정합 — 점등(ApplyShotLightsInternal)/소등 대칭
    ```
    수정 후 TurnOffShotLights 는 RING/BACK/ALIGN_COAX/BAR/RING7 **5종**을 소등한다. 삼항 분기 없음(단순 호출 1줄, Allman 유지).
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && grep -A7 "void TurnOffShotLights" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs | grep -c "LIGHT_RING7"</automated>
  </verify>
  <acceptance_criteria>
    - TurnOffShotLights() 본문에 `SetOnOff(LightHandler.LIGHT_RING7, false)` 1회 존재: `grep -A7 "void TurnOffShotLights" InspectionSequence.cs` 출력에 `LIGHT_RING7` 포함(=1).
    - 기존 4종 소등 라인(LIGHT_RING / LIGHT_BACK / LIGHT_ALIGN_COAX / LIGHT_BAR 의 false) 모두 그대로 존재 — RING7 라인은 LIGHT_BAR 소등 **뒤**에 위치.
    - 파일 전체 `grep -c "LIGHT_RING7" InspectionSequence.cs` ≥ 4 (점등 ApplyShotLightsInternal 3회 + 소등 TurnOffShotLights 1회).
    - 추가 라인에 `//260626 hbk` 주석 존재.
    - 삼항 `?:` 미사용. C# 8.0+ 문법 없음. Allman 중괄호 유지.
  </acceptance_criteria>
  <done>TurnOffShotLights 에 `SetOnOff(LIGHT_RING7, false)` 1줄 추가(LIGHT_BAR 뒤). 점등/소등 RING7 대칭 확보. 기존 4종 소등 무변경. 빌드는 Task 4 에서 통합 검증.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: 빌드 검증 (Debug/x64) + 회귀 0 확인</name>
  <files>WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs, WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs (Task 1 결과)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (Task 2 점등 + Task 3 소등 결과)
  </read_first>
  <action>
    Task 1+2+3 변경분을 통합 빌드로 검증한다. 신규 파일이 없으므로 csproj 수정은 불필요(기존 ShotConfig.cs/InspectionSequence.cs 만 수정 — 이미 Compile 등록됨).

    msbuild Debug/x64 빌드를 실행하고 error 0 을 확인한다. 빌드 실패 시 컴파일 오류를 수정(프로퍼티 누락/오타/중괄호 불일치)한 뒤 재빌드.

    회귀 점검: 빌드 후 ShotConfig 의 기존 4종 조명 필드(Ring/Back/Coax/Side)와 InspectionSequence 의 기존 4종 매핑 블록이 무변경인지 git diff 로 확인(추가 라인만 존재, 기존 라인 삭제/수정 0).
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal /nologo 2>&1 | grep -iE "error|Build succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 출력에 `Build succeeded` 포함, `error` 0건.
    - `git diff --stat` 가 ShotConfig.cs + InspectionSequence.cs 2파일만 변경으로 표시.
    - `git diff WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 가 기존 LIGHT_RING/LIGHT_BACK/LIGHT_ALIGN_COAX/LIGHT_BAR 블록 라인을 삭제(`-`)하지 않음 — 추가 라인 `+` 만(ApplyShotLightsInternal Ring7 점등 블록 + 주석 갱신 1줄 + TurnOffShotLights RING7 소등 1줄).
  </acceptance_criteria>
  <done>msbuild Debug/x64 PASS (error 0, Build succeeded). 변경 2파일만. 기존 4종 조명 매핑 회귀 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (없음 — 신규 경계 도입 없음) | 이 Plan 은 로컬 신뢰 I/O 만: ShotConfig 프로퍼티(ParamBase INI 직렬화, Phase 5+ 기존) + LightHandler 시리얼/COM 조명 제어(기존 ApplyShotLights 경로). 신규 네트워크 엔드포인트/인증/외부 입력 파싱 없음. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Tampering | ShotConfig INI (Ring7Light 키) | accept | 로컬 신뢰 레시피 파일. 누락 키 = ParamBase 기본값(false/0) 안전 로드(기존 하위호환 패턴). 신규 공격면 0. |
| T-66-02 | Denial of Service | LightHandler.SetOnOff/SetLevel(LIGHT_RING7) | accept | 기존 4종 조명과 동일 경로. Ring7 그룹은 Phase 64 에서 이미 등록됨. 신규 채널/배선 없음(D-08). |

신규 high/medium 위협 없음 — 표면은 로컬 신뢰 I/O(INI 직렬화 + 기존 조명 제어 경로 재사용)이며, 인젝션 벡터/신규 네트워크 표면/인증 변경 없음.
</threat_model>

<verification>
- msbuild Debug/x64 PASS (error 0).
- ShotConfig: Ring7Light_Enabled/Brightness 프로퍼티 + `[Category("Light|Ring7")]` 존재. CoaxLight_* 위 `[Browsable(false)]` 존재, 필드 자체 보존.
- InspectionSequence: ApplyShotLightsInternal 에 Ring7Light → LIGHT_RING7 점등 if-else 블록 + 주석 갱신. TurnOffShotLights 에 RING7 소등 1줄(점등/소등 대칭). 기존 4종 점등/소등 매핑 무변경.
- 모든 변경 라인에 `//260626 hbk` 주석.
- 기존 Ring/Back/Side/Coax 동작 회귀 0 (git diff 추가 라인 only).
</verification>

<success_criteria>
검사 Shot>Light 탭이 Ring/Back/Bar/Ring7 4종 자유 조합을 노출하고(Coax 숨김), $PREP(ApplyShotLights)에서 Ring7Light_Enabled Shot 이 LIGHT_RING7 을 제어하며, $PREP Op==0(사이클 종료, TurnOffShotLights)에서 RING7 포함 전 조명이 소등된다(점등/소등 대칭). Coax INI 키 보존(하위호환). msbuild Debug/x64 PASS. 기존 조명/검사 회귀 0.
</success_criteria>

<output>
완료 후 `.planning/phases/66-ring7-coax-align-2026-06-26/66-01-SUMMARY.md` 생성.
</output>
