---
phase: 66-ring7-coax-align-2026-06-26
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/EthernetVision/AlignRefPose.cs
  - WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs
  - WPF_Example/Custom/SystemHandler.cs
autonomous: true
requirements: [AV-08]

must_haves:
  truths:
    - "Align 슬롯/Tray 레퍼런스 JSON(Bottom_{slot}.json / Tray.json)에 동축 조명 값(CoaxEnabled/CoaxLevel)이 함께 저장된다"
    - "기존 JSON(동축 키 부재 = 구 레시피)을 로드해도 CoaxEnabled=false / CoaxLevel=0 으로 안전 로드되어 티칭 데이터가 깨지지 않는다 (하위호환)"
    - "동축 값을 저장할 때 기존 티칭 데이터(Ref1Row/Col 등)는 덮어쓰이지 않고 보존된다 (load-merge-save)"
    - "$ALIGN_TEST Run(RunBottomAlign) 시 해당 슬롯 저장 동축값이 grab 직전 자동 적용된다 (CoaxEnabled=true 면 ON+밝기, false 면 OFF)"
    - "동축 적용 중 예외/미티칭 슬롯에서도 TCP 스레드가 크래시하지 않고 동축 OFF 로 안전 폴백한다"
  artifacts:
    - path: "WPF_Example/Custom/EthernetVision/AlignRefPose.cs"
      provides: "CoaxEnabled(bool)/CoaxLevel(int) POCO 필드"
      contains: "CoaxEnabled"
    - path: "WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs"
      provides: "public GetSlotRefPose(mode,slot) 로드 래퍼 + public TrySaveCoax(mode,slot,enabled,level) load-merge-save"
      contains: "GetSlotRefPose"
    - path: "WPF_Example/Custom/SystemHandler.cs"
      provides: "ApplyCoaxLightForSlot(slot) 헬퍼 + RunBottomAlign grab 직전 호출"
      contains: "ApplyCoaxLightForSlot"
  key_links:
    - from: "SystemHandler.RunBottomAlign"
      to: "ApplyCoaxLightForSlot(slot) → grab"
      via: "grab 직전 동축 자동 적용"
      pattern: "ApplyCoaxLightForSlot"
    - from: "SystemHandler.ApplyCoaxLightForSlot"
      to: "Matcher.GetSlotRefPose(Bottom, slot) → LIGHT_ALIGN_COAX"
      via: "슬롯 JSON 동축값 읽어 SetOnOff/SetLevel"
      pattern: "GetSlotRefPose|LIGHT_ALIGN_COAX"
    - from: "AlignShapeMatchService.TrySaveCoax"
      to: "Bottom_{slot}.json / Tray.json"
      via: "LoadRefPose → Coax 필드 덮어쓰기 → 직렬화"
      pattern: "TrySaveCoax"
---

<objective>
Align 동축 조명 워크스트림(AV-08)의 **데이터/런타임 백엔드**를 만든다. 3개 파일:
1. **AlignRefPose POCO** — 슬롯/Tray 레퍼런스 JSON 스키마에 `CoaxEnabled`(bool) + `CoaxLevel`(int) 필드를 추가한다 (Phase 61.1 Roi1Len1 추가 선례와 동일). 키 부재 = false/0 자동 폴백 → 하위호환.
2. **AlignShapeMatchService 공개 API** — TCP 경로(SystemHandler)와 UI(Plan 03)가 슬롯 JSON 의 동축값을 읽고 쓸 수 있도록 두 public 메서드를 추가한다: `GetSlotRefPose`(로드 래퍼) + `TrySaveCoax`(load-merge-save). 기존 private `BuildJsonPath`/`LoadRefPose`/`TrySaveRefPose` 는 무변경.
3. **SystemHandler.RunBottomAlign 자동 적용** — `$ALIGN_TEST` Run 시 해당 슬롯 저장 동축값을 grab 직전 자동 ON 하는 `ApplyCoaxLightForSlot(slot)` 헬퍼를 추가하고, RunBottomAlign 의 `Camera.Grab()` 직전에 호출한다 (D-06, 검사 ApplyShotLights 패턴 일관).

Purpose: 슬롯 하나로 모델+레퍼런스+조명을 묶고(D-05), 티칭 조명 = 런타임 조명 일치를 보장하여 매칭 스코어를 안정화한다(D-07). UI(Plan 03)가 이 공개 API 를 소비한다. 이 Plan 은 Plan 01(검사 조명)과 파일이 겹치지 않아 같은 wave 에서 병렬 실행 가능하다.
Output: Coax 필드가 추가된 AlignRefPose + GetSlotRefPose/TrySaveCoax 가 추가된 AlignShapeMatchService + ApplyCoaxLightForSlot 자동 적용이 추가된 SystemHandler.

**잠금 결정(CONTEXT.md):** D-04(동축 = LIGHT_ALIGN_COAX 단일 채널), D-05(저장 단위 = Bottom 6슬롯별 동축값을 Bottom_{slot}.json 에 필드 추가, Tray 는 Tray.json 단일, Phase 65 JSON 경로 재사용), D-06(런타임 = 비전 PC 자동, RunBottomAlign grab 직전 자동 ON, PLC 미관여), D-07(티칭 조명 = 런타임 조명 일치).

**설계 노트 (PATTERNS.md "추가 설계 결정" 보강):** PATTERNS.md 는 `TrySaveRefPose` 시그니처에 coax 파라미터를 추가하고 티칭 호출부(TryTeach L349)를 갱신하는 안을 제시했으나, 본 Plan 은 대신 **public `TrySaveCoax`(load-merge-save)** 를 채택한다. 이유: (a) 임계 경로인 티칭 호출부(TryTeach)를 건드리지 않아 회귀 위험이 낮고, (b) UI 동축 토글 시 필요한 "기존 티칭 데이터 보존하며 동축만 갱신" 의미론(PATTERNS.md SaveSlotCoaxToJson 설명: LoadRefPose→Coax 덮어쓰기→재저장)을 정확히 구현하며, (c) 단일 공개 메서드로 Bottom 슬롯 + Tray 를 모두 처리한다. POCO 필드(CoaxEnabled/CoaxLevel)는 PATTERNS.md 와 동일.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-ring7-coax-align-2026-06-26/66-CONTEXT.md
@.planning/phases/66-ring7-coax-align-2026-06-26/66-PATTERNS.md

# 핵심 변경 대상 — 반드시 먼저 읽을 것
@WPF_Example/Custom/EthernetVision/AlignRefPose.cs
@WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs

# Phase 65 슬롯 구조 참고
@.planning/phases/65-bottom-4jig-face-align-2026-06-25/65-01-SUMMARY.md

<interfaces>
<!-- 코드 검증 완료(2026-06-26). 실행자는 탐색 없이 이 계약을 그대로 사용. -->

AlignRefPose.cs — namespace `ReringProject` (※ ReringProject.EthernetVision 아님). 순수 POCO, Newtonsoft.Json(TypeNameHandling.None) 직렬화. 기존 필드: Ref1Row/Col, Ref2Row/Col, RefBaselineRad, AngleExtentDeg, Engine, Roi1Len1/Len2, Roi2Len1/Len2 (L13-41). Phase 61.1 선례(L33-41)와 동일하게 필드 추가.

AlignShapeMatchService.cs — namespace `ReringProject`. `using ReringProject.Setting`(EBottomAlignSlot), `Newtonsoft.Json`, `System.IO`, `System`(Exception) 이미 존재. EthernetVisionHandler.Handle.Matcher 로 접근.
관련 private 멤버 (무변경, 호출만):
```csharp
// 슬롯 json 경로 — _1.shm 에서 마지막 _1 만 제거하여 .json 유도 (토큰 내부 _1 오치환 방지). L122
private string BuildJsonPath(EEthernetVisionMode mode, EBottomAlignSlot slot = EBottomAlignSlot.None);
// 사이드카 json 로드 — 파일 없거나 예외 시 null. TypeNameHandling.None. L201
private AlignRefPose LoadRefPose(string jsonPath);
// 사이드카 json 저장 — Indented + TypeNameHandling.None. L163 (무변경)
private bool TrySaveRefPose(string jsonPath, double ref1Row, ... , out string error);
// 직렬화 상수 패턴(TrySaveRefPose 내부): JsonSerializerSettings { TypeNameHandling=None; Formatting=Indented } + File.WriteAllText(path, json, Encoding.UTF8)
```
EBottomAlignSlot (namespace ReringProject.Setting): None=-1, Slot3DTop=0 … Slot2DSide2=5. EEthernetVisionMode: None=0, Tray=1, Bottom=2.

SystemHandler.cs (Custom) — namespace `ReringProject`. `using HalconDotNet`(L1), Logging/ELogType/LightHandler/EthernetVisionHandler 이미 사용 중. RunBottomAlign(L271-349) 내부 grab 직전 코드 (L299-300, 정확히 확인됨):
```csharp
//260626 hbk EthernetAlignCamera.Grab() — IsOpen 이면 라이브 grab, 아니면 폴백(D-05, SIMUL 지원)
img = EthernetVisionHandler.Handle.Camera.Grab();   // ← 이 줄 직전에 ApplyCoaxLightForSlot(slot) 삽입
```
RunBottomAlign 시그니처: `private bool RunBottomAlign(EBottomAlignSlot slot, AlignResultPacket pResult)`. catch 블록 패턴(L342-348): Logging.PrintLog((int)ELogType.Error, "...//260626 hbk", ex.Message) + throw 금지 + return false.

LightHandler 동축 그룹 상수 (이미 존재):
```csharp
public const string LIGHT_ALIGN_COAX = "ALIGN_COAX";   // WPF_Example/Custom/Device/LightHandler.cs:20, 등록 L72
// LightHandler.Handle.SetOnOff(group,bool) / SetLevel(group,int)
```
</interfaces>
</context>

<coding_rules>
- 모든 추가/수정 라인에 `//260626 hbk` + 한국어 사유 1줄 주석 필수.
- 삼항 연산자 `?:` 금지 → `if-else`. 헝가리언(`b`/`n`/`sz` prefix 지역변수, PascalCase 메서드/프로퍼티) — 인접 코드 관행 유지.
- C# 7.2 (.NET 4.8) — switch expression / nullable reference / record / using 선언 등 8.0+ 문법 금지.
- AlignShapeMatchService 는 Allman 중괄호 + 기존 try/catch 패턴(실패 시 return false / null, throw 금지) 유지.
- SystemHandler RunBottomAlign 경로는 TCP 스레드 — **throw 절대 금지**. 예외는 Logging 후 동축 OFF.
- **회귀 0**: 기존 TryTeach/Run/TrySaveRefPose/LoadRefPose/BuildJsonPath 시그니처·동작 무변경. RunBottomAlign 의 기존 grab/Run/Dispose 로직 무변경(동축 1줄 삽입만). AlignRefPose 기존 필드 무변경.
- 함수 30~40줄 유지. ApplyCoaxLightForSlot 은 약 25줄 — 단일 메서드.
</coding_rules>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AlignRefPose POCO 에 CoaxEnabled/CoaxLevel 필드 추가 (D-05)</name>
  <files>WPF_Example/Custom/EthernetVision/AlignRefPose.cs</files>
  <read_first>
    - WPF_Example/Custom/EthernetVision/AlignRefPose.cs (전체 — 특히 L33-41 Phase 61.1 필드 추가 선례)
  </read_first>
  <action>
    AlignRefPose.cs 의 마지막 필드(Roi2Len2, L41) **뒤**, 클래스 닫는 `}`(L42) **앞**에 동축 2필드를 추가한다 (Phase 61.1 Roi1Len1 선례와 동일 스타일 — XML doc 주석 + public 프로퍼티):
    ```csharp

    //260626 hbk Phase 66 D-05 — Align 동축 조명 저장. 키 부재(구 JSON) → false/0 자동 폴백(하위호환).
    /// <summary>Align 동축 조명 ON/OFF. 저장 시점 슬롯/Tray 설정값.</summary>
    public bool CoaxEnabled { get; set; }
    /// <summary>Align 동축 밝기 0~255. 저장 시점 설정값.</summary>
    public int CoaxLevel { get; set; }
    ```

    기존 필드(Ref1Row/Col … Roi2Len2)는 무변경. POCO 이므로 Newtonsoft.Json 이 구 JSON(동축 키 부재) 역직렬화 시 CoaxEnabled=false / CoaxLevel=0 자동 적용 → 하위호환 충족.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && grep -c "CoaxEnabled\|CoaxLevel" WPF_Example/Custom/EthernetVision/AlignRefPose.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "CoaxEnabled" AlignRefPose.cs` → `public bool CoaxEnabled { get; set; }` 존재.
    - `grep "CoaxLevel" AlignRefPose.cs` → `public int CoaxLevel { get; set; }` 존재.
    - 기존 필드 11개(Ref1Row/Col, Ref2Row/Col, RefBaselineRad, AngleExtentDeg, Engine, Roi1Len1/Len2, Roi2Len1/Len2) 모두 보존: `grep -c "public .* { get; set; }" AlignRefPose.cs` ≥ 13.
    - `grep -c "260626 hbk" AlignRefPose.cs` ≥ 1.
    - 삼항/8.0+ 문법 없음.
  </acceptance_criteria>
  <done>AlignRefPose 에 CoaxEnabled(bool)/CoaxLevel(int) 추가, 기존 필드 보존. `//260626 hbk` 주석. 빌드는 Task 4 통합 검증.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AlignShapeMatchService 에 public GetSlotRefPose + TrySaveCoax 추가 (D-05)</name>
  <files>WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs</files>
  <read_first>
    - WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs (L100-214 — BuildJsonPath/LoadRefPose/TrySaveRefPose 패턴 + 직렬화 상수)
    - WPF_Example/Custom/EthernetVision/AlignRefPose.cs (Task 1 결과 — CoaxEnabled/CoaxLevel)
  </read_first>
  <action>
    AlignShapeMatchService 에 두 개의 **public** 메서드를 추가한다 (private BuildJsonPath/LoadRefPose 가 TCP/UI 에서 직접 접근 불가하므로 공개 래퍼 필요 — PATTERNS.md 추가 설계 결정 #1). LoadRefPose 정의 뒤(L214) 또는 "사이드카 JSON 저장/로드" 리전 내에 삽입. Allman 중괄호, 삼항 금지, try/catch.

    (1) **public 로드 래퍼** — SystemHandler/UI 가 슬롯 동축값을 읽는 진입점:
    ```csharp
    //260626 hbk Phase 66 D-05 — 슬롯/Tray 레퍼런스 JSON 을 mode+slot 으로 로드(공개 래퍼).
    //  private BuildJsonPath + LoadRefPose 위임. 파일 없음/예외 → null (호출자 null-guard).
    public AlignRefPose GetSlotRefPose(EEthernetVisionMode mode, EBottomAlignSlot slot)
    {
        try
        {
            string jsonPath = BuildJsonPath(mode, slot);   //260626 hbk 슬롯/Tray json 경로
            if (jsonPath == null)
            {
                return null;   //260626 hbk 경로 산출 실패(RecipeSavePath 미설정 등)
            }
            return LoadRefPose(jsonPath);   //260626 hbk 기존 private 로더 재사용(키 부재 → Coax false/0)
        }
        catch
        {
            return null;   //260626 hbk 예외 시 null — 호출자가 동축 off 폴백
        }
    }
    ```

    (2) **public load-merge-save** — 동축값만 갱신하고 기존 티칭 데이터 보존:
    ```csharp
    //260626 hbk Phase 66 D-05 — 동축값만 슬롯/Tray JSON 에 반영(티칭 데이터 보존 load-merge-save).
    //  기존 refPose 로드 → Coax 필드만 덮어쓰기 → 재저장. 미티칭(json 없음) 슬롯은 새 refPose 로 동축만 기록.
    public bool TrySaveCoax(EEthernetVisionMode mode, EBottomAlignSlot slot,
        bool coaxEnabled, int coaxLevel, out string error)
    {
        error = null;
        try
        {
            string jsonPath = BuildJsonPath(mode, slot);   //260626 hbk 슬롯/Tray json 경로
            if (jsonPath == null)
            {
                error = "jsonPath 산출 실패";   //260626 hbk RecipeSavePath 미설정
                return false;
            }

            AlignRefPose refPose = LoadRefPose(jsonPath);   //260626 hbk 기존 티칭 데이터 로드(있으면 보존)
            if (refPose == null)
            {
                refPose = new AlignRefPose();   //260626 hbk 미티칭 슬롯 — 동축만 기록(티칭 데이터는 추후 TryTeach 가 채움)
            }

            refPose.CoaxEnabled = coaxEnabled;   //260626 hbk 동축 ON/OFF 갱신
            refPose.CoaxLevel = coaxLevel;       //260626 hbk 동축 밝기 갱신

            Directory.CreateDirectory(Path.GetDirectoryName(jsonPath));   //260626 hbk 폴더 부재 대비(미티칭 슬롯 쓰기)

            JsonSerializerSettings saveSettings = new JsonSerializerSettings();   //260626 hbk 기존 TrySaveRefPose 와 동일 설정
            saveSettings.TypeNameHandling = TypeNameHandling.None;
            saveSettings.Formatting = Formatting.Indented;
            string json = JsonConvert.SerializeObject(refPose, saveSettings);
            File.WriteAllText(jsonPath, json, System.Text.Encoding.UTF8);
            return true;
        }
        catch (Exception ex)
        {
            error = "TrySaveCoax: " + ex.Message;   //260626 hbk 직렬화/IO 실패
            return false;
        }
    }
    ```

    기존 BuildJsonPath/LoadRefPose/TrySaveRefPose/TryTeach/Run 은 **무변경**. `Path`/`Directory`/`JsonConvert`/`JsonSerializerSettings`/`Formatting`/`TypeNameHandling` 은 기존 using(System.IO, Newtonsoft.Json)으로 충분.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && grep -c "public AlignRefPose GetSlotRefPose\|public bool TrySaveCoax" WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "public AlignRefPose GetSlotRefPose(EEthernetVisionMode mode, EBottomAlignSlot slot)" AlignShapeMatchService.cs` 존재.
    - `grep "public bool TrySaveCoax(EEthernetVisionMode mode, EBottomAlignSlot slot" AlignShapeMatchService.cs` 존재.
    - TrySaveCoax 내부에 `refPose.CoaxEnabled = coaxEnabled` + `refPose.CoaxLevel = coaxLevel` + `Directory.CreateDirectory` + `File.WriteAllText` 존재.
    - 기존 private 시그니처 무변경: `grep -c "private string BuildJsonPath\|private AlignRefPose LoadRefPose\|private bool TrySaveRefPose" AlignShapeMatchService.cs` = 3.
    - `grep -c "260626 hbk" AlignShapeMatchService.cs` 가 변경 전보다 ≥ 8 증가(신규 2메서드 주석).
    - 삼항/8.0+ 문법 없음. try/catch 로 감싸고 throw 없음.
  </acceptance_criteria>
  <done>GetSlotRefPose(로드 래퍼) + TrySaveCoax(load-merge-save, Directory.CreateDirectory 포함) public 추가. 기존 private 메서드 무변경. `//260626 hbk` 주석. 빌드는 Task 4 통합 검증.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: SystemHandler.RunBottomAlign 에 grab 직전 동축 자동 적용 (D-06)</name>
  <files>WPF_Example/Custom/SystemHandler.cs</files>
  <read_first>
    - WPF_Example/Custom/SystemHandler.cs:262-350 (RunBottomAlign 전체 — grab 직전 L299-300, catch 패턴 L342-348)
    - WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs (Task 2 결과 — GetSlotRefPose)
    - WPF_Example/Custom/Device/LightHandler.cs:20,72 (LIGHT_ALIGN_COAX 상수)
  </read_first>
  <action>
    SystemHandler.cs(Custom)에 동축 자동 적용 헬퍼를 추가하고 RunBottomAlign 의 grab 직전에 호출한다.

    (1) **grab 직전 호출 삽입:** RunBottomAlign 내부 L300 `img = EthernetVisionHandler.Handle.Camera.Grab();` **바로 앞 줄**에 추가 (L299 주석과 L300 사이):
    ```csharp
    //260626 hbk Phase 66 D-06/D-07 — grab 직전 해당 슬롯 동축값 자동 적용(티칭=런타임 조명 일치)
    ApplyCoaxLightForSlot(slot);
    img = EthernetVisionHandler.Handle.Camera.Grab();
    ```
    기존 grab/Run/Dispose/catch 로직은 한 글자도 수정하지 말 것(회귀 0).

    (2) **신규 헬퍼 메서드 추가** — RunBottomAlign 메서드 닫는 `}`(L349 이후) 뒤에 삽입. Allman, 삼항 금지, try/catch(throw 금지 — TCP 스레드):
    ```csharp
    //260626 hbk Phase 66 D-06 — 슬롯 JSON 의 CoaxEnabled/CoaxLevel 을 읽어 LIGHT_ALIGN_COAX 적용.
    //  JSON 없음(미티칭)/null → 동축 off. 예외 → 로그 후 off (throw 금지, TCP 스레드 크래시 방지 T-66-01).
    private void ApplyCoaxLightForSlot(EBottomAlignSlot slot)
    {
        try
        {
            AlignRefPose refPose = EthernetVisionHandler.Handle.Matcher.GetSlotRefPose(EEthernetVisionMode.Bottom, slot);   //260626 hbk 슬롯 동축값 로드
            bool bEnabled = false;   //260626 hbk 기본 off (미티칭/null 안전)
            int nLevel = 0;
            if (refPose != null)
            {
                bEnabled = refPose.CoaxEnabled;   //260626 hbk 저장 동축 ON/OFF
                nLevel = refPose.CoaxLevel;       //260626 hbk 저장 동축 밝기
            }

            if (bEnabled)
            {
                LightHandler.Handle.SetOnOff(LightHandler.LIGHT_ALIGN_COAX, true);   //260626 hbk 동축 ON
                LightHandler.Handle.SetLevel(LightHandler.LIGHT_ALIGN_COAX, nLevel);   //260626 hbk 동축 밝기
            }
            else
            {
                LightHandler.Handle.SetOnOff(LightHandler.LIGHT_ALIGN_COAX, false);   //260626 hbk 동축 OFF
            }
        }
        catch (Exception ex)
        {
            Logging.PrintLog((int)ELogType.Error,
                "[ALIGN_TEST] ApplyCoaxLightForSlot 예외: {0} //260626 hbk", ex.Message);   //260626 hbk 로그 후 off
            LightHandler.Handle.SetOnOff(LightHandler.LIGHT_ALIGN_COAX, false);   //260626 hbk 예외 시 안전 off (throw 금지)
        }
    }
    ```

    `EEthernetVisionMode` / `EBottomAlignSlot` 는 `ReringProject.Setting` 네임스페이스 — SystemHandler.cs 가 이미 RunBottomAlign 에서 EBottomAlignSlot 을 사용하므로 using 충분(필요 시 `using ReringProject.Setting;` 존재 확인). `AlignRefPose` 는 `ReringProject` 네임스페이스(SystemHandler 와 동일) — using 불필요.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && grep -c "ApplyCoaxLightForSlot" WPF_Example/Custom/SystemHandler.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ApplyCoaxLightForSlot(slot);" SystemHandler.cs` 가 RunBottomAlign 내 grab 직전에 1회 호출로 존재.
    - `grep "private void ApplyCoaxLightForSlot(EBottomAlignSlot slot)" SystemHandler.cs` 헬퍼 정의 존재.
    - 헬퍼 내부에 `GetSlotRefPose(EEthernetVisionMode.Bottom, slot)` + `LIGHT_ALIGN_COAX` SetOnOff(true/false) + SetLevel + catch(throw 없음) 존재.
    - grab 호출 `img = EthernetVisionHandler.Handle.Camera.Grab();` 가 헬퍼 호출 **다음 줄**에 위치(순서: ApplyCoaxLightForSlot → Grab).
    - 기존 RunBottomAlign 의 HasTemplate/Camera null 가드/finally Dispose/catch 블록 무변경: `grep -c "FillAlignPoseZero" SystemHandler.cs` 변경 전과 동일.
    - `grep -c "260626 hbk" SystemHandler.cs` 가 변경 전보다 ≥ 6 증가.
    - 삼항/8.0+ 문법 없음. throw 없음.
  </acceptance_criteria>
  <done>ApplyCoaxLightForSlot 헬퍼 추가 + RunBottomAlign grab 직전 호출. 기존 RunBottomAlign 로직 무변경. throw 없음. `//260626 hbk` 주석. 빌드는 Task 4 통합 검증.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: 빌드 검증 (Debug/x64) + 회귀 0 확인</name>
  <files>WPF_Example/Custom/EthernetVision/AlignRefPose.cs, WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs, WPF_Example/Custom/SystemHandler.cs</files>
  <read_first>
    - WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs (Task 2 결과)
    - WPF_Example/Custom/SystemHandler.cs (Task 3 결과)
  </read_first>
  <action>
    Task 1~3 변경분을 통합 빌드로 검증한다. 신규 파일 없음(기존 3파일 수정) → csproj 무변경.

    msbuild Debug/x64 빌드 실행, error 0 확인. 실패 시 컴파일 오류 수정(메서드 시그니처/네임스페이스/중괄호) 후 재빌드.

    회귀 점검: git diff 로 기존 TryTeach/Run/TrySaveRefPose/LoadRefPose/BuildJsonPath 시그니처와 RunBottomAlign 의 기존 grab/Run/Dispose/catch 로직이 무변경(추가 라인만)인지 확인.
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal /nologo 2>&1 | grep -iE "error|Build succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 출력에 `Build succeeded` 포함, `error` 0건.
    - `git diff --stat` 가 AlignRefPose.cs + AlignShapeMatchService.cs + SystemHandler.cs 3파일만 변경으로 표시.
    - `git diff WPF_Example/Custom/SystemHandler.cs` 가 기존 grab/Run/Dispose/catch 라인을 삭제(`-`)하지 않음(추가 라인 `+` 만: ApplyCoaxLightForSlot 호출 1줄 + 헬퍼 메서드).
    - `git diff WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs` 가 기존 BuildJsonPath/LoadRefPose/TrySaveRefPose/TryTeach/Run 본문 라인을 삭제하지 않음.
  </acceptance_criteria>
  <done>msbuild Debug/x64 PASS (error 0). 변경 3파일만. 기존 align 티칭/검색/저장/RunBottomAlign 회귀 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TCP 핸들러($ALIGN_TEST) → 로컬 조명 제어 | $ALIGN_TEST 수신 → RunBottomAlign → ApplyCoaxLightForSlot. 입력(AlignFace 0~5)은 Phase 65 에서 이미 검증·범위 거부됨(FromAlignFace OOB→None). 이 Plan 은 신규 입력 파싱 없음 — 기존 slot 값으로 로컬 JSON 읽기 + LightHandler(시리얼/COM) 제어만. |
| 로컬 슬롯 JSON 읽기/쓰기 | Bottom_{slot}.json / Tray.json 역직렬화(TypeNameHandling.None — RCE 방지 설정 유지) + 쓰기. 로컬 신뢰 레시피 파일. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Denial of Service | SystemHandler.ApplyCoaxLightForSlot (TCP 스레드) | mitigate | try/catch 로 감싸고 throw 금지 — 예외 시 Logging 후 동축 OFF + 정상 반환. GetSlotRefPose 도 null 반환(예외 삼킴) → grab 진행. TCP 스레드 크래시 불가 (Phase 65 T-65-06 선례 일관). |
| T-66-02 | Tampering | AlignRefPose JSON 역직렬화 | accept | 기존 LoadRefPose 의 `TypeNameHandling.None`(RCE 방지) 설정을 재사용 — 신규 역직렬화 경로 추가 없음. 로컬 신뢰 파일. Coax 필드는 단순 bool/int. |
| T-66-03 | Tampering | TrySaveCoax 파일 쓰기 경로 | accept | 경로 = BuildJsonPath(검증된 RecipeSavePath 하위 ETHERNET_ALIGN 폴더, Phase 65 경로). 외부 입력으로 경로 조작 불가(mode/slot enum 만). Directory.CreateDirectory 는 동일 신뢰 폴더 트리 내. |

신규 high 위협 없음. 유일한 mitigate 항목(T-66-01)은 기존 Phase 65 TCP-스레드 throw-금지 패턴을 그대로 적용하여 해소. 나머지는 로컬 신뢰 I/O(기존 RCE 방지 설정 재사용)로 accept.
</threat_model>

<verification>
- msbuild Debug/x64 PASS (error 0).
- AlignRefPose: CoaxEnabled/CoaxLevel 추가, 기존 필드 보존.
- AlignShapeMatchService: public GetSlotRefPose + TrySaveCoax(load-merge-save) 추가, 기존 private 메서드 무변경.
- SystemHandler: ApplyCoaxLightForSlot 헬퍼 + RunBottomAlign grab 직전 호출, throw 없음, 기존 로직 무변경.
- 모든 변경 라인에 `//260626 hbk` 주석.
- 기존 align 티칭/검색/저장/RunBottomAlign 회귀 0 (git diff 추가 라인 only).
</verification>

<success_criteria>
슬롯/Tray JSON 에 동축값이 함께 저장/로드되고(키 부재 시 false/0 폴백), 동축 저장이 티칭 데이터를 보존하며, $ALIGN_TEST Run 시 grab 직전 슬롯 동축값이 자동 적용된다(예외/미티칭 시 안전 OFF, TCP 스레드 무중단). msbuild Debug/x64 PASS. 기존 align 백엔드 회귀 0.
</success_criteria>

<output>
완료 후 `.planning/phases/66-ring7-coax-align-2026-06-26/66-02-SUMMARY.md` 생성.
</output>
