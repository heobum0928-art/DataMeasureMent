---
phase: 66-ring7-coax-align-2026-06-26
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - WPF_Example/Custom/UI/BottomVisionView.xaml
  - WPF_Example/Custom/UI/BottomVisionView.xaml.cs
  - WPF_Example/Custom/UI/TrayVisionView.xaml
  - WPF_Example/Custom/UI/TrayVisionView.xaml.cs
autonomous: true
requirements: [AV-08]

must_haves:
  truths:
    - "Bottom Align 창 좌측 패널에 동축(ALIGN_COAX) ON/OFF 체크박스 + 밝기 슬라이더(0~255)가 있다"
    - "Tray Align 창 좌측 패널에 동축 ON/OFF 체크박스 + 밝기 슬라이더(0~255)가 있다"
    - "Bottom 에서 슬롯 선택 시 해당 슬롯 JSON 의 동축값이 UI 에 복원된다 (미티칭/None 슬롯은 off/0)"
    - "동축 체크박스/슬라이더 변경 시 즉시 LIGHT_ALIGN_COAX 에 적용되고 슬롯/Tray JSON 에 저장된다 (수동 override, D-07)"
    - "Bottom/Tray 에서 Teach/Run/Grab(실카메라) 직전에 현재 동축값이 자동 적용된다 (티칭=런타임 조명 일치)"
  artifacts:
    - path: "WPF_Example/Custom/UI/BottomVisionView.xaml"
      provides: "동축 조명 GroupBox (chk_coaxEnabled + sld_coaxLevel + lbl_coaxLevel)"
      contains: "chk_coaxEnabled"
    - path: "WPF_Example/Custom/UI/BottomVisionView.xaml.cs"
      provides: "ApplyCoaxLight + Coax 이벤트 핸들러 + 슬롯 복원/저장 + Teach/Run/Grab 자동적용 + using ReringProject.Device"
      contains: "ApplyCoaxLight"
    - path: "WPF_Example/Custom/UI/TrayVisionView.xaml"
      provides: "동축 조명 GroupBox (chk_coaxEnabled + sld_coaxLevel + lbl_coaxLevel)"
      contains: "chk_coaxEnabled"
    - path: "WPF_Example/Custom/UI/TrayVisionView.xaml.cs"
      provides: "ApplyCoaxLight + Coax 이벤트 핸들러 + Tray 단일 복원/저장 + Teach/Run/Grab 자동적용"
      contains: "ApplyCoaxLight"
  key_links:
    - from: "BottomVisionView.SlotComboBox_SelectionChanged"
      to: "Matcher.GetSlotRefPose(Bottom, slot) → chk/sld 복원"
      via: "슬롯 전환 시 동축 UI 복원"
      pattern: "GetSlotRefPose"
    - from: "CoaxCheckBox_Changed / CoaxSlider_ValueChanged"
      to: "ApplyCoaxLight() + Matcher.TrySaveCoax(...)"
      via: "수동 override 즉시 적용 + 저장"
      pattern: "TrySaveCoax"
    - from: "Grab/Teach/Run 핸들러"
      to: "ApplyCoaxLight() (호출 직전)"
      via: "런타임=티칭 조명 일치 자동 적용"
      pattern: "ApplyCoaxLight\\(\\)"
---

<objective>
Align 동축 조명 워크스트림(AV-08)의 **UI 계층**을 만든다. Bottom/Tray Align 창 좌측 패널에 동축(ALIGN_COAX) ON/OFF + 밝기(0~255) 컨트롤을 추가하고, Plan 02 의 공개 API(`GetSlotRefPose`/`TrySaveCoax`)로 슬롯/Tray 동축값을 복원·저장하며, Teach/Run/Grab(실카메라) 직전에 동축을 자동 적용한다 (티칭 조명 = 런타임 조명 일치, D-07).

- **Bottom (슬롯-aware):** 6슬롯별 동축값. 슬롯 ComboBox 선택 시 해당 슬롯 JSON 의 동축값 복원, 체크/슬라이더 변경 시 즉시 적용 + 슬롯 JSON 저장. `using ReringProject.Device;` 추가 필요(현재 없음).
- **Tray (단일):** 단일 동축값. Tray.json 에 저장. 슬롯 개념 없음.

Purpose: 작업자가 Align 창에서 동축 조명을 켜고/조정하여 티칭하면 그 값이 슬롯에 저장되고, $ALIGN_TEST 런타임(Plan 02 RunBottomAlign)과 수동 Teach/Run/Grab 에서 동일 조명이 자동 재현되어 매칭 스코어가 안정화된다. 이 Plan 은 Wave 2 — Plan 02 의 `GetSlotRefPose`/`TrySaveCoax`/`AlignRefPose.CoaxEnabled` 에 의존한다.
Output: 동축 GroupBox + 핸들러가 추가된 BottomVisionView(.xaml+.cs) + TrayVisionView(.xaml+.cs).

**잠금 결정(CONTEXT.md):** D-04(동축 ON/OFF + 밝기 0~255 컨트롤 1개, 그룹 드롭다운 불필요 — 채널 1개), D-05(Bottom = 슬롯 JSON 저장, Tray = Tray.json 단일), D-07(Teach/Run/Grab 직전 모두 자동 ON + 작업자 슬라이더 수동 override 허용, 조정값 슬롯 JSON 저장).

**Claude 재량(CONTEXT.md):** 동축 컨트롤 위젯 형태 = 표준 WPF CheckBox + Slider + TextBlock(기존 chk_showRoi 스타일 일관, PropertyTools HeaderedEntrySlider 미사용 — PATTERNS.md 추가 설계 #4). JSON 동축 필드 키명/기본값(off/0) = Plan 02 의 CoaxEnabled/CoaxLevel POCO 가 처리.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-ring7-coax-align-2026-06-26/66-CONTEXT.md
@.planning/phases/66-ring7-coax-align-2026-06-26/66-PATTERNS.md

# 핵심 변경 대상 — 반드시 먼저 읽을 것
@WPF_Example/Custom/UI/BottomVisionView.xaml
@WPF_Example/Custom/UI/BottomVisionView.xaml.cs
@WPF_Example/Custom/UI/TrayVisionView.xaml
@WPF_Example/Custom/UI/TrayVisionView.xaml.cs

<interfaces>
<!-- 코드 검증 완료(2026-06-26). 실행자는 탐색 없이 이 계약을 그대로 사용. -->

Plan 02 가 추가한 공개 API (AlignShapeMatchService, EthernetVisionHandler.Handle.Matcher 로 접근):
```csharp
public AlignRefPose GetSlotRefPose(EEthernetVisionMode mode, EBottomAlignSlot slot);   // 로드 래퍼(null 가능)
public bool TrySaveCoax(EEthernetVisionMode mode, EBottomAlignSlot slot, bool coaxEnabled, int coaxLevel, out string error);   // load-merge-save
// AlignRefPose.CoaxEnabled(bool) / CoaxLevel(int)  — namespace ReringProject
```

LightHandler 동축 그룹 (ReringProject.Device):
```csharp
public const string LIGHT_ALIGN_COAX = "ALIGN_COAX";
LightHandler.Handle.SetOnOff(string group, bool onOff);
LightHandler.Handle.SetLevel(string group, int level);
```

BottomVisionView.xaml.cs (정확히 확인됨):
- using 블록 L2-12: `ReringProject.Setting` 있음, **`ReringProject.Device` 없음** → 추가 필요.
- 필드: `private EBottomAlignSlot _selectedSlot`(L50, None 초기), `private const EEthernetVisionMode VIEW_MODE = EEthernetVisionMode.Bottom`(L26).
- `SlotComboBox_SelectionChanged`(L140-186): _selectedSlot 갱신 + ROI 복원. **여기 끝(L181 RefreshStatus 전후)에 동축 복원 추가.**
- `GrabButton_Click`(L190-231): `#if SIMUL_MODE`(파일 다이얼로그, grab 없음) / `#else`(L207-230, L215 `img = ...Camera.Grab();`). **#else 의 try 블록 안 grab 직전(L214 try 뒤, L215 앞)에 ApplyCoaxLight() 추가.** SIMUL 분기는 실제 카메라 grab 없음 → 동축 적용 불필요.
- `TeachButton_Click`(L311-371): L348 `TryTeach(...)` 호출. **try 진입 후 TryTeach 직전에 ApplyCoaxLight() 추가.**
- `RunButton_Click`(L375-402): L386 `Matcher.Run(...)`. **try 진입(L383) 후 Run 직전에 ApplyCoaxLight() 추가.**
- 상태 라벨: `lbl_status`(상단). 동축 핸들러 catch 는 `lbl_status.Text = "..."` 사용(기존 패턴 L228).

TrayVisionView.xaml.cs (정확히 확인됨):
- using 블록 L2-13: **`ReringProject.Device` 있음(L9)** → 추가 불필요.
- 필드: `private const EEthernetVisionMode VIEW_MODE = EEthernetVisionMode.Tray`(L25). 슬롯 필드 없음.
- `GrabButton_Click`(L75-116): `#if SIMUL_MODE`/`#else`(L92-115, L100 `img = ...Grab();`). #else grab 직전에 ApplyCoaxLight() 추가.
- `TeachButton_Click`(L196-243): L224 `TryTeach(...)`. 직전에 ApplyCoaxLight() 추가.
- `RunButton_Click`(L247-272): L256 `Matcher.Run(...)`. 직전에 ApplyCoaxLight() 추가.
- `TrayVisionView_Loaded`(L69-71): RefreshStatus 호출. **여기에 동축 복원(LoadTrayCoaxToUi) 1회 추가.**

XAML 좌측 패널 Grid 구조:
- BottomVisionView.xaml: 7 RowDefinitions(L69-77), Row 0=헤더,1=상태,2=카메라/로더,3=티칭,4=검사결과,5=피커캘,6=여백(`Height="*"`). 버튼스타일 키 = `BottomActionButtonStyle`. ScrollViewer 안에 Grid.
- TrayVisionView.xaml: 6 RowDefinitions(L67-74), Row 0=헤더,1=상태,2=카메라/로더,3=티칭,4=검사결과,5=여백(`Height="*"`). 버튼스타일 키 = `TrayActionButtonStyle`. ScrollViewer 없음(Grid 직접).
- 기존 시각화 체크박스 패턴(BottomVisionView.xaml L166-169): `<CheckBox x:Name="chk_showRoi" Content="ROI 표시" IsChecked="True" Margin="0,8,0,0" Checked="..." Unchecked="..."/>`
</interfaces>
</context>

<coding_rules>
- 모든 추가/수정 라인에 `//260626 hbk`(.cs) 또는 `<!--260626 hbk ...-->`(.xaml) + 한국어 사유 주석 필수.
- 삼항 연산자 `?:` 금지 → `if-else`. 헝가리언(`b`/`n` prefix 지역변수, PascalCase 메서드) — 인접 코드 관행 유지.
- C# 7.2 (.NET 4.8) — switch expression / nullable reference / record / using 선언 등 8.0+ 문법 금지.
- 모든 서비스/조명 호출은 try/catch — 예외 시 상태 라벨 갱신만, **throw 금지**(기존 D-05 패턴, BottomVisionView 전반).
- WPF 동축 컨트롤 = 표준 CheckBox + Slider + TextBlock (PATTERNS.md #4). PropertyTools HeaderedEntrySlider 미사용.
- **회귀 0**: 기존 Grab/Live/Stop/Teach/Run/슬롯선택/피커캘/오프라인로더 동작 무변경(동축 추가 라인만). 기존 RowDefinition 의 GroupBox Grid.Row 재배치 시 누락/중복 금지.
- 함수 30~40줄 유지. ApplyCoaxLight 약 15줄, 핸들러 각 5~10줄.
- airspace 주의: 동축 컨트롤은 좌측 WPF 패널 내부에 배치(HALCON HWND 위 아님) — 기존 chk_showRoi 와 같은 영역.
</coding_rules>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: BottomVisionView 동축 컨트롤 + 핸들러 + 슬롯 복원/저장 + 자동적용 (D-04/05/07)</name>
  <files>WPF_Example/Custom/UI/BottomVisionView.xaml, WPF_Example/Custom/UI/BottomVisionView.xaml.cs</files>
  <read_first>
    - WPF_Example/Custom/UI/BottomVisionView.xaml (전체 — RowDefinitions L69-77, Row 2~6 GroupBox, chk_showRoi L166)
    - WPF_Example/Custom/UI/BottomVisionView.xaml.cs (전체 — using L2-12, SlotComboBox_SelectionChanged L140-186, Grab/Teach/Run L190/311/375)
    - WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs (Plan 02 — GetSlotRefPose/TrySaveCoax 시그니처)
  </read_first>
  <action>
    **(A) XAML — 동축 GroupBox 추가 (BottomVisionView.xaml):**

    좌측 패널 Grid 에 RowDefinition 1개를 추가하고 동축 GroupBox 를 삽입한다. 방법:
    1. `<Grid.RowDefinitions>`(L69-77)에 `<RowDefinition Height="Auto"/>` **1개 추가** (총 7개 Auto + 1 `*` = 8개. 기존 마지막 `<RowDefinition Height="*"/>` 앞에 Auto 추가).
    2. 피커캘 GroupBox(`Grid.Row="5"`)와 여백(`Grid.Row="6"`) **사이**에 동축 GroupBox(`Grid.Row="6"`)를 삽입하고, 기존 여백 Grid 를 `Grid.Row="7"` 로 변경. (즉 Row 매핑: 피커캘=5 유지, 동축=신규 6, 여백 6→7.)
    3. 동축 GroupBox 내용 (chk_showRoi 스타일과 일관, `BottomActionButtonStyle` 불필요 — 표준 컨트롤):
    ```xml
    <!--260626 hbk Phase 66 D-04 — 동축 조명 컨트롤(ALIGN_COAX 단일 채널). airspace-safe: 좌측 WPF 패널 내부.-->
    <GroupBox Grid.Row="6" Header="동축 조명 (Align 전용)" Margin="0,0,0,8">
        <StackPanel Margin="8">
            <CheckBox x:Name="chk_coaxEnabled" Content="동축 ON/OFF" Margin="0,0,0,6"
                      Checked="CoaxCheckBox_Changed" Unchecked="CoaxCheckBox_Changed"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="밝기:" VerticalAlignment="Center" Margin="0,0,6,0" FontSize="12" Foreground="#334155"/>
                <Slider x:Name="sld_coaxLevel" Minimum="0" Maximum="255" Width="180"
                        VerticalAlignment="Center" ValueChanged="CoaxSlider_ValueChanged"/>
                <TextBlock x:Name="lbl_coaxLevel" Text="0" Margin="6,0,0,0" FontSize="12"
                           VerticalAlignment="Center" Width="32" Foreground="#334155"/>
            </StackPanel>
        </StackPanel>
    </GroupBox>
    ```
    **주의:** 기존 피커캘 GroupBox(Row 5)와 여백 Grid 외 다른 Grid.Row 는 변경 금지. RowDefinition 추가 후 총 row 수(8) = Grid.Row 최대값(7)+1 일치 확인.

    **(B) code-behind — using + 헬퍼 + 핸들러 + 복원/저장 + 자동적용 (BottomVisionView.xaml.cs):**

    (B1) **using 추가:** L2-12 using 블록에 `using ReringProject.Device;`(L11 `using ReringProject.Setting;` 인접) 추가. `//260626 hbk Phase 66 — LightHandler 참조` 주석.

    (B2) **ApplyCoaxLight 헬퍼 + 이벤트 핸들러 추가** (private 헬퍼 리전 또는 파일 끝 근처). 삼항 금지, try/catch:
    ```csharp
    //260626 hbk Phase 66 D-06/D-07 — 현재 UI 동축값을 LIGHT_ALIGN_COAX 에 적용. Enabled=true: ON+밝기, false: OFF.
    private void ApplyCoaxLight()
    {
        try
        {
            bool bEnabled = (chk_coaxEnabled.IsChecked == true);   //260626 hbk 체크 상태
            int nLevel = (int)sld_coaxLevel.Value;                 //260626 hbk 슬라이더 밝기
            if (bEnabled)
            {
                LightHandler.Handle.SetOnOff(LightHandler.LIGHT_ALIGN_COAX, true);   //260626 hbk 동축 ON
                LightHandler.Handle.SetLevel(LightHandler.LIGHT_ALIGN_COAX, nLevel); //260626 hbk 동축 밝기
            }
            else
            {
                LightHandler.Handle.SetOnOff(LightHandler.LIGHT_ALIGN_COAX, false);  //260626 hbk 동축 OFF
            }
        }
        catch (Exception ex)
        {
            lbl_status.Text = "동축 적용 오류: " + ex.Message;   //260626 hbk throw 금지 — 상태 라벨만
        }
    }

    //260626 hbk Phase 66 D-07 — 동축 체크 변경: 즉시 적용 + 슬롯 JSON 저장(수동 override)
    private void CoaxCheckBox_Changed(object sender, RoutedEventArgs e)
    {
        ApplyCoaxLight();          //260626 hbk 즉시 반영
        SaveSlotCoaxToJson();      //260626 hbk 슬롯 JSON 갱신
    }

    //260626 hbk Phase 66 D-07 — 동축 슬라이더 변경: 라벨 갱신 + 즉시 적용 + 슬롯 JSON 저장
    private void CoaxSlider_ValueChanged(object sender, RoutedPropertyChangedEventArgs<double> e)
    {
        int nLevel = (int)e.NewValue;        //260626 hbk 새 밝기
        if (lbl_coaxLevel != null)
        {
            lbl_coaxLevel.Text = nLevel.ToString();   //260626 hbk 라벨 갱신(초기화 전 null 가드)
        }
        ApplyCoaxLight();
        SaveSlotCoaxToJson();
    }

    //260626 hbk Phase 66 D-05 — 현재 선택 슬롯 JSON 에 동축값 저장(TrySaveCoax = 티칭 데이터 보존 load-merge-save).
    //  슬롯 None(미선택)이면 저장 스킵 — 단일 경로 동축은 본 phase 범위 외(슬롯별 저장).
    private void SaveSlotCoaxToJson()
    {
        if (_selectedSlot == EBottomAlignSlot.None)
        {
            return;   //260626 hbk 슬롯 미선택 — 저장 대상 없음
        }
        try
        {
            bool bEnabled = (chk_coaxEnabled.IsChecked == true);
            int nLevel = (int)sld_coaxLevel.Value;
            string error;
            bool bOk = EthernetVisionHandler.Handle.Matcher.TrySaveCoax(VIEW_MODE, _selectedSlot, bEnabled, nLevel, out error);   //260626 hbk 슬롯 JSON 동축 갱신
            if (!bOk)
            {
                lbl_status.Text = "동축 저장 실패: " + error;   //260626 hbk 저장 실패 상태 표시
            }
        }
        catch (Exception ex)
        {
            lbl_status.Text = "동축 저장 오류: " + ex.Message;   //260626 hbk throw 금지
        }
    }

    //260626 hbk Phase 66 D-05 — 슬롯 JSON 의 동축값을 UI(chk/sld/lbl)에 복원. null/미티칭 → off/0.
    private void LoadSlotCoaxToUi()
    {
        try
        {
            AlignRefPose refPose = null;   //260626 hbk 슬롯 동축값
            if (_selectedSlot != EBottomAlignSlot.None)
            {
                refPose = EthernetVisionHandler.Handle.Matcher.GetSlotRefPose(VIEW_MODE, _selectedSlot);   //260626 hbk 슬롯 JSON 로드
            }
            bool bEnabled = false;   //260626 hbk 기본 off
            int nLevel = 0;
            if (refPose != null)
            {
                bEnabled = refPose.CoaxEnabled;
                nLevel = refPose.CoaxLevel;
            }
            chk_coaxEnabled.IsChecked = bEnabled;   //260626 hbk UI 복원
            sld_coaxLevel.Value = nLevel;
            lbl_coaxLevel.Text = nLevel.ToString();
        }
        catch (Exception ex)
        {
            lbl_status.Text = "동축 복원 오류: " + ex.Message;   //260626 hbk throw 금지
        }
    }
    ```
    `AlignRefPose` 는 `ReringProject` 네임스페이스 — BottomVisionView 는 `namespace ReringProject.Custom.UI` 이므로 `using ReringProject;` 가 필요할 수 있다. 빌드 시 형식 미해결이면 `using ReringProject;` 추가(`//260626 hbk AlignRefPose 참조`).

    (B3) **슬롯 전환 시 동축 복원:** `SlotComboBox_SelectionChanged`(L140-186)의 try 블록 끝, `RefreshStatus();`(L181) **직전 또는 직후**에 `LoadSlotCoaxToUi();` 한 줄 추가(`//260626 hbk Phase 66 — 슬롯 동축값 복원`). 기존 ROI 복원/_selectedSlot 갱신 로직은 무변경.

    (B4) **Grab/Teach/Run 자동 적용 (D-07):**
    - `GrabButton_Click` `#else` 분기(L207~): `try {`(L214) 다음, `HImage img = ...Camera.Grab();`(L215) **직전**에 `ApplyCoaxLight();` 추가(`//260626 hbk Phase 66 — grab 직전 동축 자동 적용`). SIMUL(`#if` 분기)는 실제 grab 없음 → 추가 불필요.
    - `TeachButton_Click`(L311~): TryTeach 호출(L348) **직전**(가드/ROI 변환 다음)에 `ApplyCoaxLight();` 추가(`//260626 hbk Phase 66 — 티칭 직전 동축 자동 적용`).
    - `RunButton_Click`(L375~): `Matcher.Run(...)`(L386) **직전**(L384 `lbl_status.Text="검사중";` 다음)에 `ApplyCoaxLight();` 추가(`//260626 hbk Phase 66 — 검사 직전 동축 자동 적용`).

    기존 핸들러의 다른 라인은 무변경(회귀 0).
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && grep -c "chk_coaxEnabled\|ApplyCoaxLight\|TrySaveCoax\|GetSlotRefPose\|using ReringProject.Device" WPF_Example/Custom/UI/BottomVisionView.xaml.cs WPF_Example/Custom/UI/BottomVisionView.xaml</automated>
  </verify>
  <acceptance_criteria>
    - XAML: `grep "chk_coaxEnabled" BottomVisionView.xaml` + `grep "sld_coaxLevel" ...` + `grep "lbl_coaxLevel" ...` 모두 존재. `grep -c 'RowDefinition' BottomVisionView.xaml` 가 변경 전(7)보다 1 증가(=8). 동축 GroupBox `Grid.Row="6"`, 여백 Grid `Grid.Row="7"` 로 변경됨.
    - cs: `grep "using ReringProject.Device;" BottomVisionView.xaml.cs` 존재(신규).
    - cs: `private void ApplyCoaxLight()` + `CoaxCheckBox_Changed` + `CoaxSlider_ValueChanged` + `SaveSlotCoaxToJson` + `LoadSlotCoaxToUi` 5개 메서드 정의 존재.
    - cs: `LIGHT_ALIGN_COAX` SetOnOff(true/false)+SetLevel 존재. `TrySaveCoax(VIEW_MODE, _selectedSlot,` 호출 존재. `GetSlotRefPose(VIEW_MODE, _selectedSlot)` 호출 존재.
    - cs: `ApplyCoaxLight();` 호출이 SlotComboBox 복원 경로 외에 **최소 3회** 추가(Grab #else / Teach / Run). `grep -c "ApplyCoaxLight();" BottomVisionView.xaml.cs` ≥ 4 (3 자동적용 + CoaxCheckBox_Changed 내 1 + Slider 내 1 → 실제 ≥ 5).
    - cs: `LoadSlotCoaxToUi();` 가 SlotComboBox_SelectionChanged 내 1회 호출.
    - `//260626 hbk` 주석: `grep -c "260626 hbk" BottomVisionView.xaml.cs` ≥ 12. `grep -c "260626 hbk" BottomVisionView.xaml` ≥ 1.
    - 삼항/8.0+ 문법 없음. 모든 동축 핸들러 try/catch + throw 없음.
  </acceptance_criteria>
  <done>Bottom 동축 GroupBox(chk/sld/lbl) + RowDefinition +1 + Grid.Row 재배치. using Device 추가. ApplyCoaxLight/핸들러/SaveSlotCoaxToJson/LoadSlotCoaxToUi 추가. 슬롯 복원 + Grab(#else)/Teach/Run 자동 적용. 기존 핸들러 무변경. 빌드는 Task 3 통합 검증.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TrayVisionView 동축 컨트롤 + 핸들러 + Tray 단일 복원/저장 + 자동적용 (D-04/05/07)</name>
  <files>WPF_Example/Custom/UI/TrayVisionView.xaml, WPF_Example/Custom/UI/TrayVisionView.xaml.cs</files>
  <read_first>
    - WPF_Example/Custom/UI/TrayVisionView.xaml (전체 — RowDefinitions L67-74, Row 4 검사결과, Row 5 여백)
    - WPF_Example/Custom/UI/TrayVisionView.xaml.cs (전체 — using L9 Device 있음, Loaded L69, Grab/Teach/Run L75/196/247)
    - WPF_Example/Custom/UI/BottomVisionView.xaml.cs (Task 1 결과 — 동일 ApplyCoaxLight/핸들러 패턴 참조)
  </read_first>
  <action>
    Bottom(Task 1)과 동일 패턴이되 **슬롯 없음(Tray 단일, _selectedSlot 대신 EBottomAlignSlot.None 사용)**.

    **(A) XAML — 동축 GroupBox 추가 (TrayVisionView.xaml):**
    1. `<Grid.RowDefinitions>`(L67-74)에 `<RowDefinition Height="Auto"/>` 1개 추가 (총 5 Auto + 1 `*` = 6개 → 6 Auto + 1 `*` = 7개. 기존 `*` 앞에 Auto 추가).
    2. 검사결과 GroupBox(`Grid.Row="4"`)와 여백(`Grid.Row="5"`) 사이에 동축 GroupBox(`Grid.Row="5"`) 삽입, 기존 여백 Grid 를 `Grid.Row="6"` 로 변경.
    3. GroupBox 내용 = Task 1 (A)-3 과 동일(`chk_coaxEnabled`/`sld_coaxLevel`/`lbl_coaxLevel`, 동일 이벤트명 CoaxCheckBox_Changed/CoaxSlider_ValueChanged):
    ```xml
    <!--260626 hbk Phase 66 D-04 — 동축 조명 컨트롤(ALIGN_COAX 단일). Tray 단일 동축값.-->
    <GroupBox Grid.Row="5" Header="동축 조명 (Align 전용)" Margin="0,0,0,8">
        <StackPanel Margin="8">
            <CheckBox x:Name="chk_coaxEnabled" Content="동축 ON/OFF" Margin="0,0,0,6"
                      Checked="CoaxCheckBox_Changed" Unchecked="CoaxCheckBox_Changed"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="밝기:" VerticalAlignment="Center" Margin="0,0,6,0" FontSize="12" Foreground="#334155"/>
                <Slider x:Name="sld_coaxLevel" Minimum="0" Maximum="255" Width="180"
                        VerticalAlignment="Center" ValueChanged="CoaxSlider_ValueChanged"/>
                <TextBlock x:Name="lbl_coaxLevel" Text="0" Margin="6,0,0,0" FontSize="12"
                           VerticalAlignment="Center" Width="32" Foreground="#334155"/>
            </StackPanel>
        </StackPanel>
    </GroupBox>
    ```

    **(B) code-behind — 헬퍼 + 핸들러 + Tray 복원/저장 + 자동적용 (TrayVisionView.xaml.cs):**

    (B1) **using:** `using ReringProject.Device;` 는 **이미 L9 존재** → 추가 불필요. `AlignRefPose` 참조용 `using ReringProject;` 필요 시 추가(빌드 미해결 시).

    (B2) **헬퍼 + 핸들러** — Task 1 (B2)의 ApplyCoaxLight / CoaxCheckBox_Changed / CoaxSlider_ValueChanged 와 **동일**(그대로 복제). 단, 저장/복원은 Tray 전용:
    ```csharp
    //260626 hbk Phase 66 D-05 — Tray 단일 동축값을 Tray.json 에 저장(slot=None = Tray 단일 경로, BuildJsonPath(Tray,None)=Tray.json).
    private void SaveTrayCoaxToJson()
    {
        try
        {
            bool bEnabled = (chk_coaxEnabled.IsChecked == true);
            int nLevel = (int)sld_coaxLevel.Value;
            string error;
            bool bOk = EthernetVisionHandler.Handle.Matcher.TrySaveCoax(VIEW_MODE, EBottomAlignSlot.None, bEnabled, nLevel, out error);   //260626 hbk Tray 단일 — slot None
            if (!bOk)
            {
                lbl_status.Text = "동축 저장 실패: " + error;   //260626 hbk
            }
        }
        catch (Exception ex)
        {
            lbl_status.Text = "동축 저장 오류: " + ex.Message;   //260626 hbk throw 금지
        }
    }

    //260626 hbk Phase 66 D-05 — Tray.json 동축값을 UI 에 복원. null → off/0.
    private void LoadTrayCoaxToUi()
    {
        try
        {
            AlignRefPose refPose = EthernetVisionHandler.Handle.Matcher.GetSlotRefPose(VIEW_MODE, EBottomAlignSlot.None);   //260626 hbk Tray 단일 로드
            bool bEnabled = false;
            int nLevel = 0;
            if (refPose != null)
            {
                bEnabled = refPose.CoaxEnabled;
                nLevel = refPose.CoaxLevel;
            }
            chk_coaxEnabled.IsChecked = bEnabled;   //260626 hbk UI 복원
            sld_coaxLevel.Value = nLevel;
            lbl_coaxLevel.Text = nLevel.ToString();
        }
        catch (Exception ex)
        {
            lbl_status.Text = "동축 복원 오류: " + ex.Message;   //260626 hbk throw 금지
        }
    }
    ```
    CoaxCheckBox_Changed / CoaxSlider_ValueChanged 내부에서 `SaveSlotCoaxToJson()` 대신 **`SaveTrayCoaxToJson()`** 호출(Tray 전용). `EBottomAlignSlot` 는 `ReringProject.Setting`(이미 using L12).

    (B3) **Loaded 시 복원:** `TrayVisionView_Loaded`(L69-71)의 `RefreshStatus();` 다음에 `LoadTrayCoaxToUi();` 추가(`//260626 hbk Phase 66 — Tray 동축값 복원`).

    (B4) **Grab/Teach/Run 자동 적용:**
    - `GrabButton_Click` `#else` 분기: `img = ...Camera.Grab();`(L100) 직전에 `ApplyCoaxLight();` 추가.
    - `TeachButton_Click`: TryTeach(L224) 직전에 `ApplyCoaxLight();` 추가.
    - `RunButton_Click`: `Matcher.Run(...)`(L256) 직전(`lbl_status.Text="검사중";` 다음)에 `ApplyCoaxLight();` 추가.

    기존 핸들러 다른 라인 무변경(회귀 0).
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && grep -c "chk_coaxEnabled\|ApplyCoaxLight\|SaveTrayCoaxToJson\|LoadTrayCoaxToUi\|TrySaveCoax" WPF_Example/Custom/UI/TrayVisionView.xaml.cs WPF_Example/Custom/UI/TrayVisionView.xaml</automated>
  </verify>
  <acceptance_criteria>
    - XAML: `grep "chk_coaxEnabled" TrayVisionView.xaml` + `sld_coaxLevel` + `lbl_coaxLevel` 존재. `grep -c 'RowDefinition' TrayVisionView.xaml` 가 변경 전(6)보다 1 증가(=7). 동축 GroupBox `Grid.Row="5"`, 여백 Grid `Grid.Row="6"`.
    - cs: `private void ApplyCoaxLight()` + `CoaxCheckBox_Changed` + `CoaxSlider_ValueChanged` + `SaveTrayCoaxToJson` + `LoadTrayCoaxToUi` 정의 존재.
    - cs: `TrySaveCoax(VIEW_MODE, EBottomAlignSlot.None,` + `GetSlotRefPose(VIEW_MODE, EBottomAlignSlot.None)` 호출 존재(Tray 단일).
    - cs: `LoadTrayCoaxToUi();` 가 TrayVisionView_Loaded 내 1회. `ApplyCoaxLight();` ≥ 5회(Grab#else/Teach/Run + 핸들러 2).
    - cs: `LIGHT_ALIGN_COAX` SetOnOff/SetLevel 존재.
    - `//260626 hbk` 주석: `grep -c "260626 hbk" TrayVisionView.xaml.cs` ≥ 12. `grep -c "260626 hbk" TrayVisionView.xaml` ≥ 1.
    - 삼항/8.0+ 문법 없음. try/catch + throw 없음.
  </acceptance_criteria>
  <done>Tray 동축 GroupBox + RowDefinition +1 + Grid.Row 재배치. ApplyCoaxLight/핸들러/SaveTrayCoaxToJson/LoadTrayCoaxToUi 추가(slot=None Tray 단일). Loaded 복원 + Grab(#else)/Teach/Run 자동 적용. 기존 핸들러 무변경. 빌드는 Task 3 통합 검증.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 빌드 검증 (Debug/x64) + 회귀 0 확인</name>
  <files>WPF_Example/Custom/UI/BottomVisionView.xaml, WPF_Example/Custom/UI/BottomVisionView.xaml.cs, WPF_Example/Custom/UI/TrayVisionView.xaml, WPF_Example/Custom/UI/TrayVisionView.xaml.cs</files>
  <read_first>
    - WPF_Example/Custom/UI/BottomVisionView.xaml.cs (Task 1 결과)
    - WPF_Example/Custom/UI/TrayVisionView.xaml.cs (Task 2 결과)
  </read_first>
  <action>
    Task 1+2 변경분(4 UI 파일)을 통합 빌드로 검증한다. 신규 파일 없음(기존 4파일 수정) → csproj 무변경.

    msbuild Debug/x64 빌드 실행, error 0 확인. 흔한 실패 원인: (a) `AlignRefPose` 형식 미해결 → `using ReringProject;` 추가, (b) XAML x:Name 과 cs 이벤트 핸들러명 불일치 → CoaxCheckBox_Changed/CoaxSlider_ValueChanged 시그니처 확인(RoutedEventArgs / RoutedPropertyChangedEventArgs<double>), (c) Grid.Row 번호와 RowDefinition 개수 불일치(XAML 컴파일은 통과하나 런타임 레이아웃 깨짐 — Grid.Row 최대값+1 = RowDefinition 수 확인). 수정 후 재빌드.

    회귀 점검: git diff 로 기존 Grab/Live/Stop/Teach/Run/SlotComboBox/피커캘/오프라인로더 로직이 무변경(동축 추가 라인만)인지 확인. 특히 기존 GroupBox 의 Grid.Row 가 의도대로만 재배치되었는지(피커캘/여백, 검사결과/여백) 확인.
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal /nologo 2>&1 | grep -iE "error|Build succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 출력에 `Build succeeded` 포함, `error` 0건.
    - `git diff --stat` 가 BottomVisionView.xaml(.cs) + TrayVisionView.xaml(.cs) 4파일만 변경으로 표시.
    - BottomVisionView.xaml: RowDefinition 8개, Grid.Row 최대값 7. TrayVisionView.xaml: RowDefinition 7개, Grid.Row 최대값 6.
    - `git diff` 가 기존 GrabButton_Click/TeachButton_Click/RunButton_Click/SlotComboBox_SelectionChanged 의 기존 로직 라인을 삭제(`-`)하지 않음(ApplyCoaxLight/LoadSlotCoaxToUi 추가 라인 `+` 만).
  </acceptance_criteria>
  <done>msbuild Debug/x64 PASS (error 0). 변경 4파일만. 기존 Align UI(Grab/Teach/Run/슬롯/피커캘/로더) 회귀 0. Grid.Row 정합.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 작업자 WPF 입력(체크박스/슬라이더) → 로컬 조명 + 로컬 JSON | 동축 ON/OFF + 밝기(0~255 슬라이더 범위 제한) → LightHandler(시리얼/COM) + TrySaveCoax(로컬 JSON). 신규 네트워크/인증/외부 파서 없음. 로컬 신뢰 작업자 입력. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-UI-01 | Denial of Service | Coax 이벤트 핸들러 / ApplyCoaxLight (UI 스레드) | mitigate | 모든 동축 핸들러를 try/catch 로 감싸고 throw 금지 — 예외 시 lbl_status 텍스트만 갱신(기존 D-05 패턴). LightHandler/Matcher null/예외 시 UI 무중단. |
| T-66-UI-02 | Tampering | sld_coaxLevel 입력 범위 | accept | Slider Minimum=0/Maximum=255 로 밝기 범위가 UI 레벨에서 제한됨. (int) 캐스트로 정수화. 범위 외 값 불가. |
| T-66-UI-03 | Tampering | TrySaveCoax 로컬 JSON 쓰기 | accept | Plan 02 TrySaveCoax 가 검증된 경로(BuildJsonPath)에만 쓰며 mode/slot enum 만 받음. 경로 조작 불가. 로컬 신뢰 파일. |

신규 high 위협 없음. mitigate 항목(T-66-UI-01)은 기존 Align 창 throw-금지 try/catch 패턴 적용으로 해소. 입력 범위는 Slider 위젯이 제한(accept). 신규 공격 표면 없음 — 로컬 WPF + 로컬 JSON I/O.
</threat_model>

<verification>
- msbuild Debug/x64 PASS (error 0).
- Bottom/Tray 좌측 패널에 동축 GroupBox(chk_coaxEnabled + sld_coaxLevel + lbl_coaxLevel) 존재.
- Bottom: 슬롯 전환 시 GetSlotRefPose 로 동축 복원, 변경 시 TrySaveCoax 로 슬롯 JSON 저장.
- Tray: Loaded 시 Tray.json 동축 복원, 변경 시 TrySaveCoax(slot=None) 저장.
- 양쪽: Grab(#else)/Teach/Run 직전 ApplyCoaxLight 자동 적용.
- 모든 변경 라인에 `//260626 hbk` / `<!--260626 hbk-->` 주석.
- 기존 Align UI 동작 회귀 0. Grid.Row/RowDefinition 정합.
</verification>

<success_criteria>
Bottom/Tray Align 창에서 작업자가 동축 ON/OFF + 밝기를 조정하면 즉시 LIGHT_ALIGN_COAX 에 반영되고 슬롯/Tray JSON 에 저장된다. 슬롯 선택/창 진입 시 저장 동축값이 UI 에 복원되고, Teach/Run/Grab 직전 자동 적용되어 티칭=런타임 조명이 일치한다. msbuild Debug/x64 PASS. 기존 Align UI 회귀 0.
</success_criteria>

<output>
완료 후 `.planning/phases/66-ring7-coax-align-2026-06-26/66-03-SUMMARY.md` 생성.
</output>
