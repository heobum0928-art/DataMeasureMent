---
phase: 67-stat-01-2026-07-07
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Setting/SystemSetting.cs
  - WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs
  - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [STAT-01]

must_haves:
  truths:
    - "검사 완료(SaveAsync 호출) 시 StatisticsSavePath\\yyyyMMdd.csv 에 측정 항목당 1행이 append 된다 (v2.6/v1.0/수동 3경로 모두 SaveAsync 로 수렴 → 단일 훅으로 자동 커버)"
    - "CSV append 는 별도 try/catch 로 격리되어 실패해도 검사/TCP 응답/cycle.json 저장에 영향이 없다 (D-04)"
    - "측정명/타입명에 콤마·따옴표가 있어도 RFC4180 따옴표 이스케이프로 CSV 가 깨지지 않는다 (D-03)"
    - "복수 InspectionSequence 스레드가 근접 완료해도 static lock 으로 append 경합이 방지된다 (D-05)"
    - "설정 창(PropertyGrid)에 StatisticsSavePath(기본값 AppDomain BaseDirectory + Statistics)가 노출되고 경로 변경이 가능하다 (D-01)"
    - "구 INI 에 StatisticsSavePath 키가 없어도 기본값이 보존되어 로드가 깨지지 않는다 (하위호환)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs"
      provides: "Append(CycleResultDto) 정적 메서드 — 측정항목당 1행 CSV append + RFC4180 이스케이프 + static lock + 일자별 파일"
      contains: "public static void Append"
    - path: "WPF_Example/Setting/SystemSetting.cs"
      provides: "StatisticsSavePath [DirectoryPath][AutoUpdateText] 프로퍼티"
      contains: "StatisticsSavePath"
    - path: "WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs"
      provides: "SaveAsync 내부 Task 에 MeasurementHistoryCsvWriter.Append 훅 (독립 try/catch)"
      contains: "MeasurementHistoryCsvWriter.Append"
  key_links:
    - from: "CycleResultSerializer.SaveAsync (background Task)"
      to: "MeasurementHistoryCsvWriter.Append(dto)"
      via: "JSON 쓰기 뒤 별도 try/catch 호출"
      pattern: "MeasurementHistoryCsvWriter\\.Append\\(dto\\)"
    - from: "MeasurementHistoryCsvWriter.Append"
      to: "StatisticsSavePath\\yyyyMMdd.csv"
      via: "SystemHandler.Handle.Setting.StatisticsSavePath + File.AppendAllText (lock)"
      pattern: "StatisticsSavePath"
---

<objective>
양산 검사 결과를 일자별 CSV 로 지속 누적하는 **수집 계층**을 만든다. 세 가지 변경:
1. **StatisticsSavePath 설정 추가** — `SystemSetting` 에 `[DirectoryPath][AutoUpdateText]` 프로퍼티(기본값 `AppDomain.CurrentDomain.BaseDirectory + @"Statistics"`), `ResultSavePath` 와 동일 패턴 (D-01).
2. **신규 CSV writer** — `MeasurementHistoryCsvWriter.Append(CycleResultDto)` 정적 메서드. dto 를 Shot→FAI→Measurement 3중 루프로 평탄화하여 **측정 항목당 1행**을 `StatisticsSavePath\yyyyMMdd.csv` 에 append. RFC4180 따옴표 이스케이프 + static lock + 파일 신규 생성 시 헤더 1행 (D-02/D-03/D-05).
3. **수집 훅** — `CycleResultSerializer.SaveAsync` 의 background Task 안, cycle.json 쓰기 직후에 `MeasurementHistoryCsvWriter.Append(dto)` 를 **독립 try/catch** 로 호출 (D-04). v2.6/v1.0/수동 3경로가 모두 SaveAsync 로 수렴하므로 이 한 곳만 훅하면 전 경로 자동 커버.

Purpose: 수천 cycle.json 폴더 스캔 없이 기간·레시피로 빠르게 조회 가능한 누적 이력 스토어를 확보한다. 이 Plan 은 데이터 계층 — 조회(Plan 02)/UI(Plan 03) 가 이 CSV 스키마를 소비한다.
Output: StatisticsSavePath 설정 + CSV writer + SaveAsync 훅. 검사/TCP 회귀 0.

**잠금 결정(CONTEXT.md):** D-01(StatisticsSavePath), D-02(일자별 1파일 yyyyMMdd.csv, 레시피 혼합/컬럼 구분), D-03(고정 14컬럼 스키마 + RFC4180 이스케이프), D-04(SaveAsync 단일 훅, 예외 격리), D-05(static lock append).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-stat-01-2026-07-07/67-CONTEXT.md
@.planning/phases/67-stat-01-2026-07-07/67-PATTERNS.md

# 핵심 변경/참조 대상 — 반드시 먼저 읽을 것
@WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs
@WPF_Example/UI/ViewModel/CycleResultDto.cs
@WPF_Example/Setting/SystemSetting.cs

<interfaces>
<!-- 코드 검증 완료 2026-07-07. 실행자는 아래 계약을 그대로 사용 — 재탐색 불필요. -->

CycleResultDto 계층 (WPF_Example/UI/ViewModel/CycleResultDto.cs — 확인됨):
```csharp
class CycleResultDto {
    DateTime InspectionTime;
    string RecipeName;
    int IndexNumber = -1;          // 자재번호, -1=미수신
    string OverallJudgement;       // "OK" | "NG" | "DETECT_FAIL"
    List<ShotResultDto> Shots;
}
class ShotResultDto { string ShotName; string OwnerSequenceName; List<FaiResultDto> FAIs; }
class FaiResultDto  { string FAIName; bool IsPass; bool WasDatumSkipped; List<MeasurementResultDto> Measurements; }
class MeasurementResultDto {
    string MeasurementName; string TypeName;
    double NominalValue; double TolerancePlus; double ToleranceMinus;
    double LastMeasuredValue;
    bool LastJudgement;    // true=OK, false=NG/미측정
    bool LastHasResult;    // false=측정 미실행 (0.0 정상결과 구분)
    string LastSkipReason; // null=정상, "DATUM_FAIL" | "NO_IMAGE"=검출실패
}
```

CycleResultSerializer.SaveAsync 현재 본문 (line 162-200 — 훅 삽입 대상):
```csharp
public static void SaveAsync(CycleResultDto dto)
{
    if (dto == null) { return; }
    string dateDir = Path.Combine(SystemHandler.Handle.Setting.ResultSavePath, dto.InspectionTime.ToString("yyyyMMdd"));
    string cycleDir = Path.Combine(dateDir, dto.InspectionTime.ToString("HHmmss") + "_cycle");
    dto.CycleFolderPath = cycleDir;
    Task.Factory.StartNew(() =>
    {
        try
        {
            Directory.CreateDirectory(cycleDir);
            string jsonPath = Path.Combine(cycleDir, "cycle.json");
            string json = JsonConvert.SerializeObject(dto, Formatting.Indented);
            File.WriteAllText(jsonPath, json, System.Text.Encoding.UTF8);
        }
        catch (Exception ex)
        {
            try { Logging.PrintErrLog((int)ELogType.Error, "[CycleResultSerializer] Save failed: " + ex.Message); } catch { }
        }
    });
}
```

SystemSetting.cs 경로 프로퍼티 패턴 (line 64-66, 84-89 — 확인됨):
```csharp
[DirectoryPath]
[AutoUpdateText]
public string ResultSavePath { get; set; } = AppDomain.CurrentDomain.BaseDirectory + @"Result";   // line 64-66, [Category("Path|Log")] 그룹 상속
...
public int LogDeleteDay { get; set; } = 30;   // line 84 (Path|Log 그룹)
//data path
[Category("Path|MapData")]                     // line 87 — 여기서 그룹 리셋
[DirectoryPath]
[AutoUpdateText]
public string ...MapData... 
```

SystemSetting.Load() 그룹 상속 로직 (line 197-243 — 확인됨, ⚠ 배치 규칙의 근거):
- `group` 변수는 `[Category]` 어트리뷰트가 있는 프로퍼티에서만 갱신되고 이후 프로퍼티로 **상속**된다.
- String + `[DirectoryPath]` 프로퍼티는 `loadFile[group][name]` 값으로 로드. **키 부재 시** 빈 문자열 → `Directory.Exists("")==false` → `Directory.CreateDirectory("")` 가 예외 → try/catch 삼킴 → `prop.SetValue` **미실행** → C# 프로퍼티 초기값(기본 경로) **보존**. (누락 키 하위호환 자동 성립.)
</interfaces>
</context>

<coding_rules>
- **삼항 `?:` 금지 → if/else. null 병합 `??` 금지 → if/else.** (CycleResultSerializer 기존 코드 스타일 무관 — 신규/수정 라인은 규칙 준수.)
- 헝가리언: 지역/필드 `b/n/f/d/sz` 접두, 멤버 `m_`(신규 파일), 상수 UPPER_SNAKE_CASE.
- 함수 30줄 이내 / 단일책임. 매직넘버 상수화.
- C# 7.2 (.NET 4.8) — switch expression / nullable ref / record / using 선언 / range 금지.
- 신규 파일 `MeasurementHistoryCsvWriter.cs` = **Allman 중괄호**(최근 Halcon/Service 관례). 기존 파일(SystemSetting/CycleResultSerializer) 수정분은 그 파일 관례를 따름.
- 수정/추가 라인 끝(또는 위)에 `//260707 hbk` + 한국어 사유 주석.
- 파일 I/O 는 try/catch + Logging.PrintErrLog (기존 SaveAsync 패턴). 비치명 실패는 bare `catch { }` 허용.
- 숫자 포맷은 **InvariantCulture** 사용(로케일 콤마 소수점 방지) — `ToString("F4", CultureInfo.InvariantCulture)`.
</coding_rules>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SystemSetting 에 StatisticsSavePath 추가 (D-01)</name>
  <files>WPF_Example/Setting/SystemSetting.cs</files>
  <read_first>
    - WPF_Example/Setting/SystemSetting.cs:40-90 (ResultSavePath 등 [DirectoryPath] 패턴 + LogDeleteDay(line 84) + [Category("Path|MapData")](line 87))
    - WPF_Example/Setting/SystemSetting.cs:197-260 (Load() 의 group 상속 로직 — 배치 규칙 근거)
  </read_first>
  <action>
    `StatisticsSavePath` String 프로퍼티를 추가한다.

    **배치 규칙(⚠ 회귀 방지 — 반드시 준수):** `LogDeleteDay`(line 84) **뒤**, `//data path` 주석 + `[Category("Path|MapData")]`(line 86-87) **앞**에 삽입한다. 여기에 **자체 `[Category("Path|Statistics")]`** 를 붙인다. 이유: SystemSetting.Load()/Save() 의 `group` 변수는 `[Category]` 있는 프로퍼티에서만 갱신·이후 상속된다. 만약 ResultSavePath 인접(Path|Log 그룹 중간)에 자체 Category 를 붙이면 **뒤따르는 ErrorSavePath/CameraLogSavePath 등이 새 그룹으로 밀려 INI 섹션이 바뀌는 회귀**가 생긴다. LogDeleteDay 뒤 = 바로 다음 프로퍼티가 이미 `[Category("Path|MapData")]` 로 그룹을 리셋하므로, StatisticsSavePath 만 Path|Statistics 그룹이 되고 누출 0.

    삽입할 코드:
    ```csharp
    //data statistics                                              //260707 hbk STAT-01 D-01: 양산 이력 통계 CSV 저장 경로
    [Category("Path|Statistics")]                                   //260707 hbk STAT-01 D-01: 자체 그룹(뒤 MapData 가 리셋 → 그룹 누출 0)
    [DirectoryPath]                                                 //260707 hbk STAT-01 D-01
    [AutoUpdateText]                                                //260707 hbk STAT-01 D-01
    public string StatisticsSavePath { get; set; } = AppDomain.CurrentDomain.BaseDirectory + @"Statistics";   //260707 hbk STAT-01 D-01
    ```

    주의: 구 INI 에 `[Path|Statistics]` 섹션/키가 없으면 Load 시 CreateDirectory("") 예외 → SetValue 미실행 → 위 기본값 보존(하위호환 자동 성립, interfaces 블록 참조). Custom/SystemSetting.cs 별도 폴백 불필요.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && grep -c "StatisticsSavePath" WPF_Example/Setting/SystemSetting.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "public string StatisticsSavePath" WPF_Example/Setting/SystemSetting.cs` → `= AppDomain.CurrentDomain.BaseDirectory + @"Statistics";` 존재.
    - `grep -B4 "public string StatisticsSavePath" ...` 출력에 `[DirectoryPath]` + `[AutoUpdateText]` + `[Category("Path|Statistics")]` 3종 모두 포함.
    - StatisticsSavePath 는 LogDeleteDay 뒤 · `[Category("Path|MapData")]` 앞에 위치: `grep -A2 "LogDeleteDay" ...` 인근에 Statistics 관련 라인 등장.
    - 기존 ResultSavePath/ErrorSavePath 라인 무변경 (회귀 0).
    - 삼항 `?:` / `??` 미사용. `//260707 hbk` 주석 존재.
  </acceptance_criteria>
  <done>StatisticsSavePath 프로퍼티 추가(자체 Path|Statistics 그룹, 기본값 BaseDirectory+Statistics). 그룹 누출·기존 경로 회귀 0. 빌드는 Task 4 에서 통합 검증.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MeasurementHistoryCsvWriter 신규 생성 — 측정항목당 1행 append (D-02/D-03/D-05)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs:35-156 (BuildDto 의 Shot→FAI→Measurement 3중 루프 구조 — writer 가 동일 루프로 평탄화)
    - WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs:60-127 (AddSample 의 skip/hasResult/judgement 정책 — Judgement 컬럼 도출 근거)
    - WPF_Example/UI/ViewModel/CycleResultDto.cs (DTO 필드)
  </read_first>
  <action>
    신규 파일 `WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs` 를 생성한다. 네임스페이스 `ReringProject.Sequence` (CycleResultSerializer/RepeatMeasurementStats 와 동일). Allman 중괄호.

    **using:** System, System.Collections.Generic, System.Globalization, System.IO, System.Text, ReringProject.UI (CycleResultDto), ReringProject.Utility (Logging/ELogType), ReringProject.Setting.

    **클래스 = `public static class MeasurementHistoryCsvWriter`.**

    **상수 (매직넘버 금지):**
    ```csharp
    private const string CSV_EXT = ".csv";
    private const string CSV_HEADER = "검사일시,RecipeName,IndexNumber,ShotName,FAIName,MeasurementName,TypeName,NominalValue,TolerancePlus,ToleranceMinus,MeasuredValue,Judgement,HasResult,OverallCycleResult";
    private const string NUM_FORMAT = "F4";
    ```

    **static lock (D-05):**
    ```csharp
    private static readonly object s_lock = new object();   //260707 hbk STAT-01 D-05: 복수 InspectionSequence append 경합 방지
    ```

    **(1) public static void Append(CycleResultDto dto):**
    - `if (dto == null) { return; }`  그리고 `if (dto.Shots == null) { return; }`
    - 경로: `string szDir = SystemHandler.Handle.Setting.StatisticsSavePath;` → `if (string.IsNullOrEmpty(szDir)) { return; }`
    - 파일: `string szPath = Path.Combine(szDir, dto.InspectionTime.ToString("yyyyMMdd") + CSV_EXT);`
    - `string szOverall = MapOverall(dto.OverallJudgement);` (헬퍼, 아래)
    - StringBuilder 에 dto 를 3중 루프로 평탄화하여 측정 항목당 1줄 생성 (BuildLine 헬퍼 호출). 루프 도중 shot/fai/measurements null 가드(RepeatMeasurementStats.AddSample 와 동일하게 continue).
    - **파일 쓰기 (lock 구간 최소화):** 생성된 문자열이 비어있지 않으면
      ```csharp
      lock (s_lock)
      {
          Directory.CreateDirectory(szDir);                          //260707 hbk 존재해도 무해
          bool bNewFile = !File.Exists(szPath);                      //260707 hbk 신규 파일 → 헤더 1행
          if (bNewFile)
          {
              File.AppendAllText(szPath, CSV_HEADER + Environment.NewLine, Encoding.UTF8);
          }
          File.AppendAllText(szPath, sb.ToString(), Encoding.UTF8);
      }
      ```
    - **예외:** 메서드 전체를 try/catch 로 감싸고 `catch (Exception ex) { try { Logging.PrintErrLog((int)ELogType.Error, "[MeasurementHistoryCsvWriter] Append failed: " + ex.Message); } catch { } }`. (SaveAsync 훅 쪽에도 별도 try 있으나 방어적 이중 격리.)

    **(2) private static void AppendMeasurementLines(StringBuilder sb, CycleResultDto dto, string szOverall):** (Append 를 30줄 이내로 유지하기 위해 3중 루프를 분리)
    - `foreach shot in dto.Shots` (null continue) → `foreach fai in shot.FAIs` (null continue) → `foreach meas in fai.Measurements` (null continue) → `sb.Append(BuildLine(dto, shot, fai, meas, szOverall)); sb.Append(Environment.NewLine);`

    **(3) private static string BuildLine(CycleResultDto dto, ShotResultDto shot, FaiResultDto fai, MeasurementResultDto meas, string szOverall):** 14개 필드를 콤마로 join. 컬럼 순서 = CSV_HEADER 그대로:
    1. `dto.InspectionTime.ToString("yyyy-MM-dd HH:mm:ss")`
    2. Esc(dto.RecipeName)
    3. `dto.IndexNumber.ToString(CultureInfo.InvariantCulture)`
    4. Esc(shot.ShotName)
    5. Esc(fai.FAIName)
    6. Esc(meas.MeasurementName)
    7. Esc(meas.TypeName)
    8. `meas.NominalValue.ToString(NUM_FORMAT, CultureInfo.InvariantCulture)`
    9. `meas.TolerancePlus.ToString(NUM_FORMAT, CultureInfo.InvariantCulture)`
    10. `meas.ToleranceMinus.ToString(NUM_FORMAT, CultureInfo.InvariantCulture)`
    11. `meas.LastMeasuredValue.ToString(NUM_FORMAT, CultureInfo.InvariantCulture)`
    12. `MapJudgement(meas)` (헬퍼)
    13. `meas.LastHasResult.ToString()` (True/False)
    14. Esc(szOverall)
    → `string.Join(",", 위 14개)`.

    **(4) private static string MapJudgement(MeasurementResultDto meas):** RepeatMeasurementStats 정책과 100% 일치(if/else, 삼항 금지):
    ```csharp
    if (meas.LastSkipReason == "DATUM_FAIL") { return "DATUM_FAIL"; }
    if (meas.LastSkipReason == "NO_IMAGE")   { return "NO_IMAGE"; }
    if (meas.LastHasResult == false)         { return "NO_RESULT"; }   //260707 hbk 미측정(값 없음) — 로더가 통계서 제외
    if (meas.LastJudgement)                  { return "OK"; }
    return "NG";
    ```

    **(5) private static string MapOverall(string szOverallJudgement):** dto.OverallJudgement("OK"/"NG"/"DETECT_FAIL") → D-03 의 P|F|N (if/else):
    ```csharp
    if (szOverallJudgement == "OK") { return "P"; }
    if (szOverallJudgement == "NG") { return "F"; }
    return "N";   // DETECT_FAIL 또는 기타
    ```

    **(6) private static string Esc(string szValue) — RFC4180 이스케이프 (D-03):**
    ```csharp
    if (szValue == null) { szValue = ""; }
    bool bNeedQuote = szValue.IndexOf(',') >= 0 || szValue.IndexOf('"') >= 0 || szValue.IndexOf('\r') >= 0 || szValue.IndexOf('\n') >= 0;
    if (bNeedQuote)
    {
        string szEscaped = szValue.Replace("\"", "\"\"");
        return "\"" + szEscaped + "\"";
    }
    return szValue;
    ```
    (CSV 인젝션 프리픽스(`=/+/-/@`)는 적용하지 않는다 — 내부 기계 생성 측정 데이터, 저위험 accept. 프리픽스는 로더 왕복 파싱을 깨뜨리므로 미적용. threat_model 참조.)

    각 헬퍼는 단일책임·30줄 이내를 유지한다.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && grep -c "public static void Append\|CSV_HEADER\|s_lock\|private static string Esc\|MapJudgement\|MapOverall" WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs</automated>
  </verify>
  <acceptance_criteria>
    - 파일 존재 + `public static class MeasurementHistoryCsvWriter` + `namespace ReringProject.Sequence`.
    - `public static void Append(CycleResultDto dto)` 시그니처 존재.
    - CSV_HEADER 상수 = 정확히 14 컬럼 순서(검사일시,RecipeName,IndexNumber,ShotName,FAIName,MeasurementName,TypeName,NominalValue,TolerancePlus,ToleranceMinus,MeasuredValue,Judgement,HasResult,OverallCycleResult).
    - `private static readonly object s_lock` + `lock (s_lock)` 존재. `File.AppendAllText` 가 lock 구간 내부.
    - RFC4180 이스케이프 헬퍼: `Replace("\"", "\"\"")` + 콤마/따옴표/개행 감지.
    - MapJudgement 5분기(DATUM_FAIL/NO_IMAGE/NO_RESULT/OK/NG) + MapOverall(P/F/N) 존재.
    - `ToString("F4", CultureInfo.InvariantCulture)` 등 InvariantCulture 사용.
    - 삼항 `?:` / `??` 미사용. Allman 중괄호. `//260707 hbk` 주석 다수.
  </acceptance_criteria>
  <done>MeasurementHistoryCsvWriter.Append 가 dto 를 측정항목당 1행으로 StatisticsSavePath\yyyyMMdd.csv 에 append(신규 파일 헤더 + RFC4180 이스케이프 + static lock). 빌드는 Task 4.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: CycleResultSerializer.SaveAsync 에 CSV append 훅 삽입 — 독립 try/catch (D-04)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs:162-200 (SaveAsync 의 Task.Factory.StartNew 본문 — 훅 대상)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs (Task 2 결과 — Append 시그니처 확인)
  </read_first>
  <action>
    SaveAsync 의 `Task.Factory.StartNew(() => { ... })` 람다 본문을 수정한다. 기존은 **하나의 try/catch** 로 JSON 쓰기를 감싼다. 이를 **두 개의 독립 try/catch** 로 재구성한다: (1) 기존 JSON 쓰기 try (그대로 유지), (2) 그 뒤에 CSV append 를 위한 **새 try/catch**. D-04 = CSV append 는 JSON 성공/실패와 독립 + 검사/TCP 무영향.

    람다 본문을 아래로 교체한다 (기존 JSON try 블록은 한 글자도 바꾸지 말고, CSV try 블록만 그 **뒤**에 추가):
    ```csharp
    Task.Factory.StartNew(() =>
    {
        try
        {
            Directory.CreateDirectory(cycleDir);
            string jsonPath = Path.Combine(cycleDir, "cycle.json");
            string json = JsonConvert.SerializeObject(dto, Formatting.Indented);
            File.WriteAllText(jsonPath, json, System.Text.Encoding.UTF8);
        }
        catch (Exception ex)
        {
            try { Logging.PrintErrLog((int)ELogType.Error, "[CycleResultSerializer] Save failed: " + ex.Message); } catch { }
        }

        try   //260707 hbk STAT-01 D-04: CSV 이력 append — JSON 성공/실패와 독립(검사/TCP 무영향). v2.6/v1.0/수동 3경로 자동 커버.
        {
            MeasurementHistoryCsvWriter.Append(dto);
        }
        catch (Exception exCsv)   //260707 hbk STAT-01 D-04
        {
            try { Logging.PrintErrLog((int)ELogType.Error, "[CycleResultSerializer] CSV history append failed: " + exCsv.Message); } catch { }
        }
    });
    ```
    MeasurementHistoryCsvWriter 는 동일 네임스페이스(ReringProject.Sequence)이므로 추가 using 불필요. SaveAsync 시그니처/상단 경로 계산부는 무변경.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && grep -c "MeasurementHistoryCsvWriter.Append(dto)" WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "MeasurementHistoryCsvWriter.Append(dto)" CycleResultSerializer.cs` → 1건.
    - CSV append 가 **별도 try/catch** 안에 위치(JSON try 와 분리): `grep -c "catch (Exception exCsv)" CycleResultSerializer.cs` == 1.
    - 기존 JSON 쓰기 라인(`File.WriteAllText(jsonPath, json, ...)` + `"[CycleResultSerializer] Save failed:"`) 무변경.
    - CSV append 는 `Task.Factory.StartNew` 람다 **내부**(백그라운드 스레드).
    - `//260707 hbk` 주석 존재. 삼항 `?:` / `??` 미사용.
  </acceptance_criteria>
  <done>SaveAsync background Task 에 MeasurementHistoryCsvWriter.Append(dto) 훅이 독립 try/catch 로 추가됨. JSON 쓰기 무변경. 빌드는 Task 4.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: csproj 등록 + 빌드 검증 (Debug/x64) + 회귀 0</name>
  <files>WPF_Example/DatumMeasurement.csproj</files>
  <read_first>
    - WPF_Example/DatumMeasurement.csproj:259-260 (Custom\Sequence\Inspection\ 아래 Compile Include 등록 패턴 — CycleResultSerializer.cs / RepeatMeasurementStats.cs 인접)
  </read_first>
  <action>
    (1) **csproj 등록:** classic 스타일 csproj 는 신규 .cs 를 명시 등록해야 컴파일된다. 259-260 라인의 CycleResultSerializer/RepeatMeasurementStats Compile Include 인접(같은 ItemGroup)에 아래 한 줄을 추가한다 (알파벳/그룹 순서 무관, 인접 배치 권장):
    ```xml
    <Compile Include="Custom\Sequence\Inspection\MeasurementHistoryCsvWriter.cs" />
    ```

    (2) **빌드:** msbuild Debug/x64 를 실행하고 error 0, `Build succeeded` 를 확인한다. 실패 시 컴파일 오류(누락 using/오타/중괄호)를 수정 후 재빌드.

    (3) **회귀 점검:** `git diff --stat` 로 변경 파일이 SystemSetting.cs + MeasurementHistoryCsvWriter.cs(신규) + CycleResultSerializer.cs + DatumMeasurement.csproj 4개인지 확인. CycleResultSerializer 의 기존 JSON 쓰기 블록이 삭제/수정되지 않았는지(추가 라인만) git diff 로 확인.
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal /nologo 2>&1 | grep -iE "error|Build succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "MeasurementHistoryCsvWriter.cs" WPF_Example/DatumMeasurement.csproj` → `<Compile Include="Custom\Sequence\Inspection\MeasurementHistoryCsvWriter.cs" />` 존재.
    - msbuild Debug/x64 출력에 `Build succeeded`, `error` 0건.
    - `git diff` 가 CycleResultSerializer.cs 기존 JSON 쓰기 라인을 삭제(`-`)하지 않음 — CSV try 블록 추가(`+`)만.
    - 변경 파일 4개(SystemSetting/writer 신규/serializer/csproj)만.
  </acceptance_criteria>
  <done>msbuild Debug/x64 PASS. writer 신규 파일 csproj 등록. 기존 SaveAsync/설정 회귀 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (신규 경계 없음) | CSV 경로 = SystemSetting.StatisticsSavePath + `dto.InspectionTime` 날짜 포맷팅만 사용. 외부 입력 없음 → path traversal 불가(기존 SaveAsync 의 ResultSavePath 패턴과 동일). CSV 내용 = 내부 InspectionRecipeManager 스냅샷(측정값/레시피명/측정명) — 신뢰 데이터. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Tampering | CSV 파일(측정명·타입명 콤마 포함) | mitigate | RFC4180 따옴표 이스케이프(Esc 헬퍼)로 필드 구분자 오염 방지 — 컬럼 어긋남/파싱 붕괴 차단. |
| T-67-02 | Elevation (CSV injection) | Excel 오픈 시 `=/+/-/@` 시작 필드 수식 실행 | accept | 필드 = 내부 기계 생성 측정 데이터(측정명/레시피명). 외부 사용자 입력 유입점 없음 → 저위험. 프리픽스 이스케이프는 writer↔loader 왕복 파싱을 깨뜨려 미적용(정확성 우선). |
| T-67-03 | Denial of Service | 동시 append 경합(복수 InspectionSequence) | mitigate | static `s_lock` 단일 구간 append. 실패해도 독립 try/catch 로 검사/TCP 무영향(D-04). |

신규 high/medium 위협 없음 — 로컬 신뢰 파일 I/O.
</threat_model>

<verification>
- msbuild Debug/x64 PASS (error 0).
- SystemSetting: StatisticsSavePath [DirectoryPath][AutoUpdateText][Category("Path|Statistics")] 존재, 기존 경로 회귀 0.
- MeasurementHistoryCsvWriter: Append(dto) 측정항목당 1행 + 14컬럼 헤더 + RFC4180 Esc + static lock.
- CycleResultSerializer: SaveAsync Task 에 Append(dto) 독립 try/catch 훅. JSON 쓰기 무변경.
- csproj: writer 등록. 모든 변경 라인 `//260707 hbk`.
</verification>

<success_criteria>
검사 완료 시 StatisticsSavePath\yyyyMMdd.csv 에 측정항목당 1행이 자동 누적되고(3경로 자동 커버), CSV append 실패가 검사/TCP/cycle.json 에 영향 없으며, 설정창에 StatisticsSavePath 가 노출된다. msbuild Debug/x64 PASS, 회귀 0.
</success_criteria>

<output>
완료 후 `.planning/phases/67-stat-01-2026-07-07/67-01-SUMMARY.md` 생성.
</output>
