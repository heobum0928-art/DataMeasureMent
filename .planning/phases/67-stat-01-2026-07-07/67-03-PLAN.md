---
phase: 67-stat-01-2026-07-07
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - WPF_Example/UI/Statistics/StatisticsWindow.xaml
  - WPF_Example/UI/Statistics/StatisticsWindow.xaml.cs
  - WPF_Example/MainWindow.xaml.cs
  - WPF_Example/UI/MenuBar.xaml
  - WPF_Example/UI/MenuBar.xaml.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [STAT-01]

must_haves:
  truths:
    - "메뉴바 '통계분석' 버튼 클릭 → EPageType.Statistics → StatisticsWindow 가 비모달 Show() 로 열리고, 이미 열려있으면 재사용/포커스한다 (라이브 MainView 방해 없음) (D-08/D-09)"
    - "창 오픈 시 기간 DatePicker 가 오늘(from=to=today) 기본값이고 오늘 CSV 1개만 로드한다 (D-10)"
    - "기간(from~to) + 레시피 드롭다운(로드된 distinct RecipeName) + [조회] 버튼으로 필터한다 (D-11)"
    - "DataGrid 에 측정 항목별 N/Mean/StdDev/Range/Cpk/OK/NG/DetectFail/불량률 이 표시된다 (불량률=NG/(OK+NG), Cpk=∞ 표시 처리) (D-06)"
    - "DataGrid 행 클릭(SelectionChanged) → 해당 Shot/FAI/측정명의 히스토그램(공차 USL/LSL 수직선) + 추이 차트(x=샘플 인덱스, 평균/USL/LSL 선)가 갱신된다 (D-12/D-13/D-14)"
    - "차트는 ChartDirector.Net(netchartdir XYChart + WPFChartViewer, 기존 의존성)로 렌더된다"
    - "msbuild Debug/x64 PASS, 기존 검사/Align/리뷰어 회귀 0"
  artifacts:
    - path: "WPF_Example/UI/Statistics/StatisticsWindow.xaml.cs"
      provides: "비모달 통계창 code-behind — DoQuery(MeasurementHistoryCsvLoader.Query) + DataGrid 바인딩 + SelectionChanged 차트 갱신 + ChartDirector 렌더"
      contains: "MeasurementHistoryCsvLoader.Query"
    - path: "WPF_Example/UI/Statistics/StatisticsWindow.xaml"
      provides: "기간 DatePicker + 레시피 ComboBox + [조회] + DataGrid + WPFChartViewer 2개(히스토그램/추이)"
      contains: "WPFChartViewer"
    - path: "WPF_Example/MainWindow.xaml.cs"
      provides: "EPageType.Statistics + mStatisticsWindow 멤버 + PopupView 비모달 Show 분기"
      contains: "EPageType.Statistics"
    - path: "WPF_Example/UI/MenuBar.xaml.cs"
      provides: "Button_Statistics_Click → PopupView(EPageType.Statistics)"
      contains: "EPageType.Statistics"
  key_links:
    - from: "MenuBar Button_Statistics"
      to: "MainWindow.PopupView(EPageType.Statistics)"
      via: "Button_Statistics_Click 핸들러"
      pattern: "EPageType.Statistics"
    - from: "StatisticsWindow [조회]"
      to: "MeasurementHistoryCsvLoader.Query(from,to,recipe)"
      via: "DoQuery → DataGrid ItemsSource + 레시피 드롭다운"
      pattern: "MeasurementHistoryCsvLoader\\.Query"
    - from: "DataGrid SelectionChanged"
      to: "WPFChartViewer 히스토그램 + 추이 갱신"
      via: "선택 StatRow.Key → StatisticsQueryResult.Series[key] → XYChart 렌더"
      pattern: "SelectionChanged"
---

<objective>
통계 조회 **UI 계층** — 신규 비모달 `StatisticsWindow`(ReviewerWindow 미러링) + 메뉴 진입점. 구성:
1. **StatisticsWindow (신규 xaml + xaml.cs)** — 기간 DatePicker(기본 오늘) + 레시피 ComboBox + [조회] 버튼 + DataGrid(통계 테이블) + WPFChartViewer 2개(히스토그램/추이). `MeasurementHistoryCsvLoader.Query` 소비. DataGrid 행 클릭 → 선택 항목 차트 갱신 (D-10~D-14).
2. **메뉴 진입 (MainWindow + MenuBar)** — `EPageType.Statistics` enum + `mStatisticsWindow` 멤버 + `PopupView` 비모달 Show 분기(Reviewer 완전 미러) + MenuBar "통계분석" 버튼 (D-08/D-09).
3. **차트 (ChartDirector.Net)** — 히스토그램 = BarLayer(분포) + xAxis USL/LSL 수직 markLine. 추이 = LineLayer(x=샘플 인덱스 1..N) + yAxis 평균/USL/LSL 수평 markLine. netchartdir 는 in-repo 사용처가 없으므로 아래 API 스케치 + 패키지 XML 문서를 참조 (D-13/D-14).

Purpose: 작업자가 라이브 검사와 병행하여 기간·레시피별 공정능력(Cpk/불량률)과 분포/추이를 한 창에서 확인한다.
Output: StatisticsWindow + 메뉴 버튼. 검사/리뷰어 회귀 0.

**잠금 결정:** D-08(ReviewerWindow 미러, 비모달, mStatisticsWindow 재사용), D-09(EPageType.Statistics + "통계분석"), D-10(기본 오늘), D-11(기간+레시피+조회), D-12(행클릭→차트), D-13(추이 x=샘플인덱스+평균선), D-14(히스토그램+USL/LSL 수직선, bin=고정 20).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-stat-01-2026-07-07/67-CONTEXT.md
@.planning/phases/67-stat-01-2026-07-07/67-PATTERNS.md
@.planning/phases/67-stat-01-2026-07-07/67-02-PLAN.md

# 핵심 참조/미러링 대상 — 반드시 먼저 읽을 것
@WPF_Example/UI/Reviewer/ReviewerWindow.xaml
@WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs

<interfaces>
<!-- 코드 검증 완료 2026-07-07. -->

Plan 02 조회 API (WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs):
```csharp
namespace ReringProject.Sequence;
public class StatisticsQueryResult {
    Dictionary<string, MeasurementStat> Stats;   // key = ShotName + "/" + FAIName + "/" + MeasurementName
    Dictionary<string, List<double>> Series;      // 동일 key, 순서 유지 원시 측정값
    List<string> RecipeNames;                     // distinct(필터 무관)
    int TotalRowCount;
}
public static class MeasurementHistoryCsvLoader {
    static StatisticsQueryResult Query(DateTime dtFrom, DateTime dtTo, string szRecipeFilter);  // 필터 "" = 전체
}
// MeasurementStat (namespace ReringProject.Sequence): ShotName/FAIName/MeasurementName/TypeName/N/Mean/StdDev/
//   Range/Cpk/NominalValue/TolerancePlus/ToleranceMinus/OkCount/NgCount/DetectFailCount. Cpk=PositiveInfinity 가능.
```

MainWindow EPageType enum (WPF_Example/MainWindow.xaml.cs:20-31 — 확인됨):
```csharp
public enum EPageType {
    Ready, Recipe, Log, Setting, Camera, Light, Connect, Login, ProcessMonitor,
    Reviewer,   // Phase 40 OUT-01 D-08 — 결과 리뷰어 비모달 창
}
```
멤버 필드 (line 68-70): `private ProcessMonitorWindow mProcMonitorWindow; private UI.ReviewerWindow mReviewerWindow;`

PopupView Reviewer 케이스 (line 355-363 — 미러 대상, switch(page) 내부, K&R 스타일):
```csharp
case EPageType.Reviewer:   // 비모달 Show()
    if (mReviewerWindow != null && mReviewerWindow.IsLoaded) { mReviewerWindow.Show(); return; }
    mReviewerWindow = new UI.ReviewerWindow();
    mReviewerWindow.Owner = this;
    mReviewerWindow.Show();   // 비모달
    break;
```

MenuBar Reviewer 버튼 (WPF_Example/UI/MenuBar.xaml) — Button_Reviewer 는 Grid.Column="3" Grid.Row="1" 버튼(line 206-212). ⚠ 이 버튼이 속한 **부모 그리드**(및 그 ColumnDefinitions)를 read_first 로 확인하고, 그 그리드에 5번째 컬럼 + Grid.Column="4" 버튼을 추가한다. (일부 그리드는 4컬럼이 이미 참(Camera/Light/Connect/Reviewer). 컬럼 부족 시 ColumnDefinition 1개 추가.)
MenuBar 핸들러 (MenuBar.xaml.cs:104-107): `private void Button_Reviewer_Click(object sender, RoutedEventArgs e) { mParentWindow.PopupView(EPageType.Reviewer); }`  (mParentWindow = MainWindow, K&R 스타일)

ReviewerWindow code-behind 스타일 (WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs — 확인됨): ViewModel 없이 직접 code-behind, `x:Class="ReringProject.UI.ReviewerWindow"`, DataGrid `ItemsSource` 직접 세팅, try/catch + Logging.PrintErrLog 파일가드. StatisticsWindow 도 동일 스타일.

ChartDirector (csproj 이미 참조됨 — line 91-105 netchartdir + Desktop.Controls, net40):
- 어셈블리 `netchartdir` → 네임스페이스 `ChartDirector` (XYChart/BarLayer/LineLayer/Axis 확인됨).
- WPF 뷰어 컨트롤 `WPFChartViewer` (ChartDirector.Net.Desktop.Controls.dll, 네임스페이스 `ChartDirector` — DLL 심볼 확인됨).
- 확인된 API: `new XYChart(nW, nH)`, `chart.setPlotArea(...)`, `chart.addBarLayer(double[])`, `chart.addLineLayer(double[])`, `chart.xAxis()`, `chart.yAxis()`, `Axis.addMark(double value, int color, string label)`, `Axis.setLabels(...)`.
- 뷰어 바인딩: `wpfChartViewer.Chart = chart;` (BaseChart 대입 → 렌더).
- 상세 시그니처는 `packages/ChartDirector.Net.7.1.0/lib/net40/netchartdir.xml` 참조(실행자 필독).
</interfaces>
</context>

<coding_rules>
- **삼항 `?:` 금지 → if/else. null 병합 `??` 금지 → if/else.**
- 헝가리언: 지역/필드 `b/n/f/d/sz`, 멤버 `m_`(신규 파일), 상수 UPPER_SNAKE_CASE.
- 함수 30줄 이내 / 단일책임. 매직넘버 상수화(BIN_COUNT/CHART_W/CHART_H 등).
- C# 7.2 (.NET 4.8) — switch expression / nullable ref / record / range 금지.
- 신규 파일 = **Allman 중괄호**. MainWindow/MenuBar 수정분 = 그 파일 관례(K&R 같은 줄).
- 수정/추가 라인에 `//260707 hbk` + 한국어 사유 주석.
- 파일/조회 실패는 try/catch + Logging.PrintErrLog + 빈 상태 폴백(ReviewerWindow 패턴).
- 불량률/Cpk 계산은 if/else — 분모 0, PositiveInfinity, NaN 방어.
</coding_rules>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: StatisticsWindow 신규 생성 (xaml + xaml.cs) — 조회/테이블/차트 (D-10~D-14)</name>
  <files>WPF_Example/UI/Statistics/StatisticsWindow.xaml, WPF_Example/UI/Statistics/StatisticsWindow.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml (Window 루트/DataGrid/컬럼/RowStyle 패턴 — 미러)
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs (code-behind 스타일: ItemsSource 직접 세팅 + try/catch + Logging)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs (Query/StatisticsQueryResult/MeasurementStat 계약)
    - packages/ChartDirector.Net.7.1.0/lib/net40/netchartdir.xml (XYChart/BarLayer/LineLayer/Axis.addMark 시그니처 — 차트 API 확정)
  </read_first>
  <action>
    신규 폴더 `WPF_Example/UI/Statistics/` 에 `StatisticsWindow.xaml` + `StatisticsWindow.xaml.cs` 생성. `x:Class="ReringProject.UI.StatisticsWindow"`, 네임스페이스 `ReringProject.UI`.

    **(A) StatisticsWindow.xaml — 레이아웃:**
    - Window: `Title="양산 이력 통계 분석" Height="820" Width="1280" WindowStartupLocation="CenterOwner"`.
    - ChartDirector 뷰어 xmlns: `xmlns:cd="clr-namespace:ChartDirector;assembly=ChartDirector.Net.Desktop.Controls"`.
    - 루트 Grid 3행: Row0=Auto(필터바), Row1=*(DataGrid), Row2=Auto(차트 340px).
    - **Row0 필터바 (StackPanel Horizontal):** `<DatePicker x:Name="dp_From"/>` `<DatePicker x:Name="dp_To"/>` `<ComboBox x:Name="combo_Recipe" Width="180"/>` `<Button x:Name="btn_Query" Content="조회" Click="Btn_Query_Click"/>`. 레이블 TextBlock("기간"/"~"/"레시피") 포함.
    - **Row1 DataGrid:** `x:Name="grid_Stats" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single" SelectionChanged="Grid_Stats_SelectionChanged"`. 컬럼(모두 DataGridTextColumn, Binding):
      `Shot`(ShotName) / `FAI`(FAIName) / `측정명`(MeasurementName) / `N`(N) / `평균`(Mean, StringFormat=F4) / `표준편차`(StdDev, F4) / `범위`(Range, F4) / `Cpk`(CpkText) / `OK`(OkCount) / `NG`(NgCount) / `검출실패`(DetectFailCount) / `불량률`(DefectRateText).
      NG 행 강조 RowStyle(선택) — ReviewerWindow line 186-191 DataTrigger 패턴 참고(필수 아님).
    - **Row2 차트 (Grid 2열 분할):** 좌 `<cd:WPFChartViewer x:Name="viewer_Histogram"/>` (헤더 "분포 히스토그램"), 우 `<cd:WPFChartViewer x:Name="viewer_Trend"/>` (헤더 "측정값 추이").

    **(B) StatisticsWindow.xaml.cs — code-behind:**
    using: System, System.Collections.Generic, System.Windows, System.Windows.Controls, ChartDirector, ReringProject.Sequence, ReringProject.Utility.

    **화면 행 클래스 (같은 파일 또는 인접):**
    ```csharp
    public class StatRow
    {
        public string ShotName { get; set; }
        public string FAIName { get; set; }
        public string MeasurementName { get; set; }
        public int N { get; set; }
        public double Mean { get; set; }
        public double StdDev { get; set; }
        public double Range { get; set; }
        public string CpkText { get; set; }        //260707 hbk ∞/NaN 표시 처리
        public int OkCount { get; set; }
        public int NgCount { get; set; }
        public int DetectFailCount { get; set; }
        public string DefectRateText { get; set; } //260707 hbk NG/(OK+NG)
        public string Key { get; set; }             //260707 hbk Series 조인 키
        public double NominalValue { get; set; }
        public double TolerancePlus { get; set; }
        public double ToleranceMinus { get; set; }
    }
    ```

    **멤버/상수:**
    ```csharp
    private StatisticsQueryResult m_lastResult;   //260707 hbk 마지막 조회 결과(Series 조회용)
    private const int BIN_COUNT = 20;             //260707 hbk D-14 히스토그램 bin
    private const int CHART_W = 560;
    private const int CHART_H = 320;
    private const int COLOR_USL = 0xcc0000;       // 빨강 (공차 상한)
    private const int COLOR_LSL = 0xcc0000;       // 빨강 (공차 하한)
    private const int COLOR_MEAN = 0x008800;      // 초록 (평균)
    private const int COLOR_BAR = 0x3366cc;
    private const int COLOR_LINE = 0x3366cc;
    ```

    **생성자 + 기본값(D-10):**
    ```csharp
    public StatisticsWindow()
    {
        InitializeComponent();
        dp_From.SelectedDate = DateTime.Today;   //260707 hbk D-10 기본 오늘
        dp_To.SelectedDate = DateTime.Today;
        DoQuery("");   // 오픈 시 오늘 전체 레시피
    }
    ```

    **Btn_Query_Click:** DatePicker/콤보 읽어 DoQuery 호출. 콤보 선택이 "전체"거나 null 이면 필터 "".
    ```csharp
    private void Btn_Query_Click(object sender, RoutedEventArgs e)
    {
        string szRecipe = "";
        if (combo_Recipe.SelectedItem != null)
        {
            string szSel = combo_Recipe.SelectedItem.ToString();
            if (szSel != "전체") { szRecipe = szSel; }
        }
        DoQuery(szRecipe);
    }
    ```

    **DoQuery(string szRecipeFilter):** (단일책임 — 조회 + 바인딩 + 드롭다운. 30줄 넘으면 드롭다운 채우기 PopulateRecipeCombo 로 분리.)
    ```csharp
    private void DoQuery(string szRecipeFilter)
    {
        try
        {
            DateTime dtFrom = DateTime.Today;
            if (dp_From.SelectedDate.HasValue) { dtFrom = dp_From.SelectedDate.Value; }
            DateTime dtTo = DateTime.Today;
            if (dp_To.SelectedDate.HasValue) { dtTo = dp_To.SelectedDate.Value; }

            m_lastResult = MeasurementHistoryCsvLoader.Query(dtFrom, dtTo, szRecipeFilter);
            PopulateRecipeCombo(m_lastResult.RecipeNames, szRecipeFilter);
            grid_Stats.ItemsSource = BuildRows(m_lastResult.Stats);
            ClearCharts();   // 새 조회 → 이전 차트 비움(행 선택 시 갱신)
        }
        catch (Exception ex)
        {
            try { Logging.PrintErrLog((int)ELogType.Error, "[StatisticsWindow] DoQuery: " + ex.Message); } catch { }
        }
    }
    ```

    **BuildRows(Dictionary<string,MeasurementStat> stats) → List<StatRow>:** 각 KeyValuePair 를 StatRow 로 변환. Key = kv.Key. CpkText/DefectRateText 는 헬퍼로 산출.
    - CpkText: `if (double.IsPositiveInfinity(cpk)) return "∞"; if (double.IsNaN(cpk)) return "-"; return cpk.ToString("F3");` (if/else).
    - DefectRateText: `int nTotal = ok + ng; if (nTotal == 0) return "-"; double d = ng * 100.0 / nTotal; return d.ToString("F2") + "%";`

    **PopulateRecipeCombo(List<string> names, string szCurrent):** 콤보를 재구성 — 첫 항목 "전체" + names. 현재 필터가 목록에 있으면 SelectedItem 유지, 없으면 "전체" 선택. (조회 버튼 방식이므로 SelectionChanged 핸들러는 두지 않음 → 재진입 이슈 없음. Items.Clear/Add 사용.)

    **Grid_Stats_SelectionChanged (D-12):**
    ```csharp
    private void Grid_Stats_SelectionChanged(object sender, SelectionChangedEventArgs e)
    {
        StatRow row = grid_Stats.SelectedItem as StatRow;
        if (row == null) { return; }
        if (m_lastResult == null) { return; }
        List<double> values;
        if (!m_lastResult.Series.TryGetValue(row.Key, out values)) { values = new List<double>(); }
        double dUsl = row.NominalValue + row.TolerancePlus;
        double dLsl = row.NominalValue - Math.Abs(row.ToleranceMinus);
        RenderHistogram(values, dUsl, dLsl);
        RenderTrend(values, row.Mean, dUsl, dLsl);
    }
    ```

    **RenderHistogram(List<double> values, double dUsl, double dLsl) — ChartDirector BarLayer (D-14):**
    - values 비었으면 `viewer_Histogram.Chart = null; return;`
    - min/max 산출 → BIN_COUNT 구간으로 도수 배열 `double[] freq` 계산(BuildHistogramBins 헬퍼) + bin 중심 라벨.
    - `XYChart c = new XYChart(CHART_W, CHART_H); c.setPlotArea(55, 25, CHART_W-90, CHART_H-70);`
    - `c.addBarLayer(freq, COLOR_BAR);`  x축 라벨 = bin 중심(문자열 배열) `c.xAxis().setLabels(labels);`
    - **USL/LSL 수직선:** bin 인덱스로 환산하여 `c.xAxis().addMark(nUslBin, COLOR_USL, "USL"); c.xAxis().addMark(nLslBin, COLOR_LSL, "LSL");` (값→bin 인덱스 변환 헬퍼). (netchartdir.xml 로 addMark 인자 확정 — 값 기반 mark 가 가능하면 값 그대로 사용.)
    - `viewer_Histogram.Chart = c;`

    **RenderTrend(List<double> values, double dMean, double dUsl, double dLsl) — LineLayer (D-13):**
    - values 비었으면 `viewer_Trend.Chart = null; return;`
    - `double[] data = values.ToArray();` x = 샘플 인덱스 1..N.
    - `XYChart c = new XYChart(CHART_W, CHART_H); c.setPlotArea(...); c.addLineLayer(data, COLOR_LINE);`
    - x축 라벨 = 1..N (`c.xAxis().setLabels(...)` 또는 인덱스 자동).
    - **평균/USL/LSL 수평선:** `c.yAxis().addMark(dMean, COLOR_MEAN, "평균"); c.yAxis().addMark(dUsl, COLOR_USL, "USL"); c.yAxis().addMark(dLsl, COLOR_LSL, "LSL");`
    - `viewer_Trend.Chart = c;`

    **ClearCharts():** `viewer_Histogram.Chart = null; viewer_Trend.Chart = null;`

    **BuildHistogramBins(List<double> values, int nBins, out string[] labels) → double[]:** min~max 균등 분할 도수 계산(단일책임 헬퍼). max==min 이면 단일 bin 처리(0 나눗셈 방어, if/else).

    ⚠ **ChartDirector API 검증:** in-repo 사용처가 없으므로 `netchartdir.xml` 로 `addBarLayer`/`addLineLayer`/`Axis.addMark`/`setLabels`/`WPFChartViewer.Chart` 정확한 시그니처를 확인하고 맞춘다. 빌드 오류 시 XML 문서 기준으로 인자 조정. **폴백(ChartDirector 통합 실패 시에만):** WPF Canvas/Rectangle 로 히스토그램/추이 직접 렌더 — 단 우선 ChartDirector 시도(CONTEXT 명시 의존성).

    각 렌더/헬퍼 메서드는 30줄 이내로 분리. 삼항/`??` 금지.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && grep -c "MeasurementHistoryCsvLoader.Query\|Grid_Stats_SelectionChanged\|RenderHistogram\|RenderTrend\|class StatRow" WPF_Example/UI/Statistics/StatisticsWindow.xaml.cs; grep -c "WPFChartViewer\|dp_From\|combo_Recipe\|grid_Stats" WPF_Example/UI/Statistics/StatisticsWindow.xaml</automated>
  </verify>
  <acceptance_criteria>
    - 두 파일 존재. xaml `x:Class="ReringProject.UI.StatisticsWindow"` + `xmlns:cd="clr-namespace:ChartDirector;assembly=ChartDirector.Net.Desktop.Controls"`.
    - xaml: `DatePicker x:Name="dp_From"` + `dp_To` + `ComboBox x:Name="combo_Recipe"` + `Button ... Content="조회"` + `DataGrid x:Name="grid_Stats"` + `cd:WPFChartViewer x:Name="viewer_Histogram"` + `viewer_Trend`.
    - DataGrid 컬럼에 Cpk / OK / NG / 검출실패 / 불량률(DefectRateText) 바인딩 존재.
    - xaml.cs: `MeasurementHistoryCsvLoader.Query(` 호출 + `Grid_Stats_SelectionChanged` + `RenderHistogram` + `RenderTrend` + `StatRow` 클래스.
    - CpkText 가 `double.IsPositiveInfinity` 처리, DefectRateText 가 분모 0 방어(if/else).
    - 생성자에서 `dp_From.SelectedDate = DateTime.Today` (D-10).
    - `addBarLayer`(히스토그램) + `addLineLayer`(추이) + `addMark`(USL/LSL/평균 선) 사용.
    - 삼항 `?:` / `??` 미사용. Allman. `//260707 hbk` 주석 다수.
  </acceptance_criteria>
  <done>StatisticsWindow 가 Query 소비 → DataGrid 통계 테이블 + 레시피 드롭다운 + 행클릭 히스토그램/추이(ChartDirector) 렌더. 기본 오늘. 빌드는 Task 3.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 메뉴 진입점 — EPageType.Statistics + PopupView 분기 + MenuBar "통계분석" 버튼 (D-08/D-09)</name>
  <files>WPF_Example/MainWindow.xaml.cs, WPF_Example/UI/MenuBar.xaml, WPF_Example/UI/MenuBar.xaml.cs</files>
  <read_first>
    - WPF_Example/MainWindow.xaml.cs:20-31 (EPageType enum), :68-70 (멤버 필드), :355-363 (PopupView Reviewer 케이스)
    - WPF_Example/UI/MenuBar.xaml:160-213 (Button_Reviewer 및 그 부모 그리드 ColumnDefinitions)
    - WPF_Example/UI/MenuBar.xaml.cs:104-107 (Button_Reviewer_Click)
  </read_first>
  <action>
    (1) **MainWindow.xaml.cs — EPageType enum (line 20-31):** `Reviewer,` 줄 뒤에 추가:
    ```csharp
            Statistics,   //260707 hbk STAT-01 D-09 — 양산 이력 통계분석 비모달 창
    ```

    (2) **MainWindow.xaml.cs — 멤버 필드 (line 70, mReviewerWindow 뒤):**
    ```csharp
        private UI.StatisticsWindow mStatisticsWindow;   //260707 hbk STAT-01 D-08 — 비모달 재사용 멤버
    ```

    (3) **MainWindow.xaml.cs — PopupView switch (Reviewer 케이스 line 355-363 뒤, switch 내부):** Reviewer 를 완전 미러한 Statistics 케이스 추가(content-anchor = "case EPageType.Reviewer 블록의 break; 직후"). K&R 같은 줄 중괄호(파일 관례):
    ```csharp
                case EPageType.Statistics:   //260707 hbk STAT-01 D-08/D-09 — 비모달 Show() (라이브 MainView 방해 안 함)
                    if (mStatisticsWindow != null && mStatisticsWindow.IsLoaded) {
                        mStatisticsWindow.Show();
                        return;
                    }
                    mStatisticsWindow = new UI.StatisticsWindow();
                    mStatisticsWindow.Owner = this;
                    mStatisticsWindow.Show();   // 비모달
                    break;
    ```

    (4) **MenuBar.xaml — "통계분석" 버튼 추가:** read_first 로 Button_Reviewer(Grid.Column="3" Grid.Row="1", line 206-212)가 속한 **부모 그리드**와 그 ColumnDefinitions 를 확인한다. 해당 그리드가 4컬럼(0~3)만 있으면 5번째 `<ColumnDefinition></ColumnDefinition>` 를 추가한다. Button_Reviewer XAML 블록 **직후**(content-anchor)에 아래 버튼을 추가(Grid.Column="4" Grid.Row="1", Reviewer 스타일 미러 — `disalbedStyle` StaticResource 키명 오타 그대로):
    ```xml
                    <!-- 통계분석 -->
                    <!--260707 hbk STAT-01 D-09 — 양산 이력 통계 비모달 버튼(항상 활성)-->
                    <Button x:Name="Button_Statistics" Grid.Column="4" Grid.Row="1" HorizontalContentAlignment="Center"
                VerticalContentAlignment="Center" Click="Button_Statistics_Click" Style="{StaticResource disalbedStyle}">
                        <StackPanel Orientation="Vertical" Width="Auto" Height="Auto">
                            <TextBlock Text="[S]" FontSize="22" HorizontalAlignment="Center" FontWeight="Bold" Foreground="#1E3A5F"></TextBlock>
                            <TextBlock Text="통계분석" FontSize="12"></TextBlock>
                        </StackPanel>
                    </Button>
    ```

    (5) **MenuBar.xaml.cs — 핸들러 (Button_Reviewer_Click line 104-107 뒤, K&R):**
    ```csharp
            // 통계분석 비모달 창 열기
            private void Button_Statistics_Click(object sender, RoutedEventArgs e) {   //260707 hbk STAT-01 D-09
                mParentWindow.PopupView(EPageType.Statistics);
            }
    ```

    기존 EPageType 값/Reviewer 케이스/Button_Reviewer 버튼·핸들러는 무변경(회귀 0).
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && grep -c "EPageType.Statistics" WPF_Example/MainWindow.xaml.cs; grep -c "Button_Statistics" WPF_Example/UI/MenuBar.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - MainWindow.xaml.cs: EPageType enum 에 `Statistics,` 추가(Reviewer 뒤). `private UI.StatisticsWindow mStatisticsWindow;` 멤버 존재.
    - MainWindow.xaml.cs: PopupView switch 에 `case EPageType.Statistics:` 존재 — `mStatisticsWindow.IsLoaded` 재사용 + `new UI.StatisticsWindow()` + `Owner = this` + `Show()`(비모달, ShowDialog 아님).
    - MenuBar.xaml: `Button_Statistics` 버튼 + `Click="Button_Statistics_Click"` + "통계분석" 텍스트. Button_Reviewer 부모 그리드에 Grid.Column="4" 배치 가능(컬럼 부족 시 ColumnDefinition 추가됨).
    - MenuBar.xaml.cs: `Button_Statistics_Click` → `mParentWindow.PopupView(EPageType.Statistics)`.
    - 기존 EPageType 값/Reviewer 케이스/Button_Reviewer 무변경(회귀 0).
    - 삼항 `?:` / `??` 미사용. `//260707 hbk` 주석 존재.
  </acceptance_criteria>
  <done>메뉴 "통계분석" → EPageType.Statistics → StatisticsWindow 비모달 Show/재사용. Reviewer 완전 미러. 빌드는 Task 3.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: csproj 등록(StatisticsWindow xaml/cs) + 빌드 검증(Debug/x64) + 회귀 0</name>
  <files>WPF_Example/DatumMeasurement.csproj</files>
  <read_first>
    - WPF_Example/DatumMeasurement.csproj:390-408 (ReviewerWindow.xaml.cs Compile+DependentUpon 패턴)
    - WPF_Example/DatumMeasurement.csproj:525-532 (ReviewerWindow.xaml Page 패턴)
  </read_first>
  <action>
    (1) **csproj 등록 — Compile(코드비하인드):** ReviewerWindow.xaml.cs Compile 블록(line 392-395) 인접(같은 ItemGroup)에 추가:
    ```xml
    <Compile Include="UI\Statistics\StatisticsWindow.xaml.cs">
      <DependentUpon>StatisticsWindow.xaml</DependentUpon>
    </Compile>
    ```
    (2) **csproj 등록 — Page(XAML):** ReviewerWindow.xaml Page 블록(line 527~) 인접(같은 ItemGroup)에 추가:
    ```xml
    <Page Include="UI\Statistics\StatisticsWindow.xaml">
      <SubType>Designer</SubType>
      <Generator>MSBuild:Compile</Generator>
    </Page>
    ```
    StatRow 클래스가 StatisticsWindow.xaml.cs 안에 있으면 별도 Compile 불필요(같은 파일).

    (3) **빌드:** msbuild Debug/x64 실행, error 0 + `Build succeeded`. ChartDirector 네임스페이스/WPFChartViewer 미해결·XAML 파서 오류 시 xmlns/using(`using ChartDirector;`)/netchartdir.xml 시그니처를 수정 후 재빌드.

    (4) **회귀 점검:** `git diff --name-only` → 변경 = StatisticsWindow.xaml(신규) + StatisticsWindow.xaml.cs(신규) + MainWindow.xaml.cs + MenuBar.xaml + MenuBar.xaml.cs + DatumMeasurement.csproj 6개. ReviewerWindow / 검사·Align 시퀀스 코드 무변경 확인.
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj" /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal /nologo 2>&1 | grep -iE "error|Build succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "StatisticsWindow.xaml.cs" WPF_Example/DatumMeasurement.csproj` → Compile Include + DependentUpon 존재.
    - `grep 'StatisticsWindow.xaml"' WPF_Example/DatumMeasurement.csproj` → Page Include 존재.
    - msbuild Debug/x64 `Build succeeded`, error 0건.
    - `git diff --name-only` 에 ReviewerWindow.xaml(.cs) 미포함(무수정). 검사/Align 시퀀스 파일 미포함.
  </acceptance_criteria>
  <done>msbuild Debug/x64 PASS. StatisticsWindow xaml/cs csproj 등록. ChartDirector 렌더 컴파일 통과. 리뷰어/검사 회귀 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV 파일 → UI 표시 (신뢰 경계 낮음) | StatisticsWindow 는 Plan 02 loader 를 통해 내부 CSV 를 읽어 표시만 한다. 사용자 입력 = 기간/레시피 선택(파일 경로 조작 불가 — SystemSetting 경로 + 날짜 포맷만). 외부 네트워크/인증 표면 없음. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-06 | DoS | 대용량 기간 조회 UI 프리즈 | accept | 기본값 오늘 1일(D-10). 일자별 텍스트 CSV — 파일 수=일수. 최대 일수 경고는 재량(저위험). |
| T-67-07 | Info Disclosure | 통계 창에 측정 데이터 노출 | accept | 로컬 데스크톱 앱, 기존 리뷰어와 동일 신뢰 수준. 신규 노출 채널 없음. |

신규 high/medium 위협 없음 — 로컬 신뢰 파일 표시 UI.
</threat_model>

<verification>
- msbuild Debug/x64 PASS (error 0).
- StatisticsWindow: 기간 DatePicker(기본 오늘) + 레시피 콤보 + [조회] + DataGrid(N/Mean/StdDev/Range/Cpk/OK/NG/검출실패/불량률) + WPFChartViewer 히스토그램/추이.
- 행 클릭 → 선택 항목 히스토그램(USL/LSL 수직선) + 추이(평균/USL/LSL 선) 갱신.
- 메뉴 "통계분석" → EPageType.Statistics → 비모달 Show/재사용. Reviewer 미러.
- 기존 리뷰어/검사/Align 회귀 0. 모든 변경 라인 `//260707 hbk`.
</verification>

<success_criteria>
메뉴 "통계분석" 으로 비모달 통계창이 열리고(라이브 검사 병행), 기간·레시피 조회 시 측정 항목별 통계 테이블이 표시되며, 행 클릭 시 공차선 포함 히스토그램 + 샘플 인덱스 추이 차트가 갱신된다. msbuild Debug/x64 PASS, 회귀 0.
</success_criteria>

<output>
완료 후 `.planning/phases/67-stat-01-2026-07-07/67-03-SUMMARY.md` 생성.
</output>
