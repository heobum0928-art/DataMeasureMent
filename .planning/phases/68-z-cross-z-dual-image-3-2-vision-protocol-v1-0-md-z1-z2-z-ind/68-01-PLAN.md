---
phase: 68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/SkipReason.cs
autonomous: true
requirements: [PROTO-Z-CROSS]
must_haves:
  truths:
    - "DualImageEdgeDistanceMeasurement에 ZIndexA/ZIndexB int 필드가 존재하고 미설정 기본값 -1로 INI 하위호환 로드된다"
    - "DatumConfig(VerticalTwoHorizontalDualImage)에 ZIndexA/ZIndexB 필드가 존재하고 미설정 기본값 -1이며 다른 algorithm에서는 PropertyGrid에서 숨겨진다"
    - "구 레시피(ZIndexA/B 키 미존재)를 로드해도 필드가 0이 아닌 -1(미설정 sentinel)로 복원된다"
    - "SkipReason에 ZINDEX_MISCONFIGURED 상수가 추가되어 크로스-Z 오설정 NG의 단일 소스가 된다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs"
      provides: "ZIndexA/ZIndexB 프로퍼티 + Load override(sentinel -1 복원)"
      contains: "ZIndexA"
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "ZIndexA/ZIndexB 프로퍼티 + Load override(sentinel -1) + ICustomTypeDescriptor hide 동기"
      contains: "ZIndexA"
    - path: "WPF_Example/Custom/Sequence/Inspection/SkipReason.cs"
      provides: "ZINDEX_MISCONFIGURED 상수"
      contains: "ZINDEX_MISCONFIGURED"
  key_links:
    - from: "ParamBase.Load (Int32 리플렉션, 키 부재→0)"
      to: "DualImageEdgeDistanceMeasurement.Load / DatumConfig.Load override"
      via: "sentinel -1 복원으로 하위호환 보장"
      pattern: "ZIndexA"
---

<objective>
D-04/D-07(하위호환) + D-05의 상수 계층을 구축한다: `DualImageEdgeDistanceMeasurement`와 `DatumConfig`(VerticalTwoHorizontalDualImage)에 `ZIndexA`/`ZIndexB` int 필드를 추가하고, 미설정 sentinel(-1)이 구 레시피 로드 시 `ParamBase.Load`의 Int32 리플렉션(키 부재 → 0 덮어쓰기)에 삼켜지지 않도록 `Load` override로 복원한다. 크로스-Z 오설정 명시적 NG의 단일 소스 상수 `ZINDEX_MISCONFIGURED`도 추가한다.

이 세 필드/상수는 Wave 2의 실행 스코프 필터(Plan 02 — measurement/datum-ZIndexA/B-aware 필터)와 Wave 3/4의 크로스-Z 캡처(Plan 03/04)가 소비하는 기반이다. **왜 필드가 먼저인가:** 사용자 확정 설계(CONTEXT specifics)는 "별도 Shot 두 개로 쪼개는 설계가 아니라 하나의 측정/Datum이 두 z_index를 참조" — 즉 크로스-Z 측정의 owning Shot이 ZIndexA/ZIndexB 두 위치 모두에서 실행되어야 하므로, 실행 필터(Plan 02)가 이 ZIndexA/B 필드를 읽어야 한다. 따라서 필드가 실행 필터의 하드 의존.

Purpose: 크로스-Z의 데이터 모델 + 하위호환 계약 + NG 사유 상수.
Output: ZIndexA/B 필드(측정+Datum), sentinel -1 하위호환 로드, ZINDEX_MISCONFIGURED 상수.

**REQ-ID 미확정 (Open):** REQUIREMENTS.md에 이 capability의 REQ-ID 없음. 작업 라벨 `PROTO-Z-CROSS` 사용, 정식 REQ-ID(예: PROTO-07) 신설 여부는 사용자 결정 대기 (planner가 REQUIREMENTS.md 임의 backfill 안 함).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-CONTEXT.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-RESEARCH.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-PATTERNS.md

<coding_guideline_scope>
D-09: 이 플랜의 `DualImageEdgeDistanceMeasurement.cs`/`DatumConfig.cs`는 **순수 측정/데이터 클래스로 D-09 지침 대상 아님** — 기존 CLAUDE.md 파일 스타일(카멜케이스, 기존 프로퍼티 컨벤션) 유지. `SkipReason.cs`(상수 클래스)도 대상 아님.
</coding_guideline_scope>

<interfaces>
확인된 현재 코드 앵커 (2026-07-22):
- `DualImageEdgeDistanceMeasurement.cs`: `class DualImageEdgeDistanceMeasurement : MeasurementBase, IDatumOriginConsumer` (line 18). 기존 `TeachingImagePath_Vertical` (line 29, `[Category("Image|DualImage")]`), `TeachingImagePath_Horizontal` (line 37). **현재 Load override 없음** (TryExecute만 override). RuntimeImageA/B (line 124/128).
- `MeasurementBase.Load` override 선례(MeasurementBase.cs:143-155): `base.Load()` 호출 후 `!sec.ContainsKey("MeasCorrectionFactor")` 이면 1.0 복원.
- ParamBase.Load Int32 케이스(ParamBase.cs:377-380): 키 부재 시 IniFile 인덱서 기본값 0으로 덮어씀 → C# 초기화자 -1을 무력화.
- `DatumConfig.cs`: `[Category("Datum|ImageSource")]` 블록(line 29-38/128-129), `TeachingImagePath_Vertical` (line 129). **Load override 없음** — sentinel 정규화는 `EnsurePerRoiDefaults()` (line 824-869, find-time 호출). `IsHiddenForAlgorithm(name, alg)` (line 982-1014): TwoLineIntersect(986)/CircleTwoHorizontal(993)/VerticalTwoHorizontal(1001) case가 `if (name == "TeachingImagePath_Vertical") return true;`로 DualImage 전용 필드 hide, VerticalTwoHorizontalDualImage(1007)는 노출.
- `SkipReason.cs` (13줄): DATUM_FAIL/ALIGN_FAIL/NO_IMAGE/DATUM_REF_MISSING const string. 헤더 주석: "값은 와이어/CSV/로그에 그대로 나가므로 절대 변경 금지."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DualImageEdgeDistanceMeasurement ZIndexA/ZIndexB 필드 + Load override (D-04/D-07)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs (전체 — 배치 line 22-37)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (Load override 선례 line 143-155)
    - WPF_Example/Sequence/Param/ParamBase.cs (Int32 Load 리플렉션 line 377-380)
    - .planning/phases/68-.../68-PATTERNS.md (DualImageEdgeDistanceMeasurement 섹션 Analog 1/2; RESEARCH Code Example 2)
  </read_first>
  <action>
    기존 `TeachingImagePath_Horizontal`(line 37) 바로 아래 같은 `[Category("Image|DualImage")]`에 두 int 프로퍼티 추가: `ZIndexA` (기본값 -1, DisplayName "Point z_index (ZIndexA)", Description "PointROI(에지 A) 라이브 캡처 z_index. -1=미설정(기존 정적 이미지 경로 사용)"), `ZIndexB` (기본값 -1, DisplayName "Line z_index (ZIndexB)", LineROI/에지 B). 그 다음 이 클래스에 신규 `public override bool Load(IniFile loadFile, string groupName)` 추가: `bool result = base.Load(loadFile, groupName);` 먼저 호출(→ MeasurementBase.Load가 MeasCorrectionFactor 복원까지 위임 처리, 중복 금지) 후 `loadFile.TryGetSection(groupName, out sec)`으로 섹션 확보하고 `!sec.ContainsKey("ZIndexA")` 이면 `ZIndexA = -1`, `!sec.ContainsKey("ZIndexB")` 이면 `ZIndexB = -1` 복원(MeasurementBase.Load ContainsKey 가드 shape 미러, 2필드). MeasCorrectionFactor 복원 로직 재작성 금지(base.Load 위임). 파일 스타일은 기존 클래스 컨벤션 유지(D-09 미적용). -1 = 미설정 → 기존 static TeachingImagePath 경로 폴백(D-07).
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 빌드 PASS (0 errors, 0 new warnings)
    - `public int ZIndexA { get; set; } = -1;` 및 `ZIndexB` 존재, Category("Image|DualImage")
    - `public override bool Load(...)` 존재, base.Load() 호출 후 ZIndexA/ZIndexB 각각 ContainsKey 가드로 -1 복원
    - MeasCorrectionFactor 복원 코드가 이 override에 중복 작성되지 않음
  </acceptance_criteria>
  <done>ZIndexA/B 필드 + 구 레시피 로드 시 -1 복원, 빌드 PASS</done>
</task>

<task type="auto">
  <name>Task 2: DatumConfig ZIndexA/ZIndexB 필드 + Load override + ICustomTypeDescriptor hide 동기 (D-04/D-06/D-07)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (Category ImageSource line 29-38/128-129, EnsurePerRoiDefaults line 824-869, IsHiddenForAlgorithm line 982-1014)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (Load override 스타일 line 143-155)
    - .planning/phases/68-.../68-PATTERNS.md (DatumConfig 섹션 Analog 1/2/3 — Load override 없음, EnsurePerRoiDefaults가 기존 훅이나 find-time라 0/미설정 구분 불가 → 신규 Load override 채택 확정)
  </read_first>
  <action>
    `TeachingImagePath_Vertical`(line 129) 근처 `[Category("Datum|ImageSource")]` 블록에 `ZIndexA`/`ZIndexB` int 프로퍼티(기본 -1) 추가(측정 클래스와 동일 의미 — PointROI/LineROI 라이브 캡처 z_index). DatumConfig는 현재 Load override가 없고 sentinel 정규화가 find-time `EnsurePerRoiDefaults`라 로드 시점에 0/미설정을 구분 못 하므로, load-time 확정을 위해 **신규 `public override bool Load(IniFile loadFile, string groupName)`**를 추가한다(측정 클래스와 동일 계약): `base.Load()` 호출(DatumConfig는 ParamBase 직접 상속 → ParamBase.Load로 resolve) 후 `!sec.ContainsKey("ZIndexA")`/`("ZIndexB")` 가드로 -1 복원. 또 `IsHiddenForAlgorithm`에서 TeachingImagePath_Vertical을 hide하는 3개 non-DualImage case(TwoLineIntersect 986, CircleTwoHorizontal 993, VerticalTwoHorizontal 1001)에 `if (name == "ZIndexA" || name == "ZIndexB") return true;`를 추가해 ZIndexA/B가 TeachingImagePath_Vertical과 정확히 동일하게 VerticalTwoHorizontalDualImage에서만 노출되도록 동기화. 파일 스타일은 기존 DatumConfig 컨벤션 유지(D-09 미적용). 유효성 검증(ZIndexA==ZIndexB 등)은 여기 아님 — Plan 03/04 실행 시점(D-05).
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 빌드 PASS (0 errors, 0 new warnings)
    - DatumConfig에 ZIndexA/ZIndexB int 프로퍼티(= -1) Category("Datum|ImageSource") 존재
    - DatumConfig에 신규 `public override bool Load`, base.Load 후 ZIndexA/ZIndexB ContainsKey 가드로 -1 복원
    - IsHiddenForAlgorithm의 TwoLineIntersect/CircleTwoHorizontal/VerticalTwoHorizontal 3 case에 ZIndexA/ZIndexB hide 라인 추가 (VerticalTwoHorizontalDualImage 노출)
  </acceptance_criteria>
  <done>Datum 레벨 ZIndexA/B 필드 + 하위호환 로드 + PropertyGrid hide 동기화, 빌드 PASS</done>
</task>

<task type="auto">
  <name>Task 3: SkipReason.ZINDEX_MISCONFIGURED 상수 (D-05)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/SkipReason.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/SkipReason.cs (전체 13줄)
    - .planning/phases/68-.../68-PATTERNS.md (SkipReason 섹션)
  </read_first>
  <action>
    `SkipReason` static 클래스에 `public const string ZINDEX_MISCONFIGURED = "ZINDEX_MISCONFIGURED";`를 기존 `DATUM_REF_MISSING` 다음에 추가. 값 문자열은 와이어/CSV/로그에 그대로 나가므로(헤더 주석) ship 후 개명 금지 — 상수명=값 일치. Plan 03(측정)/Plan 04(Datum)에서 ZIndexA==ZIndexB 또는 존재하지 않는 z_index 참조 시 명시적 NG 사유로 소비.
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 빌드 PASS
    - SkipReason.cs에 `public const string ZINDEX_MISCONFIGURED = "ZINDEX_MISCONFIGURED";` 존재
    - 상수명과 문자열 값 일치
  </acceptance_criteria>
  <done>크로스-Z 오설정 NG 사유 단일 소스 상수 존재, 빌드 PASS</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레시피 INI(디스크) → ParamBase.Load → ZIndexA/ZIndexB | 레시피 int 값은 반신뢰 입력 — 키 부재/구 레시피 시 0 덮어쓰기가 유효 z_index 0과 혼동될 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-03 | Denial of Service / 잘못된 상태 | ZIndexA/ZIndexB INI 로드 (키 부재 → ParamBase 0 덮어쓰기) | mitigate | Load override(측정+Datum)에서 ContainsKey 가드로 -1(미설정) 복원 — 구 레시피가 "z=0 명시"로 오인되어 실행 스코프/캡처 오작동 방지. 실제 범위 검증은 Plan 03/04 실행 시점(D-05, V5 입력검증). |

**Supply chain:** 신규 패키지 설치 없음 (RESEARCH Package Legitimacy Audit: 해당 없음) — T-68-SC N/A.
</threat_model>

<verification>
- msbuild Debug/x64 빌드 PASS (매 task)
- 하위호환 회귀(D-07)는 Plan 05 SIMUL UAT에서 SHOT_E5(D:\Data\Recipe\FAI_1\main.ini) 로드로 검증
</verification>

<success_criteria>
- ZIndexA/ZIndexB 필드가 측정+Datum 양쪽에 존재, 미설정 기본 -1
- 구 레시피(키 부재) 로드 시 0이 아닌 -1로 복원
- DatumConfig ZIndexA/B가 VerticalTwoHorizontalDualImage에서만 PropertyGrid 노출
- ZINDEX_MISCONFIGURED 상수 존재
</success_criteria>

<output>
Create `.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-01-SUMMARY.md` when done
</output>
