---
phase: 68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind
plan: 02
type: execute
wave: 2
depends_on: [68-01]
files_modified:
  - WPF_Example/Custom/Sequence/SequenceHandler.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Custom/SystemHandler.cs
autonomous: true
requirements: [PROTO-Z-CROSS]
must_haves:
  truths:
    - "v1.0(UseProtocolV1) 경로에서 $TEST(z=N, N>=1) 도착 시 그 z_index에 매핑된 Shot(Action)만 실행된다"
    - "크로스-Z 측정/Datum의 owning Shot은 자기 ShotConfig.ZIndex뿐 아니라 owned ZIndexA/ZIndexB 중 하나가 N과 같으면 z=N 실행에 포함된다(동일 Shot이 두 위치 모두에서 실행 — 사용자 확정 설계)"
    - "$TEST(z=0, Datum) 도착 시 기존처럼 전체 Shot(StartAll)이 실행되어 Datum 검출이 정상 동작한다 (회귀 0)"
    - "같은 z_index를 가진 Shot들이 Actions[] 배열에서 항상 연속 블록으로 배치되어 StartSubset의 min-max 연속구간 실행이 안전하다"
    - "v2.6/legacy(비 IsDynamicFAIMode) 경로는 byte-identical 무수정"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/SequenceHandler.cs"
      provides: "RebuildInspectionActions가 시퀀스 소유 Shot을 ZIndex 기준 안정 정렬 후 Actions[] 구성"
      contains: "ZIndex"
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "z_index → Actions[] 인덱스 다중매칭 헬퍼 (ShotConfig.ZIndex + owned ZIndexA/ZIndexB aware)"
      contains: "FindActionIndicesByZIndex"
    - path: "WPF_Example/Custom/SystemHandler.cs"
      provides: "ProcessTest에서 z=0→StartAll / z>=1→StartSubset 분기 배선"
      contains: "StartSubset"
  key_links:
    - from: "WPF_Example/Custom/SystemHandler.cs (ProcessTest)"
      to: "InspectionSequence.FindActionIndicesByZIndex + SequenceBase.StartSubset"
      via: "z_index 기반 실행 스코프 결정"
      pattern: "StartSubset"
---

<objective>
D-01/D-01a/D-01b를 구현한다: v1.0 경로에서 `$TEST(z=N)` 수신 시 실행(grab) 레벨의 z_index 스코프 필터링을 닫는다. Phase 49는 응답 집계 레벨에서만 필터를 구현했고 `ProcessTest`는 여전히 무조건 `StartAll`을 호출한다.

**사용자 확정 설계(CONTEXT specifics — "별도 Shot 두 개로 쪼개는 설계가 아니라 하나의 측정/Datum이 두 z_index를 참조"):** 크로스-Z 측정/Datum의 owning Shot은 두 z_index 위치 모두에서 실행되어야 하므로, 실행 필터는 `ShotConfig.ZIndex`뿐 아니라 그 Shot이 소유한 DualImage 측정/DatumConfig의 `ZIndexA`/`ZIndexB`(Plan 01 필드)도 매칭해야 한다. 그래서 이 플랜은 Plan 01(필드)에 의존한다.

세 변경: (1) `RebuildInspectionActions`가 같은 z_index Shot을 `Actions[]`에서 연속 배치하도록 ZIndex 안정 정렬(D-01b — StartSubset min-max 연속구간 제약), (2) `FindActionIndicesByZIndex` 다중매칭 헬퍼(ShotConfig.ZIndex OR owned ZIndexA/ZIndexB == N), (3) `ProcessTest`에서 z=0→StartAll(D-01a), z>=1→StartSubset 배선.

Purpose: 크로스-Z 측정 실행 기반 + 조명/HW 낭비 제거.
Output: 실행 스코프 필터링된 v1.0 $TEST 경로, v2.6/legacy 무수정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-CONTEXT.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-RESEARCH.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-PATTERNS.md
@.planning/refs/control-sequence-coding-guideline.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-01-SUMMARY.md

<coding_guideline_scope>
D-09 (LOCKED): 이 플랜이 만지는 `InspectionSequence.cs` / `Custom/SystemHandler.cs` / `SequenceHandler.cs`의 신규/수정 코드는 `.planning/refs/control-sequence-coding-guideline.md` 준수 — 헝가리언 표기법(nZIndex, bMatch, szName), `if / else if / else`만(삼항·null병합 금지), 조건식은 bool 변수로 추출, 매직넘버 const, 함수 30줄 초과 시 분리. 기존 무수정 라인(예: RebuildInspectionActions의 `?? SEQ_TOP` 삼항)은 그대로 두고 새 코드만 준수.
</coding_guideline_scope>

<interfaces>
확인된 현재 코드 앵커 (2026-07-22):
- `Custom/SystemHandler.cs`: `private volatile int _lastPrepZIndex` (line 18). ProcessTest(line ~211-217): `packet.TestID = _lastPrepZIndex.ToString();` 후 `if (Sequences.IsDynamicFAIMode) { string seqName = packet.Identifier; SequenceBase seq = Sequences[seqName]; if (seq == null) return false; return seq.StartAll(packet); }`. line 213-217 = D-01 갭. IsRecipeReady/OfflineInspectMode 가드 상단.
- `Custom/Sequence/SequenceHandler.cs` `RebuildInspectionActions(ESequence)` (line 99-130): `RecipeManager.Shots` 순회 → `shotOwner = ... ?? SEQ_TOP; if (shotOwner != targetSeqName) continue;` → `EAction actionId = (EAction)((int)EAction.FAI_Base + actionIdx)` → `new Action_FAIMeasurement(actionId, actionName, shot)`. ZIndex 정렬 없음(append).
- `InspectionSequence.cs` `FindShotByZIndex(int nZIndex)` (line 303-327): 단일매칭, `shot.OwnerSequenceName == Name && shot.ZIndex == nZIndex`. `ParseCurrentZIndex()` (line 496-518): int.TryParse+음수정규화 방어 가드(재사용 패턴). `DATUM_Z_INDEX` 상수(=0) 존재(HandleDatumIndexResponse에서 사용).
- `SequenceBase.StartSubset(int[], TestPacket)`/`StartCore(first,last,packet)`: actionIndices 정렬 후 first/last만 취해 그 사이 연속 구간 전체 실행(스파스 아님). 빈 배열 → false. `Actions`(protected) InspectionSequence 접근 가능(HandleRunStartResetResults 순회 확인).
- `Action_FAIMeasurement.ShotParam` (ShotConfig) 노출 확인됨. ShotConfig.FAIList → 각 FAI.Measurements(MeasurementBase[]). DualImageEdgeDistanceMeasurement.ZIndexA/ZIndexB (Plan 01 신규). DatumConfig는 InspectionSequence.DatumConfigs 리스트로 시퀀스 소유(Shot과 별개) — Datum ZIndexA/B 매칭은 시퀀스 레벨.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RebuildInspectionActions ZIndex 안정 정렬 (D-01b) + EAction 비영속 빌드 검증</name>
  <files>WPF_Example/Custom/Sequence/SequenceHandler.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/SequenceHandler.cs (전체 — RebuildInspectionActions line 99-130)
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs (ZIndex 프로퍼티 line 17-58)
    - .planning/phases/68-.../68-PATTERNS.md (SequenceHandler 섹션)
    - .planning/phases/68-.../68-RESEARCH.md (Pitfall 2 + Assumption A1)
  </read_first>
  <action>
    RebuildInspectionActions에서 "먼저 이 시퀀스 소유(shotOwner == targetSeqName) Shot만 필터링한 리스트를 만들고, 그 필터된 리스트를 `ShotConfig.ZIndex` 오름차순 **안정 정렬**(동일 ZIndex는 기존 append 순서 유지)한 뒤 그 순서로 `EAction.FAI_Base + actionIdx` id 부여 + `Action_FAIMeasurement` 생성"으로 변경. 목표: 같은 ZIndex Shot들이 `Actions[]`에서 항상 연속 블록. 안정성은 `List.Sort` comparer에 원래 인덱스를 tie-breaker로 쓰거나 `OrderBy(zindex).ThenBy(원래인덱스)`로 보장. 기존 `?? SEQ_TOP` 삼항 라인은 무수정 유지. EAction id가 다른 곳에서 고정 인덱스/영속 참조되지 않는지(Assumption A1) 빌드 + `EAction.FAI_Base` 소비처 grep으로 검증. 주의: 크로스-Z Shot의 own ZIndex는 무관하게 정렬되지만, Task 2 필터가 ZIndexA/B로 추가 매칭하므로 정렬은 own ZIndex 기준만으로 충분(연속성은 own-ZIndex 그룹에 대해 보장, ZIndexA/B 스파스 매칭은 Task 2/Plan 03에서 다룸 — 이 정렬은 own-ZIndex 그룹 연속성 확보가 목적).
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 빌드 PASS (0 errors, 0 new warnings)
    - RebuildInspectionActions 소스에 ShotConfig.ZIndex 기준 안정 정렬 로직 존재
    - `EAction.FAI_Base` 소비처가 이 루프 내 생성 지점 외 고정 인덱스로 재참조하지 않음을 grep 확인 (레시피 영속화 없음)
    - OwnerSequenceName 필터 로직 보존 (전역 Shots 재정렬 아님, 시퀀스별 필터 후 정렬)
  </acceptance_criteria>
  <done>같은 own-z_index Shot이 Actions[]에서 연속 배치, 빌드 PASS, EAction 비영속 확인</done>
</task>

<task type="auto">
  <name>Task 2: FindActionIndicesByZIndex 다중매칭 헬퍼 — ShotConfig.ZIndex + owned ZIndexA/ZIndexB aware (D-01) + Actions[] 접근 빌드 검증</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (FindShotByZIndex line 303-327, ParseCurrentZIndex line 496-518)
    - WPF_Example/Sequence/Sequence/SequenceBase.cs (Actions 접근제한자 + StartSubset/StartCore line 374-386)
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (ShotParam / ShotConfig.FAIList 노출 확인)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs (Plan 01 신규 ZIndexA/ZIndexB)
    - .planning/phases/68-.../68-PATTERNS.md (InspectionSequence Analog 3 — Code Example 1)
  </read_first>
  <action>
    `FindShotByZIndex`(line 303) 아래에 신규 private 헬퍼 `FindActionIndicesByZIndex(int nZIndex)` 추가. 반환 `List<int>` (매칭 `Actions[]` 인덱스, 다중매칭). 매칭 조건(bool 변수로 추출, 헝가리언): `Actions[i] as Action_FAIMeasurement`가 이 시퀀스 소유(`faiAct.ShotParam.OwnerSequenceName == Name`)이고 다음 중 하나라도 참이면 매칭 — (a) `faiAct.ShotParam.ZIndex == nZIndex` (own-ZIndex, 기존 스코프), 또는 (b) 그 Shot이 소유한 DualImageEdgeDistanceMeasurement 중 하나라도 `ZIndexA == nZIndex || ZIndexB == nZIndex` (크로스-Z owning Shot이 두 위치 모두에서 실행되도록 — 사용자 확정 설계). (b) 판정은 `ShotParam.FAIList`의 각 FAI.Measurements를 순회해 `meas as DualImageEdgeDistanceMeasurement`에서 ZIndexA/B == nZIndex 확인하는 private sub-헬퍼(`bShotOwnsZIndexA/B`)로 분리(함수 30줄 이하, D-09). -1(미설정) sentinel은 어떤 실제 z_index와도 매칭되지 않음(nZIndex는 실행 시 >=1이므로 -1 == N 은 false, 자연 배제). guard-clause + if 스타일 + 삼항/null병합 금지. `Actions`(protected)/`ShotParam`/`FAIList` 접근 가능 여부를 빌드로 즉시 검증(Open Question #3). 정의만, 소비는 Task 3.
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 빌드 PASS (0 errors, 0 new warnings) — Actions[]/ShotParam/FAIList/ZIndexA 접근 입증
    - `private List<int> FindActionIndicesByZIndex(int nZIndex)` 존재, 반환 List<int>
    - 매칭 = OwnerSequenceName==Name AND (ShotConfig.ZIndex==N OR owned DualImage 측정 ZIndexA/ZIndexB==N)
    - 크로스-Z 매칭 판정이 별도 sub-헬퍼로 분리(30줄 이하), 삼항/null병합 미사용 (D-09)
  </acceptance_criteria>
  <done>크로스-Z owning Shot이 own-ZIndex OR ZIndexA/B 매칭으로 z=N 실행에 포함되는 헬퍼 컴파일 PASS</done>
</task>

<task type="auto">
  <name>Task 3: ProcessTest z=0→StartAll / z>=1→StartSubset 배선 (D-01/D-01a)</name>
  <files>WPF_Example/Custom/SystemHandler.cs</files>
  <read_first>
    - WPF_Example/Custom/SystemHandler.cs (ProcessTest line 195-220, _lastPrepZIndex line 18, ProcessPrep line 700-740 guard-clause 스타일)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (Task 2 신규 FindActionIndicesByZIndex, DATUM_Z_INDEX 상수)
    - .planning/phases/68-.../68-PATTERNS.md (SystemHandler ProcessTest 섹션 — cast 스타일, StartAll 라인)
  </read_first>
  <action>
    ProcessTest의 `if (Sequences.IsDynamicFAIMode) { ... return seq.StartAll(packet); }`(line 213-217)를 z_index 분기로 교체. `_lastPrepZIndex`가 이 $TEST의 z_index 단일 소스(packet.TestID 재파싱 금지 — 방금 대입됨). 분기(bool 변수 추출, 헝가리언): z_index == 0(Datum) → 기존 그대로 `seq.StartAll(packet)`(D-01a — z=0에 매칭 Shot 없으면 필터 시 0개 실행 → Datum 검출 정지 회귀 방지); z_index >= 1 → `seq as InspectionSequence` 캐스트(dynamic-FAI 런타임 타입은 항상 InspectionSequence)해 `FindActionIndicesByZIndex(nZIndex)` 호출 → 결과가 비어있지 않으면 `seq.StartSubset(indices, packet)`, 비어있으면(그 z_index 매칭 Shot 0개) 로그 남기고 폴백(기존 StartAll — 조용한 무시 금지, "명시적 처리" 정신). z=0 판별 매직넘버는 named const(기존 DATUM_Z_INDEX 재사용, SystemHandler에서 접근 불가면 로컬 const). 삼항/null병합 금지(D-09). IsDynamicFAIMode==false인 v2.6/legacy 경로(`Sequences.Start(packet)`) + IsRecipeReady/OfflineInspectMode 가드는 byte-identical 유지. StartSubset이 Task 2의 다중매칭 + Task 1의 연속정렬 덕에 안전하게 min-max 구간 실행(크로스-Z Shot이 스파스 위치여도 최소 own-ZIndex 그룹은 연속, ZIndexA/B 추가 매칭 Shot이 구간 밖이면 min-max 확장으로 포함됨 — 이 확장 시 사이 무관 Shot 재실행 가능성은 Plan 05 UAT에서 grab 로그로 관측/기록).
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 빌드 PASS (0 errors, 0 new warnings)
    - ProcessTest에 z_index==0 → StartAll, z_index>=1 → StartSubset 분기 존재
    - z=0 판별에 매직넘버 0 대신 named const 사용
    - v2.6/legacy(`Sequences.Start`)와 IsRecipeReady/OfflineInspectMode 가드 라인 무수정
    - 빈 매칭 리스트 시 로그 + 폴백 (조용한 무시 아님), 삼항/null병합 미사용
  </acceptance_criteria>
  <done>v1.0 $TEST(z>=1)는 매핑(크로스-Z 포함) Shot만, $TEST(z=0)는 전체 실행, 빌드 PASS</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLC/핸들러(PC1/PC2) → TCP VisionServer → ProcessTest | z_index(TestID) 값은 프로토콜 입력 — 범위/부호 미검증 시 배열 인덱스 예외 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Denial of Service | ProcessTest z_index → StartSubset(indices) | mitigate | `_lastPrepZIndex`는 ProcessPrep 기존 int 저장(정수 보장). FindActionIndicesByZIndex는 매칭 없으면 빈 리스트 → StartSubset 빈 배열에 즉시 false(예외 없음), 로그+StartAll 폴백. 음수/범위밖 z_index는 매칭 0개 → 안전. TCP 스레드 크래시 방지(RunBottomAlign T-65-06 원칙). |
| T-68-02 | Tampering | RebuildInspectionActions ZIndex 재정렬 → Actions[] 인덱스 이동 | mitigate | EAction id가 레시피 영속화/고정인덱스 참조되지 않음을 Task 1 빌드+grep 검증 후에만 재정렬 (Assumption A1). |

**Supply chain:** 신규 패키지 설치 없음 — T-68-SC N/A.
</threat_model>

<verification>
- msbuild Debug/x64 빌드 PASS (매 task)
- SIMUL UAT(Plan 05): z=1/z=2 각각 `$TEST` 시 그 z_index Shot(크로스-Z owning Shot 포함)만 grab 로그, z=0은 전체 Shot + Datum 검출 정상
</verification>

<success_criteria>
- v1.0 $TEST(z>=1)가 매핑 Shot(own-ZIndex + 크로스-Z ZIndexA/B owning Shot)만 실행
- $TEST(z=0)는 StartAll 유지 → Datum 검출 회귀 0
- 같은 own-z_index Shot이 Actions[]에서 연속 배치 → StartSubset 안전
- v2.6/legacy 경로 무수정
</success_criteria>

<output>
Create `.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-02-SUMMARY.md` when done
</output>
