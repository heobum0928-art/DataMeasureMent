---
phase: 68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind
plan: 03
type: execute
wave: 3
depends_on: [68-01, 68-02]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: true
requirements: [PROTO-Z-CROSS]
must_haves:
  truths:
    - "InspectionSequence에 크로스-Z 이미지 저장소(HImage 보관, 측정식별자+z_index 키)가 존재하고 z_index=0 수신 시 저장 이미지가 Dispose되며 리셋된다"
    - "ZIndexA/ZIndexB가 설정된 DualImage 측정은 첫 z_index 처리 시 이미지만 캡처(항목 미보고, NG 아님)하고, 완성 z_index 처리 시 두 이미지를 RuntimeImageA/B에 주입해 기존 TryExecute를 한 번 호출해 거리 계산+보고한다"
    - "D-08 버그 수정: TryGrabOrLoadFaiDualImages의 imageA가 ShotParam.HasImage(라이브 grab) 이미지를 최우선 사용한다"
    - "ZIndexA==ZIndexB 또는 존재하지 않는 z_index 참조 시 측정은 명시적 NG(ZINDEX_MISCONFIGURED, 로그) — 조용한 폴백 없음"
    - "ZIndexA/B 미설정(-1) 기존 레시피는 기존 static TeachingImagePath 경로로 100% 동작(회귀 0)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "크로스-Z HImage 저장소 멤버 + Store/Take/Clear(Dispose) 헬퍼 + ResetCycleState 연동"
      contains: "Dispose"
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "D-08 라이브 우선 fix + ZIndexA/B 크로스-Z 캡처/주입 + Z1 pending + D-05 게이트"
      contains: "ZINDEX_MISCONFIGURED"
  key_links:
    - from: "Action_FAIMeasurement (EStep.DatumPhase grab / EStep.Measure)"
      to: "InspectionSequence 크로스-Z 저장소"
      via: "Z1 캡처 저장 → Z2 주입"
      pattern: "RuntimeImageA"
    - from: "Action_FAIMeasurement.TryGrabOrLoadFaiDualImages"
      to: "ShotParam.HasImage / GetImage"
      via: "D-08 라이브 이미지 최우선"
      pattern: "HasImage"
---

<objective>
D-02/D-02a/D-03(크로스-Z 저장) + D-05(측정 게이트) + D-08(버그수정)의 핵심을 구현한다. `InspectionSequence`에 크로스-Z 이미지 저장소(계산값이 아니라 HImage 보관 — D-02a)를 `_datumTransforms`/`_failedDatums`와 동일 lifecycle 멤버로 추가하고, `ResetCycleState()`(z=0 수신) 훅에서 저장 이미지 Dispose+Clear(D-03). `DualImageEdgeDistanceMeasurement`의 크로스-Z 실행: 첫 z_index 처리 시 라이브 이미지만 캡처해 저장소에 보관(항목 미보고=B), 완성 z_index 처리 시 저장분+방금 캡처분을 `RuntimeImageA`/`RuntimeImageB`에 주입해 **기존 TryExecute를 변경 없이 한 번 호출**(D-02a — 알고리즘 무변경). 겸사겸사 D-08(imageA가 라이브 grab 무시하고 파일 재로드) 수정 + D-05 측정 게이트(ZIndexA==ZIndexB/존재하지 않는 index → ZINDEX_MISCONFIGURED NG).

Purpose: 요구 3-2 "Z1 정보 보유 → Z2에서 측정"의 실동작.
Output: 크로스-Z 저장소 + FAI DualImage 크로스-Z 캡처/계산 + D-08 fix + 오설정 NG.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-CONTEXT.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-RESEARCH.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-PATTERNS.md
@.planning/refs/control-sequence-coding-guideline.md
@.planning/debug/shared-lighthandler-race.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-01-SUMMARY.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-02-SUMMARY.md

<coding_guideline_scope>
D-09 (LOCKED): `InspectionSequence.cs`/`Action_FAIMeasurement.cs`의 신규/수정 코드는 `.planning/refs/control-sequence-coding-guideline.md` 준수 — 헝가리언, `if/else if/else`만(삼항·null병합 금지), 조건식 bool 변수화, 매직넘버 const, 함수 30줄 이하 분리. (DualImageEdgeDistanceMeasurement.cs 알고리즘은 무변경이라 미해당.)
</coding_guideline_scope>

<lock_order_constraint>
**(2026-07-21 shared-lighthandler-race)** 신규 grab 호출(크로스-Z 라이브 캡처)은 반드시 기존 `Action_FAIMeasurement.EStep.DatumPhase`의 `lock (LightHandler.Handle.GrabSyncLock)` 블록(line 94-229) **안**에 둔다. `GrabSyncLock`(항상 바깥) → `cam.GrabLock`(항상 안쪽). 새 lock scope 추가 금지 — 동일 레이스 재발 방지.
</lock_order_constraint>

<interfaces>
확인된 현재 코드 앵커 (2026-07-22):
- `InspectionSequence.cs`: `_datumTransforms`(line 55, `Dictionary<string,HTuple>`), `_failedDatums`(line 58) = 크로스-Z 저장소 lifecycle 모델. `ResetCycleState()`(line 486-492): m_bCycleHasNG/m_bCycleDatumFailed/m_nCurrentZIndex/m_nLastZIndex 리셋 — HandleDatumIndexResponse(line ~595)가 z=0 시 호출(D-03 편승 지점). `ClearDatumTransforms()`(line 845-847): _datumTransforms.Clear()+_failedDatums.Clear() — **단, EStep.DatumPhase마다(매 Action) 호출될 수 있어 크로스-Z용 리셋 site로는 부적합 → 크로스-Z Clear는 ResetCycleState()에** 둘 것. AggregateIndexFais/BuildScopedResponse(z_index별 FAI 집계) = Z1 pending 항목 미보고 처리 지점.
- `ShotConfig.cs`(참고, 무수정): `_image`/`_imageLock`/`SetImage`(CopyImage 저장+기존 Dispose)/`GetImage`(CopyImage 반환)/`HasImage`(line 236-374) = 소유클론 lock 계약 모델.
- `Action_FAIMeasurement.cs`: EStep.DatumPhase grab 블록(line 81-233, `lock(GrabSyncLock)` line 94-229, Shot 라이브 grab line ~207 `ShotParam.SetImage(image)`). EStep.Measure 루프(line 264-313): `foreach fai in ShotParam.FAIList → foreach meas in fai.Measurements`. 게이트: IsDatumFailed(line 271-277)/IsDatumRefUnresolvable(line 281-287) → Mark*+faiAllPass=false+measuredCount++/continue. TryExecuteMeasurement 호출 line 293.
- `TryGrabOrLoadFaiDualImages`(line 444-483): pathA = `TeachingImagePath_Horizontal 존재?→그것:else ShotParam.SimulImagePath`(**HasImage 미확인 = D-08 버그**), pathB = TeachingImagePath_Vertical. 한쪽 로드 실패 시 양쪽 Dispose+false.
- `TryExecuteMeasurement`(line 668-702): DualImage 분기 → TryGrabOrLoadFaiDualImages → RuntimeImageA=imgA/RuntimeImageB=imgB → TryExecute(image 인자 무시) → finally에서 imgA/imgB Dispose + RuntimeImageA/B=null. **주입 소유권 = finally Dispose 계약** → 크로스-Z 저장분도 반드시 클론으로 주입(저장소 참조 직접 주입 금지).
- `MarkMeasurementDatumRefMissing`(line 606-615): ClearResult+LastSkipReason=...+LastJudgement=false+로그 = D-05 명시적 NG 미러 대상. `SkipReason.ZINDEX_MISCONFIGURED`(Plan 01 신규). DualImageEdgeDistanceMeasurement.ZIndexA/ZIndexB(Plan 01 신규).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: InspectionSequence 크로스-Z 이미지 저장소 + Store/Take/Clear(Dispose) + ResetCycleState 연동 (D-02/D-02a/D-03)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (멤버 선언 line 54-64, ResetCycleState line 486-492, ClearDatumTransforms line 845-847, HandleDatumIndexResponse z=0 호출 지점 ~595)
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs (SetImage/GetImage/HasImage/_imageLock line 236-374 — 소유클론 lock 계약)
    - .planning/phases/68-.../68-PATTERNS.md (InspectionSequence Analog 1/2 + Shared Pattern 3/4)
  </read_first>
  <action>
    `_datumTransforms`/`_failedDatums`(line 55-58) 옆에 크로스-Z 저장소 멤버 추가: `private readonly Dictionary<string, HImage> m_dicCrossZImages = new Dictionary<string, HImage>();`(필드명 헝가리언 준수, 이름 planner 재량). 저장소 접근을 위한 private 헬퍼 3개(각 30줄 이하, 헝가리언, if 스타일): (1) `StoreCrossZImage(string szKey, HImage image)` — 기존 키 있으면 그 HImage Dispose 후 `image.CopyImage()` 저장(ShotConfig.SetImage 계약 미러, 소유 클론 보관); (2) `TakeCrossZImageCopy(string szKey)` — 있으면 `.CopyImage()` 반환(호출부 소유, 없으면 null); (3) `HasCrossZImage(string szKey)` — 존재 여부. 그리고 `ClearCrossZImages()` — 전 엔트리 HImage Dispose 후 `.Clear()`. 이 `ClearCrossZImages()` 호출을 **`ResetCycleState()`**(z=0 리셋, D-03 편승) 안에 추가 — `ClearDatumTransforms()`(매 DatumPhase 호출)가 **아니라** ResetCycleState여야 크로스-Z 이미지가 사이클(여러 Shot/Action) 전체를 살아남고 z=0에서만 정리됨(PATTERNS.md 명시). thread-safety: `_datumTransforms` 접근 컨벤션(현 파일)과 동일 수준 적용(별도 lock이 필요하면 저장소 전용 object 도입, ShotConfig._imageLock 패턴). Z2 미도달(PLC 중단) 시 다음 부품 z=0 도착으로 자동 Dispose+리셋되어 누수 없음(D-03).
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 빌드 PASS (0 errors, 0 new warnings)
    - InspectionSequence에 Dictionary<string, HImage> 크로스-Z 저장소 멤버 존재
    - Store(기존 키 Dispose 후 CopyImage 저장)/Take(CopyImage 반환)/Has/Clear(전 엔트리 Dispose) 헬퍼 존재, 각 30줄 이하
    - ClearCrossZImages() 호출이 ResetCycleState() 안에 있음 (ClearDatumTransforms 아님)
    - 삼항/null병합 미사용 (D-09)
  </acceptance_criteria>
  <done>z=0에서 Dispose되는 소유클론 크로스-Z 이미지 저장소, 빌드 PASS</done>
</task>

<task type="auto">
  <name>Task 2: D-08 버그 수정 — TryGrabOrLoadFaiDualImages imageA 라이브 최우선 (D-08)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (TryGrabOrLoadFaiDualImages line 444-483, EStep.DatumPhase의 ShotParam.SetImage line ~207, TryExecuteMeasurement finally Dispose line 668-702)
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs (HasImage/GetImage line 236-374)
    - .planning/phases/68-.../68-PATTERNS.md (Action_FAIMeasurement Analog 1 + RESEARCH Pitfall 3/Code Example 3)
  </read_first>
  <action>
    `TryGrabOrLoadFaiDualImages`의 pathA 산출(line 456-463)을 3단 우선순위 if/else-if/else로 재정렬: (1) `ShotParam.HasImage` 참이면 라이브 grab 이미지 최우선 사용 — 이 경우 파일 경로가 아니라 `ShotParam.GetImage()`(CopyImage 반환, 소유 클론)를 imageA로 직접 대입(호출부 TryExecuteMeasurement가 finally Dispose하므로 소유권 일치, pathA 파일 로드 스킵); (2) 아니면 `TeachingImagePath_Horizontal` 존재+파일 있으면 그 경로 로드(기존); (3) 아니면 `ShotParam.SimulImagePath` 폴백(기존). imageA를 라이브로 이미 확보한 경우 pathA 기반 `new HImage(pathA)`를 다시 하지 않도록 분기(라이브 확보 시 imageA 이미 non-null). imageB(pathB=TeachingImagePath_Vertical) 경로는 이 task에서 무변경(크로스-Z는 Task 3). 헝가리언 bool 변수(bHasLiveImage) 추출 + if/else-if/else(삼항 금지, D-09). 기존 로드실패 시 양쪽 Dispose+false 계약 보존. SIMUL_MODE 경로 영향 확인(SIMUL에서는 DatumPhase가 SimulImagePath로 SetImage하므로 HasImage 참 → 라이브 분기 자연 진입).
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 빌드 PASS (0 errors, 0 new warnings)
    - TryGrabOrLoadFaiDualImages pathA 산출이 ShotParam.HasImage(라이브) → TeachingImagePath_Horizontal → SimulImagePath 3단 우선순위
    - 라이브 확보 시 ShotParam.GetImage()(클론) 사용, 파일 재로드 안 함
    - imageA 로드 실패 시 양쪽 Dispose+false 계약 보존, 삼항 미사용
  </acceptance_criteria>
  <done>라이브 grab된 이미지가 있으면 imageA가 파일 재로드 대신 라이브 사용, 빌드 PASS</done>
</task>

<task type="auto">
  <name>Task 3: ZIndexA/B 크로스-Z 캡처/주입 + Z1 pending 미보고 + D-05 측정 게이트 (D-02a/D-05)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (EStep.Measure 루프 line 264-313, 게이트 line 271-287, TryExecuteMeasurement DualImage 분기 line 668-702, TryGrabOrLoadFaiDualImages line 444-483, MarkMeasurementDatumRefMissing line 606-615)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (Task 1 신규 Store/Take/Has 헬퍼, AggregateIndexFais/BuildScopedResponse z_index별 집계, ParseCurrentZIndex line 496-518, m_nCurrentZIndex)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs (ZIndexA/ZIndexB, RuntimeImageA/B line 124-128)
    - .planning/phases/68-.../68-PATTERNS.md (Action_FAIMeasurement Analog 2/3/4)
  </read_first>
  <action>
    두 부분:

    (A) 크로스-Z 캡처/주입 (D-02a): DualImageEdgeDistanceMeasurement에서 `ZIndexA != -1 && ZIndexB != -1`(둘 다 설정)이면 크로스-Z 경로 진입. 현재 사이클 z_index(m_nCurrentZIndex/ParseCurrentZIndex)를 nCurZ로 얻어: 이 측정의 두 참조 z_index 중 nCurZ에 해당하는 라이브 이미지를 `ShotParam.GetImage()`(클론)로 얻어 `parentSeq2.StoreCrossZImage(측정키+역할)`에 저장(측정키 = MeasurementName 등 사이클 내 안정 문자열, 역할 A/B는 nCurZ==ZIndexA인지 ZIndexB인지로 결정). 그 다음 두 역할(A,B) 이미지가 모두 저장소에 존재하면(HasCrossZImage 양쪽 true = 완성 z_index) → `TakeCrossZImageCopy`로 A/B 클론 각각 얻어 RuntimeImageA/RuntimeImageB에 주입 → **기존 TryExecute를 그대로 한 번 호출**(알고리즘 무변경) → EvaluateJudgement(정상 보고) → finally에서 주입 이미지 Dispose. 한쪽만 있으면(첫 z_index=Z1) → 캡처만 하고 이 측정은 이번 index에서 **미보고(pending, NG 아님)** — faiAllPass=false 설정 금지, NG mark 금지, RESULT 미포함. pending 항목이 Z1 index 응답(AggregateIndexFais/BuildScopedResponse)에 NG나 실패로 잡히지 않도록 처리(예: LastSkipReason에 pending 표식 또는 집계에서 pending 측정 제외 — 정확한 훅은 AggregateIndexFais 코드 확인 후 결정, "완성 index 응답에만 담김"=CONTEXT specifics 준수). 라이브 캡처의 grab은 이미 EStep.DatumPhase(GrabSyncLock 내)에서 ShotParam에 SetImage됨 → 여기서는 GetImage 클론만 사용(새 grab 호출 없음 = lock 위반 없음). ZIndexA/B 중 하나만 설정(-1/비-1 혼합)은 오설정 → (B) 게이트로.

    (B) D-05 게이트: EStep.Measure 루프의 IsDatumRefUnresolvable 게이트(line 281-287) 바로 옆에 크로스-Z 오설정 게이트 추가(동일 continue-after-mark shape). 오설정 판정(bool 변수, private 헬퍼 `IsZIndexMisconfigured(meas)` 30줄 이하): DualImage 측정에서 `ZIndexA == ZIndexB`(둘 다 설정인데 동일) 또는 참조 z_index가 레시피에 존재하지 않음(어떤 Shot/측정도 그 z_index를 안 가짐 — FindActionIndicesByZIndex류 조회로 확인). 오설정이면 신규 `MarkMeasurementZIndexMisconfigured(meas)`(MarkMeasurementDatumRefMissing 미러: ClearResult+LastSkipReason=SkipReason.ZINDEX_MISCONFIGURED+LastJudgement=false+ELogType.Error 로그로 측정명/ZIndexA/ZIndexB 명시) 호출 + faiAllPass=false + measuredCount++ + continue. 조용한 폴백 금지(DatumConfig.SourceShotName의 Shots[0] 폴백 반면교사). 단일-설정(-1/비-1 혼합)도 오설정으로 처리.

    ZIndexA/B 미설정(-1/-1)은 크로스-Z/게이트 진입 없이 기존 TryGrabOrLoadFaiDualImages 경로(Task 2 D-08 포함) 그대로 → 회귀 0(D-07).
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 빌드 PASS (0 errors, 0 new warnings)
    - ZIndexA/B 둘 다 설정 시 크로스-Z 경로: 첫 index=캡처만(NG/보고 없음), 완성 index=A/B 주입 후 기존 TryExecute 1회 호출 + EvaluateJudgement
    - RuntimeImageA/B 주입은 저장소 클론(TakeCrossZImageCopy) — 저장소 참조 직접 주입 아님, finally Dispose 계약 보존
    - 새 grab 호출 없음(EStep.DatumPhase GetImage 클론 재사용) — lock 위반 없음
    - IsZIndexMisconfigured 게이트가 EStep.Measure 루프 IsDatumRefUnresolvable 옆에 존재, ZIndexA==ZIndexB/존재하지 않는 index/단일설정 → MarkMeasurementZIndexMisconfigured(SkipReason.ZINDEX_MISCONFIGURED, 로그) + continue
    - ZIndexA/B 미설정(-1/-1) 측정은 기존 경로 그대로(회귀 0)
    - 삼항/null병합 미사용, 헬퍼 30줄 이하 (D-09)
  </acceptance_criteria>
  <done>Z1 캡처→Z2 계산 크로스-Z 측정 동작 + 오설정 명시적 NG, 미설정 레시피 회귀 0, 빌드 PASS</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레시피 ZIndexA/ZIndexB → 실행 시점 참조 조회 → 크로스-Z 저장소 접근 | 레시피 int 값이 존재하지 않는 index/음수/동일값이면 잘못된 조회·NG 유발 가능 |
| 크로스-Z HImage 저장소 (프로세스 메모리) | 미완성 사이클(Z2 미도달) 반복 시 HImage 누적 → 메모리 고갈 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-04 | Denial of Service | ZIndexA/B 런타임 참조 조회 (존재하지 않는/음수/동일 index) | mitigate | IsZIndexMisconfigured 게이트가 실행 시점에 범위/존재/동일값 검증(V5 입력검증) → 예외 대신 명시적 NG(ZINDEX_MISCONFIGURED). ParseCurrentZIndex 방어 가드(TryParse+비음수) 패턴 재사용. 배열 인덱스 예외/TCP 스레드 크래시 방지. |
| T-68-05 | Resource exhaustion (메모리) | 크로스-Z HImage 저장소 누수 | mitigate | Store가 기존 키 Dispose 후 교체, ClearCrossZImages가 z=0(다음 부품 시작)마다 전 엔트리 Dispose+Clear(D-03). Z2 미도달해도 다음 z=0에서 자동 정리 → 무한 누적 없음. |

**Supply chain:** 신규 패키지 설치 없음 — T-68-SC N/A.
</threat_model>

<verification>
- msbuild Debug/x64 빌드 PASS (매 task)
- SIMUL UAT(Plan 05): z=ZIndexA $TEST(캡처만/미보고) → z=ZIndexB $TEST(거리 보고), 다음 부품 z=0 후 잔류 이미지 없음, ZIndexA==ZIndexB 레시피 → ZINDEX_MISCONFIGURED NG, 미설정 SHOT_E5 회귀 0
</verification>

<success_criteria>
- Z1 캡처 → Z2 완성 계산이 기존 TryExecute 무변경으로 동작
- 크로스-Z 이미지가 z=0에서 Dispose+리셋 (누수 0)
- D-08: 라이브 grab 이미지 우선 사용
- 오설정 → 명시적 NG(ZINDEX_MISCONFIGURED), 조용한 폴백 없음
- ZIndexA/B 미설정 레시피 회귀 0
</success_criteria>

<output>
Create `.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-03-SUMMARY.md` when done
</output>
