---
phase: 68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind
plan: 04
type: execute
wave: 4
depends_on: [68-01, 68-03]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: true
requirements: [PROTO-Z-CROSS]
must_haves:
  truths:
    - "DatumConfig(VerticalTwoHorizontalDualImage)의 ZIndexA/ZIndexB가 설정되면 두 Datum 이미지를 각 z_index 라이브 캡처로 취득해 크로스-Z 저장소를 거쳐 검출한다"
    - "Datum 크로스-Z 첫 z_index 처리 시 이미지만 캡처하고 검출을 보류(datum 실패 mark 없음), 완성 z_index에서 두 이미지로 기존 TryRunSingleDatum 검출을 수행한다"
    - "Datum ZIndexA==ZIndexB 또는 존재하지 않는 index 참조 시 명시적 실패 처리(ZINDEX_MISCONFIGURED 로그 + MarkDatumFailed) — 조용한 static 폴백 없음"
    - "DatumConfig ZIndexA/B 미설정(-1) 기존 Side/Bottom Datum은 static TeachingImagePath/_Vertical 경로로 100% 동작(회귀 0)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "TryGrabOrLoadDualDatumImages 크로스-Z 라이브 캡처/주입 + Datum 오설정 게이트"
      contains: "ZINDEX_MISCONFIGURED"
  key_links:
    - from: "Action_FAIMeasurement.EStep.DatumPhase (TryGrabOrLoadDualDatumImages)"
      to: "InspectionSequence 크로스-Z 저장소 (Plan 03)"
      via: "Datum Z1 캡처 저장 → Z2 검출"
      pattern: "CrossZImage"
---

<objective>
D-06을 구현한다: Datum 레벨(`VerticalTwoHorizontalDualImage`, Side/Bottom에서 이미 사용 중)에도 측정 레벨(Plan 03)과 동일한 크로스-Z 라이브 캡처를 적용한다. `DatumConfig.ZIndexA/ZIndexB`(Plan 01 필드)가 설정되면 `TryGrabOrLoadDualDatumImages`가 고정 static teaching 파일이 아니라 두 z_index 라이브 캡처로 두 Datum 이미지를 취득하고, Plan 03의 공유 크로스-Z 저장소를 거쳐(첫 z=캡처만 보류, 완성 z=두 이미지로 검출) 기존 `TryRunSingleDatum` 검출을 무변경 호출한다. Datum 오설정(ZIndexA==ZIndexB/존재하지 않는 index)은 명시적 실패(로그+MarkDatumFailed) — 조용한 static 폴백 금지(D-05 정신). 미설정 레시피는 기존 static 경로 그대로(회귀 0, D-07).

Datum 캡처는 이미 `EStep.DatumPhase`의 `GrabSyncLock` 블록 안에서 일어나므로(라이브 grab 추가 시 같은 lock 유지) 락 순서 회귀 없음.

Purpose: 측정과 근본원인 동일(고정 static 파일 재사용)한 Datum 레벨 크로스-Z 완성 — D-06.
Output: Datum DualImage 크로스-Z 라이브 캡처/검출, 미설정 회귀 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-CONTEXT.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-RESEARCH.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-PATTERNS.md
@.planning/refs/control-sequence-coding-guideline.md
@.planning/debug/shared-lighthandler-race.md
@.planning/phases/37-side-multi-datum-dualimage-2026-05-28/37-CONTEXT.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-01-SUMMARY.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-03-SUMMARY.md

<coding_guideline_scope>
D-09 (LOCKED): `Action_FAIMeasurement.cs` 신규/수정 코드는 `.planning/refs/control-sequence-coding-guideline.md` 준수 — 헝가리언, `if/else if/else`만(삼항·null병합 금지), 조건식 bool 변수화, 매직넘버 const, 함수 30줄 이하 분리.
</coding_guideline_scope>

<lock_order_constraint>
Datum 크로스-Z 라이브 grab 추가는 반드시 기존 `EStep.DatumPhase`의 `lock (LightHandler.Handle.GrabSyncLock)` 블록(line 94-229) 안에서. TryGrabOrLoadDualDatumImages는 이미 line 111(lock 내부)에서 호출됨 — 그 안에 grab을 추가해도 lock 유지. 새 lock scope 금지.
</lock_order_constraint>

<interfaces>
확인된 현재 코드 앵커 (2026-07-22):
- `Action_FAIMeasurement.cs` EStep.DatumPhase Datum 루프(line 100-196, lock 내부): `ApplyDatumLights(datum)`(line 103) → `WaitForPendingWrites()`(line 107) → VerticalTwoHorizontalDualImage 분기(line 108): `TryGrabOrLoadDualDatumImages(datum, out imgH, out imgV)`(line 111) 실패 시 MarkDatumFailed+continue; 성공 시 IsPatternAlignEnabled→TryComposeAlign / else→`TryRunSingleDatum(datum, imgH, imgV, out derr)`(line 140); finally에서 imgH/imgV Dispose(line 150-152).
- `TryGrabOrLoadDualDatumImages`(line 408-438): imgH=TeachingImagePath, imgV=TeachingImagePath_Vertical 두 static 파일 로드만(라이브 grab 분기 없음). 경로 빈/파일없음→false+로그. 한쪽 실패 시 양쪽 Dispose+false.
- Shot 라이브 grab 선례(line 207-227, lock 내): `SystemHandler.Handle.Devices.GrabHalconImage(ShotParam)` / SIMUL은 `LoadShotInspectionImage()`. Datum 크로스-Z 라이브 grab도 이 grab 경로 재사용(새 메커니즘 금지, D-01).
- InspectionSequence 크로스-Z 저장소(Plan 03): StoreCrossZImage/TakeCrossZImageCopy/HasCrossZImage/ClearCrossZImages — Datum 엔트리도 동일 저장소 공유(측정 키와 충돌 없는 datum 키 네임스페이스). `parentSeq` 로 접근.
- DatumConfig.ZIndexA/ZIndexB(Plan 01 신규, 기본 -1). `SkipReason.ZINDEX_MISCONFIGURED`(Plan 01). MarkDatumFailed(parentSeq).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TryGrabOrLoadDualDatumImages 크로스-Z 라이브 캡처/주입 + Z1 보류(D-06)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (Datum 루프 line 100-196, TryGrabOrLoadDualDatumImages line 408-438, Shot 라이브 grab 선례 line 207-227)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (Plan 03 크로스-Z 저장소 헬퍼, ParseCurrentZIndex line 496-518, m_nCurrentZIndex)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (ZIndexA/ZIndexB Plan 01 신규, TeachingImagePath/_Vertical)
    - .planning/phases/68-.../68-PATTERNS.md (Action_FAIMeasurement Analog 2/4)
    - .planning/phases/68-.../68-03-SUMMARY.md (Plan 03 실제 저장소 시그니처 확인)
  </read_first>
  <action>
    `TryGrabOrLoadDualDatumImages`(line 408-438)에 크로스-Z 우선 분기 추가(Plan 03 Task 3 FAI 로직 미러). `datum.ZIndexA != -1 && datum.ZIndexB != -1`(둘 다 설정)이면 크로스-Z 경로: 현재 사이클 z_index(nCurZ = m_nCurrentZIndex/ParseCurrentZIndex)에 해당하는 Datum 이미지를 라이브 grab(SIMUL은 LoadShotInspectionImage 계열, 실HW는 GrabHalconImage — Shot 라이브 grab 선례 line 207-227 재사용, 이미 GrabSyncLock 내부이므로 새 lock 없음)해 `parentSeq.StoreCrossZImage(datum키+역할)`에 저장(datum키 = datum.DatumName 기반 안정 문자열, 측정 키와 네임스페이스 구분). 두 역할(A=ZIndexA, B=ZIndexB) 이미지가 모두 저장소에 존재하면(완성 z_index) → TakeCrossZImageCopy로 imgH/imgV 클론 각각 얻어 out 파라미터로 반환(호출부가 finally Dispose — 기존 계약) → 호출부의 기존 TryRunSingleDatum/TryComposeAlign 검출 그대로 수행. 한쪽만 있으면(첫 z_index) → 캡처만 하고 out imgH/imgV = null + **특수 반환값으로 "보류(pending, 실패 아님)"를 호출부에 신호** (예: 별도 out bool `bPending=true` 또는 별도 반환 코드) → 호출부(line 111 분기)가 pending일 때는 MarkDatumFailed/RuntimeDetectFailed를 **설정하지 않고** 그냥 이번 index datum 검출을 skip(다음 index에서 완성). 이 "pending vs 실패" 구분이 핵심 — 기존 false=실패(MarkDatumFailed) 경로와 섞이지 않게 호출부 분기 명확화. ZIndexA/B 미설정(-1/-1)이면 크로스-Z 미진입, 기존 static 파일 로드 경로 그대로(회귀 0, D-07). 헝가리언 + if/else-if/else + 30줄 초과 시 private 헬퍼 분리(D-09). 라이브 grab 이미지는 소유 클론으로 저장(Store가 CopyImage), out 반환도 클론.
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 빌드 PASS (0 errors, 0 new warnings)
    - TryGrabOrLoadDualDatumImages에 ZIndexA/B 둘 다 설정 시 크로스-Z 라이브 캡처 분기 존재
    - 첫 z_index: 캡처만 + pending 신호(MarkDatumFailed/RuntimeDetectFailed 미설정), 완성 z_index: imgH/imgV 클론 반환 후 기존 TryRunSingleDatum 검출
    - 라이브 grab이 기존 grab 경로(GrabHalconImage/LoadShotInspectionImage) 재사용, GrabSyncLock 내부 유지(새 lock 없음)
    - ZIndexA/B 미설정(-1/-1)은 기존 static 파일 경로 그대로 (회귀 0)
    - 호출부(line 111)가 pending과 실패(기존 false)를 구분 처리, 삼항/null병합 미사용
  </acceptance_criteria>
  <done>Datum DualImage가 두 z_index 라이브 캡처로 검출(Z1 보류→Z2 검출), 미설정 회귀 0, 빌드 PASS</done>
</task>

<task type="auto">
  <name>Task 2: Datum ZIndexA/B 오설정 게이트 + Side/Bottom Datum 회귀 빌드 검증 (D-05 for Datum)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (Datum 루프 line 100-196, Task 1 신규 크로스-Z 분기, MarkDatumFailed 호출 패턴 line 120/147)
    - WPF_Example/Custom/Sequence/Inspection/SkipReason.cs (ZINDEX_MISCONFIGURED Plan 01)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (ZIndexA/ZIndexB)
    - .planning/phases/37-side-multi-datum-dualimage-2026-05-28/37-CONTEXT.md (Side Datum DualImage 회귀 기준)
  </read_first>
  <action>
    Datum 크로스-Z 진입 전에 오설정 게이트 추가: DatumConfig에서 `ZIndexA == ZIndexB`(둘 다 설정인데 동일) 또는 참조 z_index가 레시피에 존재하지 않음 또는 단일-설정(-1/비-1 혼합)이면 오설정으로 판정(bool 변수, private 헬퍼 `IsDatumZIndexMisconfigured(datum)` 30줄 이하). 오설정이면 `ELogType.Error` 로그(datum명 + ZIndexA/ZIndexB + `SkipReason.ZINDEX_MISCONFIGURED` 명시) + `parentSeq.MarkDatumFailed(datum.DatumName)` + `datum.RuntimeDetectFailed = true` + continue(datum skip) — 조용한 static 폴백 금지(DatumConfig.SourceShotName Shots[0] 반면교사). 게이트는 line 108 VerticalTwoHorizontalDualImage 분기 진입 직후, TryGrabOrLoadDualDatumImages 호출 전. 미설정(-1/-1)은 게이트 미해당 → 기존 static 경로. 그리고 이 task에서 전체 재빌드로 Side/Bottom Datum(ZIndexA/B 미설정) 코드 경로가 컴파일/구조상 무변경임을 확인(회귀 0 — 실제 검출 회귀는 Plan 05 UAT). 헝가리언 + if/else + 매직넘버 const(D-09).
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal</automated>
  </verify>
  <acceptance_criteria>
    - msbuild Debug/x64 빌드 PASS (0 errors, 0 new warnings)
    - IsDatumZIndexMisconfigured 게이트가 VerticalTwoHorizontalDualImage 분기 진입 직후·TryGrabOrLoadDualDatumImages 호출 전 존재
    - 오설정 시 ELogType.Error 로그(ZINDEX_MISCONFIGURED 명시) + MarkDatumFailed + RuntimeDetectFailed=true + continue (조용한 폴백 없음)
    - ZIndexA/B 미설정(-1/-1) Datum은 게이트 미해당, 기존 static 경로 무변경
    - 삼항/null병합 미사용, 헬퍼 30줄 이하 (D-09)
  </acceptance_criteria>
  <done>Datum 오설정 명시적 실패 + Side/Bottom Datum 미설정 회귀 0(빌드), 빌드 PASS</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레시피 DatumConfig.ZIndexA/ZIndexB → EStep.DatumPhase 실행 시점 참조 | 존재하지 않는/음수/동일 index면 잘못된 조회 또는 검출 오작동 |
| Datum 크로스-Z HImage 저장소 엔트리 (Plan 03 공유 저장소) | Z2 미도달 반복 시 datum 이미지 누적 위험 (측정과 동일 저장소 = z=0 리셋 공유) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-06 | Denial of Service | Datum ZIndexA/B 런타임 참조 (존재하지 않는/동일/단일설정) | mitigate | IsDatumZIndexMisconfigured 게이트가 실행 시점 검증(V5) → 예외 대신 명시적 MarkDatumFailed+ZINDEX_MISCONFIGURED 로그. Datum 검출 정지/TCP 스레드 크래시 방지. |
| T-68-05b | Resource exhaustion (메모리) | Datum 크로스-Z 이미지 (Plan 03 공유 저장소) | mitigate | Plan 03 ClearCrossZImages가 z=0마다 datum 엔트리 포함 전부 Dispose+Clear — datum도 동일 lifecycle, 별도 누수 경로 없음. |

**Supply chain:** 신규 패키지 설치 없음 — T-68-SC N/A.
</threat_model>

<verification>
- msbuild Debug/x64 빌드 PASS (매 task)
- SIMUL UAT(Plan 05): Side/Bottom Datum ZIndexA/B 설정 시 두 z_index 캡처로 검출, 미설정 Side/Bottom Datum(Phase 37 기준) 회귀 0, Datum ZIndexA==ZIndexB → MarkDatumFailed
</verification>

<success_criteria>
- Datum DualImage가 크로스-Z 라이브 캡처(Z1 보류→Z2 검출)로 동작
- Datum 오설정 → 명시적 실패(ZINDEX_MISCONFIGURED 로그 + MarkDatumFailed)
- ZIndexA/B 미설정 Side/Bottom Datum 회귀 0
- 라이브 grab 락 순서(GrabSyncLock 내부) 준수
</success_criteria>

<output>
Create `.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-04-SUMMARY.md` when done
</output>
