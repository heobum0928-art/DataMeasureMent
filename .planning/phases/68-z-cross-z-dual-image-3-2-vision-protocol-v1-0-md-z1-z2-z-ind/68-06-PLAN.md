---
phase: 68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind
plan: 06
type: execute
wave: 6
depends_on: [68-02, 68-03]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Custom/SystemHandler.cs
autonomous: true
gap_closure: true
requirements: [PROTO-Z-CROSS]
must_haves:
  truths:
    - "FIX-0: z=0에서 캡처된 크로스-Z Datum role A 이미지가 z=0 응답 생성 시점에 더 이상 Dispose/Clear 되지 않고, z=1(role B) 도착 시점까지 저장소에 살아남는다"
    - "크로스-Z 저장소 리셋은 z=0 $TEST 수신 즉시(그 tick의 Action 실행 시작 전, StartV1Scoped z=0 분기)에 1회만 일어난다 — z=0 응답 생성 시점의 저장소 리셋은 제거됨"
    - "DebugManualZTrigger(수동 Z 브리지)도 동일 경로(ProcessTest→StartV1Scoped)를 타므로 SIMUL UAT에서도 리셋 타이밍이 동일하게 동작한다"
    - "D-07 회귀 0: ZIndexA/B 미설정 기존 레시피는 저장소가 항상 비어 리셋이 no-op → 기존 동작 100% 동일. TOP/BOTTOM 시퀀스 무영향"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "BeginCrossZImageCycle() 수신-시점 리셋 진입점 + ClearCrossZImages() 를 ResetCycleState 에서 제거"
      contains: "BeginCrossZImageCycle"
    - path: "WPF_Example/Custom/SystemHandler.cs"
      provides: "StartV1Scoped z=0 분기가 StartAll 이전에 BeginCrossZImageCycle() 호출"
      contains: "BeginCrossZImageCycle"
  key_links:
    - from: "Custom/SystemHandler.StartV1Scoped (z=0 분기)"
      to: "InspectionSequence.BeginCrossZImageCycle"
      via: "StartAll 호출 직전 (Action 실행 전)"
      pattern: "BeginCrossZImageCycle"
    - from: "InspectionSequence.ResetCycleState"
      to: "(더 이상 ClearCrossZImages 호출 안 함)"
      via: "cross-Z 저장소 clear 를 수신 시점으로 이동"
      pattern: "ResetCycleState"
---

<objective>
FIX-0(우선순위 0, 선행 필수): 크로스-Z 사이클 리셋 타이밍 버그를 고친다. 현재 ClearCrossZImages() 는 ResetCycleState() 안에서 호출되고, ResetCycleState() 는 z=0 응답 생성 시점(HandleDatumIndexResponse, 모든 z=0 Action 실행이 끝난 뒤)에 실행된다. 그 결과 z=0 tick 동안 StoreCrossZImage 로 저장된 role A 이미지가 같은 z=0의 응답 단계에서 즉시 Dispose+Clear 되어, z=1 도착 시 합칠 role A 가 없어 크로스-Z Datum 검출이 구조적으로 영원히 불가능하다(68-GAP-ANALYSIS.md FIX-0). 리셋을 "z=0 $TEST 수신 즉시(그 tick의 Action 실행 전)"로 이동한다.

Purpose: 이 수정 없이는 GAP-1/2/3, CROSS-1을 전부 고쳐도 관측 가능한 동작 변화가 없다(도달 불가능한 죽은 코드) — 나머지 모든 gap-closure 플랜의 전제조건이다.
Output: 수신-시점 리셋 진입점 배선 + ResetCycleState 에서 크로스-Z clear 제거.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-GAP-ANALYSIS.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-03-SUMMARY.md
@.planning/refs/control-sequence-coding-guideline.md

<interfaces>
InspectionSequence.cs 현재 크로스-Z 저장소 + 리셋 (수정 대상, 현재 트리 2026-07-22):
- 멤버: private readonly object _crossZImageLock; private readonly Dictionary string,HImage m_dicCrossZImages (line 64-65)
- private void ResetCycleState() (line 565-572): m_bCycleHasNG=false; m_bCycleDatumFailed=false; m_nCurrentZIndex=0; m_nLastZIndex=0; ClearCrossZImages(); (마지막 한 줄이 문제)
- private void ClearCrossZImages() (line 643-657): 전 엔트리 Dispose 후 m_dicCrossZImages.Clear() (락 보유). 유지 — 호출부만 이동.
- public StoreCrossZImage / TakeCrossZImageCopy / HasCrossZImage — role A 이미지가 z=0 DatumPhase 에서 여기로 저장됨. 무수정.
- ResetCycleState() 는 오직 HandleDatumIndexResponse()(line 773-781, z=0 응답 경로)에서만 호출됨.

핵심 사실(수정 안전성 근거): m_bCycleHasNG/m_bCycleDatumFailed 는 응답 생성 시점에만 write 된다(ClassifyMeasurement line 938/944, DetectDatumFailure line 778) — Action 실행 중엔 write 안 됨. Action 실행(z=0 DatumPhase) 중 채워지는 유일한 사이클 상태가 크로스-Z 저장소(role A)다. 따라서 저장소 clear 만 Action 실행 이전(수신 시점)으로 옮기면 되고, 나머지 accumulator 리셋은 z=0 응답 시점에 그대로 둬도 정확하다.

Custom/SystemHandler.cs 현재 z=0 실행 배선 (수정 대상):
- private bool StartV1Scoped(SequenceBase seq, TestPacket packet) (line 231-254). z=0 분기: bool bIsDatumZIndex = _lastPrepZIndex == DATUM_TEST_Z_INDEX; if (bIsDatumZIndex) { return seq.StartAll(packet); } (line 233-237). DATUM_TEST_Z_INDEX = 0 상수(line 201) 존재.
- z>=1 분기는 이미 InspectionSequence inspSeq = seq as InspectionSequence; 캐스트 패턴 사용(line 238). 동일 캐스트 스타일 재사용.
- DebugManualZTrigger(line 783+) 도 ProcessPrep→ProcessTest→StartV1Scoped 경로를 그대로 탐 → UAT 자동 수혜.
</interfaces>
</context>

<coding_guideline_scope>
D-09(control-sequence-coding-guideline.md, LOCKED) 준수 대상: InspectionSequence.cs, Custom/SystemHandler.cs 의 신규/수정 코드 전부 — 헝가리언 접두사(b/n/sz/m_), if/else if/else 만(삼항 · null병합 금지), 조건식 변수화, 매직넘버 상수화, 함수 30줄 이하.
</coding_guideline_scope>

<tasks>

<task type="auto">
  <name>Task 1: InspectionSequence — 크로스-Z 저장소 clear 를 수신-시점 진입점으로 이동</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - InspectionSequence.cs line 563-657 (ResetCycleState, StoreCrossZImage, ClearCrossZImages)
    - InspectionSequence.cs line 773-781 (HandleDatumIndexResponse — ResetCycleState 유일 호출부)
  </read_first>
  <action>
    ResetCycleState()(line 565-572)에서 ClearCrossZImages(); 한 줄(line 571)을 제거한다. 주석을 갱신해 "크로스-Z 저장소 clear 는 BeginCrossZImageCycle(수신 시점)으로 이동됨 — FIX-0(68-GAP-ANALYSIS.md)"을 명시하고, m_bCycleHasNG/m_bCycleDatumFailed/m_nCurrentZIndex/m_nLastZIndex 리셋은 그대로 유지한다(응답 생성 시점에만 write 되므로 안전).
    새 public 메서드 BeginCrossZImageCycle() 를 ClearCrossZImages 인근에 추가한다: 본문은 ClearCrossZImages(); 호출만(락은 ClearCrossZImages 내부가 이미 보유). 주석의 '왜': z=0 $TEST 수신 즉시(그 tick의 Action 실행 전) 크로스-Z 저장소를 클린 슬레이트로 만든다 — z=0 DatumPhase 가 role A 를 저장하기 전에 비워야 하고, 그 role A 는 z=0 응답 시점의 accumulator 리셋에 영향받지 않고 z=1 까지 살아남아야 한다(FIX-0). public 인 이유는 Custom/SystemHandler.StartV1Scoped(다른 클래스)가 직접 호출하기 때문(ApplyShotLights/FindActionIndicesByZIndex 와 동일 cross-class 노출 컨벤션, Plan 02 선례).
    Dispose lifecycle(D-03): ClearCrossZImages 가 이미 전 엔트리 Dispose 후 Clear 하므로 누수 0 — 리셋 지점만 이동, 누수 방지 로직은 무변경. D-09 준수.
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -t:Build -v:minimal 2>&1 | grep -E "Build succeeded|: error"</automated>
    <manual-check>InspectionSequence.cs 에 public void BeginCrossZImageCycle 존재 확인. ResetCycleState 본문에서 ClearCrossZImages 실제 호출(주석 라인 제외)이 0건인지 확인.</manual-check>
  </verify>
  <done>빌드 PASS(0 errors, 0 new warnings). BeginCrossZImageCycle 존재. ResetCycleState 는 더 이상 ClearCrossZImages 를 호출하지 않음.</done>
</task>

<task type="auto">
  <name>Task 2: SystemHandler — StartV1Scoped z=0 분기가 StartAll 전에 BeginCrossZImageCycle 호출</name>
  <files>WPF_Example/Custom/SystemHandler.cs</files>
  <read_first>
    - Custom/SystemHandler.cs line 231-254 (StartV1Scoped, z=0/z>=1 분기)
    - Custom/SystemHandler.cs line 208-221 (ProcessTest — packet.TestID 세팅 지점)
  </read_first>
  <action>
    StartV1Scoped(line 231-254)의 z=0 분기(bIsDatumZIndex true, line 234-237)를 수정한다: return seq.StartAll(packet); 직전에 seq 를 InspectionSequence 로 캐스트(InspectionSequence inspDatumSeq = seq as InspectionSequence;, z>=1 분기의 기존 캐스트 스타일 그대로)하고, bool bIsInspSeq = inspDatumSeq != null; if (bIsInspSeq) { inspDatumSeq.BeginCrossZImageCycle(); } 로 수신-시점 리셋을 호출한 뒤 return seq.StartAll(packet);. 캐스트 실패(방어적, 실제 미도달)면 리셋 없이 StartAll 만.
    이 지점이 정확히 "z=0 $TEST 수신 직후, StartAll(=z=0 Action 실행) 시작 전"이다. 주석에 FIX-0 근거(응답 시점 리셋 → 수신 시점 리셋 이동, role A 보존)를 남긴다. v2.6/legacy 경로(ProcessTest 의 Sequences.Start(packet), !IsDynamicFAIMode)와 z>=1 분기는 byte-identical 유지 — 크로스-Z 는 v1.0 전용. DebugManualZTrigger 는 이 경로를 그대로 타므로 별도 배선 불필요. D-09 준수(헝가리언 bIsInspSeq, if/else, 매직넘버 없음).
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -t:Build -v:minimal 2>&1 | grep -E "Build succeeded|: error"</automated>
    <manual-check>Custom/SystemHandler.cs StartV1Scoped z=0 분기에서 StartAll 호출 직전에 BeginCrossZImageCycle() 호출이 존재하는지 확인.</manual-check>
  </verify>
  <done>빌드 PASS. z=0 $TEST(및 DebugManualZTrigger z=0) 수신 시 StartAll 이전에 크로스-Z 저장소가 리셋됨. z>=1/v2.6 경로 무변경.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLC/handler → TCP $PREP/$TEST | 신뢰된 폐쇄망(PC1/PC2), 인증 계층 없음. z_index 정수 입력이 경계를 넘음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-05 | Denial of Service (자원 누수) | m_dicCrossZImages HImage 캐시 | mitigate | 리셋 지점을 z=0 수신으로 이동해도 ClearCrossZImages 의 전-엔트리 Dispose 는 무변경 — 누수 방지 강도 동일. Z2 미도달 시 다음 z=0 수신에서 정리(D-03 편승 유지) |
| T-68-06 | Tampering (잘못된 z_index) | StartV1Scoped z=0 판정 | accept | z=0 판정은 _lastPrepZIndex==DATUM_TEST_Z_INDEX 상수 비교 — ParseCurrentZIndex 의 음수/미수신 정규화(기존)로 이미 방어됨, 신규 표면 없음 |
</threat_model>

<verification>
- msbuild Debug/x64 Build PASS (0 errors, 기존 CS0618 obsolete 경고 외 0 new warnings)
- ResetCycleState 는 ClearCrossZImages 호출 없음 / BeginCrossZImageCycle 가 유일한 크로스-Z clear 진입점
- StartV1Scoped z=0 분기가 StartAll 직전 BeginCrossZImageCycle 호출
- z>=1 / v2.6 / legacy 경로 byte-identical (grep diff 로 확인)
</verification>

<success_criteria>
- 크로스-Z 저장소 리셋이 z=0 응답 생성 시점 → z=0 수신 시점으로 이동됨 (FIX-0)
- z=0에서 저장한 role A 이미지가 z=0 응답 후에도 저장소에 남음 (후속 플랜 68-07/68-08 의 z=1/z=2 소비 전제조건 충족)
- D-07 회귀 0 (미설정 레시피 no-op), TOP/BOTTOM 무영향
</success_criteria>

<output>
Create `.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-06-SUMMARY.md` when done
</output>
