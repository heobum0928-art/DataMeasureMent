---
phase: 68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind
plan: 07
type: execute
wave: 7
depends_on: [68-06]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Custom/SystemHandler.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: true
gap_closure: true
requirements: [PROTO-Z-CROSS]
must_haves:
  truths:
    - "GAP-1: '존재하는 z_index' 판정이 Shot own ZIndex + 측정 ZIndexA/B + Datum ZIndexA/B 합집합(선언된 z_index 유니버스)으로 확장되어, Side z=1(측정 Shot 없음, Datum ZIndexB=1) 및 SHOT_E5 측정(ZIndexA=1, own ZIndex=0)이 더 이상 ZINDEX_MISCONFIGURED 로 하드 실패하지 않는다"
    - "GAP-1: 위험 규칙(완성 index=max(ZIndexA,ZIndexB)가 시퀀스 최대 shot.ZIndex 초과 시 오설정)은 채택하지 않는다 — Plan 03 BLOCKER(완성 index는 shot.ZIndex와 독립)를 깨지 않음"
    - "GAP-1+GAP-2 공유: '이 z_index가 크로스-Z Datum에 쓰이는가'를 단일 소스 헬퍼(IsZIndexUsedByCrossZDatum / BuildCrossZDatumIndexSet)로 판정하고 GAP-1 유니버스와 GAP-2 실행스코프가 그 하나를 재사용한다(유사 순회 헬퍼 난립 방지, D-09)"
    - "GAP-2: Side z=1 처럼 오직 크로스-Z Datum 만 쓰는 z_index($TEST) 도착 시, StartAll 폴백+매 사이클 Error 로그 대신 최소 1개 Action(datum.SourceShotName Shot)만 실행하고 그 Action은 DatumPhase 이후 Grab/Measure 를 건너뛴다(AdvanceAfterDatumPhase)"
    - "GAP-2 필수 가드: IsDatumOnlyExecutionIndex 는 최상단에서 nZIndex==DATUM_Z_INDEX(0) 이면 반드시 false 를 반환한다 — z=0의 StartAll 전량 실행(D-01a)이 무너지지 않음"
    - "D-07 회귀 0: ZIndexA/B 미설정 기존 레시피는 선언 유니버스=Shot own ZIndex 집합으로 기존 DoesZIndexExistInRecipe 와 동일 결과, IsDatumOnlyExecutionIndex 항상 false → 기존 경로 100% 유지. TOP/BOTTOM 무영향"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "BuildCrossZDatumIndexSet/IsZIndexUsedByCrossZDatum(단일 소스), BuildDeclaredZIndexSet, DoesZIndexExistInRecipe 재작성, IsDatumOnlyExecutionIndex(z=0 가드), FindDatumOnlyActionIndices"
      contains: "IsDatumOnlyExecutionIndex"
    - path: "WPF_Example/Custom/SystemHandler.cs"
      provides: "StartV1Scoped 빈-매칭 시 datum-only 실행 라우팅(로그 억제)"
      contains: "IsDatumOnlyExecutionIndex"
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "EStep.DatumPhase 종료 시 datum-only index 면 EStep.End 로 스킵(AdvanceAfterDatumPhase)"
      contains: "IsDatumOnlyExecutionIndex"
  key_links:
    - from: "InspectionSequence.DoesZIndexExistInRecipe"
      to: "BuildDeclaredZIndexSet (Shot own + 측정 ZIndexA/B + Datum ZIndexA/B)"
      via: "membership 검사(FindShotByZIndex 단독 위임 제거)"
      pattern: "BuildDeclaredZIndexSet"
    - from: "Custom/SystemHandler.StartV1Scoped (빈 매칭)"
      to: "InspectionSequence.IsDatumOnlyExecutionIndex + FindDatumOnlyActionIndices"
      via: "StartSubset(datum-only action indices)"
      pattern: "FindDatumOnlyActionIndices"
    - from: "Action_FAIMeasurement.EStep.DatumPhase 종료"
      to: "EStep.End (Grab/Measure 스킵)"
      via: "parentSeq.IsDatumOnlyExecutionIndex(nCurZ)"
      pattern: "IsDatumOnlyExecutionIndex"
---

<objective>
GAP-1 + GAP-2를 한 플랜에서 처리한다(68-GAP-ANALYSIS.md 우선순위 1/2, 지침 #3~5). GAP-1: DoesZIndexExistInRecipe 가 FindShotByZIndex(shot.ZIndex만)에 위임돼 DatumConfig.ZIndexA/B 및 측정 ZIndexA/B 가 반영 안 되던 것을 "선언된 z_index 유니버스"로 확장 — Side(z=1을 own하는 Shot 없음)/SHOT_E5(own ZIndex=0, ZIndexA=1)가 매 사이클 ZINDEX_MISCONFIGURED 로 하드 실패하던 것을 닫는다. GAP-2: FindActionIndicesByZIndex 가 크로스-Z Datum 을 몰라 Side z=1 매칭 0건 → StartAll 폴백 + 매 사이클 Error 로그 나던 것을, 오직 크로스-Z Datum 만 쓰는 index 는 최소 1개 Action(DatumPhase 트리거)만 실행하고 Grab/Measure 는 스킵하도록 라우팅한다. 두 gap 은 "이 z_index 가 크로스-Z Datum 에 쓰이는가"를 단일 소스 헬퍼로 공유한다.

Purpose: Side(z=0,1=Datum 2위치 캡처, 측정 Shot z=2+) 배포를 막는 핵심 두 갭을 닫되, Plan 03 BLOCKER(완성 index 독립)와 D-01a(z=0 StartAll)를 깨지 않는다.
Output: 선언 유니버스 판정 + datum-only 실행 스코프 + DatumPhase 후 스킵.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-GAP-ANALYSIS.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-02-SUMMARY.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-03-SUMMARY.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-04-SUMMARY.md
@.planning/refs/control-sequence-coding-guideline.md

<interfaces>
InspectionSequence.cs 현재(2026-07-22):
- private const int DATUM_Z_INDEX = 0; private const int CROSS_Z_UNSET = -1; (line 73-74)
- public List DatumConfigs (line 52) — 이 시퀀스 소유 DatumConfig 리스트. 각 DatumConfig 에 ZIndexA/ZIndexB(-1=미설정, Plan 04) 존재.
- private bool DoesShotOwnCrossZIndex(ShotConfig shot, int nZIndex) (line 329-361): shot 의 FAIList 측정 중 DualImageEdgeDistanceMeasurement.ZIndexA/B == nZIndex 이면 true. 재사용 가능.
- public List FindActionIndicesByZIndex(int nZIndex) (line 370-406): Actions[] 순회, shot.ZIndex==nZIndex OR DoesShotOwnCrossZIndex → Actions[] 인덱스 목록. Side z=1 은 여기서 0건.
- private ShotConfig FindShotByZIndex(int nZIndex) (line 300-324): 이 시퀀스 소유 && shot.ZIndex==nZIndex 첫 매칭. DoesZIndexExistInRecipe 가 이것에 위임 중.
- public bool DoesZIndexExistInRecipe(int nZIndex) (line 669-672): 현재 return FindShotByZIndex(nZIndex) != null; ← GAP-1 재작성 대상.
- public int GetExecutionZIndex() (line 662-665): ParseCurrentZIndex() 래퍼. 현재 $TEST z_index 반환.
- private void WarnIfEmptyScope(TestResultPacket, int nMatchedShots, int nZIndex) (line 953-967): 매칭 0건이면 PrintErrLog. datum-only index 는 적법하게 비어있으므로 억제 필요.
- Datum SourceShotName→ShotConfig 역추적 선례: ResolveDatumSourceShot 인근(line 1136-1150, foreach shots: s.ShotName == datum.SourceShotName).

Custom/SystemHandler.cs 현재:
- private bool StartV1Scoped(SequenceBase seq, TestPacket packet) (line 231-254): z=0 → StartAll(D-01a). z>=1 → inspSeq.FindActionIndicesByZIndex(_lastPrepZIndex); 매칭>0 → StartSubset; 매칭 0 → PrintLog Error + StartAll 폴백(line 250-253). ← GAP-2 라우팅 지점.
- _lastPrepZIndex(volatile int, line 18) = 이번 $TEST 의 z_index 단일 소스.

Action_FAIMeasurement.cs 현재:
- EStep.DatumPhase(case 88-226): DatumConfigs 전체 재검출. 종료부 line 214-224: ApplyShotLights 복귀 후 Step=(int)EStep.Grab; (line 224). ← GAP-2 스킵 지점.
- parentSeq(InspectionSequence) 는 line 89-91 에서 획득됨.
- SequenceBase.StartSubset(int[] indices) 는 min-max 연속구간 실행 — 단일 인덱스 배열이면 그 Action 1개만 실행(StartCore(idx,idx)).

Side 실제 레이아웃(사용자 확정 2026-07-22): z=0,1 = Datum 2위치 캡처(측정 Shot 없음), 측정 Shot 은 z=2 부터. Datum(VerticalTwoHorizontalDualImage) ZIndexA=0, ZIndexB=1.
</interfaces>
</context>

<coding_guideline_scope>
D-09(LOCKED) 준수 대상: InspectionSequence.cs, Custom/SystemHandler.cs, Action_FAIMeasurement.cs 의 신규/수정 코드 전부 — 헝가리언, if/else if/else 만(삼항 · null병합 금지), 조건식 변수화, 매직넘버 상수화, 함수 30줄 이하(유니버스 헬퍼는 반드시 sub-함수로 분리).
</coding_guideline_scope>

<tasks>

<task type="auto">
  <name>Task 1: InspectionSequence — 선언 유니버스(GAP-1) + 크로스-Z Datum 단일소스 + datum-only 실행 판정(GAP-2)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - InspectionSequence.cs line 300-406 (FindShotByZIndex, DoesShotOwnCrossZIndex, FindActionIndicesByZIndex)
    - InspectionSequence.cs line 662-672 (GetExecutionZIndex, DoesZIndexExistInRecipe)
    - InspectionSequence.cs line 950-967 (WarnIfEmptyScope)
    - InspectionSequence.cs line 1130-1155 (SourceShotName→ShotConfig 역추적 선례)
  </read_first>
  <action>
    (a) 단일 소스 크로스-Z Datum 헬퍼: private HashSet int BuildCrossZDatumIndexSet() — DatumConfigs 순회, 각 datum 의 ZIndexA/ZIndexB 가 CROSS_Z_UNSET 아니면 set 에 add. private bool IsZIndexUsedByCrossZDatum(int nZIndex) = BuildCrossZDatumIndexSet().Contains(nZIndex). 이 둘이 "이 z_index 가 크로스-Z Datum 에 쓰이는가"의 유일한 소스(GAP-1 유니버스와 GAP-2 실행스코프가 공유 — 지침 #4).
    (b) GAP-1 선언 유니버스: private HashSet int BuildDeclaredZIndexSet() — 이 시퀀스 소유 Shot(OwnerSequenceName==Name) 순회하여 shot.ZIndex add + 각 Shot 의 DualImageEdgeDistanceMeasurement 중 ZIndexA/B != CROSS_Z_UNSET 인 값 add; 그리고 BuildCrossZDatumIndexSet() 의 원소를 UnionWith. 전 add 는 != CROSS_Z_UNSET 게이트 하에서만(D-07). RecipeManager 는 SystemHandler.Handle.Sequences.RecipeManager(FindShotByZIndex 선례) 사용.
    (c) DoesZIndexExistInRecipe(nZIndex) 재작성: return BuildDeclaredZIndexSet().Contains(nZIndex); (FindShotByZIndex 단독 위임 제거). ★ 위험 규칙 금지: "완성 index(max(ZIndexA,ZIndexB))가 시퀀스 최대 shot.ZIndex 초과 시 오설정" 규칙은 절대 추가하지 말 것(지침 #3 — Plan 03 BLOCKER 회귀). 동일값/단일설정/미선언 판정은 이미 IsZIndexMisconfigured/IsDatumZIndexMisconfigured(Action_FAIMeasurement) 가 담당하므로 여기선 순수 membership 만.
    (d) GAP-2 실행 판정: public bool IsDatumOnlyExecutionIndex(int nZIndex) — 최상단에 반드시 bool bIsDatumTestZIndex = nZIndex == DATUM_Z_INDEX; if (bIsDatumTestZIndex) { return false; } 가드(지침 #5, z=0 StartAll 유지 D-01a). 그 다음: bool bUsedByCrossZDatum = IsZIndexUsedByCrossZDatum(nZIndex); bool bHasRegularExec = FindActionIndicesByZIndex(nZIndex).Count > 0; return bUsedByCrossZDatum && !bHasRegularExec; (오직 크로스-Z Datum 만 쓰고 일반 실행 매칭이 없는 index).
    (e) datum-only 실행 대상: public List int FindDatumOnlyActionIndices(int nZIndex) — DatumConfigs 중 ZIndexA/B == nZIndex 인 datum 들의 SourceShotName 을 Actions[] 에서 역추적(faiAct.ShotParam.ShotName == datum.SourceShotName && OwnerSequenceName==Name)해 Action 인덱스를 수집(중복 제거). SourceShotName 미해결 datum 이 있으면, DatumPhase 는 어차피 실행 Action 하나에서 DatumConfigs 전체를 재검출하므로 이 시퀀스 소유 첫 Action 인덱스(가장 작은 owned index)를 트리거로 add 하고 로그를 남긴다. (D-05 의 조용한 Shots[0] 데이터-오귀속 안티패턴과 구분: 여기선 '데이터'가 아니라 'DatumPhase 트리거용 Action' 선택이며 검출은 SourceShotName 상속 카메라/조명 기준으로 datum 이 스스로 수행 — 로그로 명시.)
    (f) WarnIfEmptyScope(line 953-967): 최상단에 bool bDatumOnlyIndex = IsDatumOnlyExecutionIndex(nZIndex); if (bDatumOnlyIndex) { return; } 추가 — datum-only index(Side z=1)는 적법하게 비어있으므로(측정 항목 0, 완성 index 아님) 매 사이클 스퓨리어스 Error 로그를 억제. 나머지 빈-스코프 경고 동작은 무변경.
    함수 30줄 이하 유지(각 헬퍼 분리). public 노출 이유: IsDatumOnlyExecutionIndex/FindDatumOnlyActionIndices 는 Custom/SystemHandler + Action_FAIMeasurement(다른 클래스)가 호출(FindActionIndicesByZIndex 선례).
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -t:Build -v:minimal 2>&1 | grep -E "Build succeeded|: error"</automated>
    <manual-check>grep 로 IsDatumOnlyExecutionIndex 최상단에 DATUM_Z_INDEX 가드 존재, BuildDeclaredZIndexSet/IsZIndexUsedByCrossZDatum 존재, DoesZIndexExistInRecipe 가 BuildDeclaredZIndexSet 사용(FindShotByZIndex 직접 위임 제거), "완성 index > 최대 shot.ZIndex" 류 비교가 없음 확인.</manual-check>
  </verify>
  <done>빌드 PASS. Side z=1 이 선언 유니버스에 포함(Datum ZIndexB=1). IsDatumOnlyExecutionIndex(0)==false 보장. 위험 규칙 미포함. WarnIfEmptyScope 가 datum-only index 억제.</done>
</task>

<task type="auto">
  <name>Task 2: SystemHandler — StartV1Scoped 빈-매칭 시 datum-only 라우팅(GAP-2)</name>
  <files>WPF_Example/Custom/SystemHandler.cs</files>
  <read_first>
    - Custom/SystemHandler.cs line 231-254 (StartV1Scoped, 빈-매칭 StartAll 폴백)
  </read_first>
  <action>
    StartV1Scoped 의 z>=1 경로, matchedIndices 가 빈(bHasMatch==false) 분기(현재 line 250-253: PrintLog Error + StartAll 폴백)를 재구성한다: 빈 매칭이면 먼저 bool bDatumOnly = inspSeq.IsDatumOnlyExecutionIndex(_lastPrepZIndex); 확인. bDatumOnly true → List datumIndices = inspSeq.FindDatumOnlyActionIndices(_lastPrepZIndex); bool bHasDatumTrigger = datumIndices != null && datumIndices.Count > 0; if (bHasDatumTrigger) { return seq.StartSubset(datumIndices.ToArray(), packet); } (Error 로그 없음 — Side z=1 의 설계된 정상 경로). bDatumOnly false(진짜 운용 오류) 또는 datum 트리거 0건 → 기존 PrintLog Error + StartAll 폴백 유지(T-68-01 DoS 방지). 매칭>0 분기(StartSubset)와 z=0 분기(StartAll+68-06 리셋)는 무변경. D-09 준수(헝가리언 bDatumOnly/bHasDatumTrigger, if/else, 매직넘버 없음).
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -t:Build -v:minimal 2>&1 | grep -E "Build succeeded|: error"</automated>
    <manual-check>StartV1Scoped 빈-매칭 분기에서 IsDatumOnlyExecutionIndex → FindDatumOnlyActionIndices → StartSubset 경로가 StartAll 폴백보다 먼저 존재하고, datum-only 경로에는 Error 로그가 없는지 확인.</manual-check>
  </verify>
  <done>빌드 PASS. Side z=1 $TEST → datum-only Action 만 StartSubset 실행(무관 Shot 재-grab 0, 매 사이클 Error 로그 0). 진짜 운용 오류는 기존 StartAll 폴백+경고 유지.</done>
</task>

<task type="auto">
  <name>Task 3: Action_FAIMeasurement — datum-only index 는 DatumPhase 후 Grab/Measure 스킵(AdvanceAfterDatumPhase)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <read_first>
    - Action_FAIMeasurement.cs line 88-226 (EStep.DatumPhase, 특히 종료부 214-224)
    - Action_FAIMeasurement.cs line 228-252 (EStep.Grab — 스킵되면 Shot 측정 grab 미실행)
  </read_first>
  <action>
    EStep.DatumPhase 종료부(line 214-224, ApplyShotLights 복귀 후 Step=(int)EStep.Grab; line 224)를 조건 분기로 교체한다: int nCurZ = 0; bool bDatumOnly = false; if (parentSeq != null) { nCurZ = parentSeq.GetExecutionZIndex(); bDatumOnly = parentSeq.IsDatumOnlyExecutionIndex(nCurZ); } if (bDatumOnly) { Step = (int)EStep.End; } else { Step = (int)EStep.Grab; }. 주석의 '왜': datum-only index(Side z=1)에서는 이 Action 이 오직 DatumPhase(크로스-Z Datum role 캡처 + 검출)를 트리거하려고 실행됐다 — 이 Shot 의 Grab/Measure 를 실행하면 그 Shot 의 일반 측정이 잘못된 물리 Z(z=1은 Datum 위치)에서 재실행되어 cycle.json/저장이미지/화면이 오염된다(GAP-2 남은 리스크). 그래서 Grab/Measure 를 건너뛰고 곧장 End 로 간다.
    EStep.End(line 389-394)는 pMyContext.AllPass(스킵 시 default true) 기준으로 FinishAction — datum-only index 응답은 z>=1 BuildScopedResponse 로 가되 완성 index 아니면 빈 B(측정 항목 0, WarnIfEmptyScope 는 Task 1 (f)에서 억제됨)로 처리되어 프로토콜상 정상. ApplyShotLights 조명 복귀는 스킵 여부와 무관하게 DatumPhase 종료부에서 이미 수행되므로 유지. D-09 준수(헝가리언 nCurZ/bDatumOnly, if/else, 매직넘버 없음).
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -t:Build -v:minimal 2>&1 | grep -E "Build succeeded|: error"</automated>
    <manual-check>EStep.DatumPhase 종료부가 IsDatumOnlyExecutionIndex 결과에 따라 EStep.End vs EStep.Grab 로 분기하는지 확인. 비 datum-only(일반 z>=1, z=0) 경로는 여전히 EStep.Grab 로 진행하는지 확인.</manual-check>
  </verify>
  <done>빌드 PASS. datum-only index 에서 Action 은 DatumPhase 만 수행 후 End(Grab/Measure 스킵). 일반 index/z=0 는 기존대로 Grab→Measure 진행(회귀 0).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLC → $TEST z_index | 신뢰 폐쇄망. 정수 z_index 가 실행 스코프/유니버스 판정으로 흐름 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Denial of Service | StartV1Scoped datum-only 라우팅 | mitigate | datum 트리거 0건/진짜 오류 시 조용한 무시 금지 — StartAll 폴백+Error 로그 유지. datum-only 경로만 로그 억제(적법 empty) |
| T-68-07 | Tampering (범위 밖 z_index) | BuildDeclaredZIndexSet / IsDatumOnlyExecutionIndex | accept | z_index 는 ParseCurrentZIndex(기존)로 음수/미수신 정규화. set membership 는 예외 표면 없음(HashSet.Contains) |
| T-68-08 | Repudiation (오설정 은폐) | DoesZIndexExistInRecipe 확장 | mitigate | 유니버스 확장은 '적법 선언'만 통과시킴 — 동일값/단일설정은 IsZIndexMisconfigured/IsDatumZIndexMisconfigured 명시 NG(Plan 03/04)로 별도 차단, 위험 완성-index 규칙 미도입 |
</threat_model>

<verification>
- msbuild Debug/x64 Build PASS (0 errors, 0 new warnings)
- IsDatumOnlyExecutionIndex 최상단 z=0 가드 존재
- "완성 index vs 최대 shot.ZIndex 비교" 규칙 미포함(grep 확인)
- 단일 소스: DatumConfigs 순회 크로스-Z 인덱스 판정이 BuildCrossZDatumIndexSet 한 곳에서만 (유사 순회 헬퍼 난립 없음)
- D-07: 미설정 레시피에서 BuildDeclaredZIndexSet == {shot own ZIndex 집합}, IsDatumOnlyExecutionIndex 항상 false (논리 검토 + 빌드)
</verification>

<success_criteria>
- Side z=1(오직 Datum 캡처)이 하드 실패 없이 datum-only Action 만 실행 + 매 사이클 Error 로그 0
- SHOT_E5 측정(own ZIndex=0, ZIndexA=1)이 ZINDEX_MISCONFIGURED 오판정에서 해제됨
- GAP-1 위험 규칙 미채택(Plan 03 BLOCKER 무회귀)
- GAP-1/GAP-2 크로스-Z Datum 판정 단일 소스 공유
- z=0 StartAll(D-01a), D-07 회귀 0, TOP/BOTTOM 무영향
</success_criteria>

<output>
Create `.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-07-SUMMARY.md` when done
</output>
