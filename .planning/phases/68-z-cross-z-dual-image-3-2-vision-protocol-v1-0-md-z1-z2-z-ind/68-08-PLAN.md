---
phase: 68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind
plan: 08
type: execute
wave: 8
depends_on: [68-07]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: true
gap_closure: true
requirements: [PROTO-Z-CROSS]
must_haves:
  truths:
    - "CROSS-1: 크로스-Z Datum 기반 측정이 z=2(소비 index)에서 identity(무보정)로 조용히 폴백하지 않고, 저장소에 보관된 양 role 이미지로 결정론적 재검출되어 transform 이 복원된다"
    - "매 Action의 EStep.DatumPhase 진입마다 ClearDatumTransforms() 로 _datumTransforms 가 비워져도, 크로스-Z Datum 은 소비 index(자기 ZIndexA/B 아님)에서 저장 이미지로 재검출되어 transform/실패신호가 모든 소비 index에 복원된다"
    - "비-크로스-Z Datum 의 '매 index 재검출' 동작(회귀 0 대상)은 그대로 유지된다 — 이 수정은 datum.ZIndexA/B 둘 다 설정된 크로스-Z 분기에만 적용"
    - "D-07 회귀 0: ZIndexA/B 미설정 Side/Bottom Datum(Phase 37 baseline)은 static 경로 그대로 100% 동작. TOP/BOTTOM 무영향"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "TryGrabOrLoadCrossZDatumImages 의 !bRelevant 조기반환을 '양 role 저장 완료 시 결정론적 재검출'로 교체 + TryReDetectCrossZDatumFromStore 헬퍼"
      contains: "TryReDetectCrossZDatumFromStore"
  key_links:
    - from: "Action_FAIMeasurement.TryGrabOrLoadCrossZDatumImages (!bRelevant)"
      to: "InspectionSequence.HasCrossZImage/TakeCrossZImageCopy (저장 이미지 재사용)"
      via: "소비 index에서 저장 이미지로 재검출 → TryRunSingleDatum → _datumTransforms 복원"
      pattern: "TakeCrossZImageCopy"
---

<objective>
CROSS-1(교차 이슈, 지침 #2): FIX-0/GAP-1/2/3 를 다 적용해도 z=1에서 크로스-Z Datum 검출이 성공해도 z=2 측정 시점엔 그 transform 이 이미 사라져 있는 문제를 고친다. ClearDatumTransforms() 가 매 Action 의 EStep.DatumPhase 진입마다 _datumTransforms 를 전부 비우고, 크로스-Z Datum 은 z=2 tick 에서 bRelevant=false(자기 ZIndexA/B 아님)로 재검출을 스킵한다 → transform 없음 → ResolveDatumTransform 이 identity 로 조용히 폴백 → 무보정 측정이 정상 측정처럼 P/F 를 낸다(68-GAP-ANALYSIS.md CROSS-1). TryGrabOrLoadCrossZDatumImages 의 !bRelevant 조기반환을 "양 role 이미지가 저장소에 이미 있으면 그걸로 결정론적 재검출"로 바꿔 모든 소비 index 에서 transform/실패신호가 복원되도록 한다.

Purpose: 이 수정 없이 GAP-3만 머지하면 "즉시 F" 반쪽 안전장치만 생기고 핵심 기능(보정된 측정)은 여전히 깨진 채 남는다.
Output: 크로스-Z Datum 의 소비-index 결정론적 재검출.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-GAP-ANALYSIS.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-04-SUMMARY.md
@.planning/refs/control-sequence-coding-guideline.md

<interfaces>
Action_FAIMeasurement.cs 현재(2026-07-22):
- EStep.DatumPhase 진입 직후 line 93: parentSeq.ClearDatumTransforms(); — 매 Action 실행마다 _datumTransforms 전체 clear(비-크로스-Z 의 '매 index 재검출' 전제).
- private bool TryGrabOrLoadDualDatumImages(datum, parentSeq, out imgH, out imgV, out bPending) (line 463-476): datum.ZIndexA/B 둘 다 != UNSET_ZINDEX 면 TryGrabOrLoadCrossZDatumImages 로 위임, 아니면 TryLoadStaticDualDatumImages(static, D-07).
- private bool TryGrabOrLoadCrossZDatumImages(datum, parentSeq, out imgH, out imgV, out bPending) (line 513-533): nCurZ=parentSeq.GetExecutionZIndex(); bIsRoleA=nCurZ==ZIndexA; bIsRoleB=nCurZ==ZIndexB; bRelevant=A||B. !bRelevant → bPending=true; return false; (line 524-528) ← CROSS-1 수정 대상. bRelevant → CaptureAndStoreCrossZDatumImage → TryTakeCompletedCrossZDatumImages.
- private bool TryTakeCompletedCrossZDatumImages(datum, parentSeq, out imgH, out imgV, out bPending) (line 552-575): keyA/keyB HasCrossZImage 둘 다면 TakeCrossZImageCopy 로 클론 반환(호출부 finally Dispose 계약), 아니면 bPending=true.
- private string BuildCrossZDatumKey(datum) (line 578-584): "DATUM|"+DatumName. keyA=+CROSS_Z_ROLE_SUFFIX_A, keyB=+CROSS_Z_ROLE_SUFFIX_B.
- 호출부(EStep.DatumPhase, line 122-166): TryGrabOrLoadDualDatumImages 성공 → TryComposeAlign 또는 TryRunSingleDatum(datum, imgH, imgV) → _datumTransforms 채움. bPending → continue(스킵, MarkDatumFailed 안 함). imgH/imgV 는 finally 에서 Dispose(line 167-170).
- InspectionSequence.HasCrossZImage(szKey)/TakeCrossZImageCopy(szKey) public(Plan 03) — 저장 이미지 조회/클론 취득.

동작 흐름(Side, Datum ZIndexA=0, ZIndexB=1):
- z=0: bIsRoleA → 저장 A. z=1: bIsRoleB → 저장 B → 완성 → TryRunSingleDatum → _datumTransforms 채움.
- z=2(측정 index): DatumPhase 재진입 → ClearDatumTransforms → _datumTransforms 빔. TryGrabOrLoadCrossZDatumImages: nCurZ=2, !bRelevant → 현재는 bPending=true continue → 재검출 없음 → 측정이 identity 폴백. ★CROSS-1이 고칠 지점.
</interfaces>
</context>

<coding_guideline_scope>
D-09(LOCKED) 준수 대상: Action_FAIMeasurement.cs 의 신규/수정 코드 전부 — 헝가리언, if/else if/else 만(삼항 · null병합 금지), 조건식 변수화, 매직넘버 상수화, 함수 30줄 이하.
</coding_guideline_scope>

<tasks>

<task type="auto">
  <name>Task 1: TryGrabOrLoadCrossZDatumImages — 소비 index에서 저장 이미지로 결정론적 재검출</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <read_first>
    - Action_FAIMeasurement.cs line 513-584 (TryGrabOrLoadCrossZDatumImages, CaptureAndStoreCrossZDatumImage, TryTakeCompletedCrossZDatumImages, BuildCrossZDatumKey)
    - Action_FAIMeasurement.cs line 122-170 (EStep.DatumPhase 크로스-Z 호출부 + finally Dispose)
    - Action_FAIMeasurement.cs line 88-93 (ClearDatumTransforms 진입 시점)
  </read_first>
  <action>
    TryGrabOrLoadCrossZDatumImages 의 !bRelevant 분기(line 524-528, 현재 bPending=true; return false;)를 교체한다: !bRelevant 여도 양 role 이미지가 저장소에 이미 있으면(bool bBothStored = parentSeq.HasCrossZImage(baseKey+CROSS_Z_ROLE_SUFFIX_A) && parentSeq.HasCrossZImage(baseKey+CROSS_Z_ROLE_SUFFIX_B)) 결정론적 재검출 경로로 보낸다 — 새 private 헬퍼 TryReDetectCrossZDatumFromStore(datum, parentSeq, out imgH, out imgV) 가 TakeCrossZImageCopy(keyA/keyB) 로 양 클론을 반환(호출부 finally Dispose 계약, TryTakeCompletedCrossZDatumImages 의 클론 반환 패턴 재사용). bBothStored true → return TryReDetectCrossZDatumFromStore(...) 로 imgH/imgV 반환(bPending=false) → 호출부가 TryRunSingleDatum/TryComposeAlign 을 그대로 호출해 _datumTransforms 를 복원. bBothStored false(아직 양 role 미완성이면서 이 tick 도 무관) → 기존대로 bPending=true; return false; (Z1 이전 소비 index 는 없지만 방어).
    함수 30줄 이하 유지 — 재검출 취득 로직은 TryReDetectCrossZDatumFromStore 로 분리. BuildCrossZDatumKey 재사용해 키 도출 단일화(중복 순회 금지).
    회귀 경계: 이 변경은 datum.ZIndexA/B 둘 다 설정된 크로스-Z 분기(TryGrabOrLoadCrossZDatumImages)에서만 적용된다. 비-크로스-Z Datum(TryLoadStaticDualDatumImages / 1-image GrabOrLoadDatumImage)의 "매 index 재검출" 동작은 무수정(D-07 회귀 0). ClearDatumTransforms(line 93)는 유지 — 비-크로스-Z 는 매 index 재검출로 채우고, 크로스-Z 는 저장 이미지 재검출로 채운다. D-09 준수(헝가리언 bBothStored, if/else, 매직넘버/키 상수화).
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -t:Build -v:minimal 2>&1 | grep -E "Build succeeded|: error"</automated>
    <manual-check>grep 로 TryReDetectCrossZDatumFromStore 존재, TryGrabOrLoadCrossZDatumImages 의 !bRelevant 분기가 HasCrossZImage(양 role) 확인 후 재검출로 분기하는지 확인. TryLoadStaticDualDatumImages 무변경 확인.</manual-check>
  </verify>
  <done>빌드 PASS. 크로스-Z Datum 이 소비 index(z=2)에서 저장 이미지로 재검출되어 transform 복원(identity 폴백 제거). 비-크로스-Z Datum/static 경로 무회귀.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 저장 HImage 캐시 → 측정 transform | 사이클 내부 상태, 외부 미노출. 잘못된 재사용 시 측정 오염 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-09 | Tampering (무보정 은폐) | ResolveDatumTransform identity 폴백 | mitigate | 소비 index 재검출로 transform 복원 — identity 폴백이 정상 측정으로 위장하던 결함 제거(CROSS-1 핵심) |
| T-68-05 | Denial of Service (이미지 누수) | TakeCrossZImageCopy 클론 | accept | 클론 반환 → 호출부 finally Dispose(기존 계약). 저장소 원본은 다음 z=0 리셋(68-06)에서 Dispose |
</threat_model>

<verification>
- msbuild Debug/x64 Build PASS (0 errors, 0 new warnings)
- TryGrabOrLoadCrossZDatumImages !bRelevant 분기가 양 role 저장 완료 시 재검출 (grep 확인)
- 비-크로스-Z Datum(static/1-image) 경로 byte-identical (grep diff)
- 크로스-Z 재검출 이미지 소유권: 클론 반환 + 호출부 finally Dispose (기존 계약 유지)
</verification>

<success_criteria>
- 크로스-Z Datum transform 이 z=1 검출 후 z=2 측정에서도 유효(identity 무보정 제거)
- 비-크로스-Z Datum 매 index 재검출 무회귀, D-07 static 경로 무회귀, TOP/BOTTOM 무영향
</success_criteria>

<output>
Create `.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-08-SUMMARY.md` when done
</output>
