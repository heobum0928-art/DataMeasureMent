---
phase: 68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind
plan: 09
type: execute
wave: 8
depends_on: [68-07]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
autonomous: true
gap_closure: true
requirements: [PROTO-Z-CROSS]
must_haves:
  truths:
    - "CROSS-2: '마지막 Index' 진실원(ComputeLastZIndex)이 크로스-Z 완성 index(측정 + Datum)를 반영한다 — 크로스-Z 완성 index가 시퀀스 최대 shot.ZIndex를 넘어도 한 사이클에 P/F가 2회 송신되지 않는다"
    - "ComputeLastZIndex = max(이 시퀀스 소유 전 shot.ZIndex, 소유 Shot의 크로스-Z 측정 완성 index, 이 시퀀스 크로스-Z Datum 완성 index)"
    - "GetDatumCompletionZIndex(datum) = max(ZIndexA,ZIndexB) (크로스-Z Datum), 비-크로스-Z Datum 은 미해당(CROSS_Z_UNSET) — GAP-3(68-10)가 재사용하는 단일 소스"
    - "D-07 회귀 0: 크로스-Z 미설정 레시피는 ComputeLastZIndex == max(shot.ZIndex) 로 기존과 동일. TOP/BOTTOM 무영향(크로스-Z 없음)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "ComputeLastZIndex 확장(크로스-Z 완성 index 반영) + GetDatumCompletionZIndex 단일 소스"
      contains: "GetDatumCompletionZIndex"
  key_links:
    - from: "InspectionSequence.ComputeLastZIndex"
      to: "GetMeasurementCompletionZIndex(측정) + GetDatumCompletionZIndex(Datum)"
      via: "max(shot.ZIndex 전체, 크로스-Z 완성 index 전체)"
      pattern: "GetDatumCompletionZIndex"
---

<objective>
CROSS-2(교차 이슈, 지침 #8): "마지막 Index" 진실원이 완성 index 와 독립적인 일반적 위험을 닫는다. ComputeLastZIndex(shot.ZIndex 최댓값만)와 GetMeasurementCompletionZIndex(shot.ZIndex 무관하게 max(ZIndexA,ZIndexB) 반환)가 서로 무관해서, 크로스-Z 완성 index 가 시퀀스의 최대 shot.ZIndex 를 넘으면 한 사이클에 P/F 가 2회 송신될 위험이 있다 — Side 특수 레이아웃과 무관하게 크로스-Z 측정이 하나만 있어도 발생 가능한 전 phase 회귀 위험(68-GAP-ANALYSIS.md CROSS-2). ComputeLastZIndex 를 max(전 shot.ZIndex, 모든 크로스-Z 완성 index)로 확장한다. 동시에 GAP-3(68-10)가 재사용할 GetDatumCompletionZIndex 단일 소스를 여기서 정의한다(지침 #8 GAP-3 조율).

Purpose: 크로스-Z 완성 index 가 마지막 Index 로 올바르게 인식되어 종합 P/F 가 정확히 1회, 올바른 index 에서 송신된다.
Output: ComputeLastZIndex 확장 + GetDatumCompletionZIndex 헬퍼.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-GAP-ANALYSIS.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-03-SUMMARY.md
@.planning/refs/control-sequence-coding-guideline.md

<interfaces>
InspectionSequence.cs 현재(2026-07-22):
- private const int CROSS_Z_UNSET = -1; DATUM_Z_INDEX = 0; (line 73-74)
- public List DatumConfigs (line 52) — DatumConfig.ZIndexA/ZIndexB(-1=미설정).
- private int ComputeLastZIndex(InspectionRecipeManager recipeManager) (line 268-295): 이 시퀀스 소유(shot.OwnerSequenceName==Name) Shot 의 shot.ZIndex 최댓값(nMax)만 반환. ← CROSS-2 확장 대상.
- private int GetMeasurementCompletionZIndex(MeasurementBase meas, ShotConfig shot) (line 807-816): DualImageEdgeDistanceMeasurement 이고 ZIndexA/B 둘 다 != CROSS_Z_UNSET 이면 max(ZIndexA,ZIndexB), 아니면 shot.ZIndex. 재사용.
- ComputeLastZIndex 호출부: AddResponseV1Cycle(line 765, 측정 경로) + HandleDatumIndexResponse(line 777, z=0 직후 재산출). bIsLastIndex = m_nCurrentZIndex >= m_nLastZIndex (line 766).
- RecipeManager 접근: recipeManager.Shots (ComputeLastZIndex 인자). 측정 완성 index 는 각 소유 shot 의 FAIList → Measurements 순회.

CROSS-2 위험 예: 크로스-Z 측정 ZIndexA=2,ZIndexB=3, 그 owning Shot 의 own ZIndex=0, 시퀀스 최대 shot.ZIndex=2. 완성 index=3 > 2. 현재 ComputeLastZIndex=2 → z=2 를 마지막으로 오인 → z=2 에서 P/F 송신 + z=3 에서 또 P/F 송신(2회) 위험.
</interfaces>
</context>

<coding_guideline_scope>
D-09(LOCKED) 준수 대상: InspectionSequence.cs 의 신규/수정 코드 전부 — 헝가리언, if/else if/else 만(삼항 · null병합 금지), 조건식 변수화, 매직넘버 상수화, 함수 30줄 이하(완성-index 수집은 sub-함수로 분리).
</coding_guideline_scope>

<tasks>

<task type="auto">
  <name>Task 1: ComputeLastZIndex 확장 + GetDatumCompletionZIndex 단일 소스</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - InspectionSequence.cs line 265-295 (ComputeLastZIndex)
    - InspectionSequence.cs line 804-851 (GetMeasurementCompletionZIndex, ShotHasCrossZMeasurementCompletingAt)
    - InspectionSequence.cs line 754-781 (AddResponseV1Cycle, HandleDatumIndexResponse — ComputeLastZIndex 호출부)
  </read_first>
  <action>
    (a) GetDatumCompletionZIndex 단일 소스: private int GetDatumCompletionZIndex(DatumConfig datum) — bool bIsCrossZ = datum != null && datum.ZIndexA != CROSS_Z_UNSET && datum.ZIndexB != CROSS_Z_UNSET; if (bIsCrossZ) { return System.Math.Max(datum.ZIndexA, datum.ZIndexB); } return CROSS_Z_UNSET; (비-크로스-Z Datum 은 완성 index 개념 없음 → CROSS_Z_UNSET). GetMeasurementCompletionZIndex(측정)와 대칭. GAP-3(68-10)가 이 메서드를 재사용하므로 private 로 두되 68-10 이 같은 파일에서 접근(같은 클래스).
    (b) ComputeLastZIndex(line 268-295) 확장: 기존 nMax(소유 shot.ZIndex 최댓값) 산출 후, 크로스-Z 완성 index 를 반영해 최종 max 를 산출한다. sub-함수로 분리 — private int MaxCrossZCompletionZIndex(InspectionRecipeManager recipeManager): 이 시퀀스 소유 Shot 순회하며 각 Shot 의 DualImageEdgeDistanceMeasurement 완성 index(GetMeasurementCompletionZIndex(meas, shot), 크로스-Z 인 것만) 최댓값 + DatumConfigs 순회하며 GetDatumCompletionZIndex(datum)(CROSS_Z_UNSET 아닌 것만) 최댓값을 취해 반환(없으면 0). ComputeLastZIndex 마지막에 nMax = System.Math.Max(nMax, MaxCrossZCompletionZIndex(recipeManager)); 후 return nMax. 각 완성 index 반영은 != CROSS_Z_UNSET 게이트 하에서만(D-07).
    회귀 경계: 크로스-Z 없는 레시피 → MaxCrossZCompletionZIndex 는 0(또는 shot.ZIndex 이하) → ComputeLastZIndex == 기존 max(shot.ZIndex). ★위험 규칙 금지: 이건 "마지막 index"를 크로스-Z 완성 index까지 확장하는 것일 뿐, GAP-1 이 배제한 "완성 index > shot.ZIndex 면 오설정" 판정과 무관(오히려 정상 케이스로 수용). 함수 30줄 이하(MaxCrossZCompletionZIndex 분리). D-09 준수(헝가리언 nMax/bIsCrossZ, if/else, 매직넘버 상수화).
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -t:Build -v:minimal 2>&1 | grep -E "Build succeeded|: error"</automated>
    <manual-check>grep 로 GetDatumCompletionZIndex 존재, ComputeLastZIndex 가 MaxCrossZCompletionZIndex 를 max 로 반영하는지 확인. 크로스-Z 없는 경로가 기존 max(shot.ZIndex)와 동치인지 논리 검토.</manual-check>
  </verify>
  <done>빌드 PASS. ComputeLastZIndex 가 크로스-Z 완성 index(측정+Datum)를 반영. GetDatumCompletionZIndex 단일 소스 존재(68-10 재사용 준비). 크로스-Z 없는 레시피 무회귀.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사이클 판정(m_nLastZIndex) → $RESULT P/F 송신 | 마지막 index 오판정 시 중복/누락 판정이 PLC 로 나감 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-10 | Repudiation (중복 P/F) | ComputeLastZIndex vs 완성 index 불일치 | mitigate | 마지막 index 를 크로스-Z 완성 index 까지 확장 → 종합 P/F 정확히 1회, 올바른 index 송신 |
| T-68-08 | Tampering (오설정 규칙 오용) | 완성 index 확장 | accept | 완성 index > shot.ZIndex 를 '정상'으로 수용 — GAP-1 이 배제한 '오설정' 규칙 미도입(Plan 03 BLOCKER 무회귀) |
</threat_model>

<verification>
- msbuild Debug/x64 Build PASS (0 errors, 0 new warnings)
- ComputeLastZIndex 가 크로스-Z 완성 index 반영(grep + 논리 검토)
- GetDatumCompletionZIndex 단일 소스 존재
- 크로스-Z 없는 레시피 ComputeLastZIndex 무회귀(D-07)
</verification>

<success_criteria>
- 크로스-Z 완성 index 가 마지막 Index 로 인식되어 종합 P/F 1회 송신
- GetDatumCompletionZIndex 단일 소스가 GAP-3(68-10)에 준비됨
- D-07 회귀 0, TOP/BOTTOM 무영향
</success_criteria>

<output>
Create `.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-09-SUMMARY.md` when done
</output>
