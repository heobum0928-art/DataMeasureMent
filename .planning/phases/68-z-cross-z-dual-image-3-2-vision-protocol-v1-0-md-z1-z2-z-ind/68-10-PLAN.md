---
phase: 68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind
plan: 10
type: execute
wave: 9
depends_on: [68-09]
files_modified:
  - WPF_Example/Setting/SystemSetting.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
autonomous: false
gap_closure: true
requirements: [PROTO-Z-CROSS]
must_haves:
  truths:
    - "GAP-3: 크로스-Z Datum 실패의 '즉시 F'가 z=0에서 1회만 산출되던 것을, 완성 index(GetDatumCompletionZIndex, Side는 z=1)의 응답 생성 시점에도 재평가한다 — 크로스-Z Datum이 완성 index에서 실패하면 그 index에서 F가 나갈 수 있다(게이팅 시)"
    - "GAP-3 필수: 신규 m_bImmediateFailSent latch가 기존 z==0 즉시-F 분기(BuildDatumShotResponse)에도 세팅되어, 한 사이클에 F가 2번 나가는 blocking 회귀가 없다(지침 #6)"
    - "GAP-3 게이팅(지침 #7): 완성 index에서의 '즉시 F' 프로토콜 활성화는 SystemSetting.EnableCrossZDatumImmediateFail(기본 false=OFF)로 게이팅된다 — 이 케이스는 Vision-Protocol-v1.0.md 원문(Idx0 단일위치 Datum)과 충돌하고 제어팀(PLC) 합의가 필요하므로, 합의 전까지 기본 OFF로 배포한다. 코드는 준비되되 활성화는 사람이 결정"
    - "플래그 OFF(기본)일 때 동작은 현재와 100% 동일 — 크로스-Z Datum 실패는 기존 per-FAI 게이트로 마지막 index 종합 F에 반영(D-07/회귀 0). TOP/BOTTOM 무영향(크로스-Z Datum 없음)"
    - "m_bImmediateFailSent latch는 사이클 시작(z=0 리셋)에 초기화되어 다음 부품으로 새지 않는다"
  artifacts:
    - path: "WPF_Example/Setting/SystemSetting.cs"
      provides: "EnableCrossZDatumImmediateFail bool 플래그(Category Connection|Protocol, 기본 false)"
      contains: "EnableCrossZDatumImmediateFail"
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "m_bImmediateFailSent latch(z=0 리셋 초기화 + z=0/완성 index 양 경로 세팅) + 완성 index 크로스-Z Datum 실패 재평가(게이팅)"
      contains: "m_bImmediateFailSent"
  key_links:
    - from: "InspectionSequence 측정 index 응답 경로(BuildScopedResponse/AddResponseV1Cycle)"
      to: "GetDatumCompletionZIndex + IsDatumFailed (완성 index 재평가)"
      via: "EnableCrossZDatumImmediateFail && !m_bImmediateFailSent 게이팅"
      pattern: "EnableCrossZDatumImmediateFail"
    - from: "BuildDatumShotResponse (z==0 즉시-F 분기)"
      to: "m_bImmediateFailSent = true"
      via: "z=0 F 송신 시 latch 세팅(중복-F 방지)"
      pattern: "m_bImmediateFailSent"
---

<objective>
GAP-3(우선순위 3, 지침 #6/#7): Datum 실패 "즉시 F"가 크로스-Z 에 미적용되던 것을 고치되, 프로토콜 활성화는 게이팅한다. m_bCycleDatumFailed 는 z==0 에서 딱 1회 산출되고 재평가 없다 — 크로스-Z Datum 의 실제 검출은 완성 index(Side는 1)에서 일어나므로 z=0 시점엔 항상 미실패. z=1 에서 실패해도 "즉시 F" 계약이 이행 안 된다(68-GAP-ANALYSIS.md GAP-3). GetDatumCompletionZIndex(68-09) 를 재사용해 완성 index 응답 생성 시점에 크로스-Z Datum 실패를 재평가하고, 신규 m_bImmediateFailSent latch 로 한 사이클 1회 F 를 보장한다(기존 z=0 즉시-F 분기에도 latch 세팅 — 지침 #6). ★"완성 index 즉시 F"는 Vision-Protocol-v1.0.md(Idx0 단일위치 Datum)와 충돌 + 제어팀(PLC) 협의 필요 사안이므로, SystemSetting 플래그(기본 OFF)로 게이팅하고 활성화는 사람 체크포인트로 결정한다(지침 #7).

Purpose: 크로스-Z Datum 즉시-F 코드는 준비하되, 프로토콜 문서 개정+PLC 합의 전까지 기본 OFF 로 안전 배포. 중복-F blocking 회귀 차단.
Output: 게이팅 플래그 + latch + 완성 index 재평가 + 프로토콜/PLC 협의 체크포인트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-GAP-ANALYSIS.md
@.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-04-SUMMARY.md
@.planning/refs/Vision-Protocol-v1.0.md
@.planning/refs/control-sequence-coding-guideline.md

<interfaces>
SystemSetting.cs 현재(2026-07-22):
- public bool UseProtocolV1 { get; set; } = false; [Category("Connection|Protocol")] (line 111-112) — 신규 플래그 배치/스타일 선례. bool 기본 false 는 INI 누락 키 → ToBool 기본 false 로 로드되므로 Load 오버라이드 불필요(int sentinel 과 다름).

InspectionSequence.cs 현재:
- private bool m_bCycleDatumFailed = false; (line 76) — z=0 에서 DetectDatumFailure()로 1회 산출(line 778). m_bCycleHasNG(line 75).
- private void ResetCycleState() (line 565-572, 68-06 수정 후) — 사이클 시작 accumulator 리셋. latch 초기화 자리.
- private bool DetectDatumFailure() (line 731-748): DatumConfigs 중 IsDatumFailed(datum.DatumName) 1건이라도면 true.
- public bool IsDatumFailed(string datumName) (line 1090 인근) — _failedDatums 조회.
- private TestResultPacket BuildDatumShotResponse() (line 703-727): z==0 응답. bImmediateFail = UseProtocolV1 && m_bCycleDatumFailed → IsBuffer=false; Result=NG(즉시 F, line 716-722). ← latch 세팅 추가 대상(지침 #6).
- private void AddResponseV1Cycle() (line 754-769): z==0 → HandleDatumIndexResponse; z>=1 → ComputeLastZIndex 산출 + BuildScopedResponse + PersistAndEnqueueV1.
- private TestResultPacket BuildScopedResponse(recipeManager, nZIndex, bIsLastIndex) (line 787-802): AggregateIndexFais + WarnIfEmptyScope + ApplyCycleJudgement. ← 완성 index 재평가 삽입 지점.
- private int GetDatumCompletionZIndex(DatumConfig datum) (68-09 신규) — max(ZIndexA,ZIndexB) 크로스-Z, else CROSS_Z_UNSET. 재사용.
- ApplyCycleJudgement(packet, bIsLastIndex, nMatchedShots) (line 972): bCycleFail = m_bCycleHasNG || m_bCycleDatumFailed || bEmptyLastScope.

동작(Side, Datum ZIndexA=0/ZIndexB=1, 완성 index=1):
- 현재: z=0 DetectDatumFailure → 아직 role A 만, 미검출 → 항상 false. z=1 검출 실패해도 m_bCycleDatumFailed 재평가 없음 → 즉시 F 미이행.
- 목표(플래그 ON): z=1(완성 index) 응답 생성 시점에 그 Datum 실패면 즉시 F. 플래그 OFF(기본): 현행대로 per-FAI 게이트 → 마지막 index 종합 F(D-07 회귀 0).
</interfaces>
</context>

<coding_guideline_scope>
D-09(LOCKED) 준수 대상: InspectionSequence.cs 의 신규/수정 코드 전부. SystemSetting.cs 는 순수 설정 프로퍼티(기존 파일 스타일 유지, D-09 대상 아님). 신규 상수(플래그 이름 등)는 문자열/enum, 매직넘버 금지.
</coding_guideline_scope>

<tasks>

<task type="auto">
  <name>Task 1: 게이팅 플래그 + immediate-F latch + 완성 index 재평가(게이팅, 기본 OFF)</name>
  <files>WPF_Example/Setting/SystemSetting.cs, WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - SystemSetting.cs line 108-137 (UseProtocolV1 플래그 배치 선례)
    - InspectionSequence.cs line 565-572 (ResetCycleState), 703-748 (BuildDatumShotResponse, DetectDatumFailure), 754-802 (AddResponseV1Cycle, BuildScopedResponse), 972-989 (ApplyCycleJudgement)
    - InspectionSequence.cs 68-09 신규 GetDatumCompletionZIndex
  </read_first>
  <action>
    (a) SystemSetting.cs: public bool EnableCrossZDatumImmediateFail { get; set; } = false; 을 [Category("Connection|Protocol")] 로 UseProtocolV1 인근에 추가. 주석: 크로스-Z Datum(2위치, 완성 index>=1) 실패 시 완성 index 에서 즉시 F 를 보낼지 게이팅. 기본 false — Vision-Protocol-v1.0.md 는 Datum(Idx0) 단일위치만 명시하므로 z>=1 F 를 PLC 가 올바르게 해석하는지 제어팀 합의 전까지 OFF. bool 기본 false → INI 누락 시 자동 OFF(Load 오버라이드 불필요).
    (b) InspectionSequence.cs latch: private bool m_bImmediateFailSent = false; 멤버 추가(m_bCycleDatumFailed 인근). ResetCycleState() 에 m_bImmediateFailSent = false; 추가(사이클 시작 초기화 — 다음 부품으로 누수 방지).
    (c) z==0 즉시-F 분기 latch 세팅(지침 #6 필수): BuildDatumShotResponse()(line 716-722)의 bImmediateFail true 분기(IsBuffer=false; Result=NG; return)에서 return 전에 m_bImmediateFailSent = true; 세팅. 이게 없으면 z=0 에서 F 나간 뒤 후속 index 완성 재평가가 또 F 를 보내 한 사이클 2회 F 가 나갈 수 있는 blocking 회귀.
    (d) 완성 index 재평가(게이팅): BuildScopedResponse(또는 AddResponseV1Cycle z>=1 경로)에 크로스-Z Datum 완성 index 즉시-F 판정을 추가 — private 헬퍼 TryApplyCrossZDatumImmediateFail(TestResultPacket packet, int nZIndex): bool bFlagOn = SystemHandler.Handle.Setting.EnableCrossZDatumImmediateFail && SystemHandler.Handle.Setting.UseProtocolV1; if (!bFlagOn) { return; } if (m_bImmediateFailSent) { return; } DatumConfigs 순회하며 bool bCompletesHere = GetDatumCompletionZIndex(datum) == nZIndex; bool bFailed = IsDatumFailed(datum.DatumName); if (bCompletesHere && bFailed) { packet.IsBuffer = false; packet.Result = EVisionResultType.NG; m_bImmediateFailSent = true; return; }. BuildScopedResponse 의 ApplyCycleJudgement 직후(packet.Result 확정 후)에 이 헬퍼를 호출해 완성 index F 로 덮어쓴다(게이팅). 함수 30줄 이하.
    회귀 경계: 플래그 OFF(기본) → TryApplyCrossZDatumImmediateFail 즉시 return → packet 무변경 → 현행 동작 100% 동일(크로스-Z Datum 실패는 per-FAI 게이트로 마지막 index 종합 F, D-07 회귀 0). z=0 latch 세팅은 플래그와 무관하게 항상(기존 UseProtocolV1 즉시-F 분기의 중복-F 방지). TOP/BOTTOM 은 크로스-Z Datum 없음 → 재평가 no-op. D-09 준수(헝가리언 bFlagOn/bCompletesHere/bFailed/m_b*, if/else, 매직넘버 없음).
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -t:Build -v:minimal 2>&1 | grep -E "Build succeeded|: error"</automated>
    <manual-check>grep 로 EnableCrossZDatumImmediateFail(SystemSetting.cs, 기본 false), m_bImmediateFailSent(ResetCycleState 초기화 + BuildDatumShotResponse z=0 분기 세팅 + 완성 index 재평가 세팅) 존재 확인. TryApplyCrossZDatumImmediateFail 최상단 플래그 OFF 시 즉시 return 확인.</manual-check>
  </verify>
  <done>빌드 PASS. 플래그 OFF(기본) 시 현행 동작 동일(D-07 회귀 0). 플래그 ON 시 완성 index 크로스-Z Datum 실패가 그 index 에서 F. z=0 즉시-F 분기에도 latch 세팅되어 중복-F 없음. latch 사이클 시작 초기화.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: 크로스-Z Datum 즉시-F 프로토콜 활성화 결정(제어팀/PLC 협의 게이트)</name>
  <action>Task 1이 게이팅 코드(EnableCrossZDatumImmediateFail, 기본 OFF)를 이미 준비했다. 이 체크포인트는 완성 index(z>=1)에서의 크로스-Z Datum "즉시 F" 프로토콜을 지금 활성화할지, 제어팀(PLC) 합의 전까지 기본 OFF 로 둘지 사람이 결정하는 관문이다. 코드 변경 없음 — 결정만. 미응답 시 keep-off 로 진행(프로토콜 협의 게이트, auto_advance 무시).</action>
  <decision>크로스-Z Datum(2위치, 완성 index z>=1) 실패의 "완성 index 즉시 F" 프로토콜을 활성화(EnableCrossZDatumImmediateFail=ON)할 것인가, 제어팀(PLC) 합의 전까지 기본 OFF 로 배포할 것인가?</decision>
  <context>
    Vision-Protocol-v1.0.md 는 "Datum(Index 0) 실패 시 즉시 F"만 명시하고 Index Table 예시(SIDE_1/SIDE_2)도 전부 Datum 단일위치(Idx0)로만 기록되어 있다. "2위치 Datum"(z=0,1 모두 Datum) 레이아웃 자체가 현재 프로토콜 문서에 없는 케이스이며, z=1(비-0 index)에서 F 를 보내는 것을 PLC 가 올바르게 해석하는지 코드/문서 어디에도 근거가 없다. 이건 엔지니어링 선택이 아니라 프로토콜 문서 개정 + 제어팀(PLC) 합의 사안이다(68-GAP-ANALYSIS.md GAP-3 경고). Task 1 이 코드+게이팅 플래그(기본 OFF)를 이미 준비했다.
  </context>
  <options>
    <option id="keep-off">
      <name>기본 OFF 유지(권고) — 제어팀 합의 전까지</name>
      <pros>프로토콜 문서/PLC 해석 위험 0. 크로스-Z Datum 실패는 여전히 per-FAI 게이트로 마지막 index 종합 F 에 반영되어 사이클 판정은 올바름(only fail-fast 만 미적용). D-07 회귀 0. 배포 안전.</pros>
      <cons>Datum 실패 시 PLC 가 완성 index 이후 index 를 계속 돌리는 낭비(fail-fast 미적용). 크로스-Z Datum 실패가 후속 참조 측정이 0건인 이론적 엣지에선 사이클 판정에 미반영될 수 있음.</cons>
    </option>
    <option id="enable-after-agreement">
      <name>제어팀 합의 후 ON</name>
      <pros>완전한 fail-fast(완성 index 에서 즉시 F). 프로토콜 의도 부합(합의 시).</pros>
      <cons>Vision-Protocol-v1.0.md 개정 + 김민우 선임(제어팀) 동기화 선행 필수(PROTO-06 성격). 합의 없이 ON 하면 PLC 오해석 위험.</cons>
    </option>
  </options>
  <resume-signal>keep-off (기본 OFF 유지) 또는 enable-after-agreement (제어팀 합의 완료 근거 제시 후 플래그 기본값 변경 요청) 중 선택. 미응답 시 keep-off 로 진행(auto_advance 무시 — 프로토콜 협의 게이트).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사이클 판정 → $RESULT F 송신 | z!=0 index 에서 F 송신을 PLC 가 올바르게 해석하는지 미검증(프로토콜 공백) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-11 | Repudiation (중복 F) | 완성 index 재평가 vs z=0 즉시-F | mitigate | m_bImmediateFailSent latch 를 z=0/완성 index 양 경로에 세팅 → 한 사이클 최대 1회 F(지침 #6) |
| T-68-12 | 프로토콜 오해석 (z!=0 F) | EnableCrossZDatumImmediateFail | transfer/mitigate | 기본 OFF + 사람 체크포인트로 게이팅 — PLC 합의 전 활성화 차단(지침 #7). 합의는 제어팀(PLC)로 이전 |
| T-68-13 | 상태 누수(latch) | m_bImmediateFailSent | mitigate | ResetCycleState(z=0)에서 초기화 — 다음 부품으로 latch 누수 없음 |
</threat_model>

<verification>
- msbuild Debug/x64 Build PASS (0 errors, 0 new warnings)
- EnableCrossZDatumImmediateFail 기본 false(OFF) — 미설정 INI 자동 OFF
- 플래그 OFF 시 packet 무변경(현행 동작, grep + 논리 검토)
- m_bImmediateFailSent: ResetCycleState 초기화 + z=0 즉시-F 분기 세팅 + 완성 index 재평가 세팅(3지점 grep 확인)
- 체크포인트: 프로토콜 활성화 결정 사람 승인(기본 keep-off)
</verification>

<success_criteria>
- 크로스-Z Datum 즉시-F 코드 준비됨(latch + 완성 index 재평가), 활성화는 게이팅(기본 OFF)
- 중복-F blocking 회귀 없음(z=0 분기 latch 세팅, 지침 #6)
- 플래그 OFF 시 D-07 회귀 0, TOP/BOTTOM 무영향
- 프로토콜/PLC 협의 체크포인트로 활성화 결정(지침 #7)
</success_criteria>

<output>
Create `.planning/phases/68-z-cross-z-dual-image-3-2-vision-protocol-v1-0-md-z1-z2-z-ind/68-10-SUMMARY.md` when done
</output>
