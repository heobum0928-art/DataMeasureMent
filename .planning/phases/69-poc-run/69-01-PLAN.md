---
phase: 69-poc-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/SequenceHandler.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: true
requirements: [MAINT-POC-01]

must_haves:
  truths:
    - "SIMUL_MODE(CameraRole=TopBottom)에서 TOP 시퀀스가 실행 중이어도 BOTTOM 시퀀스를 RUN 버튼으로 시작할 수 있다"
    - "같은 물리 카메라 객체를 공유하는 시퀀스끼리는 지금처럼 상호배타로 차단된다 (실HW TopBottom = CAM_TOP/CAM_BOTTOM 동일 MilCamera 인스턴스)"
    - "RUN 이 차단되면 에러 메시지가 어느 시퀀스 때문에 막혔는지 이름으로 알려준다"
    - "일괄검사(Btn_batchRun_Click)도 단일 RUN 과 동일한 시퀀스 단위 판정을 쓴다"
    - "SequenceHandler.StateAll/IsIdle 자체는 변경되지 않아 TCP/MainWindow/MainView 등 기존 호출부 동작이 그대로다"
    - "RUN 이 차단될 때 Trace 로그에 차단 대상/원인 시퀀스가 한 줄 기록된다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/SequenceHandler.cs"
      provides: "시퀀스 단위 RUN 차단 판정 API + 물리 카메라 공유 여부 참조 동일성 판정"
      contains: "TryGetBlockingSequence"
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "RUN 진입점 4곳을 시퀀스 단위 판정으로 교체 + 차단 사유 메시지"
      contains: "TryGetBlockingSequence"
  key_links:
    - from: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      to: "SequenceHandler.TryGetBlockingSequence"
      via: "Btn_start_Click / Btn_batchRun_Click 게이트 호출"
      pattern: "Sequences\\.TryGetBlockingSequence\\("
    - from: "WPF_Example/Custom/Sequence/SequenceHandler.cs"
      to: "DeviceHandler[deviceName]"
      via: "카메라 객체 참조 동일성(ReferenceEquals) 비교"
      pattern: "ReferenceEquals\\(listCamA"
---

<objective>
RUN 버튼(`btn_start` / `btn_batchRun`)이 "다른 시퀀스가 돌고 있다"는 이유만으로 간헐적으로 막히는 결함을 고친다.
원인은 `SequenceHandler.StateAll`/`IsIdle`(`WPF_Example/Sequence/SequenceHandler.cs:87-112`)이 **등록된 모든 시퀀스를 하나로 묶어** 판정하기 때문이다.

**Purpose:** 서로 다른 물리 카메라를 쓰는 시퀀스는 독립 실행되게 하되, **실제로 같은 물리 카메라 인스턴스를 공유하는 시퀀스끼리만** 지금처럼 상호배타를 유지해 하드웨어 안전성을 보존한다(D-01). 동시에 차단 시 원인 시퀀스를 메시지에 명시한다(D-03).

**Output:**
- `SequenceHandler` 에 시퀀스 단위 차단 판정 API 추가 (`TryGetBlockingSequence`)
- `InspectionListView` 의 RUN 게이트 4곳을 새 API/시퀀스 단위 상태 조회로 교체 + 사유 메시지

**D-02 는 코드 산출물이 없다 (결정 기록 전용):** 메인 화면 하단 "POC 자동화 임시 테스트" 패널(`MainView.xaml` ~583-600, `ManualZTriggerButton_Click`)은 **지금 그대로 유지**한다. 이 plan 은 `MainView.xaml` / `MainView.xaml.cs` 를 건드리지 않는다 (아래 anti-goal 검증 참조).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-poc-run/69-CONTEXT.md
@CLAUDE.md

<coding_rules>
프로젝트 상시 코딩 규칙 — 위반 시 리뷰 리젝트:
- **C# 7.2 전용.** switch expression / nullable reference types / record 금지. `out` 변수 인라인 선언(`out string s`)은 C# 7.0 기능이라 허용되지만, 이 plan 의 신규 코드는 **선언 후 전달** 스타일로 통일한다(기존 파일 가독성 우선).
- **삼항 연산자 `?:` 금지.** 반드시 `if / else` 로 쓴다.
- **헝가리언 접두사**를 신규 지역변수에 적용: `s`(string), `b`(bool), `e`(enum), `list`(List), `seq`(SequenceBase), `cam`(VirtualCamera), `n`/`i`(int).
- 함수 30~40줄 이내 유지, 신입 개발자가 읽어 이해 가능할 것.
- 주석은 **"왜"만** 최소로. `//YYMMDD hbk` 날짜 접두사는 2026-06-11 정책 전환으로 **더 이상 필수 아님** — 하지만 이 두 파일의 기존 주석이 전부 그 형식이므로, 신규 블록 주석은 파일 일관성을 위해 `//260805 hbk Phase 69 D-0X:` 형식을 따른다.
- 브레이스 스타일: 두 대상 파일 모두 K&R(여는 중괄호 같은 줄) — 그대로 따른다.
- 회귀 0: 이 plan 이 건드리지 않기로 한 호출부는 한 글자도 바꾸지 않는다.
</coding_rules>

<interfaces>
<!-- 실행자가 바로 쓸 수 있도록 코드베이스에서 추출한 실제 시그니처. 추가 탐색 불필요. -->

WPF_Example/Sequence/SequenceHandler.cs (framework partial — 이 plan 에서 수정 금지):
```csharp
public sealed partial class SequenceHandler : IDisposable {
    public static SequenceHandler Handle { get; }
    private readonly Dictionary<ESequence, SequenceBase> Sequences;   // 같은 partial class 라 Custom 파일에서 접근 가능
    public int Count { get; }                                          // = Sequences.Count
    public SequenceBase this[int index] { get; }                       // ElementAtOrDefault(index).Value
    public SequenceBase this[ESequence id] { get; }                    // 미등록이면 null
    public SequenceBase this[string name] { get; }
    public string StateSequenceName { get; }                           // = _StateSeqName (수정 금지)
    public EContextState StateAll { get; }                             // 전역 판정 (수정 금지)
    public bool IsIdle { get; }                                        // = StateAll == Idle (수정 금지)
    public EContextState GetSequenceState(ESequence id);               // 미등록이면 Idle 반환
    public EContextState GetSequenceState(string name);
}
```

WPF_Example/Custom/Sequence/SequenceHandler.cs (custom partial — Task 1 의 수정 대상):
```csharp
public sealed partial class SequenceHandler {
    public const string SEQ_TOP = "TOP";
    public const string SEQ_SIDE = "SIDE";
    public const string SEQ_BOTTOM = "BOTTOM";
    public static string ResolveSequenceName(ESequence seqId);          // Top->"TOP", Side->"SIDE", Bottom->"BOTTOM"
    private static bool IsSequenceActive(ESequence seqId);              // CameraRole 기반 등록 정책 (참고용, 재사용 X)
    public void RebuildInspectionActions(ESequence seqId);
}
// 파일 상단 using: System, System.Collections.Generic, System.IO, System.Linq,
//                 ReringProject.Define, ReringProject.Device, ReringProject.Setting, ReringProject.Utility
// namespace ReringProject.Sequence  → EContextState/SequenceBase/ActionBase/ICameraParam/CameraMasterParam 모두 using 불필요
```

WPF_Example/Sequence/Sequence/SequenceBase.cs:
```csharp
public ESequence ID { get; private set; }
public string Name { get; private set; }
public EContextState State { get; }        // Idle / Paused / Running / Finish / Error
public ParamBase Param { get; protected set; }
public int ActionCount { get; }
public ActionBase this[int index] { get; } // Actions[index]
```

WPF_Example/Sequence/Sequence/SequenceContext.cs:
```csharp
public enum EContextState { Idle, Paused, Running, Finish, Error }
```

WPF_Example/Sequence/Param/CameraParam.cs:
```csharp
public interface ICameraParam {
    string LightGroupName { get; }
    int LightLevel { get; }
    string DeviceName { get; }
    ...
}
```
`CameraSlaveParam : ParamBase, ICameraParam` / `ShotConfig : CameraSlaveParam, IOfflineImageParam`
`CameraMasterParam : ParamBase` — **ICameraParam 을 구현하지 않는다.** `public string DeviceName { get; set; }` 는 별도로 가짐.

WPF_Example/Device/DeviceHandler.cs:
```csharp
public sealed partial class DeviceHandler : IDisposable {
    public static DeviceHandler Handle { get; }
    public VirtualCamera this[string name] { get; }   // 미등록/null 이름이면 null
    public int Count { get; }
    public const string CAMERA_TOP = "CAM_TOP";
    public const string CAMERA_SIDE = "CAM_SIDE";
    public const string CAMERA_BOTTOM = "CAM_BOTTOM";
}
```
`SystemHandler.Handle.Devices` → `DeviceHandler` (WPF_Example/SystemHandler.cs:37)

**핵심 안전성 사실 (WPF_Example/Device/DeviceHandler.cs:221-247, `case ECameraType.MIL`):**
- `#if SIMUL_MODE` → `AddVirtualCamera(id)` 가 매 역할마다 **독립 VirtualCamera 인스턴스** 생성 → `Devices["CAM_TOP"] != Devices["CAM_BOTTOM"]`
- `#else`(실HW) → 이미 열린 MIL 카메라가 있으면 `sharedMil` 을 그대로 `Devices.Add(id.Identifier, sharedMil)` → `Devices["CAM_TOP"]` 와 `Devices["CAM_BOTTOM"]` 이 **완전히 같은 참조**
- `MilCamera.GrabHalconImage`/`MdigGrab` 경로에는 동시 접근 lock 이 **없다** → 공유 조합의 동시 grab 은 이미지 오염/MIL 에러 위험

**Quick 260805-jtj 가 이미 넣어둔 것(중복 구현 금지):** `MilCamera._roleInfoMap` / `RegisterRoleInfo(DeviceInfo)` / `ResolveRoleInfo(string)` (`WPF_Example/Device/Camera/Mil/MilCamera.cs:20,52,62`). 이건 **역할별 ReverseX/Y·Rotate 를 grab 시점에 되찾기 위한 것**이고, "어느 시퀀스가 어느 인스턴스를 공유하는가"는 알려주지 않는다. 이 plan 은 `_roleInfoMap` 을 건드리지 않고 `DeviceHandler` 인덱서가 돌려주는 **객체 참조 동일성**으로 판정한다.
</interfaces>

<current_call_sites>
`WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` 의 `IsIdle` 사용처 (총 7 + 1):

| 줄 | 코드 | 이 plan 에서 |
|---|---|---|
| 387 | `if (!SystemHandler.Handle.Sequences.IsIdle) {` (Btn_start_Click) | **교체** |
| 446 | `if (seqHandler.IsIdle) {` (ResolveRunnableAction 안의 rebuild 게이트) | **교체** |
| 539 | `if (!SystemHandler.Handle.Sequences.IsIdle) {` (Btn_batchRun_Click) | **교체** |
| 562 | `... && SystemHandler.Handle.Sequences.IsIdle) {` (batch rebuild 게이트) | **교체** |
| 963 | `button_grab_Click` — 라이브 grab | **그대로 둔다 (범위 밖)** |
| 968 | `button_grab_Click` — 라이브 grab | **그대로 둔다 (범위 밖)** |
| 983 | `button_grabInsp_Click` — 검사이미지 grab | **그대로 둔다 (범위 밖)** |
| 1138 | `button_light_Click` — 조명 제어 | **그대로 둔다 (범위 밖)** |

범위 밖 4곳은 "RUN" 이 아니라 UI 가 직접 카메라/조명을 점유하는 경로다. 시퀀스 단위로 완화하면 UI grab 과 검사 grab 이 같은 카메라를 동시에 건드릴 수 있어 이번 phase 에서는 보수적으로 전역 게이트를 유지한다.
</current_call_sites>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SequenceHandler 에 시퀀스 단위 RUN 차단 판정 API 추가 (D-01)</name>

  <files>WPF_Example/Custom/Sequence/SequenceHandler.cs</files>

  <read_first>
    - `WPF_Example/Custom/Sequence/SequenceHandler.cs` (전체 — 수정 대상. `IsSequenceActive`(L43-48) 의 "DeviceHandler 등록 정책과 1:1 동기화" 주석 패턴을 신규 메서드 주석에도 따른다)
    - `WPF_Example/Sequence/SequenceHandler.cs` L84-135 (`_StateSeqName` / `StateAll` / `IsIdle` / 인덱서 / `Count` — **읽기만 하고 절대 수정하지 않는다**)
    - `WPF_Example/Device/DeviceHandler.cs` L221-256 (`case ECameraType.MIL` sharedMil 공유 경로 + 인덱서 L258-264)
    - `WPF_Example/Device/Camera/Mil/MilCamera.cs` L15-70 (`_roleInfoMap` / `RegisterRoleInfo` / `ResolveRoleInfo` — 중복 구현하지 않기 위해 확인만)
    - `WPF_Example/Sequence/Param/CameraMasterParam.cs` L16-50 (`DeviceName` 프로퍼티)
    - `WPF_Example/Sequence/Param/CameraParam.cs` L15-30 (`ICameraParam.DeviceName`)
  </read_first>

  <action>
`WPF_Example/Custom/Sequence/SequenceHandler.cs` 의 `IsSequenceActive` 메서드(L43-48) **바로 아래**에 다음 4개 멤버를 추가한다. 기존 코드는 한 줄도 수정/삭제하지 않는다(순수 추가).

```csharp
        //260805 hbk Phase 69 D-01: RUN 게이트를 "전역 IsIdle" 에서 "시퀀스 단위 + 물리 카메라 공유 시에만 상호배타" 로 좁힌다.
        //  기존 StateAll/IsIdle 은 등록된 시퀀스 중 하나라도 non-Idle 이면 전체를 non-Idle 로 본다 → TOP 이 도는 중
        //  놀고 있는 BOTTOM 도 RUN 이 막혔다(간헐적 "RUN 미동작"의 실제 원인).
        //  단, 실 HW TopBottom 역할에서는 CAM_TOP/CAM_BOTTOM 이 같은 MilCamera 인스턴스를 공유하고
        //  (DeviceHandler.cs sharedMil 경로) grab 경로에 lock 이 없다 → 그 조합만은 반드시 상호배타로 남겨야 한다.
        //  판정 근거는 역할 이름 문자열이 아니라 DeviceHandler 가 돌려주는 실제 카메라 객체의 참조 동일성이다
        //  (디바이스 등록 구조가 바뀌어도 안전성이 깨지지 않도록).
        //  StateAll/IsIdle 자체는 건드리지 않는다 — TCP/MainWindow/MainView 호출부 회귀 0.
        /// <summary>
        /// 지정한 시퀀스를 지금 RUN 해도 되는지 판정한다.
        /// </summary>
        /// <param name="eTargetSeqId">실행하려는 시퀀스</param>
        /// <param name="sBlockingSeqName">차단된 경우 원인 시퀀스 이름. 실행 가능하면 null.</param>
        /// <returns>차단되어야 하면 true</returns>
        public bool TryGetBlockingSequence(ESequence eTargetSeqId, out string sBlockingSeqName) {
            sBlockingSeqName = FindBlockingSequenceName(eTargetSeqId);
            if (sBlockingSeqName == null) {
                return false;
            }
            //260805 hbk Phase 69: 차단 사실을 남겨야 "왜 안 눌렸는지" 사후 추적이 된다(사용자 클릭 빈도라 로그 폭주 없음).
            Logging.PrintLog((int)ELogType.Trace, "[RUN-GATE] blocked: target={0}, busy={1}",
                ResolveSequenceName(eTargetSeqId), sBlockingSeqName);
            return true;
        }

        //260805 hbk Phase 69 D-01: 차단 원인 시퀀스 이름을 찾는다. 실행 가능하면 null.
        private string FindBlockingSequenceName(ESequence eTargetSeqId) {
            SequenceBase seqTarget = this[eTargetSeqId];
            if (seqTarget == null) {
                //이 PC(CameraRole)에 등록되지 않은 시퀀스 — 실행 대상 자체가 없으므로 차단한다.
                return ResolveSequenceName(eTargetSeqId);
            }

            //1) 자기 자신이 Idle 이 아니면 무조건 차단 (기존 동작 유지. Finish/Error 도 non-Idle 로 본다)
            if (seqTarget.State != EContextState.Idle) {
                return seqTarget.Name;
            }

            //2) 물리 카메라를 실제로 공유하는 다른 시퀀스가 non-Idle 이면 차단
            for (int i = 0; i < Count; i++) {
                SequenceBase seqOther = this[i];
                if (seqOther == null) {
                    continue;
                }
                if (ReferenceEquals(seqOther, seqTarget)) {
                    continue;
                }
                if (seqOther.State == EContextState.Idle) {
                    continue;
                }
                if (SharesCameraDevice(seqTarget, seqOther) == false) {
                    continue;
                }
                return seqOther.Name;
            }

            return null;
        }

        //260805 hbk Phase 69 D-01: 두 시퀀스가 같은 물리 카메라 객체를 쓰는지 참조 동일성으로 판정한다.
        //  해석 불가(이름 공백/미등록/DeviceHandler null)는 "공유한다"로 간주 — 안전한 쪽(fail-closed)으로만 틀리게 한다.
        private bool SharesCameraDevice(SequenceBase seqA, SequenceBase seqB) {
            List<VirtualCamera> listCamA = new List<VirtualCamera>();
            List<VirtualCamera> listCamB = new List<VirtualCamera>();

            if (TryCollectSequenceCameras(seqA, listCamA) == false) {
                return true;
            }
            if (TryCollectSequenceCameras(seqB, listCamB) == false) {
                return true;
            }

            for (int i = 0; i < listCamA.Count; i++) {
                for (int j = 0; j < listCamB.Count; j++) {
                    if (ReferenceEquals(listCamA[i], listCamB[j])) {
                        return true;
                    }
                }
            }
            return false;
        }

        //260805 hbk Phase 69 D-01: 시퀀스가 grab 에 쓰는 카메라 객체를 모은다.
        //  마스터 파라미터(CameraMasterParam.DeviceName) + 각 Action 의 ICameraParam.DeviceName 을 모두 본다 —
        //  자식 Action 이 마스터와 다른 DeviceName 을 가질 수 있기 때문(SequenceBase.cs L109-110 상속은 빈 값일 때만).
        //  하나라도 해석 실패하면 false 를 돌려 호출부가 fail-closed 로 처리하게 한다.
        private bool TryCollectSequenceCameras(SequenceBase seq, List<VirtualCamera> listOut) {
            if (seq == null) {
                return false;
            }
            DeviceHandler devs = SystemHandler.Handle.Devices;
            if (devs == null) {
                return false;
            }

            List<string> listNames = new List<string>();

            CameraMasterParam masterParam = seq.Param as CameraMasterParam;
            if (masterParam != null) {
                listNames.Add(masterParam.DeviceName);
            }
            for (int i = 0; i < seq.ActionCount; i++) {
                ActionBase act = seq[i];
                if (act == null) {
                    continue;
                }
                ICameraParam camParam = act.Param as ICameraParam;
                if (camParam == null) {
                    continue;
                }
                listNames.Add(camParam.DeviceName);
            }

            for (int i = 0; i < listNames.Count; i++) {
                string sName = listNames[i];
                if (string.IsNullOrEmpty(sName)) {
                    //빈 이름은 마스터에서 상속받는 자식 — 마스터 항목으로 이미 커버된다.
                    continue;
                }
                VirtualCamera cam = devs[sName];
                if (cam == null) {
                    //등록되지 않은 디바이스 이름 — 어느 물리 카메라인지 알 수 없으므로 해석 실패로 처리.
                    return false;
                }
                if (listOut.Contains(cam) == false) {
                    listOut.Add(cam);
                }
            }

            //하나도 못 모았으면 판정 근거가 없다 → 해석 실패.
            return listOut.Count > 0;
        }
```

**추가 using 은 필요 없다.** 이 파일은 이미 `System.Collections.Generic`, `ReringProject.Define`(ELogType), `ReringProject.Device`(DeviceHandler/VirtualCamera), `ReringProject.Utility`(Logging) 를 import 하고 있고, `namespace ReringProject.Sequence` 안이라 `SequenceBase`/`ActionBase`/`EContextState`/`ICameraParam`/`CameraMasterParam` 은 그대로 보인다. 빌드 에러가 나면 using 을 추가하지 말고 **먼저 오타/시그니처를 의심**할 것.

**D-03 설계 노트 (CONTEXT.md 의 "_StateSeqName 재사용" 제안 대비 조정 근거):**
CONTEXT.md D-03 은 `SequenceHandler._StateSeqName` 재사용을 "우선 검토"하라고 했다. 검토 결과 **재사용하지 않는다** — `_StateSeqName` 은 전역 `StateAll` getter 의 부수효과 필드라, 시퀀스 단위로 좁혀진 새 판정에서는 **엉뚱한 시퀀스 이름**(대상과 카메라를 공유하지 않는 시퀀스)을 돌려줄 수 있다. 대신 `TryGetBlockingSequence` 가 원인 이름을 직접 반환해 D-03 의 의도(어느 시퀀스가 바쁜지 알려주기)를 더 정확히 충족한다. `_StateSeqName`/`StateSequenceName`/`StateAll`/`IsIdle` 은 **수정하지 않는다.**
  </action>

  <acceptance_criteria>
    - `rg -c "public bool TryGetBlockingSequence\(ESequence eTargetSeqId, out string sBlockingSeqName\)" WPF_Example/Custom/Sequence/SequenceHandler.cs` → `1`
    - `rg -c "private string FindBlockingSequenceName\(ESequence eTargetSeqId\)" WPF_Example/Custom/Sequence/SequenceHandler.cs` → `1`
    - `rg -c "private bool SharesCameraDevice\(SequenceBase seqA, SequenceBase seqB\)" WPF_Example/Custom/Sequence/SequenceHandler.cs` → `1`
    - `rg -c "private bool TryCollectSequenceCameras\(SequenceBase seq, List<VirtualCamera> listOut\)" WPF_Example/Custom/Sequence/SequenceHandler.cs` → `1`
    - `rg -c "ReferenceEquals\(listCamA\[i\], listCamB\[j\]\)" WPF_Example/Custom/Sequence/SequenceHandler.cs` → `1` (역할 이름 문자열 매칭이 아니라 객체 참조 비교임을 입증)
    - `rg -c "\[RUN-GATE\] blocked: target=\{0\}, busy=\{1\}" WPF_Example/Custom/Sequence/SequenceHandler.cs` → `1`
    - fail-closed 3곳 존재: `rg -c "return true;" WPF_Example/Custom/Sequence/SequenceHandler.cs` 결과 중 `SharesCameraDevice` 블록 안에 `TryCollectSequenceCameras(...) == false` 직후 `return true;` 가 2회 존재 (`rg -A2 "TryCollectSequenceCameras\(seq[AB], listCam[AB]\) == false"` 로 확인)
    - **anti-goal:** `git diff --stat HEAD -- WPF_Example/Sequence/SequenceHandler.cs` → 출력 없음 (framework StateAll/IsIdle 무수정)
    - **anti-goal:** `git diff --stat HEAD -- WPF_Example/Device/Camera/Mil/MilCamera.cs WPF_Example/Device/DeviceHandler.cs` → 출력 없음 (quick 260805-jtj 영역 무수정)
    - **anti-goal:** `rg -c "\?.*:" 로 신규 블록에 삼항 연산자 없음` — `rg -n "IsSequenceActive" -A 200 WPF_Example/Custom/Sequence/SequenceHandler.cs | rg "\s\?\s.*\s:\s"` → 출력 없음
    - 빌드 통과 (아래 verify)
  </acceptance_criteria>

  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal</automated>
  </verify>

  <done>
    `SequenceHandler.TryGetBlockingSequence(ESequence, out string)` 가 존재하고, 카메라 공유 판정이 `DeviceHandler` 인덱서 반환 객체의 `ReferenceEquals` 로만 이뤄지며, 해석 실패 시 fail-closed(차단)로 동작한다. `WPF_Example/Sequence/SequenceHandler.cs`, `DeviceHandler.cs`, `MilCamera.cs` 는 무수정. msbuild Debug/x64 0 errors / 0 new warnings.
  </done>
</task>

<task type="auto">
  <name>Task 2: RUN 진입점 4곳을 시퀀스 단위 판정으로 교체 + 차단 사유 메시지 (D-01/D-03)</name>

  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>

  <read_first>
    - `WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` L360-600 (`Btn_start_Click` / `ResolveRunnableAction` / `Btn_batchRun_Click` — 수정 대상 4곳 전부 이 구간에 있다)
    - `WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` L1-40 (using 목록 — `ReringProject.Sequence` import 여부 확인. `EContextState` 사용에 필요)
    - `WPF_Example/Custom/Sequence/SequenceHandler.cs` (Task 1 에서 추가한 `TryGetBlockingSequence` 시그니처 확인)
  </read_first>

  <action>
**(A) `Btn_start_Click` — 현재 L387-390**

기존:
```csharp
            if (!SystemHandler.Handle.Sequences.IsIdle) {
                CustomMessageBox.Show("Error", "Sequence is already running.", MessageBoxImage.Error);
                return;
            }
```
로 교체:
```csharp
            //260805 hbk Phase 69 D-01/D-03: 전역 IsIdle 이 아니라 "이 시퀀스 + 실제 같은 물리 카메라를 쓰는 시퀀스" 만 본다.
            //  다른 물리 카메라를 쓰는 시퀀스가 돌고 있어도 이 시퀀스는 독립 실행 가능(전역 IsIdle 이 막던 결함).
            string sBlockingSeqName;
            if (SystemHandler.Handle.Sequences.TryGetBlockingSequence(seqID, out sBlockingSeqName)) {
                CustomMessageBox.Show("Error",
                    string.Format(
                        "실행할 수 없습니다 — '{0}' 시퀀스가 아직 Idle 이 아닙니다.\n(자기 자신이거나, 같은 물리 카메라를 공유하는 시퀀스입니다.)",
                        sBlockingSeqName),
                    MessageBoxImage.Error);
                return;
            }
```
(이 블록은 `SequenceBase seq = SystemHandler.Handle.Sequences[seqID];` null 체크 **뒤**, `ResolveRunnableAction` 호출 **앞** 이라는 현재 위치를 그대로 유지한다.)

**(B) `ResolveRunnableAction` 안의 rebuild 게이트 — 현재 L446**

기존:
```csharp
                if (seqHandler.IsIdle) {
```
로 교체:
```csharp
                //260805 hbk Phase 69 D-01: rebuild 는 이 시퀀스의 Actions[] 만 재생성한다(SequenceHandler.RebuildInspectionActions).
                //  다른 시퀀스가 돌고 있는지는 무관 — 대상 시퀀스만 Idle 이면 안전하다.
                if (seqHandler.GetSequenceState(seq.ID) == EContextState.Idle) {
```

**(C) `Btn_batchRun_Click` — 현재 L539-542**

기존:
```csharp
            if (!SystemHandler.Handle.Sequences.IsIdle) {
                CustomMessageBox.Show("일괄 검사", "시퀀스가 이미 실행 중입니다.", MessageBoxImage.Error);
                return;
            }
```
로 교체:
```csharp
            //260805 hbk Phase 69 D-01/D-03: 단일 RUN(Btn_start_Click)과 동일한 시퀀스 단위 판정을 쓴다.
            string sBlockingSeqName;
            if (SystemHandler.Handle.Sequences.TryGetBlockingSequence(seqID, out sBlockingSeqName)) {
                CustomMessageBox.Show("일괄 검사",
                    string.Format(
                        "실행할 수 없습니다 — '{0}' 시퀀스가 아직 Idle 이 아닙니다.\n(자기 자신이거나, 같은 물리 카메라를 공유하는 시퀀스입니다.)",
                        sBlockingSeqName),
                    MessageBoxImage.Error);
                return;
            }
```

**(D) batch rebuild 게이트 — 현재 L562**

기존:
```csharp
            if (resolvedPairs.Any(p => p.Item2 < 0) && SystemHandler.Handle.Sequences.IsIdle) {
```
로 교체:
```csharp
            //260805 hbk Phase 69 D-01: (B)와 같은 이유 — 대상 시퀀스만 Idle 이면 rebuild 안전.
            if (resolvedPairs.Any(p => p.Item2 < 0)
                && SystemHandler.Handle.Sequences.GetSequenceState(seqID) == EContextState.Idle) {
```

**(E) 범위 밖 4곳은 절대 건드리지 않는다:** L963 / L968 / L983 / L1138 (`button_grab_Click`, `button_grabInsp_Click`, `button_light_Click`). RUN 경로가 아니라 UI 가 직접 카메라·조명을 점유하는 경로라, 시퀀스 단위로 완화하면 검사 grab 과 UI grab 이 같은 카메라를 동시에 건드릴 수 있다. 보수적으로 전역 `IsIdle` 을 유지한다.

**using 확인:** `EContextState` 는 `ReringProject.Sequence` 네임스페이스다. 파일 상단에 `using ReringProject.Sequence;` 가 이미 있는지 확인하고, 없으면 그때만 추가한다(파일이 `SequenceBase`/`seq.State` 를 이미 쓰고 있으므로 있을 가능성이 높다).
  </action>

  <acceptance_criteria>
    - `rg -c "Sequences\.IsIdle" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` → `4` (교체 전 7 → 범위 밖 L963/968/983/1138 만 남음)
    - `rg -c "seqHandler\.IsIdle" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` → 매치 없음 (exit code 1)
    - `rg -c "Sequence is already running" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` → 매치 없음 (exit code 1)
    - `rg -c "시퀀스가 이미 실행 중입니다" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` → 매치 없음 (exit code 1)
    - `rg -c "Sequences\.TryGetBlockingSequence\(seqID, out sBlockingSeqName\)" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` → `2` (단일 RUN + 일괄검사)
    - `rg -c "실행할 수 없습니다 — '\{0\}' 시퀀스가 아직 Idle 이 아닙니다" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` → `2` (D-03: 두 메시지 모두 시퀀스 이름 포함)
    - `rg -c "GetSequenceState\(seq\.ID\) == EContextState\.Idle" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` → `1`
    - `rg -c "GetSequenceState\(seqID\) == EContextState\.Idle" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` → `1`
    - `rg -n "button_grab_Click|button_grabInsp_Click|button_light_Click" -A 12 WPF_Example/UI/ControlItem/InspectionListView.xaml.cs | rg -c "Sequences\.IsIdle"` → `4` (범위 밖 4곳 원형 보존)
    - **anti-goal (D-02):** `git diff --stat HEAD -- WPF_Example/UI/ContentItem/MainView.xaml WPF_Example/UI/ContentItem/MainView.xaml.cs` → 출력 없음 (POC 패널 무수정)
    - **anti-goal:** `git diff -U0 HEAD -- WPF_Example/UI/ControlItem/InspectionListView.xaml.cs | rg "^\+" | rg "\s\?\s.*\s:\s"` → 출력 없음 (신규 삼항 없음)
    - 빌드 통과 (아래 verify)
  </acceptance_criteria>

  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal</automated>
  </verify>

  <done>
    RUN 진입점 4곳(L387/446/539/562)이 시퀀스 단위 판정을 쓰고, 차단 메시지 2곳 모두 원인 시퀀스 이름을 표시한다. 범위 밖 4곳(963/968/983/1138)과 `MainView.xaml*`(D-02)은 무수정. msbuild Debug/x64 0 errors / 0 new warnings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 운영자(UI) → SequenceHandler RUN 게이트 | 사람이 임의 타이밍/연타로 RUN 을 누른다. 게이트가 유일한 동시실행 1차 방어선이다. |
| SequenceHandler → 물리 카메라(MIL 보드) | 실HW TopBottom 에서 CAM_TOP/CAM_BOTTOM 이 **같은 MilCamera 인스턴스**를 가리키고, `MdigGrab` 경로에 lock 이 없다. 동시 진입 = 하드웨어 손상 없는 대신 **이미지 오염 / MIL 에러 / 앱 크래시**. |
| TCP 호스트($TEST/$PREP) → SequenceHandler.Start | 이번 phase 에서 경로 변경 없음. 단, UI RUN 완화가 TCP 트리거와 겹칠 수 있다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Tampering | `SharesCameraDevice` / `TryCollectSequenceCameras` (Custom/Sequence/SequenceHandler.cs) | mitigate | **이 phase 가 존재하는 이유인 핵심 위험**: 공유 판정이 틀려 두 시퀀스가 같은 물리 MilCamera 에 동시 grab → 버퍼 오염된 이미지로 측정/판정. 3중 방어: (1) 판정을 역할 이름 문자열이 아니라 `DeviceHandler[name]` 이 돌려주는 **객체 참조 동일성**(`ReferenceEquals`)으로만 수행 → `sharedMil` 재사용 경로를 구조적으로 정확히 포착. (2) **fail-closed**: DeviceHandler null / DeviceName 공백뿐인 시퀀스 / 미등록 디바이스 이름 → `TryCollectSequenceCameras` 가 false 반환 → `SharesCameraDevice` 가 `true`(공유로 간주) → 차단. 즉 판정 불가는 항상 "현재와 동일한 보수적 차단"으로 수렴한다. (3) 마스터 파라미터뿐 아니라 **각 Action 의 `ICameraParam.DeviceName` 도 전부** 수집해, 자식 Action 이 마스터와 다른 카메라를 쓰는 경우도 놓치지 않는다. |
| T-69-02 | Denial of Service | `MilCamera.GrabHalconImage` / `MdigGrab` (동시 진입) | mitigate | 위 T-69-01 의 fail-closed 로 1차 차단. 2차 방어로 **기존 `SequenceBase.StartCore` 의 `_startLock` + `State != Idle` 원자 점유(SequenceBase.cs L334-350)를 그대로 남긴다** — 게이트를 뚫어도 같은 시퀀스의 이중 시작은 여전히 불가. 3차로 대상 시퀀스 자기 자신이 non-Idle 이면 카메라 공유 여부와 무관하게 항상 차단(`FindBlockingSequenceName` 1)단계). |
| T-69-03 | Denial of Service | 기존 전역 `IsIdle` 사용처(TCP `$TEST` / MainWindow / MainView 라이브뷰) | mitigate | `StateAll`/`IsIdle`/`_StateSeqName` 을 **전혀 수정하지 않는다**(새 메서드만 추가). InspectionListView 의 범위 밖 4곳(라이브 grab / 검사이미지 grab / 조명)도 전역 게이트 유지 → UI 직접 카메라 점유와 검사 grab 이 겹칠 여지를 늘리지 않는다. anti-goal 로 `git diff --stat` 검증. |
| T-69-04 | Information Disclosure | 차단 메시지 / `[RUN-GATE]` Trace 로그 | accept | 노출되는 값은 내부 시퀀스 이름(`TOP`/`SIDE`/`BOTTOM`)뿐이다. 비밀·PII 아님, 로컬 산업용 PC(네트워크 DB/클라우드 없음), 오히려 운영자 진단에 필수. |
| T-69-05 | Repudiation | RUN 차단 이벤트 | mitigate | 차단 시 `Logging.PrintLog((int)ELogType.Trace, "[RUN-GATE] blocked: target={0}, busy={1}", ...)` 로 1줄 기록 → "RUN 이 안 눌렸다" 신고가 들어와도 어느 시퀀스가 원인이었는지 사후 재구성 가능. 사람 클릭 빈도라 로그 폭주 없음. |
| T-69-06 | Spoofing | — | accept | 로컬 WPF UI 단일 사용자 세션. 인증 경계(LoginManager)는 이 게이트 이전 단계이며 이 phase 가 바꾸지 않는다. |
| T-69-07 | Elevation of Privilege | — | accept | 새 API 는 `SequenceHandler` 인스턴스 메서드이고 어떤 권한/역할 검사도 우회하지 않는다. 실행 권한 자체는 기존 `SequenceBase.Start` 경로가 그대로 판단한다. |
</threat_model>

<verification>
1. `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal` → 0 errors / 0 new warnings
2. `rg -c "Sequences\.IsIdle" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` → `4`
3. `rg -c "Sequences\.TryGetBlockingSequence" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` → `2`
4. `git diff --stat HEAD -- WPF_Example/Sequence/SequenceHandler.cs WPF_Example/Device/DeviceHandler.cs WPF_Example/Device/Camera/Mil/MilCamera.cs WPF_Example/UI/ContentItem/MainView.xaml WPF_Example/UI/ContentItem/MainView.xaml.cs` → 출력 없음
5. `git diff --name-only HEAD` → `WPF_Example/Custom/Sequence/SequenceHandler.cs` 와 `WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` 두 개만 (사전 존재하던 `WPF_Example/DatumMeasurement.csproj` 변경 제외)
</verification>

<success_criteria>
- `SequenceHandler.TryGetBlockingSequence(ESequence, out string)` 존재, 카메라 공유 판정이 `ReferenceEquals` 기반
- 해석 실패 = 차단(fail-closed) 경로 2곳 존재
- RUN 진입점 4곳 교체 완료, 차단 메시지 2곳 모두 원인 시퀀스 이름 포함 (D-03)
- `StateAll`/`IsIdle`/`_StateSeqName` 및 `MainView.xaml*`(D-02) 무수정
- msbuild Debug/x64 빌드 PASS
</success_criteria>

<output>
완료 후 `.planning/phases/69-poc-run/69-01-SUMMARY.md` 를 생성한다.
SUMMARY 에 반드시 포함할 것:
- 추가된 4개 멤버의 최종 시그니처
- fail-closed 로 처리되는 정확한 조건 목록
- 범위 밖으로 남긴 `IsIdle` 4곳의 줄 번호와 남긴 이유
- 실HW(TopBottom, MIL 공유) 상호배타는 SIMUL 에서 검증 불가 → 69-02 에서 기록만 한다는 사실
</output>
