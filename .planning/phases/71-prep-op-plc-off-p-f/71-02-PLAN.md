---
phase: 71-prep-op-plc-off-p-f
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
autonomous: true
requirements: [PROTO-PREP-01]

must_haves:
  truths:
    - "사이클이 마지막 z_index 에서 P(OK)로 확정되는 순간 전 조명 채널이 자동 소등된다"
    - "사이클이 NG 누적으로 F 확정되는 순간에도 전 조명 채널이 자동 소등된다"
    - "Index 0 Datum 검출 즉시실패로 사이클이 조기 F 종료되는 경로에서도 전 조명 채널이 자동 소등된다 (BuildScopedResponse 를 안 거치는 별도 경로)"
    - "크로스-Z Datum 실패로 완성 index 에서 조기 F 확정되는 경로에서도 소등된다"
    - "중간 z_index 의 B(buffer) 응답에서는 소등되지 않는다 — 다음 촬영이 남아있으므로"
    - "소등이 실제로 일어났는지 [CycleLightOff] 로그 한 줄로 사후 추적 가능하고, 3개 종료 경로가 로그에서 서로 구분된다"
    - "v2.6 레거시 경로(UseProtocolV1=false)는 전혀 영향받지 않는다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "사이클 P/F 확정 시 자동 소등 단일 헬퍼 + 2개 종료 경로 훅"
      contains: "TryTurnOffLightsOnCycleEnd"
  key_links:
    - from: "BuildScopedResponse"
      to: "TryTurnOffLightsOnCycleEnd"
      via: "ApplyCycleJudgement + TryApplyCrossZDatumImmediateFail 두 호출이 끝난 뒤 IsBuffer==false 단일 게이트"
      pattern: "TryTurnOffLightsOnCycleEnd\\(packet, \"scoped\", nZIndex\\);"
    - from: "HandleDatumIndexResponse"
      to: "TryTurnOffLightsOnCycleEnd"
      via: "BuildDatumShotResponse 반환 직후 (Index 0 즉시-F 별도 종료 경로)"
      pattern: "TryTurnOffLightsOnCycleEnd\\(datumPacket, \"datum-index0\", DATUM_Z_INDEX\\);"
    - from: "TryTurnOffLightsOnCycleEnd"
      to: "TurnOffShotLights"
      via: "직접 호출 (같은 클래스, 이미 public)"
      pattern: "TurnOffShotLights\\(\\);"
---

<objective>
사이클이 **P 또는 F 로 확정되는 순간** 비전이 스스로 전 조명 채널을 소등하도록 만든다. PLC 가 `$PREP` 로 명시적 OFF 를 요청하던 역할을 비전 내부 이벤트로 대체한다.

**Purpose:** 71-01 이 `$PREP` 에서 `Op` 필드를 없앴으므로 소등 트리거가 사라졌다. 소등의 실제 목적(측정 정확도와 무관, **LED 수명/안전**)은 "사이클 완전 종료 후 전부 끄기" 하나뿐이고, 그 시점을 아는 주체는 PLC 가 아니라 **판정을 내리는 비전 자신**이다.

**Output:**
- `TryTurnOffLightsOnCycleEnd(packet, szPath, nZIndex)` 헬퍼 신설 — `IsBuffer == false` 단일 게이트 + `[CycleLightOff]` 추적 로그
- 종료 경로 **2곳**에 훅 배선:
  1. `BuildScopedResponse` — 정상 마지막-index P/F **및** 크로스-Z Datum 즉시 F (두 판정 함수 호출이 끝난 뒤 한 번만 체크)
  2. `HandleDatumIndexResponse` — Index 0 Datum 즉시 F (**`BuildScopedResponse` 를 거치지 않는 별도 경로**)

**이 plan 의 스코프 밖 (건드리지 말 것):**
- `$PREP` wire 포맷/파서/ACK = **71-01** 담당. 이 plan 은 `TcpServer/` 와 `Custom/SystemHandler.cs` 를 한 글자도 수정하지 않는다.
- `TurnOffShotLights()` 본문(702-709) — **재사용만 하고 내부는 무수정**(CONTEXT locked).
- `ApplyShotLightsInternal` / `ApplyShotLights` / 조명 안정화 대기(ACK 타이밍) 로직 — 전부 무수정.
- `ApplyCycleJudgement` / `TryApplyCrossZDatumImmediateFail` / `BuildDatumShotResponse` **세 함수의 내부** — 훅은 이 함수들 **바깥**에 두어 판정 로직을 오염시키지 않는다(CONTEXT 권장 설계).
- v2.6 레거시 경로(`AddResponse` 의 `UseProtocolV1==false` 폴백 블록, 129줄 이하) — 무수정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-prep-op-plc-off-p-f/71-CONTEXT.md
@.planning/phases/71-prep-op-plc-off-p-f/71-PATTERNS.md
@CLAUDE.md

<coding_rules>
프로젝트 상시 코딩 규칙 — 위반 시 리뷰 리젝트:
- **C# 7.2 전용.** switch expression / nullable reference types / record 금지.
- **삼항 연산자 `?:` 절대 금지.** 반드시 `if / else`.
- **헝가리언 접두사**: `b`(bool), `n`(int), `sz`(string). 새 지역/파라미터 변수에 반드시 적용.
- 이 파일은 **Allman 브레이스**(여는 중괄호 새 줄)다 — 신규 메서드도 Allman.
- 함수 30~40줄 이내, 신입 개발자가 읽어 이해 가능할 것.
- 주석은 **"왜"만**. 이 파일의 기존 주석이 전부 `//YYMMDD hbk Phase NN:` 형식이므로 파일 일관성을 위해 신규 블록도 `//260806 hbk Phase 71:` 형식을 따른다.
- 새 `try/catch` 를 도입하지 않는다 — 세 훅 대상 함수 모두 원래 try/catch 가 없고, `TurnOffShotLights` 내부의 `LightHandler.SetOnOff` 는 기존 `ApplyShotLightsInternal` 경로에서도 보호 없이 호출되고 있다(동일 위험도 유지 = 회귀 0).
- **회귀 0**: 건드리지 않기로 한 함수/줄은 한 글자도 바꾸지 않는다. 리포맷 금지.
</coding_rules>

<repo_state_warning>
작업 시작 시점에 이미 uncommitted 상태인 파일 2개(`WPF_Example/Halcon/Algorithms/PatternMatchService.cs`, `WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs`)는 **사용자가 의도적으로 진행 중인 실험**이다. 사용자가 "우선 냅둬"라고 명시했다.
**절대 `git checkout` / `git stash` / `git restore` 하지 말 것.** 커밋 시 `git add` 는 이 plan 의 대상 파일 1개만 명시적으로 지정한다.
</repo_state_warning>

<interfaces>
<!-- 라이브 코드베이스에서 추출한 실제 현재 상태(2026-08-06 재검증). -->
<!-- 라인 번호는 편집 전 기준이며 드리프트 가능 — 편집 시작 전 Grep 으로 반드시 재확인할 것. -->

**사이클 종료 경로 전체 구조 (이걸 먼저 이해해야 훅 위치가 납득된다):**
```
AddResponse()                                   :119   ← UseProtocolV1 게이트
  └ bUseV1Cycle == false → v2.6 레거시 블록      :129   ← 무수정, 훅 없음
  └ bUseV1Cycle == true  → AddResponseV1Cycle() :126
        │
        ├ z == 0 (Datum 샷)
        │   └ HandleDatumIndexResponse()        :1390
        │        ├ ResetCycleState() / DetectDatumFailure()
        │        ├ BuildDatumShotResponse()     :1396 호출 → :1318 정의
        │        │     · 즉시F: IsBuffer=false, Result=NG, m_bImmediateFailSent=true   ← 종료 경로 (2)
        │        │     · 정상 : IsBuffer=true,  Result=OK                              ← 종료 아님
        │        └ PersistAndEnqueueV1()        :1397
        │        ★★ BuildScopedResponse 를 절대 거치지 않는다 — 별도 훅 필수 ★★
        │
        └ z >= 1 (측정 샷)
            └ BuildScopedResponse()             :1384 호출 → :1404 정의
                 ├ AggregateIndexFais()
                 ├ WarnIfEmptyScope()
                 ├ ApplyCycleJudgement()        :1416 호출 → :1657 정의
                 │     · 중간 index : IsBuffer=true,  Result=OK        ← 종료 아님(B)
                 │     · 마지막 index: IsBuffer=false, Result=OK|NG    ← 종료 경로 (1)
                 ├ TryApplyCrossZDatumImmediateFail() :1417 호출 → :1459 정의
                 │     · 크로스-Z Datum 실패: IsBuffer=false, Result=NG ← 종료 경로 (3)
                 ├ pMyContext.ResultInfo = packet.Result;   :1418
                 └ return packet;                          :1419
```
→ **종료 경로 (1)/(3) 은 `BuildScopedResponse` 안의 단일 게이트 하나로 동시에 커버**되고,
   **종료 경로 (2) 는 `HandleDatumIndexResponse` 에 두 번째 훅이 반드시 필요**하다.

**재사용 대상 (무수정, InspectionSequence.cs:700-709):**
```csharp
        //260626 hbk v3.0: $PREP Op==0(사이클 종료) → 전 조명 그룹 소등. ApplyShotLightsInternal 의 4그룹 OFF.
        //  $LIGHT OFF 명령 폐기 대체. HW 트리거 전환 시에도 $PREP 가 OFF 담당.
        public void TurnOffShotLights()
        {
            LightHandler.Handle.SetOnOff(LightHandler.LIGHT_RING, false);
            LightHandler.Handle.SetOnOff(LightHandler.LIGHT_BACK, false);
            LightHandler.Handle.SetOnOff(LightHandler.LIGHT_ALIGN_COAX, false);
            LightHandler.Handle.SetOnOff(LightHandler.LIGHT_BAR, false);
            LightHandler.Handle.SetOnOff(LightHandler.LIGHT_RING7, false);   //260626 hbk Phase 66: Ring7 소등 정합
        }
```
같은 클래스 안이므로 그냥 `TurnOffShotLights();` 로 호출하면 된다. 접근제한자 변경 불필요.

**이 파일에서 이미 쓰고 있는 로깅 형태 (InspectionSequence.cs:684-685) — 신규 로그는 이 형태를 따른다:**
```csharp
                Logging.PrintLog((int)ELogType.LightController,
                    "[PREP] Shot not found for ZIndex={0}, Seq={1} //260625 hbk Phase 64", nZIndex, Name);
```
`Logging.PrintLog(int logID, string format, params object[] args)` 오버로드가 존재한다(`Utility/Logging.cs:261`).
`ELogType.LightController` 를 쓰면 기존 `[PREP]` 점등 로그와 **같은 로그 파일**에 남아 UAT 때 점등/소등을 한 파일에서 대조할 수 있다.

**상수 (이미 존재, 그대로 사용):** `private const int DATUM_Z_INDEX = 0;` (:73)
**속성 (SequenceBase 상속):** `Name` — 시퀀스 이름 문자열
**패킷 타입:** `TestResultPacket` (같은 파일 안에서 이미 사용 중, using 추가 불필요)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TryTurnOffLightsOnCycleEnd 헬퍼 신설 + BuildScopedResponse 훅 (정상 P/F + 크로스-Z Datum 즉시 F)</name>

  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>

  <read_first>
    - `WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 의 692-716 (TurnOffShotLights 주변 — 헬퍼를 여기 삽입한다)
    - 같은 파일 1400-1420 (BuildScopedResponse 전문)
    - 같은 파일 1654-1678 (ApplyCycleJudgement — IsBuffer 를 어떻게 뒤집는지 확인, **수정하지 않는다**)
    - 같은 파일 1459-1490 (TryApplyCrossZDatumImmediateFail — 동일, **수정하지 않는다**)
    - 이 plan 의 `<interfaces>` 안 "사이클 종료 경로 전체 구조" 다이어그램
  </read_first>

  <action>
편집 전에 라인 드리프트를 확인한다:
```
rg -n "public void TurnOffShotLights|private TestResultPacket BuildScopedResponse|TryApplyCrossZDatumImmediateFail\(packet, nZIndex\)|pMyContext.ResultInfo = packet.Result;" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
```

***

**(1) 헬퍼 신설 — `TurnOffShotLights()` 메서드 바로 아래(현재 709번 줄 닫는 중괄호 다음)에 삽입**

**왜 여기인가:** 소등 로직을 한곳에 모아, "`TurnOffShotLights` 를 이제 누가 부르나?"라는 질문이 한눈에 답이 되게 한다(71-01 에서 `SystemHandler.TurnOffPrepLights` 가 호출자를 잃었기 때문에 특히 중요하다).

```csharp
        //260806 hbk Phase 71: 사이클이 P/F 로 확정되는 순간(IsBuffer==false) 전 조명 소등.
        //  $PREP Op=0(PLC 의 명시적 OFF 요청) 폐기 대체 — 사이클 종료 시점을 아는 주체는 PLC 가 아니라 판정을 내리는 비전이다.
        //  IsBuffer==true(중간 index B 응답)면 다음 z_index 촬영이 남아있으므로 끄지 않는다.
        //  소등해도 다음 촬영은 안전하다: 모든 $TEST 앞에 $PREP 가 선행하고 ApplyShotLightsInternal 이 전 채널을 선언적으로 재적용한다.
        //  호출 지점 2곳(둘 다 필요, 중복 아님):
        //   (a) BuildScopedResponse — 정상 마지막 index P/F + 크로스-Z Datum 즉시 F
        //   (b) HandleDatumIndexResponse — Index 0 Datum 즉시 F (BuildScopedResponse 를 거치지 않는 별도 경로)
        private void TryTurnOffLightsOnCycleEnd(TestResultPacket packet, string szPath, int nZIndex)
        {
            bool bHasPacket = packet != null;
            if (!bHasPacket)
            {
                return;
            }
            bool bCycleEnded = !packet.IsBuffer;   // B(=buffer) 가 아니면 P 또는 F 로 확정된 것 = 사이클 끝
            if (!bCycleEnded)
            {
                return;
            }
            TurnOffShotLights();
            Logging.PrintLog((int)ELogType.LightController,
                "[CycleLightOff] Seq={0}, path={1}, z={2}, result={3} //260806 hbk Phase 71", Name, szPath, nZIndex, packet.Result);
        }
```
- **`?:` 삼항 금지** — 위 코드처럼 early-return `if` 로만 작성한다.
- 로그는 `ELogType.LightController` (기존 `[PREP]` 점등 로그와 같은 파일 → UAT 시 점등/소등 1:1 대조 가능).
- `szPath` 는 3개 UAT 시나리오를 로그에서 구분하기 위한 값이다. 호출부가 리터럴로 넘긴다.

***

**(2) `BuildScopedResponse` 훅 배선 (현재 1404-1420)**

`TryApplyCrossZDatumImmediateFail(packet, nZIndex);` **다음 줄**, `pMyContext.ResultInfo = packet.Result;` **앞**에 한 줄을 삽입한다. **다른 줄은 건드리지 않는다.**

```csharp
            ApplyCycleJudgement(packet, bIsLastIndex, nMatchedShots);   // B vs 종합 P/F (D-03/불변식), WR-01: 매칭 0건 전달
            TryApplyCrossZDatumImmediateFail(packet, nZIndex);   //260722 hbk Phase 68 GAP-3(68-10): 완성 index 크로스-Z Datum 실패 재평가(게이팅, 기본 OFF no-op)
            TryTurnOffLightsOnCycleEnd(packet, "scoped", nZIndex);   //260806 hbk Phase 71: 위 두 판정이 모두 끝난 뒤 IsBuffer==false 단일 게이트로 소등(개별 함수 수정 없이 누락 방지)
            pMyContext.ResultInfo = packet.Result;
            return packet;
```

**왜 두 판정 함수 뒤 단일 지점인가 (CONTEXT 권장 설계):** `ApplyCycleJudgement` 와 `TryApplyCrossZDatumImmediateFail` 둘 다 `packet.IsBuffer` 를 in-place 로 뒤집는다. 두 함수 각각에 훅을 심으면 (a) 어느 쪽이 뒤집었는지에 따라 중복 소등이 나거나 (b) 나중에 세 번째 판정 함수가 추가될 때 누락된다. **둘 다 끝난 뒤 결과만 한 번 보는 게 구조적으로 누락 불가능하다.**

`ApplyCycleJudgement`(1657-1678)와 `TryApplyCrossZDatumImmediateFail`(1459-1490) 의 **본문은 한 글자도 수정하지 않는다.**
  </action>

  <acceptance_criteria>
    - `rg -c "private void TryTurnOffLightsOnCycleEnd\(TestResultPacket packet, string szPath, int nZIndex\)" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` → `1`
    - `rg -c "TryTurnOffLightsOnCycleEnd\(packet, \"scoped\", nZIndex\);" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` → `1`
    - 삽입 위치 검증 (두 판정 함수 **뒤**, ResultInfo 대입 **앞**): `rg -n "TryApplyCrossZDatumImmediateFail\(packet, nZIndex\);" -A 2 WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 출력의 **바로 다음 줄**이 `TryTurnOffLightsOnCycleEnd(packet, "scoped", nZIndex);` 이고 그 다음 줄이 `pMyContext.ResultInfo = packet.Result;` 일 것
    - 헬퍼 위치 검증 (TurnOffShotLights 직후): `rg -n "public void TurnOffShotLights" -A 14 WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs | rg -c "TryTurnOffLightsOnCycleEnd"` → `1`
    - `rg -c "\[CycleLightOff\] Seq=\{0\}, path=\{1\}, z=\{2\}, result=\{3\}" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` → `1`
    - `rg -c "ELogType.LightController" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` → `3` (기존 584 / 684 + 신규 1)
    - `rg -c "public void TurnOffShotLights\(\)" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` → `1` (**삭제 금지 확인**)
    - **anti-goal — 판정 함수 내부 무수정:** `git diff -U0 HEAD -- WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs | rg "^\+" | rg "IsBuffer = |Result = EVisionResultType|m_bImmediateFailSent"` → 출력 없음 (판정 로직을 건드리지 않았음을 입증)
    - **anti-goal — 삼항 미사용:** `rg -n "TryTurnOffLightsOnCycleEnd" -A 20 WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs | rg "\s\?\s.*\s:\s"` → 출력 없음
    - **anti-goal — 다른 파일 무수정:** `git diff --stat HEAD -- WPF_Example/TcpServer/ WPF_Example/Custom/SystemHandler.cs` → 출력 없음
    - **anti-goal — 사용자 실험 미훼손:** `git diff --stat HEAD -- WPF_Example/Halcon/Algorithms/PatternMatchService.cs WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs` 의 변경 줄 수가 작업 전과 동일
    - 빌드 통과 (아래 verify)
  </acceptance_criteria>

  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal</automated>
  </verify>

  <done>
`TryTurnOffLightsOnCycleEnd` 헬퍼가 `TurnOffShotLights()` 바로 아래에 존재하고, `BuildScopedResponse` 가 두 판정 함수 호출 직후·`ResultInfo` 대입 직전에 이를 `"scoped"` 경로로 호출한다. `ApplyCycleJudgement` / `TryApplyCrossZDatumImmediateFail` / `TurnOffShotLights` 본문은 무수정. msbuild Debug/x64 0 errors / 0 new warnings.
  </done>
</task>

<task type="auto">
  <name>Task 2: HandleDatumIndexResponse 훅 — Index 0 Datum 즉시실패 별도 종료 경로 (누락 위험 경로)</name>

  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>

  <read_first>
    - `WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 의 1315-1344 (BuildDatumShotResponse 전문 — 즉시F 분기와 정상 분기의 IsBuffer 값 차이 확인)
    - 같은 파일 1388-1398 (HandleDatumIndexResponse 전문 — 훅을 여기 넣는다)
    - Task 1 에서 추가한 `TryTurnOffLightsOnCycleEnd` 헬퍼 (이 태스크는 그 헬퍼를 재사용만 한다)
  </read_first>

  <action>
**이 태스크가 별도로 존재하는 이유 (읽고 시작할 것):**
CONTEXT.md 는 "`BuildScopedResponse` 안에서 한 번만 체크하는 단일 지점" 설계를 권장했지만, 라이브 코드 확인 결과 **Index 0 Datum 즉시실패는 `BuildScopedResponse` 를 전혀 거치지 않는다.** `AddResponseV1Cycle`(:1371)이 `z == 0` 이면 `HandleDatumIndexResponse`(:1390)로 분기해 `BuildDatumShotResponse`(:1318)로 `IsBuffer=false; Result=NG;` 를 만들고 곧장 `PersistAndEnqueueV1` 로 보낸다. Task 1 의 훅 하나만으로는 **이 경로에서 조명이 켜진 채 사이클이 끝난다.**

CONTEXT.md 완료 기준도 이 경로를 명시적으로 "누락되기 쉬우니 반드시 별도 테스트"로 못박았다. 두 훅은 중복이 아니라 **둘 다 필수**다.

***

편집 전 라인 확인:
```
rg -n "private void HandleDatumIndexResponse|BuildDatumShotResponse\(\);|PersistAndEnqueueV1\(recipeManager, datumPacket\);" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
```

**`HandleDatumIndexResponse` (현재 1390-1398) 에 한 줄 삽입** — `BuildDatumShotResponse()` 반환 직후, `PersistAndEnqueueV1` 앞:

```csharp
        private void HandleDatumIndexResponse(InspectionRecipeManager recipeManager)
        {
            ResetCycleState();                                  // D-08: 사이클 시작 = 클린 슬레이트
            m_nCurrentZIndex = DATUM_Z_INDEX;
            m_nLastZIndex = ComputeLastZIndex(recipeManager);   // 리셋 직후 재산출(호출부 의무, ResetCycleState 주석)
            m_bCycleDatumFailed = DetectDatumFailure();         // D-04: Datum 검출 실패 감지
            TestResultPacket datumPacket = BuildDatumShotResponse();
            TryTurnOffLightsOnCycleEnd(datumPacket, "datum-index0", DATUM_Z_INDEX);   //260806 hbk Phase 71: Index 0 즉시-F 는 BuildScopedResponse 를 안 거치는 별도 종료 경로 — 두 번째 훅 필수
            PersistAndEnqueueV1(recipeManager, datumPacket);
        }
```

**동작 검증 포인트 (코드를 읽고 스스로 확인할 것):**
- 정상 Datum 샷: `BuildDatumShotResponse` 가 `IsBuffer=true` 를 반환 → 헬퍼가 early-return → **소등 안 됨** ✔ (뒤에 z>=1 촬영이 남아있으므로 이게 맞다)
- 즉시 F: `IsBuffer=false, Result=NG` → **소등됨** ✔
- `bImmediateFail = bUseV1 && bDatumFailed` 이므로 `UseProtocolV1=false`(v2.6)면 애초에 `AddResponseV1Cycle` 자체가 호출되지 않는다 → **v2.6 회귀 0** ✔

**`BuildDatumShotResponse`(1318-1344) 의 본문은 한 글자도 수정하지 않는다.** 훅을 `bImmediateFail` 분기 안에 인라인으로 넣지 말고 반드시 호출부(`HandleDatumIndexResponse`)에 둔다 — 그래야 Task 1 훅과 **완전히 동일한 게이트 조건(`!packet.IsBuffer`)** 을 공유하고, 판정 함수는 판정만 하는 구조가 유지된다.

**주의: 훅을 `PersistAndEnqueueV1` **뒤**로 옮기지 말 것.** Task 1 의 훅은 패킷이 반환/영속화되기 전에 실행된다. 두 경로의 소등 타이밍을 동일하게 맞춰야 UAT 결과가 일관된다.
  </action>

  <acceptance_criteria>
    - `rg -c "TryTurnOffLightsOnCycleEnd\(datumPacket, \"datum-index0\", DATUM_Z_INDEX\);" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` → `1`
    - 삽입 위치 검증: `rg -n "TestResultPacket datumPacket = BuildDatumShotResponse\(\);" -A 2 WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 출력의 **바로 다음 줄**이 `TryTurnOffLightsOnCycleEnd(datumPacket, "datum-index0", DATUM_Z_INDEX);` 이고 그 다음 줄이 `PersistAndEnqueueV1(recipeManager, datumPacket);` 일 것
    - 훅 총 2곳: `rg -c "TryTurnOffLightsOnCycleEnd\(" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` → `3` (정의 1 + 호출 2)
    - **anti-goal — BuildDatumShotResponse 내부 무수정:** `rg -n "private TestResultPacket BuildDatumShotResponse" -A 27 WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs | rg "TryTurnOffLightsOnCycleEnd"` → 출력 없음 (훅이 판정 함수 안으로 들어가지 않았음)
    - **anti-goal — 판정 로직 무수정:** `git diff -U0 HEAD -- WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs | rg "^\+" | rg "IsBuffer = |Result = EVisionResultType|m_bImmediateFailSent|m_bCycleDatumFailed = "` → 출력 없음
    - **anti-goal — v2.6 경로 무수정:** `git diff -U0 HEAD -- WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs | rg "^@@"` 로 나오는 hunk 가 3개 이하이고 전부 700~710 / 1390~1400 / 1404~1420 범위 안일 것 (v2.6 폴백 블록 129줄 이하는 diff 에 등장하지 않음)
    - **anti-goal — 다른 파일 무수정:** `git diff --stat HEAD -- WPF_Example/TcpServer/ WPF_Example/Custom/SystemHandler.cs` → 출력 없음
    - 빌드 통과 (아래 verify)
  </acceptance_criteria>

  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal</automated>
  </verify>

  <done>
`HandleDatumIndexResponse` 가 `BuildDatumShotResponse()` 직후 `TryTurnOffLightsOnCycleEnd(datumPacket, "datum-index0", DATUM_Z_INDEX)` 를 호출하고, 파일 전체에 훅 호출이 정확히 2곳 존재한다. `BuildDatumShotResponse` 본문과 v2.6 폴백 경로는 무수정. msbuild Debug/x64 0 errors / 0 new warnings.
  </done>
</task>

</tasks>

<build_lock_fallback>
`msbuild` 가 `MSB3027` / `MSB3021`(파일이 다른 프로세스에 의해 사용 중) 로 실패하면:

1. **절대 프로세스를 종료하지 말 것** (`taskkill` / `Stop-Process` 금지 — 이 프로젝트에서 과거 사고가 있었다).
2. 스크래치 OutDir 로 **컴파일만** 검증한다:
   ```
   msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal /p:OutputPath=C:\Users\tech\AppData\Local\Temp\claude\C--Info-Project-DataMeasurement\6daecb8f-c376-47ac-89d1-018d55afefc3\scratchpad\bin71\
   ```
3. 그래도 잠기면 **빌드 산출물이 잠겨 있다는 사실을 그대로 보고하고 중단**한다. 잠김을 우회하려고 파일을 지우거나 이름을 바꾸지 않는다.
4. SUMMARY 에 어떤 경로로 검증했는지 반드시 명시한다.
</build_lock_fallback>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사이클 판정 로직 → `LightHandler`(시리얼 조명 컨트롤러) | 소프트웨어 판정이 **물리 조명 상태**를 바꾼다. 시점이 틀리면 암(暗)영상으로 측정 → 전량 오판정. 이 phase 가 새로 여는 유일한 경계다. |
| 시퀀스 A 의 사이클 종료 → 시퀀스 B 의 촬영 | `LightHandler` 는 전역 싱글턴이라 한 시퀀스의 소등이 **같은 PC 의 다른 시퀀스 조명까지** 끈다(TOP/SIDE 공존 시). |
| PLC 의 z_index 진행 → 비전의 IsBuffer 판정 | PLC 가 사이클을 중도 포기(abort)하면 P/F 확정이 영영 안 와 소등도 안 된다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-11 | Denial of Service | `TryTurnOffLightsOnCycleEnd` — **조기 소등으로 암영상 측정** | **mitigate** | 이 phase 최대 위험. 3중 방어: (1) 게이트를 `!packet.IsBuffer` 로 좁혀 **중간 index B 응답에서는 절대 끄지 않는다** — 다음 촬영이 남아있는 유일한 상태가 B 다. (2) 소등 후 다음 촬영 전에 **반드시 `$PREP` 가 선행**하고 `ApplyShotLightsInternal` 이 전 채널을 선언적으로 재적용한다(부분 갱신이 아니라 전체 재적용이라 이전 소등 상태가 남지 않는다). 규격 문서도 "HW 트리거 전환 시에도 PREP 유지(트리거만 교체)"로 이 선행을 보장한다. (3) 71-03 의 UAT-B(한 사이클 내 z_index 다중 전환 회귀 0)가 실기로 검증한다. |
| T-71-12 | Denial of Service | 멀티 시퀀스 PC(TOP+SIDE)에서 **한 시퀀스의 사이클 종료가 다른 시퀀스 조명까지 소등** | **mitigate** | `LightHandler` 전역 싱글턴이므로 실제로 전 채널이 꺼진다. 그러나 T-71-11 의 방어 (2)와 동일 논리로 안전하다 — 다른 시퀀스의 다음 `$TEST` 앞에도 `$PREP` 가 선행해 조명이 재적용된다. **오히려 이게 의도한 동작**이다(CONTEXT: "전체 조명 소등"). 시퀀스별 부분 소등을 시도하면 채널 소유권 모델이 없어 더 위험하다. UAT-B 로 검증. |
| T-71-13 | Denial of Service | PLC 가 사이클을 abort → P/F 가 영영 안 옴 → **조명 계속 켜짐** | **accept** | 변경 **전과 동일한 위험도**다(기존에도 PLC 가 `$PREP Op=0` 을 안 보내면 켜진 채로 남았다). 오히려 자동소등이 정상 종료 케이스를 커버하므로 켜진 채 방치되는 빈도는 줄어든다. 영향은 LED 수명뿐(안전/측정 무관). 필요 시 별도 phase 에서 idle 타임아웃 소등을 검토 — 이번 스코프 아님. |
| T-71-14 | Repudiation | 소등이 실제로 일어났는지 사후 추적 불가 | **mitigate** | `[CycleLightOff] Seq=..., path=..., z=..., result=...` 1줄을 `ELogType.LightController` 로 기록. 기존 `[PREP] Shot not found...` 점등 로그와 **같은 로그 파일**이라 점등/소등 시퀀스를 한 파일에서 대조 가능. `path` 값(`scoped` / `datum-index0`)과 `result` 값으로 **UAT 3개 시나리오가 로그에서 서로 구분**된다 — "안 껐다"는 신고가 와도 훅이 탔는지 여부를 즉시 판별할 수 있다. 사이클당 최대 1줄이라 로그 폭주 없음. |
| T-71-15 | Tampering | v2.6 레거시 경로 오염 | **mitigate** | 두 훅 모두 `AddResponseV1Cycle` 하위(`UseProtocolV1==true` 게이트, `AddResponse:123-128`)에만 존재한다. v2.6 폴백 블록(129줄 이하)은 diff 에 등장조차 하지 않는다 — acceptance criteria 에서 hunk 범위를 grep 으로 못박았다. **v2.6 회귀 0.** |
| T-71-16 | Tampering | 판정 로직(`ApplyCycleJudgement` / `TryApplyCrossZDatumImmediateFail` / `BuildDatumShotResponse`) 오염 | **mitigate** | 훅을 세 함수 **바깥**(호출부)에만 두어 판정 함수는 판정만 하는 구조를 유지한다. `git diff` 의 `+` 줄에 `IsBuffer = ` / `Result = EVisionResultType` / `m_bImmediateFailSent` 가 하나도 없어야 한다는 anti-goal 로 검증한다. Phase 49(D-01~D-08)/Phase 68(GAP-3 latch) 계약이 손상되지 않음을 기계적으로 보장. |
| T-71-17 | Denial of Service | `LightHandler.SetOnOff` 시리얼 통신 실패/블로킹 | **accept** | 기존 `ApplyShotLightsInternal` 경로가 이미 보호 없이 같은 호출을 하고 있다 — **동일 위험도 유지(회귀 0)**. 여기서만 try/catch 를 새로 두면 두 경로의 실패 처리가 갈려 오히려 진단이 어려워진다. 시리얼 예외는 기존 상위 핸들러 정책을 그대로 따른다. |
| T-71-18 | Information Disclosure | `[CycleLightOff]` 로그 내용 | **accept** | 노출 값은 내부 시퀀스명(`TOP`/`SIDE`/`BOTTOM`), z_index, P/F 결과뿐. 비밀·PII 아님, 로컬 산업용 PC(클라우드/네트워크DB 없음), 운영자 진단에 필수. |
| T-71-19 | Elevation of Privilege | — | **accept** | 신규 멤버는 `private` 단일 메서드이며 어떤 권한 검사도 우회하지 않는다. `TurnOffShotLights` 의 접근제한자(`public`)도 바꾸지 않는다. |
</threat_model>

<verification>
1. `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal` → 0 errors / 0 new warnings
2. `rg -c "TryTurnOffLightsOnCycleEnd\(" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` → `3` (정의 1 + 호출 2)
3. `rg -c "TryTurnOffLightsOnCycleEnd\(packet, \"scoped\", nZIndex\);" ...InspectionSequence.cs` → `1`
4. `rg -c "TryTurnOffLightsOnCycleEnd\(datumPacket, \"datum-index0\", DATUM_Z_INDEX\);" ...InspectionSequence.cs` → `1`
5. `rg -c "public void TurnOffShotLights\(\)" ...InspectionSequence.cs` → `1` (CONTEXT locked: 삭제 금지)
6. `git diff -U0 HEAD -- WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs | rg "^\+" | rg "IsBuffer = |Result = EVisionResultType|m_bImmediateFailSent"` → 출력 없음 (판정 로직 무오염)
7. `rg -n "TryTurnOffLightsOnCycleEnd" -A 20 ...InspectionSequence.cs | rg "\s\?\s.*\s:\s"` → 출력 없음 (삼항 미도입)
8. `git diff --name-only HEAD` → 이 plan 이 만든 변경은 `WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 1개 파일만. `PatternMatchService.cs` / `PickerCenterCalibrationService.cs` 는 내용 무변경으로 그대로.
</verification>

<success_criteria>
- 사이클 종료(`IsBuffer==false`) 판정이 단일 헬퍼 1곳에 집중되고, 종료 경로 2곳 모두에 배선됨
- 중간 index B 응답에서는 소등되지 않음 (게이트가 `!IsBuffer`)
- 3개 UAT 시나리오(정상 P / NG 누적 F / Datum 즉시실패 F)가 `[CycleLightOff]` 로그의 `path`+`result` 조합으로 서로 구분 가능
- `TurnOffShotLights()` 본문 + 판정 함수 3개 본문 + v2.6 폴백 경로 전부 무수정 (회귀 0)
- `TcpServer/` 와 `Custom/SystemHandler.cs` 무수정 (71-01 담당)
- msbuild Debug/x64 빌드 PASS
</success_criteria>

<output>
완료 후 `.planning/phases/71-prep-op-plc-off-p-f/71-02-SUMMARY.md` 를 생성한다.
SUMMARY 에 반드시 포함할 것:
- `TryTurnOffLightsOnCycleEnd` 최종 시그니처와 삽입 라인 번호
- 훅 2곳의 최종 라인 번호 + 각각이 커버하는 종료 경로 이름
- **3개 UAT 시나리오별로 어떤 로그 라인이 찍히는지 예시 문자열** (71-03/71-04 UAT 에서 그대로 대조용으로 씀) — 예:
  - 정상 P: `[CycleLightOff] Seq=BOTTOM, path=scoped, z=3, result=OK`
  - NG 누적 F: `[CycleLightOff] Seq=BOTTOM, path=scoped, z=3, result=NG`
  - Index 0 Datum 즉시 F: `[CycleLightOff] Seq=BOTTOM, path=datum-index0, z=0, result=NG`
  - (참고) 크로스-Z Datum F: `[CycleLightOff] Seq=SIDE, path=scoped, z=1, result=NG`
- 해당 로그가 기록되는 **로그 파일 경로**(`ELogType.LightController` 가 매핑된 실제 파일명 — `Logging.SetLog` 등록부에서 확인해 적을 것)
- 빌드 검증 경로 (정상 빌드 / 스크래치 OutDir / 잠김 보고 중 무엇이었는지)
</output>
