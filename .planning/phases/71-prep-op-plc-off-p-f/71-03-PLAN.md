---
phase: 71-prep-op-plc-off-p-f
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified: []
autonomous: false
requirements: [PROTO-PREP-01]

must_haves:
  truths:
    - "통합 Debug/x64 빌드가 통과하고, 71-01 과 71-02 의 변경이 서로 충돌하지 않는다"
    - "$PREP:1,3@ (Op 없이 2필드) 요청에 $PREP_ACK:1,3,OK@ 가 실제로 돌아온다 (실기 확인)"
    - "구 3필드 $PREP:1,3,1@ 및 $PREP:1,3,0@ 요청도 무응답 없이 $PREP_ACK:1,3,OK@ 를 받는다 (D-71-01 하위호환)"
    - "한 사이클 안에서 z_index 를 여러 번 전환할 때 조명이 매번 올바르게 전환된다 (기존 동작 회귀 0)"
  artifacts:
    - path: ".planning/phases/71-prep-op-plc-off-p-f/71-03-SUMMARY.md"
      provides: "코드 레벨 게이트 결과 + UAT-A/UAT-B 실기 판정 기록"
  key_links:
    - from: "UAT-A 실기 결과"
      to: "D-71-01 하위호환 결정"
      via: "구 3필드 요청 실기 응답 확인"
      pattern: "PREP_ACK"
---

<objective>
71-01(wire 포맷)과 71-02(조명 자동소등)를 **통합 검증**한다. 이 plan 은 코드 레벨 게이트(자동)를 먼저 통과시킨 뒤, 사람만 확인할 수 있는 **프로토콜 wire 실기 동작**을 체크포인트로 확인한다.

**Purpose:** 이 프로젝트에는 단위 테스트 프레임워크가 없다. 따라서 (1) 빌드 + grep 기반 정적 전수 검증으로 기계적으로 확인 가능한 것을 전부 잡고, (2) 나머지는 실제 TCP 왕복으로 사람이 확인한다. 특히 `$PREP_ACK` 는 필드 개수가 4→3 으로 **줄어드는 breaking change** 라, 핸들러/PLC 쪽 파서와의 실제 정합은 실기로만 확인된다.

**분담:**
- 이 plan(71-03) = **프로토콜 wire** (UAT-A: 2필드/구 3필드 하위호환, UAT-B: z_index 다중 전환 회귀 0)
- 다음 plan(71-04) = **조명 자동소등 3 시나리오** (UAT-C/D/E)

**Output:** 71-03-SUMMARY.md 에 기록된 정적 게이트 결과 + UAT-A/UAT-B 판정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-prep-op-plc-off-p-f/71-CONTEXT.md
@.planning/phases/71-prep-op-plc-off-p-f/71-01-PLAN.md
@.planning/phases/71-prep-op-plc-off-p-f/71-02-PLAN.md
@CLAUDE.md

<canonical_spec>
프로토콜 정답지 (**편집 금지, 참조 전용** — CONTEXT.md locked decision):
`Z:\DOC_EXPORT\2026-08-06[17.51.59]\디팜스테크_Vision_Protocol_v1.3.xlsx`

이 문서의 PREP / PREP_ACK 행은 **이미 목표 상태**로 갱신되어 있다(사용자가 스크린샷으로 직접 확인):
- `PREP ★` : `$PREP:site,z_index@` — "비전이 z_index 맞는 조명 자동 선정 / ACK = 조명 안정·촬영 준비 완료 신호 / HW 트리거 전환 시에도 PREP 유지(트리거만 교체)"
  - 예시: `[조명 ON] $PREP:1,0@ → $PREP_ACK:1,0,OK@` / `[조명 OFF] $PREP:1,0@ → $PREP_ACK:1,0,OK@` (Op 가 없으므로 두 예시가 같은 형태)
- `PREP_ACK` : `$PREP_ACK:site,z_index,OK@` / `$PREP_ACK:site,z_index,FAIL@` — "z_index: Echo, OK: 조명 점등/소등 완료, FAIL: 해당 z_index Shot 없음 등"

**즉 코드가 이 문서에 맞춰야 하는 것이지, 문서를 고치는 태스크는 존재하지 않는다.**
</canonical_spec>

<repo_state_warning>
`WPF_Example/Halcon/Algorithms/PatternMatchService.cs` 와 `WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs` 는 **사용자가 의도적으로 진행 중인 미커밋 실험**이다. `git checkout` / `git stash` / `git restore` 금지.
</repo_state_warning>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 통합 빌드 + 정적 전수 검증 + UAT 보조 스크립트 준비</name>

  <files>(리포지토리 파일 무수정 — 스크래치 디렉터리에만 보조 스크립트 생성)</files>

  <read_first>
    - `.planning/phases/71-prep-op-plc-off-p-f/71-01-SUMMARY.md` (71-01 실행 결과 — 실제 변경 라인/빌드 검증 경로)
    - `.planning/phases/71-prep-op-plc-off-p-f/71-02-SUMMARY.md` (71-02 실행 결과 — **3개 시나리오별 로그 예시 문자열**과 로그 파일 경로. 71-04 UAT 에서 그대로 쓴다)
    - `WPF_Example/Setting/SystemSetting.cs` 에서 `ServerPort` 기본값 확인
  </read_first>

  <action>
**(1) 통합 빌드**
```
msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal
```
71-01 과 71-02 가 병렬(Wave 1)로 실행됐으므로, 두 변경이 합쳐진 상태에서 처음으로 함께 빌드되는 지점이다. 0 errors / 0 new warnings 여야 한다.

**(2) 정적 전수 검증 — 아래 표를 그대로 실행하고 결과를 SUMMARY 에 표로 기록한다**

| # | 명령 | 기대값 | 무엇을 증명하나 |
|---|---|---|---|
| S1 | `rg -n "\.Op\b\|public int Op" WPF_Example/` | 출력 없음 | 전 리포 Op 참조 0개 (DebugManualZTrigger:841 포함) |
| S2 | `rg -n "TryParsePrepFields" -A 4 WPF_Example/TcpServer/VisionRequestPacket.cs \| rg -c "dataList\.Length >= 2"` | `1` | D-71-01 관대한 파싱(`== 2` 로 안 바뀜) |
| S3 | `rg -n "TryParsePrepFields" -A 25 WPF_Example/TcpServer/VisionRequestPacket.cs \| rg "dataList\[2\]"` | 출력 없음 | 3번째 필드를 아예 읽지 않음 |
| S4 | `rg -n "BuildPrepAckMessage" -A 24 WPF_Example/TcpServer/VisionResponsePacket.cs \| rg -c "MSG_CONTENTS_SEPERATOR"` | `2` | ACK wire 포맷 = `site,z,OK` |
| S5 | `rg -c "bool bApplied = ApplyPrepToSequences\(packet\.ZIndex\);" WPF_Example/Custom/SystemHandler.cs` | `1` | ProcessPrep 단일 경로 |
| S6 | `rg -c "private bool TurnOffPrepLights\(\)" WPF_Example/Custom/SystemHandler.cs` | `1` | CONTEXT locked: 삭제 금지 |
| S7 | `rg -c "public void TurnOffShotLights\(\)" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` | `1` | CONTEXT locked: 삭제 금지 |
| S8 | `rg -c "TryTurnOffLightsOnCycleEnd\(" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` | `3` | 정의 1 + 훅 2 (두 종료 경로 모두 배선) |
| S9 | `rg -c "TryTurnOffLightsOnCycleEnd\(datumPacket, \"datum-index0\", DATUM_Z_INDEX\);" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` | `1` | **누락 위험 경로**(Index 0 즉시F) 배선 확인 |
| S10 | `git diff -U0 HEAD -- WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs \| rg "^\+" \| rg "IsBuffer = \|Result = EVisionResultType\|m_bImmediateFailSent"` | 출력 없음 | 판정 로직 무오염 (Phase 49/68 계약 보존) |
| S11 | `git diff --name-only HEAD` | 4개 파일(71-01 의 3개 + 71-02 의 1개)만 + 사전 존재 미커밋 2개 | 스코프 밖 파일 무수정 |
| S12 | `git diff --stat HEAD -- WPF_Example/Halcon/Algorithms/PatternMatchService.cs WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs` | 71-01/71-02 실행 전과 동일한 변경 줄 수 | 사용자 실험 미훼손 |

**하나라도 기대값과 다르면 체크포인트로 넘어가지 말고 즉시 보고하고 멈춘다.**

**(3) 리포지토리 내 프로토콜 문서 사본 스캔 (문서 편집은 하지 않음, 발견 보고만)**
```
rg -n "PREP" .planning/refs/ --glob "*.md"
```
`.planning/refs/Vision-Protocol-v1.0.md` 는 **v1.0 스냅샷**(아직 `$LIGHT:site,type,OP@` / `$TEST:site,null,z_index@` 시대)이라 이번 phase 의 갱신 대상이 아니다. 다른 사본이 v3.x 를 기술하면서 `$PREP` 에 Op 를 남기고 있으면 **고치지 말고 SUMMARY 에 "발견: 구버전 사본" 으로 플래그**만 한다(문서 편집은 CONTEXT locked 로 스코프 밖).

**(4) UAT 보조 스크립트 준비 — 스크래치 디렉터리에만 생성 (리포지토리 오염 금지)**

먼저 실제 포트를 확인한다:
```
rg -n "ServerPort" WPF_Example/Setting/SystemSetting.cs
```
그리고 실행 디렉터리의 `Setting.ini` 에 `ServerPort` 항목이 있으면 그 값이 우선이다(기본 2505. 리포의 `Test/mock_vision_client.py` 는 7701 로 하드코딩돼 있으니 **그 값을 믿지 말고** 실제 설정을 따를 것).

`C:\Users\tech\AppData\Local\Temp\claude\C--Info-Project-DataMeasurement\6daecb8f-c376-47ac-89d1-018d55afefc3\scratchpad\uat71\send.py` 를 만든다 (`Test/mock_vision_client.py` 의 구조를 그대로 따르되 인자화):

```python
import socket
import sys

HOST = "127.0.0.1"
PORT = int(sys.argv[1])
MESSAGE = sys.argv[2]

with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as client:
    client.settimeout(5.0)
    client.connect((HOST, PORT))
    client.sendall(MESSAGE.encode("utf-8"))
    print("SEND " + MESSAGE, flush=True)
    try:
        response = client.recv(1024).decode("utf-8", errors="replace")
        print("RECV " + response, flush=True)
    except socket.timeout:
        print("RECV <TIMEOUT - 응답 없음>", flush=True)
```
사용법: `python <경로>\send.py <포트> "$PREP:1,3@"`

`python` 이 PATH 에 없으면 PowerShell 대체 수단을 SUMMARY 에 적어둔다:
```powershell
$c=New-Object Net.Sockets.TcpClient("127.0.0.1",<포트>); $s=$c.GetStream()
$b=[Text.Encoding]::UTF8.GetBytes('$PREP:1,3@'); $s.Write($b,0,$b.Length)
$r=New-Object byte[] 1024; Start-Sleep -m 500; $n=$s.Read($r,0,1024)
[Text.Encoding]::UTF8.GetString($r,0,$n); $c.Close()
```

**주의: `Test/mock_vision_client.py` 를 수정하지 말 것** (리포지토리 파일이다). 스크래치에만 만든다.

**(5) 앱 실행 준비 안내를 SUMMARY 초안에 적는다**
빌드가 잠겨 있어 스크래치 OutDir 로만 컴파일 검증했다면, 사용자가 앱을 종료한 뒤 정상 빌드를 다시 돌려야 UAT 가 가능하다는 사실을 명시한다. **프로세스를 임의로 종료하지 말 것.**
  </action>

  <acceptance_criteria>
    - msbuild Debug/x64 → 0 errors / 0 new warnings (또는 빌드 잠김 시 `<build_lock_fallback>` 절차대로 스크래치 OutDir 컴파일 성공 + 잠김 사실 보고)
    - S1~S12 정적 검증 12개 항목이 **전부** 기대값과 일치 (하나라도 불일치 시 중단 보고)
    - 스크래치 `uat71\send.py` 생성 완료 + 실제 사용할 포트 번호가 SUMMARY 초안에 확정 기재됨
    - `git status --porcelain` 에 `Test/` 하위 변경이 **없음** (보조 스크립트가 리포지토리에 안 들어갔음)
    - `.planning/refs/` 프로토콜 사본 스캔 결과가 SUMMARY 에 기록됨 (편집은 하지 않았음)
  </acceptance_criteria>

  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal</automated>
  </verify>

  <done>
통합 빌드 PASS 이고 S1~S12 정적 검증이 전부 통과했으며, UAT 용 TCP 송신 스크립트와 확정된 포트 번호가 준비되어 사람이 바로 UAT-A 를 시작할 수 있다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT-A: $PREP 2필드 정상 동작 + 구 3필드 클라이언트 하위호환 (D-71-01 실기 확인)</name>

  <read_first>
    - Task 1 이 SUMMARY 초안에 확정한 **실제 ServerPort 번호**
    - 이 plan 의 `<canonical_spec>` 블록 (규격 문서상 기대 응답 형태)
  </read_first>

  <action>
Task 1 의 정적 게이트가 전부 통과한 상태에서, 아래 `<how-to-verify>` 절차를 **사용자에게 그대로 제시하고 실행을 멈춘다.**
실행자가 대신 판정하거나 결과를 추정하지 않는다. 앱 실행/장비 조작은 사용자 몫이다.

절차를 제시하기 전에 준비 상태를 확인해서 함께 안내한다:
- 정상 빌드 산출물이 최신인지 (스크래치 OutDir 로만 검증했다면 사용자에게 "앱 종료 후 재빌드 필요"를 먼저 알린다)
- 사용할 포트 번호
- `send.py` 전체 경로
  </action>

  <what-built>
`$PREP` wire 포맷에서 `Op` 필드를 제거했습니다.

| | 변경 전 | 변경 후 |
|---|---|---|
| 요청 | `$PREP:site,z_index,Op@` | `$PREP:site,z_index@` |
| 응답 | `$PREP_ACK:site,z_index,Op,OK@` | `$PREP_ACK:site,z_index,OK@` |

**하위호환 결정 (D-71-01):** 구 PLC 펌웨어가 3필드(`$PREP:1,3,1@`)를 계속 보내도 **파싱 실패시키지 않고** 3번째 필드를 무시합니다.
이유: 파싱 실패는 코드상 `return null` = **응답 자체가 안 나감**이라, 구 펌웨어 PLC 가 ACK 를 무한 대기해 설비가 멈춥니다.
  </what-built>

  <how-to-verify>
**준비**
1. 앱을 Debug/x64(SIMUL_MODE) 로 실행합니다. TCP 서버가 떠 있어야 합니다(`ServerPort` = Task 1 이 알려준 값).
2. 별도 터미널을 엽니다.

***

**A-1. Op 없는 2필드 요청 (신규 정상 경로)**
```
python <scratchpad>\uat71\send.py <포트> "$PREP:1,3@"
```
- **합격:** `RECV $PREP_ACK:1,3,OK@`
- **불합격 예:** 필드가 4개로 나옴(`$PREP_ACK:1,3,1,OK@`) / `<TIMEOUT - 응답 없음>` / `FAIL`
- ※ 레시피에 z_index=3 인 Shot 이 없으면 정상적으로 `FAIL` 이 나옵니다. 그럴 땐 **레시피에 실제로 존재하는 z_index** 로 바꿔서 다시 확인해 주세요(예: `$PREP:1,1@`). 확인하려는 건 "OK/FAIL" 자체가 아니라 **필드가 3개인가**입니다.

**A-2. 구 3필드 요청, Op=1 (구 펌웨어 ON)**
```
python <scratchpad>\uat71\send.py <포트> "$PREP:1,3,1@"
```
- **합격:** A-1 과 **완전히 동일한 응답** (`$PREP_ACK:1,3,OK@`). 무응답이 아님.
- **불합격:** `<TIMEOUT - 응답 없음>` (= 하위호환 실패, 설비 정지 위험)

**A-3. 구 3필드 요청, Op=0 (구 펌웨어의 명시적 OFF 요청)**
```
python <scratchpad>\uat71\send.py <포트> "$PREP:1,3,0@"
```
- **합격:** `$PREP_ACK:1,3,OK@` 가 돌아오고, **조명이 꺼지는 게 아니라 z_index=3 조명이 켜집니다.**
- 이건 **의도된 동작**입니다(D-71-01). 구 클라이언트의 "OFF" 의도가 이제 "점등"으로 해석되지만, 소등은 사이클 종료 시 비전이 자동으로 하므로 실질 영향은 "사이클 끝까지 LED 가 켜져 있음"뿐입니다.
- **불합격:** 무응답이거나 앱이 예외/크래시

**A-4. 깨진 요청 (기존 방어 회귀 확인)**
```
python <scratchpad>\uat71\send.py <포트> "$PREP:1@"
python <scratchpad>\uat71\send.py <포트> "$PREP:abc,xyz@"
```
- **합격:** 둘 다 응답 없음(TIMEOUT) + **앱이 죽지 않음**. (필드 부족/비정수는 원래부터 null 처리였습니다 — 변경 전과 동일해야 정상입니다.)
- **불합격:** 앱 크래시 / 예외 모달

***

**A-5. (중요) 핸들러/PLC 실물 연동 — 가능한 경우에만**
실제 핸들러(제어 PC)와 연결된 상태라면, 핸들러가 `$PREP_ACK` 를 **3필드**로 정상 파싱하는지 확인해 주세요.
규격 문서 `디팜스테크_Vision_Protocol_v1.3.xlsx` 는 이미 3필드로 갱신돼 있지만, **핸들러 펌웨어가 그 버전으로 배포됐는지는 코드로 알 수 없습니다.**
- 미배포 상태면 → 비전만 먼저 올리면 안 됩니다. 배포 타이밍을 제어팀(김민우 선임)과 맞춰 주세요.
- 실물 연동이 지금 불가능하면 "N/A(추후 확인)"로 답해 주셔도 됩니다. 그 경우 SUMMARY 에 미검증 항목으로 남깁니다.
  </how-to-verify>

  <verify>
    <automated>MISSING — TCP 왕복 실기 확인은 앱 실행이 필요해 자동화 불가. Task 1 의 S2/S3/S4 정적 검증이 코드 레벨 대응 게이트다.</automated>
  </verify>

  <acceptance_criteria>
    - 사용자가 A-1 ~ A-4 각각에 대해 개별 PASS/FAIL 을 답한다 (뭉뚱그린 "OK" 하나로 넘어가지 않는다)
    - A-1 응답의 **필드 개수가 3개**임이 확인된다 (`site,z_index,OK`)
    - A-2 가 **무응답이 아님**이 확인된다 (D-71-01 의 핵심 근거)
    - A-5 는 PASS / FAIL / N/A(추후 확인) 중 하나로 명시적으로 기록된다
  </acceptance_criteria>

  <resume-signal>
A-1 / A-2 / A-3 / A-4 각각에 PASS 또는 FAIL(+증상)을 적어 주세요. A-5 는 PASS / FAIL / N/A 중 하나로 답해 주세요.
예: `A-1 PASS, A-2 PASS, A-3 PASS, A-4 PASS, A-5 N/A`
하나라도 FAIL 이면 다음 단계로 넘어가지 않고 원인부터 수정합니다.
  </resume-signal>

  <done>
A-1~A-4 가 모두 PASS 이고 A-5 판정이 기록되었다. `$PREP` 2필드 정상 동작과 구 3필드 하위호환이 실기로 입증되었다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT-B: 한 사이클 내 z_index 다중 전환 시 조명 정확 전환 (기존 동작 회귀 0)</name>

  <read_first>
    - `.planning/phases/71-prep-op-plc-off-p-f/71-02-SUMMARY.md` 의 `[CycleLightOff]` 로그 예시와 **로그 파일 경로**
    - UAT-A 결과 (A-1 이 PASS 여야 이 단계 의미가 있다)
  </read_first>

  <action>
아래 `<how-to-verify>` 를 사용자에게 그대로 제시하고 멈춘다. 실행자가 판정하지 않는다.

**이 체크포인트가 별도로 존재하는 이유:** 71-02 의 자동소등이 만들어낸 **가장 큰 위험(T-71-11: 조기 소등 → 암영상 측정 → 전량 오판)** 을 직접 겨냥한 테스트다. "중간 z_index 의 B 응답에서는 절대 꺼지면 안 된다"를 사람 눈으로 확인하는 유일한 수단이다.
  </action>

  <what-built>
사이클 종료(P/F 확정) 시 비전이 스스로 전 조명을 끄는 훅을 넣었습니다.
게이트는 `IsBuffer == false`(= B 응답이 아님 = 사이클 확정) 하나입니다. **중간 z_index 의 B 응답에서는 끄지 않습니다** — 다음 촬영이 남아있기 때문입니다.
확인이 필요한 건 이 게이트가 정확한지, 즉 **사이클 도중에 불이 꺼지지 않는지**입니다.
  </what-built>

  <how-to-verify>
**준비**
- 앱 실행(Debug/x64), 조명이 실제로 보이는 상태(실장비 또는 조명 컨트롤러 상태창)
- 로그 파일을 열어둡니다: 71-02-SUMMARY 가 알려준 `ELogType.LightController` 로그 파일

***

**B-1. z_index 순차 전환 중 조명이 매번 올바르게 바뀌는가**
z_index 를 0 → 1 → 2 → ... 순서로 진행시킵니다(핸들러 사이클 또는 메인 화면 하단 "POC 자동화 임시 테스트" 패널의 수동 Z 트리거).
각 단계마다:
- **합격:** 해당 z_index 의 Shot 설정대로 조명이 켜지고, 이전 z_index 에서 쓰던 채널 중 이번에 안 쓰는 건 꺼진다. **변경 전과 완전히 동일한 동작.**
- **불합격:** 특정 z_index 에서 조명이 아예 안 켜짐 / 이전 채널이 안 꺼짐 / 깜빡임이 새로 생김

**B-2. 사이클 도중(중간 index)에는 절대 전소등되지 않는가 — 이번 변경의 최대 위험**
중간 z_index 단계(마지막이 아닌 단계)에서:
- **합격:** 조명이 **전부 꺼지는 순간이 없다**. 로그 파일에도 그 시점에 `[CycleLightOff]` 줄이 **찍히지 않는다.**
- **불합격:** 중간 단계인데 전소등되거나 `[CycleLightOff]` 로그가 조기에 찍힘 → **즉시 중단하고 보고**. 이건 암영상 측정으로 이어지는 심각한 결함입니다.

**B-3. 측정 결과가 변경 전과 같은가 (회귀 0)**
같은 부품을 변경 전과 동일한 조건으로 한 사이클 돌립니다.
- **합격:** 측정값/판정이 변경 전과 동일 범위. 새로 어두워진 이미지나 밝기 차이로 인한 값 이상이 없다.
- **불합격:** 특정 z_index 의 이미지가 어둡거나 측정값이 튄다

**B-4. 여러 시퀀스가 있는 PC(TOP+SIDE 등)일 때** — 해당하는 경우만
한 시퀀스가 먼저 끝나 소등한 뒤, 다른 시퀀스의 다음 촬영이:
- **합격:** `$PREP` 가 선행하면서 조명이 다시 켜져 정상 촬영된다.
- **불합격:** 뒤 시퀀스가 어두운 이미지로 촬영한다
- 단일 시퀀스 PC 라면 "N/A" 로 답해 주세요.
  </how-to-verify>

  <verify>
    <automated>MISSING — 물리 조명 상태와 이미지 밝기는 사람 눈/실측으로만 확인 가능. 71-02 Task 1 의 `!packet.IsBuffer` 게이트와 acceptance criteria(삽입 위치 검증)가 코드 레벨 대응 게이트다.</automated>
  </verify>

  <acceptance_criteria>
    - B-1 ~ B-3 각각에 개별 PASS/FAIL 이 기록된다
    - B-2 는 **로그 파일 확인 결과까지 포함**해서 답한다 (중간 index 구간에 `[CycleLightOff]` 줄이 없음)
    - B-4 는 PASS / FAIL / N/A 중 하나로 기록된다
  </acceptance_criteria>

  <resume-signal>
B-1 / B-2 / B-3 각각에 PASS 또는 FAIL(+증상)을, B-4 는 PASS / FAIL / N/A 로 답해 주세요.
예: `B-1 PASS, B-2 PASS(로그에 중간 CycleLightOff 없음), B-3 PASS, B-4 N/A`
**B-2 가 FAIL 이면 즉시 멈추고 71-02 의 게이트 조건부터 다시 봅니다.**
  </resume-signal>

  <done>
B-1~B-3 이 모두 PASS(특히 B-2 가 로그 확인까지 포함해 PASS)이고 B-4 판정이 기록되었다. 조기 소등 위험(T-71-11)이 실기로 반증되었고 기존 z_index 전환 동작에 회귀가 없다.
  </done>
</task>

</tasks>

<build_lock_fallback>
`msbuild` 가 `MSB3027` / `MSB3021`(파일 사용 중)로 실패하면:
1. **절대 프로세스를 종료하지 말 것** (`taskkill` / `Stop-Process` 금지).
2. 스크래치 OutDir 로 컴파일만 검증:
   `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal /p:OutputPath=C:\Users\tech\AppData\Local\Temp\claude\C--Info-Project-DataMeasurement\6daecb8f-c376-47ac-89d1-018d55afefc3\scratchpad\bin71\`
3. 이 경우 **UAT 는 아직 최신 산출물로 못 돈다.** 사용자에게 "앱을 종료해 주시면 정상 빌드 후 UAT 를 시작하겠습니다"라고 안내하고 대기한다. 임의 종료 금지.
4. SUMMARY 에 검증 경로를 명시한다.
</build_lock_fallback>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 비전 → 핸들러/PLC (`$PREP_ACK` 3필드) | 필드 개수가 줄어드는 breaking change. 상대측 파서 버전은 코드로 알 수 없다 — 실기로만 확인된다. |
| UAT 보조 스크립트 → VisionServer TCP 포트 | 테스트 트래픽이 프로덕션과 같은 포트로 들어간다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-21 | Denial of Service | 핸들러 펌웨어가 4필드 `$PREP_ACK` 를 기대하는 상태로 비전만 배포 | **mitigate** | UAT-A 의 **A-5 항목**으로 실물 연동 정합을 명시적으로 확인하고, 미확인 시 `N/A(추후 확인)` 로 SUMMARY 에 **미검증 리스크로 남긴다**(조용히 통과시키지 않는다). 규격 문서 v1.3 이 이미 3필드이므로 합의 자체는 존재하나, 배포 타이밍 조율은 사용자/제어팀 책임임을 체크포인트 본문에 명시했다. |
| T-71-22 | Denial of Service | UAT 중 `$PREP` 로 조명을 켜둔 채 세션 종료 | **accept** | 영향은 LED 수명뿐(안전/측정 무관). 다음 사이클의 P/F 확정 또는 앱 종료로 해소된다. UAT 절차 자체가 조명 상태를 계속 관찰하게 되어 있어 방치 가능성이 낮다. |
| T-71-23 | Tampering | UAT 보조 스크립트가 리포지토리에 커밋되어 잔재로 남음 | **mitigate** | 스크립트를 **스크래치 디렉터리에만** 생성하고, `Test/mock_vision_client.py` 수정을 금지했다. acceptance criteria 에 `git status --porcelain` 의 `Test/` 무변경을 못박았다. |
| T-71-24 | Tampering | 검증 중 실수로 사용자 미커밋 실험(`PatternMatchService.cs` 등)을 되돌림 | **mitigate** | `<repo_state_warning>` 에 `git checkout`/`stash`/`restore` 금지를 명시하고, S12 정적 검증으로 변경 줄 수 동일성을 기계적으로 확인한다. |
| T-71-25 | Repudiation | UAT 결과가 "대충 OK" 로 뭉뚱그려져 나중에 어느 항목이 미검증인지 알 수 없게 됨 | **mitigate** | 각 체크포인트의 `<resume-signal>` 이 **항목별 개별 PASS/FAIL** 을 요구하고, acceptance criteria 가 "뭉뚱그린 OK 하나로 넘어가지 않는다"를 명문화했다. N/A 도 명시적으로 기록된다. |
| T-71-26 | Information Disclosure | UAT 로그/응답에 노출되는 값 | **accept** | site 번호, z_index, OK/FAIL, 시퀀스명뿐. 비밀·PII 아님. 로컬 폐쇄망. |
| T-71-27 | Spoofing / Elevation | 테스트 스크립트가 인증 없이 프로덕션 포트에 접속 | **accept** | 기존 그대로 — VisionServer 는 원래 인증이 없고 이 phase 가 그 모델을 바꾸지 않는다. 폐쇄된 설비 내부망 전제. |
</threat_model>

<verification>
1. Task 1 의 S1~S12 정적 검증 12개 전부 통과
2. 통합 msbuild Debug/x64 → 0 errors / 0 new warnings
3. UAT-A: A-1~A-4 전부 PASS, A-5 판정 기록됨
4. UAT-B: B-1~B-3 전부 PASS(B-2 는 로그 확인 포함), B-4 판정 기록됨
5. `git status --porcelain` 에 `Test/` 하위 변경 없음
</verification>

<success_criteria>
- 71-01 + 71-02 통합 빌드 PASS
- 정적 전수 검증 12항목 통과 (특히 S9 = Index 0 즉시F 훅 배선, S10 = 판정 로직 무오염)
- `$PREP:1,3@` → `$PREP_ACK:1,3,OK@` 실기 확인 (필드 3개)
- 구 3필드 요청이 무응답 없이 처리됨 실기 확인 (D-71-01)
- 한 사이클 내 z_index 다중 전환 회귀 0 + **중간 index 조기 소등 없음** 실기 확인
</success_criteria>

<output>
완료 후 `.planning/phases/71-prep-op-plc-off-p-f/71-03-SUMMARY.md` 를 생성한다.
SUMMARY 에 반드시 포함할 것:
- S1~S12 정적 검증 결과 표 (명령 / 기대값 / 실제값 / PASS·FAIL)
- UAT-A 항목별 판정 (A-1~A-5) — **A-5 가 N/A 면 "핸들러 펌웨어 동기화 미검증" 을 열린 리스크로 명시**
- UAT-B 항목별 판정 (B-1~B-4)
- 실제 사용한 ServerPort 번호와 `send.py` 경로 (71-04 UAT 에서 재사용)
- `.planning/refs/` 프로토콜 사본 스캔 결과 (구버전 사본 발견 여부 — 편집하지 않았음을 명시)
- 빌드 검증 경로 (정상 빌드 / 스크래치 OutDir / 잠김 보고)
</output>
