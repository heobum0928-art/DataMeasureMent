---
phase: 71-prep-op-plc-off-p-f
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified: []
autonomous: false
requirements: [PROTO-PREP-01]

must_haves:
  truths:
    - "정상 종료(P) 직후 전 조명이 자동 소등되는 것을 사람이 실제로 확인했다"
    - "NG 누적 종료(F) 직후 전 조명이 자동 소등되는 것을 사람이 실제로 확인했다"
    - "Datum 즉시실패(F) 경로에서도 전 조명이 자동 소등되는 것을 사람이 실제로 확인했다"
    - "3개 경로가 [CycleLightOff] 로그에서 서로 구분되어 어느 훅이 탔는지 사후 판별 가능하다"
    - "PLC 가 더 이상 명시적 OFF 를 보내지 않아도 조명이 방치되지 않는다"
  artifacts:
    - path: ".planning/phases/71-prep-op-plc-off-p-f/71-04-SUMMARY.md"
      provides: "3개 소등 시나리오 개별 실기 판정 기록 + phase 71 최종 완료 판정"
  key_links:
    - from: "UAT-E(Datum 즉시실패) 결과"
      to: "71-02 Task 2 의 datum-index0 훅"
      via: "[CycleLightOff] path=datum-index0 로그 확인"
      pattern: "path=datum-index0"
---

<objective>
CONTEXT.md 완료 기준의 **조명 자동소등 3개 시나리오**를 각각 **독립적으로** 실기 검증한다.

**Purpose:** 세 시나리오는 코드상 **서로 다른 종료 경로**를 탄다. 하나가 통과한다고 나머지가 통과하는 게 아니다. 특히 (3) Datum 즉시실패는 `BuildScopedResponse` 를 아예 거치지 않는 별도 경로라 CONTEXT.md 가 "이 경로가 누락되기 쉬우니 반드시 별도 테스트"로 못박은 항목이다. 따라서 하나의 뭉뚱그린 "사이클 끝나면 꺼짐" 테스트가 아니라 **3개의 개별 체크포인트**로 나눈다.

| UAT | 시나리오 | 종료 경로 | 기대 로그 |
|---|---|---|---|
| UAT-C | 정상 종료 P | `BuildScopedResponse` → `ApplyCycleJudgement`(마지막 index, NG 없음) | `path=scoped, result=OK` |
| UAT-D | NG 누적 종료 F | `BuildScopedResponse` → `ApplyCycleJudgement`(마지막 index, `m_bCycleHasNG`) | `path=scoped, result=NG` |
| UAT-E | Datum 즉시실패 F | `HandleDatumIndexResponse` → `BuildDatumShotResponse`(**별도 경로**) | `path=datum-index0, z=0, result=NG` |
| (부록) | 크로스-Z Datum 실패 F | `BuildScopedResponse` → `TryApplyCrossZDatumImmediateFail` | `path=scoped, result=NG` (z>=1) |

**Output:** 71-04-SUMMARY.md 에 기록된 UAT-C/D/E 개별 판정 + Phase 71 최종 완료 판정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-prep-op-plc-off-p-f/71-CONTEXT.md
@.planning/phases/71-prep-op-plc-off-p-f/71-02-PLAN.md
@CLAUDE.md

<prerequisite>
이 plan 은 **71-03 이 전부 PASS 된 뒤에만** 시작한다.
- 71-03 Task 1 의 정적 검증 S1~S12 통과 (특히 **S9** = `datum-index0` 훅 배선 확인 — 이게 실패한 채로 UAT-E 를 돌리면 시간 낭비다)
- 71-03 UAT-B(중간 index 조기 소등 없음) PASS — 조기 소등 결함이 남아있으면 UAT-C/D/E 결과 해석이 무의미하다
- 최신 정상 빌드 산출물로 앱이 실행 가능한 상태 (스크래치 OutDir 컴파일만 했다면 UAT 불가 — 사용자에게 앱 종료 후 재빌드를 먼저 요청한다. **프로세스 임의 종료 금지.**)
</prerequisite>

<repo_state_warning>
`WPF_Example/Halcon/Algorithms/PatternMatchService.cs` 와 `WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs` 는 사용자가 의도적으로 진행 중인 미커밋 실험이다. `git checkout` / `git stash` / `git restore` 금지.
</repo_state_warning>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT-C: 정상 종료(P) 후 전체 조명 소등</name>

  <read_first>
    - `.planning/phases/71-prep-op-plc-off-p-f/71-02-SUMMARY.md` — `[CycleLightOff]` 로그 예시 문자열과 **로그 파일 실제 경로**
    - `.planning/phases/71-prep-op-plc-off-p-f/71-03-SUMMARY.md` — 71-03 전 항목 PASS 여부, ServerPort, `send.py` 경로
  </read_first>

  <action>
71-03 이 전부 PASS 인지 먼저 확인한다. 하나라도 FAIL/미기록이면 이 plan 을 시작하지 않고 그 사실을 보고한다.

그 다음 아래 `<how-to-verify>` 를 사용자에게 **그대로 제시하고 실행을 멈춘다.** 실행자가 대신 판정하거나 결과를 추정하지 않는다.

사용자에게 제시할 때 함께 알려줄 것:
- 로그 파일 전체 경로 (71-02-SUMMARY 기준)
- 이번에 확인할 기대 로그 문자열: `[CycleLightOff] Seq=..., path=scoped, z=<마지막 z_index>, result=OK`
  </action>

  <what-built>
`$PREP` 에서 `Op` 필드를 없앤 대신, **사이클이 P 또는 F 로 확정되는 순간 비전이 스스로 전 조명을 끄도록** 만들었습니다.
PLC 는 이제 소등을 요청할 필요가 없습니다.

이번 항목은 그중 **가장 흔한 경로 — 정상 합격(P)으로 끝나는 사이클**을 확인합니다.
  </what-built>

  <how-to-verify>
**준비**
- 앱 실행(Debug/x64), 조명이 실제로 보이는 상태(실장비 또는 조명 컨트롤러 상태창)
- 로그 파일을 열어둡니다 (71-02-SUMMARY 가 알려준 `LightController` 로그 파일). 테스트 직전 시각을 메모해 두면 찾기 쉽습니다.

***

**C-1. 전부 합격(P)으로 끝나는 사이클을 1회 완주시킵니다.**
- 정상 부품으로, 모든 z_index 를 마지막까지 진행합니다.
- 마지막 index 응답이 `P` 로 나가는 것을 확인합니다.

**C-2. P 확정 직후 조명 상태**
- **합격:** 마지막 index 의 `P` 응답과 **동시에 전 조명 채널이 꺼진다** (Ring / Back / Coax / Bar / Ring7 전부).
- **불합격:** 일부 채널만 꺼짐 / 전혀 안 꺼짐 / 한참 뒤에 꺼짐

**C-3. 로그 확인**
- **합격:** 로그 파일에 다음 형태의 줄이 **정확히 1줄** 찍혀 있다.
  ```
  [CycleLightOff] Seq=<시퀀스명>, path=scoped, z=<마지막 z_index>, result=OK
  ```
- **불합격:** 줄이 없음(훅이 안 탐) / 여러 줄(중복 소등) / `path` 가 `datum-index0`(엉뚱한 경로)

**C-4. 소등 후 다음 사이클이 정상인가 (가장 중요한 회귀 확인)**
- 소등된 상태에서 **다음 부품 사이클을 바로 시작**합니다.
- **합격:** `$PREP` 가 오면서 조명이 다시 켜지고, 첫 촬영 이미지가 어둡지 않으며, 측정값이 정상 범위다.
- **불합격:** 첫 z_index 이미지가 어둡다 / 측정값이 튄다 / 조명이 안 켜진다
- ※ 이게 FAIL 이면 **즉시 중단하고 보고**해 주세요. 조명 재점등 경로가 깨진 것이라 심각합니다.
  </how-to-verify>

  <verify>
    <automated>MISSING — 물리 조명 소등은 사람 눈/조명 컨트롤러로만 확인 가능. 71-03 Task 1 의 S8(훅 2곳 배선) + 71-02 Task 1 acceptance criteria(삽입 위치 검증)가 코드 레벨 대응 게이트다.</automated>
  </verify>

  <acceptance_criteria>
    - C-1 ~ C-4 각각에 개별 PASS/FAIL 이 기록된다
    - C-3 은 **로그에서 실제로 본 줄을 그대로 붙여넣어** 답한다 (`path=scoped`, `result=OK` 확인)
    - C-4 가 FAIL 이면 UAT-D/E 로 진행하지 않는다
  </acceptance_criteria>

  <resume-signal>
C-1 / C-2 / C-3 / C-4 각각에 PASS 또는 FAIL(+증상)을 적어 주세요.
C-3 은 로그에서 본 줄을 그대로 복사해 주시면 가장 확실합니다.
예: `C-1 PASS, C-2 PASS, C-3 PASS ([CycleLightOff] Seq=BOTTOM, path=scoped, z=3, result=OK), C-4 PASS`
  </resume-signal>

  <done>
정상 P 종료 시 전 조명이 소등되고, `path=scoped, result=OK` 로그가 1줄 남으며, 다음 사이클의 재점등에 회귀가 없음이 실기로 확인되었다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT-D: NG 누적 종료(F) 후 전체 조명 소등</name>

  <read_first>
    - UAT-C 결과 (C-4 가 PASS 여야 이 단계 의미가 있다)
    - `.planning/phases/71-prep-op-plc-off-p-f/71-02-SUMMARY.md` 의 로그 예시
  </read_first>

  <action>
UAT-C 가 전부 PASS 인지 확인한 뒤, 아래 `<how-to-verify>` 를 사용자에게 그대로 제시하고 멈춘다.

**이 경로가 UAT-C 와 다른 점을 사용자에게 함께 설명한다:** 같은 `BuildScopedResponse` 훅을 타지만, 판정 결과가 `NG` 라 `ApplyCycleJudgement` 의 **다른 분기**(`m_bCycleHasNG` → `EVisionResultType.NG`)를 거친다. 소등 게이트는 `IsBuffer==false` 하나이므로 P/F 무관하게 동작해야 하지만, 그게 실제로 그런지는 확인해야 한다.
  </action>

  <what-built>
사이클이 **NG 누적으로 F(불합격) 확정**될 때도 조명이 자동 소등되는지 확인합니다.

이 프로젝트의 판정 불변식상, NG 가 중간에 발견돼도 **측정은 마지막 index 까지 계속 진행**되고 종합 판정만 마지막에 F 로 나갑니다. 따라서 소등 시점도 UAT-C(P)와 동일하게 **마지막 index** 여야 합니다 — 중간에 NG 가 났다고 조기에 꺼지면 안 됩니다.
  </what-built>

  <how-to-verify>
**준비** — UAT-C 와 동일 (앱 실행 + 로그 파일 열어두기)

***

**D-1. NG 가 나는 사이클을 1회 완주시킵니다.**
- 공차를 벗어난 부품을 쓰거나, 특정 FAI 항목의 공차를 일시적으로 좁혀 확실히 NG 가 나게 만듭니다.
- **중요:** NG 는 중간 z_index 에서 나야 합니다(마지막 index 에서만 나면 UAT-C 와 구분이 안 됩니다).

**D-2. NG 발생 직후 ~ 마지막 index 직전 구간의 조명**
- **합격:** NG 가 났어도 **조명이 꺼지지 않고** 남은 z_index 촬영이 정상 진행된다. 이 구간 로그에 `[CycleLightOff]` 줄이 **없다.**
- **불합격:** NG 발견 시점에 조기 소등 → 남은 index 가 암영상으로 측정됨. **즉시 중단하고 보고.**

**D-3. 마지막 index 에서 F 확정된 직후 조명 상태**
- **합격:** `F` 응답과 동시에 전 조명 채널이 꺼진다 (UAT-C 와 동일한 소등 동작).
- **불합격:** 일부만 꺼짐 / 안 꺼짐

**D-4. 로그 확인**
- **합격:** 다음 형태의 줄이 **정확히 1줄**:
  ```
  [CycleLightOff] Seq=<시퀀스명>, path=scoped, z=<마지막 z_index>, result=NG
  ```
  UAT-C 와 `path` 는 같고 `result` 만 `OK` → `NG` 로 다릅니다.
- **불합격:** 줄이 없음 / 여러 줄 / `result=OK` (판정 전달이 잘못됨)

**D-5. 공차를 원래대로 되돌립니다** (D-1 에서 임시로 좁혔다면).
- 되돌린 뒤 정상 부품으로 1회 더 돌려 P 가 정상적으로 나오는지 확인해 주세요.
  </how-to-verify>

  <verify>
    <automated>MISSING — 물리 조명 + NG 유도가 필요해 자동화 불가. 71-02 Task 1 의 `!packet.IsBuffer` 단일 게이트(P/F 무관)가 코드 레벨 근거다.</automated>
  </verify>

  <acceptance_criteria>
    - D-1 ~ D-5 각각에 개별 PASS/FAIL 이 기록된다
    - D-2 는 **로그 확인 결과 포함**해서 답한다 (NG 발생 ~ 마지막 index 구간에 `[CycleLightOff]` 없음)
    - D-4 는 로그 줄을 그대로 붙여넣어 답한다 (`result=NG` 확인)
    - D-5(공차 원복)가 수행되었음이 명시된다 — 임시 변경이 레시피에 남지 않도록
  </acceptance_criteria>

  <resume-signal>
D-1 / D-2 / D-3 / D-4 / D-5 각각에 PASS 또는 FAIL(+증상)을 적어 주세요.
D-4 는 로그에서 본 줄을 그대로 복사해 주시면 가장 확실합니다.
예: `D-1 PASS, D-2 PASS(중간 구간 로그 없음), D-3 PASS, D-4 PASS ([CycleLightOff] Seq=BOTTOM, path=scoped, z=3, result=NG), D-5 PASS(공차 원복 완료)`
  </resume-signal>

  <done>
NG 누적 F 종료 시 전 조명이 소등되고, `path=scoped, result=NG` 로그가 1줄 남으며, NG 발견 시점의 조기 소등이 없음이 실기로 확인되었다. 임시 공차 변경은 원복되었다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>UAT-E: Datum 즉시실패(F) 경로에서도 전체 조명 소등 (누락되기 쉬운 별도 경로)</name>

  <read_first>
    - UAT-C / UAT-D 결과
    - `.planning/phases/71-prep-op-plc-off-p-f/71-02-PLAN.md` 의 Task 2 (이 경로가 왜 별도 훅을 필요로 하는지)
    - `.planning/phases/71-prep-op-plc-off-p-f/71-02-SUMMARY.md` 의 `datum-index0` 로그 예시
    - `WPF_Example/Setting/` 에서 `UseProtocolV1` 설정 위치 (이 경로는 `UseProtocolV1=true` 일 때만 동작)
  </read_first>

  <action>
UAT-C / UAT-D 가 전부 PASS 인지 확인한 뒤, 아래 `<how-to-verify>` 를 사용자에게 그대로 제시하고 멈춘다.

**사용자에게 반드시 함께 설명할 것 — 왜 이게 별도 테스트인가:**
> UAT-C/D 는 `BuildScopedResponse` 라는 한 함수를 통과하지만, **Index 0 에서 Datum 검출이 실패해 즉시 F 로 끝나는 경우는 그 함수를 아예 거치지 않습니다.** 완전히 다른 경로(`HandleDatumIndexResponse` → `BuildDatumShotResponse`)라서 훅을 따로 넣었습니다. CONTEXT.md 도 "이 경로가 누락되기 쉬우니 반드시 별도 테스트"로 못박은 항목입니다. **UAT-C/D 가 PASS 여도 이건 독립적으로 실패할 수 있습니다.**
  </action>

  <what-built>
Datum(기준) 검출이 Index 0 에서 실패하면 사이클이 **즉시 F 로 조기 종료**됩니다(뒤의 z_index 는 진행하지 않음).

이 경로는 정상/NG 종료와 **코드상 완전히 다른 길**을 타기 때문에, 조명 소등 훅을 두 번째로 따로 넣었습니다. 이번 항목은 그 두 번째 훅이 실제로 작동하는지를 확인합니다.
  </what-built>

  <how-to-verify>
**준비**
- 앱 실행(Debug/x64), 조명 관찰 가능 상태, 로그 파일 열어두기
- `UseProtocolV1` 설정이 **켜져 있어야** 합니다(이 즉시-F 경로는 v1.0 프로토콜 전용). 꺼져 있으면 이 시나리오 자체가 발생하지 않습니다.

***

**E-1. Index 0 에서 Datum 검출이 실패하도록 만듭니다.**
가장 확실한 방법 중 하나를 선택하세요:
- (a) Datum 이 보이지 않는 부품/빈 지그로 사이클을 시작한다
- (b) Datum ROI 를 실제 특징이 없는 영역으로 일시 이동시킨다
- (c) Datum 티칭 모델을 못 찾게 만든다 (**주의:** `.shm` 파일 이름을 바꾸는 방식은 과거 실사고(quick-260806-nrm)와 같은 상황을 만듭니다. 하려면 반드시 **원복 가능하게** 하고, 끝난 뒤 정확히 되돌려 주세요. (a)/(b) 를 우선 권장합니다.)

**E-2. 사이클을 시작하고 Index 0 에서 즉시 F 가 나가는지 확인**
- **합격:** Index 0 응답이 `F` 로 나가고, 뒤의 z_index 는 진행하지 않는다(기존 동작 그대로).
- **불합격:** `B` 가 나가거나 사이클이 계속 진행됨 → 이건 이번 변경과 무관한 기존 로직 문제일 수 있으니 증상을 그대로 알려 주세요.

**E-3. 즉시 F 직후 조명 상태 — 이번 항목의 핵심**
- **합격:** 즉시 F 와 **동시에 전 조명 채널이 꺼진다** (Ring / Back / Coax / Bar / Ring7 전부).
- **불합격:** 조명이 켜진 채로 남는다 → **두 번째 훅이 안 탄 것**입니다. 즉시 보고해 주세요.

**E-4. 로그 확인 — 경로가 구분되는지**
- **합격:** 다음 형태의 줄이 **정확히 1줄**:
  ```
  [CycleLightOff] Seq=<시퀀스명>, path=datum-index0, z=0, result=NG
  ```
  UAT-C/D 와 달리 **`path` 가 `datum-index0`, `z` 가 `0`** 이어야 합니다. 이게 두 번째 훅이 탔다는 증거입니다.
- **불합격:** 줄이 없음(훅 미작동) / `path=scoped`(엉뚱한 훅) / 여러 줄

**E-5. 정상 Datum 사이클에서는 Index 0 에 소등이 없는지 (반대 방향 확인)**
- E-1 의 조작을 되돌려 **정상 Datum** 으로 사이클을 시작합니다.
- **합격:** Index 0 에서 조명이 꺼지지 않고(=`B` 응답이므로) 사이클이 z_index 1, 2, ... 로 정상 진행된다. Index 0 시점 로그에 `[CycleLightOff]` 줄이 **없다.**
- **불합격:** 정상 Datum 인데 Index 0 에서 소등됨 → 게이트 조건이 잘못된 것. **즉시 보고.**

**E-6. E-1 에서 바꾼 것을 전부 원복했는지 확인**
- ROI 위치 / 부품 / 모델 파일 등 임시 변경 사항을 정확히 되돌렸는지 확인해 주세요.

***

**E-7. (부록, 해당하는 경우만) 크로스-Z Datum 실패 경로**
`EnableCrossZDatumImmediateFail` 이 켜져 있고 크로스-Z Datum(z=1 등에서 완성되는 Datum)을 쓰는 시퀀스(예: SIDE)가 있다면, 그 Datum 을 실패시켜서도 확인해 주세요.
- **합격:** `[CycleLightOff] Seq=SIDE, path=scoped, z=1, result=NG` (이건 첫 번째 훅이 커버하는 경로입니다)
- 해당 없으면 "N/A" 로 답해 주세요.
  </how-to-verify>

  <verify>
    <automated>MISSING — Datum 실패 유도 + 물리 조명 관찰이 필요해 자동화 불가. 71-03 Task 1 의 **S9**(`TryTurnOffLightsOnCycleEnd(datumPacket, "datum-index0", DATUM_Z_INDEX)` 배선 확인)가 코드 레벨 대응 게이트다.</automated>
  </verify>

  <acceptance_criteria>
    - E-1 ~ E-6 각각에 개별 PASS/FAIL 이 기록된다
    - E-4 는 로그 줄을 그대로 붙여넣어 답한다 — **`path=datum-index0` 이 확인되어야 한다** (이게 이 UAT 의 존재 이유)
    - E-5(정상 Datum 에서는 미소등)가 확인된다 — 게이트 조건의 반대 방향 검증
    - E-6(원복 완료)이 명시된다 — 특히 (c) 모델 파일 방식을 썼다면 반드시
    - E-7 은 PASS / FAIL / N/A 중 하나로 기록된다
  </acceptance_criteria>

  <resume-signal>
E-1 / E-2 / E-3 / E-4 / E-5 / E-6 각각에 PASS 또는 FAIL(+증상)을, E-7 은 PASS / FAIL / N/A 로 답해 주세요.
E-4 는 로그에서 본 줄을 그대로 복사해 주시면 가장 확실합니다.
예: `E-1 PASS, E-2 PASS, E-3 PASS, E-4 PASS ([CycleLightOff] Seq=BOTTOM, path=datum-index0, z=0, result=NG), E-5 PASS, E-6 PASS(ROI 원복), E-7 N/A`

**E-3 또는 E-4 가 FAIL 이면** = 두 번째 훅(`HandleDatumIndexResponse`)이 작동하지 않는 것이므로 71-02 Task 2 를 다시 봅니다.
  </resume-signal>

  <done>
Index 0 Datum 즉시실패 경로에서도 전 조명이 소등되고, `path=datum-index0, z=0, result=NG` 로그로 두 번째 훅이 탔음이 입증되었으며, 정상 Datum 에서는 소등되지 않음(E-5)까지 확인되었다. 임시 조작은 전부 원복되었다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT 조작(공차 임시 변경 / ROI 이동 / 모델 파일 조작) → 프로덕션 레시피 | 테스트를 위해 실제 레시피/티칭 데이터를 건드린다. 원복 실패 시 **다음 생산이 전량 오판정**된다. |
| Datum 즉시실패 유도 → 조명/사이클 상태 | 조기 종료 상태에서 장비가 예상 밖 상태로 남을 수 있다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-31 | Tampering | UAT-D 의 임시 공차 축소 / UAT-E 의 ROI·모델 조작이 레시피에 잔존 | **mitigate** | **D-5 와 E-6 을 독립적인 PASS/FAIL 항목으로 만들어** 원복을 절차의 일부로 강제했다. 특히 UAT-E 의 `.shm` 모델 파일 리네임 방식은 2026-08-06 실사고(quick-260806-nrm: SIDE Datum 개명 → 모델 파일 못 찾음 → **모달 없이 조용히** align 실패 → 하위 FAI 전량 Fail)와 동일한 실패 모드를 인위적으로 만드는 것이므로, 절차에서 **(a)/(b) 비파괴 방법을 우선 권장**하고 (c) 선택 시 경고를 명시했다. |
| T-71-32 | Denial of Service | UAT-E 조기 종료 후 장비/사이클 상태가 어중간하게 남음 | **accept** | 즉시-F 조기 종료는 이번 변경 **이전부터 존재하던 정상 프로토콜 동작**(Phase 49 D-04/D-05, 후속 index skip 은 핸들러 주도)이다. 이 phase 는 그 경로에 소등 1줄을 더할 뿐 종료 semantics 를 바꾸지 않는다. 다음 사이클의 `$PREP` 로 상태가 초기화된다. |
| T-71-33 | Denial of Service | UAT-C 의 C-4(소등 후 재점등) 실패가 발견되지 않고 넘어감 | **mitigate** | C-4 를 독립 항목으로 분리하고 "FAIL 이면 즉시 중단하고 보고"를 절차에 명시했다. 소등 자체보다 **재점등이 깨지는 게 훨씬 치명적**(암영상 측정 = 전량 오판)이므로 UAT-D/E 진행 전 게이트로 배치했다. |
| T-71-34 | Repudiation | "다 잘 됐다"로 뭉뚱그려져 어느 경로가 실제로 검증됐는지 불명 | **mitigate** | 3개 시나리오를 **별도 task/체크포인트**로 분리하고, 각 `<resume-signal>` 이 항목별 개별 PASS/FAIL 을 요구하며, C-3/D-4/E-4 는 **로그 원문 붙여넣기**를 요구한다. `path` 값(`scoped` vs `datum-index0`)이 서로 다르므로 로그만으로 어느 훅이 탔는지 위조 없이 판별된다. |
| T-71-35 | Spoofing | 실제로는 UAT-C 경로가 탔는데 UAT-E 로 오인 기록 | **mitigate** | 71-02 가 로그에 `path` / `z` 를 함께 찍도록 설계했다. UAT-E 는 `path=datum-index0` **그리고** `z=0` 두 조건을 동시에 만족해야 PASS 다 — 다른 경로가 이 조합을 만들 수 없다. |
| T-71-36 | Information Disclosure | UAT 로그 노출 값 | **accept** | 시퀀스명 / z_index / P·F 결과뿐. 비밀·PII 아님. 로컬 폐쇄망 산업용 PC. |
| T-71-37 | Elevation of Privilege | — | **accept** | 이 plan 은 코드를 수정하지 않는다(`files_modified: []`). 권한 표면 변화 없음. |
</threat_model>

<verification>
1. UAT-C: C-1~C-4 전부 PASS (C-3 로그 원문 기록, C-4 = 재점등 회귀 0)
2. UAT-D: D-1~D-5 전부 PASS (D-2 = 중간 조기소등 없음, D-4 로그 원문 `result=NG`, D-5 = 공차 원복)
3. UAT-E: E-1~E-6 전부 PASS (E-4 로그 원문 `path=datum-index0` + `z=0`, E-5 = 정상 Datum 미소등, E-6 = 원복), E-7 판정 기록
4. `git status --porcelain` 에 UAT 과정에서 생긴 의도치 않은 리포지토리 변경이 없음
5. 레시피/티칭 데이터가 UAT 시작 전 상태로 원복되었음 (D-5 / E-6)
</verification>

<success_criteria>
- CONTEXT.md 완료 기준의 조명 소등 3 시나리오가 **각각 독립적으로** 실기 PASS
  - 정상 종료(P) 후 전체 소등 — UAT-C
  - NG 누적 종료(F) 후 전체 소등 — UAT-D
  - Datum 즉시실패(F) 경로 소등 — UAT-E (별도 경로, 누락 위험 항목)
- 3개 경로가 `[CycleLightOff]` 로그의 `path`+`z`+`result` 조합으로 서로 구분되어 기록됨
- 소등 후 재점등 회귀 0 (C-4)
- 중간 index / 정상 Datum 에서는 소등되지 않음 (D-2, E-5) — 게이트 조건 양방향 검증
- UAT 를 위한 임시 조작이 전부 원복됨 (D-5, E-6)
</success_criteria>

<output>
완료 후 `.planning/phases/71-prep-op-plc-off-p-f/71-04-SUMMARY.md` 를 생성한다.
SUMMARY 에 반드시 포함할 것:
- UAT-C / UAT-D / UAT-E **항목별 개별 판정 표** (C-1~C-4, D-1~D-5, E-1~E-7)
- 각 시나리오에서 실제로 관측된 `[CycleLightOff]` 로그 **원문 3줄** (C-3 / D-4 / E-4) — 3개 경로가 구분되었음의 증거
- 원복 확인 (D-5 공차, E-6 ROI/모델) — 어떤 방법을 썼고 어떻게 되돌렸는지
- **Phase 71 최종 완료 판정** — CONTEXT.md 완료 기준 7항목 각각에 대해 충족/미충족:
  1. Debug/x64 빌드 PASS (71-03 Task 1)
  2. `$PREP:1,3@` → `$PREP_ACK:1,3,OK@` (71-03 UAT-A)
  3. 한 사이클 내 z_index 다중 전환 회귀 0 (71-03 UAT-B)
  4. 정상 종료(P) 후 전체 소등 (UAT-C)
  5. NG 누적 종료(F) 후 전체 소등 (UAT-D)
  6. Datum 즉시실패(F) 경로 소등 (UAT-E)
  7. 구 3필드 요청 처리 방식 결정 = **D-71-01 관대한 파싱** (71-01 확정 + 71-03 UAT-A 실기 확인)
  8. 프로토콜 문서 갱신 = **불필요**(CONTEXT locked: `디팜스테크_Vision_Protocol_v1.3.xlsx` 가 이미 목표 상태)
- **열린 리스크** — 71-03 UAT-A 의 A-5(핸들러 펌웨어 3필드 파서 동기화)가 `N/A` 였다면 배포 전 반드시 확인해야 할 항목으로 명시
</output>
