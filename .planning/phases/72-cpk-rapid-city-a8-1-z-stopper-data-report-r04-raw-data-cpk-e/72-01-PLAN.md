---
phase: 72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs
autonomous: true
requirements: [72]
must_haves:
  truths:
    - "MeasurementStat 이 Cp / UCpk / LCpk / MinValue / MaxValue 를 노출한다"
    - "StdDev == 0 이면 Cp / UCpk / LCpk / Cpk 가 모두 double.PositiveInfinity 다"
    - "측정키별 원시 측정값 리스트를 외부에서 읽을 수 있다"
    - "기존 ComputeAll() 시그니처와 기존 필드 값이 변하지 않는다(회귀 0)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs"
      provides: "Cp/UCpk/LCpk/MinValue/MaxValue 필드 + GetSeries()"
      contains: "public double Cp { get; set; }"
  key_links:
    - from: "RepeatMeasurementStats.ComputeAll()"
      to: "MeasurementStat.Cp/UCpk/LCpk"
      via: "동일 if (stddev == 0) 가드 블록 내부에서 대입"
      pattern: "cp = double\\.PositiveInfinity"
---

<objective>
`RepeatMeasurementStats` 를 확장해 Cpk 상세 시트가 필요로 하는 통계값 전부를 노출한다.

Purpose: 참고양식 `1Cav 세부치수_Cpk` 의 우측 블록(Maximum/Minimum/Mean/Std Dev/Cp/UCPK/LCPK/Cpk)을 채우려면 현재 `ComputeAll()` 이 계산해 놓고 버리는 `minVal`/`maxVal`/`cpkUpper`/`cpkLower` 를 내보내야 하고, Cp 하나만 신규 계산하면 된다.
Output: `MeasurementStat` 에 5개 필드 추가, `GetSeries()` 신규 메서드.
</objective>

<build_serialization_note>
**이 Phase 의 모든 plan 은 단일 wave 에 1개씩만 배치되어 있으며, 동시 실행 금지다.**
이유: 7개 plan 전부가 동일 `WPF_Example/DatumMeasurement.csproj` 를 동일 `OutputPath`(scratchpad/build72)와 동일 기본 중간 디렉터리(`obj\x64\Debug`)로 전체 빌드한다. 두 plan 을 동시에 돌리면 (a) MSB3021 / CS2012 파일 잠금 충돌, (b) 한쪽이 Task 1 만 끝낸 중간 상태를 다른 쪽 빌드가 컴파일하는 교차 오염이 발생한다. `files_modified` 교집합이 0이어도 빌드 아티팩트는 공유되므로 병렬화하면 안 된다.
</build_serialization_note>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-CONTEXT.md
@.planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-PATTERNS.md

<interfaces>
현재 `MeasurementStat` (RepeatMeasurementStats.cs:13-30) 공개 필드:
```csharp
public string ShotName; public string FAIName; public string MeasurementName; public string TypeName;
public int N; public double Mean; public double StdDev; public double Range; public double Cpk;
public double NominalValue; public double TolerancePlus; public double ToleranceMinus;
public int OkCount; public int NgCount; public int DetectFailCount;
```
(전부 `{ get; set; }` 자동 프로퍼티)

현재 `ComputeAll()` 내부 계산 블록 (RepeatMeasurementStats.cs:146-188) — `minVal`/`maxVal`/`cpkUpper`/`cpkLower` 가 지역변수로만 존재하고 버려짐.

내부 누적 타입 `KeyData` (cs:38-51) 는 `private` 이고 `List<double> Values` 를 보유. `Values` 에는 `LastSkipReason == DATUM_FAIL/NO_IMAGE` 회차가 **들어가지 않는다**(cs:108-123).

**중요(72-06 이 의존):** `AddSample` 은 DATUM_FAIL/NO_IMAGE 만 있는 측정키에 대해서도 `KeyData` 를 만든다(cs:90-101 의 `TryGetValue` 실패 시 무조건 생성). 따라서 `ComputeAll()` 결과에 **N == 0 인 엔트리가 남는다** — 이 경우 Mean/StdDev/Cp/Cpk 등은 전부 0 이다. 소비자는 `stat != null` 만으로 판단하면 안 되고 `stat.N > 0` 까지 확인해야 한다.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MeasurementStat 에 Cp/UCpk/LCpk/MinValue/MaxValue 추가 + ComputeAll 에서 지역변수 승격</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs (전체 213줄)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs</files>
  <action>
1) `MeasurementStat` 클래스(cs:13-30)의 **기존 필드 사이에 끼워넣지 말고** `public double Cpk { get; set; }` 줄 **바로 뒤에** 5줄을 append 한다 (기존 필드 순서 변경 금지 — 다른 export 코드가 object initializer 로 참조 중):
```csharp
        public double Cp { get; set; }
        public double UCpk { get; set; }
        public double LCpk { get; set; }
        public double MinValue { get; set; }
        public double MaxValue { get; set; }
```

2) `ComputeAll()`(cs:133) 초기 지역변수 선언부(cs:141-144, `double cpk = 0;` 줄 뒤)에 다음을 추가한다:
```csharp
                double cp = 0;
                double ucpk = 0;
                double lcpk = 0;
                double minValOut = 0;
                double maxValOut = 0;
```

3) `if (n > 0)` 블록 안(cs:146-188)에서:
   - `range = maxVal - minVal;`(cs:168) **바로 뒤**에 두 줄 추가:
```csharp
                    minValOut = minVal;
                    maxValOut = maxVal;
```
   - 기존 `if (stddev == 0) { cpk = double.PositiveInfinity; } else { ... }` 블록(cs:178-187)을 아래와 **정확히 이 형태**로 교체한다. 가드를 Cpk 와 갈라놓지 말 것(Pitfall 3):
```csharp
                    if (stddev == 0)
                    {
                        cp = double.PositiveInfinity;
                        ucpk = double.PositiveInfinity;
                        lcpk = double.PositiveInfinity;
                        cpk = double.PositiveInfinity;
                    }
                    else
                    {
                        cp = (d.LastTolPlus + Math.Abs(d.LastTolMinus)) / (6 * stddev);
                        double cpkUpper = (usl - mean) / (3 * stddev);
                        double cpkLower = (mean - lsl) / (3 * stddev);
                        ucpk = cpkUpper;
                        lcpk = cpkLower;
                        cpk = Math.Min(cpkUpper, cpkLower);
                    }
```
   - `usl`/`lsl` 선언(cs:176-177)은 그대로 둔다. `stddev == 0` 비교를 엡실론 비교로 바꾸지 말 것(기존 동작 변경 금지).

4) 결과 object initializer(cs:190-207)의 `Cpk = cpk,` 줄 **바로 뒤**에 추가:
```csharp
                    Cp = cp,
                    UCpk = ucpk,
                    LCpk = lcpk,
                    MinValue = minValOut,
                    MaxValue = maxValOut,
```

**금지 사항:** 삼항 연산자(`?:`) 사용 금지 — 전부 if-else. 이 파일은 무접두 지역변수(`mean`/`stddev`/`cpk`) 스타일이므로 신규 지역변수도 **헝가리언 접두 없이** 동일 스타일로 작성한다(파일 내 스타일 혼용 금지). 브레이스는 Allman. C# 7.2 — switch expression / record / nullable reference type 금지.
  </action>
  <acceptance_criteria>
    (모든 grep 에 대상 파일 경로를 명시한다 — 경로 없는 grep 은 stdin 을 기다리며 멈춘다)
    - `grep -c 'public double Cp { get; set; }' WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs` == 1
    - `grep -c 'public double UCpk { get; set; }' WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs` == 1
    - `grep -c 'public double LCpk { get; set; }' WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs` == 1
    - `grep -c 'public double MinValue { get; set; }' WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs` == 1
    - `grep -c 'public double MaxValue { get; set; }' WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs` == 1
    - `grep -c 'cp = (d.LastTolPlus + Math.Abs(d.LastTolMinus)) / (6 \* stddev);' WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs` == 1
    - `grep -c 'cp = double.PositiveInfinity;' WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs` == 1  (Cp 도 동일 가드 안에 있음)
    - `grep -nE '[^?]\? .+ : ' WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs` → 0 matches
    - 기존 필드 회귀 없음: `grep -c 'Range = range,' WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs` == 1
    - 기존 필드 회귀 없음: `grep -c 'Cpk = cpk,' WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs` == 1
  </acceptance_criteria>
  <verify>
    <automated>MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"; "$MSB" 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' -p:Configuration=Debug -p:Platform=x64 -p:OutputPath='C:\Users\tech\AppData\Local\Temp\claude\C--Info-Project-DataMeasurement\9d3a7b4d-2314-4b14-8686-52fd6346a1f9\scratchpad\build72\' -t:Build -v:minimal -nologo</automated>
  </verify>
  <done>빌드 exit 0, 에러 0. 경고는 baseline 12줄(CS0618×10 + CS0162×2) — "경고 0"을 기준으로 삼지 말 것. 빌드 산출물이 잠겨 있어도 프로세스 종료 금지(위 OutPath 는 이미 scratch).</done>
</task>

<task type="auto">
  <name>Task 2: GetSeries() 원시값 노출 메서드 추가</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs (Task 1 적용 후 상태)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs</files>
  <action>
`ComputeAll()` 메서드 **바로 뒤**(클래스 닫는 중괄호 직전)에 다음 메서드를 추가한다. 기존 `ComputeAll()` 시그니처는 절대 변경하지 않는다(호출부 존재: `RepeatExcelExportService.ExportInternal`).

```csharp
        /// <summary>
        /// 측정키별 원시 측정값 리스트를 반환한다. 차트(히스토그램/추이) 렌더 전용.
        /// 내부 리스트를 그대로 주면 외부에서 변조될 수 있으므로 복사본을 만든다.
        /// 주의: DATUM_FAIL / NO_IMAGE 회차는 애초에 누적되지 않으므로 이 리스트로는
        /// "몇 번째 회차 값인지"를 알 수 없다 — RAW DATA 열 정렬에는 쓰지 말 것.
        /// </summary>
        public Dictionary<string, List<double>> GetSeries()
        {
            var result = new Dictionary<string, List<double>>();

            foreach (var kv in _data)
            {
                result[kv.Key] = new List<double>(kv.Value.Values);
            }

            return result;
        }
```

삼항 금지, Allman 브레이스, C# 7.2.
  </action>
  <acceptance_criteria>
    - `grep -c 'public Dictionary<string, List<double>> GetSeries()' WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs` == 1
    - `grep -c 'new List<double>(kv.Value.Values)' WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs` == 1
    - `grep -c 'public Dictionary<string, MeasurementStat> ComputeAll()' WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs` == 1  (기존 시그니처 유지)
    - `grep -nE '[^?]\? .+ : ' WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs` → 0 matches
  </acceptance_criteria>
  <verify>
    <automated>MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"; "$MSB" 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' -p:Configuration=Debug -p:Platform=x64 -p:OutputPath='C:\Users\tech\AppData\Local\Temp\claude\C--Info-Project-DataMeasurement\9d3a7b4d-2314-4b14-8686-52fd6346a1f9\scratchpad\build72\' -t:Build -v:minimal -nologo</automated>
  </verify>
  <done>빌드 exit 0, 에러 0, 경고 12줄 baseline 유지.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (없음) | 이 plan 은 in-process 순수 계산만 변경. 외부 입력/경계 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Denial of Service | `GetSeries()` 리스트 복사 | accept | 반복검사 1회당 최대 수백 항목 × 수백 값 — 메모리/시간 무시 가능. export 1회당 1콜 |
</threat_model>

<verification>
1. msbuild Debug/x64 exit 0, CS 에러 0, 경고 12줄 baseline
2. `grep -nE '[^?]\? .+ : ' WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs` → 0
3. 기존 export 회귀 0: `RepeatExcelExportService.cs` 무수정 확인 (`git diff --name-only` 에 미포함)
</verification>

<success_criteria>
- `MeasurementStat` 에 Cp/UCpk/LCpk/MinValue/MaxValue 5필드 존재
- Cp 는 `(TolPlus + |TolMinus|) / (6 * StdDev)`, stddev==0 시 Cp/UCpk/LCpk/Cpk 전부 PositiveInfinity
- `GetSeries()` 가 복사본 Dictionary 반환
- 빌드 PASS
</success_criteria>

<output>
완료 후 `.planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-01-SUMMARY.md` 생성
</output>
