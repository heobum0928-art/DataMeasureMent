---
phase: 72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs
  - WPF_Example/Custom/Sequence/Inspection/BatchRunService.cs
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs
autonomous: true
requirements: [72]
must_haves:
  truths:
    - "폴더 반복검사 시작 전에 자재번호를 입력하면 그 회차의 CycleResultDto.IndexNumber 에 값이 들어간다"
    - "자재번호를 비워두면 기존과 동일하게 -1(미지정)이 유지된다"
    - "'이전 결과에 누적' 체크 시 자재 A N장 → 자재 B N장 순차 실행 결과가 하나의 리스트에 합쳐진다"
    - "TCP $TEST 경로의 자재번호 전파는 영향을 받지 않는다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs"
      provides: "MaterialIndexNumber 프로퍼티 + BuildDto 6번째 인자 전달"
      contains: "public int MaterialIndexNumber"
    - path: "WPF_Example/UI/Reviewer/ReviewerWindow.xaml"
      provides: "자재번호 입력 TextBox + 누적 CheckBox"
      contains: "txt_materialIndex"
  key_links:
    - from: "ReviewerWindow.Button_RepeatRun_Click"
      to: "RepeatRunService.MaterialIndexNumber"
      via: "StartFromImages 호출 직전 대입"
      pattern: "_repeatService\\.MaterialIndexNumber = nMaterialIndex"
    - from: "RepeatRunService.HandleFinish"
      to: "CycleResultSerializer.BuildDto"
      via: "6번째 인자 nIndexNumber"
      pattern: "recipeName, seqName, MaterialIndexNumber\\)"
---

<objective>
D-05: 오프라인 폴더 반복검사에 자재번호 입력 경로를 추가한다.

Purpose: `RepeatRunService`/`BatchRunService` 가 `BuildDto()` 호출 시 `nIndexNumber` 를 안 넘겨서 결과 DTO 의 `IndexNumber` 가 **항상 -1** 이다. 그래서 사무실(SIMUL)에서 "RAW DATA 에 자재별 열이 분리되는지"를 검증할 방법이 없다. 자재번호 입력 + 누적 실행을 붙여 검증 가능하게 만든다.
Output: 두 서비스에 `MaterialIndexNumber` 프로퍼티, ReviewerWindow 에 입력 TextBox + 누적 CheckBox.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-CONTEXT.md
@.planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-PATTERNS.md

<interfaces>
`CycleResultSerializer.BuildDto` (CycleResultSerializer.cs:35-41) — **수정 불필요**, 6번째 optional 파라미터가 이미 존재:
```csharp
        public static CycleResultDto BuildDto(
            InspectionRecipeManager recipeManager,
            EVisionResultType cycleResult,
            DateTime when,
            string recipeName,
            string ownerSequenceName = null,
            int nIndexNumber = -1)
```

현재 호출부 (둘 다 `lock (_lock)` 안, 인자 5개만 전달):
- `RepeatRunService.cs:235-236`
- `BatchRunService.cs:159-160`
```csharp
                CycleResultDto dto = CycleResultSerializer.BuildDto(
                    recipeManager, resultType, DateTime.Now, recipeName, seqName);
```

`CycleResultDto.IndexNumber` (CycleResultDto.cs:19): `public int IndexNumber { get; set; } = -1;`  — -1 = 미수신 sentinel.

`ReviewerWindow.xaml` 좌측 StackPanel (28-48): `btn_loadFolder` / `btn_exportExcel` / `Separator` / `btn_repeatRun` / `lbl_repeatProgress` / `btn_repeatExport`.
`ReviewerWindow.xaml.cs`: `Button_RepeatRun_Click`(324-410) — 폴더 선택 → 이미지 목록 → activeSeq 탐색 → `_repeatService = new RepeatRunService()` → 이벤트 구독 → `StartFromImages(activeSeq, imagePaths)`. `OnRepeatComplete` 에서 `_repeatCycles = cycles;`(402).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RepeatRunService / BatchRunService 에 MaterialIndexNumber 전파</name>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs (전체)
    - WPF_Example/Custom/Sequence/Inspection/BatchRunService.cs (전체)
  </read_first>
  <files>WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs, WPF_Example/Custom/Sequence/Inspection/BatchRunService.cs</files>
  <action>
두 파일은 의도적 쌍둥이 구조다 — **동일한 변경을 양쪽에 같은 형태로** 넣는다.

**A) `RepeatRunService.cs`**
1) `public const int DEFAULT_REPEAT_COUNT = 50;`(cs:19) 바로 뒤에 센티널 상수 추가 (-1 인라인 금지):
```csharp
        /// <summary>자재번호 미지정 sentinel. CycleResultDto.IndexNumber 기본값과 동일.</summary>
        public const int MATERIAL_NOT_SET = -1;
```
2) `public int TargetCount { get; private set; }`(cs:29) 바로 뒤에 프로퍼티 추가:
```csharp
        /// <summary>
        /// 이번 실행에 부여할 자재번호. Start/StartFromImages 호출 전에 설정한다.
        /// MATERIAL_NOT_SET(-1) 이면 기존 동작 그대로(미지정) — TCP $TEST 경로와 무관.
        /// </summary>
        public int MaterialIndexNumber { get; set; } = MATERIAL_NOT_SET;
```
3) `HandleFinish` 안의 `BuildDto` 호출(cs:235-236)을 아래로 교체한다. `MaterialIndexNumber` 읽기가 기존 `lock (_lock)` 블록 안에 있는지 확인할 것:
```csharp
                CycleResultDto dto = CycleResultSerializer.BuildDto(
                    recipeManager, resultType, DateTime.Now, recipeName, seqName, MaterialIndexNumber);
```
4) `Stop()`(cs:103-114) 에서 `MaterialIndexNumber` 는 리셋하지 **않는다** — 다음 실행 시작 시 호출자가 다시 지정하는 계약이다.

**B) `BatchRunService.cs`** — 동일 3단계:
1) 클래스 필드 영역 상단(`public bool IsRunning` 앞)에 `public const int MATERIAL_NOT_SET = -1;` + 동일 summary.
2) `public int TargetCount { get; private set; }`(cs:28) 뒤에 동일한 `MaterialIndexNumber` 프로퍼티.
3) `BuildDto` 호출(cs:159-160)을 `..., recipeName, seqName, MaterialIndexNumber);` 로 교체.
(BatchRunService 는 이번 Phase 에서 UI 입력을 붙이지 않는다 — 쌍둥이 파일 정합만 유지. 기본값 -1 이므로 회귀 0.)

**금지 사항:** 삼항(`?:`) 금지. 두 파일 모두 Allman 브레이스. 헝가리언 유지(`_` 접두 private 필드). C# 7.2 — `MaterialIndexNumber { get; set; } = MATERIAL_NOT_SET;` 형태의 자동 프로퍼티 초기화는 C# 6 기능이라 허용됨.
  </action>
  <acceptance_criteria>
    - `grep -c 'public int MaterialIndexNumber { get; set; } = MATERIAL_NOT_SET;' WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs` == 1
    - `grep -c 'public int MaterialIndexNumber { get; set; } = MATERIAL_NOT_SET;' WPF_Example/Custom/Sequence/Inspection/BatchRunService.cs` == 1
    - `grep -c 'recipeName, seqName, MaterialIndexNumber);' WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs` == 1
    - `grep -c 'recipeName, seqName, MaterialIndexNumber);' WPF_Example/Custom/Sequence/Inspection/BatchRunService.cs` == 1
    - `grep -c 'MATERIAL_NOT_SET = -1' WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs` == 1
    - `grep -rc 'recipeName, seqName);' WPF_Example/Custom/Sequence/Inspection/` → RepeatRunService/BatchRunService 에서 0
    - `grep -nE '[^?]\? .+ : ' WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs WPF_Example/Custom/Sequence/Inspection/BatchRunService.cs` → 0 matches
  </acceptance_criteria>
  <verify>
    <automated>MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"; "$MSB" 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' -p:Configuration=Debug -p:Platform=x64 -p:OutputPath='C:\Users\tech\AppData\Local\Temp\claude\C--Info-Project-DataMeasurement\9d3a7b4d-2314-4b14-8686-52fd6346a1f9\scratchpad\build72\' -t:Build -v:minimal -nologo</automated>
  </verify>
  <done>빌드 exit 0, 에러 0, 경고 12줄 baseline. `InspectionSequence.cs` 의 다른 3개 BuildDto 호출부는 무수정(TCP 경로 회귀 0).</done>
</task>

<task type="auto">
  <name>Task 2: ReviewerWindow 에 자재번호 입력 + 누적 실행 UI 추가</name>
  <read_first>
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml (라인 20-50)
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs (라인 300-460)
  </read_first>
  <files>WPF_Example/UI/Reviewer/ReviewerWindow.xaml, WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs</files>
  <action>
**A) `ReviewerWindow.xaml`** — `<Separator Margin="0,6,0,6"/>`(37) 과 `<Button x:Name="btn_repeatRun" ...>`(38) **사이**에 아래 3요소를 삽입한다. 기존 스타일 관례 준수(`FontSize="11"`, `Foreground="#334155"`, `Margin="0,4,0,0"`, `HorizontalAlignment="Stretch"`):
```xml
                <TextBlock Text="자재번호 (비우면 미지정)"
                           FontSize="11" Foreground="#334155"
                           Margin="0,0,0,2"/>
                <TextBox x:Name="txt_materialIndex" Text=""
                         FontSize="11" Padding="4,2"
                         Margin="0,0,0,4" HorizontalAlignment="Stretch"/>
                <CheckBox x:Name="chk_repeatAccumulate" Content="이전 결과에 누적"
                          FontSize="11" Foreground="#334155"
                          Margin="0,0,0,4"/>
```

**B) `ReviewerWindow.xaml.cs`** — `Button_RepeatRun_Click`(324) 수정:

1) `imagePaths.Count == 0` 가드(356-361) **직후**, `activeSeq` 탐색(363) **직전**에 자재번호 파싱 블록을 넣는다. 삼항 금지, `int.TryParse` 사용, 빈 입력은 미지정 폴백:
```csharp
            int nMaterialIndex = RepeatRunService.MATERIAL_NOT_SET;
            string szMaterial = txt_materialIndex.Text;
            if (!string.IsNullOrWhiteSpace(szMaterial))
            {
                if (!int.TryParse(szMaterial.Trim(), out nMaterialIndex))
                {
                    CustomMessageBox.Show("반복 검사", "자재번호는 숫자만 입력하세요.", MessageBoxImage.Warning);
                    return;
                }
            }
```

2) `_repeatCycles = null;`(385) 줄을 **조건부**로 바꾼다 — 누적 체크 시 이전 결과를 보존해야 자재 2종 열 분리를 검증할 수 있다:
```csharp
            bool bAccumulate = chk_repeatAccumulate.IsChecked == true;
            if (!bAccumulate)
            {
                _repeatCycles = null;
            }

            List<CycleResultDto> prevCycles = _repeatCycles;
```
(`btn_repeatExport.IsEnabled = false;`(386) 이하 줄들은 그대로 유지)

3) `_repeatService = new RepeatRunService();`(390) **직후** 한 줄 추가:
```csharp
            _repeatService.MaterialIndexNumber = nMaterialIndex;
```

4) `OnRepeatComplete` 핸들러(398-407) 안의 `_repeatCycles = cycles;`(402) 를 누적 병합으로 교체한다. `Dispatcher.Invoke` 마샬링은 **그대로 유지**:
```csharp
                    if (bAccumulate && prevCycles != null && prevCycles.Count > 0)
                    {
                        var merged = new List<CycleResultDto>(prevCycles);
                        if (cycles != null)
                        {
                            merged.AddRange(cycles);
                        }

                        _repeatCycles = merged;
                    }
                    else
                    {
                        _repeatCycles = cycles;
                    }

                    int nTotal = 0;
                    if (_repeatCycles != null)
                    {
                        nTotal = _repeatCycles.Count;
                    }

                    lbl_repeatProgress.Text = "완료: " + nTotal + "회 (누적)";
                    btn_repeatRun.Content = "이미지 폴더 반복 검사";
                    btn_repeatExport.IsEnabled = nTotal > 0;
```
   기존의 `lbl_repeatProgress.Text = "완료: " + (cycles != null ? cycles.Count : 0) + "회";` 줄은 **삼항이므로 삭제**하고 위 if-else 형태로 대체한다.
   기존 `btn_repeatExport.IsEnabled = (cycles != null && cycles.Count > 0);` 도 위 `nTotal > 0` 로 대체.

5) `_repeatService.StartFromImages(activeSeq, imagePaths);`(409) 는 그대로.

**금지 사항:** 삼항(`?:`) 절대 금지 — 이 task 는 기존 삼항 1개를 제거하는 것도 포함한다. 이 파일은 **Allman** 브레이스. 헝가리언(`nMaterialIndex`/`szMaterial`/`bAccumulate`). C# 7.2 — `out nMaterialIndex` 는 사전 선언된 변수에 대한 out 이므로 OK.
  </action>
  <acceptance_criteria>
    - `grep -c 'x:Name="txt_materialIndex"' WPF_Example/UI/Reviewer/ReviewerWindow.xaml` == 1
    - `grep -c 'x:Name="chk_repeatAccumulate"' WPF_Example/UI/Reviewer/ReviewerWindow.xaml` == 1
    - `grep -c '_repeatService.MaterialIndexNumber = nMaterialIndex;' WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs` == 1
    - `grep -c 'int.TryParse(szMaterial.Trim(), out nMaterialIndex)' WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs` == 1
    - `grep -c 'merged.AddRange(cycles);' WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs` == 1
    - `grep -c 'cycles != null ? cycles.Count : 0' WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs` == 0  (기존 삼항 제거됨)
    - `grep -nE '[^?]\? .+ : ' WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs` → 0 matches
    - `grep -c 'Dispatcher.Invoke' WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs` >= 2 (마샬링 유지)
  </acceptance_criteria>
  <verify>
    <automated>MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"; "$MSB" 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' -p:Configuration=Debug -p:Platform=x64 -p:OutputPath='C:\Users\tech\AppData\Local\Temp\claude\C--Info-Project-DataMeasurement\9d3a7b4d-2314-4b14-8686-52fd6346a1f9\scratchpad\build72\' -t:Build -v:minimal -nologo</automated>
  </verify>
  <done>빌드 exit 0, XAML 컴파일 에러 0, CS 에러 0, 경고 12줄 baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 → `txt_materialIndex` | 로컬 오퍼레이터가 입력하는 자유 텍스트 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-03 | Tampering | `txt_materialIndex` 자유 텍스트가 시트명/파일명으로 흘러감 | mitigate | `int.TryParse` 로 정수만 통과시키고 실패 시 경고 후 return — 문자열 injection 경로 원천 차단 |
| T-72-04 | Denial of Service | 누적 체크로 무한 누적 시 메모리 증가 | accept | 오퍼레이터가 명시적으로 체크해야 하며 창 닫으면 해제. 실사용 규모(수백 cycle) 무해 |
</threat_model>

<verification>
1. msbuild Debug/x64 exit 0, 에러 0, 경고 12줄 baseline
2. `grep -nE '[^?]\? .+ : '` 로 수정 4파일 전부 0 matches
3. 회귀: `InspectionSequence.cs` 무수정, `CycleResultSerializer.cs` 무수정
</verification>

<success_criteria>
- 자재번호 입력 → 해당 회차 DTO 의 IndexNumber 에 반영
- 빈 입력 → -1 유지 (기존 동작)
- 누적 체크 → 두 번의 실행 결과가 하나의 `_repeatCycles` 로 합쳐짐
- 빌드 PASS
</success_criteria>

<output>
완료 후 `.planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-03-SUMMARY.md` 생성
</output>
