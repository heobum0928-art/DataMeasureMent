---
phase: 72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e
plan: 05
type: execute
wave: 3
depends_on: ["72-01", "72-04"]
files_modified:
  - WPF_Example/Custom/Export/CpkReportExportService.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [72]
must_haves:
  truths:
    - "export 하면 워크북에 'RAW DATA(1)' 시트가 정확히 1장 생성된다"
    - "1행 = FAI 측정 항목, 열 = 샘플(#1,#2,#3...)인 가로형 매트릭스다"
    - "샘플 열은 자재번호(IndexNumber) 오름차순으로 정렬되고 4행에 자재 라벨이 표기된다"
    - "측정되지 않은 칸은 0 이 아니라 '-' 로 표기된다"
    - "'검사성적서'/'2Cav'/'RAW DATA(2)'/'안내사항' 시트는 만들지 않는다"
  artifacts:
    - path: "WPF_Example/Custom/Export/CpkReportExportService.cs"
      provides: "ExportCpkReport 진입점 + RAW DATA 가로형 pivot"
      contains: "RAW_SHEET_NAME = \"RAW DATA(1)\""
    - path: "WPF_Example/DatumMeasurement.csproj"
      provides: "신규 파일 컴파일 등록"
      contains: "Custom\\Export\\CpkReportExportService.cs"
  key_links:
    - from: "CpkReportExportService.BuildRawRows"
      to: "RepeatMeasurementStats.AddSample 키 규칙"
      via: "ShotName/FAIName/MeasurementName 동일 문자열 조합"
      pattern: "\\(shot\\.ShotName \\?\\? \"\"\\) \\+ \"/\" \\+ \\(fai\\.FAIName \\?\\? \"\"\\) \\+ \"/\" \\+ \\(m\\.MeasurementName \\?\\? \"\"\\)"
---

<objective>
신규 `CpkReportExportService` 를 만들고 `RAW DATA(1)` 가로형 매트릭스 시트를 구현한다.

Purpose: 고객 참고양식의 1번 산출물. 기존 "회차별 상세"는 세로형(1행 = 1측정×1회차)이라 재사용이 불가능하고, 근본적으로 다른 pivot 이 필요하다.
Output: `CpkReportExportService.cs` (RAW DATA 시트까지), csproj 등록.

**D-04 (REVISED) 계약 — 절대 위반 금지:** 시트는 **2장 고정**(`RAW DATA(1)`, `1Cav 세부치수_Cpk`). 자재번호는 **열 축**이지 시트 축이 아니다. RESEARCH.md Pattern 4 의 "자재번호별 동적 시트 생성(GroupBy → Worksheets.Add)"은 **폐기된 설계**다. `GroupBy` 는 열 순서 결정에만 쓴다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-CONTEXT.md
@.planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-PATTERNS.md
@.planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-01-SUMMARY.md

<interfaces>
DTO 계층 (`WPF_Example/UI/ViewModel/CycleResultDto.cs`, `namespace ReringProject.UI`):
```csharp
public class CycleResultDto {
    public DateTime InspectionTime; public string RecipeName;
    public int IndexNumber = -1;              // 자재번호. -1 = 미지정
    public string OverallJudgement; public string CycleFolderPath;
    public List<ShotResultDto> Shots;
}
public class ShotResultDto { public string ShotName; public string OwnerSequenceName; public string ResultImagePath; public List<FaiResultDto> FAIs; }
public class FaiResultDto { public string FAIName; public bool IsPass; public bool WasDatumSkipped; public List<MeasurementResultDto> Measurements; /* 외 이미지 필드 */ }
public class MeasurementResultDto {
    public string MeasurementName; public string TypeName;
    public double NominalValue; public double TolerancePlus; public double ToleranceMinus;
    public double LastMeasuredValue; public bool LastJudgement;
    public bool LastHasResult;                // 0.0 도 정상값 → 반드시 이 플래그로 판별 (CO-23-01)
    public string LastSkipReason;             // null / SkipReason.DATUM_FAIL / SkipReason.NO_IMAGE ...
    public bool IsDualImage; /* 외 경로 필드 */
}
```
(전부 `{ get; set; }` 자동 프로퍼티)

`SkipReason` (`WPF_Example/Custom/Sequence/Inspection/SkipReason.cs`, `namespace ReringProject.Sequence`): `DATUM_FAIL`, `ALIGN_FAIL`, `NO_IMAGE`, `DATUM_REF_MISSING`, `ZINDEX_MISCONFIGURED`, `CROSS_Z_INCOMPLETE` — 전부 `public const string`.

기존 export 서비스 관례 원본: `WPF_Example/Custom/Export/RepeatExcelExportService.cs`
- `namespace ReringProject.Export`, `public static class`
- 가드 3종: `if (cycles == null || cycles.Count == 0 || string.IsNullOrEmpty(outputPath)) { return false; }`
- `using (var wb = new XLWorkbook()) { ... wb.SaveAs(outputPath); }` → `return true;`
- catch: `Logging.PrintErrLog((int)ELogType.Error, "[태그] ...")` 를 다시 bare `catch { }` 로 감싸고 `return false;`
- 셀 기록은 `.Value` 만 (`FormulaA1` 금지 — D-02), 소수는 `Math.Round(x, 6)`
- 시트 마감 `ws.Columns().AdjustToContents();`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CpkReportExportService 골격 + 샘플 열/RAW 행 pivot 빌더 + csproj 등록</name>
  <read_first>
    - WPF_Example/Custom/Export/RepeatExcelExportService.cs (라인 1-130, 200-220 — 골격/에러처리/헤더루프 관례)
    - WPF_Example/UI/ViewModel/CycleResultDto.cs
    - WPF_Example/DatumMeasurement.csproj (라인 255-265)
  </read_first>
  <files>WPF_Example/Custom/Export/CpkReportExportService.cs, WPF_Example/DatumMeasurement.csproj</files>
  <action>
1) `WPF_Example/Custom/Export/CpkReportExportService.cs` 신규 생성:

```csharp
using ClosedXML.Excel;
using ReringProject.Sequence;
using ReringProject.Setting;
using ReringProject.UI;
using ReringProject.Utility;
using System;
using System.Collections.Generic;
using System.Linq;

namespace ReringProject.Export
{
    /// <summary>
    /// 고객 참고양식(Rapid City A8.1_Z Stopper Data Report R04) 대조 CPK 리포트 export.
    /// 시트는 2장 고정: "RAW DATA(1)"(가로형 매트릭스) + "1Cav 세부치수_Cpk"(통계).
    /// 자재번호(CycleResultDto.IndexNumber)는 열 축이며 시트 축이 아니다.
    /// 예외는 전부 try/catch → false + Logging (기존 export 관례 동일).
    /// </summary>
    public static class CpkReportExportService
    {
        ...
    }
}
```

2) 상수 (매직넘버 인라인 금지):
```csharp
        private const string RAW_SHEET_NAME = "RAW DATA(1)";

        private const int RAW_MATERIAL_ROW = 4;        // 자재 라벨 행
        private const int RAW_HEADER_ROW = 5;          // 고정 헤더 + #n 헤더 행
        private const int RAW_FIRST_DATA_ROW = 6;
        private const int RAW_FIRST_SAMPLE_COLUMN = 7; // A~F 가 고정 6열이므로 샘플은 G(7)부터

        private const string NO_VALUE_TEXT = "-";
        private const string MATERIAL_UNSET_LABEL = "미지정";
        private const int MATERIAL_NOT_SET = -1;
```

3) 내부 모델 2개 (`private class`, 필드 스타일은 `RepeatExcelExportService.AlgoAggData` 관례대로 프로퍼티 아닌 public 필드):
```csharp
        /// <summary>RAW DATA 의 샘플 열 1개. cycle 1개 = 열 1개.</summary>
        private class SampleColumn
        {
            public int MaterialIndex;      // CycleResultDto.IndexNumber (-1 = 미지정)
            public string HeaderLabel;     // "#1", "#2", ...
            public string MaterialLabel;   // "자재 3" 또는 "미지정"
            public CycleResultDto Cycle;
        }

        /// <summary>RAW DATA 의 데이터 행 1개 = 측정 항목 1개. Values/HasValues 는 SampleColumn 리스트와 인덱스 정렬.</summary>
        private class RawRow
        {
            public string Key;
            public string FAIName;
            public string MeasurementName;
            public string TypeName;
            public double NominalValue;
            public double TolerancePlus;
            public double ToleranceMinus;
            public List<double> Values = new List<double>();
            public List<bool> HasValues = new List<bool>();
        }
```

4) 샘플 열 빌더 — **자재번호 오름차순, 같은 자재 안에서는 원래 순서**. `GroupBy` 는 여기서만 쓰고 시트를 나누는 데는 절대 쓰지 않는다:
```csharp
        /// <summary>cycle 목록을 자재번호 오름차순(같은 자재 내에서는 입력 순서)으로 정렬해 샘플 열을 만든다.</summary>
        private static List<SampleColumn> BuildSampleColumns(List<CycleResultDto> cycles)
        {
            var indexed = new List<KeyValuePair<int, CycleResultDto>>();
            for (int i = 0; i < cycles.Count; i++)
            {
                indexed.Add(new KeyValuePair<int, CycleResultDto>(i, cycles[i]));
            }

            var ordered = indexed
                .OrderBy(p => p.Value.IndexNumber)
                .ThenBy(p => p.Key)
                .ToList();

            var columns = new List<SampleColumn>();
            for (int i = 0; i < ordered.Count; i++)
            {
                var cycle = ordered[i].Value;

                string szMaterial;
                if (cycle.IndexNumber >= 0)
                {
                    szMaterial = "자재 " + cycle.IndexNumber;
                }
                else
                {
                    szMaterial = MATERIAL_UNSET_LABEL;
                }

                var col = new SampleColumn();
                col.MaterialIndex = cycle.IndexNumber;
                col.HeaderLabel = "#" + (i + 1);
                col.MaterialLabel = szMaterial;
                col.Cycle = cycle;
                columns.Add(col);
            }

            return columns;
        }
```

5) 행 pivot 빌더 — 4중 루프 + 레벨별 `continue` 가드(프로젝트 표준). **키 규칙은 `RepeatMeasurementStats.AddSample`(cs:88)과 글자 그대로 동일해야** 72-06 의 통계 조인이 성립한다. 각 열 처리 전에 모든 기존 행을 현재 열 인덱스까지 패딩해야 열이 밀리지 않는다:
```csharp
        /// <summary>
        /// 샘플 열 순서대로 순회하며 측정항목별 가로형 행을 만든다.
        /// 주의: RepeatMeasurementStats.GetSeries() 는 DATUM_FAIL/NO_IMAGE 회차를 누락시키므로
        /// 열 정렬에 쓸 수 없다 — 그래서 여기서 cycle 인덱스 기반으로 직접 pivot 한다.
        /// </summary>
        private static List<RawRow> BuildRawRows(List<SampleColumn> columns)
        {
            var map = new Dictionary<string, RawRow>();
            var order = new List<string>();

            for (int nCol = 0; nCol < columns.Count; nCol++)
            {
                var cycle = columns[nCol].Cycle;
                if (cycle == null || cycle.Shots == null)
                {
                    continue;
                }

                foreach (var shot in cycle.Shots)
                {
                    if (shot == null || shot.FAIs == null)
                    {
                        continue;
                    }

                    foreach (var fai in shot.FAIs)
                    {
                        if (fai == null || fai.Measurements == null)
                        {
                            continue;
                        }

                        foreach (var m in fai.Measurements)
                        {
                            if (m == null)
                            {
                                continue;
                            }

                            string key = (shot.ShotName ?? "") + "/" + (fai.FAIName ?? "") + "/" + (m.MeasurementName ?? "");

                            RawRow row;
                            if (!map.TryGetValue(key, out row))
                            {
                                row = new RawRow();
                                row.Key = key;
                                row.FAIName = fai.FAIName ?? "";
                                row.MeasurementName = m.MeasurementName ?? "";
                                row.TypeName = m.TypeName ?? "";
                                map[key] = row;
                                order.Add(key);
                            }

                            // 최신 레시피 공차로 갱신 (RepeatMeasurementStats 와 동일 정책)
                            row.NominalValue = m.NominalValue;
                            row.TolerancePlus = m.TolerancePlus;
                            row.ToleranceMinus = m.ToleranceMinus;

                            PadRowTo(row, nCol);

                            // 0.0 도 정상 결과이므로 값이 아니라 LastHasResult 로 판별한다 (CO-23-01).
                            if (m.LastHasResult)
                            {
                                row.Values.Add(m.LastMeasuredValue);
                                row.HasValues.Add(true);
                            }
                            else
                            {
                                row.Values.Add(0.0);
                                row.HasValues.Add(false);
                            }
                        }
                    }
                }
            }

            var result = new List<RawRow>();
            foreach (var key in order)
            {
                RawRow row = map[key];
                PadRowTo(row, columns.Count);
                result.Add(row);
            }

            return result;
        }

        /// <summary>행의 값 목록을 nTargetCount 길이까지 "값 없음"으로 채운다. 열 밀림 방지.</summary>
        private static void PadRowTo(RawRow row, int nTargetCount)
        {
            while (row.Values.Count < nTargetCount)
            {
                row.Values.Add(0.0);
                row.HasValues.Add(false);
            }
        }
```

6) 공개 진입점 (이번 plan 에서는 RAW 시트만 Add — 72-06 이 두 번째 시트를 이 메서드 안에 추가한다):
```csharp
        /// <summary>
        /// CPK 데이터 리포트를 xlsx 로 저장한다. 시트 2장 고정.
        /// 실패 시 false + 에러 로그 (throw 금지).
        /// </summary>
        public static bool ExportCpkReport(List<CycleResultDto> cycles, string recipeName, string outputPath)
        {
            if (cycles == null || cycles.Count == 0 || string.IsNullOrEmpty(outputPath))
            {
                return false;
            }

            try
            {
                var columns = BuildSampleColumns(cycles);
                var rows = BuildRawRows(columns);

                using (var wb = new XLWorkbook())
                {
                    var wsRaw = wb.Worksheets.Add(RAW_SHEET_NAME);
                    WriteRawDataSheet(wsRaw, columns, rows, recipeName);

                    wb.SaveAs(outputPath);
                }

                return true;
            }
            catch (Exception ex)
            {
                try
                {
                    Logging.PrintErrLog((int)ELogType.Error, "[CpkReportExportService] ExportCpkReport failed: " + ex.Message);
                }
                catch { }

                return false;
            }
        }
```

7) csproj 라인 258-261 의 `Custom\Export` 블록에 알파벳 순 삽입 (`ChartImageCapture` 뒤, `ExcelExportService` 앞):
```xml
    <Compile Include="Custom\Export\CpkReportExportService.cs" />
```

**금지 사항:** 삼항(`?:`) 절대 금지 — 특히 `cycle.IndexNumber >= 0 ? ... : ...` 형태는 RESEARCH.md 예시 코드에 있지만 **규칙 위반 예시**다. `FormulaA1` 사용 금지. `wb.Worksheets.Add` 호출은 이 Phase 전체에서 **최대 2회**(RAW + Cpk). Allman 브레이스, 헝가리언(`n`/`d`/`sz`/`b`), C# 7.2.
  </action>
  <acceptance_criteria>
    - 파일 존재: `WPF_Example/Custom/Export/CpkReportExportService.cs`
    - `grep -c 'RAW_SHEET_NAME = "RAW DATA(1)"' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1
    - `grep -c 'public static bool ExportCpkReport(List<CycleResultDto> cycles, string recipeName, string outputPath)'` == 1
    - `grep -c 'private static List<SampleColumn> BuildSampleColumns'` == 1
    - `grep -c 'private static List<RawRow> BuildRawRows'` == 1
    - 키 규칙 일치: `grep -c '(shot.ShotName ?? "") + "/" + (fai.FAIName ?? "") + "/" + (m.MeasurementName ?? "")'` == 1
    - `grep -c 'FormulaA1' WPF_Example/Custom/Export/CpkReportExportService.cs` == 0
    - `grep -c '검사성적서\|2Cav\|RAW DATA(2)\|안내사항' WPF_Example/Custom/Export/CpkReportExportService.cs` == 0
    - `grep -c 'Custom\\Export\\CpkReportExportService.cs' WPF_Example/DatumMeasurement.csproj` == 1
    - `grep -nE '[^?]\? .+ : ' WPF_Example/Custom/Export/CpkReportExportService.cs` → 0 matches
  </acceptance_criteria>
  <verify>
    <automated>MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"; "$MSB" 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' -p:Configuration=Debug -p:Platform=x64 -p:OutputPath='C:\Users\tech\AppData\Local\Temp\claude\C--Info-Project-DataMeasurement\9d3a7b4d-2314-4b14-8686-52fd6346a1f9\scratchpad\build72\' -t:Build -v:minimal -nologo</automated>
  </verify>
  <done>Task 2 완료 시점에 빌드 exit 0(그 전엔 `WriteRawDataSheet` 미정의로 CS0103). 에러 0, 경고 12줄 baseline.</done>
</task>

<task type="auto">
  <name>Task 2: WriteRawDataSheet — RAW DATA(1) 가로형 시트 기록</name>
  <read_first>
    - WPF_Example/Custom/Export/CpkReportExportService.cs (Task 1 결과물)
    - WPF_Example/Custom/Export/RepeatExcelExportService.cs (라인 78-112 — 메타행/헤더배열 루프/AdjustToContents 관례)
  </read_first>
  <files>WPF_Example/Custom/Export/CpkReportExportService.cs</files>
  <action>
`ExportCpkReport` 뒤에 아래 메서드를 추가한다. **레이아웃을 아래 표 그대로** 구현한다:

| 위치 | 내용 |
|---|---|
| A1 / B1 | `모델명` / recipeName |
| A2 / B2 | `측정일시` / `yyyy-MM-dd HH:mm:ss` |
| A3 / B3 | `샘플 수` / 열 개수 |
| 4행 (G4~) | 자재 라벨 (`자재 3` 또는 `미지정`) |
| 5행 A~F | `Number` / `도면항목설명` / `측정방식` / `설계값` / `상한 공차` / `하한 공차` |
| 5행 G~ | `#1`, `#2`, `#3`, ... |
| 6행~ | 데이터. A=FAIName, B=MeasurementName, C=TypeName, D=Nominal, E=Tol+, F=Tol-, G~=측정값 또는 `-` |

```csharp
        /// <summary>RAW DATA(1) 가로형 매트릭스 시트를 기록한다. 1행 = 측정 항목, 열 = 샘플.</summary>
        private static void WriteRawDataSheet(IXLWorksheet ws, List<SampleColumn> columns, List<RawRow> rows, string recipeName)
        {
            ws.Cell(1, 1).Value = "모델명";
            if (recipeName != null)
            {
                ws.Cell(1, 2).Value = recipeName;
            }
            else
            {
                ws.Cell(1, 2).Value = "";
            }

            ws.Cell(2, 1).Value = "측정일시";
            ws.Cell(2, 2).Value = DateTime.Now.ToString("yyyy-MM-dd HH:mm:ss");
            ws.Cell(3, 1).Value = "샘플 수";
            ws.Cell(3, 2).Value = columns.Count;

            string[] hFixed = { "Number", "도면항목설명", "측정방식", "설계값", "상한 공차", "하한 공차" };
            for (int i = 0; i < hFixed.Length; i++)
            {
                ws.Cell(RAW_HEADER_ROW, i + 1).Value = hFixed[i];
            }

            for (int i = 0; i < columns.Count; i++)
            {
                int nCol = RAW_FIRST_SAMPLE_COLUMN + i;
                ws.Cell(RAW_MATERIAL_ROW, nCol).Value = columns[i].MaterialLabel;
                ws.Cell(RAW_HEADER_ROW, nCol).Value = columns[i].HeaderLabel;
            }

            int nRow = RAW_FIRST_DATA_ROW;
            foreach (var row in rows)
            {
                ws.Cell(nRow, 1).Value = row.FAIName;
                ws.Cell(nRow, 2).Value = row.MeasurementName;
                ws.Cell(nRow, 3).Value = row.TypeName;
                ws.Cell(nRow, 4).Value = row.NominalValue;
                ws.Cell(nRow, 5).Value = row.TolerancePlus;
                ws.Cell(nRow, 6).Value = row.ToleranceMinus;

                for (int i = 0; i < columns.Count; i++)
                {
                    int nCol = RAW_FIRST_SAMPLE_COLUMN + i;

                    bool bHas = false;
                    if (i < row.HasValues.Count)
                    {
                        bHas = row.HasValues[i];
                    }

                    if (bHas)
                    {
                        ws.Cell(nRow, nCol).Value = Math.Round(row.Values[i], 6);
                    }
                    else
                    {
                        ws.Cell(nRow, nCol).Value = NO_VALUE_TEXT;
                    }
                }

                nRow++;
            }

            ws.Row(RAW_HEADER_ROW).Style.Font.Bold = true;
            ws.SheetView.FreezeRows(RAW_HEADER_ROW);
            ws.SheetView.FreezeColumns(RAW_FIRST_SAMPLE_COLUMN - 1);
            ws.Columns().AdjustToContents();
        }
```

**금지 사항:** 삼항 금지, `FormulaA1` 금지, 매직넘버 인라인 금지(상수만 사용). Allman, 헝가리언, C# 7.2.
  </action>
  <acceptance_criteria>
    - `grep -c 'string\[\] hFixed = { "Number", "도면항목설명", "측정방식", "설계값", "상한 공차", "하한 공차" };' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1
    - `grep -c 'private static void WriteRawDataSheet(IXLWorksheet ws, List<SampleColumn> columns, List<RawRow> rows, string recipeName)'` == 1
    - `grep -c 'ws.Cell(nRow, nCol).Value = NO_VALUE_TEXT;'` == 1
    - `grep -c 'ws.Columns().AdjustToContents();'` == 1
    - `grep -c 'Worksheets.Add' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1  (아직 RAW 1장만)
    - `grep -c 'FormulaA1'` == 0
    - `grep -nE '[^?]\? .+ : ' WPF_Example/Custom/Export/CpkReportExportService.cs` → 0 matches
  </acceptance_criteria>
  <verify>
    <automated>MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"; "$MSB" 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' -p:Configuration=Debug -p:Platform=x64 -p:OutputPath='C:\Users\tech\AppData\Local\Temp\claude\C--Info-Project-DataMeasurement\9d3a7b4d-2314-4b14-8686-52fd6346a1f9\scratchpad\build72\' -t:Build -v:minimal -nologo</automated>
  </verify>
  <done>빌드 exit 0, CS 에러 0, 경고 12줄 baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 → xlsx 파일 | `outputPath` 는 상위 호출자(SaveFileDialog)가 정한 OS 검증 경로 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-07 | Tampering | 시트명 injection | mitigate | 시트명이 `private const` 하드코딩 2개뿐 — 사용자 입력이 시트명에 절대 섞이지 않음(D-04 고정 시트가 보안상으로도 유리) |
| T-72-08 | Denial of Service | 샘플 열 수가 매우 많을 때 열 폭증 (Excel 16384열 상한) | accept | 반복검사 실사용 규모 100~300 cycle. 상한 초과 시 ClosedXML 예외 → 기존 try/catch 가 false 반환하며 앱은 살아있음 |
</threat_model>

<verification>
1. msbuild Debug/x64 exit 0, 에러 0, 경고 12줄 baseline
2. `grep -c 'Worksheets.Add' CpkReportExportService.cs` == 1 (72-06 이후 == 2)
3. 금지 시트명 문자열 0건: `grep -c '검사성적서\|2Cav\|RAW DATA(2)\|안내사항'` == 0
4. `grep -nE '[^?]\? .+ : '` → 0
</verification>

<success_criteria>
- `RAW DATA(1)` 시트 1장이 가로형 매트릭스로 생성됨
- 샘플 열이 자재번호 오름차순 정렬 + 4행 자재 라벨
- 미측정 칸 `-` 표기 (0 이 아님)
- 빌드 PASS
</success_criteria>

<output>
완료 후 `.planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-05-SUMMARY.md` 생성
</output>
