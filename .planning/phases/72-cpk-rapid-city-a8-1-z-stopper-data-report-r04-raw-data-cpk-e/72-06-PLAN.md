---
phase: 72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e
plan: 06
type: execute
wave: 6
depends_on: ["72-05"]
files_modified:
  - WPF_Example/Custom/Export/CpkReportExportService.cs
autonomous: true
requirements: [72]
must_haves:
  truths:
    - "export 하면 워크북에 '1Cav 세부치수_Cpk' 시트가 정확히 1장 더 생성된다 (총 2장)"
    - "Cp / UCPK / LCPK / Cpk 가 참고양식 산식 그대로 계산되어 기록된다"
    - "USL / LSL 이 명시적 컬럼으로 존재한다"
    - "판정이 NG > Cpk경고 > OK 3단계로 일관 적용된다"
    - "상단에 OK/Total, NG/Total, NG FAI# 목록 요약이 있다"
    - "StdDev==0 으로 무한대가 된 지표는 '∞' 텍스트로 기록된다 (NaN/Infinity raw 기록 금지)"
    - "표본이 0개(N==0)인 항목은 통계 9칸이 전부 '-' 이며 0 이 찍히지 않는다"
  artifacts:
    - path: "WPF_Example/Custom/Export/CpkReportExportService.cs"
      provides: "WriteCpkSheet + 판정/표시 헬퍼"
      contains: "CPK_SHEET_NAME = \"1Cav 세부치수_Cpk\""
  key_links:
    - from: "WriteCpkSheet"
      to: "RepeatMeasurementStats.ComputeAll()"
      via: "RawRow.Key 와 동일 키로 Dictionary 조인"
      pattern: "statDict.TryGetValue\\(row.Key, out stat\\)"
---

<objective>
`1Cav 세부치수_Cpk` 통계 시트를 구현한다 — Cpk export 복원(Phase 51에서 제거됨), Cp 신규, USL/LSL 명시 컬럼화, 3단계 판정, 상단 요약.

Purpose: 고객 참고양식의 2번(그리고 마지막) 산출물. 이걸로 Phase 목표의 갭 2/3/4가 닫힌다.
Output: `CpkReportExportService.cs` 에 `WriteCpkSheet` + 헬퍼 3개 추가, `ExportCpkReport` 에서 두 번째 시트 Add.
</objective>

<build_serialization_note>
**동시 실행 금지 — 이 plan 은 72-05 완료 후에 단독으로 실행한다.**
이유: 이 Phase 의 모든 plan 이 동일 `DatumMeasurement.csproj` / 동일 `OutputPath` / 동일 `obj\x64\Debug` 를 공유한다.
</build_serialization_note>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-CONTEXT.md
@.planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-05-SUMMARY.md
@.planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-01-SUMMARY.md

<interfaces>
72-01 이 확장한 `MeasurementStat` (`namespace ReringProject.Sequence`) — 전부 `{ get; set; }`:
```csharp
public string ShotName; public string FAIName; public string MeasurementName; public string TypeName;
public int N; public double Mean; public double StdDev; public double Range; public double Cpk;
public double Cp; public double UCpk; public double LCpk; public double MinValue; public double MaxValue;
public double NominalValue; public double TolerancePlus; public double ToleranceMinus;
public int OkCount; public int NgCount; public int DetectFailCount;
```
집계 방법:
```csharp
var stats = new RepeatMeasurementStats();
foreach (var c in cycles) { stats.AddSample(c); }
Dictionary<string, MeasurementStat> statDict = stats.ComputeAll();
```
`statDict` 의 키 = `ShotName + "/" + FAIName + "/" + MeasurementName` — 72-05 의 `RawRow.Key` 와 **동일**.

**⚠ N==0 함정 (반드시 처리):** `RepeatMeasurementStats.AddSample`(cs:90-101)은 DATUM_FAIL/NO_IMAGE 만 발생한 측정키에 대해서도 `KeyData` 를 생성한다. 그 결과 `ComputeAll()` 에는 **`N == 0` 이고 Mean/StdDev/Cp/UCpk/LCpk/Cpk/MinValue/MaxValue 가 전부 0 인 엔트리**가 남는다. 따라서 `stat != null` 만 검사하면 통계 9칸에 `0` 이 찍히고 판정 열만 `-` 가 되어 일관성이 깨진다. **판별 조건은 반드시 `stat != null && stat.N > 0` 이어야 한다.**

72-05 가 만든 내부 타입: `private class RawRow { public string Key; public string FAIName; public string MeasurementName; public string TypeName; public double NominalValue; public double TolerancePlus; public double ToleranceMinus; public List<double> Values; public List<bool> HasValues; }`

무한대 표시 관례 원본 — `StatisticsWindow.CpkToText`(StatisticsWindow.xaml.cs:185-199): `PositiveInfinity → "∞"`, `NegativeInfinity/NaN → "-"`, 그 외 `F3`. (export 는 자리수를 6으로 쓰므로 미러 구현한다 — Window 의 private 메서드를 호출하지 않는다.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 통계 표시/판정 헬퍼 3개 추가</name>
  <read_first>
    - WPF_Example/Custom/Export/CpkReportExportService.cs (72-05 결과물 전체)
    - WPF_Example/UI/Statistics/StatisticsWindow.xaml.cs (라인 185-199 — CpkToText 원본)
  </read_first>
  <files>WPF_Example/Custom/Export/CpkReportExportService.cs</files>
  <action>
클래스 상수 영역에 추가:
```csharp
        private const string CPK_SHEET_NAME = "1Cav 세부치수_Cpk";
        private const double CPK_WARN_THRESHOLD = 1.33;

        private const int CPK_SUMMARY_OK_ROW = 1;
        private const int CPK_SUMMARY_NG_ROW = 2;
        private const int CPK_SUMMARY_LIST_ROW = 3;
        private const int CPK_HEADER_ROW = 5;
        private const int CPK_FIRST_DATA_ROW = 6;

        // 좌측 블록 B~L, 우측 통계 블록 N~V, 판정 X (참고양식 열 배치 유지, M/W 는 구분 공백열)
        private const int CPK_LEFT_FIRST_COLUMN = 2;    // B
        private const int CPK_RIGHT_FIRST_COLUMN = 14;  // N
        private const int CPK_JUDGE_COLUMN = 24;        // X

        private const string JUDGE_NG = "N G";
        private const string JUDGE_WARN = "Cpk";
        private const string JUDGE_OK = "O K";

        private const string INFINITY_TEXT = "∞";
        private const int STAT_DECIMALS = 6;
```

헬퍼 3개를 `WriteRawDataSheet` 뒤에 추가한다:

```csharp
        /// <summary>
        /// 통계 셀 1칸을 기록한다. StdDev==0 이면 Cp/UCPK/LCPK/Cpk 가 PositiveInfinity 이므로
        /// raw double 을 넣으면 엑셀이 깨진다 — 텍스트로 치환한다(StatisticsWindow.CpkToText 미러).
        /// </summary>
        private static void WriteStatCell(IXLWorksheet ws, int nRow, int nColumn, double dValue)
        {
            if (double.IsPositiveInfinity(dValue))
            {
                ws.Cell(nRow, nColumn).Value = INFINITY_TEXT;
                return;
            }

            if (double.IsNegativeInfinity(dValue) || double.IsNaN(dValue))
            {
                ws.Cell(nRow, nColumn).Value = NO_VALUE_TEXT;
                return;
            }

            ws.Cell(nRow, nColumn).Value = Math.Round(dValue, STAT_DECIMALS);
        }

        /// <summary>
        /// 3단계 판정. 참고파일의 다른 시트에는 참/거짓 분기가 동일한 수식 오류
        /// (IF(V&lt;1.33,"O K","O K"))가 있으나 이를 재현하지 않고 모든 항목에 동일 규칙을 적용한다.
        /// 표본이 없으면(N==0) 판정 불가이므로 "-".
        /// min &lt; LSL 또는 max &gt; USL → "N G" / Cpk &lt; 1.33 → "Cpk" / 그 외 → "O K".
        /// </summary>
        private static string BuildCpkJudgement(MeasurementStat stat, double dUsl, double dLsl)
        {
            if (stat == null || stat.N <= 0)
            {
                return NO_VALUE_TEXT;
            }

            if (stat.MinValue < dLsl || stat.MaxValue > dUsl)
            {
                return JUDGE_NG;
            }

            if (stat.Cpk < CPK_WARN_THRESHOLD)
            {
                return JUDGE_WARN;
            }

            return JUDGE_OK;
        }

        /// <summary>공차 유형 라벨. 상/하한 공차 존재 여부로 결정한다.</summary>
        private static string BuildToleranceTypeText(double dTolPlus, double dTolMinus)
        {
            bool bHasUpper = dTolPlus != 0.0;
            bool bHasLower = Math.Abs(dTolMinus) != 0.0;

            if (bHasUpper && bHasLower)
            {
                return "양측";
            }

            if (bHasUpper)
            {
                return "상한";
            }

            if (bHasLower)
            {
                return "하한";
            }

            return "없음";
        }
```

주의: `Cpk == PositiveInfinity` 는 `< 1.33` 이 아니므로 자동으로 `"O K"` 가 된다 — 의도된 동작이다.

**금지 사항:** 삼항 금지. Allman, 헝가리언, C# 7.2. **주석에 금지 시트명(`검사성적서`/`2Cav`/`RAW DATA(2)`/`안내사항`)을 쓰지 말 것** — Task 2 의 acceptance 가 이 파일에서 해당 문자열 0건을 요구한다.
  </action>
  <acceptance_criteria>
    (모든 grep 에 대상 파일 경로를 명시한다 — 경로 없는 grep 은 stdin 을 기다리며 멈춘다)
    - `grep -c 'CPK_SHEET_NAME = "1Cav 세부치수_Cpk"' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1
    - `grep -c 'CPK_WARN_THRESHOLD = 1.33;' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1
    - `grep -c 'private static void WriteStatCell(IXLWorksheet ws, int nRow, int nColumn, double dValue)' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1
    - `grep -c 'private static string BuildCpkJudgement(MeasurementStat stat, double dUsl, double dLsl)' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1
    - `grep -c 'private static string BuildToleranceTypeText(double dTolPlus, double dTolMinus)' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1
    - `grep -c 'stat == null || stat.N <= 0' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1
    - `grep -c 'INFINITY_TEXT' WPF_Example/Custom/Export/CpkReportExportService.cs` >= 2
    - `grep -c '검사성적서\|2Cav\|RAW DATA(2)\|안내사항' WPF_Example/Custom/Export/CpkReportExportService.cs` == 0
    - `grep -nE '[^?]\? .+ : ' WPF_Example/Custom/Export/CpkReportExportService.cs` → 0 matches
  </acceptance_criteria>
  <verify>
    <automated>MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"; "$MSB" 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' -p:Configuration=Debug -p:Platform=x64 -p:OutputPath='C:\Users\tech\AppData\Local\Temp\claude\C--Info-Project-DataMeasurement\9d3a7b4d-2314-4b14-8686-52fd6346a1f9\scratchpad\build72\' -t:Build -v:minimal -nologo</automated>
  </verify>
  <done>빌드 exit 0, 에러 0, 경고 12줄 baseline (CS0162 unreachable 경고가 늘지 않는지 확인). 이 task 의 추가분은 전부 신규 메서드/상수라 단독으로도 컴파일된다.</done>
</task>

<task type="auto">
  <name>Task 2: WriteCpkSheet 구현 + ExportCpkReport 에 2번째 시트 연결</name>
  <read_first>
    - WPF_Example/Custom/Export/CpkReportExportService.cs (Task 1 결과물 전체)
    - WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs (72-01 확장 후 — 필드명 확인)
  </read_first>
  <files>WPF_Example/Custom/Export/CpkReportExportService.cs</files>
  <action>
**A) 레이아웃 — 아래 표 그대로 구현한다.**

상단 요약 (A/B열):
| 셀 | 값 |
|---|---|
| A1 / B1 | `OK / Total` / `"{ok} / {total}"` |
| A2 / B2 | `NG / Total` / `"{ng} / {total}"` |
| A3 / B3 | `NG FAI# 항목` / NG 판정 항목의 `FAIName` 을 `, ` 로 연결(중복 제거). 없으면 `-` |

헤더 5행:
| 열 | 헤더 | 값 소스 |
|---|---|---|
| B(2) | `SPC` | 데이터 행 순번 1부터 |
| C(3) | `FAI#` | `row.FAIName` |
| D(4) | `측정 방식` | `row.TypeName` |
| E(5) | `Datum 유형` | 항상 `-` (CycleResultDto 에 datum 유형 필드가 없음 — 양식 유지용 열) |
| F(6) | `공차 유형` | `BuildToleranceTypeText(TolPlus, TolMinus)` |
| G(7) | `기준 치수` | `row.NominalValue` |
| H(8) | `+ 공차` | `row.TolerancePlus` |
| I(9) | `- 공차` | `row.ToleranceMinus` |
| J(10) | `검사 방법` | 고정 문자열 `"AOI"` (`private const string INSPECT_METHOD_TEXT = "AOI";`) |
| K(11) | `USL` | `Nominal + TolPlus` |
| L(12) | `LSL` | `Nominal - Math.Abs(TolMinus)` |
| N(14) | `Maximum` | `stat.MaxValue` |
| O(15) | `Minimum` | `stat.MinValue` |
| P(16) | `Mean` | `stat.Mean` |
| Q(17) | `#1 Target Std Dev` | 항상 `-` (참고양식 고유 목표치, 우리 시스템 미보유 — 양식 유지용 열) |
| R(18) | `Std Dev` | `stat.StdDev` |
| S(19) | `Cp` | `stat.Cp` |
| T(20) | `UCPK` | `stat.UCpk` |
| U(21) | `LCPK` | `stat.LCpk` |
| V(22) | `Cpk` | `stat.Cpk` |
| X(24) | `Judgment` | `BuildCpkJudgement(...)` |

**E열과 Q열은 데이터 유무와 무관하게 항상 `-` 다** — UAT 체크리스트(72-07)에도 이 두 열이 전부 `-` 인지 확인하는 항목이 있다.

USL/LSL 산식은 `RepeatMeasurementStats.ComputeAll()`(cs:176-177)과 **글자 그대로 동일**해야 값이 어긋나지 않는다:
```csharp
                double dUsl = row.NominalValue + row.TolerancePlus;
                double dLsl = row.NominalValue - Math.Abs(row.ToleranceMinus);
```

**B) 메서드 구현** — `WriteRawDataSheet` 뒤, 헬퍼들 앞/뒤 어디든 클래스 안에 추가:

```csharp
        /// <summary>1Cav 세부치수_Cpk 통계 시트를 기록한다. 행 순서는 RAW DATA 시트와 동일하다.</summary>
        private static void WriteCpkSheet(IXLWorksheet ws, List<RawRow> rows, Dictionary<string, MeasurementStat> statDict)
        {
            string[] hLeft = { "SPC", "FAI#", "측정 방식", "Datum 유형", "공차 유형",
                               "기준 치수", "+ 공차", "- 공차", "검사 방법", "USL", "LSL" };
            for (int i = 0; i < hLeft.Length; i++)
            {
                ws.Cell(CPK_HEADER_ROW, CPK_LEFT_FIRST_COLUMN + i).Value = hLeft[i];
            }

            string[] hRight = { "Maximum", "Minimum", "Mean", "#1 Target Std Dev", "Std Dev",
                                "Cp", "UCPK", "LCPK", "Cpk" };
            for (int i = 0; i < hRight.Length; i++)
            {
                ws.Cell(CPK_HEADER_ROW, CPK_RIGHT_FIRST_COLUMN + i).Value = hRight[i];
            }

            ws.Cell(CPK_HEADER_ROW, CPK_JUDGE_COLUMN).Value = "Judgment";

            int nRow = CPK_FIRST_DATA_ROW;
            int nSpc = 1;
            int nOk = 0;
            int nNg = 0;
            var ngNames = new List<string>();

            foreach (var row in rows)
            {
                double dUsl = row.NominalValue + row.TolerancePlus;
                double dLsl = row.NominalValue - Math.Abs(row.ToleranceMinus);

                MeasurementStat stat;
                if (!statDict.TryGetValue(row.Key, out stat))
                {
                    stat = null;
                }

                ws.Cell(nRow, 2).Value = nSpc;
                ws.Cell(nRow, 3).Value = row.FAIName;
                ws.Cell(nRow, 4).Value = row.TypeName;
                ws.Cell(nRow, 5).Value = NO_VALUE_TEXT;                                   // Datum 유형 (DTO 미보유, 항상 "-")
                ws.Cell(nRow, 6).Value = BuildToleranceTypeText(row.TolerancePlus, row.ToleranceMinus);
                ws.Cell(nRow, 7).Value = row.NominalValue;
                ws.Cell(nRow, 8).Value = row.TolerancePlus;
                ws.Cell(nRow, 9).Value = row.ToleranceMinus;
                ws.Cell(nRow, 10).Value = INSPECT_METHOD_TEXT;
                ws.Cell(nRow, 11).Value = Math.Round(dUsl, STAT_DECIMALS);
                ws.Cell(nRow, 12).Value = Math.Round(dLsl, STAT_DECIMALS);

                // N==0 엔트리(DATUM_FAIL/NO_IMAGE 만 있는 측정키)는 stat 이 null 이 아니지만 값이 전부 0 이다.
                // 그대로 쓰면 통계칸에 0 이 찍히고 판정만 "-" 가 되어 일관성이 깨지므로 N 까지 확인한다.
                bool bHasStat = stat != null && stat.N > 0;
                if (bHasStat)
                {
                    WriteStatCell(ws, nRow, 14, stat.MaxValue);
                    WriteStatCell(ws, nRow, 15, stat.MinValue);
                    WriteStatCell(ws, nRow, 16, stat.Mean);
                    ws.Cell(nRow, 17).Value = NO_VALUE_TEXT;                              // #1 Target Std Dev (양식 고유, 항상 "-")
                    WriteStatCell(ws, nRow, 18, stat.StdDev);
                    WriteStatCell(ws, nRow, 19, stat.Cp);
                    WriteStatCell(ws, nRow, 20, stat.UCpk);
                    WriteStatCell(ws, nRow, 21, stat.LCpk);
                    WriteStatCell(ws, nRow, 22, stat.Cpk);
                }
                else
                {
                    for (int nCol = 14; nCol <= 22; nCol++)
                    {
                        ws.Cell(nRow, nCol).Value = NO_VALUE_TEXT;
                    }
                }

                string szJudge = BuildCpkJudgement(stat, dUsl, dLsl);
                ws.Cell(nRow, CPK_JUDGE_COLUMN).Value = szJudge;

                if (szJudge == JUDGE_OK)
                {
                    nOk++;
                }
                else if (szJudge == JUDGE_NG)
                {
                    nNg++;
                    if (!ngNames.Contains(row.FAIName))
                    {
                        ngNames.Add(row.FAIName);
                    }
                }

                nRow++;
                nSpc++;
            }

            int nTotal = rows.Count;
            ws.Cell(CPK_SUMMARY_OK_ROW, 1).Value = "OK / Total";
            ws.Cell(CPK_SUMMARY_OK_ROW, 2).Value = nOk + " / " + nTotal;
            ws.Cell(CPK_SUMMARY_NG_ROW, 1).Value = "NG / Total";
            ws.Cell(CPK_SUMMARY_NG_ROW, 2).Value = nNg + " / " + nTotal;
            ws.Cell(CPK_SUMMARY_LIST_ROW, 1).Value = "NG FAI# 항목";

            if (ngNames.Count > 0)
            {
                ws.Cell(CPK_SUMMARY_LIST_ROW, 2).Value = string.Join(", ", ngNames);
            }
            else
            {
                ws.Cell(CPK_SUMMARY_LIST_ROW, 2).Value = NO_VALUE_TEXT;
            }

            ws.Row(CPK_HEADER_ROW).Style.Font.Bold = true;
            ws.SheetView.FreezeRows(CPK_HEADER_ROW);
            ws.Columns().AdjustToContents();
        }
```

상수 `INSPECT_METHOD_TEXT` 를 상수 영역에 추가: `private const string INSPECT_METHOD_TEXT = "AOI";`

**C) `ExportCpkReport` 연결** — `WriteRawDataSheet(...)` 호출 **뒤**, `wb.SaveAs(outputPath);` **앞**에 삽입:
```csharp
                    var stats = new RepeatMeasurementStats();
                    foreach (var c in cycles)
                    {
                        stats.AddSample(c);
                    }

                    Dictionary<string, MeasurementStat> statDict = stats.ComputeAll();

                    var wsCpk = wb.Worksheets.Add(CPK_SHEET_NAME);
                    WriteCpkSheet(wsCpk, rows, statDict);
```
`Worksheets.Add` 는 이 파일 전체에서 **정확히 2회**여야 한다.

**금지 사항:** 삼항 절대 금지. `FormulaA1` 금지 — 모든 값은 C# 계산 후 `.Value`(D-02). 매직넘버는 열 인덱스(2~24)만 인라인 허용하되 시트명/임계값/행번호는 상수 사용. 주석에 금지 시트명 문자열 금지. Allman, 헝가리언, C# 7.2.
  </action>
  <acceptance_criteria>
    (모든 grep 에 대상 파일 경로를 명시한다)
    - `grep -c 'private static void WriteCpkSheet(IXLWorksheet ws, List<RawRow> rows, Dictionary<string, MeasurementStat> statDict)' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1
    - `grep -c 'string\[\] hLeft = { "SPC", "FAI#", "측정 방식", "Datum 유형", "공차 유형",' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1
    - `grep -c '"Maximum", "Minimum", "Mean", "#1 Target Std Dev", "Std Dev",' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1
    - `grep -c 'double dUsl = row.NominalValue + row.TolerancePlus;' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1
    - `grep -c 'double dLsl = row.NominalValue - Math.Abs(row.ToleranceMinus);' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1
    - `grep -c 'statDict.TryGetValue(row.Key, out stat)' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1
    - N==0 처리: `grep -c 'bool bHasStat = stat != null && stat.N > 0;' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1
    - E열 `-` 채움: `grep -c 'ws.Cell(nRow, 5).Value = NO_VALUE_TEXT;' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1
    - Q열 `-` 채움: `grep -c 'ws.Cell(nRow, 17).Value = NO_VALUE_TEXT;' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1
    - `grep -c 'Worksheets.Add' WPF_Example/Custom/Export/CpkReportExportService.cs` == 2
    - `grep -c 'FormulaA1' WPF_Example/Custom/Export/CpkReportExportService.cs` == 0
    - `grep -c '검사성적서\|2Cav\|RAW DATA(2)\|안내사항' WPF_Example/Custom/Export/CpkReportExportService.cs` == 0
    - `grep -nE '[^?]\? .+ : ' WPF_Example/Custom/Export/CpkReportExportService.cs` → 0 matches
  </acceptance_criteria>
  <verify>
    <automated>MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"; "$MSB" 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' -p:Configuration=Debug -p:Platform=x64 -p:OutputPath='C:\Users\tech\AppData\Local\Temp\claude\C--Info-Project-DataMeasurement\9d3a7b4d-2314-4b14-8686-52fd6346a1f9\scratchpad\build72\' -t:Build -v:minimal -nologo</automated>
  </verify>
  <done>빌드 exit 0, CS 에러 0, 경고 12줄 baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 → xlsx 파일 | 레시피에서 온 FAI/측정명이 셀 텍스트로 기록됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-09 | Tampering | FAI 명이 셀 값으로 기록(CSV/수식 injection 유사) | mitigate | `.Value` 로 문자열만 기록하고 `FormulaA1` 를 전혀 쓰지 않으므로 `=`/`+` 로 시작하는 이름도 수식으로 평가되지 않음 (D-02 가 보안상으로도 유리) |
| T-72-10 | Information Disclosure | 리포트에 전체 측정 이력 포함 | accept | 로컬 오퍼레이터가 명시적으로 저장 위치를 고르는 데스크톱 앱. 기존 export 와 동일 노출 수준 |
</threat_model>

<verification>
1. msbuild Debug/x64 exit 0, 에러 0, 경고 12줄 baseline
2. `grep -c 'Worksheets.Add' WPF_Example/Custom/Export/CpkReportExportService.cs` == 2 (D-04 시트 2장 고정 준수)
3. `grep -c '검사성적서\|2Cav\|RAW DATA(2)\|안내사항' WPF_Example/Custom/Export/CpkReportExportService.cs` == 0, `grep -c 'FormulaA1' WPF_Example/Custom/Export/CpkReportExportService.cs` == 0, 삼항 0건
4. USL/LSL 산식이 `RepeatMeasurementStats.cs:176-177` 과 문자열 동일한지 육안 대조
</verification>

<success_criteria>
- `1Cav 세부치수_Cpk` 시트 생성, 워크북 총 시트 2장
- Cp/UCPK/LCPK/Cpk/USL/LSL 컬럼 존재 및 값 기록
- 판정 3단계 일관 적용, 상단 OK/NG/NG목록 요약
- 무한대는 `∞` 텍스트, N==0 항목은 통계 9칸 전부 `-`
- 빌드 PASS
</success_criteria>

<output>
완료 후 `.planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-06-SUMMARY.md` 생성
</output>
