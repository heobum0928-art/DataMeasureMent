---
phase: 72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e
plan: 07
type: execute
wave: 5
depends_on: ["72-03", "72-04", "72-06"]
files_modified:
  - WPF_Example/Custom/Export/CpkReportExportService.cs
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs
autonomous: false
requirements: [72]
must_haves:
  truths:
    - "Cpk 시트 데이터 아래 블록에 히스토그램/추이 그래프 이미지가 삽입된다"
    - "그래프 삽입 개수에 상한이 있어 대량 항목에서도 export 가 끝난다"
    - "리뷰어 창의 'CPK 리포트 export' 버튼(UI/STA 스레드)에서만 export 가 트리거된다"
    - "그래프 렌더/삽입이 실패해도 xlsx 저장은 성공한다"
    - "실제 100회 이상 반복 데이터 + 자재 2종으로 리포트가 정상 생성된다"
  artifacts:
    - path: "WPF_Example/Custom/Export/CpkReportExportService.cs"
      provides: "AppendChartBlock — 차트 PNG 삽입"
      contains: "ChartImageCapture.TryInsertChartPicture"
    - path: "WPF_Example/UI/Reviewer/ReviewerWindow.xaml"
      provides: "CPK 리포트 export 버튼"
      contains: "btn_cpkReportExport"
  key_links:
    - from: "ReviewerWindow.Button_CpkReportExport_Click"
      to: "CpkReportExportService.ExportCpkReport"
      via: "SaveFileDialog 후 완전 정규화 호출 (버튼 클릭 = UI/STA 스레드 보장)"
      pattern: "ReringProject\\.Export\\.CpkReportExportService\\.ExportCpkReport"
    - from: "AppendChartBlock"
      to: "ChartImageCapture.RenderHistogramPng"
      via: "RepeatMeasurementStats.GetSeries() 원시값"
      pattern: "ChartImageCapture\\.RenderHistogramPng"
---

<objective>
그래프 이미지 삽입(D-01)을 완성하고, 리뷰어 창에서 CPK 리포트를 저장할 수 있게 배선한 뒤, 실데이터 UAT 로 Phase 를 닫는다.

Purpose: Phase 목표의 마지막 갭(5번 그래프 export)과 실사용 진입점. 그리고 D-03/D-05 가 만든 검증 수단으로 실제 100회+ 데이터를 돌려 리포트를 눈으로 확인한다.
Output: `AppendChartBlock`, `btn_cpkReportExport`, UAT 통과.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-CONTEXT.md
@.planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-04-SUMMARY.md
@.planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-06-SUMMARY.md

<interfaces>
72-04 가 만든 API (`namespace ReringProject.Export`):
```csharp
public static byte[] ChartImageCapture.RenderHistogramPng(List<double> values, double dUsl, double dLsl);
public static byte[] ChartImageCapture.RenderTrendPng(List<double> values, double dMean, double dUsl, double dLsl);
internal static bool ChartImageCapture.TryInsertChartPicture(IXLWorksheet ws, int nRow, int nColumn, byte[] arrBytes);
```
(같은 어셈블리·같은 네임스페이스이므로 `ChartImageCapture.` 만으로 호출 가능)

72-01 이 만든 API (`namespace ReringProject.Sequence`):
```csharp
public Dictionary<string, List<double>> RepeatMeasurementStats.GetSeries();
```
키 = `ShotName/FAIName/MeasurementName` — `RawRow.Key` 및 `ComputeAll()` 키와 동일.
**이 값 리스트는 차트 전용이다.** DATUM_FAIL/NO_IMAGE 회차가 빠져 있어 "몇 번째 회차인지"를 알 수 없다 — RAW DATA 열 정렬에 쓰면 안 된다.

기존 export 버튼 핸들러 원본 — `ReviewerWindow.xaml.cs` `Button_RepeatExport_Click`(413-457):
`_repeatCycles` null/빈 가드 → `CustomMessageBox` 경고 → `SystemHandler.Handle.Setting.ResultSavePath` / `CurrentRecipeName` → `Microsoft.Win32.SaveFileDialog` (`Filter = "Excel 파일 (*.xlsx)|*.xlsx"`) → 완전 정규화 호출 → if/else 로 메시지·아이콘 분기 → `CustomMessageBox.Show(...)`.

**스레딩 계약:** 차트 렌더는 UI/STA 스레드에서만 안전하다. export 진입점은 **버튼 클릭 핸들러로 한정**하고 `OnRepeatComplete` 콜백에서 직접 호출하지 말 것.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppendChartBlock — Cpk 시트 데이터 아래에 그래프 이미지 삽입</name>
  <read_first>
    - WPF_Example/Custom/Export/CpkReportExportService.cs (72-06 결과물 전체)
    - WPF_Example/Custom/Export/ChartImageCapture.cs (72-04 결과물 — 시그니처 확인)
  </read_first>
  <files>WPF_Example/Custom/Export/CpkReportExportService.cs</files>
  <action>
1) 상수 추가:
```csharp
        // 그래프는 데이터 영역 아래 별도 블록에 놓는다(데이터 행 높이를 건드리지 않기 위함).
        private const int CHART_BLOCK_GAP_ROWS = 3;    // 마지막 데이터 행과 차트 블록 사이 여백
        private const int CHART_BLOCK_ROW_STEP = 15;   // 항목 1개당 차지하는 행 수(라벨 1 + 이미지 약 13)
        private const int CHART_HIST_COLUMN = 2;       // B
        private const int CHART_TREND_COLUMN = 9;      // I
        private const int CHART_MIN_SAMPLES = 2;       // 값이 2개 미만이면 차트를 만들지 않는다
        private const int MAX_CHART_ROWS = 30;         // export 시간 폭증 방지 상한(DoS 가드)
```

2) 메서드 추가:
```csharp
        /// <summary>
        /// Cpk 시트의 마지막 데이터 행 아래에 항목별 히스토그램/추이 그래프 이미지를 붙인다.
        /// 항목 수가 많으면 export 가 끝나지 않으므로 MAX_CHART_ROWS 개까지만 만든다.
        /// 렌더/삽입 실패는 무시하고 계속 — 그래프가 없다고 리포트를 실패시키지 않는다.
        /// </summary>
        private static void AppendChartBlock(
            IXLWorksheet ws,
            List<RawRow> rows,
            Dictionary<string, MeasurementStat> statDict,
            Dictionary<string, List<double>> seriesDict)
        {
            int nAnchorRow = CPK_FIRST_DATA_ROW + rows.Count + CHART_BLOCK_GAP_ROWS;

            ws.Cell(nAnchorRow, 1).Value = "측정 항목별 분포/추이 그래프";
            ws.Cell(nAnchorRow, 1).Style.Font.Bold = true;
            nAnchorRow = nAnchorRow + 2;

            int nMade = 0;
            foreach (var row in rows)
            {
                if (nMade >= MAX_CHART_ROWS)
                {
                    break;
                }

                List<double> values;
                if (!seriesDict.TryGetValue(row.Key, out values))
                {
                    continue;
                }

                if (values == null || values.Count < CHART_MIN_SAMPLES)
                {
                    continue;
                }

                MeasurementStat stat;
                if (!statDict.TryGetValue(row.Key, out stat))
                {
                    continue;
                }

                double dUsl = row.NominalValue + row.TolerancePlus;
                double dLsl = row.NominalValue - Math.Abs(row.ToleranceMinus);

                ws.Cell(nAnchorRow, 1).Value = row.FAIName + " / " + row.MeasurementName;

                byte[] arrHist = ChartImageCapture.RenderHistogramPng(values, dUsl, dLsl);
                byte[] arrTrend = ChartImageCapture.RenderTrendPng(values, stat.Mean, dUsl, dLsl);

                ChartImageCapture.TryInsertChartPicture(ws, nAnchorRow + 1, CHART_HIST_COLUMN, arrHist);
                ChartImageCapture.TryInsertChartPicture(ws, nAnchorRow + 1, CHART_TREND_COLUMN, arrTrend);

                nAnchorRow = nAnchorRow + CHART_BLOCK_ROW_STEP;
                nMade++;
            }

            if (nMade >= MAX_CHART_ROWS)
            {
                ws.Cell(nAnchorRow, 1).Value = "그래프는 상위 " + MAX_CHART_ROWS + "개 항목만 표시됩니다.";
            }
        }
```

3) `ExportCpkReport` 안에서 `WriteCpkSheet(wsCpk, rows, statDict);` **바로 뒤**, `wb.SaveAs(outputPath);` 앞에 추가:
```csharp
                    Dictionary<string, List<double>> seriesDict = stats.GetSeries();
                    AppendChartBlock(wsCpk, rows, statDict, seriesDict);
```
`AppendChartBlock` 은 `AdjustToContents()` **뒤에** 실행되어야 그림 위치가 안 밀린다 — `WriteCpkSheet` 마지막 줄이 `ws.Columns().AdjustToContents();` 이므로 위 순서면 조건을 만족한다. `AppendChartBlock` 안에서 `AdjustToContents()` 를 다시 호출하지 말 것.

**금지 사항:** 삼항 금지. `ws.Row(...).Height` 조작 금지(데이터 행 왜곡). Allman, 헝가리언, C# 7.2.
  </action>
  <acceptance_criteria>
    - `grep -c 'private static void AppendChartBlock(' WPF_Example/Custom/Export/CpkReportExportService.cs` == 1
    - `grep -c 'MAX_CHART_ROWS = 30;'` == 1
    - `grep -c 'ChartImageCapture.RenderHistogramPng(values, dUsl, dLsl);'` == 1
    - `grep -c 'ChartImageCapture.RenderTrendPng(values, stat.Mean, dUsl, dLsl);'` == 1
    - `grep -c 'ChartImageCapture.TryInsertChartPicture'` == 2
    - `grep -c 'stats.GetSeries();'` == 1
    - `grep -c 'AdjustToContents' WPF_Example/Custom/Export/CpkReportExportService.cs` == 2  (RAW 1 + Cpk 1, 차트 블록에서 추가 호출 없음)
    - `grep -c 'Worksheets.Add'` == 2
    - `grep -nE '[^?]\? .+ : ' WPF_Example/Custom/Export/CpkReportExportService.cs` → 0 matches
  </acceptance_criteria>
  <verify>
    <automated>MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"; "$MSB" 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' -p:Configuration=Debug -p:Platform=x64 -p:OutputPath='C:\Users\tech\AppData\Local\Temp\claude\C--Info-Project-DataMeasurement\9d3a7b4d-2314-4b14-8686-52fd6346a1f9\scratchpad\build72\' -t:Build -v:minimal -nologo</automated>
  </verify>
  <done>빌드 exit 0, 에러 0, 경고 12줄 baseline.</done>
</task>

<task type="auto">
  <name>Task 2: ReviewerWindow 에 "CPK 리포트 export" 버튼 추가</name>
  <read_first>
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml (72-04 적용 후 좌측 StackPanel)
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs (Button_RepeatExport_Click — 그대로 미러할 원본)
  </read_first>
  <files>WPF_Example/UI/Reviewer/ReviewerWindow.xaml, WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs</files>
  <action>
**A) `ReviewerWindow.xaml`** — `btn_repeatExport` **뒤**, `btn_chartSmoke` **앞**에 삽입:
```xml
                <Button x:Name="btn_cpkReportExport" Content="CPK 리포트 export"
                        Click="Button_CpkReportExport_Click"
                        IsEnabled="False"
                        Padding="8,4" Margin="0,4,0,0" HorizontalAlignment="Stretch"/>
```

**B) `ReviewerWindow.xaml.cs`**

1) `Button_RepeatExport_Click` **바로 뒤**에 핸들러 추가 (기존 핸들러를 그대로 미러, 서비스/파일명만 교체):
```csharp
        /// CPK 데이터 리포트(RAW DATA(1) + 1Cav 세부치수_Cpk) export.
        /// 버튼 클릭 핸들러 = UI/STA 스레드이므로 차트 Canvas 렌더가 안전하다.
        private void Button_CpkReportExport_Click(object sender, RoutedEventArgs e)
        {
            if (_repeatCycles == null || _repeatCycles.Count == 0)
            {
                CustomMessageBox.Show("CPK 리포트 export", "반복 실행 완료 후 사용하세요.", MessageBoxImage.Warning);
                return;
            }

            string initialDir = SystemHandler.Handle.Setting.ResultSavePath;
            string recipeName = SystemHandler.Handle.Setting.CurrentRecipeName ?? "";

            var dlg = new Microsoft.Win32.SaveFileDialog
            {
                Filter = "Excel 파일 (*.xlsx)|*.xlsx",
                FileName = "cpk_report_" + DateTime.Now.ToString("yyyyMMdd_HHmmss") + ".xlsx",
                InitialDirectory = initialDir
            };

            if (dlg.ShowDialog() == true)
            {
                bool ok = ReringProject.Export.CpkReportExportService.ExportCpkReport(
                    _repeatCycles, recipeName, dlg.FileName);

                string msg;
                if (ok)
                {
                    msg = "저장 완료:\n" + dlg.FileName;
                }
                else
                {
                    msg = "export 실패 (로그 확인)";
                }

                MessageBoxImage icon;
                if (ok)
                {
                    icon = MessageBoxImage.Information;
                }
                else
                {
                    icon = MessageBoxImage.Error;
                }

                CustomMessageBox.Show("CPK 리포트 export", msg, icon);
            }
        }
```

2) 활성/비활성 상태를 `btn_repeatExport` 와 **동기화**한다. `Button_RepeatRun_Click` 안의 두 지점을 수정:
   - `btn_repeatExport.IsEnabled = false;`(실행 시작 시) 줄 **바로 뒤**에 `btn_cpkReportExport.IsEnabled = false;` 추가
   - `OnRepeatComplete` 핸들러 안의 `btn_repeatExport.IsEnabled = nTotal > 0;`(72-03 이 만든 줄) **바로 뒤**에 `btn_cpkReportExport.IsEnabled = nTotal > 0;` 추가

3) `OnRepeatComplete` 에서 export 를 **자동 호출하지 않는다** — 백그라운드 시퀀스 스레드 발화 가능성 때문에 차트 렌더가 위험하다. 반드시 버튼 클릭 경로만 유지한다.

**금지 사항:** 삼항 금지. Allman 브레이스. 헝가리언. C# 7.2.
  </action>
  <acceptance_criteria>
    - `grep -c 'x:Name="btn_cpkReportExport"' WPF_Example/UI/Reviewer/ReviewerWindow.xaml` == 1
    - `grep -c 'private void Button_CpkReportExport_Click(object sender, RoutedEventArgs e)' WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs` == 1
    - `grep -c 'ReringProject.Export.CpkReportExportService.ExportCpkReport(' WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs` == 1
    - `grep -c 'btn_cpkReportExport.IsEnabled' WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs` == 2
    - `grep -c 'cpk_report_'` == 1
    - `grep -nE '[^?]\? .+ : ' WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs` → 0 matches
    - `OnRepeatComplete` 블록 안에 `ExportCpkReport` 호출 없음 (수동 코드 확인)
  </acceptance_criteria>
  <verify>
    <automated>MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"; "$MSB" 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' -p:Configuration=Debug -p:Platform=x64 -p:OutputPath='C:\Users\tech\AppData\Local\Temp\claude\C--Info-Project-DataMeasurement\9d3a7b4d-2314-4b14-8686-52fd6346a1f9\scratchpad\build72\' -t:Build -v:minimal -nologo</automated>
  </verify>
  <done>빌드 exit 0, XAML 컴파일 에러 0, 경고 12줄 baseline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [체크포인트] 실데이터 UAT — 100회+ 반복 / 자재 2종 / 리포트 육안 대조</name>
  <read_first>
    - .planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-CONTEXT.md (D-03, D-04, D-05)
  </read_first>
  <files>(코드 변경 없음 — 사용자 검증 전용)</files>
  <action>코드를 수정하지 않는다. 앱을 Debug/x64 로 실행하고 사용자가 UAT 1~4 를 순서대로 수행하도록 안내한 뒤, 응답이 올 때까지 대기한다. 실패 항목이 보고되면 해당 plan 으로 되돌아가 수정한다.</action>
  <what-built>
`CPK 리포트 export` 버튼 → 시트 2장짜리 xlsx (`RAW DATA(1)` 가로형 매트릭스 + `1Cav 세부치수_Cpk` 통계 + 하단 그래프 블록). 자재번호 입력과 누적 실행으로 자재별 열 분리를 오프라인 검증할 수 있다.
  </what-built>
  <how-to-verify>
**준비:** 검사용 이미지가 50장 이상 든 폴더를 2개 준비합니다(같은 폴더를 두 번 써도 됩니다). `Debug/x64` 로 실행 후 리뷰어 창을 엽니다.

**UAT 1 — 자재 1종 100회+ (D-03)**
1. `자재번호` 칸에 `1` 입력, `이전 결과에 누적` **해제**.
2. `이미지 폴더 반복 검사` → 이미지 50장 이상 폴더 선택 → 완료까지 대기.
3. `CPK 리포트 export` → 저장 → 엑셀로 연다.
   - [ ] 시트가 **정확히 2장**이다: `RAW DATA(1)`, `1Cav 세부치수_Cpk`. (`검사성적서`/`2Cav`/`RAW DATA(2)`/`안내사항` 이 **없어야** 합니다)
   - [ ] `RAW DATA(1)` 5행에 `Number/도면항목설명/측정방식/설계값/상한 공차/하한 공차` 그리고 G열부터 `#1 #2 #3 ...` 헤더
   - [ ] 4행에 `자재 1` 라벨
   - [ ] 측정 안 된 칸은 `0` 이 아니라 `-`
   - [ ] `1Cav 세부치수_Cpk` 에 `Cp / UCPK / LCPK / Cpk / USL / LSL` 컬럼이 있고 숫자가 채워져 있다
   - [ ] 아무 셀이나 클릭했을 때 수식 표시줄에 **수식이 아니라 값**이 보인다 (D-02)
   - [ ] 상단 A1~B3 에 `OK / Total`, `NG / Total`, `NG FAI# 항목` 요약이 있다
   - [ ] 데이터 아래에 히스토그램/추이 **그래프 이미지**가 보인다 (빈 흰 상자가 아님)

**UAT 2 — 자재 2종 열 분리 (D-05 / D-04)**
4. `자재번호` 칸에 `2` 입력, `이전 결과에 누적` **체크**.
5. `이미지 폴더 반복 검사` → 두 번째 폴더 선택 → 완료 대기.
6. 다시 `CPK 리포트 export` → 저장 → 연다.
   - [ ] 시트는 여전히 **2장뿐**이다 (자재가 늘어도 시트가 늘지 않는다 — D-04)
   - [ ] `RAW DATA(1)` 4행에 `자재 1` 구간과 `자재 2` 구간이 **좌→우 순서로 분리**되어 있다
   - [ ] 열 개수 = 자재1 회차수 + 자재2 회차수

**UAT 3 — Cpk 값 대조**
7. `1Cav 세부치수_Cpk` 의 아무 항목 하나를 골라 손으로 검산합니다:
   - `USL = 기준 치수 + (+공차)`, `LSL = 기준 치수 - |−공차|`
   - `Cp = ((+공차) + |−공차|) / (6 × Std Dev)`
   - `UCPK = (USL - Mean) / (3 × Std Dev)`, `LCPK = (Mean - LSL) / (3 × Std Dev)`, `Cpk = min(UCPK, LCPK)`
   - [ ] 시트 값과 손 계산이 일치한다
   - [ ] 판정 열이 `O K` / `Cpk` / `N G` 중 하나이고, `Minimum < LSL` 또는 `Maximum > USL` 인 항목은 반드시 `N G` 다

**UAT 4 — 기존 기능 회귀 0**
   - [ ] 기존 `반복도 엑셀 export` 버튼이 그대로 동작하고 시트/컬럼이 예전과 같다
   - [ ] 통계 창(StatisticsWindow)의 히스토그램/추이 차트가 예전처럼 보이고, 창 크기를 바꿔도 다시 그려진다
  </how-to-verify>
  <acceptance_criteria>
    - UAT 1~4 의 모든 체크박스 통과
    - 실패 항목은 증상 + 시트명/셀주소와 함께 보고
  </acceptance_criteria>
  <verify>
    <automated>MISSING — 이 프로젝트는 테스트 프레임워크가 없다(xUnit/NUnit/MSTest 부재). 생성된 xlsx 의 시각/수치 정합은 사람이 엑셀로 확인한다.</automated>
  </verify>
  <done>UAT 1~4 의 모든 체크박스를 사용자가 승인.</done>
  <resume-signal>"승인" 이라고 입력하거나, 실패한 항목 번호와 증상을 알려주세요.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 → SaveFileDialog 경로 | OS 파일 다이얼로그가 경로를 검증 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-11 | Denial of Service | 항목 수 × 2장 오프스크린 렌더로 UI 스레드 장시간 블로킹 | mitigate | `MAX_CHART_ROWS = 30` 상한 + `CHART_MIN_SAMPLES = 2` 스킵. 렌더 실패는 무시하고 진행 |
| T-72-12 | Tampering | export 파일 경로 | mitigate | `SaveFileDialog` (OS 검증) + 파일명은 `cpk_report_` + 타임스탬프 고정 조합, 사용자 문자열 미삽입 |
</threat_model>

<verification>
1. msbuild Debug/x64 exit 0, 에러 0, 경고 12줄 baseline
2. `grep -c 'Worksheets.Add' CpkReportExportService.cs` == 2
3. `grep -nE '[^?]\? .+ : '` — 수정 3파일 전부 0
4. UAT 1~4 통과 (Task 3)
</verification>

<success_criteria>
- 그래프 이미지가 Cpk 시트 하단 블록에 삽입됨 (상한 30개)
- 리뷰어 창 버튼에서 리포트 저장 가능
- 자재 2종 → 시트는 2장 유지, 열만 분리
- Cpk/Cp 손 검산 일치
- 기존 export/통계창 회귀 0
- 빌드 PASS
</success_criteria>

<output>
완료 후 `.planning/phases/72-cpk-rapid-city-a8-1-z-stopper-data-report-r04-raw-data-cpk-e/72-07-SUMMARY.md` 생성
</output>
