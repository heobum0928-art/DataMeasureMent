---
phase: 73-side-4-jig-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Define/ID.cs
  - WPF_Example/Custom/Sequence/SequenceHandler.cs
  - WPF_Example/Sequence/SequenceHandler.cs
autonomous: true
requirements: [PROTO-73-SEQ-SPLIT]
must_haves:
  truths:
    - "SIDE_1/SIDE_2/SIDE_3/SIDE_4 네 개의 InspectionSequence 가 PC2(CameraRole=Side)에서 생성된다"
    - "구 레시피(비-동적 FAI 포맷)의 Param0..N 매핑이 시퀀스 개수 3→6 증가로 어긋나지 않는다"
    - "Type→시퀀스 이름 해석에서 SIDE_3 이 TOP 으로 조용히 폴백되지 않는다"
  artifacts:
    - path: "WPF_Example/Custom/Define/ID.cs"
      provides: "ESequence.Side1~Side4 / EAction.Side1~Side4_Inspection"
      contains: "Side1 = 4"
    - path: "WPF_Example/Custom/Sequence/SequenceHandler.cs"
      provides: "SEQ_SIDE_1~4 상수, IsSequenceActive/ResolveSequenceName/Register*/Initialize*/RebuildInspectionActions 6분기"
      contains: "SEQ_SIDE_1"
    - path: "WPF_Example/Sequence/SequenceHandler.cs"
      provides: "IsDynamicFAIMode 일 때 레거시 Param0..N 저장 차단 + 시퀀스 개수 불일치 경고"
      contains: "ParamSequenceCount"
  key_links:
    - from: "Custom/Sequence/SequenceHandler.cs RegisterSequences"
      to: "Custom/Sequence/SequenceHandler.cs IsSequenceActive"
      via: "if (IsSequenceActive(ESequence.Side1))"
      pattern: "IsSequenceActive\\(ESequence\\.Side1\\)"
---

<objective>
SIDE 단일 시퀀스를 SIDE_1~4 네 개의 독립 `InspectionSequence` 로 분리하는 **골격**을 만든다.
enum·상수·활성화 판정·등록·액션 재구축을 한 plan 안에서 동시에 고쳐, 중간 상태에서
"시퀀스 미생성" 또는 "TOP 으로 조용한 폴백"이 발생하지 않게 한다(R5/R6).

시퀀스 개수를 늘리기 **전에** 레거시 `Param0..N` 위치 인덱스 문제(R4)를 먼저 닫는다.

Purpose: 이후 모든 plan(레시피/라우팅/조명)이 딛고 설 타입 기반.
Output: SIDE_1~4 시퀀스가 실제로 생성/등록되고, 구 레시피 로드가 깨지지 않는 빌드.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-side-4-jig-split/73-CONTEXT.md
@CLAUDE.md
@.planning/phases/73-side-4-jig-split/73-BUILD-VERIFY.md

**코딩 규칙 (위반 시 재작업):**
- 삼항 `?:` 금지 → `if/else`. `??` `?.` 축약 금지 → 명시적 분기
- 분기는 `if/else` 또는 **전통 switch**(`case X: ... break;`)만. C# 8.0 switch expression 금지
- C# 7.2 문법만. 헝가리언 접두사(b/n/sz). 파일별 기존 중괄호 스타일 유지(이 3파일은 전부 K&R — 여는 중괄호 같은 줄)
- 날짜 주석(`//YYMMDD hbk`) 새로 달지 않는다. 비자명한 "왜"만 최소 주석
- `WPF_Example/DatumMeasurement.csproj` 커밋 금지(항상 unstaged 유지)
- 실행 중 프로세스 종료 금지 — 산출물 잠김 시 스크래치 OutDir 로 컴파일만 검증

**빌드 검증은 `73-BUILD-VERIFY.md` 규격을 따른다(전 plan 공통 단일 소스).**
- msbuild 는 `-p:` 대시 형식만 사용(`/p:` 는 MSYS 경로변환으로 MSB1008 실패)
- SIMUL-OFF 는 `Release|x64` 가 아니라 `-p:DefineConstants=TRACE%3BDEBUG` 로 검증(csproj 무수정)
- 경고 baseline: 73-01 Task3 이후 **SIMUL-ON 18줄 / SIMUL-OFF 16줄**(CS0618 6줄 증가는 정상)
- 통과 기준 1순위는 "에러 0 + **새 경고 코드 종류 0건**". 숫자를 맞추려고
  `[Obsolete]` 제거 / `#pragma warning disable` / `NoWarn` 추가는 **금지**
</context>

<interfaces>
현재 상태(실측):

```csharp
// Custom/Define/ID.cs
public enum ESequence : int { Top = 1, Side = 2, Bottom = 3, }
public enum EAction : int { Top_Inspection = 2, Side_Inspection = 4, Bottom_Inspection = 6, FAI_Base = 100, Unknown = Int32.MaxValue }

// Custom/Sequence/SequenceHandler.cs
public const string SEQ_TOP = "TOP";
public const string SEQ_SIDE = "SIDE";
public const string SEQ_BOTTOM = "BOTTOM";
public const int Top_Alg_Index = 0;  Side_Alg_Index = 1;  Bottom_Alg_Index = 2;
public static string ResolveSequenceName(ESequence seqId)                 // :25
private static bool IsSequenceActive(ESequence seqId)                     // :42
private void RegisterSequences()                                          // :183
private void RegisterActions()                                            // :197
private void InitializeSequences()                                        // :210
public void RebuildInspectionActions(ESequence seqId)                     // :239
public bool TryLoadNewFormat(IniFile loadFile)                            // :~300 (Rebuild 3회 하드코딩 :310~312)

// Sequence/SequenceHandler.cs (framework)
private bool LoadFromIni(string name)   // Param0..N 로드 루프 :204~210
private bool SaveToIni(string name)     // Param0..N 저장 루프 :242~248 (IsDynamicFAIMode 무관 항상 실행)
```

`InspectionSequence` 생성자 시그니처(현행 호출부에서 확인):
`new InspectionSequence(ESequence seqId, string szName, int nAlgIndex, string szCameraName, string szLightGroupName)`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 레거시 Param0..N 위치 인덱스 시프트 차단 (R4)</name>
  <files>WPF_Example/Sequence/SequenceHandler.cs</files>
  <read_first>
    - WPF_Example/Sequence/SequenceHandler.cs 전체 (특히 LoadFromIni :180~220, SaveToIni :218~255)
    - .planning/phases/73-side-4-jig-split/73-CONTEXT.md D-73-07 의 R4 항목
    - MEMORY: reference_parambase_missing_key_zeroes_default (누락 키 0-클로버 함정)
  </read_first>
  <action>
`Sequence/SequenceHandler.cs` 의 레거시 `Param0..N` 경로를 시퀀스 개수 변화에 안전하게 만든다.
시퀀스가 3→6 개가 되면 위치 인덱스 `m` 이 밀려 구 레시피의 Param 값이 엉뚱한 Action 에 실린다.

**(1) SaveToIni — 레거시 Param 저장을 동적 FAI 모드에서 차단**

현재 `SaveToIni` 는 `if (IsDynamicFAIMode) { SaveNewFormat(...); }` 직후에 다음 루프를
**무조건** 실행한다:

```csharp
// 기존 Param0~N 방식도 항상 저장 (하위 호환)
int m = 0;
for (int i = 0; i < Sequences.Count; i++) {
    for (int j = 0; j < this[i].ActionCount; j++) {
        ParamBase param = this[i][j].Param;
        param.Save(saveFile, "Param" + m.ToString());
        m++;
    }
}
```

이 블록을 아래로 교체한다(전통 if/else, 삼항 금지):

```csharp
// 레거시 Param0..N 은 위치 인덱스 기반이라 시퀀스/Action 개수가 바뀌면 매핑이 통째로 밀린다.
// 동적 FAI 모드 레시피는 SHOTS 포맷이 단일 소스이고 로드 시 이 키를 읽지 않으므로 저장하지 않는다.
bool bSkipLegacyParam = IsDynamicFAIMode;
if (bSkipLegacyParam) {
    saveFile["Info"]["ParamSequenceCount"] = 0;
}
else {
    int m = 0;
    for (int i = 0; i < Sequences.Count; i++) {
        for (int j = 0; j < this[i].ActionCount; j++) {
            ParamBase param = this[i][j].Param;
            param.Save(saveFile, "Param" + m.ToString());
            m++;
        }
    }
    saveFile["Info"]["ParamSequenceCount"] = Sequences.Count;
}
```

**(2) LoadFromIni — 레거시 경로에 시퀀스 개수 불일치 경고**

`// 기존 Param0~N 방식` 루프 **직전**에 삽입한다:

```csharp
int nSavedSeqCount = loadFile["Info"]["ParamSequenceCount"].ToInt();
bool bHasSavedSeqCount = nSavedSeqCount > 0;
bool bSeqCountMismatch = bHasSavedSeqCount && nSavedSeqCount != Sequences.Count;
if (bSeqCountMismatch) {
    Logging.PrintLog((int)ELogType.Error,
        "[RECIPE] 레거시 Param 매핑 불일치 — 저장 시 시퀀스 {0}개 / 현재 {1}개. Param0..N 로드를 건너뛴다(오매핑 방지).",
        nSavedSeqCount, Sequences.Count);
}
```

그리고 기존 로드 루프 전체를 `if (!bSeqCountMismatch) { ... }` 로 감싼다.
`Logging` / `ELogType` using 이 없으면 추가한다 — **`Logging` 은 `ReringProject.Utility`,
`ELogType` 은 `ReringProject.Setting`**(`Setting/SystemSetting.cs:18`)이다. `ReringProject.Define` 이 아니다.

⚠ 이 task 는 반드시 Task 2 **이전에** 완료돼야 한다 — 시퀀스 개수를 늘린 뒤에 고치면
그 사이에 저장된 레시피가 이미 오염된다.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement/WPF_Example &amp;&amp; grep -c "ParamSequenceCount" Sequence/SequenceHandler.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ParamSequenceCount" WPF_Example/Sequence/SequenceHandler.cs` == 3
    - `grep -c "bSeqCountMismatch" WPF_Example/Sequence/SequenceHandler.cs` == 3
    - `grep -c "bSkipLegacyParam" WPF_Example/Sequence/SequenceHandler.cs` == 2
    - `grep -n "Param\" + m.ToString()" WPF_Example/Sequence/SequenceHandler.cs` 결과가 여전히 2건(Save 1 / Load 1)
  </acceptance_criteria>
  <done>동적 FAI 모드 저장 시 Param0..N 키가 파일에 쓰이지 않고, 레거시 로드는 개수 불일치 시 건너뛰며 Error 로그를 남긴다.</done>
</task>

<task type="auto">
  <name>Task 2: ESequence/EAction 에 SIDE_1~4 추가 + 시퀀스 상수/이름 해석</name>
  <files>WPF_Example/Custom/Define/ID.cs, WPF_Example/Custom/Sequence/SequenceHandler.cs</files>
  <read_first>
    - WPF_Example/Custom/Define/ID.cs 전체(31줄)
    - WPF_Example/Custom/Sequence/SequenceHandler.cs :1~50 (상수, ResolveSequenceName, IsSequenceActive)
    - WPF_Example/TcpServer/VisionResponsePacket.cs :226 (`testPacket.Site == (int)ESequence.Bottom` — enum 값 변경 금지 근거)
    - .planning/phases/73-side-4-jig-split/73-CONTEXT.md D-73-07 의 R5/R6
  </read_first>
  <action>
**(1) `Custom/Define/ID.cs` — enum 확장. 기존 값은 절대 바꾸지 않는다.**

`ESequence` 를 아래로 교체한다:

```csharp
public enum ESequence : int {
    Top = 1,
    // Side = 2 는 레거시 식별자로만 남긴다 — 더 이상 시퀀스로 등록하지 않는다(SIDE_1~4 로 분리됨).
    // 값 자체를 지우거나 재사용하지 않는 이유: VisionResponsePacket 이 (int)ESequence.Bottom 을
    // 와이어 site 정수와 직접 비교하고 있어 번호 재배치가 프로토콜 회귀를 만든다.
    Side = 2,
    Bottom = 3,
    Side1 = 4,
    Side2 = 5,
    Side3 = 6,
    Side4 = 7,
}
```

`EAction` 을 아래로 교체한다(FAI_Base=100 과 충돌 없음):

```csharp
public enum EAction : int {
    Top_Inspection = 2,
    Side_Inspection = 4,          // 레거시(ESequence.Side 와 짝) — 등록되지 않음
    Bottom_Inspection = 6,

    Side1_Inspection = 7,
    Side2_Inspection = 8,
    Side3_Inspection = 9,
    Side4_Inspection = 10,

    FAI_Base = 100,

    Unknown = Int32.MaxValue
}
```

**(2) `Custom/Sequence/SequenceHandler.cs` — 상수 추가**

`public const string SEQ_SIDE = "SIDE";` 아래에 추가한다(SEQ_SIDE 는 삭제하지 않는다 —
마이그레이션 스크립트/구 레시피의 구 값이며 v2.6 맵이 아직 참조한다):

```csharp
public const string SEQ_SIDE_1 = "SIDE_1";
public const string SEQ_SIDE_2 = "SIDE_2";
public const string SEQ_SIDE_3 = "SIDE_3";
public const string SEQ_SIDE_4 = "SIDE_4";
```

**(3) `ResolveSequenceName` — R6 조용한 폴백 제거**

```csharp
public static string ResolveSequenceName(ESequence seqId) {
    switch (seqId) {
        case ESequence.Top:    return SEQ_TOP;
        case ESequence.Side:   return SEQ_SIDE;      // 레거시 식별자 — 등록되지 않음
        case ESequence.Bottom: return SEQ_BOTTOM;
        case ESequence.Side1:  return SEQ_SIDE_1;
        case ESequence.Side2:  return SEQ_SIDE_2;
        case ESequence.Side3:  return SEQ_SIDE_3;
        case ESequence.Side4:  return SEQ_SIDE_4;
        default:
            // R6: 조용한 TOP 폴백이 SIDE_3 을 TOP 으로 해석해 원인 추적을 불가능하게 만들었다.
            // 폴백 자체는 유지하되(예외로 라인을 세우지 않는다) 반드시 흔적을 남긴다.
            Logging.PrintLog((int)ELogType.Error,
                "[SEQ] ResolveSequenceName 미등록 enum 값 {0} — TOP 으로 폴백(호출부 확인 필요)", (int)seqId);
            return SEQ_TOP;
    }
}
```

**(4) `IsSequenceActive` — R5**

```csharp
private static bool IsSequenceActive(ESequence seqId) {
    ECameraRole role = SystemSetting.Handle.CameraRole;
    if (role == ECameraRole.TopBottom) {
        return seqId == ESequence.Top || seqId == ESequence.Bottom;
    }
    // Side 역할 PC 는 지그 4개를 독립 시퀀스로 전부 활성화한다.
    // 레거시 ESequence.Side 는 여기서 의도적으로 제외 — SIDE_1~4 로 대체됐다.
    bool bIsSideJig = seqId == ESequence.Side1 || seqId == ESequence.Side2
        || seqId == ESequence.Side3 || seqId == ESequence.Side4;
    return bIsSideJig;
}
```

⚠ `SequenceHandler.cs` 상단 주석 ":39 SIMUL 은 전체 활성" 은 코드와 어긋나는 stale 문서 결함이다
(D-73-07 문서 결함 항목). 이번에 손대는 자리이므로 그 문장을 삭제한다.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement/WPF_Example &amp;&amp; grep -n "Side1 = 4" Custom/Define/ID.cs &amp;&amp; grep -c "SEQ_SIDE_" Custom/Sequence/SequenceHandler.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Side1 = 4" WPF_Example/Custom/Define/ID.cs` == 1, 동일하게 `Side2 = 5` / `Side3 = 6` / `Side4 = 7` 각 1건
    - `grep -c "Top = 1" WPF_Example/Custom/Define/ID.cs` == 1 이고 `Bottom = 3` 1건 (기존 값 무변경)
    - `grep -c "Side1_Inspection = 7" WPF_Example/Custom/Define/ID.cs` == 1, `Side4_Inspection = 10` 1건
    - `grep -c 'SEQ_SIDE_1 = "SIDE_1"' WPF_Example/Custom/Sequence/SequenceHandler.cs` == 1 (2/3/4 동일)
    - `ResolveSequenceName` 의 `default:` 블록에 `Logging.PrintLog` 호출 1건 존재
    - `grep -c "SIMUL 은 전체 활성" WPF_Example/Custom/Sequence/SequenceHandler.cs` == 0
    - **[W4] 리터럴 `?:` grep 금지**(C# 삼항은 `a ? b : c` 라 항상 0건 = 무의미). 이 plan diff 로 검사:
      `git diff -- <이 plan 이 고친 파일> | grep "^+" | grep -nE '\?\?|\?\.' || true` 0줄,
      `... | grep -vE '^\+\s*//' | grep -nE '\?[^;]*:' || true` 후보 육안 판별 후 진성 삼항 0건
  </acceptance_criteria>
  <done>SIDE_1~4 enum/상수가 존재하고, Side 역할 PC 에서 IsSequenceActive 가 4개 모두 true 를 반환한다.</done>
</task>

<task type="auto">
  <name>Task 3: 시퀀스 4개 등록/액션 등록/초기화/Action 재구축 6분기화</name>
  <files>WPF_Example/Custom/Sequence/SequenceHandler.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/SequenceHandler.cs :183~330 (RegisterSequences / RegisterActions / InitializeSequences / RebuildInspectionActions / TryLoadNewFormat)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs 생성자 시그니처
    - WPF_Example/Custom/Device/DeviceHandler.cs (CAMERA_SIDE 상수)
    - .planning/phases/73-side-4-jig-split/73-CONTEXT.md D-73-07 의 M5
  </read_first>
  <action>
**(1) `RegisterSequences()`** — 기존 `if (IsSequenceActive(ESequence.Side)) seqs.Add(new InspectionSequence(ESequence.Side, SEQ_SIDE, ...));` 한 줄을 아래 4블록으로 교체한다.
4개 모두 `DeviceHandler.CAMERA_SIDE` + `LightHandler.LIGHT_BAR` + `Side_Alg_Index` 를 공유한다:

```csharp
if (IsSequenceActive(ESequence.Side1))
    seqs.Add(new InspectionSequence(ESequence.Side1, SEQ_SIDE_1, Side_Alg_Index, DeviceHandler.CAMERA_SIDE, LightHandler.LIGHT_BAR));
if (IsSequenceActive(ESequence.Side2))
    seqs.Add(new InspectionSequence(ESequence.Side2, SEQ_SIDE_2, Side_Alg_Index, DeviceHandler.CAMERA_SIDE, LightHandler.LIGHT_BAR));
if (IsSequenceActive(ESequence.Side3))
    seqs.Add(new InspectionSequence(ESequence.Side3, SEQ_SIDE_3, Side_Alg_Index, DeviceHandler.CAMERA_SIDE, LightHandler.LIGHT_BAR));
if (IsSequenceActive(ESequence.Side4))
    seqs.Add(new InspectionSequence(ESequence.Side4, SEQ_SIDE_4, Side_Alg_Index, DeviceHandler.CAMERA_SIDE, LightHandler.LIGHT_BAR));
```

**(2) `RegisterActions()`** — 기존 Side 한 줄을 4블록으로 교체(전부 `TopSideInspectionAction`):

```csharp
if (IsSequenceActive(ESequence.Side1))
    acts.Add(new TopSideInspectionAction(EAction.Side1_Inspection, ACT_INSPECT, Side_Alg_Index, Inspection_Model_Index));
if (IsSequenceActive(ESequence.Side2))
    acts.Add(new TopSideInspectionAction(EAction.Side2_Inspection, ACT_INSPECT, Side_Alg_Index, Inspection_Model_Index));
if (IsSequenceActive(ESequence.Side3))
    acts.Add(new TopSideInspectionAction(EAction.Side3_Inspection, ACT_INSPECT, Side_Alg_Index, Inspection_Model_Index));
if (IsSequenceActive(ESequence.Side4))
    acts.Add(new TopSideInspectionAction(EAction.Side4_Inspection, ACT_INSPECT, Side_Alg_Index, Inspection_Model_Index));
```

**(3) `InitializeSequences()`** — 기존 Side 블록을 4블록으로 교체:

```csharp
if (IsSequenceActive(ESequence.Side1)) {
    SequenceBuilder seqSide1 = SequenceBuilder.CreateSequence(ESequence.Side1);
    seqSide1.AddAction(EAction.Side1_Inspection);
    RegisterSequence(seqSide1);
}
```
(Side2/Side3/Side4 동일 패턴, 지역변수명 `seqSide2`/`seqSide3`/`seqSide4`)

**(4) `TryLoadNewFormat()` 의 Rebuild 호출 (M5)** — `RebuildInspectionActions(ESequence.Side);` 한 줄을 아래 4줄로 교체한다. Top/Bottom 줄은 그대로 둔다:

```csharp
RebuildInspectionActions(ESequence.Side1);
RebuildInspectionActions(ESequence.Side2);
RebuildInspectionActions(ESequence.Side3);
RebuildInspectionActions(ESequence.Side4);
```

**(5) 빌드 검증** — `SIMUL_MODE` ON/OFF 양쪽으로 컴파일한다.
산출물이 잠겨 있으면 **프로세스를 종료하지 말고** 스크래치 OutDir 로 컴파일만 확인한다:

```bash
# ⚠ $SCRATCHPAD 는 이 셸에 정의돼 있지 않다 — 실제 스크래치패드 절대경로로 직접 설정할 것.
#    미설정 시 -p:OutDir="/b73on/" 가 되어 C:73on\ 에 산출된다.
SCR="/c/Users/tech/AppData/Local/Temp/claude/<session>/scratchpad"   # ← 실제 경로로 교체
mkdir -p "$SCR"
# ⚠ -t:Rebuild 필수 — 증분 빌드는 컴파일을 스킵해 경고 0줄이 나온다
# SIMUL_MODE ON
MSBuild.exe WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 \
  -p:OutDir="$SCR/b73on/" -p:IntermediateOutputPath="$SCR/objon/" -v:m -nologo
# SIMUL_MODE OFF - Release|x64 를 쓰면 안 된다(csproj 가 거기에도 SIMUL_MODE 를 켜 놨다).
MSBuild.exe WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 \
  -p:DefineConstants=TRACE%3BDEBUG -p:OutDir="$SCR/b73off/" -p:IntermediateOutputPath="$SCR/objoff/" -v:m -nologo
```

⚠ **이 Task 가 경고 baseline 을 바꾼다.** `RegisterActions()` 의 Side 호출 1줄이 4줄이 되면서
`[System.Obsolete]`(`Action_TopSideInspection.cs:233`) CS0618 이 **6줄 늘어난다**(컴파일 패스 2회 × 3줄).
→ 이 Task 완료 후부터 phase 종료까지 **SIMUL-ON 18줄 / SIMUL-OFF 16줄**이 정상이다.
에러는 0 이어야 하고, **새 경고 코드 종류가 생기면 실패**다. 숫자를 맞추려고 `[Obsolete]` 제거 /
`#pragma warning disable` / `NoWarn` 추가는 **금지**(73-BUILD-VERIFY.md §4).
`DatumMeasurement.csproj` 는 커밋하지 않는다(끝까지 unstaged 유지).
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement/WPF_Example &amp;&amp; grep -c "IsSequenceActive(ESequence.Side1)" Custom/Sequence/SequenceHandler.cs &amp;&amp; grep -c "RebuildInspectionActions(ESequence.Side" Custom/Sequence/SequenceHandler.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "IsSequenceActive(ESequence.Side1)" WPF_Example/Custom/Sequence/SequenceHandler.cs` == 3 (Register/RegisterActions/Initialize)
    - Side2/Side3/Side4 각각도 == 3
    - `grep -c "RebuildInspectionActions(ESequence.Side" WPF_Example/Custom/Sequence/SequenceHandler.cs` == 4
    - `grep -c "RebuildInspectionActions(ESequence.Side)" WPF_Example/Custom/Sequence/SequenceHandler.cs` == 0 (구 단일 호출 잔존 0)
    - `grep -c "new InspectionSequence(ESequence.Side," WPF_Example/Custom/Sequence/SequenceHandler.cs` == 0
    - SIMUL-ON 빌드 exit 0 / 에러 0 / 경고 **18줄**(73-01 Task3 이 CS0618 을 6줄 늘린다 — 정상)
    - SIMUL-OFF 빌드 exit 0 / 에러 0 / 경고 **16줄**
    - 두 빌드 모두 `grep -oE "CS[0-9]{4}" | sort -u` 결과가 {CS0618, CS0162}(ON) / {CS0618}(OFF) 를 벗어나지 않음
    - `git status --porcelain WPF_Example/DatumMeasurement.csproj` 가 staged 상태가 아님
  </acceptance_criteria>
  <done>PC2(CameraRole=Side)에서 SIDE_1~4 시퀀스 4개가 생성/등록되고 두 빌드 구성 모두 에러 0 으로 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 디스크 레시피 → 메모리 시퀀스 | 구 포맷 INI 의 Param0..N 위치 인덱스가 신뢰 없이 Action 에 매핑됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Tampering | `Sequence/SequenceHandler.cs` LoadFromIni Param0..N | mitigate | `ParamSequenceCount` 불일치 시 로드 스킵 + Error 로그(Task 1) |
| T-73-02 | Repudiation | `ResolveSequenceName` default 폴백 | mitigate | 폴백 시 Error 로그 필수(Task 2) |
| T-73-03 | Denial of Service | 미등록 시퀀스로 인한 OnCreate Error | mitigate | `IsSequenceActive` 를 등록 3지점 모두에서 동일 술어로 사용(Task 3) |
</threat_model>

<verification>
1. `grep -rn "ESequence.Side\b" WPF_Example --include=*.cs` 결과가 ID.cs / ResolveSequenceName / 주석에만 남는다(등록 경로 0건)
2. SIMUL 앱 기동 후 로그에 SIDE_1/SIDE_2/SIDE_3/SIDE_4 네 시퀀스 생성 흔적이 있다
3. 두 빌드 구성 모두 에러 0
</verification>

<success_criteria>
- SIDE_1~4 시퀀스가 생성/등록되고 각각 Inspect Action 을 1개 갖는다
- 동적 FAI 모드 저장 시 `Param0` 키가 새 레시피 파일에 존재하지 않는다
- 빌드 에러 0. 경고는 이 plan 완료 후 SIMUL-ON 18줄 / SIMUL-OFF 16줄(CS0618 6줄 증가는 정상)
</success_criteria>

<output>
완료 후 `.planning/phases/73-side-4-jig-split/73-01-SUMMARY.md` 를 작성한다.
</output>
