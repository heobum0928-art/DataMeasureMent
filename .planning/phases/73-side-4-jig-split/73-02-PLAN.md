---
phase: 73-side-4-jig-split
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/TcpServer/VisionRequestPacket.cs
  - WPF_Example/TcpServer/VisionResponsePacket.cs
autonomous: true
requirements: [PROTO-73-PREP-TYPE]
must_haves:
  truths:
    - "`$PREP:site,Type,z_index@` 3필드 수신 시 Type 과 z_index 가 각각 올바른 필드에서 파싱된다"
    - "필드 개수가 3이 아니거나 숫자 파싱에 실패해도 `$PREP_ACK ... FAIL` 이 반드시 송신된다(무응답 0)"
    - "`$PREP_ACK:site,Type,z_index,OK|FAIL@` 형식으로 응답이 나간다"
  artifacts:
    - path: "WPF_Example/TcpServer/VisionRequestPacket.cs"
      provides: "PrepPacket.Type / PrepPacket.IsRequestValid + 3필드 전용 파서"
      contains: "PREP_FIELD_COUNT"
    - path: "WPF_Example/TcpServer/VisionResponsePacket.cs"
      provides: "$PREP_ACK Type echo"
      contains: "packet.Type"
  key_links:
    - from: "TryParsePrepFields"
      to: "PrepPacket.Type / PrepPacket.ZIndex / PrepPacket.IsRequestValid"
      via: "명명 상수 인덱스, 실패 시 false 반환 대신 IsRequestValid=false"
      pattern: "IsRequestValid"
---

<objective>
`$PREP` 에 Type 필드를 도입한다(제어 협의 완료, D-73-05/D-73-08).
제어와 **동시 교체**하며 구버전 펌웨어는 존재하지 않으므로 **신규 3필드 포맷 전용**으로 구현한다.
필드 개수 분기(v1/v2 하위호환)는 넣지 않는다.

현 파서는 `dataList[1]` 을 **무조건 z_index 로 파싱**하므로 Type 이 들어오면 파싱이
"성공"하면서 Type 을 z 로 오인한다 — 예외도 FAIL 도 나지 않는 조용한 오동작이다.
명명 상수 인덱스 기반 파서로 다시 쓴다.

**단 하나의 절대 규칙:** 어떤 입력에서도 `$PREP_ACK` 이 반드시 나가야 한다.
현 파서 주석(`VisionRequestPacket.cs:428~429`)이 기록한 위험 그대로, 파싱 실패로 `null` 을
반환하면 응답 자체가 안 나가고 PLC 가 ACK 무한 대기 → 라인 정지가 된다.
따라서 파서는 **절대 false 를 반환하지 않고**, 대신 `IsRequestValid=false` 로 표시해
`ProcessPrep` 이 FAIL ACK 를 회신하게 한다.

Purpose: 이후 라우팅 plan(73-04)이 `$PREP` 에서 대상 시퀀스를 알 수 있게 하는 전제.
Output: 프로토콜 계층만 바뀐, 라우팅과 무관하게 독립 빌드 가능한 변경.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-side-4-jig-split/73-CONTEXT.md
@CLAUDE.md
@.planning/phases/73-side-4-jig-split/73-BUILD-VERIFY.md

**코딩 규칙:** 삼항 `?:` / `??` / `?.` 금지 → if-else. 전통 switch 만. C# 7.2. 헝가리언(b/n/sz).
이 두 파일은 **Allman 스타일**(여는 중괄호 새 줄)이 우세하다 — 주변 메서드 스타일을 그대로 따른다.
날짜 주석 새로 달지 않는다. csproj 커밋 금지.
실행 중 프로세스 종료 금지 — 산출물 잠김 시 스크래치 OutDir 로 컴파일만 검증.

**빌드 검증은 `73-BUILD-VERIFY.md` 규격을 따른다(전 plan 공통 단일 소스).**
- msbuild 는 `-p:` 대시 형식만 사용(`/p:` 는 MSYS 경로변환으로 MSB1008 실패)
- SIMUL-OFF 는 `Release|x64` 가 아니라 `-p:DefineConstants=TRACE%3BDEBUG` 로 검증(csproj 무수정)
- 경고 baseline: 73-01 Task3 이후 **SIMUL-ON 18줄 / SIMUL-OFF 16줄**(CS0618 6줄 증가는 정상)
- 통과 기준 1순위는 "에러 0 + **새 경고 코드 종류 0건**". 숫자를 맞추려고
  `[Obsolete]` 제거 / `#pragma warning disable` / `NoWarn` 추가는 **금지**
</context>

<interfaces>
현재 상태(실측):

```csharp
// TcpServer/VisionRequestPacket.cs :425~447 — 교체 대상
//260626 hbk v3.0: $PREP 수신 파서. dataList[0]=site, [1]=z_index.
private static bool TryParsePrepFields(string[] dataList, PrepPacket prepPacket)
{
    bool bHasFields = dataList != null && dataList.Length >= 2;
    if (!bHasFields) { return false; }
    int nSite = 0;
    bool bSiteOk = Int32.TryParse(dataList[0], out nSite);
    if (!bSiteOk) { return false; }
    prepPacket.Site = nSite;
    int nZIndex = 0;
    bool bZIndexOk = Int32.TryParse(dataList[1], out nZIndex);   // ← Type 이 와도 성공, z 로 오인
    if (!bZIndexOk) { return false; }
    prepPacket.ZIndex = nZIndex;
    return true;
}

// 호출부 :298~304 — false 면 null 반환 = ACK 미송신(라인 정지 위험)
case CMD_RECV_PREP:
    packet = new PrepPacket();
    PrepPacket prepPacket = packet.AsPrep();
    dataList = msgList[1].Split(VisionServer.MSG_CONTENTS_SEPERATOR);
    bool bPrepOk = TryParsePrepFields(dataList, prepPacket);
    if (!bPrepOk) { return null; }
    break;

// :614~620
public class PrepPacket : VisionRequestPacket { ... }   // ZIndex 보유, Type 없음

// 선례: TryParseResetFields(:454~468) 는 "절대 false 를 반환하지 않는다" 를 이미 구조로 구현했다.

// TcpServer/VisionResponsePacket.cs :432~452
private static string BuildPrepAckMessage(PrepAckPacket packet)
// 현재: $PREP_ACK:{Site},{ZIndex},{OK|FAIL}
```

`ProcessPrep`(`Custom/SystemHandler.cs:884`)은 `packet == null` 일 때만 `null` 을 반환한다.
이 plan 이후로는 `TryParsePrepFields` 가 항상 true 를 반환하므로 packet 은 절대 null 이 아니고,
`ackPacket.IsOk` 기본값이 `false` 이므로 **잘못된 입력은 자동으로 FAIL ACK 가 된다.**
`IsRequestValid` 를 실제로 읽어 조기 FAIL 처리 + 로그를 남기는 것은 73-04 Task 1 소관이다.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: $PREP 파서를 3필드 전용으로 재작성 + 무응답 경로 제거 (M10 전반)</name>
  <files>WPF_Example/TcpServer/VisionRequestPacket.cs</files>
  <read_first>
    - WPF_Example/TcpServer/VisionRequestPacket.cs :20~60 (상수 블록), :290~315 (CMD_RECV_PREP / CMD_RECV_RESET 디스패치), :425~470 (PREP/RESET 파서 — RESET 파서의 "절대 false 반환 안 함" 주석이 이번 구현의 선례), :600~640 (PrepPacket 정의)
    - .planning/phases/73-side-4-jig-split/73-CONTEXT.md D-73-05 (⚠ 파서 재작성 필수 단락)
  </read_first>
  <action>
**(1) 명명 상수 추가** — `$TEST` 상수 블록(`TEST_FIELD_SITE` 등이 있는 :40~50 부근) 바로 아래:

```csharp
// $PREP 필드 인덱스 — 제어 협의 확정 포맷 $PREP:site,Type,z_index@ 전용(3필드 고정).
// 구버전 펌웨어는 존재하지 않는다(제어와 동시 교체) — 필드 개수 분기 없음.
private const int PREP_FIELD_SITE = 0;
private const int PREP_FIELD_TYPE = 1;
private const int PREP_FIELD_ZINDEX = 2;
private const int PREP_FIELD_COUNT = 3;
```

**(2) `PrepPacket` 에 프로퍼티 2개 추가** (:614 클래스 정의 안):

```csharp
// 검사 대상 코드(0=TOP / 1=BOTTOM / 2~5=SIDE_1~4). 라우팅 해석은 ResourceMap 이 담당한다.
public string Type { get; set; } = "";

// 수신 필드가 규격($PREP:site,Type,z_index@)을 만족했는지. false 면 ProcessPrep 이 FAIL ACK 를 회신한다.
// 파서가 실패를 이 플래그로 넘기는 이유: 파서가 false 를 반환하면 호출부(:303)가 null 을 반환해
// 응답 자체가 안 나가고 PLC 가 ACK 를 무한 대기(라인 정지)한다. TryParseResetFields 와 같은 설계.
public bool IsRequestValid { get; set; } = true;
```

**(3) `TryParsePrepFields` 를 아래로 전면 교체한다** — 반환값은 **항상 true**:

```csharp
// $PREP 수신 파서 — $PREP:site,Type,z_index@ (3필드 고정).
// 절대 false 를 반환하지 않는다. 규격 위반 입력은 IsRequestValid=false 로 표시해서 넘기고,
// ProcessPrep 이 $PREP_ACK ... FAIL 을 회신한다. false 를 반환하면 호출부가 null 을 돌려
// 응답 자체가 사라지고 PLC 가 ACK 무한 대기(라인 정지)에 빠진다.
private static bool TryParsePrepFields(string[] dataList, PrepPacket prepPacket)
{
    prepPacket.IsRequestValid = false;   // 아래 전 항목을 통과해야만 true 로 승격
    prepPacket.Site = 0;
    prepPacket.Type = "";
    prepPacket.ZIndex = 0;

    bool bHasExactFields = dataList != null && dataList.Length == PREP_FIELD_COUNT;
    if (!bHasExactFields)
    {
        int nGotLength = 0;
        if (dataList != null) { nGotLength = dataList.Length; }
        Logging.PrintLog((int)ELogType.Error,
            "[PREP] 규격 위반 — 필드 {0}개 수신(기대 {1}개: site,Type,z_index). FAIL ACK 회신.",
            nGotLength, PREP_FIELD_COUNT);
        return true;
    }

    int nSite = 0;
    bool bSiteOk = Int32.TryParse(dataList[PREP_FIELD_SITE], out nSite);
    if (!bSiteOk)
    {
        Logging.PrintLog((int)ELogType.Error, "[PREP] site 파싱 실패 — 원본='{0}'. FAIL ACK 회신.", dataList[PREP_FIELD_SITE]);
        return true;
    }

    int nZIndex = 0;
    bool bZIndexOk = Int32.TryParse(dataList[PREP_FIELD_ZINDEX], out nZIndex);
    if (!bZIndexOk)
    {
        Logging.PrintLog((int)ELogType.Error, "[PREP] z_index 파싱 실패 — 원본='{0}'. FAIL ACK 회신.", dataList[PREP_FIELD_ZINDEX]);
        return true;
    }

    string szType = dataList[PREP_FIELD_TYPE];
    if (szType == null) { szType = ""; }
    szType = szType.Trim();
    bool bHasType = szType.Length > 0;
    if (!bHasType)
    {
        Logging.PrintLog((int)ELogType.Error, "[PREP] Type 필드 비어 있음. FAIL ACK 회신.");
        return true;
    }

    prepPacket.Site = nSite;
    prepPacket.Type = szType;
    prepPacket.ZIndex = nZIndex;
    prepPacket.IsRequestValid = true;
    return true;
}
```

`Logging` / `ELogType` using 이 이 파일에 없으면 `ReringProject.Utility` / `ReringProject.Define` using 을 추가한다.

**(4) 호출부(:298~304)의 `if (!bPrepOk) { return null; }` 를 제거한다:**

```csharp
case CMD_RECV_PREP:
    packet = new PrepPacket();
    PrepPacket prepPacket = packet.AsPrep();
    dataList = msgList[1].Split(VisionServer.MSG_CONTENTS_SEPERATOR);
    TryParsePrepFields(dataList, prepPacket);   // 반환값 무시 — 이 파서는 항상 true(위 주석 참고)
    break;
```
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement/WPF_Example &amp;&amp; grep -c "PREP_FIELD_COUNT\|IsRequestValid" TcpServer/VisionRequestPacket.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PREP_FIELD_TYPE" WPF_Example/TcpServer/VisionRequestPacket.cs` >= 2
    - `grep -c "PREP_FIELD_ZINDEX" WPF_Example/TcpServer/VisionRequestPacket.cs` >= 2
    - `grep -c "PREP_FIELD_COUNT" WPF_Example/TcpServer/VisionRequestPacket.cs` >= 3
    - `grep -c "IsRequestValid" WPF_Example/TcpServer/VisionRequestPacket.cs` >= 6
    - **무응답 경로 0:** `grep -n "bPrepOk" WPF_Example/TcpServer/VisionRequestPacket.cs` 결과 0건, 그리고 `CMD_RECV_PREP` case 블록 안에 `return null;` 0건
    - **하위호환 분기 없음:** `TryParsePrepFields` 본문에 `Length >= 2` / `>= 3` 형태의 개수 분기가 없고 `== PREP_FIELD_COUNT` 정확 비교 1건만 존재
    - `TryParsePrepFields` 본문에 `return false;` 0건
    - `TryParsePrepFields` 본문에 리터럴 `dataList[1]` 0건 (awk 로 메서드 범위 한정 후 `! grep -q 'dataList\[1\]'`)
    - **[W4] 코딩 규칙 위반 검사는 리터럴 `?:` grep 으로 하지 않는다** — C# 삼항은 `a ? b : c` 라
      두 글자 `?:` 는 원래부터 0건이라 항상 통과하는 무의미한 조건이다. 대신 이 plan 의 diff 에 대해:
      `git diff -- <이 plan 이 고친 파일> | grep "^+" | grep -nE '\?\?|\?\.' || true` 가 0줄이고,
      `... | grep -vE '^\+\s*//' | grep -nE '\?[^;]*:' || true` 후보를 육안 판별해 진성 삼항 0건
  </acceptance_criteria>
  <done>3필드 $PREP 은 Type/ZIndex 가 정확히 파싱되고, 필드 개수 위반·숫자 파싱 실패·빈 Type 은 Error 로그와 함께 `IsRequestValid=false` 로 표시되며 어떤 경우에도 파서가 null 경로를 만들지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: $PREP_ACK 에 Type echo 추가 (M12) + M8 커플링 주석 명시</name>
  <files>WPF_Example/TcpServer/VisionResponsePacket.cs</files>
  <read_first>
    - WPF_Example/TcpServer/VisionResponsePacket.cs :415~470 (BuildPrepAckMessage / BuildResetAckMessage)
    - WPF_Example/TcpServer/VisionResponsePacket.cs :220~235 (`testPacket.Site == (int)ESequence.Bottom` — M8)
    - `PrepAckPacket` 클래스 정의(같은 파일 내 검색)
    - .planning/phases/73-side-4-jig-split/73-CONTEXT.md D-73-05 의 M12, D-73-07 의 M8
  </read_first>
  <action>
**(1) `PrepAckPacket` 에 `Type` 프로퍼티 추가** (`Site`/`ZIndex`/`IsOk` 와 같은 자리):

```csharp
// 요청 $PREP 의 Type 을 그대로 echo 한다. 규격 위반 요청(FAIL ACK)이면 "" 가 들어온다.
public string Type { get; set; } = "";
```

**(2) `BuildPrepAckMessage` 를 아래로 교체한다** — 형식 `$PREP_ACK:site,Type,z_index,OK|FAIL@` (구분자 3개):

```csharp
// $PREP_ACK 직렬화 → $PREP_ACK:site,Type,z_index,OK|FAIL@ (구분자 3개).
//  Type 은 요청 echo. 규격 위반 요청이라 Type 을 못 읽었으면 빈 필드를 그대로 내보낸다 —
//  임의 기본값("0"=TOP)을 채우면 제어가 "우리가 안 보낸 Type 을 받았다"고 오해한다.
//  IsOk 의미(D-73-08 확정): FAIL = 조명 세팅 실패 또는 요청 규격 위반. 검사 항목 유무는 반영하지 않는다.
private static string BuildPrepAckMessage(PrepAckPacket packet)
{
    string szMsg = "";
    szMsg += CMD_SEND_PREP_ACK;
    szMsg += VisionServer.MSG_CMD_SEPERATOR;       // ':'
    szMsg += packet.Site.ToString();
    szMsg += VisionServer.MSG_CONTENTS_SEPERATOR;  // ','
    string szType = packet.Type;
    bool bHasType = !string.IsNullOrEmpty(szType);
    if (bHasType)
    {
        szMsg += szType;
    }
    szMsg += VisionServer.MSG_CONTENTS_SEPERATOR;  // ','
    szMsg += packet.ZIndex.ToString();
    szMsg += VisionServer.MSG_CONTENTS_SEPERATOR;  // ','
    bool bIsOk = packet.IsOk;
    if (bIsOk)
    {
        szMsg += "OK";
    }
    else
    {
        szMsg += "FAIL";
    }
    return szMsg;
}
```

**(3) M8 — `:226` 의 `testPacket.Site == (int)ESequence.Bottom`**

와이어 site 정수를 내부 `ESequence` enum 값과 직접 비교하는 잘못된 커플링이다. 다만 이번 phase 에서
`ESequence.Bottom = 3` 값은 **바뀌지 않으므로 동작 회귀는 없다**. 동작을 바꾸지 말고(v2.6 경로 회귀 0 우선)
그 줄 위에 아래 주석만 추가한다:

```csharp
// ⚠ 커플링 결함: 와이어 site 정수를 내부 ESequence enum 값과 직접 비교한다.
//  Phase 73 은 ESequence.Bottom=3 을 유지하므로 동작 회귀는 없으나, enum 번호를 재배치하면
//  이 비교가 조용히 깨진다. v2.6 경로 전용이라 이번 phase 에서는 동작을 바꾸지 않는다.
```

**(4) 빌드 검증** — Debug|x64 / Release|x64 양쪽. 산출물 잠김 시 스크래치 OutDir 사용, 프로세스 종료 금지.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement/WPF_Example &amp;&amp; grep -n "packet.Type" TcpServer/VisionResponsePacket.cs &amp;&amp; grep -c "커플링 결함" TcpServer/VisionResponsePacket.cs</automated>
  </verify>
  <acceptance_criteria>
    - `BuildPrepAckMessage` 본문 안의 `VisionServer.MSG_CONTENTS_SEPERATOR` 출현 == 3
    - `grep -c "packet.Type" WPF_Example/TcpServer/VisionResponsePacket.cs` >= 1
    - **[W4 정정]** `public string Type` 은 편집 전에도 이미 1건이라 `>= 1` 은 무의미하다.
      `PrepAckPacket` 클래스 범위로 한정해 센다:
      `awk '/class PrepAckPacket/{f=1} f&&/^    \}/{print;exit} f' WPF_Example/TcpServer/VisionResponsePacket.cs | grep -c "public string Type"` == 1
    - `grep -c "커플링 결함" WPF_Example/TcpServer/VisionResponsePacket.cs` == 1
    - `BuildResetAckMessage` 무변경(`git diff` 에서 해당 메서드 hunk 0)
    - SIMUL-ON 빌드 exit 0 / 에러 0 / 경고 18줄 · SIMUL-OFF 빌드 exit 0 / 에러 0 / 경고 16줄 (73-BUILD-VERIFY.md §2~4, 새 경고 코드 종류 0건)
    - **[W4] 코딩 규칙 위반 검사는 리터럴 `?:` grep 으로 하지 않는다** — C# 삼항은 `a ? b : c` 라
      두 글자 `?:` 는 원래부터 0건이라 항상 통과하는 무의미한 조건이다. 대신 이 plan 의 diff 에 대해:
      `git diff -- <이 plan 이 고친 파일> | grep "^+" | grep -nE '\?\?|\?\.' || true` 가 0줄이고,
      `... | grep -vE '^\+\s*//' | grep -nE '\?[^;]*:' || true` 후보를 육안 판별해 진성 삼항 0건
  </acceptance_criteria>
  <done>$PREP_ACK 문자열이 `PREP_ACK:2,3,1,OK` 형태(필드 4개)로 조립되고, Type 을 못 읽은 요청은 `PREP_ACK:0,,0,FAIL` 로 나간다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLC(제어) → 비전 TCP 서버 | 신뢰 없는 문자열 필드가 $PREP 로 들어온다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-04 | Spoofing | `TryParsePrepFields` Type 필드 | mitigate | Type 은 문자열로만 보관, 라우팅 해석은 73-04 의 화이트리스트 switch 가 담당(미지 값 → 폴백+로그) |
| T-73-05 | Denial of Service | 파서 실패 시 무응답 → PLC ACK 무한 대기 → 라인 정지 | mitigate | 파서가 절대 false 를 반환하지 않고 `IsRequestValid=false` 로 넘겨 FAIL ACK 를 보장 |
| T-73-06 | Tampering | 필드 위치 위조로 Type↔z_index 오인 | mitigate | 명명 상수 인덱스 + 필드 개수 정확 비교(`== 3`), 리터럴 `dataList[1]` 제거 |
</threat_model>

<verification>
1. `$PREP:2,4,1@` → Site=2, Type="4", ZIndex=1, IsRequestValid=true
2. `$PREP:2,1@` (2필드) → IsRequestValid=false, Error 로그, ACK 는 FAIL 로 송신됨
3. `$PREP:2,4,1,99@` (4필드) → IsRequestValid=false, Error 로그, FAIL ACK
4. `$PREP:2,,1@` (Type 빈값) → IsRequestValid=false, FAIL ACK
5. 위 4케이스 전부에서 `Parse` 가 null 을 반환하지 않는다
</verification>

<success_criteria>
- $PREP 이 3필드 전용으로 파싱되고 Type 이 z_index 로 오인되지 않는다
- 규격 위반 입력에서 ACK 누락 경로가 코드상 존재하지 않는다
- $PREP_ACK 에 Type 이 echo 된다
- SIMUL-ON / SIMUL-OFF 두 빌드 에러 0
</success_criteria>

<output>
완료 후 `.planning/phases/73-side-4-jig-split/73-02-SUMMARY.md` 를 작성한다.
</output>
