---
phase: 73-side-4-jig-split
plan: 03
type: execute
wave: 2
depends_on: ["73-01"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - scripts/migrate_phase73_recipe.py
autonomous: false
requirements: [PROTO-73-RECIPE-MIGRATION]
must_haves:
  truths:
    - "레시피 저장/로드가 FIXTURE_SIDE_1~4 네 섹션을 각각 SIDE_1~4 시퀀스에 매핑한다"
    - "마이그레이션 후 SIDE Datum 4개 / SIDE Shot 8개 / SIDE 측정 25개가 하나도 사라지지 않는다"
    - "Datum 패턴 모델(.shm) 경로가 여전히 `FAI_1\\SIDE\\` 를 가리켜 재티칭이 필요 없다"
  artifacts:
    - path: "scripts/migrate_phase73_recipe.py"
      provides: "main.ini 를 새 파일로 재작성 + 편집 전후 카운트 비교"
      contains: "FIXTURE_SIDE_1"
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs"
      provides: "FIXTURE_SIDE_1~4 저장/로드 (비활성 시퀀스 Datum 보존 유지)"
      contains: "FIXTURE_SIDE_1"
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "SIDE_* → SIDE 모델 폴더 정규화"
      contains: "NormalizeModelFolderName"
  key_links:
    - from: "InspectionRecipeManager.Save"
      to: "PreserveFixtureFromExisting"
      via: "시퀀스 미등록 시 DatumCount=0 덮어쓰기 금지 경로"
      pattern: "PreserveFixtureFromExisting"
    - from: "ResolveDatumModelPath"
      to: "RecipeFileHelper.GetPatternModelFilePath"
      via: "NormalizeModelFolderName(seqName)"
      pattern: "NormalizeModelFolderName"
---

<objective>
레시피 영속화를 SIDE_1~4 로 분할하고, 현 `main.ini` 를 스크립트로 마이그레이션한다.

**이 plan 이 이 phase 에서 가장 위험하다.** 과거 CameraRole 전환 저장에서 비활성 시퀀스의
Datum 이 `DatumCount=0` 으로 덮어써져 영구 소실된 사고(커밋 `3faa91b`)가 있었고,
**SIDE Datum 4개는 현재 유일본**이다. 백업은 이미
`D:\Backup\FAI_1_backup_before_phase73_260826\`(.shm 31개 포함)에 있다.

Purpose: 지그별 독립 z(0부터 시작)와 소속을 데이터에 반영.
Output: 마이그레이션된 `main.ini` + 4분할 저장/로드 코드 + .shm 재티칭 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-side-4-jig-split/73-CONTEXT.md
@.planning/phases/73-side-4-jig-split/73-01-SUMMARY.md
@CLAUDE.md
@.planning/phases/73-side-4-jig-split/73-BUILD-VERIFY.md

**코딩 규칙:** 삼항 `?:` / `??` / `?.` 금지. 전통 switch 만. C# 7.2. 헝가리언.
`InspectionRecipeManager.cs` 는 K&R(여는 중괄호 같은 줄), `InspectionSequence.cs` 는 Allman —
**파일별 기존 스타일을 그대로 따르고 섞지 않는다.**

**UI 규칙:** `MainView.xaml.cs` 는 4,300줄로 이 코드베이스 최대 문제 파일이다.
이 plan 에서 허용되는 변경은 **:4149 `roles` 배열 원소 교체 한 줄뿐**이다.
새 로직·새 메서드를 이 파일에 추가하지 않는다. 이번 phase 는 리팩토링이 목적이 아니므로
나머지 code-behind 는 그대로 둔다.

**절대 금지:** 원본 `D:\Data\Recipe\FAI_1\main.ini` 를 스크립트로 직접 덮어쓰기.
반드시 새 파일로 출력 → diff/카운트 확인 → 사람 승인 후 교체.

**빌드 검증은 `73-BUILD-VERIFY.md` 규격을 따른다(전 plan 공통 단일 소스).**
- msbuild 는 `-p:` 대시 형식만 사용(`/p:` 는 MSYS 경로변환으로 MSB1008 실패)
- SIMUL-OFF 는 `Release|x64` 가 아니라 `-p:DefineConstants=TRACE%3BDEBUG` 로 검증(csproj 무수정)
- 경고 baseline: 73-01 Task3 이후 **SIMUL-ON 18줄 / SIMUL-OFF 16줄**(CS0618 6줄 증가는 정상)
- 통과 기준 1순위는 "에러 0 + **새 경고 코드 종류 0건**". 숫자를 맞추려고
  `[Obsolete]` 제거 / `#pragma warning disable` / `NoWarn` 추가는 **금지**
</context>

<interfaces>
현행 데이터 실측(2026-08-26, `D:\Data\Recipe\FAI_1\main.ini`):

**SIDE Shot 8개** (섹션 `SHOT_n` + 카메라 서브섹션 `SHOT_n_CAM`, `OwnerSequenceName`/`ZIndex` 는 `_CAM` 쪽에 있다):

| 섹션 | ShotName | 현 ZIndex | 신규 Owner | 신규 ZIndex |
|---|---|---|---|---|
| SHOT_4  | SIDE_SHOT_3-1_D1         | 2  | SIDE_1 | 2 |
| SHOT_6  | SIDE_SHOT_3_2_D1         | 5  | SIDE_2 | 2 |
| SHOT_26 | SIDE_SHOT_3_2_D1         | 6  | SIDE_2 | 3 |
| SHOT_3  | SIDE_SHOT_4-2_H5         | 9  | SIDE_3 | 2 |
| SHOT_23 | SIDE_SHOT_4-2_C13-14_P1  | 10 | SIDE_3 | 3 |
| SHOT_24 | SIDE_SHOT_4-2_F9         | 11 | SIDE_3 | 4 |
| SHOT_5  | SIDE_SHOT_4-1_F9         | 14 | SIDE_4 | 2 |
| SHOT_25 | SIDE_SHOT_4-1_C13-14     | 15 | SIDE_4 | 3 |

**SIDE Datum 4개** (`[FIXTURE_SIDE]` DatumCount=4, 서브섹션 `FIXTURE_SIDE_DATUM_0..3`):

| 현 섹션 | DatumName | 현 ZIndexA/B | 신규 섹션 | 신규 ZIndexA/B |
|---|---|---|---|---|
| FIXTURE_SIDE_DATUM_0 | Side_Datum_3-1 | 0 / 1  | FIXTURE_SIDE_1_DATUM_0 | 0 / 1 |
| FIXTURE_SIDE_DATUM_1 | Side_Datum_3-2 | 3 / 4  | FIXTURE_SIDE_2_DATUM_0 | 0 / 1 |
| FIXTURE_SIDE_DATUM_2 | Side_Datum_4-2 | 7 / 8  | FIXTURE_SIDE_3_DATUM_0 | 0 / 1 |
| FIXTURE_SIDE_DATUM_3 | Side_Datum_4-1 | 12 / 13| FIXTURE_SIDE_4_DATUM_0 | 0 / 1 |

**지그별 z 오프셋:** SIDE_1 = −0 / SIDE_2 = −3 / SIDE_3 = −7 / SIDE_4 = −12
(Datum 은 항상 0,1 / 측정 Shot 은 2 부터 연속. 결과 z 범위: SIDE_1 0~2, SIDE_2 0~3, SIDE_3 0~4, SIDE_4 0~3)

**코드 현행:**
```csharp
// Custom/Sequence/Inspection/InspectionRecipeManager.cs
private InspectionSequence ResolveFixtureSequence(ESequence seqId)                                    // :76
private void SaveFixtureForSequence(IniFile saveFile, ESequence seqId, string sectionPrefix, IniFile existingFile)  // :85
private void PreserveFixtureFromExisting(IniFile saveFile, IniFile existingFile, string sectionPrefix)             // :107
private void LoadFixtureForSequence(IniFile loadFile, ESequence seqId, string sectionPrefix)          // :128
// 호출부
SaveFixtureForSequence(saveFile, ESequence.Side,   "FIXTURE_SIDE",   existingFile);   // :193
SaveFixtureForSequence(saveFile, ESequence.Bottom, "FIXTURE_BOTTOM", existingFile);   // :194
LoadFixtureForSequence(loadFile, ESequence.Side,   "FIXTURE_SIDE");                   // :272
LoadFixtureForSequence(loadFile, ESequence.Bottom, "FIXTURE_BOTTOM");                 // :273

// Custom/Sequence/Inspection/InspectionSequence.cs
public static string ResolveDatumModelPath(DatumConfig datum)                  // :2158
public static string ResolveDatumModelPath(DatumConfig datum, string ownerSeqName)  // :2186
public static string ResolveDatumModelPath2(DatumConfig datum)                 // :2229 (미러, propertyName 에 "_2")
// 셋 다 마지막에: SystemHandler.Handle.Recipes.GetPatternModelFilePath(recipeName, seqName, "Datum", propertyName, datum.PatternEngine)
// seqName 은 matched ShotConfig.OwnerSequenceName 에서 온다 → 마이그레이션 후 "SIDE_1" 이 된다

// Utility/RecipeFileHelper.cs :103
// 경로 = RecipeSavePath\{recipeName}\{seqName}\Datum{DatumName}.shm

// UI/ContentItem/MainView.xaml.cs :4149
ESequence[] roles = new ESequence[] { ESequence.Top, ESequence.Side, ESequence.Bottom };
```

**실존 .shm (마이그레이션 후에도 이 경로가 유지돼야 한다):**
`D:\Data\Recipe\FAI_1\SIDE\DatumSide_Datum_3-1.shm`, `..._3-1_2.shm`, `..._3-2(.shm/_2)`,
`..._4-1(.shm/_2)`, `..._4-2(.shm/_2)` — 총 8개
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 모델 폴더 정규화 (M7) — SIDE_1~4 가 기존 SIDE 폴더의 .shm 을 계속 쓰게 한다</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs :2155~2260 (ResolveDatumModelPath 1-arg / 2-arg / ResolveDatumModelPath2)
    - WPF_Example/Utility/RecipeFileHelper.cs :90~120 (GetPatternModelFilePath — 폴더 = seqName)
    - WPF_Example/Halcon/Services/TeachingStorageService.cs :219~239 (NormalizeTeachingKey — 이 경로와 **무관**하다는 것을 눈으로 확인할 것)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs :60~75 (ModelPath 프로퍼티 = 티칭 저장/로드 공통 진입점)
    - .planning/phases/73-side-4-jig-split/73-CONTEXT.md D-73-03 (특히 "단, plan 에서 확인할 것" 단락)
  </read_first>
  <action>
D-73-03 은 "`TeachingStorageService.NormalizeTeachingKey` 의 `Contains("SIDE")` 를 그대로 둔다"로
결정했지만, **실제 Datum .shm 경로는 그 함수를 거치지 않는다.** 경로는
`RecipeFileHelper.GetPatternModelFilePath(recipeName, seqName, "Datum", DatumName, engine)` 가
`RecipeSavePath\FAI_1\{seqName}\Datum{DatumName}.shm` 로 만들고, `seqName` 은 matched
`ShotConfig.OwnerSequenceName` 에서 온다. 마이그레이션으로 그 값이 `SIDE` → `SIDE_1` 이 되면
경로가 `FAI_1\SIDE_1\...` 로 바뀌어 **기존 8개 .shm 을 못 찾고 전 항목이 조용히 실패**한다.

따라서 `InspectionSequence.cs` 에 폴더 정규화 헬퍼를 추가하고, 경로를 만드는 3개 메서드가
모두 그것을 거치게 한다. 저장 경로와 로드 경로가 같은 함수를 쓰므로 대칭성이 유지된다.

**(1) 헬퍼 추가** (`ResolveDatumModelPath(DatumConfig)` 바로 위, Allman 스타일):

```csharp
// 패턴 모델(.shm/.ncm) 저장 폴더 이름 정규화.
//  Phase 73 이 SIDE 를 SIDE_1~4 로 쪼갰지만 모델 파일은 기존 FAI_1\SIDE\ 폴더에 그대로 둔다
//  (Datum 파일명이 DatumName 기준이라 4개가 서로 겹치지 않는다 → 재티칭 비용 0, D-73-03).
//  이 정규화가 없으면 OwnerSequenceName="SIDE_1" 이 FAI_1\SIDE_1\ 를 가리켜 기존 모델을 못 찾고
//  전 항목이 조용히 실패한다(예외 없음, 로그 없음).
//  저장/로드가 같은 함수를 통과하므로 이 한 곳만 바꾸면 대칭이 유지된다.
public static string NormalizeModelFolderName(string szSeqName)
{
    bool bIsEmpty = string.IsNullOrEmpty(szSeqName);
    if (bIsEmpty)
    {
        return SequenceHandler.SEQ_TOP;
    }
    bool bIsSideJig = szSeqName.StartsWith(SequenceHandler.SEQ_SIDE, StringComparison.OrdinalIgnoreCase);
    if (bIsSideJig)
    {
        return SequenceHandler.SEQ_SIDE;
    }
    return szSeqName;
}
```

**(2) 경로 조립 메서드 — 3개가 아니라 정확히 4개다. 전부 고친다.**

| # | 시그니처 | 줄 |
|---|---|---|
| 1 | `ResolveDatumModelPath(DatumConfig datum)` | :2158 |
| 2 | `ResolveDatumModelPath(DatumConfig datum, string ownerSeqName)` | :2186 |
| 3 | `ResolveDatumModelPath2(DatumConfig datum)` | :2229 |
| 4 | `ResolveDatumModelPath2(DatumConfig datum, string ownerSeqName)` | **:2253** |

⚠ **4번을 빠뜨리면 패턴2 모델(`DatumSide_Datum_*_2.shm`, 실존 4개)이 예외도 로그도 없이 안 잡힌다.**
호출부: `InspectionSequence.cs:2348`, `MainView.xaml.cs:3866`, `MainView.xaml.cs:4079`.

네 메서드 각각에서 `GetPatternModelFilePath(recipeName, seqName, ...)` 호출 **직전**에 한 줄을 넣는다:

```csharp
seqName = NormalizeModelFolderName(seqName);
```

그 외 로직은 **무변경** — 경로 불일치는 ALIGN_FAIL 로 직결되므로 리팩토링하지 않는다.

**(3) 진단 로그 1줄 추가** — `ResolveDatumModelPath(DatumConfig, string)` 의 return 직전에,
분리 직후 첫 검사에서 실제 경로를 눈으로 확인할 수 있게 한다(D-73-03 요구):

```csharp
string szModelPath = SystemHandler.Handle.Recipes.GetPatternModelFilePath(recipeName, seqName, actName, propertyName, datum.PatternEngine);
Logging.PrintLog((int)ELogType.Trace, "[DatumModelPath] owner={0} folder={1} path={2}", ownerSeqName, seqName, szModelPath);
return szModelPath;
```
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement/WPF_Example &amp;&amp; grep -c "NormalizeModelFolderName" Custom/Sequence/Inspection/InspectionSequence.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NormalizeModelFolderName" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` **== 5** (정의 1 + 호출 4 — `>= 4` 로 두면 3곳만 고쳐도 통과해 패턴2 모델이 조용히 깨진다)
    - 네 메서드 각각의 본문에 `seqName = NormalizeModelFolderName(seqName);` 가 1건씩 존재:
      `awk 'NR>=2150 && NR<=2290' WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs | grep -c "NormalizeModelFolderName(seqName)"` == 4
    - `grep -c "\[DatumModelPath\]" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` == 1
    - `WPF_Example/Halcon/Services/TeachingStorageService.cs` 무변경(`git diff --stat` 에 미등장)
    - **[W4] 코딩 규칙 위반 검사는 리터럴 `?:` grep 으로 하지 않는다** — C# 삼항은 `a ? b : c` 라
      두 글자 `?:` 는 원래부터 0건이라 항상 통과하는 무의미한 조건이다. 대신 이 plan 의 diff 에 대해:
      `git diff -- <이 plan 이 고친 파일> | grep "^+" | grep -nE '\?\?|\?\.' || true` 가 0줄이고,
      `... | grep -vE '^\+\s*//' | grep -nE '\?[^;]*:' || true` 후보를 육안 판별해 진성 삼항 0건
  </acceptance_criteria>
  <done>OwnerSequenceName 이 SIDE_1~4 여도 모델 경로 폴더가 `SIDE` 로 정규화되어 기존 8개 .shm 을 그대로 사용한다.</done>
</task>

<task type="auto">
  <name>Task 2: FIXTURE_SIDE → FIXTURE_SIDE_1~4 저장/로드 분할 (M4) + MainView roles (M9)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs, WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs :75~150 (ResolveFixtureSequence / SaveFixtureForSequence / PreserveFixtureFromExisting / LoadFixtureForSequence — **특히 :88 의 "DatumCount=0 으로 덮어쓰지 말고" 주석**)
    - WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs :175~200, :265~280 (호출부)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs :4143~4160 (GetAnyInspectionSequence / GetInspectionSequenceForDatum)
    - .planning/phases/73-side-4-jig-split/73-CONTEXT.md D-73-07 의 M4/M9 및 "위험 요소 1. Datum 소실"
  </read_first>
  <action>
**(1) 저장 호출부 (:193)** — 한 줄을 4줄로 교체한다. `SaveFixtureForSequence` /
`PreserveFixtureFromExisting` **본문은 건드리지 않는다** — 그 보존 로직이 `3faa91b` 사고의
수정 결과물이며, 4분할 후에도 섹션 prefix 단위로 그대로 동작한다:

```csharp
SaveFixtureForSequence(saveFile, ESequence.Side1, "FIXTURE_SIDE_1", existingFile);
SaveFixtureForSequence(saveFile, ESequence.Side2, "FIXTURE_SIDE_2", existingFile);
SaveFixtureForSequence(saveFile, ESequence.Side3, "FIXTURE_SIDE_3", existingFile);
SaveFixtureForSequence(saveFile, ESequence.Side4, "FIXTURE_SIDE_4", existingFile);
```
`SaveFixtureForSequence(saveFile, ESequence.Bottom, "FIXTURE_BOTTOM", existingFile);` 줄은 그대로 둔다.

**(2) 로드 호출부 (:272)** — 동일하게 4줄로 교체:

```csharp
LoadFixtureForSequence(loadFile, ESequence.Side1, "FIXTURE_SIDE_1");
LoadFixtureForSequence(loadFile, ESequence.Side2, "FIXTURE_SIDE_2");
LoadFixtureForSequence(loadFile, ESequence.Side3, "FIXTURE_SIDE_3");
LoadFixtureForSequence(loadFile, ESequence.Side4, "FIXTURE_SIDE_4");
```

**(3) [B6 — 이 phase 최대 위험] 구 `[FIXTURE_SIDE]` carry-over 보존. 반드시 구현한다.**

**소실 경로(실측):** `SaveFixtureForSequence`(`:85~99`)는 `ResolveFixtureSequence(seqId) == null`
일 때만 `PreserveFixtureFromExisting` 을 탄다. PC2 에서는 SIDE_1~4 시퀀스가 **존재**하므로
`saveFile[prefix]["DatumCount"] = seq.DatumConfigs.Count` (마이그레이션 전이라 **0**)를 쓴다.
`SavePhase6Format` 은 새 `IniFile` 에 **명시적으로 쓴 섹션만** 남기고
(`Sequence/SequenceHandler.cs:245 saveFile.Save(recipeFile)`), 구 `[FIXTURE_SIDE]`(DatumCount=4)는
아무도 쓰지 않으므로 **통째로 사라진다.** 이게 `3faa91b` 사고와 같은 경로다.

위험 구간은 이 Task 완료 시점부터 Task 4(교체) 승인 전까지 전체이며, 73-05(wave 3)/73-07 도
그 구간 안에 있다. 그 사이 사용자가 UI 에서 Recipe Save 를 한 번만 눌러도 **유일본이 소실된다.**

로드 4줄 **직후**에 감지 로그를 넣고:

```csharp
// 구 포맷(Phase 73 이전) 감지 — 마이그레이션 누락 시 SIDE Datum 이 통째로 안 보이는 것처럼 되므로 반드시 알린다.
bool bHasLegacySideFixture = loadFile.ContainsSection("FIXTURE_SIDE");
if (bHasLegacySideFixture) {
    Logging.PrintLog((int)ELogType.Error,
        "[RECIPE] 구 포맷 [FIXTURE_SIDE] 섹션이 남아 있다 — Phase 73 마이그레이션(scripts/migrate_phase73_recipe.py) 미적용. SIDE Datum 은 SIDE_1~4 로 로드되지 않으며, 저장 시 원본 섹션을 그대로 보존한다.");
}
```

**저장 쪽에 carry-over 를 추가한다.** `SaveFixtureForSequence` 4줄 **직후**에:

```csharp
// [B6] 마이그레이션 전 레시피 보호. 위 4줄은 SIDE_1~4 시퀀스가 메모리에 존재하므로
//  PreserveFixtureFromExisting 을 타지 않고 DatumCount=0 을 쓴다. 그 상태로 저장하면
//  구 [FIXTURE_SIDE] 의 Datum 4개(유일본)가 아무도 쓰지 않아 파일에서 사라진다(3faa91b 동일 경로).
//  마이그레이션이 끝나면 existingFile 에 이 섹션이 없으므로 이 블록은 자동으로 no-op 이 된다.
bool bHasLegacySideFixture = existingFile != null && existingFile.ContainsSection("FIXTURE_SIDE");
if (bHasLegacySideFixture) {
    PreserveFixtureFromExisting(saveFile, existingFile, "FIXTURE_SIDE");
    Logging.PrintLog((int)ELogType.Error,
        "[RECIPE] 구 [FIXTURE_SIDE] 섹션을 그대로 보존해 저장했다 — Phase 73 마이그레이션 미적용 상태. 마이그레이션 후에는 이 로그가 사라진다.");
}
```

`PreserveFixtureFromExisting` 은 섹션 prefix 단위로 `DatumCount` 와
`FIXTURE_SIDE_DATUM_0..N` 을 통째로 복사하므로 **본문 수정 없이 그대로 재사용**된다.

**(4) `MainView.xaml.cs:4149` (M9)** — **배열 원소만** 교체한다. 이 파일에 다른 변경은 금지:

```csharp
ESequence[] roles = new ESequence[] { ESequence.Top, ESequence.Side1, ESequence.Side2, ESequence.Side3, ESequence.Side4, ESequence.Bottom };
```

`GetInspectionSequenceForDatum` 은 `datum.OwnerName` 문자열로 `pSeq[...]` 를 조회하므로
마이그레이션 후 "SIDE_1" 로 자동 해결된다 — **무변경**.

**(5) 빌드 검증** — Debug|x64 / Release|x64. 산출물 잠김 시 스크래치 OutDir, 프로세스 종료 금지.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement/WPF_Example &amp;&amp; grep -c "FIXTURE_SIDE_" Custom/Sequence/Inspection/InspectionRecipeManager.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"FIXTURE_SIDE_1"' WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs` == 2 (Save 1 + Load 1), 2/3/4 각각도 == 2
    - `grep -c 'ESequence.Side,' WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs` == 0
    - `grep -c 'ContainsSection("FIXTURE_SIDE")' WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs` == 2 (로드 감지 1 + 저장 carry-over 1)
    - `grep -c 'PreserveFixtureFromExisting(saveFile, existingFile, "FIXTURE_SIDE")' WPF_Example/Custom/Sequence/Inspection/InspectionRecipeManager.cs` == 1
    - **[B6 실동작 검증 — 필수]** 마이그레이션 전 상태에서 앱 기동 → UI 에서 Recipe Save → 저장된 `main.ini` 에
      `[FIXTURE_SIDE]` 의 `DatumCount=4` 와 `[FIXTURE_SIDE_DATUM_0..3]` 4개 섹션이 **그대로 남아 있다**:
      `grep -A1 '^\[FIXTURE_SIDE\]$' D:/Data/Recipe/FAI_1/main.ini | grep -c 'DatumCount=4'` == 1
      `grep -c '^\[FIXTURE_SIDE_DATUM_[0-3]\]$' D:/Data/Recipe/FAI_1/main.ini` == 4
      (이 검증은 백업을 안전한 곳으로 옮긴 뒤에 수행한다 — Task 4 참고)
    - `PreserveFixtureFromExisting` / `SaveFixtureForSequence` / `LoadFixtureForSequence` **본문** 무변경 (`git diff` 에서 해당 메서드 내부 hunk 0)
    - `git diff --stat WPF_Example/UI/ContentItem/MainView.xaml.cs` 가 **1 file changed, 1 insertion, 1 deletion**
    - `grep -c "ESequence.Side1, ESequence.Side2, ESequence.Side3, ESequence.Side4" WPF_Example/UI/ContentItem/MainView.xaml.cs` == 1
    - SIMUL-ON 빌드 exit 0 / 에러 0 / 경고 18줄 · SIMUL-OFF 빌드 exit 0 / 에러 0 / 경고 16줄 (73-BUILD-VERIFY.md §2~4, 새 경고 코드 종류 0건)
  </acceptance_criteria>
  <done>레시피 저장/로드가 FIXTURE_SIDE_1~4 를 각각 SIDE_1~4 시퀀스에 매핑하고, 구 포맷 로드 시 Error 로그가 나온다.</done>
</task>

<task type="auto">
  <name>Task 3: main.ini 마이그레이션 스크립트 작성 + 무결성 카운트 검증 (M6)</name>
  <files>scripts/migrate_phase73_recipe.py</files>
  <read_first>
    - D:\Data\Recipe\FAI_1\main.ini 의 `[FIXTURE_SIDE]`(:184~) 및 `[FIXTURE_SIDE_DATUM_0]`(:188~) 전체 키 목록
    - D:\Data\Recipe\FAI_1\main.ini 의 `[SHOT_4]` / `[SHOT_4_CAM]` 전체 키 목록(Owner/ZIndex 위치 확인)
    - .planning/phases/73-side-4-jig-split/73-CONTEXT.md D-73-01(z 매핑표) / D-73-02(안전장치)
    - 이 PLAN 의 `<interfaces>` 매핑표(SHOT 8개 / DATUM 4개 / 오프셋)
  </read_first>
  <action>
`scripts/migrate_phase73_recipe.py` 를 신규 작성한다. **원본을 절대 덮어쓰지 않는다.**

**입출력**
- 입력: `D:\Data\Recipe\FAI_1\main.ini`
- 출력: `D:\Data\Recipe\FAI_1\main.phase73.ini` (새 파일)
- 실행: `python scripts/migrate_phase73_recipe.py --in <path> --out <path>`

**구현 방식:** INI 파서를 쓰지 말고 **라인 단위 편집**을 한다. `configparser` 는 키 순서·중복
섹션·주석·인코딩을 재작성하면서 조용히 정보를 잃는다. 현재 섹션 헤더 라인과 `key=value`
라인만 다루면 충분하다.

**[W13] 개행/인코딩 규격 (실측: CRLF + BOM 없음, `od -c` 확인).**
파이썬에서 반드시 `newline=''` 로 열고 쓴다:

```python
with io.open(src, "r", encoding="utf-8", newline="") as f:   # CRLF 를 그대로 읽는다
    text = f.read()
with io.open(dst, "w", encoding="utf-8", newline="") as f:   # 파이썬이 개행을 변환하지 못하게 한다
    f.write(out_text)
```

`newline=''` 를 빼면 전 라인이 LF 로 바뀌어 **diff 가 파일 전체**가 되고, 위험한 변경을
육안으로 골라낼 수 없게 된다(Task 4 체크포인트가 무력화된다). BOM 을 새로 붙이지 않는다.

**변환 규칙 (하드코딩 테이블 — 추론 금지)**

```python
SHOT_MAP = {   # 섹션 이름 → (신규 Owner, 신규 ZIndex)
    "SHOT_4":  ("SIDE_1", 2),
    "SHOT_6":  ("SIDE_2", 2),
    "SHOT_26": ("SIDE_2", 3),
    "SHOT_3":  ("SIDE_3", 2),
    "SHOT_23": ("SIDE_3", 3),
    "SHOT_24": ("SIDE_3", 4),
    "SHOT_5":  ("SIDE_4", 2),
    "SHOT_25": ("SIDE_4", 3),
}
DATUM_MAP = {  # 구 섹션 → (신규 섹션, 신규 ZIndexA, 신규 ZIndexB)
    "FIXTURE_SIDE_DATUM_0": ("FIXTURE_SIDE_1_DATUM_0", 0, 1),
    "FIXTURE_SIDE_DATUM_1": ("FIXTURE_SIDE_2_DATUM_0", 0, 1),
    "FIXTURE_SIDE_DATUM_2": ("FIXTURE_SIDE_3_DATUM_0", 0, 1),
    "FIXTURE_SIDE_DATUM_3": ("FIXTURE_SIDE_4_DATUM_0", 0, 1),
}
```

**[B4] `[Param0..7]` 섹션은 절대 건드리지 않는다.**

`grep -c "^OwnerSequenceName=SIDE$" main.ini` 는 **8이 아니라 16**이다(실측).
8건은 `[SHOT_3/4/5/6/23/24/25/26_CAM]`, 나머지 8건은 **레거시 `[Param0..7]`**
(라인 9162/9217/9272/9327/9382/9437/9492/9547)이다.

`[ParamN]` 은 `TryLoadNewFormat` 이 성공하면 아예 읽히지 않는 죽은 경로이고,
73-01 Task 1 이 동적 FAI 모드에서 Param 저장을 차단했으므로 **다음 저장 때 자연 소멸**한다.
스크립트가 이걸 편집하거나 삭제하려 들면 유일본 레시피를 건드리는 불필요한 위험만 생긴다.

따라서 **모든 카운트는 `[SHOT_*_CAM]` 섹션 스코프로 한정**한다.
"파일 전체 `OwnerSequenceName=SIDE` 가 0" 을 기대값으로 쓰지 않는다.

**적용 순서**

1. `[SHOT_n_CAM]` 섹션에서 `SHOT_n` 이 `SHOT_MAP` 에 있으면
   `OwnerSequenceName=` 값을 신규 Owner 로, `ZIndex=` 값을 신규 ZIndex 로 교체한다.
   같은 섹션에 `ZIndexA=` / `ZIndexB=` 가 **존재하고 값이 0 이상**이면 지그 오프셋
   (SIDE_1 −0 / SIDE_2 −3 / SIDE_3 −7 / SIDE_4 −12)을 빼서 교체한다. 값이 음수(-1 = 미설정)면 그대로 둔다.
2. `[FIXTURE_SIDE_DATUM_k]` 섹션 헤더를 `DATUM_MAP` 의 신규 이름으로 rename 하고,
   그 섹션 안의 `ZIndexA=` / `ZIndexB=` 를 0 / 1 로 교체한다. **다른 키는 한 글자도 건드리지 않는다**
   (RefMatch/PatternRoi/TeachingImagePath 등이 여기 들어 있다).
3. `[FIXTURE_SIDE]` 섹션을 삭제하고, 그 자리에 4개 섹션을 삽입한다.
   **[W11] `DisplayName` 에 구 값("SIDE")을 4번 복사하지 않는다** — 트리 노드 4개가 전부
   같은 이름이 되어 어느 지그인지 구분할 수 없다. 지그(Datum) 이름을 반영한다:
   ```
   [FIXTURE_SIDE_1]
   DisplayName=SIDE_1 (Datum 3-1)
   DatumCount=1

   [FIXTURE_SIDE_2]
   DisplayName=SIDE_2 (Datum 3-2)
   DatumCount=1

   [FIXTURE_SIDE_3]
   DisplayName=SIDE_3 (Datum 4-2)
   DatumCount=1

   [FIXTURE_SIDE_4]
   DisplayName=SIDE_4 (Datum 4-1)
   DatumCount=1
   ```
   지그↔Datum 대응은 이 PLAN `<interfaces>` 의 Datum 매핑표를 그대로 쓴다.

**무결성 검증 (스크립트가 스스로 수행하고, 하나라도 실패하면 출력 파일을 쓰지 않고 종료 코드 1)**

편집 전(입력)과 편집 후(출력)를 각각 파싱해 아래를 비교 출력한다:

| 항목 | 기대값 |
|---|---|
| 전체 섹션 수 | 출력 = 입력 + 3 (FIXTURE_SIDE 1개 삭제, FIXTURE_SIDE_1~4 4개 추가) |
| 전체 `SHOT_n` 섹션 수 | 27 → 27 (불변) |
| `[SHOT_*_CAM]` 스코프 OwnerSequenceName 총 개수 | 27 → 27 (불변) |
| **파일 전체** OwnerSequenceName 총 개수 | 35 → 35 (불변, `[ParamN]` 8건 포함) |
| `[SHOT_*_CAM]` 스코프에서 Owner==`SIDE` | 8 → 0 |
| `[ParamN]` 스코프에서 Owner==`SIDE` | 8 → **8 (무변경 — 손대지 않는다)** |
| `[SHOT_*_CAM]` 스코프 Owner==`SIDE_1`/`SIDE_2`/`SIDE_3`/`SIDE_4` | 0 → 1 / 2 / 3 / 2 |
| Owner 값이 `TOP` / `BOTTOM` 인 개수 | 불변 |
| SIDE Datum 섹션 수 | 4 → 4 |
| 각 Datum 섹션의 키 개수 | 섹션별로 입력과 동일 |
| `[ParamN]` 섹션 개수와 각 섹션 키 개수 | 입력과 **완전 동일**(스크립트 미접촉 증명) |
| 개행 코드 | 입력·출력 모두 CRLF, LF-only 라인 0건 |
| SIDE Shot 소속 FAI 섹션(`SHOT_n_FAI_f`) 총 개수 | 불변 |
| SIDE Shot 소속 측정 섹션(`SHOT_n_FAI_f_MEAS_m`) 총 개수 | **25 (baseline)** |
| 신규 z 커버리지 | SIDE_1 {0,1,2} / SIDE_2 {0,1,2,3} / SIDE_3 {0,1,2,3,4} / SIDE_4 {0,1,2,3} 이 빈칸 없이 연속 |

검증 결과를 사람이 읽을 수 있는 표로 stdout 에 출력한다.

**교체 절차 (사람 확인 후)**
```
diff <원본> <출력>            # 변경 라인이 위 규칙 밖에 없는지 눈으로 확인
copy main.ini main.ini.bak_260826_phase73
copy main.phase73.ini main.ini
```
백업 `D:\Backup\FAI_1_backup_before_phase73_260826\` 존재를 착수 전에 먼저 확인한다.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement &amp;&amp; python scripts/migrate_phase73_recipe.py --in "D:/Data/Recipe/FAI_1/main.ini" --out "D:/Data/Recipe/FAI_1/main.phase73.ini" &amp;&amp; awk '/^\[/{sec=$0} /^OwnerSequenceName=/{if(sec ~ /^\[SHOT_[0-9]+_CAM\]$/) print $0}' "D:/Data/Recipe/FAI_1/main.phase73.ini" | sort | uniq -c</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/migrate_phase73_recipe.py` 파일 존재
    - 스크립트 종료 코드 0, stdout 에 무결성 비교 표 출력
    - `D:/Data/Recipe/FAI_1/main.ini` 의 mtime 이 스크립트 실행 전후 동일(원본 무변경)
    - **[W6] `grep -c` 가 0 을 반환하면 exit 1 이라 verify 가 실패로 기록된다.**
      0 을 기대하는 검사는 반드시 `|| true` 를 붙이거나 `! grep -q` 형태로 쓴다. 아래 명령은 그 형식이다.
    - **[B4] Owner 카운트는 `[SHOT_*_CAM]` 스코프로만 센다** (파일 전체로 세면 `[ParamN]` 8건 때문에 절대 0 이 안 된다):
      ```bash
      awk '/^\[/{sec=$0} /^OwnerSequenceName=/{if(sec ~ /^\[SHOT_[0-9]+_CAM\]$/) print $0}' main.phase73.ini | sort | uniq -c
      ```
      기대: `SIDE_1` 1 / `SIDE_2` 2 / `SIDE_3` 3 / `SIDE_4` 2 / `SIDE` **0건(줄 자체가 없음)** / TOP·BOTTOM 은 입력과 동일
    - **[B4] `[ParamN]` 무접촉 증명:** `diff <(sed -n '/^\[Param0\]/,$p' main.ini) <(sed -n '/^\[Param0\]/,$p' main.phase73.ini)` 출력 0줄
    - `! grep -q "^\[FIXTURE_SIDE\]$" main.phase73.ini`  (구 섹션 제거됨)
    - `grep -c "^\[FIXTURE_SIDE_1\]$" main.phase73.ini` == 1 (2/3/4 각 1건)
    - `grep -c "^\[FIXTURE_SIDE_1_DATUM_0\]$" main.phase73.ini` == 1 (2/3/4 각 1건)
    - `! grep -q "^\[FIXTURE_SIDE_DATUM_" main.phase73.ini`
    - `grep -c "^\[SHOT_" main.phase73.ini` == `grep -c "^\[SHOT_" main.ini` (섹션 총수 불변)
    - 출력 파일의 `_MEAS_` 섹션 총 개수가 입력과 동일 (기대 25)
    - 각 신규 FIXTURE_SIDE_n 섹션에 `DatumCount=1` 존재
    - **[W11]** 4개 `DisplayName` 이 서로 다르다: `grep -A1 "^\[FIXTURE_SIDE_[1-4]\]$" main.phase73.ini | grep -c "^DisplayName=" ` == 4 이고 값 4개가 유일
    - **[W13]** CRLF 유지: `file main.phase73.ini` 출력에 `CRLF` 포함, 그리고
      `! grep -qP '[^
]
' main.phase73.ini` (LF-only 라인 0건)
  </acceptance_criteria>
  <done>새 파일 `main.phase73.ini` 가 생성되고 무결성 표의 모든 항목이 기대값과 일치한다. 원본은 무변경.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 마이그레이션 결과 육안 확인 후 교체 승인</name>
  <files>D:/Data/Recipe/FAI_1/main.ini (교체 대상), D:/Data/Recipe/FAI_1/main.phase73.ini</files>
  <read_first>
    - Task 3 이 stdout 에 출력한 무결성 비교 표
    - `diff D:/Data/Recipe/FAI_1/main.ini D:/Data/Recipe/FAI_1/main.phase73.ini` 출력
  </read_first>
  <action>
이 task 는 사람 확인 체크포인트다. 실행자는 아래 <how-to-verify> 절차를 사용자에게 그대로
제시하고, 승인("승인" 입력)을 받기 전까지 `main.ini` 를 교체하지 않는다.
승인 후에만 `copy main.ini main.ini.bak_260826_phase73` → `copy main.phase73.ini main.ini` 를 수행한다.
승인 없이 자체 판단으로 교체하거나 통과 처리하지 않는다.
  </action>
  <what-built>
    `main.phase73.ini` 를 생성했다(원본 무변경). SIDE Shot 8개의 소속/ZIndex 와
    SIDE Datum 4개의 섹션명/ZIndexA·B 만 바뀌었고, 측정 25개·FAI·패턴 키는 전부 보존됐다.
  </what-built>
  <how-to-verify>
    0. **[B7 — 먼저 이것부터] 유일 백업을 `RecipeSavePath` 밖으로 옮긴다.**
       `D:\Data\Setting.ini` 의 `RecipeSavePath=D:\Data\Recipe` 이므로
       `D:\Data\Recipe\FAI_1_backup_before_phase73_260826\` 는 **앱의 레시피 목록에 그대로 노출된다.**
       실수로 그 레시피를 선택한 뒤 저장하면 B6 과 같은 경로로 **백업까지 파괴된다**
       (백업 안에도 `FIXTURE_SIDE DatumCount=4` 가 실재한다 = 유일본이 둘 다 사라진다).
       - 확인: 앱 레시피 선택 목록에 `FAI_1_backup_before_phase73_260826` 이 보이는가?
       - 승인 시 실행자가 이동한다:
         `move "D:\Data\Recipe\FAI_1_backup_before_phase73_260826" "D:\Backup\FAI_1_backup_before_phase73_260826"`
       - 이동 후 `D:\Backup\...\main.ini` 에 `DatumCount=4` 가 있는지 확인하고,
         앱 레시피 목록에서 사라졌는지 확인한다.
       **이 항목이 통과하기 전에는 아래 어떤 단계도 진행하지 않는다.**
    1. Task 3 의 무결성 표에서 "측정 섹션 25개", "Owner SIDE_1/2/3/4 = 1/2/3/2", "SIDE Datum 4개" 를 확인한다
    2. `diff` 출력에 `OwnerSequenceName=` / `ZIndex=` / `ZIndexA=` / `ZIndexB=` / `[FIXTURE_SIDE*]` 헤더
       **이외의** 변경 라인이 있는지 본다 — 있으면 승인하지 말 것
    3. `D:\Backup\FAI_1_backup_before_phase73_260826\` 백업 폴더가 실제로 존재하고 .shm 31개가 있는지 확인한다
    4. 승인하면 실행자가 다음을 수행한다:
       `copy main.ini main.ini.bak_260826_phase73` → `copy main.phase73.ini main.ini`
    5. 앱을 SIMUL 로 기동해 트리에 SIDE_1~4 노드가 뜨고 각 노드 아래 Shot/Datum 이 보이는지 확인한다
       (SIDE_1: Shot 1 + Datum 1 / SIDE_2: Shot 2 + Datum 1 / SIDE_3: Shot 3 + Datum 1 / SIDE_4: Shot 2 + Datum 1)
    6. 로그에서 `[DatumModelPath]` 줄을 찾아 `path` 가 `...\FAI_1\SIDE\Datum...shm` 인지 확인한다
       (`SIDE_1\` 이면 Task 1 정규화가 안 걸린 것 — 즉시 중단)
  </how-to-verify>
  <acceptance_criteria>
    - **[B7]** 백업이 `D:\Data\Recipe\` 밖으로 이동됨 — `test ! -d "D:/Data/Recipe/FAI_1_backup_before_phase73_260826"` 이고 `test -f "D:/Backup/FAI_1_backup_before_phase73_260826/main.ini"`
    - **[B7]** 이동된 백업의 `main.ini` 에 `DatumCount=4` 존재, .shm 31개 존재
    - diff 변경 라인이 Owner/ZIndex/ZIndexA/ZIndexB/FIXTURE 섹션 헤더로만 구성됨
    - **[B4]** diff 에 `[Param0]` 이후 구간의 변경 라인 0건
    - 교체 후 SIMUL 기동 시 트리에 SIDE_1~4 노드 4개 표시
    - 로그 `[DatumModelPath]` 의 folder 값이 `SIDE`
    - `[RECIPE] 구 포맷 [FIXTURE_SIDE] 섹션이 남아 있다` Error 로그가 **나오지 않음**
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement &amp;&amp; diff "D:/Data/Recipe/FAI_1/main.ini" "D:/Data/Recipe/FAI_1/main.phase73.ini" | head -60</automated>
  </verify>
  <done>사용자 승인 후 main.ini 가 교체되고, SIMUL 트리에 SIDE_1~4 노드가 표시되며 [DatumModelPath] folder 가 SIDE 다.</done>
  <resume-signal>"승인" 을 입력하거나 문제점을 기술한다</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 스크립트 → 유일본 레시피 파일 | 잘못된 편집이 SIDE Datum 4개(유일본)를 영구 파괴할 수 있다 |
| 메모리 시퀀스 → 디스크 레시피 저장 | 시퀀스 미등록 시 DatumCount=0 덮어쓰기로 소실(3faa91b 재현) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-07 | Tampering | `migrate_phase73_recipe.py` | mitigate | 원본 무변경 + 새 파일 출력 + 12항목 무결성 카운트 + 사람 승인 체크포인트 |
| T-73-08 | Denial of Service | `SaveFixtureForSequence` 4분할 | mitigate | `PreserveFixtureFromExisting` 본문 무변경 유지(비활성 시퀀스 Datum 보존 경로 보존) |
| T-73-09 | Information Disclosure | 없음 | accept | 레시피는 로컬 디스크 전용, 외부 노출 경로 없음 |
| T-73-10 | Tampering | .shm 경로 이동으로 모델 유실 | mitigate | `NormalizeModelFolderName` + `[DatumModelPath]` 진단 로그로 실경로 확인 |
</threat_model>

<verification>
1. 마이그레이션 후 SIDE 측정 항목 총 25개가 그대로 존재한다
2. SIDE_1 z={0,1,2} / SIDE_2 z={0,1,2,3} / SIDE_3 z={0,1,2,3,4} / SIDE_4 z={0,1,2,3} 이 빈칸 없이 연속이다
3. `[DatumModelPath]` 로그의 folder 가 `SIDE` 다
4. SIMUL-ON / SIMUL-OFF 두 빌드 에러 0
</verification>

<success_criteria>
- SIDE Datum 4개 / Shot 8개 / 측정 25개가 마이그레이션 전후 동일 개수
- FIXTURE_SIDE_1~4 저장/로드 동작
- 재티칭 0 (.shm 경로 유지)
</success_criteria>

<output>
완료 후 `.planning/phases/73-side-4-jig-split/73-03-SUMMARY.md` 를 작성한다.
</output>
