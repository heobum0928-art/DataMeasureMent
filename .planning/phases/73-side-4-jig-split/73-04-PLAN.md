---
phase: 73-side-4-jig-split
plan: 04
type: execute
wave: 2
depends_on: ["73-01", "73-02"]
files_modified:
  - WPF_Example/Custom/TcpServer/ResourceMap.cs
  - WPF_Example/Custom/SystemHandler.cs
autonomous: true
requirements: [PROTO-73-TYPE-ROUTING]
must_haves:
  truths:
    - "`$TEST` 의 Type 2/3/4/5 가 PC2 에서 SIDE_1/SIDE_2/SIDE_3/SIDE_4 시퀀스로 라우팅된다"
    - "`$PREP` 의 z_index 가 대상 시퀀스별로 따로 기억되어 지그 간 섞이지 않는다"
    - "규격 위반 `$PREP` 은 조명을 건드리지 않고 FAIL ACK 로 회신된다"
    - "`$PREP_ACK` 의 FAIL 은 조명 세팅 실패(또는 요청 규격 위반) 전용이며 Shot 유무와 무관하다"
  artifacts:
    - path: "WPF_Example/Custom/TcpServer/ResourceMap.cs"
      provides: "Type→시퀀스 이름 해석 + Prep 패킷 Identifier 설정"
      contains: "TryResolveSequenceNameByType"
    - path: "WPF_Example/Custom/SystemHandler.cs"
      provides: "시퀀스별 _lastPrepZIndex 사전 + 단일 시퀀스 조명 적용 + $RESET 전체 초기화"
      contains: "_lastPrepZIndexBySeq"
  key_links:
    - from: "ProcessPrep"
      to: "ApplyPrepToSequence(szSeqName, nZIndex)"
      via: "Type 으로 해석한 대상 시퀀스 1개에만 조명 적용"
      pattern: "ApplyPrepToSequence\\("
    - from: "ProcessTest"
      to: "_lastPrepZIndexBySeq[packet.Identifier]"
      via: "시퀀스별 z_index 조회"
      pattern: "_lastPrepZIndexBySeq"
---

<objective>
`$PREP`/`$TEST` 의 Type 을 실제 라우팅에 연결한다(M3/M10 후반).
동시에 전역 단일 변수였던 `_lastPrepZIndex` 를 시퀀스별 사전으로 승격해(R2/R3),
z 가 지그마다 0 부터 시작해 값이 겹쳐도 오염되지 않게 한다.

`$PREP_ACK` 의 FAIL 의미를 **조명 세팅 실패 전용**으로 재정의한다(D-73-08).
검사 항목 유무는 응답에 반영하지 않으므로, `ApplyPrepToSequences` 의 예외 분기
(`z==0 무조건 OK`, `크로스-Z Datum 전용 tick OK`)가 전부 불필요해진다(M1 소멸).

Purpose: Type 이 실제로 대상을 정하게 만든다 — 이 phase 의 핵심.
Output: PC2 에서 Type 2~5 가 SIDE_1~4 로 라우팅되는 빌드.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-side-4-jig-split/73-CONTEXT.md
@.planning/phases/73-side-4-jig-split/73-01-SUMMARY.md
@.planning/phases/73-side-4-jig-split/73-02-SUMMARY.md
@CLAUDE.md

**코딩 규칙:** 삼항 `?:` / `??` / `?.` 금지. 분기는 if/else 또는 **전통 switch**만
(Type→시퀀스 매핑은 switch 권장). C# 8.0 switch expression 금지. C# 7.2. 헝가리언(b/n/sz).
두 파일 모두 Allman 우세 — 주변 스타일 유지. 날짜 주석 새로 달지 않는다.
빌드 경고 baseline 12줄. csproj 커밋 금지. 실행 중 프로세스 종료 금지.
</context>

<interfaces>
```csharp
// Custom/TcpServer/ResourceMap.cs
public enum ESite : int { Top = 1, Side = 2, Bottom = 3, }          // 3슬롯 한계 — 확장하지 않는다
public enum EPcRole : int { PC1_TopBottom = 1, PC2_Side = 2, }
private const int TYPE_CODE_TOP = 0;
private const int TYPE_CODE_BOTTOM = 1;
private const int TYPE_CODE_TOP_SIDE1 = 2;
private const int TYPE_CODE_BOTTOM_SIDE2 = 3;
private const int TYPE_CODE_SIDE3 = 4;
private const int TYPE_CODE_SIDE4 = 5;
private void MapPc2Resources()                                       // :106 — Sequence 슬롯 2개 모두 SEQ_SIDE
private ESite ResolveSiteSlot(int nSite)                             // :120
private bool TryResolveSlotByType(string szType, out ESite eSlot)    // :148
public bool SetIdentifier(ref VisionRequestPacket packet)            // :187 — switch 에 Prep case 가 **없다**(M10)
//   case VisionRequestType.Test: ... testPacket.Identifier = Find(EResource.Sequence, eSlot);

// Custom/SystemHandler.cs
private volatile int _lastPrepZIndex = 0;                            // :20   (R2/R3 원인)
private bool ProcessTest(TestPacket packet)                          // :226  — packet.TestID = _lastPrepZIndex.ToString();
private bool StartV1Scoped(SequenceBase seq, TestPacket packet)      // :250  — _lastPrepZIndex 를 직접 읽는다
private PrepAckPacket ProcessPrep(PrepPacket packet)                 // :884
private ResetAckPacket ProcessReset(ResetPacket packet)              // :913  — _lastPrepZIndex = 0
internal bool TriggerInspectionCycleManually(string seqName, int zIndex)  // :945 (M2 — PrepPacket 에 Type 미설정)
private bool ApplyPrepToSequences(int nZIndex)                       // :977  — 전 시퀀스 순회, 마지막이 이김

// Custom/Sequence/Inspection/InspectionSequence.cs (이 plan 에서 수정 금지 — 73-05 소관)
public bool ApplyShotLights(int nZIndex)
public bool IsDatumOnlyExecutionIndex(int nZIndex)

// TcpServer/VisionRequestPacket.cs (73-02 결과)
public class PrepPacket { public string Type; public bool IsRequestValid; public int ZIndex; public int Site; }
```

**Type ↔ 시퀀스 매핑 (D-73-05 확정):** 0=TOP, 1=BOTTOM, 2=SIDE_1, 3=SIDE_2, 4=SIDE_3, 5=SIDE_4
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ResourceMap 에 Type→시퀀스 이름 해석 + Prep case 신설 (M3 / M10 후반)</name>
  <files>WPF_Example/Custom/TcpServer/ResourceMap.cs</files>
  <read_first>
    - WPF_Example/Custom/TcpServer/ResourceMap.cs 전체(:1~240)
    - WPF_Example/Custom/Sequence/SequenceHandler.cs (SEQ_SIDE_1~4 상수 — 73-01 산출물)
    - WPF_Example/TcpServer/VisionRequestPacket.cs (PrepPacket.Type / IsRequestValid — 73-02 산출물)
    - .planning/phases/73-side-4-jig-split/73-CONTEXT.md D-73-07 의 M3/M10
  </read_first>
  <action>
`ESite` 는 3슬롯이라 SIDE_1~4 를 담을 수 없다. **`ESite` 를 확장하지 말고**,
Type 에서 **시퀀스 이름 문자열을 직접** 해석하는 경로를 신설한다(카메라/조명 슬롯 매핑은 무변경).

**(1) 시퀀스 이름 해석기 추가** (`TryResolveSlotByType` 바로 아래):

```csharp
// Type(검사 대상 코드) → 시퀀스 이름 직접 해석. ESite 는 3슬롯이라 SIDE_1~4 를 담을 수 없어
// 시퀀스 라우팅만 이 경로로 분리한다(카메라/조명 슬롯 매핑은 기존 ESite 경로 그대로).
// PcRole 별 의미:
//   PC2(Side)  : 0/1 은 이 PC 대상이 아니므로 실패 반환 / 2~5 = SIDE_1~4
//   PC1(TopBottom): Phase 73 이전 라우팅을 그대로 보존한다(회귀 0). 0,2,4,5 → TOP / 1,3 → BOTTOM.
//     ⚠ 제어 스펙상 2~5 는 SIDE 이므로 PC1 이 이 값을 받을 일은 없다. 값이 실제로 오면
//     기존 동작(폴백)을 유지하되 로그로 남긴다 — 조용한 라우팅 변경이 더 위험하다.
private bool TryResolveSequenceNameByType(string szType, out string szSeqName)
{
    szSeqName = null;
    bool bHasType = !string.IsNullOrEmpty(szType);
    if (!bHasType)
    {
        return false;
    }
    int nCode = 0;
    bool bIsNumeric = Int32.TryParse(szType, out nCode);
    if (!bIsNumeric)
    {
        return false;
    }
    int nPcRole = (int)SystemSetting.Handle.PcRole;
    bool bIsPc2Side = nPcRole == (int)EPcRole.PC2_Side;
    if (!bIsPc2Side)
    {
        return false;   // PC1 은 기존 ESite 슬롯 경로(TryResolveSlotByType)를 그대로 쓴다
    }
    switch (nCode)
    {
        case TYPE_CODE_TOP_SIDE1:    szSeqName = SequenceHandler.SEQ_SIDE_1; return true;
        case TYPE_CODE_BOTTOM_SIDE2: szSeqName = SequenceHandler.SEQ_SIDE_2; return true;
        case TYPE_CODE_SIDE3:        szSeqName = SequenceHandler.SEQ_SIDE_3; return true;
        case TYPE_CODE_SIDE4:        szSeqName = SequenceHandler.SEQ_SIDE_4; return true;
        default:
            Logging.PrintLog((int)ELogType.Error,
                "[ROUTE] PC2(Side)에서 지원하지 않는 Type={0} 수신 — 시퀀스 해석 실패", nCode);
            return false;
    }
}
```

`TYPE_CODE_*` 상수 주석도 이번에 손대는 자리이므로 갱신한다:
`TYPE_CODE_TOP_SIDE1 = 2 // SIDE_1 지그` / `TYPE_CODE_BOTTOM_SIDE2 = 3 // SIDE_2 지그` /
`TYPE_CODE_SIDE3 = 4 // SIDE_3 지그` / `TYPE_CODE_SIDE4 = 5 // SIDE_4 지그`.
상수 **이름과 값은 바꾸지 않는다**(다른 참조 회귀 방지).

**(2) `SetIdentifier` 의 `case VisionRequestType.Test`** — v1 분기에서 Type 우선 해석을 앞에 둔다.
기존 `TryResolveSlotByType` + `ResolveSiteSlot` 폴백은 **그대로 남긴다**:

```csharp
if (bUseV1)
{
    string szSeqByType;
    bool bSeqResolved = TryResolveSequenceNameByType(testPacket.Type, out szSeqByType);
    ESite eSlot;
    bool bTypeResolved = TryResolveSlotByType(testPacket.Type, out eSlot);
    if (!bTypeResolved)
    {
        eSlot = ResolveSiteSlot(testPacket.Site);
    }
    if (bSeqResolved)
    {
        testPacket.Identifier = szSeqByType;          // PC2: Type 이 시퀀스를 직접 정한다
    }
    else
    {
        testPacket.Identifier = Find(EResource.Sequence, eSlot);
    }
    testPacket.Identifier2 = Find(EResource.Action, eSlot, ETestType.Inspection);
}
```

**(3) `SetIdentifier` 에 `case VisionRequestType.Prep` 신설** (M10 — 현재 아예 없다):

```csharp
case VisionRequestType.Prep:
    PrepPacket prepPacket = packet.AsPrep();
    string szPrepSeqName;
    bool bPrepSeqResolved = TryResolveSequenceNameByType(prepPacket.Type, out szPrepSeqName);
    if (bPrepSeqResolved)
    {
        prepPacket.Identifier = szPrepSeqName;
    }
    else
    {
        prepPacket.Identifier = null;   // ProcessPrep 이 FAIL ACK 로 회신한다
    }
    break;
```

`SetIdentifier` 를 호출하는 지점이 Prep 패킷에도 도달하는지 확인하고, 아니면 호출 경로를 추가한다
(`Custom/SystemHandler.cs` 의 수신 디스패치에서 `Server.GetPacket`/`ResourceMap.SetIdentifier` 순서 확인).

**(4) `MapPc2Resources()`** — Sequence 슬롯 2개가 존재하지 않게 된 `SEQ_SIDE` 를 가리키고 있다.
Type 해석 실패 시 폴백으로 쓰이므로 `SEQ_SIDE_1` 로 바꾼다:

```csharp
Add(EResource.Sequence, ESite.Top,  SequenceHandler.SEQ_SIDE_1);
Add(EResource.Sequence, ESite.Side, SequenceHandler.SEQ_SIDE_1);
```
`InitializeV26()`(:68)의 `Add(EResource.Sequence, ESite.Side, SequenceHandler.SEQ_SIDE);` 도
같은 이유로 `SEQ_SIDE_1` 로 바꾼다(v2.6 경로도 존재하지 않는 시퀀스를 가리키면 안 된다).
Camera/Light 매핑 줄은 **무변경**.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement/WPF_Example &amp;&amp; grep -c "TryResolveSequenceNameByType" Custom/TcpServer/ResourceMap.cs &amp;&amp; grep -c "VisionRequestType.Prep" Custom/TcpServer/ResourceMap.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TryResolveSequenceNameByType" WPF_Example/Custom/TcpServer/ResourceMap.cs` == 3 (정의 1 + Test 1 + Prep 1)
    - `grep -c "case VisionRequestType.Prep:" WPF_Example/Custom/TcpServer/ResourceMap.cs` == 1
    - `grep -c "SEQ_SIDE_1" WPF_Example/Custom/TcpServer/ResourceMap.cs` == 4 (switch 1 + MapPc2 2 + V26 1)
    - `grep -c "SequenceHandler.SEQ_SIDE;" WPF_Example/Custom/TcpServer/ResourceMap.cs` == 0
    - `TryResolveSlotByType` 본문 무변경(`git diff` 에서 해당 메서드 내부 hunk 0)
    - `TryResolveSequenceNameByType` 의 분기가 전통 switch(`case X: ... return true;`)이며 switch expression(`=>`) 0건
    - 신규 `?:` / `??` / `?.` 0건
  </acceptance_criteria>
  <done>PC2 에서 Type 2~5 가 SEQ_SIDE_1~4 문자열로 해석되고, `$PREP` 패킷도 Identifier 를 갖는다.</done>
</task>

<task type="auto">
  <name>Task 2: 시퀀스별 _lastPrepZIndex 사전 (R2/R3) + 단일 시퀀스 조명 적용 (M1 소멸) + $RESET (M11)</name>
  <files>WPF_Example/Custom/SystemHandler.cs</files>
  <read_first>
    - WPF_Example/Custom/SystemHandler.cs :18~25 (`_lastPrepZIndex` 선언)
    - WPF_Example/Custom/SystemHandler.cs :210~300 (ProcessTest / StartV1Scoped — `_lastPrepZIndex` 사용 전량)
    - WPF_Example/Custom/SystemHandler.cs :880~935 (ProcessPrep / ProcessReset)
    - WPF_Example/Custom/SystemHandler.cs :975~1025 (ApplyPrepToSequences 전문 — 삭제할 예외 분기 확인)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs `ApplyShotLights(int)` 시그니처(:751)
    - .planning/phases/73-side-4-jig-split/73-CONTEXT.md D-73-08 의 "FAIL 정의 변경의 파급" 단락
  </read_first>
  <action>
**(1) 필드 교체** (:20)

```csharp
// $PREP 이 저장한 z_index. 시퀀스마다 z 가 0 부터 독립 시작하므로 값이 겹친다 —
// 전역 단일 변수로 두면 대상이 섞일 때 조용히 오염된다(R2/R3). 시퀀스 이름별로 분리 보관한다.
// MainRun(폴링 스레드)과 시퀀스 스레드가 함께 접근하므로 모든 읽기/쓰기를 _prepZIndexLock 으로 감싼다.
private readonly Dictionary<string, int> _lastPrepZIndexBySeq = new Dictionary<string, int>();
private readonly object _prepZIndexLock = new object();
```

접근 헬퍼 2개를 같은 자리에 추가한다:

```csharp
private void StorePrepZIndex(string szSeqName, int nZIndex)
{
    bool bHasName = !string.IsNullOrEmpty(szSeqName);
    if (!bHasName)
    {
        return;
    }
    lock (_prepZIndexLock)
    {
        _lastPrepZIndexBySeq[szSeqName] = nZIndex;
    }
}

// 해당 시퀀스의 마지막 $PREP z_index. $PREP 없이 $TEST 가 오면 0(=Datum 인덱스) 보수적 폴백.
private int GetPrepZIndex(string szSeqName)
{
    bool bHasName = !string.IsNullOrEmpty(szSeqName);
    if (!bHasName)
    {
        return 0;
    }
    lock (_prepZIndexLock)
    {
        int nStored = 0;
        bool bFound = _lastPrepZIndexBySeq.TryGetValue(szSeqName, out nStored);
        if (bFound)
        {
            return nStored;
        }
    }
    Logging.PrintLog((int)ELogType.Error,
        "[PREP] 시퀀스 '{0}' 에 대한 $PREP 기록이 없다 — z_index=0 으로 폴백(선행 $PREP 누락 의심)", szSeqName);
    return 0;
}
```

**(2) `ProcessTest`** — `packet.TestID = _lastPrepZIndex.ToString();` 을 교체한다:

```csharp
string szTargetSeqName = packet.Identifier;
int nPrepZIndex = GetPrepZIndex(szTargetSeqName);
packet.TestID = nPrepZIndex.ToString();
```

**(3) `StartV1Scoped`** — 이 메서드가 `_lastPrepZIndex` 를 직접 읽는 지점이 여럿이다.
시그니처를 `private bool StartV1Scoped(SequenceBase seq, TestPacket packet, int nPrepZIndex)` 로
바꾸고, 본문의 `_lastPrepZIndex` 를 전부 지역 파라미터 `nPrepZIndex` 로 치환한다.
`ProcessTest` 의 호출부도 `StartV1Scoped(seq, packet, nPrepZIndex)` 로 바꾼다.
**그 외 로직(Datum 대표트리거/StartSubset/StartEmptyScope)은 한 줄도 바꾸지 않는다.**

**(4) `ApplyPrepToSequences(int)` → `ApplyPrepToSequence(string, int)` 로 교체**

D-73-08 로 FAIL 정의가 "조명 세팅 실패 전용"이 되었으므로, 전 시퀀스 순회와 예외 분기
(`bIsDatumOnlyZero` / `bIsDatumOnlyForSomeSeq` / `bTreatAsDatumOnly`)를 **전부 삭제**한다.
기존 메서드 전체를 아래로 교체한다:

```csharp
// $PREP 대상 시퀀스 1개에만 조명을 적용한다.
//  이전 구현은 전 시퀀스를 순회하며 마지막이 이기는 구조라, z 값이 대상을 암시한다는 전제 위에
//  서 있었다 — Phase 73 이 그 전제를 깬다(지그마다 z 가 0 부터 시작).
//  반환값 의미(D-73-08): true = 조명 세팅 성공 / false = 조명 세팅 실패.
//  ⚠ "이 z 에 Shot 이 있는가"는 더 이상 판정하지 않는다. Shot 이 없는 z(기준점 전용 z, 아직 항목을
//  안 넣은 빈 z)는 조명을 건드릴 대상이 없을 뿐이며 정상이므로 true 다.
private bool ApplyPrepToSequence(string szSeqName, int nZIndex)
{
    SequenceBase seqBase = Sequences[szSeqName];
    InspectionSequence inspSeq = seqBase as InspectionSequence;
    bool bIsInsp = inspSeq != null;
    if (!bIsInsp)
    {
        Logging.PrintLog((int)ELogType.Error,
            "[PREP] 시퀀스 '{0}' 를 찾을 수 없다(이 PC 에 미등록) — 조명 세팅 실패", szSeqName);
        return false;
    }
    return inspSeq.ApplyShotLights(nZIndex);
}
```

⚠ `ApplyShotLights(int)` 가 "이 z 에 Shot 이 없음"을 false 로 돌려주는지 확인할 것.
그렇다면 **73-05 Task 2 가 그 반환 의미를 "조명 세팅 성공 여부"로 바꾼다** —
이 plan 은 호출부만 정리하고, 반환 의미 변경은 73-05 소관이다. 두 plan 이 합쳐진 뒤
`$PREP_ACK` 이 Shot 없는 z 에서 FAIL 로 나가지 않는지 73-07 이 검증한다.

**(5) `ProcessPrep` 재작성**

```csharp
private PrepAckPacket ProcessPrep(PrepPacket packet)
{
    PrepAckPacket ackPacket = new PrepAckPacket();
    bool bHasPacket = packet != null;
    if (!bHasPacket)
    {
        return null;
    }
    ackPacket.Target = packet.Sender;
    ackPacket.Site = packet.Site;
    ackPacket.Type = packet.Type;
    ackPacket.ZIndex = packet.ZIndex;
    ackPacket.IsOk = false;   // 기본 FAIL — 아래 전 단계를 통과해야만 OK 로 승격

    // 규격 위반 요청($PREP:site,Type,z_index@ 3필드 아님) — 조명을 건드리지 않고 FAIL 회신.
    // 무응답은 절대 금지(PLC ACK 무한 대기 = 라인 정지).
    bool bIsRequestValid = packet.IsRequestValid;
    if (!bIsRequestValid)
    {
        Logging.PrintLog((int)ELogType.Error, "[PREP] 규격 위반 요청 — FAIL ACK 회신, 조명 무변경");
        return ackPacket;
    }

    string szSeqName = packet.Identifier;
    bool bHasSeqName = !string.IsNullOrEmpty(szSeqName);
    if (!bHasSeqName)
    {
        Logging.PrintLog((int)ELogType.Error,
            "[PREP] Type='{0}' 로 대상 시퀀스를 해석하지 못함 — FAIL ACK 회신", packet.Type);
        return ackPacket;
    }

    StorePrepZIndex(szSeqName, packet.ZIndex);
    bool bApplied = ApplyPrepToSequence(szSeqName, packet.ZIndex);
    ackPacket.IsOk = bApplied;

    Logging.PrintLog((int)ELogType.Trace,
        "[PREP] site={0} Type={1} seq={2} z={3} 조명세팅={4}",
        packet.Site, packet.Type, szSeqName, packet.ZIndex, bApplied);
    return ackPacket;
}
```

**(6) `ProcessReset` (M11)** — `_lastPrepZIndex = 0;` 을 교체한다:

```csharp
lock (_prepZIndexLock)
{
    _lastPrepZIndexBySeq.Clear();   // 전 시퀀스의 z_index 기억을 지운다 = 다음 $TEST 는 z=0(Datum) 보수적 초기값
}
```
로그 문구의 `_lastPrepZIndex=0` 도 `_lastPrepZIndexBySeq=clear` 로 갱신한다.

**(7) `TriggerInspectionCycleManually` (M2)** — 수동 트리거가 `PrepPacket.Type` 을 안 채워
기본값(TOP)으로 라우팅되는 결함. `prepPacket.ZIndex = zIndex;` 다음 줄에 추가한다:

```csharp
// 수동 트리거는 TCP 를 거치지 않아 ResourceMap.SetIdentifier 가 돌지 않는다 —
// Identifier/IsRequestValid 를 직접 채워야 ProcessPrep 이 올바른 시퀀스에 조명을 건다.
prepPacket.Identifier = seqName;
prepPacket.Type = ResolveTypeCodeBySequenceName(seqName);
prepPacket.IsRequestValid = true;
```

그리고 같은 파일에 역매핑 헬퍼를 추가한다(전통 switch, 삼항 금지):

```csharp
// 수동 트리거 전용 — 시퀀스 이름에서 프로토콜 Type 코드 문자열을 되돌린다(ACK echo 용).
private static string ResolveTypeCodeBySequenceName(string szSeqName)
{
    switch (szSeqName)
    {
        case SequenceHandler.SEQ_TOP:    return "0";
        case SequenceHandler.SEQ_BOTTOM: return "1";
        case SequenceHandler.SEQ_SIDE_1: return "2";
        case SequenceHandler.SEQ_SIDE_2: return "3";
        case SequenceHandler.SEQ_SIDE_3: return "4";
        case SequenceHandler.SEQ_SIDE_4: return "5";
        default:                         return "";
    }
}
```

**(8) 빌드 검증** — Debug|x64 / Release|x64. 산출물 잠김 시 스크래치 OutDir, 프로세스 종료 금지.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement/WPF_Example &amp;&amp; grep -c "_lastPrepZIndex\b" Custom/SystemHandler.cs; grep -c "_lastPrepZIndexBySeq" Custom/SystemHandler.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_lastPrepZIndex\b" WPF_Example/Custom/SystemHandler.cs` == 0 (구 전역 변수 잔존 0)
    - `grep -c "_lastPrepZIndexBySeq" WPF_Example/Custom/SystemHandler.cs` >= 4
    - `grep -c "StorePrepZIndex\|GetPrepZIndex" WPF_Example/Custom/SystemHandler.cs` >= 4
    - `grep -c "ApplyPrepToSequences" WPF_Example/Custom/SystemHandler.cs` == 0
    - `grep -c "ApplyPrepToSequence(" WPF_Example/Custom/SystemHandler.cs` == 2 (정의 1 + 호출 1)
    - `grep -c "bTreatAsDatumOnly\|bIsDatumOnlyForSomeSeq\|bIsDatumOnlyZero" WPF_Example/Custom/SystemHandler.cs` == 0 (M1 예외 분기 소멸)
    - `grep -c "ResolveTypeCodeBySequenceName" WPF_Example/Custom/SystemHandler.cs` == 2
    - `grep -c "prepPacket.IsRequestValid = true" WPF_Example/Custom/SystemHandler.cs` == 1
    - `grep -c "_lastPrepZIndexBySeq.Clear()" WPF_Example/Custom/SystemHandler.cs` == 1
    - `StartV1Scoped` 시그니처에 `int nPrepZIndex` 파라미터 존재
    - `ProcessPrep` 이 `packet == null` 이외의 경로에서 `null` 을 반환하지 않는다(`return null;` 총 1건)
    - 신규 `?:` / `??` / `?.` 0건, switch expression(`=>`) 0건
    - msbuild Debug|x64 exit 0, 에러 0, 경고 ≤ 12줄 / Release|x64 exit 0, 에러 0
  </acceptance_criteria>
  <done>Type 이 대상 시퀀스를 정하고, z_index 가 시퀀스별로 분리 저장되며, $PREP_ACK 의 FAIL 이 조명/규격 사유로만 나간다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLC $PREP/$TEST Type 필드 → 내부 시퀀스 라우팅 | 신뢰 없는 정수가 실행 대상을 정한다 |
| MainRun 폴링 스레드 ↔ 시퀀스 스레드 | `_lastPrepZIndexBySeq` 공유 접근 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-11 | Spoofing | `TryResolveSequenceNameByType` | mitigate | 화이트리스트 전통 switch, default 는 실패 반환 + Error 로그(조용한 TOP 폴백 없음) |
| T-73-12 | Tampering | `_lastPrepZIndexBySeq` 교차 스레드 접근 | mitigate | 모든 읽기/쓰기를 `_prepZIndexLock` 으로 감쌈 |
| T-73-13 | Denial of Service | `ProcessPrep` 무응답 → PLC 무한 대기 | mitigate | `return null` 은 packet==null 1건뿐, 나머지 전 경로가 ACK 반환 |
| T-73-14 | Elevation of Privilege | 잘못된 Type 으로 남의 지그 조명 조작 | accept | 조명은 물리 안전 영향이 없고 PC2 의 SIDE 4개로 범위가 닫혀 있다 |
</threat_model>

<verification>
1. `$PREP:2,4,1@` → seq=SIDE_3, z=1 저장, `$PREP_ACK:2,4,1,OK`
2. `$PREP:2,9,0@` (미지 Type) → `PREP_ACK:2,9,0,FAIL` + Error 로그, 조명 무변경
3. `$PREP:2,2,0@` 직후 `$PREP:2,3,0@` → SIDE_1 과 SIDE_2 의 z 가 서로 덮어쓰지 않는다
4. `$RESET` 후 `_lastPrepZIndexBySeq` 가 비어 있다
5. 두 빌드 구성 에러 0
</verification>

<success_criteria>
- Type 2/3/4/5 → SIDE_1/2/3/4 라우팅
- 시퀀스별 z_index 독립 보관
- $PREP_ACK FAIL 이 조명/규격 사유로만 발생
</success_criteria>

<output>
완료 후 `.planning/phases/73-side-4-jig-split/73-04-SUMMARY.md` 를 작성한다.
</output>
