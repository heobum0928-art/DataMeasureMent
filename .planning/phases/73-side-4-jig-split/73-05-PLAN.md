---
phase: 73-side-4-jig-split
plan: 05
type: execute
wave: 3
depends_on: ["73-01", "73-03"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
autonomous: true
requirements: [PROTO-73-JIG-ISOLATION]
must_haves:
  truths:
    - "한 시퀀스의 $PREP 점등이 다른 시퀀스 소유 조명 채널을 건드리지 않는다"
    - "SIDE_1 사이클 종료 소등이 아직 검사 중인 형제 SIDE 시퀀스의 조명 채널을 끄지 않는다"
    - "조명 세팅 실패(그룹명 부재/채널 매핑 소실)가 $PREP_ACK 의 FAIL 로 전달된다"
    - "Shot 이 없는 z(기준점 전용 z, 빈 z)는 조명 세팅 성공(OK)으로 취급된다"
    - "자기 z 범위 밖 요청은 최종 P/F 를 내지 않고 B + Error 로그로 넘어간다"
    - "스코프 밖 조명 잔광 잔여 위험(W8)이 SUMMARY 에 알려진 제약으로 기록된다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "점등/소등 대칭 채널 스코핑 / 조명 성공 bool 전파 / 범위 밖 z 방어"
      contains: "CollectOwnedChannelScope"
  key_links:
    - from: "ApplyShotLightsInternal"
      to: "CollectOwnedChannelScope()"
      via: "자기 소유 채널 집합 안에서만 SetChannelOnOff/SetOnOff 호출"
      pattern: "CollectOwnedChannelScope"
    - from: "TurnOffOwnShotLights"
      to: "CollectBusySiblingChannels()"
      via: "non-Idle 형제 채널을 소등 대상에서 제외"
      pattern: "CollectBusySiblingChannels"
    - from: "AddResponseV1Cycle"
      to: "ApplyCycleJudgement"
      via: "bIsBeyondRange 이면 bIsLastIndex=false 로 B 유지"
      pattern: "bIsBeyondRange"
---

<objective>
지그 4개가 같은 물리 조명(`LIGHT_BAR_1~4`)과 같은 카메라를 공유하는 데서 오는 간섭을,
**점등과 소등 양쪽에서** 막는다(R1 확장).

현재 `ApplyShotLightsInternal` 은 13채널을 **절대값으로 전부 덮어쓴다**(안 쓰는 채널은 명시적 OFF).
그래서 PC1 에서는 `$PREP z=0` 하나에 TOP 조명이 적용됐다가 BOTTOM 조명이 덮어써
**`$PREP` 의 예열 기능이 사실상 무효**가 되고 있다(TOP shot 3개·BOTTOM shot 16개가 전부 z=0,
등록 순서 Top→Side→Bottom). TOP/BOTTOM 은 카메라 공유로 순차 실행이라 실질 피해가 없었지만,
**SIDE_1~4 는 같은 물리 채널을 공유**하므로 여기서 실제로 걸린다.

⚠ **[W7] 현 레시피 실측 정정:** SIDE 8개 Shot 은 `SideLight_Enabled_1~4` 가 **전부 False** 다.
실제로 켜는 채널은 **`LIGHT_BACK`(8개 Shot 전부 True)** 과 **`LIGHT_ALIGN_COAX`(SHOT_4=SIDE_1 만 True)** 뿐이다.
완화책 자체는 채널 이름을 가리지 않으므로 그대로 유효하지만, **검증 시나리오는 반드시 BACK/COAX 로 써야 한다** —
BAR 로 쓰면 원래 아무도 안 켜는 채널이라 무조건 통과하는 vacuous 테스트가 된다.

소등 쪽에는 이미 스코핑이 있다(`TurnOffOwnShotLights` -> `CollectOwnShotChannels`/
`CollectOwnDatumChannels`). **점등에 같은 대칭 구조를 만든다.**

함께 `$PREP_ACK` 의 새 FAIL 정의(조명 세팅 실패 전용)를 실제 감지 가능하게 하고(D-73-08),
FAIL 정의 변경으로 생긴 갭인 **범위 밖 z 의 미측정 PASS 위험**을 닫는다(M13).

Purpose: "지그별 독립 사이클"을 조명/판정 계층까지 관철.
Output: `InspectionSequence.cs` 단일 파일 변경.

⚠ 이 plan 은 73-03 과 **같은 파일**(`InspectionSequence.cs`)을 고친다 — 그래서 wave 3 이다.
73-03 이 추가한 `NormalizeModelFolderName` / `[DatumModelPath]` 로그를 **지우거나 옮기지 않는다.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-side-4-jig-split/73-CONTEXT.md
@.planning/phases/73-side-4-jig-split/73-01-SUMMARY.md
@.planning/phases/73-side-4-jig-split/73-03-SUMMARY.md
@CLAUDE.md
@.planning/phases/73-side-4-jig-split/73-BUILD-VERIFY.md

**코딩 규칙:** 삼항 `?:` / `??` / `?.` 금지. 전통 switch 만. C# 7.2. 헝가리언(b/n/sz).
이 파일은 **Allman 스타일**. 날짜 주석 새로 달지 않는다. csproj 커밋 금지. 실행 중 프로세스 종료 금지.

**감지 한계를 과장하지 말 것 (D-73-08 명시):** `SetOnOff(string,bool)` 이 false 를 돌려주는 건
(1) `light.ini` 그룹명 부재 (2) 채널 매핑 소실 두 경우뿐이다(`LightHandler.cs:216`).
실제 시리얼 전송은 `void` 라 **케이블 단선/컨트롤러 전원 OFF/LED 고장은 못 잡는다.**
주석에 그 한계를 반드시 적는다 — 제어가 "FAIL 이 안 왔으니 조명은 정상"이라고 믿으면 안 된다.

**[W8] 잔여 위험 — 스코프 밖 잔광.** `CollectOwnedChannelScope` 는 `AddChannelIfEnabled`
(Enabled=true 인 채널만 수집)를 재사용하므로, **자기 시퀀스가 한 번도 켜지 않는 채널은
더 이상 강제 OFF 되지 않는다.** 현 레시피에서 실제 시나리오는:
SIDE_1(SHOT_4)만 `CoaxLight_Enabled=True` → SIDE_1 이 비정상 종료(OnError/OnStop)하면
COAX 가 켜진 채 남고, SIDE_2~4 는 COAX 를 스코프에 갖고 있지 않아 끄지 못한다 → **촬영 조명 오염.**

이 plan 은 그 위험을 **제거하지 않고 명시적으로 남긴다**(범위를 지키기 위함).
대신 (i) 이 사실을 SUMMARY 의 "알려진 잔여 위험" 으로 기록하고,
(ii) 비상 경로인 `TurnOffShotLights()`(전 채널 강제 소등)를 **무변경으로 보존**해
운영자가 레시피 전환/비상정지로 복구할 수 있게 둔다.
73-07 이 이 항목을 `73-HUMAN-UAT.md` 로 옮긴다.

**회귀 주의(점등 스코핑):** 기존 동작은 "안 쓰는 채널을 OFF" 였다. 이걸 "건드리지 않음"으로
바꾸면 **이전 사이클 조명이 남을 수 있다.** 자기 채널 집합 **안에서는** 기존처럼
`Enabled=false -> OFF` 를 반드시 유지한다. 바뀌는 건 **남의 채널을 안 건드린다**는 것뿐이다.

**빌드 검증은 `73-BUILD-VERIFY.md` 규격을 따른다(전 plan 공통 단일 소스).**
- msbuild 는 `-p:` 대시 형식만 사용(`/p:` 는 MSYS 경로변환으로 MSB1008 실패)
- SIMUL-OFF 는 `Release|x64` 가 아니라 `-p:DefineConstants=TRACE%3BDEBUG` 로 검증(csproj 무수정)
- 경고 baseline: 73-01 Task3 이후 **SIMUL-ON 18줄 / SIMUL-OFF 16줄**(CS0618 6줄 증가는 정상)
- 통과 기준 1순위는 "에러 0 + **새 경고 코드 종류 0건**". 숫자를 맞추려고
  `[Obsolete]` 제거 / `#pragma warning disable` / `NoWarn` 추가는 **금지**
</context>

<interfaces>
현행 코드(실측):

```csharp
public bool ApplyShotLights(int nZIndex)                        // :751
public void ApplyShotLightsDirect(ShotConfig shot)              // :767
public void TurnOffShotLights()                                 // :776  전 그룹 강제 소등(비상정지/레시피 전환용, 무변경)
private void TurnOffOwnShotLights()                             // :792
private void CollectOwnShotChannels(HashSet<string> channels)   // :804  IsShotOwnedBySequence(shot, Name) 필터 + AddChannelIfEnabled x13
private void CollectOwnDatumChannels(HashSet<string> channels)  // :837  DatumConfigs 는 인스턴스 필드(이미 자기 소유)
private void AddChannelIfEnabled(HashSet<string> channels, bool bEnabled, string channelName)  // :866
private void ApplyShotLightsInternal(ShotConfig shot)           // :900  13채널 절대값 덮어쓰기, 반환값 전부 버림
private void ApplyDatumLightsInternal(DatumConfig datum)        // :954  동일 13채널 매핑
private void ApplyChannelLight(string channelName, bool bEnabled, int nBrightness)  // :1001  void
public void ApplyDatumLights(DatumConfig datum)                 // :947
private void TryTurnOffLightsOnCycleEnd(...)                    // :878 부근  TurnOffOwnShotLights() 호출
private void AddResponseV1Cycle()                               // :1605
private TestResultPacket BuildScopedResponse(InspectionRecipeManager, int nZIndex, bool bIsLastIndex)  // :1638
private void ApplyCycleJudgement(TestResultPacket packet, bool bIsLastIndex, int nMatchedShots)        // :1893
private int ComputeLastZIndex(InspectionRecipeManager recipeManager)   // :508
```

**13채널 매핑** (`CollectOwnShotChannels` 와 `ApplyShotLightsInternal` 이 이미 서로 미러하고 있다):
`LIGHT_RING_CH1~CH6` (RingLight_Enabled_1~6) / `LIGHT_BACK` (BackLight_Enabled) /
`LIGHT_ALIGN_COAX` (CoaxLight_Enabled) / `LIGHT_BAR_1~4` (SideLight_Enabled_1~4) /
`LIGHT_RING7` (Ring7Light_Enabled)

```csharp
// Device/LightController/LightHandler.cs
public bool SetOnOff(string groupName, bool onOff)           // :216
public bool SetLevel(string groupName, int level)            // :239
public bool SetChannelOnOff(string channelName, bool onOff)  // :278
// SetChannelLevel 의 반환 타입은 파일에서 직접 확인할 것
```

현행 판정(:1614~1617):
```csharp
m_nLastZIndex = ComputeLastZIndex(recipeManager);
bool bIsLastIndex = m_nCurrentZIndex >= m_nLastZIndex;   // 범위 밖 z 가 최종 P/F 를 낸다(M13)
```

**신규 z 배정 (D-73-01):** SIDE_1 0~2 / SIDE_2 0~3 / SIDE_3 0~4 / SIDE_4 0~3.
마지막 z 에 항상 측정 Shot 이 있으므로 `ComputeLastZIndex` 가 그대로 최종 P/F 시점이다.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 형제 시퀀스 채널 제외 소등 (R1 — 소등 측)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs :770~880 (TurnOffShotLights / TurnOffOwnShotLights / CollectOwnShotChannels / CollectOwnDatumChannels / AddChannelIfEnabled / TryTurnOffLightsOnCycleEnd) — 특히 :783~790 의 "현재 레시피에서는 발생하지 않음" 주석
    - WPF_Example/Custom/SystemHandler.cs 의 site-status 처리부(EContextState 접근자의 실제 이름 확인)
    - WPF_Example/Sequence/Sequence/SequenceBase.cs (시퀀스 상태 프로퍼티 / Sequences 컬렉션 인덱서)
    - .planning/phases/73-side-4-jig-split/73-CONTEXT.md D-73-07 의 R1 (채택안 = (a) 형제 시퀀스 non-Idle 채널 제외)
  </read_first>
  <action>
`InspectionSequence.cs:783~790` 주석이 기록한 "현재 레시피에서는 발생하지 않음" 조건을
**이번 분리가 정확히 만든다.** 상호배타 게이트(`FindBlockingSequenceName`)는 **UI RUN 전용**이고
TCP `$TEST` 경로는 거치지 않으며, `OnStop`/`OnError` 소등은 무조건 발화한다.
D-73-08 로 제어 공정이 순차임이 확인돼 실제 발생 가능성은 낮아졌으나 **코드가 강제하지 않으므로**
수정은 유지한다.

**(1) 형제 채널 수집 헬퍼 추가** — `TurnOffOwnShotLights` 바로 위:

```csharp
// 지금 검사 중(비-Idle)인 다른 InspectionSequence 가 쓰는 조명 채널을 모은다.
//  Phase 73 이 SIDE 를 4개로 쪼개면서 SIDE_1~4 가 LIGHT_BAR_1~4 라는 같은 물리 채널을 공유한다 —
//  "자기 채널만 끈다"는 스코핑만으로는 형제 지그의 조명을 끄는 것을 막지 못한다.
//  제어 공정은 지그를 순차 진행하지만 그 순서를 코드가 강제하지는 않으므로 여기서 막는다.
private HashSet<string> CollectBusySiblingChannels()
{
    var busyChannels = new HashSet<string>();
    int nCount = SystemHandler.Handle.Sequences.Count;
    for (int i = 0; i < nCount; i++)
    {
        SequenceBase seqBase = SystemHandler.Handle.Sequences[i];
        InspectionSequence sibling = seqBase as InspectionSequence;
        bool bIsInsp = sibling != null;
        if (!bIsInsp)
        {
            continue;
        }
        bool bIsSelf = ReferenceEquals(sibling, this);
        if (bIsSelf)
        {
            continue;
        }
        bool bIsIdle = sibling.State == EContextState.Idle;
        if (bIsIdle)
        {
            continue;
        }
        sibling.CollectOwnShotChannels(busyChannels);
        sibling.CollectOwnDatumChannels(busyChannels);
    }
    return busyChannels;
}
```

`State` 프로퍼티의 **실제 이름/접근자는 파일에서 확인해 그대로 쓴다**(추측 금지).
`CollectOwnShotChannels`/`CollectOwnDatumChannels` 는 같은 클래스의 다른 인스턴스라 `private`
그대로 접근 가능하다 — **접근 제한자를 넓히지 않는다.**

**(2) `TurnOffOwnShotLights` 수정** — 소등 직전에 형제 채널을 뺀다:

```csharp
private void TurnOffOwnShotLights()
{
    HashSet<string> usedChannels = CollectOwnedChannelScope();

    // 형제 지그가 아직 검사 중이면 그 채널은 남겨 둔다(공유 채널 소등 충돌 차단).
    HashSet<string> busyChannels = CollectBusySiblingChannels();
    foreach (string busyName in busyChannels)
    {
        bool bRemoved = usedChannels.Remove(busyName);
        if (bRemoved)
        {
            Logging.PrintLog((int)ELogType.LightController,
                "[CycleLightOff] Seq={0} 채널 {1} 소등 보류 — 형제 시퀀스가 사용 중", Name, busyName);
        }
    }

    foreach (string channelName in usedChannels)
    {
        LightHandler.Handle.SetChannelOnOff(channelName, false);
    }
}
```

`CollectOwnedChannelScope()` 는 Task 2 에서 만든다. Task 1 을 먼저 커밋한다면 그 자리에
기존 2줄(`CollectOwnShotChannels(usedChannels); CollectOwnDatumChannels(usedChannels);`)을
그대로 두어도 되지만, **Task 2 완료 시점에는 반드시 위 형태여야 한다** — 점등과 소등이
같은 채널 수집 기준을 쓰게 하는 것이 이 plan 의 핵심이다.

`TurnOffShotLights()`(전 채널 강제 소등)는 **무변경** — 비상정지/레시피 전환용 경로다.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement/WPF_Example &amp;&amp; grep -c "CollectBusySiblingChannels" Custom/Sequence/Inspection/InspectionSequence.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CollectBusySiblingChannels" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` == 2
    - `grep -c "소등 보류" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` == 1
    - `grep -c "EContextState.Idle" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` >= 1
    - `grep -c "ReferenceEquals(sibling, this)" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` == 1
    - `TurnOffShotLights()` 본문 무변경(`git diff` 에서 해당 메서드 내부 hunk 0)
    - `CollectOwnShotChannels` / `CollectOwnDatumChannels` 접근 제한자 무변경(`private void` 유지)
    - **[W4] 코딩 규칙 위반 검사는 리터럴 `?:` grep 으로 하지 않는다** — C# 삼항은 `a ? b : c` 라
      두 글자 `?:` 는 원래부터 0건이라 항상 통과하는 무의미한 조건이다. 대신 이 plan 의 diff 에 대해:
      `git diff -- <이 plan 이 고친 파일> | grep "^+" | grep -nE '\?\?|\?\.' || true` 가 0줄이고,
      `... | grep -vE '^\+\s*//' | grep -nE '\?[^;]*:' || true` 후보를 육안 판별해 진성 삼항 0건
  </acceptance_criteria>
  <done>형제 시퀀스가 비-Idle 이면 그 채널이 소등 대상에서 빠지고 그 사실이 로그에 남는다.</done>
</task>

<task type="auto">
  <name>Task 2: 점등도 자기 채널만 — 소등과 대칭 스코핑 + 조명 성공 bool 집계</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs :745~775 (ApplyShotLights / ApplyShotLightsDirect)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs :800~875 (CollectOwnShotChannels / CollectOwnDatumChannels / AddChannelIfEnabled — 재사용할 채널 수집 기준)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs :895~1015 (ApplyShotLightsInternal / ApplyDatumLights / ApplyDatumLightsInternal / ApplyChannelLight)
    - WPF_Example/Device/LightController/LightHandler.cs :210~300 (SetOnOff / SetLevel / SetChannelOnOff / SetChannelLevel 의 반환 타입과 false 조건)
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs :195~220 (grab 직전 조명 재적용 — 이 변경이 그 경로를 깨지 않는지 확인)
    - .planning/phases/73-side-4-jig-split/73-CONTEXT.md D-73-06 (조명 13채널 절대값 덮어쓰기 확정 사실) / D-73-08 (감지 한계)
  </read_first>
  <action>
**(A) 공용 채널 수집 헬퍼 — 점등/소등이 같은 기준을 쓰게 한다**

`CollectOwnShotChannels` 의 수집 로직을 **재사용**한다. 새 수집 기준을 발명하지 않는다 —
기준이 갈리면 점등/소등 비대칭이 다시 생긴다. `CollectOwnShotChannels` 바로 위에 추가:

```csharp
// 이 시퀀스가 소유한(자기 Shot + 자기 Datum) 조명 채널 집합.
//  점등(ApplyShotLightsInternal/ApplyDatumLightsInternal)과 소등(TurnOffOwnShotLights)이
//  반드시 같은 기준을 쓰게 하는 단일 소스 — 기준이 갈리면 "켠 채널을 안 끄거나 안 켠 채널을 끄는"
//  비대칭이 생긴다.
private HashSet<string> CollectOwnedChannelScope()
{
    var channels = new HashSet<string>();
    CollectOwnShotChannels(channels);
    CollectOwnDatumChannels(channels);
    return channels;
}
```

**(B) `ApplyChannelLight` 를 스코프 인자 + `bool` 반환으로 변경**

```csharp
// 채널 하나의 On/Off + 밝기 적용. 반환값 = 조명 세팅 성공 여부.
//  ownedScope 에 없는 채널은 이 시퀀스 소유가 아니므로 아예 건드리지 않는다(true 반환).
//   -> PC1 의 $PREP z=0 에서 BOTTOM 이 TOP 조명을 덮어쓰던 문제, SIDE_1~4 가 공유
//      LIGHT_BAR_1~4 를 서로 덮어쓰는 문제를 둘 다 막는다.
//  스코프 '안'에서는 기존 동작을 그대로 유지한다 — Enabled=false 면 반드시 OFF 를 건다.
//   그러지 않으면 이전 사이클 조명이 남는다.
//  false 가 나오는 경우는 두 가지뿐이다(LightHandler.cs:216): (1) light.ini 에 그룹/채널명이 없다
//  (2) 채널 매핑이 소실됐다. 실제 시리얼 전송은 void 라 케이블 단선/컨트롤러 전원 OFF/LED 고장은
//  여기서 잡히지 않는다 — "FAIL 이 없었으니 조명은 정상"이라는 뜻이 아니다.
private bool ApplyChannelLight(HashSet<string> ownedScope, string channelName, bool bEnabled, int nBrightness)
{
    bool bIsOwned = ownedScope.Contains(channelName);
    if (!bIsOwned)
    {
        return true;
    }
    if (bEnabled)
    {
        bool bOnOk = LightHandler.Handle.SetChannelOnOff(channelName, true);
        bool bLevelOk = LightHandler.Handle.SetChannelLevel(channelName, nBrightness);
        return bOnOk && bLevelOk;
    }
    return LightHandler.Handle.SetChannelOnOff(channelName, false);
}
```

`SetChannelLevel` 이 `void` 면 `bLevelOk` 를 만들지 말고 `bOnOk` 만 반환한다 —
**실제 시그니처를 확인하고 맞춘다.**

Back / Coax / Ring7 은 그룹 API(`SetOnOff`/`SetLevel`)를 쓴다. 같은 스코프 규칙을 적용하는
그룹용 헬퍼를 추가해 세 블록의 중복을 없앤다:

```csharp
// 그룹 API(SetOnOff/SetLevel)를 쓰는 조명의 스코프 적용판. 채널 API 판(ApplyChannelLight)과 규칙 동일.
private bool ApplyGroupLight(HashSet<string> ownedScope, string groupName, bool bEnabled, int nBrightness)
{
    bool bIsOwned = ownedScope.Contains(groupName);
    if (!bIsOwned)
    {
        return true;
    }
    if (bEnabled)
    {
        bool bOnOk = LightHandler.Handle.SetOnOff(groupName, true);
        bool bLevelOk = LightHandler.Handle.SetLevel(groupName, nBrightness);
        return bOnOk && bLevelOk;
    }
    return LightHandler.Handle.SetOnOff(groupName, false);
}
```

**(C) `ApplyShotLightsInternal` / `ApplyDatumLightsInternal` 을 `bool` 반환 + 스코프 적용으로 변경**

13채널 **전 호출을 하나도 건너뛰지 않고** 결과를 AND 집계한다(중간 return 금지 —
선언적 절대값 덮어쓰기 성질 유지). 채널 목록/순서/Enabled/Brightness 소스는 **한 줄도 바꾸지 않는다**:

```csharp
private bool ApplyShotLightsInternal(ShotConfig shot)
{
    HashSet<string> ownedScope = CollectOwnedChannelScope();
    bool bAllOk = true;
    bAllOk = ApplyChannelLight(ownedScope, LightHandler.LIGHT_RING_CH1, shot.RingLight_Enabled_1, shot.RingLight_Brightness_1) && bAllOk;
    bAllOk = ApplyChannelLight(ownedScope, LightHandler.LIGHT_RING_CH2, shot.RingLight_Enabled_2, shot.RingLight_Brightness_2) && bAllOk;
    bAllOk = ApplyChannelLight(ownedScope, LightHandler.LIGHT_RING_CH3, shot.RingLight_Enabled_3, shot.RingLight_Brightness_3) && bAllOk;
    bAllOk = ApplyChannelLight(ownedScope, LightHandler.LIGHT_RING_CH4, shot.RingLight_Enabled_4, shot.RingLight_Brightness_4) && bAllOk;
    bAllOk = ApplyChannelLight(ownedScope, LightHandler.LIGHT_RING_CH5, shot.RingLight_Enabled_5, shot.RingLight_Brightness_5) && bAllOk;
    bAllOk = ApplyChannelLight(ownedScope, LightHandler.LIGHT_RING_CH6, shot.RingLight_Enabled_6, shot.RingLight_Brightness_6) && bAllOk;
    bAllOk = ApplyGroupLight(ownedScope, LightHandler.LIGHT_BACK, shot.BackLight_Enabled, shot.BackLight_Brightness) && bAllOk;
    bAllOk = ApplyGroupLight(ownedScope, LightHandler.LIGHT_ALIGN_COAX, shot.CoaxLight_Enabled, shot.CoaxLight_Brightness) && bAllOk;
    bAllOk = ApplyChannelLight(ownedScope, LightHandler.LIGHT_BAR_1, shot.SideLight_Enabled_1, shot.SideLight_Brightness_1) && bAllOk;
    bAllOk = ApplyChannelLight(ownedScope, LightHandler.LIGHT_BAR_2, shot.SideLight_Enabled_2, shot.SideLight_Brightness_2) && bAllOk;
    bAllOk = ApplyChannelLight(ownedScope, LightHandler.LIGHT_BAR_3, shot.SideLight_Enabled_3, shot.SideLight_Brightness_3) && bAllOk;
    bAllOk = ApplyChannelLight(ownedScope, LightHandler.LIGHT_BAR_4, shot.SideLight_Enabled_4, shot.SideLight_Brightness_4) && bAllOk;
    bAllOk = ApplyGroupLight(ownedScope, LightHandler.LIGHT_RING7, shot.Ring7Light_Enabled, shot.Ring7Light_Brightness) && bAllOk;
    if (!bAllOk)
    {
        Logging.PrintLog((int)ELogType.Error,
            "[LightSet] Seq={0} shot={1} 조명 세팅 실패 — light.ini 그룹/채널명 확인 필요", Name, shot.ShotName);
    }
    return bAllOk;
}
```

`ApplyDatumLightsInternal` 도 같은 형태로 바꾼다(소스만 `datum.*`, 로그는 `datum={DatumName}`).

**(D) `ApplyShotLights(int nZIndex)` 의 반환 의미 재정의**

이 값이 곧 `$PREP_ACK` 의 OK/FAIL 이 된다. D-73-08 확정에 따라 "이 z 에 Shot 이 없음"은
**실패가 아니다**:

```csharp
// $PREP 대상 z_index 의 Shot 조명을 미리 적용한다.
//  반환값 = 조명 세팅 성공 여부($PREP_ACK 의 OK/FAIL). D-73-08: 검사 항목 유무는 반영하지 않는다 —
//  Shot 이 없는 z(기준점 전용 z, 아직 항목을 안 넣은 빈 z)는 켤 대상이 없을 뿐이므로 OK 다.
//  (과거 SIDE z=1/4/8/13 이 전부 PREP_ACK FAIL 로 나가던 회귀를 되돌리지 않기 위한 계약.)
public bool ApplyShotLights(int nZIndex)
{
    ShotConfig shot = FindShotByZIndex(nZIndex);
    bool bHasShot = shot != null;
    if (!bHasShot)
    {
        Logging.PrintLog((int)ELogType.Trace,
            "[LightSet] Seq={0} z={1} 에 Shot 없음 — 조명 세팅 대상 없음(OK)", Name, nZIndex);
        return true;
    }
    return ApplyShotLightsInternal(shot);
}
```

기존 본문의 null/가드 처리 방식은 원본을 읽고 그대로 유지하되, **반환값만** 위 계약에 맞춘다.

**(E) `ApplyShotLightsDirect` / `ApplyDatumLights` 를 `bool` 반환으로 바꾼다**(내부 값 전달).
호출부가 반환값을 무시해도 컴파일 오류가 나지 않으므로 **호출부는 수정하지 않는다.**

**(F) `TurnOffOwnShotLights` 의 채널 수집을 `CollectOwnedChannelScope()` 호출로 통일한다**
(Task 1 의 코드 형태 완성 — 점등/소등 같은 기준 보장).
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement/WPF_Example &amp;&amp; grep -c "CollectOwnedChannelScope" Custom/Sequence/Inspection/InspectionSequence.cs &amp;&amp; grep -c "ownedScope" Custom/Sequence/Inspection/InspectionSequence.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CollectOwnedChannelScope" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` == 4 (정의 1 + ApplyShotLightsInternal 1 + ApplyDatumLightsInternal 1 + TurnOffOwnShotLights 1)
    - `grep -c "private bool ApplyChannelLight(HashSet<string> ownedScope" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` == 1
    - `grep -c "private bool ApplyGroupLight(HashSet<string> ownedScope" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` == 1
    - `grep -c "private bool ApplyShotLightsInternal" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` == 1
    - `grep -c "private bool ApplyDatumLightsInternal" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` == 1
    - `grep -c "private void ApplyChannelLight\|private void ApplyShotLightsInternal\|private void ApplyDatumLightsInternal" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` == 0
    - **남의 채널 미접촉 (핵심):** 위 `awk` 범위 한정으로 `ApplyShotLightsInternal` /
      `ApplyDatumLightsInternal` 본문에 `LightHandler.Handle.` 직접 호출이 **0건**
      (`... | grep -c "LightHandler.Handle." || true` == 0) — 전부 `ApplyChannelLight` / `ApplyGroupLight` 경유
    - **스코프 안에서 OFF 유지:** `ApplyChannelLight` / `ApplyGroupLight` 각각에
      `bEnabled == false` 경로의 `SetChannelOnOff(channelName, false)` / `SetOnOff(groupName, false)` 가 존재
    - **같은 수집 기준:** `CollectOwnedChannelScope` 본문이 `CollectOwnShotChannels` + `CollectOwnDatumChannels`
      두 호출로만 구성되고, 별도 채널 목록을 새로 열거하지 않는다
    - **[W3] 메서드 본문 범위 카운트는 파일 전역 grep 으로 검증 불가**(파일 전역 `ApplyChannelLight(` 는 20건 이상).
      `awk` 로 메서드 범위를 한정해 센다:
      ```bash
      awk '/private bool ApplyShotLightsInternal/{f=1} f&&/^        \}/{print;exit} f'         WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs | grep -c "ApplyChannelLight("
      ```
      기대 == 10, 같은 방식으로 `ApplyGroupLight(` == 3, `ApplyDatumLightsInternal` 도 10/3
    - `grep -c "조명 세팅 대상 없음(OK)" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` == 1
    - `grep -c "케이블 단선" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` == 1 (감지 한계 명시)
    - `CollectOwnShotChannels` / `CollectOwnDatumChannels` / `AddChannelIfEnabled` 본문 무변경
    - **[W4] 코딩 규칙 위반 검사는 리터럴 `?:` grep 으로 하지 않는다** — C# 삼항은 `a ? b : c` 라
      두 글자 `?:` 는 원래부터 0건이라 항상 통과하는 무의미한 조건이다. 대신 이 plan 의 diff 에 대해:
      `git diff -- <이 plan 이 고친 파일> | grep "^+" | grep -nE '\?\?|\?\.' || true` 가 0줄이고,
      `... | grep -vE '^\+\s*//' | grep -nE '\?[^;]*:' || true` 후보를 육안 판별해 진성 삼항 0건
  </acceptance_criteria>
  <done>점등이 자기 소유 채널 집합 안에서만 일어나고(그 안에서는 Enabled=false -> OFF 유지), 조명 그룹명 오타 시 `ApplyShotLights` 가 false 를, Shot 이 없는 z 는 true 를 반환한다.</done>
</task>

<task type="auto">
  <name>Task 3: 범위 밖 z 방어 — 미측정 PASS 차단 (M13)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs :1600~1640 (AddResponseV1Cycle / HandleDatumIndexResponse)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs :1638~1660 (BuildScopedResponse)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs :1885~1915 (ApplyCycleJudgement — WR-01 주석)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs :500~545 (ComputeLastZIndex)
    - .planning/phases/73-side-4-jig-split/73-CONTEXT.md D-73-08 의 "신규 과제 — M13"
  </read_first>
  <action>
`$PREP_ACK` 의 FAIL 이 더 이상 "해당 z 없음"을 걸러주지 않으므로(D-73-08), 자기 범위 밖 z 요청이
그대로 들어온다. 현재 `bIsLastIndex = m_nCurrentZIndex >= m_nLastZIndex` 이므로
**범위 밖 z 가 측정 0건으로 최종 P/F 를 낸다** — 과거 안전결함 유형(미측정 PASS)이다.

`AddResponseV1Cycle` 의 판정 3줄을 아래로 교체한다:

```csharp
// 측정 Index (중간 / 마지막 / 범위 밖) — m_nLastZIndex 는 이 경로에서 1회만 산출.
m_nLastZIndex = ComputeLastZIndex(recipeManager);

// M13: $PREP_ACK 이 더 이상 "해당 z 없음"을 걸러주지 않으므로(D-73-08 FAIL=조명 전용),
//  자기 범위 밖 z 가 여기까지 들어온다. 그때 최종 P/F 를 내면 측정 0건으로 P 가 나갈 수 있다.
//  범위 밖은 판정을 미루고(B) 로그만 남긴다.
//  m_nLastZIndex == 0 (이 시퀀스에 측정 Shot 이 없는 레시피)은 범위 밖으로 취급하지 않는다 —
//  그 경우는 기존 WR-01 가드(마지막 Index 매칭 0건이면 F 강제)가 false-PASS 를 막아야 한다.
bool bHasMeasurementShots = m_nLastZIndex > 0;
bool bIsBeyondRange = bHasMeasurementShots && m_nCurrentZIndex > m_nLastZIndex;
if (bIsBeyondRange)
{
    Logging.PrintLog((int)ELogType.Error,
        "[SEQ] {0} 범위 밖 z_index 수신 — z={1}, 이 시퀀스 최대 z={2}. 최종 판정을 내지 않고 B 로 응답한다.",
        Name, m_nCurrentZIndex, m_nLastZIndex);
}
bool bIsLastIndex = false;
if (!bIsBeyondRange)
{
    bIsLastIndex = m_nCurrentZIndex >= m_nLastZIndex;
}
TestResultPacket packet = BuildScopedResponse(recipeManager, m_nCurrentZIndex, bIsLastIndex);
PersistAndEnqueueV1(recipeManager, packet);
```

`ApplyCycleJudgement` / `BuildScopedResponse` / WR-01 로직은 **한 줄도 바꾸지 않는다** —
`bIsLastIndex=false` 가 들어가면 기존 코드가 알아서 B 를 만든다.

**빌드 검증** — `73-BUILD-VERIFY.md` §2(SIMUL-ON) / §3(SIMUL-OFF) 명령 그대로 실행.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement/WPF_Example &amp;&amp; grep -c "bIsBeyondRange" Custom/Sequence/Inspection/InspectionSequence.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bIsBeyondRange" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` == 3
    - `grep -c "bHasMeasurementShots" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` == 2
    - `grep -c "범위 밖 z_index 수신" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` == 1
    - `ApplyCycleJudgement` 본문 무변경(`git diff` 에서 해당 메서드 내부 hunk 0)
    - `BuildScopedResponse` 본문 무변경
    - `grep -c "bEmptyLastScope" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` == 2 (WR-01 가드 보존)
    - SIMUL-ON 빌드 exit 0 / 에러 0 / 경고 18줄 · SIMUL-OFF 빌드 exit 0 / 에러 0 / 경고 16줄 (73-BUILD-VERIFY.md §2~4, 새 경고 코드 종류 0건)
    - **[W4] 코딩 규칙 위반 검사는 리터럴 `?:` grep 으로 하지 않는다** — C# 삼항은 `a ? b : c` 라
      두 글자 `?:` 는 원래부터 0건이라 항상 통과하는 무의미한 조건이다. 대신 이 plan 의 diff 에 대해:
      `git diff -- <이 plan 이 고친 파일> | grep "^+" | grep -nE '\?\?|\?\.' || true` 가 0줄이고,
      `... | grep -vE '^\+\s*//' | grep -nE '\?[^;]*:' || true` 후보를 육안 판별해 진성 삼항 0건
  </acceptance_criteria>
  <done>SIDE_1(최대 z=2)에 z=7 이 오면 Error 로그가 남고 응답이 B 로 나간다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLC z_index → 사이클 판정 | 범위 밖 값이 최종 합격 판정을 유발할 수 있다 |
| 공유 조명 컨트롤러 ↔ 4개 SIDE 시퀀스 | 한 시퀀스의 점등/소등이 형제 지그 촬영을 망친다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-15 | Elevation of Privilege | `AddResponseV1Cycle` 범위 밖 z | mitigate | `bIsBeyondRange` 시 최종 판정 금지 + Error 로그(미측정 PASS 차단) |
| T-73-16 | Denial of Service | `TurnOffOwnShotLights` 형제 채널 소등 | mitigate | non-Idle 형제 채널 제외 + 보류 로그 |
| T-73-19 | Tampering | `ApplyShotLightsInternal` 이 남의 채널을 덮어씀 | mitigate | `CollectOwnedChannelScope` 밖 채널 미접촉(점등/소등 동일 기준) |
| T-73-17 | Repudiation | 조명 세팅 실패가 조용히 삼켜짐 | mitigate | 13채널 AND 집계 → `$PREP_ACK` FAIL + Error 로그 |
| T-73-18 | Tampering | 조명 감지 한계 오해(단선/전원 OFF 미검출) | accept | 주석과 SUMMARY 에 한계 명시, 제어에 과장 전달 금지 |
</threat_model>

<verification>
**[W7] 현 레시피 실측 기준 시나리오 — BAR 채널은 아무도 켜지 않으므로 쓰지 않는다.**
SIDE Shot 8개가 켜는 채널: `LIGHT_BACK`(8개 전부) / `LIGHT_ALIGN_COAX`(SHOT_4 = SIDE_1 만).

⚠ **로그 표기에 `LIGHT_` 접두사가 없다.** `LightHandler.cs:234` 가 groupName **값**을 찍는다
(`LIGHT_BACK` 의 값은 `"BACK"`). 실제 출력은 `BACK - Set On : True` 형태이므로
`LIGHT_BACK` 으로 grep 하면 양성 조건은 false-negative, 음성 조건은 vacuous 가 된다.

1. **양성 조건 포함:** SIDE_2 의 `$PREP`(z=2) 직후 조명 로그에 `BACK - Set On` 줄이 **존재하고**,
   `ALIGN_COAX - Set On` 줄은 **0건**(COAX 는 SIDE_1 소유라 SIDE_2 스코프 밖)
2. SIDE_1 의 `$PREP`(z=2) 직후에는 `BACK - Set On` 과 `ALIGN_COAX - Set On` 줄이 **둘 다** 존재
3. SIDE_1 사이클 종료 소등 시, SIDE_2 가 비-Idle 이면 `BACK` 채널에 대해
   `[CycleLightOff] ... 소등 보류` 로그가 남고 실제 OFF 가 호출되지 않는다(공유 채널 = BACK)
4. `D:/Data/Light/light.ini` 의 `[Controller0] ChannelNames` 에서 `BACK` 을 틀리면
   (그룹명이 아니라 **채널명** — 그룹은 C# 하드코딩) `RebindChannels` 가 group.Count=0 을 만들어
   `ApplyShotLights` 가 false → `$PREP_ACK ... FAIL`
5. Shot 없는 z(예: SIDE_1 z=1)는 `$PREP_ACK ... OK`
6. SIDE_1 최대 z=2 인데 z=7 요청 → B + Error 로그, P/F 없음
7. SIMUL-ON / SIMUL-OFF 두 빌드 에러 0
</verification>

<success_criteria>
- 점등/소등이 같은 채널 수집 기준으로 자기 소유 채널에만 작용
- 자기 채널 안에서는 Enabled=false → OFF 유지(이전 사이클 잔광 방지)
- 조명 실패가 ACK FAIL 로 전달되고, Shot 부재는 OK
- 범위 밖 z 가 최종 P/F 를 만들지 않는다
- [W8] 스코프 밖 잔광 잔여 위험이 SUMMARY "알려진 잔여 위험" 으로 기록됨(73-07 이 73-HUMAN-UAT.md K1 로 이월)
</success_criteria>

<output>
완료 후 `.planning/phases/73-side-4-jig-split/73-05-SUMMARY.md` 를 작성한다.
</output>
