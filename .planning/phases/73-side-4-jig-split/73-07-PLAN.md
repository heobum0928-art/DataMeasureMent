---
phase: 73-side-4-jig-split
plan: 07
type: execute
wave: 4
depends_on: ["73-01", "73-02", "73-03", "73-04", "73-05", "73-06"]
files_modified:
  - WPF_Example/Custom/Define/ID.cs
  - .planning/phases/73-side-4-jig-split/73-HUMAN-UAT.md
  - WPF_Example/VersionDefine.cs
autonomous: false
requirements: [PROTO-73-VERIFICATION]
must_haves:
  truths:
    - "Type 2/3/4/5 가 각각 SIDE_1/2/3/4 만 실행하고 지그별 개별 P/F 를 낸다"
    - "지그 4개 합산 측정 25개 / 공차이탈 7개가 분리 전 baseline 과 일치한다"
    - "규격 위반 $PREP 이 FAIL 로 회신되고 ACK 누락이 0건이다"
    - "Top/Bottom(Type 0/1) 이 회귀 없이 동작한다"
    - "실기 필요 항목이 별도 파일로 분리되어 phase 완료를 막지 않는다"
  artifacts:
    - path: ".planning/phases/73-side-4-jig-split/73-HUMAN-UAT.md"
      provides: "실기 전용 검증 항목 추적"
      contains: "실기 필요"
    - path: "WPF_Example/Custom/Define/ID.cs"
      provides: "레거시 ESequence.Side 정리 확인"
      contains: "Side1 = 4"
  key_links:
    - from: "SIMUL 앱"
      to: "CommunicationTest 클라이언트"
      via: "TCP $PREP/$TEST"
      pattern: "PREP_ACK"
---

<objective>
**SIMUL + TCP 만으로 이 phase 를 완료 판정한다.** 사용자가 당장 실장비를 쓸 수 없으므로,
실기가 필요한 항목은 `73-HUMAN-UAT.md` 로 분리해 추적하고 phase 완료를 막지 않게 한다
(Phase 65 의 `65-HUMAN-UAT.md` 선례).

Purpose: 구조 변경이 측정 결과를 바꾸지 않았음을 증명하고, 남은 위험을 명시적으로 이월.
Output: 검증 결과 + 코드 리뷰 + 실기 UAT 추적 파일 + 버전 갱신.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-side-4-jig-split/73-CONTEXT.md
@.planning/phases/73-side-4-jig-split/73-01-SUMMARY.md
@.planning/phases/73-side-4-jig-split/73-02-SUMMARY.md
@.planning/phases/73-side-4-jig-split/73-03-SUMMARY.md
@.planning/phases/73-side-4-jig-split/73-04-SUMMARY.md
@.planning/phases/73-side-4-jig-split/73-05-SUMMARY.md
@.planning/phases/73-side-4-jig-split/73-06-SUMMARY.md

**절대 규칙:** 실행 중 프로세스를 종료하지 않는다. 빌드 산출물이 잠기면 스크래치 OutDir 로
컴파일만 검증하고 잠김 사실을 보고한다. 빌드 경고 baseline = **12줄(CS0618×10 + CS0162×2)**.
`DatumMeasurement.csproj` 커밋 금지.

**baseline (2026-08-26 09:17 SIDE 자동검사, 분리 전 실측):**

| Shot | 측정 | 이탈 |
|---|---|---|
| 3-1_D1 | 2 | 2 |
| 3_2_D1 (×2) | 2 | 0 |
| 4-2_H5 | 1 | 0 |
| 4-2_C13-14_P1 | 6 | 3 |
| 4-2_F9 | 3 | 0 |
| 4-1_F9 | 3 | 0 |
| 4-1_C13-14 | 6 | 2 |
| **합계** | **25** | **7** |
</context>

<tasks>

<task type="auto">
  <name>Task 1: 정적 전수 대조 + 레거시 ESequence.Side 정리 + 코드 리뷰</name>
  <files>WPF_Example/Custom/Define/ID.cs</files>
  <read_first>
    - 73-01 ~ 73-06 의 SUMMARY 전부
    - .planning/phases/73-side-4-jig-split/73-CONTEXT.md D-73-07 (R1~R6 / M2~M13 전수 목록)
    - WPF_Example/Custom/Define/ID.cs
    - WPF_Example/Custom/Sequence/SequenceHandler.cs (ResolveSequenceName 의 레거시 case)
  </read_first>
  <action>
**(1) R/M 커버리지 전수 대조** — 아래 표를 실제 grep 으로 채워 SUMMARY 에 남긴다.
하나라도 미충족이면 이 task 를 통과시키지 않는다.

| 항목 | 확인 명령 | 기대 |
|---|---|---|
| R1 소등 | `grep -c CollectBusySiblingChannels InspectionSequence.cs` | 2 |
| R1 점등 | `grep -c CollectOwnedChannelScope InspectionSequence.cs` | 4 |
| R2/R3 | `grep -c "_lastPrepZIndex\b" Custom/SystemHandler.cs` | 0 |
| R4 | `grep -c ParamSequenceCount Sequence/SequenceHandler.cs` | 3 |
| R5 | `grep -c "IsSequenceActive(ESequence.Side1)" Custom/Sequence/SequenceHandler.cs` | 3 |
| R6 | `ResolveSequenceName` default 에 `Logging.PrintLog` 존재 | 1 |
| M2 | `grep -c "prepPacket.IsRequestValid = true" Custom/SystemHandler.cs` | 1 |
| M3 | `grep -c TryResolveSequenceNameByType Custom/TcpServer/ResourceMap.cs` | 3 |
| M4 | `grep -c '"FIXTURE_SIDE_1"' InspectionRecipeManager.cs` | 2 |
| M5 | `grep -c "RebuildInspectionActions(ESequence.Side" Custom/Sequence/SequenceHandler.cs` | 4 |
| M6 | `grep -c "^OwnerSequenceName=SIDE$" D:/Data/Recipe/FAI_1/main.ini` | 0 |
| M7 | `grep -c NormalizeModelFolderName InspectionSequence.cs` | >= 4 |
| M8 | `grep -c "커플링 결함" VisionResponsePacket.cs` | 1 |
| M9 | `grep -c "ESequence.Side1, ESequence.Side2" MainView.xaml.cs` | 1 |
| M10 | `grep -c "case VisionRequestType.Prep:" ResourceMap.cs` | 1 |
| M11 | `grep -c "_lastPrepZIndexBySeq.Clear()" Custom/SystemHandler.cs` | 1 |
| M12 | `BuildPrepAckMessage` 의 구분자 개수 | 3 |
| M13 | `grep -c bIsBeyondRange InspectionSequence.cs` | 3 |

**(2) 코딩 규칙 위반 전수 검사** — 이 phase 가 손댄 파일 전부에 대해:

```
git diff <phase73 시작 커밋>..HEAD -- WPF_Example | grep "^+" | grep -E "\?\s*[^:]+\s*:|\?\?|\?\."
```
결과 0줄이어야 한다(문자열 리터럴 안의 `?` 는 수동 판별). C# 8.0 switch expression(`=>` 를 쓴 case)도 0건.

**(3) 레거시 `ESequence.Side` 정리 판단**

`grep -rn "ESequence.Side\b" WPF_Example --include=*.cs` 를 돌린다.
남아 있는 곳이 `ID.cs` 의 enum 선언과 `ResolveSequenceName` 의 case **둘뿐**이면 그대로 둔다
(레거시 식별자로 문서화된 상태). 그 밖에 실동작 경로에 남아 있으면 그 지점을 SIDE_1~4 로 고친다.

`grep -rn "SEQ_SIDE\b" WPF_Example --include=*.cs` 도 같은 기준으로 확인한다
(`NormalizeModelFolderName` 이 `SEQ_SIDE` 를 폴더명으로 쓰는 것은 **정상**이다).

**(4) `gsd-code-reviewer` 실행 — 필수, 세밀하게.** 아래 6항목을 지정해 검토시킨다:
   1. 삼항/`??`/`?.` 축약 잔존 0건 (신규 코드 전수)
   2. `ESequence.Side` → 4종 전환에서 누락된 참조
   3. Datum 소실 위험 경로 — `InspectionRecipeManager` 의 DatumCount 보존 로직이 4분할에서도 유효한지
   4. `.shm` 경로 — `NormalizeModelFolderName` 이 저장/로드 양쪽에 대칭 적용됐는지
   5. 크로스-Z ZIndexA/B 재매김이 완성 index(`max(A,B)`) 계약을 깨지 않는지
   6. `CanRunSequence` 상호배타 — SIDE 4개가 같은 카메라 객체를 공유하므로 참조 동일성 판정에 걸리는지

리뷰 지적사항은 이 task 안에서 수정하고, 수정 후 재빌드한다.

**(5) 빌드 최종 확인** — Debug|x64(SIMUL_MODE ON) / Release|x64(OFF) 양쪽 clean rebuild,
에러 0 / 경고 ≤ 12줄.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement &amp;&amp; git diff --stat &amp;&amp; grep -rn "ESequence.Side\b" WPF_Example --include=*.cs | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - R/M 대조표 18항목 전부 기대값 일치, SUMMARY 에 표로 기록됨
    - phase 73 diff 의 추가 라인에 삼항 `?:` / `??` / `?.` 0건
    - phase 73 diff 의 추가 라인에 switch expression(`case ... =>`) 0건
    - `grep -rn "ESequence.Side\b" WPF_Example --include=*.cs | wc -l` <= 2
    - `gsd-code-reviewer` 실행 결과와 지적사항 처리 내역이 SUMMARY 에 기록됨
    - msbuild Debug|x64 clean rebuild exit 0, 에러 0, 경고 ≤ 12줄
    - msbuild Release|x64 clean rebuild exit 0, 에러 0
    - `git status --porcelain` 에 `WPF_Example/DatumMeasurement.csproj` 가 staged 로 나타나지 않음
  </acceptance_criteria>
  <done>18항목 대조 통과, 코딩 규칙 위반 0건, 코드 리뷰 완료, 두 빌드 구성 통과.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SIMUL + TCP 검증 S1~S9 (이 phase 의 완료 조건)</name>
  <files>(검증 전용 — 코드 변경 없음). 임시 편집 후 원복: D:/Data/Recipe/FAI_1/light.ini</files>
  <read_first>
    - 73-06-SUMMARY.md (테스트 클라이언트 사용법 + 예외 케이스 3종 입력 문자열)
    - .planning/phases/73-side-4-jig-split/73-CONTEXT.md 의 baseline 표(Shot별 측정/이탈)
    - `D:\Data\Recipe\FAI_1\light.ini` (S7 에서 일시적으로 손댔다가 반드시 원복할 파일)
  </read_first>
  <action>
이 task 는 사람 확인 체크포인트다. 실행자는 SIMUL_MODE Debug|x64 빌드를 준비하고
아래 <how-to-verify> 의 S1~S9 절차를 사용자에게 그대로 제시한 뒤 결과를 받는다.
S7 은 `light.ini` 를 일시적으로 훼손하므로, 사용자가 어떤 결과를 보고하든
**원복 확인(백업본과 바이트 동일)까지 마친 뒤에만** 이 task 를 통과 처리한다.
실행자가 결과를 자체 판단으로 PASS 처리하지 않는다.
  </action>
  <what-built>
    SIDE 를 SIDE_1~4 로 분리하고, `$PREP` 에 Type 을 도입했으며, 조명 점등/소등을
    시퀀스 소유 채널로 스코핑했고, 범위 밖 z 방어를 넣었다. 레시피는 마이그레이션됐다.
  </what-built>
  <how-to-verify>
    **준비:** SIMUL_MODE Debug|x64 로 앱 기동. `CameraRole = Side`, `UseProtocolV1 = true`,
    `PcRole = 2`. `CommunicationTest` 를 실행해 접속(site=2).

    ---
    **S1. RUN 회귀 (수동 트리거)**
    UI 에서 SIDE_1~4 각각을 RUN 한다. 측정값이 baseline 표와 같은지 리뷰어 화면에서 확인.
    통과 기준: 4개 시퀀스 합산 측정 25개, `Fail to Start Sequence` 0건.

    **S2~S5. Type 2/3/4/5 개별 자동검사**
    클라이언트에서 대상을 SIDE_1 만 선택해 1회 실행 → SIDE_2 → SIDE_3 → SIDE_4.
    각각에 대해:
    - 그 시퀀스만 실행된다(다른 SIDE 시퀀스 로그에 실행 흔적 없음)
    - 중간 z 응답은 `B`
    - **마지막 z 에서만** `P` 또는 `F` 가 나온다
      (SIDE_1 → z=2 / SIDE_2 → z=3 / SIDE_3 → z=4 / SIDE_4 → z=3)
    - `$PREP_ACK` 이 `PREP_ACK:2,{Type},{z},OK` 형식(필드 4개)

    **S6. 4 Type 연속 ("전체") × 5회 반복**
    - 4지그 전부 검사되고, **합산 측정 25개 / 공차이탈 7개**가 baseline 과 일치
    - Shot 별 측정/이탈 개수도 CONTEXT 표와 일치
    - 5회 전부 완주, `Fail to Start Sequence` 0건
    - 사이클 시간이 구 44초에서 크게 줄었는지 기록(참고용, 통과 기준 아님)

    **S7. 조명 세팅 FAIL 재현 (반드시 원복)**
    SIMUL 에는 실제 조명 컨트롤러가 없어 FAIL 이 자연 발생하지 않는다.
    `light.ini` 의 조명 그룹명을 일부러 틀리게 만들면 재현된다 — 그게 정확히 FAIL 조건이다
    (`LightHandler.SetOnOff` 가 false 를 돌려주는 두 경우 중 "그룹명 부재").
    1. `light.ini` 를 `light.ini.bak_s7` 로 복사(원복용)
    2. SIDE_1 이 쓰는 그룹/채널명 하나를 오타로 바꾼다(예: `BAR_1` → `BAR_1X`)
    3. 앱 재기동 후 `$PREP:2,2,2@` 송신 → `PREP_ACK:2,2,2,FAIL` 확인
    4. 로그에 `[LightSet] ... 조명 세팅 실패` Error 확인
    5. **`light.ini` 를 원복**하고 앱 재기동 → `$PREP:2,2,2@` 가 다시 `OK` 인지 확인

    **S8. 프로토콜 예외 케이스 (ACK 누락 0)**
    클라이언트 자유 문자열 송신으로 아래를 보내고 **전부 응답이 오는지** 확인:
    - `$PREP:2,1@` (2필드) → `PREP_ACK ... FAIL`
    - `$PREP:2,4,1,99@` (4필드) → `PREP_ACK ... FAIL`
    - `$PREP:2,,1@` (Type 빈값) → `PREP_ACK ... FAIL`
    - `$PREP:2,9,0@` (미지 Type) → `PREP_ACK:2,9,0,FAIL`
    무응답(타임아웃)이 하나라도 나오면 실패.

    **S9. 범위 밖 z (M13) + Top/Bottom 회귀**
    - `$PREP:2,2,7@` → `PREP_ACK:2,2,7,OK` (Shot 없는 z 는 OK)
    - 이어서 `$TEST:2,2,1@` → 응답이 `B`, **최종 P/F 가 나오지 않음**,
      로그에 `[SEQ] SIDE_1 범위 밖 z_index 수신` Error
    - `CameraRole = TopBottom`, `PcRole = 1` 로 바꿔 재기동 후 Type 0 / Type 1 자동검사 1회씩:
      TOP/BOTTOM 이 분리 전과 동일하게 동작(측정 개수 동일, P/F 정상)
    - **점등 스코핑 확인:** Type 0(TOP) `$PREP` 직후 조명 로그에 `LIGHT_BACK` 설정 호출이 없고,
      Type 1(BOTTOM) `$PREP` 직후 `LIGHT_RING*` 설정 호출이 없다
  </how-to-verify>
  <acceptance_criteria>
    - S1: SIDE_1~4 RUN 합산 측정 25개, 시작 실패 0건
    - S2~S5: 각 Type 이 해당 시퀀스만 실행, 중간 z = B, 마지막 z(2/3/4/3)에서만 P/F
    - S2~S5: `$PREP_ACK` 필드 4개, Type echo 일치
    - S6: 5회 반복 전부 완주, 매회 합산 측정 25 / 이탈 7, Shot별 개수도 CONTEXT 표 일치
    - S7: 그룹명 오타 시 `PREP_ACK ... FAIL` + `[LightSet]` Error 로그 확인
    - S7 원복: `light.ini` 가 `light.ini.bak_s7` 와 **바이트 동일**하고, 원복 후 `$PREP` 이 `OK`
    - S8: 4개 예외 입력 전부 응답 수신(타임아웃 0건), 전부 FAIL
    - S9: 범위 밖 z 에서 응답 `B` + `범위 밖 z_index 수신` Error, 최종 P/F 없음
    - S9: Type 0/1 회귀 통과, 점등 스코핑 로그 확인
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement &amp;&amp; ls -la "D:/Data/Recipe/FAI_1/light.ini"</automated>
  </verify>
  <done>S1~S9 결과를 사용자가 보고했고, light.ini 가 원복되었으며, 실패 항목은 SUMMARY 에 기록되었다.</done>
  <resume-signal>각 항목의 PASS/FAIL 을 알려주거나 "전부 통과" 를 입력한다</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 실기 UAT 추적 파일 작성 + 버전 갱신 + ROADMAP 반영</name>
  <files>.planning/phases/73-side-4-jig-split/73-HUMAN-UAT.md, WPF_Example/VersionDefine.cs, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/65-*/65-HUMAN-UAT.md (형식 선례)
    - WPF_Example/VersionDefine.cs (VERSION / BUILD_DATE / [Version] 이력 블록)
    - .planning/ROADMAP.md Phase 73 섹션(:1055~1085)
    - .planning/phases/73-side-4-jig-split/73-CONTEXT.md D-73-06 의 "조명 예열 손실의 실제 영향 — 미측정"
  </read_first>
  <action>
**(1) `.planning/phases/73-side-4-jig-split/73-HUMAN-UAT.md` 신규 작성**

SIMUL 로 확인 불가능해 **실기가 필요한 항목만** 담는다. 이 파일은 phase 완료를 **막지 않는다**.

포함할 항목:

| # | 항목 | 왜 SIMUL 로 안 되나 | 통과 기준 |
|---|---|---|---|
| H1 | 조명 예열 효과 정량화 | SIMUL 은 `VirtualLightController` — 실제 LED 안정화 시간이 없다. D-73-06 이 "예열 유무 산포 비교 실측 없음"으로 명시 | `$PREP`~`$TEST` 간격 장/단 각 20회 반복 측정, 측정 산포 비교 수치 기록 |
| H2 | 실제 Z축 이동 타이밍 | SIMUL 은 축을 움직이지 않는다 | 각 지그 z 전환에서 이미지 흔들림/미도달 0건 |
| H3 | PLC 실제 송신 순서 | 제어 공정(Side#1 → 피커 이송 → 바텀얼라인 → Side#2)이 코드로 강제되지 않는다 | 실제 순서가 D-73-08 회신과 일치, SIDE 동시 실행 창 0 |
| H4 | 크로스-Z 실촬영 | SIMUL 은 저장 이미지 경로를 쓴다(커밋 `8d6982c` 로 role B 결함 수정 직후라 기준선이 막 잡힘) | 4개 Datum 전부 실촬영 2장으로 검출 성공 |
| H5 | 조명 실패 실물 검출 한계 | `SetOnOff` 는 케이블 단선/전원 OFF/LED 고장을 못 잡는다(D-73-08) | 제어팀에 이 한계를 명시 전달하고 확인받기 |
| H6 | 지그별 배출 연동 | 개별 P/F 를 제어가 실제로 지그 배출에 쓰는지 | 지그 1개만 NG 인 자재에서 해당 지그만 배출 |

각 항목에 "상태: PENDING(실기 대기)" 를 적고, 확인 시점에 갱신하도록 안내한다.
**H1 은 근거를 부풀리지 말 것** — D-73-06 이 "예산 유무 산포 비교 실측이 없다. 근거를 부풀리지 말 것"
이라고 명시했다. 실측 전까지는 "영향 미측정" 으로 적는다.

**(2) `VersionDefine.cs` 갱신** — `VERSION` / `BUILD_DATE` 를 올리고 `[Version]` 이력을 쌓는다.
`AssemblyVersion` 은 `const` 여야 한다(CS0182). 이력 문구 예:
"SIDE 시퀀스를 지그 4개(SIDE_1~4)로 분리 — Type 2~5 라우팅, 지그별 z 0부터 독립 시작,
지그별 개별 P/F. `$PREP` 에 Type 필드 도입(3필드) + `$PREP_ACK` Type echo,
FAIL=조명 세팅 실패 전용. 조명 점등/소등 시퀀스 소유 채널 스코핑. 범위 밖 z 미측정 PASS 차단."

**(3) `ROADMAP.md` Phase 73 섹션 갱신** — `Plans: (plan 단계 진행 중)` 를 실제 목록으로 교체하고,
상태와 SIMUL 검증 결과 / 실기 이월 항목을 한 줄로 기록한다.

**(4) 커밋** — `DatumMeasurement.csproj` 는 제외한다.
CommunicationTest 변경은 **별도 저장소에서 별도 커밋**한다.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement &amp;&amp; ls .planning/phases/73-side-4-jig-split/73-HUMAN-UAT.md &amp;&amp; grep -c "PENDING" .planning/phases/73-side-4-jig-split/73-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/73-side-4-jig-split/73-HUMAN-UAT.md` 존재, H1~H6 6항목 전부 기재
    - 각 항목에 "왜 SIMUL 로 안 되나" 사유와 통과 기준이 있다
    - H1 항목에 "영향 미측정" 취지의 문장이 있고, 예열 효과를 단정하는 표현이 없다
    - `grep -c "PENDING" 73-HUMAN-UAT.md` >= 6
    - `WPF_Example/VersionDefine.cs` 의 `VERSION` 값이 이전 커밋과 다르다
    - `.planning/ROADMAP.md` 의 Phase 73 섹션에 `Plans: (plan 단계 진행 중)` 문자열이 없다
    - `git log -1 --stat` 에 `DatumMeasurement.csproj` 가 없다
  </acceptance_criteria>
  <done>실기 항목이 별도 파일로 추적되고, 버전/ROADMAP 이 갱신되며, 커밋이 완료된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SIMUL 검증 결과 → 실장비 배포 | SIMUL 통과가 실기 통과를 보장하지 않는다 |
| 테스트 절차 → 운영 설정 파일(`light.ini`) | 테스트가 설정을 망가뜨린 채 끝날 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-22 | Tampering | S7 의 `light.ini` 오타 주입 | mitigate | 백업 후 주입, 원복 바이트 동일 확인을 acceptance_criteria 에 포함 |
| T-73-23 | Repudiation | SIMUL 통과를 실기 검증으로 오인 | mitigate | `73-HUMAN-UAT.md` 로 실기 항목 6건 명시 분리, H1 근거 과장 금지 |
| T-73-24 | Elevation of Privilege | 미검증 상태로 현장 배포 | accept | 배포 판단은 사용자 권한. 이월 항목을 문서로 명시 전달 |
</threat_model>

<verification>
1. 정적 R/M 대조 18항목 통과
2. SIMUL S1~S9 통과
3. 실기 항목 6건이 `73-HUMAN-UAT.md` 로 분리 추적
4. 두 빌드 구성 에러 0
</verification>

<success_criteria>
- Type 2/3/4/5 → SIDE_1/2/3/4 개별 실행 + 개별 P/F
- 합산 측정 25개 / 이탈 7개 baseline 일치 (5회 반복)
- ACK 누락 0건, 규격 위반은 FAIL 회신
- Top/Bottom 회귀 0
- `light.ini` 원복 확인
- 실기 항목이 phase 완료를 막지 않는다
</success_criteria>

<output>
완료 후 `.planning/phases/73-side-4-jig-split/73-07-SUMMARY.md` 를 작성한다.
</output>
