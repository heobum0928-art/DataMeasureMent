# 레시피 TOP/BOTTOM 복원 기록 (2026-08-27)

**Phase 73 범위 밖**이지만 검증 중 드러나 함께 처리했다. 경위를 남긴다.

## 발단

Phase 73 완료 후 TOP/BOTTOM 확인 중 이미지가 안 보였다. 추적해보니 이미지 경로뿐 아니라
**설정 전체가 초기값으로 리셋**돼 있었다.

## 실제 상태 (발견 당시)

| 항목 | 08-14 백업 | 발견 당시 |
|---|---|---|
| TOP/BOTTOM Shot 수 | **25** | 19 |
| Shot ZIndex | TOP 1~3 / BOTTOM 1~23 | **전부 0** |
| `PixelResolution` | 0.002398 | **1** (mm 아닌 픽셀로 측정됨) |
| `Exposure` | 10000 | **0** |
| `RingLight_1~6` | 255… | **0** |
| `SequenceName` | BOTTOM | **(빈값)** |
| `IsPatternAlignEnabled` | True | **False** |
| `RefMatch*` / `PatternRoi_*` | 실제 좌표 | **0** |
| 이미지 경로 | `D:\Data\Image\OfflineInspect\` | `C:\Info\Doc\...`(폴더 부재) |

Datum 2개에서 각 100여 개 키 차이, Shot/측정 포함 **2,091개 값**이 어긋나 있었다.

## 원인

**구 버전 레시피(6월 23일 계열)가 유입됐다.** 사용자 확인: *"오프라인 PC에 배포하려고
C드라이브로 했던 적이 있다."*

근거:
- 7월에 추가된 Shot 7개(`G5-6`, `G7-8`, `G11-12`, `I5~I8`)가 없고, 6월에만 있던 `SHOT_G5` 존재
- 08-14 에 있던 키 일부가 아예 없음(`MirrorX`, `Erosion`, `FindAngleExtentDeg` 등)
- 이 저장소는 **INI 키가 없으면 로드 시 0 으로 채워진다** — 키 빠진 레시피를 열고 저장하면 굳는다

시점별 Shot 수: `06-23: 19` → `07-23: 25` → `08-14: 25` → **발견 당시 19**

## 증상

`z=0` 은 기준점 전용 자리라 측정을 건너뛴다. 모든 Shot 이 z=0 이면 **측정 차례가 오지 않아**
검사가 계속 `B`(진행중)로만 끝난다. 앱은 규칙대로 동작했고 데이터가 규칙에 안 맞았다.

## 복원 방법

`main.ini.reanchor_bak_20260819152425`(08-14, 정상 TOP/BOTTOM 보유)에서 이식.

**⚠ 섹션 번호가 밀려 있어 번호로 복사하면 안 됐다** — 08-14 의 `SHOT_7` 은 `E5` 인데
당시 `SHOT_7` 은 `E1-4` 였다. **`ShotName` 으로 짝을 맞춰** 11개 오배치를 피했다.

**최종 재구성**: TOP(3) + BOTTOM(22) = 08-14 / SIDE_1~4(8) = 현재(지그분리 결과 유지)
→ SHOT **33개**, 측정 **125개**, `[SHOTS]Count=33`, 번호 0~32 연속.

## 검증 (2026-08-27)

| | 결과 |
|---|---|
| TOP (Type 0) | z=0~3, 측정 **61** / 이탈 0 → **P** |
| BOTTOM (Type 1) | z=0~24, Shot **23** / 측정 **41** / 이탈 0 → **P** |
| SIDE 전체 | Shot **8** / 측정 **25** / 이탈 **7** → baseline **완전 일치** |

되살아난 Shot 7개 전부 정상 동작. SIDE 는 재구성에도 영향 없음.

## 함께 드러난 것

**BOTTOM `SHOT_E5` 티칭 문제** — 크로스-Z 측정이 회차마다 갈린다(F/P). 사용자 확인 결과
**티칭 문제**. 레시피 설정은 08-14 와 완전 동일하고 크로스-Z 조합 로직도 정상 작동
(`DualImageEdgeDistance×2`). 재티칭 필요 — `73-HUMAN-UAT.md` PENDING.

**BOTTOM 실제 z 상한은 23 이 아니라 24** — `SHOT_E5` 크로스-Z 짝이 `ZIndexA=23 / ZIndexB=24`.
Shot 최대 z 만 보면 틀린다. 테스트 클라이언트 `ResolveMaxZIndexByType` 에 이 사실을 주석으로 명시.

## 백업 (5단계)

```
main.ini.bak_260827_before_planA     ← 재구성 직전
main.ini.bak_260826_before_graftC    ← 1차 이식 직전
main.ini.bak_260826_imgpath          ← 경로 변경 직전
main.ini.bak_260826_phase73          ← 지그 분리 직전
D:\Backup\FAI_1_backup_before_phase73_260826\   ← 착수 전 전체(.shm 31개)
```

## 재발 방지 (미착수)

1. **레시피 이상 감지 부재** — Shot 이 전부 z=0 이어도, `PixelResolution=1` 이어도,
   검사가 계속 B 로만 끝나도 앱은 조용했다. 클라이언트가 먼저 알려줬다.
2. **키 누락 → 0 클로버** — 키가 빠진 레시피를 열고 저장하면 값이 0 으로 굳는다.
   외부 레시피를 가져올 때는 **열기 전 백업** 필요.
3. **이미지 경로가 절대경로로 INI 에 박힌다** — 배포 때마다 재작업이 발생한다.
   상대경로 또는 `ImageSavePath` 기준으로 바꾸는 것을 별도 과제로 검토.
