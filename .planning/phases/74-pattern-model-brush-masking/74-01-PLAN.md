---
phase: 74-pattern-model-brush-masking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Setting/SystemSetting.cs
  - WPF_Example/Halcon/Services/PatternMaskService.cs
  - WPF_Example/Halcon/Algorithms/PatternMatchService.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [D-74-02, D-74-03]

must_haves:
  truths:
    - "옵션이 꺼져 있으면 마스크 파일이 있어도 모델 생성 경로가 기존과 동일하다"
    - "마스크 파일은 모델 파일(.shm/.ncm)과 같은 폴더에 같은 이름으로 떨어진다"
    - "마스크가 있으면 패턴 ROI 에서 마스크가 빠진 영역으로 모델이 만들어진다"
    - "마스크가 ROI 를 거의 다 덮으면 사람이 읽을 수 있는 오류로 실패한다"
  artifacts:
    - path: "WPF_Example/Halcon/Services/PatternMaskService.cs"
      provides: "마스크 경로 도출 + 저장/로드/삭제 + 옵션 게이트"
      contains: "EXTENSION_PATTERN_MASK"
    - path: "WPF_Example/Setting/SystemSetting.cs"
      provides: "UsePatternBrushMask 토글 (기본 false)"
      contains: "UsePatternBrushMask"
    - path: "WPF_Example/Halcon/Algorithms/PatternMatchService.cs"
      provides: "TryCreateModel 안에서 ROI - 마스크 Difference"
      contains: "PatternMaskService.TryLoadMask"
  key_links:
    - from: "WPF_Example/Halcon/Algorithms/PatternMatchService.cs"
      to: "PatternMaskService.TryLoadMask"
      via: "TryCreateModel 의 GenRectangle2 와 ReduceDomain 사이"
      pattern: "PatternMaskService\\.TryLoadMask\\(modelPath"
    - from: "WPF_Example/Halcon/Services/PatternMaskService.cs"
      to: "SystemSetting.Handle.UsePatternBrushMask"
      via: "TryLoadMask 첫 게이트"
      pattern: "SystemSetting\\.Handle\\.UsePatternBrushMask"
---

<objective>
패턴 모델 생성 경로에 **브러시 마스크 반영 코어**를 넣는다. UI 는 이 plan 범위 밖이다.

Purpose: D-74-03 대로 `PatternMatchService.TryCreateModel` **한 곳만** 고쳐서 Align 과 Datum 양쪽에 자동 적용시킨다. D-74-02 대로 마스크는 모델 파일 옆 HALCON region 파일에 저장한다.
Output: `PatternMaskService.cs`(신규), `SystemSetting.UsePatternBrushMask` 토글, `TryCreateModel` 의 `Difference` 삽입.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-pattern-model-brush-masking/74-CONTEXT.md
</context>

<coding_rules>
- **삼항 `?:` 금지** → `if/else`. **`??` `?.` 금지** → 명시 분기.
- **전통 `switch` 만**(C# 8.0 switch expression 금지). **C# 7.2 문법만.**
- 긴 조건은 이름 있는 `bool` 로 선추출. 예: `bool bEnabled = ...; if (bEnabled == false)`.
- 헝가리언: `b`(bool) `n`(int) `sz`(string) `d`(double) `hv`(HTuple).
- 중괄호 스타일: `PatternMatchService.cs` 와 신규 `Halcon/Services/*.cs` = **Allman**, `SystemSetting.cs` = **K&R**.
- 날짜 주석(`//YYMMDD hbk`) 새로 달지 않는다. 비자명한 "왜"만 최소로.
- `HImage` / `HObject` 는 반드시 Dispose. `finally` 에서 `try { x.Dispose(); } catch { }`.
- **실행 중 프로세스 종료 절대 금지.** 잠기면 스크래치 OutDir 로 컴파일만 검증.
- **`WPF_Example/DatumMeasurement.csproj` 는 커밋 금지** — 수정만 하고 unstaged 로 남긴다.
</coding_rules>

<interfaces>
`WPF_Example/Halcon/Algorithms/PatternMatchService.cs:134` 수정 대상 단일 진입점:

```csharp
public bool TryCreateModel(
    HImage templateImage,
    double roiRow, double roiCol, double roiPhi,
    double roiLen1, double roiLen2,
    string engine,
    double angleExtentDeg,
    string modelPath,
    out string error)
```

같은 파일 156~168줄 현재 코드(이 사이에 삽입):

```csharp
HObject rect = null;
HObject reducedImage = null;
HTuple modelId = null;

try
{
    double angleExtentRad = angleExtentDeg * Math.PI / 180.0;
    double angleStartRad = -angleExtentRad / 2.0;

    // Step 1: template ROI 생성 -> reduce_domain
    HOperatorSet.GenRectangle2(out rect, roiRow, roiCol, roiPhi, roiLen1, roiLen2);
    HOperatorSet.ReduceDomain(templateImage, rect, out reducedImage);
```

HALCON .NET 시그니처 (24.11 실측 확인):

```csharp
HOperatorSet.Difference(HObject Region, HObject Sub, out HObject RegionDifference);
HOperatorSet.AreaCenter(HObject Regions, out HTuple Area, out HTuple Row, out HTuple Column);
HOperatorSet.CountObj(HObject Objects, out HTuple Number);
HOperatorSet.ReadRegion(out HObject Region, HTuple FileName);
HOperatorSet.WriteRegion(HObject Region, HTuple FileName);
```

`write_region` 은 확장자로 포맷을 정한다. `.hobj` = HALCON Iconic Object(기본/권장), `.reg` = HALCON 12 이전 호환 legacy.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SystemSetting 에 UsePatternBrushMask 토글 추가 (기본 false)</name>
  <files>WPF_Example/Setting/SystemSetting.cs</files>

  <read_first>
    - `WPF_Example/Setting/SystemSetting.cs` 167~200줄 — `System|Enviroment` 카테고리 블록
    - `WPF_Example/Setting/SystemSetting.cs` 의 `public void Load()` 안 `case "Boolean"` 분기 — INI 키가 없으면 false 로 로드되는 것 확인
  </read_first>

  <action>
`public bool DisableViewerDuringAutoInspect { get; set; } = false;` **바로 아래**, `OriginImageFormat` 주석 블록 **위**에 그대로 추가한다.

```csharp
        // Phase 74 — 패턴 모델(.shm/.ncm) 생성 시 브러시 마스크를 적용할지 여부(D-74-02/03).
        //  기본 false = 기존 경로 그대로(회귀 0). false 인 동안에는 마스크 파일이 디스크에 남아 있어도
        //  PatternMaskService.TryLoadMask 가 파일 존재 여부를 보지도 않고 즉시 false 를 돌려준다.
        //  INI 키 누락 시 Load 의 ToBool 기본값이 false 라 기존 설치본은 자동으로 기존 동작을 유지한다.
        [Category("System|Enviroment")]
        public bool UsePatternBrushMask { get; set; } = false;
```

`[Category]` 는 바로 다음 멤버 1개에만 적용된다(이 저장소 기확인). 위 블록은 자체 Category 를 들고 있으므로 아래 `OriginImageFormat` 그룹에 영향이 없다.

**브러시 크기를 int 로 여기 넣지 말 것.** 이 저장소는 INI 키 누락 시 int 를 0 으로 클로버하는 알려진 함정이 있다. 브러시 크기는 Plan 03 ViewModel 의 세션 상태로만 둔다.
  </action>

  <verify>
    <automated>cd /c/Info/Project/DataMeasurement; grep -c "public bool UsePatternBrushMask { get; set; } = false;" WPF_Example/Setting/SystemSetting.cs</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "public bool UsePatternBrushMask { get; set; } = false;" WPF_Example/Setting/SystemSetting.cs` 가 정확히 `1`
    - `grep -c "UsePatternBrushMask" WPF_Example/Setting/SystemSetting.cs` 가 정확히 `1`
    - `grep -n -B1 "public bool UsePatternBrushMask" WPF_Example/Setting/SystemSetting.cs` 출력에 `[Category("System|Enviroment")]` 포함
    - `grep -c "OriginImageFormat" WPF_Example/Setting/SystemSetting.cs` 가 편집 전 실측값과 동일 (편집 전에 먼저 실측해 SUMMARY 에 기록)
  </acceptance_criteria>

  <done>`SystemSetting.Handle.UsePatternBrushMask` 가 존재하고 기본값 false 이며 `System|Enviroment` 카테고리에 속한다.</done>
</task>

<task type="auto">
  <name>Task 2: PatternMaskService 신규 — 마스크 경로 도출 + 저장/로드/삭제</name>
  <files>WPF_Example/Halcon/Services/PatternMaskService.cs, WPF_Example/DatumMeasurement.csproj</files>

  <read_first>
    - `WPF_Example/Halcon/Services/TeachingStorageService.cs` 머리 ~30줄 — `namespace ReringProject.Halcon.Services` 와 Allman 스타일
    - `WPF_Example/Utility/RecipeFileHelper.cs` **102~114줄** `GetPatternModelFilePath` — **112줄에서 `Directory.CreateDirectory` 로 없는 폴더를 새로 만든다.** 이번 서비스가 절대 흉내내면 안 되는 부분
    - `WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` **2250~2270줄** `NormalizeModelFolderName` — Phase 73 이 `.shm` 을 조용히 못 찾을 뻔했던 방어 코드
    - `WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs` **77~127줄** `BuildShmPath` / `GetShmPath` — Align 은 `RecipeFiles` 헬퍼를 쓰지 않는다. 그래서 마스크 경로는 "확정된 modelPath 문자열"에서 파생해야 양쪽 모두 커버된다
    - `WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs` **265~300줄** — 이 저장소의 `HObject` 생성/Dispose 관습
  </read_first>

  <action>
신규 파일 `WPF_Example/Halcon/Services/PatternMaskService.cs`. **Allman**, `namespace ReringProject.Halcon.Services`, `public static class PatternMaskService`.

핵심 설계(반드시 이대로):
- 마스크 경로는 **호출부가 이미 정규 헬퍼로 산출해 넘긴 `modelPath` 문자열에서만 파생**한다. 폴더 규약을 새로 만들지 않는다. 그래서 Datum(`GetPatternModelFilePath`) 이든 Align(`BuildShmPath`) 이든 무조건 모델과 같은 폴더에 떨어진다.
- 확장자 `.mask.hobj`.
- `Path.GetFileNameWithoutExtension` 을 쓰므로 `X.shm` 과 `X.ncm` 이 같은 마스크 `X.mask.hobj` 를 공유한다. 마스크는 ROI 에 속하지 엔진에 속하지 않으므로 이게 맞다.
- `ResolveMaskPath` / `TryLoadMask` 는 **디렉터리를 절대 만들지 않는다**. `Directory.CreateDirectory` 는 `TrySaveMask` 안에서만 허용.

using: `System`, `System.IO`, `HalconDotNet`, `ReringProject.Define`, `ReringProject.Setting`, `ReringProject.Utility`.

상수:

```csharp
        // HALCON write_region 기본 포맷(HOBJ). '.reg' 는 HALCON 12 이전 호환용 legacy 라 쓰지 않는다.
        public const string EXTENSION_PATTERN_MASK = ".mask.hobj";
```

`public static string ResolveMaskPath(string szModelPath)`

```csharp
            bool bEmpty = string.IsNullOrEmpty(szModelPath);
            if (bEmpty == true)
            {
                return null;
            }
            string szDir = Path.GetDirectoryName(szModelPath);
            string szBase = Path.GetFileNameWithoutExtension(szModelPath);
            bool bBadPath = string.IsNullOrEmpty(szDir) || string.IsNullOrEmpty(szBase);
            if (bBadPath == true)
            {
                return null;
            }
            return Path.Combine(szDir, szBase + EXTENSION_PATTERN_MASK);
```

`public static bool IsMaskEnabled()` → `return SystemSetting.Handle.UsePatternBrushMask;`

`public static bool HasMask(string szModelPath)` — `ResolveMaskPath` 후 `File.Exists`. **옵션 토글을 보지 않는다**(마스크 있음/없음 표시는 토글과 무관해야 사용자가 상태를 안다). 경로 null 이면 false.

`public static bool TryLoadMask(string szModelPath, out HObject maskRegion)` — **첫 문장이 옵션 게이트여야 한다.**

```csharp
            maskRegion = null;

            // 게이트 1: 옵션 OFF 면 파일 존재 여부조차 보지 않고 즉시 false.
            //  이 분기가 "토글 OFF = 기존 경로 그대로" 를 보장한다.
            bool bEnabled = SystemSetting.Handle.UsePatternBrushMask;
            if (bEnabled == false)
            {
                return false;
            }

            string szMaskPath = ResolveMaskPath(szModelPath);
            if (string.IsNullOrEmpty(szMaskPath))
            {
                return false;
            }
            bool bExists = File.Exists(szMaskPath);
            if (bExists == false)
            {
                return false;
            }

            try
            {
                HObject loaded;
                HOperatorSet.ReadRegion(out loaded, szMaskPath);

                HTuple hvCount;
                HOperatorSet.CountObj(loaded, out hvCount);
                bool bEmptyRegion = true;
                if (hvCount.Length > 0)
                {
                    if (hvCount[0].I > 0)
                    {
                        bEmptyRegion = false;
                    }
                }
                if (bEmptyRegion == true)
                {
                    try { loaded.Dispose(); } catch { }
                    return false;
                }

                maskRegion = loaded;
                Logging.PrintLog((int)ELogType.Trace, "[PatternMask] 마스크 로드: {0}", szMaskPath);
                return true;
            }
            catch (Exception ex)
            {
                // 마스크를 못 읽으면 마스크 없이 진행한다 — 모델 생성 자체를 막지 않는다.
                Logging.PrintErrLog((int)ELogType.Error, "[PatternMask] 마스크 로드 실패(마스크 없이 진행): " + szMaskPath + " — " + ex.Message);
                maskRegion = null;
                return false;
            }
```

`public static bool TrySaveMask(string szModelPath, HObject maskRegion, out string szError)`

```csharp
            szError = null;
            string szMaskPath = ResolveMaskPath(szModelPath);
            if (string.IsNullOrEmpty(szMaskPath))
            {
                szError = "마스크 경로 산출 실패 (modelPath 확인)";
                return false;
            }
            if (maskRegion == null)
            {
                szError = "maskRegion is null";
                return false;
            }
            try
            {
                // 여기서만 CreateDirectory 를 허용한다. 이 폴더는 모델 경로 헬퍼가 이미 만든 '모델 폴더'이며
                //  새 규약 폴더를 만드는 것이 아니다(Phase 73 의 조용한 모델 미탐지 사고 재발 방지).
                string szDir = Path.GetDirectoryName(szMaskPath);
                if (Directory.Exists(szDir) == false)
                {
                    Directory.CreateDirectory(szDir);
                }
                HOperatorSet.WriteRegion(maskRegion, szMaskPath);
                Logging.PrintLog((int)ELogType.Trace, "[PatternMask] 마스크 저장: {0}", szMaskPath);
                return true;
            }
            catch (Exception ex)
            {
                szError = ex.Message;
                Logging.PrintErrLog((int)ELogType.Error, "[PatternMask] 마스크 저장 실패: " + szMaskPath + " — " + ex.Message);
                return false;
            }
```

`public static bool DeleteMask(string szModelPath)` — `ResolveMaskPath` null 이면 false. `File.Exists` false 면 조용히 false. 있으면 `File.Delete` + Trace 로그 + true. 예외는 `Logging.PrintErrLog` 후 false. 고아 마스크 정리 진입점이며 Plan 03 의 [마스크 초기화] 버튼이 호출한다.

마지막으로 `WPF_Example/DatumMeasurement.csproj` 의 `TeachingStorageService.cs` Compile 항목 바로 다음 줄에 `PatternMaskService.cs` Compile 항목을 같은 형식으로 추가한다(경로는 `Halcon\Services\PatternMaskService.cs`). csproj 는 수정만 하고 **커밋하지 않는다**.
  </action>

  <verify>
    <automated>cd /c/Info/Project/DataMeasurement; grep -c "public static" WPF_Example/Halcon/Services/PatternMaskService.cs; grep -F -c 'PatternMaskService.cs' WPF_Example/DatumMeasurement.csproj</automated>
  </verify>

  <acceptance_criteria>
    - `WPF_Example/Halcon/Services/PatternMaskService.cs` 존재
    - 아래 6개 시그니처가 각각 정확히 1회: `public static string ResolveMaskPath(string szModelPath)` / `public static bool IsMaskEnabled()` / `public static bool HasMask(string szModelPath)` / `public static bool TryLoadMask(string szModelPath, out HObject maskRegion)` / `public static bool TrySaveMask(string szModelPath, HObject maskRegion, out string szError)` / `public static bool DeleteMask(string szModelPath)`
    - `grep -c 'EXTENSION_PATTERN_MASK = ".mask.hobj";' WPF_Example/Halcon/Services/PatternMaskService.cs` 가 `1`
    - **부작용 0**: `grep -c "Directory.CreateDirectory" WPF_Example/Halcon/Services/PatternMaskService.cs` 가 정확히 `1`, 그리고 `awk '/public static bool TrySaveMask/,/^        }$/' WPF_Example/Halcon/Services/PatternMaskService.cs | grep -c "Directory.CreateDirectory"` 도 `1` (즉 유일한 호출이 TrySaveMask 안에 있다)
    - **게이트 순서**: `awk '/public static bool TryLoadMask/,/^        }$/' WPF_Example/Halcon/Services/PatternMaskService.cs | grep -n "UsePatternBrushMask\|File.Exists"` 출력에서 `UsePatternBrushMask` 줄 번호가 `File.Exists` 줄 번호보다 작다
    - `grep -c " ? " WPF_Example/Halcon/Services/PatternMaskService.cs` 가 `0`
    - `grep -c '??' WPF_Example/Halcon/Services/PatternMaskService.cs` 가 `0`
    - `grep -F -c 'PatternMaskService.cs' WPF_Example/DatumMeasurement.csproj` 가 `1`
    - `git status --porcelain WPF_Example/DatumMeasurement.csproj` 이 ` M` 으로 시작한다(unstaged). `M ` 이면 안 된다
  </acceptance_criteria>

  <done>`PatternMaskService` 가 컴파일 대상에 등록되었고, 마스크 경로가 모델 경로에서만 파생되며, 옵션 OFF 시 로드가 즉시 차단된다.</done>
</task>

<task type="auto">
  <name>Task 3: TryCreateModel 에 마스크 Difference 삽입 + 빌드 검증</name>
  <files>WPF_Example/Halcon/Algorithms/PatternMatchService.cs</files>

  <read_first>
    - `WPF_Example/Halcon/Algorithms/PatternMatchService.cs` **118~236줄** 전체 (`TryCreateModel` 본문 + finally)
    - `WPF_Example/Halcon/Services/PatternMaskService.cs` (Task 2 산출물)
    - `WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs` **370~395줄** — Align 쪽 호출부 2곳(무변경 확인용)
    - `WPF_Example/UI/ContentItem/MainView.xaml.cs` **4055~4090줄** — Datum 쪽 호출부 2곳(무변경 확인용)
  </read_first>

  <action>
`WPF_Example/Halcon/Algorithms/PatternMatchService.cs` 만 고친다. **호출부는 한 줄도 고치지 않는다**(D-74-03 단일 진입점).

(1) `private const int DEFAULT_NCC_NUM_LEVELS = 4;` 바로 아래에 상수 추가:

```csharp
        // 마스크를 뺀 뒤 남은 ROI 면적의 최소 허용치(px). 이보다 작으면 create_*_model 이
        //  난해한 HALCON 예외를 내므로, 그 전에 사람이 읽을 수 있는 오류로 바꿔 돌려준다.
        private const double MIN_MASKED_ROI_AREA_PX = 100.0;
```

(2) `TryCreateModel` 지역변수 선언부(현재 156~158줄)에 한 줄 추가:

```csharp
            HObject rect = null;
            HObject reducedImage = null;
            HObject maskRegion = null;   // 브러시 마스크(D-74-02). 옵션 OFF 면 끝까지 null 이다.
            HTuple modelId = null;
```

(3) `HOperatorSet.GenRectangle2(...)` 와 `HOperatorSet.ReduceDomain(...)` **사이**에 삽입. 두 줄의 순서/인자는 절대 바꾸지 않는다.

```csharp
                // 브러시 마스크 적용(D-74-02/03). TryLoadMask 는 옵션 OFF 또는 마스크 파일 없음이면 false 를
                //  돌려주므로, 이 분기에 들어가지 않으면 아래 ReduceDomain 은 기존과 완전히 동일한 rect 를 받는다(회귀 0).
                bool bMaskLoaded = ReringProject.Halcon.Services.PatternMaskService.TryLoadMask(modelPath, out maskRegion);
                if (bMaskLoaded == true && maskRegion != null)
                {
                    HObject maskedRect = null;
                    HOperatorSet.Difference(rect, maskRegion, out maskedRect);
                    try { rect.Dispose(); } catch { }
                    rect = maskedRect;   // finally 가 그대로 Dispose 하도록 같은 변수에 재대입

                    HTuple hvArea, hvCenterRow, hvCenterCol;
                    HOperatorSet.AreaCenter(rect, out hvArea, out hvCenterRow, out hvCenterCol);
                    double dRemainArea = 0.0;
                    if (hvArea.Length > 0)
                    {
                        dRemainArea = hvArea[0].D;
                    }
                    if (dRemainArea < MIN_MASKED_ROI_AREA_PX)
                    {
                        error = "브러시 마스크가 패턴 ROI 를 거의 전부 덮었습니다(남은 면적 "
                              + dRemainArea.ToString("F0") + "px). 마스크를 지우고 다시 시도하세요.";
                        return false;
                    }
                }
```

(4) `finally` 블록의 `reducedImage` Dispose 줄 바로 다음에 추가:

```csharp
                if (maskRegion != null) { try { maskRegion.Dispose(); } catch { } }
```

(5) `TryCreateModel` 의 `<summary>` XML 주석에 한 줄 추가: `SystemSetting.UsePatternBrushMask 가 true 이고 modelPath 옆에 마스크 파일이 있으면 ROI 에서 마스크를 뺀다(D-74-03).`

**절대 하지 말 것**: `CreateShapeModel`/`CreateNccModel`/`WriteShapeModel`/`WriteNccModel` 호출 변경, `InvalidateCache(modelPath)` 위치 변경, `TryFindRefPose`/`TryFindPose` 변경, 호출부(`AlignShapeMatchService.cs`, `MainView.xaml.cs`) 변경.

**빌드 검증** (실행 중 프로세스 종료 금지 — 반드시 스크래치 OutDir):

```bash
cd /c/Info/Project/DataMeasurement
SCR="/c/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/7fe7f8e3-9e05-4190-9941-3b1cfdde3971/scratchpad"
SCRW=$(cygpath -w "$SCR")
MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"
rm -rf "$SCR/obj74" "$SCR/out74"
"$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 "-p:OutputPath=$SCRW\\out74\\" "-p:IntermediateOutputPath=$SCRW\\obj74\\" -v:m -nologo > "$SCR/build74.log" 2>&1
grep -c ": error " "$SCR/build74.log"
grep -o "warning CS[0-9]*" "$SCR/build74.log" | sort | uniq -c
```

`MSBuild.exe` 는 PATH 에 없다(위 절대경로 사용). `-p:` 대시 형식만 쓴다 — `/p:` 는 MSYS 경로 변환으로 깨진다. `-t:Rebuild` + fresh `IntermediateOutputPath` 필수(증분이면 경고가 0줄로 나와 기준이 무의미).

기준선(2026-08-27 실측, 이 phase 착수 시점): **에러 0줄 / 경고 18줄 = CS0618 x16 + CS0162 x2.**
  </action>

  <verify>
    <automated>cd /c/Info/Project/DataMeasurement; SCR="/c/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/7fe7f8e3-9e05-4190-9941-3b1cfdde3971/scratchpad"; SCRW=$(cygpath -w "$SCR"); MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"; rm -rf "$SCR/obj74" "$SCR/out74"; "$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 "-p:OutputPath=$SCRW\\out74\\" "-p:IntermediateOutputPath=$SCRW\\obj74\\" -v:m -nologo > "$SCR/build74.log" 2>&1; grep -c ": error " "$SCR/build74.log"; grep -o "warning CS[0-9]*" "$SCR/build74.log" | sort | uniq -c</automated>
  </verify>

  <acceptance_criteria>
    - 빌드 로그의 `grep -c ": error " "$SCR/build74.log"` 가 `0`
    - `grep -o "warning CS[0-9]*" "$SCR/build74.log" | sort -u` 결과가 정확히 `warning CS0162` 와 `warning CS0618` **두 종류뿐** (새 경고 코드 종류 0건). 경고 총 줄 수는 18 유지가 기대값이며 다르면 SUMMARY 에 사유를 적는다
    - `grep -c "HOperatorSet.ReduceDomain" WPF_Example/Halcon/Algorithms/PatternMatchService.cs` 가 `2` (편집 전 실측값과 동일)
    - `grep -c "HOperatorSet.GenRectangle2" WPF_Example/Halcon/Algorithms/PatternMatchService.cs` 가 `1`
    - `grep -c "HOperatorSet.CreateShapeModel" WPF_Example/Halcon/Algorithms/PatternMatchService.cs` 가 `1`, `grep -c "HOperatorSet.CreateNccModel" ...` 가 `1`
    - `grep -c "PatternMaskService.TryLoadMask(modelPath, out maskRegion)" WPF_Example/Halcon/Algorithms/PatternMatchService.cs` 가 `1`
    - **삽입 위치**: `grep -n "GenRectangle2\|TryLoadMask\|ReduceDomain(templateImage" WPF_Example/Halcon/Algorithms/PatternMatchService.cs` 출력의 줄 번호가 `GenRectangle2 < TryLoadMask < ReduceDomain(templateImage` 순서다
    - `grep -c "maskRegion.Dispose()" WPF_Example/Halcon/Algorithms/PatternMatchService.cs` 가 `1`
    - **호출부 무변경**: `git diff --stat WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs WPF_Example/UI/ContentItem/MainView.xaml.cs` 출력이 비어 있다
    - `grep -c " ? " WPF_Example/Halcon/Algorithms/PatternMatchService.cs` 값이 편집 전 실측값과 동일(이 파일에는 기존 삼항이 있다 — **줄이지도 늘리지도 말 것**)
  </acceptance_criteria>

  <done>마스크가 없거나 옵션이 꺼져 있으면 `TryCreateModel` 이 기존과 동일한 `rect` 로 `ReduceDomain` 을 호출하고, 마스크가 있으면 `Difference` 결과로 호출한다. 빌드 에러 0, 새 경고 코드 종류 0.</done>
</task>

</tasks>

<verification>
1. 빌드: 에러 0줄, 경고 코드 종류가 CS0618/CS0162 두 종류뿐
2. `PatternMaskService.cs` 안에 `Directory.CreateDirectory` 가 `TrySaveMask` 한 곳뿐
3. `TryLoadMask` 첫 게이트가 `UsePatternBrushMask` (파일 존재 확인보다 앞)
4. `TryCreateModel` 호출부 2개 파일이 git diff 상 무변경
5. csproj 는 unstaged
</verification>

<success_criteria>
- 옵션 OFF 상태에서 `TryCreateModel` 의 실행 경로가 편집 전과 논리적으로 동일하다(마스크 분기 미진입)
- 마스크 파일 경로가 항상 모델 파일과 같은 폴더, 같은 basename 이다
- 마스크가 ROI 를 다 덮으면 한국어 오류 메시지로 실패한다
</success_criteria>

<output>
완료 후 `.planning/phases/74-pattern-model-brush-masking/74-01-SUMMARY.md` 를 작성한다.
편집 전 실측한 grep 기준값(OriginImageFormat 카운트, PatternMatchService 삼항 카운트, 경고 줄 수)을 반드시 기록한다.
</output>
