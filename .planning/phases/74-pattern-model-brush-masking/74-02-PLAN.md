---
phase: 74-pattern-model-brush-masking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
  - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
autonomous: true
requirements: [D-74-01, D-74-04]

must_haves:
  truths:
    - "브러시 모드에서 마우스를 누른 채 끌면 지나간 자리가 마스크에 누적된다"
    - "지우개 모드에서 끌면 누적분에서 그만큼 빠진다"
    - "칠해진 영역이 HALCON 창 안에 반투명으로 보인다"
    - "마우스를 놓으면 칠하기 종료 이벤트가 정확히 1회 발생한다"
    - "브러시 모드가 꺼져 있으면 기존 뷰어 동작(팬/줌/ROI 편집/폴리곤/측정)이 그대로다"
  artifacts:
    - path: "WPF_Example/Halcon/Display/HalconDisplayService.cs"
      provides: "HALCON 창 내부 반투명 region 렌더 + 브러시 커서 원"
      contains: "RenderBrushMask"
    - path: "WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs"
      provides: "브러시 모드 상태머신 + 마우스 이동/놓기 누적 + 공개 API"
      contains: "StartBrushMasking"
  key_links:
    - from: "MainResultViewerControl.ViewerHost_HMouseMove"
      to: "_brushMaskRegion"
      via: "DilationCircle(GenRegionLine) 후 Union2/Difference 누적"
      pattern: "HOperatorSet\\.DilationCircle"
    - from: "MainResultViewerControl.RenderNow"
      to: "HalconDisplayService.RenderBrushMask"
      via: "브러시 모드 활성 시 창 내부 렌더"
      pattern: "RenderBrushMask\\(ViewerHost\\.HalconWindow"
    - from: "MainResultViewerControl.ViewerHost_HMouseUp"
      to: "BrushStrokeCompleted"
      via: "칠하기 1획 종료 시 1회 발생 (D-74-04 자동 재생성 트리거)"
      pattern: "BrushStrokeCompleted"
---

<objective>
뷰어에 **브러시 입력**(D-74-01)과 **HALCON 창 내부 반투명 표시**를 만든다. 파일 저장/모델 재생성은 이 plan 범위 밖이다.

Purpose: `MainResultViewerControl` 은 Datum(MainView) · Bottom Align · Tray Align 세 화면이 공유하는 단일 뷰어다. 여기에 브러시를 넣으면 세 화면이 자동으로 같은 입력을 얻는다.
Output: 뷰어 공개 API(아래 계약), `HalconDisplayService.RenderBrushMask` / `RenderBrushCursor`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-pattern-model-brush-masking/74-CONTEXT.md
</context>

<coding_rules>
- **삼항 `?:` 금지** → `if/else`. **`??` `?.` 금지** → 명시 분기.
- **전통 `switch` 만**. **C# 7.2 문법만.**
- 긴 조건은 이름 있는 `bool` 로 선추출.
- 헝가리언: `b` `n` `sz` `d` `hv`.
- 중괄호 스타일: 두 파일 모두 **Allman**(기존 그대로).
- 날짜 주석 새로 달지 않는다.
- `HObject` 는 반드시 Dispose. 교체할 때는 **새 것을 만든 뒤 옛 것을 Dispose** 한다(순서 뒤집으면 렌더 스레드가 죽은 핸들을 본다).
- 실행 중 프로세스 종료 금지. 빌드는 스크래치 OutDir.
- csproj 는 이 plan 에서 **건드리지 않는다**(신규 파일 없음).
</coding_rules>

<interfaces>
### 이 plan 이 만들어야 하는 공개 계약 (Plan 03/04/05 가 이대로 호출한다 — 이름·시그니처 변경 금지)

```csharp
// MainResultViewerControl (namespace ReringProject.UI)

// 브러시 마스킹 모드 진입/종료
public void StartBrushMasking();
public void StopBrushMasking();
public bool IsBrushMaskingActive { get; }

// 브러시 파라미터 (이미지 픽셀 단위. 화면 배율과 무관)
public double BrushRadiusPx { get; set; }     // 기본 20.0, setter 에서 5.0~200.0 클램프
public bool IsBrushEraseMode { get; set; }    // false=칠하기, true=지우개

// 누적 마스크. 소유권은 컨트롤에 있다.
public HObject CloneBrushMaskRegion();        // 비어 있으면 null. 반환본 Dispose 책임은 호출자
public void SetBrushMaskRegion(HObject region); // 내부에 복사본 보관. null 이면 초기화
public void ClearBrushMask();
public bool HasBrushMask { get; }

// 칠하기 1획 종료(마우스 놓기) 시 1회 발생 — D-74-04 자동 재생성 트리거
public event EventHandler BrushStrokeCompleted;
```

```csharp
// HalconDisplayService (namespace ReringProject.Halcon.Display)
public void RenderBrushMask(HWindow window, HObject maskRegion, string fillColor, string outlineColor);
public void RenderBrushCursor(HWindow window, double row, double col, double radius, string color);
```

### 기존 뷰어 구조 (수정 지점)

- `MainResultViewerControl.xaml.cs:73-74` — `private const int HalconLeftButton = 1; private const int HalconRightButton = 4;`
- `:157-159` — 생성자에서 `ViewerHost.HMouseDown/HMouseMove/HMouseUp` 배선 (이미 3개 다 있음. **새 이벤트를 배선할 필요가 없다**)
- `:1082` `ViewerHost_HMouseDown(object, HMouseEventArgsWPF)` — `var mouseState = GetMouseState(); _lastMouseImagePoint = mouseState.ImagePoint;` 로 시작, 우클릭 블록, 그다음 `if ((mouseState.Buttons & HalconLeftButton) != HalconLeftButton) return;`
- `:1227` `ViewerHost_HMouseMove` — `if (!HasImage) return;` → `var mouseState = GetMouseState(); _lastMouseImagePoint = mouseState.ImagePoint;` → 리사이즈/이동/Rect/Circle/팬 분기
- `:1359` `ViewerHost_HMouseUp` — 리사이즈/이동 마감 분기
- `:431` `IsAnyDrawingModeActive()` — `_isDrawingRect || _isDrawingCircle || 폴리곤 || 캘리브`
- `:841 Render()` / `:852 RenderNow()` — `RenderNow` 마지막이 `RenderEditHandles();`
- `:829 Dispose()` — `DisposeImage(); DisposeAlignContourXld();`
- `:757 SetAlignContourXld(HObject)` / `:769 DisposeAlignContourXld()` — **HObject 소유/교체/Dispose 의 모범 선례. 브러시 마스크도 똑같은 형태로 만든다.**

### HALCON 사실 (24.11 문서 실측 확인 — 추측 아님)

- `set_color` 는 `'#rrggbb'` 뿐 아니라 **`'#rrggbbaa'` RGBA 4쌍**을 받는다. `aa` 가 알파이며 이것이 **반투명 region 표시의 유일한 정식 수단**이다. 표준 색상명(`"red"` 등)에는 알파가 없다.
- 이 저장소 기확인 함정: **SetColor 에 비표준 색상명을 주면 예외 → catch 로 삼켜짐 → 아무것도 안 보인다.** 반드시 try/catch 를 나눠 "채우기 실패해도 외곽선은 그린다" 로 만들 것.
- `HalconDisplayService.Render` 는 전역으로 `window.SetDraw("margin")` 을 걸어 둔다(45줄). `"fill"` 로 바꿨으면 **반드시 `"margin"` 으로 원복**한다.
- 사용 가능(전부 이 저장소에서 이미 쓰거나 24.11 어셈블리에서 확인):
  `HOperatorSet.GenCircle(out HObject, HTuple Row, HTuple Col, HTuple Radius)`
  `HOperatorSet.GenRegionLine(out HObject, HTuple R1, HTuple C1, HTuple R2, HTuple C2)`
  `HOperatorSet.DilationCircle(HObject, out HObject, HTuple Radius)`
  `HOperatorSet.Union2(HObject, HObject, out HObject)`
  `HOperatorSet.Difference(HObject, HObject, out HObject)`
  `HOperatorSet.CopyObj(HObject, out HObject, HTuple Index, HTuple NumObj)`  // Index=1, NumObj=-1
  `HOperatorSet.CountObj(HObject, out HTuple)`
  `HOperatorSet.AreaCenter(HObject, out HTuple, out HTuple, out HTuple)`
  `window.DispObj(HObject)` / `window.DispCircle(double, double, double)`
- 선례: `WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs:265~300` 이 `GenRegionLine` + `DilationCircle` + `Union2` 를 이미 쓴다.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: HalconDisplayService 에 반투명 마스크 + 브러시 커서 렌더 추가</name>
  <files>WPF_Example/Halcon/Display/HalconDisplayService.cs</files>

  <read_first>
    - `WPF_Example/Halcon/Display/HalconDisplayService.cs` **1~60줄** (필드/`Render` 머리 — `window.SetDraw("margin")` 전역 규약 확인)
    - `WPF_Example/Halcon/Display/HalconDisplayService.cs` **627~645줄** `RenderAlignContourXld` — 이 파일의 렌더 메서드 작성 관습(null 가드 + try/catch 삼킴)
    - `WPF_Example/Halcon/Display/HalconDisplayService.cs` **645~672줄** `RenderPolygonPoints` — `window.SetColor` / `SetLineWidth` / `DispLine` 사용 형태
  </read_first>

  <action>
`WPF_Example/Halcon/Display/HalconDisplayService.cs` 의 `RenderAlignContourXld` 메서드 **바로 아래**에 두 메서드를 추가한다.

```csharp
        // 브러시 마스크 표시. HWND airspace 때문에 창 위에 얹은 WPF 요소는 가려지므로 HALCON 창 안에 직접 그린다.
        //  채우기 색은 '#rrggbbaa' RGBA 표기 — HALCON 24.11 set_color 가 공식 지원하는 유일한 반투명 수단이다.
        //  표준 색상명에는 알파가 없어서 이미지가 완전히 가려진다.
        //  ⚠ SetDraw 를 'fill' 로 바꾸므로 반드시 'margin' 으로 원복한다(Render 전역 규약).
        public void RenderBrushMask(HWindow window, HObject maskRegion, string fillColor, string outlineColor)
        {
            if (window == null) return;
            if (maskRegion == null) return;

            bool bFilled = false;
            try
            {
                HOperatorSet.SetDraw(window, "fill");
                HOperatorSet.SetColor(window, fillColor);
                window.DispObj(maskRegion);
                bFilled = true;
            }
            catch
            {
                // '#rrggbbaa' 를 못 받는 환경이면 채우기를 포기한다. 아래 외곽선으로 최소한 위치는 보이게 한다.
            }

            try
            {
                HOperatorSet.SetDraw(window, "margin");
                HOperatorSet.SetColor(window, outlineColor);
                HOperatorSet.SetLineWidth(window, 2);
                window.DispObj(maskRegion);
            }
            catch
            {
                // 표시 실패는 삼킨다(기존 렌더 catch 관습).
            }

            // 채우기 시도 여부와 무관하게 전역 SetDraw 를 'margin' 으로 되돌린다.
            try { HOperatorSet.SetDraw(window, "margin"); } catch { }
            if (bFilled == false)
            {
                // 채우기가 실패했음을 호출부가 알 필요는 없다 — 로그도 남기지 않는다(렌더 루프에서 매 프레임 호출됨).
            }
        }

        // 브러시 크기 미리보기 원(외곽선만). 지금 클릭하면 얼마나 칠해지는지 눈으로 확인시킨다.
        public void RenderBrushCursor(HWindow window, double row, double col, double radius, string color)
        {
            if (window == null) return;
            if (radius <= 0.0) return;
            try
            {
                HOperatorSet.SetDraw(window, "margin");
                HOperatorSet.SetColor(window, color);
                HOperatorSet.SetLineWidth(window, 1);
                window.DispCircle(row, col, radius);
            }
            catch
            {
                // 표시 실패는 삼킨다(기존 렌더 catch 관습).
            }
        }
```

`RenderBrushMask` 안의 `if (bFilled == false)` 빈 블록이 CS0162(도달 불가) 경고를 내지 않는지 확인하고, 경고가 나면 그 블록을 통째로 지운다(`bFilled` 변수도 함께). 경고 코드 종류를 늘리면 안 된다.
  </action>

  <verify>
    <automated>cd /c/Info/Project/DataMeasurement; grep -c "public void RenderBrushMask(HWindow window, HObject maskRegion, string fillColor, string outlineColor)" WPF_Example/Halcon/Display/HalconDisplayService.cs; grep -c "public void RenderBrushCursor(HWindow window, double row, double col, double radius, string color)" WPF_Example/Halcon/Display/HalconDisplayService.cs</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "public void RenderBrushMask(HWindow window, HObject maskRegion, string fillColor, string outlineColor)" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 `1`
    - `grep -c "public void RenderBrushCursor(HWindow window, double row, double col, double radius, string color)" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 `1`
    - `awk '/public void RenderBrushMask/,/^        }$/' WPF_Example/Halcon/Display/HalconDisplayService.cs | grep -c 'SetDraw(window, "margin")'` 가 `2` 이상 (원복 보장)
    - `awk '/public void RenderBrushMask/,/^        }$/' WPF_Example/Halcon/Display/HalconDisplayService.cs | grep -c 'SetDraw(window, "fill")'` 가 `1`
    - `grep -c "SetDraw" WPF_Example/Halcon/Display/HalconDisplayService.cs` 가 편집 전 실측값 `3` 보다 크다(새 SetDraw 가 추가되었다). 편집 전 값을 직접 재실측해 SUMMARY 에 적을 것
    - `grep -c " ? " WPF_Example/Halcon/Display/HalconDisplayService.cs` 값이 편집 전 실측값과 동일(새 삼항 0건)
  </acceptance_criteria>

  <done>두 렌더 메서드가 존재하고, 채우기 실패 시에도 외곽선을 그리며, 전역 `SetDraw` 가 항상 `"margin"` 으로 원복된다.</done>
</task>

<task type="auto">
  <name>Task 2: 뷰어에 브러시 상태 + 공개 API + 렌더 연결</name>
  <files>WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs</files>

  <read_first>
    - `WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` **60~200줄** (상수/필드/생성자/공개 이벤트)
    - 같은 파일 **298~345줄** (`FitImage`, `StartRectangleDrawing`, `CommitActiveRectangle`, `StartCircleDrawing`, `CommitActiveCircle`) — 모드 진입/종료 API 작성 관습
    - 같은 파일 **425~437줄** `IsAnyDrawingModeActive()`
    - 같은 파일 **755~840줄** (`SetAlignContourXld` / `DisposeAlignContourXld` / `Dispose`) — **HObject 소유·교체·Dispose 모범 선례. 브러시 마스크를 똑같은 형태로 만든다**
    - 같은 파일 **852~1005줄** `RenderNow()` — 렌더 삽입 지점(마지막 `RenderEditHandles();` 직전)
  </read_first>

  <action>
`WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` 에 브러시 상태와 공개 API 를 추가한다. **마우스 핸들러는 Task 3 에서 건드린다.**

(1) 상수 — `HalconRightButton` 상수 근처에 추가:

```csharp
        // 브러시 마스크 표시색. '#rrggbbaa' 의 마지막 2자리가 알파(0x60 ≒ 38%) — HALCON 정식 반투명 표기.
        private const string BrushMaskFillColor = "#ff3b3b60";
        private const string BrushMaskOutlineColor = "red";
        private const string BrushCursorColor = "yellow";
        private const double BrushRadiusMinPx = 5.0;
        private const double BrushRadiusMaxPx = 200.0;
        private const double BrushRadiusDefaultPx = 20.0;
```

(2) 필드 — `_alignContourXld` 필드 근처에 추가:

```csharp
        // 브러시 마스킹 상태. 소유권=이 컨트롤(SetAlignContourXld 와 동일 규약).
        private bool _isBrushMasking;
        private bool _isBrushStroking;          // 좌버튼을 누른 채 끄는 중인지
        private HObject _brushMaskRegion;       // 누적 마스크. 비어 있으면 null
        private System.Windows.Point _brushLastPoint;
        private bool _hasBrushLastPoint;
        private double _brushRadiusPx = BrushRadiusDefaultPx;
        private bool _isBrushEraseMode;
```

(3) 공개 API — `SetAlignContourXld` 근처(같은 region)에 추가. `<interfaces>` 의 시그니처를 **그대로** 쓴다.

- `StartBrushMasking()` : `_isBrushMasking = true; _isBrushStroking = false; _hasBrushLastPoint = false; Render();`
- `StopBrushMasking()` : `_isBrushMasking = false; _isBrushStroking = false; _hasBrushLastPoint = false; Render();` — **마스크 자체는 지우지 않는다**(모드만 끈다)
- `IsBrushMaskingActive` : `get { return _isBrushMasking; }`
- `HasBrushMask` : `get { if (_brushMaskRegion == null) return false; return true; }`
- `BrushRadiusPx` : getter 는 `_brushRadiusPx`. setter 는 클램프 —
```csharp
            set
            {
                double dValue = value;
                if (dValue < BrushRadiusMinPx) { dValue = BrushRadiusMinPx; }
                if (dValue > BrushRadiusMaxPx) { dValue = BrushRadiusMaxPx; }
                _brushRadiusPx = dValue;
                Render();
            }
```
- `IsBrushEraseMode` : 단순 get/set + `Render();`
- `CloneBrushMaskRegion()` :
```csharp
            if (_brushMaskRegion == null)
            {
                return null;
            }
            try
            {
                HObject copied;
                HOperatorSet.CopyObj(_brushMaskRegion, out copied, 1, -1);
                return copied;
            }
            catch
            {
                return null;
            }
```
- `SetBrushMaskRegion(HObject region)` : region 이 null 이면 `ClearBrushMask()` 후 return. null 이 아니면 **먼저 복사본을 만들고**(CopyObj) **그다음 기존 것을 Dispose** 한 뒤 교체하고 `Render();`
- `ClearBrushMask()` : `DisposeBrushMaskRegion(); Render();`
- `private void DisposeBrushMaskRegion()` : `DisposeAlignContourXld` 와 같은 형태 — null 아니면 `try { Dispose(); } catch { }` 후 null 대입
- `public event EventHandler BrushStrokeCompleted;` — 다른 공개 이벤트들 옆에 선언

(4) `Dispose()` 에 `DisposeBrushMaskRegion();` 을 추가한다(`DisposeAlignContourXld();` 다음 줄).

(5) `IsAnyDrawingModeActive()` 에 브러시를 포함시킨다:
```csharp
            return _isBrushMasking || _isDrawingRect || _isDrawingCircle
```
이유: 브러시 모드에서 ROI 편집 핸들 드래그가 함께 걸리면 두 동작이 겹친다. 기존 Rect/Circle 과 동일한 취급이 맞다.

(6) `RenderNow()` 의 마지막 `RenderEditHandles();` **바로 앞**에 렌더를 추가한다:

```csharp
            // 브러시 마스크는 HALCON 창 안에 직접 그린다 — 창 위에 얹은 WPF 요소는 HWND airspace 로 가려진다.
            //  브러시 모드가 아니어도 마스크가 있으면 계속 보여준다(무엇이 빠진 채 모델이 만들어졌는지 확인용).
            if (_brushMaskRegion != null)
            {
                _displayService.RenderBrushMask(ViewerHost.HalconWindow, _brushMaskRegion, BrushMaskFillColor, BrushMaskOutlineColor);
            }
            if (_isBrushMasking && HasImage)
            {
                _displayService.RenderBrushCursor(ViewerHost.HalconWindow, _lastMouseImagePoint.Y, _lastMouseImagePoint.X, _brushRadiusPx, BrushCursorColor);
            }
```

**주의**: `RenderNow` 는 이미 `if (!HasImage) return;` 로 시작하므로 이미지가 없으면 이 코드는 실행되지 않는다. 그대로 둔다.
  </action>

  <verify>
    <automated>cd /c/Info/Project/DataMeasurement; grep -c "public void StartBrushMasking()\|public void StopBrushMasking()\|public bool IsBrushMaskingActive\|public double BrushRadiusPx\|public bool IsBrushEraseMode\|public HObject CloneBrushMaskRegion()\|public void SetBrushMaskRegion(HObject region)\|public void ClearBrushMask()\|public bool HasBrushMask\|public event EventHandler BrushStrokeCompleted" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs</automated>
  </verify>

  <acceptance_criteria>
    - 위 `automated` 출력이 정확히 `10` (공개 API 10개가 각 1회씩)
    - `grep -c "private void DisposeBrushMaskRegion()" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` 가 `1`
    - `awk '/public void Dispose\(\)/,/^        }$/' WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs | grep -c "DisposeBrushMaskRegion();"` 가 `1`
    - `awk '/private bool IsAnyDrawingModeActive/,/^        }$/' WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs | grep -c "_isBrushMasking"` 가 `1`
    - `grep -c "RenderBrushMask(ViewerHost.HalconWindow" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` 가 `1`
    - `grep -c "RenderBrushCursor(ViewerHost.HalconWindow" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` 가 `1`
    - **렌더 순서**: `grep -n "RenderBrushMask(ViewerHost\|RenderEditHandles();" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` 에서 `RenderBrushMask` 줄 번호가 `RenderEditHandles();` 줄 번호보다 작다
    - `grep -c '"#ff3b3b60"' WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` 가 `1`
    - `grep -c " ? " WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` 값이 편집 전 실측값과 동일(새 삼항 0건). 편집 전 값을 직접 재실측해 SUMMARY 에 적을 것
  </acceptance_criteria>

  <done>브러시 공개 API 10개가 존재하고, 마스크 HObject 소유권이 컨트롤에 있으며 `Dispose()` 에서 해제되고, 마스크와 브러시 커서가 HALCON 창 안에 렌더된다.</done>
</task>

<task type="auto">
  <name>Task 3: 마우스 이동/놓기로 드래그 칠하기 (D-74-01) + 빌드 검증</name>
  <files>WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs</files>

  <read_first>
    - `WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` **1082~1225줄** `ViewerHost_HMouseDown` 전체
    - 같은 파일 **1227~1360줄** `ViewerHost_HMouseMove` 전체
    - 같은 파일 **1359~1430줄** `ViewerHost_HMouseUp` 머리
    - `WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs` **265~300줄** — `GenRegionLine` + `DilationCircle` + `Union2` 실사용 선례
  </read_first>

  <action>
드래그 칠하기를 세 핸들러에 넣는다. **브러시 분기는 각 핸들러에서 가장 먼저 검사**해야 팬/줌/ROI편집/폴리곤/측정과 겹치지 않는다.

(1) 누적 헬퍼를 private 메서드로 추가한다(공개 API 근처):

```csharp
        // 한 번의 마우스 이동 구간(prev -> cur)을 브러시 굵기의 자국으로 만들어 누적/차감한다.
        //  점 하나만 찍는 경우(누른 직후)는 GenCircle, 끌고 간 경우는 선을 굵히는 DilationCircle 을 쓴다.
        //  선을 굵히는 방식이라 마우스 이벤트가 듬성듬성 와도 자국이 끊기지 않는다.
        private void ApplyBrushStamp(System.Windows.Point curPoint)
        {
            HObject stamp = null;
            HObject merged = null;
            try
            {
                if (_hasBrushLastPoint == false)
                {
                    HOperatorSet.GenCircle(out stamp, curPoint.Y, curPoint.X, _brushRadiusPx);
                }
                else
                {
                    HObject line = null;
                    try
                    {
                        HOperatorSet.GenRegionLine(out line, _brushLastPoint.Y, _brushLastPoint.X, curPoint.Y, curPoint.X);
                        HOperatorSet.DilationCircle(line, out stamp, _brushRadiusPx);
                    }
                    finally
                    {
                        if (line != null) { try { line.Dispose(); } catch { } }
                    }
                }

                if (_brushMaskRegion == null)
                {
                    if (_isBrushEraseMode == true)
                    {
                        // 아직 칠한 게 없으면 지울 것도 없다.
                        return;
                    }
                    HOperatorSet.CopyObj(stamp, out merged, 1, -1);
                }
                else
                {
                    if (_isBrushEraseMode == true)
                    {
                        HOperatorSet.Difference(_brushMaskRegion, stamp, out merged);
                    }
                    else
                    {
                        HOperatorSet.Union2(_brushMaskRegion, stamp, out merged);
                    }
                }

                // 새 것을 만든 뒤 옛 것을 버린다(순서를 뒤집으면 렌더가 죽은 핸들을 본다).
                HObject old = _brushMaskRegion;
                _brushMaskRegion = merged;
                merged = null;
                if (old != null) { try { old.Dispose(); } catch { } }

                // 지우개로 전부 지워 빈 region 이 되면 null 로 정규화한다(HasBrushMask 가 거짓말하지 않게).
                if (_brushMaskRegion != null)
                {
                    HTuple hvArea, hvRow, hvCol;
                    HOperatorSet.AreaCenter(_brushMaskRegion, out hvArea, out hvRow, out hvCol);
                    double dArea = 0.0;
                    if (hvArea.Length > 0)
                    {
                        dArea = hvArea[0].D;
                    }
                    if (dArea <= 0.0)
                    {
                        DisposeBrushMaskRegion();
                    }
                }
            }
            catch
            {
                // 자국 하나 실패로 칠하기 세션을 죽이지 않는다.
            }
            finally
            {
                if (stamp != null) { try { stamp.Dispose(); } catch { } }
                if (merged != null) { try { merged.Dispose(); } catch { } }
            }

            _brushLastPoint = curPoint;
            _hasBrushLastPoint = true;
        }
```

(2) `ViewerHost_HMouseDown` — 우클릭 블록이 끝나고 `if ((mouseState.Buttons & HalconLeftButton) != HalconLeftButton) { return; }` **바로 다음**에 삽입:

```csharp
            // 브러시 마스킹은 좌클릭 드래그 전용. 다른 어떤 좌클릭 동작보다 먼저 가로챈다.
            if (_isBrushMasking && HasImage)
            {
                _isBrushStroking = true;
                _hasBrushLastPoint = false;
                ApplyBrushStamp(mouseState.ImagePoint);
                Render();
                PublishPointerInfo();
                return;
            }
```

(3) `ViewerHost_HMouseMove` — `_lastMouseImagePoint = mouseState.ImagePoint;` **바로 다음**에 삽입:

```csharp
            if (_isBrushMasking)
            {
                bool bLeftHeld = (mouseState.Buttons & HalconLeftButton) == HalconLeftButton;
                if (_isBrushStroking && bLeftHeld)
                {
                    ApplyBrushStamp(mouseState.ImagePoint);
                }
                // 끌지 않는 동안에도 브러시 크기 미리보기 원이 포인터를 따라다니게 매번 재렌더한다.
                Render();
                PublishPointerInfo();
                return;
            }
```

(4) `ViewerHost_HMouseUp` — 메서드 **맨 첫 줄**에 삽입(다른 어떤 마감 분기보다 앞):

```csharp
            if (_isBrushStroking)
            {
                _isBrushStroking = false;
                _hasBrushLastPoint = false;
                Render();
                // D-74-04: 자국 하나마다가 아니라 '칠하기가 끝난 시점'에 딱 1회만 알린다.
                //  호출부(ViewModel)가 이 시점에 마스크를 저장하고 모델을 재생성한다.
                var brushStrokeCompletedHandler = BrushStrokeCompleted;
                if (brushStrokeCompletedHandler != null) { brushStrokeCompletedHandler(this, EventArgs.Empty); }
                return;
            }
```

**절대 하지 말 것**
- `ViewerHost.HMouseDown/HMouseMove/HMouseUp` 배선을 새로 추가(생성자 157~159줄에 이미 있다)
- 기존 분기의 순서 변경 또는 조건 수정
- `_isPanningImage` / `_isDrawingRect` / `_isDrawingCircle` / `_isEditMode` 관련 로직 수정

**빌드 검증**:

```bash
cd /c/Info/Project/DataMeasurement
SCR="/c/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/7fe7f8e3-9e05-4190-9941-3b1cfdde3971/scratchpad"
SCRW=$(cygpath -w "$SCR")
MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"
rm -rf "$SCR/obj74b" "$SCR/out74b"
"$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 "-p:OutputPath=$SCRW\\out74b\\" "-p:IntermediateOutputPath=$SCRW\\obj74b\\" -v:m -nologo > "$SCR/build74b.log" 2>&1
grep -c ": error " "$SCR/build74b.log"
grep -o "warning CS[0-9]*" "$SCR/build74b.log" | sort | uniq -c
```

기준선(2026-08-27 실측): **에러 0줄 / 경고 18줄 = CS0618 x16 + CS0162 x2.**
  </action>

  <verify>
    <automated>cd /c/Info/Project/DataMeasurement; SCR="/c/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/7fe7f8e3-9e05-4190-9941-3b1cfdde3971/scratchpad"; SCRW=$(cygpath -w "$SCR"); MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"; rm -rf "$SCR/obj74b" "$SCR/out74b"; "$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 "-p:OutputPath=$SCRW\\out74b\\" "-p:IntermediateOutputPath=$SCRW\\obj74b\\" -v:m -nologo > "$SCR/build74b.log" 2>&1; grep -c ": error " "$SCR/build74b.log"; grep -o "warning CS[0-9]*" "$SCR/build74b.log" | sort | uniq -c</automated>
  </verify>

  <acceptance_criteria>
    - 빌드 에러 `0`
    - `grep -o "warning CS[0-9]*" "$SCR/build74b.log" | sort -u` 가 `warning CS0162` 와 `warning CS0618` **두 종류뿐**
    - `grep -c "private void ApplyBrushStamp(System.Windows.Point curPoint)" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` 가 `1`
    - `grep -c "ApplyBrushStamp(mouseState.ImagePoint);" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` 가 `2` (Down 1회 + Move 1회)
    - `grep -c "HOperatorSet.DilationCircle" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` 가 `1`
    - `grep -c "HOperatorSet.Union2" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` 가 `1`, `grep -c "HOperatorSet.Difference" ...` 가 `1`
    - `grep -c "brushStrokeCompletedHandler(this, EventArgs.Empty);" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` 가 `1` (칠하기 종료 시 정확히 1회 발생)
    - **가로채기 위치**: `grep -n "private void ViewerHost_HMouseUp" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` 로 얻은 줄 번호와 `grep -n "_isBrushStroking = false;" ...` 첫 줄 번호의 차이가 **5 이하** (핸들러 맨 앞에 있다)
    - **기존 배선 무증가**: `grep -c "ViewerHost.HMouseDown +=" WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` 가 `1`, `ViewerHost.HMouseMove +=` 가 `1`, `ViewerHost.HMouseUp +=` 가 `1`
    - `grep -c " ? " WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` 값이 Task 2 완료 시점 값과 동일
  </acceptance_criteria>

  <done>브러시 모드에서 좌클릭 드래그로 마스크가 누적/차감되고, 마우스를 놓을 때 `BrushStrokeCompleted` 가 1회 발생하며, 브러시 모드가 꺼져 있으면 기존 마우스 동작이 그대로다.</done>
</task>

</tasks>

<verification>
1. 빌드 에러 0, 경고 코드 종류 CS0618/CS0162 두 종류뿐
2. 공개 API 10개 + `RenderBrushMask`/`RenderBrushCursor` 존재
3. 브러시 분기가 세 마우스 핸들러 각각에서 **가장 먼저** 검사된다
4. `Dispose()` 에서 마스크 HObject 가 해제된다
5. `SetDraw` 가 항상 `"margin"` 으로 원복된다
</verification>

<success_criteria>
- 브러시 모드 OFF 상태에서 뷰어의 팬/줌/ROI 편집/폴리곤/캘리브 동작 코드 경로가 편집 전과 동일하다
- 마스크는 HALCON 창 **안**에 그려진다(WPF 오버레이 0건 — airspace 회피)
- 마우스를 놓을 때만 완료 이벤트가 나간다(자국마다가 아니다)
</success_criteria>

<output>
완료 후 `.planning/phases/74-pattern-model-brush-masking/74-02-SUMMARY.md` 를 작성한다.
편집 전 실측한 grep 기준값(`SetDraw` 카운트, 두 파일의 `" ? "` 카운트, 경고 줄 수)을 반드시 기록한다.
공개 API 최종 시그니처를 SUMMARY 에 그대로 옮겨 적는다 — Plan 03/04/05 가 그것을 보고 호출한다.
</output>
