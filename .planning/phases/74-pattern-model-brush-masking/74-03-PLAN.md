---
phase: 74-pattern-model-brush-masking
plan: 03
type: execute
wave: 2
depends_on: [74-01, 74-02]
files_modified:
  - WPF_Example/UI/ViewModel/PatternBrushMaskViewModel.cs
  - WPF_Example/UI/ControlItem/PatternBrushPanel.xaml
  - WPF_Example/UI/ControlItem/PatternBrushPanel.xaml.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [D-74-01, D-74-02, D-74-04]

must_haves:
  truths:
    - "칠하기가 끝나면 마스크가 파일로 저장되고 모델이 자동으로 다시 만들어진다"
    - "재생성은 자국마다가 아니라 마우스를 놓을 때 딱 1회 일어난다"
    - "모달 팝업 없이 패널 안 상태 문구로만 결과를 알린다"
    - "옵션이 꺼져 있으면 패널이 그 사실을 문구로 경고한다"
    - "[마스크 초기화] 를 누르면 화면 자국과 디스크 마스크 파일이 함께 사라진다"
  artifacts:
    - path: "WPF_Example/UI/ViewModel/PatternBrushMaskViewModel.cs"
      provides: "브러시 패널 상태 + 저장/재생성 오케스트레이션"
      contains: "BrushStrokeCompleted"
    - path: "WPF_Example/UI/ControlItem/PatternBrushPanel.xaml"
      provides: "창 밖 사이드 패널 UI (airspace 회피)"
      contains: "PatternBrushPanel"
    - path: "WPF_Example/UI/ControlItem/PatternBrushPanel.xaml.cs"
      provides: "얇은 code-behind — VM 메서드 호출만"
      contains: "PatternBrushMaskViewModel"
  key_links:
    - from: "PatternBrushMaskViewModel.Attach"
      to: "MainResultViewerControl.BrushStrokeCompleted"
      via: "칠하기 종료 구독 (D-74-04)"
      pattern: "BrushStrokeCompleted \\+="
    - from: "PatternBrushMaskViewModel.SaveAndRegenerate"
      to: "PatternMaskService.TrySaveMask"
      via: "모델 경로마다 마스크 저장 후 재생성"
      pattern: "PatternMaskService\\.TrySaveMask"
---

<objective>
브러시 패널의 **상태와 동작을 ViewModel 로 분리**하고, 세 화면(Datum / Bottom Align / Tray Align)이 공유할 **사이드 패널 UserControl** 을 만든다.

Purpose: `MainView.xaml.cs`(4,476줄)에 새 로직을 넣지 않기 위한 필수 단계다. 호스트는 훅 3개만 채우면 되고, 저장·재생성·상태문구는 전부 여기서 처리한다.
Output: `PatternBrushMaskViewModel.cs`(신규), `PatternBrushPanel.xaml`/`.xaml.cs`(신규), csproj 등록.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-pattern-model-brush-masking/74-CONTEXT.md
@.planning/phases/74-pattern-model-brush-masking/74-01-SUMMARY.md
@.planning/phases/74-pattern-model-brush-masking/74-02-SUMMARY.md
</context>

<coding_rules>
- **삼항 `?:` 금지** → `if/else`. **`??` `?.` 금지** → 명시 분기.
- **전통 `switch` 만**. **C# 7.2 문법만** (`nameof` 는 C# 6 라 사용 가능).
- 긴 조건은 이름 있는 `bool` 로 선추출.
- 헝가리언: `b` `n` `sz` `d` `hv`.
- 신규 파일은 **Allman** 중괄호.
- 날짜 주석 새로 달지 않는다.
- `HObject` 는 반드시 Dispose.
- **모달 대화상자(`CustomMessageBox`) 금지** — 칠할 때마다 팝업이 뜨면 못 쓴다. 결과는 패널 안 상태 문구로만 알린다.
- 실행 중 프로세스 종료 금지. 빌드는 스크래치 OutDir.
- **`WPF_Example/DatumMeasurement.csproj` 는 커밋 금지**(수정만, unstaged 유지).
</coding_rules>

<interfaces>
### Plan 01 이 만든 것 (`ReringProject.Halcon.Services.PatternMaskService`, static)

```csharp
public const string EXTENSION_PATTERN_MASK = ".mask.hobj";
public static string ResolveMaskPath(string szModelPath);
public static bool IsMaskEnabled();
public static bool HasMask(string szModelPath);
public static bool TryLoadMask(string szModelPath, out HObject maskRegion);   // true 면 호출자가 Dispose
public static bool TrySaveMask(string szModelPath, HObject maskRegion, out string szError);
public static bool DeleteMask(string szModelPath);
```

`SystemSetting.Handle.UsePatternBrushMask` (bool, 기본 false)

### Plan 02 가 만든 것 (`ReringProject.UI.MainResultViewerControl`)

```csharp
public void StartBrushMasking();
public void StopBrushMasking();
public bool IsBrushMaskingActive { get; }
public double BrushRadiusPx { get; set; }        // 5.0~200.0 클램프, 기본 20.0
public bool IsBrushEraseMode { get; set; }
public HObject CloneBrushMaskRegion();           // 비어 있으면 null. 반환본 Dispose 는 호출자 책임
public void SetBrushMaskRegion(HObject region);  // 내부 복사본 보관. null 이면 초기화
public void ClearBrushMask();
public bool HasBrushMask { get; }
public event EventHandler BrushStrokeCompleted;
```

### 이 plan 이 만들어야 하는 공개 계약 (Plan 04/05 가 이대로 쓴다 — 이름 변경 금지)

```csharp
// namespace ReringProject.UI
public class PatternBrushMaskViewModel : INotifyPropertyChanged
{
    // 호스트가 채우는 훅 2개
    public Func<IList<string>> ModelPathsProvider { get; set; }  // 마스크를 붙일 모델 파일 경로 목록
    public Func<string> ModelRegenerator { get; set; }           // 성공=null, 실패=오류 문자열

    public void Attach(MainResultViewerControl viewer);
    public void Detach();

    public bool IsMaskEnabled { get; set; }   // SystemSetting.Handle.UsePatternBrushMask 와 양방향
    public bool IsBrushActive { get; set; }   // 칠하기 모드
    public bool IsEraseMode { get; set; }
    public double BrushRadiusPx { get; set; }
    public string StatusText { get; }

    public void ClearMask();                  // 화면 자국 + 디스크 마스크 파일 삭제 + 재생성
    public void ReloadMaskFromDisk();         // 대상(Datum/슬롯) 전환 시 호출
    public void RefreshStatus();
}

// namespace ReringProject.UI
public partial class PatternBrushPanel : UserControl
{
    public PatternBrushMaskViewModel ViewModel { get; }   // 생성자에서 만들어 DataContext 로 걸어 둔다
}
```

### 호스트 XAML 사용 예 (Plan 04/05 참고)

```xml
xmlns:ui="clr-namespace:ReringProject.UI"
...
<ui:PatternBrushPanel x:Name="brushPanel" Margin="0,0,0,8"/>
```

### 참고 선례

- `WPF_Example/UI/ControlItem/InspectionListView.xaml` 1~10줄 — `x:Class="ReringProject.UI.InspectionListView"`, `xmlns:local="clr-namespace:ReringProject.UI"`
- `WPF_Example/Custom/UI/BottomVisionView.xaml` 230~275줄 — `GroupBox` + `StackPanel` + `Slider` + `TextBlock` 사이드 패널 구성 관습
- `WPF_Example/DatumMeasurement.csproj` 448~450줄 — `Compile Include` + `DependentUpon` 형식
- `WPF_Example/DatumMeasurement.csproj` 488~491줄 — `Page Include` + `Generator MSBuild:Compile` + `SubType Designer` 형식
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: PatternBrushMaskViewModel 신규 — 저장/재생성 오케스트레이션</name>
  <files>WPF_Example/UI/ViewModel/PatternBrushMaskViewModel.cs</files>

  <read_first>
    - `.planning/phases/74-pattern-model-brush-masking/74-01-SUMMARY.md` 와 `74-02-SUMMARY.md` — 실제 확정된 시그니처 확인
    - `WPF_Example/Halcon/Services/PatternMaskService.cs` 전체
    - `WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs` 의 브러시 공개 API 블록
    - `WPF_Example/UI/ViewModel/CalibrationViewModel.cs` 1~40줄 — 이 저장소 ViewModel 파일 머리/네임스페이스 관습
    - `WPF_Example/UI/ControlItem/InspectionListViewModel.cs` 에서 `INotifyPropertyChanged` 구현 형태 (grep: `PropertyChanged`)
  </read_first>

  <action>
신규 파일 `WPF_Example/UI/ViewModel/PatternBrushMaskViewModel.cs`. **Allman**, `namespace ReringProject.UI`.

using: `System`, `System.Collections.Generic`, `System.ComponentModel`, `System.Runtime.CompilerServices`, `HalconDotNet`, `ReringProject.Define`, `ReringProject.Halcon.Services`, `ReringProject.Setting`, `ReringProject.Utility`.

**INotifyPropertyChanged** 는 직접 구현한다:

```csharp
        public event PropertyChangedEventHandler PropertyChanged;

        private void Raise([CallerMemberName] string szName = null)
        {
            PropertyChangedEventHandler handler = PropertyChanged;
            if (handler != null)
            {
                handler(this, new PropertyChangedEventArgs(szName));
            }
        }
```

**필드/훅**

```csharp
        private MainResultViewerControl _viewer;
        private string _statusText = "브러시 마스킹 대기";

        // 호스트가 채우는 훅. null 이면 저장/재생성을 건너뛰고 상태 문구만 남긴다.
        public Func<IList<string>> ModelPathsProvider { get; set; }
        public Func<string> ModelRegenerator { get; set; }
```

**Attach / Detach** — 구독 해제 대칭을 반드시 지킨다(이 저장소의 `RectDrawingCompleted -= ...; += ...` 관습과 동일).

```csharp
        public void Attach(MainResultViewerControl viewer)
        {
            Detach();
            _viewer = viewer;
            if (_viewer == null)
            {
                return;
            }
            _viewer.BrushStrokeCompleted -= OnBrushStrokeCompleted;
            _viewer.BrushStrokeCompleted += OnBrushStrokeCompleted;
            _viewer.BrushRadiusPx = BrushRadiusPx;
            _viewer.IsBrushEraseMode = IsEraseMode;
            RefreshStatus();
        }

        public void Detach()
        {
            if (_viewer == null)
            {
                return;
            }
            _viewer.BrushStrokeCompleted -= OnBrushStrokeCompleted;
            _viewer.StopBrushMasking();
            _viewer = null;
        }
```

**바인딩 프로퍼티**

- `IsMaskEnabled` : getter 는 `SystemSetting.Handle.UsePatternBrushMask`. setter 는 그 값을 쓰고 `Raise()` + `RefreshStatus()`. (시스템 설정 저장은 기존 설정 화면이 담당하므로 여기서 `Save()` 를 부르지 않는다 — 세션 내 즉시 적용만.)
- `IsBrushActive` : setter 에서 뷰어 모드 전환
```csharp
            set
            {
                if (_isBrushActive == value)
                {
                    return;
                }
                _isBrushActive = value;
                if (_viewer != null)
                {
                    if (_isBrushActive == true)
                    {
                        _viewer.StartBrushMasking();
                    }
                    else
                    {
                        _viewer.StopBrushMasking();
                    }
                }
                Raise();
                RefreshStatus();
            }
```
- `IsEraseMode` : setter 에서 `_viewer.IsBrushEraseMode = value;` (뷰어 null 가드) + `Raise()` + `RefreshStatus()`
- `BrushRadiusPx` : 기본 `20.0`. setter 에서 `_viewer.BrushRadiusPx = value;` 후 **뷰어가 클램프한 값을 다시 읽어** 필드에 넣는다(패널 슬라이더와 실제 값이 어긋나지 않게). 뷰어가 null 이면 직접 5.0~200.0 클램프.
- `StatusText` : get 전용, `_statusText` 반환.

**RefreshStatus** — 사용자가 지금 상태를 한 문장으로 알 수 있게 만든다.

```csharp
        public void RefreshStatus()
        {
            string szMode = "칠하기";
            if (IsEraseMode == true)
            {
                szMode = "지우개";
            }

            string szMask = "마스크 없음";
            if (_viewer != null)
            {
                if (_viewer.HasBrushMask == true)
                {
                    szMask = "마스크 있음";
                }
            }

            string szWarn = "";
            bool bDisabled = SystemSetting.Handle.UsePatternBrushMask == false;
            if (bDisabled == true)
            {
                szWarn = "  ⚠ 옵션이 꺼져 있어 모델에 반영되지 않습니다";
            }

            SetStatus(szMode + " · " + szMask + " · 굵기 " + BrushRadiusPx.ToString("F0") + "px" + szWarn);
        }

        private void SetStatus(string szText)
        {
            _statusText = szText;
            Raise(nameof(StatusText));
        }
```

**OnBrushStrokeCompleted → 저장 + 재생성 (D-74-04)**

```csharp
        private void OnBrushStrokeCompleted(object sender, EventArgs e)
        {
            SaveAndRegenerate();
        }

        // 칠하기 1획이 끝난 시점에만 호출된다. 파일 쓰기 + 모델 재생성이라 자국마다 하면 안 된다(D-74-04).
        private void SaveAndRegenerate()
        {
            if (_viewer == null)
            {
                return;
            }

            IList<string> paths = null;
            if (ModelPathsProvider != null)
            {
                paths = ModelPathsProvider();
            }
            bool bNoPath = (paths == null) || (paths.Count == 0);
            if (bNoPath == true)
            {
                SetStatus("칠했지만 대상 모델 경로가 없습니다 — 패턴 ROI/슬롯을 먼저 선택하세요");
                return;
            }

            HObject region = _viewer.CloneBrushMaskRegion();
            int nSaved = 0;
            int nFailed = 0;
            string szLastError = null;
            try
            {
                foreach (string szModelPath in paths)
                {
                    if (string.IsNullOrEmpty(szModelPath))
                    {
                        continue;
                    }
                    if (region == null)
                    {
                        // 마스크가 비었다 = 지우개로 다 지웠다. 파일도 같이 지워 고아를 남기지 않는다.
                        PatternMaskService.DeleteMask(szModelPath);
                        nSaved = nSaved + 1;
                        continue;
                    }
                    string szError;
                    bool bOk = PatternMaskService.TrySaveMask(szModelPath, region, out szError);
                    if (bOk == true)
                    {
                        nSaved = nSaved + 1;
                    }
                    else
                    {
                        nFailed = nFailed + 1;
                        szLastError = szError;
                    }
                }
            }
            finally
            {
                if (region != null) { try { region.Dispose(); } catch { } }
            }

            if (nFailed > 0)
            {
                SetStatus("마스크 저장 실패 " + nFailed.ToString() + "건: " + szLastError);
                return;
            }

            // 마스크와 모델이 항상 일치하도록 즉시 재생성한다(D-74-04). 저장이 먼저여야 한다 —
            //  TryCreateModel 이 디스크의 마스크 파일을 읽기 때문이다.
            if (ModelRegenerator == null)
            {
                SetStatus("마스크 저장 완료 " + nSaved.ToString() + "건 (재생성 훅 없음)");
                return;
            }

            string szRegenError = ModelRegenerator();
            if (string.IsNullOrEmpty(szRegenError))
            {
                SetStatus("마스크 반영 + 모델 재생성 완료 (" + nSaved.ToString() + "개 경로)");
            }
            else
            {
                SetStatus("마스크는 저장했지만 모델 재생성 실패: " + szRegenError);
                Logging.PrintErrLog((int)ELogType.Error, "[PatternMask] 모델 재생성 실패: " + szRegenError);
            }
        }
```

**ClearMask** — 화면 자국과 디스크 파일을 함께 지우고 재생성한다(고아 마스크 방지).

```csharp
        public void ClearMask()
        {
            if (_viewer != null)
            {
                _viewer.ClearBrushMask();
            }

            IList<string> paths = null;
            if (ModelPathsProvider != null)
            {
                paths = ModelPathsProvider();
            }
            int nDeleted = 0;
            if (paths != null)
            {
                foreach (string szModelPath in paths)
                {
                    if (string.IsNullOrEmpty(szModelPath))
                    {
                        continue;
                    }
                    bool bDeleted = PatternMaskService.DeleteMask(szModelPath);
                    if (bDeleted == true)
                    {
                        nDeleted = nDeleted + 1;
                    }
                }
            }

            if (ModelRegenerator != null)
            {
                string szRegenError = ModelRegenerator();
                if (string.IsNullOrEmpty(szRegenError) == false)
                {
                    SetStatus("마스크 파일 " + nDeleted.ToString() + "개 삭제, 모델 재생성 실패: " + szRegenError);
                    return;
                }
            }
            SetStatus("마스크 초기화 완료 (파일 " + nDeleted.ToString() + "개 삭제)");
        }
```

**ReloadMaskFromDisk** — 대상(Datum 선택/Align 슬롯)이 바뀔 때 호스트가 부른다. 첫 번째로 존재하는 마스크 파일을 뷰어에 올린다.

```csharp
        public void ReloadMaskFromDisk()
        {
            if (_viewer == null)
            {
                return;
            }
            _viewer.ClearBrushMask();

            IList<string> paths = null;
            if (ModelPathsProvider != null)
            {
                paths = ModelPathsProvider();
            }
            if (paths == null)
            {
                RefreshStatus();
                return;
            }

            foreach (string szModelPath in paths)
            {
                if (string.IsNullOrEmpty(szModelPath))
                {
                    continue;
                }
                // 옵션이 꺼져 있어도 '이미 칠해 둔 것'은 화면에 보여준다 — 그래야 사용자가 상태를 안다.
                // TryLoadMask 는 옵션 게이트를 타므로 여기서는 파일 존재만 보고 직접 읽는다.
                bool bHas = PatternMaskService.HasMask(szModelPath);
                if (bHas == false)
                {
                    continue;
                }
                HObject loaded = null;
                try
                {
                    HOperatorSet.ReadRegion(out loaded, PatternMaskService.ResolveMaskPath(szModelPath));
                    _viewer.SetBrushMaskRegion(loaded);
                }
                catch (Exception ex)
                {
                    Logging.PrintErrLog((int)ELogType.Error, "[PatternMask] 마스크 표시 로드 실패: " + ex.Message);
                }
                finally
                {
                    if (loaded != null) { try { loaded.Dispose(); } catch { } }
                }
                break;
            }
            RefreshStatus();
        }
```
  </action>

  <verify>
    <automated>cd /c/Info/Project/DataMeasurement; grep -c "public void Attach(MainResultViewerControl viewer)\|public void Detach()\|public void ClearMask()\|public void ReloadMaskFromDisk()\|public void RefreshStatus()\|public Func<IList<string>> ModelPathsProvider\|public Func<string> ModelRegenerator" WPF_Example/UI/ViewModel/PatternBrushMaskViewModel.cs</automated>
  </verify>

  <acceptance_criteria>
    - `WPF_Example/UI/ViewModel/PatternBrushMaskViewModel.cs` 존재, `namespace ReringProject.UI`
    - 위 `automated` 출력이 정확히 `7`
    - `grep -c "class PatternBrushMaskViewModel : INotifyPropertyChanged" WPF_Example/UI/ViewModel/PatternBrushMaskViewModel.cs` 가 `1`
    - `grep -c "BrushStrokeCompleted -= OnBrushStrokeCompleted;" WPF_Example/UI/ViewModel/PatternBrushMaskViewModel.cs` 가 `2` (Attach 의 방어적 해제 1 + Detach 1)
    - `grep -c "BrushStrokeCompleted += OnBrushStrokeCompleted;" WPF_Example/UI/ViewModel/PatternBrushMaskViewModel.cs` 가 `1`
    - `grep -c "PatternMaskService.TrySaveMask" WPF_Example/UI/ViewModel/PatternBrushMaskViewModel.cs` 가 `1`
    - `grep -c "PatternMaskService.DeleteMask" WPF_Example/UI/ViewModel/PatternBrushMaskViewModel.cs` 가 `2` (지우개로 비운 경우 1 + ClearMask 1)
    - **저장이 재생성보다 먼저**: `awk '/private void SaveAndRegenerate/,/^        }$/' WPF_Example/UI/ViewModel/PatternBrushMaskViewModel.cs | grep -n "TrySaveMask\|ModelRegenerator()"` 에서 `TrySaveMask` 줄 번호가 `ModelRegenerator()` 줄 번호보다 작다
    - **모달 금지**: `grep -c "CustomMessageBox" WPF_Example/UI/ViewModel/PatternBrushMaskViewModel.cs` 가 `0`
    - **Dispose 누락 없음**: `grep -c "region.Dispose()" WPF_Example/UI/ViewModel/PatternBrushMaskViewModel.cs` 가 `1`, `grep -c "loaded.Dispose()" ...` 가 `1`
    - `grep -c " ? " WPF_Example/UI/ViewModel/PatternBrushMaskViewModel.cs` 가 `0`
    - `grep -c '??' WPF_Example/UI/ViewModel/PatternBrushMaskViewModel.cs` 가 `0`
  </acceptance_criteria>

  <done>ViewModel 이 칠하기 종료를 구독해 마스크를 저장하고 모델을 재생성하며, 모달 팝업 없이 상태 문구로만 결과를 알린다.</done>
</task>

<task type="auto">
  <name>Task 2: PatternBrushPanel UserControl 신규 + csproj 등록 + 빌드 검증</name>
  <files>WPF_Example/UI/ControlItem/PatternBrushPanel.xaml, WPF_Example/UI/ControlItem/PatternBrushPanel.xaml.cs, WPF_Example/DatumMeasurement.csproj</files>

  <read_first>
    - `WPF_Example/UI/ControlItem/InspectionListView.xaml` **1~12줄** — `x:Class` / `xmlns:local` 선언 형식
    - `WPF_Example/Custom/UI/BottomVisionView.xaml` **230~250줄** — `GroupBox` + `StackPanel` + `Slider` + `TextBlock` 사이드 패널 구성 관습
    - `WPF_Example/DatumMeasurement.csproj` **444~495줄** — `Compile Include` + `DependentUpon`, `Page Include` + `Generator`/`SubType` 형식
    - Task 1 산출물 `WPF_Example/UI/ViewModel/PatternBrushMaskViewModel.cs`
  </read_first>

  <action>
신규 `WPF_Example/UI/ControlItem/PatternBrushPanel.xaml` — `x:Class="ReringProject.UI.PatternBrushPanel"`, 루트는 `UserControl`.

**airspace 규칙**: 이 패널은 **HALCON 창 밖**(호스트의 좌측 WPF 컨트롤 패널)에 놓인다. 브러시 자국·미리보기는 Plan 02 가 이미 HALCON 창 안에 그린다. 이 패널에는 그림 요소를 두지 않는다.

구성 (루트 `GroupBox Header="브러시 마스킹 (모델 제외 영역)"` 안에 세로 `StackPanel Margin="8"`):

1. `CheckBox x:Name="chk_maskEnabled" Content="브러시 마스킹 사용 (끄면 기존 동작)" IsChecked="{Binding IsMaskEnabled, Mode=TwoWay}"`
2. 가로 `StackPanel`:
   - `ToggleButton x:Name="btn_brushActive" Content="칠하기 모드" IsChecked="{Binding IsBrushActive, Mode=TwoWay}" MinWidth="90" Height="26" Margin="0,0,6,0"`
   - `ToggleButton x:Name="btn_eraseMode" Content="지우개" IsChecked="{Binding IsEraseMode, Mode=TwoWay}" MinWidth="70" Height="26"`
3. 가로 `StackPanel` (굵기):
   - `TextBlock Text="굵기:" VerticalAlignment="Center" Margin="0,0,6,0" FontSize="12"`
   - `Slider x:Name="sld_brushRadius" Minimum="5" Maximum="200" Width="180" VerticalAlignment="Center" Value="{Binding BrushRadiusPx, Mode=TwoWay}"`
   - `TextBlock Text="{Binding BrushRadiusPx, StringFormat={}{0:F0}px}" Width="46" Margin="6,0,0,0" FontSize="12" VerticalAlignment="Center"`
4. `Button x:Name="btn_clearMask" Content="마스크 초기화" Click="ClearMaskButton_Click" Height="26" Margin="0,6,0,0"`
5. `TextBlock x:Name="lbl_brushStatus" Text="{Binding StatusText}" Margin="0,6,0,0" FontSize="12" Foreground="#475569" TextWrapping="Wrap"`

신규 `WPF_Example/UI/ControlItem/PatternBrushPanel.xaml.cs` — **얇게**. `namespace ReringProject.UI`, `public partial class PatternBrushPanel : UserControl`.

```csharp
        public PatternBrushMaskViewModel ViewModel { get; private set; }

        public PatternBrushPanel()
        {
            InitializeComponent();
            ViewModel = new PatternBrushMaskViewModel();
            DataContext = ViewModel;
        }

        private void ClearMaskButton_Click(object sender, RoutedEventArgs e)
        {
            if (ViewModel == null)
            {
                return;
            }
            ViewModel.ClearMask();
        }
```

code-behind 에 위 두 멤버 외 다른 로직을 넣지 않는다.

**csproj 등록** (`WPF_Example/DatumMeasurement.csproj`, 커밋 금지):
- `UI\ViewModel\CalibrationViewModel.cs` Compile 항목 근처에 `UI\ViewModel\PatternBrushMaskViewModel.cs` Compile 항목 추가
- `UI\ControlItem\InspectionListView.xaml.cs` Compile 항목 근처에 `UI\ControlItem\PatternBrushPanel.xaml.cs` Compile 항목 추가 (`DependentUpon` 은 `PatternBrushPanel.xaml`)
- `UI\ControlItem\InspectionListView.xaml` Page 항목 근처에 `UI\ControlItem\PatternBrushPanel.xaml` Page 항목 추가 (`Generator` = `MSBuild:Compile`, `SubType` = `Designer`)

**빌드 검증**:

```bash
cd /c/Info/Project/DataMeasurement
SCR="/c/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/7fe7f8e3-9e05-4190-9941-3b1cfdde3971/scratchpad"
SCRW=$(cygpath -w "$SCR")
MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"
rm -rf "$SCR/obj74c" "$SCR/out74c"
"$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 "-p:OutputPath=$SCRW\\out74c\\" "-p:IntermediateOutputPath=$SCRW\\obj74c\\" -v:m -nologo > "$SCR/build74c.log" 2>&1
grep -c ": error " "$SCR/build74c.log"
grep -o "warning CS[0-9]*" "$SCR/build74c.log" | sort | uniq -c
```

XAML 오타는 **컴파일 에러**로 잡히므로 이 빌드가 패널 XAML 의 실질 검증이다. `Page Include` 를 빠뜨리면 `InitializeComponent` 미정의(CS0103)로 즉시 드러난다.
  </action>

  <verify>
    <automated>cd /c/Info/Project/DataMeasurement; SCR="/c/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/7fe7f8e3-9e05-4190-9941-3b1cfdde3971/scratchpad"; SCRW=$(cygpath -w "$SCR"); MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"; rm -rf "$SCR/obj74c" "$SCR/out74c"; "$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 "-p:OutputPath=$SCRW\\out74c\\" "-p:IntermediateOutputPath=$SCRW\\obj74c\\" -v:m -nologo > "$SCR/build74c.log" 2>&1; grep -c ": error " "$SCR/build74c.log"; grep -o "warning CS[0-9]*" "$SCR/build74c.log" | sort | uniq -c</automated>
  </verify>

  <acceptance_criteria>
    - 빌드 에러 `0`
    - `grep -o "warning CS[0-9]*" "$SCR/build74c.log" | sort -u` 가 `warning CS0162` 와 `warning CS0618` **두 종류뿐**
    - `grep -c 'x:Class="ReringProject.UI.PatternBrushPanel"' WPF_Example/UI/ControlItem/PatternBrushPanel.xaml` 가 `1`
    - `grep -c "public PatternBrushMaskViewModel ViewModel { get; private set; }" WPF_Example/UI/ControlItem/PatternBrushPanel.xaml.cs` 가 `1`
    - **code-behind 얇음**: `grep -c "private void \|public void " WPF_Example/UI/ControlItem/PatternBrushPanel.xaml.cs` 가 `1` (`ClearMaskButton_Click` 하나뿐)
    - `grep -c "wc" /dev/null; wc -l < WPF_Example/UI/ControlItem/PatternBrushPanel.xaml.cs` 가 `40` 이하
    - `grep -F -c 'PatternBrushMaskViewModel.cs' WPF_Example/DatumMeasurement.csproj` 가 `1`
    - `grep -F -c 'PatternBrushPanel.xaml.cs' WPF_Example/DatumMeasurement.csproj` 가 `1`
    - `grep -F -c 'PatternBrushPanel.xaml"' WPF_Example/DatumMeasurement.csproj` 가 `1` (Page 항목)
    - `grep -c "Binding IsMaskEnabled\|Binding IsBrushActive\|Binding IsEraseMode\|Binding BrushRadiusPx\|Binding StatusText" WPF_Example/UI/ControlItem/PatternBrushPanel.xaml` 가 `6` 이상 (BrushRadiusPx 가 Slider + TextBlock 2회)
    - `git status --porcelain WPF_Example/DatumMeasurement.csproj` 이 ` M` 으로 시작(unstaged)
  </acceptance_criteria>

  <done>패널 UserControl 이 컴파일되고 ViewModel 에 바인딩되며, code-behind 에는 버튼 핸들러 하나만 있다.</done>
</task>

</tasks>

<verification>
1. 빌드 에러 0, 경고 코드 종류 CS0618/CS0162 두 종류뿐
2. `PatternBrushPanel.xaml.cs` 가 40줄 이하(얇은 view)
3. ViewModel 에 `CustomMessageBox` 참조 0건(모달 금지)
4. 저장(`TrySaveMask`)이 재생성(`ModelRegenerator()`)보다 먼저 실행된다
5. csproj 3개 항목 추가, unstaged 유지
</verification>

<success_criteria>
- 호스트는 `ModelPathsProvider` / `ModelRegenerator` 두 훅과 `Attach(viewer)` 만 채우면 된다
- 칠하기 종료 → 저장 → 재생성 순서가 코드상 강제된다
- 마스크가 비면 마스크 파일도 함께 삭제되어 고아가 남지 않는다
</success_criteria>

<output>
완료 후 `.planning/phases/74-pattern-model-brush-masking/74-03-SUMMARY.md` 를 작성한다.
`PatternBrushMaskViewModel` / `PatternBrushPanel` 의 최종 공개 시그니처를 그대로 옮겨 적는다 — Plan 04/05 가 그것을 보고 배선한다.
</output>
