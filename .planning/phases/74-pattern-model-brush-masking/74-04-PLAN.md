---
phase: 74-pattern-model-brush-masking
plan: 04
type: execute
wave: 3
depends_on: [74-03]
files_modified:
  - WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs
  - WPF_Example/Custom/UI/BottomVisionView.xaml
  - WPF_Example/Custom/UI/BottomVisionView.xaml.cs
  - WPF_Example/Custom/UI/TrayVisionView.xaml
  - WPF_Example/Custom/UI/TrayVisionView.xaml.cs
autonomous: true
requirements: [D-74-01, D-74-03, D-74-04]

must_haves:
  truths:
    - "Bottom Align 화면에서 브러시로 칠하면 그 슬롯의 두 모델 옆에 마스크가 저장된다"
    - "칠하기를 놓으면 같은 ROI 로 티칭이 자동 재실행되어 모델이 마스크를 반영한다"
    - "슬롯을 바꾸면 그 슬롯에 저장된 마스크가 화면에 다시 뜬다"
    - "Tray Align 화면에서도 같은 패널이 동작한다"
    - "브러시 패널을 쓰지 않으면 기존 티칭/검사 흐름이 그대로다"
  artifacts:
    - path: "WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs"
      provides: "마스크 대상 모델 경로 2개 조회(부작용 0)"
      contains: "GetModelPathsForMask"
    - path: "WPF_Example/Custom/UI/BottomVisionView.xaml"
      provides: "티칭 GroupBox 안 브러시 패널"
      contains: "PatternBrushPanel"
    - path: "WPF_Example/Custom/UI/TrayVisionView.xaml"
      provides: "티칭 GroupBox 안 브러시 패널"
      contains: "PatternBrushPanel"
  key_links:
    - from: "BottomVisionView.AttachSharedViewer"
      to: "brushPanel.ViewModel.Attach"
      via: "공유 뷰어 주입 시 브러시 VM 연결"
      pattern: "brushPanel\\.ViewModel\\.Attach\\(viewer\\)"
    - from: "BottomVisionView.SlotComboBox_SelectionChanged"
      to: "brushPanel.ViewModel.ReloadMaskFromDisk"
      via: "슬롯 전환 시 마스크 재로드"
      pattern: "ReloadMaskFromDisk\\(\\)"
    - from: "BottomVisionView.RegenerateTeachSilent"
      to: "Matcher.TryTeach"
      via: "모달 없이 같은 ROI 로 재티칭 (D-74-04)"
      pattern: "Matcher\\.TryTeach"
---

<objective>
**Align 화면(Bottom / Tray)** 에 브러시 패널을 붙인다. 이 phase 의 원래 동기(Bottom Align 캘리브레이션 노이즈)가 여기서 실현된다.

Purpose: Align 모델은 `AlignShapeMatchService.BuildShmPath` 로 경로를 만들어 `RecipeFiles` 헬퍼를 거치지 않는다. 그래서 마스크 대상 경로를 조회하는 공개 창구가 하나 필요하다. 재생성은 기존 `TryTeach` 를 그대로 재호출해 새 로직을 만들지 않는다.
Output: `GetModelPathsForMask`, 두 화면의 패널 배치와 훅 배선.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-pattern-model-brush-masking/74-CONTEXT.md
@.planning/phases/74-pattern-model-brush-masking/74-03-SUMMARY.md
</context>

<coding_rules>
- **삼항 `?:` 금지** → `if/else`. **`??` `?.` 금지** → 명시 분기. (람다 `=>` 는 삼항이 아니므로 허용)
- **전통 `switch` 만**. **C# 7.2 문법만.**
- 긴 조건은 이름 있는 `bool` 로 선추출.
- 헝가리언: `b` `n` `sz` `d`.
- **중괄호 스타일**: `AlignShapeMatchService.cs` / `BottomVisionView.xaml.cs` / `TrayVisionView.xaml.cs` 는 전부 **K&R**(여는 중괄호를 같은 줄에). Allman 으로 쓰지 말 것.
- 날짜 주석 새로 달지 않는다.
- 실행 중 프로세스 종료 금지. 빌드는 스크래치 OutDir.
- csproj 는 이 plan 에서 **건드리지 않는다**(신규 파일 없음).
- **기존 티칭/검사/캘리브 핸들러의 기존 줄을 수정하지 않는다.** 추가만 한다.
</coding_rules>

<interfaces>
### Plan 03 이 만든 것

```csharp
// namespace ReringProject.UI
public partial class PatternBrushPanel : UserControl
{
    public PatternBrushMaskViewModel ViewModel { get; }
}

public class PatternBrushMaskViewModel : INotifyPropertyChanged
{
    public Func<IList<string>> ModelPathsProvider { get; set; }
    public Func<string> ModelRegenerator { get; set; }   // 성공=null, 실패=오류 문자열
    public void Attach(MainResultViewerControl viewer);
    public void Detach();
    public void ClearMask();
    public void ReloadMaskFromDisk();
    public void RefreshStatus();
}
```

### 기존 Align 코드 (수정 지점)

`WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs`
- `:77` `private string BuildShmPath(EEthernetVisionMode mode, int modelIndex, EBottomAlignSlot slot = EBottomAlignSlot.None)` — **순수 문자열 도출, IO 부작용 없음**. `RecipeSavePath` 가 비면 null.
- `:119` `private string GetShmPath(...)` — `BuildShmPath` + `Directory.CreateDirectory`. **마스크 경로 조회에는 이걸 쓰면 안 된다**(폴더를 만든다).
- `:300` `public bool HasTemplate(EEthernetVisionMode mode, EBottomAlignSlot slot = EBottomAlignSlot.None)` — `BuildShmPath` 를 쓰는 기존 공개 메서드. 새 메서드도 같은 자리에 둔다.
- `TryTeach` 오버로드 4종(실측):
  - `:320` `(img, roi1x5, roi2x5, mode, out error)`
  - `:334` `(img, roi1x5, roi2x5, mode, slot, out error)`
  - `:446` `(img, roi1x5, roi2x5, mode, out dScore1, out dScore2, out error)`
  - `:462` `(img, roi1x5, roi2x5, mode, slot, out dScore1, out dScore2, out error)`

`WPF_Example/Custom/UI/BottomVisionView.xaml.cs`
- `:31` 근처 `private const EEthernetVisionMode VIEW_MODE = EEthernetVisionMode.Bottom;`
- `:45` `private MainResultViewerControl _viewer;`
- `:50~51` `_roi1` / `_roi2` (`RoiDefinition`)
- `:61` `_selectedSlot` (`EBottomAlignSlot`, 기본 `None`)
- `:63` `_slotRois` (`Dictionary<EBottomAlignSlot, RoiDefinition[]>`)
- `:92` `public void AttachSharedViewer(MainResultViewerControl viewer)` — `_viewer = viewer; ViewerHostBorder.Child = viewer;` 후 여러 구독/콜백 등록
- `:222` `private void SlotComboBox_SelectionChanged(...)` — 마지막이 `LoadSlotCoaxToUi(); RefreshStatus();`
- `:639` `private void TeachButton_Click(...)` — `ValidateRois()` / `RectToTeachParams(...)` / `Matcher.TryTeach(...)` 사용
- `:787` 근처 `private string ValidateRois()` (오류 문자열 반환, 정상이면 null)
- `RectToTeachParams(RoiDefinition roi, out double r, out double c, out double phi, out double l1, out double l2)`

`WPF_Example/Custom/UI/TrayVisionView.xaml.cs`
- `:31` `private const EEthernetVisionMode VIEW_MODE = EEthernetVisionMode.Tray;`
- `:43` `_viewer`, `:50~51` `_roi1`/`_roi2`
- `:77` `public void AttachSharedViewer(MainResultViewerControl viewer)`
- `:88` `private void TrayVisionView_Loaded(...)`
- `:459` 근처 `TeachButton_Click`, `:787` 근처 `ValidateRois()`, `:814` 근처 `RectToTeachParams`
- Tray 는 슬롯이 없다 → 항상 `EBottomAlignSlot.None`

### XAML 배치 지점

- `WPF_Example/Custom/UI/BottomVisionView.xaml` **135~159줄** `GroupBox Grid.Row="3" Header="티칭 (선택 슬롯 · ROI 2개)"` 안의 `StackPanel Margin="8"`, `lbl_teachStatus` **다음**
- `WPF_Example/Custom/UI/TrayVisionView.xaml` **121~145줄** `GroupBox Grid.Row="3" Header="티칭 (ROI 2개)"` 안의 `StackPanel`, `lbl_teachStatus` **다음**
- 두 파일 모두 루트 `UserControl` 에 `xmlns:ui="clr-namespace:ReringProject.UI"` 를 추가해야 한다
- **`RowDefinition` 을 새로 추가하지 않는다** — 기존 GroupBox 안에 넣으므로 Grid.Row 번호가 하나도 바뀌지 않는다(과거 Row 번호 재배치가 사고의 원인이었다)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: AlignShapeMatchService 에 GetModelPathsForMask 추가</name>
  <files>WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs</files>

  <read_first>
    - `WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs` **70~135줄** (`BuildShmPath` / `GetShmPath` / `BuildJsonPath`)
    - 같은 파일 **296~315줄** `HasTemplate` — 공개 메서드 작성 관습 + "BuildShmPath 사용, IO 부작용 없음" 주석
  </read_first>

  <action>
`WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs` 의 `HasTemplate` 메서드 **바로 아래**에 추가한다. **K&R 스타일.**

```csharp
        // 브러시 마스크를 붙일 대상 모델 파일 경로 2개(_1.shm, _2.shm)를 돌려준다(Phase 74 D-74-02).
        //  BuildShmPath 를 쓰는 이유 = 순수 문자열 도출이라 폴더를 만들지 않는다.
        //  GetShmPath 는 Directory.CreateDirectory 부작용이 있어 '조회' 용도로는 쓰면 안 된다.
        public IList<string> GetModelPathsForMask(EEthernetVisionMode mode, EBottomAlignSlot slot = EBottomAlignSlot.None) {
            List<string> list = new List<string>();
            if (mode == EEthernetVisionMode.None) {
                return list;
            }
            string szPath1 = BuildShmPath(mode, 1, slot);
            string szPath2 = BuildShmPath(mode, 2, slot);
            if (string.IsNullOrEmpty(szPath1) == false) {
                list.Add(szPath1);
            }
            if (string.IsNullOrEmpty(szPath2) == false) {
                list.Add(szPath2);
            }
            return list;
        }
```

`using System.Collections.Generic;` 가 파일 상단에 없으면 추가한다(있으면 그대로 둔다).

**절대 하지 말 것**: `BuildShmPath` / `GetShmPath` / `BuildJsonPath` / `HasTemplate` / `TryTeach` 본문 수정.
  </action>

  <verify>
    <automated>cd /c/Info/Project/DataMeasurement; grep -c "public IList<string> GetModelPathsForMask" WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs; awk '/public IList<string> GetModelPathsForMask/,/^        }$/' WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs | grep -c "GetShmPath"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "public IList<string> GetModelPathsForMask" WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs` 가 `1`
    - `awk '/public IList<string> GetModelPathsForMask/,/^        }$/' ... | grep -c "BuildShmPath"` 가 `2`
    - `awk '/public IList<string> GetModelPathsForMask/,/^        }$/' ... | grep -c "GetShmPath"` 가 `0` (**부작용 있는 헬퍼를 쓰지 않았다**)
    - `grep -c "Directory.CreateDirectory" WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs` 값이 편집 전 실측값과 동일 (편집 전에 먼저 실측)
    - `grep -c "public bool TryTeach" WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs` 가 `4` (오버로드 4종 그대로)
    - `grep -c " ? " WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs` 값이 편집 전 실측값과 동일
  </acceptance_criteria>

  <done>Align 모델 2개 경로를 폴더 생성 부작용 없이 조회하는 공개 메서드가 존재한다.</done>
</task>

<task type="auto">
  <name>Task 2: BottomVisionView 에 브러시 패널 배치 + 훅 배선 + 무모달 재티칭</name>
  <files>WPF_Example/Custom/UI/BottomVisionView.xaml, WPF_Example/Custom/UI/BottomVisionView.xaml.cs</files>

  <read_first>
    - `WPF_Example/Custom/UI/BottomVisionView.xaml` **1~10줄**(xmlns) 과 **135~159줄**(티칭 GroupBox)
    - `WPF_Example/Custom/UI/BottomVisionView.xaml.cs` **28~70줄**(상수/필드), **92~135줄**(`AttachSharedViewer`), **222~270줄**(`SlotComboBox_SelectionChanged`), **639~715줄**(`TeachButton_Click`), **780~840줄**(`ValidateRois` / `RectToTeachParams`)
    - `.planning/phases/74-pattern-model-brush-masking/74-03-SUMMARY.md` — 확정된 VM 시그니처
  </read_first>

  <action>
**(A) XAML** — `WPF_Example/Custom/UI/BottomVisionView.xaml`

1. 루트 `UserControl` 여는 태그의 `xmlns:mc=...` 다음 줄에 `xmlns:ui="clr-namespace:ReringProject.UI"` 를 추가한다.
2. `GroupBox Grid.Row="3" Header="티칭 (선택 슬롯 · ROI 2개)"` 안 `StackPanel Margin="8"` 의 `lbl_teachStatus` **바로 다음**에 추가한다:

```xml
                            <!--Phase 74: 브러시 마스킹 패널. HALCON 창 밖(좌측 WPF 패널)이라 airspace 안전.-->
                            <ui:PatternBrushPanel x:Name="brushPanel" Margin="0,8,0,0"/>
```

`Grid.RowDefinitions` 와 다른 `Grid.Row` 번호는 **한 글자도 건드리지 않는다.**

**(B) code-behind** — `WPF_Example/Custom/UI/BottomVisionView.xaml.cs` (**K&R 스타일**)

1. `AttachSharedViewer` 의 `ViewerHostBorder.Child = viewer;` **다음 줄**에 브러시 배선을 추가한다:

```csharp
            // Phase 74 브러시 마스킹 배선. 훅 2개만 채우면 저장/재생성/상태문구는 ViewModel 이 처리한다.
            if (brushPanel != null) {
                brushPanel.ViewModel.ModelPathsProvider = () => EthernetVisionHandler.Handle.Matcher.GetModelPathsForMask(VIEW_MODE, _selectedSlot);
                brushPanel.ViewModel.ModelRegenerator = RegenerateTeachSilent;
                brushPanel.ViewModel.Attach(viewer);
                brushPanel.ViewModel.ReloadMaskFromDisk();
            }
```

2. 무모달 재티칭 메서드를 `TeachButton_Click` **바로 아래**에 추가한다:

```csharp
        // 마스크가 바뀌었을 때 모달 없이 같은 ROI 로 다시 티칭한다(D-74-04).
        //  TryTeach 가 모델 재생성 + ref pose 재기록을 전부 담당하므로 여기서 새 로직을 만들지 않는다.
        //  성공하면 null, 실패하면 오류 문자열을 돌려준다(ViewModel 계약).
        private string RegenerateTeachSilent() {
            if (_viewer == null) {
                return "뷰어 없음";
            }
            if (_viewer.CurrentImage == null) {
                return "이미지 없음 — Grab 먼저";
            }
            if (_selectedSlot == EBottomAlignSlot.None) {
                return "면 슬롯을 먼저 선택하세요";
            }
            string szValid = ValidateRois();
            if (szValid != null) {
                return szValid;
            }

            double r1, c1, phi1, l1_1, l1_2;
            RectToTeachParams(_roi1, out r1, out c1, out phi1, out l1_1, out l1_2);
            double r2, c2, phi2, l2_1, l2_2;
            RectToTeachParams(_roi2, out r2, out c2, out phi2, out l2_1, out l2_2);

            string szError;
            double dScore1, dScore2;
            bool bOk = EthernetVisionHandler.Handle.Matcher.TryTeach(
                _viewer.CurrentImage,
                r1, c1, phi1, l1_1, l1_2,
                r2, c2, phi2, l2_1, l2_2,
                VIEW_MODE, _selectedSlot,
                out dScore1, out dScore2,
                out szError);
            if (bOk == true) {
                return null;
            }
            return szError;
        }
```

3. `SlotComboBox_SelectionChanged` 의 `RefreshStatus();` **다음 줄**에 추가한다:

```csharp
                if (brushPanel != null) {
                    brushPanel.ViewModel.ReloadMaskFromDisk(); // 슬롯이 바뀌면 그 슬롯의 마스크를 화면에 다시 올린다
                }
```

4. `TeachButton_Click` 의 성공 분기에서 `_slotRois[_selectedSlot] = ...` **다음 줄**에 추가한다:

```csharp
                    if (brushPanel != null) {
                        brushPanel.ViewModel.ReloadMaskFromDisk(); // 새로 티칭한 슬롯의 마스크 상태를 화면과 맞춘다
                    }
```

**절대 하지 말 것**
- `TeachButton_Click` / `RunButton_Click` / 캘리브 핸들러의 **기존 줄 수정**
- `Grid.Row` 번호 변경 또는 `RowDefinition` 추가
- `_viewer.RectDrawingCompleted` 구독 변경
- 브러시 관련 로직을 `RegenerateTeachSilent` 밖에 흩뿌리기
  </action>

  <verify>
    <automated>cd /c/Info/Project/DataMeasurement; grep -c 'xmlns:ui="clr-namespace:ReringProject.UI"' WPF_Example/Custom/UI/BottomVisionView.xaml; grep -c "ui:PatternBrushPanel" WPF_Example/Custom/UI/BottomVisionView.xaml; grep -c "brushPanel.ViewModel.ReloadMaskFromDisk();" WPF_Example/Custom/UI/BottomVisionView.xaml.cs; grep -c "private string RegenerateTeachSilent()" WPF_Example/Custom/UI/BottomVisionView.xaml.cs</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'xmlns:ui="clr-namespace:ReringProject.UI"' WPF_Example/Custom/UI/BottomVisionView.xaml` 가 `1`
    - `grep -c "ui:PatternBrushPanel" WPF_Example/Custom/UI/BottomVisionView.xaml` 가 `1`
    - `grep -c 'x:Name="brushPanel"' WPF_Example/Custom/UI/BottomVisionView.xaml` 가 `1`
    - **레이아웃 무변경**: `grep -c "<RowDefinition" WPF_Example/Custom/UI/BottomVisionView.xaml` 가 편집 전 실측값 `9` 와 동일이며, `grep -o 'Grid.Row="[0-9]"' WPF_Example/Custom/UI/BottomVisionView.xaml | sort | uniq -c` 결과가 편집 전과 동일하다(편집 전에 먼저 실측해 저장할 것)
    - `grep -c "private string RegenerateTeachSilent()" WPF_Example/Custom/UI/BottomVisionView.xaml.cs` 가 `1`
    - `grep -c "brushPanel.ViewModel.ModelPathsProvider" WPF_Example/Custom/UI/BottomVisionView.xaml.cs` 가 `1`
    - `grep -c "brushPanel.ViewModel.ModelRegenerator = RegenerateTeachSilent;" WPF_Example/Custom/UI/BottomVisionView.xaml.cs` 가 `1`
    - `grep -c "brushPanel.ViewModel.Attach(viewer);" WPF_Example/Custom/UI/BottomVisionView.xaml.cs` 가 `1`
    - `grep -c "brushPanel.ViewModel.ReloadMaskFromDisk();" WPF_Example/Custom/UI/BottomVisionView.xaml.cs` 가 `3` (Attach 1 + 슬롯전환 1 + 티칭성공 1)
    - **무모달**: `awk '/private string RegenerateTeachSilent/,/^        }$/' WPF_Example/Custom/UI/BottomVisionView.xaml.cs | grep -c "CustomMessageBox"` 가 `0`
    - **기존 호출 무증가**: `grep -c "Matcher.TryTeach(" WPF_Example/Custom/UI/BottomVisionView.xaml.cs` 가 `2` (기존 TeachButton_Click 1 + 신규 RegenerateTeachSilent 1). 편집 전 값이 `1` 인지 먼저 실측할 것
    - `grep -c " ? " WPF_Example/Custom/UI/BottomVisionView.xaml.cs` 값이 편집 전 실측값과 동일
  </acceptance_criteria>

  <done>Bottom Align 화면에 브러시 패널이 보이고, 칠하기 종료 시 같은 ROI 로 모달 없이 재티칭되며, 슬롯 전환 시 마스크가 재로드된다.</done>
</task>

<task type="auto">
  <name>Task 3: TrayVisionView 동일 배선 + 빌드 검증</name>
  <files>WPF_Example/Custom/UI/TrayVisionView.xaml, WPF_Example/Custom/UI/TrayVisionView.xaml.cs</files>

  <read_first>
    - `WPF_Example/Custom/UI/TrayVisionView.xaml` **1~10줄**(xmlns) 과 **121~145줄**(티칭 GroupBox)
    - `WPF_Example/Custom/UI/TrayVisionView.xaml.cs` **28~60줄**(상수/필드), **70~95줄**(`AttachSharedViewer`, `TrayVisionView_Loaded`), **459~520줄**(`TeachButton_Click`), **780~840줄**(`ValidateRois` / `RectToTeachParams`)
    - Task 2 결과물(같은 구조를 슬롯 없이 반복한다)
  </read_first>

  <action>
Task 2 와 **동일한 패턴**을 Tray 에 적용한다. Tray 는 슬롯이 없으므로 항상 `EBottomAlignSlot.None` 을 쓰고, `TryTeach` 는 **슬롯 없는 score 오버로드**(`AlignShapeMatchService.cs:446`)를 호출한다.

**(A) XAML** — `WPF_Example/Custom/UI/TrayVisionView.xaml`
1. 루트 `UserControl` 에 `xmlns:ui="clr-namespace:ReringProject.UI"` 추가
2. `GroupBox Grid.Row="3" Header="티칭 (ROI 2개)"` 안의 `lbl_teachStatus` **바로 다음**에:

```xml
                            <!--Phase 74: 브러시 마스킹 패널. HALCON 창 밖(좌측 WPF 패널)이라 airspace 안전.-->
                            <ui:PatternBrushPanel x:Name="brushPanel" Margin="0,8,0,0"/>
```

`RowDefinition` / `Grid.Row` 는 건드리지 않는다.

**(B) code-behind** — `WPF_Example/Custom/UI/TrayVisionView.xaml.cs` (**K&R**)

1. `AttachSharedViewer` 의 `ViewerHostBorder.Child = viewer;` 다음 줄:

```csharp
            // Phase 74 브러시 마스킹 배선. Tray 는 슬롯이 없어 항상 None 경로를 쓴다.
            if (brushPanel != null) {
                brushPanel.ViewModel.ModelPathsProvider = () => EthernetVisionHandler.Handle.Matcher.GetModelPathsForMask(VIEW_MODE, EBottomAlignSlot.None);
                brushPanel.ViewModel.ModelRegenerator = RegenerateTeachSilent;
                brushPanel.ViewModel.Attach(viewer);
                brushPanel.ViewModel.ReloadMaskFromDisk();
            }
```

2. `TeachButton_Click` 바로 아래에:

```csharp
        // 마스크가 바뀌었을 때 모달 없이 같은 ROI 로 다시 티칭한다(D-74-04).
        //  성공하면 null, 실패하면 오류 문자열을 돌려준다(ViewModel 계약).
        private string RegenerateTeachSilent() {
            if (_viewer == null) {
                return "뷰어 없음";
            }
            if (_viewer.CurrentImage == null) {
                return "이미지 없음 — Grab 먼저";
            }
            string szValid = ValidateRois();
            if (szValid != null) {
                return szValid;
            }

            double r1, c1, phi1, l1_1, l1_2;
            RectToTeachParams(_roi1, out r1, out c1, out phi1, out l1_1, out l1_2);
            double r2, c2, phi2, l2_1, l2_2;
            RectToTeachParams(_roi2, out r2, out c2, out phi2, out l2_1, out l2_2);

            string szError;
            double dScore1, dScore2;
            bool bOk = EthernetVisionHandler.Handle.Matcher.TryTeach(
                _viewer.CurrentImage,
                r1, c1, phi1, l1_1, l1_2,
                r2, c2, phi2, l2_1, l2_2,
                VIEW_MODE,
                out dScore1, out dScore2,
                out szError);
            if (bOk == true) {
                return null;
            }
            return szError;
        }
```

3. `TeachButton_Click` 성공 분기의 `lbl_teachStatus.Foreground = TeachDiag.GradeBrush(teachGrade);` **다음 줄**에:

```csharp
                    if (brushPanel != null) {
                        brushPanel.ViewModel.ReloadMaskFromDisk();
                    }
```

`EBottomAlignSlot` 이 이 파일에 already `using` 되어 있는지 확인하고, 없으면 필요한 `using` 을 추가한다.

**빌드 검증** (전체 통합):

```bash
cd /c/Info/Project/DataMeasurement
SCR="/c/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/7fe7f8e3-9e05-4190-9941-3b1cfdde3971/scratchpad"
SCRW=$(cygpath -w "$SCR")
MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"
rm -rf "$SCR/obj74d" "$SCR/out74d"
"$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 "-p:OutputPath=$SCRW\\out74d\\" "-p:IntermediateOutputPath=$SCRW\\obj74d\\" -v:m -nologo > "$SCR/build74d.log" 2>&1
grep -c ": error " "$SCR/build74d.log"
grep -o "warning CS[0-9]*" "$SCR/build74d.log" | sort | uniq -c
```
  </action>

  <verify>
    <automated>cd /c/Info/Project/DataMeasurement; SCR="/c/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/7fe7f8e3-9e05-4190-9941-3b1cfdde3971/scratchpad"; SCRW=$(cygpath -w "$SCR"); MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"; rm -rf "$SCR/obj74d" "$SCR/out74d"; "$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 "-p:OutputPath=$SCRW\\out74d\\" "-p:IntermediateOutputPath=$SCRW\\obj74d\\" -v:m -nologo > "$SCR/build74d.log" 2>&1; grep -c ": error " "$SCR/build74d.log"; grep -o "warning CS[0-9]*" "$SCR/build74d.log" | sort | uniq -c</automated>
  </verify>

  <acceptance_criteria>
    - 빌드 에러 `0`
    - `grep -o "warning CS[0-9]*" "$SCR/build74d.log" | sort -u` 가 `warning CS0162` 와 `warning CS0618` **두 종류뿐**
    - `grep -c 'xmlns:ui="clr-namespace:ReringProject.UI"' WPF_Example/Custom/UI/TrayVisionView.xaml` 가 `1`
    - `grep -c 'x:Name="brushPanel"' WPF_Example/Custom/UI/TrayVisionView.xaml` 가 `1`
    - `grep -c "<RowDefinition" WPF_Example/Custom/UI/TrayVisionView.xaml` 가 편집 전 실측값 `8` 과 동일
    - `grep -c "private string RegenerateTeachSilent()" WPF_Example/Custom/UI/TrayVisionView.xaml.cs` 가 `1`
    - `grep -c "brushPanel.ViewModel.ReloadMaskFromDisk();" WPF_Example/Custom/UI/TrayVisionView.xaml.cs` 가 `2` (Attach 1 + 티칭성공 1)
    - `awk '/private string RegenerateTeachSilent/,/^        }$/' WPF_Example/Custom/UI/TrayVisionView.xaml.cs | grep -c "EBottomAlignSlot"` 가 `0` (슬롯 없는 오버로드를 썼다)
    - `grep -c "Matcher.TryTeach(" WPF_Example/Custom/UI/TrayVisionView.xaml.cs` 가 `2`
    - `git diff --stat WPF_Example/Halcon/ WPF_Example/UI/ WPF_Example/Setting/` 이 비어 있다 (이 plan 은 Align 쪽만 건드린다)
  </acceptance_criteria>

  <done>Tray Align 화면에도 같은 패널이 붙었고, 전체 솔루션이 에러 0 으로 빌드된다.</done>
</task>

</tasks>

<verification>
1. 빌드 에러 0, 경고 코드 종류 CS0618/CS0162 두 종류뿐
2. `GetModelPathsForMask` 가 `GetShmPath`(폴더 생성)를 쓰지 않는다
3. 두 XAML 의 `RowDefinition` 수와 `Grid.Row` 분포가 편집 전과 동일
4. `RegenerateTeachSilent` 안에 `CustomMessageBox` 0건
5. `Matcher.TryTeach(` 호출이 각 파일에서 2회(기존 1 + 신규 1)
</verification>

<success_criteria>
- 브러시 패널을 쓰지 않으면 Bottom/Tray 의 기존 티칭·검사 코드 경로가 편집 전과 동일하다
- 마스크가 슬롯별로 올바른 모델(`Bottom_{token}_1.shm` / `_2.shm`) 옆에 저장된다
- 재생성이 기존 `TryTeach` 재사용으로 이루어져 중복 구현이 없다
</success_criteria>

<output>
완료 후 `.planning/phases/74-pattern-model-brush-masking/74-04-SUMMARY.md` 를 작성한다.
편집 전 실측값(두 XAML 의 RowDefinition 수와 Grid.Row 분포, 각 .cs 의 `Matcher.TryTeach(` 카운트, `" ? "` 카운트, `Directory.CreateDirectory` 카운트)을 반드시 기록한다.
</output>
