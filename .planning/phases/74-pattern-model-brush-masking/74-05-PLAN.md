---
phase: 74-pattern-model-brush-masking
plan: 05
type: execute
wave: 3
depends_on: [74-03]
files_modified:
  - WPF_Example/Halcon/Services/DatumPatternModelRegenService.cs
  - WPF_Example/UI/ContentItem/MainView.xaml
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [D-74-01, D-74-03, D-74-04]

must_haves:
  truths:
    - "Datum 티칭 화면에서 [브러시] 를 누르면 창 밖 패널이 열린다"
    - "칠하기를 놓으면 그 Datum 의 패턴 모델이 마스크를 반영해 다시 만들어진다"
    - "패턴 2 가 설정된 Datum 은 두 모델 모두 마스크 대상이 된다"
    - "Datum 을 바꾸면 그 Datum 의 마스크가 화면에 다시 뜬다"
    - "브러시를 쓰지 않으면 [패턴 모델 생성] 버튼 흐름이 기존과 동일하다"
  artifacts:
    - path: "WPF_Example/Halcon/Services/DatumPatternModelRegenService.cs"
      provides: "Datum 패턴 모델 경로 조회 + 모달 없는 재생성"
      contains: "RegenerateSilent"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml"
      provides: "툴바 [브러시] 토글 + 툴바 아래 브러시 패널(HWND 밖)"
      contains: "PatternBrushPanel"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "얇은 배선만 — 훅 2개 + 선택 변경 시 재로드"
      contains: "brushPanel.ViewModel"
  key_links:
    - from: "MainView.PublishDatumRoiCandidates"
      to: "brushPanel.ViewModel.ReloadMaskFromDisk"
      via: "Datum 선택 변경 시 마스크 재로드"
      pattern: "ReloadMaskFromDisk\\(\\)"
    - from: "DatumPatternModelRegenService.RegenerateSilent"
      to: "PatternMatchService.TryCreateModel"
      via: "모달 없는 재생성 (D-74-04)"
      pattern: "TryCreateModel\\("
---

<objective>
**Datum 티칭 화면(MainView)** 에 브러시 패널을 붙인다. D-74-03 이 `TryCreateModel` 한 곳만 고쳐 Datum 까지 자동 커버하므로, 여기서는 "칠할 수 있게 하는 UI" 만 붙이면 된다.

Purpose: `MainView.xaml.cs` 는 4,476줄이다. 새 로직을 여기 넣지 않기 위해 재생성 로직을 별도 서비스로 빼고, code-behind 에는 배선만 남긴다.
Output: `DatumPatternModelRegenService.cs`(신규), 툴바 [브러시] 토글 + 패널 배치, 훅 배선.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-pattern-model-brush-masking/74-CONTEXT.md
@.planning/phases/74-pattern-model-brush-masking/74-03-SUMMARY.md
</context>

<coding_rules>
- **삼항 `?:` 금지** → `if/else`. **`??` `?.` 금지** → 명시 분기. (람다 `=>` 는 허용)
- **전통 `switch` 만**. **C# 7.2 문법만.**
- 긴 조건은 이름 있는 `bool` 로 선추출.
- 헝가리언: `b` `n` `sz` `d`.
- **중괄호 스타일**: `MainView.xaml.cs` 는 **K&R**. 신규 `Halcon/Services/*.cs` 는 **Allman**.
- 날짜 주석 새로 달지 않는다.
- **`MainView.xaml.cs` 에 새 "로직" 을 넣지 않는다.** 허용되는 것은 (a) 훅 대입, (b) 서비스 호출 1줄 래퍼, (c) 라벨 갱신 3줄뿐. 조건 분기가 들어간 계산 로직은 전부 `DatumPatternModelRegenService` 로 보낸다.
- **모달 대화상자 금지**(칠할 때마다 팝업이 뜨면 못 쓴다).
- 실행 중 프로세스 종료 금지. 빌드는 스크래치 OutDir.
- **`WPF_Example/DatumMeasurement.csproj` 는 커밋 금지**(수정만, unstaged 유지).
</coding_rules>

<interfaces>
### Plan 03 이 만든 것

```csharp
// namespace ReringProject.UI
public partial class PatternBrushPanel : UserControl { public PatternBrushMaskViewModel ViewModel { get; } }

public class PatternBrushMaskViewModel : INotifyPropertyChanged
{
    public Func<IList<string>> ModelPathsProvider { get; set; }
    public Func<string> ModelRegenerator { get; set; }   // 성공=null, 실패=오류 문자열
    public void Attach(MainResultViewerControl viewer);
    public void Detach();
    public void ClearMask();
    public void ReloadMaskFromDisk();
    public void RefreshStatus();
}
```

### 기존 Datum 경로 (그대로 재사용 — 새 경로 규약 금지)

`ReringProject.Sequence.InspectionSequence` (static)
```csharp
public static string ResolveDatumModelPath(DatumConfig datum, string ownerSeqName);
public static string ResolveDatumModelPath2(DatumConfig datum, string ownerSeqName);  // propertyName 에 "_2"
```
두 함수 모두 내부에서 `NormalizeModelFolderName` 을 거쳐 `RecipeFiles.GetPatternModelFilePath` 를 호출한다. **직접 경로를 조립하지 말 것.**

`ReringProject.Halcon.Algorithms.PatternMatchService` (인스턴스)
```csharp
public bool TryCreateModel(HImage templateImage, double roiRow, double roiCol, double roiPhi,
                           double roiLen1, double roiLen2, string engine, double angleExtentDeg,
                           string modelPath, out string error);

public bool TryFindPose(HImage runtimeImage, string engine, string modelPath,
                        double roiRow, double roiCol, double roiLen1, double roiLen2,
                        double marginPx, double minScore, double downsampleFactor,
                        out double curRow, out double curCol, out double curAngleDeg, out double curScore,
                        out string error, double angleExtentDeg = 180.0);
```

`ReringProject.Sequence.DatumConfig` 사용 멤버 (전부 기존):
`OwnerName`, `EnsurePerRoiDefaults()`, `RaisePropertyChanged(string)`,
`PatternRoi_Row/_Col/_Phi/_Length1/_Length2`, `PatternRoi2_Row/_Col/_Phi/_Length1/_Length2`,
`PatternEngine`, `PatternAngleExtentDeg`, `PatternSearchMarginPx`, `PatternMinScore`, `FindAngleExtentDeg`,
`RefMatchRow/RefMatchCol/RefMatchAngleDeg`, `RefMatch2Row/RefMatch2Col`

### 기존 MainView 참고 코드 (복제 원본)

`WPF_Example/UI/ContentItem/MainView.xaml.cs` **4008~4140줄** `InvokeCreatePatternModel()` — 모달(`CustomMessageBox`)이 잔뜩 붙은 버전. 신규 서비스는 **이 흐름에서 모달과 Recipe Save 확인만 걷어낸 것**이다. 계산/호출 순서는 그대로 따른다.

`WPF_Example/UI/ContentItem/MainView.xaml.cs` **3996~4000줄** — `label_drawHint` 갱신 관습:
```csharp
            label_drawHint.Content = "...";
            label_drawHint.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FF4ADE80"));
            label_drawHint.Visibility = Visibility.Visible;
```

### XAML 배치 (airspace 안전 — 반드시 이 방식)

`WPF_Example/UI/ContentItem/MainView.xaml`
- 루트에 이미 `xmlns:dmv="clr-namespace:ReringProject.UI"` 가 있다(7줄). **새 xmlns 를 추가할 필요가 없다.**
- **143~150줄** `Border x:Name="canvasToolbar" Grid.Row="0"` 안의 `Grid` 에는 `ColumnDefinitions` 3개만 있고 **`RowDefinitions` 가 없다.** 여기에 `RowDefinitions` 2개(Auto/Auto)를 추가하면 기존 자식들은 `Grid.Row` 속성이 없으므로 **전부 Row 0 에 그대로 남는다**(재배치 0). 새 패널만 `Grid.Row="1" Grid.ColumnSpan="3"` 으로 둔다.
- **434줄** `Grid Grid.Row="1"` 은 `halconViewer`(HWND)를 담는다. **여기에는 아무것도 넣지 않는다** — WPF 요소를 얹으면 airspace 로 가려진다.
- 루트 Grid 의 `RowDefinitions`(133~140줄)와 모든 `Grid.Row` 번호는 **한 글자도 건드리지 않는다.**
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DatumPatternModelRegenService 신규 — 경로 조회 + 모달 없는 재생성</name>
  <files>WPF_Example/Halcon/Services/DatumPatternModelRegenService.cs, WPF_Example/DatumMeasurement.csproj</files>

  <read_first>
    - `WPF_Example/UI/ContentItem/MainView.xaml.cs` **4008~4140줄** `InvokeCreatePatternModel()` 전체 — 복제 원본
    - `WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` **2265~2300줄**(`ResolveDatumModelPath`) 과 **2340~2365줄**(`ResolveDatumModelPath2`)
    - `WPF_Example/Halcon/Algorithms/PatternMatchService.cs` **118~236줄**(`TryCreateModel`) 과 **372~410줄**(`TryFindPose` 시그니처)
    - `WPF_Example/Halcon/Services/PatternMaskService.cs` (Plan 01 산출물)
  </read_first>

  <action>
신규 파일 `WPF_Example/Halcon/Services/DatumPatternModelRegenService.cs`. **Allman**, `namespace ReringProject.Halcon.Services`, `public static class DatumPatternModelRegenService`.

이 파일에 두는 이유: `MainView.xaml.cs` 는 4,476줄이라 새 로직을 더 넣으면 안 된다. `PatternMaskService` 와 같은 폴더에 둬서 "패턴 모델 주변 서비스"를 한곳에 모은다.

using: `System`, `System.Collections.Generic`, `HalconDotNet`, `ReringProject.Halcon.Algorithms`, `ReringProject.Sequence`.

```csharp
        /// 이 Datum 의 브러시 마스크가 붙을 모델 파일 경로. 패턴 2 가 설정돼 있으면 2개.
        ///  경로 조립을 직접 하지 않고 기존 단일 소스(ResolveDatumModelPath/2)만 쓴다 —
        ///  폴더 규약이 갈리면 모델을 조용히 못 찾는 사고가 난다(Phase 73 전례).
        public static IList<string> GetModelPathsForMask(DatumConfig datum)
        {
            List<string> list = new List<string>();
            if (datum == null)
            {
                return list;
            }

            string szPath1 = InspectionSequence.ResolveDatumModelPath(datum, datum.OwnerName);
            if (string.IsNullOrEmpty(szPath1) == false)
            {
                list.Add(szPath1);
            }

            bool bHasPattern2 = (datum.PatternRoi2_Length1 > 0.0) && (datum.PatternRoi2_Length2 > 0.0);
            if (bHasPattern2 == true)
            {
                string szPath2 = InspectionSequence.ResolveDatumModelPath2(datum, datum.OwnerName);
                if (string.IsNullOrEmpty(szPath2) == false)
                {
                    list.Add(szPath2);
                }
            }
            return list;
        }
```

```csharp
        /// 모달 없이 패턴 1(+ 패턴 2) 모델을 다시 만들고 RefMatch 를 재기록한다(D-74-04).
        ///  MainView.InvokeCreatePatternModel 의 계산 흐름을 그대로 따르되 CustomMessageBox 와
        ///  Recipe Save 확인만 제거한 것이다. 성공하면 null, 실패하면 사람이 읽을 오류 문자열을 돌려준다.
        ///  ※ .shm 은 즉시 디스크에 쓰이지만 RefMatch 는 메모리 값이라 사용자가 Recipe Save 를 해야 영속된다.
        public static string RegenerateSilent(DatumConfig datum, HImage templateImage)
```

본문 순서(반드시 이대로):

1. `datum == null` → `"Datum 을 먼저 선택하세요"`
2. `templateImage == null` → `"이미지가 없습니다. 먼저 Grab 또는 Load Image 를 수행하세요."`
3. `datum.EnsurePerRoiDefaults();` — sentinel 0 이면 `PatternAngleExtentDeg` 가 0 이 되어 0° 전용 모델이 나온다(기존 hotfix 이유). 빠뜨리지 말 것.
4. `bool bRoiMissing = (datum.PatternRoi_Length1 <= 0.0) || (datum.PatternRoi_Length2 <= 0.0);` → true 면 `"패턴 ROI(Rect) 를 먼저 그리세요."`
5. `string szModelPath = InspectionSequence.ResolveDatumModelPath(datum, datum.OwnerName);` 비었으면 `"모델 경로 도출 실패 (레시피/Shot 확인)"`
6. `PatternMatchService svc = new PatternMatchService();`
   `TryCreateModel(templateImage, PatternRoi_Row, PatternRoi_Col, PatternRoi_Phi, PatternRoi_Length1, PatternRoi_Length2, PatternEngine, PatternAngleExtentDeg, szModelPath, out szError)` → false 면 `szError` 반환
7. `TryFindPose(templateImage, PatternEngine, szModelPath, PatternRoi_Row, PatternRoi_Col, PatternRoi_Length1, PatternRoi_Length2, PatternSearchMarginPx, PatternMinScore, 1.0, out dRow, out dCol, out dAngle, out dScore, out szRefError, datum.FindAngleExtentDeg)` → false 면 `"기준 위치 기록 실패: " + szRefError`
   성공이면 `datum.RefMatchRow = dRow; datum.RefMatchCol = dCol; datum.RefMatchAngleDeg = dAngle;`
8. 패턴 2 가 설정된 경우(`bHasPattern2`)만:
   `ResolveDatumModelPath2` → `TryCreateModel`(패턴2 파라미터) → `TryFindPose`(패턴2 파라미터) → `datum.RefMatch2Row/RefMatch2Col` 기록.
   **패턴 2 실패는 전체 실패로 만들지 않는다** — 기존 `InvokeCreatePatternModel` 도 단일 패턴 폴백으로 계속 진행한다. 실패 사유를 `szPattern2Warning` 문자열에 담아 둔다.
9. `try { datum.RaisePropertyChanged(string.Empty); } catch { }`
10. `szPattern2Warning` 이 비어 있으면 `return null;`, 아니면 `return "패턴 1 성공, 패턴 2 경고: " + szPattern2Warning;`
    — 단, ViewModel 계약상 **null 이 아닌 반환은 실패로 표시**되므로, 패턴 2 경고는 이 문자열 그대로 상태 문구에 나온다. 그게 의도다(사용자가 알아야 한다).

`downsampleFactor` 는 **반드시 `1.0`** 을 넘긴다(티칭 시 원본 해상도로 확인하는 기존 규약).

csproj 에 `Halcon\Services\DatumPatternModelRegenService.cs` Compile 항목을 `PatternMaskService.cs` 항목 옆에 추가한다. **커밋하지 않는다.**
  </action>

  <verify>
    <automated>cd /c/Info/Project/DataMeasurement; grep -c "public static IList<string> GetModelPathsForMask(DatumConfig datum)" WPF_Example/Halcon/Services/DatumPatternModelRegenService.cs; grep -c "public static string RegenerateSilent(DatumConfig datum, HImage templateImage)" WPF_Example/Halcon/Services/DatumPatternModelRegenService.cs; grep -F -c 'DatumPatternModelRegenService.cs' WPF_Example/DatumMeasurement.csproj</automated>
  </verify>

  <acceptance_criteria>
    - 위 3개 grep 이 모두 `1`
    - `grep -c "InspectionSequence.ResolveDatumModelPath(" WPF_Example/Halcon/Services/DatumPatternModelRegenService.cs` 가 `2` (경로조회 1 + 재생성 1)
    - `grep -c "InspectionSequence.ResolveDatumModelPath2(" WPF_Example/Halcon/Services/DatumPatternModelRegenService.cs` 가 `2`
    - `grep -c "GetPatternModelFilePath\|Path.Combine" WPF_Example/Halcon/Services/DatumPatternModelRegenService.cs` 가 `0` (**경로를 직접 조립하지 않았다**)
    - `grep -c "TryCreateModel(" WPF_Example/Halcon/Services/DatumPatternModelRegenService.cs` 가 `2` (패턴1 + 패턴2)
    - `grep -c "TryFindPose(" WPF_Example/Halcon/Services/DatumPatternModelRegenService.cs` 가 `2`
    - `grep -c "EnsurePerRoiDefaults();" WPF_Example/Halcon/Services/DatumPatternModelRegenService.cs` 가 `1`
    - `grep -c "CustomMessageBox" WPF_Example/Halcon/Services/DatumPatternModelRegenService.cs` 가 `0` (**모달 금지**)
    - `grep -c "SaveRecipe" WPF_Example/Halcon/Services/DatumPatternModelRegenService.cs` 가 `0`
    - `grep -c " ? " WPF_Example/Halcon/Services/DatumPatternModelRegenService.cs` 가 `0`, `grep -c '??' ...` 가 `0`
    - `git status --porcelain WPF_Example/DatumMeasurement.csproj` 이 ` M` 으로 시작(unstaged)
  </acceptance_criteria>

  <done>Datum 패턴 모델 경로 조회와 모달 없는 재생성이 MainView 밖의 서비스로 존재한다.</done>
</task>

<task type="auto">
  <name>Task 2: MainView 툴바에 [브러시] 토글 + 패널 배치 (airspace 안전)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml</files>

  <read_first>
    - `WPF_Example/UI/ContentItem/MainView.xaml` **1~10줄**(xmlns — `dmv` 가 이미 `ReringProject.UI` 를 가리킨다), **132~152줄**(루트 Grid + `canvasToolbar` 내부 Grid 의 ColumnDefinitions), **225~250줄**(`btn_createPatternModel` 버튼 정의), **405~432줄**(Column 1/2 자식들), **434~440줄**(HWND 를 담는 Row 1 Grid)
    - `WPF_Example/UI/ContentItem/MainView.xaml.cs` **3996~4005줄**(`label_drawHint` 갱신 관습)
  </read_first>

  <action>
`WPF_Example/UI/ContentItem/MainView.xaml` 만 고친다.

(1) `canvasToolbar` 안 `Grid` 의 `<Grid.ColumnDefinitions>` **바로 앞**에 RowDefinitions 를 추가한다. 기존 자식들은 `Grid.Row` 속성이 없어 자동으로 Row 0 에 남으므로 **재배치가 발생하지 않는다.**

```xml
                <!--Phase 74: 툴바 아래 줄에 브러시 패널을 붙이기 위한 Row 추가.
                     기존 자식들은 Grid.Row 속성이 없어 전부 Row 0 에 그대로 남는다(재배치 0).-->
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
```

(2) `btn_createPatternModel` 버튼 정의 **바로 다음**에 토글 버튼을 추가한다(같은 Column 0 StackPanel 안):

```xml
                    <!--Phase 74: 브러시 마스킹 패널 열기/닫기. 패널 자체는 HALCON 창 밖(툴바 아래 줄)에 있다.-->
                    <ToggleButton x:Name="btn_brushMask" Content="브러시"
                        Click="BrushMaskToggleButton_Click"
                        Style="{StaticResource RoiToggleButtonStyle}"
                        MinWidth="70" Height="28" Padding="12,4"
                        FontSize="13" FontWeight="SemiBold" Margin="0,0,4,0"
                        ToolTip="패턴 모델에서 제외할 영역을 브러시로 칠합니다. 칠하기를 마치면 모델이 자동으로 다시 만들어집니다."/>
```

(3) `canvasToolbar` 내부 Grid 의 **마지막 자식으로**(닫는 `</Grid>` 직전) 패널을 추가한다:

```xml
                <!--Phase 74: 브러시 패널. HALCON 창(Grid.Row=1) 밖이라 airspace 안전.
                     기본 숨김 — [브러시] 토글로만 연다.-->
                <dmv:PatternBrushPanel x:Name="brushPanel"
                                       Grid.Row="1" Grid.ColumnSpan="3"
                                       Visibility="Collapsed"
                                       Margin="0,6,0,0"/>
```

**절대 하지 말 것**
- 루트 Grid(133~140줄)의 `RowDefinitions` 변경 또는 `Grid.Row` 번호 변경
- `Grid Grid.Row="1"`(434줄, HWND 컨테이너) 안에 WPF 요소 추가 — airspace 로 가려진다
- 기존 버튼/라벨의 `Grid.Column` 값 변경
  </action>

  <verify>
    <automated>cd /c/Info/Project/DataMeasurement; grep -c "dmv:PatternBrushPanel" WPF_Example/UI/ContentItem/MainView.xaml; grep -c 'x:Name="btn_brushMask"' WPF_Example/UI/ContentItem/MainView.xaml; grep -c "<RowDefinition" WPF_Example/UI/ContentItem/MainView.xaml</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "dmv:PatternBrushPanel" WPF_Example/UI/ContentItem/MainView.xaml` 가 `1`
    - `grep -c 'x:Name="brushPanel"' WPF_Example/UI/ContentItem/MainView.xaml` 가 `1`
    - `grep -c 'x:Name="btn_brushMask"' WPF_Example/UI/ContentItem/MainView.xaml` 가 `1`
    - `grep -c "<RowDefinition" WPF_Example/UI/ContentItem/MainView.xaml` 가 편집 전 실측값 **+2** 이다 (편집 전 값을 먼저 실측해 기록할 것)
    - **기존 Grid.Row 분포 무변경**: `grep -o 'Grid.Row="[0-9]"' WPF_Example/UI/ContentItem/MainView.xaml | sort | uniq -c` 결과에서 `Grid.Row="0"` ~ `Grid.Row="4"` 각각의 개수가 편집 전과 동일하고, `Grid.Row="1"` 만 **1 증가**한다(새 패널). 편집 전 분포를 먼저 실측해 저장할 것
    - **기존 Grid.Column 분포 무변경**: `grep -o 'Grid.Column="[0-9]"' WPF_Example/UI/ContentItem/MainView.xaml | sort | uniq -c` 가 편집 전과 완전히 동일
    - **airspace 회피 확인**: `awk 'NR>=434 && NR<=540' WPF_Example/UI/ContentItem/MainView.xaml | grep -c "PatternBrushPanel"` 가 `0` (HWND 컨테이너 안에 없다). 줄 번호는 편집 후 `grep -n 'Grid.Row="1" Background="#FF303030"'` 로 다시 확인해 쓸 것
  </acceptance_criteria>

  <done>툴바에 [브러시] 토글이 생기고, 패널이 HALCON 창 밖 툴바 아래 줄에 숨겨진 채로 배치된다.</done>
</task>

<task type="auto">
  <name>Task 3: MainView 배선 (훅 2개 + 선택 변경 재로드) + 빌드 검증</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>

  <read_first>
    - `WPF_Example/UI/ContentItem/MainView.xaml.cs` **50~120줄**(필드, 생성자, `MainView_Loaded`)
    - 같은 파일 **2395~2440줄** `PublishDatumRoiCandidates(DatumConfig datum)` — Datum 선택 변경 진입점
    - 같은 파일 **3996~4010줄** `label_drawHint` 갱신 관습
    - 같은 파일 **4008~4030줄** `CreatePatternModelButton_Click` / `InvokeCreatePatternModel` 머리 (기존 흐름 무변경 확인용)
    - Task 1 산출물 `WPF_Example/Halcon/Services/DatumPatternModelRegenService.cs`
  </read_first>

  <action>
`WPF_Example/UI/ContentItem/MainView.xaml.cs` 에 **배선만** 추가한다. **K&R 스타일.**

(1) 필드 추가 — `_editingDatum` 필드 근처:

```csharp
        // 브러시 마스킹 대상 Datum. PublishDatumRoiCandidates(선택 변경) 에서만 갱신한다.
        private DatumConfig _brushTargetDatum;
```

(2) `MainView_Loaded` 의 `PopulateManualCycleSeqCombo();` **바로 앞**에 배선을 추가한다:

```csharp
            // Phase 74 브러시 마스킹 배선. 훅 2개만 채우면 저장/재생성/상태문구는 ViewModel 이 처리한다.
            if (brushPanel != null) {
                brushPanel.ViewModel.ModelPathsProvider = () => ReringProject.Halcon.Services.DatumPatternModelRegenService.GetModelPathsForMask(_brushTargetDatum);
                brushPanel.ViewModel.ModelRegenerator = RegenerateDatumPatternSilent;
                brushPanel.ViewModel.Attach(halconViewer);
            }
```

(3) 재생성 래퍼 + 토글 핸들러를 `CreatePatternModelButton_Click` **바로 앞**에 추가한다. 계산 로직은 서비스에 있고 여기는 호출 + 라벨뿐이다.

```csharp
        // Phase 74: 브러시 패널 열기/닫기. 패널은 HALCON 창 밖(툴바 아래 줄)이라 airspace 영향이 없다.
        private void BrushMaskToggleButton_Click(object sender, RoutedEventArgs e) {
            if (brushPanel == null) {
                return;
            }
            bool bOn = false;
            if (btn_brushMask.IsChecked == true) {
                bOn = true;
            }
            if (bOn == true) {
                brushPanel.Visibility = Visibility.Visible;
                brushPanel.ViewModel.ReloadMaskFromDisk();
            }
            else {
                brushPanel.ViewModel.IsBrushActive = false;
                brushPanel.Visibility = Visibility.Collapsed;
            }
        }

        // Phase 74 D-74-04: 칠하기가 끝나면 ViewModel 이 이 훅을 부른다. 성공=null, 실패=오류 문자열.
        //  계산은 DatumPatternModelRegenService 가 한다 — 이 메서드는 호출 + 화면 갱신만.
        private string RegenerateDatumPatternSilent() {
            string szError = ReringProject.Halcon.Services.DatumPatternModelRegenService.RegenerateSilent(
                _brushTargetDatum, halconViewer.CurrentImage);
            try { if (mParentWindow != null && mParentWindow.inspectionList != null) mParentWindow.inspectionList.RefreshParamEditor(); } catch { }
            if (szError == null) {
                label_drawHint.Content = "마스크 반영 모델 재생성 완료 — 종료 전 Recipe Save 필요 (.shm 은 저장됨, 기준값은 메모리)";
                label_drawHint.Foreground = new SolidColorBrush((Color)ColorConverter.ConvertFromString("#FF4ADE80"));
                label_drawHint.Visibility = Visibility.Visible;
            }
            return szError;
        }
```

(4) `PublishDatumRoiCandidates(DatumConfig datum)` 의 **가장 마지막 줄 다음**(메서드 닫는 중괄호 직전)에 2줄을 추가한다. 기존 줄은 하나도 고치지 않는다:

```csharp
            // Phase 74: 브러시 마스킹 대상 갱신 + 그 Datum 의 마스크를 화면에 다시 올린다.
            _brushTargetDatum = datum;
            if (brushPanel != null) { brushPanel.ViewModel.ReloadMaskFromDisk(); }
```

**절대 하지 말 것**
- `InvokeCreatePatternModel()` / `CreatePatternModelButton_Click` **본문 수정** (기존 [패턴 모델 생성] 흐름은 그대로 유지 — 회귀 0)
- `PublishDatumRoiCandidates` 의 기존 줄 수정
- 계산/판정 로직을 `MainView.xaml.cs` 에 추가

**빌드 검증** (전체 통합):

```bash
cd /c/Info/Project/DataMeasurement
SCR="/c/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/7fe7f8e3-9e05-4190-9941-3b1cfdde3971/scratchpad"
SCRW=$(cygpath -w "$SCR")
MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"
rm -rf "$SCR/obj74e" "$SCR/out74e"
"$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 "-p:OutputPath=$SCRW\\out74e\\" "-p:IntermediateOutputPath=$SCRW\\obj74e\\" -v:m -nologo > "$SCR/build74e.log" 2>&1
grep -c ": error " "$SCR/build74e.log"
grep -o "warning CS[0-9]*" "$SCR/build74e.log" | sort | uniq -c
```
  </action>

  <verify>
    <automated>cd /c/Info/Project/DataMeasurement; SCR="/c/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/7fe7f8e3-9e05-4190-9941-3b1cfdde3971/scratchpad"; SCRW=$(cygpath -w "$SCR"); MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"; rm -rf "$SCR/obj74e" "$SCR/out74e"; "$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 "-p:OutputPath=$SCRW\\out74e\\" "-p:IntermediateOutputPath=$SCRW\\obj74e\\" -v:m -nologo > "$SCR/build74e.log" 2>&1; grep -c ": error " "$SCR/build74e.log"; grep -o "warning CS[0-9]*" "$SCR/build74e.log" | sort | uniq -c</automated>
  </verify>

  <acceptance_criteria>
    - 빌드 에러 `0`
    - `grep -o "warning CS[0-9]*" "$SCR/build74e.log" | sort -u` 가 `warning CS0162` 와 `warning CS0618` **두 종류뿐**
    - `grep -c "private string RegenerateDatumPatternSilent()" WPF_Example/UI/ContentItem/MainView.xaml.cs` 가 `1`
    - `grep -c "private void BrushMaskToggleButton_Click" WPF_Example/UI/ContentItem/MainView.xaml.cs` 가 `1`
    - `grep -c "brushPanel.ViewModel" WPF_Example/UI/ContentItem/MainView.xaml.cs` 가 `6` (Loaded 3 + 토글 2 + 선택변경 1)
    - `grep -c "_brushTargetDatum" WPF_Example/UI/ContentItem/MainView.xaml.cs` 가 `4` (필드 선언 1 + ModelPathsProvider 1 + 재생성 1 + 선택변경 대입 1)
    - **기존 흐름 무변경**: `grep -c "svc.TryCreateModel(img," WPF_Example/UI/ContentItem/MainView.xaml.cs` 가 `2` (`InvokeCreatePatternModel` 의 기존 2회 그대로. 편집 전 값이 `2` 인지 먼저 실측)
    - `grep -c "CustomMessageBox" WPF_Example/UI/ContentItem/MainView.xaml.cs` 값이 편집 전 실측값과 동일 (**새 모달 0건**)
    - **파일 증가량 확인**: `wc -l < WPF_Example/UI/ContentItem/MainView.xaml.cs` 가 편집 전 실측값 `4476` 대비 **+45줄 이내** (얇은 배선만 넣었다는 증거)
    - `grep -c " ? " WPF_Example/UI/ContentItem/MainView.xaml.cs` 값이 편집 전 실측값과 동일
    - `git diff --stat WPF_Example/Custom/` 이 비어 있다 (이 plan 은 Align 쪽을 건드리지 않는다)
  </acceptance_criteria>

  <done>MainView 가 브러시 패널에 훅 2개를 채우고 Datum 선택 변경 시 마스크를 재로드하며, 기존 [패턴 모델 생성] 흐름은 그대로다.</done>
</task>

</tasks>

<verification>
1. 빌드 에러 0, 경고 코드 종류 CS0618/CS0162 두 종류뿐
2. `MainView.xaml.cs` 증가량 +45줄 이내, 새 `CustomMessageBox` 0건
3. `DatumPatternModelRegenService` 가 경로를 직접 조립하지 않는다(`Path.Combine` 0건)
4. 브러시 패널이 HWND 컨테이너(`Grid Grid.Row="1"`) 밖에 있다
5. MainView.xaml 의 기존 `Grid.Row` / `Grid.Column` 분포 무변경
</verification>

<success_criteria>
- Datum 패턴 모델 경로가 기존 단일 소스(`ResolveDatumModelPath`/`2`)로만 도출된다
- 재생성 로직이 `MainView.xaml.cs` 밖에 있다
- 브러시를 쓰지 않으면 기존 [패턴 모델 생성] 버튼 동작이 코드상 동일하다
</success_criteria>

<output>
완료 후 `.planning/phases/74-pattern-model-brush-masking/74-05-SUMMARY.md` 를 작성한다.
편집 전 실측값(MainView.xaml 의 `<RowDefinition` 수 / `Grid.Row` 분포 / `Grid.Column` 분포, MainView.xaml.cs 의 줄 수 · `CustomMessageBox` 카운트 · `" ? "` 카운트 · `svc.TryCreateModel(img,` 카운트)을 반드시 기록한다.
</output>
