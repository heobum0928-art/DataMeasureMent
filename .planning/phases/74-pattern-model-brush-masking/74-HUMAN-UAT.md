# Phase 74 — 실기 확인 (HUMAN UAT)

**상태:** 사용자 회신 대기
**빌드:** SIMUL-ON 에러 0 / 경고 18줄 · SIMUL-OFF 에러 0 / 경고 16줄 — 둘 다 baseline 유지

각 항목에 `PASS` / `중단(증상)` 중 하나로 답해 주시면 된다.

---

## 무엇을 만들었나

Shape/NCC 패턴 모델을 만들 때 원하지 않는 영역을 브러시로 칠해 제외하는 **옵션 기능**.

- 마스크는 모델 파일(`.shm`/`.ncm`) 옆에 `같은이름.mask.hobj` 로 저장된다
- 적용 지점은 `PatternMatchService.TryCreateModel` **한 곳** — Bottom Align · Tray Align · Datum 티칭 자동 적용
- 칠하기를 끝내는 순간(마우스 놓기) 모델이 자동으로 다시 만들어진다
- 브러시 자국은 HALCON 창 **안**에 반투명 빨강. 컨트롤은 창 **밖** 사이드 패널
- **설정의 `UsePatternBrushMask` 가 꺼져 있으면(기본값) 마스크 파일이 있어도 무시된다**

**의도한 사용 순서 (사용자 확인 완료):** ROI 그리기 → 브러시로 뺄 부분 칠하기 → 마우스 놓기 →
마스크 저장 → 자동 재티칭 → **ROI − 마스크 영역으로 모델 등록**.
마스크 경로는 모델 파일 존재 여부와 무관하게 계산되므로 **등록 전에 칠해도 된다.**

**ROI 밖에 칠한 자국:** 자유롭게 칠할 수 있고 ROI 밖은 모델에 영향이 없다(사용자 결정).
화면에는 ROI 밖 자국도 보인다 — 정상이다.

---

- [ ] **A. 회귀 0 확인 (옵션 OFF 상태 — 가장 중요)** — PENDING

1. 앱을 새로 실행한다 (설정 미변경 = `UsePatternBrushMask` 꺼짐)
2. 평소처럼 Datum 티칭에서 [패턴 1] ROI 를 그리고 [패턴 모델 생성]
3. Bottom 비전에서 슬롯을 고르고 ROI 2개를 그린 뒤 [티칭 저장]

**PASS:** 두 경우 모두 기존과 똑같은 메시지/점수/모달. 화면에 빨간 반투명 자국 없음.
**중단:** 없던 오류·팝업·점수 급락 / 칠하지도 않았는데 빨간 자국 / 티칭 점수 하락.

---

- [ ] **B. ROI 가 보이는가 (이번에 고친 것)** — PENDING

Bottom 비전에서 [ROI 1] / [ROI 2] 로 사각형을 그려 확정한다.

**PASS:** 확정 후에도 **주황 사각형이 화면에 남아 있다.** 슬롯을 바꿨다 돌아와도 다시 보인다.
**중단:** 그리고 나면 사라진다(이전 증상 그대로).

---

- [ ] **C. Bottom Align 브러시 칠하기 (본래 목적)** — PENDING

1. Bottom 비전 → [Grab]/[폴더 열기] 로 이미지
2. **면 슬롯을 고른다** ← 반드시. 미선택이면 대상이 없다고 나온다(의도된 동작)
3. [ROI 1]/[ROI 2] 로 패턴 ROI 두 개 확정
4. "브러시 마스킹" 패널 → [브러시 마스킹 사용] 체크 → [칠하기 모드]
5. 굵기 30px 정도 → ROI 안 노이즈 위를 **왼쪽 버튼 누른 채 끌어서** 칠한다 → 놓는다

**PASS:**
- 끄는 동안 지나간 자리가 **빨간 반투명**으로 이어져 칠해진다(점이 뚝뚝 끊기면 안 된다)
- 포인터를 따라 **노란 원**(굵기 미리보기)이 움직인다
- 놓는 즉시 상태 문구가 **"마스크 반영 + 모델 재생성 완료 (2개 경로)"**
- 그 자리에서 [검사] 를 눌러도 매칭이 성공한다

**중단:**
- 칠했는데 아무것도 안 보인다 → **HALCON 색상/알파 문제.** 즉시 알려 주시라
- 스크롤·확대하면 자국이 사라지거나 어긋난다
- 놓을 때마다 팝업이 뜬다(모달 금지 위반)
- "마스크 저장 실패" → 문구 전문을 알려 주시라
- 자국 하나마다 화면이 몇 초씩 멈춘다(재생성이 자국마다 도는 것)

---

- [ ] **D. 마스크 저장/복원 + 경로** — PENDING

1. C 를 끝낸 뒤 다른 슬롯으로 갔다가 돌아온다
2. 앱을 껐다 켜고 같은 슬롯을 고른다
3. 탐색기로 `D:\Data\Recipe\FAI_1\ETHERNET_ALIGN\` 을 연다

**PASS:** 슬롯을 돌아오면 자국이 그대로 다시 뜬다. 앱 재실행 후도 동일.
`Bottom_XXX_1.shm` **옆 같은 폴더**에 `Bottom_XXX_1.mask.hobj` 가 있다.
**중단:** 마스크가 엉뚱한 폴더(새로 생긴 빈 폴더)에 있다 → Phase 73 과 같은 경로 사고. 즉시 중단.
다른 슬롯의 자국이 뜬다. 앱 재실행 후 사라진다.

---

- [ ] **E. 지우개 · 마스크 초기화** — PENDING

[지우개] 로 일부를 문질러 지운다 → [칠하기 모드] 로 조금 더 칠한다 → [마스크 초기화]

**PASS:** 문지른 부분만 사라지고 놓을 때마다 상태 문구 갱신.
초기화 후 자국이 전부 사라지고 **"마스크 초기화 완료 (파일 N개 삭제)"**.
탐색기에서 `.mask.hobj` 가 실제로 사라져 있다(고아 없음).
**중단:** 초기화했는데 파일이 남아 있다 / 초기화 후에도 모델이 마스크를 반영한 상태다.

---

- [ ] **F. Datum 티칭 화면 브러시 (툴바 높이 수정 확인)** — PENDING

1. 검사 메인 → Datum 티칭 존 선택 → [패턴 1] ROI → [패턴 모델 생성]
2. 툴바의 **[브러시]** 클릭 → 툴바 **아래**에 패널이 펼쳐진다
3. [브러시 마스킹 사용] → [칠하기 모드] → ROI 안을 칠한다 → 놓는다
4. 다른 Datum 을 클릭했다가 돌아온다
5. **[Recipe Save]** 로 저장한다

**PASS:**
- 패널이 **툴바 아래**(이미지 창 밖)에 온전히 펼쳐진다 — 잘리거나 이미지에 가려지면 안 된다
  (← B-1 블로커 수정 확인 지점: 툴바 고정 높이 36px 를 `MinHeight` 로 풀었다)
- 칠하고 놓으면 상단 힌트에 **"마스크 반영 모델 재생성 완료 — 종료 전 Recipe Save 필요"**
- 다른 Datum 갔다 오면 그 Datum 의 마스크가 다시 뜬다
**중단:** 패널이 잘리거나 이미지 창에 가려진다 → airspace 문제. 즉시 중단.
Datum 을 바꿨는데 이전 Datum 의 마스크가 남아 있다. 툴바 버튼 배치가 어긋났다.

---

- [ ] **G. 옵션을 다시 끄면** — PENDING

설정에서 `UsePatternBrushMask` **끄기** → 앱 재시작 → 마스크를 칠해 둔 대상으로 다시 티칭.

**PASS:** 마스크 파일이 디스크에 그대로인데도 **모델은 마스크 없이** 만들어진다
(= 옵션을 끄면 예전과 똑같다).

---

## ⚠ 실기에서 이미 발견된 것 (참고)

`D:\Data\Recipe\FAI_1\ETHERNET_ALIGN\` 에 **`Bottom_1.mask.hobj` / `Bottom_2.mask.hobj`**
(2026-08-27 13:35, 각 7,140 bytes)가 남아 있다. **슬롯을 고르지 않은 상태에서 칠한 결과**로,
구형 경로(`Bottom_1/2.shm`)에 저장돼 모델 재생성이 되지 않은 마스크다.

이 경로는 커밋 `252345c` 로 **막았다**(Bottom + 슬롯 미선택 = 대상 없음).
남아 있는 두 파일은 **사용자 판단으로 삭제**하시면 된다 — 지금 상태로 두면
옵션을 켰을 때 `Bottom_1/2.shm` 재생성 시 반영된다.

## 회신 형식

`A: PASS` / `C: 중단 — (증상)` 형태. 전부 통과면 `전부 PASS` 한 마디로 충분하다.


---

# UAT 실측 결과 (2026-08-27)

**핵심 기능 PASS.** 브러시로 칠한 영역이 모델에서 실제로 제외되는 것까지 확인됨
(로그 실측: `[PatternMask] 마스크 저장 → 로드`, 상태줄 `마스크 반영 + 모델 재생성 완료 (2개 경로)`).

| # | 항목 | 결과 |
|---|---|---|
| 1 | 슬롯 선택 → 좌상단 `면 슬롯:` 표시 | ✅ PASS |
| 2 | `ROI 1 그리기` → 확정 즉시 주황 + 이름표 | ✅ PASS |
| 3 | `ROI 2 그리기` → 〃 | ✅ PASS |
| 4 | `티칭 저장` → 녹색 외곽선 | ✅ PASS |
| 5 | `검사` → 검출 박스에 이름표 유지 | ✅ PASS |
| 6 | 라이브 화면 중심 십자 | ⏸ **현장 확인 필요**(라이브 카메라 없음) |
| 7 | X/Y/밝기 | ❌→수정 후 재확인 대기 |
| 8 | 거리 측정(신규) | ⏸ 재확인 대기 |
| 9 | Tray 비전 화면 전반 | ⏸ 재확인 대기 (시작 크래시 수정 후) |

## UAT 중 발견해 고친 것 (계획 밖)

| 증상 | 원인 | 커밋 |
|---|---|---|
| ROI 가 그린 뒤 사라짐 | `CommitActiveRectangle` 이 draft 를 지우는데 Align 뷰가 확정 ROI 를 뷰어에 안 돌려줌 | `17da571` |
| 슬롯 미선택 시 구형 경로에 마스크만 저장 | `slot=None` 이 legacy 경로를 반환 | `252345c` |
| 옵션 꺼도 칠해짐 / 칠하기·지우개 동시선택 / 캘 ROI 미표시 | 게이트 부재 · 상호배타 부재 · 확정 ROI 미표시 | `1c6a39a` |
| ROI 가 빨간 draft 로 남음 | 확정이 "다음 버튼 클릭" 까지 지연 | `6484be1` |
| 티칭 후 녹색 미표시 / 검사 시 자국 미제거 | 녹색은 Run 경로에서만 설정됨 · 마스크 표시가 모드와 무관 | `54217da` |
| 슬롯 미선택인데 조작이 다 허용됨 | 경고만 있고 게이트 없음 | (커밋됨) |
| 라벨이 엉뚱한 박스에 붙음 | 2-인자 세터가 라벨 미정리 | `63b01bb` |
| 검사 후 이름표 사라짐 | 위 수정의 후속 — 검출 경로가 라벨 없이 교체 | `930ac99` |
| X/Y/밝기 미표시 | WPF 라벨 스크롤 가림 + hover 시 `Render()` 미호출 | `6096a32` |
| **시작 시 크래시** | Tray 버튼이 Bottom 전용 `StaticResource` 참조 | `a63dbee` |

### ⚠ `StaticResource` 는 빌드로 안 잡힌다

`a63dbee` 는 빌드가 계속 통과(에러 0/경고 18)했는데도 **실행하면 죽는** 상태였다.
정의되지 않은 `StaticResource` 는 컴파일이 아니라 **XAML 파싱(화면 최초 생성) 시점**에 터진다.
Tray 화면을 한 번도 열지 않아 드러나지 않았다.

→ 재발 방지로 이번 phase 가 건드린 XAML 6개의 `StaticResource` 참조를 각 파일의 `x:Key` 와
대조 감사(미정의 0건). **XAML 을 건드리면 그 화면을 반드시 한 번 열어봐야 한다.**

## 확정 결정

**Tray 에는 피커센터 캘을 넣지 않는다** (사용자 확인 2026-08-27).
Phase 60 결정(`대상: BOTTOM만, TRAY 캘리브 없음`)을 유지한다.
피커센터 캘은 *피커가 부품을 잡고 회전*할 때의 회전중심을 찾는 작업인데,
Tray 는 부품이 트레이에 놓여 있어 피커 회전이라는 개념 자체가 없다.

**Bottom 은 완료**로 마감한다 (사용자 확인 2026-08-27).
