---
phase: 75-align-corrected-image-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/EthernetVision/AlignVerifyResult.cs
  - WPF_Example/Custom/EthernetVision/AlignShapeMatchService.Verify.cs
  - WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [D-75-01]
user_setup: []

must_haves:
  truths:
    - "Align 검출이 성공한 이미지에 대해, 비전이 계산한 보정을 그대로 적용한 이미지에서 다시 매칭이 돌아간다"
    - "재매칭 결과로 잔여 offset(mm) 2개 + 잔여 theta(deg) 1개 + 매칭점수가 산출된다"
    - "실패해도 예외가 밖으로 나가지 않고 실패사유 문자열이 남는다"
    - "기존 AlignShapeMatchService.Run() 의 동작은 한 줄도 바뀌지 않는다"
    - "Run() 이 이미 낸 1차 검출 좌표를 넘기면 그 검출을 다시 하지 않는다 (TryFindPose 4회 → 2회)"
    - "재매칭에 실제로 몇 ms 걸렸는지가 로그에 숫자로 남는다 (PLC 응답 경로에서 동기 실행되므로)"
  artifacts:
    - path: "WPF_Example/Custom/EthernetVision/AlignVerifyResult.cs"
      provides: "① 재매칭 결과 POCO"
      contains: "ResidualOffsetXmm"
    - path: "WPF_Example/Custom/EthernetVision/AlignShapeMatchService.Verify.cs"
      provides: "RunCorrectedRecheck — 보정 이미지 생성 + 재매칭 + 잔여 산출"
      contains: "RunCorrectedRecheck"
  key_links:
    - from: "AlignShapeMatchService.Verify.cs"
      to: "PatternMatchService.TryFindPose"
      via: "_matcher 재사용(partial 이므로 private 필드 접근 가능)"
      pattern: "_matcher\\.TryFindPose"
    - from: "AlignShapeMatchService.Verify.cs"
      to: "HALCON affine_trans_image"
      via: "VectorAngleToRigid 로 만든 강체행렬 적용"
      pattern: "AffineTransImage"
---

<objective>
① "보정 후 다시 재봄" 의 계산 엔진을 만든다.

Align 이 낸 보정값대로 부품을 옮겼다고 가정한 이미지를 실제로 만들고(HALCON 강체변환),
그 이미지에서 패턴 2개를 **다시 매칭**해서 기준 자세와 얼마나 어긋나 있는지(잔여 offset/theta)를 낸다.
잔여가 0 에 가까우면 "비전 계산은 맞았다"는 증거가 된다.

Purpose: 나중에 "Align 이 잘못됐다"는 분쟁이 났을 때 비전이 방어할 수 있는 숫자를 만든다.
Output: `AlignVerifyResult` POCO + `AlignShapeMatchService.RunCorrectedRecheck()`

**이 plan 은 계산만 한다.** 저장(CSV/이미지)과 호출 배선은 75-02 / 75-03 이 한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-align-corrected-image-evidence/75-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- 실행자는 아래 계약을 그대로 쓴다. 코드베이스 탐색 불필요. -->

WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs 안에 이미 있는 것들
(전부 private 이지만, 이 plan 이 같은 클래스의 partial 파일을 추가하므로 그대로 호출 가능):

```csharp
private const string ENGINE = "Shape";
private const double MIN_SCORE = 0.5;
private const double UM_PER_MM = 1000.0;          // EthernetPixelResolution 은 μm/px → /1000 = mm/px
private const double FULL_SEARCH_LEN = 99999.0;
private readonly PatternMatchService _matcher;

private string BuildShmPath(EEthernetVisionMode mode, int modelIndex, EBottomAlignSlot slot = EBottomAlignSlot.None);
private string BuildJsonPath(EEthernetVisionMode mode, EBottomAlignSlot slot = EBottomAlignSlot.None);
private AlignRefPose LoadRefPose(string jsonPath);
private double ComputeAngleLx(double row1, double col1, double row2, double col2);   // 실패 시 double.NaN
```

WPF_Example/Halcon/Algorithms/PatternMatchService.cs:393
```csharp
public bool TryFindPose(
    HImage runtimeImage, string engine, string modelPath,
    double roiRow, double roiCol, double roiLen1, double roiLen2,
    double marginPx, double minScore, double downsampleFactor,
    out double curRow, out double curCol, out double curAngleDeg, out double curScore,
    out string error);
```

WPF_Example/Custom/EthernetVision/AlignRefPose.cs
```csharp
public double Ref1Row, Ref1Col, Ref2Row, Ref2Col;   // 실제로는 { get; set; } 프로퍼티
public double RefBaselineRad;
```

AlignShapeMatchService.Run() 이 쓰는 검색 인자(재매칭도 **동일하게** 써야 결과가 비교 가능하다):
```csharp
_matcher.TryFindPose(img, ENGINE, shmPath,
    0.0, 0.0, FULL_SEARCH_LEN, FULL_SEARCH_LEN,
    0.0, MIN_SCORE, 1.0,
    out fRow, out fCol, out fAngleDeg, out fScore, out findErr);
```

HObject → HImage 변환 선례 (WPF_Example/Halcon/Algorithms/DatumFindingService.cs:1769):
```csharp
erodedImage = new HImage(erodedObj);
```

로깅:
```csharp
Logging.PrintLog((int)ELogType.Algorithm, "[TAG] fmt {0}", arg);
Logging.PrintLog((int)ELogType.Error,     "[TAG] fmt {0}", arg);
```
</interfaces>
</context>

<coding_rules>
사용자 명시 규칙 — 위반 시 회귀로 간주한다.

- 삼항 `?:` **금지** → `if/else`. `??` / `?.` **금지** → 명시적 null 분기
- 전통 `switch` 만 (C# 8.0 switch expression 금지). **C# 7.2 문법만**
- 긴 조건은 이름 있는 `bool` 로 선추출 (`bool bFound1 = ...; if (bFound1) {...}`)
- 헝가리언 접두사: `b`(bool) / `n`(int) / `sz`(string)
- 이 폴더(`Custom/EthernetVision/`)의 중괄호 스타일 = **K&R** (여는 중괄호를 선언과 같은 줄에).
  `AlignShapeMatchService.cs` 를 보고 그대로 맞춘다
- 날짜 주석(`//YYMMDD hbk`) **신규 금지**. 비자명한 "왜" 만 최소한으로 남긴다
- **HImage / HObject / HTuple 은 반드시 Dispose** (finally 또는 소유권 이전 명시)
- `WPF_Example/DatumMeasurement.csproj` 는 **컴파일을 위해 편집은 하되 절대 stage/commit 하지 않는다.**
  작업 후 `git status` 에 `M WPF_Example/DatumMeasurement.csproj` 가 남아 있는 것이 정상이다
- **실행 중인 프로세스를 종료하지 않는다.** 빌드 산출물이 잠겨 있으면 스크래치 `OutDir` 로 컴파일만 검증하고 보고한다
</coding_rules>

<build_verification>
```bash
MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"
SCR="/c/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/7fe7f8e3-9e05-4190-9941-3b1cfdde3971/scratchpad"

# SIMUL-ON (Debug|x64)
"$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 \
  -p:OutDir="$SCR/build75/on/" -p:IntermediateOutputPath="$SCR/build75/obj-on/" -v:m -nologo
```
- msbuild 옵션은 **대시(`-p:`) 형식만** 쓴다. `/p:` 는 MSYS 경로 변환으로 깨진다
- 판정 기준: **에러 0** + **새 경고 코드 종류 0건**.
  경고는 baseline(SIMUL-ON 18줄 = CS0618×16 + CS0162×2)이 이미 존재한다.
  "경고 0" 을 목표로 삼지 말 것. 편집 **전** 한 번 빌드해 경고 코드 집합을 기록하고, 편집 후와 비교한다
</build_verification>

<tasks>

<task type="auto">
  <name>Task 1: AlignVerifyResult POCO 신설 + AlignShapeMatchService 를 partial 로 전환</name>

  <read_first>
    - `WPF_Example/Custom/EthernetVision/AlignResult.cs` (전체 68줄 — POCO 작성 스타일/주석 톤의 기준)
    - `WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs:21` (`public class AlignShapeMatchService {`)
    - `WPF_Example/DatumMeasurement.csproj` 에서 `Custom\EthernetVision\AlignResult.cs` 항목 (신규 파일 등록 위치 확인용)
  </read_first>

  <files>
    WPF_Example/Custom/EthernetVision/AlignVerifyResult.cs (신규),
    WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs (1줄),
    WPF_Example/DatumMeasurement.csproj (커밋 금지)
  </files>

  <action>
1. `WPF_Example/Custom/EthernetVision/AlignVerifyResult.cs` 를 새로 만든다.
   네임스페이스 `ReringProject`, K&R 중괄호, 순수 POCO(로직/HALCON/IO 없음).
   `AlignResult.cs` 와 동일한 XML doc 주석 톤을 쓴다.

   프로퍼티 (전부 `{ get; set; }`):
   - `bool Verified` — 재매칭까지 전부 성공해 잔여값이 유효한가. false 면 아래 수치 미사용
   - `double ResidualOffsetXmm` — 보정 후 남은 X 오프셋(mm). Col 축
   - `double ResidualOffsetYmm` — 보정 후 남은 Y 오프셋(mm). Row 축
   - `double ResidualThetaDeg` — 보정 후 남은 각도(deg)
   - `double ResidualDistanceMm` — `sqrt(X^2 + Y^2)`. 화면 표시용 크기값(계산은 RunCorrectedRecheck 가 채운다)
   - `double Score` — 재매칭 두 패턴 점수 중 **작은 값**(보수적 지표)
   - `string FailReason` — 실패 사유. 성공 시 빈 문자열 `""`

   클래스 상단 요약 주석에 **다음 문장을 그대로 넣는다**(범위 오해 방지 — 미결 D 해소):
   > 이 결과는 "검출 + 강체변환의 자기일관성" 만 검증한다.
   > Bottom 의 `ApplyPickerCenterCorrection`(피커센터 기준 재표현, 부호 규약 미확정)은 여기서 검증되지 않는다.
   > 그 구간은 ②(검사 시 Datum 원점 기록)가 잡는다. Tray/Bottom 모두 동일한 정의를 쓴다.

2. `WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs:21` 을
   `public class AlignShapeMatchService {` → `public partial class AlignShapeMatchService {` 로 바꾼다.
   **이 파일에서 바꾸는 것은 이 한 단어가 전부다.** 다른 어떤 줄도 건드리지 않는다.

3. `WPF_Example/DatumMeasurement.csproj` 의 `<Compile Include="Custom\EthernetVision\AlignResult.cs" />`
   근처에 `<Compile Include="Custom\EthernetVision\AlignVerifyResult.cs" />` 를 추가한다.
   경로 구분자는 백슬래시(`\`)다. **csproj 는 stage/commit 하지 않는다.**
  </action>

  <verify>
    <automated>
    cd /c/Info/Project/DataMeasurement && \
    test -f WPF_Example/Custom/EthernetVision/AlignVerifyResult.cs && \
    git diff --numstat -- WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs && \
    grep -c "public partial class AlignShapeMatchService" WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs && \
    grep -F -c 'Custom\EthernetVision\AlignVerifyResult.cs' WPF_Example/DatumMeasurement.csproj
    </automated>
  </verify>

  <acceptance_criteria>
    - `git diff --numstat -- WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs` 출력이 정확히 `1	1	...`
      (삽입 1줄 / 삭제 1줄 = partial 한 단어만 바뀜). 다른 숫자면 실패다
    - `grep -c "public partial class AlignShapeMatchService" .../AlignShapeMatchService.cs` == `1`
    - `grep -c "ResidualOffsetXmm\|ResidualOffsetYmm\|ResidualThetaDeg\|ResidualDistanceMm\|FailReason\|Verified\|Score" AlignVerifyResult.cs` >= `7`
    - `grep -F -c 'Custom\EthernetVision\AlignVerifyResult.cs' WPF_Example/DatumMeasurement.csproj` == `1`
      (Git Bash 에서 백슬래시 경로 grep 은 반드시 `-F` 를 쓴다 — MSYS 경로 변환 때문에 없으면 항상 0 이 나온다)
    - `grep -nE '\?\?|\?\.|\? .+ :' WPF_Example/Custom/EthernetVision/AlignVerifyResult.cs` 출력 **없음**
    - `grep -c 'hbk' WPF_Example/Custom/EthernetVision/AlignVerifyResult.cs` == `0` (날짜 주석 신규 금지)
  </acceptance_criteria>

  <done>AlignVerifyResult.cs 가 존재하고 csproj 에 등록되었으며, AlignShapeMatchService.cs 는 partial 한 단어만 바뀌었다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: RunCorrectedRecheck 구현 (보정 이미지 생성 → 재매칭 → 잔여 산출)</name>

  <read_first>
    - `WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs:489~640` (`Run()` 전체 — Step1~4 계산을 그대로 재현해야 한다)
    - `WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs:735~775` (`TryBuildMovedContour` — `VectorAngleToRigid` + HALCON 핸들 finally-Dispose 패턴)
    - `WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs:153` (`ComputeAngleLx`)
    - `WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs:786~830` (`ApplyPickerCenterCorrection` — **호출하지 않는다**. 왜 안 하는지 이해하려고 읽는다)
    - `WPF_Example/Halcon/Algorithms/DatumFindingService.cs:1755~1775` (`new HImage(hobject)` 변환 선례)
  </read_first>

  <files>
    WPF_Example/Custom/EthernetVision/AlignShapeMatchService.Verify.cs (신규),
    WPF_Example/DatumMeasurement.csproj (커밋 금지)
  </files>

  <action>
`WPF_Example/Custom/EthernetVision/AlignShapeMatchService.Verify.cs` 를 새로 만든다.
`namespace ReringProject { public partial class AlignShapeMatchService { ... } }`, K&R 중괄호.
`using System; using HalconDotNet; using ReringProject.Setting; using ReringProject.Utility;`

**공개 메서드 시그니처 (정확히 이대로 — 오버로드 2개):**
```csharp
// (A) 자체 검출판 — 1차 검출을 직접 수행한다. 오프라인/단독 호출용.
public AlignVerifyResult RunCorrectedRecheck(
    HImage img, EEthernetVisionMode mode, EBottomAlignSlot slot, out HImage correctedImage)

// (B) 검출 재사용판 — Run() 이 이미 낸 1차 검출 좌표를 받아 그 2회를 건너뛴다. 실운전 경로(75-03)가 쓴다.
public AlignVerifyResult RunCorrectedRecheck(
    HImage img, EEthernetVisionMode mode, EBottomAlignSlot slot,
    bool bHasDetection, double dDet1Row, double dDet1Col, double dDet2Row, double dDet2Col,
    out HImage correctedImage)
```

**(A) 는 `(B)` 를 `bHasDetection: false` 로 호출하는 한 줄 위임이다.** 구현 본체는 (B) 하나뿐이다.

### ⚠ 왜 오버로드가 필요한가 — 택트 (반드시 이대로 구현할 것)

`RunCorrectedRecheck` 는 `RunBottomAlign` 의 `finally` 에서 **동기**로 돈다 = **PLC 응답이 나가기 전에** 실행된다.
아래 5단계를 그대로 구현하면 `_matcher.TryFindPose` 가 **4회**(1차 2 + 재매칭 2) + 전체 이미지
`AffineTransImage` 1회다. 검색 인자가 `FULL_SEARCH_LEN=99999` / `downsample=1.0` 전역 탐색이라
CONTEXT D-75-01 의 "매칭 1회 추가 = 수십 ms" 추정보다 비용이 크다.

그런데 **1차 검출 2회는 낭비다.** `Run()` 이 방금 같은 이미지에서 같은 검출을 이미 했고, 그 결과가
`AlignResult.HasDetection` / `DetectedRow1` / `DetectedCol1` / `DetectedRow2` / `DetectedCol2` 에 들어 있으며
호출부(`75-03` 의 `RecordAlignVerify`)가 그 `res` 를 손에 쥐고 있다. (B) 로 그 값을 넘기면 **추가 비용이 절반**이 된다.

이건 이 저장소에서 이미 문제가 된 적 있는 지점이다 — PLC 가 응답 후 **22ms** 만에 다음 요청을 보내
시퀀스가 Idle 이 아니면 거부가 나가는 이슈가 기록돼 있다(`project_plc_rapid_retest_start_reject`).
응답 경로에 수백 ms 를 얹으면 그 증상이 악화된다.

**동작 순서 — 이 순서를 그대로 구현한다 (본체 = 오버로드 (B)):**

1. `correctedImage = null;` / `AlignVerifyResult result = new AlignVerifyResult(); result.Verified = false; result.FailReason = "";`
2. 입력 가드 (전부 `if/else`, 삼항 금지):
   - `img == null` → `FailReason = "입력 이미지 없음"` → return
   - `mode == EEthernetVisionMode.None` → `FailReason = "모드 None"` → return
3. `BuildShmPath(mode, 1, slot)` / `BuildShmPath(mode, 2, slot)` / `BuildJsonPath(mode, slot)`
   셋 중 하나라도 null → `FailReason = "경로 미설정"` → return
4. `LoadRefPose(jsonPath)` == null → `FailReason = "기준 pose 없음"` → return
5. **1차 검출 — 넘겨받았으면 건너뛴다.**
   ```csharp
   double f1Row, f1Col, f2Row, f2Col;
   if (bHasDetection == true)
   {
       // Run() 이 방금 같은 이미지에서 낸 검출을 그대로 쓴다. TryFindPose 2회를 통째로 아낀다.
       f1Row = dDet1Row; f1Col = dDet1Col;
       f2Row = dDet2Row; f2Col = dDet2Col;
   }
   else
   {
       // 폴백: 오프라인/단독 호출. 인자는 Run() 과 완전히 동일해야 결과가 비교 가능하다.
       // 실패 시 FailReason = "1차 검출 실패[1]" / "1차 검출 실패[2]" → return
       // _matcher.TryFindPose(img, ENGINE, shm1Path, 0.0, 0.0, FULL_SEARCH_LEN, FULL_SEARCH_LEN,
       //                      0.0, MIN_SCORE, 1.0, out f1Row, out f1Col, out fAngle1, out fScore1, out findErr1);
       // (패턴 2도 동일하게 1회 더)
   }
   ```
   ⚠ 폴백 경로의 `TryFindPose` 인자는 `<interfaces>` 에 적힌 `Run()` 의 인자와 **완전히 동일**해야 한다
   (`0.0, 0.0, FULL_SEARCH_LEN, FULL_SEARCH_LEN, 0.0, MIN_SCORE, 1.0`). 달라지면 잔여값이 비교 불가능해진다.
6. `double dRuntimeBaselineRad = ComputeAngleLx(f1Row, f1Col, f2Row, f2Col);`
   `double.IsNaN(...)` → `FailReason = "baseline 산출 실패"` → return
7. midpoint 4개 계산 (`Run()` Step 4 와 동일):
   ```
   midFRow = (f1Row + f2Row) / 2.0;  midFCol = (f1Col + f2Col) / 2.0;
   midRRow = (refPose.Ref1Row + refPose.Ref2Row) / 2.0;
   midRCol = (refPose.Ref1Col + refPose.Ref2Col) / 2.0;
   ```
8. **보정 강체변환 생성** — 검출 자세(midF + 런타임 baseline)를 기준 자세(midR + RefBaselineRad)로 보내는 행렬:
   ```csharp
   HOperatorSet.VectorAngleToRigid(
       midFRow, midFCol, dRuntimeBaselineRad,
       midRRow, midRCol, refPose.RefBaselineRad,
       out homMat);
   ```
9. **보정 이미지 생성:**
   ```csharp
   HOperatorSet.AffineTransImage(img, out correctedObj, homMat, "bilinear", "false");
   correctedImage = new HImage(correctedObj);
   ```
   - 마지막 인자 `AdaptImageSize` 는 **반드시 `"false"`** 다. `"true"` 로 두면 이미지 원점이 이동해
     `refPose` 좌표계가 무효가 되고 잔여값이 통째로 틀린다. 이 이유를 코드 주석 1줄로 남긴다
   - `correctedObj` 는 `new HImage(...)` 로 감싼 **직후** Dispose 한다
10. **재매칭** — `correctedImage` 에 대해 5번과 완전히 동일한 인자로 `_matcher.TryFindPose` 2회.
    실패 시 `FailReason = "재검출 실패[1]"` / `"재검출 실패[2]"` → return
    **주의: 이때 `correctedImage` 는 Dispose 하지 않고 out 으로 그대로 넘긴다** — 실패한 보정 이미지가
    바로 NG 증거이기 때문이다(75-03 이 저장한다).
11. **잔여 산출:**
    ```
    dCheckBaselineRad = ComputeAngleLx(c1Row, c1Col, c2Row, c2Col);   // NaN → FailReason="재baseline 산출 실패" → return
    mid2Row = (c1Row + c2Row)/2.0;  mid2Col = (c1Col + c2Col)/2.0;
    dRow = mid2Row - midRRow;       dCol = mid2Col - midRCol;
    dResMm = SystemSetting.Handle.EthernetPixelResolution / UM_PER_MM;   // μm/px → mm/px
    result.ResidualOffsetXmm  = dCol * dResMm;        // Col → X (Run() 규약과 동일)
    result.ResidualOffsetYmm  = dRow * dResMm;        // Row → Y
    result.ResidualThetaDeg   = (dCheckBaselineRad - refPose.RefBaselineRad) * 180.0 / Math.PI;
    result.ResidualDistanceMm = Math.Sqrt(X*X + Y*Y);
    result.Score              = Math.Min(c1Score, c2Score);
    result.Verified           = true;
    ```
12. **`ApplyPickerCenterCorrection` 은 호출하지 않는다.** 코드 주석으로 이유를 남긴다:
    "① 은 검출+강체변환 자기일관성만 검증한다. 피커센터 재표현은 부호 규약(PICKER_ROTATION_SIGN)이
     아직 UAT 미확정이라 여기 넣으면 미확정 규약을 검증 결과로 오해하게 된다. 그 구간은 ②가 잡는다."
13. **로그 1줄** (성공/실패 공통, `ELogType.Algorithm`) — **소요시간(ms)과 검출 재사용 여부를 반드시 포함한다:**
    ```csharp
    "[ALIGN_VERIFY] recheck ({0}/{1}) verified={2} residual=({3:F4},{4:F4})mm dist={5:F4}mm theta={6:F4} score={7:F3} reused={8} elapsed={9}ms reason={10}"
    ```
    - `reused` = `bHasDetection`(1차 검출을 재사용했는가)
    - `elapsed` = 메서드 진입부터 로그 시점까지 **실측 ms**.
      메서드 첫 줄에 `System.Diagnostics.Stopwatch swVerify = System.Diagnostics.Stopwatch.StartNew();` 를 두고
      로그에서 `swVerify.ElapsedMilliseconds` 를 쓴다. `Stopwatch` 는 `finally` 에서 별도 정리가 필요 없다.

    **왜 시간을 재는가:** 이 메서드는 PLC 응답 경로에서 동기로 돈다. PLC 가 응답 후 22ms 만에 다음 요청을
    보내는 이슈가 이미 기록돼 있어(`project_plc_rapid_retest_start_reject`), **실제로 몇 ms 를 얹었는지가
    숫자로 남아 있어야** 나중에 택트 문제가 났을 때 이 기능이 원인인지 즉시 가른다.
    이 값은 75-06 의 실기 확인(UAT) 판단 근거이기도 하다.
14. **예외 처리:** 메서드 전체를 `try / catch (Exception ex) / finally` 로 감싼다.
    - `catch`: `result.Verified = false; result.FailReason = "예외: " + ex.Message;`
      그리고 `correctedImage` 가 non-null 이면 Dispose 후 null 로 만든다. **throw 금지**(TCP 스레드에서 호출된다)
    - `finally`: `homMat` / `correctedObj` 등 HALCON 핸들 Dispose.
      `TryBuildMovedContour` 의 `if (x != null) { try { x.Dispose(); } catch { } }` 패턴을 그대로 쓴다
  </action>

  <verify>
    <automated>
    cd /c/Info/Project/DataMeasurement && \
    V=WPF_Example/Custom/EthernetVision/AlignShapeMatchService.Verify.cs && \
    echo "TryFindPose=$(grep -c '_matcher.TryFindPose' $V)" && \
    echo "AffineTransImage=$(grep -c 'AffineTransImage' $V)" && \
    echo "AdaptFalse=$(grep -c 'AffineTransImage(img, out correctedObj, homMat, \"bilinear\", \"false\")' $V)" && \
    echo "PickerCorr=$(grep -c 'ApplyPickerCenterCorrection(' $V)" && \
    echo "Throw=$(grep -c 'throw ' $V)" && \
    echo "Ternary=$(grep -cE '\?\?|\?\.|\? .+ :' $V)" && \
    git diff --numstat -- WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '_matcher.TryFindPose' AlignShapeMatchService.Verify.cs` == `4`
      (폴백 1차 2회 + 재매칭 2회. **폴백 2회는 `bHasDetection == true` 분기 안에 있으면 안 된다** —
      `awk '/if \(bHasDetection == true\) \{/,/^                else \{$/' ...Verify.cs | grep -c '_matcher.TryFindPose'` == `0`.
      ⚠ awk 종료 패턴은 이 파일의 K&R 스타일(`                else {`)에 맞춘 것이다. 들여쓰기가 다르면 패턴도 맞춰 고칠 것)
    - **오버로드 2개 존재**:
      `grep -c 'public AlignVerifyResult RunCorrectedRecheck(' ...Verify.cs` == `2`
    - **(A) 는 위임 한 줄이다**: `grep -c 'bHasDetection: false\|false, 0.0, 0.0, 0.0, 0.0' ...Verify.cs` >= `1`
    - **검출 재사용 분기 존재**: `grep -c 'bHasDetection' ...Verify.cs` >= `3` (파라미터 1 + 분기 1 + 로그 1)
    - **소요시간 계측**: `grep -c 'Stopwatch' ...Verify.cs` >= `1` **그리고** `grep -c 'ElapsedMilliseconds' ...Verify.cs` == `1`
      (⚠ `grep -c` 는 **줄** 단위다. `System.Diagnostics.Stopwatch sw = System.Diagnostics.Stopwatch.StartNew();` 는
      한 줄이라 `Stopwatch` 가 2회 나와도 카운트는 1이다. 발생 횟수로 재려면 `grep -o ... | wc -l` 을 쓸 것)
    - **로그에 elapsed/reused 포함**: `grep -c 'elapsed=' ...Verify.cs` == `1` 그리고 `grep -c 'reused=' ...Verify.cs` == `1`
    - `grep -c 'AffineTransImage' ...Verify.cs` == `1`
    - `AffineTransImage` 호출의 마지막 인자가 `"false"` 다 —
      `grep -c 'AffineTransImage(img, out correctedObj, homMat, "bilinear", "false")' ...Verify.cs` == `1`
    - `grep -c 'ApplyPickerCenterCorrection(' ...Verify.cs` == `0` (① 은 피커센터 보정을 쓰지 않는다)
    - `grep -c 'throw ' ...Verify.cs` == `0` (TCP 스레드 크래시 방지)
    - `grep -cE '\?\?|\?\.|\? .+ :' ...Verify.cs` == `0`
    - `grep -c 'Dispose' ...Verify.cs` >= `3` (homMat / correctedObj / 예외경로 correctedImage)
    - `grep -c 'EthernetPixelResolution / UM_PER_MM' ...Verify.cs` == `1` (μm→mm 변환 단일 지점)
    - `grep -c 'hbk' ...Verify.cs` == `0`
    - `git diff --numstat -- .../AlignShapeMatchService.cs` 는 여전히 `1	1` (Task 1 이후 추가 변경 없음)
  </acceptance_criteria>

  <done>RunCorrectedRecheck 가 존재하고, 보정 이미지 생성 → 재매칭 → 잔여 offset/theta 산출까지 코드로 완결되어 있다.</done>
</task>

<task type="auto">
  <name>Task 3: csproj 등록 + 빌드 통과 + Run() 무변경 확인</name>

  <read_first>
    - `WPF_Example/DatumMeasurement.csproj` 의 `Custom\EthernetVision\` 항목 블록
    - 이 파일의 `<build_verification>` 섹션
  </read_first>

  <files>WPF_Example/DatumMeasurement.csproj (커밋 금지)</files>

  <action>
1. csproj 에 `<Compile Include="Custom\EthernetVision\AlignShapeMatchService.Verify.cs" />` 를 추가한다.
2. **편집 전 baseline 을 먼저 잰다** — 이미 편집했다면 `git stash` 대신 그냥 현재 상태로 빌드하고,
   경고 목록에서 **경고 코드 종류만** 뽑아 비교한다:
   ```bash
   "$MSB" ... -v:m -nologo 2>&1 | grep -oE 'CS[0-9]{4}' | sort -u
   ```
   결과 집합이 `CS0162`, `CS0618` 두 종류를 넘지 않아야 한다.
3. SIMUL-ON 빌드를 `<build_verification>` 의 명령 그대로 돌린다. 에러 0 확인.
4. `git status --short` 로 `M WPF_Example/DatumMeasurement.csproj` 가 **unstaged 로 남아 있는지** 확인한다.
5. 커밋한다 — csproj 를 제외하고 명시적으로 파일을 나열한다:
   ```bash
   git add WPF_Example/Custom/EthernetVision/AlignVerifyResult.cs \
           WPF_Example/Custom/EthernetVision/AlignShapeMatchService.Verify.cs \
           WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs
   git commit -m "feat(75-01): Align 보정 후 재매칭 엔진(RunCorrectedRecheck) 추가"
   ```
  </action>

  <verify>
    <automated>
    cd /c/Info/Project/DataMeasurement && \
    MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" && \
    SCR="/c/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/7fe7f8e3-9e05-4190-9941-3b1cfdde3971/scratchpad" && \
    "$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 \
      -p:OutDir="$SCR/build75/on/" -p:IntermediateOutputPath="$SCR/build75/obj-on/" -v:m -nologo 2>&1 \
      | tee "$SCR/build75-01.log" | tail -5 ; \
    echo "ERR=$(grep -c ' error ' "$SCR/build75-01.log")" ; \
    echo "CODES=$(grep -oE 'CS[0-9]{4}' "$SCR/build75-01.log" | sort -u | tr '\n' ' ')" ; \
    git status --short -- WPF_Example/DatumMeasurement.csproj
    </automated>
  </verify>

  <acceptance_criteria>
    - 빌드 로그에서 `grep -c ' error '` == `0`
    - 빌드 로그의 경고 코드 집합(`grep -oE 'CS[0-9]{4}' | sort -u`)이 `CS0162 CS0618` 를 벗어나지 않는다.
      **경고 총 줄 수는 판정 기준이 아니다.** 새 코드 종류가 0건인지만 본다
    - `git status --short -- WPF_Example/DatumMeasurement.csproj` 가 ` M ...` (unstaged) 를 출력한다.
      `M  ...` (staged) 면 실패 — `git restore --staged` 로 되돌린다
    - `git show --stat HEAD` 에 `DatumMeasurement.csproj` 가 **없다**
    - `git show HEAD -- WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs | grep -c '^-[^-]'` == `1`
      (partial 한 단어 교체로 인한 삭제 1줄 외에 기존 코드 삭제 없음)
  </acceptance_criteria>

  <done>SIMUL-ON 빌드 에러 0, 새 경고 코드 0건, csproj 는 커밋되지 않았다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLC → TCP 스레드 → RunCorrectedRecheck | 외부 트리거로 호출된다. 여기서 예외가 나면 TCP 스레드가 죽는다 |
| 레시피 폴더(.shm/.json) → 파일 IO | 레시피 경로가 없거나 손상된 상태로 호출될 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01 | Denial of Service | RunCorrectedRecheck | mitigate | 메서드 전체 try/catch, `throw` 0건(acceptance 로 검증). 실패는 `FailReason` 문자열로만 전달 |
| T-75-02 | Denial of Service | HALCON 핸들 누수 | mitigate | homMat/correctedObj finally-Dispose, 예외 경로에서 correctedImage Dispose. 누적 시 halcon.DLL 크래시 이력이 실재한다 |
| T-75-03 | Tampering | 잘못된 refPose/경로 | mitigate | 경로 null / refPose null 을 각각 별도 FailReason 으로 조기 return. 계산 진행 금지 |
| T-75-04 | Information Disclosure | 로그 | accept | 좌표/점수만 기록. 비밀정보 없음 |
</threat_model>

<verification>
1. SIMUL-ON `Debug|x64` 빌드 에러 0, 새 경고 코드 종류 0건
2. `AlignShapeMatchService.cs` 변경이 `1	1` (partial 한 단어) 뿐 — `Run()` 무변경
3. `RunCorrectedRecheck` 안에 `throw` 0건, `?:`/`??`/`?.` 0건
4. `AffineTransImage` 의 `AdaptImageSize` 인자가 `"false"`
5. csproj 미커밋
</verification>

<success_criteria>
- `RunCorrectedRecheck(img, mode, slot, out corrected)` 호출로 잔여 offset(mm)·theta(deg)·score 를 얻을 수 있다
- 어떤 실패 경로에서도 예외가 밖으로 나가지 않고 `FailReason` 이 채워진다
- 기존 Align 동작(`Run()`)은 한 줄도 바뀌지 않았다
</success_criteria>

<output>
완료 후 `.planning/phases/75-align-corrected-image-evidence/75-01-SUMMARY.md` 를 작성한다.
반드시 포함할 것:
- `RunCorrectedRecheck` **오버로드 2개**의 정확한 시그니처 (75-03 은 **검출 재사용판 (B)** 를 호출한다)
- 폴백 조건(`bHasDetection == false`)에서만 `TryFindPose` 가 4회 도는 구조라는 점
- `AlignVerifyResult` 의 프로퍼티 목록 (75-02 의 CSV 컬럼, 75-05 의 UI 가 소비한다)
- `correctedImage` 의 **소유권 규약**: 호출자가 Dispose 책임을 진다. 재매칭 실패 시에도 non-null 일 수 있다
</output>
