---
phase: 75-align-corrected-image-evidence
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - WPF_Example/Utility/CaptureImageSaveService.cs
  - WPF_Example/Custom/SystemHandler.cs
autonomous: true
requirements: [D-75-01, D-75-04]
user_setup: []

must_haves:
  truths:
    - "PLC 가 $ALIGN_TEST 를 보낼 때마다 ① 잔여 offset/theta 가 CSV 에 1행씩 쌓인다 (정상/NG 무관)"
    - "Align 이 NG 이거나 재매칭이 실패한 건에 한해 보정 이미지가 파일로 남는다"
    - "정상 건에서는 이미지가 저장되지 않아 디스크/메모리가 늘지 않는다"
    - "기존 Align 응답(IsPass/OffsetX/OffsetY/Theta)과 PLC 로 나가는 값은 하나도 바뀌지 않는다"
    - "이미지 저장 큐가 밀려도 TCP 응답이 지연되지 않는다"
  artifacts:
    - path: "WPF_Example/Custom/SystemHandler.cs"
      provides: "RecordAlignVerify — ① 실행 + CSV append + NG 이미지 큐잉"
      contains: "RecordAlignVerify"
    - path: "WPF_Example/Utility/CaptureImageSaveService.cs"
      provides: "DirectoryOverride — Align 증거를 전용 폴더에 저장"
      contains: "DirectoryOverride"
  key_links:
    - from: "WPF_Example/Custom/SystemHandler.cs"
      to: "AlignShapeMatchService.RunCorrectedRecheck"
      via: "RunBottomAlign/RunTrayAlign 의 finally 에서 호출"
      pattern: "RunCorrectedRecheck"
    - from: "WPF_Example/Custom/SystemHandler.cs"
      to: "AlignVerifyCsvWriter.Append"
      via: "매 Align 1행"
      pattern: "AlignVerifyCsvWriter\\.Append"
    - from: "WPF_Example/Custom/SystemHandler.cs"
      to: "CaptureImageSaveService.Enqueue"
      via: "NG 한정 SharedHImage 큐잉"
      pattern: "CaptureImageSaver\\.Enqueue"
---

<objective>
① 을 실제 Align 경로에 붙이고, NG 건의 보정 이미지를 남긴다.

- D-75-01: **매 Align 마다** — 정상이든 NG 든 항상 ① 을 돌리고 기록한다
- D-75-04: **이미지는 NG 일 때만** — 정상 건은 숫자(①)만 남긴다

Purpose: 분쟁은 나중에 생긴다. "그 건은 OK 라 안 남겼습니다" 는 방어가 안 된다.
Output: `Custom/SystemHandler.cs` 의 Align 경로에 기록 배선 + `CaptureImageSaveRequest.DirectoryOverride`

**이 plan 의 최우선 제약은 회귀 0 이다.** PLC 로 나가는 응답은 한 비트도 바뀌면 안 된다.
그래서 모든 수정은 **순수 삽입(삭제 0줄)** 이어야 한다 — acceptance 로 강제한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-align-corrected-image-evidence/75-CONTEXT.md
@.planning/phases/75-align-corrected-image-evidence/75-01-SUMMARY.md
@.planning/phases/75-align-corrected-image-evidence/75-02-SUMMARY.md
@CLAUDE.md

<memory_incident_history>
## 🔴 이 저장소는 이미지 저장으로 두 번 크게 당했다 — 협상 대상 아님

- **58.3GB 폭증** — 일괄검사 이미지 큐 무제한 누적. 큐 상한 50 + 생산측 백프레셔로 해결(커밋 `44339bc`)
- **34~41GB + halcon.DLL 크래시** — Shot 이미지 영구보존 + 비동기 저장 레이스.
  **Dispose 해도 OS 메모리 미반환은 미해결**

지켜야 할 것:
1. **새 저장 메커니즘을 만들지 않는다.** 기존 `CaptureImageSaveService` 를 그대로 쓴다
2. **큐 상한 + 백프레셔** — 이미 서비스 안에 있다(`MAX_QUEUE_DEPTH=50`, `WaitForQueueSpace`)
3. **HImage 반드시 Dispose** — `SharedHImage` refcount 규약을 정확히 지킨다
4. **보관 상한** — 75-02 의 `AlignVerifyRetention` 이 담당한다
5. **NG 만 저장** — 정상 건은 이미지를 아예 만들지 않는다
</memory_incident_history>

<interfaces>
### 편집 대상 1: WPF_Example/Custom/SystemHandler.cs

`RunBottomAlign(EBottomAlignSlot slot, AlignResultPacket pResult)` (약 439~522줄) 의 현재 골격:
```csharp
try {
    bool bHasTemplate = ...;
    if (!bHasTemplate) { ...; FillAlignPoseZero(pResult); return false; }     // ← 조기 return A
    bool bCameraReady = ...;
    if (!bCameraReady) { ...; FillAlignPoseZero(pResult); return false; }     // ← 조기 return B

    HImage img = null;
    AlignResult res = null;
    try {
        ApplyCoaxLightForSlot(slot);
        img = EthernetVisionHandler.Handle.Camera.Grab();
        if (img == null) { ...; FillAlignPoseZero(pResult); return false; }
        res = EthernetVisionHandler.Handle.Matcher.Run(img, EEthernetVisionMode.Bottom, slot);
        if (!res.Found) { ...; FillAlignPoseZero(pResult); return false; }
        FillAlignPose(pResult, res);
        Logging.PrintLog(...);
        return true;                                                          // ← 성공 return
    }
    finally {
        if (img != null) { img.Dispose(); img = null; }                       // ← ① 는 이 앞에서 돌아야 한다
        if (res != null && res.DetectedContourXld != null) { ... }
        LightHandler.Handle.SetOnOff(LightHandler.LIGHT_ALIGN_COAX, false);
    }
}
catch (Exception ex) { ...; FillAlignPoseZero(pResult); return false; }
```
`RunTrayAlign(AlignResultPacket pResult)` (약 526~600줄) 도 **구조가 동일**하다
(슬롯이 없고 `EEthernetVisionMode.Tray` / `ApplyCoaxLightForTray()` 를 쓴다는 것만 다르다).

호출 시점에 `pResult.MaterialNo`(int, 미수신 `-1`) 와 `pResult.AlignFace`(int) 는 이미 채워져 있다
(`ProcessAlignTest` 가 진입 직후 echo 대입한다).

슬롯 토큰: `EBottomAlignSlotMap.ToFileToken(slot)` → 예: `"3D_Top"`, `"2D_SIDE_1"`

### 편집 대상 2: WPF_Example/Utility/CaptureImageSaveService.cs

```csharp
public sealed class CaptureImageSaveRequest : IDisposable {
    public SharedHImage Shared { get; set; }
    public bool   NeedsRender { get; set; }
    public string FileName { get; set; }
    public bool   IsCapture { get; set; }
    public DateTime Timestamp { get; set; } = DateTime.Now;
}

public sealed class CaptureImageSaveService : IDisposable {
    public int QueueDepth { get; }                       // 대기 + 처리중 합계
    public void Enqueue(CaptureImageSaveRequest request); // 내부에서 WaitForQueueSpace() = 백프레셔
    public static string BuildFileName(string prefix, string sequence, string faiName,
                                       string measurePointSegment, string judgement,
                                       DateTime ts, int nIndexNumber);
    public static string BuildDirectory(bool isCapture, DateTime ts);
}
```

`SaveRequest()` 내부 (약 312줄):
```csharp
string baseDirectory = BuildDirectory(request.IsCapture, request.Timestamp);
Directory.CreateDirectory(baseDirectory);
```

### SharedHImage refcount 규약 (Action_FAIMeasurement.cs:1322~1344 의 실제 사용 패턴)
```csharp
SharedHImage shared = new SharedHImage(image.CopyImage());  // ref=1 (생성자 = 호출부 소유)
shared.AddRef();                                            // ref=2 (요청 소유)
saver.Enqueue(new CaptureImageSaveRequest { Shared = shared, ... });
// 워커가 처리 후 request.Dispose() → Release() → ref=1
shared.Release();                                           // 호출부 소유 해제 → ref=0 → 내부 HImage Dispose
```
**AddRef 없이 Enqueue 하면 이중 Release, Release 를 빼먹으면 이미지 누수다.**
`saver == null` 인 경로에서도 `shared.Release()` 는 반드시 호출해야 한다.

### 75-02 가 만든 것
- `AlignVerifyRecord` (POCO, 상수 `KIND_ALIGN`/`TARGET_TRAY`/`TARGET_BOTTOM`/`JUDGE_OK`/`JUDGE_NG`/`NO_MATERIAL`)
- `AlignVerifyCsvWriter.Append(AlignVerifyRecord rec)`
- `AlignVerifyRetention.BuildAlignImageDirectory(DateTime ts)` → `{ResultSavePath}\AlignVerify\{yyMMdd}`

### 75-01 이 만든 것
```csharp
public AlignVerifyResult RunCorrectedRecheck(
    HImage img, EEthernetVisionMode mode, EBottomAlignSlot slot, out HImage correctedImage);
```
`correctedImage` 는 **호출자가 Dispose 책임**을 진다. 재매칭 실패 시에도 non-null 일 수 있다.
</interfaces>
</context>

<coding_rules>
- 삼항 `?:` **금지** → `if/else`. `??` / `?.` **금지**
- 전통 `switch` 만. **C# 7.2 문법만**. 긴 조건은 이름 있는 `bool` 로 선추출
- 헝가리언 `b`/`n`/`sz`
- **중괄호 스타일**: `Custom/SystemHandler.cs` = **Allman**(여는 중괄호 새 줄),
  `Utility/CaptureImageSaveService.cs` = **K&R**. 파일별로 맞춘다
- 날짜 주석 신규 금지
- **모든 수정은 순수 삽입(삭제 0줄)** — acceptance 로 검증한다
- 실행 중 프로세스 종료 금지. `DatumMeasurement.csproj` 커밋 금지(이 plan 은 신규 파일이 없어 원래 안 건드린다)
</coding_rules>

<build_verification>
```bash
MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"
SCR="/c/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/7fe7f8e3-9e05-4190-9941-3b1cfdde3971/scratchpad"
"$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 \
  -p:OutDir="$SCR/build75/on/" -p:IntermediateOutputPath="$SCR/build75/obj-on/" -v:m -nologo
```
판정 = **에러 0 + 새 경고 코드 종류 0건**(`CS0162`/`CS0618` 외 등장 금지). "경고 0" 아님
</build_verification>

<tasks>

<task type="auto">
  <name>Task 1: CaptureImageSaveRequest 에 DirectoryOverride 추가 (순수 삽입)</name>

  <read_first>
    - `WPF_Example/Utility/CaptureImageSaveService.cs:78~112` (`CaptureImageSaveRequest` 클래스 전체)
    - `WPF_Example/Utility/CaptureImageSaveService.cs:290~345` (`SaveRequest` — `baseDirectory` 계산부와 포맷 결정부)
    - `WPF_Example/Utility/CaptureImageSaveService.cs:404~425` (`BuildDirectory` / `BuildFilePath`)
  </read_first>

  <files>WPF_Example/Utility/CaptureImageSaveService.cs</files>

  <action>
Align 증거 이미지를 검사 이미지 트리(`Image\{yyMMdd}\{HHmm}\capture`)에 섞지 않기 위해
저장 폴더를 요청 단위로 덮어쓸 수 있게 한다. **기존 호출부 동작은 그대로다**(null/빈 문자열이면 기존 로직).

**(1) `CaptureImageSaveRequest` 에 프로퍼티 1개 추가** (기존 프로퍼티 블록 끝에 삽입):
```csharp
/// <summary>
/// 저장 폴더 강제 지정. 비어 있으면 기존 BuildDirectory(IsCapture, Timestamp) 규칙을 그대로 쓴다.
/// Align 정합 증거 이미지를 검사 이미지 트리와 분리해 별도 보관 정책을 걸기 위해 도입.
/// </summary>
public string DirectoryOverride { get; set; }
```

**(2) `SaveRequest()` 에서 override 적용** — 기존 줄
```csharp
string baseDirectory = BuildDirectory(request.IsCapture, request.Timestamp);
```
**바로 다음 줄에** 아래를 삽입한다. **위 줄은 고치지 않는다**(순수 삽입 유지):
```csharp
bool bHasDirOverride = !string.IsNullOrEmpty(request.DirectoryOverride);
if (bHasDirOverride) { baseDirectory = request.DirectoryOverride; }
```

이 파일에서 하는 일은 **이 두 군데 삽입이 전부**다. 다른 줄은 손대지 않는다.
(파일 스타일이 K&R 이므로 여는 중괄호는 같은 줄에.)
  </action>

  <verify>
    <automated>
    cd /c/Info/Project/DataMeasurement && \
    C=WPF_Example/Utility/CaptureImageSaveService.cs && \
    echo "prop=$(grep -c 'public string DirectoryOverride' $C)" && \
    echo "use=$(grep -c 'bHasDirOverride' $C)" && \
    echo "deletions=$(git diff -U0 -- $C | grep '^-' | grep -vc '^---')" && \
    echo "queueConst=$(grep -c 'MAX_QUEUE_DEPTH = 50' $C)"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'public string DirectoryOverride' CaptureImageSaveService.cs` == `1`
    - `grep -c 'bHasDirOverride' CaptureImageSaveService.cs` == `2` (선언 1 + 사용 1)
    - **순수 삽입:** `git diff -U0 -- WPF_Example/Utility/CaptureImageSaveService.cs | grep '^-' | grep -v '^---' | wc -l` == `0`
    - **큐 상한 무변경:** `grep -c 'MAX_QUEUE_DEPTH = 50' CaptureImageSaveService.cs` == `1`
    - **워커 수 무변경:** `grep -c 'WORKER_COUNT = 6' CaptureImageSaveService.cs` == `1`
    - **백프레셔 무변경:** `grep -c 'WaitForQueueSpace();' CaptureImageSaveService.cs` == `1`
    - `git diff -U0 -- WPF_Example/Utility/CaptureImageSaveService.cs | grep '^+' | grep -cE '\?\?|\?\.|\? .+ :'` == `0`
  </acceptance_criteria>

  <done>저장 폴더를 요청 단위로 지정할 수 있고, 기존 저장 경로/큐/백프레셔 동작은 그대로다.</done>
</task>

<task type="auto">
  <name>Task 2: Align 경로에 ① 실행 + CSV 기록 + NG 이미지 큐잉 배선 (순수 삽입)</name>

  <read_first>
    - `WPF_Example/Custom/SystemHandler.cs:396~437` (`ProcessAlignTest` — MaterialNo/AlignFace echo 지점)
    - `WPF_Example/Custom/SystemHandler.cs:439~522` (`RunBottomAlign` 전체)
    - `WPF_Example/Custom/SystemHandler.cs:524~600` (`RunTrayAlign` 전체)
    - `WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs:1322~1344` (`SharedHImage` AddRef/Enqueue/Release 실제 패턴)
    - `.planning/phases/75-align-corrected-image-evidence/75-01-SUMMARY.md` (RunCorrectedRecheck 시그니처/소유권)
    - `.planning/phases/75-align-corrected-image-evidence/75-02-SUMMARY.md` (AlignVerifyRecord 필드 ↔ 컬럼)
  </read_first>

  <files>WPF_Example/Custom/SystemHandler.cs</files>

  <action>
**모든 편집은 순수 삽입이다. 기존 줄을 수정/삭제하지 않는다.**

### (A) 상수 2개를 클래스 상단(기존 상수 근처)에 삽입
```csharp
// Align 증거 이미지는 "있으면 좋은" 보조 자료다. 저장 큐가 밀렸을 때 TCP 응답 스레드를
// 백프레셔로 세우면 PLC 사이클이 지연된다 — 그 상황에서는 이미지를 포기하고 숫자만 남긴다.
private const int ALIGN_IMAGE_MAX_QUEUE_DEPTH = 25;
private const string ALIGN_IMAGE_PREFIX_CORRECTED = "aligncorr";
private const string ALIGN_IMAGE_PREFIX_RAW = "alignraw";
```

### (B) `RunBottomAlign` 에 3곳 삽입

1. 조기 return A(`!bHasTemplate`) 의 `return false;` **직전**:
   `RecordAlignFailureOnly(EEthernetVisionMode.Bottom, slot, pResult, "미티칭");`
2. 조기 return B(`!bCameraReady`) 의 `return false;` **직전**:
   `RecordAlignFailureOnly(EEthernetVisionMode.Bottom, slot, pResult, "카메라 미연결");`
3. 내부 블록:
   - `AlignResult res = null;` **다음 줄**에 `bool bAlignPass = false;` 삽입
   - 성공 경로의 `return true;` **직전**에 `bAlignPass = true;` 삽입
   - `finally {` 블록 안, **기존 `if (img != null)` 줄보다 위**에:
     `RecordAlignVerify(EEthernetVisionMode.Bottom, slot, pResult, img, res, bAlignPass);`
     (① 은 `img` 가 살아 있는 동안 돌아야 한다)

### (C) `RunTrayAlign` 에 동일하게 삽입
`EEthernetVisionMode.Tray` / `EBottomAlignSlot.None` 을 넘긴다. 나머지는 (B)와 동일.

### (D) 새 private 메서드 3개를 `FillAlignPoseZero` 아래에 추가 (Allman 스타일)

```csharp
// D-75-01: 정상/NG 무관 매 Align 마다 ① 재매칭 결과를 기록한다.
//  분쟁은 나중에 생긴다 — "그 건은 OK 라 안 남겼습니다" 는 방어가 안 된다.
//  실패해도 throw 하지 않는다(TCP 스레드 크래시 방지). Align 응답에는 일절 영향을 주지 않는다.
private void RecordAlignVerify(EEthernetVisionMode mode, EBottomAlignSlot slot,
                               AlignResultPacket pResult, HImage img, AlignResult res, bool bAlignPass)
```
동작 순서:
1. `HImage corrected = null;` / `AlignVerifyResult verify = null;`
2. `bool bCanRecheck = (img != null) && (res != null) && res.Found;`
   `if (bCanRecheck) { verify = EthernetVisionHandler.Handle.Matcher.RunCorrectedRecheck(img, mode, slot, out corrected); }`
3. `AlignVerifyRecord rec = new AlignVerifyRecord();` 채우기:
   - `RecordTime = DateTime.Now`
   - `Kind = AlignVerifyRecord.KIND_ALIGN`
   - `MaterialNo = pResult.MaterialNo`
   - `Target` — `if (mode == EEthernetVisionMode.Bottom) { TARGET_BOTTOM } else { TARGET_TRAY }`
   - `SlotToken` — Bottom 이고 `slot != None` 이면 `EBottomAlignSlotMap.ToFileToken(slot)`, 아니면 `""`
   - `Judgement` — `if (bAlignPass) { JUDGE_OK } else { JUDGE_NG }`
   - `verify != null && verify.Verified` 이면
     `ResidualOffsetXmm/ResidualOffsetYmm/ResidualThetaDeg/Score` 대입 + `HasResidual = true`
   - `FailReason` — `verify != null` 이면 `verify.FailReason`, 아니면 `"Align 검출 실패"` (`??` 금지, if/else)
4. **NG 한정 이미지 저장 (D-75-04)**:
   ```csharp
   bool bVerifyFailed = false;
   if (verify != null) { if (!verify.Verified) { bVerifyFailed = true; } }
   bool bNeedImage = (!bAlignPass) || bVerifyFailed;
   if (bNeedImage) { rec.ImageFileName = EnqueueAlignEvidenceImage(corrected, img, pResult, mode, slot, rec.RecordTime); }
   ```
   **정상 건에서는 `EnqueueAlignEvidenceImage` 를 호출조차 하지 않는다** — CopyImage 도 안 일어난다.
5. `AlignVerifyCsvWriter.Append(rec);`
6. 전체를 `try / catch (Exception ex) { Logging.PrintLog((int)ELogType.Error, "[ALIGN_VERIFY] 기록 실패: {0}", ex.Message); } finally { if (corrected != null) { corrected.Dispose(); corrected = null; } }`
   로 감싼다. **`corrected` 는 어떤 경로에서도 반드시 Dispose 된다.**

```csharp
// 이미지 없이 실패한 Align(미티칭/카메라 미연결)도 기록은 남긴다 — D-75-01.
private void RecordAlignFailureOnly(EEthernetVisionMode mode, EBottomAlignSlot slot,
                                    AlignResultPacket pResult, string szReason)
```
→ `Kind=KIND_ALIGN`, `Judgement=JUDGE_NG`, `HasResidual=false`, `FailReason=szReason` 인 레코드 1건 Append.
전체 try/catch, throw 금지.

```csharp
// NG 증거 이미지 1장 큐잉. 저장 실패/생략은 기록의 다른 값에 영향을 주지 않는다.
// 반환값 = 저장될 파일명(생략 시 빈 문자열).
private string EnqueueAlignEvidenceImage(HImage corrected, HImage raw, AlignResultPacket pResult,
                                         EEthernetVisionMode mode, EBottomAlignSlot slot, DateTime ts)
```
동작:
1. `CaptureImageSaveService saver = SystemHandler.Handle.CaptureImageSaver;`
   `if (saver == null) { return ""; }`
2. **큐 포화 가드 (TCP 스레드 정지 방지)**:
   ```csharp
   bool bQueueBusy = saver.QueueDepth >= ALIGN_IMAGE_MAX_QUEUE_DEPTH;
   if (bQueueBusy)
   {
       Logging.PrintLog((int)ELogType.Error,
           "[ALIGN_VERIFY] 저장 큐 혼잡(depth={0}) — 증거 이미지 생략, 수치 기록은 유지",
           saver.QueueDepth);
       return "";
   }
   ```
3. 소스 선택 (삼항 금지):
   ```csharp
   HImage src = null; string szPrefix = ALIGN_IMAGE_PREFIX_RAW;
   if (corrected != null) { src = corrected; szPrefix = ALIGN_IMAGE_PREFIX_CORRECTED; }
   else if (raw != null)  { src = raw; }
   if (src == null) { return ""; }
   ```
   보정 이미지가 있으면 그것을 우선 저장한다 — 원 요구가 "**보정한** 이미지 저장" 이다.
   보정 자체가 불가능했던 건(검출 실패)은 원본이라도 남긴다.
4. 파일명 / 폴더:
   ```csharp
   string szTargetToken = "TRAY"; if (mode == EEthernetVisionMode.Bottom) { szTargetToken = "BOTTOM"; }
   string szSlotToken = ""; if (slot != EBottomAlignSlot.None) { szSlotToken = EBottomAlignSlotMap.ToFileToken(slot); }
   string szFileName = CaptureImageSaveService.BuildFileName(
       szPrefix, "ALIGN_" + szTargetToken, szSlotToken, "", "NG", ts, pResult.MaterialNo);
   string szDir = AlignVerifyRetention.BuildAlignImageDirectory(ts);
   ```
5. **SharedHImage 규약 — 순서를 바꾸지 말 것**:
   ```csharp
   SharedHImage shared = new SharedHImage(src.CopyImage());   // ref=1
   try
   {
       shared.AddRef();                                        // ref=2 (요청 소유)
       CaptureImageSaveRequest req = new CaptureImageSaveRequest();
       req.Shared = shared;
       req.NeedsRender = false;        // 오버레이 렌더 불필요 — 원본/보정본 그대로
       req.IsCapture = true;           // jpeg 고정 경로(파일명 확장자와 일치)
       req.Timestamp = ts;
       req.DirectoryOverride = szDir;
       req.FileName = szFileName;
       saver.Enqueue(req);
   }
   finally
   {
       shared.Release();               // 생성자 소유 해제. 워커의 req.Dispose() 와 합쳐 ref 0 → HImage Dispose
   }
   return szFileName;
   ```
   `src.CopyImage()` 를 쓰는 이유: `img`/`corrected` 의 수명은 호출부(`finally`)가 이미 소유하고 있다.
   같은 핸들을 워커에 넘기면 이중 Dispose/use-after-dispose 가 된다. **NG 건에서만 일어나는 복사다.**
6. 전체 `try/catch` — 실패해도 `""` 반환, throw 금지.
  </action>

  <verify>
    <automated>
    cd /c/Info/Project/DataMeasurement && \
    H=WPF_Example/Custom/SystemHandler.cs && \
    echo "deletions=$(git diff -U0 -- $H | grep '^-' | grep -vc '^---')" && \
    echo "recheck=$(grep -c 'RunCorrectedRecheck' $H)" && \
    echo "append=$(grep -c 'AlignVerifyCsvWriter.Append' $H)" && \
    echo "recordCalls=$(grep -c 'RecordAlignVerify(' $H)" && \
    echo "failOnly=$(grep -c 'RecordAlignFailureOnly(' $H)" && \
    echo "addref=$(grep -c 'shared.AddRef();' $H)" && \
    echo "release=$(grep -c 'shared.Release();' $H)" && \
    echo "copy=$(grep -c 'CopyImage()' $H)" && \
    echo "queueGuard=$(grep -c 'ALIGN_IMAGE_MAX_QUEUE_DEPTH' $H)" && \
    echo "correctedDispose=$(grep -c 'corrected.Dispose();' $H)" && \
    echo "newThrow=$(git diff -U0 -- $H | grep '^+' | grep -c 'throw ')" && \
    echo "newTernary=$(git diff -U0 -- $H | grep '^+' | grep -cE '\?\?|\?\.|\? .+ :')"
    </automated>
  </verify>

  <acceptance_criteria>
    ⚠ 아래 grep 수치는 **편집 전 실측값이 0 임을 먼저 확인**하고(전부 신규 심볼이다) 편집 후 값을 본다.
    수치가 안 맞는다고 **기존 코드를 지워서 맞추지 말 것** — 삽입 위치를 재확인하는 것이 정답이다.

    - **회귀 0 (최우선):** `git diff -U0 -- WPF_Example/Custom/SystemHandler.cs | grep '^-' | grep -v '^---' | wc -l` == `0`
      (삭제/수정 0줄 = 기존 판정·응답 경로 무변경)
    - `grep -c 'RecordAlignVerify(' Custom/SystemHandler.cs` == `3` (정의 1 + RunBottomAlign 1 + RunTrayAlign 1)
    - `grep -c 'RecordAlignFailureOnly(' Custom/SystemHandler.cs` == `5` (정의 1 + 조기return 4곳)
    - `grep -c 'RunCorrectedRecheck' Custom/SystemHandler.cs` == `1`
    - `grep -c 'AlignVerifyCsvWriter.Append' Custom/SystemHandler.cs` == `2` (RecordAlignVerify 1 + RecordAlignFailureOnly 1)
    - **SharedHImage 규약:** `grep -c 'shared.AddRef();'` == `1` **그리고** `grep -c 'shared.Release();'` == `1`
      (둘 중 하나만 있으면 누수 또는 이중 해제다)
    - `grep -c 'CopyImage()' Custom/SystemHandler.cs` == `1` (NG 경로 1곳뿐)
    - `grep -c 'ALIGN_IMAGE_MAX_QUEUE_DEPTH' Custom/SystemHandler.cs` == `2` (상수 1 + 가드 1)
    - `grep -c 'corrected.Dispose();' Custom/SystemHandler.cs` == `1` (finally 단일 지점)
    - 추가된 줄에 `throw` 0건: `git diff -U0 -- ... | grep '^+' | grep -c 'throw '` == `0`
    - 추가된 줄에 `?:`/`??`/`?.` 0건
    - **판정 로직 무변경 증거:** `git diff -U0 -- ... | grep '^+' | grep -c 'FillAlignPose\|IsPass\|EVisionResultType'` == `0`
      (응답 채움/판정 심볼을 새로 건드리지 않는다)
  </acceptance_criteria>

  <done>매 Align 마다 ① 이 돌아 CSV 1행이 쌓이고, NG 건만 보정 이미지가 큐잉되며, 기존 Align 응답 경로는 한 줄도 바뀌지 않았다.</done>
</task>

<task type="auto">
  <name>Task 3: 빌드 + 회귀 0 최종 확인 + 커밋</name>

  <read_first>
    - 이 파일의 `<build_verification>` 섹션
    - `git diff --stat` 출력 (이번 plan 이 만진 파일이 정확히 2개인지)
  </read_first>

  <files>없음 (검증 전용)</files>

  <action>
1. SIMUL-ON 빌드를 돌린다. 에러 0 / 새 경고 코드 0건 확인.
2. SIMUL-OFF 도 돌린다 (`-p:DefineConstants=TRACE%3BDEBUG`). `Release|x64` 는 SIMUL-OFF 가 **아니다**:
   ```bash
   "$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 \
     -p:DefineConstants=TRACE%3BDEBUG \
     -p:OutDir="$SCR/build75/off/" -p:IntermediateOutputPath="$SCR/build75/obj-off/" -v:m -nologo
   ```
3. `git diff --stat` 이 `CaptureImageSaveService.cs` 와 `Custom/SystemHandler.cs` **2개만** 보여야 한다.
   `DatumMeasurement.csproj` 가 이번 plan 때문에 바뀌었다면 되돌린다(신규 파일이 없으므로 바뀔 이유가 없다).
4. 커밋:
   ```bash
   git add WPF_Example/Utility/CaptureImageSaveService.cs WPF_Example/Custom/SystemHandler.cs
   git commit -m "feat(75-03): 매 Align 마다 보정 후 재매칭 기록 + NG 건 보정 이미지 저장"
   ```
  </action>

  <verify>
    <automated>
    cd /c/Info/Project/DataMeasurement && \
    MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" && \
    SCR="/c/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/7fe7f8e3-9e05-4190-9941-3b1cfdde3971/scratchpad" && \
    "$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 \
      -p:OutDir="$SCR/build75/on/" -p:IntermediateOutputPath="$SCR/build75/obj-on/" -v:m -nologo 2>&1 \
      | tee "$SCR/build75-03-on.log" | tail -3 ; \
    "$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 \
      -p:DefineConstants=TRACE%3BDEBUG \
      -p:OutDir="$SCR/build75/off/" -p:IntermediateOutputPath="$SCR/build75/obj-off/" -v:m -nologo 2>&1 \
      | tee "$SCR/build75-03-off.log" | tail -3 ; \
    echo "ERR_ON=$(grep -c ' error ' "$SCR/build75-03-on.log")" ; \
    echo "ERR_OFF=$(grep -c ' error ' "$SCR/build75-03-off.log")" ; \
    echo "CODES_ON=$(grep -oE 'CS[0-9]{4}' "$SCR/build75-03-on.log" | sort -u | tr '\n' ' ')" ; \
    echo "CODES_OFF=$(grep -oE 'CS[0-9]{4}' "$SCR/build75-03-off.log" | sort -u | tr '\n' ' ')" ; \
    git diff --stat --cached
    </automated>
  </verify>

  <acceptance_criteria>
    - SIMUL-ON / SIMUL-OFF 둘 다 `grep -c ' error '` == `0`
    - 두 로그의 경고 코드 집합이 `CS0162 CS0618` 를 벗어나지 않는다
    - `git show --stat HEAD` 에 파일이 정확히 2개 (`CaptureImageSaveService.cs`, `Custom/SystemHandler.cs`)
    - `git show --stat HEAD | grep -c csproj` == `0`
    - `git show HEAD | grep '^-' | grep -v '^---' | wc -l` == `0` (커밋 전체가 순수 추가)
  </acceptance_criteria>

  <done>두 빌드 구성 모두 통과하고, 커밋 전체가 순수 추가로 구성되어 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLC → TCP 스레드 → RecordAlignVerify | 외부 트리거. 여기서 예외/블로킹이 나면 PLC 사이클이 멈춘다 |
| TCP 스레드 → CaptureImageSaveService 큐 | 큐가 밀리면 백프레셔가 호출 스레드를 최대 30초까지 세운다 |
| Align 이미지 → 디스크 | NG 건마다 파일이 쌓인다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-10 | Denial of Service | Enqueue 백프레셔가 TCP 스레드를 정지 | mitigate | `ALIGN_IMAGE_MAX_QUEUE_DEPTH(25)` 사전 가드 — 큐가 절반 이상 차면 이미지를 포기하고 즉시 반환. 수치 기록은 유지 |
| T-75-11 | Denial of Service | 메모리 폭증(과거 58.3GB/41GB 사고) | mitigate | ① NG 한정 저장(정상 건 CopyImage 0회) ② 기존 큐 상한 50 재사용(무변경) ③ `SharedHImage` AddRef/Release 1:1 ④ `corrected` finally-Dispose ⑤ 75-02 보관 상한 |
| T-75-12 | Denial of Service | RecordAlignVerify 예외가 TCP 스레드 종료 | mitigate | 세 메서드 모두 전체 try/catch, 추가된 줄에 `throw` 0건(acceptance 검증) |
| T-75-13 | Tampering | 파일명 path traversal | mitigate | `CaptureImageSaveService.BuildFileName` 이 `Path.GetInvalidFileNameChars()` 로 segment 를 sanitize 하고, `SaveRequest` 가 `SanitizeFileName` 으로 2차 방어한다. MaterialNo 는 int 라 traversal 문자가 불가능하다 |
| T-75-14 | Elevation of Privilege | `DirectoryOverride` 로 임의 경로 쓰기 | accept | 값의 출처가 코드 내부(`AlignVerifyRetention.BuildAlignImageDirectory`)뿐이고 외부 입력이 들어오지 않는다. 외부 입력을 넣게 되면 그때 화이트리스트가 필요하다 |
</threat_model>

<verification>
1. `Custom/SystemHandler.cs` 삭제 0줄 (기존 판정/응답 경로 무변경)
2. `CaptureImageSaveService.cs` 삭제 0줄 + 큐 상한/워커수/백프레셔 상수 무변경
3. `shared.AddRef()` 1건 ↔ `shared.Release()` 1건 (refcount 균형)
4. `CopyImage()` 1건 (NG 경로 한정)
5. 추가 줄에 `throw` 0건, `?:`/`??`/`?.` 0건
6. SIMUL-ON / SIMUL-OFF 빌드 에러 0, 새 경고 코드 0건
</verification>

<success_criteria>
- 매 Align(정상 포함)마다 `{AlignVerifySavePath}\yyyyMMdd.csv` 에 `ALIGN` 행이 1건 추가된다
- Align NG 또는 재매칭 실패 건에서만 `{ResultSavePath}\AlignVerify\{yyMMdd}\aligncorr_*.jpg` 가 생긴다
- 정상 건에서는 이미지 파일이 만들어지지 않고 `CopyImage` 도 호출되지 않는다
- PLC 로 나가는 `$ALIGN_RESULT` 값과 `IsPass` 는 변화 없다
</success_criteria>

<output>
완료 후 `.planning/phases/75-align-corrected-image-evidence/75-03-SUMMARY.md` 를 작성한다.
반드시 포함할 것:
- 실제로 삽입한 위치(파일:줄) 목록과 `git diff` 삭제 0줄 증거
- 이미지 저장 파일명 실제 형식 예시 1개 (75-05 UI 가 파일을 찾아 열 때 쓴다)
- `ALIGN_IMAGE_MAX_QUEUE_DEPTH` 값과 "큐 혼잡 시 이미지 생략" 정책 (75-06 문서화 대상)
</output>
