---
phase: 75-align-corrected-image-evidence
plan: 05
type: execute
wave: 2
depends_on: [02]
files_modified:
  - WPF_Example/Custom/EthernetVision/AlignVerifyCsvLoader.cs
  - WPF_Example/UI/Reviewer/AlignVerifyViewModel.cs
  - WPF_Example/UI/Reviewer/AlignVerifyWindow.xaml
  - WPF_Example/UI/Reviewer/AlignVerifyWindow.xaml.cs
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [D-75-02, D-75-03]
user_setup: []

must_haves:
  truths:
    - "결과 리뷰어에서 버튼 하나로 Align 정합 조회 창을 열 수 있다"
    - "자재번호를 넣으면 그 자재의 ① Align 계산값과 ② 안착 위치가 한 화면에 나란히 보인다"
    - "임계값이 설정되지 않은 상태에서는 정상/벗어남 판정을 하지 않고 숫자만 보여준다"
    - "최근 N개 ①/② 평균·최대 추세를 볼 수 있고, SIDE 는 지그 4개가 따로 나온다"
    - "새 화면의 로직은 ViewModel 에 있고 code-behind 에는 UI 배선만 있다"
  artifacts:
    - path: "WPF_Example/Custom/EthernetVision/AlignVerifyCsvLoader.cs"
      provides: "CSV 조회/집계 — 자재번호 조인 + 최근 N개 통계"
      contains: "Query"
    - path: "WPF_Example/UI/Reviewer/AlignVerifyViewModel.cs"
      provides: "조회 화면 상태/표시 문자열 (INotifyPropertyChanged)"
      contains: "INotifyPropertyChanged"
    - path: "WPF_Example/UI/Reviewer/AlignVerifyWindow.xaml"
      provides: "Align 정합 조회 창 (MVVM 바인딩)"
      contains: "DataContext"
  key_links:
    - from: "WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs"
      to: "AlignVerifyWindow"
      via: "좌측 버튼 스택의 새 버튼 클릭"
      pattern: "new AlignVerifyWindow"
    - from: "AlignVerifyViewModel"
      to: "AlignVerifyCsvLoader.Query"
      via: "조회 버튼"
      pattern: "AlignVerifyCsvLoader\\.Query"
---

<objective>
쌓인 기록을 사람이 읽는 화면을 만든다.

읽는 법은 하나뿐이다 — **①부터 본다.**
- ① 이 벗어남 → **비전** 문제
- ① 정상인데 ② 가 벗어남 → **피커** 문제

Purpose: 숫자가 CSV 에만 있으면 분쟁 현장에서 못 쓴다. 자재번호 하나로 즉시 답이 나와야 한다.
Output: `AlignVerifyCsvLoader` + `AlignVerifyViewModel` + `AlignVerifyWindow` + 리뷰어 버튼 1개

## 🔴 임계값은 이번에 정하지 않는다
"몇 mm 를 벗어남으로 볼지" 는 **실측 산포 없이 정할 수 없다.**
임계를 임의로 넣으면 정상품을 버리는 사고가 난다.

→ 설정(`AlignVerifyResidualLimitMm` / `AlignVerifySeatLimitMm`)의 기본값은 **0 = 미설정**이다.
   **0 인 동안 화면은 판정 문구를 절대 표시하지 않는다.** 숫자와 추세만 보여준다.
   사용자가 나중에 값을 넣으면 그때부터 정상/벗어남과 결론 문구가 나온다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-align-corrected-image-evidence/75-CONTEXT.md
@.planning/phases/75-align-corrected-image-evidence/75-02-SUMMARY.md
@CLAUDE.md

<ui_spec>
## 화면 구성 (사용자 합의 그림 그대로)

```
┌ Align 정합 조회 ───────────────────────────────────────────┐
│ 자재번호 [ 12345 ]  기간 [2026-08-01]~[2026-08-27]  [조회]  │
│ 최근 N개 [1000 ▼]                                          │
├────────────────────────────────────────────────────────────┤
│ ■ 자재번호 12345                                            │
│    ① Align 계산    0.0231 mm   theta 0.0040°   (판정 기준 미설정) │
│    ② 안착 위치     +0.0412 mm                  (판정 기준 미설정) │
│    → 판정 기준이 설정되지 않아 결론을 내지 않습니다.          │
├────────────────────────────────────────────────────────────┤
│ ■ 최근 1000개 추세                                          │
│    ① Align 계산    평균 0.0300 mm   최대 0.1100 mm  (n=1000) │
│    ② 안착 위치     평균 0.0500 mm   최대 0.1800 mm  (n=980)  │
├────────────────────────────────────────────────────────────┤
│ ■ 시퀀스별 ② 안착 편차 (지그별)      [DataGrid]              │
│    시퀀스 | Datum | 건수 | 평균mm | 최대mm | 최근mm          │
│    TOP    | D1    | 250  | 0.041  | 0.120  | 0.038          │
│    SIDE_1 | D1    | 250  | 0.052  | 0.180  | 0.049          │
│    ...                                                      │
├────────────────────────────────────────────────────────────┤
│ ■ 조회 자재 상세 [DataGrid]                                  │
│    시각 | 구분 | 대상/시퀀스 | 슬롯/Datum | 값 | 판정 | 이미지파일 │
└────────────────────────────────────────────────────────────┘
```

## 표시 규칙 (전부 ViewModel 에서 문자열로 만들어 바인딩한다)

| 항목 | 계산 | 표시 |
|------|------|------|
| ① 크기 | ALIGN 행의 `sqrt(잔여X² + 잔여Y²)` | `F4` + `" mm"` |
| ① theta | ALIGN 행의 `잔여ThetaDeg` | `F4` + `"°"` |
| ② 편차 | SEAT 행의 `sqrt((검출Row-기준Row)² + (검출Col-기준Col)²) × 해상도mmPerPx` | `F4` + `" mm"` |
| ② 해상도 0 | `해상도mmPerPx <= 0` | 값 대신 `"환산 불가(px 만): {px}px"` 로 표시. **0 을 곱해 0mm 로 보여주면 안 된다** |
| 판정 | 해당 임계값 `> 0` 일 때만 | `"정상"` / `"벗어남"`. 임계 `0` 이면 `"(판정 기준 미설정)"` |
| 결론 | ①/② 임계가 **둘 다** `> 0` 일 때만 | 아래 표 |

결론 문구 (임계 둘 다 설정된 경우에만):
| ① | ② | 문구 |
|---|---|------|
| 정상 | 정상 | `"정상"` |
| 정상 | 벗어남 | `"비전은 맞게 줬는데 놓는 위치가 틀어졌습니다 (피커 쪽 점검)"` |
| 벗어남 | (무관) | `"Align 계산 자체가 기준에 못 미칩니다 (비전 쪽 점검)"` |
| 데이터 없음 | | `"해당 자재번호의 기록이 없습니다"` |

## 알려진 한계 — 화면 하단에 항상 고정 표시할 것
```
※ SIDE 는 측면 촬영이라 앞뒤(깊이) 방향은 검증되지 않습니다. 좌우·높이만 확인됩니다.
※ ① 은 검출·강체변환의 자기일관성만 검증합니다. 피커센터 기준 재표현(부호 규약)은 ②로 확인합니다.
```
(SEED/CONTEXT 의 "알려진 한계" 를 화면에서 지우지 말 것. 이 문구가 오해를 막는다.)
</ui_spec>

<interfaces>
### 75-02 산출물
- `AlignVerifyRecord` — 20 컬럼 대응 POCO. 상수 `KIND_ALIGN`/`KIND_SEAT`/`NO_MATERIAL`
- `AlignVerifyCsvWriter.CSV_HEADER` — 컬럼 순서 단일 소스 (75-02-SUMMARY 에 원문 있음)
- 설정: `AlignVerifySavePath` / `AlignVerifyResidualLimitMm`(0=미설정) / `AlignVerifySeatLimitMm`(0=미설정)

### 그대로 베낄 로더 원본: WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs (519줄)
```csharp
private const int COLUMN_COUNT = 14;      // 컬럼 인덱스 상수 + fields.Count 가드 패턴
private const string HEADER_FIRST_TOKEN = "검사일시";
public static StatisticsQueryResult Query(DateTime dtFrom, DateTime dtTo, string szRecipeFilter)
{
    for (DateTime d = dtFrom.Date; d <= dtTo.Date; d = d.AddDays(1)) {
        string szPath = Path.Combine(szDir, d.ToString("yyyyMMdd") + CSV_EXT);
        if (!File.Exists(szPath)) { continue; }
        ...
    }
}
```
**RFC4180 파싱(따옴표 안의 콤마)도 이 원본에 이미 구현되어 있다 — 그 함수를 그대로 복제한다.
직접 `Split(',')` 를 새로 짜지 말 것.**

### ViewModel 패턴: WPF_Example/UI/Login/AccountManagerViewModel.cs
```csharp
public class AccountManagerViewModel : INotifyPropertyChanged {
    public event PropertyChangedEventHandler PropertyChanged;
    ...
}
```
⚠ 원본은 `PropertyChanged?.Invoke(...)` 를 쓰지만 **`?.` 는 이번 규칙상 금지**다. 대신:
```csharp
private void Raise(string szName)
{
    PropertyChangedEventHandler handler = PropertyChanged;
    if (handler != null) { handler(this, new PropertyChangedEventArgs(szName)); }
}
```

### 창 여는 패턴: WPF_Example/MainWindow.xaml.cs:396~402
```csharp
mReviewerWindow = new UI.ReviewerWindow();
mReviewerWindow.Owner = this;
mReviewerWindow.Show();   // 비모달
```

### ReviewerWindow 좌측 버튼 스택 (WPF_Example/UI/Reviewer/ReviewerWindow.xaml:37~73)
`btn_loadFolder` / `btn_exportExcel` / (Separator) / `txt_materialIndex` / `chk_repeatAccumulate` /
`btn_repeatRun` / `lbl_repeatProgress` / `btn_repeatExport` / `btn_cpkReportExport` / `btn_chartSmoke`
가 하나의 `<StackPanel Grid.Row="0" Orientation="Vertical">` 안에 있다.

⚠ `txt_materialIndex` 는 **"이미지 폴더 반복 검사"의 입력란**이다. 조회용으로 용도를 바꾸지 말 것.
   새 창을 열 때 그 값이 비어 있지 않으면 **초기값으로 복사만** 한다.
</interfaces>
</context>

<coding_rules>
- 삼항 `?:` **금지** → `if/else`. `??` / `?.` **금지** (`PropertyChanged?.Invoke` 포함)
- 전통 `switch` 만. **C# 7.2 문법만**. 긴 조건은 이름 있는 `bool` 로 선추출
- 헝가리언 `b`/`n`/`sz`
- **UI 는 MVVM** — 조회/집계/문자열 생성은 전부 ViewModel(또는 Loader)에 둔다.
  `AlignVerifyWindow.xaml.cs` 에는 생성자에서 `DataContext = new AlignVerifyViewModel();` 와
  버튼 클릭 → VM 메서드 호출 정도만 남긴다. **계산 로직을 code-behind 에 넣지 않는다**
- `ReviewerWindow` 는 **순수 삽입만** (기존 화면 회귀 0)
- 날짜 주석 신규 금지
- `DatumMeasurement.csproj` 는 편집하되 **stage/commit 금지**
- 실행 중 프로세스 종료 금지
</coding_rules>

<build_verification>
```bash
MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"
SCR="/c/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/7fe7f8e3-9e05-4190-9941-3b1cfdde3971/scratchpad"
"$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 \
  -p:OutDir="$SCR/build75/on5/" -p:IntermediateOutputPath="$SCR/build75/obj-on5/" -v:m -nologo
```
⚠ 새 XAML 은 csproj 에 `<Page Include="UI\Reviewer\AlignVerifyWindow.xaml">` + `<Generator>MSBuild:Compile</Generator>`
  + `<SubType>Designer</SubType>` 로, code-behind 는 `<Compile Include="...xaml.cs"><DependentUpon>AlignVerifyWindow.xaml</DependentUpon></Compile>`
  로 등록해야 한다. 기존 `ReviewerWindow.xaml` 항목을 그대로 보고 베낀다.
판정 = **에러 0 + 새 경고 코드 종류 0건**
</build_verification>

<tasks>

<task type="auto">
  <name>Task 1: AlignVerifyCsvLoader — 자재번호 조인 + 최근 N개 집계</name>

  <read_first>
    - `WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs` (519줄 **전체** — 특히 파일 순회, 컬럼 인덱스 상수, RFC4180 파싱 함수)
    - `.planning/phases/75-align-corrected-image-evidence/75-02-SUMMARY.md` (`CSV_HEADER` 원문 + 컬럼 인덱스 대응표)
    - `WPF_Example/Custom/EthernetVision/AlignVerifyRecord.cs` (75-02 산출물)
  </read_first>

  <files>
    WPF_Example/Custom/EthernetVision/AlignVerifyCsvLoader.cs (신규),
    WPF_Example/DatumMeasurement.csproj (커밋 금지)
  </files>

  <action>
`namespace ReringProject`, `public static class AlignVerifyCsvLoader`.

**컬럼 인덱스 상수 20개**를 `COL_TIME = 0` ~ `COL_IMAGE = 19` 로 선언한다.
`private const int COLUMN_COUNT = 20;` + `HEADER_FIRST_TOKEN = "기록시각"`.
행 파싱은 `fields.Count < COLUMN_COUNT` 로 가드한다(원본 로더와 동일 방어).

**RFC4180 파싱 함수는 `MeasurementHistoryCsvLoader` 의 것을 그대로 복제한다.**
`Split(',')` 를 새로 짜면 Datum 이름에 콤마가 들어간 순간 컬럼이 밀린다.

**반환 컨테이너** (같은 파일 안에 선언):
```csharp
public class AlignVerifySeatStat
{
    public string SequenceName = "";
    public string DatumName = "";
    public int    Count;
    public double AvgDeviationMm;
    public double MaxDeviationMm;
    public double LastDeviationMm;
    public bool   HasResolution;      // false = mm 환산 불가(해상도 0)
    public double AvgDeviationPx;
    public double MaxDeviationPx;
}

public class AlignVerifyQueryResult
{
    public List<AlignVerifyRecord> MaterialRows = new List<AlignVerifyRecord>();  // 조회 자재의 전체 행(시간순)
    public bool   HasMaterialAlign;         // 조회 자재에 ALIGN 행이 있었나
    public double MaterialAlignDistanceMm;  // 그 자재의 최신 ALIGN 잔여 크기
    public double MaterialAlignThetaDeg;
    public bool   HasMaterialSeat;
    public double MaterialSeatDeviationMm;  // 그 자재의 SEAT 편차 평균
    public bool   MaterialSeatHasResolution;

    public int    TrendAlignCount;
    public double TrendAlignAvgMm;
    public double TrendAlignMaxMm;
    public int    TrendSeatCount;
    public double TrendSeatAvgMm;
    public double TrendSeatMaxMm;

    public List<AlignVerifySeatStat> SeatStats = new List<AlignVerifySeatStat>();  // 시퀀스+Datum 별
    public int    TotalRowCount;
}
```

**공개 메서드 1개:**
```csharp
public static AlignVerifyQueryResult Query(DateTime dtFrom, DateTime dtTo, int nMaterialNo, int nRecentCount)
```
동작:
1. `SystemHandler.Handle.Setting.AlignVerifySavePath` 가 비었으면 빈 결과 반환
2. `dtTo.Date < dtFrom.Date` 방어
3. `dtFrom` ~ `dtTo` 날짜를 하루씩 돌며 `yyyyMMdd.csv` 를 읽어 전체 행을 파싱(헤더 행 스킵)
4. **자재 조인** — `nMaterialNo >= 0` 이면 `자재번호 == nMaterialNo` 인 행을 `MaterialRows` 에 시간순으로 모은다.
   그 중 `구분 == ALIGN` 인 **가장 최근** 행에서 잔여 크기/theta 를 뽑고,
   `구분 == SEAT` 행들의 편차 평균을 낸다. `nMaterialNo < 0` 이면 자재 섹션은 비운다
5. **추세** — 전체 행을 시간 오름차순 정렬 후 **뒤에서 `nRecentCount` 개**만 취해
   ALIGN/SEAT 각각 평균·최대·건수를 낸다
6. **시퀀스별 집계** — SEAT 행을 `시퀀스 + "/" + Datum` 키로 묶어 `AlignVerifySeatStat` 을 만든다.
   `SIDE_1`~`SIDE_4` 가 각각 별도 키가 되므로 지그별 집계가 자연히 나온다
7. 전체 `try/catch` — 실패 시 부분 결과라도 반환하고 로그만 남긴다. **throw 금지**

**편차 계산 헬퍼 (단일 지점으로 만들 것 — 화면이 이걸 다시 계산하면 안 된다):**
```csharp
// 반환: true 면 outMm 유효. false 면 해상도 미상이라 px 만 유효.
public static bool TryComputeSeatDeviation(AlignVerifyRecord rec, out double outPx, out double outMm)
```
```
dRow = rec.DetectedRow - rec.RefRow;
dCol = rec.DetectedCol - rec.RefCol;
outPx = Math.Sqrt(dRow*dRow + dCol*dCol);
bool bHasRes = rec.PixelResolutionMmPerPx > 0.0;
if (bHasRes) { outMm = outPx * rec.PixelResolutionMmPerPx; return true; }   // mm/px 이므로 그냥 곱한다
outMm = 0.0; return false;
```
⚠ `PixelResolutionMmPerPx` 는 **mm/px** 다. `EthernetPixelResolution`(μm/px)과 혼동하면 1000배 틀린다.

**① 크기 헬퍼:**
```csharp
public static double ComputeAlignDistanceMm(AlignVerifyRecord rec);  // sqrt(X^2 + Y^2)
```

csproj 에 `<Compile Include="Custom\EthernetVision\AlignVerifyCsvLoader.cs" />` 추가. **커밋 금지.**
  </action>

  <verify>
    <automated>
    cd /c/Info/Project/DataMeasurement && \
    L=WPF_Example/Custom/EthernetVision/AlignVerifyCsvLoader.cs && \
    echo "colCount=$(grep -c 'COLUMN_COUNT = 20' $L)" && \
    echo "cols=$(grep -cE 'private const int COL_' $L)" && \
    echo "query=$(grep -c 'public static AlignVerifyQueryResult Query' $L)" && \
    echo "seatDev=$(grep -c 'TryComputeSeatDeviation' $L)" && \
    echo "naiveSplit=$(grep -c \"Split(',')\" $L)" && \
    echo "throw=$(grep -c 'throw ' $L)" && \
    echo "ternary=$(grep -cE '\?\?|\?\.|\? .+ :' $L)" && \
    echo "histTouched=$(git status --short -- WPF_Example/Custom/Sequence/Inspection/ | wc -l)"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'COLUMN_COUNT = 20' AlignVerifyCsvLoader.cs` == `1`
    - `grep -cE 'private const int COL_' AlignVerifyCsvLoader.cs` == `20` (컬럼 인덱스 상수 20개)
    - `grep -c 'public static AlignVerifyQueryResult Query' ...` == `1`
    - `grep -c 'TryComputeSeatDeviation' ...` >= `2` (정의 + 내부 사용)
    - **직접 Split 금지:** `grep -c "Split(',')" AlignVerifyCsvLoader.cs` == `0`
      (원본 로더의 RFC4180 파서를 복제해 쓴다)
    - `grep -c 'throw ' ...` == `0`
    - `grep -cE '\?\?|\?\.|\? .+ :' ...` == `0`
    - **기존 통계 계층 무접촉:** `git status --short -- WPF_Example/Custom/Sequence/Inspection/` 출력 줄 수 == `0`
    - `grep -c 'hbk' ...` == `0`
  </acceptance_criteria>

  <done>자재번호 하나로 ①/② 행을 뽑고, 최근 N개 추세와 시퀀스별 집계를 낼 수 있다.</done>
</task>

<task type="auto">
  <name>Task 2: AlignVerifyViewModel + AlignVerifyWindow (MVVM)</name>

  <read_first>
    - `WPF_Example/UI/Login/AccountManagerViewModel.cs:1~60` (INotifyPropertyChanged 패턴)
    - `WPF_Example/UI/Reviewer/ReviewerWindow.xaml:1~30` (Window 헤더/스타일 톤)
    - `WPF_Example/UI/Reviewer/ReviewerWindow.xaml:150~197` (DataGrid 컬럼/RowStyle 작성 스타일)
    - 이 파일의 `<ui_spec>` 섹션 (표시 규칙 전부)
  </read_first>

  <files>
    WPF_Example/UI/Reviewer/AlignVerifyViewModel.cs (신규),
    WPF_Example/UI/Reviewer/AlignVerifyWindow.xaml (신규),
    WPF_Example/UI/Reviewer/AlignVerifyWindow.xaml.cs (신규),
    WPF_Example/DatumMeasurement.csproj (커밋 금지)
  </files>

  <action>
### (1) `AlignVerifyViewModel.cs` — `namespace ReringProject.UI`, `public class AlignVerifyViewModel : INotifyPropertyChanged`

입력 상태 (바인딩 대상, 전부 `Raise(nameof(...))` 로 통지):
- `string MaterialNoText` (기본 `""`)
- `DateTime DateFrom` (기본 `DateTime.Today.AddDays(-7)`) / `DateTime DateTo` (기본 `DateTime.Today`)
- `int RecentCount` (기본 `1000`) + `List<int> RecentCountOptions = { 100, 500, 1000, 5000 }`

출력 상태 (전부 **완성된 표시 문자열**로 만들어 둔다 — XAML 에 계산을 넣지 않는다):
- `string MaterialHeaderText` — `"자재번호 12345"` 또는 `"자재번호 미지정"`
- `string AlignValueText` / `string AlignJudgeText`
- `string SeatValueText` / `string SeatJudgeText`
- `string ConclusionText`
- `string TrendAlignText` / `string TrendSeatText`
- `ObservableCollection<AlignVerifySeatStat> SeatStats`
- `ObservableCollection<AlignVerifyRowView> DetailRows` — 상세 그리드용 표시 전용 뷰 모델
  (`시각/구분/대상시퀀스/슬롯Datum/값/판정/이미지파일` 7개 string 프로퍼티)
- `string StatusText` — `"조회 결과 1,234행"` 또는 오류 메시지
- `string LimitNoticeText` — 임계 미설정 안내
- `string KnownLimitText` — `<ui_spec>` 의 "알려진 한계" 2줄 (상수 문자열)

공개 메서드 1개: `public void ExecuteQuery()`
1. `MaterialNoText` 를 `int.TryParse` — 실패하거나 비어 있으면 `nMaterialNo = AlignVerifyRecord.NO_MATERIAL`
2. `AlignVerifyCsvLoader.Query(DateFrom, DateTo, nMaterialNo, RecentCount)` 호출
3. `<ui_spec>` 의 표시 규칙대로 문자열을 만든다
4. **임계 게이트** (여기가 이 Task 의 핵심이다):
```csharp
double dResidualLimit = SystemHandler.Handle.Setting.AlignVerifyResidualLimitMm;
double dSeatLimit     = SystemHandler.Handle.Setting.AlignVerifySeatLimitMm;
bool bResidualLimitSet = dResidualLimit > 0.0;
bool bSeatLimitSet     = dSeatLimit > 0.0;

if (bResidualLimitSet)
{
    if (result.MaterialAlignDistanceMm <= dResidualLimit) { AlignJudgeText = "정상"; }
    else { AlignJudgeText = "벗어남"; }
}
else
{
    AlignJudgeText = "(판정 기준 미설정)";
}
```
   `② 도 동일. **결론 문구는 `bResidualLimitSet && bSeatLimitSet` 일 때만** `<ui_spec>` 표대로 만든다.
   아니면 `ConclusionText = "판정 기준이 설정되지 않아 결론을 내지 않습니다. (설정 → Path|AlignVerify)"`
5. `② 해상도 0` 인 경우 `SeatValueText` 는 `"환산 불가(px 만): {px:F2}px"` 로 만든다
6. 전체 `try/catch` — 실패 시 `StatusText` 에 메시지, **throw 금지**

`Raise(string szName)` 헬퍼로 통지한다 (`?.` 금지 — `<interfaces>` 참조).

### (2) `AlignVerifyWindow.xaml`
- `Title="Align 정합 조회"`, `Height="820" Width="1100"`, `WindowStartupLocation="CenterOwner"`
- 상단: 자재번호 `TextBox`(`{Binding MaterialNoText, UpdateSourceTrigger=PropertyChanged}`),
  `DatePicker` 2개, 최근 N개 `ComboBox`(`ItemsSource={Binding RecentCountOptions}` / `SelectedItem={Binding RecentCount}`),
  `Button Content="조회" Click="Button_Query_Click"`
- 중단: 요약 `TextBlock` 들 (`MaterialHeaderText`, `AlignValueText`+`AlignJudgeText`,
  `SeatValueText`+`SeatJudgeText`, `ConclusionText`, `TrendAlignText`, `TrendSeatText`)
- `DataGrid` 2개 (`SeatStats`, `DetailRows`) — `AutoGenerateColumns="False"`, `IsReadOnly="True"`,
  `FontSize="11"`, `RowHeight="18"` (ReviewerWindow 그리드 톤 그대로)
- 하단: `TextBlock Text="{Binding KnownLimitText}"` — `TextWrapping="Wrap"`, 회색.
  **이 줄은 조건부가 아니라 항상 보인다**
- 상태줄: `{Binding StatusText}`

### (3) `AlignVerifyWindow.xaml.cs`
```csharp
public partial class AlignVerifyWindow : Window
{
    private readonly AlignVerifyViewModel _vm = new AlignVerifyViewModel();

    public AlignVerifyWindow()
    {
        InitializeComponent();
        DataContext = _vm;
    }

    /// <summary>자재번호 초기값 주입(리뷰어의 입력란 값 복사).</summary>
    public void SetInitialMaterial(string szMaterialNo)
    {
        _vm.MaterialNoText = szMaterialNo;
    }

    private void Button_Query_Click(object sender, RoutedEventArgs e)
    {
        _vm.ExecuteQuery();
    }
}
```
**code-behind 는 이게 전부다.** 계산/포맷/파일 IO 를 여기에 넣지 않는다.

### (4) csproj 등록
`ReviewerWindow.xaml` / `ReviewerWindow.xaml.cs` 항목을 그대로 베껴 `AlignVerifyWindow` 3개 파일을 등록한다
(`<Page>` + `<Generator>MSBuild:Compile</Generator>` + `<SubType>Designer</SubType>`,
 `<Compile><DependentUpon>`). `AlignVerifyViewModel.cs` 는 일반 `<Compile>`. **커밋 금지.**
  </action>

  <verify>
    <automated>
    cd /c/Info/Project/DataMeasurement && \
    V=WPF_Example/UI/Reviewer/AlignVerifyViewModel.cs && \
    X=WPF_Example/UI/Reviewer/AlignVerifyWindow.xaml && \
    B=WPF_Example/UI/Reviewer/AlignVerifyWindow.xaml.cs && \
    echo "inpc=$(grep -c 'INotifyPropertyChanged' $V)" && \
    echo "limitGate=$(grep -c 'bResidualLimitSet\|bSeatLimitSet' $V)" && \
    echo "notSetText=$(grep -c '판정 기준 미설정' $V)" && \
    echo "loaderCall=$(grep -c 'AlignVerifyCsvLoader.Query' $V)" && \
    echo "vmTernary=$(grep -cE '\?\?|\?\.|\? .+ :' $V)" && \
    echo "cbLines=$(grep -c ';' $B)" && \
    echo "cbForbidden=$(grep -cE 'AlignVerifyCsvLoader|File\.|Math\.|Directory\.' $B)" && \
    echo "knownLimit=$(grep -c 'KnownLimitText' $X)"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'INotifyPropertyChanged' AlignVerifyViewModel.cs` >= `1`
    - **임계 게이트 존재:** `grep -c 'bResidualLimitSet' AlignVerifyViewModel.cs` >= `2`
      **그리고** `grep -c 'bSeatLimitSet' AlignVerifyViewModel.cs` >= `2`
    - **임계 미설정 시 판정 안 함:** `grep -c '판정 기준 미설정' AlignVerifyViewModel.cs` >= `2`
      (① 판정 + ② 판정 각각)
    - `grep -c 'AlignVerifyCsvLoader.Query' AlignVerifyViewModel.cs` == `1`
    - `grep -cE '\?\?|\?\.|\? .+ :' AlignVerifyViewModel.cs` == `0`
      (`PropertyChanged?.Invoke` 를 그대로 베끼면 여기서 걸린다)
    - **MVVM 준수:** `grep -cE 'AlignVerifyCsvLoader|File\.|Directory\.|Math\.' AlignVerifyWindow.xaml.cs` == `0`
      (code-behind 에 조회/계산/IO 없음)
    - `grep -c ';' AlignVerifyWindow.xaml.cs` <= `12` (code-behind 가 배선 수준으로 얇다)
    - **한계 문구 상시 표시:** `grep -c 'KnownLimitText' AlignVerifyWindow.xaml` == `1`
      **그리고** `grep -c 'Visibility' AlignVerifyWindow.xaml | grep -c KnownLimit` 로 조건부 숨김이 없음을 확인
    - `grep -c 'hbk' AlignVerifyViewModel.cs AlignVerifyWindow.xaml.cs` 합계 == `0`
  </acceptance_criteria>

  <done>임계가 미설정이면 판정 없이 숫자만 나오고, 설정되면 결론까지 나오는 조회 창이 완성됐다.</done>
</task>

<task type="auto">
  <name>Task 3: 결과 리뷰어에 진입 버튼 삽입 + 빌드 + 커밋</name>

  <read_first>
    - `WPF_Example/UI/Reviewer/ReviewerWindow.xaml:37~73` (좌측 버튼 `StackPanel` 전체)
    - `WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs` 의 기존 `Button_..._Click` 핸들러 하나 (스타일 참고)
    - `WPF_Example/MainWindow.xaml.cs:394~404` (비모달 Show + Owner 패턴)
  </read_first>

  <files>
    WPF_Example/UI/Reviewer/ReviewerWindow.xaml,
    WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs
  </files>

  <action>
**둘 다 순수 삽입이다. 기존 줄을 수정/삭제하지 않는다.**

### (1) `ReviewerWindow.xaml` — `btn_chartSmoke` **다음 줄**에 버튼 1개 삽입
```xml
                <Button x:Name="btn_alignVerify" Content="Align 정합 조회"
                        Click="Button_AlignVerify_Click"
                        Padding="8,4" Margin="0,4,0,0" HorizontalAlignment="Stretch"/>
```

### (2) `ReviewerWindow.xaml.cs` — 핸들러 1개 추가 (파일 끝 클래스 안)
```csharp
// Align 정합 조회 창(비모달). 리뷰어의 자재번호 입력란에 값이 있으면 초기값으로만 복사한다
//  — 그 입력란은 '이미지 폴더 반복 검사' 소유이므로 용도를 바꾸지 않는다.
private void Button_AlignVerify_Click(object sender, RoutedEventArgs e)
{
    try
    {
        AlignVerifyWindow win = new AlignVerifyWindow();
        win.Owner = this;
        string szMaterial = "";
        if (txt_materialIndex != null)
        {
            if (!string.IsNullOrWhiteSpace(txt_materialIndex.Text)) { szMaterial = txt_materialIndex.Text.Trim(); }
        }
        win.SetInitialMaterial(szMaterial);
        win.Show();
    }
    catch (Exception ex)
    {
        Logging.PrintErrLog((int)ELogType.Error, "[Reviewer] Align 정합 조회 창 열기 실패: " + ex.Message);
    }
}
```
(파일에 `Logging`/`ELogType` using 이 없으면 완전정규화 `ReringProject.Utility.Logging` 으로 쓴다 — using 줄 추가도 순수 삽입이다.)

### (3) 빌드
SIMUL-ON + SIMUL-OFF 둘 다 돌린다.
XAML 신규 파일은 csproj 등록이 빠지면 `InitializeComponent` 미정의 에러(CS0103)가 난다 —
그 에러가 나면 Task 2 의 csproj 등록을 다시 확인한다.

### (4) 커밋 (csproj 제외)
```bash
git add WPF_Example/Custom/EthernetVision/AlignVerifyCsvLoader.cs \
        WPF_Example/UI/Reviewer/AlignVerifyViewModel.cs \
        WPF_Example/UI/Reviewer/AlignVerifyWindow.xaml \
        WPF_Example/UI/Reviewer/AlignVerifyWindow.xaml.cs \
        WPF_Example/UI/Reviewer/ReviewerWindow.xaml \
        WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs
git commit -m "feat(75-05): Align 정합 조회 화면(자재번호 조인 + 추세, 임계 미설정 시 판정 없음)"
```
  </action>

  <verify>
    <automated>
    cd /c/Info/Project/DataMeasurement && \
    MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" && \
    SCR="/c/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/7fe7f8e3-9e05-4190-9941-3b1cfdde3971/scratchpad" && \
    echo "xamlDel=$(git diff -U0 -- WPF_Example/UI/Reviewer/ReviewerWindow.xaml | grep '^-' | grep -vc '^---')" && \
    echo "csDel=$(git diff -U0 -- WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs | grep '^-' | grep -vc '^---')" && \
    echo "btn=$(grep -c 'btn_alignVerify' WPF_Example/UI/Reviewer/ReviewerWindow.xaml)" && \
    echo "handler=$(grep -c 'Button_AlignVerify_Click' WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs)" && \
    "$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 \
      -p:OutDir="$SCR/build75/on5/" -p:IntermediateOutputPath="$SCR/build75/obj-on5/" -v:m -nologo 2>&1 \
      | tee "$SCR/build75-05-on.log" | tail -3 ; \
    "$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 \
      -p:DefineConstants=TRACE%3BDEBUG \
      -p:OutDir="$SCR/build75/off5/" -p:IntermediateOutputPath="$SCR/build75/obj-off5/" -v:m -nologo 2>&1 \
      | tee "$SCR/build75-05-off.log" | tail -3 ; \
    echo "ERR_ON=$(grep -c ' error ' "$SCR/build75-05-on.log")" ; \
    echo "ERR_OFF=$(grep -c ' error ' "$SCR/build75-05-off.log")" ; \
    echo "CODES=$(grep -oE 'CS[0-9]{4}' "$SCR/build75-05-on.log" | sort -u | tr '\n' ' ')"
    </automated>
  </verify>

  <acceptance_criteria>
    - **리뷰어 회귀 0:** `ReviewerWindow.xaml` / `ReviewerWindow.xaml.cs` 각각
      `git diff -U0 | grep '^-' | grep -v '^---' | wc -l` == `0`
    - `grep -c 'btn_alignVerify' ReviewerWindow.xaml` == `1`
    - `grep -c 'Button_AlignVerify_Click' ReviewerWindow.xaml.cs` == `1`
      **그리고** `grep -c 'Button_AlignVerify_Click' ReviewerWindow.xaml` == `1`
    - **기존 입력란 용도 보존:** `git diff -U0 -- ReviewerWindow.xaml | grep '^+' | grep -c 'txt_materialIndex'` == `0`
      (XAML 쪽 입력란은 손대지 않는다. code-behind 에서 값을 읽기만 한다)
    - SIMUL-ON / SIMUL-OFF `grep -c ' error '` 둘 다 == `0`
    - 경고 코드 집합이 `CS0162 CS0618` 를 벗어나지 않는다
    - `git show --stat HEAD | grep -c csproj` == `0`
  </acceptance_criteria>

  <done>리뷰어에서 "Align 정합 조회" 버튼으로 새 창이 열리고, 두 빌드 구성 모두 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV 파일(외부에서 편집 가능) → 파서 | 손상되거나 조작된 행이 들어올 수 있다 |
| 사용자 입력(자재번호/기간) → 파일 경로 조합 | 날짜 문자열이 경로에 들어간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-20 | Denial of Service | 손상 CSV 행 파싱 예외 | mitigate | `fields.Count < COLUMN_COUNT` 가드 + 행 단위 continue + 전체 try/catch. `throw` 0건 |
| T-75-21 | Denial of Service | 대용량 기간 조회로 UI 정지 | mitigate | 조회 범위는 사용자가 지정한 날짜 폴더 파일로 한정되고, 추세는 최근 N개(기본 1000)만 집계한다. 기본 기간을 최근 7일로 둔다 |
| T-75-22 | Tampering | 자재번호 입력이 경로에 섞임 | mitigate | 자재번호는 `int.TryParse` 후 숫자 비교에만 쓴다. 경로에는 `DateTime.ToString("yyyyMMdd")` 만 들어간다 |
| T-75-23 | Repudiation | 임계 미설정인데 판정처럼 보이는 화면 | mitigate | 임계 `> 0` 게이트가 없으면 판정/결론 문구를 만들지 않는다(acceptance 로 검증). 오판정으로 정상품을 버리는 사고 방지 |
| T-75-24 | Information Disclosure | 조회 화면 노출 | accept | 사내 장비 로컬 화면. 기존 리뷰어와 동일 신뢰 수준 |
</threat_model>

<verification>
1. 로더에 컬럼 인덱스 상수 20개 + `COLUMN_COUNT = 20`
2. 로더가 원본 RFC4180 파서를 복제해 쓴다 (`Split(',')` 0건)
3. ViewModel 에 임계 게이트 존재, 미설정 시 판정 문구 없음
4. code-behind 에 조회/계산/IO 0건 (MVVM)
5. `ReviewerWindow` 두 파일 모두 삭제 0줄
6. SIMUL-ON / SIMUL-OFF 빌드 에러 0, 새 경고 코드 0건
</verification>

<success_criteria>
- 리뷰어 → "Align 정합 조회" → 자재번호 입력 → 조회 로 ①/② 숫자가 한 화면에 나온다
- 임계 미설정 상태에서 정상/벗어남 문구가 **나오지 않는다**
- 최근 N개 ①/② 평균·최대가 보이고, SIDE 는 `SIDE_1`~`SIDE_4` 가 별도 행으로 집계된다
- 화면 하단에 "SIDE 는 깊이 방향 미검증" 한계 문구가 항상 보인다
</success_criteria>

<output>
완료 후 `.planning/phases/75-align-corrected-image-evidence/75-05-SUMMARY.md` 를 작성한다.
반드시 포함할 것:
- 임계 설정 2종의 이름과 "0 = 판정 없음" 동작 (75-06 문서화 + 사용자 안내 대상)
- 화면 진입 경로 (리뷰어 좌측 버튼)
- `PixelResolutionMmPerPx == 0` 일 때 화면이 어떻게 보이는지
</output>
