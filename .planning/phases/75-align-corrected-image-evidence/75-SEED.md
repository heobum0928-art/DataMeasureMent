# Phase 75 — SEED

**Align 정합 검증 — 비전/피커 책임 분리**
신설 2026-08-27 · 개정 2026-08-27(범위 확대)

## 발단

사용자 요구:
1. *"Bottom Align 보정한 이미지 저장하는 기능도 있어야 할 것 같다.
   나중에 Align 이 잘못됐다는 소리 할까봐."*
2. 논의 중 확장: *"근데 피커가 잘못되었는지 비전이 잘못되었는지 알 수 없자나"*

## 목표

**틀어졌을 때 비전 탓인지 피커 탓인지 갈라낸다.**

지금은 Align 결과 수치(Offset/Theta)만 남아 "그 숫자가 맞았다"는 증거가 없다.
두 지점에서 재면 책임이 나뉜다.

## 핵심 구조 — 두 지점에서 잰다

```
Align 측정 → ① 보정 후 다시 재봄 → 피커가 놓음 → ② 검사 때 기준점 좌표
                  ↑                                      ↑
            비전 계산이 맞았나                      실제로 어디 놓였나
```

| ① 보정 후 | ② 안착 위치 | 결론 |
|---|---|---|
| 0 에 가까움 | 정상 | 문제 없음 |
| 0 에 가까움 | 튐 | **피커 문제** |
| 안 맞음 | 튐 | **비전 문제** |

**①이 빨간불이면 비전, ①이 파란불인데 ②가 빨간불이면 피커.** 판정 규칙은 이것뿐이다.

## 짝 구조 (사용자 확인)

```
Tray Align    → 집어서 → Top/Bottom 지그 → Top/Bottom 검사
Bottom Align  → 집어서 → Side 지그(4개)  → Side 검사
```

② 는 **짝이 되는 검사**의 Datum 검출 원점에서 가져온다.
Side 는 Phase 73 에서 나눈 지그 4개(SIDE_1~4)**별로 따로 집계**한다 —
어느 지그가 유독 흔들리는지 자체가 정보다.

**연결 고리 = 자재번호.** 양쪽 프로토콜이 이미 주고받는다:
- `$ALIGN_RESULT:BOTTOM,MaterialNo,AlignFace,OK|NG,OffsetX,OffsetY,Theta@`
- `$TEST:site,Type,자재번호@`

## 기반 (조사 완료 — 신규 구축 최소)

| 항목 | 상태 |
|---|---|
| ① 재매칭 | 매칭 코드 존재(`AlignShapeMatchService`) — 보정 적용 후 한 번 더 호출 |
| 보정 변환 | `AlignShapeMatchService.cs:756` `VectorAngleToRigid`, `:809~810` `HomMat2dRotate` |
| ② 기준점 좌표 | **이미 매 사이클 계산 중** — `DatumConfig.DetectedOriginRow/Col`(`Action_FAIMeasurement.cs:1301,1593`). 쓰고 버리는 중 |
| 저장 서비스 | `Utility/CaptureImageSaveService.cs`(큐 상한 50 + 백프레셔, 커밋 `44339bc`), `RawImageSaveService.cs` |
| 자재번호 | `VisionResponsePacket.cs:738` `MaterialNo`, `VisionRequestPacket.cs:44` `TEST_FIELD_MATERIAL` |

**카메라 추가 없음. 택트 증가 없음.** ②는 이미 계산하는 값을 저장만 한다.

## 범위

1. **① 보정 후 재매칭** — 보정 적용 이미지로 다시 매칭, 잔여 offset/theta 산출·기록
2. **② 안착 위치 기록** — 검사 Datum 검출 원점을 자재번호와 함께 저장 (Top/Bottom / SIDE_1~4 각각)
3. **자재번호 조인** — ①과 ②를 한 줄로 묶어 조회 가능하게
4. **판정 표시** — 아래 UI 안
5. **보정 이미지 저장** (원 요구) — 옵션, 보관 정책 포함
6. **추세** — 최근 N개 평균/최대

## UI (사용자 합의 그림)

```
자재번호 12345

  ① Align 계산     0.02mm     ✅ 정상
  ② 안착 위치      +0.04mm    ✅ 정상
  → 정상
```
```
  ① Align 계산     0.02mm     ✅ 정상
  ② 안착 위치      +3.10mm    ⚠ 벗어남
  → 비전은 맞게 줬는데 놓는 위치가 틀어졌습니다
```
```
최근 1000개
  ① Align 계산    평균 0.03mm   최대 0.11mm
  ② 안착 위치     평균 0.05mm   최대 0.18mm
```

**위치:** `결과 리뷰어` 화면에서 자재번호로 조회. 이미 검사 결과를 그곳에서 본다.

## 알려진 한계 — 반드시 문서화할 것

**Side 는 앞뒤(깊이) 방향을 못 본다.** 측면 촬영이라 카메라 쪽으로 밀려도 그림이 거의 안 변하고
**초점만 흐려진다.** 좌우·높이만 ②로 검증된다.

사용자 확인: **Side 는 잡아주는 지그가 없다** → 앞뒤로 밀릴 여지가 실재한다.
초점이 흐려지면 에지가 뭉개져 **측정값이 조용히 밀린다**(에러 없음).
관련 신호가 이미 로그에 있다 — `[FitLine] low strip coverage: ok 8/20 (noEdge 12)`.

**이번 phase 범위 밖.** 한계로 기록하고, 실제 문제로 확인되면 별도 phase(초점/에지 품질 감시)로 다룬다.

## 미결 (discuss 에서 확정)

- ① 재매칭 시점 — 매 Align 마다 vs 옵션
- 판정 임계값 — ①/② 각각 몇 mm 를 "벗어남"으로 볼지 (실측 후 결정)
- 저장 매체 — CSV vs 기존 측정이력 저장소 재사용
- 보정 이미지 저장 — 매번 vs NG 만, 형식(BMP/PNG), 보관 정책(기간/개수/용량)
- Align PC 와 검사 PC 가 물리적으로 다른가 — 다르면 로그 병합 방식 필요 **(미확인)**
- ②를 실시간 판정에 쓸지, 사후 조회 전용으로 둘지

## ⚠ 메모리 사고 이력 — 협상 대상 아님

- **58.3GB 폭증** — 일괄검사 이미지 큐 무제한 누적. 큐 상한 50 + 백프레셔로 해결
  (`project_capture_queue_memory_leak`)
- **34~41GB + halcon.DLL 크래시** — Shot 이미지 영구보존 + 저장 레이스.
  Dispose 해도 OS 메모리 미반환은 **미해결** (`project_batch_memory_never_shrinks_260806`)

이미지 저장을 붙이는 phase 라 정확히 그 지뢰밭이다.
**큐 상한 · 백프레셔 · HImage Dispose 필수.**

## 코딩 규칙

삼항 `?:` / `??` / `?.` 금지 · 전통 `switch` 만 · C# 7.2 · 헝가리언 ·
날짜 주석 신규 금지 · **UI 는 MVVM** · 빌드 경고 baseline 준수 ·
실행 중 프로세스 종료 금지 · `DatumMeasurement.csproj` 커밋 금지 · **HImage 반드시 Dispose**
