---
phase: 76-side-datum
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
autonomous: true
requirements: [SDV-01, SDV-02, SDV-04]
user_setup: []

must_haves:
  truths:
    - "SIDE Datum(AlgorithmType=VerticalTwoHorizontalDualImage) 속성창 Datum|Algorithm 그룹에 IsVerticalLineDisabled 항목이 보이고, TwoLineIntersect / CircleTwoHorizontal / VerticalTwoHorizontal Datum 속성창에는 보이지 않는다 (SDV-01, D-76-01)"
    - "레시피 저장 시 Datum 섹션마다 IsVerticalLineDisabled 키가 기록되고, 다시 로드하면 Datum 별로 저장한 값이 복원된다 — SIDE Datum 하나만 켜고 나머지를 끈 상태도 그대로 유지된다 (SDV-01, D-76-01)"
    - "[edge SDV-01 empty] IsVerticalLineDisabled 키가 없는 INI(이 phase 이전 레시피)를 로드하면 모든 Datum 이 false 로 읽힌다 — ParamBase.Load 의 Boolean 분기가 IniValue.ToBool() 기본값 false 를 쓰며, DatumConfig.Load 오버라이드에는 이 키용 코드가 추가되지 않는다"
    - "[edge SDV-01 adjacency] AlgorithmType 이 VerticalTwoHorizontalDualImage 가 아닌 Datum 에 INI 수기 편집이나 알고리즘 변경 후 잔존으로 IsVerticalLineDisabled=True 가 남아 있어도 IsHorizontalOnlyActive() 는 false 이고, 그 Datum 은 H-only 분기에 들어가지 않는다"
    - "[edge SDV-01 ordering] IsVerticalLineDisabled 키가 Datum 섹션 안 어느 위치에 있든(손으로 순서를 바꾼 INI 포함) 로드 값은 같다 — IniSection 인덱서는 키 이름으로 조회한다(Ini.cs:953-960)"
    - "옵션 ON Datum 의 Find 는 세로 ROI 라인 검출을 아예 호출하지 않는다 — 세로 이미지가 흐려도 가로 결합선과 패턴매칭이 성공하면 Find 가 성공한다. 자동 사이클(RunDatumDualImageDetection)과 수동 Test Find(BtnTestFindDatum_Click)가 모두 InspectionSequence.TryComposeAlign 에서 DatumFindingService.TryFindDatum(2-image) 로 가는 한 경로를 지나므로 같은 규칙이다 (SDV-02, D-76-02, D-76-04)"
    - "옵션 ON Find 결과는 DetectedOriginCol = 패턴매칭 transform(AlignPreTransform)으로 옮긴 티칭 원점 열, DetectedOriginRow = 그 열에서 가로 결합선의 행, DetectedRefAngle = 가로 결합선 각도, DetectedRefAngle2 = 0.0(미설정 표식) 이다 (SDV-02, D-76-05)"
    - "옵션 ON 이어도 패턴 transform 이 없으면 Datum 은 실패한다 — 패턴매칭 실패는 TryComposeAlign 1단계에서 이미 ALIGN_FAIL 로 끝나고, transform 이 전달되지 않은 경로(IsPatternAlignEnabled=false, seq 없는 폴백)는 'requires pattern align' 오류로 실패한다 (SDV-02)"
    - "[edge SDV-04 empty] IsPatternAlignEnabled=false 인 DualImage Datum 은 옵션 OFF 면 TryRunSingleDatum 기존 경로 그대로 동작하고, 옵션 ON 이면 'requires pattern align' 오류로 실패해 MarkDatumFailed 로 이어진다 — 매칭 없이 만든 원점은 없다"
    - "[edge SDV-04 adjacency] 같은 SIDE 시퀀스나 같은 크로스-Z 쌍 안에서 옵션 ON Datum 과 OFF Datum 이 나란히 있어도 서로 영향이 없다 — 옵션은 각 DatumConfig 인스턴스에서만 읽히고 새 static 상태는 추가되지 않는다"
    - "옵션 ON Find 마다 Algorithm 로그(ELogType.Algorithm)에 '[Datum.Vertical] skipped' 줄이 Datum 이름과 함께 남고, 결과가 '[Datum.HorizontalOnly] ok' 또는 '[Datum.HorizontalOnly] fail' 줄로 남는다"
    - "옵션 OFF(키 부재 포함) Datum 과 TOP/BOTTOM Datum 은 기존 검출 코드를 그대로 탄다 — DatumConfig.cs / DatumFindingService.cs 의 기존 줄 삭제 0(git diff -w), 티칭 경로(TryTeachDatum) 무변경이라 기존 RefOriginRow/Col/RefAngleRad 를 재티칭 없이 재사용한다 (SDV-04, D-76-07)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "IsVerticalLineDisabled 옵션(INI 저장) + IsHorizontalOnlyActive() 효력 규칙 + DualImage 전용 PropertyGrid 노출"
      contains: "public bool IsVerticalLineDisabled { get; set; } = false;"
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "DualImage Find 진입부 H-only 분기 + TryFindDualImageHorizontalOnly + TryMapTaughtOriginColumn"
      contains: "private bool TryFindDualImageHorizontalOnly("
  key_links:
    - from: "DatumFindingService.TryFindVerticalTwoHorizontalDualImage"
      to: "DatumFindingService.TryFindDualImageHorizontalOnly"
      via: "기존 본문 앞에 삽입된 if (config.IsHorizontalOnlyActive()) 분기"
      pattern: "if .config.IsHorizontalOnlyActive"
    - from: "DatumFindingService.TryMapTaughtOriginColumn"
      to: "InspectionSequence.TryComposeAlign 이 주입한 AlignPreTransform (alignRigid, InspectionSequence.cs:3011)"
      via: "HOperatorSet.AffineTransPoint2d 로 티칭 원점을 패턴 pose 로 이동"
      pattern: "AffineTransPoint2d.AlignPreTransform, config.RefOriginRow, config.RefOriginCol"
    - from: "DatumConfig.IsHorizontalOnlyActive"
      to: "DatumConfig.AlgorithmTypeEnum + DatumConfig.IsVerticalLineDisabled"
      via: "DualImage 알고리즘일 때만 옵션이 효력을 가진다 (TOP/BOTTOM 보호)"
      pattern: "VerticalTwoHorizontalDualImage"
  prohibitions:
    - statement: "MUST NOT 가로선 전용 모드에서 패턴매칭 transform 없이 Datum 을 OK(LastFindSucceeded=true, TryComposeAlign true)로 보고하지 않는다 — transform 이 없거나 변환이 실패하면 옮기지 않은 티칭 열로 조용히 되돌아가지 않고 실패한다"
      status: resolved
      verification: judgment
    - statement: "MUST NOT 옵션 ON Datum 에서 세로선 검출을 사이클마다 시도했다가 실패 시 매칭으로 폴백하는 식으로 동작하지 않는다 — 원점 X 출처가 사이클마다 교점과 매칭 사이에서 바뀌면 측정값이 튄다 (D-76-02)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT 세로선이 없는 Datum 의 DetectedRefAngle2 에 가로각+90도 같은 추정값을 넣지 않는다 — 측정되지 않은 값이 측정값처럼 X축 소비자에게 전달되면 안 되며, 미설정 표식 0.0 을 유지한다"
      status: resolved
      verification: judgment
---

<objective>
SIDE Datum 에 Datum 별 옵션 `IsVerticalLineDisabled` 를 만들고(D-76-01), 옵션이 켜진 Datum 은 세로선 검출을 아예 돌리지 않고(D-76-02) 가로 결합선과 패턴매칭만으로 Find 를 성공시킨다(D-76-04, D-76-05).
세로 이미지는 지금처럼 ZIndexB 에서 찍고 저장한다(D-76-03 — 촬영 코드 무변경). 티칭 경로도 바꾸지 않는다(D-76-07).

Purpose: SIDE 세로 기준 이미지의 초점이 자주 흐려져 세로선 검출이 실패하면 SIDE 검사 전체가 멈춘다. 코드를 확인한 결과 SIDE 4개 Datum 은 전부 `IsPatternAlignEnabled=True` 이고,
측정 ROI 위치는 이미 패턴매칭 결과(`alignRigid`)만으로 정해진다(`InspectionSequence.cs:3044`, `:3049` — 라인 검출 transform 은 곧바로 덮어써짐). 세로선은 성공/실패 게이트와
`DetectedOrigin*` 채움에만 쓰인다. 그래서 이 plan 은 transform 설계를 바꾸지 않고, 단일 초크포인트(`DatumFindingService.TryFindVerticalTwoHorizontalDualImage`)에 가드된 분기 하나를 넣는다.
자동 사이클과 수동 Test Find 는 둘 다 `TryComposeAlign` 에서 이 함수로 오므로 한 곳만 고치면 둘 다 같은 규칙이 된다(D-76-04).

Output: `DatumConfig.cs` 에 옵션 + 효력 규칙 메서드 + PropertyGrid 노출 범위, `DatumFindingService.cs` 에 H-only 분기와 새 private 메서드 2개. 옵션 OFF 는 기존 줄 삭제 0 이다.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/76-side-datum/76-CONTEXT.md
@.planning/phases/76-side-datum/76-RESEARCH.md
@.planning/phases/76-side-datum/76-PATTERNS.md
@.planning/phases/76-side-datum/76-VALIDATION.md

<interfaces>
기존 코드 계약. 실행자는 이 값들을 그대로 쓴다(계획 시점 라인 번호).

DatumFindingService.cs
- `:18` `private const int MIN_HORIZONTAL_EDGES = 10;` / `:21` `HORIZONTAL_TOLERANCE_DEG = 15.0` / `:24` `PERPENDICULAR_TOLERANCE_DEG = 10.0`
- `:29` `public HTuple AlignPreTransform { get; set; } = null;` — TryComposeAlign 이 `alignRigid` 를 주입(InspectionSequence.cs:3011). null 이면 무보정 경로.
- `:85-117` `public bool TryFindDatum(HImage imageHorizontal, HImage imageVertical, DatumConfig config, out HTuple transform, out string error)` — null/SameFrame/알고리즘 가드 후 `:116` 에서 DualImage Find 로 위임. `:97` 에서 `LastFindSucceeded=false` 리셋.
- `:625` `private bool TryFindVerticalTwoHorizontalDualImage(HImage imageHorizontal, HImage imageVertical, DatumConfig config, out HTuple transform, out string error)` — 호출처는 `:116` 한 곳뿐.
- `:656-716` 가로 A/B 에지 추출 + TupleConcat + GenContourPolygonXld + `FitLineContourXld(contour, "tukey", -1, 0, 5, 2, ...)`
- `:737` `double curAngle = Math.Atan2(hrE.D - hrB.D, hcE.D - hcB.D);`
- `:748-755` transform = Identity → HomMat2dTranslate(dRow, dCol) → HomMat2dRotate(dAngle, curRow, curCol)
- `:1567-1599` `ValidateHorizontalVerticalAngles(horizPhiRad, vertPhiRad, out error)` — 가로 방향 검사 + 직각성 검사가 한 함수
- `:1687-1691` 로그 관례: `Logging.PrintLog((int)ELogType.Algorithm, string.Format("[Datum.{0}] ...", ...))`
- `:1903` `private bool TryExtractEdgePoints(HImage image, HTuple imageWidth, HTuple imageHeight, double roiRow, double roiCol, double roiPhi, double roiLength1, double roiLength2, double sigma, int threshold, string polarity, string direction, string selection, int sampleCount, int trimCount, int erosion, out HTuple rowEdge, out HTuple colEdge, out string error, string roiLabel)` — 내부에서 AlignPreTransform 으로 ROI 를 옮긴다(`:1932-1941`)

InspectionSequence.cs (읽기 전용 — 수정 금지)
- `:2938-2947` 1단계 패턴매칭 실패 → `return false` (호출부 MarkAlignFailed, lenient D-10)
- `:2994-3000` `alignRigid` = Identity → HomMat2dRotate(theta, RefMatchRow, RefMatchCol) → HomMat2dTranslate(dRow, dCol)
- `:3011` `detectSvc.AlignPreTransform = alignRigid;` `:3018` 2-image TryFindDatum 호출
- `:3044` `_datumTransforms[datumKey] = alignRigid;` `:3049` `datum.CurrentTransform = alignRigid;` `:3050` `LastFindSucceeded = true`

DatumConfig.cs
- `:132-134` `IsPatternAlignEnabled` — `[Category]` + `[System.ComponentModel.Description]` + `public bool ... { get; set; } = false;` (새 옵션의 모양 선례)
- `:398-407` `AlgorithmTypeEnum` 게터 (Allman)
- `:419-427` `ExpectedAngleDeg`, `AngleTolerance` (`[Category("Datum|Algorithm")]`)
- `:1012-1041` `DetectedOriginRow/Col`, `DetectedRefAngle`, `DetectedRefAngle2` (transient)
- `:1277-1313` `private static bool IsHiddenForAlgorithm(string name, EDatumAlgorithm alg)` — 4개 case
- `:1325-1355` `Load` 오버라이드 (Int32 키 부재 가드 — bool 은 불필요, 건드리지 않음)

INI bool 로드 (검증 완료, 수정 없음): `ParamBase.cs:396-399` Boolean → `loadFile[group][name].ToBool()`; `Ini.cs:953-960` 키 부재 → `IniValue.Default`; `Ini.cs:153-159` `ToBool(valueIfInvalid=false)` → false.
</interfaces>
</context>

## 계획 단계 결정 (CONTEXT '계획 단계에서 정할 것' 1~6 + orchestrator 메모)

| # | 질문 | 결정 | 근거 |
|---|---|---|---|
| 1 | 세로선 없을 때 `DetectedRefAngle2` | **0.0 (미설정 표식) 유지** — 상수 `DETECTED_REF_ANGLE2_UNSET` | `Action_FAIMeasurement.cs:1396` 캡처 게이트와 `EdgeToLineDistance` 의 `!= 0.0` 판정이 0.0 을 '2차 축 없음' 으로 읽는다. 가로각+90도는 측정되지 않은 값을 측정값처럼 흘린다. 무가드 X축 소비자 5종의 위험은 속성 설명문과 76-03 한계 문서에 명시 |
| 2·3 | 옵션 ON 티칭 경로 / 기존 티칭 재사용 | **티칭 무변경(D-76-07)** — H-only Find 는 기존 `RefOriginRow/Col/RefAngleRad` 를 기준점으로만 읽는다 | 재티칭 불필요. 티칭 시점부터 세로가 영구 불가한 경우는 범위 밖(CONTEXT) |
| 4 | 직각성 검증 | **가로 방향 15도 검사는 유지, 직각성 검사는 하지 않음** (Task 2) | 세로선이 없으므로 직각성은 정의되지 않는다. 가로 방향 검사는 세로와 무관하고 오검출을 막는다 |
| 5 | 오프라인 이미지 자동 채움 | **현행 유지(코드 변경 없음)** | D-76-03 취지(세로 사진을 남겨 초점 분석에 쓴다)와 맞다. 76-03 UAT 한계 절에 기록 |
| 6 | 옵션 ON 에서 패턴매칭 실패 | **Datum 실패(오늘과 동일)** — 매칭 실패는 TryComposeAlign 1단계에서 이미 ALIGN_FAIL. 새로 막는 것은 transform 이 아예 전달되지 않는 경로(IsPatternAlignEnabled=false, seq 없는 폴백) → 'requires pattern align' 실패 | 옵션 ON 에서는 매칭이 X 의 유일한 출처다. 오늘(옵션 OFF) 합부 판정은 바뀌지 않는다 — 옵션 ON 에서만 닿는 새 코드. reversibility: **reversible**(코드 한 곳, 저장 형식 무관) |
| D-76-05 | 원점 X 산출식 | **후보 (i)**: `AffineTransPoint2d(AlignPreTransform, RefOriginRow, RefOriginCol)` 의 열. Y = 그 열에서 가로 결합선의 행, 각도 = 가로 결합선 각도 | `alignRigid` 는 RefMatch 중심 회전 후 평행이동이다. 티칭 원점이 RefMatch 에서 멀면 회전의 지렛대 효과(거리 × sin theta)가 생기는데 후보 (ii) `RefOriginCol + dCol` 은 이를 버린다. 기존 datum ROI 이동(`:262-272`, `:1932-1941`)과 같은 규약이다 |

## Spec-less probe 반영 (edge 8건 / prohibition 6건 — phase 전체 원장)

| 항목 | 처리 | 위치 |
|---|---|---|
| SDV-01 adjacency / empty / ordering | covered → truths | 76-01 (이 plan) |
| SDV-04 adjacency / empty | covered → truths | 76-01 (이 plan) |
| SDV-04 ordering | covered → truth | 76-03 |
| SDV-02 unclassified | **미해결 — flagged assumption A-76-E1** | 76-01 (아래) |
| SDV-03 unclassified | **미해결 — flagged assumption A-76-E2** | 76-02 |
| PR-2 매칭 없는 OK 금지 / PR-4 사이클별 폴백 금지 / PR-6 추정 세로각 금지 | prohibitions (descriptor 없음) | 76-01 (이 plan) |
| PR-5 오래된·티칭 복원 세로선 표시 금지 | prohibition | 76-02 |
| PR-1 TOP/BOTTOM 무변경 / PR-3 ZIndexB 촬영·저장 무변경 | prohibitions | 76-03 |

### Flagged assumption A-76-E1 (SDV-02, edge probe 'unclassified' — 미해결로 남김, 자동 해소 금지)
자동 분류기가 SDV-02 의 경계 사례를 분류하지 못했다. 이 plan 이 직접 다루는 경계: 패턴매칭 실패(TryComposeAlign 1단계, 기존 그대로 ALIGN_FAIL) / 패턴 transform 없음(명시적 실패) /
티칭 원점 없음(RefOrigin 0,0 → 실패, Task 2) / 가로 에지 부족·피팅 실패 / 가로선 방향 15도 초과(Task 2) / 가로선 열 폭 1px 미만 / 세로·가로 이미지 크기 불일치(기존 SameFrame 가드).
**다루지 않는 것:** 패턴매칭이 '성공했지만 틀린 위치'(false match)일 때 옵션 ON 은 세로선이라는 독립 교차검증이 없어 원점 X 가 틀린 매칭을 그대로 따른다. 가로 ROI 는 여전히 매칭으로
옮겨지므로 위아래로 크게 틀린 매칭은 가로 에지 실패로 걸러지지만, 가로 에지를 따라 좌우로만 틀린 매칭은 걸러지지 않는다. 측정 ROI 위치는 원래부터 매칭만으로 정해지므로 측정 영향은 옵션과 무관하다.
사용자 확인 필요 — 76-03 UAT U-3 / U-5 에서 관찰한다.

## Artifacts this phase produces (phase 전체)

| 심볼 / 산출물 | 종류 | 파일 | 만드는 곳 |
|---|---|---|---|
| `IsVerticalLineDisabled` | public bool 프로퍼티, 기본 false, `[Category("Datum|Algorithm")]` | DatumConfig.cs | 76-01 T1 |
| INI 키 `IsVerticalLineDisabled` (`True`/`False`) | 레시피 INI, Datum 섹션마다 (ParamBase.Save 리플렉션) | 레시피 main.ini | 76-01 T1 (다음 레시피 저장부터 기록) |
| `IsHorizontalOnlyActive()` | public bool 메서드 (DualImage 이고 옵션 ON 일 때만 true) | DatumConfig.cs | 76-01 T1 |
| `TryFindDualImageHorizontalOnly(HImage, DatumConfig, out HTuple, out string)` | private 메서드 | DatumFindingService.cs | 76-01 T1, T2 보강 |
| `TryMapTaughtOriginColumn(DatumConfig, out double, out string)` | private 메서드 | DatumFindingService.cs | 76-01 T1, T2 보강 |
| `DETECTED_REF_ANGLE2_UNSET`, `MIN_HORIZONTAL_LINE_SPAN_PX`, `RAD_TO_DEG`, `HORIZONTAL_FIT_ALGORITHM`, `HORIZONTAL_FIT_MAX_POINTS`, `HORIZONTAL_FIT_CLIP_END_POINTS`, `HORIZONTAL_FIT_ITERATIONS`, `HORIZONTAL_FIT_CLIP_FACTOR` | private const | DatumFindingService.cs | 76-01 T1 |
| `DEG_FULL_TURN`, `DEG_HALF_TURN`, `DEG_QUARTER_TURN` | private const | DatumFindingService.cs | 76-01 T2 |
| 로그 태그 `[Datum.Vertical] skipped`, `[Datum.HorizontalOnly] ok`, `[Datum.HorizontalOnly] fail` | ELogType.Algorithm | DatumFindingService.cs | 76-01 T1 |
| 오류 문구 `requires pattern align`, `Horizontal line too short`, `taught origin missing`, `Horizontal line orientation out of range` | Find error (ALIGN_FAIL 로그 / Test Find 모달) | DatumFindingService.cs | 76-01 T1, T2 |
| 지역 bool `bDrawVertical` (RenderDatumFindResult, RenderDatumDetectedOverlay) | 표시 게이트 | HalconDisplayService.cs | 76-02 T1, T2 |
| `DatumCaptureOverlay.HideOriginVerticalArm` | public bool 프로퍼티, 기본 false | EdgeInspectionOverlay.cs | 76-02 T2 |
| VersionDefine 새 항목(계획 시점 기준 `1.7.41.0`) | changelog + VERSION/BUILD_DATE | VersionDefine.cs | 76-03 T1 |
| `76-HUMAN-UAT.md` | 실기 UAT 절차서 + 알려진 한계 | phase 디렉터리 | 76-03 T1 |

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): 옵션 ON SIDE Datum 한 개가 가로선 + 패턴매칭만으로 Find 성공 — 옵션 필드부터 검출 분기, 원점 산출, 결과 필드, 로그까지 한 경로</name>
  <precondition>`git status --porcelain -- WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 출력이 비어 있다 (다른 작업의 미커밋 변경이 BASE 비교에 섞이지 않는다)</precondition>
  <reversibility rating="costly">INI 키 이름 IsVerticalLineDisabled 는 다음 레시피 저장부터 모든 Datum 섹션에 기록된다 — 나중에 이름을 바꾸면 SIDE PC 에서 켜 둔 옵션이 조용히 꺼져(키 부재 = false, 기존 동작) 4개 Datum 을 다시 켜야 한다. 데이터 마이그레이션은 없다.</reversibility>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs, WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - CLAUDE.md (Code Style — 가독성 하드룰 전체, 위반 = 회귀)
    - .planning/phases/76-side-datum/76-CONTEXT.md (D-76-01 ~ D-76-07)
    - .planning/phases/76-side-datum/76-RESEARCH.md §3 (transform 구조), §4 (DetectedRefAngle2 = 0.0), §5 (원점 산출식), §8 (INI bool), §10 (회귀 표면)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs:1-40 (상수 블록, AlignPreTransform), :85-117 (2-image TryFindDatum), :241-272 (AffineTransPoint2d 선례), :622-818 (DualImage Find 전체 — 가로 부분을 그대로 옮겨 쓴다), :1567-1599, :1687-1691 (로그 관례), :1903-1941 (TryExtractEdgePoints 시그니처와 AlignPreTransform 소비)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs:125-140 (IsPatternAlignEnabled 선례), :373-427 (Datum|Algorithm 구역, AlgorithmTypeEnum), :1012-1045 (Detected* 필드), :1325-1355 (Load 오버라이드 — 읽기만)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs:2926-3052 (TryComposeAlign — 읽기만, 수정 금지)
    - WPF_Example/Sequence/Param/ParamBase.cs:318-399 (bool Save/Load — 읽기만)
  </read_first>
  <action>
D-76-01, D-76-02, D-76-04, D-76-05 를 한 경로로 관통시킨다. 옵션 OFF 경로는 기존 줄을 한 글자도 바꾸지 않는다(D-76-02 분기는 새 if 한 개, SDV-04).

(1) DatumConfig.cs — `AngleTolerance` 선언(계획 시점 :427) 바로 아래에 공개 bool 자동 속성 `IsVerticalLineDisabled` 를 추가한다. 기본값 false. 어트리뷰트는 이웃과 같은 `[Category("Datum|Algorithm")]` 와 `[System.ComponentModel.Description(...)]` 두 개(IsPatternAlignEnabled :132-134 와 같은 모양, RaisePropertyChanged·경고창 없음). Description 문구(한국어, 문구 안에 물음표 문자 없이): "세로선 끄기 (VerticalTwoHorizontalDualImage 전용). 켜면 세로 이미지(ZIndexB)는 그대로 촬영·저장하지만 세로선 검출은 하지 않고, 가로 결합선과 패턴매칭만으로 기준점을 만든다 — 원점 X = 패턴매칭으로 옮긴 티칭 원점 열, 원점 Y·각도 = 가로 결합선. 패턴 정렬(IsPatternAlignEnabled)이 켜져 있어야 하며 꺼져 있으면 Datum 찾기가 실패한다. 세로 기준각이 없으므로 이 Datum 을 참조하는 X축(MeasureAxis=X) 측정에는 쓰지 않는다. 티칭은 지금처럼 세로선이 필요하다. 기본 꺼짐." INI 하위호환(키 부재 = false)은 ParamBase.Load Boolean 분기가 이미 보장하므로 `Load` 오버라이드(:1325-1355)에는 아무것도 넣지 않는다(D-76-01).

(2) DatumConfig.cs — 새 속성 바로 아래에 공개 메서드 `public bool IsHorizontalOnlyActive()` 를 추가한다. 프로퍼티가 아니라 메서드여야 한다(ParamBase.Save 가 공개 bool 프로퍼티를 전부 INI 에 쓰므로 계산값을 프로퍼티로 두면 쓸모없는 키가 생긴다). 본문: 알고리즘이 VerticalTwoHorizontalDualImage 인지를 이름 있는 bool `bDualImage` 로 먼저 뽑고(AlgorithmTypeEnum 비교), bDualImage 가 false 면 false 를 반환, 아니면 IsVerticalLineDisabled 를 반환. 스타일은 바로 위 AlgorithmTypeEnum 게터(:398-407)처럼 Allman. XML 요약 주석 한 줄: 옵션이 실제로 효력을 가지는지의 유일한 규칙이며 검출·표시·캡처 모든 소비처가 이 메서드만 부른다(TOP/BOTTOM 보호).

(3) DatumFindingService.cs — 클래스 상단 기존 상수(:17-24) 아래에 이 기능 전용 private const 를 추가한다(기존 const 3개는 수정 금지). `DETECTED_REF_ANGLE2_UNSET = 0.0` (세로 기준각 미설정 표식 — 캡처 게이트와 EdgeToLineDistance 가 0.0 을 2차 축 없음으로 읽는다), `MIN_HORIZONTAL_LINE_SPAN_PX = 1.0` (가로 결합선 두 끝점의 열 차이 하한 — 이보다 작으면 열에서 행을 구하는 나눗셈이 무의미), `RAD_TO_DEG = 180.0 / Math.PI`, 그리고 기존 가로선 피팅(:708-710)과 **완전히 같은 값**의 `HORIZONTAL_FIT_ALGORITHM = "tukey"`, `HORIZONTAL_FIT_MAX_POINTS = -1`, `HORIZONTAL_FIT_CLIP_END_POINTS = 0`, `HORIZONTAL_FIT_ITERATIONS = 5`, `HORIZONTAL_FIT_CLIP_FACTOR = 2.0` (값이 하나라도 다르면 옵션 ON/OFF 의 가로선이 달라져 측정값이 달라진다). 각 const 옆에 이유 한 줄 주석.

(4) DatumFindingService.cs — `TryFindVerticalTwoHorizontalDualImage`(:625) 안, 기존 두 줄(error 초기화, HomMat2dIdentity) 바로 다음이자 `HObject contour = null;` 바로 앞에 새 분기만 삽입한다: `config.IsHorizontalOnlyActive()` 가 true 면 `TryFindDualImageHorizontalOnly(imageHorizontal, config, out transform, out error)` 결과를 bool 지역변수 `bHorizontalOnlyOk` 에 받고, bHorizontalOnlyOk 가 false 면 `Logging.PrintLog((int)ELogType.Algorithm, ...)` 로 `[Datum.HorizontalOnly] fail — datum=<DatumName>: <error>` 한 줄을 남긴 뒤 bHorizontalOnlyOk 를 반환한다(DatumName 이 null 이면 빈 문자열 — 명시적 if). 기존 본문(:630-817)은 손대지 않는다 — 옵션 OFF 는 이 if 를 지나쳐 기존 코드를 그대로 탄다(D-76-02, SDV-04).

(5) DatumFindingService.cs — `TryFindVerticalTwoHorizontalDualImage` 가 끝나는 닫는 중괄호(:818) 바로 다음에 새 private 메서드 두 개를 Allman 스타일로 추가한다. 새 지역변수는 헝가리언 접두사(HTuple = hv, double = d, string = sz, bool = b).

(5-a) `private bool TryMapTaughtOriginColumn(DatumConfig config, out double xOriginCol, out string error)` — 원점 X 산출(D-76-05, RESEARCH §5 후보 (i)). 순서: out 값 초기화 → `bool bNoPatternTransform` = AlignPreTransform 이 null 이거나 길이 0 → true 면 error 에 `Vertical line disabled (IsVerticalLineDisabled) requires pattern align: no pattern-match transform (IsPatternAlignEnabled must be true)` 를 넣고 false → 아니면 try 안에서 `HOperatorSet.AffineTransPoint2d(AlignPreTransform, config.RefOriginRow, config.RefOriginCol, out hvRow, out hvCol)` 후 xOriginCol 에 hvCol 의 D 값, true 반환 → `catch (Exception ex)` 는 error = `Pattern transform mapping of taught origin failed: ` + ex.Message 로 false. TryFindCircleTwoHorizontal(:262-272)처럼 실패 시 원래 좌표로 조용히 되돌아가면 좌우 이동을 못 따라가면서 OK 가 나오므로 절대 폴백하지 않는다(PR-2). finally 에서 hvRow·hvCol 을 null 검사 후 각각 try-catch 로 Dispose.

(5-b) `private bool TryFindDualImageHorizontalOnly(HImage imageHorizontal, DatumConfig config, out HTuple transform, out string error)` — 세로 이미지를 매개변수로 받지 않는다. 세로 ROI 라인 검출기는 호출하지 않고, 실패 시 세로 검출로 되돌아가는 폴백도 없다(D-76-02, PR-4). 순서:
  1. error 초기화, `HOperatorSet.HomMat2dIdentity(out transform)`. Datum 이름을 `szDatumName` 에 담는다(DatumName 이 null 이면 빈 문자열 — 명시적 if).
  2. `Logging.PrintLog((int)ELogType.Algorithm, ...)` 로 `[Datum.Vertical] skipped — datum=<szDatumName> (IsVerticalLineDisabled): 세로선 검출 생략, 원점 X = 패턴매칭, 원점 Y·각도 = 가로 결합선` 한 줄(orchestrator 메모 10 — 사용자가 로그로 모드를 확인).
  3. `TryMapTaughtOriginColumn(config, out dOriginCol, out szMapError)` — 실패하면 error = szMapError, false (X 출처가 없으면 가로 검출을 할 이유가 없으므로 가로 검출보다 먼저).
  4. try 앞에서 `HObject contour = null;` 과 finally 에서 해제할 HTuple 임시 변수들(hvWidth, hvHeight, hvAllRows, hvAllCols, 피팅 출력 7개)을 null 로 먼저 선언한다. try 블록: 가로 이미지 크기(hvWidth, hvHeight) → Horizontal_A, Horizontal_B 의 `TryExtractEdgePoints` 호출은 기존 :659-691 과 인자·ROI 라벨(`"Horizontal_A"`, `"Horizontal_B"`)·오류 문구(`Horizontal_A: ` / `Horizontal_B: ` 접두)까지 똑같이 → `totalEdges < MIN_HORIZONTAL_EDGES` 검사(:693-698 과 같은 문구) → A+B TupleConcat(hvAllRows, hvAllCols) → `GenContourPolygonXld` → `FitLineContourXld(contour, HORIZONTAL_FIT_ALGORITHM, HORIZONTAL_FIT_MAX_POINTS, HORIZONTAL_FIT_CLIP_END_POINTS, HORIZONTAL_FIT_ITERATIONS, HORIZONTAL_FIT_CLIP_FACTOR, ...)` (내부 try-catch 오류 문구는 :712-716 과 같게, 출력은 hvRowBegin, hvColBegin, hvRowEnd, hvColEnd, hvNr, hvNc, hvDist 로 받는다 — 아래 식의 hrB, hcB, hrE, hcE 는 각각 hvRowBegin, hvColBegin, hvRowEnd, hvColEnd 의 D 값) → `dCurAngle = Math.Atan2(hrE - hrB, hcE - hcB)` (:737 과 같은 식).
  5. `dSpanCol` = hcE - hcB. 절댓값이 MIN_HORIZONTAL_LINE_SPAN_PX 미만이면 error `Horizontal line too short at origin column: span <dSpanCol, F3> px` 로 false.
  6. `dOriginRow` = hrB + (dOriginCol - hcB) × (hrE - hrB) ÷ dSpanCol — 가로 결합선을 열 dOriginCol 에서 평가한 행(D-76-05 'Y = 그 X 에서의 가로 결합선의 행'. 완전히 수직인 가상 세로선과의 교점과 같다).
  7. transform 은 기존 :748-755 와 같은 구성: dRow = dOriginRow - RefOriginRow, dCol = dOriginCol - RefOriginCol, dAngle = dCurAngle - RefAngleRad → Identity → HomMat2dTranslate(dRow, dCol) → HomMat2dRotate(dAngle, dOriginRow, dOriginCol) 결과를 out transform 에. align 경로에서는 TryComposeAlign(:3049)이 곧바로 alignRigid 로 덮어써서 측정 ROI 위치는 원래부터 패턴매칭만으로 정해진다 — 이 의미를 바꾸지 않는다.
  8. 결과 필드: DetectedOriginRow = dOriginRow, DetectedOriginCol = dOriginCol, DetectedRefAngle = dCurAngle, DetectedRefAngle2 = DETECTED_REF_ANGLE2_UNSET (PR-6), DetectedEdgeCount = totalEdges (세로 에지 없음), DetectedFitRMSE = 0.0, DetectedAngleDeg = dCurAngle × RAD_TO_DEG.
  9. 화면용 휘발 필드: Line2Detected_RBegin/CBegin/REnd/CEnd = 가로 결합선 끝점 4개 값, Horizontal_A_DetectedEdgeRows/Cols 와 Horizontal_B_DetectedEdgeRows/Cols = A/B 에지 튜플. 세로 흔적은 지운다 — Line1Detected_RBegin/CBegin/REnd/CEnd = 0.0, Vertical_DetectedEdgeRows/Cols = new HTuple() (이전 사이클·티칭 복원 값이 남아 화면에 그려지지 않게 — D-76-06 근거, 표시 게이트는 76-02).
  10. `config.LastFindSucceeded = true;` 다음 string.Format 으로 `[Datum.HorizontalOnly] ok — datum={0} origin=({1:F1}, {2:F1}) angle={3:F3}deg edges={4}` 를 ELogType.Algorithm 에 남기고 true.
  11. `catch (Exception ex)`: error = ex.Message, transform 을 다시 Identity, false. `finally`: contour(HObject)와 이 메서드 안에서만 쓴 HTuple 임시값(hvWidth, hvHeight, hvAllRows, hvAllCols, 피팅 출력 7개)을 null 검사 후 각각 try-catch Dispose. config 에 넣은 에지 HTuple 4개와 out transform 은 Dispose 하지 않는다.
  (가로선 방향 15도 검사, 티칭 원점 미설정 가드, AngleTolerance 배지 평가는 Task 2 가 이 메서드에 더한다.)

(6) 건드리지 않는 것: InspectionSequence.cs(TryComposeAlign), Action_FAIMeasurement.cs(세로 이미지 ZIndexB 촬영 TryGrabOrLoadDualDatumImages 와 오프라인 자동 채움 AutoFillDatumOfflineImage 포함 — D-76-03), UI/ContentItem/MainView.xaml.cs(신규 로직 금지), TryTeachDatum 과 TryTeachVerticalTwoHorizontalDualImage(D-76-07 — 기존 티칭 원점 재사용), Halcon/Algorithms/FAIEdgeMeasurementService.cs(D-02 LOCKED), 기존 const·기존 메서드 본문 전부. 새 .cs 파일 금지(WPF_Example/DatumMeasurement.csproj 스테이징 금지). 운영 레시피 D:\Data\Recipe 는 읽기만.

(7) 커밋: `git add WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 로 두 파일만 지정한다(전체 추가 금지). 메시지 예: feat(76-01): SIDE Datum 세로선 끄기 옵션 — 가로선 + 패턴매칭 전용 Find 경로.

CLAUDE.md 하드룰(위반 = 회귀, 새로 쓰거나 고치는 모든 줄에 적용): 조건 연산자 금지 — 삼항 대신 if/else, null 병합·null 조건 연산자 대신 명시적 null 비교. C# 8 switch 식 금지(전통 switch 문과 case 마다 break). C# 7.2 문법만(nullable 참조형·record·새 패턴매칭 금지). 한 줄 분기에도 중괄호 필수, 한 줄에 한 문장. 조건이 셋 이상 이어지면 이름 있는 bool 로 먼저 뽑는다. if 중첩 3단계 이상이면 메서드를 나눈다. 헝가리언 접두사 b / n / sz / d / hv. 매직넘버 금지(이름 있는 const). 날짜와 이니셜을 붙인 서명 주석 신규 금지 — 주석은 비자명한 이유만. HImage / HObject / HTuple 은 finally 에서 개별 try-catch 로 Dispose. 새 줄(문자열·주석 포함)에 물음표 문자를 쓰지 않는다(삼항 검사 grep 오탐 방지).
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
BASE=$(git log --format=%H -1 -- .planning/phases/76-side-datum/76-01-PLAN.md)
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
SCR="$(cygpath -m "$TEMP")/p76build"
"$MSB" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -p:OutDir="$SCR/on/" -p:IntermediateOutputPath="$SCR/obj-on/" -v:minimal -nologo > "$SCR-on.log" 2>&1; echo "msbuild_exit=$?"
echo "errors=$(grep -cE ': error [A-Z]+[0-9]+' "$SCR-on.log")"
echo "warn_codes=$(grep -oE 'warning CS[0-9]+' "$SCR-on.log" | sort -u | tr '\n' ' ')"
DC=WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
DF=WPF_Example/Halcon/Algorithms/DatumFindingService.cs
echo "prop=$(grep -c 'public bool IsVerticalLineDisabled { get; set; } = false;' $DC)"
echo "rule=$(tr -d '\r' < $DC | awk '/public bool IsHorizontalOnlyActive\(\)/,/^        }$/' | grep -cE 'VerticalTwoHorizontalDualImage|IsVerticalLineDisabled')"
echo "branch=$(tr -d '\r' < $DF | awk '/private bool TryFindVerticalTwoHorizontalDualImage\(/,/HObject contour = null;/' | grep -c 'if (config.IsHorizontalOnlyActive())')"
echo "calls=$(tr -d '\r' < $DF | grep -vE '^\s*//' | grep -c 'IsHorizontalOnlyActive()')"
echo "honly=$(grep -c 'private bool TryFindDualImageHorizontalOnly(HImage imageHorizontal, DatumConfig config, out HTuple transform, out string error)' $DF)"
echo "novert=$(tr -d '\r' < $DF | awk '/private bool TryFindDualImageHorizontalOnly\(/,/^        }$/' | grep -vE '^\s*//' | grep -cE 'TryFindLine\s*\(')"
echo "map=$(tr -d '\r' < $DF | awk '/private bool TryMapTaughtOriginColumn\(/,/^        }$/' | grep -c 'AffineTransPoint2d(AlignPreTransform, config.RefOriginRow, config.RefOriginCol')"
echo "angle2=$(tr -d '\r' < $DF | awk '/private bool TryFindDualImageHorizontalOnly\(/,/^        }$/' | grep -c 'config.DetectedRefAngle2 = DETECTED_REF_ANGLE2_UNSET;')"
echo "fitconst=$(grep -cE 'HORIZONTAL_FIT_ALGORITHM = "tukey"|HORIZONTAL_FIT_MAX_POINTS = -1;|HORIZONTAL_FIT_CLIP_END_POINTS = 0;|HORIZONTAL_FIT_ITERATIONS = 5;|HORIZONTAL_FIT_CLIP_FACTOR = 2.0;' $DF)"
echo "skiplog=$(grep -c '\[Datum.Vertical\] skipped' $DF) oklog=$(grep -c '\[Datum.HorizontalOnly\] ok' $DF) faillog=$(grep -c '\[Datum.HorizontalOnly\] fail' $DF)"
echo "patternmsg=$(grep -c 'requires pattern align' $DF)"
for F in $DC $DF; do
  ADD=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+')
  echo "$F deleted=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk')"
done
echo "changed=$(git diff --name-only "$BASE" -- WPF_Example | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - 빌드: `msbuild_exit=0`, `errors=0`, `warn_codes` 가 `warning CS0169 warning CS0618` 를 벗어나지 않는다(계획 시점 기준 경고 코드 집합 — 새 코드 종류 0)
    - `prop=1` — DatumConfig.cs 에 `public bool IsVerticalLineDisabled { get; set; } = false;` 정확히 1줄
    - `rule` >= 2 — IsHorizontalOnlyActive() 본문이 VerticalTwoHorizontalDualImage 와 IsVerticalLineDisabled 를 모두 참조(TOP/BOTTOM 보호, D-76-01)
    - `branch=1` — 분기가 TryFindVerticalTwoHorizontalDualImage 시그니처와 그 메서드의 첫 `HObject contour = null;` 사이에 있다(기존 본문 앞, D-76-02)
    - `calls=1` — DatumFindingService.cs 에서 IsHorizontalOnlyActive() 호출은 그 분기 한 곳뿐(주석 줄 제외)
    - `honly=1` — H-only 메서드 시그니처가 가로 이미지 하나만 받는다(세로 이미지 매개변수 없음)
    - `novert=0` — H-only 메서드 안에서 세로 라인 검출기 호출 0 (주석 줄 제외, 영역 한정)
    - `map=1` — TryMapTaughtOriginColumn 이 AlignPreTransform 으로 RefOriginRow/RefOriginCol 을 변환한다(D-76-05 후보 (i))
    - `angle2=1` — H-only 결과의 DetectedRefAngle2 는 미설정 표식 상수
    - `fitconst=5` — 피팅 상수 5개가 기존 호출(:708-710)과 같은 값으로 선언됨
    - `skiplog=1`, `oklog` >= 1, `faillog` >= 1, `patternmsg` >= 1
    - 두 파일 모두 `deleted=0` (git diff -w 기준 기존 줄 삭제·수정 0 — SDV-04)
    - 두 파일 모두 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0` (추가 줄 한정 하드룰 grep 5종: `\?[^?]*:`, `??`, `?.`, `switch.*=>`, `hbk`)
    - `changed=` 출력이 정확히 `WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs WPF_Example/Halcon/Algorithms/DatumFindingService.cs` (InspectionSequence.cs, Action_FAIMeasurement.cs, MainView.xaml.cs, FAIEdgeMeasurementService.cs, DatumMeasurement.csproj 미포함)
    - `git show --stat HEAD` 에 csproj 가 없다
  </acceptance_criteria>
  <done>옵션 ON DualImage Datum 의 Find 가 세로 검출 없이 가로 결합선 + 패턴매칭 원점으로 성공하는 경로가 컴파일되고 커밋되었으며, 옵션 OFF 경로는 기존 줄 삭제 0 이다.</done>
</task>

<task type="auto">
  <name>Task 2: 옵션 노출을 DualImage Datum 으로 한정하고 H-only Find 의 오류 상태(티칭 원점 없음, 가로선 방향)와 각도 배지 평가를 기존 경로와 맞춘다</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs, WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <read_first>
    - CLAUDE.md (Code Style 하드룰)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs 의 `IsHiddenForAlgorithm` 메서드 전체(계획 시점 :1273-1313 — Task 1 삽입으로 뒤로 밀림, 메서드 이름으로 찾는다)
    - WPF_Example/Halcon/Algorithms/DatumFindingService.cs:737-785 (기존 가로 방향/직각성 호출과 AngleTolerance 게이트), :1567-1599 (ValidateHorizontalVerticalAngles 의 가로 방향 정규화와 오류 문구), Task 1 이 추가한 TryFindDualImageHorizontalOnly 와 TryMapTaughtOriginColumn 전체
    - .planning/phases/76-side-datum/76-RESEARCH.md §5 (horizontal-only 방향성 체크 권장), §8 (IsHiddenForAlgorithm 선례)
  </read_first>
  <action>
(1) DatumConfig.cs `IsHiddenForAlgorithm` — case `TwoLineIntersect`, `CircleTwoHorizontal`, `VerticalTwoHorizontal` 세 곳에만, 각 case 의 ExpectedAngleDeg/AngleTolerance 숨김 줄 바로 다음에 `if (name == nameof(IsVerticalLineDisabled)) { return true; }` 한 줄씩 넣는다(중괄호 포함, 이 메서드의 K&R 스타일). `VerticalTwoHorizontalDualImage` case 에는 넣지 않는다 — 숨김 조건이 없어야 그 알고리즘에서만 보인다(ExpectedAngleDeg/AngleTolerance/ZIndexA/ZIndexB 와 같은 선례, D-76-01). 기존 줄은 수정하지 않는다.

(2) DatumFindingService.cs 상수 블록(Task 1 의 const 들 옆)에 `DEG_FULL_TURN = 360.0`, `DEG_HALF_TURN = 180.0`, `DEG_QUARTER_TURN = 90.0` 을 추가한다(각도 정규화용, 이유 주석 한 줄).

(3) `TryMapTaughtOriginColumn` — transform 존재 검사 다음, 변환 전에 티칭 원점 가드를 넣는다: `bool bOriginNotTaught` = RefOriginRow 가 0.0 이고 RefOriginCol 도 0.0. true 면 error `Vertical line disabled requires a taught origin: taught origin missing (RefOriginRow/RefOriginCol = 0) — teach the datum first` 로 false. IsConfigured 는 쓰지 않는다(DualImage 2-image 경로는 기존에도 IsConfigured 를 보지 않으므로 기존 SIDE 레시피가 이 가드에 걸리면 안 된다). 티칭은 여전히 세로선으로 한다(D-76-07) — 이 가드는 티칭 없이 H-only 원점이 만들어지는 것을 막는다.

(4) `TryFindDualImageHorizontalOnly` — dCurAngle 계산 직후, 열 폭 검사 전에 가로 방향 검사를 넣는다. ValidateHorizontalVerticalAngles(:1571-1581)와 같은 정규화: dHorizDeg = dCurAngle × RAD_TO_DEG 의 절댓값, DEG_QUARTER_TURN 보다 크면 DEG_HALF_TURN 에서 뺀 값으로 바꾼다, HORIZONTAL_TOLERANCE_DEG 보다 크면 error `Horizontal line orientation out of range: <dHorizDeg F1> deg (expected +/-<HORIZONTAL_TOLERANCE_DEG F1> deg)` (기존과 같은 문구 형식)로 false. 직각성 검사(세로각 필요)는 하지 않는다 — 세로선이 없으므로 정의되지 않는다(CONTEXT 계획 단계 결정 4). 기존 ValidateHorizontalVerticalAngles 는 수정하지 않는다.

(5) `TryFindDualImageHorizontalOnly` — 결과 필드(DetectedAngleDeg) 기록 직후에 AngleTolerance 배지 평가를 기존 :768-785 와 같은 규칙으로 넣는다(중괄호 필수 — 기존 코드는 중괄호가 없지만 새 코드는 반드시 붙인다): AngleTolerance 가 0.0 보다 크면 dAngleDiff = DetectedAngleDeg - ExpectedAngleDeg 를 (dAngleDiff + DEG_FULL_TURN + DEG_HALF_TURN) 을 DEG_FULL_TURN 으로 나눈 나머지에서 DEG_HALF_TURN 을 뺀 값으로 정규화하고, 그 절댓값이 AngleTolerance 이하면 AngleValidationStatus = EAngleValidationStatus.Pass, 아니면 Fail. AngleTolerance 가 0.0 이하면 None. 오류로 반환하지 않는다(Find 는 성공 유지, 배지로만 표시 — 기존과 동일).

(6) 순서 불변식을 지킨다: H-only 메서드 안에서 TryMapTaughtOriginColumn 호출이 LastFindSucceeded = true 보다 먼저 있고, 모든 실패 분기는 LastFindSucceeded 를 건드리지 않은 채 false 를 반환한다(TryFindDatum :97 의 false 리셋이 유지되도록, PR-2). 실패 로그는 Task 1 분기의 `[Datum.HorizontalOnly] fail` 한 곳에서만 남긴다.

(7) 커밋: `git add WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 두 파일만. 메시지 예: feat(76-01): 세로선 끄기 옵션 DualImage 전용 노출 + H-only 가드(티칭 원점/가로 방향) + 각도 배지.

CLAUDE.md 하드룰(위반 = 회귀): 조건 연산자 금지 — 삼항 대신 if/else, null 병합·null 조건 연산자 대신 명시적 null 비교. C# 8 switch 식 금지. C# 7.2 문법만. 한 줄 분기에도 중괄호 필수, 한 줄에 한 문장. 조건이 셋 이상이면 이름 있는 bool 로 먼저 뽑는다. if 중첩 3단계 이상 금지. 헝가리언 접두사 b / n / sz / d / hv. 매직넘버 금지. 날짜와 이니셜을 붙인 서명 주석 신규 금지. HImage / HObject / HTuple 은 finally 에서 개별 try-catch Dispose. 새 줄에 물음표 문자를 쓰지 않는다. 새 .cs 파일 금지, csproj 스테이징 금지, InspectionSequence.cs / Action_FAIMeasurement.cs / MainView.xaml.cs / FAIEdgeMeasurementService.cs 수정 금지.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
BASE=$(git log --format=%H -1 -- .planning/phases/76-side-datum/76-01-PLAN.md)
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
SCR="$(cygpath -m "$TEMP")/p76build"
"$MSB" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -p:OutDir="$SCR/on/" -p:IntermediateOutputPath="$SCR/obj-on/" -v:minimal -nologo > "$SCR-on.log" 2>&1; echo "msbuild_exit=$?"
echo "errors=$(grep -cE ': error [A-Z]+[0-9]+' "$SCR-on.log")"
echo "warn_codes=$(grep -oE 'warning CS[0-9]+' "$SCR-on.log" | sort -u | tr '\n' ' ')"
DC=WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
DF=WPF_Example/Halcon/Algorithms/DatumFindingService.cs
echo "hide_total=$(tr -d '\r' < $DC | awk '/private static bool IsHiddenForAlgorithm\(/,/^        }$/' | grep -c 'nameof(IsVerticalLineDisabled)')"
echo "hide_tli=$(tr -d '\r' < $DC | awk '/case EDatumAlgorithm.TwoLineIntersect:/,/return false;/' | grep -c 'nameof(IsVerticalLineDisabled)')"
echo "hide_cth=$(tr -d '\r' < $DC | awk '/case EDatumAlgorithm.CircleTwoHorizontal:/,/return false;/' | grep -c 'nameof(IsVerticalLineDisabled)')"
echo "hide_vth=$(tr -d '\r' < $DC | awk '/case EDatumAlgorithm.VerticalTwoHorizontal:/,/return false;/' | grep -c 'nameof(IsVerticalLineDisabled)')"
HO=$(tr -d '\r' < $DF | awk '/private bool TryFindDualImageHorizontalOnly\(/,/^        }$/')
MP=$(tr -d '\r' < $DF | awk '/private bool TryMapTaughtOriginColumn\(/,/^        }$/')
echo "orient=$(printf '%s\n' "$HO" | grep -c 'HORIZONTAL_TOLERANCE_DEG') orientmsg=$(printf '%s\n' "$HO" | grep -c 'Horizontal line orientation out of range')"
echo "badge=$(printf '%s\n' "$HO" | grep -cE 'AngleValidationStatus = EAngleValidationStatus\.(Pass|Fail|None);')"
echo "taught=$(printf '%s\n' "$MP" | grep -c 'taught origin missing')"
echo "degconst=$(grep -cE 'DEG_FULL_TURN = 360.0;|DEG_HALF_TURN = 180.0;|DEG_QUARTER_TURN = 90.0;' $DF)"
echo "order_ok=$(printf '%s\n' "$HO" | awk '/TryMapTaughtOriginColumn\(/{if(a==0){a=NR}} /config.LastFindSucceeded = true;/{b=NR} END{if(a>0 && b>a){print 1}else{print 0}}')"
for F in $DC $DF; do
  ADD=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+')
  echo "$F deleted=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk')"
done
echo "changed=$(git diff --name-only "$BASE" -- WPF_Example | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - 빌드: `msbuild_exit=0`, `errors=0`, `warn_codes` 가 `warning CS0169 warning CS0618` 를 벗어나지 않는다
    - `hide_total=3`, `hide_tli=1`, `hide_cth=1`, `hide_vth=1` — 숨김은 DualImage 가 아닌 세 case 에만(합계 3 이고 세 case 가 각 1 이므로 DualImage case 는 0, D-76-01)
    - `orient` >= 1 이고 `orientmsg` >= 1 — H-only 메서드가 기존 가로 방향 임계(15도)를 쓴다
    - `badge=3` — Pass/Fail/None 세 상태 모두 H-only 메서드에서 기록
    - `taught=1` — TryMapTaughtOriginColumn 에 티칭 원점 미설정 가드
    - `degconst=3`
    - `order_ok=1` — 원점 X 산출(TryMapTaughtOriginColumn 호출)이 LastFindSucceeded = true 보다 앞에 있다(PR-2)
    - 두 파일 모두 `deleted=0` 이고 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0`
    - `changed=` 출력이 정확히 DatumConfig.cs 와 DatumFindingService.cs 두 파일
  </acceptance_criteria>
  <done>옵션은 DualImage Datum 속성창에만 보이고, H-only Find 는 티칭 원점 없음·가로선 방향 이탈을 오류로 막으며 각도 배지를 기존과 같은 규칙으로 평가한다. 옵션 OFF 경로 기존 줄 삭제 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레시피 INI(현장 운영자 편집·수기 편집) → DatumConfig | IsVerticalLineDisabled 값이 어느 Datum 에든 들어올 수 있다 |
| 패턴매칭 결과(AlignPreTransform) → Datum 원점 → 측정·PLC 합부 | 원점 X 의 유일한 출처. 없거나 틀리면 합부가 틀린다 |
| 검사 스레드 → 새 검출 코드 | 예외·NaN 이 사이클을 멈추면 안 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-76-01 | Tampering | DatumConfig.IsVerticalLineDisabled 가 TOP/BOTTOM(CircleTwoHorizontal) Datum INI 에 수기로 들어감 | medium | mitigate | 효력은 IsHorizontalOnlyActive() 한 곳에서만 판정하고 AlgorithmTypeEnum == VerticalTwoHorizontalDualImage 를 요구. 검출 분기는 DualImage 전용 메서드 안에만 존재(Task 1 `branch=1`, `calls=1`) |
| T-76-02 | Spoofing | TryFindDualImageHorizontalOnly — 패턴 transform 없이 원점을 만들어 OK 로 보고(좌우 이동 미추종 상태로 PLC 에 합격) | high | mitigate | TryMapTaughtOriginColumn 이 AlignPreTransform null/빈 값·변환 예외에서 false, 원래 좌표로 폴백 금지. 원점 산출이 LastFindSucceeded = true 보다 앞(`order_ok=1`). 매칭 실패는 TryComposeAlign 1단계에서 이미 ALIGN_FAIL |
| T-76-03 | Tampering | 티칭되지 않은 Datum(RefOrigin 0,0)에서 H-only 원점 산출 | medium | mitigate | Task 2 `taught origin missing` 가드로 실패 |
| T-76-04 | Denial of Service | 새 코드의 예외·0 나눗셈이 검사 스레드를 멈춤 | medium | mitigate | 메서드 전체 try-catch → false(Never throw from Run 관례), MIN_HORIZONTAL_LINE_SPAN_PX 가드, HObject/HTuple finally Dispose |
| T-76-05 | Repudiation | 어떤 모드로 원점이 만들어졌는지 운영자가 확인 불가 | low | mitigate | `[Datum.Vertical] skipped`, `[Datum.HorizontalOnly] ok/fail` 로그(Datum 이름 포함, ELogType.Algorithm) |
| T-76-06 | Information Disclosure | 새 로그 내용 | low | accept | 좌표·각도만 로컬 로그에 기록, 네트워크로 나가지 않음 |
</threat_model>

<verification>
1. Debug|x64 빌드 에러 0, 경고 코드 집합 CS0169 / CS0618 유지 (Task 1, Task 2 각각)
2. DatumConfig.cs / DatumFindingService.cs 의 `git diff -w` 삭제 0 — 옵션 OFF 경로와 TOP/BOTTOM 경로 무변경(SDV-04)
3. 분기 위치(`branch=1`), 호출 1곳(`calls=1`), 세로 검출 미호출(`novert=0`), 원점 변환식(`map=1`), 세로각 표식(`angle2=1`)
4. PropertyGrid 노출 범위(`hide_*`), 가드와 배지(`taught`, `orient`, `badge`), 순서 불변식(`order_ok=1`)
5. 추가 줄 하드룰 grep 5종 0, 변경 파일 목록 정확히 2개, csproj 미커밋
6. 기능 확인(옵션 ON 에서 세로 흐림에도 Find OK, 로그 확인)은 76-03 의 SIDE PC UAT 에서 사용자가 수행한다 — 이 plan 은 자동 검증만 한다
</verification>

<success_criteria>
- IsVerticalLineDisabled 가 DualImage Datum 에만 노출되고 INI 에 저장·복원되며 키 부재 시 false
- 옵션 ON DualImage Datum 은 세로 검출 없이 가로 결합선 + 패턴매칭 원점으로 Find 성공, 패턴 transform·티칭 원점이 없으면 실패
- 원점 X = AlignPreTransform 으로 옮긴 RefOriginCol, Y = 그 열의 가로 결합선 행, 각도 = 가로 결합선, DetectedRefAngle2 = 0.0
- 옵션 OFF 는 기존 줄 삭제 0, 티칭 경로 무변경
</success_criteria>

<output>
완료 후 `.planning/phases/76-side-datum/76-01-SUMMARY.md` 를 작성한다. 반드시 포함:
- BASE 해시와 두 태스크 커밋 해시, 빌드 결과(에러 수, 경고 코드 집합), 두 파일의 `git diff -w` 삭제 수
- 새 심볼 목록(프로퍼티/메서드/const/로그 태그/오류 문구) — 76-02 가 IsHorizontalOnlyActive() 를 그대로 쓴다
- D-76-05 원점 산출식과 DetectedRefAngle2 = 0.0 결정, 계획 단계 결정 1~6 요약
- Flagged assumption A-76-E1(false match 교차검증 상실)을 그대로 옮겨 적는다
</output>
