---
phase: 76-side-datum
plan: 03
type: execute
wave: 3
depends_on: ["76-01", "76-02"]
files_modified:
  - WPF_Example/VersionDefine.cs
  - .planning/phases/76-side-datum/76-HUMAN-UAT.md
autonomous: false
requirements: [SDV-01, SDV-02, SDV-03, SDV-04]
user_setup: []

must_haves:
  truths:
    - "phase 전체가 SIMUL-ON(기본 Debug|x64)과 SIMUL-OFF(DefineConstants=TRACE;DEBUG) 두 구성에서 에러 0 으로 빌드되고, 경고 코드 집합이 계획 시점 기준 CS0169 / CS0618 을 벗어나지 않는다"
    - "phase 전체 diff 에서 6개 동작 파일의 기존 줄 삭제 0(git diff -w)이고, WPF_Example 아래 변경 파일은 정확히 그 6개와 VersionDefine.cs 뿐이다 — 옵션 OFF 와 TOP/BOTTOM 동작 경로는 코드상 무변경이다 (SDV-04)"
    - "[edge SDV-04 ordering] TCP 응답, 측정 결과, Datum 처리 순서는 바뀌지 않는다 — 순서를 정하는 TcpServer/VisionServer, InspectionSequence, CycleResultSerializer, 측정 클래스 파일은 이 phase 에서 수정되지 않는다(변경 파일 목록이 정확히 7개)"
    - "세로 이미지(ZIndexB) 촬영·저장 코드와 z index·PLC 응답 코드는 바뀌지 않는다 — Action_FAIMeasurement.cs 의 변경은 BuildDatumCaptureSnapshot 한 hunk 뿐이다 (D-76-03)"
    - "옵션 효력 판정은 DatumConfig.IsHorizontalOnlyActive() 한 규칙으로만 읽힌다 — 6개 파일 전체에서 이 메서드 선언 1 + 호출 4(검출 1, 표시 2, 캡처 1) 외 사용처가 없다"
    - "SIDE PC 실기 확인 절차(76-HUMAN-UAT.md, U-1 ~ U-8 과 알려진 한계 6개)가 있고, 사용자 회신 결과가 항목별 PASS / FAIL / PENDING 으로 기록된다"
    - "VersionDefine 에 이 phase 의 [Version] 항목이 하나 늘고, VERSION 상수가 그 번호와 같으며, 이전 항목은 모두 남아 있다"
  artifacts:
    - path: "WPF_Example/VersionDefine.cs"
      provides: "phase 76 changelog 항목 + VERSION / BUILD_DATE"
      contains: "IsVerticalLineDisabled"
    - path: ".planning/phases/76-side-datum/76-HUMAN-UAT.md"
      provides: "SIDE PC 실기 확인 절차 U-1 ~ U-8 + 결과 기록란 + 알려진 한계"
      contains: "U-8"
  key_links:
    - from: ".planning/phases/76-side-datum/76-HUMAN-UAT.md"
      to: "76-CONTEXT.md 의 '실기 검증 (SIDE PC)' 5개 시나리오 + 76-VALIDATION.md Manual-Only 5개"
      via: "U-2 ~ U-6 으로 옮기고 U-1(설정), U-7(회귀), U-8(매칭 없음)을 더함"
      pattern: "U-5"
    - from: "WPF_Example/VersionDefine.cs VERSION 상수"
      to: "새 [Version] 항목의 Number"
      via: "같은 번호 문자열"
      pattern: "public const string VERSION"
  prohibitions:
    - statement: "MUST NOT TOP/BOTTOM(CircleTwoHorizontal) Datum 의 검출, 표시, 캡처, 측정을 바꾸지 않는다 — 그 Datum 섹션에 IsVerticalLineDisabled=True 가 수기로 들어가 있어도 마찬가지다"
      status: resolved
      verification: judgment
    - statement: "MUST NOT 옵션 ON 일 때 ZIndexB(세로) 이미지 촬영·저장을 건너뛰거나 z index 순서, PLC 응답의 Datum index 를 바꾸지 않는다 (D-76-03)"
      status: resolved
      verification: judgment
---

<objective>
phase 를 닫기 전에 옵션 → 검출 → 표시 → 캡처 전 경로를 두 빌드 구성에서 한 번에 컴파일해 회귀 0 을 증거로 남기고(SDV-04),
SIDE PC 로 넘길 버전을 찍고, 사람이 실기에서 확인할 절차(SDV-01 ~ SDV-03, D-76-01 ~ D-76-07)를 문서로 만든 뒤 사용자 확인을 받는다.

Purpose: 이 phase 의 기능 확인(세로 흐림에도 사이클 완주, 옵션 ON/OFF 측정값 동등, 좌우 이동 추종, 화면에 세로선 없음)은 실제 카메라와 부품이 있어야 한다.
자동으로 증명할 수 있는 것(빌드, 기존 줄 삭제 0, 변경 파일 범위)과 사람이 봐야 하는 것을 분리해 둘 다 기록한다.

Output: 두 구성 빌드 로그 증거, `VersionDefine.cs` 새 항목, `76-HUMAN-UAT.md`(U-1 ~ U-8 + 알려진 한계), 사용자 회신 기록.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/76-side-datum/76-CONTEXT.md
@.planning/phases/76-side-datum/76-VALIDATION.md
@.planning/phases/76-side-datum/76-01-SUMMARY.md
@.planning/phases/76-side-datum/76-02-SUMMARY.md

<version_rules>
버전 단일 소스 = `WPF_Example/VersionDefine.cs` (계획 시점 560줄)
- `:557` `public const string VERSION = "1.7.40.0";` 와 `:558` `public const string BUILD_DATE = "2026-09-11";` — 실행 시점 값을 읽고, VERSION 은 세 번째 자리를 1 올린다(계획 시점 기준 1.7.41.0). 다른 작업이 먼저 올렸으면 그 값에서 올린다
- 마지막 `[Version(...)]` 항목(계획 시점 `:519-553`, Number 1.7.40.0) 아래, `public static class VersionDefine`(:554) 위에 새 항목을 **하나** 쌓는다. 기존 항목은 지우지 않는다
- `AssemblyVersion` 은 const 여야 한다(CS0182) — 구조를 바꾸지 않는다
</version_rules>

<build_baseline>
계획 시점(BASE = 76-01-PLAN.md 커밋) 실측: SIMUL-ON / SIMUL-OFF 둘 다 MSBuild exit 0, 에러 0, 경고 코드 집합 {CS0169, CS0618}. 스크래치 OutDir 빌드는 저장소를 더럽히지 않는다(bin/, obj/ 는 .gitignore).
</build_baseline>
</context>

## Spec-less probe 반영
이 plan 이 맡는 항목: SDV-04 ordering → truth, prohibitions PR-1(TOP/BOTTOM), PR-3(ZIndexB). phase 전체 원장은 76-01-PLAN.md 'Spec-less probe 반영' 표. 미해결 flagged assumption A-76-E1(76-01), A-76-E2(76-02)는 76-HUMAN-UAT.md 의 알려진 한계와 U-3 / U-5 / U-6 에서 사용자에게 다시 보여준다.

## Artifacts this phase produces (phase 전체)

| 심볼 / 산출물 | 종류 | 파일 | 만드는 곳 |
|---|---|---|---|
| `IsVerticalLineDisabled` / INI 키 `IsVerticalLineDisabled` | public bool 프로퍼티(기본 false) / 레시피 INI 키 | DatumConfig.cs / 레시피 | 76-01 T1 |
| `IsHorizontalOnlyActive()` | public bool 메서드 | DatumConfig.cs | 76-01 T1 |
| `TryFindDualImageHorizontalOnly`, `TryMapTaughtOriginColumn` | private 메서드 | DatumFindingService.cs | 76-01 |
| `DETECTED_REF_ANGLE2_UNSET`, `MIN_HORIZONTAL_LINE_SPAN_PX`, `RAD_TO_DEG`, `HORIZONTAL_FIT_*`(5), `DEG_FULL_TURN`, `DEG_HALF_TURN`, `DEG_QUARTER_TURN` | private const | DatumFindingService.cs | 76-01 |
| 로그 태그 `[Datum.Vertical] skipped`, `[Datum.HorizontalOnly] ok`, `[Datum.HorizontalOnly] fail` | ELogType.Algorithm | DatumFindingService.cs | 76-01 T1 |
| 지역 bool `bDrawVertical` | 표시 게이트 | HalconDisplayService.cs | 76-02 |
| `DatumCaptureOverlay.HideOriginVerticalArm` | public bool 프로퍼티(기본 false) | EdgeInspectionOverlay.cs | 76-02 T2 |
| VersionDefine 새 `[Version]` 항목(계획 시점 기준 `1.7.41.0`) + VERSION / BUILD_DATE | changelog | VersionDefine.cs | 76-03 T1 (이 plan) |
| `76-HUMAN-UAT.md` | 실기 UAT 절차서 + 알려진 한계 | phase 디렉터리 | 76-03 T1, 결과는 T2 (이 plan) |

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): 옵션 → 검출 → 표시 → 캡처 전 경로를 두 구성으로 빌드해 회귀 0 을 증거화하고, 버전과 SIDE PC UAT 절차서를 만든다</name>
  <files>WPF_Example/VersionDefine.cs, .planning/phases/76-side-datum/76-HUMAN-UAT.md</files>
  <read_first>
    - CLAUDE.md (Code Style 하드룰 — VersionDefine 추가 줄에도 적용)
    - .planning/phases/76-side-datum/76-CONTEXT.md ('실기 검증 (SIDE PC)', 성공 기준 1~4, D-76-01 ~ D-76-07)
    - .planning/phases/76-side-datum/76-VALIDATION.md (Manual-Only Verifications)
    - .planning/phases/76-side-datum/76-01-SUMMARY.md, 76-02-SUMMARY.md (커밋 해시, 새 심볼, A-76-E1 / A-76-E2)
    - .planning/phases/76-side-datum/76-01-PLAN.md 의 '계획 단계 결정' 표와 'Flagged assumption A-76-E1', 76-02-PLAN.md 의 'Flagged assumption A-76-E2'
    - WPF_Example/VersionDefine.cs:1-20 (버전 규칙 주석), :515-560 (마지막 항목과 상수)
    - .planning/phases/75-align-corrected-image-evidence/75-HUMAN-UAT.md (UAT 체크리스트 형식 선례)
  </read_first>
  <action>
(1) 통합 빌드 두 구성 — 스크래치 OutDir 로 빌드해 실행 중인 앱의 bin 과 충돌하지 않게 한다(실행 중인 프로세스는 종료하지 않는다). SIMUL-ON = Debug|x64 기본, SIMUL-OFF = 같은 구성에 DefineConstants 를 TRACE 와 DEBUG 로 덮어쓴다(아래 verify 명령 그대로). 두 로그를 스크래치에 남기고 에러 수·경고 코드 집합을 SUMMARY 에 적는다.

(2) phase 전체 회귀 증거(SDV-04) — BASE = 76-01-PLAN.md 를 마지막으로 커밋한 해시. 6개 동작 파일(DatumConfig.cs, DatumFindingService.cs, HalconDisplayService.cs, EdgeInspectionOverlay.cs, Action_FAIMeasurement.cs, OverlayCaptureRenderer.cs)의 git diff -w 기존 줄 삭제 0, WPF_Example 아래 변경 파일이 정확히 그 6개 + VersionDefine.cs, Action_FAIMeasurement.cs 변경 hunk 1개(세로 이미지 ZIndexB 촬영·저장 코드 무변경 — D-76-03), 옵션 효력 규칙 사용처가 선언 1 + 호출 4 뿐(TOP/BOTTOM 보호 — D-76-01), 7개 파일 추가 줄 하드룰 grep 5종 0, csproj 커밋 0. 숫자가 기준과 다르면 어느 plan 이 어떤 줄을 건드렸는지 찾아 SUMMARY 에 기록하고 멈춘다(자체 수정으로 숫자를 맞추지 않는다).

(3) 버전 — version_rules 대로 VERSION(세 번째 자리 + 1)과 BUILD_DATE(작업일) 두 상수만 고치고, 마지막 [Version] 아래에 새 항목 하나를 쌓는다. Number 는 새 VERSION 과 같은 문자열, Date 는 작업일. Change 문구(한국어, 문구 안에 물음표 문자 없이, 기존 항목처럼 문자열 이어붙이기): SIDE Datum 세로선 끄기 옵션(IsVerticalLineDisabled, VerticalTwoHorizontalDualImage 전용, Datum 별) — 켜면 세로 이미지(ZIndexB)는 그대로 촬영·저장하지만 세로선 검출은 하지 않고 가로 결합선과 패턴매칭만으로 Datum 을 만든다(원점 X = 패턴매칭 transform 으로 옮긴 티칭 원점 열, 원점 Y·각도 = 가로 결합선, 세로 기준각은 미설정 0). 자동 사이클과 수동 Test Find 가 같은 규칙이고, 패턴 transform 이나 티칭 원점이 없으면 Datum 찾기가 실패한다(매칭 없는 OK 없음). 옵션 ON Datum 은 결과 화면·Test Find·Datum 선택 화면·저장 캡처에서 세로선, 세로 에지점, 세로 기준선, 원점 십자 세로 팔을 그리지 않는다. 옵션 OFF(기본, 키 없는 옛 레시피 포함)와 TOP/BOTTOM 은 기존 동작 그대로이며 기존 코드 줄 삭제 0. 티칭은 지금처럼 세로선이 필요하다. 옵션 ON Datum 을 참조하는 X축 측정은 쓰지 않는다. (D-76-01 ~ D-76-07)

(4) `.planning/phases/76-side-datum/76-HUMAN-UAT.md` 를 만든다. 쉬운 한국어 서술체. 구성:
  - 상단 '실행 환경' — SIMUL PC 에서 가능한 항목(U-1, U-3 의 Test Find 부분, U-6 의 Test Find·Datum 선택 화면, U-8)과 SIDE PC 필수 항목(U-2 ~ U-7). 새 빌드 버전 번호. 레시피는 먼저 복사본에서 확인할 것을 권한다(운영 레시피 저장은 사용자가 확인 후 직접).
  - 체크리스트 8줄, 정확히 `- [ ] U-<n>. <제목> — PENDING` 형식(n = 1 ~ 8), 각 항목 아래에 확인 절차와 '결과:' 기록란:
    U-1. 옵션 노출·저장·옛 레시피 (SDV-01, D-76-01): Side_Datum_1~4 속성창 Datum|Algorithm 에 IsVerticalLineDisabled 가 보이고 Top_Datum / Bottom_Datum 에는 없다 / 한 SIDE Datum 만 켜고 저장·재로드하면 그 Datum 만 켜져 있다 / 이 빌드 전 레시피를 열면 전부 꺼져 있다.
    U-2. 옵션 OFF 기준값 (SDV-04): 옵션 OFF 로 SIDE 1사이클 — 25개 측정값과 각 SIDE Datum Test Find 성공 창의 '검출 원점' (row, col) 을 적는다.
    U-3. 옵션 ON 동등성과 로그 (SDV-02, D-76-04, D-76-05): 같은 부품 그대로 옵션 ON 1사이클 — 25개 값이 U-2 와 반복성 범위 안 / Test Find '검출 원점' 의 col 이 U-2 교점 col 과 몇 px 안(크게 다르면 기록 — 패턴 기준점과 티칭 원점이 다른 이미지에서 잡혔을 수 있다) / Algorithm 로그(D:\Data\Algorithm)에 '[Datum.Vertical] skipped' 와 '[Datum.HorizontalOnly] ok' 가 Datum 이름과 함께 있다.
    U-4. 세로 흐림 내성 (SDV-02, D-76-02): 세로 이미지를 일부러 흐리게(ZIndexB 초점 이탈) — 옵션 ON 은 Datum OK 로 사이클 완주, 옵션 OFF 는 기존대로 ALIGN_FAIL(Vertical 오류).
    U-5. 좌우 이동 추종 (SDV-02, D-76-05): 부품을 좌우로 밀어 재안착 — 옵션 ON 에서 측정 ROI(결과 화면 초록 박스)가 따라가고 값이 안정. A-76-E1(좌우로만 틀린 패턴 매칭은 세로선이 더 이상 걸러주지 않음)을 함께 관찰.
    U-6. 표시 (SDV-03, D-76-06): 옵션 ON Datum 의 결과 화면, Test Find 결과, Datum 노드 선택 화면, 저장 캡처 JPEG 모두에서 세로 기준선·노랑 세로 검출선·주황 세로 에지점·원점 십자 세로 팔이 없고 가로 기준선·가로 요소는 있다 / 옵션 OFF Datum 과 TOP/BOTTOM 은 이전과 같다 / A-76-E2 해석 확인 — 티칭 원점 십자(마젠타, 빨강)와 세로 ROI 사각형은 남아 있다. 이것까지 숨겨야 하면 FAIL 로 적는다.
    U-7. 회귀 (SDV-04, D-76-03): TOP·BOTTOM 1사이클의 PLC 합부와 측정값이 이전과 같다 / SIDE 옵션 ON 에서도 z index 순서와 PLC 응답의 Datum index 가 그대로이고, 저장 폴더에 SIDE 세로(ZIndexB) 이미지가 계속 저장된다.
    U-8. 매칭 없으면 OK 아님 (SDV-02, PR-2): 옵션 ON 에서 패턴이 안 맞게 한 Test Find 는 'Find 실패', 자동 사이클은 ALIGN_FAIL / 가능하면 IsPatternAlignEnabled 를 끄고 옵션 ON 으로 Test Find — 'requires pattern align' 으로 실패.
  - 하단 '알려진 한계' 6개 번호 목록:
    1) 티칭은 여전히 세로선이 필요하다(D-76-07). 처음부터 세로를 쓸 수 없는 설비는 이 phase 범위 밖.
    2) 세로 기준각이 없다(DetectedRefAngle2 = 0.0). 옵션 ON Datum 을 참조하는 X축(MeasureAxis=X) 측정은 쓰지 않는다 — EdgeToLineDistance 는 가상 수직선으로 폴백하지만 CircleCenterDistance, CompoundCenterBDistance, CompoundCenterCDistance, ArcEdgeDistance, ArcLineIntersectDistance 는 0.0(가로 방향)을 X 기준으로 써서 틀린 값을 낸다. 현재 SIDE 레시피의 X축 측정은 0건.
    3) 패턴 false match 교차검증 상실(A-76-E1 전문).
    4) 표시 해석(A-76-E2 전문) — 옵션 ON 으로 티칭하면 세로 검출선이 안 보이므로 옵션을 잠시 끄고 확인한다.
    5) 오프라인 이미지 자동 채움은 Datum OK 이면 흐린 세로 이미지도 저장한다(의도 — D-76-03, 초점 분석용).
    6) Phase 75 의 SEAT 기록(AlignVerify CSV) 검출Col 은 옵션 ON Datum 에서 패턴매칭으로 옮긴 티칭 원점 열이다(세로선 교점이 아니다).

(5) 앱(DatumMeasurement.exe)이 실행 중이 아니면 기본 출력으로 Debug|x64 빌드를 한 번 더 해서 사용자가 이 PC 에서 바로 SIMUL 확인을 할 수 있게 한다. 실행 중이면 종료하지 않고, SUMMARY 와 체크포인트 안내에 '앱 종료 후 빌드 필요' 로 적는다.

(6) 커밋: `git add WPF_Example/VersionDefine.cs .planning/phases/76-side-datum/76-HUMAN-UAT.md` 두 파일만(전체 추가 금지, csproj 금지). 메시지 예: docs(76-03): 통합 빌드·회귀 0 증거 + 버전 + SIDE PC UAT 절차서.

CLAUDE.md 하드룰(새 줄 전부): 조건 연산자 금지 — 삼항 대신 if/else, null 병합·null 조건 연산자 대신 명시적 null 비교. C# 8 switch 식 금지. C# 7.2 문법만. 중괄호 필수. 매직넘버 금지. 날짜와 이니셜을 붙인 서명 주석 신규 금지(단 [Version(Date=...)] 필드는 형식상 필수라 해당 없음). 새 줄에 물음표 문자를 쓰지 않는다. 새 .cs 파일 금지, 운영 레시피 D:\Data\Recipe 쓰기 금지.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
BASE=$(git log --format=%H -1 -- .planning/phases/76-side-datum/76-01-PLAN.md)
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
SCR="$(cygpath -m "$TEMP")/p76build"
"$MSB" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -p:OutDir="$SCR/final-on/" -p:IntermediateOutputPath="$SCR/obj-final-on/" -v:minimal -nologo > "$SCR-final-on.log" 2>&1; echo "on_exit=$?"
"$MSB" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -p:DefineConstants=TRACE%3BDEBUG -p:OutDir="$SCR/final-off/" -p:IntermediateOutputPath="$SCR/obj-final-off/" -v:minimal -nologo > "$SCR-final-off.log" 2>&1; echo "off_exit=$?"
echo "on_errors=$(grep -cE ': error [A-Z]+[0-9]+' "$SCR-final-on.log") off_errors=$(grep -cE ': error [A-Z]+[0-9]+' "$SCR-final-off.log")"
echo "on_warn=$(grep -oE 'warning CS[0-9]+' "$SCR-final-on.log" | sort -u | tr '\n' ' ') off_warn=$(grep -oE 'warning CS[0-9]+' "$SCR-final-off.log" | sort -u | tr '\n' ' ')"
FILES="WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs WPF_Example/Halcon/Algorithms/DatumFindingService.cs WPF_Example/Halcon/Display/HalconDisplayService.cs WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs"
VD=WPF_Example/VersionDefine.cs
for F in $FILES $VD; do
  ADD=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+')
  echo "$F deleted=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk')"
done
echo "vd_const_deleted=$(git diff -U0 "$BASE" -- $VD | grep -E '^-' | grep -vE '^---' | grep -cE 'public const string (VERSION|BUILD_DATE)')"
echo "changed_count=$(git diff --name-only "$BASE" -- WPF_Example | wc -l)"
echo "changed=$(git diff --name-only "$BASE" -- WPF_Example | sort | tr '\n' ' ')"
echo "af_hunks=$(git diff -U0 "$BASE" -- WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs | grep -c '^@@')"
echo "rule_uses=$(for F in $FILES; do tr -d '\r' < "$F" | grep -vE '^\s*//' | grep -c 'IsHorizontalOnlyActive()'; done | awk '{s+=$1} END{print s}')"
echo "csproj_commits=$(git log --format=%H "$BASE"..HEAD -- WPF_Example/DatumMeasurement.csproj | wc -l)"
NB=$(git show "$BASE":$VD | grep -c 'Number = "')
NA=$(grep -c 'Number = "' $VD)
NEWV=$(tr -d '\r' < $VD | grep -oE 'public const string VERSION = "[0-9.]+"' | grep -oE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+')
OLDV=$(git show "$BASE":$VD | tr -d '\r' | grep -oE 'public const string VERSION = "[0-9.]+"' | grep -oE '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+')
echo "entries_before=$NB entries_after=$NA old_version=$OLDV new_version=$NEWV entry_match=$(grep -c "Number = \"$NEWV\"" $VD) old_entry_kept=$(grep -c "Number = \"$OLDV\"" $VD) entry_mentions=$(grep -c 'IsVerticalLineDisabled' $VD)"
U=.planning/phases/76-side-datum/76-HUMAN-UAT.md
test -f $U && echo "uat_items=$(grep -cE '^- \[.\] U-[1-8]\. ' $U) uat_pending=$(grep -cE '^- \[.\] U-[1-8]\. .*PENDING' $U) uat_limits=$(grep -c '알려진 한계' $U) uat_e1=$(grep -c 'A-76-E1' $U) uat_e2=$(grep -c 'A-76-E2' $U)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `on_exit=0`, `off_exit=0`, `on_errors=0`, `off_errors=0`, `on_warn` 과 `off_warn` 모두 `warning CS0169 warning CS0618` 를 벗어나지 않는다
    - 6개 동작 파일 모두 `deleted=0` (git diff -w — SDV-04)
    - VersionDefine.cs 는 `deleted` 가 1 또는 2 이고 `vd_const_deleted` 와 같다(지워진 줄은 VERSION / BUILD_DATE 상수 줄뿐)
    - 7개 파일 모두 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0` (추가 줄 한정: `\?[^?]*:`, `??`, `?.`, `switch.*=>`, `hbk`)
    - `changed_count=7` 이고 `changed=` 가 6개 동작 파일 + `WPF_Example/VersionDefine.cs` (TcpServer, InspectionSequence.cs, CycleResultSerializer.cs, MainView.xaml.cs, FAIEdgeMeasurementService.cs, 측정 클래스, csproj 없음 — SDV-04 ordering)
    - `af_hunks=1` (D-76-03 — 세로 이미지 촬영·저장 코드 무변경)
    - `rule_uses=5` (IsHorizontalOnlyActive 선언 1 + 검출 1 + 표시 2 + 캡처 1 — TOP/BOTTOM 보호 규칙 단일화)
    - `csproj_commits=0`
    - `entries_after` = `entries_before` + 1, `new_version` 이 `old_version` 과 다르고, `entry_match=1`, `old_entry_kept=1`, `entry_mentions` >= 1
    - `uat_items=8`, `uat_pending=8`, `uat_limits` >= 1, `uat_e1` >= 1, `uat_e2` >= 1
  </acceptance_criteria>
  <done>두 구성 빌드와 phase 전체 회귀 0 이 증거로 남고, 새 버전 항목과 SIDE PC UAT 절차서(U-1 ~ U-8, 알려진 한계 6개)가 커밋되었다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SIDE PC 실기 확인 (사용자) — U-1 ~ U-8 결과 기록</name>
  <files>.planning/phases/76-side-datum/76-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/76-side-datum/76-HUMAN-UAT.md (Task 1 산출)
    - .planning/phases/76-side-datum/76-03-SUMMARY.md 가 아직 없으면 Task 1 의 빌드·회귀 증거 출력
  </read_first>
  <action>
체크포인트에서 실행자가 할 일:
1. 사용자에게 아래 how-to-verify 와 `76-HUMAN-UAT.md` 경로, 새 버전 번호, 앱 재빌드 필요 여부(Task 1 의 (5) 결과)를 보여 주고 **회신을 기다린다.** 실행자가 임의로 PASS 로 바꾸거나 스스로 승인하지 않는다.
2. 회신을 받으면 각 항목 줄의 PENDING 을 PASS / FAIL / PENDING 중 회신 내용으로 바꾸고, '결과:' 란에 사용자가 준 숫자·관찰을 적는다. 사용자가 나중에 하겠다고 한 항목은 PENDING 그대로 둔다.
3. FAIL 항목이 있으면 어떤 결정(D-76-01 ~ D-76-07)이나 해석(A-76-E1, A-76-E2)과 관련되는지 한 줄씩 적는다. 코드 수정은 이 태스크에서 하지 않는다(별도 후속 작업으로 사용자가 결정).
4. 커밋: `git add .planning/phases/76-side-datum/76-HUMAN-UAT.md` 한 파일만. 메시지 예: docs(76-03): SIDE PC 실기 UAT 결과 기록.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
U=.planning/phases/76-side-datum/76-HUMAN-UAT.md
test -f $U && echo "items=$(grep -cE '^- \[.\] U-[1-8]\. ' $U) states=$(grep -cE '^- \[.\] U-[1-8]\. .*(PASS|FAIL|PENDING)' $U)"
    </automated>
  </verify>
  <what-built>
SIDE Datum 별 '세로선 끄기' 옵션(IsVerticalLineDisabled). 켜면 세로 이미지는 그대로 찍고 저장하지만 세로선 검출은 하지 않고, 가로 결합선과 패턴매칭만으로 기준점을 만든다
(원점 X = 패턴매칭으로 옮긴 티칭 원점 열, 원점 Y·각도 = 가로선). 자동 사이클과 수동 Test Find 가 같은 규칙이다. 화면과 저장 캡처에는 가로 요소만 그린다.
옵션을 끈 Datum 과 TOP/BOTTOM 은 기존 코드를 한 줄도 지우지 않은 채 그대로 동작한다(두 빌드 구성 에러 0, 6개 파일 git diff -w 삭제 0).
  </what-built>
  <how-to-verify>
`76-HUMAN-UAT.md` 의 U-1 ~ U-8 을 순서대로 확인한다. 이 PC(SIMUL)에서 먼저 할 수 있는 것: U-1, U-3 의 Test Find 부분, U-6 의 Test Find·Datum 선택 화면, U-8.
SIDE PC 에서 해야 하는 것: U-2(옵션 OFF 기준값) → U-3(같은 부품 옵션 ON 비교, 로그) → U-4(세로 초점 일부러 흐리게) → U-5(좌우로 밀어 재안착) → U-6(저장 캡처 JPEG) → U-7(TOP/BOTTOM 과 z/PLC 회귀).
로그 확인 위치: D:\Data\Algorithm 의 오늘 로그에서 '[Datum.Vertical] skipped', '[Datum.HorizontalOnly] ok' / 'fail'.
레시피는 복사본에서 먼저 확인하고, 운영 레시피 저장은 확인 후 직접 한다.
  </how-to-verify>
  <acceptance_criteria>
    - `items=8` 이고 `states=8` — 여덟 항목 모두 PASS / FAIL / PENDING 중 하나가 기록됨
    - 사용자가 '승인' 또는 항목별 문제를 회신하기 전에는 다음 단계로 가지 않는다
    - 실행자는 어떤 항목도 스스로 PASS 로 바꾸지 않는다(회신에 근거한 값만 기록)
  </acceptance_criteria>
  <resume-signal>'승인' 이라고 답하거나, 문제가 있으면 어느 항목(U-1 ~ U-8)이 어떻게 달랐는지 알려 주세요. 나중에 할 항목은 '보류: U-n' 으로 알려 주세요.</resume-signal>
  <done>U-1 ~ U-8 결과가 76-HUMAN-UAT.md 에 기록되고 사용자 회신을 받았다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 개발 PC 저장소 → SIDE PC 배포 빌드 | 의도하지 않은 파일(csproj 등)이 섞이면 현장 빌드가 달라진다 |
| 실행자 → 사용자 승인 | 실기 확인은 사람만 할 수 있다 |
| 개발 PC → 운영 레시피 D:\Data\Recipe | Claude 는 읽기만 한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-76-10 | Tampering | phase 커밋 범위 — csproj·무관 파일 스테이징, 기존 줄 변경 | high | mitigate | 파일 지정 git add 만, `changed_count=7` 정확 일치, `csproj_commits=0`, 6개 파일 git diff -w 삭제 0 (Task 1) |
| T-76-11 | Repudiation | 실기 UAT 를 실행자가 스스로 승인 | medium | mitigate | blocking 체크포인트, 회신 전 진행 금지, 항목별 결과는 회신 근거만 기록 |
| T-76-12 | Tampering | 운영 레시피 D:\Data\Recipe 덮어쓰기 | medium | mitigate | 이 plan 은 레시피를 쓰지 않는다. UAT 절차서가 복사본 확인을 권하고 운영 레시피 저장은 사용자가 직접 |
</threat_model>

<verification>
1. SIMUL-ON / SIMUL-OFF 통합 빌드 에러 0, 새 경고 코드 0
2. 6개 동작 파일 git diff -w 삭제 0, 변경 파일 목록 정확히 7개, Action_FAIMeasurement 변경 hunk 1개, 옵션 규칙 사용처 5
3. 추가 줄 하드룰 grep 5종 0, csproj 커밋 0
4. VersionDefine 항목 +1, VERSION 과 Number 일치, 이전 항목 보존
5. 76-HUMAN-UAT.md U-1 ~ U-8 + 알려진 한계 6개, 사용자 회신 기록
</verification>

<success_criteria>
- phase 전체가 두 구성으로 빌드되고 옵션 OFF / TOP / BOTTOM 경로가 코드상 무변경임이 diff 로 증명된다
- SIDE PC 에서 확인할 절차와 한계가 문서로 남고, 사용자 확인 결과가 기록된다
</success_criteria>

<output>
완료 후 `.planning/phases/76-side-datum/76-03-SUMMARY.md` 를 작성한다. 반드시 포함:
- 두 구성 빌드 결과(에러 수, 경고 코드 집합), 7개 파일의 git diff -w 삭제 수, 변경 파일 목록, rule_uses 값
- 새 버전 번호와 changelog 요약
- 사용자 UAT 회신(항목별 PASS / FAIL / PENDING)과 FAIL 항목의 관련 결정·해석
- PENDING 으로 남은 항목과, A-76-E1 / A-76-E2 에 대한 사용자 판단
</output>
