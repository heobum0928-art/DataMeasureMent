---
phase: 77-side-z-focus-select
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
  - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
  - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/SkipReason.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: true
requirements: [SZF-01, SZF-02, SZF-03, SZF-05]
user_setup: []

must_haves:
  truths:
    - "Shot 속성창 Shot|Identity 에 'Z 범위 끝'(ZIndexEnd) 칸 1개만 추가되고 시작 번호 칸은 없다 — 범위 = 기존 ZIndex ~ ZIndexEnd, 기준 Z = ZIndex (SZF-01, D-77-03, D-77-07 ①⑧)"
    - "[edge SZF-01 empty] ZIndexEnd 키가 없는 옛 레시피는 0 으로 로드되어 IsZRangeEnabled()=false — 새 분기(저장·대기·선택·완성 index·마지막 index·빈 z 경고 억제)가 전부 미도달이라 기존 동작과 같다 (SZF-05, O-2)"
    - "PLC(Sender 있는 $TEST) 사이클에서 범위 Shot 은 ZIndex..ZIndexEnd 의 z 마다 실행되어 그 z 사진을 m_dicZRangeImages 에 Shot명|z 키로 모은다. 범위 밖 z 는 저장하지 않는다 (SZF-02, D-77-01)"
    - "범위 Shot 의 측정은 ZIndexEnd 가 아닌 z 에서는 실행하지 않고 Z_RANGE_PENDING 으로 표시되어 PLC 응답에 담기지 않으며(완성 index = ZIndexEnd), ZIndexEnd 에서 한 번 평가되어 응답에 담긴다. 이 시퀀스의 마지막 Index 는 ZIndexEnd 까지 포함한다 (SZF-02, O-4)"
    - "ZIndexEnd tick 에서 EdgeToLineDistance 측정은 모인 후보 사진마다 실제로 측정을 돌리고, 점수 = Σ(strip 안 최대 |amp|) ÷ strip 수(에지 없는 strip = 0) 가 가장 높은 z 의 측정값·오버레이를 그대로 채택한다 — 재계산 없음 (SZF-03, D-77-02, D-77-04, D-77-05)"
    - "[edge SZF-03 empty] 후보 사진 측정이 전부 실패하면 그 측정은 실패(MEASURE_FAIL, NG)로 기록되고 선택 Z 는 첫 후보(ZIndex 쪽) z 이다. 후보 사진이 하나도 없으면(촬영 실패) 기존 단일 사진 경로로 측정한다 — PASS 로 비워 두지 않는다"
    - "[edge SZF-03 ordering] 후보는 PLC 도착 순서와 무관하게 z 오름차순으로 평가하고, 점수가 같으면 먼저(작은 z) 평가된 후보를 유지한다(엄격한 초과 비교)"
    - "[edge SZF-03 precision] 점수는 double 원값으로 비교하고 반올림하지 않는다 — 로그의 F2 표기는 표시 전용이다"
    - "선택 결과는 Algorithm 로그 '[ZFocus]' 한 줄(측정별 후보 z·점수·선택 z)과 '[FitLine] edge-strength' 줄(strip 별 최대 |amp|·합·분모)로 남는다 (SZF-03, VALIDATION Wave 0)"
    - "평가가 끝난 후보 사진은 그 tick 의 측정 직후 Dispose 되고, 사이클 시작(BeginCrossZImageCycle)·배치 정리·$RESET 에서도 비워진다 (O-5)"
    - "[edge SZF-05 ordering] TOP/BOTTOM 과 범위 끈 Shot 의 z 라우팅·응답 순서는 기존과 같다 — 새 판정은 모두 shot.IsZRangeEnabled() 가드 뒤에 있고, 기존 TryFitLine 호출부(EdgeScore 미설정)는 점수 계산을 하지 않는다"
    - "수동 RUN·수동 트리거(Sender 없음)·오프라인은 이 plan 에서 기존 단일 사진 경로를 그대로 탄다(선택 없음). 안내 문구·오프라인 선택은 77-04 가 더한다 (D-77-06)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs"
      provides: "ZIndexEnd 프로퍼티 + IsZRangeEnabled() + Z_RANGE_OFF/MIN_Z_RANGE_BASE_INDEX/MAX_Z_RANGE_COUNT"
      contains: "public bool IsZRangeEnabled()"
    - path: "WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs"
      provides: "EdgeStrengthScore 클래스 + EdgeScore opt-in 프로퍼티 + strip 최대 |amp| 누적"
      contains: "public class EdgeStrengthScore"
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "m_dicZRangeImages 저장소 + DoesShotOwnZRangeIndex + 완성/마지막 index 확장"
      contains: "public bool DoesShotOwnZRangeIndex(ShotConfig shot, int nZIndex)"
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "EZRangeMode 판정 + 후보 저장 + 대기 표시 + 후보별 실행·선택"
      contains: "private bool TryHandleZRangeMeasurement("
  key_links:
    - from: "Action_FAIMeasurement.RunGrab"
      to: "InspectionSequence.StoreZRangeImage"
      via: "SetImage 다음 줄에 삽입한 StoreZRangeCandidateImage(image) — AutoPending/AutoCompletion 이고 DoesShotOwnZRangeIndex 일 때만"
      pattern: "StoreZRangeCandidateImage\\(image\\)"
    - from: "Action_FAIMeasurement.ProcessOneMeasurement"
      to: "Action_FAIMeasurement.TryHandleZRangeMeasurement"
      via: "InjectDatumOrigin 다음 줄 삽입 — true 면 return, false 면 기존 경로"
      pattern: "if \\(TryHandleZRangeMeasurement\\("
    - from: "EdgeToLineDistanceMeasurement.TryExecute"
      to: "VisionAlgorithmService.EdgeScore"
      via: "svc.EdgeScore = edgeScore 후 TryFitLine, 성공 시 LastFitScore = edgeScore.Average"
      pattern: "svc.EdgeScore = edgeScore;"
    - from: "InspectionSequence.AddFaiResult (GetMeasurementCompletionZIndex)"
      to: "ShotConfig.ZIndexEnd"
      via: "범위 Shot 의 비-크로스-Z 측정 완성 index = ZIndexEnd"
      pattern: "return shot.ZIndexEnd;"
  prohibitions:
    - statement: "MUST NOT 범위 Shot 의 측정을 실제로 재지 않은 채 PLC 응답에 P 로 보고하지 않는다 — 중간 z 대기 측정은 응답 대상에서 빠지고, ZIndexEnd 에서 후보가 전부 실패하면 NG 로 보고한다"
      status: resolved
      verification: judgment
    - statement: "MUST NOT 에지 강도 점수를 합부 판정 기준으로 쓰지 않는다 — 점수는 사진 선택에만 쓰고 판정은 채택된 측정값의 공차 비교(EvaluateJudgement) 그대로다"
      status: resolved
      verification: judgment
---

<objective>
Tracer 한 줄: SIDE 범위 Shot 1개(ZIndex~ZIndexEnd) × EdgeToLineDistance 측정 1종을 PLC 자동 사이클 경로로 끝까지 관통시킨다 — 설정 칸(ShotConfig) → z 라우팅·저장소(InspectionSequence) → 촬영 누적·대기·선택(Action_FAIMeasurement) → 점수(VisionAlgorithmService) → 측정 모델(MeasurementBase/EdgeToLineDistance) → 응답 완성 index.

Purpose: 부품 높이 차이로 SIDE 측정 ROI 가 한 Z 에서 모두 초점이 맞지 않는다. 가장 위험한 설계 가정(여러 z tick 에 걸친 누적 → 마지막 z 에서 평가 → 응답 index 정합)을 첫 커밋에서 증명한다. 동점 규칙·겹침 제외·EdgeToLineAngle·편집 경고는 77-02, 기록·표시는 77-03, 수동/오프라인/재검사·후보 저장은 77-04 가 이 뼈대에 붙인다.

Output: 7개 기존 파일에 삽입 위주 변경(기존 줄 삭제 0). 신규 .cs 없음.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/77-side-z-focus-select/77-CONTEXT.md
@.planning/phases/77-side-z-focus-select/77-RESEARCH.md
@.planning/phases/77-side-z-focus-select/77-PATTERNS.md
@.planning/phases/77-side-z-focus-select/77-VALIDATION.md

<interfaces>
계획 시점 라인 번호(삽입 전 기준). 실행자는 이 계약을 그대로 쓴다.

ShotConfig.cs (K&R/혼합, `using PropertyTools.DataAnnotations;` 이미 있음)
- `:173-174` `[Category("Shot|Identity")] public string OwnerSequenceName`
- `:176-182` ZIndex 주석 + `public int ZIndex { get; set; } = 0;`
- `:39` `[DisplayName("...")]` 선례(PropertyTools), `:160-167` ShotName 세터의 RaisePropertyChanged(nameof(...)) 선례
- `:384-419` Load 오버라이드, `:424-486` CopyTo, `:587-592` ClearAllResults

VisionAlgorithmService.cs (Allman)
- `:20-31` TryFitLine 시그니처(마지막 optional `collectedEdges = null`) — 시그니처는 바꾸지 않는다
- `:127-130` stripCount 정규화(0 → 20), `:155-174` / `:178-197` strip 루프, 각 `AppendStrip(...)` 호출은 `ref allRows, ref allCols);` 로 끝남
- `:205-207` `[FitLine] strips ok` 로그
- `:298-317` FindStrongestEdgeIndex(HTuple) — 비었으면 -1
- `:319-374` AppendStrip: `:344-347` NoEdge return, `:357-359` Strongest Ok return, `:361-363` All/First/Last Ok return, `:365-368` catch Failed

MeasurementBase.cs (Allman)
- `:96-116` LastMeasuredValue/LastJudgement/LastHasResult/LastSkipReason 프로퍼티, `:116` `public string LastErrorMessage;` (필드 = INI·복사 제외 선례)
- `:127-133` abstract TryExecute, `:175-182` ClearResult

EdgeToLineDistanceMeasurement.cs
- `:104-108` out 초기화 + overlays 생성, `:117` `var svc = new VisionAlgorithmService();`, `:122-131` TryFitLine if 블록(실패 시 return false)

SkipReason.cs `:4` `public static class SkipReason`, `:16` `CROSS_Z_INCOMPLETE` 상수

InspectionSequence.cs (Allman)
- `:72-73` `_crossZImageLock`, `m_dicCrossZImages`
- `:831-859` ComputeLastZIndex (`:857` `nMax = System.Math.Max(nMax, MaxCrossZCompletionZIndex(recipeManager));`)
- `:1033-1069` FindActionIndicesByZIndex (`:1062` `bool bMatch = bOwnZIndexMatch || bCrossZMatch;`)
- `:1463-1525` Store/Take/HasCrossZImage, `:1529-1543` ClearCrossZImages
- `:1551-1554` BeginCrossZImageCycle, `:1560-1563` ClearCrossZImagesAfterBatchCycle, `:1571-1576` ResetCycleStateForProtocolReset
- `:1594-1609` GetExecutionZIndex / IsProtocolDrivenCycle (RequestPacket != null). `RequestPacket.Sender` (VisionRequestPacket.cs:59, string) — 수동 트리거는 Sender 가 비어 있다(Custom/SystemHandler.cs:78-82, :1347-1381)
- `:2112-2121` GetMeasurementCompletionZIndex (`:2120` `return shot.ZIndex;`)
- `:2217-2247` AggregateIndexFais (`:2235` `bool bInScope = bOwnedByThisSeq && (bZMatch || bCrossZCompletesHere);`)
- `:2318-2339` WarnIfEmptyScope (`:2322` `bool bSuppressWarning = ...;`)

Action_FAIMeasurement.cs (K&R, 신규 헬퍼 일부 Allman 혼재 — 인접 메서드 스타일 따름)
- `:164-168` RunInit, `:475-492` RunGrab (`:483` SetImage, `:485` image.Dispose())
- `:554-595` RunMeasure (`:574` MeasureShotFaiList 호출)
- `:677-703` ProcessOneMeasurement (`:688` InjectDatumOrigin, `:702` RecordMeasurementResult)
- `:823-853` RecordMeasurementResult (실패 시 ClearResult 호출), `:1882-1916` TryExecuteMeasurement
- `:1969-1974` CrossZCaptureTickResult (중첩 private 클래스·필드 선례), `SafeDisposeImage(HImage)` 기존 헬퍼
</interfaces>
</context>

## 계획 단계 결정 (phase 전체 — 77-02~06 도 이 표를 따른다)

| # | 질문 | 결정 | 근거 |
|---|---|---|---|
| P-1 | 입력 칸 | **ShotConfig.ZIndexEnd 1개**(표시명 "Z 범위 끝", 0=꺼짐). 범위 = ZIndex..ZIndexEnd, 기준 Z = ZIndex. 시작 번호 칸 없음 | D-77-07 ①⑧ (D-77-03 의 start/end 표현 대체), O-2 |
| P-2 | 켜짐 규칙 | IsZRangeEnabled() = ZIndexEnd ≠ 0 이고 ZIndex ≥ 1 이고 ZIndexEnd > ZIndex 이고 z 개수(ZIndexEnd-ZIndex+1) ≤ MAX_Z_RANGE_COUNT(10). 아니면 꺼짐(기존 동작) — 오설정 경고는 77-02 | D-77-07 ①, O-5(메모리: 127~152MB/장). 10 은 내부 const, reversible |
| P-3 | 점수 | strip 강도 = strip 안 최대 \|amp\| 1개(모든 EdgeSelection 공통 — First/Last 는 MeasurePos 가 1개만 주므로 그 에지). 점수 = Σ ÷ 실제 strip 수(EdgeSampleCount, 0 이면 TryFitLine 기본 20), 에지 없는/실패 strip = 0 | D-77-02, D-77-05 (리서치 A1 '평균' 안은 D-77-05 로 기각) |
| P-4 | 점수 전달 | TryFitLine 시그니처는 바꾸지 않는다. VisionAlgorithmService 에 opt-in 프로퍼티 EdgeScore(기본 null) — null 이면 계산·로그 0. AppendStrip 은 반환 계약 유지, 마지막 strip 강도를 private 필드에 남긴다 | 기존 15개 호출부 무변경(SZF-05), 삽입만으로 구현 |
| P-5 | 측정 결과 필드 | MeasurementBase 에 **public 필드** LastFitScore(double), LastSelectedZIndex(int, -1) — 프로퍼티가 아니라 필드라 INI 저장·붙여넣기 복사 대상이 아니다 | LastErrorMessage 필드 선례(MeasurementBase.cs:112-116). 레시피 INI 에 키가 새로 생기지 않음 |
| P-6 | 대기 표시 | 중간 z 에서 범위 측정 = SkipReason.Z_RANGE_PENDING(신규), LastJudgement=false, FaiAllPass=false. 응답에는 완성 index 게이트로 빠진다. 'tick 에서 처리됨' 판정 3곳은 CROSS_Z_INCOMPLETE 처럼 제외(77-03/77-04) | 크로스-Z 대기 선례(Action_FAIMeasurement.cs:902-918). 미측정을 PASS 로 방치하지 않음 |
| P-7 | 평가 시점 | ZIndexEnd tick 1회(O-4). 후보는 그 시점에 저장소에서 소유권째 꺼내(추가 복사 없음) 측정 직후 Dispose(O-5) | 크로스-Z 완성 판정 선례 |
| P-8 | 동점 | 최고 점수와 기준 Z(ZIndex) 점수 차가 **3%(내부 const)** 이내면 기준 Z. 후보마다 결과를 보관하므로 기준 Z 자신의 측정값·오버레이를 그대로 채택(리서치 Open Question 2 / A2 해소). 구현은 77-02 | D-77-07 ③, O-3 |
| P-9 | 겹침 | 범위 안이라도 다른 Shot 의 ZIndex·크로스-Z 측정 z·Datum(ZIndexA/B·기준점 z)·다른 범위 Shot 의 범위에 속한 z 는 이 Shot 후보가 아니다(라우팅·저장 모두 제외) + 편집 경고. 구현은 77-02 | D-77-07 ⑤. CONTEXT 예시(z3,z5,z6,z7 / z4=F9)가 그대로 동작. reversible |
| P-10 | 수동·오프라인·재검사 | 라이브 수동(RUN, 수동 트리거)=1장·선택 없음+안내. 오프라인(OfflineInspectMode, SIMUL 파일 로드)·재검사=z 별 저장 사진으로 같은 선택 로직, 없으면 1장+경고. 이 plan 에서는 전부 기존 경로(Off). 구현은 77-04 | D-77-06, D-77-07 ⑦ |
| P-11 | 표시·기록 | 결과 그리드·리뷰어·CSV 에 선택 Z 한 칸("z5"), 오버레이 라벨에 "z5". 점수는 Algorithm 로그에만. 화면 사진은 ZIndexEnd tick 에서 기준 Z 사진으로 교체(77-02) | D-77-07 ⑥, O-7 |
| P-12 | 미지원 측정 | 범위 Shot 의 EdgeToLine* 외 타입(크로스-Z 제외)은 기준 Z 사진으로 1회 측정 + 로그 1줄(77-02). 크로스-Z 측정은 기존 ZIndexA/B 규칙 그대로 | D-77-07 ② |
| P-13 | 완성 z 중복·순서 뒤바뀜 | 상태 없이 처리 — ZIndexEnd 가 다시 오면 남은 후보로 재평가, 늦게 온 중간 z 는 다음 사이클 시작에 비움. flagged assumption A-77-E1 로 UAT 확인 | 리서치 Pitfall 3 / A3 |

## Spec-less probe 원장 (edge 19건 / prohibition 5건 — phase 전체)

| 항목 | 처리 | 위치 |
|---|---|---|
| SZF-01 empty | covered → truth | 77-01 |
| SZF-01 adjacency / ordering | covered → truth | 77-02 |
| SZF-02 empty | covered → truth(후보 0 → 기존 1장 경로) | 77-01, 77-04(오프라인 0장) |
| SZF-02 adjacency | covered → truth(겹침 제외) | 77-02 |
| SZF-02 ordering | **backstop** + flagged assumption A-77-E1 | 77-04 |
| SZF-03 empty / ordering / precision | covered → truth | 77-01 |
| SZF-03 boundary / adjacency | covered → truth(동점 3% 경계, 동점 후보) | 77-02 |
| SZF-04 boundary / adjacency / empty / ordering / precision | covered → truth | 77-03 |
| SZF-05 empty / ordering | covered → truth | 77-01 |
| SZF-05 adjacency | covered → truth + **backstop**(실측값 동일) | 77-05 |
| PR-1 미측정 P 금지 / PR-2 점수 판정 사용 금지 | prohibitions (descriptor 없음) | 77-01 |
| PR-3 남의 z 사진 후보 사용 금지 | prohibition | 77-02 |
| PR-4 설정 없이 후보 사진 저장 금지 / PR-5 수동·오프라인 무안내 금지 | prohibitions | 77-04 |
| (canon) 파일명 경로 조작 | breadcrumb — /gsd-secure-phase 소관, 기존 SanitizeFilePart 사용. 여기서 prohibition 으로 만들지 않음 | — |

### Flagged assumption A-77-E1 (SZF-02 ordering — 자동 해소 금지)
PLC 가 ZIndexEnd 를 두 번 보내면 첫 평가 후 후보가 이미 Dispose 되어 두 번째 평가는 ZIndexEnd 사진 1장으로 다시 선택하고 결과·CSV 행이 한 번 더 생긴다. ZIndexEnd 뒤에 늦게 온 중간 z 사진은 평가되지 않고 다음 사이클 시작에 비워진다. O-5(즉시 해제)를 우선한 결과이며 77-06 UAT U-4 에서 사용자 확인.

### Flagged assumption A-77-O1 / O8 / O9
O-1(PLC z 번호 배정) 미결 → SIDE 실기 UAT 는 77-06 체크포인트에서 협의 후. O-8 사이클 타임, O-9 Z 이동 중 XY 흔들림은 실측 전 미지수 — 로그의 측정 시간(ms)과 UAT 로 확인.

## Artifacts this phase produces (phase 전체 원장)

| 심볼 / 산출물 | 종류 | 파일 | 만드는 곳 |
|---|---|---|---|
| `ZIndexEnd` (INI 키 `ZIndexEnd`, 표시명 "Z 범위 끝"), `Z_RANGE_OFF`, `MIN_Z_RANGE_BASE_INDEX`, `MAX_Z_RANGE_COUNT`, `IsZRangeEnabled()` | 프로퍼티/const/메서드 | ShotConfig.cs | 77-01 |
| `IsZRangeMisconfigured()`, `BuildZRangeMisconfigText()`, `WarnZIndexEndChanged()`, `_suppressUserEditWarning` | 메서드/필드 | ShotConfig.cs | 77-02 |
| `RerunZRangeImagePaths` (public 필드 Dictionary<int,string>) | 런타임 필드 | ShotConfig.cs | 77-04 |
| `EdgeStrengthScore` (Reset/AddStrip/StripCount/SumMaxAbsAmp/Average/BuildStripListText), `EdgeScore`, `_dLastStripMaxAbsAmp`, `ComputeStripMaxAbsAmp` | 클래스/프로퍼티/필드/메서드 | VisionAlgorithmService.cs | 77-01 |
| 로그 `[FitLine] edge-strength` | ELogType.Algorithm | VisionAlgorithmService.cs | 77-01 |
| `LastFitScore`, `LastSelectedZIndex` (public 필드), `SELECTED_Z_NONE`, `SELECTED_Z_PREFIX`, `SupportsEdgeStrengthScore()`, `FormatSelectedZ(int)`, `ParseSelectedZ(string)` | 필드/const/메서드 | MeasurementBase.cs | 77-01 |
| `SupportsEdgeStrengthScore()` override | 메서드 | EdgeToLineDistanceMeasurement.cs (77-01), EdgeToLineAngleMeasurement.cs (77-02) | 77-01/02 |
| `SkipReason.Z_RANGE_PENDING` | const | SkipReason.cs | 77-01 |
| `m_dicZRangeImages`, `ZRANGE_KEY_SEPARATOR`, `BuildZRangeImageKey`, `StoreZRangeImage`, `TakeZRangeImageCopy`, `HasZRangeImage`, `TakeZRangeImages`, `ClearZRangeImages`, `DoesShotOwnZRangeIndex`, `BuildZRangeCandidateIndices`, `IsZIndexUsedByZRangeCapture`, `ShotHasZRangeCompletingAt`, `MaxZRangeCompletionZIndex`, `IsManualTriggerCycle` | 필드/const/메서드 | InspectionSequence.cs | 77-01 |
| `IsZIndexReservedOutsideZRange`, `BuildZRangeConflictText`, FindShotByZIndex 3패스 로그 `[PREP ZRange]` | 메서드/로그 | InspectionSequence.cs | 77-02 |
| `_tickZRangeImages`, `RecordTickZRangeImage`, `TakeTickZRangeImagesSnapshot` | 필드/메서드 | InspectionSequence.cs | 77-04 |
| `EZRangeMode`, `ZFocusRunResult`, `ZFOCUS_LOG_TAG`, `ZFOCUS_SCORE_FORMAT`, `_lstZRangeCandidates`, `_bZRangeCandidatesLoaded`, `ResolveZRangeMode`, `StoreZRangeCandidateImage`, `TryHandleZRangeMeasurement`, `MarkMeasurementZRangePending`, `EnsureZRangeCandidatesLoaded`, `ReleaseZRangeCandidates`, `ExecuteZRangeSelection`, `RunZFocusCandidates`, `PickZFocusResult`, `LogZFocusSelection` | enum/클래스/const/필드/메서드 | Action_FAIMeasurement.cs | 77-01 |
| 로그 `[ZFocus]` (선택·대기) | ELogType.Algorithm | Action_FAIMeasurement.cs | 77-01 |
| `Z_FOCUS_TIE_PERCENT`(3.0), `PERCENT_SCALE`, `FindZFocusResultByZ`, `ExecuteZRangeBaseImageMeasurement`, `ApplyZRangeBaseImageForDisplay`, `_nZRangeDisplayZIndex`, `LogZRangeMisconfigIfNeeded`, `_bZRangeMisconfigLogged` | const/메서드/필드 | Action_FAIMeasurement.cs | 77-02 |
| `LogZRangeManualNotice`, `_bZRangeNoticeLogged`, `LoadOfflineZRangeCandidates`, `SaveZRangeCandidateImageIfEnabled`, `AutoFillZRangeOfflineImage`, `ApplySelectedZLabel` | 메서드/필드 | Action_FAIMeasurement.cs | 77-04 |
| `MeasurementResultDto.SelectedZIndex` (JSON, 기본 -1) | 프로퍼티 | CycleResultDto.cs | 77-03 |
| `CycleResultDto.ZRangeImages`, `ZRangeImageRecordDto`(ShotName/ZIndex/Path) | 프로퍼티/클래스 | CycleResultDto.cs | 77-04 |
| CSV 컬럼 `선택Z` (값 `z5` / 빈칸), `MapSelectedZ`, `COL_SELECTED_Z`(15), `ParseSelectedZIndex` | CSV/메서드/const | MeasurementHistoryCsvWriter.cs / Loader.cs | 77-03 |
| `SelectedZText` | VM 프로퍼티 | MeasurementResultRow.cs, ReviewMeasurementRow.cs | 77-03 |
| DataGrid 열 "선택Z" | XAML | MainView.xaml, ReviewerWindow.xaml | 77-03 |
| `EdgeInspectionOverlay.SelectedZLabel` + 라벨 출력 | 프로퍼티/표시 | EdgeInspectionOverlay.cs, HalconDisplayService.cs | 77-03 |
| `SaveZRangeCandidateImages` (체크박스, INI 키, 기본 false) | 설정 | Setting/SystemSetting.cs | 77-04 |
| `ZRANGE_CANDIDATE_PREFIX`, `BuildZRangeCandidateFileName` | const/메서드 | CaptureImageSaveService.cs | 77-04 |
| `OFFLINE_SUFFIX_Z` (`_z`) → 오프라인 파일 `shot_<ShotName>_z<N>.bmp` | const/파일 규약 | RecipeFileHelper.cs | 77-04 |
| `SavedCycleRerunPart.ZRangePhotoPaths`, `FillPartZRangePhotos` | 프로퍼티/메서드 | RepeatRunService.cs | 77-04 |
| VersionDefine 1.7.47.0 항목 | changelog | VersionDefine.cs | 77-05 |
| `77-HUMAN-UAT.md` | UAT 절차서 | phase 디렉터리 | 77-06 |

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): 범위 Shot 1개 × EdgeToLineDistance — z 마다 사진 누적, ZIndexEnd 에서 후보별 측정·점수 선택, 완성 index 응답까지 한 경로</name>
  <precondition>`git status --porcelain -- WPF_Example/Custom/Sequence/Inspection WPF_Example/Halcon/Algorithms` 출력이 비어 있다(다른 작업의 미커밋 변경이 BASE 비교에 섞이지 않는다)</precondition>
  <reversibility rating="costly">INI 키 이름 ZIndexEnd 는 사용자가 값을 넣고 레시피를 저장하는 순간부터 main.ini Shot 섹션에 기록된다 — 나중에 이름을 바꾸면 켜 둔 범위가 조용히 꺼진다(키 부재 = 0 = 기존 동작). 데이터 마이그레이션은 없다.</reversibility>
  <files>WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs, WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs, WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs, WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/SkipReason.cs, WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs, WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <read_first>
    - CLAUDE.md (Code Style — 가독성 하드룰 전체, 위반 = 회귀)
    - .planning/phases/77-side-z-focus-select/77-CONTEXT.md (D-77-01 ~ D-77-07 전부)
    - 이 PLAN 의 '계획 단계 결정' P-1 ~ P-7, P-10 과 interfaces 블록
    - .planning/phases/77-side-z-focus-select/77-PATTERNS.md 의 'CLAUDE.md 가독성 규칙 위반 주의' 표 (analog 의 삼항·&& 3개 줄을 복사하지 않는다)
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs:1-40, :155-183, :384-419
    - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs:1-260, :283-374
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs:90-233
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs:90-135
    - WPF_Example/Custom/Sequence/Inspection/SkipReason.cs 전체
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs:68-74, :823-1069, :1460-1609, :2109-2339
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs:28-60, :140-168, :473-492, :551-703, :819-918, :1880-2054
    - WPF_Example/Custom/SystemHandler.cs:360-445, :1347-1381 (읽기만 — $TEST 라우팅과 수동 트리거 Sender 공백)
  </read_first>
  <action>
모든 변경은 **기존 줄 사이에 새 줄을 끼워 넣는 방식**이다. 기존 줄은 한 글자도 고치거나 지우지 않는다(SZF-05, `git diff -w` 삭제 0). 범위 기능이 꺼진 Shot(ZIndexEnd=0, 키 없는 옛 레시피)·TOP/BOTTOM 은 새 코드에서 가장 먼저 false/Off 로 빠져야 한다.

(1) ShotConfig.cs — `public int ZIndex { get; set; } = 0;`(:182) 바로 아래에 삽입(D-77-07 ①⑧, D-77-03, O-2):
- `public const int Z_RANGE_OFF = 0;`, `public const int MIN_Z_RANGE_BASE_INDEX = 1;`, `public const int MAX_Z_RANGE_COUNT = 10;` (각 한 줄 이유 주석: 꺼짐 표식 / 기준점 0 번은 범위 시작 불가 / 127~152MB 사진 누적 상한).
- `private int _zIndexEnd = Z_RANGE_OFF;` 와 프로퍼티 `ZIndexEnd`. 어트리뷰트 3개: `[Category("Shot|Identity")]`, `[DisplayName("Z 범위 끝")]`(이 파일 :39 의 PropertyTools DisplayName 과 같은 것), `[System.ComponentModel.Description("PLC 가 ZIndex~Z 범위 끝 번호로 차례로 촬영해야 동작")]` — 설명 문구는 정확히 이 한 줄. getter 는 필드 반환, setter 는 같은 값이면 return, 아니면 대입 후 `RaisePropertyChanged(nameof(ZIndexEnd));`. 편집 경고는 77-02 가 setter 에 더한다.
- 시작 번호 프로퍼티는 만들지 않는다(D-77-07 ①).
- `public bool IsZRangeEnabled()` 메서드(프로퍼티 금지 — ParamBase 리플렉션이 공개 프로퍼티를 INI 에 쓴다). 가드 절만으로: ZIndexEnd 가 Z_RANGE_OFF 면 false → ZIndex 가 MIN_Z_RANGE_BASE_INDEX 미만이면 false → ZIndexEnd 가 ZIndex 이하면 false → `int nZCount = ZIndexEnd - ZIndex + 1;` 이 MAX_Z_RANGE_COUNT 초과면 false → true. 조건을 && 로 한 줄에 잇지 않는다.

(2) SkipReason.cs — CROSS_Z_INCOMPLETE 상수 아래에 `public const string Z_RANGE_PENDING = "Z_RANGE_PENDING";` (주석: Z 범위 Shot 의 중간 z tick — 후보 사진을 모으는 중이라 아직 측정 안 함, 고장 아님, PASS 아님).

(3) MeasurementBase.cs (P-5):
- `public string LastErrorMessage;`(:116) 아래: `public const int SELECTED_Z_NONE = -1;`, `public const string SELECTED_Z_PREFIX = "z";`, `private const int MIN_SELECTED_Z_INDEX = 1;`, 필드 `public double LastFitScore;`, `public int LastSelectedZIndex = SELECTED_Z_NONE;` (필드인 이유 주석 한 줄: INI 저장·붙여넣기 복사 제외, LastErrorMessage 와 같은 이유).
- `ClearResult()` 의 `LastErrorMessage = null;` 다음 줄에 `LastFitScore = 0.0;` 과 `LastSelectedZIndex = SELECTED_Z_NONE;` 삽입.
- ClearResult 아래에 `public virtual bool SupportsEdgeStrengthScore()` — false 반환(에지 강도 점수로 Z 를 고를 수 있는 측정만 override).
- `public static string FormatSelectedZ(int nZIndex)` — nZIndex 가 MIN_SELECTED_Z_INDEX 미만이면 string.Empty, 아니면 SELECTED_Z_PREFIX + nZIndex.ToString(). `public static int ParseSelectedZ(string szText)` — null/빈 문자열·접두사 불일치·int.TryParse 실패·MIN 미만이면 SELECTED_Z_NONE, 아니면 숫자. 두 함수가 화면·CSV·로더의 단일 규칙이다(D-77-07 ⑥).

(4) VisionAlgorithmService.cs (P-3, P-4, D-77-02, D-77-05):
- 클래스 첫 멤버로 `public EdgeStrengthScore EdgeScore { get; set; } = null;`(XML 요약: 설정하면 TryFitLine 이 strip 별 최대 |amp| 를 모은다, null 이면 아무것도 안 함)와 `private double _dLastStripMaxAbsAmp = NO_EDGE_STRIP_AMP;`, `private const double NO_EDGE_STRIP_AMP = 0.0;` 삽입.
- TryFitLine: `if (stripCount < 1) stripCount = 1;` 다음 줄에 EdgeScore 가 null 이 아니면 `EdgeScore.Reset(stripCount);`. 두 strip 루프 각각에서 `AppendStrip(...)` 호출 문장(`ref allRows, ref allCols);` 로 끝나는 줄) 바로 다음 줄에 EdgeScore 가 null 이 아니면 `EdgeScore.AddStrip(_dLastStripMaxAbsAmp);`. `[FitLine] strips ok` 로그 문장 다음에 EdgeScore 가 null 이 아니면 ELogType.Algorithm 로 `[FitLine] edge-strength strips=[{0}] sum={1:F2} denom={2} score={3:F2}`(BuildStripListText, SumMaxAbsAmp, StripCount, Average). TryFitLine 시그니처는 그대로.
- AppendStrip: 본문 첫 문장으로 `_dLastStripMaxAbsAmp = NO_EDGE_STRIP_AMP;` 삽입(NoEdge·Failed 는 0 유지). `return EStripResult.Ok;` 두 곳 각각 바로 앞 줄에 `_dLastStripMaxAbsAmp = ComputeStripMaxAbsAmp(amp);` 삽입.
- FindStrongestEdgeIndex 아래에 `private static double ComputeStripMaxAbsAmp(HTuple amplitudes)` — FindStrongestEdgeIndex 결과가 0 미만이면 NO_EDGE_STRIP_AMP, 아니면 그 원소의 D 절댓값.
- VisionAlgorithmService 클래스 닫는 중괄호 다음(같은 namespace)에 `public class EdgeStrengthScore`(Allman): `private readonly List<double> _lstStripMaxAbsAmp = new List<double>();`, `private int _nStripCount;`, `private const string STRIP_TEXT_FORMAT = "F1";`, `private const string STRIP_TEXT_SEPARATOR = ",";`, `public void Reset(int nStripCount)`(목록 비우고 분모 저장), `public void AddStrip(double dStripMaxAbsAmp)`, `public int StripCount`(getter), `public double SumMaxAbsAmp`(getter — 목록 합), `public double Average`(getter — _nStripCount 가 0 보다 크면 합 ÷ _nStripCount, 아니면 0.0; if/else), `public string BuildStripListText()`(각 값을 STRIP_TEXT_FORMAT 로 만들어 구분자로 연결). 분모는 모은 strip 개수가 아니라 Reset 에 받은 strip 수다(에지 없는 strip 도 분모 포함, D-77-05).

(5) EdgeToLineDistanceMeasurement.cs — 생성자 아래에 `public override bool SupportsEdgeStrengthScore()` true 반환. TryExecute 안: `overlays = new List<EdgeInspectionOverlay>();` 다음 줄에 `LastFitScore = 0.0;`(모든 실패 경로 점수 0), `var svc = new VisionAlgorithmService();` 다음 줄에 `var edgeScore = new EdgeStrengthScore();` 와 `svc.EdgeScore = edgeScore;`, TryFitLine if 블록 닫는 중괄호 다음 줄에 `LastFitScore = edgeScore.Average;`.

(6) InspectionSequence.cs (SZF-02, O-4, O-5):
- `m_dicCrossZImages` 선언(:73) 아래에 `private readonly Dictionary<string, HImage> m_dicZRangeImages = new Dictionary<string, HImage>();` 와 `private const string ZRANGE_KEY_SEPARATOR = "|z";`(주석: 크로스-Z 저장소와 키 체계가 달라 별도 사전, 락은 _crossZImageLock 재사용 — 크로스-Z 사전에 섞지 않는다).
- HasCrossZImage(:1512-1525) 다음에 삽입: `private static string BuildZRangeImageKey(string szShotName, int nZIndex)`; `public void StoreZRangeImage(string szShotName, int nZIndex, HImage image)`(ShotName 비면 return, 락 안에서 기존 값 try-catch Dispose 후 image 가 null 이 아니면 CopyImage 저장 — StoreCrossZImage 와 같은 소유 규약); `public HImage TakeZRangeImageCopy(string szShotName, int nZIndex)`(사본 반환, 호출자 Dispose); `public bool HasZRangeImage(string szShotName, int nZIndex)`; `public List<KeyValuePair<int, HImage>> TakeZRangeImages(string szShotName, List<int> lstZIndices)`(락 안에서 목록 순서대로 키를 찾아 사전에서 제거하고 사진 소유권을 그대로 넘긴다 — 추가 복사 없음, 호출자 Dispose 책임, null 사진은 건너뜀); `private void ClearZRangeImages()`(전 엔트리 try-catch Dispose 후 Clear).
- 수명: `BeginCrossZImageCycle()` 과 `ClearCrossZImagesAfterBatchCycle()` 본문의 `ClearCrossZImages();` 다음 줄, `ResetCycleStateForProtocolReset()` 의 `ClearCrossZImages();` 줄 다음 줄에 각각 `ClearZRangeImages();`.
- DoesShotOwnCrossZIndex(:992-1024) 다음에 삽입: `public bool DoesShotOwnZRangeIndex(ShotConfig shot, int nZIndex)` — 가드 순서: shot null → false, `!shot.IsZRangeEnabled()` → false(꺼짐은 여기서 끝, 회귀 0), shot.OwnerSequenceName 이 Name 과 다르면 false, nZIndex 가 shot.ZIndex 미만이면 false, shot.ZIndexEnd 초과면 false, true(겹침 제외 가드는 77-02 가 마지막 return 앞에 넣는다). `public List<int> BuildZRangeCandidateIndices(ShotConfig shot)` — shot null 이거나 꺼짐이면 빈 목록, 아니면 ZIndex 부터 ZIndexEnd 까지 오름차순으로 DoesShotOwnZRangeIndex 인 z 만. `public bool IsZIndexUsedByZRangeCapture(int nZIndex)` — RecipeManager null 가드(FindShotByZIndex 와 같은 방식) 후 Shots 중 DoesShotOwnZRangeIndex 가 하나라도 있으면 true. `private bool ShotHasZRangeCompletingAt(ShotConfig shot, int nZIndex)` — null·꺼짐이면 false, 아니면 ZIndexEnd 가 nZIndex 와 같은지. `private int MaxZRangeCompletionZIndex(InspectionRecipeManager recipeManager)` — 이 시퀀스 소유이고 켜진 Shot 의 ZIndexEnd 최댓값(없으면 0). `public bool IsManualTriggerCycle()` — RequestPacket 이 null 이면 false, 아니면 `string.IsNullOrEmpty(RequestPacket.Sender)`(수동 트리거는 TCP 클라이언트가 없어 Sender 가 비어 있다).
- 라우팅 삽입(기존 줄 무수정): FindActionIndicesByZIndex 의 `bool bMatch = bOwnZIndexMatch || bCrossZMatch;` 다음 줄에 `bool bZRangeMatch = DoesShotOwnZRangeIndex(shot, nZIndex);` 와 bZRangeMatch 면 `bMatch = true;`. GetMeasurementCompletionZIndex 의 `return shot.ZIndex;` 바로 앞에 `bool bZRangeShot = shot.IsZRangeEnabled();` 와 bZRangeShot 이면 `return shot.ZIndexEnd;`(크로스-Z 측정은 위의 기존 분기가 먼저 반환하므로 영향 없음). AggregateIndexFais 의 `bool bInScope = ...;` 다음 줄에 `bool bZRangeCompletesHere = bOwnedByThisSeq && ShotHasZRangeCompletingAt(shot, nZIndex);` 와 참이면 `bInScope = true;`. ComputeLastZIndex 의 `nMax = System.Math.Max(nMax, MaxCrossZCompletionZIndex(recipeManager));` 다음 줄에 `nMax = System.Math.Max(nMax, MaxZRangeCompletionZIndex(recipeManager));`(ZIndexEnd 가 최댓값이면 마지막 Index 오판정 방지). WarnIfEmptyScope 의 `bool bSuppressWarning = ...;` 다음 줄에 `bool bZRangeCaptureIndex = IsZIndexUsedByZRangeCapture(nZIndex);` 와 참이면 `return;`(중간 z 는 결과 0건이 정상).

(7) Action_FAIMeasurement.cs (SZF-02, SZF-03, D-77-02~05, P-6, P-7):
- ECrossZGate enum(:50) 아래에 `private enum EZRangeMode { Off, AutoPending, AutoCompletion, ManualSingle, OfflineSelect }`(여러 줄, 멤버마다 한 줄 주석). LOG_TAG 상수 근처에 `private const string ZFOCUS_LOG_TAG = "[ZFocus] ";`, `private const string ZFOCUS_SCORE_FORMAT = "F2";`, `private const double ZFOCUS_FAILED_SCORE = 0.0;`. 필드 `private List<KeyValuePair<int, HImage>> _lstZRangeCandidates = null;`, `private bool _bZRangeCandidatesLoaded = false;`.
- CrossZCaptureTickResult(:1969-1974) 아래에 `private class ZFocusRunResult` — 필드 `public int ZIndex; public bool Ok; public double Score; public double Value; public string Error; public List<EdgeInspectionOverlay> Overlays;`(같은 K&R 필드 스타일).
- 삽입 지점 4곳: RunInit 본문 첫 문장 `ReleaseZRangeCandidates();`(이전 tick 잔여 안전망). RunGrab 의 `ShotParam.SetImage(image);` 다음 줄 `StoreZRangeCandidateImage(image);`. RunMeasure 의 `MeasureShotFaiList(...);` 호출 다음 줄(같은 if 블록 안) `ReleaseZRangeCandidates();`(O-5 평가 직후 해제). ProcessOneMeasurement 의 `InjectDatumOrigin(meas, parentSeq2);` 다음 줄에 `TryHandleZRangeMeasurement(meas, parentSeq2, bHasAnyZIndex, dualMeasForGate, image, transform, pixRes, acc, overlayAcc, faiOverlays, dctAlgoUsed)` 가 true 면 return(중괄호 필수).
- 새 private 메서드(인접 K&R 스타일): 
  `ResolveZRangeMode(InspectionSequence parentSeq2)` — 가드 순서: ShotParam null → Off, `!ShotParam.IsZRangeEnabled()` → Off(가장 먼저), parentSeq2 null → Off, `!parentSeq2.IsProtocolDrivenCycle()` → Off, `parentSeq2.IsManualTriggerCycle()` → Off(수동·오프라인 모드는 77-04 가 이 두 줄을 ManualSingle/OfflineSelect 로 바꾼다), 현재 z(GetExecutionZIndex)가 ShotParam.ZIndexEnd 와 같으면 AutoCompletion, 아니면 AutoPending.
  `StoreZRangeCandidateImage(HImage image)` — image·ShotParam·부모 시퀀스 null 가드, 모드가 AutoPending 도 AutoCompletion 도 아니면 return, 현재 z 가 `DoesShotOwnZRangeIndex(ShotParam, nCurZ)` 가 아니면 return(범위 밖 z 저장 금지 — T-77-01), `StoreZRangeImage(ShotParam.ShotName, nCurZ, image)` 후 Algorithm 로그 `[ZFocus] 후보 저장 — <Shot> z=<N>`.
  `TryHandleZRangeMeasurement(...)` — 모드 Off → false, bHasAnyZIndex(크로스-Z 측정) → false, AutoPending → MarkMeasurementZRangePending 후 true, AutoCompletion → `bool bScoreSupported = dualMeasForGate == null && meas.SupportsEdgeStrengthScore();` 가 false 면 false(미지원 타입은 77-02 가 기준 Z 사진 경로로 바꾼다), 참이면 ExecuteZRangeSelection 결과 반환, 그 외 모드 → false.
  `MarkMeasurementZRangePending(MeasurementBase meas, InspectionSequence parentSeq2, ShotMeasureAccumulator acc)` — ClearResult, LastSkipReason = SkipReason.Z_RANGE_PENDING, LastJudgement = false, acc.FaiAllPass = false, acc.MeasuredCount++, Algorithm 로그 `[ZFocus] 대기 — <Shot> · <측정> z=<현재> 끝=<ZIndexEnd>: 후보 사진을 모으는 중이라 아직 측정 안 함(PASS 아님)`(측정 이름은 기존 GetMeasurementDisplayName).
  `EnsureZRangeCandidatesLoaded(InspectionSequence parentSeq2)` — 이미 로드면 return, 로드 표시 후 `BuildZRangeCandidateIndices(ShotParam)` 목록으로 `TakeZRangeImages(ShotParam.ShotName, ...)` 결과를 _lstZRangeCandidates 에 보관(첫 지원 측정에서 1회, 같은 Shot 의 다음 측정은 재사용).
  `ReleaseZRangeCandidates()` — 목록이 있으면 각 사진 SafeDisposeImage, 목록 null, 로드 표시 false.
  `ExecuteZRangeSelection(MeasurementBase meas, InspectionSequence parentSeq2, HTuple transform, double pixRes, ShotMeasureAccumulator acc, List<EdgeInspectionOverlay> overlayAcc, List<EdgeInspectionOverlay> faiOverlays, Dictionary<string, int> dctAlgoUsed)` — EnsureZRangeCandidatesLoaded, 후보가 null 이거나 0개면 ELogType.Error 로 `[ZFocus] 후보 사진 없음 — <Shot> · <측정>: 현재 사진 1장으로 측정` 남기고 false(기존 경로로 측정 — PASS 로 비워 두지 않음). 아니면 Stopwatch 시작 → RunZFocusCandidates → PickZFocusResult → LogZFocusSelection → 기존 `RecordMeasurementResult(meas, false, chosen.Ok, chosen.Value, chosen.Error, chosen.Overlays, overlayAcc, faiOverlays, dctAlgoUsed, sw, acc)` → 그 다음 줄에 `meas.LastSelectedZIndex = chosen.ZIndex;`(RecordMeasurementResult 실패 분기의 ClearResult 뒤라 선택 Z 가 남는다) → true.
  `RunZFocusCandidates(MeasurementBase meas, HTuple transform, double pixRes)` — 후보 목록 순서(z 오름차순)대로 기존 `TryExecuteMeasurement(meas, 후보사진, transform, pixRes, out ..., out ..., out ...)` 를 실제로 돌리고 ZFocusRunResult 에 z·성공·값·오류·오버레이를 담는다. 점수는 성공이면 meas.LastFitScore, 실패면 ZFOCUS_FAILED_SCORE(if/else).
  `PickZFocusResult(List<ZFocusRunResult> lstResults)` — 성공한 결과 중 Score 가 현재 최고보다 **엄격히 클 때만** 교체(동점이면 먼저 평가된 작은 z 유지). 성공이 하나도 없으면 목록 첫 결과(가장 작은 z — 보통 기준 Z)를 반환해 그 실패가 그대로 기록되게 한다. 동점 3% 규칙은 77-02.
  `LogZFocusSelection(MeasurementBase meas, List<ZFocusRunResult> lstResults, ZFocusRunResult chosen, Stopwatch sw)` — StringBuilder 로 후보마다 `z<N>=<점수 F2>` 또는 실패면 `z<N>=실패` 를 이어 붙여 ELogType.Algorithm 로 `[ZFocus] 선택 — <Shot> · <측정> 후보 <목록> → z<선택> (<ms>ms)` 한 줄.

(8) 건드리지 않는 것: MainView.xaml.cs(신규 로직 금지), Custom/SystemHandler.cs, 기존 TryFitLine 호출부(다른 측정 타입 8종), DatumConfig.cs, csproj. 새 .cs 파일 금지. D:\Data 레시피는 읽기만.

(9) 커밋: `git add` 로 위 7개 경로만 지정한다(전체 추가·stash·checkout 금지). 메시지 예: feat(77-01): SIDE Z 범위 tracer — z 누적·ZIndexEnd 에서 측정별 에지 강도 선택.

CLAUDE.md 하드룰(새로 쓰는 모든 줄 — 위반 = 회귀): 삼항 금지(if/else), null 병합·null 조건 연산자 금지(명시적 null 비교), C# 8 switch 식 금지, C# 7.2 문법만, 한 줄 분기도 중괄호, 한 줄에 한 문장, && 나 || 를 3개 이상 한 식에 잇지 않고 이름 있는 bool 로 뽑는다, if 중첩 3단계 이상이면 메서드 분리, 헝가리언 접두사 b/n/sz/d/hv, 매직넘버는 이름 있는 const, 날짜·이니셜 서명 주석 신규 금지, HImage/HObject/HTuple 은 finally 에서 개별 try-catch Dispose, 수정 파일의 기존 brace 스타일 유지. 새 줄(문자열·주석 포함)에 물음표 문자를 쓰지 않는다(삼항 grep 오탐 방지). 기존 줄의 삼항·null 병합(예: GetMeasurementDisplayName, SortAndTrimPercent)은 복사하지도 고치지도 않는다.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
BASE=$(git log --format=%H -1 -- .planning/phases/77-side-z-focus-select/77-01-PLAN.md)
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > "$TEMP/p77-01.log" 2>&1; echo "msbuild_exit=$?"
echo "errors=$(grep -cE ': error [A-Z]+[0-9]+' "$TEMP/p77-01.log")"
SC=WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
VA=WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
MB=WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
ED=WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs
SR=WPF_Example/Custom/Sequence/Inspection/SkipReason.cs
IS=WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
AF=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
echo "zend=$(grep -c 'public int ZIndexEnd' $SC) desc=$(grep -c 'PLC 가 ZIndex~Z 범위 끝 번호로 차례로 촬영해야 동작' $SC) enabled=$(grep -c 'public bool IsZRangeEnabled()' $SC)"
echo "nostart=$(grep -rhoF 'ZIndexStart' WPF_Example --include=*.cs | wc -l)"
echo "sig=$(grep -c 'List<ValueTuple<double, double>> collectedEdges = null)' $VA) scoreclass=$(grep -c 'public class EdgeStrengthScore' $VA) addstrip=$(tr -d '\r' < $VA | grep -vE '^\s*//' | grep -c 'EdgeScore.AddStrip(_dLastStripMaxAbsAmp);') okamp=$(grep -c '_dLastStripMaxAbsAmp = ComputeStripMaxAbsAmp(amp);' $VA) edgelog=$(grep -c '\[FitLine\] edge-strength' $VA)"
echo "mbfields=$(grep -cE 'public double LastFitScore;|public int LastSelectedZIndex = SELECTED_Z_NONE;' $MB) mbclear=$(tr -d '\r' < $MB | awk '/public void ClearResult\(\)/,/^        }$/' | grep -cE 'LastFitScore = 0.0;|LastSelectedZIndex = SELECTED_Z_NONE;') mbfmt=$(grep -cE 'public static string FormatSelectedZ\(int nZIndex\)|public static int ParseSelectedZ\(string szText\)|public virtual bool SupportsEdgeStrengthScore\(\)' $MB)"
echo "edwire=$(grep -cE 'svc.EdgeScore = edgeScore;|LastFitScore = edgeScore.Average;|public override bool SupportsEdgeStrengthScore\(\)' $ED) pendingconst=$(grep -c 'Z_RANGE_PENDING = "Z_RANGE_PENDING"' $SR)"
echo "route=$(tr -d '\r' < $IS | awk '/public List<int> FindActionIndicesByZIndex\(/,/^        }$/' | grep -c 'DoesShotOwnZRangeIndex(shot, nZIndex)') completion=$(tr -d '\r' < $IS | awk '/private int GetMeasurementCompletionZIndex\(/,/^        }$/' | grep -c 'return shot.ZIndexEnd;') aggregate=$(tr -d '\r' < $IS | awk '/private int AggregateIndexFais\(/,/^        }$/' | grep -c 'ShotHasZRangeCompletingAt(shot, nZIndex)') lastidx=$(tr -d '\r' < $IS | awk '/private int ComputeLastZIndex\(/,/^        }$/' | grep -c 'MaxZRangeCompletionZIndex(recipeManager)') warnskip=$(tr -d '\r' < $IS | awk '/private void WarnIfEmptyScope\(/,/^        }$/' | grep -c 'IsZIndexUsedByZRangeCapture(nZIndex)')"
echo "lifecycle=$(tr -d '\r' < $IS | grep -vE '^\s*//' | grep -c 'ClearZRangeImages();') ownguard=$(tr -d '\r' < $IS | awk '/public bool DoesShotOwnZRangeIndex\(/,/^        }$/' | grep -c 'IsZRangeEnabled()') manual=$(grep -c 'public bool IsManualTriggerCycle()' $IS)"
echo "storecall=$(tr -d '\r' < $AF | awk '/private void RunGrab\(\)/,/^        }$/' | grep -c 'StoreZRangeCandidateImage(image);') gate=$(tr -d '\r' < $AF | awk '/private void ProcessOneMeasurement\(/,/^        }$/' | grep -c 'TryHandleZRangeMeasurement(') release=$(tr -d '\r' < $AF | grep -vE '^\s*//' | grep -c 'ReleaseZRangeCandidates();') pending=$(tr -d '\r' < $AF | grep -vE '^\s*//' | grep -c 'SkipReason.Z_RANGE_PENDING') selz=$(grep -c 'meas.LastSelectedZIndex = chosen.ZIndex;' $AF)"
echo "offfirst=$(tr -d '\r' < $AF | awk '/private EZRangeMode ResolveZRangeMode\(/,/^        }$/' | awk '/IsZRangeEnabled\(\)/{if(a==0){a=NR}} /return EZRangeMode\.Auto/{if(b==0){b=NR}} END{if(a>0 && b>a){print 1}else{print 0}}')"
for F in $SC $VA $MB $ED $SR $IS $AF; do
  ADD=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+')
  echo "$F deleted=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk')"
done
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - 빌드: `msbuild_exit=0`, `errors=0` (Debug|x64 만. Release|x64 빌드 금지 — OutputPath D:\Data\). 실행 중인 앱이 exe 를 잠가 복사 단계만 실패하면 `-p:OutDir=<scratch>` 를 붙여 재실행(76 선례)
    - `zend=1 desc=1 enabled=1`, `nostart=0` (시작 번호 프로퍼티 없음 — D-77-07 ①, VALIDATION)
    - `sig=1`(TryFitLine 시그니처 무변경), `scoreclass=1`, `addstrip=2`, `okamp=2`, `edgelog=1`
    - `mbfields=2`, `mbclear=2`, `mbfmt=3`
    - `edwire=3`, `pendingconst=1`
    - `route=1 completion=1 aggregate=1 lastidx=1 warnskip=1`, `lifecycle=3`, `ownguard>=1`, `manual=1`
    - `storecall=1 gate=1 release=2 pending>=1 selz=1`, `offfirst=1` (범위 꺼짐 판정이 Auto 모드 반환보다 먼저 — SZF-05)
    - 7개 파일 모두 `deleted=0` 이고 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0` (추가 줄 한정 하드룰 grep 5종)
    - `commit_files` 가 정확히 위 7개 경로(csproj·MainView.xaml.cs·SystemHandler.cs 미포함)
  </acceptance_criteria>
  <done>범위 Shot 의 PLC 자동 사이클 경로(z 누적 → 중간 z 대기 → ZIndexEnd 에서 EdgeToLineDistance 후보별 측정·최고 점수 채택 → 완성 index 응답·마지막 Index)가 컴파일되고 커밋되었다. 범위 꺼짐 경로는 기존 줄 삭제 0 이며 모든 새 분기가 IsZRangeEnabled() 가드 뒤에 있다. 런타임 end-to-end 증거(SIMUL TCP 사이클 로그)는 77-06 UAT U-1 에서 확인한다 — 이 코드베이스에는 앱을 자동 기동하는 테스트 하네스가 없다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLC TCP $PREP/$TEST z_index → z 라우팅·사진 저장소 | 외부 장비가 보내는 z 번호가 어느 Shot 을 실행하고 무엇을 메모리에 쌓을지 정한다 |
| 레시피 INI(현장 편집·수기 편집) → ShotConfig.ZIndexEnd | 값이 크거나 뒤집혀 들어올 수 있다 |
| 후보 사진 선택 → 측정값 → PLC P/F | 틀린 사진이 채택되면 합부가 틀린다 |
| 검사 스레드 → 새 선택 코드 | 예외·누수가 사이클을 멈추거나 메모리를 고갈시키면 안 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-77-01 | Denial of Service | InspectionSequence.m_dicZRangeImages — 반복·임의 z 로 사진(127~152MB/장) 무한 누적 | high | mitigate | 저장은 DoesShotOwnZRangeIndex(범위 안 z)일 때만(`storecall` + 가드), 키 = Shot명\|z 라 같은 z 재전송은 교체(누적 아님), ZIndexEnd tick 측정 직후 ReleaseZRangeCandidates, 사이클 시작·배치 정리·$RESET 에서 ClearZRangeImages(`lifecycle=3`), 범위 z 개수 상한 MAX_Z_RANGE_COUNT=10 |
| T-77-02 | Tampering | ShotConfig.ZIndexEnd 수기 편집(거대값·역순) | medium | mitigate | IsZRangeEnabled() 가 역순·0 이하 기준·상한 초과를 꺼짐으로 처리 → 기존 1장 경로. 편집 경고는 77-02 |
| T-77-03 | Spoofing | 중간 z 대기 측정이 PLC 에 P 로 보고됨 | high | mitigate | 대기 측정은 Z_RANGE_PENDING + LastJudgement=false, 응답은 GetMeasurementCompletionZIndex=ZIndexEnd 게이트로 제외(`completion=1`), 마지막 Index 에 ZIndexEnd 반영(`lastidx=1`) — PR-1 |
| T-77-04 | Denial of Service | 후보별 TryExecute 예외·HImage 누수 | medium | mitigate | 기존 TryExecuteMeasurement 가 예외를 잡아 false, 후보 사진은 소유권 이동 후 SafeDisposeImage(RunMeasure 직후 + RunInit 안전망) |
| T-77-05 | Repudiation | 어떤 z 가 왜 채택됐는지 확인 불가 | low | mitigate | `[ZFocus]` 선택·대기·후보 없음 로그와 `[FitLine] edge-strength` strip 별 값 로그(ELogType.Algorithm) |
| T-77-06 | Information Disclosure | 새 로그 내용 | low | accept | 좌표·점수·z 번호만 로컬 로그, 네트워크 전송 없음 |
</threat_model>

<verification>
1. Debug|x64 빌드 에러 0 (Release|x64 금지)
2. 7개 파일 `git diff -w` 삭제 0, 추가 줄 하드룰 grep 5종 0
3. 경로 배선: 설정(`zend`, `enabled`) → 저장(`storecall`) → 라우팅(`route`) → 대기(`pending`) → 점수(`addstrip`, `okamp`, `edwire`) → 선택·기록(`gate`, `selz`) → 응답(`completion`, `aggregate`, `lastidx`) → 해제(`release`, `lifecycle`)
4. 범위 꺼짐 우선 판정(`offfirst=1`, `ownguard>=1`), TryFitLine 시그니처 무변경(`sig=1`), 시작 번호 프로퍼티 0(`nostart=0`)
5. 런타임 확인(SIMUL TCP 모의 사이클에서 `[ZFocus] 대기`·`[ZFocus] 선택`·`[FitLine] edge-strength` 로그와 PLC 응답 B/P/F)은 77-06 UAT U-1
</verification>

<success_criteria>
- ZIndexEnd 1칸으로 범위를 켜고 끌 수 있고, 키 없는 옛 레시피는 꺼짐
- PLC 자동 사이클에서 범위 z 사진이 모이고 ZIndexEnd 에서 EdgeToLineDistance 가 에지 강도 최고 z 결과를 채택하며, 중간 z 는 응답에 섞이지 않는다
- 범위 꺼짐 Shot·TOP/BOTTOM·수동 경로는 기존 줄 삭제 0 으로 동작 동일
</success_criteria>

<output>
완료 후 `.planning/phases/77-side-z-focus-select/77-01-SUMMARY.md` 를 작성한다. 반드시 포함:
- BASE 해시, 커밋 해시, 빌드 결과(에러 수), 7개 파일 `git diff -w` 삭제 수
- 새 심볼 목록(이 PLAN 'Artifacts' 표의 77-01 행과 대조) — 77-02/03/04 가 ResolveZRangeMode·PickZFocusResult·EnsureZRangeCandidatesLoaded·FormatSelectedZ 를 확장한다
- 계획 단계 결정 P-1~P-13 요약, flagged assumption A-77-E1 / A-77-O1 / O8 / O9 그대로 옮겨 적기
</output>
