---
phase: 77-side-z-focus-select
plan: 02
type: execute
wave: 2
depends_on: ["77-01"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineAngleMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
autonomous: true
requirements: [SZF-01, SZF-02, SZF-03, SZF-05]
user_setup: []

must_haves:
  truths:
    - "범위 Shot 의 EdgeToLineAngle 측정도 EdgeToLineDistance 와 같은 점수 규칙으로 z 를 자동 선택한다 — 측정별 설정 칸은 없다 (SZF-03, D-77-03, D-77-07 ②)"
    - "[edge SZF-03 boundary] 최고 점수 z 와 기준 Z(ZIndex) 의 점수 차가 최고 점수의 3.0% 이하(경계 포함)면 기준 Z 를 채택하고, 3.0% 를 넘으면 최고 점수 z 를 채택한다. 3% 는 화면에 없는 내부 const Z_FOCUS_TIE_PERCENT 다 (O-3, D-77-07 ③)"
    - "동점 규칙으로 기준 Z 가 채택되면 기준 Z 후보 자신의 측정값·오류·오버레이가 그대로 쓰인다 — 다른 후보 결과와 섞이지 않고 재계산도 없다 (D-77-02, 리서치 Open Question 2)"
    - "[edge SZF-03 boundary] 최고 점수가 0 이면(모든 strip 에 에지 없음) 차이 비율을 계산하지 않는다(0 나눗셈 없음) — 먼저 평가된 기준 Z 결과가 유지된다"
    - "[edge SZF-03 adjacency] 기준 Z 가 아닌 두 후보가 같은 점수면 작은 z 가 채택되고, 기준 Z 가 최고 점수와 같으면 기준 Z 가 채택된다"
    - "범위 Shot 의 지원 안 되는 측정 타입(EdgeToLine* 외, 크로스-Z 제외)은 ZIndexEnd tick 에서 기준 Z 사진으로 1회 측정하고 Algorithm 로그 1줄을 남긴다 (D-77-07 ②)"
    - "ZIndexEnd tick 에서 화면·원본 저장·미지원 측정에 쓰이는 Shot 사진은 기준 Z(ZIndex) 사진으로 바뀐다. 기준 Z 사진이 없으면 현재 z 사진을 쓰고 Error 로그를 남긴다 (O-7)"
    - "중간 z 가 빠지면 ZIndexEnd 에서 빠진 z 번호를 Error 로그 한 줄로 남기고 도착한 사진만으로 선택해 사이클을 계속한다 (O-4, 성공 기준 4)"
    - "[edge SZF-02 adjacency] 범위 안 z 라도 다른 Shot 의 ZIndex, 다른 Shot 의 크로스-Z 측정 z, Datum ZIndexA/B, 이 시퀀스 기준점 z, 다른 범위 Shot 의 범위에 든 z 는 이 Shot 후보가 아니다 — $TEST 라우팅·사진 저장·후보 목록에서 모두 빠진다. 기준 Z(ZIndex) 자신은 항상 후보다 (D-77-07 ⑤)"
    - "$PREP 조명은 범위 z 에서 그 z 를 소유한 범위 Shot 의 조명으로 켜진다 — 기존 정확일치·크로스-Z 매칭이 먼저이고 범위 매칭은 3번째다"
    - "[edge SZF-01 adjacency] ZIndexEnd 를 ZIndex 와 같게 넣으면 편집 즉시 한국어 경고 한 줄이 뜨고 런타임은 꺼짐(기존 1장 경로)이다. ZIndex+1 이면 후보 2개로 켜진다"
    - "[edge SZF-01 ordering] ZIndexEnd 를 ZIndex 보다 작게 넣거나 상한(10개)을 넘기거나 범위가 다른 Shot·기준점 z 와 겹치면 속성창 편집 즉시 한국어 경고 한 줄이 뜬다. 레시피 로드·붙여넣기(리플렉션)에서는 뜨지 않는다. 저장은 막지 않는다 (D-77-07 ⑤, DatumConfig 경고 선례)"
    - "꺼짐으로 처리된 오설정 범위 Shot 은 검사 tick 마다 Error 로그 한 줄로 이유를 남긴다 — 조용히 꺼지지 않는다 (리서치 Pitfall 4)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "동점 규칙 + 기준 Z 사진 표시 교체 + 미지원 타입 기준 Z 측정 + 누락·오설정 로그"
      contains: "private const double Z_FOCUS_TIE_PERCENT = 3.0;"
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "겹침 z 제외 + $PREP 3패스 + 편집 경고용 겹침 문구"
      contains: "private bool IsZIndexReservedOutsideZRange(ShotConfig rangeShot, int nZIndex)"
    - path: "WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs"
      provides: "ZIndexEnd 편집 즉시 경고 + 로드·붙여넣기 억제"
      contains: "private void WarnZIndexEndChanged()"
  key_links:
    - from: "Action_FAIMeasurement.PickZFocusResult"
      to: "ShotConfig.ZIndex 후보 결과 (FindZFocusResultByZ)"
      via: "차이 비율 ≤ Z_FOCUS_TIE_PERCENT 면 기준 Z 결과 반환"
      pattern: "Z_FOCUS_TIE_PERCENT"
    - from: "InspectionSequence.DoesShotOwnZRangeIndex"
      to: "InspectionSequence.IsZIndexReservedOutsideZRange"
      via: "기준 Z 즉시 true 다음, 마지막 return 앞의 제외 가드"
      pattern: "IsZIndexReservedOutsideZRange\\(shot, nZIndex\\)"
    - from: "ShotConfig.ZIndexEnd setter"
      to: "InspectionSequence.BuildZRangeConflictText"
      via: "WarnZIndexEndChanged → Parent as InspectionSequence"
      pattern: "BuildZRangeConflictText\\(this\\)"
  prohibitions:
    - statement: "MUST NOT 다른 Shot·Datum 이 쓰는 z(그 Shot 의 조명·높이로 찍힌 사진)를 이 Shot 의 후보로 점수 경쟁에 넣지 않는다 — 조명이 다르면 에지 강도가 달라 틀린 사진이 선택된다"
      status: resolved
      verification: judgment
---

<objective>
77-01 tracer 뼈대에 선택 정책과 설정 안전장치를 붙인다: EdgeToLineAngle 지원, 기준 Z 동점 규칙(3% 내부 const, 기준 Z 결과 그대로 재사용), 미지원 타입의 기준 Z 사진 측정, ZIndexEnd tick 화면 사진을 기준 Z 로 교체, 중간 z 누락 경고, 겹치는 z 제외와 $PREP 조명 매칭, ZIndexEnd 편집 즉시 한국어 경고.

Purpose: 같은 부품 반복 사이클에서 선택 Z 가 흔들려 값이 튀지 않게 하고(성공 기준 3), 다른 Shot 조명으로 찍힌 사진이 경쟁에 끼지 않게 하며(D-77-07 ⑤), 운영자가 칸 하나만 넣어도 실수를 바로 알게 한다(D-77-07).

Output: 4개 기존 파일 삽입(77-01 이 만든 메서드 확장 포함). BASE 이전부터 있던 줄 삭제 0.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/77-side-z-focus-select/77-CONTEXT.md
@.planning/phases/77-side-z-focus-select/77-01-PLAN.md
@.planning/phases/77-side-z-focus-select/77-01-SUMMARY.md

<interfaces>
77-01 이 만든 계약(77-01-SUMMARY 로 이름 확인 후 사용):
- ShotConfig: `ZIndexEnd`(setter: 같은 값 return → 대입 → RaisePropertyChanged), `IsZRangeEnabled()`, `Z_RANGE_OFF`, `MIN_Z_RANGE_BASE_INDEX`, `MAX_Z_RANGE_COUNT`
- InspectionSequence: `DoesShotOwnZRangeIndex(ShotConfig, int)`(마지막 `return true;`), `BuildZRangeCandidateIndices(ShotConfig)`, `HasZRangeImage`, `TakeZRangeImageCopy`, `TakeZRangeImages`
- Action_FAIMeasurement: `EZRangeMode`, `ZFocusRunResult`, `ResolveZRangeMode`, `StoreZRangeCandidateImage`, `TryHandleZRangeMeasurement`(AutoCompletion 미지원 → return false 분기), `EnsureZRangeCandidatesLoaded`, `ExecuteZRangeSelection`, `RunZFocusCandidates`, `PickZFocusResult`, `LogZFocusSelection`, `ZFOCUS_FAILED_SCORE`, `ZFOCUS_LOG_TAG`
- MeasurementBase: `LastFitScore`, `LastSelectedZIndex`, `SELECTED_Z_NONE`, `SupportsEdgeStrengthScore()`, `FormatSelectedZ(int)`

기존 코드(계획 시점 라인, 77-01 삽입으로 밀릴 수 있어 메서드 이름으로 찾는다):
- EdgeToLineAngleMeasurement.cs `:89` 생성자, `:99-101` out 초기화·overlays, `:103` `var svc = new VisionAlgorithmService();`, `:109-119` TryFitLine if 블록
- InspectionSequence.cs `:715` TryGetOwnedShotZIndexRange(PropertyGrid 경로 null 가드 선례 주석 :712-714), `:779` GetDatumZIndex, `:941-987` FindShotByZIndex(2패스 뒤 `return null;`), `:992` DoesShotOwnCrossZIndex, `:1647` IsZIndexUsedByCrossZDatum, `:1722-1739` AddShotDeclaredZIndices(`declaredSet.Add(shot.ZIndex);`)
- ShotConfig.cs `:384-419` Load(`bool result = base.Load(loadFile, groupName);`), `:424-432` CopyTo(`bool result = base.CopyTo(param);`, target 은 그 앞에서 계산)
- DatumConfig.cs `:263-300` WarnDatumZIndexChanged + `_suppressUserEditWarning`(경고 문구·CustomMessageBox.Show 호출 형태 선례), `:1346-1356` Load 억제, `:1409-1418` CopyTo 억제
- Action_FAIMeasurement.cs RunInit, RunGrab(`image.Dispose();`), RunMeasure(첫 LogSeqStep 문장), UpdateViewerCopy(HImage), TryExecuteMeasurement, RecordMeasurementResult, SafeDisposeImage
</interfaces>
</context>

## Artifacts this phase produces

phase 전체 원장은 `77-01-PLAN.md` 의 'Artifacts this phase produces' 표가 단일 소스다. 이 plan 이 만드는 것:

| 심볼 | 종류 | 파일 |
|---|---|---|
| `Z_FOCUS_TIE_PERCENT`(3.0), `PERCENT_SCALE`(100.0), `FindZFocusResultByZ`, `ApplyBaseZTieRule`, `ExecuteZRangeBaseImageMeasurement`, `ApplyZRangeBaseImageForDisplay`, `_nZRangeDisplayZIndex`, `LogZRangeMisconfigIfNeeded`, `_bZRangeMisconfigLogged`, 로그 `[ZFocus] 기준 Z 사진 사용` / `[ZFocus] 후보 누락` / `[ZFocus] 설정 확인` | const/메서드/필드/로그 | Action_FAIMeasurement.cs |
| `SupportsEdgeStrengthScore()` override + EdgeScore 배선 | 메서드 | EdgeToLineAngleMeasurement.cs |
| `IsZRangeMisconfigured()`, `BuildZRangeMisconfigText()`, `WarnZIndexEndChanged()`, `_suppressUserEditWarning` | 메서드/필드 | ShotConfig.cs |
| `IsZIndexReservedOutsideZRange`, `IsZIndexInsideRawZRange`, `BuildZRangeConflictText`, 로그 `[PREP ZRange]` | 메서드/로그 | InspectionSequence.cs |

<tasks>

<task type="auto">
  <name>Task 1: 선택 정책 완성 — EdgeToLineAngle 점수, 기준 Z 동점 규칙(결과 재사용), 미지원 타입 기준 Z 측정, 화면 사진 기준 Z 교체, 누락 z 경고</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineAngleMeasurement.cs</files>
  <read_first>
    - CLAUDE.md (Code Style 하드룰)
    - .planning/phases/77-side-z-focus-select/77-01-PLAN.md 의 계획 단계 결정 P-6 ~ P-12
    - .planning/phases/77-side-z-focus-select/77-01-SUMMARY.md (77-01 이 만든 메서드 이름·위치)
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs 의 RunInit, RunGrab, UpdateViewerCopy, RunMeasure, ProcessOneMeasurement, RecordMeasurementResult, TryExecuteMeasurement 와 77-01 이 추가한 ZRange/ZFocus 메서드 전체
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineAngleMeasurement.cs:85-125
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs 의 77-01 배선(똑같이 따라 한다)
  </read_first>
  <action>
(1) EdgeToLineAngleMeasurement.cs — 77-01 의 EdgeToLineDistance 배선을 그대로 따른다(D-77-07 ②): 생성자 아래 `public override bool SupportsEdgeStrengthScore()` true 반환, `overlays = new List<EdgeInspectionOverlay>();` 다음 줄 `LastFitScore = 0.0;`, `var svc = new VisionAlgorithmService();` 다음 줄 `var edgeScore = new EdgeStrengthScore();` 와 `svc.EdgeScore = edgeScore;`, TryFitLine if 블록 닫는 중괄호 다음 줄 `LastFitScore = edgeScore.Average;`. 기존 줄 무수정.

(2) Action_FAIMeasurement.cs 상수·필드: `private const double Z_FOCUS_TIE_PERCENT = 3.0;`(주석: 화면에 노출하지 않는 내부 동점 허용치 — D-77-07 ③, O-3), `private const double PERCENT_SCALE = 100.0;`, `private int _nZRangeDisplayZIndex = MeasurementBase.SELECTED_Z_NONE;`. RunInit 의 77-01 삽입 줄(`ReleaseZRangeCandidates();`) 다음 줄에 `_nZRangeDisplayZIndex = MeasurementBase.SELECTED_Z_NONE;`.

(3) 동점 규칙(O-3, 리서치 Open Question 2 해소): 77-01 의 `PickZFocusResult` 를 확장한다 — 최고 점수 결과(best)를 고른 뒤 `FindZFocusResultByZ(lstResults, ShotParam.ZIndex)` 로 기준 Z 결과(baseResult)를 찾아 `ApplyBaseZTieRule(best, baseResult)` 결과를 반환한다. 성공이 하나도 없을 때의 기존 반환(목록 첫 결과)은 유지.
- `private static ZFocusRunResult FindZFocusResultByZ(List<ZFocusRunResult> lstResults, int nZIndex)` — z 가 같은 첫 결과, 없으면 null.
- `private ZFocusRunResult ApplyBaseZTieRule(ZFocusRunResult best, ZFocusRunResult baseResult)` — 가드 절만으로: best null → baseResult 반환, baseResult null → best, `!baseResult.Ok` → best, best.ZIndex 와 baseResult.ZIndex 가 같으면 best, best.Score 가 ZFOCUS_FAILED_SCORE 이하면 baseResult(0 나눗셈 없음 — 모두 에지 없음이면 기준 Z), `double dGapPercent = (best.Score - baseResult.Score) / best.Score * PERCENT_SCALE;` 가 Z_FOCUS_TIE_PERCENT 이하면 baseResult, 아니면 best. 경계 포함(이하). 반올림 없음. 반환된 결과 객체의 Value·Error·Overlays 를 그대로 쓰므로 기준 Z 채택 시 기준 Z 자신의 측정 결과가 기록된다(재계산 없음, D-77-02).
- `LogZFocusSelection` 의 로그 끝에 기준 Z 정보를 덧붙인다: 기준 Z 결과가 있으면 ` 기준 z<N>=<점수 F2> 허용 <Z_FOCUS_TIE_PERCENT F1>%`, 없으면 ` 기준 z<N> 후보 없음`. 점수 상세는 이 로그에만 있다(D-77-07 ⑥).

(4) 누락 z 경고(O-4): 77-01 의 `EnsureZRangeCandidatesLoaded` 에서 TakeZRangeImages 다음에, 기대 목록(BuildZRangeCandidateIndices) 중 받은 목록에 없는 z 를 모아 1개 이상이면 ELogType.Error 로 `[ZFocus] 후보 누락 — <Shot>: <z3,z5 형식, FormatSelectedZ 사용> 사진 없음, 도착한 <n>장으로 선택(사이클 계속)` 한 줄. 측정은 중단하지 않는다.

(5) 화면·원본 사진을 기준 Z 로(O-7): RunGrab 의 `image.Dispose();` 다음 줄(같은 if 블록 안)에 `ApplyZRangeBaseImageForDisplay();` 삽입. 새 메서드: ShotParam·부모 시퀀스 null 가드 → ResolveZRangeMode 가 AutoCompletion 이 아니면 return → `int nCurZ = parentSeq.GetExecutionZIndex();` 를 `_nZRangeDisplayZIndex` 에 넣음 → ShotParam.ZIndex 가 nCurZ 와 같으면 return → `HasZRangeImage(ShotParam.ShotName, ShotParam.ZIndex)` 가 false 면 ELogType.Error `[ZFocus] 기준 Z 사진 없음 — <Shot>: 화면·미지원 측정은 z<현재> 사진 사용` 후 return → `HImage baseImage = null;` try 안에서 `TakeZRangeImageCopy` 로 사본을 받고 null 이 아니면 `ShotParam.SetImage(baseImage);`, `UpdateViewerCopy(baseImage);`, `_nZRangeDisplayZIndex = ShotParam.ZIndex;` → finally 에서 baseImage 가 null 이 아니면 try-catch Dispose. 저장소의 기준 Z 원본은 그대로 남아 후보로 쓰인다.

(6) 미지원 타입(D-77-07 ②): 77-01 의 `TryHandleZRangeMeasurement` 에서 AutoCompletion 이고 bScoreSupported 가 false 일 때 false 를 반환하던 분기를 `ExecuteZRangeBaseImageMeasurement(meas, image, transform, pixRes, acc, overlayAcc, faiOverlays, dctAlgoUsed)` 호출 후 true 반환으로 바꾼다(77-01 이 쓴 줄이라 BASE 기준 삭제 아님). 새 메서드: Stopwatch 시작 → 기존 `TryExecuteMeasurement(meas, image, transform, pixRes, out ..., out ..., out ...)`(image 는 (5) 로 기준 Z 사진) → 기존 RecordMeasurementResult(bHasAnyZIndex 자리 false) → 다음 줄 `meas.LastSelectedZIndex = _nZRangeDisplayZIndex;` → ELogType.Algorithm `[ZFocus] 기준 Z 사진 사용 — <Shot> · <측정> type=<TypeName> z=<_nZRangeDisplayZIndex> (에지 강도 선택 미지원 타입)` 한 줄.

(7) 커밋: `git add` 로 두 파일만. 메시지 예: feat(77-02): Z 선택 동점 규칙(기준 Z 결과 재사용)·EdgeToLineAngle·기준 Z 사진 표시.

CLAUDE.md 하드룰(새 줄 전부): 삼항·null 병합·null 조건 연산자·switch 식 금지, C# 7.2, 한 줄 분기도 중괄호, &&/|| 3개 이상 한 식 금지(이름 있는 bool 또는 가드 절), if 중첩 3단계 금지, 헝가리언 b/n/sz/d/hv, 매직넘버 금지, 날짜·이니셜 서명 주석 금지, HImage finally Dispose, 기존 K&R 스타일 유지, 새 줄에 물음표 문자 금지. BASE 이전 줄은 고치지 않는다.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
BASE=$(git log --format=%H -1 -- .planning/phases/77-side-z-focus-select/77-02-PLAN.md)
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > "$TEMP/p77-02a.log" 2>&1; echo "msbuild_exit=$?"
echo "errors=$(grep -cE ': error [A-Z]+[0-9]+' "$TEMP/p77-02a.log")"
AF=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
EA=WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineAngleMeasurement.cs
echo "eawire=$(grep -cE 'svc.EdgeScore = edgeScore;|LastFitScore = edgeScore.Average;|public override bool SupportsEdgeStrengthScore\(\)' $EA)"
echo "tieconst=$(grep -c 'private const double Z_FOCUS_TIE_PERCENT = 3.0;' $AF) tieuse=$(tr -d '\r' < $AF | awk '/private ZFocusRunResult ApplyBaseZTieRule\(/,/^        }$/' | grep -cE 'Z_FOCUS_TIE_PERCENT|ZFOCUS_FAILED_SCORE') pickcall=$(tr -d '\r' < $AF | awk '/private ZFocusRunResult PickZFocusResult\(/,/^        }$/' | grep -c 'ApplyBaseZTieRule(')"
echo "display=$(tr -d '\r' < $AF | awk '/private void RunGrab\(\)/,/^        }$/' | grep -c 'ApplyZRangeBaseImageForDisplay();') baseexec=$(tr -d '\r' < $AF | awk '/private bool TryHandleZRangeMeasurement\(/,/^        }$/' | grep -c 'ExecuteZRangeBaseImageMeasurement(') basesel=$(grep -c 'meas.LastSelectedZIndex = _nZRangeDisplayZIndex;' $AF)"
echo "missing=$(grep -c '후보 누락' $AF) baselog=$(grep -c '기준 Z 사진 사용' $AF)"
for F in $AF $EA; do
  ADD=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+')
  echo "$F deleted_since_plan=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk')"
done
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`, `errors=0`
    - `eawire=3`
    - `tieconst=1`, `tieuse>=2`, `pickcall=1`
    - `display=1`, `baseexec=1`, `basesel=1`, `missing>=1`, `baselog>=1`
    - `deleted_since_plan` 은 77-01 이 추가한 줄(TryHandleZRangeMeasurement 의 미지원 분기, PickZFocusResult 반환부, LogZFocusSelection 로그 문장)의 수정만 포함한다 — 두 파일 모두 BASE 이전 줄 삭제 여부는 77-05 누적 감사(BASE = 77-01 커밋 이전)에서 0 으로 확인. 이 태스크에서는 `git diff -w "$BASE"` 의 `-` 줄이 전부 ZRange/ZFocus 식별자를 포함하는지 SUMMARY 에 기록
    - 두 파일 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0`
    - `commit_files` 가 정확히 두 파일
  </acceptance_criteria>
  <done>범위 Shot 의 EdgeToLineDistance·EdgeToLineAngle 은 3% 기준 Z 동점 규칙으로 선택되고(기준 Z 채택 시 기준 Z 결과 그대로), 미지원 타입은 기준 Z 사진으로 측정되며, ZIndexEnd tick 화면 사진은 기준 Z 사진이다. 누락 z 는 경고 후 계속 진행한다.</done>
</task>

<task type="auto">
  <name>Task 2: z 소유 규칙 강화와 운영 실수 경고 — 겹치는 z 제외, $PREP 조명 3패스, ZIndexEnd 편집 즉시 경고, 오설정 런타임 로그</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs, WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs, WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <read_first>
    - CLAUDE.md (Code Style 하드룰)
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs:234-300 (WarnDatumZIndexChanged 경고 형태), :1340-1360, :1405-1420 (억제 플래그)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs 의 TryGetOwnedShotZIndexRange(주석 :708-714 null 가드 이유), GetDatumZIndex, FindShotByZIndex, DoesShotOwnCrossZIndex, IsZIndexUsedByCrossZDatum, AddShotDeclaredZIndices, 77-01 의 DoesShotOwnZRangeIndex
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs 의 77-01 ZIndexEnd 블록, Load(:384-419), CopyTo(:424-486)
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs 의 RunInit, RunMeasure 첫 LogSeqStep 문장
  </read_first>
  <action>
(1) InspectionSequence.cs — 겹침 제외(D-77-07 ⑤, PR-3):
- `private static bool IsZIndexInsideRawZRange(ShotConfig shot, int nZIndex)` — shot null·`!shot.IsZRangeEnabled()` 이면 false, nZIndex 가 shot.ZIndex 이상이고 shot.ZIndexEnd 이하인지(이름 있는 bool 두 개). DoesShotOwnZRangeIndex 를 부르지 않는다(재귀 방지).
- `private bool IsZIndexReservedOutsideZRange(ShotConfig rangeShot, int nZIndex)` — 순서: nZIndex 가 GetDatumZIndex() 와 같으면 true, IsZIndexUsedByCrossZDatum(nZIndex) 면 true, RecipeManager 를 SystemHandler.Handle → Sequences → RecipeManager 순서로 각각 null 가드(PropertyGrid 경로에서도 불린다 — TryGetOwnedShotZIndexRange 주석과 같은 이유)해 하나라도 null 이면 false, Shots 순회: null·rangeShot 과 같은 인스턴스(object.ReferenceEquals)·다른 시퀀스 소유는 continue, 그 Shot 의 ZIndex 가 nZIndex 면 true, DoesShotOwnCrossZIndex(그 Shot, nZIndex) 면 true, IsZIndexInsideRawZRange(그 Shot, nZIndex) 면 true. 끝나면 false. 순회 안 조건은 가드 continue/return 으로 중첩 2단계 이내.
- 77-01 의 `DoesShotOwnZRangeIndex` 마지막 `return true;` 바로 앞에 두 가드 삽입: nZIndex 가 shot.ZIndex 와 같으면 `return true;`(기준 Z 는 항상 소유), `IsZIndexReservedOutsideZRange(shot, nZIndex)` 면 `return false;`. 이로써 $TEST 라우팅(FindActionIndicesByZIndex)·사진 저장·후보 목록·완성 판정 입력이 한꺼번에 같은 규칙을 쓴다.
- FindShotByZIndex 의 2패스 루프 뒤 `return null;` 바로 앞에 3패스 루프 삽입: null continue, 소유 시퀀스가 다르면 continue, `DoesShotOwnZRangeIndex(shot, nZIndex)` 면 ELogType.LightController 로 `[PREP ZRange] Shot={0}, ZIndex={1}~{2}, RequestedZIndex={3}, Seq={4}` 남기고 그 Shot 반환(1·2패스가 먼저 — 기존 매칭 결과 불변).
- AddShotDeclaredZIndices 의 `declaredSet.Add(shot.ZIndex);` 다음 줄에 `BuildZRangeCandidateIndices(shot)` 결과를 모두 declaredSet 에 추가하는 foreach(범위 z 도 '레시피에 존재하는 z' — 크로스-Z 오설정 판정 오탐 방지).
- `public string BuildZRangeConflictText(ShotConfig shot)` — shot null·꺼짐이면 string.Empty. ZIndex+1 부터 ZIndexEnd 까지 IsZIndexReservedOutsideZRange 인 z 를 `MeasurementBase.FormatSelectedZ` 로 모아 `, ` 로 잇고, 없으면 string.Empty, 있으면 `"Z 범위 안의 " + 목록 + " 은 다른 Shot·기준점이 쓰는 번호라 후보에서 빠집니다."` (한국어 한 줄).

(2) ShotConfig.cs — 편집 즉시 경고(D-77-07 ①⑤):
- `private bool _suppressUserEditWarning;`(주석: INI 로드·붙여넣기 리플렉션 SetValue 에서는 경고 억제 — DatumConfig 와 같은 관용구).
- `public bool IsZRangeMisconfigured()` — ZIndexEnd 가 Z_RANGE_OFF 가 아니고 IsZRangeEnabled() 가 false 인지(이름 있는 bool 두 개).
- `public string BuildZRangeMisconfigText()` — 가드 순서로 한국어 한 줄 반환: ZIndexEnd 가 Z_RANGE_OFF 면 string.Empty; ZIndex 가 MIN_Z_RANGE_BASE_INDEX 미만이면 `"ZIndex 가 1 이상이어야 Z 범위를 쓸 수 있어 Z 범위 기능이 꺼집니다."`; ZIndexEnd 가 ZIndex 이하면 `"Z 범위 끝(" + ZIndexEnd + ")이 ZIndex(" + ZIndex + ") 이하라 Z 범위 기능이 꺼집니다."`; z 개수가 MAX_Z_RANGE_COUNT 초과면 `"Z 범위가 " + 개수 + "개로 최대 " + MAX_Z_RANGE_COUNT + "개를 넘어 Z 범위 기능이 꺼집니다."`; 나머지 string.Empty.
- `private void WarnZIndexEndChanged()` — _suppressUserEditWarning 이면 return, `string szMessage = BuildZRangeMisconfigText();` 가 비어 있으면 `Parent as InspectionSequence` 가 null 이 아닐 때 `owner.BuildZRangeConflictText(this)` 로 채움, 그래도 비면 return, `ReringProject.UI.CustomMessageBox.Show("Z 범위 확인", szMessage, System.Windows.MessageBoxImage.Warning, true, false);`(DatumConfig 와 같은 호출 형태). 저장은 막지 않는다.
- 77-01 ZIndexEnd setter 의 RaisePropertyChanged 다음 줄에 `WarnZIndexEndChanged();`.
- Load 오버라이드: `bool result = base.Load(loadFile, groupName);` 바로 앞 줄 `_suppressUserEditWarning = true;`, 바로 뒤 줄 `_suppressUserEditWarning = false;`(기존 줄 무수정 — 예외 시 플래그가 남아도 경고 UX 만 영향). CopyTo: `bool result = base.CopyTo(param);` 바로 앞에 target 이 null 이 아니면 `target._suppressUserEditWarning = true;`, 바로 뒤에 target 이 null 이 아니면 false.

(3) Action_FAIMeasurement.cs — 오설정 런타임 로그(리서치 Pitfall 4): 필드 `private bool _bZRangeMisconfigLogged = false;`, RunInit 의 77-01·77-02 삽입 줄들 다음에 `_bZRangeMisconfigLogged = false;`. RunMeasure 의 첫 LogSeqStep("Measure", ...) 문장(두 줄에 걸침) 다음 줄에 `LogZRangeMisconfigIfNeeded();`. 새 메서드: ShotParam null·이미 기록이면 return; 기록 표시; `ShotParam.IsZRangeMisconfigured()` 면 ELogType.Error `[ZFocus] 설정 확인 — <Shot>: <BuildZRangeMisconfigText> (기존 1장 경로로 측정)`; 켜진 범위이고 부모 시퀀스의 BuildZRangeConflictText(ShotParam) 가 비어 있지 않으면 ELogType.Algorithm `[ZFocus] 설정 확인 — <Shot>: <문구>`.

(4) 커밋: `git add` 로 세 파일만. 메시지 예: feat(77-02): Z 범위 겹침 z 제외·$PREP 3패스·ZIndexEnd 편집 경고.

CLAUDE.md 하드룰 동일(Task 1 참조). InspectionSequence 는 Allman, ShotConfig Load/CopyTo 는 K&R — 각 메서드 스타일 유지. MainView.xaml.cs 수정 금지(경고는 모델 세터의 기존 CustomMessageBox 패턴).
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
BASE=$(git log --format=%H -1 -- .planning/phases/77-side-z-focus-select/77-02-PLAN.md)
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > "$TEMP/p77-02b.log" 2>&1; echo "msbuild_exit=$?"
echo "errors=$(grep -cE ': error [A-Z]+[0-9]+' "$TEMP/p77-02b.log")"
IS=WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
SC=WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
AF=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
OWN=$(tr -d '\r' < $IS | awk '/public bool DoesShotOwnZRangeIndex\(/,/^        }$/')
echo "reserveguard=$(printf '%s\n' "$OWN" | grep -c 'IsZIndexReservedOutsideZRange(shot, nZIndex)') baseorder=$(printf '%s\n' "$OWN" | awk '/nZIndex == shot.ZIndex/{if(a==0){a=NR}} /IsZIndexReservedOutsideZRange/{if(b==0){b=NR}} END{if(a>0 && b>a){print 1}else{print 0}}')"
echo "norecurse=$(tr -d '\r' < $IS | awk '/private bool IsZIndexReservedOutsideZRange\(/,/^        }$/' | grep -c 'DoesShotOwnZRangeIndex(')"
echo "prep3=$(tr -d '\r' < $IS | awk '/private ShotConfig FindShotByZIndex\(/,/^        }$/' | grep -c 'DoesShotOwnZRangeIndex(shot, nZIndex)') preplog=$(grep -c '\[PREP ZRange\]' $IS) declared=$(tr -d '\r' < $IS | awk '/private void AddShotDeclaredZIndices\(/,/^        }$/' | grep -c 'BuildZRangeCandidateIndices(shot)') conflict=$(grep -c 'public string BuildZRangeConflictText(ShotConfig shot)' $IS)"
echo "warnfn=$(grep -c 'private void WarnZIndexEndChanged()' $SC) warncall=$(tr -d '\r' < $SC | grep -vE '^\s*//' | grep -c 'WarnZIndexEndChanged();') suppress=$(tr -d '\r' < $SC | grep -vE '^\s*//' | grep -c '_suppressUserEditWarning = ') conflictcall=$(grep -c 'BuildZRangeConflictText(this)' $SC) misfn=$(grep -cE 'public bool IsZRangeMisconfigured\(\)|public string BuildZRangeMisconfigText\(\)' $SC)"
echo "misconflog=$(tr -d '\r' < $AF | awk '/private void RunMeasure\(\)/,/^        }$/' | grep -c 'LogZRangeMisconfigIfNeeded();')"
for F in $IS $SC $AF; do
  ADD=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+')
  echo "$F ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk')"
done
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`, `errors=0`
    - `reserveguard=1`, `baseorder=1`(기준 Z true 가드가 제외 가드보다 먼저), `norecurse=0`
    - `prep3=1`, `preplog=1`, `declared=1`, `conflict=1`
    - `warnfn=1`, `warncall=1`, `suppress=4`(Load 2 + CopyTo 2), `conflictcall=1`, `misfn=2`
    - `misconflog=1`
    - 세 파일 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0`
    - `commit_files` 가 정확히 세 파일
  </acceptance_criteria>
  <done>다른 Shot·Datum·다른 범위가 쓰는 z 는 범위 Shot 의 라우팅·저장·후보에서 빠지고, 범위 z 의 $PREP 조명은 범위 Shot 설정으로 켜진다. ZIndexEnd 오입력·겹침은 편집 즉시 한국어 한 줄로 경고되고(로드·붙여넣기 제외), 런타임 오설정은 tick 마다 Error 로그로 드러난다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레시피 INI·속성창 편집 → ZIndexEnd / ZIndex | 오입력·겹침이 들어올 수 있다 |
| PLC z_index → 범위 z 소유 판정 → 조명·촬영·후보 | 다른 Shot 의 z 가 후보로 섞이면 틀린 사진이 선택된다 |
| 후보 점수 → 채택 결과 → PLC P/F | 동점 흔들림이 측정값을 튀게 할 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-77-07 | Spoofing | 다른 Shot·Datum 조명으로 찍힌 z 사진이 후보로 채택 | high | mitigate | IsZIndexReservedOutsideZRange 가드를 DoesShotOwnZRangeIndex 한 곳에 넣어 라우팅·저장·후보가 같은 규칙(`reserveguard=1`, `baseorder=1`), 재귀 없음(`norecurse=0`), 편집 경고 BuildZRangeConflictText — PR-3 |
| T-77-08 | Tampering | ZIndexEnd 역순·과대·ZIndex 0 오입력이 조용히 꺼짐 | medium | mitigate | 편집 즉시 한국어 경고(`warncall=1`), 런타임 tick 마다 Error 로그(`misconflog=1`), 로드·붙여넣기 억제(`suppress=4`) |
| T-77-09 | Tampering | 동점 근처 후보 사이 선택 흔들림으로 측정값 튐 | medium | mitigate | 3% 기준 Z 규칙(`tieconst=1`, `pickcall=1`), 기준 Z 채택 시 기준 Z 자신의 결과 사용 |
| T-77-10 | Denial of Service | 0 점수 나눗셈·기준 Z 사진 교체 중 누수 | low | mitigate | best.Score ≤ 0 가드 후 비율 계산, 기준 Z 사본 finally Dispose |
| T-77-11 | Repudiation | 누락 z·미지원 타입·오설정이 기록 없이 처리 | low | mitigate | `[ZFocus] 후보 누락`, `[ZFocus] 기준 Z 사진 사용`, `[ZFocus] 설정 확인`, `[PREP ZRange]` 로그 |
| T-77-12 | Information Disclosure | 경고 문구·로그 | low | accept | z 번호·Shot 이름만, 로컬 표시 |
</threat_model>

<verification>
1. 각 태스크 Debug|x64 빌드 에러 0
2. 추가 줄 하드룰 grep 5종 0, 커밋 파일 목록 정확
3. 동점 규칙·결과 재사용(`tieuse`, `pickcall`), 미지원·표시(`baseexec`, `display`, `basesel`), 누락 경고(`missing`)
4. 겹침 제외 순서(`reserveguard`, `baseorder`, `norecurse`), $PREP 3패스(`prep3`), 편집 경고·억제(`warncall`, `suppress`)
5. BASE(77-01 이전) 기준 누적 삭제 0 은 77-05 감사가 확인. 동작 확인(동점·겹침·경고 창)은 77-06 UAT U-2, U-3, U-5
</verification>

<success_criteria>
- EdgeToLineDistance·EdgeToLineAngle 이 3% 기준 Z 동점 규칙으로 선택되고, 기준 Z 채택 시 기준 Z 결과가 그대로 쓰인다
- 다른 Shot·Datum·다른 범위의 z 는 후보가 되지 않는다
- ZIndexEnd 오입력·겹침은 편집 즉시 한국어 한 줄 경고, 런타임은 꺼짐 + 로그
</success_criteria>

<output>
완료 후 `.planning/phases/77-side-z-focus-select/77-02-SUMMARY.md` 작성. 포함: 태스크별 커밋 해시·빌드 결과, BASE 대비 `-` 줄 목록(전부 77-01 추가 줄인지), 새 심볼, 경고 문구 원문 3종 + 겹침 문구, 동점 규칙 경계(≤ 3.0%) 확인 메모.
</output>
