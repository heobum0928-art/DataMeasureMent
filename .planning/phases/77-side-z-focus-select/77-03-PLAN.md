---
phase: 77-side-z-focus-select
plan: 03
type: execute
wave: 2
depends_on: ["77-01"]
files_modified:
  - WPF_Example/UI/ViewModel/CycleResultDto.cs
  - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs
  - WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs
  - WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs
  - WPF_Example/UI/ViewModel/MeasurementResultRow.cs
  - WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs
  - WPF_Example/UI/ContentItem/MainView.xaml
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml
  - WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
autonomous: true
requirements: [SZF-04, SZF-05]
user_setup: []

must_haves:
  truths:
    - "검사 결과 그리드(MainView)와 리뷰어 그리드에 '선택Z' 열이 생기고, 범위 Shot 에서 선택된 측정은 'z5' 처럼 한 칸, 범위를 안 쓴 측정은 빈칸이다. 점수는 어느 화면·파일에도 나오지 않고 Algorithm 로그에만 있다 (SZF-04, D-77-07 ⑥)"
    - "cycle.json 의 측정 항목에 SelectedZIndex(int) 가 기록되고, 일자별 CSV 맨 뒤에 '선택Z' 열(값 z5 / 빈칸)이 붙는다 (SZF-04, O-7)"
    - "[edge SZF-04 boundary] SelectedZIndex 가 -1(미적용)이나 1 미만이면 화면·CSV 모두 빈칸이고, 1 이상이면 'z'+숫자다 — 형식은 MeasurementBase.FormatSelectedZ / ParseSelectedZ 한 곳에서만 정한다"
    - "[edge SZF-04 adjacency] 새 CSV 열은 기존 '검사구분' 뒤(인덱스 15)에만 붙고 앞 열 위치는 그대로다. 로더의 COLUMN_COUNT(14) 는 바꾸지 않아 14·15열 옛 파일과, 헤더는 옛것이고 행만 16열인 같은 날짜 파일도 손상 행으로 걸러지지 않는다"
    - "[edge SZF-04 empty] SelectedZIndex 가 없는 옛 cycle.json 은 -1 로 로드되어 빈칸, 선택Z 열이 없는 옛 CSV 행도 -1 로 읽힌다 — 크래시·예외 없음"
    - "[edge SZF-04 ordering] 선택Z 열 위치는 헤더 이름이 아니라 고정 인덱스 COL_SELECTED_Z=15 로 읽는다(기존 로더 규약과 같음)"
    - "[edge SZF-04 precision] 점수는 CSV·cycle.json 에 저장하지 않으므로 기록물에 반올림 계약이 없다 — 선택 Z 는 정수 한 칸이다"
    - "Z_RANGE_PENDING 측정은 tick 요약(FillTickSummary)과 리뷰어 목록 라벨의 '이 tick 에서 처리됨'·NG 집계에서 CROSS_Z_INCOMPLETE 와 같이 제외되고, 리뷰어 판정 칸에는 'Z 범위 대기' 로 표시된다 — 중간 z tick 이 NG tick 으로 보이지 않는다"
    - "측정 오버레이 라벨(선택된 측정을 강조할 때 나오는 이름)에 선택 Z 가 있으면 이름 뒤에 ' z5' 가 붙는다 — 강조 대상 비교(HighlightMeasurementName)는 기존 MeasurementName 그대로라 깨지지 않는다 (O-7)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs"
      provides: "CSV 선택Z 열"
      contains: "MapSelectedZ"
    - path: "WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs"
      provides: "선택Z 옵션 열 파싱"
      contains: "private const int COL_SELECTED_Z = 15;"
    - path: "WPF_Example/UI/ViewModel/MeasurementResultRow.cs"
      provides: "결과 그리드 선택Z 칸"
      contains: "SelectedZText"
    - path: "WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs"
      provides: "오버레이 선택 Z 라벨"
      contains: "public string SelectedZLabel { get; set; }"
  key_links:
    - from: "CycleResultSerializer.BuildDto"
      to: "MeasurementBase.LastSelectedZIndex"
      via: "measDto.SelectedZIndex = meas.LastSelectedZIndex"
      pattern: "measDto.SelectedZIndex = meas.LastSelectedZIndex;"
    - from: "MainView.xaml 결과 DataGrid"
      to: "MeasurementResultRow.SelectedZText"
      via: "DataGridTextColumn Binding"
      pattern: "Binding=\"\\{Binding SelectedZText\\}\""
    - from: "HalconDisplayService.RenderMeasurementNameLabel"
      to: "EdgeInspectionOverlay.SelectedZLabel"
      via: "이름 WriteString 다음 이어 쓰기"
      pattern: "overlay.SelectedZLabel"
---

<objective>
선택 Z 를 운영자가 보는 곳(결과 그리드, 리뷰어, 오버레이 라벨)과 남는 곳(cycle.json, 일자별 CSV)에 한 칸으로 기록한다(D-77-07 ⑥, O-7). 점수 상세는 기록하지 않는다. 중간 z 대기 상태가 tick 판정·리뷰어 목록에서 NG 로 보이지 않게 한다.

Purpose: 범위 Shot 의 측정마다 어느 Z 사진을 썼는지 사람이 바로 확인하고, 반복 사이클 선택 분포를 CSV 로 비교할 수 있게 한다(성공 기준 1, 3).

Output: 10개 기존 파일 삽입(CSV 헤더 1줄, 오버레이 Clone 1줄만 수정). 77-02 와 파일이 겹치지 않아 같은 wave 로 병렬 실행. 오버레이 라벨 값 채우기는 Action 소유라 77-04 가 한다.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/77-side-z-focus-select/77-CONTEXT.md
@.planning/phases/77-side-z-focus-select/77-01-PLAN.md
@.planning/phases/77-side-z-focus-select/77-01-SUMMARY.md

<interfaces>
77-01 계약: `MeasurementBase.LastSelectedZIndex`(public int 필드, 기본 SELECTED_Z_NONE=-1), `MeasurementBase.FormatSelectedZ(int)`(1 미만 → 빈 문자열, 아니면 "z"+N), `MeasurementBase.ParseSelectedZ(string)`, `SkipReason.Z_RANGE_PENDING`.

기존 코드(계획 시점 라인):
- CycleResultDto.cs `:115-148` MeasurementResultDto(마지막 `VerticalImagePath`), `:186-192` ReviewerListLabelBuilder.IsHandled(`:189` bHasReason 줄)
- CycleResultSerializer.cs `:122-134` measDto 초기화(마지막 멤버 `LastErrorMessage = meas.LastErrorMessage` 쉼표 없음 → 초기화 블록은 건드리지 않고 `};` 다음에 대입), `:151` `faiDto.Measurements.Add(measDto);`, `:180` FillTickSummary bHasReason 줄
- MeasurementHistoryCsvWriter.cs `:24` CSV_HEADER(한 줄 문자열 — 유일하게 수정하는 줄), `:95-117` BuildLine(`:113` 마지막 원소 `MapRunMode(dto)`, `:114` `};`, `:116` return), `:130-137` MapJudgement(Z_RANGE_PENDING 은 LastHasResult=false 라 NO_RESULT → 로더가 통계 제외, 수정 불필요)
- MeasurementHistoryCsvLoader.cs `:122` COLUMN_COUNT=14, `:136-139` COL_RUNMODE=14 옵션 열 선례, `:368-409` BuildMeasFromRow(`:408` return meas;), `:633-656` ParseRunMode(fields.Count 가드 선례)
- MeasurementResultRow.cs `:62` MeasuredValueText, `:68-76` Refresh
- ReviewMeasurementRow.cs `:29` ResultDisplay, `:84` ResultDisplay 대입, `:95-102` CROSS_Z_INCOMPLETE → MEASURE_FAIL else-if 사슬
- MainView.xaml `:625` `<DataGridTextColumn Header="측정값" Binding="{Binding MeasuredValueText}" Width="2*"/>`
- ReviewerWindow.xaml `:208` `<DataGridTextColumn Header="측정값"  Binding="{Binding ResultDisplay}"  Width="75"/>`
- EdgeInspectionOverlay.cs `:50` MeasurementName, `:62-77` Clone(초기화 마지막 `LineColumn2 = LineColumn2` 쉼표 없음)
- HalconDisplayService.cs `:526-548` RenderMeasurementNameLabel(`:541` `HOperatorSet.WriteString(window, overlay.MeasurementName);` — HALCON write_string 은 출력 뒤로 커서가 이동한다)
</interfaces>
</context>

## Artifacts this phase produces

phase 전체 원장은 `77-01-PLAN.md` 'Artifacts this phase produces' 표. 이 plan 이 만드는 것:

| 심볼 / 산출물 | 종류 | 파일 |
|---|---|---|
| `MeasurementResultDto.SelectedZIndex`(기본 -1, cycle.json 키 `SelectedZIndex`) | 프로퍼티 | CycleResultDto.cs |
| CSV 헤더 끝 `,선택Z`, `MapSelectedZ(MeasurementResultDto)` | CSV 열/메서드 | MeasurementHistoryCsvWriter.cs |
| `COL_SELECTED_Z`(15), `ParseSelectedZIndex(List<string>)` | const/메서드 | MeasurementHistoryCsvLoader.cs |
| `SelectedZText` | VM 프로퍼티 | MeasurementResultRow.cs, ReviewMeasurementRow.cs |
| 판정 라벨 `JUDGE_Z_RANGE_PENDING` = "Z 범위 대기" | const | ReviewMeasurementRow.cs |
| DataGrid 열 Header="선택Z" | XAML | MainView.xaml, ReviewerWindow.xaml |
| `EdgeInspectionOverlay.SelectedZLabel` | 프로퍼티(JSON 포함) | EdgeInspectionOverlay.cs |
| `LABEL_TEXT_SEPARATOR` | const | HalconDisplayService.cs |

## 계획 단계 결정 (이 plan)

| # | 결정 | 근거 |
|---|---|---|
| R-1 | CSV 열 이름은 한국어 `선택Z`, 값은 `z5` / 빈칸 — 기존 `검사구분` 처럼 사람이 엑셀에서 바로 읽는 표기 | D-77-07 ⑥. reversibility costly(아래 Task 1) |
| R-2 | 결과 그리드는 기존 `MeasurementResultRow` VM 에 프로퍼티만 추가하고 XAML 은 열 한 줄 삽입 — MainView.xaml.cs 수정 없음 | CLAUDE.md MVVM 규칙 |
| R-3 | 오버레이 라벨은 MeasurementName 에 섞지 않고 별도 SelectedZLabel — HighlightMeasurementName 문자열 비교를 깨지 않기 위해 | HalconDisplayService.cs:531-532, EdgeInspectionOverlay.cs:46-49 RoiId 분리 선례 |

<tasks>

<task type="auto">
  <name>Task 1: 기록 — cycle.json 측정 항목 SelectedZIndex, CSV '선택Z' 열 쓰기·읽기, 대기 상태 tick 집계 제외</name>
  <reversibility rating="costly">CSV 열 순서·이름은 현장 PC 에 일자별 파일로 누적되고 사용자가 엑셀로 가공한다 — 한 번 배포 후 열 위치를 옮기면 이미 쌓인 파일과 어긋난다. 코드 되돌리기는 쉽지만 누적 파일은 되돌릴 수 없다.</reversibility>
  <files>WPF_Example/UI/ViewModel/CycleResultDto.cs, WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs, WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs, WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs</files>
  <read_first>
    - CLAUDE.md (Code Style 하드룰)
    - WPF_Example/UI/ViewModel/CycleResultDto.cs:1-200
    - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs:60-200
    - WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs 전체
    - WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs:115-145, :240-260, :360-410, :625-660
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs 의 77-01 FormatSelectedZ / ParseSelectedZ
  </read_first>
  <action>
(1) CycleResultDto.cs: MeasurementResultDto 의 `VerticalImagePath` 프로퍼티 아래에 XML 요약(범위 Shot 에서 이 측정이 채택한 z 번호, -1 = 범위 미적용·옛 JSON)과 `public int SelectedZIndex { get; set; } = -1;` 삽입 — 초기값은 리터럴 대신 `MeasurementBase.SELECTED_Z_NONE`(using ReringProject.Sequence 이미 있음). ReviewerListLabelBuilder.IsHandled 의 `bool bHasReason = ...;` 다음 줄에 LastSkipReason 이 SkipReason.Z_RANGE_PENDING 이면 `bHasReason = false;`(중괄호 필수).

(2) CycleResultSerializer.cs: measDto 초기화 블록(`};`) 다음 줄에 `measDto.SelectedZIndex = meas.LastSelectedZIndex;`. FillTickSummary 의 `bool bHasReason = ...;` 다음 줄에 같은 Z_RANGE_PENDING 제외(`bHasReason = false;`). 초기화 블록 내부·기존 줄은 고치지 않는다.

(3) MeasurementHistoryCsvWriter.cs (D-77-07 ⑥): `CSV_HEADER` 문자열 끝에 `,선택Z` 를 붙인다 — 이 plan 에서 기존 줄을 수정하는 유일한 곳(삭제 1줄 허용). 위 주석 블록에 한 줄 추가: 선택Z 는 검사구분 뒤 맨 끝, 로더는 인덱스 15 옵션 열로 읽는다. BuildLine 의 `var fields = new List<string> { ... };` 다음 줄에 `fields.Add(MapSelectedZ(meas));` 삽입(초기화 블록 무수정). MapRunMode 아래에 `private static string MapSelectedZ(MeasurementResultDto meas)` — meas 가 null 이면 string.Empty, 아니면 `MeasurementBase.FormatSelectedZ(meas.SelectedZIndex)`(형식 규칙 복제 금지 — 단일 소스). MapJudgement 는 고치지 않는다(대기 측정은 LastHasResult=false → NO_RESULT → 통계 제외).

(4) MeasurementHistoryCsvLoader.cs: COL_RUNMODE 선언 아래에 주석(선택Z 열 도입 전 파일은 이 인덱스가 없다 — fields.Count 확인 후 읽고 없으면 -1, COLUMN_COUNT 는 14 유지)과 `private const int COL_SELECTED_Z = 15;`. ParseRunMode 아래에 `private static int ParseSelectedZIndex(List<string> fields)` — `bool bHasColumn = fields.Count > COL_SELECTED_Z;` 가 false 면 MeasurementBase.SELECTED_Z_NONE, 아니면 `MeasurementBase.ParseSelectedZ(fields[COL_SELECTED_Z])`. BuildMeasFromRow 의 `return meas;` 바로 앞 줄에 `meas.SelectedZIndex = ParseSelectedZIndex(fields);`. COLUMN_COUNT·기존 COL_ 상수 무수정.

(5) 커밋: `git add` 로 네 파일만. 메시지 예: feat(77-03): 선택 Z 기록 — cycle.json SelectedZIndex, CSV 선택Z 열.

CLAUDE.md 하드룰(새 줄): 삼항·null 병합·null 조건 연산자·switch 식 금지, 한 줄 분기 중괄호, &&/|| 3개 이상 금지, 헝가리언, 매직넘버 금지(15 는 COL_SELECTED_Z, -1 은 SELECTED_Z_NONE), 날짜·이니셜 서명 주석 금지, 새 줄에 물음표 문자 금지, 파일별 brace 스타일 유지.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
BASE=$(git log --format=%H -1 -- .planning/phases/77-side-z-focus-select/77-03-PLAN.md)
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > "$TEMP/p77-03a.log" 2>&1; echo "msbuild_exit=$?"
echo "errors=$(grep -cE ': error [A-Z]+[0-9]+' "$TEMP/p77-03a.log")"
DT=WPF_Example/UI/ViewModel/CycleResultDto.cs
SE=WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs
CW=WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs
CL=WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs
echo "dtoprop=$(grep -c 'public int SelectedZIndex { get; set; }' $DT) dtopending=$(tr -d '\r' < $DT | awk '/private static bool IsHandled\(/,/^        }$/' | grep -c 'Z_RANGE_PENDING')"
echo "sercopy=$(grep -c 'measDto.SelectedZIndex = meas.LastSelectedZIndex;' $SE) serpending=$(tr -d '\r' < $SE | awk '/private static void FillTickSummary\(/,/^        }$/' | grep -c 'Z_RANGE_PENDING')"
echo "header=$(grep -c '검사구분,선택Z";' $CW) mapfn=$(grep -c 'private static string MapSelectedZ(MeasurementResultDto meas)' $CW) mapadd=$(tr -d '\r' < $CW | awk '/private static string BuildLine\(/,/^        }$/' | grep -c 'fields.Add(MapSelectedZ(meas));') fmtreuse=$(tr -d '\r' < $CW | awk '/private static string MapSelectedZ\(/,/^        }$/' | grep -c 'MeasurementBase.FormatSelectedZ(')"
echo "colconst=$(grep -c 'private const int COL_SELECTED_Z = 15;' $CL) colcount=$(grep -c 'private const int COLUMN_COUNT = 14;' $CL) parseguard=$(tr -d '\r' < $CL | awk '/private static int ParseSelectedZIndex\(/,/^        }$/' | grep -c 'fields.Count > COL_SELECTED_Z') parsecall=$(tr -d '\r' < $CL | awk '/private static MeasurementResultDto BuildMeasFromRow\(/,/^        }$/' | grep -c 'ParseSelectedZIndex(fields)')"
for F in $DT $SE $CW $CL; do
  ADD=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+')
  echo "$F deleted=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk')"
done
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`, `errors=0`
    - `dtoprop=1 dtopending=1 sercopy=1 serpending=1`
    - `header=1 mapfn=1 mapadd=1 fmtreuse=1`
    - `colconst=1 colcount=1 parseguard=1 parsecall=1`
    - `deleted`: CycleResultDto.cs=0, CycleResultSerializer.cs=0, MeasurementHistoryCsvLoader.cs=0, MeasurementHistoryCsvWriter.cs=1(CSV_HEADER 줄만)
    - 네 파일 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0`
    - `commit_files` 가 정확히 네 파일
  </acceptance_criteria>
  <done>cycle.json 과 CSV 에 측정별 선택 Z 한 칸이 기록되고, 옛 JSON·CSV 는 -1 로 안전하게 읽히며, 중간 z 대기 측정은 tick 판정·리뷰어 목록 NG 집계에서 빠진다.</done>
</task>

<task type="auto">
  <name>Task 2: 표시 — 결과 그리드·리뷰어 그리드 '선택Z' 열, 리뷰어 판정 'Z 범위 대기'</name>
  <files>WPF_Example/UI/ViewModel/MeasurementResultRow.cs, WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs, WPF_Example/UI/ContentItem/MainView.xaml, WPF_Example/UI/Reviewer/ReviewerWindow.xaml</files>
  <read_first>
    - CLAUDE.md (MVVM 규칙 — code-behind 에 로직 금지)
    - WPF_Example/UI/ViewModel/MeasurementResultRow.cs 전체
    - WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs 전체
    - WPF_Example/UI/ContentItem/MainView.xaml:600-630
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml:195-212
  </read_first>
  <action>
(1) MeasurementResultRow.cs: MeasuredValueText 아래에 `public string SelectedZText` — getter 는 `MeasurementBase.FormatSelectedZ(_measurement.LastSelectedZIndex)`(여러 줄 get 블록, 이 파일의 한 줄 getter 삼항·중괄호 생략 스타일을 복사하지 않는다). Refresh 의 마지막 RaisePropertyChanged 다음 줄에 `RaisePropertyChanged("SelectedZText");`.

(2) ReviewMeasurementRow.cs: JUDGE_MEASURE_FAIL 아래에 `public const string JUDGE_Z_RANGE_PENDING = "Z 범위 대기";`. ResultDisplay 프로퍼티 아래에 XML 요약과 `public string SelectedZText { get; set; }`. 생성자에서 ResultDisplay 대입 줄 다음 줄에 `SelectedZText = MeasurementBase.FormatSelectedZ(m.SelectedZIndex);`. 판정 사슬에서 CROSS_Z_INCOMPLETE 분기의 닫는 `}` 줄과 `else if (m.LastSkipReason == SkipReason.MEASURE_FAIL)` 줄 사이에 `else if (m.LastSkipReason == SkipReason.Z_RANGE_PENDING)` 분기(JudgeText = JUDGE_Z_RANGE_PENDING)를 끼워 넣는다 — 기존 줄 무수정.

(3) MainView.xaml: `Header="측정값"` DataGridTextColumn 줄 바로 다음 줄에 `<DataGridTextColumn Header="선택Z" Binding="{Binding SelectedZText}" Width="Auto"/>`. MainView.xaml.cs 는 열지도 고치지도 않는다.

(4) ReviewerWindow.xaml: `Header="측정값"` 줄 바로 다음 줄에 `<DataGridTextColumn Header="선택Z"  Binding="{Binding SelectedZText}"  Width="50"/>`. code-behind 무수정.

(5) 커밋: `git add` 로 네 파일만. 메시지 예: feat(77-03): 결과·리뷰어 그리드 선택Z 열.

하드룰 동일. 새 C# 줄에 삼항·null 병합·null 조건 연산자 금지, 중괄호 필수, 물음표 문자 금지.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
BASE=$(git log --format=%H -1 -- .planning/phases/77-side-z-focus-select/77-03-PLAN.md)
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > "$TEMP/p77-03b.log" 2>&1; echo "msbuild_exit=$?"
echo "errors=$(grep -cE ': error [A-Z]+[0-9]+' "$TEMP/p77-03b.log")"
MR=WPF_Example/UI/ViewModel/MeasurementResultRow.cs
RR=WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs
MX=WPF_Example/UI/ContentItem/MainView.xaml
RX=WPF_Example/UI/Reviewer/ReviewerWindow.xaml
echo "mrprop=$(grep -c 'public string SelectedZText' $MR) mrraise=$(grep -c 'RaisePropertyChanged("SelectedZText");' $MR) mrfmt=$(grep -c 'MeasurementBase.FormatSelectedZ(_measurement.LastSelectedZIndex)' $MR)"
echo "rrprop=$(grep -c 'public string SelectedZText { get; set; }' $RR) rrassign=$(grep -c 'SelectedZText = MeasurementBase.FormatSelectedZ(m.SelectedZIndex);' $RR) rrlabel=$(grep -c 'JudgeText = JUDGE_Z_RANGE_PENDING;' $RR)"
echo "mxcol=$(grep -c 'Header="선택Z" Binding="{Binding SelectedZText}"' $MX) rxcol=$(grep -cE 'Header="선택Z" +Binding="\{Binding SelectedZText\}"' $RX)"
echo "codebehind=$(git diff --name-only "$BASE" -- WPF_Example/UI/ContentItem/MainView.xaml.cs WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs | wc -l)"
for F in $MR $RR $MX $RX; do
  ADD=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+')
  echo "$F deleted=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk')"
done
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`, `errors=0`
    - `mrprop=1 mrraise=1 mrfmt=1`, `rrprop=1 rrassign=1 rrlabel=1`, `mxcol=1 rxcol=1`
    - `codebehind=0` (MainView.xaml.cs / ReviewerWindow.xaml.cs 무수정)
    - 네 파일 `deleted=0`, `ternary=0 coalesce=0 nullcond=0 datesig=0`
    - `commit_files` 가 정확히 네 파일
  </acceptance_criteria>
  <done>결과 그리드와 리뷰어 그리드에 측정별 선택 Z 한 칸이 보이고, 중간 z 대기 측정은 리뷰어에서 'Z 범위 대기' 로 보인다. code-behind 변경 없음.</done>
</task>

<task type="auto">
  <name>Task 3: 오버레이 라벨 — EdgeInspectionOverlay.SelectedZLabel 과 강조 라벨 뒤 ' z5' 출력</name>
  <files>WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs, WPF_Example/Halcon/Display/HalconDisplayService.cs</files>
  <read_first>
    - CLAUDE.md
    - WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs 전체
    - WPF_Example/Halcon/Display/HalconDisplayService.cs:500-560 (RenderMeasurementNameLabel, LABEL_ROW_OFFSET 등 상수 위치)
  </read_first>
  <action>
(1) EdgeInspectionOverlay.cs: MeasurementName 프로퍼티 아래에 주석(표시 전용 선택 Z 라벨 'z5' — MeasurementName 에 섞지 않는 이유: 강조 대상 문자열 비교가 깨짐, 옛 cycle.json 은 null → 라벨 없음)과 `public string SelectedZLabel { get; set; }`. Clone 의 객체 초기화에 `SelectedZLabel = SelectedZLabel` 를 넣는다 — 마지막 멤버 `LineColumn2 = LineColumn2` 줄 끝에 쉼표를 붙이고 다음 줄에 추가(이 plan 에서 기존 줄을 수정하는 두 번째이자 마지막 곳, 삭제 1줄 허용).

(2) HalconDisplayService.cs: 클래스 상수 영역(LABEL_ROW_OFFSET 근처)에 `private const string LABEL_TEXT_SEPARATOR = " ";`. RenderMeasurementNameLabel 의 `HOperatorSet.WriteString(window, overlay.MeasurementName);` 다음 줄에 `bool bHasSelectedZ = !string.IsNullOrEmpty(overlay.SelectedZLabel);` 와 참이면 `HOperatorSet.WriteString(window, LABEL_TEXT_SEPARATOR + overlay.SelectedZLabel);`(write_string 은 앞 문자열 뒤에 이어 쓴다). 강조 비교 줄·기존 줄 무수정.

(3) 커밋: `git add` 로 두 파일만. 메시지 예: feat(77-03): 측정 오버레이 선택 Z 라벨.

하드룰 동일(새 줄 물음표 문자 금지, 중괄호 필수).
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
BASE=$(git log --format=%H -1 -- .planning/phases/77-side-z-focus-select/77-03-PLAN.md)
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > "$TEMP/p77-03c.log" 2>&1; echo "msbuild_exit=$?"
echo "errors=$(grep -cE ': error [A-Z]+[0-9]+' "$TEMP/p77-03c.log")"
EO=WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs
HD=WPF_Example/Halcon/Display/HalconDisplayService.cs
echo "prop=$(grep -c 'public string SelectedZLabel { get; set; }' $EO) clone=$(tr -d '\r' < $EO | awk '/public EdgeInspectionOverlay Clone\(\)/,/^        }$/' | grep -c 'SelectedZLabel = SelectedZLabel')"
echo "label=$(tr -d '\r' < $HD | awk '/private void RenderMeasurementNameLabel\(/,/^        }$/' | grep -c 'LABEL_TEXT_SEPARATOR + overlay.SelectedZLabel') highlight=$(tr -d '\r' < $HD | awk '/private void RenderMeasurementNameLabel\(/,/^        }$/' | grep -c 'string.Equals(overlay.MeasurementName, HighlightMeasurementName')"
for F in $EO $HD; do
  ADD=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+')
  echo "$F deleted=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk')"
done
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`, `errors=0`
    - `prop=1 clone=1 label=1 highlight=1`(강조 비교 줄 그대로)
    - `deleted`: EdgeInspectionOverlay.cs=1(LineColumn2 줄 쉼표), HalconDisplayService.cs=0
    - 두 파일 `ternary=0 coalesce=0 nullcond=0 datesig=0`, `commit_files` 가 정확히 두 파일
  </acceptance_criteria>
  <done>오버레이에 선택 Z 라벨 필드가 생기고 사본·cycle.json 으로 전달되며, 강조된 측정 이름 뒤에 ' z5' 가 표시될 수 있다(값 채우기는 77-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 디스크의 옛·수기 편집 CSV / cycle.json → 로더 | 열 개수·값 형식이 제각각이다 |
| 측정 결과 → 운영자 화면·통계 파일 | 잘못 표시되면 운영 판단이 틀린다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-77-13 | Tampering | MeasurementHistoryCsvLoader — 열 부족·'z' 아닌 값 | medium | mitigate | fields.Count > COL_SELECTED_Z 가드(`parseguard=1`), ParseSelectedZ 실패 시 -1, COLUMN_COUNT 14 유지(`colcount=1`) |
| T-77-14 | Denial of Service | 옛 파일 로드 시 인덱스 예외로 통계 화면 중단 | medium | mitigate | 위 가드 + JSON 기본값 -1 |
| T-77-15 | Repudiation | 중간 z 대기가 NG tick 으로 기록되어 원인 추적 혼선 | low | mitigate | FillTickSummary·IsHandled 제외(`serpending`, `dtopending`), 리뷰어 'Z 범위 대기' 라벨 |
| T-77-16 | Information Disclosure | CSV·화면에 선택 Z 번호 노출 | low | accept | 공정 정보, 로컬 저장 |
</threat_model>

<verification>
1. 태스크마다 Debug|x64 빌드 에러 0, 커밋 파일 정확
2. 삭제 허용: MeasurementHistoryCsvWriter.cs 1줄(CSV_HEADER), EdgeInspectionOverlay.cs 1줄(Clone 쉼표), 나머지 0
3. 기록 배선(`sercopy`, `mapadd`, `parsecall`), 표시 배선(`mxcol`, `rxcol`, `label`), 대기 제외(`serpending`, `dtopending`, `rrlabel`)
4. MainView.xaml.cs·ReviewerWindow.xaml.cs 무수정(`codebehind=0`)
5. 옛 CSV·cycle.json 로드 호환과 화면 확인은 77-06 UAT U-6
</verification>

<success_criteria>
- 결과 그리드·리뷰어·CSV·cycle.json 에 측정별 선택 Z 한 칸, 점수는 로그에만
- 옛 파일은 빈칸으로 안전하게 읽힘, 중간 z 대기는 NG tick 으로 보이지 않음
</success_criteria>

<output>
완료 후 `.planning/phases/77-side-z-focus-select/77-03-SUMMARY.md` 작성. 포함: 태스크별 커밋 해시·빌드 결과·파일별 삭제 수(허용 2줄 명시), 새 CSV 헤더 전문, SelectedZLabel 이 77-04 에서 채워져야 한다는 인계 메모.
</output>
