---
phase: 77-side-z-focus-select
plan: 04
type: execute
wave: 3
depends_on: ["77-02", "77-03"]
files_modified:
  - WPF_Example/Setting/SystemSetting.cs
  - WPF_Example/Utility/CaptureImageSaveService.cs
  - WPF_Example/UI/ViewModel/CycleResultDto.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - WPF_Example/Utility/RecipeFileHelper.cs
  - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs
autonomous: true
requirements: [SZF-02, SZF-03, SZF-04, SZF-05]
user_setup: []

must_haves:
  truths:
    - "설정 창 System|Enviroment 에 체크박스 'SaveZRangeCandidateImages' 1개가 생기고 기본은 꺼짐이다 — Setting.ini 에 키가 없으면 꺼짐으로 로드된다 (D-77-06 ③, D-77-07 ④)"
    - "체크박스를 켜고 라이브 촬영 PLC 자동 사이클을 돌리면 범위 Shot 의 후보 z 사진이 원본 저장 폴더(ResultSavePath\\Image\\yyMMdd\\HHmm\\original)에 'shotz_<시퀀스>_<Shot>_z<N>_<시각>' 이름으로 모두 저장되고, 그 tick 의 cycle.json ZRangeImages 에 ShotName·ZIndex·Path 로 기록된다 (D-77-06 ③)"
    - "기존 체크박스 AutoFillOfflineImages 가 켜진 라이브 PLC 사이클은 후보 z 사진을 오프라인 폴더에도 '<ImageSavePath>\\OfflineInspect\\<레시피>\\shot_<ShotName>_z<N>.bmp' 로 채운다 — Shot 의 SimulImagePath 는 바꾸지 않는다 (D-77-06 ②, D-77-07 ⑦)"
    - "화면 RUN(라이브 빌드)과 '수동 트리거 실행'(Sender 없는 $TEST)에서 범위 Shot 은 현재 사진 1장으로 측정하고 선택하지 않으며, 시퀀스 로그(LogSeqStep)와 Algorithm 로그에 'Z 범위 Shot — 수동은 Z 선택 안 함' 안내가 Shot tick 마다 한 번 남는다. MainView.xaml.cs 는 바뀌지 않는다 (D-77-06 ①)"
    - "오프라인 검사(OfflineInspectMode 켜짐 또는 SIMUL 빌드의 RUN)에서 범위 Shot 은 shot_<ShotName>_z<N>.bmp 파일들을 후보로 읽어 자동검사와 같은 선택 로직(점수·3% 기준 Z 동점·겹침 제외)으로 측정한다 (D-77-06 ②)"
    - "[edge SZF-02 empty] 오프라인·재검사에서 z 별 사진이 하나도 없으면 현재 사진 1장으로 측정하고 Error 로그 '[ZFocus] 후보 사진 없음' 을 남긴다. 일부만 있으면 있는 것으로 선택하고 '[ZFocus] 후보 누락' 을 남긴다 (D-77-06 ②, O-4)"
    - "저장 사이클 재검사는 cycle.json ZRangeImages 로 만든 z 별 경로만 쓰고 오프라인 폴더 사진과 섞지 않는다. 재검사가 끝나거나 중단되면 그 경로는 비워진다. z 사진이 없는 부품도 재검사 대상에서 빠지지 않고 1장 폴백 + 경고로 돈다 (D-77-06 ②)"
    - "Z_RANGE_PENDING 측정은 재검사 계획의 '처리된 Shot' 판정에서 CROSS_Z_INCOMPLETE 처럼 제외된다 — 중간 z tick 의 원본 사진이 Shot 대표 사진으로 채택되지 않는다"
    - "선택 또는 기준 Z 측정이 끝나면 그 측정 오버레이 SelectedZLabel 에 'z5' 가 들어가 강조 라벨·cycle.json 오버레이에 나타난다 (O-7, D-77-07 ⑥)"
    - {statement: "[edge SZF-02 ordering] PLC 가 ZIndexEnd 를 두 번 보내거나 중간 z 를 ZIndexEnd 뒤에 보내도 사이클이 멈추지 않고 로그로 드러난다(재평가는 남은 후보로, 늦은 z 는 다음 사이클 시작에 해제) — 실제 PLC 시나리오 확인 필요(flagged assumption A-77-E1)", verification: backstop}
  artifacts:
    - path: "WPF_Example/Setting/SystemSetting.cs"
      provides: "후보 z 사진 저장 체크박스"
      contains: "public bool SaveZRangeCandidateImages { get; set; } = false;"
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "수동 안내 + 오프라인·재검사 후보 로드 + 후보 저장 + 오버레이 라벨"
      contains: "private List<KeyValuePair<int, HImage>> LoadOfflineZRangeCandidates("
    - path: "WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs"
      provides: "재검사 부품별 z 사진 경로"
      contains: "private static void FillPartZRangePhotos(SavedCycleRerunPart part)"
  key_links:
    - from: "Action_FAIMeasurement.SaveZRangeCandidateImageIfEnabled"
      to: "InspectionSequence.RecordTickZRangeImage → CycleResultDto.ZRangeImages"
      via: "EnqueueCycleDatumImageCopy 성공 시 기록, BuildDto 직후 스냅샷 부착(2곳)"
      pattern: "cycleDto.ZRangeImages = TakeTickZRangeImagesSnapshot\\(\\);"
    - from: "RepeatRunService.ApplySavedCyclePart"
      to: "ShotConfig.RerunZRangeImagePaths → Action_FAIMeasurement.ResolveOfflineZRangeImagePath"
      via: "부품별 z→경로 사전 주입, null 이 아니면 오프라인 폴더 폴백 금지"
      pattern: "RerunZRangeImagePaths"
    - from: "Action_FAIMeasurement.EnsureZRangeCandidatesLoaded"
      to: "Action_FAIMeasurement.LoadOfflineZRangeCandidates"
      via: "모드가 OfflineSelect 면 저장소 대신 파일 로드"
      pattern: "LoadOfflineZRangeCandidates\\(parentSeq2\\)"
  prohibitions:
    - statement: "MUST NOT 체크박스가 꺼져 있는데 후보 z 사진을 결과 폴더에 저장하지 않고, 재검사·오프라인·SIMUL 이 이 설정을 켜거나 저장을 대신하지 않는다 — SIDE 한 장 약 127MB × z 개수로 디스크를 조용히 채우면 안 된다"
      status: resolved
      verification: judgment
    - statement: "MUST NOT 수동 RUN·수동 트리거·오프라인·재검사에서 범위 Shot 을 Z 선택 없이(또는 1장 폴백으로) 측정하면서 운영자에게 알리지 않는 일이 없다 — 안내·경고 로그 없이 자동검사와 같은 결과처럼 보이면 안 된다"
      status: resolved
      verification: judgment
---

<objective>
D-77-06(수동검사 동작)을 구현한다: ① 라이브 수동(RUN·수동 트리거)은 1장 측정 + 안내, ② 오프라인·저장 사이클 재검사는 z 별 저장 사진으로 자동검사와 같은 선택 로직(없으면 1장 + 경고), ③ 후보 z 사진 저장 체크박스(기본 꺼짐)와 cycle.json 기록. 77-03 이 만든 오버레이 SelectedZLabel 도 여기서 채운다.

Purpose: O-1(PLC z 번호 배정) 전에도 사무실에서 저장 사진으로 선택 로직을 검증하고 동점 규칙을 조정할 수 있게 한다(D-77-06 목적). 운영자 추가 조작 없이 자동 판단 + 안내(D-77-07 ⑦).

Output: 8개 기존 파일 삽입(77-01 이 쓴 ResolveZRangeMode 두 줄은 이 plan 에서 모드 분기로 교체 — BASE 이전 줄 삭제 0).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/77-side-z-focus-select/77-CONTEXT.md
@.planning/phases/77-side-z-focus-select/77-01-PLAN.md
@.planning/phases/77-side-z-focus-select/77-01-SUMMARY.md
@.planning/phases/77-side-z-focus-select/77-02-SUMMARY.md
@.planning/phases/77-side-z-focus-select/77-03-SUMMARY.md

<interfaces>
77-01~03 계약: `EZRangeMode{Off,AutoPending,AutoCompletion,ManualSingle,OfflineSelect}`, `ResolveZRangeMode`(비프로토콜 → Off, 수동 트리거 → Off 두 가드), `StoreZRangeCandidateImage`(저장 직후 지점), `TryHandleZRangeMeasurement`, `EnsureZRangeCandidatesLoaded`, `ExecuteZRangeSelection`(후보 0 → '[ZFocus] 후보 사진 없음' Error + false), `ExecuteZRangeBaseImageMeasurement`, `_nZRangeDisplayZIndex`, `IsManualTriggerCycle()`, `BuildZRangeCandidateIndices`, `EdgeInspectionOverlay.SelectedZLabel`, `MeasurementBase.FormatSelectedZ`, `SkipReason.Z_RANGE_PENDING`.

기존 코드(계획 시점 라인):
- Setting/SystemSetting.cs `:212-217` AutoFillOfflineImages(`[Category("System|Enviroment")]`, PropertyTools Category, 키 누락 → false)
- Utility/CaptureImageSaveService.cs `:392-407` DATUM_PREFIX + BuildDatumFileName(szSequence, szDatumName, szRole, ts), `:411-417` ResolveOriginImageExtension, `:468-470` BuildFilePath, `SanitizeFilePart`(private static)
- Utility/RecipeFileHelper.cs `:170-180` RecipeFiles.OFFLINE_* 상수, `:192-209` BuildOfflineImagePath(szBaseName, out szError) → `<ImageSavePath>\OfflineInspect\<recipe>\<이름>.bmp`
- UI/ViewModel/CycleResultDto.cs `:54-57` DatumImages, `:63-75` DatumImageRecordDto
- InspectionSequence.cs `:57-60` _tickDatumImageLock/_tickDatumImages, `:269` 과 `:2393` `cycleDto.DatumImages = TakeTickDatumImagesSnapshot();`, `:436` HandleRunStartResetResults 의 ClearTickDatumImages 호출, `:510-536` RecordTickDatumImage / ClearTickDatumImages / TakeTickDatumImagesSnapshot
- Action_FAIMeasurement.cs `:970-982` LoadShotInspectionImage, `:1443-1457` IsOfflineAutoFillEnabled / IsLiveCaptureMode(SIMUL 빌드 = false), `:1489-1505` EnqueueOfflineImageCopy(src, szBaseName), `:1510-1537` EnqueueCycleDatumImageCopy(src, szFileName, ts), `:1542-1563` ArchiveDatumImageForCycle(저장+기록 선례, try/catch 격리), LogSeqStep(string, string)
- RepeatRunService.cs `:442-453` SavedCycleOverrideSnapshot(OwnedShots), `:495-517` RestoreOverridePathsOnly(static, snap), `:704-756` ApplySavedCyclePart(ShotPhotoPaths 적용 루프 :708-715), `:1075-1088` SavedCycleRerunPart, `:1153-1209` BuildPlan(`:1175` FillPartDualPhotos 호출), `:1326-1339` IsMeasurementHandled(`:1328` bHasReason 줄), `:1341-1363` FillPartDatumPhotos(선례), `:1644-1681` ValidatePart(z 사진은 요구하지 않는다), 재검사는 OfflineInspectMode 를 켠 채 StartAll(null)(비프로토콜)
</interfaces>
</context>

## Artifacts this phase produces

phase 전체 원장은 `77-01-PLAN.md` 표. 이 plan 이 만드는 것:

| 심볼 / 산출물 | 종류 | 파일 |
|---|---|---|
| `SaveZRangeCandidateImages` (Setting.ini 키, 기본 false) | 설정 체크박스 | Setting/SystemSetting.cs |
| `ZRANGE_CANDIDATE_PREFIX`("shotz"), `ZRANGE_FILE_Z_SEGMENT`("_z"), `BuildZRangeCandidateFileName(string, string, int, DateTime)` → `shotz_<seq>_<shot>_z<N>_HHmmssfff.<jpg or bmp>` | const/메서드/파일명 | Utility/CaptureImageSaveService.cs |
| `CycleResultDto.ZRangeImages`, `ZRangeImageRecordDto`(ShotName, ZIndex, Path) | cycle.json 필드/클래스 | UI/ViewModel/CycleResultDto.cs |
| `_tickZRangeImages`, `RecordTickZRangeImage`, `TakeTickZRangeImagesSnapshot` | 필드/메서드 | InspectionSequence.cs |
| `SaveZRangeCandidateImageIfEnabled`, `AutoFillZRangeOfflineImage`, `LogZRangeManualNotice`, `_bZRangeNoticeLogged`, `LoadOfflineZRangeCandidates`, `ResolveOfflineZRangeImagePath`, `FindZRangeCandidateImage`, `ApplySelectedZLabel`, 로그 `[ZFocus] 후보 사진 저장` / `Z 범위 Shot — 수동은 Z 선택 안 함` | 메서드/필드/로그 | Action_FAIMeasurement.cs |
| `OFFLINE_SUFFIX_Z`("_z") → `shot_<ShotName>_z<N>.bmp` | const/파일 규약 | Utility/RecipeFileHelper.cs |
| `RerunZRangeImagePaths` (public 필드 Dictionary<int,string>, 기본 null = 재검사 아님) | 런타임 필드 | ShotConfig.cs |
| `SavedCycleRerunPart.ZRangePhotoPaths`, `FillPartZRangePhotos`, `BuildRerunZRangeMap` | 프로퍼티/메서드 | RepeatRunService.cs |

## 계획 단계 결정 (이 plan)

| # | 결정 | 근거 |
|---|---|---|
| M-1 | 모드 판정: PLC(Sender 있음) → Auto*, 수동 트리거(Sender 없음) → ManualSingle, 비프로토콜 + 라이브 촬영 모드 → ManualSingle, 비프로토콜 + 파일 읽기 모드(OfflineInspectMode 또는 SIMUL 빌드) → OfflineSelect | D-77-06 ①②. 재검사는 OfflineInspectMode 를 켜고 비프로토콜로 돌므로 OfflineSelect 에 포함 |
| M-2 | 오프라인 파일 규약 `shot_<ShotName>_z<N>.bmp`(기존 `shot_<ShotName>.bmp` + `_z<N>`), 같은 폴더·확장자·Sanitize 규칙 재사용 | 코드 확인: RecipeFiles.OFFLINE_PREFIX_SHOT + BuildOfflineImagePath. reversibility costly(현장 파일 이름) |
| M-3 | 재검사 경로 우선: ShotConfig.RerunZRangeImagePaths 가 null 이 아니면(재검사 중) 그 사전만 쓰고 오프라인 폴더로 폴백하지 않는다 — 다른 부품 사진이 섞이지 않게 | D-77-06 ② '그 Shot 범위의 z 별 저장 사진' |
| M-4 | 후보 저장은 라이브 촬영 + PLC 자동(Auto*) 에서만, 파일은 원본(origin) 폴더·포맷 규칙(BuildDatumFileName 과 같은 확장자) | ArchiveDatumImageForCycle 선례, PR-4 |
| M-5 | 오프라인 미지원 타입은 기준 Z 파일이 있으면 그 사진으로, 없으면 현재 사진으로 측정(각각 로그) | D-77-07 ② 를 오프라인에도 동일 적용 |

<tasks>

<task type="auto">
  <name>Task 1: 후보 z 사진 저장 체크박스(기본 꺼짐)와 cycle.json ZRangeImages 기록</name>
  <reversibility rating="costly">저장 파일 이름 규약(shotz_..._z<N>_...)과 cycle.json 필드 ZRangeImages 는 현장에 누적되고 재검사가 그 이름·필드로 사진을 찾는다 — 나중에 바꾸면 이미 쌓인 사이클은 z 사진 없이 1장 폴백으로 재검사된다.</reversibility>
  <files>WPF_Example/Setting/SystemSetting.cs, WPF_Example/Utility/CaptureImageSaveService.cs, WPF_Example/UI/ViewModel/CycleResultDto.cs, WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs, WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <read_first>
    - CLAUDE.md (Code Style 하드룰)
    - WPF_Example/Setting/SystemSetting.cs:185-225
    - WPF_Example/Utility/CaptureImageSaveService.cs:385-490
    - WPF_Example/UI/ViewModel/CycleResultDto.cs:1-80
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs:54-62, :240-280, :427-440, :508-537, :2365-2400
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs:1440-1563 (IsLiveCaptureMode, EnqueueCycleDatumImageCopy, ArchiveDatumImageForCycle) 와 77-01 StoreZRangeCandidateImage
  </read_first>
  <action>
(1) Setting/SystemSetting.cs — AutoFillOfflineImages 선언 아래에 주석(자동 검사 중 Z 범위 Shot 의 후보 z 사진을 전부 원본 폴더에 저장하고 cycle.json 에 기록해 사무실 재검사로 선택 로직을 검증하는 용도, SIDE 한 장 약 127MB × z 개수 — 평소 생산 중에는 끈다, 키 누락 → false)과 `[Category("System|Enviroment")]` `public bool SaveZRangeCandidateImages { get; set; } = false;` 삽입(D-77-06 ③, D-77-07 ④ 체크박스 1개). 같은 파일의 PropertyTools Category 를 쓴다.

(2) Utility/CaptureImageSaveService.cs — BuildDatumFileName 아래에 `public const string ZRANGE_CANDIDATE_PREFIX = "shotz";`, `private const string ZRANGE_FILE_Z_SEGMENT = "_z";`, `public static string BuildZRangeCandidateFileName(string szSequence, string szShotName, int nZIndex, DateTime ts)` — BuildDatumFileName 과 같은 순서로 SanitizeFilePart(szSequence, "SEQ"), SanitizeFilePart(szShotName, "SHOT"), 시각 `HHmmssfff`, 결과 `shotz_<seq>_<shot>_z<N>_<시각>` + ResolveOriginImageExtension(). 기존 헬퍼는 호출만 하고 고치지 않는다.

(3) UI/ViewModel/CycleResultDto.cs — CycleResultDto 의 DatumImages 아래에 XML 요약(자동 검사 tick 에서 저장된 Z 범위 후보 사진 — 설정이 꺼져 있거나 옛 cycle.json 이면 빈 목록)과 `public List<ZRangeImageRecordDto> ZRangeImages { get; set; } = new List<ZRangeImageRecordDto>();`. DatumImageRecordDto 클래스 아래에 `public class ZRangeImageRecordDto` — `public string ShotName { get; set; }`, `public int ZIndex { get; set; }`, `public string Path { get; set; }`.

(4) InspectionSequence.cs — `_tickDatumImages` 선언 아래에 `private readonly List<ZRangeImageRecordDto> _tickZRangeImages = new List<ZRangeImageRecordDto>();`(락은 _tickDatumImageLock 재사용 주석). RecordTickDatumImage 아래에 `public void RecordTickZRangeImage(string szShotName, int nZIndex, string szPath)`(빈 이름·경로면 return, 락 안에서 Add). `ClearTickDatumImages()` 락 블록 안 `_tickDatumImages.Clear();` 다음 줄에 `_tickZRangeImages.Clear();`. TakeTickDatumImagesSnapshot 아래에 `private List<ZRangeImageRecordDto> TakeTickZRangeImagesSnapshot()`(락 안 복사본). `cycleDto.DatumImages = TakeTickDatumImagesSnapshot();` 두 곳 각각 다음 줄에 `cycleDto.ZRangeImages = TakeTickZRangeImagesSnapshot();`.

(5) Action_FAIMeasurement.cs — `private void SaveZRangeCandidateImageIfEnabled(HImage image, InspectionSequence parentSeq, int nZIndex)`: 전체 try/catch(Exception) 로 감싸 예외는 Logging.PrintErrLog 로만(검사 흐름에 전파 금지 — ArchiveDatumImageForCycle 선례). 가드: image·parentSeq·ShotParam null 이면 return, `SystemSetting.Handle.SaveZRangeCandidateImages` 가 false 면 return(PR-4), `IsLiveCaptureMode()` 가 false 면 return(SIMUL·오프라인이 읽은 파일을 다시 쓰지 않음). `DateTime ts = DateTime.Now;` → `BuildZRangeCandidateFileName(parentSeq.Name, ShotParam.ShotName, nZIndex, ts)` → `CaptureImageSaveService.BuildFilePath(false, szFileName, ts)` → 기존 `EnqueueCycleDatumImageCopy(image, szFileName, ts)` 가 true 면 `parentSeq.RecordTickZRangeImage(ShotParam.ShotName, nZIndex, szPath)` 와 ELogType.Algorithm `[ZFocus] 후보 사진 저장 — <Shot> z<N>: <경로>`. 77-01 StoreZRangeCandidateImage 의 `StoreZRangeImage(...)` 호출 다음 줄에 `SaveZRangeCandidateImageIfEnabled(image, parentSeq, nCurZ);` 삽입(Auto 모드이고 범위 소유 z 일 때만 도달).

(6) 커밋: `git add` 로 다섯 파일만. 메시지 예: feat(77-04): Z 범위 후보 사진 저장 설정·cycle.json ZRangeImages.

CLAUDE.md 하드룰(새 줄): 삼항·null 병합·null 조건 연산자·switch 식 금지, 한 줄 분기 중괄호, &&/|| 3개 이상 금지, 헝가리언, 매직넘버 금지, 날짜·이니셜 서명 주석 금지, HImage 사본 수명은 기존 EnqueueCycleDatumImageCopy 규약 그대로(여기서 Dispose 하지 않음 — 호출자 image 는 RunGrab 이 Dispose), 새 줄에 물음표 문자 금지, 파일별 brace 스타일 유지.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
BASE=$(git log --format=%H -1 -- .planning/phases/77-side-z-focus-select/77-04-PLAN.md)
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > "$TEMP/p77-04a.log" 2>&1; echo "msbuild_exit=$?"
echo "errors=$(grep -cE ': error [A-Z]+[0-9]+' "$TEMP/p77-04a.log")"
SS=WPF_Example/Setting/SystemSetting.cs
CI=WPF_Example/Utility/CaptureImageSaveService.cs
DT=WPF_Example/UI/ViewModel/CycleResultDto.cs
IS=WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
AF=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
echo "setting=$(grep -c 'public bool SaveZRangeCandidateImages { get; set; } = false;' $SS) fname=$(grep -c 'public static string BuildZRangeCandidateFileName(string szSequence, string szShotName, int nZIndex, DateTime ts)' $CI) prefix=$(grep -c 'ZRANGE_CANDIDATE_PREFIX = "shotz";' $CI)"
echo "dtolist=$(grep -c 'public List<ZRangeImageRecordDto> ZRangeImages' $DT) dtoclass=$(grep -c 'public class ZRangeImageRecordDto' $DT)"
echo "record=$(grep -c 'public void RecordTickZRangeImage(string szShotName, int nZIndex, string szPath)' $IS) attach=$(tr -d '\r' < $IS | grep -vE '^\s*//' | grep -c 'cycleDto.ZRangeImages = TakeTickZRangeImagesSnapshot();') clear=$(tr -d '\r' < $IS | awk '/private void ClearTickDatumImages\(\)/,/^        }$/' | grep -c '_tickZRangeImages.Clear();')"
SAVE=$(tr -d '\r' < $AF | awk '/private void SaveZRangeCandidateImageIfEnabled\(/,/^        }$/')
echo "savegate=$(printf '%s\n' "$SAVE" | grep -c 'SaveZRangeCandidateImages') livegate=$(printf '%s\n' "$SAVE" | grep -c 'IsLiveCaptureMode()') enqueue=$(printf '%s\n' "$SAVE" | grep -c 'EnqueueCycleDatumImageCopy(') catchguard=$(printf '%s\n' "$SAVE" | grep -c 'catch (Exception')"
echo "savecall=$(tr -d '\r' < $AF | awk '/private void StoreZRangeCandidateImage\(/,/^        }$/' | grep -c 'SaveZRangeCandidateImageIfEnabled(image, parentSeq, nCurZ);')"
for F in $SS $CI $DT $IS $AF; do
  ADD=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+')
  echo "$F ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk')"
done
for F in $SS $CI; do echo "$F deleted=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l)"; done
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`, `errors=0`
    - `setting=1 fname=1 prefix=1 dtolist=1 dtoclass=1`
    - `record=1 attach=2 clear=1`
    - `savegate>=1 livegate>=1 enqueue=1 catchguard>=1 savecall=1`
    - 다섯 파일 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0`
    - SystemSetting.cs·CaptureImageSaveService.cs `deleted=0` (나머지 세 파일의 BASE 이전 줄 삭제 0 은 77-05 누적 감사)
    - `commit_files` 가 정확히 다섯 파일
  </acceptance_criteria>
  <done>체크박스가 켜진 라이브 PLC 사이클에서만 후보 z 사진이 z 번호가 드러나는 이름으로 저장되고 cycle.json ZRangeImages 에 기록된다. 꺼져 있으면 저장 0.</done>
</task>

<task type="auto">
  <name>Task 2: 수동·오프라인·재검사 모드 — 라이브 수동 안내, z 별 저장 사진으로 같은 선택, 오프라인 z 사진 자동채움, 재검사 경로 주입, 오버레이 선택 Z 라벨</name>
  <reversibility rating="costly">오프라인 파일 이름 shot_&lt;ShotName&gt;_z&lt;N&gt;.bmp 는 운영자·자동채움이 현장 폴더에 쌓는 규약이다 — 바꾸면 기존 파일을 못 찾아 1장 폴백으로 조용히 바뀐다(경고 로그는 남음).</reversibility>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs, WPF_Example/Utility/RecipeFileHelper.cs, WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs, WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs</files>
  <read_first>
    - CLAUDE.md (Code Style 하드룰, MVVM — MainView.xaml.cs 신규 로직 금지)
    - .planning/phases/77-side-z-focus-select/77-CONTEXT.md D-77-06, D-77-07 ⑦
    - 이 PLAN '계획 단계 결정' M-1 ~ M-5
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs 의 77-01/02 ZRange·ZFocus 메서드 전체, LoadShotInspectionImage(:970-982), IsOfflineAutoFillEnabled/IsLiveCaptureMode(:1443-1457), EnqueueOfflineImageCopy(:1489-1505), AutoFillShotOfflineImage(:1593-1604), LogSeqStep
    - WPF_Example/Utility/RecipeFileHelper.cs:170-210
    - WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs:438-520, :700-760, :1070-1090, :1150-1210, :1320-1365
  </read_first>
  <action>
(1) RecipeFileHelper.cs — OFFLINE_SUFFIX_VERTICAL 아래에 주석(Z 범위 Shot 의 z 별 오프라인 사진: shot_&lt;ShotName&gt;_z&lt;N&gt;.bmp — 자동채움과 오프라인 검사가 이 상수를 공유)과 `public const string OFFLINE_SUFFIX_Z = "_z";`.

(2) ShotConfig.cs — 77-01 IsZRangeEnabled 아래에 주석(저장 사이클 재검사 중에만 RepeatRunService 가 채우는 z→사진 경로. null = 재검사 아님. 필드라 INI·붙여넣기 대상 아님)과 `public Dictionary<int, string> RerunZRangeImagePaths = null;`.

(3) Action_FAIMeasurement.cs — 모드 판정(M-1, D-77-06 ①②): 77-01 ResolveZRangeMode 의 `!parentSeq2.IsProtocolDrivenCycle()` 가드가 Off 를 반환하던 것을 `IsLiveCaptureMode()` 면 ManualSingle, 아니면 OfflineSelect 반환으로, `IsManualTriggerCycle()` 가드의 Off 를 ManualSingle 로 바꾼다(77-01 이 쓴 줄 — BASE 이전 줄 아님). IsZRangeEnabled() 가 false 면 Off 를 가장 먼저 반환하는 순서는 유지.
- 필드 `private bool _bZRangeNoticeLogged = false;`, RunInit 의 기존 ZRange 초기화 줄들 다음에 `_bZRangeNoticeLogged = false;`.
- `private void LogZRangeManualNotice()` — 이미 기록이면 return, 기록 표시, `string szNotice = "Z 범위 Shot — 수동은 Z 선택 안 함, 현재 사진 1장으로 측정 (" + ShotParam.ShotName + " z" + ShotParam.ZIndex + "~z" + ShotParam.ZIndexEnd + ")";` 를 기존 `LogSeqStep("Measure", szNotice)` 와 ELogType.Algorithm(ZFOCUS_LOG_TAG 접두) 두 곳에 남긴다(상태 표시 = 시퀀스 로그 탭, 화면 코드 수정 없음 — PR-5).
- `TryHandleZRangeMeasurement` 에 모드 분기 추가(77-01 의 '그 외 모드 → false' 앞): ManualSingle → `LogZRangeManualNotice();` 후 false(기존 1장 경로). OfflineSelect → 크로스-Z(bHasAnyZIndex)면 false, 지원 측정이면 `ExecuteZRangeSelection(...)` 결과 반환(후보 0 이면 그 메서드가 '[ZFocus] 후보 사진 없음' Error 후 false → 1장 폴백, D-77-06 ②), 미지원 측정이면 `HImage baseImage = FindZRangeCandidateImage(ShotParam.ZIndex);`(EnsureZRangeCandidatesLoaded 먼저) 가 null 이 아니면 `_nZRangeDisplayZIndex = ShotParam.ZIndex;` 후 `ExecuteZRangeBaseImageMeasurement(meas, baseImage, ...)` 결과 true, null 이면 ELogType.Error `[ZFocus] 기준 Z 사진 없음 — <Shot> · <측정>: 현재 사진 1장으로 측정` 후 false(M-5). baseImage 는 후보 목록 소유라 여기서 Dispose 하지 않는다.
- `private HImage FindZRangeCandidateImage(int nZIndex)` — _lstZRangeCandidates 가 null 이면 null, 키가 같은 첫 사진(소유권 이동 없음).
- 77-01/02 `EnsureZRangeCandidatesLoaded`: 로드 표시 다음에 `ResolveZRangeMode(parentSeq2)` 가 OfflineSelect 면 `_lstZRangeCandidates = LoadOfflineZRangeCandidates(parentSeq2);`, 아니면 기존 TakeZRangeImages 경로(if/else). 77-02 누락 경고는 두 경로 모두 적용.
- `private List<KeyValuePair<int, HImage>> LoadOfflineZRangeCandidates(InspectionSequence parentSeq2)` — 새 목록, `BuildZRangeCandidateIndices(ShotParam)` 의 z 마다 `string szPath = ResolveOfflineZRangeImagePath(nZ);` 가 비었거나 File.Exists 가 false 면 continue, try 안에서 `new HImage(szPath)` 를 목록에 추가, catch(Exception) 는 ELogType.Error `[ZFocus] 오프라인 z 사진 로드 실패 — <경로>: <메시지>` 후 continue. 반환된 사진은 기존 ReleaseZRangeCandidates 가 Dispose.
- `private string ResolveOfflineZRangeImagePath(int nZIndex)` (M-3) — ShotParam.RerunZRangeImagePaths 가 null 이 아니면(재검사 중) TryGetValue 결과 경로, 없으면 string.Empty 반환 — 오프라인 폴더로 폴백하지 않는다. null 이면 `RecipeFiles.BuildOfflineImagePath(RecipeFiles.OFFLINE_PREFIX_SHOT + ShotParam.ShotName + RecipeFiles.OFFLINE_SUFFIX_Z + nZIndex, out szError)` 결과(null 이면 string.Empty).
- 오프라인 z 자동채움(D-77-06 ②, D-77-07 ⑦): `private void AutoFillZRangeOfflineImage(HImage image, int nZIndex)` — `IsOfflineAutoFillEnabled()` 와 `IsLiveCaptureMode()` 를 이름 있는 bool 두 개로 뽑아 둘 다 참일 때만, ShotName 이 비면 return, 기존 `EnqueueOfflineImageCopy(image, RecipeFiles.OFFLINE_PREFIX_SHOT + ShotParam.ShotName + RecipeFiles.OFFLINE_SUFFIX_Z + nZIndex)` 결과 경로가 null 이 아니면 ELogType.Trace 로그. SimulImagePath 는 건드리지 않는다. StoreZRangeCandidateImage 의 SaveZRangeCandidateImageIfEnabled 호출 다음 줄에 `AutoFillZRangeOfflineImage(image, nCurZ);`.
- 오버레이 라벨(O-7): `private static void ApplySelectedZLabel(List<EdgeInspectionOverlay> lstOverlays, int nZIndex)` — null 이면 return, `string szLabel = MeasurementBase.FormatSelectedZ(nZIndex);`, null 아닌 각 오버레이 `SelectedZLabel = szLabel`. ExecuteZRangeSelection 의 `meas.LastSelectedZIndex = chosen.ZIndex;` 다음 줄에 `ApplySelectedZLabel(chosen.Overlays, chosen.ZIndex);`, ExecuteZRangeBaseImageMeasurement 의 선택 Z 대입 다음 줄에 그 측정 오버레이 목록으로 같은 호출(RecordMeasurementResult 가 같은 리스트 객체를 누적하므로 표시·저장에 반영).

(4) RepeatRunService.cs (D-77-06 ②):
- SavedCycleRerunPart 의 DualPhotos 아래에 XML 요약(키 = ShotName, 값 = z → 후보 사진 절대경로. 비어 있어도 부품은 제외하지 않는다 — 1장 폴백)과 `public Dictionary<string, Dictionary<int, string>> ZRangePhotoPaths { get; set; } = new Dictionary<string, Dictionary<int, string>>();`.
- FillPartDatumPhotos 아래에 `private static void FillPartZRangePhotos(SavedCycleRerunPart part)` — Ticks 순회, `dto.ZRangeImages` null 이면 continue, 각 기록이 null·빈 ShotName·빈 Path 면 continue, Shot 사전이 없으면 만들고, 같은 z 가 이미 있으면 먼저 것 유지(첫 기록 우선).
- BuildPlan 의 `FillPartDualPhotos(part, seq, recipeManager);` 다음 줄에 `FillPartZRangePhotos(part);`. ValidatePart·AllAdoptedPathsExist 는 고치지 않는다(z 사진 부재는 제외 사유가 아님).
- `private static Dictionary<int, string> BuildRerunZRangeMap(SavedCycleRerunPart part, string szShotName)` — 항상 새 사전 반환(part 에 그 Shot 이 있으면 항목 복사).
- ApplySavedCyclePart 의 ShotPhotoPaths 적용 foreach 닫는 중괄호 다음에 `_savedCycleSnapshot.OwnedShots` 순회로 `shot.RerunZRangeImagePaths = BuildRerunZRangeMap(part, shot.ShotName);`.
- RestoreOverridePathsOnly 끝(마지막 foreach 다음)에 `snap.OwnedShots` 순회로 `shot.RerunZRangeImagePaths = null;` — 부품마다 원복 후 재주입, 최종 종료 시 null(재검사 밖에서는 오프라인 폴더 규약으로 돌아감).
- IsMeasurementHandled 의 `bool bHasReason = ...;` 다음 줄에 LastSkipReason 이 SkipReason.Z_RANGE_PENDING 이면 `bHasReason = false;`.

(5) 커밋: `git add` 로 네 파일만. 메시지 예: feat(77-04): 수동 안내·오프라인/재검사 z 사진 선택·z 자동채움·선택 Z 라벨.

CLAUDE.md 하드룰(새 줄): 삼항·null 병합·null 조건 연산자·switch 식 금지, 한 줄 분기 중괄호, &&/|| 3개 이상 금지, if 중첩 3단계 이상이면 메서드 분리, 헝가리언, 매직넘버 금지, 날짜·이니셜 서명 주석 금지, HImage 는 목록 소유·ReleaseZRangeCandidates 에서 Dispose(로드 실패 시 만든 객체 없음), 새 줄에 물음표 문자 금지, RepeatRunService 는 Allman·Action 은 K&R 유지. MainView.xaml.cs·SystemHandler.cs 수정 금지.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
BASE=$(git log --format=%H -1 -- .planning/phases/77-side-z-focus-select/77-04-PLAN.md)
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > "$TEMP/p77-04b.log" 2>&1; echo "msbuild_exit=$?"
echo "errors=$(grep -cE ': error [A-Z]+[0-9]+' "$TEMP/p77-04b.log")"
AF=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
RF=WPF_Example/Utility/RecipeFileHelper.cs
SC=WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
RR=WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs
MODE=$(tr -d '\r' < $AF | awk '/private EZRangeMode ResolveZRangeMode\(/,/^        }$/')
echo "modes=$(printf '%s\n' "$MODE" | grep -cE 'return EZRangeMode\.(ManualSingle|OfflineSelect);') offfirst=$(printf '%s\n' "$MODE" | awk '/IsZRangeEnabled\(\)/{if(a==0){a=NR}} /return EZRangeMode\.(Auto|Manual|Offline)/{if(b==0){b=NR}} END{if(a>0 && b>a){print 1}else{print 0}}')"
echo "notice=$(grep -c 'Z 범위 Shot — 수동은 Z 선택 안 함' $AF) noticeseq=$(tr -d '\r' < $AF | awk '/private void LogZRangeManualNotice\(\)/,/^        }$/' | grep -c 'LogSeqStep(')"
echo "offload=$(tr -d '\r' < $AF | awk '/private void EnsureZRangeCandidatesLoaded\(/,/^        }$/' | grep -c 'LoadOfflineZRangeCandidates(parentSeq2)') rerunfirst=$(tr -d '\r' < $AF | awk '/private string ResolveOfflineZRangeImagePath\(/,/^        }$/' | awk '/RerunZRangeImagePaths/{if(a==0){a=NR}} /BuildOfflineImagePath/{if(b==0){b=NR}} END{if(a>0 && b>a){print 1}else{print 0}}') suffixuse=$(grep -c 'RecipeFiles.OFFLINE_SUFFIX_Z' $AF)"
echo "autofill=$(tr -d '\r' < $AF | awk '/private void StoreZRangeCandidateImage\(/,/^        }$/' | grep -c 'AutoFillZRangeOfflineImage(image, nCurZ);') labelcalls=$(tr -d '\r' < $AF | grep -vE '^\s*//' | grep -c 'ApplySelectedZLabel(')"
echo "suffix=$(grep -c 'public const string OFFLINE_SUFFIX_Z = "_z";' $RF) rerunfield=$(grep -c 'public Dictionary<int, string> RerunZRangeImagePaths = null;' $SC)"
echo "partprop=$(grep -c 'ZRangePhotoPaths { get; set; }' $RR) fill=$(tr -d '\r' < $RR | awk '/public static SavedCycleRerunPlan BuildPlan\(/,/^        }$/' | grep -c 'FillPartZRangePhotos(part);') apply=$(tr -d '\r' < $RR | awk '/private void ApplySavedCyclePart\(/,/^        }$/' | grep -c 'RerunZRangeImagePaths = BuildRerunZRangeMap(') restore=$(tr -d '\r' < $RR | awk '/private static void RestoreOverridePathsOnly\(/,/^        }$/' | grep -c 'RerunZRangeImagePaths = null;') handled=$(tr -d '\r' < $RR | awk '/IsMeasurementHandled\(/,/^        }$/' | grep -c 'Z_RANGE_PENDING') validate=$(tr -d '\r' < $RR | awk '/private static string ValidatePart\(/,/^        }$/' | grep -c 'ZRange')"
echo "forbidden=$(git diff --name-only "$BASE" -- WPF_Example/UI/ContentItem/MainView.xaml.cs WPF_Example/Custom/SystemHandler.cs | wc -l)"
for F in $AF $RF $SC $RR; do
  ADD=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+')
  echo "$F ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk')"
done
for F in $RF $RR; do echo "$F deleted=$(git diff -w -U0 "$BASE" -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l)"; done
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`, `errors=0`
    - `modes>=2`, `offfirst=1`, `notice>=1`, `noticeseq=1`
    - `offload=1`, `rerunfirst=1`(재검사 경로 판정이 오프라인 폴더 규약보다 먼저), `suffixuse>=2`
    - `autofill=1`, `labelcalls>=3`(정의 1 + 호출 2)
    - `suffix=1 rerunfield=1`
    - `partprop=1 fill=1 apply=1 restore=1 handled=1 validate=0`(z 사진은 제외 사유 아님)
    - `forbidden=0`
    - 네 파일 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0`, RecipeFileHelper.cs·RepeatRunService.cs `deleted=0`
    - `commit_files` 가 정확히 네 파일
  </acceptance_criteria>
  <done>라이브 수동은 1장 + 안내, 오프라인·재검사는 z 별 저장 사진으로 자동검사와 같은 선택(없으면 1장 + 경고), 재검사 경로는 오프라인 폴더와 섞이지 않고 종료 시 해제, 자동채움이 z 사진도 채우며, 선택 Z 라벨이 오버레이에 들어간다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 설정 체크박스 → 결과 폴더 디스크 | 켜면 사이클마다 수백 MB 가 쌓인다 |
| 디스크 사진 파일·cycle.json(수기 복사·옛 파일) → 오프라인·재검사 후보 | 이름만 맞으면 어떤 사진이든 후보가 된다 |
| Shot 이름(레시피 편집값) → 파일 경로 | 경로 문자가 섞일 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-77-17 | Denial of Service | SaveZRangeCandidateImages — 디스크 고갈 | high | mitigate | 기본 false·키 누락 false(`setting=1`), 라이브 촬영 + PLC Auto 범위 z 에서만(`savegate`, `livegate`, StoreZRangeCandidateImage 경유), 설정 설명에 용량 명시 — PR-4 |
| T-77-18 | Tampering | 파일명에 Shot 이름 사용(경로 조작 문자) | medium | mitigate | 기존 SanitizeFilePart / BuildOfflineImagePath 의 SanitizeOfflineFilePart 경유만 사용(canon — 상세 점검은 /gsd-secure-phase) |
| T-77-19 | Spoofing | 재검사 중 다른 부품의 오프라인 폴더 사진이 섞여 선택 | high | mitigate | RerunZRangeImagePaths 가 null 이 아니면 폴더 폴백 금지(`rerunfirst=1`), 부품마다 원복 후 재주입·종료 시 null(`apply=1`, `restore=1`) |
| T-77-20 | Denial of Service | 손상·거대 오프라인 파일 로드 예외 | medium | mitigate | 파일별 try/catch 후 건너뜀 + Error 로그, 저장 경로 전체 try/catch |
| T-77-21 | Repudiation | 수동·오프라인이 선택 없이 측정됐는지 불명 | medium | mitigate | `Z 범위 Shot — 수동은 Z 선택 안 함`(시퀀스 로그+Algorithm), `[ZFocus] 후보 사진 없음/누락` — PR-5 |
| T-77-25 | Information Disclosure | 저장 사진·cycle.json 경로 | low | accept | 로컬 결과 폴더, 기존 원본 저장과 같은 위치·권한 |
</threat_model>

<verification>
1. 태스크마다 Debug|x64 빌드 에러 0, 커밋 파일 정확, 하드룰 grep 0
2. 저장 게이트(`savegate`, `livegate`, `savecall`), 기록(`record`, `attach=2`)
3. 모드 분기(`modes`, `offfirst`), 안내(`notice`, `noticeseq`), 오프라인·재검사 로드(`offload`, `rerunfirst`), 재검사 주입·해제(`apply`, `restore`), 대기 제외(`handled`)
4. MainView.xaml.cs·SystemHandler.cs 무변경(`forbidden=0`)
5. 사무실 검증(오프라인 z 파일로 선택 로그, 재검사, 저장 체크박스)은 77-06 UAT U-7
</verification>

<success_criteria>
- 라이브 수동 1장 + 안내, 오프라인·재검사 z 별 사진 선택(없으면 1장 + 경고)
- 후보 z 사진 저장은 체크박스 켤 때만, cycle.json 에 기록되어 재검사가 찾는다
- O-1 없이 사무실에서 저장 사진으로 선택 로직을 돌려볼 수 있다
</success_criteria>

<output>
완료 후 `.planning/phases/77-side-z-focus-select/77-04-SUMMARY.md` 작성: 태스크별 커밋·빌드, 모드 판정표(PLC/수동 트리거/RUN 라이브/RUN SIMUL·오프라인/재검사 → 모드), 파일 이름 규약 2종 실제 예시, 77-06 UAT 가 쓸 오프라인 z 파일 준비 방법 메모.
</output>
