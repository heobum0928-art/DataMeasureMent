---
phase: 77-side-z-focus-select
plan: 05
type: execute
wave: 4
depends_on: ["77-04"]
files_modified:
  - WPF_Example/VersionDefine.cs
autonomous: true
requirements: [SZF-05]
user_setup: []

must_haves:
  truths:
    - "VersionDefine.cs 에 새 [Version] 항목 1.7.47.0(Date 2026-09-15)이 기존 항목을 지우지 않고 추가되고, VERSION = 1.7.47.0, BUILD_DATE = 2026-09-15 로 올라간다"
    - "phase 77 전체(77-01~04) 누적 diff 에서 BASE 이전부터 있던 줄의 삭제·수정은 허용 목록뿐이다 — MeasurementHistoryCsvWriter.cs 1줄(CSV_HEADER), EdgeInspectionOverlay.cs 1줄(Clone 쉼표), VersionDefine.cs 2줄(VERSION, BUILD_DATE). 나머지 수정 파일은 0 (SZF-05)"
    - "[edge SZF-05 adjacency] 범위 켠 Shot 과 끈 Shot 이 같은 시퀀스에 나란히 있어도 끈 Shot 의 라우팅·측정 경로는 새 분기에 들어가지 않는다 — 새 판정 진입점(DoesShotOwnZRangeIndex, BuildZRangeCandidateIndices, ShotHasZRangeCompletingAt, ResolveZRangeMode)이 전부 IsZRangeEnabled() 가드로 시작하고, EdgeScore 를 설정하는 곳은 EdgeToLineDistance·EdgeToLineAngle 두 파일뿐이다"
    - {statement: "[edge SZF-05 adjacency/empty] 범위를 쓰지 않는 기존 레시피로 자동 사이클·수동 RUN 을 돌렸을 때 측정값·판정·응답이 이전 버전(1.7.46.0)과 같다 — 실측 비교 필요(77-06 UAT U-8)", verification: backstop}
    - "phase 누적 추가 줄에 삼항·null 병합·null 조건 연산자·switch 식·날짜 서명 주석이 0 이고, 시작 번호 프로퍼티 이름이 코드에 0 이며, csproj 와 MainView.xaml.cs 는 phase 동안 바뀌지 않았다"
  artifacts:
    - path: "WPF_Example/VersionDefine.cs"
      provides: "1.7.47.0 changelog + VERSION/BUILD_DATE"
      contains: "Number = \"1.7.47.0\""
  key_links:
    - from: "VersionDefine.VERSION"
      to: "AssemblyVersion / RecipeFileHelper.GetVersion"
      via: "기존 단일 상수 참조(변경 없음)"
      pattern: "public const string VERSION = \"1.7.47.0\";"
---

<objective>
Phase 77 을 버전으로 표기하고(1.7.46.0 → 1.7.47.0), 77-01~04 누적 변경이 범위 꺼짐 경로를 건드리지 않았는지 정적 감사로 확정한다(SZF-05).

Purpose: 배포 전 changelog 를 남기고, 회귀 0 을 코드 근거로 한 번에 증명한다. 실측 동일성은 77-06 UAT 가 맡는다.

Output: VersionDefine.cs 1개 파일 수정 + 감사 결과를 77-05-SUMMARY.md 에 기록. 배포(Release 빌드)는 phase 밖(사용자 승인 후).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/77-side-z-focus-select/77-01-SUMMARY.md
@.planning/phases/77-side-z-focus-select/77-02-SUMMARY.md
@.planning/phases/77-side-z-focus-select/77-03-SUMMARY.md
@.planning/phases/77-side-z-focus-select/77-04-SUMMARY.md

<interfaces>
- VersionDefine.cs `:3` 규칙 주석(항목은 지우지 않고 새로 쌓는다), `:613` 마지막 항목 `Number = "1.7.46.0"`, `:631` `public const string VERSION = "1.7.46.0";`, `:632` `public const string BUILD_DATE = "2026-09-14";`
- phase 수정 파일 목록 = 77-01~04 frontmatter files_modified 합집합(21개)
</interfaces>
</context>

## Artifacts this phase produces

phase 전체 원장은 `77-01-PLAN.md` 표. 이 plan 이 만드는 것: VersionDefine `[Version(Number = "1.7.47.0", Date = "2026-09-15", Change = ...)]` 항목, VERSION/BUILD_DATE 값 갱신.

<tasks>

<task type="auto">
  <name>Task 1: VersionDefine 1.7.47.0 — SIDE Z 범위 자동 초점 선택 changelog</name>
  <files>WPF_Example/VersionDefine.cs</files>
  <read_first>
    - WPF_Example/VersionDefine.cs:1-10, :600-640
    - .planning/phases/77-side-z-focus-select/77-01-SUMMARY.md ~ 77-04-SUMMARY.md (실제 구현 요약)
  </read_first>
  <action>
(1) 마지막 [Version] 항목(1.7.46.0) 다음에 새 항목을 같은 형식으로 추가한다: `Number = "1.7.47.0"`, `Date = "2026-09-15"`, `Change` = 한국어 요약 한 덩어리(여러 줄이면 기존처럼 `" +` 연결). 포함 내용: Shot 'Z 범위 끝'(ZIndexEnd, 0=꺼짐) 한 칸으로 ZIndex~끝 범위 지정 / PLC 가 범위 z 마다 촬영한 사진을 모아 끝 z 에서 EdgeToLineDistance·EdgeToLineAngle 측정마다 measure_pos 에지 강도(strip 최대 |amp| 평균) 최고 사진의 측정값 채택, 기준 Z 3% 동점 규칙 / 다른 Shot·Datum z 겹침 제외와 편집 즉시 경고 / 결과 그리드·리뷰어·CSV '선택Z' 열과 오버레이 라벨 / 수동은 1장 측정+안내, 오프라인·재검사는 z 별 저장 사진(shot_<Shot>_z<N>.bmp, cycle.json ZRangeImages)으로 같은 선택 / 후보 z 사진 저장 체크박스(기본 꺼짐) / 범위를 쓰지 않는 레시피는 동작 동일. 문자열에 물음표 문자·날짜 서명 주석을 넣지 않는다.
(2) `VERSION` 을 "1.7.47.0", `BUILD_DATE` 를 "2026-09-15" 로 바꾼다(이 두 줄은 값 수정 허용).
(3) 커밋: `git add WPF_Example/VersionDefine.cs` 만. 메시지 예: chore(77-05): 버전 1.7.47.0 — SIDE Z 범위 자동 초점 선택.
Release|x64 빌드 금지(OutputPath D:\Data\ — 배포 exe 덮어씀). 배포는 사용자 승인 후 phase 밖.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
BASE=$(git log --format=%H -1 -- .planning/phases/77-side-z-focus-select/77-05-PLAN.md)
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > "$TEMP/p77-05a.log" 2>&1; echo "msbuild_exit=$?"
echo "errors=$(grep -cE ': error [A-Z]+[0-9]+' "$TEMP/p77-05a.log")"
VD=WPF_Example/VersionDefine.cs
echo "entry=$(grep -c 'Number = "1.7.47.0"' $VD) prev=$(grep -c 'Number = "1.7.46.0"' $VD) ver=$(grep -c 'public const string VERSION = "1.7.47.0";' $VD) date=$(grep -c 'public const string BUILD_DATE = "2026-09-15";' $VD)"
ADD=$(git diff -w -U0 "$BASE" -- $VD | grep -E '^\+' | grep -vE '^\+\+\+')
echo "deleted=$(git diff -w -U0 "$BASE" -- $VD | grep -E '^-' | grep -vE '^---' | wc -l) qmark=$(printf '%s\n' "$ADD" | grep -cF '?') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk')"
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`, `errors=0`
    - `entry=1 prev=1 ver=1 date=1`
    - `deleted=2`(VERSION, BUILD_DATE 줄), `qmark=0`, `datesig=0`
    - `commit_files` 가 VersionDefine.cs 하나
  </acceptance_criteria>
  <done>1.7.47.0 changelog 가 추가되고 VERSION/BUILD_DATE 가 갱신되었다. 기존 항목 삭제 없음.</done>
</task>

<task type="auto">
  <name>Task 2: SZF-05 누적 회귀 감사 — phase 전체 삭제 줄 허용 목록, 가드 선행, 하드룰, 금지 파일 무변경</name>
  <files>.planning/phases/77-side-z-focus-select/77-05-SUMMARY.md (감사 결과 기록만 — 코드 변경 없음)</files>
  <read_first>
    - .planning/phases/77-side-z-focus-select/77-01-PLAN.md (Artifacts 원장, 계획 단계 결정 P-4·P-5)
    - 77-01~04 SUMMARY 의 BASE 해시·커밋 해시
  </read_first>
  <action>
아래 verify 스크립트를 실행하고 출력 전체를 77-05-SUMMARY.md 의 '회귀 감사' 절에 붙인다. PASS 기준을 벗어나는 항목이 있으면 코드를 고치지 말고(이 태스크는 감사 전용) 해당 파일·줄을 SUMMARY 에 적고 실행을 멈춰 오케스트레이터에 보고한다 — 숫자를 맞추려 코드를 지우는 행위 금지(STATE 2026-08-27 교훈). PHASE_BASE 는 77-01 첫 실행 커밋의 부모다(77-01-SUMMARY 의 BASE 해시와 같아야 한다 — 다르면 SUMMARY 값을 쓴다).
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
PHASE_BASE=$(git log --format=%H -1 -- .planning/phases/77-side-z-focus-select/77-01-PLAN.md)
echo "phase_base=$PHASE_BASE"
FILES="WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineAngleMeasurement.cs WPF_Example/Custom/Sequence/Inspection/SkipReason.cs WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs WPF_Example/UI/ViewModel/CycleResultDto.cs WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs WPF_Example/UI/ViewModel/MeasurementResultRow.cs WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs WPF_Example/UI/ContentItem/MainView.xaml WPF_Example/UI/Reviewer/ReviewerWindow.xaml WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs WPF_Example/Halcon/Display/HalconDisplayService.cs WPF_Example/Setting/SystemSetting.cs WPF_Example/Utility/CaptureImageSaveService.cs WPF_Example/Utility/RecipeFileHelper.cs WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs WPF_Example/VersionDefine.cs"
for F in $FILES; do
  ADD=$(git diff -w -U0 "$PHASE_BASE" -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+')
  echo "$F deleted=$(git diff -w -U0 "$PHASE_BASE" -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk')"
done
echo "unexpected_files=$(git diff --name-only "$PHASE_BASE" -- WPF_Example | grep -vxF -f <(printf '%s\n' $FILES) | tr '\n' ' ')"
echo "csproj=$(git diff --name-only "$PHASE_BASE" -- WPF_Example/DatumMeasurement.csproj | wc -l) mainview_cs=$(git diff --name-only "$PHASE_BASE" -- WPF_Example/UI/ContentItem/MainView.xaml.cs | wc -l) systemhandler=$(git diff --name-only "$PHASE_BASE" -- WPF_Example/Custom/SystemHandler.cs | wc -l) newcs=$(git diff --name-only --diff-filter=A "$PHASE_BASE" -- 'WPF_Example/*.cs' | wc -l)"
echo "nostart=$(grep -rhoF 'ZIndexStart' WPF_Example --include=*.cs | wc -l)"
IS=WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
AF=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
for FN in 'public bool DoesShotOwnZRangeIndex\(' 'public List<int> BuildZRangeCandidateIndices\(' 'private bool ShotHasZRangeCompletingAt\('; do
  echo "guard[$FN]=$(tr -d '\r' < $IS | awk "/$FN/,/^        }\$/" | grep -c 'IsZRangeEnabled()')"
done
echo "offfirst=$(tr -d '\r' < $AF | awk '/private EZRangeMode ResolveZRangeMode\(/,/^        }$/' | awk '/IsZRangeEnabled\(\)/{if(a==0){a=NR}} /return EZRangeMode\.(Auto|Manual|Offline)/{if(b==0){b=NR}} END{if(a>0 && b>a){print 1}else{print 0}}')"
echo "edgescore_setters=$(grep -rlF 'svc.EdgeScore = ' WPF_Example --include=*.cs | sort | tr '\n' ' ')"
VA=WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
echo "edgescore_uses=$(tr -d '\r' < $VA | grep -vE '^\s*//' | grep -cE 'EdgeScore\.(Reset|AddStrip|BuildStripListText)') edgescore_nullguards=$(tr -d '\r' < $VA | grep -vE '^\s*//' | grep -c 'EdgeScore != null')"
echo "savegate=$(tr -d '\r' < $AF | awk '/private void SaveZRangeCandidateImageIfEnabled\(/,/^        }$/' | grep -c 'SaveZRangeCandidateImages')"
    </automated>
  </verify>
  <acceptance_criteria>
    - 파일별 `deleted`: MeasurementHistoryCsvWriter.cs=1, EdgeInspectionOverlay.cs=1, VersionDefine.cs=2, 나머지 20개 파일=0
    - 23개 파일 모두 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0`
    - `unexpected_files=` 비어 있음, `csproj=0 mainview_cs=0 systemhandler=0 newcs=0`, `nostart=0`
    - `guard[...]` 3개 모두 >= 1, `offfirst=1`
    - `edgescore_setters` 가 정확히 EdgeToLineAngleMeasurement.cs 와 EdgeToLineDistanceMeasurement.cs 두 경로
    - `edgescore_nullguards` >= 3 (Reset·AddStrip 2곳·로그를 감싸는 가드 — 사용처가 가드 밖에 없는지 SUMMARY 에 해당 줄을 붙여 확인)
    - `savegate>=1`
    - 위 출력 전문이 77-05-SUMMARY.md 에 기록됨
  </acceptance_criteria>
  <done>phase 77 누적 변경이 허용 목록 외 기존 줄을 건드리지 않았고, 모든 새 분기가 범위 켜짐 가드 뒤에 있으며, 하드룰·금지 파일 규칙을 지켰음이 한 번의 감사 출력으로 기록되었다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 개발 PC 빌드 → 현장 배포 exe | 잘못된 구성 빌드가 배포 exe 를 덮어쓸 수 있다 |
| phase 변경 → 기존 운영 레시피 동작 | 범위를 안 쓰는 레시피 결과가 바뀌면 안 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-77-22 | Tampering | Release|x64 빌드가 D:\Data\ 배포 exe 덮어씀 | high | mitigate | 검증 빌드는 Debug|x64 만(모든 verify 명령), 배포는 phase 밖 사용자 승인 |
| T-77-23 | Tampering | 누적 변경이 범위 꺼짐 경로의 기존 줄을 수정 | high | mitigate | Task 2 파일별 삭제 허용 목록 감사, 가드 선행(`guard`, `offfirst`), EdgeScore 설정처 2곳 한정 |
| T-77-24 | Repudiation | 배포본에 무엇이 들어갔는지 추적 불가 | low | mitigate | VersionDefine changelog 1.7.47.0 |
</threat_model>

<verification>
1. Debug|x64 빌드 에러 0
2. VersionDefine 1.7.47.0 항목·상수
3. Task 2 감사 출력의 모든 PASS 기준 충족, SUMMARY 기록
4. 실측 회귀 동일성은 77-06 UAT U-8(backstop)
</verification>

<success_criteria>
- 버전 1.7.47.0 표기
- 누적 삭제 줄이 허용 목록(4줄)뿐이고 새 분기는 모두 가드 뒤 — 코드 근거로 SZF-05 확정
</success_criteria>

<output>
완료 후 `.planning/phases/77-side-z-focus-select/77-05-SUMMARY.md` 작성: 버전 커밋 해시, 빌드 결과, Task 2 감사 출력 전문과 PASS/FAIL 판정표.
</output>
