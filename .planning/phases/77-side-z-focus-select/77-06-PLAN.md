---
phase: 77-side-z-focus-select
plan: 06
type: execute
wave: 5
depends_on: ["77-05"]
files_modified:
  - .planning/phases/77-side-z-focus-select/77-HUMAN-UAT.md
autonomous: false
requirements: [SZF-01, SZF-02, SZF-03, SZF-04, SZF-05]
user_setup: []

must_haves:
  truths:
    - "77-HUMAN-UAT.md 가 사무실(SIMUL·오프라인·재검사·TCP 모의) 절차 U-1~U-8 과 SIDE 실기 절차 U-9 를 단계·기대 로그·합격 기준으로 담는다 — U-9 는 O-1(PLC z 번호 배정) 협의 후에만 수행한다고 명시한다"
    - "U-1 은 tracer 경로(범위 Shot 1개 × EdgeToLineDistance)의 런타임 end-to-end 증거다: TCP 로 $PREP/$TEST 를 ZIndex..ZIndexEnd 순서로 보내 중간 z 는 B + '[ZFocus] 대기', 끝 z 는 '[ZFocus] 선택' + '[FitLine] edge-strength' + 응답에 측정 항목 포함, 마지막 Index 판정이 끝 z 에서 나온다 (SZF-02, SZF-03, D-77-01)"
    - "U-2 는 동점 규칙을 저장 사진으로 확인한다 — 같은 사진을 모든 z 에 두면 기준 Z 선택, 기준 Z 만 흐린 사진이면 다른 z 선택, 로그의 기준 z 차 % 와 3% 경계 (O-3, D-77-07 ③)"
    - "U-7 은 D-77-06 ①②③ 을 각각 확인한다 — RUN·수동 트리거 안내 로그, 오프라인 z 파일 선택과 파일 없음 1장 폴백 경고, 체크박스 켠 자동 사이클의 후보 저장·cycle.json ZRangeImages·그 사이클 재검사 선택"
    - {statement: "[edge SZF-02 ordering] U-4: 끝 z 중복 전송·중간 z 늦은 전송 시 사이클 완주와 로그 확인 — flagged assumption A-77-E1 에 대한 사용자 판단 기록", verification: backstop}
    - {statement: "[edge SZF-05 adjacency/empty] U-8: 범위를 쓰지 않는 기존 레시피로 자동·수동 사이클 결과가 1.7.46.0 과 같다", verification: backstop}
    - "사용자 승인 없이 이 plan 을 완료로 표시하지 않는다 — 체크포인트는 blocking 이다"
  artifacts:
    - path: ".planning/phases/77-side-z-focus-select/77-HUMAN-UAT.md"
      provides: "사무실·SIDE 실기 UAT 절차서"
      contains: "U-9"
  key_links:
    - from: "77-HUMAN-UAT.md U-1"
      to: "77-01 tracer 경로"
      via: "로그 태그 [ZFocus] 대기/선택, [FitLine] edge-strength"
      pattern: "\\[ZFocus\\]"
---

<objective>
Phase 77 의 사람 검증 절차서를 쓰고, 사용자가 사무실 절차(U-1~U-8)를 확인한 뒤 승인한다. SIDE 실기 절차(U-9)는 O-1 협의 후 별도 세션으로 남긴다.

Purpose: 이 코드베이스에는 앱을 자동 기동하는 테스트 하네스가 없어, tracer 의 런타임 end-to-end 증거와 동점·겹침·수동/오프라인/재검사·회귀 동일성은 사람이 앱을 돌려 로그로 확인해야 한다. D-77-06 은 O-1 전에도 저장 사진으로 사무실 검증이 가능하도록 설계되었다.

Output: `77-HUMAN-UAT.md` + 사용자 승인 기록.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/77-side-z-focus-select/77-CONTEXT.md
@.planning/phases/77-side-z-focus-select/77-VALIDATION.md
@.planning/phases/77-side-z-focus-select/77-01-PLAN.md
@.planning/phases/77-side-z-focus-select/77-04-SUMMARY.md
@.planning/phases/77-side-z-focus-select/77-05-SUMMARY.md
</context>

## Artifacts this phase produces

phase 전체 원장은 `77-01-PLAN.md` 표. 이 plan 이 만드는 것: `77-HUMAN-UAT.md`(U-1~U-9 절차·기대 결과·합격 기준·결과 기록 칸).

<tasks>

<task type="auto">
  <name>Task 1: 77-HUMAN-UAT.md 작성 — 사무실 U-1~U-8, SIDE 실기 U-9(O-1 후)</name>
  <files>.planning/phases/77-side-z-focus-select/77-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/77-side-z-focus-select/77-CONTEXT.md (D-77-01~07, O-1·O-8·O-9, 성공 기준 1~4)
    - .planning/phases/77-side-z-focus-select/77-VALIDATION.md 'Manual-Only Verifications'
    - .planning/phases/77-side-z-focus-select/77-01-SUMMARY.md ~ 77-05-SUMMARY.md (실제 로그 문구·파일 이름·설정 이름)
    - Test/mock_vision_client.py, Test/HandlerCommunicationTest.py (TCP 송신 예시 — 포트·메시지 형식)
    - 이전 phase UAT 문서 형식 1개(예: .planning/phases/76-side-datum/76-HUMAN-UAT.md, 없으면 74/75 HUMAN-UAT)
  </read_first>
  <action>
절차서를 한국어로 쓴다. 각 항목에 목적 / 준비 / 단계(번호) / 기대 결과(정확한 로그 태그·화면 칸·파일 이름) / 합격 기준 / 결과 기록 칸(PASS·FAIL·메모)을 둔다. 로그 문구·설정 이름·파일 이름은 SUMMARY 에 기록된 실제 값을 쓴다. 운영 레시피(D:\Data\Recipe 의 main.ini)는 절차에서 직접 수정하지 않는다 — 시험용 레시피 복사본을 만들어 쓰도록 적는다.

- 공통 준비: Debug|x64 빌드(SIMUL) 실행 방법, 시험 레시피 복사, SIDE Shot 1개에 ZIndexEnd 입력(예: ZIndex 3 → Z 범위 끝 5), Algorithm/Sequence 로그 탭 위치.
- U-1 (tracer, SZF-02/03, D-77-01): TCP 로 $PREP/$TEST 를 기준점 z → ZIndex → … → ZIndexEnd 순서로 송신(HandlerCommunicationTest.py 또는 mock 스크립트 형식 인용). 기대: 중간 z 응답 B, `[ZFocus] 후보 저장`·`[ZFocus] 대기`, 끝 z 에서 `[ZFocus] 선택`·`[FitLine] edge-strength` 한 줄씩(측정마다), 끝 z 응답에 범위 Shot 측정 항목 포함, 최종 P/F 는 끝 z 에서. SIMUL 은 모든 z 가 같은 사진이라 기준 Z 가 선택되는 것이 정상임을 적는다. strip 별 값·합·분모를 로그에서 한 측정 손으로 대조(VALIDATION Wave 0).
- U-2 (동점, O-3): 오프라인 z 파일(shot_<Shot>_z<N>.bmp)을 같은 사진으로 채움 → 기준 Z 선택. 기준 Z 파일만 초점 흐린 사진(또는 가우시안 흐림 처리본)으로 바꿈 → 다른 z 선택 + 로그의 기준 z 차 % 가 3% 초과. 3% 이내 사례가 나오면 기준 Z 채택 확인.
- U-3 (겹침, D-77-07 ⑤): 범위 안에 다른 Shot 의 ZIndex 가 있도록 설정 → 편집 즉시 '다른 Shot·기준점이 쓰는 번호라 후보에서 빠집니다' 경고 한 줄, 로그 후보 목록에 그 z 없음, $PREP 조명 로그 `[PREP ZRange]`.
- U-4 (A-77-E1, SZF-02 ordering): 끝 z $TEST 두 번 / 중간 z 를 끝 z 뒤에 송신 / 중간 z 하나 생략 → 사이클 멈춤 없음, `[ZFocus] 후보 누락` 로그, 중복 시 결과·CSV 행이 한 번 더 생기는지 기록하고 사용자가 허용 여부 판단.
- U-5 (오설정, D-77-07 ①): Z 범위 끝을 ZIndex 와 같게 / 작게 / 11개 이상으로 입력 → 편집 즉시 한국어 경고 한 줄, 검사 시 `[ZFocus] 설정 확인` Error 로그, 1장 경로 측정. 레시피 다시 열기(로드) 때 경고 창이 안 뜨는지.
- U-6 (기록·표시, SZF-04): 결과 그리드·리뷰어 '선택Z' 열 'z5', 측정 강조 시 오버레이 이름 뒤 ' z5', 중간 z tick 리뷰어 판정 'Z 범위 대기', 일자별 CSV 맨 끝 '선택Z' 열, 이 기능 이전의 옛 CSV·cycle.json 이 통계·리뷰어에서 정상 로드(빈칸).
- U-7 (D-77-06): ① 라이브 빌드 RUN 과 '수동 트리거 실행'에서 `Z 범위 Shot — 수동은 Z 선택 안 함` 이 시퀀스 로그에 한 번 + 1장 측정(SIMUL RUN 은 오프라인 모드로 동작함을 적는다). ② OfflineInspectMode 로 z 파일 있음 → 선택 로그, z 파일 모두 제거 → `[ZFocus] 후보 사진 없음` + 1장. ③ SaveZRangeCandidateImages 체크 → 자동 사이클 → original 폴더에 shotz_…_z<N>_… 파일, cycle.json ZRangeImages 항목 → 저장 사이클 재검사 → 같은 선택 로그. 체크 해제 시 저장 0. 용량 주의 문구.
- U-8 (SZF-05 backstop): 범위를 쓰지 않는 기존 레시피(1.7.46.0 결과가 있는 같은 사진·같은 사이클)로 자동·수동 → 측정값·판정·응답이 이전 버전과 같음(측정 25개 표로 비교).
- U-9 (SIDE 실기, O-1 협의 후 — 이번 승인 범위 아님): 제어팀 z 번호 표 확정 기록 칸, 범위 켠 SIDE Shot 으로 같은 부품 10회 → 측정별 선택 Z 분포·측정값 범위(성공 기준 3), 사이클 타임 이전 대비(O-8, `[ALGO]` ms·Measure 단계 초), 같은 Z 반복 vs 다른 Z 선택 측정값 차이로 XY 흔들림 확인(O-9), 체크박스 켠 사이클 후 디스크 사용량.
- 끝에 '알려진 한계' 절: A-77-E1(중복·늦은 z), 오프라인 파일 이름 규약, 편집 경고는 Z 범위 끝을 바꿀 때만 뜬다(ZIndex 를 나중에 바꾸면 런타임 로그로만), 점수는 로그에만 있음(D-77-07 ⑥).

커밋: `git add .planning/phases/77-side-z-focus-select/77-HUMAN-UAT.md` 만. 메시지 예: docs(77-06): SIDE Z 범위 UAT 절차서.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
U=.planning/phases/77-side-z-focus-select/77-HUMAN-UAT.md
echo "exists=$(test -f $U && echo 1 || echo 0)"
for K in U-1 U-2 U-3 U-4 U-5 U-6 U-7 U-8 U-9; do echo "$K=$(grep -c "$K" $U)"; done
echo "o1=$(grep -c 'O-1' $U) e1=$(grep -c 'A-77-E1' $U) d06=$(grep -c 'D-77-06' $U) zfocus=$(grep -c '\[ZFocus\]' $U) edge=$(grep -c 'edge-strength' $U) selz=$(grep -c '선택Z' $U) cand=$(grep -c 'SaveZRangeCandidateImages' $U) offname=$(grep -c '_z' $U)"
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `exists=1`, `U-1` ~ `U-9` 각각 >= 1
    - `o1>=1 e1>=1 d06>=1 zfocus>=3 edge>=1 selz>=1 cand>=1 offname>=1`
    - `commit_files` 가 77-HUMAN-UAT.md 하나
  </acceptance_criteria>
  <done>사무실 U-1~U-8 과 O-1 후 SIDE 실기 U-9 절차서가 실제 로그·설정·파일 이름으로 작성되어 커밋되었다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 사용자 확인 — 사무실 UAT U-1~U-8 승인(U-9 SIDE 실기는 O-1 후)</name>
  <what-built>Phase 77 코드 전체(77-01~05: Z 범위 끝 설정, z 사진 누적, 측정별 에지 강도 선택, 3% 기준 Z 동점, 겹침 제외·편집 경고, 선택Z 기록·표시, 수동 안내·오프라인/재검사 선택·후보 사진 저장, 버전 1.7.47.0, 회귀 감사)와 UAT 절차서 77-HUMAN-UAT.md.</what-built>
  <how-to-verify>
    1. `.planning/phases/77-side-z-focus-select/77-HUMAN-UAT.md` 를 연다.
    2. 사무실 PC 에서 U-1(TCP 모의 자동 사이클 — tracer 런타임 증거)부터 U-8(기존 레시피 회귀 동일)까지 수행하고 각 결과 칸에 PASS/FAIL 과 로그 발췌를 적는다.
    3. U-4 에서 A-77-E1(끝 z 중복 시 재평가·CSV 행 추가)을 허용할지 판단해 적는다.
    4. U-9(SIDE 실기)는 제어팀 z 번호 협의(O-1) 전이면 '대기'로 둔다.
    5. 배포(Release 빌드, D:\Data 덮어쓰기)는 이 체크포인트 범위가 아니다 — 앱 종료 확인 후 사용자 승인으로 별도 진행.
  </how-to-verify>
  <resume-signal>"approved" (U-1~U-8 PASS, U-9 대기 포함) 또는 실패한 U 번호와 증상을 적어 주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 시험 절차 → 운영 레시피·배포 exe | 절차 중 운영 파일을 건드리면 현장이 멈춘다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-77-26 | Tampering | UAT 중 D:\Data\Recipe main.ini 직접 수정 | high | mitigate | 절차서가 시험용 레시피 복사본 사용을 명시(Task 1), 운영 main.ini 수정 금지 문구 |
| T-77-27 | Tampering | UAT 중 Release 빌드로 배포 exe 덮어쓰기 | high | mitigate | 체크포인트 how-to-verify 5번 — 배포는 범위 밖, Debug|x64(SIMUL) 로만 시험 |
| T-77-28 | Denial of Service | U-7 체크박스 켠 채 방치로 디스크 고갈 | medium | mitigate | 절차서에 시험 후 체크 해제·파일 정리 단계와 용량 주의 명시 |
</threat_model>

<verification>
1. 77-HUMAN-UAT.md 에 U-1~U-9, O-1 게이트, A-77-E1, D-77-06, 실제 로그 태그 포함
2. 사용자 체크포인트 승인(또는 실패 항목 보고) 기록
</verification>

<success_criteria>
- 사무실 UAT(U-1~U-8) 사용자 승인, SIDE 실기 U-9 는 O-1 후 수행으로 명시
- tracer 경로의 런타임 end-to-end 증거(U-1) 확보
</success_criteria>

<output>
완료 후 `.planning/phases/77-side-z-focus-select/77-06-SUMMARY.md` 작성: UAT 문서 커밋 해시, 사용자 응답 원문, U 번호별 결과표, A-77-E1 판단, U-9 대기 여부.
</output>
