---
phase: 78-reviewer-ng-cause-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ViewModel/CycleResultDto.cs
  - WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs
autonomous: true
requirements: [NGA-01, NGA-02, NGA-05, NGA-07]
user_setup: []

must_haves:
  truths:
    - "리뷰어에서 날짜 폴더 20260916 을 열고 C13_P1(164530363_cycle) NG 행을 누르면 오른쪽 측정표 아래 패널에 '추정 원인: 보정값 또는 티칭이 한쪽으로 치우쳐 있습니다' / '근거: 최근 N회 평균 …' / '확인할 일: …' 세 줄이 보인다 (NGA-01, NGA-02, D-78-01, D-78-04, D-78-05)"
    - "20260916 의 NG 행 36개는 전부 원인 코드 R6 으로 분류된다 — 규칙 엔진은 cycle.json 과 같은 날짜 폴더 이력만 입력으로 받는 순수 함수다 (NGA-01, D-78-03)"
    - "NG 범위 = 사유 Z_RANGE_PENDING·CROSS_Z_INCOMPLETE 제외, 그 밖의 사유가 있으면 NG, 사유 없으면 결과 있고 판정 false 일 때 NG. 20260811 NG 338개는 전부 R2, 20260915 는 R3 33개·R2 159개로 분류된다 (NGA-01, O-78-03)"
    - "[edge NGA-01 empty] Analyze 에 null cycle·null 측정·null 이력이 들어와도 예외 없이 빈 결과(또는 추세 규칙 건너뜀 → R0)를 돌려주고, OK 행은 'NG 가 아닌 항목' 문구, Z_RANGE_PENDING 행은 원인 없음이다"
    - "[edge NGA-05 empty] 옛 cycle.json(20260601·20260811 — 신규 필드 없음)을 전부 읽어도 예외 0 이고, 새 DTO 필드는 기본값(ZRangeStartIndex=-1, ZRangeEndIndex=-1, ZCandidateScores·DatumDiagnostics 빈 목록)으로 채워진다 (NGA-05, NGA-07)"
    - "ReviewerWindow.xaml.cs 추가 줄에는 if/switch/for/foreach/while 이 없다 — 패널 표시는 XAML 바인딩, 원인 계산은 NgCauseAnalyzer, 문자열 조립은 VM(ReviewMeasurementRow) (NGA-02, CLAUDE.md MVVM)"
  artifacts:
    - path: "WPF_Example/UI/ViewModel/CycleResultDto.cs"
      provides: "NgCauseResult / NgCauseHistory / NgCauseAnalyzer(R6·R0·RX·R1~R4) + NGA-07 DTO 계약(ZCandidateScoreDto, DatumDiagnosticDto, ZRange 필드)"
      contains: "public static class NgCauseAnalyzer"
    - path: "WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs"
      provides: "5인자 생성자 + Cause + CausePanelText"
      contains: "public string CausePanelText"
    - path: "WPF_Example/UI/Reviewer/ReviewerWindow.xaml"
      provides: "측정표 아래 NG 원인 패널"
      contains: "txt_ngCausePanel"
  key_links:
    - from: "ReviewerWindow.LoadCycleFolders"
      to: "NgCauseHistory.AddCycle"
      via: "cycle.json 을 읽는 람다 안에서 history.AddCycle(dto) 1줄"
      pattern: "history\\.AddCycle\\(dto\\);"
    - from: "ReviewerWindow.DisplayCycle"
      to: "ReviewMeasurementRow 5인자 생성자 → NgCauseAnalyzer.Analyze"
      via: "rows.Add(new ReviewMeasurementRow(shot, fai, m, cycle, _ngCauseHistory))"
      pattern: "new ReviewMeasurementRow\\(shot, fai, m, cycle, _ngCauseHistory\\)"
    - from: "ReviewerWindow.xaml txt_ngCausePanel"
      to: "ReviewMeasurementRow.CausePanelText"
      via: "Binding SelectedItem.CausePanelText, ElementName=dataGrid_measurements"
      pattern: "SelectedItem.CausePanelText"
  prohibitions:
    - statement: "MUST NOT 추정 원인을 확정된 사실처럼 보여 주지 않는다 — 화면 패널 제목과 첫 줄, 엑셀 열 이름에 '추정' 을 붙인다(비전 초보 운영자가 추정을 사실로 믿고 보정값을 잘못 바꾸는 것을 막는다)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT 원인 분석이 외부 서비스·네트워크·외부 AI 를 쓰지 않는다 — 장비 PC 안에서 저장된 cycle.json 만으로 판단한다 (D-78-03)"
      status: resolved
      verification: judgment
---

<objective>
Tracer 한 줄: **R6(보정값/티칭 치우침) 규칙 1개**를 실데이터 cycle.json → 날짜 폴더 이력(NgCauseHistory) → 규칙 엔진(NgCauseAnalyzer) → 행 VM(ReviewMeasurementRow) → 리뷰어 패널(XAML 바인딩)까지 끝까지 관통시키고, 20260916 실데이터로 확인한다. 이어서 사유 규칙(R1~R4)과 NGA-07 DTO 계약(다른 plan 이 쓰고 읽을 필드)을 같은 파일에 더한다.

Purpose: 가장 위험한 설계 가정 — "리뷰어가 이미 읽은 날짜 폴더 cycle.json 만으로 추세 규칙을 판정하고, code-behind 분기 없이 화면에 3줄을 띄울 수 있다" — 을 첫 커밋에서 증명한다. R5·R7·R8·R9 와 대표/함께 의심 순서는 78-02, 진단 값 기록은 78-03, 사진·버튼 정리는 78-04, NG 누적 엑셀은 78-05 가 이 뼈대에 붙인다.

Output: 기존 파일 4개에 삽입 위주 변경. 신규 .cs 없음. 저장소 밖 probe(NgCauseProbe) 생성.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-CONTEXT.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-RESEARCH.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-PATTERNS.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-VALIDATION.md

<interfaces>
계획 시점 라인 번호(편집 전 기준). 실행자는 이 계약을 그대로 쓴다.

CycleResultDto.cs (Allman, namespace ReringProject.UI, using System / System.Collections.Generic / System.Text / ReringProject.Halcon.Models / ReringProject.Sequence)
- `:13-67` CycleResultDto (`:34` CycleFolderPath, `:52` ZIndex=-1, `:28` IsProtocolDriven, `:16` InspectionTime, `:63` ZRangeImages 이니셜라이저 선례)
- `:94-107` ShotResultDto (`:104` ResultImagePath, `:106` FAIs)
- `:110-128` FaiResultDto (`:120` OriginImageFileName, `:127` LastOverlays = List<EdgeInspectionOverlay>)
- `:131-168` MeasurementResultDto (`:143` LastMeasuredValue, `:146` LastJudgement, `:149` LastHasResult, `:152` LastSkipReason, `:155` LastErrorMessage, `:167` SelectedZIndex = MeasurementBase.SELECTED_Z_NONE)
- `:189-609` ReviewerListLabelBuilder (`:231-238` IsHandled — 같은 NG 정의 선례), `:610` namespace 닫는 중괄호. 새 클래스는 `:609` 다음, `:610` 앞에 넣는다.

ReviewMeasurementRow.cs (Allman, namespace ReringProject.UI)
- `:11-123` class, `:55-122` 기존 3인자 생성자(m.MeasurementName 을 null 검사 없이 읽음), `:35` SelectedZText 선례

ReviewerWindow.xaml.cs (Allman)
- `:33-36` 필드(_allRows, _allCycleItems), `:56-97` LoadCycleFolders — `:61-66` 폴더 없음 분기, `:68-84` LINQ Select 람다(`:73` `var dto = CycleResultSerializer.Load(...)`), `:86` `_allCycleItems = items;`
- `:126-188` DisplayCycle — `:157` `rows.Add(new ReviewMeasurementRow(shot, fai, m));`

ReviewerWindow.xaml
- `:156-220` 오른쪽 DataGrid(`:157-158` `x:Name="dataGrid_measurements"` + `Grid.Row="1" Grid.Column="4"`), Halcon 창은 `:154` Grid.Column="2"(airspace — 그 위에 WPF 요소 금지)

SkipReason.cs 상수: DATUM_FAIL, ALIGN_FAIL, NO_IMAGE, DATUM_REF_MISSING, ZINDEX_MISCONFIGURED, CROSS_Z_INCOMPLETE, MEASURE_FAIL, Z_RANGE_PENDING
MeasurementBase: `SELECTED_Z_NONE`(-1), `FormatSelectedZ(int)`, EvaluateJudgement 공차 규칙(lower = 공칭-|Tol-|, upper = 공칭+|Tol+|, lower>upper 면 교환, 경계 포함)
</interfaces>
</context>

## 계획 단계 결정 (phase 전체 — 78-02~07 도 이 표를 따른다)

| # | 질문 | 결정 | 근거 |
|---|---|---|---|
| P-1 | NG 범위 | `IsNgMeasurement`: 측정 null → false, 사유 Z_RANGE_PENDING·CROSS_Z_INCOMPLETE → false, 그 밖의 사유가 비어 있지 않으면 → true(모르는 새 사유 포함), 사유 없으면 LastHasResult 이고 LastJudgement false 일 때만 true. CROSS_Z_INCOMPLETE 는 NG 가 아니라 엑셀에서 빠지지만, 그 행을 누르면 R4 설명을 보여 준다 | O-78-03, 기존 IsHandled/FillTickSummary 와 같은 NG 정의 |
| P-2 | 규칙 입력 | 순수 함수 `Analyze(cycle, shot, fai, m, history)`. 파일 I/O·전역 싱글턴·현재 레시피 조회 없음. 추세 이력 = 같은 날짜 폴더 cycle.json 전체(리뷰어가 목록을 만들 때 이미 읽은 것) | D-78-03, RESEARCH Pitfall 3, OQ2 |
| P-3 | 이력 키·정렬 | 측정 키 = ShotName + U+001F + FAIName + U+001F + MeasurementName(null → 빈 문자열). 사이클 키 = CycleFolderPath 끝 `\` `/` 제거, 비면 `"T"` + InspectionTime.Ticks. 정렬 = InspectionTime 오름차순 → 사이클 키 string.CompareOrdinal. 같은 사이클 키 두 번 추가는 무시 | edge NGA-01 ordering |
| P-4 | 대표 원인 순서 | 사유 규칙(R1~R4, RX)은 단독. 값 이탈은 발동 목록을 **R5(강) → R9 → R6 → R8 → R7 → R5(약)** 순서로 쌓고 첫 항목 = 대표, 나머지 = 함께 의심. 없으면 R0 | CONTEXT "가장 구체적인 것 1개", 09-15 사용자 분석(치우침 = 보정/티칭 문제) |
| P-5 | R6 | 창 = 이 사이클까지의 같은 측정 결과 최근 5개(`R6_WINDOW_SIZE`), 3개(`R6_MIN_SAMPLES`) 미만이면 뒤 사이클 결과로 5개까지 채움. 3개 이상 + 전부 공칭보다 엄격히 크거나 전부 엄격히 작음 + 평균이 허용 범위 밖(엄격) | 계획 단계 실데이터 시뮬레이션: 20260916 NG 36/36 |
| P-6 | R9 | 넘은 쪽 허용 폭(위쪽 |Tol+|, 아래쪽 |Tol-|) > 0, 넘은 양 ≤ 허용 폭 × 0.10(`R9_BOUNDARY_RATIO`, 포함), 이력상 바로 앞 결과가 OK | A1 |
| P-7 | R5 | 같은 사이클·같은 FAI 안에서 TypeName `EdgeToLineDistance` + RoiId `FAI-DistLine` 오버레이(같은 MeasurementName, Points ≥ 2, 발끝→에지 길이 > 0) + 이웃 결과(이력에서 앞 2개·뒤 2개, 자기 제외) 2개 이상인 측정 묶음. 편차 = 값 − 이웃 중앙값, 묶음 첫 측정의 발끝→에지 단위벡터와 내적이 0 이상이면 이동량 = −편차, 아니면 +편차. 발동 = 묶음 2개 이상 + 이동량 전부 같은 부호(0 제외) + |이동량 중앙값| ≥ 0.15 × 묶음 최소 허용 폭 + 최대 |이동량| ≤ 2.0 × 최소 |이동량|. 강 = |중앙값| ≥ 이 측정 허용 폭(min(|Tol+|,|Tol-|)) 또는 (값 − 편차) 가 허용 범위 안 | 실데이터 시뮬레이션: 20260915 34건(강 2 — 135946717 C13_P3), 20260916 3건 |
| P-8 | R8 | 범위 끝 = ShotResultDto.ZRangeEndIndex(78-03 이 기록, ≥1). 없으면 cycle.IsProtocolDriven 이고 cycle.ZIndex ≥ 1 이고 측정 SelectedZIndex ≥ 1 일 때 cycle.ZIndex(범위 선택은 범위 끝 tick 에서만 일어나므로 검사 당시 기록된 z 번호 = 범위 끝). 둘 다 없으면 건너뜀(데이터 없음). **현재 레시피는 절대 읽지 않는다.** 범위 시작 쪽 선택은 동점 규칙의 기본값이라 판정하지 않는다 | OQ2(현재 레시피 금지·기록값 사용) + CONTEXT 성공 기준 3(09-16 은 신규 필드가 없는 파일) 조정. 실측 SelectedZ > tick ZIndex 0건 |
| P-9 | R7 | 사이클 전체 NG 수(P-1 기준) = 1 이고 R5(강)·R6·R9 가 발동하지 않음 | 20260601 에서 4건 |
| P-10 | 문구·숫자 | 원인/근거/확인할 일은 const 문자열 + string.Format 템플릿. 패널 줄 접두 `추정 원인: ` / `근거: ` / `확인할 일: ` / `함께 의심: `. 값·공칭·허용 F4, mm 이동량 F4, 선명도 점수 F1, 각도 F3, 패턴 점수 F2 — 표시 전용, 판정은 double 원값 비교(반올림·epsilon 없음) | D-78-05, edge precision |
| P-11 | 화면 | 오른쪽 열(Grid.Column 4) 안에 Grid 를 두고 DataGrid 아래 패널. `txt_ngCausePanel.Text` = `{Binding SelectedItem.CausePanelText, ElementName=dataGrid_measurements, FallbackValue=…}` — code-behind 코드 없음 | O-78-05, airspace |
| P-12 | 엑셀 | `ResultSavePath\NG_분석_누적.xlsx`, 시트 `NG 누적`, 20열(78-05 표). 중복 키 = 사이클 폴더 + Shot + FAI + 측정명 | O-78-01/04/07 + OQ1. OQ1 키(폴더+측정명)를 Shot·FAI 로 좁힘 — 20260811 에서 한 사이클 안 같은 측정명이 다른 Shot/FAI 에 3,699건(20260601 10건), 폴더+측정명만 쓰면 서로 다른 NG 가 버려져 D-78-02 위반 |
| P-13 | 엑셀 쓰기 | 이 날짜 폴더(리뷰어가 연 폴더)만 대상. 잠금 선검사(FileShare.None 열기) → 열기/새로 만들기 → 추가 → 같은 폴더 임시 xlsx 에 SaveAs → File.Replace(대상 없으면 File.Move). 잠금·실패 시 원본 불변, 한국어 안내 | OQ1, 계획 단계 ClosedXML 0.105 왕복·잠금 실측 PASS |
| P-14 | 사진 | 행: FAI OriginImageFileName 이 절대 경로·존재·크기 > 0 이면 그것, 아니면 Shot ResultImagePath(존재할 때), 둘 다 아니면 사진 교체 안 함. 사이클 전체 보기: 측정 결과(또는 결과성 사유)가 있는 첫 FAI 의 origin, 없으면 첫 Shot ResultImagePath | D-78-07. 실데이터 20260915~17 origin 은 전부 절대 경로·존재 |
| P-15 | 진단 기록 | `CycleResultDto.DatumDiagnostics`(시퀀스의 Datum 전부), `MeasurementResultDto.ZCandidateScores`(평가 순서), `ShotResultDto.ZRangeStartIndex/ZRangeEndIndex`(IsZRangeEnabled 일 때만). 런타임 보관은 public 필드 + `[Newtonsoft.Json.JsonIgnore]` — 레시피 INI/JSON 에 새 키 없음. 비유한수 → 0.0 | D-78-08 |
| P-16 | 점수 노출 | Z 후보 선명도 점수는 결과 그리드·CSV 에는 계속 안 보이고 NG 원인 패널·NG 누적 엑셀의 R8 근거에만 숫자로 보인다 | D-78-03(근거 숫자) + D-78-08 이 D-77-07 ⑥ 을 이 두 곳에 한해 재개방 |
| P-17 | 삭제 범위 | 리뷰어 버튼 2개 + 그 Click 핸들러 2개 + 사이클 1건 xlsx 생성 메서드 + 스모크 PNG 메서드만. ExcelExportService 공용 헬퍼·ChartImageCapture 차트 렌더는 유지 | D-78-06, RESEARCH Pitfall 1 |

## Spec-less probe 원장 (edge 21건 / prohibition 5건 — phase 전체)

| 항목 | 처리 | 위치 |
|---|---|---|
| NGA-01 empty | covered → truth (null/빈 이력/OK 행) | 78-01, 78-02 |
| NGA-01 boundary | covered → truth (R9 10% 포함, R6 최소 3개·엄격 비교, R5 0.15·2.0 포함) | 78-02 |
| NGA-01 adjacency | covered → truth (여러 규칙 동시 발동 → 대표 1 + 함께 의심, 공칭과 같은 값은 한쪽 아님) | 78-02 |
| NGA-01 ordering | covered → truth (이력 정렬·동시각 ordinal, 후보 점수 z 오름차순 표시, 함께 의심 고정 순서) | 78-02 |
| NGA-01 precision | covered → truth (double 원값 비교, 표시만 F4/F1) | 78-02 |
| NGA-02 unclassified | **unresolved → flagged assumption A-78-E1** (비전 초보 가독성·패널 폭) | 78-07 UAT U-1 |
| NGA-03 empty | covered → truth (폴더 미선택·사이클 0·NG 0·새 NG 0 → 파일 안 건드림) | 78-05 |
| NGA-03 encoding | covered → truth (키 비교: 폴더 경로 대소문자 무시·끝 구분자 제거, 이름은 ordinal 그대로, 한글 xlsx 왕복) | 78-05 |
| NGA-03 concurrency | covered → truth (잠금 → 중단·원본 해시 불변, 임시 파일 + File.Replace, 쓰는 중 cycle.json → 건너뜀·다음에 추가) | 78-05 |
| NGA-04 adjacency | covered → truth (공용 헬퍼 유지 — 반복도/CPK export 컴파일·호출 유지) | 78-04 |
| NGA-04 empty | covered → truth (사이클 미선택 상태에서 사라진 버튼 참조 0) | 78-04 |
| NGA-04 ordering | covered → truth (왼쪽 버튼 스택 순서 고정) | 78-04, 78-05 |
| NGA-04 idempotency | covered → truth (NG 누적 버튼 두 번 → 두 번째 추가 0) | 78-05 |
| NGA-04 concurrency | **backstop** (반복 검사 진행 중·라이브 검사 중 NG 누적 저장) | 78-07 U-7 |
| NGA-05 concurrency | covered → truth (쓰는 중 cycle.json → Load null → 목록 폴더명·분석 건너뜀, 늘어나는 20260917 probe 예외 0) | 78-02, 78-06 |
| NGA-06 concurrency | **backstop** + flagged assumption A-78-A3 (방금 끝난 사이클의 원본 사진 비동기 쓰기) | 78-04, 78-07 U-2 |
| NGA-07 boundary | covered → truth (범위 꺼짐·오설정 → -1, 이번 tick 검출 = 스탬프 ≥ tick 시작) | 78-03 |
| NGA-07 adjacency | covered → truth (같은 점수 후보도 평가 순서대로 모두 기록) | 78-03 |
| NGA-07 empty | covered → truth (후보 없음 → 빈 목록, ClearResult 에서 null 리셋, 옛 JSON 기본값) | 78-01, 78-03 |
| NGA-07 ordering | covered → truth (후보 = 평가 순서 z 오름차순, Datum = DatumConfigs 순서) | 78-03 |
| NGA-07 precision | covered → truth (JSON double 왕복 무손실, NaN/무한대 → 0.0) | 78-03 |
| PR-1 추정을 사실처럼 표시 금지 / PR-2 외부 AI·네트워크 금지 | prohibitions (descriptor 없음) | 78-01 |
| PR-3 검사 판정·PLC 응답·통계 CSV 변경 금지 | prohibition | 78-03 |
| PR-4 과거 사이클을 현재 레시피로 판정 금지 | prohibition | 78-02 |
| PR-5 NG 누적 파일 1개 외 D:\Data 쓰기·cycle.json/사진 수정 금지 | prohibition | 78-05 |
| (canon) xlsx 경로 조작·수식 주입 | breadcrumb — /gsd-secure-phase 소관. 파일명은 고정 const, 셀 값은 문자열 대입. prohibition 으로 만들지 않음 | — |

### Flagged assumptions (자동 해소 금지)
- **A-78-E1 (NGA-02 unclassified):** 원인 3줄(+함께 의심)이 폭 430px 오른쪽 열에서 비전 초보에게 읽히는지는 사람 판단이다. 78-07 UAT U-1 에서 사용자가 판단하고, 문구 수정은 const 만 바꾸면 된다.
- **A-78-A1:** R5/R6/R9 임계값(0.15·2.0 / 5·3 / 0.10)은 20260915~17 SIDE_1 표본으로만 맞췄다. TOP/BOTTOM·다른 제품에서 과다/과소 판정 여부는 UAT 와 운영 중 확인한다.
- **A-78-A2:** R5 는 "같은 FAI = 같은 기준선" 으로 묶는다(cycle.json 측정에 DatumRef 가 없다). FAI 하나에 기준선이 여럿인 레시피는 묶음 조건(같은 부호·비슷한 크기)에서 대부분 걸러지지만 확인 필요.
- **A-78-A3 (NGA-06 concurrency):** 원본 사진은 검사 직후 워커가 비동기로 쓴다. 쓰는 도중(크기 > 0, 미완성)인 파일을 리뷰어가 열면 기존 전역 예외 창이 뜰 수 있다 — 빈도 낮음, 다시 누르면 정상. 78-07 U-2 에서 확인.

## Artifacts this phase produces (phase 전체 원장)

| 심볼 / 산출물 | 종류 | 파일 | 만드는 곳 |
|---|---|---|---|
| `NgCauseResult` (IsNg, HasCause, CauseCode, CauseText, EvidenceText, ActionText, SuspectCodes, SuspectText, `Empty()`, `NotNg()`) | 클래스 | CycleResultDto.cs | 78-01 |
| `NgHistorySample` (InspectionTime, CycleKey, Value, IsOk), `NgCauseHistory` (`AddCycle`, `ResolveCycleKey`, `SampleCount`, `GetSamples`, `KEY_SEPARATOR`, `CYCLE_KEY_TIME_PREFIX`) | 클래스 | CycleResultDto.cs | 78-01 |
| `NgCauseAnalyzer` — `IsNgMeasurement`, `Analyze`, `BuildPanelText`, `CODE_R0`~`CODE_R9`·`CODE_UNKNOWN`("RX")·`CODE_NONE`(""), `R6_WINDOW_SIZE`(5), `R6_MIN_SAMPLES`(3), R0/R6/RX/R1~R4 문구 const, `NOT_NG_TEXT`, 패널 접두 const | 정적 클래스/const | CycleResultDto.cs | 78-01 |
| `R9_BOUNDARY_RATIO`(0.10), `R5_NEIGHBORS_EACH_SIDE`(2), `R5_MIN_NEIGHBORS`(2), `R5_MIN_SHARED_COUNT`(2), `R5_MIN_SHIFT_TOL_RATIO`(0.15), `R5_SIMILAR_FACTOR`(2.0), `R5_SUPPORTED_TYPE_NAME`("EdgeToLineDistance"), `DIST_LINE_ROI_ID`("FAI-DistLine"), `R7_LONE_NG_COUNT`(1), R5/R7/R8/R9 문구·라벨 const, `Median`, 발동 목록·함께 의심 조립 | const/메서드 | CycleResultDto.cs | 78-02 |
| `ZCandidateScoreDto`(ZIndex, Ok, Score), `DatumDiagnosticDto`(DatumName, IsDetected, IsDetectedThisTick, DetectTime, OriginRow, OriginCol, AngleDeg, EdgeCount, FitRmse, AlignMatchScore, AlignMatchRow, AlignMatchCol, AlignMatchAngleDeg, Align2Score, AlignThetaDeg), `ShotResultDto.Z_RANGE_NONE`(-1)/`ZRangeStartIndex`/`ZRangeEndIndex`, `MeasurementResultDto.ZCandidateScores`, `CycleResultDto.DatumDiagnostics` | DTO(JSON) | CycleResultDto.cs | 78-01 |
| `ReviewMeasurementRow(shot, fai, m, cycle, history)` 생성자, `Cause`, `CausePanelText` | VM | ReviewMeasurementRow.cs | 78-01 |
| `ReviewerImagePathResolver` (`ResolveRowImagePath`, `ResolveCycleImagePath`, `IsUsableImageFile`, `MIN_IMAGE_FILE_BYTES`) | 정적 클래스 | ReviewMeasurementRow.cs | 78-04 |
| XAML `border_ngCause`, `txt_ngCauseHeader`("NG 원인 분석 (추정)"), `txt_ngCausePanel` | XAML | ReviewerWindow.xaml | 78-01 |
| `_ngCauseHistory` 필드 | 배선 | ReviewerWindow.xaml.cs | 78-01 |
| `_loadedDateFolder`, `Button_NgAccumExport_Click`, XAML `btn_ngAccumExport`("NG 누적 엑셀 저장") | 배선/XAML | ReviewerWindow.xaml(.cs) | 78-05 |
| 삭제: 사이클 1건 엑셀 버튼·핸들러, 차트 캡처 점검 버튼·핸들러, 사이클 1건 xlsx 생성 메서드, 스모크 PNG 메서드 | 삭제 | ReviewerWindow.xaml(.cs), ExcelExportService.cs, ChartImageCapture.cs | 78-04 |
| `MeasurementBase.LastZCandidateScores` (public 필드, JsonIgnore) | 런타임 필드 | MeasurementBase.cs | 78-03 |
| `Action_FAIMeasurement.BuildZCandidateScoreList` | 메서드 | Action_FAIMeasurement.cs | 78-03 |
| `DatumConfig.LastAlignMatchScore/LastAlignMatchRow/LastAlignMatchCol/LastAlignMatchAngleDeg` (public 필드, JsonIgnore) | 런타임 필드 | DatumConfig.cs | 78-03 |
| `CycleResultSerializer.BuildDatumDiagnostic(DatumConfig, DateTime)`, `ToFiniteOrZero(double)` | 메서드 | CycleResultSerializer.cs | 78-03 |
| `InspectionSequence.BuildTickDatumDiagnosticsSnapshot()` | 메서드 | InspectionSequence.cs | 78-03 |
| `NgAccumulationExportService` (`OUTPUT_FILE_NAME`="NG_분석_누적.xlsx", `SHEET_NAME`="NG 누적", `MESSAGE_TITLE`="NG 누적 엑셀", `BuildOutputPath`, `AppendDateFolder`), `NgAccumExportOutcome`, `ENgAccumExportStatus`(Added/NothingNew/NoNg/NoCycles/NoFolder/FileLocked/Failed) | 정적 클래스/클래스/enum | ExcelExportService.cs | 78-05 |
| 파일 `D:\Data\Result\NG_분석_누적.xlsx` (ResultSavePath 기준, 버튼을 누를 때만 생성) | 산출 파일 | — | 78-05 |
| VersionDefine 1.7.49.0 항목 | changelog | VersionDefine.cs | 78-06 |
| `78-HUMAN-UAT.md` | UAT 절차서 | phase 디렉터리 | 78-07 |
| `NgCauseProbe.cs/.exe` (저장소 밖 `C:/Users/admin/AppData/Local/Temp/p78-probe`, 커밋 금지) — 모드 rows / synthetic / json / image / xlsx | probe | — | 78-01~05 |

## Probe 규격 (저장소 밖, 커밋 금지 — 78-02~06 도 이 규격을 확장한다)

- 폴더: `H=/c/Users/admin/AppData/Local/Temp/p78-probe` (Windows 표기 `C:/Users/admin/AppData/Local/Temp/p78-probe`). probe 는 저장소 코드가 아니라 CLAUDE.md 가독성 규칙 대상이 아니다. D:\Data 는 읽기만 한다.
- 소스 `$H/NgCauseProbe.cs` 는 C# 5 수준. Quick 260915-k5g `ReviewerLabelProbe` 와 같은 골격: `Main(args)` 가 `args[0]`(binDir)을 저장하고 `AppDomain.CurrentDomain.AssemblyResolve` 에서 binDir 의 `<이름>.dll`/`<이름>.exe` 를 LoadFrom, 실제 작업은 `[MethodImpl(MethodImplOptions.NoInlining)]` 메서드에서 한다. `args[1]` = 모드.
- 컴파일(매 빌드 후 다시):
  `FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug; cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config NgCauseProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:NgCauseProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:ClosedXML.dll -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll NgCauseProbe.cs`
  (계획 단계에서 이 명령으로 DatumMeasurement.exe·ClosedXML·DatumConfig 생성이 되는 것을 확인했다. exe.config 복사는 ClosedXML 바인딩 리다이렉트 때문에 필수.)
- **rows 모드** (78-01): `NgCauseProbe.exe <binDir> rows <outDir> <dateDir>...`. 날짜 폴더마다: 하위 폴더를 이름 ordinal 정렬, `cycle.json` 있는 것만 `CycleResultSerializer.Load`(null 이면 loadnull 증가), 로드된 dto 전부를 `NgCauseHistory.AddCycle` 한 뒤 dto 마다 Shot/FAI/측정 전부에 `new ReviewMeasurementRow(shot, fai, m, dto, history)` 를 만든다. 예외는 잡아서 exceptions 를 세고 계속한다. `<outDir>/<날짜>.tsv` 에 1행 = 1측정: `날짜/폴더명 \t ShotName \t FAIName \t MeasurementName \t JudgeText \t IsNg(0|1) \t CauseCode(비면 -) \t SuspectCodes(쉼표, 없으면 -) \t CausePanelText(줄바꿈 → " / ", 탭 제거)`. 표준 출력 1줄: `date=<날짜> cycles=<n> loadnull=<k> rows=<r> ng=<IsNg 1 인 행 수> codes=R0:<>,R1:<>,R2:<>,R3:<>,R4:<>,R5:<>,R6:<>,R7:<>,R8:<>,R9:<>,RX:<> suspects=R5:<>,R6:<>,R7:<>,R8:<>,R9:<> newfields=<ZRangeStartIndex≥1 또는 ZRangeEndIndex≥1 인 Shot 수 + ZCandidateScores 가 비지 않은 측정 수 + DatumDiagnostics 가 비지 않은 사이클 수> exceptions=<e>`. codes 는 IsNg=1 행의 CauseCode 집계, suspects 는 IsNg=1 행의 SuspectCodes 집계.
- 이후 plan 이 추가하는 모드: `synthetic`(78-01 Task 2 시작, 78-02 확장), `json`(78-03), `image`(78-04), `xlsx`·`xlsxlock`·`xlsxempty`·`xlsxequal`(78-05). 기존 모드는 지우지 않는다.

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): R6 치우침 1개 규칙 — cycle.json → 날짜 폴더 이력 → 규칙 엔진 → 행 VM → 리뷰어 패널, 20260916 실데이터 확인</name>
  <precondition>`git status --porcelain -- WPF_Example/UI` 출력이 비어 있고 Debug|x64 빌드가 편집 전 상태에서 성공한다(다른 작업의 미커밋 변경이 비교에 섞이지 않는다)</precondition>
  <reversibility rating="reversible">리뷰어 표시·순수 함수 추가뿐이다. cycle.json·CSV·레시피 형식은 이 task 에서 바뀌지 않는다.</reversibility>
  <files>WPF_Example/UI/ViewModel/CycleResultDto.cs, WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs, WPF_Example/UI/Reviewer/ReviewerWindow.xaml, WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs</files>
  <read_first>
    - CLAUDE.md (Code Style — 가독성 하드룰 전체, 위반 = 회귀)
    - .planning/phases/78-reviewer-ng-cause-analysis/78-CONTEXT.md (D-78-01~08, 원인 규칙 후보 R1~R9, O-78-03/05)
    - 이 PLAN 의 '계획 단계 결정' P-1~P-5, P-10, P-11 과 interfaces 블록, 'Probe 규격'
    - .planning/phases/78-reviewer-ng-cause-analysis/78-PATTERNS.md (CycleResultDto.cs · ReviewMeasurementRow.cs · ReviewerWindow 절)
    - WPF_Example/UI/ViewModel/CycleResultDto.cs 전체
    - WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs 전체
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml 전체, WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs:1-190
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs:140-195 (EvaluateJudgement 공차 규칙)
    - .planning/quick/260915-k5g-z/260915-k5g-PLAN.md:111-200 (probe 골격·AssemblyResolve)
  </read_first>
  <action>
모든 변경은 기존 줄 사이에 새 줄을 끼워 넣는 방식이다. 기존 줄 수정은 ReviewerWindow.xaml.cs 의 `rows.Add(...)` 1줄과 ReviewerWindow.xaml 의 DataGrid `Grid.Row`/`Grid.Column` 속성 줄뿐이다.

(0) probe 준비: `mkdir -p $H` 후 `git rev-parse HEAD > $H/base-78-01.txt`. 편집 전 Debug|x64 빌드가 성공하는지 한 번 확인한다(실패하면 멈추고 보고).

(1) CycleResultDto.cs — ReviewerListLabelBuilder 닫는 중괄호(:609) 다음, namespace 닫기(:610) 앞에 Allman 스타일로 3개 클래스를 추가한다(P-2, P-3, P-10, D-78-03, D-78-05).
- `public class NgCauseResult`: 자동 프로퍼티 `bool IsNg`, `bool HasCause`, `string CauseCode`(= ""), `string CauseText`(= ""), `string EvidenceText`(= ""), `string ActionText`(= ""), `List<string> SuspectCodes`(= new List<string>()), `string SuspectText`(= ""). `public static NgCauseResult Empty()` — 전부 기본값. `public static NgCauseResult NotNg()` — IsNg false, HasCause false, CauseText = NgCauseAnalyzer.NOT_NG_TEXT. XML 요약 주석으로 "화면 패널과 NG 누적 엑셀이 같은 결과를 쓴다(D-78-04)" 한 줄.
- `public class NgHistorySample`: 자동 프로퍼티 `DateTime InspectionTime`, `string CycleKey`, `double Value`, `bool IsOk`.
- `public class NgCauseHistory`: `public const string KEY_SEPARATOR = "\u001F";`, `public const string CYCLE_KEY_TIME_PREFIX = "T";`, 필드 `private readonly Dictionary<string, List<NgHistorySample>> _dicSamples`, `private readonly HashSet<string> _setCycleKeys`(ordinal), `private bool _bSorted`. 메서드: `public static string ResolveCycleKey(CycleResultDto dto)` — dto null 이면 빈 문자열, CycleFolderPath 가 비어 있지 않으면 끝의 `\`·`/` 를 TrimEnd 한 값, 비어 있으면 CYCLE_KEY_TIME_PREFIX + InspectionTime.Ticks. `public static string BuildMeasurementKey(string szShot, string szFai, string szMeas)` — null 은 빈 문자열로 바꾸고 KEY_SEPARATOR 로 연결. `public void AddCycle(CycleResultDto dto)` — dto null 이면 return, 사이클 키가 이미 있으면 return(같은 사이클 두 번 추가 무시), Shots/FAIs/Measurements 각 단계 null 가드(ReviewerListLabelBuilder.CollectMeasurements 와 같은 방식), LastHasResult 인 측정만 NgHistorySample 로 추가, `_bSorted = false`. `public int SampleCount` getter. `public List<NgHistorySample> GetSamples(string szMeasurementKey)` — 없으면 빈 목록, 반환 전 `_bSorted` 가 false 면 모든 목록을 InspectionTime 오름차순 → CycleKey string.CompareOrdinal 로 정렬(비교 메서드는 이름 있는 private static 메서드, 람다 식 본문 금지).
- `public static class NgCauseAnalyzer` (이 task 범위): 코드 const `CODE_NONE = ""`, `CODE_R0 = "R0"` … `CODE_R9 = "R9"`, `CODE_UNKNOWN = "RX"`. 문구 const: `NOT_NG_TEXT = "이 항목은 NG 가 아니라 원인 분석 대상이 아닙니다"`, `PANEL_CAUSE_PREFIX = "추정 원인: "`, `PANEL_EVIDENCE_PREFIX = "근거: "`, `PANEL_ACTION_PREFIX = "확인할 일: "`, `PANEL_SUSPECT_PREFIX = "함께 의심: "`, `PANEL_LINE_SEPARATOR = "\n"`, `VALUE_FORMAT = "F4"`, `SIDE_BIG_TEXT = "큰"`, `SIDE_SMALL_TEXT = "작은"`. R6: `R6_WINDOW_SIZE = 5`, `R6_MIN_SAMPLES = 3`, `R6_CAUSE_TEXT = "보정값 또는 티칭이 한쪽으로 치우쳐 있습니다"`, `R6_EVIDENCE_FORMAT = "최근 {0}회 평균 {1} · 기준 {2} (허용 {3} ~ {4}) · {0}회 모두 {5} 쪽"`, `R6_ACTION_TEXT = "Shot 보정값과 이 측정의 티칭 위치를 확인하세요"`. R0: `R0_CAUSE_TEXT = "공차를 벗어났습니다 (규칙으로 원인을 특정하지 못함)"`, `R0_EVIDENCE_FORMAT = "측정 {0} · 허용 {1} ~ {2} · {3} 벗어남"`, `R0_ACTION_TEXT = "사진에서 측정 위치를 보고, 같은 자재로 다시 검사해 보세요"`. RX: `RX_CAUSE_TEXT = "알 수 없는 사유로 측정하지 못했습니다"`, `RX_EVIDENCE_FORMAT = "사유 {0}"`, `RX_ACTION_TEXT = "같은 시각의 Error 로그를 확인하세요"`.
  메서드: `public static bool IsNgMeasurement(MeasurementResultDto m)` — P-1 순서 그대로(가드 절). `public static NgCauseResult Analyze(CycleResultDto cycle, ShotResultDto shot, FaiResultDto fai, MeasurementResultDto m, NgCauseHistory history)` — cycle 또는 m 이 null 이면 Empty(); IsNgMeasurement false 면 NotNg(); 사유가 비어 있지 않으면 BuildReasonResult(m)(이 task 에서는 모든 사유 → RX, 증거 = 사유 문자열); 그 외(값 이탈) → BuildOutOfToleranceResult(...). `private static void ResolveToleranceBand(MeasurementResultDto m, out double dLower, out double dUpper)` — EvaluateJudgement 와 같은 규칙(교환 포함). BuildOutOfToleranceResult: history 가 null 이 아니고 R6 발동이면 R6 결과, 아니면 R0 결과. `private static bool TryEvaluateR6(...)` — P-5 그대로: history.GetSamples(키)에서 이 사이클 키 위치를 찾고(없으면 false), 앞쪽 최대 R6_WINDOW_SIZE 개(자기 포함)를 모은 뒤 R6_MIN_SAMPLES 미만이면 뒤쪽으로 R6_WINDOW_SIZE 개까지 채우고, 개수·전부 한쪽(엄격)·평균 범위 밖(엄격)을 이름 있는 bool 로 판정, out 으로 개수·평균·방향. R0 증거의 넘은 양 = 값이 위로 넘으면 값 − upper, 아래로 넘으면 lower − 값. 모든 숫자는 VALUE_FORMAT, 방향은 SIDE_BIG_TEXT/SIDE_SMALL_TEXT. `public static string BuildPanelText(NgCauseResult r)` — r null 이면 빈 문자열, HasCause false 면 r.CauseText, 아니면 접두 붙인 원인/근거/확인할 일 3줄 + SuspectText 가 비어 있지 않으면 함께 의심 줄, PANEL_LINE_SEPARATOR 로 연결. HasCause 는 코드가 정해진 모든 결과에서 true.
  이 클래스 안에서는 파일 I/O·전역 싱글턴·레시피 객체를 쓰지 않는다(P-2).

(2) ReviewMeasurementRow.cs — SelectedZText 프로퍼티 다음에 `public NgCauseResult Cause { get; set; } = NgCauseResult.Empty();` 와 `public string CausePanelText { get; set; } = "";`(XML 요약: NG 원인 패널에 바인딩되는 표시 문자열, NgCauseAnalyzer.BuildPanelText 결과). 기존 3인자 생성자 다음에 `public ReviewMeasurementRow(ShotResultDto shot, FaiResultDto fai, MeasurementResultDto m, CycleResultDto cycle, NgCauseHistory history) : this(shot, fai, m)` — 본문 2줄: `Cause = NgCauseAnalyzer.Analyze(cycle, shot, fai, m, history);`, `CausePanelText = NgCauseAnalyzer.BuildPanelText(Cause);`. 기존 생성자는 건드리지 않는다.

(3) ReviewerWindow.xaml — 오른쪽 DataGrid 를 오른쪽 열 안의 Grid 로 감싼다(P-11, O-78-05). DataGrid 여는 태그 바로 앞에 `<Grid Grid.Row="1" Grid.Column="4">` 와 RowDefinitions(`*`, `Auto`)를 넣고, DataGrid 의 `Grid.Row="1" Grid.Column="4"` 를 `Grid.Row="0"` 으로 바꾼다(DataGrid 블록은 들여쓰기를 바꾸지 않는다). `</DataGrid>` 다음에 `Border x:Name="border_ngCause" Grid.Row="1"`(BorderBrush Gray, BorderThickness 0,1,0,0, Background #FFF8E1, Padding 6,4) 안에 StackPanel: `TextBlock x:Name="txt_ngCauseHeader" Text="NG 원인 분석 (추정)"`(FontSize 11, Foreground Gray), `TextBlock x:Name="txt_ngCausePanel"`(FontSize 12, TextWrapping Wrap, Foreground #334155, Text 바인딩 = `SelectedItem.CausePanelText`, ElementName `dataGrid_measurements`, FallbackValue `'NG 행을 누르면 추정 원인이 여기에 보입니다'`, TargetNullValue 같은 문구). 그 뒤에 `</Grid>`. 새 XAML 주석은 `<!-- Phase 78 NGA-02: 측정표 아래(Halcon 창과 겹치지 않는 WPF 영역) NG 원인 패널 -->` 한 줄만.

(4) ReviewerWindow.xaml.cs — 배선만(분기·반복문 추가 금지):
- `_allCycleItems` 필드 다음에 `private NgCauseHistory _ngCauseHistory = new NgCauseHistory();`(주석: 날짜 폴더 cycle.json 이력 — 추세 원인 규칙 입력).
- LoadCycleFolders: 폴더 없음 분기의 `_allCycleItems = new List<CycleListItem>();` 다음 줄에 `_ngCauseHistory = new NgCauseHistory();`. `var items = Directory.GetDirectories(...)` 문장 바로 앞 줄에 `NgCauseHistory history = new NgCauseHistory();`. 람다 안 `var dto = CycleResultSerializer.Load(...)` 다음 줄에 `history.AddCycle(dto);`. `_allCycleItems = items;` 다음 줄에 `_ngCauseHistory = history;`.
- DisplayCycle: `rows.Add(new ReviewMeasurementRow(shot, fai, m));` 를 `rows.Add(new ReviewMeasurementRow(shot, fai, m, cycle, _ngCauseHistory));` 로 바꾼다.
- MainView.xaml.cs 는 건드리지 않는다.

(5) probe: 'Probe 규격' 의 rows 모드로 `$H/NgCauseProbe.cs` 를 작성한다. 빌드 → probe 컴파일 → `MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN rows C:/Users/admin/AppData/Local/Temp/p78-probe/out D:/Data/Result/20260601 D:/Data/Result/20260811 D:/Data/Result/20260916`. 빌드가 실행 중인 Debug 앱의 exe 잠금으로 복사 단계만 실패하면 프로세스를 죽이지 말고 멈춰서 보고한다.

(6) 커밋: `git add WPF_Example/UI/ViewModel/CycleResultDto.cs WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs WPF_Example/UI/Reviewer/ReviewerWindow.xaml WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs` — 명시 경로만(stash·checkout --·restore 금지, csproj·.planning·STATE.md·ROADMAP.md 스테이징 금지). 메시지 예: feat(78-01): NG 원인 tracer — R6 치우침 규칙 cycle.json→리뷰어 패널.

CLAUDE.md 하드룰(새로 쓰는 모든 줄 — 위반 = 회귀): 삼항 연산자 금지(if/else), null 병합·null 조건 연산자 금지(명시적 null 비교), C# 8 switch 식 금지, C# 7.2 문법만, 한 줄 분기도 중괄호, 한 줄에 한 문장, 논리 연산자 3개 이상을 한 식에 잇지 않고 이름 있는 bool 로 뽑는다, if 중첩 3단계 이상이면 메서드 분리, 헝가리언 접두사 b/n/sz/d/hv, 매직넘버는 이름 있는 const, 날짜·이니셜 서명 주석 신규 금지(새 주석은 `// Phase 78 NGA-0x: 이유` 형식), 수정 파일의 기존 brace 스타일 유지. 새 줄(문자열·주석 포함)에 물음표 문자를 쓰지 않는다(삼항 grep 오탐 방지). 기존 줄의 위반은 복사하지도 고치지도 않는다. 새 .cs 파일 금지. D:\Data 는 읽기만. Release 빌드 금지(OutputPath D:\Data).
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p78-probe; FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug
PB=$(cat $H/base-78-01.txt)
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-78-01.log 2>&1; echo "msbuild_exit=$?"
echo "errors=$(grep -cE ': error [A-Z]+[0-9]+' $H/build-78-01.log)"
(cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config NgCauseProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:NgCauseProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:ClosedXML.dll -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll NgCauseProbe.cs && mkdir -p out && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN rows C:/Users/admin/AppData/Local/Temp/p78-probe/out D:/Data/Result/20260601 D:/Data/Result/20260811 D:/Data/Result/20260916) | tee $H/rows-78-01.txt
echo "named_code=$(grep -P '^20260916/164530363_cycle\t[^\t]*\t[^\t]*\tC13_P1\t' $H/out/20260916.tsv | cut -f7) named_panel=$(grep -P '^20260916/164530363_cycle\t[^\t]*\t[^\t]*\tC13_P1\t' $H/out/20260916.tsv | cut -f9 | grep -c '추정 원인: 보정값 또는 티칭이 한쪽으로 치우쳐 있습니다')"
DTO=WPF_Example/UI/ViewModel/CycleResultDto.cs; ROW=WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs; XA=WPF_Example/UI/Reviewer/ReviewerWindow.xaml; XC=WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs
echo "classes=$(grep -cE 'public class NgCauseResult|public class NgCauseHistory|public static class NgCauseAnalyzer|public class NgHistorySample' $DTO) ctor=$(grep -c 'MeasurementResultDto m, CycleResultDto cycle, NgCauseHistory history) : this(shot, fai, m)' $ROW) panelbind=$(grep -c 'SelectedItem.CausePanelText' $XA) header=$(grep -c 'NG 원인 분석 (추정)' $XA)"
echo "wire_add=$(grep -c 'history.AddCycle(dto);' $XC) wire_row=$(grep -c 'new ReviewMeasurementRow(shot, fai, m, cycle, _ngCauseHistory)' $XC) wire_field=$(grep -c 'private NgCauseHistory _ngCauseHistory' $XC)"
echo "pure=$(tr -d '\r' < $DTO | awk '/public static class NgCauseAnalyzer/,/^    }$/' | grep -cE 'SystemHandler|File\.|Directory\.|RecipeManager')"
XADD=$(git diff -w -U0 $PB -- $XC | grep -E '^\+' | grep -vE '^\+\+\+'); echo "branch_kw=$(printf '%s\n' "$XADD" | grep -cE '\b(if|switch|for|foreach|while)\b')"
for F in $DTO $ROW $XC; do ADD=$(git diff -w -U0 $PB -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "$F deleted=$(git diff -w -U0 $PB -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') logic3=$(printf '%s\n' "$ADD" | grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)')"; done
echo "commit_files=$(git diff --name-only $PB HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - 빌드: `msbuild_exit=0`, `errors=0` (Debug|x64 만)
    - probe 출력 `date=20260916` 줄: `ng=36`, `codes=` 에 `R6:36`, `exceptions=0`, `loadnull=0`
    - probe 출력 `date=20260601` 줄 `ng=140 exceptions=0`, `date=20260811` 줄 `ng=338 exceptions=0` (옛 JSON 무크래시 — NGA-05)
    - `named_code=R6`, `named_panel=1`
    - `classes=4 ctor=1 panelbind=1 header=1`, `wire_add=1 wire_row=1 wire_field=1`
    - `pure=0` (분석기에 파일 I/O·전역 싱글턴·레시피 참조 없음 — P-2, PR-2)
    - `branch_kw=0` (ReviewerWindow.xaml.cs 추가 줄 분기·반복 0 — 배선만)
    - 3개 .cs 모두 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 logic3=0`; `CycleResultDto.cs deleted=0`, `ReviewMeasurementRow.cs deleted=0`, `ReviewerWindow.xaml.cs deleted=1`
    - `commit_files` 가 위 4개 경로뿐(csproj·MainView.xaml.cs 미포함)
  </acceptance_criteria>
  <done>R6 규칙 한 경로가 cycle.json → 이력 → 규칙 엔진 → 행 VM → XAML 패널까지 연결되어 컴파일·커밋되었고, 20260916 실데이터 NG 36행이 전부 R6 으로, 옛 JSON 두 날짜가 예외 없이 분류됨을 probe 로 확인했다. 화면에 실제로 보이는지는 78-07 UAT U-1 에서 확인한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 사유 규칙 R1~R4 — 사진 없음·기준점 못 찾음·측정 선 못 찾음·설정 오류 문구와 CROSS_Z_INCOMPLETE 설명</name>
  <files>WPF_Example/UI/ViewModel/CycleResultDto.cs</files>
  <read_first>
    - WPF_Example/UI/ViewModel/CycleResultDto.cs (Task 1 결과 — NgCauseAnalyzer 전체)
    - WPF_Example/Custom/Sequence/Inspection/SkipReason.cs 전체 (사유 상수 의미 주석)
    - WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs (JudgeText 분기 — 같은 사유 순서)
    - .planning/phases/78-reviewer-ng-cause-analysis/78-CONTEXT.md 원인 규칙 후보 R1~R4
    - 이 PLAN 의 P-1, P-10 과 'Probe 규격'
  </read_first>
  <behavior>
    - NO_IMAGE → IsNg true, CauseCode R1
    - DATUM_FAIL / ALIGN_FAIL / DATUM_REF_MISSING → R2 (확인할 일 문구는 DATUM_REF_MISSING 만 다름)
    - MEASURE_FAIL → R3, 근거에 LastErrorMessage 앞 60자(없으면 '오류 내용 없음')
    - ZINDEX_MISCONFIGURED → IsNg true, R4 / CROSS_Z_INCOMPLETE → IsNg false, HasCause true, R4
    - 모르는 사유 'SOMETHING_NEW' → IsNg true, RX, 근거에 사유 문자열
    - Z_RANGE_PENDING → IsNg false, HasCause false / 결과 OK 행 → NotNg 문구 / 측정 null → Empty, 예외 없음
  </behavior>
  <action>
(1) NgCauseAnalyzer 에 사유 규칙 문구 const 를 추가한다(D-78-05, 원인 1줄 + 근거 1줄 + 확인할 일 1줄):
- R1: `R1_CAUSE_TEXT = "사진이 안 찍혔습니다"`, `R1_EVIDENCE_TEXT = "사진이 없어 측정하지 못함 (사유 NO_IMAGE)"`, `R1_ACTION_TEXT = "카메라 연결과 PLC 촬영 신호(z 번호)를 확인하세요"`.
- R2: `R2_CAUSE_TEXT = "기준점을 못 찾았습니다"`, `R2_EVIDENCE_DATUM_FAIL_TEXT = "기준선 검출 실패로 측정 안 함 (사유 DATUM_FAIL)"`, `R2_EVIDENCE_ALIGN_FAIL_TEXT = "기준 패턴 매칭 실패로 측정 안 함 (사유 ALIGN_FAIL)"`, `R2_EVIDENCE_REF_MISSING_TEXT = "측정이 가리키는 기준점 이름이 레시피에 없음 (사유 DATUM_REF_MISSING)"`, `R2_ACTION_TEXT = "자재가 제대로 놓였는지, 기준점 티칭과 조명을 확인하세요"`, `R2_ACTION_REF_MISSING_TEXT = "레시피에서 이 측정이 쓰는 기준점 이름을 확인하세요"`.
- R3: `R3_CAUSE_TEXT = "측정할 선(에지)을 못 찾았습니다"`, `R3_EVIDENCE_FORMAT = "측정 실패 · {0}"`, `R3_NO_ERROR_TEXT = "오류 내용 없음"`, `EVIDENCE_ERROR_MAX_CHARS = 60`, `R3_ACTION_TEXT = "사진에서 측정 위치(ROI)·조명·초점을 확인하세요"`.
- R4: `R4_CAUSE_TEXT = "설정 오류로 측정하지 못했습니다"`, `R4_EVIDENCE_ZINDEX_TEXT = "z 번호 설정이 맞지 않아 측정 안 함 (사유 ZINDEX_MISCONFIGURED)"`, `R4_EVIDENCE_CROSS_Z_TEXT = "두 z 사진이 모두 필요한 측정인데 PLC 자동 검사가 아니라 측정 안 함 (사유 CROSS_Z_INCOMPLETE)"`, `R4_ACTION_ZINDEX_TEXT = "PLC z 번호와 레시피 Z 설정을 확인하세요"`, `R4_ACTION_CROSS_Z_TEXT = "PLC 자동 검사로 다시 확인하세요 (수동 실행은 두 장짜리 측정을 못 합니다)"`.
(2) Task 1 의 사유 처리(모든 사유 → RX)를 `BuildReasonResult(MeasurementResultDto m)` 안의 if / else if 사슬로 바꾼다(전통 분기, SkipReason 상수 비교): NO_IMAGE → R1, DATUM_FAIL·ALIGN_FAIL·DATUM_REF_MISSING → R2(근거·확인할 일은 사유별), MEASURE_FAIL → R3(오류 문자열이 비어 있으면 R3_NO_ERROR_TEXT, EVIDENCE_ERROR_MAX_CHARS 보다 길면 앞부분만 Substring), ZINDEX_MISCONFIGURED → R4, 그 밖 → RX. 결과 객체는 IsNg true, HasCause true. 공통 조립은 `private static NgCauseResult BuildResult(bool bIsNg, string szCode, string szCause, string szEvidence, string szAction)` 헬퍼 하나로 한다.
(3) Analyze 의 NG 판정 앞에 CROSS_Z_INCOMPLETE 분기를 넣는다: 사유가 CROSS_Z_INCOMPLETE 면 BuildResult(false, CODE_R4, R4_CAUSE_TEXT, R4_EVIDENCE_CROSS_Z_TEXT, R4_ACTION_CROSS_Z_TEXT) 반환(P-1 — NG 아님, 설명은 보임). Z_RANGE_PENDING 은 기존대로 NotNg.
(4) probe 에 `synthetic` 모드를 추가한다(`NgCauseProbe.exe <binDir> synthetic`): 메모리에서 CycleResultDto 1개(InspectionTime 고정, CycleFolderPath "C:/synthetic/c1") + ShotResultDto/FaiResultDto 를 만들고 behavior 목록의 각 사례를 `NgCauseAnalyzer.Analyze(cycle, shot, fai, m, null)` 로 평가해 `case=<이름> expect=<기대> got=<IsNg>/<HasCause>/<CauseCode> PASS|FAIL` 을 한 줄씩, 마지막에 `synthetic_fail=<FAIL 수>` 를 찍는다. 사례 이름: `r1_no_image`, `r2_datum_fail`, `r2_align_fail`, `r2_ref_missing`, `r3_measure_fail`, `r3_long_error`(오류 200자 → 근거 길이 ≤ 60 + 접두), `r4_zindex`, `r4_cross_z_not_ng`, `rx_unknown`, `pending_not_ng`, `ok_not_ng`, `null_measurement`, `null_cycle`.
(5) 빌드 → probe 재컴파일 → rows(20260811, 20260915) + synthetic 실행. 커밋: `git add WPF_Example/UI/ViewModel/CycleResultDto.cs` 만. 메시지 예: feat(78-01): NG 원인 사유 규칙 R1~R4.

CLAUDE.md 하드룰(Task 1 과 동일 — 새 줄 전부): 삼항·null 병합·null 조건 연산자 금지, switch 식 금지, 중괄호 필수, 논리 연산자 3개 이상 금지(이름 있는 bool), 헝가리언, 매직넘버 const, 날짜·이니셜 서명 주석 금지, 물음표 문자 금지, 새 .cs 금지.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p78-probe; FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug
PB=$(cat $H/base-78-01.txt)
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-78-01b.log 2>&1; echo "msbuild_exit=$?"
(cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config NgCauseProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:NgCauseProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:ClosedXML.dll -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll NgCauseProbe.cs && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN rows C:/Users/admin/AppData/Local/Temp/p78-probe/out D:/Data/Result/20260811 D:/Data/Result/20260915 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN synthetic) | tee $H/rows-78-01b.txt
DTO=WPF_Example/UI/ViewModel/CycleResultDto.cs
echo "consts=$(grep -cE 'R1_CAUSE_TEXT =|R2_CAUSE_TEXT =|R3_CAUSE_TEXT =|R4_CAUSE_TEXT =|EVIDENCE_ERROR_MAX_CHARS = 60' $DTO) helper=$(grep -c 'private static NgCauseResult BuildResult(bool bIsNg' $DTO)"
ADD=$(git diff -w -U0 $PB -- $DTO | grep -E '^\+' | grep -vE '^\+\+\+'); echo "ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') logic3=$(printf '%s\n' "$ADD" | grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)') deleted=$(git diff -w -U0 $PB -- $DTO | grep -E '^-' | grep -vE '^---' | wc -l)"
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`
    - `date=20260811` 줄 `codes=` 에 `R2:338`, `RX:0`, `exceptions=0`
    - `date=20260915` 줄 `codes=` 에 `R3:33`, `R2:159`, `RX:0`, `exceptions=0`
    - synthetic 출력에 사례 13개 모두 `PASS`, `synthetic_fail=0`
    - `consts=5 helper=1`
    - CycleResultDto.cs `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 logic3=0 deleted=0`
    - `commit_files` = `WPF_Example/UI/ViewModel/CycleResultDto.cs`
  </acceptance_criteria>
  <done>저장된 사유만으로 판정되는 원인 4종(+모르는 사유)이 쉬운 한국어 3줄로 분류되고, CROSS_Z_INCOMPLETE 는 NG 에서 빠지되 설명은 보이며, 실데이터와 합성 사례로 확인되어 커밋되었다.</done>
</task>

<task type="auto">
  <name>Task 3: NGA-07 DTO 계약 — Z 후보 선명도·범위·기준점 진단 필드(옛 cycle.json 기본값 호환)</name>
  <files>WPF_Example/UI/ViewModel/CycleResultDto.cs</files>
  <read_first>
    - WPF_Example/UI/ViewModel/CycleResultDto.cs:9-168 (DTO 선언 관례 — 리스트는 이니셜라이저, 기본값 const)
    - .planning/phases/78-reviewer-ng-cause-analysis/78-RESEARCH.md 'Pattern 4: D-78-08 진단 값 기록'
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs:1028-1124 (DetectedOrigin*/Align2Score/AlignThetaDeg/DetectedEdgeCount/DetectedFitRMSE/DetectedAngleDeg 의미)
    - 이 PLAN 의 P-8, P-15 와 'Probe 규격'
  </read_first>
  <action>
이 task 는 필드 선언만 한다. 값을 채우는 코드는 78-03, 읽는 규칙은 78-02 가 만든다(interface-first).

(1) `ZRangeImageRecordDto` 클래스 다음에 `public class ZCandidateScoreDto` — XML 요약 "Z 범위 Shot 에서 이 측정의 후보 z 하나를 실제로 측정한 결과(Phase 78 NGA-07, D-78-08). 평가 순서대로 기록". 자동 프로퍼티 `int ZIndex`, `bool Ok`, `double Score`.
(2) 그 다음에 `public class DatumDiagnosticDto` — XML 요약 "tick 저장 시점의 기준점 진단 값(기록 전용, 판정 무관 — D-78-08). IsDetectedThisTick=false 면 이전 tick 에서 검출된 값". 자동 프로퍼티: `string DatumName`, `bool IsDetected`, `bool IsDetectedThisTick`, `DateTime DetectTime`, `double OriginRow`, `double OriginCol`, `double AngleDeg`, `int EdgeCount`, `double FitRmse`, `double AlignMatchScore`, `double AlignMatchRow`, `double AlignMatchCol`, `double AlignMatchAngleDeg`, `double Align2Score`, `double AlignThetaDeg`.
(3) `ShotResultDto` 의 `ResultImagePath` 프로퍼티 다음에 `public const int Z_RANGE_NONE = -1;` 와 `public int ZRangeStartIndex { get; set; } = Z_RANGE_NONE;`, `public int ZRangeEndIndex { get; set; } = Z_RANGE_NONE;` (XML 요약: 검사 당시 Shot 의 Z 범위, 범위 꺼짐·옛 cycle.json = -1, R8 판정 입력 — 현재 레시피가 아니라 이 값을 쓴다).
(4) `MeasurementResultDto` 의 `SelectedZIndex` 다음에 `public List<ZCandidateScoreDto> ZCandidateScores { get; set; } = new List<ZCandidateScoreDto>();` (XML 요약: 후보 z 별 선명도, 범위 미적용·옛 JSON = 빈 목록).
(5) `CycleResultDto` 의 `ZRangeImages` 다음에 `public List<DatumDiagnosticDto> DatumDiagnostics { get; set; } = new List<DatumDiagnosticDto>();` (XML 요약: 이 시퀀스 Datum 진단 값, 옛 JSON = 빈 목록).
(6) 기존 필드 이름·타입은 바꾸지 않는다. `[JsonProperty(Required ...)]` 류 어트리뷰트를 붙이지 않는다(옛 파일 로드 호환 — NGA-05).
(7) 빌드 → probe 재컴파일 → rows 모드로 20260601·20260811·20260916 실행(newfields 는 아직 기록 코드가 없으므로 0 이어야 한다). 커밋: `git add WPF_Example/UI/ViewModel/CycleResultDto.cs` 만. 메시지 예: feat(78-01): NGA-07 cycle.json 진단 필드 계약.

CLAUDE.md 하드룰(새 줄 전부): 삼항·null 병합·null 조건 연산자 금지, 중괄호 필수, 헝가리언, 매직넘버 const, 날짜·이니셜 서명 주석 금지, 물음표 문자 금지, 새 .cs 금지.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p78-probe; FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug
PB=$(cat $H/base-78-01.txt)
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-78-01c.log 2>&1; echo "msbuild_exit=$?"
(cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config NgCauseProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:NgCauseProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:ClosedXML.dll -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll NgCauseProbe.cs && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN rows C:/Users/admin/AppData/Local/Temp/p78-probe/out D:/Data/Result/20260601 D:/Data/Result/20260811 D:/Data/Result/20260916) | tee $H/rows-78-01c.txt
DTO=WPF_Example/UI/ViewModel/CycleResultDto.cs
echo "dto=$(grep -cE 'public class ZCandidateScoreDto|public class DatumDiagnosticDto|public int ZRangeStartIndex \{ get; set; \} = Z_RANGE_NONE;|public int ZRangeEndIndex \{ get; set; \} = Z_RANGE_NONE;|public List<ZCandidateScoreDto> ZCandidateScores \{ get; set; \} = new List<ZCandidateScoreDto>\(\);|public List<DatumDiagnosticDto> DatumDiagnostics \{ get; set; \} = new List<DatumDiagnosticDto>\(\);' $DTO) datumprops=$(tr -d '\r' < $DTO | awk '/public class DatumDiagnosticDto/,/^    }$/' | grep -c '{ get; set; }') required=$(grep -c 'Required' $DTO)"
ADD=$(git diff -w -U0 $PB -- $DTO | grep -E '^\+' | grep -vE '^\+\+\+'); echo "ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') deleted=$(git diff -w -U0 $PB -- $DTO | grep -E '^-' | grep -vE '^---' | wc -l)"
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`
    - rows 출력 3개 날짜 모두 `exceptions=0`, `newfields=0` (옛 JSON 로드 시 새 필드 = 기본값)
    - `dto=6 datumprops=15 required=0`
    - `ternary=0 coalesce=0 nullcond=0 datesig=0 deleted=0`
    - `commit_files` = `WPF_Example/UI/ViewModel/CycleResultDto.cs`
  </acceptance_criteria>
  <done>78-02(읽기)·78-03(쓰기)가 공유할 cycle.json 진단 필드 계약이 기본값과 함께 선언되고, 옛 cycle.json 이 기본값으로 로드됨을 확인해 커밋했다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 디스크의 cycle.json(과거·손상·수기 편집 가능) → 리뷰어 이력·규칙 엔진 | 외부에서 손댄 JSON 이 역직렬화·규칙 계산에 들어온다 |
| 규칙 엔진 결과 → 비전 초보 운영자 판단 | 추정이 사실처럼 보이면 잘못된 보정·티칭 변경으로 이어진다 |
| 리뷰어 UI 스레드 → 날짜 폴더 대량 파일(최대 1,112+ 사이클) | 규칙 계산이 클릭마다 디스크를 다시 읽으면 화면이 멈춘다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-01 | Tampering | CycleResultSerializer.Load(손상·악성 cycle.json) | medium | mitigate | 기존 TypeNameHandling.None + try/catch→null 경로를 그대로 사용(새 역직렬화 경로 없음). AddCycle/Analyze 는 null dto·null 하위 목록을 가드(synthetic `null_cycle`/`null_measurement` PASS) |
| T-78-02 | Denial of Service | NgCauseHistory / 행 생성 | medium | mitigate | 이력은 목록을 만들 때 이미 읽은 dto 로 1회 구성, Analyze 는 파일 I/O 0(`pure=0`), 정렬은 1회 지연 수행 |
| T-78-03 | Repudiation | 추정 원인 표시 | medium | mitigate | 패널 제목 'NG 원인 분석 (추정)', 첫 줄 '추정 원인: ', 근거 줄에 판단 숫자(평균·기준·허용) 동시 표시 — PR-1 |
| T-78-04 | Information Disclosure | probe 산출 TSV | low | accept | 저장소 밖 로컬 Temp, 측정명·수치만, 커밋·전송 없음 |
</threat_model>

<verification>
1. Debug|x64 빌드 에러 0 (Release 금지)
2. probe rows: 20260916 NG 36행 전부 R6, 164530363 C13_P1 패널 문구 일치 / 20260811 R2 338 / 20260915 R3 33·R2 159 / 옛 JSON 예외 0·newfields 0
3. probe synthetic 13 사례 PASS
4. 배선: history.AddCycle → 5인자 생성자 → SelectedItem.CausePanelText 바인딩, xaml.cs 추가 줄 분기 0
5. 추가 줄 하드룰 grep 6종 0, 분석기 순수성(pure=0)
</verification>

<success_criteria>
- 리뷰어 NG 행 선택 시 R6/R1~R4/R0/RX 원인 3줄이 패널에 바인딩으로 표시되는 경로가 컴파일·커밋됨
- 실데이터 20260916 에서 R6 판정, 옛 JSON 무크래시
- 78-02/03 이 쓸 DTO 계약 선언 완료
</success_criteria>

<output>
완료 후 `.planning/phases/78-reviewer-ng-cause-analysis/78-01-SUMMARY.md` 를 작성한다. 반드시 포함:
- base-78-01 해시, 커밋 해시 3개, 빌드 결과, 파일별 `git diff -w` 삭제 수
- probe rows 출력 줄 원문(날짜별), synthetic 출력 원문
- 새 심볼 목록('Artifacts' 표의 78-01 행과 대조)
- 계획 단계 결정 P-1~P-17 요약, flagged assumption A-78-E1/A1/A2/A3 그대로 옮겨 적기
</output>
