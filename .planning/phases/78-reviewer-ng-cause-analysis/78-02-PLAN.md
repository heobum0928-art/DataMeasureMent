---
phase: 78-reviewer-ng-cause-analysis
plan: 02
type: execute
wave: 2
depends_on: ["78-01"]
files_modified:
  - WPF_Example/UI/ViewModel/CycleResultDto.cs
autonomous: true
requirements: [NGA-01, NGA-05]
user_setup: []

must_haves:
  truths:
    - "20260916 실데이터: NG 36행 대표 원인 전부 R6(치우침), 164530363_cycle C13_P1 은 대표 R6 + 함께 의심 R8(초점 범위 끝 z5), 함께 의심 R8 20건 이상 (NGA-01, CONTEXT 성공 기준 3)"
    - "20260915 실데이터: 135946717_cycle C13_P3 대표 원인 R5(기준점 흔들림), R5 가 대표 또는 함께 의심으로 20건 이상, R8 이 대표 또는 함께 의심으로 400건 이상, 대표 R6 1,700건 이상 (NGA-01, 성공 기준 3)"
    - "여러 규칙이 동시에 걸리면 R5(강) → R9 → R6 → R8 → R7 → R5(약) 순서의 첫 항목이 대표, 나머지는 '함께 의심: ' 줄로 같은 순서로 보인다. 아무 규칙도 없으면 R0 (D-78-01, CONTEXT '가장 구체적인 것 1개')"
    - "[edge NGA-01 boundary] R9 는 넘은 양 ≤ 허용 폭×0.10 이면 발동, 초과면 미발동, 허용 폭 0 이면 미발동 / R6 은 표본 3개부터 발동·2개면 미발동 / R5 는 이동량이 0.15×허용 폭 미만이면 미발동, 최대/최소 비가 2.0 초과면 미발동"
    - "[edge NGA-01 adjacency] 공칭과 똑같은 값이 창에 하나라도 있으면 '전부 한쪽' 이 아니라 R6 미발동. 발끝→에지 방향이 반대인 측정은 이동량 부호를 뒤집어 같은 기준선 이동으로 묶는다"
    - "[edge NGA-01 ordering] 이력은 InspectionTime 오름차순 → 사이클 키 ordinal 순서로 정렬되어 같은 시각 두 사이클의 '직전 검사' 가 항상 같다. R8 근거의 후보 선명도는 기록 순서와 무관하게 z 오름차순으로 보인다"
    - "[edge NGA-01 precision] 모든 판정은 double 원값 비교(반올림·epsilon 없음)이고 근거 문자열의 F4/F1/F3/F2 는 표시 전용이다 — 값 2.10004(상한 2.1)는 R9 로 판정되고 근거에는 2.1000 으로 보인다"
    - "[edge NGA-01 empty] 이력 null·이력 1개·묶음 1개·오버레이 없음·DatumDiagnostics 없음이면 해당 규칙만 건너뛰고(R5 근거는 '기준점 기록 없음') 예외가 없다"
    - "[edge NGA-05 concurrency] 검사가 계속 쓰고 있는 20260917 폴더를 probe 로 읽어도 예외 0 — 쓰는 도중인 cycle.json 은 Load 가 null 을 돌려 이력·분석에서 빠진다"
    - "옛 cycle.json(20260601·20260811)에서는 R8·R5 가 한 번도 나오지 않는다(범위 기록·오버레이 없음 → 데이터 없음으로 건너뜀), 20260601 에서 R7 이 1건 이상 나온다 (NGA-05)"
  artifacts:
    - path: "WPF_Example/UI/ViewModel/CycleResultDto.cs"
      provides: "R5/R7/R8/R9 규칙, 발동 목록 → 대표/함께 의심 조립"
      contains: "R5_MIN_SHIFT_TOL_RATIO = 0.15"
  key_links:
    - from: "NgCauseAnalyzer.Analyze (값 이탈)"
      to: "R5 → R9 → R6 → R8 → R7 → R5(약) 발동 목록"
      via: "BuildOutOfToleranceResult 가 규칙별 private 평가 메서드 결과를 고정 순서로 목록에 넣는다"
      pattern: "private static NgCauseResult BuildOutOfToleranceResult\\("
    - from: "R8 범위 끝"
      to: "ShotResultDto.ZRangeEndIndex → (없으면) CycleResultDto.ZIndex"
      via: "ResolveZRangeEnd — 현재 레시피 조회 없음"
      pattern: "private static int ResolveZRangeEnd\\("
  prohibitions:
    - statement: "MUST NOT 과거 cycle.json 을 현재 레시피 설정으로 판정하지 않는다 — R8 의 Z 범위는 cycle.json 에 검사 당시 기록된 값만 쓰고, 없으면 '데이터 없음' 으로 건너뛴다 (OQ2)"
      status: resolved
      verification: judgment
---

<objective>
78-01 뼈대 위에 나머지 값 기반 규칙 4개(R5 기준점 흔들림, R7 한 곳만, R8 초점 범위 끝, R9 경계 흔들림)와 "대표 1개 + 함께 의심" 조립을 더한다. 실데이터 20260915/20260916 에서 R5·R6·R8 이 판정되는 것(CONTEXT 성공 기준 3)과 옛 데이터 호환을 probe 로 확인한다.

Purpose: D-78-01 "NG 가 왜 났는지" 의 핵심 가치. 09-15 z1 기준점 사진 흔들림, 09-16 보정/티칭 치우침·z5 끝값 사례가 실제로 잡혀야 운영자가 쓸 수 있다.

Output: CycleResultDto.cs 의 NgCauseAnalyzer 확장(삽입 위주), probe synthetic 사례 확장.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-CONTEXT.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-01-PLAN.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-01-SUMMARY.md

<interfaces>
78-01 이 만든 계약(이 plan 은 이것만 쓴다):
- `NgCauseResult` (IsNg, HasCause, CauseCode, CauseText, EvidenceText, ActionText, SuspectCodes, SuspectText), `BuildResult(bool, string, string, string, string)` 헬퍼
- `NgCauseHistory.GetSamples(string key)` → InspectionTime·CycleKey 정렬된 `List<NgHistorySample>`, `ResolveCycleKey(dto)`, `BuildMeasurementKey(shot, fai, meas)`
- `NgCauseAnalyzer.Analyze(cycle, shot, fai, m, history)`, `ResolveToleranceBand(m, out lower, out upper)`, R6 평가(TryEvaluateR6), `BuildOutOfToleranceResult` 가 현재 R6 아니면 R0
- DTO: `ShotResultDto.ZRangeStartIndex/ZRangeEndIndex`(-1), `MeasurementResultDto.ZCandidateScores`(ZIndex/Ok/Score), `MeasurementResultDto.SelectedZIndex`(-1), `CycleResultDto.IsProtocolDriven/ZIndex/DatumDiagnostics`, `FaiResultDto.LastOverlays`(EdgeInspectionOverlay: RoiId, MeasurementName, Points = List<EdgeInspectionPoint>{Row, Column})
- `MeasurementBase.FormatSelectedZ(int)` — "z5" 표기 단일 소스
- 실데이터 구조(계획 단계 확인): EdgeToLineDistance 의 RoiId `FAI-DistLine` 오버레이 Points[0] = 기준선 발끝, Points[1] = 에지 점 (D:/Data/Result/20260916/164530363_cycle/cycle.json)
</interfaces>
</context>

## 이 plan 의 규칙 규격 (78-01 P-4~P-10 상세)

공통: band = ResolveToleranceBand. 넘은 쪽: 값 > upper 면 위쪽(허용 폭 = |Tol+|, 넘은 양 = 값 − upper), 아니면 아래쪽(허용 폭 = |Tol-|, 넘은 양 = lower − 값). 측정 키 = BuildMeasurementKey(shot.ShotName, fai.FAIName, m.MeasurementName), 사이클 키 = ResolveCycleKey(cycle), idx = 그 키 표본 목록에서 CycleKey 가 같은 위치(없으면 이력 기반 규칙 R5·R6·R9 건너뜀).

| 규칙 | 발동 조건 | 원인 / 근거 템플릿 / 확인할 일 |
|---|---|---|
| R9 | 허용 폭 > 0, 넘은 양 ≤ 허용 폭 × `R9_BOUNDARY_RATIO`(0.10), idx > 0 이고 표본[idx−1].IsOk | `공차 경계에서 값이 흔들립니다` / `측정 {0} · 허용 {1} ~ {2} · 경계를 {3} 넘음 (허용 폭의 {4}% 이내) · 직전 검사는 OK` ({4} = 0.10 × `PERCENT_SCALE`(100.0) 을 `F0`) / `같은 자재로 반복 검사를 해서 값이 흔들리는지 확인하세요` |
| R8 | 범위 끝 = `ResolveZRangeEnd(cycle, shot, m)`: shot.ZRangeEndIndex ≥ 1 이면 그것, 아니면 (cycle.IsProtocolDriven, cycle.ZIndex ≥ 1, m.SelectedZIndex ≥ 1 을 각각 이름 있는 bool 로) 셋 다 참이면 cycle.ZIndex, 아니면 -1. 범위 끝 ≥ 1 이고 m.SelectedZIndex == 범위 끝 | `초점 범위가 부족할 수 있습니다` / `가장 선명한 사진이 범위 끝 {0} 에서 나옴 {1} · {2}` ({0} = FormatSelectedZ(범위 끝); {1} = 기록 범위면 `(범위 {시작}~{끝})`(각각 FormatSelectedZ, 시작이 1 미만이면 `(범위 끝 {끝})`), 대체면 `(범위 끝 = 검사 당시 z 번호)`; {2} = 후보가 있으면 `선명도 ` + z 오름차순(같은 z 는 기록 순서 유지) `z3=28.3 z4=실패 z5=32.5`(점수 `F1`, 실패 = `실패`), 없으면 `후보별 선명도 기록 없음(이 기능 이전 데이터)`) / `PLC 에 Z 범위를 끝 쪽으로 더 넓혀 달라고 요청하세요` |
| R7 | 사이클 전체(모든 Shot/FAI/측정, null 가드)에서 IsNgMeasurement 인 측정 수 == `R7_LONE_NG_COUNT`(1), 그리고 R5(강)·R6·R9 미발동 | `이 위치만 벗어났습니다 (실제 불량이나 이물 가능성)` / `이번 검사 NG 는 이 측정 1개뿐 · 측정 {0} · 허용 {1} ~ {2}` / `사진에서 이 위치를 보고 실물(이물·찍힘)을 확인하세요` |
| R5 | 아래 'R5 묶음 계산'. 강 = |이동 중앙값| ≥ 이 측정 허용 폭(min(|Tol+|,|Tol-|)) 또는 (값 − 이 측정 편차)가 [lower, upper] 안 | `기준점 위치가 흔들렸습니다` / `같은 기준선을 쓰는 측정 {0}개가 이웃 검사보다 같은 방향으로 약 {1}mm 움직임 · {2}` ({1} = |중앙값| F4; {2} = 기준점 설명) / `자재 안착과 기준점 촬영(촬영 전 대기시간)이 흔들리지 않는지 확인하세요` |

**R5 묶음 계산:** 같은 cycle·같은 fai 의 측정 g 중 (g.LastHasResult) 이고 (g.TypeName == `R5_SUPPORTED_TYPE_NAME` "EdgeToLineDistance") 이고 fai.LastOverlays 에 RoiId == `DIST_LINE_ROI_ID` "FAI-DistLine", MeasurementName == g.MeasurementName, Points 2개 이상, 발끝(Points[0])→에지(Points[1]) 길이 > 0 인 오버레이가 있고, g 의 이력 표본에서 자기 위치 앞 `R5_NEIGHBORS_EACH_SIDE`(2)개 + 뒤 2개(범위 밖은 잘림, 자기 제외) 가 `R5_MIN_NEIGHBORS`(2)개 이상인 것만 묶음에 넣는다(각 조건은 이름 있는 bool 또는 continue 가드). 편차 = g 값 − 이웃 값 중앙값(`Median`: 복사 정렬, 홀수 = 가운데, 짝수 = 가운데 두 값 평균). 기준 축 = 묶음 첫 측정(fai.Measurements 순서)의 단위벡터(에지−발끝)/길이. 이동량 = 단위벡터·기준축 내적이 0 이상이면 −편차, 아니면 +편차. 발동 = 현재 측정 m 이 묶음에 있음 + 묶음 수 ≥ `R5_MIN_SHARED_COUNT`(2) + 이동량 전부 > 0 또는 전부 < 0 + |이동량 중앙값| ≥ `R5_MIN_SHIFT_TOL_RATIO`(0.15) × 묶음 최소 허용 폭 + 최대 |이동량| ≤ `R5_SIMILAR_FACTOR`(2.0) × 최소 |이동량|.

**R5 기준점 설명:** cycle.DatumDiagnostics 가 null 이거나 비었으면 `기준점 기록 없음(이 기능 이전 데이터)`. IsDetected 인 기록이 없으면 `기준점 검출 기록 없음`. 있으면 DetectTime 이 가장 늦은 기록(같으면 목록 앞쪽)으로 `기준점 {DatumName} 각도 {AngleDeg F3}°` + AlignMatchScore > 0 이면 ` · 패턴 점수 {F2}`.

**라벨(함께 의심 줄):** R5 `기준점 흔들림`, R6 `보정값/티칭 치우침`, R7 `한 곳만 벗어남`, R8 `초점 범위 부족`, R9 `공차 경계 흔들림`, 구분자 `, `.

(아래 두 시그니처는 verify 가 존재를 확인하는 양성 grep 대상이다 — 부재 검사 아님)
<!-- planner-discipline-allow: private static int ResolveZRangeEnd( -->
<!-- planner-discipline-allow: private static double Median(List<double> lstValues) -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 발동 목록 조립 + R9 경계 흔들림 + R8 초점 범위 끝 + R7 한 곳만</name>
  <files>WPF_Example/UI/ViewModel/CycleResultDto.cs</files>
  <read_first>
    - WPF_Example/UI/ViewModel/CycleResultDto.cs (78-01 결과 — NgCauseAnalyzer 전체, NgCauseHistory, DTO 필드)
    - .planning/phases/78-reviewer-ng-cause-analysis/78-01-SUMMARY.md (실제 심볼 이름·probe 명령)
    - .planning/phases/78-reviewer-ng-cause-analysis/78-01-PLAN.md 'Probe 규격', 결정 P-4·P-6·P-8·P-9·P-10
    - 이 PLAN 의 '이 plan 의 규칙 규격' 표
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs:118-128 (SELECTED_Z_NONE, FormatSelectedZ)
  </read_first>
  <behavior>
    probe synthetic 사례(이름 = 기대). 별도 표시 없으면 공칭 2.0, Tol ±0.1, 같은 사이클에 MEASURE_FAIL 측정 1개를 더 둬서 NG 2개(R7 억제):
    - r9_inside: 직전 2.05 OK, 현재 2.105 → R9
    - r9_outside: 직전 2.05 OK, 현재 2.115 → R0
    - r9_prev_ng: 직전 2.15 NG, 현재 2.105 → R0
    - r9_zero_width: Tol+ 0, Tol- 0.1, 직전 1.95 OK, 현재 2.001 → R0
    - r6_min3: 2.3, 2.3, 2.3(현재) → R6 / r6_two_only: 2.3, 2.3(현재) → R0 / r6_on_nominal: 2.0, 2.3, 2.3(현재) → R0 / r6_fill_forward: 2.3(현재), 2.3, 2.3(뒤 사이클) → R6
    - (r8_*, r7_lone 은 값 2.3(NG)·이력 없음이 기본) r8_recorded: 범위 3~5, 자동 tick ZIndex 5, 선택 5, 후보 기록 순서 [z5=32.5 성공, z3=28.3 성공, z4 실패] → R8, 근거에 '(범위 z3~z5)' 와 '선명도 z3=28.3 z4=실패 z5=32.5'
    - r8_not_end: 범위 3~5, 선택 4 → R0 / r8_fallback_tick: 범위 없음, 자동 tick ZIndex 5, 선택 5 → R8, 근거에 '(범위 끝 = 검사 당시 z 번호)' 와 '후보별 선명도 기록 없음' / r8_manual_skip: 범위 없음, 수동(ZIndex -1), 선택 5 → R0 / r8_recorded_wins: 범위 끝 6 기록, tick ZIndex 5, 선택 5 → R0
    - r7_lone: 사이클 NG 1개, 이력 없음 → R7 / r7_suppressed_by_r6: NG 1개 + r6_min3 이력 → R6
    - order_r6_r8: r6_min3 이력 + r8_recorded → 대표 R6, 함께 의심 R8, 패널에 '함께 의심: 초점 범위 부족' / order_r9_r8: r9_inside + 범위 끝 선택 → 대표 R9, 함께 의심 R8
    - order_same_time: 이력 두 사이클이 같은 InspectionTime(키 'C:/s/b' 2.05 OK, 'C:/s/a' 1.85 NG), 현재 'C:/s/c' 더 늦은 시각 2.105 → 직전 = 'C:/s/b'(ordinal 뒤) → R9 (정렬이 틀려 직전이 'C:/s/a' 면 R0)
    - precision_display: 직전 2.05 OK, 현재 2.10004 → R9, 근거에 '측정 2.1000'
    - history_null: 현재 2.3, 이력 null → R0
  </behavior>
  <action>
(0) `git rev-parse HEAD > $H/base-78-02.txt` (H = /c/Users/admin/AppData/Local/Temp/p78-probe).

(1) NgCauseAnalyzer 에 const 를 추가한다(값·문구는 '이 plan 의 규칙 규격' 표 그대로): `R9_BOUNDARY_RATIO = 0.10`, `PERCENT_SCALE = 100.0`, `PERCENT_FORMAT = "F0"`, `R9_CAUSE_TEXT`, `R9_EVIDENCE_FORMAT`, `R9_ACTION_TEXT`, `R8_CAUSE_TEXT`, `R8_EVIDENCE_FORMAT`, `R8_RANGE_RECORDED_FORMAT = "(범위 {0}~{1})"`, `R8_RANGE_END_ONLY_FORMAT = "(범위 끝 {0})"`, `R8_RANGE_FALLBACK_TEXT = "(범위 끝 = 검사 당시 z 번호)"`, `R8_SCORES_PREFIX = "선명도 "`, `R8_SCORE_ITEM_FORMAT = "{0}={1}"`, `R8_SCORE_FAILED_TEXT = "실패"`, `R8_SCORE_FORMAT = "F1"`, `R8_SCORE_ITEM_SEPARATOR = " "`, `R8_NO_SCORES_TEXT = "후보별 선명도 기록 없음(이 기능 이전 데이터)"`, `R8_ACTION_TEXT`, `MIN_VALID_Z_INDEX = 1`, `R7_LONE_NG_COUNT = 1`, `R7_CAUSE_TEXT`, `R7_EVIDENCE_FORMAT`, `R7_ACTION_TEXT`, 라벨 `LABEL_R5`~`LABEL_R9`, `SUSPECT_SEPARATOR = ", "`.
(2) 규칙 결과 보관용 `private class RuleHit`(필드 `public string Code; public string CauseText; public string EvidenceText; public string ActionText;`)를 NgCauseAnalyzer 안에 둔다.
(3) `BuildOutOfToleranceResult` 를 다음 구조로 바꾼다(78-01 의 R6/R0 동작은 그대로 유지): band·키·표본·idx 를 한 번 계산 → `RuleHit r9 = TryEvaluateR9(...)`, `RuleHit r6 = (78-01 R6 평가를 RuleHit 반환형으로 감싼 것)`, `RuleHit r8 = TryEvaluateR8(cycle, shot, m)`, `RuleHit r7 = TryEvaluateR7(cycle, m, band, 강한 R5 발동 여부, r6 != null, r9 != null)` → `List<RuleHit> lstFired` 에 R5 자리(Task 2 가 채움 — 이 task 에서는 비워 둠) → r9 → r6 → r8 → r7 순서로 null 이 아닌 것만 추가 → 비었으면 R0 결과, 아니면 첫 항목을 대표로 BuildResult(true, …) 하고 나머지 코드를 SuspectCodes 에, 라벨을 SUSPECT_SEPARATOR 로 이어 SuspectText 에 넣는다. 라벨 조회는 `private static string GetRuleLabel(string szCode)` — 전통 switch 문(case 마다 break 또는 return) 또는 if/else.
(4) `private static int ResolveZRangeEnd(CycleResultDto cycle, ShotResultDto shot, MeasurementResultDto m, out bool bRecorded)` — 규격 표 R8 행 그대로. 이 메서드와 R8 평가는 cycle.json DTO 값만 읽는다(현재 레시피·설정 객체 참조 금지 — PR-4). 후보 점수 문자열은 `private static string BuildCandidateScoreText(List<ZCandidateScoreDto> lstScores)` — null/빈 목록이면 R8_NO_SCORES_TEXT, 아니면 (원래 인덱스, 항목) 쌍 목록을 만들어 ZIndex 오름차순 → 원래 인덱스 오름차순 비교 메서드로 정렬(List.Sort 불안정성 회피)한 뒤 조립. z 표기는 MeasurementBase.FormatSelectedZ.
(5) R9·R7 평가 메서드는 규격 표 그대로. 사이클 NG 수는 `private static int CountNgMeasurements(CycleResultDto cycle)`(null 가드, IsNgMeasurement 재사용).
(6) probe synthetic 모드에 behavior 사례를 추가한다. 사례마다 메모리에서 CycleResultDto(InspectionTime·CycleFolderPath 지정)들을 만들어 새 NgCauseHistory 에 AddCycle 하고 현재 사이클 측정을 Analyze 한다. 출력 `case=<이름> expect=<대표>[+<함께 의심>] got=<대표>[+<함께 의심>] PASS|FAIL`, 문구 포함 조건이 있는 사례는 got 뒤에 `text=ok|missing`. 78-01 사례도 계속 실행하고 마지막 `synthetic_fail=<n>`.
(7) 빌드 → probe 재컴파일 → synthetic 실행. 커밋: `git add WPF_Example/UI/ViewModel/CycleResultDto.cs` 만. 메시지 예: feat(78-02): NG 원인 R9·R8·R7 규칙과 대표/함께 의심 조립.

CLAUDE.md 하드룰(새 줄 전부 — 위반 = 회귀): 삼항 연산자 금지(if/else), null 병합·null 조건 연산자 금지(명시적 null 비교), C# 8 switch 식 금지(전통 switch + break), C# 7.2 문법만, 한 줄 분기도 중괄호, 한 줄에 한 문장, 논리 연산자 3개 이상을 한 식에 잇지 않고 이름 있는 bool, if 중첩 3단계 이상이면 메서드 분리, 헝가리언 b/n/sz/d/hv, 매직넘버는 이름 있는 const, 날짜·이니셜 서명 주석 금지(`// Phase 78 NGA-01: 이유`), 새 줄에 물음표 문자 금지, 람다 대신 이름 있는 비교 메서드, 새 .cs 파일 금지. D:\Data 읽기만. Release 빌드 금지.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p78-probe; FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug
PB=$(cat $H/base-78-02.txt)
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-78-02a.log 2>&1; echo "msbuild_exit=$?"
(cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config NgCauseProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:NgCauseProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:ClosedXML.dll -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll NgCauseProbe.cs && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN synthetic) | tee $H/synthetic-78-02a.txt
echo "cases=$(grep -cE '^case=(r9_inside|r9_outside|r9_prev_ng|r9_zero_width|r6_min3|r6_two_only|r6_on_nominal|r6_fill_forward|r8_recorded|r8_not_end|r8_fallback_tick|r8_manual_skip|r8_recorded_wins|r7_lone|r7_suppressed_by_r6|order_r6_r8|order_r9_r8|order_same_time|precision_display|history_null) .*PASS' $H/synthetic-78-02a.txt) textmissing=$(grep -c 'text=missing' $H/synthetic-78-02a.txt)"
DTO=WPF_Example/UI/ViewModel/CycleResultDto.cs
echo "consts=$(grep -cE 'R9_BOUNDARY_RATIO = 0.10;|R7_LONE_NG_COUNT = 1;|R8_NO_SCORES_TEXT = ' $DTO) resolver=$(grep -c 'private static int ResolveZRangeEnd(' $DTO) recipe=$(tr -d '\r' < $DTO | awk '/public static class NgCauseAnalyzer/,/^    }$/' | grep -cE 'SystemHandler|RecipeManager|ShotConfig|SystemSetting|File\.|Directory\.')"
ADD=$(git diff -w -U0 $PB -- $DTO | grep -E '^\+' | grep -vE '^\+\+\+'); echo "ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') logic3=$(printf '%s\n' "$ADD" | grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)') lambda=$(printf '%s\n' "$ADD" | grep -cF '=>')"
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`
    - `cases=20`, `textmissing=0`, synthetic 마지막 줄 `synthetic_fail=0`(78-01 사례 포함)
    - `consts=3 resolver=1 recipe=0` (분석기에 현재 레시피·설정·파일 참조 없음 — PR-4)
    - CycleResultDto.cs 추가 줄 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 logic3=0 lambda=0`
    - `commit_files` = `WPF_Example/UI/ViewModel/CycleResultDto.cs`
  </acceptance_criteria>
  <done>R9·R8·R7 이 규격대로 판정되고 여러 규칙이 걸리면 고정 순서로 대표/함께 의심이 조립되며, 경계·같은 시각·표시 반올림·이력 없음 사례가 합성 probe 로 확인되어 커밋되었다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: R5 기준점 흔들림(같은 기준선 측정 묶음 + 기준점 진단 근거) + 실데이터 성공 기준 3 확인</name>
  <files>WPF_Example/UI/ViewModel/CycleResultDto.cs</files>
  <read_first>
    - WPF_Example/UI/ViewModel/CycleResultDto.cs (Task 1 결과)
    - 이 PLAN 의 'R5 묶음 계산', 'R5 기준점 설명' 절
    - WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs:40-70 (RoiId, MeasurementName, Points)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs:350-370 (FAI-DistLine Points 순서: 발끝, 에지)
    - C:/Users/admin/.claude/projects/C--code-DataMeasurement/memory/side_z1_datum_image_shift.md (09-15 z1 사진 흔들림 사례)
  </read_first>
  <behavior>
    공칭 2.0, Tol ±0.03, 측정 A·B 둘 다 EdgeToLineDistance, 같은 FAI, 오버레이 발끝 (100,100) → 에지 (0,100)(단위벡터 (-1,0)), 이웃 = 앞 2 + 뒤 2 사이클:
    - r5_strong: 이웃 A·B 전부 2.0, 현재 A 2.035·B 2.035 → R5
    - r5_weak: 이웃 전부 2.1, 현재 A 2.12·B 2.115 → 대표 R6, 함께 의심 R5
    - r5_not_similar: 이웃 2.0, 현재 A 2.035·B 2.08 → R0 (비율 2.0 초과로 R5 없음)
    - r5_opposite_sign: 이웃 2.0, 현재 A 2.035·B 1.965 (같은 방향 오버레이) → R0
    - r5_opposite_geometry: 이웃 2.0, 현재 A 2.035, B 1.965 이고 B 오버레이 발끝 (100,100) → 에지 (200,100) → R5
    - r5_single: 이웃 2.0, 현재 A·B 2.035, B 오버레이 없음 → R0 / r5_other_type: 같은 값, B TypeName EdgeToLineAngle → R0
    - r5_below_ratio: 이웃 2.1, 현재 A·B 2.104 → R6 (R5 없음) / r5_above_ratio: 이웃 2.1, 현재 A·B 2.105 → 대표 R6, 함께 의심 R5
    - 모든 사례의 평가 대상은 측정 A, A·B 모두 결과 있음(LastHasResult true, LastJudgement 는 허용 범위로 계산해 넣음)
    - r5_datum_enrich: r5_strong + DatumDiagnostics [DatumName D1, IsDetected, AngleDeg 0.123, AlignMatchScore 0.98] → 근거에 '기준점 D1 각도 0.123°' 와 '패턴 점수 0.98'
    - r5_datum_none: r5_strong + DatumDiagnostics 빈 목록 → 근거에 '기준점 기록 없음'
  </behavior>
  <action>
(1) NgCauseAnalyzer 에 const 추가: `R5_NEIGHBORS_EACH_SIDE = 2`, `R5_MIN_NEIGHBORS = 2`, `R5_MIN_SHARED_COUNT = 2`, `R5_MIN_SHIFT_TOL_RATIO = 0.15`, `R5_SIMILAR_FACTOR = 2.0`, `R5_SUPPORTED_TYPE_NAME = "EdgeToLineDistance"`, `DIST_LINE_ROI_ID = "FAI-DistLine"`, `DIST_LINE_FOOT_INDEX = 0`, `DIST_LINE_EDGE_INDEX = 1`, `DIST_LINE_MIN_POINTS = 2`, `R5_CAUSE_TEXT`, `R5_EVIDENCE_FORMAT`, `R5_ACTION_TEXT`, `R5_DATUM_FORMAT = "기준점 {0} 각도 {1}°"`, `R5_DATUM_SCORE_FORMAT = " · 패턴 점수 {0}"`, `ANGLE_FORMAT = "F3"`, `MATCH_SCORE_FORMAT = "F2"`, `R5_NO_DATUM_RECORD_TEXT = "기준점 기록 없음(이 기능 이전 데이터)"`, `R5_NO_DATUM_DETECTED_TEXT = "기준점 검출 기록 없음"`.
(2) 묶음 멤버 보관용 `private class R5Member`(필드 `public MeasurementResultDto Measurement; public double Deviation; public double UnitRow; public double UnitCol; public double HalfWidth;`).
(3) 메서드(각각 가드 절, 중첩 3단계 미만):
- `private static double Median(List<double> lstValues)` — 복사 후 Sort, 홀수·짝수 규칙.
- `private static bool TryFindDistLineUnit(FaiResultDto fai, string szMeasName, out double dUnitRow, out double dUnitCol)` — 규격대로, 길이는 Math.Sqrt.
- `private static List<double> CollectNeighborValues(List<NgHistorySample> lstSamples, string szCycleKey)` — 자기 위치 못 찾으면 빈 목록.
- `private static List<R5Member> BuildR5Group(CycleResultDto cycle, ShotResultDto shot, FaiResultDto fai, NgCauseHistory history)` — fai.Measurements 순서대로 조건 통과 멤버.
- `private static RuleHit TryEvaluateR5(CycleResultDto cycle, ShotResultDto shot, FaiResultDto fai, MeasurementResultDto m, double dLower, double dUpper, NgCauseHistory history, out bool bStrong)` — history null 이면 null. 규격의 발동 조건·강 조건. 근거 {0} = 묶음 수, {1} = |중앙값| F4, {2} = BuildDatumEvidenceText(cycle).
- `private static string BuildDatumEvidenceText(CycleResultDto cycle)` — 'R5 기준점 설명' 규격.
(4) BuildOutOfToleranceResult 에서 R5 를 가장 먼저 평가하고, 강이면 lstFired 맨 앞, 약이면 맨 뒤(R7 다음)에 넣는다. R7 평가에 강 여부를 넘긴다.
(5) probe synthetic 에 behavior 사례 11개를 추가한다(출력 형식 Task 1 과 같음).
(6) 빌드 → probe 재컴파일 → synthetic + rows 5개 날짜(`D:/Data/Result/20260601 20260811 20260915 20260916 20260917`) 실행. rows 는 Task 1 까지의 코드로도 돌 수 있지만 이 task 커밋 전 결과만 판정에 쓴다.
(7) 커밋: `git add WPF_Example/UI/ViewModel/CycleResultDto.cs` 만. 메시지 예: feat(78-02): NG 원인 R5 기준점 흔들림 규칙.

CLAUDE.md 하드룰(Task 1 과 동일). 루프 안 복잡한 조건은 continue 가드 + 이름 있는 bool 로 쪼갠다.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p78-probe; FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug
PB=$(cat $H/base-78-02.txt)
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-78-02b.log 2>&1; echo "msbuild_exit=$?"
(cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config NgCauseProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:NgCauseProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:ClosedXML.dll -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll NgCauseProbe.cs && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN synthetic > synthetic-78-02b.txt && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN rows C:/Users/admin/AppData/Local/Temp/p78-probe/out D:/Data/Result/20260601 D:/Data/Result/20260811 D:/Data/Result/20260915 D:/Data/Result/20260916 D:/Data/Result/20260917) | tee $H/rows-78-02b.txt
echo "synthetic_fail_line=$(grep '^synthetic_fail=' $H/synthetic-78-02b.txt) r5cases=$(grep -cE '^case=r5_.*PASS' $H/synthetic-78-02b.txt) textmissing=$(grep -c 'text=missing' $H/synthetic-78-02b.txt)"
T15=$H/out/20260915.tsv; T16=$H/out/20260916.tsv; T01=$H/out/20260601.tsv; T11=$H/out/20260811.tsv
echo "n16_c13p1=$(grep -P '^20260916/164530363_cycle\t[^\t]*\t[^\t]*\tC13_P1\t' $T16 | cut -f7,8 | tr '\t' '|') n15_c13p3=$(grep -P '^20260915/135946717_cycle\t[^\t]*\t[^\t]*\tC13_P3\t' $T15 | cut -f7)"
echo "d16_ng=$(awk -F'\t' '$6==1' $T16 | wc -l) d16_r6=$(awk -F'\t' '$6==1 && $7=="R6"' $T16 | wc -l) d16_r8any=$(awk -F'\t' '$6==1 && ($7=="R8" || $8 ~ /R8/)' $T16 | wc -l)"
echo "d15_r5any=$(awk -F'\t' '$6==1 && ($7=="R5" || $8 ~ /R5/)' $T15 | wc -l) d15_r8any=$(awk -F'\t' '$6==1 && ($7=="R8" || $8 ~ /R8/)' $T15 | wc -l) d15_r6=$(awk -F'\t' '$6==1 && $7=="R6"' $T15 | wc -l)"
echo "old_r8r5=$(cat $T01 $T11 | awk -F'\t' '$6==1 && ($7 ~ /R8|R5/ || $8 ~ /R8|R5/)' | wc -l) d01_r7=$(awk -F'\t' '$6==1 && $7=="R7"' $T01 | wc -l) exceptions_nonzero=$(grep -c 'exceptions=[1-9]' $H/rows-78-02b.txt)"
DTO=WPF_Example/UI/ViewModel/CycleResultDto.cs
echo "consts=$(grep -cE 'R5_MIN_SHIFT_TOL_RATIO = 0.15;|R5_SIMILAR_FACTOR = 2.0;|R5_SUPPORTED_TYPE_NAME = \"EdgeToLineDistance\";|DIST_LINE_ROI_ID = \"FAI-DistLine\";' $DTO) median=$(grep -c 'private static double Median(List<double> lstValues)' $DTO)"
ADD=$(git diff -w -U0 $PB -- $DTO | grep -E '^\+' | grep -vE '^\+\+\+'); echo "ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') logic3=$(printf '%s\n' "$ADD" | grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)') lambda=$(printf '%s\n' "$ADD" | grep -cF '=>') deleted=$(git diff -w -U0 $PB -- $DTO | grep -E '^-' | grep -vE '^---' | wc -l)"
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`, `synthetic_fail_line=synthetic_fail=0`, `r5cases=11`, `textmissing=0`
    - `n16_c13p1=R6|R8` (대표 R6, 함께 의심 R8), `n15_c13p3=R5`
    - `d16_ng=36 d16_r6=36`, `d16_r8any` ≥ 20
    - `d15_r5any` ≥ 20, `d15_r8any` ≥ 400, `d15_r6` ≥ 1700
    - `old_r8r5=0`, `d01_r7` ≥ 1, `exceptions_nonzero=0` (20260917 포함 — 쓰는 중인 폴더도 예외 0)
    - `consts=4 median=1`
    - CycleResultDto.cs 추가 줄 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 logic3=0 lambda=0`, `deleted` ≤ 40 (Task 1 의 BuildOutOfToleranceResult·R6 평가 재구성분만 — 78-01 의 다른 클래스·DTO 줄 삭제 0)
    - `commit_files` = `WPF_Example/UI/ViewModel/CycleResultDto.cs`
  </acceptance_criteria>
  <done>R5 가 같은 기준선 측정 묶음의 공통 이동으로 판정되고 기준점 진단 값이 있으면 근거에 붙으며, 09-15/09-16 실데이터에서 R5·R6·R8 이 판정되고 옛 데이터·쓰는 중인 폴더에서 예외가 없음을 확인해 커밋했다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 디스크 cycle.json 의 오버레이 좌표·측정값(손상·비정상 값 가능) → R5 기하 계산 | 0 길이·NaN 좌표·극단값이 나눗셈·정렬에 들어온다 |
| 추정 결과 → 운영자 조치(PLC 범위 요청·보정값 변경) | 오판이 잘못된 장비 조치로 이어진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-05 | Denial of Service | TryFindDistLineUnit / Median | medium | mitigate | 길이 > 0 가드(0 나눗셈 없음), Points 개수 가드, 빈 목록 Median 호출 전 개수 조건(R5_MIN_NEIGHBORS·R5_MIN_SHARED_COUNT), 실데이터 5개 날짜 `exceptions_nonzero=0` |
| T-78-06 | Tampering | R8 범위 판정 | high | mitigate | 범위 끝은 cycle.json 기록값·검사 당시 tick z 번호만 사용, 현재 레시피 참조 0(`recipe=0`) — PR-4. 근거 줄에 '(범위 끝 = 검사 당시 z 번호)' 로 어떤 값을 썼는지 표시 |
| T-78-07 | Repudiation | 대표 원인 선택 | medium | mitigate | 고정 순서 + 함께 의심 줄에 나머지 발동 규칙 모두 표시(숨기지 않음), 근거 숫자 동시 표시 |
| T-78-08 | Information Disclosure | 새 문구 | low | accept | 측정명·수치·z 번호만 로컬 화면 표시 |
</threat_model>

<verification>
1. Debug|x64 빌드 에러 0
2. synthetic 31+ 사례 PASS (78-01 13 + Task 1 20 + Task 2 11), text=missing 0
3. 실데이터: 20260916 R6 36/36 + C13_P1 함께 의심 R8, 20260915 C13_P3 R5 + R5 ≥ 20 + R8 ≥ 400, 옛 JSON R5·R8 0, 20260601 R7 ≥ 1, 5개 날짜 예외 0
4. 분석기 현재 레시피·설정·파일 참조 0, 추가 줄 하드룰 grep 0
</verification>

<success_criteria>
- CONTEXT 성공 기준 3: 09-15/09-16 실데이터에서 기준점 흔들림(R5)·치우침(R6)·초점 범위 끝값(R8) 판정
- 여러 규칙 발동 시 대표 1 + 함께 의심 표시
- 옛 cycle.json 은 가능한 규칙만 평가(성공 기준 5)
</success_criteria>

<output>
완료 후 `.planning/phases/78-reviewer-ng-cause-analysis/78-02-SUMMARY.md` 작성: base-78-02 해시, 커밋 2개, synthetic 출력 원문, rows 출력 줄 원문(5개 날짜), 위 verify 의 d15/d16/old 숫자, 임계값 const 값(A-78-A1 재확인용), 규격과 다르게 구현한 점이 있으면 그 이유.
</output>
