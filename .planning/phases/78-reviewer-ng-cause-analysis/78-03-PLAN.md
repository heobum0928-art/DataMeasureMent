---
phase: 78-reviewer-ng-cause-analysis
plan: 03
type: execute
wave: 2
depends_on: ["78-01"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
autonomous: true
requirements: [NGA-07, NGA-05]
user_setup: []

must_haves:
  truths:
    - "Z 범위 Shot 의 범위 끝 tick 에서 에지 강도로 Z 를 고른 측정은 cycle.json MeasurementResultDto.ZCandidateScores 에 후보 z 마다 {ZIndex, Ok, Score} 가 기록되고, 그 Shot 의 ShotResultDto 에 ZRangeStartIndex/ZRangeEndIndex(검사 당시 ZIndex/ZIndexEnd)가 기록된다 (NGA-07, D-78-08)"
    - "모든 자동·수동 cycle.json 에 그 시퀀스 Datum 마다 DatumDiagnostics{DatumName, IsDetected, IsDetectedThisTick, DetectTime, OriginRow/Col, AngleDeg, EdgeCount, FitRmse, AlignMatchScore/Row/Col/AngleDeg, Align2Score, AlignThetaDeg} 가 기록된다 — 패턴매칭 1번 패턴 점수는 TryComposeAlign 이 매칭 성공 직후 DatumConfig 에 남긴 값 (NGA-07, D-78-08)"
    - "검사 판정·PLC 응답·통계 CSV 는 바뀌지 않는다 — 5개 파일 모두 기존 줄 삭제 0, 추가 줄에 판정·응답 대입 없음, MeasurementHistoryCsvWriter.cs 무변경 (NGA-05, PR-3)"
    - "새 런타임 보관 칸(MeasurementBase.LastZCandidateScores, DatumConfig.LastAlignMatch* 4개)은 public 필드 + JsonIgnore 라 레시피 INI·레시피 JSON·붙여넣기 복사에 새 키가 생기지 않는다 (NGA-05)"
    - "[edge NGA-07 boundary] Z 범위가 꺼졌거나 오설정(IsZRangeEnabled false)인 Shot 은 ZRangeStartIndex/ZRangeEndIndex = -1. IsDetectedThisTick 은 LastFindSucceeded 이고 검출 스탬프가 tick 시작 시각 이상(같은 시각 포함)일 때만 true"
    - "[edge NGA-07 adjacency] 점수가 같은 후보도 하나로 합치지 않고 평가 순서대로 모두 기록된다"
    - "[edge NGA-07 empty] 후보가 없는 측정(범위 미적용·미지원 타입·후보 사진 없음 폴백)은 ZCandidateScores 가 빈 배열로 저장되고, ClearResult 가 이전 사이클 후보 목록을 null 로 지운다. Datum 이 없는 시퀀스는 DatumDiagnostics 빈 배열"
    - "[edge NGA-07 ordering] ZCandidateScores 는 RunZFocusCandidates 평가 순서(z 오름차순), DatumDiagnostics 는 DatumConfigs 목록 순서다"
    - "[edge NGA-07 precision] double 은 Newtonsoft 왕복으로 값이 그대로 돌아오고(probe 비트 단위 비교), NaN·무한대 진단 값은 0.0 으로 기록된다"
    - {statement: "SIMUL 또는 실기 자동 사이클 1회 후 실제 cycle.json 에 DatumDiagnostics·ZCandidateScores·ZRangeStartIndex 가 채워진다 — 실행 하네스가 없어 사람이 확인 (78-07 UAT U-6)", verification: backstop}
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs"
      provides: "BuildDto 의 ZRange·후보 점수 복사 + BuildDatumDiagnostic 매퍼"
      contains: "public static DatumDiagnosticDto BuildDatumDiagnostic(DatumConfig datum, DateTime dtTickStartUtc)"
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "BuildTickDatumDiagnosticsSnapshot + 저장 3곳 배선 + TryComposeAlign 점수 기록"
      contains: "BuildTickDatumDiagnosticsSnapshot()"
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "BuildZCandidateScoreList + ExecuteZRangeSelection write-back"
      contains: "meas.LastZCandidateScores = BuildZCandidateScoreList(lstResults);"
  key_links:
    - from: "Action_FAIMeasurement.ExecuteZRangeSelection"
      to: "MeasurementBase.LastZCandidateScores"
      via: "ApplySelectedZLabel 다음 줄 write-back"
      pattern: "meas\\.LastZCandidateScores = BuildZCandidateScoreList\\(lstResults\\);"
    - from: "CycleResultSerializer.BuildDto"
      to: "MeasurementResultDto.ZCandidateScores / ShotResultDto.ZRangeEndIndex"
      via: "measDto.SelectedZIndex 복사 다음 줄 / shotDto 생성 다음 IsZRangeEnabled 가드"
      pattern: "shotDto\\.ZRangeEndIndex = shot\\.ZIndexEnd;"
    - from: "InspectionSequence 저장 3곳(AddResponse, HandleManualCyclePersist, PersistAndEnqueueV1)"
      to: "CycleResultDto.DatumDiagnostics"
      via: "cycleDto.DatumDiagnostics = BuildTickDatumDiagnosticsSnapshot();"
      pattern: "cycleDto\\.DatumDiagnostics = BuildTickDatumDiagnosticsSnapshot\\(\\);"
  prohibitions:
    - statement: "MUST NOT 진단 값 기록 때문에 검사 판정·측정값·PLC 응답·통계 CSV 가 달라지지 않는다 — 이미 계산된 값을 복사만 하고 새 계산·재측정·판정 대입을 하지 않는다 (D-78-08)"
      status: resolved
      verification: judgment
---

<objective>
D-78-08: 로그에만 있던 진단 값(Z 후보별 선명도 점수, 기준점 원점·각도·매칭 점수)과 검사 당시 Z 범위를 cycle.json 에 **기록만** 추가한다. 78-02 규칙(R5 근거·R8 판정/근거)이 이 필드를 읽는다.

Purpose: 리뷰어가 로그 파일 없이 "왜 z5 를 골랐나 / 기준점이 얼마나 잘 잡혔나" 를 근거 숫자로 보여 줄 수 있게 한다. 옛 파일은 필드가 없어 해당 근거만 '기록 없음' 으로 건너뛴다(78-02).

Output: 5개 기존 파일에 삽입만. 신규 .cs 없음. probe json 모드.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-CONTEXT.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-RESEARCH.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-01-PLAN.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-01-SUMMARY.md

<interfaces>
계획 시점 라인 번호(편집 전 기준).

78-01 DTO 계약(CycleResultDto.cs, namespace ReringProject.UI): `ZCandidateScoreDto{ZIndex, Ok, Score}`, `DatumDiagnosticDto{DatumName, IsDetected, IsDetectedThisTick, DetectTime, OriginRow, OriginCol, AngleDeg, EdgeCount, FitRmse, AlignMatchScore, AlignMatchRow, AlignMatchCol, AlignMatchAngleDeg, Align2Score, AlignThetaDeg}`, `ShotResultDto.ZRangeStartIndex/ZRangeEndIndex`(-1), `MeasurementResultDto.ZCandidateScores`, `CycleResultDto.DatumDiagnostics`.

MeasurementBase.cs (Allman, namespace ReringProject.Sequence, using System.Collections.Generic 있음, ReringProject.UI using 없음)
- `:118-125` Phase 77 필드 주석 + `public double LastFitScore;` `public int LastSelectedZIndex = SELECTED_Z_NONE;`
- `:184-193` ClearResult (`:192` `LastSelectedZIndex = SELECTED_Z_NONE; // Phase 77: ...`)

Action_FAIMeasurement.cs (K&R 파일, 이 구간은 Allman; ReringProject.UI using 없음)
- `:2372-2397` ExecuteZRangeSelection (`:2390` `List<ZFocusRunResult> lstResults = RunZFocusCandidates(...)`, `:2394` `meas.LastSelectedZIndex = chosen.ZIndex;`, `:2395` `ApplySelectedZLabel(chosen.Overlays, chosen.ZIndex); ...`)
- `:2529-2555` RunZFocusCandidates (후보 목록 순서대로 평가 — z 오름차순), `:2743-2750` private class ZFocusRunResult {ZIndex, Ok, Score, Value, Error, Overlays}
- `:2140`, `:2153` 오프라인 선택도 같은 ExecuteZRangeSelection 을 부른다

CycleResultSerializer.cs (Allman, namespace ReringProject.Sequence, using ReringProject.UI 있음)
- `:81-91` `var shotDto = new ShotResultDto { ... ResultImagePath = resultImagePath };` (초기화자 닫힘 `};` 는 `:91`)
- `:135` `measDto.SelectedZIndex = meas.LastSelectedZIndex;`
- `:36-44` BuildDto 시그니처, `:250-265` Load

DatumConfig.cs (K&R, namespace ReringProject.Sequence)
- `:1078-1082` Align2Score(transient 선례), `:1084-1088` AlignThetaDeg, `:1028-1036` DetectedOriginRow/Col(JsonIgnore), `:1111-1123` DetectedEdgeCount/FitRMSE/AngleDeg, `:1043-1045` LastFindTimeUtc, `:924-937` LastFindSucceeded(true 대입 시 스탬프)
- `:1334` `public DatumConfig(object owner) : base(owner)` — 계획 단계에서 `new DatumConfig(null)` 이 앱 밖 probe 에서 생성·LastFindSucceeded 스탬프 동작함을 확인

InspectionSequence.cs (혼합 스타일, using ReringProject.UI 있음)
- `:52` DatumConfigs, `:55` `_dtCycleStartUtc`(tick 시작 UTC, `:444` HandleRunStartResetResults 에서 갱신)
- `:268-279` AddResponse 저장(`:277` DatumImages, `:278` ZRangeImages, `:279` SaveAsync)
- `:296-305` HandleManualCyclePersist(`:304` `Name); // 이 시퀀스 소유 shot 만 cycle 에 포함`, `:305` SaveAsync)
- `:2841-2852` PersistAndEnqueueV1(`:2850` DatumImages, `:2851` ZRangeImages, `:2852` SaveAsync)
- `:563-567` TakeTickZRangeImagesSnapshot(K&R)
- `:3384-3405` TryComposeAlign — `:3393` `var svc = new PatternMatchService();`, `:3394` `double curRow, curCol, curAngleDeg, curScore;`, `:3395-3405` TryFindPose if 블록(`:3404` return false), `:3406` `// ② tilt(θ)` 주석
</interfaces>
</context>

## Artifacts this phase produces (이 plan 분)

phase 전체 원장은 `78-01-PLAN.md`. 이 plan 이 만드는 것: `MeasurementBase.LastZCandidateScores`, `Action_FAIMeasurement.BuildZCandidateScoreList`, `DatumConfig.ALIGN_MATCH_NONE`·`LastAlignMatchScore`·`LastAlignMatchRow`·`LastAlignMatchCol`·`LastAlignMatchAngleDeg`, `CycleResultSerializer.BuildDatumDiagnostic`·`ToFiniteOrZero`·`NON_FINITE_REPLACEMENT`, `InspectionSequence.BuildTickDatumDiagnosticsSnapshot`, cycle.json 키 `ZRangeStartIndex`·`ZRangeEndIndex`·`ZCandidateScores`·`DatumDiagnostics` 실제 기록, probe `json` 모드.

<tasks>

<task type="auto">
  <name>Task 1: Z 후보 선명도 점수와 검사 당시 Z 범위를 cycle.json 에 기록</name>
  <precondition>78-01 이 커밋되어 `grep -c 'public class ZCandidateScoreDto' WPF_Example/UI/ViewModel/CycleResultDto.cs` 가 1 이다</precondition>
  <reversibility rating="costly">cycle.json 키 이름(ZCandidateScores, ZRangeStartIndex, ZRangeEndIndex)은 배포 후 쌓이는 파일에 남는다. 나중에 이름을 바꾸면 그 사이 파일은 R8 근거가 '기록 없음' 으로 보인다(데이터 손상은 없음).</reversibility>
  <files>WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs, WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs</files>
  <read_first>
    - CLAUDE.md (Code Style 하드룰 전체)
    - .planning/phases/78-reviewer-ng-cause-analysis/78-RESEARCH.md 'Pattern 4' 와 'Pitfall 5'(잔재 방지)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs:95-200
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs:2360-2420, :2525-2560, :2735-2752
    - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs:36-160
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs 에서 `IsZRangeEnabled` 정의 부분(grep 으로 위치 확인)
    - WPF_Example/UI/ViewModel/CycleResultDto.cs 의 ZCandidateScoreDto·ShotResultDto·MeasurementResultDto
  </read_first>
  <action>
모든 변경은 기존 줄 사이에 새 줄을 끼워 넣는 방식이다. 기존 줄은 한 글자도 고치거나 지우지 않는다(D-78-08, PR-3).

(0) `H=/c/Users/admin/AppData/Local/Temp/p78-probe; mkdir -p $H; git rev-parse HEAD > $H/base-78-03.txt`.

(1) MeasurementBase.cs — `public int LastSelectedZIndex = SELECTED_Z_NONE;` 다음 줄에 주석 1줄(`// Phase 78 NGA-07: Z 후보별 선명도 — cycle.json 기록 전용. 필드라 INI·붙여넣기 제외, JsonIgnore 로 레시피 JSON 제외`)과 `[Newtonsoft.Json.JsonIgnore]`, `public List<ReringProject.UI.ZCandidateScoreDto> LastZCandidateScores;` 를 넣는다. ClearResult 의 `LastSelectedZIndex = SELECTED_Z_NONE; // Phase 77: ...` 다음 줄에 `LastZCandidateScores = null; // Phase 78 NGA-07: 이전 사이클 후보 점수 잔재 방지` 를 넣는다(RESEARCH Pitfall 5).

(2) Action_FAIMeasurement.cs — ExecuteZRangeSelection 의 `ApplySelectedZLabel(chosen.Overlays, chosen.ZIndex);` 줄 다음에 `meas.LastZCandidateScores = BuildZCandidateScoreList(lstResults); // Phase 78 NGA-07: 이미 평가한 후보 결과를 복사만 한다(재계산 없음)` 를 넣는다(RecordMeasurementResult 의 실패 분기 ClearResult 뒤라 값이 남는다). RunZFocusCandidates 메서드 다음에 Allman 스타일로 `private static List<ReringProject.UI.ZCandidateScoreDto> BuildZCandidateScoreList(List<ZFocusRunResult> lstResults)` — 새 목록을 만들고, lstResults 가 null 이면 빈 목록 반환, 아니면 목록 순서대로(정렬·중복 제거 없음) 각 결과가 null 이 아니면 `ZIndex`, `Ok`, `Score` 를 복사한 DTO 를 추가해 반환. 오프라인 선택(ExecuteZRangeSelection 공용)도 같은 경로로 기록된다.

(3) CycleResultSerializer.cs — BuildDto:
- `var shotDto = new ShotResultDto { ... };` 초기화 문장이 끝난 다음 줄에 `bool bZRangeEnabled = shot.IsZRangeEnabled(); // Phase 78 NGA-07: 검사 당시 Z 범위 기록 — 리뷰어 R8 은 현재 레시피가 아니라 이 값을 쓴다` 와 `if (bZRangeEnabled)` 블록(중괄호) 안에 `shotDto.ZRangeStartIndex = shot.ZIndex;` `shotDto.ZRangeEndIndex = shot.ZIndexEnd;`. 꺼짐·오설정이면 DTO 기본값 -1 그대로.
- `measDto.SelectedZIndex = meas.LastSelectedZIndex;` 다음 줄에 `if (meas.LastZCandidateScores != null)` 블록 안에 `measDto.ZCandidateScores = new List<ZCandidateScoreDto>(meas.LastZCandidateScores);`. null 이면 DTO 이니셜라이저의 빈 목록이 그대로 JSON 빈 배열로 저장된다.

(4) probe `json` 모드(`NgCauseProbe.exe <binDir> json <scratchDir> <oldCycleJsonPath>`)를 추가한다:
- 메모리에서 CycleResultDto 를 만들고 Shot 1개(ZRangeStartIndex 3, ZRangeEndIndex 5), 측정 1개(ZCandidateScores = [{3, true, 28.284271247461902}, {4, true, 30.1}, {5, true, 30.1}, {6, false, 0.0}])를 넣는다 → `JsonConvert.SerializeObject(dto, Formatting.Indented)` 를 `<scratchDir>/roundtrip/cycle.json` 에 UTF8 로 쓴다 → `CycleResultSerializer.Load` → ZRange 값, 후보 개수·순서·ZIndex·Ok, Score 를 `BitConverter.DoubleToInt64Bits` 로 비교 → `json roundtrip=PASS|FAIL`.
- oldCycleJsonPath 를 Load → `json old zstart=<첫 Shot ZRangeStartIndex> zend=<ZRangeEndIndex> cand=<ZCandidateScores 총 개수> datum=<DatumDiagnostics 개수>`.
- 리플렉션: `typeof(ReringProject.Sequence.MeasurementBase)` 에서 `LastZCandidateScores` 가 필드이고(GetField 비-null, GetProperty null) Newtonsoft JsonIgnoreAttribute 가 1개 → `json meas_field=1 meas_jsonignore=1`.
(5) Debug|x64 빌드 → probe 재컴파일 → `json` 실행(old = `D:/Data/Result/20260601/163944_cycle/cycle.json`). 커밋: `git add` 로 위 3개 경로만. 메시지 예: feat(78-03): Z 후보 선명도·검사 당시 Z 범위 cycle.json 기록.

CLAUDE.md 하드룰(새 줄 전부 — 위반 = 회귀): 삼항 연산자 금지, null 병합·null 조건 연산자 금지, switch 식 금지, 한 줄 분기도 중괄호, 한 줄에 한 문장, 논리 연산자 3개 이상 금지(이름 있는 bool), 헝가리언, 매직넘버 const, 날짜·이니셜 서명 주석 금지(`// Phase 78 NGA-07: 이유`), 새 줄에 물음표 문자 금지, 수정 파일의 기존 brace 스타일 유지, 기존 줄의 위반은 복사·수정하지 않는다. 판정·측정값·응답 필드에 대입하는 줄을 추가하지 않는다. 새 .cs 파일·csproj 스테이징 금지. MainView.xaml.cs·MeasurementHistoryCsvWriter.cs 는 건드리지 않는다.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p78-probe; FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug
PB=$(cat $H/base-78-03.txt)
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-78-03a.log 2>&1; echo "msbuild_exit=$?"
(cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config NgCauseProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:NgCauseProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:ClosedXML.dll -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll NgCauseProbe.cs && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN json C:/Users/admin/AppData/Local/Temp/p78-probe/json D:/Data/Result/20260601/163944_cycle/cycle.json) | tee $H/json-78-03a.txt
MB=WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs; AF=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs; CS=WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs
echo "mbfield=$(grep -c 'public List<ReringProject.UI.ZCandidateScoreDto> LastZCandidateScores;' $MB) mbclear=$(tr -d '\r' < $MB | awk '/public void ClearResult\(\)/,/^        }$/' | grep -c 'LastZCandidateScores = null;') writeback=$(tr -d '\r' < $AF | awk '/private bool ExecuteZRangeSelection\(/,/^        }$/' | grep -c 'meas.LastZCandidateScores = BuildZCandidateScoreList(lstResults);') builder=$(grep -c 'private static List<ReringProject.UI.ZCandidateScoreDto> BuildZCandidateScoreList(List<ZFocusRunResult> lstResults)' $AF)"
echo "shotcopy=$(grep -cE 'shotDto.ZRangeStartIndex = shot.ZIndex;|shotDto.ZRangeEndIndex = shot.ZIndexEnd;' $CS) candcopy=$(grep -c 'measDto.ZCandidateScores = new List<ZCandidateScoreDto>(meas.LastZCandidateScores);' $CS) csv_untouched=$(git diff --name-only $PB -- WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs | wc -l)"
for F in $MB $AF $CS; do ADD=$(git diff -w -U0 $PB -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "$F deleted=$(git diff -w -U0 $PB -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') logic3=$(printf '%s\n' "$ADD" | grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)') judge=$(printf '%s\n' "$ADD" | grep -cE 'LastJudgement\s*=|LastMeasuredValue\s*=|FaiAllPass\s*=|ResponseQueue')"; done
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`
    - probe: `json roundtrip=PASS`, `json old zstart=-1 zend=-1 cand=0 datum=0`, `json meas_field=1 meas_jsonignore=1`
    - `mbfield=1 mbclear=1 writeback=1 builder=1`, `shotcopy=2 candcopy=1 csv_untouched=0`
    - 3개 파일 모두 `deleted=0 ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 logic3=0 judge=0`
    - `commit_files` 가 위 3개 경로뿐
  </acceptance_criteria>
  <done>범위 선택 측정의 후보 선명도와 검사 당시 Z 범위가 판정 경로를 건드리지 않고 cycle.json 에 기록되도록 삽입만으로 배선되었고, JSON 왕복·옛 파일 기본값·레시피 JSON 제외가 probe 로 확인되어 커밋되었다.</done>
</task>

<task type="auto">
  <name>Task 2: 기준점 진단 값(원점·각도·에지 수·RMSE·패턴 점수)을 모든 cycle.json 에 기록</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs, WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs, WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs:915-940, :1025-1125, :1330-1336, :1370-1400 (_copyExclude — 필드는 대상 아님 확인)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs:50-66, :245-345 (저장·RecordSeatingEvidence 의 신선도 판정 선례), :436-450, :515-570, :2825-2865, :3380-3450
    - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs (Task 1 결과)
    - WPF_Example/UI/ViewModel/CycleResultDto.cs 의 DatumDiagnosticDto
  </read_first>
  <action>
기존 줄은 한 글자도 고치거나 지우지 않는다.

(1) DatumConfig.cs — `public double AlignThetaDeg { get; set; }` 다음에(K&R 파일, 필드 선언은 한 줄씩): 주석 2줄(`// Phase 78 NGA-07: 1번(주) 패턴 매칭 결과 — cycle.json 진단 기록 전용(판정 무관). TryComposeAlign 이 매칭 전 0 으로 지우고 성공 직후 기록한다.` / `//  프로퍼티가 아닌 필드라 INI·붙여넣기·PropertyGrid 에 나오지 않고, JsonIgnore 로 레시피 JSON 에서도 빠진다.`), `public const double ALIGN_MATCH_NONE = 0.0;`, 그리고 `[Newtonsoft.Json.JsonIgnore]` 를 각각 붙인 `public double LastAlignMatchScore;`, `public double LastAlignMatchRow;`, `public double LastAlignMatchCol;`, `public double LastAlignMatchAngleDeg;`.

(2) InspectionSequence.cs — TryComposeAlign:
- `double curRow, curCol, curAngleDeg, curScore;` 줄 바로 앞에 `datum.LastAlignMatchScore = DatumConfig.ALIGN_MATCH_NONE;` 외 3개(Row, Col, AngleDeg) — 매칭 실패 시 이전 사이클 값이 남지 않게(주석 1줄 `// Phase 78 NGA-07: 매칭 실패 시 이전 값 잔재 방지`).
- TryFindPose if 블록 닫는 중괄호 다음 줄(`// ② tilt(θ)` 주석 앞)에 `datum.LastAlignMatchScore = curScore;`, `datum.LastAlignMatchRow = curRow;`, `datum.LastAlignMatchCol = curCol;`, `datum.LastAlignMatchAngleDeg = curAngleDeg;` (주석 `// Phase 78 NGA-07: 1번 패턴 매칭 결과 기록만 — 아래 보정 계산은 그대로`).

(3) CycleResultSerializer.cs — BuildDto 메서드 다음에 Allman 스타일로:
- `private const double NON_FINITE_REPLACEMENT = 0.0;`
- `private static double ToFiniteOrZero(double dValue)` — `double.IsNaN` 또는 `double.IsInfinity` 이면(이름 있는 bool) NON_FINITE_REPLACEMENT, 아니면 dValue.
- `public static DatumDiagnosticDto BuildDatumDiagnostic(DatumConfig datum, DateTime dtTickStartUtc)` (XML 요약: tick 저장 시점 Datum 진단 스냅샷, 기록 전용) — datum null 이면 null. `bool bHasStamp = datum.LastFindTimeUtc > DateTime.MinValue;`, `bool bStampInTick = bHasStamp && datum.LastFindTimeUtc >= dtTickStartUtc;`, `bool bDetectedThisTick = datum.LastFindSucceeded && bStampInTick;`. DTO 채우기: DatumName(null → 빈 문자열, if 로), IsDetected = LastFindSucceeded, IsDetectedThisTick, DetectTime = bHasStamp 이면 LastFindTimeUtc.ToLocalTime() 아니면 DateTime.MinValue(if/else), OriginRow/OriginCol = ToFiniteOrZero(DetectedOriginRow/Col), AngleDeg = ToFiniteOrZero(DetectedAngleDeg), EdgeCount = DetectedEdgeCount, FitRmse = ToFiniteOrZero(DetectedFitRMSE), AlignMatchScore/Row/Col/AngleDeg = ToFiniteOrZero(LastAlignMatch*), Align2Score = ToFiniteOrZero(Align2Score), AlignThetaDeg = ToFiniteOrZero(AlignThetaDeg).

(4) InspectionSequence.cs — TakeTickZRangeImagesSnapshot 다음에 K&R 스타일로 `private List<DatumDiagnosticDto> BuildTickDatumDiagnosticsSnapshot()` — 새 목록, try 안에서 DatumConfigs 가 null 이면 목록 반환, 아니면 순서대로 null 아닌 Datum 마다 `CycleResultSerializer.BuildDatumDiagnostic(d, _dtCycleStartUtc)` 결과가 null 이 아니면 추가; catch (Exception ex) 에서 `Logging.PrintErrLog((int)ELogType.Error, "[NGA-07] 기준점 진단 기록 실패(무시): " + ex.Message)` 를 try-catch 로 감싸 호출하고 새 빈 목록 반환(검사 스레드 보호 — 예외를 밖으로 내보내지 않는다).
- 배선 3곳(각 1줄 삽입): AddResponse 의 `cycleDto.ZRangeImages = TakeTickZRangeImagesSnapshot();` 다음 줄, PersistAndEnqueueV1 의 같은 줄 다음 줄, HandleManualCyclePersist 의 BuildDto 호출 문장(`Name); // 이 시퀀스 소유 shot 만 cycle 에 포함`) 다음 줄 — 모두 `cycleDto.DatumDiagnostics = BuildTickDatumDiagnosticsSnapshot();`. SaveAsync 호출 순서는 그대로(배선 줄이 SaveAsync 보다 앞).

(5) probe `json` 모드에 datummap 사례를 추가한다: `var dc = new ReringProject.Sequence.DatumConfig(null);` DetectedOriginRow 6601.9, DetectedOriginCol 634.2, DetectedAngleDeg 0.015, DetectedEdgeCount 40, DetectedFitRMSE double.NaN, LastAlignMatchScore 0.98, AlignThetaDeg double.PositiveInfinity 설정 → `DateTime before = DateTime.UtcNow;` → `dc.LastFindSucceeded = true;` → BuildDatumDiagnostic(dc, before) 에서 IsDetected·IsDetectedThisTick true, FitRmse 0.0, AlignThetaDeg 0.0, AlignMatchScore 0.98, OriginRow 6601.9 이면 `datummap fresh=PASS`; BuildDatumDiagnostic(dc, DateTime.UtcNow.AddMinutes(1)) 의 IsDetectedThisTick false 면 `datummap stale=PASS`; 검출한 적 없는 새 DatumConfig 는 IsDetected false·DetectTime == DateTime.MinValue 면 `datummap never=PASS`; null 입력 → null 이면 `datummap null=PASS`. fresh DTO 를 넣은 CycleResultDto 를 JSON 왕복해 필드 값이 비트 단위로 같으면 `datummap roundtrip=PASS`. 리플렉션으로 DatumConfig 의 LastAlignMatch* 4개가 필드(프로퍼티 아님)이고 JsonIgnore 를 가지면 `json datum_fields=4 datum_jsonignore=4`.
(6) 빌드 → probe 재컴파일 → json 실행. 커밋: `git add WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs`. 메시지 예: feat(78-03): 기준점 진단 값 cycle.json 기록.

CLAUDE.md 하드룰(Task 1 과 동일). HALCON 객체를 새로 만들지 않는다. 락을 새로 추가하지 않는다(읽기 스냅샷 + try/catch).
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p78-probe; FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug
PB=$(cat $H/base-78-03.txt)
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-78-03b.log 2>&1; echo "msbuild_exit=$?"
(cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config NgCauseProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:NgCauseProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:ClosedXML.dll -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll NgCauseProbe.cs && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN json C:/Users/admin/AppData/Local/Temp/p78-probe/json D:/Data/Result/20260601/163944_cycle/cycle.json) | tee $H/json-78-03b.txt
DC=WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs; IS=WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs; CS=WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs; AF=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs; MB=WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
echo "dcfields=$(grep -cE '^\s*public double LastAlignMatch(Score|Row|Col|AngleDeg);' $DC) wire=$(grep -c 'cycleDto.DatumDiagnostics = BuildTickDatumDiagnosticsSnapshot();' $IS) snap=$(grep -c 'private List<DatumDiagnosticDto> BuildTickDatumDiagnosticsSnapshot()' $IS) mapper=$(grep -c 'public static DatumDiagnosticDto BuildDatumDiagnostic(DatumConfig datum, DateTime dtTickStartUtc)' $CS)"
echo "align_reset=$(tr -d '\r' < $IS | awk '/public bool TryComposeAlign\(DatumConfig datum, HImage refImage, HImage refImageVertical/,/double curRow, curCol, curAngleDeg, curScore;/' | grep -c 'LastAlignMatch.* = DatumConfig.ALIGN_MATCH_NONE;') align_set=$(grep -cE 'datum.LastAlignMatchScore = curScore;|datum.LastAlignMatchRow = curRow;|datum.LastAlignMatchCol = curCol;|datum.LastAlignMatchAngleDeg = curAngleDeg;' $IS)"
echo "order_before_save=$(tr -d '\r' < $IS | grep -nE 'cycleDto.DatumDiagnostics = BuildTickDatumDiagnosticsSnapshot\(\);|CycleResultSerializer.SaveAsync\(cycleDto\);' | awk -F: 'BEGIN{p=0;ok=0} /DatumDiagnostics/{p=1;next} /SaveAsync/{if(p==1){ok++} p=0} END{print ok}')"
for F in $DC $IS $CS $AF $MB; do ADD=$(git diff -w -U0 $PB -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "$F deleted=$(git diff -w -U0 $PB -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') logic3=$(printf '%s\n' "$ADD" | grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)') judge=$(printf '%s\n' "$ADD" | grep -cE 'LastJudgement\s*=|LastMeasuredValue\s*=|FaiAllPass\s*=|ResponseQueue|LastFindSucceeded\s*=')"; done
echo "untouched=$(git diff --name-only $PB -- WPF_Example/UI/ContentItem/MainView.xaml.cs WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs WPF_Example/DatumMeasurement.csproj | wc -l) newcs=$(git diff --name-status $PB | grep -cE '^A.*\.cs$')"
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`
    - probe: `datummap fresh=PASS`, `datummap stale=PASS`, `datummap never=PASS`, `datummap null=PASS`, `datummap roundtrip=PASS`, `json datum_fields=4 datum_jsonignore=4`, Task 1 줄(`json roundtrip=PASS` 등) 유지
    - `dcfields=4 wire=3 snap=1 mapper=1`, `align_reset=4 align_set=4`, `order_before_save=3`
    - 5개 파일 모두 `deleted=0 ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 logic3=0 judge=0`
    - `untouched=0 newcs=0`
    - `commit_files` 가 DatumConfig.cs·InspectionSequence.cs·CycleResultSerializer.cs 3개뿐
  </acceptance_criteria>
  <done>기준점 진단 값과 1번 패턴 매칭 점수가 판정 경로·레시피 형식을 건드리지 않고 모든 cycle.json 에 기록되도록 삽입만으로 배선되었고, 매핑·신선도·비유한수·JSON 왕복이 probe 로 확인되어 커밋되었다. 실제 사이클 파일에 채워지는지는 78-07 UAT U-6 에서 확인한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 검사 스레드(AddResponse) → 새 스냅샷 코드 | 예외가 나면 TCP 응답 Enqueue 가 막힐 수 있다 |
| 런타임 객체(DatumConfig/MeasurementBase) → 레시피 저장(INI·JSON) | 새 칸이 레시피 파일에 새어 들어가면 레시피 형식이 바뀐다 |
| UI Test Find ↔ 검사 스레드 (TryComposeAlign 공유 DatumConfig) | 동시에 같은 진단 칸을 쓸 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-09 | Denial of Service | BuildTickDatumDiagnosticsSnapshot | high | mitigate | 메서드 전체 try/catch → 빈 목록 + 로그, 기존 저장 try 블록 안에서 SaveAsync 앞에만 호출(`order_before_save=3`), 새 락 없음 |
| T-78-10 | Tampering | 판정·응답 경로 | high | mitigate | 5개 파일 삭제 0 + 추가 줄에 판정·측정값·응답·검출 성공 대입 0(`judge=0`), CSV writer 무변경(`csv_untouched=0`) — PR-3 |
| T-78-11 | Tampering | 레시피 INI/JSON | medium | mitigate | 새 런타임 칸은 필드(ParamBase 프로퍼티 반사 대상 아님) + JsonIgnore(probe `meas_jsonignore=1`, `datum_jsonignore=4`) |
| T-78-12 | Tampering | TryComposeAlign 진단 칸 동시 쓰기 | low | accept | double 기록 전용 칸, 판정에 쓰이지 않음. 찢어진 읽기는 진단 숫자 1회 오차뿐 |
| T-78-13 | Information Disclosure | cycle.json 진단 값 | low | accept | 좌표·각도·점수만 로컬 파일, 네트워크 전송 없음 |
</threat_model>

<verification>
1. Debug|x64 빌드 에러 0
2. probe json: 후보 점수·범위 JSON 왕복 비트 동일, 옛 파일 기본값, Datum 매핑(신선/묵은/미검출/null/비유한수), JsonIgnore·필드 여부
3. 배선: ExecuteZRangeSelection write-back 1, BuildDto 복사 3, 저장 3곳 DatumDiagnostics(SaveAsync 앞), TryComposeAlign 리셋 4 + 기록 4
4. 5개 파일 삭제 0, 판정 대입 0, 하드룰 grep 0, CSV·MainView·csproj 무변경
5. 런타임 기록 확인은 78-07 U-6 (backstop)
</verification>

<success_criteria>
- D-78-08: 기준점 각도·원점·매칭 점수와 Z 후보별 선명도가 cycle.json 에 기록 추가(검사 로직·판정 불변, 옛 파일 호환)
- 레시피 형식 불변
</success_criteria>

<output>
완료 후 `.planning/phases/78-reviewer-ng-cause-analysis/78-03-SUMMARY.md` 작성: base-78-03 해시, 커밋 2개, probe json 출력 원문, 5개 파일 삭제 수·judge grep 결과, 새 JSON 키 예시(probe 가 쓴 roundtrip/cycle.json 에서 ZCandidateScores·DatumDiagnostics 부분 발췌).
</output>
