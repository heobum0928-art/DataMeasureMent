---
phase: 78-reviewer-ng-cause-analysis
plan: 04
type: execute
wave: 2
depends_on: ["78-01"]
files_modified:
  - WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml
  - WPF_Example/Custom/Export/ExcelExportService.cs
  - WPF_Example/Custom/Export/ChartImageCapture.cs
autonomous: true
requirements: [NGA-06, NGA-04]
user_setup: []

must_haves:
  truths:
    - "리뷰어에서 측정 행을 누르면 그 FAI 의 실제 촬영 원본 사진(OriginImageFileName — 절대 경로, 존재, 크기 > 0)이 뜨고, 없을 때만 기존 Shot ResultImagePath(존재할 때)로 폴백한다. 사이클 전체 보기는 측정 결과가 있는 첫 FAI 의 원본 사진, 없으면 첫 Shot ResultImagePath (NGA-06, D-78-07)"
    - "20260916 실데이터의 216개 측정 행은 전부 원본 사진 경로로 해석되고 선택된 경로는 모두 실제 파일이다. 원본 경로가 없는 20260601 행은 원본으로 해석되는 행이 0 이다 (NGA-06)"
    - "리뷰어 왼쪽 버튼에서 '사이클 1건 엑셀 export' 와 '차트 이미지 캡처 점검' 버튼, 그 Click 핸들러, 사이클 1건 xlsx 생성 메서드, 스모크 PNG 메서드가 사라지고 전체 코드에서 참조 0 이다 (NGA-04, D-78-06)"
    - "[edge NGA-04 adjacency] 반복도 엑셀 export·CPK 리포트·일괄검사 export 가 쓰는 공용 헬퍼(BuildJudgementText, LoadCaptureImageBytes, TryInsertCaptureImage, ApplyCaptureColumnWidth, RenderHistogramPng, RenderTrendPng, TryInsertChartPicture)는 그대로 있고 빌드가 통과한다"
    - "[edge NGA-04 empty] 사이클을 선택하지 않은 상태에서도 사라진 버튼을 켜고 끄는 코드가 남아 있지 않다(참조 0, 빌드 통과)"
    - "[edge NGA-04 ordering] 남은 왼쪽 스택 순서는 날짜 폴더 열기 → (구분선) 자재번호 → 이전 결과에 누적 → 이미지 폴더 반복 검사 → 진행 표시 → 반복도 엑셀 export → CPK 리포트 export → Align 정합 조회 → 중간 단계도 보기 이다 (반복검사 묶음·Align 정합 조회 유지 — D-78-06)"
    - {statement: "[edge NGA-06 concurrency] 방금 끝난 사이클은 원본 사진을 워커가 아직 쓰는 중일 수 있다 — 파일이 없거나 0 바이트면 폴백, 쓰는 도중 파일을 열 때의 동작은 사람이 확인 (flagged assumption A-78-A3, 78-07 UAT U-2)", verification: backstop}
  artifacts:
    - path: "WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs"
      provides: "ReviewerImagePathResolver — 원본 사진 우선 경로 해석"
      contains: "public static class ReviewerImagePathResolver"
    - path: "WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs"
      provides: "사진 로드 2곳을 해석기 호출로 교체, 삭제 버튼 핸들러 제거"
      contains: "ReviewerImagePathResolver.ResolveRowImagePath(row.OwnerShot, row.OwnerFai)"
  key_links:
    - from: "ReviewerWindow.MeasurementGrid_SelectionChanged"
      to: "ReviewerImagePathResolver.ResolveRowImagePath"
      via: "string imgPath = ReviewerImagePathResolver.ResolveRowImagePath(row.OwnerShot, row.OwnerFai);"
      pattern: "ResolveRowImagePath\\(row\\.OwnerShot, row\\.OwnerFai\\)"
    - from: "ReviewerWindow.DisplayCycle"
      to: "ReviewerImagePathResolver.ResolveCycleImagePath"
      via: "string szCycleImagePath = ReviewerImagePathResolver.ResolveCycleImagePath(cycle);"
      pattern: "ResolveCycleImagePath\\(cycle\\)"
---

<objective>
두 가지 리뷰어 정리: (1) D-78-07 — 라이브 검사 결과도 시뮬레이션 사진 위에 그려지던 것을 그 검사의 실제 촬영 원본 사진으로 바꾼다. (2) D-78-06 — 필요 없어진 버튼 2개와 그 코드만 지우고, 반복검사 묶음·Align 정합 조회·공용 export 헬퍼는 남긴다.

Purpose: NG 위치를 눈으로 확인하려면 그 검사의 실제 사진이 필요하다(원인 패널의 '사진에서 확인' 안내가 의미를 가지려면 필수). 사이클 1건 export 는 78-05 NG 누적 엑셀로 대체된다.

Output: 5개 기존 파일 수정(삭제 포함), 신규 .cs 없음, probe image 모드.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-CONTEXT.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-RESEARCH.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-01-PLAN.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-01-SUMMARY.md

<interfaces>
계획 시점 라인 번호(78-01 편집 전 기준 — 78-01 이 ReviewerWindow.xaml.cs 에 4줄, xaml 에 패널을 넣었으므로 실행자는 grep 으로 현재 위치를 다시 찾는다).

ReviewerWindow.xaml.cs (Allman)
- `:122` 사이클 선택 핸들러 안에서 사이클 1건 엑셀 버튼의 IsEnabled 를 켜는 1줄
- `:164-172` DisplayCycle 의 첫 Shot 사진 로드(`var firstShot = cycle.Shots.FirstOrDefault();` + 3조건 if + LoadImage)
- `:254-260` MeasurementGrid_SelectionChanged 일반 측정 분기의 사진 로드(`string imgPath;` + 한 줄 if/else + 2조건 if + LoadImage)
- `:336-370` 사이클 1건 엑셀 export Click 핸들러(앞 주석 1줄 포함), `:604-623` 차트 이미지 캡처 점검 Click 핸들러(앞 `///` 주석 1줄 포함)
- 유지: `Button_LoadFolder_Click`, `Button_RepeatRun_Click`, `Button_RepeatExport_Click`, `Button_CpkReportExport_Click`, `Button_AlignVerify_Click`

ReviewerWindow.xaml
- `:38-40` btn_loadFolder, `:41-44` 사이클 1건 엑셀 export Button 요소(Content "엑셀 export"), `:46` Separator, `:47-75` 반복검사 묶음·Align 정합 조회, `:70-72` 차트 이미지 캡처 점검 Button 요소(Content "차트 이미지 캡처 점검"), `:77-80` chk_showIntermediate

ExcelExportService.cs (Allman, namespace ReringProject.Export)
- `:15-20` 클래스 요약 주석(사이클 1건 export 설명), `:23` 캡쳐 열 번호 const, `:28` export 1회 대기 예산 const
- `:40-147` 사이클 1건 xlsx 생성 public 메서드(요약 주석 `:40-42` 포함)
- 유지(internal static): `:153` LoadCaptureImageBytes, `:184` WaitForCaptureImage(private), `:213` TryReadCompleteJpeg(private), `:240` TryInsertCaptureImage, `:310` ApplyCaptureColumnWidth, `:319` BuildJudgementText — RepeatExcelExportService.cs:294,346,366,367 가 호출

ChartImageCapture.cs
- `:211-255` 스모크 PNG 저장 public 메서드(요약 주석 `:211-214` 포함) — 리뷰어 버튼만 호출
- 유지: RenderHistogramPng, RenderTrendPng, TryInsertChartPicture (CpkReportExportService.cs:403-407)

실데이터(계획 단계 확인, 읽기 전용): 20260811·20260915·20260916·20260917 의 FAI OriginImageFileName 은 전부 `D:\Data\Result\Image\...\original\origin_*.jpg` 절대 경로이고 실제 존재(2,224/48/354/3,288건). 20260601 은 전부 빈 문자열. 원본은 해상도 축소 없이 저장(CaptureImageSaveService 워커 `toWrite = src`)이라 오버레이 좌표가 맞는다.
</interfaces>
</context>

## Artifacts this phase produces (이 plan 분)

phase 전체 원장은 `78-01-PLAN.md`. 이 plan 이 만드는 것: `ReviewerImagePathResolver`(`ResolveRowImagePath`, `ResolveCycleImagePath`, `IsUsableImageFile`, `MIN_IMAGE_FILE_BYTES`, private `IsExistingFile`, `FaiHasMeasuredResult`), probe `image` 모드. 이 plan 이 지우는 것: 리뷰어 버튼 2개 + Click 핸들러 2개 + IsEnabled 1줄, ExcelExportService 의 사이클 1건 xlsx 생성 메서드와 그 메서드만 쓰던 const, ChartImageCapture 의 스모크 PNG 메서드.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 리뷰어 사진 = 실제 촬영 원본(OriginImageFileName 우선, 없으면 기존 경로)</name>
  <precondition>78-01 이 커밋되어 `grep -c 'CausePanelText' WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs` 가 1 이상이다</precondition>
  <reversibility rating="reversible">화면에 어떤 사진을 띄울지만 바뀐다. 해석기 호출 2줄을 되돌리면 이전 동작이다.</reversibility>
  <files>WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs, WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs</files>
  <read_first>
    - CLAUDE.md (Code Style 하드룰)
    - .planning/phases/78-reviewer-ng-cause-analysis/78-RESEARCH.md 'Pattern 3: D-78-07 실제 촬영 사진'
    - .planning/phases/78-reviewer-ng-cause-analysis/78-01-PLAN.md 결정 P-14, 'Probe 규격'
    - WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs 전체(78-01 결과)
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs:120-265
    - WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs:118-145 (LoadImage(string) — 파일을 HImage 로 연다)
    - WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs:1518-1555 (ResolveShotOriginPath — 측정이 처리된 FAI 의 원본을 고르는 선례)
  </read_first>
  <behavior>
    probe image 합성 사례(스크래치 폴더의 임시 파일 사용, D:\Data 쓰기 금지):
    - row_origin_exists: 원본 = 존재하는 10바이트 절대 경로 파일 → 원본
    - row_origin_missing: 원본 = 없는 절대 경로, ResultImagePath = 존재 파일 → ResultImagePath
    - row_origin_relative: 원본 = 상대 파일명(현재 폴더에 같은 이름 파일 존재) → ResultImagePath
    - row_origin_zero_bytes: 원본 = 0바이트 파일 → ResultImagePath
    - row_both_missing: 둘 다 없음 → null / row_null_inputs: shot·fai null → null, 예외 없음
    - cycle_first_measured: Shot1 FAI(측정 결과 없음, 원본 존재) + Shot2 FAI(결과 있음, 원본 존재) → Shot2 원본
    - cycle_pending_only: 유일한 FAI 가 Z_RANGE_PENDING 측정뿐(원본 존재) → 첫 Shot ResultImagePath
    - cycle_null: null → null
  </behavior>
  <action>
(0) `H=/c/Users/admin/AppData/Local/Temp/p78-probe; mkdir -p $H; git rev-parse HEAD > $H/base-78-04.txt`.

(1) ReviewMeasurementRow.cs — 파일 맨 위 using 목록 끝에 `using System.IO;` 를 추가한다(기존 using 줄은 건드리지 않는다). ReviewMeasurementRow 클래스 닫는 중괄호 다음(같은 namespace)에 Allman 스타일 `public static class ReviewerImagePathResolver` (XML 요약: 리뷰어가 띄울 사진 경로 — 그 검사의 실제 촬영 원본 우선, D-78-07. 순수 로직 + 파일 존재 확인만):
- `public const long MIN_IMAGE_FILE_BYTES = 1;` (주석: 0바이트 = 워커가 막 만든 파일)
- `public static bool IsUsableImageFile(string szPath)` — 비었으면 false. try 안에서 `Path.IsPathRooted` false 면 false(파일명만 있는 옛 기록은 기준 폴더를 알 수 없음), `File.Exists` false 면 false, `new FileInfo(szPath).Length` 가 MIN_IMAGE_FILE_BYTES 미만이면 false, 아니면 true. catch(잘못된 경로 문자 등) → false.
- `private static bool IsExistingFile(string szPath)` — 기존 리뷰어 규칙 그대로: 비어 있지 않고 File.Exists(try/catch → false). ResultImagePath 폴백에만 쓴다(회귀 0).
- `private static bool FaiHasMeasuredResult(FaiResultDto fai)` — fai·Measurements null 이면 false. 측정 중 null 이 아닌 것이 LastHasResult 이거나, 사유가 비어 있지 않고 SkipReason.Z_RANGE_PENDING 이 아니면(이름 있는 bool) true.
- `public static string ResolveRowImagePath(ShotResultDto shot, FaiResultDto fai)` — fai 가 null 이 아니고 IsUsableImageFile(fai.OriginImageFileName) 이면 그 경로(이름 있는 bool 로 뽑는다), 아니면 shot 이 null 이 아니고 IsExistingFile(shot.ResultImagePath) 이면 그 경로, 아니면 null.
- `public static string ResolveCycleImagePath(CycleResultDto cycle)` — cycle 또는 Shots 가 null 이면 null. Shots 순서 → FAIs 순서로(각 null 이면 continue) FaiHasMeasuredResult 이고 IsUsableImageFile(원본) 인 첫 FAI 원본 반환. 없으면 Shots 의 첫 항목(null 이면 null 반환)의 ResultImagePath 가 IsExistingFile 이면 반환, 아니면 null. 중첩이 3단계를 넘으면 FAI 순회를 `private static string FindMeasuredOriginInShot(ShotResultDto shot)` 로 분리.

(2) ReviewerWindow.xaml.cs — 배선만:
- DisplayCycle 의 첫 Shot 사진 로드 블록(`var firstShot = cycle.Shots.FirstOrDefault();` 부터 LoadImage 블록 닫는 중괄호까지 7줄)을 `string szCycleImagePath = ReviewerImagePathResolver.ResolveCycleImagePath(cycle); // Phase 78 NGA-06: 실제 촬영 원본 우선` + `if (!string.IsNullOrEmpty(szCycleImagePath))` + 중괄호 안 `halconViewer.LoadImage(szCycleImagePath);` 로 바꾼다. 바로 위 기존 주석 2줄은 그대로 둔다. LoadImage → SetInspectionOverlays 순서 유지.
- MeasurementGrid_SelectionChanged 일반 측정 분기의 `string imgPath;` 부터 LoadImage 블록 닫는 중괄호까지(6줄)를 `string imgPath = ReviewerImagePathResolver.ResolveRowImagePath(row.OwnerShot, row.OwnerFai); // Phase 78 NGA-06` + `if (!string.IsNullOrEmpty(imgPath))` + 중괄호 안 `halconViewer.LoadImage(imgPath);` 로 바꾼다. DualImage 분기(ShowAxisImage)는 건드리지 않는다.
(3) probe `image` 모드(`NgCauseProbe.exe <binDir> image <scratchDir> <dateDir>...`)를 추가한다: 먼저 behavior 합성 사례를 `<scratchDir>/img` 안의 임시 파일로 평가해 `imgcase=<이름> PASS|FAIL` 을 찍고, 날짜 폴더마다 사이클·행 전부를 해석해 `image date=<날짜> cycles=<n> cycle_origin=<원본 선택 수> cycle_fallback=<ResultImagePath 선택 수> cycle_none=<null 수> rows=<행 수> row_origin=<> row_fallback=<> row_none=<> chosen_missing=<선택된 경로 중 File.Exists false 수>`, 마지막에 `imgcase_fail=<n>`. 원본 판정 = 선택 경로가 그 FAI 의 OriginImageFileName 과 같음.
(4) 빌드 → probe 재컴파일 → `image C:/Users/admin/AppData/Local/Temp/p78-probe D:/Data/Result/20260601 D:/Data/Result/20260916`. 커밋: `git add WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs`. 메시지 예: feat(78-04): 리뷰어 사진을 실제 촬영 원본으로.

CLAUDE.md 하드룰(새 줄 전부 — 위반 = 회귀): 삼항 연산자 금지, null 병합·null 조건 연산자 금지, switch 식 금지, 한 줄 분기도 중괄호, 한 줄에 한 문장, 논리 연산자 3개 이상 금지(이름 있는 bool), if 중첩 3단계 이상이면 메서드 분리, 헝가리언, 매직넘버 const, 날짜·이니셜 서명 주석 금지, 새 줄에 물음표 문자 금지, 기존 줄의 위반은 복사·수정하지 않는다. ReviewerWindow.xaml.cs 는 배선만(해석 로직은 해석기에). MainView.xaml.cs 무변경. 새 .cs 금지. D:\Data 읽기만.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p78-probe; FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug
PB=$(cat $H/base-78-04.txt)
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-78-04a.log 2>&1; echo "msbuild_exit=$?"
(cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config NgCauseProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:NgCauseProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:ClosedXML.dll -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll NgCauseProbe.cs && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN image C:/Users/admin/AppData/Local/Temp/p78-probe D:/Data/Result/20260601 D:/Data/Result/20260916) | tee $H/image-78-04a.txt
ROW=WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs; XC=WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs
echo "resolver=$(grep -cE 'public static class ReviewerImagePathResolver|public static string ResolveRowImagePath\(ShotResultDto shot, FaiResultDto fai\)|public static string ResolveCycleImagePath\(CycleResultDto cycle\)|public static bool IsUsableImageFile\(string szPath\)' $ROW) wire=$(grep -cE 'ReviewerImagePathResolver.ResolveCycleImagePath\(cycle\)|ReviewerImagePathResolver.ResolveRowImagePath\(row.OwnerShot, row.OwnerFai\)' $XC) oldload=$(grep -c 'halconViewer.LoadImage(firstShot.ResultImagePath)' $XC)"
XADD=$(git diff -w -U0 $PB -- $XC | grep -E '^\+' | grep -vE '^\+\+\+'); echo "branch_kw=$(printf '%s\n' "$XADD" | grep -cE '\b(if|switch|for|foreach|while)\b')"
for F in $ROW $XC; do ADD=$(git diff -w -U0 $PB -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "$F deleted=$(git diff -w -U0 $PB -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') logic3=$(printf '%s\n' "$ADD" | grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)')"; done
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`
    - probe: 합성 사례 9개 `imgcase=... PASS`, `imgcase_fail=0`
    - `image date=20260916` 줄: `rows=216 row_origin=216 row_fallback=0 chosen_missing=0`, `cycle_origin` ≥ 1
    - `image date=20260601` 줄: `row_origin=0 chosen_missing=0`
    - `resolver=4 wire=2 oldload=0`, `branch_kw=2` (교체된 null 가드 if 2개뿐)
    - ReviewMeasurementRow.cs `deleted=0`, ReviewerWindow.xaml.cs `deleted` 1~13 (교체한 사진 로드 2블록 안에서만 — diff 가 같은 중괄호 줄을 공통으로 볼 수 있어 범위로 판정), 두 파일 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 logic3=0`
    - `commit_files` 가 위 2개 경로뿐
  </acceptance_criteria>
  <done>리뷰어 사진 로드 2곳이 실제 촬영 원본 우선 해석기를 거치도록 교체되고, 실데이터(원본 있음/없음)와 합성 경계 사례로 폴백 규칙이 확인되어 커밋되었다.</done>
</task>

<task type="auto">
  <name>Task 2: 필요 없는 리뷰어 기능 삭제 — 사이클 1건 엑셀 export·차트 이미지 캡처 점검(공용 헬퍼·반복검사 묶음·Align 정합 조회 유지)</name>
  <files>WPF_Example/UI/Reviewer/ReviewerWindow.xaml, WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs, WPF_Example/Custom/Export/ExcelExportService.cs, WPF_Example/Custom/Export/ChartImageCapture.cs</files>
  <read_first>
    - .planning/phases/78-reviewer-ng-cause-analysis/78-RESEARCH.md 'Pitfall 1: 삭제 범위 오판', 'Anti-Patterns'
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml:28-82
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs (Task 1 결과 — 사이클 선택 핸들러, 사이클 1건 엑셀 핸들러, 차트 캡처 점검 핸들러 위치를 grep 으로 확인)
    - WPF_Example/Custom/Export/ExcelExportService.cs 전체
    - WPF_Example/Custom/Export/ChartImageCapture.cs:1-40, :205-257
    - WPF_Example/Custom/Export/RepeatExcelExportService.cs:285-370, WPF_Example/Custom/Export/CpkReportExportService.cs:395-410 (유지 대상 호출부)
  </read_first>
  <action>
삭제 전에 `grep -rn` 으로 아래 삭제 대상 각각의 호출부가 리뷰어 한 곳뿐인지 다시 확인한다(계획 단계 확인: 둘 다 ReviewerWindow.xaml.cs 에서만 호출). 삭제한 자리에 "삭제함" 같은 흔적 주석을 남기지 않는다.

(1) ReviewerWindow.xaml — 왼쪽 버튼 StackPanel 에서 두 Button 요소를 요소 단위로 통째로 지운다: Content 가 "엑셀 export" 인 Button(날짜 폴더 열기 버튼 바로 아래, 4줄)과 Content 가 "차트 이미지 캡처 점검" 인 Button(CPK 리포트 export 와 Align 정합 조회 사이, 3줄). 기존 XAML 주석 줄은 고치지 않는다. 나머지 요소 순서·속성은 그대로(must_haves 의 ordering 순서).
(2) ReviewerWindow.xaml.cs:
- CycleList_SelectionChanged 안에서 사이클 1건 엑셀 버튼의 IsEnabled 를 설정하는 1줄을 지운다(바로 앞 빈 줄도 함께 지워 메서드 끝에 빈 줄이 남지 않게).
- 사이클 1건 xlsx 를 저장하는 Click 핸들러 메서드 전체(바로 위 `// 현재 선택된 cycle 을 xlsx 로 export...` 주석 1줄과 메서드 뒤 빈 줄 1줄 포함)를 지운다.
- 차트 이미지 캡처 점검 Click 핸들러 메서드 전체(바로 위 `/// 오프스크린 Canvas → PNG 캡처...` 주석 1줄과 메서드 뒤 빈 줄 1줄 포함)를 지운다.
- 남는 필드·using 은 그대로(`_currentCycle` 은 DisplayCycle 경로에서 계속 쓰인다).
(3) ExcelExportService.cs:
- 사이클 1건 xlsx 를 새로 만드는 public static 메서드(요약 주석 3줄 포함, 메서드 뒤 빈 줄 1줄 포함)를 지운다.
- 그 결과 선언만 남고 참조가 0 이 된 private const(캡쳐 열 번호 const, export 1회 대기 예산 const)를 `grep -c` 로 참조 수를 확인한 뒤 선언 줄(과 그 줄 끝 주석)만 지운다. 다른 const 는 남긴다.
- 클래스 요약 주석(`/// <summary>` ~ `/// </summary>` 6줄)을 새 3줄로 바꾼다: `/// <summary>` / `/// 결과 xlsx 공용 헬퍼 — 판정 문구, 캡쳐 JPG 대기·셀 삽입. 반복도·일괄검사 export(RepeatExcelExportService)가 공유한다.` / `/// </summary>`. 클래스 이름·internal 헬퍼 4개(BuildJudgementText, LoadCaptureImageBytes, TryInsertCaptureImage, ApplyCaptureColumnWidth)와 private 헬퍼(WaitForCaptureImage, TryReadCompleteJpeg)는 그대로 둔다.
(4) ChartImageCapture.cs — 합성 샘플로 PNG 2장을 폴더에 저장하는 public static 스모크 메서드 전체(요약 주석 4줄과 그 앞 빈 줄 1줄 포함)를 지운다. RenderHistogramPng·RenderTrendPng·TryInsertChartPicture 는 그대로.
(5) Debug|x64 빌드 → probe 재컴파일 → rows(20260916) + image(20260916) 재실행으로 회귀 확인. 커밋: `git add WPF_Example/UI/Reviewer/ReviewerWindow.xaml WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs WPF_Example/Custom/Export/ExcelExportService.cs WPF_Example/Custom/Export/ChartImageCapture.cs`. 메시지 예: refactor(78-04): 리뷰어 사이클 1건 엑셀·차트 캡처 점검 기능 삭제 (D-78-06).

CLAUDE.md 하드룰: 이 task 의 추가 줄은 요약 주석 3줄뿐 — 날짜·이니셜 서명 금지, 물음표 문자 금지. 새 .cs 금지, csproj 무변경, ExcelExportSmokeTest.cs·RepeatExcelExportService.cs·CpkReportExportService.cs·MainView.xaml.cs 는 건드리지 않는다.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p78-probe; FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug
PB=$(cat $H/base-78-04.txt)
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-78-04b.log 2>&1; echo "msbuild_exit=$? errors=$(grep -cE ': error [A-Z]+[0-9]+' $H/build-78-04b.log)"
(cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config NgCauseProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:NgCauseProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:ClosedXML.dll -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll NgCauseProbe.cs && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN rows C:/Users/admin/AppData/Local/Temp/p78-probe/out D:/Data/Result/20260916 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN image C:/Users/admin/AppData/Local/Temp/p78-probe D:/Data/Result/20260916) | tee $H/regress-78-04b.txt
XA=WPF_Example/UI/Reviewer/ReviewerWindow.xaml; XC=WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs; EX=WPF_Example/Custom/Export/ExcelExportService.cs; CH=WPF_Example/Custom/Export/ChartImageCapture.cs
echo "gone_xaml=$(grep -cE 'btn_exportExcel|btn_chartSmoke' $XA) gone_refs=$(grep -rlE 'Button_ExportExcel_Click|Button_ChartSmoke_Click|TrySaveSmokePng|ExcelExportService\.Export\(' WPF_Example --include=*.cs --include=*.xaml | grep -v '/obj/' | wc -l) gone_method=$(grep -c 'public static bool Export(CycleResultDto' $EX)"
echo "keep_helpers=$(grep -cE 'internal static string BuildJudgementText\(|internal static byte\[\] LoadCaptureImageBytes\(|internal static bool TryInsertCaptureImage\(|internal static void ApplyCaptureColumnWidth\(' $EX) keep_charts=$(grep -cE 'RenderHistogramPng\(|RenderTrendPng\(|TryInsertChartPicture\(' $CH) keep_callers=$(grep -cE 'ExcelExportService\.(ApplyCaptureColumnWidth|BuildJudgementText|LoadCaptureImageBytes|TryInsertCaptureImage)' WPF_Example/Custom/Export/RepeatExcelExportService.cs)"
echo "keep_xaml=$(grep -cE 'x:Name=\"(btn_loadFolder|txt_materialIndex|chk_repeatAccumulate|btn_repeatRun|lbl_repeatProgress|btn_repeatExport|btn_cpkReportExport|btn_alignVerify|chk_showIntermediate)\"' $XA) keep_handlers=$(grep -cE 'private void (Button_LoadFolder_Click|Button_RepeatRun_Click|Button_RepeatExport_Click|Button_CpkReportExport_Click|Button_AlignVerify_Click)\(' $XC)"
echo "order=$(for N in btn_loadFolder txt_materialIndex chk_repeatAccumulate btn_repeatRun lbl_repeatProgress btn_repeatExport btn_cpkReportExport btn_alignVerify chk_showIntermediate; do grep -n "x:Name=\"$N\"" $XA | cut -d: -f1; done | awk 'NR==1{p=$1;ok=1;next} {if($1<=p){ok=0} p=$1} END{print ok}')"
echo "deleted_xaml=$(git diff -w -U0 $PB -- $XA | grep -E '^-' | grep -vE '^---' | wc -l) deleted_xc=$(git diff -w -U0 $PB -- $XC | grep -E '^-' | grep -vE '^---' | wc -l) deleted_ex=$(git diff -w -U0 $PB -- $EX | grep -E '^-' | grep -vE '^---' | wc -l) deleted_ch=$(git diff -w -U0 $PB -- $CH | grep -E '^-' | grep -vE '^---' | wc -l)"
ADD=$(git diff -w -U0 HEAD~1 -- $EX $CH $XC | grep -E '^\+' | grep -vE '^\+\+\+'); echo "added_task2=$(printf '%s\n' "$ADD" | grep -c .) datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') qmark=$(printf '%s\n' "$ADD" | grep -cF '?')"
echo "untouched=$(git diff --name-only $PB -- WPF_Example/Custom/Export/RepeatExcelExportService.cs WPF_Example/Custom/Export/CpkReportExportService.cs WPF_Example/Custom/Export/ExcelExportSmokeTest.cs WPF_Example/UI/ContentItem/MainView.xaml.cs WPF_Example/DatumMeasurement.csproj | wc -l)"
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0 errors=0` (반복도·CPK·일괄검사 export 컴파일 유지 — adjacency)
    - probe 회귀: `date=20260916` 줄 `ng=36 exceptions=0`, `image date=20260916` 줄 `row_origin=216 chosen_missing=0`
    - `gone_xaml=0 gone_refs=0 gone_method=0`
    - `keep_helpers=4 keep_charts` ≥ 3 `keep_callers=4`
    - `keep_xaml=9 keep_handlers=5 order=1`
    - `deleted_xaml` ≤ 8, `deleted_xc` ≤ 80(Task 1 교체 13줄 포함), `deleted_ex` ≤ 125, `deleted_ch` ≤ 50
    - `added_task2` ≤ 3, `datesig=0 qmark=0`
    - `untouched=0`
    - `commit_files` 가 위 4개 경로뿐
  </acceptance_criteria>
  <done>D-78-06 삭제 대상만 정확히 제거되고 공용 헬퍼·반복검사 묶음·Align 정합 조회·차트 렌더는 그대로이며, 빌드와 probe 회귀가 통과해 커밋되었다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cycle.json 의 사진 경로 문자열(손상·수기 편집 가능) → 리뷰어 파일 열기 | 임의 경로·잘못된 문자·거대 파일이 LoadImage 로 간다 |
| 코드 삭제 → 유지 기능(반복도·CPK·일괄검사 export) | 공용 헬퍼를 잘못 지우면 유지 기능이 깨진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-14 | Tampering | ReviewerImagePathResolver 경로 입력 | medium | mitigate | 절대 경로·존재·크기 > 0 만 허용, 잘못된 경로 문자 예외는 catch → false, 읽기 전용(쓰기·삭제 없음). 사진 파일은 로컬 D:\Data 결과 폴더 기록값 |
| T-78-15 | Denial of Service | 쓰는 도중인 원본 JPG 열기 | low | accept | 0바이트는 폴백, 미완성 파일 열기 실패는 기존 전역 예외 창 경로 — 빈도 낮고 재클릭으로 회복(A-78-A3, UAT U-2) |
| T-78-16 | Denial of Service | 공용 export 헬퍼 삭제 오판 | high | mitigate | 삭제 전 grep 호출부 확인, `keep_helpers=4 keep_callers=4 keep_charts≥3`, 빌드 에러 0, 유지 파일 무변경(`untouched=0`) |
| T-78-17 | Information Disclosure | probe 합성 파일 | low | accept | 스크래치 폴더의 10바이트 임시 파일, 커밋 없음 |
</threat_model>

<verification>
1. Debug|x64 빌드 에러 0
2. probe image: 합성 9 사례 PASS, 20260916 216행 원본·선택 경로 실존, 20260601 원본 0
3. 삭제 대상 참조 0, 공용 헬퍼·차트 렌더·유지 버튼·핸들러·버튼 순서 유지
4. 추가 줄 하드룰 grep 0, code-behind 추가 분기는 교체된 null 가드 2개뿐
</verification>

<success_criteria>
- D-78-07: 리뷰어가 실제 촬영 사진(OriginImageFileName 우선)을 띄움
- D-78-06 / 성공 기준 4: 차트 이미지 캡처 점검·사이클 1건 엑셀 export 버튼 제거, 나머지 기능 회귀 0
</success_criteria>

<output>
완료 후 `.planning/phases/78-reviewer-ng-cause-analysis/78-04-SUMMARY.md` 작성: base-78-04 해시, 커밋 2개, probe image 출력 원문, 파일별 삭제 줄 수, 지운 const 이름, 삭제 전 호출부 grep 결과.
</output>
