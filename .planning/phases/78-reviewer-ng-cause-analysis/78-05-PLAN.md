---
phase: 78-reviewer-ng-cause-analysis
plan: 05
type: execute
wave: 3
depends_on: ["78-02", "78-04"]
files_modified:
  - WPF_Example/Custom/Export/ExcelExportService.cs
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs
autonomous: true
requirements: [NGA-03, NGA-04]
user_setup: []

must_haves:
  truths:
    - "리뷰어에서 날짜 폴더를 연 뒤 'NG 누적 엑셀 저장' 을 누르면 그 날짜 폴더의 NG 측정만 ResultSavePath\\NG_분석_누적.xlsx 시트 'NG 누적' 에 20열(검사시각·검사구분·레시피·자재번호·Shot·FAI·측정명·측정값·공칭·공차+·공차-·판정·사용 Z·추정 원인·근거·확인할 일·함께 의심·원인 코드·사진 경로·사이클 폴더)로 추가된다 (NGA-03, D-78-02, O-78-01/07)"
    - "엑셀의 추정 원인·근거·확인할 일·함께 의심·원인 코드는 같은 날짜 폴더를 연 리뷰어 화면 패널과 글자까지 같다 — 20260916 36행 비교 불일치 0 (D-78-04)"
    - "다른 날짜 폴더를 열고 누르면 같은 파일 아래에 계속 쌓인다 — 20260916 36행 뒤에 20260601 140행이 붙어 176행 (D-78-02)"
    - "[edge NGA-04 idempotency] 같은 날짜로 버튼을 두 번 누르면 두 번째는 '새로 추가할 NG 가 없습니다' 로 끝나고 행 수·파일 내용이 그대로다"
    - "[edge NGA-03 encoding] 중복 키 = 사이클 폴더(끝 구분자 제거, 대소문자 무시) + Shot + FAI + 측정명(대소문자·공백 그대로 ordinal) — 같은 사이클에서 측정명이 같아도 Shot/FAI 가 다르면 서로 다른 행이고, 한글 문구는 xlsx 왕복 후 글자 그대로다"
    - "[edge NGA-03 empty] 날짜 폴더를 안 열었거나 없는 폴더면 '먼저 날짜 폴더 열기' 안내, cycle.json 이 하나도 없으면 '날짜 폴더를 여세요' 안내, NG 가 0 이면 '추가 0건' 안내 — 세 경우 모두 엑셀 파일을 만들거나 건드리지 않는다"
    - "[edge NGA-03 concurrency] 엑셀 파일이 다른 프로그램에 열려 있으면 '엑셀을 닫고 다시 누르세요' 안내 후 중단하고 파일은 바이트 단위로 그대로, 임시 파일도 남지 않는다. 저장은 같은 폴더 임시 xlsx → File.Replace 로 교체해 중간에 끊겨도 원본이 반쯤 쓰이지 않는다. 검사가 쓰는 도중인 cycle.json 은 '읽지 못한 결과 N건' 으로 세고 건너뛰며 다음에 누르면 추가된다"
    - "ReviewerWindow.xaml.cs 추가 줄은 필드 1 + 대입 1 + 핸들러(서비스 호출 2줄 + 메시지 1줄)뿐이고 분기·반복이 없다 (CLAUDE.md MVVM)"
  artifacts:
    - path: "WPF_Example/Custom/Export/ExcelExportService.cs"
      provides: "NgAccumulationExportService / NgAccumExportOutcome / ENgAccumExportStatus"
      contains: "public static class NgAccumulationExportService"
    - path: "WPF_Example/UI/Reviewer/ReviewerWindow.xaml"
      provides: "btn_ngAccumExport 'NG 누적 엑셀 저장' 버튼"
      contains: "btn_ngAccumExport"
  key_links:
    - from: "ReviewerWindow.Button_NgAccumExport_Click"
      to: "NgAccumulationExportService.AppendDateFolder"
      via: "_loadedDateFolder + BuildOutputPath(ResultSavePath)"
      pattern: "NgAccumulationExportService\\.AppendDateFolder\\(_loadedDateFolder, szOutputPath\\)"
    - from: "NgAccumulationExportService.AppendDateFolder"
      to: "NgCauseHistory + NgCauseAnalyzer.Analyze (화면과 같은 규칙 엔진)"
      via: "날짜 폴더 cycle.json 전부로 이력 구성 후 NG 측정마다 Analyze"
      pattern: "NgCauseAnalyzer\\.Analyze\\("
  prohibitions:
    - statement: "MUST NOT NG 누적 저장이 NG_분석_누적.xlsx(와 같은 폴더의 임시 xlsx) 외의 파일을 쓰거나, cycle.json·사진·CSV 를 수정·삭제하지 않는다 — 검사 증거 데이터는 읽기만 한다"
      status: resolved
      verification: judgment
---

<objective>
D-78-02/D-78-04: NG 만 한 엑셀 파일에 계속 쌓는 서비스를 만들고 리뷰어 버튼으로 연결한다. 행마다 화면 패널과 같은 추정 원인·근거·확인할 일이 들어간다.

Purpose: 비전 초보 운영자가 날짜와 상관없이 NG 원인 이력을 한 파일에서 필터·정렬로 볼 수 있게 한다(사이클 1건 export 를 대체).

Output: ExcelExportService.cs 에 서비스 추가, 리뷰어 버튼·핸들러, probe xlsx 모드.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-CONTEXT.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-01-PLAN.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-02-SUMMARY.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-04-SUMMARY.md

<interfaces>
78-01/02/04 결과(이 plan 은 이것만 쓴다):
- `NgCauseHistory` (`AddCycle(dto)`, `ResolveCycleKey(dto)`), `NgCauseAnalyzer.IsNgMeasurement(m)`, `NgCauseAnalyzer.Analyze(cycle, shot, fai, m, history)` → `NgCauseResult`(CauseCode, CauseText, EvidenceText, ActionText, SuspectText)
- `ExcelExportService.BuildJudgementText(m)` (internal static, 같은 파일)
- `MeasurementBase.FormatSelectedZ(int)`, `CycleResultSerializer.Load(path)`(손상 → null)
- `CycleResultDto` InspectionTime / IsProtocolDriven / RecipeName / IndexNumber(-1 = 없음) / CycleFolderPath
- ClosedXML 0.105.0: 계획 단계 실측 — `new XLWorkbook(path)` 로 연 파일에 행 추가 → 같은 폴더 `*.tmp.xlsx` 로 SaveAs → `File.Replace(tmp, path, null)` 왕복 성공, 다른 핸들이 쓰기 잠금 중이면 `new FileStream(path, FileMode.Open, FileAccess.ReadWrite, FileShare.None)` 이 IOException(잠금 감지), 잠금 중 `new XLWorkbook(path)` 도 IOException
- ReviewerWindow.xaml.cs(78-04 후): `LoadCycleFolders(string dateFolderPath)` 의 try 첫 문장 위치, 기존 CPK 리포트 핸들러의 `CustomMessageBox.Show(title, msg, icon)` 패턴, `using ReringProject.Export;` 있음
- ReviewerWindow.xaml(78-04 후): 왼쪽 StackPanel 첫 요소 `btn_loadFolder`, 그 다음 `Separator`
- `SystemHandler.Handle.Setting.ResultSavePath` 기본값 `D:\Data\Result`
</interfaces>
</context>

## NG 누적 엑셀 규격

| 열 | 머리글 | 값 |
|---|---|---|
| 1 | 검사시각 | InspectionTime `yyyy-MM-dd HH:mm:ss` |
| 2 | 검사구분 | IsProtocolDriven 이면 `자동`, 아니면 `수동` |
| 3 | 레시피 | RecipeName(null → 빈 문자열) |
| 4 | 자재번호 | IndexNumber ≥ 0 이면 숫자, 아니면 `-` |
| 5 | Shot | ShotName |
| 6 | FAI | FAIName |
| 7 | 측정명 | MeasurementName |
| 8 | 측정값 | LastHasResult 이면 double, 아니면 `-` |
| 9~11 | 공칭 / 공차+ / 공차- | NominalValue / TolerancePlus / ToleranceMinus (double) |
| 12 | 판정 | ExcelExportService.BuildJudgementText(m) |
| 13 | 사용 Z | MeasurementBase.FormatSelectedZ(m.SelectedZIndex) |
| 14 | 추정 원인 | NgCauseResult.CauseText |
| 15 | 근거 | EvidenceText |
| 16 | 확인할 일 | ActionText |
| 17 | 함께 의심 | SuspectText |
| 18 | 원인 코드 | CauseCode |
| 19 | 사진 경로 | FAI OriginImageFileName 이 비어 있지 않으면 그것, 아니면 Shot ResultImagePath(기록값 그대로 — 존재 확인 안 함) |
| 20 | 사이클 폴더 | dto.CycleFolderPath 가 비어 있지 않으면 그것, 아니면 스캔한 하위 폴더 경로 |

- 파일 `NG_분석_누적.xlsx`, 시트 `NG 누적`, 1행 = 머리글, 2행부터 데이터. 시트가 없으면 만들고 머리글을 쓴다(새 시트일 때만 열 너비 AdjustToContents).
- 중복 키 = NormalizeFolderKey(20열) + U+001F + 5열 + U+001F + 6열 + U+001F + 7열. NormalizeFolderKey = 끝 `\` `/` TrimEnd 후 ToUpperInvariant. HashSet ordinal. 기존 행 키를 먼저 모두 읽고, 이번에 추가하는 행도 넣어 같은 실행 안 중복도 막는다.
- 추가 순서 = 사이클 InspectionTime 오름차순 → 사이클 폴더 ordinal → Shot/FAI/측정 DTO 순서.
- 상태·안내(`ENgAccumExportStatus` → 메시지 const → 아이콘):
  - NoFolder: `먼저 '날짜 폴더 열기' 로 날짜 폴더를 여세요.` → Warning
  - NoCycles: `이 폴더에는 검사 결과(cycle.json)가 없습니다. 날짜 폴더(예: 20260916)를 여세요.` + 줄바꿈 + 폴더 → Warning
  - NoNg: `이 날짜 폴더에는 NG 가 없습니다 (추가 0건).` + 줄바꿈 + 폴더 → Information
  - NothingNew: `새로 추가할 NG 가 없습니다 — 이미 들어 있는 {0}건은 건너뛰었습니다.` + 줄바꿈 + 파일 → Information
  - Added: `NG {0}건을 추가했습니다 (이미 있던 {1}건 건너뜀).` + 줄바꿈 + 파일 → Information
  - FileLocked: `NG 누적 엑셀 파일이 열려 있어 저장하지 못했습니다. 엑셀을 닫고 다시 누르세요.` + 줄바꿈 + 파일 → Warning
  - Failed: `NG 누적 엑셀 저장에 실패했습니다 (Error 로그 확인).` + 줄바꿈 + 파일 → Error
  - Added/NothingNew/NoNg 에 읽지 못한 cycle.json 이 1건 이상이면 줄바꿈 + `읽지 못한 검사 결과 {0}건 — 저장 중이었거나 손상된 파일입니다. 잠시 후 다시 누르면 추가됩니다.`
  - Added 후 데이터 행이 `LARGE_FILE_ROW_WARNING`(100000) 이상이면 줄바꿈 + `파일이 커졌습니다({0}행). 파일 이름을 바꿔 보관하면 다음부터 새 파일로 시작합니다.`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NgAccumulationExportService — 날짜 폴더 NG 를 한 xlsx 에 중복 없이 추가(잠금·손상·빈 폴더 안전)</name>
  <precondition>78-02·78-04 가 커밋되어 `grep -c 'R5_MIN_SHIFT_TOL_RATIO = 0.15;' WPF_Example/UI/ViewModel/CycleResultDto.cs` 가 1 이고 `grep -c 'public static class ReviewerImagePathResolver' WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs` 가 1 이다</precondition>
  <reversibility rating="costly">파일 이름 `NG_분석_누적.xlsx`·시트 이름·20열 순서는 운영자가 쌓기 시작하면 바꾸기 어렵다(바꾸면 새 파일로 다시 쌓이거나 중복 판정 열이 어긋난다). 데이터 손상은 없다.</reversibility>
  <files>WPF_Example/Custom/Export/ExcelExportService.cs</files>
  <read_first>
    - CLAUDE.md (Code Style 하드룰)
    - .planning/phases/78-reviewer-ng-cause-analysis/78-RESEARCH.md 'Pitfall 4: 파일 잠금', 'Code Examples > ClosedXML 열기 vs 새로 만들기', 'Anti-Patterns'(매번 새 파일 금지)
    - 이 PLAN 의 'NG 누적 엑셀 규격'
    - .planning/phases/78-reviewer-ng-cause-analysis/78-01-PLAN.md 결정 P-12·P-13, 'Probe 규격'
    - WPF_Example/Custom/Export/ExcelExportService.cs 전체(78-04 후)
    - WPF_Example/Custom/Export/RepeatExcelExportService.cs:60-80, :195-210 (XLWorkbook 사용·로그 패턴)
    - WPF_Example/UI/ViewModel/CycleResultDto.cs 의 NgCauseHistory·NgCauseAnalyzer 공개 메서드
  </read_first>
  <behavior>
    probe(스크래치 경로만, D:\Data 쓰기 금지):
    - xlsx 20260916 새 파일 → Added, added=36, dup=0, 파일 데이터 행 36, 고유 키 36
    - 같은 명령 두 번째 → NothingNew, added=0, dup=36, 행 36
    - xlsx 20260601 같은 파일 → Added, added=140, 행 176, 고유 키 176
    - xlsxlock: 파일을 FileShare.None 으로 잡은 채 20260916 → FileLocked, 파일 SHA256 전후 같음, 임시 xlsx 없음
    - xlsxempty: 빈 문자열 폴더 → NoFolder / 없는 폴더 → NoFolder / 빈 스크래치 폴더 → NoCycles / 20260916 에서 NG 0 인 첫 사이클만 복사한 폴더 → NoNg — 네 경우 모두 대상 xlsx 가 생기지 않음
    - xlsxpartial: 20260916 NG 사이클 1개 복사 + `{broken` 내용의 cycle.json 폴더 1개 → Added, unreadable=1
    - xlsxequal: 20260916 결과 파일의 14~18열이 같은 폴더로 만든 ReviewMeasurementRow.Cause 와 전부 같음 → mismatch=0
  </behavior>
  <action>
(0) `H=/c/Users/admin/AppData/Local/Temp/p78-probe; mkdir -p $H; git rev-parse HEAD > $H/base-78-05.txt`.

(1) ExcelExportService.cs 맨 위 using 끝에 `using System.Windows;` 추가(MessageBoxImage). ExcelExportService 클래스 닫는 중괄호 다음(같은 namespace)에 Allman 스타일로:
- `public enum ENgAccumExportStatus { Added, NothingNew, NoNg, NoCycles, NoFolder, FileLocked, Failed }` (여러 줄, 멤버마다 한 줄 주석).
- `public class NgAccumExportOutcome` — 자동 프로퍼티 `ENgAccumExportStatus Status`, `int AddedCount`, `int SkippedDuplicateCount`, `int UnreadableCycleCount`, `int TotalDataRows`, `string Message`(= ""), `MessageBoxImage Icon`.
- `public static class NgAccumulationExportService` (XML 요약: 리뷰어가 연 날짜 폴더의 NG 만 한 xlsx 에 누적 — D-78-02. 화면과 같은 NgCauseAnalyzer 결과를 쓴다 — D-78-04):
  - const: `OUTPUT_FILE_NAME = "NG_분석_누적.xlsx"`, `SHEET_NAME = "NG 누적"`, `MESSAGE_TITLE = "NG 누적 엑셀"`, `TEMP_FILE_SUFFIX = "_저장중.xlsx"`, `CYCLE_JSON_NAME = "cycle.json"`, `HEADER_ROW = 1`, `FIRST_DATA_ROW = 2`, 열 번호 `COL_TIME = 1` … `COL_CYCLE_FOLDER = 20`(규격 표 20개 각각 이름 있는 const), `TIME_FORMAT = "yyyy-MM-dd HH:mm:ss"`, `AUTO_TEXT = "자동"`, `MANUAL_TEXT = "수동"`, `NO_VALUE_TEXT = "-"`, `KEY_SEPARATOR = "\u001F"`, `LARGE_FILE_ROW_WARNING = 100000`, 규격의 메시지 문구 const 전부. 머리글은 `private static readonly string[] HEADER_TEXTS`(규격 순서 20개).
  - `public static string BuildOutputPath(string szResultSavePath)` — 비었으면 빈 문자열, 아니면 Path.Combine(szResultSavePath, OUTPUT_FILE_NAME).
  - `public static NgAccumExportOutcome AppendDateFolder(string szDateFolderPath, string szOutputPath)` — 흐름(각 단계는 private 메서드, 가드 절):
    1. 폴더가 비었거나 Directory.Exists false → NoFolder(파일 안 건드림). 출력 경로가 비었으면 Failed.
    2. `LoadDateFolderCycles(folder, out int nUnreadable)` — 하위 폴더를 ordinal 정렬해 cycle.json 이 있는 것만 CycleResultSerializer.Load, null 이면 nUnreadable 증가, 아니면 `private class LoadedCycle { public CycleResultDto Dto; public string FolderPath; }` 목록에 추가 → 이름 있는 비교 메서드(InspectionTime → 폴더 키 ordinal)로 정렬. 로드된 사이클도 읽지 못한 것도 0 이면 NoCycles.
    3. NgCauseHistory 를 만들어 전부 AddCycle.
    4. `BuildNgRows(...)` — 사이클·Shot·FAI·측정 순서대로 IsNgMeasurement 인 것만 Analyze 해서 20개 셀 값과 중복 키를 가진 `private class NgRowData` 목록. 0 이면 NoNg(파일 안 건드림).
    5. 출력 파일이 있으면 `IsFileLocked(path)`(FileMode.Open, FileAccess.ReadWrite, FileShare.None 을 using 으로 열었다 닫음, IOException → true) → FileLocked(쓰기 전에 중단).
    6. 파일이 있으면 `new XLWorkbook(path)`, 없으면 `new XLWorkbook()` — using. 시트가 있으면 가져오고, 없으면 추가 + 머리글. `ReadExistingKeys(ws)` — FIRST_DATA_ROW 부터 LastRowUsed 까지 5·6·7·20열 GetString 으로 키 HashSet.
    7. 키가 없는 행만 LastRowUsed 다음 행부터 쓰고(숫자 열은 double 대입, `-` 는 문자열), 쓴 키는 HashSet 에 추가. 추가 0 이면 NothingNew(저장 안 함).
    8. 임시 경로 = 같은 폴더 + 파일명(확장자 제외) + TEMP_FILE_SUFFIX. 임시 파일 삭제는 `private static void TryDeleteTempFile(string szTempPath)`(존재하면 try 안에서 File.Delete, 예외 무시) 한 곳에서만 한다. 남아 있던 임시 파일을 그 헬퍼로 지운 뒤 `wb.SaveAs(임시)` → using 이 끝난 뒤 대상이 있으면 `File.Replace(임시, 대상, null)`, 없으면 `File.Move(임시, 대상)`. 교체 중 IOException → 임시 파일 try 삭제 후 FileLocked. 그 밖의 예외 → 임시 파일 try 삭제, `Logging.PrintErrLog((int)ELogType.Error, "[NgAccumulationExportService] " + ex.Message)` 를 try-catch 로 감싸 기록, Failed.
    9. 결과 메시지·아이콘은 `BuildOutcome(status, …)` 한 곳에서 규격 문구로 조립(읽지 못한 건수 줄, 큰 파일 줄 포함).
  - AppendDateFolder 는 어떤 경우에도 예외를 밖으로 내보내지 않는다(최상위 try/catch → Failed). 입력 폴더 아래 파일과 cycle.json·사진은 읽기만 한다(PR-5).
(2) probe 에 모드 추가(`Probe 규격` 관례): `xlsx <outXlsx> <dateDir>` → `xlsx status=<> added=<> dup=<> unreadable=<> filerows=<데이터 행> uniquekeys=<5·6·7·20열 고유 키 수>`(ClosedXML 로 다시 열어 셈). `xlsxlock <outXlsx> <dateDir>` → 파일 SHA256 계산, FileStream(FileShare.None) 을 연 채 AppendDateFolder, 닫은 뒤 SHA256 재계산 → `xlsxlock status=<> unchanged=<1|0> templeft=<임시 xlsx 존재 1|0>`. `xlsxempty <scratchDir> <dateDir>` → behavior 네 경우 → `xlsxempty nofolder=PASS|FAIL missing=… nocycles=… nong=… created=<생긴 xlsx 수>`. `xlsxpartial <scratchDir> <dateDir>` → `xlsxpartial status=<> added=<> unreadable=<>`. `xlsxequal <outXlsx> <dateDir>` → 같은 폴더로 이력을 만들고 파일 각 행을 (20·5·6·7열) 키로 ReviewMeasurementRow(5인자) 와 짝지어 14~18열 비교 → `xlsxequal rows=<> mismatch=<> unmatched=<>`. 스크래치 폴더 복사는 File.Copy 로만 하고 D:\Data 원본은 읽기만.
(3) 빌드 → probe 재컴파일 → verify 순서대로 실행. 커밋: `git add WPF_Example/Custom/Export/ExcelExportService.cs` 만. 메시지 예: feat(78-05): NG 누적 엑셀 서비스(중복 제외·잠금 안전).

CLAUDE.md 하드룰(새 줄 전부 — 위반 = 회귀): 삼항 연산자 금지(이 파일의 기존 삼항 줄은 복사하지 않는다), null 병합·null 조건 연산자 금지, switch 식 금지(전통 switch + break), 한 줄 분기도 중괄호, 한 줄에 한 문장, 논리 연산자 3개 이상 금지(이름 있는 bool), if 중첩 3단계 이상이면 메서드 분리, 헝가리언, 매직넘버 const, 날짜·이니셜 서명 주석 금지(`// Phase 78 NGA-03: 이유`), 새 줄에 물음표 문자 금지, 람다·LINQ 대신 이름 있는 메서드·for 문, 새 .cs 금지. D:\Data 는 probe 가 읽기만(출력 xlsx 는 스크래치 경로).
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p78-probe; FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug; W=C:/Users/admin/AppData/Local/Temp/p78-probe
PB=$(cat $H/base-78-05.txt)
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-78-05a.log 2>&1; echo "msbuild_exit=$?"
rm -rf $H/xl && mkdir -p $H/xl
(cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config NgCauseProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:NgCauseProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:ClosedXML.dll -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll NgCauseProbe.cs \
 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN xlsx $W/xl/ng_accum.xlsx D:/Data/Result/20260916 \
 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN xlsx $W/xl/ng_accum.xlsx D:/Data/Result/20260916 \
 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN xlsxequal $W/xl/ng_accum.xlsx D:/Data/Result/20260916 \
 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN xlsx $W/xl/ng_accum.xlsx D:/Data/Result/20260601 \
 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN xlsxlock $W/xl/ng_accum.xlsx D:/Data/Result/20260916 \
 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN xlsxempty $W/xl/empty D:/Data/Result/20260916 \
 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN xlsxpartial $W/xl/partial D:/Data/Result/20260916) | tee $H/xlsx-78-05a.txt
EX=WPF_Example/Custom/Export/ExcelExportService.cs
echo "svc=$(grep -cE 'public static class NgAccumulationExportService|public class NgAccumExportOutcome|public enum ENgAccumExportStatus|public static NgAccumExportOutcome AppendDateFolder\(string szDateFolderPath, string szOutputPath\)|public static string BuildOutputPath\(string szResultSavePath\)' $EX) consts=$(grep -cE 'OUTPUT_FILE_NAME = \"NG_분석_누적.xlsx\";|SHEET_NAME = \"NG 누적\";|LARGE_FILE_ROW_WARNING = 100000;' $EX) replace=$(grep -c 'File.Replace(' $EX) sharenone=$(grep -c 'FileShare.None' $EX) analyze=$(grep -c 'NgCauseAnalyzer.Analyze(' $EX)"
echo "writes=$(tr -d '\r' < $EX | awk '/public static class NgAccumulationExportService/,/^    }$/' | grep -cE 'File\.(Delete|WriteAll|AppendAll|Create)\(|Directory\.(Delete|Create)' ) dataroot=$(grep -ciE 'D:[\\/]+Data' $EX)"
ADD=$(git diff -w -U0 $PB -- $EX | grep -E '^\+' | grep -vE '^\+\+\+'); echo "ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') logic3=$(printf '%s\n' "$ADD" | grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)') lambda=$(printf '%s\n' "$ADD" | grep -cF '=>') deleted=$(git diff -w -U0 $PB -- $EX | grep -E '^-' | grep -vE '^---' | wc -l)"
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`
    - xlsx 출력 1번째: `status=Added added=36 dup=0 unreadable=0 filerows=36 uniquekeys=36`
    - xlsx 출력 2번째: `status=NothingNew added=0 dup=36 filerows=36` (idempotency)
    - `xlsxequal rows=36 mismatch=0 unmatched=0` (화면 = 엑셀, 한글 왕복)
    - xlsx 출력 3번째(20260601): `status=Added added=140 filerows=176 uniquekeys=176` (날짜 넘어 누적, 같은 측정명·다른 Shot/FAI 구분)
    - `xlsxlock status=FileLocked unchanged=1 templeft=0`
    - `xlsxempty nofolder=PASS missing=PASS nocycles=PASS nong=PASS created=0`
    - `xlsxpartial status=Added` 이고 `unreadable=1`
    - `svc=5 consts=3 replace=1 sharenone` ≥ 1 `analyze=1`
    - `writes=1` (TryDeleteTempFile 의 File.Delete 1곳만 — PR-5), `dataroot=0` (하드코딩 데이터 루트 경로 없음)
    - 추가 줄 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 logic3=0 lambda=0`, `deleted` ≤ 1(using 줄 정렬 외 삭제 없음)
    - `commit_files` = `WPF_Example/Custom/Export/ExcelExportService.cs`
  </acceptance_criteria>
  <done>날짜 폴더의 NG 만 화면과 같은 원인 문구로 한 xlsx 에 중복 없이 누적되고, 두 번 실행·날짜 누적·잠금·빈 폴더·손상 파일이 스크래치 경로 probe 로 확인되어 커밋되었다.</done>
</task>

<task type="auto">
  <name>Task 2: 리뷰어 'NG 누적 엑셀 저장' 버튼 배선 (연 날짜 폴더 → 서비스 → 안내 창)</name>
  <files>WPF_Example/UI/Reviewer/ReviewerWindow.xaml, WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml:28-80 (78-04 후 왼쪽 버튼 스택)
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs:20-100 (필드, LoadCycleFolders), CPK 리포트 핸들러(CustomMessageBox.Show 패턴)
    - WPF_Example/Custom/Export/ExcelExportService.cs 의 NgAccumulationExportService 공개 멤버(Task 1 결과)
  </read_first>
  <action>
(1) ReviewerWindow.xaml — `btn_loadFolder` Button 요소 바로 다음(Separator 앞)에 Button 1개: `x:Name="btn_ngAccumExport"`, `Content="NG 누적 엑셀 저장"`, `Click="Button_NgAccumExport_Click"`, `Padding="8,4"`, `Margin="0,4,0,0"`, `HorizontalAlignment="Stretch"`, `ToolTip="열어 둔 날짜 폴더의 NG 를 NG_분석_누적.xlsx 한 파일에 추가 (이미 들어간 항목은 건너뜀)"`. IsEnabled 는 지정하지 않는다(폴더를 안 열었으면 서비스가 안내). 앞에 XAML 주석 1줄 `<!-- Phase 78 NGA-03: NG 만 한 엑셀 파일에 누적 (D-78-02) -->`.
(2) ReviewerWindow.xaml.cs — 배선만(분기·반복 추가 금지):
- `_ngCauseHistory` 필드 다음에 `private string _loadedDateFolder;` (주석: 리뷰어가 마지막으로 연 날짜 폴더 — NG 누적 엑셀 대상).
- LoadCycleFolders 의 try 블록 첫 문장으로 `_loadedDateFolder = dateFolderPath;`.
- 기존 CPK 리포트 핸들러 다음에 `private void Button_NgAccumExport_Click(object sender, RoutedEventArgs e)` — 본문 3문장: `string szOutputPath = NgAccumulationExportService.BuildOutputPath(SystemHandler.Handle.Setting.ResultSavePath);`, `NgAccumExportOutcome outcome = NgAccumulationExportService.AppendDateFolder(_loadedDateFolder, szOutputPath);`, `CustomMessageBox.Show(NgAccumulationExportService.MESSAGE_TITLE, outcome.Message, outcome.Icon);`. 메서드 위 주석 1줄 `// Phase 78 NGA-03: 연 날짜 폴더의 NG 를 누적 엑셀에 추가 — 판단·문구는 서비스가 만든다`.
(3) Debug|x64 빌드 → probe 재컴파일 → rows(20260916) 회귀 + xlsx 1회(스크래치 새 파일) 재확인. 커밋: `git add WPF_Example/UI/Reviewer/ReviewerWindow.xaml WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs`. 메시지 예: feat(78-05): 리뷰어 NG 누적 엑셀 저장 버튼.

CLAUDE.md 하드룰: 새 줄에 삼항·null 병합·null 조건 연산자·switch 식 금지, 날짜·이니셜 서명 주석 금지, 물음표 문자 금지. MainView.xaml.cs 무변경. 새 .cs 금지.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p78-probe; FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug; W=C:/Users/admin/AppData/Local/Temp/p78-probe
PB=$(cat $H/base-78-05.txt)
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-78-05b.log 2>&1; echo "msbuild_exit=$?"
rm -rf $H/xl2 && mkdir -p $H/xl2
(cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config NgCauseProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:NgCauseProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:ClosedXML.dll -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll NgCauseProbe.cs && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN rows $W/out D:/Data/Result/20260916 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN xlsx $W/xl2/ng_accum.xlsx D:/Data/Result/20260916) | tee $H/regress-78-05b.txt
XA=WPF_Example/UI/Reviewer/ReviewerWindow.xaml; XC=WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs
echo "button=$(grep -c 'x:Name=\"btn_ngAccumExport\"' $XA) content=$(grep -c 'Content=\"NG 누적 엑셀 저장\"' $XA) after_load=$(L1=$(grep -n 'x:Name=\"btn_loadFolder\"' $XA | cut -d: -f1); L2=$(grep -n 'x:Name=\"btn_ngAccumExport\"' $XA | cut -d: -f1); L3=$(grep -n '<Separator' $XA | head -1 | cut -d: -f1); if [ "$L1" -lt "$L2" ] && [ "$L2" -lt "$L3" ]; then echo 1; else echo 0; fi)"
echo "handler=$(grep -c 'private void Button_NgAccumExport_Click(object sender, RoutedEventArgs e)' $XC) call=$(grep -c 'NgAccumulationExportService.AppendDateFolder(_loadedDateFolder, szOutputPath)' $XC) assign=$(tr -d '\r' < $XC | awk '/private void LoadCycleFolders\(string dateFolderPath\)/,/^        }$/' | grep -c '_loadedDateFolder = dateFolderPath;') field=$(grep -c 'private string _loadedDateFolder;' $XC)"
XADD=$(git diff -w -U0 $PB -- $XC | grep -E '^\+' | grep -vE '^\+\+\+'); echo "branch_kw=$(printf '%s\n' "$XADD" | grep -cE '\b(if|switch|for|foreach|while)\b') codelines=$(printf '%s\n' "$XADD" | grep -vE '^\+\s*(//|$|\{|\})' | grep -c .) ternary=$(printf '%s\n' "$XADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$XADD" | grep -cF '??') nullcond=$(printf '%s\n' "$XADD" | grep -cF '?.') datesig=$(printf '%s\n' "$XADD" | grep -cF 'hbk') deleted=$(git diff -w -U0 $PB -- $XC | grep -E '^-' | grep -vE '^---' | wc -l)"
echo "untouched=$(git diff --name-only $PB -- WPF_Example/UI/ContentItem/MainView.xaml.cs WPF_Example/DatumMeasurement.csproj | wc -l)"
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`
    - 회귀: `date=20260916` 줄 `ng=36 exceptions=0`, xlsx 줄 `status=Added added=36`
    - `button=1 content=1 after_load=1`
    - `handler=1 call=1 assign=1 field=1`
    - `branch_kw=0`, `codelines` ≤ 6, `ternary=0 coalesce=0 nullcond=0 datesig=0`, `deleted=0`
    - `untouched=0`
    - `commit_files` 가 ReviewerWindow.xaml·ReviewerWindow.xaml.cs 2개뿐
  </acceptance_criteria>
  <done>리뷰어에 'NG 누적 엑셀 저장' 버튼이 날짜 폴더 열기 바로 아래에 생기고, 연 날짜 폴더를 서비스에 넘겨 안내 창만 띄우는 배선이 분기 없이 커밋되었다. 실제 화면·엑셀 확인은 78-07 UAT U-3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 날짜 폴더의 cycle.json(쓰는 중·손상·수기 편집) → 엑셀 행 | 불완전 파일·이상 문자열이 누적 파일로 들어간다 |
| 리뷰어 버튼 → 디스크 쓰기(D:\Data\Result) | 앱이 처음으로 기존 파일을 열어 덮어쓰는 경로 |
| 사용자가 연 Excel 프로그램 ↔ 저장 | 파일 잠금 충돌 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-18 | Tampering | 누적 xlsx 부분 쓰기 | high | mitigate | 잠금 선검사로 쓰기 전 중단, 임시 xlsx SaveAs → File.Replace 교체, 실패 시 임시 삭제·원본 불변(`xlsxlock unchanged=1 templeft=0`) |
| T-78-19 | Tampering | 입력 증거 데이터(cycle.json·사진·CSV) | high | mitigate | 서비스는 입력을 Load 로 읽기만, 쓰기 API 는 임시 xlsx 삭제 1곳뿐(`writes=1`), 하드코딩 D:\Data 경로 0 — PR-5 |
| T-78-20 | Denial of Service | 대량 사이클·거대 누적 파일(UI 스레드 동기 실행) | medium | mitigate | 시트는 1회 열기, 키 HashSet, 새 시트일 때만 열 너비 계산, 10만 행 이상이면 파일 이름 바꿔 보관 안내. 잔여 위험(수초 대기)은 UAT U-3 에서 확인 |
| T-78-21 | Tampering | 셀 값의 수식 해석 | low | accept | ClosedXML `Value = string` 은 문자열 셀로 저장(수식 아님). canon 항목 — /gsd-secure-phase 소관 breadcrumb |
| T-78-22 | Denial of Service | 쓰는 도중 cycle.json | low | mitigate | Load null → 건너뜀·'읽지 못한 N건' 안내, 다음 실행에서 추가(`xlsxpartial unreadable=1`) |
</threat_model>

<verification>
1. Debug|x64 빌드 에러 0
2. probe xlsx: 추가 36 → 재실행 0 → 다른 날짜 140 누적 176, 화면=엑셀 불일치 0, 잠금 시 파일 불변·임시 없음, 빈 폴더 4경우 파일 미생성, 손상 1건 건너뜀
3. 버튼 위치(날짜 폴더 열기 바로 아래), 핸들러 배선 분기 0
4. 추가 줄 하드룰 grep 0, 입력 데이터 쓰기 0
</verification>

<success_criteria>
- CONTEXT 성공 기준 2: NG 누적 엑셀 버튼 1번에 해당 날짜 NG 가 한 파일에 추가되고, 다시 눌러도 중복 행이 안 생긴다
- D-78-04: 엑셀에 화면과 같은 원인·근거·확인할 일
</success_criteria>

<output>
완료 후 `.planning/phases/78-reviewer-ng-cause-analysis/78-05-SUMMARY.md` 작성: base-78-05 해시, 커밋 2개, probe xlsx 계열 출력 원문, 엑셀 머리글 20개 실제 문자열, 메시지 문구 const 목록, 규격과 다르게 구현한 점과 이유.
</output>
