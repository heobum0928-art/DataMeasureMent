---
phase: 78-reviewer-ng-cause-analysis
plan: 06
type: execute
wave: 4
depends_on: ["78-03", "78-05"]
files_modified:
  - WPF_Example/VersionDefine.cs
autonomous: true
requirements: [NGA-05]
user_setup: []

must_haves:
  truths:
    - "VersionDefine.VERSION 이 1.7.49.0 이고, 1.7.48.0 항목 아래에 Phase 78 변경(NG 원인 추정 패널·NG 누적 엑셀·실제 촬영 사진·진단 값 기록·버튼 2개 삭제)을 쉬운 한국어로 적은 [Version] 항목이 하나 더 쌓인다(기존 항목 삭제 0)"
    - "phase 78 전체 누적 감사: 78-01 시작 커밋 이후 바뀐 코드 파일은 계획한 12개뿐이고, 새 .cs·csproj·MainView.xaml.cs·통계 CSV writer/loader·SkipReason.cs 변경 0, 추가 줄 하드룰 grep 0, 삽입 전용 파일 삭제 줄 0 (NGA-05)"
    - "누적 probe 회귀가 한 번에 통과한다 — 5개 날짜 rows 예외 0, 20260916 R6 36/36·C13_P1 함께 의심 R8, 20260915 C13_P3 R5, 옛 JSON R5·R8 0, synthetic 실패 0, json·image·xlsx 계열 PASS (NGA-01~07 회귀 0)"
    - "[edge NGA-05 concurrency] 검사가 계속 쓰는 중인 20260917 폴더를 포함한 probe 에서 예외 0 (쓰는 도중 cycle.json 은 loadnull 로만 집계)"
  artifacts:
    - path: "WPF_Example/VersionDefine.cs"
      provides: "1.7.49.0 changelog + VERSION/BUILD_DATE"
      contains: "Number = \"1.7.49.0\""
  key_links:
    - from: "VersionDefine.VERSION"
      to: "AssemblyVersion/AssemblyFileVersion/RecipeFileHelper.GetVersion"
      via: "기존 단일 상수 참조(변경 없음)"
      pattern: "public const string VERSION = \"1.7.49.0\";"
---

<objective>
Phase 78 코드 변경을 버전 1.7.49.0 으로 기록하고, 78-01~05 전체를 한 번에 감사한다(변경 파일 범위·하드룰·삭제 범위·probe 회귀).

Purpose: 여러 plan 에 나뉜 변경이 합쳐졌을 때도 검사 판정·CSV·레시피 형식 회귀가 없고 CLAUDE.md 규칙을 지켰음을 배포 전에 증명한다. 배포(Release 빌드, D:\Data 덮어쓰기)는 이 plan 범위가 아니다 — 사용자 승인 후 별도.

Output: VersionDefine.cs 1개 수정, 감사 결과(SUMMARY).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-01-PLAN.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-01-SUMMARY.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-02-SUMMARY.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-03-SUMMARY.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-04-SUMMARY.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-05-SUMMARY.md

<interfaces>
VersionDefine.cs
- `:1-3` 규칙 주석: 버전을 올릴 때 VERSION/BUILD_DATE 를 수정하고 [Version] 항목을 하나 더 쌓는다(기존 항목 삭제 금지)
- `:647-660` 마지막 항목 `Number = "1.7.48.0"`, `Date = "2026-09-15"`, `Change = "..." + "..."` 연결 형식
- `:662` `public const string VERSION = "1.7.48.0";`, `:663` `public const string BUILD_DATE = "2026-09-15";`

Phase 78 계획 변경 파일(12): WPF_Example/UI/ViewModel/CycleResultDto.cs, WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs, WPF_Example/UI/Reviewer/ReviewerWindow.xaml, WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs, WPF_Example/Custom/Export/ExcelExportService.cs, WPF_Example/Custom/Export/ChartImageCapture.cs, WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs, WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs, WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs, WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs, WPF_Example/VersionDefine.cs
</interfaces>
</context>

## Artifacts this phase produces (이 plan 분)

phase 전체 원장은 `78-01-PLAN.md`. 이 plan 이 만드는 것: VersionDefine `[Version(Number = "1.7.49.0", ...)]` 항목, `VERSION = "1.7.49.0"`, `BUILD_DATE` = 실행일.

<tasks>

<task type="auto">
  <name>Task 1: 버전 1.7.49.0 기록 (VersionDefine [Version] 항목 쌓기)</name>
  <precondition>78-01~78-05 SUMMARY 5개가 모두 존재한다(`ls .planning/phases/78-reviewer-ng-cause-analysis/78-0[1-5]-SUMMARY.md | wc -l` = 5)</precondition>
  <files>WPF_Example/VersionDefine.cs</files>
  <read_first>
    - WPF_Example/VersionDefine.cs:1-20, :600-665
    - .planning/phases/78-reviewer-ng-cause-analysis/78-0[1-5]-SUMMARY.md (실제 구현 이름·문구·상수)
    - .planning/phases/78-reviewer-ng-cause-analysis/78-CONTEXT.md (D-78-01~08)
  </read_first>
  <action>
(0) `H=/c/Users/admin/AppData/Local/Temp/p78-probe; git rev-parse HEAD > $H/base-78-06.txt`.
(1) 1.7.48.0 [Version] 항목 닫는 `)]` 다음, 클래스 선언 앞에 같은 형식으로 `[Version(Number = "1.7.49.0", Date = "<실행일 yyyy-MM-dd>", Change = ...)]` 를 추가한다. Change 는 기존 항목처럼 `"..." +` 연결 문자열 여러 줄, 쉬운 한국어로 다음 내용을 SUMMARY 의 실제 이름·숫자로 적는다: ① 결과 리뷰어 NG 원인 추정 — NG 행을 누르면 측정표 아래에 '추정 원인 / 근거 / 확인할 일 (+함께 의심)' 이 보인다, 규칙 R1~R9(사진 없음·기준점 못 찾음·측정 선 못 찾음·설정 오류·기준점 흔들림·보정/티칭 치우침·한 곳만 벗어남·초점 범위 끝·공차 경계 흔들림) 과 대표 선택 순서, 외부 AI 없이 저장된 cycle.json 만으로 판단(D-78-01/03/04/05). ② 'NG 누적 엑셀 저장' — 연 날짜 폴더의 NG 만 ResultSavePath 의 NG_분석_누적.xlsx 한 파일에 20열로 추가, 중복 키(사이클 폴더+Shot+FAI+측정명), 파일이 열려 있으면 안내 후 중단·원본 불변(D-78-02). ③ 리뷰어 사진을 그 검사의 실제 촬영 원본(OriginImageFileName)으로, 없으면 기존 경로(D-78-07). ④ cycle.json 에 기록만 추가 — Datum 진단 값(DatumDiagnostics), Z 후보별 선명도(ZCandidateScores), 검사 당시 Z 범위(ZRangeStartIndex/EndIndex). 검사 판정·PLC 응답·통계 CSV·레시피 형식 불변, 옛 cycle.json 호환(D-78-08). ⑤ 리뷰어 '엑셀 export'(사이클 1건)·'차트 이미지 캡처 점검' 버튼 삭제, 반복검사 묶음·Align 정합 조회 유지(D-78-06). 문자열 안에 물음표 문자를 쓰지 않는다. 날짜·이니셜 서명 주석을 새로 달지 않는다.
(2) `VERSION` 상수를 `"1.7.49.0"`, `BUILD_DATE` 를 같은 실행일로 바꾼다(이 두 줄만 수정).
(3) Debug|x64 빌드. 커밋: `git add WPF_Example/VersionDefine.cs` 만. 메시지 예: chore(78-06): 버전 1.7.49.0 — 리뷰어 NG 원인 분석.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p78-probe; PB=$(cat $H/base-78-06.txt); VD=WPF_Example/VersionDefine.cs
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-78-06a.log 2>&1; echo "msbuild_exit=$?"
echo "number=$(grep -c 'Number = \"1.7.49.0\"' $VD) version=$(grep -c 'public const string VERSION = \"1.7.49.0\";' $VD) prev_kept=$(grep -c 'Number = \"1.7.48.0\"' $VD) order=$(A=$(grep -n 'Number = \"1.7.48.0\"' $VD | cut -d: -f1); B=$(grep -n 'Number = \"1.7.49.0\"' $VD | cut -d: -f1); if [ "$A" -lt "$B" ]; then echo 1; else echo 0; fi)"
echo "content=$(tr -d '\r' < $VD | awk '/Number = \"1.7.49.0\"/,/\)\]/' | grep -cE 'NG_분석_누적.xlsx|OriginImageFileName|DatumDiagnostics|ZCandidateScores|추정 원인')"
ADD=$(git diff -w -U0 $PB -- $VD | grep -E '^\+' | grep -vE '^\+\+\+'); echo "deleted=$(git diff -w -U0 $PB -- $VD | grep -E '^-' | grep -vE '^---' | wc -l) qmark=$(printf '%s\n' "$ADD" | grep -cF '?') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk')"
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`
    - `number=1 version=1 prev_kept=1 order=1`
    - `content` ≥ 5 (다섯 가지 변경 키워드가 changelog 에 있음)
    - `deleted=2` (VERSION·BUILD_DATE 두 줄만), `qmark=0 datesig=0`
    - `commit_files` = `WPF_Example/VersionDefine.cs`
  </acceptance_criteria>
  <done>버전 1.7.49.0 과 Phase 78 changelog 가 기존 항목을 지우지 않고 쌓여 커밋되었다.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 78 누적 회귀 감사 — 변경 범위·하드룰·삭제 범위·probe 전체 재실행</name>
  <files>(코드 변경 없음 — 감사 결과는 78-06-SUMMARY.md 에 기록)</files>
  <read_first>
    - .planning/phases/78-reviewer-ng-cause-analysis/78-01-PLAN.md 'Probe 규격'
    - .planning/phases/78-reviewer-ng-cause-analysis/78-0[1-5]-SUMMARY.md (각 plan 의 probe 출력 기준값)
    - CLAUDE.md 검증용 grep 절
  </read_first>
  <action>
코드를 고치지 않는다. 감사에서 실패가 나오면 고치지 말고 실패 항목·원인 후보를 SUMMARY 에 적고 멈춘다(수정은 별도 gap plan).
(1) BASE = `$H/base-78-01.txt`(78-01 시작 전 HEAD). verify 명령을 그대로 실행한다.
(2) probe 는 새 스크래치 폴더(`$H/audit`)에서 전체 모드를 다시 돈다: rows 5개 날짜, synthetic, json, image, xlsx 3회(20260916 → 20260916 → 20260601) + xlsxequal + xlsxlock + xlsxempty + xlsxpartial.
(3) 결과 전체를 78-06-SUMMARY.md 에 붙인다(Task 1 커밋과 함께 SUMMARY 는 실행 워크플로가 커밋).
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p78-probe; FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug; W=C:/Users/admin/AppData/Local/Temp/p78-probe/audit
BASE=$(cat $H/base-78-01.txt)
EXPECT="WPF_Example/Custom/Export/ChartImageCapture.cs WPF_Example/Custom/Export/ExcelExportService.cs WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs WPF_Example/UI/Reviewer/ReviewerWindow.xaml WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs WPF_Example/UI/ViewModel/CycleResultDto.cs WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs WPF_Example/VersionDefine.cs"
GOT=$(git diff --name-only $BASE HEAD -- WPF_Example | sort | tr '\n' ' ' | sed 's/ $//')
echo "scope_match=$( [ "$GOT" = "$EXPECT" ] && echo 1 || echo 0 )"; echo "got=$GOT"
echo "newcs=$(git diff --name-status $BASE HEAD | grep -cE '^A.*\.cs$') forbidden=$(git diff --name-only $BASE HEAD -- WPF_Example/DatumMeasurement.csproj WPF_Example/UI/ContentItem/MainView.xaml.cs WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs WPF_Example/Custom/Sequence/Inspection/SkipReason.cs WPF_Example/Custom/SystemHandler.cs | wc -l)"
for F in $(echo $EXPECT | tr ' ' '\n' | grep '\.cs$'); do ADD=$(git diff -w -U0 $BASE HEAD -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "$F deleted=$(git diff -w -U0 $BASE HEAD -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') logic3=$(printf '%s\n' "$ADD" | grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)')"; done | tee $H/audit-hardrule.txt
echo "hardrule_violations=$(grep -oE '(ternary|coalesce|nullcond|switchexpr|datesig|logic3)=[1-9][0-9]*' $H/audit-hardrule.txt | wc -l)"
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-78-06b.log 2>&1; echo "msbuild_exit=$?"
rm -rf $W && mkdir -p $W/out $W/xl
(cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config NgCauseProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:NgCauseProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:ClosedXML.dll -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll NgCauseProbe.cs \
 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN rows $W/out D:/Data/Result/20260601 D:/Data/Result/20260811 D:/Data/Result/20260915 D:/Data/Result/20260916 D:/Data/Result/20260917 \
 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN synthetic \
 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN json $W/json D:/Data/Result/20260601/163944_cycle/cycle.json \
 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN image $W D:/Data/Result/20260601 D:/Data/Result/20260916 \
 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN xlsx $W/xl/ng_accum.xlsx D:/Data/Result/20260916 \
 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN xlsx $W/xl/ng_accum.xlsx D:/Data/Result/20260916 \
 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN xlsxequal $W/xl/ng_accum.xlsx D:/Data/Result/20260916 \
 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN xlsx $W/xl/ng_accum.xlsx D:/Data/Result/20260601 \
 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN xlsxlock $W/xl/ng_accum.xlsx D:/Data/Result/20260916 \
 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN xlsxempty $W/xl/empty D:/Data/Result/20260916 \
 && MSYS_NO_PATHCONV=1 ./NgCauseProbe.exe $BIN xlsxpartial $W/xl/partial D:/Data/Result/20260916) > $H/audit-probe.txt 2>&1; echo "probe_exit=$?"
T15=$W/out/20260915.tsv; T16=$W/out/20260916.tsv; T01=$W/out/20260601.tsv; T11=$W/out/20260811.tsv
echo "exceptions_nonzero=$(grep -c 'exceptions=[1-9]' $H/audit-probe.txt) synthetic_fail=$(grep -o 'synthetic_fail=[0-9]*' $H/audit-probe.txt) imgcase_fail=$(grep -o 'imgcase_fail=[0-9]*' $H/audit-probe.txt) fails=$(grep -cE 'FAIL' $H/audit-probe.txt)"
echo "d16_r6=$(awk -F'\t' '$6==1 && $7=="R6"' $T16 | wc -l) n16=$(grep -P '^20260916/164530363_cycle\t[^\t]*\t[^\t]*\tC13_P1\t' $T16 | cut -f7,8 | tr '\t' '|') n15=$(grep -P '^20260915/135946717_cycle\t[^\t]*\t[^\t]*\tC13_P3\t' $T15 | cut -f7) old_r8r5=$(cat $T01 $T11 | awk -F'\t' '$6==1 && ($7 ~ /R8|R5/ || $8 ~ /R8|R5/)' | wc -l)"
grep -E '^(json|datummap|image date|xlsx|xlsxequal|xlsxlock|xlsxempty|xlsxpartial) ' $H/audit-probe.txt
    </automated>
  </verify>
  <acceptance_criteria>
    - `scope_match=1`, `newcs=0`, `forbidden=0`
    - 삽입 전용 파일 `deleted=0`: MeasurementBase.cs, Action_FAIMeasurement.cs, CycleResultSerializer.cs, DatumConfig.cs, InspectionSequence.cs, ReviewMeasurementRow.cs. 나머지 상한: CycleResultDto.cs ≤ 40, ReviewerWindow.xaml.cs ≤ 81, ExcelExportService.cs ≤ 126, ChartImageCapture.cs ≤ 50, VersionDefine.cs = 2
    - `hardrule_violations=0`
    - `msbuild_exit=0`, `probe_exit=0`
    - `exceptions_nonzero=0`, `synthetic_fail=0`, `imgcase_fail=0`, `fails=0`
    - `d16_r6=36`, `n16=R6|R8`, `n15=R5`, `old_r8r5=0`
    - 출력 줄: `json roundtrip=PASS`, `datummap fresh=PASS`, `image date=20260916 ... row_origin=216 ... chosen_missing=0`, xlsx 3줄 `added=36` / `added=0` / `added=140 filerows=176`, `xlsxequal rows=36 mismatch=0 unmatched=0`, `xlsxlock status=FileLocked unchanged=1 templeft=0`, `xlsxempty ... created=0`, `xlsxpartial status=Added ... unreadable=1`
  </acceptance_criteria>
  <done>Phase 78 전체가 계획한 파일 범위 안에서 하드룰 위반 없이 합쳐졌고, 규칙·기록·사진·엑셀 probe 회귀가 한 번에 통과함을 SUMMARY 에 증거로 남겼다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 저장소 코드 → 배포 exe(D:\Data) | Release 빌드는 실행 중인 운영 exe 를 덮어쓴다 |
| 누적 감사 → 사용자 배포 판단 | 감사가 거짓 PASS 면 회귀가 현장에 나간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-23 | Tampering | 배포 exe 덮어쓰기 | high | mitigate | 이 plan 의 빌드는 Debug|x64 만, Release 빌드·D:\Data 복사 없음. 배포는 사용자 승인 후 별도 |
| T-78-24 | Repudiation | 감사 증거 | medium | mitigate | 변경 파일 목록 정확 일치(`scope_match=1`), 파일별 하드룰·삭제 수, probe 전체 출력 원문을 SUMMARY 에 첨부. 실패 시 수정하지 않고 기록 후 중단 |
| T-78-25 | Information Disclosure | changelog 문구 | low | accept | 기능 설명만, 경로는 기존 설정값 이름 수준 |
</threat_model>

<verification>
1. VersionDefine 1.7.49.0 항목 추가·VERSION/BUILD_DATE 갱신, 기존 항목 유지
2. Phase 78 변경 파일 12개 정확 일치, 금지 파일 무변경, 새 .cs 0
3. 11개 .cs 추가 줄 하드룰 grep 0, 삽입 전용 6개 파일 삭제 0
4. probe 전체 재실행 PASS(규칙·기록·사진·엑셀)
</verification>

<success_criteria>
- CONTEXT 성공 기준 4·5(나머지 리뷰어 기능 회귀 0, 옛 cycle.json 호환)의 자동 증거 확보
- 버전 1.7.49.0 기록
</success_criteria>

<output>
완료 후 `.planning/phases/78-reviewer-ng-cause-analysis/78-06-SUMMARY.md` 작성: base-78-01·base-78-06 해시, 버전 커밋 해시, verify 출력 원문 전체(scope, 파일별 하드룰·삭제 수, probe 요약 줄), 실패가 있었다면 항목·원인 후보와 '수정하지 않고 중단' 기록.
</output>
