---
phase: 78-reviewer-ng-cause-analysis
plan: 07
type: execute
wave: 5
depends_on: ["78-06"]
files_modified:
  - .planning/phases/78-reviewer-ng-cause-analysis/78-HUMAN-UAT.md
autonomous: false
requirements: [NGA-01, NGA-02, NGA-03, NGA-04, NGA-05, NGA-06, NGA-07]
user_setup: []

must_haves:
  truths:
    - "78-HUMAN-UAT.md 가 사무실 PC(Debug|x64) 에서 할 수 있는 U-1~U-7 과 자동 사이클이 필요한 U-8 을 목적·준비·단계·기대 결과(실제 화면 문구·파일 이름·JSON 키)·합격 기준·결과 칸으로 담는다"
    - "U-1 은 tracer 경로의 화면 end-to-end 증거다: 리뷰어 → 20260916 → C13_P1 NG 행 클릭 → 측정표 아래 패널에 '추정 원인: 보정값 또는 티칭이 한쪽으로 치우쳐 있습니다' / '근거: …' / '확인할 일: …' / '함께 의심: 초점 범위 부족' 이 보이고 글자가 잘리지 않는다 (NGA-01, NGA-02)"
    - "U-3 은 NG 누적 엑셀을 실제 Excel 로 연다: 버튼 1회 → 파일 생성·36행, 2회 → '새로 추가할 NG 가 없습니다', 파일을 Excel 로 연 채 3회 → '엑셀을 닫고 다시 누르세요' 안내 + 파일 그대로 (NGA-03, D-78-02)"
    - {statement: "[flagged A-78-E1 / edge NGA-02 unclassified] 비전 초보 운영자 기준으로 원인 3줄 문구가 이해되는지 사용자가 판단해 기록한다 — 자동 해소 금지", verification: backstop}
    - {statement: "[edge NGA-04 concurrency] U-7: 반복 검사 진행 중·라이브(또는 SIMUL TCP) 검사가 같은 날짜 폴더에 cycle.json 을 쓰는 중에 NG 누적 저장을 눌러도 앱이 멈추거나 죽지 않고, 읽지 못한 건수 안내 후 다음 실행에서 추가된다", verification: backstop}
    - {statement: "[edge NGA-06 concurrency / A-78-A3] U-2: 방금 끝난 사이클의 행을 바로 눌렀을 때 사진이 원본 또는 폴백으로 뜨고, 예외 창이 뜨면 증상을 기록한다", verification: backstop}
    - {statement: "[NGA-07 runtime] U-8: 자동(또는 SIMUL TCP) 사이클 1회 후 새 cycle.json 에 DatumDiagnostics(패턴 Datum 이면 AlignMatchScore > 0), Z 범위 Shot 이면 ZRangeStartIndex/ZRangeEndIndex 와 측정별 ZCandidateScores 가 있고, 레시피를 저장해도 main.ini 에 LastAlignMatch·LastZCandidateScores 키가 생기지 않는다", verification: backstop}
    - "사용자 승인 없이 이 plan 을 완료로 표시하지 않는다 — 체크포인트는 blocking 이다"
  artifacts:
    - path: ".planning/phases/78-reviewer-ng-cause-analysis/78-HUMAN-UAT.md"
      provides: "Phase 78 사람 확인 절차서"
      contains: "U-8"
  key_links:
    - from: "78-HUMAN-UAT.md U-1"
      to: "78-01 tracer 경로(리뷰어 패널)"
      via: "화면 문구 '추정 원인: ' / 헤더 'NG 원인 분석 (추정)'"
      pattern: "NG 원인 분석 \\(추정\\)"
---

<objective>
Phase 78 의 사람 확인 절차서를 쓰고, 사용자가 사무실에서 U-1~U-7 을 확인한 뒤 승인한다. U-8(실제 자동 사이클의 cycle.json 기록)은 SIMUL TCP 또는 실기에서 확인한다.

Purpose: 이 코드베이스에는 앱을 자동 기동하는 테스트 하네스가 없어, 패널이 실제로 보이는지·문구가 초보에게 읽히는지·Excel 파일 잠금·실제 사이클 기록은 사람이 앱을 돌려 확인해야 한다(A-78-E1 은 자동 해소 금지).

Output: `78-HUMAN-UAT.md` + 사용자 승인 기록.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-CONTEXT.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-01-PLAN.md
@.planning/phases/78-reviewer-ng-cause-analysis/78-06-SUMMARY.md
</context>

## Artifacts this phase produces (이 plan 분)

phase 전체 원장은 `78-01-PLAN.md`. 이 plan 이 만드는 것: `78-HUMAN-UAT.md`(U-1~U-8 절차·기대 결과·합격 기준·결과 칸, 알려진 한계 절).

<tasks>

<task type="auto">
  <name>Task 1: 78-HUMAN-UAT.md 작성 — 리뷰어 원인 패널·실제 사진·NG 누적 엑셀·삭제 회귀·옛 데이터·동시 작업·진단 기록</name>
  <files>.planning/phases/78-reviewer-ng-cause-analysis/78-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/78-reviewer-ng-cause-analysis/78-CONTEXT.md (D-78-01~08, 성공 기준 1~5)
    - .planning/phases/78-reviewer-ng-cause-analysis/78-01-PLAN.md (결정 P-1~P-17, flagged assumptions A-78-E1/A1/A2/A3)
    - .planning/phases/78-reviewer-ng-cause-analysis/78-0[1-6]-SUMMARY.md (실제 문구·버튼 이름·파일 이름·probe 수치)
    - .planning/phases/77-side-z-focus-select/77-HUMAN-UAT.md (문서 형식 — 상태/버전/빌드/실행 환경/공통 준비/항목별 목적·단계·합격 기준·결과)
    - .planning/phases/78-reviewer-ng-cause-analysis/78-VALIDATION.md 'Manual-Only Verifications'
  </read_first>
  <action>
77-HUMAN-UAT.md 형식으로 한국어 절차서를 쓴다. 머리: 상태(사용자 확인 대기), 버전 1.7.49.0, 빌드 = Debug|x64 전용(Release 는 OutputPath D:\Data 라 운영 exe 를 덮어씀 — 배포는 앱 종료 확인 후 사용자 승인으로 별도). 각 항목은 체크박스 제목 + (요구사항·결정 ID) + 목적 / 단계(번호) / 기대 결과(SUMMARY 에 기록된 실제 문구·이름) / 합격 기준 / 결과 칸. 문구·숫자는 SUMMARY 실제 값을 쓴다.

공통 준비: Debug|x64 빌드 명령, 앱 실행 → 메뉴에서 결과 리뷰어 열기. 'NG 누적 엑셀 저장' 은 설정 ResultSavePath(기본 D:\Data\Result) 에 `NG_분석_누적.xlsx` 파일 1개를 만든다 — 시험 후 지워도 되며, 지우면 다음 누름에서 새로 시작한다는 점을 적는다. cycle.json·사진은 리뷰어가 읽기만 한다.

- U-1 (NGA-01/02, D-78-04/05, A-78-E1): 날짜 폴더 20260916 열기 → 첫 NG 행 자동 선택 시 패널 표시 → `SIDE_SHOT_1_C13-14 / C13_P1`(164530363 사이클 줄) 클릭 → 패널 헤더 'NG 원인 분석 (추정)', 4줄(추정 원인 R6 문구, 근거에 최근 N회 평균·기준·허용, 확인할 일, 함께 의심 초점 범위 부족). F9_P1 행은 R6 만. OK 행 클릭 시 'NG 가 아닌 항목' 문구, 행 선택 해제 시 안내 문구. 패널이 Halcon 사진 창을 가리지 않고 글자가 잘리지 않는지. **A-78-E1 판단 칸**: 비전 초보가 읽고 무엇을 확인할지 알 수 있는가(예/아니오 + 고칠 문구).
- U-2 (NGA-06, D-78-07, A-78-A3): 20260916 행 클릭 시 사진이 `D:\Data\Result\Image\260916\...\original\origin_*.jpg`(창 제목·로그 또는 사진 모양으로 확인) 이고 오버레이 선이 사진의 실제 에지 위에 맞는지. 사이클 줄 클릭(전체 보기)도 원본. 20260601(원본 기록 없음) 행은 예전과 같은 사진. 가능하면 SIMUL TCP 사이클 직후 방금 생긴 줄을 바로 눌러 사진이 뜨는지(예외 창이 뜨면 증상 기록).
- U-3 (NGA-03, D-78-02/04): 20260916 열기 → 'NG 누적 엑셀 저장' → 'NG 36건을 추가했습니다' 안내 → Excel 로 열어 시트 'NG 누적' 20열 머리글·36행, 원인 열이 U-1 화면 문구와 같은지 → Excel 닫기 → 다시 누르기 → '새로 추가할 NG 가 없습니다' → 20260915 열고 누르기 → 행이 아래에 추가(1,992건 규모라 몇 초 걸릴 수 있음 — 소요 시간 기록) → Excel 로 파일을 연 채 누르기 → '엑셀을 닫고 다시 누르세요' 안내, Excel 을 닫고 파일 수정 시각이 바뀌지 않았는지 → 날짜 폴더를 안 연 새 리뷰어 창에서 누르기 → '먼저 날짜 폴더 열기' 안내.
- U-4 (NGA-04, D-78-06, 성공 기준 4): 왼쪽 버튼에 '엑셀 export'·'차트 이미지 캡처 점검' 이 없고 순서가 날짜 폴더 열기 → NG 누적 엑셀 저장 → 자재번호 → 이전 결과에 누적 → 이미지 폴더 반복 검사 → 반복도 엑셀 export → CPK 리포트 export → Align 정합 조회 → 중간 단계도 보기. 이미지 폴더 반복 검사 소량(예 3장) → 반복도 엑셀 export·CPK 리포트 export 파일 생성·열기 정상, Align 정합 조회 창 열림, '불량만 보기'·'중간 단계도 보기'·'측정명 표시' 동작 이전과 같음.
- U-5 (NGA-05, 성공 기준 5): 20260601·20260811 열기 — 목록·측정표 정상, NG 행 패널에 R0/R2/R7 등 원인 표시, '초점 범위 부족'·'기준점 흔들림' 은 나오지 않음(옛 파일 데이터 없음). 20260915 의 135946717 사이클 C13_P3 → '기준점 위치가 흔들렸습니다' + 근거 끝 '기준점 기록 없음(이 기능 이전 데이터)'.
- U-6 (NGA-01 표본 확인, A-78-A1/A2): 20260917(또는 최신 날짜)에서 NG 몇 행을 골라 대표 원인이 현장 판단과 맞는지 표로 기록(행, 화면 원인, 사용자 판단, 맞음/틀림). 틀린 경우 임계값(R5 0.15·2.0 / R6 5·3 / R9 10%) 조정 필요 여부 메모 — 이번 승인에서 코드 수정은 하지 않는다.
- U-7 (edge NGA-04/05 concurrency): SIMUL TCP 모의 사이클(77-HUMAN-UAT 공통 준비의 mock 스크립트 형식) 또는 이미지 폴더 반복 검사를 돌리는 동안 같은 날짜 폴더로 리뷰어 열기·NG 누적 저장 → 앱이 멈추거나 죽지 않고, '읽지 못한 검사 결과 N건' 안내가 나오면 끝난 뒤 다시 눌러 추가되는지.
- U-8 (NGA-07, D-78-08 — SIMUL TCP 또는 실기, 사무실에서 가능하면 이번 승인에 포함): 자동 사이클 1회 → 새 cycle.json 을 메모장으로 열어 `DatumDiagnostics`(DatumName, IsDetected, IsDetectedThisTick, OriginRow, AngleDeg, AlignMatchScore — 패턴 Datum 이면 0 보다 큼), Z 범위 Shot(77 의 SIDE_SHOT_1_F9 z4~z6 시험 설정) 이면 `ZRangeStartIndex`/`ZRangeEndIndex` 와 측정별 `ZCandidateScores`(Algorithm 로그 `[ZFocus] 선택` 줄의 후보 점수와 같은 값) 확인. 시험 레시피를 저장한 뒤 main.ini 에 `LastAlignMatch`·`LastZCandidateScores` 문자열이 없는지(메모장 찾기). 판정·PLC 응답·일자별 CSV 열 구성이 1.7.48.0 과 같은지. 운영 레시피는 수정하지 않고 시험 복사본을 쓴다(77-HUMAN-UAT 공통 준비 2번).
- 끝에 '알려진 한계' 절: A-78-E1(문구 가독성은 사용자 판단), A-78-A1(임계값은 09-15~17 SIDE_1 표본 기준), A-78-A2(R5 는 같은 FAI = 같은 기준선 가정), A-78-A3(쓰는 도중 원본 사진), R8 은 범위 끝 쪽만 판정(시작 쪽은 동점 규칙 기본값), 옛 cycle.json 은 R8 을 검사 당시 z 번호로만 판정, 누적 엑셀은 UI 스레드에서 동기 실행(큰 날짜 폴더는 수 초), 10만 행 이상이면 파일 이름을 바꿔 보관 안내.

커밋: `git add .planning/phases/78-reviewer-ng-cause-analysis/78-HUMAN-UAT.md` 만. 메시지 예: docs(78-07): 리뷰어 NG 원인 분석 UAT 절차서.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
U=.planning/phases/78-reviewer-ng-cause-analysis/78-HUMAN-UAT.md
echo "exists=$(test -f $U && echo 1 || echo 0)"
for K in U-1 U-2 U-3 U-4 U-5 U-6 U-7 U-8; do echo "$K=$(grep -c "$K" $U)"; done
echo "e1=$(grep -c 'A-78-E1' $U) a3=$(grep -c 'A-78-A3' $U) header=$(grep -c 'NG 원인 분석 (추정)' $U) file=$(grep -c 'NG_분석_누적.xlsx' $U) locked=$(grep -c '엑셀을 닫고 다시 누르세요' $U) keys=$(grep -cE 'DatumDiagnostics|ZCandidateScores|ZRangeStartIndex' $U) debugonly=$(grep -c 'Debug|x64' $U) version=$(grep -c '1.7.49.0' $U)"
echo "commit_files=$(git show --name-only --format= HEAD | sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `exists=1`, `U-1` ~ `U-8` 각각 ≥ 1
    - `e1` ≥ 1, `a3` ≥ 1, `header` ≥ 1, `file` ≥ 1, `locked` ≥ 1, `keys` ≥ 3, `debugonly` ≥ 1, `version` ≥ 1
    - `commit_files` = `.planning/phases/78-reviewer-ng-cause-analysis/78-HUMAN-UAT.md`
  </acceptance_criteria>
  <done>사무실 U-1~U-7 과 사이클 기록 확인 U-8 절차서가 실제 문구·파일 이름·JSON 키로 작성되어 커밋되었다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 사용자 확인 — 리뷰어 NG 원인 분석 UAT (U-1~U-7 필수, U-8 가능 시)</name>
  <what-built>Phase 78 전체(78-01~06): 리뷰어 NG 행 원인 패널(R1~R9 규칙, 대표 + 함께 의심), NG 누적 엑셀 저장 버튼(NG_분석_누적.xlsx, 중복 제외·잠금 안전), 리뷰어 실제 촬영 원본 사진, cycle.json 진단 값 기록(DatumDiagnostics·ZCandidateScores·Z 범위), 사이클 1건 엑셀·차트 캡처 점검 버튼 삭제, 버전 1.7.49.0, 누적 감사 PASS. 절차서 78-HUMAN-UAT.md.</what-built>
  <how-to-verify>
    1. `.planning/phases/78-reviewer-ng-cause-analysis/78-HUMAN-UAT.md` 를 연다.
    2. Debug|x64 로 빌드한 앱에서 U-1(리뷰어 20260916 C13_P1 원인 패널 — tracer 화면 증거)부터 U-7 까지 수행하고 결과 칸에 PASS/FAIL 과 메모를 적는다.
    3. U-1 의 A-78-E1 판단(비전 초보가 읽고 이해 가능한가, 고칠 문구)을 적는다.
    4. U-8(자동 사이클 cycle.json 진단 값)은 SIMUL TCP 로 가능하면 수행, 어렵다면 '대기' 로 둔다.
    5. 배포(Release 빌드, D:\Data 덮어쓰기)는 이 체크포인트 범위가 아니다 — 앱 종료 확인 후 사용자 승인으로 별도 진행.
  </how-to-verify>
  <resume-signal>"approved" (U-1~U-7 PASS, U-8 PASS 또는 대기) 또는 실패한 U 번호와 증상·고칠 문구를 적어 주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT 절차 → 운영 레시피·배포 exe | 절차 중 운영 파일을 건드리면 현장이 멈춘다 |
| UAT 절차 → 운영 결과 폴더(D:\Data\Result) | NG 누적 엑셀 버튼이 파일 1개를 만든다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-78-26 | Tampering | UAT 중 Release 빌드로 배포 exe 덮어쓰기 | high | mitigate | 절차서 머리·체크포인트 5번에 Debug|x64 전용·배포 범위 밖 명시(`debugonly≥1`) |
| T-78-27 | Tampering | U-8 레시피 저장 시 운영 main.ini 변경 | high | mitigate | 시험 레시피 복사본 사용을 U-8 단계에 명시(77-HUMAN-UAT 공통 준비 2번 참조) |
| T-78-28 | Tampering | 결과 폴더의 NG 누적 파일 생성 | low | accept | 설계된 동작(파일 1개, cycle.json·사진 무변경), 시험 후 삭제 가능하다고 절차서에 명시 |
</threat_model>

<verification>
1. 78-HUMAN-UAT.md 에 U-1~U-8, A-78-E1/A3, 실제 화면 문구·파일 이름·JSON 키, Debug|x64 전용 명시
2. 사용자 체크포인트 승인(또는 실패 항목 보고) 기록
</verification>

<success_criteria>
- CONTEXT 성공 기준 1~5 의 사람 확인(화면 표시·엑셀 누적·실데이터 판정·삭제 회귀·옛 데이터) 승인
- A-78-E1 사용자 판단 기록
</success_criteria>

<output>
완료 후 `.planning/phases/78-reviewer-ng-cause-analysis/78-07-SUMMARY.md` 작성: UAT 문서 커밋 해시, 사용자 응답 원문, U 번호별 결과표, A-78-E1 판단과 고칠 문구, U-8 수행 여부.
</output>
