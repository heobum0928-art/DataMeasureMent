---
phase: 79-side-local-strip-datum
plan: 02
type: execute
wave: 2
depends_on: ["79-01"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
  - WPF_Example/UI/ViewModel/CycleResultDto.cs
  - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs
  - WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs
  - WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs
  - WPF_Example/UI/ViewModel/MeasurementResultRow.cs
  - WPF_Example/UI/ContentItem/MainView.xaml
  - WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml
autonomous: true
requirements: [LSR-04, LSR-05]
user_setup: []

must_haves:
  truths:
    - "[LSR-04, D-79-07] cycle.json 의 측정마다 RefSource 가 기록된다 — 국부를 쓴 측정 'Local', 전환된 측정 'LocalFallback', 옵션 꺼진 측정 null. 새 DTO 를 JSON 으로 썼다가 CycleResultSerializer.Load 로 읽으면 세 값이 그대로 돌아오고, 옛 cycle.json(20260916 164530363, 20260601 163944)은 예외 없이 전부 null 이다 (probe records json)"
    - "[LSR-04, D-79-07] 일자별 CSV 머리글이 '...,검사구분,선택Z,사용기준' 이고 행의 17번째 칸(인덱스 16)이 '국부' / '국부실패→전역' / 빈칸이다. 로더는 COL_REF_SOURCE=16 을 칸이 있을 때만 읽고 COLUMN_COUNT 는 14 그대로다 (probe records csv)"
    - "[edge LSR-04 boundary] MeasurementBase.FormatRefSource: null · 빈 문자열 · 모르는 코드 · 대소문자 다른 'local' → 빈칸, 'Local' → '국부', 'LocalFallback' → '국부실패→전역'. ParseRefSource 는 그 반대이고 빈칸·모르는 글자·앞뒤 공백 붙은 글자는 null. CSV 15칸·16칸(옛) 행은 RefSource null, 17칸 행은 읽는다 (probe records format, csv)"
    - "[edge LSR-04 precision] RefSource 는 코드 문자열이라 반올림·정밀도 손실이 없다. 옵션 켠 측정값도 기존 double 경로(수선 거리 평균 × pixelResolution)를 거쳐 CSV 는 기존 F4, cycle.json 은 double 그대로 기록된다 — 이 plan 은 값 칸 생성 코드를 바꾸지 않는다(CSV writer 삭제 줄 = 머리글 1줄)"
    - "[edge LSR-05 ordering] CSV 열 순서는 기존 16칸 뒤에 사용기준 1칸만 붙는다 — 선택Z(15) → 사용기준(16). 옛 로더는 인덱스로 읽어 새 파일도 그대로 읽힌다"
    - "[LSR-04, D-79-07] 결과 그리드(MainView)와 리뷰어 측정표에 '기준' 열이 선택Z/사용 Z 바로 뒤에 생기고, 두 VM 의 RefSourceText 는 같은 MeasurementBase.FormatRefSource 한 곳에서 만든다 (probe display)"
    - "[LSR-05, D-79-04] 옵션 꺼진 측정은 '기준' 칸 빈칸, cycle.json RefSource null, CSV 는 끝에 빈 칸 1개만 붙고 기존 16칸 글자는 그대로다. 측정값·판정 계산 코드는 이 plan 에서 한 줄도 바뀌지 않는다 (regress_diff=0)"
    - "[edge LSR-04 concurrency] 결과 그리드(UI 스레드)는 LastRefSource 를 읽기만 하고 시퀀스 스레드가 TryExecute 입구에서 1번 대입·ClearResult 에서 null — LastSelectedZIndex 와 같은 방식이라 새 락을 더하지 않는다"
  artifacts:
    - path: "WPF_Example/UI/ViewModel/CycleResultDto.cs"
      provides: "MeasurementResultDto.RefSource"
      contains: "public string RefSource { get; set; }"
    - path: "WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs"
      provides: "REF_SOURCE_LOCAL_TEXT / REF_SOURCE_FALLBACK_TEXT, FormatRefSource, ParseRefSource"
      contains: "public static string FormatRefSource(string szRefSource)"
    - path: "WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs"
      provides: "CSV 끝 열 사용기준 + MapRefSource"
      contains: "선택Z,사용기준"
    - path: "WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs"
      provides: "COL_REF_SOURCE=16 조건부 읽기"
      contains: "private const int COL_REF_SOURCE = 16;"
    - path: "WPF_Example/UI/ViewModel/MeasurementResultRow.cs"
      provides: "결과 그리드 RefSourceText"
      contains: "public string RefSourceText"
    - path: "WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs"
      provides: "리뷰어 RefSourceText"
      contains: "public string RefSourceText { get; set; }"
  key_links:
    - from: "CycleResultSerializer.BuildDto"
      to: "MeasurementResultDto.RefSource"
      via: "SelectedZIndex 대입 바로 다음 1줄"
      pattern: "measDto.RefSource = meas.LastRefSource;"
    - from: "MeasurementHistoryCsvWriter.BuildLine"
      to: "MeasurementBase.FormatRefSource"
      via: "MapRefSource — 선택Z 다음 칸"
      pattern: "fields.Add\\(MapRefSource\\(meas\\)\\);"
    - from: "MeasurementHistoryCsvLoader.BuildMeasFromRow"
      to: "MeasurementBase.ParseRefSource"
      via: "ParseRefSourceColumn — 칸이 있을 때만"
      pattern: "meas.RefSource = ParseRefSourceColumn\\(fields\\);"
    - from: "MainView.xaml / ReviewerWindow.xaml 열 '기준'"
      to: "RefSourceText"
      via: "DataGridTextColumn Binding"
      pattern: "Binding=\"\\{Binding RefSourceText\\}\""
---

<objective>
79-01 이 측정 객체에 남긴 사용 기준(LastRefSource)을 **기록(cycle.json·일자별 CSV)과 화면(결과 그리드·리뷰어 표)** 에 보이게 한다(D-79-07, LSR-04). 옵션 꺼진 측정은 빈칸·null 이라 기존 칸과 값은 그대로다(D-79-04, LSR-05).

Purpose: 나중에 값이 달라 보일 때 "국부로 쟀는지, 국부가 실패해 전역으로 바뀌었는지" 를 기록과 화면에서 바로 알게 한다. Phase 77-03 선택Z 와 같은 방식(DTO 필드 추가, CSV 끝 열, 두 그리드 열)으로 해서 옛 파일 호환을 그대로 지킨다.

Output: 기존 파일 9개에 삽입 위주 변경(CSV 머리글 1줄만 수정). 신규 .cs 없음. LocalRefProbe 에 records·display 모드 추가.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/79-side-local-strip-datum/79-CONTEXT.md
@.planning/phases/79-side-local-strip-datum/79-01-PLAN.md
@.planning/phases/79-side-local-strip-datum/79-01-SUMMARY.md

<interfaces>
계획 시점 라인 번호(79-01 편집 전 기준 — MeasurementBase.cs 는 79-01 삽입만큼 밀려 있으니 심볼로 찾는다).

79-01 이 만든 것(이 plan 이 쓴다): `MeasurementBase.REF_SOURCE_LOCAL`("Local"), `MeasurementBase.REF_SOURCE_FALLBACK`("LocalFallback"), `MeasurementBase.LastRefSource`(public 필드, null = 옵션 꺼짐)

MeasurementBase.cs (Allman): `FormatSelectedZ`(:207-211) / `ParseSelectedZ`(:213-223) — 표시·저장·로드 단일 규칙 선례. 새 메서드는 ParseSelectedZ 다음에 둔다.
CycleResultDto.cs (Allman, namespace ReringProject.UI): `MeasurementResultDto` :190-231 — `:226` SelectedZIndex, `:229-230` ZCandidateScores(마지막 프로퍼티), `:231` 클래스 끝
CycleResultSerializer.cs: BuildDto 안 `:128-140` measDto 이니셜라이저, **`:141` `measDto.SelectedZIndex = meas.LastSelectedZIndex;`**, `:142-145` ZCandidateScores 복사
MeasurementHistoryCsvWriter.cs (Allman): `:24` 선택Z 주석, **`:25` CSV_HEADER**(끝 `,검사구분,선택Z`), `:98-119` BuildLine(`:116` `fields.Add(MapSelectedZ(meas));`), `:131-136` MapSelectedZ — CSV 는 새 파일일 때만 머리글을 쓴다(이미 있는 오늘 파일 머리글은 16칸 그대로 남는다 — 77-03 과 같음)
MeasurementHistoryCsvLoader.cs (Allman): `:122` COLUMN_COUNT = 14(바꾸지 않는다), `:139-142` COL_SELECTED_Z = 15 + 주석, `:371-413` BuildMeasFromRow(**`:411` `meas.SelectedZIndex = ParseSelectedZIndex(fields);`**), `:662-671` ParseSelectedZIndex(칸 있을 때만 읽기 선례), `:458` `public static List<CycleResultDto> QueryCyclesDirectory(string szDir, StatisticsTimeRange range, string szRecipeFilter)`(SystemHandler 없이 호출 가능), `StatisticsTimeRange.FromDates(DateTime, DateTime)`
MeasurementResultRow.cs (Allman): `:65-71` SelectedZText(getter 가 `_measurement.LastSelectedZIndex` 로 계산), `:76-86` Refresh(`:85` `RaisePropertyChanged("SelectedZText");`)
MainView.xaml: `:617-628` 결과 DataGrid 열 — **`:626` `<DataGridTextColumn Header="선택Z" Binding="{Binding SelectedZText}" Width="Auto"/>`**
ReviewMeasurementRow.cs (Allman): **`:36` `public string SelectedZText { get; set; }`**, 3인자 생성자 **`:98` `SelectedZText = MeasurementBase.FormatSelectedZ(m.SelectedZIndex);`**
ReviewerWindow.xaml: 오른쪽 측정표 열 — **`:221` `<DataGridTextColumn Header="사용 Z"  Binding="{Binding SelectedZText}"  Width="55"/>`**
</interfaces>
</context>

## Artifacts this phase produces (이 plan 분)

phase 전체 원장은 `79-01-PLAN.md`. 이 plan 이 만드는 것: `MeasurementBase.REF_SOURCE_LOCAL_TEXT`("국부"), `REF_SOURCE_FALLBACK_TEXT`("국부실패→전역"), `FormatRefSource(string)`, `ParseRefSource(string)`; cycle.json `MeasurementResultDto.RefSource`; CSV 끝 열 머리글 `사용기준`(인덱스 16), `MeasurementHistoryCsvWriter.MapRefSource`, `MeasurementHistoryCsvLoader.COL_REF_SOURCE`(16)·`ParseRefSourceColumn`; `MeasurementResultRow.RefSourceText`, `ReviewMeasurementRow.RefSourceText`; XAML 열 머리 `기준`(MainView.xaml, ReviewerWindow.xaml); LocalRefProbe `records`·`display` 모드.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 기록 — cycle.json RefSource + 일자별 CSV 끝 열 '사용기준' + 표시·파싱 단일 규칙(FormatRefSource/ParseRefSource)</name>
  <precondition>79-01-SUMMARY.md 가 있고 `grep -c 'public string LastRefSource;' WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs` = 1</precondition>
  <reversibility rating="reversible">cycle.json 은 필드 추가뿐이고 CSV 는 끝 열 추가뿐이다. 코드를 되돌려도 옛 로더는 인덱스로 읽어 17칸 파일을 그대로 연다.</reversibility>
  <files>WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs, WPF_Example/UI/ViewModel/CycleResultDto.cs, WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs, WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs, WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs</files>
  <read_first>
    - CLAUDE.md (Code Style — 가독성 하드룰 전체)
    - .planning/phases/79-side-local-strip-datum/79-01-PLAN.md '계획 단계 결정' P-10, P-11 과 'Probe 규격'
    - .planning/phases/79-side-local-strip-datum/79-01-SUMMARY.md (실제 상수 이름·값)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (FormatSelectedZ/ParseSelectedZ 주변, 79-01 이 넣은 REF_SOURCE_* 주변)
    - WPF_Example/UI/ViewModel/CycleResultDto.cs:185-232
    - WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs:115-150, :280-350 (SaveAsync 의 JsonConvert 설정, Load)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs 전체
    - WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs:115-145, :365-415, :455-520, :655-720
  </read_first>
  <behavior>
    - format: FormatRefSource(null) = "", ("") = "", ("Local") = "국부", ("LocalFallback") = "국부실패→전역", ("local") = "", ("XYZ") = ""
    - format: ParseRefSource("국부") = "Local", ("국부실패→전역") = "LocalFallback", ("") = null, (null) = null, (" 국부") = null, ("전역") = null
    - json: 새 DTO(RefSource null / Local / LocalFallback 3측정)를 `JsonConvert.SerializeObject(dto, Formatting.Indented)` 로 쓴 파일을 CycleResultSerializer.Load → 세 값 그대로; 옛 cycle.json 2개 Load → 예외 0, RefSource 전부 null
    - csv: 리플렉션으로 MeasurementHistoryCsvWriter.BuildLine 을 불러 만든 3행은 칸 17개, 마지막 칸 "" / "국부" / "국부실패→전역"; 새 머리글 + 이 3행 + 옛 16칸 행 1개 + 옛 15칸 행 1개로 만든 임시 CSV 를 QueryCyclesDirectory 로 읽으면 RefSource = null, Local, LocalFallback, null, null
  </behavior>
  <action>
(0) 저장소 파일을 고치기 전에 `git rev-parse HEAD > /c/Users/admin/AppData/Local/Temp/p79-probe/base-79-02.txt` (이 plan 의 비교 기준 — 이후 덮어쓰지 않는다).
(1) MeasurementBase.cs: ParseSelectedZ 다음에(Allman) 주석 `// Phase 79 LSR-04: 사용 기준 표시·저장·로드 단일 규칙 — 결과 그리드·리뷰어·CSV 가 여기 한 곳만 쓴다. 옵션 꺼짐(null) = 빈칸(기존 전역 표시 불변, D-79-04)` + `public const string REF_SOURCE_LOCAL_TEXT = "국부";` + `public const string REF_SOURCE_FALLBACK_TEXT = "국부실패→전역";` + `public static string FormatRefSource(string szRefSource)` — string.Equals ordinal 로 REF_SOURCE_LOCAL 이면 LOCAL_TEXT, REF_SOURCE_FALLBACK 이면 FALLBACK_TEXT, 그 밖(null 포함)은 string.Empty (가드 절, if 마다 중괄호) + `public static string ParseRefSource(string szText)` — 비었으면 null, LOCAL_TEXT 와 ordinal 같으면 REF_SOURCE_LOCAL, FALLBACK_TEXT 와 같으면 REF_SOURCE_FALLBACK, 그 밖은 null.
(2) CycleResultDto.cs: MeasurementResultDto 의 ZCandidateScores 프로퍼티(:230) 다음에 XML 요약 `/// <summary>Phase 79 LSR-04(D-79-07): 사용 기준 코드 — "Local" = 핀 옆 띠(국부), "LocalFallback" = 국부 실패로 전역 전환, null = 옵션 꺼짐(전역, 기존과 같음)·옛 JSON.</summary>` + `public string RefSource { get; set; }` (초기값·JsonProperty 어트리뷰트 없음 — 옛 파일 로드 호환).
(3) CycleResultSerializer.cs: `:141` 줄 바로 다음에 `measDto.RefSource = meas.LastRefSource; // Phase 79 LSR-04: 사용 기준(옵션 꺼짐 = null)`.
(4) MeasurementHistoryCsvWriter.cs: `:24` 주석 다음 줄에 `// Phase 79 LSR-04: 사용기준 은 선택Z 뒤 맨 끝(인덱스 16)에만 붙는다 — 로더는 COL_REF_SOURCE(16) 옵션 열로 읽는다` 를 넣고, `:25` CSV_HEADER 문자열 끝 `,선택Z"` 를 `,선택Z,사용기준"` 로 바꾼다(이 파일의 유일한 기존 줄 수정). `:116` 줄 다음에 `fields.Add(MapRefSource(meas));   // Phase 79 LSR-04: 사용기준 — 선택Z 뒤 맨 끝`. MapSelectedZ 다음에 `private static string MapRefSource(MeasurementResultDto meas)` — meas null 이면 string.Empty, 아니면 `MeasurementBase.FormatRefSource(meas.RefSource)` (Esc 불필요 — 고정 문자열 2개와 빈칸뿐, 쉼표·따옴표 없음).
(5) MeasurementHistoryCsvLoader.cs: `:142` COL_SELECTED_Z 다음에 주석 `// Phase 79 LSR-04: 사용기준 컬럼. 이 컬럼 도입 전 파일은 이 인덱스가 없다 — 칸이 있을 때만 읽고 없으면 null. COLUMN_COUNT 는 14 유지.` + `private const int COL_REF_SOURCE = 16;`. `:411` 줄 다음에 `meas.RefSource = ParseRefSourceColumn(fields);`. ParseSelectedZIndex 다음에 `private static string ParseRefSourceColumn(List<string> fields)` — `bool bHasColumn = fields.Count > COL_REF_SOURCE;` 없으면 null, 있으면 `MeasurementBase.ParseRefSource(fields[COL_REF_SOURCE])`.
(6) probe: `LocalRefProbe.cs` 에 `records` 모드(`LocalRefProbe.exe <binDir> records <scratchDir> <oldCycleJson1> <oldCycleJson2>`)를 추가한다 — behavior 의 format(표 6+6개), json(scratchDir/json/cycle.json), csv(scratchDir/csv/<오늘 yyyyMMdd>.csv, BuildLine 은 `typeof(MeasurementHistoryCsvWriter).GetMethod("BuildLine", BindingFlags.NonPublic | BindingFlags.Static)` 로 부르고 szOverall 은 "OK", 조회 범위는 오늘 하루, 레시피 필터는 로더 코드를 읽고 '전체' 에 해당하는 값). 판정 줄 `case=<이름> ... PASS|FAIL`, 끝 줄 `records_fail=<n>`. 옛 16칸 행은 새 3행 중 첫 행에서 마지막 칸을 뗀 것, 15칸 행은 마지막 두 칸을 뗀 것(시각·측정명은 다르게 바꿔 한 사이클로 묶이지 않게)으로 만든다.
(7) 빌드 → probe 재컴파일 → `records $W/rec D:/Data/Result/20260916/164530363_cycle/cycle.json D:/Data/Result/20260601/163944_cycle/cycle.json` → OffRegressProbe(새 exe) → cmp. 커밋: `git add` 로 이 task 의 5개 경로만. 메시지 예: feat(79-02): 사용 기준 기록 — cycle.json RefSource·CSV 사용기준 열.

CLAUDE.md 하드룰(새 줄 전부 — 위반 = 회귀): 삼항·null 병합·null 조건 연산자 금지(명시적 null 분기), switch 식 금지, C# 7.2, 한 줄 분기도 중괄호, 한 줄 한 문장, 논리 연산자 3개 이상 금지(이름 있는 bool), 헝가리언 접두사, 매직넘버 const, 날짜·이니셜 서명 주석 신규 금지(새 주석은 `// Phase 79 LSR-04: 이유`), 기존 brace 스타일 유지, 새 줄에 물음표 문자 금지, 기존 줄 위반은 복사·수정하지 않는다, 새 .cs 금지, D:/Data 는 읽기만, Release 빌드 금지. 판정·값 계산 코드와 COLUMN_COUNT 는 건드리지 않는다.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p79-probe; W=C:/Users/admin/AppData/Local/Temp/p79-probe; FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug
PB=$(cat $H/base-79-02.txt)
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-79-02a.log 2>&1; echo "msbuild_exit=$?"
rm -rf $H/rec && mkdir -p $H/rec
(cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config LocalRefProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:LocalRefProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll LocalRefProbe.cs && MSYS_NO_PATHCONV=1 ./LocalRefProbe.exe $BIN records $W/rec D:/Data/Result/20260916/164530363_cycle/cycle.json D:/Data/Result/20260601/163944_cycle/cycle.json && MSYS_NO_PATHCONV=1 ./LocalRefProbe.exe $BIN synthetic) | tee $H/probe-79-02a.txt
(cd $H && MSYS_NO_PATHCONV=1 ./OffRegressProbe.exe $BIN $BIN $W/main-snapshot.ini $W/regress-new-02a.txt > /dev/null 2>&1); echo "regress_diff=$(cmp -s $H/regress-base.txt $H/regress-new-02a.txt && echo 0 || echo 1)"
M=WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs; D=WPF_Example/UI/ViewModel/CycleResultDto.cs; SZ=WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs; CW=WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs; CL=WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs
echo "fmt=$(grep -cE 'public static string FormatRefSource\(string szRefSource\)|public static string ParseRefSource\(string szText\)|REF_SOURCE_LOCAL_TEXT = \"국부\";|REF_SOURCE_FALLBACK_TEXT = \"국부실패→전역\";' $M) dto=$(grep -c 'public string RefSource { get; set; }' $D) map=$(grep -c 'measDto.RefSource = meas.LastRefSource;' $SZ)"
echo "header=$(grep -c ',검사구분,선택Z,사용기준\";' $CW) addfield=$(grep -c 'fields.Add(MapRefSource(meas));' $CW) colconst=$(grep -c 'private const int COL_REF_SOURCE = 16;' $CL) colcount14=$(grep -c 'private const int COLUMN_COUNT = 14;' $CL) parse=$(grep -c 'meas.RefSource = ParseRefSourceColumn(fields);' $CL)"
for F in $M $D $SZ $CW $CL; do ADD=$(git diff -w -U0 $PB -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "$F deleted=$(git diff -w -U0 $PB -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') logic3=$(printf '%s\n' "$ADD" | grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)') qmark=$(printf '%s\n' "$ADD" | grep -cF '?')"; done
echo "commit_files=$(git show --name-only --format= HEAD | LC_ALL=C sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`
    - records 출력의 format 12사례·json(새 3값 왕복, 옛 파일 2개 예외 0·RefSource 전부 null)·csv(BuildLine 17칸, 조회 결과 null/Local/LocalFallback/null/null) 사례가 모두 `PASS`, `records_fail=0`; synthetic 여전히 `synthetic_fail=0`
    - `regress_diff=0`
    - `fmt=4 dto=1 map=1`, `header=1 addfield=1 colconst=1 colcount14=1 parse=1`
    - 5개 파일 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 logic3=0 qmark=0`; `MeasurementHistoryCsvWriter.cs deleted=1`(머리글), 나머지 4개 `deleted=0`
    - `commit_files` = 위 5개 경로뿐
  </acceptance_criteria>
  <done>사용 기준이 cycle.json 과 일자별 CSV 끝 열에 기록되고, 옛 cycle.json·옛 CSV 행은 null 로 읽히며, 표시·파싱 규칙이 MeasurementBase 한 곳에 있음을 probe 로 확인해 커밋했다.</done>
</task>

<task type="auto">
  <name>Task 2: 화면 — 결과 그리드·리뷰어 측정표에 '기준' 열(국부 / 국부실패→전역 / 빈칸)</name>
  <files>WPF_Example/UI/ViewModel/MeasurementResultRow.cs, WPF_Example/UI/ContentItem/MainView.xaml, WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs, WPF_Example/UI/Reviewer/ReviewerWindow.xaml</files>
  <read_first>
    - WPF_Example/UI/ViewModel/MeasurementResultRow.cs 전체
    - WPF_Example/UI/ContentItem/MainView.xaml:610-630
    - WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs:1-110
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml:205-225
    - Task 1 결과의 MeasurementBase.FormatRefSource
    - CLAUDE.md 'UI 는 MVVM' (문자열은 VM 에서 만들고 View 는 바인딩만)
  </read_first>
  <action>
MainView.xaml.cs·ReviewerWindow.xaml.cs 는 건드리지 않는다(표시 문자열은 VM, View 는 바인딩만 — CLAUDE.md MVVM).
(1) MeasurementResultRow.cs: SelectedZText 프로퍼티(:65-71) 다음에 주석 `// Phase 79 LSR-04(D-79-07): 사용 기준 표시 — 국부 / 국부실패→전역 / 옵션 꺼짐 빈칸` + `public string RefSourceText` (getter 본문 여러 줄 Allman: `return MeasurementBase.FormatRefSource(_measurement.LastRefSource);`). Refresh() 의 `RaisePropertyChanged("SelectedZText");`(:85) 다음 줄에 `RaisePropertyChanged("RefSourceText");`.
(2) MainView.xaml: `:626` 선택Z 열 줄 바로 다음에 `<DataGridTextColumn Header="기준" Binding="{Binding RefSourceText}" Width="Auto"/>` 1줄만(앞 줄과 같은 들여쓰기, XAML 주석은 달지 않는다 — 열 목록 위에 기존 머리 주석이 있고, 두 열이 바로 붙어 있어야 verify 가 순서를 확인한다).
(3) ReviewMeasurementRow.cs: `:36` SelectedZText 다음에 XML 요약 `/// <summary>사용 기준 표시(Phase 79 LSR-04) — 국부 / 국부실패→전역, 옵션 꺼짐·옛 cycle.json 은 빈칸.</summary>` + `public string RefSourceText { get; set; }`. 3인자 생성자의 `:98` 줄 다음에 `RefSourceText = MeasurementBase.FormatRefSource(m.RefSource);` (5인자 생성자는 3인자를 부르므로 자동 적용).
(4) ReviewerWindow.xaml: `:221` 사용 Z 열 줄 바로 다음에 `<DataGridTextColumn Header="기준"    Binding="{Binding RefSourceText}"   Width="70"/>` 1줄만(주변 열과 같은 정렬 공백 방식, XAML 주석 없음).
(5) probe: `display` 모드(`LocalRefProbe.exe <binDir> display`)를 추가한다 — ReviewMeasurementRow 3인자 생성자에 RefSource null / Local / LocalFallback 인 MeasurementResultDto(나머지 필드 기본, MeasurementName "M")를 넣어 RefSourceText 가 "" / "국부" / "국부실패→전역" 인지, `new MeasurementResultRow(측정, "FAI")` 에서 측정의 LastRefSource 를 같은 세 값으로 바꿔 RefSourceText 를 확인. 끝 줄 `display_fail=<n>`.
(6) 빌드 → probe 재컴파일 → display, records → OffRegressProbe → cmp. 커밋: `git add` 로 이 task 의 4개 경로만. 메시지 예: feat(79-02): 결과 그리드·리뷰어 '기준' 열.

CLAUDE.md 하드룰(새 줄 전부): Task 1 과 동일. XAML 은 파일마다 열 1줄만.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p79-probe; W=C:/Users/admin/AppData/Local/Temp/p79-probe; FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug
PB=$(cat $H/base-79-02.txt)
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-79-02b.log 2>&1; echo "msbuild_exit=$?"
rm -rf $H/rec && mkdir -p $H/rec
(cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config LocalRefProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:LocalRefProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll LocalRefProbe.cs && MSYS_NO_PATHCONV=1 ./LocalRefProbe.exe $BIN display && MSYS_NO_PATHCONV=1 ./LocalRefProbe.exe $BIN records $W/rec D:/Data/Result/20260916/164530363_cycle/cycle.json D:/Data/Result/20260601/163944_cycle/cycle.json) | tee $H/probe-79-02b.txt
(cd $H && MSYS_NO_PATHCONV=1 ./OffRegressProbe.exe $BIN $BIN $W/main-snapshot.ini $W/regress-new-02b.txt > /dev/null 2>&1); echo "regress_diff=$(cmp -s $H/regress-base.txt $H/regress-new-02b.txt && echo 0 || echo 1)"
R=WPF_Example/UI/ViewModel/MeasurementResultRow.cs; X=WPF_Example/UI/ContentItem/MainView.xaml; RV=WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs; RX=WPF_Example/UI/Reviewer/ReviewerWindow.xaml
echo "vm_main=$(grep -c 'return MeasurementBase.FormatRefSource(_measurement.LastRefSource);' $R) raise=$(grep -c 'RaisePropertyChanged(\"RefSourceText\");' $R) vm_review=$(grep -c 'RefSourceText = MeasurementBase.FormatRefSource(m.RefSource);' $RV)"
echo "col_main=$(tr -d '\r' < $X | grep -A1 'Binding=\"{Binding SelectedZText}\"' | grep -c 'Header=\"기준\" Binding=\"{Binding RefSourceText}\"') col_review=$(tr -d '\r' < $RX | grep -A1 'Binding=\"{Binding SelectedZText}\"' | grep -c 'Binding=\"{Binding RefSourceText}\"') codebehind=$(git diff --name-only $PB -- WPF_Example/UI/ContentItem/MainView.xaml.cs WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs | wc -l)"
for F in $R $RV; do ADD=$(git diff -w -U0 $PB -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "$F deleted=$(git diff -w -U0 $PB -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') qmark=$(printf '%s\n' "$ADD" | grep -cF '?')"; done
for F in $X $RX; do ADD=$(git diff -w -U0 $PB -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "$F deleted=$(git diff -w -U0 $PB -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) added=$(printf '%s\n' "$ADD" | grep -c . ) qmark=$(printf '%s\n' "$ADD" | grep -cF '?')"; done
echo "commit_files=$(git show --name-only --format= HEAD | LC_ALL=C sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`
    - display 6사례 `PASS`, `display_fail=0`; records 여전히 `records_fail=0`
    - `regress_diff=0`
    - `vm_main=1 raise=1 vm_review=1`, `col_main=1 col_review=1`, `codebehind=0`
    - `MeasurementResultRow.cs`·`ReviewMeasurementRow.cs`: `deleted=0 ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 qmark=0`
    - `MainView.xaml`·`ReviewerWindow.xaml`: `deleted=0`, `added=1`, `qmark=0`
    - `commit_files` = 위 4개 경로뿐
  </acceptance_criteria>
  <done>결과 그리드와 리뷰어 측정표에 '기준' 열이 VM 바인딩으로 추가되어 국부 / 국부실패→전역 / 빈칸을 보여 주고, code-behind 변경 없이 커밋되었다. 실제 화면은 79-05 U-2·U-4 에서 확인한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 디스크의 cycle.json·CSV(과거·수기 편집 가능) → 리뷰어·통계 로더 | 손댄 파일의 RefSource 칸이 모르는 값일 수 있다 |
| 기록 → 사용자 판단 | 전환된 값이 국부 값처럼 기록되면 자재 편차를 잘못 결론 낸다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-07 | Tampering | 모르는 RefSource 값(수기 편집 cycle.json·CSV) | low | mitigate | FormatRefSource/ParseRefSource 는 모르는 값을 빈칸/null 로(probe format), 역직렬화는 기존 TypeNameHandling.None 경로 그대로 |
| T-79-08 | Repudiation | 전환 값이 국부로 기록됨 | medium | mitigate | 코드가 LastRefSource 를 그대로 옮긴다(79-01 이 전환 시 LocalFallback 보장) — probe json·csv 3값 왕복 |
| T-79-09 | Denial of Service | CSV COLUMN_COUNT 상향으로 옛 파일 전부 손상 행 처리 | high | mitigate | COLUMN_COUNT 14 유지(`colcount14=1`), 새 칸은 칸이 있을 때만 읽기(probe 15·16칸 행) |
| T-79-10 | Tampering | CSV 수식 주입 | low | accept | 새 칸 값은 고정 const 2개와 빈칸뿐(사용자 입력 없음) — canon 은 /gsd-secure-phase |
</threat_model>

<verification>
1. Debug|x64 빌드 에러 0
2. probe records: 표시·파싱 경계 12사례, cycle.json 왕복·옛 파일 null, CSV 17칸·옛 15/16칸 조회
3. probe display: 두 VM RefSourceText 가 같은 규칙
4. regress_diff=0 (값·판정 불변)
5. COLUMN_COUNT 14, CSV writer 삭제 = 머리글 1줄, code-behind 변경 0
</verification>

<success_criteria>
- 사용 기준이 cycle.json·CSV·결과 그리드·리뷰어에 기록·표시됨 (LSR-04, D-79-07)
- 옵션 꺼짐 측정은 빈칸·null 이고 값·판정·기존 칸 불변 (LSR-05, D-79-04)
</success_criteria>

<output>
완료 후 `.planning/phases/79-side-local-strip-datum/79-02-SUMMARY.md` 작성: base-79-02 해시, 커밋 해시 2개, 빌드 결과, 파일별 삭제 수, probe records·display 출력 원문, regress_diff, 새 심볼 목록(79-01 'Artifacts' 표 79-02 행과 대조).
</output>
