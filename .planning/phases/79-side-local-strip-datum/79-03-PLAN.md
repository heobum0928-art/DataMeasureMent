---
phase: 79-side-local-strip-datum
plan: 03
type: execute
wave: 2
depends_on: ["79-01"]
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
autonomous: true
requirements: [LSR-01, LSR-04, LSR-05]
user_setup: []

must_haves:
  truths:
    - "[LSR-01, D-79-03, P-12] 기준 ROI 를 티칭한(LocalRef_Length1·LocalRef_Length2 > 0) EdgeToLineDistance 측정은 캔버스 ROI 목록에 두 번째 ROI 가 생긴다 — Id '<FAI>_<측정>_LocalRef', Name '<측정>_LocalRef', 상자 = LocalRef_Row ± LocalRef_Length1 · LocalRef_Col ± LocalRef_Length2, IsTaught true. 첫 번째(Point ROI) 항목은 기준 ROI 가 없을 때와 비트 동일하다 (probe roidefs_untaught_single, roidefs_taught_two)"
    - "[LSR-01, P-12] 기준 ROI 를 끌기·크기 바꾸기·삭제(Delete)하면 기존 측정 ROI 핸들러(HalconViewer_RoiMoveCompleted / RoiGeometryChanged / RoiDeleteRequested)가 그대로 subKey 'LocalRef' 로 해석해 LocalRef_Row/Col/Length1/Length2 만 바꾸고 Point_* 는 그대로다. subKey 가 없으면(null) 지금처럼 Point_* 만 바뀐다. 삭제는 IsLocalRefEnabled 를 바꾸지 않는다 (probe move_localref, move_point, center_localref, center_point, resize_localref, clear_localref)"
    - "[LSR-01] 마스터 재앵커(TransformMeasurementGeometry)는 티칭된 기준 ROI 의 중심을 Point ROI 와 같은 변환 T 로 옮기고 LocalRef_Phi 에 같은 회전각을 더한다. 미티칭(Length 0) 기준 ROI 는 Row·Col·Phi 가 0 그대로 남아 옛 레시피 값이 바뀌지 않는다 (probe transform_taught, transform_untaught)"
    - "[LSR-05, D-79-04] 기준 ROI 를 티칭하지 않은 측정(옛 레시피 전부)은 ROI 목록(Id·Name·좌표)·이동·중심 읽기·크기 바꾸기·삭제·재앵커 결과가 79-03 편집 전 exe 와 바이트 단위로 같다 — main.ini 스냅샷 EdgeToLineDistance 96개 + DualImage·ArcLineIntersect·EdgeToLineAngle·CompoundShortAxis 합성 4개 (roi_regress_diff=0)"
    - "[LSR-05] MainView.xaml.cs 변경은 ROI 배선 7함수(BuildPointRoiDefinitions, TryResolvePointRoiTarget, ApplyPointRoiMoveDelta, TryGetPointRoiCenter, ApplyPointRoiResize, ApplyPointRoiClear, TransformMeasurementGeometry) 안 삽입뿐이다 — 7함수 밖 줄과 CommitRectRoi 는 편집 전과 같고 삭제 줄 0 (outside_same=1, commit_same=1, deleted=0)"
    - "[LSR-04, D-79-07] 화면 오버레이 FAI-RefLine(79-01 이 국부를 쓴 측정에만 만든다)은 주황 2px 로 그려진다 — HalconDisplayService.Render 가 그린 픽셀 색이 같은 버퍼 창에 SetColor('orange') 로 그린 기준 픽셀과 같고, 모르는 RoiId 의 기본 색(파랑)과 다르다. FAI-DistLine 은 여전히 청록이다 (probe overlaycolor)"
    - "[edge LSR-05 adjacency] 같은 FAI 안에서 기준 ROI 를 가진 측정과 없는 측정이 섞여도, 없는 측정의 ROI 항목·핸들러 동작은 바뀌지 않는다 — RoiId 는 측정 이름까지 포함한 전체 문자열 비교라 다른 측정으로 해석되지 않는다"
    - "[P-12, A-79-A5] CommitRectRoi(새 ROI 그리기 마법사)는 바꾸지 않는다 — 첫 기준 ROI 는 속성창 Local Ref|ROI 에 숫자를 넣고 측정 노드를 다시 누르면 캔버스에 나타나며, 그 뒤 끌어 맞춘다(79-05 U-1)"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "기준 ROI(RoiId 끝 _LocalRef, subKey LocalRef) 표시·해석·이동·중심·크기·삭제·재앵커 — 7함수 삽입"
      contains: "subKey = EdgeToLineDistanceMeasurement.LOCAL_REF_ROI_SUBKEY;"
    - path: "WPF_Example/Halcon/Display/HalconDisplayService.cs"
      provides: "LOCAL_REF_LINE_COLOR / LOCAL_REF_LINE_WIDTH + FAI-RefLine 색 분기"
      contains: "private const string LOCAL_REF_LINE_COLOR = \"orange\";"
  key_links:
    - from: "MainView.BuildPointRoiDefinitions"
      to: "MainView.TryResolvePointRoiTarget"
      via: "같은 Id 규칙 faiName + _ + measName + _ + LOCAL_REF_ROI_SUBKEY 로 만들고 해석"
      pattern: "EdgeToLineDistanceMeasurement.LOCAL_REF_ROI_SUBKEY"
    - from: "MainView.HalconViewer_RoiMoveCompleted / HalconViewer_RoiGeometryChanged / HalconViewer_RoiDeleteRequested (변경 없음)"
      to: "ApplyPointRoiMoveDelta / ApplyPointRoiResize / ApplyPointRoiClear 의 LocalRef 분기"
      via: "TryResolvePointRoiTarget 가 돌려준 subKey"
      pattern: "etldLocalRef\\.LocalRef_(Row|Col) \\+= delta(Row|Col);"
    - from: "HalconDisplayService.Render 오버레이 색 분기"
      to: "EdgeToLineDistanceMeasurement.LOCAL_REF_OVERLAY_ROI_ID"
      via: "FAI-DistLine 분기 다음 else if"
      pattern: "string.Equals\\(overlay.RoiId, EdgeToLineDistanceMeasurement.LOCAL_REF_OVERLAY_ROI_ID, StringComparison.OrdinalIgnoreCase\\)"
---

<objective>
79-01 이 만든 기준 ROI(LocalRef_*)를 **캔버스에서 보고 끌어 맞출 수 있게** 하고(LSR-01, D-79-03), 국부 기준선 오버레이 FAI-RefLine 을 **주황으로 구분해 보이게** 한다(LSR-04, D-79-07). 기준 ROI 를 티칭하지 않은 측정(옛 레시피 전부)은 ROI 표시·편집 결과가 한 비트도 바뀌지 않음을 편집 전 exe 비교로 증명한다(LSR-05, D-79-04).

Purpose: 기준 ROI 는 기준점 가로 사진(z1) 좌표로 평가되지만 같은 카메라라 측정 사진 위에 그려도 좌표가 같다(RESEARCH O-79-09). 새 UI 없이 기존 측정 ROI 배선(DualImage 의 Point/Line 2-ROI 모양)에 두 번째 ROI 로 끼워 넣어, 운영자가 핀 옆 띠 위치에 상자를 맞추고 결과 화면에서 어느 선을 기준으로 썼는지 바로 보게 한다.

Output: 기존 파일 2개에 삽입만(삭제 0). 신규 .cs 없음. MainView.xaml.cs 는 ROI 배선 7함수 안에만 넣는다(P-12 — CONTEXT 핸드오프 '4함수 최소 확장' 에 이동·크기 쓰기에 필요한 RoiId 해석, 삭제, 마스터 재앵커 3개를 더한 것). 저장소 밖 probe: RoiRegressProbe(새), LocalRefProbe 에 roidefs·overlaycolor 모드 추가.

같은 wave 의 79-02 와 저장소 파일은 겹치지 않지만 빌드 출력 폴더와 probe 소스(LocalRefProbe.cs)를 같이 쓰므로 두 plan 을 동시에 실행하지 않는다(use_worktrees=false 순차 실행).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/79-side-local-strip-datum/79-CONTEXT.md
@.planning/phases/79-side-local-strip-datum/79-01-PLAN.md
@.planning/phases/79-side-local-strip-datum/79-01-SUMMARY.md
@.planning/phases/79-side-local-strip-datum/79-PATTERNS.md

<interfaces>
계획 시점 라인 번호(79-03 편집 전 = 79-01·79-02 는 이 두 파일을 건드리지 않으므로 그대로다). 실행자는 이 계약을 그대로 쓴다.

79-01 이 만든 것(이 plan 이 쓴다): `EdgeToLineDistanceMeasurement.LOCAL_REF_ROI_SUBKEY` = "LocalRef", `EdgeToLineDistanceMeasurement.LOCAL_REF_OVERLAY_ROI_ID` = "FAI-RefLine", 속성 `LocalRef_Row`, `LocalRef_Col`, `LocalRef_Phi`, `LocalRef_Length1`(행 반폭), `LocalRef_Length2`(열 반폭), `IsLocalRefEnabled` (namespace ReringProject.Sequence)

WPF_Example/UI/ContentItem/MainView.xaml.cs (K&R, namespace ReringProject.UI, `public partial class MainView : UserControl`, :71-72 static frozen brush 2개)
- `:428-525` `private static List<RoiDefinition> BuildPointRoiDefinitions(MeasurementBase m, string faiName)` — `:430-431` measName(비면 TypeName), `:476-497` DualImage 2-ROI 선례(Id 끝 "_Point"/"_Line", 조기 return), `:499` 지역변수 pRow·pCol·pLen1·pLen2, **`:500` `var etld = m as EdgeToLineDistanceMeasurement;`**, `:514` `if (pLen1 <= 0 || pLen2 <= 0) return result;`(Point 미티칭이면 빈 목록), `:515-523` Point ROI `result.Add(new RoiDefinition { Id = faiName + "_" + measName, ... });`, **`:524` `return result;`**
- `:841-873` `private bool TryResolvePointRoiTarget(string roiId, out FAIConfig fai, out MeasurementBase meas, out string subKey)` — `:843` GetCurrentShotContext(UI 상태), `:858-862` DualImage 분기(subKey "Point"/"Line"), **`:863` `bool isSinglePointType = m is EdgeToLineDistanceMeasurement || ...`**, `:867-869` 단일 ROI 는 subKey null
- `:876-905` `private void ApplyPointRoiMoveDelta(MeasurementBase meas, string subKey, double deltaRow, double deltaCol)` — `:885-890` DualImage 블록, **`:891` `var etld = meas as EdgeToLineDistanceMeasurement;`**, `:892` Point 이동 후 return
- `:908-939` `private bool TryGetPointRoiCenter(MeasurementBase meas, string subKey, out double row, out double col)` — **`:924` `var etld = ...`**
- `:952-986` `private void ApplyPointRoiResize(MeasurementBase meas, string subKey, double row1, double col1, double row2, double col2)` — `:953-956` cRow·cCol·halfR·halfC, **`:972` `var etld = ...`**
- `:989-1018` `private void ApplyPointRoiClear(MeasurementBase meas, string subKey)` — **`:1004` `var etld = ...`**
- `:1024-1029` `TransformPointInPlace(HTuple T, ref double row, ref double col)`, `:1031-1033` `RotAngleOf(HTuple T)`
- `:1037-1099` `private void TransformMeasurementGeometry(MeasurementBase m, HTuple T)` — `:1039` `double rot = RotAngleOf(T);`, `:1040` `double r, c;`, `:1050-1055` DualImage 블록, **`:1056` `var etld = m as EdgeToLineDistanceMeasurement;`**, `:1057` Point 변환 후 return
- 호출부(변경 금지): `:1783-1841` HalconViewer_RoiGeometryChanged(:1793 Resolve → :1794 Resize), `:1843-1893` HalconViewer_RoiDeleteRequested(:1866 → :1867 Clear), `:1903-1947` HalconViewer_RoiMoveCompleted(:1918 → Edit 모드면 :1927-1942 보정 역변환 → :1943 MoveDelta), `:588-605` TryGetRoiCorrection(Id 가 'FAI_측정_' 로 시작하면 그 측정의 DatumRef 변환 — 기준 ROI 도 자동 포함), `:681` BuildCorrectedResultRoiOverlays(결과 화면 초록 보정 상자 — 기준 ROI 도 자동 포함), `:2791` `private void CommitRectRoi()`(변경 금지), `:4325` 재앵커 미리보기 BuildPointRoiDefinitions(m, "PREVIEW")
- 속성창 숫자 편집 후 캔버스 갱신 = 노드 재클릭(:307 주석 — live INPC 없음)

WPF_Example/Halcon/Display/HalconDisplayService.cs (Allman, namespace ReringProject.Halcon.Display, using ReringProject.Sequence 있음)
- `:18-21` private const 블록(LABEL_ROW_OFFSET, LABEL_COL_OFFSET, LABEL_TEXT_SEPARATOR)
- `:30-37` `public void Render(HWindow window, HImage image, IEnumerable<RoiDefinition> rois, string selectedRoiId, RoiDefinition draftRoi = null, IEnumerable<EdgeInspectionOverlay> inspectionOverlays = null, IEnumerable<string> displayMessages = null)`
- `:258-271` FAI-Edge* 녹/적 분기, **`:272-276` `else if (string.Equals(overlay.RoiId, "FAI-DistLine", StringComparison.OrdinalIgnoreCase)) { window.SetColor("cyan"); window.SetLineWidth(1); }`**, **`:277-281` 그 외 `else { window.SetColor("blue"); window.SetLineWidth(2); }`**, `:283` DispLine, `:289` Points 가 null 이면 continue(EdgeInspectionOverlay.Points 기본값은 빈 목록이라 X 표시 없음)
- 같은 메서드에 이미 "orange" 사용(:175 Cross, :186 ManualMeasure-Line) — HALCON 표준 색 이름

OverlayCaptureRenderer.cs(변경 금지 — A-79-A7): 색 표가 실측 RGB 8색 고정(ResolveColorRgb), FAI-RefLine 은 '그 외' 파랑으로 저장 사진에 그려진다.
</interfaces>
</context>

## Artifacts this phase produces (이 plan 분)

phase 전체 원장은 `79-01-PLAN.md`. 이 plan 이 만드는 것:
- RoiId `<FAI>_<측정>_LocalRef` / Name `<측정>_LocalRef` / subKey `LocalRef`(= `EdgeToLineDistanceMeasurement.LOCAL_REF_ROI_SUBKEY`) — MainView.xaml.cs ROI 배선 7함수 삽입, 지역변수 `etldLocalRef`, `bLocalRefTaught`, `bIsLocalRefRoi`, `bMoveLocalRef`, `bCenterLocalRef`, `bResizeLocalRef`, `bClearLocalRef`
- `HalconDisplayService.LOCAL_REF_LINE_COLOR`("orange"), `HalconDisplayService.LOCAL_REF_LINE_WIDTH`(2), FAI-RefLine 색 분기 1개
- 저장소 밖: `RoiRegressProbe.cs/.exe`, `$H/base-bin-03/DatumMeasurement.exe`, `$H/base-79-03.txt`, `$H/roi-base.txt`, `$H/roi-base2.txt`, LocalRefProbe `roidefs`·`overlaycolor` 모드

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 기준 ROI 캔버스 배선 — ROI 목록·RoiId 해석·이동·중심·크기·삭제·재앵커(7함수 삽입) + 미티칭 측정 비트 동일 증명</name>
  <precondition>79-01-SUMMARY.md 가 있고 `grep -c 'LOCAL_REF_ROI_SUBKEY = "LocalRef";' WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs` = 1, 그리고 Debug 앱이 실행 중이 아니다(exe 잠금이면 프로세스를 죽이지 말고 멈춰서 보고)</precondition>
  <reversibility rating="reversible">UI code-behind 의 삽입뿐이고 새 RoiId 는 화면 안에서만 쓰인다. 레시피에는 79-01 의 LocalRef_* 키만 저장된다.</reversibility>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - CLAUDE.md (Code Style — 가독성 하드룰 전체, 'UI 는 MVVM' 의 "이번에 손대는 지점에만" 규칙)
    - .planning/phases/79-side-local-strip-datum/79-01-PLAN.md '계획 단계 결정' P-12 와 'Probe 규격'
    - .planning/phases/79-side-local-strip-datum/79-01-SUMMARY.md (실제 상수 이름·값, probe 컴파일에 더한 참조)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs:380-530, :580-700, :835-1100, :1780-1950
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs (79-01 결과 — LocalRef 속성·상수)
    - WPF_Example/Halcon/Models/RoiDefinition.cs:15-50
    - C:/Users/admin/AppData/Local/Temp/p79-probe/OffRegressProbe.cs (probe 골격·AssemblyResolve — 그대로 본뜬다)
  </read_first>
  <behavior>
    - roidefs_untaught_single: EdgeToLineDistanceMeasurement(MeasurementName "M", Point_Row 5569.75, Point_Col 12866.41, Point_Length1 48.5, Point_Length2 87, 기준 ROI 0) → BuildPointRoiDefinitions(m, "F") 1개, Id "F_M"
    - roidefs_taught_two: 같은 측정 + LocalRef_Row 6510, LocalRef_Col 12690, LocalRef_Length1 110, LocalRef_Length2 90 → 2개, [0] 은 앞 사례와 비트 동일, [1] Id "F_M_LocalRef", Name "M_LocalRef", Row1 6400, Column1 12600, Row2 6620, Column2 12780, IsTaught true
    - roidefs_point_untaught: Point_Length1 0 + 기준 ROI 티칭 → 0개(Point 미티칭이면 빈 목록 — 기존 :514 가드를 그대로 둔다)
    - move_localref: ApplyPointRoiMoveDelta(m, "LocalRef", 5.0, -3.0) → LocalRef_Row 6515, LocalRef_Col 12687, Point 불변 / move_point: subKey null, (2.0, 1.0) → Point 만 이동
    - center_localref: TryGetPointRoiCenter(m, "LocalRef") → true, (LocalRef_Row, LocalRef_Col) / center_point: subKey null → (Point_Row, Point_Col)
    - resize_localref: ApplyPointRoiResize(m, "LocalRef", 6400, 12600, 6620, 12780) → LocalRef 6510 / 12690 / 110 / 90, Point 불변
    - clear_localref: ApplyPointRoiClear(m, "LocalRef") → LocalRef_Row·Col·Length1·Length2 = 0, IsLocalRefEnabled 그대로, Point 불변
    - transform_taught: T = 단위 → 회전 0.01 rad (중심 5000, 5000) → 이동 (10, -5); TransformMeasurementGeometry(m, T) → LocalRef 중심 = AffineTransPoint2d(T, 옛 중심) 비트 동일, LocalRef_Phi = 옛 Phi + Atan2(-T[1], T[0]), Point 도 같은 규칙
    - transform_untaught: 기준 ROI 0 → 변환 뒤에도 LocalRef_Row·Col·Phi = 0
  </behavior>
  <action>
모든 변경은 기존 줄 사이에 새 줄을 끼워 넣는다(삭제·수정 0). 7함수 밖, 특히 CommitRectRoi 와 이벤트 핸들러는 건드리지 않는다. 파일의 K&R 스타일을 따르되 새 줄은 CLAUDE.md 하드룰(한 줄 한 문장, 중괄호 필수)을 지킨다 — 옆의 기존 한 줄짜리 여러 문장 모양은 복사하지 않는다.

(0) 편집 전 기준 (저장소 파일을 고치기 전): `git rev-parse HEAD > $H/base-79-03.txt`. Debug|x64 빌드 → `mkdir -p $H/base-bin-03 && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe $H/base-bin-03/`. 아래 규격으로 `$H/RoiRegressProbe.cs` 를 쓰고 `$H/base-bin-03/DatumMeasurement.exe` 를 참조해 컴파일(79-01 'Probe 규격' 의 OffRegressProbe 컴파일 줄에서 `-out:RoiRegressProbe.exe`, 참조 exe 경로만 바꾼다. 설정 파일 `RoiRegressProbe.exe.config`). exeDir=`$W/base-bin-03` depDir=BIN 으로 2번 실행해 `$H/roi-base.txt`, `$H/roi-base2.txt`. 두 파일이 다르면 멈추고 보고한다. 이후 RoiRegressProbe 는 다시 컴파일하지 않는다.
  - RoiRegressProbe 규격: 실행 `RoiRegressProbe.exe <exeDir> <depDir> <mainIni> <outFile>`, 골격은 OffRegressProbe 와 같다(STAThread, AssemblyResolve — 이름이 DatumMeasurement 면 exeDir, 그 밖은 depDir, 작업 메서드 NoInlining). **LocalRef 로 시작하는 API·79-01 이 만든 새 멤버는 하나도 쓰지 않는다**(편집 전·후 exe 에 같은 코드로 돌아야 한다). MainView 메서드는 `typeof(ReringProject.UI.MainView).GetMethod(이름, BindingFlags.NonPublic | BindingFlags.Static | BindingFlags.Instance)` 로 찾고, 인스턴스 메서드는 `System.Runtime.Serialization.FormatterServices.GetUninitializedObject(typeof(ReringProject.UI.MainView))` 로 만든 객체에 부른다(생성자·InitializeComponent 를 부르지 않는다. TryResolvePointRoiTarget 은 UI 상태를 읽으므로 부르지 않는다). 변환 T = HomMat2dIdentity → HomMat2dRotate(0.01, 5000, 5000) → HomMat2dTranslate(10, -5).
  - 경우 (가): mainIni 를 IniFile 로 읽어 `TypeName=EdgeToLineDistance` 섹션 전부(섹션 이름 ordinal 정렬)를 새 `EdgeToLineDistanceMeasurement(null)` 에 Load. 연산마다 새로 Load 한 객체를 쓴다: build(BuildPointRoiDefinitions(m, "F") — 개수와 항목마다 Id, Name, Row1, Column1, Row2, Column2, IsTaught), move(subKey null, 3.5, -2.25 → Point_Row, Point_Col), center(subKey null → 반환값, row, col), resize(subKey null, Point_Row-40, Point_Col-60, Point_Row+40, Point_Col+60 → Point_Row·Col·Length1·Length2), clear(subKey null → Point 4값), transform(T → Point_Row·Col·Phi).
  - 경우 (나) 합성 4개(고정 값): DualImageEdgeDistanceMeasurement(PointROI_* · LineROI_* 설정, subKey "Point" 와 "Line" 각각), ArcLineIntersectDistanceMeasurement(EdgeA1·EdgeB1·EdgeA2·EdgeB2 설정, subKey 4개 각각), EdgeToLineAngleMeasurement(Point_*, subKey null), CompoundShortAxisDistanceMeasurement(Rect_*, subKey null) — 같은 6연산, 바뀌는 필드 출력.
  - 출력: 연산마다 1줄 `roi case=<섹션 또는 합성 이름> op=<build|move|center|resize|clear|transform> sub=<subKey 또는 -> <값들>` (double 은 전부 `ToString("R", CultureInfo.InvariantCulture)`), 마지막 줄 `total=<줄 수>`.

(1) BuildPointRoiDefinitions (P-12, D-79-03): `:523` 의 Point ROI `});` 와 `:524` `return result;` 사이에 주석 `// Phase 79 LSR-01: 국부 기준 ROI(핀 옆 띠) — 티칭됐을 때만 두 번째 ROI 로 보여 준다(RoiId 끝 _LocalRef)` 와 블록: `if (etld != null)` 안에서 `bool bLocalRefTaught = etld.LocalRef_Length1 > 0 && etld.LocalRef_Length2 > 0;` → `if (bLocalRefTaught)` 안에서 `result.Add(new RoiDefinition { ... });` — 초기화 항목은 한 줄에 하나: `Id = faiName + "_" + measName + "_" + EdgeToLineDistanceMeasurement.LOCAL_REF_ROI_SUBKEY,` / `Name = measName + "_" + EdgeToLineDistanceMeasurement.LOCAL_REF_ROI_SUBKEY,` / `Row1 = etld.LocalRef_Row - etld.LocalRef_Length1,` / `Column1 = etld.LocalRef_Col - etld.LocalRef_Length2,` / `Row2 = etld.LocalRef_Row + etld.LocalRef_Length1,` / `Column2 = etld.LocalRef_Col + etld.LocalRef_Length2,` / `IsTaught = true`. (기존 `:500` 의 etld 를 그대로 쓴다. Point 미티칭이면 :514 에서 이미 return 하므로 기준 ROI 도 안 보인다 — 동작으로 받아들이고 SUMMARY 에 적는다.)

(2) TryResolvePointRoiTarget: `:863` `bool isSinglePointType` 줄 바로 위에 주석 `// Phase 79 LSR-01: EdgeToLineDistance 의 국부 기준 ROI 는 subKey LocalRef 로 해석한다` + `bool bIsLocalRefRoi = m is EdgeToLineDistanceMeasurement && roiId == f.FAIName + "_" + measName + "_" + EdgeToLineDistanceMeasurement.LOCAL_REF_ROI_SUBKEY;` + `if (bIsLocalRefRoi)` 블록 안에 `fai = f;` `meas = m;` `subKey = EdgeToLineDistanceMeasurement.LOCAL_REF_ROI_SUBKEY;` `return true;` (각 한 줄).

(3) ApplyPointRoiMoveDelta: `:891` `var etld = ...` 줄 바로 위에 주석 `// Phase 79 LSR-01: 국부 기준 ROI 이동 — Point ROI 는 아래 기존 줄이 그대로 처리한다` + `var etldLocalRef = meas as EdgeToLineDistanceMeasurement;` + `bool bMoveLocalRef = etldLocalRef != null && subKey == EdgeToLineDistanceMeasurement.LOCAL_REF_ROI_SUBKEY;` + `if (bMoveLocalRef)` 블록 안에 `etldLocalRef.LocalRef_Row += deltaRow;` `etldLocalRef.LocalRef_Col += deltaCol;` `return;`.

(4) TryGetPointRoiCenter: `:924` 줄 바로 위에 같은 모양(주석 `// Phase 79 LSR-01: 국부 기준 ROI 중심 읽기(Edit 모드 드래그 역보정용)`, 지역변수 `etldLocalRef`, `bool bCenterLocalRef = ...`) — 블록 안 `row = etldLocalRef.LocalRef_Row;` `col = etldLocalRef.LocalRef_Col;` `return true;`.

(5) ApplyPointRoiResize: `:972` 줄 바로 위에 같은 모양(주석 `// Phase 79 LSR-01: 국부 기준 ROI 크기·위치 쓰기`, `bool bResizeLocalRef = ...`) — 블록 안 `etldLocalRef.LocalRef_Row = cRow;` `etldLocalRef.LocalRef_Col = cCol;` `etldLocalRef.LocalRef_Length1 = halfR;` `etldLocalRef.LocalRef_Length2 = halfC;` `return;` (LocalRef_Phi 는 건드리지 않는다 — 기존 Point 분기와 같음).

(6) ApplyPointRoiClear: `:1004` 줄 바로 위에 같은 모양(주석 `// Phase 79 LSR-01: 국부 기준 ROI 삭제(0 리셋) — 옵션 체크는 그대로라 다음 검사에서 '미티칭' 으로 전역 전환된다`, `bool bClearLocalRef = ...`) — 블록 안 `etldLocalRef.LocalRef_Row = 0;` `etldLocalRef.LocalRef_Col = 0;` `etldLocalRef.LocalRef_Length1 = 0;` `etldLocalRef.LocalRef_Length2 = 0;` `return;`.

(7) TransformMeasurementGeometry: `:1056` `var etld = m as ...` 줄 바로 위에 주석 `// Phase 79 LSR-01: 마스터 재앵커 때 티칭된 국부 기준 ROI 도 같이 옮긴다 — 미티칭(0) 값은 그대로 둬 옛 레시피 값이 바뀌지 않게 한다. return 하지 않고 아래 기존 줄이 Point ROI 를 옮긴다` + `var etldLocalRef = m as EdgeToLineDistanceMeasurement;` + `if (etldLocalRef != null)` 안에서 `bool bLocalRefTaught = etldLocalRef.LocalRef_Length1 > 0 && etldLocalRef.LocalRef_Length2 > 0;` → `if (bLocalRefTaught)` 안에서 `r = etldLocalRef.LocalRef_Row;` `c = etldLocalRef.LocalRef_Col;` `TransformPointInPlace(T, ref r, ref c);` `etldLocalRef.LocalRef_Row = r;` `etldLocalRef.LocalRef_Col = c;` `etldLocalRef.LocalRef_Phi = etldLocalRef.LocalRef_Phi + rot;` (기존 :1039 rot, :1040 r·c 를 쓴다).

(8) LocalRefProbe 에 `roidefs` 모드(`LocalRefProbe.exe <binDir> roidefs`)를 더한다 — behavior 의 11사례를 RoiRegressProbe 와 같은 리플렉션 방식(정적 메서드는 인스턴스 없이, 인스턴스 메서드는 GetUninitializedObject 객체)으로 확인. 판정 줄 `case=<이름> ... PASS|FAIL`, 끝 줄 `roidefs_fail=<n>`. 컴파일에 참조가 더 필요하면(예: System.Xaml) 더하고 SUMMARY 에 적는다.

(9) 빌드 → LocalRefProbe 재컴파일 → roidefs, synthetic → RoiRegressProbe(exeDir=BIN) → cmp roi-base → OffRegressProbe(새 exe) → cmp regress-base. 커밋: `git add WPF_Example/UI/ContentItem/MainView.xaml.cs` 만. 메시지 예: feat(79-03): 기준 ROI 캔버스 배선 — 표시·해석·이동·크기·삭제·재앵커.

CLAUDE.md 하드룰(새로 쓰는 모든 줄 — 위반 = 회귀): 삼항 연산자 금지(if/else), null 병합·null 조건 연산자 금지(명시적 null 비교), C# 8 switch 식 금지, C# 7.2 문법만, 한 줄 분기도 중괄호, 한 줄에 한 문장, 논리 연산자 3개 이상을 한 식에 잇지 않고 이름 있는 bool 로 뽑는다, if 중첩 3단계 이상 금지, 헝가리언 접두사 b/n/sz/d/hv, 매직넘버는 이름 있는 const(0 비교·기존 규약 제외), 날짜·이니셜 서명 주석 신규 금지(새 주석은 `// Phase 79 LSR-01: 이유` 형식), 새 HALCON 객체를 만들면 finally 에서 Dispose(이 task 는 새로 만들지 않는다), 수정 파일의 기존 brace 스타일(K&R) 유지. 새 줄(문자열·주석 포함)에 물음표 문자를 쓰지 않는다. 기존 줄의 위반은 복사하지도 고치지도 않는다. MainView.xaml.cs 에 새 메서드·필드를 만들지 않는다(7함수 안 삽입만). 새 .cs 파일 금지. D:/Data 는 읽기만. Release 빌드 금지.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p79-probe; W=C:/Users/admin/AppData/Local/Temp/p79-probe; FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug
PB=$(cat $H/base-79-03.txt); MV=WPF_Example/UI/ContentItem/MainView.xaml.cs
echo "roi_base_repeat_diff=$(cmp -s $H/roi-base.txt $H/roi-base2.txt && echo 0 || echo 1)"
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-79-03a.log 2>&1; echo "msbuild_exit=$?"
(cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config LocalRefProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:LocalRefProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll -r:$FW/System.Xaml.dll LocalRefProbe.cs && MSYS_NO_PATHCONV=1 ./LocalRefProbe.exe $BIN roidefs && MSYS_NO_PATHCONV=1 ./LocalRefProbe.exe $BIN synthetic) | tee $H/probe-79-03a.txt
(cd $H && MSYS_NO_PATHCONV=1 ./RoiRegressProbe.exe $BIN $BIN $W/main-snapshot.ini $W/roi-new-03a.txt > /dev/null 2>&1); echo "roi_regress_diff=$(cmp -s $H/roi-base.txt $H/roi-new-03a.txt && echo 0 || echo 1) roi_total=$(grep -o 'total=[0-9]*' $H/roi-new-03a.txt | cut -d= -f2)"
(cd $H && MSYS_NO_PATHCONV=1 ./OffRegressProbe.exe $BIN $BIN $W/main-snapshot.ini $W/regress-new-03a.txt > /dev/null 2>&1); echo "regress_diff=$(cmp -s $H/regress-base.txt $H/regress-new-03a.txt && echo 0 || echo 1)"
F7='BuildPointRoiDefinitions|TryResolvePointRoiTarget|ApplyPointRoiMoveDelta|TryGetPointRoiCenter|ApplyPointRoiResize|ApplyPointRoiClear|TransformMeasurementGeometry'
RE="^        private (static )?[A-Za-z<>]+ ($F7)[(]"
AWKP='BEGIN{s=0} $0 ~ re {s=1} s==0 {print} s==1 && /^        }$/ {s=0}'
echo "outside_same=$(diff <(git show $PB:$MV | tr -d '\r' | awk -v re="$RE" "$AWKP") <(tr -d '\r' < $MV | awk -v re="$RE" "$AWKP") > /dev/null && echo 1 || echo 0) commit_same=$(diff <(git show $PB:$MV | tr -d '\r' | awk '/private void CommitRectRoi\(\)/,/^        }$/') <(tr -d '\r' < $MV | awk '/private void CommitRectRoi\(\)/,/^        }$/') > /dev/null && echo 1 || echo 0)"
echo "build_id=$(grep -c 'Id = faiName + "_" + measName + "_" + EdgeToLineDistanceMeasurement.LOCAL_REF_ROI_SUBKEY,' $MV) resolve=$(grep -c 'subKey = EdgeToLineDistanceMeasurement.LOCAL_REF_ROI_SUBKEY;' $MV) move=$(grep -cE 'etldLocalRef\.LocalRef_(Row|Col) \+= delta(Row|Col);' $MV) center=$(grep -cE '(row|col) = etldLocalRef\.LocalRef_(Row|Col);' $MV) resize=$(grep -cE 'etldLocalRef\.LocalRef_(Row|Col|Length1|Length2) = (cRow|cCol|halfR|halfC);' $MV) clear=$(grep -cE 'etldLocalRef\.LocalRef_(Row|Col|Length1|Length2) =[ ]0;' $MV) reanchor=$(grep -c 'etldLocalRef.LocalRef_Phi = etldLocalRef.LocalRef_Phi + rot;' $MV)"
ADD=$(git diff -w -U0 $PB -- $MV | grep -E '^\+' | grep -vE '^\+\+\+'); echo "$MV deleted=$(git diff -w -U0 $PB -- $MV | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') logic3=$(printf '%s\n' "$ADD" | grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)') qmark=$(printf '%s\n' "$ADD" | grep -cF '?') newmethod=$(printf '%s\n' "$ADD" | grep -cE '^\+\s*(private|public|protected|internal) ')"
echo "commit_files=$(git show --name-only --format= HEAD | LC_ALL=C sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `roi_base_repeat_diff=0` (편집 전 exe 2회 결과가 같다)
    - `msbuild_exit=0`
    - probe 출력: roidefs 11사례(roidefs_untaught_single, roidefs_taught_two, roidefs_point_untaught, move_localref, move_point, center_localref, center_point, resize_localref, clear_localref, transform_taught, transform_untaught) 모두 `PASS`, `roidefs_fail=0`; synthetic 여전히 `synthetic_fail=0`
    - `roi_regress_diff=0` 이고 `roi_total` 이 roi-base.txt 의 total 과 같다; `regress_diff=0`
    - `outside_same=1`, `commit_same=1`
    - `build_id=1 resolve=1 move=2 center=2 resize=4 clear=4 reanchor=1`
    - MainView.xaml.cs `deleted=0 ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 logic3=0 qmark=0 newmethod=0`
    - `commit_files` = `WPF_Example/UI/ContentItem/MainView.xaml.cs`
  </acceptance_criteria>
  <done>티칭된 기준 ROI 가 캔버스에 두 번째 ROI 로 보이고 기존 핸들러로 끌기·크기·삭제·재앵커가 LocalRef_* 에만 적용되며, 기준 ROI 가 없는 측정은 ROI 동작이 편집 전 exe 와 바이트 동일함을 probe 로 확인해 커밋했다. 실제 화면 조작은 79-05 U-1 에서 확인한다.</done>
</task>

<task type="auto">
  <name>Task 2: 국부 기준선 오버레이 FAI-RefLine 주황 표시 (HalconDisplayService 색 분기) + 픽셀 확인</name>
  <files>WPF_Example/Halcon/Display/HalconDisplayService.cs</files>
  <read_first>
    - WPF_Example/Halcon/Display/HalconDisplayService.cs:1-60, :145-310, :520-590
    - WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs:163-235 (색 표 — 바꾸지 않는 이유 확인, A-79-A7)
    - WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs:40-65
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs (79-01 의 FAI-RefLine 오버레이 생성 위치·LOCAL_REF_OVERLAY_ROI_ID)
    - CLAUDE.md (Code Style)
  </read_first>
  <action>
(1) const 블록(`:21` LABEL_TEXT_SEPARATOR 다음 줄)에 주석 `// Phase 79 D-79-07: 국부 기준선(FAI-RefLine) 색·두께 — 거리선(청록)·에지선(녹/적)·그 밖의 선(파랑)과 구분한다` + `private const string LOCAL_REF_LINE_COLOR = "orange";` + `private const int LOCAL_REF_LINE_WIDTH = 2;`.
(2) Render 오버레이 색 분기에서 FAI-DistLine 분기 닫는 중괄호(`:276`)와 그 외 `else`(`:277`) 사이에 Allman 으로 `else if (string.Equals(overlay.RoiId, EdgeToLineDistanceMeasurement.LOCAL_REF_OVERLAY_ROI_ID, StringComparison.OrdinalIgnoreCase))` 블록을 끼워 넣는다 — 안에 주석 `// Phase 79 D-79-07: 국부 기준선 — 국부를 쓴 측정에만 생긴다` + `window.SetColor(LOCAL_REF_LINE_COLOR);` + `window.SetLineWidth(LOCAL_REF_LINE_WIDTH);`. 기존 분기 순서·문자열은 바꾸지 않는다(isFaiEdgeLine 은 false 그대로라 X 표시 색 분기에 영향 없음).
(3) OverlayCaptureRenderer.cs 는 바꾸지 않는다(A-79-A7 — 저장 사진은 기존 '그 외' 파랑).
(4) LocalRefProbe 에 `overlaycolor` 모드(`LocalRefProbe.exe <binDir> overlaycolor`)를 더한다: 300×300 byte 검정 사진(GenImageConst 후 값 0), HALCON 버퍼 창(`OpenWindow(0, 0, 300, 300, 0, "buffer", "")`, SetPart 0,0,299,299) 에 `new HalconDisplayService().Render(창, 사진, null, null, null, 오버레이 3개, null)` — 오버레이 = FAI-RefLine(행 50, 열 20 에서 280 까지 수평선), FAI-DistLine(행 150), 'Probe-Other'(행 250). DumpWindowImage 뒤 세 선의 (행, 열 150) 픽셀 RGB 를 읽는다. 기준 색은 같은 창을 ClearWindow 한 뒤 SetColor("orange") · SetColor("cyan") · SetColor("blue") 로 같은 위치에 DispLine 해 DumpWindowImage 로 읽은 RGB(숫자를 코드에 적지 않는다). 사례: `overlay_refline_is_orange`(FAI-RefLine RGB = 기준 orange), `overlay_refline_not_blue`(FAI-RefLine RGB ≠ Probe-Other RGB), `overlay_distline_cyan`(FAI-DistLine RGB = 기준 cyan), `overlay_other_blue`(Probe-Other RGB = 기준 blue). 줄마다 RGB 를 같이 찍고 끝 줄 `overlaycolor_fail=<n>`. 사진·창은 끝에서 Dispose. 버퍼 창을 못 열어 예외가 나면 예외 문구를 찍고 FAIL 로 센다(멈추고 보고).
(5) 빌드 → probe 재컴파일 → overlaycolor, roidefs → RoiRegressProbe → OffRegressProbe. 커밋: `git add WPF_Example/Halcon/Display/HalconDisplayService.cs` 만. 메시지 예: feat(79-03): 국부 기준선 오버레이 주황 표시.

CLAUDE.md 하드룰(새 줄 전부): Task 1 과 동일(삼항·null 병합·null 조건 연산자 금지, switch 식 금지, 중괄호 필수, 한 줄 한 문장, 헝가리언, 매직넘버는 const, 날짜·이니셜 서명 주석 금지, 물음표 문자 금지, 기존 Allman 스타일 유지, 새 .cs 금지).
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p79-probe; W=C:/Users/admin/AppData/Local/Temp/p79-probe; FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug
PB=$(cat $H/base-79-03.txt); HD=WPF_Example/Halcon/Display/HalconDisplayService.cs
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-79-03b.log 2>&1; echo "msbuild_exit=$?"
(cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config LocalRefProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:LocalRefProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll -r:$FW/System.Xaml.dll LocalRefProbe.cs && MSYS_NO_PATHCONV=1 ./LocalRefProbe.exe $BIN overlaycolor && MSYS_NO_PATHCONV=1 ./LocalRefProbe.exe $BIN roidefs) | tee $H/probe-79-03b.txt
(cd $H && MSYS_NO_PATHCONV=1 ./RoiRegressProbe.exe $BIN $BIN $W/main-snapshot.ini $W/roi-new-03b.txt > /dev/null 2>&1); echo "roi_regress_diff=$(cmp -s $H/roi-base.txt $H/roi-new-03b.txt && echo 0 || echo 1)"
(cd $H && MSYS_NO_PATHCONV=1 ./OffRegressProbe.exe $BIN $BIN $W/main-snapshot.ini $W/regress-new-03b.txt > /dev/null 2>&1); echo "regress_diff=$(cmp -s $H/regress-base.txt $H/regress-new-03b.txt && echo 0 || echo 1)"
echo "color_const=$(grep -c 'private const string LOCAL_REF_LINE_COLOR = "orange";' $HD) width_const=$(grep -c 'private const int LOCAL_REF_LINE_WIDTH = 2;' $HD) branch=$(grep -c 'string.Equals(overlay.RoiId, EdgeToLineDistanceMeasurement.LOCAL_REF_OVERLAY_ROI_ID, StringComparison.OrdinalIgnoreCase)' $HD) uses=$(grep -cE 'window.Set(Color\(LOCAL_REF_LINE_COLOR\)|LineWidth\(LOCAL_REF_LINE_WIDTH\));' $HD) after_distline=$(tr -d '\r' < $HD | grep -A6 'string.Equals(overlay.RoiId, "FAI-DistLine"' | grep -c 'LOCAL_REF_OVERLAY_ROI_ID') capture_changed=$(git diff --name-only $PB -- WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs | wc -l)"
ADD=$(git diff -w -U0 $PB -- $HD | grep -E '^\+' | grep -vE '^\+\+\+'); echo "$HD deleted=$(git diff -w -U0 $PB -- $HD | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') qmark=$(printf '%s\n' "$ADD" | grep -cF '?')"
echo "commit_files=$(git show --name-only --format= HEAD | LC_ALL=C sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`
    - probe 출력: `overlay_refline_is_orange`, `overlay_refline_not_blue`, `overlay_distline_cyan`, `overlay_other_blue` 4줄 `PASS`, `overlaycolor_fail=0`; roidefs 여전히 `roidefs_fail=0`
    - `roi_regress_diff=0`, `regress_diff=0`
    - `color_const=1 width_const=1 branch=1 uses=2 after_distline=1 capture_changed=0`
    - HalconDisplayService.cs `deleted=0 ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 qmark=0`
    - `commit_files` = `WPF_Example/Halcon/Display/HalconDisplayService.cs`
  </acceptance_criteria>
  <done>국부 기준선 오버레이가 화면에서 주황 2px 로 그려짐을 버퍼 창 픽셀로 확인했고, 기존 선 색(청록·파랑)과 옵션 꺼짐 결과는 그대로이며 저장 사진 렌더러는 건드리지 않았다(A-79-A7 은 79-05 U-4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 운영자 마우스 조작(캔버스) → 레시피 측정 객체 | 잘못된 RoiId 해석이 다른 ROI 필드를 덮어쓸 수 있다 |
| 마스터 재앵커(일괄 변환) → 레시피 전체 | 미티칭 0 값까지 옮기면 옛 레시피 INI 값이 조용히 바뀐다 |
| 저장소 코드 → 배포 exe(D:/Data) | Release 빌드는 운영 exe 를 덮어쓴다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-11 | Tampering | TryResolvePointRoiTarget / ApplyPointRoi* 의 LocalRef 분기 | medium | mitigate | RoiId 는 측정 이름까지 포함한 전체 문자열 비교, subKey 가 LocalRef 일 때만 LocalRef_* 를 쓰고 그 밖은 기존 줄 그대로 — probe move_point·center_point(Point 경로 불변), RoiRegressProbe 비트 비교(`roi_regress_diff=0`) |
| T-79-12 | Tampering | TransformMeasurementGeometry 의 미티칭 기준 ROI | medium | mitigate | `bLocalRefTaught` 가드로 Length 0 이면 옮기지 않음 — probe transform_untaught |
| T-79-13 | Spoofing | 오버레이 색 혼동(주황은 라벨·Cross 에도 쓰임) | low | accept | 선 모양(띠 위 수평선)과 위치로 구분되고, 사용 기준은 79-02 의 '기준' 열이 글자로 보여 준다. U-4 에서 사용자 확인 |
| T-79-14 | Denial of Service | Render 새 분기 | low | mitigate | 문자열 비교 1개와 같은 메서드가 이미 쓰는 표준 색 이름 "orange" — probe overlaycolor 로 실제 렌더 확인 |
| T-79-06 | Tampering | Release 빌드로 D:/Data 배포 exe 덮어쓰기 | high | mitigate | 이 plan 의 빌드는 Debug|x64 만, probe 는 D:/Data 를 읽기만(main.ini 는 스냅샷 복사본) |
</threat_model>

<verification>
1. Debug|x64 빌드 에러 0 (Release 금지)
2. probe roidefs 11사례 — 기준 ROI 표시·해석 없는 이동·중심·크기·삭제·재앵커
3. RoiRegressProbe — 기준 ROI 없는 측정 96개 + 합성 4개의 ROI 배선 결과가 편집 전 exe 와 바이트 동일
4. probe overlaycolor — FAI-RefLine 주황, 기존 청록·파랑 불변
5. MainView.xaml.cs 변경이 7함수 안 삽입뿐(outside_same, commit_same, deleted=0, newmethod=0), OverlayCaptureRenderer 무변경
6. OffRegressProbe 여전히 비트 동일(측정값 불변)
</verification>

<success_criteria>
- 운영자가 기준 ROI 를 캔버스에서 보고 끌어 맞출 수 있다 (LSR-01, D-79-03)
- 국부 기준선이 화면에서 색으로 구분된다 (LSR-04, D-79-07)
- 기준 ROI 를 쓰지 않는 측정의 화면 ROI 동작·측정값은 그대로다 (LSR-05, D-79-04)
</success_criteria>

<output>
완료 후 `.planning/phases/79-side-local-strip-datum/79-03-SUMMARY.md` 작성: base-79-03 해시, 커밋 해시 2개, 빌드 결과, 파일별 삭제 수, probe roidefs·overlaycolor 출력 원문(RGB 값 포함), roi_regress_diff·regress_diff, RoiRegressProbe total, probe 컴파일에 더한 참조, Point 미티칭이면 기준 ROI 도 안 보이는 동작 메모, 새 심볼 목록(79-01 'Artifacts' 표의 79-03 행과 대조).
</output>
