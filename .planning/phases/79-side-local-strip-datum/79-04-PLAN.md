---
phase: 79-side-local-strip-datum
plan: 04
type: execute
wave: 3
depends_on: ["79-02", "79-03"]
files_modified:
  - WPF_Example/VersionDefine.cs
autonomous: true
requirements: [LSR-05]
user_setup: []

must_haves:
  truths:
    - "VersionDefine.VERSION 이 1.7.50.0 이고, 1.7.49.0 항목 아래에 Phase 79 변경(EdgeToLineDistance 'Local Ref' 옵션 — 핀 옆 띠 기준, 기준점 가로 사진에서 기준 ROI 를 한 번 찾아 기준선 위치로 사용, 못 찾으면 전역 기준선 자동 전환과 [LocalRef] 로그, 사용 기준 기록 RefSource·사용기준 열·기준 열·주황 선, 옵션 꺼짐·옛 레시피는 그대로)을 쉬운 한국어로 적은 [Version] 항목이 하나 더 쌓인다(기존 항목 삭제 0)"
    - "[LSR-05, D-79-04, D-79-08] phase 79 누적 감사: 79-01 시작 커밋 이후 바뀐 WPF_Example 파일은 계획한 15개뿐이다 — 새 .cs 0, csproj Compile 항목 수 불변, VisionAlgorithmService.cs·DatumFindingService.cs·EDatumAlgorithm.cs·TcpServer·OverlayCaptureRenderer.cs·ReviewerWindow.xaml.cs·ExcelExportService.cs 변경 0 (scope_match=1)"
    - "[LSR-05] 기존 줄 삭제는 계획한 곳뿐이다 — EdgeToLineDistanceMeasurement.cs 12줄(기준선 원점 치환), MeasurementHistoryCsvWriter.cs 1줄(CSV 머리글), VersionDefine.cs 2줄(VERSION·BUILD_DATE), 나머지 12개 파일 0줄. 모든 .cs 추가 줄의 CLAUDE.md 하드룰 grep(삼항·null 병합·null 조건·switch 식·날짜 서명·논리 3개·물음표) 위반 0"
    - "[LSR-05, D-79-04] 누적 비트 비교가 한 번에 통과한다 — OffRegressProbe(79-01 편집 전 exe 기준 측정값·점수·오류·오버레이) regress_diff=0, RoiRegressProbe(79-03 편집 전 exe 기준 ROI 배선) roi_regress_diff=0"
    - "[LSR-01~05] LocalRefProbe 전체 모드(synthetic, real1, ini, records, display, roidefs, overlaycolor)가 새 스크래치 폴더에서 한 번에 통과한다 — 각 *_fail=0, real1 stripL·stripR anyfound=1"
    - "[LSR-05] Debug|x64 전체 다시 빌드(Rebuild)의 경고 목록(줄 번호·프로젝트 꼬리 제거, 정렬·중복 제거)이 79-01 편집 전 기준선보다 늘지 않는다 (warn_added=0) — VALIDATION Wave 0 경고 기준선"
    - "[edge LSR-04 boundary] MeasurementHistoryCsvLoader.COLUMN_COUNT 는 14 그대로이고 사용기준 열은 COL_REF_SOURCE=16 조건부 읽기다"
  artifacts:
    - path: "WPF_Example/VersionDefine.cs"
      provides: "1.7.50.0 changelog + VERSION/BUILD_DATE"
      contains: "Number = \"1.7.50.0\""
  key_links:
    - from: "VersionDefine.VERSION"
      to: "AssemblyVersion/AssemblyFileVersion/RecipeFileHelper.GetVersion"
      via: "기존 단일 상수 참조(변경 없음)"
      pattern: "public const string VERSION = \"1.7.50.0\";"
  prohibitions:
    - statement: "MUST NOT Release 빌드로 D:/Data 배포 exe 를 덮어쓰거나 감사 중 D:/Data 아래 레시피·결과·사진을 쓰지 않는다 — 배포는 앱 종료 확인 뒤 사용자 승인으로 별도 진행한다"
      status: resolved
      verification: judgment
    - statement: "MUST NOT 감사에서 실패가 나오면 숫자를 맞추려고 코드를 고치거나 지우지 않는다 — 실패 항목과 원인 후보를 SUMMARY 에 적고 멈춘다(수정은 별도 gap plan)"
      status: resolved
      verification: judgment
---

<objective>
Phase 79 코드 변경을 버전 1.7.50.0 으로 기록하고, 79-01~03 전체를 한 번에 감사한다(변경 파일 범위·삭제 범위·하드룰·빌드 경고·probe 전체·편집 전 exe 비트 비교).

Purpose: 여러 plan 에 나뉜 변경이 합쳐졌을 때도 옵션 꺼짐·옛 레시피의 측정값·판정·기록·표시가 그대로이고(D-79-04), Datum 알고리즘·Z 선택·프로토콜이 바뀌지 않았으며(D-79-08), CLAUDE.md 규칙을 지켰음을 사용자 확인(79-05) 전에 증명한다. 배포(Release 빌드, D:/Data 덮어쓰기)는 이 plan 범위가 아니다.

Output: VersionDefine.cs 1개 수정, 감사 결과(SUMMARY).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/79-side-local-strip-datum/79-01-PLAN.md
@.planning/phases/79-side-local-strip-datum/79-01-SUMMARY.md
@.planning/phases/79-side-local-strip-datum/79-02-SUMMARY.md
@.planning/phases/79-side-local-strip-datum/79-03-SUMMARY.md

<interfaces>
VersionDefine.cs
- `:1-3` 규칙 주석: 버전을 올릴 때 VERSION/BUILD_DATE 를 수정하고 [Version] 항목을 하나 더 쌓는다(기존 항목 삭제 금지)
- 마지막 항목 `Number = "1.7.49.0"`, `Date = "2026-09-17"`, `Change = "..." + "..."` 연결 형식, 그 뒤 `public static class VersionDefine`
- `public const string VERSION = "1.7.49.0";`, `public const string BUILD_DATE = "2026-09-17";` (그 위 기존 주석 줄은 건드리지 않는다)

Phase 79 계획 변경 파일(15):
WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (79-01)
WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs (79-02)
WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (79-01)
WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (79-01, 79-02)
WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs (79-02)
WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs (79-02)
WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs (79-01)
WPF_Example/Halcon/Display/HalconDisplayService.cs (79-03)
WPF_Example/UI/ContentItem/MainView.xaml (79-02)
WPF_Example/UI/ContentItem/MainView.xaml.cs (79-03)
WPF_Example/UI/Reviewer/ReviewerWindow.xaml (79-02)
WPF_Example/UI/ViewModel/CycleResultDto.cs (79-02)
WPF_Example/UI/ViewModel/MeasurementResultRow.cs (79-02)
WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs (79-02)
WPF_Example/VersionDefine.cs (79-04)

비교 기준(저장소 밖 `$H=/c/Users/admin/AppData/Local/Temp/p79-probe`): `base-79-01.txt`(79-01 시작 전 HEAD), `regress-base.txt`(79-01 편집 전 exe 의 OffRegressProbe 출력), `roi-base.txt`(79-03 편집 전 exe 의 RoiRegressProbe 출력), `build-base.log`(79-01 편집 전 Rebuild 출력 — 경고 기준선 원본)
</interfaces>
</context>

## Artifacts this phase produces (이 plan 분)

phase 전체 원장은 `79-01-PLAN.md`. 이 plan 이 만드는 것: VersionDefine `[Version(Number = "1.7.50.0", ...)]` 항목, `VERSION = "1.7.50.0"`, `BUILD_DATE` = 실행일. 저장소 밖: `$H/base-79-04.txt`, `$H/audit79/`(probe 재실행 산출물), `$H/warn-base-norm.txt`, `$H/warn-head.txt`, `$H/audit79-hardrule.txt`, `$H/audit79-probe.txt`.

<tasks>

<task type="auto">
  <name>Task 1: 버전 1.7.50.0 기록 (VersionDefine [Version] 항목 쌓기)</name>
  <precondition>79-01~79-03 SUMMARY 3개가 모두 있다(`ls .planning/phases/79-side-local-strip-datum/79-0[1-3]-SUMMARY.md | wc -l` = 3)</precondition>
  <files>WPF_Example/VersionDefine.cs</files>
  <read_first>
    - WPF_Example/VersionDefine.cs:1-20 과 파일 끝 60줄
    - .planning/phases/79-side-local-strip-datum/79-0[1-3]-SUMMARY.md (실제 구현 이름·문구·상수)
    - .planning/phases/79-side-local-strip-datum/79-CONTEXT.md (D-79-01~09)
  </read_first>
  <action>
(0) `H=/c/Users/admin/AppData/Local/Temp/p79-probe; git rev-parse HEAD > $H/base-79-04.txt`.
(1) 1.7.49.0 [Version] 항목 닫는 `)]` 다음, `public static class VersionDefine` 앞에 같은 형식으로 `[Version(Number = "1.7.50.0", Date = "<실행일 yyyy-MM-dd>", Change = ...)]` 를 추가한다. Change 는 기존 항목처럼 `"..." +` 연결 문자열 여러 줄, 쉬운 한국어로 다음 내용을 SUMMARY 의 실제 이름·숫자로 적는다: ① EdgeToLineDistance 측정 속성창에 'Local Ref' 탭(국부 기준 사용 체크 IsLocalRefEnabled, 기준 ROI LocalRef_Row/Col/Phi/Length1/Length2, 기준 ROI 전용 에지 설정 7개)을 추가했다 — 기본 꺼짐이라 옛 레시피는 그대로다(D-79-03/04). ② 켜면 그 측정의 기준점이 가로선을 찾은 사진(SIDE 는 기준점 가로 사진 z1)에서 기준 ROI(핀 바로 옆 띠)를 기준점을 찾을 때 한 번 선 피팅해 두고, 측정 때 그 선의 가운데를 기준선 위치로 쓴다(각도는 기존 기준선 그대로). 핀은 지금처럼 측정 사진에서 재고 Z 자동 선택은 바뀌지 않는다(D-79-05/08). SIDE·TOP·BOTTOM 모든 EdgeToLineDistance 측정에서 레시피 설정만으로 켤 수 있다(D-79-09). ③ 기준 ROI 를 못 찾거나(미티칭·에지 없음), 이번 검사에서 계산되지 않았거나, 기준점을 찾은 뒤 설정이 바뀌면 기존 전역 기준선으로 자동 전환하고 Error 로그 '[LocalRef] 전역 기준선으로 전환' 에 원인을 남긴다 — 검사는 멈추지 않는다(D-79-06). ④ 어느 기준을 썼는지 기록·표시한다 — cycle.json RefSource(Local / LocalFallback), 일자별 CSV 끝 열 '사용기준'(국부 / 국부실패→전역), 결과 그리드·리뷰어 '기준' 열, 화면의 주황 국부 기준선(FAI-RefLine). 옵션 꺼진 측정은 빈칸이고 기존 칸·값은 그대로다(D-79-07). ⑤ 캔버스에서 기준 ROI(이름 끝 _LocalRef)를 끌어 옮기고 크기를 바꾸고 지울 수 있다. ⑥ 켜면 0점이 바뀌어 값이 달라지므로 켠 측정은 기준값·공차를 확인해야 한다(코드가 자동으로 바꾸지 않음). 문자열 안에 물음표 문자를 쓰지 않는다. 날짜·이니셜 서명 주석을 새로 달지 않는다.
(2) `VERSION` 상수를 `"1.7.50.0"`, `BUILD_DATE` 를 같은 실행일로 바꾼다(이 두 줄만 수정).
(3) Debug|x64 빌드. 커밋: `git add WPF_Example/VersionDefine.cs` 만. 메시지 예: chore(79-04): 버전 1.7.50.0 — EdgeToLineDistance 핀 옆 띠 기준 옵션.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p79-probe; PB=$(cat $H/base-79-04.txt); VD=WPF_Example/VersionDefine.cs
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-79-04a.log 2>&1; echo "msbuild_exit=$?"
echo "number=$(grep -c 'Number = "1.7.50.0"' $VD) version=$(grep -c 'public const string VERSION = "1.7.50.0";' $VD) prev_kept=$(grep -c 'Number = "1.7.49.0"' $VD) order=$(A=$(grep -n 'Number = "1.7.49.0"' $VD | cut -d: -f1); B=$(grep -n 'Number = "1.7.50.0"' $VD | cut -d: -f1); if [ "$A" -lt "$B" ]; then echo 1; else echo 0; fi)"
BLK=$(tr -d '\r' < $VD | awk '/Number = "1.7.50.0"/,/\)\]/'); for K in 'Local Ref' 'IsLocalRefEnabled' 'RefSource' '사용기준' '\[LocalRef\] 전역 기준선으로 전환' '기준점 가로 사진' '기준값'; do echo "kw[$K]=$(printf '%s\n' "$BLK" | grep -c "$K")"; done
ADD=$(git diff -w -U0 $PB -- $VD | grep -E '^\+' | grep -vE '^\+\+\+'); echo "deleted=$(git diff -w -U0 $PB -- $VD | grep -E '^-' | grep -vE '^---' | wc -l) qmark=$(printf '%s\n' "$ADD" | grep -cF '?') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk')"
echo "commit_files=$(git show --name-only --format= HEAD | LC_ALL=C sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `msbuild_exit=0`
    - `number=1 version=1 prev_kept=1 order=1`
    - 7개 `kw[...]` 가 모두 1 이상 (다섯 가지 변경 + 기준값 안내가 changelog 에 있음)
    - `deleted=2` (VERSION·BUILD_DATE 두 줄만), `qmark=0 datesig=0`
    - `commit_files` = `WPF_Example/VersionDefine.cs`
  </acceptance_criteria>
  <done>버전 1.7.50.0 과 Phase 79 changelog 가 기존 항목을 지우지 않고 쌓여 커밋되었다.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 79 누적 회귀 감사 — 변경 범위·삭제 범위·하드룰·빌드 경고·probe 전체·편집 전 exe 비트 비교</name>
  <files>(코드 변경 없음 — 감사 결과는 79-04-SUMMARY.md 에 기록)</files>
  <read_first>
    - .planning/phases/79-side-local-strip-datum/79-01-PLAN.md 'Probe 규격'
    - .planning/phases/79-side-local-strip-datum/79-0[1-3]-SUMMARY.md (각 plan 의 probe 출력·삭제 수 기준값)
    - CLAUDE.md 검증용 grep 절
  </read_first>
  <action>
코드를 고치지 않는다. 감사에서 실패가 나오면 고치지 말고 실패 항목·원인 후보를 SUMMARY 에 적고 멈춘다(수정은 별도 gap plan).
(1) BASE = `$H/base-79-01.txt`(79-01 시작 전 HEAD). verify 명령을 그대로 실행한다. Debug 앱이 실행 중이면 Rebuild 가 exe 잠금으로 실패한다 — 프로세스를 죽이지 말고 멈춰서 보고한다.
(2) 빌드는 `-t:Rebuild -v:minimal`(ErrorsOnly 없이)로 돌려 경고를 모은다. 경고 비교는 79-01 의 `build-base.log` 와 이번 로그를 **같은 명령**(verify 의 WNORM)으로 정규화해 비교한다.
(3) probe 는 새 스크래치 폴더 `$H/audit79` 에서 전부 다시 돈다: LocalRefProbe(재컴파일) synthetic · real1 · ini · records · display · roidefs · overlaycolor, OffRegressProbe(재컴파일 금지, 새 exe 로 실행 → regress-base 와 cmp), RoiRegressProbe(재컴파일 금지 → roi-base 와 cmp).
(4) 결과 전체를 79-04-SUMMARY.md 에 붙인다(SUMMARY 는 실행 워크플로가 커밋).
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p79-probe; W=C:/Users/admin/AppData/Local/Temp/p79-probe; FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug; A=$H/audit79; AW=$W/audit79
BASE=$(cat $H/base-79-01.txt)
EXPECT=$(printf '%s\n' WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs WPF_Example/Custom/Sequence/Inspection/CycleResultSerializer.cs WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs WPF_Example/Halcon/Display/HalconDisplayService.cs WPF_Example/UI/ContentItem/MainView.xaml WPF_Example/UI/ContentItem/MainView.xaml.cs WPF_Example/UI/Reviewer/ReviewerWindow.xaml WPF_Example/UI/ViewModel/CycleResultDto.cs WPF_Example/UI/ViewModel/MeasurementResultRow.cs WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs WPF_Example/VersionDefine.cs | LC_ALL=C sort | tr '\n' ' ')
GOT=$(git diff --name-only $BASE HEAD -- WPF_Example | LC_ALL=C sort | tr '\n' ' ')
echo "scope_match=$( [ "$GOT" = "$EXPECT" ] && echo 1 || echo 0 )"; echo "got=$GOT"
echo "newcs=$(git diff --name-status $BASE HEAD -- WPF_Example | grep -cE '^A.*\.cs$') compile_same=$( [ "$(git show $BASE:WPF_Example/DatumMeasurement.csproj | grep -c '<Compile Include')" = "$(grep -c '<Compile Include' WPF_Example/DatumMeasurement.csproj)" ] && echo 1 || echo 0 ) colcount14=$(grep -c 'private const int COLUMN_COUNT = 14;' WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs) colref16=$(grep -c 'private const int COL_REF_SOURCE = 16;' WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs)"
for F in $EXPECT; do ADD=$(git diff -w -U0 $BASE HEAD -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "$F deleted=$(git diff -w -U0 $BASE HEAD -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') logic3=$(printf '%s\n' "$ADD" | grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)') qmark=$(printf '%s\n' "$ADD" | grep -cF '?')"; done | tee $H/audit79-hardrule.txt
echo "hardrule_violations=$(grep -oE '(ternary|coalesce|nullcond|switchexpr|datesig|logic3|qmark)=[1-9][0-9]*' $H/audit79-hardrule.txt | wc -l)"
echo "deleted_unexpected=$(grep -vE '(EdgeToLineDistanceMeasurement\.cs deleted=12|MeasurementHistoryCsvWriter\.cs deleted=1|VersionDefine\.cs deleted=2) ' $H/audit79-hardrule.txt | grep -cvE ' deleted=0 ')"
"/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal > $H/build-audit79.log 2>&1; echo "msbuild_exit=$?"
WNORM() { tr -d '\r' < "$1" | grep -E 'warning (CS|MSB|XLS)[0-9]+' | sed -E 's/\([0-9]+,[0-9]+\)//; s/ *\[[^]]*\]$//; s/^ *[0-9]+>//' | LC_ALL=C sort -u; }
WNORM $H/build-base.log > $H/warn-base-norm.txt; WNORM $H/build-audit79.log > $H/warn-head.txt
echo "warn_base=$(wc -l < $H/warn-base-norm.txt) warn_head=$(wc -l < $H/warn-head.txt) warn_added=$(LC_ALL=C comm -13 $H/warn-base-norm.txt $H/warn-head.txt | wc -l)"; LC_ALL=C comm -13 $H/warn-base-norm.txt $H/warn-head.txt | head -20
rm -rf $A && mkdir -p $A/rec
(cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config LocalRefProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:LocalRefProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll -r:$FW/System.Xaml.dll LocalRefProbe.cs \
 && MSYS_NO_PATHCONV=1 ./LocalRefProbe.exe $BIN synthetic \
 && MSYS_NO_PATHCONV=1 ./LocalRefProbe.exe $BIN real1 D:/Data/Result/Image/260917/1738/original/datum_SIDE_1_Side_Datum_1_H_173855718.jpg \
 && MSYS_NO_PATHCONV=1 ./LocalRefProbe.exe $BIN ini $W/main-snapshot.ini \
 && MSYS_NO_PATHCONV=1 ./LocalRefProbe.exe $BIN records $AW/rec D:/Data/Result/20260916/164530363_cycle/cycle.json D:/Data/Result/20260601/163944_cycle/cycle.json \
 && MSYS_NO_PATHCONV=1 ./LocalRefProbe.exe $BIN display \
 && MSYS_NO_PATHCONV=1 ./LocalRefProbe.exe $BIN roidefs \
 && MSYS_NO_PATHCONV=1 ./LocalRefProbe.exe $BIN overlaycolor) > $H/audit79-probe.txt 2>&1; echo "probe_exit=$?"
(cd $H && MSYS_NO_PATHCONV=1 ./OffRegressProbe.exe $BIN $BIN $W/main-snapshot.ini $AW/regress-audit.txt > /dev/null 2>&1); echo "regress_diff=$(cmp -s $H/regress-base.txt $A/regress-audit.txt && echo 0 || echo 1) regress_total=$(grep -o 'total=[0-9]*' $A/regress-audit.txt | cut -d= -f2)"
(cd $H && MSYS_NO_PATHCONV=1 ./RoiRegressProbe.exe $BIN $BIN $W/main-snapshot.ini $AW/roi-audit.txt > /dev/null 2>&1); echo "roi_regress_diff=$(cmp -s $H/roi-base.txt $A/roi-audit.txt && echo 0 || echo 1)"
grep -oE '(synthetic|ini|records|display|roidefs|overlaycolor)_fail=[0-9]+' $H/audit79-probe.txt; grep -E '^real1 stripL' $H/audit79-probe.txt; echo "fails=$(grep -cE 'FAIL' $H/audit79-probe.txt)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `scope_match=1`, `newcs=0`, `compile_same=1`, `colcount14=1`, `colref16=1`
    - `hardrule_violations=0`
    - `deleted_unexpected=0` (EdgeToLineDistanceMeasurement.cs `deleted=12`, MeasurementHistoryCsvWriter.cs `deleted=1`, VersionDefine.cs `deleted=2`, 나머지 12개 파일 `deleted=0`)
    - `msbuild_exit=0`, `warn_added=0` (새 경고가 있으면 목록을 SUMMARY 에 붙이고 멈춘다)
    - `probe_exit=0`, `synthetic_fail=0 ini_fail=0 records_fail=0 display_fail=0 roidefs_fail=0 overlaycolor_fail=0`, `real1 stripL anyfound=1 stripR anyfound=1 exceptions=0`, `fails=0`
    - `regress_diff=0` (regress_total 은 79-01 SUMMARY 의 값과 같음), `roi_regress_diff=0`
  </acceptance_criteria>
  <done>Phase 79 전체가 계획한 15개 파일 안에서 계획한 삭제만으로 합쳐졌고, 하드룰 위반·새 경고가 없으며, 편집 전 exe 대비 측정값·ROI 배선이 바이트 동일하고 probe 전체가 한 번에 통과함을 SUMMARY 에 증거로 남겼다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 저장소 코드 → 배포 exe(D:/Data) | Release 빌드는 실행 중인 운영 exe 를 덮어쓴다 |
| 누적 감사 → 사용자 배포 판단 | 감사가 거짓 PASS 면 회귀가 현장에 나간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-15 | Tampering | 배포 exe 덮어쓰기 | high | mitigate | 이 plan 의 빌드는 Debug|x64(Build·Rebuild)만, Release 빌드·D:/Data 복사 없음. 배포는 사용자 승인 후 별도 |
| T-79-16 | Repudiation | 감사 증거 | medium | mitigate | 변경 파일 목록 정확 일치(`scope_match=1`), 파일별 하드룰·삭제 수, 경고 차이 목록, probe 출력 원문, 편집 전 exe 비트 비교 두 종을 SUMMARY 에 첨부. 실패 시 수정하지 않고 기록 후 중단 |
| T-79-17 | Information Disclosure | changelog 문구 | low | accept | 기능 설명만, 경로는 설정 이름 수준 |
</threat_model>

<verification>
1. VersionDefine 1.7.50.0 항목 추가·VERSION/BUILD_DATE 갱신, 기존 항목 유지
2. Phase 79 변경 파일 15개 정확 일치, 새 .cs 0, csproj Compile 수 불변, COLUMN_COUNT 14
3. .cs 추가 줄 하드룰 grep 0, 삭제는 계획한 3개 파일(12·1·2줄)뿐
4. Rebuild 경고가 79-01 편집 전 기준선보다 늘지 않음
5. LocalRefProbe 전체 모드, OffRegressProbe·RoiRegressProbe 비트 비교 PASS
</verification>

<success_criteria>
- CONTEXT 성공 기준 4(옵션 꺼진 측정·옛 레시피 회귀 0)의 자동 증거 확보 (LSR-05, D-79-04)
- Datum 알고리즘·Z 선택·프로토콜 코드 무변경 증거 (D-79-08)
- 버전 1.7.50.0 기록
</success_criteria>

<output>
완료 후 `.planning/phases/79-side-local-strip-datum/79-04-SUMMARY.md` 작성: base-79-01·base-79-04 해시, 버전 커밋 해시, verify 출력 원문 전체(scope, 파일별 하드룰·삭제 수, 경고 기준·현재·추가 수와 추가 목록, probe 요약 줄, regress·roi 비교), 실패가 있었다면 항목·원인 후보와 '수정하지 않고 중단' 기록.
</output>
