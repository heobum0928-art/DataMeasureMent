---
phase: 79-side-local-strip-datum
plan: 05
type: execute
wave: 4
depends_on: ["79-04"]
files_modified:
  - .planning/phases/79-side-local-strip-datum/79-HUMAN-UAT.md
autonomous: false
requirements: [LSR-06, LSR-01, LSR-02, LSR-03, LSR-04, LSR-05]
user_setup: []

must_haves:
  truths:
    - "[LSR-06, D-79-01, D-79-02] realab: 09-17 자재 A 4사이클·B 3사이클의 C13·C14 6점마다 후보 창 2개(각 창의 최적 에지 조합)로 국부 값을 운영 코드 경로 그대로(ComputeLocalRefLine → InjectedLocalRef → TryExecute → EvaluateJudgement) 구하고, 운영 전역 값(최종 tick cycle.json 의 LastMeasuredValue)과 나란히 자재 평균 A−B(µm)·같은 자재 안 흔들림(µm)을 낸다 (realab cycles=7 A=4 B=3 exceptions=0, realab_meas 12줄)"
    - "[LSR-06, O-79-10] realab 은 창 6개 × 에지 조합 8개마다 찾음 수(/7)·자재별 midRow 흔들림·평균 에지 세기를 내고, P1 stripL·P3 stripR 은 찾음 7/7 인 조합이 있다. 측정별 추천 창은 '찾음 7/7 이고 같은 자재 안 midRow 흔들림이 가장 작은 창(같으면 핀에 가까운 창)' 규칙으로 정하고, A−B 는 참고 값으로만 표에 적는다 — 같은 7사이클로 고르고 평가하는 치우침을 피한다 (realab_pick 6줄)"
    - "[LSR-06, A-79-A2] P2(C13_P2·C14_P2)는 stripM·stripM2 결과로 따로 판정한다 — 찾음 7/7 조합이 없으면 UAT 표에 'P2 는 켜지 않음(창 재조사 필요)' 으로 적고 자동으로 다른 창을 만들지 않는다"
    - "[LSR-01~06] 79-HUMAN-UAT.md 가 U-1~U-8 을 목적·준비·단계·기대 결과(실제 로그 문구·화면 문구·CSV 열 이름·JSON 키)·합격 기준·결과 칸으로 담고, realab 추천 티칭 표(측정별 LocalRef_Row/Col/Phi/Length1/Length2 + 에지 설정 7개)와 전역·국부 A−B 비교 표를 싣는다"
    - {statement: "[flagged A-79-E1 / edge LSR-01 unclassified, A-79-A5] U-1: Local Ref 탭 구성(Option / ROI / Edge)과 '숫자 입력 → 측정 노드 다시 누르기 → 캔버스에서 끌어 맞추기' 티칭 방식이 운영자에게 쓸 만한지 사용자가 판단해 기록한다 — 자동 해소 금지", verification: backstop}
    - {statement: "[flagged A-79-E2 / edge LSR-02 unclassified, D-79-05, tracer 화면 증거] U-2: 실제 SIDE 사이클(SIMUL 또는 장비)에서 C13_P3 만 켜면 Algorithm 로그 '[LocalRef] 기준선 찾음 — ' 이 기준점 검출마다 1번 찍히고, C13_P3 의 '기준' 칸이 '국부', 주황 선이 띠 에지 위에 놓이며, 같은 사이클의 C14_P3 는 '기준' 빈칸·값이 옵션 추가 전과 같다", verification: backstop}
    - {statement: "[flagged A-79-E3 / edge LSR-03 unclassified, D-79-06, A-79-A6] U-3: 기준 ROI 삭제 · 평평한 곳으로 이동 · Test Find 뒤 기준 ROI 변경 3경우에서 Error 로그 '[LocalRef] 전역 기준선으로 전환 — <Shot> · <측정>: <원인>' 과 '국부실패→전역' 표시가 나오고, 값은 옵션을 끈 값과 같으며 검사가 멈추거나 NG 로 바뀌지 않는다. 원인 문구가 원인 찾기에 충분한지 사용자가 판단한다", verification: backstop}
    - {statement: "[D-79-04, D-79-07, A-79-A4, A-79-A7] U-4: 옵션 꺼진 측정의 '기준' 칸을 빈칸으로 둔 표시와 저장 캡처 사진의 파란 국부 기준선(화면은 주황)이 괜찮은지 사용자가 판단해 기록한다", verification: backstop}
    - {statement: "[flagged A-79-E4 / edge LSR-06 unclassified, D-79-01] U-6: 켠 측정의 자재 A−B 편차가 전역 기준보다 뚜렷이 줄었는지(목표: 09-17 분석 수준 수 µm) 사용자가 realab 표와 실제 사이클 결과로 판단하고, 켠 측정마다 새 값 기준으로 기준값·공차를 확인한다 — 자동 해소 금지", verification: backstop}
    - {statement: "[D-79-09, A-79-A3] U-7: 장비 PC 에서 TOP·BOTTOM EdgeToLineDistance 1~2개에 레시피 티칭만으로 옵션을 켜 '국부' 동작과 '국부실패→전역' 전환을 확인한다 — 코드 수정 없이 (사무실 PC 에 TOP·BOTTOM 사진 없음)", verification: backstop}
    - {statement: "[edge LSR-04 concurrency] U-8: SIMUL TCP 반복 검사 도중 Test Find·기준 ROI 끌기를 해도 앱이 멈추거나 죽지 않고, 그 사이클의 전환 로그 원인이 '기준점을 찾은 뒤 기준 ROI 또는 에지 설정이 바뀜' 또는 '이번 사이클에 ... 구하지 않음' 으로 남는다", verification: backstop}
    - "사용자 승인 없이 이 plan 을 완료로 표시하지 않는다 — 체크포인트는 blocking 이다. 실행자는 D:/Data 아래 레시피·사진 폴더를 고치지 않는다(시험 레시피 준비·운영 레시피 적용은 사용자, P-15)"
  artifacts:
    - path: ".planning/phases/79-side-local-strip-datum/79-HUMAN-UAT.md"
      provides: "Phase 79 사람 확인 절차서 + realab 추천 티칭 표 + A−B 비교 표"
      contains: "U-8"
  key_links:
    - from: "79-HUMAN-UAT.md U-2"
      to: "79-01 tracer 경로(기준점 검출 훅 → 주입 → TryExecute)"
      via: "Algorithm 로그 '[LocalRef] 기준선 찾음 — ' 과 결과 그리드 '기준' 열"
      pattern: "\\[LocalRef\\] 기준선 찾음"
    - from: "79-HUMAN-UAT.md 추천 티칭 표"
      to: "realab 출력 realab_pick / realab_best"
      via: "측정별 창의 행·열 범위 → LocalRef_Row/Col/Length1/Length2"
      pattern: "LocalRef_Length1"
  prohibitions:
    - statement: "MUST NOT 실행자가 D:/Data 아래 레시피(main.ini)·결과·사진 폴더를 쓰거나 고치지 않고, Release 빌드로 배포 exe 를 덮어쓰지 않는다 — realab 은 읽기만 하고 결과는 스크래치 폴더와 UAT 문서에만 쓴다"
      status: resolved
      verification: judgment
    - statement: "MUST NOT 코드나 실행자가 옵션을 켠 측정의 기준값(NominalValue)·공차를 자동으로 바꾸지 않는다 — 0점이 바뀌어 값이 달라지는 것은 사용자가 U-6 에서 확인해 정한다 (O-79-05, P-14)"
      status: resolved
      verification: judgment
---

<objective>
Phase 79 의 효과(LSR-06)와 사람이 봐야 하는 동작을 확인한다. 먼저 저장된 09-17 자재 A·B 사진으로 운영 코드 경로를 그대로 돌려 C13·C14 6점의 **국부 기준 A−B 와 전역 기준 A−B 를 나란히** 계산하고 측정별 기준 ROI 추천 값을 뽑는다(realab). 그 표를 담은 UAT 절차서를 쓰고, 사용자가 시험 레시피로 티칭·확인한 뒤 승인한다.

Purpose: 기준 ROI 를 "어느 창" 에 두느냐가 효과를 좌우한다(RESEARCH Pitfall 1 — 가까운 창 −2.7µm, 조금 먼 창 +60µm). 첫 기준 ROI 는 속성창 숫자로 넣으므로(A-79-A5) 숫자 표가 필요하고, 편차가 실제로 줄었는지·기준값을 어떻게 할지는 사용자 판단이다(A-79-E4, O-79-05). 실행자는 D:/Data 를 읽기만 한다.

Output: 저장소 밖 realab 결과(`$H/realab.txt`), `79-HUMAN-UAT.md`(커밋), 사용자 승인 기록.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/79-side-local-strip-datum/79-CONTEXT.md
@.planning/phases/79-side-local-strip-datum/79-RESEARCH.md
@.planning/phases/79-side-local-strip-datum/79-01-PLAN.md
@.planning/phases/79-side-local-strip-datum/79-04-SUMMARY.md

<interfaces>
계획 시점 조사값(D:/Data 읽기 전용 확인, 2026-09-18).

레시피 스냅샷 `$W/main-snapshot.ini`(79-01 이 D:/Data/Recipe/FAI_1/main.ini 를 복사): `[SHOT_5]` ShotName=SIDE_SHOT_1_C13-14, 측정 섹션 `SHOT_5_FAI_0_MEAS_0`~`_5` = C13_P1, C14_P1, C13_P2, C14_P2, C13_P3, C14_P3 (모두 EdgeToLineDistance, MeasureAxis=Y, DatumRef=Side_Datum_1, Sigma 3, EdgeSampleCount 100, EdgeTrimCount 10, DatumOriginRow 6597.30 · DatumOriginCol 1006.91 · DatumAngleRad -0.009116). 핀 열 Point_Col: P1 ≈ 1693, P2 ≈ 7400, P3 ≈ 12870. C13_P1 만 MeasCorrectionEnabled=True(1.04). PixelResolution 0.0023696. (같은 이름 측정이 `SHOT_23` 에도 있다 — realab 은 ShotName=SIDE_SHOT_1_C13-14 인 Shot 섹션 번호를 찾아 그 Shot 의 섹션만 쓴다.)

09-17 사이클(자재 표기: 사진 이름 `_M1_` = A, `_M2_` = B):

| 이름 | 기준점 가로 사진(z1) | 최종 tick cycle.json (ZIndex 9) |
|---|---|---|
| A1 | D:/Data/Result/Image/260917/1738/original/datum_SIDE_1_Side_Datum_1_H_173855718.jpg | D:/Data/Result/20260917/173904650_cycle/cycle.json |
| A2 | D:/Data/Result/Image/260917/1739/original/datum_SIDE_1_Side_Datum_1_H_173956317.jpg | D:/Data/Result/20260917/174005377_cycle/cycle.json |
| A3 | D:/Data/Result/Image/260917/1740/original/datum_SIDE_1_Side_Datum_1_H_174058421.jpg | D:/Data/Result/20260917/174107154_cycle/cycle.json |
| A4 | D:/Data/Result/Image/260917/1742/original/datum_SIDE_1_Side_Datum_1_H_174211118.jpg | D:/Data/Result/20260917/174220129_cycle/cycle.json |
| B1 | D:/Data/Result/Image/260917/1743/original/datum_SIDE_1_Side_Datum_1_H_174313615.jpg | D:/Data/Result/20260917/174323431_cycle/cycle.json |
| B2 | D:/Data/Result/Image/260917/1744/original/datum_SIDE_1_Side_Datum_1_H_174427026.jpg | D:/Data/Result/20260917/174437728_cycle/cycle.json |
| B3 | D:/Data/Result/Image/260917/1745/original/datum_SIDE_1_Side_Datum_1_H_174557221.jpg | D:/Data/Result/20260917/174607834_cycle/cycle.json |

cycle.json 구조(옛 exe 기록): 최상위 `ZIndex`, `Shots[]` → `ShotName`, `FAIs[]` → `OriginImageFileName`(그 tick 의 측정 사진 전체 경로), `Measurements[]` → `MeasurementName`, `LastMeasuredValue`, `LastHasResult`, `SelectedZIndex`(최종 tick 에서 C13_P1 z3, C14_P1 A z3 / B z5, C13·C14_P2 z7, C13·C14_P3 z9). tick 마다 `*_cycle` 폴더 1개(예: A1 = 173857815 z3 … 173904650 z9). `DatumImages`·`ZRangeImages`·`DatumDiagnostics` 는 비어 있다(기록 기능 이전).

기준 ROI 후보 창(z1 좌표, RESEARCH Pitfall 1 표 + P2 재조사 — Row = 행 중앙, Col = 열 중앙, Length1 = 행 폭 절반, Length2 = 열 폭 절반, Phi 0, TryFitLine 규약 roiLength1 = 행 반폭):
- P1: stripL 행 6480~6700 · 열 1400~1580 / stripL2 행 6480~6700 · 열 1760~1940
- P2: stripM 행 6380~6720 · 열 6900~7080 / stripM2 행 6380~6720 · 열 7480~7660
- P3: stripR 행 6400~6620 · 열 12600~12780 / stripR2 행 6400~6620 · 열 12950~13130

79-01 이 만든 API(이 plan 이 쓴다): `EdgeToLineDistanceMeasurement.ComputeLocalRefLine(HImage, HTuple)` → `LocalRefLineResult`(Found, Row1~Col2, MidRow, MidCol, EdgeScore, Error), `InjectedLocalRef`, `IsLocalRefEnabled`, `LocalRef_*`, `LocalRefEdge*`/`LocalRefSigma`, `MeasurementBase.LastRefSource`, `MeasurementBase.EvaluateJudgement(double)`(보정계수·부호 적용 후 LastMeasuredValue 기록 — 운영과 같은 값 경로)
</interfaces>
</context>

## Artifacts this phase produces (이 plan 분)

phase 전체 원장은 `79-01-PLAN.md`. 이 plan 이 만드는 것: `79-HUMAN-UAT.md`(U-1~U-8, 추천 티칭 표, A−B 비교 표, 알려진 한계). 저장소 밖: LocalRefProbe `realab` 모드, `$H/realab.txt`, `$H/recipe-mtime-before.txt`.

<tasks>

<task type="auto">
  <name>Task 1: realab — 09-17 자재 A·B 사진으로 C13·C14 6점의 국부/전역 A−B 계산 + 기준 ROI 창·에지 설정 추천 (P2 재조사 포함)</name>
  <precondition>79-04-SUMMARY.md 가 있고 감사가 PASS 로 기록되어 있다. D:/Data/Result/Image/260917 과 D:/Data/Result/20260917 이 읽힌다.</precondition>
  <files>(저장소 변경 없음 — probe 는 스크래치 폴더, 결과는 Task 2 UAT 문서와 SUMMARY)</files>
  <read_first>
    - .planning/phases/79-side-local-strip-datum/79-01-PLAN.md 'Probe 규격'
    - .planning/phases/79-side-local-strip-datum/79-01-SUMMARY.md (real1 에서 찾은 조합·midRow — 출발점)
    - .planning/phases/79-side-local-strip-datum/79-RESEARCH.md 'Pitfall 1' 표와 'Open Questions' 3
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs (ComputeLocalRefLine, TryExecute 시그니처)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs (EvaluateJudgement)
    - C:/Users/admin/AppData/Local/Temp/p79-probe/LocalRefProbe.cs (기존 모드 구조)
  </read_first>
  <action>
저장소 파일은 고치지 않는다. D:/Data 는 읽기만 한다. 시작할 때 `stat -c %Y /d/Data/Recipe/FAI_1/main.ini > $H/recipe-mtime-before.txt`.

LocalRefProbe 에 `realab` 모드를 더한다: `LocalRefProbe.exe <binDir> realab <mainIni>` (결과는 표준 출력, verify 가 `$H/realab.txt` 로 받는다). 사진은 `new HImage(path)` 후 채널 3 이면 Rgb1ToGray, 한 사이클 처리가 끝나면 그 사이클 사진을 전부 Dispose 한다(13376×9528 사진 — 사이클 사이에 들고 있지 않는다). double 출력은 InvariantCulture.
(1) 레시피: mainIni 를 IniFile 로 읽어 ShotName=SIDE_SHOT_1_C13-14 인 `[SHOT_n]` 을 찾고, `SHOT_n_FAI_*_MEAS_*` 섹션 중 MeasurementName 이 C13_P1, C14_P1, C13_P2, C14_P2, C13_P3, C14_P3 인 6개 섹션 이름을 기억한다. 측정 객체는 매번 새 `EdgeToLineDistanceMeasurement(null)` 에 Load 해서 쓴다.
(2) 사이클 7개(interfaces 표): 최종 tick cycle.json 을 Newtonsoft JObject 로 읽어 Shot SIDE_SHOT_1_C13-14 의 6측정 LastMeasuredValue·LastHasResult·SelectedZIndex 를 얻는다. z 사진 표: `D:/Data/Result/20260917` 의 `*_cycle` 폴더를 이름순으로 두고 최종 tick 폴더에서 앞으로 거슬러 가며 각 cycle.json 의 ZIndex 와 그 Shot FAI 의 OriginImageFileName 을 모아 z3~z9 7장을 채운다(ZIndex 3 을 만나면 멈춤). 7장이 다 있고 파일이 있고 이름에 A 는 `_M1_`, B 는 `_M2_` 가 있어야 한다 — 아니면 `realab_cycle label=<이름> ok=0 reason=<...>` 를 찍고 예외로 센다. 정상이면 `realab_cycle label=<이름> ok=1 z1=<파일 이름> z3=<파일 이름> … z9=<파일 이름>`.
(3) 창 조사: 사이클마다 z1 사진 1장에서 창 6개 × 에지 조합 8개(방향 TtoB·BtoT × 극성 DarkToLight·LightToDark × 문턱 10·5, Sigma 1.0, SampleCount 20, Trim 10, Selection Strongest)를 C13_P1 섹션을 Load 한 측정에 IsLocalRefEnabled=true·LocalRef_*·LocalRef 에지 설정을 넣어 `ComputeLocalRefLine(z1, 단위 변환)` 으로 부른다. 줄 `realab_win cycle=<이름> window=<창> combo=<방향>/<극성>/<문턱> found=<0|1> midRow=<F2> score=<F1>`. 7사이클이 끝나면 창·조합마다 `realab_winsum window=<창> combo=<조합> found=<n>/7 midRowRangeA=<F2> midRowRangeB=<F2> meanScore=<F1>`(범위 = 같은 자재 안 midRow 최대−최소, 찾은 사이클만), 창마다 `realab_best window=<창> combo=<조합 또는 none> found=<n>/7 midRowRangeA=<F2> midRowRangeB=<F2> meanScore=<F1>` — 최적 = found 7/7 중 meanScore 가장 큰 조합(같으면 위 나열 순서 앞), 7/7 이 없으면 none.
(4) 측정 값: 측정마다 자기 핀의 창 2개(P1 = stripL·stripL2, P2 = stripM·stripM2, P3 = stripR·stripR2) 중 best 가 none 이 아닌 창에 대해, 사이클마다 새로 Load 한 측정에 IsLocalRefEnabled=true·그 창·best 조합을 넣고 `ComputeLocalRefLine(z1, 단위 변환)` → Found 면 `InjectedLocalRef` 에 대입 → 그 측정의 SelectedZIndex 사진으로 `TryExecute(사진, 단위 변환, 0.0023696, ...)` → 성공이면 `EvaluateJudgement(원값)` 뒤 `LastMeasuredValue` 가 국부 값(LastRefSource 가 'Local' 이 아니면 ok=0). 줄 `realab_val meas=<측정> window=<창> cycle=<이름> z=<n> ok=<0|1> refsource=<값 또는 -> local=<F4> global=<F4>`. 측정·창마다 `realab_meas meas=<측정> window=<창> combo=<조합> pin_ok=<n>/7 localA=<F4> localB=<F4> localAB_um=<F1> globalA=<F4> globalB=<F4> globalAB_um=<F1> localRangeA_um=<F1> localRangeB_um=<F1> globalRangeA_um=<F1> globalRangeB_um=<F1>` (AB = (A 평균 − B 평균) × 1000, 범위 = 같은 자재 안 최대−최소 × 1000, 국부는 ok=1 인 사이클만, 전역은 LastHasResult 인 사이클만). best 가 none 인 창은 `realab_meas meas=<측정> window=<창> combo=none` 만 찍는다.
(5) 추천: 측정마다 `realab_pick meas=<측정> window=<창 또는 none> row=<F1> col=<F1> len1=<F1> len2=<F1> combo=<조합>` — 규칙: 자기 창 2개 중 best 가 none 이 아니고 pin_ok 7/7 인 창에서 max(midRowRangeA, midRowRangeB) 가 작은 창, 같으면 창 열 중앙이 Point_Col 에 가까운 창. A−B 값으로 고르지 않는다. 둘 다 안 되면 none(P2 는 A-79-A2 로 'P2 는 켜지 않음').
(6) 끝 줄 `realab cycles=<정상 사이클 수> A=<n> B=<n> exceptions=<예외 수>` (예외는 잡아서 세고 문구를 찍는다).
(7) 컴파일 → `LocalRefProbe.exe $BIN realab $W/main-snapshot.ini > $H/realab.txt`. stripL 또는 stripR 의 best 가 none 이면 설계 전제(z1 에서 띠를 찾는다)가 깨진 것이므로 멈추고 보고한다. 커밋할 저장소 파일은 없다.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p79-probe; W=C:/Users/admin/AppData/Local/Temp/p79-probe; FW=C:/Windows/Microsoft.NET/Framework64/v4.0.30319; BIN=C:/code/DataMeasurement/WPF_Example/bin/x64/Debug
(cd $H && cp /c/code/DataMeasurement/WPF_Example/bin/x64/Debug/DatumMeasurement.exe.config LocalRefProbe.exe.config && MSYS_NO_PATHCONV=1 $FW/csc.exe -nologo -codepage:65001 -platform:x64 -out:LocalRefProbe.exe -lib:$BIN -r:DatumMeasurement.exe -r:Newtonsoft.Json.dll -r:halcondotnet.dll -r:$FW/netstandard.dll -r:$FW/WPF/PresentationFramework.dll -r:$FW/WPF/PresentationCore.dll -r:$FW/WPF/WindowsBase.dll -r:$FW/System.Xaml.dll LocalRefProbe.cs && MSYS_NO_PATHCONV=1 ./LocalRefProbe.exe $BIN realab $W/main-snapshot.ini > $H/realab.txt 2>&1); echo "probe_exit=$?"
R=$H/realab.txt
grep -E '^realab cycles=' $R; grep -E '^realab_(best|pick|meas) ' $R
echo "cycles_ok=$(grep -c '^realab_cycle .* ok=1' $R) winsum=$(grep -c '^realab_winsum ' $R) best=$(grep -c '^realab_best ' $R) meas=$(grep -c '^realab_meas ' $R) pick=$(grep -c '^realab_pick ' $R) stripL_ok=$(grep '^realab_best window=stripL ' $R | grep -vc 'combo=none') stripR_ok=$(grep '^realab_best window=stripR ' $R | grep -vc 'combo=none')"
echo "recipe_untouched=$( [ "$(cat $H/recipe-mtime-before.txt)" = "$(stat -c %Y /d/Data/Recipe/FAI_1/main.ini)" ] && echo 1 || echo 0 ) repo_clean=$(git status --porcelain -- WPF_Example | wc -l)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `probe_exit=0`, 출력에 `realab cycles=7 A=4 B=3 exceptions=0`
    - `cycles_ok=7 winsum=48 best=6 meas=12 pick=6`
    - `stripL_ok=1 stripR_ok=1` (P1·P3 창은 7사이클 모두 띠를 찾는 조합이 있다 — 아니면 멈추고 보고)
    - `recipe_untouched=1`, `repo_clean=0`
  </acceptance_criteria>
  <done>09-17 자재 A·B 7사이클에서 C13·C14 6점의 국부/전역 A−B, 창별 찾음 수와 흔들림, 측정별 추천 기준 ROI 값이 운영 코드 경로로 계산되어 스크래치 파일에 남았고, 레시피·저장소는 그대로다.</done>
</task>

<task type="auto">
  <name>Task 2: 79-HUMAN-UAT.md 작성 — 티칭 표·A−B 표 + U-1~U-8 (옵션 UI·국부 측정·자동 전환·표시 기록·창 선택·효과·TOP/BOTTOM·동시 작업)</name>
  <files>.planning/phases/79-side-local-strip-datum/79-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/79-side-local-strip-datum/79-CONTEXT.md (D-79-01~09, 성공 기준 1~4)
    - .planning/phases/79-side-local-strip-datum/79-01-PLAN.md ('계획 단계 결정' P-1~P-15, flagged assumptions A-79-E1~E4 / A1~A7)
    - .planning/phases/79-side-local-strip-datum/79-0[1-4]-SUMMARY.md (실제 로그 문구·속성 이름·상수·probe 수치)
    - C:/Users/admin/AppData/Local/Temp/p79-probe/realab.txt (Task 1 결과)
    - .planning/phases/77-side-z-focus-select/77-HUMAN-UAT.md (문서 형식, 공통 준비 — SIMUL TCP 모의 클라이언트·OfflineInspect z 사진·시험 레시피 복사본)
    - .planning/phases/79-side-local-strip-datum/79-VALIDATION.md 'Manual-Only Verifications'
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs 의 GrabOrLoadDatumImage, TryGrabOrLoadDualDatumImages, TryGrabOrLoadCrossZDatumImages, TryLoadStaticDualDatumImages (SIMUL·오프라인에서 기준점 가로 사진이 어디서 오는지 — U-2 준비 단계를 정확히 쓰기 위해)
  </read_first>
  <action>
77-HUMAN-UAT.md 형식으로 쉬운 한국어 절차서를 쓴다. 머리: 상태(사용자 확인 대기), 버전 1.7.50.0, 빌드 = Debug|x64 전용(Release 는 OutputPath D:\Data 라 운영 exe 를 덮어씀 — 배포는 앱 종료 확인 후 사용자 승인으로 별도), 실행 환경(사무실 PC SIMUL 로 U-1~U-6·U-8, 장비 PC 로 U-7 과 가능하면 U-6 실물 A·B). 각 항목은 체크박스 제목 + (요구사항·결정·가정 ID) + 목적 / 준비 / 단계(번호) / 기대 결과(SUMMARY 에 기록된 실제 로그·화면 문구, CSV 열 이름 '사용기준', JSON 키 'RefSource') / 합격 기준 / 결과 칸. 문구·숫자는 SUMMARY·realab 실제 값을 쓴다.

공통 준비: 운영 레시피(D:\Data\Recipe\FAI_1\main.ini)는 이 UAT 중 고치지 않는다 — 77-HUMAN-UAT 공통 준비의 시험 레시피 복사본 방식을 그대로 안내하고, 복사·사진 준비는 사용자가 한다. 운영 레시피 적용은 UAT 합격 뒤 사용자 결정(P-15). 옵션을 켜면 0점이 바뀌어 값이 달라진다는 점(기준값·공차 확인 — PR-5, O-79-05)을 머리에 적는다.

표 두 개(realab.txt 에서 옮긴다):
- '추천 티칭 표': 측정 6행 × (추천 창, LocalRef_Row, LocalRef_Col, LocalRef_Phi 0, LocalRef_Length1, LocalRef_Length2, LocalRefEdgeThreshold, LocalRefSigma 1.0, LocalRefEdgeSampleCount 20, LocalRefEdgeTrimCount 10, LocalRefEdgePolarity, LocalRefEdgeDirection, LocalRefEdgeSelection Strongest). realab_pick 이 none 인 측정은 '켜지 않음(창 재조사 필요)' 으로 적는다(P2 는 A-79-A2).
- 'A−B 비교 표': 측정 × 창 12행 × (찾음 수, 전역 A−B µm, 국부 A−B µm, 전역·국부 같은 자재 안 흔들림 µm). 표 아래에 "추천 창은 A−B 로 고르지 않았다(같은 7사이클로 고르고 평가하면 좋게만 보인다) — 최종 선택은 U-5" 를 적는다.

항목:
- U-1 (LSR-01, D-79-03/04, A-79-E1, A-79-A5): 시험 레시피에서 C13_P3 선택 → 속성창 'Local Ref' 탭(Option / ROI / Edge), 옛 레시피는 꺼짐·에지 기본값(10 / 1.0 / 20 / 10 / DarkToLight / TtoB / Strongest) 확인 → 추천 티칭 표 숫자 입력 → 측정 노드를 다시 누르면 캔버스에 `C13_P3_LocalRef` 상자 → 끌기·크기 바꾸기·Edit 모드 보정 위치 표시 → 레시피 저장 뒤 main.ini(시험 복사본) 그 측정 섹션에만 IsLocalRefEnabled 등 13개 키가 생겼는지(다른 섹션 무변경, 메모장 비교). **A-79-E1 판단 칸**(쓸 만한가, 고칠 점).
- U-2 (LSR-02, D-79-05, A-79-E2 — tracer 화면 증거): C13_P3 만 켜고 C14_P3 는 끈 채 SIDE 사이클(SIMUL: 09-17 A1 사이클 사진을 코드에서 확인한 방식대로 준비 — 준비 절차를 구체적으로 적고, 사진을 넣을 폴더가 D:\Data 아래면 '사용자가 복사' 로 표시) → Algorithm 로그 `[LocalRef] 기준선 찾음 — ` 가 기준점 검출마다 1번, `국부 기준선 1개 계산` 요약 줄, 결과 그리드 C13_P3 '기준' = 국부 / C14_P3 빈칸, 주황 선이 띠 에지 위, cycle.json RefSource 'Local', 일자별 CSV 끝 열 사용기준 '국부'. C13_P3 값이 realab 의 A1 국부 값과 비슷한지(차이를 기록). C14_P3 값이 옵션 추가 전 같은 사진 값과 같은지.
- U-3 (LSR-03, D-79-06, A-79-E3, A-79-A6): (a) 기준 ROI 삭제(Delete) (b) 기준 ROI 를 띠가 없는 평평한 곳으로 이동 (c) Test Find 로 기준점을 잡은 뒤 기준 ROI 를 조금 옮기고 수동 RUN — 각각 Error 로그 `[LocalRef] 전역 기준선으로 전환 — <Shot> · <측정>: <원인>`(원인 문구는 SUMMARY 의 실제 상수 문구), '기준' = 국부실패→전역, 값 = 옵션 끈 값, 검사 계속·강제 NG 없음. **A-79-E3 판단 칸**(원인 문구가 충분한가).
- U-4 (LSR-04/05, D-79-04/07, A-79-A4, A-79-A7): 옵션 꺼진 측정의 '기준' 칸 빈칸, 리뷰어에서 옛 날짜(20260916 등) 열기 → '기준' 열 빈칸·예외 없음, 새 사이클은 국부 / 국부실패→전역 표시, 저장 캡처 사진의 국부 기준선은 파랑(화면은 주황). **A-79-A4·A-79-A7 판단 칸**.
- U-5 (LSR-06, O-79-10, A-79-A2, D-79-02): 추천 티칭 표·A−B 표를 보고 C13·C14 6점 중 켤 측정과 창을 정한다(P2 포함 여부). 도면 C13·C14 높이 기준 = 핀 옆 띠 면(D-79-02) 이라는 점을 적는다.
- U-6 (LSR-06, D-79-01, A-79-E4, PR-5): U-5 에서 정한 측정을 켜고 자재 A·B 사이클(장비 PC 실물이 가능하면 실물, 아니면 09-17 사진 재검사)을 돌려 전역(옵션 끔) 대비 A−B 가 줄었는지 기록 표(측정, 전역 A−B, 국부 A−B, 판정) + 켠 측정마다 새 값 기준의 기준값·공차 결정 칸. **A-79-E4 판단 칸**.
- U-7 (D-79-09, A-79-A3 — 장비 PC): TOP·BOTTOM EdgeToLineDistance 1~2개에 기준 ROI 를 티칭해 켜기 → '국부' 동작, 기준 ROI 삭제 → '국부실패→전역' 전환. 코드 수정 없이 레시피만으로.
- U-8 (edge LSR-04 concurrency): SIMUL TCP 반복 검사 중 Test Find·기준 ROI 끌기 → 앱 멈춤·죽음 없음, 전환 원인 로그 기록.
- 끝 '알려진 한계' 절: A-79-A1(기울기는 전역 각도 유지), A-79-A2(P2), A-79-A3(TOP·BOTTOM 사진 없음), A-79-A4(꺼진 측정 빈칸), A-79-A5(첫 ROI 숫자 입력), A-79-A6(Test Find 재사용), A-79-A7(저장 사진 파랑), z1 사진 흔들림은 전역과 똑같이 남음(CONTEXT 별건), realab 은 단위 변환(기준점 보정 없이)으로 ROI 를 놓았으므로 앱 값과 조금 다를 수 있음.

커밋: `git add .planning/phases/79-side-local-strip-datum/79-HUMAN-UAT.md` 만. 메시지 예: docs(79-05): 핀 옆 띠 기준 UAT 절차서·추천 티칭 표.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
U=.planning/phases/79-side-local-strip-datum/79-HUMAN-UAT.md
echo "exists=$(test -f $U && echo 1 || echo 0)"
for K in U-1 U-2 U-3 U-4 U-5 U-6 U-7 U-8 A-79-E1 A-79-E2 A-79-E3 A-79-E4 A-79-A2 A-79-A7; do echo "$K=$(grep -c "$K" $U)"; done
echo "table_meas=$(grep -cE '^\|.*(C13_P1|C14_P1|C13_P2|C14_P2|C13_P3|C14_P3)' $U) teach_keys=$(grep -cE 'LocalRef_Length1|LocalRefEdgePolarity' $U) log_found=$(grep -c '\[LocalRef\] 기준선 찾음' $U) log_fallback=$(grep -c '\[LocalRef\] 전역 기준선으로 전환' $U) fallback_text=$(grep -c '국부실패→전역' $U) csvcol=$(grep -c '사용기준' $U) jsonkey=$(grep -c 'RefSource' $U) debugonly=$(grep -c 'Debug|x64' $U) version=$(grep -c '1.7.50.0' $U) recipe_policy=$(grep -cE '운영 레시피' $U)"
echo "commit_files=$(git show --name-only --format= HEAD | LC_ALL=C sort | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `exists=1`, `U-1` ~ `U-8` 각각 ≥ 1, `A-79-E1` ~ `A-79-E4` 각각 ≥ 1, `A-79-A2` ≥ 1, `A-79-A7` ≥ 1
    - `table_meas` ≥ 18 (티칭 표 6행 + A−B 표 12행), `teach_keys` ≥ 2
    - `log_found` ≥ 1, `log_fallback` ≥ 1, `fallback_text` ≥ 1, `csvcol` ≥ 1, `jsonkey` ≥ 1, `debugonly` ≥ 1, `version` ≥ 1, `recipe_policy` ≥ 1
    - `commit_files` = `.planning/phases/79-side-local-strip-datum/79-HUMAN-UAT.md`
  </acceptance_criteria>
  <done>realab 표와 U-1~U-8 절차가 실제 로그 문구·열 이름·JSON 키로 작성되어 커밋되었다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 사용자 확인 — 핀 옆 띠 기준 옵션 UAT (U-1~U-6·U-8 사무실, U-7 장비) + 켤 측정·창·기준값 결정</name>
  <what-built>Phase 79 전체(79-01~04): EdgeToLineDistance 'Local Ref' 옵션(기본 꺼짐) — 기준점 가로 사진에서 기준 ROI 를 기준점 검출 때 한 번 피팅해 기준선 위치로 사용, 실패 시 전역 기준선 자동 전환 + [LocalRef] 로그, 사용 기준 기록·표시(cycle.json RefSource, CSV 사용기준 열, 결과 그리드·리뷰어 '기준' 열, 주황 선), 캔버스 기준 ROI 배선, 버전 1.7.50.0, 누적 감사 PASS. realab 로 09-17 A·B 사진의 C13·C14 6점 국부/전역 A−B 와 추천 티칭 값을 계산했다. 절차서 79-HUMAN-UAT.md.</what-built>
  <how-to-verify>
    1. `.planning/phases/79-side-local-strip-datum/79-HUMAN-UAT.md` 를 연다.
    2. 운영 레시피는 그대로 두고 시험 레시피 복사본으로, Debug|x64 앱에서 U-1(옵션 UI·티칭)과 U-2(C13_P3 만 켠 SIDE 사이클 — tracer 화면 증거)부터 U-6, U-8 을 수행하고 결과 칸에 PASS/FAIL 과 메모를 적는다.
    3. U-1 의 A-79-E1, U-3 의 A-79-E3, U-4 의 A-79-A4·A-79-A7, U-6 의 A-79-E4 판단을 적는다.
    4. U-5 에서 켤 측정·창(P2 포함 여부)을 정하고, U-6 에서 켠 측정의 기준값·공차를 정한다(코드가 자동으로 바꾸지 않는다).
    5. U-7(TOP·BOTTOM)은 장비 PC 에서 가능하면 수행, 어렵다면 '대기' 로 둔다.
    6. 운영 레시피 적용·배포(Release 빌드, D:\Data 덮어쓰기)는 이 체크포인트 범위가 아니다 — UAT 합격 뒤 앱 종료 확인과 사용자 승인으로 별도 진행.
  </how-to-verify>
  <resume-signal>"approved" (U-1~U-6·U-8 PASS, U-7 PASS 또는 대기, 켤 측정·창·기준값 결정 기록) 또는 실패한 U 번호와 증상을 적어 주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| realab probe → 운영 데이터(D:/Data 레시피·결과·사진) | 읽기만 해야 한다 — 쓰면 현장 레시피·기록이 바뀐다 |
| UAT 절차 → 운영 레시피·배포 exe | 절차 중 운영 파일을 건드리면 현장 판정이 조용히 바뀐다 |
| realab 추천 → 사용자 티칭 결정 | 같은 데이터로 고르고 평가하면 효과가 부풀려 보인다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-18 | Tampering | realab 이 D:/Data 를 씀 | high | mitigate | 레시피는 스냅샷 복사본만 Load, 사진·cycle.json 은 읽기, 출력은 스크래치 — `recipe_untouched=1`, `repo_clean=0` |
| T-79-19 | Tampering | UAT 중 운영 레시피 수정·Release 빌드 | high | mitigate | 절차서 머리·체크포인트 2·6번에 시험 레시피 복사본·Debug|x64 전용·배포 범위 밖 명시(`debugonly`, `recipe_policy`) |
| T-79-20 | Repudiation | 추천 창이 A−B 로 골라져 효과가 부풀려짐 | medium | mitigate | 추천 규칙은 찾음 7/7 + 흔들림(A−B 미사용), 표 아래에 이유를 적고 최종 선택은 사용자(U-5) |
| T-79-21 | Tampering | 켠 측정 기준값·공차 자동 변경 | medium | mitigate | 코드·실행자는 기준값을 바꾸지 않는다(prohibition), U-6 에서 사용자가 결정 |
</threat_model>

<verification>
1. realab: 7사이클(A 4, B 3) 정상, 창 6 × 조합 8 조사, 측정 6 × 창 2 의 국부/전역 A−B, 추천 6줄, stripL·stripR 7/7 조합 있음, 레시피 무변경
2. 79-HUMAN-UAT.md 에 추천 티칭 표·A−B 표, U-1~U-8, A-79-E1~E4·A2·A7, 실제 로그 문구·열 이름·JSON 키, Debug|x64 전용·운영 레시피 정책 명시
3. 사용자 체크포인트 승인(또는 실패 항목 보고) 기록
</verification>

<success_criteria>
- CONTEXT 성공 기준 1(옵션 켠 측정이 z1 핀 옆 띠 기준 값), 2(C13·C14 6점 A−B 감소 — 사용자 판단), 3(기준 ROI 실패에도 검사 계속 + 전환 표시), 4(옵션 꺼짐·TOP/BOTTOM 회귀 0 — 79-04 자동 증거 + U-4·U-7)의 사람 확인
- A-79-E1~E4 사용자 판단 기록, 켤 측정·창·기준값 결정 기록 (LSR-06)
</success_criteria>

<output>
완료 후 `.planning/phases/79-side-local-strip-datum/79-05-SUMMARY.md` 작성: realab.txt 요약(realab_best·realab_meas·realab_pick 줄 원문, 끝 줄), UAT 문서 커밋 해시, 사용자 응답 원문, U 번호별 결과표, A-79-E1~E4 판단, 켤 측정·창·기준값 결정, U-7 수행 여부.
</output>
