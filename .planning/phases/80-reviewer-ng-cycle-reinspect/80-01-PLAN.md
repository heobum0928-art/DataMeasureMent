---
phase: 80-reviewer-ng-cycle-reinspect
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs
  - WPF_Example/Custom/Sequence/Inspection/ReviewerReinspectService.cs
  - WPF_Example/UI/Reviewer/ReviewerReinspectViewModel.cs
  - WPF_Example/DatumMeasurement.csproj
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs
  - WPF_Example/UI/ContentItem/MainView.xaml
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/MainWindow.xaml.cs
  - WPF_Example/Custom/SystemHandler.cs
autonomous: true
requirements: [D-80-00, D-80-01, D-80-02, D-80-03, D-80-10, D-80-11, D-80-12, D-80-13, D-80-16, D-80-17, D-80-18]
user_setup: []

must_haves:
  truths:
    - "[D-80-01, D-80-00] 리뷰어 NG 원인 패널(border_ngCause) 안, 원인 문구(txt_ngCausePanel) 바로 아래에 큰 버튼 '이 사진으로 파라미터 수정' 1개가 있다. 더블클릭·오른쪽 클릭 메뉴는 만들지 않고, 새 창·새 설정 항목도 없다"
    - "[D-80-02, D-80-17] 행을 고르지 않았거나 불러올 수 없는 행(사진 파일 없음 / 지금 레시피와 다른 레시피 / 이 PC 에 없는 검사 / 지금 레시피에 없는 Shot)이면 버튼이 비활성이고 버튼 옆 한 줄에 쉬운 한국어 이유가 보인다. 활성 여부와 이유 문자열은 ReviewerReinspectViewModel 이 만들어 바인딩한다(CanApply / DisableReasonText)"
    - "[D-80-03, D-80-13] 버튼을 누르면 확인창 없이 바로 고른 사이클의 Shot 사진이 그 Shot 의 SimulImagePath 와 RerunZRangeImagePaths 로 들어가고, OfflineInspectMode 가 꺼져 있었으면 메모리에서만 켠다(Setting.Save 를 부르지 않는다). 기준점 캐시(ClearDatumTransforms)는 비운다. 메인 창이 앞으로 온다(Activate)"
    - "[D-80-12] 불러온 동안 메인 화면 툴바 아래 노란 상태 줄 1줄 '리뷰어 사진 사용 중 — MM-dd HH:mm 자재 N'(자재번호가 없으면 '자재번호 없음') + [해제] 버튼이 보이고 Trace 로그에 '[ReviewerLoad] 불러옴 — ' 1줄이 남는다. 해제되면 상태 줄이 사라지고 '[ReviewerLoad] 해제 — <이유>' 1줄이 남는다"
    - "[D-80-11] 원래 사진 경로(ShotConfig.SimulImagePath · DatumConfig.TeachingImagePath · TeachingImagePath_Vertical · DualImageEdgeDistanceMeasurement.TeachingImagePath_Horizontal · TeachingImagePath_Vertical · ShotConfig.RerunZRangeImagePaths)와 OfflineInspectMode 는 [해제] · PLC $TEST 수신(ForceOfflineInspectModeOffForAutoTest 보다 먼저) · 레시피 변경(SequenceHandler.OnRecipeChanged) · 프로그램 종료(mSystemHandler.Release 보다 먼저) 4경로에서 되돌아간다. 고친 파라미터는 그대로다. 다른 행을 또 불러오면 첫 스냅샷(원래 값)은 그대로 두고 사진만 바뀐다"
    - "[D-80-10] 불러온 상태에서 레시피를 저장하면 저장하는 순간에만 원래 사진 경로로 되돌려 SequenceHandler.SaveRecipe 를 부르고, 끝나면(예외여도) 불러온 경로를 다시 넣는다. 저장을 막지 않는다. 기존 RepeatRunService.IsSavedCycleRerunActive 저장 차단은 그대로 먼저 걸린다"
    - "[D-80-18] 새 파일 ReviewerReinspectService.cs(namespace ReringProject.Sequence)와 ReviewerReinspectViewModel.cs(namespace ReringProject.UI)가 DatumMeasurement.csproj 에 Compile Include 로 등록되어 빌드에 들어간다"
    - "[D-80-17] ReviewerWindow.xaml.cs 의 Button_ApplyRowToMain_Click 과 MainView.xaml.cs 의 BtnReviewerRelease_Click 본문은 VM 호출 1문장이다. 불러오기·스냅샷·복원 로직은 서비스에, 표시 문자열은 VM 에 있다"
    - "[D-80-16] 새 파일 2개 전체와 기존 파일에 더한 줄에서 삼항·null 병합·null 조건·switch 식·날짜 주석·물음표 문자가 0 이고, &&/|| 가 3개 이상인 줄이 0 이다. HImage 는 finally 에서 Dispose 한다. Debug|x64 빌드 오류 0"
    - "[D-80-07 tracer 단계] SavedCycleRerunPlanner.BuildPartForSingleCycle 이 public static 으로 있고(이 plan 에서는 고른 사이클 tick 1개로 부품을 만든다 — 같은 자재 묶기는 80-03 Task 1), 기존 BuildPlan · ValidatePart · RepeatRunService 반복검사 코드는 한 줄도 바뀌지 않는다(RepeatRunService.cs 삭제 줄 0)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/ReviewerReinspectService.cs"
      provides: "리뷰어 사진 불러오기·스냅샷·복원·저장 보호·자동 해제 정적 서비스"
      contains: "public static ReviewerReinspectLoadResult LoadForRow("
    - path: "WPF_Example/UI/Reviewer/ReviewerReinspectViewModel.cs"
      provides: "버튼 활성·이유·상태 줄 문자열 VM (싱글턴 Instance)"
      contains: "public static readonly ReviewerReinspectViewModel Instance"
    - path: "WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs"
      provides: "SavedCycleRerunPlanner.BuildPartForSingleCycle 진입점"
      contains: "public static SavedCycleRerunPart BuildPartForSingleCycle("
    - path: "WPF_Example/UI/Reviewer/ReviewerWindow.xaml"
      provides: "리뷰어 버튼 1개 + 이유 1줄"
      contains: "이 사진으로 파라미터 수정"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml"
      provides: "메인 상태 줄 1줄 + [해제]"
      contains: "x:Name=\"panel_reviewerReinspect\""
  key_links:
    - from: "ReviewerWindow.xaml btn_applyRowToMain"
      to: "ReviewerReinspectService.LoadForRow"
      via: "Button_ApplyRowToMain_Click → ReviewerReinspectViewModel.ApplySelection → LoadForRow → SavedCycleRerunPlanner.BuildPartForSingleCycle"
      pattern: "ReviewerReinspectService\\.LoadForRow\\("
    - from: "MainView.xaml panel_reviewerReinspect"
      to: "ReviewerReinspectViewModel.StatusText / IsActive"
      via: "panel_reviewerReinspect.DataContext = ReviewerReinspectViewModel.Instance"
      pattern: "\\{Binding StatusText\\}"
    - from: "Custom/SystemHandler.cs MainRun case VisionRequestType.Test"
      to: "ReviewerReinspectService.Release"
      via: "ForceOfflineInspectModeOffForAutoTest 바로 앞 1줄"
      pattern: "ReviewerReinspectService\\.Release\\(ReviewerReinspectService\\.RELEASE_REASON_PLC_TEST\\)"
    - from: "MainWindow.SaveRecipe"
      to: "ReviewerReinspectService.RunWithOriginalPaths"
      via: "SequenceHandler.SaveRecipe 호출을 감싼다"
      pattern: "ReviewerReinspectService\\.RunWithOriginalPaths\\("
---

<objective>
리뷰어에서 NG 행을 고르고 버튼 한 번을 누르면 그 사이클의 Shot 사진이 메인 화면 레시피 객체에 들어가고, 메인 화면에 "리뷰어 사진 사용 중" 상태 줄과 [해제] 가 뜨는 **가장 얇은 끝에서 끝까지 경로(tracer)** 를 만든다. 같은 plan 에서 운영 레시피 보호(저장 시 원래 경로, PLC 자동 검사·레시피 변경·종료 시 자동 해제)까지 넣어, 이 plan 이후 어떤 상태에서도 리뷰어 사진 경로가 운영 레시피 파일이나 PLC 자동 검사로 새지 않게 한다.

Purpose: 사용자가 1738·1743 폴더에서 사진 짝을 손으로 찾던 일을 프로그램이 대신하는 기능의 뼈대다. 부품 묶기(80-03), 기준점 사진·Z 후보(80-03), 트리 선택·자동 Test Find(80-04)는 이 뼈대의 같은 메서드·같은 상태 객체 위에 기능만 더한다(구조 변경 없음).

Output: 새 파일 2개(서비스·VM) + csproj 등록, 기존 파일 8개에 삽입(삭제는 MainWindow.SaveRecipe 의 저장 호출 1줄 교체뿐).

검증 방식 메모: ⚠ spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip). 이 phase 는 REQ-ID 가 없고 80-CONTEXT.md 의 D-80-00~19 가 계약이다. 단위 테스트 프레임워크가 없고 사무실 PC 의 Debug 빌드는 라이선스 키가 없어 실행할 수 없으므로, 자동 검증 = Debug|x64 빌드 + 배선 사슬 grep + CLAUDE.md 가독성 grep(더한 줄 기준) 이고, 실제 동작은 80-05 의 장비 PC Release UAT 에서 확인한다.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/80-reviewer-ng-cycle-reinspect/80-CONTEXT.md
@.planning/phases/80-reviewer-ng-cycle-reinspect/80-RESEARCH.md
@.planning/phases/80-reviewer-ng-cycle-reinspect/80-PATTERNS.md

<interfaces>
계획 시점(2026-09-18, HEAD bd4235e5) 라인 번호. 실행자는 파일을 열어 앞뒤 문맥으로 위치를 다시 확인한다.

WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs (Allman, namespace ReringProject.Sequence, using ReringProject.UI 있음)
- :20 `public class RepeatRunService`, :25 `public const int MATERIAL_NOT_SET = -1;`
- :402 `public static bool IsSavedCycleRerunActive`
- :442-453 `private sealed class SavedCycleOverrideSnapshot` (ShotSimulImagePaths / DatumTeachingPaths / DatumTeachingPathsVertical / DualHorizontalPaths / DualVerticalPaths / OwnedShots / OfflineBefore / OfflineSetByRerun / RecipeName)
- :455-491 `BuildOverrideSnapshot(seq, recipeManager)` — `InspectionSequence.IsShotOwnedBySequence(shot, seq.Name)` 로 소유 Shot 만, 소유 Shot 의 DualImageEdgeDistanceMeasurement 가로/세로 경로, `seq.DatumConfigs` 의 TeachingImagePath / TeachingImagePath_Vertical
- :495-521 `RestoreOverridePathsOnly(snap)` — 끝에 소유 Shot 의 `RerunZRangeImagePaths = null`
- :584-589 OfflineInspectMode 메모리에서만 켜기(OfflineSetByRerun), :1076-1080 되돌리기(우리가 켰고 아직 켜져 있을 때만)
- :692 부품 적용 뒤 `_savedCycleSeq.ClearDatumTransforms();`
- :769-782 `BuildRerunZRangeMap(part, szShotName)` — 항상 새 Dictionary
- :1101-1120 `public class SavedCycleRerunPart` (StartTime, IndexNumber, Ticks, DatumPhotoPaths, ShotPhotoPaths, DualPhotos, ZRangePhotoPaths — 전부 public)
- :1165 `public static class SavedCycleRerunPlanner`, :1185-1242 `BuildPlan`(바꾸지 않음), :1242 BuildPlan 닫는 중괄호
- private static 형제: :1375 `FillPartDatumPhotos(part)`, :1401 `FillPartZRangePhotos(part)`, :1477 `FillPartShotPhotos(part)`, :1558 `FillPartDualPhotos(part, seq, recipeManager)`

WPF_Example/UI/ViewModel/CycleResultDto.cs (namespace ReringProject.UI)
- `CycleResultDto`: InspectionTime, RecipeName, IndexNumber(-1 = 없음), IsProtocolDriven, CycleFolderPath, ZIndex, DatumImages, ZRangeImages, Shots
- `ShotResultDto`: ShotName, OwnerSequenceName(비면 "TOP" 로 본다 — RepeatRunService.cs:1302-1310 규칙), FAIs
- `FaiResultDto`: FAIName, OriginImageFileName, LastOverlays / `MeasurementResultDto`: MeasurementName, TypeName

WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs
- `ReviewMeasurementRow.OwnerShot` / `OwnerFai` / `Source`
- `ReviewerImagePathResolver.ResolveRowImagePath(ShotResultDto, FaiResultDto)` :225, `IsUsableImageFile(string)` :152

WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (K&R)
- :52 `DatumConfigs`, :705 `public static bool IsShotOwnedBySequence(ShotConfig shot, string szSeqName)`, :2956 `public void ClearDatumTransforms()`
- SequenceBase `public EContextState State` (Sequence/Sequence/SequenceBase.cs:174)
WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs — :24 `SimulImagePath`, :160 `ShotName`, :174 `OwnerSequenceName`, :236 필드 `public Dictionary<int, string> RerunZRangeImagePaths = null;`, :626 `SetImage(HImage)`(내부 CopyImage), :659 `ClearImage()`
WPF_Example/Custom/Sequence/SequenceHandler.cs — :13 `SEQ_TOP = "TOP"`, :49 `RecipeManager`
WPF_Example/Sequence/SequenceHandler.cs — :17 `RecipeChangedEventArgs`, :25 `delegate void OnRecipeChangedEvent(object sender, RecipeChangedEventArgs arg)`, :46 `event OnRecipeChanged`, :125 `this[int]`, :72 `Count`, :170 `SaveRecipe(name, fileType)`
SystemSetting — `SystemSetting.Handle.OfflineInspectMode`, `SystemHandler.Handle.Setting.CurrentRecipeName`

WPF_Example/UI/Reviewer/ReviewerWindow.xaml(.cs) (Allman, namespace ReringProject.UI)
- .cs :23 `_currentCycle`, :30 `_selectedRow`, :44-47 생성자, :136 `DisplayCycle(CycleResultDto cycle)` 안 :140 `_selectedRow = null;`, :245 `MeasurementGrid_SelectionChanged` 안 :252 `_selectedRow = row;`
- .xaml :226-232 `border_ngCause` > StackPanel > `txt_ngCauseHeader`, `txt_ngCausePanel`(ElementName 바인딩 — Window DataContext 없음)
- 선례 VM 배선: WPF_Example/UI/Reviewer/AlignVerifyWindow.xaml.cs:11-27, AlignVerifyViewModel.cs:29-52(Raise 패턴)

WPF_Example/UI/ContentItem/MainView.xaml (Row 0 = 툴바 canvasToolbar, Row 1 = HALCON 창 — airspace 때문에 WPF 요소를 Row 1 위에 겹치지 않는다)
- :146-459 `canvasToolbar` Border > Grid, :151-154 RowDefinitions 2개(Auto, Auto), :454-457 `brushPanel` Grid.Row="1" Grid.ColumnSpan="3"
- 스타일 DataTrigger 선례: :488-528 `border_angleValidationBadge`
WPF_Example/UI/ContentItem/MainView.xaml.cs (K&R, namespace ReringProject.UI, using ReringProject.Sequence 있음) — :83-101 생성자
WPF_Example/MainWindow.xaml.cs (K&R, namespace ReringProject)
- :77-104 생성자, :89 `mSystemHandler.Sequences.OnRecipeChanged += this.OnLoadRecipe;`
- :292-319 `SaveRecipe(string name=null)` — :299-302 IsSavedCycleRerunActive 차단, :314 `if(!mSystemHandler.Sequences.SaveRecipe(name, ERecipeFileType.Ini)) {`
- :452-471 `Window_Closing` — :469 `RepeatRunService.RestoreActiveSavedCycleOverridesForShutdown();`, :470 `mSystemHandler.Release();`(Setting.Save 포함)
WPF_Example/Custom/SystemHandler.cs (partial SystemHandler, using ReringProject.Sequence 있음)
- :117-127 `case VisionRequestType.Test:` — :118 AutoLogout, :120 `ForceOfflineInspectModeOffForAutoTest();`, :122 `ProcessTest(...)` (MainRun = 백그라운드 스레드)
</interfaces>
</context>

## Artifacts this phase produces (phase 전체 원장 — 각 plan 은 자기 분만 다시 적는다)

| Plan | 파일 | 새 심볼 |
|---|---|---|
| 80-01 | Custom/Sequence/Inspection/ReviewerReinspectService.cs (새) | `enum EReviewerRowBlock { None, NoRow, NoPhoto, RecipeMismatch, SequenceNotFound, ShotNotFound, SequenceBusy, SavedCycleRerunActive }`, `class ReviewerReinspectLoadResult { IsLoaded, BlockReason, BlockDetail, LiveShot, LiveFai, LiveMeasurement }`, `class ReviewerReinspectState { IsActive, CycleTime, IndexNumber, ShotName, MeasurementName }`, `static class ReviewerReinspectService { LOG_TAG, RELEASE_REASON_USER, RELEASE_REASON_PLC_TEST, RELEASE_REASON_RECIPE_CHANGED, RELEASE_REASON_SHUTDOWN, event StateChanged, IsActive, GetState(), CheckRow(...), LoadForRow(...), Release(string), ReleaseByUser(), RunWithOriginalPaths(Func<bool>), HandleRecipeChanged(object, RecipeChangedEventArgs) }` + private `ReviewerOverrideSnapshot`, `BuildSnapshot`, `RestorePathsOnly`, `ApplyPartPaths`, `BuildZRangeMap`, `LoadShotBuffer`, `ClearReviewerBuffers`, `ReleaseCore`, `ResolveSequence`, `FindLiveShot`, `FindLiveFai`, `FindLiveMeasurement`, `BuildMeasurementKey`, `RaiseStateChanged` |
| 80-01 | UI/Reviewer/ReviewerReinspectViewModel.cs (새) | `class ReviewerReinspectViewModel : INotifyPropertyChanged { static readonly Instance, IsActive, StatusText, CanApply, DisableReasonText, MainNavigator, EvaluateSelection(...), ApplySelection(...), ReleaseByUser() }` + 문구 const |
| 80-01 | RepeatRunService.cs | `SavedCycleRerunPlanner.BuildPartForSingleCycle(CycleResultDto, InspectionSequence, InspectionRecipeManager)`, private `BuildSelectedTickPart(CycleResultDto)` |
| 80-01 | ReviewerWindow.xaml(.cs) | `panel_reinspect`, `btn_applyRowToMain`, `txt_applyRowReason`, 필드 `_reinspectVm`, `Button_ApplyRowToMain_Click` |
| 80-01 | MainView.xaml(.cs) | 툴바 3번째 RowDefinition, `panel_reviewerReinspect`, `txt_reviewerReinspectStatus`, `btn_reviewerRelease`, `BtnReviewerRelease_Click` |
| 80-01 | MainWindow.xaml.cs | `OnReviewerPhotosLoaded(ReviewerReinspectLoadResult)` |
| 80-02 | InspectionSequence.cs | `StoreRecomputedLocalRefLine(EdgeToLineDistanceMeasurement, LocalRefLineResult)`, `MarkDatumFoundFromTeachingPhotos(string)`, `IsDatumTransformFromTeachingPhotos(string)`, private `TeachingPhotoProvenance`, `_teachingPhotoProvenance`, `RecordTeachingPhotoProvenance`, `ForgetTeachingPhotoProvenance`, `RecordHeldDatumProvenance`, `FindDatumConfigByName` (+ HoldManualDatum·ClearDatumTransforms 1줄씩) |
| 80-02 | Action_FAIMeasurement.cs | `TryUseRecomputedLocalRef`, `TryRecomputeStaleLocalRef`, `FindSequenceDatum`, `BuildLocalRefFailReason`, `GetShotNameForLog`, const `LOCAL_REF_RECOMPUTED_TEXT`, `LOCAL_REF_NOT_TEACHING_PHOTO_TEXT` |
| 80-02 | Custom/Sequence/Inspection/DatumTestFindService.cs (새) | `static class DatumTestFindService { LOG_TAG, ERR_* const, IsTaughtForTestFind(DatumConfig), TryRunFromTeachingImages(InspectionSequence, DatumConfig, bool, out string) }` |
| 80-02 | Custom/Sequence/Inspection/LocalRefTestFindService.cs (새) | `class LocalRefTestFindOutcome { Ok, Message, ImagePath, Datum, Overlays }`, `static class LocalRefTestFindService { TITLE, MSG_* const, Run(ParamBase) }` |
| 80-02 | MainView.xaml(.cs) | `btn_testFindLocalRef`, `BtnTestFindLocalRef_Click`, `ShowLocalRefTestFindOutcome(ParamBase, LocalRefTestFindOutcome)` |
| 80-03 | RepeatRunService.cs | `SavedCycleRerunPlanner.IsDatumPhotoSetComplete(SavedCycleRerunPart, InspectionSequence, string)`, private `FindPartContainingCycle`, `SeedSelectedTickPhotos`, const `PREVIOUS_DAY_OFFSET`, `RepeatRunService.ERROR_REVIEWER_PHOTOS_ACTIVE` |
| 80-03 | ReviewerReinspectService.cs | `ReviewerReinspectLoadResult.IsDatumPhotoMissing`, `ReviewerReinspectState.IsJpgPhoto / IsZCandidateMissing / IsDatumPhotoKept`, private `ApplyDatumPhotos`, `ApplyDualPhotos`, `FindOwnedDualMeasurement`, `ApplyReviewerOverlays`, `FindSourceShotDto`, `IsJpgPath`, const `JPG_EXTENSION`, `JPEG_EXTENSION` |
| 80-03 | ReviewerReinspectViewModel.cs | `AlertPresenter`, 알림·안내 문구 const |
| 80-03 | ReviewMeasurementRow.cs | `ReviewerImagePathResolver.ResolveCycleImagePath(CycleResultDto, out ShotResultDto)`, `ReviewerImagePathResolver.CollectShotOverlays(ShotResultDto)` |
| 80-03 | ReviewerWindow.xaml.cs | `ShowReinspectAlert(string, string)` |
| 80-04 | InspectionListView.xaml.cs | `SelectShotAndMeasurement(ShotConfig, FAIConfig, MeasurementBase)`, `FindNodeByParam(NodeViewModel, object)`, `ResolveRunNodeForSelection(NodeViewModel)` |
| 80-04 | MainView.xaml.cs | `ForgetDisplayedShotImage()` |
| 80-04 | ReviewerReinspectService.cs / VM | `enum EReviewerTestFindResult { NotRun, Succeeded, Failed }`, `ReviewerReinspectState.TestFindResult`, private `RunAutoTestFind`, VM `event Action Released` |
| 80-05 | VersionDefine.cs | 버전 1.7.51.0 항목 |
| 80-05 | .planning/phases/80-reviewer-ng-cycle-reinspect/80-HUMAN-UAT.md (새) | UAT 절차서 |

이 plan(80-01)이 만드는 것은 위 표의 80-01 행이다. 저장소 밖 파일: `/c/Users/admin/AppData/Local/Temp/p80/base-80-01.txt`, `base-80-phase.txt`, `build-80-01.txt`.

## Source Coverage Audit (phase 전체 — 모든 항목 COVERED)

| SOURCE | ID | 항목 | Plan | Status |
|---|---|---|---|---|
| GOAL | — | 리뷰어 NG 검사 사진 전부를 한 번에 메인 화면으로 불러와 파라미터 수정·재검사 | 80-01·03·04 | COVERED |
| CONTEXT | D-80-00 | UI 는 단순 — 새 UI 3개뿐, 새 창·설정·확인창 없음 | 80-01(버튼·상태 줄), 80-02(시험 찾기 버튼), 80-05 감사 | COVERED |
| CONTEXT | D-80-01 | 리뷰어 버튼 1개, 원인 설명 바로 아래, 크게 | 80-01 | COVERED |
| CONTEXT | D-80-02 | 비활성 + 이유 한 줄 | 80-01 | COVERED |
| CONTEXT | D-80-03 | 확인창 없이 바로 | 80-01 | COVERED |
| CONTEXT | D-80-04 | 리뷰어 창 열어 둔 채 메인 화면 앞으로 | 80-01(Activate), 80-04(최소화·복원) | COVERED |
| CONTEXT | D-80-05 | NG Shot + 측정 선택 | 80-04 | COVERED |
| CONTEXT | D-80-06 | Test Find 까지만 자동, 사진 출처 선택 대화상자 없이 | 80-02(공용 헬퍼), 80-04 | COVERED |
| CONTEXT | D-80-07 | 같은 자재 모든 Shot + 기준점, GroupIntoParts 같은 규칙 | 80-01(진입점), 80-03 | COVERED |
| CONTEXT | D-80-08 | Z 후보 전부, 없으면 선택 z 한 장 + 안내 | 80-01(사전 주입), 80-03 | COVERED |
| CONTEXT | D-80-09 | 기준점 사진 없음 → 알림 후 Shot 만, Test Find 안 함 | 80-03, 80-04 | COVERED |
| CONTEXT | D-80-10 | 저장 = 파라미터만, 사진 경로는 원래 값 | 80-01 | COVERED |
| CONTEXT | D-80-11 | 해제 4경로, 파라미터 유지, 대체 시 원래 값 유지 | 80-01 | COVERED |
| CONTEXT | D-80-12 | 상태 줄 + 로그 | 80-01, 80-03(안내 추가) | COVERED |
| CONTEXT | D-80-13 | OfflineInspectMode 켜기·되돌리기 | 80-01 | COVERED |
| CONTEXT | D-80-14 | JPG 안내 상태 줄 | 80-03 | COVERED |
| CONTEXT | D-80-15 | 기준 ROI 시험 찾기 | 80-02 | COVERED |
| CONTEXT | D-80-16 | 가독성 규칙 | 전 plan | COVERED |
| CONTEXT | D-80-17 | MVVM | 80-01·02·03·04 | COVERED |
| CONTEXT | D-80-18 | 새 .cs 허용 + csproj 등록 | 80-01, 80-02 | COVERED |
| CONTEXT | D-80-19 | 기준점 사진 일부만 = 없음 | 80-03 | COVERED |
| CONTEXT(함께 처리 1) | — | 리뷰어 사이클 전체 보기 선 겹침 | 80-03 Task 3 | COVERED |
| CONTEXT(함께 처리 2) | — | 수동 RUN Local Ref stale 재계산, PLC 영향 없음 | 80-02 Task 1 | COVERED |
| CONTEXT(함께 처리 3) | — | 기준 ROI 시험 찾기(= D-80-15) | 80-02 Task 2·3 | COVERED |
| RESEARCH | Pattern 1 | 단일 사이클 → 부품, ValidatePart 건너뜀 | 80-01, 80-03 | COVERED |
| RESEARCH | Pattern 2 | 스냅샷/주입/복원 새 서비스 | 80-01, 80-03 | COVERED |
| RESEARCH | Pattern 3 | 자동 Test Find, 비패턴 기준점도 캐시 채움 | 80-02, 80-04 | COVERED |
| RESEARCH | Pattern 4 | stale 재계산(계획 단계 교정: 파일 직접 로드) | 80-02 | COVERED |
| RESEARCH | Pattern 5 | 트리 Shot+측정 프로그램 선택 | 80-04 | COVERED |
| RESEARCH | Pattern 6 | 오버레이 겹침 수정 | 80-03 | COVERED |
| RESEARCH | Pattern 7 | 기준 ROI 시험 찾기 | 80-02 | COVERED |
| RESEARCH | Pitfall 1~5 | Z 후보 게이트 차이 / 캐시 skip / OnRecipeChanged 시점 / 저장 스왑 / 트리 재진입 | 80-03 / 80-02 / 80-01 / 80-01 / 80-04 | COVERED |
| Deferred | — | 재검사 결과와 리뷰어 원래 값 나란히 비교 | 없음(제외) | 범위 밖 |

## 계획 단계 결정 (Claude's Discretion — 근거 포함)

- P-80-1 스냅샷/복원은 RepeatRunService 인스턴스에 얹지 않고 새 정적 서비스 `ReviewerReinspectService` 로 분리한다(RESEARCH 결론). 필드 집합은 RepeatRunService.SavedCycleOverrideSnapshot 과 같다.
- P-80-2 리뷰어 → 메인 연결은 VM 의 훅 속성 `MainNavigator`(MainWindow 가 채움)와 `AlertPresenter`(ReviewerWindow 가 채움, 80-03)로 한다 — PatternBrushMaskViewModel 의 훅 선례. 리뷰어 창은 MainWindow 가 Owner 라 메인 뒤로 갈 수 없으므로 80-04 에서 불러온 뒤 리뷰어를 최소화하고, 메뉴의 리뷰어 버튼을 다시 누르면 복원한다(A-80-E1 로 UAT 판단).
- P-80-3 자동 해제 PLC 경로는 경로 복원이 ProcessTest 보다 반드시 먼저 끝나야 하므로 MainRun 스레드에서 동기로 부른다. VM 갱신만 Dispatcher 로 넘긴다.
- P-80-4 저장 보호는 SequenceHandler.SaveRecipe 를 건드리지 않고 MainWindow.SaveRecipe 의 저장 호출 1줄을 서비스 `RunWithOriginalPaths` 로 감싼다(RESEARCH Pitfall 4).

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): 리뷰어 버튼 → 서비스 → 부품 진입점 → Shot 사진·OfflineInspectMode 적용 → 메인 상태 줄 + [해제] — 한 경로만 끝까지</name>
  <precondition>Debug 앱(DatumMeasurement.exe)이 실행 중이 아니다(빌드 출력 exe 잠금 — 실행 중이면 프로세스를 죽이지 말고 멈춰서 보고)</precondition>
  <reversibility rating="reversible">새 파일 2개와 기존 파일 삽입뿐이고 레시피·설정 파일 형식은 바뀌지 않는다(경로 주입은 메모리 전용)</reversibility>
  <files>WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs, WPF_Example/Custom/Sequence/Inspection/ReviewerReinspectService.cs, WPF_Example/UI/Reviewer/ReviewerReinspectViewModel.cs, WPF_Example/DatumMeasurement.csproj, WPF_Example/UI/Reviewer/ReviewerWindow.xaml, WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs, WPF_Example/UI/ContentItem/MainView.xaml, WPF_Example/UI/ContentItem/MainView.xaml.cs, WPF_Example/MainWindow.xaml.cs</files>
  <read_first>
    - CLAUDE.md (Code Style 🔒 가독성 규칙 1~5 전부, "UI 는 MVVM", Error Handling, Logging)
    - .planning/phases/80-reviewer-ng-cycle-reinspect/80-CONTEXT.md (D-80-00~03, D-80-11~13, D-80-16~18)
    - .planning/phases/80-reviewer-ng-cycle-reinspect/80-PATTERNS.md ("ReviewerReinspectService.cs", "ReviewerReinspectViewModel.cs", "DatumMeasurement.csproj" 절)
    - WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs:385-610, :700-830, :1085-1260, :1320-1560
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml (전체) 와 ReviewerWindow.xaml.cs:1-340
    - WPF_Example/UI/Reviewer/AlignVerifyViewModel.cs:1-80 와 AlignVerifyWindow.xaml.cs (VM 배선 선례)
    - WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs (ReviewMeasurementRow, ReviewerImagePathResolver)
    - WPF_Example/UI/ViewModel/CycleResultDto.cs:1-240
    - WPF_Example/UI/ContentItem/MainView.xaml:130-160, :285-310, :440-560
    - WPF_Example/UI/ContentItem/MainView.xaml.cs:83-130
    - WPF_Example/MainWindow.xaml.cs:77-104
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs:1-40, :150-240, :480-500, :620-665
    - WPF_Example/DatumMeasurement.csproj 의 Compile Include 목록(RepeatRunService.cs :277, AlignVerifyViewModel.cs :414 근처)
  </read_first>
  <action>
모든 새 코드는 CLAUDE.md 🔒 규칙(D-80-16)을 지킨다: 조건 연산자 3종(삼항 / null 병합 / null 조건)과 switch 식을 쓰지 않고 if/else·명시적 null 비교·전통 switch(+break)만 쓴다. 한 줄 분기도 중괄호. &&/|| 가 3개 이상이면 이름 있는 bool 로 먼저 뽑는다. if 중첩은 2단계까지, 넘으면 메서드를 쪼갠다. 헝가리언(b/n/sz/d/hv), 숫자·문구는 const. 날짜 주석을 새로 달지 않고 비자명한 이유만 "// Phase 80 D-80-xx: ..." 형식으로 짧게 적는다. 새 파일은 AlignVerifyViewModel.cs 처럼 Allman, 기존 파일은 그 파일의 괄호 스타일을 따른다. 문자열·주석에 물음표 문자를 넣지 않는다(검증 grep 이 물음표를 센다).

(0) 편집 전 기준: `mkdir -p /c/Users/admin/AppData/Local/Temp/p80`, `git rev-parse HEAD > /c/Users/admin/AppData/Local/Temp/p80/base-80-01.txt`, 그리고 `base-80-phase.txt` 가 없을 때만 같은 해시를 `base-80-phase.txt` 로 복사한다.

(1) RepeatRunService.cs — `SavedCycleRerunPlanner` 안, BuildPlan 이 끝나는 중괄호(:1242) 바로 아래에 새 메서드 2개만 삽입한다(기존 줄 삭제·수정 0):
 - `public static SavedCycleRerunPart BuildPartForSingleCycle(CycleResultDto selectedCycle, InspectionSequence seq, InspectionRecipeManager recipeManager)`: 요약 주석(리뷰어가 고른 사이클 하나가 속한 부품을 돌려준다. ValidatePart 를 타지 않으므로 기준점 사진이 없어도 부품은 돌아온다 — 없음 판단은 ReviewerReinspectService, D-80-07/09). `bool bMissingArgs` 로 세 인자 null 검사 → null 반환. `SavedCycleRerunPart part = BuildSelectedTickPart(selectedCycle);` 다음 `FillPartDualPhotos(part, seq, recipeManager);` 후 part 반환. 같은 자재 묶기(GroupIntoParts)는 80-03 Task 1 이 이 메서드 본문에 더한다.
 - `private static SavedCycleRerunPart BuildSelectedTickPart(CycleResultDto selectedCycle)`: 새 부품, `StartTime = selectedCycle.InspectionTime`, `IndexNumber = selectedCycle.IndexNumber`, `Ticks.Add(selectedCycle)`, 이어서 `FillPartDatumPhotos(part)`, `FillPartShotPhotos(part)`, `FillPartZRangePhotos(part)` 를 불러 채워 반환.

(2) 새 파일 `WPF_Example/Custom/Sequence/Inspection/ReviewerReinspectService.cs` (namespace ReringProject.Sequence, using System / System.Collections.Generic / System.IO / HalconDotNet / ReringProject.Setting / ReringProject.UI / ReringProject.Utility). 한 파일에 공개 타입 4개:
 - `public enum EReviewerRowBlock { None, NoRow, NoPhoto, RecipeMismatch, SequenceNotFound, ShotNotFound, SequenceBusy, SavedCycleRerunActive }`
 - `public class ReviewerReinspectLoadResult` — 자동 속성 `bool IsLoaded`, `EReviewerRowBlock BlockReason`, `string BlockDetail`, `ShotConfig LiveShot`, `FAIConfig LiveFai`, `MeasurementBase LiveMeasurement`.
 - `public class ReviewerReinspectState` — 자동 속성 `bool IsActive`, `DateTime CycleTime`, `int IndexNumber`, `string ShotName`, `string MeasurementName`, 그리고 같은 값을 복사한 새 객체를 돌려주는 `public ReviewerReinspectState Clone()`.
 - `public static class ReviewerReinspectService`:
   * const: `LOG_TAG = "[ReviewerLoad] "`, `RELEASE_REASON_USER = "사용자 해제"`, `RELEASE_REASON_PLC_TEST = "PLC 자동 검사 수신"`, `RELEASE_REASON_RECIPE_CHANGED = "레시피 변경"`, `RELEASE_REASON_SHUTDOWN = "프로그램 종료"`, `LOG_TIME_FORMAT = "yyyy-MM-dd HH:mm:ss"`.
   * private sealed nested `ReviewerOverrideSnapshot`: RepeatRunService.SavedCycleOverrideSnapshot(:442-453)과 같은 사전 5개 + `List<ShotConfig> OwnedShots` + `InspectionSequence Sequence` + `bool OfflineBefore` + `bool OfflineSetByReviewer`.
   * static 상태: `private static readonly object s_lock`, `s_snapshot`(null = 해제 상태), `SavedCycleRerunPart s_activePart`, `ReviewerReinspectState s_state`, `List<ShotConfig> s_lstBufferedShots`.
   * `public static event Action StateChanged;` 와 private `RaiseStateChanged()` — 핸들러를 지역 변수로 복사, null 이 아닐 때 try 안에서 호출, catch 는 Logging.PrintErrLog 후 삼킨다. 잠금 밖에서만 부른다.
   * `public static bool IsActive` — lock 안에서 `s_snapshot != null`.
   * `public static ReviewerReinspectState GetState()` — lock 안에서 활성이면 `s_state.Clone()`, 아니면 `IsActive = false` 인 새 객체.
   * `public static EReviewerRowBlock CheckRow(CycleResultDto cycle, ShotResultDto shotDto, FaiResultDto faiDto, out string szDetail)` — szDetail 은 먼저 빈 문자열. 순서: shotDto null → NoRow. `ReviewerImagePathResolver.ResolveRowImagePath(shotDto, faiDto)` 가 빈 문자열/null → NoPhoto. cycle null 이거나 `cycle.RecipeName` 이 `SystemHandler.Handle.Setting.CurrentRecipeName` 과 Ordinal 로 다르면 RecipeMismatch(szDetail = cycle.RecipeName, cycle null 이면 빈 문자열). `ResolveSequence(shotDto.OwnerSequenceName)` 가 null → SequenceNotFound(szDetail = 해석한 소유 이름). `FindLiveShot(seq, shotDto.ShotName)` null → ShotNotFound(szDetail = ShotName). 그 밖 None.
   * private `ResolveSequence(string szOwnerName)` — 비었으면 `SequenceHandler.SEQ_TOP`(RepeatRunService.cs:1302-1310 규칙), `SystemHandler.Handle.Sequences` 를 `Count`/`this[int]` 로 돌며 `as InspectionSequence` 이고 Name 이 OrdinalIgnoreCase 로 같으면 반환. `FindLiveShot(seq, szShotName)` — `SystemHandler.Handle.Sequences.RecipeManager.Shots` 중 `InspectionSequence.IsShotOwnedBySequence(shot, seq.Name)` 이고 ShotName Ordinal 일치. `FindLiveFai(shot, szFaiName)` — FAIList 에서 FAIName Ordinal. `FindLiveMeasurement(fai, szKey)` 와 `BuildMeasurementKey(string szName, string szTypeName)`(이름이 비면 TypeName — RepeatRunService.cs:805-813 규칙). RecipeManager·Shots null 이면 null.
   * `public static ReviewerReinspectLoadResult LoadForRow(CycleResultDto cycle, ShotResultDto shotDto, FaiResultDto faiDto, MeasurementResultDto measDto)` — UI 스레드 전용(요약 주석에 적는다):
     a. 잠금 밖: CheckRow → None 이 아니면 `IsLoaded = false`, BlockReason, BlockDetail 을 채워 반환. seq · liveShot · liveFai(faiDto 가 있으면) · liveMeas(measDto 가 있으면 BuildMeasurementKey(measDto.MeasurementName, measDto.TypeName)) 해석. `seq.State != EContextState.Idle` → SequenceBusy, `RepeatRunService.IsSavedCycleRerunActive` → SavedCycleRerunActive 로 막는다(버튼은 활성 그대로, 이유만 표시). `SavedCycleRerunPart part = SavedCycleRerunPlanner.BuildPartForSingleCycle(cycle, seq, SystemHandler.Handle.Sequences.RecipeManager);` — 부품 만들기는 파일만 읽으므로 잠금 밖.
     b. lock(s_lock): 활성 스냅샷의 Sequence 가 이번 seq 와 다르면 `RestorePathsOnly(옛 스냅샷)` · `ClearReviewerBuffers()` · `옛 Sequence.ClearDatumTransforms()` 를 하고 OfflineBefore·OfflineSetByReviewer 를 넘겨받은 새 스냅샷을 뜬다. 스냅샷이 없으면 `BuildSnapshot(seq, recipeManager)`(OfflineBefore = 지금 값). 같은 seq 로 이미 활성이면 스냅샷은 그대로 두고 `RestorePathsOnly(s_snapshot)` 만 한다(D-80-11 대체 규칙: 원래 값은 처음 것 유지 — RepeatRunService.ApplySavedCyclePart:708-710 와 같은 "먼저 전부 원복 후 덮어쓰기").
     c. `ApplyPartPaths(part)` — 스냅샷 OwnedShots 마다: `part.ShotPhotoPaths` 에 그 ShotName 이 있고 `ReviewerImagePathResolver.IsUsableImageFile(경로)` 이면 `SimulImagePath` 에 넣는다. 모든 소유 Shot 의 `RerunZRangeImagePaths = BuildZRangeMap(part, shot.ShotName)`(RepeatRunService.BuildRerunZRangeMap:769-782 와 같은 규칙으로 항상 새 사전 — 비어 있으면 오프라인 폴더를 읽지 않고 1장 폴백, Action_FAIMeasurement.cs:2337-2348).
     d. `SystemSetting.Handle.OfflineInspectMode` 가 false 면 true 로 바꾸고 `s_snapshot.OfflineSetByReviewer = true`(메모리에서만, Setting.Save 호출 금지 — D-80-13, RepeatRunService.cs:584-589 와 같음).
     e. `seq.ClearDatumTransforms();` — 옛 사진으로 잡아 둔 기준점 캐시와 Test Find 유지 표시를 비운다(RepeatRunService.cs:692 와 같은 이유).
     f. `LoadShotBuffer(liveShot)` — `ClearReviewerBuffers()` 뒤에, 새 경로가 쓸 수 있는 파일이면 `HImage img = null; try { img = new HImage(경로); liveShot.SetImage(img); s_lstBufferedShots 에 추가 } catch (Exception ex) { Logging.PrintErrLog } finally { if (img != null) { try { img.Dispose(); } catch { } } }` — 메인 캔버스가 Shot 버퍼를 먼저 보기 때문이다(MainView.DisplayShotImage). `ClearReviewerBuffers()` 는 s_lstBufferedShots 의 Shot 마다 `ClearImage()` 후 목록을 비운다.
     g. `s_activePart = part`; `s_state` = 새 ReviewerReinspectState(IsActive true, CycleTime = cycle.InspectionTime, IndexNumber = cycle.IndexNumber 인데 그 값이 `RepeatRunService.MATERIAL_NOT_SET` 이면 part.IndexNumber, ShotName = liveShot.ShotName, MeasurementName = measDto 키 또는 빈 문자열).
     h. Trace 로그 1줄(D-80-12): `LOG_TAG + "불러옴 — " + seq.Name + " · " + 시각(LOG_TIME_FORMAT) + " · 자재 " + 번호 + " · Shot 사진 " + 적용 수 + "장 · OfflineInspectMode 켬=" + OfflineSetByReviewer`. `(int)ELogType.Trace` 로 캐스팅.
     i. 잠금 해제 후 `RaiseStateChanged()`, 결과 `IsLoaded = true` + Live 참조 반환.
   * private `BuildSnapshot(InspectionSequence seq, InspectionRecipeManager recipeManager)` / `RestorePathsOnly(ReviewerOverrideSnapshot snap)` — RepeatRunService.BuildOverrideSnapshot(:455-491) / RestoreOverridePathsOnly(:495-521) 와 같은 필드·같은 순서로 옮겨 쓴다(끝에 소유 Shot 의 RerunZRangeImagePaths = null).
   * `public static void Release(string szReason)` → `ReleaseCore(szReason, false)`, `public static void ReleaseByUser()` → `ReleaseCore(RELEASE_REASON_USER, true)`(두 번째 인자는 UI 스레드 전용 화면 상태 정리 여부 — 80-03 이 선 지우기를 여기에 단다).
   * private `ReleaseCore(string szReason, bool bClearDisplayState)`: 전체를 try/catch(Exception) 로 감싸 절대 throw 하지 않는다(MainRun 스레드 보호) — catch 는 Logging.PrintErrLog. lock 안: s_snapshot null 이면 아무것도 안 하고 끝(로그 없음). `RestorePathsOnly` → `bool bShouldTurnOff = s_snapshot.OfflineSetByReviewer && SystemSetting.Handle.OfflineInspectMode;` 이면 false(Save 없음, RepeatRunService.cs:1076-1080) → `s_snapshot.Sequence.ClearDatumTransforms()` → `ClearReviewerBuffers()` → Trace 로그 `LOG_TAG + "해제 — " + szReason` → s_snapshot · s_activePart · s_state 비움. 해제한 경우에만 잠금 밖에서 `RaiseStateChanged()`.

(3) 새 파일 `WPF_Example/UI/Reviewer/ReviewerReinspectViewModel.cs` (namespace ReringProject.UI, using System / System.ComponentModel / System.Windows / ReringProject.Sequence): `public class ReviewerReinspectViewModel : INotifyPropertyChanged`, AlignVerifyViewModel.cs:29-52 의 Raise 패턴 그대로.
 - `public static readonly ReviewerReinspectViewModel Instance = new ReviewerReinspectViewModel();`, private 생성자에서 `ReviewerReinspectService.StateChanged += OnServiceStateChanged;`.
 - 속성(setter 에서 Raise): `bool IsActive`, `string StatusText`(기본 ""), `bool CanApply`, `string DisableReasonText`(기본 REASON_NO_ROW).
 - 훅: `public Action<ReviewerReinspectLoadResult> MainNavigator { get; set; }`.
 - const 문구(쉬운 한국어 한 줄, D-80-00): `STATUS_PREFIX = "리뷰어 사진 사용 중 — "`, `STATUS_TIME_FORMAT = "MM-dd HH:mm"`, `STATUS_MATERIAL_PREFIX = " 자재 "`, `STATUS_MATERIAL_NONE = " 자재번호 없음"`, `REASON_NO_ROW = "측정 행을 먼저 고르세요"`, `REASON_NO_PHOTO = "이 검사의 사진 파일이 없어 불러올 수 없습니다"`, `REASON_RECIPE_MISMATCH_PREFIX = "지금 레시피와 다른 레시피의 검사입니다 — 검사 레시피: "`, `REASON_SEQUENCE_NOT_FOUND_PREFIX = "이 PC 에 없는 검사입니다 — "`, `REASON_SHOT_NOT_FOUND_PREFIX = "지금 레시피에 이 Shot 이 없습니다 — "`, `REASON_SEQUENCE_BUSY = "검사가 진행 중입니다 — 끝난 뒤 다시 누르세요"`, `REASON_RERUN_ACTIVE = "저장 사진 재검사 중에는 쓸 수 없습니다"`.
 - `public void EvaluateSelection(CycleResultDto cycle, ReviewMeasurementRow row)`: row null → CanApply false, DisableReasonText = REASON_NO_ROW. 아니면 `ReviewerReinspectService.CheckRow(cycle, row.OwnerShot, row.OwnerFai, out szDetail)` → CanApply = (None), DisableReasonText = `BuildReasonText(block, szDetail)`.
 - `public void ApplySelection(CycleResultDto cycle, ReviewMeasurementRow row)`: row null 이면 끝. `ReviewerReinspectService.LoadForRow(cycle, row.OwnerShot, row.OwnerFai, row.Source)` → IsLoaded false 면 DisableReasonText 만 바꾸고 끝(대화상자 없음, D-80-03). true 면 DisableReasonText = "", MainNavigator 가 null 이 아니면 호출, `RefreshFromService()`.
 - `public void ReleaseByUser()` → `ReviewerReinspectService.ReleaseByUser();`.
 - private `OnServiceStateChanged()` — `Application.Current` 가 null 이면 끝. `Dispatcher.CheckAccess()` 면 바로 `RefreshFromService()`, 아니면 `BeginInvoke(new Action(RefreshFromService))`(PLC 해제는 MainRun 백그라운드 스레드에서 온다).
 - private `RefreshFromService()` — `GetState()` → IsActive, StatusText(활성일 때 `BuildStatusText(state)`, 아니면 "").
 - private `BuildStatusText(ReviewerReinspectState state)` — STATUS_PREFIX + CycleTime.ToString(STATUS_TIME_FORMAT) + (IndexNumber 가 0 이상이면 STATUS_MATERIAL_PREFIX + 번호, 아니면 STATUS_MATERIAL_NONE) — if/else. private `BuildReasonText(EReviewerRowBlock block, string szDetail)` — 전통 switch, case 마다 break, None 은 "".

(4) DatumMeasurement.csproj — 기존 `<Compile Include="Custom\Sequence\Inspection\RepeatRunService.cs" />` 근처에 `<Compile Include="Custom\Sequence\Inspection\ReviewerReinspectService.cs" />`, `<Compile Include="UI\Reviewer\AlignVerifyViewModel.cs" />` 근처에 `<Compile Include="UI\Reviewer\ReviewerReinspectViewModel.cs" />` 를 단독 항목으로 더한다(D-80-18, DependentUpon 없음). 기존 항목·줄 끝 형식은 건드리지 않는다.

(5) ReviewerWindow.xaml — `border_ngCause` StackPanel 안 `txt_ngCausePanel` 바로 다음에 `<Grid x:Name="panel_reinspect" Margin="0,8,0,0">`(ColumnDefinitions: Auto, *) 를 넣는다: Grid.Column 0 `<Button x:Name="btn_applyRowToMain" Content="이 사진으로 파라미터 수정" FontSize="15" FontWeight="Bold" Padding="14,8" MinHeight="38" Background="#1565C0" Foreground="White" IsEnabled="{Binding CanApply}" Click="Button_ApplyRowToMain_Click" ToolTip="이 검사의 사진(같은 자재의 Shot·기준점 사진)을 메인 화면에 불러옵니다. 레시피의 원래 사진 경로는 바뀌지 않습니다"/>`, Grid.Column 1 `<TextBlock x:Name="txt_applyRowReason" Text="{Binding DisableReasonText}" FontSize="12" Foreground="#B45309" TextWrapping="Wrap" VerticalAlignment="Center" Margin="10,0,0,0"/>`. 다른 줄은 건드리지 않는다(D-80-01/02, 더블클릭·우클릭 메뉴 없음).

(6) ReviewerWindow.xaml.cs — 배선만(D-80-17): 필드 `private readonly ReviewerReinspectViewModel _reinspectVm = ReviewerReinspectViewModel.Instance;`, 생성자 InitializeComponent 다음 `panel_reinspect.DataContext = _reinspectVm;` 와 `_reinspectVm.EvaluateSelection(null, null);`(VM 이 싱글턴이라 창을 다시 열 때 이전 활성 상태가 남지 않게), DisplayCycle 의 `_selectedRow = null;` 다음 줄 `_reinspectVm.EvaluateSelection(cycle, null);`, MeasurementGrid_SelectionChanged 의 `_selectedRow = row;` 다음 줄 `_reinspectVm.EvaluateSelection(_currentCycle, row);`, 새 핸들러 `private void Button_ApplyRowToMain_Click(object sender, RoutedEventArgs e)` 본문 1문장 `_reinspectVm.ApplySelection(_currentCycle, _selectedRow);`(한 줄 요약 주석 "// Phase 80 D-80-01/03: 확인창 없이 바로 불러온다 — 로직은 VM·서비스").

(7) MainView.xaml — `canvasToolbar` 안 Grid 의 RowDefinitions 끝에 `<RowDefinition Height="Auto"/>` 1개를 더하고(기존 자식은 Row 0/1 그대로), `brushPanel` 다음에 상태 줄을 넣는다: `<Border x:Name="panel_reviewerReinspect" Grid.Row="2" Grid.ColumnSpan="3" Margin="0,6,0,0" Padding="10,4" CornerRadius="4" Background="#FEF3C7" BorderBrush="#F59E0B" BorderThickness="1">` — Visibility 속성은 쓰지 않고 `<Border.Style><Style TargetType="Border">` 에서 기본 Setter Visibility=Collapsed + `<DataTrigger Binding="{Binding IsActive}" Value="True">` 에서 Visible(border_angleValidationBadge :496-511 선례). 안에 DockPanel: `<Button x:Name="btn_reviewerRelease" DockPanel.Dock="Right" Content="해제" MinWidth="70" Height="28" FontSize="13" FontWeight="SemiBold" Margin="10,0,0,0" Click="BtnReviewerRelease_Click" ToolTip="불러온 리뷰어 사진을 내려놓고 원래 사진으로 되돌립니다 (고친 파라미터는 그대로)"/>` 와 `<TextBlock x:Name="txt_reviewerReinspectStatus" Text="{Binding StatusText}" FontSize="14" FontWeight="SemiBold" Foreground="#92400E" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>`. Row 0 툴바라 HALCON 창(Row 1) 위에 겹치지 않는다(airspace 안전).

(8) MainView.xaml.cs — 생성자 InitializeComponent 다음 `panel_reviewerReinspect.DataContext = ReviewerReinspectViewModel.Instance;` 1줄과 핸들러 `private void BtnReviewerRelease_Click(object sender, RoutedEventArgs e)` 본문 1문장 `ReviewerReinspectViewModel.Instance.ReleaseByUser();` 만 더한다(K&R, 비대한 code-behind 에 로직 추가 금지 — CLAUDE.md 규칙 4).

(9) MainWindow.xaml.cs — 생성자의 `OnRecipeChanged += this.OnLoadRecipe;` 다음 줄 `ReviewerReinspectViewModel.Instance.MainNavigator = OnReviewerPhotosLoaded;`, 새 메서드 `private void OnReviewerPhotosLoaded(ReviewerReinspectLoadResult result)` 본문 `Activate();`(K&R, 요약 주석 "// Phase 80 D-80-04: 리뷰어에서 불러온 뒤 메인 화면을 앞으로 — 리뷰어 최소화·트리 선택은 80-04").

끝나면 `git add` 로 이 Task 의 파일만 올리고 커밋한다(stash·checkout·restore 금지).
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p80; PB=$(cat $H/base-80-01.txt); W=WPF_Example
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" $W/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-80-01.txt 2>&1; echo "build_exit=$? cs_errors=$(grep -c 'error CS' $H/build-80-01.txt)"
SVC=$W/Custom/Sequence/Inspection/ReviewerReinspectService.cs; VM=$W/UI/Reviewer/ReviewerReinspectViewModel.cs
for F in $SVC $VM; do test -f $F || echo "MISSING $F"; echo "$(basename $F) ternary=$(grep -cE '\?[^?]*:' $F) coalesce=$(grep -cF '??' $F) nullcond=$(grep -cF '?.' $F) switchexpr=$(grep -cE 'switch.*=>' $F) datesig=$(grep -cF 'hbk' $F) qmark=$(grep -cF '?' $F) logic3=$(grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)' $F)"; done
for F in $W/Custom/Sequence/Inspection/RepeatRunService.cs $W/DatumMeasurement.csproj $W/UI/Reviewer/ReviewerWindow.xaml $W/UI/Reviewer/ReviewerWindow.xaml.cs $W/UI/ContentItem/MainView.xaml $W/UI/ContentItem/MainView.xaml.cs $W/MainWindow.xaml.cs; do ADD=$(git diff -w -U0 $PB -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "$(basename $F) deleted=$(git diff -w -U0 $PB -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') qmark=$(printf '%s\n' "$ADD" | grep -cF '?') logic3=$(printf '%s\n' "$ADD" | grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)')"; done
echo "csproj_svc=$(grep -cF 'Compile Include="Custom\Sequence\Inspection\ReviewerReinspectService.cs"' $W/DatumMeasurement.csproj) csproj_vm=$(grep -cF 'Compile Include="UI\Reviewer\ReviewerReinspectViewModel.cs"' $W/DatumMeasurement.csproj)"
echo "chain_btn=$(grep -cF 'Click="Button_ApplyRowToMain_Click"' $W/UI/Reviewer/ReviewerWindow.xaml) chain_enabled=$(grep -cF 'IsEnabled="{Binding CanApply}"' $W/UI/Reviewer/ReviewerWindow.xaml) chain_reason=$(grep -cF '{Binding DisableReasonText}' $W/UI/Reviewer/ReviewerWindow.xaml) btn_text=$(grep -cF '이 사진으로 파라미터 수정' $W/UI/Reviewer/ReviewerWindow.xaml)"
echo "chain_apply=$(grep -cF '_reinspectVm.ApplySelection(_currentCycle, _selectedRow);' $W/UI/Reviewer/ReviewerWindow.xaml.cs) chain_eval=$(grep -cF '_reinspectVm.EvaluateSelection(' $W/UI/Reviewer/ReviewerWindow.xaml.cs) chain_load=$(grep -cF 'ReviewerReinspectService.LoadForRow(' $VM) chain_part=$(grep -cF 'SavedCycleRerunPlanner.BuildPartForSingleCycle(' $SVC)"
echo "handler_apply_stmts=$(tr -d '\r' < $W/UI/Reviewer/ReviewerWindow.xaml.cs | awk '/private void Button_ApplyRowToMain_Click/,/^        }$/' | grep -c ';') handler_release_stmts=$(tr -d '\r' < $W/UI/ContentItem/MainView.xaml.cs | awk '/private void BtnReviewerRelease_Click/,/^        }$/' | grep -c ';')"
echo "status_bind=$(grep -cF '{Binding StatusText}' $W/UI/ContentItem/MainView.xaml) status_trigger=$(grep -cF 'Binding="{Binding IsActive}"' $W/UI/ContentItem/MainView.xaml) release_click=$(grep -cF 'Click="BtnReviewerRelease_Click"' $W/UI/ContentItem/MainView.xaml) release_call=$(grep -cF 'ReviewerReinspectViewModel.Instance.ReleaseByUser();' $W/UI/ContentItem/MainView.xaml.cs) navigator=$(grep -cF 'MainNavigator = OnReviewerPhotosLoaded' $W/MainWindow.xaml.cs)"
echo "no_save_in_svc=$(grep -cE '(Setting|Handle)\.Save\(\)' $SVC) offline_on=$(grep -c 'OfflineSetByReviewer = true' $SVC) clear_datum=$(grep -c 'ClearDatumTransforms()' $SVC) dispose_finally=$(grep -c 'Dispose(); } catch { }' $SVC)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `build_exit=0 cs_errors=0`
    - 새 파일 2개 줄: `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 qmark=0 logic3=0`
    - 기존 파일 7개 줄: `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 qmark=0 logic3=0`, 그리고 `deleted=0`(이 Task 는 삽입만)
    - `csproj_svc=1 csproj_vm=1`
    - `chain_btn=1 chain_enabled=1 chain_reason=1 btn_text=1`, `chain_apply=1 chain_eval=3`(생성자 초기화 + DisplayCycle + 행 선택), `chain_load>=1 chain_part>=1`
    - `handler_apply_stmts=1 handler_release_stmts=1` (code-behind 핸들러 본문은 1문장 — D-80-17)
    - `status_bind=1 status_trigger=1 release_click=1 release_call=1 navigator=1`
    - `no_save_in_svc=0 offline_on=1`, `clear_datum>=2`(불러오기·해제), `dispose_finally>=1`
  </acceptance_criteria>
  <done>리뷰어에서 행을 고르면 버튼 활성·이유가 VM 으로 표시되고, 누르면 고른 사이클의 Shot 사진이 레시피 객체(메모리)에 들어가며 OfflineInspectMode 가 메모리에서 켜지고, 메인 화면에 상태 줄과 [해제] 가 뜨며 [해제] 가 원래 경로·모드를 되돌리는 경로가 빌드되고 커밋되었다.</done>
</task>

<task type="auto">
  <name>Task 2: 운영 레시피 보호 — 저장 시 원래 사진 경로(D-80-10) + PLC $TEST·레시피 변경·프로그램 종료 자동 해제(D-80-11)</name>
  <reversibility rating="reversible">MainWindow.SaveRecipe 의 저장 호출 1줄과 해제 호출 3줄을 더할 뿐이고, 저장 파일 형식·PLC 프로토콜은 그대로다</reversibility>
  <files>WPF_Example/Custom/Sequence/Inspection/ReviewerReinspectService.cs, WPF_Example/MainWindow.xaml.cs, WPF_Example/Custom/SystemHandler.cs</files>
  <read_first>
    - CLAUDE.md (🔒 가독성 규칙, Error Handling — MainRun 스레드에서 예외 금지)
    - .planning/phases/80-reviewer-ng-cycle-reinspect/80-RESEARCH.md "Pitfall 3" "Pitfall 4" 와 "자동 해제 훅 3곳"
    - WPF_Example/Custom/Sequence/Inspection/ReviewerReinspectService.cs (Task 1 결과 전체)
    - WPF_Example/MainWindow.xaml.cs:77-104, :282-319, :452-471
    - WPF_Example/Custom/SystemHandler.cs:73-150, :290-315
    - WPF_Example/Sequence/SequenceHandler.cs:15-50, :150-180
    - WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs:385-430 (종료 시 복원 선례)
  </read_first>
  <action>
(1) ReviewerReinspectService.cs 에 공개 메서드 2개를 더한다(Allman, 요약 주석 포함):
 - `public static bool RunWithOriginalPaths(Func<bool> fnSave)` — fnSave 가 null 이면 false. lock(s_lock) 안: s_snapshot 이 null 이면 `return fnSave();`. 활성이면 `RestorePathsOnly(s_snapshot)` 뒤 `try { bool bSaved = fnSave(); Trace 로그 LOG_TAG + "레시피 저장 — 사진 경로는 불러오기 전 값으로 저장, 파라미터는 저장됨"; return bSaved; } finally { ApplyPartPaths(s_activePart); }` — 저장 뒤(예외여도) 불러온 경로를 다시 넣는다(D-80-10, RESEARCH Pitfall 4). 버퍼·기준점 캐시·OfflineInspectMode 는 건드리지 않는다. 80-03 이 ApplyPartPaths 에 기준점·두 장짜리 경로를 더하면 저장 뒤 재적용에도 그대로 따라온다(같은 메서드).
 - `public static void HandleRecipeChanged(object sender, RecipeChangedEventArgs args)` — 본문 `Release(RELEASE_REASON_RECIPE_CHANGED);`. 요약 주석: OnRecipeChanged 는 새 레시피를 읽은 뒤 발화하므로 옛 Shot 객체의 경로 복원은 무해하고, 꼭 필요한 것은 상태 해제와 OfflineInspectMode 복원이다(RESEARCH Pitfall 3).

(2) MainWindow.xaml.cs (K&R):
 - 생성자: Task 1 이 넣은 MainNavigator 줄 다음에 `mSystemHandler.Sequences.OnRecipeChanged += ReviewerReinspectService.HandleRecipeChanged;`.
 - SaveRecipe: :314 의 `if(!mSystemHandler.Sequences.SaveRecipe(name, ERecipeFileType.Ini)) {` 한 줄을 `bool bSaved = ReviewerReinspectService.RunWithOriginalPaths(람다);` + `if(!bSaved) {` 로 바꾼다. 람다는 인자 없이 `mSystemHandler.Sequences.SaveRecipe(name, ERecipeFileType.Ini)` 를 돌려주는 식 람다다(C# 3 문법, 7.2 허용). 그 위 IsSavedCycleRerunActive·mixedShot 차단과 아래 메시지 줄은 그대로 둔다. 주석 "// Phase 80 D-80-10: 리뷰어 사진을 쓰는 중이면 저장하는 순간만 원래 사진 경로로 — 파라미터는 저장된다".
 - Window_Closing: `RepeatRunService.RestoreActiveSavedCycleOverridesForShutdown();` 다음, `mSystemHandler.Release();` 앞에 `ReviewerReinspectService.Release(ReviewerReinspectService.RELEASE_REASON_SHUTDOWN);` — Release() 안의 Setting.Save 가 켜 둔 OfflineInspectMode 를 영속화하지 않게 반드시 그 앞(D-80-11/13).

(3) Custom/SystemHandler.cs — MainRun 의 `case VisionRequestType.Test:` 안, AutoLogout 줄 다음·`ForceOfflineInspectModeOffForAutoTest();` 바로 앞에 `ReviewerReinspectService.Release(ReviewerReinspectService.RELEASE_REASON_PLC_TEST);` 1줄과 주석 "// Phase 80 D-80-11: PLC 자동 검사가 리뷰어 사진·오프라인 모드를 쓰지 않도록 ProcessTest 전에 먼저 되돌린다". 이 스레드는 백그라운드라 경로 복원은 동기로 끝나고 VM 갱신만 Dispatcher 로 넘어간다(Task 1 의 OnServiceStateChanged). 다른 줄은 건드리지 않는다.

끝나면 `git add` 로 세 파일만 커밋한다.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p80; PB=$(cat $H/base-80-01.txt); W=WPF_Example
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" $W/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-80-01.txt 2>&1; echo "build_exit=$? cs_errors=$(grep -c 'error CS' $H/build-80-01.txt)"
SVC=$W/Custom/Sequence/Inspection/ReviewerReinspectService.cs
echo "svc ternary=$(grep -cE '\?[^?]*:' $SVC) coalesce=$(grep -cF '??' $SVC) nullcond=$(grep -cF '?.' $SVC) switchexpr=$(grep -cE 'switch.*=>' $SVC) datesig=$(grep -cF 'hbk' $SVC) qmark=$(grep -cF '?' $SVC) logic3=$(grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)' $SVC)"
for F in $W/MainWindow.xaml.cs $W/Custom/SystemHandler.cs; do ADD=$(git diff -w -U0 $PB -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "$(basename $F) deleted=$(git diff -w -U0 $PB -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') qmark=$(printf '%s\n' "$ADD" | grep -cF '?')"; done
echo "save_wrap=$(grep -cF 'ReviewerReinspectService.RunWithOriginalPaths(' $W/MainWindow.xaml.cs) recipe_sub=$(grep -cF 'OnRecipeChanged += ReviewerReinspectService.HandleRecipeChanged;' $W/MainWindow.xaml.cs) rerun_block_kept=$(grep -cF 'if(RepeatRunService.IsSavedCycleRerunActive) {' $W/MainWindow.xaml.cs)"
echo "closing_order_ok=$(tr -d '\r' < $W/MainWindow.xaml.cs | awk '/private void Window_Closing/,/^        }$/' | awk '/RELEASE_REASON_SHUTDOWN/{a=NR} /mSystemHandler\.Release\(\);/{b=NR} END{ if (a>0 && b>a) print 1; else print 0 }')"
echo "plc_order_ok=$(tr -d '\r' < $W/Custom/SystemHandler.cs | awk '/case VisionRequestType.Test:/,/break;/' | awk '/RELEASE_REASON_PLC_TEST/{a=NR} /ForceOfflineInspectModeOffForAutoTest\(\);/{b=NR} /ProcessTest\(/{c=NR} END{ if (a>0 && b>a && c>b) print 1; else print 0 }')"
echo "save_finally=$(tr -d '\r' < $SVC | awk '/public static bool RunWithOriginalPaths/,/^        }$/' | grep -c 'finally') save_reapply=$(tr -d '\r' < $SVC | awk '/public static bool RunWithOriginalPaths/,/^        }$/' | grep -c 'ApplyPartPaths(s_activePart);') release_trycatch=$(tr -d '\r' < $SVC | awk '/private static void ReleaseCore/,/^        }$/' | grep -c 'catch (Exception')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `build_exit=0 cs_errors=0`
    - `svc ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 qmark=0 logic3=0`
    - `MainWindow.xaml.cs deleted=1`(저장 호출 줄 1개 교체), `SystemHandler.cs deleted=0`, 두 파일 더한 줄 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 qmark=0`
    - `save_wrap=1 recipe_sub=1 rerun_block_kept=1`
    - `closing_order_ok=1` (종료 해제가 mSystemHandler.Release 보다 먼저)
    - `plc_order_ok=1` (해제 → 오프라인 강제 끔 → ProcessTest 순서)
    - `save_finally>=1 save_reapply=1 release_trycatch>=1`
  </acceptance_criteria>
  <done>불러온 상태에서 저장해도 레시피 파일에는 원래 사진 경로만 들어가고, PLC $TEST·레시피 변경·프로그램 종료 때 리뷰어 사진과 OfflineInspectMode 가 자동으로 되돌아가는 배선이 빌드·커밋되었다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 리뷰어 cycle.json(로컬 파일) → 메모리 레시피 객체 | 저장된 사진 경로 문자열이 운영 레시피 객체에 임시로 들어간다 |
| 메모리 레시피 객체 → 레시피 INI 파일 | 저장 때 임시 경로가 파일로 새면 현장 판정이 바뀐다 |
| PLC $TEST(TCP, MainRun 백그라운드 스레드) → 시퀀스 | 자동 검사가 리뷰어 사진·오프라인 모드로 돌면 실물과 무관한 판정이 나간다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80-01 | Tampering | MainWindow.SaveRecipe → SequenceHandler.SaveRecipe | high | mitigate | `RunWithOriginalPaths` 가 저장 순간 스냅샷 경로로 되돌리고 finally 에서 재적용 (`save_wrap=1`, `save_finally>=1`) |
| T-80-02 | Tampering | Custom/SystemHandler.cs MainRun Test | high | mitigate | ProcessTest·ForceOffline 보다 먼저 `Release(RELEASE_REASON_PLC_TEST)` — 경로·OfflineInspectMode·기준점 캐시 복원 (`plc_order_ok=1`) |
| T-80-03 | Tampering | 종료 시 Setting.Save | medium | mitigate | Window_Closing 에서 mSystemHandler.Release 앞에 해제, 서비스는 Setting.Save 를 부르지 않음 (`closing_order_ok=1`, `no_save_in_svc=0`) |
| T-80-04 | Denial of Service | ReleaseCore 가 MainRun 스레드에서 예외 | high | mitigate | ReleaseCore 전체 try/catch + PrintErrLog, 절대 throw 안 함 (`release_trycatch>=1`) |
| T-80-05 | Tampering | cycle.json 의 사진 경로 필드 | low | accept | 로컬 장비 PC 파일, 읽기 전용 이미지 로드, `IsUsableImageFile`(절대경로·존재·크기>0)만 적용 |
| T-80-06 | Tampering | UI 스레드(불러오기·저장) vs MainRun(해제) 동시 접근 | medium | mitigate | 스냅샷·적용·복원 전부 `lock(s_lock)` 안, 이벤트는 잠금 밖에서 발화 |
</threat_model>

<verification>
1. Debug|x64 빌드 오류 0
2. 배선 사슬: 리뷰어 버튼 → VM.ApplySelection → 서비스.LoadForRow → SavedCycleRerunPlanner.BuildPartForSingleCycle, 상태 줄 → VM.StatusText/IsActive, [해제] → VM.ReleaseByUser → 서비스
3. 보호: 저장 감싸기, PLC 해제 순서, 종료 해제 순서, 레시피 변경 구독
4. 가독성 grep(새 파일 전체 + 기존 파일 더한 줄) 0, 기존 파일 삭제 줄은 MainWindow 저장 호출 1줄뿐
</verification>

<success_criteria>
- D-80-01/02/03/10/11/12/13/17/18 이 코드로 존재하고 빌드된다 (실제 화면 동작은 80-05 UAT U-1·U-5·U-6)
- RepeatRunService 기존 반복검사 코드 삭제 줄 0
</success_criteria>

<output>
완료 후 `.planning/phases/80-reviewer-ng-cycle-reinspect/80-01-SUMMARY.md` 작성: base 해시, 커밋 해시 2개, 빌드 결과, 파일별 더한/지운 줄 수, 검증 출력 원문, 새 심볼 목록(실제 이름), OnReviewerPhotosLoaded 와 상태 줄 문구 원문.
</output>
</content>
</invoke>
