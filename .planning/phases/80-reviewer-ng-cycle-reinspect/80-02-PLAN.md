---
phase: 80-reviewer-ng-cycle-reinspect
plan: 02
type: execute
wave: 2
depends_on: ["80-01"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Custom/Sequence/Inspection/DatumTestFindService.cs
  - WPF_Example/Custom/Sequence/Inspection/LocalRefTestFindService.cs
  - WPF_Example/DatumMeasurement.csproj
  - WPF_Example/UI/ContentItem/MainView.xaml
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: true
requirements: [D-80-15, D-80-06, D-80-00, D-80-16, D-80-17, D-80-18]
user_setup: []

must_haves:
  truths:
    - "[함께 처리 2] 수동 RUN(프로토콜 아님)에서 Test Find 로 기준점을 잡아 둔 뒤 기준 ROI(LocalRef_*)나 기준 ROI 에지 설정을 고치고 RUN 만 누르면, 측정 직전 설정 키가 달라진 것을 보고 그 기준점의 가로 사진 파일(DatumConfig.TeachingImagePath)과 이미 잡아 둔 기준점 변환으로 국부 기준선을 그 자리에서 다시 구해 쓴다 — 결과 기준 = '국부'(RefSource Local), Algorithm 로그 '[LocalRef] 기준 ROI·에지 설정이 바뀌어 기준점 가로 사진에서 국부 기준선을 다시 구함 — ' 1줄. 다시 구한 선은 시퀀스 저장소에 들어가 다음 RUN 은 사진을 다시 읽지 않는다"
    - "[함께 처리 2, D-79-04, D-79-08] PLC 자동(프로토콜) 사이클에서는 다시 구하기를 하지 않고 지금과 똑같이 '기준점을 찾은 뒤 기준 ROI 또는 에지 설정이 바뀜' 사유로 전역 기준선으로 전환한다(IsProtocolDrivenCycle 가드). ProcessOneDatum · HandleRunStartResetResults · ComputeLocalRefLinesForDatum · InjectLocalRef 는 한 줄도 바뀌지 않는다"
    - "[함께 처리 2, D-79-05] 다시 구하기는 기준점 가로 사진 파일만 쓴다 — 라이브 촬영이나 측정 사진(SimulImagePath) 폴백을 쓰지 않는다. 파일이 없거나 변환이 없으면 지금처럼 전역 전환 + 원인 로그. 다시 구했는데 띠 에지를 못 찾으면 그 실제 원인 문구로 전환한다"
    - "[함께 처리 2, D-79-04 조용히 틀리지 않기] 다시 구하기는 잡아 둔 기준점 변환이 지금의 TeachingImagePath(두 장짜리면 +TeachingImagePath_Vertical) 파일에서 나온 것이 확인될 때만 한다(사진 출처 기록 — InspectionSequence.IsDatumTransformFromTeachingPhotos: 기록한 변환 객체가 지금 캐시 객체와 같고 기록한 경로가 지금 경로와 같음). 출처 기록은 ① 기존 Test Find 버튼이 두 장짜리 기준점을 잡을 때(그 분기는 두 파일을 디스크에서 읽고 없으면 중단 — HoldManualDatum 이 기록) ② DatumTestFindService 로 잡을 때(1장·2장 모두) 생긴다. 1장 기준점을 기존 버튼으로 잡으면(화면 사진 또는 파일 선택 — TeachingImagePath 가 아님) 기록을 지우고, 캐시가 다른 경로(수동 RUN 재검출·다른 Test Find)로 바뀌면 변환 객체가 달라 기록이 무효, ClearDatumTransforms 는 기록을 비운다. 확인되지 않으면 다시 구하지 않고 지금과 같은 STALE 전환 + Algorithm 로그 '[LocalRef] 기준점을 기준점 사진 파일이 아닌 사진으로 찾아서 국부 기준선을 다시 구하지 않음 — ' 1줄"
    - "[D-80-15, D-80-00] 메인 툴바 Test Find 옆에 버튼 '기준 ROI 시험 찾기' 1개가 있다. 국부 기준을 켠 EdgeToLineDistance 측정을 고르고 누르면 그 측정 기준점의 가로 사진(z1)을 캔버스 배경으로 띄우고, 기준 ROI 상자(편집 가능 상자 + 보정 위치 상자)를 보이고, 런타임과 같은 기준점 찾기 경로로 그 사진에서 기준점을 찾으며 계산된 국부 기준선을 주황 선(FAI-RefLine)으로 그린다. 결과(찾음 + 에지 세기 / 못 찾은 이유 1줄)는 툴바의 기존 결과 라벨(label_testFindResult)에 초록/빨강 한 줄로 보인다 — 새 대화상자를 만들지 않는다(D-80-00: 새 대화상자는 D-80-09 알림 1개뿐)"
    - "[D-80-06 공용 헬퍼] DatumTestFindService.TryRunFromTeachingImages 는 기준점의 TeachingImagePath(와 두 장짜리면 TeachingImagePath_Vertical)를 파일에서 직접 읽어, 패턴 정렬이면 InspectionSequence.TryComposeAlign, 아니면 InspectionSequence.TryRunSingleDatum 을 부른다 — 사진 출처 선택 대화상자를 거치지 않고, 비패턴 기준점도 시퀀스 기준점 캐시와 국부 기준선을 채운다. 시퀀스가 Idle 이 아니면 거절한다. 기존 Test Find 버튼(BtnTestFindDatum_Click)과 그 대화상자 메서드는 한 줄도 바뀌지 않는다"
    - "[D-80-17, D-80-18] 시험 찾기의 판단·사진 로드·기준점 찾기·선 만들기·문구는 새 파일 LocalRefTestFindService.cs / DatumTestFindService.cs 에 있고 csproj 에 등록된다. MainView.xaml.cs 에는 서비스 호출과 캔버스 표시 호출만 있다"
    - "[D-80-16] 새 파일 전체와 기존 파일 더한 줄의 가독성 grep 0, HImage 는 finally Dispose, 시퀀스 캐시가 가진 HTuple 변환은 Dispose 하지 않는다. Debug|x64 빌드 오류 0"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "stale 국부 기준선 수동 RUN 재계산"
      contains: "private LocalRefLineResult TryRecomputeStaleLocalRef("
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "다시 구한 국부 기준선 저장 + 로그, 잡아 둔 기준점의 사진 출처 기록·확인"
      contains: "public bool IsDatumTransformFromTeachingPhotos("
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumTestFindService.cs"
      provides: "대화상자 없는 공용 Test Find"
      contains: "public static bool TryRunFromTeachingImages("
    - path: "WPF_Example/Custom/Sequence/Inspection/LocalRefTestFindService.cs"
      provides: "기준 ROI 시험 찾기 판단·결과"
      contains: "public static LocalRefTestFindOutcome Run("
    - path: "WPF_Example/UI/ContentItem/MainView.xaml"
      provides: "기준 ROI 시험 찾기 버튼"
      contains: "x:Name=\"btn_testFindLocalRef\""
  key_links:
    - from: "Action_FAIMeasurement.TryResolveLocalRef (bStale 분기)"
      to: "InspectionSequence.StoreRecomputedLocalRefLine"
      via: "TryUseRecomputedLocalRef → TryRecomputeStaleLocalRef → EdgeToLineDistanceMeasurement.ComputeLocalRefLine"
      pattern: "return TryUseRecomputedLocalRef\\(etld, parentSeq2, out szReason\\);"
    - from: "Action_FAIMeasurement.TryRecomputeStaleLocalRef (PLC 가드 다음)"
      to: "InspectionSequence.IsDatumTransformFromTeachingPhotos"
      via: "출처 기록 — HoldManualDatum(두 장짜리) / DatumTestFindService → MarkDatumFoundFromTeachingPhotos"
      pattern: "IsDatumTransformFromTeachingPhotos\\(etld\\.DatumRef\\)"
    - from: "MainView.BtnTestFindLocalRef_Click"
      to: "LocalRefTestFindService.Run → DatumTestFindService.TryRunFromTeachingImages → InspectionSequence.TryGetLocalRefLine"
      via: "선택된 측정(InspectionListView.SelectedParam)"
      pattern: "LocalRefTestFindService\\.Run\\("
---

<objective>
국부 기준(Local Ref, Phase 79) 사용 불편 두 가지를 없앤다. (1) 함께 처리 2 — 수동 RUN 에서 기준 ROI 를 고친 뒤 Test Find 없이 RUN 만 눌러도 새 기준 ROI 로 국부 기준선을 다시 구한다(2026-09-18 17:24~17:27 수동 RUN 9번이 전부 LocalFallback 이던 실사례). (2) D-80-15 — 기준 ROI 를 티칭할 때 버튼 1개로 z1 기준점 가로 사진 위에서 띠 에지 선을 바로 확인한다. 둘 다 이미 있는 계산(ComputeLocalRefLine, TryComposeAlign/TryRunSingleDatum)을 그대로 쓰고 PLC 자동 경로는 바꾸지 않는다.

Purpose: 80-04 의 리뷰어 자동 Test Find(D-80-06)도 이 plan 의 공용 헬퍼 DatumTestFindService 를 쓴다 — 기존 Test Find 버튼은 건드리지 않고 대화상자 없는 경로를 새로 둔다(회귀 0).

Output: 기존 파일 4개 삽입(Action_FAIMeasurement 는 stale 분기 2줄 교체, InspectionSequence 는 HoldManualDatum·ClearDatumTransforms 에 1줄씩 추가), 새 파일 2개 + csproj 등록.

계획 단계 교정(RESEARCH Pattern 4): 다시 구할 사진은 기존 기준점 사진 취득 헬퍼로 얻지 않고 DatumConfig.TeachingImagePath 파일을 직접 읽는다. 그 헬퍼는 라이브 모드에서 지금 z(측정 높이) 사진을 찍고, 오프라인에서 TeachingImagePath 가 없으면 측정 사진(SimulImagePath)으로 폴백해 D-79-05(기준 ROI 는 z1 기준점 가로 사진)를 어긴다. 2026-09-18 사례는 OfflineInspectMode 가 켜진 수동 RUN 이었다(Trace.log 17:21~17:29 확인).

계획 단계 코드 확인 — 잡아 둔 기준점이 어느 사진에서 나오는가(2026-09-18 개정, 코디네이터 지적 반영):
- `InspectionSequence.HoldManualDatum`(:1413-1416)은 `_bManualDatumHeld = true` + 로그뿐이고, 호출하는 곳은 저장소 전체에서 MainView.xaml.cs:4590 하나(기존 Test Find 버튼, 두 분기 모두 성공 뒤)다.
- 기존 Test Find 버튼 두 장짜리 분기(MainView.xaml.cs:4501-4544): TeachingImagePath 와 TeachingImagePath_Vertical 을 디스크에서 직접 읽고, 둘 중 하나라도 없으면 알림 후 중단한다(폴백 없음). 패턴 정렬이면 `TryComposeAlign(datum, imgH, imgV, ...)` 가 그 두 파일로 변환·국부 기준선을 캐시한다 → 출처 = 티칭 사진 파일(2026-09-18 SIDE_1 사례가 이 경로).
- 기존 버튼 1장 분기(:4545-4563): 사진 출처 선택 메서드(:4636-4671)가 [예] 지금 화면 사진(halconViewer.CurrentImage) 또는 [아니오] 파일 선택(halconViewer.LoadImage 만 하고 TeachingImagePath 에 쓰지 않음 — 경로를 기록하는 곳은 LoadAndDisplay 의 pathSink 뿐)을 돌려준다. 패턴 정렬이면 `TryComposeAlign(datum, testImage, ...)` 가 그 사진으로 캐시한다 → 출처 = TeachingImagePath 가 아닐 수 있다. 여기서 TeachingImagePath 로 다시 구하면 다른 사진의 선이 '국부' 로 표시되는 조용한 오류가 된다 — 그래서 출처 기록이 확인될 때만 다시 구한다. 비패턴(라인핏)이면 두 분기 모두 `DatumFindingService.TryFindDatum` 을 직접 불러 캐시를 채우지 않으므로 다음 RUN 이 기준점을 다시 찾고 그 RUN 안에서는 stale 이 생기지 않는다.
- 수동 RUN 의 기준점 단계(캐시가 없을 때만)가 채우는 캐시는 출처 기록이 없어 다시 구하지 않는다(지금과 같은 전환) — 정적 두 장짜리 경로(TryLoadStaticDualDatumImages :1204)는 TeachingImagePath 가 없으면 SimulImagePath 로 폴백하므로 확인된 출처로 보지 않는다.

⚠ spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/80-reviewer-ng-cycle-reinspect/80-CONTEXT.md
@.planning/phases/80-reviewer-ng-cycle-reinspect/80-RESEARCH.md
@.planning/phases/79-side-local-strip-datum/79-CONTEXT.md

<interfaces>
계획 시점 라인 번호(80-01 은 아래 두 Sequence 파일을 건드리지 않는다). 실행자는 문맥으로 재확인한다.

WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs (K&R, namespace ReringProject.Sequence, using System.IO · HalconDotNet 있음)
- :167 `private static void SafeDisposeImage(HImage image)` (null 이면 반환, try Dispose catch)
- :173 `GetMeasurementDisplayName(MeasurementBase meas)`
- :295-335 `ProcessOneDatum`(바꾸지 않음), :1080-1114 기준점 사진 취득 헬퍼(바꾸지 않음, 새 코드에서 부르지 않음)
- :1891-1924 `InjectDatumOrigin` — DatumConfigs 에서 `d.DatumName == meas.DatumRef` 로 찾는 규칙 :1897-1903
- :1928-1943 `InjectLocalRef`(바꾸지 않음) — 실패 시 Error 로그 `LOCAL_REF_LOG_TAG + "전역 기준선으로 전환 — " + Shot + " · " + 측정 + ": " + 원인`
- :1947-1983 `private bool TryResolveLocalRef(EdgeToLineDistanceMeasurement etld, InspectionSequence parentSeq2, out string szReason)` — :1957 parentSeq2 null 이면 NOT_COMPUTED 로 먼저 반환, :1967-1974 `!result.Found` 원인(Error 비면 LOCAL_REF_ERR_FIT_FAILED), **:1975 `bool bStale = result.SettingsKey != etld.BuildLocalRefSettingsKey();` :1976-1979 `if (bStale) { szReason = EdgeToLineDistanceMeasurement.LOCAL_REF_REASON_STALE; return false; }`**, :1980-1982 성공 주입
WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (K&R)
- :2060 `public bool IsProtocolDrivenCycle()`
- :1411-1416 `private bool _bManualDatumHeld` + `public void HoldManualDatum(string szDatumName) { _bManualDatumHeld = true; Logging.PrintLog(...); }` — 호출하는 곳은 MainView.xaml.cs:4590 하나(기존 Test Find 성공 뒤). 이 plan 은 이 메서드에 1줄만 더한다
- :2956-2971 `public void ClearDatumTransforms()` — `_bManualDatumHeld = false;` 다음 `lock (_datumStateLock) { _datumTransforms.Clear(); _failedDatums.Clear(); _alignFailedDatums.Clear(); _localRefLines.Clear(); }` — 이 plan 은 lock 블록 안에 1줄만 더한다
- `_datumTransforms`(Dictionary<string, HTuple>, 키 = DatumName, `_datumStateLock` 보호) — TryComposeAlign(:3546) · TryRunSingleDatum(:3600) 이 성공할 때마다 새 HTuple 객체를 대입한다(참조가 바뀜)
- :1204 Action_FAIMeasurement `TryLoadStaticDualDatumImages` — 수동 RUN 의 두 장짜리 기준점 단계, TeachingImagePath 가 없으면 측정 사진으로 폴백(확인된 출처로 보지 않는다)
- :3418 `public bool TryComposeAlign(DatumConfig datum, HImage refImage, HImage refImageVertical, string modelPath, out string error)` — 성공 시 _datumTransforms 저장 + `ComputeLocalRefLinesForDatum(datum, refImage, alignRigid)`
- :3559 `public bool TryRunSingleDatum(DatumConfig datum, HImage imageH, HImage imageV, out string error)` — 두 장짜리면 imageH·imageV 둘 다 필요, 성공 시 캐시 + `ComputeLocalRefLinesForDatum(datum, imageH, transform)`
- :3610 `public bool TryGetDatumTransform(string datumRef, out HTuple transform)`(캐시 객체를 돌려준다 — Dispose 금지), :3625 `HasCachedDatumTransform`, :3633 `public bool TryGetLocalRefLine(MeasurementBase meas, out LocalRefLineResult result)`
- :3291 `public static string ResolveDatumModelPath(DatumConfig datum, string ownerSeqName)`
- :3733-3737 `private void StoreLocalRefLine(EdgeToLineDistanceMeasurement etld, LocalRefLineResult result)`, :3739-3755 `private void LogLocalRefLine(string szDatumName, EdgeToLineDistanceMeasurement etld, LocalRefLineResult result)`
WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs
- :24 `LOCAL_REF_LOG_TAG = "[LocalRef] "`, :25 `LOCAL_REF_OVERLAY_ROI_ID = "FAI-RefLine"`, :30 `LOCAL_REF_ERR_FIT_FAILED`, :34 `LOCAL_REF_REASON_STALE`
- :93 `IsLocalRefEnabled`, :97-101 `LocalRef_Row/Col/Phi/Length1/Length2`, `DatumRef`(MeasurementBase)
- :494 `public LocalRefLineResult ComputeLocalRefLine(HImage imgHorizontal, HTuple datumTransform)`(항상 null 아닌 결과), :572 `BuildLocalRefSettingsKey()`, :659 `LocalRefLineResult { DatumName, Found, Row1, Col1, Row2, Col2, EdgeScore, Error, SettingsKey }`
WPF_Example/UI/ContentItem/MainView.xaml.cs (K&R) — :200 `_lastDisplayedImageShot`, :758 `public void HighlightSelectedRoi(ParamBase param)`, :2302 `public void ShowResultDatumOverlays(List<DatumConfig> datums)`(보정 위치 초록 상자 포함), **:4475-4600 `BtnTestFindDatum_Click` · :4636-4671 사진 출처 선택 메서드 — 바꾸지 않는다(기준 비교 대상)**, :4484-4489 티칭 완료 판정(라인핏 IsConfigured && LastTeachSucceeded, 패턴은 모델 파일 존재)
WPF_Example/UI/ContentItem/MainView.xaml — :291-309 `btn_testFindDatum`(파랑 템플릿), :311 `btn_reanchor`
EdgeInspectionOverlay (ReringProject.Halcon.Models) — RoiId, LineRow1, LineColumn1, LineRow2, LineColumn2 (EdgeToLineDistanceMeasurement.cs:444-451 이 FAI-RefLine 을 만드는 방식)
</interfaces>
</context>

## Artifacts this phase produces (이 plan 분)

phase 전체 원장은 `80-01-PLAN.md`. 이 plan 이 만드는 것:
- InspectionSequence: `public void StoreRecomputedLocalRefLine(EdgeToLineDistanceMeasurement etld, LocalRefLineResult result)`, `public void MarkDatumFoundFromTeachingPhotos(string szDatumName)`, `public bool IsDatumTransformFromTeachingPhotos(string szDatumName)`, private nested `TeachingPhotoProvenance { Transform, HorizontalPath, VerticalPath }`, 필드 `_teachingPhotoProvenance`(Dictionary<string, TeachingPhotoProvenance>, `_datumStateLock` 보호), private `RecordTeachingPhotoProvenance(string)`, `ForgetTeachingPhotoProvenance(string)`, `RecordHeldDatumProvenance(string)`, `FindDatumConfigByName(string)`; HoldManualDatum 1줄 추가(`RecordHeldDatumProvenance(szDatumName);`), ClearDatumTransforms 1줄 추가(`_teachingPhotoProvenance.Clear();`)
- Action_FAIMeasurement: private `TryUseRecomputedLocalRef(EdgeToLineDistanceMeasurement, InspectionSequence, out string)`, `TryRecomputeStaleLocalRef(EdgeToLineDistanceMeasurement, InspectionSequence)`, `FindSequenceDatum(InspectionSequence, string)`(static), `BuildLocalRefFailReason(LocalRefLineResult)`(static), `GetShotNameForLog()`, const `LOCAL_REF_RECOMPUTED_TEXT`, `LOCAL_REF_NOT_TEACHING_PHOTO_TEXT`
- 새 파일 `Custom/Sequence/Inspection/DatumTestFindService.cs`: `public static class DatumTestFindService` — const `LOG_TAG`, `ERR_NO_DATUM`, `ERR_NO_SEQUENCE`, `ERR_SEQUENCE_BUSY`, `ERR_NOT_TAUGHT`, `ERR_NO_HORIZONTAL`, `ERR_NO_VERTICAL`, `ERR_LOAD_FAILED_PREFIX`; `IsTaughtForTestFind(DatumConfig)`, `TryRunFromTeachingImages(InspectionSequence, DatumConfig, bool, out string)`
- 새 파일 `Custom/Sequence/Inspection/LocalRefTestFindService.cs`: `public class LocalRefTestFindOutcome { Ok, Message, ImagePath, Datum, Overlays }`, `public static class LocalRefTestFindService` — const `TITLE`, `MSG_SELECT_MEASUREMENT`, `MSG_OPTION_OFF`, `MSG_ROI_NOT_TAUGHT`, `MSG_NO_DATUM`, `MSG_NO_PHOTO`, `MSG_DATUM_FIND_FAILED_PREFIX`, `MSG_NOT_COMPUTED`, `MSG_EDGE_NOT_FOUND_PREFIX`; `Run(ParamBase)`, private `FindOwnerSequence(EdgeToLineDistanceMeasurement)`, `BuildRefLineOverlay(LocalRefLineResult)`
- MainView.xaml `btn_testFindLocalRef`, MainView.xaml.cs `BtnTestFindLocalRef_Click`, `ShowLocalRefTestFindOutcome(ParamBase, LocalRefTestFindOutcome)`
- 저장소 밖: `/c/Users/admin/AppData/Local/Temp/p80/base-80-02.txt`, `build-80-02.txt`

<tasks>

<task type="auto">
  <name>Task 1: 함께 처리 2 — 수동 RUN 의 stale 국부 기준선을 기준점 가로 사진 파일로 다시 구하기 (잡아 둔 기준점이 그 사진에서 나온 것이 확인될 때만, PLC 자동 경로 불변)</name>
  <precondition>80-01-SUMMARY.md 가 있고 Debug 앱이 실행 중이 아니다</precondition>
  <reversibility rating="reversible">stale 분기 2줄 교체 + private 헬퍼·출처 기록 추가, HoldManualDatum·ClearDatumTransforms 에 1줄씩 추가뿐이고 레시피·cycle.json·CSV 형식은 그대로다</reversibility>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <read_first>
    - CLAUDE.md (🔒 가독성 규칙 — if 중첩 3단계면 메서드 분리, HImage Dispose)
    - .planning/phases/80-reviewer-ng-cycle-reinspect/80-RESEARCH.md "Pattern 4" 와 "Pitfall 2"
    - .planning/phases/79-side-local-strip-datum/79-CONTEXT.md (D-79-04/05/06/08)
    - 이 plan objective 의 "계획 단계 코드 확인 — 잡아 둔 기준점이 어느 사진에서 나오는가"
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs:160-180, :295-335, :1080-1140, :1200-1240, :1875-1990
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs:1405-1417, :2040-2065, :2950-2972, :3540-3640, :3700-3760
    - WPF_Example/UI/ContentItem/MainView.xaml.cs:4475-4600, :4634-4671 (기존 Test Find 두 분기의 사진 출처 — 읽기만, 바꾸지 않음)
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs:20-45, :490-610, :655-683
  </read_first>
  <action>
(0) 편집 전: `git rev-parse HEAD > /c/Users/admin/AppData/Local/Temp/p80/base-80-02.txt`.

(1) InspectionSequence.cs (K&R) — `StoreLocalRefLine`(:3733-3737) 바로 다음에 `public void StoreRecomputedLocalRefLine(EdgeToLineDistanceMeasurement etld, LocalRefLineResult result)` 를 더한다: `bool bMissing = etld == null || result == null;` 이면 return. `StoreLocalRefLine(etld, result);` 다음 `LogLocalRefLine(result.DatumName, etld, result);`(기존 '[LocalRef] 기준선 찾음 — ' / '기준 ROI 실패 — ' 로그 형식 그대로). 주석 "// Phase 80 함께 처리 2: 수동 RUN 에서 기준 ROI 를 고친 뒤 다시 구한 국부 기준선을 저장소에 바꿔 넣는다 — 다음 RUN 은 설정 키가 같아 사진을 다시 읽지 않는다".

(1-2) InspectionSequence.cs — 잡아 둔 기준점의 사진 출처 기록(D-79-04: 조용히 틀리지 않기). 기존 1장 Test Find 는 화면 사진이나 고른 파일로 기준점을 잡을 수 있어(objective 의 코드 확인), 그 변환에 TeachingImagePath 사진의 선을 섞으면 다른 사진의 선이 '국부' 로 표시된다. 그래서 변환이 지금의 티칭 사진 파일에서 나온 것이 확인될 때만 다시 구한다:
 - `_localRefLines` 선언(:88) 근처에 private sealed nested class `TeachingPhotoProvenance`(public 필드 `HTuple Transform`, `string HorizontalPath`, `string VerticalPath`)와 필드 `private readonly Dictionary<string, TeachingPhotoProvenance> _teachingPhotoProvenance = new Dictionary<string, TeachingPhotoProvenance>();`(주석: `_datumStateLock` 으로 보호, 키 = DatumName).
 - private `FindDatumConfigByName(string szDatumName)` — 이름이 비었거나 DatumConfigs 가 null 이면 null, `d != null` 이고 `d.DatumName == szDatumName` 인 첫 항목.
 - private `RecordTeachingPhotoProvenance(string szDatumName)` — 이름이 비면 return. datum 을 찾고 `lock (_datumStateLock)` 안에서 `_datumTransforms.TryGetValue(szDatumName, out hvTransform)`. `bool bCannotRecord = datum == null || !bCached;` 이면 `_teachingPhotoProvenance.Remove(szDatumName)` 후 return. 아니면 새 TeachingPhotoProvenance(Transform = 캐시에 있는 그 HTuple 참조 — 복사·Dispose 하지 않음, HorizontalPath = datum.TeachingImagePath, VerticalPath = datum.TeachingImagePath_Vertical)를 그 키에 넣는다.
 - private `ForgetTeachingPhotoProvenance(string szDatumName)` — 이름이 비면 return, lock 안에서 Remove.
 - private `RecordHeldDatumProvenance(string szDatumName)` — `DatumConfig datum = FindDatumConfigByName(szDatumName);` `bool bDualImage = datum != null && datum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage;` 이면 RecordTeachingPhotoProvenance(기존 Test Find 두 장짜리 분기는 TeachingImagePath·TeachingImagePath_Vertical 을 디스크에서 읽고 없으면 중단한다 — MainView.xaml.cs:4501-4511), 아니면 ForgetTeachingPhotoProvenance(1장 분기는 화면 사진 또는 고른 파일이라 출처를 모른다 — :4545-4547). 두 분기 이유를 주석으로 적는다.
 - `public void MarkDatumFoundFromTeachingPhotos(string szDatumName)` — 본문 `RecordTeachingPhotoProvenance(szDatumName);`. 요약 주석: DatumTestFindService 가 TeachingImagePath 파일(두 장짜리면 +세로)로 기준점을 찾은 직후 부른다(Task 2).
 - `public bool IsDatumTransformFromTeachingPhotos(string szDatumName)` — 요약 주석: 지금 캐시의 변환이 기록한 그 객체이고(캐시가 다른 경로로 다시 쓰이면 새 HTuple 이라 달라진다) 티칭 사진 경로가 기록 때와 같을 때만 true. 이름이 비거나 datum 이 없으면 false. lock 안에서 기록이 없거나 캐시가 없으면 false, `bool bSameTransform = ReferenceEquals(entry.Transform, hvCached);` `bool bSameHorizontal = string.Equals(entry.HorizontalPath, datum.TeachingImagePath, StringComparison.Ordinal);` `bool bSameVertical = string.Equals(entry.VerticalPath, datum.TeachingImagePath_Vertical, StringComparison.Ordinal);` 세 bool 을 차례로 if 로 검사해 하나라도 false 면 false, 끝에 true(한 줄에 && 를 셋 이상 늘어놓지 않는다).
 - HoldManualDatum(:1413-1416) — `_bManualDatumHeld = true;` 줄 바로 다음에 **한 줄만** 더한다: `RecordHeldDatumProvenance(szDatumName); // Phase 80 함께 처리 2: 두 장짜리는 티칭 사진 파일 출처로 기록, 1장은 출처 모름으로 지움` (주석은 같은 줄 끝). 다른 줄은 그대로.
 - ClearDatumTransforms(:2956-2971) — lock 블록 안 `_localRefLines.Clear();` 줄 바로 다음에 **한 줄만** 더한다: `_teachingPhotoProvenance.Clear(); // Phase 80 함께 처리 2: 잡아 둔 기준점의 사진 출처 기록도 같이 비운다`. 다른 줄은 그대로.
 - ProcessOneDatum · HandleRunStartResetResults · ComputeLocalRefLinesForDatum · InjectLocalRef · TryComposeAlign · TryRunSingleDatum 과 기존 Test Find 버튼·사진 출처 선택 메서드는 한 글자도 바꾸지 않는다. 캐시를 다시 쓰는 경로(수동 RUN 재검출, 기존 1장 Test Find, 다른 Test Find)는 새 HTuple 을 넣으므로 참조 비교만으로 기록이 무효가 된다 — 그 메서드들에 줄을 더할 필요가 없다.

(2) Action_FAIMeasurement.cs (K&R):
 - TryResolveLocalRef 의 `if (bStale) {` 블록 안 두 줄(원인을 STALE 로 채우고 false 반환)을 `return TryUseRecomputedLocalRef(etld, parentSeq2, out szReason);` 한 줄로 바꾼다. 이 메서드의 다른 줄, InjectLocalRef, ProcessOneDatum, RunDatumPhase, 기준점 사진 취득 헬퍼는 건드리지 않는다.
 - const `private const string LOCAL_REF_RECOMPUTED_TEXT = "기준 ROI·에지 설정이 바뀌어 기준점 가로 사진에서 국부 기준선을 다시 구함 — ";` 와 `private const string LOCAL_REF_NOT_TEACHING_PHOTO_TEXT = "기준점을 기준점 사진 파일이 아닌 사진으로 찾아서 국부 기준선을 다시 구하지 않음 (기준점 Test Find 또는 '기준 ROI 시험 찾기' 를 다시 누르세요) — ";` (파일의 기존 const 근처).
 - private `GetShotNameForLog()` — ShotParam 이 null 이면 "", 아니면 ShotParam.ShotName(if/else, 두 로그가 같이 쓴다).
 - `private bool TryUseRecomputedLocalRef(EdgeToLineDistanceMeasurement etld, InspectionSequence parentSeq2, out string szReason)`: `LocalRefLineResult recomputed = TryRecomputeStaleLocalRef(etld, parentSeq2);` null 이면 szReason = `EdgeToLineDistanceMeasurement.LOCAL_REF_REASON_STALE`, false(지금과 같은 전환). `!recomputed.Found` 면 szReason = `BuildLocalRefFailReason(recomputed)`, false. 그 밖 `etld.InjectedLocalRef = recomputed; szReason = null; return true;`.
 - `private static string BuildLocalRefFailReason(LocalRefLineResult result)`: Error 가 비면 `EdgeToLineDistanceMeasurement.LOCAL_REF_ERR_FIT_FAILED`, 아니면 Error(:1967-1972 와 같은 규칙).
 - `private LocalRefLineResult TryRecomputeStaleLocalRef(EdgeToLineDistanceMeasurement etld, InspectionSequence parentSeq2)` — 요약 주석에 이유(D-79-05 사진 규칙, PLC 불변)를 적는다. 순서: ① `if (parentSeq2.IsProtocolDrivenCycle()) { return null; }` — PLC 자동 사이클은 사이클 중 설정이 바뀔 수 없고 결과가 지금과 비트 같아야 한다(D-79-04/08). ①-2 `bool bFromTeachingPhotos = parentSeq2.IsDatumTransformFromTeachingPhotos(etld.DatumRef);` 가 false 면 Algorithm 로그 `EdgeToLineDistanceMeasurement.LOCAL_REF_LOG_TAG + LOCAL_REF_NOT_TEACHING_PHOTO_TEXT + GetShotNameForLog() + " · " + GetMeasurementDisplayName(etld)` 1줄 후 `return null;`(= 지금과 같은 STALE 전환 — 사진을 읽기 전에 멈춘다). ② `DatumConfig datum = FindSequenceDatum(parentSeq2, etld.DatumRef);` null → null. ③ `string szPath = datum.TeachingImagePath;` `bool bNoPhoto = string.IsNullOrEmpty(szPath) || !File.Exists(szPath);` → null. 기존 기준점 사진 취득 헬퍼는 부르지 않는다(라이브 촬영·측정 사진 폴백이 z1 규칙을 어김). ④ `HTuple hvTransform;` `bool bHasTransform = parentSeq2.TryGetDatumTransform(etld.DatumRef, out hvTransform);` 아니면 null — hvTransform 은 시퀀스 캐시 객체라 Dispose 하지 않는다(주석). ⑤ `HImage img = null; LocalRefLineResult result = null; try { img = new HImage(szPath); result = etld.ComputeLocalRefLine(img, hvTransform); } catch (Exception ex) { Error 로그 LOCAL_REF_LOG_TAG + "다시 구하기 실패 — " + ex.Message; return null; } finally { SafeDisposeImage(img); }`. ⑥ `parentSeq2.StoreRecomputedLocalRefLine(etld, result);` ⑦ Algorithm 로그 `EdgeToLineDistanceMeasurement.LOCAL_REF_LOG_TAG + LOCAL_REF_RECOMPUTED_TEXT + GetShotNameForLog() + " · " + GetMeasurementDisplayName(etld)`. ⑧ result 반환.
 - `private static DatumConfig FindSequenceDatum(InspectionSequence seq, string szDatumName)`: 이름이 비었거나 seq·DatumConfigs 가 null 이면 null, DatumConfigs 를 돌며 `d != null` 이고 `d.DatumName == szDatumName` 인 첫 항목(InjectDatumOrigin :1897-1903 와 같은 규칙).
 - 새 줄에 조건 연산자 3종·switch 식·물음표를 쓰지 않는다. 기존 코드의 같은 모양(한 줄 여러 문장, null 병합 사용 등)은 복사하지 않는다.

끝나면 두 파일만 `git add` 후 커밋.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p80; PB=$(cat $H/base-80-02.txt); W=WPF_Example; A=$W/Custom/Sequence/Inspection/Action_FAIMeasurement.cs; S=$W/Custom/Sequence/Inspection/InspectionSequence.cs
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" $W/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-80-02.txt 2>&1; echo "build_exit=$? cs_errors=$(grep -c 'error CS' $H/build-80-02.txt)"
for F in $A $S; do ADD=$(git diff -w -U0 $PB -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "$(basename $F) deleted=$(git diff -w -U0 $PB -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') qmark=$(printf '%s\n' "$ADD" | grep -cF '?') logic3=$(printf '%s\n' "$ADD" | grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)')"; done
echo "deleted_are_stale=$(git diff -w -U0 $PB -- $A | grep -E '^-' | grep -vE '^---' | grep -cE 'LOCAL_REF_REASON_STALE|return false;')"
echo "stale_call=$(grep -cF 'return TryUseRecomputedLocalRef(etld, parentSeq2, out szReason);' $A) store_call=$(grep -cF 'parentSeq2.StoreRecomputedLocalRefLine(etld, result);' $A) store_def=$(grep -cF 'public void StoreRecomputedLocalRefLine(' $S)"
R=$(tr -d '\r' < $A | awk '/private LocalRefLineResult TryRecomputeStaleLocalRef/,/^        }$/')
echo "guard_protocol=$(printf '%s\n' "$R" | grep -c 'IsProtocolDrivenCycle()') first_stmt_guard=$(printf '%s\n' "$R" | grep -vE '^\s*(//|$|private|\{)' | head -1 | grep -c 'IsProtocolDrivenCycle') uses_teaching=$(printf '%s\n' "$R" | grep -c 'TeachingImagePath') uses_grabhelper=$(printf '%s\n' "$R" | grep -c 'GrabOrLoad') uses_simul=$(printf '%s\n' "$R" | grep -c 'SimulImagePath') safe_dispose=$(printf '%s\n' "$R" | grep -c 'SafeDisposeImage(img)') transform_dispose=$(printf '%s\n' "$R" | grep -c 'hvTransform.Dispose')"
for FN in 'private void ProcessOneDatum' 'private void InjectLocalRef' 'private HImage GrabOrLoadDatumImage'; do B=$(git show $PB:$A | tr -d '\r' | awk -v s="$FN" 'index($0,s){f=1} f{print} f&&/^        }$/{exit}' | md5sum | cut -c1-8); N=$(tr -d '\r' < $A | awk -v s="$FN" 'index($0,s){f=1} f{print} f&&/^        }$/{exit}' | md5sum | cut -c1-8); echo "same[$FN]=$([ "$B" = "$N" ] && echo 1 || echo 0)"; done
for FN in 'private void HandleRunStartResetResults' 'private void ComputeLocalRefLinesForDatum' 'public bool TryComposeAlign(DatumConfig datum, HImage refImage, HImage refImageVertical' 'public bool TryRunSingleDatum'; do B=$(git show $PB:$S | tr -d '\r' | awk -v s="$FN" 'index($0,s){f=1} f{print} f&&/^        }$/{exit}' | md5sum | cut -c1-8); N=$(tr -d '\r' < $S | awk -v s="$FN" 'index($0,s){f=1} f{print} f&&/^        }$/{exit}' | md5sum | cut -c1-8); echo "same[$FN]=$([ "$B" = "$N" ] && echo 1 || echo 0)"; done
region() { awk -v s="$1" 'index($0,s){f=1} f{print} f&&/^        }$/{exit}'; }
for FN in 'public void HoldManualDatum' 'public void ClearDatumTransforms'; do D=$(diff <(git show $PB:$S | tr -d '\r' | region "$FN") <(tr -d '\r' < $S | region "$FN")); echo "onlyadd[$FN] added=$(printf '%s\n' "$D" | grep -c '^>') removed=$(printf '%s\n' "$D" | grep -c '^<')"; done
echo "hold_records=$(tr -d '\r' < $S | region 'public void HoldManualDatum' | grep -c 'RecordHeldDatumProvenance(szDatumName);') clear_prov=$(tr -d '\r' < $S | region 'public void ClearDatumTransforms' | grep -c '_teachingPhotoProvenance.Clear();') prov_def=$(grep -c 'public bool IsDatumTransformFromTeachingPhotos(string szDatumName)' $S) mark_def=$(grep -c 'public void MarkDatumFoundFromTeachingPhotos(string szDatumName)' $S) ref_check=$(tr -d '\r' < $S | region 'public bool IsDatumTransformFromTeachingPhotos' | grep -c 'ReferenceEquals(') dual_rule=$(tr -d '\r' < $S | region 'private void RecordHeldDatumProvenance' | grep -c 'VerticalTwoHorizontalDualImage')"
echo "prov_guard=$(printf '%s\n' "$R" | grep -c 'IsDatumTransformFromTeachingPhotos(etld.DatumRef)') prov_order_ok=$(printf '%s\n' "$R" | awk '/IsProtocolDrivenCycle\(\)/{a=NR} /IsDatumTransformFromTeachingPhotos\(/{b=NR} /new HImage\(/{c=NR} END{ if (a>0 && b>a && c>b) print 1; else print 0 }') not_teaching_const=$(grep -c 'LOCAL_REF_NOT_TEACHING_PHOTO_TEXT' $A) not_teaching_text=$(grep -c '기준점 사진 파일이 아닌 사진으로 찾아서' $A)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `build_exit=0 cs_errors=0`
    - Action_FAIMeasurement.cs `deleted=2` 이고 `deleted_are_stale=2`(지운 줄은 stale 분기의 원인 대입·false 반환 2줄뿐), InspectionSequence.cs `deleted=0`
    - 두 파일 더한 줄 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 qmark=0 logic3=0`
    - `stale_call=1 store_call=1 store_def=1`
    - `guard_protocol=1 first_stmt_guard=1 uses_teaching>=1 uses_grabhelper=0 uses_simul=0 safe_dispose=1 transform_dispose=0`
    - `same[...]=1` 7개 전부 (ProcessOneDatum, InjectLocalRef, 기준점 사진 취득 헬퍼, HandleRunStartResetResults, ComputeLocalRefLinesForDatum, TryComposeAlign 5-인자, TryRunSingleDatum 무변경)
    - `onlyadd[public void HoldManualDatum] added=1 removed=0`, `onlyadd[public void ClearDatumTransforms] added=1 removed=0` (두 메서드는 줄 1개 추가만)
    - `hold_records=1 clear_prov=1 prov_def=1 mark_def=1 ref_check>=1 dual_rule>=1`
    - `prov_guard=1 prov_order_ok=1` (PLC 가드 → 출처 확인 → 사진 로드 순서), `not_teaching_const>=2 not_teaching_text=1`
  </acceptance_criteria>
  <done>수동 RUN 에서 기준 ROI 를 고치면, 잡아 둔 기준점 변환이 지금의 티칭 사진 파일에서 나온 것이 확인될 때만 그 파일로 국부 기준선을 다시 구해 쓰고 저장소에 넣는다. 확인되지 않으면(기존 1장 Test Find 의 화면 사진·고른 파일 등) 이유 로그와 함께 지금처럼 전역 전환한다. PLC 자동 사이클과 기준점 검출 경로는 이전과 같다.</done>
</task>

<task type="auto">
  <name>Task 2: 공용 대화상자 없는 Test Find(DatumTestFindService) + 기준 ROI 시험 찾기 판단(LocalRefTestFindService) — 새 파일 2개</name>
  <reversibility rating="reversible">새 파일 2개와 csproj 등록뿐, 기존 Test Find 버튼·런타임 경로는 그대로다</reversibility>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumTestFindService.cs, WPF_Example/Custom/Sequence/Inspection/LocalRefTestFindService.cs, WPF_Example/DatumMeasurement.csproj</files>
  <read_first>
    - CLAUDE.md (🔒 가독성 규칙, HImage Dispose, Error Handling — HALCON 호출은 try/catch)
    - .planning/phases/80-reviewer-ng-cycle-reinspect/80-RESEARCH.md "Pattern 3" "Pattern 7"
    - WPF_Example/UI/ContentItem/MainView.xaml.cs:4475-4600 (기존 Test Find — 판정·분기·HoldManualDatum 을 그대로 본뜨되 이 파일은 바꾸지 않는다), :4336-4361 (GetInspectionSequenceForDatum)
    - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs:1407-1416, :3285-3300, :3405-3420, :3555-3640
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs:18-45, :90-115, :440-455, :655-683
    - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs (DatumName, TeachingImagePath, TeachingImagePath_Vertical, AlgorithmTypeEnum, IsPatternAlignEnabled, IsConfigured, LastTeachSucceeded, OwnerName)
    - WPF_Example/Custom/Sequence/Inspection/ReviewerReinspectService.cs (80-01 — ResolveSequence·FindLiveShot 모양 참고)
  </read_first>
  <action>
두 새 파일 모두 namespace ReringProject.Sequence, Allman, AlignVerifyViewModel.cs 와 같은 파일 머리 스타일. 문구는 쉬운 한국어 한 줄(D-80-00), 물음표 문자 없음.

(1) `WPF_Example/Custom/Sequence/Inspection/DatumTestFindService.cs` — `public static class DatumTestFindService` (using System, System.IO, HalconDotNet, ReringProject.Utility):
 - const: `LOG_TAG = "[TestFind] "`, `ERR_NO_DATUM = "기준점을 찾지 못했습니다"`, `ERR_NO_SEQUENCE = "기준점이 속한 검사 시퀀스를 찾지 못했습니다"`, `ERR_SEQUENCE_BUSY = "검사가 진행 중입니다 — 끝난 뒤 다시 누르세요"`, `ERR_NOT_TAUGHT = "기준점 티칭(라인핏 또는 패턴 모델 생성)이 끝나지 않았습니다"`, `ERR_NO_HORIZONTAL = "기준점 가로 사진(TeachingImagePath) 파일이 없습니다"`, `ERR_NO_VERTICAL = "기준점 세로 사진(TeachingImagePath_Vertical) 파일이 없습니다"`, `ERR_LOAD_FAILED_PREFIX = "기준점 사진을 읽지 못했습니다: "`.
 - `public static bool IsTaughtForTestFind(DatumConfig datum)` — 기존 Test Find 버튼 :4484-4489 와 같은 판정: `bool bTaughtLineFit = datum.IsConfigured && datum.LastTeachSucceeded;` 패턴 정렬이면 `InspectionSequence.ResolveDatumModelPath(datum, datum.OwnerName)` 파일이 있으면 true. datum null 이면 false.
 - `public static bool TryRunFromTeachingImages(InspectionSequence seq, DatumConfig datum, bool bHoldForManualRun, out string szError)` — 요약 주석: 사진 출처 선택 대화상자 없이 기준점의 티칭 사진 파일로 런타임과 같은 기준점 찾기를 돌린다. 비패턴 기준점도 TryRunSingleDatum 으로 시퀀스 기준점 캐시·국부 기준선을 채운다(RESEARCH Pattern 3). 순서: datum null → ERR_NO_DATUM, seq null → ERR_NO_SEQUENCE, `seq.State != EContextState.Idle` → ERR_SEQUENCE_BUSY(자동 검사 중 캐시를 건드리지 않게), `!IsTaughtForTestFind(datum)` → ERR_NOT_TAUGHT. `bool bDual = datum.AlgorithmTypeEnum == EDatumAlgorithm.VerticalTwoHorizontalDualImage;` 가로 경로 파일 없음 → ERR_NO_HORIZONTAL, 두 장짜리인데 세로 파일 없음 → ERR_NO_VERTICAL. `HImage imgH = null; HImage imgV = null; bool bOk = false;` try: imgH 로드, bDual 이면 imgV 로드, 패턴 정렬이면 `seq.TryComposeAlign(datum, imgH, imgV, InspectionSequence.ResolveDatumModelPath(datum, datum.OwnerName), out szError)`(1장 기준점이면 imgV 는 null — 5-인자 오버로드가 1장 검출로 간다), 아니면 `seq.TryRunSingleDatum(datum, imgH, imgV, out szError)`. catch(Exception ex) → szError = ERR_LOAD_FAILED_PREFIX + ex.Message, bOk false. finally 에서 두 사진 각각 `if (x != null) { try { x.Dispose(); } catch { } }`. 성공이고 bHoldForManualRun 이면 `seq.HoldManualDatum(datum.DatumName)`(기존 Test Find 버튼과 같은 의미 — 다음 수동 RUN 이 재사용). 성공이면 bHoldForManualRun 과 상관없이 **그 다음 줄에서** `seq.MarkDatumFoundFromTeachingPhotos(datum.DatumName);` — 이 경로는 1장·2장 모두 TeachingImagePath 파일로 찾았으므로 출처를 확인된 것으로 기록한다(Task 1 의 다시 구하기 조건). HoldManualDatum 이 1장 기준점의 기록을 지우므로 반드시 HoldManualDatum 뒤에 부른다(주석). Trace 로그 1줄 `LOG_TAG + seq.Name + " · " + datum.DatumName + " 성공"` 또는 `" 실패 — " + szError`. 반환 bOk.

(2) `WPF_Example/Custom/Sequence/Inspection/LocalRefTestFindService.cs` (using System, System.Collections.Generic, System.IO, ReringProject.Halcon.Models, ReringProject.Utility):
 - `public class LocalRefTestFindOutcome` — 자동 속성 `bool Ok`, `string Message`(기본 ""), `string ImagePath`, `DatumConfig Datum`, `List<EdgeInspectionOverlay> Overlays`(기본 새 List).
 - `public static class LocalRefTestFindService` — Message 는 화면 라벨에 그대로 보일 한 줄이다: 모든 결과를 `TITLE + " — " + 문구` 로 만든다(성공도 포함). const `TITLE = "기준 ROI 시험 찾기"`, `MSG_FOUND_PREFIX = "띠 에지를 찾았습니다 — 에지 세기 "`, `MSG_SELECT_MEASUREMENT = "국부 기준(Local Ref)을 켠 EdgeToLineDistance 측정을 트리에서 먼저 고르세요"`, `MSG_OPTION_OFF = "이 측정은 국부 기준(Local Ref)이 꺼져 있습니다 — 속성창 Local Ref 탭에서 켜세요"`, `MSG_ROI_NOT_TAUGHT = "기준 ROI 가 아직 없습니다 — 속성창 Local Ref 탭에 숫자를 넣고 측정 노드를 다시 누르세요"`, `MSG_NO_DATUM = "이 측정의 기준점(DatumRef)을 찾지 못했습니다"`, `MSG_NO_PHOTO = "이 측정 기준점의 가로 사진(z1)이 없습니다 — 기준점 노드에서 사진을 먼저 불러오세요"`, `MSG_DATUM_FIND_FAILED_PREFIX = "기준점을 먼저 찾지 못했습니다: "`, `MSG_NOT_COMPUTED = "국부 기준선이 계산되지 않았습니다 — 측정의 DatumRef 와 Local Ref 설정을 확인하세요"`, `MSG_EDGE_NOT_FOUND_PREFIX = "기준 ROI 에서 띠 에지를 찾지 못했습니다: "`.
 - `public static LocalRefTestFindOutcome Run(ParamBase selectedParam)` — 요약 주석: D-80-15, D-79-05 와 같은 사진(그 측정 기준점의 가로 사진 z1)·같은 찾기 로직(런타임 기준점 찾기가 부르는 ComputeLocalRefLinesForDatum). 순서: `EdgeToLineDistanceMeasurement etld = selectedParam as EdgeToLineDistanceMeasurement;` null → MSG_SELECT_MEASUREMENT. `!etld.IsLocalRefEnabled` → MSG_OPTION_OFF. `bool bRoiTaught = etld.LocalRef_Length1 > 0 && etld.LocalRef_Length2 > 0;` 아니면 MSG_ROI_NOT_TAUGHT. `InspectionSequence seq = FindOwnerSequence(etld);` 와 그 seq.DatumConfigs 에서 DatumName == etld.DatumRef 인 datum — 없으면 MSG_NO_DATUM. 가로 사진 파일 없음 → MSG_NO_PHOTO. 여기부터 outcome.ImagePath = datum.TeachingImagePath, outcome.Datum = datum(실패여도 사진과 상자는 보여 주어 사용자가 상자를 옮길 수 있게). `DatumTestFindService.TryRunFromTeachingImages(seq, datum, false, out szError)` 실패 → Message = MSG_DATUM_FIND_FAILED_PREFIX + szError. 성공이면 `seq.TryGetLocalRefLine(etld, out result)` 가 false 면 MSG_NOT_COMPUTED, `!result.Found` 면 MSG_EDGE_NOT_FOUND_PREFIX + result.Error, Found 면 Ok = true, `Overlays.Add(BuildRefLineOverlay(result))`, Message = TITLE + " — " + MSG_FOUND_PREFIX + EdgeScore(소수 1자리 const 형식). 끝에 Algorithm 로그 1줄(`EdgeToLineDistanceMeasurement.LOCAL_REF_LOG_TAG + "시험 찾기 — " + ...` 찾음이면 중점 MidRow·MidCol·EdgeScore, 아니면 Message). 조건이 많으니 단계별 early return 으로 중첩 2단계 이하.
 - private `FindOwnerSequence(EdgeToLineDistanceMeasurement etld)` — `SystemHandler.Handle.Sequences.RecipeManager.Shots` 를 돌며 FAIList·Measurements 에 etld 가 들어 있는 Shot 을 찾고(참조 비교), `SystemHandler.Handle.Sequences` 를 Count/this[int] 로 돌며 `as InspectionSequence` 이고 `InspectionSequence.IsShotOwnedBySequence(shot, seq.Name)` 인 시퀀스를 반환. 없으면 null. 중첩이 깊어지면 Shot 찾기와 시퀀스 찾기를 private 메서드 2개로 나눈다.
 - private `BuildRefLineOverlay(LocalRefLineResult result)` — `RoiId = EdgeToLineDistanceMeasurement.LOCAL_REF_OVERLAY_ROI_ID`, LineRow1/LineColumn1/LineRow2/LineColumn2 = result 의 Row1/Col1/Row2/Col2(EdgeToLineDistanceMeasurement.cs:444-451 과 같은 모양 → 화면 주황 선).

(3) DatumMeasurement.csproj — `<Compile Include="Custom\Sequence\Inspection\DatumTestFindService.cs" />`, `<Compile Include="Custom\Sequence\Inspection\LocalRefTestFindService.cs" />` 를 80-01 이 넣은 ReviewerReinspectService.cs 항목 근처에 단독 항목으로 더한다(D-80-18).

끝나면 세 파일만 `git add` 후 커밋.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p80; PB=$(cat $H/base-80-02.txt); W=WPF_Example; D=$W/Custom/Sequence/Inspection/DatumTestFindService.cs; L=$W/Custom/Sequence/Inspection/LocalRefTestFindService.cs
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" $W/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-80-02.txt 2>&1; echo "build_exit=$? cs_errors=$(grep -c 'error CS' $H/build-80-02.txt)"
for F in $D $L; do test -f $F || echo "MISSING $F"; echo "$(basename $F) ternary=$(grep -cE '\?[^?]*:' $F) coalesce=$(grep -cF '??' $F) nullcond=$(grep -cF '?.' $F) switchexpr=$(grep -cE 'switch.*=>' $F) datesig=$(grep -cF 'hbk' $F) qmark=$(grep -cF '?' $F) logic3=$(grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)' $F)"; done
echo "csproj_add=$(git diff -U0 $PB -- $W/DatumMeasurement.csproj | grep -cE '^\+.*Compile Include=\"Custom.Sequence.Inspection.(DatumTestFindService|LocalRefTestFindService)\.cs\"') csproj_deleted=$(git diff -U0 $PB -- $W/DatumMeasurement.csproj | grep -E '^-' | grep -vcE '^---')"
echo "mark=$(grep -c 'seq.MarkDatumFoundFromTeachingPhotos(datum.DatumName);' $D) mark_after_hold=$(tr -d '\r' < $D | awk '/seq\.HoldManualDatum\(/{a=NR} /seq\.MarkDatumFoundFromTeachingPhotos\(/{b=NR} END{ if (a>0 && b>a) print 1; else print 0 }')"
echo "compose=$(grep -c 'seq.TryComposeAlign(' $D) single=$(grep -c 'seq.TryRunSingleDatum(' $D) hold=$(grep -c 'seq.HoldManualDatum(' $D) busy=$(grep -c 'EContextState.Idle' $D) dispose=$(grep -c 'Dispose(); } catch { }' $D) dialog_free=$(grep -cE 'Message[B]ox|OpenFile[D]ialog' $D $L | awk -F: '{s+=$2} END{print s}')"
echo "uses_helper=$(grep -c 'DatumTestFindService.TryRunFromTeachingImages(seq, datum, false' $L) reads_line=$(grep -c 'TryGetLocalRefLine(' $L) overlay_id=$(grep -c 'LOCAL_REF_OVERLAY_ROI_ID' $L)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `build_exit=0 cs_errors=0`
    - 새 파일 2개 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 qmark=0 logic3=0`
    - `csproj_add=2 csproj_deleted=0`
    - `compose>=1 single>=1 hold=1 busy>=1 dispose>=2 dialog_free=0`
    - `mark=1 mark_after_hold=1` (티칭 사진 파일로 찾은 기준점만 출처 기록 — Task 1 다시 구하기 조건)
    - `uses_helper=1 reads_line>=1 overlay_id>=1` (시험 찾기는 세 번째 인자 false — 다음 수동 RUN 재사용 표시를 걸지 않는다), `dialog_free=0` (두 서비스에 대화상자·파일 선택 없음)
  </acceptance_criteria>
  <done>대화상자 없는 공용 Test Find 와 기준 ROI 시험 찾기 판단 서비스가 빌드에 들어갔고, 시험 찾기는 런타임과 같은 기준점 찾기 경로가 계산한 국부 기준선을 돌려준다.</done>
</task>

<task type="auto">
  <name>Task 3: 메인 툴바 '기준 ROI 시험 찾기' 버튼 1개 — z1 사진 배경 + 기준 ROI 상자 + 주황 선 표시 (D-80-15)</name>
  <reversibility rating="reversible">툴바 버튼 1개와 표시 전용 핸들러 추가뿐</reversibility>
  <files>WPF_Example/UI/ContentItem/MainView.xaml, WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - CLAUDE.md ("UI 는 MVVM" — MainView.xaml.cs 에 새 로직 금지, 이번에 손대는 지점만)
    - WPF_Example/UI/ContentItem/MainView.xaml:285-345
    - WPF_Example/UI/ContentItem/MainView.xaml.cs:196-275 (_lastDisplayedImageShot, DisplayShotImage 캐시), :755-800 (HighlightSelectedRoi), :2296-2320 (ShowResultDatumOverlays), :4475-4600 (기존 Test Find — 바꾸지 않음)
    - WPF_Example/Custom/Sequence/Inspection/LocalRefTestFindService.cs (Task 2 결과)
  </read_first>
  <action>
(1) MainView.xaml — `btn_testFindDatum` 의 닫는 `</Button>`(:309) 다음, `btn_reanchor` 앞에 새 버튼 1개(D-80-00 의 세 번째 새 UI): `<Button x:Name="btn_testFindLocalRef" Content="기준 ROI 시험 찾기" Click="BtnTestFindLocalRef_Click" MinWidth="80" Height="28" Padding="12,4" FontSize="13" FontWeight="SemiBold" Background="#EA580C" Foreground="White" BorderBrush="#C2410C" BorderThickness="1" Margin="0,0,4,0" ToolTip="국부 기준(Local Ref)을 켠 측정을 고른 뒤 누르면, 기준점 가로 사진(z1)에서 기준 ROI 의 띠 에지 선을 찾아 주황 선으로 보여 줍니다">` + btn_testFindDatum 과 같은 Button.Template(CornerRadius 4 Border + ContentPresenter). 다른 줄은 건드리지 않는다.

(2) MainView.xaml.cs (K&R) — 기존 `BtnTestFindDatum_Click` 바로 위(그 메서드와 사진 출처 선택 메서드는 한 글자도 바꾸지 않는다)에 2개 메서드:
 - `private void BtnTestFindLocalRef_Click(object sender, RoutedEventArgs e)`: 선택 파라미터를 `mParentWindow.inspectionList.SelectedParam` 에서 얻고(mParentWindow·inspectionList 가 null 이면 null — if 블록), `LocalRefTestFindOutcome outcome = LocalRefTestFindService.Run(selected);` 다음 `ShowLocalRefTestFindOutcome(selected, outcome);`. 판단·문구는 서비스에 있다(D-80-17).
 - `private void ShowLocalRefTestFindOutcome(ParamBase selected, LocalRefTestFindOutcome outcome)`: 캔버스 표시만 한다 — `bool bHasImage = !string.IsNullOrEmpty(outcome.ImagePath);` 이면: `halconViewer.LoadImage(outcome.ImagePath);` → `_lastDisplayedImageShot = null;`(같은 Shot 재선택 때 z1 사진이 남지 않게 — DisplayShotImage 캐시) → `label_message.Visibility = Visibility.Collapsed;` → `HighlightSelectedRoi(selected);`(편집 가능한 기준 ROI 상자) → `ShowResultDatumOverlays(new List<DatumConfig> { outcome.Datum });`(보정 위치 초록 상자 + 기준점 선) → `halconViewer.SetInspectionOverlays(outcome.Overlays);`(주황 국부 기준선). 결과 한 줄은 툴바의 기존 라벨에 보인다(대화상자 없음 — D-80-00): `label_testFindResult.Content = outcome.Message;`, outcome.Ok 면 `label_testFindResult.Foreground = Brushes.LimeGreen;` 아니면 `Brushes.Red`(if/else — 라벨 선언 주석의 성공 LimeGreen / 실패 Red 규칙), `label_testFindResult.Visibility = Visibility.Visible;`. 사진이 없는 실패(측정 미선택 등)도 라벨만 바꾼다. 이 메서드에 계산·파일 판단을 넣지 않는다.

끝나면 두 파일만 `git add` 후 커밋.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p80; PB=$(cat $H/base-80-02.txt); W=WPF_Example; MV=$W/UI/ContentItem/MainView.xaml.cs
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" $W/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-80-02.txt 2>&1; echo "build_exit=$? cs_errors=$(grep -c 'error CS' $H/build-80-02.txt)"
for F in $W/UI/ContentItem/MainView.xaml $MV; do ADD=$(git diff -w -U0 $PB -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "$(basename $F) deleted=$(git diff -w -U0 $PB -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') qmark=$(printf '%s\n' "$ADD" | grep -cF '?')"; done
echo "btn=$(grep -cF 'x:Name="btn_testFindLocalRef"' $W/UI/ContentItem/MainView.xaml) btn_text=$(grep -cF 'Content="기준 ROI 시험 찾기"' $W/UI/ContentItem/MainView.xaml) click=$(grep -cF 'Click="BtnTestFindLocalRef_Click"' $W/UI/ContentItem/MainView.xaml)"
MVADD=$(git diff -w -U0 $PB -- "$MV" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "mv_dialog=$(printf '%s\n' "$MVADD" | grep -cE 'Message[B]ox') label_set=$(printf '%s\n' "$MVADD" | grep -c 'label_testFindResult.Content = outcome.Message;')"
echo "svc_call=$(grep -cF 'LocalRefTestFindService.Run(selected);' $MV) cache_reset=$(tr -d '\r' < $MV | awk '/private void ShowLocalRefTestFindOutcome/,/^        }$/' | grep -c '_lastDisplayedImageShot = null;') overlays=$(tr -d '\r' < $MV | awk '/private void ShowLocalRefTestFindOutcome/,/^        }$/' | grep -c 'SetInspectionOverlays(outcome.Overlays)')"
for FN in 'private void BtnTestFindDatum_Click' 'private HImage AskTestImageSource'; do B=$(git show $PB:$MV | tr -d '\r' | awk -v s="$FN" 'index($0,s){f=1} f{print} f&&/^        }$/{exit}' | md5sum | cut -c1-8); N=$(tr -d '\r' < $MV | awk -v s="$FN" 'index($0,s){f=1} f{print} f&&/^        }$/{exit}' | md5sum | cut -c1-8); echo "same[$FN]=$([ "$B" = "$N" ] && echo 1 || echo 0)"; done
    </automated>
  </verify>
  <acceptance_criteria>
    - `build_exit=0 cs_errors=0`
    - MainView.xaml `deleted=0`, MainView.xaml.cs `deleted=0`, 두 파일 더한 줄 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 qmark=0`
    - `btn=1 btn_text=1 click=1`
    - `svc_call=1 cache_reset=1 overlays=1`
    - `mv_dialog=0 label_set=1` (시험 찾기 결과는 새 대화상자가 아니라 기존 결과 라벨 한 줄 — D-80-00)
    - `same[private void BtnTestFindDatum_Click]=1`, `same[private HImage AskTestImageSource]=1` (기존 Test Find 무변경 — 회귀 0)
  </acceptance_criteria>
  <done>국부 기준을 켠 측정을 고르고 '기준 ROI 시험 찾기' 를 누르면 z1 사진 위에 기준 ROI 상자와 주황 국부 기준선이 보이는 배선이 빌드·커밋되었고, 기존 Test Find 버튼은 그대로다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 수동 RUN 측정 직전 → 사이클 국부 기준선 저장소 | 다시 구한 선이 PLC 자동 사이클 결과에 끼어들면 판정이 바뀐다 |
| UI 버튼(시험 찾기) → 시퀀스 기준점 캐시 | 자동 검사 중 캐시를 바꾸면 진행 중 자재 결과가 바뀐다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80-07 | Tampering | Action_FAIMeasurement.TryRecomputeStaleLocalRef | high | mitigate | 첫 문장 `IsProtocolDrivenCycle()` 가드로 PLC 자동 사이클은 null → 기존 전환과 비트 동일 (`first_stmt_guard=1`), 기준점 검출·주입 메서드 md5 무변경 |
| T-80-08 | Denial of Service | 다시 구하기·시험 찾기의 HImage | medium | mitigate | finally 에서 SafeDisposeImage / try Dispose, 캐시 HTuple 은 Dispose 안 함 (`safe_dispose=1`, `transform_dispose=0`, `dispose>=2`) |
| T-80-09 | Tampering | DatumTestFindService 가 자동 검사 중 캐시 변경 | medium | mitigate | `seq.State != Idle` 이면 ERR_SEQUENCE_BUSY 로 거절 (`busy>=1`) |
| T-80-10 | Tampering | 다시 구하기가 z1 이 아닌 사진(라이브 측정 높이·측정 사진) 사용 | medium | mitigate | TeachingImagePath 파일만 읽음, 취득 헬퍼·SimulImagePath 미사용 (`uses_grabhelper=0 uses_simul=0`) |
| T-80-22 | Tampering | 잡아 둔 기준점 변환은 다른 사진(기존 1장 Test Find 의 화면 사진·고른 파일)에서 나왔는데 선은 TeachingImagePath 로 다시 구해 '국부' 로 표시되는 조용한 오측정 | high | mitigate | 출처 기록 확인 뒤에만 다시 구함 — `IsDatumTransformFromTeachingPhotos`(기록한 HTuple 참조 = 지금 캐시 참조 + 경로 동일), 기록은 두 장짜리 HoldManualDatum 과 DatumTestFindService 에서만, 1장 HoldManualDatum 은 기록을 지움, ClearDatumTransforms 가 비움. 확인 안 되면 STALE 전환 + 이유 로그 (`prov_order_ok=1`, `hold_records=1`, `mark_after_hold=1`) |
</threat_model>

<verification>
1. Debug|x64 빌드 오류 0
2. stale 분기만 바뀌고 기준점 검출·주입·런 시작 메서드는 md5 동일, PLC 가드가 첫 문장, 그다음 사진 출처 확인(HoldManualDatum·ClearDatumTransforms 는 1줄 추가만)
3. 공용 Test Find 는 대화상자·파일 선택 없이 TeachingImagePath 로 TryComposeAlign/TryRunSingleDatum, 기존 BtnTestFindDatum_Click·사진 출처 선택 메서드 md5 동일
4. 가독성 grep 0 (새 파일 전체 + 기존 파일 더한 줄)
</verification>

<success_criteria>
- 함께 처리 2 와 D-80-15 가 코드로 존재하고 빌드된다 — 실제 동작은 80-05 UAT(U-8 Local Ref 재계산, U-9 시험 찾기)
- D-79-04/08 불변(PLC 자동·옵션 꺼짐 경로 무변경)
</success_criteria>

<output>
완료 후 `.planning/phases/80-reviewer-ng-cycle-reinspect/80-02-SUMMARY.md` 작성: base 해시, 커밋 해시 3개, 빌드 결과, 검증 출력 원문(same[...] 줄 포함), 새 심볼 실제 이름, 로그 문구 원문(다시 구함 / 시험 찾기 / TestFind), 버튼 문구.
</output>
</content>
</invoke>
