---
phase: 80-reviewer-ng-cycle-reinspect
plan: 03
type: execute
wave: 2
depends_on: ["80-01"]
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs
  - WPF_Example/Custom/Sequence/Inspection/ReviewerReinspectService.cs
  - WPF_Example/UI/Reviewer/ReviewerReinspectViewModel.cs
  - WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs
autonomous: true
requirements: [D-80-07, D-80-08, D-80-09, D-80-12, D-80-14, D-80-19, D-80-16, D-80-17]
user_setup: []

must_haves:
  truths:
    - "[D-80-07] PLC 자동 검사 NG 행을 불러오면 같은 자재의 모든 Shot 사진이 들어온다 — '같은 자재' 는 반복검사 저장 사이클 재검사와 같은 규칙(SavedCycleRerunPlanner.CollectAutoTicks → 시간순 → GroupIntoParts: 기준점 z tick 에서 새 자재 시작)이고, 고른 사이클 날짜와 그 전날 폴더를 본다(자정을 넘긴 자재). 고른 사이클의 사진이 같은 Shot 의 다른 tick 사진보다 우선이다(리뷰어에서 본 사진과 같게)"
    - "[D-80-07, D-80-19] NG 측정이 쓰는 기준점에 필요한 사진(단일 / 가로 H / 세로 V — ComputeRequiredDatumRoleKeys 규칙)이 부품에 전부 있고 파일도 있으면 기준점 사진(TeachingImagePath / TeachingImagePath_Vertical)을 불러온다. 하나라도 없으면 '없음' 으로 보고 기준점 사진은 하나도 바꾸지 않는다(짝이 반만 맞는 상태로 재검사하지 않음). 다른 기준점도 자기 사진이 전부 있을 때만 바뀐다. 두 장짜리 측정 사진(TeachingImagePath_Horizontal/Vertical)은 있는 것만 들어온다"
    - "[D-80-09] 짝이 맞는 기준점 사진이 없으면(수동 검사 기록 — IsProtocolDriven false 라 부품 스캔에서 빠지고 DatumImages 가 비어 있음 — 또는 파일 없음) 리뷰어에서 알림 대화상자 1개(제목 '기준점 사진 없음')를 띄운 뒤 Shot 사진만 불러오고, 상태 줄에 '(기준점 사진 없음 — 지금 기준점 사진 사용)' 을 붙인다. 이 대화상자가 이 phase 의 유일한 새 대화상자다(D-80-00)"
    - "[D-80-08] Z 범위 Shot 은 부품의 Z 후보 사진 전부가 RerunZRangeImagePaths 로 들어가 Z 자동 선택이 리뷰어와 같게 재현된다. 후보 사진이 없으면 고른 z 사진 1장(SimulImagePath)으로 검사하고 상태 줄에 '(Z 후보 사진 없음 — 고른 z 사진 1장으로 검사)' 를 붙인다. 수동 RUN 이 저장한 Z 후보 사진(PLC 조건 없는 저장 게이트 — RESEARCH Pitfall 1)도 있으면 쓴다"
    - "[D-80-14] 불러온 NG Shot 사진 확장자가 .jpg/.jpeg 면 상태 줄에 '(JPG 사진 — 실제 검사값과 조금 다를 수 있음)' 이 붙는다. 팝업 없음"
    - "[D-80-07, 로드맵 '사진·선은 리뷰어와 똑같이'] 사진이 들어온 Shot 마다 그 사진을 찍은 tick 의 FAI 오버레이(LastOverlays)가 메모리 FAI 에 복사되어, 메인 화면에서 그 Shot/측정을 누르면 리뷰어에서 본 사진과 선이 보인다. 사진이 들어온 모든 Shot 의 화면 버퍼(ShotConfig.SetImage)도 새 사진으로 바뀐다. [해제] 하면 복사한 선을 비운다"
    - "[함께 처리 1] 리뷰어 사이클 전체 보기(사이클 클릭)는 화면에 띄운 사진을 낸 Shot 의 FAI 선만 그린다 — 다른 Shot 의 선이 한 사진에 겹치지 않는다. 측정 행 클릭(FAI 한 개 보기)과 기존 ResolveCycleImagePath(cycle) 는 그대로다"
    - "[D-80-11 보강] 리뷰어 사진을 쓰는 동안 반복검사의 '저장 사진 재검사' 시작은 이유 문구와 함께 거절된다(RepeatRunService.StartFromSavedCycles) — 반대로 저장 사진 재검사 중에는 리뷰어 버튼이 막힌다(80-01). RepeatRunService 의 기존 줄은 한 줄도 지워지지 않는다"
    - "[D-80-16, D-80-17] 더한 줄·새 코드 가독성 grep 0, 부품 묶기는 SavedCycleRerunPlanner 안 private 형제 메서드 재사용(새 그룹핑 알고리즘 없음), 알림 문구·상태 줄 안내 문구는 VM 에서 만들고 ReviewerWindow.xaml.cs 에는 알림 표시 1문장만 있다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs"
      provides: "같은 자재 묶기 진입점 + 기준점 사진 완전성 판정 + 재검사 상호 배제"
      contains: "public static bool IsDatumPhotoSetComplete("
    - path: "WPF_Example/Custom/Sequence/Inspection/ReviewerReinspectService.cs"
      provides: "기준점·두 장짜리·Z 사진 적용, 모든 Shot 버퍼, 리뷰어 선 복사, 안내 상태"
      contains: "private static void ApplyReviewerOverlays("
    - path: "WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs"
      provides: "사진을 낸 Shot 을 함께 돌려주는 오버로드 + Shot 선 모으기"
      contains: "public static string ResolveCycleImagePath(CycleResultDto cycle, out ShotResultDto ownerShot)"
  key_links:
    - from: "ReviewerReinspectService.LoadForRow"
      to: "SavedCycleRerunPlanner.GroupIntoParts / FillPart*"
      via: "BuildPartForSingleCycle(CollectAutoTicks → GroupIntoParts → FindPartContainingCycle → SeedSelectedTickPhotos → Fill*)"
      pattern: "GroupIntoParts\\(lstTicks, seq"
    - from: "ReviewerReinspectService.LoadForRow"
      to: "SavedCycleRerunPlanner.IsDatumPhotoSetComplete"
      via: "NG 측정의 DatumRef 로 완전성 판정 → IsDatumPhotoMissing → VM AlertPresenter"
      pattern: "SavedCycleRerunPlanner\\.IsDatumPhotoSetComplete\\("
    - from: "ReviewerWindow.DisplayCycle"
      to: "ReviewerImagePathResolver.ResolveCycleImagePath(cycle, out ownerShot) + CollectShotOverlays(ownerShot)"
      via: "사이클 전체 보기 사진과 같은 Shot 의 선만"
      pattern: "CollectShotOverlays\\("
---

<objective>
80-01 의 tracer(고른 tick 1개)를 **같은 자재 전체**로 넓힌다: 모든 Shot 사진 + NG 측정 기준점의 가로·세로 사진 + Z 후보 사진을 반복검사와 같은 부품 규칙으로 모아 넣고(D-80-07/08), 기준점 사진 짝이 안 맞으면 알림 후 Shot 사진만 넣는다(D-80-09/19). 상태 줄에 JPG·Z 후보·기준점 안내를 붙인다(D-80-12/14). 불러온 사진과 선이 리뷰어에서 본 것과 같도록 리뷰어의 사이클 전체 보기 선 겹침 버그(함께 처리 1)도 고친다.

Purpose: 사용자가 1738·1743 폴더에서 기준점 사진 짝을 손으로 찾던 일을 프로그램이 대신하는 핵심 가치(CONTEXT specifics).

Output: RepeatRunService.cs(SavedCycleRerunPlanner 메서드 본문 확장 + 새 메서드, 기존 줄 삭제 0), 서비스·VM 확장, ReviewMeasurementRow.cs 오버로드 추가, ReviewerWindow.xaml.cs DisplayCycle 오버레이 수정.

⚠ spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/80-reviewer-ng-cycle-reinspect/80-CONTEXT.md
@.planning/phases/80-reviewer-ng-cycle-reinspect/80-RESEARCH.md
@.planning/phases/80-reviewer-ng-cycle-reinspect/80-01-SUMMARY.md

<interfaces>
80-01 이 만든 것(이 plan 이 확장): `SavedCycleRerunPlanner.BuildPartForSingleCycle(CycleResultDto, InspectionSequence, InspectionRecipeManager)`(지금은 BuildSelectedTickPart + FillPartDualPhotos), private `BuildSelectedTickPart(CycleResultDto)`, `ReviewerReinspectService` 의 `LoadForRow`, `ApplyPartPaths(SavedCycleRerunPart)`, `ReleaseCore(string, bool bClearDisplayState)`, `LoadShotBuffer`, `ClearReviewerBuffers`, `s_snapshot`(OwnedShots, Sequence), `s_activePart`, `s_state`, `ReviewerReinspectLoadResult`, `ReviewerReinspectState.Clone()`, VM `ApplySelection`, `BuildStatusText`, `RefreshFromService`. 실제 이름은 80-01-SUMMARY.md 로 확인한다.

WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs (Allman) — 80-01 이후 라인은 밀렸을 수 있으니 이름으로 찾는다
- `StartFromSavedCycles(InspectionSequence seq, SavedCycleRerunPlan plan, out string szError)` — 첫 가드 `if (IsRunning) { szError = "이미 반복 실행 중입니다"; return false; }`
- `ApplySavedCyclePart` — 기준점 역할 키로 경로 넣는 규칙(ROLE_SINGLE·ROLE_HORIZONTAL → TeachingImagePath, ROLE_VERTICAL → TeachingImagePath_Vertical), 두 장짜리 측정은 HorizontalPath/VerticalPath 가 비지 않을 때만
- `FindOwnedDualMeasurement(szShotName, szFaiName, szMeasKey)` — 키는 MeasurementName 이 비면 TypeName
- SavedCycleRerunPlanner: `BuildDatumRoleKey(name, role)` = name + "|" + role, `CollectAutoTicks(StatisticsTimeRange, string, InspectionSequence)`(ResultSavePath\yyyyMMdd\*_cycle, IsEligibleAutoTick: IsProtocolDriven && ZIndex>=0 && 레시피 일치 && 이 시퀀스 Shot 포함), `CompareByInspectionTime`, `GroupIntoParts(lstTicks, seq, plan)`(plan 은 제외 집계용 — 여기선 버리는 새 SavedCycleRerunPlan), `FillPartDatumPhotos/ShotPhotos/DualPhotos/ZRangePhotos`(전부 "첫 기록 우선"), `ComputeRequiredDatumRoleKeys(seq)`(두 장짜리 크로스-Z 설정이면 H·V, 두 장짜리 정적이면 요구 없음, 그 밖 단일), `ValidatePart`(부르지 않음)
WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs:92 `public static StatisticsTimeRange FromDates(DateTime dtFromDate, DateTime dtToDate)`(하루 전체) — StatisticsTimeRange 의 FirstDate/LastDate 는 읽기 전용(대입 불가)
WPF_Example/UI/ViewModel/CycleResultDto.cs — :75-77 `DatumImageRecordDto.ROLE_SINGLE = ""`, `ROLE_HORIZONTAL = "H"`, `ROLE_VERTICAL = "V"`, :737 `public static string NgCauseHistory.ResolveCycleKey(CycleResultDto dto)`(CycleFolderPath 우선, 없으면 시각 Ticks)
WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs — `IsZRangeEnabled()` :224, `FAIList` :155
WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs — :107 `public List<EdgeInspectionOverlay> LastOverlays { get; set; }`, :124 `FAIName`
WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs (Allman, namespace ReringProject.UI) — :240-269 `public static string ResolveCycleImagePath(CycleResultDto cycle)`(바꾸지 않음), :272-292 `private static string FindMeasuredOriginInShot(ShotResultDto shot)`, :182 `IsExistingFile`
WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs (Allman) — DisplayCycle 안 :174-193(주석 "기본 전체 보기" → `ResolveCycleImagePath(cycle)` → LoadImage → `cycle.Shots.SelectMany(...)` 로 전 Shot 선 합산 → SetInspectionOverlays), :295-304 `ApplyFaiOverlays`(바꾸지 않음), 80-01 이 넣은 `_reinspectVm` 필드·생성자 배선
</interfaces>
</context>

## Artifacts this phase produces (이 plan 분)

phase 전체 원장은 `80-01-PLAN.md`. 이 plan 이 만드는 것:
- RepeatRunService.cs: `SavedCycleRerunPlanner.IsDatumPhotoSetComplete(SavedCycleRerunPart part, InspectionSequence seq, string szDatumName)`, private `FindPartContainingCycle(List<SavedCycleRerunPart>, string)`, `SeedSelectedTickPhotos(SavedCycleRerunPart target, SavedCycleRerunPart selectedOnly)`, `LogSingleCyclePart(...)`, const `PREVIOUS_DAY_OFFSET`, `RepeatRunService.ERROR_REVIEWER_PHOTOS_ACTIVE`
- ReviewerReinspectService.cs: `ReviewerReinspectLoadResult.IsDatumPhotoMissing`, `ReviewerReinspectState.IsJpgPhoto / IsZCandidateMissing / IsDatumPhotoKept`, private `ApplyDatumPhotos`, `ApplyDualPhotos`, `FindOwnedDualMeasurement`, `ApplyReviewerOverlays`, `ClearReviewerOverlays`, `FindSourceShotDto`, `IsJpgPath`, static `s_bDatumPhotosApplied`, `s_lstOverlayFais`, const `JPG_EXTENSION`, `JPEG_EXTENSION`
- ReviewerReinspectViewModel.cs: `AlertPresenter`, const `ALERT_TITLE_DATUM_MISSING`, `ALERT_TEXT_DATUM_MISSING`, `HINT_JPG`, `HINT_Z_MISSING`, `HINT_DATUM_KEPT`
- ReviewMeasurementRow.cs: `ReviewerImagePathResolver.ResolveCycleImagePath(CycleResultDto, out ShotResultDto)`, `ReviewerImagePathResolver.CollectShotOverlays(ShotResultDto)`
- ReviewerWindow.xaml.cs: `ShowReinspectAlert(string, string)`
- 저장소 밖: `/c/Users/admin/AppData/Local/Temp/p80/base-80-03.txt`, `build-80-03.txt`

<tasks>

<task type="auto">
  <name>Task 1: 같은 자재 묶기 — BuildPartForSingleCycle 을 GroupIntoParts 로 넓히고, 기준점 사진 완전성 판정·저장 사진 재검사 상호 배제 추가</name>
  <precondition>80-01-SUMMARY.md 가 있고 `grep -c 'public static SavedCycleRerunPart BuildPartForSingleCycle(' WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs` = 1, Debug 앱 미실행</precondition>
  <reversibility rating="reversible">SavedCycleRerunPlanner 안 새 메서드와 80-01 이 만든 메서드 본문 변경, RepeatRunService 가드 1개 — 기존 반복검사 줄 삭제 0</reversibility>
  <files>WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs</files>
  <read_first>
    - CLAUDE.md (🔒 가독성 규칙)
    - .planning/phases/80-reviewer-ng-cycle-reinspect/80-RESEARCH.md "Pattern 1", "Pitfall 1", "Open Questions" 2
    - .planning/phases/80-reviewer-ng-cycle-reinspect/80-01-SUMMARY.md
    - WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs (StartFromSavedCycles 앞부분, ApplySavedCyclePart, SavedCycleRerunPlanner 전체)
    - WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs:28-102 (StatisticsTimeRange)
    - WPF_Example/UI/ViewModel/CycleResultDto.cs:1-100, :725-760
  </read_first>
  <action>
(0) 편집 전: `git rev-parse HEAD > /c/Users/admin/AppData/Local/Temp/p80/base-80-03.txt`.

(1) SavedCycleRerunPlanner 에 const `private const int PREVIOUS_DAY_OFFSET = -1;`(자정을 넘긴 자재를 위해 전날 폴더도 본다).

(2) `BuildPartForSingleCycle` 본문(80-01 이 쓴 줄)을 이렇게 바꾼다 — 시그니처·요약 주석은 유지하고 "같은 자재 묶기(D-80-07)" 설명을 더한다:
 ① 인자 null 검사(그대로). ② `SavedCycleRerunPart selectedOnly = BuildSelectedTickPart(selectedCycle);`. ③ `bool bAutoTick = IsEligibleAutoTick(selectedCycle, selectedCycle.RecipeName, seq);` 아니면(수동 RUN 기록 — 부품 개념 없음, D-80-09 경로) `FillPartDualPhotos(selectedOnly, seq, recipeManager)` 후 `LogSingleCyclePart(seq, selectedOnly, "수동 기록 — 고른 사이클만")` 하고 selectedOnly 반환. ④ `DateTime dtDay = selectedCycle.InspectionTime.Date;` `StatisticsTimeRange range = StatisticsTimeRange.FromDates(dtDay.AddDays(PREVIOUS_DAY_OFFSET), dtDay);` (FirstDate/LastDate 는 읽기 전용이라 FromDates 로만 만든다). ⑤ `List<CycleResultDto> lstTicks = CollectAutoTicks(range, selectedCycle.RecipeName, seq);` `lstTicks.Sort(CompareByInspectionTime);` `List<SavedCycleRerunPart> lstParts = GroupIntoParts(lstTicks, seq, new SavedCycleRerunPlan());`(제외 집계는 버린다 — ValidatePart 를 부르지 않는다). ⑥ `SavedCycleRerunPart target = FindPartContainingCycle(lstParts, NgCauseHistory.ResolveCycleKey(selectedCycle));` null 이면(기준점 tick 없이 시작된 사이클 등) selectedOnly 에 FillPartDualPhotos 후 `LogSingleCyclePart(seq, selectedOnly, "같은 자재 묶음을 찾지 못함 — 고른 사이클만")` 하고 반환. ⑦ `SeedSelectedTickPhotos(target, selectedOnly);` 다음 `FillPartDatumPhotos(target)`, `FillPartShotPhotos(target)`, `FillPartDualPhotos(target, seq, recipeManager)`, `FillPartZRangePhotos(target)`. ⑧ `LogSingleCyclePart(seq, target, "같은 자재")` 후 target 반환. 스톱워치로 걸린 ms 를 로그에 넣는다(UI 스레드에서 두 날짜 폴더 cycle.json 을 읽으므로).
 - private static `FindPartContainingCycle(List<SavedCycleRerunPart> lstParts, string szCycleKey)` — 부품·tick 을 돌며 `NgCauseHistory.ResolveCycleKey(tick)` 이 szCycleKey 와 Ordinal 로 같은 첫 부품. 중첩 2단계 이하(안쪽 판단은 bool 선추출).
 - private static `SeedSelectedTickPhotos(SavedCycleRerunPart target, SavedCycleRerunPart selectedOnly)` — selectedOnly.ShotPhotoPaths 를 target.ShotPhotoPaths 에 먼저 넣고(같은 키면 덮어씀), selectedOnly.ZRangePhotoPaths 의 Shot 마다 새 Dictionary 복사본을 target 에 넣는다. Fill 메서드들이 "첫 기록 우선" 이므로 고른 사이클 사진이 이긴다(리뷰어에서 본 사진과 같게).
 - private static `LogSingleCyclePart(InspectionSequence seq, SavedCycleRerunPart part, string szKind)` 와 스톱워치 ms 인자 — Trace 로그 `"[Rerun] 리뷰어 부품(" + szKind + ") — " + seq.Name + " tick " + part.Ticks.Count + "개 · Shot 사진 " + part.ShotPhotoPaths.Count + " · 기준점 사진 " + part.DatumPhotoPaths.Count + " · Z 후보 Shot " + part.ZRangePhotoPaths.Count + " · " + ms + " ms"`, try/catch 로 감싼다.

(3) `public static bool IsDatumPhotoSetComplete(SavedCycleRerunPart part, InspectionSequence seq, string szDatumName)` — 요약 주석: D-80-19 — 이 기준점에 필요한 사진이 하나라도 없거나 파일이 없으면 false(짝이 반만 맞는 재검사 금지). 인자 검사(part·seq null, 이름 빈 문자열 → false). seq.DatumConfigs 에 그 이름의 기준점이 없으면 false. 이 기준점 역할 키 3개(`BuildDatumRoleKey(szDatumName, DatumImageRecordDto.ROLE_SINGLE/ROLE_HORIZONTAL/ROLE_VERTICAL)`)를 만들고 `ComputeRequiredDatumRoleKeys(seq)` 결과 중 그 셋 가운데 하나와 같은 키만 본다(`bool bThisDatum`). 그 키가 `part.DatumPhotoPaths` 에 없거나 `File.Exists` 가 false 면 false. 끝까지 통과하면 true(요구 키가 없는 두 장짜리 정적 기준점은 지금 티칭 사진을 그대로 쓰므로 true — RepeatRunService 재량 결정과 같음). ComputeRequiredDatumRoleKeys 자체는 바꾸지 않는다.

(4) RepeatRunService 클래스: const `public const string ERROR_REVIEWER_PHOTOS_ACTIVE = "리뷰어 사진 사용 중에는 저장 사진 재검사를 시작할 수 없습니다 — 메인 화면 상태 줄의 [해제] 를 누른 뒤 다시 시작하세요";` 와 StartFromSavedCycles 의 IsRunning 가드 블록 바로 다음에 `if (ReviewerReinspectService.IsActive) { szError = ERROR_REVIEWER_PHOTOS_ACTIVE; return false; }` 블록 — 주석 "// Phase 80: 리뷰어 경로 스냅샷과 재검사 스냅샷이 겹치면 해제 뒤 리뷰어 경로가 되살아난다".

기존 줄(BuildPlan, ValidatePart, GroupIntoParts, CollectAutoTicks, Fill*, ComputeRequiredDatumRoleKeys, ApplySavedCyclePart, 스냅샷/복원)은 한 글자도 바꾸지 않는다. 끝나면 이 파일만 `git add` 후 커밋.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p80; PB=$(cat $H/base-80-03.txt); PH=$(cat $H/base-80-phase.txt); W=WPF_Example; R=$W/Custom/Sequence/Inspection/RepeatRunService.cs
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" $W/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-80-03.txt 2>&1; echo "build_exit=$? cs_errors=$(grep -c 'error CS' $H/build-80-03.txt)"
ADD=$(git diff -w -U0 $PB -- "$R" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "rrs ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') qmark=$(printf '%s\n' "$ADD" | grep -cF '?') logic3=$(printf '%s\n' "$ADD" | grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)')"
echo "deleted_vs_phase=$(git diff -w -U0 $PH -- $R | grep -E '^-' | grep -vE '^---' | wc -l)"
B=$(tr -d '\r' < $R | awk '/public static SavedCycleRerunPart BuildPartForSingleCycle/,/^        }$/')
echo "group=$(printf '%s\n' "$B" | grep -c 'GroupIntoParts(lstTicks, seq') collect=$(printf '%s\n' "$B" | grep -c 'CollectAutoTicks(range') fromdates=$(printf '%s\n' "$B" | grep -c 'StatisticsTimeRange.FromDates(') seed=$(printf '%s\n' "$B" | grep -c 'SeedSelectedTickPhotos(target, selectedOnly)') validate=$(printf '%s\n' "$B" | grep -c 'ValidatePart(') cyclekey=$(printf '%s\n' "$B" | grep -c 'NgCauseHistory.ResolveCycleKey(selectedCycle)')"
echo "complete_def=$(grep -c 'public static bool IsDatumPhotoSetComplete(SavedCycleRerunPart part, InspectionSequence seq, string szDatumName)' $R) complete_exists=$(tr -d '\r' < $R | awk '/public static bool IsDatumPhotoSetComplete/,/^        }$/' | grep -c 'File.Exists') guard=$(grep -c 'if (ReviewerReinspectService.IsActive)' $R) guard_msg=$(grep -c 'ERROR_REVIEWER_PHOTOS_ACTIVE' $R)"
for FN in 'public static SavedCycleRerunPlan BuildPlan' 'private static string ValidatePart' 'private static List<SavedCycleRerunPart> GroupIntoParts' 'private static List<CycleResultDto> CollectAutoTicks' 'private static void FillPartDatumPhotos' 'private static void FillPartShotPhotos' 'private static void FillPartZRangePhotos' 'private static List<string> ComputeRequiredDatumRoleKeys' 'private void ApplySavedCyclePart'; do Bh=$(git show $PH:$R | tr -d '\r' | awk -v s="$FN" 'index($0,s){f=1} f{print} f&&/^        }$/{exit}' | md5sum | cut -c1-8); Nh=$(tr -d '\r' < $R | awk -v s="$FN" 'index($0,s){f=1} f{print} f&&/^        }$/{exit}' | md5sum | cut -c1-8); echo "same[$FN]=$([ "$Bh" = "$Nh" ] && echo 1 || echo 0)"; done
    </automated>
  </verify>
  <acceptance_criteria>
    - `build_exit=0 cs_errors=0`
    - 더한 줄 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 qmark=0 logic3=0`
    - `deleted_vs_phase=0` (phase 시작 대비 RepeatRunService 기존 줄 삭제 0)
    - `group=1 collect=1 fromdates=1 seed=1 validate=0 cyclekey=1`
    - `complete_def=1 complete_exists>=1 guard=1 guard_msg=2`
    - `same[...]=1` 9개 전부 (BuildPlan · ValidatePart · GroupIntoParts · CollectAutoTicks · Fill 3종 · ComputeRequiredDatumRoleKeys · ApplySavedCyclePart 무변경)
  </acceptance_criteria>
  <done>고른 PLC 자동 사이클이 속한 자재 부품 전체(Shot·기준점·Z 후보·두 장짜리 사진)가 반복검사와 같은 규칙으로 모이고, 기준점 사진 완전성을 판정할 수 있으며, 리뷰어 사진 사용 중에는 저장 사진 재검사가 시작되지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: 서비스·VM — 기준점(완전할 때만)·두 장짜리·Z 사진 적용, 모든 Shot 버퍼, 리뷰어 선 복사, 기준점 없음 알림·JPG·Z 안내</name>
  <reversibility rating="reversible">메모리 경로·화면 버퍼·오버레이만 바꾸고 해제 때 되돌린다</reversibility>
  <files>WPF_Example/Custom/Sequence/Inspection/ReviewerReinspectService.cs, WPF_Example/UI/Reviewer/ReviewerReinspectViewModel.cs</files>
  <read_first>
    - CLAUDE.md (🔒 가독성 규칙, HImage Dispose, MVVM — 문구는 VM)
    - .planning/phases/80-reviewer-ng-cycle-reinspect/80-CONTEXT.md (D-80-07/08/09/12/14/19)
    - WPF_Example/Custom/Sequence/Inspection/ReviewerReinspectService.cs (80-01 결과 전체)
    - WPF_Example/UI/Reviewer/ReviewerReinspectViewModel.cs (80-01 결과 전체)
    - WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs 의 ApplySavedCyclePart, FindOwnedDualMeasurement, IsDatumPhotoSetComplete(Task 1)
    - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs:100-130
    - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs:215-240
  </read_first>
  <action>
(1) ReviewerReinspectService.cs:
 - `ReviewerReinspectLoadResult` 에 `bool IsDatumPhotoMissing`, `ReviewerReinspectState` 에 `bool IsJpgPhoto`, `bool IsZCandidateMissing`, `bool IsDatumPhotoKept`(Clone 에도 복사).
 - const `JPG_EXTENSION = ".jpg"`, `JPEG_EXTENSION = ".jpeg"`. static `private static bool s_bDatumPhotosApplied;`, `private static List<FAIConfig> s_lstOverlayFais = new List<FAIConfig>();`.
 - LoadForRow 잠금 밖(부품 만든 뒤): `string szNgDatumRef` = liveMeas 가 있으면 liveMeas.DatumRef, 없으면 ""(if/else). `bool bNeedsDatum = !string.IsNullOrEmpty(szNgDatumRef);` `bool bDatumComplete = true;` bNeedsDatum 이면 `SavedCycleRerunPlanner.IsDatumPhotoSetComplete(part, seq, szNgDatumRef)`.
 - 잠금 안, ApplyPartPaths 호출 전에 `s_bDatumPhotosApplied = bDatumComplete;`. ApplyPartPaths 에 두 단계를 더한다(같은 메서드라 80-01 의 RunWithOriginalPaths 재적용에도 그대로 따라온다): s_bDatumPhotosApplied 면 `ApplyDatumPhotos(part, s_snapshot.Sequence)` — 시퀀스 DatumConfigs 마다 `SavedCycleRerunPlanner.IsDatumPhotoSetComplete(part, seq, datum.DatumName)` 인 기준점만 역할 키로 경로를 넣는다(RepeatRunService.ApplySavedCyclePart 의 기준점 부분과 같은 키·같은 대상: SINGLE·H → TeachingImagePath, V → TeachingImagePath_Vertical). 이어서 항상 `ApplyDualPhotos(part)` — part.DualPhotos 마다 `FindOwnedDualMeasurement(ShotName, FAIName, MeasurementName)`(RepeatRunService:784-822 와 같은 규칙으로 s_snapshot.OwnedShots 에서 찾는 private 복사본 — 인스턴스 메서드를 바꾸지 않는다) 결과가 있고 경로가 비지 않고 파일이 있을 때만 TeachingImagePath_Horizontal / TeachingImagePath_Vertical 에 넣는다. 기준점 완전성이 false 면 기준점 경로는 하나도 바꾸지 않는다(D-80-09/19).
 - 화면 버퍼: ClearReviewerBuffers 뒤, 스냅샷 OwnedShots 중 SimulImagePath 가 스냅샷 값과 달라진(부품 사진이 들어온) Shot 마다 LoadShotBuffer — NG Shot 만이 아니라 전부(D-80-07 "사진이 메인 화면에 들어온다"). HImage 규칙은 80-01 과 같다(finally Dispose).
 - 리뷰어 선: `ClearReviewerOverlays()`(이전 불러오기에서 복사한 FAI 들의 LastOverlays 를 새 빈 List 로, 목록 비움) 다음 `ApplyReviewerOverlays(part, shotDto)` — 사진이 들어온 소유 Shot 마다 원천 ShotResultDto 를 정한다: 그 Shot 이 NG Shot(shotDto.ShotName 과 같음)이면 shotDto, 아니면 `FindSourceShotDto(part, shot.ShotName, shot.SimulImagePath)`(part.Ticks → Shots 에서 ShotName 이 같고 FAIs 중 OriginImageFileName 이 그 경로와 OrdinalIgnoreCase 로 같은 첫 ShotResultDto). 원천이 있으면 메모리 FAI 마다 FAIName 이 같은 FaiResultDto 의 LastOverlays 를 `new List<EdgeInspectionOverlay>(...)` 로 복사해 대입, 없거나 null 이면 새 빈 List 대입(다른 자재의 선이 남지 않게). 대입한 FAI 를 s_lstOverlayFais 에 넣는다. 이 두 메서드는 UI 스레드(LoadForRow)에서만 부른다.
 - ReleaseCore: bClearDisplayState(=사용자 [해제], UI 스레드)면 `ClearReviewerOverlays()`, 아니면(PLC·레시피·종료 — 백그라운드이거나 곧 새 검사가 선을 다시 채움) s_lstOverlayFais 목록만 비운다. s_bDatumPhotosApplied 는 false 로.
 - 상태: `IsJpgPhoto = IsJpgPath(liveShot.SimulImagePath)`(private static — Path.GetExtension 이 JPG_EXTENSION 또는 JPEG_EXTENSION 과 OrdinalIgnoreCase 로 같음, 빈 경로면 false), `IsZCandidateMissing` = liveShot.IsZRangeEnabled() 이면서 part.ZRangePhotoPaths 에 그 Shot 이 없거나 사전이 비었을 때(bool 선추출), `IsDatumPhotoKept = bNeedsDatum && !bDatumComplete`. 결과 `IsDatumPhotoMissing` 도 같은 값.
 - 불러옴 로그에 " · 기준점 사진 " + (적용 / 지금 것 유지) + " · Z 후보 " + 그 Shot 후보 수 + "장" 을 더한다.

(2) ReviewerReinspectViewModel.cs:
 - 훅 `public Action<string, string> AlertPresenter { get; set; }`(ReviewerWindow 가 채움 — Task 3).
 - const `ALERT_TITLE_DATUM_MISSING = "기준점 사진 없음"`, `ALERT_TEXT_DATUM_MISSING = "이 검사에는 짝이 맞는 기준점 사진이 없습니다 (수동 검사 기록이거나 사진 파일이 없음).\nShot 사진만 불러오고, 기준점 사진은 지금 것을 그대로 씁니다.\n기준점 Test Find 는 자동으로 하지 않습니다."`, `HINT_JPG = "  (JPG 사진 — 실제 검사값과 조금 다를 수 있음)"`, `HINT_Z_MISSING = "  (Z 후보 사진 없음 — 고른 z 사진 1장으로 검사)"`, `HINT_DATUM_KEPT = "  (기준점 사진 없음 — 지금 기준점 사진 사용)"`.
 - ApplySelection: 불러오기 성공 뒤 MainNavigator 보다 먼저, `bool bShowAlert = result.IsDatumPhotoMissing && AlertPresenter != null;` 이면 `AlertPresenter(ALERT_TITLE_DATUM_MISSING, ALERT_TEXT_DATUM_MISSING)`(D-80-09 알림 후 메인 화면).
 - BuildStatusText: 기본 문구 뒤에 IsJpgPhoto → HINT_JPG, IsZCandidateMissing → HINT_Z_MISSING, IsDatumPhotoKept → HINT_DATUM_KEPT 순으로 붙인다(각각 if, D-80-12/14/08/09).

끝나면 두 파일만 `git add` 후 커밋.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p80; W=WPF_Example; SVC=$W/Custom/Sequence/Inspection/ReviewerReinspectService.cs; VM=$W/UI/Reviewer/ReviewerReinspectViewModel.cs
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" $W/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-80-03.txt 2>&1; echo "build_exit=$? cs_errors=$(grep -c 'error CS' $H/build-80-03.txt)"
for F in $SVC $VM; do echo "$(basename $F) ternary=$(grep -cE '\?[^?]*:' $F) coalesce=$(grep -cF '??' $F) nullcond=$(grep -cF '?.' $F) switchexpr=$(grep -cE 'switch.*=>' $F) datesig=$(grep -cF 'hbk' $F) qmark=$(grep -cF '?' $F) logic3=$(grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)' $F)"; done
echo "complete_call=$(grep -c 'SavedCycleRerunPlanner.IsDatumPhotoSetComplete(' $SVC) datum_apply=$(grep -c 'ApplyDatumPhotos(' $SVC) dual_apply=$(grep -c 'ApplyDualPhotos(' $SVC) overlays=$(grep -c 'ApplyReviewerOverlays(' $SVC) overlay_copy=$(grep -c 'new List<EdgeInspectionOverlay>(' $SVC) jpg=$(grep -c 'JPEG_EXTENSION' $SVC) zmiss=$(grep -c 'IsZCandidateMissing' $SVC) dispose_finally=$(grep -c 'Dispose(); } catch { }' $SVC)"
echo "flag_in_apply=$(tr -d '\r' < $SVC | awk '/private static void ApplyPartPaths/,/^        }$/' | grep -c 's_bDatumPhotosApplied')"
echo "alert_hook=$(grep -c 'public Action<string, string> AlertPresenter' $VM) alert_call=$(grep -c 'AlertPresenter(ALERT_TITLE_DATUM_MISSING, ALERT_TEXT_DATUM_MISSING)' $VM) hints=$(grep -cE 'HINT_JPG|HINT_Z_MISSING|HINT_DATUM_KEPT' $VM) jpg_text=$(grep -cF 'JPG 사진 — 실제 검사값과 조금 다를 수 있음' $VM)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `build_exit=0 cs_errors=0`
    - 두 파일 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 qmark=0 logic3=0`
    - `complete_call>=2`(NG 기준점 판정 + 기준점별 적용), `datum_apply>=2 dual_apply>=2 overlays>=2 overlay_copy>=1 jpg>=2 zmiss>=2 dispose_finally>=1`
    - `flag_in_apply>=1` (저장 뒤 재적용도 같은 기준점 규칙을 따른다)
    - `alert_hook=1 alert_call=1 hints>=6 jpg_text=1`
  </acceptance_criteria>
  <done>PLC 자동 NG 행을 불러오면 같은 자재의 Shot·기준점(완전할 때만)·두 장짜리·Z 후보 사진과 리뷰어 선이 메인 레시피 객체·화면 버퍼에 들어가고, 기준점 짝이 안 맞으면 알림 1개 후 Shot 사진만 들어가며, 상태 줄에 JPG·Z·기준점 안내가 붙는다.</done>
</task>

<task type="auto">
  <name>Task 3: 리뷰어 창 — 사이클 전체 보기 선 겹침 수정(함께 처리 1) + 기준점 없음 알림 배선</name>
  <reversibility rating="reversible">DisplayCycle 의 오버레이 합산 몇 줄 교체와 알림 배선 1줄 — 저장 데이터 무변경</reversibility>
  <files>WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs, WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs</files>
  <read_first>
    - CLAUDE.md (🔒 가독성 규칙, MVVM — code-behind 는 배선만)
    - .planning/phases/80-reviewer-ng-cycle-reinspect/80-RESEARCH.md "Pattern 6"
    - WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs:143-294
    - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs:1-310 (80-01 배선 포함)
  </read_first>
  <action>
(1) ReviewMeasurementRow.cs — `ReviewerImagePathResolver` 안에 두 메서드를 더한다(Allman, using System.Collections.Generic · ReringProject.Halcon.Models 를 파일 머리에 더함). 기존 `ResolveCycleImagePath(CycleResultDto cycle)` 와 나머지 메서드는 한 글자도 바꾸지 않는다(회귀 0):
 - `public static string ResolveCycleImagePath(CycleResultDto cycle, out ShotResultDto ownerShot)` — 기존 오버로드와 같은 규칙(Shot 순서대로 `FindMeasuredOriginInShot` 가 경로를 내면 그 Shot 을 ownerShot 으로, 없으면 첫 Shot 의 ResultImagePath 가 `IsExistingFile` 이면 그 Shot)으로 경로와 그 경로를 낸 Shot 을 함께 돌려준다. 못 찾으면 ownerShot null, 반환 null.
 - `public static List<EdgeInspectionOverlay> CollectShotOverlays(ShotResultDto shot)` — shot 이나 FAIs 가 null 이면 빈 List. FAI 마다 LastOverlays 가 null 이 아니면 AddRange. 한 Shot 의 선만 모은다.

(2) ReviewerWindow.xaml.cs DisplayCycle — 사이클 전체 보기 부분(주석 "기본 전체 보기" 부터 `halconViewer.SetInspectionOverlays(allOverlays);` 까지)을 이렇게 바꾼다: `ShotResultDto ownerShot;` `string szCycleImagePath = ReviewerImagePathResolver.ResolveCycleImagePath(cycle, out ownerShot);` → 기존과 같은 LoadImage 가드 → `halconViewer.SetInspectionOverlays(ReviewerImagePathResolver.CollectShotOverlays(ownerShot));`. 주석 "// Phase 80 함께 처리 1: 화면에 띄운 사진을 낸 Shot 의 선만 그린다 — 전 Shot 선을 한 사진에 겹쳐 그리던 문제. 순서: LoadImage → SetInspectionOverlays". 사진이 없으면 ownerShot 이 null 이라 선도 없다. DisplayCycle 의 다른 줄(행 만들기, ApplyRowFilter, 80-01 의 EvaluateSelection)과 MeasurementGrid_SelectionChanged·ApplyFaiOverlays·ShowAxisImage 는 바꾸지 않는다.

(3) ReviewerWindow.xaml.cs 알림 배선(D-80-09, D-80-17): 생성자의 `panel_reinspect.DataContext = _reinspectVm;` 다음에 `_reinspectVm.AlertPresenter = ShowReinspectAlert;` 1줄, 새 메서드 `private void ShowReinspectAlert(string szTitle, string szMessage)` 본문 1문장 — 프로젝트 공용 알림 창의 Show(제목, 문구, MessageBoxImage.Warning) 호출(Button_NgAccumExport_Click 이 쓰는 것과 같은 알림 창). 문구는 VM 이 준다.

끝나면 두 파일만 `git add` 후 커밋.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p80; PB=$(cat $H/base-80-03.txt); W=WPF_Example; RR=$W/UI/ViewModel/ReviewMeasurementRow.cs; RW=$W/UI/Reviewer/ReviewerWindow.xaml.cs
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" $W/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-80-03.txt 2>&1; echo "build_exit=$? cs_errors=$(grep -c 'error CS' $H/build-80-03.txt)"
for F in $RR $RW; do ADD=$(git diff -w -U0 $PB -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "$(basename $F) deleted=$(git diff -w -U0 $PB -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') qmark=$(printf '%s\n' "$ADD" | grep -cF '?')"; done
echo "rr_deleted=$(git diff -w -U0 $PB -- $RR | grep -E '^-' | grep -vcE '^---')"
DC=$(tr -d '\r' < $RW | awk '/private void DisplayCycle/,/^        }$/')
echo "dc_overload=$(printf '%s\n' "$DC" | grep -c 'ResolveCycleImagePath(cycle, out ownerShot)') dc_collect=$(printf '%s\n' "$DC" | grep -c 'CollectShotOverlays(ownerShot)') dc_selectmany=$(printf '%s\n' "$DC" | grep -c 'SelectMany') dc_eval_kept=$(printf '%s\n' "$DC" | grep -c '_reinspectVm.EvaluateSelection(cycle, null);')"
echo "rw_deleted_outside_dc=$(git diff -w -U0 $PB -- $RW | grep -E '^-' | grep -vE '^---' | grep -vcE 'ResolveCycleImagePath|SelectMany|allOverlays|FaiResultDto|LastOverlays|SetInspectionOverlays|^-\s*[{})]|^-\s*\.|^-\s*//|^-\s*var t|^-\s*if \(t == null\)|^-\s*return t;|^-\s*$')"
echo "alert_wire=$(grep -cF '_reinspectVm.AlertPresenter = ShowReinspectAlert;' $RW) alert_stmts=$(tr -d '\r' < $RW | awk '/private void ShowReinspectAlert/,/^        }$/' | grep -c ';')"
for FN in 'public static string ResolveCycleImagePath(CycleResultDto cycle)' 'public static string ResolveRowImagePath' 'private static string FindMeasuredOriginInShot'; do Bh=$(git show $PB:$RR | tr -d '\r' | awk -v s="$FN" 'index($0,s){f=1} f{print} f&&/^        }$/{exit}' | md5sum | cut -c1-8); Nh=$(tr -d '\r' < $RR | awk -v s="$FN" 'index($0,s){f=1} f{print} f&&/^        }$/{exit}' | md5sum | cut -c1-8); echo "same[$FN]=$([ "$Bh" = "$Nh" ] && echo 1 || echo 0)"; done
for FN in 'private void MeasurementGrid_SelectionChanged' 'private void ApplyFaiOverlays' 'private void ShowAxisImage'; do Bh=$(git show $PB:$RW | tr -d '\r' | awk -v s="$FN" 'index($0,s){f=1} f{print} f&&/^        }$/{exit}' | md5sum | cut -c1-8); Nh=$(tr -d '\r' < $RW | awk -v s="$FN" 'index($0,s){f=1} f{print} f&&/^        }$/{exit}' | md5sum | cut -c1-8); echo "same[$FN]=$([ "$Bh" = "$Nh" ] && echo 1 || echo 0)"; done
    </automated>
  </verify>
  <acceptance_criteria>
    - `build_exit=0 cs_errors=0`
    - 두 파일 더한 줄 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 qmark=0`
    - `rr_deleted=0` (ReviewMeasurementRow.cs 는 추가만)
    - `dc_overload=1 dc_collect=1 dc_selectmany=0 dc_eval_kept=1`
    - `rw_deleted_outside_dc=0` (ReviewerWindow.xaml.cs 에서 지운 줄은 사이클 전체 보기 오버레이 합산 부분뿐)
    - `alert_wire=1 alert_stmts=1`
    - `same[...]=1` 6개 전부 (기존 ResolveCycleImagePath · ResolveRowImagePath · FindMeasuredOriginInShot · 행 클릭 · FAI 선 · 가로/세로 전환 무변경)
  </acceptance_criteria>
  <done>리뷰어 사이클 전체 보기는 띄운 사진의 Shot 선만 그리고, 기준점 사진이 없는 행을 불러오면 리뷰어에서 알림 1개가 뜬다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 날짜 폴더 cycle.json(로컬) → 부품 묶기 | 다른 자재 사진이 섞이면 재검사 값이 비교 불가한 숫자가 된다 |
| 리뷰어 경로 스냅샷 ↔ 저장 사진 재검사 스냅샷 | 두 기능이 겹치면 해제 뒤 임시 경로가 되살아난다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80-11 | Tampering | 기준점 사진 반쪽 짝(H 만 / V 만) | medium | mitigate | `IsDatumPhotoSetComplete` 가 필요한 역할 키 전부 + File.Exists 를 요구, 아니면 기준점 경로 하나도 안 바꿈 + 알림 (D-80-19) |
| T-80-12 | Tampering | StartFromSavedCycles 가 리뷰어 활성 중 시작 | medium | mitigate | `if (ReviewerReinspectService.IsActive)` 거절 + 문구, 반대 방향은 80-01 SavedCycleRerunActive 차단 (`guard=1`) |
| T-80-13 | Denial of Service | 두 날짜 폴더 cycle.json 스캔(UI 스레드) | low | accept | 리뷰어가 폴더를 열 때와 같은 양(하루 수백 개, 개당 약 16KB), 걸린 ms 를 로그로 남겨 UAT 에서 확인 |
| T-80-14 | Tampering | 다른 자재의 선이 메모리 FAI 에 남음 | low | mitigate | 불러올 때 이전 복사 선을 비우고, 원천 없는 Shot 은 빈 List, 사용자 [해제] 때 비움 |
</threat_model>

<verification>
1. Debug|x64 빌드 오류 0
2. 부품 묶기는 CollectAutoTicks → GroupIntoParts 재사용, ValidatePart 미사용, RepeatRunService 기존 메서드 md5 무변경·phase 대비 삭제 0
3. 기준점 완전성 판정 → 적용/알림, Z 후보·JPG·기준점 안내 문구는 VM
4. 리뷰어 DisplayCycle 은 ownerShot 선만, 기존 해석기·행 클릭 경로 md5 무변경
</verification>

<success_criteria>
- D-80-07/08/09/14/19 와 함께 처리 1 이 코드로 존재하고 빌드된다 — 실제 동작은 80-05 UAT U-2(같은 자재 사진·선), U-3(기준점 없음 알림), U-7(JPG·Z 안내), U-10(선 겹침)
</success_criteria>

<output>
완료 후 `.planning/phases/80-reviewer-ng-cycle-reinspect/80-03-SUMMARY.md` 작성: base 해시, 커밋 해시 3개, 빌드 결과, 검증 출력 원문(same[...] 포함), 새 심볼 실제 이름, 알림·안내 문구 원문, [Rerun] 리뷰어 부품 로그 문구 원문.
</output>
</content>
</invoke>
