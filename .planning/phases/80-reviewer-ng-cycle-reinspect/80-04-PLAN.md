---
phase: 80-reviewer-ng-cycle-reinspect
plan: 04
type: execute
wave: 3
depends_on: ["80-02", "80-03"]
files_modified:
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  - WPF_Example/MainWindow.xaml.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/UI/Reviewer/ReviewerReinspectViewModel.cs
  - WPF_Example/Custom/Sequence/Inspection/ReviewerReinspectService.cs
autonomous: true
requirements: [D-80-04, D-80-05, D-80-06, D-80-00, D-80-16, D-80-17]
user_setup: []

must_haves:
  truths:
    - "[D-80-04] 불러온 뒤 리뷰어 창은 닫히지 않고 최소화되며 메인 창이 앞으로 온다(리뷰어는 MainWindow 소유 창이라 메인 뒤로 갈 수 없어 최소화로 비킨다 — A-80-E1). 메뉴의 리뷰어 버튼(PopupView Reviewer)을 다시 누르면 최소화가 풀리고 앞으로 와서 다른 NG 행을 이어서 고를 수 있다"
    - "[D-80-05] 메인 트리에서 NG 난 Shot 의 그 측정 노드가 펼쳐지고 선택되어 속성창에 그 측정 파라미터가 바로 보인다(측정을 못 찾으면 FAI, 그다음 Shot 노드). 이미 그 노드가 선택돼 있어도 선택을 한 번 풀었다 다시 걸어 새 사진·선으로 다시 그린다. 선택은 트리 컨테이너 생성과 겹치지 않도록 Dispatcher Background 로 미룬다(InspectionList_SelectionChanged 재진입 경고)"
    - "[D-80-05] 불러오기·해제 때 메인 화면의 'Shot 사진 재표시 건너뛰기' 캐시(_lastDisplayedImageShot)를 잊게 해, 같은 Shot 을 다시 눌러도 옛 사진이 남지 않는다"
    - "[D-80-06] 기준점 사진이 짝 맞게 들어왔으면(D-80-19 완전) 불러오기 직후 그 NG 측정의 기준점에 대해 Test Find 를 자동으로 한 번 돌린다 — 사진 출처 선택 대화상자 없이 불러온 기준점 사진 파일로(DatumTestFindService.TryRunFromTeachingImages, 다음 수동 RUN 이 재사용하도록 HoldManualDatum). 기준점 사진이 없으면(D-80-09) 자동 Test Find 를 하지 않는다. 서비스는 시퀀스를 시작하지 않는다 — RUN 은 사용자가 직접 누른다. 실패하면 상태 줄에 '(기준점 찾기 실패 — 기준점 노드에서 Test Find 로 확인)' 이 붙고 Trace 로그에 원인이 남는다"
    - "[D-80-05, D-80-06] 측정·FAI 노드를 고른 채 RUN(btn_start)을 누르면 그 측정이 속한 Shot 을 실행한다(지금까지는 'Select a Sequence or Shot/Action node' 오류). Sequence·Shot·Datum 노드의 RUN 동작은 그대로다"
    - "[D-80-00] 리뷰어 사진 사용 중(상태 줄 표시 중)에는 RUN 때 OfflineInspectMode 확인창을 띄우지 않는다 — 리뷰어가 켠 오프라인 모드이고 상태 줄이 이미 보여 준다. 리뷰어 사진을 쓰지 않을 때의 확인창은 그대로다"
    - "[D-80-16, D-80-17] 더한 줄 가독성 grep 0. 트리 선택·RUN 노드 해석은 InspectionListView 의 이번에 손대는 지점에만, MainView.xaml.cs 에는 캐시 잊기 1문장 메서드와 이벤트 배선 1줄만, 상태 문구는 VM"
  artifacts:
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "Shot/FAI/측정 노드 프로그램 선택 + 측정 노드 RUN 해석"
      contains: "public void SelectShotAndMeasurement("
    - path: "WPF_Example/Custom/Sequence/Inspection/ReviewerReinspectService.cs"
      provides: "자동 Test Find"
      contains: "DatumTestFindService.TryRunFromTeachingImages("
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "Shot 사진 캐시 잊기"
      contains: "public void ForgetDisplayedShotImage()"
  key_links:
    - from: "MainWindow.OnReviewerPhotosLoaded"
      to: "InspectionListView.SelectShotAndMeasurement"
      via: "VM.MainNavigator 훅 (LoadForRow 결과의 LiveShot/LiveFai/LiveMeasurement)"
      pattern: "inspectionList\\.SelectShotAndMeasurement\\(result\\.LiveShot, result\\.LiveFai, result\\.LiveMeasurement\\)"
    - from: "ReviewerReinspectService.LoadForRow"
      to: "DatumTestFindService.TryRunFromTeachingImages(seq, datum, true, ...)"
      via: "RunAutoTestFind — 기준점 사진 완전할 때만"
      pattern: "RunAutoTestFind\\("
    - from: "InspectionListView.Btn_start_Click"
      to: "ResolveRunNodeForSelection → ResolveRunnableAction"
      via: "측정/FAI 노드 → 부모 Shot(Action) 노드"
      pattern: "node = ResolveRunNodeForSelection\\(node\\);"
---

<objective>
리뷰어에서 불러온 뒤 사용자가 **파라미터만 고치고 RUN 한 번** 누르면 되게 메인 화면을 이어받는다: 리뷰어는 비키고(D-80-04), NG Shot·측정 노드가 선택되어 파라미터가 보이며(D-80-05), 기준점 Test Find 가 대화상자 없이 자동으로 한 번 돈다(D-80-06). 측정 노드를 고른 채 RUN 을 누를 수 있게 하고, 리뷰어 사진 사용 중에는 오프라인 확인창을 띄우지 않는다(D-80-00).

Purpose: CONTEXT 흐름도 "파라미터 수정 → RUN(직접) → 재검사 결과" 를 실제로 한 번에 되게 하는 마지막 연결. 계획 단계에서 확인한 사실: RUN(Btn_start_Click)은 지금 FAI·측정 노드에서 오류를 내고(InspectionListView.xaml.cs:386-392), OfflineInspectMode 가 켜져 있으면 RUN 마다 확인창을 띄운다(:425-434, 2026-09-18 Trace.log 에 RUN 마다 기록). 이 두 가지를 그대로 두면 D-80-05(측정 선택) 뒤 RUN 이 막히거나 리뷰어가 켠 오프라인 모드 때문에 확인창이 매번 뜬다 — 계획 단계 재량(P-80-5)으로 고친다(A-80-E2 로 UAT 판단).

Output: 기존 파일 5개에 삽입(InspectionListView 의 오프라인 확인 if 줄 1개 교체).

⚠ spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/80-reviewer-ng-cycle-reinspect/80-CONTEXT.md
@.planning/phases/80-reviewer-ng-cycle-reinspect/80-RESEARCH.md
@.planning/phases/80-reviewer-ng-cycle-reinspect/80-02-SUMMARY.md
@.planning/phases/80-reviewer-ng-cycle-reinspect/80-03-SUMMARY.md

<interfaces>
80-01~03 이 만든 것(실제 이름은 SUMMARY 로 확인): `ReviewerReinspectService.LoadForRow`(잠금 안 순서: 스냅샷 → ApplyPartPaths → OfflineInspectMode → `seq.ClearDatumTransforms()` → 버퍼 → 리뷰어 선), 지역 값 `szNgDatumRef`, `bNeedsDatum`, `bDatumComplete`, `ReviewerReinspectState`(+Clone), `ReviewerReinspectLoadResult { LiveShot, LiveFai, LiveMeasurement, IsDatumPhotoMissing }`, VM `RefreshFromService`, `BuildStatusText`, `MainNavigator`, MainWindow `OnReviewerPhotosLoaded(result)`(본문 `Activate();`), MainView 생성자의 `panel_reviewerReinspect.DataContext = ...` 줄. 80-02: `DatumTestFindService.TryRunFromTeachingImages(InspectionSequence seq, DatumConfig datum, bool bHoldForManualRun, out string szError)`.

WPF_Example/UI/ControlItem/InspectionListView.xaml.cs (K&R, namespace ReringProject.UI, using System.Windows.Threading · ReringProject.Sequence 있음)
- :26 `private InspectionListViewModel ViewModel;`(`.RootModel` = 루트 NodeViewModel), :28 `SelectedParam`
- :382-446 `Btn_start_Click` — :384 `if (!(treeListBox_sequence.SelectedItem is NodeViewModel node)) return;`, :387-392 Sequence/Action 외 노드 오류, :413 `ResolveRunnableAction(node, seq, out EAction actID)`, **:425 `if (SystemHandler.Handle.Setting.OfflineInspectMode) {`** ~ :434 확인창
- :448-499 `ResolveRunnableAction`(바꾸지 않음) — Action 노드 + ShotConfig Param 이면 살아있는 Actions[] 에서 참조 비교
- :840-853 `SetSelectionChange(string)` — `item.IsSelected = true; treeListBox_sequence.ScrollIntoView(item);` 선례
- :855-887 `InspectionList_SelectionChanged` — :880-885 재진입 크래시 경고, :886 Dispatcher.BeginInvoke(Background) 로 무거운 처리 지연, :958-997 측정 노드 분기(RenderInspectionResultForNode → Shot 버퍼 사진 + FAI LastOverlays, ShowResultDatumOverlays)
WPF_Example/UI/ControlItem/NodeViewModel.cs — `Parent`, `Children`(지연 로딩), `Param`(Node.ParamData — ShotConfig/FAIConfig/MeasurementBase 참조 그대로), `NodeType`(ENodeType: Recipe, Sequence, Action, SubSequence, FAI, Datum, Measurement), `IsExpanded`, `IsSelected`, `ExpandParents()` :243
WPF_Example/MainWindow.xaml.cs — :71 `mReviewerWindow`, :401-409 PopupView Reviewer(`IsLoaded` 면 `Show()` 후 return), 80-01 의 `OnReviewerPhotosLoaded`
WPF_Example/UI/ContentItem/MainView.xaml.cs — :200 `private ShotConfig _lastDisplayedImageShot;`, :203-244 DisplayShotImage(같은 Shot 이면 재로드 건너뜀)
</interfaces>
</context>

## Artifacts this phase produces (이 plan 분)

phase 전체 원장은 `80-01-PLAN.md`. 이 plan 이 만드는 것:
- InspectionListView.xaml.cs: `public void SelectShotAndMeasurement(ShotConfig liveShot, FAIConfig liveFai, MeasurementBase liveMeasurement)`, private `FindNodeByParam(NodeViewModel node, object target)`, private `SelectNodeDeferred(NodeViewModel node)`, private static `ResolveRunNodeForSelection(NodeViewModel node)`, 지역 bool `bAskOfflineConfirm`
- MainWindow.xaml.cs: OnReviewerPhotosLoaded 확장, PopupView Reviewer 복원 2줄
- MainView.xaml.cs: `public void ForgetDisplayedShotImage()` + Released 구독 1줄
- ReviewerReinspectViewModel.cs: `public event Action Released;`, const `HINT_TESTFIND_FAILED`
- ReviewerReinspectService.cs: `public enum EReviewerTestFindResult { NotRun, Succeeded, Failed }`, `ReviewerReinspectState.TestFindResult`, private `RunAutoTestFind(InspectionSequence, string, bool)`
- 저장소 밖: `/c/Users/admin/AppData/Local/Temp/p80/base-80-04.txt`, `build-80-04.txt`

<tasks>

<task type="auto">
  <name>Task 1: 메인 화면 이어받기 — 리뷰어 최소화·복원(D-80-04), NG Shot·측정 노드 자동 선택(D-80-05), 사진 캐시 잊기</name>
  <precondition>80-02-SUMMARY.md 와 80-03-SUMMARY.md 가 있고 Debug 앱이 실행 중이 아니다</precondition>
  <reversibility rating="reversible">화면 선택·창 상태 배선뿐</reversibility>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs, WPF_Example/MainWindow.xaml.cs, WPF_Example/UI/ContentItem/MainView.xaml.cs, WPF_Example/UI/Reviewer/ReviewerReinspectViewModel.cs</files>
  <read_first>
    - CLAUDE.md (🔒 가독성 규칙, "UI 는 MVVM" — 이번에 손대는 지점만)
    - .planning/phases/80-reviewer-ng-cycle-reinspect/80-RESEARCH.md "Pattern 5", "Pitfall 5"
    - .planning/phases/80-reviewer-ng-cycle-reinspect/80-PATTERNS.md "InspectionListView.xaml.cs" 절
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs:20-30, :836-900, :950-1000
    - WPF_Example/UI/ControlItem/NodeViewModel.cs (전체)
    - WPF_Example/MainWindow.xaml.cs:60-110, :320-420 와 80-01 의 OnReviewerPhotosLoaded
    - WPF_Example/UI/ContentItem/MainView.xaml.cs:83-110, :196-245
    - WPF_Example/UI/Reviewer/ReviewerReinspectViewModel.cs (80-01·03 결과)
  </read_first>
  <action>
(0) 편집 전: `git rev-parse HEAD > /c/Users/admin/AppData/Local/Temp/p80/base-80-04.txt`.

(1) InspectionListView.xaml.cs (K&R) — SetSelectionChange 바로 다음에 3개 메서드(기존 줄 수정 0):
 - `public void SelectShotAndMeasurement(ShotConfig liveShot, FAIConfig liveFai, MeasurementBase liveMeasurement)` — 요약 주석(D-80-05: 리뷰어에서 불러온 NG 측정 노드를 선택해 속성창에 파라미터를 보인다). `NodeViewModel root = ViewModel.RootModel;` null 이면 return. `NodeViewModel found = FindNodeByParam(root, liveMeasurement);` null 이면 liveFai 로, 그래도 null 이면 liveShot 으로 다시 찾는다(if 연속, 중첩 없이). 끝까지 null 이면 Trace 로그 "[ReviewerLoad] 트리에서 노드를 찾지 못함" 후 return. `found.ExpandParents();` 다음 `Dispatcher.BeginInvoke(DispatcherPriority.Background, new Action(() => SelectNodeDeferred(found)));` — 트리 컨테이너 생성 중 동기 선택 크래시(:880-885 경고) 회피.
 - `private void SelectNodeDeferred(NodeViewModel node)` — `NodeViewModel prev = treeListBox_sequence.SelectedItem as NodeViewModel;` prev 가 null 이 아니면 `prev.IsSelected = false;`(같은 노드여도 풀었다 다시 걸어 SelectionChanged 가 새 사진·선으로 다시 그리게), `node.IsSelected = true;`, `treeListBox_sequence.ScrollIntoView(node);`(SetSelectionChange 선례).
 - `private NodeViewModel FindNodeByParam(NodeViewModel node, object target)` — node 또는 target 이 null 이면 null, `ReferenceEquals(node.Param, target)` 면 node, 아니면 `node.Children` 을 돌며 재귀 결과가 null 이 아니면 반환, 끝에 null.

(2) MainWindow.xaml.cs (K&R):
 - OnReviewerPhotosLoaded — 기존 `Activate();` 줄은 두고, 그 앞에 `if (mReviewerWindow != null) { mReviewerWindow.WindowState = WindowState.Minimized; }`(D-80-04: 리뷰어는 닫지 않고 최소화 — 소유 창이라 메인 뒤로 못 감), 그 뒤에 `mainView.ForgetDisplayedShotImage();` 와 `inspectionList.SelectShotAndMeasurement(result.LiveShot, result.LiveFai, result.LiveMeasurement);`.
 - PopupView 의 `case EPageType.Reviewer:` 안 `if (mReviewerWindow != null && mReviewerWindow.IsLoaded) {` 블록: `mReviewerWindow.Show();` 앞에 `if (mReviewerWindow.WindowState == WindowState.Minimized) { mReviewerWindow.WindowState = WindowState.Normal; }`, 뒤에 `mReviewerWindow.Activate();`(다른 NG 행을 이어서 고르기 — D-80-04). 새 창 만들기 경로는 그대로.

(3) MainView.xaml.cs (K&R) — `public void ForgetDisplayedShotImage()` 본문 1문장 `_lastDisplayedImageShot = null;`(요약 주석: 리뷰어 사진을 불러오거나 해제한 뒤 같은 Shot 을 다시 눌러도 새 사진을 다시 읽게 한다), 생성자의 80-01 DataContext 줄 다음에 `ReviewerReinspectViewModel.Instance.Released += ForgetDisplayedShotImage;` 1줄.

(4) ReviewerReinspectViewModel.cs — `public event Action Released;`. RefreshFromService 에서 새 상태를 넣기 전 `bool bWasActive = IsActive;`, 넣은 뒤 `bool bJustReleased = bWasActive && !state.IsActive;` 면 Released 핸들러를 지역 변수로 복사해 null 이 아니면 호출(UI 스레드 — RefreshFromService 는 Dispatcher 위에서 돈다).

끝나면 네 파일만 `git add` 후 커밋.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p80; PB=$(cat $H/base-80-04.txt); W=WPF_Example; IL=$W/UI/ControlItem/InspectionListView.xaml.cs; MW=$W/MainWindow.xaml.cs; MV=$W/UI/ContentItem/MainView.xaml.cs; VM=$W/UI/Reviewer/ReviewerReinspectViewModel.cs
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" $W/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-80-04.txt 2>&1; echo "build_exit=$? cs_errors=$(grep -c 'error CS' $H/build-80-04.txt)"
for F in $IL $MW $MV $VM; do ADD=$(git diff -w -U0 $PB -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "$(basename $F) deleted=$(git diff -w -U0 $PB -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') qmark=$(printf '%s\n' "$ADD" | grep -cF '?') logic3=$(printf '%s\n' "$ADD" | grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)')"; done
echo "select_def=$(grep -c 'public void SelectShotAndMeasurement(ShotConfig liveShot, FAIConfig liveFai, MeasurementBase liveMeasurement)' $IL) deferred=$(tr -d '\r' < $IL | awk '/public void SelectShotAndMeasurement/,/^        }$/' | grep -c 'DispatcherPriority.Background') expand=$(tr -d '\r' < $IL | awk '/public void SelectShotAndMeasurement/,/^        }$/' | grep -c 'ExpandParents()') reselect=$(tr -d '\r' < $IL | awk '/private void SelectNodeDeferred/,/^        }$/' | grep -c 'IsSelected = false')"
echo "nav_select=$(grep -cF 'inspectionList.SelectShotAndMeasurement(result.LiveShot, result.LiveFai, result.LiveMeasurement);' $MW) nav_min=$(tr -d '\r' < $MW | awk '/private void OnReviewerPhotosLoaded/,/^        }$/' | grep -c 'WindowState.Minimized') nav_forget=$(grep -cF 'mainView.ForgetDisplayedShotImage();' $MW) popup_restore=$(tr -d '\r' < $MW | awk '/case EPageType.Reviewer:/,/break;/' | grep -c 'WindowState.Normal') popup_activate=$(tr -d '\r' < $MW | awk '/case EPageType.Reviewer:/,/break;/' | grep -c 'mReviewerWindow.Activate();')"
echo "forget_def=$(grep -c 'public void ForgetDisplayedShotImage()' $MV) forget_stmts=$(tr -d '\r' < $MV | awk '/public void ForgetDisplayedShotImage/,/^        }$/' | grep -c ';') released_sub=$(grep -cF 'ReviewerReinspectViewModel.Instance.Released += ForgetDisplayedShotImage;' $MV) released_evt=$(grep -c 'public event Action Released;' $VM)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `build_exit=0 cs_errors=0`
    - 네 파일 `deleted=0` 과 더한 줄 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 qmark=0 logic3=0`
    - `select_def=1 deferred=1 expand=1 reselect=1`
    - `nav_select=1 nav_min=1 nav_forget=1 popup_restore=1 popup_activate=1`
    - `forget_def=1 forget_stmts=1 released_sub=1 released_evt=1`
  </acceptance_criteria>
  <done>불러오면 리뷰어가 최소화되고 메인이 앞으로 오며 NG 측정 노드가 선택되어 새 사진·선과 파라미터가 보이고, 리뷰어 메뉴로 다시 열면 복원된다.</done>
</task>

<task type="auto">
  <name>Task 2: 자동 Test Find (D-80-06) — 기준점 사진이 짝 맞게 들어왔을 때만, 대화상자 없이, RUN 은 사용자 몫</name>
  <reversibility rating="reversible">서비스 내부 호출 추가, 기준점 캐시는 해제 때 ClearDatumTransforms 로 비워진다(80-01)</reversibility>
  <files>WPF_Example/Custom/Sequence/Inspection/ReviewerReinspectService.cs, WPF_Example/UI/Reviewer/ReviewerReinspectViewModel.cs</files>
  <read_first>
    - CLAUDE.md (🔒 가독성 규칙)
    - .planning/phases/80-reviewer-ng-cycle-reinspect/80-CONTEXT.md (D-80-06, D-80-09)
    - .planning/phases/80-reviewer-ng-cycle-reinspect/80-RESEARCH.md "Pattern 3"
    - WPF_Example/Custom/Sequence/Inspection/ReviewerReinspectService.cs (80-01·03 결과 전체)
    - WPF_Example/Custom/Sequence/Inspection/DatumTestFindService.cs (80-02 결과)
    - WPF_Example/UI/Reviewer/ReviewerReinspectViewModel.cs
  </read_first>
  <action>
(1) ReviewerReinspectService.cs:
 - `public enum EReviewerTestFindResult { NotRun, Succeeded, Failed }`, `ReviewerReinspectState` 에 `EReviewerTestFindResult TestFindResult`(기본 NotRun, Clone 복사).
 - private `RunAutoTestFind(InspectionSequence seq, string szDatumRef, bool bEligible)` → EReviewerTestFindResult: bEligible false 면 NotRun(D-80-09 — 기준점 사진이 없으면 자동 Test Find 안 함). seq.DatumConfigs 에서 DatumName == szDatumRef 인 기준점을 찾고 없으면 Trace 로그 후 Failed. `string szError; bool bOk = DatumTestFindService.TryRunFromTeachingImages(seq, datum, true, out szError);`(세 번째 인자 true = 다음 수동 RUN 이 이 기준점을 재사용 — 기존 Test Find 버튼과 같은 의미). Trace 로그 `LOG_TAG + "자동 Test Find — " + seq.Name + " · " + szDatumRef + " 성공"` 또는 `" 실패: " + szError`. 결과 반환.
 - LoadForRow 잠금 안, `seq.ClearDatumTransforms()` 와 경로·버퍼·선 적용이 끝난 뒤(기준점 사진이 들어간 다음이어야 한다) `bool bRunTestFind = bNeedsDatum && bDatumComplete;` 로 `s_state.TestFindResult = RunAutoTestFind(seq, szNgDatumRef, bRunTestFind);`. 불러옴 로그에 Test Find 결과를 붙인다.
 - 이 서비스는 시퀀스를 시작하지 않는다(D-80-06: RUN 은 사용자가 직접) — 시퀀스 시작 메서드를 부르는 줄을 넣지 않는다.

(2) ReviewerReinspectViewModel.cs — const `HINT_TESTFIND_FAILED = "  (기준점 찾기 실패 — 기준점 노드에서 Test Find 로 확인)"`, BuildStatusText 에서 `state.TestFindResult == EReviewerTestFindResult.Failed` 이면 붙인다(다른 안내 뒤).

끝나면 두 파일만 `git add` 후 커밋.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p80; W=WPF_Example; SVC=$W/Custom/Sequence/Inspection/ReviewerReinspectService.cs; VM=$W/UI/Reviewer/ReviewerReinspectViewModel.cs
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" $W/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-80-04.txt 2>&1; echo "build_exit=$? cs_errors=$(grep -c 'error CS' $H/build-80-04.txt)"
for F in $SVC $VM; do echo "$(basename $F) ternary=$(grep -cE '\?[^?]*:' $F) coalesce=$(grep -cF '??' $F) nullcond=$(grep -cF '?.' $F) switchexpr=$(grep -cE 'switch.*=>' $F) datesig=$(grep -cF 'hbk' $F) qmark=$(grep -cF '?' $F) logic3=$(grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)' $F)"; done
echo "helper_call=$(grep -c 'DatumTestFindService.TryRunFromTeachingImages(seq, datum, true, out szError)' $SVC) run_call=$(grep -c 'RunAutoTestFind(seq, szNgDatumRef, bRunTestFind)' $SVC) eligible=$(grep -c 'bool bRunTestFind = bNeedsDatum && bDatumComplete;' $SVC) no_seq_start=$(grep -cE '\.Start(All)?\(' $SVC) enum_def=$(grep -c 'public enum EReviewerTestFindResult' $SVC)"
L=$(tr -d '\r' < $SVC | awk '/public static ReviewerReinspectLoadResult LoadForRow/,/^        }$/'); echo "order_ok=$(printf '%s\n' "$L" | awk '/ClearDatumTransforms\(\)/{a=NR} /RunAutoTestFind\(/{b=NR} END{ if (a>0 && b>a) print 1; else print 0 }')"
echo "hint=$(grep -c 'HINT_TESTFIND_FAILED' $VM)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `build_exit=0 cs_errors=0`
    - 두 파일 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 qmark=0 logic3=0`
    - `helper_call=1 run_call=1 eligible=1 no_seq_start=0 enum_def=1`
    - `order_ok=1` (기준점 캐시를 비운 뒤에 자동 Test Find)
    - `hint>=2`
  </acceptance_criteria>
  <done>기준점 사진이 짝 맞게 들어오면 불러오기 직후 그 기준점 Test Find 가 대화상자 없이 한 번 돌아 다음 수동 RUN 이 그 결과(기준점 변환·국부 기준선)를 쓰고, 실패는 상태 줄 안내와 로그로 남는다.</done>
</task>

<task type="auto">
  <name>Task 3: RUN 한 번 — 측정·FAI 노드에서도 그 Shot 실행, 리뷰어 사진 사용 중에는 오프라인 확인창 생략</name>
  <reversibility rating="reversible">RUN 핸들러의 노드 해석 1줄 추가와 확인창 조건 1줄 교체 — 이전에 오류였던 경우만 동작이 생긴다</reversibility>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <read_first>
    - CLAUDE.md (🔒 가독성 규칙 — 조건 3개 이상은 bool 선추출)
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs:380-500
    - WPF_Example/UI/ControlItem/NodeViewModel.cs:11-25, :100-115
    - WPF_Example/UI/ControlItem/Node.cs:10-18 (ENodeType)
  </read_first>
  <action>
InspectionListView.xaml.cs (K&R) Btn_start_Click:
 - `if (!(treeListBox_sequence.SelectedItem is NodeViewModel node)) return;` 줄 다음에 `node = ResolveRunNodeForSelection(node);` 1줄과 주석 "// Phase 80: 측정·FAI 노드를 고른 채 RUN 을 누르면 그 측정이 속한 Shot 을 실행한다 — 리뷰어에서 불러와 파라미터를 고친 뒤 RUN 한 번(D-80-05/06)". 뒤의 노드 종류 오류·ResolveRunnableAction 흐름은 그대로 — 측정·FAI 노드는 이제 부모 Shot(Action) 노드로 바뀌어 기존 Shot 경로를 탄다.
 - 오프라인 확인 `if (SystemHandler.Handle.Setting.OfflineInspectMode) {` 한 줄을 `bool bAskOfflineConfirm = SystemHandler.Handle.Setting.OfflineInspectMode && !ReviewerReinspectService.IsActive;` + `if (bAskOfflineConfirm) {` 로 바꾸고 주석 "// Phase 80 D-80-00: 리뷰어 사진 사용 중에는 리뷰어가 켠 오프라인 모드이고 상태 줄이 보여 주므로 확인창을 띄우지 않는다". 확인창 본문은 그대로.
 - 새 메서드 `private static NodeViewModel ResolveRunNodeForSelection(NodeViewModel node)`(ResolveRunnableAction 바로 위): node 가 null 이면 null. `bool bIsShotChild = node.NodeType == ENodeType.FAI || node.NodeType == ENodeType.Measurement;` 아니면 node 그대로 반환(Sequence·Action·Datum 동작 불변). 맞으면 `NodeViewModel cur = node.Parent;` while cur 가 null 이 아니면 `cur.NodeType == ENodeType.Action` 인 첫 조상을 반환, 끝까지 없으면 node 반환.
 - ResolveRunnableAction 과 나머지 Btn_start_Click 줄은 바꾸지 않는다.

끝나면 이 파일만 `git add` 후 커밋.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p80; PB=$(cat $H/base-80-04.txt); W=WPF_Example; IL=$W/UI/ControlItem/InspectionListView.xaml.cs
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" $W/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-80-04.txt 2>&1; echo "build_exit=$? cs_errors=$(grep -c 'error CS' $H/build-80-04.txt)"
ADD=$(git diff -w -U0 $PB -- "$IL" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "il deleted=$(git diff -w -U0 $PB -- "$IL" | grep -E '^-' | grep -vE '^---' | wc -l) deleted_is_offline_if=$(git diff -w -U0 $PB -- "$IL" | grep -E '^-' | grep -vE '^---' | grep -c 'if (SystemHandler.Handle.Setting.OfflineInspectMode)') ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') qmark=$(printf '%s\n' "$ADD" | grep -cF '?') logic3=$(printf '%s\n' "$ADD" | grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)')"
S=$(tr -d '\r' < $IL | awk '/private void Btn_start_Click/,/^        }$/')
echo "resolve_line=$(printf '%s\n' "$S" | grep -c 'node = ResolveRunNodeForSelection(node);') ask_bool=$(printf '%s\n' "$S" | grep -c 'bool bAskOfflineConfirm = SystemHandler.Handle.Setting.OfflineInspectMode && !ReviewerReinspectService.IsActive;') ask_if=$(printf '%s\n' "$S" | grep -c 'if (bAskOfflineConfirm) {') resolve_before_check=$(printf '%s\n' "$S" | awk '/ResolveRunNodeForSelection\(node\)/{a=NR} /node.NodeType != ENodeType.Sequence/{b=NR} END{ if (a>0 && b>a) print 1; else print 0 }')"
echo "helper=$(grep -c 'private static NodeViewModel ResolveRunNodeForSelection(NodeViewModel node)' $IL) helper_action=$(tr -d '\r' < $IL | awk '/private static NodeViewModel ResolveRunNodeForSelection/,/^        }$/' | grep -c 'ENodeType.Action')"
B=$(git show $PB:$IL | tr -d '\r' | awk 'index($0,"private bool ResolveRunnableAction"){f=1} f{print} f&&/^        }$/{exit}' | md5sum | cut -c1-8); N=$(tr -d '\r' < $IL | awk 'index($0,"private bool ResolveRunnableAction"){f=1} f{print} f&&/^        }$/{exit}' | md5sum | cut -c1-8); echo "same_resolve_runnable=$([ "$B" = "$N" ] && echo 1 || echo 0)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `build_exit=0 cs_errors=0`
    - `il deleted=1 deleted_is_offline_if=1` (지운 줄은 오프라인 확인 if 1줄), 더한 줄 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 qmark=0 logic3=0`
    - `resolve_line=1 ask_bool=1 ask_if=1 resolve_before_check=1`
    - `helper=1 helper_action>=1 same_resolve_runnable=1`
  </acceptance_criteria>
  <done>리뷰어에서 불러와 측정 노드가 선택된 채로 파라미터를 고치고 RUN 을 누르면 확인창 없이 그 Shot 이 재검사되고, 리뷰어 사진을 쓰지 않을 때의 RUN·확인창은 이전과 같다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 자동 Test Find → 시퀀스 기준점 캐시(HoldManualDatum) | 리뷰어 사진으로 잡은 기준점이 해제 뒤 수동·PLC 검사로 새면 다른 자재 기준으로 잰다 |
| RUN 확인창 생략 | 실물 라인에서 저장 사진으로 조용히 검사되는 것을 막던 확인이 사라진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80-15 | Tampering | 리뷰어 사진 기준점 캐시 잔류 | medium | mitigate | 불러오기 때 ClearDatumTransforms 뒤 Test Find (`order_ok=1`), 해제 4경로가 ClearDatumTransforms(보유 표시 포함)를 부른다(80-01) |
| T-80-16 | Spoofing | 오프라인 확인창 생략으로 저장 사진 검사 오인 | low | mitigate | ReviewerReinspectService.IsActive 일 때만 생략 — 그동안 노란 상태 줄이 보이고, PLC $TEST 는 자동 해제(80-01) |
| T-80-17 | Denial of Service | 트리 컨테이너 생성 중 동기 재선택 크래시 | medium | mitigate | 선택은 Dispatcher Background 로 지연 (`deferred=1`) — 기존 :880-885 경고와 같은 회피 |
| T-80-18 | Tampering | 서비스가 시퀀스를 스스로 시작 | low | mitigate | 서비스에 시퀀스 시작 호출 0 (`no_seq_start=0`) — D-80-06 |
</threat_model>

<verification>
1. Debug|x64 빌드 오류 0
2. 불러오기 → 리뷰어 최소화 → 메인 Activate → 캐시 잊기 → 측정 노드 선택(지연) 배선, 리뷰어 메뉴 복원
3. 자동 Test Find 는 기준점 사진 완전할 때만, ClearDatumTransforms 뒤, 시퀀스 시작 없음
4. RUN 노드 해석·확인창 조건만 바뀌고 ResolveRunnableAction md5 동일
5. 더한 줄 가독성 grep 0
</verification>

<success_criteria>
- D-80-04/05/06 이 코드로 존재하고 빌드된다 — 실제 동작은 80-05 UAT U-2(이어받기·자동 Test Find), U-4(RUN 한 번)
</success_criteria>

<output>
완료 후 `.planning/phases/80-reviewer-ng-cycle-reinspect/80-04-SUMMARY.md` 작성: base 해시, 커밋 해시 3개, 빌드 결과, 검증 출력 원문, 새 심볼 실제 이름, 자동 Test Find 로그 문구·상태 줄 안내 원문, RUN 동작 변화(오류였던 FAI·측정 노드 → Shot 실행) 한 줄 요약.
</output>
</content>
</invoke>
