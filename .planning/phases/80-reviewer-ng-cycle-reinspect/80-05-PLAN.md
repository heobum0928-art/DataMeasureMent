---
phase: 80-reviewer-ng-cycle-reinspect
plan: 05
type: execute
wave: 4
depends_on: ["80-01", "80-02", "80-03", "80-04"]
files_modified:
  - WPF_Example/VersionDefine.cs
  - .planning/phases/80-reviewer-ng-cycle-reinspect/80-HUMAN-UAT.md
autonomous: false
requirements: [D-80-00, D-80-01, D-80-02, D-80-03, D-80-04, D-80-05, D-80-06, D-80-07, D-80-08, D-80-09, D-80-10, D-80-11, D-80-12, D-80-13, D-80-14, D-80-15, D-80-16, D-80-17, D-80-18, D-80-19]
user_setup: []

must_haves:
  truths:
    - "[D-80-16, D-80-17, D-80-18] 누적 감사: phase 시작 커밋 대비 새 .cs 4개 전체와 기존 파일에 더한 모든 줄에서 가독성 grep(삼항·null 병합·null 조건·switch 식·날짜 주석·물음표·3중 &&/||) 0, 새 .cs 4개 csproj 등록, code-behind 새 핸들러 본문 1문장, Debug|x64 빌드 오류 0"
    - "[D-80-00] 누적 감사: 새 XAML 파일 0 · 새 Window 클래스 0 · 설정(SystemSetting) 파일 변경 0 · XAML 에 새로 더한 Button 3개(리뷰어 버튼 1 + 상태 줄 [해제] 1 + 기준 ROI 시험 찾기 1) · C# 에 새로 더한 대화상자 호출 1개(D-80-09 기준점 없음 알림)"
    - "[회귀 0] 누적 감사: 기존 Test Find 버튼·사진 출처 선택 메서드, 기준점 검출(ProcessOneDatum)·국부 기준선 주입(InjectLocalRef)·런 시작 초기화·국부 기준선 사전 계산, 반복검사(BuildPlan · ValidatePart · ApplySavedCyclePart · 스냅샷/복원), RUN 액션 해석(ResolveRunnableAction), 리뷰어 행 사진 해석(ResolveCycleImagePath(cycle) · ResolveRowImagePath) 메서드 본문이 phase 시작과 md5 로 같다. RepeatRunService.cs 는 phase 시작 대비 지운 줄 0"
    - "버전은 1.7.51.0(VersionDefine.VERSION·BUILD_DATE·[Version] 항목)이고 변경 설명은 쉬운 한국어로 이 phase 의 기능 7가지(리뷰어 버튼·같은 자재 사진·기준점 없음 알림·자동 Test Find·레시피 보호/자동 해제·Local Ref 재계산·기준 ROI 시험 찾기)와 리뷰어 선 겹침 수정을 적는다"
    - "80-HUMAN-UAT.md 가 U-1~U-11 을 목적·준비·단계·기대 결과(실제 화면 문구·로그 문구)·합격 기준·결과 칸으로 담고, A-80-E1~E5 판단 칸과 '장비 PC Release 빌드로만 실행(Debug 는 라이선스 키 없음), 배포는 앱 종료·백업·사용자 승인 뒤' 를 명시한다"
    - "사용자 승인 없이 이 plan 을 완료로 표시하지 않는다(체크포인트 blocking). 실행자는 Release 빌드를 하지 않고(OutputPath D:\\Data — 운영 exe 덮어씀), D:/Data 아래 레시피·결과·사진을 쓰거나 고치지 않는다"
  artifacts:
    - path: "WPF_Example/VersionDefine.cs"
      provides: "버전 1.7.51.0 + 변경 설명"
      contains: "public const string VERSION = \"1.7.51.0\";"
    - path: ".planning/phases/80-reviewer-ng-cycle-reinspect/80-HUMAN-UAT.md"
      provides: "Phase 80 장비 PC UAT 절차서"
      contains: "U-11"
  key_links:
    - from: "80-HUMAN-UAT.md U-2"
      to: "80-01 tracer 경로 + 80-03 같은 자재 + 80-04 이어받기"
      via: "Trace 로그 '[ReviewerLoad] 불러옴 — ' · '[Rerun] 리뷰어 부품(같은 자재)' · '[ReviewerLoad] 자동 Test Find — ' 와 상태 줄 문구"
      pattern: "\\[ReviewerLoad\\] 불러옴"
    - from: "80-HUMAN-UAT.md U-8"
      to: "80-02 Task 1 stale 재계산 + 사진 출처 확인"
      via: "Algorithm 로그 '[LocalRef] 기준 ROI·에지 설정이 바뀌어 기준점 가로 사진에서 국부 기준선을 다시 구함 — '(두 장짜리·시험 찾기) / '… 사진 파일이 아닌 사진으로 찾아서 … 다시 구하지 않음'(1장 화면 사진 Test Find) 와 결과 '기준' 열"
      pattern: "다시 구함"
---

<objective>
Phase 80 을 마무리한다: 버전을 1.7.51.0 으로 올리고, phase 시작 대비 누적 감사(가독성·MVVM·UI 3개 제한·회귀 0)를 자동으로 증명한 뒤, 장비 PC Release 빌드에서 사용자가 확인할 UAT 절차서를 쓰고 사용자 승인을 받는다.

Purpose: 사무실 PC 의 Debug 빌드는 라이선스 키가 없어 실행할 수 없으므로(메모리: UAT 는 Release 빌드로) 실제 화면·동작 확인은 사용자 몫이다. 실행자는 앱을 실행하지 않는다.

Output: VersionDefine.cs(버전), 80-HUMAN-UAT.md(커밋), 사용자 승인 기록.

⚠ spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/80-reviewer-ng-cycle-reinspect/80-CONTEXT.md
@.planning/phases/80-reviewer-ng-cycle-reinspect/80-VALIDATION.md
@.planning/phases/80-reviewer-ng-cycle-reinspect/80-01-SUMMARY.md
@.planning/phases/80-reviewer-ng-cycle-reinspect/80-02-SUMMARY.md
@.planning/phases/80-reviewer-ng-cycle-reinspect/80-03-SUMMARY.md
@.planning/phases/80-reviewer-ng-cycle-reinspect/80-04-SUMMARY.md

<interfaces>
WPF_Example/VersionDefine.cs — 마지막 `[Version(Number = "1.7.50.0", Date = "2026-09-18", Change = "..." )]` 항목(:677-687) 다음에 새 항목, 클래스 안 `public const string VERSION = "1.7.50.0";` · `public const string BUILD_DATE = "2026-09-18";`(:691-692). 변경 설명 문체는 1.7.49.0 / 1.7.50.0 항목과 같다(사용자 관점의 쉬운 문장, 문자열 + 연결).
80-01~04 SUMMARY — 실제 로그 문구·상태 줄 문구·버튼 문구·알림 문구·새 심볼 이름의 출처.
참고 문서 형식: .planning/phases/79-side-local-strip-datum/79-HUMAN-UAT.md, .planning/phases/77-side-z-focus-select/77-HUMAN-UAT.md
PLC $TEST 시험 수단: 실장비 PLC, 또는 Test/mock_vision_client.py (화면의 '수동 트리거 실행' 버튼은 MainRun 의 네트워크 $TEST 경로를 거치지 않아 D-80-11 PLC 해제 시험에 쓸 수 없다 — Custom/SystemHandler.cs:119 주석)
</interfaces>
</context>

## Artifacts this phase produces (이 plan 분)

phase 전체 원장은 `80-01-PLAN.md`. 이 plan 이 만드는 것: VersionDefine 1.7.51.0 항목, `.planning/phases/80-reviewer-ng-cycle-reinspect/80-HUMAN-UAT.md`, 저장소 밖 `/c/Users/admin/AppData/Local/Temp/p80/audit-80.txt`, `build-80-05.txt`.

## Flagged assumptions (UAT 에서 사용자 판단 — 자동 해소 금지)

- A-80-E1 (D-80-04): 리뷰어 창은 MainWindow 소유 창이라 메인 뒤로 갈 수 없어, 불러온 뒤 리뷰어를 최소화하고 메뉴 리뷰어 버튼으로 복원한다. 이 방식이 "열어 둔 채 메인 화면을 앞으로" 에 맞는지.
- A-80-E2 (D-80-05/06, D-80-00): 측정·FAI 노드를 고른 채 RUN 하면 그 Shot 을 실행하도록 바꿨고(전에는 오류), 리뷰어 사진 사용 중에는 오프라인 확인창을 생략한다.
- A-80-E3 (D-80-19): 크로스-Z 설정이 없는 두 장짜리(정적) 기준점은 사이클마다 기준점 사진을 저장하지 않으므로 '필요 사진 없음 = 완전' 으로 보고, 지금 티칭 사진으로 자동 Test Find 한다(반복검사 저장 사진 재검사와 같은 규칙).
- A-80-E4 (D-80-14): 저장 사진이 JPG 라 재검사 값이 실제 검사값과 조금 다를 수 있다는 안내 문구가 충분한지.
- A-80-E5 (D-80-07): 같은 자재 묶기가 두 날짜 폴더 cycle.json 을 읽는 시간('[Rerun] 리뷰어 부품' 로그의 ms)이 버튼 반응으로 괜찮은지.

<tasks>

<task type="auto">
  <name>Task 1: 버전 1.7.51.0 + phase 누적 감사(가독성 · MVVM · UI 3개 제한 · 회귀 0)</name>
  <precondition>80-01~80-04 SUMMARY.md 가 모두 있고 `/c/Users/admin/AppData/Local/Temp/p80/base-80-phase.txt` 가 있다. Debug 앱이 실행 중이 아니다</precondition>
  <reversibility rating="reversible">버전 상수·설명 문자열 변경뿐</reversibility>
  <files>WPF_Example/VersionDefine.cs</files>
  <read_first>
    - CLAUDE.md (🔒 가독성 규칙 — 버전 설명 문자열에도 물음표 금지)
    - WPF_Example/VersionDefine.cs:640-693
    - .planning/phases/80-reviewer-ng-cycle-reinspect/80-01-SUMMARY.md ~ 80-04-SUMMARY.md (실제 문구)
  </read_first>
  <action>
(1) VersionDefine.cs — 1.7.50.0 `[Version(...)]` 항목 다음에 `Number = "1.7.51.0"`, `Date` = 실행한 날짜(yyyy-MM-dd), `Change` = 쉬운 한국어 설명을 1.7.50.0 과 같은 문자열 + 연결 형식으로 더한다. 담을 내용: ① 결과 리뷰어 NG 원인 설명 아래 '이 사진으로 파라미터 수정' 버튼 — 누르면 확인창 없이 같은 자재의 모든 Shot 사진·기준점 가로/세로 사진·Z 후보 사진을 메인 화면에 불러오고, NG 측정 노드를 골라 파라미터를 보여 주며, 기준점 Test Find 를 한 번 자동으로 한다(RUN 은 직접). 불러올 수 없는 행은 버튼이 꺼지고 옆에 이유가 보인다. ② 짝이 맞는 기준점 사진이 없으면(수동 검사 기록 등) 알림 후 Shot 사진만 불러온다. ③ 불러온 동안 메인 화면에 노란 상태 줄 '리뷰어 사진 사용 중' + [해제], JPG·Z 후보 없음·기준점 사진 없음·기준점 찾기 실패 안내. ④ 레시피를 저장해도 사진 경로는 원래 값으로 저장되고(파라미터는 저장), [해제]·PLC 자동 검사 수신·레시피 변경·프로그램 종료 때 원래 사진과 오프라인 모드로 돌아간다. ⑤ 측정 노드를 고른 채 RUN 하면 그 Shot 을 실행하고, 리뷰어 사진 사용 중에는 오프라인 확인창을 띄우지 않는다. ⑥ 수동 RUN 에서 기준 ROI(Local Ref)를 고치면 Test Find 없이도 기준점 가로 사진에서 국부 기준선을 다시 구한다(PLC 자동 검사는 그대로). ⑦ 툴바 '기준 ROI 시험 찾기' 버튼 — 기준점 가로 사진(z1) 위에 기준 ROI 상자와 찾은 띠 에지 선(주황)을 보여 준다. ⑧ 리뷰어 사이클 전체 보기에서 다른 Shot 의 선이 사진 한 장에 겹쳐 그려지던 문제를 고쳤다. 문자열 안에 물음표 문자를 쓰지 않는다.
 - `public const string VERSION = "1.7.51.0";`, `BUILD_DATE` = 같은 날짜. 다른 줄은 건드리지 않는다.

(2) 누적 감사는 아래 verify 스크립트를 그대로 돌려 출력 전체를 `/c/Users/admin/AppData/Local/Temp/p80/audit-80.txt` 에 남기고 SUMMARY 에 붙인다. 하나라도 기준을 못 넘으면 고치지 말고 멈춰서 어느 줄인지 보고한다(이전 plan 코드를 이 Task 에서 고치지 않는다).

끝나면 VersionDefine.cs 만 `git add` 후 커밋. Release 빌드 금지.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
H=/c/Users/admin/AppData/Local/Temp/p80; PH=$(cat $H/base-80-phase.txt); W=WPF_Example
MSB="/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe"
{
"$MSB" $W/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -m -nologo -v:minimal -clp:ErrorsOnly > $H/build-80-05.txt 2>&1; echo "build_exit=$? cs_errors=$(grep -c 'error CS' $H/build-80-05.txt)"
NEW="$W/Custom/Sequence/Inspection/ReviewerReinspectService.cs $W/UI/Reviewer/ReviewerReinspectViewModel.cs $W/Custom/Sequence/Inspection/DatumTestFindService.cs $W/Custom/Sequence/Inspection/LocalRefTestFindService.cs"
for F in $NEW; do test -f $F || echo "MISSING $F"; echo "NEW $(basename $F) ternary=$(grep -cE '\?[^?]*:' $F) coalesce=$(grep -cF '??' $F) nullcond=$(grep -cF '?.' $F) switchexpr=$(grep -cE 'switch.*=>' $F) datesig=$(grep -cF 'hbk' $F) qmark=$(grep -cF '?' $F) logic3=$(grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)' $F)"; done
OLD="$W/Custom/Sequence/Inspection/RepeatRunService.cs $W/Custom/Sequence/Inspection/Action_FAIMeasurement.cs $W/Custom/Sequence/Inspection/InspectionSequence.cs $W/UI/ViewModel/ReviewMeasurementRow.cs $W/UI/Reviewer/ReviewerWindow.xaml $W/UI/Reviewer/ReviewerWindow.xaml.cs $W/UI/ContentItem/MainView.xaml $W/UI/ContentItem/MainView.xaml.cs $W/MainWindow.xaml.cs $W/Custom/SystemHandler.cs $W/UI/ControlItem/InspectionListView.xaml.cs $W/DatumMeasurement.csproj $W/VersionDefine.cs"
for F in $OLD; do ADD=$(git diff -w -U0 $PH -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+'); echo "OLD $(basename $F) added=$(printf '%s\n' "$ADD" | grep -c .) deleted=$(git diff -w -U0 $PH -- "$F" | grep -E '^-' | grep -vE '^---' | wc -l) ternary=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:') coalesce=$(printf '%s\n' "$ADD" | grep -cF '??') nullcond=$(printf '%s\n' "$ADD" | grep -cF '?.') switchexpr=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>') datesig=$(printf '%s\n' "$ADD" | grep -cF 'hbk') qmark=$(printf '%s\n' "$ADD" | grep -cF '?') logic3=$(printf '%s\n' "$ADD" | grep -cE '(&&|\|\|).*(&&|\|\|).*(&&|\|\|)')"; done
echo "csproj_new=$(grep -cE 'Compile Include="(Custom.Sequence.Inspection.(ReviewerReinspectService|DatumTestFindService|LocalRefTestFindService)|UI.Reviewer.ReviewerReinspectViewModel)\.cs"' $W/DatumMeasurement.csproj)"
echo "ui_new_xaml=$(git diff --name-only --diff-filter=A $PH -- '*.xaml' | wc -l) ui_new_window=$(git diff -U0 $PH -- $W | grep -E '^\+' | grep -cE ':\s*Window\b') ui_setting_changed=$(git diff --name-only $PH -- $W/Setting $W/Custom/SystemSetting.cs | wc -l) ui_new_buttons=$(git diff -U0 $PH -- $W/UI/Reviewer/ReviewerWindow.xaml $W/UI/ContentItem/MainView.xaml | grep -E '^\+' | grep -c '<Button ')"
DLG=0; for F in $(git diff --name-only $PH -- $W | grep '\.cs$'); do N=$(git diff -U0 $PH -- "$F" | grep -E '^\+' | grep -vE '^\+\+\+' | grep -cE 'Message[B]ox\.Show'); DLG=$((DLG+N)); done; echo "ui_new_dialog_calls=$DLG"
echo "h_apply=$(tr -d '\r' < $W/UI/Reviewer/ReviewerWindow.xaml.cs | awk '/private void Button_ApplyRowToMain_Click/,/^        }$/' | grep -c ';') h_alert=$(tr -d '\r' < $W/UI/Reviewer/ReviewerWindow.xaml.cs | awk '/private void ShowReinspectAlert/,/^        }$/' | grep -c ';') h_release=$(tr -d '\r' < $W/UI/ContentItem/MainView.xaml.cs | awk '/private void BtnReviewerRelease_Click/,/^        }$/' | grep -c ';') h_forget=$(tr -d '\r' < $W/UI/ContentItem/MainView.xaml.cs | awk '/public void ForgetDisplayedShotImage/,/^        }$/' | grep -c ';')"
same() { B=$(git show $PH:$1 | tr -d '\r' | awk -v s="$2" 'index($0,s){f=1} f{print} f&&/^        }$/{exit}' | md5sum | cut -c1-8); N=$(tr -d '\r' < $1 | awk -v s="$2" 'index($0,s){f=1} f{print} f&&/^        }$/{exit}' | md5sum | cut -c1-8); echo "same[$2]=$([ "$B" = "$N" ] && echo 1 || echo 0)"; }
same $W/UI/ContentItem/MainView.xaml.cs 'private void BtnTestFindDatum_Click'
same $W/UI/ContentItem/MainView.xaml.cs 'private HImage AskTestImageSource'
same $W/Custom/Sequence/Inspection/Action_FAIMeasurement.cs 'private void ProcessOneDatum'
same $W/Custom/Sequence/Inspection/Action_FAIMeasurement.cs 'private void InjectLocalRef'
same $W/Custom/Sequence/Inspection/InspectionSequence.cs 'private void HandleRunStartResetResults'
same $W/Custom/Sequence/Inspection/InspectionSequence.cs 'private void ComputeLocalRefLinesForDatum'
same $W/Custom/Sequence/Inspection/RepeatRunService.cs 'public static SavedCycleRerunPlan BuildPlan'
same $W/Custom/Sequence/Inspection/RepeatRunService.cs 'private static string ValidatePart'
same $W/Custom/Sequence/Inspection/RepeatRunService.cs 'private void ApplySavedCyclePart'
same $W/Custom/Sequence/Inspection/RepeatRunService.cs 'private SavedCycleOverrideSnapshot BuildOverrideSnapshot'
same $W/Custom/Sequence/Inspection/RepeatRunService.cs 'private static void RestoreOverridePathsOnly'
same $W/UI/ControlItem/InspectionListView.xaml.cs 'private bool ResolveRunnableAction'
same $W/UI/ViewModel/ReviewMeasurementRow.cs 'public static string ResolveCycleImagePath(CycleResultDto cycle)'
same $W/UI/ViewModel/ReviewMeasurementRow.cs 'public static string ResolveRowImagePath'
echo "version=$(grep -c 'public const string VERSION = "1.7.51.0";' $W/VersionDefine.cs) version_entry=$(grep -c 'Number = "1.7.51.0"' $W/VersionDefine.cs)"
} | tee $H/audit-80.txt
    </automated>
  </verify>
  <acceptance_criteria>
    - `build_exit=0 cs_errors=0`
    - NEW 4줄 전부 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 qmark=0 logic3=0`
    - OLD 13줄 전부 `ternary=0 coalesce=0 nullcond=0 switchexpr=0 datesig=0 qmark=0 logic3=0`, 그리고 `RepeatRunService.cs deleted=0`
    - `csproj_new=4`
    - `ui_new_xaml=0 ui_new_window=0 ui_setting_changed=0 ui_new_buttons=3 ui_new_dialog_calls=1`
    - `h_apply=1 h_alert=1 h_release=1 h_forget=1`
    - `same[...]=1` 14줄 전부
    - `version=1 version_entry=1`
  </acceptance_criteria>
  <done>버전이 1.7.51.0 으로 올라갔고, phase 시작 대비 가독성·MVVM·UI 3개 제한·회귀 0 이 audit-80.txt 로 증명되었다.</done>
</task>

<task type="auto">
  <name>Task 2: 80-HUMAN-UAT.md 작성 — 장비 PC Release UAT U-1~U-11 + A-80-E1~E5 판단 칸</name>
  <files>.planning/phases/80-reviewer-ng-cycle-reinspect/80-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/80-reviewer-ng-cycle-reinspect/80-CONTEXT.md (D-80-00~19, 함께 처리 1~3, specifics 실사례 17:21~17:29)
    - .planning/phases/80-reviewer-ng-cycle-reinspect/80-VALIDATION.md "Manual-Only Verifications"
    - .planning/phases/80-reviewer-ng-cycle-reinspect/80-01-SUMMARY.md ~ 80-04-SUMMARY.md (실제 버튼·상태 줄·알림·로그 문구)
    - .planning/phases/79-side-local-strip-datum/79-HUMAN-UAT.md (문서 형식)
    - /c/Users/admin/AppData/Local/Temp/p80/audit-80.txt (Task 1)
  </read_first>
  <action>
79-HUMAN-UAT.md 형식으로 쉬운 한국어 절차서를 쓴다. 머리: 상태(사용자 확인 대기), 버전 1.7.51.0, 실행 환경 = **장비 PC 의 Release 빌드만**(사무실 Debug 빌드는 라이선스 키가 없어 실행되지 않음). Release 빌드는 OutputPath 가 D:\Data 라 운영 exe 를 덮어쓰므로 배포는 앱 종료 확인 → D:\Data exe·main.ini 백업 → 사용자 승인 뒤 사용자가 한다(실행자는 하지 않음). 운영 레시피 D:\Data\Recipe\FAI_1\main.ini 는 U-5 전에 복사본을 떠 둔다. 누적 감사 결과(audit-80.txt 요약: 빌드·가독성·UI 3개·회귀 0)를 머리에 한 단락으로 적는다.

각 항목은 목적(관련 D-80 번호) · 준비 · 단계(번호) · 기대 결과(실제 화면 문구·로그 문구는 SUMMARY 에서 그대로 옮긴다) · 합격 기준 · 결과 칸(PASS/FAIL/메모):
- U-1 (D-80-01/02/03, D-80-00): 리뷰어에서 날짜 폴더를 열고 행을 안 고른 상태 → 버튼 꺼짐 + 이유 '측정 행을 먼저 고르세요'. 다른 레시피 사이클 행 → 이유 한 줄. NG 행 → 버튼 켜짐, 누르면 확인창 없이 바로 진행. 더블클릭·우클릭 메뉴 없음.
- U-2 (D-80-04/05/06/07/08, 로드맵 '사진·선 똑같이'): PLC 자동 검사 NG 행(기준점 사진이 저장된 사이클) → 리뷰어 최소화, 메인 앞으로, NG Shot 의 측정 노드 선택·파라미터 표시, 같은 자재의 다른 Shot 노드를 눌러도 그 자재 사진, NG 측정 선이 리뷰어와 같음, 기준점 노드의 TeachingImagePath 가 그 자재 기준점 사진(파일명 datum_…), Trace 로그 '[ReviewerLoad] 불러옴 — ' · '[Rerun] 리뷰어 부품(같은 자재)' · '[ReviewerLoad] 자동 Test Find — … 성공'. Z 범위 Shot 이면 RUN 뒤 선택 Z 가 리뷰어 '사용 Z' 와 같음. 리뷰어 메뉴 버튼으로 리뷰어 복원 후 다른 NG 행 불러오기(대체) 확인.
- U-3 (D-80-09/19): 수동 RUN 기록 행(IsProtocolDriven false) → 알림 '기준점 사진 없음' 1개 → Shot 사진만, 기준점 사진 경로 그대로, 자동 Test Find 없음(로그 없음), 상태 줄 '(기준점 사진 없음 — 지금 기준점 사진 사용)'. 가능하면 기준점 사진 파일 하나를 지운(복사본 폴더) 사이클로도 같은 결과.
- U-4 (D-80-05/06, A-80-E2): 불러온 상태에서 측정 파라미터(예: 에지 임계값)를 고치고 측정 노드가 선택된 채 RUN → 오류·오프라인 확인창 없이 그 Shot 재검사, 결과 표시. 리뷰어 사진을 쓰지 않을 때(해제 후) OfflineInspectMode 를 켜고 RUN → 기존 확인창이 그대로 뜸.
- U-5 (D-80-10): 파라미터 하나를 고친 채 레시피 저장 → main.ini 를 저장 전 복사본과 비교해 SimulImagePath · TeachingImagePath · TeachingImagePath_Vertical · TeachingImagePath_Horizontal 줄이 바뀌지 않았고 고친 파라미터 줄만 바뀜, Trace 로그 '레시피 저장 — 사진 경로는 불러오기 전 값으로 저장'. 저장 뒤에도 상태 줄 유지·사진 그대로.
- U-6 (D-80-11/12/13): 해제 4경로 각각 — [해제] 버튼 / PLC $TEST(실장비 또는 Test/mock_vision_client.py — 화면의 '수동 트리거 실행' 은 이 경로가 아님) / 레시피 변경 / 프로그램 종료 후 재시작 — 에서 상태 줄 사라짐, Trace '[ReviewerLoad] 해제 — <이유>', 사진 경로 원래 값, OfflineInspectMode 원래 값(불러오기 전 꺼져 있었으면 꺼짐, 재시작 후에도 꺼짐), 고친 파라미터는 유지. PLC 경로는 그 자동 검사가 실물 촬영으로 도는지(리뷰어 사진이 아닌지) 확인.
- U-7 (D-80-14/08, A-80-E4): JPG 사진 사이클 → 상태 줄 '(JPG 사진 — 실제 검사값과 조금 다를 수 있음)'. Z 후보 사진이 저장되지 않은 Z 범위 Shot → '(Z 후보 사진 없음 — 고른 z 사진 1장으로 검사)'. 문구가 충분한지 판단 칸.
- U-8 (함께 처리 2, D-79-04): (a) 2026-09-18 사례 재현 — SIDE_1(두 장짜리 기준점)에서 기존 Test Find 로 기준점을 잡고 C13_P3 기준 ROI 를 옮긴 뒤 RUN 만 → 결과 '기준' 열 '국부'(국부실패→전역 아님), Algorithm 로그 '[LocalRef] 기준 ROI·에지 설정이 바뀌어 기준점 가로 사진에서 국부 기준선을 다시 구함 — ', 두 번째 RUN 은 다시 구함 로그 없음. 띠가 없는 곳으로 옮기면 실제 원인 문구로 전역 전환. (b) 1장 기준점(TOP/BOTTOM, 국부 기준 켠 측정 — 장비 PC 에서 가능할 때): 기존 Test Find 를 [예] 지금 화면 사진(또는 [아니오] 다른 파일)으로 잡고 기준 ROI 를 옮긴 뒤 RUN → '국부실패→전역' 유지, Algorithm 로그 '[LocalRef] 기준점을 기준점 사진 파일이 아닌 사진으로 찾아서 국부 기준선을 다시 구하지 않음' 1줄(다른 사진의 선을 '국부' 로 조용히 쓰지 않음). 이어서 '기준 ROI 시험 찾기' 를 누른 뒤 ROI 를 다시 옮기고 RUN → '국부' + 다시 구함 로그(티칭 사진 파일로 잡은 기준점). (c) 두 장짜리 기준점을 Test Find 로 잡은 뒤 기준점 노드에서 가로 사진을 다른 파일로 Load 하고 RUN → 다시 구하지 않음 로그 + 전역 전환(사진 경로가 바뀜). PLC 자동 검사 결과는 전과 같음(옵션 켠 측정의 '기준' 이 국부, 다시 구함·다시 구하지 않음 로그 0).
- U-9 (D-80-15): 국부 기준을 켠 EdgeToLineDistance 측정 선택 → '기준 ROI 시험 찾기' → z1 기준점 가로 사진 배경, 기준 ROI 상자, 주황 선, 툴바 결과 라벨 초록 '… 띠 에지를 찾았습니다 — 에지 세기 N'. 상자를 끌어 옮기고 다시 누르면 선이 따라옴. 측정을 안 고르고 누르면 빨강 라벨 이유, 대화상자 없음. 같은 Shot 노드를 다시 누르면 측정 사진으로 돌아감.
- U-10 (함께 처리 1): Shot 이 여러 개인 사이클을 리뷰어에서 클릭(전체 보기) → 화면 사진에 찍힌 Shot 의 선만 보임. 행 클릭은 전과 같음.
- U-11 (회귀 0): 기존 Test Find 버튼(1장 기준점은 사진 출처 선택 대화상자 그대로), 반복검사 '저장 사진 재검사'(리뷰어 사진 사용 중이면 거절 문구, 해제 후 정상), 국부 기준 옵션 꺼진 측정 값·표시 불변, PLC 자동 사이클 판정·응답 불변.
- 끝 '알려진 한계' 절: PLC 자재 진행 도중(기준점 tick 과 다음 tick 사이)에 불러오면 그 자재는 기준점 캐시가 바뀌어 NG 가 날 수 있으니 설비 정지 상태에서 쓴다 / 불러온 동안 설정 창에서 설정을 저장하면 켜 둔 OfflineInspectMode 가 저장될 수 있음(종료 때 원래 값으로 다시 저장됨) / 로그인하지 않으면 속성창이 숨겨져 파라미터를 고칠 수 없음 / 재검사 결과와 리뷰어 원래 값 나란히 비교는 범위 밖(Deferred).
- 'A-80-E1~E5 판단' 절: 각 가정(이 plan 의 Flagged assumptions 문장 그대로)과 사용자 판단 칸.

커밋: `git add .planning/phases/80-reviewer-ng-cycle-reinspect/80-HUMAN-UAT.md` 만. 메시지 예: docs(80-05): 리뷰어 NG 사진 불러오기 UAT 절차서.
  </action>
  <verify>
    <automated>
cd /c/code/DataMeasurement
U=.planning/phases/80-reviewer-ng-cycle-reinspect/80-HUMAN-UAT.md
echo "exists=$(test -f $U && echo 1 || echo 0)"
for K in U-1 U-2 U-3 U-4 U-5 U-6 U-7 U-8 U-9 U-10 U-11 A-80-E1 A-80-E2 A-80-E3 A-80-E4 A-80-E5; do echo "$K=$(grep -c "$K" $U)"; done
echo "release_only=$(grep -c 'Release' $U) license=$(grep -c '라이선스' $U) backup=$(grep -c '백업' $U) log_load=$(grep -c '\[ReviewerLoad\] 불러옴' $U) log_release=$(grep -c '\[ReviewerLoad\] 해제' $U) log_recompute=$(grep -c '다시 구함' $U) log_not_teaching=$(grep -c '사진 파일이 아닌 사진으로 찾아서' $U) mock=$(grep -c 'mock_vision_client' $U) status_text=$(grep -c '리뷰어 사진 사용 중' $U) btn_text=$(grep -c '이 사진으로 파라미터 수정' $U) version=$(grep -c '1.7.51.0' $U)"
echo "commit_files=$(git show --name-only --format= HEAD | tr '\n' ' ')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `exists=1`, `U-1` ~ `U-11` 각각 >= 1, `A-80-E1` ~ `A-80-E5` 각각 >= 1
    - `release_only>=1 license>=1 backup>=1 log_load>=1 log_release>=1 log_recompute>=1 log_not_teaching>=1 mock>=1 status_text>=1 btn_text>=1 version>=1`
    - `commit_files` = `.planning/phases/80-reviewer-ng-cycle-reinspect/80-HUMAN-UAT.md`
  </acceptance_criteria>
  <done>장비 PC Release UAT 절차 U-1~U-11 과 가정 판단 칸이 실제 문구로 작성·커밋되었다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 사용자 확인 — 장비 PC Release 빌드로 리뷰어 NG 사진 불러오기·재검사 UAT (U-1~U-11) + A-80-E1~E5 판단</name>
  <what-built>Phase 80 전체(80-01~04): 리뷰어 '이 사진으로 파라미터 수정' 버튼(같은 자재 Shot·기준점·Z 후보 사진 불러오기, 기준점 없음 알림, NG 측정 노드 선택, 자동 Test Find), 메인 화면 상태 줄 + [해제], 레시피 저장 시 원래 사진 경로, PLC·레시피 변경·종료 자동 해제, 측정 노드 RUN, 수동 RUN Local Ref 재계산, 기준 ROI 시험 찾기, 리뷰어 선 겹침 수정. 버전 1.7.51.0, 누적 감사 audit-80.txt PASS.</what-built>
  <how-to-verify>
    1. `.planning/phases/80-reviewer-ng-cycle-reinspect/80-HUMAN-UAT.md` 를 연다.
    2. 장비 PC 에서 앱 종료를 확인하고 D:\Data 의 exe·main.ini 를 백업한 뒤, 승인한 Release 빌드를 배포한다(Debug 는 라이선스 키가 없어 실행 불가).
    3. U-1 → U-11 순서로 수행하고 결과 칸에 PASS/FAIL 과 메모를 적는다. U-5 는 main.ini 복사본과 비교, U-6 의 PLC 경로는 실장비 또는 Test/mock_vision_client.py 로 $TEST 를 보낸다.
    4. A-80-E1(리뷰어 최소화 방식), A-80-E2(측정 노드 RUN·오프라인 확인창 생략), A-80-E3(정적 두 장짜리 기준점), A-80-E4(JPG 안내 문구), A-80-E5(불러오기 반응 시간) 판단을 적는다.
    5. 이상이 있으면 U 번호와 증상(화면·Trace/Algorithm 로그 줄)을 알려 준다.
  </how-to-verify>
  <resume-signal>"approved" (U-1~U-11 PASS, A-80-E1~E5 판단 기록) 또는 실패한 U 번호와 증상을 적어 주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 실행자 빌드 → D:\Data 배포 exe | Release 빌드 OutputPath 가 운영 폴더라 실행자가 빌드하면 운영 exe 가 바뀐다 |
| UAT 절차 → 운영 레시피 main.ini | 저장 시험(U-5)이 운영 레시피를 바꾼다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80-19 | Tampering | 실행자 Release 빌드 | high | mitigate | Task 1 은 Debug|x64 만, 배포는 체크포인트 2번에서 사용자가 앱 종료·백업·승인 뒤 |
| T-80-20 | Tampering | U-5 레시피 저장 | medium | mitigate | 절차서가 저장 전 main.ini 복사본을 뜨게 하고 비교로 확인 |
| T-80-21 | Repudiation | 가정(A-80-E1~E5) 자동 해소 | low | mitigate | 판단 칸을 사용자가 채우기 전 승인 불가(blocking checkpoint) |
</threat_model>

<verification>
1. audit-80.txt: 빌드 0 오류, 가독성 0, csproj 4, UI 3개 제한(새 XAML·Window·설정 0, 새 Button 3, 새 대화상자 호출 1), 핸들러 1문장, 회귀 md5 14개 동일, RepeatRunService 삭제 0, 버전 1.7.51.0
2. 80-HUMAN-UAT.md U-1~U-11 · A-80-E1~E5 · Release 전용 · 백업 · 실제 로그 문구
3. 사용자 승인(또는 실패 U 번호) 기록
</verification>

<success_criteria>
- CONTEXT D-80-00~19 와 함께 처리 1~3 전부 사용자 UAT 로 확인
- 회귀 0 (기존 Test Find · 반복검사 · PLC 자동 · 옵션 꺼진 국부 기준)
</success_criteria>

<output>
완료 후 `.planning/phases/80-reviewer-ng-cycle-reinspect/80-05-SUMMARY.md` 작성: audit-80.txt 원문, 버전 커밋 해시, UAT 문서 커밋 해시, 사용자 응답 원문, U 번호별 결과표, A-80-E1~E5 판단.
</output>
</content>
</invoke>
