---
phase: quick-260517-l5e
plan: "02"
subsystem: Halcon/Measurements
tags: [edgetolinedistance, projection_pl, MeasureAxis, datum, overlay]
dependency_graph:
  requires: ["260517-l5e-01"]
  provides: ["X/Y 축 선택 가능한 projection_pl 기반 datum 기준선 수직거리 측정"]
  affects:
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs
tech_stack:
  added: []
  patterns:
    - "HOperatorSet.ProjectionPl — 에지 중점을 datum 기준선에 정사영해 수선의 발 계산"
    - "measureX 분기 — datum 수평선(Y) vs 수직선(X) 2점 구성 + 부호식 분리"
    - "footRow/footCol/footOk — projection 결과를 overlay 에 재사용 (측정값 계산과 공유)"
key_files:
  created: []
  modified:
    - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs
decisions:
  - "MeasureAxis 미설정/null/레거시 INI → MeasureAxis == 'X' false → Y 동작 (null-safe)"
  - "cosT < 0 정규화 — Atan2 방향(좌→우/우→좌) 무관 부호 일관성 보장"
  - "projection 실패 시 foot=에지점 → 측정값 0, FAI-DistLine skip (FAI-Edge1 + return true 유지)"
  - "레거시 else 2곳(AffineTransPoint2d/HomMat2dInvert) byte-identical 무변경 — datum 미주입 케이스 전용"
metrics:
  duration: "~15min"
  completed: "2026-05-17"
  tasks_completed: 2
  files_modified: 1
---

# Phase quick-260517-l5e Plan 02: projection_pl X/Y 일반화 Summary

## One-liner

EdgeToLineDistanceMeasurement에 MeasureAxis(Y/X) 프로퍼티 추가 + HOperatorSet.ProjectionPl 기반 datum 기준선 수직거리 측정으로 교체, FAI-DistLine overlay를 수선의 발 수직 드롭선으로 갱신.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | MeasureAxis 프로퍼티 추가 + TryExecute projection_pl X/Y 일반화 교체 | e838542 | EdgeToLineDistanceMeasurement.cs |
| 2 | msbuild Debug/x64 빌드 검증 | (no commit) | — |

## Changes Detail

### Task 1 — 5개 Edit 적용

**Edit A — MeasureAxis 프로퍼티 + MeasureAxisList wrapper (L56-64)**
- `MeasureAxis { get; set; } = "Y"` — [Category("Edge")], [ItemsSourceProperty], [Description]
- `MeasureAxisList` — [Browsable(false)] ComboBox 소스 래퍼
- 마커: `//260517 hbk l5e-03`

**Edit B — BuildFilteredProperties sourceNames 에 MeasureAxisList 추가 (L284)**
- `nameof(MeasureAxisList)` HashSet 에 등록 — PropertyGrid ComboBox 렌더 보장

**Edit C — TryExecute 정상 경로 projection_pl X/Y 일반화 교체 (L126-180)**
- `footRow/footCol/footOk` 도입 (overlay 재사용용)
- `measureX = (MeasureAxis == "X")` — null/""/"Y" → false
- `cosT < 0` 정규화로 Atan2 방향 무관 부호 일관성
- datum 수평선(Y): 방향벡터 (sinθ, cosθ), 2점 = 교점 ±200px
- datum 수직선(X): 방향벡터 (cosθ, -sinθ), 2점 = 교점 ±200px
- `HOperatorSet.ProjectionPl(pRow, pCol, axisR1, axisC1, axisR2, axisC2, out prRow, out prCol)`
- signedPx 부호식: Y → `(pRow-footRow)*(-cosT) + (pCol-footCol)*sinT` (D-02 +위쪽), X → `(pRow-footRow)*sinT + (pCol-footCol)*cosT` (+오른쪽)
- 레거시 `else` (AffineTransPoint2d, L181-196) byte-identical 무변경

**Edit D — FAI-DistLine 시작점을 수선의 발로 교체 (L219-224)**
- `originRow = footRow`, `originCol = footCol`, `originOk = footOk`
- projection 실패 시 `footOk=false` → `originOk=false` → FAI-DistLine skip
- 레거시 `else` (HomMat2dInvert, L225-241) byte-identical 무변경

**Edit E — stale 주석 3곳 정정**
- (a) 클래스 XML 주석 — "Y방향 거리" → X/Y 양 축 + projection_pl 설명으로 갱신
- (b) L215 주석 — "Y거리 선 overlay: 교점" → "수직 드롭선 overlay: 수선의 발"
- (c) L248 주석 — "교점(Datum 원점)" → "수선의 발 (projection foot, datum 기준선 위)"

### Task 2 — msbuild Debug/x64 빌드 결과

- C# 컴파일 오류(CS####): **0개**
- 신규 경고: **0개 (신규 없음)**
- 잔존 baseline 경고 2건 (plan 01 기준과 동일):
  - MSB3884: MinimumRecommendedRules.ruleset 미존재 (pre-existing)
  - CS0162: VirtualCamera.cs:266 접근 불가 코드 (pre-existing, SIMUL_MODE 상수 분기)
- 바이너리 복사 실패(MSB3027/MSB3021): Visual Studio Insiders + DatumMeasurement.exe 프로세스가 bin\x64\Debug\DatumMeasurement.exe 잠금 — 컴파일 자체와 무관, plan 01 빌드 환경과 동일 조건
- **판정: C# 컴파일 PASS (바이너리 복사 실패는 환경 조건, 오류 아님)**

## Deviations from Plan

None — 계획의 5개 Edit 를 정확히 적용했다. 기하 수식(projection_pl 인자 순서, sin/cos 축 끝점 공식, signedPx 부호식) 무수정.

## Known Stubs

None.

## Threat Flags

None — 신규 네트워크 엔드포인트, 인증 경로, 파일 접근 패턴, 스키마 변경 없음.

## Self-Check: PASSED

- `e838542` commit 존재 확인: git log 출력에서 확인됨
- `EdgeToLineDistanceMeasurement.cs` 수정 확인: 1 file changed, 75 insertions(+), 18 deletions(-)
- MeasureAxis 프로퍼티 존재: L62 `public string MeasureAxis { get; set; } = "Y";`
- HOperatorSet.ProjectionPl 호출 존재: L161
- footRow/footCol/footOk 도입: L134-136
- 레거시 else AffineTransPoint2d: L181-196 (byte-identical 무변경)
- 레거시 else HomMat2dInvert: L225-241 (byte-identical 무변경)
- D-08 리터럴 "All": L119 (무변경)
- D-09 ICustomTypeDescriptor: L20, BuildFilteredProperties (무변경)
- MeasurementBase.TryExecute 시그니처: 무변경
- Action_FAIMeasurement.cs: 무변경
