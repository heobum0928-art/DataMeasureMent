---
phase: quick-260519-c08
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
autonomous: false
requirements: [UAT2-3-REFRESH, UAT2-6a-HIGHLIGHT, UAT2-6b-LABEL]
must_haves:
  truths:
    - "Datum 노드에서 Load 후 새 이미지 경로가 PropertyGrid(TeachingImagePath)에 즉시 표시된다 (트리 재선택 불필요)"
    - "InspectionList 트리에서 Measurement/FAI 노드를 일반 클릭하면 해당 ROI 가 캔버스에서 노란색으로 하이라이트된다 (Edit 모드 진입 불필요)"
    - "ROI 명칭 라벨 글자 크기가 기존 대비 30% 작게 렌더된다"
  artifacts:
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "Datum Load 후 RefreshParamEditor 호출"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "HighlightSelectedRoi 일반선택 동작 보강"
    - path: "WPF_Example/Halcon/Display/HalconDisplayService.cs"
      provides: "ROI 라벨 폰트 30% 축소"
  key_links:
    - from: "button_loadImage_Click (Datum 분기)"
      to: "RefreshParamEditor()"
      via: "LoadAndDisplay 직후 호출"
    - from: "HighlightSelectedRoi"
      to: "MainResultViewerControl 렌더"
      via: "selectedRoiId 매칭 + 렌더 순서 보장"
---

<objective>
Phase 23.1 UAT 2차 보충 수정 3건. 모두 독립적인 WPF UI 버그 픽스이며 quick task 260518-vxp (commits 2260353 / 1fcaed6) 의 후속 잔여 항목이다.

- #3-refresh: Datum 노드 Load 후 TeachingImagePath 가 PropertyGrid 에 즉시 안 보임 → Load 완료 직후 강제 재바인딩.
- #6-a: ROI 노란 하이라이트가 Edit ROI 모드에서만 보이고 일반 트리 노드 클릭 시 안 보임 → 일반선택 하이라이트 경로 정상화.
- #6-b: ROI 명칭 라벨 글자가 너무 큼 → 폰트 크기 30% 축소.

Purpose: SOP 운용자가 Datum/ROI 티칭 작업을 트리 재탐색 없이 시각적으로 즉시 확인할 수 있게 한다.
Output: 3 파일 수정, msbuild Debug/x64 PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- 기존 코드 계약 — 코드베이스 재탐색 불필요 -->

InspectionListView.xaml.cs (이미 존재하는 공개 헬퍼, L29-39):
```csharp
// 자동 속성 write-back 후 PropertyGrid 강제 재렌더 (SelectedObject null→재할당)
public void RefreshParamEditor()  // _isRebinding 가드 내장, SelectedParam 기준 재바인딩
```

InspectionListView.xaml.cs — Datum Load 진입점 (L620-634):
```csharp
private void button_loadImage_Click(object sender, RoutedEventArgs e) {
    if (SelectedParam == null) return;
    if (SelectedParam is DatumConfig datumForLoad) {
        ICameraParam resolved = ResolveDatumCameraParam(datumForLoad);
        if (resolved == null) return;
        mParentWindow.mainView.LoadAndDisplay(resolved, datumForLoad);  // datumForLoad = 경로 sink
        return;
    }
    ...
}
```

MainView.xaml.cs — LoadAndDisplay 2-인자 오버로드 (L327~). 현재 반환형 void.
파일 선택 취소 시 `if (dialog.ShowDialog() != true) return;` 로 조기 종료.

MainView.xaml.cs — HighlightSelectedRoi (L211~, commit 1fcaed6):
```csharp
public void HighlightSelectedRoi(ParamBase param) {
    string selRoiId = null;
    if (param is FAIConfig faiSel) { selRoiId = faiSel.FAIName; }
    else if (param is MeasurementBase measSel) {
        string faiName = FindFaiNameContainingMeasurement(measSel);
        string mName = measSel.MeasurementName;
        if (string.IsNullOrEmpty(mName)) mName = measSel.TypeName;
        if (!string.IsNullOrEmpty(faiName)) selRoiId = faiName + "_" + mName;
    }
    var rois = GetCurrentFAIRois();
    halconViewer.UpdateDisplayState(rois, selRoiId, null, null);
}
```

GetCurrentFAIRois (L173~): dataGrid_faiResults.Items 를 순회해 FAI ROI 수집.
일반 FAI rectangle ROI 의 Id = fai.FAIName.
EdgeToLineDistanceMeasurement Point ROI 만 Id = fai.FAIName + "_" + measName 형태로 추가됨 (L191).

HalconDisplayService.cs — ROI 라벨 렌더 (L812~):
```csharp
private void DrawRoiLabelAt(HWindow window, double row, double col, string label) {
    EnsureFontInitialized(window);
    HOperatorSet.SetColor(window, "yellow");
    HOperatorSet.SetTposition(window, row, col);
    HOperatorSet.WriteString(window, label);
}
```
폰트는 EnsureFontInitialized 에서 1회 설정. 라벨 호출처: Rect L108 / Circle L89 / Polygon L101.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Datum Load 후 PropertyGrid 즉시 refresh (#3-refresh)</name>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <action>
`button_loadImage_Click` 의 Datum 분기에서 `LoadAndDisplay(resolved, datumForLoad)` 호출 직후, `return` 이전에 `RefreshParamEditor()` 를 호출한다.

이유: `LoadAndDisplay` 가 `datumForLoad.SetLatestImagePath(...)` 로 `DatumConfig.TeachingImagePath` (자동 속성) 를 write-back 하지만, 자동 속성은 INotifyPropertyChanged 를 발동하지 않아 PropertyGrid 가 stale 상태로 남는다. `RefreshParamEditor()` 는 SelectedObject 를 null→재할당해 강제 재렌더하는 기존 공개 헬퍼(L29-39)다 — Phase 12 Gap-3 에서 동일 목적으로 추가된 것이므로 재사용한다.

배치: `mParentWindow.mainView.LoadAndDisplay(resolved, datumForLoad);` 다음 줄에 추가:
```csharp
//260519 hbk #3-refresh — Datum Load 후 TeachingImagePath 자동속성 write-back 은 INPC 미발동 → PropertyGrid 강제 재바인딩
RefreshParamEditor();
```

주의:
- 사용자가 OpenFileDialog 를 취소한 경우에도 RefreshParamEditor 가 호출되지만, TeachingImagePath 가 변하지 않았으므로 무해(no-op 재렌더). 별도 분기 불필요.
- `RefreshParamEditor()` 는 `_isRebinding` 가드를 내장하므로 SelectionChanged 무한루프 위험 없음.
- 다른 분기(비-Datum ICameraParam Load)는 수정하지 않는다 — Shot 등은 ICameraParam 이며 PropertyGrid stale 이슈 보고 없음. 회귀 0 유지.
  </action>
  <verify>
    <automated>MISSING — msbuild Debug/x64 빌드로 컴파일 검증 (Task 3 끝에서 일괄 빌드)</automated>
  </verify>
  <done>Datum 분기 LoadAndDisplay 직후 RefreshParamEditor() 호출 추가. //260519 hbk 주석 포함. 컴파일 에러 0.</done>
</task>

<task type="auto">
  <name>Task 2: 일반 트리 노드 선택 시 ROI 노란 하이라이트 정상화 (#6-a)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <action>
현상: commit 1fcaed6 가 `HighlightSelectedRoi` 를 InspectionListView Measurement/FAI 분기(L466-468, L489-491)에서 호출하도록 배선했으나, 일반 노드 클릭 시 노란 하이라이트가 안 보이고 Edit ROI 모드에서만 보인다.

원인 2가지 — 둘 다 `HighlightSelectedRoi` (MainView.xaml.cs L211~) 안에서 수정한다:

(1) **selectedRoiId 불일치**: Measurement 노드 선택 시 selRoiId 를 `faiName + "_" + mName` 으로 도출하지만, `GetCurrentFAIRois()` 가 반환하는 일반 FAI 의 rectangle ROI 는 Id 가 `fai.FAIName` 이다 (composite ID 가 아님). composite ID 형태 ROI 는 EdgeToLineDistanceMeasurement Point ROI 일 때만 존재(GetCurrentFAIRois L191). 따라서 일반 Measurement 노드 선택 시 어떤 ROI 와도 매칭되지 않아 하이라이트 0.
   → 수정: Measurement 분기에서 composite ID 로 매칭되는 ROI 가 `rois` 안에 없으면 부모 FAI 의 ROI Id(`faiName`)로 fallback 한다. 즉 selRoiId 결정 직후, `GetCurrentFAIRois()` 결과에 `r.Id == selRoiId` 가 하나도 없고 `faiName` 이 비어있지 않으면 `selRoiId = faiName` 으로 대체.

(2) **렌더 순서 / 빈 ROI 목록**: `HighlightSelectedRoi` 는 `GetCurrentFAIRois()` (dataGrid_faiResults.Items 순회) 에 의존한다. FAI 노드 분기에서는 호출 직전 `DisplayFAIImage(faiConfig)` → `DisplayShotImage` → `halconViewer.LoadImage(img)` 가 실행돼 캔버스가 새 이미지로 재렌더되며 이때 `_selectedRoiId` 가 클리어/덮어쓰기 될 수 있다. InspectionListView 호출 순서상 HighlightSelectedRoi 가 DisplayFAIImage 보다 뒤(L489)이면 OK 지만, Measurement 분기는 LoadImage 가 없으므로 그대로 두면 된다.
   → 수정: `HighlightSelectedRoi` 마지막의 `halconViewer.UpdateDisplayState(rois, selRoiId, null, null);` 호출을 `Dispatcher.BeginInvoke(..., DispatcherPriority.Render)` 로 1회 defer 한다. 이렇게 하면 같은 selection 사이클에서 발생한 LoadImage/ClearResults 등의 선행 렌더가 끝난 뒤 하이라이트 상태가 최종 적용돼 덮어쓰기를 방지한다. (InspectionListView 의 TryTriggerDatumAutoReteach 가 동일 패턴으로 Dispatcher.BeginInvoke 를 쓴다 — L52 참조, 동일 스타일.)

구현 (HighlightSelectedRoi 본문 교체):
```csharp
public void HighlightSelectedRoi(ParamBase param) {
    //260519 hbk #6-a — 선택 노드의 ROI 하이라이트 ID 도출
    string selRoiId = null;
    string faiNameForFallback = null;
    if (param is FAIConfig faiSel) {
        selRoiId = faiSel.FAIName;
    }
    else if (param is MeasurementBase measSel) {
        string faiName = FindFaiNameContainingMeasurement(measSel);
        faiNameForFallback = faiName;
        string mName = measSel.MeasurementName;
        if (string.IsNullOrEmpty(mName)) mName = measSel.TypeName;
        if (!string.IsNullOrEmpty(faiName)) selRoiId = faiName + "_" + mName;
    }
    var rois = GetCurrentFAIRois();
    //260519 hbk #6-a — composite ID 매칭 ROI 없으면 부모 FAI ROI 로 fallback (일반 FAI rect ROI 는 Id=FAIName)
    if (!string.IsNullOrEmpty(selRoiId) && !string.IsNullOrEmpty(faiNameForFallback)) {
        bool matched = false;
        foreach (var r in rois) {
            if (r != null && r.Id == selRoiId) { matched = true; break; }
        }
        if (!matched) selRoiId = faiNameForFallback;
    }
    //260519 hbk #6-a — 선행 LoadImage/ClearResults 렌더가 끝난 뒤 하이라이트 최종 적용 (덮어쓰기 방지)
    string finalRoiId = selRoiId;
    var finalRois = rois;
    Dispatcher.BeginInvoke(new Action(() => {
        halconViewer.UpdateDisplayState(finalRois, finalRoiId, null, null);
    }), DispatcherPriority.Render);
}
```

주의:
- `using System.Windows.Threading;` 가 이미 import 되어 있는지 확인. 없으면 추가(InspectionListView 는 DispatcherPriority 사용 중이므로 패턴 존재). MainView 에 없으면 fully-qualified `System.Windows.Threading.DispatcherPriority.Render` 사용으로 import 0 유지 가능 — 파일 기존 스타일 따를 것.
- `C# 7.2` 준수: `is` 패턴 매칭(`is FAIConfig faiSel`)은 7.0 기능이므로 OK. switch expression / nullable ref 금지.
- 라벨 자체는 selectedRoiId 무관하게 항상 렌더되므로(HalconDisplayService) 이 Task 범위 아님.
- 만약 실제 빌드/UAT 에서 fallback 만으로 불충분(여전히 Edit 모드에서만 보임)하면, MainResultViewerControl.LoadImage 가 _selectedRoiId 를 클리어하는지 확인 — 클리어한다면 Dispatcher defer 로 이미 해소됨. 추가 조사는 Task 3 빌드 후 SIMUL UAT 에서 확인.
  </action>
  <verify>
    <automated>MISSING — msbuild Debug/x64 빌드 (Task 3 일괄)</automated>
  </verify>
  <done>HighlightSelectedRoi 가 (a) composite ID 미매칭 시 부모 FAI ROI fallback, (b) UpdateDisplayState 호출을 Dispatcher.Render defer. //260519 hbk 주석 포함. 컴파일 에러 0.</done>
</task>

<task type="auto">
  <name>Task 3: ROI 명칭 라벨 폰트 30% 축소 + 일괄 빌드 (#6-b)</name>
  <files>WPF_Example/Halcon/Display/HalconDisplayService.cs</files>
  <action>
ROI 명칭 라벨 글자 크기를 기존 대비 30% 축소한다 (commit 1fcaed6 의 DrawRoiLabelAt 라벨이 너무 큼).

(1) `EnsureFontInitialized` 메서드를 찾는다 (HalconDisplayService.cs 내). 이 메서드가 `HOperatorSet.SetFont` 또는 `window.SetFont` 로 폰트를 설정하며 폰트 크기 토큰(예: `-Default-14-...` 또는 size 인자)을 포함할 것이다.

(2) 폰트 크기 결정 방식 확인 후 30% 축소:
   - HALCON 폰트 문자열이 size 토큰을 포함하면(예: `"-Consolas-16-*-*-*-*-*"` 또는 `"Consolas-16"`), 현재 size 값을 읽어 `round(size * 0.7)` 로 교체.
   - 현재 size 가 명시되지 않았다면 HALCON 기본(통상 16px 내외)을 가정하지 말고, **DrawRoiLabelAt 전용으로 라벨 렌더 직전에 작은 폰트를 명시 적용**한다.

   `EnsureFontInitialized` 가 전역 폰트를 설정하고 다른 오버레이 텍스트(좌표 hover, 결과 메시지 등)도 같은 폰트를 공유하면, 전역 폰트를 줄이지 말 것 — 회귀 위험. 대신 `DrawRoiLabelAt` 안에서 라벨 그리기 직전에 축소 폰트를 SetFont 하고, WriteString 후 원복은 불필요(다음 EnsureFontInitialized 호출 또는 다른 렌더가 재설정). 단 같은 Render 패스 내 후속 텍스트가 영향받지 않도록 주의 — DrawRoiLabelAt 가 ROI 루프 안에서 호출되고 이후 좌표/메시지 텍스트가 별도 폰트로 SetFont 되는지 확인.

   안전한 접근: `DrawRoiLabelAt` 안에서 축소 폰트 적용 후 라벨 렌더하고, 메서드 끝에서 `EnsureFontInitialized(window)` 를 다시 호출해 전역 폰트를 원복한다. 이렇게 하면 라벨만 작아지고 다른 텍스트는 영향 0.

   구현 예 (DrawRoiLabelAt 교체):
```csharp
//260424 hbk Phase 12 Gap-2 — 주어진 (row, col) 에 yellow 텍스트 라벨 렌더
//260519 hbk #6-b — 라벨 폰트 30% 축소 (전역 폰트 영향 0: 렌더 후 원복)
private void DrawRoiLabelAt(HWindow window, double row, double col, string label)
{
    try
    {
        EnsureFontInitialized(window);
        //260519 hbk #6-b — 라벨 전용 축소 폰트 (기존 대비 ~70%)
        HOperatorSet.SetFont(window, "<축소 폰트 문자열>");
        HOperatorSet.SetColor(window, "yellow");
        HOperatorSet.SetTposition(window, row, col);
        HOperatorSet.WriteString(window, label);
        //260519 hbk #6-b — 전역 폰트 원복 (좌표/메시지 텍스트 회귀 방지)
        EnsureFontInitialized(window);
    }
    ...
}
```
   `<축소 폰트 문자열>` 은 `EnsureFontInitialized` 가 쓰는 폰트 문자열의 size 만 30% 줄인 값으로 채운다. 예: EnsureFontInitialized 가 size 16 을 쓰면 `round(16*0.7)=11`. HALCON 폰트 문자열 포맷은 EnsureFontInitialized 의 실제 코드를 그대로 따른다.

   주의 — EnsureFontInitialized 가 `_fontInitialized` 같은 1회성 가드 플래그로 SetFont 를 건너뛰면, 끝의 원복 `EnsureFontInitialized` 호출이 no-op 이 된다. 그 경우: 원복은 EnsureFontInitialized 가 실제 적용하는 폰트 문자열을 `DrawRoiLabelAt` 끝에서 직접 `HOperatorSet.SetFont` 로 재설정하는 방식으로 처리(가드 우회). EnsureFontInitialized 본문을 먼저 읽고 가드 유무에 맞춰 결정할 것.

(3) Rectangle/Circle/Polygon 3개 라벨 호출처(L89/L101/L108)는 모두 DrawRoiLabelAt 를 거치므로 추가 수정 불필요. RenderDatumOverlay 의 "Circle" 라벨(L666)도 같은 헬퍼를 쓰므로 함께 30% 축소된다 — 의도된 동작(라벨 일관성), 별도 분기 불필요.

(4) **일괄 빌드 (Task 1+2+3 검증)**:
실행 중인 DatumMeasurement.exe 가 있으면 종료(파일 잠금 MSB3027 방지 — STATE.md 의 알려진 이슈). 그 후:
```
msbuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild
```
0 errors 확인. 신규 warning 0 (기존 baseline = 4 warnings, commit b02d1cd 기준). 신규 warning 발생 시 수정.
  </action>
  <verify>
    <automated>msbuild WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Rebuild — 0 errors, 신규 warning 0</automated>
  </verify>
  <done>DrawRoiLabelAt 가 라벨 전용 30% 축소 폰트 적용 + 전역 폰트 원복. msbuild Debug/x64 Rebuild PASS (0 errors). //260519 hbk 주석 포함.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: SIMUL UAT 3건 육안 검증</name>
  <what-built>
3건의 UI 수정 — Datum Load PropertyGrid 즉시 refresh / 일반 노드선택 ROI 노란 하이라이트 / ROI 라벨 폰트 30% 축소.
  </what-built>
  <how-to-verify>
앱을 실행(SIMUL_MODE)하고 검사 레시피 로드 후:

1. **#3-refresh** — InspectionList 트리에서 Datum 노드 선택 → Load Image 버튼 클릭 → 이미지 파일 선택.
   기대: PropertyGrid 의 TeachingImagePath 항목에 방금 선택한 경로가 **즉시** 표시된다 (다른 노드로 이동했다 돌아올 필요 없음).

2. **#6-a** — InspectionList 트리에서 FAI 노드를 일반 클릭(Edit ROI 모드 진입 안 함).
   기대: 해당 FAI 의 ROI 가 캔버스에서 **노란색**(width 3)으로 하이라이트된다.
   이어서 그 FAI 의 자식 Measurement 노드를 클릭 → 부모 FAI ROI 가 노란색으로 하이라이트 유지.

3. **#6-b** — 위 2번 상태에서 ROI 명칭 라벨 글자 크기 확인.
   기대: 라벨 글자가 이전(commit 1fcaed6) 대비 눈에 띄게 작다 (약 30% 축소).

이상 발견 시 구체적으로 기술.
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 발견된 문제 기술</resume-signal>
</task>

</tasks>

<verification>
- msbuild Debug/x64 Rebuild: 0 errors, 신규 warning 0
- 3 파일 모두 //260519 hbk 주석 포함
- C# 7.2 준수 (switch expression / nullable ref / record 미사용)
- 비-Datum Load 경로, 좌표/메시지 텍스트 폰트 회귀 0
</verification>

<success_criteria>
- Datum Load 후 TeachingImagePath 가 PropertyGrid 에 즉시 표시
- 일반 트리 노드(FAI/Measurement) 클릭 시 ROI 노란 하이라이트 표시 (Edit 모드 불필요)
- ROI 명칭 라벨 폰트 30% 축소
- SIMUL UAT 3/3 사용자 승인
</success_criteria>

<output>
완료 후 `.planning/quick/260519-c08-phase-23-1-uat-2-3-datum-refresh-roi-hig/260519-c08-SUMMARY.md` 생성.
커밋 메시지: `fix(quick-260519-c08): Phase 23.1 UAT 2차 보충 3건 — Datum Load refresh / ROI 하이라이트 / 라벨 축소`
</output>
