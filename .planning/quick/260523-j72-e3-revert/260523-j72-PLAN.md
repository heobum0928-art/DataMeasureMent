---
phase: quick-260523-j72
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundLongAxisDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundShortAxisDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs
  - WPF_Example/DatumMeasurement.csproj
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: false
requirements:
  - QUICK-260523-J72-REVERT
  - QUICK-260523-J72-ALGO
must_haves:
  truths:
    - "E3 type name 은 'CompoundShortAxisDistance' (단축) 로 표시된다 (PropertyGrid + INI Type dispatch + UI 화이트리스트 일관)"
    - "사각형 추출 후 짧은 변 폭(2×length2) 거리가 mm 로 측정된다 — 결과값은 revert 직후 2×min(length1,length2)×pixelResolution 과 byte-identical"
    - "긴 변 2개가 라인 오버레이로 표시된다 (FAI-LongEdge1 / FAI-LongEdge2)"
    - "중심을 지나는 수직 측정선이 오버레이로 표시된다 (FAI-MeasureLine)"
    - "두 교점이 점 마커로 표시되며 (FAI-Intersection1 / FAI-Intersection2), 교점 간 거리선이 표시된다 (FAI-DistLine)"
    - "TryFindLargestContourRect 또는 TryIntersectLines 실패 시 measurement 가 false 반환 — overlay 빈 리스트, 앱 무크래시"
    - "Datum 비의존 유지 (IDatumOriginConsumer 미구현) — datumTransform 은 ROI 영역 변환에만 사용"
    - "msbuild Debug/x64 Rebuild PASS, 신규 errors/warnings 0"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundShortAxisDistanceMeasurement.cs"
      provides: "Phase 32-05 revert + 교점 기반 알고리즘 (single-class file, git mv from CompoundLongAxisDistanceMeasurement.cs)"
      contains: "public class CompoundShortAxisDistanceMeasurement"
    - path: "WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs"
      provides: "case 'CompoundShortAxisDistance' + GetTypeNames 15번째 원소"
      contains: "CompoundShortAxisDistance"
    - path: "WPF_Example/DatumMeasurement.csproj"
      provides: "Compile Include 경로 revert"
      contains: "CompoundShortAxisDistanceMeasurement.cs"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "3 spot type cast revert (L295, L1397, L1578~1593)"
      contains: "CompoundShortAxisDistanceMeasurement"
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "Rect ROI whitelist entry revert (L473)"
      contains: "CompoundShortAxisDistanceMeasurement"
  key_links:
    - from: "MeasurementFactory.Create"
      to: "CompoundShortAxisDistanceMeasurement(owner)"
      via: "switch case 'CompoundShortAxisDistance'"
      pattern: "case \"CompoundShortAxisDistance\""
    - from: "CompoundShortAxisDistanceMeasurement.TryExecute"
      to: "VisionAlgorithmService.TryFindLargestContourRect"
      via: "rectangle 중심/각도/장단축 추출"
      pattern: "TryFindLargestContourRect"
    - from: "CompoundShortAxisDistanceMeasurement.TryExecute"
      to: "VisionAlgorithmService.TryIntersectLines"
      via: "수직 측정선 ↔ 긴변1/긴변2 교점 산출 (× 2회 호출)"
      pattern: "TryIntersectLines"
---

<objective>
Phase 32 commit 3343250 (단축→장축 정정) 을 revert + E3 측정 알고리즘을 교점 기반으로 재작성.

**문제 1 (revert):** SOP 와 사용자 의도 재확인 결과 E3 는 단축(짧은 변) 거리가 맞음. commit 3343250 의 단축→장축 변경 (`2*min`→`2*max`, ShortAxis→LongAxis 리네임) 을 모두 되돌린다.

**문제 2 (algorithm 교체):** 현재 스칼라 계산 `2 * min(length1, length2) * pixelResolution` 만 있고 측정점/측정선 오버레이가 없다. 사용자 요청 형상 — "긴변 2개 추출 → 긴변 하나를 90도 회전한 방향으로 긴변 간 거리 측정" — 을 명시 교점으로 구현:

  1. TryFindLargestContourRect 로 (centerRow, centerCol, phi, length1, length2) 획득
  2. phi 방향 = 사각형 장축 (HALCON SmallestRectangle2 컨벤션, length1 이 첫번째 축)
  3. 긴 변 2개 = phi 방향 ± length2 (수직 거리 length2 만큼 평행이동한 양 끝 직선)
  4. 중심을 지나는 phi+π/2 방향 직선 = 측정선 (단축 방향)
  5. 측정선 ↔ 긴변1, 측정선 ↔ 긴변2 각각 TryIntersectLines 로 교점 산출
  6. 두 교점 간 거리 × pixelResolution = 결과값 (수학적으로 2×length2×pixelResolution 과 동일하므로 revert 직후 스칼라와 byte-identical)
  7. 오버레이: 긴변2(FAI-LongEdge1/2) + 측정선(FAI-MeasureLine) + 교점2(FAI-Intersection1/2) + 거리선(FAI-DistLine)

**Datum 비의존 유지:** 단축 폭은 사각형 자체 기하 → IDatumOriginConsumer 미구현 (현재와 동일).

Purpose: SOP 일치 (E3 = 단축 거리, 공차 0.600±0.030) + 측정 결과 시각적 검증 가능성 확보 + ArcLineIntersect 의 명시 교점 패턴과의 일관성.
Output: 단일 클래스 파일 git mv + 알고리즘 본문 재작성 + 5개 wiring 파일 type 이름 revert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

# 현재 E3 파일 — Part A revert + Part B 알고리즘 본문 교체 대상
@WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundLongAxisDistanceMeasurement.cs

# Part B 교점 기반 알고리즘 reference template
@WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs

# Part A wiring revert 대상
@WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs

<interfaces>
<!-- VisionAlgorithmService 의 키 메서드 — Part B 알고리즘에서 직접 호출 -->
<!-- 시그니처 발췌. 본문 무변경. -->

From WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs:
```csharp
// L776 — Part B Step 1: 컨투어 → 최대 사각형
public bool TryFindLargestContourRect(
    HImage image,
    double roiRow, double roiCol, double roiPhi, double roiLength1, double roiLength2,
    HTuple datumTransform,
    double cannyAlpha, int cannyLow, int cannyHigh, double unionDistance,
    out double centerRow, out double centerCol, out double phi,
    out double length1, out double length2, out string error);

// L926 — Part B Step 5: 직선 ↔ 직선 교점 (정적 메서드, 평행/근접 시 false)
public static bool TryIntersectLines(
    double row1a, double col1a, double row1b, double col1b,
    double row2a, double col2a, double row2b, double col2b,
    out double intRow, out double intCol);
```

From WPF_Example/Custom/Sequence/Inspection/Measurements/MeasurementBase.cs (시그니처):
```csharp
public abstract bool TryExecute(
    HImage image,
    HTuple datumTransform,
    double pixelResolution,
    out double resultValue,
    out string error,
    out List<EdgeInspectionOverlay> overlays);
```

From WPF_Example/Halcon/Models/EdgeInspectionOverlay.cs (오버레이 구조):
```csharp
// LineRow1/LineColumn1 ~ LineRow2/LineColumn2 — 라인 양 끝점
// LineRow1==LineRow2 && LineColumn1==LineColumn2 — 점 마커
// Points = List<EdgeInspectionPoint{Row,Column}> — X마커
```
</interfaces>

<style_notes>
- CompoundLongAxisDistanceMeasurement.cs 의 현재 스타일: **Allman** (브레이스 새 줄)
- 주석 컨벤션: 모든 신규/수정 라인에 `//260523 hbk Phase 32 — E3 단축 환원` (Part A) 또는 `//260523 hbk Phase 32 — E3 교점 기반 알고리즘` (Part B)
- 기존 `//260521 hbk Phase 32 ...` 주석은 ArcLineIntersect 처럼 stack 보존 가능 (Phase 20 D-12 패턴) — 다만 본 파일은 전면 재작성이므로 Phase 32 (Plan 32-05) 주석은 새 라인의 한쪽에만 남기고 신규 라인은 260523 marker 만 부착 가능. 판단은 executor 재량 (혼란 시 신규 한 줄만 부착).
</style_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Part A revert (commit 3343250) + Part B 알고리즘 본문 재작성 (단일 git mv + 파일 재작성)</name>
  <files>
    WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundLongAxisDistanceMeasurement.cs (삭제),
    WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundShortAxisDistanceMeasurement.cs (신규, git mv 로부터)
  </files>
  <action>
    **Step 1 — git mv 파일 이름 환원:**
    ```
    git mv WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundLongAxisDistanceMeasurement.cs \
           WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundShortAxisDistanceMeasurement.cs
    ```

    **Step 2 — 파일 전체를 아래 구조로 재작성 (Allman 스타일 + 260523 hbk 주석):**

    클래스 헤더 & 필드:
      - File-leading 주석 교체: `//260523 hbk Phase 32 — E3 단축 환원 + 교점 기반 알고리즘 재작성`
      - using: 기존 그대로 (System.Collections.Generic, HalconDotNet, PropertyTools.DataAnnotations, ReringProject.Halcon.Algorithms, ReringProject.Halcon.Models, System)
      - namespace: ReringProject.Sequence (변경 없음)
      - 클래스명: `CompoundShortAxisDistanceMeasurement` (Long→Short revert)
      - 생성자명: `CompoundShortAxisDistanceMeasurement(object owner) : base(owner) { }`
      - TypeName getter: `return "CompoundShortAxisDistance";` (revert)
      - XML doc comment: "Rect ROI 1개 → 공통 컨투어 알고리즘(canny→union_adjacent→LargestRect) → 단축 폭(mm) 측정. E3(SOP p.50): CompoundShortAxisDistance — 짧은 변 거리, 공차 0.600±0.030. 알고리즘: 긴변 2개(phi 방향 직선) ↔ 중심 지나는 phi+π/2 측정선 교점 2개의 거리 = 2×length2×pixelResolution. Datum 비의존 (IDatumOriginConsumer 미구현)."
      - Rect ROI 필드 5개 (Rect_Row/Col/Phi/Length1/Length2): 그대로 유지 (Phase 32-05 의 `//260521 hbk Phase 32 E3` 주석 보존 OK, 또는 신규 marker 로 정리)
      - Contour 파라미터 4개 (CannyAlpha=1.0, CannyLow=20, CannyHigh=40, UnionDistance=700.0): 그대로 유지
      - IDatumOriginConsumer 인터페이스 **미구현** 유지 — `: MeasurementBase` 만 상속 (단축 폭은 Datum 비의존)

    TryExecute 본문 (override) — 6 단계:

    1. **초기화:** `resultValue = 0; error = null; overlays = new List<EdgeInspectionOverlay>();` + `var svc = new VisionAlgorithmService();`

    2. **Step 1: TryFindLargestContourRect 호출** (기존과 동일)
       ```csharp
       double centerRow, centerCol, phi, length1, length2;
       if (!svc.TryFindLargestContourRect(image,
           Rect_Row, Rect_Col, Rect_Phi, Rect_Length1, Rect_Length2,
           datumTransform,
           CannyAlpha, CannyLow, CannyHigh, UnionDistance,
           out centerRow, out centerCol, out phi, out length1, out length2, out error))
       {
           return false;
       }
       ```

    3. **Step 2: 긴 변 2개의 양 끝점 계산.**
       HALCON SmallestRectangle2 컨벤션: phi = 첫번째 축 방향 (length1 = phi 방향 반길이). 따라서 **긴 변(장축) 방향 = phi**, 긴 변의 길이 = 2×length1, 긴 변 간의 수직거리 = 2×length2.
       (Part B 사용자 인용: "긴변 2개" = 사각형의 길게 늘어진 두 면. 두 긴변은 서로 평행하고 phi 방향. 그 사이의 짧은 거리 = 단축 폭.)
       ```csharp
       double sinPhi = Math.Sin(phi);
       double cosPhi = Math.Cos(phi);
       double sinPerp = Math.Sin(phi + Math.PI / 2.0);   // = cosPhi
       double cosPerp = Math.Cos(phi + Math.PI / 2.0);   // = -sinPhi
       // 긴 변 1 중점 = 중심에서 단축 방향(perp) 으로 -length2
       double edge1MidRow = centerRow - length2 * sinPerp;
       double edge1MidCol = centerCol - length2 * cosPerp;
       // 긴 변 2 중점 = 중심에서 단축 방향(perp) 으로 +length2
       double edge2MidRow = centerRow + length2 * sinPerp;
       double edge2MidCol = centerCol + length2 * cosPerp;
       // 긴 변 1 양 끝점 = 중점 ± length1 × 장축 방향(phi)
       double edge1aRow = edge1MidRow - length1 * sinPhi;
       double edge1aCol = edge1MidCol - length1 * cosPhi;
       double edge1bRow = edge1MidRow + length1 * sinPhi;
       double edge1bCol = edge1MidCol + length1 * cosPhi;
       // 긴 변 2 양 끝점
       double edge2aRow = edge2MidRow - length1 * sinPhi;
       double edge2aCol = edge2MidCol - length1 * cosPhi;
       double edge2bRow = edge2MidRow + length1 * sinPhi;
       double edge2bCol = edge2MidCol + length1 * cosPhi;
       ```

    4. **Step 3: 측정선 (중심을 지나는 단축 방향 = phi+π/2 방향 직선) 의 양 끝점.**
       sweep 길이는 length2 의 1.5배 정도로 충분 (긴변 너머까지 닿게 하여 교점 보장).
       ```csharp
       double measureExtent = length2 * 1.5; // 긴 변 너머까지 보장 (교점 안전)
       double measureRow1 = centerRow - measureExtent * sinPerp;
       double measureCol1 = centerCol - measureExtent * cosPerp;
       double measureRow2 = centerRow + measureExtent * sinPerp;
       double measureCol2 = centerCol + measureExtent * cosPerp;
       ```

    5. **Step 4: TryIntersectLines × 2 (측정선 ↔ 긴변1, 측정선 ↔ 긴변2).**
       ```csharp
       double int1Row, int1Col;
       if (!VisionAlgorithmService.TryIntersectLines(
           measureRow1, measureCol1, measureRow2, measureCol2,
           edge1aRow, edge1aCol, edge1bRow, edge1bCol,
           out int1Row, out int1Col))
       {
           error = "긴변1↔측정선 교점 산출 실패 (평행/근접)";
           return false;
       }
       double int2Row, int2Col;
       if (!VisionAlgorithmService.TryIntersectLines(
           measureRow1, measureCol1, measureRow2, measureCol2,
           edge2aRow, edge2aCol, edge2bRow, edge2bCol,
           out int2Row, out int2Col))
       {
           error = "긴변2↔측정선 교점 산출 실패 (평행/근접)";
           return false;
       }
       ```

    6. **Step 5: 결과값 = 두 교점 간 euclidean 거리 × pixelResolution.**
       수학적으로 이 값은 정확히 `2 × length2 × pixelResolution` (revert 직후 스칼라와 byte-identical, 단 floating-point 차이는 측정 가능 한도 이하).
       ```csharp
       double dRow = int2Row - int1Row;
       double dCol = int2Col - int1Col;
       double distPx = Math.Sqrt(dRow * dRow + dCol * dCol);
       resultValue = distPx * pixelResolution;
       ```

    7. **Step 6: 오버레이 5개 추가** — return true 직전, ADDITIVE, HALCON 재호출 0 (ArcLineIntersect 패턴):
       ```csharp
       // 긴변 1 (라인 + 중점 마커)
       overlays.Add(new EdgeInspectionOverlay
       {
           RoiId = "FAI-LongEdge1",
           LineRow1 = edge1aRow, LineColumn1 = edge1aCol,
           LineRow2 = edge1bRow, LineColumn2 = edge1bCol,
           Points = new List<EdgeInspectionPoint>
           {
               new EdgeInspectionPoint { Row = edge1MidRow, Column = edge1MidCol }
           }
       });
       // 긴변 2
       overlays.Add(new EdgeInspectionOverlay
       {
           RoiId = "FAI-LongEdge2",
           LineRow1 = edge2aRow, LineColumn1 = edge2aCol,
           LineRow2 = edge2bRow, LineColumn2 = edge2bCol,
           Points = new List<EdgeInspectionPoint>
           {
               new EdgeInspectionPoint { Row = edge2MidRow, Column = edge2MidCol }
           }
       });
       // 측정선 (중심 지나는 phi+π/2 방향)
       overlays.Add(new EdgeInspectionOverlay
       {
           RoiId = "FAI-MeasureLine",
           LineRow1 = measureRow1, LineColumn1 = measureCol1,
           LineRow2 = measureRow2, LineColumn2 = measureCol2,
           Points = new List<EdgeInspectionPoint>
           {
               new EdgeInspectionPoint { Row = centerRow, Column = centerCol }
           }
       });
       // 교점 1 (점 마커)
       overlays.Add(new EdgeInspectionOverlay
       {
           RoiId = "FAI-Intersection1",
           LineRow1 = int1Row, LineColumn1 = int1Col,
           LineRow2 = int1Row, LineColumn2 = int1Col,
           Points = new List<EdgeInspectionPoint>
           {
               new EdgeInspectionPoint { Row = int1Row, Column = int1Col }
           }
       });
       // 교점 2 (점 마커)
       overlays.Add(new EdgeInspectionOverlay
       {
           RoiId = "FAI-Intersection2",
           LineRow1 = int2Row, LineColumn1 = int2Col,
           LineRow2 = int2Row, LineColumn2 = int2Col,
           Points = new List<EdgeInspectionPoint>
           {
               new EdgeInspectionPoint { Row = int2Row, Column = int2Col }
           }
       });
       // 측정 거리선 — 교점1 ↔ 교점2
       overlays.Add(new EdgeInspectionOverlay
       {
           RoiId = "FAI-DistLine",
           LineRow1 = int1Row, LineColumn1 = int1Col,
           LineRow2 = int2Row, LineColumn2 = int2Col,
           Points = new List<EdgeInspectionPoint>
           {
               new EdgeInspectionPoint { Row = int1Row, Column = int1Col },
               new EdgeInspectionPoint { Row = int2Row, Column = int2Col }
           }
       });

       return true;
       ```

    **주석 규칙:**
    - 신규/재작성 라인 끝에 `//260523 hbk Phase 32 — E3 교점 기반 알고리즘` (Part B)
    - revert(Long→Short 이름/리터럴 환원) 라인 끝에 `//260523 hbk Phase 32 — E3 단축 환원` (Part A)
    - 한 라인이 양쪽 해당 시 단축 환원 주석 1개로 통합 가능
    - 기존 Phase 32-05 (`//260521 hbk Phase 32 E3`) 주석은 핵심 라인(필드 선언 등) 보존 가능. 단 본 파일은 거의 전면 재작성이므로 executor 가 가독성 기준으로 결정 (구식 32 UAT 주석은 제거 권장)

    **Critical 확인사항:**
    - IDatumOriginConsumer 미구현 유지 (단축은 Datum 비의존)
    - DatumOrigin*/DatumAngle* 필드 추가 금지
    - `using System;` 추가 필요 (Math.Sin/Cos/Sqrt/PI). 기존 코드는 `System.Math.Max` 로 fully-qualified 사용 중이므로 `using System;` 없이도 `System.Math.*` 로 작성해도 OK — 일관성 선호 (기존 패턴 유지).
    - 결과값 수치 동등성: distance(int1, int2) = 2*length2 mathematically. 부동소수점 노이즈 < 1e-9 px, 측정 결과(소수점 3자리 mm) 영향 0.
  </action>
  <verify>
    <automated>powershell -Command "$msb=Get-ChildItem 'C:\Program Files (x86)\Microsoft Visual Studio\*\*\MSBuild\*\Bin\MSBuild.exe' -Recurse -ErrorAction SilentlyContinue | Select-Object -First 1 -ExpandProperty FullName; if ($msb) { & $msb 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' /t:Build /p:Configuration=Debug /p:Platform=x64 /v:minimal } else { Write-Host 'MSBuild not found — fallback dotnet build'; dotnet build 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' -c Debug }"</automated>
  </verify>
  <done>
    - CompoundShortAxisDistanceMeasurement.cs 단일 파일이 존재하고 CompoundLongAxisDistanceMeasurement.cs 는 없음 (git mv 완료)
    - 클래스명/생성자명/TypeName = `CompoundShortAxisDistance`
    - TryExecute 본문이 TryFindLargestContourRect → 긴변 2개 직선 계산 → 측정선 직선 계산 → TryIntersectLines × 2 → 교점 거리 결과 → 6개 오버레이 (FAI-LongEdge1/2, FAI-MeasureLine, FAI-Intersection1/2, FAI-DistLine) 추가 흐름
    - IDatumOriginConsumer 미구현 유지
    - 변경/추가 라인 모두 `//260523 hbk Phase 32 — E3 …` 주석 부착
    - 빌드 차단 issue 본 task 단독으로는 발생할 수 있음 (Task 2 wiring revert 전이라 csproj/Factory/UI 미동기) — Task 2 까지 완료해야 PASS 기대
  </done>
</task>

<task type="auto">
  <name>Task 2: Wiring 5파일 Long→Short revert (csproj + Factory + MainView ×3 + InspectionListView ×1)</name>
  <files>
    WPF_Example/DatumMeasurement.csproj,
    WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs,
    WPF_Example/UI/ContentItem/MainView.xaml.cs,
    WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  </files>
  <action>
    Task 1 의 git mv 와 클래스 리네임에 맞춰 4개 wiring 파일의 type 참조를 모두 `CompoundShortAxisDistance` / `CompoundShortAxisDistanceMeasurement` 로 환원.

    **(A) WPF_Example/DatumMeasurement.csproj L234:**
    ```xml
    <Compile Include="Custom\Sequence\Inspection\Measurements\CompoundShortAxisDistanceMeasurement.cs" /> <!--260523 hbk Phase 32 — E3 단축 환원-->
    ```
    - 경로의 파일명만 `CompoundLong…` → `CompoundShort…` 변경. 주석 갱신.

    **(B) WPF_Example/Custom/Sequence/Inspection/MeasurementFactory.cs L44~45 + L69:**
    ```csharp
    // L44~45 (switch case)
    case "CompoundShortAxisDistance": //260523 hbk Phase 32 — E3 단축 환원
        return new CompoundShortAxisDistanceMeasurement(owner); //260523 hbk Phase 32 — E3 단축 환원
    // L69 (GetTypeNames 15번째 원소)
    "CompoundShortAxisDistance" //260523 hbk Phase 32 — E3 단축 환원
    ```

    **(C) WPF_Example/UI/ContentItem/MainView.xaml.cs (3 spots):**
    - L295: `var cShort = m as CompoundShortAxisDistanceMeasurement; //260523 hbk Phase 32 — E3 단축 환원`
    - L1397: `var cShort = _editingMeasurement as CompoundShortAxisDistanceMeasurement; //260523 hbk Phase 32 — E3 단축 환원`
    - L1578: `if (selParam is CompoundShortAxisDistanceMeasurement) return (MeasurementBase)selParam; //260523 hbk Phase 32 — E3 단축 환원`
    - L1593: `if (m is CompoundShortAxisDistanceMeasurement) return m; //260523 hbk Phase 32 — E3 단축 환원`

    **(D) WPF_Example/UI/ControlItem/InspectionListView.xaml.cs L473:**
    ```csharp
    || item.Param is CompoundShortAxisDistanceMeasurement; //260523 hbk Phase 32 — E3 단축 환원
    ```

    **검증 grep 후 점검:**
    - `CompoundLongAxisDistance` 키워드 검색 시 hit 0 건 (단, 이 PLAN.md 자신 + summary 등 .planning/* 는 제외)
    - `CompoundShortAxisDistance` 키워드 검색 시 hit 7 곳 이상 (Measurement.cs 본문 + csproj + Factory ×2 + MainView ×4 + InspectionListView ×1 정도)

    **Critical:** L295 의 `cShort` 변수명도 유지 (revert 직전 phase 32-05 가 변수명을 그대로 두었음 — 이미 cShort 였다. Long 으로 한 번도 변경 안 됨). 동일 적용 L1397.

    **빌드 차단 점검 (Rule 1/2/3):**
    - 모든 type 참조가 Task 1 의 새 클래스명과 일치하는지 확인 → CS0246 (type not found) 차단
    - csproj Compile Include 경로가 git mv 후 실제 파일 경로와 일치하는지 확인 → MSB3027 / 컴파일 누락 차단
  </action>
  <verify>
    <automated>powershell -Command "$msb=Get-ChildItem 'C:\Program Files (x86)\Microsoft Visual Studio\*\*\MSBuild\*\Bin\MSBuild.exe' -Recurse -ErrorAction SilentlyContinue | Select-Object -First 1 -ExpandProperty FullName; if ($msb) { & $msb 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /v:minimal } else { dotnet build 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' -c Debug }"</automated>
  </verify>
  <done>
    - msbuild Debug/x64 Rebuild PASS — 0 errors, 신규 warnings 0 (baseline 대비)
    - `grep -r "CompoundLongAxisDistance" WPF_Example/` 결과 0 건
    - `grep -r "CompoundShortAxisDistance" WPF_Example/` 결과 csproj 1 + Factory 2 + Measurement.cs 3+ + MainView 4 + InspectionListView 1 = 11 건 이상
    - 모든 변경 라인에 `//260523 hbk Phase 32 — E3 단축 환원` 주석
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SIMUL UAT — E3 단축 거리 + 교점 오버레이 시각 검증</name>
  <what-built>
    - E3 (CompoundShortAxisDistance) 단축 거리 측정 환원 (장축 폭 → 짧은 변 폭)
    - 측정 알고리즘 = 사각형 긴변 2개 ↔ 중심 지나는 측정선의 명시 교점 2개 거리
    - 6개 오버레이 시각화 (긴변 2개 + 측정선 + 교점 2개 + 거리선)
    - Datum 비의존 유지
  </what-built>
  <how-to-verify>
    1. 앱 종료 → 재빌드 (Debug/x64) → SIMUL_MODE 실행
    2. 기존 Phase 32 UAT 레시피 로드 — E3 (CompoundShortAxisDistance) 측정 항목이 있는 FAI 선택
    3. **PropertyGrid 확인:**
       - Type 콤보박스에 `CompoundShortAxisDistance` 항목이 존재
       - Type = CompoundShortAxisDistance 선택 시 PropertyGrid 에 Rect ROI 5개 + Contour 4개 파라미터(Canny/Union) 만 노출 (Datum* 필드 없음 = IDatumOriginConsumer 미구현 확인)
    4. **측정 실행:**
       - Inspection Run 또는 Test Run 으로 E3 측정 트리거
       - 결과값 = 짧은 변 폭(mm) — Phase 32-05 commit 직전 (commit 2cf04e8 또는 기록 참조) 의 단축 결과값과 일치할 것
       - 공차 0.600±0.030 기준 판정 정상
    5. **오버레이 시각 확인:**
       - 사각형의 긴 변 2개 (서로 평행, 길이 = 2×length1) 가 라인으로 표시 (RoiId = FAI-LongEdge1 / FAI-LongEdge2)
       - 사각형 중심을 지나는 짧은 방향(긴변에 수직) 측정선 1개 표시 (RoiId = FAI-MeasureLine)
       - 긴변과 측정선의 두 교차점에 점 마커 2개 (RoiId = FAI-Intersection1 / FAI-Intersection2)
       - 두 교점을 잇는 거리선 (RoiId = FAI-DistLine) 표시 — 이 거리선의 길이가 결과값 mm 의 시각적 표현
    6. **실패 케이스:**
       - Rect ROI 를 빈 영역으로 이동 → Run → measurement 결과 '—' + overlay 미표시 + 앱 무크래시
    7. **데이터값 검증:**
       - 동일 이미지에서 Phase 32-05 (장축, commit 3343250 직후) 의 결과값과 비교 — 본 revert 후에는 단축 (≈ 0.6mm 근처) 으로 돌아왔는지 확인
  </how-to-verify>
  <resume-signal>Type "approved" 또는 발견된 이슈/회귀/오버레이 결함을 설명</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HALCON 호출 → 검사 스레드 | HOperatorSet 예외가 시퀀스 스레드 크래시로 전파 가능 |
| INI(Recipe) 직렬화 ↔ class TypeName | type 이름 변경 시 기존 레시피 호환성 — 본 task 는 commit 3343250 의 변경을 되돌리는 것이므로 그 commit 이전 레시피와는 호환, 그 commit 이후 잠시 저장된 레시피만 incompatible (Phase 32-05 진행 중 저장된 임시 레시피로 한정 — 운영 영향 없음) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-J72-01 | Tampering | TryIntersectLines 평행/근접 케이스 | mitigate | 평행 시 false 반환 (기존 동작) — measurement TryExecute 도 false 반환 + error 문자열 + overlay 빈 리스트 (ArcLineIntersect L232/243 패턴 차용) |
| T-J72-02 | Denial of Service | HALCON TryFindLargestContourRect 예외 (canny/union 실패) | mitigate | TryFindLargestContourRect 내부 catch(Exception) 가 이미 false 반환 — TryExecute 가 그 false 를 그대로 위임 (return false). 시퀀스 스레드 무크래시 |
| T-J72-03 | Information Disclosure | INI 레시피 backward-compat 미확보 | accept | Phase 32-05 와 본 revert 사이에 저장된 레시피만 영향 — UAT 진행 전이고, 운영 환경 미배포. v1.1 milestone close 전이므로 영향 0 |
| T-J72-04 | Repudiation | 결과값이 Phase 32-05 commit 3343250 이전과 동등함을 증명할 수 없음 | mitigate | 수학적 등가 증명 in-action: `distance(int1,int2) = 2 × length2` (긴변 평행 + 측정선이 중심+수직 통과 → 교점이 중심으로부터 단축 방향 ±length2 위치). 부동소수점 오차는 절대치 < 1e-9 px |
| T-J72-05 | Elevation of Privilege | overlay 추가로 인한 HALCON 메모리 누수 | accept | overlay 생성은 EdgeInspectionOverlay POCO + List 추가만 — HALCON HObject 미사용. HALCON 핸들 0개, 누수 경로 없음 |
</threat_model>

<verification>
- Task 1 단독 빌드는 실패 가능 (Task 2 wiring revert 미진행 시 CS0246) — Task 2 와 묶어서 atomic 확인
- Task 2 완료 후 msbuild Debug/x64 Rebuild PASS 가 핵심 자동 확인 지표
- Task 3 (SIMUL UAT) 는 사용자 시각 검증 — overlay 6개 + 결과값 단축 환원 + 실패 케이스 무크래시

추가 정적 검증 (executor 가 Task 2 완료 후 수행 가능):
- `grep -r "CompoundLongAxis" WPF_Example/` 결과 0 건 (백업 .cs 파일 제외)
- `grep -r "CompoundShortAxis" WPF_Example/` 결과 11 건 이상
- `grep -r "IDatumOriginConsumer" WPF_Example/Custom/Sequence/Inspection/Measurements/CompoundShortAxisDistanceMeasurement.cs` 결과 0 건 (Datum 비의존 보존)
</verification>

<success_criteria>
- E3 TypeName, 클래스명, 파일명, csproj Compile Include, MeasurementFactory case/GetTypeNames, MainView 3 spots, InspectionListView 1 spot — 모두 `CompoundShortAxisDistance` / `CompoundShortAxisDistanceMeasurement` 로 환원 (commit 3343250 변경 100% 되돌림)
- TryExecute 알고리즘이 명시 교점 기반 (TryFindLargestContourRect → 긴변 2개 라인 좌표 계산 → 중심 지나는 측정선 좌표 → TryIntersectLines × 2 → 교점 간 euclidean 거리) 로 재작성
- 결과값이 revert 직후 `2 × min(length1, length2) × pixelResolution` 와 수학적으로 byte-identical (부동소수점 오차 ≤ 1e-9 px, 측정 mm 영향 0)
- 6개 오버레이 (FAI-LongEdge1, FAI-LongEdge2, FAI-MeasureLine, FAI-Intersection1, FAI-Intersection2, FAI-DistLine) 가 ADDITIVE 로 생성됨
- IDatumOriginConsumer 미구현 유지 (Datum 비의존 보존)
- msbuild Debug/x64 Rebuild PASS, 신규 errors/warnings 0
- 모든 신규/수정 라인에 `//260523 hbk Phase 32 — E3 …` 주석 부착
- SIMUL UAT 사용자 "approved"
</success_criteria>

<output>
After completion, create `.planning/quick/260523-j72-e3-revert/260523-j72-SUMMARY.md` 에:
- Part A revert 변경점 리스트 (commit 3343250 대비)
- Part B 신규 알고리즘 흐름 + 수학적 등가 증명
- 빌드 결과
- UAT 결과 (사용자 approved 시점 + 확인된 오버레이/결과값)
- 차후 carry-over (있다면)
</output>
