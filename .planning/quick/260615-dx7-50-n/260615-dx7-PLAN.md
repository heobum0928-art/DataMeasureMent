---
quick_id: 260615-dx7
type: quick
date: 2026-06-15
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml
  - WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs
requirements:
  - OUT-03
---

<objective>
Phase 41.1 반복 검사의 입력 소스를 "고정 이미지 50회 반복" → "이미지 폴더 내 N장을 1장씩 순회 검사"로 변경한다.

UAT 중 발견된 요구 변경: 자재를 N번 촬영(이미지 N장)했으면 그만큼 N회 검사해야 한다.
- 1 사이클 = 이미지 1장 (단일 Shot)
- 횟수 = 폴더 내 이미지 개수 (가변, 50 고정 폐기)
- 통계(RepeatMeasurementStats) / xlsx export(RepeatExcelExportService)는 변경 없음 — 입력 소스만 교체
</objective>

<context>
현재 구조 (Phase 41.1):
- RepeatRunService.Start(seq, count) → SimulImagePath 고정한 채 StartAll 을 count 회 반복
- 검사 사이클마다 Action_FAIMeasurement 가 ShotConfig.SimulImagePath 의 이미지를 로드 (Action_FAIMeasurement.cs:154)
- ShotConfig.SimulImagePath 는 settable (ShotConfig.cs:17)
- RecipeManager 접근: SystemHandler.Handle.Sequences.RecipeManager.Shots (SequenceHandler.cs:34)

변경 후:
- 매 사이클 StartAll 직전에 recipeManager.Shots 의 SimulImagePath 를 imagePaths[CompletedCount] 로 교체
- CompletedCount 가 이미지 인덱스와 자동 동기화 (Start 시 0, 매 완료마다 +1)
- _imagePaths == null 이면 기존 고정 동작 유지 (하위호환 — 기존 Start 경로 무손상)
</context>

<tasks>

<task>
  <name>Task 1: RepeatRunService — StartFromImages + 매 사이클 SimulImagePath 교체</name>
  <files>WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs</files>
  <action>
1. 필드 추가: `private List<string> _imagePaths;` (null = 고정모드)
2. `StartFromImages(InspectionSequence seq, List<string> imagePaths)` 신규:
   - IsRunning / seq null / imagePaths null·empty 가드
   - TargetCount = imagePaths.Count, _imagePaths = imagePaths, 나머지는 Start 와 동일 초기화
   - OnFinish 구독 후 TriggerNext
3. `ApplyCurrentImage()` private 메서드: _imagePaths != null 이고 idx(=CompletedCount) 유효 시
   recipeManager.Shots 전체의 SimulImagePath 를 imagePaths[idx] 로 set
4. TriggerNext 의 `_seq.StartAll(null)` 호출 직전에 ApplyCurrentImage() 삽입
5. Stop() 에서 _imagePaths = null 로 정리
   //260615 hbk Quick 260615-dx7 마커
  </action>
  <verify>grep -n "StartFromImages\|ApplyCurrentImage\|_imagePaths" RepeatRunService.cs</verify>
  <done>StartFromImages + ApplyCurrentImage 존재, TriggerNext 가 StartAll 전 ApplyCurrentImage 호출</done>
</task>

<task>
  <name>Task 2: ReviewerWindow.xaml — 버튼 텍스트 변경</name>
  <files>WPF_Example/UI/Reviewer/ReviewerWindow.xaml</files>
  <action>btn_repeatRun Content "50회 반복 실행" → "이미지 폴더 반복 검사"</action>
  <verify>grep -n "이미지 폴더 반복 검사" ReviewerWindow.xaml</verify>
  <done>버튼 텍스트 변경</done>
</task>

<task>
  <name>Task 3: ReviewerWindow.xaml.cs — 폴더 다이얼로그 + 이미지 열거 + StartFromImages</name>
  <files>WPF_Example/UI/Reviewer/ReviewerWindow.xaml.cs</files>
  <action>
Button_RepeatRun_Click 재작성:
- 실행 중이면 Stop → 레이블/버튼 텍스트 복원 (토글 유지)
- Ookii VistaFolderBrowserDialog (Button_LoadFolder_Click 패턴) → 폴더 선택
- Directory.GetFiles 에서 .bmp/.jpg/.jpeg/.png/.tif/.tiff 만 OrdinalIgnoreCase 정렬 수집
- 이미지 0장이면 경고 후 반환
- activeSeq 탐색 (기존 로직 유지)
- 진행 레이블 "진행 중: 0/<count>", 버튼 "중단"
- _repeatService.StartFromImages(activeSeq, imagePaths)
- OnRepeatComplete 시 버튼 텍스트 "이미지 폴더 반복 검사" 로 복원
- 문자열 "50회 반복 실행" 잔재 모두 "이미지 폴더 반복 검사" 로 교체
  //260615 hbk Quick 260615-dx7 마커
  </action>
  <verify>grep -n "StartFromImages\|VistaFolderBrowserDialog\|이미지 폴더 반복 검사" ReviewerWindow.xaml.cs</verify>
  <done>폴더 다이얼로그 → 이미지 수집 → StartFromImages 호출, 50회 잔재 제거</done>
</task>

<task>
  <name>Task 4: 빌드 검증</name>
  <action>msbuild Debug/x64 → 0 errors</action>
  <verify>msbuild 출력 0 errors</verify>
  <done>빌드 성공</done>
</task>

</tasks>

<success_criteria>
- RepeatRunService.StartFromImages 가 이미지 목록 길이만큼 사이클 실행, 매 사이클 다른 이미지 로드
- 기존 Start(고정모드) 무손상 (_imagePaths null 분기)
- ReviewerWindow 버튼 → 폴더 선택 → N장 검사
- msbuild Debug/x64 0 errors
- SIMUL UAT: 폴더 선택 후 이미지 개수만큼 검사 + 2시트 xlsx 정상
</success_criteria>
