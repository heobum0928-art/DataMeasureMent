---
phase: quick-260623-jd6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs]
autonomous: false
requirements: [QUAL-01]
must_haves:
  truths:
    - "매직넘버 리터럴이 의미있는 private const로 추출되되 값은 동일(동치)하다"
    - "공개 API/HALCON 호출/이벤트핸들러/try-catch/상태플래그 라인은 diff에 등장하지 않는다"
    - "신규 const는 파일 기존 스타일(PascalCase)을 따른다"
    - "msbuild Debug/x64 빌드가 0 errors 로 통과한다"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs"
      provides: "매직넘버가 const화된 HalconViewer (기능 동치)"
      contains: "private const double"
---

<objective>
HalconViewerControl.xaml.cs (1428줄)의 산재된 매직넘버를 의미있는 private const로 추출한다 (CONVENTIONS.md §5). QUAL-01 안전(LOW) 범위 — 값 변경 0, 회귀 위험 0, 빌드로 검증.
</objective>

<context>
@.planning/CONVENTIONS.md
@WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs

<constraints>
## 절대 변경 금지
- public 메서드/프로퍼티/이벤트 시그니처 (LoadImage/SetRois/Render/Dispose 등 — MainView/Action/Teaching/Reviewer 가 소비)
- HOperatorSet.* / ViewerHost.HalconWindow.* 직접 호출 (드로잉객체 생명주기, GetPart/SetPart/Attach)
- 이벤트 핸들러 로직, try-catch 구조, 상태 플래그(_isXxx)
- 기존 const(ZoomInScaleFactor 등 line 19-24) 개명 금지 — churn

## 코드 스타일
- 신규 const = 파일 기존 스타일 PascalCase (UPPER_SNAKE 아님 — 파일 내 일관성 우선)
- 매직넘버 const는 클래스 상단 기존 const 블록(line 19-24) 바로 아래에 모아 선언
- C# 7.2, 기존 brace 스타일 유지
- 값은 한 글자도 바꾸지 말 것 (1.0→1.0, 20.0→20.0 동일)

## 안전 원칙
의심스러우면 변경하지 말 것. const화는 "리터럴→이름" 1:1 치환만.
같은 값이라도 의미가 다른 곳(예: 줌의 1.0 vs 최소크기 1.0)은 별도 const 또는 그대로 둘 것 — 잘못 묶지 말 것.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 매직넘버 const 추출</name>
  <files>WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs</files>
  <action>
파일을 정독해 아래 매직넘버 후보의 정확한 라인/문맥을 확인한 뒤, 의미가 명확한 것만 const화한다. 라인 번호는 분석 시점 기준 근사값이니 실제 코드로 재확인할 것.

추출 권장 const (클래스 상단 기존 const 블록 아래에 선언):
- `private const double MinDraftRoiSize = 20.0;`  — UpdateDraftRoi 의 ROI 최소 크기 (≈ line 691, 696)
- `private const double DraftDefaultHalfSize = 60.0;` — CreateDefaultDraftRoi 기본 반경 (≈ line 1021)
- `private const double CornerHitThreshold = 10.0;` — GetDraftRoiHitType 코너 히트 범위 (≈ line 1192)
- `private const double PanMarginScale = 0.75;` — SetImagePart 팬 마진 비율 (≈ line 856-857)
- `private const double RoiClickTolerancePixels = 3.0;` — HMouseUp 의 `moved <= 3.0` ROI 클릭 판정 (≈ line 520). 단 이 라인이 이벤트 핸들러 "로직"이 아니라 단순 임계 리터럴인지 확인 후, 리터럴 치환만 수행(분기 구조는 불변).

판단 규칙:
1. `1.0` 은 문맥이 제각각(정규화/최소크기/스케일 등 9곳)이라 잘못 묶을 위험 큼 → **이번엔 건드리지 말 것**(보류).
2. 이미 지역 const 가 있는 값(minSize 5.0 등)은 그대로 둘 것.
3. 같은 리터럴이 다른 의미로 쓰이면 묶지 말고 의미별 const 또는 보류.
4. HALCON 버튼 코드(1/4)는 이미 HalconLeftButton/HalconRightButton const 존재 — 누락 사용처가 있으면 기존 const 로 치환만.

각 매직넘버를 const 로 치환할 때 값이 정확히 동일한지 재확인. 분기/계산식 구조는 절대 변경하지 말 것.
  </action>
  <verify>
  <automated>grep -n "MinDraftRoiSize\|DraftDefaultHalfSize\|CornerHitThreshold\|PanMarginScale" WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs</automated>
  </verify>
  <done>
  - 신규 const 4개 이상 선언, 해당 매직넘버 리터럴이 사용처에서 const 참조로 치환됨
  - 값 동일, 분기/계산 구조 불변
  - 공개 API/HALCON/이벤트/try-catch diff 미등장
  </done>
</task>

<task type="auto">
  <name>Task 2: 헤더 주석 + 빌드 검증</name>
  <files>WPF_Example/UI/ContentItem/HalconViewerControl.xaml.cs</files>
  <action>
1. 클래스 신규 const 블록 위에 변경 요약 주석 1줄:
   `//260623 hbk: CONVENTIONS §5 — 매직넘버 const화 (값 동일, 동작 불변)`
2. 빌드 검증 (0 errors). MSBuild Debug/x64:
   `"/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj //p:Configuration=Debug //p:Platform=x64 //nologo //verbosity:minimal`
   (없으면 `where MSBuild` 로 탐색)
   - 빌드 실패 시 직전 변경 수정(오타/접근자). 절대 변경 금지 목록은 손대지 말 것.
   - exe 파일 잠금(MSB3027/3021)으로 copy 단계만 실패하고 컴파일은 0 errors 면 PASS 간주(앱 실행 중 가능).
  </action>
  <verify>
  <automated>cd /c/Info/Project/DataMeasurement && "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj //p:Configuration=Debug //p:Platform=x64 //nologo //verbosity:minimal 2>&1 | grep -iE "error|Build succeeded|0 Error"</automated>
  </verify>
  <done>
  - 컴파일 0 errors
  - 헤더 주석 1줄 존재
  </done>
</task>

</tasks>

<success_criteria>
- 매직넘버(20.0/60.0/10.0/0.75/3.0 등 의미 명확한 것) → 의미있는 PascalCase private const
- 값·분기·계산 구조 100% 동일 (기능 동치)
- 공개 API/HALCON/이벤트/try-catch/상태플래그 불변
- msbuild Debug/x64 0 errors
- 1.0 등 다의적 리터럴은 보류(잘못된 묶음 방지)
</success_criteria>

<output>
After completion, create `.planning/quick/260623-jd6-halconviewer-magic-const/260623-jd6-SUMMARY.md`.
</output>
