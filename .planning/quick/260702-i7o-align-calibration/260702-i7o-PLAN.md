---
phase: quick-260702-i7o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/UI/BottomVisionView.xaml.cs
  - WPF_Example/Custom/UI/TrayVisionView.xaml.cs
autonomous: true
requirements: [QUICK-260702-i7o]

must_haves:
  truths:
    - "Bottom Align 캘 '초기화' 버튼을 누르면 삭제 확인(예/아니오) 대화상자가 먼저 뜬다"
    - "확인 대화상자에서 '아니오'를 누르면 아무 삭제/초기화가 일어나지 않는다 (누적/시각화 그대로)"
    - "Tray Align 검사 버튼을 모델 없는 상태에서 누르면 '모델이 없습니다' 안내가 뜨고 검사가 실행되지 않는다"
    - "Bottom Align 검사 버튼을 (선택 슬롯 기준) 모델 없는 상태에서 누르면 '모델이 없습니다' 안내가 뜨고 검사가 실행되지 않는다"
  artifacts:
    - path: "WPF_Example/Custom/UI/BottomVisionView.xaml.cs"
      provides: "CalResetButton_Click 삭제 확인 + RunButton_Click 모델 존재 가드"
      contains: "CustomMessageBox.ShowConfirmation"
    - path: "WPF_Example/Custom/UI/TrayVisionView.xaml.cs"
      provides: "RunButton_Click 모델 존재 가드"
      contains: "HasTemplate"
  key_links:
    - from: "BottomVisionView.CalResetButton_Click"
      to: "CustomMessageBox.ShowConfirmation"
      via: "Yes/No 확인 후에만 PickerCal.Reset() 호출"
      pattern: "ShowConfirmation.*YesNo"
    - from: "TrayVisionView.RunButton_Click / BottomVisionView.RunButton_Click"
      to: "Matcher.HasTemplate"
      via: "Run 호출 전 모델 존재 확인, 없으면 CustomMessageBox.Show 후 return"
      pattern: "HasTemplate"
---

<objective>
Tray/Bottom Align 비전 UI 에 2개의 안전 가드를 추가한다.

1. Bottom Align 캘리브레이션 "초기화" 버튼 — 실수로 누적/캘 데이터를 날리지 않도록 삭제 전에 예/아니오 확인 대화상자를 띄우고, "아니오" 시 아무 부수효과 없이 중단.
2. Tray/Bottom Align "검사" 버튼 — 티칭 모델이 없는 상태에서 검사를 실행하면 예외/무의미 검색이 발생하므로, 검사 호출 전에 모델 존재를 확인하고 없으면 "모델이 없습니다" 안내 후 검사 중단.

Purpose: 작업자가 캘 데이터를 실수로 삭제하거나, 모델 없이 검사를 눌러 혼란스러운 실패 메시지를 보는 것을 방지.
Output: BottomVisionView.xaml.cs, TrayVisionView.xaml.cs 두 코드비하인드 수정. 기존 CustomMessageBox 재사용 — 신규 의존성 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
프로젝트 코딩 규칙 (반드시 준수):
- 삼항 연산자(`?:`) 금지 → 반드시 if-else 사용
- 수정한 각 라인/블록에 `//260702 hbk` 주석 (오늘 날짜)
- C# 7.2 문법만 (switch expression, record, nullable ref 금지)
- 기존 파일의 브레이스 스타일 유지 (이 두 파일은 K&R — 여는 브레이스 같은 줄)
- 신규 의존성/다이얼로그 도입 금지 — 반드시 기존 `CustomMessageBox` 재사용
- 범위: Tray Align / Bottom Align UI 만. Top/Side/Bottom FAI 검사 시퀀스는 건드리지 말 것.

<interfaces>
<!-- 실행자가 그대로 사용할 계약. 코드베이스 재탐색 불필요. -->

CustomMessageBox (namespace ReringProject.UI — 두 파일 모두 `using ReringProject.UI;` 이미 있음):
```csharp
// 동기(synchronous) 확인 대화상자 — 결과를 즉시 반환. 확인 다이얼로그용.
public static MessageBoxResult ShowConfirmation(string title, string message, MessageBoxButton buttons);

// 정보/경고 메시지 — 안내용. (내부 BeginInvoke 비동기 표시, 반환값 무시 가능)
public static bool Show(string title, string message, MessageBoxImage imageType = MessageBoxImage.Information,
                        bool isModal = true, bool isAutoClosing = true, int autoClosingTime = ...);
```
MessageBoxResult / MessageBoxButton / MessageBoxImage 는 System.Windows (두 파일 모두 `using System.Windows;` 이미 있음).

AlignShapeMatchService (EthernetVisionHandler.Handle.Matcher):
```csharp
// 슬롯 기본값 None. mode==None 이면 false 반환. 내부에서 모델 파일/캐시 존재 확인.
public bool HasTemplate(EEthernetVisionMode mode, EBottomAlignSlot slot = EBottomAlignSlot.None);
```

PickerCenterCalibrationService (EthernetVisionHandler.Handle.PickerCal):
```csharp
public void Reset();   // 누적 스텝 + 시각화 XLD 클리어 (모델 파일 자체는 유지)
```
</interfaces>

@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bottom 캘 "초기화" 삭제 확인 대화상자 추가</name>
  <files>WPF_Example/Custom/UI/BottomVisionView.xaml.cs</files>
  <action>
`CalResetButton_Click` (약 587行) 수정. 현재 이 핸들러는 PickerCal null 가드 후 `PickerCal.Reset()`(누적/시각화 초기화)를 호출한다. 작업자가 실수로 캘 데이터를 날리지 않도록, PickerCal null 가드 통과 직후·`try` 블록 진입 전에 예/아니오 확인 대화상자를 추가한다.

기존 `if (EthernetVisionHandler.Handle.PickerCal == null) { ... return; }` 블록 바로 아래에 삽입:

```csharp
//260702 hbk 캘 데이터 삭제(누적/시각화 초기화) 전 실수 방지 확인
MessageBoxResult confirmReset = CustomMessageBox.ShowConfirmation(
    "캘 초기화", "캘리브레이션 모델/누적 데이터를 삭제하시겠습니까?", MessageBoxButton.YesNo);
if (confirmReset != MessageBoxResult.Yes) {
    lbl_calStatus.Text = "초기화 취소"; //260702 hbk 사용자가 아니오 선택 — 부수효과 없이 중단
    return;
}
```

규칙: 삼항 금지(위처럼 if-else/조기 return), 수정 라인에 `//260702 hbk` 주석, 기존 K&R 브레이스 유지. `Reset()` 호출 자체나 그 의미(모델 파일 유지)는 변경하지 말 것 — 확인 게이트만 추가한다. "아니오" 경로에서는 Reset()·라벨 클리어·뷰어 클리어 등 어떤 부수효과도 실행되면 안 된다(그냥 return).
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal</automated>
  </verify>
  <done>Bottom "초기화" 버튼 클릭 시 예/아니오 대화상자가 먼저 뜨고, "아니오" 선택 시 Reset()이 호출되지 않으며(누적/시각화 유지), lbl_calStatus 가 "초기화 취소" 로 표시된다. "예" 선택 시 기존 초기화 로직이 그대로 실행된다. Debug/x64 빌드 통과.</done>
</task>

<task type="auto">
  <name>Task 2: Tray/Bottom "검사" 버튼 모델 존재 가드 추가</name>
  <files>WPF_Example/Custom/UI/TrayVisionView.xaml.cs, WPF_Example/Custom/UI/BottomVisionView.xaml.cs</files>
  <action>
두 뷰의 `RunButton_Click` (검사 버튼 핸들러)에서, 기존 이미지 null 가드(`if (_viewer == null || _viewer.CurrentImage == null) { ...; return; }`) 통과 직후·`try` 블록 진입 전에 모델 존재 확인을 추가한다. 모델이 없으면 `CustomMessageBox.Show` 로 "모델이 없습니다" 안내 후 검사(Matcher.Run)를 실행하지 않고 return.

HasTemplate 호출은 기존 RefreshStatus 패턴대로 try-catch 로 감싸 예외 시 안전하게 차단(false 취급)한다.

**TrayVisionView.xaml.cs `RunButton_Click` (약 253行)** — 단일 경로(슬롯 인자 없음):
```csharp
//260702 hbk 모델 미티칭 상태에서 검사 방지 — 안내 후 중단
bool bHasModel = false; //260702 hbk 기본 false: 예외/미초기화 시 검사 차단
try {
    bHasModel = EthernetVisionHandler.Handle.Matcher.HasTemplate(VIEW_MODE); //260702 hbk Tray 단일 경로 템플릿 확인
}
catch {
    bHasModel = false; //260702 hbk Matcher 예외 시 안전 차단
}
if (!bHasModel) {
    CustomMessageBox.Show("검사 불가", "모델이 없습니다. 먼저 티칭을 완료하세요.", MessageBoxImage.Warning); //260702 hbk 안내
    lbl_status.Text = "모델 없음"; //260702 hbk 상태 라벨 반영
    return;
}
```

**BottomVisionView.xaml.cs `RunButton_Click` (약 433行)** — 선택 슬롯 기준(`_selectedSlot`, None=단일경로 폴백):
```csharp
//260702 hbk 모델 미티칭 상태에서 검사 방지 — 안내 후 중단(선택 슬롯 기준)
bool bHasModel = false; //260702 hbk 기본 false: 예외/미초기화 시 검사 차단
try {
    bHasModel = EthernetVisionHandler.Handle.Matcher.HasTemplate(VIEW_MODE, _selectedSlot); //260702 hbk 선택 슬롯(None=단일) 템플릿 확인
}
catch {
    bHasModel = false; //260702 hbk Matcher 예외 시 안전 차단
}
if (!bHasModel) {
    CustomMessageBox.Show("검사 불가", "모델이 없습니다. 먼저 티칭을 완료하세요.", MessageBoxImage.Warning); //260702 hbk 안내
    lbl_status.Text = "모델 없음"; //260702 hbk 상태 라벨 반영
    return;
}
```

규칙: 삼항 금지(if-else 사용), 수정 라인에 `//260702 hbk` 주석, 기존 K&R 브레이스 유지. 삽입 위치는 반드시 이미지 null 가드 이후·기존 `try` 이전(모델이 없으면 ApplyCoaxLight/Matcher.Run 이 실행되지 않아야 함). Top/Side/Bottom FAI 검사 시퀀스 코드는 건드리지 말 것.
  </action>
  <verify>
    <automated>msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal</automated>
  </verify>
  <done>Tray/Bottom 검사 버튼을 모델 없는 상태에서 누르면 "모델이 없습니다" 경고 다이얼로그가 뜨고 Matcher.Run 이 호출되지 않으며 lbl_status 가 "모델 없음"으로 표시된다. 모델이 있으면 기존 검사 흐름이 그대로 동작한다(회귀 0). Debug/x64 빌드 통과.</done>
</task>

</tasks>

<verification>
- Debug/x64 통합 빌드 PASS (msbuild).
- 코드 리뷰: 삼항 연산자 미사용, `//260702 hbk` 주석 존재, 기존 K&R 브레이스 스타일 유지, 신규 using/의존성 없음.
- 회귀: "예" 확인 시 초기화 정상, 모델 존재 시 검사 정상 — 기존 흐름 무변경.
</verification>

<success_criteria>
- Bottom 캘 "초기화" → 예/아니오 확인, "아니오"=부수효과 0.
- Tray/Bottom "검사" → 모델 없으면 안내 후 중단, 모델 있으면 기존 검사 동작.
- 두 파일 모두 CustomMessageBox 재사용, 신규 의존성 0.
- 빌드 PASS.
</success_criteria>

<output>
After completion, create `.planning/quick/260702-i7o-align-calibration/260702-i7o-SUMMARY.md`
</output>
