---
phase: quick-260702-ja4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/UI/TrayVisionView.xaml.cs
  - WPF_Example/Custom/UI/BottomVisionView.xaml.cs
autonomous: true
requirements: [QUICK-260702-ja4]
must_haves:
  truths:
    - "기존 ROI(_roi1)가 있을 때 'ROI 1 그리기'를 누르면 삭제 확인 대화상자가 뜬다"
    - "기존 ROI(_roi2)가 있을 때 'ROI 2 그리기'를 누르면 삭제 확인 대화상자가 뜬다"
    - "확인에서 '아니오'를 선택하면 기존 ROI가 초기화되지 않고 드로잉도 시작되지 않는다"
    - "ROI가 아직 없으면(최초 티칭) 확인 없이 바로 기존 로직대로 그리기를 시작한다"
    - "Tray/Bottom 두 화면의 ROI1/ROI2 핸들러 4개 모두 동일 패턴이 적용된다"
  artifacts:
    - path: "WPF_Example/Custom/UI/TrayVisionView.xaml.cs"
      provides: "DrawRoi1Button_Click / DrawRoi2Button_Click 재드로잉 확인 가드"
    - path: "WPF_Example/Custom/UI/BottomVisionView.xaml.cs"
      provides: "DrawRoi1Button_Click / DrawRoi2Button_Click 재드로잉 확인 가드"
  key_links:
    - from: "DrawRoi*Button_Click"
      to: "CustomMessageBox.ShowConfirmation"
      via: "기존 ROI(_roi1/_roi2) != null 분기"
      pattern: "ShowConfirmation"
---

<objective>
Tray/Bottom Align 비전의 "ROI 1 그리기" / "ROI 2 그리기" 버튼에 재드로잉 확인 가드를 추가한다.

현재 각 핸들러는 기존 ROI를 확인 없이 즉시 `null` 로 초기화하고 재드로잉을 시작한다. 이미 티칭된 ROI가 있는 경우에만 "삭제하고 다시 그리시겠습니까?" 확인(예/아니오)을 띄우고, "아니오" 시 아무 동작 없이 중단하도록 변경한다. ROI가 없으면(최초 티칭) 확인 없이 기존 로직대로 진행 — 회귀 없음.

Purpose: 실수로 재티칭 중 기존 ROI를 날리는 사고 방지.
Output: 4개 핸들러(TrayVisionView ROI1/ROI2, BottomVisionView ROI1/ROI2)에 동일한 확인 가드.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- 이미 확인된 사실 — 재탐색 불필요 -->

두 파일 모두 상단에 `using ReringProject.UI;` + `using System.Windows;` 존재.
필드 타입 (둘 다 동일):
```csharp
private RoiDefinition _roi1;   // 참조 타입 — 미티칭 상태 = null (== null 체크 유효)
private RoiDefinition _roi2;
```

같은 화면(BottomVisionView CalResetButton_Click, 직전 quick 260702-i7o)에서 이미 사용 중인 확인 패턴:
```csharp
MessageBoxResult confirmX = CustomMessageBox.ShowConfirmation("제목", "메시지", MessageBoxButton.YesNo);
if (confirmX != MessageBoxResult.Yes) {
    return;
}
```
</interfaces>

주의:
- 규칙: 삼항 `?:` 금지 → if-else, 헝가리언, C# 7.2, 각 파일 기존 K&R 브레이스 스타일 유지.
- 신규 다이얼로그 의존성 금지 — `CustomMessageBox.ShowConfirmation` 재사용.
- 주석은 오늘 날짜: `//260702 hbk`.
- Top/Side/Bottom FAI 검사 시퀀스, 다른 화면 건드리지 말 것.
</context>

<tasks>

<task type="auto">
  <name>Task 1: TrayVisionView ROI1/ROI2 재드로잉 확인 가드</name>
  <files>WPF_Example/Custom/UI/TrayVisionView.xaml.cs</files>
  <action>
`DrawRoi1Button_Click` (line ~160): 기존 `if (_viewer == null) { ... return; }` 가드 바로 아래, `_roi1 = null;` (line 167) 실행 **전에** 다음 확인 가드를 추가한다.
```csharp
//260702 hbk 기존 ROI1이 있을 때만 재드로잉 확인 — '아니오' 시 초기화/드로잉 미실행
if (_roi1 != null) {
    MessageBoxResult confirmRoi1 = CustomMessageBox.ShowConfirmation("ROI 재드로잉", "ROI 1을(를) 삭제하고 다시 그리시겠습니까?", MessageBoxButton.YesNo);
    if (confirmRoi1 != MessageBoxResult.Yes) {
        return;
    }
}
```

`DrawRoi2Button_Click` (line ~178): 기존 `if (_viewer == null) { ... return; }` 가드 바로 아래, try 블록(슬롯1 확정 + `_roi2 = null`) **전에** 다음 확인 가드를 추가한다. 확인 조건은 이 핸들러가 초기화하는 필드인 `_roi2` 기준.
```csharp
//260702 hbk 기존 ROI2가 있을 때만 재드로잉 확인 — '아니오' 시 슬롯1 확정/초기화/드로잉 전부 미실행
if (_roi2 != null) {
    MessageBoxResult confirmRoi2 = CustomMessageBox.ShowConfirmation("ROI 재드로잉", "ROI 2을(를) 삭제하고 다시 그리시겠습니까?", MessageBoxButton.YesNo);
    if (confirmRoi2 != MessageBoxResult.Yes) {
        return;
    }
}
```
기존 로직(`_roi1 = null;`, 슬롯1 CommitActiveRectangle, `_roi2 = null;`, `StartRectangleDrawing()`, 상태 텍스트)은 확인 통과 후 그대로 유지. 최초 티칭(_roi1/_roi2 == null) 경로는 확인을 건너뛰고 기존과 동일하게 동작한다.
  </action>
  <verify>
    <automated>MSBuild.exe /nologo /v:m /p:Configuration=Debug /p:Platform=x64 WPF_Example/DatumMeasurement.csproj</automated>
  </verify>
  <done>DrawRoi1Button_Click은 `_roi1 != null`일 때만, DrawRoi2Button_Click은 `_roi2 != null`일 때만 ShowConfirmation을 호출한다. '아니오' 시 어떤 필드도 null로 초기화되지 않고 StartRectangleDrawing도 호출되지 않는다. Debug/x64 빌드 PASS.</done>
</task>

<task type="auto">
  <name>Task 2: BottomVisionView ROI1/ROI2 재드로잉 확인 가드</name>
  <files>WPF_Example/Custom/UI/BottomVisionView.xaml.cs</files>
  <action>
Task 1과 동일 패턴을 BottomVisionView의 두 핸들러에 적용한다.

`DrawRoi1Button_Click` (line ~325): `if (_viewer == null) { ... return; }` 아래, `_roi1 = null;` (line 332) **전에** `_roi1 != null` 확인 가드 추가:
```csharp
//260702 hbk 기존 ROI1이 있을 때만 재드로잉 확인 — '아니오' 시 초기화/드로잉 미실행
if (_roi1 != null) {
    MessageBoxResult confirmRoi1 = CustomMessageBox.ShowConfirmation("ROI 재드로잉", "ROI 1을(를) 삭제하고 다시 그리시겠습니까?", MessageBoxButton.YesNo);
    if (confirmRoi1 != MessageBoxResult.Yes) {
        return;
    }
}
```

`DrawRoi2Button_Click` (line ~344): `if (_viewer == null) { ... return; }` 아래, try 블록(슬롯1 확정 + `_roi2 = null`) **전에** `_roi2 != null` 확인 가드 추가:
```csharp
//260702 hbk 기존 ROI2가 있을 때만 재드로잉 확인 — '아니오' 시 슬롯1 확정/초기화/드로잉 전부 미실행
if (_roi2 != null) {
    MessageBoxResult confirmRoi2 = CustomMessageBox.ShowConfirmation("ROI 재드로잉", "ROI 2을(를) 삭제하고 다시 그리시겠습니까?", MessageBoxButton.YesNo);
    if (confirmRoi2 != MessageBoxResult.Yes) {
        return;
    }
}
```
기존 로직(`_isCalRoiDrawing = false;`, 슬롯1 확정, `_roi2 = null;`, `StartRectangleDrawing()`, 상태 텍스트)은 확인 통과 후 그대로 유지. 최초 티칭 경로 회귀 없음.
  </action>
  <verify>
    <automated>MSBuild.exe /nologo /v:m /p:Configuration=Debug /p:Platform=x64 WPF_Example/DatumMeasurement.csproj</automated>
  </verify>
  <done>Bottom의 DrawRoi1/DrawRoi2 핸들러가 각각 `_roi1`/`_roi2` != null일 때만 ShowConfirmation을 호출하고, '아니오' 시 완전히 중단(초기화·드로잉 미실행)한다. Debug/x64 빌드 PASS.</done>
</task>

</tasks>

<verification>
- Tray/Bottom 각 화면에서 최초(미티칭) 상태: ROI1/ROI2 버튼 클릭 시 확인 없이 즉시 드로잉 시작(회귀 없음).
- 티칭된 ROI가 있는 상태: 버튼 클릭 시 확인 대화상자 → '예' = 기존과 동일 재드로잉, '아니오' = 아무 변화 없음(기존 ROI 유지).
- Debug/x64 빌드 PASS.
</verification>

<success_criteria>
- 4개 핸들러 모두 기존 ROI(_roi1/_roi2) != null일 때만 CustomMessageBox.ShowConfirmation 호출.
- '아니오' 선택 시 필드 초기화·StartRectangleDrawing 미실행하고 return.
- 최초 티칭 경로 및 다른 화면 무변경.
- 삼항 미사용, //260702 hbk 주석, C# 7.2, 파일별 K&R 스타일 유지.
</success_criteria>

<output>
After completion, create `.planning/quick/260702-ja4-align-roi1-roi2/260702-ja4-SUMMARY.md`
</output>
