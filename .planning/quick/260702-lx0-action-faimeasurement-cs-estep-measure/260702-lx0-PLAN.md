---
phase: quick-260702-lx0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: false
requirements: [QUAL-01]
must_haves:
  truths:
    - "case EStep.Measure 블록이 의미 단위 private 헬퍼 호출로 분리되어, 인라인 로직이 오케스트레이션 흐름만 남는다"
    - "리팩토링 후 Debug/x64 msbuild 빌드가 오류 없이 통과한다"
    - "SIMUL $TEST 실행 시 리팩토링 전과 동일한 $RESULT(P/F/B + count) 가 나온다 (사용자 실측 회귀 확인)"
    - "추출된 각 헬퍼의 try/catch/finally 경계와 필드/지역변수 read-write 순서가 원본과 동일하다"
    - "삼항 연산자(?:) 신규 도입 0건, 수정/추가 라인에 //260702 hbk 주석"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "리팩토링된 EStep.Measure + 신규 private 추출 헬퍼 메서드들"
      contains: "case EStep.Measure"
  key_links:
    - from: "Run() case EStep.Measure"
      to: "신규 private 헬퍼 (MarkMeasurementDatumSkipped/ResolveDatumTransform/InjectDatumOrigin/TryExecuteMeasurement/ApplyOverlaySuffixAndAccumulate/AggregateFaiResult/MarkAllMeasurementsNoImage)"
      via: "메서드 호출 (Extract Method)"
      pattern: "case EStep.Measure"
    - from: "sharedSrc 생성"
      to: "finally { sharedSrc.Release(); }"
      via: "try/finally 경계 (case 내부 유지, 헬퍼로 쪼개지 않음)"
      pattern: "sharedSrc.Release"
---

<objective>
`WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 의 `Run()` 메서드 중 `case EStep.Measure` 블록(파일 기준 약 222~438행, 약 216줄)을 순수 Extract Method 리팩토링으로 의미 단위 private 헬퍼로 분리한다.

Purpose: 하나의 case 안에 Shot 이미지 공유 준비 / FAI별 측정 루프 / 측정 datum-fail 게이트 / transform 해석 / Datum origin 주입 / DualImage 측정 분기 / overlay suffix 처리 / FAI 레거시 집계 / NO_IMAGE 캐스케이드가 모두 인라인되어 있어 가독성이 낮다. 실제 부품 치수 측정 P/F/B 판정에 쓰이는 핵심 검사 로직이므로 **기능 동작은 절대 변경 금지(동치 보장)**, 로직 재배열/조건 병합/성능 개선 없이 함수 추출만 수행한다.

Output: 추출 후 짧아진 `case EStep.Measure` + 클래스 기존 private 헬퍼(BuildDatumCaptureSnapshot, QueueFaiCapture 등)와 같은 위치·스타일의 신규 private 헬퍼 메서드들.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs

<style_rules>
프로젝트 규칙 (예외 없음):
- 삼항 연산자 `?:` 신규 도입 절대 금지 → if-else 만 사용 (기존 라인 이동 시 원본에 이미 삼항이 있어도 새로 만들지 않음. 단 line 257 `meas.LastSkipReason = parentSeq2.IsAlignFailed(...) ? "ALIGN_FAIL" : "DATUM_FAIL";` 같은 기존 삼항은 이동 시 그대로 두거나 if-else 로 풀어도 됨 — 동치 보장 우선, 신규 삼항만 금지).
- 수정/추가한 각 라인 또는 블록에 `//260702 hbk` 주석.
- C# 7.2 문법만. 로컬함수 허용. switch expression / nullable ref / record 금지.
- 브레이스 스타일: 이 파일의 기존 private 헬퍼(GrabOrLoadDatumImage:451, TryGrabOrLoadDualDatumImages:475, BuildDatumCaptureSnapshot:553, QueueFaiCapture:592)는 모두 **K&R (여는 브레이스 같은 줄)**. 신규 헬퍼 메서드 선언·바디는 K&R 로 작성. 단 Measure case 안에서 **이미 Allman 으로 작성된 블록**(datum-fail 게이트 line 254 `{`, origin-injection line 280/290/299 `{`, transform 미존재 등)을 헬퍼로 옮길 때는 **원본 브레이스 배치를 그대로 보존**(diff 최소화 — 이동 라인은 verbatim). 즉 이동한 코드 내부는 원본 스타일 유지, 새로 만든 메서드 껍데기는 K&R.
- 헝가리안 점진 적용: 기존 관례(`pMyContext`, `pCamera` p-prefix; 지역변수는 대부분 camelCase)를 먼저 따른다. 새로 추출한 헬퍼의 파라미터/지역변수는 원본에서 쓰던 이름을 그대로 유지(diff/리뷰 최소화). 기존 잘 쓰이던 변수명을 이유 없이 광범위 개명 금지.
</style_rules>

<behavioral_equivalence_invariants>
아래 부작용/순서 의존성을 원본과 100% 동일하게 보존 (리팩토링 실패의 1순위 함정):
- `SharedHImage sharedSrc` 는 `new SharedHImage(image.CopyImage())` 로 생성(line 236) → try 내부 사용 → **finally 에서 `sharedSrc.Release()`(line 402-404)**. sharedSrc 의 생성·Release 는 반드시 `case EStep.Measure` 본문(using(image) 스코프)에 남긴다. try/finally 경계를 헬퍼로 쪼개지 말 것. 헬퍼는 sharedSrc 를 **파라미터로 전달**만 받는다.
- `capSaver == null` 이면 `image.CopyImage()` 자체 생략(sharedSrc=null 유지) — 이 분기(line 234-236)를 반드시 보존(성능 회귀 방지).
- transform identity fallback: `HOperatorSet.HomMat2dIdentity(out transform)` 실패 시 `transform = new HTuple()` (line 271-275) try/catch 보존.
- DualImage 분기(line 314-333): `RuntimeImageA/B` 주입 → TryExecute → **finally 에서 `imgA/imgB.Dispose()` 후 `RuntimeImageA=null; RuntimeImageB=null`** 리셋 순서 보존. 예외 시 `ok=false; resultValue=0; measError=ex.Message; measOverlays=null` 도 보존.
- 누적 순서/대입 시점: `meas.LastJudgement` / `faiAllPass` / `allPass` / `measuredCount` / `overlayAcc` / `faiOverlays`. 특히 datum-fail continue 케이스(line 253-267)도 `measuredCount++`(line 265) 는 실행됨 → continue 전에 카운트. 이 순서 반드시 유지.
- overlay suffix: `-OK`/`-NG` 는 `ov.RoiId.StartsWith("FAI-Edge", OrdinalIgnoreCase)` 인 것에만 부여(line 363), FAI-DistLine 등은 미부여. `overlayAcc.AddRange` + `faiOverlays.AddRange` 둘 다(line 368-369) 순서 보존.
- `fai.Measurements.Count > 0` 분기(line 378-395) vs 0개 분기(line 396-399: ClearResult + LastOverlays.Clear()). wasSkip 판정은 `DATUM_FAIL` 또는 `ALIGN_FAIL`(line 387). QueueFaiCapture 호출 시점(line 395: overlay+source 확정 후) 보존.
- image==null NO_IMAGE 캐스케이드(line 405-430): `allPass=false` 먼저, 각 meas `LastSkipReason="NO_IMAGE"` + `measuredCount++`, `faiHadMeas`(Measurements.Count>0) 인 fai 는 IsPass=false/MeasuredValue=0/LastOverlays.Clear(), 아닌 fai 는 ClearResult()+LastOverlays.Clear(). 처리 차이 보존.
- 예외 캐치 범위(try/catch/finally 경계)가 원본과 동일해야 함. **오직 Extract Method 만** — 로직 재배열/조건 병합/최적화 금지.
</behavioral_equivalence_invariants>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 측정 루프 내부(per-measurement) 로직을 private 헬퍼로 Extract Method</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <action>
`case EStep.Measure` 의 `foreach (var meas in fai.Measurements)` 루프 내부 로직을 아래 4개 private 헬퍼로 추출한다. 기존 클래스 private 헬퍼(BuildDatumCaptureSnapshot 등)와 동일 위치(클래스 하단, QueueFaiCapture 인근)에 K&R 스타일로 추가. 각 신규/수정 라인에 `//260702 hbk` 주석.

1) `private void MarkMeasurementDatumSkipped(MeasurementBase meas, InspectionSequence parentSeq2)`
   - 원본 line 255-263 (ClearResult / LastSkipReason 설정(IsAlignFailed 분기 — 기존 삼항이면 if-else 로 풀어 동치 유지) / LastJudgement=false / measName·datumRef null 폴백 / Logging.PrintLog) 을 그대로 이식.
   - 호출부(line 253-267): `if (parentSeq2 != null && parentSeq2.IsDatumFailed(meas.DatumRef))` 블록 안에서 `MarkMeasurementDatumSkipped(meas, parentSeq2);` 호출 후 **`faiAllPass = false; measuredCount++; continue;` 는 루프 본문에 그대로 남긴다**(순서 보존). Allman 브레이스 배치 보존.

2) `private HTuple ResolveDatumTransform(InspectionSequence parentSeq2, string datumRef)`
   - 원본 line 269-276 (`TryGetDatumTransform` 실패 시 HomMat2dIdentity try/catch, 실패 시 new HTuple()) 이식, 결과 HTuple 반환.
   - 호출부: `HTuple transform = ResolveDatumTransform(parentSeq2, meas.DatumRef);`

3) `private void InjectDatumOrigin(MeasurementBase meas, InspectionSequence parentSeq2)`
   - 원본 line 277-308 (`meas as IDatumOriginConsumer` null 체크, DatumConfigs 순회로 매칭 dc 찾기, dc!=null 이면 DetectedOrigin*/RefAngle/RefAngle2/DetectedCircle* 주입, else 0.0 주입) 이식. Allman 브레이스 배치 보존.
   - 호출부: `InjectDatumOrigin(meas, parentSeq2);`

4) `private bool TryExecuteMeasurement(MeasurementBase meas, HImage image, HTuple transform, double pixRes, out double resultValue, out string measError, out List<EdgeInspectionOverlay> measOverlays)`
   - 원본 line 309-343 (DualImage 분기 전체: `meas is DualImageEdgeDistanceMeasurement dualMeas` → TryGrabOrLoadFaiDualImages → RuntimeImageA/B 주입 → TryExecute try/catch → **finally 에서 imgA/imgB Dispose + RuntimeImageA/B=null**; else 1-image TryExecute try/catch) 이식. `bool ok` 를 반환. resultValue/measError/measOverlays 는 out. **finally 순서/예외 처리 verbatim 보존.**
   - 호출부: `double resultValue; string measError; List<EdgeInspectionOverlay> measOverlays; bool ok = TryExecuteMeasurement(meas, image, transform, pixRes, out resultValue, out measError, out measOverlays);`

주의: `sharedSrc`, `datumSnapshot`, `pixRes` 는 이 task 에서 헬퍼로 옮기지 않는다(Task 2). ok 이후의 EvaluateJudgement/실패 로그/ClearResult(line 344-354) 는 루프 본문에 그대로 둔다.
  </action>
  <verify>
    <automated>"C:\Program Files\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal — 빌드 오류 0 확인. 이 task 단독 커밋 후에도 빌드 통과해야 함. (Git Bash 사용 시: MSYS_NO_PATHCONV=1 "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal)</automated>
  </verify>
  <done>4개 헬퍼가 클래스에 추가되고 Measure 루프 본문이 해당 호출로 대체됨. Debug/x64 빌드 오류 0. faiAllPass/measuredCount/continue 순서, DualImage finally 순서, transform identity fallback 동치 유지.</done>
</task>

<task type="auto">
  <name>Task 2: Shot 단위(per-shot) 로직 Extract Method + Measure case 최종 재배선 + 빌드 검증</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <action>
남은 Shot 단위 로직을 아래 3개 private 헬퍼로 추출한다. K&R 스타일, `//260702 hbk` 주석.

1) `private void ApplyOverlaySuffixAndAccumulate(MeasurementBase meas, List<EdgeInspectionOverlay> measOverlays, List<EdgeInspectionOverlay> overlayAcc, List<EdgeInspectionOverlay> faiOverlays)`
   - 원본 line 355-370 (measOverlays!=null 이면 suffix -OK/-NG(LastJudgement 기준, if-else), FAI-Edge* StartsWith 만 suffix 부여, overlayAcc.AddRange + faiOverlays.AddRange) 이식.
   - 호출부: `ApplyOverlaySuffixAndAccumulate(meas, measOverlays, overlayAcc, faiOverlays);` (기존 위치 그대로).

2) `private void AggregateFaiResult(FAIConfig fai, bool faiAllPass, List<EdgeInspectionOverlay> faiOverlays, SharedHImage sharedSrc, List<DatumCaptureOverlay> datumSnapshot)`
   - 원본 line 376-399 (Measurements.Count>0 분기: IsPass/MeasuredValue[0]/wasSkip for-loop(DATUM_FAIL||ALIGN_FAIL)/WasDatumSkipped/LastOverlays=faiOverlays/ownerSeqName 산출/QueueFaiCapture 호출; else 분기: ClearResult + LastOverlays.Clear()) 이식.
   - ownerSeqName 산출(line 392-394)은 헬퍼 내부에서 `ShotParam` 접근 가능(인스턴스 메서드)하므로 그대로 이식.
   - 호출부(fai 루프 끝): `AggregateFaiResult(fai, faiAllPass, faiOverlays, sharedSrc, datumSnapshot);` 후 `if (!faiAllPass) allPass = false;`(line 400)는 루프 본문에 남김.

3) `private void MarkAllMeasurementsNoImage(ref int measuredCount)`
   - 원본 line 405-430 (else(image==null) 분기 본문: ShotParam.FAIList 순회, meas ClearResult/LastSkipReason="NO_IMAGE"/LastJudgement=false/measuredCount++, faiHadMeas 분기 IsPass=false/MeasuredValue=0/LastOverlays.Clear() vs ClearResult()+LastOverlays.Clear(), shotName 로그) 이식. `allPass=false`(line 409) 는 호출부에 남기거나 헬퍼 밖에서 명시적으로 설정 — 동치 유지. measuredCount 는 ref.
   - 호출부(else 분기): `allPass = false; MarkAllMeasurementsNoImage(ref measuredCount);`

**주의(경계 보존):** `sharedSrc` 생성(capSaver null 체크 포함, line 234-236), `datumSnapshot`/`pixRes`/CorrectionFactor 가드(line 238-244), 그리고 `try { foreach(fai) ... } finally { if(sharedSrc!=null) sharedSrc.Release(); }`(line 245, 402-404) 의 try/finally 경계는 **Measure case 본문에 그대로 유지**. sharedSrc 는 AggregateFaiResult 에 파라미터로만 전달. pMyContext.AllPass/MeasuredCount/InspectionOverlays 대입(line 433-435)과 Step 전이(line 436)도 그대로.

재배선 완료 후 `case EStep.Measure` 는 오케스트레이션 흐름(image 획득 → shared 준비 → foreach fai { foreach meas { gate/transform/origin/execute/judge/overlay } aggregate } → context 반영)만 남아야 함.
  </action>
  <verify>
    <automated>"C:\Program Files\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe" WPF_Example\DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal — 빌드 오류 0 확인. (Git Bash 사용 시: MSYS_NO_PATHCONV=1 "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal)</automated>
  </verify>
  <done>3개 헬퍼 추가 + Measure case 재배선 완료. Debug/x64 빌드 오류 0. try/finally(sharedSrc.Release) 경계·capSaver null skip·NO_IMAGE 캐스케이드 동치 유지. 신규 삼항 0건, 수정 라인 //260702 hbk 주석.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
case EStep.Measure 블록을 7개 private 헬퍼(MarkMeasurementDatumSkipped, ResolveDatumTransform, InjectDatumOrigin, TryExecuteMeasurement, ApplyOverlaySuffixAndAccumulate, AggregateFaiResult, MarkAllMeasurementsNoImage)로 순수 Extract Method 리팩토링. 빌드 Debug/x64 PASS 확인 완료. 단, 실제 측정 판정(P/F/B) 동치는 자동 검증 불가 — 에이전트가 TCP $TEST 시뮬레이션 환경을 재현하기 어려움.
  </what-built>
  <how-to-verify>
1. 리팩토링 전 커밋(HEAD~) 기준으로 빌드한 앱에서, 대표 SIMUL 레시피 몇 개에 대해 `$TEST` 명령을 실행하고 각 SHOT/FAI 의 `$RESULT`(site;Type;P|F|B;count) 값을 기록한다. (또는 리팩토링 직전에 기준값을 미리 캡쳐)
2. 리팩토링 후 앱을 재빌드 후 앱 종료→재실행, **동일한 레시피·동일한 SimulImagePath** 로 같은 `$TEST` 를 재실행한다.
3. 각 항목의 P/F/B 판정, measuredCount, MeasuredValue, overlay(에지 -OK/-NG 색), NO_IMAGE/DATUM_FAIL/ALIGN_FAIL skip 사유가 리팩토링 전과 **완전히 동일**한지 비교한다.
4. 특히: (a) SimulImagePath 무효 SHOT 이 여전히 NO_IMAGE NG 로만 처리되고 다른 SHOT 에 전파 안 되는지, (b) datum 검출 실패 measurement 가 여전히 DATUM_FAIL/ALIGN_FAIL 로 skip 되며 count 는 증가하는지, (c) DualImage FAI 측정값이 동일한지 확인.
  </how-to-verify>
  <resume-signal>동일하면 "approved", 값 차이가 있으면 어떤 항목이 어떻게 달라졌는지 기술 (회귀 → 즉시 수정)</resume-signal>
</task>

</tasks>

<verification>
- Debug/x64 msbuild 빌드 오류 0 (Task 2 automated).
- diff 검토: 이동한 코드 라인은 verbatim(로직 재배열/조건 병합 없음), 신규 삼항 0건, 각 수정/추가 블록 //260702 hbk.
- 경계 체크리스트: sharedSrc try/finally.Release 위치 유지 / capSaver null 시 CopyImage skip / DualImage finally Dispose+null 순서 / measuredCount++ (datum-fail continue 포함) / overlay FAI-Edge* 한정 suffix / NO_IMAGE 캐스케이드 fai 분기.
- 사용자 SIMUL $TEST 회귀 확인 (checkpoint) — 최종 실측 동치는 사용자 몫.
</verification>

<success_criteria>
- case EStep.Measure 가 7개 헬퍼 호출 중심의 짧은 오케스트레이션으로 축소.
- 다른 case(Init/MoveZ/DatumPhase/Grab/End) 및 기존 헬퍼 무수정.
- 필드 추가/삭제 0, 로직 재배열 0, 동작 동치.
- 빌드 PASS + 사용자 $RESULT 동일 확인.
</success_criteria>

<output>
After completion, create `.planning/quick/260702-lx0-action-faimeasurement-cs-estep-measure/260702-lx0-SUMMARY.md`
</output>
