---
phase: quick-260710-di7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Custom/Sequence/Bottom/Bottom_Sequence_0428.zip
  - WPF_Example/Custom/Sequence/Inspection/SkipReason.cs
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs
  - WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs
  - WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs
  - WPF_Example/Custom/Export/ExcelExportService.cs
  - WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [CLEANUP-DI7]

must_haves:
  truths:
    - "ClassifyFai / 두 TryRunDatumPhase 오버로드가 코드베이스에서 완전히 제거된다"
    - "Bottom_Sequence_0428.zip 백업 파일이 git 추적에서 제거된다"
    - "세 skip-사유 토큰(DATUM_FAIL/ALIGN_FAIL/NO_IMAGE)이 단일 상수 클래스에서만 리터럴로 선언되고, 나머지 실행 코드는 상수를 참조한다"
    - "Debug/x64 빌드가 PASS 하고 런타임 동작(와이어/CSV/로그 값)은 이전과 100% 동일하다 (회귀 0)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/SkipReason.cs"
      provides: "DATUM_FAIL/ALIGN_FAIL/NO_IMAGE 단일 소스 상수"
      contains: "public const string DATUM_FAIL"
    - path: "WPF_Example/DatumMeasurement.csproj"
      provides: "신규 SkipReason.cs Compile Include 등록"
      contains: "SkipReason.cs"
  key_links:
    - from: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      to: "SkipReason.DATUM_FAIL / ALIGN_FAIL / NO_IMAGE"
      via: "상수 참조 치환"
      pattern: "SkipReason\\.(DATUM_FAIL|ALIGN_FAIL|NO_IMAGE)"
    - from: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      to: "SkipReason.DATUM_FAIL / ALIGN_FAIL"
      via: "상수 참조 치환"
      pattern: "SkipReason\\.(DATUM_FAIL|ALIGN_FAIL)"
---

<objective>
시퀀스 계층의 죽은 코드를 삭제하고, 흩어진 skip-사유 매직 문자열을 단일 상수로 통합한다. 기능 영향 0 (순수 정리 + 리터럴→상수 치환).

Purpose: (1) 호출부 0 인 죽은 메서드/백업 zip 제거로 유지보수 표면 축소. (2) `"DATUM_FAIL"`/`"ALIGN_FAIL"`/`"NO_IMAGE"` 문자열이 여러 파일에 복붙되어 오타 시 silent 오동작(잘못된 판정/CSV 왜곡) 위험 → 단일 상수로 통합해 컴파일 타임 안전성 확보.
Output: 죽은 코드 3메서드+zip 삭제, 신규 `SkipReason.cs` 상수 클래스, 전 실행 코드가 상수 참조.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

# 사전조사로 확인된 정확한 위치 (executor 는 grep 으로 재확인 후 작업)
# 죽은 코드 (호출부 0):
#   InspectionSequence.cs:631  private EVisionResultType ClassifyFai(FAIConfig fai)   -> 살아있는 ClassifyMeasurement(:612)로 대체됨
#   InspectionSequence.cs:611  주석 안 "(ClassifyFai 측정 단위 복제)" 문구 -> ClassifyFai 삭제 시 문구 정리
#   InspectionSequence.cs:771  public bool TryRunDatumPhase(HImage image, out string error)
#   InspectionSequence.cs:807  public bool TryRunDatumPhase(HImage image1, HImage image2, out string error)
#   WPF_Example/Custom/Sequence/Bottom/Bottom_Sequence_0428.zip  (방치된 백업 zip)
#
# skip-사유 문자열 리터럴 — 실행 코드 출현 위치 (grep 확정, 전부 ReringProject.Sequence 아니면 using 필요):
#   Action_FAIMeasurement.cs:526,527,656,677      (namespace ReringProject.Sequence — using 불필요)
#   InspectionSequence.cs:615                     (namespace ReringProject.Sequence — using 불필요)
#   MeasurementHistoryCsvWriter.cs:114,115        (namespace ReringProject.Sequence — using 불필요)
#   MeasurementHistoryCsvLoader.cs:180,182,185,187(namespace ReringProject.Sequence — using 불필요)
#   RepeatMeasurementStats.cs:108                 (namespace ReringProject.Sequence — using 불필요)
#   ExcelExportService.cs:91,95                   (namespace ReringProject.Export — using ReringProject.Sequence 추가 필요)
#   ReviewMeasurementRow.cs:83,87                 (namespace ReringProject.UI  — using ReringProject.Sequence 추가 필요)
# 주석 안에만 등장(수정 대상 아님, grep 잔존 정상): CycleResultSerializer.cs:128, MeasurementBase.cs:65, CycleResultDto.cs:89, RepeatMeasurementStats.cs:57, ReviewMeasurementRow.cs:28
</context>

<coding_rules>
하위 executor 는 아래 규칙을 반드시 준수한다:
- 삼항 연산자 `?:` **신규 사용 금지** → if-else 로 작성. (기존 1-depth 삼항/`??` 는 건드리지 말 것)
- 헝가리언 접두사(p/b/n/sz/d) 기존 표기 유지.
- 회귀 0 — 런타임 동작/문자열 **값** 일절 변경 없음. 상수화는 값이 동일한 리터럴 → 상수 참조 치환일 뿐.
- 초보자 가독성 우선.
- 수정/추가한 라인에 `//260710 hbk` 주석 필수.
- C# 7.2 / .NET Framework 4.8 — C# 8.0+ 기능(nullable ref, switch expression, record) 금지.
- 파일별 기존 brace 스타일 유지 (파일 안에서 섞지 말 것).

**절대 건드리지 말 것 (사전조사 함정):**
- `[Obsolete]` TopInspectionAction / BottomInspectionAction 삭제 금지 (SequenceHandler.cs:68-72 에서 살아있는 플레이스홀더).
- `Action_FAIMeasurement.Run()` 거대 switch 분할 금지.
- Param 티칭 헬퍼 4중 복붙 통합 금지.
- 매직넘버 10 (픽커 배열) 상수화 제외.
- `//YYMMDD hbk` 날짜 주석 일괄 제거 금지.
- ParamBase.Load 누락키 0-덮어쓰기 영역 접근 금지.
</coding_rules>

<tasks>

<task type="auto">
  <name>Task 1: 죽은 코드 3메서드 + 백업 zip 삭제</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs, WPF_Example/Custom/Sequence/Bottom/Bottom_Sequence_0428.zip</files>
  <action>
1. **삭제 전 재검증(필수):** 다음 명령으로 호출부 0 을 재확인한다.
   `grep -rn "ClassifyFai\|TryRunDatumPhase" --include=*.cs WPF_Example/`
   기대 결과: `InspectionSequence.cs` 의 정의부(4줄: 611 주석·631 ClassifyFai·771/807 TryRunDatumPhase)만 출현. 그 외 파일에서 호출부가 **하나라도** 나오면 해당 항목은 삭제하지 말고 건너뛰고 SUMMARY 에 "호출부 발견 → skip" 으로 기록한다.

2. `InspectionSequence.cs` 에서 아래를 삭제:
   - `private EVisionResultType ClassifyFai(FAIConfig fai)` 메서드 전체(대략 :630 주석 라인 `//260623 hbk Phase 49 (D-02)...` 부터 닫는 `}` 까지). 이 메서드는 살아있는 `ClassifyMeasurement`(:612)로 완전 대체됨.
   - `public bool TryRunDatumPhase(HImage image, out string error)` 메서드 전체(대략 :770 주석 `// Datum phase 실행...` 부터 :804 닫는 `}` 까지).
   - `public bool TryRunDatumPhase(HImage image1, HImage image2, out string error)` 메서드 전체(대략 :805 주석 `// VerticalTwoHorizontalDualImage 전용...` 부터 :833 닫는 `}` 까지).
   - :611 의 `ClassifyMeasurement` 앞 주석에서 `(ClassifyFai 측정 단위 복제)` 문구만 제거(주석 나머지는 유지). 예: `... 그 외 OK('P'). (ClassifyFai 측정 단위 복제)` → `... 그 외 OK('P'). //260710 hbk ClassifyFai 제거로 문구 정리`

   주의: 두 TryRunDatumPhase 만 삭제하고, `ClearDatumTransforms` / `MarkDatumFailed` / `IsDatumFailed` / `MarkAlignFailed` 등 인접한 살아있는 datum 헬퍼는 절대 건드리지 말 것.

3. 방치된 백업 zip 을 git 추적에서 삭제:
   `git rm "WPF_Example/Custom/Sequence/Bottom/Bottom_Sequence_0428.zip"`
   (파일이 없거나 untracked 이면 `rm -f` 후 SUMMARY 기록.)

4. Debug/x64 빌드로 컴파일 성공 확인(아래 verify).
  </action>
  <verify>
    <automated>grep -rn "ClassifyFai\|TryRunDatumPhase" --include=*.cs WPF_Example/ | wc -l  # 기대: 0</automated>
    <automated>MSBUILD=$(ls "/c/Program Files/Microsoft Visual Studio/"*/*/MSBuild/Current/Bin/MSBuild.exe "/c/Program Files (x86)/Microsoft Visual Studio/"*/*/MSBuild/Current/Bin/MSBuild.exe 2>/dev/null | head -1); "$MSBUILD" WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /t:Build</automated>
  </verify>
  <done>세 메서드명 grep 결과 0건, Bottom_Sequence_0428.zip git 추적 제거, Debug/x64 빌드 PASS. 인접 datum 헬퍼 및 ClassifyMeasurement 무손상.</done>
</task>

<task type="auto">
  <name>Task 2: skip-사유 문자열 상수화 (SkipReason 단일 소스)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/SkipReason.cs, WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs, WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs, WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvWriter.cs, WPF_Example/Custom/Sequence/Inspection/MeasurementHistoryCsvLoader.cs, WPF_Example/Custom/Sequence/Inspection/RepeatMeasurementStats.cs, WPF_Example/Custom/Export/ExcelExportService.cs, WPF_Example/UI/ViewModel/ReviewMeasurementRow.cs, WPF_Example/DatumMeasurement.csproj</files>
  <action>
0. **작업 전 전체 출현 위치 확정(필수):**
   `grep -rn '"DATUM_FAIL"\|"ALIGN_FAIL"\|"NO_IMAGE"' --include=*.cs WPF_Example/`
   결과를 **실행 코드 리터럴** vs **주석** 으로 분류한다. 주석 안 리터럴(CycleResultSerializer.cs, MeasurementBase.cs, CycleResultDto.cs, RepeatMeasurementStats.cs:57, ReviewMeasurementRow.cs:28 등)은 **수정 대상 아님** — 그대로 둔다. 오직 실행 코드의 리터럴만 상수로 치환한다.

   범위 참고: 사전조사 seed 는 2개 파일만 명시했으나, 실제 동일 토큰이 아래 파일들의 실행 코드에도 존재한다(grep 확정). 통합의 목적(오타 방지)과 이 plan 의 verify 기준을 충족하려면 세 토큰의 **모든 실행 코드 출현**을 치환해야 한다. 이는 새로운 리팩토링 유형이 아니라 동일 작업(skip-사유 상수화)의 완결이다. **세 토큰 외 다른 매직값/리팩토링은 절대 확장 금지.**

1. **신규 상수 파일 생성** `WPF_Example/Custom/Sequence/Inspection/SkipReason.cs`, namespace `ReringProject.Sequence`:
   ```csharp
   //260710 hbk skip-사유 단일 소스 상수. 값은 와이어/CSV/로그에 그대로 나가므로 절대 변경 금지.
   namespace ReringProject.Sequence
   {
       public static class SkipReason
       {
           public const string DATUM_FAIL = "DATUM_FAIL"; //260710 hbk datum 검출 실패 skip
           public const string ALIGN_FAIL = "ALIGN_FAIL"; //260710 hbk align 패턴매칭 실패 skip
           public const string NO_IMAGE = "NO_IMAGE";     //260710 hbk 이미지 없음 skip
       }
   }
   ```
   값 문자열은 기존 리터럴과 **바이트 단위 동일**해야 한다.

2. **csproj 등록(필수):** `WPF_Example/DatumMeasurement.csproj` 의 `<Compile Include="Custom\Sequence\Inspection\MeasurementBase.cs" />` 인접 위치에 한 줄 추가:
   `<Compile Include="Custom\Sequence\Inspection\SkipReason.cs" />`
   (classic-style csproj — 자동 포함 안 됨. 누락 시 컴파일 제외되어 CS0103 발생.)

3. **실행 코드 리터럴 치환** (각 수정 라인에 `//260710 hbk` 주석):
   - `Action_FAIMeasurement.cs:526` `"ALIGN_FAIL"` → `SkipReason.ALIGN_FAIL`
   - `Action_FAIMeasurement.cs:527` `"DATUM_FAIL"` → `SkipReason.DATUM_FAIL`
   - `Action_FAIMeasurement.cs:656` `"DATUM_FAIL"`, `"ALIGN_FAIL"` → 각 상수
   - `Action_FAIMeasurement.cs:677` `"NO_IMAGE"` → `SkipReason.NO_IMAGE`
   - `InspectionSequence.cs:615` `"DATUM_FAIL"`, `"ALIGN_FAIL"` → 각 상수
   - `MeasurementHistoryCsvWriter.cs:114,115` 비교문 **및** return 값 `"DATUM_FAIL"`/`"NO_IMAGE"` → 각 상수 (동일 토큰이라 값 보존)
   - `MeasurementHistoryCsvLoader.cs:180,182,185,187` `"DATUM_FAIL"`/`"NO_IMAGE"` → 각 상수
   - `RepeatMeasurementStats.cs:108` `"DATUM_FAIL"`/`"NO_IMAGE"` → 각 상수
   - `ExcelExportService.cs:91,95` `"DATUM_FAIL"`/`"NO_IMAGE"` → 각 상수. 이 파일은 namespace `ReringProject.Export` 이며 `using ReringProject.Sequence;` 없음 → using 디렉티브 추가(기존 using 블록 알파벳/그룹 위치에 삽입).
   - `ReviewMeasurementRow.cs:83,87` `"DATUM_FAIL"`/`"NO_IMAGE"` → 각 상수. 이 파일은 namespace `ReringProject.UI` 이며 `using ReringProject.Sequence;` 없음 → using 디렉티브 추가.

   라인 번호는 참고값 — 0단계 grep 결과를 기준으로 실제 위치를 확정해 치환한다. 각 파일의 기존 brace/포맷 스타일 유지.

4. Debug/x64 빌드 PASS 확인.
  </action>
  <verify>
    <automated>MSBUILD=$(ls "/c/Program Files/Microsoft Visual Studio/"*/*/MSBuild/Current/Bin/MSBuild.exe "/c/Program Files (x86)/Microsoft Visual Studio/"*/*/MSBuild/Current/Bin/MSBuild.exe 2>/dev/null | head -1); "$MSBUILD" WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /v:minimal /t:Build</automated>
    <automated>grep -rn '"DATUM_FAIL"\|"ALIGN_FAIL"\|"NO_IMAGE"' --include=*.cs WPF_Example/ | grep -v "SkipReason.cs"</automated>
  </verify>
  <done>SkipReason.cs 존재+csproj 등록, 실행 코드의 세 토큰 리터럴이 모두 SkipReason.* 참조로 치환됨. 두 번째 verify grep 의 잔존 hit 는 **주석 라인뿐**(CycleResultSerializer/MeasurementBase/CycleResultDto/RepeatMeasurementStats:57/ReviewMeasurementRow:28) — 실행문(=/return/비교) 리터럴은 0. Debug/x64 빌드 PASS. 문자열 값 무변경.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
변경 없음. 이 plan 은 죽은 코드 삭제 + 리터럴→상수 치환(값 동일)만 수행하며 새로운 입력 경로/신뢰 경계를 도입하지 않는다.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-di7-01 | Tampering | skip-사유 문자열 값 우발적 변경 | mitigate | SkipReason 상수 값은 기존 리터럴과 바이트 동일하게 선언; verify 에서 빌드+grep 로 회귀 검출 |
</threat_model>

<verification>
- Task 1: `grep -rn "ClassifyFai\|TryRunDatumPhase" --include=*.cs WPF_Example/` = 0건.
- Task 2: 실행 코드에 세 토큰 bare 리터럴 0 (SkipReason.cs 선언부 제외, 주석 제외).
- 통합: Debug/x64 MSBuild PASS. 런타임 동작 무변경 (와이어/CSV/로그 출력 값 이전과 동일).
</verification>

<success_criteria>
- 죽은 메서드 3개 + 백업 zip 제거, 빌드 PASS.
- skip-사유 세 토큰이 SkipReason 단일 소스에서만 리터럴 선언, 전 실행 코드가 상수 참조.
- 회귀 0: 어떤 문자열 값도 변경되지 않음.
- 각 task 는 code-only atomic commit.
</success_criteria>

<output>
After completion, create `.planning/quick/260710-di7-dead-code-const-cleanup/260710-di7-SUMMARY.md`.
Task 1/Task2 는 각각 atomic commit (code changes only). Task 1 재검증서 호출부 발견 항목이나 zip 부재 등 예외는 SUMMARY 에 기록.
</output>
