---
phase: quick-260710-f4k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Bottom/Action_BottomCalibration.cs
  - WPF_Example/Custom/Sequence/Top/Action_TopCalibration.cs
  - WPF_Example/ExternalLib/VisionLib/Alligator/Alligator.cs
  - WPF_Example/ExternalLib/VisionLib/Alligator/AlligatorDef.cs
  - WPF_Example/ExternalLib/VisionLib/AlligatorAlgMil/AlligatorAlgMil.cs
  - WPF_Example/ExternalLib/VisionLib/AlligatorAlgMil/AlligatorAlgMilDef.cs
  - WPF_Example/TcpServer/VisionResponsePacket.cs
  - WPF_Example/TcpServer/TcpServer.cs
  - WPF_Example/TcpServer/VisionRequestPacket.cs
  - WPF_Example/TcpServer/VisionServer.cs
  - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
  - WPF_Example/Halcon/Display/HalconDisplayService.cs
  - WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  - WPF_Example/UI/ViewModel/ModelFinderViewModel.cs
  - WPF_Example/UI/ViewModel/CalibrationViewModel.cs
  - WPF_Example/Device/Camera/Basler/BaslerCamera.cs
  - WPF_Example/Device/Camera/Hik/HikCamera.cs
  - WPF_Example/Utility/Ini.cs
  - WPF_Example/Custom/UI/ViewModel/NodeViewModel.cs
autonomous: true
requirements: [DEADCODE-SWEEP-01]

must_haves:
  truths:
    - "6개 csproj 미등록 죽은 소스 파일이 리포지토리에서 삭제됨"
    - "참조 0 이 재검증된 죽은 메서드/필드가 제거됨 (오버로드 오삭제 없음)"
    - "주석처리 코드 블록 + 미사용 using 이 정리됨"
    - "3개 그룹 각각 커밋 전 Debug/x64 빌드 PASS"
    - "런타임 동작 변경 0 (순수 삭제만)"
  artifacts:
    - path: "WPF_Example/DatumMeasurement.csproj"
      provides: "빌드 목록 (수정 불필요, 미등록 확인만)"
  key_links:
    - from: "각 삭제 대상 심볼"
      to: "grep 참조 0 + XAML Click/Changed/x:Name 0"
      via: "삭제 전 재검증 게이트"
      pattern: "grep -rn 참조 0"
---

<objective>
코드베이스 전체에서 무위험 등급 죽은 코드(약 2,300줄)를 3개 그룹으로 나눠 삭제한다. 직전 quick 260710-di7(시퀀스 계층 정리)의 후속으로, 인프라/Halcon/UI 3개 영역 병렬 감사 + 오케스트레이터 직접 grep 재검증으로 확정된 범위다.

Purpose: 빌드에 들어가지도 않는 죽은 파일과 참조 0 심볼을 제거해 유지보수 표면을 줄인다. 기능 손상 0.
Output: 6개 파일 삭제 + 참조 0 메서드/필드 제거 + 주석블록/미사용 using 정리. 3개 atomic commit, 각 커밋 전 Debug/x64 빌드 PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<coding_rules>
하위 executor 는 아래 규칙을 반드시 지킨다:
- 삼항 `?:` 신규 사용 금지 → if-else (기존 삼항 제거도 금지)
- 헝가리언 접두사(p/b/n/sz/d/m) 기존 표기 유지
- 회귀 0 — 런타임 동작 일절 변경 없음 (순수 삭제)
- 초보자 가독성 우선
- 삭제로 인해 인접 코드를 수정해야 하는 경우만 `//260710 hbk` 주석 추가. 순수 삭제는 주석 불필요.
- `//YYMMDD hbk` 기존 날짜 주석 일괄 제거 금지
- C# 7.2 / .NET Framework 4.8 — C# 8.0+ 기능 금지
- 파일별 기존 brace 스타일 유지 (구코드 K&R / 신코드 Allman)
- 계획에 없는 파일 수정 금지
</coding_rules>

<absolute_do_not>
- 버그 3건(DeviceHandler LINQ Select, BaslerCamera {2} FormatException, Logging Debug.Assert(true)) 수정 금지 — SUMMARY 기록만.
- WPF_Example/Utility/RecipeFileHelper.cs 절대 수정 금지 (line 187 은 bin DLL FileVersionInfo, 소스 .cs 아님)
- DatumFindingService.cs 는 `horect` 2줄 외 절대 건드리지 말 것 (검출 핵심)
- VisionResponsePacket 의 `Convert(VisionResponsePacket)` (~283) 은 살아있음 — 삭제 대상은 `Convert(string)` (~96) 뿐
- VisionAlgorithmService 의 `TryFindCircle`(~256) 은 살아있음 — 삭제 금지
- MainView 의 `NotifyDatumParamMaybeChanged`(~1217) 는 삭제 금지 (라이브 호출자 존재)
- HikCamera 의 `ID` 필드는 흔한 이름 — 반드시 클래스 범위 참조 0 + 상속/인터페이스 멤버 아님 확인 후 삭제
- [Obsolete] TopInspectionAction/BottomInspectionAction 삭제 금지 (SequenceHandler.cs:68-72 에서 new 됨)
- Ini.cs/Logging.cs 미호출 public API 삭제 금지 (`#if JS` 블록만 예외)
- 검출 파라미터(sigma/threshold/trimCount), HALCON SetColor 색상 문자열, HImage Dispose 구조 건드리지 말 것
</absolute_do_not>

<verified_build_command>
Git Bash 에서 MSBuild 스위치는 `-p:` 를 쓸 것 (`/p:` 는 경로로 해석되어 MSB1008 오류). 검증된 명령:
"/c/Program Files/Microsoft Visual Studio/18/Insiders/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal -t:Build
</verified_build_command>

<xaml_false_positive_gate>
private 메서드/필드 삭제 전 반드시 대응 `.xaml` 에서 `Click=` / `Changed=` / `x:Name=` grep. XAML 참조가 있으면 삭제 금지. WPF 이벤트 핸들러는 C# grep 상 참조 0 으로 보인다.
</xaml_false_positive_gate>
</context>

<tasks>

<task type="auto">
  <name>Task 1: csproj 미등록 죽은 파일 6개 삭제 (1,746줄)</name>
  <files>
WPF_Example/Custom/Sequence/Bottom/Action_BottomCalibration.cs
WPF_Example/Custom/Sequence/Top/Action_TopCalibration.cs
WPF_Example/ExternalLib/VisionLib/Alligator/Alligator.cs
WPF_Example/ExternalLib/VisionLib/Alligator/AlligatorDef.cs
WPF_Example/ExternalLib/VisionLib/AlligatorAlgMil/AlligatorAlgMil.cs
WPF_Example/ExternalLib/VisionLib/AlligatorAlgMil/AlligatorAlgMilDef.cs
  </files>
  <action>
아래 6개 파일은 DatumMeasurement.csproj 에 &lt;Compile Include&gt; 가 없어 빌드에 아예 포함되지 않는다(오케스트레이터 확인 완료). 클래스 참조도 grep 0.

**삭제 전 재검증 (모두 통과해야 삭제 진행):**
1. `grep -rn "Alligator" --include=*.cs WPF_Example/ | grep -v ExternalLib` → RecipeFileHelper.cs:187 (dll 문자열) 1건만 나와야 함. 다른 결과가 나오면 삭제 중단하고 SUMMARY 기록.
2. `grep -rn "TopCalibrationAction\|BottomCalibrationAction\|TopCalibrationParam\|BottomCalibrationParam" --include=*.cs --include=*.xaml WPF_Example/ | grep -v "Action_TopCalibration.cs\|Action_BottomCalibration.cs"` → 0건.
3. `grep -n "Calibration.cs\|Alligator" WPF_Example/DatumMeasurement.csproj` → 0건 (미등록 확인).

재검증 통과 시 `git rm` 또는 파일 삭제로 6개 제거. csproj 는 미등록이므로 수정 불필요 — 확인만 한다.
RecipeFileHelper.cs 는 절대 수정하지 않는다 (bin DLL 의 FileVersionInfo 를 읽는 것이지 이 소스와 무관).
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/18/Insiders/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal -t:Build</automated>
  </verify>
  <done>6개 파일 삭제됨, 위 3개 grep 게이트 통과 기록됨, Debug/x64 빌드 PASS, `git diff --diff-filter=D --name-only` 에 의도한 6개만 나옴. 커밋 완료.</done>
</task>

<task type="auto">
  <name>Task 2: 참조 0 죽은 메서드/필드 제거 (약 400줄)</name>
  <files>
WPF_Example/TcpServer/VisionResponsePacket.cs
WPF_Example/TcpServer/TcpServer.cs
WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
WPF_Example/Halcon/Algorithms/DatumFindingService.cs
WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs
WPF_Example/UI/ContentItem/MainView.xaml.cs
WPF_Example/Device/Camera/Basler/BaslerCamera.cs
WPF_Example/Device/Camera/Hik/HikCamera.cs
  </files>
  <action>
각 항목 삭제 전 `grep -rn "&lt;심볼명&gt;" --include=*.cs --include=*.xaml WPF_Example/` 로 참조 0 재확인. 하나라도 나오면 건너뛰고 SUMMARY 에 기록.

**VisionResponsePacket.cs**: `public static VisionResponsePacket Convert(string msg)` (~96-277) + 이 메서드에서만 쓰이는 헬퍼 `SetOnFromString`(~939-942), `SetSiteStatusFromString`(~720-724) 삭제.
⚠⚠ `public static string Convert(VisionResponsePacket packet)` (~283) 는 살아있다 — `ToString()`(~279) 이 `Convert(this)` 로 호출. 삭제 대상은 **string 을 받아 VisionResponsePacket 을 반환하는** 오버로드뿐. 헷갈리지 말 것.
(참고: 이 메서드 내부의 zone 주석 6곳 ~123-129 등은 함께 삭제됨 — Task 3 에서 중복 처리 금지.)

**TcpServer.cs**: `mSendBuffer` 필드(~95), `SIZE_SEND_BUFFER` const(~66), `SIZE_RECV_BUFFER` const(~65), `mReceiveTimer` 필드(~347) 삭제. 아무도 읽지 않는 껍데기 잔재(가변 List&lt;byte&gt; + mStream.Write 로 이미 정상화됨).

**VisionAlgorithmService.cs**: `RunPhiSmokeTest`(~521), `TryFitArc`(~750, XML doc ~746 포함), `TryIntersectCircleLine`(~885), `TryIntersectContours`(~1122) 삭제. 추가로 `TryFindCircle`(~256) **내부**의 항상 null 인 `circleBorder`(~274 선언, ~333 finally 의 circleBorder.Dispose()) 제거.
⚠ `TryFindCircle`(~256) 자체는 살아있음(CircleDiameterMeasurement.cs:89, CircleCenterDistanceMeasurement.cs:107) — 삭제 금지. TryFitArc 의 XML doc 이 "I9/I10 에서 사용"이라 주장해도 실제 호출부 0 (거짓 주석) → grep 확인 후 삭제.

**DatumFindingService.cs**: 죽은 지역변수 `horect`(~894-895, `TryTeachTwoLineIntersect` 내 GenRectangle2 후 미사용+미Dispose) **딱 이 2줄만.** 나머지는 절대 건드리지 말 것.

**AlignShapeMatchService.cs**: `TryLoadTemplate`(~308, HasTemplate 로 위임하는 얇은 래퍼) 삭제.

**MainView.xaml.cs**: `InvokeTryTeachDatumForEdit`(~1221-1246, 26줄), `ChangeTabPage`(~607-609, 본문 no-op 3줄) 삭제.
⚠ `NotifyDatumParamMaybeChanged`(~1217, return; no-op)는 삭제 금지 — InspectionListView.xaml.cs:125 에 라이브 호출자 존재. XAML 게이트: MainView.xaml 에서 두 심볼의 Click=/Changed= 참조 0 확인.

**BaslerCamera.cs**: `FrameDurationTicks` 필드(~33), 이 필드로만 흘러가는 `RENDERFPS`(~34) 와 ctor 계산(~176-178) 삭제.

**HikCamera.cs**: `FrameDurationTicks`(~35), `ID` 필드(~38 — `this.ID = id` 대입만 있고 읽기 0, 콜백은 파라미터 id 사용), `CopyMemory` DllImport 선언(~26-27, 호출 0), 빈 Dispose if 블록(~286-288 `if(CameraHandle != null) { }`) 삭제.
⚠ `ID` 는 흔한 필드명 — 반드시 이 클래스 범위 참조 0 + 상속/인터페이스 멤버가 아님을 확인한 뒤 삭제.
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/18/Insiders/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal -t:Build</automated>
  </verify>
  <done>각 심볼 참조 0 grep 기록됨, 살아있는 오버로드(Convert(packet)/TryFindCircle/NotifyDatumParamMaybeChanged) 보존됨, XAML 게이트 통과, Debug/x64 빌드 PASS, `git diff --diff-filter=D --name-only` 파일 삭제 0. 커밋 완료.</done>
</task>

<task type="auto">
  <name>Task 3: 주석처리 코드 블록 + 미사용 using 정리 (약 150줄)</name>
  <files>
WPF_Example/TcpServer/VisionRequestPacket.cs
WPF_Example/Device/Camera/Basler/BaslerCamera.cs
WPF_Example/Device/Camera/Hik/HikCamera.cs
WPF_Example/Utility/Ini.cs
WPF_Example/Custom/UI/ViewModel/NodeViewModel.cs
WPF_Example/Halcon/Display/HalconDisplayService.cs
WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
WPF_Example/UI/ViewModel/ModelFinderViewModel.cs
WPF_Example/UI/ViewModel/CalibrationViewModel.cs
WPF_Example/TcpServer/VisionServer.cs
  </files>
  <action>
**주석 블록 삭제 (연속 3줄 이상, 실제 코드가 주석처리된 것만):**
- VisionRequestPacket.cs: zone 파싱 주석 4곳 (~177-183, 200-206, 230-236, 250-256)
- BaslerCamera.cs (~290-295 Balance White Auto)
- HikCamera.cs (~186-194 GigE ip fallback, ~390-392 구 MV_CC_*_NET API)
- Ini.cs (~615-631 `#if JS` 블록 — JS 심볼 미정의라 도달 불가. 이 블록만 예외적으로 삭제 허용)
- NodeViewModel.cs (~20-60 IDragSource/IDropTarget 구현 통째 주석 + 클래스 선언부의 `//, IDragSource, IDropTarget`, ~264-266 SortNodeChildren 호출 주석)

⚠ VisionResponsePacket 의 zone 주석 6곳은 Task 2 에서 이미 삭제됨 — 중복 처리 금지.

**빈 if no-op:**
- InspectionListView.xaml.cs (~1032-1034, 조건 평가 후 본문 주석 한 줄뿐인 빈 블록) 삭제. ⚠ InspectionList_SelectionChanged 구조는 변경 금지(재진입 크래시 이력) — 이 빈 if 블록만.

**미사용 using 제거 (파일마다 실제 사용 여부 grep 후 제거. 확신 없으면 남길 것):**
- HalconDisplayService.cs: `using System.Linq;` (742/743 의 .Min 은 Math.Min 이라 LINQ 아님 — 확인 후 제거)
- MainResultViewerControl.xaml.cs: `using System.Runtime.InteropServices;`
- InspectionListView.xaml.cs: `using System.Threading.Tasks;`
- ModelFinderViewModel.cs, CalibrationViewModel.cs: Collections.Generic / Linq / Text / Threading.Tasks 각 4개
- VisionServer.cs, VisionRequestPacket.cs: Collections.Generic / Linq / Text / Threading.Tasks

각 using 제거 전 해당 파일 내 실제 심볼 사용을 grep 으로 확인한다(예: Linq 는 `.Select/.Where/.Any/.ToList` 등, Threading.Tasks 는 `Task`/`async`). 사용 흔적이 조금이라도 있으면 그 using 은 남긴다.
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/18/Insiders/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal -t:Build</automated>
  </verify>
  <done>주석 블록/빈 if/미사용 using 제거됨(확신 없는 using 은 보존), Task 2 와 중복 처리 없음, Debug/x64 빌드 PASS(제거한 using 이 실제 사용 중이었다면 CS0246/CS1061 로 즉시 드러남 → 원복), `git diff --diff-filter=D --name-only` 파일 삭제 0. 커밋 완료.</done>
</task>

</tasks>

<verification>
- Task 1/2/3 각 그룹 커밋 전 Debug/x64 빌드 PASS (신규 CS 오류 0). 실패 시 커밋하지 말고 원인(신규 CS 오류 vs 환경)을 구분해 보고.
- `git diff --diff-filter=D --name-only`: Task 1 은 의도된 6개만, Task 2/3 은 파일 삭제 0.
- 살아있는 오버로드/심볼 보존: Convert(packet), TryFindCircle, NotifyDatumParamMaybeChanged, [Obsolete] Top/BottomInspectionAction.
- RecipeFileHelper.cs / DatumFindingService(horect 외) / 검출 파라미터 / SetColor / HImage Dispose 무변경.
- 런타임 동작 변경 0.
</verification>

<success_criteria>
- 6개 죽은 파일 삭제 + 참조 0 메서드/필드 제거 + 주석블록/미사용 using 정리 완료
- 3개 atomic commit, 각각 Debug/x64 빌드 PASS
- 재검증 grep 게이트 + XAML 오탐 게이트 통과 기록
- 회귀 0, 계획 밖 파일 수정 0
</success_criteria>

<output>
완료 후 `.planning/quick/260710-f4k-dead-code-sweep/260710-f4k-SUMMARY.md` 생성.

**SUMMARY 에 반드시 기록할 것 (이번 범위 밖 — 수정하지 않고 발견만 보고):**
1. Device/DeviceHandler.cs:83,89 — `if (IDList.Select(id => id.CamType == ECameraType.Basler) != null)`. LINQ Select 는 null 을 반환하지 않아 항상 참. `.Any(...)` 의도로 보임.
2. Device/Camera/Basler/BaslerCamera.cs:660 — `"[ERROR] {0} StopStream. ({2})"` 인자 2개인데 {2} 참조 → catch 블록 안에서 FormatException.
3. Utility/Logging.cs:366 — `Debug.Assert(true, e.Message)` 조건이 상수 true → 절대 미발화 (`false` 의도로 추정).

**추가 기록:** 재검증 중 참조가 발견되어 삭제를 건너뛴 심볼(있으면), 실제 사용 중이라 남긴 using(있으면).
</output>
