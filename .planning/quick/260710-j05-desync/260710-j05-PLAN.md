---
phase: quick-260710-j05
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/Halcon/Models/JobPosition.cs
  - WPF_Example/Halcon/Services/TeachingStorageService.cs
  - WPF_Example/Halcon/Algorithms/PatternMatchService.cs
  - WPF_Example/DatumMeasurement.csproj
autonomous: true
requirements: [DESYNC-01, DEADCODE-01]

must_haves:
  truths:
    - "패턴 모델 생성 성공 시 확인 모달이 뜨고, Yes 선택 시 즉시 Recipe Save 가 실행되어 .shm 과 RefMatch 가 함께 영속화된다"
    - "No/닫기 선택 시 오늘과 완전히 동일하게 동작한다 (경고 남기고 흐름 종료)"
    - "Recipe Save 가 실패해도 티칭 흐름/메모리 RefMatch 상태는 유지되고 로그+경고만 남는다"
    - "미사용 심볼 3종(JobPosition/BuildTeachingPath/TryBuildAlignRigid)이 코드베이스에서 제거된다"
    - "Debug/x64 Rebuild 가 PASS 한다 (csproj Compile 항목 정리 포함)"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "모델 생성 성공부 Recipe Save 확인 모달 (기존 SaveRecipe 경로 재사용)"
      contains: "ShowConfirmation"
    - path: "WPF_Example/DatumMeasurement.csproj"
      provides: "JobPosition.cs Compile Include 제거된 프로젝트 파일"
  key_links:
    - from: "MainView.InvokeCreatePatternModel 성공부"
      to: "MainWindow.SaveRecipe()"
      via: "Window.GetWindow(this) as MainWindow → mw.SaveRecipe() (try/catch 가드)"
      pattern: "mw\\.SaveRecipe\\(\\)"
---

<objective>
티칭 패턴모델 ↔ RefMatch desync 를 차단하고, 참조 0 인 무위험 죽은 코드를 정리한다.

Purpose: "패턴 모델 생성" 시 `.shm` 는 디스크에 즉시 기록되지만 RefMatch 값은 in-memory `DatumConfig` 에만 남아, Recipe Save 없이 종료하면 `.shm`(신규)↔RefMatch(구/0) 불일치로 런타임 정렬이 조용히 틀린다(과거 3faa91b CameraRole Datum 소실과 동형). 모델 생성 성공 시 확인 모달로 Recipe Save 를 유도해 이 창을 닫는다.

Output: 확인 모달 추가(엄격 additive) + 미사용 심볼 3종 삭제.

최우선 제약(사용자 명시): **"되던 것도 안 되면 안 된다." 회귀 0 이 다른 모든 것보다 우선한다.** 조금이라도 기존 동작을 바꿀 소지가 있으면 건너뛰고 SUMMARY 에 기록한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<coding_rules>
프로젝트 코딩 규칙 — executor 는 아래를 매 수정마다 준수한다:
- C# 7.2 / .NET Framework 4.8 — C# 8.0+ 기능(nullable ref, switch expression, record 등) 금지
- 삼항 `?:` 신규 사용 금지 → if-else 로 작성. 기존 삼항은 건드리지 말 것
- 헝가리언 접두사 등 기존 표기 유지
- `MainView.xaml.cs` 는 K&R brace 스타일 — 기존 스타일 유지
- 초보자 가독성 우선
- 수정/추가 라인에 `//260710 hbk` 주석. 순수 삭제 라인은 주석 불필요
</coding_rules>

<interfaces>
확인·검증 완료된 기존 시그니처/패턴 (executor 는 새 로직 없이 이것만 재사용):

CustomMessageBox (UI/Dialog/CustomMessageBox.cs:68):
```csharp
public static MessageBoxResult ShowConfirmation(string title, string message, MessageBoxButton buttons);
public static bool Show(string title, string message); // 기존 알림
```

MainWindow (MainWindow.xaml.cs:265):
```csharp
public void SaveRecipe(string name = null); // Running 가드 + existingFile 보존 경로 내장 (3faa91b)
```

기존 SaveRecipe 재사용 패턴 (MainView.xaml.cs:2351-2353, 그대로 미러):
```csharp
MainWindow mw = Window.GetWindow(this) as MainWindow;
if (mw != null) mw.SaveRecipe();
```

기존 YesNo 확인 패턴 (MainView.xaml.cs:2449-2453):
```csharp
var reteachChoice = CustomMessageBox.ShowConfirmation(title, msg, MessageBoxButton.YesNo);
if (reteachChoice != MessageBoxResult.Yes) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 패턴 모델 생성 성공 시 Recipe Save 확인 모달 추가 (엄격 additive, 회귀 0)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <action>
`InvokeCreatePatternModel` 의 **성공 종료부 단 한 줄**(현재 `MainView.xaml.cs:3027`)만 교체한다. 다른 라인은 한 글자도 바꾸지 않는다.

현재 라인(3027):
```csharp
CustomMessageBox.Show("모델 생성 완료", "패턴 모델 생성·ref pose 기록 완료 (score " + rs.ToString("F3") + ") — Recipe Save 권장" + alignMsg);
```

이것을 아래 블록으로 교체한다 (K&R brace, if-else, `//260710 hbk` 주석):
```csharp
//260710 hbk desync 차단: 모델 생성 성공 시 Recipe Save 확인 모달. Yes → 즉시 저장(.shm↔RefMatch 함께 영속화).
//  No/닫기 → 오늘과 동일(경고만). 저장 실패해도 티칭 상태(메모리 RefMatch) 유지.
MessageBoxResult saveChoice = CustomMessageBox.ShowConfirmation(
    "모델 생성 완료",
    "패턴 모델 생성·ref pose 기록 완료 (score " + rs.ToString("F3") + ")" + alignMsg
        + "\n\n지금 Recipe Save 하시겠습니까? (.shm 모델과 RefMatch 기준값이 함께 저장됩니다.\n현재 미저장 편집이 함께 저장됩니다.)\n\n저장하지 않으면 다음 저장 시점까지 모델↔기준값이 어긋날 수 있습니다.",
    MessageBoxButton.YesNo);
if (saveChoice == MessageBoxResult.Yes) {
    //260710 hbk 기존 SaveRecipe 경로 재사용 (Running 가드 + existingFile 보존, 3faa91b). 새 저장 로직 금지.
    try {
        MainWindow mw = Window.GetWindow(this) as MainWindow;
        if (mw != null) mw.SaveRecipe();
    }
    catch (Exception saveEx) {
        //260710 hbk 저장 실패해도 티칭 흐름/메모리 RefMatch 는 그대로 유지. 로그+경고만.
        Logging.PrintErrLog((int)ELogType.Error, "패턴 모델 Recipe Save 실패: " + saveEx.Message);
        CustomMessageBox.Show("저장 실패", "Recipe Save 실패 — 티칭 상태는 유지됩니다. 수동 저장 필요.\n" + saveEx.Message);
    }
}
else {
    //260710 hbk No/닫기 → 오늘과 동일. Recipe Save 권장 경고 남김.
    CustomMessageBox.Show("Recipe Save 권장", "모델은 기록되었으나 저장하지 않았습니다. 종료 전 Recipe Save 를 권장합니다.");
}
```

주의:
- `Logging`, `ELogType`, `MessageBoxResult`, `MessageBoxButton`, `Window`, `MainWindow` 네임스페이스가 이미 이 파일에서 사용 중인지 확인(2352·2449 라인이 이미 사용 → 추가 using 불필요). 없으면 기존 사용 라인과 동일한 정규화 이름으로 참조.
- `alignMsg` 는 **반드시 보존**하여 모달 메시지에 포함(패턴2 폴백 경고가 담김).
- `ShowConfirmation` catch 는 내부에서 이미 방어됨(CustomMessageBox.cs:69). 여기서 try/catch 는 `SaveRecipe` 호출에만 씌운다.

**아래는 한 줄도 바꾸지 말 것 (회귀 0 가드):**
- `svc.TryCreateModel(...)` / `svc.TryFindRefPose(...)` 호출·인자·순서
- `.shm`/`.ncm` 쓰기 타이밍(즉시 쓰기 그대로)
- `datum.RefMatchRow/Col/AngleDeg`, `datum.RefMatch2Row/Col` 대입
- 패턴2 분기(3002-3023), `alignMsg` 조립 로직 전체
- 실패 경로 3종 문구: `"ref pose 기록 실패"`(3030), `"패턴 2 모델 생성 실패"`(3019 내), `"모델 경로 도출 실패"`(2980)
- `datum.RaisePropertyChanged(string.Empty)`(3025) / `mParentWindow.inspectionList.RefreshParamEditor()`(3026) 호출
- 패턴2 미설정 OKCancel 모달(2967-2975), `EnsurePerRoiDefaults`(2950), ROI 가드(2959·2966)

**실행 전 재확인:** `grep -rn "InvokeCreatePatternModel" WPF_Example/` → 호출부가 `MainView.xaml.cs:2935` 버튼 핸들러 1건뿐인지 확인(모달이 무인 실행을 막지 않음 보장). 2건 이상이면 건너뛰고 SUMMARY 기록.

**절대 건드리지 말 것:** `Custom/EthernetVision/AlignShapeMatchService.cs`(별개 서브시스템, desync 없음), `DatumFindingService.cs`, `ParamBase.cs`, `InspectionListView.InspectionList_SelectionChanged`, `NotifyDatumParamMaybeChanged`/`TryTriggerDatumAutoReteach` 체인.
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/18/Insiders/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal -t:Rebuild</automated>
    추가 회귀 확인(수동, SUMMARY 에 diff 인용): `git diff WPF_Example/UI/ContentItem/MainView.xaml.cs` 에서 `TryCreateModel`/`TryFindRefPose` 호출 라인과 실패 경로 3종 문구(`ref pose 기록 실패`, `패턴 2 모델 생성 실패`, `모델 경로 도출 실패`)가 **변경되지 않았음**을 확인.
  </verify>
  <done>
- Debug/x64 Rebuild PASS.
- `git diff` 에서 추가된 것은 3027 라인 교체 블록뿐이고, 회귀 0 가드 목록의 라인은 무변경.
- 모달 메시지에 `alignMsg` 와 "현재 미저장 편집이 함께 저장됩니다" 문구 포함.
- SaveRecipe 호출이 try/catch 로 감싸짐.
- atomic commit 완료 (message: `fix(quick-260710-j05): 패턴 모델 생성 후 Recipe Save 확인 모달 (desync 차단)`).
  </done>
</task>

<task type="auto">
  <name>Task 2: 참조 0 미사용 심볼 3종 삭제 (JobPosition / BuildTeachingPath / TryBuildAlignRigid)</name>
  <files>WPF_Example/Halcon/Models/JobPosition.cs, WPF_Example/DatumMeasurement.csproj, WPF_Example/Halcon/Services/TeachingStorageService.cs, WPF_Example/Halcon/Algorithms/PatternMatchService.cs</files>
  <action>
**삭제 전 각 심볼 참조 0 을 다시 grep 으로 확인한다. 정의부 외 참조가 하나라도 나오면 그 항목은 건너뛰고 SUMMARY 에 기록한다.**

재확인 명령:
```
grep -rn "JobPosition\|BuildTeachingPath\|TryBuildAlignRigid" --include=*.cs --include=*.xaml WPF_Example/
```
기대: 정의부 3건만 (`JobPosition.cs:7`, `TeachingStorageService.cs:75`, `PatternMatchService.cs:479`).

각 항목:

1. **JobPosition.cs (클래스 전체, 28줄)** — 파일 삭제:
   - `WPF_Example/Halcon/Models/JobPosition.cs` 파일 자체를 삭제한다 (`git rm` 또는 파일 제거).
   - **`WPF_Example/DatumMeasurement.csproj:587` 의 `<Compile Include="Halcon\Models\JobPosition.cs" />` 라인도 함께 제거한다.** (등록돼 있어 빠뜨리면 빌드 깨짐.)

2. **TeachingStorageService.BuildTeachingPath (`:75`)** — `public static string BuildTeachingPath(ParamBase param, string logicalName)` 메서드 블록 전체(75-84 라인) 삭제. 인접 메서드(`Save` 72라인, `BuildFixedTeachingPath` 86라인)는 건드리지 말 것.

3. **PatternMatchService.TryBuildAlignRigid (`:479`)** — `public bool TryBuildAlignRigid(...)` 메서드 블록 전체(XML doc 주석 포함, 470라인 `<param>` 부터 메서드 닫는 `}` 까지) 삭제. `TryComposeAlign` 은 자체 compose 를 사용하므로 무관. 인접 멤버는 건드리지 말 것.

순수 삭제이므로 `//260710 hbk` 주석 불필요. C# 7.2/K&R 무관(삭제만).

**절대 건드리지 말 것:** carry-over 버그 3건(`DeviceHandler.cs:83,89`, `BaslerCamera.cs:660`, `Logging.cs:366`), 확장자 상수 이중정의, `NormalizeTeachingKey`, `MenuBar`/`VersionDefine.cs`/`SkipReason.cs`, 계획에 없는 파일.
  </action>
  <verify>
    <automated>"/c/Program Files/Microsoft Visual Studio/18/Insiders/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal -t:Rebuild</automated>
    삭제 확인:
    - `grep -rn "JobPosition\|BuildTeachingPath\|TryBuildAlignRigid" --include=*.cs --include=*.xaml WPF_Example/` → 0건
    - `grep -n "JobPosition" WPF_Example/DatumMeasurement.csproj` → 0건
    - `git diff --diff-filter=D --name-only` → `WPF_Example/Halcon/Models/JobPosition.cs` 1개만
  </verify>
  <done>
- 3종 심볼 grep 결과 0건, csproj JobPosition 0건.
- 삭제된 파일은 JobPosition.cs 1개뿐.
- Debug/x64 Rebuild PASS.
- atomic commit 완료 (message: `chore(quick-260710-j05): 참조 0 미사용 심볼 3종 삭제 (JobPosition/BuildTeachingPath/TryBuildAlignRigid)`).
  </done>
</task>

</tasks>

<verification>
- Debug/x64 **Rebuild** 두 task 각각 PASS (Git Bash 에서 `-p:` 사용, `/p:` 는 MSB1008 유발).
- Task 1: `git diff` 로 회귀 0 가드 라인 무변경 확인 + 모달/try-catch 추가 확인.
- Task 2: grep 0건 3종 + csproj 0건 + 삭제 파일 1개.
</verification>

<success_criteria>
- 패턴 모델 생성 성공 시 확인 모달 → Yes 저장 / No 오늘과 동일 / 저장 실패 시 티칭 상태 유지.
- 미사용 심볼 3종 제거, 빌드 무손상.
- 회귀 0: 검출·저장·패턴2·실패경로 로직 전부 무변경.
</success_criteria>

<output>
완료 후 `.planning/quick/260710-j05-desync/260710-j05-SUMMARY.md` 를 작성한다.

SUMMARY 에 반드시 포함:
- Task 1: 3027 라인 교체 diff 인용 + 회귀 0 가드(TryCreateModel/TryFindRefPose 호출, 실패 경로 3종 문구) 무변경 확인.
- Task 2: 삭제 3종 grep 0건 결과, csproj 정리 확인.
- 실행 중 건너뛴 항목이 있으면(참조 발견 등) 사유와 함께 기록.
- **육안 UAT 항목(작업자 실측 대기):**
  1. 패턴 모델 생성 → Yes 선택 시 레시피가 실제 저장되는지(.shm↔RefMatch 동기).
  2. No/닫기 선택 시 오늘과 동일하게 동작하는지.
  3. 저장 실패를 유도해도(예: Running 중) 티칭 상태(메모리 RefMatch)가 유지되는지.
</output>
