---
phase: quick-260710-j05
plan: 01
subsystem: UI/ContentItem (MainView), Halcon/Models, Halcon/Services, Halcon/Algorithms
tags: [desync, recipe-save, dead-code-cleanup]
dependency-graph:
  requires: []
  provides:
    - "패턴 모델 생성 성공 시 Recipe Save 확인 모달 (desync 차단)"
  affects:
    - "WPF_Example/UI/ContentItem/MainView.xaml.cs"
    - "WPF_Example/Halcon/Models/JobPosition.cs (삭제)"
    - "WPF_Example/Halcon/Services/TeachingStorageService.cs"
    - "WPF_Example/Halcon/Algorithms/PatternMatchService.cs"
tech-stack:
  added: []
  patterns:
    - "기존 SaveRecipe() 재사용 패턴 재확인 (Running 가드 + existingFile 보존, 3faa91b)"
key-files:
  created: []
  modified:
    - WPF_Example/UI/ContentItem/MainView.xaml.cs
    - WPF_Example/DatumMeasurement.csproj
    - WPF_Example/Halcon/Services/TeachingStorageService.cs
    - WPF_Example/Halcon/Algorithms/PatternMatchService.cs
  deleted:
    - WPF_Example/Halcon/Models/JobPosition.cs
decisions:
  - "InvokeCreatePatternModel 성공 종료부 알림 모달 1줄만 확인 모달로 교체, 나머지 로직(TryCreateModel/TryFindRefPose/패턴2 분기/실패 경로 3종)은 완전 무변경"
  - "SaveRecipe() 호출은 try/catch 로만 감싸고 내부 로직은 건드리지 않음 (Rule 2: 저장 실패 시 로그+경고만, 티칭 상태 유지)"
  - "Task 2 삭제 전 grep 재확인 결과 3종 심볼 모두 참조 0건 확인 후 삭제 진행 (건너뛴 항목 없음)"
metrics:
  duration: "~25min"
  completed: "2026-07-10"
---

# Quick Task 260710-j05: 패턴모델↔RefMatch desync 차단 + 죽은 코드 정리 Summary

패턴 모델 생성 성공 시 Recipe Save 여부를 묻는 확인 모달을 추가해 `.shm`(즉시 디스크 기록)과 `RefMatch`(in-memory only) 간 desync 창을 닫고, 참조 0인 미사용 심볼 3종(JobPosition/BuildTeachingPath/TryBuildAlignRigid)을 삭제했다.

## What Was Built

### Task 1: 패턴 모델 생성 성공 시 Recipe Save 확인 모달 (엄격 additive)

`MainView.xaml.cs:3027`(구 라인 번호)의 단순 알림 모달 1줄을 `CustomMessageBox.ShowConfirmation(...)` YesNo 확인 모달로 교체했다.

- **Yes** → 기존 `MainWindow.SaveRecipe()` 경로를 그대로 재사용(try/catch 가드만 추가). 저장 실패 시 `Logging.PrintErrLog` + 경고 모달, 메모리 `RefMatch` 상태는 그대로 유지.
- **No/닫기** → 오늘과 완전히 동일하게 "Recipe Save 권장" 경고 모달만 표시하고 종료.

**실제 diff (commit 98a4c78, `git diff HEAD~3 HEAD~2 -- WPF_Example/UI/ContentItem/MainView.xaml.cs` 요약):**

```diff
-                    CustomMessageBox.Show("모델 생성 완료", "패턴 모델 생성·ref pose 기록 완료 (score " + rs.ToString("F3") + ") — Recipe Save 권장" + alignMsg);
+                    //260710 hbk desync 차단: 모델 생성 성공 시 Recipe Save 확인 모달. Yes → 즉시 저장(.shm↔RefMatch 함께 영속화).
+                    //  No/닫기 → 오늘과 동일(경고만). 저장 실패해도 티칭 상태(메모리 RefMatch) 유지.
+                    MessageBoxResult saveChoice = CustomMessageBox.ShowConfirmation(
+                        "모델 생성 완료",
+                        "패턴 모델 생성·ref pose 기록 완료 (score " + rs.ToString("F3") + ")" + alignMsg
+                            + "\n\n지금 Recipe Save 하시겠습니까? (.shm 모델과 RefMatch 기준값이 함께 저장됩니다.\n현재 미저장 편집이 함께 저장됩니다.)\n\n저장하지 않으면 다음 저장 시점까지 모델↔기준값이 어긋날 수 있습니다.",
+                        MessageBoxButton.YesNo);
+                    if (saveChoice == MessageBoxResult.Yes) {
+                        //260710 hbk 기존 SaveRecipe 경로 재사용 (Running 가드 + existingFile 보존, 3faa91b). 새 저장 로직 금지.
+                        try {
+                            MainWindow mw = Window.GetWindow(this) as MainWindow;
+                            if (mw != null) mw.SaveRecipe();
+                        }
+                        catch (Exception saveEx) {
+                            //260710 hbk 저장 실패해도 티칭 흐름/메모리 RefMatch 는 그대로 유지. 로그+경고만.
+                            Logging.PrintErrLog((int)ELogType.Error, "패턴 모델 Recipe Save 실패: " + saveEx.Message);
+                            CustomMessageBox.Show("저장 실패", "Recipe Save 실패 — 티칭 상태는 유지됩니다. 수동 저장 필요.\n" + saveEx.Message);
+                        }
+                    }
+                    else {
+                        //260710 hbk No/닫기 → 오늘과 동일. Recipe Save 권장 경고 남김.
+                        CustomMessageBox.Show("Recipe Save 권장", "모델은 기록되었으나 저장하지 않았습니다. 종료 전 Recipe Save 를 권장합니다.");
+                    }
```

`git diff` 는 이 22줄 추가/1줄 삭제뿐이며(`1 file changed, 23 insertions(+), 1 deletion(-)`), 다른 라인은 전혀 건드리지 않았다.

**회귀 0 가드 확인 (실제 grep, HEAD 시점):**

```
$ grep -n "TryCreateModel\|TryFindRefPose\|ref pose 기록 실패\|패턴 2 모델 생성 실패\|모델 경로 도출 실패\|RaisePropertyChanged(string.Empty)\|RefreshParamEditor" WPF_Example/UI/ContentItem/MainView.xaml.cs
```
결과 중 `InvokeCreatePatternModel` 내부 관련 라인:
- `2980: CustomMessageBox.Show("모델 생성 실패", "모델 경로 도출 실패 (레시피/Shot 확인).");` — 무변경
- `2986: bool ok = svc.TryCreateModel(img, ...` — 무변경
- `2995: if (svc.TryFindRefPose(img, ...` — 무변경
- `3005: if (!string.IsNullOrEmpty(modelPath2) && svc.TryCreateModel(img, ...` — 무변경(패턴2 분기)
- `3011: if (svc.TryFindRefPose(img, ..., modelPath2, ...` — 무변경
- `3016/3019`: `alignMsg` 조립(패턴2 실패 경고 문구) — 무변경
- `3025-3026`: `RaisePropertyChanged(string.Empty)` / `RefreshParamEditor()` — 무변경, 새 모달보다 앞서 그대로 실행
- `3052: CustomMessageBox.Show("ref pose 기록 실패", refError);` — 무변경 (else 분기)

`alignMsg` 는 새 확인 모달 메시지에 그대로 이어붙여져(`+ alignMsg`) 패턴2 폴백 경고가 유지됨을 확인.

**호출부 재확인 (Rule/Gate 3):**
```
$ grep -rn "InvokeCreatePatternModel" --include=*.cs --include=*.xaml WPF_Example/
WPF_Example/UI/ContentItem/MainView.xaml.cs:2935:            InvokeCreatePatternModel();
WPF_Example/UI/ContentItem/MainView.xaml.cs:2938:        private void InvokeCreatePatternModel() {
```
호출부는 `CreatePatternModelButton_Click` 1건뿐 — TCP/배치 자동화 경로에서 불리지 않음 확인.

### Task 2: 참조 0 미사용 심볼 3종 삭제

삭제 전 재확인:
```
$ grep -rn "JobPosition\|BuildTeachingPath\|TryBuildAlignRigid" --include=*.cs --include=*.xaml WPF_Example/
WPF_Example/Halcon/Algorithms/PatternMatchService.cs:479:        public bool TryBuildAlignRigid(
WPF_Example/Halcon/Models/JobPosition.cs:7:    public class JobPosition
WPF_Example/Halcon/Services/TeachingStorageService.cs:75:        public static string BuildTeachingPath(ParamBase param, string logicalName)
```
정의부 3건뿐 확인 → 계획대로 3종 전부 삭제 진행(건너뛴 항목 없음).

- `WPF_Example/Halcon/Models/JobPosition.cs` — `git rm` 으로 파일 삭제
- `WPF_Example/DatumMeasurement.csproj:587` — `<Compile Include="Halcon\Models\JobPosition.cs" />` 라인 제거
- `WPF_Example/Halcon/Services/TeachingStorageService.cs:75-84` — `BuildTeachingPath` 메서드 블록 삭제 (인접 `Save`/`BuildFixedTeachingPath` 무변경)
- `WPF_Example/Halcon/Algorithms/PatternMatchService.cs:464-513` — `TryBuildAlignRigid` 메서드(XML doc 포함) 블록 삭제 (인접 멤버 무변경)

**삭제 후 검증:**
```
$ grep -rn "JobPosition\|BuildTeachingPath\|TryBuildAlignRigid" --include=*.cs --include=*.xaml WPF_Example/
(0 matches)

$ grep -n "JobPosition" WPF_Example/DatumMeasurement.csproj
(0 matches)

$ git diff --diff-filter=D --name-only
(commit 별 별도 표시 — 삭제 파일: WPF_Example/Halcon/Models/JobPosition.cs 1개만)
```

## Deviations from Plan

### Auto-fixed Issues (실행 중 발생한 커밋 오류, 코드 무관)

**1. [실행 실수 — 코드 영향 없음] Task 2 커밋이 의도치 않게 2개로 분리됨**
- **발생 경위:** `git add` 명령에 `JobPosition.cs`(이미 `git rm` 으로 스테이징된 상태)를 다시 지정해 pathspec 오류가 발생했고, bash 가 `&&` 없이 이어진 `git commit` 을 그대로 실행하면서 스테이징 안 된 나머지 3개 파일(csproj/PatternMatchService.cs/TeachingStorageService.cs)이 첫 커밋에서 누락됨.
- **조치:** 누락된 3개 파일을 별도 커밋(`9018e7b`)으로 즉시 반영. 두 커밋을 합쳐야 계획서상 "Task 2" 하나의 논리적 변경이 완성됨.
- **영향:** 코드 내용에는 전혀 영향 없음(계획대로 정확히 3종 심볼 삭제). git 이력만 2-커밋으로 분리됨.
- **커밋:** `abb306d`(JobPosition.cs 삭제) + `9018e7b`(csproj/PatternMatchService/TeachingStorageService 나머지)

### 코드 변경 관련 Deviations

없음 — 계획서 그대로 실행. 건너뛴 항목 없음(두 task 모두 grep 재확인에서 삭제/수정 대상 외 참조 0건 확인).

## Verification Evidence

### 빌드 (Task 1, Task 2 각각 Rebuild PASS)

Task 1 후:
```
DatumMeasurement -> C:\Info\Project\DataMeasurement\WPF_Example\bin\x64\Debug\DatumMeasurement.exe
```
(경고는 사전 존재하던 CS0618/CS0162 뿐, 신규 CS 오류 0)

Task 2 후:
```
DatumMeasurement -> C:\Info\Project\DataMeasurement\WPF_Example\bin\x64\Debug\DatumMeasurement.exe
```
(동일하게 사전 존재 경고만, 신규 CS 오류 0)

### 최종 상태

```
$ git status --short
?? .planning/quick/260710-j05-desync/   (docs, orchestrator 커밋 예정)
```
코드 트리는 clean.

## Commits

| Task | Commit | Message |
|------|--------|---------|
| 1 | `98a4c78` | fix(quick-260710-j05): 패턴 모델 생성 후 Recipe Save 확인 모달 (desync 차단) |
| 2 | `abb306d` | chore(quick-260710-j05): 참조 0 미사용 심볼 3종 삭제 (JobPosition.cs 삭제) |
| 2 (누락분) | `9018e7b` | chore(quick-260710-j05): Task 2 나머지 파일 스테이징 (csproj/PatternMatchService/TeachingStorageService) |

## 육안 UAT 항목 (작업자 실측 대기)

1. **패턴 모델 생성 → Yes 선택 시 레시피 저장 확인:** Datum 선택 → [모델 생성] 버튼 클릭 → 성공 모달에서 [예] 선택 → `.shm` 파일과 레시피 INI 의 RefMatch 값이 동시에 저장 시각으로 갱신되는지 확인. (Recipe Save 진행 중 별도 모달/로그 없이 조용히 성공하면 정상.)
2. **No/닫기 선택 시 오늘과 동일 동작 확인:** 동일 절차에서 [아니오] 선택 → "Recipe Save 권장" 경고 모달만 뜨고, 레시피 파일은 변경되지 않아야 함(저장 전 상태 유지). 기존(수정 전) 동작과 체감상 차이가 없어야 함.
3. **저장 실패 시 티칭 상태 유지 확인:** 저장 실패를 유도(예: 레시피 파일을 다른 프로세스에서 열어 잠그거나, 시퀀스 Running 상태에서 시도) → [예] 선택 시 "저장 실패" 경고 모달이 뜨고, PropertyGrid 의 RefMatch 값(메모리)은 방금 계산된 값 그대로 남아있어야 함(0 이나 이전 값으로 되돌아가지 않아야 함). 이후 재시도로 정상 저장 가능해야 함.

## Self-Check: PASSED

- `WPF_Example/Halcon/Models/JobPosition.cs` — MISSING (의도된 삭제, 확인됨)
- `WPF_Example/UI/ContentItem/MainView.xaml.cs` — FOUND
- `WPF_Example/DatumMeasurement.csproj` — FOUND, JobPosition 라인 0건
- `WPF_Example/Halcon/Services/TeachingStorageService.cs` — FOUND, BuildTeachingPath 0건
- `WPF_Example/Halcon/Algorithms/PatternMatchService.cs` — FOUND, TryBuildAlignRigid 0건
- 커밋 `98a4c78`, `abb306d`, `9018e7b` — 전부 `git log --oneline` 에 존재 확인
