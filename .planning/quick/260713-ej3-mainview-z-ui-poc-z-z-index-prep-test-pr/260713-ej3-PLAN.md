---
phase: quick-260713-ej3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/SystemHandler.cs
  - WPF_Example/UI/ContentItem/MainView.xaml
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: true
requirements: [MANUAL-Z-TRIGGER]
must_haves:
  truths:
    - "조작자가 MainView 화면에서 시퀀스와 z_index 를 골라 버튼을 누르면 실제 $PREP+$TEST 내부 경로(ProcessPrep→ProcessTest)가 실행된다"
    - "버튼 클릭 시 조명이 적용되고(ApplyPrepToSequences) 선택 시퀀스가 해당 z_index 로 검사 실행된다"
    - "트리거 결과(성공/실패, 대상 시퀀스, z_index)가 로그와 메시지박스로 표시된다"
    - "임시 UI 패널이 시각적으로 프로덕션 제어부와 명확히 구분되고 '임시/삭제 필요' 표식이 코드+화면에 남는다"
    - "기존 ProcessPrep/ProcessTest/StartAll/Start 및 기존 UI 요소는 시그니처·로직 무변경 (회귀 0)"
  artifacts:
    - path: "WPF_Example/Custom/SystemHandler.cs"
      provides: "임시 wrapper 메서드 DebugManualZTrigger — ProcessPrep+ProcessTest 를 순차 호출"
      contains: "DebugManualZTrigger"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml"
      provides: "임시 수동 Z축 트리거 패널 (ComboBox+z_index 입력+실행 버튼)"
      contains: "ManualZTrigger"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "패널 초기화(시퀀스 채우기)+버튼 클릭 핸들러"
  key_links:
    - from: "MainView.xaml.cs 버튼 핸들러"
      to: "SystemHandler.Handle.DebugManualZTrigger(seqName, zIndex)"
      via: "직접 메서드 호출"
      pattern: "DebugManualZTrigger"
    - from: "DebugManualZTrigger"
      to: "ProcessPrep / ProcessTest"
      via: "PrepPacket/TestPacket 생성 후 private 메서드 호출"
      pattern: "ProcessPrep\\(.*\\).*ProcessTest\\("
---

<objective>
POC 자동 Z축 연동(IAxisController 구현) 전까지 사용할 **임시** 수동 Z축 트리거 UI 를 MainView 에 추가한다.

배경: Z축은 현재 수동 지그(다이얼)로 맞춘다. 조작자가 지그를 특정 z_index 로 맞춘 뒤 "이 z_index 로 검사 돌려봐"라고 소프트웨어에 알릴 방법이 TCP `$PREP`+`$TEST` 패킷 외엔 없다(로컬 UI 트리거 부재). 이 갭을 메우는 로컬 버튼을 추가한다.

Purpose: 수동 지그 ↔ 소프트웨어 사이의 임시 브리지. IAxisController 가 실제 구현되면 이 wrapper 메서드 + UI 패널 전체를 통째로 삭제한다.
Output: DebugManualZTrigger wrapper 메서드 + MainView 하단 임시 트리거 패널.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<constraints>
- 삼항 연산자(`?:`) **금지** — 반드시 if-else 사용.
- 브레이스 스타일: `Custom/SystemHandler.cs` 는 **Allman**(여는 중괄호 새 줄), `MainView.xaml.cs` 는 **K&R**(여는 중괄호 같은 줄). 각 파일의 기존 스타일을 그대로 따를 것.
- 주석에 **날짜+이니셜(`//YYMMDD hbk`) 형식 쓰지 말 것**(2026-06-11 폐기 컨벤션). 대신 명확한 한글 문장으로 "임시/TEMP" 성격 + 삭제 조건을 명시.
- 회귀 0: `ProcessPrep`/`ProcessTest`/`StartAll`/`Start` 등 프로덕션 경로는 무변경, "호출"만 추가. 기존 UI 요소 무변경, 순수 추가만.
</constraints>

<interfaces>
<!-- 아래 시그니처/필드는 이미 조사 완료. executor 는 재조사 없이 그대로 사용. -->

WPF_Example/Custom/SystemHandler.cs (partial class SystemHandler):
```csharp
// 둘 다 private — UI 에서 직접 호출 불가. 새 wrapper 에서만 호출한다.
private PrepAckPacket ProcessPrep(PrepPacket packet)   // ~700행. Op=1 이면 _lastPrepZIndex=ZIndex 저장 + ApplyPrepToSequences(ZIndex)(조명 적용). PrepAckPacket.IsOk 로 성공 여부.
private bool ProcessTest(TestPacket packet)            // ~200행. IsRecipeReady false 면 즉시 false. packet.TestID = _lastPrepZIndex 로 덮어씀. IsDynamicFAIMode 면 Sequences[packet.Identifier].StartAll(packet), 아니면 Sequences.Start(packet).
```

WPF_Example/SystemHandler.cs:
```csharp
public static SystemHandler Handle;   // line 28, 싱글턴
public SequenceHandler Sequences;     // line 46
```

WPF_Example/TcpServer/VisionRequestPacket.cs:
```csharp
public class VisionRequestPacket {         // base
    public string Sender { get; set; }
    public string Identifier { get; set; } // 시퀀스 이름 문자열 (ProcessTest 가 Sequences[Identifier] 조회에 사용)
    public int Site { get; set; }          // ACK echo 용, 라우팅에 미사용
}
public class PrepPacket : VisionRequestPacket {
    public int ZIndex { get; set; }
    public int Op { get; set; } = 1;       // 1=ON / 0=OFF
    public PrepPacket() : base(VisionRequestType.Prep) { }
}
public class TestPacket : VisionRequestPacket {
    public int TestType { get; set; }
    public string TestID { get; set; }     // ProcessTest 가 _lastPrepZIndex 로 덮어씀 — 직접 세팅 불필요
    public string Identifier2 { get; set; }// 비동적 Start(packet) 경로에서만 action 이름으로 사용. 동적(StartAll) 경로 미사용.
    public int IndexNumber { get; set; } = SENTINEL_NO_MATERIAL; // -1 = 자재없음
    public string Type { get; set; } = "";
    public TestPacket() : base(VisionRequestType.Test) { }
}
```

WPF_Example/Sequence/SequenceHandler.cs:
```csharp
public int Count { get; }                       // line 72
public SequenceBase this[int index] { get; }    // line 125
public SequenceBase this[string name] { get; }  // line 138
public bool IsDynamicFAIMode { get; }
```

WPF_Example/Sequence/Sequence/SequenceBase.cs:
```csharp
public string Name { get; private set; }        // line 40, 시퀀스 표시 이름
public bool StartAll(TestPacket packet)         // line 342. 동적 FAI 경로. RequestPacket=packet 저장 후 action 0..N 순차 실행. TestType/IndexNumber/Type/Identifier2 미소비(트리거엔 무관, 다운스트림 결과리포트용).
```

MainView.xaml.cs (partial class MainView : UserControl):
```csharp
private SequenceHandler pSeq;   // line 32, 이미 존재. SystemHandler.Handle.Sequences 참조.
// 진입점: MainView_Loaded (XAML Loaded="MainView_Loaded")
```

**핵심 확정 사실 (StartAll/Start 구현 조사 결과):**
- 이 프로젝트는 IsDynamicFAIMode(Shot-FAI 동적) → ProcessTest 는 `Sequences[Identifier].StartAll(packet)` 경로를 탄다.
- StartAll 은 오직 `RequestPacket=packet` 저장 + action 0..N 실행만 함. **TestType / IndexNumber / Type / Identifier2 는 검사 트리거 흐름에 영향 없음** (자재번호/Type 은 다운스트림 결과 전송에만 쓰임).
- 따라서 수동 트리거용 TestPacket 안전 기본값: `Identifier = seqName` 만 세팅. TestType=0(기본), IndexNumber=-1(SENTINEL_NO_MATERIAL, 자재없음), Type=""(기본) 은 기본값 그대로 두면 됨. TestID 는 세팅하지 말 것(ProcessTest 가 _lastPrepZIndex 로 덮어씀).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Custom/SystemHandler.cs 에 임시 wrapper 메서드 DebugManualZTrigger 추가</name>
  <files>WPF_Example/Custom/SystemHandler.cs</files>
  <action>
partial class SystemHandler 안, 기존 ProcessTest/ProcessPrep 근처(예: ProcessPrep 정의 바로 위 또는 아래)에 새 internal 메서드를 **추가만** 한다. 기존 메서드는 절대 수정하지 않는다.

시그니처: `internal bool DebugManualZTrigger(string seqName, int zIndex)`

동작(순서대로):
1. seqName 유효성 확인 — null/빈 문자열이면 로그 남기고 false 반환.
2. `PrepPacket prepPacket = new PrepPacket();` 생성 후 `prepPacket.ZIndex = zIndex;` `prepPacket.Op = 1;` (오브젝트 이니셜라이저 대신 필드 개별 대입도 무방 — 파일 스타일 유지). ZIndex/Op 만 세팅(Site/Sender 미세팅 — 라우팅 미사용).
3. `PrepAckPacket ack = ProcessPrep(prepPacket);` 호출 → 조명 적용 + `_lastPrepZIndex` 저장이 실제 경로로 발생.
4. ack 이 null 이거나 `ack.IsOk == false` 면 로그(`Logging.PrintLog((int)ELogType.Error, ...)`)로 PREP 실패를 남기되, **TEST 는 그대로 진행하지 말고 여기서 false 반환**(조명/z_index 준비 실패 상태로 검사 돌리면 오동작). if-else 로 분기.
5. `TestPacket testPacket = new TestPacket();` 생성 후 `testPacket.Identifier = seqName;` 만 세팅. TestID/TestType/IndexNumber/Type/Identifier2 는 손대지 말 것(기본값이 안전. 근거: interfaces 블록의 StartAll 조사 결과 — 동적 FAI 경로에서 이 필드들은 트리거에 무관).
6. `bool bTestOk = ProcessTest(testPacket);` 호출.
7. 성공/실패를 `Logging.PrintLog` 로 남긴다 — 어떤 시퀀스(seqName)가 어떤 z_index 로 트리거됐는지, PREP IsOk 와 TEST 결과를 포함. 예: "[임시 수동Z트리거] 시퀀스={0} z_index={1} PREP={2} TEST={3}".
8. `return bTestOk;`

메서드 위에 **임시 표식 주석**을 반드시 단다(날짜+이니셜 형식 금지, 한글 문장으로):
```
// [임시 / TEMP] 수동 Z축 트리거 브리지.
// 존재 이유: Z축을 아직 수동 지그(다이얼)로 맞추는 단계라, 조작자가 맞춘 z_index 로
//   검사를 돌리도록 알리는 로컬 경로가 없다($PREP+$TEST TCP 패킷만 존재). 이 wrapper 는
//   그 두 패킷을 코드로 만들어 실제 처리 경로(ProcessPrep→ProcessTest)로 그대로 흘려보낸다.
// 삭제 조건: POC 장비의 자동 Z축(IAxisController) 실제 구현이 완료되면
//   이 메서드 전체 + MainView 의 수동 Z 트리거 UI 패널 전체를 삭제할 것.
// 주의: ProcessPrep/ProcessTest 는 프로덕션 TCP 경로 — 시그니처/로직 변경 금지, 호출만 한다.
```

브레이스는 Allman(이 파일 스타일). 삼항 금지 — 모든 분기 if-else.
  </action>
  <verify>
    <automated>MISSING — Task 3 의 MSBuild Debug|x64 빌드 PASS 로 검증. 별도 단위테스트 프레임워크 없음.</automated>
  </verify>
  <done>DebugManualZTrigger(string,int) 가 internal 로 추가되고, 내부에서 PrepPacket→ProcessPrep→(성공 시)TestPacket→ProcessTest 를 호출하며, 임시/삭제조건 한글 주석이 붙어 있다. 기존 ProcessPrep/ProcessTest 무변경.</done>
</task>

<task type="auto">
  <name>Task 2: MainView 하단에 임시 수동 Z 트리거 패널 추가 (XAML + 코드비하인드)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml, WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <action>
**A. MainView.xaml (순수 추가, 기존 Grid.Row 값 무변경):**

루트 `<Grid>` 는 line 132~ 에 있고 RowDefinitions(line 133-138)는 현재 4행(0=툴바 Auto, 1=이미지 6*, 2=GridSplitter 5, 3=DataGrid 4*).

1. RowDefinitions 맨 끝에 `<RowDefinition Height="Auto"/>` 를 **추가**한다(5번째 행, index 4). 기존 4개 행 정의는 그대로 둔다 → 기존 요소 Grid.Row 0~3 전부 무변경(순수 additive).
2. 루트 Grid 의 자식 끝부분(닫는 `</Grid>` 직전)에 임시 패널 Border 를 Grid.Row="4" 로 추가한다.

**중요 — airspace 회피:** 이미지 뷰어(HWindowControlWPF/HWindowControlWPF 호스팅)는 Row 1 에 있고 그 위에 WPF 오버레이를 얹으면 airspace 로 가려진다. 새 패널은 반드시 **Row 4(WPF 전용 영역, HALCON 창 아님)** 에 독립 배치할 것 — Row 1 위에 겹치지 말 것.

패널 구성(Border x:Name="panel_ManualZTrigger" Grid.Row="4"):
- 눈에 띄는 경고성 배경색(예: Background="#4A2A00" 다크앰버 계열)+테두리로 프로덕션 제어부와 시각적으로 확실히 구분.
- 가로 StackPanel(Orientation="Horizontal") 안에:
  - Label/TextBlock: "[임시 테스트용 — POC 자동화(IAxisController) 전까지만 사용]" (경고색 Foreground).
  - Label "시퀀스:" + `<ComboBox x:Name="combo_ManualZSeq" Width="160"/>`
  - Label "z_index:" + `<TextBox x:Name="txt_ManualZIndex" Width="60" Text="0"/>`
  - `<Button x:Name="btn_ManualZTrigger" Content="수동 트리거 실행" Click="ManualZTriggerButton_Click"/>`
- XAML 주석으로 남길 것(한글, 날짜+이니셜 금지):
  `<!-- [임시 / TEMP] 수동 Z축 트리거 패널. 자동 Z축(IAxisController) 구현 완료 시 이 Border 전체 + RowDefinition(Row 4) 삭제. -->`

기존 canvasToolbar/이미지/DataGrid 등 다른 요소는 절대 수정하지 말 것.

**B. MainView.xaml.cs (K&R 스타일, 순수 추가):**

1. ComboBox 채우기: 기존 진입점 `MainView_Loaded` 핸들러(있음) 안 **끝부분에** `PopulateManualZSeqCombo();` 호출 한 줄만 추가하거나, 별도 초기화 지점이 애매하면 핸들러 내 안전한 위치에 추가. 기존 로직은 건드리지 말 것.
2. 새 private 메서드 `PopulateManualZSeqCombo()`:
   - `SystemHandler.Handle` 또는 `SystemHandler.Handle.Sequences` null 가드(초기화 전 호출 대비) — null 이면 그냥 return.
   - `combo_ManualZSeq.Items.Clear();` 후 `Sequences.Count` 만큼 루프 돌며 `Sequences[i].Name` 을 Items 에 Add. (이미 존재하는 `pSeq` 필드가 세팅돼 있으면 그걸 써도 됨 — 없으면 `SystemHandler.Handle.Sequences` 직접 사용.)
   - Count>0 이면 `combo_ManualZSeq.SelectedIndex = 0;`
3. 새 이벤트 핸들러 `private void ManualZTriggerButton_Click(object sender, RoutedEventArgs e)`:
   - 선택 시퀀스 확인: `combo_ManualZSeq.SelectedItem` null 이면 메시지박스("시퀀스를 선택하세요")+return.
   - z_index 파싱: `int.TryParse(txt_ManualZIndex.Text, out int nZIndex)` 실패 시 메시지박스("z_index 는 정수")+return. (삼항 금지 — if-else.)
   - `string seqName = combo_ManualZSeq.SelectedItem.ToString();`
   - `bool bOk = SystemHandler.Handle.DebugManualZTrigger(seqName, nZIndex);`
   - 결과 표시: `Logging.PrintLog(...)` + `MessageBox.Show(...)` 로 성공/실패 + 시퀀스명 + z_index 안내. if-else 로 성공/실패 분기.
   - try/catch(Exception ex) 로 감싸고 예외는 로그+메시지박스.
4. 메서드/핸들러 위에 한글 임시 주석:
   `// [임시 / TEMP] 수동 Z 트리거. IAxisController 자동 Z축 구현 완료 시 이 메서드/핸들러 + XAML 패널 삭제.`

삼항 금지, K&R 브레이스, 기존 코드 무수정·순수 추가.
  </action>
  <verify>
    <automated>MISSING — Task 3 의 MSBuild Debug|x64 빌드 PASS 로 검증(XAML 컴파일 포함).</automated>
  </verify>
  <done>MainView 하단(Row 4)에 시각적으로 구분되는 임시 패널(ComboBox+z_index TextBox+실행 버튼)이 순수 additive 로 추가되고, Loaded 시 시퀀스 목록이 채워지며, 버튼 클릭이 DebugManualZTrigger 를 호출해 로그+메시지박스로 결과를 표시한다. XAML/코드비하인드 양쪽에 임시/삭제조건 한글 주석이 있다.</done>
</task>

<task type="auto">
  <name>Task 3: Debug|x64 빌드 검증 + 로직 정적 재확인</name>
  <files></files>
  <action>
1. MSBuild Debug|x64 빌드 실행(경로 확정):
   `"C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal /nologo`
   — 빌드 PASS(에러 0) 필수. 경고는 신규 추가분에서 늘지 않았는지 확인.
2. 정적 로직 재확인(회귀 0 게이트):
   - `ProcessPrep`/`ProcessTest`/`StartAll`/`SequenceHandler.Start` 시그니처·본문이 변경되지 않았는지 grep/diff 로 확인.
   - DebugManualZTrigger 가 PREP 실패 시 TEST 를 진행하지 않고 false 반환하는지 확인.
   - TestPacket 에 TestID 를 직접 세팅하지 않았는지(ProcessTest 가 덮어씀) 확인.
   - 삼항 연산자(`?:`)가 신규 코드에 없는지 확인.
3. (선택, 가능하면) SIMUL_MODE Debug 로 앱 실행 → 임시 패널에서 시퀀스+z_index 선택 후 버튼 클릭 → 로그에 "어떤 시퀀스가 어떤 z_index 로 트리거됐는지 + PREP/TEST 결과"가 남고 조명 적용(ApplyPrepToSequences)이 발생하는지 확인. 앱 실행이 어려우면 이 단계는 생략하고 빌드 PASS + 정적 재확인으로 마무리.
  </action>
  <verify>
    <automated>"C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal /nologo</automated>
  </verify>
  <done>Debug|x64 빌드 PASS(에러 0). 프로덕션 경로(ProcessPrep/ProcessTest/StartAll/Start) 무변경 확인. 신규 코드에 삼항 없음. (앱 실행 가능 시) 버튼 클릭으로 시퀀스+z_index 트리거 + 조명 적용이 로그로 확인됨.</done>
</task>

</tasks>

<verification>
- Debug|x64 MSBuild 빌드 PASS (XAML + C# 컴파일).
- DebugManualZTrigger 가 ProcessPrep(조명+z_index 저장)→ProcessTest(검사 트리거)를 실제 경로로 호출.
- MainView 하단에 시각 구분된 임시 패널, 시퀀스 목록 채워짐, 버튼→wrapper 호출→로그/메시지박스.
- 기존 ProcessPrep/ProcessTest/StartAll/Start + 기존 UI 무변경(회귀 0).
- 신규 코드 삼항 없음, 파일별 브레이스 스타일 준수, 임시/삭제조건 한글 주석 존재(날짜+이니셜 형식 미사용).
</verification>

<success_criteria>
- 조작자가 MainView 에서 시퀀스+z_index 선택 후 "수동 트리거 실행"을 누르면 실제 $PREP+$TEST 내부 경로가 실행되어 조명 적용 + 해당 z_index 검사가 돈다.
- 결과가 로그+메시지박스로 표시된다.
- IAxisController 구현 시 삭제 대상(wrapper 메서드 + UI 패널)이 주석으로 명확히 표식돼 있다.
- Debug|x64 빌드 PASS, 회귀 0.
</success_criteria>

<output>
After completion, create `.planning/quick/260713-ej3-mainview-z-ui-poc-z-z-index-prep-test-pr/260713-ej3-SUMMARY.md`
</output>
