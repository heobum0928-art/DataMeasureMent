---
phase: 260714-d99
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
autonomous: false
requirements: [BUG-01]

must_haves:
  truths:
    - "TryFitLine 이 strip 성공/실패 개수를 Trace 로그로 남긴다 (성공 N / 전체 stripCount)"
    - "FitLineContourXld 잔차(df)가 로그에 기록된다 (기존에는 받아놓고 버려졌음)"
    - "strip 커버리지가 50% 미만이면 Error 로그로 경고가 남는다 — 단 측정은 그대로 계속 진행된다"
    - "판정 로직 무변경: 신규 return false 0건, edgeCount < 2 게이트 그대로"
    - "TryFitLine public 시그니처 무변경 → 호출부 19곳 무수정, Debug|x64 빌드 PASS"
  artifacts:
    - path: "WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs"
      provides: "AppendStrip 실패 관측화 + TryFitLine strip 통계/잔차 로깅"
      contains: "Logging.PrintLog"
  key_links:
    - from: "VisionAlgorithmService.AppendStrip"
      to: "VisionAlgorithmService.TryFitLine strip 카운터"
      via: "반환값(EStripResult)"
      pattern: "AppendStrip\\("
    - from: "VisionAlgorithmService.TryFitLine"
      to: "Logging (ELogType.Trace / ELogType.Error)"
      via: "Logging.PrintLog((int)ELogType.Trace, ...)"
      pattern: "Logging\\.PrintLog\\(\\(int\\)ELogType\\."
---

<objective>
`VisionAlgorithmService.TryFitLine` 의 strip-loop 은폐를 **관측 가능**하게 만든다.

`AppendStrip`(211~251행)은 현재 `void` 반환 + 본문 전체 `try{}catch{}` + 빈 결과 시 조용한 `return` 이라, 20개 strip 중 19개가 실패해도 카운트·로그·반환값이 전혀 없다. `TryFitLine` 의 유일한 검증은 `edgeCount < 2`(150행)뿐이고, `FitLineContourXld` 가 주는 잔차 `df`(159행)는 받아놓고 버려진다. 이 파일 전체에 Logging 호출이 0건이라 현장 strip 성공률을 아무도 모른다.

Purpose: **1단계 = 관측 전용.** 판정 로직(`edgeCount < 2` 게이트, `return false` 조건, 반환 로직)은 **일절 건드리지 않는다.** 임계값 상향은 심층검증에서 유해로 판정되어 범위에서 제외됐다. 회귀 0 이 절대 조건인 산업용 검사 시스템이다.
Output: `VisionAlgorithmService.cs` 단일 파일. strip 성공률 + 피팅 잔차(df)가 Trace 로그에 남고, 커버리지 저조 시 Error 경고 1줄이 추가된다.

**범위 밖 (절대 금지 — 심층검증에서 나온 함정):**
- `edgeCount < 2` 임계값 변경 금지 (`< N` 상향 금지 — 무효하고 유해)
- 신규 `return false` 조건 추가 금지
- 레시피 파라미터(ParamBase 프로퍼티)로 임계값 노출 금지 (`ParamBase.Load` 가 누락 키를 0 으로 클로버 → 현장 게이트 통째 무력화)
- CSV 헤더(`MeasurementHistoryCsvWriter` 등) 변경 금지 (고정 상수 → 파서 하위호환 파괴). 로그로 충분
- `TryFitLine` public 시그니처 변경 금지 (호출부 19곳: EdgeToLineDistance / LineToLineAngle / ArcLineIntersect / DualImage 등)
- `FAIEdgeMeasurementService` / `DatumFindingService` 수정 금지 (계열 결함이나 동시 수정 시 회귀 원인 분리 불가)
- 버그 2(Stop 후 Step 미리셋) = NOT_A_BUG, 버그 3(Fail 덮어쓰기) = 범위 제외. **SequenceBase.cs 를 열지 말 것**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs

**로그 포맷 선례 (그대로 미러할 것)** — `WPF_Example/Halcon/Algorithms/DatumFindingService.cs:1720-1723`:
```csharp
int edgeCount = allRows.TupleLength();
Logging.PrintLog((int)ELogType.Trace,
    string.Format("[Datum.{0}] strip-loop accumulated {1} edge points across {2} strips",
        lbl, edgeCount, stripCount));
```

<interfaces>
현재 `VisionAlgorithmService.cs` usings (1~3행): `System`, `System.Collections.Generic`, `HalconDotNet`.
로깅 타입은 **다른 네임스페이스**에 있어 using 추가가 필요하다:
- `Logging` (static class) → `ReringProject.Utility` (WPF_Example/Utility/Logging.cs:20,26)
- `ELogType` (enum: `Trace = 0` … `Error = 6`) → `ReringProject.Setting` (WPF_Example/Setting/SystemSetting.cs:18-26)
- 호출 관례: `Logging.PrintLog((int)ELogType.Trace, string)` — ELogType 을 int 로 캐스팅하는 것이 이 프로젝트 관례

`AppendStrip` 호출부는 **정확히 2곳** (grep 확인 완료):
- `VisionAlgorithmService.cs:129` — scanHorizontal 루프
- `VisionAlgorithmService.cs:140` — else 루프
`private` 메서드이므로 시그니처 변경이 안전하다.

`FitLineContourXld` 잔차: 157~159행에서 `out df` 로 받지만 **현재 완전히 버려짐**. `df` 는 `HTuple` → 접근 전 `df != null && df.Length > 0` 가드 후 `df.D` 사용.
</interfaces>

**프로젝트 코딩 규칙 (강제):**
- **삼항 연산자 `?:` 금지 → if-else 만 사용**
- C# 7.2 전용 (record / `??=` / `using var` / switch expression 금지)
- 브레이스: 이 파일은 **Allman** (여는 중괄호 새 줄) — 준수
- **`//YYMMDD hbk` 날짜+이니셜 주석 형식 금지** (2026-06-11 폐기). 파일 안에 옛 주석이 남아 있어도 **새 주석은 그 형식을 쓰지 말 것**. 주석은 비자명한 "왜"만 한글로 간결히
- 예외를 상위로 던지지 말 것 — `AppendStrip` 의 `catch` 를 **제거하지 말 것**. 실패는 반환값으로만 알린다
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppendStrip 실패 관측화 + TryFitLine strip 통계/잔차 로깅</name>
  <files>WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs</files>
  <action>
**1) usings 추가 (1~3행 블록)**
```csharp
using ReringProject.Setting;   // ELogType
using ReringProject.Utility;   // Logging
```

**2) strip 결과 구분 enum 추가** (`VisionAlgorithmService` 클래스 내부, `AppendStrip` 바로 위)
"예외 실패"와 "에지 0개 실패"를 구분해야 하므로 bool 로는 부족하다. out 파라미터/ref 카운터보다 enum 이 단순하다. **과설계 금지 — 멤버 3개로 끝.**
```csharp
// strip 결과 구분: 에지 0개(NoEdge)와 예외(Failed)를 나눠 세기 위함
private enum EStripResult
{
    Ok = 0,
    NoEdge = 1,
    Failed = 2,
}
```

**3) `AppendStrip`(211~251행) 시그니처를 `private void` → `private EStripResult` 로 변경**
- `TupleConcat` 2회까지 정상 도달 → `return EStripResult.Ok;`
- 빈 결과(236~239행: `edgeRows.TupleLength() <= 0 || edgeCols.TupleLength() <= 0`) → `return EStripResult.NoEdge;`
- `catch`(243~245행) → `return EStripResult.Failed;` (**catch 블록 자체는 유지** — 예외가 상위로 튀면 기존 동작이 깨진다)
- `finally` 의 measureHandle/stripRegion 정리는 그대로 유지
- 메서드 상단 주석의 "strip 실패(빈 결과 / 예외)는 swallow" 문구를 "swallow 하되 결과를 반환한다(호출자가 카운트)"는 취지로 갱신
- Halcon 호출 순서/인자(GenRectangle1 → SmallestRectangle2 → GenMeasureRectangle2 → MeasurePos)는 **무변경**

**4) `TryFitLine` 의 두 strip 루프(123~144행)에서 결과 카운트**
루프 진입 전(120~121행 `allRows`/`allCols` 선언 근처)에 카운터 3개:
```csharp
int okStrips = 0;
int noEdgeStrips = 0;
int failedStrips = 0;
```
각 루프 안에서 `AppendStrip(...)` 반환값을 받아 **if-else** 로 분기 누적 (삼항 금지):
```csharp
EStripResult sr = AppendStrip(image, r1, left, r2, right, imageWidth, imageHeight,
    Math.Max(0.4, sigma), Math.Max(1, threshold), pol, measurePhi, measureSel,
    ref allRows, ref allCols);
if (sr == EStripResult.Ok)
{
    okStrips++;
}
else if (sr == EStripResult.NoEdge)
{
    noEdgeStrips++;
}
else
{
    failedStrips++;
}
```
(else 루프(140행)도 동일 패턴. **인자는 기존 그대로** — 순서/값 변경 금지.)

**5) Trace 로그 #1 — `edgeCount` 계산 직후(148행 아래), `if (edgeCount < 2)` 게이트 앞**
게이트 앞에 두어야 실패 경로에서도 통계가 남는다. `DatumFindingService.cs:1721-1723` 포맷 미러:
```csharp
Logging.PrintLog((int)ELogType.Trace,
    string.Format("[FitLine] strips ok {0}/{1} (noEdge {2}, failed {3}) -> {4} edge points",
        okStrips, stripCount, noEdgeStrips, failedStrips, edgeCount));
```

**6) 커버리지 경고 — Trace 로그 #1 바로 뒤, 역시 게이트 앞**
성공 strip 이 절반 미만이면 Error 로그 1줄. **`return false` 하지 말 것 — 측정은 그대로 계속 진행한다. 이것이 1단계의 핵심(판정 무변경).**
```csharp
// 커버리지 저조 = 피팅 신뢰도 경고. 판정은 바꾸지 않고 로그만 남긴다(관측 전용 단계).
if (okStrips * 2 < stripCount)
{
    Logging.PrintLog((int)ELogType.Error,
        string.Format("[FitLine] low strip coverage: ok {0}/{1} (noEdge {2}, failed {3}) sigma={4:F2} threshold={5} polarity={6}",
            okStrips, stripCount, noEdgeStrips, failedStrips, sigma, threshold, pol));
}
```

**7) Trace 로그 #2 — 잔차 df (157~159행 `FitLineContourXld` 호출 뒤, `row1 = lr1.D;` 할당 근처)**
`df` 는 158행 이후에만 존재하므로 로그를 2회로 나눈다. HTuple 가드 필수(삼항 금지 → if-else):
```csharp
double fitResidual = -1.0;   // df 부재 sentinel
if (df != null && df.Length > 0)
{
    fitResidual = df.D;
}
Logging.PrintLog((int)ELogType.Trace,
    string.Format("[FitLine] fit residual(df)={0:F4} from {1} edges (strips ok {2}/{3})",
        fitResidual, edgeCount, okStrips, stripCount));
```
`df` 는 **기록만** 한다 — 판정에 사용하지 않는다.

**변경하지 않는 것 (git diff 로 증명되어야 함):**
- `if (edgeCount < 2) { error = ...; return false; }` — 조건·본문 그대로
- `TryFitLine` public 시그니처, 반환 로직(`return true` / `catch → return false`), `finally` contour dispose
- `SortAndTrimPercent` 호출 위치·인자
- `collectedEdges` opt-in 블록
  </action>
  <verify>
    <automated>MSYS2_ARG_CONV_EXCL="*" "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" -p:Configuration=Debug -p:Platform=x64 -t:Build -nologo -v:m -clp:ErrorsOnly</automated>
    <automated>git diff -U0 -- WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs | grep -E "^\+" | grep -c "return false" ; echo "위 값이 0 이어야 함 (신규 return false 0건)"</automated>
  </verify>
  <done>
- Debug|x64 빌드 PASS (에러 0)
- `AppendStrip` 이 `EStripResult` 반환, 호출부 2곳 모두 반환값 사용 (grep 상 호출부 여전히 2곳)
- `Logging.PrintLog` 3곳 존재 (Trace 2 + 조건부 Error 1)
- `git diff` 상 신규 `return false` 0건, `edgeCount < 2` 라인 무변경, `TryFitLine` 시그니처 무변경
- 신규 삼항 연산자 `?:` 0건
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 판정 무변경 증명 + 로그 실측 확인</name>
  <what-built>
`VisionAlgorithmService.TryFitLine` 의 strip-loop 관측성을 확보했습니다.

지금까지는 ROI 를 20조각(strip)으로 쪼개 에지를 찾는데, 그중 19개가 실패해도 아무 흔적 없이 넘어갔습니다 (에지 2개만 모이면 직선이 그려짐). 이번 수정으로 **몇 개 strip 이 성공했는지 / 라인 피팅이 얼마나 잘 맞았는지(잔차 df)** 가 로그에 남고, 성공률이 절반 미만이면 Error 로그로 경고가 뜹니다.

**판정 로직은 한 줄도 바꾸지 않았습니다.** 통과/실패 기준, 측정값(mm), TCP 결과($RESULT P/F/B) 모두 수정 전과 100% 동일해야 합니다. 늘어난 것은 로그뿐입니다. (임계값 상향은 심층검증에서 유해로 판정되어 다음 단계로 미뤘습니다.)
  </what-built>
  <how-to-verify>
**증명 1 — 판정 무변경 (코드 레벨, 필수)**
```
git diff WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
```
확인 사항:
1. 추가된 `return false` 가 **0건**인가?
2. `if (edgeCount < 2)` 라인이 **그대로**인가? (`< 3` / `< 5` 등으로 바뀌지 않았는가)
3. `public bool TryFitLine(...)` 시그니처가 **그대로**인가? (호출부 19곳 무수정)
4. 추가된 코드가 전부 카운터(`okStrips` 등) / `Logging.PrintLog` / `EStripResult` enum 뿐인가?
5. `SequenceBase.cs` 등 다른 파일이 diff 에 **없는가**?

**증명 2 — 실제 로그 확인 (SIMUL 또는 실HW)**
1. 앱 실행 → 기존 레시피 로드 → 라인 측정이 포함된 FAI 항목으로 검사 1회 실행
2. Trace 로그에서 다음 2줄이 찍히는지 확인:
   - `[FitLine] strips ok N/20 (noEdge X, failed Y) -> M edge points`
   - `[FitLine] fit residual(df)=0.xxxx from M edges (strips ok N/20)`
3. **판정 결과가 수정 전과 동일한지** — 같은 이미지/레시피에서 측정값(mm)과 OK/NG 가 그대로인지 비교
4. (선택) strip 성공률이 낮은 항목이 있다면 Error 로그에 `low strip coverage` 경고가 뜨는지 확인. **이 경고가 떠도 측정은 계속 진행되고 결과가 나와야 정상입니다.**

**핵심 판단 기준:** 로그가 늘었는데 판정 결과가 하나라도 달라졌다면 → 회귀. 즉시 보고.
  </how-to-verify>
  <resume-signal>Type "approved" 또는 발견한 문제를 설명하세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 카메라 이미지 → Halcon 측정 | 로컬 HW 입력. 본 변경은 입력 처리 경로를 바꾸지 않음 |
| 측정 코드 → 로그 파일 | 로컬 디스크 쓰기. 측정 루프당 신규 로그 최대 3줄 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-d99-01 | D (DoS) | TryFitLine 내 Logging.PrintLog | mitigate | 로그를 strip 루프 **바깥**에 배치 → 측정 1회당 최대 3줄(strip 20회당 1줄 꼴). DatumFindingService 기존 로깅과 동일 밀도. 로그 로테이션(`Logging.DeleteLogByDay`) 이미 존재 |
| T-d99-02 | I (Info disclosure) | Error 로그의 sigma/threshold/polarity 값 | accept | 오프라인 로컬 산업 장비, PII 없음, 로그는 장비 내 디스크에만 기록 |
| T-d99-03 | T (Tampering) | 판정 로직 무결성 (측정 결과 조작 위험) | mitigate | 신규 `return false` 0건 + `edgeCount < 2` 게이트 무변경 + TryFitLine 시그니처 무변경을 `git diff` 로 명시 검증 (Task 2 증명 1) |
</threat_model>

<verification>
1. Debug|x64 MSBuild 빌드 PASS (에러 0)
2. `git diff` — 신규 `return false` 0건 / `edgeCount < 2` 무변경 / `TryFitLine` public 시그니처 무변경 / 수정 파일이 `VisionAlgorithmService.cs` 단 1개
3. `grep -rn "AppendStrip" --include=*.cs .` — 호출부 여전히 2곳
4. 신규 삼항 연산자 `?:` 0건
5. HUMAN-UAT: 기존 레시피 검사 1회 → Trace 로그에 `[FitLine] strips ok N/M` + `fit residual(df)` 출력 + **판정 결과가 수정 전과 동일**
</verification>

<success_criteria>
- strip 성공/실패(에지 0개 vs 예외) 개수와 피팅 잔차 df 가 로그로 관측 가능하다
- 커버리지 50% 미만 시 Error 경고가 남되 **측정은 계속 진행된다**
- 판정 로직 무변경 — 회귀 0 (git diff 로 증명)
- 빌드 PASS, TryFitLine 호출부 19곳 무수정, 수정 파일 1개
</success_criteria>

<output>
완료 후 `.planning/quick/260714-d99-3-1-strip-2-2-stop-step-isdonebegin-3-sh/260714-d99-SUMMARY.md` 생성
</output>
</content>
