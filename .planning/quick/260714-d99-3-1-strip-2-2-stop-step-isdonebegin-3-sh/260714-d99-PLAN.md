---
phase: 260714-d99
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
  - WPF_Example/Sequence/Sequence/SequenceBase.cs
autonomous: true
requirements: [BUG-01, BUG-02, BUG-03]

must_haves:
  truths:
    - "strip 대다수가 예외로 실패하면 TryFitLine 이 false + 원인 메시지를 반환한다 (조용히 2점 직선을 만들지 않는다)"
    - "strip 실패가 발생하면 Error 로그에 실패 strip 수와 첫 예외 메시지가 남는다"
    - "Stop 후 재시작하면 IsDoneBegin 이 false 라서 OnBegin 이 반드시 호출된다"
    - "멀티샷 사이클에서 앞선 액션이 Fail 이면 마지막 액션이 Pass 여도 최종 Context.Result 가 Fail 이다"
    - "단일 액션 실행(Start(int)) 의 판정 동작은 수정 전과 동일하다"
    - "Debug|x64 빌드가 통과한다"
  artifacts:
    - path: "WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs"
      provides: "strip 성공/실패 계수 + 임계 가드 + 실패 로깅"
      contains: "EStripOutcome"
    - path: "WPF_Example/Sequence/Sequence/SequenceBase.cs"
      provides: "IsDoneBegin 리셋 + sticky-fail 집계"
      contains: "IsAnyActionFail"
  key_links:
    - from: "TryFitLine strip 루프"
      to: "AppendStrip 반환값"
      via: "EStripOutcome 계수 → 예외 과반 시 return false"
      pattern: "nStripsErrored"
    - from: "Start(int)/StartAll/StartSubset"
      to: "ExecuteAction 의 OnBegin 호출 분기"
      via: "IsDoneBegin = false"
      pattern: "IsDoneBegin = false"
    - from: "ExecuteAction 액션 Fail 감지"
      to: "Finish() 직전 Context.Result 확정"
      via: "IsAnyActionFail sticky 플래그"
      pattern: "IsAnyActionFail"
---

<objective>
멀티에이전트 버그 헌트에서 확정된 11건 중, 불량이 합격으로 나갈 수 있는 최우선 3건만 최소 침습으로 수정한다.

Purpose: 산업용 비전 검사 시스템에서 회귀 = 불량 유출. 3건 모두 "실패가 조용히 성공으로 둔갑" 하는 은폐형 버그다.
Output: 2개 파일 수정, 3개 원자적 커밋, Debug|x64 빌드 PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

@WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs
@WPF_Example/Sequence/Sequence/SequenceBase.cs

<verified_findings>
planner 가 코드를 직접 읽고 확인한 사실. executor 는 이 전제를 신뢰해도 된다.

**버그 1 — 확인됨 (보고 내용 정확)**
- `AppendStrip` (211~251행): 본문 전체가 `try { ... } catch { }` (243~245행) 로 감싸여 모든 실패를 무음 처리. `void` 반환이라 호출부가 실패를 알 방법이 없음.
- `TryFitLine` (18~183행): strip 루프(123~144행) 후 유일한 검증은 `edgeCount < 2` (150~154행).
  20 strip 중 19개가 예외로 죽고 1개 strip 에서 2점만 나와도 `FitLineContourXld` 로 직선을 만들어 `return true` (156~172행).
- 파급: `TryFitLine` 은 13개 측정 클래스(LineToLineDistance, EdgeToLineAngle, ArcLineIntersectDistance, DualImageEdgeDistance 등)가 호출. **시그니처 변경 금지** — 내부 로직만 강화한다.

**버그 2 — 버그는 실재하나 보고된 메커니즘은 부정확. 아래가 실제 사실**
- 보고: "Step 이 EStep.Measure 로 남아 Init/Grab 을 건너뛰고 이전 이미지로 측정한다" → **틀림**
- 실제: **Step 은 리셋된다.** `Start`/`StartAll`/`StartSubset` 이 호출하는 `Context.Clear()` (SequenceContext.cs 147~165행) 가 모든 액션을 순회하며 `act.Context.Clear()` 를 호출하고, `ActionContext.Clear()` 가 `CurrentStep = 0` 을 설정(58행). `ActionBase.Step` 은 `Context.CurrentStep` 의 패스스루(ActionBase.cs 22행).
  → **"이전 부품 측정값이 새 부품 결과로 전송" 은 발생하지 않는다.**
- 실제로 유실되는 것: `IsDoneBegin` 이 true 로 남으면 `ExecuteAction` 의 `if (IsDoneBegin == false)` 블록(206~212행) **전체**를 건너뛴다:
  - `Context.ActionParam = action.Param;` (207행) **유실** → SequenceContext.ActionParam 이 **이전 액션의 Param 을 계속 가리킴**. 소비처: `MainView.xaml.cs` (815/817/823/828행 — ROI 오버레이 렌더링), `RuntimeResizer.cs` (432~433행 — `pParam = SequenceResult.ActionParam` 으로 **ROI 편집 대상 바인딩**). → 운용자가 **엉뚱한 액션의 ROI 를 편집**할 수 있는 티칭 데이터 오염 위험.
  - `Context.TargetCode = TargetID;` (208~209행) **유실** → 이전 사이클의 target code 잔존.
  - `action.OnBegin(Context)` (210행) **유실** → `Context.Timer.Restart()` 미실행(액션 소요시간 오표시), `Context.State = EContextState.Running` 미설정(ActionContext.State 가 Idle 로 남음).
- `OnBegin` 오버라이드는 **전 코드베이스에 0건** (base `ActionBase.OnBegin` 만 존재). 따라서 파생 클래스의 per-cycle 초기화 유실은 없다.
- 결론: 심각도는 보고보다 낮다(측정값 오염 아님). 그러나 **ROI 편집 대상 오바인딩 = 티칭 오염** 은 실재하며, 수정은 3줄 · 회귀 0 이므로 그대로 진행한다.

**버그 3 — 확인됨. 와이어 판정은 오염되지 않음 (보고의 주의사항이 옳음)**
- `SequenceContext.CopyFrom(ActionContext)` (SequenceContext.cs 167~184행) 는 Result 를 복사하지 않는다.
- 그러나 `InspectionSequenceContext.CopyFrom` (InspectionSequence.cs 26~35행) 이 **오버라이드해서 `Result = actionContext.Result;` (28행) 를 매 액션마다 실행** → 검사 시퀀스에서는 매 액션 결과가 sequence Result 를 덮어씀 → **마지막 액션이 이긴다**.
- `ExecuteAction` 237행 `Context.Result = actionContext.Result;` 도 동일하게 마지막 액션 결과로 확정.
- **수정 위치 함정**: 231행 `Context.CopyFrom(actionContext)` 가 (오버라이드를 통해) Result 를 덮어쓰므로, sticky-fail 로직은 **반드시 CopyFrom 이후**인 236~238행 지점에서 최종 확정해야 한다. 그렇지 않으면 오버라이드가 클로버한다.
- **와이어 판정(`$RESULT` P/F/B)은 오염되지 않는다**: `m_bCycleHasNG` (InspectionSequence.cs 67행) 가 `ClassifyMeasurement` 에서 누적(626/632행)되고 `ApplyCycleJudgement` (660~672행) 가 `bCycleFail = m_bCycleHasNG || m_bCycleDatumFailed || bEmptyLastScope` 로 독립 계산한다. `Context.Result` 를 읽지 않는다.
- 따라서 **실제 피해 = ① `Finish()` 465~467행이 `Context.Result == Fail` 일 때만 `SaveResultImage` 를 호출 → NG 사이클인데 실패 이미지 미저장, ② UI/로그가 "Finished.(Pass)" 로 오표시 → 운용자가 NG 를 놓침.** 심각도는 "불량 은폐(운용/증거)" 이지 "와이어 오판정" 은 아니다.

**enum 값 (SequenceContext.cs 14~21행)**
`EContextResult { None, Pass, Fail, Error, WaferAngleError, WaferDieTeaching }`
`Error` 는 `ExecuteAction` 216행에서 즉시 `Error()` 로 단락되어 `Finish()` 에 도달하지 않는다. 따라서 sticky-fail 은 **Error 를 고려할 필요 없이 Fail 만** 다루면 된다 (우선순위 Error > Fail 자동 성립).
</verified_findings>

<code_rules>
- **삼항 연산자 `?:` 금지 — if-else 만 사용**
- C# 7.2 전용 (record / `??=` / `using var` / switch expression 금지)
- 브레이스 스타일: `VisionAlgorithmService.cs` = **Allman**, `SequenceBase.cs` = **K&R**. 각 파일의 기존 스타일을 따른다.
- **`//YYMMDD hbk` 주석 형식 금지** (2026-06-11 폐기). 주석은 비자명한 "왜" 만 한글로 간결히.
- 신규 .cs 파일 없음 → csproj 수정 불필요.
- 최소 침습. 리팩토링/코드 정리 금지.
</code_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 버그 1 — strip 실패 은폐 차단 (TryFitLine)</name>
  <files>WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs</files>
  <action>
`TryFitLine` 의 시그니처는 **절대 변경하지 않는다** (13개 측정 클래스가 호출). 내부 로직만 강화한다.

**1) private enum 추가** (`VisionAlgorithmService` 클래스 내부, Allman):

```csharp
// strip 단위 실행 결과. Error 는 정상 측정에서는 발생하지 않는다 —
// ROI 가 이미지 밖으로 이탈했거나 파라미터가 무효일 때만 나온다.
private enum EStripOutcome
{
    Edges,      // 정상 실행 + 에지 검출
    NoEdges,    // 정상 실행 + 에지 없음 (에지가 이 strip 을 지나지 않음 — 정상 상황)
    Error       // 예외 (기하/파라미터 무효)
}
```

**2) `AppendStrip` (211~251행) 을 `void` → `EStripOutcome` 반환으로 변경**
- 파라미터 끝에 `ref string firstStripError` 추가 (첫 예외 메시지만 보존).
- `catch { }` (243~245행) → `catch (Exception ex) { if (firstStripError == null) { firstStripError = ex.Message; } return EStripOutcome.Error; }`
- 236~239행 빈 결과 분기 → `return EStripOutcome.NoEdges;`
- TupleConcat 성공 후 → `return EStripOutcome.Edges;`
- `finally` 블록(246~250행)의 measureHandle/stripRegion 정리는 **그대로 유지**.
- 210행 "strip 실패는 swallow" 주석을 새 동작에 맞게 갱신.

**3) `TryFitLine` strip 루프(123~144행)에서 계수**

루프 진입 전 선언:
```csharp
int nStripsWithEdges = 0;
int nStripsErrored = 0;
string firstStripError = null;
```

scanHorizontal 양쪽 분기 모두에서 반환값을 받아 계수 (삼항 금지):
```csharp
EStripOutcome outcome = AppendStrip(..., ref allRows, ref allCols, ref firstStripError);
if (outcome == EStripOutcome.Edges)
{
    nStripsWithEdges++;
}
else if (outcome == EStripOutcome.Error)
{
    nStripsErrored++;
}
```

**4) 가드 2개를 strip 루프 직후 · `SortAndTrimPercent`(147행) 직전에 삽입**

```csharp
// 가드 A — 예외로 죽은 strip 이 과반이면 ROI 기하 자체가 무효다(정렬 보정으로 이미지 밖 이탈 등).
// 살아남은 소수 strip 의 에지점으로 직선을 세우면 실제와 전혀 다른 각도/위치가 "정상 측정값" 으로 둔갑한다.
// 정상 측정은 예외 strip 이 0 개다 → 과반 기준은 기존 정상 케이스를 절대 건드리지 않는다.
if (nStripsErrored * 2 > stripCount)
{
    error = "strip failure: " + nStripsErrored + "/" + stripCount + " strips errored (first: " + firstStripError + ")";
    Logging.PrintErrLog((int)ELogType.Error, "[TryFitLine] " + error);
    return false;
}

// 가드 B — 에지점이 단 1개 strip 에서만 나왔다면 직선의 기준선(span)이 없다.
// 2점을 잇는 임의 직선이 나와 각도가 무의미해진다.
// strip 1~3 개짜리 초소형/포인트 ROI 는 기존 동작 보존을 위해 제외한다.
if (stripCount >= 4 && nStripsWithEdges < 2)
{
    error = "insufficient strip coverage: edges from " + nStripsWithEdges + " of " + stripCount + " strips";
    Logging.PrintErrLog((int)ELogType.Error, "[TryFitLine] " + error);
    return false;
}
```

**5) 가드를 통과했으나 예외 strip 이 있었던 경우(소수 실패) 흔적 남기기** — 가드 B 직후:
```csharp
if (nStripsErrored > 0)
{
    Logging.PrintLog((int)ELogType.Trace, "[TryFitLine] " + nStripsErrored + "/" + stripCount
        + " strips errored but fit proceeded (first: " + firstStripError + ")");
}
```
`error` out 파라미터는 **건드리지 않는다** — 성공 경로에서 non-null 로 만들면 호출부가 오작동할 수 있다.

**6) 기존 `edgeCount < 2` 가드(150~154행)는 그대로 유지.**

**7) using 추가** — `Logging` / `ELogType` 사용을 위해 `WPF_Example/Halcon/Algorithms/DatumFindingService.cs` 상단 using 블록(1~10행)을 **먼저 읽고 동일 네임스페이스를 미러링**한다. 추측하지 말 것 (같은 폴더의 검증된 선례다).

**임계값 선택 근거 (SUMMARY 에 반드시 기록)**: "성공 strip N% 이상" 같은 성공률 기준은 채택하지 않았다 — 부분 에지(ROI 절반만 에지가 지나감)는 정상이며 성공률 기준은 이를 오탐해 회귀를 만든다. 대신 **예외(Error)는 건강한 측정에서 0건이라는 성질**을 이용해 "예외 strip 과반" 이라는 느슨한 기준을 택했다. 구조적으로 기존 정상 측정(예외 0건)을 절대 실패시키지 않으면서, 보고된 실패(19/20 예외)는 확실히 잡는다.

커밋: `fix(quick-260714-d99): TryFitLine strip 실패 은폐 차단 (예외 과반/span 부족 시 false + 로깅)`
  </action>
  <verify>
    <automated>MSYS2_ARG_CONV_EXCL="*" "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" -p:Configuration=Debug -p:Platform=x64 -t:Build -nologo -v:m -clp:ErrorsOnly</automated>
  </verify>
  <done>
- 빌드 PASS (에러 0).
- `TryFitLine` 시그니처 무변경 (`git diff` 로 18~29행 파라미터 목록 동일 확인).
- `AppendStrip` 이 `EStripOutcome` 반환, `catch` 가 더 이상 비어 있지 않음.
- 가드 A/B 존재, 기존 `edgeCount < 2` 가드 유지.
- 삼항 연산자 0개, Allman 스타일 유지.
  </done>
</task>

<task type="auto">
  <name>Task 2: 버그 2 — 중단 후 재시작 시 IsDoneBegin 미리셋</name>
  <files>WPF_Example/Sequence/Sequence/SequenceBase.cs</files>
  <action>
`IsDoneBegin` (64행, `protected bool`) 을 세 진입점에서 리셋한다.

**Stop() 이 아니라 Start 쪽에서 리셋한다** — Stop 외의 경로(예외 중단 등)로 중단된 경우까지 커버되기 때문이다. `Stop()` 은 무수정.

각 메서드의 기존 `IsFinished = false;` 바로 다음 줄에 추가 (K&R 스타일, 들여쓰기 12칸):

1. `Start(int actionIndex = 0)` — 328행 `IsFinished = false;` 다음
2. `StartAll(TestPacket packet)` — 351행 `IsFinished = false;` 다음
3. `StartSubset(int[] actionIndices, TestPacket packet)` — 377행 `IsFinished = false;` 다음

첫 번째 진입점에만 주석 전문을 달고, 나머지 두 곳은 한 줄만 둔다 (중복 주석 지양):
```csharp
            // 중단(Stop/예외)으로 IsDoneBegin=true 가 남으면 ExecuteAction 이 OnBegin 블록을 통째로 건너뛴다.
            // → Context.ActionParam 이 이전 액션의 Param 을 계속 가리켜 ROI 오버레이/편집이 엉뚱한 액션에 바인딩된다.
            IsDoneBegin = false;
```

**공통 헬퍼 추출 금지** — 회귀 위험 최소화가 우선이다. 3줄 중복을 허용한다.

**회귀 논증 (SUMMARY 에 기록)**: 정상 흐름(Idle → Start → 완주 → Finish)에서는 `ExecuteAction` 이 액션 종료 시 이미 `IsDoneBegin = false` 로 되돌린다(218행 Error 경로, 229행 Finish 경로). 따라서 Start 시점의 `IsDoneBegin` 은 이미 false 이며 이 대입은 **no-op** 이다. 정상 흐름 동작 변화 0.

커밋: `fix(quick-260714-d99): Start/StartAll/StartSubset 에서 IsDoneBegin 리셋 (중단 후 재시작 시 OnBegin 스킵 차단)`
  </action>
  <verify>
    <automated>MSYS2_ARG_CONV_EXCL="*" "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" -p:Configuration=Debug -p:Platform=x64 -t:Build -nologo -v:m -clp:ErrorsOnly</automated>
  </verify>
  <done>
- 빌드 PASS.
- `git diff` 상 `IsDoneBegin = false;` 가 정확히 3곳(Start/StartAll/StartSubset)에만 추가됨. 그 외 로직 변경 0.
- `Stop()` 무수정, `ExecuteAction` 무수정.
  </done>
</task>

<task type="auto">
  <name>Task 3: 버그 3 — 마지막 액션이 앞선 Fail 을 덮어쓰는 문제 (sticky fail)</name>
  <files>WPF_Example/Sequence/Sequence/SequenceBase.cs</files>
  <action>
멀티샷 사이클에서 **하나라도 Fail 이면 최종 Fail** 로 집계한다.

**1) 필드 추가** — `IsDoneBegin` 선언(64행) 인근에 동일 스타일로:
```csharp
        // 사이클 중 Fail 액션이 1건이라도 있었는지. 마지막 액션 결과가 앞선 SHOT 의 Fail 을 덮어쓰는 것을 막는다.
        // Error 는 ExecuteAction 에서 즉시 Error() 로 단락되므로 여기서 다루지 않는다.
        protected bool IsAnyActionFail = false;
```

**2) 세 진입점에서 리셋** — Task 2 에서 추가한 `IsDoneBegin = false;` 바로 다음 줄에 각각 (Start / StartAll / StartSubset 3곳):
```csharp
            IsAnyActionFail = false;
```

**3) `ExecuteAction` 에서 누적 + 최종 확정**

**함정 주의**: 231행 `Context.CopyFrom(actionContext)` 는 `InspectionSequenceContext.CopyFrom` 오버라이드를 통해 `Result` 를 덮어쓴다. 따라서 sticky 로직은 **반드시 CopyFrom 이후**에 최종 확정해야 한다.

227행 `else if (actionContext.State == EContextState.Finish) {` 블록 안, `CurAction.OnEnd();` (228행) 인근 — CopyFrom(231행) **이전**에 누적:
```csharp
                if (actionContext.Result == EContextResult.Fail) {
                    IsAnyActionFail = true;
                }
```

236~238행의 최종 확정 분기를 아래로 교체 (삼항 금지):
```csharp
                if (CurrentActionIndex >= EndActionIndex) {
                    // 앞선 액션(SHOT)의 Fail 이 마지막 액션의 Pass 로 덮어써지지 않도록 sticky 집계.
                    // CopyFrom(InspectionSequenceContext 오버라이드)이 Result 를 덮어쓴 뒤에 확정해야 한다.
                    if (IsAnyActionFail == true) {
                        Context.Result = EContextResult.Fail;
                    } else {
                        Context.Result = actionContext.Result;
                    }
                    Finish();
                } else {
                    CurrentActionIndex++;
                    CurAction = Actions[CurrentActionIndex];
                }
```

**Error 경로(216~226행)는 무수정** — `Error()` 가 `Context.Result = EContextResult.Error` 로 확정하며 `Finish()` 에 도달하지 않는다.

**회귀 논증 (SUMMARY 에 기록)**:
- 단일 액션 실행 `Start(int)`: `CurrentActionIndex == EndActionIndex` 라 액션 1개만 실행. Fail 이면 `IsAnyActionFail = true` → 최종 Fail (기존과 동일). Pass 면 false → `actionContext.Result` 그대로 (기존과 동일). **동작 변화 0.**
- 멀티샷 전원 Pass: `IsAnyActionFail` false 유지 → 기존과 동일.
- 멀티샷 중 1건 Fail: 기존 = 마지막 액션 결과(Pass 가능) / 수정 후 = Fail. **이것이 의도된 유일한 동작 변화.**
- 효과: `Finish()` 465~467행이 `Context.Result == Fail` 에서 `SaveResultImage` 를 호출 → NG 사이클 실패 이미지가 저장되고, UI/로그가 Fail 로 정상 표시.
- 와이어 판정(`$RESULT` P/F/B)은 `m_bCycleHasNG` / `ApplyCycleJudgement` 독립 경로라 **영향 없음** (기존에도 정상이었음).

커밋: `fix(quick-260714-d99): 멀티샷 sticky fail 집계 (앞선 SHOT Fail 이 마지막 액션 Pass 로 은폐되는 문제)`
  </action>
  <verify>
    <automated>MSYS2_ARG_CONV_EXCL="*" "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" -p:Configuration=Debug -p:Platform=x64 -t:Build -nologo -v:m -clp:ErrorsOnly</automated>
  </verify>
  <done>
- 빌드 PASS.
- `IsAnyActionFail` 필드 선언 + 3개 진입점 리셋 + `ExecuteAction` 누적/확정 존재.
- Error 경로 무수정.
- 삼항 연산자 0개, K&R 스타일 유지.
  </done>
</task>

<task type="auto">
  <name>Task 4: 통합 검증 — 빌드 + 회귀 가드 논증</name>
  <files>없음 (검증 전용 — 코드 수정 금지)</files>
  <action>
**코드를 수정하지 않는다.** Task 1~3 의 결과를 검증하고 SUMMARY 를 작성한다.

**1) 통합 빌드** (아래 verify 명령). 에러 0 확인.

**2) 전체 diff 리뷰** — `git diff HEAD~3 -- WPF_Example/` 로 3개 커밋의 누적 변경을 확인하고 아래를 하나씩 체크:
- 삼항 연산자 `?:` 신규 도입 0건
- C# 8+ 문법(`??=`, `using var`, switch expression, record) 0건
- `//YYMMDD hbk` 형식 주석 신규 도입 0건
- `TryFitLine` public 시그니처 무변경
- `VisionAlgorithmService.cs` = Allman, `SequenceBase.cs` = K&R 유지
- 의도한 2개 파일 외 변경 0건, 신규 파일 0건 (csproj 무수정)

**3) 회귀 가드 논증** (diff 기반, SUMMARY 에 "수정 전 / 수정 후" 대조표로 기록):

| 시나리오 | 수정 전 | 수정 후 | 회귀? |
|---|---|---|---|
| 정상 측정 (예외 strip 0개) | TryFitLine true | TryFitLine true (가드 A/B 모두 미발동) | 없음 |
| 정렬 보정으로 ROI 이탈 (19/20 예외) | **true + 엉터리 직선** | false + 원인 메시지 + Error 로그 | 의도된 변화 |
| 정상 흐름 Idle→Start→완주 | IsDoneBegin 이미 false | 동일 (대입은 no-op) | 없음 |
| Stop 후 재시작 | OnBegin 스킵 → ActionParam stale | OnBegin 호출 → ActionParam 갱신 | 의도된 변화 |
| 단일 액션 Start(int) | 그 액션 결과가 최종 | 동일 | 없음 |
| 멀티샷 전원 Pass | Pass | Pass | 없음 |
| 멀티샷 중 1건 Fail | **마지막 액션 결과(Pass 가능)** | Fail (실패 이미지 저장 + UI Fail 표시) | 의도된 변화 |

**4) SUMMARY 에 반드시 포함할 것**:
- 버그 1 **임계값 선택 근거** (성공률 기준을 버리고 "예외 과반" 을 택한 이유 — Task 1 참조).
- 버그 2 **보고 정정**: Step 은 `Context.Clear()` 가 이미 리셋하므로 "이전 부품 측정값 전송" 은 발생하지 않았다. 실제 피해는 `Context.ActionParam` stale → **ROI 편집/오버레이 오바인딩(티칭 오염)**.
- 버그 3 **영향 범위 정정**: 와이어 판정(`$RESULT`)은 `m_bCycleHasNG` 독립 경로라 오염 없음. 실제 피해는 **실패 이미지 미저장 + UI/로그 오표시**.
- **실HW UAT 이관 항목** (테스트 프레임워크 부재 → 빌드만으로는 검증 불가):
  1. 정상 레시피 전 FAI 재측정 → 측정값이 수정 전과 동일한지 (버그 1 회귀 확인 — **최대 리스크**)
  2. 검사 중 Stop → 재시작 → ROI 오버레이가 현재 액션 기준으로 그려지는지 (버그 2)
  3. 멀티샷 중 앞 SHOT 만 NG → 최종 Fail 표시 + 실패 이미지 저장 확인 (버그 3)

**5) 이번 범위 밖 8건**은 문서로만 남긴다 (수정 금지).
  </action>
  <verify>
    <automated>MSYS2_ARG_CONV_EXCL="*" "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" -p:Configuration=Debug -p:Platform=x64 -t:Build -nologo -v:m -clp:ErrorsOnly</automated>
  </verify>
  <done>
- Debug|x64 빌드 PASS (에러 0).
- diff 리뷰 체크리스트 6항목 전부 통과.
- 회귀 대조표 7행이 SUMMARY 에 기록됨.
- 실HW UAT 이관 항목 3건이 SUMMARY 에 기록됨.
- 커밋 3개가 버그별로 분리되어 있음 (롤백 용이).
  </done>
</task>

</tasks>

<verification>
- Debug|x64 MSBuild 빌드 PASS (유일한 자동 게이트 — 테스트 프레임워크 없음)
- `TryFitLine` public 시그니처 무변경 → 13개 호출부 무수정
- 정상 흐름(예외 strip 0개 / Idle→Start→완주 / 단일 액션 / 멀티샷 전원 Pass)에서 동작 변화 0
- 커밋 3개 분리 (버그별 롤백 가능)
</verification>

<success_criteria>
- 3개 버그가 각각 독립 커밋으로 수정됨
- 빌드 PASS
- 회귀 대조표로 "정상 케이스 동작 변화 0" 이 diff 기반으로 논증됨
- 버그 2/3 의 실제 영향 범위 정정이 SUMMARY 에 기록됨 (보고서와 다른 부분)
- 실HW UAT 이관 항목 3건 명시
</success_criteria>

<output>
완료 후 `.planning/quick/260714-d99-3-1-strip-2-2-stop-step-isdonebegin-3-sh/260714-d99-SUMMARY.md` 생성
</output>
