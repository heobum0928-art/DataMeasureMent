---
phase: 260722-p5d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/Measurements/ArcEdgeDistanceMeasurement.cs
autonomous: true
requirements:
  - FIX-1-FarClose        # ArcLineIntersect IntersectionPointSelection (Far/Close) 복원
  - FIX-2-PerPointAvg     # 3종 측정 per-edge-point projection 평균화

must_haves:
  truths:
    - "ArcLineIntersect: IntersectionPointSelection=\"Close\" 는 좌 교점(int1)에서, \"Far\"/기본/미설정 은 우 교점(int2)에서 세그먼트를 측정한다 — 두 설정이 서로 다른 결과를 낸다."
    - "DualImage/EdgeToLine/ArcEdge: 거리값 = 라인 피팅에 쓰인 개별 에지점 각각을 기준선에 투영한 per-point 거리들의 산술평균 (단일 중점 투영 아님)."
    - "각 파일은 자신의 기존 부호/축 규약을 per-point 로 보존한다 (DualImage=unsigned sqrt, EdgeToLine main=signed 분기, ArcEdge=ComputeProjectionDistance signed)."
    - "collectedEdges 리스트가 비거나 모든 per-point 투영이 실패하면 기존 단일-중점 동작으로 폴백 — 크래시/NaN/0-나눗셈 없음."
    - "솔루션이 msbuild Debug/x64 로 신규 에러/경고 0 으로 빌드된다."
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs"
      provides: "Far/Close 교점 선택 분기 복원 (int1/int2)"
      contains: "IntersectionPointSelection"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs"
      provides: "PointROI per-edge-point unsigned projection 평균"
      contains: "collectedEdgePoints"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs"
      provides: "signed per-edge-point projection 평균 (datumOriginInjected 경로)"
      contains: "collectedEdgePoints"
    - path: "WPF_Example/Custom/Sequence/Inspection/Measurements/ArcEdgeDistanceMeasurement.cs"
      provides: "ComputeProjectionDistance per-edge-point 평균"
      contains: "collectedEdgePoints"
  key_links:
    - from: "TryFitLine(..., collectedEdges)"
      to: "HOperatorSet.ProjectionPl / ComputeProjectionDistance per-point loop"
      via: "collected (row,col) 리스트 순회 후 거리 평균"
      pattern: "collectedEdgePoints"
---

<objective>
비전 측정 알고리즘 2건 버그 수정 (사용자 확정, 재조사 불필요 — 구현만).

Fix 1: `ArcLineIntersectDistanceMeasurement.cs` 의 `IntersectionPointSelection`(Far/Close) 토글이 260716 재설계 때 하드코딩(항상 int2=우 교점)으로 폐기된 것을 복원한다. Close → 좌 교점(int1), Far/기본/미설정 → 우 교점(int2, INI 하위호환).

Fix 2: 3종 측정(`DualImageEdgeDistance`, `EdgeToLineDistance`, `ArcEdgeDistance`)이 fit 라인의 단일 중점 1점만 기준선에 투영해 거리를 내던 것을, 라인 피팅에 실제 쓰인 개별 에지점 각각을 투영해 per-point 거리들을 산술평균하도록 바꾼다. `VisionAlgorithmService.TryFitLine` 의 기존 opt-in `collectedEdges` 파라미터를 재사용한다 (TryFitLine 자체는 무수정).

Purpose: 프로덕션 레시피(main.ini)가 실제 사용하는 측정 수학 정확도 개선. Close 레시피(SHOT_I10)가 Far 와 동일 동작하던 회귀 제거 + 중점-투영의 노이즈 민감성 완화.
Output: 측정 파일 4개 수정, msbuild Debug/x64 클린 빌드.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<constraints>
- C# 7.2 / .NET Framework 4.8. C# 8+ 문법 금지 (switch expression, nullable ref types, using declaration 등).
- 이 파일들은 측정/HALCON 코드 → `.planning/refs/control-sequence-coding-guideline.md` (헝가리안/삼항금지 등) 적용 대상이 **아님**. 각 파일의 기존 스타일(Allman 브레이스, 혼합 PascalCase/camelCase 로컬) 그대로 유지. 새 헬퍼/추상화 도입 금지 — 각 파일 내 인라인으로만.
- 각 파일은 서로 byte-identical 이 아니며 각자 고유한 부호/축 규약이 있다 — per-point 루프에서 그 파일의 **기존** 부호식을 그대로 재사용할 것(새 부호 공식 발명 금지).
</constraints>

<interfaces>
<!-- VisionAlgorithmService.cs 에서 추출 — executor 는 이 계약을 직접 사용, 코드베이스 재탐색 불필요 -->

TryFitLine 후행 opt-in 파라미터 (WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs:20~31):
  public bool TryFitLine(
      HImage image, double roiRow, double roiCol, double roiPhi, double l1, double l2,
      HTuple datumTransform, int sampleCount, int trimCount, double sigma, int threshold,
      string direction, string polarity,
      out double row1, out double col1, out double row2, out double col2, out string error,
      string selection = "all",
      List<ValueTuple<double, double>> collectedEdges = null)
  - collectedEdges != null 이면, SortAndTrimPercent(trim 후) 라인 피팅에 실제 쓰인 (row,col) 점들을
    edgeCount 개 append (VisionAlgorithmService.cs:219~224). 반환 true 면 edgeCount>=2 보장(:194).
  - Item1=row, Item2=col. 좌표계는 image 좌표 (중점 계산에 쓰던 pr1..pc2 와 동일 좌표계).
  - 현재 코드베이스에 non-null 리스트를 넘기는 caller 없음(확인 완료) → 추가해도 타 동작 무영향.

ComputeProjectionDistance foot 반환 오버로드 (VisionAlgorithmService.cs:695~754):
  public static double ComputeProjectionDistance(
      double pointRow, double pointCol,
      double datumOriginRow, double datumOriginCol, double lineAngleRad,
      double pixelResolution, string measureAxis,
      out double footRow, out double footCol, out bool footOk)
  - 반환값 = signed 거리(mm, 이미 × pixelResolution). 내부에서 ProjectionPl 수행.
  - 투영 실패 시 반환 0.0 + footOk=false (throw 없음).
  - measureAxis 부호 규약: "X"=+오른쪽 양수, 그 외=+위쪽 양수(D-02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore IntersectionPointSelection (Far/Close) in ArcLineIntersectDistanceMeasurement</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/ArcLineIntersectDistanceMeasurement.cs</files>
  <action>
현재 최종 세그먼트/오버레이가 항상 우 교점(int2)에서 측정하도록 하드코딩된 것을 `IntersectionPointSelection` 값으로 분기 복원한다.

1. 최종 거리/부호 계산 블록(현재 ~L275: `double segDrow = int2Row - measurePointRow;` 시작) **직전**에 target 교점 로컬을 추가한다:
   - `double targetRow, targetCol;` 선언.
   - `if (string.Equals(IntersectionPointSelection, "Close", System.StringComparison.OrdinalIgnoreCase)) { targetRow = int1Row; targetCol = int1Col; } else { targetRow = int2Row; targetCol = int2Col; }`
   - "Close" → 좌 교점 int1, 그 외("Far"/기본/공백/미설정) → 우 교점 int2 (INI 하위호환, 기본 회귀 0). 대소문자 무시.
2. 이어지는 세그먼트 계산에서 `int2Row`/`int2Col` 직접 참조를 `targetRow`/`targetCol` 로 교체: `segDrow = targetRow - measurePointRow; segDcol = targetCol - measurePointCol;`. distPx/normR/normC/sideSign/resultValue 나머지 줄은 **그대로** — segDrow/segDcol 만 target 반영하면 부호(오른쪽=양수)는 실제 target 위치로부터 자동 정확 산출됨(새 부호식 도입 금지).
3. 오버레이 foot 점(현재 ~L286 `double footRow = int2Row, footCol = int2Col;`)을 `double footRow = targetRow, footCol = targetCol;` 로 교체 — "FAI-DistLine" 오버레이가 실제 측정한 교점과 시각적으로 일치하도록.
4. `measurePointP`, `int1Row/int1Col`, `int2Row/int2Col`, L_cross, L_datum(`ldR1,ldC1,ldR2,ldC2`) 산출 로직은 **한 줄도 바꾸지 않는다**. 오직 어느 교점에서 최종 세그먼트/오버레이를 측정하느냐만 변경.
5. 주석 정정: 260716 재정의 주석 블록(현재 ~L245~248) 끝의 "IntersectionPointSelection 폐기." 문구를 260722 복원 사실로 교체(예: 폐기 문구 삭제 + "260722 hbk: IntersectionPointSelection(Far/Close) 재활성 — Close=좌 교점, Far=우 교점"). (8)번 주석(~L272~274)의 "우측 교점(int2)" 표현과 오버레이 주석(~L285)의 "우측 교점 int2" 표현을 "선택된 교점(Far=int2 기본/Close=int1)" 으로 정정. L120~126 프로퍼티의 `[Description(...)]` 은 이미 정확(Far/Close 설명)하므로 유지.

파일 스타일: Allman 브레이스, 기존 로컬 명명(segDrow/segDcol/sideSign/measurePointRow) 유지. `if/else` 평범한 idiomatic C#.
  </action>
  <verify>
    <automated>msbuild DatumMeasurement.sln /p:Configuration=Debug /p:Platform=x64 /t:Build /v:m</automated>
    자기검토(부호): Close 선택 시 targetRow/Col=int1 이고 segDrow/segDcol 이 measurePointP→int1 을 가리키는지, sideSign 이 target 실좌표 기반으로 산출되는지 육안 확인. int1/int2/measurePointP 산출은 무변경임을 diff 로 확인.
  </verify>
  <done>IntersectionPointSelection 이 TryExecute 에서 실제 읽힌다(int1 vs int2 분기). Close/Far 가 서로 다른 세그먼트를 측정한다. "폐기" 주석 제거. 빌드 신규 에러/경고 0.</done>
</task>

<task type="auto">
  <name>Task 2: Average per-edge-point projection in DualImageEdgeDistanceMeasurement (unsigned)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/DualImageEdgeDistanceMeasurement.cs</files>
  <action>
PointROI(RuntimeImageA) fit 의 단일 중점 투영을, 개별 에지점 per-point 투영 평균으로 교체한다. LineROI(RuntimeImageB, 기준선 `lb_r1,lb_c1,lb_r2,lb_c2`) fit 은 **무변경**(투영 대상 라인).

1. PointROI `TryFitLine` 호출(현재 ~L186) 직전에 `List<ValueTuple<double, double>> collectedEdgePoints = new List<ValueTuple<double, double>>();` 선언하고, 호출의 후행 인자로 넘긴다: `... out error, PointROI_EdgeSelection, collectedEdgePoints))`. (선언 순서: selection 다음이 collectedEdges.)
2. 기존 `pointRow`/`pointCol` 중점(현재 ~L196~197)은 **폴백용으로 유지**.
3. 기존 투영 블록(현재 ~L212~232: try{ProjectionPl(pointRow,...)} + `dRow/dCol/distPx/resultValue`)을 다음으로 교체:
   - collectedEdgePoints 순회: 각 `(er,ec)` 에 대해 `HOperatorSet.ProjectionPl(er, ec, lb_r1, lb_c1, lb_r2, lb_c2, out prRow, out prCol)` 를 try 로 감싸 호출(throw 시 그 점 skip/continue). foot `(fr,fc)=(prRow.D,prCol.D)`. per-point 거리 = **기존 UNSIGNED 공식** `System.Math.Sqrt((er-fr)*(er-fr)+(ec-fc)*(ec-fc))`. 누적: sumDistPx, sumFootRow/Col, sumPtRow/Col, nPts++, anyFootOk=true.
   - 성공 점 수 `nPts>=1` 이면: `resultValue = (sumDistPx / nPts) * pixelResolution;` (px 평균 후 × 해상도 — 스칼라 곱과 평균은 교환가능). 표시용 재대입: `pointRow = sumPtRow/nPts; pointCol = sumPtCol/nPts; footRow = sumFootRow/nPts; footCol = sumFootCol/nPts; footOk = true;` (표시 좌표만 — 이 평균-위치는 resultValue 수학에 절대 사용 안 함).
   - **폴백**: collectedEdgePoints 비었거나 모든 투영 실패(nPts==0)면 기존 단일-중점 동작 그대로 수행 — `ProjectionPl(pointRow,pointCol,lb_*)` → unsigned sqrt → `resultValue = distPx * pixelResolution`, footOk 설정. (기존 catch 실패 시 `error="projection_pl 실패"; return false;` 동작 보존.)
4. 오버레이: FAI-Edge1 라인은 fit 끝점 `pa_r1..pa_c2` 그대로. FAI-Edge1 의 단일 포인트 마커(Points, 현재 pointRow/pointCol)와 FAI-DistLine 의 foot 끝점은 3번에서 재대입된 **평균 좌표**(pointRow/pointCol=수집점 평균, footRow/footCol=foot 평균)를 사용. FAI-Edge2(LineROI) 오버레이는 무변경. 10~20개 개별 점 마커는 렌더링하지 않음.

파일 스타일 유지(Allman, camelCase 로컬). ValueTuple 은 `.Item1`(row)/`.Item2`(col) 로 접근.
  </action>
  <verify>
    <automated>msbuild DatumMeasurement.sln /p:Configuration=Debug /p:Platform=x64 /t:Build /v:m</automated>
    자기검토: per-point 거리식이 기존 UNSIGNED sqrt 와 동일한지, 평균이 per-point 거리들의 평균(위치평균 후 단일투영 아님)인지, nPts==0 폴백이 크래시/NaN 없이 기존 동작 재현하는지 확인.
  </verify>
  <done>PointROI 측정값이 수집 에지점들의 per-point unsigned 투영거리 평균. LineROI/기준선 무변경. 빈 리스트 폴백 존재. 빌드 신규 에러/경고 0.</done>
</task>

<task type="auto">
  <name>Task 3: Average per-edge-point projection in EdgeToLineDistanceMeasurement (signed, main path only)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/EdgeToLineDistanceMeasurement.cs</files>
  <action>
`datumOriginInjected==true` 인 **정상 투영 경로**만 per-point signed 평균으로 교체한다. 레거시 폴백(else, AffineTransPoint2d)은 **무변경**(사유: affine 는 선형 사상 → per-point 평균 == 중점변환 이 수학적으로 동일. 이 이유를 한 줄 주석으로 남길 것).

1. 에지 `TryFitLine` 호출(현재 ~L121) 직전에 `List<ValueTuple<double, double>> collectedEdgePoints = new List<ValueTuple<double, double>>();` 선언, 후행 인자로 전달: `... out error, EdgeSelection, collectedEdgePoints))`. `pRow`/`pCol` 중점(~L131~132)은 폴백/레거시경로용으로 유지.
2. `datumOriginInjected` 분기 안에서 기준선 2점 `axisR1,axisC1,axisR2,axisC2`(현재 ~L175~199)은 datum 에만 의존하므로 **한 번만** 그대로 계산(무변경). `sinT/cosT/measureX/useAngle2` 정규화 로직도 무변경.
3. 단일-중점 투영+signedPx 블록(현재 ~L200~228: `ProjectionPl(pRow,pCol,axis...)` → footRow/footCol/footOk → `signedPx` 분기 → `resultValue = signedPx * pixelResolution`)을 per-point 루프로 교체:
   - collectedEdgePoints 순회: 각 `(er,ec)` 에 `HOperatorSet.ProjectionPl(er, ec, axisR1, axisC1, axisR2, axisC2, out prRow, out prCol)` (try, throw 시 skip). foot `(fr,fc)`.
   - per-point signed 거리 = **기존 분기식 그대로**(er/ec 를 pRow/pCol 자리에, fr/fc 를 footRow/footCol 자리에 대입):
     · measureX && useAngle2 → `(er-fr)*cosT - (ec-fc)*sinT`
     · measureX && !useAngle2(폴백) → `(er-fr)*sinT + (ec-fc)*cosT`
     · else(Y, +위쪽) → `(er-fr)*(-cosT) + (ec-fc)*sinT`
   - 누적: sumSignedPx, sumFootRow/Col, sumPtRow/Col, nPts++, anyFootOk=true.
   - `nPts>=1`: `resultValue = (sumSignedPx / nPts) * pixelResolution;`. 표시용 재대입: `pRow = sumPtRow/nPts; pCol = sumPtCol/nPts; footRow = sumFootRow/nPts; footCol = sumFootCol/nPts; footOk = true;` (표시 전용, resultValue 수학과 분리).
   - **폴백**(nPts==0): 기존 단일-중점 signedPx 경로 그대로 1회 수행(현행 코드 보존). footOk 는 해당 투영 성공 여부.
4. 레거시 else 경로(현재 ~L230~245, `AffineTransPoint2d`→`-datumRow`)와 그 오버레이(HomMat2dInvert 경로)는 **무변경**. affine 선형성으로 per-point 평균 불필요함을 명시하는 짧은 '왜' 주석만 추가.
5. 오버레이(현재 ~L250~306): FAI-Edge1 라인은 fit 끝점 `pr1..pc2` 그대로. main 경로에서 FAI-Edge1 의 포인트 마커(현재 pRow/pCol)와 FAI-DistLine 의 originRow/originCol(=footRow/footCol)은 3번 재대입된 평균 좌표를 자동으로 사용(오버레이 코드가 pRow/pCol/footRow/footCol 를 읽으므로 추가 수정 불필요). "에지 중점 1개" 주석은 "수집 에지점 평균" 으로 정정.

주의: 이 파일은 signed 측정이다 — per-point 에서 부호식을 축(measureX/useAngle2/Y)별로 정확히 보존해야 함. 부호 뒤집힘 방지 정규화(sinT/cosT)는 루프 밖에서 한 번만.
  </action>
  <verify>
    <automated>msbuild DatumMeasurement.sln /p:Configuration=Debug /p:Platform=x64 /t:Build /v:m</automated>
    자기검토(부호/축): per-point signedPx 3분기가 기존 L217/L221/L226 공식과 항별로 동일한지(축·부호 혼선 없음), 정규화가 루프 밖 1회인지, 레거시 else 무변경인지, 빈 리스트 폴백이 기존 동작 재현인지 확인.
  </verify>
  <done>datumOriginInjected 경로 측정값이 per-point signed 투영거리 평균. 축별 부호식 보존. 레거시 폴백 무변경. 빌드 신규 에러/경고 0.</done>
</task>

<task type="auto">
  <name>Task 4: Average per-edge-point projection in ArcEdgeDistanceMeasurement (helper loop)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Measurements/ArcEdgeDistanceMeasurement.cs</files>
  <action>
`ComputeProjectionDistance` 단일-중점 호출을 개별 에지점 per-point 호출 평균으로 교체한다. 이 파일은 EdgeToLine 과 byte-identical 이 아님 — 자체 helper(`VisionAlgorithmService.ComputeProjectionDistance`) 기반이므로 부호식 직접 구현 없이 helper 를 루프.

1. 에지 `TryFitLine` 호출(현재 ~L103) 직전에 `List<ValueTuple<double, double>> collectedEdgePoints = new List<ValueTuple<double, double>>();` 선언, 후행 인자로 전달: `... out error, EdgeSelection, collectedEdgePoints))`. `pRow`/`pCol` 중점(~L115~116)은 폴백용 유지.
2. `measureLineAngle`(MeasureAxis 로 DatumAngle2Rad/DatumAngleRad 선택, 현재 ~L123~127)은 무변경.
3. 단일 helper 호출(현재 ~L130~134: `resultValue = ComputeProjectionDistance(pRow, pCol, DatumOriginRow, DatumOriginCol, measureLineAngle, pixelResolution, MeasureAxis, out footRow, out footCol, out footOk);`)을 per-point 루프로 교체:
   - collectedEdgePoints 순회: 각 `(er,ec)` 에 `double d = VisionAlgorithmService.ComputeProjectionDistance(er, ec, DatumOriginRow, DatumOriginCol, measureLineAngle, pixelResolution, MeasureAxis, out fr, out fc, out fOk);`. **fOk==true 인 점만** 누적(helper 는 투영 실패 시 0.0+fOk=false 반환하므로 실패값이 평균 오염하지 않도록 반드시 fOk 게이트). 누적: sumDist(=d, 이미 mm), sumFootRow/Col, sumPtRow/Col, nPts++.
   - `nPts>=1`: `resultValue = sumDist / nPts;` (helper 반환이 이미 ×pixelResolution 이므로 mm 평균 = px 평균×해상도, 동일). 표시용: `pRow = sumPtRow/nPts; pCol = sumPtCol/nPts; footRow = sumFootRow/nPts; footCol = sumFootCol/nPts; footOk = true;`.
   - **폴백**(nPts==0): 기존 단일-중점 helper 호출 1회 그대로(`ComputeProjectionDistance(pRow, pCol, ..., out footRow, out footCol, out footOk)`) → 현행 동작 보존.
   - `footRow`/`footCol`/`footOk` 는 오버레이(현재 L196 이후 FAI-DistLine)에서 쓰이므로 위 재대입/폴백에서 반드시 채워질 것.
4. 오버레이: FAI-Edge1 라인은 fit 끝점 `pr1..pc2` 그대로, 포인트 마커(현재 pRow/pCol)는 평균 좌표 사용(3번 재대입 자동 반영). FAI-DatumLine 2개(현재 ~L153~192, datum 의존)는 **무변경**. FAI-DistLine(현재 ~L196~209)의 footRow/footCol→pRow/pCol 은 평균 좌표 자동 반영. "에지 중점" 주석은 "수집 에지점 평균" 으로 정정.

파일 스타일 유지. helper 시그니처는 <interfaces> 참조(foot 반환 오버로드).
  </action>
  <verify>
    <automated>msbuild DatumMeasurement.sln /p:Configuration=Debug /p:Platform=x64 /t:Build /v:m</automated>
    자기검토: helper 를 per-point 로 루프하고 fOk 게이트로 실패점 제외 후 평균하는지, 폴백(nPts==0)이 기존 단일 호출 재현인지, FAI-DatumLine 무변경인지 확인.
  </verify>
  <done>ArcEdge 측정값이 수집 에지점별 ComputeProjectionDistance 평균(fOk 점만). datum 오버레이 무변경. 빈 리스트/전실패 폴백 존재. 빌드 신규 에러/경고 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
순수 내부 측정 수학 수정. 신규 외부 입력/네트워크/패키지 설치/파일 I/O 없음 — 새 신뢰 경계 없음.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-p5d-01 | Tampering | 측정 결과 정확도(부호/축 회귀) | mitigate | 각 파일 기존 부호식 per-point 재사용(신규 공식 금지) + Task별 자기검토(부호/축 diff) + nPts==0 폴백으로 기존 동작 보존 |
| T-p5d-02 | Denial of Service | ProjectionPl per-point 예외 | accept | 점당 try/skip + 전실패 시 단일-중점 폴백 → 크래시/무한루프 없음. 에지점 ≤ sampleCount(~20) 로 루프 비용 유한 |
</threat_model>

<verification>
전체 확인 (per-file 부호/축 혼선 방지 — "컴파일됨" 이상):
1. **빌드**: `msbuild DatumMeasurement.sln /p:Configuration=Debug /p:Platform=x64 /t:Build /v:m` — 신규 에러/경고 0.
2. **Fix 1 diff 검토**: int1/int2/measurePointP/L_cross/L_datum 산출은 무변경(diff 로 확인), 오직 target 선택 분기 + segDrow/segDcol/foot 참조만 변경. "폐기" 주석 제거됨.
3. **Fix 2 per-file 부호 검토** (파일마다 독립):
   - DualImage = UNSIGNED sqrt (부호 없음).
   - EdgeToLine = SIGNED, 3분기(measureX+useAngle2 / measureX 폴백 / Y)가 기존 L217/L221/L226 과 항별 동일, 정규화 루프 밖 1회, 레거시 else 무변경.
   - ArcEdge = helper 반환(signed mm) 평균, fOk 게이트로 실패점 제외.
4. **폴백 검토**: 3파일 모두 collectedEdges 빈 리스트/전투영실패(nPts==0) 시 기존 단일-중점 경로로 폴백 → NaN/0-나눗셈/크래시 경로 없음.
5. **무영향 검토**: `collectedEdges` non-null caller 신규 추가가 기존 오버레이 caller 와 충돌 없음(기존 non-null caller 부재 확인됨). LineROI/datum 기준선/DatumLine 오버레이 무변경.
6. ArcLineIntersect 는 Fix 2 대상 아님(TryIntersectLines 사용, ProjectionPl 미사용) — 무변경 유지.
</verification>

<success_criteria>
- ArcLineIntersect: Close 레시피(SHOT_I10 등)가 Far 와 다른 측정값 산출(좌 교점 int1 기준). 기본/미설정 은 int2(회귀 0).
- DualImage/EdgeToLine/ArcEdge: 측정값 = per-edge-point 투영거리 산술평균, 각 파일 기존 부호/축 규약 보존.
- 3파일 모두 빈-리스트 폴백으로 무회귀.
- msbuild Debug/x64 신규 에러/경고 0.
</success_criteria>

<output>
Create `.planning/quick/260722-p5d-fix-arclineintersectdistancemeasurement-/260722-p5d-SUMMARY.md` when done
</output>
