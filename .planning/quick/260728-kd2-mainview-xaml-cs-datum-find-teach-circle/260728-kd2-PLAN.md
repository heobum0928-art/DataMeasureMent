---
phase: 260728-kd2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: true
requirements:
  - FIX-DatumStageError-Find
  - FIX-DatumStageError-Teach

must_haves:
  truths:
    - "When a multi-stage datum algorithm (CircleTwoHorizontal / VerticalTwoHorizontal / TwoLineIntersect and their DualImage variants) fails at a specific stage, the raw error string produced by DatumFindingService - including its stage prefix (\"Circle: \" / \"Horizontal_A: \" / \"Horizontal_B: \" / \"Line1: \" / \"Line2: \" / \"Vertical: \" / \"Circle fit failed: \" / \"Horizontal line fit failed: \" / \"Vertical line fit failed: \") - is visible verbatim inside the modal message shown to the operator. The stage information is never discarded."
    - "When the failing stage is the Circle stage, the remediation hint names RadialDirection(Inward/Outward) - NOT EdgeDirection. The Circle algorithm has no EdgeDirection field (DatumConfig.cs:383 permanently hides it), so the old EdgeDirection hint was actively wrong for that case."
    - "When the failing stage is anything other than Circle (Horizontal_A / Horizontal_B / Line1 / Line2 / Vertical, or an unprefixed error), the remediation hint still names EdgeDirection exactly as before - zero behavior change for those cases apart from the newly appended raw error text."
    - "The Circle-stage test catches BOTH producers: the Find path emits \"Circle: \" (DatumFindingService.cs:275) and the Teach path emits \"Circle fit failed: \" (DatumFindingService.cs:1056). A StartsWith(\"Circle\") test covers both; a literal StartsWith(\"Circle:\") test would silently miss the entire Teach path."
    - "The non-edge failure branch (the trailing \"Datum Find 에 실패했습니다: \" / \"티칭에 실패했습니다: \" returns) is untouched - it already echoed the raw err."
    - "FormatTeachError still emits its \"[DatumName] \" prefix on every return path, computed by the same existing datum != null && !string.IsNullOrEmpty(datum.DatumName) logic."
    - "Both method signatures are byte-identical to today (private static string FormatFindError(string err) / private static string FormatTeachError(DatumConfig datum, string err)) and all 3 call sites (MainView.xaml.cs:1136, :3624, :4151) are unmodified."
    - "No new file, no new class, no new method is created. Only the bodies (and the two adjacent doc-comments) of the two existing methods change."
    - "C# 7.2 only - plain if/else and string concatenation. No switch expression, no pattern matching, no interpolated string, no null-coalescing assignment."
    - "MSBuild Debug/x64 //t:Build of WPF_Example/DatumMeasurement.csproj produces zero 'error CS' diagnostics."
    - "Exactly one file is modified."
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "Stage-aware datum Find/Teach failure messages with algorithm-correct remediation hint"
      contains: "err.StartsWith(\"Circle\", System.StringComparison.OrdinalIgnoreCase)"
  key_links:
    - from: "DatumFindingService stage prefix (\"Horizontal_A: \" etc.)"
      to: "CustomMessageBox modal body"
      via: "raw err concatenated into the message instead of being dropped"
      pattern: "검출된 에지가 없습니다 \\("
    - from: "err.StartsWith(\"Circle\", ...)"
      to: "hint local variable"
      via: "explicit if/else selecting RadialDirection vs EdgeDirection wording"
      pattern: "RadialDirection\\(Inward/Outward\\)"
---

<objective>
MainView.xaml.cs 의 `FormatFindError` (2048행) 와 `FormatTeachError` (2034행) 는 datum 에지 검출 실패 시 실패 단계 정보를 전부 버리고 항상 동일한 제네릭 메시지 하나만 보여준다. 그 결과 (a) 사용자는 Circle / Horizontal_A / Horizontal_B / Line1 / Line2 / Vertical 중 **어느 ROI** 가 문제인지 알 수 없고, (b) Circle 단계 실패에는 존재하지도 않는 `EdgeDirection` 을 바꾸라는 **틀린 안내**가 나간다 (Circle 은 `RadialDirection` Inward/Outward 사용, DatumConfig.cs:383 주석 참조).

Purpose: 실패 원인 진단 시간을 없앤다. 모달 하나로 어느 ROI 가 실패했는지 + 어떤 파라미터를 뒤집어야 하는지 즉시 알 수 있게 한다.
Output: `WPF_Example/UI/ContentItem/MainView.xaml.cs` 단일 파일의 두 메서드 본문 수정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

Read-first (수정 대상 및 근거):
- `WPF_Example/UI/ContentItem/MainView.xaml.cs` — 2033~2056행 (두 메서드 전체). **이 파일은 K&R 스타일** (여는 중괄호가 선언과 같은 줄). `System.StringComparison` 을 `using` 없이 **전체 한정명으로** 쓰는 것이 이 블록의 기존 관례 (2039~2041, 2050~2052행). 그대로 따를 것. 파일 인코딩은 UTF-8 / CRLF (플래너 확인).
- `WPF_Example/Halcon/Algorithms/DatumFindingService.cs` — 아래 <error_string_survey> 에 조사 결과 요약. **다시 열어볼 필요 없음.**
</context>

<error_string_survey>
플래너가 이미 전수 조사한 결과. 실행자는 이 표를 사실로 받아들이고 DatumFindingService.cs 를 열지 말 것.

**Find 경로 (`TryFind*`) — 단계명 접두사를 붙임:**

| DatumFindingService.cs 행 | 생성되는 err 문자열 |
|---|---|
| 137 / 158 | `"Line1: " + lineError` / `"Line2: " + lineError` |
| 275 | `"Circle: " + circleError` |
| 292 / 309 | `"Horizontal_A: " + edgeErrorA` / `"Horizontal_B: " + edgeErrorB` |
| 459 / 642 | `"Vertical: " + lineError` |
| 476 / 493 / 662 / 679 | `"Horizontal_A: " / "Horizontal_B: "` |
| 316 / 500 / 686 | `"Horizontal line fit failed: insufficient edges (N)"` (A/B 합산 임계 — 단계 구분 없음) |

**Teach 경로 (`TryTeach*`) — 접두사 문구가 다름. 이것이 이 작업의 핵심 함정:**

| DatumFindingService.cs 행 | 생성되는 err 문자열 |
|---|---|
| 1056 | `"Circle fit failed: " + circleError` ← **`"Circle:"` 로 시작하지 않는다** |
| 1083 / 1105 / 1117 | `"Horizontal line fit failed: " + edgeErrorA/B` |
| 1245 / 1418 | `"Vertical line fit failed: " + lineError` |
| 909 / 932 | `"Line1: " / "Line2: "` |

**하위 단계 원문 (양쪽 경로 공통, `TryExtractEdgePoints` 내부):**
- 2088행: `"[{0}] no edges found across {1} strips. sigma=... threshold=... polarity=... scan=..."` — `{0}` = `"Horizontal_A"` / `"Horizontal_B"` 등 라벨
- 1828행: `"[{0}] insufficient edges across {1} strips: got {2} (need >=2). ..."`
- `VisionAlgorithmService.cs:553`: `"insufficient polar samples (N)"` — Circle 폴라 샘플링 전용

**결론 2가지 (구현에 직접 반영됨):**
1. Teach 경로 Horizontal 실패의 err 은 `"Horizontal line fit failed: [Horizontal_A] no edges found across 12 strips. sigma=..."` 형태다 — A/B 구분은 **대괄호 라벨 안에** 들어 있다. 그래서 err 원문을 그대로 보여주기만 하면 Teach 경로도 A/B 가 드러난다. 별도 파싱 불필요.
2. Circle 단계 판정은 반드시 `StartsWith("Circle", ...)` 이어야 한다. 지시서 문구는 `"Circle:"` 이지만, 그 리터럴을 그대로 쓰면 Teach 경로의 `"Circle fit failed: "` 를 놓쳐서 **정확히 문제 제기된 그 잘못된 EdgeDirection 안내가 Teach 쪽에 그대로 남는다.** `StartsWith("Circle")` 는 요구사항의 상위집합이며, 이 분기는 에지 실패 블록 **안에서만** 평가되므로 오탐 위험이 없다 (`ValidateRoiPresence` 의 "Circle ROI 가 없습니다" 같은 한국어 메시지는 애초에 이 함수를 통과하지 않고, `no edges`/`insufficient edges`/`insufficient polar samples` 중 어느 것도 포함하지 않는다).
</error_string_survey>

<tasks>

<task type="auto">
  <name>Task 1: FormatFindError / FormatTeachError 에 실패 단계 원문 보존 + Circle 전용 RadialDirection 힌트 분기 추가</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <action>
두 메서드 각각에서, `no edges` / `insufficient edges` / `insufficient polar samples` 3-조건 `if` 블록 **내부의 단 하나뿐인 `return` 문**을 아래 4단계 로직으로 교체한다. 3-조건 `if` 자체(2039~2041, 2050~2052행)는 한 글자도 건드리지 않는다.

1단계 — 힌트 지역변수 선언: `string hint;`

2단계 — Circle 단계 판정. 조건식은 정확히 이 토큰이어야 한다 (grep 게이트가 이 문자열을 센다):
`err.StartsWith("Circle", System.StringComparison.OrdinalIgnoreCase)`
`"Circle:"` (콜론 포함) 을 쓰지 말 것 — <error_string_survey> 결론 2 참조. Teach 경로가 `"Circle fit failed: "` 를 만들기 때문에 콜론을 넣으면 Teach 쪽 버그가 그대로 남는다.

3단계 — 두 힌트 문자열 대입. 리터럴은 아래와 정확히 일치해야 한다:
- 참 분기: `hint = "Circle 단계 실패 — RadialDirection(Inward/Outward) 설정을 반대로 변경한 후 다시 시도하세요.";`
- 거짓 분기: `hint = "EdgeDirection 설정을 반대로 변경한 후 다시 시도하세요.";`
거짓 분기 문구는 기존 제네릭 메시지의 뒷부분을 **글자 그대로** 재사용한 것이다 (Horizontal_A/B, Line1/Line2, Vertical, 접두사 없음 케이스의 회귀 0). 참 분기 문구의 `—` 는 U+2014 em dash 로, 이 파일 1137행 `"변환(transform) 획득 실패 — 유효하지 않은 결과입니다."` 와 동일한 관례다. 파일은 UTF-8 이므로 그대로 쓰면 된다.

4단계 — 원본 err 를 버리지 않고 메시지에 삽입해 반환:
- `FormatFindError`: `return "검출된 에지가 없습니다 (" + err + "). " + hint;`
- `FormatTeachError`: `return prefix + "검출된 에지가 없습니다 (" + err + "). " + hint;` — 기존 `prefix` (`"[" + datum.DatumName + "] "`) 계산 로직 및 그 위 4줄(2036~2038행)은 그대로 둔다.

스타일: 이 파일은 K&R 이고, 바로 위 2037~2038행이 `if (...) prefix = ...;` / `else` + 공백정렬 형태의 **중괄호 없는 단문 if/else** 를 쓴다. 새 if/else 도 같은 형태(중괄호 없는 단문)로 작성해 국소 관례를 맞춘다. C# 7.2 제약: 삼항연산자로 뭉치거나 switch expression / 문자열 보간(`$"..."`) 을 쓰지 말 것 — 평범한 if/else 와 `+` 연결만 사용한다.

주석 갱신 (선택 아님, 두 줄만): 2033행과 2047행의 doc-comment 는 현재 각각 `// [DatumName] 접두사 포함 티칭 에러 메시지 반환.` / `// Test Find 실패 사유 모달 메시지 변환. 검출 0개 케이스에 EdgeDirection 힌트 통합.` 인데, 뒤쪽은 이제 사실이 아니다. 두 줄을 "실패 단계 원문(err) 보존 + Circle 은 RadialDirection, 그 외는 EdgeDirection 힌트" 취지로 갱신한다.

건드리지 말 것 (명시적 금지):
- 호출부 3곳 — 1136행 `FormatFindError(err)`, 3624행 `FormatTeachError(_editingDatum, error)`, 4151행 `FormatFindError(error)` 및 3622/4150행의 호출부 주석. "기존 두 함수만 수정" 제약 범위 밖이다.
- 두 메서드의 시그니처, 접근제한자, `if (err == null) err = "unknown";` 방어 줄, 마지막 non-edge 반환 줄 2줄.
- `ValidateRoiPresence`, `ClearDatumRoiFields`, 그 외 파일 내 모든 코드.
- `DatumFindingService.cs`, `VisionAlgorithmService.cs`, `DatumConfig.cs` — 이번 작업에서 수정 금지.
  </action>
  <verify>
    <automated>cd "C:/code/DataMeasurement" && F=WPF_Example/UI/ContentItem/MainView.xaml.cs && CODE=$(grep -v '^[[:space:]]*//' "$F"); echo "--- structural (comment lines stripped) ---"; echo "circle_branch=$(echo "$CODE" | grep -cF 'err.StartsWith("Circle", System.StringComparison.OrdinalIgnoreCase)') (want 2)"; echo "no_colon_variant=$(echo "$CODE" | grep -cF 'StartsWith("Circle:"') (want 0)"; echo "radial_hint=$(echo "$CODE" | grep -cF 'RadialDirection(Inward/Outward)') (want 2)"; echo "edge_hint_kept=$(echo "$CODE" | grep -cF 'EdgeDirection 설정을 반대로 변경한 후 다시 시도하세요.') (want 2)"; echo "raw_err_echoed=$(echo "$CODE" | grep -cF '검출된 에지가 없습니다 (" + err + "). ') (want 2)"; echo "teach_prefix_used=$(echo "$CODE" | grep -cF 'return prefix + "검출된 에지가 없습니다 (') (want 1)"; echo "old_generic_gone=$(echo "$CODE" | grep -cF '검출된 에지가 없습니다. EdgeDirection') (want 0)"; echo "hint_local=$(echo "$CODE" | grep -cF 'string hint;') (want 2)"; echo "datumname_prefix_kept=$(echo "$CODE" | grep -cF '"[" + datum.DatumName + "] "') (want 1)"; echo "sig_find=$(echo "$CODE" | grep -cF 'private static string FormatFindError(string err) {') (want 1)"; echo "sig_teach=$(echo "$CODE" | grep -cF 'private static string FormatTeachError(DatumConfig datum, string err) {') (want 1)"; echo "guard_kept=$(echo "$CODE" | grep -cF 'if (err == null) err = "unknown";') (want 2)"; echo "find_tail_kept=$(echo "$CODE" | grep -cF 'return "Datum Find 에 실패했습니다: " + err;') (want 1)"; echo "teach_tail_kept=$(echo "$CODE" | grep -cF 'return prefix + "티칭에 실패했습니다: " + err;') (want 1)"; echo "callers_untouched=$(echo "$CODE" | grep -cE 'FormatFindError\(err\)|FormatFindError\(error\)|FormatTeachError\(_editingDatum, error\)') (want 3)"; echo "no_csharp8_added=$(git diff -U0 -- "$F" | grep '^+' | grep -cE '[$]"|=>|switch[[:space:]]*[{]') (want 0)"; echo "--- scope (must list ONLY MainView.xaml.cs) ---"; git diff --name-only; echo "--- build: any 'error CS' line below = FAIL. MSB3021/3026/3027 (bin copy lock from a running DatumMeasurement.exe) are NOT failures and are filtered out. ---"; "C:/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj //t:Build //p:Configuration=Debug //p:Platform=x64 //v:m //nologo 2>&1 | grep -E "error CS" | head -20; echo "CS_ERROR_LIST_ABOVE_MUST_BE_EMPTY"</automated>
  </verify>
  <done>
- 위 automated 게이트의 모든 `(want N)` 수치가 실제 출력과 일치한다. (플래너가 수정 전 상태로 이 게이트를 드라이런해 quoting 을 검증했고, 불변 항목 `guard_kept=2` `datumname_prefix_kept=1` `sig_find=1` `sig_teach=1` `find_tail_kept=1` `teach_tail_kept=1` `callers_untouched=3` `no_colon_variant=0` `edge_hint_kept=2` 는 이미 통과했다. 수정으로 바뀌어야 하는 값은 `circle_branch` 0→2, `radial_hint` 0→2, `hint_local` 0→2, `raw_err_echoed` 0→2, `teach_prefix_used` 0→1, `old_generic_gone` 2→0 이다.)
- `git diff --name-only` 출력이 `WPF_Example/UI/ContentItem/MainView.xaml.cs` 한 줄뿐이다.
- 빌드 출력에 `error CS` 가 한 줄도 없다.
- Circle 단계 실패 시 모달 본문이 원본 err 전문 + RadialDirection(Inward/Outward) 안내를 담는다.
- Horizontal_A / Horizontal_B / Line1 / Line2 / Vertical 및 접두사 없는 실패 시 모달 본문이 원본 err 전문 + 기존과 동일한 EdgeDirection 안내를 담는다.
- FormatTeachError 반환문 모든 경로가 여전히 `[DatumName] ` 로 시작한다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DatumFindingService 내부 진단 문자열 → 운영자 화면(CustomMessageBox) | 지금까지 로그(ELogType.Trace)에만 남던 상세 진단 문구가 UI 모달로 승격된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260728kd2-01 | Information Disclosure | `CustomMessageBox.Show("Find 실패"/"티칭 실패", ...)` 본문에 새로 노출되는 err 전문 (sigma / threshold / polarity / scan / strip count / ROI 라벨) | accept | 노출되는 값은 전량 로컬 비전 알고리즘 튜닝 파라미터로, 자격증명·PII·네트워크 토폴로지·경로 비밀이 없다. 동일 문자열이 이미 `Logging.PrintLog((int)ELogType.Trace, error)` 로 로컬 디스크에 기록된다(DatumFindingService.cs:1831, 2091). 대상 관객은 장비 앞의 운영자 단독(로컬 WPF 모달, 원격 전송 없음). 오히려 노출이 목적이며, 진단 정보 은닉이 이번 버그의 원인이었다. |
| T-260728kd2-02 | Denial of Service | 모달 본문 길이 — err 이 최대 ~200자(포맷 문자열 2088행 기준)까지 늘어남 | accept | `CustomMessageBox` 는 자동 줄바꿈 WPF 모달이며 200자 수준은 기존 다른 메시지(1137행 등)와 동급이다. 사용자 입력이 err 에 섞이지 않으므로 무한 증식 경로가 없다. |
| T-260728kd2-03 | Tampering | 잘못된 분기로 Circle 실패에 EdgeDirection 안내가 남아, 운영자가 존재하지 않는 필드를 찾아 헤매거나 엉뚱한 ROI 파라미터를 되돌려 정상 레시피를 훼손 | mitigate | `StartsWith("Circle", OrdinalIgnoreCase)` 로 Find(`"Circle: "`) / Teach(`"Circle fit failed: "`) **양 경로**를 포착. 게이트 `circle_branch=2` + `no_colon_variant=0` 으로 강제. |
| T-260728kd2-SC | Tampering | npm/pip/cargo 설치 | n/a | 이 작업은 패키지 설치가 전혀 없다 (기존 파일 1개의 메서드 2개 본문 수정). Package Legitimacy Gate 미적용. |
</threat_model>

<verification>
1. 구조 게이트: Task 1 의 `<automated>` 블록 전체 (주석 제거 후 토큰 카운트 + 변경 파일 범위 + `error CS` 0건).
2. 회귀 경계 확인: `git diff --stat` 이 단일 파일 / 대략 +14~18 -2 줄 수준. 이보다 크게 벌어지면 범위 이탈이므로 되돌릴 것.
3. 빌드 환경 주의 (플래너가 2026-07-28 실측 확인함):
   - 이 PC 에 **VS2022 는 없다.** 과거 quick 플랜(260727-mll / 260727-jna)에 적힌 `C:/Program Files/Microsoft Visual Studio/2022/Community/...` 경로는 더 이상 존재하지 않는다. 유효 경로는 `C:/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe` (MSBuild 18.7.1.23011). `C:/Windows/Microsoft.NET/Framework64/v4.0.30319/MSBuild.exe` 도 존재하지만 ToolsVersion 15.0 + WPF 마크업 컴파일에 부적합하므로 쓰지 말 것.
   - `//t:Compile` 이 아니라 **`//t:Build`** 를 `.sln` 이 아닌 **`.csproj`** 에 걸 것 (`//t:Compile` 은 MarkupCompilePass 를 건너뛰어 XAML 연동 파일에서 대량 위양성 CS0103/CS1061 을 낸다).
   - Git Bash 이므로 `/t:` 가 아니라 **`//t:`** 로 써야 경로 변환을 피한다.
   - 플래너 실측 기준선(수정 전): `error CS` 0건, `warning CS0618` 5건(Phase 33 마이그레이션 잔재, 무관), 그리고 실행 중인 `DatumMeasurement.exe`(PID 25928) + VS 가 `bin/x64/Debug/DatumMeasurement.exe` 를 잠가서 **MSB3026/MSB3027/MSB3021 로 종료코드 1**. 즉 **종료코드는 신뢰하지 말 것.** 판정 기준은 오직 `error CS` 라인 유무다. 앱과 VS 를 닫고 재빌드하면 종료코드 0 이 되지만 필수는 아니다.
</verification>

<success_criteria>
- `FormatFindError` / `FormatTeachError` 두 메서드 모두, 에지 검출 실패 분기에서 원본 err 문자열을 손실 없이 사용자 메시지에 포함한다.
- Circle 단계 실패(Find 의 `"Circle: "`, Teach 의 `"Circle fit failed: "` 양쪽)에는 RadialDirection(Inward/Outward) 안내가 나간다.
- 그 외 모든 실패에는 기존과 동일한 EdgeDirection 안내가 나간다.
- 두 메서드 시그니처와 3개 호출부가 무변경이다.
- 새 파일/새 클래스/새 메서드 생성 0건, 수정 파일 1개.
- Debug/x64 빌드에 `error CS` 0건.
</success_criteria>

<human_uat_pending>
자동 게이트는 "코드가 옳은 문자열을 만들도록 짜였고 컴파일된다" 까지만 증명한다. 실제 화면 문구는 아래를 실행해 확인해야 한다 (실행자 자동 범위 밖, 260727-mll 선례와 동일한 처리).

쉬운 말로 확인 순서:

1. 프로그램을 다시 빌드해서 실행한다. (지금 프로그램이 켜져 있으면 먼저 끄고 빌드해야 새 exe 가 bin 에 복사된다.)
2. CircleTwoHorizontal 을 쓰는 datum 을 하나 고른다.
3. **Horizontal_A 쪽을 일부러 틀리게** 만든다 — 예: Horizontal A 의 ROI 를 에지가 전혀 없는 빈 곳으로 옮긴다.
4. Find 버튼과 Teach 버튼을 각각 눌러본다.
   - 기대: 모달에 `Horizontal_A` 라는 글자가 보인다. 그리고 안내는 **EdgeDirection** 을 바꾸라고 나온다.
   - 예전: 어느 쪽이 문제인지 전혀 안 나오고 무조건 EdgeDirection 안내만 나왔다.
5. 이번엔 Horizontal_A 를 원래대로 되돌리고, **원(Circle) 쪽을 일부러 틀리게** 만든다 — 예: Circle ROI 반지름을 원이 없는 엉뚱한 크기로 준다.
6. Find 버튼과 Teach 버튼을 각각 눌러본다.
   - 기대: 모달에 `Circle` 이라는 글자가 보이고, 안내가 **RadialDirection(Inward/Outward)** 을 바꾸라고 나온다. EdgeDirection 이라는 말은 나오면 안 된다.
   - 특히 **Teach 버튼 쪽도** RadialDirection 으로 나오는지 꼭 확인할 것. Teach 는 내부 문구가 `Circle fit failed:` 로 달라서 이번에 별도로 맞춰둔 부분이다.
7. 여유가 되면 VerticalTwoHorizontal datum 으로도 Vertical 쪽을 틀리게 해서, 모달에 `Vertical` 이 보이고 안내는 EdgeDirection 인지 확인한다.
</human_uat_pending>

<output>
Create `.planning/quick/260728-kd2-mainview-xaml-cs-datum-find-teach-circle/260728-kd2-SUMMARY.md` when done
</output>
