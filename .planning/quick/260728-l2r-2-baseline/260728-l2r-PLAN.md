---
phase: quick-260728-l2r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: false
requirements: [L2R-01]
tags: [datum, pattern-match, align, refmatch, baseline, teach]

must_haves:
  truths:
    - "기준(ref) 포즈를 기록/갱신하는 경로가 실제 검사(TryComposeAlign)와 완전히 동일한 검색 범위·파라미터(ROI±margin, minScore, downsample 1.0)를 사용한다"
    - "align ON + 2-패턴 datum 을 재티칭한 직후 같은 이미지로 Test Find 하면 baseline 회전각이 ≈0° 로 나온다 ([ALIGN2] thetaDeg 로그)"
    - "패턴2 기준 갱신은 datum.PatternRoi2_* 필드(패턴1 필드가 아님)로 검색한다"
    - "패턴 ROI 미확보/매칭 실패 시 기존 RefMatch 가 그대로 유지되고 티칭 성공 자체는 취소되지 않는다"
    - "PatternMatchService.TryFindRefPose 본문과 AlignShapeMatchService 의 그 함수 사용은 전혀 바뀌지 않는다"
    - "InspectionSequence.TryComposeAlign 은 한 글자도 바뀌지 않는다"
    - "직전 커밋(260728-kd2)의 FormatTeachError/FormatFindError 변경이 그대로 보존된다"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "RefreshPatternRefPoseAfterTeach + InvokeCreatePatternModel 의 ref pose 기록이 범위제한 TryFindPose 로 통일"
      contains: "svc.TryFindPose("
  key_links:
    - from: "MainView.RefreshPatternRefPoseAfterTeach"
      to: "PatternMatchService.TryFindPose"
      via: "패턴1 = PatternRoi_*, 패턴2 = PatternRoi2_* + PatternSearchMarginPx/PatternMinScore/1.0"
      pattern: "svc\\.TryFindPose\\("
    - from: "MainView.InvokeCreatePatternModel"
      to: "PatternMatchService.TryFindPose"
      via: "모델 생성 직후 ref pose 기록(패턴1/패턴2)"
      pattern: "svc\\.TryFindPose\\("
    - from: "MainView.RefreshPatternRefPoseAfterTeach"
      to: "DatumConfig.EnsurePerRoiDefaults"
      via: "sentinel 0 → margin 100px / minScore 0.6 복원 (TryComposeAlign 진입부 미러)"
      pattern: "datum\\.EnsurePerRoiDefaults\\(\\);"
---

<objective>
2-패턴 baseline 각도 보정에서 **기준(ref) 포즈는 이미지 전체 무제한 검색**으로 구하고 **실제 검사는 좁은 범위 제한 검색**으로 구하던 불일치를 제거한다.

`MainView.xaml.cs` 의 ref pose 기록/갱신 4곳(`RefreshPatternRefPoseAfterTeach` 패턴1·패턴2, `InvokeCreatePatternModel` 패턴1·패턴2)이 호출하는 `PatternMatchService.TryFindRefPose`(전체 이미지 검색)를 `PatternMatchService.TryFindPose`(ROI±margin 범위 제한 검색)로 교체하고, 인자를 `InspectionSequence.TryComposeAlign` 의 라이브 매칭 호출과 byte-identical 하게 맞춘다.

Purpose: 패턴 점수가 낮은(0.74대) 템플릿에서 전체검색 최댓값과 범위검색 최댓값이 서로 다른 위치를 잡아 baseline 각이 1.4~2° 틀어지고, 패턴1↔패턴2 거리(~1만 px)만큼 증폭되어 datum ROI 가 수백 px 어긋나 검출 실패("Circle: insufficient polar samples (0)")로 이어지는 문제를 근본 제거. Phase 57.1-11 의 원래 의도("재티칭 직후엔 alignRigid≈identity → Find=Teach")를 복원한다.

Output: `WPF_Example/UI/ContentItem/MainView.xaml.cs` 단일 파일 수정 + Debug/x64 빌드 통과 + 실사용 재검증.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

수정 대상:
@WPF_Example/UI/ContentItem/MainView.xaml.cs

읽기 전용 참조(절대 수정 금지):
@WPF_Example/Halcon/Algorithms/PatternMatchService.cs
@WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs

<baseline_note>
**계획 수립 도중 같은 파일에 다른 quick 작업이 커밋되었다.**

`260728-kd2`(Datum Find/Teach 실패 메시지에 단계 원문 보존 + Circle RadialDirection 힌트) 가
`MainView.xaml.cs` 의 `FormatTeachError`/`FormatFindError`(약 2036~2063행)를 수정하고
커밋 `55f801b` + `fdb2b4b` 로 **이미 HEAD 에 들어갔다**. 워킹트리는 현재 깨끗하다.

- 아래 인용한 행 번호는 **kd2 커밋 이후(HEAD 기준)로 플래너가 재확인한 값**이다.
- 그래도 편집 전에 파일을 다시 Read 해서 확인하고, **행 번호가 아니라 식별자/문자열로 위치를 잡아라.**
- kd2 가 넣은 `RadialDirection(Inward/Outward)` 힌트 2줄을 지우면 안 된다 — 게이트의 `kd2_hunk_intact=2` 가 검사한다.
- 이번 작업 후 `git diff --name-only` 에는 `WPF_Example/UI/ContentItem/MainView.xaml.cs` **하나만** 떠야 한다.
</baseline_note>

<interfaces>
<!-- 플래너가 실제 소스에서 확인한 계약. 실행자는 재탐색 없이 이걸 그대로 쓴다. -->

PatternMatchService.cs:170 — 지금 쓰고 있는(=버려야 할) 시그니처:
  public bool TryFindRefPose(HImage templateImage, string engine, string modelPath, double minScore,
                             out double refRow, out double refCol, out double refAngleDeg, out double refScore, out string error)
  내부: 이미지 전체 대상 FindShapeModel/FindNccModel, reduce_domain 없음.

PatternMatchService.cs:304 — 바꿔 쓸(=목표) 시그니처:
  public bool TryFindPose(HImage runtimeImage, string engine, string modelPath,
                          double roiRow, double roiCol, double roiLen1, double roiLen2,
                          double marginPx, double minScore, double downsampleFactor,
                          out double curRow, out double curCol, out double curAngleDeg, out double curScore, out string error)
  내부: GenRectangle1(roiRow±(roiLen2+margin), roiCol±(roiLen1+margin)) → ReduceDomain → find. downsampleFactor<=1.0 이면 zoom 없음.

**출력 파라미터 확인 결과(플래너가 직접 대조함 — 실행자는 재확인 불필요):**
두 함수의 out 파라미터는 **개수·순서·타입이 완전히 동일**하다 —
`out double row, out double col, out double angleDeg, out double score, out string error` (double×4 + string).
따라서 호출부는 **out 변수 이름/순서를 그대로 두고, `modelPath` 와 첫 out 사이에 위치 인자 7개
(roiRow, roiCol, roiLen1, roiLen2, marginPx, minScore, downsampleFactor)를 끼워 넣기만 하면 된다.**
기존 `datum.PatternMinScore` 인자는 4번째 자리에서 8번째 자리(minScore)로 이동한다.

InspectionSequence.cs:1801 — 복제 대상(라이브 매칭, 패턴1):
  svc.TryFindPose(refImage, datum.PatternEngine, modelPath,
      datum.PatternRoi_Row, datum.PatternRoi_Col, datum.PatternRoi_Length1, datum.PatternRoi_Length2,
      datum.PatternSearchMarginPx, datum.PatternMinScore, /*downsampleFactor*/ 1.0,
      out curRow, out curCol, out curAngleDeg, out curScore, out error)

InspectionSequence.cs:1830 — 복제 대상(라이브 매칭, 패턴2 — **PatternRoi2_* 필드 사용**):
  svc.TryFindPose(refImage, datum.PatternEngine, modelPath2,
      datum.PatternRoi2_Row, datum.PatternRoi2_Col, datum.PatternRoi2_Length1, datum.PatternRoi2_Length2,
      datum.PatternSearchMarginPx, datum.PatternMinScore, /*downsampleFactor*/ 1.0,
      out cur2Row, out cur2Col, out cur2AngleDeg, out cur2Score, out err2)

InspectionSequence.cs:1797 — 라이브 경로는 매칭 **전에** 반드시 이걸 부른다:
  datum.EnsurePerRoiDefaults();
  (DatumConfig.cs:974-976 — PatternMinScore 0→0.6, PatternAngleExtentDeg 0→10.0, PatternSearchMarginPx 0→100.0. 멱등.)

**MainView.xaml.cs 편집 지점 (HEAD 기준 재확인 완료):**
  3643  private void RefreshPatternRefPoseAfterTeach(DatumConfig datum, HImage patternImage) {
  3648  var svc = new ReringProject.Halcon.Algorithms.PatternMatchService();   ← 새 두 줄을 이 앞에
  3651  if (svc.TryFindRefPose(patternImage, ... modelPath,  ...)              ← 패턴1 교체 대상
  3661  && svc.TryFindRefPose(patternImage, ... modelPath2, ...)              ← 패턴2 교체 대상
  3798  private void InvokeCreatePatternModel() {
  3810  datum.EnsurePerRoiDefaults();                                          ← 이미 존재(추가 금지)
  3855  if (svc.TryFindRefPose(img, ... modelPath,  ...)                       ← 패턴1 교체 대상
  3871  if (svc.TryFindRefPose(img, ... modelPath2, ...)                       ← 패턴2 교체 대상
</interfaces>

<root_cause_note>
플래너가 두 함수를 직접 대조한 결과, `TryFindRefPose` 와 `TryFindPose` 의 **각도 검색 범위는 둘 다 `-Math.PI, 2.0*Math.PI` 로 동일**하다(각도 제한은 `PatternAngleExtentDeg` 로 만들어진 **모델 파일 자체**에 들어 있고, 두 경로가 같은 모델 파일을 쓴다). 또 `downsampleFactor=1.0` 이면 zoom 도 없다.
→ **유일한 실질 차이는 검색 영역(전체 이미지 vs ROI±margin)이며, 그것이 이번 버그의 전부다.**
그러므로 이 작업에서 각도 관련 인자를 새로 넘기거나 `PatternAngleExtentDeg` 를 건드릴 일은 **없다**. 시도하지 말 것.
</root_cause_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ref pose 기록 4곳을 범위제한 TryFindPose 로 통일 (MainView.xaml.cs 단일 파일)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <action>
`WPF_Example/UI/ContentItem/MainView.xaml.cs` **한 파일만** 수정한다. 이 파일은 K&R 스타일(여는 중괄호가 선언과 같은 줄)이므로 그 스타일을 유지한다. C# 7.2 문법만 사용 — 보간 문자열(`$"`), switch expression, expression-bodied member(`=>`) 신규 도입 금지. 새 파일·새 클래스·새 메서드 생성 금지(아래 지정한 두 줄 추가 외에는 라인 추가 없음). `FormatTeachError`/`FormatFindError`(약 2036~2063행, 260728-kd2 커밋분) 는 **절대 건드리지 않는다**.

**수정 A — `RefreshPatternRefPoseAfterTeach` (3643~3670행)**

A-1. `if (string.IsNullOrEmpty(modelPath) || !System.IO.File.Exists(modelPath)) return;` 다음 줄, 3648행 `var svc = new ReringProject.Halcon.Algorithms.PatternMatchService();` **바로 앞**에 두 줄을 추가한다(각각 위에 `//260728 hbk quick-fix(260728-l2r)` 주석을 달아 이유를 남길 것):
  - 첫째 줄: `datum.EnsurePerRoiDefaults();` — 이 헬퍼는 현재 이걸 부르지 않아서 `PatternSearchMarginPx`/`PatternMinScore` 가 sentinel 0 인 채로 검색될 수 있다. sentinel 0 이면 검색영역이 ROI 에 딱 밀착(margin 0)해 라이브 경로(`InspectionSequence.cs:1797` 이 항상 이걸 먼저 부름)와 조건이 달라진다. 멱등이므로 매 호출 안전.
  - 둘째 줄: `if (datum.PatternRoi_Length1 <= 0.0 || datum.PatternRoi_Length2 <= 0.0) return;` — 패턴1 ROI 가 비어 있으면(구 레시피에 모델 파일만 남고 ROI 필드가 0인 경우) 범위제한 검색의 사각형이 이미지 좌상단 구석의 작은 박스로 무너져 무조건 no-match 가 된다. **이 경우 갱신을 시도하지 않고 조용히 빠져나가 기존 RefMatch 를 그대로 보존한다**(라이브 `TryComposeAlign` 도 같은 조건에서 실패하므로 새 위험을 만들지 않는다). 티칭 성공 자체는 이미 호출부에서 확정된 상태라 영향 없음.

A-2. 3651행 패턴1 호출 `if (svc.TryFindRefPose(patternImage, datum.PatternEngine, modelPath, datum.PatternMinScore, out rr, out rc, out ra, out rs, out refErr)) {` 를 `svc.TryFindPose(...)` 호출로 교체한다. 인자 순서는 정확히: `patternImage`, `datum.PatternEngine`, `modelPath`, `datum.PatternRoi_Row`, `datum.PatternRoi_Col`, `datum.PatternRoi_Length1`, `datum.PatternRoi_Length2`, `datum.PatternSearchMarginPx`, `datum.PatternMinScore`, `/*downsampleFactor*/ 1.0`, `out rr`, `out rc`, `out ra`, `out rs`, `out refErr`.
  줄바꿈은 `InspectionSequence.cs:1801-1807` 과 동일하게 3줄로 나눈다 — 2번째 줄은 `datum.PatternRoi_Row, datum.PatternRoi_Col, datum.PatternRoi_Length1, datum.PatternRoi_Length2,` 로 **정확히 이 텍스트**, 3번째 줄은 `datum.PatternSearchMarginPx, datum.PatternMinScore, /*downsampleFactor*/ 1.0,` 로 **정확히 이 텍스트**(검증 게이트가 이 리터럴을 센다). out 변수명 `rr/rc/ra/rs/refErr` 와 그 아래 `datum.RefMatchRow/Col/AngleDeg` 대입 3줄은 **그대로 둔다**.

A-3. 3661행 패턴2 호출 `&& svc.TryFindRefPose(patternImage, datum.PatternEngine, modelPath2, datum.PatternMinScore, out rr2, out rc2, out ra2, out rs2, out refErr2)` 를 같은 방식으로 `&& svc.TryFindPose(...)` 로 교체하되 ROI 인자는 **반드시 패턴2 필드**를 쓴다: `datum.PatternRoi2_Row`, `datum.PatternRoi2_Col`, `datum.PatternRoi2_Length1`, `datum.PatternRoi2_Length2`. margin/minScore/downsample 은 패턴1과 동일(`datum.PatternSearchMarginPx, datum.PatternMinScore, /*downsampleFactor*/ 1.0,`). 이 호출을 감싸는 기존 조건 `if (datum.PatternRoi2_Length1 > 0.0 && datum.PatternRoi2_Length2 > 0.0)` 과 `!string.IsNullOrEmpty(modelPath2) && System.IO.File.Exists(modelPath2) &&` 단락평가 가드는 **문자 하나도 바꾸지 않는다**. `datum.RefMatch2Row/Col` 대입 2줄도 그대로.

A-4. 헬퍼 맨 아래의 `[ALIGN-REFRESH]` Trace 로그, 실패 시 침묵(기존 RefMatch 유지) 동작, 메서드 시그니처, 호출부 2곳(`if (ok) { RefreshPatternRefPoseAfterTeach(...); }`)은 **전부 그대로**.

**수정 B — `InvokeCreatePatternModel` (3798행 시작)**

B-1. 3855행 패턴1: `if (svc.TryFindRefPose(img, datum.PatternEngine, modelPath, datum.PatternMinScore, out rr, out rc, out ra, out rs, out refError)) {` → A-2 와 동일 형태의 `svc.TryFindPose(...)` 로 교체(첫 인자만 `img`, 마지막 out 만 `refError`). 이 메서드는 이미 3810행에서 `datum.EnsurePerRoiDefaults();` 를 부르고 그 아래에서 `datum.PatternRoi_Length1 <= 0.0 || datum.PatternRoi_Length2 <= 0.0` 모달 가드를 하드로 걸어두므로 **추가 가드는 넣지 않는다**(넣으면 게이트의 `ensure_defaults=2` / `helper_p1_guard=1` 수치가 깨진다).

B-2. 3871행 패턴2: `if (svc.TryFindRefPose(img, datum.PatternEngine, modelPath2, datum.PatternMinScore, out rr2, out rc2, out ra2, out rs2, out refErr2)) {` → A-3 와 동일하게 `datum.PatternRoi2_*` 를 쓰는 `svc.TryFindPose(...)` 로 교체.

B-3. 실패 처리 경로는 **전부 유지**: 패턴1 실패 시 `CustomMessageBox.Show("ref pose 기록 실패", refError);`, 패턴2 실패 시 `alignMsg = "\n[경고] 패턴 2 모델 생성됨, ref pose 기록 실패 → 단일 패턴 폴백: " + (refErr2 ?? "");`. 여기서 실패 위험이 커지지 않는 이유: 모델을 방금 그 이미지의 그 ROI 에서 만들었으므로 패턴은 ROI 중심에 정확히 있고, 검색창은 ROI±margin(기본 100px)이라 반드시 잡힌다.

**절대 건드리지 말 것 (수정 시 롤백 대상)**
- `WPF_Example/Halcon/Algorithms/PatternMatchService.cs` — `TryFindRefPose` 본문/시그니처 유지. `AlignShapeMatchService.cs`(별개 EthernetVision 서브시스템)가 이 함수를 그대로 쓰고 있어 함수를 고치면 그쪽이 깨진다.
- `WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs` — 무변경.
- `WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` — `TryComposeAlign` 은 이미 정상. 무변경.
- `WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` — 무변경.
- `MainView.xaml.cs` 의 `FormatTeachError` / `FormatFindError` — 260728-kd2 커밋분. 무변경·무롤백.
- `PatternRoi_Phi` / `PatternRoi2_Phi` 는 `TryFindPose` 에 넘기지 않는다(그 함수는 축정렬 사각형만 만든다 — 라이브 경로와 동일). `PatternAngleExtentDeg` 도 넘기지 않는다(위 root_cause_note 참조).
  </action>
  <verify>
    <automated>cd "C:/code/DataMeasurement" && F=WPF_Example/UI/ContentItem/MainView.xaml.cs && CODE=$(grep -v '^[[:space:]]*//' "$F") && PMODAL='ref pose 기록 실패", refError);' && PC8='[$]"|=>|switch[[:space:]]*[{]'; echo "--- must CHANGE ---"; echo "refpose_calls_gone=$(echo "$CODE" | grep -cF 'svc.TryFindRefPose(') (want 0, was 4)"; echo "findpose_calls=$(echo "$CODE" | grep -cF 'svc.TryFindPose(') (want 4, was 0)"; echo "p1_roi_args=$(echo "$CODE" | grep -cF 'datum.PatternRoi_Row, datum.PatternRoi_Col, datum.PatternRoi_Length1, datum.PatternRoi_Length2,') (want 2, was 0)"; echo "p2_roi_args=$(echo "$CODE" | grep -cF 'datum.PatternRoi2_Row, datum.PatternRoi2_Col, datum.PatternRoi2_Length1, datum.PatternRoi2_Length2,') (want 2, was 0)"; echo "margin_minscore_ds=$(echo "$CODE" | grep -cF 'datum.PatternSearchMarginPx, datum.PatternMinScore, /*downsampleFactor*/ 1.0,') (want 4, was 0)"; echo "ensure_defaults=$(echo "$CODE" | grep -cF 'datum.EnsurePerRoiDefaults();') (want 2, was 1)"; echo "helper_p1_guard=$(echo "$CODE" | grep -cF 'if (datum.PatternRoi_Length1 <= 0.0 || datum.PatternRoi_Length2 <= 0.0) return;') (want 1, was 0)"; echo "--- must NOT change ---"; echo "helper_sig=$(echo "$CODE" | grep -cF 'private void RefreshPatternRefPoseAfterTeach(DatumConfig datum, HImage patternImage) {') (want 1)"; echo "teach_ok_hook=$(echo "$CODE" | grep -cF 'if (ok) { RefreshPatternRefPoseAfterTeach(') (want 2)"; echo "gate_align=$(echo "$CODE" | grep -cF 'if (!datum.IsPatternAlignEnabled) return;') (want 1)"; echo "gate_p2=$(echo "$CODE" | grep -cF 'datum.PatternRoi2_Length1 > 0.0 && datum.PatternRoi2_Length2 > 0.0') (want 2)"; echo "w2_modal_guard=$(echo "$CODE" | grep -cF '패턴 ROI(Rect) 를 먼저 그리세요') (want 1)"; echo "refpose_fail_modal=$(echo "$CODE" | grep -cF "$PMODAL") (want 1)"; echo "p2_fallback_msg=$(echo "$CODE" | grep -cF 'ref pose 기록 실패 → 단일 패턴 폴백') (want 1)"; echo "align_refresh_log=$(echo "$CODE" | grep -cF '[ALIGN-REFRESH]') (want 1)"; echo "kd2_hunk_intact=$(echo "$CODE" | grep -cF 'RadialDirection(Inward/Outward)') (want 2 - 260728-kd2 커밋분 보존)"; echo "svc_refpose_intact=$(grep -c 'public bool TryFindRefPose(' WPF_Example/Halcon/Algorithms/PatternMatchService.cs) (want 1)"; echo "align_svc_refpose_users=$(grep -c 'TryFindRefPose(' WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs) (want 2)"; echo "no_csharp8_added=$(git diff -U0 -- $F | grep '^+' | grep -cE "$PC8") (want 0)"; echo "--- scope: MainView.xaml.cs 외 소스파일이 뜨면 FAIL (.planning/ 은 무시) ---"; git diff --name-only; echo "--- build: 'error CS'/신규 'warning CS' 줄이 하나라도 있으면 FAIL. MSB3021/3026/3027(실행중 exe 파일잠금)은 컴파일 실패가 아니므로 애초에 걸러진다 ---"; "C:/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj //t:Build //p:Configuration=Debug //p:Platform=x64 //v:m //nologo 2>&1 | grep -E "error CS|warning CS" | grep -v -E "CS0618|CS0162" | head -20; echo "CS_LIST_ABOVE_MUST_BE_EMPTY"</automated>
  </verify>
  <done>
- 위 게이트의 모든 `(want N)` 이 실제 출력과 일치한다. 플래너가 **수정 전 상태에서 이 게이트를 계획서에서 그대로 추출해 드라이런**했고 quoting 과 불변 항목이 이미 통과했다: `helper_sig=1` `teach_ok_hook=2` `gate_align=1` `gate_p2=2` `w2_modal_guard=1` `refpose_fail_modal=1` `p2_fallback_msg=1` `align_refresh_log=1` `kd2_hunk_intact=2` `svc_refpose_intact=1` `align_svc_refpose_users=2` `no_csharp8_added=0` → **수정 후에도 그대로여야 한다**. 바뀌어야 하는 값은 `refpose_calls_gone` 4→0, `findpose_calls` 0→4, `p1_roi_args` 0→2, `p2_roi_args` 0→2, `margin_minscore_ds` 0→4, `ensure_defaults` 1→2, `helper_p1_guard` 0→1 뿐이다.
- 특히 `p1_roi_args=2` 와 `p2_roi_args=2` 가 동시에 성립해야 한다 — 패턴2 자리에 패턴1 필드를 복사-붙여넣기 했다면 `p1_roi_args=4, p2_roi_args=0` 으로 즉시 드러난다.
- `kd2_hunk_intact=2` 가 유지된다 — 0 이면 260728-kd2 커밋분을 지운 것이므로 즉시 되돌린다.
- `git diff --name-only` 출력의 소스파일은 `WPF_Example/UI/ContentItem/MainView.xaml.cs` **하나뿐**이다(작업 시작 시점 워킹트리는 깨끗했다).
- `CS_LIST_ABOVE_MUST_BE_EMPTY` 위에 `error CS` / 신규 `warning CS` 줄이 없다.
- **exe 파일잠금 주의(플래너 실측 확인됨):** `DatumMeasurement.exe` 가 실행 중이면 MSBuild 는 컴파일에 성공하고 `obj → bin` 복사 단계에서 MSB3026/MSB3027/MSB3021 로 끝난다. 이건 컴파일 실패가 **아니다**. 다만 **이 상태로는 `bin/x64/Debug/DatumMeasurement.exe` 가 갱신되지 않는다** — Task 2 로 넘기기 전에 실행 중인 DatumMeasurement.exe(및 Visual Studio 디버그 세션)를 닫고 빌드를 한 번 더 돌려 복사까지 성공시켰는지 확인하고, 그 사실을 사용자에게 알린다.
- 읽기 재확인: 중괄호 균형, `svc` 선언이 새로 추가한 두 줄보다 **뒤에** 있고 새 두 줄이 `modelPath` 존재 가드보다 **뒤에** 있다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 실사용 재검증 — 재티칭 직후 Test Find 정상 + baseline 각 ≈0 (checkpoint)</name>
  <files>(코드 수정 없음 — 실행 중인 앱에서 육안/로그 확인만 수행)</files>
  <action>Task 1 빌드 산출물(Debug/x64 `DatumMeasurement.exe`)을 사용자가 직접 실행해 아래 how-to-verify 절차를 수행하고 결과를 보고한다. 실행자는 사용자 응답을 받기 전까지 추가 코드 수정을 하지 않는다. 사용자가 실패 항목을 보고하면 그 로그(`[ALIGN2]` / `[ALIGN-REFRESH]`)를 근거로 원인을 분석해 보고하고, 새 범위 수정 전에 승인을 받는다.</action>
  <what-built>
기준값(ref)을 만들 때와 실제로 검사할 때가 **서로 다른 방식으로 사진을 뒤지던 것**을 하나로 맞췄습니다.

전에는 기준값을 만들 때 사진 전체를 훑어서 제일 비슷한 곳을 골랐고, 실제 검사할 때는 지정한 네모 근처만 훑었습니다. 패턴이 흐릿해서 점수가 낮으면(예: 패턴2가 0.74) 두 방식이 같은 사진에서도 살짝 다른 지점을 집을 수 있었고, 그러면 부품이 전혀 안 움직였는데도 각도가 1.4~2도 틀어진 것처럼 계산됐습니다. 두 패턴 사이가 1만 픽셀쯤 떨어져 있어서 그 작은 각도가 수백 픽셀 위치 오차로 커졌고, 그래서 원(Circle) 검출이 "샘플 0개"로 실패했던 겁니다.

이제 기준값도 실제 검사와 똑같이 "지정한 네모 근처만" 훑습니다. 그래서 방금 다시 티칭한 사진에서는 두 값이 반드시 같아지고, 부품이 진짜로 움직였을 때만 각도가 나옵니다.
  </what-built>
  <how-to-verify>
아래 순서대로 확인해 주세요.

0. **먼저 이게 제일 중요합니다 — 새 프로그램으로 테스트하는지 확인.**
   지금 켜져 있는 DatumMeasurement 프로그램을 **완전히 닫고**(Visual Studio 로 디버깅 중이면 그것도 정지), 새로 빌드된 것을 다시 실행해 주세요. 안 닫으면 파일이 잠겨서 **예전 프로그램이 그대로 실행**되고, 고친 게 하나도 반영되지 않은 상태로 테스트하게 됩니다.
1. **문제가 났던 그 Datum** 을 트리에서 고릅니다. 속성창에서 `Is pattern align enabled` 를 **켭니다(ON)** — 지난번에 이걸 끄고 확인했던 그 항목입니다.
2. `[패턴 모델 생성]` 버튼을 누릅니다. 완료 창에 나오는 **패턴1 점수와 패턴2 점수**를 적어 주세요. (패턴2가 0.74 근처로 나와도 정상입니다.)
   - 저장할지 물어보면 **예(Recipe Save)** 를 누릅니다.
3. `[Datum 티칭]` 을 눌러 재티칭을 완료합니다.
4. `[Test Find]` 를 누릅니다.
   - **기대**: 예전처럼 "Circle: insufficient polar samples (0)" 같은 실패가 나지 않고 정상 검출된다. 파란(Find) 선이 노란(Teach) 선과 거의 겹친다.
5. Trace 로그를 열어 방금 Test Find 의 `[ALIGN2]` 줄을 찾습니다.
   - **기대**: `thetaDeg` 값이 **0에 매우 가깝다**(대략 ±0.05도 이내). 예전처럼 1.4~2도가 나오면 실패입니다.
   - `[ALIGN-REFRESH]` 줄도 함께 남아 있는지 봐 주세요.
6. **회귀 확인(중요)**: `Is pattern align enabled` 가 **꺼져 있는** 다른 Datum 하나를 골라 티칭 → Test Find 를 해 봅니다.
   - **기대**: 예전과 똑같이 동작한다(아무 변화 없음).
7. **여유 있으면**: 부품을 실제로 살짝 돌려서 다시 Test Find 를 해 봅니다.
   - **기대**: 이때는 `thetaDeg` 가 0이 아니라 실제 돌린 만큼 나오고, 검출도 성공한다. (보정이 죽은 게 아니라 살아 있다는 확인)

문제가 있으면 `[ALIGN2]` / `[ALIGN-REFRESH]` 로그 줄과 화면 상태를 알려 주세요.
  </how-to-verify>
  <verify>
    <human-check>0번(새 빌드로 실행) 전제 하에 4번(Test Find 성공) + 5번(thetaDeg≈0) + 6번(align OFF 무변화) 세 가지가 모두 통과</human-check>
    <automated>MISSING — 이 프로젝트는 테스트 프레임워크가 없고 이 항목은 실제 HALCON 매칭 결과/화면 확인이라 자동화 불가. 구조·빌드 검증은 Task 1 게이트가 전담한다.</automated>
  </verify>
  <done>
- 사용자가 0번(새 빌드 실행 확인) 후 4번(Test Find 가 예전 실패 없이 정상 검출), 5번(`[ALIGN2] thetaDeg` 가 0 근처), 6번(align OFF Datum 무변화) 세 항목을 모두 통과로 확인했다.
- 패턴1/패턴2 점수와 `[ALIGN2] thetaDeg` 실측값이 SUMMARY 에 기록됐다.
- 실패 항목이 있었다면 로그와 함께 그대로 기록하고, 임의로 추가 수정하지 않았다.
  </done>
  <resume-signal>"approved" 라고 입력하거나, 안 된 항목의 번호와 로그를 알려 주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레시피 파일(INI) → DatumConfig | 사용자/구버전 레시피가 `PatternRoi_*` 를 0 으로 남길 수 있음 (신뢰 불가 입력) |
| 티칭 이미지 파일 → HImage | 로컬 디스크 이미지, 기존 경로와 동일 (신규 노출 없음) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-l2r-01 | Tampering | 구 레시피의 `PatternRoi_Length1/2 == 0` 상태에서 범위검색이 좌상단 구석 박스로 무너져 기준값이 잘못 갱신 | mitigate | Task 1 A-1 의 패턴1 ROI 부재 가드(`<= 0.0` 이면 즉시 return) — 갱신 시도 없이 기존 RefMatch 보존 |
| T-l2r-02 | Tampering | `PatternSearchMarginPx`/`PatternMinScore` sentinel 0 로 인해 ref 와 live 조건이 또 갈라짐 | mitigate | Task 1 A-1 의 `datum.EnsurePerRoiDefaults();` — `TryComposeAlign` 진입부와 동일한 멱등 폴백 |
| T-l2r-03 | Denial of Service | `TryFindPose` 실패 시 티칭/모델생성 흐름이 막힘 | accept | 기존 실패 경로 그대로 유지 — 헬퍼는 silent return(RefMatch 유지), 모델생성은 기존 모달/폴백 메시지. 티칭 성공은 취소되지 않음 |
| T-l2r-04 | Tampering | 공유 함수 `TryFindRefPose` 를 고쳐 `AlignShapeMatchService`(EthernetVision) 가 깨짐 | mitigate | `PatternMatchService.cs` 무변경 강제 + 게이트의 `svc_refpose_intact=1`, `align_svc_refpose_users=2`, `git diff --name-only` 스코프 검사 |
| T-l2r-05 | Spoofing | 실행 중 exe 파일잠금으로 **옛 바이너리**를 새 것으로 착각해 UAT 통과 판정 | mitigate | Task 1 done 의 exe 잠금 확인 + Task 2 how-to-verify 0번(프로그램 완전 종료 후 재실행) |
| T-l2r-06 | Tampering | 직전 커밋(260728-kd2)의 같은 파일 변경을 되돌림 | mitigate | baseline_note + 게이트 `kd2_hunk_intact=2` |
| T-l2r-SC | Tampering | npm/pip/cargo 설치 | n/a | 이번 작업에 패키지 설치 없음 — 신규 의존성 0 |
</threat_model>

<verification>
1. Task 1 의 automated 게이트 전 항목 통과 (수치 일치 + `error CS`/신규 `warning CS` 0건 + diff 스코프 소스파일 1개).
2. `git diff -- WPF_Example/Halcon/Algorithms/PatternMatchService.cs` 가 **빈 출력**.
3. `git diff -- WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 가 **빈 출력**.
4. `git diff -- WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs` 가 **빈 출력**.
5. 260728-kd2 의 `FormatTeachError`/`FormatFindError` 코드가 파일에 그대로 살아 있다 (`kd2_hunk_intact=2`).
6. `bin/x64/Debug/DatumMeasurement.exe` 가 실제로 갱신됐다(exe 잠금 해제 후 복사 성공).
7. Task 2 human-verify 3개 항목(Test Find 성공 / thetaDeg≈0 / align OFF 무변화) 통과.
</verification>

<success_criteria>
- `MainView.xaml.cs` 안에 `svc.TryFindRefPose(` 가 0건, `svc.TryFindPose(` 가 4건.
- 4건 모두 `datum.PatternSearchMarginPx, datum.PatternMinScore, /*downsampleFactor*/ 1.0,` 을 넘긴다 (라이브 경로와 동일).
- 패턴1 호출 2건은 `PatternRoi_*`, 패턴2 호출 2건은 `PatternRoi2_*` 를 쓴다 (교차 오염 0).
- `RefreshPatternRefPoseAfterTeach` 가 매칭 전에 `EnsurePerRoiDefaults()` 를 부르고 패턴1 ROI 부재 시 조용히 return 한다.
- 기존 가드/실패 처리/로그/호출부는 무변경. 260728-kd2 커밋분 보존.
- `PatternMatchService.cs` / `InspectionSequence.cs` / `AlignShapeMatchService.cs` 무변경.
- Debug/x64 빌드 0 error CS, 신규 CS warning 0.
- 재티칭 직후 Test Find 에서 `[ALIGN2] thetaDeg` ≈ 0 (Phase 57.1-11 의도 복원), align OFF 경로 회귀 0.
</success_criteria>

<output>
Create `.planning/quick/260728-l2r-2-baseline/260728-l2r-SUMMARY.md` when done.

SUMMARY 에 반드시 기록할 것:
- 게이트 실측 수치 표 (want vs actual).
- 패턴2 점수가 낮은 상태(0.74대)에서도 thetaDeg≈0 이 나왔는지 — 안 나왔다면 그 수치.
- 행동 변화 경계: 패턴이 티칭 위치에서 `PatternSearchMarginPx`(기본 100px) 를 넘어 벗어난 이미지로 재티칭하면, 이제 ref 갱신이 실패해 **기존 RefMatch 가 유지**된다(예전엔 전체검색이라 어떻게든 갱신됐다). 이건 의도된 변화이며, 그런 경우 사용자는 패턴 ROI 를 새 위치로 다시 그려야 한다 — 이 문장을 사용자에게 그대로 전달할 것.
</output>
