---
phase: quick-260729-e7v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: true
requirements: [E7V-01]
tags: [datum, inspection-sequence, owner-name, mainview, refactor, hardening]

must_haves:
  truths:
    - "datum 범위 TryComposeAlign 호출 3곳이 모두 그 datum 이 실제로 속한 시퀀스 인스턴스로 실행된다 (Bottom_Datum → BOTTOM 인스턴스, Top_Datum → TOP 인스턴스)"
    - "datum 이 null 이거나 OwnerName 이 비었거나 그 role 시퀀스가 이 PC 에 미등록/비활성이면 GetAnyInspectionSequence() 결과로 폴백해 오늘과 동일하게 동작한다"
    - "GetAnyInspectionSequence() 의 본문(시그니처 포함)은 한 줄도 바뀌지 않는다 — 주석만 '폴백 전용' 명시로 갱신"
    - "GetAnyInspectionSequence() 의 호출부가 코드 기준 정확히 1곳(GetInspectionSequenceForDatum 의 폴백)만 남는다"
    - "Debug/x64 빌드가 error CS 0건 + 신규 warning CS 0건으로 통과한다"
    - "MainView.xaml.cs 외 어떤 소스 파일도 수정되지 않는다 — 특히 InspectionSequence.cs 의 ResolveDatumModelPath/ResolveDatumModelPath2/TryComposeAlign 무변경"
    - "사용자가 관찰하는 Test Find / 재앵커 동작은 오늘과 동일하다 — 모델 경로는 이미 datum.OwnerName 기준이라 인스턴스가 바뀌어도 읽는 .shm 이 달라지지 않는다"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "datum 소유 시퀀스 인스턴스를 role-aware 로 반환하는 private 헬퍼 + 기존 임의-인스턴스 조회의 폴백 격하"
      contains: "private ReringProject.Sequence.InspectionSequence GetInspectionSequenceForDatum(DatumConfig datum)"
  key_links:
    - from: "MainView.GetInspectionSequenceForDatum"
      to: "SequenceHandler this[string name] 인덱서"
      via: "pSeq[datum.OwnerName] 결과를 InspectionSequence 로 as-cast"
      pattern: "pSeq\\[datum\\.OwnerName\\] as ReringProject\\.Sequence\\.InspectionSequence"
    - from: "MainView.GetInspectionSequenceForDatum"
      to: "MainView.GetAnyInspectionSequence"
      via: "소유 시퀀스 미해결 시 폴백 return (유일한 잔여 호출부)"
      pattern: "return GetAnyInspectionSequence\\(\\);"
    - from: "TryFindTransformForReanchor / BtnTestFindDatum_Click(DualImage) / BtnTestFindDatum_Click(단일)"
      to: "MainView.GetInspectionSequenceForDatum"
      via: "기존 GetAnyInspectionSequence() 호출을 GetInspectionSequenceForDatum(datum) 으로 교체 (지역변수 datum 이미 스코프 내)"
      pattern: "InspectionSequence seq = GetInspectionSequenceForDatum\\(datum\\);"
---

<objective>
`MainView.xaml.cs` 의 datum 범위 `TryComposeAlign` 호출 3곳이 **임의 InspectionSequence 인스턴스**(`GetAnyInspectionSequence()`, 실질적으로 항상 TOP) 대신 **그 datum 을 실제로 소유한 시퀀스 인스턴스**를 쓰도록 role-aware 헬퍼 `GetInspectionSequenceForDatum(datum)` 를 추가하고 호출부를 교체한다.

Purpose: 오늘 두 번(260728-n7b, 260728-l2r) 발생한 "인스턴스 정체성에 의존하는 로직이 조용히 남의 시퀀스 데이터를 집는" 버그 계열의 재발을 구조적으로 차단한다. 현재는 modelPath/modelPath2 가 이미 `datum.OwnerName` 기준으로 독립 해석되므로 **증상은 없다**. 그러나 앞으로 `seq` 인스턴스 정체성에 의존하는 코드가 `TryComposeAlign` 안팎에 하나라도 생기면 즉시 같은 버그가 재현된다. 이번 작업은 그 구조적 방어선을 세우는 순수 리팩토링이다.

Output: `WPF_Example/UI/ContentItem/MainView.xaml.cs` **단일 파일** 수정(신규 private 메서드 1개 + 호출부 3줄 교체 + 주석 갱신) + Debug/x64 빌드 통과. 신규 파일 없음, 동작 변경 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

수정 대상 (이 파일 하나만):
@WPF_Example/UI/ContentItem/MainView.xaml.cs

읽기 전용 참조 (절대 수정 금지):
@WPF_Example/Sequence/SequenceHandler.cs
@WPF_Example/Sequence/Param/ParamBase.cs
@WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs

<baseline_note>
작업 시작 시점 워킹트리는 **깨끗하다**(플래너가 `git status --porcelain` 로 확인, HEAD = `9a5732e`). 260728-mxj 진단 로그는 이미 커밋에 포함되어 있으므로 이번 diff 에는 잡히지 않는다. 따라서 이 작업의 `git diff --name-only` 에는 **`WPF_Example/UI/ContentItem/MainView.xaml.cs` 단 하나**만 나와야 한다(`.planning/` 문서 제외).
</baseline_note>

<interfaces>
플래너가 코드베이스에서 직접 확인한 계약 — 실행자는 추가 탐색 없이 이대로 사용한다.

`WPF_Example/Sequence/SequenceHandler.cs:138` — 이름으로 시퀀스를 찾는 인덱서(이미 존재, 신규 인프라 불필요):

```csharp
public SequenceBase this[string name] {
    get {
        for(int i = 0; i < Sequences.Count; i++) {
            SequenceBase seq = Sequences.ElementAt(i).Value;
            if (seq.Name == name) {
                return seq;
            }
        }
        return null;
    }
}
```

`WPF_Example/Sequence/Param/ParamBase.cs:41` — `OwnerName` 은 소유자가 `SequenceBase` 면 그 `.Name` 을 그대로 반환한다:

```csharp
public string OwnerName
{
    get{
        if (Owner is ActionBase)   return (Owner as ActionBase).Name;
        if (Owner is SequenceBase) return (Owner as SequenceBase).Name;
        return null;
    }
}
```

`WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs:1562` — `DatumConfig` 의 **유일한** 생성 경로. `this` 는 항상 소유 `InspectionSequence` 이므로 `datum.OwnerName` 은 "TOP"/"SIDE"/"BOTTOM" 중 하나로 신뢰 가능:

```csharp
var datum = new DatumConfig(this);
```

`WPF_Example/UI/ContentItem/MainView.xaml.cs:32,105` — 필드는 이미 존재:

```csharp
private SequenceHandler pSeq;   // line 32
pSeq = SystemHandler.Handle.Sequences;   // line 105
```
</interfaces>

<target_shape>
아래는 플래너가 확정한 **최종 코드 형태**다. 실행자는 이 모양 그대로 적용한다(주석 문구는 의미가 유지되면 미세 조정 가능, 코드 라인은 그대로).

**(A) 기존 `GetAnyInspectionSequence()` 위 주석 — 마지막 두 줄만 추가. 본문은 무변경.**

```csharp
        //260622 hbk Phase 57.1 Test Find 패턴 보정 연결 — TryComposeAlign 호출용 임의 활성 InspectionSequence 인스턴스 획득
        //  TryComposeAlign 은 sequence 실행 상태 무관(인자로 입력, _datumTransforms 만 transient) → 어느 인스턴스든 가능.
        //260729 hbk quick(260729-e7v): 이제 datum 범위 조회의 1차 경로가 아니라 **폴백 전용**이다.
        //  datum 이 딸린 시퀀스를 써야 하는 호출은 반드시 GetInspectionSequenceForDatum(datum) 을 쓴다.
        private ReringProject.Sequence.InspectionSequence GetAnyInspectionSequence() {
            if (pSeq == null) return null;
            ESequence[] roles = new ESequence[] { ESequence.Top, ESequence.Side, ESequence.Bottom };
            for (int i = 0; i < roles.Length; i++) {
                ReringProject.Sequence.InspectionSequence seq = pSeq[roles[i]] as ReringProject.Sequence.InspectionSequence;
                if (seq != null) return seq;
            }
            return null;
        }
```

**(B) 신규 메서드 — (A) 바로 아래, `// 재앵커 대기 상태` 주석 블록 위에 삽입.**

```csharp
        //260729 hbk quick(260729-e7v): datum 범위 호출은 그 datum 을 **실제로 소유한** 시퀀스 인스턴스로 해야 한다.
        //  임의 인스턴스(GetAnyInspectionSequence, 사실상 항상 TOP)를 넘기면, 인스턴스 정체성에 의존하는 로직이
        //  조용히 남의 시퀀스 데이터를 집는다 — 이 버그 계열로 하루에 두 번 당했다(260728-n7b 패턴2 모델경로, 260728-l2r ref pose baseline).
        //  DatumConfig.OwnerName 은 생성 경로가 InspectionSequence.AddDatum() → new DatumConfig(this) 단 하나라 소유 시퀀스명("TOP"/"SIDE"/"BOTTOM")으로 신뢰 가능.
        //  해당 role 이 이 PC 에서 미등록(SequenceHandler.IsSequenceActive 가 Top/Bottom 롤 PC 에서 Side 를 제외)이면 조회 실패 → 기존 동작 보존 위해 폴백.
        private ReringProject.Sequence.InspectionSequence GetInspectionSequenceForDatum(DatumConfig datum) {
            if (pSeq == null) return null;
            if (datum != null && !string.IsNullOrEmpty(datum.OwnerName)) {
                ReringProject.Sequence.InspectionSequence owner = pSeq[datum.OwnerName] as ReringProject.Sequence.InspectionSequence;
                if (owner != null) return owner;
            }
            return GetAnyInspectionSequence();
        }
```

**(C) 호출부 3곳 — 우변만 교체(좌변/변수명/주변 코드 무변경).**

교체 전(3곳 모두 동일 문자열):

```csharp
                    ReringProject.Sequence.InspectionSequence seq = GetAnyInspectionSequence();
```

교체 후:

```csharp
                    ReringProject.Sequence.InspectionSequence seq = GetInspectionSequenceForDatum(datum);
```

위치(현재 줄번호, 삽입으로 밀리므로 문자열로 찾을 것):
- `TryFindTransformForReanchor` 내 `if (datum.IsPatternAlignEnabled)` 분기 — 현재 1119행 (지역 파라미터 `DatumConfig datum` 스코프 내)
- `BtnTestFindDatum_Click` DualImage 분기 — 현재 4112행 (지역변수 `datum` 스코프 내)
- `BtnTestFindDatum_Click` 단일 이미지 분기 — 현재 4139행 (지역변수 `datum` 스코프 내)

각 줄의 **들여쓰기는 현재 파일 그대로 유지**한다(세 곳의 들여쓰기 깊이가 서로 다르다: 1119행은 16칸, 4112행은 28칸, 4139행은 20칸).
</target_shape>

<style_note>
`MainView.xaml.cs` 는 **K&R 브레이스**다(여는 중괄호가 선언과 같은 줄). 1080/1105/3942/4064행 전부 K&R 확인. Allman(중괄호 새 줄)로 쓰지 말 것 — `InspectionSequence.cs` 의 Allman 스타일과 혼동 금지. CLAUDE.md 의 "편집 중인 파일/모듈의 스타일을 따르고 한 파일 안에서 섞지 않는다" 규칙 적용.
주석은 WHY 만 — 코드가 그대로 말해주는 WHAT(예: "pSeq 가 null 이면 null 반환")은 쓰지 않는다.
</style_note>

<no_behavior_change_note>
이번 변경은 **사용자 관찰 동작을 바꾸지 않는다**. 플래너가 확인한 근거:

1. 3곳 모두 `seq` 로 하는 일은 `seq.TryComposeAlign(...)` 호출뿐이고, 넘기는 `modelPath` 는 이미 `ResolveDatumModelPath(datum, datum.OwnerName)` 로 인스턴스와 무관하게 산출된다. `TryComposeAlign` 내부의 패턴2 경로도 260728-n7b 수정으로 `ResolveDatumModelPath2(datum, datum.OwnerName)` 이 됐다. 즉 **읽는 .shm 파일은 인스턴스가 바뀌어도 동일**하다.
2. `TryComposeAlign` 이 인스턴스에 남기는 유일한 상태는 `_datumTransforms[datumKey]`(InspectionSequence.cs:1912) 인데, 이 사전은 매 검사 사이클 시작 시 `ClearDatumTransforms()`(InspectionSequence.cs:1577)로 비워진다. 따라서 Test Find 가 TOP 대신 BOTTOM 인스턴스 사전에 쓰게 되더라도 다음 AUTO 검사에 누수되지 않는다(오히려 소유자 사전에 쓰는 쪽이 의미상 정확하다).
3. `datum.CurrentTransform`(InspectionSequence.cs:1916)은 datum 객체 자체에 쓰이므로 인스턴스와 무관.

→ 실행자는 이번 작업에서 **동작을 "고치려고" 어떤 추가 수정도 하지 않는다.** 동작 차이를 발견하면 수정하지 말고 보고한다.
</no_behavior_change_note>

<tasks>

<task type="auto">
  <name>Task 1: GetInspectionSequenceForDatum 추가 + datum 범위 호출부 3곳 교체</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <action>
`<target_shape>` 의 (A)(B)(C) 를 그 순서대로 정확히 적용한다.

(A) 현재 3940~3941행의 `GetAnyInspectionSequence()` 위 주석 블록 끝에 260729-e7v 폴백 격하 주석 2줄을 덧붙인다. 기존 주석 2줄과 메서드 본문 6줄은 **한 글자도 건드리지 않는다**.

(B) `GetAnyInspectionSequence()` 닫는 중괄호 바로 다음(현재 3950행 뒤, `// 재앵커 대기 상태` 주석 위)에 빈 줄 하나를 두고 `GetInspectionSequenceForDatum(DatumConfig datum)` 를 삽입한다. 클래스 멤버 들여쓰기 8칸, K&R 브레이스. 주석은 `<target_shape>` (B) 의 WHY 5줄 — 왜 임의 인스턴스가 위험한지, 두 선행 버그(260728-n7b / 260728-l2r) 참조, `OwnerName` 을 믿을 수 있는 근거, 그리고 폴백이 존재하는 이유를 담는다.

(C) `GetAnyInspectionSequence();` 로 끝나는 호출부 3줄(현재 1119 / 4112 / 4139행)을 문자열로 찾아 우변만 `GetInspectionSequenceForDatum(datum);` 로 바꾼다. 좌변 타입/변수명 `seq`, 각 줄의 들여쓰기, 앞뒤 주석과 분기 구조는 그대로 둔다. 세 곳 모두 지역 `datum` 이 이미 스코프에 있으므로 새 변수 선언이나 null 체크를 추가하지 않는다.

금지 사항:
- `GetAnyInspectionSequence()` 본문 수정·삭제 금지 (폴백으로 살아 있어야 한다).
- `InspectionSequence.cs` 의 `ResolveDatumModelPath` / `ResolveDatumModelPath2` / `TryComposeAlign` 을 포함해 **다른 어떤 파일도 수정 금지**. 신규 파일 생성 금지.
- 로직 변경 금지 — 이번 커밋은 순수 리팩토링/하드닝이다. `datum` null 체크 추가, 에러 메시지 변경, 로그 추가 등 "겸사겸사" 수정 금지.
- C# 8+ 문법 금지(이 프로젝트는 C# 7.2): 보간 문자열 `$"..."`, expression-bodied member `=>`, switch expression 사용하지 않는다. 문자열 결합은 `+`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && M=WPF_Example/UI/ContentItem/MainView.xaml.cs && CODE=$(grep -v '^[[:space:]]*//' "$M") && OLDCALL='ReringProject.Sequence.InspectionSequence seq = GetAnyInspectionSequence();' && NEWCALL='ReringProject.Sequence.InspectionSequence seq = GetInspectionSequenceForDatum(datum);' && GAS='private ReringProject.Sequence.InspectionSequence GetAnyInspectionSequence() {' && GFD='private ReringProject.Sequence.InspectionSequence GetInspectionSequenceForDatum(DatumConfig datum) {' && PC8='[$]"|=>|switch[[:space:]]*[{]'; echo "--- must CHANGE (baseline = 플래너 수정전 실측) ---"; echo "getany_total=$(echo "$CODE" | grep -cF 'GetAnyInspectionSequence') (want 2 = def + 폴백호출, was 4)"; echo "old_callsites=$(echo "$CODE" | grep -cF "$OLDCALL") (want 0, was 3)"; echo "new_callsites=$(echo "$CODE" | grep -cF "$NEWCALL") (want 3, was 0)"; echo "getfordatum_def=$(echo "$CODE" | grep -cF "$GFD") (want 1, was 0)"; echo "fallback_return=$(echo "$CODE" | grep -cF 'return GetAnyInspectionSequence();') (want 1, was 0)"; echo "owner_lookup=$(echo "$CODE" | grep -cF 'pSeq[datum.OwnerName] as ReringProject.Sequence.InspectionSequence') (want 1, was 0)"; echo "--- must NOT change ---"; echo "getany_def=$(echo "$CODE" | grep -cF "$GAS") (want 1)"; echo "getany_roles=$(echo "$CODE" | grep -cF 'ESequence[] roles = new ESequence[] { ESequence.Top, ESequence.Side, ESequence.Bottom };') (want 1)"; echo "trycompose_calls=$(echo "$CODE" | grep -cF 'seq.TryComposeAlign(') (want 3)"; echo "p1_callers=$(grep -cF 'ResolveDatumModelPath(datum, datum.OwnerName)' "$M") (want 6)"; echo "no_csharp8_added=$(git diff -U0 -- "$M" | grep '^+' | grep -v '^+++' | grep -vE '^\+[[:space:]]*//' | grep -cE "$PC8") (want 0)"; echo "--- diff 형상: 삭제는 옛 호출부 3줄뿐이어야 한다 ---"; echo "deleted_lines_total=$(git diff -U0 -- "$M" | grep '^-' | grep -vc '^---') (want 3)"; echo "deleted_are_oldcall=$(git diff -U0 -- "$M" | grep '^-' | grep -v '^---' | grep -cF 'GetAnyInspectionSequence();') (want 3)"; echo "--- scope: 아래 목록에 MainView.xaml.cs 외 소스파일이 뜨면 FAIL (.planning/ 은 무시) ---"; git diff --name-only; echo "--- build: 'error CS' / 신규 'warning CS' 줄이 하나라도 있으면 FAIL. MSB3021/3026/3027(실행중 exe 파일잠금)은 컴파일 실패가 아니라 걸러진다 ---"; "C:/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj //t:Build //p:Configuration=Debug //p:Platform=x64 //v:m //nologo 2>&1 | grep -E "error CS|warning CS" | grep -v -E "CS0618|CS0162" | head -20; echo "CS_LIST_ABOVE_MUST_BE_EMPTY"</automated>
  </verify>
  <done>
- 위 게이트의 모든 `(want N)` 이 실제 출력과 일치한다. 플래너가 **수정 전 상태에서 이 게이트를 그대로 드라이런**해 얻은 baseline: `getany_total=4` `old_callsites=3` `new_callsites=0` `getfordatum_def=0` `fallback_return=0` `owner_lookup=0` `getany_def=1` `getany_roles=1` `trycompose_calls=3` `p1_callers=6` `no_csharp8_added=0`. 수정 후 바뀌어야 하는 값은 `getany_total` 4→2, `old_callsites` 3→0, `new_callsites` 0→3, `getfordatum_def` 0→1, `fallback_return` 0→1, `owner_lookup` 0→1 **여섯 개뿐**이고, `getany_def=1` `getany_roles=1` `trycompose_calls=3` `p1_callers=6` `no_csharp8_added=0` 은 그대로여야 한다.
- `deleted_lines_total=3` 이고 `deleted_are_oldcall=3` 이다 — 삭제 줄이 3줄을 넘거나 그중 하나라도 옛 호출부가 아니면 의도 밖 수정이므로 즉시 복구한다.
- `git diff --name-only` 에 `WPF_Example/UI/ContentItem/MainView.xaml.cs` 하나만 뜬다(`.planning/` 문서 제외). `InspectionSequence.cs` 가 뜨면 FAIL.
- `CS_LIST_ABOVE_MUST_BE_EMPTY` 바로 위가 비어 있다.
- 읽기 재확인: 신규 메서드가 K&R 브레이스이고 중괄호 균형이 맞으며, 주석에 `260729-e7v` 식별자와 두 선행 버그 참조(`260728-n7b`, `260728-l2r`)가 들어가 있다.
- **exe 파일잠금 주의(260728-l2r/n7b 에서 실측 확인됨):** `DatumMeasurement.exe` 가 실행 중이면 MSBuild 는 컴파일에 성공하고 `obj → bin` 복사 단계에서 MSB3026/MSB3027/MSB3021 로 끝난다. 컴파일 실패는 아니지만 그 상태로는 `bin/x64/Debug/DatumMeasurement.exe` 가 갱신되지 않는다. MSB302x 가 보이면 실행 중인 앱/VS 디버그 세션을 닫고 빌드를 한 번 더 돌려 복사까지 성공시키고, 그 사실을 사용자에게 알린다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI(MainView) → InspectionSequence 인스턴스 선택 | 어떤 시퀀스 인스턴스로 datum 연산을 실행할지 결정하는 지점. 잘못 고르면 인스턴스 소유 상태(_datumTransforms)와 인스턴스 기준 경로 해석이 조용히 어긋난다 |
| 레시피 폴더(TOP/SIDE/BOTTOM) ↔ .shm 모델 파일 | 이번 버그 계열의 실제 피해 지점 — 다만 이번 변경은 경로 해석을 건드리지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-e7v-01 | Tampering | `GetInspectionSequenceForDatum` 가 소유 시퀀스를 못 찾아 null 반환 → 기존 폴백 경로(svc.TryFindDatum)로 조용히 강등 | mitigate | 폴백을 null 이 아니라 `GetAnyInspectionSequence()` 로 두어 오늘과 **정확히 동일한** 인스턴스를 반환 — 미등록 role(Side 비활성 PC)에서도 동작 회귀 0 |
| T-e7v-02 | Tampering | 실행자가 "겸사겸사" `GetAnyInspectionSequence()` 본문을 삭제/수정 | mitigate | 게이트의 `getany_def=1` + `getany_roles=1` + `deleted_lines_total=3` 으로 검출 |
| T-e7v-03 | Tampering | 범위 이탈 — `InspectionSequence.cs` 의 경로 해석/TryComposeAlign 을 함께 수정 | mitigate | `git diff --name-only` 스코프 검사 + `p1_callers=6` 불변 확인 |
| T-e7v-04 | Denial of Service | `datum.OwnerName` 이 null → 조회 실패로 datum 연산 불가 | accept | 생성 경로가 `new DatumConfig(this)` 단 1곳이고 `this` 는 항상 `InspectionSequence : SequenceBase` 라 null 불가. 설령 null 이어도 `string.IsNullOrEmpty` 가드 → 폴백으로 오늘과 동일 동작(악화 없음) |
| T-e7v-05 | Information Disclosure | Test Find 가 TOP 대신 소유 인스턴스의 `_datumTransforms` 에 transient 값을 남겨 다음 AUTO 검사에 누수 | accept | `ClearDatumTransforms()`(InspectionSequence.cs:1577)가 DatumPhase 루프 시작 시 매번 비운다 — 누수 경로 없음. 오히려 소유자 사전에 쓰는 쪽이 의미상 정확 |
| T-e7v-06 | Spoofing | 실행 중 exe 파일잠금으로 **옛 바이너리**를 새 것으로 착각 | mitigate | Task 1 done 의 MSB302x 확인 절차 |
| T-e7v-SC | Tampering | npm/pip/cargo 설치 | n/a | 이번 작업에 패키지 설치 없음 — 신규 의존성 0 |
</threat_model>

<verification>
1. Task 1 automated 게이트 전 항목 통과(수치 일치 + `error CS`/신규 `warning CS` 0건 + diff 스코프 소스파일 1개).
2. `git diff -- WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 가 **빈 출력**.
3. `git diff -- WPF_Example/Sequence/SequenceHandler.cs` 가 **빈 출력**.
4. `git diff -- WPF_Example/Sequence/Param/ParamBase.cs` 가 **빈 출력**.
5. `git status --porcelain` 에 신규 소스 파일(`??`)이 없다(`.planning/` 문서 제외).
6. `bin/x64/Debug/DatumMeasurement.exe` 가 실제로 갱신됐다(exe 잠금 해제 후 복사 성공).
</verification>

<success_criteria>
- `MainView.xaml.cs` 안에 `GetInspectionSequenceForDatum(DatumConfig datum)` 정의 1건, 그 호출 3건.
- `GetAnyInspectionSequence()` 정의 1건 유지 + 코드 기준 호출부는 `GetInspectionSequenceForDatum` 의 폴백 1건뿐(총 등장 2건).
- `seq.TryComposeAlign(` 호출 3건 그대로, 주변 로직/에러 메시지/오버레이 처리 무변경.
- Debug/x64 빌드 통과, 신규 컴파일러 진단 0건.
- 수정 파일 1개, 신규 파일 0개, 삭제 라인 3줄(전부 옛 호출부).
</success_criteria>

<output>
Create `.planning/quick/260729-e7v-mainview-getanyinspectionsequence-datum-/260729-e7v-SUMMARY.md` when done.

SUMMARY 에 반드시 기록할 것:
- 게이트 실측 출력(각 `(want N)` 대비 실제값)
- `git diff --numstat -- WPF_Example/UI/ContentItem/MainView.xaml.cs` 결과
- 신규 메서드 최종 코드 블록(주석 포함)
- MSB302x(exe 잠금) 발생 여부와 재빌드 결과
</output>
