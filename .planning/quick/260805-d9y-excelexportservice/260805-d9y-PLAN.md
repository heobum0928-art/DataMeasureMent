---
phase: quick-260805-d9y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Export/ExcelExportService.cs
autonomous: false
requirements: [D9Y-01, D9Y-02, D9Y-03, D9Y-04]

must_haves:
  truths:
    - "일반 검사 엑셀(리뷰어 [엑셀 export]) 결과 xlsx 의 11번째 컬럼(K) 헤더가 '캡쳐이미지' 이고, 캡쳐 JPG 가 디스크에 있는 측정 행마다 그 셀에 실제 이미지가 보인다"
    - "캡쳐 JPG 가 아직 저장되지 않았거나 손상된 행은 셀이 빈 칸으로 남고, Export 는 여전히 true 를 반환한다 (경고만 로그)"
    - "기존 9번(원본이미지 경로)/10번(캡쳐이미지 경로) 컬럼의 텍스트 값과 1~8번 컬럼(Shot~판정) 값은 변경 전과 100% 동일하다"
    - "WPF_Example/Custom/Export/RepeatExcelExportService.cs 는 단 한 줄도 수정되지 않는다"
    - "새 라이브러리/NuGet 패키지 추가 없이 기존 ClosedXML 0.105.0 만으로 동작한다 (packages.config, DatumMeasurement.csproj 무수정)"
    - "캡쳐 이미지가 전부 없는 cycle 을 export 해도 UI 가 수 분간 멈추지 않는다 (전체 대기 예산 상한이 존재)"
    - "Debug|x64 MSBuild 빌드가 에러 0 으로 성공한다"
  artifacts:
    - path: "WPF_Example/Custom/Export/ExcelExportService.cs"
      provides: "캡쳐이미지 첨부 컬럼(11) + 비동기 write 레이스 폴링 대기 + 경로별 캐시 + 종횡비 유지 셀 삽입"
      contains: "AddPicture"
      min_lines: 200
  key_links:
    - from: "ExcelExportService.Export (측정 행 루프)"
      to: "ClosedXML IXLWorksheet.AddPicture"
      via: "MemoryStream(byte[]) + XLPictureFormat.Jpeg 로 셀 앵커 삽입"
      pattern: "AddPicture\\("
    - from: "ExcelExportService (캡쳐 파일 로더)"
      to: "fai.CaptureImageFileName 이 가리키는 디스크 JPG"
      via: "File.Exists 폴링 후 File.ReadAllBytes + JPEG EOI(FFD9) 완결 검사"
      pattern: "File\\.ReadAllBytes"
    - from: "ExcelExportService (폴링 대기)"
      to: "CaptureImageSaveService 워커 스레드의 비동기 WriteImage"
      via: "Thread.Sleep 100ms 간격 폴링 + 전체 대기 예산 Stopwatch"
      pattern: "Thread\\.Sleep"
    - from: "ExcelExportService (컬럼 폭 설정)"
      to: "ws.Columns().AdjustToContents()"
      via: "AdjustToContents 호출 '이후' 에 11번 컬럼 폭을 덮어써서 이미지 박스 폭 확보"
      pattern: "AdjustToContents"
---

<objective>
일반 검사 결과 엑셀(`ExcelExportService`)의 기존 "캡쳐이미지 경로" 텍스트 컬럼 **뒤에** 새 컬럼을 추가하여,
그 경로가 가리키는 캡쳐 JPG(오버레이가 입혀진 결과 이미지)를 **셀 안에 실제 그림으로 삽입**한다.

Purpose: 지금은 엑셀에 경로 문자열만 있어서 결과를 눈으로 확인하려면 탐색기로 파일을 하나씩 열어야 한다.
이미지를 셀에 바로 넣으면 엑셀 한 장으로 측정값 + 판정 + 실제 검출 화면을 동시에 검토할 수 있다.

핵심 난이도는 **비동기 write 레이스**다. `CaptureImageSaveService` 가 JPG 를 백그라운드 워커 스레드에서 저장하는데,
경로 문자열은 `Action_FAIMeasurement.QueueFaiCapture` 에서 **동기로 먼저 확정**되어 DTO 에 기록된다
(`Action_FAIMeasurement.cs:878-879` → `CycleResultSerializer.cs:107`).
즉 export 시점에 경로는 있는데 파일은 아직 디스크에 없을 수 있다. flush/wait 지점이 코드 어디에도 없다(확인됨).
→ 짧게 폴링 대기하고, 그래도 없으면 **빈 칸 + 경고 로그**로 넘어간다. Export 자체는 절대 실패시키지 않는다.

Output: `ExcelExportService.cs` 1개 파일 수정. 그 외 파일 변경 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260805-d9y-excelexportservice/260805-d9y-CONTEXT.md
@WPF_Example/Custom/Export/ExcelExportService.cs

<coding_rules>
이 프로젝트의 상시 코딩 규칙 — 예외 없이 적용:

1. **삼항 연산자 `?:` 금지.** 새로 쓰는 코드는 전부 `if / else` 블록으로 쓴다.
   (기존 파일 38/42/65/68/71/74-76/114-115 행에 이미 있는 삼항은 **그대로 둔다** — 회귀 0 우선, 리팩토링 금지.)
2. **헝가리언 표기.** 새 지역변수/필드/파라미터 접두사: `n`(int), `b`(bool), `sz`(string), `d`(double),
   `arr`(배열), `dic`(Dictionary), `sw`(Stopwatch), `ms`(MemoryStream).
   (같은 파일 `Action_FAIMeasurement.cs` 의 `nIndexNumber`/`bHasMaterial`/`szMat` 와 동일한 스타일.)
3. **날짜 주석 규칙 폐기됨.** `//260805 hbk ...` 같은 날짜/이니셜 접두 주석을 **새로 달지 않는다.**
   주석은 "왜 이렇게 했는지"가 자명하지 않은 곳에만 최소한으로 단다.
   (기존 파일에 있는 옛 날짜 주석은 건드리지 않는다.)
4. **매직넘버 금지.** 폴링 간격/타임아웃/컬럼 번호/셀 크기 전부 `private const` 로 이름 붙여 선언한다.
5. **함수 길이 30~40줄.** `Export()` 는 이미 110줄이 넘으므로 이미지 로직을 **인라인으로 밀어넣지 말고**
   private static 헬퍼로 분리한다.
6. **C# 7.2.** `switch expression`, `record`, nullable reference type, `??=` 등 C# 8.0+ 문법 금지.
7. **에러 처리 패턴 준수.** 이미지 관련 실패는 전부 `try/catch` 로 삼키고 빈 칸 처리 + `Logging.PrintErrLog` 경고.
   Export 전체를 실패시키지 않는다.
8. **초보자도 읽히게.** 조건은 이름 붙인 bool 변수로 먼저 뽑고 나서 `if` 에 쓴다.
</coding_rules>

<interfaces>
<!-- 실제 코드베이스/DLL 리플렉션으로 확인한 계약. executor 는 코드베이스를 다시 탐색할 필요 없음. -->

**1) 삽입 대상 경로 — 이미 절대 경로다 (재조합 불필요)**

`WPF_Example/UI/ViewModel/CycleResultDto.cs:60` 의 XML 주석에는 "(경로 미포함)" 이라고 적혀 있으나 **그 주석은 낡았다.**
실제 대입부는 절대 경로다:

```csharp
// Action_FAIMeasurement.cs:878-879
fai.LastOriginImageFileName  = CaptureImageSaveService.BuildFilePath(false, originName, ts);
fai.LastCaptureImageFileName = CaptureImageSaveService.BuildFilePath(true,  captureName, ts);
// → CycleResultSerializer.cs:107 에서 FaiResultDto.CaptureImageFileName 으로 그대로 복사됨

// CaptureImageSaveService.cs:235
public static string BuildFilePath(bool isCapture, string fileName, DateTime ts) {
    return Path.Combine(BuildDirectory(isCapture, ts), SanitizeFileName(fileName));
}
// BuildDirectory = {ResultSavePath}\Image\{yyMMdd}\{HHmm}\{original|capture}
```

따라서 `fai.CaptureImageFileName` 을 **그대로** `File.Exists` / `File.ReadAllBytes` 에 넘기면 된다.
경로를 다시 만들거나 `SystemHandler.Handle.Setting.ResultSavePath` 를 참조하지 말 것.

**2) 파일 확장자는 .jpg (JPEG)**

```csharp
// CaptureImageSaveService.cs:156 — 워커가 실제로 쓰는 write
toWrite.WriteImage("jpeg", 0, filePath);
// BuildFileName 은 항상 ".jpg" 로 끝나는 이름을 만든다 (CaptureImageSaveService.cs:183, 210)
```

**3) ClosedXML 0.105.0 이미지 API — DLL 리플렉션으로 확인 완료**

```csharp
using ClosedXML.Excel;
using ClosedXML.Excel.Drawings;   // 신규 using 필요 (XLPictureFormat / XLPicturePlacement / IXLPicture)

// IXLWorksheet 에 존재하는 오버로드
IXLPicture AddPicture(System.IO.Stream stream);
IXLPicture AddPicture(System.IO.Stream stream, XLPictureFormat format);
IXLPicture AddPicture(string imageFile);

// IXLPicture (namespace ClosedXML.Excel.Drawings)
int  OriginalWidth  { get; }    // 원본 픽셀 폭
int  OriginalHeight { get; }    // 원본 픽셀 높이
IXLPicture MoveTo(IXLCell cell);                          // 단일 셀 앵커
IXLPicture WithSize(int width, int height);               // 표시 크기(픽셀)
IXLPicture WithPlacement(XLPicturePlacement placement);   // Move / MoveAndSize / FreeFloating

enum XLPictureFormat { Unknown, Bmp, Gif, Png, Tiff, Icon, Pcx, Jpeg, Emf, Wmf, Webp }
```

- JPEG 디코딩은 ClosedXML 기본 그래픽 엔진이 자체 처리한다 → **SixLabors.ImageSharp / System.Drawing 추가 참조 불필요.**
- `AddPicture(Stream)` 은 스트림 내용을 ClosedXML 내부 `MemoryStream` 으로 **복사**한다 →
  호출 후 `using` 으로 스트림을 닫아도 안전하다.
- **주의:** `WithSize` 는 표시 크기만 바꾼다. 워크북에는 항상 **원본 바이트 전체**가 임베드된다(리샘플링 아님).

**4) 현재 컬럼 배치 (ExcelExportService.cs:58) — 이 순서를 지킨다**

```
1 Shot | 2 FAI | 3 측정명 | 4 Nominal | 5 Tol+ | 6 Tol- | 7 측정값 | 8 판정
| 9 원본이미지 경로 | 10 캡쳐이미지 경로 | ← 여기 뒤에 11 캡쳐이미지(신규)
```

**5) 유일한 호출부 = UI 스레드 (블로킹 예산이 중요한 이유)**

```csharp
// ReviewerWindow.xaml.cs:308 — 버튼 클릭 핸들러에서 동기 호출. 여기가 유일한 호출부.
bool ok = ExcelExportService.Export(_currentCycle, dlg.FileName);
```
→ 행마다 무제한 폴링하면 UI 가 수 분간 얼어붙는다. 전체 대기 예산 상한이 반드시 필요하다.

**6) 로깅 패턴 (같은 파일 131행과 동일하게)**

```csharp
using ReringProject.Utility;   // 이미 있음
try { Logging.PrintErrLog((int)ELogType.Error, "[ExcelExportService] ..."); } catch { }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExcelExportService 에 캡쳐이미지 첨부 컬럼 + 비동기 레이스 대기 구현</name>
  <files>WPF_Example/Custom/Export/ExcelExportService.cs</files>
  <action>
`WPF_Example/Custom/Export/ExcelExportService.cs` **한 파일만** 수정한다.
`RepeatExcelExportService.cs`, `packages.config`, `DatumMeasurement.csproj` 는 절대 건드리지 않는다 (D9Y-03).

**(a) using 추가**

```csharp
using ClosedXML.Excel.Drawings;   // XLPictureFormat, XLPicturePlacement, IXLPicture
using System.Diagnostics;         // Stopwatch (전체 대기 예산)
using System.Threading;           // Thread.Sleep (폴링)
```
`System.IO`, `System.Collections.Generic`, `ReringProject.Utility` 는 이미 있다.

**(b) 클래스 상단에 const 선언 (매직넘버 금지)**

```csharp
private const int CAPTURE_IMAGE_COLUMN = 11;          // 캡쳐이미지 경로(10) 바로 뒤

// 비동기 write 레이스 대기 파라미터 (CONTEXT 재량 범위 1~2초 안에서 선택)
private const int CAPTURE_WAIT_TIMEOUT_MS = 1500;     // 파일 1개당 최대 대기
private const int CAPTURE_WAIT_POLL_MS = 100;         // 폴링 간격
private const int CAPTURE_WAIT_BUDGET_MS = 5000;      // export 1회 전체 대기 예산 상한

// 셀 안 이미지 표시 박스(픽셀)
private const int CAPTURE_BOX_WIDTH_PX = 160;
private const int CAPTURE_BOX_HEIGHT_PX = 120;

// 엑셀 단위 환산: 컬럼 폭은 문자수, 행 높이는 포인트(96dpi 기준 1px = 0.75pt)
private const double EXCEL_PIXELS_PER_WIDTH_UNIT = 7.0;
private const double EXCEL_POINTS_PER_PIXEL = 0.75;

private const int JPEG_MIN_BYTES = 4;                 // SOI(2) + EOI(2) 최소 길이
```

**(c) 헤더 배열 확장 (D9Y-01)**

58행의 `headers` 배열 **맨 끝에만** `"캡쳐이미지"` 를 추가한다.
앞의 10개 문자열은 순서/내용 그대로 유지. 헤더 write 루프(`for i < headers.Length`)는 그대로 두면 자동으로 11번째까지 쓴다.

**(d) Export() 안: 대기 예산 + 경로별 캐시 준비**

`using (var wb = new XLWorkbook())` 안, 측정 행 루프 **시작 전에** 선언한다:

```csharp
// 같은 FAI 의 여러 측정 행은 캡쳐 경로가 동일하다 → 경로당 1회만 대기/읽기 (null 결과도 캐시해서 재대기 방지)
var dicCaptureCache = new Dictionary<string, byte[]>();
// export 1회 전체 폴링 대기 상한. UI 스레드 호출이므로 이미지가 통째로 없는 cycle 에서 수 분 멈추는 것을 막는다.
var swWaitBudget = Stopwatch.StartNew();
```

**(e) Export() 안: 측정 행 루프에서 이미지 삽입 (D9Y-02)**

115행 `ws.Cell(row, 10).Value = ...` **바로 다음**, `row++` **앞**에 삽입한다.
9/10 컬럼 대입 라인은 **한 글자도 바꾸지 않는다** (D9Y-01).

```csharp
byte[] arrCaptureBytes = LoadCaptureImageBytes(fai.CaptureImageFileName, dicCaptureCache, swWaitBudget);
TryInsertCaptureImage(ws, row, arrCaptureBytes);
```

**(f) Export() 안: 컬럼 폭은 AdjustToContents '이후' 에 설정 (재량 사항)**

122행 `ws.Columns().AdjustToContents();` 는 그대로 두고, **그 아래 / `wb.SaveAs` 위**에 한 줄 추가한다.
(`AdjustToContents` 는 텍스트 기준으로만 폭을 잡고 그림은 고려하지 않으므로, 먼저 호출한 뒤 11번 컬럼만 덮어써야 한다.)

```csharp
ws.Column(CAPTURE_IMAGE_COLUMN).Width = CAPTURE_BOX_WIDTH_PX / EXCEL_PIXELS_PER_WIDTH_UNIT;
```

**(g) private static 헬퍼 3개 추가 (Export() 메서드 밖, 클래스 안)**

```csharp
// 캡쳐 JPG 를 바이트로 읽는다. 경로당 1회만 실제 대기/읽기 (결과가 null 이어도 캐시).
private static byte[] LoadCaptureImageBytes(string szPath, Dictionary<string, byte[]> dicCache, Stopwatch swBudget)
```
- `szPath` 가 null/빈 문자열이면 즉시 `null`.
- `dicCache.ContainsKey(szPath)` 면 캐시값 반환 (재대기 금지).
- 아니면 `WaitForCaptureImage(szPath, swBudget)` 호출 → 결과를 캐시에 저장.
- 결과가 null 이면 경고 로그 1회:
  `"[ExcelExportService] capture image not ready, cell left blank: " + szPath`
  (`try { Logging.PrintErrLog((int)ELogType.Error, ...); } catch { }` 패턴)
- 캐시 덕분에 같은 파일에 대한 경고 로그도 1회만 남는다.

```csharp
// CaptureImageSaveService 워커가 JPG 를 비동기로 쓰므로 export 시점에 아직 없거나 쓰는 중일 수 있다.
private static byte[] WaitForCaptureImage(string szPath, Stopwatch swBudget)
```
- `nWaitedMs = 0` 으로 시작하는 루프.
- 매 회차: `File.Exists(szPath)` 가 true 면 `TryReadCompleteJpeg(szPath)` 시도 → null 아니면 즉시 반환.
- 계속 대기할지 판정 (조건은 이름 붙인 bool 로 먼저 뽑을 것):
  `bBudgetLeft = swBudget.ElapsedMilliseconds < CAPTURE_WAIT_BUDGET_MS`,
  `bTimeLeft = nWaitedMs < CAPTURE_WAIT_TIMEOUT_MS`.
  둘 중 하나라도 false 면 `return null`.
- 아니면 `Thread.Sleep(CAPTURE_WAIT_POLL_MS)` 후 `nWaitedMs += CAPTURE_WAIT_POLL_MS`.

```csharp
// 워커가 쓰는 도중에 읽으면 잘린 JPEG 이 나온다 → EOI 마커(FF D9)로 파일 완결 여부를 확인한다.
private static byte[] TryReadCompleteJpeg(string szPath)
```
- `try` 안에서 `File.ReadAllBytes(szPath)`.
- `arrBytes.Length < JPEG_MIN_BYTES` 면 `null` (아직 쓰는 중).
- 마지막 2바이트가 `0xFF`, `0xD9` (JPEG EOI) 가 아니면 `null` (잘린 파일 → 다음 폴링에서 재시도).
- 통과하면 `arrBytes` 반환.
- `catch { return null; }` — 워커가 파일을 쥐고 있어 IOException 이 나도 다음 폴링에서 재시도된다.

```csharp
// 종횡비를 유지한 채 셀 박스 안에 맞춰 그림을 넣는다. 실패해도 export 는 계속된다.
private static bool TryInsertCaptureImage(IXLWorksheet ws, int nRow, byte[] arrBytes)
```
- `arrBytes` 가 null 이거나 길이 0 이면 `false` (셀은 빈 칸 그대로 — D9Y-04).
- `try` 안에서:
  - `using (var ms = new MemoryStream(arrBytes))` 로 `IXLPicture pic = ws.AddPicture(ms, XLPictureFormat.Jpeg);`
  - `nOriginalWidth = pic.OriginalWidth`, `nOriginalHeight = pic.OriginalHeight`.
    둘 중 하나라도 `<= 0` 이면 `pic.Delete()` 후 `false` 반환.
  - 축소 배율(삼항 금지, if 로):
    ```csharp
    double dScaleWidth  = (double)CAPTURE_BOX_WIDTH_PX  / nOriginalWidth;
    double dScaleHeight = (double)CAPTURE_BOX_HEIGHT_PX / nOriginalHeight;
    double dScale = dScaleWidth;
    if (dScaleHeight < dScale) { dScale = dScaleHeight; }
    if (dScale > 1.0) { dScale = 1.0; }   // 원본보다 키우지 않는다
    ```
  - `nTargetWidth = (int)Math.Round(nOriginalWidth * dScale)` (높이도 동일), 각각 최소 1 로 클램프.
  - `pic.WithPlacement(XLPicturePlacement.Move);` → `pic.WithSize(nTargetWidth, nTargetHeight);` → `pic.MoveTo(ws.Cell(nRow, CAPTURE_IMAGE_COLUMN));`
  - 그림이 들어간 행만 높이를 키운다: `ws.Row(nRow).Height = CAPTURE_BOX_HEIGHT_PX * EXCEL_POINTS_PER_PIXEL;`
  - `return true;`
- `catch (Exception ex)`: 경고 로그
  `"[ExcelExportService] capture image insert failed (row " + nRow + "): " + ex.Message"` 후 `return false`.
  **여기서 예외를 밖으로 던지면 안 된다** — Export 전체가 false 가 되면 D9Y-04 위반.

**(h) 하지 말 것 (스코프 가드)**
- `RepeatExcelExportService.cs` 수정 금지 (D9Y-03).
- NuGet 패키지 추가 / `csproj` 참조 추가 금지 — ClosedXML 0.105.0 로 충분하다 (D9Y-02).
  (`csproj` 에 이미 있는 `SIMUL_MODE` 로컬 변경분은 그대로 둔다.)
- 9/10 컬럼(원본/캡쳐 경로 텍스트) 값·순서 변경 금지.
- 이미지 리샘플링(썸네일 생성) 금지 — CONTEXT 결정 범위 밖이다. `WithSize` 로 표시 크기만 조절한다.
- 기존 삼항 연산자 리팩토링 금지 (회귀 0).
  </action>
  <verify>
    <automated>
cd C:/Info/Project/DataMeasurement && \
msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /nologo /v:minimal 2>&1 | tail -15 && \
echo "=== structural checks ===" && \
grep -c "AddPicture(ms, XLPictureFormat.Jpeg)" WPF_Example/Custom/Export/ExcelExportService.cs && \
grep -c "CAPTURE_IMAGE_COLUMN = 11" WPF_Example/Custom/Export/ExcelExportService.cs && \
grep -c "캡쳐이미지\"" WPF_Example/Custom/Export/ExcelExportService.cs && \
grep -c "File.ReadAllBytes" WPF_Example/Custom/Export/ExcelExportService.cs && \
grep -c "Thread.Sleep(CAPTURE_WAIT_POLL_MS)" WPF_Example/Custom/Export/ExcelExportService.cs && \
grep -c "CAPTURE_WAIT_BUDGET_MS" WPF_Example/Custom/Export/ExcelExportService.cs && \
echo "=== scope guard: 아래 3줄은 모두 0 이어야 함 ===" && \
git diff --name-only | grep -c "RepeatExcelExportService.cs" ; \
git diff --name-only | grep -c "packages.config" ; \
git diff -- WPF_Example/Custom/Export/ExcelExportService.cs | grep -c "^+.*?.*:.*;" ; \
echo "=== 9/10 컬럼 불변 확인 (변경 라인 0 이어야 함) ===" && \
git diff -- WPF_Example/Custom/Export/ExcelExportService.cs | grep -c "^-.*ws.Cell(row, 9).Value"
    </automated>

판정 기준:
- msbuild 결과에 `error` 0건 (기존 CS0618 등 warning 은 허용).
- `AddPicture(ms, XLPictureFormat.Jpeg)` / `CAPTURE_IMAGE_COLUMN = 11` / `"캡쳐이미지"` /
  `File.ReadAllBytes` / `Thread.Sleep(CAPTURE_WAIT_POLL_MS)` / `CAPTURE_WAIT_BUDGET_MS` 각각 1 이상.
- scope guard 3줄 전부 `0` — 각각 RepeatExcelExportService 미수정 / packages.config 미수정 /
  새로 추가한 라인에 삼항 연산자 없음.
- 마지막 줄 `0` — 9번 컬럼 대입 라인이 삭제/수정되지 않음.
  </verify>
  <done>
- `ExcelExportService.cs` 만 수정됨. Debug|x64 빌드 에러 0.
- 헤더 11번째 = "캡쳐이미지", 1~10번 헤더 문자열 불변.
- 캡쳐 JPG 존재 시 해당 행 11번 셀에 종횡비 유지 축소 이미지가 앵커되고, 그 행 높이가 90pt 로 설정된다.
- 캡쳐 JPG 부재/미완결 시 그 셀은 빈 칸이고 `Logging.PrintErrLog` 경고가 경로당 1회 남으며 `Export` 는 true 를 반환한다.
- 동일 경로는 캐시되어 대기/읽기가 1회만 발생하고, export 1회 전체 폴링 대기가 5초를 넘지 않는다.
- `RepeatExcelExportService.cs` / `packages.config` / `csproj` 참조 무수정.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 실제 검사 → 엑셀 export 육안 확인</name>
  <what-built>
`ExcelExportService` 에 11번째 컬럼 "캡쳐이미지" 를 추가했습니다.
기존 "캡쳐이미지 경로" 텍스트 컬럼은 그대로 두고, 그 오른쪽 셀에 그 경로의 JPG(오버레이가 그려진 결과 이미지)를
실제 그림으로 넣습니다.

캡쳐 JPG 는 백그라운드 스레드가 나중에 저장하기 때문에, export 하는 순간 파일이 아직 없을 수 있습니다.
그래서 파일이 나타날 때까지 100ms 간격으로 잠깐(파일당 최대 1.5초) 기다리고,
그래도 없으면 그 셀만 비워둔 채 로그에 경고를 남기고 계속 진행합니다 — export 가 실패하는 일은 없습니다.
검사 직후 바로 export 하면 뒤쪽 항목 몇 개가 빈 칸일 수 있는데, 이건 설계된 동작입니다.
  </what-built>
  <files>(코드 변경 없음 — 실행 중인 앱과 저장된 xlsx 를 사람이 확인)</files>
  <action>
실행을 멈추고 개발자에게 아래 확인을 요청한다. 승인 전에는 커밋/후속 작업을 진행하지 않는다.
확인 결과 문제가 보고되면 Task 1 로 돌아가 수정한 뒤 다시 이 체크포인트를 요청한다.
  </action>
  <how-to-verify>
1. `Debug|x64` 로 빌드 후 앱 실행 (SIMUL_MODE 로 충분합니다).
2. 검사를 1 cycle 돌려서 캡쳐 이미지가 생성되게 합니다.
3. 리뷰어 창을 열고 방금 cycle 을 선택 → **[엑셀 export]** 버튼 → 아무 위치에나 저장.
4. 저장된 `.xlsx` 를 엑셀로 열고 다음을 확인해 주세요:
   - [ ] I열 "원본이미지 경로", J열 "캡쳐이미지 경로" 텍스트가 **예전과 똑같이** 나오는지
   - [ ] K열 헤더가 **"캡쳐이미지"** 이고, 그 아래 셀에 **오버레이가 그려진 결과 이미지**가 실제로 보이는지
   - [ ] 이미지가 찌그러지지 않고(종횡비 유지) 셀 안에 적당한 크기로 들어가는지
   - [ ] A~H열(Shot/FAI/측정명/Nominal/Tol+/Tol-/측정값/판정) 값이 예전과 동일한지
5. **빈 칸 동작 확인:** 검사가 끝나자마자 곧바로 export 해 보세요.
   뒤쪽 몇 행의 K열이 비어 있어도 정상이며, 저장은 "저장 완료" 로 성공해야 합니다.
   (로그 파일에 `capture image not ready, cell left blank` 경고가 남습니다.)
6. **멈춤 확인:** 캡쳐 이미지 폴더가 아예 없는(또는 지운) 오래된 cycle 을 골라 export 해 보세요.
   길어야 5초 안에 저장 완료 창이 떠야 합니다. 수십 초~수 분 멈추면 실패입니다.
7. **반복검사 export 회귀 확인:** 리뷰어의 반복검사(Gage R&R) export 버튼도 한 번 눌러
   기존과 똑같이 동작하는지 확인해 주세요 (이번 작업에서 손대지 않은 영역입니다).

참고로 알아두실 점: 셀에 들어가는 그림은 **원본 JPG 바이트 그대로** 엑셀에 포함됩니다
(화면에 작게 보이는 것은 표시 크기만 줄인 것). 측정 항목이 많은 cycle 은 xlsx 용량이 꽤 커질 수 있습니다.
용량이 부담스러우면 "썸네일로 줄여서 넣기" 를 별도 작업으로 요청해 주세요 — 이번 범위에는 넣지 않았습니다.
  </how-to-verify>
  <verify>
    <automated>MANUAL — 사람이 앱을 실행해 xlsx 를 열어보는 것 외에 자동 검증 수단이 없다 (이 저장소에 테스트 프레임워크 없음). 자동 검증은 Task 1 의 빌드 + 구조 grep 이 담당한다.</automated>
  </verify>
  <done>아래 how-to-verify 체크리스트 4~7번 항목을 개발자가 모두 확인하고 승인했다.</done>
  <resume-signal>"approved" 라고 입력하시거나, 문제가 있으면 어떤 항목이 어떻게 다른지 알려주세요.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 디스크 cycle.json → ExcelExportService | `CaptureImageFileName` 경로 문자열이 앱 외부에서 편집 가능한 JSON 파일을 거쳐 들어온다 |
| 캡쳐 JPG 파일 → ClosedXML 이미지 디코더 | 워커가 쓰는 중이거나 손상된 바이트가 파서로 들어갈 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-D9Y-01 | Information Disclosure | `LoadCaptureImageBytes` 의 `File.ReadAllBytes(szPath)` | accept | 경로는 앱 자신이 `BuildFilePath` 로 생성해 로컬 cycle.json 에 기록한 값이고, 실행 사용자와 동일 권한으로 읽어 같은 사용자가 저장하는 워크북에만 넣는다. 경로를 조작하려면 이미 로컬 파일 쓰기 권한이 필요하므로 추가 권한 상승이 없다 |
| T-D9Y-02 | Denial of Service | `WaitForCaptureImage` 폴링 (UI 스레드 블로킹) | mitigate | 파일당 `CAPTURE_WAIT_TIMEOUT_MS`(1500ms) + export 1회 전체 `CAPTURE_WAIT_BUDGET_MS`(5000ms) 이중 상한, 경로별 결과 캐시로 재대기 차단 |
| T-D9Y-03 | Tampering | ClosedXML JPEG 디코더에 잘리거나 손상된 바이트 유입 | mitigate | `TryReadCompleteJpeg` 가 최소 길이 + JPEG EOI(FF D9) 마커를 검사하고, `TryInsertCaptureImage` 전체를 try/catch 로 감싸 실패 시 빈 칸 처리 |
| T-D9Y-04 | Denial of Service | 대용량 이미지 다수 임베드로 인한 메모리/파일 크기 증가 | accept | 캡쳐 JPG 는 앱이 생성한 자체 산출물이며 크기가 예측 가능하다. 썸네일 리샘플링은 CONTEXT 결정 범위 밖 → Task 2 체크포인트에서 실측 용량을 개발자에게 보고해 후속 판단에 맡긴다 |
</threat_model>

<verification>
1. `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64` — error 0.
2. `git diff --name-only` 결과가 `WPF_Example/Custom/Export/ExcelExportService.cs` 단 하나
   (기존 `DatumMeasurement.csproj` 의 SIMUL_MODE 로컬 변경분 제외).
3. `grep -n "AddPicture\|CAPTURE_IMAGE_COLUMN\|CAPTURE_WAIT_BUDGET_MS\|TryReadCompleteJpeg" WPF_Example/Custom/Export/ExcelExportService.cs`
   — 4개 심볼 모두 존재.
4. 새로 추가된 라인에 삼항 연산자 `?:` 와 `//YYMMDD hbk` 형식 주석이 없음.
5. Task 2 human-verify 체크리스트 전 항목 통과.
</verification>

<success_criteria>
- 일반 검사 xlsx 의 K열에 캡쳐 이미지가 실제 그림으로 삽입된다 (D9Y-01, D9Y-02).
- 캡쳐 JPG 가 아직 없으면 그 셀만 비고 경고 로그가 남으며 Export 는 성공한다 (D9Y-04).
- 기존 A~J 컬럼 동작/값 회귀 0, `RepeatExcelExportService.cs` 무수정 (D9Y-03).
- 새 NuGet 패키지 0개, Debug|x64 빌드 에러 0.
- 이미지가 하나도 없는 cycle 을 export 해도 5초 안에 끝난다.
</success_criteria>

<output>
완료 후 `.planning/quick/260805-d9y-excelexportservice/260805-d9y-SUMMARY.md` 를 작성한다.
</output>
