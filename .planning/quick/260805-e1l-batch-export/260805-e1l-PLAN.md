---
phase: quick-260805-e1l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Export/ExcelExportService.cs
  - WPF_Example/Custom/Export/RepeatExcelExportService.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: false
requirements: [E1L-01, E1L-02, E1L-03, E1L-04, E1L-05, E1L-06]

must_haves:
  truths:
    - "일괄검사 [엑셀 Export] 로 저장한 xlsx 에 시트가 3개 있다: '반복도 통계', '알고리즘 통계', '회차별 상세' (이 순서)"
    - "'회차별 상세' 시트의 헤더 12개가 순서대로 회차|Shot|FAI|측정명|Nominal|Tol+|Tol-|측정값|판정|원본이미지 경로|캡쳐이미지 경로|캡쳐이미지 이다"
    - "상세 시트의 '회차' 값이 _batchAccumulated 리스트 순서대로 1부터 증가하고, 각 cycle 의 모든 Shot×FAI×측정이 1행씩 펼쳐진다"
    - "캡쳐 JPG 가 디스크에 있는 상세 행은 12번(L)열에 실제 이미지가 종횡비를 유지한 채 보인다"
    - "캡쳐 JPG 가 없거나 미완결인 행은 12번 열이 빈 칸으로 남고 경고 로그만 남으며 ExportBatch 는 true 를 반환한다"
    - "반복검사(Gage R&R, ReviewerWindow) export 결과 xlsx 는 여전히 시트 2개이고 셀 값/포맷이 변경 전과 100% 동일하다 (RepeatExcelExportService.Export 시그니처 무변경)"
    - "이미지 삽입/폴링 대기 로직은 ExcelExportService.cs 에만 존재한다 — RepeatExcelExportService.cs 에 AddPicture / File.ReadAllBytes / Thread.Sleep 이 단 한 번도 나오지 않는다"
    - "판정 라벨('DETECT FAIL'/'NO IMAGE'/'CROSS-Z INCOMPLETE'/OK/NG) 문자열은 ExcelExportService.cs 한 곳에만 존재한다"
    - "캡쳐 이미지가 하나도 없는 대량 batch(수십 cycle)를 export 해도 전체 폴링 대기가 15초를 넘지 않는다"
    - "새 NuGet 패키지 0개, Debug|x64 MSBuild 빌드 error 0"
  artifacts:
    - path: "WPF_Example/Custom/Export/ExcelExportService.cs"
      provides: "재사용 가능한 캡쳐이미지 로더/삽입 헬퍼(internal static, 컬럼·대기예산 파라미터화) + 판정 라벨 단일 소스"
      contains: "internal static"
      min_lines: 320
    - path: "WPF_Example/Custom/Export/RepeatExcelExportService.cs"
      provides: "ExportBatch(회차별 상세 시트 + 캡쳐이미지 포함) / 기존 Export 는 무변경 위임"
      contains: "ExportBatch"
      min_lines: 300
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "일괄검사 export 버튼이 ExportBatch 를 호출"
      contains: "ExportBatch"
  key_links:
    - from: "InspectionListView.Btn_batchExport_Click"
      to: "RepeatExcelExportService.ExportBatch"
      via: "일괄 Export 버튼 클릭 시 동기 호출 (기존 Export 호출 1줄만 교체)"
      pattern: "RepeatExcelExportService\\.ExportBatch\\("
    - from: "RepeatExcelExportService.ExportBatch / Export"
      to: "RepeatExcelExportService.ExportInternal"
      via: "bWithDetailSheet 플래그로 상세 시트 추가 여부만 분기 (집계 2시트 코드는 단일 소스)"
      pattern: "ExportInternal\\("
    - from: "RepeatExcelExportService.WriteDetailRow"
      to: "ExcelExportService.LoadCaptureImageBytes / TryInsertCaptureImage"
      via: "internal static 헬퍼 직접 호출 (같은 네임스페이스 ReringProject.Export, 로직 복제 금지)"
      pattern: "ExcelExportService\\.(LoadCaptureImageBytes|TryInsertCaptureImage)"
    - from: "RepeatExcelExportService.AppendDetailSheet"
      to: "cycle 수 비례 대기 예산(하한 5s / 상한 15s)"
      via: "Stopwatch 1개 + nBudgetMs 를 모든 LoadCaptureImageBytes 호출에 공유"
      pattern: "BATCH_CAPTURE_WAIT_BUDGET_MAX_MS"
---

<objective>
일괄검사(Batch) 엑셀 export 를 반복검사(Gage R&R) export 에서 **분리**하고, 일괄검사 쪽에만
**회차별(cycle 별) 상세 행 + 캡쳐이미지**를 담은 세 번째 시트를 추가한다.

Purpose: 지금은 일괄검사와 반복검사가 완전히 같은 함수(`RepeatExcelExportService.Export`)를 써서
집계 통계 2시트만 나온다. 반복검사는 **같은 부품을 N회 반복**하므로 회차별 이미지가 전부 같아 의미가 없지만,
일괄검사는 **서로 다른 실물 부품 N개**를 순서대로 검사하므로 "몇 번째 부품의 어느 측정이 얼마였고
그때 화면이 어떻게 보였는가"가 핵심 정보다. 이게 지금 엑셀에서 완전히 사라진다.

Output: 파일 3개 수정. 새 파일/새 NuGet 0개.
- `ExcelExportService.cs` — quick 260805-d9y(커밋 656dc45)에서 만든 이미지 헬퍼를 **재사용 가능하게 개방**(private→internal, 컬럼/예산 파라미터화). 기존 일반검사 export 동작은 불변.
- `RepeatExcelExportService.cs` — `Export` 본문을 `ExportInternal` 로 그대로 옮기고 `ExportBatch` 추가 + 상세 시트 작성.
- `InspectionListView.xaml.cs` — 621행 호출 1줄만 `ExportBatch` 로 교체.

**가장 중요한 불변식:** `ReviewerWindow.xaml.cs:433`(반복검사 버튼)이 호출하는
`RepeatExcelExportService.Export(cycles, recipeName, outputPath)` 의 시그니처와 산출 xlsx 는
**바이트 단위로 동일**해야 한다. 이 경로는 이미 UAT 통과 상태이므로 회귀 0 이 최우선이다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260805-e1l-batch-export/260805-e1l-CONTEXT.md
@WPF_Example/Custom/Export/ExcelExportService.cs
@WPF_Example/Custom/Export/RepeatExcelExportService.cs

<coding_rules>
이 프로젝트의 상시 코딩 규칙 — 예외 없이 적용:

1. **삼항 연산자 `?:` 금지.** 새로 쓰는 코드는 전부 `if / else` 블록.
   (`RepeatExcelExportService.cs` 151/152행, `ExcelExportService.cs` 58/62/85/94/97/100~102/140/141행에
   이미 있는 삼항/`??` 는 **그대로 둔다** — 회귀 0 우선, 리팩토링 금지.)
2. **헝가리언 표기.** 새 지역변수/필드/파라미터 접두사: `n`(int), `b`(bool), `sz`(string), `d`(double),
   `arr`(배열), `dic`(Dictionary), `sw`(Stopwatch), `ws`(worksheet), `pic`(picture).
3. **날짜 주석 규칙 폐기됨.** `//260805 hbk ...` 같은 날짜/이니셜 접두 주석을 **새로 달지 않는다.**
   주석은 "왜 이렇게 했는지"가 자명하지 않은 곳에만 최소한으로. (기존 날짜 주석은 건드리지 않는다.)
4. **매직넘버 금지.** 컬럼 번호/대기 예산/시트명 전부 `private const` 로 이름 붙여 선언한다.
5. **함수 길이 30~40줄.** 상세 시트 작성은 `AppendDetailSheet`(시트 준비 + cycle 루프)와
   `WriteDetailRow`(측정 1행 기록)로 반드시 분리한다.
6. **C# 7.2.** `switch expression`, `record`, nullable reference type, `??=`, target-typed `new` 금지.
7. **에러 처리 패턴.** 이미지 관련 실패는 전부 삼키고 빈 칸 + `Logging.PrintErrLog` 경고.
   Export 전체를 실패시키지 않는다.
8. **초보자도 읽히게.** 조건은 이름 붙인 bool 변수로 먼저 뽑고 나서 `if` 에 쓴다.
</coding_rules>

<interfaces>
<!-- 실제 코드베이스에서 확인한 계약. executor 는 코드베이스를 다시 탐색할 필요 없음. -->

**1) 두 클래스는 같은 네임스페이스다 → `internal` 로 충분하다 (`public` 승격 금지)**

```csharp
// ExcelExportService.cs:13      namespace ReringProject.Export
// RepeatExcelExportService.cs:11 namespace ReringProject.Export
// 둘 다 같은 어셈블리(DatumMeasurement.exe) → internal static 접근 OK
```

**2) 현재 `ExcelExportService` 의 헬퍼 4종 (quick 260805-d9y, 커밋 656dc45) — 이번에 개방할 대상**

```csharp
private static byte[] LoadCaptureImageBytes(string szPath, Dictionary<string,byte[]> dicCache, Stopwatch swBudget); // 171행
private static byte[] WaitForCaptureImage(string szPath, Stopwatch swBudget);   // 202행 — 예산 const 가 하드코딩됨
private static byte[] TryReadCompleteJpeg(string szPath);                        // 231행
private static bool   TryInsertCaptureImage(IXLWorksheet ws, int nRow, byte[] arrBytes); // 258행 — 컬럼이 하드코딩됨
```
호출부는 단 한 곳: `ExcelExportService.Export` 143~144행.

**3) 이번에 쓸 DTO 필드 (전부 `ReringProject.UI` 네임스페이스, 두 파일 모두 이미 `using` 함)**

```csharp
CycleResultDto      : List<ShotResultDto> Shots
ShotResultDto       : string ShotName, List<FaiResultDto> FAIs
FaiResultDto        : string FAIName, List<MeasurementResultDto> Measurements,
                      string OriginImageFileName, string CaptureImageFileName   // 둘 다 이미 절대경로 (재조합 불필요)
MeasurementResultDto: string MeasurementName, double NominalValue, double TolerancePlus, double ToleranceMinus,
                      bool LastHasResult, double LastMeasuredValue, bool LastJudgement, string LastSkipReason, string TypeName
```

**4) 판정 라벨 로직 (현재 `ExcelExportService.Export` 118~137행) — 상세 시트와 100% 같아야 한다**

```csharp
if (m.LastSkipReason == SkipReason.DATUM_FAIL)            → "DETECT FAIL"
else if (m.LastSkipReason == SkipReason.NO_IMAGE)         → "NO IMAGE"
else if (m.LastSkipReason == SkipReason.CROSS_Z_INCOMPLETE) → "CROSS-Z INCOMPLETE"
else if (m.LastHasResult)                                 → "OK" 또는 "NG"  (m.LastJudgement)
else                                                      → "-"
// SkipReason 상수는 ReringProject.Sequence — 두 파일 모두 이미 using 함
```

**5) 기존 `RepeatExcelExportService.Export` 구조 (건드리면 안 되는 산출물)**

```
시트1 "반복도 통계"  : 메타 3행(모델명/측정일시/반복횟수) + 헤더 5행 + 데이터 6행~ (12컬럼)
시트2 "알고리즘 통계" : 헤더 1행 + 데이터 2행~ (6컬럼)
→ wb.SaveAs(outputPath); return true;
catch → Logging.PrintErrLog("[RepeatExcelExportService] Export failed: " + ex.Message); return false;
```

**6) 유일한 호출부 2개 (UI 스레드 동기 호출 — 대기 예산 상한이 필요한 이유)**

```csharp
// ReviewerWindow.xaml.cs:433 — 반복검사(Gage R&R). 이번 작업에서 절대 건드리지 않는다.
RepeatExcelExportService.Export(_repeatCycles, recipeName, dlg.FileName);

// InspectionListView.xaml.cs:621~622 — 일괄검사. 이 한 줄만 ExportBatch 로 바꾼다.
bool ok = ReringProject.Export.RepeatExcelExportService.Export(_batchAccumulated, recipeName, dlg.FileName);
```

**7) ClosedXML 0.105.0 이미지 API (d9y 에서 DLL 리플렉션으로 검증 완료)**

```csharp
IXLPicture IXLWorksheet.AddPicture(Stream stream, XLPictureFormat format);
IXLPicture.OriginalWidth / OriginalHeight / MoveTo(IXLCell) / WithSize(int,int) / WithPlacement(XLPicturePlacement)
```
→ 이번 작업에서 이 API 를 **직접 호출하지 않는다.** 전부 `ExcelExportService.TryInsertCaptureImage` 안에 있다.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExcelExportService 헬퍼를 재사용 가능하게 개방 (일반검사 동작 불변)</name>
  <files>WPF_Example/Custom/Export/ExcelExportService.cs</files>
  <action>
`ExcelExportService.cs` **한 파일만** 수정한다. 목적은 오직 **재사용 가능한 계약 노출**이며,
일반검사 export 의 산출 xlsx 는 이 태스크 전후로 **완전히 동일**해야 한다 (E1L-03).

**(a) `LoadCaptureImageBytes` — 대기 예산을 파라미터로 (E1L-05)**

171행 시그니처를 아래로 바꾼다. 본문 로직은 그대로 두고, `WaitForCaptureImage` 호출에 `nBudgetMs` 를 전달만 한다.

```csharp
/// <summary>
/// 캡쳐 JPG 를 바이트로 읽는다. 경로당 1회만 실제 대기/읽기 (결과가 null 이어도 캐시).
/// swBudget/nBudgetMs 는 export 1회 전체 폴링 대기 상한 — 호출자가 데이터 규모에 맞춰 정한다.
/// </summary>
internal static byte[] LoadCaptureImageBytes(string szPath, Dictionary<string, byte[]> dicCache, Stopwatch swBudget, int nBudgetMs)
```

**(b) `WaitForCaptureImage` — 예산 const 를 파라미터로 (private 유지)**

202행 시그니처를 `private static byte[] WaitForCaptureImage(string szPath, Stopwatch swBudget, int nBudgetMs)` 로 바꾸고,
216행 `bool bBudgetLeft = swBudget.ElapsedMilliseconds < CAPTURE_WAIT_BUDGET_MS;` 를
`bool bBudgetLeft = swBudget.ElapsedMilliseconds < nBudgetMs;` 로 바꾼다. 그 외 본문 불변.
`CAPTURE_WAIT_BUDGET_MS`(5000) const 는 **삭제하지 말고** 그대로 두고 (d) 에서 Export 가 넘긴다.

`WaitForCaptureImage` 와 `TryReadCompleteJpeg` 는 **private 그대로 둔다.**
외부에서 직접 부를 일이 없고(=`LoadCaptureImageBytes` 가 유일한 진입점), 노출 면적을 최소화한다.
CONTEXT 의 "헬퍼 4종 재사용" 요구는 2개 직접 호출 + 2개 전이적 재사용으로 충족된다 — 복제는 0 이다.

**(c) `TryInsertCaptureImage` — 대상 컬럼을 파라미터로**

258행 시그니처를 아래로 바꾼다.

```csharp
internal static bool TryInsertCaptureImage(IXLWorksheet ws, int nRow, int nColumn, byte[] arrBytes)
```
308행 `pic.MoveTo(ws.Cell(nRow, CAPTURE_IMAGE_COLUMN));` → `pic.MoveTo(ws.Cell(nRow, nColumn));`
그 외 본문(스케일 계산, 행 높이, catch 로깅) 불변.

**(d) `Export` 안의 호출부 2줄만 인자 추가 (값은 기존 const 그대로 → 동작 동일)**

143~144행:
```csharp
byte[] arrCaptureBytes = LoadCaptureImageBytes(fai.CaptureImageFileName, dicCaptureCache, swWaitBudget, CAPTURE_WAIT_BUDGET_MS);
TryInsertCaptureImage(ws, row, CAPTURE_IMAGE_COLUMN, arrCaptureBytes);
```

**(e) 컬럼 폭 설정을 헬퍼로 추출 (박스 크기 단일 소스)**

152행 `ws.Column(CAPTURE_IMAGE_COLUMN).Width = CAPTURE_BOX_WIDTH_PX / EXCEL_PIXELS_PER_WIDTH_UNIT;` 를
`ApplyCaptureColumnWidth(ws, CAPTURE_IMAGE_COLUMN);` 로 바꾸고, 아래 헬퍼를 클래스에 추가한다.

```csharp
/// <summary>
/// 캡쳐이미지 컬럼 폭을 이미지 박스 폭에 맞춘다. AdjustToContents 는 그림을 고려하지 않으므로 '그 뒤에' 불러야 한다.
/// </summary>
internal static void ApplyCaptureColumnWidth(IXLWorksheet ws, int nColumn)
{
    ws.Column(nColumn).Width = CAPTURE_BOX_WIDTH_PX / EXCEL_PIXELS_PER_WIDTH_UNIT;
}
```

**(f) 판정 라벨을 순수 함수로 추출 (라벨 단일 소스 — 상세 시트와의 divergence 방지)**

118~137행의 if/else 체인을 아래 헬퍼로 옮기고, `Export` 본문에서는
`ws.Cell(row, 8).Value = BuildJudgementText(m);` 한 줄로 대체한다.
**분기 순서와 반환 문자열을 한 글자도 바꾸지 않는다** (기존 주석도 함께 옮긴다).

```csharp
/// <summary>
/// 측정 1건의 판정 표시 문자열. DATUM_FAIL > NO_IMAGE > CROSS_Z_INCOMPLETE > OK/NG > "-" 순서.
/// ReviewMeasurementRow 로직과 일치해야 하며, 일괄검사 상세 시트도 이 함수를 공유한다.
/// </summary>
internal static string BuildJudgementText(MeasurementResultDto m)
```
- `m` 이 null 이면 `"-"` 반환 (방어).
- `m.LastHasResult` 분기 안의 OK/NG 는 삼항 대신 `if (m.LastJudgement) { return "OK"; } else { return "NG"; }` 로 쓴다
  (기존 132행의 삼항은 이 이동 과정에서 if/else 로 풀어쓰는 것이 규칙 1 준수).

**(g) 하지 말 것 (스코프 가드)**
- `RepeatExcelExportService.cs` / `InspectionListView.xaml.cs` 는 이 태스크에서 **건드리지 않는다** (Task 2 담당).
- `public` 승격 금지 — `internal` 로 충분하다.
- 컬럼 순서/헤더 문자열/셀 값 변경 금지. 이 태스크의 diff 는 순수 리팩토링이어야 한다.
- 기존 삼항 리팩토링 금지 (단, (f) 에서 **옮기는** 132행 삼항만 if/else 로 푼다).
- NuGet/csproj 변경 금지.
  </action>
  <verify>
    <automated>
msbuild C:/Info/Project/DataMeasurement/WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /nologo /v:minimal 2>&1 | tail -15 ; \
echo "=== 개방된 계약 (각 1) ===" ; \
grep -c "internal static byte\[\] LoadCaptureImageBytes(string szPath, Dictionary<string, byte\[\]> dicCache, Stopwatch swBudget, int nBudgetMs)" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/ExcelExportService.cs ; \
grep -c "internal static bool TryInsertCaptureImage(IXLWorksheet ws, int nRow, int nColumn, byte\[\] arrBytes)" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/ExcelExportService.cs ; \
grep -c "internal static void ApplyCaptureColumnWidth" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/ExcelExportService.cs ; \
grep -c "internal static string BuildJudgementText" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/ExcelExportService.cs ; \
echo "=== Export 호출부가 기존 예산/컬럼 상수를 그대로 넘김 (각 1) ===" ; \
grep -c "LoadCaptureImageBytes(fai.CaptureImageFileName, dicCaptureCache, swWaitBudget, CAPTURE_WAIT_BUDGET_MS)" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/ExcelExportService.cs ; \
grep -c "TryInsertCaptureImage(ws, row, CAPTURE_IMAGE_COLUMN, arrCaptureBytes)" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/ExcelExportService.cs ; \
echo "=== 라벨/헤더 불변 (판정 라벨 각 1, 헤더 1) ===" ; \
grep -c '"DETECT FAIL"' C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/ExcelExportService.cs ; \
grep -c '"CROSS-Z INCOMPLETE"' C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/ExcelExportService.cs ; \
grep -c '"Shot", "FAI", "측정명", "Nominal", "Tol+", "Tol-", "측정값", "판정", "원본이미지 경로", "캡쳐이미지 경로", "캡쳐이미지"' C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/ExcelExportService.cs ; \
echo "=== 스코프 가드 (아래 3줄 전부 0) ===" ; \
git -C C:/Info/Project/DataMeasurement diff --name-only | grep -c "RepeatExcelExportService.cs" ; \
git -C C:/Info/Project/DataMeasurement diff --name-only | grep -c "InspectionListView.xaml.cs" ; \
git -C C:/Info/Project/DataMeasurement diff -- WPF_Example/Custom/Export/ExcelExportService.cs | grep "^+" | grep -c " ? .* : "
    </automated>

판정 기준:
- msbuild 출력에 `error` 0건 (기존 warning 허용).
- "개방된 계약" 4줄 전부 `1`, "호출부" 2줄 전부 `1`.
- 판정 라벨 2줄 `1`, 헤더 배열 1줄 `1` (문자열이 한 글자도 안 바뀜).
- 스코프 가드 3줄 전부 `0` — 다른 두 파일 미수정 + 새로 추가한 라인에 삼항 없음.
  </verify>
  <done>
- `ExcelExportService.cs` 만 수정됨. Debug|x64 빌드 error 0.
- `LoadCaptureImageBytes` / `TryInsertCaptureImage` / `ApplyCaptureColumnWidth` / `BuildJudgementText` 가 `internal static` 으로 노출됨.
- `WaitForCaptureImage` / `TryReadCompleteJpeg` 는 `private` 유지.
- 일반검사 export 의 컬럼/헤더/셀 값/판정 라벨/대기 동작이 리팩토링 전과 동일 (순수 리팩토링 diff).
  </done>
</task>

<task type="auto">
  <name>Task 2: ExportBatch + '회차별 상세' 시트 추가 및 일괄검사 버튼 배선</name>
  <files>WPF_Example/Custom/Export/RepeatExcelExportService.cs, WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <action>
**(a) `RepeatExcelExportService.cs` — using 1줄 추가**

```csharp
using System.Diagnostics;   // Stopwatch (전체 대기 예산)
```
`ClosedXML.Excel.Drawings` / `System.IO` / `System.Threading` 은 **추가하지 않는다.**
이미지 I/O 는 전부 `ExcelExportService` 헬퍼 안에 있다 — 여기서 직접 파일을 읽거나 그림을 넣으면 E1L-03 위반이다.

**(b) 클래스 상단 const 추가 (매직넘버 금지)**

```csharp
private const string DETAIL_SHEET_NAME = "회차별 상세";
private const int DETAIL_CAPTURE_IMAGE_COLUMN = 12;   // 회차(1) 가 앞에 붙어 단일 cycle 포맷보다 1 밀림

// cycle 수에 비례해 늘리되 상한을 둔다. UI 스레드 동기 호출이므로 무한정 블로킹은 금지 (E1L-05).
private const int BATCH_CAPTURE_WAIT_PER_CYCLE_MS = 1000;
private const int BATCH_CAPTURE_WAIT_BUDGET_MIN_MS = 5000;
private const int BATCH_CAPTURE_WAIT_BUDGET_MAX_MS = 15000;
```

**(c) 기존 `Export` 본문을 `ExportInternal` 로 그대로 이관 (E1L-01 — 회귀 0 의 핵심)**

33행 `public static bool Export(...)` 의 **본문 전체(35~190행)를 한 글자도 바꾸지 않고**
새 private 메서드로 옮기고, `Export` 는 위임 1줄만 남긴다.

```csharp
/// <summary>
/// 반복검사(Gage R&R) export. 집계 2시트만 생성한다 — 기존 동작/포맷 그대로.
/// </summary>
public static bool Export(List<CycleResultDto> cycles, string recipeName, string outputPath)
{
    return ExportInternal(cycles, recipeName, outputPath, false);
}

/// <summary>
/// 일괄검사(Batch) export. 집계 2시트 + 회차별 상세 시트(캡쳐이미지 포함)를 생성한다.
/// 회차별 실물 부품이 서로 다르므로 cycle 별 측정값/이미지가 의미를 가진다.
/// </summary>
public static bool ExportBatch(List<CycleResultDto> cycles, string recipeName, string outputPath)
{
    return ExportInternal(cycles, recipeName, outputPath, true);
}

private static bool ExportInternal(List<CycleResultDto> cycles, string recipeName, string outputPath, bool bWithDetailSheet)
{
    ... 기존 Export 본문 그대로 ...
}
```
`ExportInternal` 본문에서 **유일하게 추가되는 것**은 176행 `wb.SaveAs(outputPath);` **바로 앞** 한 블록이다:

```csharp
if (bWithDetailSheet)
{
    AppendDetailSheet(wb, cycles);
}
```
- 시트 순서: 집계 2시트 **뒤**(3번째)에 온다.
- 시트1/시트2 작성 코드, `ws1.Cell(...)`/`ws2.Cell(...)` 대입, `AdjustToContents`, catch 블록의
  `"[RepeatExcelExportService] Export failed: "` 문자열은 **전부 그대로**다.

**(d) `AppendDetailSheet` 추가 (30~40줄)**

```csharp
/// <summary>
/// 회차별 상세 시트: cycle(=실물 부품 1개) × Shot × FAI × 측정을 1행씩 펼치고 캡쳐이미지를 셀에 넣는다.
/// 이미지 로드/삽입은 ExcelExportService 헬퍼를 그대로 재사용한다 (로직 복제 금지).
/// </summary>
private static void AppendDetailSheet(XLWorkbook wb, List<CycleResultDto> cycles)
```
구현:
1. `var ws = wb.Worksheets.Add(DETAIL_SHEET_NAME);`
2. 헤더 1행 (**이 12개 순서 고정 — CONTEXT 확정 사항, 컬럼 추가/삭제 금지**):
   ```csharp
   string[] h3 = { "회차", "Shot", "FAI", "측정명", "Nominal", "Tol+", "Tol-", "측정값", "판정",
                   "원본이미지 경로", "캡쳐이미지 경로", "캡쳐이미지" };
   ```
   `for (int i = 0; i < h3.Length; i++) { ws.Cell(1, i + 1).Value = h3[i]; }`
3. 대기 예산 계산 (삼항 금지, 곱셈 오버플로 방지를 위해 상한 먼저 판정):
   ```csharp
   int nCycleCount = cycles.Count;
   int nBudgetMs = BATCH_CAPTURE_WAIT_BUDGET_MAX_MS;
   bool bUnderMaxCycles = nCycleCount < (BATCH_CAPTURE_WAIT_BUDGET_MAX_MS / BATCH_CAPTURE_WAIT_PER_CYCLE_MS);
   if (bUnderMaxCycles)
   {
       nBudgetMs = nCycleCount * BATCH_CAPTURE_WAIT_PER_CYCLE_MS;
   }
   if (nBudgetMs < BATCH_CAPTURE_WAIT_BUDGET_MIN_MS)
   {
       nBudgetMs = BATCH_CAPTURE_WAIT_BUDGET_MIN_MS;
   }
   ```
4. 시트 전체에서 **공유**하는 캐시 1개 + Stopwatch 1개:
   ```csharp
   var dicCaptureCache = new Dictionary<string, byte[]>();
   var swWaitBudget = Stopwatch.StartNew();
   ```
   (cycle 마다 새로 만들면 예산이 cycle 수만큼 곱해져 상한이 무의미해진다 — 반드시 밖에서 1개.)
5. `int nRow = 2; int nCycleNo = 1;` 로 시작해 `foreach (var cycle in cycles)` 루프:
   - `cycle` 이 null 이면 `nCycleNo++` 후 `continue`.
   - `cycle.Shots` null 이면 `nCycleNo++` 후 `continue` (**회차 번호는 건너뛴 cycle 도 소비한다** — 리스트 순번과 일치해야 함).
   - Shot/FAI/Measurement 3중 루프에서 각 측정마다
     `WriteDetailRow(ws, nRow, nCycleNo, shot, fai, m, dicCaptureCache, swWaitBudget, nBudgetMs); nRow++;`
   - null 컬렉션은 기존 시트2 코드와 같은 `if (xxx == null) { continue; }` 패턴으로 건너뛴다.
   - 루프 끝에 `nCycleNo++`.
6. 마무리:
   ```csharp
   ws.Columns().AdjustToContents();
   ExcelExportService.ApplyCaptureColumnWidth(ws, DETAIL_CAPTURE_IMAGE_COLUMN);
   ```
   (`AdjustToContents` 는 그림을 고려하지 않으므로 폭 지정은 반드시 그 **뒤**.)

**(e) `WriteDetailRow` 추가 (30~40줄)**

```csharp
/// <summary>
/// 상세 시트 1행 = 측정 1건. 값/판정은 단일 cycle 포맷(ExcelExportService)과 동일하게 맞춘다.
/// </summary>
private static void WriteDetailRow(IXLWorksheet ws, int nRow, int nCycleNo, ShotResultDto shot, FaiResultDto fai,
                                   MeasurementResultDto m, Dictionary<string, byte[]> dicCache, Stopwatch swBudget, int nBudgetMs)
```
구현 (컬럼 번호 순서대로, 삼항 금지 — `??` 대신 if/else 로 빈 문자열 처리):
1. `ws.Cell(nRow, 1).Value = nCycleNo;`
2. 2=`shot.ShotName`, 3=`fai.FAIName`, 4=`m.MeasurementName` — null 이면 `""`.
3. 5=`m.NominalValue`, 6=`m.TolerancePlus`, 7=`m.ToleranceMinus`.
4. 8 측정값: `if (m.LastHasResult) { ws.Cell(nRow, 8).Value = m.LastMeasuredValue; } else { ws.Cell(nRow, 8).Value = "-"; }`
   (0.0 도 정상 결과이므로 값이 아니라 `LastHasResult` 로 판별한다 — CO-23-01.)
5. 9 판정: `ws.Cell(nRow, 9).Value = ExcelExportService.BuildJudgementText(m);`
   **여기서 라벨 if/else 를 복사해 쓰지 말 것** — 라벨은 단일 소스여야 한다.
6. 10=`fai.OriginImageFileName`, 11=`fai.CaptureImageFileName` — null 이면 `""`.
7. 캡쳐이미지 (E1L-02/E1L-06):
   ```csharp
   byte[] arrCaptureBytes = ExcelExportService.LoadCaptureImageBytes(fai.CaptureImageFileName, dicCache, swBudget, nBudgetMs);
   ExcelExportService.TryInsertCaptureImage(ws, nRow, DETAIL_CAPTURE_IMAGE_COLUMN, arrCaptureBytes);
   ```
   반환값을 검사하지 않는다 — 이미지가 없으면 셀이 빈 칸으로 남고 경고 로그만 남는 것이 정상 동작이며,
   export 자체는 계속 진행되어 `true` 를 반환해야 한다.

**(f) `InspectionListView.xaml.cs` — 호출 1줄만 교체 (E1L-04)**

621~622행:
```csharp
bool ok = ReringProject.Export.RepeatExcelExportService.ExportBatch(
    _batchAccumulated, recipeName, dlg.FileName);
```
`Btn_batchExport_Click` 의 나머지(가드, 파일명 기본값, SaveFileDialog, 결과 메시지박스)와
이 파일의 다른 코드는 **한 줄도 바꾸지 않는다.**

**(g) 하지 말 것 (스코프 가드)**
- `ReviewerWindow.xaml.cs` 수정 금지 — 반복검사 경로는 이번 작업의 통제 대상이 아니다.
- `RepeatExcelExportService.cs` 안에서 `AddPicture` / `File.ReadAllBytes` / `Thread.Sleep` /
  JPEG EOI 검사 / 판정 라벨 if-체인을 **직접 작성 금지** (E1L-03 복제 금지).
- 시트1/시트2 의 컬럼·헤더·값·`MapAlgorithmCategory` 변경 금지.
- 상세 시트 컬럼 12개 구성 변경 금지 (자재번호 등 임의 추가 금지 — CONTEXT 확정 사항).
- 대기 예산 상한(`BATCH_CAPTURE_WAIT_BUDGET_MAX_MS`) 제거/무력화 금지.
- NuGet/csproj 변경 금지.
  </action>
  <verify>
    <automated>
msbuild C:/Info/Project/DataMeasurement/WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /nologo /v:minimal 2>&1 | tail -15 ; \
echo "=== 새 진입점/구조 (각 1 이상) ===" ; \
grep -c "public static bool ExportBatch(List<CycleResultDto> cycles, string recipeName, string outputPath)" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
grep -c "private static bool ExportInternal(List<CycleResultDto> cycles, string recipeName, string outputPath, bool bWithDetailSheet)" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
grep -c "private static void AppendDetailSheet" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
grep -c "private static void WriteDetailRow" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
echo "=== 기존 Export 시그니처 보존 + 위임 (각 1) ===" ; \
grep -c "public static bool Export(List<CycleResultDto> cycles, string recipeName, string outputPath)" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
grep -c "return ExportInternal(cycles, recipeName, outputPath, false);" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
echo "=== 헬퍼 재사용 (각 1 이상) ===" ; \
grep -c "ExcelExportService.LoadCaptureImageBytes(" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
grep -c "ExcelExportService.TryInsertCaptureImage(ws, nRow, DETAIL_CAPTURE_IMAGE_COLUMN, arrCaptureBytes)" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
grep -c "ExcelExportService.BuildJudgementText(m)" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
grep -c "ExcelExportService.ApplyCaptureColumnWidth(ws, DETAIL_CAPTURE_IMAGE_COLUMN)" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
echo "=== 상세 헤더 12개 + 예산 상한 (각 1) ===" ; \
grep -c '"회차", "Shot", "FAI", "측정명", "Nominal", "Tol+", "Tol-", "측정값", "판정", "원본이미지 경로", "캡쳐이미지 경로", "캡쳐이미지"' C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
grep -c "BATCH_CAPTURE_WAIT_BUDGET_MAX_MS = 15000" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
echo "=== 배선 (1) ===" ; \
grep -c "RepeatExcelExportService.ExportBatch(" C:/Info/Project/DataMeasurement/WPF_Example/UI/ControlItem/InspectionListView.xaml.cs ; \
echo "=== 복제 금지 가드 (아래 6줄 전부 0) ===" ; \
grep -c "AddPicture" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
grep -c "File.ReadAllBytes" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
grep -c "Thread.Sleep" C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
grep -c '"DETECT FAIL"' C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
grep -c '"CROSS-Z INCOMPLETE"' C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
git -C C:/Info/Project/DataMeasurement diff --name-only | grep -c "ReviewerWindow.xaml.cs" ; \
echo "=== 시트1/시트2 값 불변 (각 1) ===" ; \
grep -c 'Worksheets.Add("반복도 통계")' C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
grep -c 'Worksheets.Add("알고리즘 통계")' C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
grep -c 'Math.Round(Math.Abs(s.Mean - s.NominalValue), 6)' C:/Info/Project/DataMeasurement/WPF_Example/Custom/Export/RepeatExcelExportService.cs ; \
echo "=== 새 코드 삼항 금지 (0) ===" ; \
git -C C:/Info/Project/DataMeasurement diff -- WPF_Example/Custom/Export/RepeatExcelExportService.cs | grep "^+" | grep -v "successRate\|mean2" | grep -c " ? .* : "
    </automated>

판정 기준:
- msbuild 출력에 `error` 0건.
- "새 진입점/구조" 4줄, "기존 Export 시그니처 보존" 2줄, "헬퍼 재사용" 4줄, "상세 헤더/예산" 2줄, "배선" 1줄 — 전부 `1` 이상.
- "복제 금지 가드" 6줄 전부 `0` (RepeatExcelExportService 에 이미지 I/O·판정 라벨 없음 + ReviewerWindow 미수정).
- "시트1/시트2 값 불변" 3줄 전부 `1`.
- "새 코드 삼항 금지" `0` (기존 151/152행 삼항은 grep -v 로 제외됨).
  </verify>
  <done>
- Debug|x64 빌드 error 0. 수정 파일은 `ExcelExportService.cs`, `RepeatExcelExportService.cs`, `InspectionListView.xaml.cs` 3개뿐.
- `RepeatExcelExportService.Export` 시그니처/산출물 불변, `ExportBatch` 신규 추가.
- `ExportBatch` 로 만든 xlsx 는 시트 3개, 3번째가 "회차별 상세"(12컬럼, 1열 회차).
- 상세 행의 이미지 로드/삽입/판정 라벨이 전부 `ExcelExportService` 헬퍼 호출이며 복제 코드 0.
- 대기 예산 = cycle 수 × 1s, 하한 5s / 상한 15s 로 클램프되고 Stopwatch·캐시는 시트 전체 공유 1개.
- 일괄검사 버튼이 `ExportBatch` 를 호출한다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 일괄검사 export 육안 확인 + 반복검사 회귀 확인</name>
  <what-built>
일괄검사(Batch) 엑셀 export 를 반복검사(Gage R&R) export 에서 분리했습니다.

- **반복검사 버튼(리뷰어)**: 예전 그대로입니다. 시트 2개("반복도 통계", "알고리즘 통계")만 나옵니다.
  같은 부품을 N회 반복하는 검사라 회차별 이미지가 전부 똑같아서 의미가 없기 때문입니다.
- **일괄검사 버튼(검사목록 화면)**: 위 2개 시트 뒤에 **"회차별 상세"** 시트가 하나 더 붙습니다.
  1열이 **회차**(1, 2, 3...)이고, 그 뒤로 Shot / FAI / 측정명 / Nominal / Tol+ / Tol- / 측정값 / 판정 /
  원본이미지 경로 / 캡쳐이미지 경로 / **캡쳐이미지(실제 그림)** 순입니다.
  일괄검사는 서로 다른 실물 부품을 순서대로 찍는 검사라 회차별 값과 이미지가 전부 의미가 있습니다.

캡쳐 JPG 는 백그라운드 스레드가 나중에 저장하기 때문에 export 하는 순간 파일이 아직 없을 수 있습니다.
그래서 잠깐(파일당 최대 1.5초) 기다렸다가, 그래도 없으면 그 셀만 비워둔 채 로그 경고만 남기고 계속 진행합니다.
export 가 실패하는 일은 없습니다.

또 하나 중요한 점은 **전체 대기 시간에 상한**을 뒀다는 것입니다. 회차가 많아질수록 기다릴 수 있는 시간이
회차당 1초씩 늘어나되, 아무리 많아도 **총 15초**를 넘지 않습니다. 이미지가 하나도 없는 큰 batch 를
export 해도 화면이 몇 분씩 멈추는 일은 없습니다.
  </what-built>
  <files>(코드 변경 없음 — 실행 중인 앱과 저장된 xlsx 를 사람이 확인)</files>
  <action>
실행을 멈추고 개발자에게 아래 확인을 요청한다. 승인 전에는 커밋/후속 작업을 진행하지 않는다.
문제가 보고되면 Task 2(필요 시 Task 1)로 돌아가 수정한 뒤 이 체크포인트를 다시 요청한다.
  </action>
  <how-to-verify>
1. `Debug|x64` 로 빌드 후 앱 실행 (SIMUL_MODE 로 충분합니다).
2. 검사목록에서 SHOT 몇 개를 체크하고 **일괄 검사**를 **2회 이상** 실행해서 회차가 2 이상 쌓이게 합니다.
3. **[일괄 Export]** 버튼 → 아무 위치에나 저장 → 저장된 `.xlsx` 를 엑셀로 열고 확인해 주세요:
   - [ ] 시트 탭이 **3개** ("반복도 통계", "알고리즘 통계", "회차별 상세") 이고 순서가 이대로인지
   - [ ] "회차별 상세" 시트 1행 헤더가 **회차 | Shot | FAI | 측정명 | Nominal | Tol+ | Tol- | 측정값 | 판정 | 원본이미지 경로 | 캡쳐이미지 경로 | 캡쳐이미지** 인지
   - [ ] A열 **회차** 값이 1, 2, ... 로 검사한 순서대로 붙고, 같은 회차 안에 그 회차의 모든 Shot/FAI/측정 행이 들어있는지
   - [ ] **L열에 실제 캡쳐 이미지**(오버레이가 그려진 결과 화면)가 보이고, 회차마다 **서로 다른 이미지**인지
   - [ ] 이미지가 찌그러지지 않고 셀 안에 적당한 크기로 들어가는지
   - [ ] 판정 열 값이 화면(검사목록 그리드)에서 보이는 판정과 일치하는지 (OK/NG/DETECT FAIL/NO IMAGE/CROSS-Z INCOMPLETE)
   - [ ] 앞의 2개 집계 시트("반복도 통계"/"알고리즘 통계") 값이 예전 일괄 export 와 동일한지
4. **반복검사 회귀 확인 (가장 중요):** 리뷰어 창의 **반복검사(Gage R&R) export** 버튼을 눌러 저장한 뒤,
   - [ ] 시트가 **2개뿐**인지 ("회차별 상세" 가 **없어야** 정상입니다)
   - [ ] 두 시트의 값/컬럼이 이번 작업 전과 완전히 동일한지
5. **빈 칸 동작 확인:** 일괄검사가 끝나자마자 곧바로 export 해 보세요.
   뒤쪽 몇 행의 L열이 비어 있어도 정상이며, "저장 완료" 로 성공해야 합니다.
   (로그에 `capture image not ready, cell left blank` 경고가 남습니다.)
6. **멈춤 확인:** 캡쳐 이미지 폴더를 지운(또는 아예 없는) 상태에서 회차가 여러 개 쌓인 batch 를 export 해 보세요.
   길어야 **15초** 안에 저장 완료 창이 떠야 합니다. 수십 초~수 분 멈추면 실패입니다.

참고로 알아두실 점: 셀에 들어가는 그림은 **원본 JPG 바이트 그대로** 엑셀에 포함됩니다
(작게 보이는 건 표시 크기만 줄인 것). 회차 × 측정 항목이 많아지면 xlsx 용량이 꽤 커질 수 있습니다.
실제 용량이 부담스러우면 "썸네일로 줄여 넣기"를 별도 작업으로 요청해 주세요 — 이번 범위 밖입니다.
  </how-to-verify>
  <verify>
    <automated>MANUAL — 사람이 앱을 실행해 xlsx 를 열어보는 것 외에 자동 검증 수단이 없다 (이 저장소에 테스트 프레임워크 없음). 자동 검증은 Task 1/2 의 빌드 + 구조 grep 이 담당한다.</automated>
  </verify>
  <done>위 how-to-verify 3~6번 체크리스트를 개발자가 모두 확인하고 승인했다. 특히 4번(반복검사 시트 2개 유지) 이 통과해야 한다.</done>
  <resume-signal>"approved" 라고 입력하시거나, 문제가 있으면 어떤 항목이 어떻게 다른지 알려주세요.</resume-signal>
</task>

</tasks>

<source_coverage_audit>
Quick 작업이므로 소스는 CONTEXT.md 하나다 (ROADMAP/REQUIREMENTS/RESEARCH 해당 없음).

| ID | 출처 | 항목 | 상태 | 커버 위치 |
|----|------|------|------|-----------|
| E1L-01 | CONTEXT 분리 방식 | `Export(...)` 시그니처/동작 무변경, 새 메서드 추가 | COVERED | Task 2 (c) — 본문 이관 + 위임, verify 의 "기존 Export 시그니처 보존" |
| E1L-02 | CONTEXT 상세 시트 구성 | 회차 선두 12컬럼 상세 시트 + 캡쳐이미지 | COVERED | Task 2 (d)(e) |
| E1L-03 | CONTEXT 상세 시트 구성 | 이미지 로직 복붙 금지, 헬퍼 재사용 | COVERED | Task 1 전체 + Task 2 verify "복제 금지 가드" |
| E1L-04 | CONTEXT 분리 방식 | `InspectionListView.xaml.cs:621` 호출부만 교체 | COVERED | Task 2 (f) |
| E1L-05 | CONTEXT 재량 + 제약 | cycle 수 비례 대기 예산, UI 무한 블로킹 금지 | COVERED | Task 2 (b)(d) — 1s/cycle, 5s~15s 클램프, Stopwatch 공유 |
| E1L-06 | CONTEXT 상세 시트 구성 | 캡쳐 부재 행 = 빈 칸 + 경고, export 성공 유지 | COVERED | Task 2 (e)7 + Task 3 체크 5 |

Claude 재량 항목 처리:
- 시트 이름 → `"회차별 상세"`, 순서 → 집계 2시트 **뒤**(CONTEXT 권고 그대로).
- 헬퍼 재사용 방식 → 별도 공유 클래스 추출 대신 `ExcelExportService` 헬퍼 **부분 개방**(`internal static`).
  `WaitForCaptureImage`/`TryReadCompleteJpeg` 는 외부 호출 지점이 없어 `private` 유지 — 노출 최소화, 복제 0.
- 대기 예산 → 고정값이 아니라 **cycle 비례 + 하한/상한 클램프**. 상한 15s 는 UI 체감상 허용 가능한 최악값.

이연/제외 항목: 없음 (CONTEXT 에 Deferred Ideas 섹션 없음).
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 누적 batch 결과(`_batchAccumulated`) → RepeatExcelExportService | cycle DTO 의 `CaptureImageFileName` 경로 문자열이 그대로 파일 I/O 로 흘러간다 |
| 캡쳐 JPG 파일 → ClosedXML 이미지 디코더 | 워커가 쓰는 중이거나 손상된 바이트가 파서로 들어갈 수 있다 |
| UI 스레드(버튼 클릭) → 동기 export | 폴링 대기가 화면 프리즈로 직결된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-E1L-01 | Denial of Service | `AppendDetailSheet` 의 cycle × FAI 폴링 대기 (UI 스레드 블로킹) | mitigate | Stopwatch/캐시를 시트 전체에 **1개만** 두고, 예산을 `cycles.Count × 1s` 로 산정하되 `BATCH_CAPTURE_WAIT_BUDGET_MAX_MS`(15s) 로 하드 클램프. 파일당 상한 1.5s 는 기존대로 유지 |
| T-E1L-02 | Tampering | 잘리거나 손상된 JPEG 이 ClosedXML 디코더로 유입 | mitigate | `TryReadCompleteJpeg` 의 EOI(FF D9) 검사 + `TryInsertCaptureImage` 전체 try/catch 를 **그대로 재사용**(복제하지 않으므로 검사 로직이 갈라질 수 없다) |
| T-E1L-03 | Information Disclosure | `LoadCaptureImageBytes` 의 `File.ReadAllBytes(szPath)` | accept | 경로는 앱이 `CaptureImageSaveService.BuildFilePath` 로 만들어 로컬에 기록한 값이고, 실행 사용자 권한으로 읽어 같은 사용자가 저장하는 워크북에만 넣는다. 권한 상승 없음 (T-D9Y-01 과 동일 판단) |
| T-E1L-04 | Denial of Service | 회차 × 측정 수만큼 원본 JPG 를 임베드 → xlsx 용량/메모리 급증 | accept | 캡쳐 JPG 는 앱 자체 산출물로 크기가 예측 가능하다. 썸네일 리샘플링은 CONTEXT 범위 밖 → Task 3 체크포인트에서 실측 용량을 개발자에게 보고해 후속 판단에 맡긴다 |
| T-E1L-05 | Tampering | 리팩토링(`Export` 본문 이관) 중 반복검사 산출물 변질 | mitigate | 본문을 **한 글자도 바꾸지 않고** 이관하고 `Export` 는 위임 1줄만 남긴다. verify 에서 시트명/편차 수식/헤더 문자열 grep + Task 3 체크 4 에서 사람이 실제 xlsx 비교 |
</threat_model>

<verification>
1. `msbuild WPF_Example/DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64` — error 0.
2. `git diff --name-only` 결과가 아래 3개뿐 (기존 `DatumMeasurement.csproj` 의 SIMUL_MODE 로컬 변경분 제외):
   `ExcelExportService.cs`, `RepeatExcelExportService.cs`, `InspectionListView.xaml.cs`.
3. `RepeatExcelExportService.cs` 에 `AddPicture` / `File.ReadAllBytes` / `Thread.Sleep` /
   `"DETECT FAIL"` / `"CROSS-Z INCOMPLETE"` 가 **0건** (이미지·판정 로직 복제 0).
4. `RepeatExcelExportService.Export(List<CycleResultDto>, string, string)` 시그니처가 그대로 존재하고
   `ExportInternal(cycles, recipeName, outputPath, false)` 로 위임한다.
5. 새로 추가된 라인에 삼항 연산자 `?:` 와 `//YYMMDD hbk` 형식 주석이 없다.
6. Task 3 human-verify 체크리스트 전 항목 통과 (특히 반복검사 시트 2개 유지).
</verification>

<success_criteria>
- 일괄검사 xlsx 에 "회차별 상세" 시트가 생기고, 회차 컬럼 + 회차별 캡쳐이미지가 실제로 보인다 (E1L-02).
- 반복검사(Gage R&R) export 는 시트 2개 그대로, 값/포맷 회귀 0 (E1L-01).
- 이미지 로드/삽입/판정 라벨 로직이 `ExcelExportService.cs` 한 곳에만 존재한다 (E1L-03).
- 일괄검사 버튼만 `ExportBatch` 를 호출하고 그 외 UI 변경 0 (E1L-04).
- 이미지가 하나도 없는 대량 batch export 가 15초 안에 끝난다 (E1L-05).
- 캡쳐 JPG 부재 행은 빈 칸 + 경고 로그이고 export 는 성공한다 (E1L-06).
- 새 NuGet 0개, Debug|x64 빌드 error 0.
</success_criteria>

<output>
완료 후 `.planning/quick/260805-e1l-batch-export/260805-e1l-SUMMARY.md` 를 작성한다.
</output>
