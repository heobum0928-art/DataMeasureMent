---
phase: quick-260805-f3w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Setting/SystemSetting.cs
  - WPF_Example/SystemHandler.cs
  - WPF_Example/Utility/FlowLog.cs
  - WPF_Example/DatumMeasurement.csproj
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
autonomous: false
requirements: [D-F3W-01, D-F3W-02-REV, D-F3W-03-S1, D-F3W-03-S2, D-F3W-03-S11, D-F3W-03-S12]

must_haves:
  truths:
    - "새 로그 카테고리 Flow 가 로그 뷰어 메뉴에 자동으로 뜨고, 파일로도 저장된다"
    - "검사 사이클 1회가 끝나면 Flow 로그에 딱 1줄이 남는다"
    - "그 1줄에 시퀀스명 / 종합판정(OK·NG) / 측정 개수 / 벗어난 개수 / 전체 소요시간(초)이 모두 있다"
    - "흐름 로그에 measurePhi/strip/transform/XLD/HTuple 같은 전문용어가 없다"
    - "Trace 로그에서 strip 단위 반복 출력(사이클당 40~240줄)이 사라지고 ROI 단위 요약 1줄로 대체된다"
    - "같은 ROI 에서 strip 예외가 여러 번 나도 로그엔 첫 1건만 남고, 나머지는 요약줄의 failed 카운트로만 집계된다"
    - "[ALIGN] datumDetectRotDeg vs patternThetaDeg 확증 로그는 그대로 남아 있다"
    - "측정 결과값/판정/제어 흐름은 이전과 동일하다 (로그만 변경)"
  artifacts:
    - path: "WPF_Example/Utility/FlowLog.cs"
      provides: "흐름 로그 단일 진입점. public 메서드 1개(CycleEnd)뿐. 절대 throw 하지 않음"
      contains: "public static class FlowLog"
    - path: "WPF_Example/Setting/SystemSetting.cs"
      provides: "ELogType.Flow = 7 (append) + FlowLogSavePath + GetLogSavePath case"
      contains: "Flow = 7"
    - path: "WPF_Example/SystemHandler.cs"
      provides: "Logging.SetLog((int)ELogType.Flow, ...) 등록"
      contains: "ELogType.Flow"
    - path: "WPF_Example/Halcon/Algorithms/DatumFindingService.cs"
      provides: "strip 결과 집계(EStripOutcome) + ROI 단위 예외 로그 dedup 가드"
      contains: "EStripOutcome"
  key_links:
    - from: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      to: "WPF_Example/Utility/FlowLog.cs"
      via: "OnFinish / OnStop / OnError 구독 → 사이클 요약 1줄"
      pattern: "FlowLog\\.CycleEnd"
    - from: "WPF_Example/Utility/FlowLog.cs"
      to: "WPF_Example/Utility/Logging.cs"
      via: "Logging.PrintLog((int)ELogType.Flow, msg)"
      pattern: "ELogType\\.Flow"
    - from: "DatumFindingService.AppendEdgePointsFromStrip (strip 단위)"
      to: "DatumFindingService.TryFindLine / TryExtractEdgePoints (ROI 단위 요약줄)"
      via: "EStripOutcome 반환 → nOk/nNoEdge/nFailed 집계 + 예외 첫 1건만 로그"
      pattern: "EStripOutcome\\.(Ok|NoEdge|Failed)"
---

<objective>
**검사 사이클 1회당 요약 1줄**을 남기는 전용 로그 카테고리를 신설한다.

```
■ [SIDE] 사이클 종료 — 종합판정 NG (측정 30개 중 6개 벗어남, 소요 4.2초)
```

Purpose: 어느 시퀀스가 얼마나 걸려 무슨 결과를 냈는지 한 줄로 파악한다. 단계별 상세 로그는 D-F3W-02-REV 로 폐기됐다.
Output: `ELogType.Flow` 카테고리 + `FlowLog` 헬퍼(공개 메서드 1개) + `InspectionSequence` 사이클 요약 배선 + S1 축약(+S2 dedup 가드) + S11 삭제.
</objective>

<scope_coverage>
## Source Coverage (CONTEXT.md D-F3W-01 / D-F3W-02-REV / D-F3W-03)

| 항목 | 출처 | 상태 | 위치 |
|------|------|------|------|
| 흐름 로그 카테고리 신설 (ELogType 뒤에 append) | D-F3W-01 | COVERED | Task 1 |
| SetLog 등록 / 로그 창 카테고리 분리 | D-F3W-01 | COVERED | Task 1 (LogViewWindow 자동 생성 — UI 작업 불필요) |
| 시퀀스명 | D-F3W-02-REV #1 | COVERED | Task 2 (`FlowLog.CycleEnd` 첫 인자) |
| 전체 tact (초) | D-F3W-02-REV #2 | COVERED | Task 2 (`_flowStopwatch`, OnStart→종료) |
| 최종 결과값 (종합판정 + 측정개수/NG개수) | D-F3W-02-REV #3 | COVERED | Task 2 |
| 전문용어 금지 | D-F3W-02-REV | COVERED | Task 1 출력 규약 + Task 4 검증 |
| **S1** strip 루프 최내곽 (사이클당 40~240줄) → ROI 요약 1줄 | D-F3W-03 | COVERED | Task 3 (1)(2) |
| **S2** strip catch 예외 메시지 첫 1건만 dedup | D-F3W-03 | COVERED | Task 3 (3) |
| **S11** `[ALIGN-DIAG-LIVE]` 삭제 | D-F3W-03 | COVERED | Task 2 (5) |
| **S12** `[ALIGN] datumDetectRotDeg vs patternThetaDeg` 절대 유지 | D-F3W-03 | COVERED (보존 검증) | Task 2 verify |
| S5 `low strip coverage` (조건부 Error, 정상 시 무출력) | D-F3W-03 | 무변경 (원래 "유지" 결정) | — |
| `[FAI CrossZ IMG]` (Trace 유지) | D-F3W-03 | 무변경 | — |

### 스팸 정리 범위 = **S1 (+S2 dedup 가드) / S11 만** — 사용자 명시 승인

> 근거: **CONTEXT.md `### D-F3W-03: 스팸 정리 — 범위 축소됨` (2026-08-05 사용자 확정, LOCKED)**
>
> — *"이전 결정('전체 정리', 12건 전부)은 폐기됐다. 사용자가 '필요한 것들만' / '중요한건 시퀀스야'로 범위를 좁혀 확정: **S1과 S11만 이번 quick task 범위에 포함한다.** 나머지(S2/S3+S4/S6+S7+S8/S9/S10)는 이번 범위에서 명시적으로 제외 — ROI당 1~2줄 수준이라 흐름 로그를 가리지 않는다는 판단이며, 필요해지면 별도 작업으로 처리한다. S12는 여전히 절대 삭제 금지."*
>
> — *"**단, S2는 예외 — S1과 묶어서 같이 처리한다.** S1 수정으로 ROI 요약줄에 `failedStrips`(실패한 strip 개수) 카운트가 새로 추가되는데, 이는 S2 조건('요약줄에 failedStrips 카운트가 생기면 예외 메시지는 첫 1건만 남기고 dedup')을 그대로 발동시킨다. 이건 별도 스코프가 아니라 **S1 구현의 직접적인 결과**이므로, S1을 구현하는 김에 이 dedup 가드(같은 ROI에서 첫 예외 메시지 1건만 남기고 이후는 조용히 카운트만 증가)도 같이 넣는다 — 코드 몇 줄이면 되고, **안 넣으면 S1 자신이 만든 조건을 스스로 어기는 모순이 생긴다.**"*

즉 이 축소는 플래너의 임의 판단이 아니라 **사용자가 CONTEXT.md 를 직접 갱신해 확정한 범위**다. 그리고 **S2 는 "제외"가 아니라 S1 에 종속된 필수 구성요소**로 Task 3 안에 포함된다 (plan-checker 지적 반영).

### 폐기 — D-F3W-02-REV 가 D-F3W-02 를 대체함 (2026-08-05)

사용자 지시: 사이클 1회당 **시퀀스명 + 전체 tact + 최종 결과값**만 압축해서 남긴다.

| 폐기된 설계 | 원래 위치 | 폐기 사유 |
|-------------|-----------|-----------|
| `[1단계] 기준선 찾기` / `[2단계] 사진 촬영` / `[3단계] 측정` 단계 전환 로그 | Action_FAIMeasurement.cs EStep | D-F3W-02-REV: "검사 단계 전환 로그는 이번 범위에서 넣지 않는다" |
| 기준선(Datum) 개별 검출 결과 줄 | Action_FAIMeasurement.cs DatumPhase | 상동 |
| 측정 항목별 라인 (측정명/측정값/Nominal/Tol/합불) | Action_FAIMeasurement.cs Measure | 상동 — 측정개수/NG개수 집계로 대체 |
| `FlowLog.Line` / `Step` / `Measurement` / `CycleBegin` | FlowLog.cs | 소비처가 사라짐. `CycleEnd` 1개만 남김 |
| `FlowLog.SkipReasonToKorean` + SkipReason 한국어 매핑표 | FlowLog.cs | 항목별 skip 사유를 출력하지 않으므로 소비처 없음 |

→ 이 폐기로 **`Action_FAIMeasurement.cs` 는 이번 작업에서 수정하지 않는다.**

> **plan-checker 지적사항(측정명 fallback `==null` → `string.IsNullOrEmpty`) 처리 결과: 범위에서 소멸.**
> 해당 결함은 폐기된 "측정 항목별 라인"에서 `MeasurementName` 이 빈 문자열일 때 `TypeName` 으로 폴백하는 코드에만 존재했다. 압축 설계에는 측정명을 출력하는 지점이 **한 곳도 없다**(집계 카운트만 사용). 따라서 해당 코드 자체가 계획에서 사라졌다.
> 다만 동일 부류의 문자열 폴백/판정이 **2곳** 남는다 → 둘 다 **반드시 `string.IsNullOrEmpty`** 를 쓰도록 명시했다 (`== null` 단독 비교 금지):
> 1. Task 1 — `FlowLog.CycleEnd` 의 시퀀스명 폴백
> 2. Task 2 — 집계 루프의 `LastSkipReason` 유무 판정

### 이번 범위 제외 — 필요해지면 별도 작업

근거: S1 혼자 사이클당 40~240줄로 전체 스팸의 대부분이고, 아래 항목들은 **ROI당 1~2줄 수준**이라 흐름 로그를 가리지 않는다. 조사 결과는 기록으로 남겨두되 이번엔 손대지 않는다. (S2 는 위 인용대로 **제외 목록에서 빠져 Task 3 으로 이동**했다.)

| 건 | 위치 | 원래 계획했던 조치 | 제외 사유 |
|----|------|-------------------|-----------|
| S3+S4 | `VisionAlgorithmService.cs:182`, `214` | 연속 2줄 → 1줄 병합 | 호출당 2줄, 흐름 로그를 가리지 않음 |
| S6+S7+S8 | `DatumFindingService.cs:1677`/`1811`/`1820` 및 미러(`1950`/`2067`/`2076`) | 3줄 → 1줄 병합 | ROI당 3줄. S1 제거만으로 충분히 조용해짐 |
| S9 | `DatumFindingService.cs:1762`, `2018` (erosion tact) | 임계(50ms) 초과 시만 출력 | ROI당 최대 1줄, erosion 미사용 시 0줄 |
| S10 | `FAIEdgeMeasurementService.cs:76` (`[ALIGN-ROI]`) | Shot 단위 1줄로 dedup | align 사용 시에만, 측정당 1줄 |

→ 위 항목들 때문에 `VisionAlgorithmService.cs` 와 `FAIEdgeMeasurementService.cs` 는 **이번에 수정하지 않는다** (VisionAlgorithmService 는 S1 구현의 참조 패턴으로 읽기만 함).
</scope_coverage>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/quick/260805-f3w-flow-log/260805-f3w-CONTEXT.md

@WPF_Example/Setting/SystemSetting.cs
@WPF_Example/Utility/Logging.cs
@WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
@WPF_Example/Halcon/Algorithms/DatumFindingService.cs

<!-- 읽기 전용 참조: S1 집계 패턴의 정본. 이 파일은 수정하지 않는다. -->
@WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs

<coding_rules>
프로젝트 상시 코딩 규칙 (하위 에이전트 포함 전원 준수):
- **C# 7.2 전용.** `$"..."` 보간 문자열, `=>` 식 본문 멤버, `switch` 식, `record`, nullable 참조형식 **금지**. 문자열은 `string.Format` 또는 `+` 연결.
- **삼항 연산자 `?:` 금지 → 반드시 `if/else`.** `??` 도 쓰지 말고 임시변수 + null 체크로 푼다.
- **문자열 비어있음 판정은 `string.IsNullOrEmpty(...)`.** `== null` 단독 비교 금지 (빈 문자열을 놓친다 — plan-checker 지적 사항).
- 헝가리언 접두(신규 지역변수/필드): bool `b`, int `n`, string `sz`.
- 날짜 주석(`//YYMMDD hbk`) 규칙은 폐기됨. 비자명한 "왜"만 최소 주석.
- 함수는 30~40줄 이내로 유지, 초보자가 읽어 이해되게.
- **회귀 0.** 로그 변경이 측정값/판정/제어 흐름/타이밍을 절대 바꾸면 안 된다.
</coding_rules>

<interfaces>
<!-- 실행자가 코드베이스를 헤맬 필요 없도록 필요한 계약을 여기 박아둔다. -->

WPF_Example/Setting/SystemSetting.cs:18-26 (현재)
```csharp
public enum ELogType : int {
    Trace = 0, Camera = 1, LightController = 2, TcpConnection = 3,
    Result = 4, Image = 5, Error = 6,
}
```
※ `LogViewWindow.xaml.cs:36` 이 `Enum.GetNames(typeof(ELogType))` 로 메뉴를 자동 생성하므로 enum 에 추가만 하면 로그 창 메뉴가 자동으로 생긴다. UI 작업 불필요.
※ `GetLogSavePath(ELogType type, params string[] subDirs)` 는 SystemSetting.cs:210 의 switch. case 미추가 시 basePath = 실행폴더로 폴백(크래시 없음)이나 반드시 case 를 추가한다.

WPF_Example/Utility/Logging.cs
```csharp
public static bool SetLog(int id, string name, string savePath = null, Control listView = null);
public static bool PrintLog(int logID, string msg);   // 등록 안 된 id → 조용히 false, throw 없음
```

WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs — **집계에 쓰는 3개 필드만 사용한다**
```csharp
public bool   LastHasResult { get; set; }      // false = 이번 사이클에 측정 안 됨
public bool   LastJudgement { get; set; }      // true = OK
public string LastSkipReason { get; set; }     // SkipReason 상수 또는 null
```
※ `MeasurementName` / `TypeName` / `NominalValue` / `TolerancePlus` / `ToleranceMinus` / `LastMeasuredValue` 는 **이번 범위에서 사용하지 않는다** (항목별 출력 폐기).

WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
```csharp
// SequenceBase 이벤트: OnStart / OnFinish / OnStop / OnError  (모두 EventSequenceStateChanged(SequenceContext))
// SequenceBase.Name — 이 시퀀스 이름 (TOP / SIDE / BOTTOM 등)
// 생성자 :101 에 이미 `OnStart += HandleRunStartResetResults;` 구독 선례가 있다.
```
```csharp
// 순회 패턴 정본 (InspectionSequence.cs:226 HandleRunStartResetResults 발췌) — 집계도 이걸 그대로 복사한다
if (Actions == null) return;
foreach (var act in Actions) {
    var faiAct = act as Action_FAIMeasurement;
    if (faiAct == null) continue;
    var shot = faiAct.ShotParam;
    if (shot == null) continue;
    foreach (var fai in shot.FAIList) {
        if (fai == null) continue;
        if (fai.Measurements != null) {
            foreach (var m in fai.Measurements) { /* ... */ }
        }
    }
}
```

WPF_Example/Halcon/Algorithms/DatumFindingService.cs — S1/S2 작업 대상 구조 (라인 검증 완료)
```
:1605  private bool TryFindLine(...)                          ← ROI 단위 진입점 A
:1783, :1799  AppendEdgePointsFromStrip 호출 (scanHorizontal true/false 분기)
:1811~1813    "strip-loop accumulated {1} edge points across {2} strips"   ← 여기에 집계 병합
:1881  private bool TryExtractEdgePoints(...)                 ← A 의 미러
:2039, :2055  AppendEdgePointsFromStrip 호출
:2067~2069    "strip-loop(extract) accumulated ..."           ← 여기에 집계 병합
:2139  private void AppendEdgePointsFromStrip(...)            ← strip 단위. private 인스턴스 메서드
:2182~2185    [S1] per-strip "strip MeasurePos: ..." PrintLog ← 삭제 대상
:2195~2205    [S2] catch 안 "strip swallowed: {msg}" PrintLog ← dedup 가드 대상
```
※ 호출부는 총 **4곳**(1783 / 1799 / 2039 / 2055) — 반환값을 받는 것만 달라지고 인자 목록은 그대로다.
※ `DatumFindingService` 는 **매번 `new` 로 생성**된다 (`InspectionSequence.cs:1910`, `:1958` 2곳뿐, static 인스턴스 없음). `AlignPreTransform` 도 이미 인스턴스 상태로 쓰인다 → 인스턴스 필드는 한 검출 흐름 전용이며 스레드 공유가 없다.

WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs:125-184 (S1 집계 패턴의 정본 — 읽기 전용)
```csharp
int okStrips = 0; int noEdgeStrips = 0; int failedStrips = 0;
for (...) {
    EStripResult sr = AppendStrip(...);
    if (sr == EStripResult.Ok) { okStrips++; }
    else if (sr == EStripResult.NoEdge) { noEdgeStrips++; }
    else { failedStrips++; }
}
// 루프 종료 후 요약 1줄
Logging.PrintLog((int)ELogType.Trace,
    string.Format("[FitLine] strips ok {0}/{1} (noEdge {2}, failed {3}) -> {4} edge points",
        okStrips, stripCount, noEdgeStrips, failedStrips, edgeCount));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 흐름 로그 카테고리 + FlowLog 헬퍼 신설 (공개 메서드 1개)</name>
  <files>
    WPF_Example/Setting/SystemSetting.cs,
    WPF_Example/SystemHandler.cs,
    WPF_Example/Utility/FlowLog.cs (신규),
    WPF_Example/DatumMeasurement.csproj
  </files>
  <action>
D-F3W-01 구현. **꼭 필요한 최소로 — 아래 명시된 것 외에 메서드/기능을 추가하지 말 것.**

**(1) `WPF_Example/Setting/SystemSetting.cs`**
- `ELogType` 에 `Flow = 7,` 을 **맨 뒤에 추가**한다. 기존 0~6 값은 절대 재번호하지 않는다 (로그 파일 경로/영속 설정 하위호환).
- 로그 경로 프로퍼티 그룹(`:58-80`) 에 아래를 추가한다. **어트리뷰트는 인접 멤버 1개에만 적용되므로 반드시 자기 어트리뷰트를 붙인다** (과거 IN-01 결함):
  ```csharp
  [DirectoryPath]
  [AutoUpdateText]
  public string FlowLogSavePath { get; set; } = @"D:\Data\Flow";
  ```
- `GetLogSavePath` switch(`:212`) 에 `case ELogType.Flow: basePath = FlowLogSavePath; break;` 추가.

**(2) `WPF_Example/SystemHandler.cs:88-93`**
- 기존 등록 블록과 **완전히 같은 패턴**으로 한 줄 추가 (`Logging.Start()` 호출 전):
  ```csharp
  Logging.SetLog((int)ELogType.Flow, Enum.GetName(typeof(ELogType), ELogType.Flow), Setting.GetLogSavePath(ELogType.Flow));
  ```

**(3) `WPF_Example/Utility/FlowLog.cs` (신규 파일, namespace `ReringProject.Utility`)**
흐름 로그의 **단일 진입점**. 공개 메서드는 **딱 1개**다.

```csharp
public static class FlowLog
{
    // 검사 사이클 1회 요약 — 이 한 줄이 흐름 로그의 전부다.
    // 출력 예: "■ [SIDE] 사이클 종료 — 종합판정 NG (측정 30개 중 6개 벗어남, 소요 4.2초)"
    public static void CycleEnd(string szSequenceName, bool bAllPass, int nMeasured, int nNg, double dElapsedSec);
}
```

구현 규약 (전부 이 파일 안에서만 결정된다):
- **메서드 본문 전체를 `try { ... } catch { }` 로 감싼다.** 검사 스레드를 보호해야 하므로 어떤 경우에도 예외를 밖으로 던지지 않는다.
- 시퀀스명 폴백: **`if (string.IsNullOrEmpty(szSequenceName)) { szName = "이름없음"; } else { szName = szSequenceName; }`**
  — **`== null` 단독 비교 금지.** 빈 문자열이면 `[]` 처럼 비어 보이는 라벨이 나온다 (plan-checker 지적 사항의 동일 부류).
- 판정 문구: `bAllPass == true` → `"OK"`, `false` → `"NG"`. (if/else, 삼항 금지. P/F/O/X 같은 코드 문자 금지)
- 소요시간: `dElapsedSec.ToString("F1")` + `"초"`.
- 최종 문자열은 `string.Format` 으로 조립한다 (`$"..."` 금지):
  ```
  "■ [{0}] 사이클 종료 — 종합판정 {1} (측정 {2}개 중 {3}개 벗어남, 소요 {4}초)"
  ```
- 실제 출력은 `Logging.PrintLog((int)ELogType.Flow, szMsg)` 한 번뿐.
- **전문용어 금지어**: measurePhi, strip, transform, XLD, HTuple, ROI, phi, sigma, polarity, datum(단독 영문).

**(4) `WPF_Example/DatumMeasurement.csproj`**
- classic-style 프로젝트이므로 신규 `.cs` 는 자동 포함되지 않는다. `Utility\Logging.cs` 항목 근처에 `<Compile Include="Utility\FlowLog.cs" />` 를 추가한다.
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && S=WPF_Example/Setting/SystemSetting.cs && H=WPF_Example/SystemHandler.cs && F=WPF_Example/Utility/FlowLog.cs && P=WPF_Example/DatumMeasurement.csproj; echo "--- 신설 (각 want >=1) ---"; echo "enum_flow=$(grep -cE '^\s*Flow = 7,' "$S")"; echo "path_prop=$(grep -cF 'public string FlowLogSavePath' "$S")"; echo "path_case=$(grep -cF 'case ELogType.Flow:' "$S")"; echo "setlog=$(grep -cF 'ELogType.Flow' "$H")"; echo "flowlog_class=$(grep -cF 'public static class FlowLog' "$F")"; echo "csproj=$(grep -cF 'Utility\FlowLog.cs' "$P")"; echo "--- 최소 API: public 메서드는 CycleEnd 1개뿐 (want 1) ---"; echo "public_methods=$(grep -cE '^\s*public static (void|string|bool|int) ' "$F")"; echo "cycleend=$(grep -cF 'public static void CycleEnd(' "$F")"; echo "--- 폐기된 API 가 남아있으면 FAIL (각 want 0) ---"; for m in 'CycleBegin' 'SkipReasonToKorean' 'public static void Step(' 'public static void Measurement(' 'public static void Line('; do echo "$m=$(grep -cF "$m" "$F")"; done; echo "--- 문자열 폴백은 IsNullOrEmpty (want >=1, ==null 비교 want 0) ---"; echo "isnullorempty=$(grep -cF 'string.IsNullOrEmpty(szSequenceName)' "$F")"; echo "eqnull=$(grep -cE 'szSequenceName\s*==\s*null' "$F")"; echo "--- throw 방지 (want >=1) ---"; echo "catches=$(grep -cE 'catch' "$F")"; echo "--- 기존 enum 값 무변경 (각 want 1) ---"; for k in 'Trace = 0' 'Camera = 1' 'LightController = 2' 'TcpConnection = 3' 'Result = 4' 'Image = 5' 'Error = 6'; do echo "$k=$(grep -cF "$k" "$S")"; done; echo "--- C# 7.2 위반 (want 0) ---"; git diff -U0 -- "$S" "$H" "$P" | grep '^+' | grep -cE '[$]"|=>|switch[[:space:]]*[{]'; grep -cE '[$]"' "$F"; echo "--- 전문용어 금지 (want 0) ---"; grep -ciE 'measurePhi|XLD|HTuple|polarity|sigma' "$F"; echo "--- build ---"; "C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj //t:Build //p:Configuration=Debug //p:Platform=x64 //v:m //nologo 2>&1 | grep -E "error CS|warning CS" | grep -v -E "CS0618|CS0162" | head -20; echo "CS_LIST_ABOVE_MUST_BE_EMPTY"</automated>
  </verify>
  <done>
`ELogType.Flow = 7` 이 맨 뒤에 추가되고 기존 0~6 은 그대로. `FlowLogSavePath` + `GetLogSavePath` case + `SetLog` 등록 완료. `FlowLog` 의 public 메서드는 `CycleEnd` **1개뿐**이고 본문이 try/catch 로 보호되며 시퀀스명 폴백에 `string.IsNullOrEmpty` 사용. 폐기 API(CycleBegin/Step/Measurement/Line/SkipReasonToKorean) 0건. csproj 에 Compile Include 추가. Debug/x64 빌드에 `error CS` / 신규 `warning CS` 0건.
  </done>
</task>

<task type="auto">
  <name>Task 2: 사이클 요약 1줄 배선 + S11 진단 스캐폴딩 삭제 (InspectionSequence) — 이 작업의 본체</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <action>
D-F3W-02-REV 구현. **사이클당 Flow 로그는 정확히 1줄이어야 한다** (시작 줄 없음 — 압축 결정).

**(1) 필드 2개 추가**
```csharp
private readonly System.Diagnostics.Stopwatch _flowStopwatch = new System.Diagnostics.Stopwatch();
private bool _bFlowCycleLogged = false;   // 종료 이벤트 중복 발화 시 두 줄 나가는 것 방지
```

**(2) 사이클 시작 — `OnStart` 구독 (로그 출력 없음, 시계만 돌린다)**
- 생성자(`:101`)의 `OnStart += HandleRunStartResetResults;` **바로 아래**에 `OnStart += HandleFlowLogCycleBegin;` 를 추가한다.
  (구독 순서가 곧 발화 순서 — 결과 초기화가 먼저, 시계 리셋이 나중. 이 순서를 지킬 것.)
- `private void HandleFlowLogCycleBegin(SequenceContext context)`:
  - `_flowStopwatch.Restart();`
  - `_bFlowCycleLogged = false;`
  - 전체 `try { } catch { }`.
  - **여기서는 아무것도 출력하지 않는다.** 사이클 시작 줄은 압축 결정으로 제외됐다.

**(3) 사이클 종료 — `OnFinish` / `OnStop` / `OnError` 구독 → 요약 1줄**
- 생성자에 세 이벤트를 같은 핸들러 `HandleFlowLogCycleEnd` 로 구독한다.
- `private void HandleFlowLogCycleEnd(SequenceContext context)`:
  - 전체를 `try { } catch { }` 로 감싼다. **이 핸들러는 어떤 경우에도 예외를 밖으로 던지면 안 된다** (SequenceBase 가 이벤트 발화 실패 시 잠금 영구화 위험 — `SequenceBase.cs:402` 주석 참조).
  - `if (_bFlowCycleLogged) return;` — 중복 방지 가드. 통과하면 즉시 `_bFlowCycleLogged = true;`
  - `_flowStopwatch.Stop();`
  - `<interfaces>` 에 발췌한 `HandleRunStartResetResults`(`:226`) 순회 패턴을 **그대로 복사**해 이 시퀀스가 소유한 측정만 집계한다:
    `Actions` → `act as Action_FAIMeasurement` → `.ShotParam` → `.FAIList` → `.Measurements`
  - 집계 규칙 (전부 if/else, 삼항 금지):
    - **시도된 항목** = `m.LastHasResult == true` **또는** `string.IsNullOrEmpty(m.LastSkipReason) == false`
      (skip 처리된 항목도 "이번 사이클에 다뤄진 항목"이므로 분모에 포함한다. **`== null` 비교 금지** — plan-checker 지적 사항.)
    - `nMeasured` = 시도된 항목 수
    - `nNg` = 시도된 항목 중 `m.LastJudgement == false` 인 수
    - `bAllPass` = `nNg == 0 && nMeasured > 0`
  - `if (nMeasured == 0) return;` — 이 시퀀스가 이번 사이클에 한 일이 없으면 **출력하지 않는다** (조용).
  - `FlowLog.CycleEnd(Name, bAllPass, nMeasured, nNg, _flowStopwatch.Elapsed.TotalSeconds);`
  - 집계 루프가 30~40줄을 넘으면 `private void CountFlowCycleResults(out int nMeasured, out int nNg)` 로 분리한다.

**(4) 스레드 주의 (구현 시 참고, 코드 변경 아님)**
`OnStart` 는 호출자 스레드(UI/TCP)에서, 종료 이벤트는 시퀀스 스레드에서 발화될 수 있다. `Stopwatch` 는 thread-safe 가 아니지만 start → run → stop 이 시간순으로 분리돼 있고 **로그 전용**이라 위험이 없다. 여기에 lock 을 추가하지 말 것 (검사 스레드 타이밍에 영향 금지).

**(5) S11 삭제 — `[ALIGN-DIAG-LIVE]` 임시 진단 로그**
- `:1856-1859` 의 주석 2줄 + `HTuple diagImgW, diagImgH;` 선언 + `refImage.GetImageSize(out diagImgW, out diagImgH);` + `PrintLog` 1줄을 **전부 삭제**한다 (`diagImgW/diagImgH` 는 이 로그 전용이므로 미사용 변수 경고를 남기지 않도록 선언까지 삭제).
- `:1868-1869` 의 `[ALIGN-DIAG-LIVE] p2` 주석 1줄 + `PrintLog` 1줄을 삭제한다. **`modelPath2` 변수 자체는 바로 아래 `svc.TryFindPose(...)` 가 사용하므로 절대 삭제하지 말 것.**

**(6) S12 — 절대 유지 (CRITICAL)**
아래 3개 로그는 **한 글자도 건드리지 않는다.** 실제 부호 버그를 잡아낸 확증 로그다(커밋 a719073):
- `:1850` `[ALIGN] ... patAngDeg ... thetaDeg ... src=pattern ...`
- `:1879` `[ALIGN2] ... refBaseDeg ... curBaseDeg ... (baseline θ)`
- `:1936` `[ALIGN] ... datumDetectAngleDeg ... datumDetectRotDeg ... vs patternThetaDeg ...`
`:1888` 의 `[ALIGN2] 패턴2 매칭 실패` Error 로그도 유지한다.
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && I=WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs && A=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs && CI=$(grep -v '^[[:space:]]*//' "$I"); echo "--- 신설 (각 want >=1) ---"; echo "cycle_end=$(echo "$CI" | grep -cF 'FlowLog.CycleEnd(')"; echo "sub_start=$(echo "$CI" | grep -cF 'OnStart += HandleFlowLogCycleBegin')"; echo "sub_finish=$(echo "$CI" | grep -cF 'OnFinish += HandleFlowLogCycleEnd')"; echo "sub_stop=$(echo "$CI" | grep -cF 'OnStop += HandleFlowLogCycleEnd')"; echo "sub_error=$(echo "$CI" | grep -cF 'OnError += HandleFlowLogCycleEnd')"; echo "stopwatch=$(echo "$CI" | grep -cF '_flowStopwatch')"; echo "dup_guard=$(echo "$CI" | grep -cF '_bFlowCycleLogged')"; echo "--- 사이클당 1줄: FlowLog 호출은 총 1건, 시작줄 출력 0건 ---"; echo "flowlog_total_calls=$(echo "$CI" | grep -cF 'FlowLog.') (want 1)"; echo "cycle_begin_out=$(echo "$CI" | grep -cF 'FlowLog.CycleBegin') (want 0)"; echo "--- 폐기 확인: Action_FAIMeasurement 는 미수정 (want 0 + diff 없음) ---"; echo "action_flowlog=$(grep -cF 'FlowLog' "$A")"; git diff --name-only -- "$A"; echo "ACTION_FILE_MUST_NOT_APPEAR_ABOVE"; echo "--- 문자열 판정은 IsNullOrEmpty (want >=1, ==null want 0) ---"; echo "skip_isnullorempty=$(echo "$CI" | grep -cF 'string.IsNullOrEmpty(m.LastSkipReason)')"; echo "skip_eqnull=$(echo "$CI" | grep -cE 'LastSkipReason\s*==\s*null')"; echo "--- S11 삭제 (각 want 0) ---"; echo "diag_live=$(grep -cF 'ALIGN-DIAG-LIVE' "$I")"; echo "diagImgW=$(grep -cF 'diagImgW' "$I")"; echo "--- S12 절대 유지 (각 want 1) ---"; echo "s12_a=$(grep -cF 'src=pattern' "$I")"; echo "s12_b=$(grep -cF '(baseline θ)' "$I")"; echo "s12_c=$(grep -cF 'vs patternThetaDeg' "$I")"; echo "s12_d=$(grep -cF 'datumDetectRotDeg' "$I")"; echo "align2_err=$(grep -cF '패턴2 매칭 실패' "$I")"; echo "--- modelPath2 보존 (want >=2: 선언+사용) ---"; echo "modelPath2=$(grep -cF 'modelPath2' "$I")"; echo "--- 기존 구독 보존 (want 1) ---"; echo "reset_sub=$(echo "$CI" | grep -cF 'OnStart += HandleRunStartResetResults')"; echo "--- 검사 타이밍 영향 금지: 신규 lock 0건 (want 0) ---"; git diff -U0 -- "$I" | grep '^+' | grep -cE '\block\s*\('; echo "--- C# 7.2 위반 신규 라인 (want 0) ---"; git diff -U0 -- "$I" | grep '^+' | grep -cE '[$]"|=>|switch[[:space:]]*[{]|[?][^?:]*:'; echo "--- build ---"; "C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj //t:Build //p:Configuration=Debug //p:Platform=x64 //v:m //nologo 2>&1 | grep -E "error CS|warning CS" | grep -v -E "CS0618|CS0162" | head -20; echo "CS_LIST_ABOVE_MUST_BE_EMPTY"</automated>
  </verify>
  <done>
`FlowLog.` 호출이 이 파일에 **정확히 1건**(`CycleEnd`)이고 OnFinish/OnStop/OnError 로 배선됨. OnStart 핸들러는 시계 리셋만 하고 출력 없음. 중복 발화 가드 존재. skip 판정에 `string.IsNullOrEmpty` 사용, `== null` 비교 0건. `Action_FAIMeasurement.cs` 는 diff 에 나타나지 않음. `[ALIGN-DIAG-LIVE]` 2줄 + `diagImgW/diagImgH` 전용 코드 삭제, S12 확증 로그 3줄 존재, `modelPath2` 보존. 신규 lock 0건. 빌드 PASS.
  </done>
</task>

<task type="auto">
  <name>Task 3: S1 축약 + S2 dedup 가드 — strip 반복 로그를 ROI 요약 1줄로 (DatumFindingService)</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <action>
D-F3W-03 의 **S1 과 S2 를 함께** 처리한다. 둘은 한 몸이다 — S1 이 요약줄에 `failed` 카운트를 만드는 순간 S2 조건(예외 메시지 첫 1건만 남기고 dedup)이 발동하므로, S1 만 하고 S2 를 빼면 **자기가 만든 조건을 자기가 어기는 모순**이 된다 (CONTEXT.md D-F3W-03 명시).
**S3/S4/S6/S7/S8/S9/S10 은 이번 범위에서 제외** — `<scope_coverage>` 의 제외 표 참조. 해당 로그들은 **손대지 않는다.**

**변경 원칙: 로그 문자열과 로그 조건만 바꾼다. 측정 계산식·파라미터·반환값·예외 처리 정책은 한 글자도 바꾸지 않는다.**

참조 구현: `VisionAlgorithmService.cs:125-184` 의 okStrips/noEdgeStrips/failedStrips 집계 패턴 (위 `<interfaces>` 에 발췌).

**(1) [S1] strip 결과를 호출부로 올린다**
- `DatumFindingService` 안에 private nested enum 추가:
  `private enum EStripOutcome { Ok, NoEdge, Failed }`
- `AppendEdgePointsFromStrip`(`:2139`) 의 반환형을 `void` → `EStripOutcome` 로 바꾼다. **파라미터 목록은 건드리지 않는다** (호출부 4곳의 인자가 그대로 유지되도록 — 반환값을 받는 것만 달라진다).
  - `:2182-2185` 의 per-strip `[Datum.{0}] strip MeasurePos: dir=... measurePhi=... sel=... edges=...` PrintLog 를 **삭제**한다. **이 한 줄이 S1 의 전부다** (사이클당 40~240줄의 원인).
  - edge 0건(`edgeRows.TupleLength() <= 0 || edgeCols.TupleLength() <= 0`) → 기존 `return;` 을 `return EStripOutcome.NoEdge;` 로.
  - 정상 → `TupleConcat` 2회 후 `return EStripOutcome.Ok;`
  - `catch (Exception ex)` 블록 끝 → `return EStripOutcome.Failed;` (아래 (3)에서 이 블록을 다시 다룬다)
  - `finally` 의 `CloseMeasure`/`Dispose` 정리는 무변경.

**(2) [S1] 호출부 2개 메서드에서 집계 → 기존 요약줄에 병합**
호출부는 미러 쌍이다: `TryFindLine` 의 strip 루프(`:1783`, `:1799`)와 `TryExtractEdgePoints` 의 미러 루프(`:2039`, `:2055`). **두 메서드 모두 동일하게** 처리한다.
- 루프 앞에 `int nOkStrips = 0; int nNoEdgeStrips = 0; int nFailedStrips = 0;` 선언.
- 각 `AppendEdgePointsFromStrip(...)` 호출의 반환값을 받아 if/else 로 카운트 (삼항 금지, VisionAlgorithmService 패턴 그대로).
- **기존 accumulated 요약줄에 병합한다** — 새 줄을 추가하지 말고 기존 줄의 포맷 문자열만 확장한다:
  - `TryFindLine` `:1811-1813` 현재:
    `"[Datum.{0}] strip-loop accumulated {1} edge points across {2} strips"`
    → 다음으로 교체:
    `"[Datum.{0}] strip-loop accumulated {1} edge points across {2} strips (ok {3}, noEdge {4}, failed {5})"`
    (인자: lbl, edgeCount, stripCount, nOkStrips, nNoEdgeStrips, nFailedStrips)
  - `TryExtractEdgePoints` `:2067-2069` 의 `strip-loop(extract) accumulated ...` 도 **동일한 방식**으로 확장 (`(extract)` 표기 유지 — 두 경로 구분용).
- **이 줄들의 위치·출력 조건은 바꾸지 않는다.** `:1677`/`:1950` 의 bounds 로그, `:1820`/`:2076` 의 trimmed 로그, `:1762`/`:2018` 의 erosion tact 로그는 **전부 그대로 둔다** (S6/S7/S8/S9 = 이번 범위 제외).
- `edgeCount < 2` 실패 게이트(`:1825-1833`)의 error 문자열 / `PrintLog` / `return false` 는 **무변경**.

**(3) [S2] 같은 ROI 안에서 strip 예외 메시지는 첫 1건만 — dedup 가드**
위 (2)로 요약줄에 `failed {5}` 카운트가 생겼으므로, `:2195-2205` catch 의 `[Datum.{0}] strip swallowed: {msg}` 는 **첫 1건만 남기고** 나머지는 그 카운트로만 집계되게 한다 (같은 ROI 에서 20 strip 이 전부 실패하면 지금은 20줄 → 가드 후 1줄 + `failed 20`).

- 인스턴스 필드 1개 추가:
  ```csharp
  // 같은 ROI 안에서 strip 예외 메시지를 첫 1건만 남기기 위한 가드.
  // 실패 개수는 요약줄의 failed 카운트가 담당하므로 같은 메시지를 20번 찍을 이유가 없다.
  private bool _bStripErrorLoggedThisRoi = false;
  ```
  → **인스턴스 필드가 안전한 근거:** `DatumFindingService` 는 매 검출마다 `new` 로 생성되고(`InspectionSequence.cs:1910`, `:1958` 2곳뿐, static 인스턴스 없음), `AlignPreTransform` 도 이미 인스턴스 상태로 쓰인다 → 스레드 공유 없음.
  → **`static` 으로 만들지 말 것.** 시퀀스 3개(Top/Side/Bottom)가 동시에 돌면 서로의 가드를 덮어써 다른 ROI 의 첫 예외가 통째로 사라진다.
- **리셋 지점 2곳:** `TryFindLine` 과 `TryExtractEdgePoints` 각각에서 strip 루프 진입 **직전**(위 (2)의 `nOkStrips` 선언과 같은 자리)에 `_bStripErrorLoggedThisRoi = false;` 를 넣는다. 이러면 가드 범위가 정확히 **ROI 1개**가 된다.
- **catch 블록**은 아래 구조로 바꾼다. **로그 문자열 포맷(`[Datum.{0}] strip swallowed: {1}`)은 그대로 유지**하고 출력 조건만 추가한다:
  ```csharp
  catch (Exception ex)
  {
      if (!_bStripErrorLoggedThisRoi)
      {
          _bStripErrorLoggedThisRoi = true;
          try
          {
              string lblCatch = "?";
              if (roiLabel != null) lblCatch = roiLabel;
              Logging.PrintLog((int)ELogType.Trace,
                  string.Format("[Datum.{0}] strip swallowed: {1}", lblCatch, ex.Message));
          }
          catch { }
      }
      return EStripOutcome.Failed;
  }
  ```
- **예외를 삼키는 정책 자체는 그대로 유지한다** — strip 하나가 실패해도 루프는 계속 돈다. 바뀌는 건 "몇 줄을 찍느냐"뿐이고 검출 동작·결과는 동일하다.

**무변경 확인 대상:** `HOperatorSet.*` 호출, `ComputeMeasurePhi`, sigma/threshold/polarity/selection 전달, `SortAndTrimPercent` 호출, 모든 반환값, strip 루프의 반복 횟수.
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && D=WPF_Example/Halcon/Algorithms/DatumFindingService.cs && V=WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs && E=WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs; echo "--- S1: per-strip 로그 제거 (want 0) ---"; echo "strip_measurepos=$(grep -cF 'strip MeasurePos' "$D")"; echo "--- S1: 집계 신설 (각 want >=1) ---"; echo "enum=$(grep -cF 'private enum EStripOutcome' "$D")"; echo "ok_counter=$(grep -cF 'nOkStrips' "$D")"; echo "noedge_ret=$(grep -cF 'EStripOutcome.NoEdge' "$D")"; echo "failed_ret=$(grep -cF 'EStripOutcome.Failed' "$D")"; echo "merged_summary=$(grep -cF 'noEdge {4}, failed {5}' "$D") (want 2: TryFindLine + extract 미러)"; echo "--- S2: dedup 가드 ---"; echo "guard_refs=$(grep -cF '_bStripErrorLoggedThisRoi' "$D") (want >=4)"; echo "guard_reset=$(grep -cF '_bStripErrorLoggedThisRoi = false' "$D") (want 3: 선언 초기화 + ROI 리셋 2곳)"; echo "guard_check=$(grep -cF 'if (!_bStripErrorLoggedThisRoi)' "$D") (want 1)"; echo "guard_not_static=$(grep -cE 'static\s+bool\s+_bStripErrorLoggedThisRoi' "$D") (want 0 — static 금지)"; echo "swallow_log_kept=$(grep -cF 'strip swallowed' "$D") (want 1 — 포맷 유지, 조건만 추가)"; echo "--- 범위 제외 항목은 그대로 있어야 함 ---"; echo "S6_bounds=$(grep -cF 'strip-loop: bounds' "$D") (want 1)"; echo "S6_bounds_ex=$(grep -cF 'strip-loop(extract): bounds' "$D") (want 1)"; echo "S8_trimmed=$(grep -cF 'trimmed {1}%' "$D") (want 2)"; echo "S9_tact=$(grep -cF 'tact = {2}ms' "$D") (want 2)"; echo "--- 미수정 파일 확인 (아래 diff 목록은 비어 있어야 함) ---"; git diff --name-only -- "$V" "$E"; echo "VISION_AND_FAIEDGE_MUST_BE_EMPTY_ABOVE"; echo "--- 알고리즘 무변경: 호출 수 (변경 전과 동일해야 함) ---"; echo "D_hop=$(grep -cF 'HOperatorSet.' "$D")"; echo "append_refs=$(grep -cF 'AppendEdgePointsFromStrip(' "$D") (want 5: 선언 1 + 호출 4)"; echo "sortandtrim=$(grep -cF 'SortAndTrimPercent' "$D") (want 2)"; echo "computephi=$(grep -cF 'ComputeMeasurePhi(' "$D")"; echo "--- C# 7.2 위반 신규 라인 (want 0) ---"; git diff -U0 -- "$D" | grep '^+' | grep -cE '[$]"|=>|switch[[:space:]]*[{]|[?][^?:]*:'; echo "--- build ---"; "C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj //t:Build //p:Configuration=Debug //p:Platform=x64 //v:m //nologo 2>&1 | grep -E "error CS|warning CS" | grep -v -E "CS0618|CS0162" | head -20; echo "CS_LIST_ABOVE_MUST_BE_EMPTY"</automated>
  </verify>
  <done>
strip 단위 `strip MeasurePos` 로그 0건. `EStripOutcome` 집계가 미러 2곳 모두에서 기존 accumulated 요약줄에 병합됨(`ok/noEdge/failed`). `_bStripErrorLoggedThisRoi` 가 **인스턴스 필드**(static 아님)로 선언되고 ROI 루프 진입 직전 2곳에서 리셋되며, catch 는 첫 1건만 로그. `strip swallowed` 포맷 문자열 자체는 보존. 범위 제외 로그(S6/S8/S9)는 그대로 존재. `VisionAlgorithmService.cs` / `FAIEdgeMeasurementService.cs` 는 diff 에 없음. `HOperatorSet.*` / `SortAndTrimPercent` / `AppendEdgePointsFromStrip` 참조 수 변경 전과 동일. 빌드 PASS.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 압축 요약 로그 육안 검증</name>
  <what-built>
신규 `Flow` 로그 카테고리 + **검사 사이클 1회당 요약 1줄**(시퀀스명 / 종합판정 / 측정개수 / NG개수 / 소요시간). 부수적으로 strip 단위 반복 Trace 로그(S1) 축약 + 같은 ROI 예외 로그 dedup(S2) + `[ALIGN-DIAG-LIVE]` 임시 진단 로그 삭제(S11).
  </what-built>
  <how-to-verify>
1. `bin/x64/Debug/DatumMeasurement.exe` 실행 (빌드 시 exe 파일잠금 방지를 위해 기존 프로세스는 미리 종료).
2. 로그 창 메뉴에 **`Flow`** 항목이 새로 보이는지 확인 → 열어 둔다.
3. RUN(또는 일괄검사)로 검사 **2~3 사이클** 실행.
4. **Flow 창** 확인 — 합격 기준은 이게 전부다:
   - [ ] 사이클 1회당 **딱 1줄**만 나오는가? (2줄 이상이면 FAIL)
   - [ ] 그 줄에 **시퀀스명**(예: `[SIDE]`)이 있는가?
   - [ ] **종합판정 OK/NG** 가 있는가?
   - [ ] **측정 개수 / 벗어난 개수** 가 있는가?
   - [ ] **소요시간(초)** 이 있고, 실제 체감 시간과 얼추 맞는가?
   - [ ] `measurePhi`, `strip`, `transform`, `XLD`, `HTuple` 같은 전문용어가 하나도 없는가?
   - [ ] 여러 시퀀스(Top/Side/Bottom)를 돌렸을 때 어느 줄이 어느 시퀀스인지 구분되는가?
5. **Trace 창** — 세 가지 확인:
   - [ ] `strip MeasurePos` 반복 줄이 사라졌는가?
   - [ ] **[S2] 검출이 실패한 ROI 가 있었다면**, 그 ROI 의 `strip swallowed:` 줄이 **1줄만** 나오는가? (예전엔 실패한 strip 개수만큼 반복해서 나왔다. 실패 총 개수는 같은 ROI 의 `strip-loop accumulated ... (ok N, noEdge N, failed N)` 요약줄의 `failed` 값으로 확인된다.)
     — 이번 실행에서 실패 ROI 가 아예 없었다면 이 항목은 "해당 없음"으로 넘어간다.
   - [ ] `[ALIGN] ... datumDetectRotDeg ... vs patternThetaDeg ...` 줄은 **여전히 있는가?** (없으면 FAIL — 즉시 중단)
6. **회귀 확인:** 같은 레시피/같은 이미지로 검사했을 때 측정값과 합불 판정이 이 작업 **전과 동일**한가?
  </how-to-verify>
  <resume-signal>"승인" 이라고 입력하거나, 줄에서 빠진 정보 / 숫자가 이상한 지점을 구체적으로 알려주세요.</resume-signal>
</task>

</tasks>

<verification>
- Debug/x64 빌드: `error CS` 0건, 신규 `warning CS` 0건
- `git diff --name-only` 결과가 위 `files_modified` 목록을 벗어나지 않을 것 — 특히 `Action_FAIMeasurement.cs` / `VisionAlgorithmService.cs` / `FAIEdgeMeasurementService.cs` 가 나타나면 범위 위반
- `FlowLog.cs` 의 public 메서드는 `CycleEnd` 1개뿐 (폐기 API 잔존 0건)
- 문자열 폴백/판정에 `string.IsNullOrEmpty` 사용, `== null` 단독 비교 0건 (plan-checker 지적 사항)
- `InspectionSequence.cs` 의 `FlowLog.` 호출 수 = 1 (사이클당 1줄 보증)
- **S1↔S2 정합:** 요약줄에 `failed` 카운트가 생겼으면 `_bStripErrorLoggedThisRoi` 가드도 반드시 함께 존재 — 둘 중 하나만 있으면 FAIL
- `_bStripErrorLoggedThisRoi` 는 **인스턴스 필드** (static 선언 0건)
- `ELogType` 의 기존 정수값(0~6) 무변경 — grep 으로 7개 항목 모두 확인
- `InspectionSequence.cs` 의 S12 확증 로그 3줄 존재 — grep (`src=pattern`, `(baseline θ)`, `vs patternThetaDeg`)
- 범위 제외 로그(S6/S8/S9)가 `DatumFindingService.cs` 에 그대로 존재 — grep
- `HOperatorSet.` 호출 수 / `SortAndTrimPercent` 호출 수 / `AppendEdgePointsFromStrip` 참조 수가 변경 전과 동일 (알고리즘 무변경 증명)
- 검사 스레드에 신규 `lock` 0건
</verification>

<success_criteria>
1. 로그 창 메뉴에 `Flow` 카테고리가 뜨고, `D:\Data\Flow` 에 파일이 쌓인다.
2. **검사 사이클 1회당 Flow 로그 1줄에 시퀀스명 + 종합판정 + 측정/NG 개수 + 소요시간이 모두 담긴다** (Task 4 사용자 승인). ← 이 작업의 핵심
3. Trace 에서 strip 단위 반복 줄(사이클당 40~240줄)이 사라진다.
4. 같은 ROI 의 strip 예외 로그가 첫 1건으로 줄고, 실패 총 개수는 요약줄의 `failed` 카운트로 보존된다.
5. `[ALIGN] datumDetectRotDeg vs patternThetaDeg` 확증 로그가 살아 있다.
6. 측정값·판정·제어 흐름 회귀 0.
</success_criteria>

<output>
완료 후 `.planning/quick/260805-f3w-flow-log/260805-f3w-SUMMARY.md` 를 생성한다. 아래 2건을 carry-over 로 한 줄씩 기록한다:
- 폐기된 단계별 상세 흐름 로그(D-F3W-02) — 필요해지면 재검토
- 범위 제외한 스팸 항목 S3/S4/S6/S7/S8/S9/S10 (S2 는 이번에 S1 과 함께 처리 완료)
</output>
</content>
