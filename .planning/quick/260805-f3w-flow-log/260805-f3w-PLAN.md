---
phase: quick-260805-f3w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Setting/SystemSetting.cs
  - WPF_Example/SystemHandler.cs
  - WPF_Example/Utility/FlowLog.cs
  - WPF_Example/DatumMeasurement.csproj
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Halcon/Algorithms/DatumFindingService.cs
autonomous: false
requirements: [D-F3W-01, D-F3W-02, D-F3W-03-S1, D-F3W-03-S11, D-F3W-03-S12]

must_haves:
  truths:
    - "새 로그 카테고리 Flow 가 로그 뷰어 메뉴에 자동으로 뜨고, 파일로도 저장된다"
    - "검사 한 사이클의 흐름 로그를 위에서 아래로 읽으면 시작 → 기준선 찾기 → 촬영 → 측정 → 종합판정 순서로 읽힌다"
    - "측정 항목마다 측정명/측정값/기준/허용범위/합불이 한 줄로 보인다"
    - "건너뛴 항목은 이유가 쉬운 한국어 문장으로 보인다 (DATUM_FAIL 같은 영문 상수 노출 없음)"
    - "흐름 로그에 measurePhi/strip/transform/XLD/HTuple 같은 전문용어가 없다"
    - "Trace 로그에서 strip 단위 반복 출력(사이클당 40~240줄)이 사라지고 ROI 단위 요약 1줄로 대체된다"
    - "[ALIGN] datumDetectRotDeg vs patternThetaDeg 확증 로그는 그대로 남아 있다"
    - "측정 결과값/판정/제어 흐름은 이전과 동일하다 (로그만 변경)"
  artifacts:
    - path: "WPF_Example/Utility/FlowLog.cs"
      provides: "흐름 로그 단일 진입점 + skip 사유 한국어 매핑. 절대 throw 하지 않음"
      contains: "public static class FlowLog"
    - path: "WPF_Example/Setting/SystemSetting.cs"
      provides: "ELogType.Flow = 7 (append) + FlowLogSavePath + GetLogSavePath case"
      contains: "Flow = 7"
    - path: "WPF_Example/SystemHandler.cs"
      provides: "Logging.SetLog((int)ELogType.Flow, ...) 등록"
      contains: "ELogType.Flow"
  key_links:
    - from: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      to: "WPF_Example/Utility/FlowLog.cs"
      via: "EStep.DatumPhase / Grab / Measure 단계별 FlowLog 호출"
      pattern: "FlowLog\\."
    - from: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      to: "WPF_Example/Utility/FlowLog.cs"
      via: "OnStart / OnFinish / OnStop / OnError 구독 → 사이클 시작·종료 요약"
      pattern: "FlowLog\\."
    - from: "WPF_Example/Utility/FlowLog.cs"
      to: "WPF_Example/Utility/Logging.cs"
      via: "Logging.PrintLog((int)ELogType.Flow, msg)"
      pattern: "ELogType\\.Flow"
---

<objective>
**검사 시퀀스가 순서대로 읽히는 흐름 로그를 신설한다.** 이게 이 작업의 전부다.

로그 정리는 "흐름 로그가 파묻히지 않을 만큼"만 한다 — 사이클당 40~240줄을 쏟아내는 S1(strip 단위 반복 로그) 하나만 요약하고, ROI당 1~2줄 수준의 나머지 스팸은 이번 범위에서 제외한다.

Purpose: 이 시스템을 처음 보는 사람이 로그만 읽고 "지금 무슨 단계를 하고 있고 무엇이 나왔는지" 이해할 수 있어야 한다.
Output: 신규 `ELogType.Flow` 카테고리 + `FlowLog` 헬퍼 + 3개 검사 단계 지점의 흐름 로그 + 사이클 시작/종료 요약 + S1 축약 + S11 삭제.
</objective>

<scope_coverage>
## Source Coverage (CONTEXT.md D-F3W-01/02/03)

| 항목 | 출처 | 상태 | 위치 |
|------|------|------|------|
| 흐름 로그 카테고리 신설 (ELogType 뒤에 append) | D-F3W-01 | COVERED | Task 1 |
| SetLog 등록 / 로그 창 카테고리 분리 | D-F3W-01 | COVERED | Task 1 (LogViewWindow 자동 생성 — UI 작업 불필요) |
| 단계 전환 1줄 (기준선 → 촬영 → 측정) | D-F3W-02 | COVERED | Task 2 |
| 측정 항목마다 1줄 (측정명/값/기준/공차/합불) | D-F3W-02 | COVERED | Task 2 |
| 사이클 시작/종료 요약 1줄 | D-F3W-02 | COVERED | Task 3 |
| SkipReason → 쉬운 한국어 매핑 | D-F3W-02 | COVERED | Task 1 (`FlowLog.SkipReasonToKorean`) |
| 전문용어 금지 | D-F3W-02 | COVERED | Task 1 출력 규약 + Task 2/5 검증 |
| **S1** strip 루프 최내곽 (사이클당 40~240줄) | D-F3W-03 | COVERED | Task 4 |
| **S11** `[ALIGN-DIAG-LIVE]` 삭제 | D-F3W-03 | COVERED | Task 3 |
| **S12** `[ALIGN] datumDetectRotDeg vs patternThetaDeg` 절대 유지 | D-F3W-03 | COVERED (보존 검증) | Task 3 verify |
| S5 `low strip coverage` (조건부 Error, 정상 시 무출력) | D-F3W-03 | 무변경 (원래 "유지" 결정) | — |
| `[FAI CrossZ IMG]` (Trace 유지) | D-F3W-03 | 무변경 | — |

### 이번 범위 제외 — 필요해지면 별도 작업

사용자 지시(2026-08-05): *"필요한 것들만" / "중요한건 시퀀스야"*.
근거: S1 혼자 사이클당 40~240줄로 전체 스팸의 대부분이고, 아래 항목들은 **ROI당 1~2줄 수준**이라 흐름 로그를 가리지 않는다. 조사 결과는 기록으로 남겨두되 이번엔 손대지 않는다.

| 건 | 위치 | 원래 계획했던 조치 | 제외 사유 |
|----|------|-------------------|-----------|
| S2 | `DatumFindingService.cs:2202` (strip catch) | 예외 메시지 첫 1건만 dedup | 예외 발생 시에만 출력 — 정상 사이클엔 조용 |
| S3+S4 | `VisionAlgorithmService.cs:182`, `214` | 연속 2줄 → 1줄 병합 | 호출당 2줄, 흐름 로그를 가리지 않음 |
| S6+S7+S8 | `DatumFindingService.cs:1677`/`1811`/`1820` 및 미러(`1950`/`2067`/`2076`) | 3줄 → 1줄 병합 | ROI당 3줄. S1 제거만으로 충분히 조용해짐 |
| S9 | `DatumFindingService.cs:1762`, `2018` (erosion tact) | 임계(50ms) 초과 시만 출력 | ROI당 최대 1줄, erosion 미사용 시 0줄 |
| S10 | `FAIEdgeMeasurementService.cs:76` (`[ALIGN-ROI]`) | Shot 단위 1줄로 dedup | align 사용 시에만, 측정당 1줄 |

→ 위 항목들 때문에 `VisionAlgorithmService.cs` 와 `FAIEdgeMeasurementService.cs` 는 **이번에 수정하지 않는다** (VisionAlgorithmService 는 S1 구현의 참조 패턴으로 읽기만 함).
</scope_coverage>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/quick/260805-f3w-flow-log/260805-f3w-CONTEXT.md

@WPF_Example/Setting/SystemSetting.cs
@WPF_Example/Utility/Logging.cs
@WPF_Example/Custom/Sequence/Inspection/SkipReason.cs
@WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
@WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
@WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
@WPF_Example/Halcon/Algorithms/DatumFindingService.cs

<!-- 읽기 전용 참조: S1 집계 패턴의 정본. 이 파일은 수정하지 않는다. -->
@WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs

<coding_rules>
프로젝트 상시 코딩 규칙 (하위 에이전트 포함 전원 준수):
- **C# 7.2 전용.** `$"..."` 보간 문자열, `=>` 식 본문 멤버, `switch` 식, `record`, nullable 참조형식 **금지**. 문자열은 `string.Format` 또는 `+` 연결.
- **삼항 연산자 `?:` 금지 → 반드시 `if/else`.** `??` 도 쓰지 말고 임시변수 + null 체크로 푼다.
- 헝가리언 접두(신규 지역변수): bool `b`, int `n`, string `sz`.
- 날짜 주석(`//YYMMDD hbk`) 규칙은 폐기됨. 비자명한 "왜"만 최소 주석.
- 함수는 30~40줄 이내로 유지, 초보자가 읽어 이해되게.
- **회귀 0.** 로그 변경이 측정값/판정/제어 흐름/타이밍을 절대 바꾸면 안 된다.
</coding_rules>

<interfaces>
<!-- 실행자가 코드베이스를 헤맬 필요 없도록 필요한 계약을 여기 박아둔다. -->

WPF_Example/Setting/SystemSetting.cs:18-26 (현재)
```csharp
public enum ELogType : int {
    Trace = 0, Camera = 1, LightController = 2, TcpConnection = 3,
    Result = 4, Image = 5, Error = 6,
}
```
※ `LogViewWindow.xaml.cs:36` 이 `Enum.GetNames(typeof(ELogType))` 로 메뉴를 자동 생성하므로 enum 에 추가만 하면 로그 창 메뉴가 자동으로 생긴다. UI 작업 불필요.
※ `GetLogSavePath(ELogType type, params string[] subDirs)` 는 SystemSetting.cs:210 의 switch. case 미추가 시 basePath = 실행폴더로 폴백(크래시 없음)이나 반드시 case 를 추가한다.

WPF_Example/Utility/Logging.cs
```csharp
public static bool SetLog(int id, string name, string savePath = null, Control listView = null);
public static bool PrintLog(int logID, string msg);   // 등록 안 된 id → 조용히 false, throw 없음
```

WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
```csharp
public string MeasurementName { get; }      // 비어있을 수 있음 → 비면 TypeName 사용
public abstract string TypeName { get; }
public double NominalValue { get; set; }
public double TolerancePlus { get; set; }
public double ToleranceMinus { get; set; }
public double LastMeasuredValue { get; set; }  // 휘발성
public bool   LastJudgement { get; set; }      // true = OK
public bool   LastHasResult { get; set; }      // false = 이번에 측정 안 됨
public string LastSkipReason { get; set; }     // SkipReason 상수 또는 null
public void ClearResult();                     // 위 Last* 를 0/false/null 로 복원
```

WPF_Example/Custom/Sequence/Inspection/SkipReason.cs (값 변경 금지 — 와이어/CSV 계약)
```
DATUM_FAIL, ALIGN_FAIL, NO_IMAGE, DATUM_REF_MISSING, ZINDEX_MISCONFIGURED, CROSS_Z_INCOMPLETE
```

WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
```csharp
public void MarkDatumFailed(string datumName);          // :1625
public bool IsDatumFailed(string datumRef);             // :1632
public void MarkAlignFailed(string datumName);          // :1655
public bool IsAlignFailed(string datumRef);             // :1665
public List<DatumConfig> DatumConfigs { get; }
// SequenceBase 이벤트: OnStart / OnFinish / OnStop / OnError  (모두 EventSequenceStateChanged(SequenceContext))
// 생성자 :101 에 이미 `OnStart += HandleRunStartResetResults;` 구독 선례가 있다.
// :226 HandleRunStartResetResults 가 Actions → Action_FAIMeasurement.ShotParam → FAIList → Measurements
//   순회 패턴의 정본이다. 사이클 요약 집계도 이 패턴을 그대로 복사한다(이 시퀀스 소유 Shot 만 세짐).
```

WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
```csharp
public string DatumName { get; }               // :19
public bool   RuntimeDetectFailed { get; set; } // :712
public double DetectedAngleDeg { get; set; }    // :860
```

WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
```csharp
public string ShotName { get; set; }
public List<FAIConfig> FAIList { get; }
```
FAIConfig: `public string FAIName`, `public List<MeasurementBase> Measurements`

WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs:125-184 (S1 집계 패턴의 정본 — 읽기 전용)
```csharp
int okStrips = 0; int noEdgeStrips = 0; int failedStrips = 0;
for (...) {
    EStripResult sr = AppendStrip(...);
    if (sr == EStripResult.Ok) { okStrips++; }
    else if (sr == EStripResult.NoEdge) { noEdgeStrips++; }
    else { failedStrips++; }
}
// 루프 종료 후 요약 1줄
Logging.PrintLog((int)ELogType.Trace,
    string.Format("[FitLine] strips ok {0}/{1} (noEdge {2}, failed {3}) -> {4} edge points",
        okStrips, stripCount, noEdgeStrips, failedStrips, edgeCount));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 흐름 로그 카테고리 + FlowLog 헬퍼 신설</name>
  <files>
    WPF_Example/Setting/SystemSetting.cs,
    WPF_Example/SystemHandler.cs,
    WPF_Example/Utility/FlowLog.cs (신규),
    WPF_Example/DatumMeasurement.csproj
  </files>
  <action>
D-F3W-01 구현. 흐름 로그의 인프라만 만든다 (아직 호출부 없음). **꼭 필요한 최소로 — 아래 명시된 것 외에 기능을 추가하지 말 것.**

**(1) `WPF_Example/Setting/SystemSetting.cs`**
- `ELogType` 에 `Flow = 7,` 을 **맨 뒤에 추가**한다. 기존 0~6 값은 절대 재번호하지 않는다 (로그 파일 경로/영속 설정 하위호환).
- 로그 경로 프로퍼티 그룹(`:58-80`) 에 아래를 추가한다. **어트리뷰트는 인접 멤버 1개에만 적용되므로 반드시 자기 어트리뷰트를 붙인다** (과거 IN-01 결함):
  ```csharp
  [DirectoryPath]
  [AutoUpdateText]
  public string FlowLogSavePath { get; set; } = @"D:\Data\Flow";
  ```
- `GetLogSavePath` switch(`:212`) 에 `case ELogType.Flow: basePath = FlowLogSavePath; break;` 추가.

**(2) `WPF_Example/SystemHandler.cs:88-93`**
- 기존 등록 블록과 **완전히 같은 패턴**으로 한 줄 추가 (`Logging.Start()` 호출 전):
  ```csharp
  Logging.SetLog((int)ELogType.Flow, Enum.GetName(typeof(ELogType), ELogType.Flow), Setting.GetLogSavePath(ELogType.Flow));
  ```

**(3) `WPF_Example/Utility/FlowLog.cs` (신규 파일, namespace `ReringProject.Utility`)**
흐름 로그의 **단일 진입점**. 요구: 절대 throw 하지 않는다 (모든 public 메서드 본문을 `try { ... } catch { }` 로 감싼다 — 검사 스레드 보호). 아래 6개 메서드가 전부다.

```csharp
public static class FlowLog
{
    // 흐름 로그 한 줄. nIndentLevel 0=최상위, 1=단계, 2=항목.
    public static void Line(int nIndentLevel, string szText);

    // 사이클 시작:  "▶ 검사 시작 — {szSequenceName}"
    public static void CycleBegin(string szSequenceName);

    // 사이클 종료:  "■ 검사 끝 — 종합판정 {OK|NG} (측정 30개 중 6개 벗어남, 소요 4.2초)"
    public static void CycleEnd(string szSequenceName, bool bAllPass, int nMeasured, int nNg, double dElapsedSec);

    // 단계 머리글: "  [{nStepNo}단계] {szTitle}"
    public static void Step(int nStepNo, string szTitle);

    // 측정 한 줄:  "      → RD = 92.524  (기준 90.000, 허용 +0.500/-1.500)  ✗ 벗어남"
    //  bHasResult=false 이면 값 대신 SkipReasonToKorean(szSkipReason) 문장을 출력한다.
    public static void Measurement(string szName, bool bHasResult, double dValue,
                                   double dNominal, double dTolPlus, double dTolMinus,
                                   bool bPass, string szSkipReason);

    // SkipReason 상수 → 초보자가 읽는 한국어 문장. 아래 표 그대로.
    public static string SkipReasonToKorean(string szSkipReason);
}
```

`SkipReasonToKorean` 매핑 (if/else 체인, `switch` 식은 금지):
| 상수 | 출력 문장 |
|------|-----------|
| `DATUM_FAIL` | `"기준선을 못 찾아서 이 항목은 건너뜀"` |
| `ALIGN_FAIL` | `"제품 위치를 못 찾아서 이 항목은 건너뜀"` |
| `NO_IMAGE` | `"사진이 없어서 이 항목은 건너뜀"` |
| `DATUM_REF_MISSING` | `"레시피에 지정된 기준선 이름이 없어서 건너뜀"` |
| `ZINDEX_MISCONFIGURED` | `"높이(Z) 설정이 잘못되어 건너뜀"` |
| `CROSS_Z_INCOMPLETE` | `"두 높이에서 찍은 사진 중 한쪽이 없어 측정 못 함"` |
| null/빈 문자열 | `"이번에는 측정하지 않음"` |
| 그 외 알 수 없는 값 | `"측정 실패"` (원본 영문 상수를 흐름 로그에 노출하지 말 것) |

출력 규약 (전부 이 파일 안에서만 결정된다):
- 들여쓰기: 레벨당 공백 4칸.
- 판정 표기: OK = `✓ 합격`, NG = `✗ 벗어남`. (P/F/O/X 같은 코드 문자 금지)
- 수치: 측정값/기준/공차 모두 `ToString("F3")`. `string.Format` 사용.
- **전문용어 금지어**: measurePhi, strip, transform, XLD, HTuple, ROI, phi, sigma, polarity, datum(단독 영문). "기준선(Datum)" 처럼 한국어 병기는 허용.
- 실제 출력은 `Logging.PrintLog((int)ELogType.Flow, szMsg)` 한 곳으로만 나간다.

**(4) `WPF_Example/DatumMeasurement.csproj`**
- classic-style 프로젝트이므로 신규 `.cs` 는 자동 포함되지 않는다. `Utility\Logging.cs` 항목 근처에 `<Compile Include="Utility\FlowLog.cs" />` 를 추가한다.
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && S=WPF_Example/Setting/SystemSetting.cs && H=WPF_Example/SystemHandler.cs && F=WPF_Example/Utility/FlowLog.cs && P=WPF_Example/DatumMeasurement.csproj; echo "--- 신설 (각 want >=1) ---"; echo "enum_flow=$(grep -cE '^\s*Flow = 7,' "$S")"; echo "path_prop=$(grep -cF 'public string FlowLogSavePath' "$S")"; echo "path_case=$(grep -cF 'case ELogType.Flow:' "$S")"; echo "setlog=$(grep -cF 'ELogType.Flow' "$H")"; echo "flowlog_class=$(grep -cF 'public static class FlowLog' "$F")"; echo "csproj=$(grep -cF 'Utility\FlowLog.cs' "$P")"; echo "--- 기존 enum 값 무변경 (각 want 1) ---"; for k in 'Trace = 0' 'Camera = 1' 'LightController = 2' 'TcpConnection = 3' 'Result = 4' 'Image = 5' 'Error = 6'; do echo "$k=$(grep -cF "$k" "$S")"; done; echo "--- FlowLog 는 throw 하면 안 됨: catch 개수 (want >=6) ---"; echo "catches=$(grep -cE 'catch' "$F")"; echo "--- C# 7.2 위반 신규 라인 (want 0) ---"; git diff -U0 -- "$S" "$H" "$P" | grep '^+' | grep -cE '[$]"|=>|switch[[:space:]]*[{]'; grep -cE '[$]"' "$F"; echo "--- 흐름 로그 전문용어 금지 (want 0) ---"; grep -ciE 'measurePhi|XLD|HTuple|polarity|sigma' "$F"; echo "--- build ---"; "C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj //t:Build //p:Configuration=Debug //p:Platform=x64 //v:m //nologo 2>&1 | grep -E "error CS|warning CS" | grep -v -E "CS0618|CS0162" | head -20; echo "CS_LIST_ABOVE_MUST_BE_EMPTY"</automated>
  </verify>
  <done>
`ELogType.Flow = 7` 이 맨 뒤에 추가되고 기존 0~6 은 그대로. `FlowLogSavePath` + `GetLogSavePath` case + `SetLog` 등록 완료. `FlowLog` 클래스가 위 6개 public 메서드를 갖고 모든 본문이 try/catch 로 보호됨. csproj 에 Compile Include 추가. Debug/x64 빌드에 `error CS` / 신규 `warning CS` 0건.
  </done>
</task>

<task type="auto">
  <name>Task 2: 검사 단계별 흐름 로그 심기 (Action_FAIMeasurement) — 이 작업의 본체</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <action>
D-F3W-02 구현. `Action_FAIMeasurement.Run()` 의 EStep 상태머신 3곳에 흐름 로그를 심는다. **이 Task 가 이번 작업의 본체다 — 여기서 "한 사이클이 순서대로 읽힌다"가 성립해야 한다.**

**핵심 설계 제약 (반드시 지킬 것 — 회귀 0 하드 요구):**
기존 `foreach` 루프 안에는 `continue` 분기가 여러 개 있다. 루프 **안**에 결과 로그를 넣으려면 제어흐름을 건드려야 하므로 **넣지 않는다.** 대신 **루프가 끝난 직후** 이미 채워진 상태 필드(`RuntimeDetectFailed`, `IsDatumFailed`, `IsAlignFailed`, `Last*`)를 읽어 결과 줄을 출력한다. 즉 이 Task 의 모든 변경은 **기존 문장 사이에 로그 호출을 삽입하는 것뿐**이며, 기존 조건문/루프/`continue`/`Step` 대입은 **한 글자도 수정하지 않는다.**

**(1) `EStep.DatumPhase` — 1단계 "기준선 찾기"**
- `if (parentSeq != null && parentSeq.DatumConfigs.Count > 0)` 블록 **진입 직후**(`ClearDatumTransforms()` 호출 직전)에:
  `FlowLog.Step(1, "기준선(기준 위치) 찾기 — 총 " + parentSeq.DatumConfigs.Count + "개");`
- `foreach (var datum in parentSeq.DatumConfigs) { ... }` 루프가 **끝난 직후**(`ApplyShotLights` 복귀 블록 직전, 현재 `:213` 과 `:214` 사이)에 결과 요약 루프를 새로 추가한다. 새 private 헬퍼 `LogFlowDatumResults(InspectionSequence parentSeq)` 로 빼서 Run() 이 길어지지 않게 한다:
  - datum 마다 이름을 얻고(null → `""`),
  - `parentSeq.IsAlignFailed(name)` 이면 → `FlowLog.Line(2, "→ " + name + " : 제품 위치를 못 찾음 → 이 기준선을 쓰는 측정은 건너뜁니다")`
  - 아니고 `parentSeq.IsDatumFailed(name) || datum.RuntimeDetectFailed` 이면 → `"→ " + name + " : 못 찾음 → 이 기준선을 쓰는 측정은 건너뜁니다"`
  - 그 외 → `"→ " + name + " : 찾음 (기울기 " + datum.DetectedAngleDeg.ToString("F1") + "도)"`
  - 헬퍼 전체를 `try { } catch { }` 로 감싼다.
- `DatumConfigs.Count == 0` 인 경우엔 1단계 줄을 출력하지 않는다(할 일이 없으므로 조용).
- `bDatumOnly == true` 로 `EStep.End` 로 빠지는 분기(`:237-241`)에서는 `FlowLog.Line(1, "이번 차례는 기준선만 찾는 단계라 측정은 하지 않습니다")` 를 추가한다 (초보자가 "왜 측정이 없지?" 를 이해할 수 있게).

**(2) `EStep.Grab` — 2단계 "사진 촬영"**
- `case EStep.Grab:` 진입 직후 (`if (ShotParam != null && !ShotParam.HasImage)` 앞):
  Shot 이름을 임시변수로 얻고 (`ShotParam` null 이면 `""`) `FlowLog.Step(2, "사진 촬영 — " + szShotName);`
- `Step = (int)EStep.Measure;` **직전**에 결과 한 줄:
  - `ShotParam != null && ShotParam.HasImage` 이면 `FlowLog.Line(2, "→ 촬영 완료");`
  - 아니면 `FlowLog.Line(2, "→ 사진을 얻지 못했습니다 — 이 위치의 측정은 모두 실패로 처리됩니다");`

**(3) `EStep.Measure` — 3단계 "측정"**
- `case EStep.Measure:` 블록 진입 직후, `foreach (var fai in ShotParam.FAIList)` 시작 전에 한 번:
  `FlowLog.Step(3, "측정 — " + szShotName);`  (szShotName 은 위와 같은 방식으로 취득)
- `foreach (var meas in fai.Measurements) { ... }` 루프가 **끝난 직후**(현재 `:439` 닫는 중괄호와 `:440` `try {` 사이)에 새 private 헬퍼 호출 `LogFlowFaiMeasurements(fai);` 를 추가한다. 헬퍼는:
  - `FlowLog.Line(1, "[" + faiName + "]")` 로 FAI 머리글 한 줄 (faiName null/빈값 → `"(이름없음)"`),
  - `fai.Measurements` 순회하며 각 항목에 대해
    `FlowLog.Measurement(measName, meas.LastHasResult, meas.LastMeasuredValue, meas.NominalValue, meas.TolerancePlus, meas.ToleranceMinus, meas.LastJudgement, meas.LastSkipReason);`
    (measName: `MeasurementName` 이 비면 `TypeName`)
  - 전체를 `try { } catch { }` 로 감싼다.
- `MarkAllMeasurementsNoImage` 로 가는 image==null 분기(`:472-478`)에서도 해당 Shot 의 FAI 별로 같은 헬퍼가 돌도록, `MarkAllMeasurementsNoImage(ref measuredCount);` 직후에 `ShotParam.FAIList` 순회로 `LogFlowFaiMeasurements(fai)` 를 호출한다.

**(4) `:461` `[FAI CrossZ IMG]` Trace 로그 — 유지.** 이 로그는 Trace 카테고리라 흐름 로그와 섞이지 않는다. 손대지 않는다.

**금지 사항 재확인:** 기존 `Logging.PrintLog((int)ELogType.Error, ...)` 32건(실패/skip 경로)은 삭제·수정하지 않는다. 흐름 로그는 **추가만** 한다.
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && A=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs && CA=$(grep -v '^[[:space:]]*//' "$A"); echo "--- 신설 (각 want >=1) ---"; echo "flowlog_calls=$(echo "$CA" | grep -cF 'FlowLog.')"; echo "step1=$(echo "$CA" | grep -cF 'FlowLog.Step(1,')"; echo "step2=$(echo "$CA" | grep -cF 'FlowLog.Step(2,')"; echo "step3=$(echo "$CA" | grep -cF 'FlowLog.Step(3,')"; echo "datum_helper=$(echo "$CA" | grep -cF 'private void LogFlowDatumResults(')"; echo "fai_helper=$(echo "$CA" | grep -cF 'private void LogFlowFaiMeasurements(')"; echo "meas_line=$(echo "$CA" | grep -cF 'FlowLog.Measurement(')"; echo "--- 기존 로그/제어흐름 보존 ---"; echo "error_logs=$(echo "$CA" | grep -cF 'Logging.PrintLog((int)ELogType.Error') (want 32, 감소하면 FAIL)"; echo "crossz_img_log=$(echo "$CA" | grep -cF '[FAI CrossZ IMG]') (want 1)"; echo "--- 기존 코드 라인이 삭제되지 않았는지: 삭제(-) 라인 중 빈줄이 아닌 것 (want 0) ---"; git diff -U0 -- "$A" | grep '^-' | grep -v '^---' | grep -vcE '^\-\s*$'; echo "--- C# 7.2 위반 신규 라인 (want 0) ---"; git diff -U0 -- "$A" | grep '^+' | grep -cE '[$]"|=>|switch[[:space:]]*[{]|[?][^?:]*:'; echo "--- 전문용어 금지: 신규 라인 (want 0) ---"; git diff -U0 -- "$A" | grep '^+' | grep -F 'FlowLog' | grep -ciE 'measurePhi|strip|transform|XLD|HTuple'; echo "--- scope: 이 파일만 (want: Action_FAIMeasurement.cs 1줄) ---"; git diff --name-only -- WPF_Example/; echo "--- build ---"; "C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj //t:Build //p:Configuration=Debug //p:Platform=x64 //v:m //nologo 2>&1 | grep -E "error CS|warning CS" | grep -v -E "CS0618|CS0162" | head -20; echo "CS_LIST_ABOVE_MUST_BE_EMPTY"</automated>
  </verify>
  <done>
1/2/3단계 흐름 로그가 DatumPhase/Grab/Measure 에 삽입됨. 기존 조건문·루프·`continue`·`Step` 대입·Error 로그는 하나도 수정/삭제되지 않음(diff 의 삭제 라인 0). 결과 줄은 모두 루프 종료 후 상태 필드 읽기 방식. 빌드 PASS.
  </done>
</task>

<task type="auto">
  <name>Task 3: 사이클 시작/종료 요약 + S11 진단 스캐폴딩 삭제 (InspectionSequence)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <action>
D-F3W-02 의 사이클 요약과 D-F3W-03 의 S11 삭제(한 줄짜리라 같이 간다)를 처리한다.

**(1) 사이클 시작 — `OnStart` 구독 추가**
- 생성자(`:101`)의 `OnStart += HandleRunStartResetResults;` **바로 아래**에 `OnStart += HandleFlowLogCycleBegin;` 를 추가한다.
- `HandleFlowLogCycleBegin(SequenceContext context)`:
  - `FlowLog.CycleBegin(Name);`
  - 경과시간용 `Stopwatch` 필드(`private readonly System.Diagnostics.Stopwatch _flowStopwatch = new System.Diagnostics.Stopwatch();`) 를 `Restart()` 한다.
  - 전체 `try { } catch { }`.

**(2) 사이클 종료 — `OnFinish` / `OnStop` / `OnError` 구독 추가**
- 생성자에 세 이벤트를 같은 핸들러 `HandleFlowLogCycleEnd` 로 구독한다.
- `HandleFlowLogCycleEnd(SequenceContext context)`:
  - `_flowStopwatch.Stop();`
  - `HandleRunStartResetResults`(`:226`) 의 순회 패턴을 **그대로 복사**해 이 시퀀스가 소유한 측정만 집계한다:
    `Actions` → `act as Action_FAIMeasurement` → `.ShotParam` → `.FAIList` → `.Measurements`
  - 집계 규칙 (전부 if/else, 삼항 금지):
    - `nMeasured`: `m.LastHasResult == true` 또는 `string.IsNullOrEmpty(m.LastSkipReason) == false` 인 항목 수 (= 이번 사이클에 시도된 항목).
    - `nNg`: 위 시도 항목 중 `m.LastJudgement == false` 인 항목 수.
    - `bAllPass`: `nNg == 0 && nMeasured > 0`.
  - `nMeasured == 0` 이면 요약 줄을 출력하지 않는다(이 시퀀스가 이번 사이클에 한 일이 없음 — 조용).
  - `FlowLog.CycleEnd(Name, bAllPass, nMeasured, nNg, _flowStopwatch.Elapsed.TotalSeconds);`
  - 전체 `try { } catch { }`. **이 핸들러는 어떤 경우에도 예외를 밖으로 던지면 안 된다** (SequenceBase 가 이벤트 발화 실패 시 잠금 영구화 위험 — `SequenceBase.cs:402` 주석 참조).

**(3) S11 삭제 — `[ALIGN-DIAG-LIVE]` 임시 진단 로그**
- `:1856-1859` 의 주석 2줄 + `HTuple diagImgW, diagImgH;` 선언 + `refImage.GetImageSize(out diagImgW, out diagImgH);` + `PrintLog` 1줄을 **전부 삭제**한다 (`diagImgW/diagImgH` 는 이 로그 전용이므로 미사용 변수 경고를 남기지 않도록 선언까지 삭제).
- `:1868-1869` 의 `[ALIGN-DIAG-LIVE] p2` 주석 1줄 + `PrintLog` 1줄을 삭제한다. **`modelPath2` 변수 자체는 바로 아래 `svc.TryFindPose(...)` 가 사용하므로 절대 삭제하지 말 것.**

**(4) S12 — 절대 유지 (CRITICAL)**
아래 3개 로그는 **한 글자도 건드리지 않는다.** 실제 부호 버그를 잡아낸 확증 로그다(커밋 a719073):
- `:1850` `[ALIGN] ... patAngDeg ... thetaDeg ... src=pattern ...`
- `:1879` `[ALIGN2] ... refBaseDeg ... curBaseDeg ... (baseline θ)`
- `:1936` `[ALIGN] ... datumDetectAngleDeg ... datumDetectRotDeg ... vs patternThetaDeg ...`
`:1888` 의 `[ALIGN2] 패턴2 매칭 실패` Error 로그도 유지한다.
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && I=WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs && CI=$(grep -v '^[[:space:]]*//' "$I"); echo "--- 신설 (각 want >=1) ---"; echo "cycle_begin=$(echo "$CI" | grep -cF 'FlowLog.CycleBegin(')"; echo "cycle_end=$(echo "$CI" | grep -cF 'FlowLog.CycleEnd(')"; echo "sub_start=$(echo "$CI" | grep -cF 'OnStart += HandleFlowLogCycleBegin')"; echo "sub_finish=$(echo "$CI" | grep -cF 'OnFinish += HandleFlowLogCycleEnd')"; echo "sub_stop=$(echo "$CI" | grep -cF 'OnStop += HandleFlowLogCycleEnd')"; echo "sub_error=$(echo "$CI" | grep -cF 'OnError += HandleFlowLogCycleEnd')"; echo "stopwatch=$(echo "$CI" | grep -cF '_flowStopwatch')"; echo "--- S11 삭제 (각 want 0) ---"; echo "diag_live=$(grep -cF 'ALIGN-DIAG-LIVE' "$I")"; echo "diagImgW=$(grep -cF 'diagImgW' "$I")"; echo "--- S12 절대 유지 (각 want 1) ---"; echo "s12_a=$(grep -cF 'src=pattern' "$I")"; echo "s12_b=$(grep -cF '(baseline θ)' "$I")"; echo "s12_c=$(grep -cF 'vs patternThetaDeg' "$I")"; echo "s12_d=$(grep -cF 'datumDetectRotDeg' "$I")"; echo "align2_err=$(grep -cF '패턴2 매칭 실패' "$I")"; echo "--- modelPath2 보존 (want >=2: 선언+사용) ---"; echo "modelPath2=$(grep -cF 'modelPath2' "$I")"; echo "--- 기존 구독 보존 (want 1) ---"; echo "reset_sub=$(echo "$CI" | grep -cF 'OnStart += HandleRunStartResetResults')"; echo "--- C# 7.2 위반 신규 라인 (want 0) ---"; git diff -U0 -- "$I" | grep '^+' | grep -cE '[$]"|=>|switch[[:space:]]*[{]|[?][^?:]*:'; echo "--- build ---"; "C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj //t:Build //p:Configuration=Debug //p:Platform=x64 //v:m //nologo 2>&1 | grep -E "error CS|warning CS" | grep -v -E "CS0618|CS0162" | head -20; echo "CS_LIST_ABOVE_MUST_BE_EMPTY"</automated>
  </verify>
  <done>
사이클 시작/종료 요약이 OnStart/OnFinish/OnStop/OnError 로 배선됨. `[ALIGN-DIAG-LIVE]` 2줄 + `diagImgW/diagImgH` 전용 코드 삭제. S12 확증 로그 3줄 전부 grep 으로 존재 확인. `modelPath2` 보존. 빌드 PASS.
  </done>
</task>

<task type="auto">
  <name>Task 4: S1 축약 — strip 단위 반복 로그를 ROI 단위 요약 1줄로 (DatumFindingService)</name>
  <files>WPF_Example/Halcon/Algorithms/DatumFindingService.cs</files>
  <action>
D-F3W-03 중 **S1 하나만** 처리한다. 사이클당 40~240줄을 쏟아내는 유일한 원인이라 이것만 걷어내면 흐름 로그가 파묻히지 않는다.
**S2/S3/S4/S6/S7/S8/S9/S10 은 이번 범위에서 제외** — `<scope_coverage>` 의 제외 표 참조. 해당 로그들은 **손대지 않는다.**

**변경 원칙: 로그 문자열과 로그 조건만 바꾼다. 측정 계산식·파라미터·반환값·예외 처리 정책은 한 글자도 바꾸지 않는다.**

참조 구현: `VisionAlgorithmService.cs:125-184` 의 okStrips/noEdgeStrips/failedStrips 집계 패턴 (위 `<interfaces>` 에 발췌).

**(1) strip 결과를 호출부로 올린다**
- `DatumFindingService` 안에 private nested enum 추가:
  `private enum EStripOutcome { Ok, NoEdge, Failed }`
- `AppendEdgePointsFromStrip`(`:2136` 부근 선언) 의 반환형을 `void` → `EStripOutcome` 로 바꾼다. **파라미터는 추가·변경하지 않는다** (out 파라미터 불필요 — 예외 메시지 dedup 은 S2 로 이번 범위 제외).
  - `:2182-2185` 의 per-strip `[Datum.{0}] strip MeasurePos: dir=... measurePhi=... sel=... edges=...` PrintLog 를 **삭제**한다. **이 한 줄이 S1 의 전부다.**
  - edge 0건(`edgeRows.TupleLength() <= 0 || edgeCols.TupleLength() <= 0`) → 기존 `return;` 을 `return EStripOutcome.NoEdge;` 로.
  - 정상 → `TupleConcat` 2회 후 `return EStripOutcome.Ok;`
  - `catch (Exception ex)` 블록 → `return EStripOutcome.Failed;` 만 추가한다. **`:2202-2203` 의 `[Datum.{0}] strip swallowed: ...` PrintLog 는 S2 로 제외되었으므로 그대로 둔다.** 삼키는 정책도 그대로.
  - `finally` 의 `CloseMeasure`/`Dispose` 정리는 무변경.

**(2) 호출부 2곳에서 집계 → 기존 요약줄에 병합**
호출부는 미러 쌍이다: `TryFindLine` 의 strip 루프(`:1779-1808`)와 `TryExtractEdgePoints` 의 동일 구조 미러 루프. **두 곳 모두 동일하게** 처리한다.
- 루프 앞에 `int nOkStrips = 0; int nNoEdgeStrips = 0; int nFailedStrips = 0;` 선언.
- 각 `AppendEdgePointsFromStrip(...)` 호출의 반환값을 받아 if/else 로 카운트 (삼항 금지, VisionAlgorithmService 패턴 그대로).
- **기존 accumulated 요약줄에 병합한다** — 새 줄을 추가하지 말고 기존 줄의 포맷 문자열만 확장한다:
  - `TryFindLine` `:1811-1813` 현재:
    `"[Datum.{0}] strip-loop accumulated {1} edge points across {2} strips"`
    → 다음으로 교체:
    `"[Datum.{0}] strip-loop accumulated {1} edge points across {2} strips (ok {3}, noEdge {4}, failed {5})"`
    (인자: lbl, edgeCount, stripCount, nOkStrips, nNoEdgeStrips, nFailedStrips)
  - `TryExtractEdgePoints` `:2067-2069` 의 `strip-loop(extract) accumulated ...` 도 **동일한 방식**으로 확장 (`(extract)` 표기 유지 — 두 경로 구분용).
- **이 줄들의 위치·출력 조건은 바꾸지 않는다.** `:1677`/`:1950` 의 bounds 로그, `:1820`/`:2076` 의 trimmed 로그, `:1762`/`:2018` 의 erosion tact 로그는 **전부 그대로 둔다** (S6/S7/S8/S9 = 이번 범위 제외).
- `edgeCount < 2` 실패 게이트(`:1825-1833`)의 error 문자열 / `PrintLog` / `return false` 는 **무변경**.

**무변경 확인 대상:** `HOperatorSet.*` 호출, `ComputeMeasurePhi`, sigma/threshold/polarity/selection 전달, `SortAndTrimPercent` 호출, 모든 반환값.
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && D=WPF_Example/Halcon/Algorithms/DatumFindingService.cs && V=WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs && E=WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs; echo "--- S1 제거 (want 0) ---"; echo "strip_measurepos=$(grep -cF 'strip MeasurePos' "$D")"; echo "--- S1 집계 신설 (각 want >=1) ---"; echo "enum=$(grep -cF 'private enum EStripOutcome' "$D")"; echo "ok_counter=$(grep -cF 'nOkStrips' "$D")"; echo "noedge_ret=$(grep -cF 'EStripOutcome.NoEdge' "$D")"; echo "failed_ret=$(grep -cF 'EStripOutcome.Failed' "$D")"; echo "merged_summary=$(grep -cF 'noEdge {4}, failed {5}' "$D") (want 2: TryFindLine + extract 미러)"; echo "--- 범위 제외 항목은 그대로 있어야 함 ---"; echo "S2_swallowed=$(grep -cF 'strip swallowed' "$D") (want 1)"; echo "S6_bounds=$(grep -cF 'strip-loop: bounds' "$D") (want 1)"; echo "S6_bounds_ex=$(grep -cF 'strip-loop(extract): bounds' "$D") (want 1)"; echo "S8_trimmed=$(grep -cF 'trimmed {1}%' "$D") (want 2)"; echo "S9_tact=$(grep -cF 'tact = {2}ms' "$D") (want 2)"; echo "--- 미수정 파일 확인 (want: 아래 diff 목록에 없어야 함) ---"; git diff --name-only -- "$V" "$E"; echo "VISION_AND_FAIEDGE_MUST_BE_EMPTY_ABOVE"; echo "--- 알고리즘 무변경: 호출 수 (변경 전과 동일해야 함) ---"; echo "D_hop=$(grep -cF 'HOperatorSet.' "$D")"; echo "sortandtrim=$(grep -cF 'SortAndTrimPercent' "$D") (want 2)"; echo "computephi=$(grep -cF 'ComputeMeasurePhi(' "$D")"; echo "--- C# 7.2 위반 신규 라인 (want 0) ---"; git diff -U0 -- "$D" | grep '^+' | grep -cE '[$]"|=>|switch[[:space:]]*[{]|[?][^?:]*:'; echo "--- build ---"; "C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj //t:Build //p:Configuration=Debug //p:Platform=x64 //v:m //nologo 2>&1 | grep -E "error CS|warning CS" | grep -v -E "CS0618|CS0162" | head -20; echo "CS_LIST_ABOVE_MUST_BE_EMPTY"</automated>
  </verify>
  <done>
strip 단위 `strip MeasurePos` 로그 0건. `EStripOutcome` 집계가 미러 2곳 모두에서 기존 accumulated 요약줄에 병합됨. 범위 제외 로그(S2/S6/S8/S9)는 grep 으로 그대로 존재 확인. `VisionAlgorithmService.cs` / `FAIEdgeMeasurementService.cs` 는 diff 에 나타나지 않음. `HOperatorSet.*` / `SortAndTrimPercent` 호출 수 변경 전과 동일. 빌드 PASS.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: 흐름 로그 육안 검증 — 한 사이클이 순서대로 읽히는가</name>
  <what-built>
신규 `Flow` 로그 카테고리 + 검사 1사이클 전체 흐름 로그(시작 → 기준선 → 촬영 → 측정 → 종합판정). 부수적으로 strip 단위 반복 Trace 로그(S1) 축약 + `[ALIGN-DIAG-LIVE]` 임시 진단 로그 삭제.
  </what-built>
  <how-to-verify>
1. `bin/x64/Debug/DatumMeasurement.exe` 실행 (빌드 시 exe 파일잠금 방지를 위해 기존 프로세스는 미리 종료).
2. 로그 창 메뉴에 **`Flow`** 항목이 새로 보이는지 확인 → 열어 둔다.
3. RUN(또는 일괄검사)로 검사 1사이클 실행.
4. **Flow 창만 보고** 아래를 확인한다 (이게 이 작업의 합격 기준 전부다):
   - [ ] `▶ 검사 시작` → `[1단계] 기준선` → `[2단계] 사진 촬영` → `[3단계] 측정` → `■ 검사 끝` 순서로 **위에서 아래로** 읽히는가?
   - [ ] 단계가 건너뛰어지거나 순서가 뒤엉킨 곳은 없는가?
   - [ ] 측정 줄에 **측정명 / 측정값 / 기준 / 허용범위(+,-) / 합불** 5가지가 모두 있는가?
   - [ ] 건너뛴 항목의 이유가 **쉬운 한국어 문장**인가? (`DATUM_FAIL` 같은 영문 상수가 그대로 노출되면 FAIL)
   - [ ] `measurePhi`, `strip`, `transform`, `XLD`, `HTuple` 같은 전문용어가 **하나도** 없는가?
   - [ ] 마지막 요약에 종합판정 / 측정 개수 / NG 개수 / 소요시간이 있는가?
   - [ ] **가장 중요:** 이 코드를 처음 보는 사람이 이 로그만 읽고 "기계가 무슨 일을 했는지" 이해할 수 있는가?
5. **Trace 창** — 딱 두 가지만 확인:
   - [ ] `strip MeasurePos` 반복 줄이 사라졌는가?
   - [ ] `[ALIGN] ... datumDetectRotDeg ... vs patternThetaDeg ...` 줄은 **여전히 있는가?** (없으면 FAIL — 즉시 중단)
6. **회귀 확인:** 같은 레시피/같은 이미지로 검사했을 때 측정값과 합불 판정이 이 작업 **전과 동일**한가?
  </how-to-verify>
  <resume-signal>"승인" 이라고 입력하거나, 순서가 어색한 지점 / 이해 안 되는 줄을 구체적으로 알려주세요.</resume-signal>
</task>

</tasks>

<verification>
- Debug/x64 빌드: `error CS` 0건, 신규 `warning CS` 0건
- `git diff --name-only` 결과가 위 `files_modified` 목록을 벗어나지 않을 것 — 특히 `VisionAlgorithmService.cs` / `FAIEdgeMeasurementService.cs` 가 나타나면 범위 위반
- `ELogType` 의 기존 정수값(0~6) 무변경 — grep 으로 7개 항목 모두 확인
- `InspectionSequence.cs` 의 S12 확증 로그 3줄 존재 — grep (`src=pattern`, `(baseline θ)`, `vs patternThetaDeg`)
- 범위 제외 로그(S2/S6/S8/S9)가 `DatumFindingService.cs` 에 그대로 존재 — grep
- `HOperatorSet.` 호출 수 / `SortAndTrimPercent` 호출 수가 변경 전과 동일 (알고리즘 무변경 증명)
- 흐름 로그 신규 라인에 금지 전문어 0건
</verification>

<success_criteria>
1. 로그 창 메뉴에 `Flow` 카테고리가 뜨고, `D:\Data\Flow` 에 파일이 쌓인다.
2. **검사 1사이클의 흐름 로그를 위에서 아래로 읽으면 시퀀스 순서가 그대로 읽힌다** (Task 5 사용자 승인). ← 이 작업의 핵심
3. Trace 에서 strip 단위 반복 줄(사이클당 40~240줄)이 사라진다.
4. `[ALIGN] datumDetectRotDeg vs patternThetaDeg` 확증 로그가 살아 있다.
5. 측정값·판정·제어 흐름 회귀 0.
</success_criteria>

<output>
완료 후 `.planning/quick/260805-f3w-flow-log/260805-f3w-SUMMARY.md` 를 생성한다. 범위에서 제외한 스팸 항목(S2/S3/S4/S6/S7/S8/S9/S10)은 SUMMARY 의 carry-over 로 한 줄 기록한다.
</output>
</content>
