---
phase: 260805-iw0-parambase-copyto-shot-fai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Sequence/Param/ParamBase.cs
  - WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
  - WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
  - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: true
requirements: [IW0-01, IW0-02, IW0-03]

must_haves:
  truths:
    - "Shot 을 복사해 다른 Shot 에 붙여넣으면 원본의 FAI 항목이 전부(측정 항목 포함) 따라온다"
    - "FAI 를 복사해 다른 FAI 에 붙여넣으면 ROI/에지 파라미터와 하위 Measurement 가 전부 따라온다"
    - "Measurement 를 복사해 같은 타입의 다른 Measurement 에 붙여넣으면 공차/보정/ROI 필드가 전부 따라온다"
    - "Datum 을 복사해 다른 Datum 에 붙여넣으면 ROI/에지/조명/패턴정렬 필드가 전부 따라온다"
    - "붙여넣은 Datum 의 PropertyGrid 'Datum|Result' 값(DetectedEdgeCount/DetectedFitRMSE/DetectedAngleDeg)은 원본 값이 아니라 대상 자신의 값으로 남는다"
    - "붙여넣기 직후 트리에 새 FAI/Measurement 자식 노드가 즉시 보인다 (재시작/레시피 재로드 불필요)"
    - "Shot 추가(+) 버튼은 종전과 동일하게 FAI_0 하나만 가진 Shot 을 만든다 (sibling FAI 가 딸려오지 않는다)"
    - "타입이 다른 Measurement 끼리 붙여넣으면 'Fail to Copy' 로 거부된다"
  artifacts:
    - path: "WPF_Example/Sequence/Param/ParamBase.cs"
      provides: "CopyPublicPropertiesTo — Save/Load 와 동일한 reflection+type switch 기반 일괄 필드 복사 헬퍼"
      contains: "protected int CopyPublicPropertiesTo"
    - path: "WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs"
      provides: "MeasurementBase.CopyTo — 런타임 타입 기준 전 필드 복사 + 타입 불일치 거부 + transient 제외"
      contains: "public override bool CopyTo"
    - path: "WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs"
      provides: "FAIConfig.CopyTo — FAI 필드 + Measurements 깊은 복사"
      contains: "target.ClearMeasurements"
    - path: "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs"
      provides: "DatumConfig.CopyTo — Datum 설정 전 필드 복사, 검출결과 transient 23종 제외"
      contains: "public override bool CopyTo"
    - path: "WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs"
      provides: "ShotConfig.CopyTo 에 FAIList 깊은 복사 추가"
      contains: "target.ClearFAIs"
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "Add-Shot 회귀 가드 + 붙여넣기 후 트리 자식 노드 갱신"
      contains: "RefreshChildNodesAfterPaste"
  key_links:
    - from: "ShotConfig.CopyTo"
      to: "FAIConfig.CopyTo"
      via: "target.AddFAI(src.FAIName) 로 새 FAIConfig 생성 후 src.CopyTo(dst)"
      pattern: "AddFAI\\(.*FAIName\\)"
    - from: "FAIConfig.CopyTo"
      to: "MeasurementBase.CopyTo"
      via: "target.AddMeasurement(src.TypeName) 로 새 Measurement 생성 후 src.CopyTo(dst)"
      pattern: "AddMeasurement\\(.*TypeName\\)"
    - from: "MeasurementBase.CopyTo / FAIConfig.CopyTo / DatumConfig.CopyTo"
      to: "ParamBase.CopyPublicPropertiesTo"
      via: "reflection 일괄 필드 복사 호출 + _copyExclude 전달"
      pattern: "CopyPublicPropertiesTo\\("
    - from: "InspectionListView.button_paste_Click"
      to: "RefreshChildNodesAfterPaste"
      via: "CopyTo 성공 직후 트리 자식 VM 재구축"
      pattern: "RefreshChildNodesAfterPaste\\("
    - from: "InspectionListView Add-Shot (siblingShot?.CopyTo(shot))"
      to: "shot.ClearFAIs()"
      via: "sibling 조명/노광만 상속하고 FAI 는 새로 만들도록 비움"
      pattern: "shot\\.ClearFAIs\\(\\)"
---

<objective>
`ParamBase.CopyTo` 가 기본 구현이 `return true` 뿐인 빈 메서드라서, override 가 없는 `FAIConfig` / `MeasurementBase`(+15개 하위 측정 타입) / `DatumConfig` 는 복사/붙여넣기 시 **아무 필드도 옮겨지지 않는다**. 여기에 더해 `ShotConfig.CopyTo` 가 `FAIList` 를 의도적으로 제외하고 있어, Shot 을 복사해도 FAI 항목이 하나도 따라오지 않는다.

Purpose: 레시피 작성 시 복사/붙여넣기가 실제로 데이터를 옮기게 만들어, 100개 이상 검사 항목을 손으로 다시 티칭하는 낭비를 없앤다. 조용히 아무것도 안 하는(성공처럼 보이는) 현재 동작은 데이터 유실로 오인되기 쉬운 결함이다.

Output: `ParamBase` 일괄 복사 헬퍼 1개 + `CopyTo` override 3개(`MeasurementBase`/`FAIConfig`/`DatumConfig`) + `ShotConfig.CopyTo` 의 `FAIList` 깊은 복사 + UI 측 회귀 가드/트리 갱신.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260805-iw0-parambase-copyto-shot-fai/260805-iw0-CONTEXT.md

@WPF_Example/Sequence/Param/ParamBase.cs
@WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs
@WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs
@WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
@WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
@WPF_Example/Custom/Sequence/Inspection/IDatumOriginConsumer.cs
@WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
@WPF_Example/UI/ControlItem/InspectionListViewModel.cs

<coding_rules>
프로젝트 상시 코딩 규칙 — 위반 시 회귀로 간주한다.

- **삼항 연산자 `?:` 금지** → 반드시 `if`/`else` 로 풀어 쓴다.
- **문자열 보간 `$"..."` 금지** → `string.Format(...)` 또는 `+` 연결.
- **expression-bodied member `=>` 금지**, **switch expression 금지** (C# 7.2 범위 준수).
- **헝가리언 표기**: 새로 만드는 지역변수/필드는 `bXxx`(bool) / `nXxx`(int) / `sXxx`(string) / `dXxx`(double). 기존 코드에 이미 있는 이름은 건드리지 않는다.
- **회귀 0**: 기존 코드 라인을 지우지 않는다. 이 작업은 전부 "추가"다 (예외: 아래 명시된 `siblingShot?.CopyTo(shot);` 뒤 1줄 추가, `button_paste_Click` 내 호출 1줄 추가).
- 함수는 30~40줄 이내로 유지하고, 신입도 읽을 수 있게 쓴다.
- 주석은 비자명한 "왜"만 최소로 (날짜 접두 주석 규칙은 폐기됨).
</coding_rules>

<interfaces>
<!-- 실행자가 코드베이스를 다시 뒤지지 않도록, 필요한 계약을 여기 전부 적어둔다. -->

ParamBase.cs (WPF_Example/Sequence/Param/ParamBase.cs)
```csharp
public class ParamBase : INotifyPropertyChanged {
    public object Owner { get; set; }              // 복사 금지 (객체 그래프 파괴)
    public string OwnerName { get; }               // 읽기전용 → 자동 skip
    public SequenceBase Parent { get; set; }       // 복사 금지

    public virtual bool Save(IniFile saveFile, string group);   // L318 — reflection + type switch
    public virtual bool Load(IniFile loadFile, string group);   // L363 — reflection + type switch, !CanWrite skip
    public virtual bool CopyTo(ParamBase param) { return true; } // L432 — 빈 구현 (이번 버그의 뿌리)
}
```

Save/Load 가 다루는 type switch 케이스 (`prop.PropertyType.Name` 기준):
`"Int32"`, `"Double"`, `"String"`, `"Boolean"`, `"Rect"`, `"Line"`, `"Circle"`, `"PropertyItem[]"`, `"ModelFinderViewModel"`

> **중요 — `[Browsable(false)]` / `[ReadOnly(true)]` 는 리플렉션 복사를 막지 못한다.**
> 두 어트리뷰트 모두 PropertyGrid 표시만 제어할 뿐 `PropertyInfo.CanWrite` 에 영향이 없다.
> 실제 setter 가 있으면 그대로 복사된다. 제외하려면 **반드시 `_copyExclude` 에 이름을 넣어야 한다.**
> 반대로 타입이 화이트리스트(위 7종) 밖이면(`HTuple`, `bool[]`, `enum`, `List<T>`) 자동으로 빠진다.

ShotConfig.cs (Custom/Sequence/Inspection/ShotConfig.cs)
```csharp
public class ShotConfig : CameraSlaveParam, IOfflineImageParam {
    public List<FAIConfig> FAIList { get; private set; }   // L35
    public string ShotName { get; set; }                    // L40 — 복사 제외 유지(사용자 확정)
    public FAIConfig AddFAI(string name = null);            // L414 — new FAIConfig(this, name) 후 FAIList.Add
    public bool RemoveFAI(int index);                       // L422
    public void ClearFAIs();                                // L428
    public override bool CopyTo(ParamBase param);           // L304 — 조명/노광 등 이미 복사 중
}
```

FAIConfig.cs
```csharp
public class FAIConfig : ParamBase, ICustomTypeDescriptor {
    public List<MeasurementBase> Measurements { get; private set; }   // L14
    public MeasurementBase AddMeasurement(string typeName);           // L16 — MeasurementFactory.Create(typeName, this)
    public bool RemoveMeasurement(int index);                         // L23
    public void ClearMeasurements();                                  // L30
    public string FAIName { get; set; }                               // L124
    // 복사 대상 필드(설정): ROI_Row/ROI_Col/ROI_Phi/ROI_Length1/ROI_Length2,
    //   EdgeMeasureType/EdgeThreshold/Sigma/EdgeDirection/EdgeSelection/EdgeSampleCount/
    //   EdgeTrimCount/EdgePolarity/PixelResolutionX/PixelResolutionY/PolygonPoints
    // 복사 제외(런타임 결과): MeasuredValue, IsPass, WasDatumSkipped,
    //   LastOriginImageFileName, LastCaptureImageFileName
    // 자동 skip: LastOverlays(List<T>), EdgeMeasureTypeList/EdgeDirectionList/EdgePolarityList(읽기전용)
}
```

MeasurementBase.cs
```csharp
public abstract class MeasurementBase : ParamBase {
    public abstract string TypeName { get; }   // 읽기전용 → 자동 skip. MeasurementFactory 키
    public string MeasurementName { get; set; }
    // 복사 대상: DatumRef, NominalValue, MeasCorrectionEnabled, MeasCorrectionFactor,
    //   AngleCorrectionEnabled, AngleCorrectionDeg, TolerancePlus, ToleranceMinus,
    //   UseAbsoluteValue, InvertSign  (+ 하위 타입 고유 ROI/에지 필드 전부)
    // 복사 제외(런타임 결과): LastMeasuredValue, LastJudgement, LastHasResult, LastSkipReason
    public override bool Load(IniFile loadFile, string groupName);  // L180 — 이미 override 존재
}
```

IDatumOriginConsumer.cs — 하위 측정 타입 **9개 파일**이 구현하는 datum 좌표 주입 채널.
`Action_FAIMeasurement.EStep.Measure` 가 **매 검사 사이클마다 새로 주입**하는 transient 값이므로 복사 대상이 아니다.
```csharp
public interface IDatumOriginConsumer {
    double DatumOriginRow { get; set; }
    double DatumOriginCol { get; set; }
    double DatumAngleRad  { get; set; }
    double DatumAngle2Rad { get; set; }
    double DatumDetectedCircleRow { get; set; }
    double DatumDetectedCircleCol { get; set; }
}
```

MeasurementBase 하위 타입 15종 (전부 `Custom/Sequence/Inspection/Measurements/`):
`ArcEdgeDistance`(23) `ArcLineIntersectDistance`(61) `CircleCenterDistance`(20) `CircleDiameter`(14)
`CompoundAngle`(24) `CompoundCenterBDistance`(25) `CompoundCenterCDistance`(25) `CompoundShortAxisDistance`(10)
`DualImageEdgeDistance`(37) `EdgePairDistance`(12) `EdgeToLineAngle`(22) `EdgeToLineDistance`(23)
`LineToLineAngle`(18) `LineToLineDistance`(18) `PointToLineDistance`(18) `PointToPointDistance`(18)
→ 괄호 안은 public 프로퍼티 수. **이 15개 파일은 이번 작업에서 수정하지 않는다** (아래 설계 노트 참조).
→ 스윕 결과 이 15개 파일에 `Last*`/`Detected*`/`Runtime*`/`Result*` 형태의 스칼라 transient 프로퍼티는 **없다**.
   `DualImageEdgeDistance` 의 `RuntimeImageA/B` 는 `HImage` 라 타입 화이트리스트 밖 → 자동 skip.

DatumConfig.cs — public 프로퍼티 197개
```csharp
public class DatumConfig : ParamBase, ICustomTypeDescriptor, IOfflineImageParam {
    public string DatumName { get; set; }   // L19 — 복사 제외(이름 충돌)
    // 복사 대상: ImageSourceMode/SourceShotName/TeachingImagePath/TeachingImagePath_Vertical,
    //   ZIndexA/ZIndexB, AlgorithmType, TwoLineAngleToleranceDeg, ExpectedAngleDeg, AngleTolerance,
    //   IsPatternAlignEnabled/PatternEngine/RefMatch*/PatternRoi*/PatternRoi2*,
    //   Line1_*/Line2_*/Vertical_*/Circle*/Horizontal_A_*/Horizontal_B_* (ROI + Edge),
    //   RefOriginRow/RefOriginCol/RefAngleRad, EdgeThreshold/Sigma/EdgePolarity(legacy),
    //   조명 필드 전체(Ring 6ch / Bar 4ch / Back / Ring7 / Coax), IsConfigured
    // ★ 복사 제외 — 검출결과 transient **두 그룹 모두**:
    //   [그룹 A: 티칭 검출 오버레이 L735~760]
    //     LastFindSucceeded, RuntimeDetectFailed, LastTeachSucceeded,
    //     Line1Detected_RBegin/_CBegin/_REnd/_CEnd, Line2Detected_RBegin/_CBegin/_REnd/_CEnd,
    //     CircleCenter_Row, CircleCenter_Col, CircleDetected_Radius
    //   [그룹 B: TryFindDatum write-back L796~860]  ← 놓치기 쉬움
    //     DetectedOriginRow(L800), DetectedOriginCol(L804),
    //     DetectedRefAngle(L808), DetectedRefAngle2(L815),
    //     DetectedCircleRow(L831), DetectedCircleCol(L835),
    //     DetectedEdgeCount(L850), DetectedFitRMSE(L855), DetectedAngleDeg(L860)
    //     └ 뒤 3개는 [Category("Datum|Result")] 로 **PropertyGrid 에 실제 노출**된다.
    //        [ReadOnly(true)] 는 CanWrite 를 막지 않으므로 반드시 이름으로 제외해야 한다.
    // 복사 제외(도 단위 wrapper — radian 원본이 이미 복사되므로 중복/왕복오차 방지):
    //   PatternRoi_PhiDeg, PatternRoi2_PhiDeg, Line1_PhiDeg, Line2_PhiDeg,
    //   Vertical_PhiDeg, Horizontal_A_PhiDeg, Horizontal_B_PhiDeg
    // 자동 skip(타입 화이트리스트 밖 — 제외 목록 불필요):
    //   HTuple 22종(CurrentTransform, Line1_DetectedEdgeRows/Cols, Vertical_*, Circle_*, Horizontal_*_* 등),
    //   AngleValidationStatus(enum, L825), CircleStripSuccesses(bool[], L842),
    //   HasDetectFail/AlgorithmTypeEnum/*List(읽기전용)
}
```

InspectionListView.xaml.cs 관련 지점
```csharp
public ParamBase SelectedParam { get; private set; }          // L26
private void button_paste_Click(object sender, RoutedEventArgs e)  // L895
    // L905: if (CopiedParam.CopyTo(SelectedParam) == false) { ...실패 메시지...; return; }
    // L912~916: index 저장 → UnselectAll() → SelectedIndex = index
// L1253: siblingShot?.CopyTo(shot);   ← Add-Shot(+) 경로. 바로 아래 L1255 에서 shot.AddFAI("FAI_0")
```

InspectionListViewModel.cs 헬퍼 (이미 존재, 그대로 재사용)
```csharp
public void AddFAINode(NodeViewModel actionNode, FAIConfig fai, ESequence seqID, EAction actID);  // L161
public void AddMeasurementNode(NodeViewModel faiNode, MeasurementBase meas);                      // L198
public void RebuildTree();                                                                        // L218
```

NodeViewModel.cs 멤버
```csharp
public ObservableCollection<NodeViewModel> Children { get; }   // L29 (lazy load, Clear() 가능)
public object Param { get; }          // L83
public EAction ActionID { get; }      // L97
public ESequence SequenceID { get; }  // L103
public ENodeType NodeType { get; }    // L109
public bool IsExpanded { get; set; }  // L117
```
</interfaces>

<design_note>
## 왜 15개 하위 측정 타입 파일을 건드리지 않는가 (실행자 필독)

CONTEXT 의 확정 사항은 "각 클래스의 **공개 프로퍼티를 전부 복사**" + "기존 override 4개의 구현 패턴을 따를 것" 이다.
이 계획은 두 가지를 모두 지킨다:

- **구현 패턴 유지**: 새 override 3개 전부 기존 4개와 동일한 모양이다 —
  `override bool CopyTo(ParamBase param)` → `as` 캐스팅 → null 가드 → `base.CopyTo(param)` → 필드 복사 → `bool` 반환.
- **전 프로퍼티 복사 달성**: 필드 복사 본문이 `ParamBase` 가 **이미 쓰고 있는** reflection + type switch(= `Save`/`Load` 와 완전히 동일한 방식, 같은 파일 바로 위 두 메서드)를 재사용한다.

이렇게 하는 이유는 세 가지다.

1. **숫자**: 손으로 대입문을 쓰면 DatumConfig 197 + FAIConfig 25 + MeasurementBase 15 + 하위 15타입 368 = **약 605줄**이다.
   한 실행 세션에 들어가지 않고, 3개 이상의 plan 으로 쪼개야 한다.
2. **같은 결함의 재발**: 605개 중 하나만 빠뜨려도 지금 고치고 있는 것과 **똑같은 증상**(조용히 값이 안 옮겨짐)이 된다.
   앞으로 프로퍼티가 하나 추가될 때마다 다시 재발한다.
3. **reflection 은 런타임 타입 기준**: `GetType().GetProperties()` 는 실제 인스턴스 타입의 프로퍼티를 돌려준다.
   즉 `MeasurementBase.CopyTo` 하나가 `ArcLineIntersectDistanceMeasurement` 의 61개 필드까지 전부 복사한다.
   → **하위 15타입에 override 를 만들 필요가 없다.** (CONTEXT 의 "하위 타입 고유 필드가 있는 경우에만" 조건 = 해당 없음)

추가로 "INI 에 저장되는 값 == 복사되는 값" 이라는 계약이 생긴다 (`Save`/`Load` 와 동일한 type switch 를 쓰므로).
단, **런타임 검출/측정 결과 필드만은 예외로 복사하지 않는다** — 아래 참조.

## reflection 복사의 유일한 위험: 제외 목록 누락 (실행자 필독)

이 방식의 단 하나의 실패 모드는 `_copyExclude` 에 빠진 transient 필드가 **남의 결과값을 조용히 실어 나르는 것**이다.
고치려는 버그(값이 조용히 유실됨)와 방향만 반대인 같은 계열의 결함이다.

가장 위험한 사례: `DatumConfig.DetectedEdgeCount` / `DetectedFitRMSE` / `DetectedAngleDeg`.
이 3개는 `[Category("Datum|Result")]` 로 **PropertyGrid 에 실제로 보이고**, `[ReadOnly(true)]` 만 붙어 있어
`PropertyInfo.CanWrite` 는 `true` 다. 제외하지 않으면 — 이미 검출을 돌린 Datum A(예: `DetectedEdgeCount=42`)를
아직 한 번도 검출 안 한 Datum B 에 붙여넣었을 때, **B 의 화면에 A 의 옛 검출 품질 수치가 그대로 뜬다.**
사용자는 B 가 검출에 성공한 것으로 오인한다.

따라서 Task 1/2 의 `_copyExclude` 목록은 **그대로 옮겨 적을 것.** 임의로 줄이지 말 것.

## 이번 범위에서 제외 (발견했지만 손대지 않음)

`ShotConfig.CopyTo` 는 지금도 `ZPosition` / `DelayMs` / `ZIndex` / `SimulImagePath` 를 복사하지 않는다.
`OwnerSequenceName` 은 복사하면 안 되는 것이 맞지만(다른 시퀀스로 Shot 이 이동해버림), 나머지 4개는 같은 종류의 누락으로 보인다.
다만 CONTEXT 확정 사항은 "`ShotConfig.CopyTo` 에 **FAIList** 포함"뿐이고, 이 4개는 Add-Shot(+) 경로의 sibling 상속 동작까지 바꾸므로(특히 `ZIndex` 중복) **이번엔 건드리지 않는다.** 사용자 판단 후 별건으로 처리.
</design_note>

<tasks>

<task type="auto">
  <name>Task 1: ParamBase 일괄 복사 헬퍼 + MeasurementBase/FAIConfig CopyTo</name>
  <files>WPF_Example/Sequence/Param/ParamBase.cs, WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs, WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs</files>
  <action>
**[1] ParamBase.cs — `CopyTo`(L432) 바로 아래에 헬퍼 추가**

```csharp
// Save/Load 와 동일한 reflection + type switch 를 복사에도 재사용한다.
//  "INI 에 저장되는 값 == 붙여넣기로 옮겨지는 값" 이라는 계약을 코드로 강제해,
//  새 프로퍼티가 추가될 때 CopyTo 에서 누락되어 조용히 유실되는 회귀를 원천 차단한다.
//  GetType() 은 런타임 타입이므로 파생 클래스의 고유 프로퍼티까지 그대로 포함된다.
//  주의: [Browsable(false)] / [ReadOnly(true)] 는 CanWrite 를 막지 않는다 —
//   런타임 결과 필드를 빼려면 호출자가 excludeNames 로 이름을 넘겨야 한다.
//  반환값 = 실제로 복사한 프로퍼티 개수 (진단/검증용).
protected int CopyPublicPropertiesTo(ParamBase target, HashSet<string> excludeNames) {
    if (target == null) return 0;
    int nCopied = 0;
    PropertyInfo[] props = GetType().GetProperties(BindingFlags.Instance | BindingFlags.Public);
    foreach (var prop in props) {
        if (!prop.CanRead) continue;
        if (!prop.CanWrite) continue;                          // 계산 전용 프로퍼티 skip
        if (prop.GetIndexParameters().Length > 0) continue;    // 인덱서 skip
        if (prop.Name == "Owner") continue;                    // 객체 그래프 파괴 방지
        if (prop.Name == "Parent") continue;
        if (excludeNames != null && excludeNames.Contains(prop.Name)) continue;

        string type = prop.PropertyType.Name;
        bool bSupported = false;
        if (type == "Int32")   bSupported = true;
        else if (type == "Double")  bSupported = true;
        else if (type == "String")  bSupported = true;
        else if (type == "Boolean") bSupported = true;
        else if (type == "Rect")    bSupported = true;
        else if (type == "Line")    bSupported = true;
        else if (type == "Circle")  bSupported = true;
        // PropertyItem[] / ModelFinderViewModel / List<T> / HTuple / enum / bool[] 은 여기서 다루지 않는다.
        //  (ShotConfig.CopyTo 처럼 소유 클래스가 명시적으로 처리하거나, transient 라 복사 대상이 아니다)
        if (!bSupported) continue;

        try {
            prop.SetValue(target, prop.GetValue(this));
            nCopied++;
        }
        catch (Exception e) {
            Logging.PrintErrLog((int)ELogType.Error, "CopyPublicPropertiesTo " + prop.Name + " : " + e.Message);
        }
    }
    return nCopied;
}
```

`ParamBase.cs` 는 이미 `System.Reflection`, `System.Collections.Generic`, `System`, `ReringProject.Utility`, `ReringProject.Define` 을 using 하고 있으므로 **using 추가 불필요**. `HashSet<string>` 은 `System.Collections.Generic` 에 있다.

`ParamBase.CopyTo` 의 본문(`return true;`)은 **그대로 둔다** — 이 헬퍼를 기본 구현에 밀어 넣으면 기존 4개 override 의 동작까지 바뀌어 회귀 위험이 생긴다.

**[2] MeasurementBase.cs — `Load` override(L180) 아래에 `CopyTo` 추가**

```csharp
// 복사 제외 목록: 이름(붙여넣기 시 이름 충돌 방지) + 런타임 측정 결과(다음 검사 사이클에서 다시 채워짐)
//  + IDatumOriginConsumer 주입 필드(Action_FAIMeasurement 가 매 사이클 datum 절대좌표를 새로 주입).
//  뒤 6개는 MeasurementBase 자신에는 없고 하위 9개 타입에만 있다. 이름 기반 제외라 해당 필드가
//  없는 타입에서는 그냥 무시되므로, 하위 타입마다 흩어 놓지 않고 여기 한 곳에 모아 둔다.
private static readonly HashSet<string> _copyExclude = new HashSet<string> {
    "MeasurementName",
    "LastMeasuredValue", "LastJudgement", "LastHasResult", "LastSkipReason",
    "DatumOriginRow", "DatumOriginCol", "DatumAngleRad", "DatumAngle2Rad",
    "DatumDetectedCircleRow", "DatumDetectedCircleCol"
};

// 기존 override 4개(CameraMasterParam/CameraParam/CameraSlaveParam/ShotConfig)와 동일한 형태.
//  필드 복사는 ParamBase 의 reflection 헬퍼가 런타임 타입 기준으로 수행하므로,
//  파생 측정 타입 15종의 고유 필드까지 별도 override 없이 전부 복사된다.
public override bool CopyTo(ParamBase param) {
    if (param == null) return false;
    // 서로 다른 측정 타입끼리는 필드 의미가 달라 복사 불가 — paste 는 'Fail to Copy' 로 거부된다.
    if (param.GetType() != GetType()) return false;
    MeasurementBase target = param as MeasurementBase;
    if (target == null) return false;
    base.CopyTo(param);
    CopyPublicPropertiesTo(target, _copyExclude);
    return true;
}
```

`MeasurementBase.cs` 는 이미 `System`, `System.Collections.Generic` 을 using 중 → 추가 불필요.

**[3] FAIConfig.cs — `ClearResult()`(L142) 아래에 `CopyTo` 추가**

```csharp
// 복사 제외: 이름(충돌 방지) + 런타임 결과 필드.
//  LastOverlays 는 List 라 reflection type switch 밖 → 자동 제외된다.
private static readonly HashSet<string> _copyExclude = new HashSet<string> {
    "FAIName",
    "MeasuredValue", "IsPass", "WasDatumSkipped",
    "LastOriginImageFileName", "LastCaptureImageFileName"
};

public override bool CopyTo(ParamBase param) {
    FAIConfig target = param as FAIConfig;
    if (target == null) return false;
    base.CopyTo(param);
    CopyPublicPropertiesTo(target, _copyExclude);

    // Measurements 깊은 복사 — 참조를 공유하면 한쪽 편집이 다른 쪽에 번지므로 새 인스턴스를 만든다.
    //  MeasurementName 은 _copyExclude 로 빠지므로 여기서 직접 옮긴다.
    target.ClearMeasurements();
    for (int i = 0; i < Measurements.Count; i++) {
        MeasurementBase srcMeas = Measurements[i];
        MeasurementBase dstMeas = target.AddMeasurement(srcMeas.TypeName);
        if (dstMeas == null) continue;   // 알 수 없는 TypeName → factory 가 null
        dstMeas.MeasurementName = srcMeas.MeasurementName;
        srcMeas.CopyTo(dstMeas);
    }
    return true;
}
```

`FAIConfig.cs` 는 이미 `System.Collections.Generic` 을 using 중 → 추가 불필요.

주의: `FAIName` 을 복사하지 않는 이유는 `ShotConfig.ShotName` 을 제외하는 이유와 같다(같은 Shot 안에서 이름이 겹치면 트리/INI 그룹키가 충돌). Shot 단위 복사에서는 Task 2 가 `AddFAI(srcFai.FAIName)` 로 이름을 직접 넘겨 보존한다.
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && P=WPF_Example/Sequence/Param/ParamBase.cs && M=WPF_Example/Custom/Sequence/Inspection/MeasurementBase.cs && F=WPF_Example/Custom/Sequence/Inspection/FAIConfig.cs; echo "--- 신설 (각 want >=1) ---"; echo "helper=$(grep -cF 'protected int CopyPublicPropertiesTo' "$P")"; echo "helper_owner_guard=$(grep -cF 'prop.Name == \"Owner\"' "$P")"; echo "helper_parent_guard=$(grep -cF 'prop.Name == \"Parent\"' "$P")"; echo "helper_canwrite=$(grep -cF '!prop.CanWrite' "$P")"; echo "helper_indexer=$(grep -cF 'GetIndexParameters().Length > 0' "$P")"; echo "meas_copyto=$(grep -cF 'public override bool CopyTo' "$M")"; echo "meas_typeguard=$(grep -cF 'param.GetType() != GetType()' "$M")"; echo "fai_copyto=$(grep -cF 'public override bool CopyTo' "$F")"; echo "fai_clearmeas=$(grep -cF 'target.ClearMeasurements()' "$F")"; echo "fai_addmeas=$(grep -cF 'target.AddMeasurement(srcMeas.TypeName)' "$F")"; echo "fai_measname=$(grep -cF 'dstMeas.MeasurementName = srcMeas.MeasurementName' "$F")"; echo "--- MeasurementBase 제외목록 11개 전수 (각 want 1) ---"; for k in MeasurementName LastMeasuredValue LastJudgement LastHasResult LastSkipReason DatumOriginRow DatumOriginCol DatumAngleRad DatumAngle2Rad DatumDetectedCircleRow DatumDetectedCircleCol; do echo "M_$k=$(grep -cF "\"$k\"" "$M")"; done; echo "--- FAIConfig 제외목록 6개 전수 (각 want 1) ---"; for k in FAIName MeasuredValue IsPass WasDatumSkipped LastOriginImageFileName LastCaptureImageFileName; do echo "F_$k=$(grep -cF "\"$k\"" "$F")"; done; echo "--- ParamBase.CopyTo 기본 구현 보존 (want 1) ---"; echo "base_copyto=$(grep -cF 'public virtual bool CopyTo' "$P")"; echo "--- 하위 측정 15타입 미수정 (want: 아래 목록 비어야 함) ---"; git diff --name-only -- WPF_Example/Custom/Sequence/Inspection/Measurements/; echo "MEASUREMENTS_DIR_MUST_BE_EMPTY_ABOVE"; echo "--- C# 7.2/컨벤션 위반 신규 라인 (want 0) ---"; git diff -U0 -- "$P" "$M" "$F" | grep '^+' | grep -cE '[$]"|=>|switch[[:space:]]*[{]|[^?]\?[^?.]*:[^:]'; echo "--- 기존 라인 삭제 없음 (want 0) ---"; git diff -U0 -- "$P" "$M" "$F" | grep '^-' | grep -v '^---' | grep -vcE '^\-\s*$'; echo "--- build ---"; "C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj //t:Build //p:Configuration=Debug //p:Platform=x64 //v:m //nologo 2>&1 | grep -E "error CS" | head -20; echo "CS_LIST_ABOVE_MUST_BE_EMPTY"</automated>
  </verify>
  <done>
- `ParamBase.CopyPublicPropertiesTo` 가 존재하고 `Owner`/`Parent`/`!CanWrite`/인덱서를 건너뛴다.
- `ParamBase.CopyTo` 의 기존 `return true` 기본 구현은 그대로 남아 있다.
- `MeasurementBase._copyExclude` 에 11개 이름이 전부 있다 (결과 4 + 이름 1 + IDatumOriginConsumer 6).
- `MeasurementBase.CopyTo` 가 타입 불일치 시 `false` 를 반환한다.
- `FAIConfig._copyExclude` 에 6개 이름이 전부 있다.
- `FAIConfig.CopyTo` 가 `Measurements` 를 새 인스턴스로 깊은 복사하고 `MeasurementName` 을 옮긴다.
- `Measurements/` 폴더의 15개 파일은 하나도 수정되지 않았다.
- Debug|x64 빌드 error CS 0건.
  </done>
</task>

<task type="auto">
  <name>Task 2: DatumConfig.CopyTo 신설 + ShotConfig.CopyTo 에 FAIList 깊은 복사 추가</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs, WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs</files>
  <action>
**[1] DatumConfig.cs — 클래스 안(파일 끝의 마지막 메서드 뒤)에 `CopyTo` 추가**

제외 목록 31개는 **한 글자도 빼지 말고 그대로** 옮겨 적을 것. 하나라도 빠지면 남의 검출결과가 조용히 복사된다.

```csharp
// 복사 제외 목록.
//  (a) DatumName — 붙여넣기 시 이름 충돌 방지 (ShotName/FAIName 과 동일한 이유).
//  (b) 검출결과 transient 2그룹 — 대상 Datum 이 아직 검출을 안 했는데도 원본의 옛 결과가
//      화면에 남아 "검출 성공"으로 오인되는 것을 막는다. 특히 DetectedEdgeCount/DetectedFitRMSE/
//      DetectedAngleDeg 는 [Category("Datum|Result")] 로 PropertyGrid 에 실제 노출되며,
//      [ReadOnly(true)] 는 리플렉션의 CanWrite 를 막지 못하므로 반드시 이름으로 제외해야 한다.
//  (c) *_PhiDeg — radian 원본(*_Phi)이 이미 복사되므로 중복이며, 도↔라디안 왕복 오차만 생긴다.
private static readonly HashSet<string> _copyExclude = new HashSet<string> {
    // (a) 이름
    "DatumName",
    // (b-1) 티칭 검출 오버레이 transient
    "LastFindSucceeded", "RuntimeDetectFailed", "LastTeachSucceeded",
    "Line1Detected_RBegin", "Line1Detected_CBegin", "Line1Detected_REnd", "Line1Detected_CEnd",
    "Line2Detected_RBegin", "Line2Detected_CBegin", "Line2Detected_REnd", "Line2Detected_CEnd",
    "CircleCenter_Row", "CircleCenter_Col", "CircleDetected_Radius",
    // (b-2) TryFindDatum write-back transient
    "DetectedOriginRow", "DetectedOriginCol",
    "DetectedRefAngle", "DetectedRefAngle2",
    "DetectedCircleRow", "DetectedCircleCol",
    "DetectedEdgeCount", "DetectedFitRMSE", "DetectedAngleDeg",
    // (c) 도 단위 wrapper
    "PatternRoi_PhiDeg", "PatternRoi2_PhiDeg",
    "Line1_PhiDeg", "Line2_PhiDeg", "Vertical_PhiDeg",
    "Horizontal_A_PhiDeg", "Horizontal_B_PhiDeg"
};

// Datum 노드 복사/붙여넣기. 기존엔 override 가 없어 아무 필드도 옮겨지지 않았다.
//  ROI/에지 파라미터/패턴정렬/조명/기준원점(RefOrigin*, RefAngleRad)/IsConfigured 까지 전부 복사된다.
//  HTuple/enum/bool[] 필드(CurrentTransform, *_DetectedEdge*, AngleValidationStatus,
//  CircleStripSuccesses)는 reflection type switch 밖이라 자동으로 빠진다.
public override bool CopyTo(ParamBase param) {
    DatumConfig target = param as DatumConfig;
    if (target == null) return false;
    base.CopyTo(param);
    CopyPublicPropertiesTo(target, _copyExclude);
    return true;
}
```

`DatumConfig.cs` 는 이미 `System.Collections.Generic` 을 using 중 → 추가 불필요.

**[2] ShotConfig.cs — `CopyTo`(L304) 안, 마지막 `return true;`(L354) 바로 위에 FAIList 블록 추가**

기존 조명/노광 복사 코드는 **한 줄도 건드리지 말 것.** 아래 블록만 끼워 넣는다.

```csharp
            // FAI 리스트 깊은 복사. 기존엔 의도적으로 제외되어 있어, Shot 을 복사해도 검사 항목이
            //  하나도 따라오지 않았다(사용자가 100개 넘는 항목을 손으로 다시 티칭해야 했던 원인).
            //  참조 공유가 아니라 새 FAIConfig 를 만들어 채운다 — 안 그러면 한쪽 편집이 다른 쪽에 번진다.
            //  FAIName 은 FAIConfig.CopyTo 의 제외 목록에 있으므로 AddFAI 인자로 직접 넘겨 보존한다.
            target.ClearFAIs();
            for (int i = 0; i < FAIList.Count; i++) {
                FAIConfig srcFai = FAIList[i];
                FAIConfig dstFai = target.AddFAI(srcFai.FAIName);
                srcFai.CopyTo(dstFai);
            }
```

L301~303 의 기존 주석 중 `ShotName/FAIList/_image 는 ... 의도적으로 제외한다` 문구는 이제 사실과 달라지므로 **`ShotName/_image 는 이름 충돌·이미지 소유권 문제로 의도적으로 제외한다. FAIList 는 깊은 복사한다.`** 로 문구만 고친다 (코드 변경 아님).

주의: `ShotName` 제외는 **그대로 유지**한다(사용자 확정 사항). `_image` 제외도 유지 — `_image` 는 private 필드라 reflection 대상도 아니다.
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && D=WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs && S=WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs; echo "--- DatumConfig 신설 (각 want >=1) ---"; echo "datum_copyto=$(grep -cF 'public override bool CopyTo' "$D")"; echo "datum_helper_call=$(grep -cF 'CopyPublicPropertiesTo(target, _copyExclude)' "$D")"; echo "--- [a] 이름 제외 (want 1) ---"; echo "DatumName=$(grep -cF '\"DatumName\"' "$D")"; echo "--- [b-1] 티칭 검출 오버레이 transient 14개 (각 want 1) ---"; for k in LastFindSucceeded RuntimeDetectFailed LastTeachSucceeded Line1Detected_RBegin Line1Detected_CBegin Line1Detected_REnd Line1Detected_CEnd Line2Detected_RBegin Line2Detected_CBegin Line2Detected_REnd Line2Detected_CEnd CircleCenter_Row CircleCenter_Col CircleDetected_Radius; do echo "$k=$(grep -cF "\"$k\"" "$D")"; done; echo "--- [b-2] TryFindDatum write-back transient 9개 (각 want 1) === 체커 지적 핵심 ==="; for k in DetectedOriginRow DetectedOriginCol DetectedRefAngle DetectedRefAngle2 DetectedCircleRow DetectedCircleCol DetectedEdgeCount DetectedFitRMSE DetectedAngleDeg; do echo "$k=$(grep -cF "\"$k\"" "$D")"; done; echo "--- [c] 도 단위 wrapper 7개 (각 want 1) ---"; for k in PatternRoi_PhiDeg PatternRoi2_PhiDeg Line1_PhiDeg Line2_PhiDeg Vertical_PhiDeg Horizontal_A_PhiDeg Horizontal_B_PhiDeg; do echo "$k=$(grep -cF "\"$k\"" "$D")"; done; echo "--- 제외목록 총 원소 수 (want 31 = 1+14+9+7) ---"; echo "exclude_count=$(sed -n '/private static readonly HashSet<string> _copyExclude/,/};/p' "$D" | grep -oE '\"[A-Za-z0-9_]+\"' | sort -u | wc -l)"; echo "--- ShotConfig FAIList 깊은복사 (각 want >=1) ---"; echo "shot_clearfais=$(grep -cF 'target.ClearFAIs()' "$S")"; echo "shot_addfai=$(grep -cF 'target.AddFAI(srcFai.FAIName)' "$S")"; echo "shot_faicopyto=$(grep -cF 'srcFai.CopyTo(dstFai)' "$S")"; echo "--- ShotName 제외 유지 (want 0 = ShotName 대입 없음) ---"; echo "shotname_assign=$(grep -cF 'target.ShotName' "$S")"; echo "--- 기존 조명 복사 보존 (각 want 1) ---"; for k in 'target.RingLight_Brightness_1 = RingLight_Brightness_1' 'target.SideLight_Brightness_4 = SideLight_Brightness_4' 'target.Ring7Light_Brightness = Ring7Light_Brightness' 'target.CoaxLight_Brightness = CoaxLight_Brightness' 'target.PixelResolution = PixelResolution' 'target.CorrectionFactor = CorrectionFactor' 'target.DeviceName = DeviceName'; do echo "$k => $(grep -cF "$k" "$S")"; done; echo "--- 기존 라인 삭제 없음 (want <=1; 주석 문구 수정만 허용) ---"; git diff -U0 -- "$D" "$S" | grep '^-' | grep -v '^---' | grep -vcE '^\-\s*$'; echo "--- C# 7.2/컨벤션 위반 신규 라인 (want 0) ---"; git diff -U0 -- "$D" "$S" | grep '^+' | grep -cE '[$]"|=>|switch[[:space:]]*[{]|[^?]\?[^?.]*:[^:]'; echo "--- build ---"; "C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj //t:Build //p:Configuration=Debug //p:Platform=x64 //v:m //nologo 2>&1 | grep -E "error CS" | head -20; echo "CS_LIST_ABOVE_MUST_BE_EMPTY"</automated>
  </verify>
  <done>
- `DatumConfig.CopyTo` 가 존재한다.
- `DatumConfig._copyExclude` 의 고유 원소가 **정확히 31개**다 — 이름 1 + 티칭 오버레이 transient 14 + `TryFindDatum` write-back transient 9 + `*_PhiDeg` 7.
- 특히 `DetectedEdgeCount` / `DetectedFitRMSE` / `DetectedAngleDeg`(PropertyGrid 노출 항목) 3개가 제외 목록에 있다.
- `ShotConfig.CopyTo` 가 `ClearFAIs()` → `AddFAI(srcFai.FAIName)` → `srcFai.CopyTo(dstFai)` 순으로 FAI 를 깊은 복사한다.
- `target.ShotName` 대입은 여전히 없다 (사용자 확정 제외 유지).
- 기존 조명/노광/해상도 복사 7종이 전부 그대로 남아 있다.
- Debug|x64 빌드 error CS 0건.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add-Shot 회귀 가드 + 붙여넣기 후 트리 자식 노드 갱신</name>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <action>
Task 2 로 `ShotConfig.CopyTo` 가 FAIList 까지 복사하게 되면서 **기존 호출부 두 곳이 영향을 받는다.** 둘 다 이 태스크에서 처리한다.

**[1] Add-Shot(+) 경로 회귀 가드 — L1253 `siblingShot?.CopyTo(shot);` 바로 아래 1줄 추가**

이 경로는 새 Shot 을 만들 때 같은 시퀀스의 마지막 sibling Shot 에서 **조명/노광/해상도 기본값만** 물려받으려는 의도다.
Task 2 이후로는 sibling 의 FAI 전체가 딸려오므로, 바로 아래 `shot.AddFAI("FAI_0")` 와 합쳐져 "새 Shot 인데 FAI 가 N+1개" 가 된다 — 명백한 회귀.

```csharp
            siblingShot?.CopyTo(shot);
            // ShotConfig.CopyTo 가 FAIList 까지 깊은 복사하도록 바뀌었다. 여기서 필요한 건 sibling 의
            //  조명/노광 기본값뿐이고 FAI 는 바로 아래에서 새로 만든다 — 딸려온 FAI 를 비운다.
            //  이 줄이 없으면 Shot 추가 시 sibling 의 FAI 전체가 복제된다.
            shot.ClearFAIs();
```

L1242~1249 의 기존 주석 중 `ShotName/FAIList/_image는 의도적으로 제외` 문구도 사실과 달라지므로
`ShotName/_image는 제외; FAIList는 CopyTo가 복사하지만 바로 아래 ClearFAIs로 비운다` 로 문구만 고친다.

**[2] 붙여넣기 후 트리 자식 노드 갱신 — `button_paste_Click`(L895) 수정 + 헬퍼 신설**

현재 붙여넣기는 데이터만 바꾸고 트리는 그대로 둔다. FAI/Measurement 를 복사해도 트리에는 **예전 `FAIConfig` 객체를 가리키는 노드**가 남아, 사용자 눈엔 "아무 일도 안 일어난 것"처럼 보이고 편집하면 버려질 객체를 만지게 된다.

`button_paste_Click` 을 아래처럼 고친다. 기존 라인은 지우지 않고, 표시된 2줄만 추가한다.

```csharp
        private void button_paste_Click(object sender, RoutedEventArgs e) {
            if((SelectedParam != null) && (CopiedParam != null)) {
                //confirm message
                if (SelectedParam == CopiedParam) return;

                MessageBoxResult res = CustomMessageBox.ShowConfirmation("Confirmation", string.Format("Would you like to paste {0} into {1}?", CopiedParam.ToString(), SelectedParam.ToString()), MessageBoxButton.OKCancel);
                if(res != MessageBoxResult.OK) {
                    return;
                }
                // [추가 1] CopyTo 로 자식 컬렉션이 통째로 바뀌므로, 갱신할 대상 노드를 미리 잡아둔다.
                NodeViewModel pasteTargetNode = treeListBox_sequence.SelectedItem as NodeViewModel;
                //paste
                if(CopiedParam.CopyTo(SelectedParam) == false) {
                    //fail
                    CustomMessageBox.Show("Fail to Copy", string.Format("Copy Failed From {0} into {1}", CopiedParam.ToString(), SelectedParam.ToString()), MessageBoxImage.Error);
                    return;
                }
                // [추가 2] 복사된 FAI/Measurement 를 트리에 즉시 반영 (전체 RebuildTree 는 쓰지 않는다 —
                //  RebuildTree 는 seq.Actions[] 를 기준으로 돌아서, 아직 Actions[] 에 반영되지 않은
                //  이번 세션 신규 Shot 이 트리에서 사라질 수 있다).
                RefreshChildNodesAfterPaste(pasteTargetNode);
                //success
                mParentWindow.statusBar.Model.SetText(string.Format("Pasted : {0} to {1}",CopiedParam.ToString(), SelectedParam.ToString()));
                int index = treeListBox_sequence.SelectedIndex;

                //reselect (update)
                treeListBox_sequence.UnselectAll();
                treeListBox_sequence.SelectedIndex = index;
            }
        }
```

그리고 `button_paste_Click` 바로 아래에 헬퍼를 신설한다.

```csharp
        // 붙여넣기로 자식 컬렉션이 교체된 노드의 트리 자식 VM 을 다시 만든다.
        //  Shot 노드 → FAI + 그 아래 Measurement, FAI 노드 → Measurement 만 재구축한다.
        //  그 외 노드 타입(Datum/Sequence)은 자식이 없거나 바뀌지 않으므로 아무것도 하지 않는다.
        private void RefreshChildNodesAfterPaste(NodeViewModel targetNode) {
            if (targetNode == null) return;

            ShotConfig pastedShot = targetNode.Param as ShotConfig;
            if (pastedShot != null) {
                targetNode.Children.Clear();
                for (int i = 0; i < pastedShot.FAIList.Count; i++) {
                    FAIConfig fai = pastedShot.FAIList[i];
                    ViewModel.AddFAINode(targetNode, fai, targetNode.SequenceID, targetNode.ActionID);
                    NodeViewModel faiVm = targetNode.Children[targetNode.Children.Count - 1];
                    for (int j = 0; j < fai.Measurements.Count; j++) {
                        ViewModel.AddMeasurementNode(faiVm, fai.Measurements[j]);
                    }
                }
                targetNode.IsExpanded = true;
                return;
            }

            FAIConfig pastedFai = targetNode.Param as FAIConfig;
            if (pastedFai != null) {
                targetNode.Children.Clear();
                for (int j = 0; j < pastedFai.Measurements.Count; j++) {
                    ViewModel.AddMeasurementNode(targetNode, pastedFai.Measurements[j]);
                }
                targetNode.IsExpanded = true;
            }
        }
```

`AddFAINode` 는 `void` 이므로 방금 추가된 VM 은 `Children` 의 마지막 원소로 집는다 (`AddFAINode` 는 항상 `cn.Add(faiVm)` 로 뒤에 붙인다).

`InspectionListView.xaml.cs` 는 이미 `ReringProject.Sequence` / `ReringProject.UI` 를 using 중이므로 using 추가 불필요 (`NodeViewModel`, `ShotConfig`, `FAIConfig` 모두 파일 내에서 이미 사용 중).
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && V=WPF_Example/UI/ControlItem/InspectionListView.xaml.cs; echo "--- 신설 (각 want >=1) ---"; echo "guard_clearfais=$(grep -cF 'shot.ClearFAIs();' "$V")"; echo "helper_decl=$(grep -cF 'private void RefreshChildNodesAfterPaste(NodeViewModel targetNode)' "$V")"; echo "helper_call=$(grep -cF 'RefreshChildNodesAfterPaste(pasteTargetNode)' "$V")"; echo "capture_node=$(grep -cF 'NodeViewModel pasteTargetNode = treeListBox_sequence.SelectedItem as NodeViewModel' "$V")"; echo "helper_addfai=$(grep -cF 'ViewModel.AddFAINode(targetNode, fai, targetNode.SequenceID, targetNode.ActionID)' "$V")"; echo "helper_addmeas=$(grep -cF 'ViewModel.AddMeasurementNode(' "$V")"; echo "--- 순서 검증: 캡처 < CopyTo < Refresh (want: 오름차순) ---"; grep -nF 'NodeViewModel pasteTargetNode' "$V" | head -1; grep -nF 'CopiedParam.CopyTo(SelectedParam)' "$V" | head -1; grep -nF 'RefreshChildNodesAfterPaste(pasteTargetNode)' "$V" | head -1; echo "--- Add-Shot 순서: CopyTo → ClearFAIs → AddFAI (want: 오름차순) ---"; grep -nF 'siblingShot?.CopyTo(shot)' "$V"; grep -nF 'shot.ClearFAIs();' "$V"; grep -nF 'shot.AddFAI(\"FAI_0\")' "$V"; echo "--- RebuildTree 를 paste 경로에 넣지 않았는지 (want 2 = 기존 호출 2곳만) ---"; echo "rebuildtree=$(grep -cF 'ViewModel.RebuildTree()' "$V")"; echo "--- 기존 라인 삭제 없음 (want <=1; 주석 문구 수정만 허용) ---"; git diff -U0 -- "$V" | grep '^-' | grep -v '^---' | grep -vcE '^\-\s*$'; echo "--- C# 7.2/컨벤션 위반 신규 라인 (want 0) ---"; git diff -U0 -- "$V" | grep '^+' | grep -cE '[$]"|=>|switch[[:space:]]*[{]|[^?]\?[^?.]*:[^:]'; echo "--- 최종 전체 빌드 ---"; "C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj //t:Build //p:Configuration=Debug //p:Platform=x64 //v:m //nologo 2>&1 | grep -E "error CS" | head -20; echo "CS_LIST_ABOVE_MUST_BE_EMPTY"; echo "--- 이번 작업 수정 파일 목록 (want: 정확히 6개) ---"; git diff --name-only -- WPF_Example/</automated>
  </verify>
  <done>
- `siblingShot?.CopyTo(shot);` 다음 줄에 `shot.ClearFAIs();` 가 있고, 그 뒤에 `shot.AddFAI("FAI_0")` 가 온다.
- `button_paste_Click` 이 `CopyTo` 전에 대상 `NodeViewModel` 을 잡고, 성공 후 `RefreshChildNodesAfterPaste` 를 호출한다.
- `RefreshChildNodesAfterPaste` 가 Shot 노드는 FAI+Measurement 2단계, FAI 노드는 Measurement 1단계로 재구축한다.
- paste 경로에 `RebuildTree()` 를 추가하지 않았다 (기존 호출 2곳 유지).
- Debug|x64 빌드 error CS 0건, 수정 파일은 정확히 6개.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음 (신규 외부 입력 없음) | 이번 변경은 in-process 객체 간 필드 복사만 수행한다. 네트워크/파일/사용자 문자열 입력 경로가 새로 생기지 않는다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-iw0-01 | Tampering | `CopyPublicPropertiesTo` 가 `Owner`/`Parent` 를 복사해 객체 그래프를 오염 | mitigate | 헬퍼에 `Owner`/`Parent` 하드 가드 추가 (Task 1). 검증 grep 으로 존재 확인. |
| T-iw0-02 | Tampering | 얕은 복사로 원본과 대상이 같은 `FAIConfig`/`MeasurementBase` 인스턴스를 공유해 한쪽 편집이 다른 쪽에 번짐 | mitigate | `AddFAI`/`AddMeasurement` 로 새 인스턴스를 만들고 값만 옮기는 깊은 복사 (Task 1/2). |
| T-iw0-03 | Denial of Service | reflection `SetValue` 예외가 대량 발생해 동기 파일 로깅으로 UI 정지 (ParamBase.Load 11초 병목 선례) | mitigate | `CanWrite`/`CanRead`/인덱서/타입 화이트리스트 가드로 예외를 0에 수렴시킨다. 복사는 사용자 클릭 1회 경로라 핫패스도 아니다. |
| T-iw0-04 | Tampering | 타입이 다른 Measurement 간 붙여넣기로 의미가 다른 필드가 섞임 | mitigate | `MeasurementBase.CopyTo` 에서 `param.GetType() != GetType()` 이면 `false` 반환 → UI 가 'Fail to Copy' 표시 (Task 1). |
| T-iw0-05 | Tampering | Add-Shot(+) 경로가 sibling FAI 를 통째로 복제 (기능 회귀 = 데이터 오염) | mitigate | `shot.ClearFAIs()` 가드 추가 (Task 3). 순서 검증 grep 포함. |
| T-iw0-06 | Information Disclosure | 런타임 측정/검출 결과가 복사되어, 아직 검사·검출을 안 한 대상에 **남의 결과값이 그대로 표시**됨 (사용자가 성공으로 오인) | mitigate | 3개 `_copyExclude` 에 결과 필드를 전수 명시 (Task 1/2). **특히 `DatumConfig` 의 `DetectedEdgeCount`/`DetectedFitRMSE`/`DetectedAngleDeg` 는 PropertyGrid 노출 항목이고 `[ReadOnly(true)]` 가 `CanWrite` 를 막지 못하므로 반드시 이름 제외.** Task 2 verify 가 transient 23개 이름을 개별 grep 하고 총 원소 수 31을 확인한다. |
| T-iw0-07 | Information Disclosure | `IDatumOriginConsumer` 6필드(datum 절대좌표 주입값)가 복사되어 대상 측정이 남의 datum 기준으로 보임 | mitigate | `MeasurementBase._copyExclude` 에 6개 명시 (Task 1). 매 사이클 재주입되어 실질 위험은 낮으나 방어적으로 차단. |
| T-iw0-08 | Elevation of Privilege | — | accept | 권한 경계 없음. 단일 데스크톱 프로세스 내부 동작. |
</threat_model>

<verification>
전 태스크 완료 후 통합 확인.

**정적 검증 (자동)**
1. Debug|x64 MSBuild `error CS` 0건.
2. 수정 파일 정확히 6개: `ParamBase.cs`, `MeasurementBase.cs`, `FAIConfig.cs`, `DatumConfig.cs`, `ShotConfig.cs`, `InspectionListView.xaml.cs`.
3. `Measurements/` 폴더 15개 파일 무수정.
4. `_copyExclude` 원소 수: `MeasurementBase` 11개, `FAIConfig` 6개, `DatumConfig` 31개.
5. 신규 라인에 `$"` / `=>` / 삼항 `?:` / switch expression 0건.
6. 기존 라인 삭제 0건 (주석 문구 수정 2줄 제외).

**동작 확인 (SIMUL 실행 — 사용자 실기 검증용 체크리스트)**
| # | 절차 | 기대 |
|---|------|------|
| 1 | FAI 가 여러 개 있는 Shot 선택 → Copy → 다른 Shot 선택 → Paste | 대상 Shot 트리에 원본과 같은 FAI 목록 + 각 FAI 아래 Measurement 가 즉시 나타난다. Shot 이름은 대상 것 유지. |
| 2 | 1번 후 아무 FAI 선택 → PropertyGrid | ROI/에지/공차 값이 원본과 동일. |
| 3 | 1번 후 레시피 저장 → 앱 재시작 → 레시피 로드 | 붙여넣은 FAI/Measurement 가 그대로 남아 있다. |
| 4 | Shot 추가(+) 버튼 | 새 Shot 에 FAI 가 `FAI_0` **하나만** 있다 (sibling FAI 미복제). 조명/노광은 sibling 값 상속. |
| 5 | Datum 선택 → Copy → 다른 Datum 선택 → Paste | ROI/에지/조명/패턴정렬 값이 옮겨진다. Datum 이름은 대상 것 유지. |
| 5-1 | **(회귀 핵심)** 이미 Test Find 를 돌린 Datum A → 한 번도 안 돌린 Datum B 에 Paste 후, B 의 PropertyGrid `Datum|Result` 탭 확인 | `DetectedEdgeCount` / `DetectedFitRMSE` / `DetectedAngleDeg` 가 **A 의 값이 아니라 B 자신의 값(미검출이면 0)** 이다. A 값이 보이면 FAIL. |
| 6 | 서로 다른 타입의 Measurement 간 Paste (예: EdgeToLineDistance → CircleDiameter) | 'Fail to Copy' 메시지. 대상 값 무변경. |
| 7 | 같은 타입 Measurement 간 Paste | 공차/보정/ROI 값이 옮겨진다. Measurement 이름은 대상 것 유지. |
| 8 | 1번 직후 검사 1회 실행 | 붙여넣은 FAI 가 정상 측정되고 판정이 나온다 (stale 결과 미표시). |
</verification>

<success_criteria>
- `ParamBase` 에 `CopyPublicPropertiesTo` 헬퍼가 있고 `Owner`/`Parent`/읽기전용/인덱서를 건너뛴다.
- `MeasurementBase` / `FAIConfig` / `DatumConfig` 에 `CopyTo` override 가 각각 1개씩 존재한다.
- `MeasurementBase.CopyTo` 하나로 하위 15개 측정 타입의 고유 필드까지 복사된다 (하위 타입 파일 무수정).
- `FAIConfig.CopyTo` 가 `Measurements` 를, `ShotConfig.CopyTo` 가 `FAIList` 를 각각 새 인스턴스로 깊은 복사한다.
- **런타임 결과 필드가 하나도 복사되지 않는다** — `DatumConfig` 제외 목록 31개(검출결과 transient 23 + 이름 1 + PhiDeg 7), `MeasurementBase` 11개, `FAIConfig` 6개.
- `ShotConfig.CopyTo` 의 `ShotName` 제외는 유지된다.
- Add-Shot(+) 경로는 종전과 동일하게 FAI 1개짜리 Shot 을 만든다.
- 붙여넣기 직후 트리에 자식 노드가 즉시 반영된다.
- Debug|x64 빌드 error CS 0건.
- UI 신규 추가(컨텍스트 메뉴 / Ctrl+C·V 단축키) 없음 — 범위 외.
</success_criteria>

<output>
완료 후 `.planning/quick/260805-iw0-parambase-copyto-shot-fai/260805-iw0-SUMMARY.md` 를 작성한다.
</output>
