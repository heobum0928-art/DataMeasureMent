---
phase: quick-260805-iz8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs
autonomous: true
requirements: [IZ8-01]

must_haves:
  truths:
    - "SHOT 노드 속성창(PropertyGrid)의 'Shot | Simulation' 카테고리에서 SimulImagePath 입력 필드가 더 이상 보이지 않는다"
    - "같은 속성창의 다른 Shot 필드(DelayMs / ShotName / OwnerSequenceName / ZIndex 등)는 변경 전 그대로 보인다"
    - "SimulImagePath 값은 레시피 INI 에 변경 전과 동일하게 저장/로드된다 (ParamBase reflection 경로 무변경)"
    - "SIMUL/오프라인 검사(Action_FAIMeasurement), 반복검사(RepeatRunService), MainView Load 버튼의 경로 저장(IOfflineImageParam)이 변경 전과 동일하게 동작한다"
    - "툴바 button_simFolder(폴더 선택 → 전체 Shot 일괄 할당)는 코드 변경 0 — 계속 동작한다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs"
      provides: "SimulImagePath 프로퍼티에 PropertyGrid 전용 숨김 attribute 1줄 + 의도 주석"
      contains: "PropertyTools.DataAnnotations.Browsable(false)"
  key_links:
    - from: "ShotConfig.SimulImagePath"
      to: "ParamBase.Save / ParamBase.Load"
      via: "reflection GetProperties(Instance|Public) — public getter/setter 유지, Browsable 는 검사되지 않음"
      pattern: "public string SimulImagePath \\{ get; set; \\}"
    - from: "ShotConfig.GetLatestImagePath / SetLatestImagePath (IOfflineImageParam)"
      to: "SimulImagePath"
      via: "C# 코드에서 프로퍼티 직접 읽기/대입 (PropertyGrid 경유 아님)"
      pattern: "return SimulImagePath|SimulImagePath = imagePath"
    - from: "Action_FAIMeasurement / RepeatRunService / InspectionRecipeManager"
      to: "shot.SimulImagePath"
      via: "SIMUL/오프라인 이미지 로드 및 레시피 INI 직렬화"
      pattern: "SimulImagePath"
---

<objective>
`ShotConfig.SimulImagePath`(ShotConfig.cs:20-21)를 **SHOT 속성창(PropertyGrid)에서만 숨긴다.**

Purpose: 이 경로는 MainView 의 이미지 Load 버튼 / 툴바 폴더 일괄 할당 / 검사Grab 이 자동으로 채워주는 값이다. 속성창에 같은 값을 손으로 편집하는 칸이 또 뜨면 현장 작업자에게 혼란만 준다. 화면에서만 치우고 내부 값·직렬화·런타임 동작은 100% 그대로 둔다.

Output: `ShotConfig.cs` 1개 파일에 `[PropertyTools.DataAnnotations.Browsable(false)]` 1줄 + 의도 주석 1줄 추가. 그 외 변경 0, 삭제 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260805-iz8-shotconfig-simulimagepath-propertygrid/260805-iz8-CONTEXT.md
@WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs

**절대 금지 (CONTEXT 잠금 결정):**
- `SimulImagePath` 필드/프로퍼티/INI 직렬화 **삭제 금지.** 삭제하면 SIMUL/오프라인 검사(`Action_FAIMeasurement.cs` 17곳), 레시피 INI 저장/로드(`InspectionRecipeManager.cs`), 반복검사(`RepeatRunService.cs`), 크로스-Z 듀얼이미지 fallback(`DualImageEdgeDistanceMeasurement.cs`)이 전부 깨진다.
- `[System.ComponentModel.Browsable(false)]` 추가 금지.
- `[Newtonsoft.Json.JsonIgnore]` 추가 금지.
- 위 둘은 직렬화까지 차단하는 **패턴 B** 다. 이번엔 **패턴 A(PropertyTools 전용, 표시만 숨김)** 만 쓴다.
- 툴바 버튼 `button_simFolder`(`InspectionListView.xaml` / `InspectionListView.xaml.cs`) 는 **건드리지 않는다.** 이번 작업 범위 밖.
- 다른 파일 수정 금지. 새 파일/클래스/메서드 생성 금지. C# 7.2 문법만.

**작업 트리 주의 — 되돌리지 말 것:**
`WPF_Example/DatumMeasurement.csproj` 는 이미 로컬에서 수정된 상태다(`DefineConstants`에 `SIMUL_MODE` 추가 — 이 PC 전용 SIMUL 설정). **이 변경을 되돌리거나 이번 커밋에 포함하지 말 것.** `git add` 는 `ShotConfig.cs` 만 한다.

**적용할 패턴 (레퍼런스 커밋 `d7896d1` 실제 diff):**
```csharp
        [AutoUpdateText]
        //260729 hbk quick-kpy: PropertyGrid 표시만 숨김. System.ComponentModel.Browsable/JsonIgnore 는 절대 추가 금지 — ...
        [PropertyTools.DataAnnotations.Browsable(false)]
        public string TeachingImagePath_Vertical { get; set; } = "";
```
즉 **기존 attribute 블록의 맨 아래 / 프로퍼티 선언 바로 위**에 완전수식 attribute 1줄을 삽입하고, 그 위에 "왜 PropertyTools 전용인지" 한 줄 주석을 단다.

**대상 코드 (ShotConfig.cs 현재 상태, 20-21행):**
```csharp
        [Category("Shot|Simulation")]
        public string SimulImagePath { get; set; } = "";
```

**직렬화가 안전한 근거 (이미 코드로 확인됨 — 재확인 불필요):**
- `ParamBase.Save`: `GetType().GetProperties(BindingFlags.Instance | BindingFlags.Public)` 순회 → `case "String"` 저장. Browsable attribute 를 보지 않는다.
- `ParamBase.Load`: `GetType().GetProperties()` 순회, 유일한 제외 조건은 `!prop.CanWrite`. `{ get; set; }` 이므로 통과.
- `ShotConfig` 는 `ICustomTypeDescriptor` 를 구현하지 않는다(구현체는 `FAIConfig` / `DatumConfig` / `CircleDiameterMeasurement` 뿐) → PropertyTools 기본 TypeDescriptor 경로를 타므로 `PropertyTools.DataAnnotations.Browsable` 가 그대로 먹는다.
- 같은 파일의 `ZPosition`(15행) / `FAIList`(34행) / `RingLight_Enabled`(67행) 등 9곳이 이미 같은 방식으로 숨겨져 있고 정상 저장/로드된다 — 검증된 선례.

**attribute 기준선 (주석 라인 제외 집계, ShotConfig.cs):**
| 문자열 | 변경 전 | 변경 후 기대값 |
|--------|---------|----------------|
| `PropertyTools.DataAnnotations.Browsable(false)` (완전수식) | 0 | **1** |
| `[Browsable(false)]` (짧은 이름, 줄 시작) | 9 | **9 (불변)** |
| `System.ComponentModel.Browsable` | 0 | **0 (불변)** |
| `JsonIgnore` | 0 | **0 (불변)** |

**주석 정책:** 날짜 접두사(`//260805 hbk`)는 붙이지 않는다(현행 정책 — 비자명한 "왜"만 최소로). 삼항 연산자 금지 / if-else 사용 규칙은 이번 변경에 해당 사항 없음(attribute 1줄).
</context>

<tasks>

<task type="auto">
  <name>Task 1: SimulImagePath 에 PropertyGrid 전용 숨김 attribute 추가 + 값 보존 정적 게이트 + 빌드</name>
  <files>WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs</files>
  <action>
**1) 파일을 fresh Read 로 열어 현재 라인 번호를 확인한다.** (기준 시점: `[Category("Shot|Simulation")]` 20행, `public string SimulImagePath` 21행.)

**2) 변경 — `SimulImagePath` 한 곳만.**
`[Category("Shot|Simulation")]` 줄 **바로 아래**, `public string SimulImagePath { get; set; } = "";` 선언 **바로 위**에 아래 2줄을 삽입한다. 들여쓰기는 주변과 동일하게 스페이스 8칸:

```
        // PropertyGrid 표시만 숨김 — MainView Load 버튼/툴바 폴더 일괄 할당/검사Grab 이 채우고 SIMUL·오프라인 검사와 레시피 INI 가 읽는 값이라
        //  System.ComponentModel.Browsable / Newtonsoft.Json.JsonIgnore 는 절대 추가 금지(직렬화가 끊겨 값이 소실된다).
        [PropertyTools.DataAnnotations.Browsable(false)]
```
(주석은 2줄로 줄바꿈해도 되고 1줄로 합쳐도 된다. 핵심은 "왜 PropertyTools 전용인가"가 남는 것.)

attribute 는 **반드시 완전수식** `PropertyTools.DataAnnotations.Browsable(false)` 로 쓴다. 파일에 이미 있는 짧은 이름 `[Browsable(false)]` 과 동작은 같지만, 완전수식이 "이건 PropertyGrid 전용이지 직렬화 차단이 아니다"를 코드 자체로 못박아 준다(잠금 결정).

**3) 절대 하지 말 것:**
- `[System.ComponentModel.Browsable(false)]` / `[Newtonsoft.Json.JsonIgnore]` 추가 금지
- 기존 `[Category("Shot|Simulation")]` 삭제·수정 금지
- `public string SimulImagePath { get; set; } = "";` 시그니처·기본값 변경 금지 (public getter/setter 가 사라지면 ParamBase 순회에서 빠져 값이 날아간다)
- `GetLatestImagePath()` / `SetLatestImagePath()` (23-32행, `IOfflineImageParam` 구현) 손대지 말 것
- `EnsureDefaults` 계열의 `if (SimulImagePath == null) SimulImagePath = "";` 널 가드(409-410행) 손대지 말 것
- 그 외 어떤 프로퍼티도(ZPosition / DelayMs / ZIndex / OwnerSequenceName / 조명 필드 전부) 손대지 말 것
- 다른 파일 수정 금지 — 특히 `InspectionListView.xaml.cs`(button_simFolder), `MainView.xaml.cs`, `Action_FAIMeasurement.cs`, `RepeatRunService.cs`, `InspectionRecipeManager.cs` 변경 0
- `DatumMeasurement.csproj` 의 기존 로컬 변경(`SIMUL_MODE`) 되돌리기 금지

**4) 빌드.** `Debug|x64` 로 빌드한다.
MSBuild 경로: `C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe`
타깃: `WPF_Example/DatumMeasurement.csproj`, 옵션 `//t:Build //p:Configuration=Debug //p:Platform=x64 //v:m //nologo`

빌드 실패 시(특히 `Browsable` 모호성 CS0104 류) attribute 를 완전수식으로 썼는지 재확인한다.

**5) 값 보존 정적 게이트.** 아래 verify 명령이 카운트 게이트를 출력한다. `SCM=0` / `JSONIGNORE=0` 이 아니면 **즉시 되돌리고** 패턴 A 로 다시 작업한다. `DELETED=0`(순수 추가) 도 반드시 지켜져야 한다.
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && F=WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs; "C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj //t:Build //p:Configuration=Debug //p:Platform=x64 //v:m //nologo 2>&1 | grep -E "error CS|error MSB" | head -20; echo "ERR_LIST_ABOVE_MUST_BE_EMPTY"; C=$(grep -v '^[[:space:]]*//' $F); echo "PT_FQ=$(echo "$C" | grep -c 'PropertyTools\.DataAnnotations\.Browsable(false)') EXPECT=1"; echo "PT_SHORT=$(echo "$C" | grep -c '^[[:space:]]*\[Browsable(false)\]') EXPECT=9"; echo "SCM=$(echo "$C" | grep -c 'System\.ComponentModel\.Browsable') EXPECT=0"; echo "JSONIGNORE=$(echo "$C" | grep -c 'JsonIgnore') EXPECT=0"; echo "SIG=$(grep -c 'public string SimulImagePath { get; set; } = \"\"' $F) EXPECT=1"; echo "IFACE=$(grep -c 'return SimulImagePath\|SimulImagePath = imagePath' $F) EXPECT=2"; echo "DELETED=$(git diff -- $F | grep -c '^-[^-]') EXPECT=0"; echo "== changed source files (expect ShotConfig.cs + pre-existing csproj ONLY) =="; git status --porcelain -- 'WPF_Example/*' | grep -v '^??'; ls -l --time-style=full-iso bin/x64/Debug/DatumMeasurement.exe</automated>
  </verify>
  <done>
`error CS` / `error MSB` 0건. `PT_FQ=1`, `PT_SHORT=9`, `SCM=0`, `JSONIGNORE=0`, `SIG=1`, `IFACE=2`, `DELETED=0`. `git status` 결과에 `WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs` 와 (기존) `WPF_Example/DatumMeasurement.csproj` 외 수정 파일 없음. `bin/x64/Debug/DatumMeasurement.exe` 타임스탬프 갱신됨.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (해당 없음) | 이번 변경은 UI 표시 attribute 1줄 추가로, 신뢰 경계를 넘는 입력/출력이 없다. 네트워크·파일·프로세스 경계 변화 0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-IZ8-01 | Tampering | `ShotConfig.SimulImagePath` INI 직렬화 | mitigate | 잘못된 패턴 B attribute(`System.ComponentModel.Browsable` / `JsonIgnore`) 추가 시 레시피의 SIMUL 이미지 경로가 소실되어 오프라인 검사·반복검사가 무음 실패한다. Task 1 의 카운트 고정 게이트(`SCM=0` / `JSONIGNORE=0` 불변) + 시그니처 게이트(`SIG=1`) + `DELETED=0` 로 차단. |
| T-IZ8-02 | Denial of Service | SIMUL/오프라인 검사 실행 경로 | accept | 값 소실 시 검사 실패 가능성이 있으나 위 정적 게이트 + 아래 실기 확인 절차로 커버. 외부 공격 표면 아님(로컬 단일 사용자 산업 장비). |
| T-IZ8-SC | Tampering | 패키지 설치 | (해당 없음) | 신규 NuGet/npm 패키지 설치 없음. 기존 참조만 사용. |
</threat_model>

<verification>
1. **빌드**: MSBuild `Debug|x64` — `error CS` 0, `error MSB` 0, `DatumMeasurement.exe` 타임스탬프 갱신.
2. **금지 attribute 게이트**: `System.ComponentModel.Browsable` = 0 (불변), `JsonIgnore` = 0 (불변), 완전수식 `PropertyTools.DataAnnotations.Browsable(false)` = 1 (신규), 짧은 이름 `[Browsable(false)]` = 9 (불변).
3. **순수 추가 diff**: `git diff` 삭제 라인 0, 수정 소스 파일 `ShotConfig.cs` 1개(+ 기존 로컬 csproj 변경 유지).
4. **직렬화 경로 무변경**: `public string SimulImagePath { get; set; } = "";` 시그니처 유지, `IOfflineImageParam` 구현 2곳(`return SimulImagePath` / `SimulImagePath = imagePath`) 유지.
5. **실기 확인 (실행 완료 후 사용자에게 안내 — 별도 checkpoint 태스크는 두지 않음, 잠금 결정의 검증 방식):**
   - 프로그램을 완전히 종료하고 새 빌드로 다시 실행한다(안 끄면 예전 화면이 남는다).
   - 왼쪽 트리에서 아무 `SHOT_*` 노드를 클릭 → 오른쪽 속성창의 `Shot` 탭 → `Simulation` 묶음에 **`SimulImagePath` 칸이 안 보여야** 한다.
   - 같은 속성창의 `ShotName` / `DelayMs` / `ZIndex` / `OwnerSequenceName` 등 다른 칸은 **그대로 보여야** 한다.
   - 툴바의 SIMUL 폴더 선택 버튼(전체 Shot 일괄 할당)은 **그대로 동작해야** 한다(이번에 안 건드림).
   - SIMUL/오프라인 검사를 한 번 태워서 변경 전과 **동일한 이미지가 로드되고 동일한 측정 결과**가 나오는지 확인 → 값/직렬화 보존 실증.
   - 레시피 저장 후 프로그램 재시작 → 다시 오프라인 검사 정상이면 저장/로드까지 정상.
</verification>

<success_criteria>
- [ ] `ShotConfig.SimulImagePath` 에 `[PropertyTools.DataAnnotations.Browsable(false)]` 1줄 + 의도 주석 추가됨
- [ ] `[System.ComponentModel.Browsable(false)]` / `[Newtonsoft.Json.JsonIgnore]` **추가되지 않음** (카운트 0/0 불변)
- [ ] `public string SimulImagePath { get; set; } = "";` 시그니처·기본값 그대로 유지
- [ ] `GetLatestImagePath()` / `SetLatestImagePath()` / 널 가드 변경 0
- [ ] 짧은 이름 `[Browsable(false)]` 기존 9곳 변경 0 (다른 프로퍼티 미간섭)
- [ ] `button_simFolder`(`InspectionListView.xaml.cs`) 변경 0 — 스코프 밖 유지
- [ ] 수정 소스 파일 `ShotConfig.cs` 1개뿐, 삭제 라인 0
- [ ] `DatumMeasurement.csproj` 의 기존 로컬 `SIMUL_MODE` 변경 되돌림 0 / 커밋 포함 0
- [ ] MSBuild `Debug|x64` 에러 0
- [ ] (실행 후 사용자 확인) 속성창에서 SimulImagePath 칸 소멸 + SIMUL/오프라인 검사 결과 변경 전과 동일
</success_criteria>

<output>
Create `.planning/quick/260805-iz8-shotconfig-simulimagepath-propertygrid/260805-iz8-SUMMARY.md` when done.
</output>
