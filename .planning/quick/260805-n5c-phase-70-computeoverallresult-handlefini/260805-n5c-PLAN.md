---
phase: 260805-n5c-phase-70-computeoverallresult-handlefini
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
  - WPF_Example/Custom/Sequence/Inspection/BatchRunService.cs
  - WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs
autonomous: false
requirements: [N5C-01, N5C-02, N5C-03, N5C-04]

must_haves:
  truths:
    - "Top 만 단독 RUN 했을 때 종합판정이 Top 소유 shot 의 결과만 반영한다 (Side/Bottom 미실행 shot 이 NG/NotExist 를 유발하지 않는다)"
    - "Side 단독 RUN / Bottom 단독 RUN 도 각각 자기 소유 shot 만 반영한다"
    - "v2.6 TCP 응답(TestResultPacket)의 FAIResults 에 타 시퀀스 FAI 항목이 섞이지 않는다"
    - "일괄검사(BatchRunService) 종합판정도 실행 시퀀스 소유 shot 만 반영한다"
    - "반복검사(RepeatRunService) 종합판정도 실행 시퀀스 소유 shot 만 반영한다"
    - "OwnerSequenceName 이 빈 문자열인 shot 은 TOP 판정에 계속 포함된다 (실행은 되는데 판정에서 빠지는 false PASS 회귀 없음)"
    - "v1.0 프로토콜 경로(AddResponseV1Cycle / AggregateIndexFais / ComputeLastZIndex)는 코드/동작 무변경"
    - "판정 계층(anyDatumSkip > NG > OK)과 신규 설정 항목은 변경/추가 없음"
    - "Debug/x64 빌드가 error 0 / 신규 warning 0 으로 통과한다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"
      provides: "IsShotOwnedBySequence 소유권 판정 단일 진입점 + ComputeOverallResult / AddResponse(v2.6) 소유권 필터"
      contains: "public static bool IsShotOwnedBySequence"
    - path: "WPF_Example/Custom/Sequence/Inspection/BatchRunService.cs"
      provides: "HandleFinish 종합판정 소유권 필터 (+ 삼항연산자 제거)"
      contains: "InspectionSequence.IsShotOwnedBySequence"
    - path: "WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs"
      provides: "HandleFinish 종합판정 소유권 필터 (+ 삼항연산자 제거)"
      contains: "InspectionSequence.IsShotOwnedBySequence"
  key_links:
    - from: "InspectionSequence.ComputeOverallResult"
      to: "InspectionSequence.IsShotOwnedBySequence"
      via: "foreach 루프 선두 소유권 게이트"
      pattern: "IsShotOwnedBySequence\\(shot, Name\\)"
    - from: "InspectionSequence.AddResponse (v2.6 경로)"
      to: "InspectionSequence.IsShotOwnedBySequence"
      via: "foreach 루프 선두 소유권 게이트"
      pattern: "IsShotOwnedBySequence\\(shot, Name\\)"
    - from: "BatchRunService.HandleFinish / RepeatRunService.HandleFinish"
      to: "InspectionSequence.IsShotOwnedBySequence"
      via: "_seq.Name 을 루프 앞에서 1회 산출 후 소유권 게이트로 전달"
      pattern: "InspectionSequence\\.IsShotOwnedBySequence\\(shot, seqName\\)"
    - from: "IsShotOwnedBySequence"
      to: "SequenceHandler.SEQ_TOP"
      via: "빈 OwnerSequenceName → TOP 폴백 (RebuildInspectionActions / CycleResultSerializer 와 동일 정책)"
      pattern: "SequenceHandler\\.SEQ_TOP"
---

<objective>
종합판정(cycle judgement) 코드가 `recipeManager.Shots`(Top/Side/Bottom 전체 SHOT 이 담긴 **글로벌 단일 리스트**)를 소유권 필터 없이 전역 순회해, 단독 시퀀스 RUN 시 **이번에 돌지도 않은 다른 시퀀스의 shot**(`FAIConfig.IsPass` 기본값 `false`)이 섞여 잘못된 NG/NotExist 가 나오는 버그를 고친다.

Purpose: 실행 스코프(한 번에 한 시퀀스만 RUN)와 판정 스코프(전역)의 비대칭 제거. 런 시작 리셋(`HandleRunStartResetResults`)과 cycle.json 직렬화(`CycleResultSerializer.BuildDto(seqName)`)는 이미 시퀀스 스코프인데 **종합판정만 전역**이라 결과가 서로 모순되던 상태를 정합시킨다.

Output: `bool bOwnedByThisSeq = ...; if (!bOwnedByThisSeq) continue;` 소유권 게이트를 4개 판정 지점에 이식 + 소유권 판정 단일 진입점 헬퍼 1개 신설.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-judgement-scope/70-CONTEXT.md
@CLAUDE.md

수정 대상:
@WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs
@WPF_Example/Custom/Sequence/Inspection/BatchRunService.cs
@WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs

<interfaces>
아래는 **이미 코드베이스에 존재하는 계약**이다. 실행자는 이걸 그대로 쓰고 추가 탐색하지 말 것.

SequenceHandler (WPF_Example/Custom/Sequence/SequenceHandler.cs):

```csharp
public const string SEQ_TOP    = "TOP";
public const string SEQ_SIDE   = "SIDE";
public const string SEQ_BOTTOM = "BOTTOM";
public static string ResolveSequenceName(ESequence seqId);   // Top→"TOP", Side→"SIDE", Bottom→"BOTTOM", default→"TOP"
```

세 InspectionSequence 인스턴스는 각각 `Name` = `"TOP"` / `"SIDE"` / `"BOTTOM"` 으로 생성된다(SequenceHandler.cs:189~193).
즉 `SequenceBase.Name` 과 `ShotConfig.OwnerSequenceName` 은 **같은 문자열 도메인**이다.

ShotConfig (WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs):

```csharp
public string OwnerSequenceName { get; set; } = "";   // "TOP" / "SIDE" / "BOTTOM"
public void ApplyShotDefaults();                      // 빈값이면 "TOP" 으로 정규화. INI 로드 직후 호출됨
public List<FAIConfig> FAIList { get; }
```

FAIConfig:

```csharp
public bool IsPass { get; set; }             // 기본값 false — "미측정이 NG 로 보이는" 원인
public bool WasDatumSkipped { get; set; }    // true → cycle NotExist
```

기존 소유권 필터 패턴 (그대로 이식할 원본, InspectionSequence.ComputeLastZIndex L384~388):

```csharp
bool bOwnedByThisSeq = shot.OwnerSequenceName == Name;
if (!bOwnedByThisSeq)
{
    continue;
}
```

빈값 폴백 정책이 **이미 적용된 곳** (판정도 여기 맞춰야 함):

```csharp
// SequenceHandler.RebuildInspectionActions (L255) — 어떤 시퀀스가 그 shot 을 실제로 "실행"하는지 결정
string shotOwner = string.IsNullOrEmpty(shot.OwnerSequenceName) ? SEQ_TOP : shot.OwnerSequenceName;

// CycleResultSerializer.BuildDto (L66~67) — cycle.json 에 어떤 shot 을 담을지 결정
if (string.IsNullOrEmpty(shot.OwnerSequenceName)) shotOwner = "TOP";
else shotOwner = shot.OwnerSequenceName;
```
</interfaces>
</context>

<decision_traceability>
- **D-01** (전역 순회는 버그로 확정) → 이 plan 전체의 전제.
- **D-02-1** → Task 1 의 `ComputeOverallResult` (N5C-01)
- **D-02-3** → Task 1 의 `AddResponse()` v2.6 경로 (N5C-03)
- **D-02-2** → Task 2 Part A 의 `BatchRunService.HandleFinish` (N5C-02)
- **(계획 중 발견 / D-02 확장)** → Task 2 Part B 의 `RepeatRunService.HandleFinish` (N5C-04). 아래 `<scope_note>` 참조.
- **D-03** (즉시 적용, 설정 플래그로 켜고끄지 않음) → 신규 설정 항목 0, 게이팅 0.
- **D-04** (SIMUL 다중 시퀀스 재현 테스트) → Task 3 체크포인트 2단계.
- **D-05** (에이전트 코드 리뷰 필수) → Task 3 체크포인트 1단계.
</decision_traceability>

<scope_note>
## 계획 중 발견 — D-02 의 3곳 + 1곳 (N5C-04)

`RepeatRunService.HandleFinish` (RepeatRunService.cs:153-207, 특히 **L172 `foreach (var shot in recipeManager.Shots)`**) 이
`BatchRunService.HandleFinish` 와 **거의 바이트 단위로 동일한 전역 순회**를 하고 있다.
이 경로는 죽은 코드가 아니라 `ReviewerWindow.xaml.cs:390-409` 의 **반복검사(Repeat) 기능**이 실제로 사용한다.

- CONTEXT.md D-02 는 3곳만 열거했으나, D-01("전역 순회 = 버그") 판정은 이 4번째 사본에도 그대로 적용된다.
- 빼면 **반복검사에서만 같은 오판정이 살아남는다** → 나중에 "고쳤다는데 반복검사는 왜 아직 NG?" 로 재발.
- 추가 비용은 같은 모양 6줄 수정 1건이라 사실상 0.

**개발자가 이 확장을 원하지 않으면 Task 2 의 Part B 만 빼고 실행하면 된다** (다른 부분과 완전 독립).
</scope_note>

<tasks>

<task type="auto">
  <name>Task 1: InspectionSequence — 소유권 판정 헬퍼 신설 + 판정 2곳(ComputeOverallResult, AddResponse v2.6)에 게이트 이식</name>
  <files>WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs</files>
  <action>
**(A) 소유권 판정 단일 진입점 헬퍼 신설** — `ComputeOverallResult` (현재 L342) **바로 위**에 아래 메서드를 그대로 추가한다.

```csharp
        //260805 hbk Phase 70 D-02: 종합판정용 shot 소유권 판정 단일 진입점.
        //  이 시퀀스(szSeqName)가 소유한 shot 인지만 판단한다. 판정 로직/계층은 건드리지 않는다.
        //  빈 OwnerSequenceName 은 TOP 폴백 — SequenceHandler.RebuildInspectionActions(L255) /
        //  CycleResultSerializer.BuildDto(L66) 와 동일 정책. 이 폴백이 없으면 UI 로 갓 추가된
        //  (아직 저장/재로드 전이라 OwnerSequenceName 이 빈값인) shot 을 TOP 이 "실행은 하는데
        //  판정에서는 제외" 하게 되어 NG 가 OK 로 보이는 반대 방향 회귀가 생긴다.
        //  szSeqName 이 비면 소유 시퀀스 미상 → 레거시 전역 동작 유지(과대판정 방향 = 안전).
        public static bool IsShotOwnedBySequence(ShotConfig shot, string szSeqName)
        {
            bool bIsNull = shot == null;
            if (bIsNull)
            {
                return false;
            }
            bool bUnknownSeq = string.IsNullOrEmpty(szSeqName);
            if (bUnknownSeq)
            {
                return true;
            }
            string szOwner;
            if (string.IsNullOrEmpty(shot.OwnerSequenceName))
            {
                szOwner = SequenceHandler.SEQ_TOP;
            }
            else
            {
                szOwner = shot.OwnerSequenceName;
            }
            bool bOwned = szOwner == szSeqName;
            return bOwned;
        }
```

**(B) `ComputeOverallResult` (현재 L342-359, D-02-1 / N5C-01)** — `foreach (var shot in recipeManager.Shots)` 루프 **선두**에 게이트 삽입. 그 외에는 한 글자도 바꾸지 말 것(판정 계층/반환값/시그니처 유지).

```csharp
                foreach (var shot in recipeManager.Shots)
                {
                    //260805 hbk Phase 70 D-02-1: 이 시퀀스 소유 shot 만 집계. 단독 RUN 시 이번에 돌지도 않은
                    //  타 시퀀스 shot(IsPass 기본값 false)이 섞여 잘못된 NG/NotExist 가 나오던 버그 수정.
                    bool bOwnedByThisSeq = IsShotOwnedBySequence(shot, Name);
                    if (!bOwnedByThisSeq)
                    {
                        continue;
                    }
                    foreach (var fai in shot.FAIList)
                    {
                        // (기존 본문 그대로)
                    }
                }
```

**(C) `AddResponse()` v2.6 레거시 경로 (현재 L144 `foreach (var shot in recipeManager.Shots) {`, D-02-3 / N5C-03)** — 동일 게이트 삽입. 이 블록은 K&R 스타일(`{` 같은 줄)이므로 **주변 스타일에 맞춰** 작성한다.

```csharp
            foreach (var shot in recipeManager.Shots) {
                //260805 hbk Phase 70 D-02-3: 이 시퀀스 소유 shot 만 집계/전송. 전역 순회는 타 시퀀스의
                //  미측정 FAI 로 cycle 판정(anyDatumSkip/allPass)과 FAIResults 를 동시에 오염시킨다.
                //  v2.6 wire 포맷 무변경 — FAIResults 는 원래 가변 길이(count 필드로 전달)다.
                bool bOwnedByThisSeq = IsShotOwnedBySequence(shot, Name);
                if (!bOwnedByThisSeq) continue;
                foreach (var fai in shot.FAIList) {
                    // (기존 본문 그대로)
                }
            }
```

**절대 건드리지 말 것:**
- `AddResponseV1Cycle` / `AggregateIndexFais` / `ComputeLastZIndex` / `AddFaiResult` — v1.0 경로는 이미 필터링됨(무변경).
- 판정 계층 `anyDatumSkip > NG > OK` 순서, `EVisionResultType` 매핑, `pMyContext.ResultInfo` 대입.
- `CycleResultSerializer.BuildDto(...)` 호출 인자 (이미 `Name` 을 넘겨 shot 목록을 스코프하고 있다).
- `HandleRunStartResetResults` / `CountFlowCycleResults` (이미 `Actions` 순회 = 시퀀스 스코프).

**코딩 규약(필수):** C# 7.2 / 삼항연산자 `?:` **금지 → if-else** / 헝가리언 접두사(`b`, `n`, `sz`) / 신규 메서드 30-40줄 이내 / 주석은 비자명한 "왜"만.
  </action>
  <verify>
    <automated>grep -c "IsShotOwnedBySequence" "C:/Info/Project/DataMeasurement/.claude/worktrees/dazzling-hugle-c13cb4/WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"</automated>
    <automated>grep -c "AddResponseV1Cycle\|AggregateIndexFais\|ComputeLastZIndex" "C:/Info/Project/DataMeasurement/.claude/worktrees/dazzling-hugle-c13cb4/WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs"</automated>
  </verify>
  <done>
- 첫 명령 출력 = **3** (헬퍼 정의 1 + 호출 2: ComputeOverallResult, AddResponse v2.6).
- `git diff -- WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs` 의 **삭제 라인 0** (순수 추가만).
- v1.0 경로 심볼 등장 횟수가 수정 전과 동일(두 번째 명령 출력 불변).
- 신규 삼항연산자 0개.
  </done>
</task>

<task type="auto">
  <name>Task 2: BatchRunService + RepeatRunService — HandleFinish 종합판정 게이트 이식 + 삼항 제거 + Debug/x64 빌드</name>
  <files>WPF_Example/Custom/Sequence/Inspection/BatchRunService.cs, WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs</files>
  <action>
**(Part A) `BatchRunService.HandleFinish` (현재 L78-147, D-02-2 / N5C-02)**

A-1. `string seqName` 선언을 **루프 앞으로 올린다**(현재는 L126 에서 삼항으로 선언 — 삼항 금지 규칙에도 걸린다):

```csharp
                //260805 hbk Phase 70 D-02-2: 이 배치를 실제로 돌린 시퀀스 이름. 아래 종합판정 스코프와
                //  BuildDto 의 shot 스코프가 같은 기준을 쓰도록 한 곳에서만 산출한다.
                string seqName;
                if (_seq != null)
                {
                    seqName = _seq.Name;
                }
                else
                {
                    seqName = null; // 소유 시퀀스 미상 → IsShotOwnedBySequence 가 레거시 전역 동작 유지
                }
```

A-2. `foreach (var shot in recipeManager.Shots)` (현재 L96) 선두에 게이트 삽입:

```csharp
                foreach (var shot in recipeManager.Shots)
                {
                    //260805 hbk Phase 70 D-02-2: 이 시퀀스 소유 shot 만 종합판정에 포함.
                    bool bOwnedByThisSeq = InspectionSequence.IsShotOwnedBySequence(shot, seqName);
                    if (!bOwnedByThisSeq)
                    {
                        continue;
                    }
                    foreach (var fai in shot.FAIList)
                    {
                        // (기존 본문 그대로)
                    }
                }
```

A-3. 기존 L126 `string seqName = _seq != null ? _seq.Name : null;` **줄을 삭제**한다(위에서 이미 선언됨). 바로 아래 `CycleResultSerializer.BuildDto(recipeManager, resultType, DateTime.Now, recipeName, seqName)` 호출은 **그대로** 둔다.

**(Part B) `RepeatRunService.HandleFinish` (현재 L153-207, N5C-04 — `<scope_note>` 의 확장분)**

Part A 와 **완전히 동일한 3단계**를 적용한다:
- B-1. `string seqName` 을 if-else 로 루프 앞에 선언 (주석 태그는 `//260805 hbk Phase 70 N5C-04:`).
- B-2. `foreach (var shot in recipeManager.Shots)` (현재 L172) 선두에 `InspectionSequence.IsShotOwnedBySequence(shot, seqName)` 게이트 삽입.
- B-3. 기존 L202 `string seqName = _seq != null ? _seq.Name : null;` 삭제.
- `CycleResultSerializer.SaveAsync(dto)` 호출(L207)은 **그대로** 둔다.

**두 파일 모두 절대 건드리지 말 것:** `TriggerNext` / `StartBatch` / `StartFromImages` / `Stop` / `lock (_lock)` 구조 / `OnProgressChanged`·`OnBatchComplete`·`OnRepeatComplete` 발화 순서 / `CompletedCount` 증가 규칙 / `SaveAsync` 호출 유무(Batch=미호출, Repeat=호출 — 현행 유지).

**(Part C) 빌드 검증**

Debug/x64 MSBuild 를 돌려 `error CS` 0 / 신규 warning 0 을 확인한다.

> 주의: 앱(`DatumMeasurement.exe`)이 실행 중이면 링크/복사 단계에서 파일 잠금(MSB3021/MSB3027)이 날 수 있다.
> 그건 **컴파일 실패가 아니다** — 실행 중인 앱을 종료 후 재시도하거나, 최소한 `error CS` 가 0인지로 컴파일 성공을 판정하고 잠금 사실을 SUMMARY 에 남긴다.

**코딩 규약(필수):** C# 7.2 / 삼항 `?:` 금지 → if-else / 헝가리언(`b`, `sz`) / 주석은 비자명한 "왜"만.
  </action>
  <verify>
    <automated>grep -c "InspectionSequence.IsShotOwnedBySequence" "C:/Info/Project/DataMeasurement/.claude/worktrees/dazzling-hugle-c13cb4/WPF_Example/Custom/Sequence/Inspection/BatchRunService.cs" "C:/Info/Project/DataMeasurement/.claude/worktrees/dazzling-hugle-c13cb4/WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs"</automated>
    <automated>grep -c "_seq != null ? _seq.Name" "C:/Info/Project/DataMeasurement/.claude/worktrees/dazzling-hugle-c13cb4/WPF_Example/Custom/Sequence/Inspection/BatchRunService.cs" "C:/Info/Project/DataMeasurement/.claude/worktrees/dazzling-hugle-c13cb4/WPF_Example/Custom/Sequence/Inspection/RepeatRunService.cs"</automated>
    <automated>"/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "C:/Info/Project/DataMeasurement/.claude/worktrees/dazzling-hugle-c13cb4/WPF_Example/DatumMeasurement.csproj" //p:Configuration=Debug //p:Platform=x64 //v:minimal //nologo 2>&1 | tail -20</automated>
  </verify>
  <done>
- 첫 명령: 두 파일 각각 **1** (게이트 1개씩).
- 두 번째 명령: 두 파일 각각 **0** (삼항 잔존 없음).
- MSBuild: `error CS` 0건, 신규 warning 0건 (pre-existing warning 만 잔존). 파일 잠금 오류는 컴파일 성공과 구분해 기록.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 에이전트 코드 리뷰(D-05) + SIMUL 다중 시퀀스 재현 검증(D-04)</name>
  <files>(코드 변경 없음 — 검증 전용 체크포인트)</files>
  <action>
여기서 **실행을 멈추고** 아래 `<what-built>` / `<how-to-verify>` 내용을 사용자에게 그대로 제시한다.
실행자가 임의로 통과 처리하지 말 것 — 1단계(에이전트 코드 리뷰)와 2단계(SIMUL 실측)는 사람이 수행/확인해야 완료다.
사용자가 어긋난 항목을 보고하면 해당 지점만 고치고 이 체크포인트를 다시 제시한다.
  </action>
  <what-built>
`recipeManager.Shots` 전역 순회로 종합판정을 하던 4개 지점에 "이 시퀀스가 소유한 shot 만" 필터를 넣었습니다.

쉬운 설명: 카메라가 Top / Side / Bottom 세 대인데, 검사 항목(SHOT) 목록은 세 대 것이 **한 바구니**에 같이 담겨 있습니다.
지금까지는 Top 만 검사를 돌려도 마지막 합격/불합격 도장을 찍을 때 **바구니 전체**를 봤습니다.
Side/Bottom 항목은 이번에 아예 측정을 안 했으니 값이 비어 있고, 프로그램은 그 빈 값을 "불합격"으로 읽어서
Top 이 다 잘 나왔는데도 최종 결과가 NG(또는 검출실패 N)로 찍혔습니다.
이번 수정은 도장을 찍기 전에 "지금 돌린 카메라 것만 골라내는" 체를 4곳에 끼운 것입니다.

수정한 4곳:
1. `InspectionSequence.ComputeOverallResult` — 수동 RUN 종합판정 (D-02-1)
2. `InspectionSequence.AddResponse()` v2.6 경로 — 실제 PLC/핸들러로 나가는 TCP 응답 (D-02-3)
3. `BatchRunService.HandleFinish` — 일괄검사 종합판정 (D-02-2)
4. `RepeatRunService.HandleFinish` — 반복검사 종합판정 (계획 중 발견, scope_note 참조)

부수 효과 2가지(의도된 것):
- v2.6 TCP 응답의 `FAIResults` 목록에 이제 **돌린 시퀀스의 FAI 만** 담깁니다(항목 수가 줄어듭니다. wire 포맷은 원래 가변 길이라 무변경).
- v1.0 프로토콜 경로 · 판정 계층 · 설정 항목은 **하나도 안 건드렸습니다.**
  </what-built>
  <how-to-verify>
### 1단계 — 에이전트 코드 리뷰 (D-05, 필수)

`/gsd-code-review` 를 실행해 이번 diff(3파일)를 검토받습니다. 특히 아래를 봐 달라고 지정하세요.

- 4곳의 게이트가 **판정 집계 루프 안**(`anyDatumSkip` / `allPass` 를 건드리는 루프)에 정확히 들어갔는가.
- v1.0 경로(`AddResponseV1Cycle` / `AggregateIndexFais` / `ComputeLastZIndex`)에 **변경이 하나도 없는가.**
- 빈 `OwnerSequenceName` shot 이 TOP 판정에서 **빠지지 않는가**(false PASS 방지 — `IsShotOwnedBySequence` 의 TOP 폴백).
- 삼항연산자 잔존 0 / 헝가리언 표기 준수 / C# 7.2 문법.

리뷰 지적이 나오면 고친 뒤 2단계로 갑니다.

### 2단계 — SIMUL 다중 시퀀스 재현 검증 (D-04, 필수)

준비: `SIMUL_MODE`(Debug 빌드) 로 실행. **Top / Side / Bottom 세 시퀀스 모두에 SHOT 이 하나 이상 있는 레시피**를 로드합니다.
(트리에서 TOP / SIDE / BOTTOM 노드 밑에 각각 SHOT 이 보이면 준비 완료입니다.)

**(a) 수정 전 증상 재현 — 권장(선택)**
   - 이전 빌드(수정 전 exe)가 남아 있으면 그걸로 먼저 확인합니다. 없으면 건너뛰고 (b) 만 해도 됩니다.
   - 앱 시작 직후(Side/Bottom 은 한 번도 RUN 하지 않은 상태)에서 **Top 만 RUN**.
   - Top 항목이 전부 정상(합격)인데도 최종 종합판정이 **NG 또는 검출실패(N)** 로 뜨는지 확인 → 이게 이번에 잡는 버그입니다.

**(b) 수정 후 정상 동작 확인 — 필수**
   1. 앱을 **완전히 종료** 후 새 빌드로 재실행(안 끄면 예전 exe 가 그대로 돌 수 있습니다).
   2. 같은 레시피 로드. Side/Bottom 은 **이번 세션에 한 번도 RUN 하지 않습니다.**
   3. **Top 만 RUN** → 종합판정이 **Top 자체 결과대로** 나오는지 확인 (Top 이 전부 합격이면 OK/합격).
   4. Top 항목 하나를 일부러 공차 밖으로 만들거나 NG 이미지로 돌려서 **NG 가 정상적으로 NG 로 뜨는지** 확인
      (필터가 과하게 걸려 무조건 OK 로 바뀌는 반대 방향 오류가 없는지 = false PASS 확인. **이 항목이 가장 중요합니다.**)
   5. 이번엔 **Side 만 RUN** → Side 자체 결과대로 나오는지 확인 (Top 결과가 남아 있어도 Side 판정에 안 섞여야 함).
   6. **Bottom 만 RUN** 도 동일하게 확인.

**(c) 다른 실행 경로 회귀 확인**
   7. 트리에서 SHOT 여러 개 선택 → **일괄검사** 실행 → 종합판정이 그 시퀀스 결과대로 나오는지 확인 (N5C-02).
   8. 리뷰어 창의 **반복검사**(이미지 폴더 반복) 실행 → 종합판정이 그 시퀀스 결과대로 나오는지 확인 (N5C-04).
   9. `cycle.json`(결과 저장 폴더)을 열어 **shots 목록에 담긴 시퀀스**와 **최상단 result** 가 서로 맞는지 확인
      — 수정 전에는 shots 는 Top 만인데 result 는 전역 판정이라 둘이 어긋나 있었습니다.

**(d) PLC 통신 경로 (실장비 있을 때만)**
   10. `UseProtocolV1 = false`(v2.6) 상태에서 핸들러 $TEST 를 한 번 태워, 응답의 판정 문자와 FAI 항목 수가
       **돌린 시퀀스 것만** 오는지 확인. 실장비가 없으면 carry-over 로 남기고 SUMMARY 에 적습니다.
  </how-to-verify>
  <done>
- 1단계 코드 리뷰 blocker 0.
- 2단계 (b)-3 / (b)-4 / (b)-5 / (b)-6 전부 PASS (사용자 육안 확인).
- (c)-7 / (c)-8 / (c)-9 회귀 없음. (d)-10 은 실장비 가용 시 PASS, 아니면 carry-over 명시.
  </done>
  <resume-signal>"approved" 라고 답하거나, 어긋난 항목의 번호와 실제 화면 결과를 알려주세요.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 비전 PC → 핸들러/PLC (TCP v2.6) | `AddResponse()` 가 만드는 `TestResultPacket` 이 이 경계를 넘는다. 이번 변경으로 **판정 문자와 FAIResults 항목 수가 달라진다**(더 정확해지는 방향). |
| 레시피(INI) → 판정 엔진 | `ShotConfig.OwnerSequenceName` 문자열이 판정 스코프를 결정하게 된다 — 이 필드가 비거나 오염되면 판정 대상 집합이 바뀐다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-N5C-01 | Tampering (오판정) | `IsShotOwnedBySequence` 빈값 처리 | mitigate | 빈 `OwnerSequenceName` 을 그냥 `== Name` 으로 비교하면 UI 로 갓 추가된 shot(저장 전 빈값)이 TOP 에서 **실행은 되는데 판정에서 빠져** NG 가 OK 로 둔갑한다(false PASS = 불량 유출). SEQ_TOP 폴백을 헬퍼에 내장 + Task 3 (b)-4 에서 "일부러 NG 만들어 NG 로 뜨는지" 실측 확인. |
| T-N5C-02 | Information Disclosure / 계약 변경 | v2.6 `TestResultPacket.FAIResults` | accept | 항목 수 감소는 의도된 수정(원래 타 시퀀스의 미측정 값이 섞여 나가고 있었음). 포맷은 원래 가변 길이(count 필드 동반)라 wire 호환 유지. 실장비 확인은 Task 3 (d) 로 분리, 미가용 시 carry-over. |
| T-N5C-03 | Denial of Service | 판정 루프 예외 | accept | 추가 코드는 null 가드 + 문자열 비교뿐이라 신규 예외 경로 없음. `shot == null` 을 헬퍼가 `false` 로 처리해 기존 NRE 가능성을 오히려 줄인다. |
| T-N5C-04 | Repudiation (결과 기록 불일치) | `cycle.json` result vs shots | mitigate | 이번 수정으로 result 스코프가 `BuildDto(seqName)` 의 shots 스코프와 일치하게 된다. Task 3 (c)-9 에서 실제 파일로 교차 확인. |
| T-N5C-SC | Tampering | 패키지 설치 | (해당 없음) | 신규 NuGet 패키지 0. 기존 참조만 사용. |
</threat_model>

<verification>
1. **빌드**: MSBuild `Debug|x64` — `error CS` 0, 신규 warning 0.
2. **게이트 개수**: `IsShotOwnedBySequence` 등장 = InspectionSequence.cs 3(정의1+호출2) / BatchRunService.cs 1 / RepeatRunService.cs 1 = **총 5**.
3. **삼항 잔존 0**: `_seq != null ? _seq.Name` 검색 결과 0건 (두 서비스 파일).
4. **v1.0 경로 무변경**: `git diff` 에 `AddResponseV1Cycle` / `AggregateIndexFais` / `ComputeLastZIndex` / `AddFaiResult` 본문 변경 라인 0.
5. **판정 계층 무변경**: `anyDatumSkip` → NotExist, `!allPass` → NG, else OK 순서가 4곳 모두 수정 전과 동일.
6. **신규 설정 항목 0** (D-03): `SystemSetting` 계열 파일 변경 0.
7. **코드 리뷰 통과** (D-05): `/gsd-code-review` blocker 0.
8. **SIMUL 재현 검증 통과** (D-04): Task 3 의 (b)-3 / (b)-4 / (b)-5 / (b)-6 전부 PASS.
</verification>

<success_criteria>
- [ ] Top / Side / Bottom 각각 단독 RUN 시 종합판정이 **그 시퀀스 소유 shot 만** 반영한다.
- [ ] Top 에 실제 불량이 있으면 여전히 NG 로 판정된다 (false PASS 회귀 0).
- [ ] 일괄검사 / 반복검사 / v2.6 TCP 응답 3경로 모두 동일 스코프로 판정한다.
- [ ] v1.0 프로토콜 경로 · 판정 계층 · 설정 항목 변경 0.
- [ ] Debug/x64 빌드 error 0 / 신규 warning 0.
- [ ] `/gsd-code-review` blocker 0.
- [ ] 삼항연산자 신규 0, 헝가리언 표기 준수, C# 7.2 문법 준수.
</success_criteria>

<output>
완료 후 `.planning/quick/260805-n5c-phase-70-computeoverallresult-handlefini/260805-n5c-SUMMARY.md` 생성.

SUMMARY 에 반드시 남길 것:
- 수정한 4개 지점의 최종 라인 번호와 diff 요약.
- `RepeatRunService`(N5C-04) 확장 적용 여부 — 적용했으면 사유, 뺐으면 미해결로 남은 사실.
- v2.6 `FAIResults` 항목 수가 줄어드는 부수 효과 — 핸들러(제어팀) 파서 동기화 필요 여부 판단과 함께 기록.
- 실장비 v2.6 TCP 확인(Task 3 (d))을 못 했으면 **carry-over** 로 명시.
</output>
