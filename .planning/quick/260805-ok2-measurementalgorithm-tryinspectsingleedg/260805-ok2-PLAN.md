---
phase: 260805-ok2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Halcon/Algorithms/MeasurementAlgorithm.cs
autonomous: true
requirements: [OK2-01]

must_haves:
  truths:
    - "TryInspectSingleEdgeInternal 의 strip 루프에서 MeasurePos 가 예외를 던져도 해당 strip 의 측정 handle 이 CloseMeasure 로 반드시 해제된다 (handle 누수 없음)"
    - "MeasurePos 가 정상 성공하는 기존 경로의 판정/반환값/allRows·allCols 누적 결과는 수정 전과 완전히 동일하다"
    - "삼항 연산자(?:)가 신규로 도입되지 않는다"
    - "Debug/x64 구성에서 MSBuild 빌드가 에러 없이 성공한다 (빌드 환경이 이 세션에서 불가능하면 그 사실을 명확히 보고한다)"
  artifacts:
    - path: "WPF_Example/Halcon/Algorithms/MeasurementAlgorithm.cs"
      provides: "TryInspectSingleEdgeInternal 내 MeasurePos 호출을 감싸는 try, CloseMeasure(handle) 를 finally 로 이동"
      contains: "finally"
  key_links:
    - from: "TryInspectSingleEdgeInternal strip 루프"
      to: "HOperatorSet.CloseMeasure(handle)"
      via: "try/finally (MeasurePos 예외 시에도 finally 가 실행되어 handle 해제 보장)"
      pattern: "finally"
---

<objective>
`MeasurementAlgorithm.TryInspectSingleEdgeInternal` 의 strip 측정 루프에서, `HOperatorSet.MeasurePos` 호출이 예외를 던지면 바로 다음 줄인 `HOperatorSet.CloseMeasure(handle)` 이 실행되지 않아 Halcon measure handle 이 누수되는 결함을 수정한다. `MeasurePos` 호출을 `try` 로 감싸고 `CloseMeasure(handle)` 을 `finally` 로 옮겨, 예외 발생 여부와 무관하게 handle 이 항상 정확히 1회 해제되도록 보장한다.

Purpose: 현재 예외는 두 단계 위의 `TryInspectSingleEdge` 래퍼(라인 87-98, 108-116)의 빈 `catch { return false; }` 에서 조용히 삼켜지므로, handle 누수가 겉으로 드러나지 않고 반복 검사 시 Halcon 리소스가 계속 쌓인다. `VisionAlgorithmService.AppendStrip` (라인 273-315) 과 `FAIEdgeMeasurementService` (라인 194-251, 375-426) 의 형제 구현이 이미 이 패턴(try 로 MeasurePos 감싸고 finally 에서 CloseMeasure)을 쓰고 있으므로 그 관례를 그대로 따른다.

Output: `MeasurementAlgorithm.cs` 1개 파일, `TryInspectSingleEdgeInternal` 메서드 내부 1개 블록 치환. 판정/반환값 로직은 무변경.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

<interfaces>
<!-- 실행자가 코드베이스를 탐색할 필요 없도록 형제 패턴을 여기에 전부 넣는다. -->

`WPF_Example/Halcon/Algorithms/VisionAlgorithmService.cs:280-315` (`AppendStrip`) — 동일한 문제를 이미 해결한 형제 구현. handle 을 `try` 밖에서 `null` 로 선언하고, `finally` 에서 `if (measureHandle != null) { try { HOperatorSet.CloseMeasure(measureHandle); } catch { } }` 로 닫는다. 이 플랜의 대상 메서드는 handle 이 `try` 진입 전에 `GenMeasureRectangle2` 로 이미 정상 할당되어 있음이 보장되므로(그 호출 자체가 실패하면 애초에 루프가 그 지점에서 이미 중단됨 — 이번 수정 범위 밖), `null` 가드 없이 바로 `HOperatorSet.CloseMeasure(handle);` 를 finally 에 둔다.

```csharp
private EStripResult AppendStrip(...)
{
    HObject stripRegion = null;
    HTuple measureHandle = null;
    try
    {
        ...
        HOperatorSet.GenMeasureRectangle2(rr, rc, measurePhi, rh, rw, imageWidth, imageHeight, "nearest_neighbor", out measureHandle);
        HTuple edgeRows, edgeCols, amp, dist;
        HOperatorSet.MeasurePos(image, measureHandle, sigma, threshold, polarity, selection, out edgeRows, out edgeCols, out amp, out dist);
        if (edgeRows.TupleLength() <= 0 || edgeCols.TupleLength() <= 0)
        {
            return EStripResult.NoEdge;
        }
        HOperatorSet.TupleConcat(allRows, edgeRows, out allRows);
        HOperatorSet.TupleConcat(allCols, edgeCols, out allCols);
        return EStripResult.Ok;
    }
    catch
    {
        return EStripResult.Failed;
    }
    finally
    {
        if (measureHandle != null) { try { HOperatorSet.CloseMeasure(measureHandle); } catch { } }
        if (stripRegion != null) { try { stripRegion.Dispose(); } catch { } }
    }
}
```

`WPF_Example/Halcon/Algorithms/FAIEdgeMeasurementService.cs:192-252` — 또 다른 형제 구현. `handle` 을 루프 최상단에서 `null` 로 선언 후 `try` { GenMeasureRectangle2 → MeasurePos → 누적 } `catch` { 개별 strip 실패 무시 } `finally` { `if (handle != null) { try { HOperatorSet.CloseMeasure(handle); } catch { } }` } 순서.

**이번 플랜은 위 두 형제와 달리 `catch` 를 추가하지 않는다** — 이 태스크는 순수 handle 누수 수정이며, "판정/반환값 로직을 바꾸지 않는다"는 제약(아래 태스크 참조) 때문에, 예외가 나면 지금처럼 `TryInspectSingleEdgeInternal` 전체가 (감싸는 `TryInspectSingleEdge` 의 바깥 `catch` 까지) 그대로 전파되어 ROI 전체가 실패(`false`) 처리되는 기존 동작을 유지해야 한다. `catch` 로 개별 strip 실패를 삼키면 일부 strip 만 실패해도 나머지로 성공 판정이 나는 방향으로 반환값이 바뀔 수 있어 금지 대상이다.
</interfaces>

<current_state>
`WPF_Example/Halcon/Algorithms/MeasurementAlgorithm.cs` 의 `TryInspectSingleEdgeInternal` (private static, 라인 118부터) 라이브 파일 확인 (2026-08-05):

- 라인 150: `for (var i = 0; i < sampleCount; i++)` — strip 루프, 들여쓰기 12칸.
- 라인 152-171: `stripRegion` 생성 + `SmallestRectangle2` — 이번 범위 밖, 무수정.
- 라인 173-174: `HTuple handle;` 선언 + `HOperatorSet.GenMeasureRectangle2(...)` 로 즉시 할당 — 이번 범위 밖, `try` 진입 전에 그대로 둔다.
- 라인 176-186: `rows`/`cols`/`amp`/`dist` 선언 + `polarity`/`selection` 문자열 결정(`if/else`, 삼항 없음) — 이번 범위 밖, `try` 밖에 그대로 둔다.
- 라인 187-197: `HOperatorSet.MeasurePos(...)` 호출 — **예외 발생 가능 지점. 이번 수정 대상.**
- 라인 198: `HOperatorSet.CloseMeasure(handle);` — 현재 `try`/`finally` 밖의 평범한 statement. `MeasurePos` 가 예외를 던지면 이 줄은 실행되지 않아 handle 누수. **이번 수정으로 `finally` 로 이동.**
- 라인 200-204: `if (rows.TupleLength() > 0) { TupleConcat x2 }` — `CloseMeasure` 뒤에 위치. **이번 수정으로 `try` 안, `MeasurePos` 호출 직후로 이동** (finally 는 순서와 무관하게 항상 실행되므로 기능적으로 동일 — handle 을 닫는 시점과 `rows`/`cols` 를 `allRows`/`allCols` 에 누적하는 시점은 서로 독립적인 연산이라 순서를 바꿔도 결과가 달라지지 않는다).

라인 번호는 참고용이다. 실행자는 아래 **문자열 매칭**으로 대상 블록을 찾아 치환하고, 라인 번호로 편집하지 말 것 (라인 번호가 약간 밀렸을 수 있음).

파일 전체 브레이스 스타일은 Allman(여는 중괄호가 별도 줄) 이며, 이 메서드 안의 `if (rows.TupleLength() > 0) { ... }` 블록도 이미 Allman 이다. 이번 수정의 `try`/`finally` 도 Allman 으로 작성한다.
</current_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TryInspectSingleEdgeInternal MeasurePos/CloseMeasure try-finally 적용</name>

  <files>WPF_Example/Halcon/Algorithms/MeasurementAlgorithm.cs</files>

  <action>
`TryInspectSingleEdgeInternal` 메서드 내부에서 아래 **정확히 이 텍스트**(2026-08-05 라이브 파일 기준, 들여쓰기 16칸)를 찾는다.

**찾을 텍스트 (BEFORE):**
```csharp
                HTuple handle;
                HOperatorSet.GenMeasureRectangle2(rr, rc, measurePhi, rh, rw, imageWidth, imageHeight, "nearest_neighbor", out handle);

                HTuple rows;
                HTuple cols;
                HTuple amp;
                HTuple dist;
                string polarity;
                if (string.Equals(roi.EdgePolarity, "LightToDark", StringComparison.OrdinalIgnoreCase)) polarity = "negative";
                else polarity = "positive";
                string selection;
                if (string.Equals(roi.EdgeSelection, "Last", StringComparison.OrdinalIgnoreCase)) selection = "last";
                else if (string.Equals(roi.EdgeSelection, "All", StringComparison.OrdinalIgnoreCase)) selection = "all";
                else selection = "first";
                HOperatorSet.MeasurePos(
                    image,
                    handle,
                    Math.Max(0.4, roi.Sigma),
                    Math.Max(1, roi.EdgeThreshold),
                    polarity,
                    selection,
                    out rows,
                    out cols,
                    out amp,
                    out dist);
                HOperatorSet.CloseMeasure(handle);

                if (rows.TupleLength() > 0)
                {
                    HOperatorSet.TupleConcat(allRows, rows, out allRows);
                    HOperatorSet.TupleConcat(allCols, cols, out allCols);
                }
```

**치환할 텍스트 (AFTER):**
```csharp
                HTuple handle;
                HOperatorSet.GenMeasureRectangle2(rr, rc, measurePhi, rh, rw, imageWidth, imageHeight, "nearest_neighbor", out handle);

                HTuple rows;
                HTuple cols;
                HTuple amp;
                HTuple dist;
                string polarity;
                if (string.Equals(roi.EdgePolarity, "LightToDark", StringComparison.OrdinalIgnoreCase)) polarity = "negative";
                else polarity = "positive";
                string selection;
                if (string.Equals(roi.EdgeSelection, "Last", StringComparison.OrdinalIgnoreCase)) selection = "last";
                else if (string.Equals(roi.EdgeSelection, "All", StringComparison.OrdinalIgnoreCase)) selection = "all";
                else selection = "first";
                try
                {
                    HOperatorSet.MeasurePos(
                        image,
                        handle,
                        Math.Max(0.4, roi.Sigma),
                        Math.Max(1, roi.EdgeThreshold),
                        polarity,
                        selection,
                        out rows,
                        out cols,
                        out amp,
                        out dist);

                    if (rows.TupleLength() > 0)
                    {
                        HOperatorSet.TupleConcat(allRows, rows, out allRows);
                        HOperatorSet.TupleConcat(allCols, cols, out allCols);
                    }
                }
                finally
                {
                    // MeasurePos 예외 시에도 handle 이 반드시 해제되도록 finally 로 이동 (handle 누수 방지).
                    // VisionAlgorithmService.AppendStrip / FAIEdgeMeasurementService 의 try-finally 패턴과 동일.
                    HOperatorSet.CloseMeasure(handle);
                }
```

치환 규칙 (반드시 준수):
1. `GenMeasureRectangle2` 호출과 `rows`/`cols`/`amp`/`dist` 선언, `polarity`/`selection` 결정 로직은 `try` **밖에** 그대로 남긴다 (수정하지 않는다). `handle` 은 이 시점에 이미 `GenMeasureRectangle2` 로 할당되어 있으므로 `try` 진입 전 `null` 초기화나 `null` 가드는 필요 없다.
2. `MeasurePos` 호출 전체와, 그 뒤에 있던 `if (rows.TupleLength() > 0) { ... }` 누적 블록을 그대로(조건/순서/변수명 무변경) `try` 블록 안, `MeasurePos` 호출 직후로 옮긴다.
3. `HOperatorSet.CloseMeasure(handle);` 한 줄만 `finally` 블록으로 옮긴다. `catch` 블록은 추가하지 않는다 — 예외는 그대로 위(`TryInspectSingleEdge`)까지 전파되어야 기존 반환값 동작(strip 하나가 예외를 던지면 ROI 전체가 실패 처리됨)이 보존된다.
4. 삼항 연산자(`?:`) 를 절대 도입하지 않는다 — 기존 `if/else` 로 작성된 `polarity`/`selection` 결정 로직은 이미 그대로이므로 손대지 않는다.
5. 브레이스 스타일은 Allman(여는 중괄호 별도 줄) — 파일의 나머지 코드와 통일.
6. `handle`, `rows`, `cols`, `amp`, `dist`, `allRows`, `allCols` 등 기존 변수명은 그대로 유지한다 (리네임 금지).
7. 이 블록 외 파일의 다른 어떤 부분도(다른 메서드, `TrimExtremePoints`, `Run` 오버로드들, `TryInspectSingleEdge` 래퍼) 수정하지 않는다.
8. 새 주석은 "왜 finally 로 옮겼는지"를 설명하는 위 2줄 정도로 최소화하고, 날짜/이니셜 접두사(`//YYMMDD hbk`)는 붙이지 않는다 (해당 규칙 폐기됨).
  </action>

  <verify>
    <automated>
cd "C:/Info/Project/DataMeasurement" && F=WPF_Example/Halcon/Algorithms/MeasurementAlgorithm.cs && echo "A_questionMark=$(grep -c '?' $F) EXPECT_0" && echo "B_finallyCount=$(grep -c 'finally' $F) EXPECT_1" && echo "C_closeMeasureCount=$(grep -c 'CloseMeasure(handle)' $F) EXPECT_1" && echo "D_tryCount=$(grep -c '\btry\b' $F) EXPECT_5" && echo "E_tupleConcatCount=$(grep -c 'TupleConcat(allRows' $F) EXPECT_1"
    </automated>
    <automated>
cd "C:/Info/Project/DataMeasurement" && git diff --stat -- WPF_Example/Halcon/Algorithms/MeasurementAlgorithm.cs && echo "--- 위 변경은 파일 1개여야 하고, git diff 는 아래 블록 1곳만 보여야 한다 ---" && git diff -U0 -- WPF_Example/Halcon/Algorithms/MeasurementAlgorithm.cs | grep -c '^@@' && echo "HUNK_COUNT_ABOVE_SHOULD_BE_1"
    </automated>
    <automated>
cd "C:/Info/Project/DataMeasurement" && "C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj //t:Build //p:Configuration=Debug //p:Platform=x64 //v:m //nologo 2>&1 | grep -E "error CS|warning CS" | grep -v -E "CS0618|CS0162" | head -20; echo "CS_LIST_ABOVE_MUST_BE_EMPTY"
    </automated>
  </verify>

  <done>
- `TryInspectSingleEdgeInternal` 의 strip 루프 안에서 `HOperatorSet.MeasurePos(...)` 호출과 그 뒤 `rows.TupleLength() > 0` 누적 블록이 `try` 안에 있고, `HOperatorSet.CloseMeasure(handle);` 이 그 `try` 에 대응하는 `finally` 안에 정확히 1번 있다.
- 파일 전체에서 `try` 키워드가 정확히 5개(기존 4개 + 이번에 추가한 1개), `finally` 가 정확히 1개(신규)다.
- 파일에 물음표(`?`) 문자가 0개다 — 삼항 연산자가 도입되지 않았음을 의미한다.
- `TryInspectSingleEdgeInternal` 의 반환값/판정 로직(어떤 조건에서 `true`/`false` 를 반환하는지)은 수정 전과 동일하다 — `catch` 를 추가하지 않았으므로 `MeasurePos` 예외 시 여전히 `TryInspectSingleEdgeInternal` 전체가 예외를 던지고, 이를 감싸는 `TryInspectSingleEdge`(라인 108-116)의 `catch { return false; }` 가 처리한다.
- `git diff` hunk 가 1개이고 변경 파일이 `MeasurementAlgorithm.cs` 단 1개다.
- msbuild Debug/x64 빌드에서 `error CS` 0건, 신규 `warning CS` 0건. msbuild 를 찾을 수 없거나 Halcon/카메라 SDK 참조 오류로 빌드 자체가 이 환경에서 불가능하면, "성공했다"고 주장하지 말고 그 사실을 명확히 보고한다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 내부 알고리즘 함수 호출 (신뢰 경계 없음) | `TryInspectSingleEdgeInternal` 은 외부 입력을 직접 받지 않는다(이미 검증된 `RoiDefinition`/`HImage` 만 사용). 네트워크/사용자 입력 경계는 없음 — 이번 수정은 순수 리소스 정리(cleanup) 안전성 개선이다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ok2-01 | Denial of Service (비관리 리소스 누수 → 장시간 반복 검사 시 Halcon 메모리/핸들 고갈) | `MeasurementAlgorithm.TryInspectSingleEdgeInternal` strip 루프의 measure handle | mitigate | `MeasurePos` 호출을 `try` 로 감싸고 `HOperatorSet.CloseMeasure(handle)` 을 `finally` 로 이동해, 예외 발생 여부와 무관하게 handle 이 항상 해제되도록 보장한다(본 플랜 Task 1). |
| T-ok2-02 | Tampering (판정 로직 의도치 않은 변경) | 동일 메서드의 `rows.TupleLength() > 0` 누적/반환값 로직 | accept | `catch` 를 추가하지 않아 예외 전파 경로(→ ROI 전체 실패)를 그대로 유지하므로 판정 결과가 바뀌지 않는다. 자동 검증(`git diff` hunk 1개, msbuild 무에러)으로 범위 이탈 여부를 확인한다. |
</threat_model>

<verification>
## 자동 검증 (executor 수행)
Task 1 의 `<automated>` 3개 전부 통과 (grep 카운트 5종 + diff hunk 1개 + msbuild Debug/x64 error CS 0).

## 사람 UAT
필요 없음 — 이 수정은 예외 경로에서만 관측 가능한(handle 누수 방지) 내부 리소스 정리이며, 정상 경로 판정 결과는 무변경이므로 화면상 확인할 새 동작이 없다. 기존 검사 회귀 여부는 msbuild 빌드 성공 + 코드 리뷰로 충분하다.
</verification>

<success_criteria>
- `TryInspectSingleEdgeInternal` 의 measure handle 이 `MeasurePos` 예외 발생 시에도 `finally` 를 통해 항상 해제된다.
- 정상 경로(MeasurePos 성공)의 반환값/누적 로직은 수정 전과 100% 동일하다.
- 삼항 연산자 미도입, Allman 브레이스 스타일 유지, 변수명 무변경.
- 변경 파일 1개 / diff hunk 1개 / msbuild Debug/x64 error CS 0 (빌드 환경 불가 시 그 사실을 명확히 보고).
</success_criteria>

<output>
완료 후 `.planning/quick/260805-ok2-measurementalgorithm-tryinspectsingleedg/260805-ok2-SUMMARY.md` 를 생성한다.
</output>
