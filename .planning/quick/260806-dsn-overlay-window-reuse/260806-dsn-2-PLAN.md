---
phase: quick-260806-dsn-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: false
requirements: [BATCH-MEM-02]

must_haves:
  truths:
    - "사이클 종료 직후 폴백 파일이 아직 없는 SHOT은 즉시 버려지지 않고 재시도 대기열에 들어간다"
    - "대기열의 SHOT은 DispatcherTimer(UI 스레드)가 몇 초 간격으로 재확인하며, 폴백이 생기면 자동으로 정리(dispose)된다"
    - "재확인 시점에 사용자가 그 SHOT을 보고 있으면(SelectedParam 재조회) 정리하지 않고 대기열에서만 제거한다"
    - "저장이 영구 실패해도 SHOT별 재시도 상한을 넘기면 조용히 대기열에서 제거된다(예외/사용자 알림 없음)"
    - "대기열이 비면 타이머가 멈춘다"
    - "코드에 TEMP DIAG/[DIAG-260806] 임시 진단 마커가 더 이상 존재하지 않는다"
    - "정리/재시도 어느 경로에도 동기적 블로킹 대기(Thread.Sleep/.Wait()/.Result/Task.Delay)가 도입되지 않는다"
    - "즉시 정리 경로(현재 표시 중 SHOT 보존, 폴백 성공 시 즉시 dispose)는 기존과 동일하게 동작한다(회귀 없음)"
  artifacts:
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "재시도 대기열(_pendingImageCleanup) + DispatcherTimer + 공유 정리 헬퍼(ClearShotImageCache)로 재작성된 CleanupBatchImageMemoryAfterCycle"
      contains: "_pendingImageCleanup"
  key_links:
    - from: "CleanupBatchImageMemoryAfterCycle (폴백 없는 SHOT)"
      to: "_pendingImageCleanup 대기열"
      via: "즉시 실패 시 Add (중복 가드: Contains 체크 후 추가)"
      pattern: "_pendingImageCleanup\\.Add\\(shot\\)"
    - from: "DispatcherTimer.Tick"
      to: "PendingImageCleanupTimer_Tick"
      via: "EnsurePendingImageCleanupTimer()에서 구독"
      pattern: "PendingImageCleanupTimer_Tick"
    - from: "즉시 경로 + 재시도 경로"
      to: "ClearShotImageCache(shot)"
      via: "534c742 로직을 옮긴 공유 헬퍼 재사용(중복 구현 금지)"
      pattern: "ClearShotImageCache\\(shot\\)"
    - from: "PendingImageCleanupTimer_Tick"
      to: "ResolveCurrentlyDisplayedShot()"
      via: "매 틱마다 재조회(사이클 종료 시점 값 재사용 금지)"
      pattern: "ResolveCurrentlyDisplayedShot\\(\\)"
---

<objective>
quick-260806-dsn(커밋 `3a5f4b4`/`8c327c5`/`534c742`)이 도입한 배치 사이클 완료 후 이미지 메모리 정리(`CleanupBatchImageMemoryAfterCycle`)가 배치 뒤쪽 SHOT(약 1/3~1/2)에서 매 사이클 확정적으로 실패하는 근본원인(비동기 저장 큐가 사이클 종료 시점에 10~16초+ 뒤처져 있어 `ResolveFallbackImagePath()`의 `File.Exists()` 체크가 그 시점엔 통과하지 못함 — 독립 에이전트가 실제 파일 타임스탬프 대조로 실증)을 수정한다.

**해결 방향(CONTEXT.md LOCKED)**: "즉시 실패 시 영구 포기"를 "나중에 큐가 따라잡으면 재시도"로 전환한다. 즉시 정리 경로는 무변경 유지(이미 배치 앞쪽 SHOT을 잘 처리함). 즉시 실패한 SHOT은 인스턴스 필드 재시도 대기열에 넣고, `DispatcherTimer`(UI 스레드, 새 스레드/Task 도입 없음)가 몇 초 간격으로 재확인 — 현재 표시 중인 SHOT이면 정리 없이 제외, 폴백이 생겼으면 정리, 여전히 없으면 다음 틱까지 대기, SHOT별 상한 도달 시 조용히 포기. 동기적 블로킹 대기는 절대 도입하지 않는다(실측 지연이 10~16초+이고 세션 진행에 따라 늘어나므로, 매 사이클 UI/시퀀스 스레드를 그만큼 멈추는 건 명시적으로 금지됨).

이번 수정으로 이미 작업 트리에 존재하는(커밋 안 된) `TEMP DIAG (260806)`/`[DIAG-260806]` 임시 진단 로그 3곳도 함께 제거하거나 영구 Trace 로그로 대체한다.

Purpose: "메모리가 30초 후에도 안 떨어짐, 계속 누르면 조금씩 더 올라감" 사용자 실패 재현의 확정 원인(저장 큐 레이스) 제거. 화면 표시 회귀 없이, 저장 큐가 따라잡는 대로 결국 모든 SHOT이 정리되게 한다.
Output: `InspectionListView.xaml.cs` 1개 파일 수정. Debug/x64 빌드 PASS. checkpoint:human-verify로 오늘과 동일한 재현 시나리오를 더 길게(1~2분) 재검증.

**범위 밖 (CONTEXT.md 확정, 건드리지 않음)**: `CaptureImageSaveService.cs`의 `MAX_QUEUE_DEPTH`/워커 스레드 자체(저장 처리량 증대는 별도 과제), Part A(HALCON SetSystem 3줄)와 이미 완료된 나머지 Part B 로직(`ShotConfig.ResolveFallbackImagePath`, `InspectionSequence.ClearCrossZImagesAfterBatchCycle`, `MainView.DisplayShotImage` 디스크 폴백 분기 — 전부 무변경, 그대로 재사용), `OverlayCaptureRenderer.cs`(반증된 가설).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/quick/260806-dsn-overlay-window-reuse/260806-dsn-2-CONTEXT.md
@.planning/quick/260806-dsn-overlay-window-reuse/260806-dsn-CONTEXT.md

<style_rules>
프로젝트 규칙 (예외 없음):
- 삼항 연산자 `?:` 절대 금지 → if-else 만 사용.
- 날짜 프리픽스 주석(`//YYMMDD hbk`) 정책은 폐기됨 — 새 라인에는 `// quick-260806-dsn-2: ...` 형태로 이 quick task 출처만 짧게 표기하고, 비자명한 "왜"를 한국어로 설명한다. 기존에 이미 있는 `//260616 hbk ...`, `// quick-260806-dsn ...` 형태 주석은 그대로 둔다(재작성 금지).
- C# 7.2 / .NET Framework 4.8. `is` 타입 패턴(`x is Type y`)은 이 파일에서 이미 사용 중이므로 허용. switch expression / nullable ref / record / range 연산자(`..`) 금지.
- 브레이스 스타일: `InspectionListView.xaml.cs`는 K&R(여는 브레이스 같은 줄) — 반드시 이 파일의 기존 스타일을 따를 것.
- 헝가리안 표기는 점진 적용 — 이 파일 기존 관례(`nCleared`, `nQueueDepthAtCleanup` 등 지역변수에 `n` 접두)를 따른다. 재시도 횟수 변수는 `nRetryCount`로 명명.
- `HImage` Dispose는 반드시 null 대입과 짝을 이룬다(기존 `ClearShotImageCache`/`ShotConfig.ClearImage` 패턴 그대로 재사용 — 신규 dispose 로직 추가 없음, 기존 로직을 헬퍼로 옮기기만 한다).
- using 추가 불필요: `System.Collections.Generic`(Dictionary), `System.Windows.Threading`(DispatcherTimer), `ReringProject.Define`(EContextState) 모두 이 파일 상단에 이미 존재.
</style_rules>

<interfaces>
<!-- 코드베이스에서 확인된 기존 계약 — 그대로 재사용, 코드 탐색 불필요. -->

From WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs (기존, 무변경 — 그대로 재사용):
```csharp
public string ResolveFallbackImagePath() {
    // FAIList 의 각 FAI가 보유한 원본 캡쳐 파일(LastOriginImageFileName) 중 실제 존재하는(File.Exists) 첫 경로 반환.
    // 없으면 null. 정리 가능 여부의 유일한 판단 기준 — 이번 plan에서 재확인 로직에 그대로 재사용한다.
}
public void ClearImage(); // _imageLock 보호, dispose + null, 멱등
```

From WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs (기존, 무변경):
```csharp
public void ClearCrossZImagesAfterBatchCycle(); // 배치 사이클 완료 전용 크로스-Z 정리 진입점
```

From WPF_Example/Sequence/Sequence/SequenceBase.cs (기존, 무변경 — 이번 plan이 신규로 참조):
```csharp
public EContextState State { get { return Context.State; } }
// Context.State 는 일반 auto-property(volatile 아님) — 이 프로젝트 전역에서 이미 크로스스레드로
// best-effort 스냅샷 조회에 흔히 쓰이는 패턴이다(예: SequenceHandler.GetSequenceState, 이 파일의
// 기존 라인 455/577 `seqHandler.GetSequenceState(...) == EContextState.Idle`). 완벽한 동기화가
// 아니라 레이스 창을 좁히는 용도로 이번 재시도 로직에도 동일하게 적용한다.
```

From WPF_Example/UI/ControlItem/InspectionListView.xaml.cs (기존, 무변경 — 그대로 재사용):
```csharp
public ParamBase SelectedParam { get; private set; }
private List<ShotConfig> _batchShots;
private ShotConfig ResolveCurrentlyDisplayedShot(); // SelectedParam → Shot/FAI/Measurement 3단계 역추적, 무변경 재사용
```
</interfaces>

<edit_anchors>
절대 라인 번호를 신뢰하지 말 것 — 아래 content-anchor로 위치를 찾아 편집한다. 현재 작업 트리(커밋 안 됨)에는 `TEMP DIAG (260806)` 진단 로그가 이미 들어있는 상태이므로, 그 진단 로그가 포함된 현재 버전의 `CleanupBatchImageMemoryAfterCycle`을 통째로 아래 Task 1 액션의 완성된 코드로 교체한다.

- anchor 시작: `private void OnBatchComplete(List<CycleResultDto> cycles) {` (그 앞의 `//260616 hbk Phase 51 BATCH-01: ...` 주석 포함)
- anchor 끝: `private ShotConfig ResolveCurrentlyDisplayedShot() {` 메서드의 닫는 `}` (그 다음이 `//260616 hbk Phase 51 BATCH-01: 누적분 수동 엑셀 Export` 주석 / `Btn_batchExport_Click`)
- 필드 삽입 위치: `private List<ShotConfig> _batchShots;` 선언 바로 다음(클래스 상단 필드 섹션)
</edit_anchors>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 저장 큐 레이스 수정 — 재시도 대기열 + DispatcherTimer 도입, TEMP DIAG 제거</name>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <action>
**(1) 필드 추가** — anchor: `private List<ShotConfig> _batchShots;` 바로 다음:

```csharp
        //260617 hbk Quick 260617-cq2: 일괄 검사 완료 시 결과 그리드에 펼쳐 표시할 체크 SHOT 목록
        private List<ShotConfig> _batchShots;

        // quick-260806-dsn-2: 사이클 종료 시점에 저장 큐가 아직 파일을 다 쓰지 못해 즉시 정리를 skip한 SHOT의
        //  재시도 대기열. DispatcherTimer(UI 스레드)가 이 목록을 몇 초 간격으로 재확인한다.
        private List<ShotConfig> _pendingImageCleanup = new List<ShotConfig>();
        // SHOT별 재시도 횟수 — 저장이 영구 실패(디스크 오류 등)해도 무한 재시도하지 않도록 상한 판단에 사용.
        private Dictionary<ShotConfig, int> _pendingImageCleanupRetryCount = new Dictionary<ShotConfig, int>();
        private DispatcherTimer _pendingImageCleanupTimer;
        private const int PENDING_CLEANUP_INTERVAL_SECONDS = 5;
        private const int PENDING_CLEANUP_MAX_RETRY_TICKS = 24; // 5초 x 24회 = 2분 — 이 이상 안 비워지면 조용히 포기
```

**(2) `OnBatchComplete` ~ `ResolveCurrentlyDisplayedShot` 블록 전체 교체** — 현재 작업 트리의 (TEMP DIAG 포함) 버전을 아래로 통째로 대체한다:

```csharp
        //260616 hbk Phase 51 BATCH-01: 일괄 검사 1사이클 완료 → 누적 + Export 버튼 활성 (D-04 append, D-05 수동 Export)
        private void OnBatchComplete(List<CycleResultDto> cycles) {
            Dispatcher.Invoke(new Action(delegate {
                if (cycles != null) {
                    _batchAccumulated.AddRange(cycles);
                }
                btn_batchExport.IsEnabled = (_batchAccumulated.Count > 0);
                //260617 hbk Quick 260617-cq2: 검사한 체크 SHOT 전체 측정 결과를 그리드에 펼쳐 표시.
                //  행이 live 측정 객체를 감싸므로 LastMeasuredValue/판정이 즉시 반영됨.
                if (_inspectionVm != null && _batchShots != null) {
                    _inspectionVm.ShowMeasurementsForShots(_batchShots);
                }
                // quick-260806-dsn Part B: 배치 사이클이 완전히 끝나는 유일한 시점(이 콜백, 사이클당 1회) —
                //  현재 화면에 표시 중인 노드를 제외한 나머지 SHOT의 대용량 이미지 캐시를 즉시 해제한다.
                CleanupBatchImageMemoryAfterCycle(_batchShots);
            }));
        }

        // quick-260806-dsn Part B / quick-260806-dsn-2: 사이클 완료 후 메모리 정리. 현재 트리 선택
        //  (SelectedParam)이 가리키는 SHOT은 제외하고, 폴백 파일이 이미 존재하는 SHOT은 즉시 정리한다(배치
        //  앞쪽 SHOT — 무변경, 기존과 동일하게 동작). 폴백이 아직 없는 SHOT(배치 뒤쪽 — 저장 큐가 못 따라간
        //  것뿐)은 여기서 버리지 않고 재시도 대기열로 넘긴다(quick-260806-dsn-2, 확정 원인 수정).
        //  크로스-Z 저장소는 파일 폴백 대상이 아니므로(재현 판단 불필요) 이 시점에 곧바로 정리한다.
        //  단일 RUN(Btn_start_Click)이나 사이클 도중에는 이 메서드가 호출되지 않는다(OnBatchComplete 전용 경로).
        //  동시 접근 안전성: 이 시점에 사이클을 실행한 시퀀스 스레드는 이 델리게이트가 반환할 때까지
        //  Dispatcher.Invoke 안에서 동기적으로 블로킹되어 있다(BatchRunService.HandleFinish → OnBatchComplete
        //  이벤트 발화 → 여기 UI 스레드 콜백이 끝나야 그 Invoke 호출이 리턴) — 따라서 아래에서
        //  ActionContext/ShotConfig 를 건드리는 동안 시퀀스 스레드가 같은 객체를 동시에 재실행할 수 없다.
        private void CleanupBatchImageMemoryAfterCycle(List<ShotConfig> shots) {
            if (shots == null || shots.Count == 0) return;

            ShotConfig currentShot = ResolveCurrentlyDisplayedShot();

            foreach (ShotConfig shot in shots) {
                if (shot == null) continue;
                if (ReferenceEquals(shot, currentShot)) continue; // 현재 표시 중인 노드는 보존

                string fallbackPath = shot.ResolveFallbackImagePath();
                if (string.IsNullOrEmpty(fallbackPath)) {
                    // quick-260806-dsn-2: 폴백이 아직 없다 — 저장 큐가 이 SHOT의 파일을 못 따라간 것뿐이므로
                    //  버리지 않고 재시도 대기열에 넣는다(이미 있으면 중복 추가하지 않는다).
                    if (!_pendingImageCleanup.Contains(shot)) {
                        _pendingImageCleanup.Add(shot);
                    }
                    continue;
                }

                // quick-260806-dsn-2: 지난 사이클부터 재시도 대기 중이던 SHOT이 이번엔 즉시 성공한 경우를 대비해
                //  대기열/재시도 카운트에서도 제거한다(다음 타이머 틱에서 같은 SHOT을 중복 처리하지 않도록 — 새
                //  배치 사이클이 이전 사이클의 대기열 항목과 같은 SHOT 객체를 재사용하는 경우의 상호작용 대비).
                _pendingImageCleanup.Remove(shot);
                _pendingImageCleanupRetryCount.Remove(shot);
                ClearShotImageCache(shot);
            }

            if (_pendingImageCleanup.Count > 0) {
                EnsurePendingImageCleanupTimer();
                _pendingImageCleanupTimer.Start(); // 이미 실행 중이면 WPF 특성상 안전한 no-op(간격 재시작 아님)
            }

            // 일괄검사는 단일 시퀀스 내 SHOT만 허용(Btn_batchInspect_Click 검증) — shots[0]의 소속 시퀀스로 충분.
            InspectionSequence batchSeq = shots[0].Parent as InspectionSequence;
            if (batchSeq != null) {
                batchSeq.ClearCrossZImagesAfterBatchCycle();
            }
        }

        // quick-260806-dsn-2: 즉시 정리 경로와 재시도 경로가 공유하는 단일 SHOT 정리 로직.
        //  기존 534c742 커밋의 로직을 그대로 옮긴 것 — 동작 변경 없음, 중복 구현 방지 목적으로만 분리.
        private void ClearShotImageCache(ShotConfig shot) {
            shot.ClearImage(); // ShotConfig.cs 기존 _imageLock 보호 dispose+null 재사용

            InspectionSequence shotSeq = shot.Parent as InspectionSequence;
            if (shotSeq == null) return;
            for (int i = 0; i < shotSeq.ActionCount; i++) {
                ActionBase act = shotSeq.GetAction(i);
                if (act != null && ReferenceEquals(act.Param, shot)) {
                    if (act.Context != null && act.Context.ResultHalconImage != null) {
                        act.Context.ResultHalconImage.Dispose();
                        act.Context.ResultHalconImage = null;
                    }
                    break; // SHOT 당 Action 1개 (1:1)
                }
            }
        }

        // quick-260806-dsn-2: 대기열에 재시도 대상이 처음 생겼을 때만 타이머 인스턴스를 만든다(지연 생성).
        private void EnsurePendingImageCleanupTimer() {
            if (_pendingImageCleanupTimer != null) return;
            _pendingImageCleanupTimer = new DispatcherTimer();
            _pendingImageCleanupTimer.Interval = TimeSpan.FromSeconds(PENDING_CLEANUP_INTERVAL_SECONDS);
            _pendingImageCleanupTimer.Tick += PendingImageCleanupTimer_Tick;
        }

        // quick-260806-dsn-2: 재시도 루프 본체. DispatcherTimer는 WPF Dispatcher 큐를 통해 항상 UI 스레드에서만
        //  Tick 이 실행되도록 보장되므로(공식 WPF 설계), CleanupBatchImageMemoryAfterCycle(역시 UI 스레드,
        //  Dispatcher.Invoke 콜백)과 동일 스레드 친화적이라 이 메서드와 그 메서드 사이에는 별도 락이 불필요하다.
        //  단, "새 배치 사이클이 이 SHOT을 실제로 재실행 중"인 레이스만은 남는다 — 재실행 중인 SHOT의 시퀀스는
        //  EContextState.Running 이므로, 이 상태인 동안은 정리를 건너뛰고 다음 틱에서 다시 확인한다
        //  (시퀀스 스레드가 ActionContext.ResultHalconImage 를 그 순간 쓰고 있을 수 있어 안전하지 않기 때문 —
        //  재시도 횟수도 이 경우엔 소모하지 않는다).
        private void PendingImageCleanupTimer_Tick(object sender, EventArgs e) {
            if (_pendingImageCleanup.Count == 0) {
                _pendingImageCleanupTimer.Stop();
                return;
            }

            ShotConfig currentShot = ResolveCurrentlyDisplayedShot(); // 매 틱 재조회 — 사이클 종료 시점 값을 재사용하지 않는다

            var stillPending = new List<ShotConfig>();
            foreach (ShotConfig shot in _pendingImageCleanup) {
                if (shot == null) continue;

                if (ReferenceEquals(shot, currentShot)) {
                    // 지금 사용자가 보고 있는 SHOT — 정리하지 않고 대기열에서 제거(더 이상 재시도 불필요)
                    _pendingImageCleanupRetryCount.Remove(shot);
                    continue;
                }

                InspectionSequence shotSeq = shot.Parent as InspectionSequence;
                if (shotSeq != null && shotSeq.State == EContextState.Running) {
                    // 이 SHOT이 속한 시퀀스가 지금 재실행 중 — ResultHalconImage 를 그 순간 쓰고 있을 수 있어
                    //  건드리지 않고 다음 틱까지 그대로 대기열에 남긴다(재시도 횟수도 소모하지 않음).
                    stillPending.Add(shot);
                    continue;
                }

                string fallbackPath = shot.ResolveFallbackImagePath();
                if (!string.IsNullOrEmpty(fallbackPath)) {
                    ClearShotImageCache(shot);
                    _pendingImageCleanupRetryCount.Remove(shot);
                    Logging.PrintLog((int)ELogType.Trace, string.Format(
                        "[BatchImageCleanup] 재시도 성공: shot={0}", shot.ShotName));
                    continue;
                }

                int nRetryCount;
                if (!_pendingImageCleanupRetryCount.TryGetValue(shot, out nRetryCount)) {
                    nRetryCount = 0;
                }
                nRetryCount++;
                if (nRetryCount >= PENDING_CLEANUP_MAX_RETRY_TICKS) {
                    // 영구 실패로 간주 — 조용히 포기한다(메모리 절감 실패를 감수, 예외/사용자 알림 없음).
                    _pendingImageCleanupRetryCount.Remove(shot);
                    Logging.PrintLog((int)ELogType.Trace, string.Format(
                        "[BatchImageCleanup] 재시도 포기(상한 {0}회 도달): shot={1}", PENDING_CLEANUP_MAX_RETRY_TICKS, shot.ShotName));
                    continue;
                }

                _pendingImageCleanupRetryCount[shot] = nRetryCount;
                stillPending.Add(shot);
            }

            _pendingImageCleanup = stillPending;
            if (_pendingImageCleanup.Count == 0) {
                _pendingImageCleanupTimer.Stop();
            }
        }

        // quick-260806-dsn Part B: 현재 트리에서 선택된 노드(SelectedParam)가 속한 ShotConfig를 역추적한다.
        //  MainView.GetCurrentShotContext()와 동일 목적이나 그 메서드는 private이라 직접 참조할 수 없어
        //  _batchShots 스코프 내에서 자체 해석한다(측정 노드는 이 배치에 속한 SHOT 안에서만 검색하면 충분).
        private ShotConfig ResolveCurrentlyDisplayedShot() {
            ParamBase sel = SelectedParam;
            if (sel == null) return null;
            if (sel is ShotConfig shotSel) return shotSel;
            if (sel is FAIConfig faiSel) return faiSel.Owner as ShotConfig;
            if (sel is MeasurementBase measSel && _batchShots != null) {
                foreach (ShotConfig shot in _batchShots) {
                    if (shot == null || shot.FAIList == null) continue;
                    foreach (FAIConfig fai in shot.FAIList) {
                        if (fai.Measurements != null && fai.Measurements.Contains(measSel)) return shot;
                    }
                }
            }
            return null;
        }
```

**중요 — 스코프 경계 확인(회귀 방지, 자체 grep으로 확인할 것):**
- `CleanupBatchImageMemoryAfterCycle` 호출부는 `OnBatchComplete` 안 **1곳뿐**이어야 한다(무변경 — `Btn_start_Click`/사이클 도중 어디에도 추가하지 않는다).
- `ResolveFallbackImagePath`, `ClearCrossZImagesAfterBatchCycle`, `ClearImage` 등 다른 파일(`ShotConfig.cs`/`InspectionSequence.cs`/`MainView.xaml.cs`)의 기존 헬퍼는 이번 작업에서 **전혀 수정하지 않는다** — `files_modified`가 `InspectionListView.xaml.cs` 하나뿐인 이유.
- `Thread.Sleep`, `.Wait()`, `.Result`, `Task.Delay` 등 동기적 블로킹 대기를 이번 코드 어디에도 추가하지 않는다 — `DispatcherTimer` 틱은 비동기 콜백이며 대기 없이 즉시 반환한다.
- 새로 추가하는 필드/메서드는 전부 `private`(기존 `SelectedParam`/`_batchShots`처럼 외부에 노출할 이유 없음).
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" //t:Build //p:Configuration=Debug //p:Platform=x64 //v:minimal 2>&1 | grep -iE "error|Build succeeded"; F="WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"; DIAG=$(grep -c "TEMP DIAG\|DIAG-260806" "$F"); if [ "$DIAG" -eq 0 ]; then echo "DIAG_REMOVED_OK"; else echo "DIAG_STILL_PRESENT count=$DIAG"; fi; PEND=$(grep -c "_pendingImageCleanup\b" "$F"); TIMER=$(grep -c "_pendingImageCleanupTimer" "$F"); TICK=$(grep -c "PendingImageCleanupTimer_Tick" "$F"); SHARED=$(grep -c "ClearShotImageCache" "$F"); RUNCHECK=$(grep -c "EContextState.Running" "$F"); if [ "$PEND" -gt 0 ] && [ "$TIMER" -gt 0 ] && [ "$TICK" -ge 2 ] && [ "$SHARED" -ge 3 ] && [ "$RUNCHECK" -ge 1 ]; then echo "RETRY_MECHANISM_WIRED_OK"; else echo "RETRY_MECHANISM_MISSING PEND=$PEND TIMER=$TIMER TICK=$TICK SHARED=$SHARED RUNCHECK=$RUNCHECK"; fi; SYNCWAIT=$(grep -icE "Thread\.Sleep|\.Wait\(\)|\.Result\b|Task\.Delay" "$F"); if [ "$SYNCWAIT" -eq 0 ]; then echo "NO_SYNC_WAIT_OK"; else echo "SYNC_WAIT_FOUND count=$SYNCWAIT"; fi; N=$(grep -c "CleanupBatchImageMemoryAfterCycle" "$F"); if [ "$N" -eq 2 ]; then echo "SINGLE_TRIGGER_OK"; else echo "TRIGGER_COUNT_UNEXPECTED N=$N"; fi; grep -n "public string ResolveFallbackImagePath" WPF_Example/Custom/Sequence/Inspection/ShotConfig.cs; grep -n "public void ClearCrossZImagesAfterBatchCycle" WPF_Example/Custom/Sequence/Inspection/InspectionSequence.cs; grep -n "shot.ResolveFallbackImagePath()" WPF_Example/UI/ContentItem/MainView.xaml.cs</automated>
  </verify>
  <done>`_pendingImageCleanup`/`_pendingImageCleanupTimer`/`ClearShotImageCache`/`PendingImageCleanupTimer_Tick`이 모두 존재하고 서로 올바르게 배선되어 있다(RETRY_MECHANISM_WIRED_OK). `TEMP DIAG`/`[DIAG-260806]` 마커는 완전히 제거되었다(DIAG_REMOVED_OK). 즉시 정리 경로(현재 표시 중인 SHOT 보존 + 폴백 성공 시 즉시 dispose)는 기존 534c742와 동일하게 동작한다(회귀 없음). `CleanupBatchImageMemoryAfterCycle` 호출은 `OnBatchComplete` 1곳뿐이다(SINGLE_TRIGGER_OK). 동기적 블로킹 대기가 어디에도 없다(NO_SYNC_WAIT_OK). `ShotConfig.ResolveFallbackImagePath`/`InspectionSequence.ClearCrossZImagesAfterBatchCycle`/`MainView.DisplayShotImage` 디스크 폴백은 그대로 남아있다. 빌드 error 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Task 1이 `CleanupBatchImageMemoryAfterCycle`을 재작성했다: 폴백 파일이 이미 있는 SHOT은 기존과 동일하게 즉시 정리하고(배치 앞쪽 SHOT, 무변경), 폴백이 아직 없는 SHOT(배치 뒤쪽 — 저장 큐 지연)은 버리지 않고 `_pendingImageCleanup` 대기열에 넣는다. `DispatcherTimer`(UI 스레드, 5초 간격)가 대기열이 빌 때까지 주기적으로 재확인하며, 폴백이 생기면 그때 정리하고, 사용자가 그 SHOT을 보고 있으면 정리하지 않고 대기열에서만 제거하며, SHOT별 최대 24회(약 2분) 재시도해도 안 되면 조용히 포기한다. 동기적 블로킹 대기는 전혀 추가하지 않았다(UI/시퀀스 스레드 프리징 없음).
자동 검증(Task 1)으로 빌드 PASS + 구조적 배선(TEMP DIAG 제거, 재시도 메커니즘 존재, 단일 트리거, 동기 대기 없음)을 확인했지만, **실제로 배치 뒤쪽 SHOT이 결국 정리되는지, 그리고 재시도 대기 중/재시도로 정리된 SHOT을 조작해도 안전한지는 자동 검증이 불가능하다** — 오늘 재현했던 정확히 그 시나리오로, 이번엔 더 길게 관찰해야 한다(이 수정의 전제 자체가 "즉시"가 아니라 "결국(큐가 따라잡으면)"이기 때문).
  </what-built>
  <how-to-verify>
1. 실행 중인 이전 인스턴스가 있으면 완전히 종료한다. 최신 커밋 기준으로 Debug/x64 재빌드 후 앱을 새로 실행한다.
2. PowerShell에서 메모리를 실시간 관찰할 준비를 한다(별도 창):
   ```powershell
   while ($true) { $p = Get-Process DatumMeasurement -ErrorAction SilentlyContinue; if ($p) { "{0:HH:mm:ss} {1:N0} MB" -f (Get-Date), ($p.WorkingSet64/1MB) }; Start-Sleep -Seconds 2 }
   ```
3. 트리에서 BOTTOM 시퀀스를 선택하고, 오늘 재현 때와 동일하게 약 30개 측정 항목(SHOT)을 체크한다.
4. 임의의 SHOT/FAI/Measurement 노드 하나를 클릭해 화면에 이미지가 표시된 상태로 둔다(이 노드가 "현재 표시 중인 노드" — 재시도 도중에도 이 노드만은 예외 없이 정리되면 안 된다).
5. "일괄검사" 버튼을 눌러 1사이클을 실행하고 완료를 기다린다.
6. 사이클 완료 후 **아무것도 클릭하지 말고 1~2분 이상 대기**하며 PowerShell 메모리 로그를 관찰한다(참고: 저장 큐 지연이 세션이 진행될수록 10초대에서 15초 이상까지 늘어나는 특성이 있었으므로, 필요하면 더 길게 기다린다 — `D:\Data\Error\...` 로그의 `[CaptureImageSaveService] ... depth=`가 0 근처로 내려가면 큐가 거의 비워진 신호다).
   - **(a) 확인**: 사이클 완료 직후엔 메모리가 즉시 다 떨어지지 않을 수 있지만(배치 뒤쪽 SHOT은 대기열에서 재시도 중), 1~2분 이내에 **추가로 계단식으로 더 감소**하는지 확인한다(재시도 타이머가 뒤쪽 SHOT들을 순차적으로 정리하는 신호).
   - **(b) 확인**: 4번에서 표시해뒀던 노드를 **다시 한 번 클릭**해서 이미지가 정상적으로 보이는지 확인한다(사이클 종료 시 뷰어가 다른 SHOT으로 자동 갱신되는 기존 무관 동작이 있으므로 재클릭해서 확인할 것).
7. 대기 중간(예: 사이클 완료 후 10~20초 시점, 아직 재시도가 덜 끝났을 시점)에 트리에서 4번과 **다른** SHOT/FAI/Measurement 노드 여러 개를 순서대로 클릭한다.
   - **(c) 확인**: 아직 재시도 대기열에 있는 SHOT(폴백이 아직 없어 원본 이미지가 메모리/디스크 어디에도 없을 수 있음)을 클릭해도 예외/크래시 없이 동작하는지 확인한다 — 메모리에 아직 이미지가 남아있으면 정상 표시되고, 이미 재시도로 정리됐다면 디스크 폴백으로 표시되며, 정말 아무것도 없는 극히 드문 경우엔 "NO Image"만 뜨고(빈 화면 크래시 아님) 앱이 죽지 않아야 한다.
8. 1~2분 대기 후(재시도가 끝난 뒤), 7번에서 클릭했던 SHOT 중 하나를 선택한 채로 단일 RUN(또는 그 SHOT만 다시 일괄검사)으로 재실행한다.
   - **(d) 확인**: 크래시나 예외 팝업 없이 정상적으로 재검사가 수행되고 결과/이미지가 갱신되는지 확인한다(재시도 타이머가 이미 정리한 SHOT을 재실행해도 문제없어야 한다).
9. (선택) 8번 재실행 직후 몇 초 이내에 다시 일괄검사를 시작해, 이전 사이클의 재시도 대기열이 아직 안 비워진 상태에서 새 사이클이 겹쳐도 앱이 죽거나 멈추지 않는지 확인한다.
  </how-to-verify>
  <resume-signal>(a)(b)(c)(d) 모두 정상이면 "approved". 하나라도 문제가 있으면 어떤 단계에서 무엇이 잘못됐는지(메모리가 1~2분 후에도 안 줄어듦 / 특정 노드 빈 화면·크래시 / 재실행 시 예외 / 새 사이클 겹칠 때 멈춤 등) 구체적으로 기술해서 재개.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음 (신규 trust boundary 미도입) | 이번 변경은 순수 내부 메모리 라이프사이클 관리(정리 시점을 "즉시 실패 시 포기"에서 "나중에 재시도"로 조정)이다. 새로운 외부 입력, 네트워크 노출, 사용자 입력 경로가 추가되지 않는다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260806dsn2-01 | D (Denial of Service — 불필요한 UI 스레드 자원 소모) | `_pendingImageCleanupTimer` | mitigate | 대기열이 비면 `Stop()` 호출(리소스 낭비 방지), 재사용 시(다음 사이클이 대기열에 항목 추가)에만 재시작 — 항상 도는 폴링 루프가 아니다. |
| T-260806dsn2-02 | T (Tampering — 의도치 않은 동시 접근으로 인한 상태 훼손) | `ActionContext.ResultHalconImage` / `ShotConfig._image` | mitigate | 재시도 틱은 대상 SHOT의 소속 시퀀스가 `EContextState.Running`인 동안은 정리를 건너뛰고 다음 틱까지 대기한다 — 시퀀스 스레드가 그 순간 같은 이미지 필드를 쓰고 있을 수 있는 레이스(새 배치/단일 RUN이 재시도 대상 SHOT을 재실행 중인 경우)를 회피. `ShotConfig.ClearImage`/`GetImage`/`SetImage` 자체는 기존 `_imageLock`으로 이미 보호됨(무변경). |
| T-260806dsn2-03 | D (자기 자신 메모리 회수 실패의 영구화) | `_pendingImageCleanup` 무한 누적 | accept | 저장이 영구적으로 실패하는 예외 케이스(디스크 오류 등)에 대비해 SHOT별 재시도 상한(24회, 약 2분)을 두고 초과 시 조용히 포기한다 — 그 SHOT 하나의 메모리 미회수는 감수(요구사항 명시, 예외/사용자 알림 없음). 정상 케이스(저장 큐가 결국 따라잡는 경우)에서는 발생하지 않는다. |
</threat_model>

<verification>
- Task 1의 `<automated>` 커맨드가 "Build succeeded" + error 0, 그리고 각 구조적 체크(DIAG_REMOVED_OK, RETRY_MECHANISM_WIRED_OK, NO_SYNC_WAIT_OK, SINGLE_TRIGGER_OK)가 기대값과 일치.
- `CleanupBatchImageMemoryAfterCycle`은 `OnBatchComplete` 1곳에서만 호출되고, `Btn_start_Click`/사이클 진행 중 어디에도 배선되지 않는다(무변경).
- `ShotConfig.ResolveFallbackImagePath`/`InspectionSequence.ClearCrossZImagesAfterBatchCycle`/`MainView.DisplayShotImage` 디스크 폴백 분기는 이번 plan에서 수정되지 않는다(파일 자체가 `files_modified`에 없음).
- Task 2(checkpoint)의 실기 재검증 (a)(b)(c)(d) 전부 승인.
</verification>

<success_criteria>
- 배치 뒤쪽 SHOT도 즉시는 아니더라도, 저장 큐가 따라잡는 대로(1~2분 이내) 결국 메모리에서 정리된다 — 반복 배치 실행 시 메모리가 누적되지 않고 안정화된다(사용자 실측 확인).
- 사용자가 현재 보고 있는 SHOT은 재시도 도중에도 절대 정리되지 않는다.
- 재시도 대기 중이거나 재시도로 이미 정리된 SHOT을 클릭/재실행해도 크래시나 예외 없이 정상 동작한다.
- 동기적 블로킹 대기(Thread.Sleep/.Wait()/.Result/Task.Delay)가 코드 어디에도 도입되지 않았다 — UI/시퀀스 스레드 프리징 없음.
- `TEMP DIAG`/`[DIAG-260806]` 임시 진단 마커가 코드에서 완전히 제거되었다.
</success_criteria>

<output>
After completion, create `.planning/quick/260806-dsn-overlay-window-reuse/260806-dsn-2-SUMMARY.md`
</output>
