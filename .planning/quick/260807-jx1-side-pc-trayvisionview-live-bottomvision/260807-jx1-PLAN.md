---
phase: quick-260807-jx1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/UI/TrayVisionView.xaml.cs
autonomous: true
requirements: [QUICK-260807-JX1]
tags: [wpf, ui, camera, live-view, dispatcher-timer]

must_haves:
  truths:
    - "TrayVisionView 에서 Live 버튼을 누르면 뷰어에 카메라 프레임이 약 200ms 주기로 계속 갱신된다 (Bottom 탭과 동일 체감)."
    - "Live 동작 중에는 Grab 버튼과 Live 버튼이 모두 비활성화되어 재클릭/혼선이 불가능하다."
    - "Stop 버튼을 누르면 폴링이 즉시 멈추고 Grab/Live 버튼이 다시 활성화되며 상태 라벨이 대기 문구가 된다."
    - "Live 폴링 도중 일시적 예외가 나도 앱이 죽지 않고 다음 틱에서 자연 복구된다."
    - "SIMUL_MODE 빌드와 실HW 빌드 양쪽에서 Live/Stop/타이머 코드가 동일하게 컴파일된다."
  artifacts:
    - "WPF_Example/Custom/UI/TrayVisionView.xaml.cs — _liveTimer 필드 + StartLiveTimer/StopLiveTimer/LiveTimer_Tick 3개 메서드 + 확장된 LiveButton_Click/StopButton_Click"
  key_links:
    - "LiveButton_Click 에서 Camera.Live() 성공 시 StartLiveTimer() 호출 (현재 완전히 누락된 지점 = 근본원인)"
    - "LiveTimer_Tick 에서 EthernetAlignCamera.PeekLastImage() 결과를 MainResultViewerControl.LoadImage 로 전달 (스트리밍 프레임이 화면에 도달하는 유일한 경로)"
    - "StopButton_Click 에서 StopLiveTimer() 를 Camera.Stop() 보다 먼저 호출 (역순이면 정지된 스트림을 계속 Peek 하는 틱이 남음)"
---

<objective>
SIDE PC 의 TrayVisionView 에서 Live 버튼이 실제로 라이브 영상을 보여주도록 고친다.

Purpose: 사용자 보고 "TrayAlign 비전이 라이브가 안돼". 로그 조사 결과 카메라/네트워크/설정 문제가 아님이 확정됐다 — `[ETHERNET] connected: TrayAlign` 정상 연결, HIK SDK 의 OnGrabResult 가 약 200ms 마다 정상 발화하며 GenImage1 도 매번 성공하고 있다. 즉 스트림은 이미 흐르고 있는데 UI 가 그 프레임을 한 번도 가져가지 않는다. `TrayVisionView.LiveButton_Click` 은 `Camera.Live()` 로 SDK 스트림만 켜고 상태 라벨만 바꾼 뒤 끝난다 — 프레임을 뷰어로 끌어오는 타이머가 아예 없다. `EthernetAlignCamera.PeekLastImage()` 는 바로 이 용도(재트리거 없이 최근 스트리밍 프레임만 읽어와 뷰어에 반영)로 이미 존재하지만 TrayVisionView 는 이 메서드를 호출하지 않는다.

`BottomVisionView.xaml.cs` (260724 hbk) 에 이미 검증된 정답 패턴이 있다. 이 플랜은 그 패턴을 TrayVisionView 로 1:1 포팅하는 것이다 — 재설계가 아니라 이식.

Output: `WPF_Example/Custom/UI/TrayVisionView.xaml.cs` 단일 파일 수정. XAML 변경 없음 (컨트롤 이름 btn_grab / btn_live / btn_stop / lbl_status 가 이미 일치함을 확인 완료).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

수정 대상 (유일):
@WPF_Example/Custom/UI/TrayVisionView.xaml.cs

레퍼런스 (읽기 전용 — 절대 수정 금지):
@WPF_Example/Custom/UI/BottomVisionView.xaml.cs
</context>

<reference_pattern>
BottomVisionView.xaml.cs 의 해당 구현 위치. executor 는 이 범위만 읽으면 충분하다 (파일 전체를 읽지 말 것).

| 요소 | Bottom 파일 위치 |
|------|------------------|
| `using System.Windows.Threading;` | line 8 |
| `private DispatcherTimer _liveTimer;` (+ 위 line 43 설명 주석) | line 43-44 |
| `LiveButton_Click` 전체 | line 293-317 |
| `StopButton_Click` 전체 | line 319-337 |
| `StartLiveTimer` | line 339-347 |
| `StopLiveTimer` | line 349-357 |
| `LiveTimer_Tick` | line 359-377 |

**전처리기 배치 확인 (이미 검증됨):** Bottom 파일의 `#if SIMUL_MODE` 블록은 line 250/266/290 (GrabButton_Click 내부)에서 닫히고, 그 다음 전처리기 지시문은 line 732 이다. 따라서 위 Live/Stop/타이머 코드 전체(293-377)는 어떤 조건부 컴파일 블록에도 속하지 않는다. Tray 파일도 동일하게 `#if SIMUL_MODE` 는 GrabButton_Click 내부(line 80-120)에서만 쓰이고 `LiveButton_Click`(line 123) / `StopButton_Click`(line 143)은 이미 그 바깥에 있다 — 현재 위치를 그대로 유지하면 자동으로 조건부 블록 밖이 된다.

**의도적 비-확장 (parity gap — 고치지 말 것):** Bottom 의 `StopLiveTimer` XML 주석은 뷰 전환/언로드 시에도 호출하라고 적혀 있으나, Bottom 은 실제로 Unloaded 이벤트를 후킹하지 않고 `StopButton_Click` 에서만 호출한다. 이 플랜의 목표는 충실한 1:1 포팅이므로 Tray 에도 Unloaded 훅을 추가하지 않는다. Bottom 과의 동작 차이를 만들지 말 것. 이 갭은 SUMMARY 에 알려진 잔여사항으로만 기록한다.
</reference_pattern>

<tasks>

<task type="tracer">
  <name>Task 1: BottomVisionView 의 Live 폴링 타이머 패턴을 TrayVisionView 로 1:1 포팅</name>

  <files>WPF_Example/Custom/UI/TrayVisionView.xaml.cs</files>

  <read_first>
WPF_Example/Custom/UI/BottomVisionView.xaml.cs — line 1-15 (using 블록), line 40-48 (_liveTimer 필드 선언 + 주석), line 293-377 (Live/Stop/타이머 구현 전체). 이 세 구간만 읽는다.
WPF_Example/Custom/UI/TrayVisionView.xaml.cs — 전체 (697줄, 1회 읽기로 충분).
  </read_first>

  <action>
TrayVisionView.xaml.cs 에 아래 4가지를 수행한다. 각 항목은 BottomVisionView 의 대응 코드를 그대로 옮기되 Tray 파일의 기존 브레이스 스타일(K and R — 여는 중괄호가 선언과 같은 줄)을 유지한다. Bottom 도 동일한 스타일이므로 그대로 옮기면 맞는다. 신규/수정 줄에는 프로젝트 관례대로 260807 hbk 날짜 마커 주석을 붙이고, 기존 hbk 마커는 전부 보존한다.

(1) using 추가 — Tray 파일 using 블록에서 `using System.Windows.Controls;` 바로 다음 줄에 `using System.Windows.Threading;` 를 삽입한다. Bottom 도 같은 위치(line 8)이므로 배치가 일치한다. DispatcherTimer 가 이 네임스페이스에 있어 이게 없으면 컴파일 실패한다.

(2) 필드 추가 — Tray 파일의 필드 선언부(클래스 상단, `_viewer` 선언 근처)에 `private DispatcherTimer _liveTimer;` 를 추가한다. 바로 위에 이 타이머가 재트리거 없이 최근 스트리밍 프레임만 읽어 뷰어에 반영하는 용도임을 설명하는 한 줄 주석을 Bottom line 43 과 같은 취지로 붙인다.

(3) LiveButton_Click 교체 — Tray 파일의 기존 LiveButton_Click(현재 line 123-141) 본문을 Bottom line 293-317 구현으로 대체한다. 선두의 Camera null 가드는 그대로 유지한다(미연결 라벨 후 return). 변경 내용: `Camera.Live()` 반환값이 true 인 분기에서 `btn_live.Content` 를 Live 켜짐 문자열로 바꾸고, `btn_grab.IsEnabled` 와 `btn_live.IsEnabled` 를 둘 다 false 로 내린 뒤(Live 중 Grab 금지 + Live 버튼 재클릭 방지, 해제는 오직 Stop 으로만), `StartLiveTimer()` 를 호출한다. false 분기에서는 `btn_live.Content` 를 Live 꺼짐 문자열로 되돌리고 `lbl_status.Text` 를 미연결 문구로 설정한다. catch 절에서도 `btn_live.Content` 를 Live 꺼짐 문자열로 되돌린 뒤 `lbl_status.Text` 에 Live 오류 접두어와 `ex.Message` 를 넣는다 — 기존 Tray catch 는 라벨만 건드렸으므로 버튼 글자 복원이 추가되는 지점이다. 버튼 Content 로 쓰는 두 문자열 리터럴은 Bottom line 302 / 309 / 314 의 것을 그대로 사용하고 임의로 다른 표현을 만들지 않는다.

(4) StopButton_Click 교체 + 타이머 메서드 3개 신규 추가 — Tray 파일의 기존 StopButton_Click(현재 line 143-156) 본문을 Bottom line 319-337 구현으로 대체한다. 선두 Camera null 가드는 유지. try 블록 안의 호출 순서가 중요하다: 먼저 `StopLiveTimer()`, 다음 `Camera.Stop()`, 다음 `btn_live.Content` 를 Live 꺼짐 문자열로 복원, 다음 `btn_grab.IsEnabled` 와 `btn_live.IsEnabled` 를 둘 다 true 로 복원, 마지막에 `lbl_status.Text` 를 대기 문구로 설정. catch 는 기존대로 Stop 오류 라벨 갱신만. 이어서 StopButton_Click 바로 다음에 Bottom line 339-377 의 세 메서드를 순서대로 추가한다. `StartLiveTimer()` 는 `_liveTimer` 가 이미 null 이 아니면 즉시 return 하고(중복 타이머 방지), 아니면 Interval 이 200 밀리초인 DispatcherTimer 를 새로 만들어 Tick 에 LiveTimer_Tick 을 구독한 뒤 Start 한다. `StopLiveTimer()` 는 `_liveTimer` 가 null 이면 즉시 return 하고, 아니면 Stop 호출 후 Tick 구독을 해제하고 필드를 null 로 만든다. `LiveTimer_Tick(object, EventArgs)` 는 Camera 가 null 이거나 `_viewer` 가 null 이면 조기 return 하고, HImage 지역변수를 null 로 초기화한 뒤 try 에서 `PeekLastImage()` 결과를 받아 null 이 아닐 때만 `_viewer.LoadImage(img)` 를 호출한다(LoadImage 가 내부적으로 Clone 하므로 즉시 Dispose 해도 안전하다). catch 는 아무 동작 없이 삼키며 상태 라벨을 건드리지 않는다(다음 틱 자연 복구를 기대). finally 에서 null 조건부 Dispose 로 해제한다. 세 메서드의 XML doc 주석도 Bottom 의 것을 가져오되 날짜 마커만 이번 작업 날짜로 맞춘다.

금지사항: GrabButton_Click 과 그 안의 조건부 컴파일 분기는 이미 올바르므로 한 글자도 건드리지 않는다. 티칭/검사/동축/오프라인로더 핸들러도 무관하므로 손대지 않는다. 새 Live/Stop/타이머 코드를 조건부 컴파일 블록 안쪽에 넣지 않는다 — Bottom 과 동일하게 바깥에 두어야 SIMUL 빌드와 실HW 빌드 양쪽에서 컴파일되고 도달 가능하다. Unloaded 이벤트 훅은 추가하지 않는다(reference_pattern 의 parity gap 항목 참조). BottomVisionView.xaml.cs / BottomVisionView.xaml / TrayVisionView.xaml / EthernetAlignCamera.cs 는 열어보기만 하고 저장하지 않는다.
  </action>

  <verify>
    <automated>bash .planning/quick/260807-jx1-side-pc-trayvisionview-live-bottomvision/verify.sh</automated>
    <human-check>SIDE PC 앱 실행 후 Tray 비전 탭에서 Live 클릭 시 영상이 계속 갱신되는지, Live 중 Grab/Live 버튼이 비활성인지, Stop 클릭 시 정지 + 버튼 재활성 + 상태 라벨 대기 로 돌아오는지 확인.</human-check>
  </verify>

  <done>
TrayVisionView.xaml.cs 에 _liveTimer 필드와 StartLiveTimer / StopLiveTimer / LiveTimer_Tick 세 메서드가 존재하고, LiveButton_Click 이 Live 성공 시 StartLiveTimer 를 호출하며, LiveTimer_Tick 이 PeekLastImage 결과를 뷰어로 전달한다. using System.Windows.Threading 이 추가되어 있다. BottomVisionView.xaml.cs / BottomVisionView.xaml / TrayVisionView.xaml / EthernetAlignCamera.cs 는 git diff 상 변경 0. msbuild Debug/x64 가 0 errors 로 통과하고 신규 warning 이 0 이다.
  </done>
</task>

</tasks>

<verification>
검증 스크립트 `verify.sh` 를 플랜 디렉터리에 두고 아래 4단계를 순서대로 수행한다 (executor 가 이 스크립트를 먼저 생성한다).

**S1 — 신규 심볼 존재 확인 (positive grep):** TrayVisionView.xaml.cs 안에 다음 6개 토큰이 모두 존재해야 한다.
`using System.Windows.Threading;` / `DispatcherTimer _liveTimer` / `StartLiveTimer` / `StopLiveTimer` / `LiveTimer_Tick` / `PeekLastImage`
하나라도 없으면 FAIL.

**S2 — 배선 확인:** LiveButton_Click 본문에서 StartLiveTimer 호출이, StopButton_Click 본문에서 StopLiveTimer 호출이 각각 발견되어야 한다. 심볼 정의만 있고 호출이 없으면 근본원인이 안 고쳐진 것이므로 FAIL.

**S3 — 보호 파일 무변경 확인:** `git diff --name-only` 출력에 BottomVisionView / EthernetAlignCamera / TrayVisionView.xaml (확장자가 .xaml 로 끝나는 것) 중 어느 것도 등장하지 않아야 한다. 등장하면 FAIL. (참고: 이 저장소에는 이번 작업과 무관한 기존 미커밋 변경이 있다 — Action_TopInspection.cs, DatumMeasurement.csproj, PatternMatchService.cs. 이들은 STATE.md 에 기록된 사용자의 의도적 변경이므로 건드리지 말고 커밋에도 포함하지 말 것.)

**S4 — 빌드:** `msbuild DatumMeasurement.csproj /p:Configuration=Debug /p:Platform=x64 /t:Build /v:minimal /nologo` 를 WPF_Example 디렉터리에서 실행. 0 errors 여야 하며, warning 수가 수정 전 baseline 대비 증가하지 않아야 한다.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 카메라 SDK 스트림 → UI 스레드 | HIK SDK 콜백이 채운 최근 프레임을 DispatcherTimer 가 폴링해 가져온다. 신뢰 경계를 넘는 외부 사용자 입력은 없음(로컬 산업 장비 내부 데이터 경로). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-jx1-01 | Denial of Service | LiveTimer_Tick 폴링 루프 | medium | mitigate | 매 틱 HImage 를 finally 에서 Dispose 하여 네이티브 이미지 누수 차단. 앱 전반의 HALCON 메모리 이슈(STATE.md 260806-iuy)를 재유발하지 않도록 Bottom 과 동일하게 Clone 후 즉시 해제 패턴 준수. |
| T-jx1-02 | Denial of Service | StartLiveTimer 중복 호출 | low | mitigate | `_liveTimer != null` 조기 return 으로 중복 타이머 누적 방지. 추가로 Live 성공 시 btn_live.IsEnabled=false 로 재클릭 자체를 차단(2중 방어). |
| T-jx1-03 | Denial of Service | Tick 내 예외 반복 | low | accept | catch 가 예외를 삼키고 라벨을 건드리지 않음 — 스트림 일시 오류 시 다음 틱 자연 복구. Bottom 에서 검증된 동작이며 UI 무중단이 요구사항. |
| T-jx1-SC | Tampering | 패키지 설치 | n/a | accept | 신규 패키지 설치 없음(기존 .NET Framework BCL 의 DispatcherTimer 만 사용). 공급망 게이트 해당 없음. |
</threat_model>

<success_criteria>
- S1~S4 전부 PASS.
- 변경 파일이 `WPF_Example/Custom/UI/TrayVisionView.xaml.cs` 단 하나(이 작업 범위 기준).
- 커밋은 이 파일만 스코프해서 수행 — 저장소에 남아있는 기존 미커밋 변경(Action_TopInspection.cs, DatumMeasurement.csproj, PatternMatchService.cs)을 함께 커밋하지 않는다.
- 사용자 실기 확인: SIDE PC 에서 Tray 비전 Live 가 실제로 화면 갱신된다.
</success_criteria>

<output>
Create `.planning/quick/260807-jx1-side-pc-trayvisionview-live-bottomvision/260807-jx1-SUMMARY.md` when done.
SUMMARY 에 반드시 포함: (a) 근본원인 1줄 요약(타이머 누락, 카메라 정상), (b) S1~S4 검증 결과, (c) 알려진 parity gap(Unloaded 훅 미추가 — Bottom 도 동일)을 잔여사항으로 명시.
</output>
