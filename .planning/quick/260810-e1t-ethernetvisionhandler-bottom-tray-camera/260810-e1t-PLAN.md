---
phase: quick-260810-e1t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs
  - WPF_Example/SystemHandler.cs
autonomous: true
requirements: [ETHERNET-CLOSE-01]

must_haves:
  truths:
    - "프로그램 종료(SystemHandler.Release() 실행) 시 EthernetVisionHandler.Handle.Release()가 호출되어 EthernetAlignCamera.Close()가 실행된다 — 이전에는 이 호출부가 코드 전체에 단 한 곳도 없었다"
    - "Camera가 null인 상태(EthernetVisionMode==None이라 연결 시도조차 안 한 경우)에서도 종료 시 예외 없이 정상 종료된다"
    - "Release() 내부에서 예외가 나도 앱 종료(SystemHandler.Release()의 나머지 정리 단계: Sequences.Dispose, Server.Dispose, Lights.Release 등)를 막지 않는다"
    - "이 작업과 무관한 기존 미커밋 실HW 설정 변경 2건(DatumMeasurement.csproj의 SIMUL_MODE 제거, SystemHandler.cs의 memory_allocator 라인 주석)이 그대로 남고 이번 커밋에 섞여 들어가지 않는다"
    - "Debug/x64 Rebuild가 신규 error CS 0건으로 통과한다"
  artifacts:
    - path: "WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs"
      provides: "카메라 종료 진입점 public void Release() — Camera null 가드 + Camera.Close() 호출 + try-catch 방어(절대 throw 안 함)"
      contains: "public void Release()"
    - path: "WPF_Example/SystemHandler.cs"
      provides: "SystemHandler.Release()에서 EthernetVisionHandler.Handle.Release() 호출 배선 (Devices.Dispose() 직후)"
      contains: "EthernetVisionHandler.Handle.Release()"
  key_links:
    - from: "SystemHandler.Release()"
      to: "EthernetVisionHandler.Handle.Release()"
      via: "Devices.Dispose() 직후, belt-and-suspenders 외부 try-catch로 감싸 호출 (Initialize()의 기존 배선과 동일 패턴)"
      pattern: "EthernetVisionHandler\\.Handle\\.Release\\(\\)"
    - from: "EthernetVisionHandler.Release()"
      to: "EthernetAlignCamera.Close()"
      via: "Camera != null 가드 통과 후 Camera.Close() 호출"
      pattern: "Camera\\.Close\\(\\)"
---

<objective>
프로그램 종료 시 이더넷 정렬 카메라(Bottom/Tray, Hik GigE)의 연결이 끊기지 않는 문제를 고친다.

**현재 상태(오케스트레이터가 코드로 이미 확인):**
- `EthernetAlignCamera.Close()`(정상 동작, `_hikCamera.Close()` 내부 호출)는 이미 존재하지만, 저장소 전체에서 이 메서드를 호출하는 곳이 단 한 군데도 없다.
- `EthernetVisionHandler`는 `Initialize()`만 있고 `Close()`/`Release()`류 메서드가 전혀 없다.
- `SystemHandler.Release()`(앱 종료 시 실행되는 유일한 정리 경로)는 Devices/Sequences/RawImageSaver/CaptureImageSaver/Server/Lights를 전부 정리하지만 `EthernetVisionHandler`는 건드리지 않는다.

**해결:** `EthernetVisionHandler`에 `public void Release()`를 신설(내부에서 `Camera.Close()` 호출, null 가드 + try-catch 방어, 절대 throw 안 함 — `Initialize()`와 동일한 방어적 컨벤션)하고, `SystemHandler.Release()`에서 `Devices.Dispose()` 직후 이를 호출하도록 배선한다. 배선 방식은 `SystemHandler.Initialize()`가 이미 `EthernetVisionHandler.Handle.Initialize()`를 belt-and-suspenders 외부 try-catch로 감싸 호출하는 기존 전례를 그대로 따른다.

**⚠ 이 세션 시작 시점에 이미 있던 미커밋 실HW 설정 변경 2건 — 이번 작업과 무관, 절대 건드리지도 되돌리지도 말 것:**

| 파일 | 내용 | 현재 diff hash (`git diff -- <파일> \| git hash-object --stdin`) |
|------|------|------|
| `WPF_Example/DatumMeasurement.csproj` | `DefineConstants`에서 `SIMUL_MODE` 제거 (이 PC는 현재 실HW 모드) | `f0dd3a511bd51a3cc6df91c555d4336df60e0c0d` |
| `WPF_Example/SystemHandler.cs` | `HOperatorSet.SetSystem("memory_allocator", "system");` 한 줄이 주석 처리됨 (약 128행) | `dbf56c603ff7f02cfd00b5e626a3a2cecb83f630` |

`DatumMeasurement.csproj`는 이번 작업에서 전혀 열 필요가 없다 — 건드리지 않는다.
`SystemHandler.cs`는 이번 작업의 편집 대상이지만, 편집 대상 라인(약 267~300행 부근)과 위 memory_allocator 라인(약 128행)은 서로 멀리 떨어져 있어 편집 자체는 간섭하지 않는다. **문제는 커밋 시점** — `git add WPF_Example/SystemHandler.cs`로 파일 전체를 스테이징하면 이 무관한 실HW 실험 변경까지 같이 커밋되어 버린다. Task 2의 커밋 절차(패치 기반 선택적 스테이징)를 그대로 따를 것.

**Output:**
- `EthernetVisionHandler.cs`에 `public void Release()` 신설
- `SystemHandler.Release()`에 호출 배선 1곳 추가
- Debug/x64 Rebuild PASS
- 커밋 1건 (위 두 파일의 "이번 작업분"만 포함, 무관한 실HW 실험 2건은 그대로 미커밋 유지)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/CONVENTIONS.md

**코딩 규칙 (이 프로젝트 상시 규칙 — 위반 시 리뷰 반려):**
- C# 7.2 (`nullable reference types`, `switch expression`, `record` 등 8.0+ 문법 금지)
- **해당 파일의 기존 스타일 유지** — `EthernetVisionHandler.cs`/`SystemHandler.cs` 둘 다 이 프로젝트의 K&R 스타일(여는 중괄호 같은 줄) 파일
- 신규 주석은 `quick-260810-e1t:` 접두 + 짧게, 비자명한 "왜"만. `//YYMMDD hbk` 날짜 주석 규칙은 폐기됨 — 새로 달지 말 것(기존 것은 그대로 보존)
- 메서드 사이 빈 줄 1개 (프로젝트 컨벤션)
- `Camera != null` 명시적 if 가드를 쓴다(`EthernetVisionHandler.cs` 자체는 지금까지 `?.` null-conditional 연산자를 쓴 전례가 없음 — 이 파일 내부 일관성 우선. `EthernetAlignCamera.cs`는 `?.`를 쓰지만 다른 파일이라 무관)

**이미 확정된 사실 (executor는 재조사하지 말 것):**
1. `EthernetAlignCamera.Close()`(`EthernetAlignCamera.cs` 198행)는 이미 정상 동작 — 수정 대상 아님. 이 파일은 열지도 고치지도 않는다.
2. `EthernetVisionHandler.Initialize()`의 유일한 호출부는 `SystemHandler.Initialize()` 맨 끝(234~243행)이며, `EthernetVisionHandler.Handle.Initialize()`를 belt-and-suspenders 외부 try-catch로 감싸는 전례가 이미 있다 — `Release()` 배선도 동일 패턴을 그대로 따른다.
3. `EthernetVisionHandler`는 `SystemHandler`와 같은 `ReringProject` 루트 네임스페이스라 추가 `using` 불필요(`Initialize()` 호출부가 이미 증명).
4. `EthernetVisionHandler.Camera`는 `public EthernetAlignCamera Camera { get; private set; }` — `Mode==None`이면 `Initialize()`가 일찍 `return`해서 `Camera`가 끝까지 `null`로 남는다. `Release()`는 반드시 null 가드 필요.
5. `SystemHandler.Release()`는 앱 종료 시 실행되는 **유일한** 정리 경로(`MainWindow` 종료 이벤트 등에서 호출, 이번 작업 범위 밖이라 재확인 불필요) — 여기 배선하면 충분하다.

<interfaces>
<!-- 실행자가 코드베이스를 탐색할 필요가 없도록 현재 라이브 코드와 목표 코드를 그대로 옮겨둔다. -->

**[현재] `WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs` L88~97 (`Initialize()` 끝 ~ `ShowConnectFailAlarm` 시작)**

```csharp
                if (bModeOn) {
                    ShowConnectFailAlarm(camIp, ex.Message);
                }
            }
        }

        //quick-260807-htd: 연결 실패가 로그에만 남아 사용자가 몰랐다 → 기존 카메라 실패 알림과 같은 수단으로 통일.
        // 스레드 마샬링을 여기서 하지 않는 이유: CustomMessageBox.Show 가 내부에서 이미
        // App.Current.Dispatcher.BeginInvoke 로 넘기므로 호출 스레드 무관하게 안전하다(이중 마샬링 금지).
        // isAutoClosing=false : 기본 7초 자동닫힘을 끈다. 알람은 사용자가 직접 닫아야 한다.
        private void ShowConnectFailAlarm(string camIp, string exMessage) {
```

**[목표] `Initialize()`의 닫는 중괄호(`}`) 바로 다음, `ShowConnectFailAlarm` 주석 블록 바로 앞에 아래를 삽입 (빈 줄로 앞뒤 구분)**

```csharp
        //quick-260810-e1t: 프로그램 종료 시 이 카메라 연결을 끊는 진입점이 지금까지 없었다
        // (EthernetAlignCamera.Close() 를 호출하는 곳이 코드 전체에 단 한 곳도 없었음) → 앱이 꺼져도
        // 카메라 연결이 안 끊긴 채 남는 문제. Camera 는 Mode==None 이면 null 이므로 반드시 가드.
        // 절대 throw 하지 않는다 — Initialize() 와 동일한 방어적 컨벤션(SystemHandler.Release() 가
        // 앱 종료 경로에서 호출하므로 여기서 예외가 새면 다른 리소스 정리가 중단된다).
        public void Release() {
            try {
                if (Camera != null) {
                    Camera.Close();
                    Logging.PrintLog((int)ELogType.Camera, "[ETHERNET] camera closed on release");
                }
            }
            catch (Exception ex) {
                Logging.PrintLog((int)ELogType.Error, "[ETHERNET] EthernetVisionHandler.Release error: {0}", ex.Message);
            }
        }
```

**[현재] `WPF_Example/SystemHandler.cs` L267~280 (`Release()` 시작 부분)**

```csharp
        public void Release() {
            // Persist settings.
            Setting.Save();

            // Release device resources.
            Devices.Dispose();

            //260510 hbk Phase 21: BUF-02 channel #1 — subscriber 해제 (Sequences 가 살아있는 동안 unwire)
            UnwireBufferLifecycle();
```

**[목표] `Devices.Dispose();` 다음 줄(빈 줄 포함)에 아래를 삽입, 그 다음에 기존 BUF-02 주석/코드가 이어진다**

```csharp
            // Release device resources.
            Devices.Dispose();

            //quick-260810-e1t: 이더넷 정렬 카메라(Bottom/Tray)는 Devices(Grabber)와 별도 핸들러라 여기서
            // 명시적으로 닫아야 한다 — 안 그러면 프로세스 종료 후에도 카메라 연결이 안 끊긴다.
            // EthernetVisionHandler.Release() 는 내부 전체 try-catch 로 보호되어 절대 throw 하지 않는다
            // (Initialize() 와 동일 패턴, SystemHandler.Initialize() 234~243행 전례). 이 외부 catch 는
            // 방어적 레이어로만 존재.
            try {
                EthernetVisionHandler.Handle.Release();
            }
            catch (Exception ex) {
                Logging.PrintLog((int)ELogType.Error, "[ETHERNET] EthernetVisionHandler.Release failed: {0}", ex.Message);
            }

            //260510 hbk Phase 21: BUF-02 channel #1 — subscriber 해제 (Sequences 가 살아있는 동안 unwire)
            UnwireBufferLifecycle();
```

**참고: 그대로 따라 하는 기존 전례 — `WPF_Example/SystemHandler.cs` L234~243 (`Initialize()`의 대응 배선)**

```csharp
            try {
                EthernetVisionHandler.Handle.Initialize();
            }
            catch (Exception ex) {
                Logging.PrintLog((int)ELogType.Error, "[ETHERNET] EthernetVisionHandler.Initialize failed: {0}", ex.Message);
            }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EthernetVisionHandler.Release() 신설</name>
  <files>WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs</files>
  <action>
`WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs` 한 파일만 편집한다.
위 `<interfaces>`의 첫 번째 **[목표]** 블록을 `Initialize()`의 닫는 중괄호 다음, `ShowConnectFailAlarm` 주석 블록 앞에 그대로 삽입한다. 다른 줄은 한 글자도 건드리지 않는다.

- `Camera != null` 명시적 if 가드 사용 (이 파일의 기존 스타일).
- `Camera.Close()`는 이미 자체 try-catch로 방어되어 있는 기존 메서드지만(EthernetAlignCamera.cs 198행), 이 Release()의 바깥 try-catch는 방어의 이중 레이어이며 `Logging.PrintLog` 호출 자체가 실패하는 등 예상 밖 예외까지 흡수하기 위함이다 — 제거하지 말 것.
- `EthernetAlignCamera.cs`는 열지도 고치지도 않는다.
  </action>
  <verify>
    <automated>cd "C:/code/DataMeasurement" && F=WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs && CODE=$(grep -v '^[[:space:]]*//' "$F") && echo "=== [1] public void Release() : 1 기대 ===" && (echo "$CODE" | grep -c 'public void Release()' || echo 0) && echo "=== [2] Camera != null 가드 : 1 기대 ===" && (echo "$CODE" | grep -c 'Camera != null' || echo 0) && echo "=== [3] Camera.Close() 호출 : 1 기대 ===" && (echo "$CODE" | grep -c 'Camera.Close()' || echo 0) && echo "=== [4] EthernetAlignCamera.cs 무변경 : 빈 출력 기대 ===" && git status --porcelain -- WPF_Example/Custom/EthernetVision/EthernetAlignCamera.cs</automated>
  </verify>
  <done>[1]=1, [2]=1, [3]=1, [4]=빈 출력(EthernetAlignCamera.cs 무변경). 신규 메서드가 K&R 스타일로 삽입되어 있고, `Initialize()`/`ShowConnectFailAlarm` 기존 코드는 한 글자도 바뀌지 않았다.</done>
</task>

<task type="auto">
  <name>Task 2: SystemHandler.Release() 배선 + Rebuild + 선택적 스테이징 커밋</name>
  <files>WPF_Example/SystemHandler.cs</files>
  <action>
`WPF_Example/SystemHandler.cs` 한 파일만 편집한다. 위 `<interfaces>`의 두 번째 **[목표]** 블록을 `Devices.Dispose();` 다음(BUF-02 주석 앞)에 그대로 삽입한다. 다른 줄은 한 글자도 건드리지 않는다 — 특히 약 128행의 `// HOperatorSet.SetSystem("memory_allocator", "system");` 주석 라인은 이번 작업과 무관하니 그대로 둔다(되살리지도, 다시 지우지도 않는다).

**빌드.** MSBuild 경로는 이 PC 기준 `C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe`다(못 찾으면 `vswhere.exe -latest -requires Microsoft.Component.MSBuild -find "MSBuild/**/Bin/MSBuild.exe"`로 재확인). Git Bash에서는 `-p:`/`-t:` 대시 프리픽스를 쓴다. 빌드에 1~2분 걸릴 수 있으니 Bash 툴 timeout을 300000으로 준다. 앱이 켜져 있어 `MSB3021/3026/3027/3030` 잠금 오류가 나면 프로세스를 절대 죽이지 말고 `-p:OutputPath="$TEMP/gsd-e1t-scratch/bin/" -p:BaseIntermediateOutputPath="$TEMP/gsd-e1t-scratch/obj/"`로 컴파일만 재확인하고 SUMMARY에 "산출물 잠김으로 스크래치 컴파일 검증"이라고 남긴다.

**커밋 — 선택적 스테이징 필수.** `SystemHandler.cs`에는 이번 작업과 무관한 기존 미커밋 변경(memory_allocator 주석 라인, 약 128행)이 이미 있다. `git add WPF_Example/SystemHandler.cs`로 파일 전체를 스테이징하면 그 무관한 변경까지 같이 커밋되어 버리므로, 아래 패치 기반 절차로 이번 작업분(Release() 배선 hunk)만 골라 스테이징한다:

1. 편집 전에 이미 있던 diff를 패치로 저장: `git diff -- WPF_Example/SystemHandler.cs > "$(mktemp -t e1t-preexisting.XXXXXX)"`처럼 임시 파일 경로를 변수에 저장해 둔다(예: `PATCH=$(mktemp); git diff -- WPF_Example/SystemHandler.cs > "$PATCH"`) — **이 명령은 파일 편집 전에 실행**(편집 전 diff = memory_allocator 라인 1개짜리 hunk만 들어있어야 함, 위 표의 해시 `dbf56c603ff7f02cfd00b5e626a3a2cecb83f630`와 일치하는지 `git hash-object "$PATCH"`로 먼저 확인).
2. 편집을 수행한다(Release() 배선 삽입).
3. `git add WPF_Example/SystemHandler.cs`로 파일 전체(무관한 변경 + 이번 작업분)를 스테이징한다.
4. `git apply --cached --reverse "$PATCH"`로 인덱스에서만 무관한 memory_allocator hunk를 되돌린다(워킹 디렉터리는 그대로 — `--cached`이므로 실제 파일의 주석 처리는 안 건드려짐). 이제 인덱스에는 이번 작업분(Release() 배선)만 남는다.
5. 확인: `git diff --cached -- WPF_Example/SystemHandler.cs`에 `EthernetVisionHandler.Handle.Release()`는 있고 `memory_allocator`는 없어야 한다. 하나라도 어긋나면 `git reset WPF_Example/SystemHandler.cs`로 스테이징을 취소하고 다시 시도한다 — 잘못된 상태로 커밋하지 않는다.
6. `git add WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs` (Task 1 결과물, 전체 스테이징 — 이 파일은 무관한 변경이 없음).
7. 커밋: `git commit -m "fix(quick-260810-e1t): EthernetVisionHandler 종료 시 카메라 Close() 호출 배선"` (Write/Edit 도구가 아닌 git 커밋 메시지이므로 heredoc 불필요, 한 줄 메시지로 충분).
8. `WPF_Example/DatumMeasurement.csproj`는 `git add` 하지 않는다 — 이번 작업과 무관.
  </action>
  <verify>
    <automated>cd "C:/code/DataMeasurement" && F=WPF_Example/SystemHandler.cs && CODE=$(grep -v '^[[:space:]]*//' "$F") && echo "=== [1] EthernetVisionHandler.Handle.Release() 호출 : 1 기대 ===" && (echo "$CODE" | grep -c 'EthernetVisionHandler.Handle.Release()' || echo 0) && echo "=== [2] 순서 확인 (Devices.Dispose -> Release() -> UnwireBufferLifecycle) ===" && grep -n 'Devices.Dispose()\|EthernetVisionHandler.Handle.Release()\|UnwireBufferLifecycle()' "$F" && echo "=== [3] Debug/x64 Rebuild ===" && "/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" -t:Rebuild -p:Configuration=Debug -p:Platform=x64 -v:minimal -nologo 2>&1 | grep -iE "error CS|error MSB|warning CS|Build succeeded" | head -25 && echo "=== [4] 커밋 후 DatumMeasurement.csproj diff hash 불변 (f0dd3a511bd51a3cc6df91c555d4336df60e0c0d 기대) ===" && (git diff -- WPF_Example/DatumMeasurement.csproj | git hash-object --stdin) && echo "=== [5] 커밋 후 SystemHandler.cs 잔여 diff hash 불변 (dbf56c603ff7f02cfd00b5e626a3a2cecb83f630 기대 — memory_allocator 라인만 남아야 함) ===" && (git diff -- WPF_Example/SystemHandler.cs | git hash-object --stdin) && echo "=== [6] 최신 커밋 파일 목록 (EthernetVisionHandler.cs + SystemHandler.cs 만 기대) ===" && git show --stat -1 --format="%s"</automated>
  </verify>
  <done>
- [1]=1. [2] 세 패턴이 `Devices.Dispose()` → `EthernetVisionHandler.Handle.Release()` → `UnwireBufferLifecycle()` 순서로 나온다.
- [3] `Build succeeded`, 신규 `error CS` 0건.
- [4] `f0dd3a511bd51a3cc6df91c555d4336df60e0c0d` — DatumMeasurement.csproj 변경분은 커밋에 섞이지 않고 그대로 미커밋 유지.
- [5] `dbf56c603ff7f02cfd00b5e626a3a2cecb83f630` — SystemHandler.cs의 memory_allocator 주석 라인만 여전히 미커밋으로 남고(우리 배선은 커밋됨), 두 변경이 뒤섞이지 않았음을 증명.
- [6] 커밋 메시지 확인 + `git show --stat -1`에 `EthernetVisionHandler.cs`와 `SystemHandler.cs` 두 파일만 등장(DatumMeasurement.csproj는 없음).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (신규 없음) | 이번 변경은 앱 종료 경로에 기존 카메라 정리 메서드 호출 1개를 추가하는 로컬 리소스 정리 배선일 뿐이다. 네트워크 입력 파싱, 파일 I/O, 외부 명령 실행, 신규 패키지 설치 모두 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-e1t-01 | Denial of Service | `EthernetVisionHandler.Release()`가 앱 종료 경로(`SystemHandler.Release()`)에서 호출됨 | mitigate | `Release()` 전체를 try-catch로 감싸 절대 throw 안 함(`Initialize()`와 동일 컨벤션) + `SystemHandler.Release()` 쪽에도 belt-and-suspenders 외부 try-catch 추가 — 여기서 예외가 나도 `Sequences.Dispose()`/`Server.Dispose()`/`Lights.Release()` 등 나머지 종료 단계가 중단되지 않는다 |
| T-e1t-02 | Denial of Service | `Camera.Close()` → `_hikCamera.Close()`가 하드웨어/드라이버 응답을 기다리며 블로킹될 가능성 | accept | `EthernetAlignCamera.Close()`/`HikCamera.Close()` 내부 로직은 기존 코드 그대로이며 이번 변경 범위 밖. 타임아웃 처리가 없는 것은 기존 카메라 종료 경로(`DeviceHandler.Dispose()` 등)와 동일한 리스크 수준이며 이번 배선으로 새로 도입되는 위험이 아니다 |
| T-e1t-SC | Tampering | npm/pip/cargo 설치 | N/A | 이번 작업에 패키지 설치 없음 (기존 어셈블리 참조만 사용) |
</threat_model>

<verification>
- Task 1 `<automated>` 4개 항목 전부 기대값 일치.
- Task 2 `<automated>` 6개 항목 전부 기대값 일치 (특히 [4]/[5] 해시 불변 — 무관한 실HW 실험 변경이 이번 커밋에 섞이지 않았음을 증명).
- `git log -1 --stat`이 `EthernetVisionHandler.cs` + `SystemHandler.cs` 두 파일 커밋임을 보여준다.
- `git status --porcelain`에 `DatumMeasurement.csproj`(무관, 미변경)와 `SystemHandler.cs`(memory_allocator 라인만, 미변경)가 그대로 남아있다.
</verification>

<success_criteria>
- 프로그램 종료 시 `EthernetVisionHandler.Handle.Release()` → `EthernetAlignCamera.Close()` → `_hikCamera.Close()`가 실제로 호출된다 (기존에는 이 경로가 전혀 없었음).
- `Camera == null`(Mode==None) 상태에서도 예외 없이 정상 종료.
- Debug/x64 Rebuild PASS, 신규 `error CS` 0건.
- 이번 작업과 무관한 미커밋 실HW 설정 변경 2건(csproj SIMUL_MODE, SystemHandler.cs memory_allocator)은 커밋에 섞이지 않고 그대로 보존.
- 실기 카메라 연결 후 종료→재연결 확인은 사용자가 나중에 별도로 수행(이 플랜은 빌드 성공 확인으로 충분).
</success_criteria>

<output>
Create `.planning/quick/260810-e1t-ethernetvisionhandler-bottom-tray-camera/260810-e1t-SUMMARY.md` when done
</output>
