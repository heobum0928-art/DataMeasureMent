---
phase: quick-260812-fq4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs
autonomous: true
requirements: [FQ4-01]

must_haves:
  truths:
    - "캡쳐/결과 이미지에 그려지는 일반 오버레이(측정선·기타선·X 마커·Datum 축선·중심/원점 십자)가 기존보다 정확히 1.5배 굵게 렌더링된다 — dilation 반경 2.0 → 3.0"
    - "측정 거리선(cyan, FAI-DistLine)이 1.5배 굵어진다 — 3.1 → 4.65"
    - "Datum 검출 원의 링이 1.5배 두꺼워진다 — 6.0 → 9.0"
    - "세 값의 상대 비율이 정확히 보존된다: 2.0 : 3.1 : 6.0 = 3.0 : 4.65 : 9.0 (전부 정확히 ×1.5). 어느 하나만 튀지 않는다"
    - "캡쳐 렌더링 로직은 단 한 줄도 바뀌지 않는다 — 리전 생성(GenRegionLine/GenCircle), dilation, 페인트(PaintRegionColor), z-order 3레이어 순서, 링 innerR 계산과 clamp, Dispose 정리 전부 무변경"
    - "상수 6개 중 '길이' 상수 3개(MarkerHalfSize=8.0, DatumCircleCenterCrossHalf=12.0, DatumOriginCrossHalf=20.0)는 무변경으로 남는다 — 이번 요청은 '두께'에 한정된다(마커/십자의 크기는 그대로, 획만 굵어진다)"
    - "화면 실시간 뷰 렌더링 경로(HalconDisplayService.cs / MainResultViewerControl.xaml.cs 의 SetLineWidth 계열)는 1바이트도 변하지 않는다 — 이번 요청은 '캡쳐 Overlay' 한정"
    - "작업 시작 전부터 미커밋 상태인 PickerCenterCalibrationService.cs 는 이번 작업으로 변하지 않고 커밋에도 포함되지 않는다"
    - "Debug/x64 컴파일이 신규 error CS 0 / 신규 warning CS 0 으로 통과한다"
  artifacts:
    - path: "WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs"
      provides: "캡쳐 오버레이 두께 상수 3종을 비율 유지한 채 일괄 1.5배 상향 (2.0/3.1/6.0 → 3.0/4.65/9.0)"
      contains: "DistLineThicknessRadius = 4.65"
  key_links:
    - from: "LineThicknessRadius = 3.0"
      to: "DrawLineAsRegion(radius) / HOperatorSet.DilationCircle"
      via: "동일 파일 내 5개 사용처(L229 기타선, L257 FAI 측정선, L292 X 마커, L344 Datum 축선, L378 십자) — 호출부 무수정, 상수값만으로 전파"
      pattern: "private const double LineThicknessRadius = 3\\.0;"
    - from: "DistLineThicknessRadius = 4.65"
      to: "L225 FAI-DistLine cyan 거리선"
      via: "DrawLineAsRegion 의 radius 인자 (유일 사용처 1곳)"
      pattern: "private const double DistLineThicknessRadius = 4\\.65;"
    - from: "DatumRingThickness = 9.0"
      to: "L353 innerR = radius - DatumRingThickness (링 = outer − inner Difference)"
      via: "기존 clamp `if (innerR < 1.0) innerR = 1.0;` 를 그대로 경유 — 신규 가드 추가 금지"
      pattern: "private const double DatumRingThickness = 9\\.0;"
---

<objective>
캡쳐/결과 이미지에 번인(burn-in)되는 오버레이가 너무 얇아 잘 안 보인다는 사용자 요청에 따라,
`OverlayCaptureRenderer.cs` 의 두께 상수 **3개를 비율 유지한 채 일괄 1.5배** 로 올린다.

**이게 전부다 — 상수값 3개 변경.** 로직·호출부·시그니처·다른 파일은 전혀 건드리지 않는다.

Purpose: 사용자가 "전체 다 굵게(비율 유지)"로 확정했다. 비율 유지가 요구사항인 이유는,
값마다 다른 배율을 쓰면 지금 의도적으로 유지되고 있는 상대 관계(거리선이 일반선보다 1.55배 굵고,
Datum 링이 일반선보다 3배 두꺼움 — 겹쳐 그려질 때 어느 선이 어느 것인지 구분하는 시각 단서)가 깨지기 때문이다.

Output:
- `LineThicknessRadius`      2.0  → 3.0
- `DistLineThicknessRadius`  3.1  → 4.65   (= 3.1 × 1.5)
- `DatumRingThickness`       6.0  → 9.0
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

**코딩 규칙 (이 프로젝트 상시 규칙):**
- 삼항연산자 `?:` **금지** → `if / else` (이번 작업엔 조건문 자체가 없다)
- C# 7.2 (8.0+ 문법 금지)
- 이 파일의 기존 브레이스 스타일(Allman) 유지 — 이번엔 브레이스를 건드릴 일이 없다
- 신규 주석은 `quick-260812-fq4:` 접두 + **짧게**, 비자명한 "왜"만.
  `//YYMMDD hbk` 날짜 주석 규칙은 2026-06-11 부로 폐기 — 새로 달지 말 것(기존 것은 보존)

**범위 판단: 다른 파일에 같이 반영할 상수가 있는가 → 없다 (조사 완료, 추가 조사 불필요)**

저장소 전체 grep 결과, 이 세 상수명은 **`OverlayCaptureRenderer.cs` 에서만 선언되고 이 파일 안에서만 사용된다.**
다른 파일에 재사용/중복 정의된 곳은 0건이다. 유사해 보이지만 **건드리면 안 되는** 것들:

| 위치 | 내용 | 판단 |
|------|------|------|
| `Halcon/Display/HalconDisplayService.cs` (~40곳) | `window.SetLineWidth(1~4)` 정수 리터럴 | ❌ **무변경**. 화면 실시간 HWindow 렌더링 경로로, 캡쳐 렌더러와 완전히 별개의 독립 두께 체계다. 사용자 요청은 "캡쳐 Overlay" 한정 |
| `UI/ContentItem/MainResultViewerControl.xaml.cs:586` | `window.SetLineWidth(2)` | ❌ **무변경**. 실시간 뷰어 |
| `UI/Statistics/StatisticsWindow.xaml.cs:426` | `pl.StrokeThickness = 1.5` | ❌ **무변경**. WPF 차트 선, 무관 |
| **같은 파일** `MarkerHalfSize = 8.0`, `DatumCircleCenterCrossHalf = 12.0`, `DatumOriginCrossHalf = 20.0` | 마커/십자 **반길이** | ❌ **무변경**. 이건 '두께'가 아니라 '크기'다. 이 도형들의 획 굵기는 `LineThicknessRadius` 를 쓰므로 이번 변경만으로 이미 굵어진다 |

**⚠ 작업 전 이미 미커밋인 파일 (손대지 말 것):**
`WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs` — 사용자의 **별건** 미커밋 실험이다.
읽지도 고치지도 말고 커밋에도 넣지 말 것.
baseline: `git diff --numstat` = `6  2`, diff sha = `73a89c282724fedf25b7dcf8919b09251578d789`. 작업 후에도 동일해야 한다.

<interfaces>
<!-- 실행자가 코드베이스를 탐색할 필요가 없도록 현재 라이브 코드를 그대로 옮겨둔다. -->

`WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs` **L166~173 (편집 대상 블록 전체)**:

```csharp
        // 색상 규칙은 HalconDisplayService.Render 의 FAI 오버레이 색상과 일치: FAI-Edge*(녹/적),
        // FAI-DistLine(청록), FAI-EdgeRaw(노랑 점), 그 외(파랑). FAI-Edge* 검출점 X 마커는 magenta.
        private const double LineThicknessRadius = 2.0; // 에지/마커 리전 두께(dilation 반경)
        private const double DistLineThicknessRadius = 3.1; // 측정 거리선(cyan) 두께(사용자 요청, 2.6→3.1)
        private const double MarkerHalfSize = 8.0; // X 마커 반길이(HalconDisplayService size=8.0 일치)
        private const double DatumRingThickness = 6.0; // datum 검출 원 링 두께(px), pale green 가시성↑
        private const double DatumCircleCenterCrossHalf = 12.0; // 원 중심 십자 반길이(UI L913 일치)
        private const double DatumOriginCrossHalf = 20.0; // 검출 원점 십자 반길이(UI L318 일치)
```

**사용처 7곳 (전부 무수정 — 상수값만 바뀌면 자동 전파):**
```
L225  DrawLineAsRegion(..., "cyan",  DistLineThicknessRadius)   // FAI-DistLine 거리선
L229  DrawLineAsRegion(..., "blue",  LineThicknessRadius)       // 그 외 선
L257  DrawLineAsRegion(..., lineColor, LineThicknessRadius)     // FAI-Edge 측정선(녹/적), z-order 최상위
L292  HOperatorSet.DilationCircle(u, out dilated, LineThicknessRadius)  // X 마커
L344  DrawLineAsRegion(..., color, LineThicknessRadius)         // Datum 축선(yellow/cyan)
L353  double innerR = radius - DatumRingThickness;              // Datum 원 링
L378  HOperatorSet.DilationCircle(u, out dilated, LineThicknessRadius)  // 십자(＋)
```

**L353 링 계산부 — 읽어만 두고 수정하지 말 것 (기존 clamp 가 이미 있다):**
```csharp
                double innerR = radius - DatumRingThickness;
                if (innerR < 1.0) innerR = 1.0;
                HOperatorSet.GenCircle(out outer, row, col, radius);
                HOperatorSet.GenCircle(out inner, row, col, innerR);
                HOperatorSet.Difference(outer, inner, out ring);
```
링은 **바깥 반지름을 검출 반지름에 고정한 채 안쪽으로** 두꺼워진다.
따라서 `9.0` 에서는 반지름 ≤ 10px 인 작은 원이 사실상 꽉 찬 원반으로 보인다.
이는 **기존 clamp 로 이미 처리되는 알려진 동작이며, 이번 요청(전부 굵게)의 의도된 결과다.**
→ 신규 가드/스케일링 로직을 추가하지 말 것. (실기에서 문제가 되면 별건으로 처리)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 캡쳐 오버레이 두께 상수 3종 1.5배 상향</name>
  <files>WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs</files>
  <action>
`OverlayCaptureRenderer.cs` 에서 **아래 4줄 블록(L168~171)을 통째로 찾아 교체**한다.
`MarkerHalfSize` 줄은 값이 아니라 **앵커로만** 포함된 것이다 — 내용을 바꾸지 말고 그대로 다시 써라
(3개 상수가 연속이 아니라 중간에 끼어 있어서, 한 번의 명확한 교체로 만들기 위해 포함했다).

**찾을 문자열 (편집 전, 정확히 이 4줄):**
```csharp
        private const double LineThicknessRadius = 2.0; // 에지/마커 리전 두께(dilation 반경)
        private const double DistLineThicknessRadius = 3.1; // 측정 거리선(cyan) 두께(사용자 요청, 2.6→3.1)
        private const double MarkerHalfSize = 8.0; // X 마커 반길이(HalconDisplayService size=8.0 일치)
        private const double DatumRingThickness = 6.0; // datum 검출 원 링 두께(px), pale green 가시성↑
```

**바꿀 문자열 (편집 후, 정확히 이 6줄):**
```csharp
        // quick-260812-fq4: 캡쳐 오버레이가 얇아 잘 안 보인다는 요청 → 두께 3종을 비율 유지한 채 일괄 1.5배.
        //  4.65 는 임의값이 아니라 3.1×1.5 다. 길이 상수(MarkerHalfSize/CrossHalf)는 두께가 아니므로 무변경.
        private const double LineThicknessRadius = 3.0; // 에지/마커 리전 두께(dilation 반경)
        private const double DistLineThicknessRadius = 4.65; // 측정 거리선(cyan) 두께(사용자 요청, 2.6→3.1→4.65)
        private const double MarkerHalfSize = 8.0; // X 마커 반길이(HalconDisplayService size=8.0 일치)
        private const double DatumRingThickness = 9.0; // datum 검출 원 링 두께(px), pale green 가시성↑
```

변경 요약 (이게 전부다):
| 상수 | 전 | 후 | 배율 |
|------|-----|-----|------|
| `LineThicknessRadius` | 2.0 | **3.0** | ×1.5 |
| `DistLineThicknessRadius` | 3.1 | **4.65** | ×1.5 |
| `DatumRingThickness` | 6.0 | **9.0** | ×1.5 |

주석 변경은 딱 2가지만:
1. 신규 2줄 — `4.65` 라는 어중간한 값이 오타나 임의값이 아님을 남기기 위함(비자명한 "왜")
2. `DistLineThicknessRadius` 꼬리 주석의 이력 `2.6→3.1` → `2.6→3.1→4.65` (안 고치면 사실과 어긋나는 주석이 된다)

**추가로 하지 말 것:**
- 사용처 7곳(L225/229/257/292/344/353/378) 수정 **금지** — 상수라서 자동 전파된다
- `innerR` clamp 변경·신규 가드 추가 **금지**
- `MarkerHalfSize` / `DatumCircleCenterCrossHalf` / `DatumOriginCrossHalf` 변경 **금지**
- `HalconDisplayService.cs` / `MainResultViewerControl.xaml.cs` 등 다른 파일 열기·수정 **금지**
- `VersionDefine.cs` 버전 상향 **이번 범위 밖** (사용자가 별도 커밋으로 관리)
- 리팩토링(상수를 설정값으로 승격, 배율 상수 도입 등) **금지** — 요청 범위 밖

**빌드:** 먼저 정상 경로 빌드를 시도한다. 산출물이 실행 중인 앱에 잠겨
`MSB3021 / MSB3026 / MSB3027 / MSB3030` 이 나오면 → **절대 프로세스를 죽이지 말고**
아래 verify 의 스크래치 OutDir 방식으로 컴파일만 검증한 뒤,
SUMMARY 에 "산출물 잠김으로 스크래치 컴파일 검증, 정식 bin 카피 실패" 라고 그대로 보고한다.
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && F=WPF_Example/Halcon/Display/OverlayCaptureRenderer.cs && echo "=== [A] 구값 3개 잔존: 전부 0 이어야 PASS ===" && printf 'old_2.0='; grep -c -F "double LineThicknessRadius = 2.0" "$F" || true; printf 'old_3.1='; grep -c -F "DistLineThicknessRadius = 3.1;" "$F" || true; printf 'old_6.0='; grep -c -F "DatumRingThickness = 6.0" "$F" || true; echo "=== [B] 신값 3개: 전부 1 이어야 PASS ===" && printf 'new_3.0='; grep -c -F "double LineThicknessRadius = 3.0;" "$F" || true; printf 'new_4.65='; grep -c -F "DistLineThicknessRadius = 4.65;" "$F" || true; printf 'new_9.0='; grep -c -F "DatumRingThickness = 9.0;" "$F" || true; echo "=== [C] 길이 상수 3개 무변경: 전부 1 이어야 PASS ===" && printf 'marker8='; grep -c -F "MarkerHalfSize = 8.0" "$F" || true; printf 'crossC12='; grep -c -F "DatumCircleCenterCrossHalf = 12.0" "$F" || true; printf 'crossO20='; grep -c -F "DatumOriginCrossHalf = 20.0" "$F" || true; printf 'const_total(6 기대)='; grep -c "private const double" "$F" || true; echo "=== [D] 로직 무변경 게이트: 0 이어야 PASS (diff 에 로직 라인이 한 줄도 없어야 함) ===" && printf 'logic_lines_in_diff='; git diff -- "$F" | grep -E "^[+-]" | grep -v "^[+-][+-]" | grep -cE "DilationCircle|GenCircle|GenRegionLine|Difference|innerR|PaintRegionColor|DrawLineAsRegion|DrawPointsAsRegion|DrawCrossAsRegion|Dispose|foreach|if \(" || true; echo "=== [E] diff 규모: '5  3' 기대 (신규주석2 + 값3줄 교체) ===" && git diff --numstat -- "$F" && echo "=== [F] 다른 렌더링 경로 무변경: 아래 2줄 모두 출력 없어야 PASS ===" && git status --porcelain -- WPF_Example/Halcon/Display/HalconDisplayService.cs WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs && git diff --stat -- WPF_Example/Halcon/Display/HalconDisplayService.cs WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs && echo "=== [G] Picker baseline 유지: '6  2' + sha 73a89c282724fedf25b7dcf8919b09251578d789 기대 ===" && git diff --numstat -- WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs 2>/dev/null && git diff -- WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs 2>/dev/null | git hash-object --stdin && echo "=== [H] 변경 파일 목록: OverlayCaptureRenderer.cs + Picker(기존) 2개만 기대 ===" && git status --porcelain && echo "=== [I] Debug/x64 컴파일 (스크래치 OutDir — 실제 bin/obj 미접촉, 잠김 무관) ===" && "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" //p:Configuration=Debug //p:Platform=x64 //p:OutputPath="$TEMP/gsd-fq4-scratch/bin/" //p:BaseIntermediateOutputPath="$TEMP/gsd-fq4-scratch/obj/" //v:minimal //nologo 2>&1 | grep -iE "error CS|warning CS|Build succeeded" | head -20</automated>
  </verify>
  <done>
- [A] 구값 `2.0` / `3.1` / `6.0` 잔존 **0건**
- [B] 신값 `3.0` / `4.65` / `9.0` 각 **1건**
- [C] 길이 상수 3개 각 1건 유지 + `private const double` 총 **6건**(개수 불변 = 상수 추가/삭제 없음)
- [D] diff 안에 로직 라인 **0건** — 값 변경만 있었다는 증명
- [E] `git diff --numstat` = `5  3`
- [F] `HalconDisplayService.cs` / `MainResultViewerControl.xaml.cs` diff **0줄**
- [G] Picker baseline `6  2` + sha `73a89c28...` 동일
- [H] 변경 파일 = `OverlayCaptureRenderer.cs`(이번 작업) + `PickerCenterCalibrationService.cs`(작업 전부터 미커밋) **2개뿐**
- [I] 신규 `error CS` 0건, 신규 `warning CS` 0건
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 검사 이미지 → 저장된 캡쳐 PNG (사람이 보는 증적) | 오버레이가 원본 픽셀 위에 번인된다. 오버레이가 덮는 면적이 곧 사람이 볼 수 없게 되는 원본 면적이다 |

외부 입력 경계는 없다 — 세 값 전부 `private const double` 컴파일 타임 상수이며, 사용자 입력·네트워크·파일에서 오지 않는다.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-fq4-01 | Information disclosure (증적 가림) | `DrawOverlayRegions` — 굵어진 오버레이가 원본 픽셀을 더 넓게 덮음 | accept | 사용자가 "전체 다 굵게"를 명시적으로 확정했고, **가시성 부족이 현재 실제 문제**다. 측정값은 원본 이미지에서 계산되며 오버레이는 표시 전용이라 판정 정확도에는 영향이 없다. 원본은 `RawImageSaveService` 로 별도 보존된다 |
| T-fq4-02 | Denial of service (렌더 실패) | `DrawCircleRingAsRegion` — 반지름 ≤ 10px 원에서 링이 원반화 | accept | 기존 `if (innerR < 1.0) innerR = 1.0;` clamp 가 이미 음수 반지름을 막고 있어 예외는 발생하지 않는다. 시각적 변화일 뿐이며 사용자 요청의 의도된 방향이다. 실기 확인 후 필요 시 별건 처리 |
| T-fq4-03 | Denial of service (성능) | `DilationCircle` 반경 증가 | accept | dilation 반경 2→3 은 커널 크기만 소폭 증가하며, 캡쳐 저장은 이미 비동기 큐(`CaptureImageSaveService`, 큐 상한 50)에서 처리된다. 검사 택트 경로가 아니다 |
| T-fq4-SC | Tampering (공급망) | 패키지 | n/a | 신규 패키지 0개. `csproj` 무수정, 신규 파일 0개 |
</threat_model>

<verification>
1. **정적 게이트** — Task 1 의 [A]~[H] grep/git 매트릭스. 핵심은 **[D] 로직 라인 0건** 과 **[C] 상수 총 6건 불변** 으로,
   "값만 바뀌었다"를 diff 수준에서 기계적으로 증명한다.
2. **컴파일** — [I] Debug/x64 스크래치 OutDir 빌드, 신규 error/warning CS 0건.
   (산출물 잠김 시에도 항상 실행 가능한 경로이므로 이 플랜의 완료 판정은 프로세스 종료 없이 성립한다)
3. **범위 밖 — 사용자 실기 UAT (이 플랜의 완료 조건 아님):**
   사용자가 직접 검사를 돌려 캡쳐 PNG 를 열고 육안 확인한다.
   - 측정선(녹/적)·거리선(cyan)·X 마커·Datum 축선/십자가 확실히 굵어졌는지
   - Datum 검출 원 링이 두꺼워졌는지, 작은 원이 원반처럼 보이지 않는지
   - 너무 굵어서 정작 봐야 할 에지가 가려지지는 않는지 (가려지면 배율 재조정을 별건으로 요청)
</verification>

<success_criteria>
- `OverlayCaptureRenderer.cs` 의 두께 상수 3종이 정확히 `3.0` / `4.65` / `9.0`
- 세 값의 비율이 변경 전과 동일 (전부 정확히 ×1.5)
- diff 에 로직 라인 0건, 삭제된 기능 0건, 상수 개수 변동 0건 (6개 그대로)
- 변경 파일 정확히 1개 (`OverlayCaptureRenderer.cs`) — `PickerCenterCalibrationService.cs` 는 작업 전부터 미커밋이므로 baseline 그대로 유지하고 커밋에서 제외
- Debug/x64 컴파일 신규 error CS 0 / 신규 warning CS 0 (잠김 시 스크래치 OutDir 검증, **프로세스 강제 종료 없음**)
- 실기 UAT 는 사용자 몫 — 이 플랜의 완료 조건에 포함하지 않는다
</success_criteria>

<output>
완료 시 `.planning/quick/260812-fq4-overlay-thickness-bold/260812-fq4-SUMMARY.md` 작성.

SUMMARY 에 반드시 포함할 것:
- 변경 전/후 값 3쌍과 배율(전부 ×1.5)
- 다른 파일을 건드리지 않은 근거 요약 — 세 상수는 이 파일 전용이고,
  `HalconDisplayService.cs` 의 `SetLineWidth` 계열은 **실시간 화면 렌더링용 독립 체계**라 이번 "캡쳐 한정" 요청 범위 밖이라는 판단
- 빌드 결과 (정식 경로 성공 여부 / 잠김으로 스크래치 검증했다면 그 사실 그대로)
- **사용자 UAT 안내**: 검사 1회 실행 → 캡쳐 PNG 육안 확인 → 굵기가 과하거나 부족하면 배율만 재조정하면 된다는 점
  (다음 조정 후보값을 함께 제시: ×1.25 = 2.5/3.875/7.5, ×2.0 = 4.0/6.2/12.0)
- 알려진 부작용: `DatumRingThickness = 9.0` 에서 반지름 ≤ 10px 인 작은 Datum 원은 링이 아닌 원반으로 보인다
  (기존 `innerR` clamp 로 예외는 없음). 실기에서 거슬리면 별건 처리 대상
</output>
