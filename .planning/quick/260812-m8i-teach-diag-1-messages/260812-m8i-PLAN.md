---
phase: quick-260812-m8i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Halcon/Algorithms/TeachDiagnostics.cs
  - WPF_Example/DatumMeasurement.csproj
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/Custom/UI/TrayVisionView.xaml.cs
  - WPF_Example/Custom/UI/BottomVisionView.xaml.cs
autonomous: true
requirements: [TEACH-UX-01]

must_haves:
  truths:
    - "[하드제약] 판정 로직이 diff 에 단 한 줄도 없다 — 변경된 3개 소스파일의 `git diff` 추가/삭제 줄 중 `HOperatorSet.` / `TryTeach(` / `TryCreateModel(` / `TryFindPose(` / `TryFindRefPose(` / `HasTemplate(` / `Matcher.Run` / `RefMatch` / `return` / `if (` / `else` 를 포함하는 줄이 0 건이다. 바꾸는 것은 오직 표시 문자열·Foreground 색·CustomMessageBox 옵션 인자뿐이다."
    - "[하드제약] 티칭 판정 엔진 3개 파일이 1바이트도 변하지 않는다 — `PatternMatchService.cs`, `AlignShapeMatchService.cs`, `PickerCenterCalibrationService.cs` 가 `git status --porcelain` 에 등장하지 않는다. (특히 Picker 는 사용자 실험이 stash@{0} 로 보관돼 있어 이번 작업 중 절대 열지 않는다.)"
    - "[하드제약] 제어흐름 앵커(`bool ok = svc.TryCreateModel(` 등 7종)가 삭제되지 않고 그대로 존재한다 — 단, `if (ok) {`/`if (bOk) {`/`if (svc.TryFindPose(` 패턴은 같은 파일의 무관한 다른 메서드(LiveButton_Click 등)에도 동일 텍스트로 여러 번 나타나므로 전체 파일 카운트로는 '정확히 1건'을 주장하지 않는다 — 실질적 회귀 보증은 diff 기반 하드제약 1/4(판정 심볼이 추가/삭제 줄에 없음 + 추가줄이 화이트리스트 밖으로 안 샘)이 담당하며, 이 앵커 체크는 참고용 생존 확인일 뿐이다."
    - "신규 `WPF_Example/Halcon/Algorithms/TeachDiagnostics.cs` 가 `ETeachGrade` + `ClassifyScore` + `ToKoreanMessage` + `ToStatusLine` + `GradeBrush` 5종을 노출하고, csproj `<Compile Include=\"Halcon\\Algorithms\\TeachDiagnostics.cs\" />` 로 등록돼 실제 컴파일에 포함된다(빌드 성공이 증거)."
    - "`ToKoreanMessage` 는 키워드 Contains 가 아니라 **코드가 직접 만든 explicit 문자열/고정 접두부**만 매칭한다 — 사전 항목 전부가 `PatternMatchService.cs` / `AlignShapeMatchService.cs` 의 실제 `error = \"...\"` 원문과 1:1 대조된 것이다."
    - "사전에 없는 원문은 숨기지 않는다 — `원인을 정확히 알 수 없는 오류입니다. 이 화면을 캡처해서 담당자에게 전달하세요. (원본: {rawError})` 형태로 **원문을 그대로 병기**한다."
    - "Datum 티칭 실패 모달에 raw HALCON 문구가 그대로 뜨지 않는다 — `CustomMessageBox.Show(\"모델 생성 실패\", error)` / `CustomMessageBox.Show(\"ref pose 기록 실패\", refError)` 원형이 0 건이 되고, 둘 다 `TeachDiagnostics.ToKoreanMessage(...)` 를 거친다."
    - "Datum 티칭 실패 모달이 7초 뒤 저절로 닫히지 않는다 — `InvokeCreatePatternModel` 안의 실패 알림 7곳이 전부 `MessageBoxImage.Error, true, false` (isAutoClosing=false) 로 호출된다. EthernetVisionHandler.cs L131 선례와 동일한 위치인자 스타일이다."
    - "Align 상태 라벨의 색이 stale 되지 않는다 — `lbl_teachStatus.Text` 에 대입하는 **모든** 지점(Tray 6곳 / Bottom 9곳)이 같은 줄 근처에서 `lbl_teachStatus.Foreground` 를 함께 명시한다. 성공=초록(#16A34A), 주의=주황(#D97706), 실패=빨강(#DC2626)."
    - "Align 티칭 실패 라벨이 한국어로 뜬다 — `\"티칭 실패: \" + error` / `\"티칭 예외: \" + ex.Message` 원형이 두 뷰에서 0 건이 되고 `ToKoreanMessage` 를 거친다."
    - "코딩 규칙 준수 — 신규/변경 코드에 삼항연산자 `?:` 가 0 건이고, 신규 파일은 Allman 브레이스(PatternMatchService.cs 스타일)이며 C# 7.2 문법만 쓴다."
    - "Debug/x64 컴파일이 신규 `error CS` 0 / 신규 `warning CS` 0 으로 통과한다. 산출물이 실행 중 앱에 잠겨 있으면 프로세스를 죽이지 않고 스크래치 OutDir 컴파일로 검증한다."
  artifacts:
    - path: "WPF_Example/Halcon/Algorithms/TeachDiagnostics.cs"
      provides: "ETeachGrade 열거형 + 등급 산정(ClassifyScore, Quick #2 배선용 인프라) + 한국어 문구 사전(ToKoreanMessage) + 상태문구(ToStatusLine) + 등급색(GradeBrush)"
      contains: "public static string ToKoreanMessage"
      min_lines: 120
    - path: "WPF_Example/DatumMeasurement.csproj"
      provides: "클래식 csproj 수동 Compile 등록 — SDK-style 이 아니라 빠뜨리면 컴파일 대상에서 조용히 제외된다"
      contains: "Halcon\\Algorithms\\TeachDiagnostics.cs"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "Datum 티칭 실패 모달 7곳 — 문구 한국어화(ToKoreanMessage 2곳 + 폴백 경고 2곳) + isAutoClosing=false. InvokeCreatePatternModel 메서드 밖은 무변경"
      contains: "MessageBoxImage.Error, true, false"
    - path: "WPF_Example/Custom/UI/TrayVisionView.xaml.cs"
      provides: "lbl_teachStatus 6곳 — ToStatusLine 문구 + GradeBrush 색 명시(성공 시 초록 복귀)"
      contains: "TeachDiag.GradeBrush"
    - path: "WPF_Example/Custom/UI/BottomVisionView.xaml.cs"
      provides: "lbl_teachStatus 9곳 — ToStatusLine 문구 + GradeBrush 색 명시(성공 시 초록 복귀)"
      contains: "TeachDiag.GradeBrush"
  key_links:
    - from: "MainView.xaml.cs InvokeCreatePatternModel — 모델 생성 실패 모달"
      to: "TeachDiagnostics.ToKoreanMessage(error) + isAutoClosing=false"
      via: "표시 인자만 교체. 감싸는 if/else 와 return 은 무변경"
      pattern: "CustomMessageBox\\.Show\\(\"모델 생성 실패\", TeachDiagnostics\\.ToKoreanMessage\\(error\\), MessageBoxImage\\.Error, true, false\\)"
    - from: "MainView.xaml.cs InvokeCreatePatternModel — ref pose 기록 실패 모달"
      to: "제목 한국어화 + ToKoreanMessage(refError) + isAutoClosing=false"
      via: "표시 인자만 교체. 이 else 블록의 유일한 실행문"
      pattern: "TeachDiagnostics\\.ToKoreanMessage\\(refError\\)"
    - from: "TrayVisionView.xaml.cs TeachButton_Click 실패 분기"
      to: "TeachDiag.ToStatusLine(ETeachGrade.Bad, ...) + GradeBrush(Bad)"
      via: "`lbl_teachStatus.Text = \"티칭 실패: \" + error;` 한 줄 → 문구줄 + 색줄 2줄. else 블록 구조 무변경"
      pattern: "티칭 실패: \" \\+ TeachDiag\\.ToKoreanMessage\\(error\\)"
    - from: "BottomVisionView.xaml.cs TeachButton_Click 실패 분기"
      to: "TeachDiag.ToStatusLine(ETeachGrade.Bad, ...) + GradeBrush(Bad)"
      via: "Tray 와 동일 패턴. `_slotRois[...] = ...` 성공 분기 로직은 무변경"
      pattern: "티칭 실패: \" \\+ TeachDiag\\.ToKoreanMessage\\(error\\)"
    - from: "성공 경로 라벨 (Tray 311/580, Bottom 488/946/956)"
      to: "GradeBrush(ETeachGrade.Good) = #16A34A"
      via: "실패 후 성공 시 빨강이 남는 stale 색 방지 — Text 대입마다 Foreground 동반"
      pattern: "TeachDiag\\.GradeBrush\\(ETeachGrade\\.Good\\)"
    - from: "TeachDiagnostics 사전"
      to: "PatternMatchService.cs / AlignShapeMatchService.cs 의 실제 error 문자열"
      via: "원문 대조 완료 — 추측 항목 0. 서비스 파일 자체는 읽기만 하고 무변경"
      pattern: "no match found \\(empty result\\)"
    - from: "PickerCenterCalibrationService.cs (사용자 실험 stash@{0})"
      to: "diff 무등장"
      via: "이번 Quick 범위 밖 — 읽지도 고치지도 않는다"
      pattern: "PickerCenterCalibrationService"
---

<objective>
티칭이 실패했을 때 **초보 운영자가 원인을 읽을 수 있게** 만드는 3단계 작업의 **1/3 단계**다.

지금은 티칭이 실패하면 HALCON/서비스 계층의 영문 원문(`templateImage is null`,
`TryCreateModel[1]: HALCON error #5300 ...`)이 그대로 모달과 상태 라벨에 뜨고,
그 모달마저 **7초 뒤 저절로 닫혀서** 사용자가 원인을 읽기도 전에 사라진다.

이번 단계는 **순수 표시 레이어만** 추가한다:
공용 문구 헬퍼 신설 → 실패 문구 한국어화 → 실패 모달 자동닫힘 제거 → 상태 라벨 색 인프라.

> 🔒 **하드 제약 (사용자 명시, 재차 강조):**
> 기존 검사/티칭 시퀀스의 **판정 로직·제어흐름·HALCON 호출 시퀀스는 절대 안 건드린다.**
> 바꾸는 것은 **화면에 뿌리는 문자열 / 라벨 색 / 메시지박스 옵션 인자** 이 셋뿐이다.
> pass/fail 반환값, if/else 분기, HALCON 호출 순서가 diff 에 **한 줄도** 나타나면 안 된다.

**Output:**
- 신규 `WPF_Example/Halcon/Algorithms/TeachDiagnostics.cs` (+ csproj 수동 등록)
- Datum 실패 모달 7곳 — 한국어화 2곳 + 폴백 경고 한국어화 2곳 + 자동닫힘 제거 7곳
- Align 상태 라벨 15곳 — 문구(ToStatusLine) + 색(GradeBrush) 명시
- 이번 범위 밖: 점수 등급 실제 배선(Quick #2), Calib TCP 자동경로 노출(Quick #3)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

**코딩 규칙 (이 프로젝트 상시 규칙 — 위반 시 리뷰 반려):**
- 삼항연산자 `?:` **금지** → 반드시 `if / else`. 신규 파일에도 단 하나도 넣지 않는다.
- C# 7.2 (`nullable reference types`, `switch expression`, `record` 등 8.0+ 문법 금지)
- **신규 `TeachDiagnostics.cs` 는 Allman 브레이스** (`PatternMatchService.cs` 와 통일 — 같은 폴더)
- **기존 3개 편집 파일은 각자의 기존 스타일 유지** — `MainView.xaml.cs` / `TrayVisionView.xaml.cs` /
  `BottomVisionView.xaml.cs` 는 전부 K&R(`{` 같은 줄). 주변 줄을 그대로 따라간다.
- 새 주석은 `quick-260812:` 접두 + **짧게**, 비자명한 "왜"만.
  `//YYMMDD hbk` 날짜 주석 규칙은 2026-06-11 부로 폐기 — 새로 달지 말 것.
- 파일 인코딩: **UTF-8, BOM 없음** (이 리포의 모든 .cs 가 BOM 없는 UTF-8 이고 Roslyn 이 UTF-8 로 폴백해
  한글이 정상 컴파일된다. BOM 을 새로 붙이지 말 것.)

***

## 🚫 절대 건드리면 안 되는 것

1. **`WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs`**
   - 사용자의 별건 실험(±5° 탐색범위)이 `stash@{0}` 에 보관돼 있고 **working tree 는 clean** 이다.
   - 이번 Quick 범위 완전히 밖. **읽지도 고치지도 말 것.** Quick #3 에서 다룬다.
2. **`WPF_Example/Halcon/Algorithms/PatternMatchService.cs`** — 사전 채우려고 **읽기만** 한다. 무변경.
3. **`WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs`** — 동일. 읽기만. 무변경.
4. **`MainView.xaml.cs` 의 `InvokeCreatePatternModel` 메서드 밖** — 이 파일은 3,300줄 이상이다.
   L3844~L3974 이외 지점은 열어보더라도 **편집 금지**.
5. **`ShowConfirmation` 호출들** (MainView L3877 패턴2 미설정 확인, L3945 Recipe Save 확인) —
   `MessageBoxModel` 의 confirmation 생성자는 이미 `autoClosing=false` 기본값이라 자동닫힘이 없다. **무변경.**
6. **`MainView.xaml.cs` L3964 `CustomMessageBox.Show("Recipe Save 권장", ...)`** — 실패 알림이 아니라
   성공 후 권고다. 자동닫힘이 오히려 적절 → **무변경**.

***

## 조사 완료 사실 (실제 파일 Read 로 검증됨, 2026-08-12)

### CustomMessageBox API — 브리핑 검증 결과 "기본값 true, 7초" 사실 확인

```csharp
// WPF_Example/UI/Dialog/CustomMessageBox.cs L24
public static bool Show(string title, string message,
                        MessageBoxImage imageType = MessageBoxImage.Information,
                        bool isModal = true,
                        bool isAutoClosing = true,                      // ← 기본 true
                        int autoClosingTime = MessageBoxModel.TIME_AUTOCLOSING)   // ← TIME_AUTOCLOSING = 7 (초)
```

`isAutoClosing` 은 **5번째 위치인자**다. 위치인자로 끄려면 앞의 `imageType`, `isModal` 도 명시해야 한다.

**선례 (그대로 따른다) — `WPF_Example/Custom/EthernetVision/EthernetVisionHandler.cs` L131:**
```csharp
CustomMessageBox.Show("카메라 연결 실패", message, System.Windows.MessageBoxImage.Error, true, false);
//                                                  ↑ imageType        ↑ isModal ↑ isAutoClosing=false
```
`MainView.xaml.cs` 는 `using System.Windows;` 가 이미 있으므로 `MessageBoxImage.Error` 로 짧게 쓴다.
(부수효과: 아이콘이 information.png → error.png 로 바뀐다. 실패 알림이므로 의도된 개선이다.
 두 리소스 모두 `WPF_Example/Resource/` 에 실재한다.)

### `MainView.xaml.cs` 는 `using ReringProject.Halcon.Algorithms;` 를 **이미** 가지고 있다 (L12)
→ `TeachDiagnostics.` 를 수식 없이 바로 쓴다. **새 using 추가 불필요.**

### `TrayVisionView.xaml.cs` / `BottomVisionView.xaml.cs` 에는 그 using 이 **없다**
→ `using ReringProject.Halcon.Algorithms;` 를 통째로 추가하면 `CalibrationResult` 등이
   `ReringProject.Halcon.Models` 와 이름 충돌할 위험이 있다. **using 별칭 2줄만** 추가한다(충돌 불가능).

### `lbl_teachStatus` 는 두 뷰 모두 `TextBlock` — `Foreground` 속성 존재 확인
```xml
<!-- TrayVisionView.xaml L136 / BottomVisionView.xaml L150 -->
<TextBlock x:Name="lbl_teachStatus" Margin="0,6,0,0" FontSize="12"
           Foreground="#475569" Text="티칭 없음" TextWrapping="Wrap"/>
```
기본색이 회색(#475569)이므로, **실패로 빨강을 칠하면 이후 성공/대기 상태에서 빨강이 그대로 남는다.**
그래서 `lbl_teachStatus.Text` 대입 **전부**에 색을 짝지어 명시한다(브리핑이 말한 "stale red 방지"의 완전한 이행).

### ⚠ 브리핑 줄번호 정정 (실제 Read 결과)

브리핑은 Bottom 실패 지점을 `494/768/852/895`, 성공을 `488/765/835/888` 로 적었으나
**`lbl_teachStatus` 대입은 파일 전체에 9곳뿐**이고 768/852/895 에는 존재하지 않는다
(그 부근은 `lbl_status`(별개 라벨)와 동축/캘 관련 코드다). 실측 목록은 아래 `<interfaces>` 표가 정본이다.

***

<interfaces>
<!-- 실행자가 코드베이스를 재탐색하지 않아도 되도록, 신규 파일 전문과 편집 대상 원문을 그대로 옮겨둔다. -->
<!-- 라인번호는 편집 전 기준이며 편집 중 밀린다 — Edit 은 반드시 문자열 앵커로 지정할 것. -->

## A. 신규 파일 전문 — `WPF_Example/Halcon/Algorithms/TeachDiagnostics.cs`

아래를 **그대로** 작성한다(Allman, 삼항 0, C# 7.2, UTF-8 BOM 없음).

```csharp
//quick-260812: 티칭 실패/품질 진단 표시 헬퍼. 순수 표시(문자열/색) 레이어 —
// 이 파일에는 판정도 HALCON 호출도 없다. 어떤 메서드도 pass/fail 을 만들거나 바꾸지 않는다.
using System;
using System.Collections.Generic;
using System.Windows.Media;

namespace ReringProject.Halcon.Algorithms
{
    /// <summary>
    /// 티칭 결과 등급. Good = 기준보다 여유 있음, Weak = 간신히 통과(주의), Bad = 기준 미달/실패.
    /// </summary>
    public enum ETeachGrade
    {
        Good,
        Weak,
        Bad
    }

    /// <summary>
    /// 티칭 실패 원문(서비스 계층 영문 메시지 / HALCON 예외)을 운영자용 한국어 문구로 바꾸고,
    /// 등급별 상태 문구와 색을 제공하는 표시 전용 헬퍼.
    /// 사전은 PatternMatchService / AlignShapeMatchService 가 코드에서 직접 만든 문자열만 담는다
    /// (키워드 Contains 추측 매칭 금지 — 오역이 오진단으로 이어지기 때문).
    /// </summary>
    public static class TeachDiagnostics
    {
        // 등급 색 (초록 #16A34A / 주황 #D97706 / 빨강 #DC2626)
        private static readonly Brush BRUSH_GOOD = MakeFrozenBrush(0x16, 0xA3, 0x4A);
        private static readonly Brush BRUSH_WEAK = MakeFrozenBrush(0xD9, 0x77, 0x06);
        private static readonly Brush BRUSH_BAD  = MakeFrozenBrush(0xDC, 0x26, 0x26);

        // 점수 여유 임계값 — minScore 보다 이만큼 이상 남으면 Good, 남긴 했지만 이보다 적으면 Weak.
        // 보수적 초기값, 현장 데이터로 튜닝 예정.
        private const double GOOD_MARGIN = 0.15;

        // 중첩 접두부 해석 최대 깊이 (비정상 입력에서 무한 재귀 방지)
        private const int MAX_UNWRAP_DEPTH = 3;

        private const string MSG_EMPTY   = "원인이 기록되지 않은 오류입니다. 이 화면을 캡처해서 담당자에게 전달하세요.";
        private const string MSG_UNKNOWN = "원인을 정확히 알 수 없는 오류입니다. 이 화면을 캡처해서 담당자에게 전달하세요.";

        // ── 사전 1: 코드가 만든 "고정 전문" 그대로 매칭 (Ordinal 완전일치) ─────────────
        private static readonly Dictionary<string, string> EXACT_MESSAGES =
            new Dictionary<string, string>(StringComparer.Ordinal)
        {
            // PatternMatchService.cs
            { "templateImage is null",
              "티칭에 쓸 이미지가 없습니다. 먼저 Grab 또는 이미지 불러오기를 하세요." },
            { "runtimeImage is null",
              "검사할 이미지가 없습니다. 먼저 Grab 또는 이미지 불러오기를 하세요." },
            { "modelPath is null or empty",
              "패턴 모델을 저장할 경로를 만들지 못했습니다. 레시피가 선택돼 있는지, Shot 이름이 비어있지 않은지 확인하세요." },
            { "no match found (empty result)",
              "패턴을 한 곳도 찾지 못했습니다. 조명과 초점이 티칭할 때와 같은지, 부품이 검색 범위 안에 들어와 있는지 확인하세요." },

            // AlignShapeMatchService.cs
            { "img is null",
              "이미지가 없습니다. 먼저 Grab 을 하세요." },
            { "mode is None",
              "비전 모드(Tray / Bottom)가 정해지지 않았습니다. 탭을 다시 선택하세요." },
            { "RecipeSavePath 미설정",
              "레시피 저장 폴더가 설정돼 있지 않습니다. 설정 창에서 RecipeSavePath 를 먼저 지정하세요." },
            { "jsonPath 산출 실패",
              "기준값(JSON) 저장 경로를 만들지 못했습니다. 레시피 저장 폴더와 레시피 이름을 확인하세요." },
            { "angle_lx 산출 실패 (두 중심 동일 위치 또는 HALCON 오류)",
              "두 패턴의 기울기를 계산하지 못했습니다. 패턴 1 과 패턴 2 가 서로 충분히 떨어진 위치(가급적 반대 대각)에 오도록 다시 그리세요." },
            { "두 패턴 contour 변환 모두 실패",
              "찾은 패턴의 외곽선을 화면에 그리지 못했습니다. 티칭 결과 자체에는 영향이 없습니다." },
        };

        // ── 사전 2: "고정 접두부 + 안쪽 원인" 형태 → 접두부 해석 후 나머지를 재귀 해석 ──
        private static readonly List<KeyValuePair<string, string>> NESTED_PREFIXES =
            new List<KeyValuePair<string, string>>
        {
            new KeyValuePair<string, string>("TryCreateModel[1]: ",           "패턴 1 모델을 만들지 못했습니다"),
            new KeyValuePair<string, string>("TryCreateModel[2]: ",           "패턴 2 모델을 만들지 못했습니다"),
            new KeyValuePair<string, string>("TryFindRefPose[1]: ",           "패턴 1 의 기준 위치를 기록하지 못했습니다"),
            new KeyValuePair<string, string>("TryFindRefPose[2]: ",           "패턴 2 의 기준 위치를 기록하지 못했습니다"),
            new KeyValuePair<string, string>("TrySaveRefPose: ",              "기준값 파일을 저장하지 못했습니다"),
            new KeyValuePair<string, string>("TrySaveCoax: ",                 "동축 조명 설정을 저장하지 못했습니다"),
            new KeyValuePair<string, string>("TryTeach exception: ",          "티칭 도중 예기치 못한 오류가 났습니다"),
            new KeyValuePair<string, string>("TryBuildDetectedContourXld: ",  "찾은 패턴의 외곽선을 만들지 못했습니다"),
            new KeyValuePair<string, string>("TryBuildMovedContour: ",        "패턴 외곽선을 검출 위치로 옮기지 못했습니다"),
        };

        // ── 사전 3: "고정 접두부 + 수치" 형태 → 수치는 해석하지 않고 괄호로 병기 ────────
        private static readonly List<KeyValuePair<string, string>> VALUE_PREFIXES =
            new List<KeyValuePair<string, string>>
        {
            new KeyValuePair<string, string>("NCC ref find: no match above minScore=",
                "방금 만든 패턴을 티칭 이미지에서 다시 찾지 못했습니다. 패턴 ROI 를 무늬가 뚜렷한 곳으로 옮기거나 최소 점수를 낮추세요."),
            new KeyValuePair<string, string>("Shape ref find: no match above minScore=",
                "방금 만든 패턴을 티칭 이미지에서 다시 찾지 못했습니다. 패턴 ROI 를 무늬가 뚜렷한 곳으로 옮기거나 최소 점수를 낮추세요."),
            new KeyValuePair<string, string>("match score ",
                "패턴은 찾았지만 닮은 정도가 기준 점수에 못 미칩니다. 조명과 초점이 티칭할 때와 같은지 확인하거나 최소 점수를 낮추세요."),
        };

        /// <summary>
        /// 매칭 점수가 최소 기준보다 얼마나 여유 있는지로 등급을 매긴다.
        /// 이번 Quick 에서는 호출부가 없다 — Quick #2(점수 등급 실제 배선)가 그대로 소비할 인프라다.
        /// 판정에 관여하지 않는다: 여기서 Bad 가 나와도 검사 pass/fail 은 전혀 바뀌지 않는다.
        /// </summary>
        public static ETeachGrade ClassifyScore(double score, double minScore)
        {
            if (double.IsNaN(score) || double.IsNaN(minScore))
            {
                return ETeachGrade.Bad;
            }
            double margin = score - minScore;
            if (margin < 0.0)
            {
                return ETeachGrade.Bad;
            }
            if (margin < GOOD_MARGIN)
            {
                return ETeachGrade.Weak;
            }
            return ETeachGrade.Good;
        }

        /// <summary>
        /// 티칭 실패 원문을 운영자용 한국어 문구로 바꾼다.
        /// 사전에 없으면 숨기지 않고 "(원본: ...)" 로 원문을 병기한다.
        /// </summary>
        public static string ToKoreanMessage(string rawError)
        {
            return Translate(rawError, 0);
        }

        private static string Translate(string rawError, int depth)
        {
            if (string.IsNullOrEmpty(rawError))
            {
                return MSG_EMPTY;
            }

            string trimmed = rawError.Trim();

            string exact;
            if (EXACT_MESSAGES.TryGetValue(trimmed, out exact))
            {
                return exact;
            }

            if (depth < MAX_UNWRAP_DEPTH)
            {
                foreach (KeyValuePair<string, string> entry in NESTED_PREFIXES)
                {
                    if (trimmed.StartsWith(entry.Key, StringComparison.Ordinal))
                    {
                        string inner = trimmed.Substring(entry.Key.Length);
                        return entry.Value + " — " + Translate(inner, depth + 1);
                    }
                }
            }

            foreach (KeyValuePair<string, string> entry in VALUE_PREFIXES)
            {
                if (trimmed.StartsWith(entry.Key, StringComparison.Ordinal))
                {
                    string numbers = trimmed.Substring(entry.Key.Length);
                    return entry.Value + " (수치: " + numbers + ")";
                }
            }

            return MSG_UNKNOWN + " (원본: " + trimmed + ")";
        }

        /// <summary>
        /// 등급 기호를 붙인 한 줄 상태 문구. ● = 양호, ▲ = 주의, ✕ = 실패.
        /// </summary>
        public static string ToStatusLine(ETeachGrade grade, string message)
        {
            string mark;
            if (grade == ETeachGrade.Good)
            {
                mark = "●";
            }
            else if (grade == ETeachGrade.Weak)
            {
                mark = "▲";
            }
            else
            {
                mark = "✕";
            }

            if (string.IsNullOrEmpty(message))
            {
                return mark;
            }
            return mark + " " + message;
        }

        /// <summary>
        /// 등급별 글자색. 실패 후 성공 시 색이 남지 않도록 호출부는 Text 대입마다 이 값을 함께 지정한다.
        /// </summary>
        public static Brush GradeBrush(ETeachGrade grade)
        {
            if (grade == ETeachGrade.Good)
            {
                return BRUSH_GOOD;
            }
            if (grade == ETeachGrade.Weak)
            {
                return BRUSH_WEAK;
            }
            return BRUSH_BAD;
        }

        // Freeze: UI 스레드 외에서 만들어져도 안전하게 공유되도록 고정한다.
        private static Brush MakeFrozenBrush(byte r, byte g, byte b)
        {
            SolidColorBrush brush = new SolidColorBrush(Color.FromRgb(r, g, b));
            brush.Freeze();
            return brush;
        }
    }
}
```

## B. csproj 등록 — `WPF_Example/DatumMeasurement.csproj`

L575~581 이 `Halcon\Algorithms\*` 블록이다. **L581 바로 다음 줄**에 한 줄 삽입:

```xml
    <Compile Include="Halcon\Algorithms\RoiLineIntersectionAlgorithm.cs" />
    <Compile Include="Halcon\Algorithms\TeachDiagnostics.cs" />     <!-- ← 이 줄 삽입 -->
    <Compile Include="Halcon\Display\HalconDisplayService.cs" />
```
> 백슬래시(`\`) 경로다 — 기존 항목과 동일 표기. 슬래시로 쓰지 말 것.

## C. `WPF_Example/UI/ContentItem/MainView.xaml.cs` — 편집 7곳 (전부 `InvokeCreatePatternModel` 안)

| # | 편집 전 (앵커) | 편집 후 |
|---|---|---|
| C-1 | `CustomMessageBox.Show("모델 생성 실패", "Datum 티칭 존을 먼저 선택하세요.");` | 끝 인자에 `, MessageBoxImage.Error, true, false` 추가 |
| C-2 | `CustomMessageBox.Show("모델 생성 실패", "이미지가 없습니다. 먼저 Grab 또는 Load Image 를 수행하세요.");` | 동일 |
| C-3 | `CustomMessageBox.Show("모델 생성 실패", "패턴 ROI(Rect) 를 먼저 그리세요. ([패턴 ROI] 버튼)");` | 동일 |
| C-4 | `CustomMessageBox.Show("모델 생성 실패", "모델 경로 도출 실패 (레시피/Shot 확인).");` | 동일 |
| C-5 | `alignMsg = "\n[경고] 패턴 2 모델 생성됨, ref pose 기록 실패 → 단일 패턴 폴백: " + (refErr2 ?? "");` | `... 폴백: " + TeachDiagnostics.ToKoreanMessage(refErr2);` |
| C-6 | `alignMsg = "\n[경고] 패턴 2 모델 생성 실패 → 단일 패턴 폴백: " + (err2 ?? "");` | `... 폴백: " + TeachDiagnostics.ToKoreanMessage(err2);` |
| C-7 | `CustomMessageBox.Show("저장 실패", "Recipe Save 실패 — 티칭 상태는 유지됩니다. 수동 저장 필요.\n" + saveEx.Message);` | 끝 인자에 `, MessageBoxImage.Error, true, false` 추가 (**메시지 본문은 무변경** — .NET 저장 예외라 티칭 사전 대상 아님) |
| C-8 | `CustomMessageBox.Show("ref pose 기록 실패", refError);` | `CustomMessageBox.Show("기준 위치 기록 실패", TeachDiagnostics.ToKoreanMessage(refError), MessageBoxImage.Error, true, false);` |
| C-9 | `CustomMessageBox.Show("모델 생성 실패", error);` | `CustomMessageBox.Show("모델 생성 실패", TeachDiagnostics.ToKoreanMessage(error), MessageBoxImage.Error, true, false);` |

> C-8 은 제목의 `ref pose` 라는 개발자 용어도 `기준 위치` 로 바꾼다 — 표시 문자열이므로 로직 영향 0.
> C-5/C-6 에서 `(x ?? "")` 를 없애도 안전한 이유: `ToKoreanMessage(null)` 이 `MSG_EMPTY` 를 반환한다.
> **각 `Show(...)` 바로 다음 줄의 `return;` 은 손대지 않는다** (같은 줄에 합치지 말 것 — diff 에 `return` 이 뜨면 안 된다).

## D. `WPF_Example/Custom/UI/TrayVisionView.xaml.cs` — using 별칭 2줄 + `lbl_teachStatus` 6곳

**using 블록 맨 끝(`using ReringProject.UI;` 다음)에 추가:**
```csharp
using TeachDiag   = ReringProject.Halcon.Algorithms.TeachDiagnostics;   //quick-260812: 표시 전용 헬퍼(별칭 = 이름충돌 회피)
using ETeachGrade = ReringProject.Halcon.Algorithms.ETeachGrade;
```

**대입 6곳 — 각각 한 줄을 두 줄(문구 + 색)로 바꾼다. 감싸는 if/else 는 무변경.**

| # | 편집 전 (앵커) | 등급 | 편집 후 |
|---|---|---|---|
| D-1 | `lbl_teachStatus.Text = validErr;` | Weak | `lbl_teachStatus.Text = TeachDiag.ToStatusLine(ETeachGrade.Weak, validErr);`<br>`lbl_teachStatus.Foreground = TeachDiag.GradeBrush(ETeachGrade.Weak);` |
| D-2 | `lbl_teachStatus.Text = "티칭 OK (HasTemplate=" + bHas + ")";` | Good | `lbl_teachStatus.Text = TeachDiag.ToStatusLine(ETeachGrade.Good, "티칭 OK (HasTemplate=" + bHas + ")");`<br>`lbl_teachStatus.Foreground = TeachDiag.GradeBrush(ETeachGrade.Good);` |
| D-3 | `lbl_teachStatus.Text = "티칭 실패: " + error;` | Bad | `lbl_teachStatus.Text = TeachDiag.ToStatusLine(ETeachGrade.Bad, "티칭 실패: " + TeachDiag.ToKoreanMessage(error));`<br>`lbl_teachStatus.Foreground = TeachDiag.GradeBrush(ETeachGrade.Bad);` |
| D-4 | `lbl_teachStatus.Text = "티칭 예외: " + ex.Message;` | Bad | `lbl_teachStatus.Text = TeachDiag.ToStatusLine(ETeachGrade.Bad, "티칭 예외: " + TeachDiag.ToKoreanMessage(ex.Message));`<br>`lbl_teachStatus.Foreground = TeachDiag.GradeBrush(ETeachGrade.Bad);` |
| D-5 | `lbl_teachStatus.Text = "티칭 OK (HasTemplate=True)";` (RefreshStatus) | Good | 동일 패턴 |
| D-6 | `lbl_teachStatus.Text = "티칭 없음";` (RefreshStatus) | Weak | 동일 패턴 |

## E. `WPF_Example/Custom/UI/BottomVisionView.xaml.cs` — using 별칭 2줄 + `lbl_teachStatus` 9곳

using 별칭은 D 와 동일하게 추가. 대입 9곳:

| # | 편집 전 (앵커) | 등급 |
|---|---|---|
| E-1 | `lbl_teachStatus.Text = "면 슬롯을 먼저 선택하세요";` | Weak |
| E-2 | `lbl_teachStatus.Text = validErr;` | Weak |
| E-3 | `lbl_teachStatus.Text = "[" + slotLabel + "] 티칭 OK (HasTemplate=" + bHas + ")";` | Good |
| E-4 | `lbl_teachStatus.Text = "티칭 실패: " + error;` → `... + TeachDiag.ToKoreanMessage(error)` | Bad |
| E-5 | `lbl_teachStatus.Text = "티칭 예외: " + ex.Message;` → `... + TeachDiag.ToKoreanMessage(ex.Message)` | Bad |
| E-6 | `lbl_teachStatus.Text = "티칭 OK (단일 경로)";` (RefreshTeachStatus) | Good |
| E-7 | `lbl_teachStatus.Text = "티칭 없음 (슬롯 선택 필요)";` (RefreshTeachStatus) | Weak |
| E-8 | `lbl_teachStatus.Text = "[" + slotLabel + "] 티칭 OK";` (RefreshTeachStatus) | Good |
| E-9 | `lbl_teachStatus.Text = "[" + slotLabel + "] 티칭 없음";` (RefreshTeachStatus) | Weak |

변환 규칙은 D 와 동일: `Text` 는 `TeachDiag.ToStatusLine(등급, 기존문자열식)` 로 감싸고,
바로 다음 줄에 `lbl_teachStatus.Foreground = TeachDiag.GradeBrush(등급);` 를 추가.
E-3 성공 분기 다음의 `_slotRois[_selectedSlot] = new RoiDefinition[] { _roi1, _roi2 };` 는 **무변경**.
줄 끝 `//260626 hbk ...` 주석은 그대로 살려 둔다(문자열 밖 코드 주석 = 보존 대상).

## F. 빌드 (산출물 잠김 대비)

`bin/x64/Debug/DatumMeasurement.exe` 가 실행 중이면 잠긴다. **프로세스를 절대 죽이지 않는다.**
정상 빌드를 먼저 시도하고, MSB3021/3026/3027/3030 이 나면 스크래치 OutDir 컴파일로 대체한다:

```
"/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" \
  "WPF_Example/DatumMeasurement.csproj" //p:Configuration=Debug //p:Platform=x64 \
  //p:OutputPath="$TEMP/gsd-m8i-scratch/bin/" //p:BaseIntermediateOutputPath="$TEMP/gsd-m8i-scratch/obj/" \
  //v:minimal //nologo
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TeachDiagnostics.cs 신설 + csproj 수동 등록</name>
  <files>WPF_Example/Halcon/Algorithms/TeachDiagnostics.cs, WPF_Example/DatumMeasurement.csproj</files>
  <action>
**(A) 신규 파일 작성**

`<interfaces>` 섹션 **A** 의 소스를 `WPF_Example/Halcon/Algorithms/TeachDiagnostics.cs` 로 **그대로** 작성한다.
Write 도구를 쓴다(heredoc 금지). 인코딩은 **UTF-8, BOM 없음** — 이 리포의 다른 .cs 와 동일하다.

작성 전 확인(추측 금지 규칙 이행): 사전 항목이 실제 원문과 맞는지 눈으로 1회 대조한다.
- `WPF_Example/Halcon/Algorithms/PatternMatchService.cs` 의 `error = "..."` 8종
  (L147/152/270/275/306/334/414/419/506/513 — 중복 문자열 포함)
- `WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs` 의 `error = "..."` 13종
  (L195/250/283/336/340/348/353/371/381/392/403/410/428/652/675/717)
> 이 두 파일은 **읽기만** 한다. 단 한 글자도 고치지 않는다.

**(B) csproj 등록 — 클래식 csproj라 자동 포함이 없다**

`<interfaces>` 섹션 **B** 대로 `<Compile Include="Halcon\Algorithms\RoiLineIntersectionAlgorithm.cs" />`
바로 아래에 `<Compile Include="Halcon\Algorithms\TeachDiagnostics.cs" />` 한 줄을 삽입한다.
백슬래시 표기, 들여쓰기(스페이스 4칸) 모두 주변 줄과 동일하게 맞춘다.
> 빠뜨리면 파일이 조용히 컴파일 대상에서 빠져 Task 2 가 `CS0103 TeachDiagnostics 없음` 으로 터진다.

**(C) 절대 하지 말 것**
- 삼항연산자 `?:` 사용 금지 — 전부 `if / else` (위 소스는 이미 그렇게 돼 있다).
- C# 8.0+ 문법(`switch expression`, `is not`, `nullable ref`) 금지.
- `PatternMatchService.cs` / `AlignShapeMatchService.cs` / `PickerCenterCalibrationService.cs` 편집 금지.
- `ClassifyScore` 에 호출부를 만들지 말 것 — 이번 Quick 은 인프라만이고 배선은 Quick #2 다.
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && D=WPF_Example/Halcon/Algorithms/TeachDiagnostics.cs; P=WPF_Example/DatumMeasurement.csproj; echo "=== [기대 1] 파일 존재 + BOM 없음(2f2f 또는 7573 로 시작) ===" && test -f "$D" && echo FILE_OK && head -c 2 "$D" | od -An -tx1 | tr -d ' \n' && echo "" && echo "=== [기대 각 1] 공개 API 5종 ===" && grep -c 'public enum ETeachGrade' $D; grep -c 'public static ETeachGrade ClassifyScore(double score, double minScore)' $D; grep -c 'public static string ToKoreanMessage(string rawError)' $D; grep -c 'public static string ToStatusLine(ETeachGrade grade, string message)' $D; grep -c 'public static Brush GradeBrush(ETeachGrade grade)' $D; echo "=== [기대 1] namespace + 색상 3종 ===" && grep -c 'namespace ReringProject.Halcon.Algorithms' $D; grep -c '0x16, 0xA3, 0x4A' $D; grep -c '0xD9, 0x77, 0x06' $D; grep -c '0xDC, 0x26, 0x26' $D; echo "=== [기대 1] 원문 병기 폴백 ===" && grep -c '원본: ' $D && grep -c '보수적 초기값, 현장 데이터로 튜닝 예정' $D && echo "=== [기대 0] 삼항연산자 ===" && grep -c ' ? ' $D; echo "=== [기대 각 1] 사전이 실제 원문과 대조됐는지(대표 5종) ===" && grep -c '"no match found (empty result)"' $D; grep -c '"templateImage is null"' $D; grep -c '"angle_lx 산출 실패 (두 중심 동일 위치 또는 HALCON 오류)"' $D; grep -c '"TryCreateModel\[1\]: "' $D; grep -c '"match score "' $D; echo "=== [기대 1] csproj 등록 ===" && grep -c 'Halcon\\Algorithms\\TeachDiagnostics.cs' $P && echo "=== [기대 0] 서비스 3파일 무변경 ===" && git status --porcelain -- WPF_Example/Halcon/Algorithms/PatternMatchService.cs WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs | wc -l && echo "=== 컴파일 ===" && ("/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" //p:Configuration=Debug //p:Platform=x64 //v:minimal //nologo 2>&1 | grep -iE "error|warning CS|Build succeeded" | head -25 || "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" //p:Configuration=Debug //p:Platform=x64 //p:OutputPath="$TEMP/gsd-m8i-scratch/bin/" //p:BaseIntermediateOutputPath="$TEMP/gsd-m8i-scratch/obj/" //v:minimal //nologo 2>&1 | grep -iE "error CS|warning CS|Build succeeded" | head -25)</automated>
  </verify>
  <done>
- `TeachDiagnostics.cs` 존재, BOM 없는 UTF-8, `ReringProject.Halcon.Algorithms` 네임스페이스.
- 공개 API 5종(`ETeachGrade` / `ClassifyScore` / `ToKoreanMessage` / `ToStatusLine` / `GradeBrush`) 각 1건 — 시그니처가 Quick #2 가 그대로 쓸 수 있게 확정됨.
- 색 3종(#16A34A / #D97706 / #DC2626) 존재, 임계값 주석에 "보수적 초기값, 현장 데이터로 튜닝 예정" 명시.
- 사전 대표 5종이 실제 서비스 원문과 문자 그대로 일치, 미매칭 폴백에 "(원본: ...)" 병기 존재.
- 삼항연산자 0건.
- csproj 에 `Halcon\Algorithms\TeachDiagnostics.cs` 1건 등록 → 빌드에 실제 포함.
- 서비스 3파일(`PatternMatchService` / `AlignShapeMatchService` / `PickerCenterCalibrationService`) `git status` 0줄 = 무변경.
- 신규 `error CS` 0 / 신규 `warning CS` 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: 실패 문구 한국어화 + 실패 모달 자동닫힘 제거 + 상태 라벨 색 명시 (표시 레이어만)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs, WPF_Example/Custom/UI/TrayVisionView.xaml.cs, WPF_Example/Custom/UI/BottomVisionView.xaml.cs</files>
  <action>
`<interfaces>` 섹션 **C / D / E** 의 표를 앵커로 삼아 편집한다.
**바꾸는 것은 오직 (1) 화면 표시 문자열, (2) Foreground 색, (3) CustomMessageBox 의 표시 옵션 인자 셋뿐이다.**

**(A) `MainView.xaml.cs` — `InvokeCreatePatternModel` 안 9곳 (섹션 C 표)**

- C-1~C-4, C-7 : `CustomMessageBox.Show(...)` 인자 목록 **끝에** `, MessageBoxImage.Error, true, false` 추가.
  메시지 본문은 이미 한국어라 **손대지 않는다.**
- C-5, C-6 : `+ (refErr2 ?? "")` / `+ (err2 ?? "")` → `+ TeachDiagnostics.ToKoreanMessage(refErr2)` / `(err2)`.
- C-8 : 제목 `"ref pose 기록 실패"` → `"기준 위치 기록 실패"`, 본문 `refError` → `TeachDiagnostics.ToKoreanMessage(refError)`, 끝에 `, MessageBoxImage.Error, true, false`.
- C-9 : 본문 `error` → `TeachDiagnostics.ToKoreanMessage(error)`, 끝에 `, MessageBoxImage.Error, true, false`.

`MainView.xaml.cs` 는 `using ReringProject.Halcon.Algorithms;` 와 `using System.Windows;` 를 이미 갖고 있다 —
**새 using 을 추가하지 않는다.**

> ⚠ `Show(...)` 다음 줄의 `return;` 은 그대로 둔다. 줄을 합치거나 옮기면 diff 에 `return` 이 등장해
> 하드 제약 검사(judgement-symbol grep)에 걸린다.
> ⚠ 이 메서드 밖(L3844 이전 / L3974 이후)은 **한 글자도** 고치지 않는다.

**(B) `TrayVisionView.xaml.cs` — using 별칭 2줄 + `lbl_teachStatus` 6곳 (섹션 D 표)**

using 블록 맨 끝(`using ReringProject.UI;` 다음)에 별칭 2줄 추가. 그 다음 6곳을 표대로 변환한다.
각 지점은 **한 줄 → 두 줄**(문구 줄 + 색 줄)이 되며, 들여쓰기는 원래 줄과 동일하게 맞춘다.

**(C) `BottomVisionView.xaml.cs` — using 별칭 2줄 + `lbl_teachStatus` 9곳 (섹션 E 표)**

같은 방식. `_slotRois[...] = ...` (성공 분기 보관 로직), 줄 끝 `//260626 hbk ...` 주석은 **보존**.

**(D) 공통 금지사항 — 하드 제약**
- `if` / `else` / `return` 줄을 추가·삭제·이동하지 않는다. 블록 구조 그대로.
- `TryTeach` / `TryCreateModel` / `TryFindPose` / `TryFindRefPose` / `HasTemplate` / `Matcher.Run` 호출 줄과
  그 인자, `bool bOk` / `bool ok` 대입, `datum.RefMatch*` 대입은 **한 글자도** 건드리지 않는다.
- `HOperatorSet.*` 는 이 3개 파일 편집 범위에 아예 등장하지 않는다 — 새로 넣지도 않는다.
- 삼항연산자 `?:` 금지. `??` 도 새로 넣지 않는다(C-5/C-6 은 제거하는 쪽이다).
- `PickerCenterCalibrationService.cs` 를 열지 않는다.
- `lbl_status`(다른 라벨)는 이번 범위가 아니다 — `lbl_teachStatus` 만 만진다.

**(E) 자기검증**
편집 후 `git diff -U0 -- <3파일>` 을 눈으로 훑어, 추가/삭제 줄이 전부
`lbl_teachStatus.Text=` / `lbl_teachStatus.Foreground=` / `CustomMessageBox.Show(` / `alignMsg =` / `using ... =`
중 하나인지 확인한다. 그 밖의 줄이 있으면 되돌린다. verify 가 같은 검사를 자동으로 한다.
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && M=WPF_Example/UI/ContentItem/MainView.xaml.cs; T=WPF_Example/Custom/UI/TrayVisionView.xaml.cs; B=WPF_Example/Custom/UI/BottomVisionView.xaml.cs; echo "=== [하드제약 1] 기대 0: 판정 심볼이 diff 추가/삭제 줄에 없음 ===" && git diff -U0 -- $M $T $B | grep -E '^[+-][^+-]' | grep -E 'HOperatorSet\.|TryTeach\(|TryCreateModel\(|TryFindPose\(|TryFindRefPose\(|HasTemplate\(|Matcher\.Run|RefMatch|\breturn\b|\bif *\(|\belse\b|ShowConfirmation|SaveRecipe|EnsurePerRoiDefaults|_slotRois|CommitActiveRectangle|RectToTeachParams|ValidateRois\(\)|ApplyCoaxLight' | wc -l && echo "=== [하드제약 2] 기대 0: 서비스 3파일 무변경 ===" && git status --porcelain -- WPF_Example/Halcon/Algorithms/PatternMatchService.cs WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs | wc -l && echo "=== [하드제약 3] 참고용(전체파일 카운트라 무관 코드와 섞임 — 최소 1건 이상이면 앵커 생존, 실질 보증은 하드제약 1/4) ===" && grep -c 'bool ok = svc.TryCreateModel(' $M; grep -c 'bool bOk = EthernetVisionHandler.Handle.Matcher.TryTeach(' $T; grep -c 'bool bOk = EthernetVisionHandler.Handle.Matcher.TryTeach(' $B; echo "=== [하드제약 4] 기대 0: diff 추가줄이 표시 레이어 밖으로 새지 않음 ===" && git diff -U0 -- $M $T $B | grep -E '^\+[^+]' | grep -vE 'lbl_teachStatus\.(Text|Foreground)|CustomMessageBox\.Show\(|alignMsg = |^\+using |//quick-260812' | wc -l && echo "=== [한국어화] 기대 0: raw 원형 소멸 ===" && grep -c 'CustomMessageBox.Show("모델 생성 실패", error)' $M; grep -c 'CustomMessageBox.Show("ref pose 기록 실패", refError)' $M; grep -c '"티칭 실패: " + error;' $T; grep -c '"티칭 실패: " + error;' $B; grep -c '"티칭 예외: " + ex.Message;' $T; grep -c '"티칭 예외: " + ex.Message;' $B; echo "=== [한국어화] 기대 각 1/2: ToKoreanMessage 배선 ===" && grep -c 'TeachDiagnostics.ToKoreanMessage(error)' $M; grep -c 'TeachDiagnostics.ToKoreanMessage(refError)' $M; grep -c 'TeachDiagnostics.ToKoreanMessage(refErr2)' $M; grep -c 'TeachDiagnostics.ToKoreanMessage(err2)' $M; grep -c 'TeachDiag.ToKoreanMessage' $T; grep -c 'TeachDiag.ToKoreanMessage' $B; echo "=== [자동닫힘] 기대 7 ===" && grep -c 'MessageBoxImage.Error, true, false' $M && echo "=== [색 stale] 기대 6 / 9 : Text 대입수 == Foreground 대입수 ===" && grep -c 'lbl_teachStatus.Text' $T; grep -c 'lbl_teachStatus.Foreground' $T; grep -c 'lbl_teachStatus.Text' $B; grep -c 'lbl_teachStatus.Foreground' $B; echo "=== [색] 기대 >=1: 성공 초록 명시 ===" && grep -c 'GradeBrush(ETeachGrade.Good)' $T && grep -c 'GradeBrush(ETeachGrade.Good)' $B && echo "=== [컨벤션] 기대 0: 추가줄에 삼항 ===" && git diff -- $M $T $B | grep -E '^\+[^+]' | grep -c ' ? '; echo "=== [범위] 변경 파일: 소스 5개만 (+ .planning) ===" && git status --porcelain && echo "=== Debug/x64 빌드 (잠기면 스크래치 폴백) ===" && ("/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" //t:Rebuild //p:Configuration=Debug //p:Platform=x64 //v:minimal //nologo 2>&1 | grep -iE "error|warning CS|Build succeeded" | head -25 || "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" //p:Configuration=Debug //p:Platform=x64 //p:OutputPath="$TEMP/gsd-m8i-scratch/bin/" //p:BaseIntermediateOutputPath="$TEMP/gsd-m8i-scratch/obj/" //v:minimal //nologo 2>&1 | grep -iE "error CS|warning CS|Build succeeded" | head -25)</automated>
  </verify>
  <done>
- **[하드제약 1]** diff 의 추가/삭제 줄 중 판정 심볼(`HOperatorSet.` / `TryTeach(` / `TryCreateModel(` / `TryFindPose(` / `TryFindRefPose(` / `HasTemplate(` / `Matcher.Run` / `RefMatch` / `return` / `if (` / `else` / `_slotRois` 등) 포함 줄 **0건**.
- **[하드제약 2]** `PatternMatchService.cs` / `AlignShapeMatchService.cs` / `PickerCenterCalibrationService.cs` `git status` **0줄**.
- **[하드제약 3]** 앵커 3종(생성 진입점 `bool ok/bOk = ...TryCreateModel(`/`...TryTeach(`)이 삭제되지 않고 생존(참고용 — `if (ok) {`류는 같은 파일 무관 코드에 다건 존재해 전체카운트로 "정확히 1건"을 주장하지 않음, 실질 보증은 하드제약 1/4).
- **[하드제약 4]** diff 추가줄이 `lbl_teachStatus.Text|Foreground` / `CustomMessageBox.Show(` / `alignMsg =` / `using ... =` / `//quick-260812` 밖으로 새는 건 **0건**.
- **[한국어화]** raw 원형 6종 전부 0건, `ToKoreanMessage` 배선 MainView 4곳 + Tray 2곳 + Bottom 2곳.
- **[자동닫힘]** `MessageBoxImage.Error, true, false` **7건**(C-1~C-4, C-7, C-8, C-9).
- **[색 stale]** Tray `Text` 6 == `Foreground` 6, Bottom `Text` 9 == `Foreground` 9. 성공 경로에 `GradeBrush(ETeachGrade.Good)` 존재.
- **[컨벤션]** 추가줄 삼항연산자 0건.
- **[범위]** `git status --porcelain` 에 소스 5개(신규 1 + 수정 4)와 `.planning/quick/...` 만 등장. Picker 파일 없음.
- Debug/x64 빌드 성공, 신규 `error CS` 0 / 신규 `warning CS` 0.
  </done>
</task>

</tasks>

<verification>
1. **하드 제약 (판정 로직 무변경)** — `git diff -U0` 의 `+`/`-` 줄에 판정 심볼 0건(핵심 보증) + 진입점 앵커 3종 생존(참고용) +
   판정 엔진 3파일 `git status` 0줄. 실질적 회귀 보증은 판정심볼 0건 체크(하드제약 1)와 화이트리스트 역검사(하드제약 4)가 담당한다.
2. **신규 헬퍼** — `TeachDiagnostics.cs` 가 공개 API 5종을 갖고 csproj 에 등록돼 실제 컴파일에 포함된다.
   사전 항목은 서비스 원문 대조로 채워졌고(추측 0), 미매칭 시 원문을 `(원본: ...)` 로 병기한다.
3. **한국어화** — raw 원형 6종 소멸, `ToKoreanMessage` 8지점 배선.
4. **자동닫힘 제거** — `InvokeCreatePatternModel` 실패 알림 7곳 `isAutoClosing=false`.
5. **색 stale 방지** — `lbl_teachStatus.Text` 대입 횟수 == `Foreground` 대입 횟수 (Tray 6/6, Bottom 9/9).
6. **범위 봉인** — 변경 파일 정확히 5개. `PickerCenterCalibrationService.cs` 는 diff·status 어디에도 없다.
7. **빌드** — Debug/x64 신규 `error CS` 0 / 신규 `warning CS` 0. 산출물 잠김 시 프로세스 종료 없이 스크래치 컴파일.
</verification>

<success_criteria>
- 티칭이 실패하면 **한국어 문구**가 뜬다. 사전에 없는 원문도 숨기지 않고 "(원본: ...)" 로 함께 보여 준다.
- 실패 모달이 **저절로 닫히지 않아** 운영자가 원인을 끝까지 읽을 수 있다.
- Align 상태 라벨의 색이 상태와 항상 일치한다 — 실패 후 성공했는데 빨강이 남는 일이 없다.
- **판정 로직·제어 흐름·HALCON 호출 시퀀스는 diff 에 단 한 줄도 없다(회귀 0).** 바뀐 건 문자열·색·모달 옵션뿐이다.
- `TeachDiagnostics` 시그니처가 확정돼 Quick #2(점수 등급 배선)가 코드 수정 없이 바로 소비할 수 있다.
- `PickerCenterCalibrationService.cs` 와 사용자 stash@{0} 실험이 그대로 보존된다.
- Debug/x64 빌드가 신규 에러/경고 0 으로 통과한다.
</success_criteria>

<output>
완료 후 `.planning/quick/260812-m8i-teach-diag-1-messages/260812-m8i-SUMMARY.md` 를 작성한다.

SUMMARY 에 반드시 포함:
- 처리 건수 (신규 파일 1 + csproj 1줄 / MainView 9곳 / Tray 6+2곳 / Bottom 9+2곳)
- **하드 제약 검증 결과** — judgement-symbol grep 0건, 진입점 앵커 3종 생존(참고), 서비스 3파일 무변경을
  실제 명령 출력으로 인용
- **브리핑 줄번호 정정 사실** — Bottom 실패 지점이 `768/852/895` 가 아니라 `494/499` 이고
  `lbl_teachStatus` 대입은 파일 전체에 9곳뿐이라는 실측 결과
- **범위 확장 사실과 근거** — 브리핑은 "성공 경로 색 복귀" 만 지목했으나, `Text` 대입 15곳 **전부**에
  색을 짝지어야 stale 이 실제로 사라진다는 판단으로 "없음/주의" 경로까지 포함한 점
- 사전(EXACT / NESTED / VALUE) 항목 수와, 전부 서비스 원문 대조로 채웠다는 점
- 빌드 방식 (정상 Rebuild 인지 / 산출물 잠김으로 스크래치 컴파일인지)
- Quick #2 / #3 으로 넘기는 것 (ClassifyScore 실제 배선, Calib TCP 자동경로 노출)
</output>
