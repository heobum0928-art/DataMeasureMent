---
phase: quick-260812-na0
plan: 01
type: execute
wave: 1
depends_on: [quick-260812-m8i]
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
  - WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs
  - WPF_Example/Custom/UI/TrayVisionView.xaml.cs
  - WPF_Example/Custom/UI/BottomVisionView.xaml.cs
  - WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs
  - WPF_Example/Custom/SystemHandler.cs
autonomous: true
requirements: [TEACH-UX-01]

must_haves:
  truths:
    - "[하드제약 1 — 새 HALCON 호출 0회] 이번 Quick 이 만든 diff 의 **추가줄** 어디에도 `HOperatorSet.` / `FindShapeModel(` / `FindNccModel(` / `CreateShapeModel(` / `CreateNccModel(` / `_matcher.` / `.TryCreateModel(` / `.TryFindPose(` / `.TryFindRefPose(` / `.TryTeachModel(` / `Matcher.Run(` / `.Grab(` 이 등장하지 않는다. 검사 대상은 6개 파일 전부이며, Picker 파일은 `git diff`(사용자 실험 hunk 포함)가 아니라 **편집 직전 스냅샷 대비 diff** 로 판정한다. 표시되는 점수는 전부 '이미 계산되고 버려지던 값'의 재활용이다."
    - "[하드제약 2 — Align 서비스 순수 추가] `AlignShapeMatchService.cs` 의 `git diff` 에 **삭제줄(`^-[^-]`)이 0건**이다. 삭제줄 0은 기존 오버로드 2개 시그니처와 코어 `TryTeach` 본문의 **기존 줄 전부가 바이트 단위로 동일**하다는 뜻이다(카운트 기반 주장보다 강한 보증) — 단 이것만으로 '동작 무변경'을 완전히 증명하진 않는다. 완전한 보증은 여기에 (a) 신규 오버로드 4종의 파라미터 개수가 13/14/15/16으로 전부 달라 오버로드 해석 후보가 바뀌지 않고, (b) 캡처 대입 2줄이 각 가드(`if (!bRefN) {...}`)의 닫는 `}` 바로 다음(성공 경로 전용)에만 삽입되어 제어흐름을 바꾸지 않으며, (c) 빌드가 통과한다는 3가지를 더해야 성립한다 — 이 3가지는 각각 하드제약 검사(오버로드 개수 확인/캡처 위치 확인/빌드)로 별도 검증된다."
    - "[하드제약 3 — Picker 순수 추가 + 실험 보존] `PickerCenterCalibrationService.cs` 는 **편집 직전 스냅샷 대비 삭제줄이 0건**이다. 따라서 사용자의 미커밋 실험(`htRad` / `TupleGenConst` / `TupleRad(5,...)` / `-htRad` / `2.0 * htRad`)과 `FindShapeModel` 호출 인자 전체가 한 글자도 바뀌지 않았음이 증명된다. `htRad`/`TupleRad` 를 포함한 줄은 이번 Quick 의 추가줄에 0건이다."
    - "[하드제약 4 — 실험은 커밋되지 않는다] Task 3 커밋 시 Picker 파일은 `HEAD + 이번 Quick 변경`만 담기고 사용자 실험(±5° 탐색범위)은 커밋에 들어가지 않는다. 커밋 직후 실험을 워킹트리에 복원하며, 복원본이 백업본과 `diff` 0 (바이트 동일)이고, 최종 `git diff -- Picker` 는 `TupleRad(5, out htRad)` 를 포함하고 `dScore` 를 포함하지 않는다(= 우리 변경은 HEAD 에, 실험은 워킹트리에)."
    - "[하드제약 5 — 표시 레이어 밖으로 새지 않음] 각 파일의 추가줄이 파일별 화이트리스트(점수 지역변수 선언 / `out` 인자 삽입 / `ClassifyScore` 산출 / `ToStatusLine`·`GradeBrush` 대입 / `Logging.PrintLog` 포맷 / `//quick-260812` 주석 / `using` 별칭) 밖으로 나가는 줄이 0건이다. Datum(MainView)은 여기에 더해 Quick #1 과 동일한 판정심볼 검사(`return`/`if (`/`else`/`SaveRecipe`/`RefMatch`/`EnsurePerRoiDefaults` 가 추가·삭제줄에 0건)까지 통과한다."
    - "[무변경 파일] `WPF_Example/Halcon/Algorithms/PatternMatchService.cs` 와 `WPF_Example/Halcon/Algorithms/TeachDiagnostics.cs` 가 `git status --porcelain` 에 등장하지 않는다. Quick #1 이 확정한 API 를 **소비만** 하고 고치지 않는다."
    - "Datum 완료 안내에 등급이 뜬다 — `InvokeCreatePatternModel` 성공 경로의 완료 모달 본문이 `TeachDiagnostics.ToStatusLine(grade1, ...)` 로 감싸이고, 패턴 2 성공 문구(`alignMsg`)가 `ToStatusLine(grade2, ...)` 로 감싸인다. 등급 산출 입력은 이미 스코프 안에 있던 `rs` / `rs2` 와 `datum.PatternMinScore` 다."
    - "Datum 등급의 기준값이 0 이 아니다 — 분류 직전 경로에서 `datum.EnsurePerRoiDefaults()` (같은 메서드 상단)가 `PatternMinScore<=0 → 0.6` 을 복원하므로, 선언 기본값 0.0 때문에 전부 Good 으로 뜨는 오분류가 발생하지 않는다."
    - "Align 등급은 두 패턴 중 **낮은 쪽**으로 매겨진다 — Tray/Bottom 성공 분기가 `Math.Min(dScore1, dScore2)` 를 `ClassifyScore` 에 넣는다. 이는 런타임 `Run()` 이 `result.Score = Math.Min(f1Score, f2Score)` 로 쓰는 기존 보수적 지표 규칙과 동일하다."
    - "Align 캡처는 성공 경로에서만 일어난다 — `_lastTeachScoreN = rNScore;` 두 줄이 각각 `if (!bRefN) { ... return false; }` **가드의 닫는 `}` 바로 다음 줄**에 놓인다(가드 안이면 실패 시에도 기록돼 stale 이 샌다). 검증은 해당 줄의 직전 2줄에 `return false;` 가 존재하는지로 판정한다."
    - "Align 점수 노출은 새 오버로드 2개로만 이뤄진다 — `public bool TryTeach(` 선언이 이 파일에 4건(기존 무-슬롯 1 + 기존 슬롯 1 + 신규 무-슬롯 1 + 신규 슬롯 1)이고, 4개 모두 파라미터 개수가 달라 오버로드 해석이 모호하지 않다. 신규 2개는 HALCON 을 직접 호출하지 않고 기존 코어 TryTeach 로 위임만 한다."
    - "Calib 점수는 시그니처 확장으로 노출된다 — `TryAddStep` 에 `out double dScore` 가 추가되고, 값은 기존 로그(`score={3:F3}`)가 이미 읽던 `score[0].D` 를 `finally` 의 `Dispose` **이전**에 복사한 것이다. 호출부 2곳(TCP `ProcessAlignCalib`, Bottom 수동 UI)이 모두 갱신돼 컴파일러가 누락을 강제 검출한다."
    - "Calib 등급은 TCP 경로에서 로그로, 수동 UI 경로에서 라벨 문구로 나타난다 — TCP 는 `[ALIGN_CALIB] STEP {0} OK score={1:F3} grade={2}`, UI 는 `lbl_calStatus.Text` 를 `ToStatusLine` 으로 감싼다. **`lbl_calStatus` 는 색(Foreground)을 건드리지 않는다** — 이 라벨은 같은 파일에 36곳 대입이 있어 일부에만 색을 칠하면 Quick #1 이 없앤 stale-색 버그가 재발한다."
    - "최소 점수 상수가 중복 선언되지 않는다 — `AlignShapeMatchService.MIN_SCORE` / `PickerCenterCalibrationService.FIND_MIN_SCORE` 를 읽기전용 프로퍼티(`TeachMinScore` / `FindMinScore`)로 노출해 호출부가 참조한다. 값(0.5)을 UI 쪽에 복사해 적는 매직넘버가 0건이다."
    - "코딩 규칙 준수 — 이번 Quick 추가줄에 삼항연산자 `?:` 가 0 건, C# 7.2 문법만 사용, 각 파일의 기존 브레이스 스타일(MainView/Tray/Bottom/Picker = K&R, Custom/SystemHandler = Allman, Align = K&R + 다중행 시그니처는 `{` 별행) 유지, 새 주석은 `quick-260812:` 접두."
    - "Debug/x64 컴파일이 신규 `error CS` 0 / 신규 `warning CS` 0 으로 통과한다. 산출물이 실행 중 앱에 잠겨 있으면 **프로세스를 죽이지 않고** 스크래치 OutDir 컴파일로 검증한다."
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "Datum 티칭 완료 모달 등급 표시 — grade1(rs) / grade2(rs2) 산출 + ToStatusLine 삽입. InvokeCreatePatternModel 밖은 무변경"
      contains: "TeachDiagnostics.ClassifyScore(rs, datum.PatternMinScore)"
    - path: "WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs"
      provides: "ref find 스코어 캡처 필드 2개 + 캡처 대입 2줄 + TeachMinScore 읽기전용 프로퍼티 + 스코어 노출 오버로드 2개 (전부 추가, 삭제 0)"
      contains: "public static double TeachMinScore"
    - path: "WPF_Example/Custom/UI/TrayVisionView.xaml.cs"
      provides: "Align 티칭 성공 라벨에 min(score) 기반 등급 — 실패/예외 분기는 Quick #1 상태 그대로"
      contains: "TeachDiag.ClassifyScore(dMinScore, AlignShapeMatchService.TeachMinScore)"
    - path: "WPF_Example/Custom/UI/BottomVisionView.xaml.cs"
      provides: "Align 티칭 성공 라벨 등급(Task 2) + 피커캘 스텝 라벨 등급(Task 3, 문구만·색 없음)"
      contains: "TeachDiag.ClassifyScore(calScore, PickerCenterCalibrationService.FindMinScore)"
    - path: "WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs"
      provides: "TryAddStep out double dScore 노출 + FindMinScore 읽기전용 프로퍼티. 사용자 실험(±5°)과 FindShapeModel 인자는 무변경(스냅샷 대비 삭제줄 0)"
      contains: "out double dScore,"
    - path: "WPF_Example/Custom/SystemHandler.cs"
      provides: "ProcessAlignCalib STEP 성공 로그에 score + grade 추가. UI 노출 없음(Quick #3 범위)"
      contains: "[ALIGN_CALIB] STEP {0} OK score={1:F3} grade={2}"
  key_links:
    - from: "MainView.xaml.cs L3903 `double rr, rc, ra, rs;` (TryFindPose 가 이미 채운 값)"
      to: "완료 모달 본문 등급"
      via: "ClassifyScore(rs, datum.PatternMinScore) → ToStatusLine(grade1, 기존문구). 새 find 호출 없음"
      pattern: "ToStatusLine\\(grade1, \"패턴 모델 생성"
    - from: "MainView.xaml.cs L3922 `rs2` (패턴 2 TryFindPose 결과)"
      to: "alignMsg 패턴 2 성공 문구 등급"
      via: "ClassifyScore(rs2, datum.PatternMinScore) → ToStatusLine(grade2, 기존문구). \"\\n\" 은 ToStatusLine 밖에 둬 기호가 줄 앞에 오게 한다"
      pattern: "ToStatusLine\\(grade2, \"패턴 2 모델 생성"
    - from: "AlignShapeMatchService 코어 TryTeach 의 r1Score/r2Score (TryFindRefPose 가 이미 채운 값)"
      to: "_lastTeachScore1/2 캡처 → 신규 오버로드 out"
      via: "각 `if (!bRefN) { ... }` 가드의 닫는 `}` **바로 다음 줄**에 대입 1줄씩 추가 — 성공 경로에서만 기록되고, 오버로드는 bOk==true 일 때만 읽으므로 stale 유출 불가"
      pattern: "_lastTeachScore1 = r1Score;"
    - from: "TrayVisionView / BottomVisionView TeachButton_Click"
      to: "스코어 노출 오버로드 (out dScore1, out dScore2)"
      via: "`out error);` 바로 **앞 줄에 인자 한 줄 삽입** — 기존 호출줄은 손대지 않는다(순수 추가)"
      pattern: "out dScore1, out dScore2,"
    - from: "PickerCenterCalibrationService.TryAddStep 의 score[0].D (기존 로그가 이미 읽던 값)"
      to: "out double dScore"
      via: "`foundCol = dCol;` 다음 줄에 복사 1줄 추가. finally 의 score.Dispose() 이전 시점"
      pattern: "dScore   = score\\[0\\]\\.D;"
    - from: "Custom/SystemHandler.cs ProcessAlignCalib STEP 성공 분기"
      to: "등급 로그"
      via: "`Logging.PrintLog((int)ELogType.Trace,` 줄은 그대로 두고 포맷/인자 줄만 교체 → diff 최소화"
      pattern: "grade=\\{2\\}"
    - from: "사용자 미커밋 실험 (±5° 탐색범위, git stash pop 으로 복원됨)"
      to: "워킹트리에 그대로 잔존 + 커밋에는 미포함"
      via: "편집 전 스냅샷/패치 백업 → 커밋 직전 `git apply -R` 로 실험만 되돌려 커밋 → 즉시 백업본으로 복원"
      pattern: "TupleRad\\(5, out htRad\\)"
---

<objective>
티칭 실패/품질 진단 3단계 작업의 **2/3 단계**다.

Quick #1(커밋 `ca5a380`+`5466f06`)이 `TeachDiagnostics.ClassifyScore / ToStatusLine / GradeBrush` 를
만들어 뒀지만 **`ClassifyScore` 는 아직 호출부가 0건**(레포 전체 grep 확인)이다.
지금은 티칭이 "성공"이라고만 뜨고, 그 성공이 **간신히 통과한 0.52 인지 여유 있는 0.91 인지** 알 수 없다.

이번 단계는 세 경로(Datum / Align / Calib)에서 **이미 계산되고 버려지던 매칭 점수**를 꺼내
등급(● 양호 / ▲ 주의 / ✕ 미달)으로 표시·기록한다.

> 🔒 **하드 제약 (사용자 명시, 이번 Quick 최우선 원칙):**
> 기존 검사/티칭 **판정 로직·제어흐름은 절대 안 건드린다.**
> **새 HALCON 호출(`FindShapeModel` / `FindNccModel` / `HOperatorSet.*`) 추가는 절대 금지.**
> 허용되는 것은 *이미 계산된 점수를 밖으로 빼는 시그니처 확장(`out` 파라미터 추가)* 뿐이다.
> 그래서 이 계획은 두 서비스 파일에 대해 **"diff 삭제줄 0"** 이라는, 카운트보다 강한 보증을 요구한다.

> ⚠ **사용자 실험 보존 (별건):**
> `PickerCenterCalibrationService.cs` 에는 사용자의 미커밋 실험(±5° 탐색범위)이
> `git stash pop` 으로 **워킹트리에 복원돼 있다**(stash list 는 비어 있음 — 확인 완료).
> 이 실험은 **검증되지 않은 동작 변경**(0..360° → ±5°)이라 우리 커밋에 실려 나가면 안 되고,
> 동시에 **되돌리거나 수정해서도 안 된다.** Task 3 이 이 둘을 모두 만족시키는 커밋 절차를 담는다.

**Output:**
- Datum 완료 모달 등급 2곳 (패턴1 `rs`, 패턴2 `rs2`)
- Align 스코어 노출 오버로드 2개 + Tray/Bottom 성공 라벨 등급
- Calib `TryAddStep out double dScore` + TCP 로그 등급 + Bottom 수동 UI 라벨 등급
- 이번 범위 밖: TCP 자동경로의 **화면** 노출(Quick #3)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

**코딩 규칙 (이 프로젝트 상시 규칙 — 위반 시 리뷰 반려):**
- 삼항연산자 `?:` **금지** → 반드시 `if / else`.
- C# 7.2 (`nullable reference types`, `switch expression`, `record`, `is not` 등 8.0+ 문법 금지)
- **파일별 기존 스타일을 그대로 따른다:**
  - `MainView.xaml.cs` / `TrayVisionView.xaml.cs` / `BottomVisionView.xaml.cs` / `PickerCenterCalibrationService.cs` → **K&R** (`{` 같은 줄)
  - `Custom/SystemHandler.cs` → **Allman** (`{` 별행) — 편집 대상 `ProcessAlignCalib` 부근 실측 확인됨
  - `AlignShapeMatchService.cs` → K&R 이되 **다중행 시그니처는 `{` 를 별행**에 둔다(L317/L332 선례). 신규 오버로드도 동일.
- 새 주석은 `//quick-260812:` 접두 + **짧게**, 비자명한 "왜"만.
  `//YYMMDD hbk` 날짜 주석 규칙은 2026-06-11 부로 폐기 — 새로 달지 말 것.
- ⚠ **주석 문구에 금지심볼을 넣지 말 것** — 하드제약 검사는 diff 줄을 grep 하므로, 주석에 `EnsurePerRoiDefaults`,
  `TryFindPose`, `RefMatch`, `HOperatorSet.`, `_matcher.`, `htRad`, `TupleRad`, `FindShapeModel` 같은 토큰을 적으면
  **주석 때문에 검사가 실패**한다. 설명이 필요하면 토큰 없이 우리말로 쓴다.
- 파일 인코딩: **UTF-8, BOM 없음** (Edit 도구만 사용. heredoc/`cat >` 금지)

***

## 🚫 절대 건드리면 안 되는 것

1. **`WPF_Example/Halcon/Algorithms/TeachDiagnostics.cs`** — Quick #1 산출물. **API 소비만, 무변경.**
2. **`WPF_Example/Halcon/Algorithms/PatternMatchService.cs`** — 무변경. Datum 쪽은 `MainView.xaml.cs` 의
   **기존 지역변수 `rs`/`rs2` 재사용**만 하고 서비스 파일은 열지 않는다.
3. **`PickerCenterCalibrationService.cs` 의 실험 코드** — `HTuple htRad;` / `TupleGenConst` / `TupleRad(5, out htRad)` /
   `-htRad` / `2.0 * htRad` 줄, 그리고 `FindShapeModel(...)` 호출의 **모든 인자**. 단 한 글자도 금지.
4. **`AlignShapeMatchService.TryTeach` 기존 오버로드 2개** — 시그니처/본문 판정로직 무변경.
   본문에는 **캡처 대입 2줄만** 추가한다(아래 근거 참조).
5. **`lbl_calStatus` 의 색(Foreground)** — 이 파일에 36곳 대입이 있어 일부만 칠하면 stale-색 버그가 생긴다. 문구만.
6. **`InvokeCreatePatternModel` 메서드 밖 (MainView L3848 이전 / L3974 이후)** — 이 파일은 4,000줄 이상이다. 편집 금지.

***

## 조사 완료 사실 (실제 파일 Read 로 검증, 2026-08-12)

### ① 브리핑의 추정 변수명 — **3건 모두 실제와 일치**
| 경로 | 브리핑 추정 | 실측 | 위치 |
|---|---|---|---|
| Datum 패턴1 | `rs` | ✅ `rs` | MainView L3903 `double rr, rc, ra, rs;` |
| Datum 패턴2 | `rs2` | ✅ `rs2` | MainView L3922 |
| Align | `r1Score`/`r2Score` | ✅ 동일 | AlignShapeMatchService L386 / L397 |
| Calib | `score` | ✅ 동일 | Picker L236 `HTuple score` → L275 로그에서 `score[0].D` |

### ② ⚠ 브리핑 정정 — Datum 완료 안내는 `CustomMessageBox.Show` 가 **아니다**
실제는 **`CustomMessageBox.ShowConfirmation("모델 생성 완료", ..., MessageBoxButton.YesNo)`** (L3945~3949, Recipe Save 확인 모달).
`Show` 가 아니라 `ShowConfirmation` 이며, 우리는 **본문 문자열 인자 한 줄만** 바꾼다.
`ShowConfirmation(` 호출줄 자체와 `MessageBoxButton.YesNo);` 줄은 무변경 → diff 에 등장하지 않는다.

### ③ `datum.PatternMinScore` 는 선언 기본값이 **0.0** 이다 (DatumConfig L151)
그대로 쓰면 `ClassifyScore(rs, 0.0)` → 거의 전부 Good 오분류. **하지만 안전하다:**
`InvokeCreatePatternModel` 은 티칭 전에 `datum.EnsurePerRoiDefaults()` (L3860)를 호출하고,
그 안(DatumConfig L1057)이 `if (PatternMinScore <= 0.0) PatternMinScore = 0.6;` 로 복원한다.
→ 분류 시점에는 항상 > 0. **추가 가드 코드를 넣지 말 것**(제어흐름 추가 = 하드제약 위반).

### ④ `ClassifyScore` 는 현재 **호출부 0건** (레포 전체 grep)
```
$ grep -rn "ClassifyScore" --include=*.cs WPF_Example
WPF_Example/Halcon/Algorithms/TeachDiagnostics.cs:103:  public static ETeachGrade ClassifyScore(...)
```
→ 편집 후 각 파일의 `ClassifyScore` 등장 횟수는 **전부 이번 Quick 이 저자**다.
   그래서 이 패턴에 한해 "정확히 N건" 주장이 정당하다(무관 코드와 섞일 수 없음).

### ⑤ `TryTeach` 호출부 전수 (grep 확인) — 딱 2곳 + 내부 위임 1곳
| 파일 | 줄 | 호출 형태 |
|---|---|---|
| `TrayVisionView.xaml.cs` | 305 | 무-슬롯 오버로드 |
| `BottomVisionView.xaml.cs` | 482 | 슬롯 오버로드 (`_selectedSlot`) |
| `AlignShapeMatchService.cs` | 318 | 무-슬롯 → 슬롯 내부 위임 |
→ 외부 호출부가 이 2곳뿐임이 확정. 다른 프로젝트/테스트 호출 없음.

### ⑥ `TryAddStep` 호출부 전수 (grep 확인) — 딱 2곳
| 파일 | 줄 | 경로 |
|---|---|---|
| `Custom/SystemHandler.cs` | 743 | TCP `$ALIGN_CALIB:...,STEP@` |
| `Custom/UI/BottomVisionView.xaml.cs` | 831 | 수동 UI [스텝 추가] |
→ 시그니처를 직접 확장해도 **컴파일러가 누락을 강제 검출**한다(오버로드 추가보다 안전).

### ⑦ 왜 Align 은 "오버로드 추가", Calib 은 "시그니처 확장" 인가
- Align: 사용자가 **기존 오버로드 2개 시그니처 무변경**을 명시 → 신규 오버로드 2개 추가.
  다만 스코어(`r1Score`/`r2Score`)는 코어 TryTeach 의 **지역변수**라 밖에서 볼 방법이 없다.
  → 코어 본문을 **옮기지 않고**(옮기면 100줄 이동 diff = 전사 오류 위험) **캡처 대입 2줄만 추가**하고,
    신규 오버로드가 코어를 호출한 뒤 캡처값을 넘긴다. 결과: **이 파일 diff 의 삭제줄 0** = 회귀 불가능.
  - 캡처 필드의 안전성: 티칭은 UI 스레드 단독 경로(호출부 2곳 모두 Button_Click)이고,
    TCP 스레드가 쓰는 `Run()` 은 이 필드를 건드리지 않는다.
    또한 신규 오버로드는 `bOk == true` 일 때만 읽으므로 **직전 실패/다른 슬롯의 stale 값이 새어나갈 수 없다**.
- Calib: 사용자가 **`out double dScore` 추가**를 명시 → 시그니처 확장. 호출부 2곳 모두 갱신.

### ⑧ 최소 점수 상수는 private const — 읽기전용 프로퍼티로 노출
| 파일 | 상수 | 노출 |
|---|---|---|
| `AlignShapeMatchService.cs` L27 | `private const double MIN_SCORE = 0.5;` | `public static double TeachMinScore` 신설 |
| `PickerCenterCalibrationService.cs` L33 | `private const double FIND_MIN_SCORE   = 0.5;` | `public static double FindMinScore` 신설 |
`private` → `public` 으로 **바꾸지 말 것**(삭제줄 발생 = 하드제약 2/3 위반). 프로퍼티를 **새로 추가**한다.
UI 쪽에 `0.5` 를 복사해 적는 것도 금지(매직넘버 이중소스).

### ⑨ `lbl_calStatus.Text` 는 이 파일에 **36곳** 대입 (grep 실측)
→ 이번에 만지는 1곳에만 색을 칠하면 나머지 35곳에서 색이 stale 된다. **문구(●/▲/✕)만** 넣는다.

### ⑩ Picker 파일의 현재 워킹트리 상태 (실측)
```
$ git status --porcelain
 M WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs
$ git stash list
(비어 있음 — pop 완료)
```
`git diff` hunk 는 `@@ -238,10 +238,14 @@` 단 1개: `htRad` 3줄 + 공백 1줄 추가, `new HTuple(0.0)` /
`new HTuple(Math.PI * 2.0)` 2줄이 `-htRad` / `2.0 * htRad` 로 교체.
→ **이 파일에 대한 `git diff` 기반 검사는 무의미**(실험이 이미 diff 에 있음). 반드시 **스냅샷 대비 diff** 를 쓴다.

***

<interfaces>
<!-- 실행자가 코드베이스를 재탐색하지 않도록 편집 대상 원문과 목표 코드를 그대로 옮겨둔다. -->
<!-- 줄번호는 편집 전 기준이며 편집 중 밀린다 — Edit 은 반드시 문자열 앵커로 지정할 것. -->

## A. Datum — `WPF_Example/UI/ContentItem/MainView.xaml.cs`

### A-1. 패턴 2 성공 문구 (L3930, `if (svc.TryFindPose(...))` 성공 분기 안)

편집 전:
```csharp
                                alignMsg = "\n패턴 2 모델 생성 + RefMatch2 기록 (score " + rs2.ToString("F3") + ") — 2-패턴 baseline 회전보정 활성";
```
편집 후 (3줄):
```csharp
                                //quick-260812: 이미 계산된 rs2 재사용 — 새 검색 호출 없음
                                ETeachGrade grade2 = TeachDiagnostics.ClassifyScore(rs2, datum.PatternMinScore);
                                alignMsg = "\n" + TeachDiagnostics.ToStatusLine(grade2, "패턴 2 모델 생성 + RefMatch2 기록 (score " + rs2.ToString("F3") + ") — 2-패턴 baseline 회전보정 활성");
```
> `"\n"` 을 `ToStatusLine` **밖**에 두는 이유: 기호(●/▲/✕)가 줄 맨 앞에 와야 읽힌다.

### A-2. 완료 모달 본문 (L3945~3949)

편집 전:
```csharp
                    MessageBoxResult saveChoice = CustomMessageBox.ShowConfirmation(
                        "모델 생성 완료",
                        "패턴 모델 생성·ref pose 기록 완료 (score " + rs.ToString("F3") + ")" + alignMsg
                            + "\n\n지금 Recipe Save 하시겠습니까? (.shm 모델과 RefMatch 기준값이 함께 저장됩니다.\n현재 미저장 편집이 함께 저장됩니다.)\n\n저장하지 않으면 다음 저장 시점까지 모델↔기준값이 어긋날 수 있습니다.",
                        MessageBoxButton.YesNo);
```
편집 후 (앞에 2줄 삽입 + 본문 첫 줄만 교체):
```csharp
                    //quick-260812: 이미 계산된 rs 재사용 — 새 검색 호출 없음. 최소점수 기본값 복원은 이 메서드 상단이 이미 처리
                    ETeachGrade grade1 = TeachDiagnostics.ClassifyScore(rs, datum.PatternMinScore);
                    MessageBoxResult saveChoice = CustomMessageBox.ShowConfirmation(
                        "모델 생성 완료",
                        TeachDiagnostics.ToStatusLine(grade1, "패턴 모델 생성·ref pose 기록 완료 (score " + rs.ToString("F3") + ")") + alignMsg
                            + "\n\n지금 Recipe Save 하시겠습니까? (.shm 모델과 RefMatch 기준값이 함께 저장됩니다.\n현재 미저장 편집이 함께 저장됩니다.)\n\n저장하지 않으면 다음 저장 시점까지 모델↔기준값이 어긋날 수 있습니다.",
                        MessageBoxButton.YesNo);
```
> `ShowConfirmation(` 줄, `"모델 생성 완료",` 줄, `+ "\n\n지금 Recipe Save...` 줄, `MessageBoxButton.YesNo);` 줄은 **무변경**.
> 주석에 `EnsurePerRoiDefaults` 라고 적지 말 것 — 하드제약 grep 이 diff 줄을 검사하므로 주석 때문에 실패한다.
> `MainView.xaml.cs` 는 L12 에 `using ReringProject.Halcon.Algorithms;` 가 이미 있다 → `ETeachGrade` / `TeachDiagnostics` 를 수식 없이 사용. **새 using 금지.**

***

## B. Align 서비스 — `WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs` (전부 **삽입**, 삭제 0)

### B-1. 읽기전용 최소점수 프로퍼티 — L27 다음에 삽입
앵커:
```csharp
        // D-03: 최소 스코어. NumLevels(4)/MinContrast(10)은 PatternMatchService 내부 const — 재선언 불필요.
        private const double MIN_SCORE = 0.5;
```
바로 아래에 삽입:
```csharp

        //quick-260812: 등급 산정용 최소 스코어 노출(읽기 전용). MIN_SCORE 의 값·용도는 무변경.
        public static double TeachMinScore { get { return MIN_SCORE; } }
```

### B-2. 캡처 필드 — L57 `private readonly PatternMatchService _matcher;` 다음에 삽입
```csharp

        //quick-260812: 코어 TryTeach 가 이미 계산한 기준 검색 스코어 2개를 밖으로 넘기기 위한 캡처.
        //  판정에 관여하지 않는다(진단 표시 전용). 성공 경로에서만 기록되고,
        //  스코어 오버로드가 bOk==true 일 때만 동기적으로 읽는다(티칭 = UI 스레드 단독 경로).
        private double _lastTeachScore1 = double.NaN;
        private double _lastTeachScore2 = double.NaN;
```

### B-3. 캡처 대입 2줄 — 코어 TryTeach 안, 각 가드의 **닫는 `}` 바로 다음 줄**

앵커 1 (L391~394) — 아래 4줄 중 **마지막 `}` 바로 다음 줄**(그 아래 빈 줄보다 위)에 삽입:
```csharp
                if (!bRef1) {
                    error = "TryFindRefPose[1]: " + findErr1;
                    return false;
                }
```
삽입할 줄:
```csharp
                _lastTeachScore1 = r1Score;   //quick-260812: 이미 계산된 스코어 캡처(판정 무관)
```
앵커 2 (L402~405) — `bRef2` 가드의 닫는 `}` 바로 다음 줄에 동일하게:
```csharp
                _lastTeachScore2 = r2Score;   //quick-260812: 이미 계산된 스코어 캡처(판정 무관)
```
> **위치가 중요하다.** 가드 블록 **안**에 넣으면 실패 경로에서도 기록돼 stale 이 샌다.
> 검증은 "이 줄의 직전 2줄 안에 `return false;` 가 있는가"로 판정하므로, 반드시 닫는 `}` **바로 다음 줄**이어야 한다.

### B-4. 신규 오버로드 2개 — 코어 TryTeach 의 닫는 `}` (L432) 다음, `// ─── 런타임 위치보정 ───` 주석 **앞**에 삽입
```csharp

        //quick-260812: 스코어 노출 오버로드(무-슬롯) — 기존 무-슬롯 오버로드와 동일하게 slot=None 으로 위임.
        public bool TryTeach(
            HImage img,
            double roi1Row, double roi1Col, double roi1Phi, double roi1Len1, double roi1Len2,
            double roi2Row, double roi2Col, double roi2Phi, double roi2Len1, double roi2Len2,
            EEthernetVisionMode mode,
            out double dScore1, out double dScore2,
            out string error)
        {
            return TryTeach(img,
                roi1Row, roi1Col, roi1Phi, roi1Len1, roi1Len2,
                roi2Row, roi2Col, roi2Phi, roi2Len1, roi2Len2,
                mode, EBottomAlignSlot.None, out dScore1, out dScore2, out error);
        }

        //quick-260812: 스코어 노출 오버로드(슬롯) — 기존 코어 TryTeach 를 그대로 호출하고
        //  코어가 캡처해 둔 기준 검색 스코어 2개를 넘긴다. 새 HALCON 호출 0회.
        public bool TryTeach(
            HImage img,
            double roi1Row, double roi1Col, double roi1Phi, double roi1Len1, double roi1Len2,
            double roi2Row, double roi2Col, double roi2Phi, double roi2Len1, double roi2Len2,
            EEthernetVisionMode mode,
            EBottomAlignSlot slot,
            out double dScore1, out double dScore2,
            out string error)
        {
            dScore1 = double.NaN;
            dScore2 = double.NaN;
            bool bOk = TryTeach(img,
                roi1Row, roi1Col, roi1Phi, roi1Len1, roi1Len2,
                roi2Row, roi2Col, roi2Phi, roi2Len1, roi2Len2,
                mode, slot, out error);
            if (bOk) {
                dScore1 = _lastTeachScore1;
                dScore2 = _lastTeachScore2;
            }
            return bOk;
        }
```
> 오버로드 모호성 없음 — 4개 시그니처의 파라미터 개수가 전부 다르다(무-슬롯 12+1 / 슬롯 12+2 / 무-슬롯+스코어 12+3 / 슬롯+스코어 12+4).
> 신규 슬롯 오버로드 안의 `TryTeach(..., mode, slot, out error)` 는 **기존 코어**로 해석된다.

***

## C. Align UI — `TrayVisionView.xaml.cs` (L275 `TeachButton_Click`)

`using TeachDiag` / `using ETeachGrade` 별칭은 Quick #1 이 L15~16 에 이미 추가함 → **using 추가 불필요.**

### C-1. 스코어 지역변수 — `string error;` (L304) 다음에 삽입
```csharp
                double dScore1, dScore2;   //quick-260812: 티칭이 이미 계산한 스코어 수신(등급 표시용)
```
### C-2. 호출 인자 — `VIEW_MODE,` 와 `out error);` **사이**에 한 줄 삽입 (기존 줄 무변경)
```csharp
                    VIEW_MODE,
                    out dScore1, out dScore2,
                    out error);
```
### C-3. 성공 분기 (L312~316) — 라벨 2줄 교체 + 3줄 삽입
편집 전:
```csharp
                if (bOk) {
                    bool bHas = EthernetVisionHandler.Handle.Matcher.HasTemplate(VIEW_MODE);
                    lbl_teachStatus.Text = TeachDiag.ToStatusLine(ETeachGrade.Good, "티칭 OK (HasTemplate=" + bHas + ")");
                    lbl_teachStatus.Foreground = TeachDiag.GradeBrush(ETeachGrade.Good);
                }
```
편집 후:
```csharp
                if (bOk) {
                    bool bHas = EthernetVisionHandler.Handle.Matcher.HasTemplate(VIEW_MODE);
                    //quick-260812: 두 패턴 중 낮은 쪽 = 보수적 지표(런타임 검사와 같은 규칙)
                    double dMinScore = Math.Min(dScore1, dScore2);
                    ETeachGrade teachGrade = TeachDiag.ClassifyScore(dMinScore, AlignShapeMatchService.TeachMinScore);
                    lbl_teachStatus.Text = TeachDiag.ToStatusLine(teachGrade, "티칭 OK (HasTemplate=" + bHas + ", score " + dMinScore.ToString("F3") + ")");
                    lbl_teachStatus.Foreground = TeachDiag.GradeBrush(teachGrade);
                }
```
> `if (bOk) {` / `bool bHas = ...` / `}` 는 무변경. 실패(`else`)·예외(`catch`) 분기는 Quick #1 상태 그대로 둔다.

***

## D. Align UI — `BottomVisionView.xaml.cs` (L442 `TeachButton_Click`)

C 와 동일 패턴. 차이: 슬롯 오버로드라 `VIEW_MODE, _selectedSlot,` 줄 다음에 인자를 삽입한다.

### D-1. `string error;` (L480) 다음에 삽입
```csharp
                double dScore1, dScore2;   //quick-260812: 티칭이 이미 계산한 스코어 수신(등급 표시용)
```
### D-2. 호출 인자 삽입 (기존 줄 무변경)
```csharp
                    VIEW_MODE, _selectedSlot, //260626 hbk 선택 슬롯 전달 (Plan 01 슬롯 오버로드)
                    out dScore1, out dScore2,
                    out error);
```
### D-3. 성공 분기 (L489~497) — 라벨 2줄 교체 + 3줄 삽입
편집 전:
```csharp
                    lbl_teachStatus.Text = TeachDiag.ToStatusLine(ETeachGrade.Good, "[" + slotLabel + "] 티칭 OK (HasTemplate=" + bHas + ")"); //260626 hbk 슬롯 라벨 포함 메시지
                    lbl_teachStatus.Foreground = TeachDiag.GradeBrush(ETeachGrade.Good);
```
편집 후:
```csharp
                    //quick-260812: 두 패턴 중 낮은 쪽 = 보수적 지표(런타임 검사와 같은 규칙)
                    double dMinScore = Math.Min(dScore1, dScore2);
                    ETeachGrade teachGrade = TeachDiag.ClassifyScore(dMinScore, AlignShapeMatchService.TeachMinScore);
                    lbl_teachStatus.Text = TeachDiag.ToStatusLine(teachGrade, "[" + slotLabel + "] 티칭 OK (HasTemplate=" + bHas + ", score " + dMinScore.ToString("F3") + ")"); //260626 hbk 슬롯 라벨 포함 메시지
                    lbl_teachStatus.Foreground = TeachDiag.GradeBrush(teachGrade);
```
> 줄 끝 `//260626 hbk ...` 주석은 **살려 둔다.** 다음 줄 `_slotRois[_selectedSlot] = ...` 는 **무변경.**

***

## E. Calib 서비스 — `PickerCenterCalibrationService.cs` (전부 **삽입**, 삭제 0)

### E-1. 읽기전용 최소점수 — "공개 속성" 영역의 `HasModel` 프로퍼티(L63~66) 다음에 삽입
```csharp

        /// <summary>quick-260812: 등급 산정용 최소 스코어(읽기 전용). FIND_MIN_SCORE 의 값·용도는 무변경.</summary>
        public static double FindMinScore {
            get { return FIND_MIN_SCORE; }
        }
```

### E-2. 시그니처 확장 — `out string error) {` **앞 줄에 삽입**
편집 전 (L214~220):
```csharp
        public bool TryAddStep(HImage img,
            double roiRow1, double roiCol1, double roiRow2, double roiCol2,
            out double foundRow, out double foundCol,
            out string error) {
            foundRow = 0.0;
            foundCol = 0.0;
            error    = null;
```
편집 후 (2줄 삽입, 기존 줄 전부 무변경):
```csharp
        public bool TryAddStep(HImage img,
            double roiRow1, double roiCol1, double roiRow2, double roiCol2,
            out double foundRow, out double foundCol,
            out double dScore,   //quick-260812: 이미 계산된 검색 점수 노출(추가 HALCON 호출 0)
            out string error) {
            foundRow = 0.0;
            foundCol = 0.0;
            dScore   = 0.0;
            error    = null;
```

### E-3. 값 복사 1줄 — `foundCol = dCol;` (L269) 다음에 삽입
```csharp
                foundRow = dRow;
                foundCol = dCol;
                dScore   = score[0].D;   //quick-260812: finally 의 Dispose 전에 값만 복사
```
> 검색 호출 블록(L238~255, `GenRectangle1` / `ReduceDomain` / 실험 3줄 / 모델검색 인자 전체)은
> **읽기만 하고 절대 열지 않는다.** 사용자의 ±5° 실험이 그 안에 있다.
> `score[0].D` 는 L275 로그가 이미 읽고 있는 값이다 — 새 연산이 아니다.

***

## F. Calib TCP 호출부 — `WPF_Example/Custom/SystemHandler.cs` (`ProcessAlignCalib`, Allman)

### F-1. using 별칭 2줄 — `using System.Diagnostics;` (L13) 다음에 삽입
```csharp
using TeachDiag   = ReringProject.Halcon.Algorithms.TeachDiagnostics;   //quick-260812: 표시 전용 헬퍼(별칭 = 이름충돌 회피)
using ETeachGrade = ReringProject.Halcon.Algorithms.ETeachGrade;
```
> 이 파일에는 `using ReringProject.Halcon.Algorithms;` 가 없다. 통째 using 대신 **별칭**을 쓴다(Quick #1 선례, 충돌 불가능).

### F-2. 스코어 변수 + 인자 (L741~745)
편집 전:
```csharp
                    double dFoundRow, dFoundCol;
                    string error;
                    bool bOk = EthernetVisionHandler.Handle.PickerCal.TryAddStep(
                        img, dSearchRow1, dSearchCol1, dSearchRow2, dSearchCol2,
                        out dFoundRow, out dFoundCol, out error);
```
편집 후:
```csharp
                    double dFoundRow, dFoundCol;
                    double dScore;   //quick-260812: 이미 계산된 검색 점수 수신(등급 로그용)
                    string error;
                    bool bOk = EthernetVisionHandler.Handle.PickerCal.TryAddStep(
                        img, dSearchRow1, dSearchCol1, dSearchRow2, dSearchCol2,
                        out dFoundRow, out dFoundCol, out dScore, out error);
```

### F-3. 성공 로그 (L759~760) — `Logging.PrintLog((int)ELogType.Trace,` 줄은 **그대로 두고** 포맷/인자 줄만 교체
편집 전:
```csharp
                        Logging.PrintLog((int)ELogType.Trace,
                            "[ALIGN_CALIB] STEP {0} OK", resultPacket.StepNo);
```
편집 후:
```csharp
                        //quick-260812: 이번 Quick 은 로그까지. 화면 노출은 Quick #3(TCP 자동경로) 범위.
                        ETeachGrade calGrade = TeachDiag.ClassifyScore(dScore, PickerCenterCalibrationService.FindMinScore);
                        Logging.PrintLog((int)ELogType.Trace,
                            "[ALIGN_CALIB] STEP {0} OK score={1:F3} grade={2}", resultPacket.StepNo, dScore, calGrade);
```
> `resultPacket.StepNo`/`IsPass` 대입, 뷰어 콜백 블록, `else` 실패 로그, `finally`(img.Dispose) 는 **무변경.**

***

## G. Calib 수동 UI 호출부 — `BottomVisionView.xaml.cs` (피커캘 [스텝 추가] 핸들러)

### G-1. 스코어 변수 + 인자 (L829~835)
편집 전:
```csharp
                double foundRow, foundCol;
                string error;
                bool bOk = EthernetVisionHandler.Handle.PickerCal.TryAddStep(
                    img,
                    _calRoiRect.Row1, _calRoiRect.Column1,
                    _calRoiRect.Row2, _calRoiRect.Column2,
                    out foundRow, out foundCol, out error);
```
편집 후:
```csharp
                double foundRow, foundCol;
                double calScore;   //quick-260812: 이미 계산된 검색 점수 수신(등급 표시용)
                string error;
                bool bOk = EthernetVisionHandler.Handle.PickerCal.TryAddStep(
                    img,
                    _calRoiRect.Row1, _calRoiRect.Column1,
                    _calRoiRect.Row2, _calRoiRect.Column2,
                    out foundRow, out foundCol, out calScore, out error);
```

### G-2. 스텝 성공 라벨 (L842) — **문구만, 색은 건드리지 않는다**
편집 전:
```csharp
                    lbl_calStatus.Text = "누적 " + stepCount + "  last=(" + foundRow.ToString("F1") + "," + foundCol.ToString("F1") + ")";
```
편집 후:
```csharp
                    //quick-260812: lbl_calStatus 는 이 파일에 36곳 대입 — 여기만 색을 칠하면 stale 색이 남는다. 기호만 붙인다.
                    ETeachGrade calGrade = TeachDiag.ClassifyScore(calScore, PickerCenterCalibrationService.FindMinScore);
                    lbl_calStatus.Text = TeachDiag.ToStatusLine(calGrade, "누적 " + stepCount + "  last=(" + foundRow.ToString("F1") + "," + foundCol.ToString("F1") + ")  score " + calScore.ToString("F3"));
```
> `#if SIMUL_MODE` 자동 다음이미지 블록, `_viewer.SetAlignContourXld` 블록, `else` 실패 라벨은 **무변경.**

***

## H. 빌드 (산출물 잠김 대비)

`bin/x64/Debug/DatumMeasurement.exe` 가 실행 중이면 잠긴다. **프로세스를 절대 죽이지 않는다.**
정상 빌드를 먼저 시도하고, MSB3021/3026/3027/3030 이 나면 스크래치 OutDir 컴파일로 대체한다:

```
"/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" \
  "WPF_Example/DatumMeasurement.csproj" //p:Configuration=Debug //p:Platform=x64 \
  //p:OutputPath="$TEMP/gsd-na0-scratch/bin/" //p:BaseIntermediateOutputPath="$TEMP/gsd-na0-scratch/obj/" \
  //v:minimal //nologo
```

## I. 스냅샷 경로 (Picker 검증·복원용)

bash 파일 조작에는 `$TEMP`(백슬래시 포함 가능) 대신 **`/tmp/gsd-na0`** 를 쓴다. MSBuild `//p:OutputPath` 에만 `$TEMP` 를 쓴다.
```
/tmp/gsd-na0/picker-base.cs     # Task 1 시작 시점(= 실험만 얹힌 상태) 스냅샷
/tmp/gsd-na0/experiment.patch   # 같은 시점의 git diff (실험 hunk)
/tmp/gsd-na0/picker-full.cs     # Task 3 편집 후(실험+우리변경) 백업
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 스냅샷 확보 + Datum 완료 모달 등급 배선 (rs / rs2 재사용)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <action>
**(A) Step 0 — 편집 전에 Picker 스냅샷을 먼저 뜬다 (이 순서를 어기면 Task 3 검증이 무의미해진다)**

```bash
cd "C:/Info/Project/DataMeasurement"
mkdir -p /tmp/gsd-na0
cp WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs /tmp/gsd-na0/picker-base.cs
git diff -- WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs > /tmp/gsd-na0/experiment.patch
grep -c 'TupleRad(5, out htRad)' /tmp/gsd-na0/picker-base.cs      # 기대 1
grep -c 'TupleRad(5, out htRad)' /tmp/gsd-na0/experiment.patch    # 기대 1
```
둘 중 하나라도 0 이면 **중단하고 보고**한다(실험이 사라졌거나 이미 커밋된 상태 = 전제 붕괴).

**(B) `MainView.xaml.cs` 편집 — `<interfaces>` 섹션 **A** 표 그대로 2지점**

- A-1: 패턴 2 성공 문구 → `grade2` 산출 + `ToStatusLine(grade2, ...)`. `"\n"` 은 ToStatusLine 밖.
- A-2: 완료 모달 본문 첫 줄 → `ToStatusLine(grade1, ...)`. 그 앞에 `grade1` 산출 2줄 삽입.

**(C) 절대 하지 말 것**
- `ShowConfirmation(` 호출줄 / `"모델 생성 완료",` 줄 / `MessageBoxButton.YesNo);` 줄 편집 금지.
- `svc.TryFindPose(` / `svc.TryCreateModel(` 호출과 인자, `datum.RefMatch*` 대입, `if`/`else`/`return`,
  `EnsurePerRoiDefaults()`, `SaveRecipe()` — **한 글자도** 금지.
- **새로 쓰는 주석에도 위 토큰을 넣지 말 것** — 하드제약 grep 은 diff 줄 텍스트를 검사하므로 주석이 검사를 깨뜨린다.
- `PatternMinScore` 가 0 일까 봐 가드(`if`)를 추가하지 말 것 — 메서드 상단 기본값 복원이 이미 보장한다(조사 ③).
- 새 `using` 금지(L12 에 이미 있음). 삼항 `?:` 금지.
- `InvokeCreatePatternModel` 메서드 밖 편집 금지. Picker/Align/PatternMatchService 파일은 이번 Task 에서 **열지 않는다.**

**(D) 커밋 — 경로를 명시해서 add 한다 (`git add -A` 금지: Picker 실험이 딸려 들어간다)**
```bash
git add WPF_Example/UI/ContentItem/MainView.xaml.cs
git commit -m "feat(quick-260812-na0): Datum 티칭 완료 모달에 점수 등급 표시 (rs/rs2 재사용, 새 HALCON 호출 0)"
```
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && M=WPF_Example/UI/ContentItem/MainView.xaml.cs; P=WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs; echo "=== [사전] 기대 1/1: 스냅샷에 실험이 담겼는가 ===" && grep -c 'TupleRad(5, out htRad)' /tmp/gsd-na0/picker-base.cs; grep -c 'TupleRad(5, out htRad)' /tmp/gsd-na0/experiment.patch; echo "=== [하드제약 1] 기대 0: 추가/삭제줄에 판정·HALCON 심볼 없음 ===" && git diff -U0 HEAD~1 -- $M | grep -E '^[+-][^+-]' | grep -E 'HOperatorSet\.|FindShapeModel\(|FindNccModel\(|svc\.|TryCreateModel\(|TryFindPose\(|TryFindRefPose\(|EnsurePerRoiDefaults|datum\.RefMatch|SaveRecipe|ShowConfirmation\(|MessageBoxButton|\breturn\b|\bif *\(|\belse\b' | wc -l; echo "=== [하드제약 5] 기대 0: 추가줄이 화이트리스트 밖으로 새지 않음 ===" && git diff -U0 HEAD~1 -- $M | grep -E '^\+[^+]' | grep -vE 'ETeachGrade grade[12] = TeachDiagnostics\.ClassifyScore|alignMsg = |TeachDiagnostics\.ToStatusLine\(grade1, |//quick-260812' | wc -l; echo "=== [배선] 기대 각 1 ===" && grep -cF 'ETeachGrade grade1 = TeachDiagnostics.ClassifyScore(rs, datum.PatternMinScore);' $M; grep -cF 'ETeachGrade grade2 = TeachDiagnostics.ClassifyScore(rs2, datum.PatternMinScore);' $M; grep -cF 'TeachDiagnostics.ToStatusLine(grade1, "패턴 모델 생성' $M; grep -cF 'TeachDiagnostics.ToStatusLine(grade2, "패턴 2 모델 생성' $M; echo "=== [배선] 기대 2: 이 파일의 ClassifyScore 는 전부 이번 Quick 저자(사전 호출부 0건) ===" && grep -c 'ClassifyScore' $M; echo "=== [원형 소멸] 기대 0: 등급 없는 옛 본문 ===" && grep -cF '")" + alignMsg' $M; echo "=== [원형 대체] 기대 1 ===" && grep -cF '")") + alignMsg' $M; echo "=== [컨벤션] 기대 0: 추가줄 삼항 ===" && git diff HEAD~1 -- $M | grep -E '^\+[^+]' | grep -c ' ? '; echo "=== [무변경] 기대 0줄: 서비스/헬퍼 파일 ===" && git status --porcelain -- WPF_Example/Halcon/Algorithms/PatternMatchService.cs WPF_Example/Halcon/Algorithms/TeachDiagnostics.cs WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs | wc -l; echo "=== [Picker 불가침] 기대 SAME: 워킹트리 diff 가 스냅샷 시점 패치와 동일 ===" && git diff -- $P > /tmp/gsd-na0/picker-now.patch && (diff -q /tmp/gsd-na0/experiment.patch /tmp/gsd-na0/picker-now.patch && echo SAME || echo CHANGED); echo "=== [범위] 커밋에 Picker 없음 ===" && git show --stat --name-only HEAD | tail -5; echo "=== 빌드 (잠기면 스크래치 폴백) ===" && ((set -o pipefail; "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" //p:Configuration=Debug //p:Platform=x64 //v:minimal //nologo 2>&1 | grep -iE "error|warning CS|Build succeeded" | head -25) || "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" //p:Configuration=Debug //p:Platform=x64 //p:OutputPath="$TEMP/gsd-na0-scratch/bin/" //p:BaseIntermediateOutputPath="$TEMP/gsd-na0-scratch/obj/" //v:minimal //nologo 2>&1 | grep -iE "error CS|warning CS|Build succeeded" | head -25)</automated>
  </verify>
  <done>
- `/tmp/gsd-na0/picker-base.cs` + `experiment.patch` 존재하고 둘 다 `TupleRad(5, out htRad)` 1건 포함 = 실험 스냅샷 확보.
- **[하드제약 1]** MainView diff 의 추가/삭제줄에 판정·HALCON 심볼(`svc.` / `TryFindPose(` / `datum.RefMatch` / `return` / `if (` / `else` / `ShowConfirmation(` 등) **0건**. (토큰은 `datum\.RefMatch` 로 한정 — 편집 대상 줄의 한국어 UI 문구 "RefMatch2 기록" 자체는 코드 심볼이 아니므로 검사 대상에서 정당하게 제외)
- **[하드제약 5]** 추가줄이 `ETeachGrade grade1|2 = ...ClassifyScore` / `alignMsg = ` / `ToStatusLine(grade1, ` / `//quick-260812` 밖으로 새는 것 **0건**.
- `ClassifyScore(rs, datum.PatternMinScore)` 1건 + `ClassifyScore(rs2, datum.PatternMinScore)` 1건, 파일 전체 `ClassifyScore` **2건**.
- 옛 본문 형태 `")" + alignMsg` **0건**, 새 형태 `")") + alignMsg` **1건**.
- 추가줄 삼항 0건. `PatternMatchService.cs` / `TeachDiagnostics.cs` / `AlignShapeMatchService.cs` `git status` **0줄**.
- Picker 워킹트리 diff 가 스냅샷 패치와 **동일(SAME)** — 이번 Task 에서 건드리지 않았음.
- 커밋에 `MainView.xaml.cs` 만 포함(Picker 없음). Debug/x64 신규 `error CS` 0 / 신규 `warning CS` 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Align 스코어 노출 오버로드 2개 + Tray/Bottom 성공 라벨 등급 (서비스 diff 삭제줄 0)</name>
  <files>WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs, WPF_Example/Custom/UI/TrayVisionView.xaml.cs, WPF_Example/Custom/UI/BottomVisionView.xaml.cs</files>
  <action>
`<interfaces>` 섹션 **B / C / D** 를 앵커로 삼아 편집한다.

**(A) `AlignShapeMatchService.cs` — 오직 삽입만 (이 파일의 diff 삭제줄은 0 이어야 한다)**

- B-1: `MIN_SCORE` 상수 아래 `public static double TeachMinScore { get { return MIN_SCORE; } }` 삽입.
  `private const double MIN_SCORE = 0.5;` 줄 자체를 `public` 으로 **바꾸지 말 것**(삭제줄 발생).
- B-2: `private readonly PatternMatchService _matcher;` 아래 캡처 필드 2개 삽입.
- B-3: 코어 TryTeach 안, `if (!bRef1) { ... }` 와 `if (!bRef2) { ... }` **가드의 닫는 `}` 바로 다음 줄**
  (그 아래 빈 줄보다 위)에 캡처 대입 1줄씩 삽입.
  **가드 블록 안에 넣으면 실패 경로에서도 기록돼 stale 이 샌다** — verify 가 "직전 2줄에 `return false;` 존재"로 검사한다.
- B-4: 코어 TryTeach 닫는 `}` 다음, `// ─── 런타임 위치보정 ───` 주석 앞에 신규 오버로드 2개 삽입.

> 기존 오버로드 2개의 **시그니처·본문 판정로직은 한 글자도 바꾸지 않는다.**
> 코어 본문을 신규 오버로드로 **옮기지 않는다**(100줄 이동 = 전사 오류 위험 + 삭제줄 발생).
> `_matcher.` 로 시작하는 호출을 **새로 만들지 않는다** — 신규 오버로드는 `TryTeach` 위임만 한다.
> 새 주석에 `_matcher.` / `HOperatorSet.` / `TryFindRefPose(` 같은 토큰을 적지 말 것(검사 오탐 유발).

**(B) `TrayVisionView.xaml.cs` — C-1/C-2/C-3**

`out error);` **앞 줄에 인자 한 줄을 삽입**한다. 기존 `Matcher.TryTeach(` 호출줄과 `VIEW_MODE,` 줄은 손대지 않는다.
성공 분기의 라벨 2줄만 등급 변수 기반으로 교체하고, 실패/예외 분기(Quick #1 결과)는 그대로 둔다.

**(C) `BottomVisionView.xaml.cs` — D-1/D-2/D-3 (Align 티칭 부분만)**

이번 Task 에서는 **피커캘 패널(L780~865)을 열지 않는다** — 그건 Task 3 이다.
`_slotRois[_selectedSlot] = ...` 줄과 줄 끝 `//260626 hbk` 주석은 **보존.**

**(D) 공통 금지사항 — 하드 제약**
- `HOperatorSet.` / `FindShapeModel(` / `FindNccModel(` / `_matcher.TryCreateModel(` / `_matcher.TryFindRefPose(` /
  `Matcher.Run(` / `.Grab(` 를 **새로 추가하지 않는다.** 점수는 전부 이미 계산된 값의 재활용이다.
- `MIN_SCORE` 값(0.5)을 UI 파일에 복사해 적지 않는다 — 반드시 `AlignShapeMatchService.TeachMinScore` 참조.
- 삼항 `?:` 금지. C# 7.2. `Math.Min` 은 한 번만 계산해 `dMinScore` 지역변수로 재사용.
- `lbl_teachStatus.Text` 를 바꾸는 지점은 **반드시 같은 곳에서 `Foreground` 도 함께 지정**(Quick #1 규칙 유지).
- `PickerCenterCalibrationService.cs` 를 **열지 않는다.**

**(E) 커밋 (경로 명시)**
```bash
git add WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs WPF_Example/Custom/UI/TrayVisionView.xaml.cs WPF_Example/Custom/UI/BottomVisionView.xaml.cs
git commit -m "feat(quick-260812-na0): Align 티칭 스코어 노출 오버로드 + 성공 라벨 등급 (서비스 순수 추가, 새 HALCON 호출 0)"
```
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && A=WPF_Example/Custom/EthernetVision/AlignShapeMatchService.cs; T=WPF_Example/Custom/UI/TrayVisionView.xaml.cs; B=WPF_Example/Custom/UI/BottomVisionView.xaml.cs; P=WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs; echo "=== [하드제약 2] 기대 0: Align 서비스 삭제줄 (=기존 오버로드/코어 바이트 동일 증명) ===" && git diff HEAD~1 -- $A | grep -cE '^-[^-]'; echo "=== [하드제약 1a] 기대 0: Align 서비스 추가줄에 HALCON/매처 호출 ===" && git diff HEAD~1 -- $A | grep -E '^\+[^+]' | grep -cE 'HOperatorSet\.|FindShapeModel\(|FindNccModel\(|_matcher\.'; echo "=== [하드제약 1b] 기대 0: UI 2파일 추가줄에 HALCON/매칭 호출 ===" && git diff -U0 HEAD~1 -- $T $B | grep -E '^\+[^+]' | grep -cE 'HOperatorSet\.|FindShapeModel\(|FindNccModel\(|Matcher\.Run\(|TryCreateModel\(|TryFindPose\(|TryFindRefPose\(|TryTeachModel\(|\.Grab\('; echo "=== [하드제약 5a] 기대 0: UI 추가줄 화이트리스트 이탈 ===" && git diff -U0 HEAD~1 -- $T $B | grep -E '^\+[^+]' | grep -vE 'double dScore1, dScore2;|out dScore1, out dScore2,|double dMinScore = Math\.Min\(dScore1, dScore2\);|ETeachGrade teachGrade = TeachDiag\.ClassifyScore|lbl_teachStatus\.(Text|Foreground)|//quick-260812' | wc -l; echo "=== [하드제약 5b] 기대 0: UI 삭제줄은 lbl_teachStatus 대입뿐 ===" && git diff -U0 HEAD~1 -- $T $B | grep -E '^-[^-]' | grep -vcE 'lbl_teachStatus\.(Text|Foreground)'; echo "=== [오버로드] 기대 4 = 기존2+신규2 (이 선언 패턴은 이 파일에만 존재) ===" && grep -c 'public bool TryTeach(' $A; echo "=== [오버로드] 기대 각 1: 기존 2개 시그니처 앵커 생존 ===" && grep -cF 'EBottomAlignSlot slot,   //260626 hbk 슬롯 파라미터 (mode 다음, out error 앞)' $A; grep -cF 'mode, EBottomAlignSlot.None, out error); //260626 hbk 기존 호출자 하위호환' $A; echo "=== [캡처] 기대 각 1 ===" && grep -cF '_lastTeachScore1 = r1Score;' $A; grep -cF '_lastTeachScore2 = r2Score;' $A; grep -cF 'public static double TeachMinScore' $A; echo "=== [캡처 위치] 기대 각 1: 가드의 닫는 } 바로 다음(성공 경로)인가 — 직전 2줄에 return false; 존재 ===" && grep -B2 -F '_lastTeachScore1 = r1Score;' $A | grep -c 'return false;'; grep -B2 -F '_lastTeachScore2 = r2Score;' $A | grep -c 'return false;'; echo "=== [UI 배선] 기대 각 1 ===" && grep -cF 'TeachDiag.ClassifyScore(dMinScore, AlignShapeMatchService.TeachMinScore)' $T; grep -cF 'TeachDiag.ClassifyScore(dMinScore, AlignShapeMatchService.TeachMinScore)' $B; grep -cF 'out dScore1, out dScore2,' $T; grep -cF 'out dScore1, out dScore2,' $B; echo "=== [매직넘버] 기대 0: UI 에 0.5 복사 없음 ===" && git diff HEAD~1 -- $T $B | grep -E '^\+[^+]' | grep -c '0\.5'; echo "=== [stale 색] 기대 동수: Tray 6/6, Bottom 9/9 ===" && grep -c 'lbl_teachStatus.Text' $T; grep -c 'lbl_teachStatus.Foreground' $T; grep -c 'lbl_teachStatus.Text' $B; grep -c 'lbl_teachStatus.Foreground' $B; echo "=== [컨벤션] 기대 0: 추가줄 삼항 ===" && git diff HEAD~1 -- $A $T $B | grep -E '^\+[^+]' | grep -c ' ? '; echo "=== [무변경] 기대 0줄 ===" && git status --porcelain -- WPF_Example/Halcon/Algorithms/PatternMatchService.cs WPF_Example/Halcon/Algorithms/TeachDiagnostics.cs | wc -l; echo "=== [Picker 불가침] 기대 SAME ===" && git diff -- $P > /tmp/gsd-na0/picker-now.patch && (diff -q /tmp/gsd-na0/experiment.patch /tmp/gsd-na0/picker-now.patch && echo SAME || echo CHANGED); echo "=== 빌드 (잠기면 스크래치 폴백) ===" && ((set -o pipefail; "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" //p:Configuration=Debug //p:Platform=x64 //v:minimal //nologo 2>&1 | grep -iE "error|warning CS|Build succeeded" | head -25) || "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" //p:Configuration=Debug //p:Platform=x64 //p:OutputPath="$TEMP/gsd-na0-scratch/bin/" //p:BaseIntermediateOutputPath="$TEMP/gsd-na0-scratch/obj/" //v:minimal //nologo 2>&1 | grep -iE "error CS|warning CS|Build succeeded" | head -25)</automated>
  </verify>
  <done>
- **[하드제약 2]** `AlignShapeMatchService.cs` diff 삭제줄 **0건** → 기존 오버로드 2개 시그니처와 코어 판정/제어흐름이 바이트 단위 동일.
- **[하드제약 1]** Align 서비스 추가줄에 `HOperatorSet.`/`FindShapeModel(`/`_matcher.` **0건**, UI 2파일 추가줄에 HALCON·매칭 호출 **0건**.
- **[하드제약 5]** UI 추가줄 화이트리스트 이탈 0건, UI 삭제줄은 전부 `lbl_teachStatus.Text|Foreground` 대입.
- `public bool TryTeach(` **4건**(기존 2 + 신규 2, 파라미터 개수 전부 달라 모호성 없음), 기존 2개 시그니처 앵커 각 1건 생존.
- 캡처 `_lastTeachScore1 = r1Score;` / `_lastTeachScore2 = r2Score;` 각 1건이며, **직전 2줄에 `return false;` 가 존재** = 가드 블록 밖(성공 경로)에 정확히 위치.
- Tray/Bottom 각각 `ClassifyScore(dMinScore, AlignShapeMatchService.TeachMinScore)` 1건 + `out dScore1, out dScore2,` 1건. UI 추가줄에 `0.5` 매직넘버 0건.
- `lbl_teachStatus` Text/Foreground 대입 수 동일(Tray 6/6, Bottom 9/9) — Quick #1 의 stale-색 보증 유지.
- 추가줄 삼항 0건. `PatternMatchService.cs`/`TeachDiagnostics.cs` `git status` 0줄. Picker diff 는 스냅샷과 **SAME**.
- Debug/x64 신규 `error CS` 0 / 신규 `warning CS` 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Calib out double dScore 노출 + TCP 로그/수동 UI 등급 (실험 무변경·미커밋 보존)</name>
  <files>WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs, WPF_Example/Custom/SystemHandler.cs, WPF_Example/Custom/UI/BottomVisionView.xaml.cs</files>
  <action>
`<interfaces>` 섹션 **E / F / G** 를 앵커로 삼아 편집한다. **이 Task 는 사용자 실험이 얹힌 파일을 다룬다 — 절차를 순서대로 지킨다.**

**(A) `PickerCenterCalibrationService.cs` — 오직 삽입만 (스냅샷 대비 삭제줄 0)**

- E-1: `HasModel` 프로퍼티 다음에 `public static double FindMinScore` 삽입.
  L33 `private const double FIND_MIN_SCORE   = 0.5;` 줄은 **손대지 않는다**(public 으로 바꾸지 말 것).
- E-2: `out string error) {` **앞 줄**에 `out double dScore,` 삽입 + `foundCol = 0.0;` 다음에 `dScore   = 0.0;` 삽입.
- E-3: `foundCol = dCol;` 다음에 `dScore   = score[0].D;` 삽입.

> 🚫 **L238~255 구역(사각형 ROI 생성 / 도메인 축소 / 실험 3줄 / 모델검색 호출 인자 전체)은
> 읽기만 하고 단 한 글자도 바꾸지 않는다.** 사용자의 ±5° 실험이 그 안에 있다.
> `score` 는 `finally` 에서 Dispose 되므로 **반드시 try 블록 안, 로그보다 앞**에서 값을 복사한다.

**(B) `Custom/SystemHandler.cs` — F-1/F-2/F-3 (Allman 스타일 유지)**

using 별칭 2줄 추가 → `double dScore;` 선언 추가 → 호출 인자에 `out dScore,` 추가 →
성공 로그의 **포맷/인자 줄만** 교체(그 위 `Logging.PrintLog((int)ELogType.Trace,` 줄은 그대로).
`resultPacket.StepNo`/`IsPass`, 뷰어 콜백, `else` 실패 로그, `finally`(img.Dispose) 는 **무변경.**

**(C) `BottomVisionView.xaml.cs` — G-1/G-2 (피커캘 패널만)**

`double calScore;` 선언 추가 → 호출 인자에 `out calScore,` 추가 → 스텝 성공 라벨을 `ToStatusLine` 으로 감싼다.
**`lbl_calStatus.Foreground` 는 절대 건드리지 않는다**(이 파일에 `lbl_calStatus.Text` 대입 36곳 — 일부만 칠하면 stale 색).
`#if SIMUL_MODE` 자동 다음이미지 블록, `SetAlignContourXld` 블록, `else` 실패 라벨은 무변경.
Task 2 에서 만진 Align 티칭 구역(L442~508)은 이번에 다시 열지 않는다.

**(D) 빌드 검증 — 커밋 전에 먼저 통과시킨다** (섹션 H)

**(E) 커밋 절차 — 사용자 실험을 커밋에 싣지 않으면서 워킹트리에는 남긴다**

±5° 탐색범위 실험은 **검증되지 않은 동작 변경**이라 우리 커밋에 실려 main 에 나가면 안 된다.
동시에 되돌려서도 안 된다. 그래서 아래 순서를 정확히 따른다:

**모든 단계는 실패 시 즉시 exit 하는 실제 게이트다 — 산문 설명이 아니라 아래 스크립트 자체가 중단 경로를 강제한다.**

```bash
cd "C:/Info/Project/DataMeasurement"
PK=WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs

# 0) 백업 존재 가드 — 스냅샷/패치가 없으면 이후 검사가 무데이터로 조용히 PASS 할 수 있으므로 먼저 확인
[ -f /tmp/gsd-na0/picker-base.cs ] && [ -f /tmp/gsd-na0/experiment.patch ] \
  || { echo "ABORT_BACKUP_MISSING"; exit 1; }

# 1) 현재 상태(실험 + 우리 변경) 백업 — 이후 복원의 원본
cp "$PK" /tmp/gsd-na0/picker-full.cs

# 2) 실험 hunk만 되돌린다 → 파일 = HEAD + 우리 변경 (실패 시 즉시 복원 후 중단)
git apply -R /tmp/gsd-na0/experiment.patch || git apply -R -3 /tmp/gsd-na0/experiment.patch \
  || { cp /tmp/gsd-na0/picker-full.cs "$PK"; echo "ABORT_APPLY_FAILED"; exit 1; }

# 3) 되돌림 확인 — 실패 시 즉시 복원 후 중단(grep -c 는 게이트가 아니므로 grep -q 로 승격)
if grep -q 'TupleRad(5, out htRad)' "$PK"; then
    cp /tmp/gsd-na0/picker-full.cs "$PK"; echo "ABORT_EXPERIMENT_STILL_PRESENT"; exit 1
fi
grep -q 'out double dScore,' "$PK" \
  || { cp /tmp/gsd-na0/picker-full.cs "$PK"; echo "ABORT_OUR_CHANGE_MISSING"; exit 1; }

# 3.5) 실제 커밋될 조합(HEAD + 우리 변경, 실험 없음)이 컴파일되는지 확인 — 이전엔 이 조합이 한 번도
#      빌드되지 않았다(실험 포함 상태 또는 실험 복원 후 상태만 빌드됨). 정상 경로 잠겨있으면 스크래치로.
("/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" \
    "WPF_Example/DatumMeasurement.csproj" //p:Configuration=Debug //p:Platform=x64 //v:minimal //nologo \
    2>&1 | tee /tmp/gsd-na0/prebuild.log | grep -iE "error|Build succeeded"
  rc=${PIPESTATUS[0]}
  if [ "$rc" -ne 0 ] || grep -qE "error CS" /tmp/gsd-na0/prebuild.log; then
    "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" \
      "WPF_Example/DatumMeasurement.csproj" //p:Configuration=Debug //p:Platform=x64 \
      //p:OutputPath="$TEMP/gsd-na0-scratch/bin/" //p:BaseIntermediateOutputPath="$TEMP/gsd-na0-scratch/obj/" \
      //v:minimal //nologo 2>&1 | tee /tmp/gsd-na0/prebuild-scratch.log | grep -iE "error CS|Build succeeded"
  fi) \
  || true
grep -qE "error CS" /tmp/gsd-na0/prebuild.log /tmp/gsd-na0/prebuild-scratch.log 2>/dev/null \
  && { cp /tmp/gsd-na0/picker-full.cs "$PK"; echo "ABORT_PREBUILD_FAILED"; exit 1; }

# 4) 커밋 (경로 명시 — git add -A/. 금지)
git add "$PK" WPF_Example/Custom/SystemHandler.cs WPF_Example/Custom/UI/BottomVisionView.xaml.cs
git commit -m "feat(quick-260812-na0): 피커캘 TryAddStep out dScore 노출 + 스텝 등급 로그/라벨 (실험 미포함)"

# 5) 실험 복원 — 워킹트리를 편집 직후 상태로 되돌린다
cp /tmp/gsd-na0/picker-full.cs "$PK"
diff /tmp/gsd-na0/picker-full.cs "$PK" && echo RESTORE_OK
```

> 위 스크립트의 `exit 1` 경로 중 어느 것이든 밟으면: 커밋은 이미 일어나지 않은 상태이고, `$PK`는 이미 `picker-full.cs`(실험+우리변경)로 복원돼 있다.
> **그 시점에서 즉시 작업을 중단하고 사용자에게 어떤 ABORT_* 코드가 나왔는지 보고한다.**
> 파일 원본은 `/tmp/gsd-na0/picker-full.cs` 와 `/tmp/gsd-na0/picker-base.cs` 에 2중 백업돼 있다.
> **사용자 실험을 삭제·수정·커밋하는 어떤 우회도 시도하지 않는다.**

**(F) 절대 하지 말 것**
- `git add -A` / `git add .` / `git commit -a` — 실험이 딸려 들어간다. 항상 경로 명시.
- `git checkout` / `git restore` / `git stash` 를 Picker 파일에 사용 — 실험 소실 위험.
- 빌드 산출물이 잠겨도 **프로세스 종료 금지** — 스크래치 OutDir 컴파일로 대체.
- 삼항 `?:`, C# 8.0+ 문법, `FIND_MIN_SCORE` 값(0.5) 복사.
- 새 주석에 `htRad` / `TupleRad` / `FindShapeModel` / `HOperatorSet.` 토큰 넣기(검사 오탐 유발).
  </action>
  <verify>
    <automated>cd "C:/Info/Project/DataMeasurement" && PK=WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs; S=WPF_Example/Custom/SystemHandler.cs; B=WPF_Example/Custom/UI/BottomVisionView.xaml.cs; echo "=== [하드제약 3a] 기대 0: Picker 스냅샷 대비 삭제줄 (=실험+모델검색 인자 바이트 동일) ===" && diff -u /tmp/gsd-na0/picker-base.cs "$PK" | grep -cE '^-[^-]'; echo "=== [하드제약 3b] 기대 0: Picker 추가줄에 HALCON/실험 심볼 ===" && diff -u /tmp/gsd-na0/picker-base.cs "$PK" | grep -E '^\+[^+]' | grep -cE 'HOperatorSet\.|FindShapeModel\(|htRad|TupleRad|Math\.PI'; echo "=== [하드제약 5c] 기대 0: Picker 추가줄 화이트리스트 이탈 ===" && diff -u /tmp/gsd-na0/picker-base.cs "$PK" | grep -E '^\+[^+]' | grep -vE 'out double dScore,|dScore   = 0\.0;|dScore   = score\[0\]\.D;|public static double FindMinScore|get \{ return FIND_MIN_SCORE; \}|^\+\s*\}$|///|//quick-260812' | wc -l; echo "=== [하드제약 4a] 기대 0 / 1: 실험은 커밋 밖, 우리 변경은 커밋 안 ===" && git show HEAD:"$PK" | grep -c 'TupleRad(5, out htRad)'; git show HEAD:"$PK" | grep -c 'out double dScore,'; echo "=== [하드제약 4b] 기대 1 / 0: 워킹트리 diff = 실험만 ===" && git diff -- "$PK" | grep -c 'TupleRad(5, out htRad)'; git diff -- "$PK" | grep -c 'dScore'; echo "=== [하드제약 4c] 기대 RESTORE_OK: 복원본이 편집직후 백업과 바이트 동일 ===" && (diff -q /tmp/gsd-na0/picker-full.cs "$PK" && echo RESTORE_OK || echo RESTORE_FAIL); echo "=== [Picker 배선] 기대 각 1 ===" && grep -cF 'out double dScore,' "$PK"; grep -cF 'dScore   = score[0].D;' "$PK"; grep -cF 'public static double FindMinScore' "$PK"; echo "=== [Picker 원형 보존] 기대 각 1 ===" && grep -cF 'private const double FIND_MIN_SCORE   = 0.5;' "$PK"; grep -cF 'HOperatorSet.TupleRad(5, out htRad);' "$PK"; grep -cF '"[PICKER_CAL] step {0}: center=({1:F2},{2:F2}) score={3:F3}"' "$PK"; echo "=== [하드제약 1c] 기대 0: 호출부 2파일 추가줄에 HALCON/캘 호출 ===" && git diff -U0 HEAD~1 -- $S $B | grep -E '^\+[^+]' | grep -cE 'HOperatorSet\.|FindShapeModel\(|\.Grab\(|TryLoadModel\(|TryTeachModel\(|TryComputePickerCenter\('; echo "=== [하드제약 5d] 기대 0: 호출부 추가줄 화이트리스트 이탈 ===" && git diff -U0 HEAD~1 -- $S $B | grep -E '^\+[^+]' | grep -vE '^\+using |double dScore;|double calScore;|out dFoundRow, out dFoundCol, out dScore, out error\);|out foundRow, out foundCol, out calScore, out error\);|ETeachGrade calGrade = TeachDiag\.ClassifyScore|"\[ALIGN_CALIB\] STEP \{0\} OK score=|lbl_calStatus\.Text = TeachDiag\.ToStatusLine|//quick-260812' | wc -l; echo "=== [TCP 로그] 기대 1 / 0 ===" && grep -cF '"[ALIGN_CALIB] STEP {0} OK score={1:F3} grade={2}"' $S; grep -cF '"[ALIGN_CALIB] STEP {0} OK", resultPacket.StepNo);' $S; echo "=== [UI 라벨] 기대 1: 등급 문구 ===" && grep -cF 'lbl_calStatus.Text = TeachDiag.ToStatusLine(calGrade,' $B; echo "=== [stale 색 회피] 기대 0: lbl_calStatus 색은 건드리지 않음 ===" && grep -c 'lbl_calStatus.Foreground' $B; echo "=== [배선 합계] 기대 1 / 2: SystemHandler 1건, Bottom 2건(Align+Calib) ===" && grep -c 'ClassifyScore' $S; grep -c 'ClassifyScore' $B; echo "=== [매직넘버] 기대 0 ===" && git diff HEAD~1 -- $S $B | grep -E '^\+[^+]' | grep -c '0\.5'; echo "=== [컨벤션] 기대 0: 추가줄 삼항 ===" && (diff -u /tmp/gsd-na0/picker-base.cs "$PK"; git diff HEAD~1 -- $S $B) | grep -E '^\+[^+]' | grep -c ' ? '; echo "=== [무변경] 기대 0줄 ===" && git status --porcelain -- WPF_Example/Halcon/Algorithms/PatternMatchService.cs WPF_Example/Halcon/Algorithms/TeachDiagnostics.cs | wc -l; echo "=== [최종 범위] 워킹트리에 Picker 만 남아야(+.planning) ===" && git status --porcelain; echo "=== 빌드 (잠기면 스크래치 폴백) ===" && ((set -o pipefail; "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" //t:Rebuild //p:Configuration=Debug //p:Platform=x64 //v:minimal //nologo 2>&1 | grep -iE "error|warning CS|Build succeeded" | head -25) || "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" //p:Configuration=Debug //p:Platform=x64 //p:OutputPath="$TEMP/gsd-na0-scratch/bin/" //p:BaseIntermediateOutputPath="$TEMP/gsd-na0-scratch/obj/" //v:minimal //nologo 2>&1 | grep -iE "error CS|warning CS|Build succeeded" | head -25)</automated>
  </verify>
  <done>
- **[하드제약 3]** Picker 스냅샷 대비 **삭제줄 0건** → 사용자 실험(±5° 탐색범위 3줄 + 인자 2줄)과 모델검색 호출 인자 전체가 바이트 단위 동일. 추가줄에 실험/HALCON 심볼 **0건**.
- **[하드제약 4]** `git show HEAD:Picker` 에 `TupleRad(5, out htRad)` **0건** / `out double dScore,` **1건** = 실험은 커밋 밖, 우리 변경은 커밋 안. 워킹트리 `git diff` 에는 실험 1건 / `dScore` 0건. 복원본이 백업과 바이트 동일(**RESTORE_OK**).
- **[하드제약 1]** 호출부 2파일 추가줄에 HALCON·캘 호출 **0건** — 점수는 이미 계산된 `score[0].D` 재활용.
- **[하드제약 5]** Picker/호출부 추가줄 화이트리스트 이탈 **0건**.
- `out double dScore,` / `dScore   = score[0].D;` / `public static double FindMinScore` 각 1건, `private const double FIND_MIN_SCORE   = 0.5;` 원형 1건 생존.
- TCP 로그가 `score={1:F3} grade={2}` 형태로 교체(옛 형태 0건), Bottom 스텝 라벨이 `ToStatusLine(calGrade, ...)` 로 감싸이고 **`lbl_calStatus.Foreground` 는 0건**(stale 색 회피).
- `ClassifyScore` = SystemHandler 1건 / Bottom 2건(Align+Calib). 추가줄 `0.5` 매직넘버 0건, 삼항 0건.
- 최종 `git status --porcelain` 에 ` M PickerCenterCalibrationService.cs`(실험) 와 `.planning/...` 만 남는다.
- Debug/x64 신규 `error CS` 0 / 신규 `warning CS` 0.
  </done>
</task>

</tasks>

<verification>
1. **하드제약 1 (새 HALCON 호출 0회)** — 6개 파일 전부에서, 이번 Quick 의 추가줄에
   `HOperatorSet.` / `FindShapeModel(` / `FindNccModel(` / `_matcher.` / `.TryCreateModel(` / `.TryFindPose(` /
   `.TryFindRefPose(` / `Matcher.Run(` / `.Grab(` 가 **0건**. Picker 는 `git diff` 가 아니라 **스냅샷 대비 diff** 로 판정.
2. **하드제약 2 (Align 순수 추가)** — `AlignShapeMatchService.cs` diff 삭제줄 **0건**.
   이것이 "기존 오버로드 2개 시그니처·동작 100% 동일"의 증명이다(카운트보다 강한 보증).
3. **하드제약 3 (Picker 순수 추가)** — 스냅샷 대비 삭제줄 **0건** → 실험·모델검색 인자 무변경 증명.
4. **하드제약 4 (실험 커밋 배제 + 보존)** — HEAD 에는 실험 없음/우리 변경 있음, 워킹트리에는 실험만 남고
   복원본이 백업과 바이트 동일.
5. **하드제약 5 (표시 레이어 봉인)** — 파일별 화이트리스트 역검사 0건. MainView 는 추가로 판정심볼 검사 통과.
6. **무변경 파일** — `PatternMatchService.cs`, `TeachDiagnostics.cs` 가 `git status` 에 없다.
7. **등급 실배선** — `ClassifyScore` 호출: MainView 2 / Tray 1 / Bottom 2 / SystemHandler 1 = 총 6건.
   (이번 Quick 이전 레포 전체 호출부 0건이므로 전부 이번 작업 산출물.)
8. **캡처 위치 안전성** — `_lastTeachScoreN` 대입 직전 2줄에 `return false;` 존재 = 가드 밖 성공 경로.
9. **stale 색 규칙** — `lbl_teachStatus` Text/Foreground 동수 유지(Tray 6/6, Bottom 9/9),
   `lbl_calStatus.Foreground` 는 0건(문구만).
10. **빌드** — Debug/x64 신규 `error CS` 0 / 신규 `warning CS` 0. 잠김 시 프로세스 종료 없이 스크래치 컴파일.
</verification>

<success_criteria>
- 티칭이 성공했을 때 **얼마나 잘 됐는지**가 보인다 — `● 티칭 OK (score 0.912)` / `▲ ... (score 0.523)` 형태로
  Datum 완료 모달, Align 상태 라벨, 피커캘 스텝 라벨에 등급 기호가 뜬다.
- 피커캘 TCP 자동경로는 로그에 `score=... grade=Weak` 가 남아 현장 데이터 수집이 가능해진다(화면 노출은 Quick #3).
- **새 HALCON 호출은 단 한 번도 추가되지 않았다.** 표시된 점수는 전부 이미 계산되고 버려지던 값이다.
- **판정 로직·제어흐름 회귀 0** — 두 서비스 파일은 diff 삭제줄이 0이라 기존 동작이 바이트 단위로 보존됐음이 증명된다.
- 사용자의 ±5° 실험이 **워킹트리에 그대로**, 그리고 **커밋에는 들어가지 않은 채** 보존된다.
- `TeachDiagnostics.cs` / `PatternMatchService.cs` 는 한 글자도 바뀌지 않았다.
- Debug/x64 빌드가 신규 에러/경고 0 으로 통과한다.
</success_criteria>

<output>
완료 후 `.planning/quick/260812-na0-teach-diag-2-score-grading/260812-na0-SUMMARY.md` 를 작성한다.

SUMMARY 에 반드시 포함:
- 처리 건수 (Datum 2지점 / Align 서비스 4삽입 + UI 2파일 / Calib 서비스 3삽입 + 호출부 2파일)
- **하드 제약 1~5 검증 결과를 실제 명령 출력으로 인용** — 특히
  (a) Align diff 삭제줄 0, (b) Picker 스냅샷 대비 삭제줄 0, (c) `git show HEAD:Picker` 에 실험 0건/`dScore` 1건,
  (d) 복원 RESTORE_OK
- **브리핑 정정 사실** — Datum 완료 안내는 `CustomMessageBox.Show` 가 아니라
  `CustomMessageBox.ShowConfirmation("모델 생성 완료", ..., MessageBoxButton.YesNo)` 였다는 점
- **설계 판단과 근거** —
  (1) Align 은 "코어 본문 이동" 대신 "캡처 필드 2줄 + 위임 오버로드"를 택해 삭제줄 0을 확보한 이유,
  (2) Calib 은 오버로드 대신 시그니처 확장(컴파일러가 호출부 누락을 강제 검출),
  (3) `lbl_calStatus` 는 대입 36곳이라 색을 칠하지 않고 문구만 넣은 이유,
  (4) `MIN_SCORE`/`FIND_MIN_SCORE` 를 public 으로 바꾸지 않고 읽기전용 프로퍼티를 신설한 이유
- **`datum.PatternMinScore` 기본값 0.0 이슈** — 메서드 상단 기본값 복원이 0.6 을 넣어주므로 가드를 넣지 않았다는 점
- **사용자 실험 처리 결과** — 커밋 절차(백업 → `git apply -R` → 커밋 → 복원)와 최종 `git status` 상태.
  `git apply -R` 이 실패해 중단했다면 그 사실과 현재 파일 상태를 명확히
- 등급 임계값(`GOOD_MARGIN=0.15`) 기준 실제 경계 — Datum: minScore 0.6 → Good ≥ 0.75,
  Align/Calib: 0.5 → Good ≥ 0.65. 현장 데이터로 튜닝 필요하다는 점
- 빌드 방식 (정상 Rebuild 인지 / 산출물 잠김으로 스크래치 컴파일인지)
- Quick #3 으로 넘기는 것 (Calib TCP 자동경로의 화면 노출)
</output>
