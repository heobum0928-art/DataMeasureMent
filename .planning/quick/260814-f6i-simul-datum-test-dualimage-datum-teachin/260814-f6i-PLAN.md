---
phase: quick-260814-f6i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: false
requirements: [QUICK-260814-F6I]

must_haves:
  truths:
    - "DualImage Datum(VerticalTwoHorizontalDualImage)이 TeachingImagePath / TeachingImagePath_Vertical 미설정·파일부재 상태에서도 ShotConfig.SimulImagePath 로 폴백해 이미지를 확보한다"
    - "가로축·세로축 둘 다 동일한 ShotParam.SimulImagePath 한 장으로 폴백하되, 서로 다른 HImage 인스턴스로 생성된다(호출부 finally 이중 Dispose 방지)"
    - "폴백조차 불가능하면(SimulImagePath 도 없음) 종전과 동일하게 false 반환 + 축 구분 에러 로그를 남긴다"
    - "SIMUL 통신테스트에서 $TEST(z_index=0, Datum) 요청이 이미지 부재만을 이유로 즉시 F 로 끝나지 않고 프로토콜 왕복이 완주한다"
    - "1-image Datum 경로 / 크로스-Z(ZIndexA·B 설정) 경로 / $PREP ACK / Align / Calib / P·F 판정 로직은 코드 무변경이다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "TryLoadStaticDualDatumImages 의 SimulImagePath 폴백"
      contains: "LoadDatumImageFromPath(datum, pathH, false)"
  key_links:
    - from: "TryLoadStaticDualDatumImages"
      to: "LoadDatumImageFromPath"
      via: "기존 1-image 폴백 헬퍼 재사용 (신규 폴백 로직 발명 금지)"
      pattern: "LoadDatumImageFromPath\\(datum, path[HV], false\\)"
---

<objective>
SIMUL/오프라인 통신테스트에서 `$TEST(z_index=0, Datum)` 요청이 **DualImage Datum 티칭 이미지 부재**만으로 즉시 F 응답을 내면서 TCP 프로토콜 왕복 검증 자체가 막히는 문제를 해소한다.

이미 1-image Datum 경로(`LoadDatumImageFromPath`, 629~647줄)에 구현되어 있는
`TeachingImagePath → ShotParam.SimulImagePath` 폴백을, **DualImage Datum 경로**
(`TryLoadStaticDualDatumImages`, 705~731줄)에도 **대칭적으로** 추가한다.

Purpose: 실측 정확도가 아니라 "패킷이 끝까지 도는지" 확인이 목적. 더미 이미지를 새로 만들지 않고,
이미 레시피에 등록된 SHOT 검사이미지(`ShotConfig.SimulImagePath`, UI "검사이미지 Grab" 산출물)를 재사용한다.

Output: `Action_FAIMeasurement.cs` 단일 파일의 최소 변경(폴백 순서 확장 + stale 주석 갱신).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/quick/260814-f6i-simul-datum-test-dualimage-datum-teachin/260814-f6i-CONTEXT.md
@WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs

## 코딩 규칙 (프로젝트 하드 규칙 — 위반 금지)

- **삼항 연산자 `?:` 절대 금지** → 반드시 `if / else` 로 풀어 쓴다.
- **헝가리언 표기**: bool 지역변수는 `b` 접두(`bFallbackH`), 문자열은 `sz` 또는 기존 파일 관행(`pathH`, `dName`)을 따른다.
- **브레이스 스타일**: 이 파일은 **K&R**(여는 중괄호를 선언과 같은 줄). Allman 로 바꾸지 말 것.
- **회귀 0**: 이 플랜에서 명시한 함수 외 어떤 로직도 건드리지 않는다.
- **주석**: 비자명한 "왜"만 최소로. 날짜 프리픽스(`//260814 hbk ...`)는 이 파일의 기존 관행이므로 사용해도 되고 생략해도 된다 — 필수 아님.
- **HImage/HObject**: 실패 경로에서 이미 생성된 인스턴스는 반드시 `try { x.Dispose(); } catch { }` 로 정리.
- **C# 7.2** (.NET Framework 4.8): switch expression, nullable reference type, record 등 C# 8+ 문법 금지.

## 확인된 현재 코드 (라인번호는 근사 — 실행자는 반드시 실제 파일을 먼저 읽어 확인할 것)

### 참조할 기존 폴백 패턴 — 1-image Datum (629~647줄)

```csharp
// datum 저장 이미지 로드: teachingPath → ShotParam.SimulImagePath 폴백. allowGrabFallback=true 면 둘 다 실패 시 라이브 grab.
//  오프라인 모드(allowGrabFallback=false)에선 저장 이미지 부재 시 null 반환 → datum Find 실패로 명확히 드러남.
private HImage LoadDatumImageFromPath(DatumConfig datum, string teachingPath, bool allowGrabFallback) {
    HImage image = null;
    if (!string.IsNullOrEmpty(teachingPath) && File.Exists(teachingPath)) {
        try { image = new HImage(teachingPath); } catch { image = null; }
    }
    if (image == null && ShotParam != null && !string.IsNullOrEmpty(ShotParam.SimulImagePath) && File.Exists(ShotParam.SimulImagePath)) { // 폴백
        try { image = new HImage(ShotParam.SimulImagePath); } catch { image = null; }
    }
    if (image == null) {
        if (allowGrabFallback) {
            image = SystemHandler.Handle.Devices.GrabHalconImage(ShotParam);
        } else {
            string dName = "";
            if (datum != null) dName = datum.DatumName ?? "";
            Logging.PrintLog((int)ELogType.Error, "[Datum] '" + dName + "' 오프라인 datum 이미지 없음 (TeachingImagePath/SimulImagePath) — datum Find skip. '검사이미지 Grab' 으로 먼저 확보 필요.");
        }
    }
    return image;
}
```

**핵심**: 이 헬퍼는 `allowGrabFallback=false` 로 호출하면 정확히 "teachingPath → SimulImagePath → null+로그" 순서다.
DualImage 경로가 필요한 동작과 **완전히 동일**하다 → **새 폴백 코드를 작성하지 말고 이 헬퍼를 두 번 호출**한다.

### 수정 대상 — DualImage Datum (705~731줄, 폴백 없음)

```csharp
private bool TryLoadStaticDualDatumImages(DatumConfig datum, out HImage imageHorizontal, out HImage imageVertical) {
    imageHorizontal = null;
    imageVertical = null;
    string pathH = datum.TeachingImagePath;
    string pathV = datum.TeachingImagePath_Vertical;

    if (string.IsNullOrEmpty(pathH) || !File.Exists(pathH)) {
        Logging.PrintErrLog((int)ELogType.Error, "[Datum] 가로축 티칭 이미지 경로가 비어 있거나 파일이 없습니다 (DualImage).");
        return false;
    }
    if (string.IsNullOrEmpty(pathV) || !File.Exists(pathV)) {
        Logging.PrintErrLog((int)ELogType.Error, "[Datum] 세로축 티칭 이미지 경로가 비어 있거나 파일이 없습니다 (DualImage).");
        return false;
    }

    try { imageHorizontal = new HImage(pathH); } catch (Exception ex) { ... imageHorizontal = null; }
    try { imageVertical = new HImage(pathV); } catch (Exception ex) { ... imageVertical = null; }

    if (imageHorizontal == null || imageVertical == null) {
        if (imageHorizontal != null) { try { imageHorizontal.Dispose(); } catch { } }
        if (imageVertical != null) { try { imageVertical.Dispose(); } catch { } }
        imageHorizontal = null;
        imageVertical = null;
        return false;
    }
    return true;
}
```

### 호출부 (단 1곳: 698줄) 및 소유권 계약

- `TryGrabOrLoadDualDatumImages` (685줄) → `bCrossZEnabled` 이면 크로스-Z 경로, 아니면 698줄에서 `TryLoadStaticDualDatumImages` 호출.
- 최종 소비자는 143줄 `if (!TryGrabOrLoadDualDatumImages(datum, parentSeq, out imgH, out imgV, out bDatumCrossZPending))`,
  **185~188줄 `finally` 에서 `imgH`/`imgV` 를 각각 독립적으로 `Dispose()`** 한다.
  → **폴백 시에도 두 out 파라미터는 서로 다른 HImage 인스턴스여야 한다. 같은 참조를 양쪽에 대입하면 이중 Dispose.**

### 갱신해야 할 stale 주석 (682~684줄)

```
//  우회해 기존 static 경로(TeachingImagePath/_Vertical, 무 z 의존)로 동기 취득한다. 주의: 이 fallback 은 해당
//  datum 의 TeachingImagePath/_Vertical 이 채워져 있어야 성립 — 비어있으면 TryLoadStaticDualDatumImages 가
//  false 를 반환해 명시적 DETECT FAIL/NG 로 이어진다(조용한 stale 재사용보다 안전한 실패 모드, 회귀 아님).
```
이 문장은 이번 변경으로 **사실과 달라진다**(비어 있어도 SimulImagePath 폴백이 성립). 반드시 갱신할 것.

### 관련 사실 (참고)

- `public ShotConfig ShotParam => Param as ShotConfig;` (51줄) — 같은 클래스 내 접근 가능.
- 271줄 주석: "Simul 에서 두 경로 동일 파일 가능" — 두 축이 한 장을 공유하는 폴백의 기존 근거.
- 571줄 주석: "SIMUL / 오프라인 공용" — 기존 폴백은 `SIMUL_MODE` 컴파일 가드 없이 무조건. **신규 가드 발명 금지.**
- `ELogType.Trace = 0`, `ELogType.Error = 6` (`WPF_Example/Setting/SystemSetting.cs:18`).
- `System.IO` / `HalconDotNet` using 은 이미 이 파일에 존재 (578·631줄에서 `File`/`HImage` 사용 중) — 신규 using 불필요.
</context>

<tasks>

<task type="auto">
  <name>Task 1: TryLoadStaticDualDatumImages 에 SimulImagePath 폴백 추가 + stale 주석 갱신</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <action>
**1단계 — 먼저 실제 코드를 읽어 구조를 확인한다 (라인번호는 근사값이라 이동했을 수 있음).**
`Read` 로 `WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 의 620~735 구간을 읽고
`LoadDatumImageFromPath` 와 `TryLoadStaticDualDatumImages` 의 실제 위치·시그니처·본문이
위 `<context>` 에 적힌 것과 동일한지 확인한다. 다르면 **편집 전에 차이를 보고**하고, 동일하면 진행한다.

**2단계 — `TryLoadStaticDualDatumImages` 본문을 아래로 교체한다.**
새 폴백 로직을 직접 작성하지 말고, 기존 헬퍼 `LoadDatumImageFromPath(datum, path, false)` 를
가로/세로 각각 **두 번 호출**한다. 이 헬퍼가 이미 `teachingPath → ShotParam.SimulImagePath → null` 순서를
정확히 수행하고, 호출마다 `new HImage(...)` 로 **독립 인스턴스**를 만들어 이중 Dispose 위험도 없다(CONTEXT 결정: 기존 패턴 재사용, 신규 발명 금지).

```csharp
        // 기존 static teaching 파일 로드 경로 (ZIndexA/B 미설정, D-07 회귀 0).
        //  imageHorizontal: datum.TeachingImagePath  → 부재/로드실패 시 ShotParam.SimulImagePath 폴백
        //  imageVertical:   datum.TeachingImagePath_Vertical → 부재/로드실패 시 동일 ShotParam.SimulImagePath 폴백
        //  폴백은 1-image datum 경로(LoadDatumImageFromPath)가 이미 쓰던 것과 동일한 것을 재사용한다 — SIMUL/오프라인
        //  통신테스트에서 datum 티칭 이미지 미확보만으로 $TEST 가 즉시 F 로 끝나 프로토콜 왕복 검증 자체가 막히던
        //  문제를 닫기 위함. 두 축이 같은 SHOT 검사이미지 한 장을 쓰는 것은 271줄 "Simul 에서 두 경로 동일 파일 가능"
        //  전제와 동일. SimulImagePath 조차 없으면 종전대로 false(명시적 DETECT FAIL/NG).
        //  주의: 두 out 파라미터는 호출부 finally 에서 각각 Dispose 되므로 폴백 시에도 반드시 서로 다른 HImage
        //  인스턴스여야 한다 — LoadDatumImageFromPath 를 두 번 호출해 각각 새로 생성한다(참조 공유 금지).
        private bool TryLoadStaticDualDatumImages(DatumConfig datum, out HImage imageHorizontal, out HImage imageVertical) {
            imageHorizontal = null;
            imageVertical = null;
            string pathH = datum.TeachingImagePath;
            string pathV = datum.TeachingImagePath_Vertical;

            bool bFallbackH = string.IsNullOrEmpty(pathH) || !File.Exists(pathH);
            bool bFallbackV = string.IsNullOrEmpty(pathV) || !File.Exists(pathV);

            imageHorizontal = LoadDatumImageFromPath(datum, pathH, false); // teachingPath → SimulImagePath 폴백 (grab 없음)
            imageVertical = LoadDatumImageFromPath(datum, pathV, false);   // 동일 폴백, 별도 인스턴스

            if (imageHorizontal == null) {
                Logging.PrintErrLog((int)ELogType.Error, "[Datum] 가로축 이미지 확보 실패 — TeachingImagePath / ShotParam.SimulImagePath 모두 없음 (DualImage).");
            } else if (bFallbackH) {
                // 폴백이 조용히 일어나면 "티칭 이미지로 검출했다" 고 오해할 수 있어 흔적을 남긴다.
                Logging.PrintLog((int)ELogType.Trace, "[Datum] 가로축 티칭 이미지 부재 — SHOT 검사이미지(SimulImagePath)로 폴백 (DualImage).");
            }
            if (imageVertical == null) {
                Logging.PrintErrLog((int)ELogType.Error, "[Datum] 세로축 이미지 확보 실패 — TeachingImagePath_Vertical / ShotParam.SimulImagePath 모두 없음 (DualImage).");
            } else if (bFallbackV) {
                Logging.PrintLog((int)ELogType.Trace, "[Datum] 세로축 티칭 이미지 부재 — SHOT 검사이미지(SimulImagePath)로 폴백 (DualImage).");
            }

            if (imageHorizontal == null || imageVertical == null) {
                if (imageHorizontal != null) { try { imageHorizontal.Dispose(); } catch { } }
                if (imageVertical != null) { try { imageVertical.Dispose(); } catch { } }
                imageHorizontal = null;
                imageVertical = null;
                return false;
            }
            return true;
        }
```

**3단계 — 682~684줄 stale 주석 갱신.**
`TryGrabOrLoadDualDatumImages` 위 주석 블록에서 "비어있으면 TryLoadStaticDualDatumImages 가 false 를 반환해
명시적 DETECT FAIL/NG 로 이어진다(조용한 stale 재사용보다 안전한 실패 모드, 회귀 아님)" 문장을 현재 동작에 맞게 고친다.
예시 문구(의미만 맞으면 표현은 자유):

```
//  우회해 기존 static 경로(TeachingImagePath/_Vertical, 무 z 의존)로 동기 취득한다. TeachingImagePath/_Vertical 이
//  비어 있으면 SHOT 검사이미지(ShotParam.SimulImagePath)로 폴백하고(1-image datum 경로와 동일 패턴), 그것마저
//  없을 때만 TryLoadStaticDualDatumImages 가 false 를 반환해 명시적 DETECT FAIL/NG 로 이어진다(조용한 stale 재사용 금지).
```

**절대 건드리지 말 것 (회귀 0):**
- `TryGrabOrLoadCrossZDatumImages` (크로스-Z 라이브 캡처 경로) — 로직 무변경
- `GrabOrLoadDatumImage` / `LoadDatumImageFromPath` / `LoadShotInspectionImage` — **시그니처·본문 모두 무변경**
- `$PREP` ACK 로직, Align(`TryComposeAlign`), Calib, P/F 판정(`EvaluateJudgement` 계열)
- `ShotConfig` / `DatumConfig` 필드 스키마
- `WPF_Example/DatumMeasurement.csproj`, `WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs`
  (작업 시작 시점에 이미 uncommitted 수정이 있는 파일 — 절대 추가 수정 금지)

**빌드 잠김 시 프로세스 종료 금지** (프로젝트 하드 규칙). 산출물 잠김이면 스크래치 `-p:OutDir=` 로 컴파일만 검증하고 SUMMARY 에 기록한다.
  </action>
  <verify>
    <automated>F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs && echo "=== [1] 헬퍼 재사용 2회(가로/세로) : 각 1 기대 ===" && grep -c "LoadDatumImageFromPath(datum, pathH, false)" "$F" && grep -c "LoadDatumImageFromPath(datum, pathV, false)" "$F" && echo "=== [2] 구 즉시-return 가드 제거 확인 : 0 기대 ===" && grep -c "가로축 티칭 이미지 경로가 비어 있거나 파일이 없습니다" "$F" && grep -c "세로축 티칭 이미지 경로가 비어 있거나 파일이 없습니다" "$F" && echo "=== [3] 폴백 흔적 로그 : 2 기대 ===" && grep -c "SHOT 검사이미지(SimulImagePath)로 폴백" "$F" && echo "=== [4] 실패시 양쪽 Dispose 정리 유지 : 1 기대 ===" && awk '/private bool TryLoadStaticDualDatumImages/,/^        private bool TryGrabOrLoadCrossZDatumImages/' "$F" | grep -c "imageHorizontal.Dispose" && echo "=== [5] 삼항 연산자 미사용 확인(0 기대) ===" && awk '/private bool TryLoadStaticDualDatumImages/,/^        private bool TryGrabOrLoadCrossZDatumImages/' "$F" | grep -cE "\? .* : " && echo "=== [6] 헬퍼 원형 무변경 : 각 1 기대 ===" && grep -c "private HImage LoadDatumImageFromPath(DatumConfig datum, string teachingPath, bool allowGrabFallback)" "$F" && grep -c "private HImage GrabOrLoadDatumImage(DatumConfig datum)" "$F" && grep -c "private HImage LoadShotInspectionImage()" "$F" && echo "=== [7] 크로스-Z 경로 무변경 : 1 기대 ===" && grep -c "private bool TryGrabOrLoadCrossZDatumImages(DatumConfig datum, InspectionSequence parentSeq, out HImage imageHorizontal, out HImage imageVertical, out bool bPending)" "$F" && echo "=== [8] stale 주석 갱신 확인 : 0 기대 ===" && grep -c "false 를 반환해 명시적 DETECT FAIL/NG 로 이어진다(조용한 stale 재사용보다 안전한 실패 모드" "$F" && echo "=== [9] 변경 파일 범위 : 아래 3줄만 기대 ===" && git diff --name-only && echo "=== [10] 금지 파일 무변경(각각 baseline 해시와 동일해야 함) ===" && echo "expect 3daa3bef520786d331716fb77bc93e2eb632b966" && git diff -- WPF_Example/DatumMeasurement.csproj | git hash-object --stdin && echo "expect 86d1071909389cdb13b4ff8f3032489aff26e2fe" && git diff -- WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs | git hash-object --stdin && echo "=== [11] Debug/x64 빌드 (경고 baseline 12줄: CS0618x10 + CS0162x2) ===" && "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" -p:Configuration=Debug -p:Platform=x64 -v:minimal -nologo 2>&1 | grep -iE "error CS|error MSB|warning CS|Build succeeded"</automated>
  </verify>
  <done>
- `Build succeeded`, `error CS`/`error MSB` 0건, 경고는 baseline 12줄(CS0618×10 + CS0162×2)만 존재
- `TryLoadStaticDualDatumImages` 가 `LoadDatumImageFromPath(..., false)` 를 가로/세로 각 1회 호출하고, 두 개의 독립 HImage 를 반환
- 구 "경로가 비어 있거나 파일이 없습니다" 즉시-return 가드 2개 제거됨
- 실패(양쪽 폴백 불가) 시 축 구분 에러 로그 + 부분 성공분 Dispose + `false` 반환 유지
- 682~684줄 stale 주석이 새 동작(SimulImagePath 폴백 성립)에 맞게 갱신됨
- `git diff --name-only` 결과가 정확히 3줄: Action_FAIMeasurement.cs + 기존 dirty 2파일(csproj, PickerCenterCalibrationService.cs)이며 후자 2개의 diff 해시가 baseline 과 일치
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SIMUL $TEST(z_index=0) Datum 왕복 인간 검증</name>
  <files>(코드 변경 없음 — 사용자 실행 검증)</files>
  <action>사람이 SIMUL 앱에서 아래 how-to-verify 절차를 직접 수행하고 결과를 회신할 때까지 대기한다. 실행자가 대신 판단하거나 임의로 통과 처리하지 않는다.</action>
  <what-built>
DualImage Datum 이미지 로드에 `ShotConfig.SimulImagePath` 폴백을 추가했습니다.
이제 Datum 의 가로/세로 티칭 이미지가 레시피에 없어도, 해당 SHOT 의 검사이미지(“검사이미지 Grab” 으로 확보해둔 그 파일)를
두 축 모두에 재사용해서 Datum 검출을 시도합니다. 검사이미지마저 없을 때만 예전처럼 실패합니다.
1-image Datum, 크로스-Z, $PREP, Align, Calib, 합·불 판정 로직은 손대지 않았습니다.
  </what-built>
  <how-to-verify>
SIMUL_MODE(Debug/x64) 빌드로 앱을 실행한 뒤 아래를 확인해 주세요.

1. Top SIMUL 레시피를 로드하고, 대상 SHOT 의 **검사이미지(SimulImagePath)** 가 실제로 등록되어 있는지 확인합니다.
   (비어 있으면 InspectionListView 에서 “검사이미지 Grab” 으로 먼저 한 장 확보해야 합니다 — 이 폴백은 그 이미지를 씁니다.)
2. 대상 Datum 의 AlgorithmType 이 `VerticalTwoHorizontalDualImage` 인지, 그리고 그 Datum 의
   TeachingImagePath / TeachingImagePath_Vertical 이 비어 있는지 확인합니다. (이번 수정의 대상 조건입니다.)
3. 핸들러/목 클라이언트에서 `$TEST:site,Type,자재번호@` 형태의 **z_index=0(Datum)** 통신 테스트를 보냅니다.
4. 기대 결과:
   - 로그에 `[Datum] 가로축 티칭 이미지 경로가 비어 있거나 파일이 없습니다` **가 더 이상 나오지 않습니다.**
   - 대신 `[Datum] 가로축/세로축 티칭 이미지 부재 — SHOT 검사이미지(SimulImagePath)로 폴백` Trace 로그가 보입니다.
   - `$RESULT` 응답이 “이미지 취득 실패로 즉시 F” 가 아니라 **끝까지 왕복해서 돌아옵니다.**
     (검출 성공/실패로 인한 P/F 값 자체는 이번 작업의 판정 대상이 아닙니다 — 왕복 완주가 확인 포인트입니다.)
5. 회귀 확인: 티칭 이미지가 **정상적으로 등록된** 다른 Datum(1-image, DualImage 둘 다)으로 평소처럼 RUN 을 돌려
   기존과 동일한 결과가 나오는지 확인합니다. 폴백 Trace 로그가 뜨지 않아야 정상입니다.
  </how-to-verify>
  <verify>사용자 회신으로 확인 (자동 검증 불가 — 실제 앱 실행 + TCP 왕복 필요)</verify>
  <done>사용자가 “approved” 회신, 또는 보고된 이슈가 모두 해소됨</done>
  <resume-signal>“approved” 라고 입력하시거나, 다르게 동작한 부분을 알려 주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 핸들러 PC → VisionServer TCP (`$TEST`) | 외부 장비가 보내는 검사 트리거. 이번 변경은 이 경로의 **파싱·판정을 건드리지 않음**(이미지 소스 해석만 변경). |
| 로컬 디스크 ← 레시피 INI 의 이미지 경로 | `TeachingImagePath` / `SimulImagePath` 는 운영자가 UI 로 설정한 로컬 경로. 신규 외부 입력 없음. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-f6i-01 | Tampering (데이터 오염) | `TryLoadStaticDualDatumImages` 폴백 | mitigate | 폴백이 조용히 일어나면 "티칭 이미지로 검출됨" 으로 오독될 수 있음 → 폴백 발생 시 `ELogType.Trace` 로그를 남겨 추적 가능하게 함(Task 1 코드에 포함) |
| T-f6i-02 | Denial of Service (리소스) | 폴백 HImage 인스턴스 | mitigate | 두 축에 **별도 인스턴스** 생성(참조 공유 금지) + 부분 실패 시 `Dispose` 정리 유지 → 호출부 `finally` 이중 Dispose / 누수 차단 |
| T-f6i-03 | Information Disclosure | 에러 로그 경로 문자열 | accept | 로컬 파일 경로만 로컬 로그 파일에 기록. 기존 로그와 동일 수준, 신규 노출 없음 |
| T-f6i-04 | Repudiation (오검 은폐) | 실기(비-SIMUL) 수동 RUN 경로 | accept | 실기에서도 폴백이 동작해 티칭 이미지 부재를 실패로 드러내지 않게 됨. 단 이는 1-image datum 경로가 이미 무조건 수행하던 동작과 동일하며(571줄 "SIMUL / 오프라인 공용"), CONTEXT.md 에서 "별도 신규 게이팅 발명 금지" 로 확정된 사항. T-f6i-01 의 Trace 로그가 사후 추적 수단 |
</threat_model>

<verification>
1. Debug/x64 빌드 성공, `error CS`/`error MSB` 0건, 경고는 baseline 12줄만.
2. 변경 파일이 `Action_FAIMeasurement.cs` 1개뿐(기존 dirty 2파일은 해시 baseline 유지).
3. `TryLoadStaticDualDatumImages` 만 변경됨 — `LoadDatumImageFromPath`, `GrabOrLoadDatumImage`,
   `LoadShotInspectionImage`, `TryGrabOrLoadCrossZDatumImages` 시그니처·본문 무변경.
4. 인간 검증(Task 2): SIMUL `$TEST` z_index=0 왕복 완주 + 정상 티칭 Datum 회귀 없음.
</verification>

<success_criteria>
- DualImage Datum 이 티칭 이미지 부재 상태에서 `ShotParam.SimulImagePath` 로 폴백해 이미지를 확보한다.
- 가로/세로 out 파라미터가 서로 다른 HImage 인스턴스다(이중 Dispose 없음).
- 폴백조차 불가하면 축 구분 에러 로그 + `false` 반환(기존 실패 모드 유지).
- 폴백 사용 시 Trace 로그로 흔적이 남는다.
- 범위 밖 로직($PREP ACK / Align / Calib / P·F 판정 / 크로스-Z / 1-image 경로) 코드 무변경.
- Debug/x64 빌드 PASS, 경고 baseline 12줄 초과 없음.
</success_criteria>

<output>
완료 후 `.planning/quick/260814-f6i-simul-datum-test-dualimage-datum-teachin/260814-f6i-SUMMARY.md` 를 작성한다.
</output>
