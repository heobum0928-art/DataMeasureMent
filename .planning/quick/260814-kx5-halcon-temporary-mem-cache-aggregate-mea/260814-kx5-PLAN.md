---
phase: quick-260814-kx5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/SystemHandler.cs
  - WPF_Example/Sequence/Sequence/SequenceBase.cs
autonomous: true
requirements: [MEMCACHE-AGGREGATE-01]

must_haves:
  truths:
    - "SystemHandler.Initialize()가 temporary_mem_cache를 aggregate로 설정하는 호출을, Sequences = SequenceHandler.Handle(각 SequenceBase.MainThread를 생성/Start하는 지점)보다 반드시 먼저 실행한다 — 파일 내 줄 순서로 구조적으로 보장"
    - "각 SequenceBase.MainThread가 자신의 첫 HALCON 호출 전(스레드 진입 직후) tsp_temporary_mem_cache를 aggregate로 재적용하고, 재적용 전(inherited)/후(confirmed) 모드를 로그로 남긴다"
    - "Debug|x64 빌드가 신규 오류 0건, 신규 경고 0건(기존 baseline 12줄 그대로)으로 통과한다"
    - "DatumMeasurement.csproj / PickerCenterCalibrationService.cs는 이번 작업으로 단 1바이트도 변경되지 않는다"
  artifacts:
    - path: "WPF_Example/SystemHandler.cs"
      provides: "temporary_mem_cache aggregate 전환의 근거/스레드범위/트레이드오프를 문서화하는 주석(이미 사용자가 값 자체는 uncommitted로 선반영해둔 상태)"
      contains: "quick-260814-kx5"
    - path: "WPF_Example/Sequence/Sequence/SequenceBase.cs"
      provides: "스레드별 방어적 재적용 + GetSystem 실측 로그 메서드"
      contains: "ReinforceThreadMemoryCache"
  key_links:
    - from: "SequenceBase.MainExecute"
      to: "SequenceBase.ReinforceThreadMemoryCache"
      via: "스레드 진입 직후, while 루프 진입 전 1회 호출"
      pattern: "ReinforceThreadMemoryCache\\(\\);"
    - from: "ReinforceThreadMemoryCache"
      to: "HALCON GetSystem 실측 로그"
      via: "재적용 전/후 tsp_temporary_mem_cache 값을 [MemCacheWarmup] 로 로그"
      pattern: "\\[MemCacheWarmup\\]"
---

<objective>
`.planning/debug/top-release-2x-slower.md`(status: `partial_improvement_workaround_incomplete`)의 저녁 재조사가 지목한
유력 후보 — HALCON 기본 `temporary_mem_cache`('exclusive') 모드는 스레드별 스택 구조라, 크기가 들쭉날쭉한 연속 요청
(SHOT_A1-23-C1-C12, 35개 서로 다른 ROI)마다 새 슈퍼블록을 만들고 그 아래 안 쓰는 블록을 OS에 반납하는 캐시 스래싱이
반복될 수 있다는 것 — 에 대해, MVTec 공식 기술노트가 정확히 이 워크로드를 위한 해법으로 명시하는
`set_system('temporary_mem_cache', 'aggregate')` 를 앱이 HALCON을 쓰기 시작하기 전 가장 이른 시점에 확실히 걸어둔다.

**중요 — 이번 세션에서 이미 확인된 사실 3가지 (실행 전 반드시 인지):**

1. **이 설정값은 이미 uncommitted 상태로 코드에 반영돼 있다.** `git diff -- WPF_Example/SystemHandler.cs` 확인 결과,
   `HOperatorSet.SetSystem("temporary_mem_cache", "idle")` → `"aggregate"` 로 딱 한 줄이 이미 로컬에서 바뀌어 있다
   (`git blame` 상 "Not Committed Yet", 2026-08-14 15:11:57 — 이 quick 폴더 생성 직후 사용자가 직접 실험 삼아
   먼저 바꿔둔 것으로 보인다). 이번 plan의 코드 작업은 "새로 추가"가 아니라 **"이미 된 변경을 제대로 문서화 +
   방어적으로 보강"** 이다.
2. **스레드 범위(가장 중요한 확인 사항)는 로컬 설치본 HALCON 24.11 공식 문서로 직접 확인 완료.**
   `set_system` 레퍼런스(`temporary_mem_cache` 항목)와 `HALCON Memory Management` 기술노트 §2.3
   "Switching between cache modes" 원문: `tsp_` 접두 없이 호출하면 (a) 호출한 스레드는 즉시 적용, (b) **그 호출
   이후 시작되는(started afterwards) 모든 스레드에 적용**, (c) 이미 실행 중이던 스레드는 미적용. 즉 "프로세스
   전역"도 아니고 순수 "스레드국한"도 아닌, **"호출 시점 기준 이후 시작 스레드에 상속"** 이라는 제3의 의미론이다
   — 오늘 낮의 워밍업 스레드 사고(Task.Run이 실제 검사 스레드와 다른 스레드라 무효였던 사건)와는 다른 함정이므로
   혼동하지 말 것.
3. **현재 코드 위치는 구조적으로 이미 안전하다 — 단, 방어적으로 한 겹 더 보강한다.**
   `SystemHandler.cs:137`(SetSystem 호출)이 `SystemHandler.cs:181`(`Sequences = SequenceHandler.Handle;` — 이
   대입이 `SequenceHandler` 생성자를 트리거해 `RegisterSequences()` → 각 `SequenceBase` 생성자의
   `MainThread = new Thread(MainExecute); MainThread.Start();` 를 동기 실행한다)보다 **먼저** 실행된다. 즉
   각 `SequenceBase.MainThread` 는 위 (b) 조건("이후 시작되는 스레드")에 해당해 이론상 이미 aggregate 를
   상속받는다. 다만 초기화 순서가 향후 리팩토링으로 바뀌어도 안전하도록, 사용자의 명시적 지시("애매하면 안전한
   쪽으로")에 따라 각 `SequenceBase.MainThread` 진입 시점에 `tsp_temporary_mem_cache` 로 한 번 더 재적용하고,
   **재적용 전(inherited) 값을 실측 로그로 남겨 위 이론이 실제로 맞는지 실측 증거를 확보한다.**

Purpose: measure_pos 콜드스타트 캐시 스래싱 후보에 대한 근본 수정 "시도" — 이미 반영된 aggregate 값을 근거와 함께
문서화하고, 초기화 순서 의존성을 제거하는 방어적 재적용 + 실측 검증 로그를 추가한다.
Output: `SystemHandler.cs` 주석 보강(값 변경 없음, 이미 반영됨), `SequenceBase.cs` 에 스레드별 방어적 재적용 +
실측 로그 신규 추가.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

**배경 (이전 세션 결과물, 참고용):**
@.planning/debug/top-release-2x-slower.md

**코딩 규칙 (이 프로젝트 상시 규칙):**
- 삼항연산자 `?:` 금지 → 반드시 `if / else`
- C# 7.2, .NET Framework 4.8 (8.0+ 문법 금지)
- `SystemHandler.cs`/`SequenceBase.cs` 모두 K&R 브레이스(여는 중괄호 같은 줄) — 그대로 유지
- 신규 주석은 `260814 hbk quick-260814-kx5:` 접두, 비자명한 "왜"만 담되 이번 건은 안전성이 걸린 만큼 근거를
  충분히 남긴다(과도한 축약 금지)

---

## 절대 건들면 안 되는 파일 (열지도 말 것)

| 파일 | 상태 | 지침 |
|------|------|------|
| `WPF_Example/DatumMeasurement.csproj` | 사용자의 별도 진행 중인 로컬 실험 | **절대 열지도, 건들지도 말 것** |
| `WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs` | 사용자의 별도 진행 중인 로컬 실험 | **절대 열지도, 건들지도 말 것** |

baseline blob 해시 (`git hash-object <path>` 로 이 plan 작성 시점에 측정, 작업 후에도 반드시 동일해야 함):
```
DatumMeasurement.csproj              : 761141c36ad80d58483248b8507c02e5ee0188a1
PickerCenterCalibrationService.cs    : 9f82579dc560e821b58d1d5f481639019adf52f3
```

`git add .` / `git add -A` / `git commit -a` 금지 — 반드시 수정한 파일만 명시적으로 `git add`. 새 `.cs` 파일도
만들지 않는다(csproj 는 classic-style `<Compile Include>` 라 편집이 필요해지는데, 그 파일은 절대 건들면 안 됨).

## HALCON 공식 문서 근거 (로컬 설치본, 실행 직전 재확인 대상 — Task 1의 첫 단계)

로컬 경로 (웹 아님, 이 머신에 설치된 HALCON 24.11.2.0 문서 그대로):
- `C:\Program Files\MVTec\HALCON-24.11-Progress-Steady\doc_en_US\html\reference\operators\set_system.html`
  - Description 상단 "Parallelization information" 블록(파일 내 line 602~634 부근): `'tsp_'` 접두 유무에 따른
    적용 범위 설명. `temporary_mem_cache` 항목 자체(line 2170~2234 부근)에 `*)` 와 `tsp_temporary_mem_cache`
    변형이 함께 명시됨.
- `C:\Program Files\MVTec\HALCON-24.11-Progress-Steady\doc_en_US\html\manuals\memory_management\memory_management_0007.html`
  - §2.3 "Temporary Memory Cache" > "Switching between cache modes" 항목(파일 내 "Switching between cache modes"
    검색으로 위치): 아래 원문 인용.

> "Calling the global variant without 'tsp_' will have the following effect on the active threads of a
> multithreaded program: The cache mode of the current user thread ... is changed. **The new setting is
> applied to all user threads which are started afterwards.** ... If the mode is changed in the middle of a
> multithreaded program from any one of the user threads, including the main thread, it will at least
> temporarily lead to a mixture of cache modes. To ensure that the entire program runs with the same cache
> setting, **it is best to set the cache mode at the very beginning of your program.**"

이 문구가 이번 plan의 핵심 근거다 — "프로세스 전역"도 "완전 스레드국한"도 아니라 "호출 시점 이후 시작 스레드에
상속" 이라는 제3의 의미론이며, 공식문서 스스로 "앱의 아주 초반에 설정하라"고 권고한다(현재 코드가 정확히 그 위치에
있음).

## 1) `WPF_Example/SystemHandler.cs` — 이미 반영된 aggregate 값에 근거/트레이드오프 주석 보강

**현재 코드 (line 119~142, `Initialize()` 시작부 — `temporary_mem_cache` 줄은 이미 `"aggregate"` 로 uncommitted
상태로 바뀌어 있음, `git diff` 로 확인됨):**
```csharp
        // Call after constructor to fully initialize runtime components.
        public void Initialize() {
            // quick-260806-dsn Part A: HALCON 자체 캐시(mimalloc, HALCON 24.11 Windows 기본 할당자)가 해제된
            //  메모리를 OS에 즉시 반환하지 않고 계속 쌓아두는 문제의 공식 완화책(memory_management 챕터,
            //  "Handling Suspected Memory Leaks in HALCON" 권장 3줄, 앱 시작 시 1회). 캐시 정책만 바꿀 뿐
            //  기능/정확성에는 영향 없다. Devices/Sequences 등 이후의 모든 Halcon 이미지 처리에 적용되도록
            //  이 메서드의 첫 실행문으로 둔다. 실패해도(캐시 힌트 실패일 뿐) 앱 시작을 막지 않는다.
            try {
                // quick-260806-dsn3: 위 3줄(캐시 idle)로도 메모리가 안 돌아오는 경우를 위한 같은 챕터의 다음 단계 —
                //  HALCON 내부 힙 할당자를 Windows 기본값 mimalloc 에서 Win32 기본 힙(system)으로 전환한다.
                //  문서 원문: "mimalloc tends to cache memory more aggressively than the Win32 default heap
                //  allocator ... set_system('memory_allocator', 'system')". 격리 하네스 실측에서 121MB HImage를
                //  생성/Dispose 반복 시 mimalloc 은 8회차부터 WorkingSet 이 ~152MB 에 영구 고착(GC.Collect 무효)한 반면
                //  'system' 은 15회 전부 ~30MB 로 반환됐다. 할당자 종류 설정이므로 다른 SetSystem 보다 먼저 둔다.
                //  HALCON 내부 할당 경로만 바꾸므로 이미지 데이터/측정 수치에는 영향이 없다.

                HOperatorSet.SetSystem("memory_allocator", "system");
                HOperatorSet.SetSystem("global_mem_cache", "idle");
                HOperatorSet.SetSystem("temporary_mem_cache", "aggregate");
                HOperatorSet.SetSystem("image_cache_capacity", 0);
            }
            catch (Exception ex) {
                Logging.PrintLog((int)ELogType.Error, "[STARTUP] HALCON SetSystem memory cache config failed: {0}", ex.Message);
            }
```

**교체 후 (`temporary_mem_cache` 줄 자체는 무변경 — 이미 `"aggregate"`. 그 줄 바로 위에 근거/트레이드오프 주석만
삽입):**
```csharp
        // Call after constructor to fully initialize runtime components.
        public void Initialize() {
            // quick-260806-dsn Part A: HALCON 자체 캐시(mimalloc, HALCON 24.11 Windows 기본 할당자)가 해제된
            //  메모리를 OS에 즉시 반환하지 않고 계속 쌓아두는 문제의 공식 완화책(memory_management 챕터,
            //  "Handling Suspected Memory Leaks in HALCON" 권장 3줄, 앱 시작 시 1회). 캐시 정책만 바꿀 뿐
            //  기능/정확성에는 영향 없다. Devices/Sequences 등 이후의 모든 Halcon 이미지 처리에 적용되도록
            //  이 메서드의 첫 실행문으로 둔다. 실패해도(캐시 힌트 실패일 뿐) 앱 시작을 막지 않는다.
            try {
                // quick-260806-dsn3: 위 3줄(캐시 idle)로도 메모리가 안 돌아오는 경우를 위한 같은 챕터의 다음 단계 —
                //  HALCON 내부 힙 할당자를 Windows 기본값 mimalloc 에서 Win32 기본 힙(system)으로 전환한다.
                //  문서 원문: "mimalloc tends to cache memory more aggressively than the Win32 default heap
                //  allocator ... set_system('memory_allocator', 'system')". 격리 하네스 실측에서 121MB HImage를
                //  생성/Dispose 반복 시 mimalloc 은 8회차부터 WorkingSet 이 ~152MB 에 영구 고착(GC.Collect 무효)한 반면
                //  'system' 은 15회 전부 ~30MB 로 반환됐다. 할당자 종류 설정이므로 다른 SetSystem 보다 먼저 둔다.
                //  HALCON 내부 할당 경로만 바꾸므로 이미지 데이터/측정 수치에는 영향이 없다.

                HOperatorSet.SetSystem("memory_allocator", "system");
                HOperatorSet.SetSystem("global_mem_cache", "idle");
                //260814 hbk quick-260814-kx5: 'idle'→'aggregate'. 근거: HALCON 공식 기술노트("HALCON Memory
                //  Management" §2.3)상 기본 'exclusive' 모드는 스레드별 스택 구조라 크기가 들쭉날쭉한 연속
                //  요청(SHOT_A1-23-C1-C12 35개 ROI)마다 새 슈퍼블록 생성+구블록 OS 반납을 반복하는 캐시
                //  스래싱이 생길 수 있음(.planning/debug/top-release-2x-slower.md 저녁 재조사 Evidence).
                //  'aggregate'는 작은 블록을 스레드별로 하나로 합쳐 필요한 만큼 성장시키고 큰 블록만 공유
                //  reservoir로 보내 스래싱을 줄이면서 'exclusive'보다는 메모리를 아낀다(공식문서 표현:
                //  "a good balance between memory usage and speed"). ⚠ 'idle'이 갖던 "temp 메모리 즉시 전량
                //  반환" 특성은 사라진다 — project_batch_memory_never_shrinks_260806(과거 30항목 배치
                //  34~41GB 미해결 이슈)와 상충 가능성이 있어 실기 배치 검사 중 WorkingSet 추이를 사용자가
                //  직접 관찰해야 한다(코드만으로 이 트레이드오프의 실제 영향은 검증 불가).
                //  스레드 범위(set_system.html / memory_management_0007.html "Switching between cache modes",
                //  로컬 설치본 재확인 완료): tsp_ 없이 호출 시 호출 스레드 즉시 적용 + 이후 시작되는 스레드에
                //  적용, 이미 실행 중인 스레드는 미적용. 이 호출은 바로 아래 Sequences = SequenceHandler.Handle
                //  (SequenceBase.MainThread 생성 지점)보다 먼저 실행되므로 이론상 이미 각 시퀀스 스레드에
                //  적용되지만, 초기화 순서 변경에도 안전하도록 SequenceBase.MainExecute() 시작부에 스레드
                //  전용(tsp_) 재적용 + 실측 로그를 추가해뒀다([MemCacheWarmup], SequenceBase.cs 참고).
                //  ※ "시도"이지 검증된 해결책이 아니다 — 근본원인은 top-release-2x-slower.md 기준 여전히
                //  미확정이며 실기 재현 확인은 사용자 몫이다.
                HOperatorSet.SetSystem("temporary_mem_cache", "aggregate");
                HOperatorSet.SetSystem("image_cache_capacity", 0);
            }
            catch (Exception ex) {
                Logging.PrintLog((int)ELogType.Error, "[STARTUP] HALCON SetSystem memory cache config failed: {0}", ex.Message);
            }
```

## 2) `WPF_Example/Sequence/Sequence/SequenceBase.cs` — 스레드별 방어적 재적용 + 실측 로그

**현재 코드 (line 257~304, `MainExecute()` 전체 + 그 뒤 `DrainCallbackQueue()`):**
```csharp
        private void MainExecute() {
            while(IsTerminated == false) {
                DrainCallbackQueue(); //260814 hbk quick-260814-warmup-thread-fix: Command/bCreated 상태 무관하게 매 iteration 드레인

                if (bCreated == false) {
                    Thread.Sleep(1000);
                    continue;
                }

                try { // 처리되지 않은 예외로 인한 스레드 종료 방지 — 예외 발생 시 Error()로 잠금 해제 보장
                    switch (Command) {
                        case ESequenceCommmand.Stop:
                            State = EContextState.Idle;
                            break;
                        case ESequenceCommmand.Pause:
                            State = EContextState.Paused;
                            break;
                        case ESequenceCommmand.Start:
                            State = EContextState.Running;
                            CurAction = Actions[CurrentActionIndex];
                            ExecuteAction(CurAction);
                            break;
                    }
                }
                catch (Exception ex) { //260517 hbk 예외 캐치 → Error() 호출로 OnError 이벤트 보장 (잠금 미해제 방지)
                    Logging.PrintErrLog((int)ELogType.Error,
                        string.Format("[MainExecute] Unhandled exception in sequence '{0}': {1}", Name, ex.Message));
                    IsDoneBegin = false;
                    try { Error(); } catch { } //260517 hbk Error() 내 2차 예외도 무시 (로그 스레드 재진입 방지)
                }
                Thread.Sleep(5);
            }
        }

        //260814 hbk quick-260814-warmup-thread-fix: CallbackQueue 에 쌓인 콜백을 이 스레드(MainThread) 위에서
        //  순서대로 실행한다. 콜백 예외는 이 시퀀스 스레드 자체를 죽이면 안 되므로 개별적으로 흡수한다.
        private void DrainCallbackQueue() {
            Action callback;
            while (CallbackQueue.TryDequeue(out callback)) {
                try {
                    callback();
                }
                catch (Exception ex) {
                    Logging.PrintErrLog((int)ELogType.Error,
                        string.Format("[MainExecute] CallbackQueue exception in sequence '{0}': {1}", Name, ex.Message));
                }
            }
        }
```

**교체 후 (`MainExecute()` 진입 즉시 1회 호출 추가 + 신규 `ReinforceThreadMemoryCache()` 메서드를 `MainExecute()`
바로 뒤, `DrainCallbackQueue()` 앞에 추가. `DrainCallbackQueue()` 자체는 무변경):**
```csharp
        private void MainExecute() {
            ReinforceThreadMemoryCache(); //260814 hbk quick-260814-kx5: 이 스레드가 HALCON을 처음 쓰기 전, 스레드 진입 시 1회만

            while(IsTerminated == false) {
                DrainCallbackQueue(); //260814 hbk quick-260814-warmup-thread-fix: Command/bCreated 상태 무관하게 매 iteration 드레인

                if (bCreated == false) {
                    Thread.Sleep(1000);
                    continue;
                }

                try { // 처리되지 않은 예외로 인한 스레드 종료 방지 — 예외 발생 시 Error()로 잠금 해제 보장
                    switch (Command) {
                        case ESequenceCommmand.Stop:
                            State = EContextState.Idle;
                            break;
                        case ESequenceCommmand.Pause:
                            State = EContextState.Paused;
                            break;
                        case ESequenceCommmand.Start:
                            State = EContextState.Running;
                            CurAction = Actions[CurrentActionIndex];
                            ExecuteAction(CurAction);
                            break;
                    }
                }
                catch (Exception ex) { //260517 hbk 예외 캐치 → Error() 호출로 OnError 이벤트 보장 (잠금 미해제 방지)
                    Logging.PrintErrLog((int)ELogType.Error,
                        string.Format("[MainExecute] Unhandled exception in sequence '{0}': {1}", Name, ex.Message));
                    IsDoneBegin = false;
                    try { Error(); } catch { } //260517 hbk Error() 내 2차 예외도 무시 (로그 스레드 재진입 방지)
                }
                Thread.Sleep(5);
            }
        }

        //260814 hbk quick-260814-kx5: 이 스레드(SequenceBase.MainThread) 전용 방어적 재적용 + 실측 로그.
        //  SystemHandler.Initialize() 의 전역 SetSystem("temporary_mem_cache","aggregate") 이 이 스레드
        //  생성(Sequences = SequenceHandler.Handle)보다 먼저 실행되므로 공식문서(HALCON Memory Management
        //  §2.3 "Switching between cache modes")상 이미 상속돼야 정상이지만, 초기화 순서가 향후 바뀌어도
        //  안전하도록 스레드 전용(tsp_) 변형으로 한 번 더 걸고, 재적용 전(inherited)/후(confirmed) 값을 모두
        //  GetSystem 으로 되읽어 로그로 남긴다 — "이론상 상속됨"이 아니라 실측으로 확인하기 위함
        //  (inherited=aggregate 면 위 상속 이론이 실측으로 확인된 것). 중복 SetSystem 호출은 멱등이라 무해하고,
        //  실패해도(캐시 힌트 실패일 뿐) 스레드 시작을 막지 않는다.
        private void ReinforceThreadMemoryCache() {
            try {
                HTuple inheritedMode;
                HOperatorSet.GetSystem("tsp_temporary_mem_cache", out inheritedMode);
                HOperatorSet.SetSystem("tsp_temporary_mem_cache", "aggregate");
                HTuple confirmedMode;
                HOperatorSet.GetSystem("tsp_temporary_mem_cache", out confirmedMode);
                Logging.PrintLog((int)ELogType.Trace,
                    "[MemCacheWarmup] seq={0} thread={1} inherited={2} confirmed={3}",
                    Name, Thread.CurrentThread.ManagedThreadId, inheritedMode.S, confirmedMode.S);
            }
            catch (Exception ex) {
                Logging.PrintErrLog((int)ELogType.Error,
                    string.Format("[MemCacheWarmup] seq={0} SetSystem/GetSystem exception: {1}", Name, ex.Message));
            }
        }

        //260814 hbk quick-260814-warmup-thread-fix: CallbackQueue 에 쌓인 콜백을 이 스레드(MainThread) 위에서
        //  순서대로 실행한다. 콜백 예외는 이 시퀀스 스레드 자체를 죽이면 안 되므로 개별적으로 흡수한다.
        private void DrainCallbackQueue() {
            Action callback;
            while (CallbackQueue.TryDequeue(out callback)) {
                try {
                    callback();
                }
                catch (Exception ex) {
                    Logging.PrintErrLog((int)ELogType.Error,
                        string.Format("[MainExecute] CallbackQueue exception in sequence '{0}': {1}", Name, ex.Message));
                }
            }
        }
```

(`HTuple`/`HOperatorSet` 은 이미 파일 상단 `using HalconDotNet;` 로 임포트돼 있고, `Thread.CurrentThread` 는
`using System.Threading;` 로 이미 임포트돼 있음 — 신규 using 불필요.)

빌드 환경(2026-08-14, 이전 quick 세션들과 동일):
- MSBuild: `C:\Program Files\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe`
- Git Bash 에서는 `-p:` 프리픽스를 쓴다(`//p:` 는 MSB1001 로 깨짐)
- 빌드에 1~2분 걸릴 수 있으니 Bash 툴 타임아웃을 300000 으로 준다
- 실행 중인 프로세스가 산출물을 잠그고 있으면(MSB3021/3026/3027/3030) **프로세스를 절대 죽이지 말 것**(프로젝트
  하드 규칙) — 스크래치 `-p:OutDir=<scratchpad>/build-verify/` 로 컴파일만 재검증하고 SUMMARY 에 기록
- Debug/x64 빌드 warning 기존 baseline = 정확히 12줄(`CS0618`×10 + `CS0162`×2). "0경고" 를 기준으로 삼지 말 것
  — 목표는 **신규 warning 0 / 신규 error 0**
</context>

<tasks>

<task type="auto">
  <name>Task 1: HALCON 문서 재확인 + SystemHandler.cs aggregate 전환 근거/트레이드오프 문서화</name>
  <files>WPF_Example/SystemHandler.cs</files>
  <action>
**실행 직전 재확인 (필수, 코드 수정보다 먼저):** 아래 두 로컬 파일을 Read/Grep으로 직접 열어, 위 context의
"HALCON 공식 문서 근거" 절 인용문이 실제로 그 파일 안에 존재하는지 스스로 재확인한다:
1. `C:\Program Files\MVTec\HALCON-24.11-Progress-Steady\doc_en_US\html\manuals\memory_management\memory_management_0007.html`
   에서 "Switching between cache modes" 문자열과 "started afterwards" 문자열이 실제로 존재하는지.
2. `C:\Program Files\MVTec\HALCON-24.11-Progress-Steady\doc_en_US\html\reference\operators\set_system.html`
   에서 `temporary_mem_cache` 항목에 `tsp_temporary_mem_cache` 변형이 함께 명시돼 있는지.
재확인 결과가 위 인용과 다르면(문서가 이 버전에서 다르게 서술돼 있으면) 즉시 작업을 멈추고 그 사실을 보고한다
(정상적으로는 이미 planner 가 확인한 내용과 일치할 것이다 — 이 재확인은 "다른 스레드에 걸었다가 하루를 날린"
사고를 반복하지 않기 위한 최종 안전장치).

재확인 후, 위 context의 "1) WPF_Example/SystemHandler.cs" 섹션에 명시된 "현재 코드"/"교체 후" 를 정확히 그대로
적용한다 — `HOperatorSet.SetSystem("temporary_mem_cache", "aggregate");` 이 줄 자체는 이미 uncommitted 로
`"aggregate"` 값이 반영돼 있으므로 **절대 다시 건드리지 않는다**(값 변경 없음). 그 줄 바로 위에 근거/스레드범위/
트레이드오프를 설명하는 `260814 hbk quick-260814-kx5:` 주석 블록만 삽입한다. 이 파일의 나머지 부분(Lights
초기화, Sequences 대입, 이후 Step 5~8 등)은 전혀 건들지 않는다. K&R 브레이스 스타일 그대로 유지, 삼항연산자
사용 금지.
  </action>
  <verify>
    <automated>F=WPF_Example/SystemHandler.cs && DOC1="/c/Program Files/MVTec/HALCON-24.11-Progress-Steady/doc_en_US/html/manuals/memory_management/memory_management_0007.html" && echo "=== [0a] 로컬 문서 재확인: 'started afterwards' 존재(>=1 기대) ===" && grep -c "started afterwards" "$DOC1" && echo "=== [0b] 로컬 문서 재확인: 'Switching between cache modes' 존재(>=1 기대) ===" && grep -c "Switching between cache modes" "$DOC1" && echo "=== [1] 새 주석 태그 존재(>=1 기대) ===" && grep -c "quick-260814-kx5" "$F" && echo "=== [2] temporary_mem_cache aggregate 정확히 1회 ===" && grep -c 'HOperatorSet.SetSystem("temporary_mem_cache", "aggregate")' "$F" && echo "=== [3] idle 잔존 0회(값 그대로 aggregate 유지 확인) ===" && grep -c 'HOperatorSet.SetSystem("temporary_mem_cache", "idle")' "$F" && echo "=== [4] 배치메모리 트레이드오프 cross-reference 존재(>=1 기대) ===" && grep -c "project_batch_memory_never_shrinks_260806" "$F" && echo "=== [5] 순서 보장: SetSystem 줄 번호 < Sequences=SequenceHandler.Handle 줄 번호 ===" && LINE_SET=$(grep -n 'HOperatorSet.SetSystem("temporary_mem_cache"' "$F" | head -1 | cut -d: -f1) && LINE_SEQ=$(grep -n 'Sequences = SequenceHandler.Handle;' "$F" | head -1 | cut -d: -f1) && echo "SetSystem=$LINE_SET SequenceHandler=$LINE_SEQ" && [ "$LINE_SET" -lt "$LINE_SEQ" ] && echo ORDER_OK || echo ORDER_FAIL && echo "=== [6] 금지 파일 무변경 ===" && git hash-object WPF_Example/DatumMeasurement.csproj && git hash-object WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs</automated>
  </verify>
  <done>
- [0a]/[0b] 둘 다 `1` 이상 — 로컬 HALCON 공식문서 인용이 실제로 존재함을 재확인.
- [1] `1` 이상, [2] 정확히 `1`, [3] 정확히 `0` — 값은 그대로 `aggregate`(무변경) + 새 주석만 추가됨.
- [4] `1` 이상 — 배치 메모리 트레이드오프 cross-reference 존재.
- [5] `ORDER_OK` — SetSystem 호출이 `Sequences = SequenceHandler.Handle;` (SequenceBase.MainThread 생성 지점)
  보다 파일 내에서 앞서 있음이 구조적으로 확인됨.
- [6] 두 해시가 각각 `761141c36ad80d58483248b8507c02e5ee0188a1` / `9f82579dc560e821b58d1d5f481639019adf52f3`
  와 동일 (baseline과 완전 일치, 이번 작업으로 변경 없음).
  </done>
</task>

<task type="auto">
  <name>Task 2: SequenceBase.cs 스레드별 방어적 재적용 + 실측 로그 + 빌드 검증</name>
  <files>WPF_Example/Sequence/Sequence/SequenceBase.cs</files>
  <action>
위 context의 "2) WPF_Example/Sequence/Sequence/SequenceBase.cs" 섹션에 명시된 "현재 코드"/"교체 후" 를 정확히
그대로 적용한다:
1. `MainExecute()` 메서드의 첫 실행문으로 `ReinforceThreadMemoryCache();` 호출 추가 — `while(IsTerminated ==
   false)` 루프보다 앞, 즉 이 스레드 진입 직후 딱 1회만 실행되도록 위치시킨다(루프 안에 넣지 않는다 — 매
   5ms마다 반복 호출하면 안 됨).
2. `MainExecute()` 메서드 바로 뒤, `DrainCallbackQueue()` 앞에 신규 private 메서드 `ReinforceThreadMemoryCache()`
   추가 — `tsp_temporary_mem_cache` 를 GetSystem 으로 먼저 읽어(`inherited`, 이 스레드가 SystemHandler.Initialize()
   의 전역 설정을 실제로 상속받았는지 실측), SetSystem 으로 `"aggregate"` 재적용, 다시 GetSystem 으로 읽어
   (`confirmed`) 두 값을 `[MemCacheWarmup]` 태그로 1줄 로그. try/catch 로 감싸 예외가 스레드 시작을 막지 않도록
   한다(`DrainCallbackQueue` 와 동일한 예외 격리 원칙).

이 파일의 나머지 부분(생성자, ExecuteAction, Start/Stop/Pause, CallbackQueue/EnqueueCallback/DrainCallbackQueue
로직 자체)은 전혀 건들지 않는다. K&R 브레이스 스타일 그대로 유지, 삼항연산자 사용 금지, 신규 using 없음(HTuple/
HOperatorSet 은 `using HalconDotNet;`, `Thread` 는 `using System.Threading;` 로 이미 임포트됨).

**절대 하지 말 것:**
- `WPF_Example/DatumMeasurement.csproj`, `WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs`
  — 열지도 말 것.
- 새 `.cs` 파일 생성 금지.
- Task 1 의 `SystemHandler.cs` 변경과 무관 — 이 Task 는 `SequenceBase.cs` 단독 수정.
  </action>
  <verify>
    <automated>F=WPF_Example/Sequence/Sequence/SequenceBase.cs && echo "=== [1] ReinforceThreadMemoryCache 메서드 정의 : 1 기대 ===" && grep -c "private void ReinforceThreadMemoryCache" "$F" && echo "=== [2] MainExecute 진입 시 1회 호출 : 1 기대 ===" && grep -c "ReinforceThreadMemoryCache();" "$F" && echo "=== [3] tsp_temporary_mem_cache SetSystem 호출 : 1 기대 ===" && grep -c 'HOperatorSet.SetSystem("tsp_temporary_mem_cache", "aggregate")' "$F" && echo "=== [4] tsp_temporary_mem_cache GetSystem 호출 : 2 기대(재적용 전/후) ===" && grep -c 'HOperatorSet.GetSystem("tsp_temporary_mem_cache"' "$F" && echo "=== [5] MemCacheWarmup 로그 태그 : 1 기대 ===" && grep -c "\[MemCacheWarmup\]" "$F" && echo "=== [6] 호출 위치가 while 루프보다 앞인지(MainExecute 본문에서 while 이전 줄에 위치) ===" && awk '/private void MainExecute\(\) \{/{f=1} f && /ReinforceThreadMemoryCache\(\);/{print "CALL_LINE_FOUND"; c=1} f && /while\(IsTerminated/{print "WHILE_LINE_FOUND"; if(c==1) print "ORDER_OK"; exit}' "$F" && echo "=== [7] 금지 파일 무변경 ===" && git hash-object WPF_Example/DatumMeasurement.csproj && git hash-object WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs && echo "=== [8] Debug/x64 빌드 ===" && "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" "WPF_Example/DatumMeasurement.csproj" -p:Configuration=Debug -p:Platform=x64 -v:minimal -nologo 2>&1 | grep -iE "error CS|error MSB|warning CS|Build succeeded"</automated>
  </verify>
  <done>
- [1]~[5] 전부 정확히 기대값(`1`,`1`,`1`,`2`,`1`).
- [6] `ORDER_OK` 출력 — `ReinforceThreadMemoryCache();` 호출이 `while(IsTerminated...` 보다 앞에 위치함(루프
  진입 전 1회만 실행됨을 구조적으로 확인).
- [7] 두 해시가 각각 `761141c36ad80d58483248b8507c02e5ee0188a1` / `9f82579dc560e821b58d1d5f481639019adf52f3`
  와 동일 (baseline과 완전 일치, 이번 작업으로 변경 없음).
- [8] `Build succeeded`, 신규 `error CS`/`error MSB` 0건, warning 은 기존 baseline 12줄(`CS0618`×10 +
  `CS0162`×2)과 정확히 동일(신규 warning 0). 산출물 잠김(MSB3021/3026/3027/3030)이면 프로세스를 죽이지 말고
  스크래치 `-p:OutDir=<scratchpad>/build-verify/` 컴파일 성공으로 대체하고 SUMMARY 에 기록.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

이번 수정은 신규 트러스트 바운더리를 만들지 않는다 — 순수 내부 HALCON 메모리 캐시 설정 + 진단 로그 추가이며,
신규 네트워크/파일/사용자 입력 표면이 없다. TCP/UI 게이트 체크 지점은 이번 수정으로 전혀 건드리지 않는다.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-kx5-01 | D (Denial of Service) | `SequenceBase.ReinforceThreadMemoryCache` | mitigate | SetSystem/GetSystem 호출을 try/catch 로 감싸 예외가 나도 로그만 남기고 스레드 시작(MainExecute 루프 진입)을 막지 않는다 — `DrainCallbackQueue` 와 동일한 예외 격리 원칙 |
| T-kx5-02 | D (Denial of Service / 자원고갈) | HALCON `temporary_mem_cache` = `aggregate` 전역 설정 | accept | `idle` 이 갖던 "temp 메모리 즉시 전량 반환" 특성이 사라져 `project_batch_memory_never_shrinks_260806`(과거 30항목 배치 34~41GB 미해결 이슈)와 상충할 가능성이 있음 — 코드로 자동 검증 불가, 사용자가 실기 배치 검사 중 WorkingSet 추이를 직접 관찰해야 함(SUMMARY에 명시). 필요시 후속으로 `alloctmp_max_blocksize` 상한 설정 가능(이번 범위 아님) |

</threat_model>

<verification>
- Task 1/Task 2 의 `<verify>` 체크 전부 통과 + Debug/x64 빌드(또는 스크래치 OutDir 컴파일) 성공.
- 두 파일(`SystemHandler.cs`, `SequenceBase.cs`) 외 다른 파일은 git diff 상 나타나지 않아야 한다(단,
  `DatumMeasurement.csproj`/`PickerCenterCalibrationService.cs` 는 사용자의 별도 미커밋 실험으로 이미 modified
  상태이며 이번 작업으로 그 상태가 조금이라도 더 바뀌면 안 된다 — baseline 해시로 확인).
- **런타임 검증(실제 measure_pos 캐시 스래싱 완화 효과)은 이 세션 범위 밖이다.** 앱을 재시작해:
  1. `D:\Data\Trace` 최신 로그에서 `[MemCacheWarmup] seq=... thread=... inherited=... confirmed=...` 라인이
     등록된 시퀀스 수만큼 나오는지, 그리고 `inherited=aggregate` 로 찍히는지 확인 — 찍히면 이번 plan이
     설명한 "전역 호출이 이미 스레드에 상속된다" 이론이 실측으로 확인된 것이다. `inherited=exclusive` (또는
     다른 값)로 찍히면 이론이 틀렸다는 뜻이지만, 그 경우에도 `confirmed=aggregate` 는 보장되므로 실제 동작에는
     영향 없다(방어적 재적용이 정확히 이 상황을 위한 것).
  2. `.planning/debug/top-release-2x-slower.md` 재현 절차(SHOT_A1-23-C1-C12 연속 실행)로 measureExec 소요시간이
     실제로 개선/안정화되는지 — **이건 사용자가 직접 확인해야 한다.** 이번 수정이 이론적으로 올바르게
     적용됐다는 것과, 실제 콜드스타트/스래싱 현상이 개선된다는 것은 별개다 — 과장 금지.
  3. 일괄검사(BatchRunService) 연속 3~5사이클 실행 중 `Process.WorkingSet64` 추이를 관찰해
     `project_batch_memory_never_shrinks_260806` 가 재발/악화되지 않는지 확인 — T-kx5-02 참고.
</verification>

<success_criteria>
- `SystemHandler.Initialize()` 의 `temporary_mem_cache` 설정이 `aggregate` 로 유지되며, 근거/스레드범위/
  트레이드오프가 `260814 hbk quick-260814-kx5:` 주석으로 문서화된다.
- 모든 `SequenceBase.MainThread` 가 진입 직후 `tsp_temporary_mem_cache` 를 방어적으로 재적용하고, 재적용 전/후
  값을 `[MemCacheWarmup]` 로그로 남긴다.
- Debug/x64 빌드 성공, 신규 error/warning 0건.
- 금지 파일(`DatumMeasurement.csproj`, `PickerCenterCalibrationService.cs`) 무변경 유지.
- SUMMARY 에 다음 3가지가 명시적으로 남는다: (a) 이것은 "시도"이지 검증된 해결책이 아니며 실기 테스트는
  사용자 몫이라는 문구, (b) `temporary_mem_cache` 가 이미 uncommitted 로 aggregate 값이 반영돼 있었다는 발견
  경위, (c) `idle`→`aggregate` 전환이 `project_batch_memory_never_shrinks_260806` 와 상충할 수 있다는 트레이드
  오프 리스크와 사용자가 관찰해야 할 지점.
</success_criteria>

<output>
After completion, create `.planning/quick/260814-kx5-halcon-temporary-mem-cache-aggregate-mea/260814-kx5-SUMMARY.md`
</output>
