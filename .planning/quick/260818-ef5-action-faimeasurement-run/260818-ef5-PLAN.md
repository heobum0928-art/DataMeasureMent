---
phase: quick-260818-ef5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: true
requirements: [REFACTOR-EF5-01, REFACTOR-EF5-02, REFACTOR-EF5-03]

must_haves:
  truths:
    - "리팩토링 후에도 Debug|x64 SIMUL 경로 빌드가 성공하고, 경고가 착수 전 baseline(측정치: CS0618×10 + CS0162×2 = 12줄)과 동일하다 — 신규 CS0219/CS0168(미사용 지역변수) 경고 0건"
    - "리팩토링 후에도 비-SIMUL(#else 실기 HW) 경로 컴파일이 성공한다 (DefineConstants=TRACE;DEBUG override, baseline 경고 10줄)"
    - "Run() 이 switch 6-case 디스패치만 남고 30줄 이하로 줄어든다 — case 본문 코드 0줄"
    - "파일 안에 [FaiTiming] 문자열이 0건 남는다 (호출 4곳 + 언급 주석 1곳 전부 소멸)"
    - "보존 대상 로그가 포맷/인자까지 그대로 살아있다 — LogSeqStep/LogSeqAlgo 총 11건, [ALGO] 1건, 그리고 [SEQ] tact 3종(DatumPhase/Grab/Measure 초)이 계속 출력된다"
    - "코드상의 삼항연산자(?:)가 0건이 된다 — 정제 grep 결과가 '기존 주석 1줄'만 남는다"
    - "RESEARCH §8-C 14개 1:1 대조 항목이 before/after 코드 대조로 전부 확인되고 SUMMARY 에 항목별 근거가 기록된다"
    - "의도적 비대칭 7건(D-1~D-7)이 리팩토링 후에도 그대로 남아 있다 — 특히 DUAL 분기에 nDatumOk/nDatumFail 이 추가되지 않았다"
    - "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs 외 어떤 파일도 1바이트도 변경되지 않는다 (csproj/SequenceBase.cs/SystemHandler.cs 무접촉)"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "switch 디스패치만 남은 Run() + case 1:1 대응 private 메서드 6개 + DatumPhase 하위 3 헬퍼 + Measure 하위 2 헬퍼"
      contains: "private void RunDatumPhase()"
  key_links:
    - from: "Run()"
      to: "RunInit / RunMoveZ / RunDatumPhase / RunGrab / RunMeasure / RunEnd"
      via: "switch ((EStep)Step) 의 각 case 에서 직접 호출 후 break"
      pattern: "case EStep\\.DatumPhase:\\s*RunDatumPhase\\(\\);"
    - from: "RunDatumPhase()"
      to: "ProcessOneDatum(...)"
      via: "foreach (var datum in parentSeq.DatumConfigs) 본문 1줄 호출 (ref 카운터 3개)"
      pattern: "ProcessOneDatum\\(datum, parentSeq, ref nDatumOk, ref nDatumFail, ref nDatumCached\\)"
    - from: "ProcessOneDatum()"
      to: "ProcessDatumDualImage / ProcessDatumSingleImage"
      via: "AlgorithmTypeEnum == VerticalTwoHorizontalDualImage 분기. DUAL 쪽은 ref 카운터를 의도적으로 받지 않는다(D-1 구조 고정)"
      pattern: "ProcessDatumDualImage\\(datum, parentSeq\\)"
    - from: "RunMeasure()"
      to: "ProcessOneMeasurement / FinalizeFaiTick"
      via: "using(image) / try{...}finally{sharedSrc.Release()} 스캐폴딩은 RunMeasure 에 남기고 루프 본문만 호출"
      pattern: "ProcessOneMeasurement\\("
---

<objective>
`WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 의 `Run()` (L106–L697, 592줄) 상태 머신을
**동작 100% 보존** 한 채 읽을 수 있는 크기로 재배치한다.

Purpose: 생산 라인 검사 판정 코드다. 사용자가 `case EStep.DatumPhase:` 블록을 붙여넣으며 "번잡하다"고 지목했고,
동시에 반복 강조했다 — **"제일 중요한건 기존 프로그램 영향을 절대 주면 안돼."** 따라서 이 작업은
"코드 개선"이 아니라 **"의미 보존 변환(behavior-preserving transformation)"** 이다. 판정/타이밍/부수효과가
1비트라도 달라지면 실패다.

Output: 같은 파일 1개. Run() 은 switch 디스패치만 남고, case 본문은 이름 있는 private 메서드로 이동한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260818-ef5-action-faimeasurement-run/260818-ef5-CONTEXT.md

**RESEARCH.md 는 @-로드하지 말 것** (26k 토큰). 필요한 표는 아래 플랜에 전부 인라인해 뒀다.
더 깊은 근거가 필요할 때만 해당 섹션만 잘라 읽는다:
`sed -n '219,290p' .planning/quick/260818-ef5-action-faimeasurement-run/260818-ef5-RESEARCH.md`  (§4 삭제/보존 판정)
`sed -n '291,376p' .planning/quick/260818-ef5-action-faimeasurement-run/260818-ef5-RESEARCH.md`  (§5 제어흐름 위험)
`sed -n '503,560p' .planning/quick/260818-ef5-action-faimeasurement-run/260818-ef5-RESEARCH.md`  (§9 추출 설계)

**착수 전 필수 확인 (30초):**
```bash
cd /c/Info/Project/DataMeasurement
git log -1 --oneline -- WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
git status --porcelain WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
```
기대값: `cb284f4 docs: Run() 상태 머신에 초보자용 단계별 설명 주석 추가` + 상태 clean(출력 없음).
**다르면 즉시 중단하고 사용자에게 보고할 것** — RESEARCH 의 모든 줄번호가 무효화된다.
</context>

<ground_rules>
## 이 플랜 전체에 적용되는 절대 규칙 (모든 task 에 동일 적용)

### G-1. 허용되는 변환은 정확히 3종뿐
1. **텍스트 이동** (cut & paste + 균일 dedent) — 메서드 추출
2. **`continue;` → `return;`** — 루프 **본문 전체**를 메서드로 추출할 때만
3. **삼항 `?:` → if-else + 지역변수 호이스팅** — 값 계산 결과가 동일한 형태로만

이 3종 외 **어떤 편집도 금지**. 아래는 전부 금지 대상이다:
- 중복 null 체크 제거 (예: `pixRes` 줄의 `ShotParam != null` 은 바깥에서 이미 보장됨 → **그대로 둔다**)
- 기존 지역변수 리네임 (`parentSeq2` → `parentSeq` 금지, `img`/`imgH`/`allPass` 등 전부 유지)
- try/catch 스타일 통일 (DUAL 은 `try{Dispose}catch{}`, 1-IMAGE 는 raw `Dispose()` — **다른 채로 둔다**)
- 방어 코드 추가 / 예외 처리 보강 / 로그 추가
- `pCamera`(dead field), `SequenceBase.LastSleepMs` 제거 — **scope creep 금지**
- C# 8+ 문법 (switch expression, `??=`, using declaration, nullable ref) — **C# 7.2 전용**
- 새 타입/튜플/ValueTuple 도입 — 누적자는 `ref`/`out` 파라미터로만 (파일 선례: `MarkAllMeasurementsNoImage(ref int)`)
- 주석 삭제 — quick-260807 / Phase 54 / Phase 57 / Phase 68 / 260810-egx / 260729-hwb / 260811 / 초보자용 주석
  **삭제 0건**, 코드와 함께 이동만 한다

### G-2. 보존해야 하는 "버그처럼 보이는" 의도적 비대칭 7건 (정리하고 싶어지는 함정)
| # | 현행 동작 | 절대 고치지 말 것 |
|---|-----------|-------------------|
| D-1 | **DUAL 분기는 `nDatumOk`/`nDatumFail` 을 전혀 증가시키지 않는다** (1-IMAGE 만 증가) | 카운터는 `[SEQ]` 로그 출력값 = 사용자가 보는 숫자. 통일하면 동작 변경 |
| D-2 | 1-IMAGE 의 `img == null` 실패도 `nDatumFail` 미증가 (검출 실패만 증가) | 동일 |
| D-3 | `LogSeqAlgo` 는 1-IMAGE 분기에만 있고 DUAL 에는 없다 | 동일 |
| D-4 | 조명 복귀(`ApplyShotLights`+`WaitForPendingWrites`)는 `DatumConfigs.Count > 0` **AND** `ShotParam != null` 일 때만 | 물리 조명 side-effect. 밖으로 빼면 Datum 0개 Shot 의 조명이 바뀐다 |
| D-5 | `Step = (int)EStep.Measure;` 는 `if (ShotParam != null && !ShotParam.HasImage)` **블록 밖** | 안에 넣으면 이미 이미지 있는 Shot 이 Grab 에 갇힌다 |
| D-6 | `pMyContext.AllPass/MeasuredCount/InspectionOverlays` 대입은 `if (ShotParam != null)` **밖** | 안에 넣으면 ShotParam==null 일 때 판정이 바뀐다 |
| D-7 | Measure 의 로컬명이 `parentSeq2` (DatumPhase 는 `parentSeq`) | 리네임하면 diff 가 "이동"에서 "수정"으로 바뀌어 1:1 대조가 불가능해진다 |

### G-3. `[ALGO]` 로그는 영구 기능이다 — 삭제 금지 (오케스트레이터 확정)
`Logging.PrintLog((int)ELogType.Algorithm, "[ALGO] {0} · {1} type={2} → {3} ({4}) {5}ms", ...)` 는
오늘 별개 작업("알고리즘 탭 분리", 커밋 `0522ff5`)으로 추가된 **영구 기능**이지 타이머 진단 계측이 아니다.
이 로그와, 이 로그가 소비하는 `swMeasureExec` 는 **둘 다 보존**한다.
(`swMeasureExec` 선언줄에 `//TEMP 계측` 인라인 주석이 붙어 있는 게 함정이다 — 주석 문구만 고치고 변수는 살린다.)

### G-4. 컨텍스트 규율
- 1,646줄 파일이다. **전체 읽기 금지.** `sed -n 'A,Bp'` 로 필요한 구간만 읽는다.
- 큰 블록 이동은 **손으로 재입력하지 말고 스크립트로 옮긴다** (Task 2/3 참조). 바이트 동일성이 검증의 핵심이다.
- **각 task 끝에서 커밋한다.** 다음 task 의 1:1 대조 기준점이 되므로 생략 금지.

### G-5. 빌드 규칙 (RESEARCH §8-A 실측 기준)
- 앱이 `D:\Data\` 에서 실행 중일 수 있다 → **절대 프로세스를 종료하지 말 것.** 반드시 스크래치 `OutputPath` 로 컴파일만 검증.
- **`//p:` 금지, `-p:` 사용** (경로에 `/` 가 섞이면 Git Bash 가 `MSB1001` 로 죽는다). 세미콜론은 `%3B`.
- `Release|AnyCPU` 로 비-SIMUL 검증 금지 (`AllowUnsafeBlocks` 없어 `MilCamera.cs CS0227` 로 원래 실패).
- 비-SIMUL 빌드를 돌린 뒤에는 **반드시 SIMUL 빌드를 `-t:Rebuild` 로 다시 돌려 `obj/x64/Debug/` 를 원복**한다
  (classic csproj 는 DefineConstants 변경을 증분추적 못 해, 사용자의 다음 빌드가 비-SIMUL 바이너리를 재사용할 위험).

```bash
MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"
SCR="C:\\Users\\tech\\AppData\\Local\\Temp\\claude\\C--Info-Project-DataMeasurement\\9d3a7b4d-2314-4b14-8686-52fd6346a1f9\\scratchpad"

# ① SIMUL 경로 (기본) — 기대: 성공, 경고 12줄 (CS0618x10 + CS0162x2)
"$MSB" 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' \
  -p:Configuration=Debug -p:Platform=x64 -p:OutputPath="$SCR\\ef5-simul\\" \
  -t:Rebuild -v:minimal -nologo

# ② 비-SIMUL(#else 실기 HW 분기) — 기대: 성공, 경고 10줄 (CS0618x10)
"$MSB" 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' \
  -p:Configuration=Debug -p:Platform=x64 -p:OutputPath="$SCR\\ef5-nosimul\\" \
  -p:DefineConstants="TRACE%3BDEBUG" -t:Rebuild -v:minimal -nologo

# ③ 원복 (②를 돌렸으면 필수)
"$MSB" 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' \
  -p:Configuration=Debug -p:Platform=x64 -p:OutputPath="$SCR\\ef5-simul\\" \
  -t:Rebuild -v:minimal -nologo
```
빌드가 파일 잠김으로 실패하면 스크래치 OutputPath 를 새 이름으로 바꿔 재시도하고, 그래도 안 되면
**프로세스를 죽이지 말고 사용자에게 보고**한다.
</ground_rules>

<tasks>

<task type="auto">
  <name>Task 1: [FaiTiming] 진단 계측 완전 제거 + //TEMP 주석 정리 + 삼항 10개 → if-else (코드 이동 0줄)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <action>
**왜 이걸 먼저 하는가 (순서가 중요한 구조적 이유):**
삭제 대상 지역변수 6개(`msApplyDatumLights`, `msWaitForPendingWrites`, `msDatumGrab`, `swDatumDetect`,
`msMeasureExec`, `msSaveQueue`)는 **선언 위치와 소비 위치가 앞으로 만들 헬퍼 메서드의 경계를 가로지른다.**
예: `msApplyDatumLights` 는 per-datum 루프 본문(→ `ProcessOneDatum`)에서 선언되는데, 소비는
1-IMAGE 분기(→ `ProcessDatumSingleImage`)의 `[FaiTiming] DatumDetail` 로그다.
먼저 지우지 않으면 Task 3 의 시그니처에 파라미터 6개가 불필요하게 늘어붙는다. **반드시 Task 1 → 2 → 3 순서.**

**0단계 — baseline 경고 목록 캡처 (기억이 아니라 실측을 기준으로 삼는다):**
G-5 의 ①·② 빌드를 착수 전 상태에서 한 번씩 돌려 경고 줄을 스크래치 파일에 저장한다
(`$SCR/ef5-baseline-simul.txt`, `$SCR/ef5-baseline-nosimul.txt`). 이후 모든 비교는 이 파일 기준.
②를 돌렸으므로 ③ 원복까지 수행할 것.

**1단계 — `[FaiTiming]` 호출 4곳 삭제 (선행 //TEMP 주석 포함).** 아래로 갈수록 줄번호가 밀리므로
**아래(큰 줄번호)에서 위로** 편집하거나 앵커 문자열로 찾는다.

| # | 앵커 (grep 문자열) | 삭제 범위 |
|---|---------------------|-----------|
| F-4 | `stage=Measure measuredCount=` | L679–682 PrintLog 전체 + 끝의 인라인 `//TEMP 계측(...)` |
| F-3 | `stage=Grab acquire=` | L405–407 PrintLog 전체 |
| F-2 | `stage=Datum total=` | L337–339 PrintLog + 바로 위 L337 `//TEMP` 주석 |
| F-1 | `stage=DatumDetail` | L303–305 PrintLog + 바로 위 L302 `//TEMP` 주석 |

**2단계 — 삭제 전용 지역변수 제거 (선언 + 모든 대입/누적/사용).** 아래 목록이 전부다:

`swDatumLight`, `msApplyDatumLights`, `swDatumLightWait`, `msWaitForPendingWrites`,
`swDatumGrab`, `msDatumGrab`, `swDatumDetect`,
`swAcquire`, `msAcquire`, `swDisplayCopy`, `msDisplayCopy`,
`msMeasureExec`, `msSaveQueue`, `swSaveQueue`, `swSaveQueueCrossZ`,
`szMeasureShotName`(3줄 블록), `dLastSleepMs`, `measureSeq`

세부 주의:
- `swDatumDetect` 삭제 시 **바로 다음 줄의 `try {` 는 남긴다** (1-IMAGE 분기의 finally-Dispose 계약).
- `swAcquire` 는 `#if SIMUL_MODE` **위**에서 선언되고 `#endif` **아래**(`msAcquire = swAcquire.ElapsedMilliseconds;`)
  에서 소비된다 — 두 줄 다 지우되 `#if/#else/#endif` 구조는 손대지 않는다.
- `msSaveQueue` 는 `+=` 누적이 2곳(`swSaveQueue`, `swSaveQueueCrossZ`)이다.
- `dLastSleepMs`/`measureSeq` 를 지워도 `SequenceBase.LastSleepMs` **정의는 건드리지 않는다** (public
  auto-property 라 미사용 경고 안 남).

**3단계 — 보존 변수는 반드시 살린다 (⚠ 최대 함정).**
`swDatumPhase`, `swGrabTotal`, `swMeasureTotal`, `swMeasureExec` 4개 + `nDatumOk`/`nDatumFail`/`nDatumCached`,
`dctAlgoUsed`, `nMeasNg`, `sbAlgo`.
앞 3개는 `[SEQ]` tact(`{N:F2}초`)의 유일한 소스이고, `swMeasureExec` 는 `[ALGO]` 로그가 소비한다(G-3).

**4단계 — `//TEMP` 주석 4곳은 삭제가 아니라 문구 교체** (아래에 보존 대상 Stopwatch 가 있어서 지우면
정체불명 변수가 남는다):

| 위치 | 교체 후 문구(예시, 의미만 맞으면 됨) |
|------|--------------------------------------|
| `swDatumPhase` 위 3줄 블록(L146–148) | `//260818 hbk [SEQ] DatumPhase 단계 tact 측정용 — 아래 LogSeqStep("DatumPhase", "완료 …") 가 소비한다.` |
| `swGrabTotal` 위(L352) | `//260818 hbk [SEQ] Grab 단계 tact 측정용 — 아래 LogSeqStep("Grab", …) 가 소비한다.` |
| `swMeasureTotal` 위(L423) | `//260818 hbk [SEQ] Measure 단계 tact 측정용 — 아래 LogSeqStep("Measure", "완료 …") 가 소비한다.` |
| `swMeasureExec` 인라인(L568) | `//260818 hbk [ALGO] 로그용 측정 실행시간` |

나머지 `//TEMP` 주석(L176/178/182/184/252/254/266/302/337/610/623/660/664/682)은 해당 코드와 함께 소멸한다.

**5단계 — 삼항 10개 → if-else.** 아래가 전량이다(삭제 대상 2개는 위에서 이미 사라짐):

| # | 줄 | 표현 | 변환 |
|---|-----|------|------|
| T-1/T-2 | L121 | `ShotParam != null ? ShotParam.ZIndex : -1` / `… ? ShotParam.DelayMs : 0` | `int nLogZIndex; int nLogDelayMs;` 를 LogSeqStep 위에 선언 후 if-else |
| T-3 | L155 | `parentSeq != null ? parentSeq.DatumConfigs.Count : 0` | `int nDatumRegistered;` if-else |
| T-6 | L420 | `ShotParam != null && ShotParam.FAIList != null ? ShotParam.FAIList.Count : 0` | `int nFaiCount;` if-else |
| T-7 | L453 | `ShotParam != null ? ShotParam.GetEffectivePixelResolution() : 1.0` | `double pixRes;` 선언 후 if-else. **`GetEffectivePixelResolution()` 호출은 정확히 1회만** 남아야 한다 |
| T-8 | L587 | `ShotParam != null ? ShotParam.ShotName : "?"` | `string szAlgoShotName;` if-else ([ALGO] 로그는 보존) |
| T-9 | L589 | `ok ? "OK" : "FAIL"` | `string szAlgoResult;` if-else |
| T-10 | L677 | `allPass ? "OK" : "NG"` | `string szMeasureVerdict;` if-else |
| T-11 | L678 | `sbAlgo.Length > 0 ? sbAlgo.ToString() : "없음"` | `string szAlgoSummary;` if-else |
| T-12 | L1303 | `parentSeq2 != null ? parentSeq2.GetExecutionZIndex() : UNSET_ZINDEX` | `int nCurZ;` if-else. **`GetExecutionZIndex()` 호출 1회 유지.** `MarkMeasurementCrossZIncomplete` 내부(Run() 밖)지만 파일 전체 삼항 0 목표에 포함 |

- 새로 만드는 지역변수만 헝가리언(`n`/`sz`/`b`/`d`) 적용. **기존 변수명은 절대 변경 금지**(G-1).
- ⚠ 오탐 금지: `L75`/`L76`/`L92`/`L580` 의 `"?"` 는 문자열 리터럴, `L1208` 의 `?:` 는 주석 텍스트다. 손대지 말 것.

**6단계 — 커밋.** 메시지: `refactor(260818-ef5): [FaiTiming] 임시 진단 계측 제거 + 삼항 10개 if-else 전개 (동작 무변경)`
  </action>
  <verify>
    <automated>
cd /c/Info/Project/DataMeasurement && F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs && \
echo "== R3: FaiTiming(기대 0) ==" && [ "$(grep -c 'FaiTiming' $F)" = "0" ] && \
echo "== R2: 삼항 잔여(기대 1줄=L1208 주석) ==" && [ "$(grep -nE '\?[^?:]*:' $F | grep -vE '\?\?|\?\.' | wc -l)" = "1" ] && \
echo "== R4: [ALGO] 보존(기대 1) ==" && [ "$(grep -c '\[ALGO\]' $F)" = "1" ] && \
echo "== R4: swMeasureExec 보존(기대 2=선언+ALGO소비) ==" && [ "$(grep -c 'swMeasureExec' $F)" = "2" ] && \
echo "== R4: LogSeq* 보존(기대 11) ==" && [ "$(grep -c 'LogSeqStep\|LogSeqAlgo' $F)" = "11" ] && \
echo "== 삭제변수 전멸(기대 0) ==" && [ "$(grep -cE 'swDatumLight|msApplyDatumLights|swDatumLightWait|msWaitForPendingWrites|swDatumGrab|msDatumGrab|swDatumDetect|swAcquire|msAcquire|swDisplayCopy|msDisplayCopy|msMeasureExec|msSaveQueue|swSaveQueue|swSaveQueueCrossZ|szMeasureShotName|dLastSleepMs|measureSeq' $F)" = "0" ] && \
echo "== 보존 Stopwatch 3종(각 2회 이상) ==" && [ "$(grep -c 'swDatumPhase' $F)" -ge 2 ] && [ "$(grep -c 'swGrabTotal' $F)" -ge 2 ] && [ "$(grep -c 'swMeasureTotal' $F)" -ge 2 ] && \
echo "== 대상 외 파일 무변경(기대 1줄) ==" && [ "$(git diff --name-only | wc -l)" = "1" ] && \
echo "ALL STATIC CHECKS PASS"
    </automated>
    <automated>G-5 ① SIMUL 빌드 → 성공 + 경고가 $SCR/ef5-baseline-simul.txt 와 동일(12줄). 신규 CS0219/CS0168(미사용 지역변수) 경고가 1건이라도 생기면 변수 삭제가 불완전한 것이므로 FAIL 처리하고 수정</automated>
    <automated>G-5 ② 비-SIMUL 빌드 → 성공 + baseline-nosimul.txt 와 동일(10줄). 이어서 ③ 원복 빌드 실행</automated>
  </verify>
  <done>
`[FaiTiming]` 0건, 코드 삼항 0건, `[ALGO]`/`swMeasureExec`/`LogSeq*` 11건 보존,
삭제 대상 18개 변수 전멸, 빌드 2종 baseline 동일, 변경 파일 1개, 커밋 완료.
**코드는 단 한 줄도 이동하지 않았다** (`git diff` 에 대규모 이동 hunk 가 없어야 함).
  </done>
</task>

<task type="auto">
  <name>Task 2: switch 6-case 본문 → RunInit/RunMoveZ/RunDatumPhase/RunGrab/RunMeasure/RunEnd 추출 (바이트 동일 이동)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <action>
**Task 1 커밋 SHA 를 변수로 잡아둔다** — 이 task 의 1:1 대조 기준점이다:
`T1=$(git rev-parse HEAD)`

**⚠ Task 1 로 줄번호가 전부 밀렸다.** RESEARCH 의 줄번호를 그대로 쓰지 말고 앵커로 재탐색한다:
`grep -n 'case EStep\.\|public override ActionContext Run()\|return Context;' $F`

**추출 방식 — 손으로 재입력하지 말고 스크립트로 옮긴다.**
스크래치에 파이썬 스크립트를 하나 써서 (a) 각 case 본문 줄범위를 잘라내고 (b) 균일하게 8칸(=2단계)
dedent 한 뒤 (c) 새 메서드 본문으로 붙이고 (d) 원래 자리에는 호출 1줄만 남긴다.
목적은 편의가 아니라 **바이트 동일성 보장** 이다 — 아래 verify 가 그걸 기계적으로 확인한다.

**추출 규칙:**

| 새 메서드 | 원본 case | 특이사항 |
|-----------|-----------|----------|
| `private void RunInit()` | `case EStep.Init:` | 4줄 |
| `private void RunMoveZ()` | `case EStep.MoveZ:` | `#if SIMUL_MODE`/`#else`/`#endif` 블록 **전체**가 이 메서드 안에 들어와야 한다 |
| `private void RunDatumPhase()` | `case EStep.DatumPhase: { … }` | case 스코프 `{ }` 는 추출과 함께 소멸. foreach 는 통째로 안에 남는다 |
| `private void RunGrab()` | `case EStep.Grab:` | `#if SIMUL_MODE` 블록 전체 포함. `Step = (int)EStep.Measure;` 는 **`if (ShotParam != null && !ShotParam.HasImage)` 블록 밖에 그대로**(D-5) |
| `private void RunMeasure()` | `case EStep.Measure: { … }` | 이번 task 에서는 **내부를 더 쪼개지 않는다**. 통째로 옮기기만 한다 |
| `private void RunEnd()` | `case EStep.End:` | `EContextResult finishResult;` 가 switch 스코프 → 메서드 지역변수로 내려온다 |

- **`break;` 는 switch 에 남긴다.** 추출 메서드 안으로 들어가면 컴파일 에러다.
- `Step = (int)EStep.…;` 대입은 그대로 메서드 안으로 이동해도 동일하다 —
  `Step` 은 `ActionBase:22` 의 `Context.CurrentStep` 프록시 프로퍼티다. **지역변수로 캐싱 금지.**
- `return Context;` 는 `Run()` 끝에 그대로 남긴다.
- **brace 스타일은 K&R** (`private void RunX() {`). 근거: 이 파일 39개 멤버 중 31개가 K&R이고,
  옮겨오는 본문이 전부 K&R 이라 재들여쓰기 없이 이동해 diff 가 "이동"으로만 남는다.
- **각 case 위의 초보자용 설명 주석**(`//260818 hbk [초보자용] …`)과 그 위의 기존 WHY 주석
  (quick-260807, Phase 68 등)은 **추출된 메서드 선언 바로 위로 함께 이동**한다. 삭제 0건.
  `Run()` 메서드 위의 전체 개요 주석(8줄)은 `Run()` 위에 그대로 둔다.
- **배치 순서:** `Run()` 바로 아래에 `RunInit` → `RunMoveZ` → `RunDatumPhase` → `RunGrab` → `RunMeasure` → `RunEnd`.
  기존 헬퍼군(`LoadShotInspectionImage` 이하)은 **한 줄도 이동시키지 않는다.**

**최종 `Run()` 형태 (약 15~25줄, 주석 제외):**
```csharp
public override ActionContext Run() {
    switch ((EStep)Step) {
        case EStep.Init:       RunInit();       break;
        case EStep.MoveZ:      RunMoveZ();      break;
        case EStep.DatumPhase: RunDatumPhase(); break;
        case EStep.Grab:       RunGrab();       break;
        case EStep.Measure:    RunMeasure();    break;
        case EStep.End:        RunEnd();        break;
    }
    return Context;
}
```

**커밋:** `refactor(260818-ef5): Run() switch 6-case 본문을 RunXxx() private 메서드로 추출 (순수 이동)`
  </action>
  <verify>
    <automated>
# 바이트 동일 이동 검증: 6개 case 각각에 대해, Task1 커밋의 case 본문과
# 현재 파일의 새 메서드 본문을 "앞쪽 공백 제거 + 빈줄 제거" 정규화 후 diff 한다.
# 허용되는 차이는 오직: 끝의 `break;` 소멸, DatumPhase/Measure 의 case 스코프 `{`/`}` 소멸.
# 그 외 한 줄이라도 다르면 FAIL — 사람이 눈으로 브레이스 매칭하지 말 것.
cd /c/Info/Project/DataMeasurement && F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
norm() { sed 's/^[[:space:]]*//; s/[[:space:]]*$//' | grep -v '^$'; }
git show $T1:$F | sed -n '<case본문 시작>,<끝>p' | norm > /tmp/before_datum.txt
sed -n '<새 RunDatumPhase 본문 범위>p' $F      | norm > /tmp/after_datum.txt
diff /tmp/before_datum.txt /tmp/after_datum.txt   # 기대: break; / 스코프 브레이스 외 차이 0
    </automated>
    <automated>
# 주의: case 개수는 반드시 주석 오탐을 배제한 정규식으로 센다.
#   단순 `grep -c 'case EStep\.'` 는 9 를 리턴한다 — 실제 case 라벨 6개 +
#   같은 문자열이 문장 안에 들어있는 보존 주석 3줄(Run() 위 개요 주석 / MoveZ 초보자용 주석 /
#   quick-260702 이력 주석). 이 3줄은 G-1 "주석 삭제 0건"에 따라 추출 전후 모두 살아남으므로
#   "=6" 으로 비교하면 정상 추출에서도 항상 false-fail 한다(추출 전 실측도 9).
#   아래 정규식은 "case 가 줄의 첫 비공백 토큰"인 줄만 세므로 `//` 주석을 자동 제외하고,
#   들여쓰기 깊이가 바뀌어도 깨지지 않는다. (착수 전 실측 = 6, 추출 후에도 6)
cd /c/Info/Project/DataMeasurement && F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs && \
echo "== 신규 메서드 6개 ==" && [ "$(grep -cE 'private void Run(Init|MoveZ|DatumPhase|Grab|Measure|End)\(\)' $F)" = "6" ] && \
echo "== case 6개 유지 ==" && [ "$(grep -cE '^[[:space:]]*case EStep\.[A-Za-z]+:' $F)" = "6" ] && \
echo "== Run() 본문 30줄 이하 ==" && [ "$(awk '/public override ActionContext Run\(\) \{/,/^        \}$/' $F | wc -l)" -le 30 ] && \
echo "== #if SIMUL_MODE 짝 유지 ==" && [ "$(grep -c '#if SIMUL_MODE' $F)" = "$(grep -c '#endif' $F)" ] && \
echo "== 회귀 가드: Task1 결과 유지 ==" && [ "$(grep -c 'FaiTiming' $F)" = "0" ] && [ "$(grep -c 'LogSeqStep\|LogSeqAlgo' $F)" = "11" ] && [ "$(grep -c '\[ALGO\]' $F)" = "1" ] && \
echo "== 변경 파일 1개 ==" && [ "$(git diff --name-only $T1 | wc -l)" = "1" ] && \
echo "TASK2 STATIC PASS"
    </automated>
    <automated>G-5 ① SIMUL 빌드 성공 + 경고 baseline(12줄) 동일 / ② 비-SIMUL 빌드 성공 + baseline(10줄) 동일 / ③ 원복 실행. 특히 ②가 `#if SIMUL_MODE` 블록 이동의 유일한 검증 수단이다</automated>
  </verify>
  <done>
`Run()` 이 switch 디스패치 + `return Context;` 만 남아 30줄 이하. 신규 private 메서드 6개.
6개 case 본문 전부 정규화 diff 에서 `break;`/스코프 브레이스 외 차이 0.
빌드 2종 baseline 동일. Task 1 검증 결과(FaiTiming 0 / LogSeq 11 / ALGO 1) 회귀 없음. 커밋 완료.
  </done>
</task>

<task type="auto">
  <name>Task 3: DatumPhase 3-헬퍼 + Measure 2-헬퍼 추출 (continue→return) + RESEARCH §8-C 14항목 1:1 대조</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <action>
`T2=$(git rev-parse HEAD)` 로 Task 2 커밋을 기준점으로 잡는다.

**추출 전 반드시 확인할 구조적 전제 (확인 안 되면 중단):**
1. `foreach (var datum in …DatumConfigs)` 본문에서 DUAL/1-IMAGE `if-else` **뒤에 아무 문장도 없다**
   (if-else 가 끝나면 곧바로 foreach 가 닫힌다). → 그래서 하위 헬퍼에서 `return` 해도 상위와 동치다.
2. `foreach (var meas in fai.Measurements)` 본문 뒤에는 per-FAI `try{…}finally{crossZRoleImage 정리}` 가 온다.
   → `ProcessOneMeasurement` 는 **meas 루프 본문까지만**, 그 뒤는 `FinalizeFaiTick`.

**5-A. 추출 단위 규칙 (위반 시 조용한 동작 변경):**
- **루프 본문 "전체"만 추출한다. 부분 추출 절대 금지.** 본문 일부만 빼내면 `continue` 가 메서드 경계를
  넘지 못해, `bool` 반환으로 우회하다 흐름이 미묘하게 바뀐다.
- 루프 본문 전체를 옮길 때만 **`continue;` → `return;`** 이 1:1 동치다. 변환 대상은 정확히 12곳
  (DatumPhase 6 / Measure 6). **개수를 세어 확인할 것.**
- `try{}finally{}` 블록은 통째로 이동한다. `finally` 안의 Dispose 는 `continue`→`return` 후에도
  그대로 실행된다(동치) — 단 **try 시작점/끝점을 옮기지 말 것.**

**5-B. 새 메서드 시그니처 (Option A — 새 타입 0개, `ref` 누적자. 파일 선례 `MarkAllMeasurementsNoImage(ref int)`):**
```csharp
private void ProcessOneDatum(DatumConfig datum, InspectionSequence parentSeq,
                             ref int nDatumOk, ref int nDatumFail, ref int nDatumCached);
private void ProcessDatumDualImage(DatumConfig datum, InspectionSequence parentSeq);   // ⚠ ref 카운터 없음 = 의도
private void ProcessDatumSingleImage(DatumConfig datum, InspectionSequence parentSeq,
                             ref int nDatumOk, ref int nDatumFail);
private void ProcessOneMeasurement(<meas 타입> meas, InspectionSequence parentSeq2,
                             HImage image, double pixRes,
                             ref HImage crossZRoleImage, ref bool faiAllPass,
                             ref int measuredCount, ref int nMeasNg,
                             List<EdgeInspectionOverlay> overlayAcc,
                             List<EdgeInspectionOverlay> faiOverlays,
                             Dictionary<string, int> dctAlgoUsed);
private void FinalizeFaiTick(<fai 타입> fai, bool faiAllPass,
                             List<EdgeInspectionOverlay> faiOverlays, SharedHImage sharedSrc,
                             List<DatumCaptureOverlay> datumSnapshot, string szSharedOriginPath,
                             InspectionSequence parentSeq2,
                             ref HImage crossZRoleImage, ref bool bShotDisplayImageReplaced,
                             ref bool allPass);
```
(실제 `meas`/`fai` 타입은 현재 선언에서 그대로 가져온다. 파라미터가 많은 건 의도 —
새 누적자 클래스를 도입하면 필드 초기화 누락이 **조용한 판정 오류**로 이어질 수 있어 무회귀 원칙과 충돌한다.)

> ⚠ **`ProcessDatumDualImage` 에 `ref int nDatumOk/nDatumFail` 을 넣지 않는 것이 이 설계의 핵심이다.**
> 현행 DUAL 분기는 카운터를 전혀 건드리지 않는다(G-2 D-1). 시그니처에서 아예 접근 불가하게 만들어,
> 나중에 누가 "카운터를 빼먹었네" 하고 추가하는 사고를 구조적으로 차단한다.

**5-C. 리소스 수명 계약 — 메서드 경계를 넘기면 안 되는 것들:**
| 대상 | 계약 |
|------|------|
| `using (var image = ShotParam.GetImage())` | `GetImage()` 는 **호출자 소유 clone** 반환(`ShotConfig.cs:392`). `using` 은 **`RunMeasure()` 안에 남긴다**. `ProcessOneMeasurement` 는 `image` 를 **절대 Dispose 하지 않는다** |
| `sharedSrc` (SharedHImage) | 생성은 try **밖**, `try` 시작점은 sharedSrc 생성 **직후**(260810 round4 fix), `finally { sharedSrc.Release(); }`. **이 스캐폴딩 전체가 `RunMeasure()` 에 남는다.** try 시작점을 되돌리면 refcount 누수 재발 |
| `crossZRoleImage` | per-FAI 소유. `ref HImage` 로 전달. 대입은 `ProcessOneMeasurement`, 정리는 `FinalizeFaiTick` 의 finally |
| `crossZSharedSrc` | 중첩 try/finally 통째로 `FinalizeFaiTick` 안으로 |
| `imgH`/`imgV` | 선언/try/finally(각각 try-catch swallow) 통째로 `ProcessDatumDualImage` 안으로 |
| `img` | 선언/try/finally(try-catch **없이** raw Dispose) 통째로 `ProcessDatumSingleImage` 안으로. **DUAL 과 스타일이 다른 채로 둔다** |

**5-D. 배치:** `RunDatumPhase` 바로 아래에 `ProcessOneDatum` → `ProcessDatumDualImage` → `ProcessDatumSingleImage`,
`RunMeasure` 바로 아래에 `ProcessOneMeasurement` → `FinalizeFaiTick`. 기존 헬퍼군은 무이동.

**5-E. 마지막으로 RESEARCH §8-C 14항목 1:1 대조를 수행한다.**
"빌드 됐으니 OK" 는 근거로 인정하지 않는다. 각 항목마다 `git show $T1:$F` 의 해당 구간과 현재 파일을
나란히 놓고 확인하고, **SUMMARY.md 에 항목별 판정 + 근거(줄번호 또는 grep 결과)를 표로 기록**한다:

1. **호출 순서**: `ApplyDatumLights` → `WaitForPendingWrites` → (분기) → 검출 → `ApplyShotLights` → `WaitForPendingWrites` 불변
2. **조명 복귀 조건**: `ApplyShotLights` 가 여전히 `DatumConfigs.Count > 0` **AND** `ShotParam != null` 아래에만 (D-4)
3. **캐시 스킵 위치**: `HasCachedDatumTransform` 스킵이 여전히 `ApplyDatumLights` **이전**
4. **`Mark*` 호출 7곳**: **MarkDatumFailed 5곳 / MarkAlignFailed 2곳** 이 전부 동일 조건·동일 순서.
   판정 기준은 **"총 7곳"이라는 개수와 아래 분기 대응**이다. (착수 전 실측 줄번호 = MarkDatumFailed
   L194/L217/L244/L261/L294, MarkAlignFailed L233/L278 — 단 **Task 1/2 로 줄번호가 전부 밀리므로
   숫자를 대조 기준으로 쓰지 말 것.** 분기 이름으로 찾아간다.)
   RESEARCH §5 분기표 1:1 대응 — 아래 7개가 전량이다:
   - `f1` DUAL · `IsDatumZIndexMisconfigured` 게이트 → **MarkDatumFailed**
   - `f3` DUAL · `TryGrabOrLoadDualDatumImages` 실패 중 `bPending == false` 경로만 → **MarkDatumFailed**
     (`bPending == true` 는 Mark 호출 없음 — 실패 아님. 여기를 실패로 세지 말 것)
   - `f4a` DUAL · align ON · `TryComposeAlign` 실패 → **MarkAlignFailed**
   - **`f4b` DUAL · align OFF(`else`) · `TryRunSingleDatum` 실패 → MarkDatumFailed**
   - `g2` 1-IMAGE · `img == null` → **MarkDatumFailed**
   - `g4a` 1-IMAGE · align ON · `TryComposeAlign` 실패 → **MarkAlignFailed**
   - `g4b` 1-IMAGE · align OFF(`else`) · `TryRunSingleDatum` 실패 → **MarkDatumFailed**

   ⚠ **빠뜨리기 쉬운 건은 `f4b` — DUAL 분기의 비-align `else` 경로에 있는 5번째 MarkDatumFailed 다.**
   "4곳이겠지" 하고 4개만 확인하고 멈추면 `f4b` 가 감사되지 않은 채 통과한다.
   f1 / f3 / f4a / f4b 는 전부 같은 DUAL 블록 안에 있어 추출 후 `ProcessDatumDualImage` 로 들어가고,
   g2 / g4a / g4b 는 `ProcessDatumSingleImage` 로 들어간다 (5-B 시그니처 기준 3 : 4 로 갈린다).

   기계 확인 (추출 전·후 동일하게 성립 — 헬퍼 시그니처의 파라미터명도 `parentSeq` 로 유지되므로):
   ```bash
   grep -c 'parentSeq\.MarkDatumFailed(' $F   # 5
   grep -c 'parentSeq\.MarkAlignFailed(' $F   # 2
   ```
   `parentSeq.` 접두사가 필수다 — 접두사 없이 세면 주석 안의 언급 4곳(L207 / L800 / L804 / L900)까지
   함께 잡혀 9가 나온다.
5. **카운터 비대칭 유지** (D-1/D-2/D-3): DUAL 분기에 `nDatumOk`/`nDatumFail` 이 **추가되지 않았는가**
6. **`LogSeqAlgo` 위치**: 여전히 1-IMAGE 분기에만, 성공/실패 무관 항상 호출 (2곳)
7. **Step 전이 6개**: Init→MoveZ / MoveZ→DatumPhase / DatumPhase→(bDatumOnly? End : Grab) / Grab→Measure(무조건) / Measure→End / End→FinishAction (D-5)
8. **Dispose 순서**: `imgH`→`imgV` / `img` / `crossZSharedSrc` / `crossZRoleImage` / `sharedSrc.Release()` / `image`(using) / `pMyContext.ResultHalconImage` — 전부 동일 finally 안 동일 순서
9. **`try` 시작점**: `sharedSrc` 생성 **직후**인가 (260810 round4 fix 보존)
10. **`using (var image = …)`** 가 `RunMeasure()` 안에 있고, 추출 메서드가 `image` 를 Dispose 하지 않는가
11. **`#if SIMUL_MODE` 2블록**이 각각 한 메서드 안에 온전히 들어 있는가
12. **`pMyContext.AllPass/MeasuredCount/InspectionOverlays` 대입**이 여전히 `if (ShotParam != null)` **밖**인가 (D-6)
13. **보존 Stopwatch 4개**가 살아 있고 소비 로그 4곳이 동일 포맷으로 출력되는가
14. **WHY 주석 이동 확인 — 삭제 0건**: quick-260807 / Phase 54 / Phase 57 / Phase 68 / 260810-egx / 260729-hwb / 260811 / 초보자용
    (`git show $T1:$F | grep -c '260807\|Phase 54\|Phase 57\|Phase 68\|260810\|260729\|260811\|초보자용'` 과 현재 파일 카운트 비교 → 동일하거나 증가)

**커밋:** `refactor(260818-ef5): DatumPhase/Measure 루프 본문을 헬퍼로 추출 (continue→return 동치, 동작 무변경)`
  </action>
  <verify>
    <automated>
cd /c/Info/Project/DataMeasurement && F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs && \
echo "== 신규 헬퍼 5개 ==" && [ "$(grep -cE 'private void (ProcessOneDatum|ProcessDatumDualImage|ProcessDatumSingleImage|ProcessOneMeasurement|FinalizeFaiTick)\(' $F)" = "5" ] && \
echo "== D-1 구조 고정: DUAL 헬퍼에 ref 카운터 없음 ==" && [ "$(sed -n '/private void ProcessDatumDualImage(/,/^        }$/p' $F | grep -c 'nDatumOk\|nDatumFail')" = "0" ] && \
echo "== continue 12개가 return 으로 전환 ==" && [ "$(sed -n '/public override ActionContext Run()/,/private void FinalizeFaiTick/p' $F | grep -c 'continue;')" = "0" ] && \
echo "== ProcessOneMeasurement 는 image 를 Dispose 하지 않는다 ==" && [ "$(sed -n '/private void ProcessOneMeasurement(/,/^        }$/p' $F | grep -c 'image\.Dispose()')" = "0" ] && \
echo "== using(image) / sharedSrc.Release() 는 RunMeasure 안 ==" && [ "$(sed -n '/private void RunMeasure()/,/private void ProcessOneMeasurement/p' $F | grep -c 'using (var image = ShotParam.GetImage())')" = "1" ] && [ "$(sed -n '/private void RunMeasure()/,/private void ProcessOneMeasurement/p' $F | grep -c 'sharedSrc.Release()')" = "1" ] && \
echo "== 회귀 가드 ==" && [ "$(grep -c 'FaiTiming' $F)" = "0" ] && [ "$(grep -c 'LogSeqStep\|LogSeqAlgo' $F)" = "11" ] && [ "$(grep -c '\[ALGO\]' $F)" = "1" ] && [ "$(grep -nE '\?[^?:]*:' $F | grep -vE '\?\?|\?\.' | wc -l)" = "1" ] && \
echo "== LogSeqAlgo 는 여전히 1-IMAGE 헬퍼에만(기대 2) ==" && [ "$(sed -n '/private void ProcessDatumSingleImage(/,/private void RunGrab/p' $F | grep -c 'LogSeqAlgo')" = "2" ] && \
echo "== 변경 파일 1개 ==" && [ "$(git diff --name-only $T2 | wc -l)" = "1" ] && \
echo "TASK3 STATIC PASS"
    </automated>
    <automated>
# 주석 삭제 0건 (WHY 주석 이동 확인)
cd /c/Info/Project/DataMeasurement && F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs && \
B=$(git show $T1:$F | grep -c '260807\|Phase 54\|Phase 57\|Phase 68\|260810\|260729\|260811\|초보자용') && \
A=$(grep -c '260807\|Phase 54\|Phase 57\|Phase 68\|260810\|260729\|260811\|초보자용' $F) && \
echo "before=$B after=$A" && [ "$A" -ge "$B" ]
    </automated>
    <automated>G-5 ① SIMUL 빌드 성공 + 경고 baseline(12줄) 동일, 신규 CS0219/CS0168 0건 / ② 비-SIMUL 빌드 성공 + baseline(10줄) 동일 / ③ 원복 빌드 실행</automated>
    <automated>RESEARCH §8-C 14항목 1:1 대조 완료 — SUMMARY.md 에 항목별 PASS 판정 + 근거(줄번호/grep 결과) 표가 14행 기록되어 있을 것. 자동화 불가 항목(수동 코드 대조)은 근거 문장이 반드시 있어야 하며, "빌드 통과"만 적힌 항목은 FAIL</automated>
  </verify>
  <done>
헬퍼 5개 추가, `continue` 12곳이 `return` 으로 1:1 전환, `ProcessDatumDualImage` 에 카운터 접근 불가(D-1 구조 고정),
리소스 스캐폴딩(`using(image)` / `try…finally{sharedSrc.Release()}`)이 `RunMeasure()` 에 유지,
빌드 2종 baseline 동일, §8-C 14항목 전부 근거와 함께 PASS, 커밋 완료.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

이번 작업은 순수 내부 구조 재배치로 **신규 trust boundary 를 도입하지 않는다.** 기존 경계
(외부 핸들러/PLC ↔ TCP `VisionServer` → 시퀀스, 파일시스템 ↔ 레시피 INI/이미지 로드)는 이 파일 밖에 있고
이번 편집으로 입력 검증 지점이 이동하거나 제거되지 않는다.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ef5-01 | Tampering (판정 무결성) | `Action_FAIMeasurement.Run()` 상태 머신 — 리팩토링 드리프트로 P/F 판정이 조용히 바뀔 위험 | mitigate | G-1 허용 변환 3종 제한 + G-2 비대칭 7건 명시 보존 + Task 2 정규화 diff(바이트 동일 이동 기계 검증) + Task 3 §8-C 14항목 1:1 대조. 각 task 별도 커밋으로 롤백 지점 확보 |
| T-ef5-02 | Denial of Service (리소스) | `HImage`/`SharedHImage` 수명 계약 — 추출 과정에서 Dispose/Release 누락 시 127MB 급 네이티브 누수 → 기존 배치검사 메모리 사고 재발 | mitigate | 5-C 계약표로 `using`/`try-finally` 스캐폴딩을 `RunMeasure()` 에 고정, callee Dispose 금지를 grep 으로 검증(`image.Dispose()` 0건), Dispose 순서 7종을 §8-C 항목 8 로 대조 |
| T-ef5-03 | Information Disclosure (로그) | `[FaiTiming]` 삭제로 진단 정보 소실 | accept | tact 정보는 보존 대상 `[SEQ]` 로그에 이미 포함(사용자 확정). 타이머 해상도 근본원인은 `327cb73`/`369811c` 로 종결됨 |
| T-ef5-04 | Elevation of Privilege | 해당 없음 — 권한 경계·인증 코드 무접촉 | accept | 이 파일에 권한 판정 로직 없음 |
</threat_model>

<verification>
## 플랜 전체 완료 검증 (3 task 모두 끝난 뒤 1회)

**A. 정적 (자동):**
```bash
cd /c/Info/Project/DataMeasurement && F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
grep -c 'FaiTiming' $F                                        # 0
grep -nE '\?[^?:]*:' $F | grep -vE '\?\?|\?\.' | wc -l        # 1 (기존 주석 1줄만)
grep -c '\[ALGO\]' $F                                          # 1
grep -c 'LogSeqStep\|LogSeqAlgo' $F                            # 11
grep -cE 'private void Run(Init|MoveZ|DatumPhase|Grab|Measure|End)\(\)' $F   # 6
grep -cE 'private void (ProcessOneDatum|ProcessDatumDualImage|ProcessDatumSingleImage|ProcessOneMeasurement|FinalizeFaiTick)\(' $F  # 5
git diff --name-only cb284f4 | wc -l                           # 1 (대상 파일만)
```

**B. 컴파일 (자동):** G-5 ① SIMUL(경고 12줄) + ② 비-SIMUL(경고 10줄) 둘 다 성공 → ③ 원복.
신규 CS0219/CS0168 경고는 변수 삭제 누락 신호이므로 0건이어야 한다.

**C. 정적 1:1 대조 (수동, SUMMARY 기록 필수):** RESEARCH §8-C 14항목. "빌드 통과"는 근거로 불인정.

**D. 사용자 UAT (checkpoint 아님 — SUMMARY 에 요청 문구로 남긴다):**
앱 실행 → 1 사이클 검사 →
(1) `[SEQ]` 로그의 tact 3종(DatumPhase/Grab/Measure 초)이 여전히 출력되는가
(2) `[ALGO]` 로그가 측정마다 출력되는가
(3) 판정 결과(P/F)와 측정값이 리팩토링 전과 동일한가
</verification>

<success_criteria>
- Run() 이 switch 디스패치만 남고 30줄 이하 (case 본문 코드 0줄)
- private 메서드 11개 신규 (`Run*` 6 + `Process*`/`Finalize*` 5), 기존 헬퍼군 무이동
- `[FaiTiming]` 0건 / 코드 삼항 0건 / `[SEQ]` 11건 · `[ALGO]` 1건 · 보존 Stopwatch 4개 유지
- Debug|x64 SIMUL 빌드 + 비-SIMUL(`#else`) 컴파일 둘 다 성공, 경고 baseline 동일, 신규 미사용변수 경고 0
- RESEARCH §8-C 14항목 전부 근거와 함께 PASS, SUMMARY 에 표로 기록
- 의도적 비대칭 7건(D-1~D-7) 전부 보존, WHY/초보자용 주석 삭제 0건
- 변경 파일 정확히 1개, 커밋 3개(task 별 1개)
</success_criteria>

<output>
완료 후 `.planning/quick/260818-ef5-action-faimeasurement-run/260818-ef5-SUMMARY.md` 생성.
**반드시 포함:** RESEARCH §8-C 14항목 대조표(항목 / PASS·FAIL / 근거), 빌드 2종 경고 수,
사용자 UAT 요청 3항목(위 verification D).
</output>
