---
phase: quick-260819-gf1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: true
requirements: [GF1-01, GF1-02, GF1-03]

must_haves:
  truths:
    - "`RunMeasure`(HEAD `4299401` L458–499)는 **0줄 diff** 다. `MeasureShotFaiList` 의 바깥 시그니처 5줄(L508–512)도 **0줄 diff** 다 — 이 경계는 오늘 3번 검증된 계약이라 1글자도 못 바꾼다"
    - "`TakeCrossZRoleImageIfFirst`(L741–747) / `MarkCrossZHalfPending`(L749–767) / `MarkAllMeasurementsNoImage`(L1744–) / `AggregateFaiResult`(L1716–) 4개 메서드는 **전문 0줄 diff** 다 — 호출부에서 `ref acc.필드` 로 인자만 바뀌고 이들 자신의 `ref bool`/`ref int`/`ref HImage` 시그니처는 유지된다"
    - "`ProcessOneMeasurement` 본문은 원본에 치환 4규칙(`crossZRoleImage`→`acc.CrossZRoleImage`, `faiAllPass`→`acc.FaiAllPass`, `measuredCount`→`acc.MeasuredCount`, `nMeasNg`→`acc.NMeasNg`)을 기계적으로 적용한 결과와 **바이트 단위 동일**하다(플래너 드라이런 실측: 정확히 16줄 변경, 주석/시그니처 오탐 0건)"
    - "`FinalizeFaiTick` 본문은 원본에 치환 3규칙(`crossZRoleImage`→`acc.CrossZRoleImage`, `bShotDisplayImageReplaced`→`acc.ShotDisplayImageReplaced`, `allPass`→`acc.AllPass`)을 기계적으로 적용한 결과와 **바이트 단위 동일**하다(드라이런 실측: 정확히 8줄 변경). `faiAllPass` 는 값 파라미터로 남으므로 **치환 대상이 아니며**, `\\ballPass\\b` 가 `faiAllPass`(대문자 A)를 건드리지 않음을 실측 확인했다"
    - "`FinalizeFaiTick` 본문 주석 1줄 `//  crossZRoleImage.Dispose() 는 조건과 무관하게…` 도 **함께 `acc.CrossZRoleImage.Dispose()` 로 치환된다** — 기계적 동치 증명을 깨지 않으면서 주석이 코드와 계속 일치한다. 이 줄만 원문 유지하면 동치 diff 가 1줄 뜬다(의도된 유일한 주석 변경, 삭제 아님)"
    - "`MeasureShotFaiList` 안 FAI 루프 리셋 2줄이 **원본과 같은 위치**에 있다 — `acc.FaiAllPass = true;` 는 `foreach (var fai in ShotParam.FAIList) {` 바로 다음 줄, `acc.CrossZRoleImage = null;` 은 `foreach (var meas in fai.Measurements) {` **바로 앞줄**(둘 사이에 `faiOverlays` 선언 1줄 + hwb 주석 3줄이 원본대로 끼어 있는 것이 정상이다 — '두 줄이 인접해야 한다'는 요구는 이 파일에서 거짓이다)"
    - "되쓰기 4줄(`allPass=acc.AllPass; measuredCount=acc.MeasuredCount; nMeasNg=acc.NMeasNg; bShotDisplayImageReplaced=acc.ShotDisplayImageReplaced;`)이 `using` 블록 닫는 `}` **바깥**(들여쓰기 12칸, 메서드 본문 레벨)에 있어 `image!=null` / `image==null` 두 경로가 모두 통과한다"
    - "예외 경로에서 되쓰기가 생략되는 것은 **관측 불가**하다 — `RunMeasure` 본문에 `try`/`catch` 가 0건(실측)이므로 `MeasureShotFaiList` 를 탈출한 예외는 `RunMeasure` 도 함께 unwind 시키고, ref 값을 읽는 `pMyContext.AllPass = allPass;` 이후 문장에 도달하지 못한다"
    - "`ShotMeasureAccumulator` 6개 멤버가 전부 **순수 public 필드**다 — `{ get; set; }` 0건. 프로퍼티면 `ref acc.CrossZRoleImage` 가 CS0206 으로 컴파일 실패한다(Task 1 빌드가 즉시 잡는다)"
    - "판정/흐름 무변경 — 조건식·분기 순서·`switch` case 순서·로그 문자열·`++` 위치·`return` 위치가 전부 전후 동일. 이번 변경은 **오직 '값이 지역변수냐 객체 필드냐'만** 바꾼다"
    - "기존 상세 주석 **삭제 0건** — 특히 `//260722 hbk Phase 68 D-02a/D-05`, `//260729 hbk quick-fix(260729-e9q/hwb)`, `//260810 hbk`, `//260818 hbk` 계열. `MeasureShotFaiList` 위 `⚠ 값형 4개는 반드시 ref 다(...)` 주석도 그대로 둔다(바깥 시그니처에 대해 여전히 참이다)"
    - "**Debug|x64(SIMUL) 단일 빌드로 충분하다** — 손대는 3개 메서드 + 신설 클래스 어디에도 전처리 지시문이 0건이라(실측: L500–812 구간 `#` 시작 줄 0개, 파일의 `#if` 는 L150/408/844 세 곳뿐) SIMUL/비-SIMUL 두 구성이 **완전히 동일한 C# 텍스트**를 컴파일한다. 이 전제(0건)를 매 Task 에서 재확인하고, 1건이라도 나오면 즉시 2-빌드로 승격한다"
    - "빌드 PASS — `error CS` 0건, `warning CS` 가 착수 전 baseline 과 **동일 수치**(예상 12 = CS0618×10 + CS0162×2). 신규 `CS0206`(ref 프로퍼티) / `CS0219` / `CS0168` / `CS0177` / `CS0165` / `CS0103` 0건"
    - "삼항 `?:` 0건 — 3개 메서드 구간의 `?` 포함 줄 수가 전후 모두 **0**(실측 baseline: A 0 / B 0 / C 0). 파일 전역 `=> ` 카운트는 1 유지"
    - "`Action_FAIMeasurement.cs` 단 1개 파일만 커밋된다 — 새 파일 0개(csproj 미변경), `DatumMeasurement.csproj` 의 로컬 미커밋 오염(Debug `OutputPath=D:\\Data\\`, Release `DefineConstants` 의 `SIMUL_MODE`)은 unstaged 그대로 남는다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "ShotMeasureAccumulator 중첩 private class + 3개 메서드의 ref 통합"
      contains: "private class ShotMeasureAccumulator"
  key_links:
    - from: "MeasureShotFaiList"
      to: "ProcessOneMeasurement / FinalizeFaiTick"
      via: "ShotMeasureAccumulator acc 참조 1개 (종전 4-ref / 3-ref)"
      pattern: "ProcessOneMeasurement\\(meas, parentSeq2, image, pixRes, acc,"
    - from: "MeasureShotFaiList"
      to: "RunMeasure 의 ref 파라미터 4개"
      via: "메서드 종료 직전 write-back 4줄(using 블록 바깥)"
      pattern: "bShotDisplayImageReplaced = acc\\.ShotDisplayImageReplaced;"
    - from: "ProcessOneMeasurement"
      to: "TakeCrossZRoleImageIfFirst / MarkCrossZHalfPending (시그니처 무변경)"
      via: "ref acc.필드 (필드여야만 성립 — 프로퍼티는 CS0206)"
      pattern: "ref acc\\.CrossZRoleImage\\)"
---

<objective>
`Action_FAIMeasurement.cs` 의 Shot 측정 루프 3개 메서드가 주고받는 `ref`/값 파라미터 과다
(`ProcessOneMeasurement` 11개 중 4 ref, `MeasureShotFaiList` 7개 중 4 ref, `FinalizeFaiTick` 10개 중 3 ref)를
**중첩 private class `ShotMeasureAccumulator` 참조 1개**로 통합한다.

Purpose: 파일 안 기존 주석이 이미 경고하는 결함 —
"`ref` 를 빠뜨려도 컴파일은 통과하지만 호출자 카운터가 조용히 0 으로 남는다" —
를 **구조적으로 불가능**하게 만든다. 참조형 1개를 넘기면 `ref` 를 빠뜨릴 자리 자체가 사라진다.

Output: 파일 1개 수정, 새 파일 0개, csproj 무변경. **판정 로직·검사 흐름·저장 결과 무변경(회귀 0)** 이 하드 요구다.
</objective>

<context>
@WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
@CLAUDE.md

**코드 baseline 커밋 = `4299401`** (플래너가 `git rev-parse --short HEAD` 로 실측).
그 뒤 이 PLAN 문서 커밋이 하나 쌓였지만 `.cs` 파일은 건드리지 않았다 —
따라서 모든 대조 기준은 **HEAD 가 아니라 `4299401:<파일>`** 이다. 오늘 커밋이 계속 쌓이므로 이전 task 의 HEAD 값 재사용 금지.
워킹트리 미커밋: `M WPF_Example/DatumMeasurement.csproj` **단 1건** (로컬 설정 오염 — 절대 수정/커밋 금지)
</context>

<interfaces>
플래너가 HEAD `4299401` 에서 실측한 좌표·수치. 실행 전 반드시 재확인하되, 아래와 다르면 **멈추고 보고**할 것.

### 메서드 좌표 (HEAD 4299401)
| 메서드 | 줄범위 | 시그니처 줄 | 본문 줄 |
|---|---|---|---|
| `RunMeasure` | 458–499 | — | **전문 0줄 diff 대상** |
| `MeasureShotFaiList` | 508–561 | 508–512 (**불변**) | 513–561 |
| `ProcessOneMeasurement` | 564–694 | 564–570 | 571–694 |
| `TakeCrossZRoleImageIfFirst` | 741–747 | — | **전문 0줄 diff 대상** |
| `MarkCrossZHalfPending` | 749–767 | — | **전문 0줄 diff 대상** |
| `FinalizeFaiTick` | 769–807 | 769–774 | 775–807 |
| `MarkAllMeasurementsNoImage` | 1744– | — | **전문 0줄 diff 대상** |
| `AggregateFaiResult` | 1716– | — | **전문 0줄 diff 대상** |

### 식별자 출현 수 (실측 baseline, `\b` 단어경계 기준)
| 식별자 | 파일전역 | A(564–694) | A본문(571–694) | B(769–807) | B본문(775–807) | C(508–561) |
|---|---|---|---|---|---|---|
| `crossZRoleImage` | 17 | 3 | 2 | 8 | 7 | 3 |
| `faiAllPass` | 22 | 8 | 7 | 4 | 3 | 3 |
| `measuredCount` | 20 | 8 | 7 | 0 | 0 | 3 |
| `nMeasNg` | 8 | 2 | 1 | 0 | 0 | 2 |
| `allPass` | 11 | 0 | 0 | 2 | 1 | 4 |
| `bShotDisplayImageReplaced` | 8 | 0 | 0 | 3 | 2 | 2 |

### 최종 목표 시그니처
```csharp
// 불변 (RunMeasure 와의 계약 — 1글자도 못 바꾼다)
private void MeasureShotFaiList(InspectionSequence parentSeq2,
                                List<EdgeInspectionOverlay> overlayAcc,
                                Dictionary<string, int> dctAlgoUsed,
                                ref bool allPass, ref int measuredCount,
                                ref int nMeasNg, ref bool bShotDisplayImageReplaced) {

// 변경 (4 ref → acc 1개)
private void ProcessOneMeasurement(MeasurementBase meas, InspectionSequence parentSeq2,
                             HImage image, double pixRes,
                             ShotMeasureAccumulator acc,
                             List<EdgeInspectionOverlay> overlayAcc,
                             List<EdgeInspectionOverlay> faiOverlays,
                             Dictionary<string, int> dctAlgoUsed) {

// 변경 (3 ref → acc 1개, bool faiAllPass 값 파라미터는 유지)
private void FinalizeFaiTick(FAIConfig fai, bool faiAllPass,
                             List<EdgeInspectionOverlay> faiOverlays, SharedHImage sharedSrc,
                             List<DatumCaptureOverlay> datumSnapshot, string szSharedOriginPath,
                             InspectionSequence parentSeq2,
                             ShotMeasureAccumulator acc) {

// 불변 (호출부에서 ref acc.필드 를 받을 뿐)
private void TakeCrossZRoleImageIfFirst(InspectionSequence parentSeq2, bool bCaptureOk, string szCapturedRoleKey, ref HImage crossZRoleImage) {
private void MarkCrossZHalfPending(MeasurementBase meas, InspectionSequence parentSeq2, bool bNonProtocolCycle, ref bool faiAllPass, ref int measuredCount) {
private void MarkAllMeasurementsNoImage(ref int measuredCount) {
```
</interfaces>

<ground_rules>

### G-1. 절대 금지
- `RunMeasure` 수정 / `MeasureShotFaiList` **바깥 시그니처** 수정 (파라미터 이름·순서·`ref` 여부 전부)
- `TakeCrossZRoleImageIfFirst` / `MarkCrossZHalfPending` / `MarkAllMeasurementsNoImage` / `AggregateFaiResult` 시그니처·본문 수정
- 새 파일 생성 (csproj 를 건드리게 된다) / `DatumMeasurement.csproj` 수정
- 삼항 `?:` (if-else 로) / C# 8.0+ 문법 (C# 7.2 고정) / `{ get; set; }` 프로퍼티
- 기존 주석 삭제 — **단 1줄도.** 아래 G-2 의 유일한 예외만 허용
- `git add -A` / `git commit -a` / `git checkout` / `git stash` / 빌드 잠김 시 프로세스 강제종료

### G-2. 주석 정책
- 원칙: 기존 주석 **원문 유지**.
- **유일한 예외**: `FinalizeFaiTick` 본문의 이 1줄은 기계 치환을 함께 받는다(삭제 아님, 치환).
  ```
  before:  //  crossZRoleImage.Dispose() 는 조건과 무관하게 그대로 수행된다.
  after:   //  acc.CrossZRoleImage.Dispose() 는 조건과 무관하게 그대로 수행된다.
  ```
  이유: Task 3 의 기계적 동치 diff 를 빈 출력으로 유지하면서 주석이 코드와 계속 일치하게 하려는 것.
  **이 줄을 원문 그대로 두면 동치 diff 가 1줄 뜨고, 그때 "검증식을 고치는" 대응은 금지다.**
- `MeasureShotFaiList` L556 의 주석 `//  … allPass 가 default true 로 남아 잘못 PASS 되는 것을 차단.` 은
  **원문 그대로 둔다** — 여기서 말하는 `allPass` 는 여전히 존재하는 바깥 ref 파라미터를 가리키므로 참이다.
- `MeasureShotFaiList` 위 `⚠ 값형 4개는 반드시 ref 다(allPass/measuredCount/nMeasNg/bShotDisplayImageReplaced).`
  블록도 **원문 그대로** — 바깥 시그니처에 대해 여전히 참이다.
- 신규 주석은 자유롭게 추가 가능(검증식이 주석 줄을 제외하고 센다).

### G-3. 명명 규칙 (실측 근거)
프로젝트의 중첩 `private class` 관례(`RepeatMeasurementStats.KeyData`, `MeasurementHistoryCsvLoader.CycleGroupState`)
= **PascalCase public 필드, 헝가리언 접두사 없음, `{ get; set; }` 없음.** 이 관례를 그대로 따른다.
브레이스는 **이 파일의 스타일(K&R — 여는 `{` 를 선언 줄 끝에)** 을 따른다(파일 내 `private enum EStep {` / `private enum ECrossZGate {` 와 동일).

### G-4. 셸 상수 (**Bash 호출마다 반드시 재정의**)
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
SCR="C:/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/9d3a7b4d-2314-4b14-8686-52fd6346a1f9/scratchpad"
mkdir -p "$SCR/gf1"
# 메서드 전문 추출기 — 앵커는 '^        private void <이름>' 로 고정한다.
# ⚠ 앵커에서 '^        private void ' 접두사를 빼면 호출부 줄(들여쓰기 28칸)에 먼저 걸려
#   엉뚱한 메서드를 잘라낸다(플래너가 실제로 밟은 함정).
xm() { awk -v p="$1" '$0 ~ p {i=1} i {print} i && /^        \}$/ {exit}' "$2"; }
# 본문만: 시그니처 마지막 줄(') {' 로 끝남)까지 버린다
xb() { xm "$1" "$2" | sed '1,/) {$/d'; }
```
`xm`/`xb` 앵커 8개가 전부 유일 매치임을 플래너가 실측 확인했다.

**⚠ 파일 안에 '중복 앵커'가 실재한다 (플래너가 드라이런에서 실제로 밟은 함정):**
`foreach (var fai in ShotParam.FAIList) {` 는 L538(우리 대상)과 **L1745(`MarkAllMeasurementsNoImage`)** 두 곳,
`foreach (var meas in fai.Measurements) {` 는 L545 와 **L1747** 두 곳에 있다.
따라서 이 두 앵커를 쓰는 위치 검증은 **반드시 `xm '^        private void MeasureShotFaiList' $F |` 로 먼저 범위를 좁힌 뒤** 실행한다.
파일 전역 `grep -A1/-B1` 로 검사하면 엉뚱한 메서드를 보고 참/거짓이 뒤바뀐다.

### G-5. 빌드 규칙 — **SIMUL 단일 빌드** (근거 있는 축소)
- 근거: 손대는 3개 메서드 + 신설 클래스에 전처리 지시문 **0건**(파일의 `#if` 는 L150/408/844 = `RunMoveZ`/`AcquireShotImage`/`GrabOrLoadDatumImage` 세 곳뿐, 우리 구간 밖).
  → SIMUL / 비-SIMUL 두 구성이 **동일한 C# 텍스트**를 컴파일하므로 두 번 돌릴 정보 이득이 0이다.
- **매 Task 마다 이 전제를 재확인**한다. 1건이라도 나오면 즉시 `-p:Configuration=Release -p:Platform=x64 -p:DefineConstants=TRACE` 2-빌드로 승격.
- 앱이 `D:\Data\` 에서 실행 중일 수 있다 → **프로세스 종료 절대 금지.** 스크래치 `OutputPath` 로 컴파일만 검증.
- **`//p:` 금지, `-p:` 사용** (`/` 섞이면 Git Bash 가 `MSB1001` 로 죽는다)
- **`-p:OutputPath="$SCRW\\xxx\\"` 후행 백슬래시는 반드시 `\\`** — `\"` 로 끝내면 bash unexpected EOF 로 빌드가 아예 안 돈다

```bash
MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"
SCRW="C:\\Users\\tech\\AppData\\Local\\Temp\\claude\\C--Info-Project-DataMeasurement\\9d3a7b4d-2314-4b14-8686-52fd6346a1f9\\scratchpad"
SCR="C:/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/9d3a7b4d-2314-4b14-8686-52fd6346a1f9/scratchpad"
"$MSB" 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' \
  -p:Configuration=Debug -p:Platform=x64 -p:OutputPath="$SCRW\\gf1-tN\\" \
  -t:Rebuild -v:minimal -nologo 2>&1 | tee "$SCR/gf1/tN.log" >/dev/null
```
판정(`WBASE` = Task 0 에서 캡처한 착수 전 warning 수, 예상 12):
```bash
[ "$(grep -cE 'error CS' "$SCR/gf1/tN.log")" = "0" ] &&
[ "$(grep -cE 'warning CS' "$SCR/gf1/tN.log")" = "$WBASE" ] &&
[ "$(grep -cE 'CS0206|CS0219|CS0168|CS0177|CS0165|CS0103|CS1027|CS1028' "$SCR/gf1/tN.log")" = "0" ]
```
잠김 실패 시 `OutputPath` 이름만 바꿔 재시도. 그래도 안 되면 **죽이지 말고 사용자에게 보고.**

### G-6. verify → commit → HYGIENE 순서 (엄수)
1. 코드 수정
2. **VERIFY** 블록 전부 통과 (하나라도 실패하면 커밋 금지)
3. **COMMIT** — 대상 파일만 경로 지정 스테이징
4. **HYGIENE** — 커밋 *이후*에 확인 (커밋에 파일 1개만 들어갔는가 / csproj 가 unstaged 로 남았는가)

### G-7. 자기모순 acceptance 금지
검증식이 실패하면 **코드를 고친다.** 검증식을 완화하거나 기대값을 바꾸는 것은 금지.
유일한 예외: 실패 원인이 "새로 추가한 주석 때문"임을 **해당 줄을 출력해 제시**한 경우에 한해,
주석 제외 필터(`grep -v '^\+[[:space:]]*//'`)가 이미 걸려 있는지 먼저 확인할 것.

</ground_rules>

<tasks>

<task type="auto">
  <name>Task 0: 착수 전 baseline 캡처 (읽기 전용, 수정 0)</name>
  <files>없음 — 스크래치에만 기록</files>
  <action>
착수 전 상태를 고정한다. **파일을 수정하지 않는다.**

```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
SCR="C:/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/9d3a7b4d-2314-4b14-8686-52fd6346a1f9/scratchpad"
mkdir -p "$SCR/gf1"

# 1) 코드 baseline 확인 — HEAD 자체는 plan 문서 커밋만큼 앞서 있을 수 있다.
#    반드시 확인할 것은 '대상 .cs 가 4299401 이후 한 글자도 안 바뀌었는가' 다.
git rev-parse --short HEAD
git diff 4299401 -- $F | head    # 빈 출력이어야 한다

# 2) git 워킹트리 baseline — csproj 1건만 M 이어야 한다
git status --porcelain > "$SCR/gf1/git-base.txt"; cat "$SCR/gf1/git-base.txt"

# 3) 착수 전 파일 스냅샷
git show 4299401:$F > "$SCR/gf1/base.cs"

# 4) 좌표 재확인 (<interfaces> 표와 일치해야 한다)
grep -nE "^        private void (RunMeasure|MeasureShotFaiList|ProcessOneMeasurement|TakeCrossZRoleImageIfFirst|MarkCrossZHalfPending|FinalizeFaiTick|MarkAllMeasurementsNoImage|AggregateFaiResult)" $F

# 5) 전처리 지시문 0건 전제 (G-5 근거)
echo "preproc in 500-812 = $(awk 'NR>=500&&NR<=812' $F | grep -c '^[[:space:]]*#')"   # 기대 0

# 6) 삼항 baseline
echo "'?' A=$(awk 'NR>=564&&NR<=694' $F | grep -c '?') B=$(awk 'NR>=769&&NR<=807' $F | grep -c '?') C=$(awk 'NR>=508&&NR<=561' $F | grep -c '?')"  # 기대 0 0 0
echo "'=> ' file-wide = $(grep -c '=> ' $F)"   # 기대 1
```

7) **빌드 baseline 캡처** — G-5 명령을 `OutputPath=…gf1-t0\\`, 로그 `$SCR/gf1/t0.log` 로 1회 실행.
   `grep -cE 'warning CS' "$SCR/gf1/t0.log"` 결과를 **`WBASE`** 로 기록한다(예상 12).
   `grep -cE 'error CS'` 는 0 이어야 한다. 0이 아니면 착수 전부터 깨진 상태 → 멈추고 보고.
  </action>
  <verify>
    <automated>
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
SCR="C:/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/9d3a7b4d-2314-4b14-8686-52fd6346a1f9/scratchpad"
[ -z "$(git diff 4299401 -- $F)" ] && [ -z "$(git diff --cached 4299401 -- $F)" ] && echo "OK code baseline = 4299401 (대상 .cs 무변경)" && \
[ "$(git status --porcelain | wc -l)" = "1" ] && [ "$(git status --porcelain)" = " M WPF_Example/DatumMeasurement.csproj" ] && echo "OK worktree" && \
[ "$(awk 'NR>=500&&NR<=812' $F | grep -c '^[[:space:]]*#')" = "0" ] && echo "OK no-preproc(단일빌드 근거 성립)" && \
[ -s "$SCR/gf1/base.cs" ] && echo "OK snapshot" && \
[ "$(grep -cE 'error CS' "$SCR/gf1/t0.log")" = "0" ] && echo "OK build-base error0" && \
echo "WBASE = $(grep -cE 'warning CS' "$SCR/gf1/t0.log")"
```
    </automated>
  </verify>
  <done>대상 `.cs` 가 `4299401` 과 동일함 확인(HEAD 자체는 plan 문서 커밋만큼 앞설 수 있다), 워킹트리 오염 csproj 1건뿐, 전처리 0건(단일빌드 근거 성립), `base.cs` 스냅샷 존재, `WBASE` 수치 기록 완료. 파일 수정 0줄.</done>
</task>

<task type="auto">
  <name>Task 1: ShotMeasureAccumulator 신설 + MeasureShotFaiList 내부만 acc 화 (두 헬퍼 시그니처는 아직 그대로)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <action>
**이 Task 가 끝난 시점에도 `ProcessOneMeasurement` / `FinalizeFaiTick` 은 0줄 diff 여야 한다.**
호출부에서 `ref acc.필드` 로 인자만 바꿔 넘긴다 — 이게 컴파일되는지가 곧 "필드로 선언했는가"의 증명이다(프로퍼티면 CS0206).

### 1-A. 중첩 클래스 신설
`ECrossZGate` enum 이 닫히는 `}`(L52 부근) **다음**, `private FAIMeasurementContext pMyContext;` **앞**에 삽입:

```csharp
        //260819 hbk quick-260819-gf1: Shot 측정 루프의 누적 상태 묶음.
        //  왜 필요한가 — 종전엔 이 값들을 MeasureShotFaiList → ProcessOneMeasurement / FinalizeFaiTick 로
        //  ref 파라미터에 실어 날랐다. ref 키워드를 하나만 빠뜨려도 컴파일은 통과하고 호출자 카운터만
        //  조용히 0 으로 남는 구조였다. 참조형 1개로 묶으면 그 실수를 할 자리 자체가 사라진다.
        //  ⚠ 프로퍼티가 아니라 반드시 '필드' 여야 한다 — 아직 ref 로 받는 헬퍼가 남아 있고
        //    (TakeCrossZRoleImageIfFirst / MarkCrossZHalfPending / MarkAllMeasurementsNoImage),
        //    C# 은 프로퍼티를 ref 인자로 넘기지 못한다(CS0206).
        //  ⚠ 수명이 두 종류로 섞여 있다:
        //    Shot 전체 수명 = AllPass / MeasuredCount / NMeasNg / ShotDisplayImageReplaced
        //    FAI 1개 수명   = FaiAllPass / CrossZRoleImage → FAI 루프 진입마다 반드시 리셋한다.
        private class ShotMeasureAccumulator {
            public bool AllPass;
            public int MeasuredCount;
            public int NMeasNg;
            public bool ShotDisplayImageReplaced;
            public bool FaiAllPass;
            public HImage CrossZRoleImage;
        }
```

### 1-B. `MeasureShotFaiList` — 시그니처 5줄(L508–512)은 **손대지 않는다**

**(a) `) {` 다음, `using (var image = ShotParam.GetImage()) {` 앞에 삽입 (들여쓰기 12칸):**
```csharp
            //260819 hbk quick-260819-gf1: 바깥 시그니처(ref 4개)는 RunMeasure 와의 계약이라 그대로 둔다.
            //  안쪽에서만 누적 객체로 바꿔 쓰고, 메서드 끝에서 딱 한 번 되쓴다.
            var acc = new ShotMeasureAccumulator();
            acc.AllPass = allPass;
            acc.MeasuredCount = measuredCount;
            acc.NMeasNg = nMeasNg;
            acc.ShotDisplayImageReplaced = bShotDisplayImageReplaced;
```

**(b) 본문 치환 — 정확히 아래 6줄만. 다른 줄은 공백 1칸도 건드리지 않는다.**

| 원본 줄 | before | after |
|---|---|---|
| L539 | `                        bool faiAllPass = true;` | `                        acc.FaiAllPass = true;` |
| L544 | `                        HImage crossZRoleImage = null;` | `                        acc.CrossZRoleImage = null;` |
| L546 | `…ProcessOneMeasurement(meas, parentSeq2, image, pixRes, ref crossZRoleImage, ref faiAllPass, ref measuredCount, ref nMeasNg, overlayAcc, faiOverlays, dctAlgoUsed);` | `…ProcessOneMeasurement(meas, parentSeq2, image, pixRes, ref acc.CrossZRoleImage, ref acc.FaiAllPass, ref acc.MeasuredCount, ref acc.NMeasNg, overlayAcc, faiOverlays, dctAlgoUsed);` |
| L548 | `…FinalizeFaiTick(fai, faiAllPass, faiOverlays, sharedSrc, datumSnapshot, szSharedOriginPath, parentSeq2, ref crossZRoleImage, ref bShotDisplayImageReplaced, ref allPass);` | `…FinalizeFaiTick(fai, acc.FaiAllPass, faiOverlays, sharedSrc, datumSnapshot, szSharedOriginPath, parentSeq2, ref acc.CrossZRoleImage, ref acc.ShotDisplayImageReplaced, ref acc.AllPass);` |
| L557 | `                    allPass = false;` | `                    acc.AllPass = false;` |
| L558 | `                    MarkAllMeasurementsNoImage(ref measuredCount); //260702 hbk Extract Method(Task2)` | `                    MarkAllMeasurementsNoImage(ref acc.MeasuredCount); //260702 hbk Extract Method(Task2)` |

⚠ L539 / L544 는 **선언에서 대입으로** 바뀐다(`bool` / `HImage` 타입 토큰이 사라진다). 위치는 원본 그대로.
⚠ L540(`var faiOverlays = …`)과 L541–543(hwb 3줄 주석)이 두 리셋 사이에 **원본대로 남아 있어야** 한다.
⚠ L556 주석의 `allPass` 는 **치환하지 않는다**(G-2).

**(c) `using` 블록 닫는 `}` 다음, 메서드 닫는 `}` 앞에 삽입 (들여쓰기 12칸 — `using`/`if-else` 바깥):**
```csharp
            //260819 hbk quick-260819-gf1: 되쓰기 — using 블록 바깥이라 image!=null / image==null 두 경로가
            //  전부 여기로 합류한다. 예외로 탈출하는 경우 되쓰기는 생략되지만 관측 불가하다 —
            //  RunMeasure 에는 try/catch 가 없어(실측) 호출부도 함께 unwind 되고,
            //  ref 값을 읽는 pMyContext.AllPass = allPass; 이후 문장에 애초에 도달하지 못한다.
            allPass = acc.AllPass;
            measuredCount = acc.MeasuredCount;
            nMeasNg = acc.NMeasNg;
            bShotDisplayImageReplaced = acc.ShotDisplayImageReplaced;
```

### 예상 diff 규모 (VERIFY 가 이 숫자를 강제한다)
- 삭제 줄 **정확히 6** (전부 코드, 주석 삭제 0)
- 추가 줄 중 **주석/빈줄 제외 = 정확히 23** (클래스 8 + init 5 + 되쓰기 4 + 치환 6)
  </action>
  <verify>
    <automated>
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
SCR="C:/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/9d3a7b4d-2314-4b14-8686-52fd6346a1f9/scratchpad"
B="$SCR/gf1/base.cs"
xm() { awk -v p="$1" '$0 ~ p {i=1} i {print} i && /^        \}$/ {exit}' "$2"; }

echo "== V1. 무변경 계약 메서드 6개 전문 0줄 diff ==" && \
for m in RunMeasure ProcessOneMeasurement TakeCrossZRoleImageIfFirst MarkCrossZHalfPending FinalizeFaiTick MarkAllMeasurementsNoImage; do
  diff <(xm "^        private void $m" "$B") <(xm "^        private void $m" "$F") >/dev/null || { echo "FAIL $m"; exit 1; }
done; echo "  OK 6/6" && \
diff <(xm "^        private void AggregateFaiResult" "$B") <(xm "^        private void AggregateFaiResult" "$F") >/dev/null && echo "  OK AggregateFaiResult" && \

echo "== V2. MeasureShotFaiList 바깥 시그니처 5줄 바이트 동일 ==" && \
diff <(xm "^        private void MeasureShotFaiList" "$B" | sed -n '1,/) {$/p') \
     <(xm "^        private void MeasureShotFaiList" "$F" | sed -n '1,/) {$/p') && echo "  OK sig unchanged" && \

echo "== V3. diff 규모: 삭제 정확히 6줄 / 주석·빈줄 제외 추가 정확히 23줄 ==" && \
[ "$(git diff -U0 -- $F | grep -c '^-[^-]')" = "6" ] && echo "  OK del=6" && \
[ "$(git diff -U0 -- $F | grep '^+[^+]' | grep -v '^+[[:space:]]*//' | grep -vc '^+[[:space:]]*$')" = "23" ] && echo "  OK add(code)=23" && \

echo "== V4. 삭제된 6줄이 전부 '코드'이고 주석 삭제 0건 ==" && \
[ "$(git diff -U0 -- $F | grep '^-[^-]' | grep -c '^-[[:space:]]*//')" = "0" ] && echo "  OK no comment deleted" && \
git diff -U0 -- $F | grep '^-[^-]' && \

echo "== V5. FAI 루프 리셋 2줄 위치 고정 ==" && \
xm '^        private void MeasureShotFaiList' $F | grep -A1 'foreach (var fai in ShotParam.FAIList) {' | tail -1 | grep -q '^[[:space:]]*acc\.FaiAllPass = true;$' && echo "  OK reset1 = foreach fai 바로 다음 줄" && \
xm '^        private void MeasureShotFaiList' $F | grep -B1 'foreach (var meas in fai.Measurements) {' | head -1 | grep -q '^[[:space:]]*acc\.CrossZRoleImage = null;$' && echo "  OK reset2 = foreach meas 바로 앞줄" && \
[ "$(grep -c '^                        acc.FaiAllPass = true;$' $F)" = "1" ] && \
[ "$(grep -c '^                        acc.CrossZRoleImage = null;$' $F)" = "1" ] && echo "  OK reset 유일" && \

echo "== V6. 되쓰기 4줄이 using 바깥(들여쓰기 12칸) & 메서드 마지막 4문장 ==" && \
diff <(xm "^        private void MeasureShotFaiList" "$F" | tail -5 | head -4) \
     <(printf '            allPass = acc.AllPass;\n            measuredCount = acc.MeasuredCount;\n            nMeasNg = acc.NMeasNg;\n            bShotDisplayImageReplaced = acc.ShotDisplayImageReplaced;\n') && echo "  OK write-back 위치/내용" && \

echo "== V7. ShotMeasureAccumulator 는 순수 필드 (프로퍼티 0건) ==" && \
[ "$(grep -c '^        private class ShotMeasureAccumulator {$' $F)" = "1" ] && \
[ "$(xm '^        private class ShotMeasureAccumulator' $F | grep -c '{ get; set; }')" = "0" ] && \
[ "$(xm '^        private class ShotMeasureAccumulator' $F | grep -cE '^            public (bool|int|HImage) [A-Z][A-Za-z]*;$')" = "6" ] && echo "  OK 6 pure fields" && \

echo "== V8. 식별자 잔존 수 ==" && \
[ "$(xm '^        private void MeasureShotFaiList' $F | grep -oE '\bcrossZRoleImage\b' | wc -l)" = "0" ] && \
[ "$(xm '^        private void MeasureShotFaiList' $F | grep -oE '\bfaiAllPass\b' | wc -l)" = "0" ] && \
[ "$(xm '^        private void MeasureShotFaiList' $F | grep -oE '\bmeasuredCount\b' | wc -l)" = "3" ] && \
[ "$(xm '^        private void MeasureShotFaiList' $F | grep -oE '\bnMeasNg\b' | wc -l)" = "3" ] && \
[ "$(xm '^        private void MeasureShotFaiList' $F | grep -oE '\ballPass\b' | wc -l)" = "4" ] && \
[ "$(xm '^        private void MeasureShotFaiList' $F | grep -oE '\bbShotDisplayImageReplaced\b' | wc -l)" = "3" ] && echo "  OK 잔존 0/0/3/3/4/3 = 시그니처1 + acc초기화1 + 되쓰기1 (allPass 만 L556 주석 1건 추가)" && \

echo "== V9. 스타일 ==" && \
[ "$(xm '^        private void MeasureShotFaiList' $F | grep -c '?')" = "0" ] && \
[ "$(xm '^        private class ShotMeasureAccumulator' $F | grep -c '?')" = "0" ] && echo "  OK 삼항 0" && \
[ "$(grep -c '=> ' $F)" = "1" ] && echo "  OK '=> ' 1 유지" && \
[ "$(awk '/^        private class ShotMeasureAccumulator/{i=1} i{print} i&&/^        \}$/{exit}' $F | grep -c '^[[:space:]]*#')" = "0" ] && \
[ "$(xm '^        private void MeasureShotFaiList' $F | grep -c '^[[:space:]]*#')" = "0" ] && echo "  OK 전처리 0(단일빌드 근거 유지)" && \

echo "== V10. 빌드 (G-5, OutputPath=gf1-t1, 로그 gf1/t1.log) — 별도 실행 후 아래 판정 ==" && \
WBASE="$(grep -cE 'warning CS' "$SCR/gf1/t0.log")" && \
[ "$(grep -cE 'error CS' "$SCR/gf1/t1.log")" = "0" ] && \
[ "$(grep -cE 'warning CS' "$SCR/gf1/t1.log")" = "$WBASE" ] && \
[ "$(grep -cE 'CS0206|CS0219|CS0168|CS0177|CS0165|CS0103|CS1027|CS1028' "$SCR/gf1/t1.log")" = "0" ] && echo "  OK build (warning=$WBASE)" && \
echo "===== TASK 1 VERIFY ALL PASS ====="
```
그 다음 **COMMIT**(G-6):
```bash
git add WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
git commit -m "refactor(260819-gf1): ShotMeasureAccumulator 신설 + MeasureShotFaiList 내부 누적객체화 (바깥 ref 시그니처·헬퍼 시그니처 무변경)"
```
그 다음 **HYGIENE**(커밋 이후):
```bash
[ "$(git show --stat --name-only --format='' HEAD | grep -c .)" = "1" ] && \
[ "$(git show --name-only --format='' HEAD)" = "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs" ] && \
[ "$(git status --porcelain)" = " M WPF_Example/DatumMeasurement.csproj" ] && echo "OK hygiene"
```
    </automated>
  </verify>
  <done>V1–V10 전부 PASS. `ProcessOneMeasurement`/`FinalizeFaiTick`/두 크로스-Z 헬퍼/`MarkAllMeasurementsNoImage`/`AggregateFaiResult`/`RunMeasure` 는 여전히 0줄 diff. 삭제 6줄·코드추가 23줄. 커밋 1개, 파일 1개, csproj unstaged 유지.</done>
</task>

<task type="auto">
  <name>Task 2: ProcessOneMeasurement — 4 ref → acc (기계 치환 4규칙, 동치 diff 빈 출력)</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <action>
### 2-A. 시그니처 (2줄 삭제 → 1줄)
```
before:                                      ref HImage crossZRoleImage, ref bool faiAllPass,
before:                                      ref int measuredCount, ref int nMeasNg,
after:                                       ShotMeasureAccumulator acc,
```
나머지 5줄(`private void ProcessOneMeasurement(...` / `HImage image, double pixRes,` / `List<...> overlayAcc,` / `List<...> faiOverlays,` / `Dictionary<string, int> dctAlgoUsed) {`)은 **그대로**. 들여쓰기 37칸 유지.

### 2-B. 본문 — 아래 4규칙의 **기계적 전수 치환**. 그 외 토큰 변경 0.
```
crossZRoleImage  → acc.CrossZRoleImage
faiAllPass       → acc.FaiAllPass
measuredCount    → acc.MeasuredCount
nMeasNg          → acc.NMeasNg
```
플래너 드라이런 실측 = **정확히 16줄** 변경, 주석 오탐 0건(이 본문에는 해당 식별자를 언급한 주석이 없다):
`acc.FaiAllPass = false;` ×6 / `acc.MeasuredCount++;` ×6 (그중 1줄은 `// 시도 회수 통계` 꼬리주석 유지) /
`TakeCrossZRoleImageIfFirst(..., ref acc.CrossZRoleImage);` ×2 /
`MarkCrossZHalfPending(meas, parentSeq2, bNonProtocolCycle, ref acc.FaiAllPass, ref acc.MeasuredCount);` ×1 /
`if (!meas.LastJudgement) acc.NMeasNg++; //260818 hbk [SEQ] 요약용 공차이탈 집계` ×1.
(합 6+6+2+1+1 = 16)

⚠ `switch (eGate)` case 순서/`return` 위치/`break` 위치/`ECrossZGate` 판정 순서 전부 불변.
⚠ `//260702 hbk Extract Method(Task3)…` 등 메서드 위 주석 유지.

### 2-C. 호출부 1줄 (`MeasureShotFaiList` 안)
```
before: ProcessOneMeasurement(meas, parentSeq2, image, pixRes, ref acc.CrossZRoleImage, ref acc.FaiAllPass, ref acc.MeasuredCount, ref acc.NMeasNg, overlayAcc, faiOverlays, dctAlgoUsed);
after:  ProcessOneMeasurement(meas, parentSeq2, image, pixRes, acc, overlayAcc, faiOverlays, dctAlgoUsed);
```

### 예상 diff: 삭제 19줄(sig 2 + 본문 16 + 호출부 1) / 코드추가 18줄(sig 1 + 본문 16 + 호출부 1)
  </action>
  <verify>
    <automated>
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
SCR="C:/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/9d3a7b4d-2314-4b14-8686-52fd6346a1f9/scratchpad"
B="$SCR/gf1/base.cs"
xm() { awk -v p="$1" '$0 ~ p {i=1} i {print} i && /^        \}$/ {exit}' "$2"; }
xb() { xm "$1" "$2" | sed '1,/) {$/d'; }

echo "== V1. ★기계 치환 동치 — 원본(4299401) 본문에 4규칙 적용 == 현재 본문 (빈 출력이어야 함) ==" && \
diff <(xb "^        private void ProcessOneMeasurement" "$B" | sed 's/\bcrossZRoleImage\b/acc.CrossZRoleImage/g; s/\bfaiAllPass\b/acc.FaiAllPass/g; s/\bmeasuredCount\b/acc.MeasuredCount/g; s/\bnMeasNg\b/acc.NMeasNg/g') \
     <(xb "^        private void ProcessOneMeasurement" "$F") && echo "  OK 본문 기계동치" && \

echo "== V1b. 치환 규칙 자기검증 — 규칙이 실제로 16줄만 건드리는가(원본 대비) ==" && \
[ "$(diff <(xb '^        private void ProcessOneMeasurement' "$B") <(xb '^        private void ProcessOneMeasurement' "$B" | sed 's/\bcrossZRoleImage\b/acc.CrossZRoleImage/g; s/\bfaiAllPass\b/acc.FaiAllPass/g; s/\bmeasuredCount\b/acc.MeasuredCount/g; s/\bnMeasNg\b/acc.NMeasNg/g') | grep -c '^>')" = "16" ] && echo "  OK 규칙 영향범위=16줄" && \

echo "== V2. 시그니처 정확히 6줄, acc 1개, ref 0개 ==" && \
diff <(xm "^        private void ProcessOneMeasurement" "$F" | sed -n '1,/) {$/p') \
     <(printf '        private void ProcessOneMeasurement(MeasurementBase meas, InspectionSequence parentSeq2,\n                                     HImage image, double pixRes,\n                                     ShotMeasureAccumulator acc,\n                                     List<EdgeInspectionOverlay> overlayAcc,\n                                     List<EdgeInspectionOverlay> faiOverlays,\n                                     Dictionary<string, int> dctAlgoUsed) {\n') && echo "  OK signature" && \

echo "== V3. 무변경 계약 메서드 전문 0줄 diff (vs 착수 전 4299401) ==" && \
for m in RunMeasure TakeCrossZRoleImageIfFirst MarkCrossZHalfPending FinalizeFaiTick MarkAllMeasurementsNoImage AggregateFaiResult; do
  diff <(xm "^        private void $m" "$B") <(xm "^        private void $m" "$F") >/dev/null || { echo "FAIL $m"; exit 1; }
done; echo "  OK 6/6" && \
diff <(xm "^        private void MeasureShotFaiList" "$B" | sed -n '1,/) {$/p') <(xm "^        private void MeasureShotFaiList" "$F" | sed -n '1,/) {$/p') && echo "  OK 바깥 시그니처 불변" && \

echo "== V4. 호출부 1건, 정확한 인자 ==" && \
[ "$(grep -c 'ProcessOneMeasurement(meas, parentSeq2, image, pixRes, acc, overlayAcc, faiOverlays, dctAlgoUsed);' $F)" = "1" ] && \
[ "$(grep -c 'ProcessOneMeasurement(' $F)" = "2" ] && echo "  OK 선언1+호출1" && \

echo "== V5. 크로스-Z 헬퍼 호출 개수 불변 (ruh 확정치) ==" && \
[ "$(grep -c 'TakeCrossZRoleImageIfFirst(parentSeq2, bCaptureOk, szCapturedRoleKey, ref acc.CrossZRoleImage);' $F)" = "2" ] && \
[ "$(grep -c 'MarkCrossZHalfPending(meas, parentSeq2, bNonProtocolCycle, ref acc.FaiAllPass, ref acc.MeasuredCount);' $F)" = "1" ] && echo "  OK 2 + 1" && \

echo "== V6. diff 규모 (직전 커밋 대비) 삭제19 / 코드추가18 ==" && \
[ "$(git diff -U0 -- $F | grep -c '^-[^-]')" = "19" ] && \
[ "$(git diff -U0 -- $F | grep '^+[^+]' | grep -v '^+[[:space:]]*//' | grep -vc '^+[[:space:]]*$')" = "18" ] && \
[ "$(git diff -U0 -- $F | grep '^-[^-]' | grep -c '^-[[:space:]]*//')" = "0" ] && echo "  OK 19/18, 주석삭제0" && \

echo "== V7. 스타일 ==" && \
[ "$(xm '^        private void ProcessOneMeasurement' $F | grep -c '?')" = "0" ] && echo "  OK 삼항 0" && \
[ "$(grep -c '=> ' $F)" = "1" ] && \
[ "$(xm '^        private void ProcessOneMeasurement' $F | grep -c '^[[:space:]]*#')" = "0" ] && echo "  OK 전처리 0" && \

echo "== V8. 빌드 (G-5, OutputPath=gf1-t2, 로그 gf1/t2.log) ==" && \
WBASE="$(grep -cE 'warning CS' "$SCR/gf1/t0.log")" && \
[ "$(grep -cE 'error CS' "$SCR/gf1/t2.log")" = "0" ] && \
[ "$(grep -cE 'warning CS' "$SCR/gf1/t2.log")" = "$WBASE" ] && \
[ "$(grep -cE 'CS0206|CS0219|CS0168|CS0177|CS0165|CS0103|CS1027|CS1028' "$SCR/gf1/t2.log")" = "0" ] && echo "  OK build (warning=$WBASE)" && \
echo "===== TASK 2 VERIFY ALL PASS ====="
```
그 다음 **COMMIT**:
```bash
git add WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
git commit -m "refactor(260819-gf1): ProcessOneMeasurement ref 4개를 ShotMeasureAccumulator 1개로 통합 (기계 치환, 동치 검증)"
```
그 다음 **HYGIENE**: Task 1 과 동일한 3줄 검사.
    </automated>
  </verify>
  <done>V1(기계 치환 동치 diff 빈 출력) 포함 V1–V8 전부 PASS. 시그니처 6줄 정확 일치, 호출부 1건, 헬퍼 호출 2+1, 삭제19/추가18, 빌드 PASS. 커밋 1개·파일 1개.</done>
</task>

<task type="auto">
  <name>Task 3: FinalizeFaiTick — 3 ref → acc (기계 치환 3규칙) + 최종 통합 검증</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <action>
### 3-A. 시그니처 (2줄 삭제 → 1줄). **`bool faiAllPass` 값 파라미터는 유지한다.**
```
before:                                      ref HImage crossZRoleImage, ref bool bShotDisplayImageReplaced,
before:                                      ref bool allPass) {
after:                                       ShotMeasureAccumulator acc) {
```
앞 4줄(`private void FinalizeFaiTick(FAIConfig fai, bool faiAllPass,` / `List<...> faiOverlays, SharedHImage sharedSrc,` / `List<DatumCaptureOverlay> datumSnapshot, string szSharedOriginPath,` / `InspectionSequence parentSeq2,`)은 **그대로**.

### 3-B. 본문 — 아래 3규칙의 **기계적 전수 치환**. `faiAllPass` 는 **치환하지 않는다.**
```
crossZRoleImage           → acc.CrossZRoleImage
bShotDisplayImageReplaced → acc.ShotDisplayImageReplaced
allPass                   → acc.AllPass          (\ballPass\b 는 faiAllPass 를 건드리지 않음 — 대문자 A, 실측 확인)
```
플래너 드라이런 실측 = **정확히 8줄** 변경:
1. `if (acc.CrossZRoleImage == null) {`
2. `try { crossZSharedSrc = new SharedHImage(acc.CrossZRoleImage.CopyImage()); } catch { crossZSharedSrc = null; }`
3. **주석** `//  acc.CrossZRoleImage.Dispose() 는 조건과 무관하게 그대로 수행된다.` ← **G-2 의 유일 예외. 반드시 함께 치환.**
4. `if (!acc.ShotDisplayImageReplaced && !IsViewerUpdateSkipped(parentSeq2)) {`
5. `pMyContext.ResultHalconImage = acc.CrossZRoleImage.CopyImage();`
6. `acc.ShotDisplayImageReplaced = true;`
7. `if (!faiAllPass) acc.AllPass = false;`  ← `faiAllPass` 는 그대로 남는다
8. `if (acc.CrossZRoleImage != null) { try { acc.CrossZRoleImage.Dispose(); } catch { } acc.CrossZRoleImage = null; }`

⚠ `AggregateFaiResult(fai, faiAllPass, …)` 2줄은 **무변경**(`faiAllPass` 값 파라미터 유지).
⚠ `crossZSharedSrc` 는 별개 지역변수 — 치환 대상 아님(`\bcrossZRoleImage\b` 에 안 걸린다, 실측 확인).
⚠ try/finally 구조·Dispose 순서·`//260729`/`//260807`/`//260810` 주석 블록 전부 유지.

### 3-C. 호출부 1줄 (`MeasureShotFaiList` 안)
```
before: FinalizeFaiTick(fai, acc.FaiAllPass, faiOverlays, sharedSrc, datumSnapshot, szSharedOriginPath, parentSeq2, ref acc.CrossZRoleImage, ref acc.ShotDisplayImageReplaced, ref acc.AllPass);
after:  FinalizeFaiTick(fai, acc.FaiAllPass, faiOverlays, sharedSrc, datumSnapshot, szSharedOriginPath, parentSeq2, acc);
```

### 예상 diff: 삭제 11줄(sig 2 + 본문 8 + 호출부 1) / 코드추가 9줄 + 주석추가 1줄
  → **주석 제외 코드추가 = 9** (sig 1 + 본문 코드 7 + 호출부 1). 본문 8줄 중 1줄(3번)이 주석이므로 코드 7줄.
  → 삭제 11줄 중 1줄이 주석(3번의 before) → **"주석 삭제 0" 검사는 이 1줄을 예외로 허용**한다(치환이지 삭제가 아니다).
  </action>
  <verify>
    <automated>
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
SCR="C:/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/9d3a7b4d-2314-4b14-8686-52fd6346a1f9/scratchpad"
B="$SCR/gf1/base.cs"
xm() { awk -v p="$1" '$0 ~ p {i=1} i {print} i && /^        \}$/ {exit}' "$2"; }
xb() { xm "$1" "$2" | sed '1,/) {$/d'; }

echo "== V1. ★기계 치환 동치 — 원본(4299401) 본문에 3규칙 적용 == 현재 본문 (빈 출력) ==" && \
diff <(xb "^        private void FinalizeFaiTick" "$B" | sed 's/\bcrossZRoleImage\b/acc.CrossZRoleImage/g; s/\bbShotDisplayImageReplaced\b/acc.ShotDisplayImageReplaced/g; s/\ballPass\b/acc.AllPass/g') \
     <(xb "^        private void FinalizeFaiTick" "$F") && echo "  OK 본문 기계동치" && \

echo "== V1b. 치환 규칙 자기검증 — 8줄만 건드리고 faiAllPass 3건은 살아남는가 ==" && \
[ "$(diff <(xb '^        private void FinalizeFaiTick' "$B") <(xb '^        private void FinalizeFaiTick' "$B" | sed 's/\bcrossZRoleImage\b/acc.CrossZRoleImage/g; s/\bbShotDisplayImageReplaced\b/acc.ShotDisplayImageReplaced/g; s/\ballPass\b/acc.AllPass/g') | grep -c '^>')" = "8" ] && echo "  OK 규칙 영향범위=8줄" && \
[ "$(xb '^        private void FinalizeFaiTick' "$F" | grep -oE '\bfaiAllPass\b' | wc -l)" = "3" ] && echo "  OK faiAllPass 3건 생존(값 파라미터 유지)" && \
[ "$(xb '^        private void FinalizeFaiTick' "$F" | grep -c 'AggregateFaiResult(fai, faiAllPass,')" = "2" ] && echo "  OK AggregateFaiResult 인자 무변경 2건" && \

echo "== V2. 시그니처 정확히 5줄 ==" && \
diff <(xm "^        private void FinalizeFaiTick" "$F" | sed -n '1,/) {$/p') \
     <(printf '        private void FinalizeFaiTick(FAIConfig fai, bool faiAllPass,\n                                     List<EdgeInspectionOverlay> faiOverlays, SharedHImage sharedSrc,\n                                     List<DatumCaptureOverlay> datumSnapshot, string szSharedOriginPath,\n                                     InspectionSequence parentSeq2,\n                                     ShotMeasureAccumulator acc) {\n') && echo "  OK signature" && \

echo "== V3. 무변경 계약 메서드 전문 0줄 diff (vs 착수 전 4299401) ==" && \
for m in RunMeasure TakeCrossZRoleImageIfFirst MarkCrossZHalfPending MarkAllMeasurementsNoImage AggregateFaiResult; do
  diff <(xm "^        private void $m" "$B") <(xm "^        private void $m" "$F") >/dev/null || { echo "FAIL $m"; exit 1; }
done; echo "  OK 5/5 (RunMeasure 포함)" && \
diff <(xm "^        private void MeasureShotFaiList" "$B" | sed -n '1,/) {$/p') <(xm "^        private void MeasureShotFaiList" "$F" | sed -n '1,/) {$/p') && echo "  OK 바깥 시그니처 불변" && \

echo "== V4. 호출부 1건 ==" && \
[ "$(grep -c 'FinalizeFaiTick(fai, acc.FaiAllPass, faiOverlays, sharedSrc, datumSnapshot, szSharedOriginPath, parentSeq2, acc);' $F)" = "1" ] && \
[ "$(grep -c 'FinalizeFaiTick(' $F)" = "2" ] && echo "  OK 선언1+호출1" && \

echo "== V5. diff 규모 (직전 커밋 대비) 삭제11 / 코드추가9 / 주석삭제는 치환된 1줄만 ==" && \
[ "$(git diff -U0 -- $F | grep -c '^-[^-]')" = "11" ] && \
[ "$(git diff -U0 -- $F | grep '^+[^+]' | grep -v '^+[[:space:]]*//' | grep -vc '^+[[:space:]]*$')" = "9" ] && \
[ "$(git diff -U0 -- $F | grep '^-[^-]' | grep -c '^-[[:space:]]*//')" = "1" ] && \
git diff -U0 -- $F | grep '^-[[:space:]]*//' | grep -q 'crossZRoleImage.Dispose()' && echo "  OK 11/9, 주석삭제=치환된 Dispose 주석 1줄뿐" && \

echo "== V6. ★최종 통합 — acc 식별자 전수 카운트 ==" && \
[ "$(grep -oE '\bacc\.CrossZRoleImage\b' $F | wc -l)" = "10" ] && \
[ "$(grep -oE '\bacc\.FaiAllPass\b' $F | wc -l)" = "9" ] && \
[ "$(grep -oE '\bacc\.MeasuredCount\b' $F | wc -l)" = "10" ] && \
[ "$(grep -oE '\bacc\.NMeasNg\b' $F | wc -l)" = "3" ] && \
[ "$(grep -oE '\bacc\.AllPass\b' $F | wc -l)" = "4" ] && \
[ "$(grep -oE '\bacc\.ShotDisplayImageReplaced\b' $F | wc -l)" = "4" ] && echo "  OK acc.* = 10/9/10/3/4/4" && \
echo "  (수치가 다르면 반드시 해당 줄을 출력해 원인을 제시할 것 — 기대값 수정 금지)" && \

echo "== V7. ★최종 통합 — 옛 식별자 잔존 수 (4299401 대비) ==" && \
[ "$(grep -oE '\bcrossZRoleImage\b' $F | wc -l)" = "3" ] && \
[ "$(grep -oE '\bfaiAllPass\b' $F | wc -l)" = "11" ] && \
[ "$(grep -oE '\bmeasuredCount\b' $F | wc -l)" = "12" ] && \
[ "$(grep -oE '\bnMeasNg\b' $F | wc -l)" = "7" ] && \
[ "$(grep -oE '\ballPass\b' $F | wc -l)" = "9" ] && \
[ "$(grep -oE '\bbShotDisplayImageReplaced\b' $F | wc -l)" = "6" ] && echo "  OK 잔존 3/11/12/7/9/6 (무변경 계약 메서드 + 바깥 시그니처 + acc초기화 + 되쓰기)" && \

echo "== V8. ★최종 통합 — 착수 전(4299401) 대비 파일 전체 diff 가 5개 영역에만 있는가 ==" && \
git diff --stat 4299401 -- $F && \
[ "$(git diff 4299401 -- $F | grep -c '^-[^-]')" = "34" ] && \
[ "$(git diff 4299401 -- $F | grep '^-[^-]' | grep -c '^-[[:space:]]*//')" = "1" ] && echo "  OK 누적삭제 34줄 (per-task 6+19+11=36 중 L546/L548 중복 2 차감), 주석삭제는 치환된 1줄뿐" && \

echo "== V9. 스타일 / 전처리 ==" && \
[ "$(xm '^        private void FinalizeFaiTick' $F | grep -c '?')" = "0" ] && echo "  OK 삼항 0" && \
[ "$(grep -c '=> ' $F)" = "1" ] && echo "  OK '=> ' 1" && \
[ "$(xm '^        private void FinalizeFaiTick' $F | grep -c '^[[:space:]]*#')" = "0" ] && echo "  OK 전처리 0(단일빌드 근거 최종 유지)" && \
[ "$(grep -c '{ get; set; }' $F)" = "0" ] && echo "  OK 프로퍼티 0" && \

echo "== V10. 빌드 (G-5, OutputPath=gf1-t3, 로그 gf1/t3.log) ==" && \
WBASE="$(grep -cE 'warning CS' "$SCR/gf1/t0.log")" && \
[ "$(grep -cE 'error CS' "$SCR/gf1/t3.log")" = "0" ] && \
[ "$(grep -cE 'warning CS' "$SCR/gf1/t3.log")" = "$WBASE" ] && \
[ "$(grep -cE 'CS0206|CS0219|CS0168|CS0177|CS0165|CS0103|CS1027|CS1028' "$SCR/gf1/t3.log")" = "0" ] && echo "  OK build (warning=$WBASE)" && \
echo "===== TASK 3 VERIFY ALL PASS ====="
```
그 다음 **COMMIT**:
```bash
git add WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
git commit -m "refactor(260819-gf1): FinalizeFaiTick ref 3개를 ShotMeasureAccumulator 1개로 통합 (기계 치환, 동치 검증)"
```
그 다음 **HYGIENE**:
```bash
[ "$(git show --name-only --format='' HEAD | grep -c .)" = "1" ] && \
[ "$(git show --name-only --format='' HEAD)" = "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs" ] && \
[ "$(git status --porcelain)" = " M WPF_Example/DatumMeasurement.csproj" ] && \
git log --oneline 4299401..HEAD --name-only | grep -c 'DatumMeasurement.csproj' | grep -q '^0$' && echo "OK hygiene: 3커밋 전부 파일1개, csproj 미커밋"
```
    </automated>
  </verify>
  <done>V1–V10 전부 PASS. 3개 커밋 누적으로 `RunMeasure` 0줄 diff, 4개 헬퍼 0줄 diff, `MeasureShotFaiList` 바깥 시그니처 0줄 diff, 기계 치환 동치 2건 빈 출력, 빌드 baseline 동일, csproj 미커밋.</done>
</task>

</tasks>

<verification>

### 최종 확인 (Task 3 완료 후)

| 항목 | 기대 | 방법 |
|---|---|---|
| `RunMeasure` | 0줄 diff | `diff <(xm ... base.cs) <(xm ... $F)` |
| `MeasureShotFaiList` 바깥 시그니처 | 0줄 diff | 시그니처 5줄 diff |
| `TakeCrossZRoleImageIfFirst` | 0줄 diff | 전문 diff |
| `MarkCrossZHalfPending` | 0줄 diff | 전문 diff |
| `MarkAllMeasurementsNoImage` | 0줄 diff | 전문 diff |
| `AggregateFaiResult` | 0줄 diff | 전문 diff |
| `ProcessOneMeasurement` 본문 | 4규칙 기계 치환 동치 | Task2 V1 |
| `FinalizeFaiTick` 본문 | 3규칙 기계 치환 동치 | Task3 V1 |
| FAI 리셋 2줄 | 원본 위치 고정 | Task1 V5 |
| 되쓰기 4줄 | `using` 바깥, 메서드 끝 | Task1 V6 |
| `ShotMeasureAccumulator` | 순수 필드 6개 | Task1 V7 / Task3 V9 |
| 신규 파일 | 0개 | `git show --name-only` |
| csproj | 미커밋(unstaged) | HYGIENE |
| 빌드 | error 0 / warning = `WBASE` | t1/t2/t3.log |
| 삼항 | 3개 메서드 구간 `?` 0건 | 각 Task V |

### 실패 시 대응
- **기계 치환 동치 diff 가 비지 않음** → 코드를 원본+치환 결과로 되돌린다. **sed 규칙이나 기대값을 수정하지 않는다.**
- **`acc.*` 카운트 불일치** → 어긋난 줄을 `grep -n` 으로 출력해 제시. 원인이 신규 주석이면 그 줄을 보이고, 그 외에는 코드를 고친다.
- **CS0206** → 필드를 프로퍼티로 잘못 선언한 것. `{ get; set; }` 제거.
- **전처리 지시문 0건 전제 위반** → 즉시 2-빌드(비-SIMUL `-p:DefineConstants=TRACE`)로 승격 후 진행.
- **빌드 산출물 잠김** → `OutputPath` 이름만 바꿔 재시도. **프로세스 종료 금지.** 그래도 안 되면 사용자에게 보고.

### 런타임 UAT (사람 수행, 이번 범위 밖)
정적 검증만으로 회귀 0 을 주장한다(순수 데이터 전달 방식 변경). 실장비 확인이 필요하면:
Shot 1개 검사 → `[SEQ] Measure 완료 — 측정 N개 (공차이탈 M개), 판정 X` 로그의 N/M/X 가 변경 전과 동일한지,
크로스-Z FAI 의 캡처 이미지/표시 이미지가 종전과 같은지 확인.
</verification>

<success_criteria>
- `ShotMeasureAccumulator` 중첩 private class 신설(순수 public 필드 6개), 새 파일 0개, csproj 무변경
- `MeasureShotFaiList` 바깥 시그니처 / `RunMeasure` / 크로스-Z 헬퍼 2개 / `MarkAllMeasurementsNoImage` / `AggregateFaiResult` **전부 0줄 diff**
- `ProcessOneMeasurement`(4 ref→acc) / `FinalizeFaiTick`(3 ref→acc) 본문이 **기계 치환 동치 diff 빈 출력**
- FAI 루프 리셋 2줄 원본 위치 유지, 되쓰기 4줄이 `using` 바깥 합류점
- 빌드 error 0 / warning = 착수 전 baseline, CS0206·CS0219·CS0168·CS0177·CS0165 신규 0건
- 삼항 0건, 커밋 3개 전부 `Action_FAIMeasurement.cs` 단 1파일
</success_criteria>

<output>
완료 후 `.planning/quick/260819-gf1-processonemeasurement-measureshotfailist/260819-gf1-SUMMARY.md` 작성.
</output>
