---
phase: quick-260819-m3t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml
autonomous: true
requirements: [M3T-01, M3T-02]

must_haves:
  truths:
    - "FAI 결과 그리드(`dataGrid_faiResults`, MainView.xaml L519)의 컬럼 표시 순서가 FAI → Type → Measurement → DatumRef → Nominal → Tol+ → Tol- → 측정값 → 판정 이다 (기존 FAI → Measurement → Type → ... 에서 Measurement/Type 두 컬럼 자리만 교환)"
    - "'측정값' 컬럼(Binding=MeasuredValueText)의 Width가 `2*`, '판정' 컬럼(Binding=JudgeText)의 Width가 `1.3*`으로 바뀌어 다른 컬럼보다 넓게 표시된다"
    - "9개 컬럼의 Binding 대상(FAIName/MeasurementName/TypeName/DatumRef/NominalValue/TolerancePlus/ToleranceMinus/MeasuredValueText/JudgeText)과 Header 텍스트는 단 하나도 바뀌지 않는다"
    - "그 외 6개 컬럼(FAI/Measurement/DatumRef/Nominal/Tol+/Tol-)의 Width 값도 바뀌지 않는다 (FAI·Measurement=`*`, DatumRef/Nominal/Tol+/Tol-=`Auto` 그대로)"
    - "MainView.xaml 의 이 9줄(DataGrid.Columns 블록) 외 다른 모든 줄, 다른 모든 DataGrid, 어떤 코드비하인드(.xaml.cs)도 변경되지 않는다"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml"
      provides: "FAI 결과 DataGrid 9개 컬럼 재배치(Measurement/Type 순서 교환) + 측정값/판정 폭 확대"
      contains: "<DataGridTextColumn Header=\"측정값\" Binding=\"{Binding MeasuredValueText}\" Width=\"2*\"/>"
  key_links:
    - from: "dataGrid_faiResults의 DataGrid.Columns 블록 (9개 DataGridTextColumn)"
      to: "각 컬럼의 기존 Binding 대상 (FAIName/MeasurementName/TypeName/DatumRef/NominalValue/TolerancePlus/ToleranceMinus/MeasuredValueText/JudgeText) — 전부 무변경"
      via: "XML 엘리먼트 2개(Measurement/Type) 순서만 교환 + Width 값 2개(측정값/판정)만 교체, Header/Binding 속성은 각 엘리먼트에 그대로 유지"
      pattern: "Header=\"(FAI|Measurement|Type|DatumRef|Nominal|Tol\\+|Tol-|측정값|판정)\" Binding=\"\\{Binding (FAIName|MeasurementName|TypeName|DatumRef|NominalValue|TolerancePlus|ToleranceMinus|MeasuredValueText|JudgeText)\\}\""
---

<objective>
`WPF_Example/UI/ContentItem/MainView.xaml` 의 FAI 측정 결과 그리드(`x:Name="dataGrid_faiResults"`, L519)
`<DataGrid.Columns>` 블록(9개 `<DataGridTextColumn>`, L570–580) 에서
① Measurement / Type 두 컬럼의 표시 순서를 교환하고
② 사용자가 가장 자주 보는 "측정값" / "판정" 두 컬럼의 폭을 넓힌다.

Purpose: 현재 순서(FAI→Measurement→Type)는 Type 을 먼저 보고 싶어하는 사용자 워크플로우와 맞지 않고,
"측정값"/"판정" 컬럼은 `Width="Auto"` 라 헤더 텍스트 폭까지만 잡혀 실제 측정값/판정 문자열이 잘려 보인다.

Output: `MainView.xaml` 1개 파일, 9줄 블록 안에서만 변경(순서 교환 2줄 + Width 값 교체 2줄). 커밋 1개.
순수 XAML 마크업 편집이므로 C# 코딩 컨벤션(삼항/헝가리언 등)은 해당 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

**착수 전 필수 확인 (30초). 하나라도 다르면 즉시 중단하고 사용자에게 보고 — 아래 줄번호가 무효화된다:**
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/UI/ContentItem/MainView.xaml
git rev-parse --short HEAD          # 기대: 059df48 (다르면 계속 진행하되 verify 블록의 BASE 를 이 값으로 갱신)
git status --porcelain              # 기대: " M WPF_Example/DatumMeasurement.csproj" 단 1줄 (+ 미추적 .planning/quick/* 디렉터리)
git status --porcelain -- $F        # 기대: 출력 없음 (clean)
wc -l $F                            # 기대: 602
sed -n '519p;570,580p' $F
```
기대 출력(순서대로):
```
        <DataGrid x:Name="dataGrid_faiResults" Grid.Row="3"
            <DataGrid.Columns>
                <DataGridTextColumn Header="FAI" Binding="{Binding FAIName}" Width="*"/>
                <DataGridTextColumn Header="Measurement" Binding="{Binding MeasurementName}" Width="*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding TypeName}" Width="Auto"/>
                <DataGridTextColumn Header="DatumRef" Binding="{Binding DatumRef}" Width="Auto"/>
                <DataGridTextColumn Header="Nominal" Binding="{Binding NominalValue}" Width="Auto"/>
                <DataGridTextColumn Header="Tol+" Binding="{Binding TolerancePlus}" Width="Auto"/>
                <DataGridTextColumn Header="Tol-" Binding="{Binding ToleranceMinus}" Width="Auto"/>
                <DataGridTextColumn Header="측정값" Binding="{Binding MeasuredValueText}" Width="Auto"/>
                <DataGridTextColumn Header="판정" Binding="{Binding JudgeText}" Width="Auto"/>
            </DataGrid.Columns>
```

**⚠ 워킹트리 오염 주의 (이 프로젝트 최대 사고 위험):**
`WPF_Example/DatumMeasurement.csproj` 에 **커밋하면 안 되는 로컬 설정**이 떠 있다 —
Debug `OutputPath=D:\Data\`, Release `DefineConstants` 의 `SIMUL_MODE`.
저장소에 들어가면 현장 배포본이 시뮬레이션 모드로 나간다.
→ **`git add -A` / `git add .` / `git commit -a` 절대 금지.** 반드시 대상 파일 1개만 경로로 스테이징한다.
</context>

<ground_rules>
### G-1. 허용되는 변환은 정확히 2종
1. L572("Measurement")·L573("Type") 두 `<DataGridTextColumn>` 엘리먼트의 **순서만** 교환한다.
   각 엘리먼트의 `Header`/`Binding`/`Width` 속성은 그 엘리먼트에 붙은 채로 그대로 이동한다
   (즉 Type 은 여전히 `Width="Auto"`, Measurement 는 여전히 `Width="*"` — 서로 바뀌지 않는다).
2. L578("측정값")·L579("판정") 두 엘리먼트는 위치를 바꾸지 않고 **`Width` 값만** 교체한다:
   측정값 `Auto`→`2*`, 판정 `Auto`→`1.3*`.
- **그 외 어떤 편집도 금지**: Header 텍스트/Binding 경로 수정, 다른 5개 컬럼(FAI/DatumRef/Nominal/Tol+/Tol-)의 Width 변경,
  컬럼 추가/삭제, `<DataGrid.Columns>`/`</DataGrid.Columns>` 태그 변경, 상위 `<DataGrid ...>` 속성(L519–524) 변경,
  다른 스타일 블록(ColumnHeaderStyle/CellStyle/RowStyle, L525–569) 변경, 파일의 다른 DataGrid(없음 — 이 파일엔 `dataGrid_faiResults` 1개뿐, 착수 전 확인 완료) 접촉.
- **범위 확장 금지**: `.xaml.cs` 코드비하인드, ViewModel, 다른 XAML 파일 전부 무접촉.

### G-2. 순수 마크업 편집 — C# 컨벤션 무관
이 작업은 XAML 속성값 편집이므로 삼항 연산자/헝가리언 표기법 등 C# 코딩 컨벤션은 해당 없음.

### G-3. 빌드 규칙
- 앱이 `D:\Data\` 에서 실행 중일 수 있다 → **프로세스 종료 절대 금지.** 스크래치 `OutputPath` 로 컴파일만 검증.
- **`//p:` 금지, `-p:` 사용** (`/` 섞이면 Git Bash 가 `MSB1001` 로 죽는다).
- **경고 baseline = 12줄 (CS0618×10 + CS0162×2).** "경고 0" 을 통과 기준으로 쓰면 항상 거짓 실패. XAML 편집이라도
  전체 리빌드 시 기존 .cs 파일의 경고는 그대로 나온다 — baseline 과 동일하면 PASS.
- OutputPath 후행 백슬래시는 반드시 `\\` 로 쓸 것 (`"$SCR\m3t\"` 는 bash 문법 에러).

### G-4. 커밋 위생
`git add -A`/`git add .`/`git commit -a` 절대 금지. `git add WPF_Example/UI/ContentItem/MainView.xaml` 로 대상 파일만 스테이징.
커밋 후 `WPF_Example/DatumMeasurement.csproj` 가 여전히 unstaged(` M`)인지 반드시 확인.
</ground_rules>

<tasks>

<task type="auto">
  <name>Task 1: Measurement/Type 순서 교환 + 측정값/판정 폭 확대</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml</files>
  <action>
**0단계 — 기준점 고정:**
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/UI/ContentItem/MainView.xaml
SCR="C:\Users\tech\AppData\Local\Temp\claude\C--Info-Project-DataMeasurement\9d3a7b4d-2314-4b14-8686-52fd6346a1f9\scratchpad"
BASE=$(git rev-parse --short HEAD)
[ -f "$SCR/m3t-git-baseline.txt" ] || git status --porcelain > "$SCR/m3t-git-baseline.txt"
git show $BASE:$F > "$SCR/m3t-base-mainview.txt"
wc -l "$SCR/m3t-base-mainview.txt"   # 602 이어야 함
```
G-3 빌드를 착수 전 상태에서 1회 돌려 경고 줄을 `$SCR/m3t-baseline-warn.txt` 에 저장한다(아래 빌드 커맨드 참고, 출력을 파일로 리다이렉트).

---

**1단계 — 편집.** Edit 도구로 L571–579(9줄, `<DataGrid.Columns>` 바로 아래)를 아래 "이전"에서 "이후"로 정확히 바꾼다.
들여쓰기(16칸)와 그 외 7줄은 문자 하나도 바꾸지 않는다.

이전(9줄):
```xml
                <DataGridTextColumn Header="FAI" Binding="{Binding FAIName}" Width="*"/>
                <DataGridTextColumn Header="Measurement" Binding="{Binding MeasurementName}" Width="*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding TypeName}" Width="Auto"/>
                <DataGridTextColumn Header="DatumRef" Binding="{Binding DatumRef}" Width="Auto"/>
                <DataGridTextColumn Header="Nominal" Binding="{Binding NominalValue}" Width="Auto"/>
                <DataGridTextColumn Header="Tol+" Binding="{Binding TolerancePlus}" Width="Auto"/>
                <DataGridTextColumn Header="Tol-" Binding="{Binding ToleranceMinus}" Width="Auto"/>
                <DataGridTextColumn Header="측정값" Binding="{Binding MeasuredValueText}" Width="Auto"/>
                <DataGridTextColumn Header="판정" Binding="{Binding JudgeText}" Width="Auto"/>
```

이후(9줄):
```xml
                <DataGridTextColumn Header="FAI" Binding="{Binding FAIName}" Width="*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding TypeName}" Width="Auto"/>
                <DataGridTextColumn Header="Measurement" Binding="{Binding MeasurementName}" Width="*"/>
                <DataGridTextColumn Header="DatumRef" Binding="{Binding DatumRef}" Width="Auto"/>
                <DataGridTextColumn Header="Nominal" Binding="{Binding NominalValue}" Width="Auto"/>
                <DataGridTextColumn Header="Tol+" Binding="{Binding TolerancePlus}" Width="Auto"/>
                <DataGridTextColumn Header="Tol-" Binding="{Binding ToleranceMinus}" Width="Auto"/>
                <DataGridTextColumn Header="측정값" Binding="{Binding MeasuredValueText}" Width="2*"/>
                <DataGridTextColumn Header="판정" Binding="{Binding JudgeText}" Width="1.3*"/>
```

변경점은 정확히 4곳: ②Measurement·③Type 두 줄이 자리를 바꿈(속성은 각 줄에 그대로 붙어 이동),
⑧측정값 Width `Auto→2*`, ⑨판정 Width `Auto→1.3*`. 나머지 5줄(FAI/DatumRef/Nominal/Tol+/Tol-)은 원본과 완전히 동일해야 한다.

---

**2단계 — 빌드 + 정적 검증 (커밋 전).** verify 블록의 자동화 검증 전부와 G-3 빌드를 여기서 실행한다.

**3단계 — 커밋. `git add -A` 금지, 대상 파일만:**
```bash
cd /c/Info/Project/DataMeasurement
git add WPF_Example/UI/ContentItem/MainView.xaml
git diff --cached --name-only          # 정확히 1줄이어야 함
git commit -m "fix(260819-m3t): FAI 결과 그리드 Measurement/Type 순서 교환 + 측정값/판정 폭 확대"
git status --porcelain -- WPF_Example/DatumMeasurement.csproj   # 여전히 " M" (unstaged) 여야 함
```
  </action>
  <verify>
    <automated>
# [1] 라인 수 불변 + 편집 범위 밖(L1-570, L580-602) 완전 무변경 — 가장 강한 "다른 곳 안 건드림" 증명
cd /c/Info/Project/DataMeasurement && F=WPF_Example/UI/ContentItem/MainView.xaml && \
SCR="C:\Users\tech\AppData\Local\Temp\claude\C--Info-Project-DataMeasurement\9d3a7b4d-2314-4b14-8686-52fd6346a1f9\scratchpad" && \
[ "$(wc -l < $F)" = "602" ] && \
diff <(sed -n '1,570p;580,602p' "$SCR/m3t-base-mainview.txt") <(sed -n '1,570p;580,602p' $F) && \
echo "T1 OUT-OF-SCOPE UNCHANGED PASS"
    </automated>
    <automated>
# [2] 편집 범위 안(L571-579) 정확한 순서 + Width 값 + Binding/Header 무변경
cd /c/Info/Project/DataMeasurement && F=WPF_Example/UI/ContentItem/MainView.xaml && \
[ "$(sed -n '571p' $F)" = '                <DataGridTextColumn Header="FAI" Binding="{Binding FAIName}" Width="*"/>' ] && \
[ "$(sed -n '572p' $F)" = '                <DataGridTextColumn Header="Type" Binding="{Binding TypeName}" Width="Auto"/>' ] && \
[ "$(sed -n '573p' $F)" = '                <DataGridTextColumn Header="Measurement" Binding="{Binding MeasurementName}" Width="*"/>' ] && \
[ "$(sed -n '574p' $F)" = '                <DataGridTextColumn Header="DatumRef" Binding="{Binding DatumRef}" Width="Auto"/>' ] && \
[ "$(sed -n '575p' $F)" = '                <DataGridTextColumn Header="Nominal" Binding="{Binding NominalValue}" Width="Auto"/>' ] && \
[ "$(sed -n '576p' $F)" = '                <DataGridTextColumn Header="Tol+" Binding="{Binding TolerancePlus}" Width="Auto"/>' ] && \
[ "$(sed -n '577p' $F)" = '                <DataGridTextColumn Header="Tol-" Binding="{Binding ToleranceMinus}" Width="Auto"/>' ] && \
[ "$(sed -n '578p' $F)" = '                <DataGridTextColumn Header="측정값" Binding="{Binding MeasuredValueText}" Width="2*"/>' ] && \
[ "$(sed -n '579p' $F)" = '                <DataGridTextColumn Header="판정" Binding="{Binding JudgeText}" Width="1.3*"/>' ] && \
echo "T1 COLUMN ORDER + WIDTH PASS"
    </automated>
    <automated>
# [3] Binding 대상 9종 각 1건 유지 (하나도 사라지거나 중복되지 않음) + 파일 내 DataGrid 1개뿐(다른 그리드 무접촉 확인)
cd /c/Info/Project/DataMeasurement && F=WPF_Example/UI/ContentItem/MainView.xaml && \
for b in FAIName MeasurementName TypeName DatumRef NominalValue TolerancePlus ToleranceMinus MeasuredValueText JudgeText; do \
  [ "$(grep -cF "Binding=\"{Binding $b}\"" $F)" = "1" ] || { echo "BINDING BROKEN: $b"; exit 1; }; done && \
[ "$(grep -cE '^[[:space:]]*<DataGrid x:Name=' $F)" = "1" ] && \
echo "T1 BINDINGS + SINGLE-GRID PASS"
    </automated>
    <automated>
# [4] 코드비하인드 무접촉 + 커밋 위생 (⚠ git add/commit 이후에 실행)
cd /c/Info/Project/DataMeasurement && \
SCR="C:\Users\tech\AppData\Local\Temp\claude\C--Info-Project-DataMeasurement\9d3a7b4d-2314-4b14-8686-52fd6346a1f9\scratchpad" && \
[ "$(git show --stat --name-only --format= HEAD | grep -v '^$' | wc -l)" = "1" ] && \
git show --name-only --format= HEAD | grep -q 'MainView.xaml' && \
[ "$(git show --name-only --format= HEAD | grep -cE 'DatumMeasurement.csproj|\.xaml\.cs$')" = "0" ] && \
git status --porcelain -- WPF_Example/DatumMeasurement.csproj | grep -q '^ M' && \
diff <(cut -c4- "$SCR/m3t-git-baseline.txt" | sort) <(git status --porcelain | cut -c4- | sort) | grep -c '^[<>]' | grep -qE '^[01]$' && \
echo "T1 HYGIENE PASS"
    </automated>
    <automated>
# [5] msbuild Debug|x64 스크래치 OutDir 빌드 — 성공 + 경고가 baseline(12줄: CS0618x10 + CS0162x2)과 동일
cd /c/Info/Project/DataMeasurement && \
SCR="C:\Users\tech\AppData\Local\Temp\claude\C--Info-Project-DataMeasurement\9d3a7b4d-2314-4b14-8686-52fd6346a1f9\scratchpad" && \
MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" && \
"$MSB" 'C:\Info\Project\DataMeasurement\WPF_Example\DatumMeasurement.csproj' \
  -p:Configuration=Debug -p:Platform=x64 -p:OutputPath="$SCR\\m3t-build\\" \
  -t:Rebuild -v:minimal -nologo > "$SCR/m3t-build.log" 2>&1; \
grep -c ": warning " "$SCR/m3t-build.log"   # baseline 과 동일한 12 이어야 함(다르면 신규 경고 발생, 즉시 보고)
tail -5 "$SCR/m3t-build.log"                # "Build succeeded." 확인
    </automated>
  </verify>
  <done>
`dataGrid_faiResults` 의 컬럼 순서가 FAI→Type→Measurement→DatumRef→Nominal→Tol+→Tol-→측정값→판정 으로 바뀌고,
측정값 Width=`2*`, 판정 Width=`1.3*` 로 넓어졌다. 나머지 5개 컬럼의 Width 와 9개 컬럼의 Header/Binding 은 전부 원본과 동일.
파일 전체 라인 수 602 유지, 편집 범위(L571–579) 밖은 바이트 단위로 무변경, 다른 DataGrid/코드비하인드/csproj 무접촉.
msbuild Debug|x64 성공 + 경고 12줄 baseline 동일. 커밋 1개, 스테이징 파일 정확히 1개.
  </done>
</task>

</tasks>

<verification>
1. `wc -l MainView.xaml` → 602 (라인 추가/삭제 없음, 순수 내용 교체)
2. L571–579 가 정확히 목표 9줄과 문자 단위로 일치
3. 편집 범위 밖(L1–570, L580–602)이 BASE 와 diff 0
4. Binding 대상 9종 각 1건, DataGrid 1개(`dataGrid_faiResults`)만 존재
5. `git show --name-only HEAD` → `MainView.xaml` 단 1개 파일만
6. `git status --porcelain -- DatumMeasurement.csproj` → 여전히 ` M`(unstaged)
7. msbuild Debug|x64 성공 + 경고 12줄 baseline 동일
</verification>

<success_criteria>
- FAI 결과 그리드 컬럼 순서: FAI → Type → Measurement → DatumRef → Nominal → Tol+ → Tol- → 측정값 → 판정
- "측정값"·"판정" 컬럼이 `2*`/`1.3*` 로 다른 컬럼보다 넓게 표시됨
- 9개 컬럼의 Binding/Header 100% 보존, 나머지 5개 컬럼 Width 100% 보존
- `MainView.xaml` 외 무변경(코드비하인드·csproj·다른 DataGrid 포함), 빌드 PASS(경고 12줄 baseline), 커밋 1개
</success_criteria>

<output>
완료 후 `.planning/quick/260819-m3t-fai-grid-column-order/260819-m3t-SUMMARY.md` 생성
</output>
