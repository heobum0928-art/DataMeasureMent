---
phase: quick-260819-n4d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ContentItem/MainView.xaml
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: false
requirements: [QUICK-260819-N4D-01]

must_haves:
  truths:
    - "메인 화면에 기존 Calibrate 버튼과 별개인 새 '거리측정' 버튼이 있고, 클릭 후 캔버스에서 두 점을 순서대로 클릭하면 즉시(추가 입력 없이) 두 점 사이 실제 거리(mm)가 총/가로/세로 3값 + 픽셀거리로 화면에 표시된다"
    - "선택된 FAI/Shot 을 통해 PixelResolution 을 구할 수 없는 경우(예: FAI 미선택 상태), 예외 없이 픽셀거리만 표시되는 문구로 조용히 폴백한다 — 크래시/빈 화면 없음"
    - "거리측정 기능은 shot.PixelResolution / fai.PixelResolutionX / fai.PixelResolutionY 등 어떤 캘리브레이션 값도 절대 쓰지(write) 않는다 — 순수 read-only 표시 기능이다"
    - "기존 Calibrate 버튼(2점 클릭 + 실측 mm 수동입력 → 보정값 재계산·저장) 플로우는 CalibrateButton_Click / HalconViewer_CalibrationMouseDown / FinishCalibration / ApplyCalibrationResult 4개 메서드 본문이 이번 변경으로 단 한 글자도 바뀌지 않는다 — diff 상 해당 파일 전체에서 순수 삽입만 존재해야 하며, 유일한 예외는 ECanvasMode enum 선언 1줄(기존 줄이 새 값 추가된 줄로 교체)이다"
    - "두 버튼(Calibrate / 거리측정)은 서로 독립적인 ECanvasMode(Calibration / DistanceMeasure)를 쓰므로 한쪽 진행 중 ESC 또는 다른 툴바 버튼을 눌러도 다른 쪽 상태와 뒤섞이지 않는다"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/MainView.xaml"
      provides: "btn_calibrate 옆 새 툴바 버튼 (btn_measureDistance, Click=MeasureDistanceButton_Click)"
      contains: "x:Name=\"btn_measureDistance\""
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "ECanvasMode.DistanceMeasure + _measurePoints 필드 + MeasureDistanceButton_Click/HalconViewer_MeasureMouseDown/FinishDistanceMeasure 3개 메서드 + ExitCanvasMode 거리측정 cleanup 블록"
      contains: "private void FinishDistanceMeasure()"
  key_links:
    - from: "btn_measureDistance Click (XAML)"
      to: "MeasureDistanceButton_Click (MainView.xaml.cs)"
      via: "Click=\"MeasureDistanceButton_Click\" 바인딩"
      pattern: "Click=\"MeasureDistanceButton_Click\""
    - from: "HalconViewer_MeasureMouseDown 의 2번째 클릭"
      to: "FinishDistanceMeasure"
      via: "_measurePoints.Count == 2 분기에서 직접 호출"
      pattern: "FinishDistanceMeasure\\(\\);"
    - from: "FinishDistanceMeasure 의 shot 조회"
      to: "shot.GetEffectivePixelResolution()"
      via: "dataGrid_faiResults.SelectedItem → FindFAIByName → anchorFai.Owner as ShotConfig"
      pattern: "shot\\.GetEffectivePixelResolution\\(\\)"
---

<objective>
메인 화면에 두 점을 클릭하면 즉시 실제 거리(mm)를 보여주는 새 "거리측정" 버튼을 추가한다. 기존 "Calibrate" 버튼(2점 클릭 + 실측 mm 수동입력 → 보정값 재계산·저장)은 완전히 그대로 둔다.

Purpose: 사용자가 두 가지 다른 용도를 모두 확인 — 하나는 이미 있는 보정값으로 즉시 거리를 확인(신규, read-only), 다른 하나는 실측 자값을 입력해 보정값 자체를 재계산(기존, write). 둘을 하나로 합치지 않고 버튼 2개로 분리(AskUserQuestion 으로 확정된 결정).
Output: `MainView.xaml`(버튼 1개 추가) + `MainView.xaml.cs`(enum 값 1개, 필드 1개, 메서드 3개, ExitCanvasMode 확장) — 기존 Calibrate 관련 코드는 순수 삽입 외 무변경.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

**코딩 규칙 (이 프로젝트 상시 규칙 — 위반 시 리젝)**
- 삼항 연산자 `?:` **금지** → 반드시 if-else.
- C# 7.2 문법만 (nullable 참조형 / switch 식 / record 등 8.0+ 금지). `anchorFai.Owner as ShotConfig` 같은 `as` 패턴은 기존 코드(`ApplyCalibrationResult`)에서 이미 쓰는 문법이라 안전.
- 브레이스 스타일: 이 파일은 **K&R**(여는 중괄호 같은 줄) — `CalibrateButton_Click`/`FinishCalibration` 등 기존 메서드와 동일하게 맞춘다.
- 새 `using` 추가 금지 — `SolidColorBrush`/`Colors`(`System.Windows.Media`), `System.Windows.Threading.DispatcherTimer`(정규화된 이름으로 이미 `FinishCalibration`에서 사용 중), `FAIConfig`/`ShotConfig`/`MeasurementResultRow` 전부 이미 이 파일에서 정규화 없이 쓰이고 있다 — 새 using 불필요.
- 새 주석은 `//quick-260819-n4d:` 접두, 비자명한 "왜"만.

## 절대 건드리면 안 되는 것 (이번 범위 밖)
1. **`CalibrateButton_Click` / `HalconViewer_CalibrationMouseDown` / `FinishCalibration` / `ApplyCalibrationResult`** — 4개 메서드 본문 전체. 새 코드는 이 메서드들 주변에 삽입만 하고, 내부 로직은 단 한 줄도 옮기거나 고치지 않는다. `TextInputBoxWinidow` 다이얼로그 호출부도 무변경.
2. **`_calibrationPoints` 필드, `btn_calibrate` 컨트롤, `MinCalibrationPixelDistance` 상수(값 재사용은 OK, 정의는 무변경)** — 그대로 둔다.
3. **`shot.PixelResolution` / `fai.PixelResolutionX` / `fai.PixelResolutionY` 에 대한 쓰기(write)** — 이번 신규 기능은 어디에서도 이 값들에 대입하지 않는다. 읽기만 한다.
4. **새 다이얼로그/Window 생성 금지** — 표시는 전부 기존 `label_message` 재사용(캘리브레이션 완료 토스트와 동일 패턴).

## 조사 완료 사실 (실제 파일 Read/grep 으로 검증 — 실행자는 재탐색 불필요)

**대상 파일**: `WPF_Example/UI/ContentItem/MainView.xaml` (툴바), `WPF_Example/UI/ContentItem/MainView.xaml.cs` (코드비하인드).

### (A) XAML — btn_calibrate 원문 (편집 전, L332~348)
```xml
                    <Button x:Name="btn_calibrate" Content="Calibrate"
                        Click="CalibrateButton_Click"
                        MinWidth="80" Height="28" Padding="12,4"
                        FontSize="13" FontWeight="SemiBold"
                        Background="#2563EB" Foreground="White"
                        BorderBrush="#1D4ED8" BorderThickness="1">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <!--260623 hbk Phase 53: 체커보드 픽셀 캘리브 창 진입점 (기존 2점 Calibrate 와 별도, D-04 별도 Window).-->
                    <Button x:Name="btn_checkerboardCalibrate" Content="체커보드 캘리브"
                        ...
```
바로 다음(`btn_calibrate`의 `</Button>` 직후, 체커보드 버튼의 주석/Button 이전)에 새 버튼을 삽입한다 — 순수 삽입, 기존 줄 무변경.

### (B) C# — 핵심 심볼 (편집 전, 실측 줄번호)
| 줄 | 내용 |
|---|---|
| 28 | `private const double MinCalibrationPixelDistance = 1.0;` (재사용 대상, 무변경) |
| 29 | `private const double MessageDisplaySeconds = 3.0;` (재사용 대상, 무변경) |
| 46 | `private enum ECanvasMode { None, RectRoi, PolygonRoi, CircleRoi, TeachDatum, Calibration, PatternRoi, PatternRoi2 } //260618 hbk Phase 54 ALIGN-01 / 260619 Phase 55 ALIGN-02 (PatternRoi2, AlignLineRoi 제거)` — **이 한 줄만 유일하게 교체 대상**(끝에 `DistanceMeasure` 추가) |
| 77 | `private readonly List<System.Windows.Point> _calibrationPoints = new List<System.Windows.Point>();` (참고 패턴, 무변경) |
| 2612~2647 | `ExitCanvasMode()` 전체 (아래 (C) 참조) |
| 3093~3193 | `CalibrateButton_Click` → `HalconViewer_CalibrationMouseDown` → `FinishCalibration` → `ApplyCalibrationResult` (아래 (D) 참조, 전부 무변경 — 새 메서드는 `ApplyCalibrationResult`의 닫는 `}` 직후에 삽입) |

### (C) `ExitCanvasMode()` 원문 전체 (편집 전, L2612~2647)
```csharp
        private void ExitCanvasMode() {
            // Unsubscribe Halcon 브릿지 이벤트 (safe to call even if not subscribed)
            halconViewer.ImageLeftClicked -= HalconViewer_PolygonMouseDown;
            halconViewer.ImageRightClicked -= HalconViewer_PolygonRightClick;
            halconViewer.RectDrawingCompleted -= HalconViewer_RectDrawingCompleted;
            halconViewer.CircleDrawingCompleted -= HalconViewer_CircleDrawingCompleted;
            // Datum 티칭 핸들러 unsubscribe (Double-subscribe 방지)
            halconViewer.RectDrawingCompleted   -= HalconViewer_DatumRectCompleted;
            halconViewer.CircleDrawingCompleted -= HalconViewer_DatumCircleCompleted;
            // 패턴 ROI 핸들러 unsubscribe (260618 hbk Phase 54 ALIGN-01)
            halconViewer.RectDrawingCompleted   -= HalconViewer_PatternRectCompleted;
            //260619 hbk Phase 55 ALIGN-02 패턴 2 핸들러 unsubscribe
            halconViewer.RectDrawingCompleted   -= HalconViewer_PatternRect2Completed;

            _canvasMode = ECanvasMode.None;
            _editingFai = null;
            _editingCircleMeasurement = null;
            _editingCircleFaiName = null;
            _editingMeasurement = null;
            _editingMeasurementFaiName = null;
            _editingDatum = null;
            btn_rectRoi.IsChecked = false;
            btn_polygonRoi.IsChecked = false;
            btn_circleRoi.IsChecked = false;
            btn_teachDatum.IsChecked = false;
            label_drawHint.Visibility = Visibility.Collapsed;
            label_pointCount.Visibility = Visibility.Collapsed;
            halconViewer.ClearPolygonDraft();
            _polygonPoints.Clear();

            // Calibration cleanup
            halconViewer.ImageLeftClicked -= HalconViewer_CalibrationMouseDown;
            halconViewer.ClearCalibrationOverlay();
            btn_calibrate.Content = "Calibrate";
            _calibrationPoints.Clear();
        }
```
**중요**: `halconViewer.ClearCalibrationOverlay()`(L2644)는 오버레이 표시 대상이 무엇이든(캘리브레이션 점이든 거리측정 점이든) 동일한 내부 오버레이 객체를 지우는 **공용** 메서드다(`SetCalibrationOverlay(IList<Point>)`도 마찬가지로 공용, `MainResultViewerControl.xaml.cs` L792/799 확인 완료). 따라서 거리측정 cleanup 블록에서 `ClearCalibrationOverlay()`를 **또 호출할 필요 없다** — 위 L2644 호출이 어느 모드에서 나왔든 이미 처리한다. 새 cleanup 블록은 버튼 문구 리셋 + `_measurePoints.Clear()`만 하면 된다.

### (D) `CalibrateButton_Click` ~ `ApplyCalibrationResult` 원문 전체 (편집 전, L3093~3193) — 무변경, 참고용
```csharp
        private void CalibrateButton_Click(object sender, RoutedEventArgs e) {
            ExitCanvasMode();
            _canvasMode = ECanvasMode.Calibration;
            _calibrationPoints.Clear();

            btn_calibrate.Content = "Pick Point 1";
            label_drawHint.Content = "캔버스에서 첫 번째 점을 클릭하세요";
            label_drawHint.Visibility = Visibility.Visible;

            halconViewer.ImageLeftClicked += HalconViewer_CalibrationMouseDown;
        }

        private void HalconViewer_CalibrationMouseDown(object sender, MainViewerPointerChangedEventArgs e) {
            if (_canvasMode != ECanvasMode.Calibration) return;

            var pos = new System.Windows.Point(e.X, e.Y);
            _calibrationPoints.Add(pos);
            halconViewer.SetCalibrationOverlay(_calibrationPoints);

            if (_calibrationPoints.Count == 1) {
                btn_calibrate.Content = "Pick Point 2";
                label_drawHint.Content = "캔버스에서 두 번째 점을 클릭하세요";
            }
            else if (_calibrationPoints.Count == 2) {
                halconViewer.ImageLeftClicked -= HalconViewer_CalibrationMouseDown;
                FinishCalibration();
            }
        }

        private void FinishCalibration() {
            var p1 = _calibrationPoints[0];
            var p2 = _calibrationPoints[1];

            double dx = p2.X - p1.X;
            double dy = p2.Y - p1.Y;
            double pixelDistance = Math.Sqrt(dx * dx + dy * dy);

            if (pixelDistance < MinCalibrationPixelDistance) {
                CustomMessageBox.Show("두 점 사이의 거리가 너무 가깝습니다.", "캘리브레이션");
                ExitCanvasMode();
                return;
            }

            // NOTE: class name typo in original code: TextInputBoxWinidow (not Window)
            var dlg = new TextInputBoxWinidow(
                string.Format("두 점 사이의 실제 거리(mm)를 입력하세요:\n(픽셀 거리: {0:F1} px)", pixelDistance),
                "");
            dlg.Title = "실제 거리 입력";
            dlg.Owner = Window.GetWindow(this);

            if (dlg.ShowDialog() == true) {
                double realMm;
                if (double.TryParse(dlg.Text, out realMm) && realMm > 0) {
                    double mmPerPixel = realMm / pixelDistance;

                    ApplyCalibrationResult(mmPerPixel);

                    // Show confirmation for 3 seconds (per UI-SPEC)
                    label_message.Content = string.Format("1 px = {0:F4} mm 적용됨", mmPerPixel);
                    label_message.Foreground = new SolidColorBrush(Colors.White);
                    label_message.Visibility = Visibility.Visible;

                    var timer = new System.Windows.Threading.DispatcherTimer();
                    timer.Interval = TimeSpan.FromSeconds(MessageDisplaySeconds);
                    timer.Tick += (s, args) => {
                        timer.Stop();
                        label_message.Visibility = Visibility.Collapsed;
                    };
                    timer.Start();
                }
                else {
                    CustomMessageBox.Show("유효한 숫자를 입력하세요.", "캘리브레이션");
                }
            }

            ExitCanvasMode();
        }

        /// <summary>Applies mm/pixel calibration to the current camera's CameraSlaveParam and all FAIs (per D-12).</summary>
        private void ApplyCalibrationResult(double mmPerPixel) {
            var selectedRow = dataGrid_faiResults.SelectedItem as MeasurementResultRow;
            FAIConfig anchorFai;
            if (selectedRow != null) anchorFai = FindFAIByName(selectedRow.FAIName);
            else                     anchorFai = null;
            if (anchorFai != null) {
                var shot = anchorFai.Owner as ShotConfig;
                if (shot == null) {
                    CustomMessageBox.Show("샷 정보를 찾을 수 없습니다.", "캘리브레이션");
                    return;
                }

                // CameraSlaveParam is the shot itself (ShotConfig extends CameraSlaveParam)
                shot.PixelResolution = mmPerPixel;

                // Also update all FAIs under this shot for RoiDefinition compatibility
                foreach (FAIConfig fai in shot.FAIList) {
                    fai.PixelResolutionX = mmPerPixel;
                    fai.PixelResolutionY = mmPerPixel;
                }
            }
        }
```

### (E) mm 환산 값 선택 근거 — `GetEffectivePixelResolution()` 사용 (raw `PixelResolution` 아님)
`WPF_Example/Sequence/Param/CameraSlaveParam.cs` L109~114 (실측):
```csharp
        //260619 hbk per-shot 보정계수 적용된 유효 분해능 = PixelResolution × CorrectionFactor. 측정 mm 소비 단일소스(Action_FAIMeasurement :265 + EdgePairDistance :74 양 경로 호출). 메서드 = INI 직렬화 안 됨 → PixelResolution 저장값 불변 보존.
        public double GetEffectivePixelResolution() {
            //260619 hbk CorrectionFactor ≤0(미설정 0 로드 잔재 or 오입력)은 무보정 1.0 으로 안전 처리 — 측정값 0/음수화 방지. >2% 이탈은 가드레일이 별도 경고.
            double factor = (CorrectionFactor > 0.0) ? CorrectionFactor : 1.0;
            return PixelResolution * factor;
        }
```
이 프로젝트는 실제 mm 소비(측정) 경로 전부 — `Action_FAIMeasurement.cs:531`, `EdgePairDistanceMeasurement.cs:80` — 를 `GetEffectivePixelResolution()`(PixelResolution × CorrectionFactor) 단일소스로 통일해 놓았다(per-shot 보정계수 layer, ~0.5% 캘리브 간극 보정). `ApplyCalibrationResult`/`ApplyCheckerboardCalibration`처럼 **값을 쓰는(write) 코드**는 raw `PixelResolution`을 쓰지만, 이번 신규 기능은 **값을 읽어 실거리를 보여주는(read) 코드**이므로 같은 성격의 기존 소비처(측정 경로)와 동일하게 `shot.GetEffectivePixelResolution()`을 쓴다 — 이것이 "실제 거리"라는 요구사항에 가장 정확히 부합하고, 새 휴리스틱을 발명하는 게 아니라 기존 단일소스 패턴을 그대로 따르는 것이다.

### (F) FAI/Shot 조회 체인 (read-only, `ApplyCalibrationResult`와 동일 패턴)
```csharp
var selectedRow = dataGrid_faiResults.SelectedItem as MeasurementResultRow;
FAIConfig anchorFai;
if (selectedRow != null) anchorFai = FindFAIByName(selectedRow.FAIName);
else                     anchorFai = null;
ShotConfig shot;
if (anchorFai != null) shot = anchorFai.Owner as ShotConfig;
else                   shot = null;
// shot.GetEffectivePixelResolution() — mm/pixel (보정계수 포함)
```
`FindFAIByName`(L764, 무변경 대상)은 이미 이 파일에 존재하고 null-safe(`faiName` 빈값/`pSeq` null 시 null 반환).

### (G) 오버레이/이벤트 API (기존, 무변경 — 재사용만)
- `halconViewer.SetCalibrationOverlay(IList<Point> points)` — `MainResultViewerControl.xaml.cs` L792. 점 목록을 그리는 범용 메서드(캘리브레이션 전용 아님 — 파라미터 타입이 이미 범용 `IList<Point>`).
- `halconViewer.ClearCalibrationOverlay()` — L799. 오버레이 클리어, 범용.
- `event EventHandler<MainViewerPointerChangedEventArgs> ImageLeftClicked` — L172. 좌클릭마다 발생, 여러 구독자가 각자 `_canvasMode` 가드로 필터링하는 기존 패턴(`HalconViewer_CalibrationMouseDown`, `HalconViewer_PolygonMouseDown` 등과 동일).

### (H) 빌드 baseline (편집 전, 실측)
`git status --porcelain -- WPF_Example` = 1줄 (`M WPF_Example/DatumMeasurement.csproj`, 사용자 사전 존재 변경 — 이번 작업과 무관, **절대 커밋하지 않는다**). MSBuild 실경로: `C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe`. 소스 변경 후 경고는 **12줄이 baseline**(CS0618×10 + CS0162×2, 전부 이번 범위 밖 파일) — 0경고를 합격선으로 쓰지 않는다.
</context>

<tasks>

<task type="auto">
  <name>Task 1: 거리측정 버튼 추가 (XAML 버튼 1개 + enum 값 + 필드 + 메서드 3개 + ExitCanvasMode 확장)</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml, WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <action>
**이 2개 파일 외 어떤 파일도 열거나 수정하지 말 것.** 아래 (1)~(6) 전부 **순수 삽입**이다 — 유일한 예외는 (3)의 enum 선언 1줄 교체. 기존 코드 라인은 이동/삭제/재포맷 금지.

**(1) XAML — 새 버튼 삽입** (`MainView.xaml`, `btn_calibrate`의 `</Button>` 직후, `<!--260623 hbk Phase 53: 체커보드...` 주석 앞):
```xml
                    <!--260819 hbk quick-260819-n4d: 거리측정 -- 기존 Calibrate(2점+실측입력->재보정)와 별도, 이미 있는 PixelResolution 으로 즉시 mm 거리 표시(read-only).-->
                    <Button x:Name="btn_measureDistance" Content="거리측정"
                        Click="MeasureDistanceButton_Click"
                        MinWidth="80" Height="28" Padding="12,4" Margin="4,0,0,0"
                        FontSize="13" FontWeight="SemiBold"
                        Background="#7C3AED" Foreground="White"
                        BorderBrush="#6D28D9" BorderThickness="1">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
```

**(2) C# — 필드 추가** (`MainView.xaml.cs` L77 `_calibrationPoints` 선언 바로 다음 줄에 삽입):
```csharp
        private readonly List<System.Windows.Point> _measurePoints = new List<System.Windows.Point>();
```

**(3) C# — enum 값 추가** (L46, 유일한 교체 대상. 편집 전 원문 → 편집 후로 교체, 끝에 `DistanceMeasure` 추가 + 새 주석 추가. 기존 주석은 그대로 보존):
```csharp
// 편집 전:
private enum ECanvasMode { None, RectRoi, PolygonRoi, CircleRoi, TeachDatum, Calibration, PatternRoi, PatternRoi2 } //260618 hbk Phase 54 ALIGN-01 / 260619 Phase 55 ALIGN-02 (PatternRoi2, AlignLineRoi 제거)

// 편집 후:
private enum ECanvasMode { None, RectRoi, PolygonRoi, CircleRoi, TeachDatum, Calibration, PatternRoi, PatternRoi2, DistanceMeasure } //260618 hbk Phase 54 ALIGN-01 / 260619 Phase 55 ALIGN-02 (PatternRoi2, AlignLineRoi 제거) / 260819 hbk quick-260819-n4d (DistanceMeasure 추가)
```

**(4) C# — `ExitCanvasMode()` 확장** (2곳, 기존 줄 무변경, 삽입만):

4-a. 기존 unsubscribe 블록 끝(L2624 `halconViewer.RectDrawingCompleted -= HalconViewer_PatternRect2Completed;` 바로 다음, 그 다음 빈 줄 앞)에 삽입:
```csharp
            // 거리측정 핸들러 unsubscribe (quick-260819-n4d)
            halconViewer.ImageLeftClicked -= HalconViewer_MeasureMouseDown;
```

4-b. 기존 "// Calibration cleanup" 블록 끝(L2646 `_calibrationPoints.Clear();` 바로 다음, 메서드를 닫는 `}` 앞)에 삽입:
```csharp
            // Distance measure cleanup (quick-260819-n4d) -- 오버레이는 위 Calibration cleanup 의 ClearCalibrationOverlay() 가 공용 처리하므로 여기서 재호출하지 않는다
            btn_measureDistance.Content = "거리측정";
            _measurePoints.Clear();
```

**(5) C# — 새 메서드 3개 삽입** (`ApplyCalibrationResult`의 닫는 `}` 직후, `ResolveActiveSequenceForCalibration` 위 주석 앞. 그 사이에 그대로 끼워 넣는다):
```csharp
        //quick-260819-n4d: 거리측정 -- 기존 Calibrate(2점+실측입력->mm/px 재계산.저장)와 완전히 별개 도구.
        //  이미 설정되어 있는 PixelResolution(보정계수 포함 GetEffectivePixelResolution)으로 두 클릭 사이의
        //  실제 거리(mm)를 즉시 계산해 보여주기만 한다 -- 아무 것도 저장.변경하지 않는다(read-only).
        private void MeasureDistanceButton_Click(object sender, RoutedEventArgs e) {
            ExitCanvasMode();
            _canvasMode = ECanvasMode.DistanceMeasure;
            _measurePoints.Clear();

            btn_measureDistance.Content = "Pick Point 1";
            label_drawHint.Content = "캔버스에서 첫 번째 점을 클릭하세요";
            label_drawHint.Visibility = Visibility.Visible;

            halconViewer.ImageLeftClicked += HalconViewer_MeasureMouseDown;
        }

        private void HalconViewer_MeasureMouseDown(object sender, MainViewerPointerChangedEventArgs e) {
            if (_canvasMode != ECanvasMode.DistanceMeasure) return;

            var pos = new System.Windows.Point(e.X, e.Y);
            _measurePoints.Add(pos);
            halconViewer.SetCalibrationOverlay(_measurePoints);

            if (_measurePoints.Count == 1) {
                btn_measureDistance.Content = "Pick Point 2";
                label_drawHint.Content = "캔버스에서 두 번째 점을 클릭하세요";
            }
            else if (_measurePoints.Count == 2) {
                halconViewer.ImageLeftClicked -= HalconViewer_MeasureMouseDown;
                FinishDistanceMeasure();
            }
        }

        //quick-260819-n4d: 두 클릭 사이 픽셀거리 -> 선택 FAI 의 Shot.GetEffectivePixelResolution() 으로 mm 환산해
        //  label_message 에 총/가로/세로 3값으로 표시. FAI 미선택/Shot 해석 불가 시 픽셀-only 로 조용히 폴백(크래시 금지).
        //  아무 것도 저장하지 않는다 -- PixelResolution/PixelResolutionX/Y 쓰기 없음(Calibrate 와의 핵심 차이).
        private void FinishDistanceMeasure() {
            var p1 = _measurePoints[0];
            var p2 = _measurePoints[1];

            double dx = p2.X - p1.X;
            double dy = p2.Y - p1.Y;
            double pixelDistance = Math.Sqrt(dx * dx + dy * dy);

            if (pixelDistance < MinCalibrationPixelDistance) {
                CustomMessageBox.Show("두 점 사이의 거리가 너무 가깝습니다.", "거리측정");
                ExitCanvasMode();
                return;
            }

            var selectedRow = dataGrid_faiResults.SelectedItem as MeasurementResultRow;
            FAIConfig anchorFai;
            if (selectedRow != null) anchorFai = FindFAIByName(selectedRow.FAIName);
            else                     anchorFai = null;

            ShotConfig shot;
            if (anchorFai != null) shot = anchorFai.Owner as ShotConfig;
            else                   shot = null;

            if (shot == null) {
                label_message.Content = string.Format("픽셀거리: {0:F1}px (FAI 미선택 -- mm 환산 불가)", pixelDistance);
                label_message.Foreground = new SolidColorBrush(Colors.White);
                label_message.Visibility = Visibility.Visible;
            }
            else {
                double pixelResolution = shot.GetEffectivePixelResolution();
                double totalMm = pixelDistance * pixelResolution;
                double dxMm = dx * pixelResolution;
                double dyMm = dy * pixelResolution;

                label_message.Content = string.Format(
                    "거리: {0:F3}mm (가로 {1:F3}mm, 세로 {2:F3}mm)  |  픽셀거리: {3:F1}px",
                    totalMm, Math.Abs(dxMm), Math.Abs(dyMm), pixelDistance);
                label_message.Foreground = new SolidColorBrush(Colors.White);
                label_message.Visibility = Visibility.Visible;
            }

            var timer = new System.Windows.Threading.DispatcherTimer();
            timer.Interval = TimeSpan.FromSeconds(MessageDisplaySeconds);
            timer.Tick += (s, args) => {
                timer.Stop();
                label_message.Visibility = Visibility.Collapsed;
            };
            timer.Start();

            ExitCanvasMode();
        }

```
(마지막 빈 줄 포함 — 그 다음이 기존 `//260623 hbk Phase 53: 캘리브 적용 대상 활성 시퀀스 결정...` 주석이다.)

**(6) 손대지 말 것 (재확인)**
- `CalibrateButton_Click`/`HalconViewer_CalibrationMouseDown`/`FinishCalibration`/`ApplyCalibrationResult` 4개 메서드 본문 — 전부 무변경.
- `_calibrationPoints`/`btn_calibrate`/`MinCalibrationPixelDistance`/`MessageDisplaySeconds` 정의부 — 전부 무변경(값 재사용만).
- `ExitCanvasMode()` 안의 기존 줄(RectRoi/PolygonRoi/CircleRoi/TeachDatum/PatternRoi/PatternRoi2/Calibration 관련 전부) — 전부 무변경, 위 4-a/4-b 삽입 외 손대지 않는다.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && FX=WPF_Example/UI/ContentItem/MainView.xaml && FC=WPF_Example/UI/ContentItem/MainView.xaml.cs && echo "XAML_BTN=$(grep -cF 'x:Name=\"btn_measureDistance\"' "$FX") XAML_CLICK=$(grep -cF 'Click=\"MeasureDistanceButton_Click\"' "$FX") CS_ENUM=$(grep -cF 'DistanceMeasure' "$FC") CS_FIELD=$(grep -cF '_measurePoints' "$FC") CS_CLICKFN=$(grep -cF 'private void MeasureDistanceButton_Click' "$FC") CS_MOUSEDOWN=$(grep -cF 'HalconViewer_MeasureMouseDown' "$FC") CS_FINISH=$(grep -cF 'private void FinishDistanceMeasure' "$FC") CS_GETEFF=$(grep -cF 'GetEffectivePixelResolution' "$FC") CS_BTNCONTENT=$(grep -cF 'btn_measureDistance.Content' "$FC")"</automated>
  </verify>
  <done>
위 명령 출력이 정확히 `XAML_BTN=1 XAML_CLICK=1 CS_ENUM=5 CS_FIELD=9 CS_CLICKFN=1 CS_MOUSEDOWN=4 CS_FINISH=1 CS_GETEFF=3 CS_BTNCONTENT=3` 이다.

(근거: CS_ENUM=5 = enum 선언 내 `DistanceMeasure`(1) + `_canvasMode = ECanvasMode.DistanceMeasure;`(1) + `if (_canvasMode != ECanvasMode.DistanceMeasure)`(1) + `FinishDistanceMeasure();` 호출(1) + `private void FinishDistanceMeasure()` 선언(1). CS_FIELD=9 = 필드 선언(1) + Click 핸들러의 Clear(1) + MouseDown 의 Add/SetCalibrationOverlay/Count==1/Count==2(4) + FinishDistanceMeasure 의 `[0]`/`[1]`(2) + ExitCanvasMode cleanup 의 Clear(1). CS_MOUSEDOWN=4 = 구독(1)+선언(1)+2번째클릭 시 해제(1)+ExitCanvasMode 해제(1). CS_BTNCONTENT=3 = "Pick Point 1"/"Pick Point 2"/ExitCanvasMode 리셋. CS_GETEFF=3 = 주석 2곳 + 실제 호출 1곳.)
  </done>
</task>

<task type="auto">
  <name>Task 2: 변경 범위 검증(회귀 0 / 규칙 준수) + Debug x64 빌드</name>
  <files>WPF_Example/UI/ContentItem/MainView.xaml, WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <action>
코드 수정 없음. 아래 순서로 검증하고 결과를 SUMMARY 에 기록한다.

**S1. 변경 파일 범위** — `git status --porcelain -- WPF_Example` 가 정확히 3줄:
```
 M WPF_Example/DatumMeasurement.csproj              (사전 존재, 사용자 실험 — 무관, 커밋 금지)
 M WPF_Example/UI/ContentItem/MainView.xaml         (이번 작업)
 M WPF_Example/UI/ContentItem/MainView.xaml.cs      (이번 작업)
```
위 3개 외 다른 파일이 나오면 즉시 중단하고 보고한다.

**S2. 순수 삽입 확인 (핵심 회귀 가드)** — 두 파일 모두 `git diff --numstat -- <file>` 의 삭제(deletion) 컬럼을 확인한다.
- `MainView.xaml.cs`: 삭제 라인 수가 **정확히 1**이어야 한다(유일하게 교체되는 enum 선언 줄). 1보다 크면 기존 코드를 삭제/수정한 것이므로 중단하고 보고한다.
- `MainView.xaml`: 삭제 라인 수가 **정확히 0**이어야 한다(버튼 순수 삽입). 0보다 크면 중단하고 보고한다.

추가로 `git diff -U0 -- WPF_Example/UI/ContentItem/MainView.xaml.cs | grep '^-' | grep -v '^---'` 의 유일한 출력 줄이 다음 원문과 일치하는지 확인한다(공백 포함 정확히):
```
-        private enum ECanvasMode { None, RectRoi, PolygonRoi, CircleRoi, TeachDatum, Calibration, PatternRoi, PatternRoi2 } //260618 hbk Phase 54 ALIGN-01 / 260619 Phase 55 ALIGN-02 (PatternRoi2, AlignLineRoi 제거)
```
다르면(다른 줄이 삭제/변경됨) 중단하고 보고한다.

**S3. 코딩 규칙** — 추가된 줄(`git diff -U0 -- WPF_Example/UI/ContentItem/MainView.xaml.cs WPF_Example/UI/ContentItem/MainView.xaml | grep '^+' | grep -v '^+++'`)에 삼항 연산자 `?:` 가 0건인지 확인(한국어 문구에 물음표를 쓰지 않았으므로 `?` 자체가 0건이어야 한다). 새 `using` 라인 추가도 0건인지 확인.

**S4. 빌드** — 이 리포의 확립된 방식. MSBuild 프로세스 종료코드가 유일한 성공 신호(`-v:minimal -nologo` 는 "Build succeeded." 문구를 지운다). 경고 **12줄이 baseline**(0이 아님).

```bash
cd /c/Info/Project/DataMeasurement
MSB="C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"
LOG="C:/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/9d3a7b4d-2314-4b14-8686-52fd6346a1f9/scratchpad/n4d-build.log"
"$MSB" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal -nologo > "$LOG" 2>&1
rc=$?
nerr=$(grep -c ': error' "$LOG"); nwarn=$(grep -c 'warning CS' "$LOG")
echo "BUILD_RC=$rc ERRORS=$nerr WARN_CS=$nwarn"
```
합격선: `BUILD_RC=0`, `ERRORS=0`, `WARN_CS=12`.

**출력물 잠김 폴백**: 앱이 실행 중이어서 `bin/x64/Debug` 산출물이 잠기면 **프로세스를 절대 죽이지 말 것**. 대신 스크래치 OutDir 로 컴파일만 검증한다. 단일 대시(`-p:`), 경로는 슬래시(`/`)로 쓰고 **끝도 `/`로 닫는다**(백슬래시로 끝내면 Bash 큰따옴표 안에서 조기 종료된다):
```bash
"$MSB" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal -nologo -p:OutputPath="C:/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/9d3a7b4d-2314-4b14-8686-52fd6346a1f9/scratchpad/n4d-bin/" > "$LOG" 2>&1
```
폴백을 썼다면 SUMMARY.md 에 명시한다.

**S5. csproj 오염 방지** — 커밋 시(사용자가 별도로 커밋을 요청하는 경우) `git add` 는 반드시 `WPF_Example/UI/ContentItem/MainView.xaml`과 `WPF_Example/UI/ContentItem/MainView.xaml.cs` 경로 2개만 명시한다. `git add -A`/`git add -a`/`git add .` 절대 금지. `WPF_Example/DatumMeasurement.csproj` 는 어떤 경우에도 staging 하지 않는다.
  </action>
  <verify>
    <automated>cd /c/Info/Project/DataMeasurement && MSB="C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" && LOG="C:/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/9d3a7b4d-2314-4b14-8686-52fd6346a1f9/scratchpad/n4d-build.log" && "$MSB" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal -nologo > "$LOG" 2>&1; rc=$?; echo "BUILD_RC=$rc ERRORS=$(grep -c ': error' "$LOG") WARN_CS=$(grep -c 'warning CS' "$LOG") FILES=$(git status --porcelain -- WPF_Example | wc -l) CS_DEL=$(git diff --numstat -- WPF_Example/UI/ContentItem/MainView.xaml.cs | awk '{print $2}') XAML_DEL=$(git diff --numstat -- WPF_Example/UI/ContentItem/MainView.xaml | awk '{print $2}')"</automated>
  </verify>
  <done>
`BUILD_RC=0 ERRORS=0 WARN_CS=12 FILES=3 CS_DEL=1 XAML_DEL=0` 이고, S1~S3 모두 통과(추가 변경 파일 없음 / 삭제줄이 정확히 지정된 개수 / 삼항·신규 using 0건 / 삭제된 유일한 줄이 지정된 enum 원문과 일치). S5 의 staging 규칙을 SUMMARY 에 명시(실제 커밋은 사용자 요청 시에만).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 실기 확인 (거리측정 버튼 동작 + Calibrate 회귀 없음)</name>
  <what-built>
메인 화면 툴바에 기존 Calibrate 버튼 옆에 새 **거리측정** 버튼을 추가했습니다.

- 클릭 후 캔버스에서 점 2개를 순서대로 클릭하면, 이미 설정되어 있는 픽셀분해능(보정계수 포함)으로 즉시 실제 거리(mm)를 계산해 화면에 보여줍니다 — 총거리 + 가로성분 + 세로성분 + 픽셀거리 4가지를 함께 표시합니다.
- 예: `거리: 12.345mm (가로 10.120mm, 세로 7.050mm)  |  픽셀거리: 145.3px`
- FAI 를 선택하지 않은 상태여서 분해능을 알 수 없으면, mm 없이 픽셀거리만(`픽셀거리: 145.3px (FAI 미선택 -- mm 환산 불가)`) 조용히 표시됩니다 — 에러 없음.
- 아무 것도 저장하지 않습니다 — 순수 화면 표시 기능입니다.
- 기존 Calibrate 버튼(2점 클릭 후 실측 mm 값을 직접 입력해서 보정값 자체를 재계산·저장하는 기능)은 전혀 건드리지 않았습니다 — 그대로 동일하게 동작합니다.
  </what-built>
  <how-to-verify>
1. 앱을 다시 빌드/실행하고 메인 화면으로 이동합니다.
2. FAI 결과 그리드에서 아무 항목이나 하나 선택합니다(분해능이 설정된 FAI/Shot).
3. 툴바의 **거리측정** 버튼을 클릭 → "캔버스에서 첫 번째 점을 클릭하세요" 안내가 뜨는지 확인 → 캔버스에서 임의의 두 점을 순서대로 클릭합니다.
4. 화면에 `거리: N.NNNmm (가로 N.NNNmm, 세로 N.NNNmm)  |  픽셀거리: N.Npx` 형태의 문구가 몇 초간 표시되는지 확인합니다. 값이 상식적인지 확인합니다 — 총거리(첫 값)는 가로/세로 성분보다 작을 수 없습니다(피타고라스 — 총거리 ≥ max(가로,세로)).
5. FAI 결과 그리드에서 선택을 해제(또는 선택 없는 상태)한 채로 다시 **거리측정** → 두 점 클릭 시 mm 없이 픽셀거리만 나오는지 확인합니다(크래시 없음).
6. **기존 Calibrate 버튼**을 눌러 예전과 동일하게 2점 클릭 → 실제 거리(mm) 입력 다이얼로그가 뜨는지, 입력 후 "1 px = N.NNNN mm 적용됨" 토스트가 뜨는지, 저장이 정상 동작하는지 확인합니다(이번 변경으로 전혀 달라지지 않아야 합니다).
7. 거리측정 진행 중(1번째 점만 찍은 상태)에 ESC 를 누르거나 다른 툴바 버튼(예: Rect ROI)을 누르면 상태가 깔끔하게 취소되고 다른 기능과 뒤섞이지 않는지 확인합니다.

문제가 있으면 어느 단계에서 무엇이 달랐는지(예: 값이 이상함/표시가 안 뜸/Calibrate 가 달라짐) 알려주세요.
  </how-to-verify>
  <resume-signal>"approved" 라고 쓰시거나, 문제가 있으면 단계 번호와 증상을 알려주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (해당 없음) | 이번 변경은 로컬 프로세스 내부에서 이미 계산·설정된 픽셀분해능 값을 읽어 mm 로 환산해 화면에 보여주는 순수 표시 레이어 추가다. 새로운 신뢰 경계(TCP/파일/외부 API)를 넘는 입력이 없다. 사용자 입력은 마우스 클릭 좌표(캔버스 내부, 이미 신뢰된 로컬 UI 이벤트)뿐이다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-n4d-01 | Denial of Service (크래시) | `FinishDistanceMeasure` | mitigate | `anchorFai`/`shot` null 체인 전부 if-else 가드로 방어(`shot == null` 분기에서 픽셀-only 폴백). 나머지는 순수 산술(0으로 나누는 연산 없음). `MinCalibrationPixelDistance` 가드로 두 점이 너무 가까운 극단값(0 나눗셈 유발 가능성)도 사전 차단. |
| T-n4d-02 | Tampering (표시값 신뢰성) | `shot.GetEffectivePixelResolution()` | accept | 기존 실측 경로(`Action_FAIMeasurement`, `EdgePairDistanceMeasurement`)가 이미 동일 값을 소비해 왔다. 이번 작업은 그 값을 **읽기만** 하며, 쓰기 권한이 전혀 없어 새 공격면을 추가하지 않는다. |
| T-n4d-03 | Information Disclosure | 화면 표시 문구(`label_message`) | accept | 로컬 UI 라벨 표시일 뿐, 외부로 전송/저장되지 않는다(TCP 응답/INI 저장 로직 전혀 관여하지 않음). |
| T-n4d-04 | Elevation of Privilege (기존 Calibrate 오염) | `ExitCanvasMode`, `ECanvasMode` enum | mitigate | 거리측정과 Calibrate 는 서로 다른 enum 값(`DistanceMeasure` vs `Calibration`)과 서로 다른 필드(`_measurePoints` vs `_calibrationPoints`)를 쓰므로 상태가 섞이지 않는다. `ExitCanvasMode` 의 기존 Calibration cleanup 블록은 무편집 — 새 cleanup 은 별도 블록으로 순수 추가. |
</threat_model>

<verification>
1. Task 1 grep 정적 검증 9종 (`XAML_BTN=1 XAML_CLICK=1 CS_ENUM=5 CS_FIELD=9 CS_CLICKFN=1 CS_MOUSEDOWN=4 CS_FINISH=1 CS_GETEFF=3 CS_BTNCONTENT=3`) 통과
2. `git status --porcelain -- WPF_Example` 3줄 — `MainView.xaml`/`MainView.xaml.cs` 외 신규 변경 0 (`DatumMeasurement.csproj` 는 사전 존재, 무관)
3. `MainView.xaml.cs` 삭제 라인 정확히 1(enum 선언 줄만), `MainView.xaml` 삭제 라인 정확히 0 — 순수 삽입 확인
4. 삼항 연산자 0건, 신규 `using` 0건, C# 7.2 문법 준수, K&R 브레이스 스타일 준수
5. Debug/x64 빌드 `BUILD_RC=0` / `: error` 0 / `warning CS` **12**(baseline 불변)
6. 실기 확인 Task 3 의 7단계 통과 (거리측정 표시 형태 + 값 상식성 + FAI 미선택 폴백 + 기존 Calibrate 무변경 + 모드 격리)
</verification>

<success_criteria>
- `MainView.xaml` 에 `btn_measureDistance` 버튼 존재, `Click="MeasureDistanceButton_Click"` 바인딩
- `MainView.xaml.cs` 에 `ECanvasMode.DistanceMeasure` + `_measurePoints` 필드 + `MeasureDistanceButton_Click`/`HalconViewer_MeasureMouseDown`/`FinishDistanceMeasure` 3개 메서드 존재
- 두 점 클릭 후 FAI/Shot 분해능을 구할 수 있으면 총/가로/세로 mm + 픽셀거리가 `label_message` 에 표시됨
- FAI/Shot 을 못 구하면 예외 없이 픽셀거리만 표시(폴백)
- `shot.PixelResolution`/`fai.PixelResolutionX`/`fai.PixelResolutionY` 어디에도 쓰기(write) 없음 — read-only 확인
- `CalibrateButton_Click`/`HalconViewer_CalibrationMouseDown`/`FinishCalibration`/`ApplyCalibrationResult` 4개 메서드 본문 무변경 (diff 상 삭제 라인은 enum 선언 1줄뿐)
- 변경 파일 = `MainView.xaml` + `MainView.xaml.cs` 2개(+ 사전 존재 `DatumMeasurement.csproj` 는 무관 유지)
- 빌드 baseline(error 0 / warning CS 12) 불변
</success_criteria>

<output>
완료 후 `.planning/quick/260819-n4d-distance-measure-button/260819-n4d-SUMMARY.md` 를 작성한다.
포함 항목: 실제 삽입 위치(줄 번호), 정적 검증 9종 실측 출력, 빌드 실측 출력(BUILD_RC/ERRORS/WARN_CS/FILES/CS_DEL/XAML_DEL), 스크래치 OutDir 폴백 사용 여부, 실기 확인 7단계 결과(실측 mm 값 예시 1개 이상 기록 권장).
</output>
