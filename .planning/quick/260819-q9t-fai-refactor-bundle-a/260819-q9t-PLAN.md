---
phase: quick-260819-q9t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: true
requirements: [Q9T-01, Q9T-02, Q9T-03, Q9T-04]

must_haves:
  truths:
    - "[Q9T-01] Dispose try/catch 반복 — 플래너 실측 **14곳**(사용자 원 카운트 '13곳'과 유사, 실측 우선)이 전부 `SafeDisposeImage(HImage)` 신규 헬퍼 호출로 치환된다. 12곳은 `if (X != null) { try { X.Dispose(); } catch { } }` 균일 형태(정규식 1개로 일괄 치환 가능), 2곳은 예외 형태 — L829 `acc.CrossZRoleImage`(치환 후 `acc.CrossZRoleImage = null;` 대입이 같은 줄에 이어짐), L1041 `capturedImage`(바깥 `if(!=null)` 가드가 원래 없고 bare try/catch + 꼬리주석 보존 필요)."
    - "[Q9T-02] MeasurementName→TypeName 폴백 — 플래너 실측 **5곳**(사용자 원 카운트 '5곳'과 정확히 일치, 단 실제 코드 형태는 `??` 연산자가 아니라 `if (measName == null) measName = meas.TypeName;` if-else 형태였음)이 `GetMeasurementDisplayName(MeasurementBase)` 신규 헬퍼 호출로 치환된다. **모양은 비슷하지만 폴백 의미가 달라 절대 손대면 안 되는 2곳**: L752 `(meas.MeasurementName ?? szAlgoType)`(TypeName 자체가 아니라 'TypeName 이 비었으면 \"?\"' 까지 처리된 szAlgoType 로 대체하는 다른 폴백 체인), L1548-1551 `BuildCrossZMeasurementKey` 내부의 `string.IsNullOrEmpty(measName)` 검사(null 뿐 아니라 빈 문자열도 대체 — `??` 로 바꾸면 빈 문자열 케이스의 동작이 달라짐)."
    - "[Q9T-03] null→\"\" 방어 스타일 통일 — 플래너 실측 **15곳**(사용자 원 카운트 '15곳'과 정확히 일치) `if (X == null) X = \"\";` 형태가 `X = X ?? \"\";` 로 통일된다(정규식 1개, 변수명 backreference 로 일괄 치환). 기존에 이미 `?? \"\"` 를 쓰던 **6곳**(사용자 원 카운트 '7곳'과 유사; L428/852/856/889/912/1219)은 무변경. **모양은 비슷하지만 다른 이유로 제외되는 5곳**: L888/911/1062/1543(애초에 null 검사가 없는 무조건 초기화문), L1710(`else ownerSeqName = \"\";` — ownerSeqName 자신이 아니라 ShotParam 의 null 을 검사하는 다른 패턴)."
    - "[Q9T-04] pCamera 필드 — 프로젝트 전체 grep 확인 결과 이 파일의 pCamera 는 선언(L70)·대입(L89) 단 2곳뿐이고 읽는 곳은 0곳. 대입 우변 `SystemHandler.Handle.Devices[ShotParam.DeviceName]`(DeviceHandler.cs L263-269 인덱서 — `if (name==null) return null; if (!ContainsKey) return null; return Devices[name];`) 은 예외/로그 없는 순수 Dictionary 조회라 부수효과가 없음을 확인했다 — 필드 선언 + OnLoad 안 대입문 전체(가드 if 포함)를 삭제해도 동작 변화가 없다. 다른 클래스(Action_TopSideInspection 등)의 동명 `pCamera` 필드는 별개 클래스 소유라 무관하다."
    - "검증 그렙의 자기참조 오염 방지(사용자 명시 요구) — Task1 종료 시 `SafeDisposeImage(` 리터럴 카운트는 **15**(헬퍼 선언 1 + 호출 14, 0이 아님), `.Dispose(); } catch { }` 리터럴 카운트는 **1**(헬퍼 본문 자체만 남음, 0이 아님)로 명시 기대한다. Task2 종료 시 `GetMeasurementDisplayName(` 카운트는 **6**(선언 1 + 호출 5). Task1 종료 후 '가드형 인라인' 검증은 L523 `capSaver`(SharedHImage 생성, Dispose 와 무관한 기존 코드)를 잡아 **1**이 정상이다 — 0 이 아니다."
    - "제어흐름·시그니처 무변경 — 이 파일의 public/internal 메서드 시그니처 변경 0건, 판정 로직·분기·실행 순서 변경 0건. 신규 코드에 삼항 `?:` 0건(if-else 또는 `??` 만 사용, `??` 는 이 파일에 이미 6곳 있는 기존 관용구)."
    - "Task 별 `git diff --numstat` 라인 수가 정확히 일치한다(플래너가 스크래치에 실제 git 저장소를 만들어 3커밋을 그대로 재현·실측 확인함 — 손계산이나 추정이 아님): Task1 추가 **20** / 삭제 **14**. Task2 추가 **10** / 삭제 **14**. Task3 추가 **15** / 삭제 **15**. 누적 추가 **45** / 삭제 **43**(순증가 +2줄, 1742→1744). ⚠ `git diff -U0 | grep -c '^+[^+]'` 류의 패턴은 순수 빈 줄 추가/삭제를 세지 못해 오답을 낸다(빈 줄의 diff 표시는 `+` 한 글자뿐이라 `[^+]`가 매치할 문자가 없음) — 반드시 `--numstat` 사용."
    - "빌드 PASS — 매 Task 마다 error CS 0건, warning CS 정확히 **12**건(baseline, CS0618×10+CS0162×2) 유지. 신규 CS0219/CS0168/CS0103 0건."
    - "3개 Task 모두 `Action_FAIMeasurement.cs` 단 1개 파일만 커밋, `WPF_Example/DatumMeasurement.csproj`(로컬 미커밋 오염, Debug OutputPath=D:\\Data\\)는 매 커밋 후에도 `git status`에 unstaged `M` 으로 남는다."
    - "파일 인코딩 손상 0건 — UTF-8 BOM(`ef bb bf`) 유지 + LF 개행 유지(CRLF 유입 0건, `grep -c $'\\r'`). 이 세션에서 과거 bash/python heredoc 이 한글을 깨뜨린 전례가 있어(플래너도 python 검증 중 `newline=''` 누락으로 CRLF 오염을 1회 재현·확인함 — Edit 도구는 이 문제가 없지만 매 Task 후 재확인), 각 Task 편집 직후 BOM/LF/한글 문자열이 그대로인지 확인한다."
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "SafeDisposeImage / GetMeasurementDisplayName 헬퍼 2개 신설, Dispose 반복 14곳·MeasurementName 폴백 5곳 헬퍼 통합, null→\"\" 스타일 15곳 통일, 미사용 pCamera 필드 제거"
      contains: "private static void SafeDisposeImage(HImage image) {"
  key_links:
    - from: "Dispose try/catch 14개 호출부"
      to: "SafeDisposeImage"
      via: "직접 호출(널 가드는 헬퍼 내부에 내장)"
      pattern: "SafeDisposeImage\\("
    - from: "MeasurementName 폴백 5개 호출부"
      to: "GetMeasurementDisplayName"
      via: "직접 호출"
      pattern: "GetMeasurementDisplayName\\(meas\\)"
---

<objective>
`Action_FAIMeasurement.cs`(오늘 4차례 리팩토링 완료 — fik/gf1/hyk/j6j, 전부 "동작 무변경" 검증됨) 사용자 백로그 "우선순위 2" 중
**가장 리스크가 낮은 4개 항목**(Bundle A — 순수 기계적 추출/중복제거, 제어흐름·시그니처 변경 0건)을 처리한다.

1. Dispose try/catch 반복 14곳 → `SafeDisposeImage(HImage)` 헬퍼
2. MeasurementName→TypeName 폴백 5곳 → `GetMeasurementDisplayName(MeasurementBase)` 헬퍼
3. null→"" 방어 15곳 → 기존 관용구 `X = X ?? "";` 로 통일
4. 미사용 필드 `pCamera` 제거(읽는 곳 0곳 확인됨)

Purpose: 오늘 반복된 요구 그대로 — 동작은 단 하나도 바뀌지 않으면서 반복 코드를 줄인다.
Output: 파일 1개 수정(새 파일 0개), 헬퍼 2개 신설, 커밋 3개(Task 당 1개).

⚠ **라인번호는 전부 "계획 시점 실측"이다.** 이전 Task 의 편집으로 뒤 라인번호가 밀리므로, 각 Task 는
줄번호가 아니라 본문에 제공된 정확한 원문 텍스트(grep/Edit old_string)로 위치를 다시 찾는다.

⚠ **모든 sed 정규식과 예상 diff 라인 수는 플래너가 스크래치에 실제 git 저장소를 만들어 3커밋을 전부
재현해 실측 검증한 값이다**(추정이 아님). 그대로 실행하면 그대로 재현된다. 검증 중 python 스크립트로
헬퍼 삽입을 흉내낼 때 `newline=''` 를 빠뜨려 CRLF 오염이 실제로 재현되는 것도 확인했다 — **Edit 도구는
이 문제가 없지만**, 혹시 모를 사고에 대비해 각 Task 검증에 LF 유지 확인을 포함시켰다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md

### 착수 시점 고정값 (플래너 실측)

| 항목 | 값 |
|---|---|
| HEAD | **`4a15a12`** (`git rev-parse --short HEAD`) |
| 워킹트리 | ` M WPF_Example/DatumMeasurement.csproj` **1건뿐**(커밋 금지 로컬 설정) |
| 대상 파일 | `WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` — **1742줄**, UTF-8 **BOM 있음**, LF(개행 `\n`, CRLF 아님) |
| 클래스 | `public class Action_FAIMeasurement : ActionBase`(partial 아님 — 다른 파일에 이어지는 부분 없음) |
| 사용자 원 카운트 vs 실측 | Dispose "13곳"→**14곳** / MeasurementName "5곳"→**5곳**(일치, 형태만 다름) / null→"" "15곳"→**15곳**(일치) / 기존 `?? ""` "7곳"→**6곳** |
| 예상 최종 라인수 | **1744**(1742+2, Task별 numstat 순증감 +6/-4/+0 누적) |

### 4개 항목 실측 좌표 (원본 텍스트 기준 — Edit old_string 으로 그대로 사용 가능)

**[Q9T-01] Dispose 14곳** — 균일 형태(12곳, sed 정규식 1개로 일괄):
`if (X != null) { try { X.Dispose(); } catch { } }` — X ∈ {imgH, imgV, imageHorizontal×2, imageHorizontal, imageVertical×2, imageA×2, imageB×2, imgA×2, imgB×2} (원본 L315/316/988/989/1100/1101/1169/1170/1521/1522/1662/1663)
예외 2곳: L829 `acc.CrossZRoleImage`(뒤에 `acc.CrossZRoleImage = null;` 동반), L1041 `capturedImage`(가드 없는 bare try/catch + 꼬리주석 `// Store 가 CopyImage 로 소유 클론 저장 — 원본은 여기서 즉시 해제`)

**[Q9T-02] MeasurementName 5곳** — 전부 `RecordMeasurementResult`/`MarkMeasurementDatumSkipped`/`MarkMeasurementDatumRefMissing`/`MarkMeasurementZIndexMisconfigured`/`MarkMeasurementCrossZIncomplete` 5개 메서드 안, 각각 `string measName = meas.MeasurementName;` + `if (measName == null) measName = meas.TypeName;` 2줄.

**[Q9T-03] null→"" 15곳** — 변수명만 다른 동일 형태 `if (X == null) X = "";`: misName×1(L260), datumName×4(L277/306/325/356), dn×2(L295/340), ae×2(L297/342), derrStr×2(L308/358), measErrorStr×1(L719), datumRef×2(L1330/1343), shotName×1(L1738).

**[Q9T-04] pCamera** — 필드 `private VirtualCamera pCamera;`(L70), 대입 `if (ShotParam != null) { pCamera = SystemHandler.Handle.Devices[ShotParam.DeviceName]; }`(OnLoad 안, L88-90). 읽는 곳 0곳(파일 전체 + 프로젝트 전체 grep 확인).

### 신규 헬퍼 2개 — 확정 시그니처 (Edit anchor 로 그대로 사용)

```csharp
        private static void SafeDisposeImage(HImage image) {
            if (image == null) return;
            try { image.Dispose(); } catch { }
        }
```
```csharp
        private static string GetMeasurementDisplayName(MeasurementBase meas) {
            return meas.MeasurementName ?? meas.TypeName;
        }
```
배치 위치: `LogSeqAlgo` 닫는 `}` 바로 다음(파일 상단 공용 헬퍼 클러스터 — `LogSeqStep`/`LogSeqAlgo` 와 같은 자리). `SafeDisposeImage` 가 먼저, `GetMeasurementDisplayName` 이 그 다음.
</context>

<tasks>

<task type="auto">
  <name>Task 1: SafeDisposeImage 헬퍼 신설 + Dispose try/catch 14곳 치환 [Q9T-01]</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <action>
### 0. 착수 전 재확인 (라인이 계획 시점과 다를 수 있음 — grep 으로 재확인, 카운트가 크게 다르면 진행 전 보고)
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
grep -cE 'if \([A-Za-z][A-Za-z0-9]*(\.[A-Za-z][A-Za-z0-9]*)? != null\) \{ try \{ .*\.Dispose\(\); \} catch \{ \}' "$F"   # 기대 13 (12 단순형 + L829)
grep -oF '.Dispose(); } catch { }' "$F" | wc -l   # 기대 14
grep -c 'capturedImage.Dispose' "$F"              # 기대 1 (L1041)
```

### 1. 헬퍼 신설 (Edit 도구, old_string 은 파일에 그대로 존재하는 텍스트)
old_string:
```
            catch { }
        }

        // 이 메서드는 프로그램이 켜져 있는 동안 아주 짧은 간격으로 계속 호출됩니다. 호출될 때마다
```
new_string:
```
            catch { }
        }

        //260819 hbk quick-260819-q9t: Dispose try/catch 반복 제거 — null 이면 그냥 반환, catch 는 기존과 동일하게 예외를 삼킨다.
        private static void SafeDisposeImage(HImage image) {
            if (image == null) return;
            try { image.Dispose(); } catch { }
        }

        // 이 메서드는 프로그램이 켜져 있는 동안 아주 짧은 간격으로 계속 호출됩니다. 호출될 때마다
```

### 2. 균일형 12곳 일괄 치환 (Bash, sed — 변수명을 backreference `\2` 로 강제해 오매치 불가능)
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
sed -i -E 's/^([[:space:]]*)if \(([A-Za-z][A-Za-z0-9]*) != null\) \{ try \{ \2\.Dispose\(\); \} catch \{ \} \}$/\1SafeDisposeImage(\2);/' "$F"
```

### 3. 예외 2곳 개별 치환 (Bash, sed — 각각 유일한 식별자라 안전)
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
# L829: acc.CrossZRoleImage — Dispose 뒤 null 대입은 그대로 보존
sed -i -E 's/^([[:space:]]*)if \(acc\.CrossZRoleImage != null\) \{ try \{ acc\.CrossZRoleImage\.Dispose\(\); \} catch \{ \} acc\.CrossZRoleImage = null; \}$/\1SafeDisposeImage(acc.CrossZRoleImage); acc.CrossZRoleImage = null;/' "$F"
# L1041: capturedImage — 가드 없는 bare try/catch, 꼬리주석은 그룹 캡처로 원문 그대로 보존
sed -i -E 's/^([[:space:]]*)try \{ capturedImage\.Dispose\(\); \} catch \{ \}(.*)$/\1SafeDisposeImage(capturedImage);\2/' "$F"
```

### 4. 커밋 (대상 파일 1개만 경로 지정 스테이징)
```bash
cd /c/Info/Project/DataMeasurement
git add WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
git diff --cached --name-only   # 반드시 1줄만 출력되는지 확인 후 커밋
git commit -m "refactor(260819-q9t): Dispose try/catch 반복 14곳 -> SafeDisposeImage 헬퍼로 통합"
```
  </action>
  <verify>
    <automated>
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs

echo "== 카운트(자기참조 오염 주의 — 헬퍼 자신의 정의도 함께 잡힌다) ==" && \
[ "$(grep -oF 'SafeDisposeImage(' "$F" | wc -l)" = "15" ] && echo "  OK SafeDisposeImage( = 15 (선언1+호출14)" && \
[ "$(grep -oF '.Dispose(); } catch { }' "$F" | wc -l)" = "1" ] && echo "  OK 잔존 bare dispose+catch = 1 (헬퍼 본문 자신만)" && \
[ "$(grep -cE 'if \([A-Za-z][A-Za-z0-9]*(\.[A-Za-z][A-Za-z0-9]*)? != null\) \{ try \{' "$F")" = "1" ] && echo "  OK 가드형 인라인 패턴 잔존 1 (L523 capSaver/SharedHImage — Dispose 와 무관한 기존 코드, 정상)" && \

echo "== diff 라인 수 (git --numstat, 정확히 추가20/삭제14 — 빈줄 있는 diff 는 grep -c '^+[^+]' 로 잘못 셀 수 있어 numstat 사용) ==" && \
NUMSTAT=$(git diff --numstat HEAD~1 HEAD -- "$F") && \
[ "$(echo "$NUMSTAT" | cut -f1)" = "20" ] && [ "$(echo "$NUMSTAT" | cut -f2)" = "14" ] && echo "  OK numstat add=20/del=14" && \

echo "== 인코딩/한글 보존 ==" && \
[ "$(head -c 3 "$F" | xxd -p)" = "efbbbf" ] && echo "  OK UTF-8 BOM 유지" && \
[ "$(grep -c $'\r' "$F")" = "0" ] && echo "  OK LF 유지(CRLF 오염 없음)" && \
[ "$(grep -cF 'Store 가 CopyImage 로 소유 클론 저장' "$F")" = "1" ] && echo "  OK L1041 꼬리주석 한글 보존" && \

echo "== 위생 ==" && \
[ "$(git show --name-only --format='' HEAD | grep -c .)" = "1" ] && \
[ "$(git status --porcelain)" = " M WPF_Example/DatumMeasurement.csproj" ] && echo "  OK 파일1개, csproj unstaged"
```
    </automated>
    <automated>
```bash
cd /c/Info/Project/DataMeasurement
SCR="C:/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/9d3a7b4d-2314-4b14-8686-52fd6346a1f9/scratchpad"
MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" WPF_Example/DatumMeasurement.csproj -t:Build -p:Configuration=Debug -p:Platform=x64 -p:OutputPath="$SCR\\q9t-t1\\" -v:minimal -nologo > "$SCR/q9t-t1-build.log" 2>&1
[ "$(grep -c ': error ' "$SCR/q9t-t1-build.log")" = "0" ] && [ "$(grep -c ': warning CS' "$SCR/q9t-t1-build.log")" = "12" ] && echo "BUILD PASS (error0/warning12)"
```
    </automated>
  </verify>
  <done>SafeDisposeImage 신설, Dispose try/catch 14곳(12균일+2예외) 전부 헬퍼 호출로 치환. numstat 추가20/삭제14 정확 일치. BOM/LF/한글 보존. 빌드 error0/warning12. 파일 1개만 커밋.</done>
</task>

<task type="auto">
  <name>Task 2: GetMeasurementDisplayName 헬퍼 신설 + MeasurementName 폴백 5곳 치환 + pCamera 제거 [Q9T-02, Q9T-04]</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <action>
### 0. 착수 전 재확인
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
grep -cF 'if (measName == null) measName = meas.TypeName;' "$F"   # 기대 5
grep -c 'pCamera' "$F"                                             # 기대 2 (선언+대입)
grep -cF 'meas.MeasurementName ?? szAlgoType' "$F"                 # 기대 1 (건드리면 안 됨, 확인용)
grep -cF 'string.IsNullOrEmpty(measName)' "$F"                     # 기대 1 (건드리면 안 됨, 확인용)
```

### 1. 헬퍼 신설 (Edit 도구 — Task1 에서 만든 SafeDisposeImage 바로 다음에 삽입)
old_string:
```
        private static void SafeDisposeImage(HImage image) {
            if (image == null) return;
            try { image.Dispose(); } catch { }
        }

        // 이 메서드는 프로그램이 켜져 있는 동안 아주 짧은 간격으로 계속 호출됩니다. 호출될 때마다
```
new_string:
```
        private static void SafeDisposeImage(HImage image) {
            if (image == null) return;
            try { image.Dispose(); } catch { }
        }

        //260819 hbk quick-260819-q9t: 측정 이름 미교시(null)면 TypeName 으로 대체 — 5곳 중복 통합.
        private static string GetMeasurementDisplayName(MeasurementBase meas) {
            return meas.MeasurementName ?? meas.TypeName;
        }

        // 이 메서드는 프로그램이 켜져 있는 동안 아주 짧은 간격으로 계속 호출됩니다. 호출될 때마다
```

### 2. 호출부 5곳 치환 (Edit 도구 — 각 old_string 에 바로 앞 고유 문맥 1줄을 포함시켜 유일성 보장)

**2-1 (RecordMeasurementResult)**
old: `            } else {\n                string measName = meas.MeasurementName;\n                if (measName == null) measName = meas.TypeName;`
new: `            } else {\n                string measName = GetMeasurementDisplayName(meas);`

**2-2 (MarkMeasurementDatumSkipped)**
old: `            meas.LastJudgement = false; // skip 도 NG 강도\n            string measName = meas.MeasurementName;\n            if (measName == null) measName = meas.TypeName;`
new: `            meas.LastJudgement = false; // skip 도 NG 강도\n            string measName = GetMeasurementDisplayName(meas);`

**2-3 (MarkMeasurementDatumRefMissing)**
old: `            meas.LastJudgement = false; // skip 도 NG 강도(기존 규약 동일)\n            string measName = meas.MeasurementName;\n            if (measName == null) measName = meas.TypeName;`
new: `            meas.LastJudgement = false; // skip 도 NG 강도(기존 규약 동일)\n            string measName = GetMeasurementDisplayName(meas);`

**2-4 (MarkMeasurementZIndexMisconfigured)**
old: `            meas.LastSkipReason = SkipReason.ZINDEX_MISCONFIGURED;\n            meas.LastJudgement = false;\n            string measName = meas.MeasurementName;\n            if (measName == null) measName = meas.TypeName;`
new: `            meas.LastSkipReason = SkipReason.ZINDEX_MISCONFIGURED;\n            meas.LastJudgement = false;\n            string measName = GetMeasurementDisplayName(meas);`

**2-5 (MarkMeasurementCrossZIncomplete)**
old: `            meas.LastSkipReason = SkipReason.CROSS_Z_INCOMPLETE;\n            meas.LastJudgement = false;\n            string measName = meas.MeasurementName;\n            if (measName == null) measName = meas.TypeName;`
new: `            meas.LastSkipReason = SkipReason.CROSS_Z_INCOMPLETE;\n            meas.LastJudgement = false;\n            string measName = GetMeasurementDisplayName(meas);`

(줄바꿈 `\n` 표기는 편의상 표기이며, 실제로는 각 줄을 그대로 개행해 Edit old_string/new_string 인자로 사용할 것 — 위 "실측 좌표" 섹션의 원문 들여쓰기 그대로.)

### 3. pCamera 제거 (Edit 도구, 2회)

**3-1 필드 선언**
old:
```
        private FAIMeasurementContext pMyContext;
        private VirtualCamera pCamera;
```
new:
```
        private FAIMeasurementContext pMyContext;
```

**3-2 OnLoad 대입부**
old:
```
        public override void OnLoad() {
            if (ShotParam != null) {
                pCamera = SystemHandler.Handle.Devices[ShotParam.DeviceName];
            }
            base.OnLoad();
        }
```
new:
```
        public override void OnLoad() {
            base.OnLoad();
        }
```

### 4. 커밋
```bash
cd /c/Info/Project/DataMeasurement
git add WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
git diff --cached --name-only
git commit -m "refactor(260819-q9t): MeasurementName 폴백 5곳 헬퍼화 + 미사용 pCamera 필드 제거"
```
  </action>
  <verify>
    <automated>
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs

echo "== 카운트(자기참조 오염 주의) ==" && \
[ "$(grep -oF 'GetMeasurementDisplayName(' "$F" | wc -l)" = "6" ] && echo "  OK GetMeasurementDisplayName( = 6 (선언1+호출5)" && \
[ "$(grep -cF 'if (measName == null) measName = meas.TypeName;' "$F")" = "0" ] && echo "  OK 구형 폴백 잔존 0" && \
[ "$(grep -c 'pCamera' "$F")" = "0" ] && echo "  OK pCamera 완전 제거" && \

echo "== 제외 대상 2곳 무변경 확인 ==" && \
[ "$(grep -cF 'meas.MeasurementName ?? szAlgoType' "$F")" = "1" ] && echo "  OK L752 무변경(다른 폴백 체인)" && \
[ "$(grep -cF 'string.IsNullOrEmpty(measName)' "$F")" = "1" ] && echo "  OK BuildCrossZMeasurementKey 무변경(IsNullOrEmpty 의미차)" && \

echo "== diff 라인 수 (git --numstat, 정확히 추가10/삭제14) ==" && \
NUMSTAT=$(git diff --numstat HEAD~1 HEAD -- "$F") && \
[ "$(echo "$NUMSTAT" | cut -f1)" = "10" ] && [ "$(echo "$NUMSTAT" | cut -f2)" = "14" ] && echo "  OK numstat add=10/del=14" && \

echo "== 인코딩 보존 + 위생 ==" && \
[ "$(head -c 3 "$F" | xxd -p)" = "efbbbf" ] && echo "  OK BOM 유지" && \
[ "$(grep -c $'\r' "$F")" = "0" ] && echo "  OK LF 유지(CRLF 오염 없음)" && \
[ "$(git show --name-only --format='' HEAD | grep -c .)" = "1" ] && \
[ "$(git status --porcelain)" = " M WPF_Example/DatumMeasurement.csproj" ] && echo "  OK 파일1개, csproj unstaged"
```
    </automated>
    <automated>
```bash
cd /c/Info/Project/DataMeasurement
SCR="C:/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/9d3a7b4d-2314-4b14-8686-52fd6346a1f9/scratchpad"
MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" WPF_Example/DatumMeasurement.csproj -t:Build -p:Configuration=Debug -p:Platform=x64 -p:OutputPath="$SCR\\q9t-t2\\" -v:minimal -nologo > "$SCR/q9t-t2-build.log" 2>&1
[ "$(grep -c ': error ' "$SCR/q9t-t2-build.log")" = "0" ] && [ "$(grep -c ': warning CS' "$SCR/q9t-t2-build.log")" = "12" ] && echo "BUILD PASS (error0/warning12)"
```
    </automated>
  </verify>
  <done>GetMeasurementDisplayName 신설, MeasurementName 폴백 5곳 치환(제외 2곳 무변경 확인), pCamera 필드/대입 완전 제거. numstat 추가10/삭제14 정확 일치. 빌드 error0/warning12. 파일 1개만 커밋.</done>
</task>

<task type="auto">
  <name>Task 3: null→"" 방어 15곳을 기존 `?? ""` 스타일로 통일 [Q9T-03]</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <action>
### 0. 착수 전 재확인
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
grep -cE '^[[:space:]]*if \(([A-Za-z][A-Za-z0-9]*) == null\) \1 = "";$' "$F"   # 기대 15
grep -oF '?? ""' "$F" | wc -l                                                   # 기대 6(기존 이미 사용중)
```

### 1. 15곳 일괄 치환 (Bash, sed — 변수명을 backreference `\2` 로 강제해 오매치 불가능. 제외대상 5곳은
    애초에 `if (X == null) X = "";` 형태가 아니라서 이 정규식에 매치되지 않는다 — 별도 처리 불필요)
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
sed -i -E 's/^([[:space:]]*)if \(([A-Za-z][A-Za-z0-9]*) == null\) \2 = "";$/\1\2 = \2 ?? "";/' "$F"
```

### 2. 커밋
```bash
cd /c/Info/Project/DataMeasurement
git add WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
git diff --cached --name-only
git commit -m "refactor(260819-q9t): null->\"\" 방어 15곳을 기존 ?? \"\" 스타일로 통일"
```
  </action>
  <verify>
    <automated>
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs

echo "== 카운트 ==" && \
[ "$(grep -cE '^[[:space:]]*if \(([A-Za-z][A-Za-z0-9]*) == null\) \1 = "";$' "$F")" = "0" ] && echo "  OK 구형 패턴 잔존 0" && \
[ "$(grep -oF '?? ""' "$F" | wc -l)" = "21" ] && echo "  OK ?? \"\" 카운트 = 21 (기존6+신규15)" && \

echo "== 제외 5곳 무변경 확인 ==" && \
[ "$(grep -cF 'string dNameForLog = "";' "$F")" = "1" ] && \
[ "$(grep -cF 'string dName = "";' "$F")" = "1" ] && \
[ "$(grep -cF 'string datumName = "";' "$F")" = "1" ] && \
[ "$(grep -cF 'string shotName = "";' "$F")" = "1" ] && \
[ "$(grep -cF 'else ownerSeqName = "";' "$F")" = "1" ] && echo "  OK 제외 5곳 전부 원문 유지" && \

echo "== diff 라인 수 (git --numstat, 정확히 추가15/삭제15) ==" && \
NUMSTAT=$(git diff --numstat HEAD~1 HEAD -- "$F") && \
[ "$(echo "$NUMSTAT" | cut -f1)" = "15" ] && [ "$(echo "$NUMSTAT" | cut -f2)" = "15" ] && echo "  OK numstat add=15/del=15" && \

echo "== 인코딩 보존 + 위생 ==" && \
[ "$(head -c 3 "$F" | xxd -p)" = "efbbbf" ] && echo "  OK BOM 유지" && \
[ "$(grep -c $'\r' "$F")" = "0" ] && echo "  OK LF 유지(CRLF 오염 없음)" && \
[ "$(git show --name-only --format='' HEAD | grep -c .)" = "1" ] && \
[ "$(git status --porcelain)" = " M WPF_Example/DatumMeasurement.csproj" ] && echo "  OK 파일1개, csproj unstaged"
```
    </automated>
    <automated>
```bash
cd /c/Info/Project/DataMeasurement
SCR="C:/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/9d3a7b4d-2314-4b14-8686-52fd6346a1f9/scratchpad"
MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 -p:OutputPath="$SCR\\q9t-t3\\" -v:minimal -nologo > "$SCR/q9t-t3-build.log" 2>&1
[ "$(grep -c ': error ' "$SCR/q9t-t3-build.log")" = "0" ] && [ "$(grep -c ': warning CS' "$SCR/q9t-t3-build.log")" = "12" ] && echo "BUILD PASS (error0/warning12, 최종 clean Rebuild)"
```
    </automated>
    <automated>
```bash
# 최종 종합 확인 (3커밋 누적)
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
BASE=4a15a12
echo "== 누적 diff 라인 수 (git --numstat, 추가45/삭제43, 순증가+2줄) ==" && \
NUMSTAT_ALL=$(git diff --numstat $BASE HEAD -- "$F") && \
[ "$(echo "$NUMSTAT_ALL" | cut -f1)" = "45" ] && [ "$(echo "$NUMSTAT_ALL" | cut -f2)" = "43" ] && echo "  OK 누적 numstat add=45/del=43" && \
[ "$(wc -l < "$F" | tr -d ' ')" = "1744" ] && echo "  OK 최종 라인수 1744(1742+2)" && \
echo "== BASE..HEAD 누적 변경파일 = 대상 1개뿐 ==" && \
[ "$(git diff --name-only $BASE HEAD)" = "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs" ] && echo "  OK 누적 변경파일 1개" && \
[ "$(git status --porcelain)" = " M WPF_Example/DatumMeasurement.csproj" ] && echo "  OK csproj 최종까지 unstaged"
```
    </automated>
  </verify>
  <done>null→"" 15곳 전부 `?? ""` 스타일로 통일, 기존 6곳/제외 5곳 무변경 확인. numstat 추가15/삭제15 정확 일치. 최종 clean Rebuild PASS(error0/warning12). 3커밋 누적 numstat 추가45/삭제43, 최종 1744줄, 변경파일 대상 1개뿐, csproj unstaged 유지.</done>
</task>

</tasks>

<verification>

### 실패 시 대응
- **sed 치환 후 카운트 불일치** → 파일을 그 Task 시작 커밋으로 되돌린다(`git checkout -- <file>` 은 이전 커밋 이후 미스테이징 변경에만, 이미 커밋했다면 `git revert` 상의). **정규식이나 기대값을 몰래 완화하지 않는다** — 불일치 원인을 grep -n 으로 출력해 보고.
- **Edit old_string 매치 실패** → 원문이 계획 시점과 달라졌다는 뜻. 해당 위치를 grep 으로 재탐색해 실제 원문으로 old_string 을 다시 구성(내용 자체는 절대 변형하지 말 것).
- **BOM/LF 손상 감지** → 즉시 중단하고 `git diff`로 손상 범위 확인 후 보고(자동 복구 시도 금지 — 이 세션에서 이미 1회 사고 전례).
- **빌드 산출물 잠김** → OutputPath 이름만 바꿔 재시도. **프로세스 종료 금지.**

### 런타임 UAT
정적 검증(카운트+numstat+빌드)만으로 회귀 0 을 주장한다 — 순수 텍스트 치환/추출이라 판정 로직 접근 없음. 실기 확인이 필요하면 Shot 1개 검사 후 [SEQ]/[ALGO] 로그의 측정 개수·판정이 이번 작업 이전과 동일한지 확인.

</verification>

<success_criteria>
- `SafeDisposeImage(HImage)` / `GetMeasurementDisplayName(MeasurementBase)` 헬퍼 2개 신설, 둘 다 `private static`
- Dispose try/catch 14곳, MeasurementName 폴백 5곳이 각각 헬퍼 호출로 치환 — 제외 대상(L752, BuildCrossZMeasurementKey) 무변경
- null→"" 방어 15곳이 `X = X ?? "";` 로 통일 — 기존 6곳/제외 5곳 무변경
- 미사용 필드 `pCamera` 완전 제거(선언+대입, 읽는 곳 원래 0곳)
- 3개 Task 모두 `git diff --numstat` 라인 수가 사전 실측값과 정확히 일치, 매 Task 빌드 error0/warning12
- `Action_FAIMeasurement.cs` 단 1개 파일만 3커밋에 걸쳐 변경, `DatumMeasurement.csproj` 는 끝까지 unstaged
- UTF-8 BOM 유지 + LF 개행 유지(CRLF 오염 0건) + 한글 주석/문자열 손상 0건
- 신규 코드 삼항 `?:` 0건, public/internal 시그니처 변경 0건
</success_criteria>

<output>
완료 후 `.planning/quick/260819-q9t-fai-refactor-bundle-a/260819-q9t-SUMMARY.md` 작성(Edit/Write 도구 사용 — heredoc 금지, 한글 인코딩 보존).
</output>
