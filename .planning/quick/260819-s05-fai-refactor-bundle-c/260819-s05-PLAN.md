---
phase: quick-260819-s05
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
autonomous: true
requirements: [S05-01, S05-02]

must_haves:
  truths:
    - "[S05-01] ProcessDatumDualImage(L266-330, 65줄, 외부 호출부 정확히 1곳: L259 `ProcessDatumDualImage(datum, parentSeq);`)의 `IsPatternAlignEnabled` if/else 분기(TryGrabOrLoadDualDatumImages 성공 이후, try 블록 안쪽 마지막 문장)가 신규 `RunDatumDualImageDetection(DatumConfig datum, InspectionSequence parentSeq, HImage imgH, HImage imgV)` 헬퍼로 추출된다. 이 파일의 기존 `TryExecuteMeasurement`/`TryExecuteCrossZMeasurement`(L1520/L1650, 오케스트레이터가 같은 모양의 헬퍼를 호출)와 동일한 '오케스트레이터→헬퍼 위임' 패턴 — 단, 이 헬퍼는 반환값이 필요 없으므로 `void`(Try 접두사 아님, bool 반환 없음)."
    - "[S05-02] ProcessDatumSingleImage(L333-384, 52줄, 외부 호출부 정확히 1곳: L261 `ProcessDatumSingleImage(datum, parentSeq, ref nDatumOk, ref nDatumFail);`)의 `IsPatternAlignEnabled` if/else 분기(try 블록 전체 본문)가 신규 `RunDatumSingleImageDetection(DatumConfig datum, InspectionSequence parentSeq, HImage img, ref int nDatumOk, ref int nDatumFail)` 헬퍼로 추출된다. `ref nDatumOk`/`ref nDatumFail` 유지 필수 — 이 분기 안에서 `nDatumOk++`/`nDatumFail++` 가 실제로 4곳(align 성공/실패, detect 성공/실패) 발생하므로 `out` 으로 바꾸면 안 됨."
    - "try/finally(이미지 Dispose) 래퍼는 두 원본 함수 모두에 그대로 남는다 — 헬퍼로 옮기지 않는다. Task1: `SafeDisposeImage(imgH)`/`SafeDisposeImage(imgV)` 는 ProcessDatumDualImage 의 finally 그대로. Task2: `img.Dispose()` 는 ProcessDatumSingleImage 의 finally 그대로. 헬퍼 호출은 각 원본 함수의 try 블록 안에서 발생(try 진입 전/finally 이후 아님)."
    - "순수 이동 — 추출된 블록의 코드는 헬퍼 내부에서 byte-identical(재들여쓰기만 허용, 텍스트/연산자/변수명/주석 문자 하나도 변경 없음). 제어흐름(분기/반환/예외) 변경 0건, 판정 로직 변경 0건."
    - "ProcessDatumDualImage/ProcessDatumSingleImage 둘 다 시그니처(파라미터/반환타입/접근제한자) 변경 0건 — 외부 호출부(L259, L261) 그대로 컴파일. 둘 다 여전히 정확히 1개의 외부 호출부만 가진다(신규 헬퍼 추출로 늘어나지 않음 — 헬퍼는 각 원본 함수 '내부'에서만 호출됨)."
    - "신규 헬퍼 이름(RunDatumDualImageDetection/RunDatumSingleImageDetection)이 기존 메서드명과 충돌하지 않는다(플래너가 grep 으로 사전 확인 완료, 0건). 신규 코드에 삼항 `?:` 0건, C# 7.2, 이 파일 기존 K&R 스타일(여는 중괄호 선언과 같은 줄) 그대로 유지, 헝가리언 변수명(datum/imgH/imgV/nDatumOk/nDatumFail 등) 무변경."
    - "빌드 PASS — 매 Task 마다 error CS 0건, warning CS 정확히 12건(baseline, CS0618×10+CS0162×2) 유지. 신규 CS0219/CS0168/CS0103/CS0161(out/ref 미할당) 0건."
    - "파일 최종 줄수 — Task1 종료 시 **1765**줄(1759+6, old_string 33줄→new_string 39줄 순증가), Task2 종료 시 **1771**줄(1765+6, old_string 39줄→new_string 45줄). 이 값은 플래너가 old_string/new_string 을 실제 python 문자열 치환으로 스크래치 파일에 적용해 `wc -l` 로 실측 검증(단일 매치 확인 + 중괄호 밸런스 0 확인 포함) — 손계산 추정이 아니라 결정론적 실측값이다."
    - "Action_FAIMeasurement.cs 단 1개 파일만 2커밋에 걸쳐 변경. WPF_Example/DatumMeasurement.csproj(로컬 미커밋 오염, 항상 존재)는 매 커밋 후에도 git status 에 unstaged M 으로 남는다 — git add 는 대상 파일 경로 직접 지정만 사용, `git add -A`/`-a` 금지."
    - "파일 인코딩 손상 0건 — UTF-8 BOM 유지 + LF 개행 유지(CRLF 유입 0건), 한글 주석/문자열 손상 0건. Edit 도구만 사용(bash/python heredoc 금지, 한글 텍스트 작성 시 특히)."
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs"
      provides: "RunDatumDualImageDetection / RunDatumSingleImageDetection 헬퍼 2개 신설 — ProcessDatumDualImage/ProcessDatumSingleImage 의 align/detect 분기를 오케스트레이터-헬퍼 패턴으로 분리"
      contains: "private void RunDatumDualImageDetection(DatumConfig datum, InspectionSequence parentSeq, HImage imgH, HImage imgV) {"
  key_links:
    - from: "ProcessDatumDualImage try 블록 마지막 문장"
      to: "RunDatumDualImageDetection"
      via: "직접 호출(원본 try 블록 안, finally 이전)"
      pattern: "RunDatumDualImageDetection\\(datum, parentSeq, imgH, imgV\\)"
    - from: "ProcessDatumSingleImage try 블록 전체"
      to: "RunDatumSingleImageDetection"
      via: "직접 호출(ref nDatumOk, ref nDatumFail 전달)"
      pattern: "RunDatumSingleImageDetection\\(datum, parentSeq, img, ref nDatumOk, ref nDatumFail\\)"
---

<objective>
`Action_FAIMeasurement.cs`(오늘 6차례 리팩토링 완료 — fik/gf1/hyk/j6j/q9t/rle, 전부 "동작 무변경" 검증됨, HEAD=`6a771fa`, 현재 1759줄) 사용자 요청 Bundle C, 2개 항목:

1. `ProcessDatumDualImage`(L266-330)의 `IsPatternAlignEnabled` align/detect 분기 → 신규 `RunDatumDualImageDetection` 헬퍼로 추출
2. `ProcessDatumSingleImage`(L333-384)의 `IsPatternAlignEnabled` align/detect 분기 → 신규 `RunDatumSingleImageDetection` 헬퍼로 추출

두 헬퍼 모두 이 파일에 이미 존재하는 `TryExecuteMeasurement`/`TryExecuteCrossZMeasurement`(측정 실행부, L1520/L1650)와 동일한 "오케스트레이터가 같은 모양의 헬퍼를 호출" 패턴을 Datum 검출부에도 동일하게 적용한다.

Purpose: 동작은 단 하나도 바뀌지 않으면서, DualImage/SingleImage 양쪽에서 반복되던 "align이냐 detect냐" 분기 책임을 각 함수 밖으로 명확히 분리한다.
Output: 파일 1개 수정(새 파일 0개), 헬퍼 2개 신설, 커밋 2개(Task 당 1개).

⚠ **효율 지침(사용자 명시)**: 오늘 이전 번들(q9t)은 스크래치 git 저장소를 만들어 diff 를 전부 실측 시뮬레이션하느라 ~40분이 들었다. 이번 플랜은 그 방식을 쓰지 않았다 — 현재 파일을 직접 Read/Grep 으로 확인하고, old_string/new_string 을 python 문자열 치환(단일 매치 확인 + 중괄호 밸런스 확인)으로 스크래치에 1회만 적용해 `wc -l` 최종 줄수를 결정론적으로 검증했다(9분 내외 소요). 실행 단계에서도 이 값을 그대로 신뢰하고 재시뮬레이션할 필요 없다.

⚠ 아래 old_string 은 이번 세션에서 방금 Read 로 재확인한 원문 그대로다(줄번호 변동 없음 확인 완료 — L266-330/L333-384, 사용자가 제시한 원문과 정확히 일치, rle 가 마지막 편집).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

### 착수 시점 고정값 (플래너 실측, 이번 세션)

| 항목 | 값 |
|---|---|
| HEAD | **`6a771fa`** |
| 워킹트리 | ` M WPF_Example/DatumMeasurement.csproj` 1건뿐(커밋 금지 로컬 설정 — 항상 존재) |
| 대상 파일 | `WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` — **1759줄**, UTF-8 BOM 있음, LF |
| ProcessDatumDualImage | L266-330(65줄), 외부 호출부 정확히 1곳: L259 `ProcessDatumDualImage(datum, parentSeq);` |
| ProcessDatumSingleImage | L333-384(52줄), 외부 호출부 정확히 1곳: L261 `ProcessDatumSingleImage(datum, parentSeq, ref nDatumOk, ref nDatumFail);` |
| TryExecuteMeasurement/TryExecuteCrossZMeasurement(대칭 대상) | L1520/L1650 — 오케스트레이터(측정 실행 switch)가 같은 모양의 헬퍼를 호출하는 기존 관례 |
| 이름 충돌 확인 | `RunDatumDualImageDetection`/`RunDatumSingleImageDetection` grep 결과 0건(기존 미존재, 안전) |
| 예상 최종 줄수 | Task1 후 **1765**(1759+6), Task2 후 **1771**(1765+6) — python 문자열 치환 실측(단일 매치 + 중괄호 밸런스 0 확인) |

### Task 1 대상 — ProcessDatumDualImage 원문 (old_string, 33줄 그대로)

```csharp
                //260619 hbk Phase 57 #4 DualImage align 배선 (deferred 게이트 해제, 단일이미지 분기 미러).
                //  enabled → align 단독 경로(imgH 패턴매칭 → 단일 alignRigid 를 imgH/imgV 두 검출에 적용, D-01). disabled → 기존 2-image 검출(off 회귀 0).
                //  패턴 모델은 가로축(imgH/TeachingImagePath) 1세트만 사용 — 세로엔 패턴 없음(D-04).
                if (datum.IsPatternAlignEnabled) {
                    string modelPath = InspectionSequence.ResolveDatumModelPath(datum, parentSeq.Name); // 260723 hbk quick-fix: 전역 Shots[0] 폴백 결함 수정 — 소유 시퀀스명 명시 전달 (D-07)
                    string alignErr;
                    if (!parentSeq.TryComposeAlign(datum, imgH, imgV, modelPath, out alignErr)) {
                        string dn = datum.DatumName;
                        dn = dn ?? "";
                        string ae = alignErr;
                        ae = ae ?? "";
                        Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + dn + "' DualImage 패턴매칭 실패 (ALIGN_FAIL, skip): " + ae);
                        datum.RuntimeDetectFailed = true;
                        parentSeq.MarkAlignFailed(datum.DatumName); //260619 hbk Phase 57 #5 lenient — NG 강제, abort 안 함
                    }
                } else {
                    string derr;
                    if (!parentSeq.TryRunSingleDatum(datum, imgH, imgV, out derr)) { // 기존 검출 경로 무수정
                        string datumName = datum.DatumName;
                        datumName = datumName ?? "";
                        string derrStr = derr;
                        derrStr = derrStr ?? "";
                        Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + datumName + "' 검출 실패 (skip): " + derrStr);
                        datum.RuntimeDetectFailed = true;
                        parentSeq.MarkDatumFailed(datum.DatumName);
                    }
                }
            } finally {
                SafeDisposeImage(imgH);
                SafeDisposeImage(imgV);
            }
        }

```

### Task 1 결과 — new_string (39줄, 플래너가 python 치환으로 실측 검증 완료)

```csharp
                RunDatumDualImageDetection(datum, parentSeq, imgH, imgV); //260819 hbk quick-260819-s05: align/detect 분기를 RunDatumDualImageDetection 헬퍼로 추출(측정 TryExecuteMeasurement/TryExecuteCrossZMeasurement 와 동일 패턴)
            } finally {
                SafeDisposeImage(imgH);
                SafeDisposeImage(imgV);
            }
        }

        //260819 hbk quick-260819-s05: ProcessDatumDualImage 의 align/detect 분기 본문 추출 — 측정 쪽 TryExecuteMeasurement/TryExecuteCrossZMeasurement 와
        //  동일한 "오케스트레이터가 같은 모양의 헬퍼를 호출" 패턴. 카운터(nDatumOk/nDatumFail) 미접근 구조 그대로 유지(원본 ProcessDatumDualImage 도 카운터 미사용, D-1).
        private void RunDatumDualImageDetection(DatumConfig datum, InspectionSequence parentSeq, HImage imgH, HImage imgV) {
            //260619 hbk Phase 57 #4 DualImage align 배선 (deferred 게이트 해제, 단일이미지 분기 미러).
            //  enabled → align 단독 경로(imgH 패턴매칭 → 단일 alignRigid 를 imgH/imgV 두 검출에 적용, D-01). disabled → 기존 2-image 검출(off 회귀 0).
            //  패턴 모델은 가로축(imgH/TeachingImagePath) 1세트만 사용 — 세로엔 패턴 없음(D-04).
            if (datum.IsPatternAlignEnabled) {
                string modelPath = InspectionSequence.ResolveDatumModelPath(datum, parentSeq.Name); // 260723 hbk quick-fix: 전역 Shots[0] 폴백 결함 수정 — 소유 시퀀스명 명시 전달 (D-07)
                string alignErr;
                if (!parentSeq.TryComposeAlign(datum, imgH, imgV, modelPath, out alignErr)) {
                    string dn = datum.DatumName;
                    dn = dn ?? "";
                    string ae = alignErr;
                    ae = ae ?? "";
                    Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + dn + "' DualImage 패턴매칭 실패 (ALIGN_FAIL, skip): " + ae);
                    datum.RuntimeDetectFailed = true;
                    parentSeq.MarkAlignFailed(datum.DatumName); //260619 hbk Phase 57 #5 lenient — NG 강제, abort 안 함
                }
            } else {
                string derr;
                if (!parentSeq.TryRunSingleDatum(datum, imgH, imgV, out derr)) { // 기존 검출 경로 무수정
                    string datumName = datum.DatumName;
                    datumName = datumName ?? "";
                    string derrStr = derr;
                    derrStr = derrStr ?? "";
                    Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + datumName + "' 검출 실패 (skip): " + derrStr);
                    datum.RuntimeDetectFailed = true;
                    parentSeq.MarkDatumFailed(datum.DatumName);
                }
            }
        }

```

### Task 2 대상 — ProcessDatumSingleImage 원문 (old_string, 39줄 그대로 — Task1 완료 후에는 실제 파일에서 줄번호가 +6 이동되어 있으므로 grep -n 으로 재탐색할 것. 텍스트 내용 자체는 무변경)

```csharp
                if (datum.IsPatternAlignEnabled) {
                    string modelPath = InspectionSequence.ResolveDatumModelPath(datum, parentSeq.Name); // 260723 hbk quick-fix: 전역 Shots[0] 폴백 결함 수정 — 소유 시퀀스명 명시 전달 (D-07)
                    string alignErr;
                    if (!parentSeq.TryComposeAlign(datum, img, modelPath, out alignErr)) {
                        string dn = datum.DatumName;
                        dn = dn ?? "";
                        string ae = alignErr;
                        ae = ae ?? "";
                        Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + dn + "' 패턴매칭 실패 (ALIGN_FAIL, skip): " + ae);
                        datum.RuntimeDetectFailed = true;
                        parentSeq.MarkAlignFailed(datum.DatumName); // D-10 lenient — 측정 NG(ALIGN_FAIL) 강제, abort 안 함
                        nDatumFail++;
                    } else {
                        nDatumOk++;
                    }
                    //260818 hbk [SEQ] 패턴매칭 경로를 탔음을 표시
                    LogSeqAlgo("Datum", datum.DatumName, "TryComposeAlign(패턴매칭)");
                } else {
                    string derr;
                    if (!parentSeq.TryRunSingleDatum(datum, img, null, out derr)) { // 기존 검출 경로 무수정
                        string datumName = datum.DatumName;
                        datumName = datumName ?? "";
                        string derrStr = derr;
                        derrStr = derrStr ?? "";
                        Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + datumName + "' 검출 실패 (skip): " + derrStr);
                        datum.RuntimeDetectFailed = true;
                        parentSeq.MarkDatumFailed(datum.DatumName);
                        nDatumFail++;
                    } else {
                        nDatumOk++;
                    }
                    //260818 hbk [SEQ] 어떤 검출 알고리즘을 탔는지 표시 — 알고리즘 탭에서 상세 확인
                    LogSeqAlgo("Datum", datum.DatumName, "TryRunSingleDatum/" + datum.AlgorithmTypeEnum);
                }
            } finally {
                img.Dispose();
            }
        }

```

### Task 2 결과 — new_string (45줄, 플래너가 python 치환으로 실측 검증 완료)

```csharp
                RunDatumSingleImageDetection(datum, parentSeq, img, ref nDatumOk, ref nDatumFail); //260819 hbk quick-260819-s05: align/detect 분기를 RunDatumSingleImageDetection 헬퍼로 추출(측정 TryExecuteMeasurement/TryExecuteCrossZMeasurement 와 동일 패턴)
            } finally {
                img.Dispose();
            }
        }

        //260819 hbk quick-260819-s05: ProcessDatumSingleImage 의 align/detect 분기 본문 추출 — 측정 쪽 TryExecuteMeasurement/TryExecuteCrossZMeasurement 와
        //  동일한 "오케스트레이터가 같은 모양의 헬퍼를 호출" 패턴. ref nDatumOk/nDatumFail 은 이 분기 안에서 실제로 증가되므로 ref 그대로 전달(out 아님).
        private void RunDatumSingleImageDetection(DatumConfig datum, InspectionSequence parentSeq, HImage img, ref int nDatumOk, ref int nDatumFail) {
            if (datum.IsPatternAlignEnabled) {
                string modelPath = InspectionSequence.ResolveDatumModelPath(datum, parentSeq.Name); // 260723 hbk quick-fix: 전역 Shots[0] 폴백 결함 수정 — 소유 시퀀스명 명시 전달 (D-07)
                string alignErr;
                if (!parentSeq.TryComposeAlign(datum, img, modelPath, out alignErr)) {
                    string dn = datum.DatumName;
                    dn = dn ?? "";
                    string ae = alignErr;
                    ae = ae ?? "";
                    Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + dn + "' 패턴매칭 실패 (ALIGN_FAIL, skip): " + ae);
                    datum.RuntimeDetectFailed = true;
                    parentSeq.MarkAlignFailed(datum.DatumName); // D-10 lenient — 측정 NG(ALIGN_FAIL) 강제, abort 안 함
                    nDatumFail++;
                } else {
                    nDatumOk++;
                }
                //260818 hbk [SEQ] 패턴매칭 경로를 탔음을 표시
                LogSeqAlgo("Datum", datum.DatumName, "TryComposeAlign(패턴매칭)");
            } else {
                string derr;
                if (!parentSeq.TryRunSingleDatum(datum, img, null, out derr)) { // 기존 검출 경로 무수정
                    string datumName = datum.DatumName;
                    datumName = datumName ?? "";
                    string derrStr = derr;
                    derrStr = derrStr ?? "";
                    Logging.PrintLog((int)ELogType.Error, "[FAIMeasurement] Datum '" + datumName + "' 검출 실패 (skip): " + derrStr);
                    datum.RuntimeDetectFailed = true;
                    parentSeq.MarkDatumFailed(datum.DatumName);
                    nDatumFail++;
                } else {
                    nDatumOk++;
                }
                //260818 hbk [SEQ] 어떤 검출 알고리즘을 탔는지 표시 — 알고리즘 탭에서 상세 확인
                LogSeqAlgo("Datum", datum.DatumName, "TryRunSingleDatum/" + datum.AlgorithmTypeEnum);
            }
        }
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: RunDatumDualImageDetection 헬퍼 신설 + ProcessDatumDualImage align/detect 분기 추출 [S05-01]</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <action>
### 0. 착수 전 재확인
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
wc -l "$F"   # 기대 1759
grep -cF 'private void ProcessDatumDualImage(DatumConfig datum, InspectionSequence parentSeq) {' "$F"   # 기대 1
grep -cF 'ProcessDatumDualImage(datum, parentSeq);' "$F"   # 기대 1 (L259 호출부)
grep -cF 'RunDatumDualImageDetection' "$F"   # 기대 0 (아직 미생성 — 자기참조 오염 사전 확인)
```
줄번호가 계획 시점(L266-330)과 다르면 grep -n 으로 실제 위치를 재탐색하되, 원문 텍스트 자체(context 섹션의 "Task 1 대상" old_string)는 그대로 사용 — 내용은 변형하지 않는다.

### 1. Edit 도구로 치환

old_string: context 섹션의 "Task 1 대상 — ProcessDatumDualImage 원문" 코드 블록 그대로(33줄).

new_string: context 섹션의 "Task 1 결과 — new_string" 코드 블록 그대로(39줄, 플래너가 이미 python 문자열 치환으로 단일 매치·중괄호 밸런스 0 을 실측 검증했다 — 신뢰하고 그대로 적용).

들여쓰기 규칙: 원본 if/else 블록은 try 안쪽(16칸 기준)에 있었으나, 새 헬퍼 안으로 옮기며 메서드 본문 최상위(12칸 기준)로 4칸씩 dedent 되었다 — new_string 이 이미 이 dedent 를 반영한 최종형태이므로 추가로 손댈 필요 없다.

### 2. 커밋 (대상 파일 1개만 경로 지정 스테이징)
```bash
cd /c/Info/Project/DataMeasurement
git add WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
git diff --cached --name-only   # 반드시 1줄만 출력되는지 확인 후 커밋
git commit -m "refactor(260819-s05): ProcessDatumDualImage align/detect 분기를 RunDatumDualImageDetection 헬퍼로 추출"
```
  </action>
  <verify>
    <automated>
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs

echo "== 줄수(결정론적, wc -l) ==" && \
[ "$(wc -l < "$F" | tr -d ' ')" = "1765" ] && echo "  OK 1765줄" && \

echo "== 카운트(자기참조 오염 주의 — 헬퍼 자신의 선언도 함께 잡힘) ==" && \
[ "$(grep -oF 'RunDatumDualImageDetection(' "$F" | wc -l)" = "2" ] && echo "  OK RunDatumDualImageDetection( = 2 (선언1+호출1)" && \
[ "$(grep -oF 'ProcessDatumDualImage(' "$F" | wc -l)" = "2" ] && echo "  OK ProcessDatumDualImage( = 2 (선언1+호출1, 무변경)" && \
[ "$(grep -cF 'RunDatumSingleImageDetection' "$F")" = "0" ] && echo "  OK Task2 헬퍼 아직 미생성(순서 확인)" && \

echo "== try/finally 원위치 보존 ==" && \
[ "$(grep -oF 'SafeDisposeImage(imgH)' "$F" | wc -l)" = "1" ] && \
[ "$(grep -oF 'SafeDisposeImage(imgV)' "$F" | wc -l)" = "1" ] && echo "  OK finally(Dispose) 원본 함수에 그대로 유지" && \

echo "== 전역 카운트 무변경(순수 이동 확인) ==" && \
[ "$(grep -oF 'IsPatternAlignEnabled' "$F" | wc -l)" = "2" ] && echo "  OK IsPatternAlignEnabled = 2 (전역 무변경)" && \
[ "$(grep -oF 'TryComposeAlign(' "$F" | wc -l)" = "3" ] && echo "  OK TryComposeAlign( = 3 (전역 무변경)" && \

echo "== 외부 호출부 무변경 ==" && \
[ "$(grep -cF 'ProcessDatumDualImage(datum, parentSeq);' "$F")" = "1" ] && echo "  OK ProcessDatumDualImage 외부 호출부 1곳 그대로" && \

echo "== 인코딩/한글 보존 ==" && \
[ "$(head -c 3 "$F" | xxd -p)" = "efbbbf" ] && echo "  OK UTF-8 BOM 유지" && \
[ "$(grep -c $'\r' "$F")" = "0" ] && echo "  OK LF 유지(CRLF 오염 없음)" && \

echo "== 위생 ==" && \
[ "$(git show --name-only --format='' HEAD | grep -c .)" = "1" ] && \
[ "$(git status --porcelain)" = " M WPF_Example/DatumMeasurement.csproj" ] && echo "  OK 파일1개, csproj unstaged" && \

echo "== (정보용, 하드게이트 아님) numstat ==" && \
git diff --numstat HEAD~1 HEAD -- "$F"
```
    </automated>
    <automated>
```bash
cd /c/Info/Project/DataMeasurement
SCR="C:/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/9d3a7b4d-2314-4b14-8686-52fd6346a1f9/scratchpad"
MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" WPF_Example/DatumMeasurement.csproj -t:Build -p:Configuration=Debug -p:Platform=x64 -p:OutputPath="$SCR\\s05-t1\\" -v:minimal -nologo > "$SCR/s05-t1-build.log" 2>&1
[ "$(grep -c ': error ' "$SCR/s05-t1-build.log")" = "0" ] && [ "$(grep -c ': warning CS' "$SCR/s05-t1-build.log")" = "12" ] && echo "BUILD PASS (error0/warning12)"
```
    </automated>
  </verify>
  <done>RunDatumDualImageDetection 신설(TryExecuteMeasurement/TryExecuteCrossZMeasurement 와 대칭되는 오케스트레이터-헬퍼 패턴), ProcessDatumDualImage 의 align/detect 분기가 헬퍼 호출로 치환. try/finally(SafeDisposeImage) 는 원본 함수에 그대로 유지. 파일 1765줄. 빌드 error0/warning12. 파일 1개만 커밋.</done>
</task>

<task type="auto">
  <name>Task 2: RunDatumSingleImageDetection 헬퍼 신설 + ProcessDatumSingleImage align/detect 분기 추출 [S05-02]</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs</files>
  <action>
### 0. 착수 전 재확인
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
wc -l "$F"   # 기대 1765 (Task1 완료 후)
grep -cF 'private void ProcessDatumSingleImage(DatumConfig datum, InspectionSequence parentSeq, ref int nDatumOk, ref int nDatumFail) {' "$F"   # 기대 1
grep -cF 'ProcessDatumSingleImage(datum, parentSeq, ref nDatumOk, ref nDatumFail);' "$F"   # 기대 1 (L261 상당 호출부)
grep -cF 'RunDatumSingleImageDetection' "$F"   # 기대 0 (아직 미생성)
```
줄번호가 계획 시점(원본 L333-384, Task1 완료 후 +6 이동)과 다르면 grep -n 으로 실제 위치를 재탐색하되, old_string 텍스트 자체는 그대로 사용 — Task1 이 이 함수 앞부분에 6줄을 추가했을 뿐, ProcessDatumSingleImage 본문 텍스트는 무변경이므로 old_string 은 여전히 그대로 정확히 매치되어야 한다.

### 1. Edit 도구로 치환

old_string: context 섹션의 "Task 2 대상 — ProcessDatumSingleImage 원문" 코드 블록 그대로(39줄).

new_string: context 섹션의 "Task 2 결과 — new_string" 코드 블록 그대로(45줄, 플래너가 이미 python 문자열 치환으로 단일 매치·중괄호 밸런스 0 을 실측 검증했다 — 신뢰하고 그대로 적용).

핵심 확인 사항:
- `ref nDatumOk`/`ref nDatumFail` 를 `RunDatumSingleImageDetection` 시그니처와 호출부 양쪽에 정확히 `ref` 로 유지할 것(`out` 아님) — 이 분기 안에서 `nDatumOk++`/`nDatumFail++` 가 4곳(align 성공/실패, detect 성공/실패) 발생하므로 값이 호출부(ProcessDatumSingleImage 의 파라미터, 결국 RunDatumPhase 의 지역변수까지)로 되돌아가야 한다.
- try/finally(`img.Dispose();`)는 ProcessDatumSingleImage 에 그대로 남는다 — 헬퍼로 옮기지 않는다.

### 2. 커밋
```bash
cd /c/Info/Project/DataMeasurement
git add WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
git diff --cached --name-only
git commit -m "refactor(260819-s05): ProcessDatumSingleImage align/detect 분기를 RunDatumSingleImageDetection 헬퍼로 추출"
```
  </action>
  <verify>
    <automated>
```bash
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs

echo "== 줄수(결정론적, wc -l) ==" && \
[ "$(wc -l < "$F" | tr -d ' ')" = "1771" ] && echo "  OK 1771줄" && \

echo "== 카운트(자기참조 오염 주의) ==" && \
[ "$(grep -oF 'RunDatumSingleImageDetection(' "$F" | wc -l)" = "2" ] && echo "  OK RunDatumSingleImageDetection( = 2 (선언1+호출1)" && \
[ "$(grep -oF 'ProcessDatumSingleImage(' "$F" | wc -l)" = "2" ] && echo "  OK ProcessDatumSingleImage( = 2 (선언1+호출1, 무변경)" && \
[ "$(grep -c 'ref int nDatumOk, ref int nDatumFail' "$F")" = "2" ] && echo "  OK ref 시그니처 2곳(ProcessDatumSingleImage + RunDatumSingleImageDetection) 모두 ref 유지" && \

echo "== try/finally 원위치 보존 ==" && \
[ "$(grep -oF 'img.Dispose();' "$F" | wc -l)" = "1" ] && echo "  OK finally(img.Dispose) 원본 함수에 그대로 유지" && \

echo "== 전역 카운트 무변경(순수 이동 확인) ==" && \
[ "$(grep -oF 'IsPatternAlignEnabled' "$F" | wc -l)" = "2" ] && echo "  OK IsPatternAlignEnabled = 2 (전역 무변경, 이제 헬퍼2개 안에 각 1곳)" && \
[ "$(grep -oF 'TryComposeAlign(' "$F" | wc -l)" = "3" ] && echo "  OK TryComposeAlign( = 3 (전역 무변경)" && \
[ "$(grep -oF 'TryRunSingleDatum(' "$F" | wc -l)" = "2" ] && echo "  OK TryRunSingleDatum( = 2 (전역 무변경)" && \
[ "$(grep -oF 'MarkAlignFailed(' "$F" | wc -l)" = "2" ] && echo "  OK MarkAlignFailed( = 2 (전역 무변경)" && \
[ "$(grep -oF 'MarkDatumFailed(' "$F" | wc -l)" = "6" ] && echo "  OK MarkDatumFailed( = 6 (전역 무변경)" && \
[ "$(grep -oF 'nDatumOk++' "$F" | wc -l)" = "2" ] && echo "  OK nDatumOk++ = 2 (전역 무변경)" && \
[ "$(grep -oF 'nDatumFail++' "$F" | wc -l)" = "2" ] && echo "  OK nDatumFail++ = 2 (전역 무변경)" && \
[ "$(grep -oF 'LogSeqAlgo(' "$F" | wc -l)" = "3" ] && echo "  OK LogSeqAlgo( = 3 (전역 무변경)" && \

echo "== 외부 호출부 무변경 ==" && \
[ "$(grep -cF 'ProcessDatumSingleImage(datum, parentSeq, ref nDatumOk, ref nDatumFail);' "$F")" = "1" ] && echo "  OK ProcessDatumSingleImage 외부 호출부 1곳 그대로" && \

echo "== 인코딩/한글 보존 ==" && \
[ "$(head -c 3 "$F" | xxd -p)" = "efbbbf" ] && echo "  OK UTF-8 BOM 유지" && \
[ "$(grep -c $'\r' "$F")" = "0" ] && echo "  OK LF 유지(CRLF 오염 없음)" && \

echo "== 위생 ==" && \
[ "$(git show --name-only --format='' HEAD | grep -c .)" = "1" ] && \
[ "$(git status --porcelain)" = " M WPF_Example/DatumMeasurement.csproj" ] && echo "  OK 파일1개, csproj unstaged" && \

echo "== (정보용, 하드게이트 아님) numstat ==" && \
git diff --numstat HEAD~1 HEAD -- "$F"
```
    </automated>
    <automated>
```bash
cd /c/Info/Project/DataMeasurement
SCR="C:/Users/tech/AppData/Local/Temp/claude/C--Info-Project-DataMeasurement/9d3a7b4d-2314-4b14-8686-52fd6346a1f9/scratchpad"
MSB="/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"
"$MSB" WPF_Example/DatumMeasurement.csproj -t:Rebuild -p:Configuration=Debug -p:Platform=x64 -p:OutputPath="$SCR\\s05-t2\\" -v:minimal -nologo > "$SCR/s05-t2-build.log" 2>&1
[ "$(grep -c ': error ' "$SCR/s05-t2-build.log")" = "0" ] && [ "$(grep -c ': warning CS' "$SCR/s05-t2-build.log")" = "12" ] && echo "BUILD PASS (error0/warning12, 최종 clean Rebuild)"
```
    </automated>
    <automated>
```bash
# 최종 종합 확인 (2커밋 누적)
cd /c/Info/Project/DataMeasurement
F=WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
BASE=6a771fa
echo "== BASE..HEAD 누적 변경파일 = 대상 1개뿐 ==" && \
[ "$(git diff --name-only $BASE HEAD)" = "WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs" ] && echo "  OK 누적 변경파일 1개" && \
[ "$(wc -l < "$F" | tr -d ' ')" = "1771" ] && echo "  OK 최종 줄수 1771(1759+6+6)" && \
[ "$(git status --porcelain)" = " M WPF_Example/DatumMeasurement.csproj" ] && echo "  OK csproj 최종까지 unstaged"
```
    </automated>
  </verify>
  <done>RunDatumSingleImageDetection 신설, ProcessDatumSingleImage 가 align/detect 분기(ref nDatumOk/nDatumFail 포함)를 헬퍼로 위임하도록 분리. try/finally(img.Dispose) 원본 함수에 그대로 유지. 파일 1771줄. 빌드 error0/warning12(clean Rebuild). 파일 1개만 커밋. 2커밋 누적 변경파일 1개.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

이 플랜은 순수 내부 리팩토링(Extract Method — align/detect 분기를 헬퍼로 이동)으로, 신뢰 경계를 넘는 입력·외부 통신·권한 변경이 없다. 참고용으로 기존 경계만 기록한다.

| Boundary | Description |
|----------|--------------|
| Halcon 검출/패턴매칭 결과 → RuntimeDetectFailed/MarkAlignFailed/MarkDatumFailed | 검출 실패 신호가 판정 게이트(FAI NG)로 흘러가는 경로 — 이번 변경은 이 신호 경로를 그대로 이동만 하고 분기 조건/순서를 바꾸지 않음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-s05-01 | T (변조) | RunDatumSingleImageDetection 의 ref nDatumOk/nDatumFail | mitigate | must_haves 에서 `ref` 시그니처(out 아님) 및 원본과 동일 4곳 증가 지점(align 성공/실패, detect 성공/실패)을 grep 으로 하드 검증 — out 으로 잘못 바뀌면 DatumPhase 완료 카운트 요약(LogDatumPhaseSummary)이 항상 0 을 표시하는 조용한 회귀가 발생하므로 |
| T-s05-02 | D (서비스거부) | 원본 함수의 try/finally(SafeDisposeImage/img.Dispose) | mitigate | must_haves 에서 finally 블록이 헬퍼로 이동하지 않고 원본 함수에 남는지 grep 으로 검증 — 이동되면 헬퍼 내부 조기 반환 시 HImage 누수 경로가 생길 수 있으므로(현재 헬퍼는 조기 반환 없이 항상 끝까지 실행되지만, 원칙적으로 Dispose 소유권은 이미지를 grab/load 한 원본 함수가 유지해야 함) |

</threat_model>

<verification>

### 실패 시 대응
- **Edit old_string 매치 실패** → 원문이 계획 시점과 달라졌다는 뜻(특히 Task2 는 Task1 로 인해 줄번호만 이동, 텍스트는 무변경이어야 정상). grep -n 으로 실제 위치를 재탐색해 old_string 을 실제 원문으로 재구성(내용 자체는 절대 변형하지 말 것). 매치가 2건 이상 나오면 즉시 중단 — old_string 범위를 넓혀 유일 매치가 되도록 조정.
- **줄수(wc -l) 불일치** → new_string 을 실수로 다르게 작성했다는 뜻. git diff 로 실제 삽입/삭제된 줄을 눈으로 대조해 원인 파악 후 수정. 기대값을 몰래 완화하지 않는다.
- **BOM/LF 손상 감지** → 즉시 중단하고 git diff 로 손상 범위 확인 후 보고(자동 복구 시도 금지).
- **빌드 산출물 잠김** → OutputPath 이름만 바꿔 재시도. **프로세스 종료 금지.**

### 런타임 UAT
정적 검증(grep 카운트+wc -l+빌드+중괄호 밸런스는 플래너가 사전 실측)만으로 회귀 0 을 주장한다 — 순수 텍스트 추출/재배치라 판정 로직 접근 없음. 실기 확인이 필요하면 Shot 1개(Datum 검출 있는 레시피, DualImage/SingleImage 각 1종 이상 포함)를 검사해 align enabled/disabled 양쪽 경로에서 이전과 동일한 [SEQ] 로그(TryComposeAlign(패턴매칭)/TryRunSingleDatum/{AlgorithmTypeEnum}) 와 DatumPhase 완료 요약(검출성공/실패 카운트)이 나오는지 확인.

</verification>

<success_criteria>
- `RunDatumDualImageDetection(DatumConfig,InspectionSequence,HImage,HImage)` / `RunDatumSingleImageDetection(DatumConfig,InspectionSequence,HImage,ref int,ref int)` 헬퍼 2개 신설, 둘 다 `private`(인스턴스 메서드, TryExecuteMeasurement/TryExecuteCrossZMeasurement 와 동일한 오케스트레이터-헬퍼 패턴)
- ProcessDatumDualImage/ProcessDatumSingleImage 의 `IsPatternAlignEnabled` align/detect 분기 전체가 각각 헬퍼로 이동 — try/finally(이미지 Dispose)는 원본 함수에 그대로 유지
- `ref nDatumOk`/`ref nDatumFail` 정확히 유지(out 금지) — 원본과 동일한 4개 증가 지점(align 성공/실패, detect 성공/실패) 보존
- 두 Task 모두 `wc -l` 최종 줄수 정확 일치(1765 → 1771), 매 Task 빌드 error0/warning12
- `Action_FAIMeasurement.cs` 단 1개 파일만 2커밋에 걸쳐 변경, `DatumMeasurement.csproj` 는 끝까지 unstaged
- UTF-8 BOM 유지 + LF 개행 유지(CRLF 오염 0건) + 한글 주석/문자열 손상 0건
- 신규 코드 삼항 `?:` 0건, public/internal 시그니처 변경 0건(ProcessDatumDualImage/ProcessDatumSingleImage 둘 다 무변경, 외부 호출부 1곳씩 그대로 컴파일)
</success_criteria>

<output>
완료 후 `.planning/quick/260819-s05-fai-refactor-bundle-c/260819-s05-SUMMARY.md` 작성(Edit/Write 도구 사용 — heredoc 금지, 한글 인코딩 보존).
</output>
