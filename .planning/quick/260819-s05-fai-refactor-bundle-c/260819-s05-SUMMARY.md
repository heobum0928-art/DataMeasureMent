---
phase: quick-260819-s05
plan: 01
status: complete
subsystem: inspection
tags: [csharp, refactor, extract-method, datum-detection]

requires:
  - phase: quick-260819-rle
    provides: "동일 파일 오늘자 Bundle B(LogDatumPhaseSummary/ResolveFaiCaptureFileNames 헬퍼) 결과물 위에서 진행"
provides:
  - "RunDatumDualImageDetection(DatumConfig,InspectionSequence,HImage,HImage) 헬퍼 신설 — ProcessDatumDualImage 의 IsPatternAlignEnabled align/detect 분기를 오케스트레이터-헬퍼 패턴으로 분리"
  - "RunDatumSingleImageDetection(DatumConfig,InspectionSequence,HImage,ref int,ref int) 헬퍼 신설 — ProcessDatumSingleImage 의 동일 분기를 ref 카운터(nDatumOk/nDatumFail) 유지한 채 분리"
affects: [action-faimeasurement]

tech-stack:
  added: []
  patterns:
    - "측정 실행부(TryExecuteMeasurement/TryExecuteCrossZMeasurement)와 동일한 '오케스트레이터가 같은 모양의 헬퍼를 호출' 패턴을 Datum 검출부(DualImage/SingleImage)에도 동일 적용"
    - "try/finally(이미지 Dispose) 소유권은 항상 원본 함수에 유지 — 헬퍼는 조기 반환 없이 항상 끝까지 실행되는 순수 분기 로직만 담당"

key-files:
  created: []
  modified:
    - WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs

key-decisions:
  - "RunDatumSingleImageDetection 의 nDatumOk/nDatumFail 은 ref 로 유지(out 아님) — 이 분기 안에서 4곳(align 성공/실패, detect 성공/실패)이 실제로 증가하므로 out 으로 바꾸면 DatumPhase 완료 카운트가 항상 0 이 되는 조용한 회귀가 발생"
  - "plan 의 Task2 new_string(45줄 명시)이 실제로는 44줄(RunDatumSingleImageDetection 닫는 중괄호 뒤 구분 공백줄 누락)이었음을 확인 — CLAUDE.md Code Style '메서드 사이 빈 줄 1개' 관례 및 Task1 헬퍼(빈 줄 있음)와의 일관성을 위해 공백줄 1개를 추가로 삽입, plan 이 명시한 최종 줄수(1771) 및 검증 스크립트 하드게이트와도 정확히 일치"

requirements-completed: [S05-01, S05-02]

duration: 약 10분
completed: 2026-08-19
---

# Quick 260819-s05: FAI 리팩토링 Bundle C (RunDatumDualImageDetection/RunDatumSingleImageDetection 헬퍼 추출) Summary

**`Action_FAIMeasurement.cs` 순수 Extract Method 2건 — ProcessDatumDualImage/ProcessDatumSingleImage 의 `IsPatternAlignEnabled` align/detect 분기를 각각 `RunDatumDualImageDetection`/`RunDatumSingleImageDetection` 헬퍼로 분리(측정 쪽 TryExecuteMeasurement/TryExecuteCrossZMeasurement 와 동일한 오케스트레이터-헬퍼 패턴). 2커밋 전부 `wc -l` 최종 실측값(1765→1771)과 정확히 일치, 최종 clean Rebuild error0/warning12(baseline).**

## Performance

- **Duration:** 약 10분 (커밋 2건, 검증+빌드 포함)
- **Tasks:** 2/2 완료
- **Files modified:** 1

## Accomplishments

- `RunDatumDualImageDetection(DatumConfig datum, InspectionSequence parentSeq, HImage imgH, HImage imgV)` 헬퍼 신설 — `ProcessDatumDualImage` 의 `IsPatternAlignEnabled` if/else 분기(align 단독 경로 vs 기존 2-image 검출) 전체를 이동. `TryGrabOrLoadDualDatumImages`/`IsDatumZIndexMisconfigured` 게이트, `try/finally(SafeDisposeImage)` 는 원본 함수에 그대로 유지
- `RunDatumSingleImageDetection(DatumConfig datum, InspectionSequence parentSeq, HImage img, ref int nDatumOk, ref int nDatumFail)` 헬퍼 신설 — `ProcessDatumSingleImage` 의 동일 분기(align/detect, 4곳의 `nDatumOk++`/`nDatumFail++` 포함)를 `ref` 파라미터 그대로 유지한 채 이동. `try/finally(img.Dispose)` 는 원본 함수에 그대로 유지
- 두 Task 모두 신규 코드 삼항 `?:` 0건(if-else 만 사용), 헝가리언 변수명(datum/imgH/imgV/nDatumOk/nDatumFail 등) 무변경, public/internal 시그니처 변경 0건 — 외부 호출부(L259/L261 상당) 그대로 컴파일
- 순수 이동 — 이동된 코드는 byte-identical(재들여쓰기만), 판정 로직/제어흐름 변경 0건

## Task Commits

Each task was committed atomically:

1. **Task 1: RunDatumDualImageDetection 헬퍼 신설 + ProcessDatumDualImage align/detect 분기 추출** - `dcf8aae` (refactor)
2. **Task 2: RunDatumSingleImageDetection 헬퍼 신설 + ProcessDatumSingleImage align/detect 분기 추출** - `ee6ac1e` (refactor)

## Files Created/Modified

- `WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` - 헬퍼 2개 신설(RunDatumDualImageDetection/RunDatumSingleImageDetection), ProcessDatumDualImage/ProcessDatumSingleImage 의 align/detect 분기 추출. 1759줄 → 1771줄(순증가 +12)

## Verification Results

| Task | 커밋 | wc -l (결정론적) | 기대값 | 일치 | 빌드 (error/warning) |
|---|---|---|---|---|---|
| 1 | `dcf8aae` | 1765 | 1765 | PASS | 0 / 12 (baseline) |
| 2 | `ee6ac1e` | 1771 | 1771 | PASS | 0 / 12 (baseline, 최종 `-t:Rebuild` clean 빌드) |

### Task별 카운트 검증 (자기참조 오염 방지 포함)

- **Task 1:** `RunDatumDualImageDetection(` = **2**(선언1+호출1, PASS) / `ProcessDatumDualImage(` = **2**(선언1+호출1, 무변경) / `RunDatumSingleImageDetection` 존재 = **0**(Task2 미착수 확인) / `SafeDisposeImage(imgH)`/`(imgV)` = 각 **1**(finally 원위치 유지) / `IsPatternAlignEnabled` 전역 = **2**(무변경) / `TryComposeAlign(` 전역 = **3**(무변경) / 외부 호출부 `ProcessDatumDualImage(datum, parentSeq);` = **1**(무변경) / UTF-8 BOM + LF 유지, 중괄호 밸런스 0
- **Task 2:** `RunDatumSingleImageDetection(` = **2**(선언1+호출1, PASS) / `ProcessDatumSingleImage(` = **2**(무변경) / `img.Dispose();` = **1**(finally 원위치 유지) / `IsPatternAlignEnabled`/`TryComposeAlign(`/`TryRunSingleDatum(`/`MarkAlignFailed(`/`MarkDatumFailed(`/`nDatumOk++`/`nDatumFail++`/`LogSeqAlgo(` 전역 = 각각 2/3/2/2/6/2/2/3 (전부 무변경) / 외부 호출부 `ProcessDatumSingleImage(datum, parentSeq, ref nDatumOk, ref nDatumFail);` = **1**(무변경) / UTF-8 BOM + LF 유지, 중괄호 밸런스 0
- **Task 2 검증 스크립트 자체 오탐 1건:** plan 이 `ref int nDatumOk, ref int nDatumFail` 전역 카운트를 **2**(ProcessDatumSingleImage + RunDatumSingleImageDetection)로 기대했으나 실측은 **3** — 이 파일에 이미 존재하던 무관한 `ProcessOneDatum(..., ref int nDatumOk, ref int nDatumFail, ref int nDatumCached)`(L239, `git show 6a771fa` 로 베이스라인부터 존재 확인)를 plan 이 계산에서 빠뜨린 것. 이번 편집으로 생긴 문제 아님(아래 Deviations 참고)

### 누적 확인 (2커밋)

`git diff --name-only 6a771fa HEAD` = `Action_FAIMeasurement.cs` 1개 파일뿐. 최종 `wc -l` = **1771**(1759+6+6, plan 이 명시한 최종 목표값과 정확히 일치). `git status --porcelain` = ` M WPF_Example/DatumMeasurement.csproj` 만(csproj 2커밋 내내 unstaged 유지).

### 인코딩 보존

매 Task 편집 직후 확인 — UTF-8 BOM(`efbbbf`) 유지 2회 전부 PASS, `grep -c $'\r'` = 0 2회 전부 PASS(CRLF 오염 없음). 한글 주석/문자열 손상 0건.

### 커밋 위생

매 Task 커밋 전 `git diff --cached --name-only` = 대상 파일 1줄만 출력 확인 후 커밋. 매 커밋 후 `git status --short` 로 `WPF_Example/DatumMeasurement.csproj` 가 계속 ` M`(unstaged) 상태인지 확인 — 2회 전부 PASS, 한 번도 스테이징되지 않음.

## Decisions Made

- `RunDatumSingleImageDetection` 의 `nDatumOk`/`nDatumFail` 은 `ref` 로 그대로 유지(out 금지) — 원본과 동일한 4개 증가 지점(align 성공/실패, detect 성공/실패) 보존
- plan Task2 의 new_string 이 실제로는 44줄(RunDatumSingleImageDetection 닫는 중괄호 뒤 메서드 구분 공백줄 1개 누락)이었음을 발견 — Task1 헬퍼(공백줄 있음)와의 일관성 및 CLAUDE.md Code Style("Single blank line between methods") 준수를 위해 공백줄 1개를 추가로 삽입. 결과적으로 plan 이 명시한 최종 목표 줄수(1771)와도 정확히 일치하게 됨(순수 스타일 보정, 로직/판정 변경 없음)
- plan Task2 검증 스크립트가 전제한 `ref int nDatumOk, ref int nDatumFail` 전역 카운트(기대 2)가 실측 3 으로 나온 것은 이 파일에 이미 존재하던 무관한 `ProcessOneDatum`(이번 plan 범위 밖) 때문임을 `git show 6a771fa` 로 재확인 — 회귀 아님

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1/2 - 스타일/CLAUDE.md 준수] Task2 new_string 에 누락된 메서드 구분 공백줄 1개 추가**
- **Found during:** Task 2 편집 직후 검증(wc -l 1770, 기대 1771 불일치 발견)
- **Issue:** plan 의 Task2 new_string 코드 블록이 "45줄"로 명시되어 있었으나 실제 텍스트는 44줄이었다 — `RunDatumSingleImageDetection` 닫는 중괄호(`}`) 바로 다음 줄에 있어야 할 메서드 구분 공백줄이 new_string 에 포함되지 않아, 그대로 적용 시 새 헬퍼가 다음 메서드(`//260818 hbk [초보자용] 실제로 카메라로...`)의 주석과 공백줄 없이 바로 붙는 형태가 됨. 이는 Task1 헬퍼(RunDatumDualImageDetection, 뒤에 공백줄 있음)와 스타일이 불일치하고, CLAUDE.md Code Style "Single blank line between methods" 규칙에도 위배
- **Fix:** `RunDatumSingleImageDetection` 의 닫는 `}` 와 다음 메서드 주석 사이에 공백줄 1개 추가
- **Files modified:** WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
- **Verification:** 추가 후 `wc -l` = 1771(plan 목표값과 정확히 일치), 중괄호 밸런스 0, 빌드 error0/warning12 재확인
- **Committed in:** `ee6ac1e` (Task 2 커밋에 포함)

**2. [검증 스크립트 해석 조정 — 코드 변경 아님] Task2 전역 `ref int nDatumOk, ref int nDatumFail` 카운트 정정**
- **Found during:** Task 2 정적 검증 (verify 섹션)
- **Issue:** plan 이 전역 카운트 **2** 를 기대했으나 실측 **3**(이미 존재하던 `ProcessOneDatum`(L239)의 무관한 동일 패턴 파라미터 목록 포함)
- **Fix:** 코드는 변경하지 않음(plan 대로 편집). `git show 6a771fa`(이번 plan 착수 전 베이스라인)로 `ProcessOneDatum` 이 이미 존재했음을 재확인해 회귀가 아님을 검증
- **Files modified:** 없음(검증 해석만 조정)
- **Verification:** `git show 6a771fa:WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs | grep -n 'ref int nDatumOk, ref int nDatumFail'` → `ProcessOneDatum`(L239)+`ProcessDatumSingleImage`(L333) 2건 확인, 이번 편집은 `RunDatumSingleImageDetection` 1건만 추가하여 정상적으로 3건이 됨
- **Committed in:** 해당 없음(코드 변경 없는 검증 방법 조정)

---

**Total deviations:** 2건(스타일 공백줄 1개 추가 1건 + 검증 스크립트 해석 조정 1건, 코드 로직 변경 0건)
**Impact on plan:** 판정 로직/제어흐름 영향 없음. 공백줄 추가는 순수 스타일 보정(CLAUDE.md 준수)이며 plan 이 명시한 최종 목표 줄수(1771)와도 정확히 일치. 카운트 해석 조정은 코드 변경이 전혀 없는 검증 방법 보정.

## Issues Encountered

**Task2 new_string 자체의 1줄 누락(plan 저작 단계 오류):** plan 이 "45줄"로 명시한 new_string 코드 블록이 실제로는 44줄이었다(메서드 구분 공백줄 누락). Edit 도구 적용 직후 `wc -l` 이 1770(기대 1771)으로 나와 즉시 발견, CLAUDE.md 관례에 맞춰 공백줄 1개를 추가해 plan 목표값과 일치시켰다. old_string/new_string 의 실제 코드 내용(로직) 자체는 plan 이 제시한 그대로 정확히 매치했으므로 편집 정확도에는 영향 없었음.

## User Setup Required

None - 외부 서비스 설정 불필요. 정적 검증(카운트+wc -l+빌드+중괄호 밸런스)만으로 회귀 0 결론 — 순수 텍스트 추출/재배치라 판정 로직 접근 없음. plan 이 권고한 실기 UAT(Shot 1개 검사 후 [SEQ] 로그+DatumPhase 완료 요약 카운트 비교)는 선택사항이며 이번 세션에서는 미실행.

## Next Phase Readiness

- `Action_FAIMeasurement.cs` Bundle C(RunDatumDualImageDetection/RunDatumSingleImageDetection) 정리 완료, 후속 코드 작업 없음
- Blockers 없음

## Known Stubs

없음 - 순수 리팩토링(추출/치환)이며 신규 데이터 소스/바인딩/UI 변경 없음.

## Threat Flags

없음 - 신규 네트워크 엔드포인트·인증 경로·파일 접근·스키마 변경 없음. `RunDatumSingleImageDetection` 의 `ref nDatumOk/nDatumFail`(T-s05-01, mitigate)은 위 카운트 검증(4개 증가 지점 보존)으로 충족. `try/finally`(SafeDisposeImage/img.Dispose) 소유권이 원본 함수에 그대로 남아있음(T-s05-02, mitigate)을 grep 으로 확인.

## Self-Check: PASSED

파일 존재 확인:
```
FOUND: WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs
FOUND: .planning/quick/260819-s05-fai-refactor-bundle-c/260819-s05-SUMMARY.md
```

커밋 존재 확인:
```
FOUND: dcf8aae (Task 1)
FOUND: ee6ac1e (Task 2)
```

---
*Phase: quick-260819-s05*
*Completed: 2026-08-19*
