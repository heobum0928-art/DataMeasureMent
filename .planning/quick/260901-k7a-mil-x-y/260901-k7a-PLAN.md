---
phase: quick-260901-k7a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Device/Camera/VirtualCamera.cs
  - WPF_Example/Device/Camera/Mil/MilCamera.cs
  - WPF_Example/Device/DeviceHandler.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: false
requirements: [MIRROR-LIVE-01, MIRROR-LIVE-02, MIRROR-LIVE-03]

must_haves:
  truths:
    - "검사 트리에서 Datum 노드를 선택하면 MIL 라이브 화면이 그 Datum 의 MirrorX/MirrorY 대로 반전되어 보인다"
    - "Shot(Action) 노드를 선택하면 소유 Datum 의 미러가 라이브에 그대로 반영된다(단발 grab 과 동일 규칙)"
    - "미러와 무관한 노드(Sequence/FAI/Measurement/레시피 루트)를 선택하면 라이브가 무미러 기본으로 되돌아온다"
    - "장치 창(DeviceSelector)만 열고 트리 선택을 하지 않으면 라이브는 기존과 동일한 무미러 영상이다"
    - "Hik/Basler 카메라 동작과 단발 grab / 검사 사이클 / OfflineInspectMode 결과는 변하지 않는다"
  artifacts:
    - path: "WPF_Example/Device/Camera/VirtualCamera.cs"
      provides: "SetLiveGrabRole(string) 가상 no-op 진입점"
      contains: "SetLiveGrabRole"
    - path: "WPF_Example/Device/Camera/Mil/MilCamera.cs"
      provides: "라이브 역할 DeviceInfo 참조 교체 + LiveLoop 반영"
      contains: "SetLiveGrabRole"
    - path: "WPF_Example/Device/DeviceHandler.cs"
      provides: "이름으로 카메라 조회 후 라이브 역할 지정하는 파사드"
      contains: "ApplyLiveGrabRole"
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "트리 선택 → 라이브 미러 역할 지정"
      contains: "ApplyLiveMirrorForNode"
  key_links:
    - from: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      to: "DeviceHandler.ApplyLiveGrabRole"
      via: "InspectionList_SelectionChanged 내부 Dispatcher 블록"
      pattern: "ApplyLiveGrabRole"
    - from: "WPF_Example/Device/Camera/Mil/MilCamera.cs"
      to: "GrabFromBuffer(roleInfo)"
      via: "LiveLoop 이 _liveRoleInfo 참조 사용"
      pattern: "_liveRoleInfo"
---

<objective>
MIL(CXP) 라이브 미리보기가 검사 트리에서 현재 선택된 Datum/Shot 노드의 미러 X/Y 설정을 자동으로 따라가게 한다.

Purpose: 단발 grab 은 이미 미러가 적용되는데(quick-260813-jnh 의 grab 역할 시스템) 라이브만 무미러라, 티칭 중 화면과 실제 검사 이미지 방향이 달라 사용자가 혼동한다.
Output: MilCamera 라이브 역할 지정 API + 트리 선택 연동. 단발 grab / 검사 사이클 / 오프라인 검사 경로는 무변경.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

@WPF_Example/Device/Camera/Mil/MilCamera.cs
@WPF_Example/Device/Camera/VirtualCamera.cs
@WPF_Example/Device/DeviceHandler.cs
@WPF_Example/Custom/Device/DeviceHandler.cs
@WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
@WPF_Example/UI/ContentItem/MainView.xaml.cs

<interfaces>
확인된 기존 계약(코드 재탐색 불필요):

VirtualCamera.cs
  public virtual HImage GrabHalconImage(string requestIdentifier)   // :460 — 기본 구현은 인자 무시하고 GrabHalconImage() 호출
  public virtual bool StartStream()                                 // :464
  public virtual void StopStream()                                  // :473
  → 관례 근거: "역할 식별자" 계열은 이미 VirtualCamera 에 가상 메서드 + 기본 no-op/무시, MilCamera 만 override.
     따라서 이번에도 MilCamera 캐스팅이 아니라 VirtualCamera 가상 메서드로 추가한다.

MilCamera.cs
  private Dictionary<string, DeviceInfo> _roleInfoMap                // :20
  private DeviceInfo ResolveRoleInfo(string requestIdentifier)       // :62 — 미등록/ null 이면 Info 반환
  private HImage GrabFromBuffer(DeviceInfo roleInfo)                 // :305 (#if !SIMUL_MODE) — 매 grab 직전 M_GRAB_DIRECTION_X/Y 재적용
  private void LiveLoop()                                            // :496 — 현재 GrabFromBuffer(Info) 고정

DeviceHandler.cs (framework)
  public VirtualCamera this[string deviceName]                       // 이름으로 카메라 조회(ApplyProperty :332 선례)
  public HImage GrabHalconImage(ICameraParam param, string requestIdentifier)  // :345

Custom/DeviceHandler.cs
  public static string BuildGrabRoleIdentifier(string szBaseDeviceName, bool bMirrorX, bool bMirrorY)  // :136 — 무미러면 base 이름 그대로

DatumConfig.cs
  public bool MirrorX { get; set; }   // :239
  public bool MirrorY { get; set; }   // :253

InspectionSequence.cs
  public void ResolveShotGrabMirror(ShotConfig shot, out bool bMirrorX, out bool bMirrorY)   // :2282

MainView.xaml.cs
  private static string ResolveGrabRoleIdentifier(ICameraParam param, DatumConfig datum)     // :1220 — 산출 규칙 원본(이 파일은 무변경)

InspectionListView.xaml.cs
  private void InspectionList_SelectionChanged(object sender, SelectionChangedEventArgs e)   // :850
    → 내부 Dispatcher.BeginInvoke(Background) 람다(:881~) 안에서
      NodeViewModel item = list.SelectedItem as NodeViewModel;
      object itemParam = item.Param;
      이후 item.NodeType(ENodeType.Datum / Action / FAI / Measurement / Sequence) 분기가 이어진다.
</interfaces>

<hard_rules>
CLAUDE.md 가독성 하드룰(위반 = 회귀):
- 삼항 `?:` 금지, `??`/`??=` 금지, `?.`/`?[]` 금지, switch 식(`=>`) 금지
- C# 7.2 문법만
- 헝가리언 접두사: b(bool) n(int) sz(string) d(double)
- 한 줄 분기도 중괄호 생략 금지
- 날짜 주석(`//YYMMDD hbk`) 신규 금지 — 비자명한 "왜" 만
- 수정하는 파일의 기존 브레이스 스타일 유지(MilCamera/DeviceHandler/InspectionListView 전부 K&R)

절대 무변경:
- 단발 grab 역할 경로(MainView.ResolveGrabRoleIdentifier, DeviceHandler.GrabHalconImage)
- Action_FAIMeasurement 등 검사 사이클
- OfflineInspectMode 방향 정합 코드
- HikCamera.cs / BaslerCamera.cs
- **WPF_Example/DatumMeasurement.csproj — 사용자의 미커밋 실험(SIMUL_MODE 제거)이 워킹트리에 있다. 절대 열지도, 수정하지도, 스테이징하지도 말 것.**
</hard_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MilCamera 라이브 grab 역할 지정 진입점 추가</name>
  <files>WPF_Example/Device/Camera/VirtualCamera.cs, WPF_Example/Device/Camera/Mil/MilCamera.cs, WPF_Example/Device/DeviceHandler.cs</files>
  <action>
1) VirtualCamera.cs — GrabHalconImage(string requestIdentifier) 가상 메서드(:460) 바로 아래에
   `public virtual void SetLiveGrabRole(string szRoleIdentifier)` 를 추가하고 본문은 비워 둔다(no-op).
   주석으로 "라이브 grab 방향을 역할별로 바꿀 수 있는 카메라만 override(현재 MIL). Hik/Basler 는 no-op 이라 회귀 0" 이라는 이유만 남긴다.
   MilCamera 캐스팅이 아니라 가상 메서드를 쓰는 근거는 기존 GrabHalconImage(string) 관례와 동일(위 <interfaces> 참조).

2) MilCamera.cs — _roleInfoMap 선언(:20) 근처에 `private DeviceInfo _liveRoleInfo = null;` 필드를 추가한다.
   이 필드는 `#if !SIMUL_MODE` 밖(양쪽 빌드 공통)에 둔다. SIMUL_MODE 에서는 LiveLoop 자체가 없어 사용되지 않을 뿐이다.

   `public override void SetLiveGrabRole(string szRoleIdentifier)` 를 추가한다:
     - szRoleIdentifier 가 null 또는 빈 문자열이면 `_liveRoleInfo = null;` 로 되돌린다(기본 무미러 복귀).
     - 그 외에는 기존 `ResolveRoleInfo(szRoleIdentifier)` 결과를 `_liveRoleInfo` 에 대입한다.
       (ResolveRoleInfo 는 미등록 식별자면 Info 를 돌려주므로 자동으로 안전한 폴백이 된다 — 새 조회 로직을 만들지 말고 기존 것을 재사용.)
     - 반드시 남길 근거 주석: LiveLoop 은 백그라운드 스레드, 이 메서드는 UI 스레드에서 호출된다.
       주고받는 값이 DeviceInfo "참조" 하나뿐이고 C# 에서 참조 대입/읽기는 원자적이라 찢어진 값이 보일 수 없다.
       최악의 경우 한 프레임 늦게 반영될 뿐이므로 락을 걸지 않는다(락을 걸면 grab 구간과 UI 가 얽혀 데드락 위험만 늘어남).

3) MilCamera.LiveLoop(:496) — `GrabFromBuffer(Info)` 를 다음으로 바꾼다:
     DeviceInfo liveInfo = _liveRoleInfo;      // 루프 1회당 1번만 읽어 프레임 도중 바뀌어도 일관되게 사용
     if (liveInfo == null) {
         liveInfo = Info;
     }
     HImage frame = GrabFromBuffer(liveInfo);
   (미지정 시 기존 Info 그대로 = 회귀 0.)

4) MilCamera.StopStream(:473) 끝부분에서 `_liveRoleInfo = null;` 로 초기화한다(라이브 재시작 시 이전 선택이 남지 않도록).

5) DeviceHandler.cs(framework) — ApplyProperty(:332) 인근에 파사드를 추가한다:
     public void ApplyLiveGrabRole(string szDeviceName, string szRoleIdentifier)
     - szDeviceName 이 비어 있으면 그냥 return.
     - `VirtualCamera cam = this[szDeviceName];` 로 조회하고 cam 이 null 이면 return.
       (조회는 항상 base 장치 이름으로 — GrabHalconImage(param, requestIdentifier):345 와 동일 규약.)
     - `cam.SetLiveGrabRole(szRoleIdentifier);`
  </action>
  <verify>
    <automated>cd /c/code/DataMeasurement &amp;&amp; for f in WPF_Example/Device/Camera/VirtualCamera.cs WPF_Example/Device/Camera/Mil/MilCamera.cs WPF_Example/Device/DeviceHandler.cs; do echo "$f"; grep -v '^\s*//' "$f" | grep -cE '\?[^?]*:' ; grep -v '^\s*//' "$f" | grep -cF '??'; grep -v '^\s*//' "$f" | grep -cF '?.'; done</automated>
    <automated>cd /c/code/DataMeasurement &amp;&amp; grep -n "SetLiveGrabRole\|_liveRoleInfo\|ApplyLiveGrabRole" WPF_Example/Device/Camera/VirtualCamera.cs WPF_Example/Device/Camera/Mil/MilCamera.cs WPF_Example/Device/DeviceHandler.cs</automated>
  </verify>
  <done>
  - VirtualCamera 에 가상 no-op SetLiveGrabRole 존재, Hik/Basler 파일 무변경(git status 로 확인)
  - MilCamera.LiveLoop 이 _liveRoleInfo(없으면 Info)를 사용
  - DeviceHandler.ApplyLiveGrabRole 파사드 존재
  - 위 grep 카운트 중 신규 라인에서 삼항/`??`/`?.` 도입 0 (기존 라인 카운트는 변화 없음)
  </done>
</task>

<task type="auto">
  <name>Task 2: 트리 선택 → 라이브 미러 역할 연동</name>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <action>
InspectionListView.xaml.cs 에 private 헬퍼 2개와 필드 1개를 추가하고, 선택 핸들러에서 호출한다.

1) 필드: `private string _szLiveRoleDeviceName = null;`
   — 직전에 라이브 역할을 지정했던 장치 이름. 다른 노드로 옮겼을 때 그 장치를 무미러로 되돌리기 위해 필요하다.

2) `private static string ResolveLiveRoleIdentifier(object itemParam, out string szDeviceName)`
   MainView.ResolveGrabRoleIdentifier(:1220) 와 **같은 규칙**을 쓰되, MainView 는 손대지 않고 여기 별도 구현한다.
     - szDeviceName = null 로 초기화.
     - itemParam 이 DatumConfig 이면(변수명 datumCfg): bMirrorX = datumCfg.MirrorX, bMirrorY = datumCfg.MirrorY.
       DatumConfig 는 ICameraParam 계열이므로 DeviceName 을 szDeviceName 에 넣는다.
       (DatumConfig 에 DeviceName 이 없다면 `itemParam as ICameraParam` 로 얻어 쓰고, 그것도 null 이면 null 반환.)
     - itemParam 이 ShotConfig 이면(변수명 shotCfg): szDeviceName = shotCfg.DeviceName,
       `SystemHandler.Handle.Sequences[shotCfg.SequenceName] as InspectionSequence` 로 시퀀스를 얻고 null 이 아니면
       `mirrorSeq.ResolveShotGrabMirror(shotCfg, out bMirrorX, out bMirrorY)` 호출.
     - 그 외(FAI/Measurement/Sequence/레시피 루트/ null)면 szDeviceName 은 null 인 채 null 을 반환한다.
     - 마지막에 `return DeviceHandler.BuildGrabRoleIdentifier(szDeviceName, bMirrorX, bMirrorY);`
       (무미러면 base 이름을 그대로 돌려주므로 라이브도 기본 Info 와 동일한 방향이 된다.)
     - `?.` / `??` / 삼항 금지 — 전부 명시적 null 분기 + 중괄호.

3) `private void ApplyLiveMirrorForNode(object itemParam)`
     - szNewDevice / szRoleIdentifier 를 ResolveLiveRoleIdentifier 로 계산.
     - 직전 장치(_szLiveRoleDeviceName)가 비어있지 않고 새 장치와 다르면, 먼저
       `SystemHandler.Handle.Devices.ApplyLiveGrabRole(_szLiveRoleDeviceName, null)` 로 되돌린다.
       (DeviceHandler 접근 경로는 이 파일에서 이미 쓰는 방식(pDev / SystemHandler.Handle 필드)을 그대로 따를 것 —
        새 접근 경로를 만들지 말고 파일 내 기존 선례를 찾아 재사용한다.)
     - szNewDevice 가 비어 있으면 `_szLiveRoleDeviceName = null;` 로 두고 종료(미러 무관 노드 = 무미러 복귀).
     - 아니면 `ApplyLiveGrabRole(szNewDevice, szRoleIdentifier)` 호출 후 `_szLiveRoleDeviceName = szNewDevice;`.
     - 전체를 try/catch (Exception e) 로 감싸고 실패 시 Logging.PrintLog((int)ELogType.Camera, "[WARN] ...") 만 남긴다.
       라이브 표시 편의 기능이므로 어떤 경우에도 트리 선택 처리를 깨뜨리면 안 된다.

4) 호출 지점: InspectionList_SelectionChanged(:850) 내부 `Dispatcher.BeginInvoke(...Background...)` 람다 안,
   `object itemParam = item.Param;` 바로 다음 줄에서 `ApplyLiveMirrorForNode(itemParam);` 를 1줄 호출한다.
   (기존 NodeType 분기는 전혀 건드리지 않는다. Dispatcher 지연 블록 안이라 트리 컨테이너 재진입 이슈도 기존 규약을 그대로 따른다.)

주의: 장치 창(DeviceSelector)만 쓰는 시나리오는 이 핸들러가 호출되지 않으므로 _liveRoleInfo 가 계속 null → 기존 무미러 라이브 그대로(회귀 0).
DeviceSelector.xaml.cs 는 수정하지 않는다.
  </action>
  <verify>
    <automated>cd /c/code/DataMeasurement &amp;&amp; grep -n "ApplyLiveMirrorForNode\|ResolveLiveRoleIdentifier\|_szLiveRoleDeviceName" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</automated>
    <automated>cd /c/code/DataMeasurement &amp;&amp; git status --porcelain | grep -E "DatumMeasurement.csproj|HikCamera.cs|BaslerCamera.cs|MainView.xaml.cs|DeviceSelector.xaml.cs|Action_FAIMeasurement.cs" ; test $? -ne 0</automated>
    <automated>cd /c/code/DataMeasurement &amp;&amp; "/c/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:m 2>&amp;1 | grep -E "error CS" | head -20 ; echo "CS_ERRORS_ABOVE_MUST_BE_EMPTY"</automated>
  </verify>
  <done>
  - 트리 선택 핸들러가 ApplyLiveMirrorForNode 를 1줄 호출
  - git status 에 DatumMeasurement.csproj 외 금지 파일 변경 없음, csproj 는 사용자 미커밋 실험 그대로 유지(내가 만든 변경 아님, 스테이징 금지)
  - Debug|x64 빌드에서 `error CS` 0건 (MSB3027/MSB3021 bin 복사 실패는 사용자가 VS 로 앱을 띄워둔 경우이며 통과로 간주. 프로세스 강제 종료 절대 금지)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  MIL 라이브 미리보기가 검사 트리에서 선택한 항목의 좌우/상하 뒤집기 설정을 자동으로 따라가도록 만들었습니다.
  </what-built>
  <how-to-verify>
  1) 프로그램을 실행하고 카메라(장치) 창을 열어 CXP 카메라 라이브 영상을 켭니다.
  2) 검사 목록 트리에서 좌우 뒤집기(MirrorX)가 켜져 있는 Datum 항목을 클릭합니다.
     → 라이브 영상이 좌우로 뒤집혀 보여야 합니다.
  3) 그 Datum 을 쓰는 Shot(촬영) 항목을 클릭합니다.
     → 같은 방향으로 보여야 합니다(Datum 과 동일).
  4) 시퀀스 이름 같은 위쪽 항목(뒤집기와 상관없는 항목)을 클릭합니다.
     → 라이브가 원래(안 뒤집힌) 영상으로 돌아와야 합니다.
  5) 같은 항목에서 'Grab' 버튼으로 한 장 찍어 봅니다.
     → 찍힌 사진 방향과 라이브 화면 방향이 같아야 합니다(예전에는 달랐던 부분).
  6) 트리를 아예 건드리지 않고 장치 창만 켠 상태의 라이브는 예전과 똑같이 안 뒤집힌 영상이어야 합니다.
  </how-to-verify>
  <resume-signal>"승인" 이라고 적거나, 이상한 점을 적어 주세요</resume-signal>
</task>

</tasks>

<verification>
- Hik/Basler/MainView/DeviceSelector/Action_FAIMeasurement 파일 diff 0
- WPF_Example/DatumMeasurement.csproj 를 이 작업에서 열거나 수정하거나 스테이징하지 않음
- Debug|x64 빌드 error CS 0건
- 트리 선택 없이 라이브만 쓰는 경로에서 _liveRoleInfo 는 계속 null → 기존 동작 유지
</verification>

<success_criteria>
- 트리에서 Datum/Shot 선택 시 MIL 라이브 방향이 그 노드의 미러 설정과 일치
- 미러 무관 노드 선택 시 무미러로 복귀
- 단발 grab / 검사 사이클 / 오프라인 검사 결과 무변화
- CLAUDE.md 가독성 하드룰 위반 신규 0건
</success_criteria>

<output>
Create `.planning/quick/260901-k7a-mil-x-y/260901-k7a-SUMMARY.md` when done
</output>
