---
phase: quick-260901-mc1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/UI/TrayVisionView.xaml
  - WPF_Example/Custom/UI/TrayVisionView.xaml.cs
  - WPF_Example/Custom/UI/BottomVisionView.xaml
  - WPF_Example/Custom/UI/BottomVisionView.xaml.cs
  - WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs
  - WPF_Example/Custom/SystemHandler.cs
autonomous: false
requirements: [ALIGNUI-01, ALIGNUI-02, ALIGNUI-03]

must_haves:
  truths:
    - "Tray/Bottom 두 화면 모두에서 [이미지 저장] 버튼으로 지금 화면에 보이는 영상을 bmp 파일로 저장할 수 있다"
    - "이미지가 없을 때 [이미지 저장] 을 눌러도 크래시 없이 상태 라벨 안내만 나온다"
    - "실HW 빌드(SIMUL_MODE off)에서 폴더로 불러온 영상으로 Cal 모델 티칭과 스텝 추가가 동작한다"
    - "폴더 로더 영상이 없으면 캘 티칭/스텝이 종전대로 라이브 grab 영상을 쓴다"
    - "오프라인 소스로 스텝을 추가하면 성공 시 다음 이미지로 자동으로 넘어간다"
    - "피커센터 계산 결과에 피팅 잔차(RMS·최대)가 px 와 µm 양쪽 단위로 표시된다"
    - "잔차가 커도 계산은 성공으로 남는다 — 기존 성공/실패 계약이 바뀌지 않는다"
    - "SIMUL 빌드에서 기존 캘리브 흐름의 동작이 그대로 유지된다"
  artifacts:
    - path: "WPF_Example/Custom/UI/TrayVisionView.xaml"
      provides: "이미지 저장 버튼 (btn_nextImage 뒤, 같은 WrapPanel)"
      contains: "btn_saveImage"
    - path: "WPF_Example/Custom/UI/BottomVisionView.xaml"
      provides: "이미지 저장 버튼 (btn_nextImage 뒤, 같은 WrapPanel)"
      contains: "btn_saveImage"
    - path: "WPF_Example/Custom/UI/TrayVisionView.xaml.cs"
      provides: "SaveImageButton_Click — VistaSaveFileDialog + bmp 무압축 저장"
      contains: "SaveImageButton_Click"
    - path: "WPF_Example/Custom/UI/BottomVisionView.xaml.cs"
      provides: "SaveImageButton_Click + TryResolveCalSourceImage(런타임 오프라인/라이브 소스 판정)"
      contains: "TryResolveCalSourceImage"
    - path: "WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs"
      provides: "피팅 잔차 RMS/최대 산출 + 6-out 오버로드"
      contains: "ComputeFitResiduals"
  key_links:
    - from: "WPF_Example/Custom/UI/BottomVisionView.xaml.cs"
      to: "TryResolveCalSourceImage"
      via: "CalTeachModelButton_Click / CalAddStepButton_Click 이 공통 소스 해석기를 호출"
      pattern: "bOwnsImage"
    - from: "WPF_Example/Custom/UI/BottomVisionView.xaml.cs"
      to: "PickerCenterCalibrationService.TryComputePickerCenter(6-out)"
      via: "CalComputeButton_Click 이 잔차를 받아 lbl_calStatus 에 표시"
      pattern: "dResidualRmsPx"
    - from: "WPF_Example/Custom/SystemHandler.cs"
      to: "PickerCenterCalibrationService.TryComputePickerCenter(6-out)"
      via: "TCP $ALIGN_CALIB END 로그에 잔차 병기"
      pattern: "dResidualRmsPx"
---

<objective>
실HW 빌드에서 "정렬 영상 저장 → 폴더로 불러오기 → 그 영상으로 피커 캘리브 → 피팅 품질을 숫자로 확인" 이 한 흐름으로 돌아가게 한다.

Purpose: 지금은 (a) 화면 영상을 저장할 방법이 없고, (b) 오프라인 캘리브가 `#if SIMUL_MODE` 컴파일 분기라 실HW 빌드에서 불가능하며, (c) 편심원 피팅 품질을 알 수단이 전혀 없다. 지그 Z 와 자재 Z 가 다르거나 회전축이 화면 가장자리에 있으면 누적 점들이 진짜 원에서 벗어나는데 현재는 그걸 감지할 지표가 하나도 없다.
Output: 두 뷰의 이미지 저장 버튼 + Bottom 캘리브 오프라인 소스 런타임 전환 + 피팅 잔차(RMS/최대) 표시. 판정 계약·TCP 프로토콜 무변경.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

@WPF_Example/Custom/UI/BottomVisionView.xaml.cs
@WPF_Example/Custom/UI/BottomVisionView.xaml
@WPF_Example/Custom/UI/TrayVisionView.xaml.cs
@WPF_Example/Custom/UI/TrayVisionView.xaml
@WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs
@WPF_Example/Custom/EthernetVision/EthernetAlignCamera.cs
@WPF_Example/UI/ContentItem/MainResultViewerControl.xaml.cs
@WPF_Example/Halcon/Algorithms/TeachDiagnostics.cs

<interfaces>
코드로 직접 확인한 계약이다. 라인 번호는 드리프트할 수 있으니 편집 직전에 grep 으로 재확인할 것. 그 외 사실은 재조사 불필요.

MainResultViewerControl.xaml.cs
  public HImage  CurrentImage      { get; private set; }   // :187 — 뷰어 소유. 외부에서 Dispose 금지
  public string  CurrentImagePath  { get; private set; }   // :189
  public void LoadImage(string imagePath)  // :203 — DisposeImage() 후 new HImage(path). CurrentImagePath = imagePath
  public void LoadImage(HImage image)      // :220 — DisposeImage() 후 내부 Clone. CurrentImagePath = null
  → 따라서 "화면 이미지가 파일에서 온 것인가" 는 CurrentImagePath 가 비어있지 않은지로 정확히 판별된다.
    Grab / Live 폴링(LiveTimer_Tick)은 HImage 오버로드를 쓰므로 CurrentImagePath 가 null 이 된다.

EthernetAlignCamera.cs (namespace ReringProject.Device — 두 뷰 모두 이미 using 함)
  public bool   IsOpen { get; }        // :60 — (_hikCamera != null) && _hikCamera.IsOpen
  public HImage Grab()                 // :133 — IsOpen 이 false 이거나 grab 실패면 LoadFallbackImage() 로 폴백
  public void   LoadSimulFolder(string folder)   // :32 — _simulImagePaths 등록
  private HImage LoadFallbackImage()   // :211 — _simulImagePaths 가 등록돼 있으면 그 폴더 이미지를 순차 반환,
                                       //   아니면 SystemSetting.AlignFallbackImagePath 단일 이미지
  → 핵심: LoadSimulFolder 로 등록한 폴더는 **폴백 경로에서만** 소비된다. 카메라가 정상이면 절대 안 쓰인다.

PickerCenterCalibrationService.cs
  private readonly List<double> _rows / _cols                              // :50-51 누적 지그 중심(절대 px)
  public bool TryAddStep(HImage, r1,c1,r2,c2, out foundRow, out foundCol, out dScore, out error)  // :219
  public bool TryComputePickerCenter(out row, out col, out radius, out error)                     // :303
  private const double MIN_RADIUS_PX = 1.0 / MAX_RADIUS_PX = 100000.0     // :29-30 — 유지
  public static double FindMinScore { get; }                              // :69

TryComputePickerCenter 호출부는 정확히 2곳이다(전수 grep 완료):
  WPF_Example/Custom/SystemHandler.cs:1122      // TCP $ALIGN_CALIB END
  WPF_Example/Custom/UI/BottomVisionView.xaml.cs:1571  // UI [계산] 버튼

TeachDiagnostics.cs (별칭 TeachDiag / ETeachGrade 는 두 뷰에 이미 using alias 로 선언돼 있음)
  public static ETeachGrade ClassifyScore(double score, double minScore)   // margin >= 0.15 → Good, 0 이상 → Weak, 음수 → Bad
  public static string      ToStatusLine(ETeachGrade grade, string message) // ● / ▲ / ✕ 기호 접두
  public static Brush       GradeBrush(ETeachGrade grade)                   // ← lbl_calStatus 에는 쓰지 않는다

SystemSetting
  SystemSetting.Handle.EthernetPixelResolution   // µm/px, 기본 8.652 (Custom/SystemSetting.cs:241)
  SystemSetting.Handle.ImageSavePath             // 기본 D:\Data\Image (Setting/SystemSetting.cs:70)

저장 선례 (그대로 따를 것)
  MainView.xaml.cs:1428-1435 — Directory.CreateDirectory(dir) 선행 → imageToSave.WriteImage("bmp", 0, savePath)
    → savePath 는 확장자(.bmp)를 포함한 전체 경로. PNG 압축이 127MP 원본에서 tact 병목이라 bmp 무압축 채택.
  TeachingStorageService.cs:134 — 동일 관용구(파일명에 .bmp 포함 + WriteImage("bmp", 0, path))
  Ookii.Dialogs.Wpf 5.0.1 에 VistaSaveFileDialog 존재 확인(dll 심볼 확인 완료).
    사용 가능 프로퍼티: Filter / FileName / InitialDirectory / DefaultExt / AddExtension / OverwritePrompt

Bottom 툴바 버튼 스타일 = {StaticResource BottomActionButtonStyle}  (BottomVisionView.xaml:13 에 정의)
Tray  툴바 버튼 스타일 = {StaticResource TrayActionButtonStyle}
  → 각 파일의 자기 스타일을 쓴다. 서로 복사 금지.

Bottom 캘리브 관련 현재 앵커(편집 전 재확인 필수)
  1429/1436/1449  CalTeachModelButton_Click 의 #if SIMUL_MODE / #else / #endif
  1457/1459       CalTeachModelButton_Click 의 #if !SIMUL_MODE img.Dispose(); #endif
  1497/1504/1517  CalAddStepButton_Click 의 #if SIMUL_MODE / #else / #endif
  1527/1529       CalAddStepButton_Click 의 #if !SIMUL_MODE img.Dispose(); #endif
  1540/1549       CalAddStepButton_Click 의 #if SIMUL_MODE 다음 이미지 자동 넘김 #endif
  1560            CalComputeButton_Click
  1581            저장 확인 MessageBox msg 조립
  1587 / 1590     저장 후 lbl_calStatus 대입 2곳
  1896/1901       OpenFolderButton_Click 의 #if SIMUL_MODE Camera.LoadSimulFolder(folder) #endif
  ※ 오케스트레이터 조사에는 1527 만 언급됐으나 **1457 에도 같은 `#if !SIMUL_MODE` Dispose 가 있다.** 둘 다 처리 대상이다.
</interfaces>

<hard_rules>
CLAUDE.md 가독성 하드룰 — 위반 = 회귀. 신규/수정 라인 전부에 적용.
- 삼항 조건 연산자 금지 → if / else
- null 병합 연산자, null 조건 연산자 금지 → 명시적 null 분기 + 중괄호
- C# 8.0 switch 식 금지, C# 7.2 문법만
- 한 줄 분기라도 중괄호 생략 금지
- 조건에 논리연산자 3개 이상 늘어놓지 말고 이름 있는 bool 로 선추출
- 헝가리언 접두사: b(bool) n(int) sz(string) d(double) hv(HTuple)
- 매직넘버 금지 → 이름 있는 const
- 날짜 주석(YYMMDD + 이니셜 형태) 신규 금지. 비자명한 "왜" 만 최소한으로
- HImage / HObject / HTuple 은 반드시 Dispose
- **금지 기호를 코드 주석에 그대로 적지 말 것** — 검증 grep 이 주석까지 세어 자기무효화된다
- 파일별 기존 brace 스타일 유지: 이 작업의 4개 대상 파일은 전부 K&R(여는 중괄호 같은 줄).
  단 BottomVisionView.xaml.cs 의 LoadSlotCoaxToUi / RefreshTeachStatus 등 일부 메서드는 Allman 이다 —
  **새로 쓰는 메서드는 그 파일의 지배적 스타일인 K&R 로**, 기존 메서드를 고칠 때는 그 메서드의 스타일에 맞춘다.

절대 무변경 / 금지:
- **WPF_Example/DatumMeasurement.csproj — 사용자의 미커밋 실험(Debug|x64 SIMUL_MODE 제거)이 워킹트리에 있다.
  열지도, 수정하지도, 스테이징하지도, 커밋하지도 말 것.**
- **신규 .cs 파일 생성 금지.** 이 프로젝트는 classic MSBuild(packages.config) 라 새 파일은 csproj 에
  `<Compile Include>` 등록이 필요한데 csproj 가 금지 대상이다. 공유 헬퍼 클래스를 새 파일로 빼지 말고,
  각 뷰 code-behind 안에 동일 구현을 둔다(이 저장소는 이미 Grab/Live/Stop/로더 핸들러를 두 뷰에 그대로 중복 보유 —
  기존 관용구다).
- TCP $ALIGN_CALIB 프로토콜(IsPass / StepNo / 응답 패킷 / SystemSetting.Save 시점) 무변경
- PickerCenterCalibrationService 의 성공/실패 계약 무변경(반경 범위 가드 유지, 잔차로 실패시키지 않음)
- MainView.xaml.cs / Action_FAIMeasurement.cs / EthernetAlignCamera.cs 무변경
- MVVM: 이 phase 는 리팩토링 목적이 아니므로 기존 code-behind 를 ViewModel 로 옮기지 않는다.
  이번에 손대는 지점만 최소 적용한다(CLAUDE.md 4항 단서 조항).
</hard_rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tray/Bottom 두 뷰에 [이미지 저장] 버튼 추가</name>
  <files>WPF_Example/Custom/UI/TrayVisionView.xaml, WPF_Example/Custom/UI/TrayVisionView.xaml.cs, WPF_Example/Custom/UI/BottomVisionView.xaml, WPF_Example/Custom/UI/BottomVisionView.xaml.cs</files>
  <action>
두 뷰에 **완전히 동일하게 동작하는** 이미지 저장 기능을 넣는다. 한쪽만 되면 실패다.

1) XAML — 두 파일 모두 `btn_nextImage` **바로 뒤**, 같은 `WrapPanel` 안에 버튼 1개를 추가한다.
   x:Name 은 두 파일 모두 `btn_saveImage`, Content 는 `이미지 저장`, Click 은 `SaveImageButton_Click`.
   Style 은 각 파일이 이미 쓰는 것을 그대로: TrayVisionView.xaml 은 TrayActionButtonStyle,
   BottomVisionView.xaml 은 BottomActionButtonStyle. 서로 복사하지 말 것.

2) code-behind — 두 파일 모두 오프라인 로더 핸들러 영역(OpenFolderButton_Click / PrevImageButton_Click /
   NextImageButton_Click 이 모여 있는 구간) 끝에 `SaveImageButton_Click` 을 추가한다.
   구현은 뷰 이름 접두어(Tray 는 TrayAlign, Bottom 은 BottomAlign)만 다르고 나머지는 문자 단위로 동일해야 한다.

   필요한 이름 있는 const / static 필드(각 파일에 동일하게 선언, 로더 상태 필드 근처):
     - `SAVE_IMAGE_PREFIX` : Tray 는 "TrayAlign", Bottom 은 "BottomAlign"
     - `SAVE_IMAGE_SUBFOLDER` : "AlignCapture"
     - `SAVE_IMAGE_TIMESTAMP_FORMAT` : "yyyyMMdd_HHmmss"
     - `SAVE_IMAGE_EXTENSION` : ".bmp"
     - `SAVE_IMAGE_FORMAT` : "bmp"
     - `private static string _lastSaveFolder = null;`  (기존 `_lastImageFolder` 와 같은 static 관용구 — 탭 전환에도 유지)

   동작 순서:
   a. 가드 — `_viewer` 가 null 인지, `_viewer.CurrentImage` 가 null 인지를 **이름 있는 bool 로 각각 뽑아**
      중첩 null 분기로 검사한다. 이미지가 없으면 `lbl_loaderStatus.Text` 에
      "저장할 이미지가 없습니다 — Grab 또는 [폴더 열기] 로 영상을 먼저 띄우세요" 를 넣고 return.
      절대 예외를 던지지 않는다.
   b. 기본 저장 폴더 결정 — `_lastSaveFolder` 가 비어 있지 않으면 그것을 쓰고,
      비어 있으면 `Path.Combine(SystemSetting.Handle.ImageSavePath, SAVE_IMAGE_SUBFOLDER)` 를 쓴다.
      `SystemSetting.Handle.ImageSavePath` 자체가 비어 있으면 폴더 지정을 생략한다(다이얼로그 기본 위치에 맡김).
      폴더가 없으면 `Directory.CreateDirectory` 로 만든다(선례: MainView 저장 경로 처리).
      **`AlignVerifySavePath` 를 쓰지 말 것** — AlignVerifyRetention 이 그 계열 경로를 보관일수 기준으로
      자동 삭제하므로 운영자가 남긴 캘 원본이 조용히 사라진다. ImageSavePath 하위는 어떤 정리 작업도 건드리지 않는다
      (ELogType.Image 는 Logging.SetLog 로 등록되지 않아 로그 로테이션 대상이 아님을 확인).
   c. `Ookii.Dialogs.Wpf.VistaSaveFileDialog` 를 띄운다.
      Filter 는 "BMP 이미지|*.bmp", DefaultExt 는 "bmp", AddExtension 은 true, OverwritePrompt 는 true,
      InitialDirectory 는 b 에서 정한 폴더(있을 때만),
      FileName 은 `SAVE_IMAGE_PREFIX + "_" + DateTime.Now.ToString(SAVE_IMAGE_TIMESTAMP_FORMAT) + SAVE_IMAGE_EXTENSION`.
      `ShowDialog()` 는 nullable bool 을 돌려주므로 **기존 파일의 관용구를 그대로** 쓴다:
      결과를 `bool? bResult` 로 받고 `if (bResult == true)` 로 비교한다(`.Value` 나 null 병합 사용 금지 —
      하드룰 위반이자 null 일 때 예외가 난다). true 가 아니면 아무것도 하지 않고 return(상태 라벨도 건드리지 않음).
   d. 저장 — 선택된 경로의 디렉터리가 없으면 `Directory.CreateDirectory` 로 만든 뒤
      `_viewer.CurrentImage.WriteImage(SAVE_IMAGE_FORMAT, 0, szPath)`.
      전체를 try/catch (Exception ex) 로 감싸고 실패 시 `lbl_loaderStatus.Text` 에 "저장 실패: " + ex.Message.
      성공 시 `_lastSaveFolder` 를 방금 저장한 폴더로 갱신하고
      `lbl_loaderStatus.Text` 에 "저장 완료: " + 파일명(Path.GetFileName) 을 넣는다.
   e. **`_viewer.CurrentImage` 는 뷰어 소유다. 이 핸들러에서 Dispose 하지 말 것.** 복사본도 만들지 않는다.
      (복사본을 떠서 백그라운드 저장으로 돌리고 싶어질 수 있는데, CurrentImage 는 다른 UI 조작으로
       언제든 교체·해제되므로 UI 스레드에서 동기 저장하는 편이 안전하다. 127MP 원본에서 수백 ms 정도
       화면이 멎는 것은 수동 버튼이라 허용 범위다.)

3) using 추가는 필요 없다 — 두 파일 모두 System / System.IO / ReringProject.Setting 을 이미 using 하고 있다.
   Ookii 는 기존 코드처럼 정규화 이름(Ookii.Dialogs.Wpf.VistaSaveFileDialog)으로 쓴다.
  </action>
  <verify>
    <automated>cd /c/code/DataMeasurement && grep -n "btn_saveImage" WPF_Example/Custom/UI/TrayVisionView.xaml WPF_Example/Custom/UI/BottomVisionView.xaml</automated>
    <automated>cd /c/code/DataMeasurement && grep -c "TrayActionButtonStyle" WPF_Example/Custom/UI/BottomVisionView.xaml; grep -c "BottomActionButtonStyle" WPF_Example/Custom/UI/TrayVisionView.xaml</automated>
    <automated>cd /c/code/DataMeasurement && grep -n "SaveImageButton_Click\|_lastSaveFolder\|VistaSaveFileDialog\|SAVE_IMAGE_SUBFOLDER" WPF_Example/Custom/UI/TrayVisionView.xaml.cs WPF_Example/Custom/UI/BottomVisionView.xaml.cs</automated>
    <automated>cd /c/code/DataMeasurement && grep -c "AlignVerifySavePath" WPF_Example/Custom/UI/TrayVisionView.xaml.cs WPF_Example/Custom/UI/BottomVisionView.xaml.cs</automated>
  </verify>
  <done>
  - 두 XAML 모두 btn_nextImage 다음에 btn_saveImage 존재, 각자 자기 파일의 Style 리소스 사용
    (위 2번째 verify 의 교차 스타일 카운트가 양쪽 다 0)
  - 두 code-behind 모두 SaveImageButton_Click / _lastSaveFolder / VistaSaveFileDialog 보유
  - AlignVerifySavePath 참조 0건 (두 파일 모두)
  - 이미지 없음 상태에서 눌러도 return 만 하는 가드 존재
  </done>
</task>

<task type="auto">
  <name>Task 2: Bottom 피커 캘리브 소스를 컴파일 분기에서 런타임 판단으로 전환</name>
  <files>WPF_Example/Custom/UI/BottomVisionView.xaml.cs</files>
  <action>
`CalTeachModelButton_Click` 과 `CalAddStepButton_Click` 의 `#if SIMUL_MODE` 이미지 소스 분기를 없애고
**런타임 판단** 으로 바꾼다. 이 작업의 최대 리스크는 이미지 소유권이므로 그 처리를 먼저 확정한다.

1) 공통 소스 해석기 `private bool TryResolveCalSourceImage(out HImage img, out bool bOwnsImage)` 를
   캘리브 핸들러들 근처에 추가한다. 반환 규칙:
   - `img` 를 null, `bOwnsImage` 를 false 로 초기화한다.
   - **오프라인 우선**: `_viewer` 가 null 이 아니고, `_viewer.CurrentImage` 가 null 이 아니고,
     `_viewer.CurrentImagePath` 가 비어있지 않으면 → 그 이미지가 파일에서 온 것이다.
     `img = _viewer.CurrentImage;` 로 넘기고 `bOwnsImage = false;` (뷰어 소유 — Dispose 금지) 후 true 반환.
     이 세 조건은 `bViewerHasFileImage` 같은 이름 있는 bool 로 중첩 null 분기를 써서 만든다.
     판별 근거: LoadImage(string) 만 CurrentImagePath 를 채우고 LoadImage(HImage)(Grab/Live 폴링)는 null 로 지운다.
     즉 "폴더 로더로 불러온 이미지가 지금 화면에 있는가" 와 정확히 일치한다. 별도 토글 설정이 필요 없다.
   - **라이브 폴백**: 위가 아니면 `EthernetVisionHandler.Handle.Camera` 를 지역 변수로 받고
     null 이 아닐 때만 `IsOpen` 을 읽어 `bCameraReady` 를 만든다.
     `bCameraReady` 가 false 면 `lbl_calStatus.Text` 에
     "이미지 없음 — [폴더 열기] 로 영상을 불러오거나 카메라 연결을 확인하세요" 를 넣고 false 반환.
     **`IsOpen` 을 조건으로 두는 이유(반드시 주석으로 남길 것)**: EthernetAlignCamera.Grab() 은 카메라가 안 열려
     있으면 AlignFallbackImagePath 의 정지 이미지를 조용히 돌려준다. 그걸 그대로 누적하면 운영자는 실패를
     인지하지 못한 채 엉뚱한 이미지로 캘 데이터를 쌓게 된다. 소스가 불확실하면 쌓지 않는 편이 옳다.
   - `bCameraReady` 가 true 면 `cam.Grab()` 호출. 결과가 null 이면 `lbl_calStatus.Text = "Grab 실패";` 후 false 반환.
     null 이 아니면 `_viewer` 가 null 이 아닐 때 `_viewer.LoadImage(grabbed)` 로 화면에 반영하고
     (뷰어가 내부 Clone 하므로 원본 소유권은 그대로 이쪽에 남는다),
     `img = grabbed; bOwnsImage = true;` 후 true 반환.

2) `CalTeachModelButton_Click` 재작성 — 기존 `_calRoiSet` / `PickerCal` null 가드는 그대로 두고,
   `#if SIMUL_MODE` ~ `#endif` 이미지 획득 블록과 그 아래 `#if !SIMUL_MODE img.Dispose(); #endif` 를 모두 제거한다.
   메서드 상단에 `HImage img = null;` 과 `bool bOwnsImage = false;` 를 선언하고,
   try 안에서 `TryResolveCalSourceImage(out img, out bOwnsImage)` 가 false 면 즉시 return(상태 라벨은 해석기가 세팅).
   이어서 기존대로 `TryTeachModel` 호출 → 성공/실패 라벨.
   `finally` 에서 `bOwnsImage` 가 true 이고 `img` 가 null 이 아닐 때만 try/catch 로 감싼 `img.Dispose()` 를 한다.
   **`bOwnsImage` 가 false 면 절대 Dispose 하지 않는다 — 뷰어 이미지를 조기 해제하면 화면이 죽는다.**

3) `CalAddStepButton_Click` 재작성 — 위와 동일한 골격.
   `#if SIMUL_MODE` 이미지 획득 블록, `#if !SIMUL_MODE img.Dispose(); #endif`,
   그리고 성공 처리 안의 `#if SIMUL_MODE` 자동 넘김 블록의 전처리기 지시자를 모두 제거한다.
   - 모델 자동 로드 가드(`HasModel` → `TryLoadModel`)는 위치·동작 그대로 유지.
   - `TryAddStep` 호출 인자·out 파라미터·성공 시 `TeachDiag.ClassifyScore` + `ToStatusLine` 표시는
     전부 현행 유지(색 대입 금지 규칙도 그대로).
   - 시각화 XLD 갱신(`GetVisualizationXld` → `SetAlignContourXld`)도 현행 유지.
   - **자동 넘김**: `bool bUsedOfflineImage = (bOwnsImage == false);` 를 만들고 이 값이 true 일 때만
     다음 이미지로 이동한다. 이동 가능 여부는 이름 있는 bool
     `bHasNextImage = (_loadedImageIndex >= 0) && (_loadedImageIndex < _loadedImagePaths.Count - 1)` 로 판단하고,
     true 면 인덱스를 1 증가시킨 뒤 `LoadCurrentLoaderImage()`, 아니면 `lbl_loaderStatus.Text = "마지막 이미지 도달";`.
   - **순서 함정**: `LoadCurrentLoaderImage()` 는 뷰어의 기존 CurrentImage 를 Dispose 한다.
     자동 넘김을 실행하기 **직전에 `img = null;` 로 지역 참조를 끊는다**(이 시점 이후 img 를 읽는 코드가
     생기더라도 dangling 참조가 될 수 없게). `bOwnsImage` 가 false 이므로 finally 는 아무것도 하지 않는다.
     이 두 줄의 이유를 주석으로 남긴다.

4) `OpenFolderButton_Click` 의 `#if SIMUL_MODE ... Camera.LoadSimulFolder(folder) ... #endif` 는
   **그대로 둔다(실HW 로 열지 않는다).** 근거를 코드 주석과 SUMMARY 양쪽에 남길 것:
   `LoadSimulFolder` 로 등록한 경로는 `EthernetAlignCamera.LoadFallbackImage()` 안에서만 소비되고,
   `LoadFallbackImage()` 는 카메라가 안 열렸거나 grab 이 실패했을 때만 도달한다. 실HW 에서 이걸 열어두면
   생산 중 카메라가 끊긴 순간 TCP `$ALIGN_CALIB` STEP 경로가 운영자가 예전에 열어둔 폴더 이미지를
   순차로 조용히 먹기 시작한다(등록을 해제하는 경로도 없다). 이번 작업의 오프라인 캘 흐름은
   `_viewer.CurrentImage` 를 직접 읽으므로 카메라 쪽 등록이 전혀 필요 없다. 따라서 열지 않는 것이 옳다.

5) SIMUL 빌드 회귀 확인 — SIMUL 에서는 카메라가 열리지 않으므로 `bCameraReady` 가 항상 false 다.
   폴더를 연 상태면 오프라인 분기(= 오늘과 동일), 안 연 상태면 안내 후 return(= 오늘과 동일한 차단 동작).
   문구만 실HW 도 포괄하도록 바뀐다 — 동작 변화는 없다.
  </action>
  <verify>
    <automated>cd /c/code/DataMeasurement && grep -c "SIMUL_MODE" WPF_Example/Custom/UI/BottomVisionView.xaml.cs</automated>
    <automated>cd /c/code/DataMeasurement && grep -n "SIMUL_MODE" WPF_Example/Custom/UI/BottomVisionView.xaml.cs</automated>
    <automated>cd /c/code/DataMeasurement && grep -n "TryResolveCalSourceImage\|bOwnsImage\|bUsedOfflineImage\|bHasNextImage\|CurrentImagePath" WPF_Example/Custom/UI/BottomVisionView.xaml.cs</automated>
    <automated>cd /c/code/DataMeasurement && grep -v '^\s*//' WPF_Example/Custom/UI/BottomVisionView.xaml.cs | grep -nE 'switch[^;]*=>'; echo "SWITCH_EXPR_MUST_BE_EMPTY"</automated>
  </verify>
  <done>
  - BottomVisionView.xaml.cs 의 SIMUL_MODE 출현이 **8건 → 3건**으로 줄었다.
    (작업 전 8건 = 298 / 449 / 1429 / 1457 / 1497 / 1527 / 1540 / 1896.
     제거 대상 5건 = 1429·1457 티칭, 1497·1527 스텝, 1540 자동넘김.
     의도적 잔존 3건 = GrabButton_Click, GrabEthernetCalibrationImage, OpenFolderButton_Click 의 LoadSimulFolder.)
    남은 3건이 정확히 그 세 곳인지 2번째 verify 출력으로 눈으로 확인한다.
  - TryResolveCalSourceImage 존재, bOwnsImage 로 소유권 분기, finally 에서 bOwnsImage 가 true 일 때만 Dispose
  - 자동 넘김이 bUsedOfflineImage 게이트 뒤에 있고, 그 직전에 img 를 null 로 끊는다
  - OpenFolderButton_Click 의 LoadSimulFolder 는 여전히 SIMUL 전용이며 근거 주석이 붙어 있다
  - switch 식 신규 도입 0건
  </done>
</task>

<task type="auto">
  <name>Task 3: 피커센터 편심원 피팅 잔차(RMS/최대) 산출 및 표시</name>
  <files>WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs, WPF_Example/Custom/UI/BottomVisionView.xaml.cs, WPF_Example/Custom/SystemHandler.cs</files>
  <action>
편심원 피팅 품질을 숫자로 낸다. 지표는 **누적 점이 피팅된 원에서 벗어난 정도의 잔차** 로 확정한다:
각 누적점의 중심거리에서 피팅 반경을 뺀 값이 그 점의 오차이고, 그 오차의 RMS 와 절대 최대값을 쓴다.
근거(SUMMARY 에도 남길 것): 현재 유일한 가드인 반경 범위(MIN/MAX_RADIUS_PX)는 점들이 원이 아니어도
언제나 원 하나는 피팅되므로 사실상 아무것도 거르지 못한다. 지그 Z 와 자재 Z 가 다르거나 회전축이 화면
가장자리에 있으면 점 분포가 진짜 원에서 벗어나는데, 그 이탈량을 직접 재는 것이 잔차다.
`fit_circle_contour_xld` 를 다시 부르지 않고 이미 가진 `_rows`/`_cols` 와 피팅 결과만으로 순수 C# 산술로
계산하므로 HALCON 호출이 늘지 않고 새로 Dispose 할 자원도 없다.

A) PickerCenterCalibrationService.cs
   1) private 헬퍼 추가 (private 헬퍼 영역):
      `private void ComputeFitResiduals(double dCenterRow, double dCenterCol, double dRadius, out double dRmsPx, out double dMaxPx)`
      - 두 out 을 0.0 으로 초기화. `_rows.Count` 가 0 이하면 그대로 return.
      - for 루프로 각 점의 행/열 차분 → `Math.Sqrt` 로 중심거리 → 반경을 뺀 오차 →
        제곱 누적(dSumSq)과 절대값 최대(dMaxPx) 갱신.
      - 마지막에 `dRmsPx = Math.Sqrt(dSumSq / nCount);`
      - 지역 변수는 헝가리언(dDr / dDc / dDist / dErr / dAbsErr / dSumSq / nCount)으로.
   2) 6-out 오버로드 신설:
      `public bool TryComputePickerCenter(out double row, out double col, out double radius, out double dResidualRmsPx, out double dResidualMaxPx, out string error)`
      - 본문은 **현재의 4-out 구현을 그대로 옮긴 것**이다. 새 out 2개는 진입 즉시 0.0 으로 초기화한다.
      - 반경 범위 가드를 통과한 **직후**, `SystemSetting.Handle.PickerCenter*` 대입 전에
        `ComputeFitResiduals(fittedRow, fittedCol, fittedRad, out dResidualRmsPx, out dResidualMaxPx);` 를 호출한다.
      - 기존 성공 로그 문자열 끝에 잔차 2개를 덧붙인다(포맷 인자 추가만, 로그 종류/레벨 무변경).
      - **잔차로 실패를 만들지 않는다.** 반환값, error 문구, SystemSetting 대입 시점, 시각화 XLD 추가 순서를
        하나도 바꾸지 않는다. 근거: 임계값을 정할 실측 산포 데이터가 아직 없다. 잘못 잡은 임계는 정상 캘을
        버리게 만든다. 지금 필요한 것은 판정이 아니라 운영자가 볼 수 있는 숫자다.
        (같은 판단의 선례: SystemSetting 의 AlignVerifyResidualLimitMm 기본값 0.0 = 미설정 = 판정 없음.)
      - 실패 경로(점 부족 / 피팅 실패 / 반경 비정상 / 예외)에서는 두 out 이 0.0 인 채로 남는다.
   3) 기존 4-out 시그니처는 **삭제하지 말고** 6-out 을 호출해 잔차를 버리는 얇은 래퍼로 남긴다.
      이렇게 하면 기존 호출부 2곳(전수 grep 완료)이 컴파일 깨짐 없이 유지된다.

B) BottomVisionView.xaml.cs — `CalComputeButton_Click`
   1) 6-out 오버로드로 호출을 바꾸고 `dRmsPx` / `dMaxPx` 를 받는다.
   2) 표시 문자열 헬퍼 `private string BuildFitQualityText(double dRmsPx, double dMaxPx)` 를
      `BuildPickerCenterText` 바로 아래에 추가한다.
      - `SystemSetting.Handle.EthernetPixelResolution` 는 µm/px 이므로 px 값에 곱하면 바로 µm 이다.
        (mm 가 필요하면 1000 으로 나눈다 — BuildPickerCenterText 의 UM_PER_MM 과 같은 규약.)
        잔차는 보통 수십 µm 수준이라 mm 로 찍으면 0.0xx 가 되어 읽기 나쁘므로 **µm 을 주 단위**로 하고
        px 를 괄호로 병기한다.
      - 반환 예: "피팅잔차 RMS 12.4µm(1.43px) · 최대 31.0µm(3.58px)"
      - `EthernetPixelResolution` 이 0 이하이면 µm 환산을 생략하고 px 만 담은 문구를 돌려준다(0 나눗셈/무의미 값 방어).
   3) 등급 기호는 기존 관용구 `TeachDiag.ClassifyScore` + `TeachDiag.ToStatusLine` 로 붙인다.
      잔차는 "작을수록 좋다" 라서 ClassifyScore(클수록 좋다)에 그대로 넣을 수 없으므로,
      **반경 대비 잔차 비율을 0~1 점수로 뒤집어(원형도) 넣는다**:
      `private static double ToCircularityScore(double dRmsPx, double dRadiusPx)` —
      반경이 0 이하면 0.0, 비율이 1.0 이상이면 0.0, 그 외에는 `1.0 - (dRmsPx / dRadiusPx)`.
      반경으로 정규화하는 이유(주석): 반경이 클수록 같은 절대 잔차가 덜 치명적이므로 비율이 맞는 척도다.
      이름 있는 const `FIT_SCORE_MIN = 0.80` 을 두고
      `TeachDiag.ClassifyScore(ToCircularityScore(dRmsPx, rad), FIT_SCORE_MIN)` 로 등급을 얻는다.
      (ClassifyScore 의 여유 임계 0.15 와 합쳐져 대략: 잔차가 반경의 5% 이하면 ●, 5~20% 면 ▲, 20% 초과면 ✕.
       현장 실측 전 1차 기준이며 표시 전용이라는 점을 주석으로 명시할 것.)
   4) 표시 배선 — `lbl_calStatus` 는 이 파일에서 36곳이 대입하므로 **색(Foreground)은 절대 칠하지 않고
      ToStatusLine 의 기호만** 쓴다(GradeBrush 호출 금지). 기존 문구를 지우지 말고 뒤에 덧붙인다:
      - 저장 확인 MessageBox 의 msg 에 잔차 줄을 한 줄 추가한다 — 운영자가 저장 여부를 결정하기 **전에**
        품질을 보는 것이 이 기능의 목적이다.
      - `dlgResult` 가 Yes 인 분기: `lbl_calStatus.Text = TeachDiag.ToStatusLine(fitGrade, "피커센터 저장 완료 · " + szFitQuality);`
      - else 분기: `lbl_calStatus.Text = TeachDiag.ToStatusLine(fitGrade, "저장 취소 (값은 런타임 유지, 재시작 시 초기화) · " + szFitQuality);`
      - `lbl_pickerCenter` 는 현행 `BuildPickerCenterText` 그대로 둔다(무변경).

C) WPF_Example/Custom/SystemHandler.cs — TCP $ALIGN_CALIB END 경로
   - 호출을 6-out 오버로드로 바꾸고, 기존 성공 Trace 로그 문자열 끝에 잔차 RMS/최대(px)를 덧붙인다.
   - **그 외 일체 무변경**: IsPass, StepNo=99, `SystemSetting.Handle.Save()` 시점, OnCalibEndViewer 콜백,
     실패 분기, NotifyAlignCalibError 전부 그대로. 자동 경로의 화면 노출은 이번 범위가 아니다.
   - 이 한 줄을 굳이 넣는 이유: 실제 생산 캘리브는 TCP 자동 경로로 돌기 때문에, 사후 분석에 필요한
     품질 수치가 로그에 남아야 한다.
  </action>
  <verify>
    <automated>cd /c/code/DataMeasurement && grep -n "ComputeFitResiduals\|dResidualRmsPx\|dResidualMaxPx" WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs</automated>
    <automated>cd /c/code/DataMeasurement && grep -n "MIN_RADIUS_PX\|MAX_RADIUS_PX" WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs</automated>
    <automated>cd /c/code/DataMeasurement && grep -n "BuildFitQualityText\|ToCircularityScore\|FIT_SCORE_MIN\|EthernetPixelResolution" WPF_Example/Custom/UI/BottomVisionView.xaml.cs</automated>
    <automated>cd /c/code/DataMeasurement && grep -n "GradeBrush" WPF_Example/Custom/UI/BottomVisionView.xaml.cs; echo "GRADEBRUSH_MUST_NOT_TOUCH_lbl_calStatus"</automated>
    <automated>cd /c/code/DataMeasurement && grep -n "TryComputePickerCenter" WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs WPF_Example/Custom/SystemHandler.cs WPF_Example/Custom/UI/BottomVisionView.xaml.cs</automated>
  </verify>
  <done>
  - 서비스에 ComputeFitResiduals + 6-out 오버로드 존재, 4-out 은 래퍼로 잔존
  - MIN_RADIUS_PX / MAX_RADIUS_PX 가드 코드 그대로 남아 있음(잔차로 실패시키는 분기 신설 0건)
  - CalComputeButton_Click 이 잔차를 받아 MessageBox 와 lbl_calStatus 양쪽에 표시
  - lbl_calStatus 에 Foreground 대입 신규 0건 (GradeBrush 결과가 lbl_calStatus 로 가지 않음)
  - SystemHandler 의 ALIGN_CALIB END 로그에 잔차 병기, 그 외 분기 무변경
  </done>
</task>

<task type="auto">
  <name>Task 4: 하드룰 검증 + Debug|x64 빌드</name>
  <files>WPF_Example/Custom/UI/TrayVisionView.xaml.cs, WPF_Example/Custom/UI/BottomVisionView.xaml.cs, WPF_Example/Custom/EthernetVision/PickerCenterCalibrationService.cs, WPF_Example/Custom/SystemHandler.cs</files>
  <action>
1) CLAUDE.md 하드룰 grep 을 수정 대상 4개 .cs 파일에 대해 돌린다(아래 verify). 주석 줄은 제외하고 센다.
   기존 라인에 이미 존재하는 위반(예: 두 뷰의 GrabEthernetCalibrationImage 와 LiveTimer_Tick 에 있는
   기존 null 조건 Dispose)은 이번 범위가 아니므로 **건드리지 않는다**. 확인 기준은 "이번 diff 로 새로 늘지 않았는가" 다.
   `git diff -U0` 로 추가된 줄만 뽑아 검사하면 정확하다.
2) 날짜 주석 신규 금지 확인 — 추가된 줄에 이니셜 붙은 날짜 주석 패턴이 없어야 한다.
3) Debug|x64 빌드. MSBuild 경로는 이 PC 기준
   "/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" (18.7.8) 이다.
   `error CS` 0건이어야 한다. bin 복사 실패(MSB3027/MSB3021)는 사용자가 앱을 띄워둔 경우이므로 통과로 간주하고
   **프로세스를 강제 종료하지 않는다.**
4) csproj 무변경 확인. `git status --porcelain` 에서 DatumMeasurement.csproj 는 사용자가 이미 갖고 있던
   그 한 줄 변경 그대로여야 하며, 절대 스테이징하지 않는다.
  </action>
  <verify>
    <automated>cd /c/code/DataMeasurement && ADDED=$(git diff -U0 -- WPF_Example/Custom/UI WPF_Example/Custom/EthernetVision WPF_Example/Custom/SystemHandler.cs | grep '^+' | grep -v '^+++' | grep -v '^\+\s*//'); echo "$ADDED" | wc -l; echo "--- ternary ---"; echo "$ADDED" | grep -cE '\?[^?]*:'; echo "--- null-coalesce ---"; echo "$ADDED" | grep -cF '??'; echo "--- null-conditional ---"; echo "$ADDED" | grep -cF '?.'; echo "--- switch-expr ---"; echo "$ADDED" | grep -cE 'switch.*=>'; echo "ALL_FOUR_MUST_BE_0"</automated>
    <automated>cd /c/code/DataMeasurement && git diff -U0 -- WPF_Example/Custom/UI WPF_Example/Custom/EthernetVision WPF_Example/Custom/SystemHandler.cs | grep '^+' | grep -v '^+++' | grep -cE '//[0-9]{6} '; echo "DATE_COMMENT_MUST_BE_0"</automated>
    <automated>cd /c/code/DataMeasurement && git diff --name-only | grep -c "DatumMeasurement.csproj"; git diff --cached --name-only | grep -c "DatumMeasurement.csproj"; echo "FIRST_IS_1_PREEXISTING_SECOND_MUST_BE_0"</automated>
    <automated>cd /c/code/DataMeasurement && "/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:m 2>&1 | grep -E "error CS" | head -30; echo "CS_ERRORS_ABOVE_MUST_BE_EMPTY"</automated>
  </verify>
  <done>
  - 추가된 코드 줄에서 삼항 / null 병합 / null 조건 / switch 식 카운트 전부 0
  - 추가된 코드 줄에 날짜 주석 0건
  - DatumMeasurement.csproj 는 사용자 기존 변경만 남아 있고 스테이징 0건
  - Debug|x64 빌드 error CS 0건
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: 실기 확인 — 이미지 저장 · 오프라인 캘리브 · 피팅 잔차 표시</name>
  <what-built>
  정렬 화면(Tray / Bottom) 두 곳에 [이미지 저장] 버튼을 넣었고, Bottom 화면의 피커 캘리브레이션이
  실제 장비 빌드에서도 폴더로 불러온 저장 영상으로 동작하게 바꿨습니다.
  그리고 피커센터 계산 결과에 "원이 얼마나 잘 맞았는지"를 숫자(µm)로 함께 보여줍니다.
  </what-built>
  <how-to-verify>
  1) Tray 정렬 화면을 엽니다. Grab 으로 영상을 하나 띄운 뒤 [이미지 저장] 을 누릅니다.
     → 파일 저장 창이 뜨고, 기본 파일명이 TrayAlign_(날짜)_(시간).bmp 로 채워져 있어야 합니다.
     저장하면 아래 상태 글에 "저장 완료: 파일명" 이 나와야 합니다.
  2) 아무 영상도 안 띄운 상태에서 [이미지 저장] 을 눌러 봅니다.
     → 프로그램이 죽지 않고 "저장할 이미지가 없습니다..." 안내만 나와야 합니다.
  3) Bottom 정렬 화면에서도 1)~2) 를 똑같이 해 봅니다. Tray 와 동일하게 동작해야 합니다.
  4) 피커 캘리브레이션용 회전 영상들을 여러 장 저장해 한 폴더에 모읍니다.
  5) Bottom 화면에서 [폴더 열기] 로 그 폴더를 엽니다. 첫 이미지가 화면에 보입니다.
  6) 검색 ROI 를 그리고 [Cal 모델 티칭] 을 누릅니다.
     → 카메라를 찍지 않고 화면에 보이는 그 영상으로 티칭돼야 합니다("모델 티칭 완료").
  7) [스텝 추가] 를 반복해서 누릅니다.
     → 한 번 누를 때마다 누적 숫자가 올라가고 자동으로 다음 이미지로 넘어가야 합니다.
     → 마지막 이미지에서는 "마지막 이미지 도달" 이 나와야 합니다.
  8) 6장 이상 쌓은 뒤 [계산] 을 누릅니다.
     → 저장 확인 창에 피커센터 값과 함께 "피팅잔차 RMS ...µm · 최대 ...µm" 이 보여야 합니다.
     → 예/아니오 어느 쪽을 눌러도 아래 상태 글에 기호(●/▲/✕)와 잔차 값이 함께 남아야 합니다.
     → 잔차가 크게 나와도 계산 자체는 실패로 처리되지 않아야 합니다.
  9) 폴더를 닫지 말고, 이번엔 Grab 을 눌러 카메라 영상을 화면에 띄운 뒤 [스텝 추가] 를 눌러 봅니다.
     → 이때는 폴더 이미지가 아니라 카메라 라이브 영상으로 스텝이 잡혀야 하고, 자동 넘김은 일어나지 않아야 합니다.
  10) 카메라를 연결하지 않은 상태에서, 폴더도 열지 않고 [스텝 추가] 를 눌러 봅니다.
     → "이미지 없음 — [폴더 열기] 로 영상을 불러오거나 카메라 연결을 확인하세요" 안내만 나오고,
       엉뚱한 테스트 이미지가 누적되면 안 됩니다.
  </how-to-verify>
  <resume-signal>"승인" 이라고 적거나, 이상한 점을 적어 주세요</resume-signal>
</task>

</tasks>

<verification>
- WPF_Example/DatumMeasurement.csproj 를 열거나 수정하거나 스테이징하지 않음 (사용자 미커밋 변경 보존)
- 신규 .cs 파일 0개 (csproj 등록 불필요)
- Debug|x64 빌드 error CS 0건
- 두 뷰의 이미지 저장 동작이 동일(파일명 접두어와 Style 리소스만 다름)
- BottomVisionView.xaml.cs 의 캘리브 경로에 SIMUL_MODE 전처리기 잔존 0건
  (GrabButton_Click / GrabEthernetCalibrationImage / OpenFolderButton_Click 의 것은 의도적 잔존)
- PickerCenterCalibrationService 의 성공/실패 계약 무변경 — 반경 가드 유지, 잔차 기반 실패 분기 0건
- TCP $ALIGN_CALIB 응답 필드(IsPass/StepNo) 및 저장 시점 무변경
- CLAUDE.md 하드룰 위반 신규 0건
</verification>

<success_criteria>
- Tray/Bottom 어느 화면에서든 현재 보이는 정렬 영상을 bmp 로 저장할 수 있다
- 실HW 빌드에서 저장한 영상을 폴더로 불러와 피커 캘리브 티칭·스텝·계산을 끝까지 수행할 수 있다
- 폴더 영상이 없으면 라이브 grab, 카메라도 없으면 안내 후 차단 — 폴백 정지 이미지가 조용히 누적되지 않는다
- 피커센터 계산 결과에 피팅 잔차가 µm/px 로 표시되고 기호 등급이 붙는다
- SIMUL 빌드 기존 캘리브 흐름 회귀 0
</success_criteria>

<output>
Create `.planning/quick/260901-mc1-tray-bottom-align-ui/260901-mc1-SUMMARY.md` when done
</output>
