---
phase: quick-260902-ov6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Device/Camera/Mil/MilCamera.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
  - WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
autonomous: true
requirements: [QF-OV6-01, QF-OV6-02, QF-OV6-03]
must_haves:
  truths:
    - "QF-OV6-01: 라이브를 껐다 켜도(카메라 창 닫기 → 다시 열기) 현재 트리 선택의 MirrorX/Y 방향이 유지된다 — 트리를 다시 클릭할 필요가 없다"
    - "QF-OV6-02: Datum 노드가 선택된 상태에서 PropertyGrid 로 MirrorX/MirrorY 를 토글하면 그 값이 라이브 역할에 즉시 재적용된다"
    - "QF-OV6-02: 트리 선택이 다른 노드로 바뀌면 이전 DatumConfig 의 PropertyChanged 구독이 반드시 해제된다(누수·오발동 0)"
    - "QF-OV6-02: PropertyName 화이트리스트(MirrorX/MirrorY)로만 재적용이 통과해 무한루프·과호출이 없다"
    - "라이브가 꺼져 있을 때 재적용이 호출돼도 무해하다(필드 갱신만, 예외 없음)"
    - "미러 미설정 노드/기타 노드 선택 시 기존 무미러 복귀 경로(ApplyLiveMirror(null,false,false))가 회귀 없이 동작한다"
    - "자동검사 사이클의 단발 grab 방향 경로(Action_FAIMeasurement, MainView.ResolveGrabRoleIdentifier)는 한 줄도 바뀌지 않는다"
    - "QF-OV6-03: MirrorX/MirrorY 사용자 안내 문구가 실제 동작과 어긋나지 않는다"
    - "Debug|x64 빌드 error CS 0건"
  artifacts:
    - "WPF_Example/Device/Camera/Mil/MilCamera.cs — _szLiveRoleIdentifier 필드 + StartStream 역할 복원"
    - "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs — _liveMirrorWatchedDatum 필드 + OnLiveMirrorDatumPropertyChanged 핸들러 + ApplyLiveMirrorForNode 구독 swap"
    - "WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs — MirrorX/MirrorY 안내 문구 정정"
  key_links:
    - "SetLiveGrabRole → _szLiveRoleIdentifier 보존 → StartStream 재해석 → LiveLoop 이 읽는 _liveRoleInfo"
    - "DatumConfig.MirrorX/Y setter RaisePropertyChanged → OnLiveMirrorDatumPropertyChanged → ApplyLiveMirrorForNode → ViewModel.ApplyLiveMirror → DeviceHandler.ApplyLiveGrabRole → MilCamera.SetLiveGrabRole"
    - "ApplyLiveMirrorForNode 단일 choke point 에서 구독 swap — 선택 변경마다 해제가 보장되는 유일한 지점"
---

<objective>
라이브 미러가 Datum 선택/미러값 변경을 따라오지 않는 결함 2건을 수정한다.

- 결함 ① `MilCamera.StopStream()` 이 `_liveRoleInfo` 를 지워, 라이브 → 정지 → 라이브 재시작 시 트리 선택이 그대로여도 미러가 풀린다.
- 결함 ② 미러 적용 트리거가 "트리 선택 변경" 단 하나라, 선택 중인 Datum 의 MirrorX/Y 를 PropertyGrid 로 바꿔도 재적용 호출이 없다.

Purpose: 작업자가 Datum 별 촬영 방향을 라이브로 확인할 때, 창을 다시 열거나 트리를 다시 클릭하거나 프로그램을 재시작하지 않아도 되게 한다.
Output: 기존 파일 3개 수정. 신규 `.cs` 파일 없음(`.csproj` 를 건드릴 수 없으므로 필수 제약).
</objective>

<blocking_discovery>
## 조사 중 확인된 구조적 제약 — 실행 전 반드시 읽을 것

저장소 전체를 확인한 결과 **MIL 라이브 영상의 유일한 표시 경로는 `DeviceSelector` 창** 하나뿐이다.
근거(전수 grep):
- `GuiReadyForDisplay` 구독자 = `WPF_Example/UI/Device/DeviceSelector.xaml.cs` 단독(약 :181, :174, :289)
- `StartStream()` 호출부 = 같은 파일 3곳(약 :183, :274, :299) 단독. `MainView`/`InspectionListView` 에는 라이브 진입점이 없다
- `MainView.xaml.cs` 에는 라이브 프레임을 폴링하는 경로가 없다(`GetPreviewBitmapSource`/`LastGrabHalconImage` 참조 0건)

그리고 이 창은 `MainWindow.PopupView` 의 `EPageType.Camera` 분기(약 :328-349)에서
**`ShowDialog()` 모달 + `WindowState="Maximized"`** 로 열린다.
즉 라이브가 켜져 있는 동안 메인 창의 검사 트리와 PropertyGrid 는 **입력이 차단되고 화면도 가려진다.**

**결과:** 브리프 요구 3의 시나리오 1·2("라이브 ON 상태에서 Datum 을 바꾼다 / MirrorX 를 토글한다")는
현재 UI 구조에서 **조작 자체가 불가능**하며, 이 플랜의 두 수정만으로는 눈으로 확인할 수 없다.
선행 작업 quick-260901-k7a 의 UAT 체크리스트 2~4번도 같은 이유로 수행 불가능한 항목이었고,
사용자가 "실기에서 안 먹는다" 고 보고한 근본 원인일 가능성이 높다.

**이 플랜은 결함 ①②를 정확히 고치되, 모달 문제는 건드리지 않는다.**
비모달 전환은 별건이며 사용자 판단이 필요하다 — `<followup_decision_required>` 참조.
결함 ①②는 모달 워크플로우(선택 → 창 열기 → 관찰 → 창 닫기 → 선택 변경 → 다시 열기)에서도
실제로 관찰되는 버그이므로, 비모달 여부와 무관하게 고칠 가치가 있다.
</blocking_discovery>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/quick/260901-k7a-mil-x-y/260901-k7a-SUMMARY.md

@WPF_Example/Device/Camera/Mil/MilCamera.cs
@WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
@WPF_Example/UI/ControlItem/InspectionListViewModel.cs
@WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs
</context>

<hard_rules>
CLAUDE.md 의 MANDATORY 가독성 규칙을 신규/수정 라인 전부에 적용한다. 위반은 회귀로 간주한다.

- 조건 연산자 금지: 삼항, null 병합, null 조건 전부 금지 → 명시적 if/else 분기로만 작성
- C# 8.0 switch 식 금지, C# 7.2 문법만
- 한 줄짜리 분기라도 중괄호 생략 금지, 가드 절(early return)로 중첩 낮추기
- 헝가리언 접두사: b(bool) n(int) sz(string) d(double) hv(HTuple)
- 매직넘버 금지 → 이름 있는 const
- 날짜 서명 주석 신규 생성 금지. 비자명한 "왜" 만 최소한으로
- 파일별 기존 brace 스타일 유지: `MilCamera.cs` / `InspectionListView.xaml.cs` / `DatumConfig.cs` 는 전부 K&R(여는 중괄호를 선언과 같은 줄에)
- MVVM: 미러 계산 로직은 이미 `InspectionListViewModel` 에 있다. code-behind 에는 배선만 추가한다. 기존 비대한 code-behind 리팩토링 금지

절대 건드리지 말 것:
- `WPF_Example/DatumMeasurement.csproj` — 사용자의 미커밋 실HW 세팅이 들어 있다. **스테이징/커밋 절대 금지.** 이 제약 때문에 **신규 `.cs` 파일 생성 불가**
- `WPF_Example/UI/Device/DeviceSelector.xaml.cs`, `WPF_Example/MainWindow.xaml.cs` — 모달/비모달 전환은 이번 범위 밖
- `WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs`, `WPF_Example/UI/ContentItem/MainView.xaml.cs` — 자동검사 사이클 단발 grab 경로. 이번 작업은 라이브 미리보기 표시 전용
- `WPF_Example/Device/Camera/Hik/HikCamera.cs`, `WPF_Example/Device/Camera/Basler/BaslerCamera.cs`

**모든 라인번호는 편집 전 반드시 재확인할 것.** 아래 번호는 2026-09-02 조사 시점 기준이며 drift 가능하다.

**grep 게이트 주의(과거 사고 이력):** 대상 파일들에는 이번 작업 이전부터 있던 삼항·null 조건·날짜 주석이 이미 존재한다.
검증 grep 은 전부 **diff 로 추가된 라인만** 대상으로 한다. 파일 전체 grep 은 반드시 거짓 실패한다.
**숫자를 맞추려고 기존 코드를 지우지 말 것.**
</hard_rules>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: MilCamera — 라이브 역할 식별자를 정지/재시작 사이에 보존 (결함 ①)</name>
  <files>WPF_Example/Device/Camera/Mil/MilCamera.cs</files>
  <read_first>
편집 전 아래를 실측 확인한다(라인번호 drift 주의):
- 약 :20-25 필드 선언부 — `_roleInfoMap`, `_liveRoleInfo` 가 `#if !SIMUL_MODE`(약 :27) **바깥**에 선언돼 있다
- 약 :57-76 `RegisterRoleInfo` / `ResolveRoleInfo`
- 약 :81-87 `SetLiveGrabRole` override
- 약 :453 `#if !SIMUL_MODE` ~ 약 :550 `#endif` — `StartStream` / `StopStream` / `LiveLoop` 이 전부 이 블록 안에 있다
- 약 :462 `StartStream()`, 약 :489 `StopStream()`, 약 :506 `_liveRoleInfo` 초기화 줄, 약 :518 `LiveLoop` 의 `_liveRoleInfo` 읽기
  </read_first>
  <action>
**설계 결정과 근거 (QF-OV6-01).** 브리프가 우선 검토를 요구한 "라이브가 시작될 때 현재 선택을 다시 밀어넣기" 를 채택하되, UI 배선이 아니라 **장치 계층 내부에서** 해결한다. 근거 2가지:
(a) 라이브 시작 진입점을 추적한 결과 `StartStream()` 호출부는 `DeviceSelector.xaml.cs` 3곳뿐이고, 그 파일은 이번 작업의 금지 파일이다.
(b) `MilCamera`(Device 계층)가 검사 트리(UI 계층) 상태를 되묻는 것은 계층 역전이다.
따라서 "트리가 원하는 역할" 과 "스트리밍 중에만 의미 있는 유효 역할" 을 **두 필드로 분리**한다. 그러면 UI 배선 없이 장치 계층 안에서 "역할 = 현재 트리 선택" 불변식이 유지된다.
`StopStream()` 의 방어적 초기화는 브리프 선호대로 **그대로 남긴다**(유효 역할만 비운다).

**구현 4단계.**

1. **필드 추가.** `_liveRoleInfo` 선언 바로 아래(`#if !SIMUL_MODE` 바깥)에 `private string _szLiveRoleIdentifier = null;` 을 추가한다. 역할은 "트리가 마지막으로 지정한 원하는 역할 식별자이며 라이브 정지/재시작을 넘어 살아남는다". 두 필드가 왜 따로 존재하는지를 짧은 주석으로 남긴다.

2. **`SetLiveGrabRole(string szRoleIdentifier)` 수정.**
   - 빈 값 분기에서 `_szLiveRoleIdentifier` 와 `_liveRoleInfo` 를 **둘 다** 되돌린다. 미러 무관 노드 선택 시 무미러로 복귀하는 기존 경로가 이 분기이므로, 여기서 식별자까지 지워야 불변식이 유지된다.
   - 정상 분기에서는 `_szLiveRoleIdentifier` 에 인자를 **먼저 저장**한 뒤, `ResolveRoleInfo` 에 인자가 아니라 **저장된 필드를 넘겨** `_liveRoleInfo` 를 채운다. 필드를 경유하는 이유 2가지를 주석으로 남긴다: 두 필드가 절대 어긋나지 않음이 한눈에 보이고, SIMUL_MODE 빌드에서는 `StartStream` 이 통째로 컴파일 제외되므로 이 읽기가 없으면 "대입만 되고 읽히지 않는 필드" 경고가 새로 생긴다.

3. **`StartStream()` 에 역할 복원 추가.** 이른 return 가드(이미 스트리밍 중 / 카메라 미개방) **뒤**, `_liveThread.Start()` **앞** 지점에서, `_szLiveRoleIdentifier` 가 비어 있지 않으면 `ResolveRoleInfo` 로 `_liveRoleInfo` 를 다시 채운다. 명시적 if 문과 중괄호로 작성한다. 왜 필요한지(정지 시 유효 역할을 비우므로 재시작 시 마지막 트리 선택을 다시 해석해야 한다. 그러지 않으면 트리 선택이 그대로여도 무미러로 돌아간다)를 주석으로 남긴다.

4. **`StopStream()` 주석 갱신.** `_liveRoleInfo` 초기화 줄 옆의 기존 주석은 이 변경 이후 사실과 달라진다. 새 사실("유효 역할만 비우고 원하는 역할 식별자는 남겨 두어 재시작 시 `StartStream` 이 복원한다")로 바꾼다. 초기화 코드 자체는 그대로 둔다.

`LiveLoop` 은 손대지 않는다 — 이미 매 프레임 `_liveRoleInfo` 를 한 번만 읽고 비어 있으면 `Info` 로 폴백하는 올바른 구조다.
`RegisterRoleInfo` / `ResolveRoleInfo` / `GrabHalconImage`(단발 grab) 도 손대지 않는다.
  </action>
  <verify>
    <automated>cd /c/code/DataMeasurement; ADD=$(git diff -U0 -- WPF_Example/Device/Camera/Mil/MilCamera.cs | grep '^+' | grep -v '^+++'); echo "TERNARY=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:')"; echo "NULLCOALESCE=$(printf '%s\n' "$ADD" | grep -cF '??')"; echo "NULLCOND=$(printf '%s\n' "$ADD" | grep -cF '?.')"; echo "SWITCHEXPR=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>')"; echo "DATESIG=$(printf '%s\n' "$ADD" | grep -cF 'hbk')"; echo "IDENT_USES=$(grep -c '_szLiveRoleIdentifier' WPF_Example/Device/Camera/Mil/MilCamera.cs)"; echo "IDENT_CLEARS=$(grep -c '_szLiveRoleIdentifier = null' WPF_Example/Device/Camera/Mil/MilCamera.cs)"; echo "INFO_CLEARS=$(grep -c '_liveRoleInfo = null' WPF_Example/Device/Camera/Mil/MilCamera.cs)"</automated>
    <expected>TERNARY / NULLCOALESCE / NULLCOND / SWITCHEXPR / DATESIG 는 전부 0 (추가된 라인만 대상). IDENT_USES 는 5 이상. IDENT_CLEARS 는 정확히 1 — 식별자를 지우는 곳은 `SetLiveGrabRole` 의 빈 값 분기 한 곳뿐이어야 한다. `StopStream` 에서도 지우면 2가 되고 수정이 무효가 된다. INFO_CLEARS 는 2 이상(`SetLiveGrabRole` 과 `StopStream`).</expected>
    <automated>cd /c/code/DataMeasurement; "/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal -nologo 2>&1 | grep -c "error CS"</automated>
    <expected>0. MSBuild 인자는 반드시 대시 형식(-p:)을 쓴다 — Bash(MSYS)에서 슬래시 형식은 경로 변환으로 깨진다.</expected>
  </verify>
  <done>
`_szLiveRoleIdentifier` 가 라이브 정지/재시작을 넘어 보존되고, `StartStream()` 이 이를 다시 해석해 `_liveRoleInfo` 를 복원한다. 식별자를 지우는 곳은 `SetLiveGrabRole` 의 빈 값 분기 하나뿐이다. `StopStream()` 은 유효 역할만 비우며 주석이 새 동작을 정확히 설명한다. `LiveLoop`/`RegisterRoleInfo`/`ResolveRoleInfo`/단발 grab 경로 무수정. Debug|x64 error CS 0건.
  </done>
  <reversibility rating="reversible">필드 하나 추가와 3곳 배선. 되돌리면 정확히 이전 동작으로 복귀한다.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 선택 중인 Datum 의 MirrorX/Y 변경 시 라이브 즉시 재적용 (결함 ②)</name>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <read_first>
편집 전 아래를 실측 확인한다:
- 약 :1344 `ApplyLiveMirrorForNode(object itemParam)` — 이 메서드의 **유일한** 호출부는 약 :885 `InspectionList_SelectionChanged` 안의 `Dispatcher.BeginInvoke(Background)` 블록이다. 즉 선택이 바뀔 때마다 반드시 한 번 지나가는 choke point 다
- 같은 파일 약 :1332 `ResolveDatumCameraParam` — `treeListBox_sequence.SelectedItem`(트리 UI 상태)에 의존한다. 재적용 시점에도 선택이 그대로여야 올바른 DeviceName 이 나온다
- 이 파일의 `using` 목록(약 :2-16)에 `System.ComponentModel` 이 **없다** — 아래 선례와 동일하게 정규화된 이름을 쓴다(using 추가 금지, 부수효과 회피)
- 구독/해제 선례: `WPF_Example/UI/ContentItem/MainView.xaml.cs` 약 :2317-2324 `OnSelectedDatumPropertyChanged`(PropertyName 화이트리스트)와 약 :2418-2422 `PublishDatumRoiCandidates` 안의 구독 swap
- `WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs` 약 :239-261 — `MirrorX`/`MirrorY` setter 가 값이 실제로 바뀔 때만 `RaisePropertyChanged` 를 발화한다(같은 값 재저장은 발화하지 않는다)

**ShotConfig 조사 결과 — 구독 대상에서 제외한다(브리프 요구 2에 대한 답).**
`ShotConfig` 에는 `MirrorX`/`MirrorY` 속성 자체가 없다(`ShotConfig.cs` grep 0건).
Shot 의 미러는 `InspectionSequence.ResolveShotGrabMirror`(약 :2282)가 그 Shot 이 참조하는 `DatumConfig` 들에서 **파생**시킨다.
따라서 (a) `ShotConfig` 는 미러 관련 PropertyChanged 를 발화할 수 없고, (b) Shot 노드가 선택돼 있으면 PropertyGrid 에 미러 필드가 없어 미러값 변경 자체가 일어날 수 없다.
결론: **구독은 `DatumConfig` 에만 건다.** `ShotConfig` 에 미러 속성을 새로 추가하지 않는다(레시피 INI 스키마 변경은 범위 밖).
  </read_first>
  <action>
**QF-OV6-02.** 선택 중인 `DatumConfig` 의 미러값이 바뀌면 기존 `ApplyLiveMirrorForNode` 를 재호출한다. 미러 **계산**은 이미 `InspectionListViewModel.ApplyLiveMirror` / `ResolveShotLiveMirror` 에 있으므로 새 계산 로직을 만들지 않는다. code-behind 에는 구독 배선과 화이트리스트 게이트만 추가한다(CLAUDE.md MVVM 규칙: 이벤트 배선은 code-behind, 로직은 ViewModel).

1. **필드 추가.** 클래스 필드 영역(약 :23-44 다른 private 필드들 옆)에 `private DatumConfig _liveMirrorWatchedDatum = null;` 을 추가한다. 용도(라이브 미러 재적용을 위해 현재 구독 중인 Datum. 선택이 바뀌면 반드시 해제한다)를 짧게 주석으로 남긴다.

2. **구독 swap 을 `ApplyLiveMirrorForNode` 최상단(기존 try 블록 안 첫 부분)에 넣는다.** 이 메서드가 선택 변경마다 반드시 지나가는 유일한 지점이므로, 여기에 두면 해제 누락이 구조적으로 불가능하다.
   - 인자를 `DatumConfig` 로 캐스팅한 결과를 지역 변수에 담는다(Datum 이 아니면 비게 된다).
   - 그 지역 변수가 `_liveMirrorWatchedDatum` 과 **같은 참조가 아닐 때만** swap 한다: 기존 값이 비어 있지 않으면 핸들러를 `-=` 로 해제하고, 필드를 새 값으로 바꾸고, 새 값이 비어 있지 않으면 `+=` 로 구독한다. 참조 비교는 `object.ReferenceEquals` 를 쓴다.
   - 같은 참조일 때 swap 을 건너뛰는 이유: 핸들러가 이 메서드를 재호출하므로(3번), 매번 해제/재구독하면 이벤트 발화 도중 구독 리스트를 흔들게 된다.
   - Datum 이 아닌 노드(Shot/기타)를 선택하면 지역 변수가 비어 이전 구독이 해제되고 새 구독은 걸리지 않는다 — 요구 (a) 충족.

3. **핸들러 추가.** `ApplyLiveMirrorForNode` 바로 아래에 `private void OnLiveMirrorDatumPropertyChanged(object sender, System.ComponentModel.PropertyChangedEventArgs e)` 를 추가한다. 가드 절만으로 구성하고 중첩을 만들지 않는다:
   - 이벤트 인자가 비어 있으면 return.
   - **PropertyName 화이트리스트**: 이름 있는 bool 변수(예: `bMirrorProperty`)를 false 로 두고, `nameof(DatumConfig.MirrorX)` 와 일치하면 true, `nameof(DatumConfig.MirrorY)` 와 일치하면 true 로 각각 **별도 if 문**으로 세운 뒤, false 면 return 한다. 이 게이트가 요구 (b) 무한루프·과호출 차단이다. `MainView.OnSelectedDatumPropertyChanged` 와 달리 **빈 문자열(대량 갱신)은 통과시키지 않는다** — 대량 갱신마다 장치 역할을 다시 밀어 넣을 이유가 없다.
   - `sender` 를 `DatumConfig` 로 캐스팅해 비어 있으면 return.
   - **stale 객체 방어**: `treeListBox_sequence.SelectedItem` 을 `NodeViewModel` 로 캐스팅해 비어 있으면 return 하고, 그 노드의 `Param` 이 방금 바뀐 Datum 과 같은 참조가 아니면 return 한다. 트리 재구축 등으로 더 이상 선택돼 있지 않은 옛 객체의 이벤트가 잘못된 미러를 적용하는 것을 막고, `ResolveDatumCameraParam` 이 트리 선택에 의존한다는 전제도 보장한다.
   - 여기까지 통과하면 `ApplyLiveMirrorForNode` 에 그 Datum 을 넘겨 호출한다.
   - 화이트리스트와 stale 방어가 왜 필요한지를 주석 2~3줄로 남긴다.

4. **요구 (c) 재확인(코드 추가 없음).** 재호출 경로는 `ApplyLiveMirrorForNode` → `ViewModel.ApplyLiveMirror` → `DeviceHandler.ApplyLiveGrabRole` → `MilCamera.SetLiveGrabRole` 이며, 전부 필드 갱신과 사전 등록된 역할 조회뿐이고 `DatumConfig` 속성에 쓰지 않는다. 따라서 재진입 루프가 없고, 라이브가 꺼져 있어도 무해하다(다음 라이브 시작 시 Task 1 의 복원 경로로 반영). 실행자는 이 사실을 코드로 확인만 하고 방어 코드를 추가로 넣지 않는다.

`InspectionList_SelectionChanged`(약 :885)의 기존 `ApplyLiveMirrorForNode(itemParam);` 호출은 **그대로 둔다.** `ResolveDatumCameraParam` 도 리팩토링하지 않는다(기존 code-behind 리팩토링 금지).
  </action>
  <verify>
    <automated>cd /c/code/DataMeasurement; ADD=$(git diff -U0 -- WPF_Example/UI/ControlItem/InspectionListView.xaml.cs | grep '^+' | grep -v '^+++'); echo "TERNARY=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:')"; echo "NULLCOALESCE=$(printf '%s\n' "$ADD" | grep -cF '??')"; echo "NULLCOND=$(printf '%s\n' "$ADD" | grep -cF '?.')"; echo "SWITCHEXPR=$(printf '%s\n' "$ADD" | grep -cE 'switch.*=>')"; echo "DATESIG=$(printf '%s\n' "$ADD" | grep -cF 'hbk')"; echo "SUBSCRIBE=$(grep -c 'PropertyChanged += OnLiveMirrorDatumPropertyChanged' WPF_Example/UI/ControlItem/InspectionListView.xaml.cs)"; echo "UNSUBSCRIBE=$(grep -c 'PropertyChanged -= OnLiveMirrorDatumPropertyChanged' WPF_Example/UI/ControlItem/InspectionListView.xaml.cs)"; echo "WHITELIST=$(grep -c 'nameof(DatumConfig.Mirror' WPF_Example/UI/ControlItem/InspectionListView.xaml.cs)"</automated>
    <expected>TERNARY / NULLCOALESCE / NULLCOND / SWITCHEXPR / DATESIG 는 전부 0 (**추가된 라인만** 대상. 이 파일에는 기존 삼항 4건, 기존 null 조건 1건, 기존 날짜 주석 다수가 있어 파일 전체 grep 은 반드시 거짓 실패한다). SUBSCRIBE 는 1, UNSUBSCRIBE 는 1 — 구독과 해제가 정확히 짝을 이룬다. WHITELIST 는 2(MirrorX/MirrorY).</expected>
    <automated>cd /c/code/DataMeasurement; git status --short -- WPF_Example/UI/Device/DeviceSelector.xaml.cs WPF_Example/MainWindow.xaml.cs WPF_Example/UI/ContentItem/MainView.xaml.cs WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs WPF_Example/Device/Camera/Hik/HikCamera.cs WPF_Example/Device/Camera/Basler/BaslerCamera.cs | wc -l</automated>
    <expected>0 — 금지 파일 무변경. `WPF_Example/DatumMeasurement.csproj` 는 사용자의 미커밋 변경이므로 이 목록에 넣지 않으며, 절대 스테이징하지 않는다.</expected>
  </verify>
  <done>
`_liveMirrorWatchedDatum` 구독이 `ApplyLiveMirrorForNode` 단일 지점에서 swap 되어 선택 변경마다 이전 구독이 해제된다. `OnLiveMirrorDatumPropertyChanged` 가 MirrorX/MirrorY 만 통과시키고 현재 트리 선택과 일치하는 경우에만 재적용한다. `ShotConfig` 구독 없음. 금지 파일 무변경.
  </done>
  <reversibility rating="reversible">필드 1개 + 핸들러 1개 + 구독 swap 블록. 되돌리면 트리 선택 트리거만 남는 이전 동작으로 복귀한다.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 미러 안내 문구 정정 + 최종 빌드 게이트</name>
  <files>WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs</files>
  <precondition>MSBuild 이 `C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe` 에 존재한다(2026-09-02 확인됨). 없으면 중단하고 보고한다.</precondition>
  <read_first>
**문구를 고치기 전에 "재시작 불필요" 주장이 실제로 맞는지 코드로 확인한다:**
- `WPF_Example/Custom/Device/DeviceHandler.cs` 약 :128-152 `BuildGrabRoleIdentifier` 와 `BuildMirrorRoleInfos` — 미러 4조합 역할이 **앱 시작 시 정적으로 전부 등록**되는지 확인한다(주석에 레시피 스캔 없이 4가지를 전부 등록한다고 명시돼 있어야 한다)
- `WPF_Example/Device/DeviceHandler.cs` 약 :246-251 — `BuildMirrorRoleInfos` 결과가 `RegisterRoleInfo` 로 `_roleInfoMap` 에 들어가는지 확인
- `WPF_Example/Custom/Sequence/Inspection/Action_FAIMeasurement.cs` 약 :467, :978 — 역할 식별자를 **grab 시점에** 현재 DatumConfig 값으로 조립하는지 확인(파일은 읽기만 하고 수정 금지)

세 가지가 모두 성립하면 미러 변경은 다음 촬영부터 즉시 반영되며 재시작이 필요 없다.
**하나라도 성립하지 않으면 이 Task 의 문구 수정을 건너뛰고, 확인 결과와 사유를 SUMMARY 에 남긴 뒤 빌드 게이트만 수행한다.**
  </read_first>
  <action>
**QF-OV6-03.** `DatumConfig.cs` 의 미러 관련 사용자 안내 문구가 이번 변경 이후 사실과 어긋나므로 정정한다. 안내는 초보 작업자용 안전 경고이므로 **안전 경고 내용은 한 글자도 약화시키지 않고**, 사실과 다른 "재시작" 주장만 고친다.

1. 약 :237-238 `MirrorX` 와 약 :251-252 `MirrorY` 의 `[System.ComponentModel.Description]` 속성 문자열에서 프로그램 재시작이 필요하다는 마지막 문장을, 다음 촬영부터 바로 적용된다는 취지의 문장으로 교체한다. 문장 앞부분(무엇을 하는 설정인지, 기본 꺼짐)은 그대로 둔다.

2. 약 :268-280 `WarnMirrorChanged` 의 `message` 문자열에서 **3번 항목만** 교체한다. 1번(촬영 방향 자체가 바뀐다)과 2번(같은 카메라의 다른 검사 항목까지 틀어질 수 있다)은 **원문 그대로 유지한다** — 이 둘이 이 경고의 존재 이유다. 3번은 재시작 요구 대신 "다음 촬영부터 바로 적용되므로, 바꾼 뒤에는 다른 항목들을 곧바로 다시 확인하라" 는 취지로 바꾼다.

3. `MirrorX`/`MirrorY` setter 로직, `_suppressMirrorWarning` 게이트, `RaisePropertyChanged` 호출, `CustomMessageBox.Show` 의 인자 구성은 **일절 건드리지 않는다** — Task 2 의 재적용 트리거가 이 `RaisePropertyChanged` 에 의존한다.

문자열만 바꾸는 작업이므로 새 제어 흐름을 만들지 않는다.

**마지막으로 전체 빌드 게이트를 수행한다**(아래 verify).
  </action>
  <verify>
    <automated>cd /c/code/DataMeasurement; ADD=$(git diff -U0 -- WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs | grep '^+' | grep -v '^+++'); echo "TERNARY=$(printf '%s\n' "$ADD" | grep -cE '\?[^?]*:')"; echo "NULLCOALESCE=$(printf '%s\n' "$ADD" | grep -cF '??')"; echo "NULLCOND=$(printf '%s\n' "$ADD" | grep -cF '?.')"; echo "DATESIG=$(printf '%s\n' "$ADD" | grep -cF 'hbk')"; echo "RESTART_CLAIM=$(grep -c '다시 실행해야' WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs)"; echo "RAISE_INTACT=$(grep -c 'RaisePropertyChanged(nameof(Mirror' WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs)"; echo "WARN_INTACT=$(grep -c 'WarnMirrorChanged' WPF_Example/Custom/Sequence/Inspection/DatumConfig.cs)"</automated>
    <expected>TERNARY / NULLCOALESCE / NULLCOND / DATESIG 는 전부 0 (추가된 라인만 대상). RESTART_CLAIM 은 0. RAISE_INTACT 는 2(MirrorX/MirrorY setter 의 발화가 그대로 살아 있어야 Task 2 가 동작한다). WARN_INTACT 는 3 이상(선언 1 + 호출 2).</expected>
    <automated>cd /c/code/DataMeasurement; "/c/Program Files/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" WPF_Example/DatumMeasurement.csproj -p:Configuration=Debug -p:Platform=x64 -v:minimal -nologo 2>&1 | tee /dev/stderr | grep -c "error CS"</automated>
    <expected>0. 경고는 기존 baseline(Phase 33 마이그레이션 obsolete 경고 등)이 있으므로 "경고 0" 을 기준으로 삼지 않는다. MSBuild 인자는 대시 형식(-p:)만 쓴다.</expected>
    <automated>cd /c/code/DataMeasurement; git status --short | grep -c "DatumMeasurement.csproj"; git diff --cached --name-only | grep -c "DatumMeasurement.csproj"</automated>
    <expected>첫 줄은 1(사용자의 미커밋 실HW 세팅이 그대로 남아 있음), 둘째 줄은 0(절대 스테이징되지 않았음). 둘째 줄이 0이 아니면 즉시 `git reset HEAD WPF_Example/DatumMeasurement.csproj` 로 언스테이징한다.</expected>
  </verify>
  <done>
미러 안내 문구가 실제 동작과 일치한다(또는 read_first 확인 실패 시 수정을 건너뛴 사유가 기록됐다). 미러 setter 의 `RaisePropertyChanged` 발화와 경고 호출이 그대로 살아 있다. Debug|x64 빌드 error CS 0건. `DatumMeasurement.csproj` 는 미스테이징 상태 그대로다.
  </done>
  <reversibility rating="reversible">사용자 안내 문자열 3곳. 로직 변경 없음.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (신규 없음) | 이번 변경은 프로세스 내부 UI ↔ 장치 계층 필드 갱신뿐이다. 네트워크/인증/파일 파싱 경계를 새로 만들지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ov6-01 | Tampering | `MilCamera._liveRoleInfo` (UI 스레드 쓰기 / LiveLoop 읽기) | low | accept | 참조 대입/읽기는 원자적이고 최악의 경우 한 프레임 지연이다. 기존 설계(quick-260901-k7a)의 무락 참조 스왑 계약을 그대로 유지하며 새 필드도 같은 규약을 따른다 |
| T-ov6-02 | Denial of Service | `OnLiveMirrorDatumPropertyChanged` 재진입 | medium | mitigate | PropertyName 화이트리스트(MirrorX/MirrorY 만)와 참조 동일 시 구독 swap 건너뛰기로 이벤트 폭주·무한루프 차단. 재호출 경로가 `DatumConfig` 속성에 쓰지 않음을 Task 2 4단계에서 코드로 확인 |
| T-ov6-03 | Tampering | 트리 재구축 후 stale `DatumConfig` 이벤트 | medium | mitigate | 핸들러가 `treeListBox_sequence.SelectedItem` 의 `Param` 과 참조 일치를 확인한 뒤에만 적용 |
| T-ov6-04 | Information Disclosure | 이벤트 구독 누수 | low | mitigate | `ApplyLiveMirrorForNode` 단일 choke point 에서 해제 후 재구독. SUBSCRIBE/UNSUBSCRIBE 각 1건 grep 게이트로 확인 |
| T-ov6-SC | Tampering | 패키지 설치 | n/a | n/a | 신규 패키지 설치 없음(npm/pip/cargo 미사용 프로젝트, `packages.config` 무변경) |
</threat_model>

<verification>
1. Task 1~3 의 모든 `<automated>` 게이트 통과
2. 하드룰 grep 은 전부 **diff 추가 라인 한정**으로만 판정 — 파일 전체 grep 금지, 숫자 맞추려 기존 코드 삭제 금지
3. Debug|x64 `error CS` 0건
4. 금지 파일 무변경: `DeviceSelector.xaml.cs`, `MainWindow.xaml.cs`, `MainView.xaml.cs`, `Action_FAIMeasurement.cs`, `HikCamera.cs`, `BaslerCamera.cs`
5. `DatumMeasurement.csproj` 미스테이징·미커밋 확인
6. 커밋은 task 단위로 원자적으로 나눈다
</verification>

<human_verification>
실기 UAT 는 사용자가 별도 진행한다(이 작업 범위 밖, 자동 검증 대상 아님).
SUMMARY 에 아래 4항목을 그대로 옮겨 적을 것.

**현재 UI 구조에서 검증 가능한 항목:**
- U-1 (QF-OV6-01): Datum A 를 선택 → 카메라 창을 열어 라이브 확인(미러 적용됨) → 창을 닫고 **트리를 건드리지 않은 채** 다시 연다 → 미러가 그대로 유지되어야 한다. (수정 전에는 무미러로 풀렸다)
- U-2 (QF-OV6-02): Datum A 선택 상태에서 PropertyGrid 로 MirrorX 를 토글 → 카메라 창을 연다 → **토글한 새 방향**으로 보여야 한다. (수정 전에는 무미러였다)
- U-3: 미러 미설정 Datum 또는 시퀀스 이름 같은 기타 노드를 선택 → 카메라 창을 연다 → 무미러 원본 방향으로 보여야 한다(회귀 없음)
- U-4: 같은 Datum 에서 Grab 버튼으로 한 장 촬영 → 촬영된 사진 방향과 라이브 화면 방향이 일치해야 한다(단발 grab 경로 회귀 없음)

**현재 UI 구조에서 검증 불가능한 항목(모달 제약):**
- 라이브 ON 상태에서 Datum A → B 로 트리 선택을 바꾸며 즉시 전환을 보는 것
- 라이브 ON 상태에서 MirrorX 체크를 토글하며 즉시 반영을 보는 것
→ 카메라 창이 `ShowDialog()` 모달 + Maximized 라 메인 창 조작이 차단된다. `<followup_decision_required>` 참조.
</human_verification>

<followup_decision_required>
## 사용자 판단 필요 — 라이브 창 비모달 전환 (이 플랜 범위 밖)

브리프 요구 3의 시나리오 1·2를 실제로 **보려면** 카메라 창이 비모달이어야 한다.
저장소에 선례가 있다: `MainWindow.xaml.cs` 약 :395 Reviewer 창, 약 :405 Statistics 창이
`Show()` 비모달로 열리며 주석에 "라이브 MainView 와 동시 사용 가능" 이라고 명시돼 있다.

**하지만 단순 치환이 아니다. 최소 4가지가 함께 걸린다:**
1. `DeviceSelector.Btn_ok_Click` / `Btn_cancel_Click` 이 `this.DialogResult` 를 대입한다 — 비모달 창에서 `DialogResult` 대입은 `InvalidOperationException` 을 던진다. 두 핸들러를 반드시 함께 고쳐야 한다
2. `MainWindow` 의 `ShowDialog() == true` 블록이 `SelectedDisplayConfig.DrawScale` 을 `mainView.DrawScale` 로 동기화한다 — 비모달에서는 `Closed` 이벤트 등으로 옮겨야 한다
3. `WindowState="Maximized"` 라 비모달로 바꿔도 메인 창을 덮는다 — 창 크기/배치도 함께 조정해야 실효가 있다
4. **안전 회귀 위험(가장 중요):** 현재 모달 구조는 `Sequences.IsIdle == false` 게이트와 함께 "라이브 중에는 검사를 시작할 수 없다" 를 물리적으로 보장한다. 비모달로 바꾸면 라이브 창을 열어 둔 채 RUN 이 가능해지고, `MilCamera.GrabHalconImage` 는 Streaming 중 `null` 을 반환하도록 설계돼 있어(약 :283, 의도된 안전장치) 전 항목 `NO_IMAGE` NG 가 난다. 별도 게이트 없이는 새 운영 사고 경로가 열린다

**선택지:**
- (A) 이 플랜(①②)만 실행하고 종료 — U-1/U-2 로 수정 효과는 확인 가능하나, "라이브 켠 채로 전환" 은 여전히 불가능
- (B) 이 플랜 실행 후, 비모달 전환을 **별도 quick 태스크**로 진행 — 위 4항목(특히 4번 RUN 게이트)을 설계해 넣어야 한다
- (C) 이 플랜 실행 후, 카메라 창을 여는 시점에 현재 트리 선택을 미리 적용하는 정도로 타협 — Task 1 이 이미 이를 달성하므로 추가 작업 없음

권장: **(A) 실행 → 실기에서 U-1/U-2 확인 → 그래도 부족하면 (B)** 를 별도 태스크로.
(B) 를 이 플랜에 합치지 않은 이유: 금지 파일 2개(`DeviceSelector.xaml.cs`, `MainWindow.xaml.cs`)를 열어야 하고, 4번 안전 회귀가 사용자 결정 사항이기 때문이다.
</followup_decision_required>

<success_criteria>
- [ ] `MilCamera` 가 라이브 정지/재시작을 넘어 트리 지정 역할을 보존하고 시작 시 복원한다 (QF-OV6-01)
- [ ] 선택 중인 Datum 의 MirrorX/MirrorY 변경이 라이브 역할에 즉시 재적용된다 (QF-OV6-02)
- [ ] 선택 변경 시 이전 Datum 구독이 해제되고, 화이트리스트로 과호출이 차단된다
- [ ] `ShotConfig` 는 미러 PropertyChanged 를 발화하지 않음이 확인·기록되고 구독 대상에서 제외됐다
- [ ] 미러 안내 문구가 실제 동작과 일치한다 (QF-OV6-03)
- [ ] 하드룰 grep(diff 추가 라인 한정) 전부 0
- [ ] Debug|x64 빌드 error CS 0건
- [ ] 금지 파일 무변경 · `DatumMeasurement.csproj` 미스테이징
- [ ] SUMMARY 에 U-1~U-4 실기 UAT 항목과 모달 제약(검증 불가 2항목)이 명시됐다
</success_criteria>

<output>
Create `.planning/quick/260902-ov6-datum/260902-ov6-SUMMARY.md` when done.
SUMMARY 에는 `<human_verification>` 의 U-1~U-4 와 "검증 불가능한 항목(모달 제약)" 을 그대로 옮겨 적고,
`<followup_decision_required>` 의 선택지 (A)/(B)/(C) 를 사용자 결정 대기 항목으로 남긴다.
</output>
